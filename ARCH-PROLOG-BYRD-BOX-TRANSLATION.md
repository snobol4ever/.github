# ARCH-PROLOG-BYRD-BOX-TRANSLATION — every Prolog construct as a Byrd box, translated the Proebsting way, variables aligned per box (ceo, 2026-09-02)

**⛔ LON RULING 2026-09-02 (in-chat to ceo, verbatim: *"Keep in mind that R10 and R11 are being used for statement number and BB node ID number."*): `r10` and `r11` are TAKEN — `r10` carries the statement number, `r11` the BB node id (`x86_asm.h` loads `r11` with `_.nid` at every box entry). Every "wire on `r10`" / "ball on `r11`" below is therefore VOID as written: the youngest-choice pointer `B` and the exception ball travel either in a frame slot (`F.B` written by the callee's γ/ω landing code through the frame pointer it already holds; `F.BALL` in the catch frame) or in a wire register hq_C picks from the register plane (`src/ir/zeta_choices.h`, `zeta_storage.h`) that no box entry clobbers. hq_C names the register or the slot in its review FINDING before rung 2 lands; § F question 3 is re-asked accordingly.**

**⭐ LON GRANT 2026-09-02 (in-chat to ceo, verbatim in substance: *"R13, R14, and R15 are available for Prolog use. SNOBOL4 and Icon use those for string scanning. Prolog can use those for all internal transmission requirements. Choose what should be live for the entire run and put those in a register."*) — THE CEO'S CHOICE, the three values every Prolog run reads at every step:**

| register | holds | WAM name | why it is live for the whole run | replaces |
|---|---|---|---|---|
| **`r13`** | **B** — the youngest live choice frame (an RBP-style frame address, or the root frame when no choice is open) | B | read at every binding (is the cell older than B? then log it), at every cut (`B := F.B0`), at every ω (jump to B's β) | `F.B` as a per-frame slot for the caller's view is still written at α/γ/ω landings; the three CP stacks, `g_pl_zf_pending_*`, `g_resolve_cut_flag` |
| **`r12`** | **TR — the trail, Prolog's Conditional-Assignment Stack** (Lon 2026-09-02, second grant: *"register R12 points to a C.A.S and you can also use it for Prolog as you will … it is a stack."* SNOBOL4's C.A.S. is the stack of `pat . var` assignments held pending during a match and dropped on backtrack — the role `rtx_abi.inc:16-21` records as the register's history ("r12 conditional-assignment stack pointer (own mmap area)", marked DEAD HISTORY there: r12 is FREE in today's rtx ABI, no pin, no prologue), which is exactly why Prolog can claim it; Prolog's trail is the same concept: bindings held undoable until the choice that made them is cut or fails) | TR | every conditional binding pushes `{addr, old}` at `[r12]` and bumps `r12`; ω pops down to the mark the failing choice recorded at its creation (`F.TRMARK := r12` at α of a multi-clause box); cut needs NO log promotion — entries above the surviving choice's mark simply stay, exactly as in the WAM, which retires § F question 1 (bind-time placement) and question 2 (log overflow: the trail area is one mmap arena that grows like a C.A.S. area — whether a live SNOBOL4 C.A.S. allocator still exists to share is hq_C's first check; if not, the arena is Prolog's own, and it is not a global because its only handle is `r12`) | `g_pl_trail` (area + `top`), `g_resolve_mark_stack/_top`, the trail-mark integers of `rt_pl_cp_push3`, and the per-frame `F.LOG` of § A.1 (replaced by one 8-byte `F.TRMARK`) |
| **`r14`** | **ROOT — the ζ-STANDING record** of the running program: dynamic database, `nb_setval` store, settable flags, the proc table (`name, α, β, arity`) | (none; the WAM keeps these in code space and the heap) | constant for the whole run; `call/N`, `assert/retract`, `nb_getval`, `current_prolog_flag` reach their table at a fixed offset from `r14` with no RIP-relative data and no global — § F question 4 is thereby ANSWERED: the third zeta is literally a register | `g_pl_dyn_pred_table/_n/_cap`, `g_rt_pl_nb/_n`, `g_pl_flags[]`, `g_pl_pred_table/_n`, `g_plw_preds/_n` |
| **`r15`** | **the ball, or 0** — the exception in flight during ω-with-ball propagation | (none; SWI's exception port) | every ω transition is defined over it (0 in the common case); a thrower sets it, every intermediate ω passes it untouched, only a catch box's β-handler tests it and clears it | `g_pl_throw_ball` and its nine polling sites; § F question 3 is thereby ANSWERED: the ball rides `r15`, no `F.BALL` slot |

**`r9` IS NOT A PROLOG GLOBAL-ARRAY BASE (Lon's question 2026-09-02, measured on `nrev.pl` at SCRIP `2748100d`):** `r9` is the RTCC ABI's GVA register (`RTCC_GVA_REG`, reloaded from `rtccb+48`, `x86_asm.h:347-374`) and SNOBOL4 graphs address their variables through it (`GVARQ`, 8 distinct cells in `arith_loop`). A Prolog graph addresses NO GVA cell: its variables are frame cells (`F.A`/`F.G`), and the only `[r9+off]` it emits are `+0/+4/+8` — the fields of a DESCR whose address a `call_prolog` α box has just put in `r9` as a scratch pointer (`lea r9,[rbp+…]`/`mov r9,rax`, 16 template sites, 47 boxes in `nrev`). Because the ABI treats `r9` as the GVA base for every language, each such clobber is followed by `mov r9, [rip+rtccb+48]`: **244 dead reloads per `nrev` compile.** Consequences: the ROOT record on `r14` is Prolog's only "global array" and the GVA stays SNOBOL4's; rung 2.0 re-homes the `call_prolog` scratch off `r9` as well as off `r12` (DONE-WHEN adds: `grep -c 'rtccb+48'` in a compiled Prolog `.s` = the graph-entry seeds only, not one per box), and hq_P measures the reload cost before/after on the van Roy set.

Rules that come with the grant: (1) `r13`–`r15` are callee-saved under SysV, so every C or rtx helper preserves them for free and `rtx_plunify.s` reads `r13`/`r14` directly; a C helper that must see B or TR gets them as arguments until the asm unify is the only unify. (2) At a polyglot language boundary (a `.scrip` program entering SNOBOL4 or Icon code, which scan strings in these registers) the boundary saves and restores all three — one push/pop triple per crossing, never per box. (3) The ζ-STANDING tables (dynamic database, `nb_setval` store, settable flags, proc table) are one per-program record whose base is `r14` (second grant) — the root frame the law already names, in a register. (4) Nothing else Prolog-private may claim a register: the clause cursor, the resume label, the cut barrier, the argument and permanent cells stay frame fields (§ A.1). With `r12` as the trail, the four registers are: `r12` TR · `r13` B · `r14` ROOT · `r15` BALL, and `r10`/`r11` stay the statement number and the BB node id.


**Provenance.** Lon, in-chat to ceo, 2026-09-02, verbatim: *"Maybe you should analyze the Byrd Box literature on Prolog so that you can maybe see how to properly align the variables within each Byrd Box. Get to where you understand how each Prolog construct maps into the Byrd Box for DEBUGGING and ANALYSIS, and just turn that into IMPLEMENTING using Todd Proebsting's Byrd Box translation technique."* Addendum, same session: *"You would need to scan the internet for the Prolog literature relating to Byrd Boxes and how the constructs map and are wired."* This page is a DESIGN page under `ARCH-PROLOG-THREE-ZETAS.md` (the order of the work) and `ARCH-ENGINE.md` § THE FRAME-PLACEMENT CRITERION; it restates no law. It reads the literature (§ G), maps every construct (§ B), homes every Prolog-only runtime global (§ C), and orders the rungs (§ E).

**Tree label for every `file:line` below: SCRIP `46db4457` as the committed head, read in the WORKING TREE of `/home/claude/SCRIP` at 11:33–11:37 on 2026-09-02.** ⚠ The tree was being rewritten by hq_C during the sweep: all cited files carry mtime 11:28:11, `lower_prolog.c` went 1524→1459 lines and `emit.cpp`'s suspend site moved 3444→3330 between two reads twenty minutes apart. Every anchor below was re-taken AFTER 11:28 from one tree, so they agree with each other, but they are a snapshot of an uncommitted working tree: **the symbol name is the durable key; grep it, never trust the number.** Literature citations say how they were verified (§ G): READ = the PDF was read page by page here; FETCHED = the page was fetched and quoted; RECORD = only the bibliographic record was confirmed (dblp / publisher / a citing paper); NOT FOUND = searched, no copy reached.

---

## A. The port correspondence — Byrd's four ports, SCRIP's four ports, and what holds at each

Byrd (1980, RECORD; port names quoted from Kulas 2003 READ p.1 and SICStus 3.7.1 FETCHED) draws each predicate call as a box with four ports: *call* "entering the procedure in order to solve a goal", *exit* "leaving the procedure after a success", *fail* "leaving the procedure after the failure", *redo* "re-entering the procedure, i.e. another solution is sought for". Proebsting (1997, READ p.2) renames them start/succeed/fail/resume, footnotes *"Byrd called these call, redo, fail, and exit"*, and makes the decisive move: *"In the Byrd Box, ports are locations, whereas here they are pieces of code."* SCRIP's α/β/γ/ω are Proebsting's four chunks with Lon's names.

| Byrd | Proebsting chunk | SCRIP port | direction | trace line it produces (Byrd/DEC-10 form, SICStus 3.7.1 FETCHED) | invariant that holds when control is AT this port |
|---|---|---|---|---|---|
| **CALL** | `θ.start` | **α** proceed | into the box | `(N) D Call: p(A,B)` | frame F carved, `F.B0` = the youngest choice older than F, params `F.A[i]` staged, `F.LOG` empty, `F.CUR` = first candidate clause, no binding by F yet |
| **EXIT** | `θ.succeed` | **γ** succeed | out, forward | `(N) D Exit: p(a,b)` — ECLiPSe writes `*EXIT` when the box keeps alternatives (FETCHED) | the current clause's head+body succeeded; bindings made by F's activation are live and logged in `[F.B].LOG` (§ B.11); if F still has alternatives the frame is RETAINED (Proebsting: not `bounded`) and `F.RES` names the youngest sub-goal's β; if not, the frame is released and `F.LOG` was empty or promoted (§ B.11) |
| **REDO** | `θ.resume` | **β** recede | into the box, backward | `(N) D Redo: p(a,b)` | re-entry at `F.RES` (redo the youngest sub-goal) or, when that sub-goal is exhausted, a clause step: undo `F.LOG`, re-seed locals `F.G[j]` unbound, `F.CUR` += 1, unify the next head; the caller's frame is exactly as at the matching γ (the caller's own work since then has been undone by ITS log) |
| **FAIL** | `θ.fail` | **ω** concede | out, backward | `(N) D Fail: p(A,B)` | `F.LOG` fully undone, `F.CUR` exhausted, frame released, every choice younger than F dead; the wire carries `r10 = F.B0` so the caller's `F.B` reverts (§ B.3) |
| EXCEPTION (SICStus/SWI/gprolog, FETCHED — not in Byrd) | — | **ω with ball** (`r11 ≠ 0`) | out, backward | `(N) D Exception: p(A,B)` — SWI: *"activated on each parent frame ... until the exception is caught"* (FETCHED) | same as FAIL plus `r11` = heap copy of the ball; every box's ω passes `r10:r11` through untouched; only a catch box's β-handler inspects `r11` (§ B.10) |
| UNIFY (SWI only, FETCHED: *"the neck instruction, signalling the end of the head-matching process"*) | — | γ of the head-unification sub-box | inside the box | `(N) D Unify: p(a,B)` | head of clause `F.CUR` unified; body not yet started |
| NEXT / ELSE (ECLiPSe only, FETCHED) | — | β of the clause box / disjunction box that steps `F.CUR` | inside | `NEXT`, `ELSE` | as REDO's clause-step arm |
| cut_call / cut_exit (SWI `prolog_trace_interception/4`, FETCHED) | — | α / γ of the cut box | inside | — | `F.B := F.B0`, younger choices dropped, their logs promoted (§ B.6) |

**Where the sources disagree, and what this page picks.** (1) *What REDO means after a cut.* SICStus 3.7.1 (FETCHED): an invocation *"cut out of the search space by cuts"* is simply never re-entered; *"the system fails to the latest surviving Call port before the correct one"*. SWI adds `cut_call(PC)`/`cut_exit(PC)` events so the trace SHOWS the cut; ECLiPSe shows nothing. **Pick: SWI's** — the cut is a box (`IR_CUT`, `lower_prolog.c:688`), so it has an α and a γ and the hook fires on both for free; a REDO into a cut-away box is by construction unreachable (its token is dropped, § B.6), which is SICStus's semantics with SWI's visibility. (2) *The exception port.* Byrd has four ports; SICStus, gprolog and SWI draw a fifth exit. **Pick: no fifth port.** An exception is ω with `r11 ≠ 0`: same wire, same undo discipline, one extra register test at the catch box only — the trace prints `Exception` instead of `Fail` when the hook sees `r11 ≠ 0`. (3) *Box granularity.* Proebsting (1997 READ p.4): *"It appears that Byrd used the boxes to model control flow between calls within a single clause, but not to model the flow of control between clauses within a procedure, nor to model the control-flow in and out of a procedure."* Kulas (READ p.2, Table 1) extends ports to EVERY goal, including `A,B`, `A;B`, `true`, `fail`, `T1=T2`. **Pick: Kulas's** — every IR node is a box; the predicate-call box is the one that owns a FRAME; conjunction/disjunction/unify boxes are frameless (their ζ is the enclosing frame's, as the Icon lowerer already does for `&`, `|`, `not`). (4) *EXIT determinism.* ECLiPSe's `*EXIT` and SWI's *"It is possible for the goal to have alternative"* are the same bit Proebsting calls `bounded` and SCRIP calls `deterministic` (`IR.h:218`, stamped at `lower_prolog.c:1228`). **Pick:** the γ hook reports it from `F.CUR` (exhausted or not) — one compare, no new state.

### A.1 The frame — the one record every Prolog call box owns (the variable alignment, stated once)

**⛔ CEO RULING ON hq_C's REVIEW (2026-09-02, `FINDING-2026-09-02-hq_C-translation-page-review-the-base-header-lands-on-the-lexprep2-region-and-r12-is-the-live-cas-cursor-inside-every-snobol4-match-blob.md`, measured on SCRIP `2748100d`):** (C1, ACCEPTED) the header does NOT sit at `[rbp+0..48)` — under the post-carve pin that region is what `rt_jmp_frame_lexprep2` zeroes on every β and `[rbp+0]/[rbp+8]` is `bb_suspend`'s own pair. The header lives at the TOP of the frame beside the wire triple, and every offset in the table below is read relative to **H, the header address**; **`F.B` (and `r13`) hold H, not `rbp`**, so a binder holding only B reaches `F.TRMARK`, `F.CUR`, `F.RES`, `F.HI` at fixed offsets without knowing `kt`. hq_C writes the measured layout into this table in its rung-2 commit; **DONE — MEASURED rung 2 (hq_C, SCRIP `2fc5ce73`): the header is the top 64 bytes of the pinned frame, H = `rbp + kt - 64`, and every offset below is LIVE, not symbolic: `F.TRMARK` `[H+0]` = r12 at α · `F.CUR` `[H+8]` = the ADDRESS of the next alternative (0 = exhausted) · `F.RES` `[H+16]` = the banked redo of the youngest sub-goal, consumed and zeroed by the graph β · `F.B0` `[H+24]` = r13 at α · `[H+32]` pad, still unused by Prolog · `[H+40] [H+48] [H+56]` the wire triple. `F.B` is r13 and has no slot, as ruled.** (C3, ACCEPTED, and it refines Lon's grant rather than contradicting it) r12 is today the live C.A.S. cursor inside every SNOBOL4 match blob (banked at `[rbp-8]` around calls) AND the scratch register of Prolog's own sinks (`bb_call_fn.cpp` `lea r12, [rip+…]`), and r13/r14/r15 are the σ/δ/Δ scanning pins of every SNOBOL4 blob and Icon scan; `rtx_abi.inc:16-21` is wrong to call r12 free. Therefore: **rung 2.0 (before PZ-4 (b)-(f)) re-homes every scratch use of r12 in Prolog-reachable sinks and templates** — DONE-WHEN: a grep of r12 writes reachable from a Prolog graph = 0 outside the TR push/pop/mark helpers, on the sweep's 98-file tree; **the trail arena is Prolog's own `rt_slab_region`**, never the 8 MB carve-only C.A.S. island of `pattern_match.c` (`rt_cas_carve` is bump-only and aborts the process when full — a long backtracking run would die in SNOBOL4's allocator), and it GROWS or REFUSES with a named limit, never aborts; **the polyglot boundary saves the quad r12–r15**, not a triple (`contract_regs[6]` at the co-expression boundary already lists r12). The four assignments stand: `r12` TR · `r13` B (= H) · `r14` ROOT · `r15` BALL.
**MEASURED by hq_P's BX-0 instrument (SCRIP `f4532dea`): `r15` holds a driver mmap address at every Prolog ω today, not 0 — so before any β guard or catch handler may trust `r15`, C37 zeroes it at Prolog graph entry and after every catch landing; until then the trace withholds `Exception`.** **(C9, ACCEPTED — it corrects § B.10 and the `r15` rule):** SCRIP wires box N+1's ω to box N's β, so a ball taking ω would LAND ON THE β OF EVERY RETAINED CALLEE to its left and be run as a REDO instead of propagating (and the same hole exists for the catch box's own β after it has exited — ISO: transparent once exited). Kulaš § 3.9 (txt numbering § 3.8/3.9) `ThrowAnc` walks PARENTS and EMPTIES younger sibling continuations with undo, never re-enters them. **Rule:** every β arm that can hold a retained callee — `bb_call_proc_staged`'s β, the future clause-step β, the catch box's own β — opens with `test r15, r15 ; jnz <own ω>`: one pair per retained-frame re-entry, none on leaves; the catch handler is the only β that reads `r15` for a value and clears it. **BX-0 witness (hq_P):** `catch((member(X,[1,2,3]), X>1, throw(t)), t, true)` prints no `Redo: member` after `Exception`. **(C4–C8, ACCEPTED as written in hq_C's FINDING:** the cut box's `rsp := rbp` premise, the rung-9 strike, `act+8`, `SCRIP_ZSM_CENSUS`, the rung split 2.0 / 2a / 2b — hq_C amends the named sections of this page in the rung commits that implement them; a section not yet amended reads with the FINDING beside it.) **Carve hazard (hq_C (3)):** `[kt-40]` is Pascal's display slot in the SHARED zframe prologue (`xa_flat` PAS-DISPLAY-1) — the Prolog header carve keys on `zframe_pinned_base`, never on `zframe_graph`. **Cut is now sourced, not designed:** Kulaš & Beierle 2000 § 3.8 (txt) suffix criterion — emptying up to the parent IS `B := F.B0`; Aït-Kaci § 5.11; Jahier § 3 eq. 7' (the cut box has call and exit events only, matching `bb_cut` today).

The activation frame of a Prolog call box is Icon's frame with the WAM's environment AND choice point folded into it (Lon: *"the stacks and lists moved INTO the BB box"*). It is RBP-pinned after the carve, as hq_C landed it (`xa_flat.cpp:199-204`: `sub rsp,kt` · `[kt-24]=γ` · `[kt-16]=ω` · `[kt-8]=caller rbp` · `rbp=rsp`; caller-base restore through the pin `xa_flat.cpp:183`). MEASURED rung 0 (hq_C): the header is the top 64 bytes of the pinned frame (`flat_frame_bytes` grows 48→80 over the zls region for a `zframe_pinned_base` graph; `rt_jmp_frame_lexprep2` zeroes `kt-64`); every offset in the table above is H-relative. The paragraph that follows is the pre-ruling text, kept for the reasoning it records: the header sits at the BASE because `kt` differs per graph and a binder that only knows a frame's address must find `B`, `LOG` and `HI` at fixed offsets — the same reason today's `[fb+0]/[fb+8]` "already suspended" pair is base-relative (`bb_suspend.cpp:20-33`, `rt.c:1632-1643`).

| slot | offset | WAM name (Aït-Kaci 1991, RECORD; not re-fetched) | lifetime / placement by the FRAME-PLACEMENT CRITERION | exists today? |
|---|---|---|---|---|
| `F.B` | **`r13`, no slot** (MEASURED rung 0, SCRIP rung-0 commit: H = `rbp + kt - 64`, the 64-byte header at the TOP of the pinned frame; `main/0` on the hello witness: kt=176, H=`rbp+112`) | **B** — youngest live choice frame, as seen from this activation | ζ-ACTIVATION-FRAME: read by every binding this activation makes, written at every γ/ω landing of its callees | no — the global `g_pl_zf_pending_*` + the three CP stacks play this role (`rt.c:1644-1727`) |
| `F.B0` | `[H+24]` (stored at α: `mov [rsp+kt-40], r13`; the root graph seeds `r13=0`, `r15=0`, `r14=H` first) | **B0** — the cut barrier: `F.B` as it was at α | ζ-ACTIVATION-FRAME | no — `g_resolve_cut_flag` (`resolution.c:14`) and compile-time ω rewiring (`lower_prolog.c:191-208`) |
| `F.CUR` | `[H+8]` (seeded at α with alternative 1's address; stepped by the clause step) | the clause pointer in the choice point (`BP`) | ζ-ACTIVATION-FRAME; survives γ | **YES — LANDED rung 2 (hq_C).** It holds a LABEL ADDRESS, not an index (Jcon `ir_MoveLabel` promoted into the frame), so the step is `mov rax,[H+8]; test; jz ω; jmp rax` with no table and no compare chain |
| `F.RES` | `[H+16]` (zeroed at α; banked by the clause's success trampoline; CONSUMED AND ZEROED by the graph β as it is read) | the continuation (`CP`) of the retained sub-goal | ζ-ACTIVATION-FRAME; written at γ, read at β | **YES — LANDED rung 2 (hq_C).** Zero-on-read is what makes a clause that is redone and then fails fall through to the step with no separate reset; a clause that succeeds again re-banks it |
| `F.HI` | **`[H+32]`** (MEASURED rung 3, hq_C) — **RESTORED, and it is no longer the frame top** | the log threshold | ζ-ACTIVATION-FRAME. The pinned prologue seeds it to this frame's top (`rbp+kt`), which is byte-for-byte the `B+64` rung 1 computed, so rungs 0–2 are unchanged. It is a WORD and not a constant offset because **a choice opened INSIDE an activation protects less than the whole frame**: a disjunction (§ B.5) lowers it to the frame base while a branch is untried, and every cell of that activation becomes loggable. Review C1 retired `F.HI` on the premise that a binder holding only `B` needs the frame top; the premise held only while every choice WAS a whole frame | **YES — LANDED rung 3 (hq_C)** |
| `F.TRMARK` (was `F.LOG`) | `[H+0]` (= r12 at α — MEASURED rung 1, hq_C: `mov [rsp+kt-64], r12` in every pinned prologue; the root graph seeds r12 first through `rt_pl_quad_seed`) | **TR** at choice creation — the trail mark this choice unwinds to | ζ-ACTIVATION-FRAME, 8 bytes; the entries themselves live on the `r12` C.A.S. trail (second grant), never in the frame | no — `g_pl_trail` global area (`resolution.c:16`, `pl_cell.h:49-62`) |
| `H+32` | **`F.HI`, the log threshold — see the `F.HI` row above (rung 3).** It WAS the PL-DC/N-2 pad; measured on the rung-0..3 witnesses, no Prolog graph reaches the PL-DC entry and no emitted Prolog code referenced `[H+32]` | — | header | yes |
| `H+40` / `H+48` / `H+56` | the wire triple: γ wire, ω wire, caller `rbp` (`[kt-24] [kt-16] [kt-8]` of the shared zframe prologue, unchanged) | — | header | yes |
| `F.A[i]` | after the header | **A1..An** / `Y` permanents | ζ-ACTIVATION-FRAME (params, 16 B DESCR) | yes (`g->pnames`, `lower_prolog.c:806-807`) |
| `F.G[j]` | after A | **Y1..Yn** | ζ-ACTIVATION-FRAME (clause locals, 16 B DESCR); re-seeded unbound on every clause step | yes (`g->nslots`, `lower_prolog.c:846`) |
| `F.ACT[s]` | per call site | — (the WAM has no per-site token; it is what makes β wire-able) | ζ-ACTIVATION-FRAME: the retained callee frame address or 0, one per call site of the body | yes — `callgen.act` (`zeta_storage.c:181,188`), read by Icon's β (`bb_call_proc_staged.cpp:832-838`) |
| `F.BALL` | catch boxes only | — | ζ-ACTIVATION-FRAME (16 B); the ball itself is a heap copy (escaper) | no — `g_pl_throw_ball` (`unification.c:2234`) |
| `F.ACC` | findall/bagof/setof boxes only | — | ζ-ACTIVATION-FRAME (head, tail); the collected terms are heap cells (escapers) | no — the accumulator handle returned by `rt_pl_findall_begin` (`unification.c:1678`) rides a spine cell |
| ζ-SPINE temporaries | `[rbp+off]` below the header | **X** registers | ζ-SPINE: head-unification scratch, arithmetic scratch, builtin argument staging — every consumer at a fixed offset within one activation, no γ→β window between | yes (`x86_zref`/`x86_zop` under the pin, `x86_asm.h:454-460`) |

**Pascal's display occupies the same registers (hq_P, measured 2026-09-02, `bb_var_frame.cpp:11` `frame_display_reg(lvl)` = r13/r14/r15 for levels 1–3, twin at `bb_assign_frame.cpp:16`): not live today (no Pascal graph is reachable from a Prolog one and the quad gate sweeps Prolog witnesses only), but the Pascal cure for `L ≥ 4` (`pascal-uplevel-nested-proc-hang`, re-ranked 1) is RULED a frame-resident display — ζ-ACTIVATION-FRAME, indexed by level, per Lon's FRAME-PLACEMENT CRITERION — never a fourth display register and never `r12`; the boxes are shared and language identity stops at lower.** **The B wire IS `r13` (Lon's grant above; read every "`r10`" in this paragraph as `r13`).** Every port transfer between frames carries the youngest-choice pointer in `r10`: α — caller hands `r10 = caller.F.B`, callee stores `F.B0 := r10`, `F.B := (more than one candidate clause) ? rbp : r10`; γ — callee hands `r10 = callee.F.B` (itself or something younger it retained; unchanged if bounded), caller landing stores `caller.F.B := r10`; ω — callee hands `r10 = callee.F.B0`, caller landing stores `caller.F.B := r10`; β — no wire needed, the retained callee's own `F.B` is still right (every choice younger than it died before the caller backtracked into it). This is the WAM's B register carried by wires and frames instead of a global — what Lon asked for. `rax:rdx` stay the DESCR return per THE PORT-EXIT VALUE CONTRACT; `r11` is the ball (§ B.10).

**What Icon already has and Prolog copies rather than invents:** the retained frame that survives γ (`emit.cpp:2899` α, `:3330` suspend keeps the frame, `:3255` landing repoints rbp and does NOT release); the resume label banked in the frame and jumped through by β (`bb_call_proc_staged.cpp:832-838`: `mov rax,[act+8]; mov rsp,[rax+24]; sub rsp,40; jmp [rax+32]`); the per-site callee token `callgen.act`; the `bounded` elision of β chunks (`irgen.icn:113-140` Limitation, `:167-200` Alt: `/bounded & suspend ir_chunk(p.ir.resume, …)`); the indirect-goto gate for constructs whose resume target is only known at run time (`irgen.icn:167-200` `ir_MoveLabel`/`ir_IndirectGoto`; Proebsting 1997 READ p.4 `ifstmt.gate`). What Icon does NOT have and Prolog adds: the binding log (Icon has no bindings to undo), the B/B0 pair (Icon has no cut), the clause cursor as a frame field (Icon's alternation cursor is a spine temporary because `|` never suspends across a call boundary the way a clause does).

---

## B. Per construct — port wiring, Proebsting shape, variable alignment

Notation: `X.α X.β X.γ X.ω` are the four chunks of box X. "Wire" = a compile-time `jmp` (`ARCH-ENGINE.md` § Flat-BB ABI). "Frameless" = the box has no frame of its own; its ζ lives in the enclosing call box's frame. Every subsection ends with the source whose wiring was followed.

### B.0 The box inventory — SHARED FIRST (Lon 2026-09-02, in-chat to ceo: *"Keep in mind to share BB's with Icon and SNOBOL4. No need to create a new BB that does exactly what one already does."*)

Measured on SCRIP `f4532dea`: Prolog lowers to 18 IR kinds, 14 of them shared with Icon (`IR_CALL IR_CALL_BUILTIN_GEN IR_CALL_PROC_STAGED IR_DISJUNCTION IR_FAIL IR_GOTO IR_LIT* IR_SUCCEED IR_SUSPEND IR_VAR IR_VAR_REF`), 4 Prolog-only (`IR_CALL_PROLOG IR_CUT IR_MOVE_LABEL IR_OP_COUNT`); 136 `bb_*.cpp` boxes exist, 12 carry Prolog arms; `bb_cell_ite.cpp` and `bb_cell_cut.cpp` have NO lowerer emitting their kind (dead, deleted by the cut). Every Prolog box is keyed on IR kind plus a language-blind graph property — **`zframe_pinned_base` (+ `root_graph` for the top graph)**, the key rung 0 measured (ceo ruling on hq_C's FINDING § 4, 2026-09-02: `cells_graph` is NOT introduced; `pl_cells_graph` was a dormant env-gated field no graph set, and a property with no writer is a lie) — never on a language name (`test_gate_emit_no_lang.sh`). The inventory Prolog needs, from the Byrd/Proebsting/Jcon/Kulaš/WAM sources of § B and the box list on disk:

| rung | construct(s) | § B | the box | status on `f4532dea` |
|---|---|---|---|---|
| 0 | fact / hello world: the call box with the § A.1 frame (α carve, γ retain, β via `F.RES`, ω release) | B.1 B.3 | **`bb_call_proc_staged`** (`IR_CALL_PROC_STAGED` — Icon, Raku and Prolog already lower to it); `IR_CALL_PROLOG` COLLAPSES into it | EXISTS, shared by three frontends; grows the header |
| 0 | head/body unification with the log rule | B.11 | **`bb_call`** (`IR_CALL`) around `rtx_plunify.s`; the log rule reads `F.B` through `r13` inside the service | EXISTS; the sink's global refs go with the cut |
| 0 | det leaves `write/1`, `nl/0` | B.12 | **`bb_call`** → `dop_write`/`dop_nl` direct (Icon's `bb_call_write_slot` and `bb_det_nl` are the same shape) | EXISTS |
| 1 | conjunction | B.4 | **`bb_conjunction`** (`IR_CONJUNCTION` — Icon `e1 & e2`, Jcon `ir_conjunction`) or its identical wiring | EXISTS — rung 1 LANDED as the identical wiring (hq_C): Icon's `IR_CONJUNCTION` node is a value-forwarding leaf (`bb_conjunction.cpp`) whose Byrd wiring is done by the lowerer (`lower_icon.c` TT_CONJ arm); Prolog has no value to forward, so `pl_lower_conj` (`lower_prolog.c`) does the same γ→α / ω→β threading and emits no box |
| 2 | multi-clause choice, clause step on β | B.3 | **`bb_call_proc_staged`**'s β chunk = Jcon `Alt` with `F.CUR`/`F.RES`/`F.B` in the frame | **LANDED (hq_C, SCRIP `2fc5ce73`)** — the clause step and the redo port are the GRAPH's β and a tail-emitted step label, not IR nodes (the RPO walk drops `IR_SUCCEED`/`IR_FAIL`, so a node-borne step can never fire); PZ-4 (b)-(f) and PZ-5 absorbed; `chain2b.pl` green |
| 3 | disjunction | B.5 | **`bb_disjunction`** (`IR_DISJUNCTION` — SNOBOL4 alternation, Icon `e1 \| e2`, Prolog); the cursor is the box's own zls word, which under `zframe_pinned_base` already lives in the enclosing activation frame | **LANDED (hq_C, rung 3)** — the WIRING added no template code at all (Icon's operand convention + σ/φ edge marks); what the template gained is the pinned arm that makes the choice real: the trail mark, `rt_pl_disj_open`, and the branch-step unwind |
| 4 | cut | B.6 | **`bb_cut`** (`IR_CUT`) grown to the barrier: `F.CUR := 0`, `F.RES := 0`, `B := F.B0` via `rt_pl_cut_barrier`, `rsp := the pin`; log promotion is ZERO instructions on rung 1's linear trail (SNOBOL4's FENCE, `bb_match_fence0/1`, is the pattern-level cousin, not the same box) | **LANDED (hq_C, rung 4)** — plus the compile-time half: a per-graph `cut_ω` distinct from `alt_fail` takes every ω to the right of the cut |
| 5 | if-then-else, `\+`, once, forall, ignore | B.7 B.8 B.17 | **`bb_bound`** (`IR_BOUND` — Icon's bounded if = Proebsting `ifstmt`) + **`bb_indirect_goto`**/**`bb_move_label`** for the gate | EXISTS |
| 6 | det builtins `is/2`, compare, type tests, atom ops | B.12 | **`bb_call`** / **`bb_call_fn`** / **`bb_binop_relop`** → `dop_*` direct, no name path | EXISTS |
| 7 | generators: `between/3`; `clause/2`, `retract/1`, `sub_atom/5`; `repeat/0` | B.13 B.17 | **`bb_to`**/**`bb_to_by`** (`IR_TO` — Icon, Raku: Proebsting `to`); **`bb_call_builtin_gen`** + **`bb_suspend`** (Icon, Prolog); **`bb_repalt`** (Icon `\|e`) | EXISTS |
| 8 | findall / bagof / setof | B.14 | **`bb_iterate`** / **`bb_every`** (Icon `every … do`) + a collect leaf, `F.ACC` in the frame | EXISTS |
| 9 | catch / throw | B.10 | **`bb_catch` — THE ONE NEW BOX**: a call frame plus a β-handler chunk, `F.BALL`, the ball on `r15` (Icon and SNOBOL4 have no exception box) | NEW |
| 10 | `call/N`, assert/retract, the dynamic DB, DCG | B.9 B.15 B.16 | **`bb_call_value`** (`IR_CALL_VALUE` — Icon `p!args`) + the runtime compiler (SNOBOL4's `EVAL`/`CODE` path, THE RUNTIME-GOAL RULING); the DB in a root-frame slot | EXISTS |
| 11 | last call | B.18 | a new α ARM on **`bb_call_proc_staged`** (WAM `deallocate; execute`), not a box | NEW ARM |
| — | `true`, `fail` | B.17 | **`bb_succeed`**, **`bb_fail`** | EXISTS |

Nine of the eleven shapes are boxes Icon or SNOBOL4 already own; one box (`bb_catch`) and one arm (LCO) are new. The 12 Prolog arms in shared templates that branch on Prolog identity are deleted by the cut and re-expressed, where a real difference exists, as a graph property the shared box reads.

### B.1 Fact `p(a, b).`
(i) One call box P. `P.α` → head-unify sub-box `U.α`; `U.γ` → `P.γ`; `U.ω` → next clause or `P.ω`. No β chunk when P has one clause (bounded). (ii) Proebsting's `literal` (1997 READ p.3): `start` produces the value and goes to `succeed`; `resume` goes to `fail`. Today: `lower_pl_clause_into` (`lower_prolog.c:718-738`) emits one `$unify` per argument (`:731`), a list-pattern fast path `$unify_lst` (`:727`, sunk inline at `bb_call_fn.cpp:232`), and `pl_fact_choice_lookup` (`:37-48`) recognises all-constant fact tables. (iii) `F.A[i]` params; scratch for the constant on the spine; `F.LOG` receives the binding of a caller cell to the constant (§ B.11) — and because a single-clause fact is bounded, `F.B := F.B0`, so the entry lands in the caller's youngest choice, exactly the WAM. **Source: Proebsting 1997 § 4.1 (READ); Kulas S:atom:1-3 (READ p.5).**

### B.2 Rule `p(X) :- q(X), r(X).` (head + body conjunction)
(i) `P.α` → `U.α` (head) → `Q.α` → `R.α` → `P.γ`; backward: `R.ω` → `Q.β`; `Q.ω` → (next clause | `P.ω`); `P.β` → `R.β` (the youngest sub-goal). (ii) Proebsting's `plus` (1997 READ p.3): `plus.resume: goto E2.resume`; `E2.fail: goto E1.resume`; `E1.fail: goto plus.fail` — conjunction is the binary-operator threading with the value replaced by the substitution. Today `thread_goals` (`lower_prolog.c:186-243`) wires exactly this, and records the RIGHTMOST resumable goal as the clause's β (`redo_out`, `:722`). (iii) `F.A`, `F.G` (locals X), `F.ACT[q]`, `F.ACT[r]` tokens, `F.RES` = R's β label banked at `P.γ` (Icon's `[H+32]`). **Source: Proebsting 1997 § 4.3 (READ); Kulas S:conj:1-6 (READ p.5): `fail B' → redo A`, `redo A,B → redo B`.**

### B.3 Predicate with several clauses — THE CHOICE
(i) `P.α`: `F.CUR := first candidate` (after § B.13 indexing), `F.B := rbp` if more than one candidate else `F.B0`; try clause `F.CUR`. `P.β`: if `F.RES ≠ 0` jump it (redo the youngest sub-goal of the live clause); else CLAUSE STEP: undo `F.LOG`, re-seed `F.G[*]` unbound (the `LCL-SEED` `rep stosb` precedent, `emit.cpp` LCL-SEED), `F.CUR += 1`, if exhausted → `P.ω` else → that clause's `U.α`. `P.γ`: bank `F.RES`, hand `r10 = F.B`, keep the frame. `P.ω`: `r10 = F.B0`, release. (ii) Proebsting's `Alt` in Jcon (`irgen.icn:167-200`): each alternative's `success` does `ir_MoveLabel(t, alt_i.resume); goto Alt.success`, and `Alt.resume: ir_IndirectGoto(t)` — the resume target is a TEMPORARY holding a label, and `alt_i.failure → alt_{i+1}.start`. Prolog's clause box is Jcon's `Alt` with the temporary promoted to `F.RES`/`F.CUR` in the frame (a clause suspends across an unbounded γ→β window; Icon's `|` inside one expression does not). Today the choice is `IR_DISJUNCTION` + per-clause `IR_MOVE_LABEL`/`IR_SUSPEND` (`lower_prolog.c:811,821`), `$unwind_nothrow` between clauses (`:840`), the cursor in a spine cell (`bb_disjunction.cpp:38-52`), retry addresses pushed to `g_pl_retry` (`bb_move_label.cpp:24-25`, `rt.c:1644`) and a 3-word CP to `g_pl_zf3_stack` (`bb_suspend.cpp:30-33`, `rt.c:1684-1708`). (iii) `F.CUR`, `F.RES`, `F.B`, `F.B0`, `F.LOG`, `F.G` — all activation-frame; nothing on a stack outside the box. **Source: Jcon `ir_a_Alt` (READ) for the shape; Kulas S:disj:4-6 (READ p.5) for the `OR(...)` memo = `F.CUR`; ECLiPSe's NEXT port (FETCHED) is the clause-step arm made visible.**

### B.4 Conjunction `A , B`
(i) Frameless. `C.α → A.α`; `A.γ → B.α`; `B.γ → C.γ`; `B.ω → A.β`; `A.ω → C.ω`; `C.β → B.β`. (ii) `ir_conjunction` (`irgen.icn:405-416`) verbatim; `/bounded & C.resume: goto right.resume`. Today: `thread_goals` (`lower_prolog.c:186-243`), flattening nested `,` (`:315-325`). (iii) Nothing of its own; the tokens `F.ACT[a]`, `F.ACT[b]` of its callees live in the enclosing frame. **Source: Proebsting 1997 § 4.3; Kulas S:conj (READ).**

### B.5 Disjunction `A ; B` (not if-then-else)
(i) Frameless but a CHOICE: `D.α → A.α`; `A.γ → D.γ` after banking `F.RESd := A.β`; `A.ω → B.α`; `B.γ → D.γ` banking `F.RESd := B.β`; `B.ω → D.ω`; `D.β → [F.RESd]` (indirect). Because D can be re-entered after the enclosing clause has run further goals, its cursor is NOT a spine temporary: it is one slot `F.RESd` in the enclosing call box's frame (Jcon's `ir_tmploc t` promoted). While D has an untried branch, the enclosing frame counts as a choice (`F.B := rbp`) — a disjunction never pushes a frame of its own (PZ-2 "homed per PF-1b"). (ii) Jcon `ir_a_Alt` (`irgen.icn:167-200`) exactly. Today the lowerer either inlines the branches with NO β (`lower_prolog.c:331-353`, `cx->beta = NULL` — an inline `;` cannot be resumed, which is a defect) or hoists a cut-free disjunction into a synthetic predicate `$disjN/k` (`dj_expand_node`, `:1028-1052`) so it gets a frame. (iii) `F.RESd` in the enclosing frame, one per disjunction node. `$disjN/k` hoisting becomes unnecessary once `F.RESd` exists; it stays lawful as an optimisation only where it wins. **Source: Jcon `ir_a_Alt` (READ); Kulas S:disj (READ).**
**LANDED rung 3 (hq_C, 2026-09-02).** The box is the SHARED `bb_disjunction` (`IR_DISJUNCTION`) with ZERO new template arms for the wiring: `pl_lower_disj` (`lower_prolog.c`) builds Icon's exact operand convention — `(entry_j, resume_j)` pairs then N result operands (NULL for Prolog, since a Prolog branch forwards no value) — and marks the branch γ/ω edges σ/φ the way `lower_alt_impl` does, so `flat_drive_match_alt` and the existing template do the rest. **The cursor is the zls `disj.alt_i` word, not a new `F.RESd`**: under `zframe_pinned_base` the box's own slot IS in the enclosing activation frame, so § B.5's requirement is met by the storage the box already owns, and the resume is a per-branch dispatch on that cursor rather than an indirect jump through a banked label. `pl_lower_conj` counts `IR_DISJUNCTION` as a resumable kind, so goals to the right wire ω→its β and a clause ending in one banks its β into `F.RES` through the rung-2 success trampoline.
⭐ **A det branch has no resume, and that is not a special case:** it names the disjunction itself as its `resume_j`, which the driver already maps to the φ landing — *a deterministic branch's redo IS its failure*, which is exactly Kulaš S:disj.
⛔ **THE CHOICE IS REAL, SO THE BINDINGS MUST BE UNDONE — and the rule of § B.11 could not do it.** Measured, both modes: `p :- ( X = a ; X = b ), write(X), nl, fail.  p.` printed `a` only. § B.11 logs a cell only at or above `[B+64]`, the choice frame's top — correct while every choice IS a whole frame, because a clause step re-seeds `F.G[*]`. A disjunction opens a choice **inside** an activation and its branch step re-seeds nothing, so the frame's own locals were bound and never logged. **The fold that made a choice BE a frame has no expression for a choice opened within one.** Cure, landed: `F.HI` returns as the threshold WORD at `[H+32]` (§ A.1), and the box α banks `r12` into its own pad slot and calls **`rt_pl_disj_open(H, rbp)`** — one named rtx helper, enrolled by name in the quad gate — which writes `F.HI := rbp` and raises `B` to `H` **only when the live choice is older than this frame or absent**. ⭐ That last clause is the part § B.5's bare "`F.B := rbp`" gets wrong: with a retained callee to the LEFT of the disjunction (`q(A), (write(x) ; write(y))`), the youngest choice is already younger than this frame, this frame's locals are already above `[B+64]` and already logged, and overwriting `B` would demote a live choice. The φ glue unwinds `r12` to the banked mark through `rt_pl_tr_unwind`, the same helper the rung-2 clause step uses.
⛔ **A branch that reduces to `true` is dropped by the RPO walk** (it lowers to `IR_SUCCEED`, which the walk chases through), so the driver's operand lookup missed and `PAIR(j)` fell back to the node's own ω — the branch entered the disjunction's FAILURE instead of its success and `( true ; write(b) ), write(k)` printed nothing at all. `pl_disj_entry` chases `IR_SUCCEED` and synthesises an `IR_GOTO` carrying an explicit σ γ when the branch reduces to `true`. ⭐ The general shape: **an operand that names a node the emitter never emits is not a dangling pointer — it silently resolves to the enclosing default**, and the default here was the one edge that inverts the answer.

### B.6 Cut `!`
(i) A box K inside clause k of P. `K.α`: `F.B := F.B0`; `F.CUR := exhausted`; for each call site s to the LEFT of K in this clause with `F.ACT[s] ≠ 0`: promote that callee's `LOG` chain into `[F.B0].LOG` and clear `F.ACT[s]` (its β is now unreachable); for every choice frame C between old `F.B` and `F.B0` reached through those tokens, the same, transitively; `rsp := rbp` (all younger frames released physically — the WAM's `B ← B0` reclaims the stack). Then `K.γ`. `K.ω` is never entered from below (`K.β → K.ω` trampoline, as today `bb_cut.cpp:8-13`); the lowerer already rewires every ω to the right of the cut to `cut_ω` (`lower_prolog.c:191-208`, the `cx->cut_ω = uwf` at `:816`) — that compile-time part is kept ("free, correct", PZ-3). (ii) There is no Proebsting template for cut (Icon has none); the wiring is `ifstmt`'s gate idea applied to ω: after K, the clause's failure continuation is P's ω, statically. (iii) `F.B0` (the barrier IS the frame, as Lon ruled), `F.ACT[*]` tokens, the logs being promoted. No `g_resolve_cut_flag`. **Source: SWI `cut_call/cut_exit` (FETCHED) for the two hook points; Aït-Kaci 1991 § 5.11 `B ← B0` (RECORD) for the semantics; SICStus 3.7.1 (FETCHED) for the observable: a cut-away invocation is never re-entered.**
**LANDED rung 4 (hq_C, 2026-09-02), in two halves, neither sufficient alone.** RUNTIME, in the shared `bb_cut` under the pinned arm: `F.CUR := 0` (else the rung-2 clause step cheerfully tries the next candidate — what `ladder__rung04_cut_prunes_later_clause` measures), `F.RES := 0` (a redo must not land in a callee the cut just killed), `B := F.B0` through the named **`rt_pl_cut_barrier`**, then `rsp := the pin`. COMPILE-TIME, in `lower_prolog.c`: a per-graph **`cut_ω`** node — a second `IR_FAIL` *distinct from `alt_fail`*, so the emitter resolves it to the graph's own ω and **not** to the clause step — takes the ω of the cut itself and of every goal to its right, and `pl_lower_conj` resets its resumable cursor at the cut so a clause's banked `F.RES` becomes the rightmost resumable **to the right** of the cut, or nothing when every resumable is to its left.
⭐ **THE TRAIL NEEDS NO PROMOTION, AND THAT IS A CONSEQUENCE OF RUNG 1, NOT AN OMISSION.** (i) above says to promote a cut-away callee's `LOG` chain into `[F.B0].LOG`. Rung 1 made the trail **one linear arena on `r12`**, so entries above the older choice's mark simply stay where they are and are undone when *it* backtracks — the WAM's single-trail behaviour. Promotion is **zero instructions**. A per-frame chain would have needed the walk; the design that replaced it deleted the need.
⛔ **A CUT INSIDE A DISJUNCTION BRANCH IS NOT THE ORDINARY CASE AND WAS MEASURED WRONG FIRST.** `p :- ( write(a), ! ; write(b) ), nl, fail.` printed `a b`; swipl prints `a`. The cut makes its branch non-resumable, and rung 3 encodes "non-resumable" as *the branch names the disjunction itself*, which the driver maps to the φ landing — **advance the cursor**, i.e. try branch 2. Cure: a branch containing an `IR_CUT` names **`cut_ω`** as its resume instead, which is in no `nodes[]` and so falls through to the disjunction's own ω — concede. ⭐ The lesson is rung 3's own encoding read back: *"has no resume" and "must not be resumed" are different facts that shared one spelling*, and only the cut could tell them apart.
⛔ **§ E ROW 4's `p :- q, !, p.` AT 10⁶ UNDER `ulimit -s 8192` IS A RUNG-11 CRITERION MIS-FILED ON RUNG 4** (hq_C, measured): 10⁴ passes, 10⁵ overflows. Each recursive activation keeps its own frame; reclaiming *that* is LCO, which is § E row 11 and states the same shape itself (`count(N)` to 10⁶ under `ulimit -s 512`). What the cut's `rsp := pin` release actually buys, A/B-measured on this tree with a retaining callee (`q/1` with three clauses): the depth ceiling **doubles, 10 000 → 20 000**. So the release is real and the 10⁶ is not rung 4's to earn.

### B.7 If-then-else `C -> T ; E`
(i) Frameless. `I.α → C.α`; `C.γ → cut-of-C then T.α`; `C.ω → E.α`; `T.γ → I.γ`; `T.ω → I.ω`… no: `T.ω → I.ω` only after the local cut; `E.γ → I.γ`; `E.ω → I.ω`; `I.β → [gate]` where gate = `T.β` or `E.β` set by the branch taken (the ITE itself has no alternatives: C is committed). (ii) Proebsting's `ifstmt` (1997 READ p.4): *"E1's succeed and fail chunks set gate to the appropriate chunk's — either E1's or E2's — resume label"*, `ifstmt.resume: goto [ifstmt.gate]`. The "cut of C" is § B.6 applied with barrier = the enclosing frame's `F.B` at `I.α` (a local B0, one slot `F.B0i` per ITE). Today `lower_ite` (`lower_prolog.c:295-307`) wires C/T/E with an `IR_GOTO` funnel and puts the runtime guard `$no_throw_or_fail` on C's ω edge (`:301`); that guard leaves with C37 (§ B.10) because ω-with-ball simply bypasses E. (iii) `F.B0i` (16 B, enclosing frame) and the gate slot `F.RESi`. **Source: Proebsting 1997 § 4.5 (READ); Jcon `ir_a_If` (`irgen.icn:577-619`).**

### B.8 Negation `\+ G`
(i) Frameless, bounded. `N.α → G.α` with `F.B0n := F.B`; `G.γ → cut to F.B0n, undo everything G bound (its log entries sit in choices younger than F.B0n — promote nothing, UNDO them), → N.ω`; `G.ω → N.γ`; `N.β → N.ω`. (ii) Jcon `ir_a_Not` (`irgen.icn:142-160`): the inner expression is compiled `"always bounded"`, `expr.success → Not.failure`, `expr.failure → Not.success`, `/bounded & Not.resume: goto Not.failure`. Today `\+`/`not` lower to `lower_ite(G, fail, true)` (`lower_prolog.c:406`), `once` to `lower_ite(G, true, –)` (`:407`), `ignore` to `lower_ite(G, true, true)` (`:408`), `forall(C,A)` to `\+ (C, \+ A)` (`:414-417`) — all correct reductions to § B.7 and they stay. (iii) `F.B0n`; the undo on `G.γ` is the one place a SUCCESSFUL goal's bindings are undone — it is § B.6's promotion replaced by an unwind. **Source: Jcon `ir_a_Not` (READ); Kulas has no `\+` (pure Prolog).**

### B.9 `call/N` and meta-call (`G` a variable goal)
(i) A call box whose callee is unknown at compile time: `M.α`: build the goal term (spine scratch), hand it to the runtime compiler (EVAL/CODE path, THE RUNTIME-GOAL RULING in `ARCH-ENGINE.md`), receive the α address of a wired graph, then behave as an ordinary call box (§ B.3) with `F.ACT[m]` the token. `M.β`, `M.γ`, `M.ω` as B.3. (ii) Proebsting 1997 § 4.6 (READ p.4): *"Translating a function call that generates a sequence of values requires a mechanism for suspending and resuming a function invocation"* — Jcon's `ir_Call(closure, fn, args, resume)` / `ir_ResumeValue(target, closure, resume)` (`irgen.icn:360-404`): the callee's resumability is a CLOSURE VALUE the caller re-drives from its own β; in SCRIP the closure IS the retained frame (ARCH-LANGUAGES § Byrd-box refinements). Today: `$call/N` bridges (`lower_prolog.c:380-393`, `pl_ensure_call_bridge :1004`) → `IR_CALL_BUILTIN_GEN "$call"` + `IR_SUSPEND` (`:967-1003`) → `rt_pl_call_gen` (`by_name_dispatch.c:4750`) → the `plc_*` solver (21 static definitions, `:1125-4704`) — the runtime interpreter C36 deletes. (iii) Nothing beyond B.3; the compiled goal's blob is cached in the ζ-STANDING code slab (the pattern JIT's shape). **Source: Jcon `ir_a_Call` (READ); the runtime-compile rule is Lon's (`ARCH-ENGINE.md` § THE RUNTIME-GOAL RULING).**

### B.10 `catch/3` and `throw/1`
(i) Catch box H = a call box around Goal that is ALSO a choice frame while Goal has alternatives: `H.α`: `F.B0 := r10`, `F.B := rbp`, `F.BALL := 0`, → `Goal.α`. `Goal.γ → H.γ` (retained iff Goal retained; `r10` as B.3). `Goal.ω → H.βhandler`: if `r11 == 0` → plain failure → `H.ω`; else undo `F.LOG` (ISO: bindings since the catch/3 call are undone before the catcher is unified), `F.BALL := r11`, unify(Catcher, ball): fail → `r11 := ball`, `H.ω` (re-throw, ball still on the wire); succeed → `r11 := 0`, `F.B := F.B0`, → `Recovery.α` (`Recovery.γ → H.γ`, `Recovery.ω → H.ω`). `H.β → Goal.β` (catch/3 is transparent to backtracking). `throw(Ball)`: copy Ball to the heap (`rt_pl_throw_set` already copies, `unification.c:2236-2244`), `r11 := copy`, take ω. Every intermediate box's ω path leaves `r10:r11` alone (the undo helper is inline or parks them in the frame around a call). (ii) No Proebsting template (Icon errors are `&error` failures, Jcon § 7.2 READ p.18 trampolines Java exceptions into null returns); the shape is Tarau's BinProlog source-level `catch/throw` on continuations (RECORD): *"throw/1 ... returns a special exception pattern, while catch/3 stops the engine, calls a handler on matching exceptions or re-throws non-matching ones"*. Today: `$trail_mark` + `$catch_check` (`lower_prolog.c:361-379`), polled `rt_pl_throw_pending` at 9 sites in `by_name_dispatch.c` (`:2656,2665`, `rt_pl_call_gen` region) and the ball in `g_pl_throw_ball` (`unification.c:2234`). (iii) `F.BALL`, `F.B0`, `F.LOG` of the catch frame; the ball on the heap (escaper: it must outlive every frame between throw and catch). **Source: SWI exception port semantics (FETCHED: propagates frame by frame until caught); SICStus 4.5.1 Exception port (FETCHED); BinProlog (RECORD).**

### B.11 Unification `X = T` and THE TRAIL
(i) Frameless leaf: `U.α`: unify; bind → `U.γ`; mismatch → `U.ω` (after undoing the partial bindings this unify made — a unify is atomic w.r.t. the log: it logs as it binds and unwinds its own suffix on mismatch). `U.β → U.ω` (Kulas S:unif:2: `redo T1=T2 → fail T1=T2`). (ii) Proebsting's `LessThan` (1997 READ p.3): a leaf that may fail after its operands succeed. Today `$unify` / `$unify_lst` (`lower_prolog.c:149,169`), sunk inline (`bb_call_fn.cpp:232`, trail push inline at its `g_pl_trail` block). (iii) **The log rule (this is the variable alignment that decides everything else).** Binding cell `c` to value `v` inside frame F: `B := [rbp+0]` (`F.B`); if `c` is a stack cell and `addr(c) < [B+32]` (`B.HI`: `c` is younger than the youngest choice, it dies with it) → no log; else append `{c, *c}` to `[B+40]` (`B.LOG`), spilling to a heap chunk when the inline `K` are used. Heap cells (compound arguments on the GC heap) are always logged (no `HB`: the compacting collector moves the heap mark; measure before adding one — open question § F.5). Undo = walk `B.LOG` back to zero writing `old` into `addr`. Undo sites: β clause-step of the owning choice, its ω, `\+`'s success arm, the catch handler. **Promotion sites** (entries that must outlive their frame): cut (§ B.6) and nothing else — a bounded γ never promotes because a bounded activation never made `F.B` point at itself, so its bindings were logged in the older choice from the start (the WAM's rule, zero copying on the deterministic path). The per-activation "own log" of `ARCH-PROLOG-THREE-ZETAS.md` § 1 is therefore refined: a box logs into the youngest CHOICE's frame, which is its own frame exactly when it is that choice. GC: `LOG` entries are interior heap pointers → the `zls[]` kind map marks them `PTR_GC` (ARCH-ENGINE § GC). **Source: Aït-Kaci 1991 § 5.x conditional trailing (RECORD); `ARCH-LANGUAGES.md` § Known parity gaps (gprolog `Word_Needs_Trailing`, SWI `GTrail`); Kulas S:unif (READ).**
**MEASURED rung 1 (hq_C, 2026-09-02, SCRIP rung-1 commit).** The trail is the `r12` arena: `rt_pl_tr_init` (`src/runtime/rt/rt_pl_trail.c`) takes a 2^26-byte `rt_slab_region` and aligns a 2^25-byte arena inside it (`PL_TR_ARENA_LG2`), so `base = r12 & ~(2^25-1)` — the only handle is `r12`, no global. Header: 32 bytes at `base`, `[base+0]` = the live top, written by every gate on exit and read by the GC as a root range whose top is a word (`rt_gc_root_range_add_topword`, `gc_heap.c`). Entries: 32 bytes `{cell, pad, old DESCR}` from `base+32`; overflow REFUSES rc=2 naming the limit (`rt_pl_tr_refuse`), never aborts, never wraps. **The log rule as coded** (`pl_tr_needs_log`, `rt_pl_trail.h`): with `B = r13` (= H of the youngest choice, 0 at the root), a binding of cell `c` is logged iff `B ≠ 0` and (`c` is a heap cell — at or below the C stack probe — or `c ≥ H+64`, i.e. older than the choice frame's top); a cell inside the choice frame or younger is not logged. **Every binder reachable from a Prolog graph enters through an rtx gate** in `rtx_plunify.s` (`rt_pl_dop_unify`, `_ci`, `_cs`, `rt_pl_dop_mkc`, `rt_pl_dop_is_v`) that parks `{r12, r13}` in a `pl_tr_ctx_t` on its own stack for the C body (`plw_bind`/`plw_unify_cells`/`plw_unify_vals` carry the ctx as an argument, rule (1) of the grant) and reloads `r12` on return; a unify that fails mid-way unwinds to its own entry mark (`rt_pl_tr_unwind_to`) before returning FAIL — the atomicity rule above, landed. The root seed of all four registers is ONE rtx helper, `rt_pl_quad_seed`, called from the pinned prologue of the `root_graph`; emitted code writes none of `r12`–`r15` (hq_P's quad gate: emitted quad-writes 0, rtx quad-writes 5, all in the five named helpers). Witness: `test_gate_pl_trail_mechanism.sh` (12 checks, fail-once/pass-once). **Not yet gated:** the `pl_unify`/`pl_bind` inline family of `pl_cell.h`, used by the rung-6+ value services in `unification.c` (43 sites), binds with no ctx until rung 6 gives each service a gate and a name in `QUAD_HELPER_RX` — an obligation of rung 6 (hq_P), unreachable from any graph the rung-1 lowerer admits.

### B.12 Deterministic builtins — `is/2`, comparisons, type tests, `write/1`, …
(i) Frameless bounded leaves: `α` computes, `γ` or `ω`; `β → ω`. (ii) Proebsting `uminus`/`plus` for the arithmetic tree (`$ax_*` nodes, `lower_prolog.c:268-293`), `LessThan` for `$cmp_*` (`:521-531`), `literal` for `$is_v` (`:512-520`). Today the bodies are the `dop_*` functions reached directly (`bb_call.cpp:275-280` table) or, for the residue, by name through `rt_call_arr_bl` (`bb_call_fn.cpp:583`) — P6 deletes the name path. (iii) ζ-SPINE only: argument staging cells at fixed offsets; the result DESCR in `rax:rdx` then a spine cell. **Source: Proebsting 1997 § 4.2-4.3 (READ).**

### B.13 Generator builtins — `between/3`, `member/2`, `append/3` (library), `clause/2`, `retract/1`, `sub_atom/5`
(i) A call box like § B.3 whose "clauses" are the generator's internal state: `F.CUR` holds the generator's counter/cursor (Proebsting's `to.I`), `F.B := rbp` while more values remain. `member/2`, `append/3` are ordinary predicates (library clauses) and get § B.3 unchanged. `clause/2` and `retract/1` iterate the dynamic database (§ B.15): `F.CUR` = the clause index in the ζ-STANDING table, re-validated on every β (logical update view: Kulas S:atom:2/4 memo the definition used at exit — we memo the snapshot generation number in `F.CUR`'s high half). (ii) Proebsting's `to(E1,E2)` (1997 READ p.3): *"The code below uses an extra code chunk as well as an additional temporary variable"* — `to.resume: to.I ← to.I+1; goto to.code`. Today each is a synthetic predicate wrapping `IR_CALL_BUILTIN_GEN` + `IR_SUSPEND` (`lower_prolog.c:395-405`, `:967-1003`) whose C body keeps its own state in the `resume` int64 (`rt_pl_dyn_iter_gen`, `unification.c:1928`). (iii) `F.CUR` (counter), `F.A[*]`, `F.LOG` as a choice. The C body becomes a value service that takes `(&F.CUR, args)` and returns the next value or FAIL — no state of its own. **Source: Proebsting 1997 § 4.4 (READ); Jcon `ToBy`/`Limit` (READ p.15).** ⛔ **RULED (ceo 2026-09-02 20:30 CDT, on hq_P's rung-7 blocker `FINDING-2026-09-02-hq_P-rung-7-not-landed-*`): the bare `F.B := rbp` here is SUPERSEDED by rung 3's mechanism (§ A.1 F.HI, § B.5).** A generator is a choice opened INSIDE an activation, exactly the inline disjunction's shape: at the generator's pinned α bank `r12` into the box's own pad word and call `rt_pl_disj_open(H, rbp)` (writes `F.HI := rbp`, raises `B` to `H` ONLY when the live choice is older than this frame or absent); β unwinds `r12` to the banked mark through `rt_pl_tr_unwind` before offering the next value; ω restores as `bb_disjunction` does. Emitted under `x86_fb_pinned()` in the shared box — never a language name — so Icon/Raku graphs stay byte-identical; graded on every frontend that lowers `IR_TO` (prolog 3 / raku 3 / icon 2 sites) per SHARED-NODE VERDICT SCOPE. `B`'s lifecycle under cut is rung 4's (hq_C): cut restores `B := F.B0` and MAY restore `[H+32] := rbp+kt`; over-logging is safe. hq_P implements, hq_C reviews the α/ω arms before the push; no sequencing wait — the mechanism is on origin (`9af884bd`).
⛔⭐ **hq_C's REVIEW OF THE α/ω ARMS (2026-09-02, on `salvage/hq_P-rung7-between-generator-wip-2026-09-02.patch`; a read, no build). Three points, one load-bearing.**
**(1) ⛔ WITHDRAWN — I REVIEWED A STALE ARTIFACT.** The point below was raised against `salvage/…wip…patch`, which banks the mark and never calls `rt_pl_disj_open`. hq_P's EMITTED arms *do* call it (`mov [pad], r12; lea rdi,[fb+kt-64]; mov rsi, fb; call rt_pl_disj_open`), so the point was already satisfied. Kept, struck, and named because the mistake is the reusable part: **a patch file in `salvage/` is a snapshot, not the tree, and a review of an artifact nobody promised was current grades a program that no longer exists.** Ask for the emission, which is what hq_P sent unprompted. The original text: ~~The α arm banks the mark but never OPENS the choice, so the β unwind is still a no-op.~~ `to_trail_mark()` is `mov [op_off+24], r12` and nothing else — no `rt_pl_disj_open`. Without the open, `B` is 0 or an older frame and `F.HI` is still the frame top, so the cells the generator's values go on to bind are **never logged**, and `rt_pl_tr_unwind` then walks an empty suffix. That is exactly the symptom hq_P measured, and exactly what the ruling above supplies; the patch predates it. **The α arm needs `lea rdi,[fb + kt-64]; mov rsi, fb; call rt_pl_disj_open` beside the mark**, or the unwind cures nothing.
**(2) `op_off + 24` is free ONLY IN THE ARM THAT WAS PATCHED, and nothing enforces which arm is reached.** Verified against `zeta_storage.c`: `IR_TO`'s grant is the 16-byte result plus `to.I` at `+16` and **`to.limit` at `+24`** — so `+24` is the LIMIT in the `op_zres` arm (written at α, re-read at `L(0)` every iteration) and in the `op_num_real` arm. It is unused only in the third, integer-static-operand arm, which keeps its limit in `op_sb+8` — the arm `between/3` happens to take today. ⛔ **And hq_P's stated reason for safety is wrong in a way that matters:** they wrote that "only the REAL-number arm uses it", but the `op_zres` (ζ-depth-staged) arm is an **integer** arm — it calls `to_int`, compares, writes `DT_I` — and it writes `+24` as the limit at α and re-reads it at `L(0)` every iteration. So the invariant actually relied on is not "`between/3` is integers-only" (true and irrelevant) but **"a pinned Prolog graph never takes the zd-staged arm"** — a property of the ζ-depth pass, unstated, unenforced, and not mentioned anywhere near the selector. The claim is therefore **correct as measured and unguarded as written**: a selector change, or any `between` reaching the other arms, silently aliases the trail mark onto the loop bound and the generator runs to a garbage limit. **Fix either way: grant `IR_TO` a fourth word for the mark (mirroring `IR_DISJUNCTION`'s pad, which is where rung 3 put exactly this) so the map reserves it rather than inspection; or bomb at the pinned arm when `op_zres`/`op_num_real` is set.** ⭐ The shape this page keeps meeting: *a slot that is free by inspection of the arm you are in, rather than by the grant that owns it, stays free only until someone changes the arm selector* — and nothing about that selector mentions the trail.
**(3) The MISSING ω arm is CORRECT and should say so, or someone will "fix" it.** "ω restores as `bb_disjunction` does" is satisfied by doing nothing: `bb_disjunction` deliberately leaves `F.HI` lowered on the way out, because over-logging is safe and restoring would need `kt` at a site that does not otherwise want it. Likewise the β needs **no** `test r15,r15` ball guard — § A.1 review C9 puts that on β arms that can hold a *retained callee*, and a generator leaf holds none. **And the `pl_lower_conj` hunk collides textually with rung 4**, which inserted the cut's `continue` immediately above that line; on rebase the cut test must stay ABOVE the generator test, or a cut to the left of a generator re-arms it as the clause's resumable.
⭐ **Cut × generator, checked on the rung-4 tree — NOT a blocker.** `bb_cut` zeroes `F.CUR`/`F.RES`, restores `B := F.B0`, and does `rsp := the pin`. A generator's cursor lives in its own box slot, not in `F.CUR`, so the cut does not reset it — and need not: rung 4's `cut_ω` rewiring sends every ω to the right of the cut out of the predicate, and `F.RES := 0` stops the graph β re-entering a banked sub-goal, so a generator LEFT of a cut is unreachable and one RIGHT of it stays correctly resumable. The cut does not restore `[H+32]`; a generator that lowered it stays lowered, which over-logs and is safe.

### B.14 All-solutions — `findall/3`, `bagof/3`, `setof/3` as a γ→β drive loop
(i) Box A with a frame: `A.α`: `F.ACC := empty`, `F.B0 := r10`, `F.B := rbp`, → `Goal.α`. `Goal.γ → collect`: copy Template to the heap, append to `F.ACC`, then FORCE FAILURE: → `Goal.β` (drive). `Goal.ω → finish`: undo `F.LOG`, `F.B := F.B0`, unify(Result, list(F.ACC)) → `A.γ` (bounded) or `A.ω`. `A.β → A.ω`. `bagof/setof`: same loop, then the free-variable grouping service (`rt_pl_bag_prep_cell :1229`, `rt_pl_bag_group_gen :1347`) yields groups — that outer generator is § B.13 with `F.CUR` over the groups. (ii) Jcon's `every E do B` (`irgen.icn:309-332`, Jcon paper READ p.25): `E.succeed → B.start`, `B.succeed → E.resume`, `B.fail → E.resume`, `E.fail → every.fail` — findall is `every` with `B` = "append and fail". Today: `$findall_new/$findall_add/$findall_result` and a forced-fail `IR_GOTO` to the goal's β (`lower_prolog.c:549-583`), `aggregate_all` the same shape (`:423-474`), and `pl_ll_maybe_lift` hoists non-trivial goals into `$faN/k` predicates (`:1253-1302`) so they have a β at all. (iii) `F.ACC` (two 16 B cells: head, tail) in A's frame; the collected copies on the heap (escapers by definition — they outlive the goal's frames); `F.LOG` is undone at finish so the template's bindings never leak. **Source: Jcon `ir_a_Every` (READ); Proebsting 1997 § 4.6 loops.**

### B.15 `assert/retract` and the dynamic database
(i) `assertz/1` etc.: bounded leaf (`α` → service → `γ`). The database itself: ζ-STANDING (process lifetime): the table of (name/arity → clause list, generation counter) and the clause bodies as heap cells or, for bodies to be CALLED, runtime-compiled blobs in the code slab (§ B.9). Calls to a dynamic predicate are § B.13's `clause/2`-style generator over the snapshot taken at α (logical update view: the generation number in `F.CUR`). (ii) No Proebsting template (Icon has no database); the port shape is § B.13. Today: `pl_dyn_mark` prepass (`lower_prolog.c:926`), `$dyn_iter` graphs (`:943-965`), `rt_pl_dyn_iter_gen` (`unification.c:1928`), tables `g_pl_dyn_pred_table` (`:1830`) and `g_pl_pred_table` (`:572`). (iii) The tables are ζ-STANDING by the law's own definition (root, process lifetime) and are not per-activation state — they stay as tables, not as globals reached by name from emitted code: one root-frame slot holds the database pointer (§ C). **Source: Kulas Remark 6 logical update view (READ p.6, citing Lindholm & O'Keefe 1987).**

### B.16 DCG — `phrase/2,3`
(i) Nothing new: the translation `-->` → clauses with two extra list arguments happens in the parser (`prolog_parse.c:289-337` conjunction flattening, `:649-661` fresh `_S%d` variables and tail appends); `phrase(G, L, R)` lowers to a plain call of `G/(n+2)` (`lower_prolog.c:608-627`). The boxes are § B.2/B.3. (ii)(iii) as § B.2. **Source: none needed — DCG is a source transformation in every system (SWI, gprolog manuals FETCHED name it as such).**

### B.17 `repeat/0`, `true/0`, `fail/0`, `once/1`, `forall/2`
`true`: `α → γ`, `β → ω` (Kulas S:true:1-2; today `IR_SUCCEED`, `lower_prolog.c:644`). `fail`: `α → ω` (S:fail; today `IR_GOTO ω`, `:645`). `repeat`: `α → γ`, `β → γ` — a choice with an infinite `F.CUR` (Jcon `RepAlt`, `irgen.icn:202-230`: `expr.failure → IndirectGoto(t)` where `t` alternates between `start` and `failure`; for `repeat` the resume simply re-succeeds), so the enclosing frame's `F.B := rbp` for as long as the repeat is live. `once/1` = `(G -> true)` § B.7; `forall/2` = `\+ (C, \+ A)` § B.8 (both already so lowered, `:407,:414`). **Source: Kulas (READ p.5); Jcon `ir_a_RepAlt` (READ).**

### B.18 Last call / tail recursion
(i) When the last goal L of clause k is a call and, at `L.α`, `F.B == F.B0` (this activation owns no live choice) and `rsp == rbp` (no retained callee frame lies below), P's frame can be REUSED: copy L's argument cells into `F.A[*]` (they may reference `F.G[*]` — the WAM's unsafe-variable rule: a local that escapes into the tail call's arguments is globalised to the heap first), pop nothing, jump `Callee.α` with P's own `F.γ/F.ω` wires and `F.LINK` — the callee inherits P's slot. Otherwise it is an ordinary call. (ii) Proebsting has no LCO (Icon procedures are not tail-recursive in the same sense); the WAM's `deallocate; execute` (Aït-Kaci 1991 § 5.8, RECORD). Today: none — recursion depth is bounded by frame stacking (`GOAL-PROLOG-100.md` PZ-9(a) "NO-LCO recursion cap"). (iii) The test reads two frame fields and `rsp`; nothing new. The static side (§ D) marks call boxes that are syntactically last and whose predicate is `deterministic` so the check is elided. **Source: Aït-Kaci 1991 (RECORD); Tarau's binarization (RECORD) is the continuation-passing statement of the same fact.**

---

## C. Every Prolog-only runtime global from the census, and where it goes

⛔⭐ **RULING ON EVERY "ζ-STANDING" ROW BELOW — **Lon, in-chat to ceo, 2026-09-02 ~14:20 (sixth ruling, box clock):** *"We are the main architects here. Let ensure we use the THREE ZETA's, and do not mis-use the STANDING ZETA as a surrogate global for things program wide."*** ζ-STANDING is the ROOT GRAPH'S OWN WORKING STORAGE — the program's named variables, declared or discovered at compile time (a SNOBOL4 natural variable, a `:- dynamic p/1` predicate, a constant-keyed `nb_setval` cell) — reached through `r14`; it is NEVER a surrogate global: no runtime-machine state (trail, choice, cursor, ball, counter, mode, flag of the engine) and no "table of everything" slot goes there. Under the FRAME-PLACEMENT CRITERION each datum belongs to the box whose lifetime it has: ζ-SPINE for fixed-offset temporaries within one activation, ζ-ACTIVATION-FRAME for what must survive a γ→β window, the HEAP for genuine escapers (strings, asserted clauses, collected terms, the ball), reached from the owning box's cell. Compile-time-constant data (the proc table, constant flags, the interned `'.'`) is RO in the code slab, not storage at all. Therefore, overriding the rows where they say otherwise: `g_pl_pred_table` → the compile-time RO proc table in the code slab (no storage); `g_pl_dyn_pred_table` → ONE NAMED ROOT CELL PER DYNAMIC PREDICATE (declared by `:- dynamic` or the `pl_dyn_mark` prepass, exactly as a SNOBOL4 natural variable is a root cell), holding the head of that predicate's heap clause list and its generation number — never a table of all dynamic predicates in one slot; `g_rt_pl_nb` → one named root cell per compile-time-constant `nb_setval`/`b_setval` key (a computed key is compiled through the runtime compiler, which sees the atom then and resolves the same cell — no map); `g_pl_flags` → RO constants in the slab, and `double_quotes` is a PARSER-TIME setting (it changes how the parser reads the text), so it is parser state, not runtime storage; `g_pl_cterm_*` → RO compiled terms beside the graph (as the row says); the root activation frame itself is the ROOT GRAPH'S frame (`F.A`/`F.G` of `main`'s box), not an extra record. `r14` ROOT addresses that frame so any box reaches a named root cell at a fixed offset.

Denominator: `FINDING-2026-09-02-ceo-prolog-only-globals-census-56-live-9-dead-and-what-the-term-deletion-removed.md` § A (the census counts a base/top/cap triple as one row; every named symbol is listed here). `nm -D out/libscrip_rt.so` at 11:35 exports 29 of them as data (`g_pl_copy_slot_ctr g_pl_copy_slot_mode g_pl_cp_cap g_pl_cp_stack g_pl_cp_top g_pl_env_area g_pl_functor_slot_ctr g_pl_retry g_pl_retry_cap g_pl_retry_top g_pl_trail g_plw_cellws_on g_plw_dot_sl g_plw_floor_bypass g_plw_unwind_floor g_pl_zf3_cap g_pl_zf3_stack g_pl_zf3_top g_pl_zf_pending_cursor g_pl_zf_pending_cursor_off g_pl_zf_pending_tm_hi g_pl_zf_pending_tm_lo g_pl_zf_pending_tm_off g_pl_zf_target_pcall_top g_resolve_active g_resolve_b3_call_mark g_resolve_bb_count g_resolve_bb_table g_resolve_cut_flag`). A row that says "survives" is an ASK to Lon (§ F), not a decision.

| global (today's home) | new home | why registers/stack cannot carry it (only where it survives) |
|---|---|---|
| `g_pl_trail` (`resolution.c:16`; `[g_pl_trail+32]` baked by `rtx_plcall.s`, `bb_define.cpp`, and the sinks `bb_call_fn.cpp:232,340`) | **deleted** → `F.LOG` of the youngest choice frame (§ A.1, § B.11) + heap chunks on overflow | — |
| `g_pl_env_area` (`resolution.c:48`, `rt_e_bump/mark/reset` `unification.c:536-552`) | **deleted, no equivalent**: the "environment" IS the frame (`F.A`, `F.G`); term cells built by a body live on the spine or the heap | — |
| `g_resolve_cut_flag` (`resolution.c:14`) | **deleted** → `F.B0` + the cut box (§ B.6) | — |
| `g_resolve_active` (`resolution.c:49`) | **deleted, no equivalent**: a "Prolog is loaded" bit is a language global past lower (`test_gate_emit_no_lang.sh`); its two readers (`polyglot_init`, `_usercall_hook`) key on the proc table instead | — |
| `g_resolve_mark_stack[32]`, `g_resolve_mark_top` (`unification.c:554-555`) | **deleted** → the mark is the frame (`F.LOG.n` at α is 0 by construction) | — |
| `g_pl_throw_ball` (`unification.c:2234`) | **deleted** → `r11` on the ω wire, claimed into `F.BALL` of the catch frame (§ B.10) | — |
| `g_pl_retry`, `_top`, `_cap` (`rt.c:1644`) | **deleted** → `F.RES` (§ B.3) | — |
| `g_pl_cp_stack`, `_top`, `_cap` (`rt.c:1664`) | **deleted** → `F.CUR` + `F.ACT[s]` tokens (§ B.3) | — |
| `g_pl_zf3_stack`, `_top`, `_cap` + `rt_pl_cp_push3/pop3` (`rt.c:1684-1708`) | **deleted** → `F.RES` + `F.LOG` (the 3-word CP was {trail-mark lo/hi, continuation} — both are frame fields now) | — |
| `g_pl_zf_pending_cursor`, `_cursor_off`, `_tm_lo`, `_tm_hi`, `_tm_off`, `g_pl_zf_target_pcall_top` (`rt.c:1710-1715`), consumed by `rt_jmp_frame_lexprep2` (`:1632`) | **deleted** → `F.RES`; β jumps `[F+24]` directly as Icon jumps `[H+32]`; `lexprep2`'s Prolog arm goes (its Icon arm, `rtx_icngen.S`, is not Prolog's to delete) | — |
| `g_pl_cterm_base/_cur/_end` (`rt_arena.c:88-90`) — compiled-term island | **ζ-STANDING**: compiled clause terms are program-lifetime RO data → emitted into `.rodata` / the code slab beside the graph that uses them (the `.Lsig` blob precedent); the bump island is deleted | — |
| `g_pl_cellws_base/_cur/_end`, `g_plw_cellws_on` (`rt_arena.c:131-134`; `SCRIP_PL_WS_RECLAIM`, default 0; the `$trail_mark` sink tests it, `bb_call_fn.cpp:340-362`) | **deleted, no equivalent**: a cell workspace with mark/reset is a second environment stack; cells live on the spine (fixed offset) or the heap (escaper) | — |
| `g_pl_pred_table`, `g_pl_pred_n` (`unification.c:572-573`) — static predicate α/redo addresses | **ζ-STANDING** as one language-blind table: `g_stage2.proc_table[]` already holds `name, bb_idx, nparams, is_generator` (`lower_prolog.c` registration); the runtime copy is deleted and `rt_pl_pred_alpha/beta` read the proc table | — |
| `g_pl_dyn_pred_table/_n/_cap` (`unification.c:1830`) | **ζ-STANDING**: the dynamic database (§ B.15), reached from ONE root-frame slot (the root activation frame is ζ-STANDING by name; a slot there is not a global) | — |
| `g_plw_preds[512]`, `g_plw_pred_n` (`by_name_dispatch.c:80`; read cross-language by `icn_builtin_is_known :55`) | **deleted** → the proc table above; the Icon reader keys on `proc_table` too, which closes the cross-language leak the census flagged | — |
| `g_plw_poison` (`by_name_dispatch.c:91`, env `SCRIP_PL_POISON_TRAP`) | **deleted, no equivalent**: a poison-trap on deref is a debugging arm of the deleted C solver; the port trace (§ D) replaces it | — |
| `g_plw_dot_sl` (`by_name_dispatch.c:118`; read by the sinks `bb_call_fn.cpp:232,379`) — the interned `'.'` functor slot | **ζ-STANDING → RO**: the atom id of `'.'` is fixed at intern time and never changes; emit it as an RO immediate into every sink that compares it (`x86_ro_load_q` precedent) | — |
| `g_plw_cwp/_n/_cap` (`by_name_dispatch.c:175`) — cell/arena mark pairs | **deleted** with the cell workspace | — |
| `pl_wot_stk[32]`, `pl_wot_sp` (`by_name_dispatch.c:1071-1072`) — `with_output_to` stack | **deleted** → one 16 B slot `F.WOT` in the frame of the box that lowers `with_output_to` (`lower_prolog.c:409-413` already reduces it to `$wot_begin … $wot_end/$wot_abort` around an ITE): the saved stream handle is per activation, LIFO with the frame | — |
| `g_plw_unwind_floor`, `g_plw_floor_bypass` (`by_name_dispatch.c:1465-1466`), `plc_dead_cstack` + its `/proc/self/maps` parser (`pl_cell.h:64-75`) | **deleted, no equivalent** (P7b): the floor exists only because a global trail can hold addresses of dead frames; a log owned by a live choice frame cannot | — |
| `g_pl_copy_slot_mode`, `g_pl_copy_slot_ctr` (`unification.c:1662-1663`), `g_pl_functor_slot_ctr` (`:636`) | **deleted, no equivalent**: counters for fresh-variable numbering during `copy_term`/`functor` are per-call temporaries → spine cells of the calling box, passed to the value service | — |
| `g_rt_pl_nb`, `g_rt_pl_nb_n` (`unification.c:1774-1775`) — `nb_setval` store | **ζ-STANDING**: global variables are program-lifetime by definition (ISO `nb_setval/2`); one root-frame slot holds the table | — |
| `g_pl_flags[]` (`unification.c:2090`) — `prolog_flag` table | **ζ-STANDING → RO where constant** (`bounded`, `max_integer`, `min_integer`), root-frame slot for the settable ones (`double_quotes`) | — |
| `rtx_gate_plunify` (`rtx_init.c:22`, env `SCRIP_RTX_PLUNIFY`) | **survives as a print-only/killswitch byte?** No — it selects code (asm vs C unify) at run time, a mode under ZETA HAS NO MODES; **deleted** once `rtx_plunify.s` is the only unify (its own row) | — |
| shared but Prolog-addressed: `g_zeta_mode` compared to 2 in the `$trail_mark` sink (`bb_call_fn.cpp:362`), `g_hp_fr` (`gc_heap.c:16`) | the `cmp` is deleted with the sink (a mode switch); `g_hp_fr` is the GC's, not Prolog's | — |

Compile-time globals (census § B: `g_pl_det_v[]`, `g_pl_disj_ctr`, `pl_ll_ctr`, `g_resolve_bb_table` 268 KB in the runtime image, the atom table, the parser statics) are lowerer state, out of this page's scope except one line: `g_resolve_bb_table` + `g_resolve_bb_count` (`resolution.c:50-51`) leave `libscrip_rt.so` for the lowerer's TU (the census already says nothing reads them at run time). The nine dead ones (census § C, e.g. `g_pl_catch_nodes[]` `emit.cpp:3630`) are deleted with no replacement. **Result: zero Prolog-only runtime globals survive; the four ζ-STANDING tables are reached through root-frame slots, not by name.**

---

## D. The debugging and analysis dividend

**Byrd's trace falls out of the wiring.** Every port transfer already passes ONE chokepoint: `x86_port_hook(site, port)` (`x86_asm.h:419`, defined `:1979`) is called by `x86_jmp`/`x86_jcc`/`x86_deflabel` for every α/β/γ/ω (`:427,:488,:499`; the port emitters `x86_alpha…x86_omega` at `:502-507`). A trace arm there — armed by `SCRIP_PL_TRACE=1`, print-only telemetry, the `SCRIP_ZETA_OMEGA_TRACE` carve-out — emits `call rt_port_trace(port, box_id)` with `rbp` live; the runtime prints `(N) D Port: goal` by reading `F.A[*]` through a per-graph RO descriptor blob (name, arity, the A offsets — the `.Lsig` blob shape, `xa_flat.cpp` CLASS-C prologue) and the depth D by walking `F.LINK` (`[rbp+kt-8]`). Exception vs Fail is `r11 ≠ 0` at an ω hook; `*EXIT` vs `EXIT` is `F.CUR` exhausted or not. The invocation number N is the one piece of program-lifetime state: a counter in ζ-STANDING (the root frame; the per-box cyc-proxy histogram of `ARCH-ENGINE.md` § Per-Box Profiling is the precedent). No interpreter, no second engine, and — the measured gap PZ-0 found — the ZSM ring that sees ZERO Prolog boxes today (`runtime_init.c`, `SCRIP_ZSM`) starts seeing them the moment Prolog boxes go through the same emitters.

**A port-hit profiler is the same hook with a counter instead of a print:** `count[box_id][port]++` in a ζ-STANDING array sized by the number of boxes (known at emit time). CALL counts per predicate, REDO/CALL ratio = how nondeterministic a predicate is in practice, FAIL/EXIT ratio = how often indexing would have saved a clause try — the three numbers the WAM literature tunes on.

**Static analyses the port graph enables** (each is a compile-time property of the wired graph, none needs a runtime bit):
- **Determinism** = Proebsting's `bounded`: a box is bounded iff its β chunk is unreachable — no clause after the current one can be a candidate (§ B.13 indexing leaves one), every body goal is bounded, no `;` or `repeat` without a following cut. Today `pl_det_compute` (`lower_prolog.c:1202-1217`, `pl_det_pred_ok :1191`) is a syntactic whitelist (`:1139-1140`) that stamps `deterministic = 1` (`:1228`); the port-graph version replaces the whitelist with reachability of β and is language-blind (the emitter's `op_beta_dead` already elides a dead β trampoline, `x86_asm.h:504`). A bounded predicate emits no β chunk, no `F.CUR`, no `F.RES`, and its γ releases the frame — the whole cost of choice disappears where the program never chooses.
- **First-argument indexing as a compile-time choice cut**: the `$ix_g` guards (`lower_prolog.c:827-838`, sunk at `bb_call_fn.cpp:379`) become the WAM's `switch_on_term` at `P.α`: the A0 tag selects the candidate SET (a compile-time bitmask per key), `F.CUR` iterates only that set, and when the set is a singleton `F.B` is not set to `rbp` — the call is bounded at run time even when the predicate is not bounded statically. The trace shows this as an EXIT without `*`.
- **Last-call detection**: a call box that is syntactically last in its clause AND whose enclosing predicate is bounded needs no `F.B == F.B0` check (§ B.18); the emitter marks it and the frame-reuse jump is unconditional.
- **Cut analysis**: a clause whose cut is the first body goal (`p(X) :- !, …`) makes every later clause unreachable for calls that unify its head — the candidate set after indexing is pruned statically, which is what turns a "guard-complement pair" (`pl_det_guard_comp :1176`) from a special case into an instance of the general rule.

---

## E. Implementation order — THE CONSTRUCT LADDER FROM RUNG 0 (Lon 2026-09-02, in-chat to ceo, ~13:40–13:55, box clock)

⛔ **THE LADDER'S RECEIPT RULE (ceo 2026-09-03, from hq_C's rung-4 receipt; RULES § THE INSTRUMENT LAWS batch 14 corollary):** every rung receipt reports the REPORTED master board's NEW FAILs and new PASSes BY NAME (`SUITE_LIST_ALL=1` — the harness caps its per-entry listing at 40 lines), because a rung's own witnesses cannot find a defect in a construct the rung merely UNBLOCKED: rung 4 found two rung-2 defects only by reading the board's new FAILs by name. **And the trace arm is owed at LANDING (hq_P's rule, ceo-adopted 2026-09-03 14:45):** every rung baton's DONE-WHEN carries `bash scripts/test_gate_pl_port_trace.sh --only N` immediately after its ladder arm — rung 7 landed `--only 7` 4/4 green with ZERO rung-7 trace blocks and only the audit found it (NOREF is honestly not-a-pass).

⛔⭐ **Lon's rulings, verbatim in substance, in order:** (1) *"So do we not just begin at rung 0 and begin climbing the ladder for Prolog language constructs starting at simple and climbing to complex?"* (2) *"Do we not just delete ALL the globals on the list and start from scratch. I think so."* (3) *"Have you laid out which BB's you will need for Prolog based on all the white papers you read and that are found in /home/resources folder."* (4) *"Are we not ready for a hello world Prolog program with zero globals and begin to rebuild?"* (5) *"Keep in mind to share BB's with Icon and SNOBOL4. No need to create a new BB that does exactly what one already does."* The ceo answered yes to all five with the justification in `FINDING-2026-09-02-ceo-prolog-rebuilds-from-rung-0-hello-world-with-zero-globals-the-cut-the-construct-ladder-and-the-shared-box-inventory.md`; the mechanism-ordered § E of this morning (BX-0 → 2.0 → PZ-4 → PZ-5 → PZ-2 → PZ-3 → P7b → C37 → C36 → P6 → BX-IX → BX-LCO → BX-DET → T7/T9) is RETIRED AS AN ORDER — each row survives in § E.2, mapped to the construct rung that lands it. Law: `RULES.md` § THE PROLOG REBUILD GATE.

**THE CUT — rung 0, step (a), ONE push, hq_C.** Delete every row of § C and the control machine that reads it: the `plc_*` solver and the by-name cascade for Prolog (`by_name_dispatch.c`, 21 statics, `rt_call_arr_bl` as reached from Prolog graphs), the unwind floor and the `/proc/self/maps` parser (`g_plw_unwind_floor`, `g_plw_floor_bypass`, `plc_dead_cstack`), the CP/retry/zf3/pending-cursor machinery and `rt_jmp_frame_lexprep2`'s Prolog arm (`rt.c:1632-1727`), the env area / mark stack / throw ball / copy counters (`unification.c`), `resolution.c` (69 lines), the cterm and cellws islands (`rt_arena.c`), the `$trail_mark`/`$unify` sinks' global references (`bb_call_fn.cpp`), the Prolog arms of the 12 templates that branch on Prolog identity, `bb_cell_ite.cpp` + `bb_cell_cut.cpp`, and the control lowering in `lower_prolog.c` (`thread_goals`, `dj_expand_node`, the `$disjN` hoist, the `$catch_check`/`$no_throw_or_fail` guards, `pl_ensure_call_bridge`). **NOT deleted — past this line the Term work restarts:** the parser (PASS 45), the DESCR cell layer, the atom table, `rtx_plunify.s`, the `dop_*` value bodies, the shared boxes, the BX-0 hook (`x86_port_hook`). ζ-STANDING data (dynamic DB, `nb_setval`, `prolog_flag`) become ONE root-frame slot each when rung 10 lands. Nothing is switched off; the old machine is DELETED (ZETA HAS NO MODES). Measured on `f4532dea` before the cut, so the cut's receipt has a control arm: `nm -D out/libscrip_rt.so` exports **26** Prolog-only data symbols; `hello.pl`'s `--compile` output references **0** Prolog-only globals but makes **3** `rt_call_arr_bl` and **1** `rt_plw_floor_bypass_on` calls, and its BX-0 trace shows the two leaves without a `main` box (`Call n0_lit_string … Exit n3_call_prolog $nl0`).

**THE LADDER.** Every rung is one push by one seat, started from origin. Each rung lands one witness program per construct as a `ladder__rungNN_<slug>` entry in `corpus/tests/prolog/ALL.pl` (`.ref` cut from `swipl`, its trace block in `ALL.trace`), and the ladder runner `scripts/test_prolog_ladder.sh --to N` (rung 0 mints it) runs rungs 0..N in BOTH modes, prints its denominator, REFUSES rc=2 on zero witnesses, and has failed once and passed once before its verdict is quoted. Hard on every push because the boxes are shared: SNOBOL4 FAIL=0 over the printed denominator, the Icon watermark unchanged, `nm -D` names none of § C, `strip_comments.py --check` 0. The Prolog master board is REPORTED with every receipt (348/371 of 371 on `f4532dea` is the last pre-cut reading) and is not a landing gate until rung 10.

| rung | construct(s) — the witness | § B | box (§ B.0) | owner | DONE-WHEN (a measurement) |
|---|---|---|---|---|---|
| **0** | **hello world** `:- initialization(main). main :- write(hello_world), nl.` — one call box with the § A.1 frame, two det leaves wired at compile time; the cut first | B.1 B.12 | `bb_call_proc_staged`, `bb_call` → `dop_write`/`dop_nl` | **hq_C** | the cut receipts (`nm -D` 0 of the 26; `plc_` statics 0); `--compile` of the witness references 0 Prolog-only globals AND 0 `rt_call_arr_bl`/`rt_plw_floor_bypass_on` calls; the frame is rbp-relative in the `.s`; `SCRIP_PL_TRACE=1` prints `Call main` … `Exit main` around `write` and `nl`; output = swipl both modes; `test_prolog_ladder.sh --to 0` PASS over a printed denominator ≥ 1; `make test` rc=0 |
| 1 | fact with arguments + rule body: `p(a,b). main :- p(X,_), write(X), nl.` | B.1 B.2 B.4 B.11 | `bb_conjunction`, `bb_call` → `rtx_plunify.s` with the log rule via `r13` | hq_C | head unification binds `F.A[i]` on the spine; `--to 1` PASS; trace = ref |
| 2 | multi-clause choice + redo: `p(1). p(2). p(3). main :- p(X), write(X), nl, fail. main.` | B.3 | `bb_call_proc_staged` β chunk = Jcon `Alt`, `F.CUR F.RES F.B` in the frame (absorbs PZ-4 (b)-(f), PZ-5) | hq_C | prints `1 2 3`; β jumps `[H+24]` (grep the `.s`); no pending-cursor symbol anywhere; `nested.pl`, `deep.pl` green both modes; `--to 2` |
| 3 | inline disjunction: `p :- (write(a) ; write(b)), nl, fail. p.` | B.5 | `bb_disjunction` with `F.RESd` in the enclosing frame | hq_C | prints both branches without `$disjN` hoisting; Icon watermark unchanged (the box is Icon's too); `--to 3` |
| 4 | cut | B.6 | `bb_cut` grown to the barrier | hq_C | cut-commit + cut-under-backtrack witnesses; `p :- q, !, p.` runs 10⁶ iterations under `ulimit -s 8192`; `--to 4` · **CORRECTED (ceo 2026-09-03, on hq_C's rung-4 receipt):** the `p :- q, !, p.` at 10⁶ under `ulimit -s 8192` criterion is rung 11's (LCO) — measured: 10⁴ passes, 10⁵ overflows, each recursive activation keeps its own frame; rung 4's own measure is the cut's `rsp := pin` release doubling the depth ceiling with a retaining callee (10000 → 20000, A/B by hq_C) |
| 5 | if-then-else, `\+`, once, forall, ignore | B.7 B.8 B.17 | `bb_bound` + `bb_indirect_goto`/`bb_move_label` | hq_C | the ITE witnesses incl. a condition that throws (row `prolog-if-then-else-swallows-…` stays green); `--to 5` · **PLAIN DIRECTIVE GOALS (ceo ruling 2026-09-02 18:50):** `:- Goal.` that is not a declaration is `ignore/1` at the root graph, file order, BEFORE the initialization goals, with swipl's `Warning: … Goal (directive) failed` on stderr — lands with this rung; the lowerer refuses it naming rung 5 (was 10) |
| 6 | det builtins `is/2`, compare, type tests, atom ops (P6) | B.12 | `bb_call`/`bb_call_fn`/`bb_binop_relop` → `dop_*` direct | **ceo** (moved from hq_P on Lon's 2026-09-02 16:55 CDT order *"Just start climbing the rungs from simple syntax to more complex syntax as you go along"* — the ceo climbs; hq_C holds rung 2; rung 6 is the first rung independent of the β chunk and the largest first-refusal population, 132 of 332 on `7d0ed0f8`; hq_P reviews the landing as the perf owner) | ~~`grep -c _rkfn …` = 0~~ (VACUOUS — hq_P measured 2026-09-02: the glob matched no benchmark file, `_rkfn` was 0 before the work, and the by-name symbol Prolog used is `rt_call_arr_bl`) → **the six `ladder__rung06_*` witnesses freshly `--compile`d rc=0 with `rt_call_arr_bl` = 0 and `_rkfn` = 0 in that fresh output; `test_prolog_ladder.sh --only 6` PASS over its printed denominator (12/12 at the landing, SCRIP `b19efced` · corpus `62dc7995`); `--to 6` when rungs 2–5 are on origin**; the demo artifacts regenerate to honest output (row `regen-demo-s-artifacts-writes-a-refused-marker-for-a-refusing-program`); rung-suite witness `rung22_write_canonical_write_canonical_list` green both modes against the swipl pin `[a,b]` (row `prolog-rung-red-class-write-canonical-is-ladder-rung-6`) · **THE TAIL LANDED (ceo, 2026-09-02 19:45, SCRIP `5d12c898` · corpus `16fb9da07`, receipt `FINDING-2026-09-02-ceo-rung-6-tail-lands-every-stream-arity-builtin-as-a-leaf-over-the-fh-registry-format-3-sinks-open-close-keysort-declarations-as-compile-time-facts-and-op-3-reaching-the-writer.md`; `--only 6` PASS 20/20, ten witnesses):** the stream-arity leaf family over the driver `fh_` registry (`write/2 writeq/2 print/2 write_canonical/2 writeln/2 nl/1 tab/2 put_char/2 flush_output/1 format/3` with `atom(A)/string(S)/codes(C)/chars(C)` sinks, `read/2 get_char/2 peek_char/2`, `open/3,4 close/1,2 current_output/1 current_input/1 set_output/1 set_input/1`; a stream is the alias atom or `'$stream'(N)`; a bad stream FAILS, ISO stream errors are rung 9's), `read_term/2` with `[]`, `keysort/2`, declaration directives as no-ops (`op/3 multifile discontiguous ensure_loaded use_module`, `dynamic` recorded → a clauseless declared predicate lowers to fail); witnesses `ladder__rung06_stream_*`; `--only 6` is the landing gate; `op/3` is BOTH parse-time and runtime (the writer honours user ops through the parser's op table; a mode-4 binary declares them through the `$op` leaf ahead of `main`); NOT landed, named in the receipt: the byte family, `write_term`, `with_output_to`, `sort/4`, `read_term` options, stream error terms, postfix ops in the writer |
| 7 | generators `between/3` (`bb_to`), `clause/2` `retract/1` `sub_atom/5` (`bb_call_builtin_gen`+`bb_suspend`), `repeat` (`bb_repalt`) | B.13 B.17 | shared | hq_P | `between(1,N,X), fail` visits N values both modes (hq_P measured 2 of N on `f4532dea`); `--to 7` · **RULED 2026-09-02 20:30 (ceo):** the generator opens its choice through `rt_pl_disj_open` at α exactly as the inline disjunction does (§ B.13 (i), superseding the bare `F.B := rbp`); hq_P's salvage patch (`postoffice/salvage/hq_P-rung7-between-generator-wip-2026-09-02.patch`: trail mark at α + β unwind, the lvalue-on-the-spine ordering) becomes correct the moment the open exists; witnesses graded on prolog+raku+icon `IR_TO` sites |
| 8 | findall / bagof / setof | B.14 | `bb_iterate`/`bb_every` + collect leaf, `F.ACC` | hq_P | `--to 8`; the van Roy two-number pin is taken HERE, first grid; rung-suite witnesses `rung11_findall_findall_{arith,filter}` `rung44_setof_group` `rung50_between_enum` `rung50_for_alias` green both modes (row `prolog-rung-red-class-findall-bagof-between-is-ladder-rung-8`) |
| 9 | catch / throw (C37) | B.10 | **`bb_catch`** (new) | hq_C | `grep -c rt_pl_throw_pending src/` = 0; cross-clause catch/throw witnesses + `rung66_current_stream.pl` green both modes; the trace prints `Exception`; `--to 9` (row `prolog-rung-red-class-rung66-streams-and-existence-error-is-ladder-rung-9`) |
| 10 | `call/N`, assert/retract, dynamic DB, DCG (C36) | B.9 B.15 B.16 | `bb_call_value` + the runtime compiler | hq_C | `call/1..8` witnesses; `--to 10`; from here the master board is a gate again (≥ the reported pre-cut 348 both modes); rung-suite witnesses `rung14_retract_*` (2) `rung15_abolish_*` (4, degenerate: no initialization directive, empty swipl output) `rung45_reflect_clause_*` (2) green both modes (row `prolog-rung-red-class-dynamic-db-assert-retract-abolish-clause-is-ladder-rung-10`) · **`nb_setval/nb_getval/b_setval/b_getval` and the flags (ceo ruling 2026-09-02 18:50):** ζ-STANDING slots in the root graph keyed at compile time (the key is an atom literal; `b_setval` trails the slot's old value through the ctx), never a surrogate global — land HERE with the DB, not as leaves |
| 11 | last call (BX-LCO) | B.18 | α arm on `bb_call_proc_staged` | hq_P | `nreverse` on 10⁵ and `count(N)` to 10⁶ under `ulimit -s 512`; rss flat across N (`tools/bench_rusage`) · inherits from row 4 (ceo 2026-09-03): `p :- q, !, p.` at 10⁶ under `ulimit -s 8192` passes both modes |
| 12 | indexing (BX-IX) | B.3 | static choice cut at α | hq_P | on `fact/2` with distinct constant first args `--compile` emits ZERO β labels for the predicate; trace shows `Exit` not `*Exit` |
| 13 | determinism (BX-DET); T7/T9 policing | § D | — | hq_B | `SCRIP_DET_REPORT` DET count on the master ≥ today's, every DET predicate emits no β chunk; `test_gate_term_wordref_ratchet.sh` stays 0/0 |

**Instrument lane, parallel to rung 0 (never on `src/`):** hq_B mints `test_prolog_ladder.sh` (cumulative, both modes, REFUSE rc=2, denominator printed) and cuts the swipl `.ref`/`.trace` for the rung 0–5 witnesses into `ALL.pl`/`ALL.trace` ahead of the rungs; hq_P mints the quad gate — no `r12`–`r15` write reachable from a Prolog graph outside the TR/B/ROOT/BALL helpers, in emitted `.s` and in `rtx_*.s` (`r12`–`r15` are callee-saved under SysV, so every C service preserves them for free) — and keeps the van Roy pin parked until rung 8.

### E.2 The retired mechanism rows, mapped to the rung that lands them

| row | disposition |
|---|---|
| BX-0 the instrument | LANDED `f4532dea` (hq_P) — the ladder's grading arm |
| the quad gate (instrument lane) | LANDED `9623ff55` (hq_P) — `test_gate_pl_quad_regs.sh` + `pl_quad_regs_scan.py`, last in `make test`: every r12–r15 write in emitted mode-4 TEXT is graded by (site, register, shape) against an explicit enrolment table; rtx routines the program reaches are swept transitively, TR helpers admitted by routine NAME (`QUAD_HELPER_RX`); three canaries red on every run. GREEN on rung 0 (5 writes = the § A.1 ROOT seeds; rtx 3 reachable / 0 writes). Supersedes the born-red label allow-list of `4253dd88`. A rung that adds a B/TR/BALL writer enrols ONE shape row, never a box-wide admission — `FINDING-2026-09-02-hq_P-quad-gate-lands-green-at-rung-0-by-site-and-shape-superseding-the-born-red-label-allow-list.md` |
| 2.0 re-home r12/r9 (`prolog-rung-2-0-rehome-scratch-r9-and-r12-off-prolog-reachable-templates`) | SUPERSEDED: the 16 sink sites are deleted by the cut; what survives is hq_P's quad gate above. Two brief corrections from hq_B's measurement (2026-09-02, zero source changed) are kept: the corpus is a SIBLING root (`$S4E_HOME/corpus`, never `$S4E_HOME/SCRIP/corpus`), and in `xa_flat`'s PL-DC γ/ω shims `rcx` is a live argument, so the return-address temp there is `r8`, the dead one |
| PZ-4 (b)-(f) | rungs 0 and 2 |
| PZ-5 | rung 2 |
| PZ-2 | rung 3 |
| PZ-3 | rung 4 |
| P7b | the cut |
| P6 | rung 6 |
| C37 | rung 9 |
| C36 | rung 10 |
| BX-LCO / BX-IX / BX-DET | rungs 11 / 12 / 13 |
| T7 / T9 | DONE (hq_B rung 13, `a29ea1fd`), policed by the ratchet gate |

Program gate after rung 10: ladder 0..10 PASS both modes, van Roy REFUSE 0 AND CRASH 0 AND CLEAN ≥ floor WORST-OF-N, `nm -D` names none of § C, master ≥ the reported pre-cut 348 both modes.

---

## F. Open questions for Lon (five, each with the recommended answer)

1. **Where does a binding log?** — bind-time into the youngest CHOICE frame's `LOG` via the per-frame `F.B` slot (the WAM's rule; zero copying on the deterministic path; copying only at cut), versus the literal reading of "each activation's own log" with promotion at every bounded γ. **Recommend the first**; § B.11 states why the literal reading breaks (a deterministic callee's binding of a caller cell must outlive the callee's frame).
2. **Is a log chunk a lawful escaper?** — when a retained choice's `LOG` outgrows its inline `K` entries, the overflow goes to heap cells (24 B each, `PTR_GC`-mapped). **Recommend yes**: it is exactly the "retained across a γ and grows without bound" case the criterion names, and the alternative (a fixed cap) is a REFUSE.
3. **The ball's carrier** — ⛔ `r11` is taken (BB node id) and `r10` is taken (statement number), so the choice is a free wire register from the plane versus a `BALL` slot in every catch frame. **ANSWERED by Lon's 2026-09-02 grant: the ball rides `r15`, callee-saved, no frame slot**: no per-frame cost, one register test at catch boxes only; intermediate ω paths must preserve `r10:r11` around helper calls (an emitter rule, gated by `test_gate_port_exit_value_contract.sh`'s sibling).
4. **The ζ-STANDING tables** (dynamic database, `nb_setval`, settable flags, the proc table) reached through root-frame slots — is a root-frame slot "not a global" under NO-NEW-GLOBALS, or does each need the banner ask? **Recommend: root-frame slots are ζ-STANDING by the law's own name and need no banner**; the page still lists them so the count is honest.
5. **Heap cells always logged (no HB)** — the compacting collector moves the heap mark, so the WAM's `HB` test is not available for free; log every heap-cell binding and measure on van Roy before designing a generation stamp. **Recommend accept for PZ-4, revisit under PZ-10 with a number.**

---

## G. Literature — what was read, what was fetched, what was only found, what was not found

**⭐ THE LIBRARY IS ON DISK (ceo 2026-09-02, on Lon's order *"find the original Lawrence Byrd paper and all related white papers"*): `/home/resources/byrd-box-literature/` — 28 PDFs, 18 manual pages, 4 abstracts, every PDF/HTML with a grep-able `.txt` sibling, `INDEX.md` with citation, source URL, HTTP result, size and STATUS per item, and a section "WIRING SOURCES, RANKED" giving, per construct, the document and section that pins its port wiring most exactly. Read that table before wiring any construct.** What it changes in the ledger below: **Byrd's own text IS in hand** — "Prolog debugging facilities" (DAI Occasional Paper 19, the DEC-10 `DEBUG.MEM`, `1980-byrd-prolog-debugging-facilities-DEBUG.MEM.txt`) carries the four-port figure and the CALL/EXIT/REDO/FAIL definitions in his words, and the 1982 DECsystem-10 Prolog User's Manual § 2.1 "Procedure Box" (p. 13) is the same text tightened; the Debrecen workshop paper (DAI Research Paper 151) exists only as a physical item (Computer History Museum catalogue) and was not found online anywhere. New full texts beyond the table below: **Kulaš 2000 "A Rewriting Prolog Semantics" — § 3.9 cut, § 3.10 catch/throw, § 3.13–3.16 if-then(-else) "correct with respect to cut", § 3.17–3.18 disjunction transparent for cut, § 3.5–3.6 meta-call opaque for cut, § 3.11 findall, § 5 "Specifying the Byrd model"** (the single most exact wiring source for the constructs § B had to design itself); Jahier, Ducassé, Ridoux LPE'99 (the open short version of the 2000 paper, ports as events over success/failure continuations, with cut); Tarau BinProlog § 3.3 "Handling CUT" and § 7.5 exceptions; Andrews on cut; Ducassé & Noyé 1994; Opium RR-3257/RR-3531; Kulaš's five other papers; Quintus/YAP/Ciao port definitions. Still not obtained: Tobermann & Beckstein 1993, Jahier LOPSTR 2000, Eisenstadt & Brayshaw 1988/1990 (abstracts only, paywalled), Clocksin & Mellish ch. 8, Lindgren 1994, Felleisen TR 182. Three PDFs are image scans with empty `.txt` (Finkel & Solomon, Haynes, Warren TN 309) — no OCR tool on the box. **Consequence for § B.6 and § B.10:** the cut box and the catch box are re-derived against Kulaš § 3.9/§ 3.10 and Jahier § 3 before PZ-3 and C37 land; hq_C's review FINDING cites the section for each port it wires.

**Searches run (WebSearch, 2026-09-02):** Byrd 1980 four-port box (×2, incl. Debrecen/Tärnlund pages); Proebsting 1997 PLDI (×2); Proebsting & Townsend 2000 Jcon; Warren 1983 + Aït-Kaci 1991; "Byrd box" debugger/continuation/compilation; Clocksin & Mellish box-model chapter; Tobermann & Beckstein 1993; Eisenstadt & Brayshaw TPM/AORTA; Ducassé Opium (×3); SICStus Procedure Box; Bratko + Sterling & Shapiro trace models; box model + if-then-else/negation/findall/call lecture notes; Prolog CPS compilation (Lindgren, Tarau).

| source | what this page took from it | verification |
|---|---|---|
| Byrd, L. 1980. *Understanding the control flow of Prolog programs.* Proc. Logic Programming Workshop, Debrecen, ed. S.-Å. Tärnlund, pp. 127–138; also DAI Research Paper 151, Edinburgh | the four ports and the trace they define | RECORD (Stanford SearchWorks; cited verbatim by Deransart et al. 2007 FETCHED and by Proebsting 1997 READ as "[Byr80] … Technical Report 151, University of Edinburgh, 1980"). **NOT FOUND online as text.** |
| Proebsting, T. A. 1997. *Simple Translation of Goal-Directed Evaluation.* PLDI '97, pp. 1–6 | the four chunks, every template in § B, "ports are locations, whereas here they are pieces of code", the `ifstmt` gate, the observation on Byrd's granularity | READ (6 pages, `www2.cs.arizona.edu/icon/jcon/gde97.pdf`) |
| Proebsting, T. A. & Townsend, G. M. 1999/2000. *A New Implementation of the Icon Language.* U. Arizona TR 99-13 / MSR-TR-99-64; SP&E 30(8):925–972 | `vClosure` = `retval` + `Resume()`; suspension stores temporaries and the resume PC in the closure (§ 8.2.2: "no analysis is done to determine which temporaries actually need to be saved … the compiler conservatively stores all of them"); templates for `if`, `every`, `suspend`, `break`, `create` | READ (pp. 1–36 of the TR PDF) |
| Jcon `tran/irgen.icn` (`SCRIP/refs/jcon-master/`) | `ir_a_Alt`, `ir_a_RepAlt`, `ir_a_Limitation`, `ir_a_Not`, `ir_a_Every`, `ir_a_Suspend`, `ir_a_Call`, `ir_conjunction`, the `bounded` elision | READ (local file) |
| Danvy, O., Grobauer, B., Rhiger, M. 2001. *A Unifying Approach to Goal-Directed Evaluation.* BRICS RS-01-29 / New Generation Computing 20(1) | the two-continuation reading: "the success continuation takes a failure continuation as a second argument. This failure continuation determines the resumption behavior"; Proebsting's templates "are intrinsic to the semantics of Icon and can be provably derived" | READ (pp. 1–10) |
| Kulas, M. 2003. *Pure Prolog Execution in 21 Rules.* arXiv cs/0310020 | port definitions quoted in § A; the port-transition rules for `,` `;` `true` `fail` `=` and atomary goals (Fig. 2); the logical update view memo | READ (pp. 1–6) |
| Deransart, P., Ducassé, M., Ferrand, G. 2007. *Observational Semantics of the Prolog Resolution Box Model.* arXiv 0711.4071 | the exact Byrd citation; "we do not describe exactly the model initially defined by Byrd"; no cut in the model | FETCHED |
| SICStus Prolog manual 3.7.1 § Debugging and 4.5.1 § Procedure Box | port definitions (Call/Exit/Redo/Fail/Exception, plus Block/Unblock in 4.5.1); "also called the Byrd Box model after its inventor, Lawrence Byrd"; the cut-out invocation sentence | FETCHED (both) |
| SWI-Prolog manual: *The Byrd Box Model And Ports*; *Overview of the Debugger*; `prolog_trace_interception/4` (4.0.11 mirror) | six ports incl. `unify` and `exception`; `cut_call(PC)`/`cut_exit(PC)`; exception propagates frame by frame; exit "possible for the goal to have alternative" | FETCHED (three pages) |
| ECLiPSe user manual, *The Box Model* | `NEXT`, `ELSE`, `*EXIT`, `LEAVE`, `DELAY`, `RESUME` ports | FETCHED |
| GNU Prolog manual § The procedure box model | five ports; defers to Clocksin & Mellish | FETCHED (inria mirror; gprolog.org's certificate is broken) |
| Clocksin, W. F. & Mellish, C. S. *Programming in Prolog*, ch. 8 *Debugging Prolog Programs* | the four-port presentation every manual above inherits | RECORD only (chapter named by the Amzi debugger manual and search snippets); book not fetched |
| Warren, D. H. D. 1983. *An Abstract Prolog Instruction Set.* SRI TN 309 | environments vs choice points ("the stack contains compiler-defined goals called environments") | FETCHED (SRI abstract page) |
| Aït-Kaci, H. 1991. *Warren's Abstract Machine: A Tutorial Reconstruction.* MIT Press | B, B0, TR, HB, E, CP, the choice-point frame fields, cut `B ← B0`, `deallocate; execute` | RECORD (MIT Press/ACM/GitHub mirror); chapter contents from the tutorial as remembered, **not re-fetched** |
| Tobermann, G. & Beckstein, C. 1993. *What's in a trace: The box model revisited.* AADEBUG '93, LNCS 749, pp. 171–187, DOI 10.1007/BFb0019408 | the extended box model with cut (named by the coordinator) | RECORD (dblp FETCHED); **text not reached** (Springer cookie wall, three attempts) |
| Jahier, E., Ducassé, M., Ridoux, O. 2000. *Specifying Prolog Trace Models with a Continuation Semantics.* LOPSTR 2000, DOI 10.1007/3-540-45142-0_10 | box model with cut formalised by continuations | RECORD; **text not reached** |
| Eisenstadt, M. & Brayshaw, M. 1988/1990. *The Transparent Prolog Machine (TPM)*, J. Logic Programming; *A fine-grained account of Prolog execution for teaching and debugging*, DOI 10.1007/BF00116447 | AORTA diagrams as the alternative to the box trace | RECORD; **text not reached** |
| Ducassé, M. *OPIUM: An Extendable Trace Analyser for Prolog* (1999) | a trace-query layer over the same ports | RECORD only |
| Lindgren, T. 1994. *A continuation-passing style for Prolog*; Tarau, P. *BinProlog* (binarization; catch/throw on continuations, arXiv 1102.1178) | the continuation statement of cut/catch/LCO used in § B.10, § B.18 | RECORD only |
| Finkel & Solomon 1980 *Nested iterators and recursive backtracking* (UW-Madison TR 388) | Proebsting's other four-port ancestor (power loops) | RECORD (from Proebsting's references) |
| Sterling & Shapiro *The Art of Prolog*; Bratko *Prolog Programming for AI* | asked for by the coordinator | **NOT FOUND**: searches returned no page describing their trace/box chapters; neither book fetched; not relied on |

**Constructs where no source gives a wiring and this page supplies one:** cut as a box with token drop + log promotion (§ B.6 — the manuals describe the observable, the WAM describes `B ← B0`, neither is a box); the catch box's β-handler (§ B.10); findall as `every` (§ B.14 — Jcon's loop, not a Prolog source); LCO as a frame-field test (§ B.18). Each is marked as the page's own design, not as literature.
