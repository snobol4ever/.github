# HANDOFF 2026-06-06 — Opus 4.8 — BB-FIXUP 6th attended run: stop 18 + RULE CORRECTION + ring reset

## Landed
- **Stop 18 — `bb_term_inspect.cpp` → SCRIP `5165372`.** TIER H under the OLD rule: ef 51→0, pe 3→0 (BINARY ports→literal Greek glyphs, `x86_port_of` parse-path verified first), lv 24→0 (14 `bti_*` helpers, signature-line decls, strtab_label registration ORDER preserved per arm), dead `succ_back` excised, comments purged; 161→84. Asm-diff EMPTY twice: pre-rebase (12 files: 9 corpus + 3 probes) AND post-rebase parent-vs-mine on the merged tree. LIVE probes fire ALL 8 TEXT arms (functor t/s, arg t/s, univ tt/t1/1t/ss); m2/m3/m4-run A/B identical. Concurrents merged green ×3 mid-stop (PL-GZ-7b, ICN-HY-7f, PB-12); a probe "diff" was wholly PL-GZ-7b's IR-enum renumber (59→61, 60→62) — present with either template version (pre-rebase-measurement trap, again).
- **Stop 19 — `bb_term_io.cpp` BUILT, GATED, then RESET TO A uncommitted (Lon's word).** 115→67, asm-diff EMPTY 0/10, 5 TEXT arms LIVE, behavior A/B identical — discarded on the rule correction below. Redo under corrected rule.
- **RULE CORRECTION (the run's real product) — .github `ddc19aea`, SCRIP `69529cc`.** Lon: "the rule is wrong." History check: RULES.md TEMPLATE-ONLY EMISSION was "strengthened" `515aa7d6` (05-28) with "duplicate the byte-producing code into each template — that duplication is the point", which PREDATES and CONTRADICTS the 06-02 ⛔ ONE MEDIUM, INVISIBLE canon (byte-identical ×4 in GOAL-SNOBOL4/ICON/PROLOG/RAKU-BB.md); canon intact, RULES.md never reconciled; the same-day GOAL-PRUNE (`1b15d374`) tersed RULES.md and PRESERVED the wrong clause (conflict resolved: corrected paragraph wins). Fixed: RULES.md rewritten — `x86()` internals (`x86_asm.h`) are the ONLY emitter of BINARY and TEXT, side-by-side; ZERO binary anywhere in a `bb_*.cpp`, top level OR helpers (static helpers are INSIDE the fence — relocating bytes changes nothing); byte producers PRIVATE to `x86_asm.h` (sole legacy exception `bomb_bytes`); duplication clause recorded DEAD. CORRECTION RECORD inserted byte-identically into the 4 canon files (md5 `0a9f1476…` ×4, survived the prune merge); FACT-RULES pointer added to PASCAL-BB / ICON-BB-NATIVE / PROLOG-BB-BYRD / SNOCONE-IR-BB; GOAL-BB-FIXUP: ⛔ BINARY-ARM PORTS superseded (the arms ARE the violation; glyph/XK_SYM lesson kept as conversion caution), rb ABSOLUTE sweep scope, all "[S] rb — owner GOAL-PROLOG-BB" parkings superseded.

## Inherited red (unchanged, Lon's queue)
- **prove_lower2 harness DOES NOT LINK** on main since PB-12 (`0a614f2`): `lower.c` now calls `bb_label_landing` (defined in `lower_program.c`) which the harness link line omits. Zero verdicts. Not fixup-caused (zero refs pre-PB-12; pre-rebase run with fixup edit was 66 PASS + 2 inherited FAIL). One link-line addition — owner PASCAL-BB/harness/Lon. Blocks every TIER S rung everywhere until fixed; the earlier rc=0 2-FAIL issue is buried beneath it.

## State
SCRIP `f4f6575` (cursor reset) / .github this commit. Tree clean at A; build green; gates at floors (smoke 19/0, pat-rung M2=18/M4=18 with pre-existing 053, purity 2-floor, bin_t 0, sno_pat_reg HARD, vstack 3). `# CURSOR: bb_alt.cpp` — ring TOP, corrected-rule lap, ticked-clean files re-audited.

## Next run
Session open protocol → THE LOOP at `bb_alt.cpp`. **FIRST MOVE: pin the encoder-gap list with Lon** before any conversion stop — known gaps from the byte arms: mov-imm64-to-reg (`48 B8/B9/BA/BE…` + u64), call-through-rax vs `@PLT` in BINARY, `emit_term_from_node_bin` node-ptr movabs, `E9`-rel32-0 placeholders (bb_resolve/bb_retract_throw — NOT behavior-neutral to collapse). Conversions per-instruction byte-verified vs `as`; LIVE probe where corpus silent; non-neutral → flag, never guess.
