# FINDING 2026-08-27 (ceo): ARCH audit 3 of 3 — ARCH-ICON.md and ARCH-ICON-RTX.md are MAJOR DRIFT; the REGISTER CONTRACT teaches a frame base that does not exist, and one genuine three-way law gap surfaced

**Context:** completes the audit wave (see the zeta/IR and x86/Prolog FINDINGs of this date). Note first: `src/templates/` was re-gridded TODAY 08:52 (srcreorg move-3, seat15) into `bb/` (132 .cpp) · `x86/` (x86_asm.h, x86_arg_roles.h) · `xa/` — zero top-level .cpp remain, so every flat template path in every doc is hours-stale; repairs must ls-verify the new homes.

## ARCH-ICON.md — MAJOR DRIFT (the good and the dangerous)

- **§Execution model and §String scanning are EXCELLENT** — scan semantics match `fstranl.r`/`fscan.r` field-for-field; the JCON topology checks out; the renamed no-software-value-stack parenthetical is current; 19 further claims verified (registers Σ/δ/Δ = r13/r14/r15, ZC_FRAME_RSP, coexpr via pthread, the 43 `ir_a_*`, ...).
- **⛔ §REGISTER CONTRACT — the file's stated reason for existing — is wrong in five load-bearing specifics:** (1) `emit_jmp_pin_rbp()` does not exist — the live symbol is `emit_jmp_pin_legacy()`/`emit_rec_pin()` (emit.h:607/609) with a WIDER disjunction (adds flat_lcl_proc, zframe_graph; flat_pat probe-gated). (2) `x86_fb()` is unconditionally "rsp" (x86/x86_asm.h:499) — no per-graph RBP duality. (3) `x86_fb_pinned()` is a compile-time CONSTANT ZERO (:493) — NOT the selector; the live RBP path is `emit_match_rbp()` → `xop_frame_slot()` → `op_xf_off`/`op_zread_xf[]` → ZRES/ZOPQ/XSAQ. (4) xa_flat SAVES the caller base write-only at [kt-8] and does NOT pin rbp (`xa/xa_flat.cpp:346,350,416` — its own comment says "NOT pinned"). (5) `op_flat_disp` zero hits (survivor: `x86_frame_off()` + `op_zdepth`). **A template author obeying this section literally emits against a frame base that isn't there.**
- Also: "per-box .bss arena" is wrong twice (state is the STACK-CARVED `jcon_value_region`, zls_g_region(), folded into flat_frame_bytes at emit.cpp:3308/3414; and ARBNO is the SNOBOL4 case — the doc's own cited reference file says so). Dead names: `g_bb_varslot`, `bb_pat_any` (→ bb/bb_scan_any.cpp), BB_PUMP/BB_SCAN tokens. GVA is r9-relative `GVARQ` under the default `g_rtcc_on=1`, ABSQ only under RTCC-off. jcon's `ir_info` has FIVE fields (x is the fifth). `refs/` paths → `/home/resources/jcon-master/`.

## ARCH-ICON-RTX.md — MAJOR DRIFT (sound method, decayed measurements)

- §§1-3, 7, 8 (the six checks, window-dominance, arm census, the probe-shape traps) remain fully applicable — the doc's most valuable content.
- **The §5 inventory is computed over `corpus/programs/icon/`, WHICH DOES NOT EXIST** — Icon lives at corpus/tests/icon (303) + packages/icon (~700) + benchmarks/icon (37); re-run of the doc's own filter: 316 Icon .s, 306 live, 10 dead (doc says 305/265/40). The four phantom symbols of its worked example (icn_write_str etc.) appear in ZERO .s files — the exemplar teaches from unreproducible numbers. `rt_frame` (255 sites) is itself a phantom of the class §5 warns about.
- **Gate roster: doc says six gates incl. `SCRIP_RTX_CALL`; rtx_init.c:9-27 declares FIFTEEN and CALL does not exist**; the doc's two "new" gates (SCAN, IO) were never minted — the landed Icon families are ICNVAR/ICNNUM/ICNREL/ICNAGG/ICNGEN/ICNCALL/ICNSUB. The "already assembly" list overcounts by four (`rt_proc_call_epilogue_*` are C at rt.c:1251/1257; `_slim_` spellings zero hits). Six §5 status rows say "unported" for symbols that PORTED or gated (re-derive mechanically from the `c_` rename convention). `rt_gen_spine_pass_γ`, the doc's exemplar of a non-symbol, IS now a real RTX symbol (rtx_icngen.S:66) — the teaching example became a counter-example. The claimed-symbol partition (B13) and file-ownership partition (B16, empty dirs) are both overtaken.
- `:211` carries **the ONE surviving void-"stackless" sentence in either doc** — and mis-cites ARCH-ICON.md, which no longer contains the word.

## ⛔ The genuine three-way gap (law ahead of doc AND code)

Neither doc mentions RULES.md § BB FRAME-PLACEMENT CRITERION — and the SOURCE agrees with the docs, not the law: `emit.h:607`'s rbp admission is a pure BOX-KIND disjunction, exactly what the criterion demotes to "approximation." This is not doc-only drift: the criterion-shaped predicate is icon-n2's addressing work. ARCH-ICON §REGISTER CONTRACT should record the kind test as a known-gap approximation of the criterion, not as spec. Related and load-bearing: `test_gate_icn_rbp_census_ratchet.sh` still HARD zero-asserts (basis voided by RULES.md:70 — the hq_P redesign is dispatched and confirmed still pending), and `test_gate_icn_no_stack.sh:5` still names the retired law in its header (check itself correct).

**Highest-value single fixes:** ARCH-ICON — rewrite §REGISTER CONTRACT around emit_rec_pin()/xop_frame_slot()/op_xf_off with the criterion as governing predicate. ARCH-ICON-RTX — re-run the §5 sweep on the real corpus roots and re-derive the status column from the `c_` convention (corrects five sections at once).

**Disposition:** folded into `arch-doc-repair-bundle` (rank 1); the :211 sentence rides `stackless-eradication`; gate redesign already with hq_P.
