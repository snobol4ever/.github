# FINDING 2026-07-27i — SN4 s195: two FLATDISP displacement mines killed; the ARBNO element clobber bracketed (both arms broken)

**Session result (fresh-clone baseline re-proven first):** crosscheck m3 185→221 · m4 183→219 · FAIL 130→94 · DIVERGE=1 (W06_tab) HELD EXACTLY at every step, both media in lockstep. Census ratchet NET=119 == baseline, GATE OK. Bench modes OK 8→11 · FAIL 4→0 · CRASH 4→5. SCRIP `62aaf9ff`; artifacts: corpus `2e26d600` (benchmark) + `5be21cdc` (demo), feature sweep committed in SCRIP.

**One disease, three sites.** Both landed fixes and the open ARBNO mine are the same failure shape: an rsp-relative flat-frame access whose depth compensation assumes a stack geometry the FLATDISP conversion changed. All were LATENT while every graph carried the seeded rbp frame (FR/FRQ depth-immune) and went LIVE when s193/s194 made graphs depth-static. Note the prose-rot casualty: `x86_asm.h`'s REG-7 U3 comments still say FR resolves `[rbp+off]`; `x86_fr32_prefix()` is hardcoded `[rsp + ` — the rsp spelling is the reality.

## Mine 1 — FLATDISP-LEAF-ORDER (the rt_dcap_pump segv, the 130-blocker)
MONITOR-FIRST per RULES.md: monitor bracketed statement 2 of 039_pat_any; gdb at the bracketed sink (`rt_dcap_pump`, pattern_match.c:673) found ONE 24B pend record with varname="V" CORRECT, saved_delta=0xffff9b20 (=−25824 int32), len=25826 — and 2−(−25824)=25826 proved len consistent, isolating the single garbage source: the saved-δ read. Ground truth from the .s: SAVE stored `[rsp+48]`, COND read `[rsp+32]` = head's rsp-snapshot cell (its low-32 IS 0x…9b20). Root: capture pairs allocate `[COND, SAVE, inner…]` but FLOW save→inner→cond; `fc_leaf_walk`'s running prefix registered COND before SAVE's 16 accumulated ("allocation order = flow order" holds for the spine, not the pair). Fix: the lowerer records each pair's inner allocation extent at build time (`fc_pair_extent_register/_extent`, zeta_storage.c — fch-style side table); the walk recurses [k+1, E) FIRST and registers COND at the resulting pfx = the S10c suspended depth. `fc_cond_fp` (range-sum, order-independent) audited and innocent. Recovered the 039–048/05x/capture clusters: +21 both modes.

## Mine 2 — FLATDISP-LIVE-BUMP (the define cluster: double(5)→0)
`bb_call_proc_staged`'s slim-call installs staged args while its own `sub rsp, scc_sb` save block is live. The `x86_fc_hit` arm always carried `+scc_sb` (the template comment even documents the intent); the `FRQ(slot)` fallback never did — depth-immune under rbp, 32 short under rsp. 083: arg staged at [rsp+128] pre-sub, read at [rsp+128] post-sub = the rep-stosb-zeroed frame → s=0 → 2·s=0. Fix: `FRQB(off, bump)` beside FRQ in x86_asm.h — both branches add the live bump; the offset rides the STRING so `x86_parse`'s XK_FR64 re-derivation keeps BINARY==TEXT with zero encoder change (R10). Recovered the DEFINE/gc/roman-adjacent clusters (+15) and ALL FOUR bench FAILs.

## Mine 3 — ARBNO ELEMENT CLOBBER (bracketed, NOT killed; both arms independently broken)
052 (`X POS(0) ARBNO('a') . V RPOS(0)`): m3 slab segv opaque; m4 named-symbol gdb showed the fault at `n17_match_arbno_β+36` storing THROUGH rsp — rsp itself 64MB deep. Measured with per-label breakpoints: β enters at the yield frontier; each β/yield cycle descends exactly 320 (op_sb) and never releases; the saved-cursor β reads is 0 every cycle. Cause: the right-range RPOS literal is a FLAT-slot box (its `[rsp+272]` carries op_flat_disp=0 — it has NO fcl registration and inherits the previous node's disp per the s190 sentinel law) re-run PER YIELD at element depth, and 272 == elem+HDRB: the qword-6 type store smashes {start@+0, saved@+4} — saved-cursor reads the qword's high dword, 0, forever → body re-matches at δ=0 → RPOS fails → β re-carves. TWO fixes tried and REVERTED with measurements: (a) `fct_leaf_range` cell-base registration — the literal's [rsp+272] did not move; its addressing is a THIRD scheme (value-spine window `RSP(w)` / zls, not fcl) that must be mapped before any fix is sound; (b) tail-lit-decline in `fc_tail_walk` (defensible by the residues law's own "pre-chain flat operand slots") — the decline fired, but it REGRESSED 4 green tail programs (221→217) to buy nothing: the CHAIN-arm fallback segvs on 052 too. Also found on the way: a stale FIRST-WINS `fc_cond` double-registration (052's wrap-COND read 336 while finalize's fpb=0 math gives 304 — the sno_cap_fc lowering-time registration precedes finalize's and wins). The ARBNO dig therefore needs: the value-spine/window addressing map for element residents, the tail fix, the chain-arm fix, the fc_cond first-wins audit — in that order, monitor-first per rung.

## Carried observations
- **FAIL→CRASH accounting:** one bench program (roman, by elimination) moved FAIL→CRASH — probable mask-removal: args now bind, the program runs further and reaches the arbno mine. Verify when ARBNO lands.
- **Monitor scr granularity:** scr emits LABEL events only; spl emits per-assignment VALUE events → spl/scr "diverges" at step 2 on trace SHAPE for any program. The bracket theorem still worked (statement-level), but a MON-RE-shaped rung should teach scr the VALUE events.
- **SYM-VIS (Lon question, s195):** m4 box α/β labels are ALREADY .symtab LOCALS (nm: 39 α/β symbols in 052.prog, binding `t`; `break n17_match_arbno_β` and named backtraces worked all session) — perf and gdb read local .symtab symbols in unstripped binaries, so `.globl` on every port buys strip-resilience only, and costs one directive per port on the beauty-scale path (34.5M lines, m4 termination already unsettled). The REAL gap is m3: the slab is `?? ()` to every tool. Named rung **SYM-VIS-M3**: emit the perf JIT map (`/tmp/perf-<pid>.map`, `START SIZE name` per box port) from the binary label-define walk, gated `SCRIP_PERF_MAP=1` — perf-native, zero symbol-table cost, and the same map is gdb-loadable. m3 is where the delineation ask has teeth.
