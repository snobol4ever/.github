# FINDING 2026-09-07 (ceo, MODE EXECUTIVE) — every Arizona and Jcon red named with its first divergence on SCRIP `2a01165f6`, and cut into five class rows

Tree: SCRIP `2a01165f6` corpus `2148f7185` (the Icon master 705/705 both modes on this tree). Runners: `test_icon_arizona_suite.sh` (shipped 124, graded 90, 34 ruled ungradable: 29 container/library, 1 tty, 1 nondeterministic, 3 oracle-contract) read m3 52 · m4 53 of 90; `test_icon_jcon_suite.sh` (shipped 91, graded 82, 9 ungradable) read m3 49 · m4 50 of 82. Lon 09:5x: *"What's up with the Arizone and JCON test suites?"* — neither had been re-run since the frame cure and the scan fix, and their reds had never been cut into classes (hq_I's row, stood down).

## THE REDS, BY FIRST DIVERGENCE (mode 3, the ceo's scratchpad `ariz/` and `jc/`; `< oracle` `> scrip`)
Arizona (38): arith (column spacing after a numeric divergence) · case (`co-expression_2(0)` vs `(927649600)` — the ceo's own regression, the activations field unzeroed at create) · cfuncs (bitcount: C functions) · checkc (`numeric(7r4)`) · checkfpc (`integer("3e500")` must fail, prints INT64_MIN) · ck (`real("7r4")`, `real("3e500")`) · coexpr (`co-expression_3(321)`) · errkwds (`&errornumber` 0 vs 106) · errors (rc=134 Aborted) · evalx (rc=134) · extlvals (external values) · fncs (`copy()`) · gc2 (collect counts 0 vs 1) · gener (`[ok]` suffix) · ilib (`convert:`) · image (`co-expression_3(10)`) · iobig (closing/writing file) · io (`file: &errout`) · kwds (`&features`) · large (`iand` on 21-digit integers) · lgint (`c = 3141…` vs `9223372036854775807`) · mega (strlength/bits/hex) · misc (SIGSEGV) · nargs (`1 acos`: args() of builtins) · others · overflow (`1e+308 + 1e+308` must be error 204, prints inf) · pdco (SIGSEGV) · radix (`2r111…` beyond 64 bits) · recent · recogn · sorting (`91:`) · spellnum (`-2: negative two`) · struct (`set_5(0)` vs `set_4(0)`) · tprintf (`-2147483648` row) · tracer, transmit (`&trace` lines) · traps (rc=134) · var (`co-expression_1(1)` line).
Jcon-only (8): checkfpx (`Image(2e13)` → `"2.0e13"`) · cxtrace (`&trace` on co-expressions) · lists (wraparound) · loadfunc (`hello2 from iconprocs`: a shared library) · profsum (a Java profiler listing — not an Icon program's output) · substring (`F -8.`) · traceback (`Run-time error 201` + traceback) · tracing (`&trace`). The other 20 Jcon reds are the Arizona programs above.

## THE FIVE CLASSES (minted on the bus 10:2x, rank in brackets)
1. `icon-arizona-jcon-class-large-integers-and-numeric-overflow` [0] — lgint large radix checkfpc overflow mega tprintf arith.
2. `icon-arizona-jcon-class-trace-and-error-diagnostics` [0] — tracer transmit tracing cxtrace traceback errors evalx traps errkwds.
3. `icon-arizona-jcon-class-co-expression-serial-and-activation-count` [0] — case coexpr image struct var (the ceo's regression cured first).
4. `icon-arizona-jcon-class-crashes-misc-pdco` [0] — misc pdco.
5. `icon-arizona-jcon-class-conversions-builtins-and-io-fidelity` [1] — the remainder, split as measured; the cfo's easiest Icon rows.
Named for the coo's inventory, not for a class: cfuncs, extlvals, loadfunc (need C functions / a shared library the contract does not ship) and profsum (a JVM profiler's output) — the runner's ORACLE_CONTRACT_NOT_IMPLEMENTED bucket decides.
