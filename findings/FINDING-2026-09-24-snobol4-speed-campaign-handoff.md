# FINDING 2026-09-24 — THE SNOBOL4 SPEED CAMPAIGN: HANDOFF TO THE NEXT SESSION (ceo, 17:4x CDT; CEO-1246)

Lon, in-chat to the ceo, 2026-09-24 17:3x–17:4x, verbatim: *"keep optimizing SNOBOL4 benchmarks until we are 2-3x faster than SPITBOL."* · *"But first, go run the SNOBOL4 demos as work horse benchmarks. Compare CLAWS5, TREEBANK, JSON, CALCULATOR, and PORTER for instance to SPITBOL. Use the large files."* · *"We are now seated as Fable 5.1 with max effort. The #1 priority is to find and fix all speed regressions. Then get SCRIP to be 2x or 3x faster. Use the work horse demos and benchmarks to get there."* · *"Enqueue the work item so I can /clear and next. perform hand off. Make all the notes for the next session to continue and succeed with getting SCRIP SNOBOL4 2x-3x faster than SPITBOL. This includes analyzing SPITBOL performance also to get complete insights."*

The row: `snobol4-speed-campaign-every-regression-since-380cc4162-fixed-then-2x-3x-over-spitbol-on-the-workhorse-demos-and-the-23-kernels` (owner ceo, rank 0; `s4e_msg.sh next` from the ceo seat serves it). Its DONE-WHEN is `bash SCRIP/scripts/bench_snobol4_campaign_probe.sh --bar 2.0` (SCRIP `c8ba94210`, ~20 s: four kernel twins + the treebank demo, geometric mean of SPITBOL time / SCRIP time; read 0.42x RED on `7ed69d789`). The record of the day is CEO-1243..1246 in GOAL-CEO.md; this file is the working notebook and every claim below is folded into CEO-1246.

## 0. THE PREMISE (Lon 2026-09-24 17:4x, in-chat to the ceo, verbatim)

*"We removed the RTCC veneer for many RT calls that were ASM not even C and did not need them. We have an optimized GC garbage collector. So then, we should be faster than before. And by extension, faster than SPITBOL. That is your evidence."* Read it as the campaign's null hypothesis: the tree's DESIGN since 09-03 (the RTCC veneer gone from the asm runtime calls that never needed it; the typed, non-conservative collector) should put every kernel and demo AHEAD of the 09-03 readings once the regressions are out, so any reading still below `380cc4162` is a regression to find, not a design cost to accept — and the 09-03 tree itself was already ahead of SPITBOL (1.25x / 1.33x), so the 2–3x bar is the design's own promise, not a stretch. Two of today's open items are direct instances of the premise: the GC poll is still emitted as a C-style call wrapped in the RTCC write-back/reload (three stores, four loads) although `rt_gc_poll_asm` is asm that preserves what it touches (§ 3 item 3), and the by-name call path's linear name scans (§ 3 item 1) are pure regression with no design behind them.

## 1. Where it stands (all measured today, clean SPITBOL benchmark oracle `spitbol-bench-oracle/sbl -bf`, box quiet)

**The 23 kernels** (fixed-time twins, SPITBOL time / SCRIP time, README `7ed69d789` § SNOBOL4 grid): the 09-03 tree `380cc4162` reads geomean **1.25x m3 / 1.33x m4**, ahead on 11 of 22; this morning's `54c5d91bf` read **0.19x / 0.19x**; after today's four landings `444e9893d` reads **0.70x / 0.75x**, ahead on 12 of 23. Tight loops are back (arith_loop 3.39x vs 4.03x on 09-03, var_access 3.61x vs 4.25x, op_dispatch 3.41x vs 3.73x, ident_call 2.1–2.2x vs 2.4x). Still far behind 09-03: string_manip 0.09x vs 0.66x, table_access 0.20x vs 1.29x, roman 0.24x vs 0.78x, array_sum 0.14x vs 0.27x, table_variety 0.27x vs 0.68x, indirect_dispatch 0.40x vs 0.62x, test_icon 1.62x vs 4.12x, eval_fixed 0.45x vs 0.96x, mixed_workload 0.15x vs 0.28x.

**The workhorse demos on their large inputs** (whole-program wall, best of 5, `sbl -bf -d512m -i64m -s256m`, SCRIP arena `SCRIP_HEAP_KB=524288`, stack 256 MB on all arms, outputs byte-identical on all three engines; m3 compiles in-process as sbl does, m4 is the prebuilt binary):

| demo | input | SPITBOL ms | m3 ms | m4 ms | m3 | m4 | README 08-28 m4 |
|---|---:|---:|---:|---:|---:|---:|---:|
| claws5 (CLAWS5inTASA.dat ×16) | 1,043 KB | 67.6 | 88.4 | 78.7 | 0.76x | 0.86x | 0.72x |
| treebank (treebank.input ×1024) | 327 KB | 113.1 | 1601.7 | 1490.7 | 0.07x | 0.08x | 0.50x |
| json (citm_catalog.json) | 1,686 KB | 32.8 | 103.7 | 67.4 | 0.32x | 0.49x | 0.92x |
| json (twitter.json) | 616 KB | 14.8 | 62.1 | 25.4 | 0.24x | 0.58x | — |
| porter (porter.dat ×4) | 742 KB | 91.5 | 425.8 | 223.8 | 0.21x | 0.41x | — |
| calculator-1 (.dat ×4) | 127 KB | 28.6 | 89.0 | 72.2 | 0.32x | 0.40x | 2.10x |
| calculator-2 (.dat ×4) | 127 KB | 30.5 | 92.2 | 71.1 | 0.33x | 0.43x | 2.64x |

Where the program times its own match (json, calculator print `match_ms=` on TERMINAL) the WORK multiple agrees with m4: json 0.42x, calculator 0.38–0.42x. ⛔ A timing loop that redirects `</dev/null` inside it starves a demo of its input and measures startup (0.6 ms "runs" that print the same failure on every engine): the first grid of the afternoon was void for that reason; feed the input with ONE redirect and verify stdout bytes across engines before trusting any number.

**The arena (128 KB shipped window, 4 MB hard cap) is not the kernels' problem** (64 MB moved only table_access 0.21x → 0.59x and table_variety 0.27x → 0.38x, the rest ≤1.3x), **but treebank's live set is ~20 MB, so at the shipped cap it cannot run at all** (empty output, an out-of-memory on stderr); the demos need `-d512m` on SCRIP exactly as sbl needs it. Whether the shipped cap should be larger is Lon's call (CEO-1095/1101 set 128 KB as the testing default and the cap as a hard cap) — raise it with these numbers.

## 2. Regressions found today, fixed (four landings, all pushed)

| commit | what | measured |
|---|---|---|
| `2a81a02db` (+`84e39045e`) | the per-statement `SNO$STMT` by-name hook was armed by any mention of a statement keyword in the SOURCE (Lon: invalid, EVAL/CODE can introduce one); now the `--stlimit` switch (= `SCRIP_SNO_STMTKW=1`), off by default; the correctness graders pass it, the bench harness does not; kernels no longer write `&STLIMIT = -1` (corpus `723e5aa08`) | bisected to `1e2773d98` (09-04 19:17): arith_loop 7.1 → 74 µs/rep at that one commit; geomean 0.19x → 0.39x/0.41x |
| `d752e30eb` | the monitor's per-assignment VALUE tap `comm_var` (`c3e6b16bb`, 09-04 21:17) emitted on every global assignment under its own default-on knob; now under the switch (`--monitor`/`--trace` imply it) | callgrind arith_loop: 444 M of 750 M extra Ir were the tap and its name filters, plus 196 M in the assign boxes' pushes/spill; geomean → 0.64x/0.69x |
| `ea0c115b5` | every remaining emitted monitor hook (DEFINE call/return/fail, Icon proc return/fail taps, suspend/resume/gen-return, the statement mark's &STCOUNT inc) behind one predicate `x86_trace_hooks_on`; Icon graders pass the switch (Icon `&trace` rides the hooks); the Icon CALL tap stays because `display()` reads the activation record it writes (hq_icon's row to inline) | twin instructions 1992 → 1833 |
| `444e9893d` | `trace_idle()` in `core.c`: every runtime trace entry point exits before `trace_find` (a linear scan of the TRACE set with strcmp, twice per event) when no trace is armed — the runtime's own call path called it on every call and return | indirect_dispatch 9.51 G → 2.21 G Ir (09-03: 1.39 G); geomean → 0.70x/0.75x |

Also landed: r10 no longer loaded with the statement number (r11 node id is the one diag register, ARCH-ENGINE.md); rbx is NOT the arena top in the tree (ARCH rows corrected, .github `52032d8d`); the README cell gate reads a CHECK-FAIL row; the build-behaviour probe compiles its witnesses with the switch unset.

## 3. Regressions found, OPEN — in the order to attack them (each with its callgrind number)

1. **The by-name call path scans two name tables with strcmp on EVERY call — treebank's 71%.** callgrind on treebank (×1024, m4): 09-03 `380cc4162` 2.48 G Ir; today 20.7 G. `__strcmp_avx2` 8.53 G (41%), `sn4_is_system_fn` 4.59 G (22%, `snobol4_system_fns.h`: a linear scan of 157 system-function names, inlined; 322 M strcmp calls from it — about 275 per by-name call, i.e. two full scans), `icn_builtin_is_known` 1.58 G (7.6%, `by_name_dispatch.c`: an Icon builtin-name table scanned from a SNOBOL4 program, 1,173,519 calls from `rt_call_arr_impl`, 66 strcmps each). The sites: `rt_call_arr_impl` line ~5910 `if (sn4 && !sn4_is_system_fn(fn) && !rt_proc_is_registered(fn) && FNCEX_fn(fn) && icn_builtin_is_known(fn) && ...` and ~5915 `if (strict && !rt_proc_name_exists(fn) && !icn_builtin_is_known(fn) && icn_builtin_arity(fn) == ...)`, and `sn4_call_in_scope` (~5849) which tests `sn4_is_system_fn` BEFORE `rt_proc_is_registered`. Since: `sn4_is_system_fn` arrived 2026-08-23 (`24cb07175`, ERROR 248 on redefining a system function); the Icon-table test on the SNOBOL4 path 2026-09-12 (`597c9be53`, "delete every am-I-running-Icon test"). THE CURE, in this order: (a) test `rt_proc_is_registered(fn)` FIRST (a hash lookup) and short-circuit every scan for a registered user function; (b) make `sn4_is_system_fn` and `icn_builtin_is_known` O(1) (a hashed set built once, or the baked builtin id `bid_bake_of` the emitter already computes — a call site with a baked bid is a builtin, one without is not); (c) resolve statically: a call to a name DEFINE'd in the program with a resolved proc record emits a direct open/land, no name at run time. Expected: treebank from 20.7 G toward the old 2.5 G; porter, calculator, json and every call-heavy kernel move with it.
2. **The collector's 09-19/21 call contract** (`rt_call_open_by_name` `80e3a942e`, `rt_apply_open` `ed9f7a76e`, `rt_proc_call_prologue`, `rt_proc_save_count`, `rt_proc_epilogue_named`, `rt_name_save_push`/`rt_name_restore`): a runtime-made call opens in C and lands in emitted code — about 1,100 Ir per call against 695 on 09-03 (indirect_dispatch after item 1's trace fix: 2.21 G vs 1.39 G). ARCH-RT-CALL-PROTOCOL.md § 12 has the per-function split. Cure: a direct open/land fast path for a known DEFINE'd callee with a resolved proc record and the locals' save/restore as a fixed-offset block.
3. **The GC poll is mis-sited and mis-shaped**: emitted after EVERY runtime-call return, including calls that cannot allocate (`rt_coerce_num2_d`, `rt_cmp_d`), and it is a call inside a 7-instruction RTCC spill/reload where the design wants an inline `cmp [g_gc_pending],0 / jne slow`. ~8% of arith_loop's Ir this morning; classify runtime entry points allocating/non-allocating at the emitter. NOT a diagnostic (Lon asked): it is the collector's trigger; it does not go under a switch. The cfo reviews (collector lane).
4. **The statement marks' three GOT indirections per statement in mode 4** (`bb_stmt_mark.cpp`: g_stno and g_line stores through `[rip@got]`): +17% cycles on arith_loop against 09-03 (283 M vs 266 M Ir, 74 M vs 63 M cycles). SPITBOL pays two direct moves. A packed stno:line word in one direct store, or rip-relative stores in mode 3.
5. **String, table and array classes not yet profiled**: string_manip 0.09x vs 0.66x, table_access 0.20x vs 1.29x, roman 0.24x vs 0.78x, array_sum 0.14x vs 0.27x, table_variety, mixed_workload, name_indirection, eval_fixed. Same one-kernel method (§ 5). Suspects from the demo profiles: the by-name path (item 1) inside `*function()` pattern calls, `dat_field_get`, `gc_blk_of`/`gc_hins` on table stores (table_access moved 2.8x with a 64 MB arena: collections are part of it).
6. **Under the switch** (the instrumented mode reads 0.05x): &STCOUNT in rbx (Lon's ruling, `-ffixed-rbx` on every TU; the C-to-BB glue stops restoring rbx at exit; the co-expression switch stops restoring a per-context rbx), &STLIMIT a global, `inc rbx; cmp rbx,[limit]; jg slow`, the by-name hook only on the slow path. Last, after the default path is at 2x.

## 4. The 09-03 → 09-24 timeline as measured (arith_loop m3 µs/rep, the bisect probe)
7.5 at `380cc4162` (09-03 22:04) → 7.1 at `b68c07e9b` (09-04 19:08) → **74.0 at `1e2773d98` (09-04 19:17)** → 79.6 (20:57) → 95 (09-05) → 94 (09-09) → 108–121 (09-12) → 133 (09-13) → 154–165 (09-24 morning) → **7.6 after the four landings** (the twin at 2000 reps: 09-03 6.3). The creep 09-05 → 09-24 was `comm_var` (09-04 21:17) plus many small things; the two September commits named above were 85% of it.

## 5. The method that works (use it for every item above)
1. Old tree for comparison: `git -C SCRIP worktree add <scratch>/scrip-380cc4162 380cc4162 && (cd <scratch>/scrip-380cc4162 && make -j8)` — 20 s; its own `scrip` + `out/libscrip_rt.so`. (The one-runner and freshness guards refuse a worktree: run the twin binaries directly, not the harness.)
2. Twin: `python3 SCRIP/scripts/bench_wrap_snobol4.py corpus/benchmarks/snobol4/K.sno --mode iter --n 2000 -o K_iter.sno` (fixed iterations for callgrind) or `--mode time --bud-ms 500` (for a multiple). Mode 4 binary the harness way: `scrip --compile -o K.s K_iter.sno && gcc K.s -L<tree>/out -lscrip_rt -lm -Wl,-rpath,<tree>/out -o K.bin` (never `-no-pie`).
3. `valgrind --tool=callgrind --callgrind-out-file=cg.out ./K.bin </dev/null` then `callgrind_annotate --inclusive=no cg.out | head -30` (exclusive) and `--tree=caller` (who calls the hot function, with call counts); `perf stat -e instructions,cycles,branches,branch-misses` (`.tools/bin/perf`, paranoid=1 allows user counting). Compare per box: the emitted code's boxes are named `n<id>_<kind>_bx` in the annotate.
4. Emitted asm side by side: `scrip --compile -o x.s prog.sno` on both trees; the statement's asm sits between its `# <source line>` comment and the next; histogram `grep -oE '^\s+call\s+\S+' x.s | sort | uniq -c`.
5. Cure in the runtime or the emitter, then: the 29 statement/trace/monitor/Icon gates (`grep -l SCRIP_SNO_STMTKW=1 scripts/test_gate_*.sh` + `ls scripts | grep -E 'test_gate_.*(monitor|tracing|trace_value)'`), `make preflight`, the smokes, RebM (`corpus_suite_harness.py run corpus/tests/rebus/ALL.reb ALL.ref --lang rebus --modes m3,m4`), and for a change on the SNOBOL4 engine a SnoM read is hq_snobol4's (or the ceo's under `S4E_ONE_RUNNER_OVERRIDE` for a cross-language landing, as IcnM was read today).
6. Points at 500 ms (`BENCH_BUD_MS=500`; Lon 17:2x: 5 s is overly long, 500 ms was proven — the two agreed kernel for kernel). Publish the kernel grid with `BENCH_ORACLE_ARM=1 BENCH_ITER_N=5 bash scripts/test_snobol4_bench_suite.sh` on a fixture copy (`BENCH_SNOBOL4_DIR`), the demos with the driver shape in § 1 (or `bench_triangulate_demos_snobol4.sh`), and the campaign probe for the row.
7. Every landing: `git commit -F -` with the measurement in the message; push code repos first, .github last; README grid re-issued on the pushed hash (the README cell gate and the suite-table gate must read rc=0).

## 6. ANALYZE SPITBOL — the insights the campaign needs (not yet done; do it early)
The oracle is `/home/resources/spitbol-bench-oracle/sbl` (official upstream + two class-A patches; the monitor-hooked `x64/bin/sbl` costs ~2.3x and is never timed). Its source is MINIMAL: `/home/resources/spitbol-pristine/sbl.min` (statement entry `stmgo` 16516; the &STLIMIT batch check `stgo4`; pattern matching `s_*`/`p_*` routines; `alloc`; `cfunc`/`retrn`); the x64 translation gives every MINIMAL label a symbol, so **`perf record -g ./sbl -bf twin.sno` and `perf report` name the hot MINIMAL routines** — do this for each kernel class and each demo and write the per-class cost table (instructions per statement, per call, per pattern element, per allocation) beside SCRIP's: (a) `perf stat` both engines on the same twin: SPITBOL's instructions AND cycles per repetition are the target numbers, not just its wall; (b) `callgrind` on `sbl` works too (no symbols needed for totals; `--dump-instr=yes` for the hot loops); (c) read how SPITBOL does what SCRIP does slowly: function call (`cfunc`: no name lookup at call time — the DEFINE binds the code block; locals saved on `xs` by count, not by name), variable reference (the `vrblk` is resolved at compile time, no hash at run time), string concatenation and the `alloc` bump with `gbcol`, table lookup (`t_*` hashing), pattern node dispatch (`bri` through code words, one indirect jump per node), the statement prologue (§ 1 of this file's parent entry: ~10 instructions); (d) SPITBOL's `-x` execution statistics and `&PROFILE` give per-statement counts on the oracle's side. The comparison to write is per operation class: "SPITBOL spends N, SCRIP spends M, because ..." — that is where the 2–3x comes from, since restoring 09-03 only reaches 1.3x.

## 7. Rulings today that bind the campaign (all in GOAL-CEO.md CEO-1243)
The statement instrumentation is never inferred from source; one switch (`--stlimit`) for every language, off by default, graders pass it, benchmarks do not; monitor hooks are not emitted without it; the GC poll is not a hook; r11 alone carries the node id; rbx is Lon's choice for &STCOUNT under the switch (callee-saved like r12–r15, no veneer; `-ffixed-rbx`), &STLIMIT a global, r10 stays scratch; benchmark points at 500 ms; the x-factor is the bare multiple SPITBOL time / SCRIP time.

## 8. Fleet and seats
The fleet is QUIET under CEO-1237 since 12:53; the all-clear is Lon's. hq_snobol4 holds the original speed row (its DONE-WHEN, arith_loop m3 under 20 µs/rep, is green on origin; the row closes on their own SnoM/package pass, every grader now passes the switch); the ceo holds the campaign row. hq_icon holds two rank-0 rows from the coo's audit (CEO-1245) and the `display()` activation-record finding. The cfo's compile-time-arena ruling is CEO-1244.

## 9. Session setup for the next sitting
`git -C SCRIP fetch && git -C SCRIP merge --ff-only origin/main` (same for corpus, .github), `make` in SCRIP, `bash scripts/s4e_msg.sh check`, then `bash scripts/s4e_msg.sh next` from the ceo seat serves the campaign row and runs its probe; read this file, then start at § 3 item 1 with the treebank profile (`corpus/demos/snobol4/treebank/treebank.sno`, input ×1024, `SCRIP_HEAP_KB=524288`, `ulimit -s 262144`).

## 10. ICON, MEASURED THE SAME EVENING (Lon 17:5x: *"run the Icon benchmarks and report comparison to before-reported numbers and to Arizona Icon"*)

**The ten-kernel grid** (`scripts/bench_icon_kernels.sh`, the README's own 09-10 instrument: callgrind Ir, m4 × vs Arizona `iconx` 9.5.25a = iconx Ir / m4 Ir; all ten PASS byte-identically in both modes), today's `c8ba94210` against the README's 09-10 reading on `3bbdfc8c7`:

| kernel | 09-10 | today | ratio |
|---|---:|---:|---:|
| int_loop | 9.04x | 8.93x | 1.0 |
| mod_isolate | 6.75x | 5.04x | 1.3 |
| concat_dispatch | 23.61x | 21.02x | 1.1 |
| list_dispatch | 3.55x | 0.49x | 7.2 |
| concat_strvar | 2.84x | 0.56x | 5.1 |
| table_miss_dispatch | 1.92x | 0.38x | 5.1 |
| concat_int_dispatch | 1.09x | 0.47x | 2.3 |
| table_miss_semantics | 1.09x | 0.17x | 6.3 |
| concat_intvar | 0.95x | 0.41x | 2.3 |
| concat_table | refused (wall 0.59x) | 0.20x (wall 0.06x) | ~10 |

Geometric mean over the nine the 09-10 grid published: **3.16x then, 1.00x now**; the integer loops are unchanged and every string-concatenation, list, table and dispatch kernel fell 2–7x — the same classes as SNOBOL4's losses (string_manip, table_access, name_indirection, the call path), which points at the shared runtime nodes (allocation and the GC poll on every allocating return, the by-name/dispatch path, table stores) rather than the Icon front end. The SNOBOL4 campaign's cures are therefore graded on these ten too; hq_icon holds the Icon row.

**The classic set** (`bench_icon_classic_set.sh`, the README's other 09-10 instrument) is BROKEN as an instrument: it links mode 4 with `gcc -no-pie` (line 146; the harness abandoned `-no-pie` — a PIE relocation failure now reads BUILDFAIL) and its mode-3 arm reports rc=1 on programs that run clean by hand (`concord` m3 rc=0, 61,565 bytes, at the shipped arena). Run by hand (best of 5 whole-program wall, iconx on the icont-compiled program, SCRIP arena 512 MB, outputs identical across the three engines): concord 30 KB 22.6 / 40.7 / 31.0 ms → m3 0.56x, m4 0.73x (09-10: 0.33x); rsg 23.3 / 53.6 / 30.7 → 0.43x / 0.76x (09-10: 0.33x); ipxref 6 KB 2.4 / 23.2 / 5.5 → 0.10x / 0.43x; deal and queens ran their argument-less defaults (iconx 0.6 and 1.0 ms — startup, not comparable to the 09-10 rows, which passed arguments); **geddump 379 KB 115.3 / 2583.7 / 2499.8 ms → 0.04x / 0.05x — correct now (09-10: SIGSEGV in both modes), and the worst Icon workhorse by 20x.** The instrument fix (`-no-pie` off, the m3 arm's rc read) and the geddump profile are hq_icon's; they are the Icon mirror of § 3 items 1, 3 and 5.
