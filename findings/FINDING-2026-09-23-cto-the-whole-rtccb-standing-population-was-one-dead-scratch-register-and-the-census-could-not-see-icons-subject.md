# FINDING 2026-09-23 (cto, MODE DECTET) -- THE WHOLE rtccb STANDING POPULATION WAS ONE DEAD SCRATCH REGISTER, AND THE CALLEE-SAVED CENSUS COULD NOT SEE ICON'S SUBJECT

Row: `icon-gc-the-standing-chop-104-open-safe-point-sites-worked-in-batches-by-the-name-set-method-until-the-count-is-zero` (the chop), its two side worklists: the caller-saved spill-block sites the rtccb gate names, and the language-blind relop tag (the cfo's CFO-149 correction).
Trees: cure = SCRIP this landing on 9360760da; control = a fresh worktree at 9360760da with the corpus sibling linked (see 5).

## 1. THE MEASUREMENT THAT NAMED THE SITE
`test_gate_gc_the_caller_saved_spill_block_never_holds_a_heap_reference.sh` on origin: arm (b) 1476 heap references in rtccb slot 5 (r8) over 9 of 60 witnesses (hb_arena_grow 670, hb_dvec_sort_match 604, hb_nv 190, hb_datblk 4, hb_dvec_data_convert 3, hb_blob_span_defer 2, one each in hb_defer_subject, hb_dtp, hb_nested_match_outer_subject); arm (i) 134 UNREPAIRED words after a move plus 1 swept. GC-RTCCB-STALE names hb_nv.sno line 3 at two poll sites (81+27+26 unrepaired type-2/32B, 26 type-215/80B -- the same dead word re-read after the block it named moved), hb_arena_grow line 22 (667 at one site), hb_dvec_sort_match line 27 (300+300, moved=0), PAT$1 line 23 (1 swept).
The emitted code of hb_nv line 3 (`$('VAR' I) = 'value' I`, mode 4): `n14_call_α` copies each argument DESCR into the by-name args array THROUGH r8 (`mov r8,[rsp+40]; mov [rsp+8],r8`), so r8 leaves the box holding the POINTER WORD of the last argument. The shield then spills that dead word to rtccb+40 before every later call and poll of the statement and reloads it after, so one dead scratch value reads as a stale heap pointer at every subsequent safe point until r8 is next written. The three copy loops with that shape: bb_call.cpp 452-455 (SNOBOL4 by-name call), bb_call_fn.cpp 129-132 (Icon/Prolog by-name call), bb_make_list.cpp 20-23 (Icon list). r8 is plain scratch in 40+ boxes (bb_define 21, bb_idx_*, bb_match_*, bb_assign_global's comm_var line); the rtcc.h word ANCHOR is a name, not a liveness.

## 2. THE CURE FORM FOLLOWS THE BOX
The copy goes through rax, which is dead at the copy (written before read, overwritten by the call's own return before any read) and is not shielded. Twelve tokens over three files, no poll added or moved. ASM-DIFF-FIRST: every differing line in every compiled program is `mov (r8|rax), qword ptr [rsp+N]` or its store, counts equal both ways -- hb_nv 28/28, hb_arena_grow 36/36, hb_dvec_sort_match 100/100; corpus/benchmarks 86 files: 37 SUBST, 40 IDENT, 9 compile-refused identically on both trees, 0 OTHER; the seven masters extracted standalone, 4916 entries: SNOBOL4 1947 of 1982 SUBST, Pascal 224, Snocone 337, Rebus 43; Icon 0 of 826, Prolog 0 of 563, Raku 0 (their by-name calls do not take the zres copy path); 41 refused identically on both trees; 0 OTHER.
After the cure, the rtccb gate on the pristine cure binary: 9 of 9 arms, 60 witnesses, 60 collected, 0 references, 0 unrepaired, 0 swept. hb_nv alone: 190 -> 0 detections, stdout byte-identical, .ref matches.

## 3. THE GATE HAD A VARIABLE COLLISION AND READ THE PLANT'S SWEPT COUNT AS THE POPULATION'S
After the cure arm (i) read "1 SWEPT out of 0 detections": arm (g) assigned `stale=` and `swept=` for the PLANT's own readers, overwriting arm (b)'s population counters, so arm (i) graded the plant. Renamed `pl_unrep`/`pl_swept`; the gate then reads 9 of 9. The earlier "134 unrepaired and 1 swept" was 134 real plus the plant's 1.

## 4. THE NAME-SET EVIDENCE (CEO-1127 bar, no board channel)
(a) 60 gc witnesses, both trees, at SCRIP_HEAP_KB=64 SCRIP_GC_STRESS=3 and at the shipped default: 50 GREEN + 10 NOREF on both, IDENTICAL name sets both directions, per-entry stdout byte-identical in 60 of 60 at both configurations (re-measured after the pristine rebuild and the corpus-sibling fix, same reading).
(b) the asm-diff population above.
(c) the SUBST population run standalone on both trees, mode 3, at 64 KB stress 1 and at the default: 37 benchmarks with a .ref, 0 non-green on both; 604 Pascal/Rebus/Snocone entries, 8 non-green on both (5 RC1, 2 DIFF, 1 RC4), stdout identical 604 of 604 at both configurations; 1947 SNOBOL4 entries, 107 non-green on both at 64 KB stress 1 (97 DIFF, 7 timeouts, 1 RC1, 1 CRASH139) and 90 at the default (81 DIFF, 7 timeouts, 2 RC1), IDENTICAL name sets both directions, stdout identical in 1946 of 1947 at stress 1 -- the one difference a program that times out on BOTH trees and prints nothing on re-run either side -- and 1947 of 1947 at the default.

## 5. TWO INSTRUMENT FACTS PAID FOR ON THE WAY
A worktree under the scratchpad has no `../corpus` sibling; the driver resolves `link options` through `<tree>/../corpus/packages/icon/ipl/procs/` (strace), so six Icon benchmarks read OTHER with frame sizes doubled -- the missing IPL, not the cure. `make pristine` changed nothing; a symlink beside the worktree did. A two-tree Icon diff without the sibling is a diff of the library.
`util_gc_callee_saved_census.py --corpus` REFUSES on origin, before any edit (tally buckets 2021 unclassified, list holds 1732): census() kept a second classification for the lists that filed CELL:UNCLASSIFIED under copies while bucket() peeled every prefix; the lists are now filed by bucket() itself.

## 6. THE CENSUS COULD NOT SEE ICON'S SUBJECT (cfo CFO-149, both corrections)
SUBJECT_SEED was the single literal rt_match_enter and classify_def looked back TWO instructions. Widening the seed to the set alone moved nothing (heap=13 on the same 77 files, measured). The Icon seeding at concord n16_scan_enter_α is fourteen instructions below the copy: call rt_scan_enter, four rtccb reloads, sub rsp 32, the two spill stores into the poll's record, the poll, four reloads, the two reloads out of the record, add rsp 32, test rax, then mov r13 rax and mov r15 rdx. The classifier now finds the seed by ROUTE: nearest writer of the return register; a call is the seed; a reload out of a slot sends the walk to the store into that slot and on; an rsp change or any other writer ends it. Self-test 23 of 23 including the concord shape verbatim and a planted overwrite of the record slot. Over the 77-file corpus: heap 13 -> 442 readings, r13/SUBJECT 232 (the cfo's 232 distinct sites exactly) + CELL:SUBJECT 97 + deeper 113; the nine Icon files non-empty BY NAME (tgrlink 75, rsg 53, micro 53, geddump 38, concord 28, queens 20, ipxref 20, deal 20, micsum 9) beside the four SNOBOL4 (13); unclassified 1732 -> 663; raw 10391 -> 11241 (LENGTH now read through the record).

## 7. WHAT IS STANDING RED ON ORIGIN AND WHOSE
`test_gate_gc_a_callee_saved_register_at_an_allocating_return_is_named_not_assumed.sh` arm 5, the copy ratchet: 293 against a ceiling of 289 on the CONTROL tree with the original census -- red before this landing, over the 8-witness population; the four new copies belong to whichever landing added them after the ceiling was written, not to this one. `make preflight` 59 arms, 1 red: c_allocators, the declared standing red.
