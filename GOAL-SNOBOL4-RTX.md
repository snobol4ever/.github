# GOAL-SNOBOL4-RTX.md — The SNOBOL4 Runtime in Optimized x86-64 Assembly

## ⛔⭐⭐⭐ LON DIRECTIVE (2026-08-09, routed in-session): THE CALL-PATH C SURFACE IS HIGHEST RTX PRIORITY — replace with asm
The DEFINE call protocol's per-activation C round-trips are the measured whole of the func-family deficit vs SPITBOL: `func_call_overhead` 0.52× ≈ `func_call` 0.49× (an overhead-isolating bench pricing identical to a working one ⇒ the cost IS the protocol), `fibonacci` 0.37× = same cost × recursion depth.  TARGET SET, in emitted-call order: `rt_proc_find` / `rt_proc_open_fn` (name-keyed dispatch — mostly DELETED by AB-3b's `jmp [fn_cell$FN]` site flip, which is the strict prerequisite: land or coordinate with GOAL-SNOBOL4-BB LADDER AB before spending asm here) · `rt_proc_call_epilogue_γ/ω` · the per-boundary `rtcc_wb/rtcc_rl` register writeback-reload tax (RTCC veneer is this ladder's own machinery — the call path is its first paying customer).  SPITBOL's own protocol (manual Ch.8: pushdown save of args/locals + direct branch) is the asm shape to hit.  FALSIFIABLE ACCEPTANCE: `perf` majority moves out of libscrip_rt on `func_call_overhead`; that bench → near-parity; `fibonacci` follows proportionally.  Evidence + table: FINDING-2026-08-09h §7 + SCRIP README bench section @ `1c53552a`.  Prior art this directive elevates, not replaces: FINDING-2026-07-31 "THE-HOT-C-SURFACE-IS-CALLS-SPITBOL-NEVER-MAKES".  ⛔ Concurrency: obeys this file's existing NOT-CONCURRENCY-SAFE markings; Lon routes the seat.

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

## ⛔ LIVE CURSOR — s_this (2026-08-09 Fable-3): **RTX-FUNC-0 TRANSFER PATH PROVEN LIVE END-TO-END m3 — `rt_ab_enter_env` FIRES (interposer measured, first ever). FOUR LATENT AB-3a/3b PLUMBING BUGS FIXED THIS SEAT. BUG#2 (nformals) STILL OPEN → crash moves DOWNSTREAM into block/body. SCRIP_AB DEFAULT FLIPPED OFF (opt-in =1) SO HEAD IS GREEN ON EVERY DEFINE PROGRAM (func_call ms:1932, fibonacci ms:518, hello — all pass at HEAD default).**
- ⭐⭐ **THE HEADLINE**: with `SCRIP_AB=1`, func_call m3 now executes: call site → `jmp [fn_cell$INC]` → `INC_act_α` → `rt_ab_enter_env` (LD_PRELOAD interposer prints `#1 fb=0x7fff…`). Crash is now INSIDE the block/body region (`rip` = real JIT addr, rax=3 rdi=3 at death — consistent with the known bug#2 nulled-formal shape: N nulled → INC = N+1 on garbage → downstream type dispatch dies). The prior cursor's bug#2 diagnosis stands and is now the ONLY blocker.
- ⭐ **FOUR PLUMBING BUGS FIXED (all latent in never-exercised AB-3a/3b machinery, all landed this commit):**
  1. **Bind bakes 0 (BINARY)**: `bb_ab_bind`'s `lea rax,[rip + __],(uint64_t)0,albl` routes to `x86_load_ro` which in BINARY bakes the ptr arg (0) as a movabs IMMEDIATE — there is NO forward-patch for that form (x86_asm.h:258). At DEFINE-time the bind stored 0 into fn_cell → `jmp 0` → the rip=0 crash class. FIX: BINARY bind is now a transparent relay (`x86_alpha()+x86_pair_loop()`); TEXT keeps the full store (gas resolves at link). The C-store at block-emit time IS the binary bind now.
  2. **Blocks emitted AFTER the program runs (m3)**: `bb_ab_emit_nodes` was called after `emit_chain(main)` — but m3's emit_chain EXECUTES the program inline, so the first `INC(R)` fired before any block existed. FIX: `g_emit_chain_posthook` (emit.cpp/emit.h, one-shot fn-ptr fired between `codegen_flat_chain_body` and `emitter_end()`, cleared on fire so proc chains never trigger it) + scrip.c m3 path arms it with `bb_ab_posthook` (file-statics `g_ab_posthook_g/gva`) just before the main `emit_chain`. Blocks now append INTO the main graph's live JIT session.
  3. **Port label POINTERS stale in bb_ab_emit_nodes**: BINARY `x86_deflabel`/port-jmps route through `_.lbl_α_p` etc. (x86_portlbl), NOT the name strings — with `saved_emit`'s stale pointers, the α D-record defined the PREVIOUS main-chain node's label and `INC_act_α` stayed interned-but-undefined (probe: def=0 off=-1) → C-store skipped → cell stale. FIX: `g_emit.lbl_*_p = emit_label_intern(ab_lbl_*)` alongside the name strings, so D-records and the bind-fix C-store agree on ONE bb_label_t.
  4. **Body-jmp cross-session label (BINARY)**: `x86_jmp_lblptr(emit_label_intern("proc_INC_α"))` carried an X-record `bb_label_t*` from a label pool that was RESET when the proc's own emit_chain session ended → `bb_emit_end: 1 unresolved forward reference proc_INC_α` abort. FIX: BINARY body-jmp = `x86_jmpfn(rt_proc_get_fn(fname))` (movabs+jmp rax — proc JIT fn ptr is registered by the driver proc loop BEFORE the posthook fires); TEXT keeps the label jmp. `rt_proc_get_fn` declared in the file's extern-C block (a lambda-local extern got C++ mangling → the ld failure the last seat died on).
- ⭐ **NEW MECHANISM (both future-usable)**: `g_emit_chain_posthook` + the bind-fix C-store in `bb_ab_emit_nodes` (`*cell = bb_emit_buf + emit_label_intern(ab_lbl_α)->offset` post-`bb_emit_x86`, MEDIUM_BINARY only).
- ⭐ **DEFAULT FLIP**: `SCRIP_AB` default OFF at lower_snobol4.c:2007 (`_e && *_e=='1'`) — HEAD green on all DEFINE programs; every prior default-on claim now requires explicit `SCRIP_AB=1`. RE-FLIP owed with the bug#2 fix.
- ⭐ **PRIOR-CURSOR (Fable-2) CORRECTIONS**: (a) its "STILL CRASHING" was three separate crashes deep, not one — the ZOPQ-before-spill fix it committed (`a8f6316b`) was necessary but the rip=0 class was bugs 1–3 above, unreachable to a template-only diagnosis; (b) its bug#2 next-step stands verbatim and is NOW the single next action; (c) its uncommitted-tree note resolved — `a8f6316b` landed it.
- **NEXT (in order)**: (1) BUG#2 — the two fprintf probes named in Fable-2's cursor (lower_snobol4.c:2017 `ab->seal`, bb_func_activate.cpp nformals) → fix → confirm `SCRIP_AB=1` func_call prints result:10000000 → measure ms vs 1932 baseline honestly. (2) Re-flip AB default ON. (3) Kill-switch CALL gate N≥4 both modes + `.s` regen ×3 (owed — not run this seat). (4) m4 `SCRIP_AB=1` end-to-end (TEXT bind path untouched-correct in principle, unproven). (5) Then RTX-FUNC-3-corrected / -1 / -2 per the Fable step-0 census (do not redo it).
- **TREE**: SCRIP this commit = bb_func_activate.cpp (bind-neutralize + body-jmp-fn + label-ptr fix + C-store) · emit.cpp/emit.h (posthook) · scrip.c (posthook arming + statics) · lower_snobol4.c (default flip). Sanity at HEAD default: hello ✓ func_call ✓ fibonacci ✓ m4 func_call ✓ (ms:1834).
- `handoff_status.sh` is the push truth — NOT this block.

## ⛔ LIVE CURSOR — s_this (2026-08-09 Fable-2): **RTX-FUNC-0 ZD-ARM: TWO BUGS FOUND, ONE FIXED, ONE OPEN. TREE HAS UNCOMMITTED FIX IN bb_call_proc_staged.cpp. STILL CRASHING.**
- ⭐ **PRIOR CURSOR CORRECTION:** RTX-FUNC-0 ZD code was NOT absent — it was already written into `bb_call_proc_staged.cpp` at `69ab50bf` (the AB-3b commit). The step-0 zero counts were from `rc=139` segfault in the interposer, not genuine zero calls. The ZD AB-3b arm fires and crashes.
- **BUG #1 — FIXED (uncommitted):** In the ZD AB-3b block (bb_call_proc_staged.cpp ~:246), `sub rsp, scc_sb_z` (save-set spill) happened BEFORE the `ZOPQ(i, 0/8)` reads. ZOPQ displacements are relative to rsp at box entry; the spill shifted rsp by `scc_sb_z` bytes, making every actual-install read land at the wrong address → segfault. Fix applied: moved the FOR loop installing actuals from ZOPQ into GVA slots to BEFORE the `sub rsp`. **This fix is in the working tree, NOT committed.**
- **BUG #2 — OPEN (still crashing):** The activation block (`bb_func_activate.cpp` null-formals loop, :126) nulls out formal N even after the call site installs it. The null-skip condition is `k >= 1 && k <= nformals` where `nformals = _.op_ab_nformals = nd->seal`. The compiled `.s` shows N being zeroed (line 27 of the INC_act_α block) → `nformals` is 0 at emit time despite lower_snobol4.c:2016 writing `ab->seal = d.nformals`. **Root cause not yet pinned.** Next seat: check (a) does `lc_build` zero-init the whole node so the seal write at :2016 must survive to the `g->ab_nodes[]` walk; (b) is there any code path that resets seal after :2016; (c) add `fprintf(stderr, "ab->seal=%d\n", ab->seal)` at lower_snobol4.c:2017 to confirm nformals at lower time, and `fprintf(stderr, "nformals=%ld\n", nformals)` at bb_func_activate.cpp:66 to confirm at emit time. One of those two will differ → that's the bug.
- **TREE STATE:** SCRIP `d2328f81` + uncommitted fix in `src/templates/bb_call_proc_staged.cpp` (ZOPQ-before-spill reorder). `git diff --stat HEAD` shows the change. `.github` has local commit `7e6a5778` (prior cursor + finding), NOT pushed. After bug #2 fix: commit both together, regen `.s` ×3, run kill-switch gate N≥4, measure func_call ms, then push.
- **SCRIP_AB=0** confirms the fix direction: micro (INC(3)) outputs 4 with SCRIP_AB=0, crashes without it.
- **NEXT AFTER BUG #2:** Run `util_rtx_arm_census.sh` to confirm `rt_ab_enter_env` commits > 0. Kill-switch gate N≥4. Regen `.s` artifacts. Measure func_call ms vs ms:1899 baseline. THEN RTX-FUNC-1/2 inline enter/leave per prior cursor census.

## ⛔ LIVE CURSOR — s_this (2026-08-09 Fable): **RTX-FUNC STEP 0 KILLED THE RUNG AS WRITTEN: THE AB-3b ARM IS DEAD AT HEAD `d2328f81` — rt_ab_enter_env counts ZERO on func_call m3 (open_slim counts 10,000,000). THE ZD SCOPE IN bcps_det_arm EARLY-RETURNS AT :229 WITH ITS OWN PROBE (:223, scc_z=1) AND EMITS THE CLASSIC SLIM PROTOCOL, BOTH MODES. SCRIP_ZD_PROC=0 DOES NOT RELEASE IT (wrong gate — that env is the emit.cpp:1928 planner admission).**
- ⭐ The block below this one claims "AB-3b LANDED … func_call −11%" at pre-rebase `07b56155`; the claim is NOT reproducible at HEAD (= post-rebase `69ab50bf`+README). −11% provenance OPEN: rebase-dropped hunk vs arm-state at measurement. Do NOT re-quote that number.
- ⭐⭐ **RE-ORDERED RUNG PLAN: RTX-FUNC-0 (NEW, FIRST) = port the AB-3b call-site flip into the ZD scope's `scc_z` leg (bcps_det_arm :231–252, `_z` suffix — the old cursor's NEXT(1) location, now the ONLY live path). THEN RTX-FUNC-1/2 inline enter/leave.** RTX-FUNC-1/2 as written are the s216 vacuous-port class at HEAD — §7 step 0(d)/(f) caught it pre-port.
- ⭐ STEP-0 CENSUS FOR -1/-2/-3 IS DONE — see `FINDING-2026-08-09i-…AB3B-ARM-IS-DEAD…md` §3 (do not redo): enter is straight-line (0(f-pre) discharged) and also zeroes `rt_g_want_name`; vtmark is a READ of `g_pl_trail.top` (offset 32, rtx_plcall.S precedent + existing `_Static_assert`) NOT an `inc`; β fast-path discriminants = `top==vtmark && rt_g_ret_by_name==0`; exported both-media: Σ Σlen g_pl_trail rt_g_want_name rt_g_ret_by_name kw_fnclevel; **rt_k_level is HIDDEN (dynamic-table ABSENT) ⇒ RTX-FUNC-3 must EXPORT it (opposite of the rung text) AND convert rtx_call.S:89/:144 rip-direct decs to GOT-indirect (r11 free) in the same commit or the .so link breaks.** Both-medium data primitive exists: `x86_load_got` (x86_asm.h:265).
- Tree state: SCRIP untouched (diagnostics reverted, rebuilt, sanity green: micro n=5 s=xxxxx, func_call ms:1899). No SCRIP commit this session.
- NEXT: (1) RTX-FUNC-0 per above — template surgery in the ZD scope, kill-switch CALL gate N≥4 both modes, both-medium, `.s` regen ×3 at handoff. (2) Then RTX-FUNC-3-corrected (rt_k_level export + rtx_call.S GOT fix). (3) Then RTX-FUNC-1/2 inline. (4) Re-establish the −11% number honestly on the 3-arm rail after (1).

## ⛔ LIVE CURSOR — s_this (2026-08-09 Sonnet 4.6): **AB-3b LANDED: call-site flip + body-jmp fix + nformals pipeline. func_call -11%, fibonacci -11%. WATERMARK: kill-switch gate m3 IDENTICAL=112..113 QUARANTINE=5..8 MOVER=2 (pre-existing 153/154 only). SCRIP `07b56155`.**
**⭐⭐ WATERMARK (re-proved post-commit `07b56155`, full fresh libscrip_rt rebuild): kill-switch gate m3 N=4 on 122 pattern programs: IDENTICAL≈113 QUARANTINE≈7 MOVER=2. MOVERs are pre-existing documented failures (153_pat_operand_edge_matrix, 154_pat_construction_time_hoist). ARBNO-family programs fluctuate between QUARANTINE and MOVER run-to-run due to gate-OFF non-determinism — not regressions from this seat. Zero regressions from AB-3b.**
**✅ AB-3b LANDED (SCRIP `07b56155`): DEFINE call-site flip eliminates rt_proc_call_open_slim + rt_proc_open_fn round-trips in m3 binary mode. Three components: (1) body-jmp fix in bb_func_activate — was jmp [fn_cell$FN] (self-loop: DEFINE stores &FN_act_α there, and the body-jmp inside FN_act_α reads the same cell → infinite recursion); fixed to x86_jmp_lblptr(emit_label_intern("proc_FN_α")) — direct jmp to the SNOBOL4 proc body label, known at emit time. (2) null-loop fix — skips formal cells (k=1..nformals); call site pre-installs actuals there before jmp fn_cell; only result (k=0) and locals (k>nformals) are nulled. (3) nformals pipeline — stored in ab->seal field (spare on IR_FUNC_ACTIVATE), surfaced as g_emit.op_ab_nformals in emit dispatch, consumed by the null loop and the call site template. Call site (bb_call_proc_staged scc&&!c2 arm): when g_emit_cfg->ab_n>0, replaces open_slim+open_fn+arg-install with: save-set spill → install actuals into GVA formals inline → lea rcx L(8); lea rdx L(9); jmp [fn_cell$FN] → L(8)/L(9) = add rsp,scc_sb; jmp L(2). SCRIP_AB=0 hatch: classic open_slim path unchanged (byte-identical).**
**⭐ FINDING: func_call 2124ms → 1882ms (−11%), fibonacci 548ms → 486ms (−11%) in m3. The AB-3b arm fires in m3 binary mode (MEDIUM_BINARY). The m4 TEXT artifact (func_call.s) still shows open_slim because that benchmark's call site goes through the non-SCC classic route in m4 — the SCC probe returns 0, so the scc&&!c2 arm (where AB-3b lives) never runs in TEXT mode for this program. m3 speedup is real. .s artifacts are honest current compiler output.**
**⭐ DESIGN NOTE: bb_ab_fn_cell_ptr exported as non-static (bb_func_activate.cpp + bb_templates.h declaration) for call-site template access. emit.h: op_ab_nformals field added to g_emit.**
**⛔ NEXT: (1) Extend AB-3b to the ZD-armed scc path in bcps_det_arm() (lines 231–252 of bb_call_proc_staged.cpp) — same pattern, different variable names _z suffix. (2) Extend AB-3b to m4 TEXT path: the non-SCC classic arm (rt_arg_stage + open_slim) in func_call.sno suggests the SCC probe declines for this benchmark in m4; investigate why scc==0 in TEXT mode for INC — may need SCRIP_SCC_OFF check or g_gva_active state. (3) ARM B2 for rt_cap_open (pre-existing, see prior cursor block). (4) Remaining ungradeable RTX-8 symbols.**
**`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ LIVE CURSOR — s_this (2026-08-09 Sonnet 4.6): **RTX-8 CAP-OPEN ARM A LANDED. WATERMARK HOLDS. cap_imm_nret2 WORKLOAD MINTED FOR rt_cap_finish.**
**⭐⭐ WATERMARK (re-proved post-port, SCRIP `3f08422f`): m3 fail set IDENTICAL to prior seats (42 programs). Kill-switch gate PASS both modes N=4: m3 IDENTICAL=155/QUARANTINE=0/MOVER=0 · m4 IDENTICAL=150/QUARANTINE=0/MOVER=0/SKIP=5. Corpus has grown (318 total vs 317 prior); the 42-program fail set is unchanged — zero regressions.**
**✅ RTX-8 rt_cap_open ARM A LANDED (SCRIP `3f08422f`): 100001 COMMITS / 0 bails on cap_imm_nret.sno (both modes). STEP 0 all six checks passed. Rename: c_rt_cap_open in pattern_match.c. ARM B (*VAR/NRETURN path) delegates to C — file-static g_capx/g_capx_top/g_capx_cap/rt_cas_carve are NOT reachable from a separate .S TU; ARM B2 also calls rt_cap_finish, so its port is a separate decision.**
**✅ cap_imm_nret2 WORKLOAD MINTED (corpus `2f7e9496`): `$ *STORE()` immediate-dollar NRETURN target. 200002 rt_cap_open entries, 100001 c_rt_cap_open (ARM B), 100001 rt_cap_finish. Exact 2.00x scaling (N=100→101, N=1000→1001, N=100000→100001). Two-sided gate ON=OFF=ref. This workload grades rt_cap_finish when ARM B2 is ported.**
**⭐ ARM CENSUS FINDING (FINDING-2026-08-09-...-CAP-OPEN-ARM-CENSUS...): cap_imm_nret.sno exercises ARM A only (100001 entries, plain-name NV_SET_fn path). PAT2 '. *STORE()' routes to CAPTURE_COND r12-push arm — never calls rt_cap_open. rt_cap_finish=0 on cap_imm_nret.sno explained: the template skips rt_cap_finish when rt_cap_open returns 0 (ARM A always returns 0). rt_cap_finish is NOT a phantom — live in .so, called on ARM B2. cap_imm_nret2.sno is the workload that grades it.**
**⛔ NEXT: (1) Lon direction on remaining ungradeable symbols (rt_match_value_get_pat_fn, rt_match_value_open, rt_scan_splice_empty, rt_dcap_flush, rt_cap_finish [ARM B2, g_capx promotion needed], rt_match_capture, rt_defer_step [434 static sites, 0 dynamic, per prior cursor]); (2) RTX-4 CALL surface pending Lon ruling; (3) rt_cap_finish ARM B2 port requires promoting g_capx/g_capx_top/g_capx_cap/rt_cas_carve from file-static to visibility(hidden) in pattern_match.c — straightforward but touches shared state. CREDENTIAL NEEDED FROM LON FOR PUSH.**
**`handoff_status.sh` is the push truth — NOT this block.**

### ⚠ CONCURRENT-SEAT ADDENDUM — s_this (2026-08-09, Sonnet 4.6 fourth seat, REBASING SEAT for 7 ZK-2/SLACK-RIDER/GAMMA-EXIT commits)
**⭐⭐ POST-REBASE WATERMARK CORRECTION: the top cursor's "m3 42 fail IDENTICAL" was measured at SCRIP `3f08422f` (pre-rebase). After `git pull --rebase` absorbed 7 concurrent commits (ZK-2 × 5 + SLACK-RIDER + GAMMA-EXIT + .s regen), the fail set shifted. COLLISION NOTED PER PROTOCOL — never silently dominated.**
**✅ POST-REBASE BOARD (SCRIP `d553f6aa`, post-rebase HEAD, full `touch rt.h && make libscrip_rt && make scrip`): m3 **33 fail / ~285 pass** (was 42). +14 programs now PASS (SLACK-RIDER/GAMMA-EXIT improvements to ARBNO/FENCE family — `118 119 124 129 130 143 148 149 150 161 165 180 181 183`). −5 programs now FAIL (new from same concurrent commits, all SEGV gate-OFF / C-path, so NOT caused by rt_cap_open port: `052_pat_arbno 054_pat_arbno_alt 065_capture_then_arbno 116_pat_arbno_of_fence_inline 151_pat_arbno_inline_fence_backtrack`). Zero regressions attributable to this seat's work.**
**⭐ cmovs BUG FOUND AND FIXED: the asm stub as committed used `cmovs eax, r9d` with comment "r9d is 0 at entry" — FALSE, r9d is caller-saved and unguaranteed. Fixed to `jns .Lcap_len_ok / xor eax,eax / .Lcap_len_ok:` (explicit branch). Bug was caught during pre-build audit, fixed before first build. Amended into commit `d553f6aa`.**
**✅ WORKLOAD ORACLE-CORRECT POST-REBASE: `cap_imm_nret.sno` md5 byte-identical to `.ref` at HEAD `d553f6aa` (both modes). Kill-switch gate PASS: QUARANTINE={153,154,160} — all SEGV gate-OFF (pre-existing C-side bugs, unchanged by this rung). Arm census post-rebase: COMMITS=100001 BAILED=0 ✅.**
**⛔ NEXT: same as top cursor — Lon direction on remaining ungradeable symbols; rt_cap_finish ARM B2 (cap_imm_nret2.sno is the grading workload, already minted); RTX-4 CALL surface ruling.**
**`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ LIVE CURSOR — s_this (2026-08-09 Fable, third seat): **CAP_IMM_NRET WORKLOAD MINTED — `rt_cap_open` OUT OF THE UNGRADEABLE SET (100,001 entries, exact 2.00×). VSTACK FINDING = INDEPENDENT CONFIRMATION OF f9e341a8. WATERMARK: agree 259/58/0 current.**
**✅ WORKLOAD MINT (slice-9 recipe, corpus `b70e218a`): `benchmarks/snobol4/cap_imm_nret.sno` + oracle `.ref` — 100K-iter immediate-`$` + computed-name `. *STORE()` NRETURN capture, deterministic output, m3 byte-identical to ref at `64e1bcbf`. Census: `rt_cap_open` **100,001** entries, **200,001** at 2× bound (exact 2.00×, +1 preserved) — GRADEABLE. ⚠ Entry count cannot name an arm: run the interposer arm census on THIS workload before any port.**
**⭐ MEASUREMENT FROM THE MINT: the computed-name `*STORE()` capture answers CORRECTLY (`slot: abc`) with ZERO `rt_cap_open` fbytes entries and `rt_cap_finish`=0 — the m3 emitter routes computed-name conditional capture around the C pair entirely (NCB-1b box / rbp-dcap arm). ⇒ `rt_cap_finish` cannot be minted gradeable from SNOBOL4 source until someone establishes which emitted arm still calls it. `rt_dcap_flush`/`rt_dcap_end_ok`/`rt_match_value_open` also 0 on this workload.**
**⚠ COLLISION RESOLVED (this rebasing seat): three RTX seats ran 2026-08-09. Seat A (kill-switch N=8) reads m3 259/58/0 at `d01137ac` (−1 = concurrent commits); seat B (`f9e341a8`) reads 259/58/0 as TRUE and this seat's earlier 260 as stale-build; this seat re-ran `161_pat_defer_fn_nested_match` on its own `64e1bcbf` build: **FAIL** — corroborates 259 regardless of which mechanism. All three seats now agree: **m3 259/58/0 is current.** No seat regressed anything. FINDING: `FINDING-2026-08-09-CLAUDE-SN4-RTX-VSTACK-DECLS-ALREADY-GONE-AND-CAP-OPEN-GRADEABLE-AT-100K.md`.**
**⛔ NEXT: (1) arm census on cap_imm_nret before any rt_cap_open port; (2) trace which emitted arm (if any) still calls rt_cap_finish — else it is RTX-12 eradication territory; (3) remaining ungradeables still need workloads or Lon direction; (4) RTX-4 CALL surface pending Lon ruling.**
**`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ LIVE CURSOR — s_this (2026-08-09 Sonnet 4.6): **KILL-SWITCH GATE N=8 BOTH MODES 131 PATTERN PROGRAMS — GATE PASS. ALL 5 MOVERS PRE-EXISTING. WATERMARK HOLDS AT `64e1bcbf`. NEXT: Lon direction on RTX-8 remaining candidates (ungradeable) or RTX-4 CALL surface or RTX-10 MISC/IO.**
**⭐⭐ WATERMARK HEAD `d01137ac` (post-rebase), N=1: m3 **259/58/0** · m4 **222/66/29 SKIP** · DIVERGE **14**. m3 -1 from concurrent ZK-2-FIX/MECH-BUG7 commits — zero regression from RTX seat. m4 and DIVERGE identical to session-open. `handoff_status.sh` is the push truth — NOT this block.**
**✅ KILL-SWITCH GATE (N=8, both modes, 131 pattern programs): m3 IDENTICAL=117 QUARANTINE=11 MOVER=3 · m4 IDENTICAL=111 QUARANTINE=14 MOVER=2 SKIP=4. All 5 movers (`056_pat_star_deref`, `127_pat_json_keyvalue`, `153_pat_operand_edge_matrix` ×2, `154_pat_construction_time_hoist`) are pre-existing documented failures already in crosscheck FAIL list — none are regressions from the RTX seat. Gate OFF is the C fallback; instability visible gate-OFF cannot be caused by asm. GATE PASS. FINDING: `FINDING-2026-08-09-CLAUDE-SN4-RTX-MATCH-KILLSWITCH-GATE-N8-BOTH-MODES-131-PATTERN-PROGRAMS-ALL-MOVERS-PRE-EXISTING.md`.**
**✅ CENSUS SWEEP (m3, pattern_bt.sno): RTX-8 ported symbols all commit — `rt_defer_get_pat_fn` 5.5M, `rt_match_enter`/`rt_match_ctx_restore`/`rt_dcap_end_ok_close` 500K each. Ungradeable symbols (`rt_match_value_get_pat_fn`, `rt_match_value_open`, `rt_scan_splice_empty`, `rt_dcap_flush`, `rt_cap_push`, `rt_cap_open`, `rt_cap_finish`, `rt_match_capture`, `rt_defer_step`) still zero on pattern_bt + claws5-match. m4 census SEGVs universally in this container (preload .so incompatible with m4 binaries — infrastructure limitation).**
**⛔ NEXT: Ungradeable symbols need wider corpus workload or Lon direction. RTX-10 MISC/IO needs Lon direction on which symbols to port (vstack declarations already absent — cleaned prior). RTX-4 CALL surface pending Lon ruling. vstack dead-decl audit confirms zero `rt_push_*/rt_pop_*/rt_halt_tos` anywhere in src/.**
**`handoff_status.sh` is the push truth — NOT this block.**

### ⚠ CONCURRENT-SEAT ADDENDUM — s_this (2026-08-09, second RTX seat; landed AFTER the block above, does NOT supersede it)
**⭐⭐ SEMANTIC COLLISION RESOLVED AND NOTED, per the CONCURRENCY PROTOCOL (never silently dominated): the block above attributes m3 260→**259/58/0** to "concurrent ZK-2-FIX/MECH-BUG7". THIS SEAT REACHED 259/58/0 INDEPENDENTLY, AT `64e1bcbf`, WITH NO CONCURRENT COMMITS IN THE TREE — by rebuilding. Session-open `make libscrip_rt` printed "Nothing to be done" and the 260/57/0 baseline graded that never-rebuilt `.so`; after `touch src/runtime/rt/rt.h` + a FULL rebuild the same source read 259/58/0, the mover being `161_pat_defer_fn_nested_match` (deterministic, 10/10 identical md5, output `fail2/calls=3` vs ref `match2/calls=2` — NOT the `160` non-determinism class).** ⇒ **TWO SEATS, TWO CAUSES, ONE NUMBER: 259/58/0 is likely the TRUE m3 and 260/57/0 a STALE-BUILD ARTIFACT — the s_2026-07-30 SUBJ-CELL "BUILD OK" trap. If so, every seat quoting 260 quotes a stale number ⇒ SHARED STATE, outranks RTX-10.** Cheap decisive test for whoever is next: `touch src/runtime/rt/rt.h && make libscrip_rt` at pristine HEAD, then run `161` alone.
**✅ PHANTOM CENSUS: `rt/rt.h` declares 185 functions, **83 never defined anywhere** (checked `.so` + `scrip` binary + every `.o`, then src-cross-checked for unbuilt code); 51 have ZERO refs outside their own declaration — 36 non-Prolog, 15 Prolog-family (left alone: PL-FR-4 + PL-ZETA-CELLS are live seats that may be about to implement them). ⭐⭐ **THE LADDER STRUCK `rt_nv_get`/`rt_nv_set` (s204), `rt_match_variant` (s216), `rt_incr`/`rt_decr`/`rt_neg`/`rt_exp` (s204), `rt_concat`/`rt_lcomp`/`rt_acomp` (RTX-3) IN PROSE AND NEVER DELETED THE DECLARATIONS** — which is exactly why they are re-discovered and re-`nm`-ed every few sessions. **PROPOSED ARCH §7 step-0 amendment: strike dead names in the rung text, the table, AND the header, in the same commit.** ⚠ `rt_match_blob` is a 3-line declaration — delete to the terminating `;`, never by line number.**
**⛔ The 36-symbol deletion was ATTEMPTED (222→184 lines, build clean) and **REVERTED UNPROVEN** because `161` moved; tree left CLEAN, zero diff vs HEAD. The decisive pristine A/B could not finish: **a full `libscrip_rt` rebuild (~4-5 min) exceeds one command window and a detached `nohup make` is KILLED with the tool session** — container limitation of record; build in polled segments or get a longer window, and never conclude from a build not watched reach "Built:". FINDING: `FINDING-2026-08-09-CLAUDE-SN4-RTX-RT-H-CARRIES-83-PHANTOM-DECLARATIONS-AND-THE-LADDER-STRIKE-NEVER-DELETED-THE-CODE.md`.**

## ⛔ PRIOR CURSOR — s_this (2026-08-08 Sonnet 4.6): **RTX-8 DEFER-GET-PAT-FN LANDED. WATERMARK HOLDS. NEXT: remaining RTX-8 candidates need wider corpus or Lon direction.**
**⭐⭐ WATERMARK HEAD `8ecb5cd7`, N=1, `setarch -R`: m3 **260/57/0** · m4 **243/73/1 SKIP** · DIVERGE **16**. Identical to pre-port baseline — zero regression. `handoff_status.sh` is the push truth — NOT this block.**
**✅ RTX-8 `rt_defer_get_pat_fn` NV_GET ARM LANDED (`8ecb5cd7`): 5,500,011 COMMITS / 0 bails on `pattern_bt` (N=500K, 2.00× scaling at N=1K/2K). All six ARCH §7 step-0 checks passed. Kill-switch gate PASS both modes N=4 (m3 IDENTICAL=154/QUARANTINE=1/MOVER=0 · m4 IDENTICAL=148/QUARANTINE=2/MOVER=0/SKIP=5). Quarantine set pre-existing. C body renamed `c_rt_defer_get_pat_fn`, declarations added to `rt/rt.h`. ERADICATION slice — no speed number claimed (s224 rail refuses this machine). FINDING: `FINDING-2026-08-08-CLAUDE-SN4-RTX8-DEFER-GET-PAT-FN-NV-GET-ARM-LANDED.md`.**
**✅ PHANTOM CENSUS (step-0 sweep, this session): `rt_cap_assign_cursor` and `rt_defer_match` are NOT in `nm -D out/libscrip_rt.so` — phantoms, eliminated.**
**⛔ UNGRADEABLE ON CURRENT CORPUS (step-0(d) zero on `pattern_bt` + `json.sno`): `rt_match_value_get_pat_fn`, `rt_match_value_open`, `rt_scan_splice_empty`, `rt_dcap_flush`, `rt_dcap_end_ok`, `rt_cap_push`, `rt_cap_open`, `rt_cap_finish`, `rt_match_capture`, `rt_defer_step`. Need workloads that reach them or wider corpus sweep before any port.**
**⛔ PRE-EXISTING DEFECTS (Lon routing): `rt_match_replace` cross-frame SEGV → CLIMB territory. 140/141 pcall mismatch → Lon routes.**
**⛔ NEXT: RTX-8 remaining live candidates need workloads with commits. Widen corpus or Lon direction on which family sub-group to target next.**
**`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRUNE RECORD — s_this (2026-08-09, third seat)
Three PRIOR blocks (2026-08-08: CENSUS-M4-ARM · CHAIN-ENTER · EVAL-RETURN+DEFER-LATCH) DELETED per RULES (c). Kept: this LIVE + seat-A LIVE + seat-B addendum + 2026-08-08 DEFER-GET-PAT-FN = ~4 units. **Zero `- [ ]`/`- [x]` rungs in cut range — grep-verified 0 before deleting.** Full text in `.github` git history. Prior PRUNE RECORDs preserved in history.

## ⚠ CONCURRENCY CONTRACT — RTX ‖ ζ-STORAGE (minted s186)

RTX and the ζ-storage ladder are designed to be safely parallel, but **only for phase-1 rungs.** Know which side of the line you are on.

**SAFE — RTX phase 1 (RTX-1 … RTX-10) touches ZERO templates BY CONSTRUCTION** (the ladder's own EVERY-RUNG line). Phase 1 keeps every exported symbol's EXACT current C signature; speed comes from bodies, not convention. Consequence: **no `.s` regen owed, no both-medium work, no `x86_asm.h` edit.**

| | RTX owns | ζ-storage owns |
|---|---|---|
| files | `src/runtime/rtx/*.S`, `rtx_abi.inc`, `rtx_init.c`, `src/runtime/rt/*.c`, family C sources (`gc_heap.c`, `string_ops.c`, `rt.c`, …) | `src/contracts/zeta_storage.c`, `src/emitter/emit.cpp`, `src/templates/*.cpp`, `x86_asm.h` |
| scripts | `test_rtx_unit.sh`, `util_rtx_symbol_inventory.sh`, `test_gate_rtx_inventory_live.sh` | the FC gate + `util_fc_conversion_map.py` + spine census |

**⛔ NOT SAFE — SERIALIZE WITH LON:**
- **RTX-11 (RTX-CC)** — registerized ABI changes template call sequences via `x86(...)` encoders in `x86_asm.h`, and **fires `.s` regen ×3**. That is the ζ ladder's own territory. **RTX-11 must not run concurrently with ZB-VAL/ZLS work.**
- **RTX-12 (eradication)** — deletes `c_*` bodies and gates; re-gates on the full crosscheck. Land it when the board is quiet.
- **`Makefile` link lines** — both ladders may add objects. Small, but a real rebase point.
- **THE WATERMARK ITSELF IS SHARED STATE.** Both sessions gate on the same crosscheck numbers. If one side moves the watermark mid-flight, the other's "no regression" claim is measured against a baseline that no longer exists. **Re-prove at session start; say the numbers out loud in the FINDING.**
- **RTX-7 (NV/GVAR)** — `NV_SET_fn` is DB-1's planned write-barrier choke point. Coordinate or land DB-1 first.
- **RTX-8 (MATCH SINKS)** — touches `pattern_match.c`, which the SN4-PAT / match-family rungs also reach. Check the other session's cursor before starting.

Per RULES.md s47 ⚠ CONCURRENCY: parallel sessions are safe for CODE (git rebases cleanly), and **`PLAN.md`'s Step column is stale BY DESIGN** — orientation trusts THIS file's `LIVE CURSOR`, never PLAN.md's table.

---

## Session Setup

```bash
git config --global user.name "LCherryholmes"
git config --global user.email "lcherryh@yahoo.com"
git clone https://github.com/snobol4ever/.github.git /home/claude/.github
git clone https://github.com/snobol4ever/SCRIP.git   /home/claude/SCRIP
git clone https://github.com/snobol4ever/corpus.git  /home/claude/corpus
git clone https://github.com/snobol4ever/x64         /home/claude/x64   # SPITBOL oracle
bash /home/claude/SCRIP/scripts/install_system_packages.sh
cd /home/claude/SCRIP && rm -f scrip && make -j4 scrip && make libscrip_rt
```

**READ-FIRST SET (NON-NEGOTIABLE, in this order):**
1. `RULES.md` in full — especially MONITOR-FIRST, TEMPLATE-ONLY EMISSION, and the HANDOFF-COMPLETE gate.
2. **`ARCH-SNOBOL4-RTX.md`** — the register/ABI/build contract. §7 is the per-rung checklist; §7 steps **0 / 0b / 2b** are the three hard-won ones.
3. The family's C source **in full**, then its `sbl.asm` counterpart for shapes (steal idioms, never structures).
4. **The SPITBOL manual chapter for every construct the family implements** (Lon standing directive). **CALL = Ch.8 `Program-Defined Objects`** (DEFINE, RETURN, FRETURN, the prototype string, local-variable save/restore, by-name args) **+ Ch.9 `Advanced Topics`** (NRETURN, run-time compilation). ⛔ **NOT Ch.13 — that is `Running SPITBOL`, command-line options.** s165 recorded this correction INSIDE a rung and never fixed the instruction, so three more sessions were pointed at the wrong chapter; the rung is now gone and the fix lives HERE, where it is actually read (s187). Verified against the manual's own TOC: Ch.8 p.101, Ch.9 p.121 (NRETURN p.133), Ch.13 p.161 · ARITH = mixed-mode coercion · MATCH = Ch.9 deferred evaluation + p.122 well-foundedness + p.125 FENCE.

**Verify the watermark BEFORE touching anything**, then run the ladder's first `- [ ]`.

---

## ⭐⭐ SN4-RTX — TOTAL RUNTIME REDESIGN IN OPTIMIZED x86 ASM (Lon PIVOT directive, s162 2026-07-25)
**RULING OF RECORD (promotes the s161 caveat to head rung):** the C runtime is a TRANSITIONAL ARTIFACT. The target SNOBOL4 runtime is hand-written, register-aware x86-64 assembly. We use REGISTERS freely; System V conventions bind ONLY at (a) libc call boundaries and (b) the m3 driver→blob entry edge. References: **SPITBOL x64 asm runtime** (`/home/claude/x64` — Minimal registers pinned to hard regs: IA=r12 W0=rax WA=rcx WB=rbx WC=rdx XL=rsi XR=rdi CP=r13 RA=xmm12; C-boundary = save-all-to-global-block/call/restore, `int.asm`; the runtime itself = `bootstrap/sbl.asm`, 19,064 lines) · **CSNOBOL4** C runtime (semantics reference; `git clone https://github.com/snobol4ever/csnobol4`, build via `scripts/build_csnobol4_oracle.sh`) · **SIL** macro source (algorithm reference — locate/fetch at need).
**RTX-0a MEASURED SURFACE (s162, static call sites in `claws5.s` — matches the s161 dynamic anatomy):** `str_concat_d`×38 · `rt_subscript_var`×33 (TABLE) · `rt_deref`×24 · `rt_call_arr`×22 + `rt_proc_call_epilogue_`×14 + `rt_proc_open_fn`×8 (the NRETURN/call path) · `rt_num_arith`×8 · `rt_binop_overload`×8 · `rt_defer_step`/`rt_dcap_step`×6 each · `rt_coerce_num2_d`×6. Current C runtime = **~21k LOC** (`src/runtime/`: top-level model ~12.6k, `rt/` 4.1k, `core/` 3.6k, `builtins/` 0.7k).
- [ ] **RTX-4 SLICE 3 — THE REST OF THE CALL SURFACE.** ⛔⛔ **PREMISE FALSIFIED BY MEASUREMENT, s188 — THE RUNG AS WRITTEN NAMED A COLD SYMBOL.** It read: *"`rt_call_arr` FIRST AND ALONE: 232 static refs, #1 symbol in the entire runtime … the largest single remaining lever."* **`rt_call_arr` passes step 0(a)(b)(c) — live, byte-identically spelled, linkage correctly classified — and is CALLED 5/16/8/12 TIMES on the four rail demos, FLAT ACROSS N.** The 232 is a count of emitted call SITES (the CALL BOUNDARY), exactly as ARCH §5 caveat (a) warned in writing at RTX-2 and was then not applied. **FOURTH member of the phantom family and the first LIVE one: RTX-2 DEAD names · RTX-3 INVENTED names · s165 MISRECORDED name · s188 a real, correct, COLD name.** ⇒ **STEP 0(d) MINTED: PROVE THE SYMBOL EXECUTES IN THE WINDOW YOU INTEND TO MOVE, BEFORE PORTING.** RE-TARGETED, pending Lon's ruling: port **`rt_call_arr` FUSED WITH `try_call_builtin_by_name`** — the two are called **1:1 in all 16 benchmarks**, so the 13-line setjmp wrapper alone leaves 100% of the dispatch in C — and gate it on `string_manip` (10.0M calls) · `eval_fixed` (1.0M) · `roman` (400K), **NEVER on the rail demos.** ⚠ `APPLY_fn` is **0 in all 16** (ordinary `DEFINE` calls go via `rt_proc_call_epilogue_*`, per s187's 18 movers). ⚠ Two dead ends pre-closed: `setjmp` already lowers to `_setjmp@plt` (no sigmask syscall — do not chase it) and `try_call_builtin_by_name` is **already** a `bid_of()` hash + generation-stamped `g_dtax` inline cache + `(len<<8)|fn[0]` jump table, NOT a strcmp chain — an asm port inherits that algorithm and wins only `-O0` ceremony. THEN: `rt_proc_call_open(_slim)` · `rt_proc_open_fn` · `rt_call_named_proc` · `rt_arg_stage` + proc_set family. FINDING: `FINDING-2026-07-27d-...-CALL-ARR-IS-COLD-...md`.
  ⛔ **STEP 0 IS NOW THREE CHECKS, NOT TWO** (s187): (a) live definition exists; (b) the doc's spelling round-trips byte-identically against the tree; (c) ⭐ **NEW — `nm` the LINKAGE of every global the port will touch.** Exported (`B`) ⇒ preemptible ⇒ `@GOTPCREL`; hidden ⇒ rip-direct. In the C they look identical. Slice 2 met this because a self-contained port reaches globals the previous slice only reached THROUGH a C call.
  ⛔ **AND DESIGN THE FALSIFICATION BREAK BEFORE YOU WRITE IT.** A break that is a no-op at its own site produces a green board that reads as 'unreachable' and ships an unexercised port. Ask: *what would the output look like if this code did not exist at all?* If that equals the passing output, the probe is vacuous. (s187's ω needed an INVERSION, not a corruption.)
- [x] ~~**RTX-4 DIFFERENTIAL UNIT BATTERY.**~~ **DONE s_this (2026-08-08) — SCRIP `28ef6caf`. `rtx_call_test.c` 9 cases PASS. See cursor.**
- [ ] RTX-6 (residual) **ARITH — RETARGETED TWICE; READ THIS WHOLE LINE BEFORE PLANNING ANY INLINE WORK.** `rt_cmp_d` LANDED s203. ⛔ **THE INT-INT INLINE FAST PATH THIS RUNG NAMED FOR THREE SESSIONS WAS ALREADY BUILT** — `bb_binop_arith.cpp` arm 3 did it, with compile-time immediate elision on top, i.e. strictly better than this rung's own spec (s206, measured from source). **PHANTOM RUNG: planned work that is already done — the phantom family one level up from symbols, and `nm` cannot catch it; only reading the emitting template arm can.** ⛔ **AND THE INSTRUMENT POINTED AT THE OTHER ARM:** `arith_mixed`'s 40,000,001 calls are **100% DT_R+DT_R** (interposer census, s206) ⇒ an int-int port grades a guaranteed 1.000× there. ⭐ **SUPERSEDED s207 BY LON DIRECTIVE** — *"for now all these should be ONE CALL into the runtime. We'll optimize the inline later."* All four arms collapsed to one per-op call (`rt_add`/`rt_sub`/…); **inline work is DEFERRED BY DIRECTION, not abandoned.** Measured cost of the deferral, so the inline rung has an honest floor to beat: `arith_loop` **0.64×**, `arith_mixed` **0.80×** (RT_OPT=-O0). ⭐ **WHEN INLINE RESUMES, TAKE THE FREE ~11.3 ns/call FIRST** — that is one extra `-O0` frame from the `rt_<op>`→`rt_num_arith` hop, reclaimable by giving the per-op entries their own body, and it needs NO inlining and NO `x86_asm.h` change. **THEN** the real-real SSE arm (s206 spec: both-tags-`DT_R` → `movsd`/`addsd|subsd|mulsd`; ADD/SUB/MUL only — DIV needs a zero-test (`arithmetic.c:239`), MOD needs `fmod`, POW needs `pow`; ⛔ both-same-tag only, any string tag out-of-line because `'1.5'` IS a real operand per Ch.17 String→real + `operand_is_real_str`). ⛔ That arm needs NEW SSE ENCODERS in `x86_asm.h` (`addsd`/`subsd`/`mulsd`/`ucomisd`/`cvtsi2sd` do not exist; `movsd` handles only the `f64:` immediate form) ⇒ **RTX-11/12 concurrency collision is unavoidable for it and is Lon's call.** ⛔ **~~incr decr neg exp~~ ARE PHANTOMS — struck s204 per ARCH §7.**
- [ ] RTX-7 **NV/GVAR — ⛔⛔ DOWNGRADED s208: NOT A SPEED RUNG. DO NOT PORT THIS FOR PERFORMANCE.** `NV_GET_fn`/`NV_SET_fn` (✅ both verified LIVE s204) + `rt_gvar_*` + `rt_gva_island` + `rt_subject_load_nv`. ⛔ **ARCH §5's NV row also carried `rt_nv_get`/`rt_nv_set` — BOTH PHANTOMS, struck s204, re-confirmed s208 by `nm` (zero definitions).** ⛔ **STEP 0(d) FAILED, MEASURED s208:** `NV_GET_fn` = **ZERO calls** in var_access · func_call · string_manip · table_access · fibonacci · indirect_dispatch · op_dispatch, and **ZERO static call sites** in every benchmark `.s`; `rt_gvar_get_int`/`rt_gvar_assign_descr` also **0 static sites**. Only live window is **EVAL** (`eval_dynamic`/`eval_fixed`, 2,000,000 calls, scaling proven 2× with +26 constant preserved), where `rdtsc` bounds it at **0.303% + 0.276% = 0.58% UPPER BOUND** ⇒ **a perfect port wins less than the ±3% null floor.** ⛔ **NEVER grade it on the four zero-count benchmarks — guaranteed false null, the s203 ARITH-FAMSET defect class.** ⭐⭐ **CAUSE = THE EIGHTH PHANTOM SHAPE, "THE BYPASSED FAMILY": the function is live and correct, but GVA SLOTS ALREADY TAUGHT THE EMITTER NOT TO CALL IT** (`var_access.s`: `rt_gva_island`/`gva_register` once at startup, then direct slots, no runtime call) — exactly what integer inlining did to `rt_num_arith` at s203. **A RUNG'S PREMISE DECAYS WHEN AN UNRELATED RUNG SUCCEEDS; RTX-7 was correct when written.** ⇒ if ever done, it is a **correctness/eradication** rung serving RTX-12, and the FINDING must say so rather than quoting a speed number. ⚠ **THIS ROW IS THE MIXED SHAPE AND IT IS THE HARDEST TO CATCH:** the two names a rung author is most likely to spot-check (`NV_GET_fn`/`NV_SET_fn`) are exactly the two that are real, so one confirmation "validates" a list that is 2/6 dead. **Spot-checking a symbol list is not checking it — `nm` every name.** ⚠ COORDINATE: `NV_SET_fn` is DB-1's planned write-barrier choke point — port must leave room for the flag-bit check (or land DB-1 first; walking session's call, note it in the FINDING either way).
- [ ] **⬅ ROUTED 2026-08-08 (Lon GO): executes as RUNG M-SLEN in `GOAL-SN4-ZETA-MECH.md` — this row is a POINTER, not the queue.** ⭐⭐ **POPULATE `DESCR_t.slen` AT ASSIGNMENT — NOT AN RTX RUNG; NEEDS LON'S ROUTING (raised s220 ON MEASUREMENT, and by measurement it outranks every remaining MATCH asm port).** `STRVAL` mints DT_S with `.slen = 0` and only `BSTRVAL` populates it, so **every match pays a `strlen` on a string whose length was known when it was assigned**: 2,000,001 per run on `pattern_bt_deep`. ⭐⭐ **CONFIRMED DEAD WEIGHT IN BOTH LANGUAGES BY TWO INDEPENDENT MEASUREMENTS — SNOBOL4 `slen==0` on 2,000,001/2,000,001 (s220) and Icon `slen usable = 0` on both a synthetic and a real corpus (s217-ICN).** ⇒ **STANDING RULE MEANWHILE: no RTX fast arm in either language may be keyed on `slen != 0`.** This is a LOWER/descriptor rung (who sets it, and whether every consumer of `.slen == 0`-means-unknown survives it being set) — scope it with Lon before any code.
- [ ] ⭐ **GROW THE KILL-SWITCH GATE AN m4 (`--compile`) ARM — "SUITE-WIDE BOTH MODES" IS STILL OWED AND THE SCRIPT CANNOT EXPRESS IT (s220).** The m3 sweep now runs and passes suite-wide (316 programs, 313/3/0, 48 s), but `test_gate_rtx_killswitch_sets.sh` invokes only `scrip --run`. m4 is a STANDING GATE (s214) precisely because mode 3 is structurally blind to the exported/hidden data-symbol class, so an m3-only byte-identity sweep cannot close an RTX-12 eradication re-gate. ⚠ Do not quote "suite-wide" without naming the mode.
- [ ] **CHARACTERISE THE 3 QUARANTINE PROGRAMS WITH N≫4 (s220).** `160_pat_alt_inner_gen_resume` · **`413_arith_mixed`** · **`W06_len`** — the latter two are NEW at s220 and were invisible while the sweep was truncated. All three are C-side by the gate's own argument (gate-OFF *is* the C fallback, and in both new cases ON is stable while OFF is unstable with ON's hash a member of OFF's set), so none blocks a port. ⚠ **`160`'s hash set is NOT enumerated at N=4 — three sessions have reported three different memberships; N=4 DETECTS non-determinism, it does not CHARACTERISE it.** Find the root cause or record the true set size; do not quote a set as closed.
- [ ] **`util_rtx_count_syms.sh` SEGFAULTS (rc=139) ON `rt_dcap_lazy_init` (s220)** while `rt_match_enter`/`VARVAL_fn`/`descr_to_str` thunk fine in the same harness ⇒ symbol-specific, not harness-wide. Likely the preload copy interposing libscrip_rt's INTERNAL calls with an invalid `RTLD_NEXT` resolution at first call. ⚠ **Until fixed, a zero from this tool on that symbol must NOT be read as "never called"** — that is the 0(d) false-null class the ladder has been bitten by repeatedly. 
- [ ] ⭐ **THE DEFER LATCH FIX — SPLIT OUT OF RTX-8 SLICE 2 AT s215, ON MEASUREMENT.** `rt_defer_get_pat_fn`'s one-entry `g_star_peek` latch (`pattern_match.c`, the s161 EVAL/deferred slot collision) must be replaced by **per-site resolution, no shared latch**. ⛔ **THIS IS A CORRECTNESS RUNG AND MUST NEVER QUOTE A SPEED NUMBER:** the star path it serves measures **star=0 of 402,121 opens** on json/twitter, so any ratio taken there is 1.000× by construction. **GATE: `140_pat_eval_double_fn_trick` and `141_pat_eval_double_fn_arbno`, already RED at m3 rc=139 and PASSING m4 — a two-sided gate for free.** ⭐ The manual proves the latch is structurally wrong rather than merely buggy: Ch.7 p.86 (`*` fetches the then-current value at every match-time reference; legal on an alternate/subsequent clause or the whole pattern) + Ch.9 p.122-123 (a deferred value may itself be a pattern, match state lives on a STACK, no-progress recursion plunges) ⇒ **deferred evaluation can RE-ENTER deferred evaluation, so one entry keyed by name is guaranteed to be clobbered by a nested reference.** Nesting is by design — the same reason `g_dfx` is a LIFO. `g_star_peek` is already `hidden` as of `0410aa72`, so an asm or C fix can reach it either way.
- [ ] ⭐⭐ **GIVE THE RAIL A min-OF-N MODE AND/OR HUGEPAGE PINNING (NEW s224) — THIS NOW BLOCKS EVERY SPEED CLAIM ON THIS MACHINE AND OUTRANKS ANY PORT.** `bench_rtx_3arm.sh` reports a MEDIAN and refuses on intra-arm spread; s220 established that only the MINIMUM is stable, and s224 measured the median verdict getting WORSE with more rounds (1.071× → 1.922× on an unchanged binary). The bimodality is the documented s201 hugepage class (clean ~1550/~3400 split = 2.3×). Until this lands, "the window was too short" must NOT be written as the reason a rung is ungradeable — s224 falsified it at 5704 ms.
- [ ] ⚠ **THIN-COVERAGE PORTS — MEASURED s224, EACH ON ITS OWN BATTERY, m3 ONLY. ⬅ m4 arm now available (`util_rtx_arm_census.sh <prog> both`) — re-run these on m4 before any port.** `rt_patstk_lazy_init` **0 commits** (MATCH-gated, so SNOBOL4 IS its battery and the zero stands; regression-tested by nothing, ungradeable here) · `rt_cap_match_begin` 1 program · `rt_match_replace` 3 · `rt_cap_pop` 3 · `rt_defer_open`/`_close` 7/8 · `rt_subscript_var` 2676 commits on Icon but in only **2 of 60** programs. ⇒ Rung = widen the regression base wherever coverage is 0 or 1 program, using the slice-9 recipe (mint a program, oracle `.ref`, validate two-sided at mint). ⛔ **DO NOT re-derive any of these from the SNOBOL4 table without FIRST checking which family gate the symbol belongs to — that omission produced a false vacuity claim at s224.**
- [x] ~~⭐ **GIVE `util_rtx_arm_census.sh` AN m4 (`--compile`) ARM (NEW s224).** Every coverage number above is `--run` only, so the entire audit is blind to the exported/hidden data-symbol class mode 4 exists to catch — the same gap the kill-switch gate carried between s217 and s222.~~ **DONE s_this (2026-08-08) — SCRIP `2d687dab`. `[m3|m4|both]` arg; default m3; backward-compatible. See FINDING.**
- [ ] ⛔ **`rt_defer_step` STRUCK s225 — 434 STATIC CALL SITES, ZERO DYNAMIC ENTRIES on json/calculator-1/calculator-2. Not a phantom (live definition, real call sites) but UNEXECUTED on this corpus ⇒ unfalsifiable, do NOT port it without first minting a workload that reaches it.**\n- [ ] RTX-8 **MATCH SINKS:** cap/dcap/defer families + `rt_match_enter` + `rt_match_value_get_pat_fn`/`rt_match_value_open` + ~~`rt_match_replace`~~ **[DONE — LANDED s224, slice-9 (LANDED s224 — row pruned s226, see the s224 FINDING); struck here in the SAME commit that completed it, per ARCH §7 step 0's "strike the dead names out of the ladder rung text" clause]**. ⛔ **`rt_match_ctx_restore` IS DONE — LANDED s217, LANDED s217 — row pruned s226, see git history + the s217 FINDING.** ⛔ **`match_variant` STRUCK s216 — PHANTOM (decl-only `rt/rt.h:30`, zero definitions, zero call sites); `match_value` WAS A TRUNCATION of TWO real symbols. Both corrected in ARCH §5 the same commit.** ⛔ **`rt_cap_push` IS DONE AND THE ANSWER WAS 'DO NOT PORT IT' — see the s216 cursor: 57,578 calls, 0 commits, vacuous on every workload in the tree.** ⚠ `rt_match_capture`/`rt_scan_splice_empty` have ZERO static sites in the benchmark+demo artifacts. ⚠⚠ DO NOT TRANSLITERATE `g_star_peek` — the s161 m4 EVAL/deferred slot collision lives in `rt_defer_get_pat_fn`'s one-entry latch (pattern_match.c:960); the port must FIX the order-dependent bleed (per-site resolution, no shared latch), and the 15-line s161 repro becomes the regression canary. This rung closes 2 of the 3 broken demos if done right.
- [ ] RTX-9 **PAT BUILD:** ⛔⛔ **THE RUNG'S OWN SYMBOL NAME WAS WRONG AND WOULD HAVE READ AS A DEAD FAMILY — CORRECTED s204 BEFORE IT COST A SESSION.** This rung read `rt_pat_*`; **all 30 `rt_pat_*` spellings are declaration-only in `rt/rt.h:38-67` with zero definitions.** ⛔⛔ **THAT SENTENCE IS NOW FALSE IN BOTH DIRECTIONS — CORRECTED 2026-08-09.** (i) The 30 were **DELETED** at SCRIP `1b251a7d`; `rt/rt.h:38-67` today holds `rt_acomp`/`rt_lcomp`/`rt_define_entry`, so **the line-number pointer lands on unrelated text.** (ii) There is now **exactly ONE LIVE `rt_pat_*`** — `rt_pat_prim_int`, `T` in the `.so`, added by concurrent D08-FIX (`e3ef8f7d`) for `LEN(*var)` match-time integer fetch — so "`rt_pat_*` is a dead family" would now MISREAD A LIVE SYMBOL. ⭐ **A line-number pointer into a file another seat edits rots exactly like a symbol name copied from prose.** ✅ The rest of this row survives step 0(b) at HEAD: `pat_lit`/`pat_alt`/`pat_break_`/`pat_any_cs` all round-trip byte-identically and the family is 32 defined constructors, as claimed. The live constructors carry **NO `rt_` prefix**: 32 defined in `pattern_match.c` (`pat_lit` @ `:98`, `pat_alt` @ `:215`), listed in full in ARCH §5's corrected PAT row. ⭐ **SEVENTH PHANTOM-FAMILY MEMBER, AND A NEW FAILURE MODE: THE PRIOR SIX RISKED WASTED OR DUPLICATED WORK; THIS ONE RISKED ABANDONING A LIVE, HOT FAMILY.** A step-0 grep on `rt_pat_lit` HITS (the `rt.h` declaration) so the name reads alive; `nm` then finds no definition, which is the exact signature of a phantom — so the correct-looking conclusion is "PAT is dead, skip the rung," and it is FALSE. ⚠ Take the tree's spelling exactly: `pat_break_` has a **trailing underscore** and the ANY constructor is `pat_any_cs`, not `pat_any` (step 0b). Construction-path = size-not-time (s160a measured rule) — port LAST among hot families, quote size wins honestly, never as match-loop speedups.
- [ ] RTX-10 **MISC/IO + VSTACK AUDIT:** keywords, goto_transfer, execute_code_dyn, comm_stno, io. ✅ **THE VSTACK AUDIT IS DISCHARGED s204 — MEASURED, AHEAD OF THE RUNG.** `nm -D out/libscrip_rt.so` finds **ZERO definitions** for every `rt_push_*`, `rt_pop_*`, and `rt_halt_tos`; each is declaration-only in `rt/rt.h` with exactly one src ref (its own declaration). ⇒ **the rung's "if dead, delete instead of port" branch is TAKEN.** No construct needs naming in the FINDING because nothing reaches them — the NO-VSTACK rules already won and only the header still says otherwise. **Remaining work on this rung is the live MISC/IO surface ~~plus deleting the dead declarations~~.** ⛔ **THE DELETION HALF IS ALREADY DONE — STRUCK 2026-08-09.** SCRIP `1b251a7d` deleted all 12 vstack decls (with 30 `rt_pat_*`); at HEAD `nm` finds zero definitions AND the grep finds zero declarations. **PHANTOM RUNG (planned work already done) — `nm` cannot catch it, only `git log -S` can.** ⚠ But `rt/rt.h` still carries **83 OTHER declared-but-never-defined symbols** (51 with zero references; 36 non-Prolog / 15 Prolog-family) — full census + the safe list in `FINDING-2026-08-09-...-83-PHANTOM-DECLARATIONS...md`. ⛔ Deleting them was ATTEMPTED and REVERTED UNPROVEN: one program (`161_pat_defer_fn_nested_match`) moved PASS→FAIL, which an unreferenced-declaration delete cannot cause ⇒ **suspect the BASELINE was a stale `.so` (`make libscrip_rt` said "Nothing to be done" at session open).** Re-baseline against a FULL rebuild before re-attempting.
- [ ] ⛔⭐⭐⭐ **RTX-FUNC — ZERO-C FUNCTION CALL PATH (Lon directive 2026-08-09: highest RTX priority)**
  **GOAL:** `func_call` and `fibonacci` at SPITBOL parity. SPITBOL Ch.8 protocol: save-set push → direct branch → pop on return. Zero C calls per invocation. Current gap after AB-3b: two C crossings remain on every call (`rt_ab_enter_env` at entry, `rt_ab_leave_env` at exit); name lookup eliminated, pcall record eliminated, but bookkeeping boundary still costs.
  **TARGET HOT PATH (zero C calls):**
  ```
  call site:  spill save-set → install actuals → jmp [fn_cell$FN]
  α:          inline Σ/wn save → inline vtmark → inline k_level++ → body
  β:          inline k_level-- → inline vtmark-tidy (fast path) → inline Σ restore → jmp wire
  ```
  **RUNGS — in order, each its own commit, each killswitch-gated, each measured:**

  - [ ] **RTX-FUNC-1: inline `rt_ab_enter_env` into `bb_func_activate` α.** Currently a C call doing: store Σ/Σlen into frame, store wn, call `rt_value_trail_mark()`, increment `rt_k_level`, increment `kw_fnclevel`. Port all five inline. `rt_value_trail_mark` is a hidden int bump (`resolution.c`) — promote to `visibility("hidden")` and inline as `inc dword ptr [rip + g_value_trail_top]` (or whatever the one-liner is; confirm from objdump). `rt_k_level` and `kw_fnclevel` are already hidden (`rt.c:396`). Σ/Σlen are external (`U` in rt.o) — need @GOTPCREL in TEXT, `movabs` in BINARY. **Touches `bb_func_activate.cpp` (template) ⇒ BOTH-MEDIUM mandatory, `.s` regen ×3 at handoff.** GATE: `test_gate_rtx_killswitch_sets.sh CALL` both modes N=4 (CALL family gate, distinct from MATCH — see ARCH §4). Acceptance: `rt_ab_enter_env` call sites in emitted `.s` artifacts drop to zero for programs whose call sites are on the AB-3b path.

  - [ ] **RTX-FUNC-2: inline `rt_ab_leave_env` fast path into `bb_func_activate` β.** Currently a C call doing: `rt_nret_fix()`, restore Σ/Σlen, decrement `rt_k_level`/`kw_fnclevel`, call `rt_value_trail_tidy_dead_window()`, unlink ACT-ANCHOR. **Split the fast and slow paths:** fast path = trail is clean (no entries pushed since entry vtmark) → inline k_level--, Σ restore, anchor unlink, skip tidy. Slow path = tidy needed → call C. Discriminant: `g_value_trail_top == vtmark_at_entry` (stored in frame at `AB_OFF_VTMARK`). Fast path covers `func_call`/`fibonacci` completely (no pattern matching in body → trail never grows during call). `rt_nret_fix` is tiny (`visibility("hidden")`, `rt.c:748`) — inline the common case (`rt_g_ret_by_name == 0`) as a guard + skip. **Touches `bb_func_activate.cpp` ⇒ BOTH-MEDIUM, `.s` regen ×3.** GATE: same CALL kill-switch. Acceptance: `rt_ab_leave_env` absent from emitted `.s` on the fast-path programs; `fibonacci` moves measurably toward SPITBOL parity.

  - [ ] **RTX-FUNC-3: promote `rt_value_trail_mark` to `visibility("hidden")` and confirm direct access.** Prerequisite for RTX-FUNC-1 inline. Currently `U` in `rt.o` (defined in `resolution.c`). Step 0: `nm` on `rt_pic/resolution.o` — confirm it is `T` (global) or `t` (static). If static: promote to `visibility("hidden")`. If already hidden: just verify `[rip+sym]` is safe. One-line change + `_Static_assert` on offset if baking struct field. No template change → no regen owed. GATE: build green + `nm` confirms hidden.

  - [ ] **RTX-FUNC-4: arg install — eliminate `rt_arg_stage` C calls from call site.** `rt_arg_stage(i, v)` is currently called once per actual argument before the `jmp [fn_cell]`. It does: `rt_gc_point(&v, NULL)` + `g_call_args[i] = v`. The GC safepoint is the only non-trivial piece — but the AB-3b path installs actuals directly into GVA cells (no `g_call_args` round-trip), so **`rt_arg_stage` is already absent from the AB-3b arm in `bb_call_proc_staged`**. This rung's job is to confirm the SCC probe (`bb_scc_probe`) fires for all DEFINE-bearing programs in TEXT mode (m4), so the `.s` artifacts also show zero `rt_arg_stage` calls on the hot path. **Root cause to investigate:** `func_call.sno` m4 `.s` still shows `call rt_arg_stage` + `call rt_proc_call_open_slim` — the SCC probe returns 0 for INC in m4, so the AB-3b arm never fires in TEXT mode. Diagnose: check `g_gva_active`, `scc_program_ok()`, and `x86_zc_frame()` state at the call site's emit time in m4. Fix so SCC fires in both modes. GATE: `func_call.s` and `fibonacci.s` regenerated artifacts show zero `rt_arg_stage` / zero `rt_proc_call_open_slim` in the INC call site.

  - [ ] **RTX-FUNC-5: MEASURE AND FINDING.** After RTX-FUNC-1 through RTX-FUNC-4: run `bench_rtx_3arm.sh` with min-of-N rail (ARCH §7 step 4 three-arm protocol). Report `func_call` and `fibonacci` ON/PRISTINE ratios vs s_this baseline (func_call 1882ms, fibonacci 486ms). Target: ratio ≥ 0.80× SPITBOL (directive's falsifiable acceptance). FINDING doc naming each rung's contribution. ⛔ NO SPEED NUMBER before RTX-FUNC-2 is done — the two C crossings are the whole cost; quoting partial progress misrepresents the win.

  **COORDINATION:** RTX-FUNC touches `bb_func_activate.cpp` (template) — NOT-CONCURRENCY-SAFE with any other seat editing that file. Lon routes the seat. Phase-2 work (registerized ABI, RTCC) is additive on top of this and handled by RTX-11/GOAL-RTCC. The AB-6 sweep (delete C bodies) in `GOAL-SNOBOL4-BB.md` is the terminal cleanup after RTX-FUNC proves zero-C parity.

- [ ] RTX-11 **RTX-CC — REGISTERIZED ABI (phase 2): ⬅ PROMOTED + GENERALIZED 2026-08-08 (Lon strategy pivot) — pulled forward as `GOAL-RTCC.md` (universal C-boundary veneer, not per-family); the per-family registerized-signature work re-homes to RC-4/RC-6 there; this rung is a STUB pending RTCC's fold.** per-family register signatures beyond SysV (S/F in EFLAGS instead of DT_FAIL compares; >2-arg staging; multi-returns). EVERY template call-sequence change via `x86(...)` encoders per RULES (TEMPLATE-ONLY, BOTH-MEDIUM); `.s` regen ×3 fires; per-family, measured, rail-gated.
- [ ] RTX-12 **ERADICATION + FINAL BOARD:** delete `c_*` bodies + gates family-by-family (each deletion re-gated); rail all 16 working-set programs; before/after board vs the s161 baseline (claws5-match 0.65 · json-match 0.79 · calc-1 2.11 · treebank 2.73 · calc-2 3.54 rail; match-only ms board in s161 cursor); FINDING of record. ⛔⛔ **BLOCKED ON RTX-0b — THIS BOARD IS STRUCTURALLY BLIND TO RTX-1…RTX-7 (measured s188).** The five named demos touch ALLOC twice, STR twice, and AGG/ARITH zero times in a whole N=64 run; `rt_call_arr` is flat across N. **Running this board as written would report ~zero movement and be misread as "the asm ports did nothing" when it actually means "these programs never call the asm."** By elimination the rail demos' time is in the pattern matcher ⇒ only RTX-8/RTX-9 can move this baseline, and they are ordered LAST. Fix the instrument before quoting the board.
**EVERY RUNG:** the ARCH §7 protocol verbatim — read-first set, gates at current watermark (re-prove at session start), kill-switch md5, rail same-moment A/B labeled `RTX_<FAM>`, FINDING, LIVE CURSOR move. Phase-1 rungs (1–10) touch ZERO templates ⇒ no `.s` regen owed by construction.
