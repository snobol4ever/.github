# GOAL-SNOBOL4-RTX.md — The SNOBOL4 Runtime in Optimized x86-64 Assembly

## ⛔ LIVE CURSOR — s212 (2026-07-29): **ARITH INLINE LANDED — `arith_int` 1.146×, gate differential CLEAN (zero movers, all four batteries). LON DIRECTIVE UN-DEFERRED THE s207 INLINE COLLAPSE.**
**✅ LANDED (uncommitted at time of writing — verify with `git status`): `bb_binop_arith.cpp` inline int-int ADD/SUB/MUL on BOTH arms (fc value-spine + `op_off` slot), s207's per-op call demoted to a cold `L(0)` fallback. fc 18→**12** instrs · slot 17→**11** · immediate-folded (`N = N + 1`) →**8**. Semantics unchanged: reals/DT_DATA/DIV/MOD/POW/setjmp all still route to the existing C. RECOVERED from `d61d7cba^` (s207's own deletion) rather than rewritten, then repaired: the original probed `DT_DATA` BEFORE `DT_I` (6 wasted hot instrs; guards are mutually exclusive so the reorder is free) and reloaded both tags twice.**
**⭐⭐ ZERO `x86_asm.h` CHANGES ⇒ THE RTX-11/12 CONCURRENCY COLLISION WAS NEVER OPENED. The design doc's §1.4 "five missing encoders, blocked on Lon" was costed FROM THE DESIGN, NOT THE TREE, and was WRONG — `GVA_LD` in `bb_binop_gvar_arith.cpp` already emitted this exact shape. ⇒ grep the templates for a working precedent before declaring an encoder gap, same as step 0(e) greps `.S` before declaring a symbol dead.**
**⛔ PREDICTION MISSED LOW AND IS RECORDED AS A MISS: pre-stated `arith_int` 1.15–1.45×, MEASURED **1.146×** (0.004 under the floor). `arith_mixed` **1.060×** is BELOW ARCH §7's ~1.10× trust threshold ⇒ LEAD, NOT FINDING. `arith_str` 1.02× (in band, string path correctly unmoved). All RT_OPT=`-O0`, interleaved, r1 discarded, arms non-overlapping, swapped by `LD_LIBRARY_PATH` — `scrip` is BYTE-IDENTICAL between arms (templates live in the `.so`).**
**⭐ THE INSTRUMENT IS BETTER THAN THE LADDER ASSUMES: `arith_int` reproduces to **0.3%** (1270/1272/1274). The ±3% null floor is a property of the SHORT benchmarks, not this box. A ~1.3 s benchmark makes 1.05× measurable here.**
**⭐⭐ TENTH PHANTOM SHAPE — THE WRONG TEMPLATE ARM. First implementation edited the `op_off` arm; `arith_int` never reaches it (it compiles through `vfcb()`, the fc value-spine arm). Build clean, gate green, answer correct, emission BYTE-IDENTICAL to pristine. STEP 0(d) WAS DISCHARGED AND WAS STILL INSUFFICIENT — it proves a SYMBOL executes, never WHICH ARM EMITS. Proposed step 0(f): `scrip --compile` the graded benchmark and READ THE BOX first. ⚠ BOTH obvious discriminators fail silently: comments are NOT emitted in TEXT mode, and the cold path legitimately still contains `call rt_add`. Discriminate on operand spelling (`[rsp + 16]` = fc cell · `FRQ(op_sa)` = slot).**
**⚠ A FALSIFICATION REVERT WAS INTERRUPTED AND DID NOT APPLY — the `.so` carried the corruption for one cycle before it was caught by grep. ⇒ after ANY corruption probe, re-grep the source AND rebuild AND re-run the gate before believing a number. Every number above was re-taken on the restored build.**
**⛔ OWED, NOT DONE: `.s` regen ×3 (templates changed ⇒ emitted bytes changed) · smokes 7/7×2 · beauty · 15-demo board · `test_gate_template_medium_invisible.sh --strict`. DIV/MOD deliberately NOT inlined (would re-open s207's unguarded-`idiv` SIGFPE defect; needs an explicit zero test).**
**⚠ WATERMARK DISAGREED A THIRD TIME: recorded 314/1 · s210 268/47 · s212 **277/38**. Three sessions, three numbers, suite size also disagreeing (295/315/316). The DIFFERENTIAL is the only surviving instrument. `047_pat_rtab` FAILS ON BOTH ARMS ⇒ s211's TAB/RTAB regression is still live and untouched by this rung — THAT BISECT IS STILL THE BLOCKER for anything needing a tree-wide regen.**
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-ARITH-INLINE-LANDED-AND-THE-WRONG-TEMPLATE-ARM.md`. DESIGN: `DESIGN-SN4-ARITH-INLINE-AND-DT-BITS.md` (route A hoist vs route B `DT_t` renumber still OPEN; B blocked on the bisect). `handoff_status.sh` is the push truth — not this block.**

## ⛔ PRIOR CURSOR — s211 (2026-07-29): **THE WATERMARK REGRESSION IS LOCALIZED TO TAB/RTAB — THE ABSOLUTE-CURSOR *BINDING* PATTERNS — AND NOTHING ELSE. ⛔ NO CULPRIT YET; MY ONE SUSPECT WAS BUILT AND FALSIFIED.**
**⛔⛔ THE UPSTREAM `Z4 s8 fix 1` (SCRIP `30ee3fe6`, "FLATDISP parse prefix collision", claims to close `d79a427a..cca948c5`) DOES **NOT** FIX THIS — REBUILT AT `57a7b598` AND MEASURED: TAB/RTAB STILL rc=139. ⭐ SECOND FALSIFIED SUSPECT, AND THE INSTRUCTIVE ONE: it was a LANDED FIX ON THE EXACT AXIS I NOMINATED, WITH MATCHING MAGIC NUMBERS (`COND(64)->SAVE(48)` = REM's and RTAB's own scratch offsets). Had §3's lead been written as a CAUSE, this commit would have "confirmed" it and the real defect would have been closed as fixed while 46 programs kept failing. THE `sub rsp,K` + `FR(off)` SHAPE HOSTS MORE THAN ONE DEFECT.**
**NEXT RUNG: FINISH THE BISECT. Endpoints VERIFIED (not assumed): GOOD `d79a427a` (built + ran clean), BAD `57a7b598` (built + ran, post-upstream-fix). Predicate: `scrip --run corpus/crosscheck/patterns/047_pat_rtab.sno` → rc 0 good / rc 139 bad — 6 lines, no `.input`, no oracle needed.** ⭐ **CHEAPEST FIRST MOVE: test `0e008a85` (s197 head) directly — `FINDING-2026-07-27k` records SNOBOL4 at FAIL=20/19 on 07-27 vs 47 now; if GOOD the range collapses 704 → ~100 commits.** ⚠ Suite TOTAL differs (295 vs 315) so that narrowing is a LEAD, not proven — s209 already logged a "suite-size, not regression" disagreement.
**✅ FAMILY ISOLATED (m3 vs SPITBOL oracle): `TAB(n)` and `RTAB(n)` segv — bare AND with `. V`. GREEN: `LEN . V`, `POS`, `RPOS`, literal, literal `. V`, `REM . V`, `SPAN . V`, `BREAK . V`, `ARB . V`. ⭐ The cut is SEMANTIC: TAB/RTAB are the only patterns both ABSOLUTE and BINDING ⇒ the only ones that must save/restore the cursor through a frame slot (`POS`/`RPOS` are absolute but non-binding; `LEN` is binding but relative and restores arithmetically).**
**⛔ NOT the scratch mechanism — 9 templates use `x86_scratch_off`, SEVEN WORK. ⛔ NOT `d79a427a` ("TAB/RTAB 16B cursor-save quad") — BUILT IN A WORKTREE AND FALSIFIED, it returns `abcd`/`tab-bare` rc=0. ⛔ NOT the `[0x70000000]` `RT_CAS_TOP` cell — `LEN . V` and `REM . V` use it and pass.** ⭐ Recording my own falsified suspect deliberately: a third confident guess here is the s209 mistake, and measuring first is the only reason it did not become the fourth wrong culprit.
**⚠ LEAD, NOT A FINDING: the emitted RTAB box is the `sub rsp,K` + `FR(off)` shape that FLATDISP-8's per-graph frame-base selector governs (`FINDING-2026-07-27k` point 3: `FRQB()` suppresses its live rsp bump when pinned). Look there first.**
**⛔⛔ ENVIRONMENTAL, COST ME TWO BUILDS: (1) commits before the GC-U removal DO NOT BUILD without `libgc-dev`, which `install_system_packages.sh` no longer installs — `REPO-SCRIP.md`'s "Boehm removed" is true of HEAD and FALSE of the history a bisect compiles. (2) `gdb` genuinely CANNOT be installed (apt rc=100) — VERIFIED now, so RULES step-3 single-stepping has NO instrument for emitted code; read `scrip --compile` output instead. (3) `scrip` links `libscrip_rt.so` ⇒ each bisect step = BOTH builds, ~9 min on this 1-core box; do NOT run benchmarks concurrently (s211 starved its own bisect). (4) `crosscheck/**/*.s` artifacts are STALE SINCE 2026-04-02 — not an oracle for current codegen.**
**✅ DELIVERED s211 (additive, zero codegen impact, no `.s` regen owed): `corpus/benchmarks/snobol4/arith_str.sno`+`.ref` — the STRING-OPERAND arithmetic window that did not exist. STEP 0(d) PASSED: census `other=4,000,000` at N=2M and `8,000,000` at N=4M, EXACT 2× scaling, `realreal=0`; oracle-exact `checksum: -1000000.`; ~633 ms. Also proves `S='1.5'` is NOT constant-folded. ⭐ Without it, any coercion rung is graded by a BLIND instrument (the RTX-12 defect class) — every existing arith benchmark feeds operands that are ALREADY numeric.**
**✅ DESIGN: `DESIGN-SN4-NUMERIC-COERCION.md` — hoist `IR_COERCE_NUMERIC` onto the arithmetic path so the binop can drop `setjmp`, which is what UNBLOCKS RTX-6's refused DIV/MOD/POW. ⭐ `IR_COERCE_NUMERIC` ALREADY EXISTS with the joint-decision contract (`IR.h:96`) + live template + runtime; it is wired ONLY into the relop path (`lower_snobol4.c:168-169`). This is a WIRING rung, not a construction rung. ⭐⭐ AND THE HOT PATH NEEDS NO `DT_t` RENUMBER: `DT_I=6`/`DT_R=7` differ in exactly bit 0, so given the coercion guarantee the whole mixed-mode rule is `or eax,ebx` + `test eax,1`. Renumbering buys 2 instructions on the COLD path against tree-wide `.s` churn plus one correctness trap (`driver_data.c` treats `DT_DATA` as an open-ended RANGE).**
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-WATERMARK-IS-TAB-RTAB-ONLY-AND-D79A427A-EXONERATED.md`. `handoff_status.sh` is the push truth — not this block.**

---

## ⛔ PRIOR CURSOR — s210 (2026-07-29): **RTX-6 ARITH LANDED — `rt_add`/`rt_sub`/`rt_mul` IN ASM. ⭐ THE REAL-REAL ARM IS **3.710×**, THE LADDER'S FIRST LARGE UNAMBIGUOUS SPEED WIN. ⛔⛔ AND THE WATERMARK IS FALSE AT HEAD — 268/47, NOT 314/1, PROVEN NOT MINE.**
**NEXT RUNG: ⛔ THE WATERMARK, VIA THE MONITOR, PER RULES — NOT ANOTHER PORT.** It blocks every ladder's absolute gate; 46 programs fail at HEAD and at least one is a clean segv repro (`corpus/crosscheck/patterns/047_pat_rtab.sno`, no `.input` needed). **Do NOT run another RTX rung's absolute gate until this is understood.**
**✅ PORTED (gate `SCRIP_RTX_ARITH`, C → `c_rt_*`, ZERO templates ⇒ no `.s` regen owed): `rt_add` `rt_sub` `rt_mul`, each with an int-int arm AND a **real-real SSE arm** (`addsd`/`subsd`/`mulsd`). int-int **10** insns, real-real **13** (incl. the 2-insn gate the C never pays) vs a C body of **124** containing a `setjmp` CALL. ⛔ DIV/MOD/POW deliberately NOT ported — the setjmp-bypass soundness argument does not cover them.**
**⭐⭐ BOARD (3-arm, r=4, RT_OPT=`-O0`, raw samples non-overlapping): `arith_mixed` (80,000,001 calls, 50/50 int/real) **ON/PRISTINE = 3.710×** · `arith_int` (100,000,000 calls, 100% int) **1.107×**. Arm-stability precondition PASSES on both (worst spread 1.079× vs a 3.710× gap).** The split is mechanistic: the real arm deletes a whole `setjmp`+impl frame; the int arm only sheds `-O0` ceremony because C already had an inline int arm.
**⛔ MY PRE-STATED BAND WAS FALSIFIED — I said 1.40–2.20× and UNDER-predicted by >1.7×.** Every prior falsified prediction on this ladder was an over-prediction; this is the first under. Pre-stating works in both directions.
**⭐⭐ `arith_int` IS THE STRONGEST CONFIRMATION OF s209's THESIS YET: legacy two-arm reads **1.397×** where the honest answer is **1.107×**, because OFF/PRISTINE is **0.793×** — a 100M-call family pays gate+taken-branch+PLT on EVERY call. ⇒ the kill-switch tax is NOT a constant to subtract; it SCALES WITH CALL COUNT and on a hot family it is 21%.**
**⛔⛔ WATERMARK: recorded m3 314/1 · m4 309/4 · DIVERGE=3. MEASURED at `b17e263a`: **m3 268/47 · m4 267/46 · DIVERGE=2**. PROVEN NOT MINE THREE WAYS — gate ON, gate OFF, and a genuinely PRISTINE `.so` (port stashed out, rebuilt) all give the IDENTICAL counts.** Failures are REAL crashes (`047_pat_rtab` rc=139; `strings/word1` segv when fed its real `.input`), not missing refs. ⛔ **NO CULPRIT ASSERTED — I was wrong twice in this diagnosis (env gap, then "missing refs": my `find` hit SCRIP's `test/` copies while the gate reads `$CORPUS/crosscheck`) and naming a cause on the third guess is precisely the s209 mistake.** ⭐ **THE SUBSTITUTE THAT SURVIVES A BROKEN BASELINE: a three-way ON/OFF/PRISTINE IDENTITY is a DIFFERENTIAL claim and does not depend on the absolute number. Use it until the watermark is repaired.** ⚠ PROTOCOL DEVIATION, MINE: I ported before re-proving the watermark; the identity recovered the claim, but only because the port happened to be clean.
**⭐ NINTH PHANTOM SHAPE — THE STALE CENSUS. s206 recorded `arith_mixed` as 100% DT_R+DT_R, so RTX-6's own text says an int-int port "grades a guaranteed 1.000× there." MEASURED s210: 80,000,001 calls, **50/50**, scaling exactly 1:1 at 2×. s207's OWN collapse-to-one-call pushed the integer loop counter into the runtime.** RTX-7's decay needed an UNRELATED rung; this one is self-inflicted ONE RUNG APART. **A census has a shelf life — re-run it, do not cite it.** Instrument committed: `scripts/util_rtx_arith_census.c`.
**⚠ `arith_loop` CANNOT FALSIFY THE INT ARM — IT IS A FIXED POINT THAT ABSORBS THE ERROR.** Corrupt the add to `+1` and `N = N+1` under `LT(N,1000000)` just steps by 2 and STILL lands on 1000000: output unchanged. On-family, hot (1M calls), and worth nothing as a gate. **A falsification target must be OUTPUT-SENSITIVE to the corrupted value, not merely reach it.** New `arith_int.sno` inherits the defect by design — TIMING ONLY; falsify on `arith_mixed` or the direct probe.
**GATES: falsification two-sided on BOTH arms · unit 21/21+36/36+8426/0 · Prolog 189/0 and Icon 4/0 at gate ON *and* OFF (shared `arithmetic.c`) · SNOBOL4 crosscheck identical ON/OFF/PRISTINE.** FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-6-ARITH-REAL-ARM-IS-3-7X-AND-THE-WATERMARK-IS-FALSE-AT-HEAD.md`. `handoff_status.sh` is the push truth — not this block.

---

## ⛔ PRIOR CURSOR — s209 (2026-07-29): **RTX-4 SLICE 3 LANDED, CORRECT, FALSIFIED — AND IT MEASURES A NULL. THE REASON IS THE INSTRUMENT: THE GATE-OFF ARM IS NOT A VALID CONTROL, AND ITS BIAS ALWAYS FLATTERS THE ASM.**
**NEXT RUNG: RTX-0e — THE 3-ARM INTERLEAVED HARNESS (pristine / OFF / ON). Highest-value work on the ladder; it retro-qualifies existing numbers. Do it BEFORE any further port.**
**✅ PORTED (2 files, ZERO templates ⇒ no `.s` regen owed): `rt_flat_ret_snap` **48→28** instructions, `rt_proc_open_fn` **16→13** (counts INCLUDE the 2-instruction gate the C never pays; measured by `objdump`, not estimated). Each global loaded ONCE not twice; 40-byte copy 6 memory ops not 10; no rbp frame; no `rbx` PIN saved as copy scratch.**
**⛔⛔ PRE-STATED BAND FALSIFIED — THE RUNG WINS NOTHING. vs PRISTINE C: `func_call` **1.007×** · `fibonacci` **0.995×** — both inside the ±3% null floor, one fractionally negative. Bands were 1.05–1.20 / 1.02–1.12 (RT_OPT=`-O0`).** The work WAS removed and the clock did NOT move ⇒ `func_call`'s time is not in these two leaves' ceremony.
**⭐⭐⭐ THE FINDING THAT OUTWEIGHS THE RUNG — THE TWO-ARM `ON/OFF` A/B IS **UNSTABLE**, AND THE HARNESS I BUILT TO PROVE IT FALSIFIED MY OWN FIRST EXPLANATION.** ⛔ **FIRST CLAIM, NOW WITHDRAWN:** I measured gate-OFF as 5.8%/7.8% slower than pristine and generalized a uniform "kill-switch tax" (gate `cmp` + taken `je` + PLT hop, mechanism visible in objdump). ⛔ **THE 3-ARM HARNESS KILLED THE GENERALIZATION IN ONE RUN: the tax is 1.002× on `func_call` (essentially ZERO) and 0.905× on `fibonacci`. My 5.8% rested on ONE OFF median of 2425 ms THAT DID NOT REPRODUCE (2271 ms, same binary, same gate byte) — noise with a mechanically plausible story built on top, which is exactly what made it dangerous.**
**⭐ WHAT SURVIVES IS STRONGER THAN WHAT I FIRST CLAIMED: the two-arm ratio is not biased-by-a-constant (correctable) — it is UNSTABLE. `func_call` ON/OFF read **1.066×** in harness 1 and **1.006×** in harness 2, ZERO code change between them. A 6-point swing for free can MANUFACTURE an in-band win.** ⇒ **NO TWO-ARM `ON/OFF` NUMBER BELOW ~1.10× IS TRUSTWORTHY, AND THE REASON IS INSTABILITY, NOT AN OFFSET YOU CAN SUBTRACT.** ⚠ I flagged RTX-3b's `func_call` **1.080×** as unsafe on a magnitude coincidence — **THEN RE-GRADED IT, AND MY DOUBT IS WHAT DIED. SEE THE RTX-3b LINE BELOW. FLAG WITHDRAWN.** RTX-5b 1.022×/1.002× still owes a re-grade (ALLOC = 3 symbols, not yet built).
**⭐ THE NULL IS NOW THE MOST SOLID NUMBER HERE: ON/PRISTINE = 1.007×/1.008× (`func_call`) and 0.995×/0.995× (`fibonacci`) across TWO INDEPENDENT HARNESSES, one cross-build and one same-moment interleaved. The cross-build caveat is DISCHARGED — and discharging it changed the null not at all while demolishing my explanation for it.**
**✅ RTX-0e DELIVERED — `scripts/bench_rtx_3arm.sh` (committed, not a throwaway): three arms (PRISTINE `.so` / OFF / ON) interleaved WITHIN each round via `LD_LIBRARY_PATH`, round 1 discarded, medians + every raw sample printed so range overlap stays visible. Reports ON/PRISTINE as THE ANSWER, ON/OFF only to expose how much of a legacy claim was artifact, and OFF/PRISTINE as the family's kill-switch tax.** ⚠ **UNEXPLAINED, NOT A LAW: the tax is ~0% on `func_call` (10M calls) and ~9.5% on `fibonacci` (2.7M calls) — LARGER on the program with FEWER calls, which a per-call tax cannot explain. No cause asserted; building a story on one number is the mistake this cursor records.**
**⭐⭐⭐ RTX-3b RE-GRADED WITH THE NEW HARNESS AND **VINDICATED** — SECOND SELF-CORRECTION THIS SESSION, AND THE ONE THAT MATTERS MOST BECAUSE IT CLEARS SOMEONE ELSE'S LANDED WORK.** Pristine-STR arm built by REMOVING `rtx_str.S` from the link and renaming `c_str_concat_d`→`str_concat_d` (verified by `nm`; both arms output `result: 60000012` = `var_access.ref`). **`var_access` ON/PRISTINE = 1.262× (claimed 1.366×) · `func_call` ON/PRISTINE = 1.061× (claimed 1.080×).** ⭐ **BOTH REAL, both far outside the null floor, same conclusion as originally recorded — and on `func_call`, the program I specifically impugned, the 2-arm and 3-arm numbers differ by 0.002. THE ARTIFACT I PREDICTED IS NOT THERE.**
**⛔⛔ SO THE DIAGNOSIS IN THE LINE ABOVE IS ALSO TOO STRONG — THE REAL ENEMY IS NOISE, NOT THE CONTROL'S STRUCTURE.** Kill-switch tax across all 3-arm runs now: **1.002× · 0.998× · 1.064×** — ≈nothing and **not even consistently signed** (`var_access`'s OFF came out FASTER than pristine, which a gate that only adds instructions cannot cause ⇒ link-layout, s200's confound, since removing a `.S` moves the whole `.so`). Lone outlier: CALL/`fibonacci` 0.905×. ⭐ **THE TELL IS IN THE RAW SAMPLES: `var_access` PRISTINE ran 717·595·587·737 — a 25% SPREAD INSIDE ONE ARM — while its ON arm ran 518·522·514·525, tight.** A denominator that noisy is what swings a ratio. ⇒ read that 1.262× as **"a large real win, ~1.13×–1.26×"**, not a precise number. ⇒ **CORRECTED PRESCRIPTION, weaker than my first and actually supported: the harness earns its keep by (i) including a true baseline and (ii) PRINTING EVERY RAW SAMPLE so an intra-arm spread cannot hide behind a median. It is NOT true that two-arm numbers are systematically inflated — measured, they mostly are not. REPLICATE, PUBLISH RAW SAMPLES, DISTRUST ANY RATIO WHOSE ARMS OVERLAP, whatever the arm count.**
**⭐⭐⭐ RTX-5b/ALLOC RE-GRADED — ⛔ RESULT WITHDRAWN WITHIN THE SAME SESSION BY THE GUARD I WROTE TO CATCH EXACTLY THIS. THIRD SELF-CORRECTION; READ THE WITHDRAWAL AT THE END OF THIS BLOCK BEFORE QUOTING ANY NUMBER IN IT.** Pristine-ALLOC arm built by removing `rtx_alloc.S` from the link and renaming all three `c_rt_{gcheap,str,agg}_alloc` back (verified by `nm`; both arms output `result: 4080200` = `table_churn.ref`). **`table_churn`: ON/PRISTINE = 1.084× — CREDIBLE and a REAL WIN** (PRISTINE `1419 1492 1393 1380` and ON `1334 1300 1284 1293` are both TIGHT and NON-OVERLAPPING). ⚠ That is the WHOLE family (RTX-1 + RTX-5b); since RTX-5b alone self-reported 1.022×/1.002×, **most of the 1.084× is RTX-1's `rt_gcheap_alloc`/`rt_str_alloc`, not 5b — consistent with 5b being ~null on its own, as its own rung already said.**
**⛔⛔ AND HERE IS THE TWO-ARM ARTIFACT, CAUGHT IN THE ACT AND WITH ITS REAL MECHANISM: on `table_churn` the legacy `ON/OFF` reads **1.464×** against a true **1.084×**. The cause is NOT a constant gate tax — it is that the OFF arm is **BIMODAL**: `2397 1400 2616 1392`, two distinct modes, so its median is a coin flip between them.** ⇒ **THE SYNTHESIS THAT RECONCILES MY TWO SELF-CORRECTIONS: a two-arm number is sometimes EXACT (STR/`func_call`: 1.063 vs 1.061, agreeing to 0.002) and sometimes WILDLY WRONG (ALLOC/`table_churn`: 1.464 vs 1.084) — and the failure mode is ARM INSTABILITY, NOT A SUBTRACTABLE OFFSET. ⭐ THEREFORE YOU CANNOT KNOW WHICH CASE YOU ARE IN WITHOUT THE PRISTINE ARM AND THE RAW SAMPLES. That, and not a fixed bias, is what justifies the harness.** ⛔ **NO CAUSE ASSERTED for the bimodality** (candidates: GC pacing, the s205 hugepage-compaction instability) — asserting one on a single observation is the exact mistake this cursor already records twice.
**⛔ `string_manip` IS UNGRADEABLE, REPORTED AS NO-MEASUREMENT RATHER THAN AS A NUMBER: its PRISTINE arm ran `4769 3903 1898 6509` — a 3.4× SPREAD INSIDE ONE ARM. The harness printed 2.416× and that figure is MEANINGLESS; it is quoted here only to show what a median hides when the raw samples are not read.** ⇒ ⭐ **AN ARM-STABILITY PRECONDITION BELONGS IN THE HARNESS: refuse to print a ratio when an arm's spread exceeds its inter-arm gap. Next instrument rung.**
**⛔⛔ WITHDRAWAL — THE 1.084× DID NOT REPRODUCE, AND THE INSTRUMENT CAUGHT ITS OWN AUTHOR MINUTES AFTER HE WROTE IT.** I called 1.084× "credible" because its arms looked tight and non-overlapping (`1419 1492 1393 1380` vs `1334 1300 1284 1293`). I then added the arm-stability precondition and re-ran the SAME command: **`table_churn` came back `ON/PRISTINE = 0.648×` with intra-arm spreads of PRISTINE 3.364× · OFF 5.938× · ON 5.887×, and the guard correctly refused it as UNGRADEABLE.** ⇒ **THE 1.084× IS WITHDRAWN. ALLOC IS UNGRADEABLE ON THIS BOX AT PRESENT — no number, in either direction.** ⭐⭐ **THE LESSON, AND IT INDICTS MY OWN METHOD TWICE OVER: ARM TIGHTNESS IN ONE RUN DOES NOT ESTABLISH STABILITY. A tight window is itself a sample, and I read one as if it were a property.** ⭐ **The guard is validated by having immediately falsified the person who wrote it — which is the only validation of a check like this that is worth anything.** ⚠ **WHAT STILL STANDS from the block above: the SYNTHESIS (a two-arm number is sometimes exact and sometimes wildly wrong; the failure mode is ARM INSTABILITY, not a subtractable offset) is UNAFFECTED and is in fact strengthened — the ALLOC arms are now the loudest evidence for it. ⛔ WHAT DOES NOT STAND: any ALLOC ratio, including the 1.464× two-arm figure, which is equally a product of unstable arms.**
**⭐⭐ NEW STEP-0(c) CLAUSE — `nm` ON THE `.so` CONFLATES `static` WITH `hidden`; ONLY THE `.o` SEPARATES THEM, AND FOR A CROSS-TU ASM PORT THE DIFFERENCE IS LINK-OR-FAIL.** s208 reported all four globals "linker-localized ⇒ direct `[rip+sym]`, no GOTPCREL" — **correct on addressing, and it concealed a hard blocker**: in `rt.o`, `g_pcall`/`g_pcall_top` are **`B`** (hidden global, linkable) but `g_pcall_wires`/`g_flat_ret_snapbuf` were **`b`** (`static`, **not referenceable from `.S` at all**). The `.so` cannot show this — the link demotes hidden globals to local, so both print lowercase. **FIXED:** both promoted `static` → `visibility("hidden")`, the identical attribute their two siblings already carry; hidden-ness (no dynamic entry, direct rip, interposition-proof) preserved, only sibling-TU linkage widens. ⭐ Same axis as the exported/hidden split that cost RTX-2 a rung, one level down.
**✅ GATES:** watermark **re-proven live BEFORE any edit** — **m3 268/47 · m4 267/46 SKIP=2 · DIVERGE=2 (315)**; ON and OFF both identical, **failure sets diffed LINE-BY-LINE: byte-identical, zero movers either direction.** ⛔ **s207's recorded 280/54 · 276/50 (334) DISAGREES; mine matches s200 EXACTLY. Suite-size, not regression — fourth recorded-vs-measured board disagreement on this ladder. BASELINE OF RECORD = 268/47 · 267/46.** Prolog 188/0 SKIP=1 · Icon 4/0 — ⛔ **no-regression only; citing an unmoved battery as asm evidence is a FALSE CLAIM (§7 2b).** Snocone not run (`beauty_full_bin` absent — environmental).
**✅ TWO-SIDED FALSIFICATION, aimed at the rung's DISTINCTIVE computation:** `PROC_FN` 8→0 (returns `p->name` not `p->fn`) ⇒ gate ON **248/67 · 246/67 = 20 and 21 movers, THE ASM EXECUTES**; gate OFF ⇒ **exact baseline, THE GATE REACHES C**; restored ⇒ exact watermark, byte-identical. ⚠ The *intended* probe — stride 40→32, the pre-Z4-7 `sizeof` the s208 cursor flagged as moving under the ζ session — **does not assemble** (x86 scale factors are 1/2/4/8; `[rax+rax*3]` unencodable). Stride is instead guarded by the `_Static_assert` in `rt.c`, which is **stronger than a probe: it breaks the BUILD, not the runtime.**
**📐 KEPT BY DESIGN: the error paths are NOT ported** — level-zero RETURN (Ch.8 "Return from level zero", core error 18, routed through core so `&ERROR`/`SETEXIT` trapping applies) and the no-wires diagnostic both **tail-jump to C** ⇒ **this port cannot regress error behaviour by construction.** `rdx` clobber verified safe against the machine (the -O0 C already clobbers it; the emitted floater consumes only `rax`) — `rt.c`'s "rax:rdx ride untouched" describes the tail AFTER the call, not a guarantee across it.
**⛔ OPEN FOR LON: KEEP OR REVERT THIS PORT?** Correct, gated, falsified, zero regression, buys ~0%. FOR: prerequisite for RTX-11 (registerized ABI), where these leaves stop being C-signature-bound. AGAINST: ~100 lines of asm earning nothing today.
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-4-SLICE3-IS-A-NULL-AND-THE-GATE-OFF-ARM-IS-NOT-A-VALID-CONTROL.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## ⛔ PRIOR CURSOR — s208 (2026-07-29): **RTX-7 IS VACUOUS — MEASURED. NV IS BYPASSED BY GVA SLOTS. AND `rt_call_arr` IS 87.3% OF `string_manip`, WHICH REOPENS AN s204 REJECTION.**
**NO CODE CHANGED THIS SESSION — measurement + document correction only. No gates owed (nothing built differently), no `.s` regen owed (zero templates touched).**
**⭐ FIRST, s207's BANNER WAS FALSE — CORRECTED BELOW.** It read *"LOCAL ONLY, NOT PUSHED"*; all four commits are ancestors of `origin/main` (SCRIP `d61d7cba` + `cb3da95e`, corpus `b1969af5` + `102ddfa8`), **and both `.s` regens it listed as OWED were in fact run and committed.** s207 is COMPLETE. Rule-(a) rot again — the SIXTEENTH voided banner in this project.
**⛔ RTX-7 (NV/GVAR) RETARGETED AND DOWNGRADED — NOT A SPEED RUNG.** Step 0 passes (a),(b),(c),(e) and **FAILS 0(d)**: `NV_GET_fn` is called **ZERO** times in var_access · func_call · string_manip · table_access · fibonacci · indirect_dispatch · op_dispatch, and has **ZERO static call sites** in every benchmark `.s`. Its ONLY live window is EVAL (`eval_dynamic`/`eval_fixed`: 2,000,000 calls, **scaling proven 2× with the +26 constant preserved**) — where `rdtsc` measures it at **0.303%** and `NV_SET_fn` at **0.276%**, both **UPPER BOUNDS** (interposer overhead is inside the span). ⇒ **a perfect port of the whole family wins ≤0.58%, inside the ±3% null floor.** Interception proven by the same interposer reading 26 and 2,000,000 elsewhere, so the zeros are real, not a filter defect. ⛔ **NEVER grade this family on var_access/func_call/string_manip/table_access — the count there is ZERO ⇒ guaranteed false null**, same defect class as the s203 ARITH FAMSET.
**⭐⭐ EIGHTH PHANTOM-FAMILY SHAPE — THE BYPASSED FAMILY, AND THE SHARPEST LESSON YET: A RUNG'S PREMISE DECAYS WHEN AN UNRELATED RUNG SUCCEEDS.** `NV_GET_fn` is live, correct, exported and genuinely used — **a prior optimization taught the EMITTER not to call it.** `var_access.s` calls `rt_gva_island`/`gva_register` ONCE at startup, then variables are DIRECT SLOTS with no runtime call. GVA did to NV exactly what integer inlining did to `rt_num_arith` (s203): removed the CALL SITE, not the function. **Every check except 0(d) passes — including live C call sites in `rt.c`/`rt_runtime.c`.** RTX-7 was CORRECT when written and was invalidated by GVA landing.
**⛔ SECOND VACUOUS CANDIDATE, SAME SESSION: `rt_coerce_num2_d` — 56 STATIC CALL SITES, ZERO EXECUTIONS** across all seven stable benchmarks. §5 caveat (a) demonstrated a second time in one session: a static count measures the CALL BOUNDARY, not execution. **Two of the three most attractive-looking targets in this ladder are unreachable, and both look excellent until the interposer runs.**
**⭐⭐ WHERE THE TIME ACTUALLY IS — `rt_call_arr`: `string_manip` 10,000,004 calls = 87.334% of window · table_access 0.629% · var_access 0.047% · func_call 0.002% (RT_OPT=`-O0`).** ⛔ **THE COUNT IS NOT MINE — ARCH §5's CALL row already carries s188's `string_manip` 10.0M; my number corroborates it exactly and adds nothing. ⭐ WHAT IS NEW IS THE SHARE (87.334%), and that is the part that bears on the decision** — s204 rejected this target already holding the count, and by s188's own law a count cannot predict benefit. A share can. ⭐ **STATIC COUNT DID NOT PREDICT IT** — it is #1 by static sites (85), 4 of which sit in `var_access` where it costs 0.047%. **s188's law gains a sibling: STATIC SITE COUNT DOES NOT PREDICT CALL COUNT.** ⛔ **THIS REOPENS s204's REJECTION** of `rt_call_arr` (*"wins only `-O0` ceremony"*) — that argument is about the BODY and was made without this share number; **re-decide it against the measurement, do not inherit it and do not simply reverse it.** ⛔ **NOT ESTABLISHED: 87.3% is the WHOLE CALL TREE; the PORTABLE fraction is UNMEASURED** (splitting it is the next rung's first job) · `-O0` only, `-O2` arm not built per the O2-DIRECTED-ONLY rule and this share WILL move under `-O2` · single unrepeated runs used for TARGET SELECTION, not a graded board.
**⭐⭐ RULED BY LON, SAME SESSION — `rt_call_arr` GOES TO ICON-RTX; SN4 CLOSES RTX-4 SLICE 3 AND IS NOW BENEFICIARY.** Directive: *"Choose another one versus rt_call_arr since GOAL-ICON-RTX is working on that one… Coordinate."* ⇒ ledger row vacated in `RTX-CLAIMS.md` (minted s203-ICN — **that file, not this one, is the truth on symbol ownership**), and the 87.3% share + its three unestablished gaps were handed to the Icon ladder in an INBOX block at the top of `GOAL-ICON-RTX.md`, per the ledger's mandatory-notification rule. **SN4 STAYS INSIDE ITS EXCLUSIVE SET, which is exactly what the ledger advises both ladders to do until arbitration settles.**
**⭐⭐ NEW TARGET, CHOSEN BY MEASUREMENT NOT BY LIST POSITION — `rt_flat_ret_snap` + `rt_proc_open_fn` (RTX-4 CALL family, ENTRY side; the epilogue `_γ`/`_ω` is already `DONE`). BOTH SN4-EXCLUSIVE ⇒ ZERO ARBITRATION.** Checked out `OUT:SN4-RTX:s208`. **STEP 0 IS COMPLETE, ALL FIVE CHECKS:** (a) live · (b) spelling taken from the tree · (c) ⭐ **all four globals (`g_pcall` `g_pcall_top` `g_pcall_wires` `g_flat_ret_snapbuf`) are ABSENT from the `.so` dynamic table ⇒ linker-localized ⇒ direct `[rip+sym]`, NO GOTPCREL** — the exact split that cost RTX-2 a rung, measured not assumed · (d) ⭐⭐ **`func_call`: 10,000,000 calls EACH — `rt_flat_ret_snap` 17.0–18.4%, `rt_proc_open_fn` 9.2–10.1% of window; `fibonacci` 2,692,537 each at 10.3%/10.8%. SCALING PROVEN 1:1 at two loop counts (1M→1,000,000 · 2M→2,000,000) AND THE SHARE IS STABLE ACROSS SCALES (17.03%→17.23%) — the signature of steady-state cost, not setup** · (e) not already asm (`rt.o`, C). **~27% of `func_call` in two `void*(void)` functions with no arguments** — the simplest ABI on the ladder.
**⚠ BEFORE THE PORT — OWED BY PROTOCOL:** push the ledger check-out FIRST, not with the work (`RTX-CLAIMS.md` rule: *a claim that lands with the port is not a claim, it is an announcement*) · re-prove the watermark live · pre-state the expected board so a null is informative (s187/s204 rule) · **ISOLATION ARM, not a family gate — these land into CALL, which RTX-4 already partly ported, and the family gate's error has NO known sign or bound (s204).** ⚠ `rt_flat_ret_snap` was touched by the ζ ladder's Z4-7 slice 2 (`rt_flat_wires_t` gained an r12 field, sizeof 40) — **re-read it at HEAD before porting; the struct is moving under a parallel session.**
**⚠ THREE BENCHMARKS SEGFAULT AT HEAD IN MODE 3, ALL VERIFIED WITHOUT THE INTERPOSER: `roman.sno` · `pattern_bt.sno` · `string_pattern.sno` (rc=139).** Not chased — RULES says the 2-way monitor, not guesswork. ⛔ **NOTE THE INSTRUMENT CONSEQUENCE: the two pattern-matching benchmarks are among the three broken, so the PAT/MATCH families (RTX-8/RTX-9) have NO working benchmark to be graded on. Fix the instrument before those rungs, per the RTX-0b/0c/0d tradition.**
**⚠ COLLATERAL LOGGED, NOT CHASED: `corpus/benchmarks/snobol4/roman.sno` SEGFAULTS rc=139 at HEAD in mode 3, verified WITHOUT the interposer.** Wants the 2-way monitor per RULES, not guesswork. Same disposition as s205's `LEN(2+1)` segv.
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-7-NV-IS-BYPASSED-BY-GVA-SLOTS-AND-RT-CALL-ARR-IS-87PCT.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## ⛔ PRIOR CURSOR — s207 (2026-07-29): **LON DIRECTIVE — `bb_binop_arith` FOUR ARMS COLLAPSED TO ONE PER-OP CALL. IMPLEMENTED, GATED, MEASURED. ✅ PUSHED — SCRIP `d61d7cba`+`cb3da95e`, corpus `b1969af5`+`102ddfa8`; BOTH `.s` REGENS RUN AND COMMITTED (banner corrected s208; it had read "LOCAL ONLY, NOT PUSHED", and the two regens it listed as OWED were in fact already done).**
**DIRECTIVE OF RECORD (Lon, s207):** *"for now all these should be ONE CALL into the runtime. We'll optimize the inline later. Let's just reduce these to proper ADD calls if it +, and a SUB call if it -, etc."* ⇒ **RTX-6's inline ladder is DEFERRED BY DIRECTION, not abandoned.** The s206 SSE plan (real-real arm 2) is superseded for now.
**✅ LANDED IN WORKING TREE (2 files, `arithmetic.c` +12 / `bb_binop_arith.cpp` 166→94):** nine per-op runtime entries (`rt_add` `rt_sub` `rt_mul` `rt_div` `rt_mod` `rt_pow` `rt_cunion` `rt_cdiff` `rt_cinter`) each absorbing the `DT_DATA`→`rt_binop_overload` dispatch, so overloading survives with ONE call at the site. Template: 4 arms → 2 (value-spine `rspq` / frame `FRQ`, identical shape). Flagged box `n7_binop_α` **44 → 13 instructions**; `arithmetic.s` 404→271; `mov r8d` op-shipping sites **0**.
**⭐ TEMPLATE-ONLY — ZERO `x86_asm.h` CHANGE. This is the axis s206 was BLOCKED on** (its SSE arm needed `addsd`/`movsd` memory forms ⇒ RTX-11/12 concurrency collision). This rung does not touch that file, so the sequencing question does not arise for it.
**✅ GATES:** baseline re-proven live BEFORE any edit — **m3 280/54 · m4 276/50 SKIP=8 (334)**, identical after. ⭐ **Failure sets diffed LINE-BY-LINE, not counted: zero movers either direction.** Icon 4/0 · Prolog 189/0, and **verified reached** (`generators.icn --compile` emits `rt_add`×2/`rt_sub`×1) — not the §7 step-2b unmoved-battery false claim. ⚠ Snocone not run (`beauty_full_bin` absent — environmental).
**⭐⭐ COLLATERAL DEFECT REMOVED: THE OLD INLINE `idiv` HAD NO ZERO-DIVISOR TEST** — guarded only by DT_I tag checks, so `1/0` reached `idiv` and raised **SIGFPE instead of statement failure**. In 5 committed artifacts (`arithmetic.s`, `calculator-1/2.s`, `json.s`, `op_dispatch.s`). `INT64_MIN / -1` same hole. ⚠ Missing zero-test proven STATICALLY from artifacts; fix proven DYNAMICALLY (`X = 1 / 0` now fails, rc=0). **Old binary not re-run to watch it trap — do not upgrade this to a demonstrated crash.**
**✅ FALSIFICATION TWO-SIDED, AND AIMED AT THE RUNG'S *DISTINCTIVE* COMPUTATION (s204's rule, applied on purpose):** probe corrupted `rt_sub` to compute `a.i + b.i` — the claim under test is the op→symbol routing this rung invented, not "does arithmetic run." **LOUD, no escalation needed: m3 268/66 · m4 264/62 — 12 movers EACH mode**, demo → `13/13/30/3`. Restored ⇒ **exact watermark, failure set byte-identical**, demo → `13/7/30/3`.
**⚠ PERF — THREE-WAY BOARD (RT_OPT=`-O0`, A/B/C interleaved within each round, round 1 discarded, medians 2–5, arms never swap rank).** NEW1 = per-op entries delegating to `rt_num_arith`; **NEW2 = own body (in tree)**:
| program | OLD | NEW1 | NEW2 | NEW1 | **NEW2** |
|---|---|---|---|---|---|
| `arith_loop`×60 (60M, pure INT — lost its inline `add`) | 622 ms | 964 ms | 798.5 ms | 0.645× | **0.779×** |
| `arith_mixed` (40M, REAL+REAL — was already a bare call) | 1820 ms | 2325.5 ms | 1940.5 ms | 0.783× | **0.938×** |
Checksum `60000001` unchanged throughout. ⭐ **BOTH NEW2 BANDS PRE-STATED, BOTH HELD** (0.75–1.00 → 0.779; 0.90–1.05 → 0.938). NEW1's `arith_mixed` band was **FALSIFIED** (0.9–1.0 stated, 0.783 measured) — **and recording the miss is what found the delegation hop and produced NEW2.**
⛔ **STILL A REGRESSION; THAT IS THE PRICE OF THE DIRECTIVE — the inline arms were deleted on purpose. The floor the later inline rung must beat is 0.779× / 0.938×, NOT 1.000×.**
⚠⚠ **UNEXPLAINED, DO NOT PROMOTE TO A LAW:** NEW1→NEW2 deletes **exactly one `-O0` frame** in both programs, yet reclaims **2.76 ns/call** (`arith_loop`) vs **9.6 ns/call** (`arith_mixed`) — 3.5× spread, same deleted frame. 9.6 corroborates the 11.3 estimated independently; `arith_loop` is the outlier. **No cause asserted** (candidates: single-core instability — this box gave s200's 1.340/2.497/2.606/0.510 spread — or s188's operand-shape law). **"One `-O0` frame costs N ns" is NOT established here.**
**⛔ OWED BEFORE LANDING:** (1) `.s` regen ×3 (templates touched — RULES step 4); (2) no kill-switch (unconditional template edit; A/B done by retaining both binaries); (3) Snocone battery (`beauty_full_bin` absent from clone); (4) **`-O2` arm not measured** — this rung ADDS a C call, so `-O2` will move these numbers; never quote 0.779×/0.938× without the `-O0` clause.
**⭐⭐ COLLATERAL — THE SAME `idiv` HOLE EXISTS TWICE MORE AND WAS DELIBERATELY *NOT* FIXED.** `bb_binop_gvar_arith.cpp` (122 ln, 5 arms) + `bb_binop_gvar_arith_slot.cpp` (82 ln) carry the identical unguarded `cqo ; idiv rcx` ×3 — **but both templates are DEAD**: `IR_BINOP_GVAR_ARITH` produced by NOTHING, no dispatch site, 0 hits in every committed `.s`; they merely compile+link via `bb_templates.h:66-67` + `Makefile:293-294`. Verified by **unfiltered** grep per ARCH §7's own filtered-grep lesson. ⛔ **Patching them would be UNFALSIFIABLE — the probe would come back SILENT (s204: a question, not an answer) while build, batteries and board all looked green.** ⭐ **PHANTOM FAMILY, SEVENTH SHAPE: a dead TEMPLATE, not a dead symbol — found by grepping a code SHAPE, and the shape is real in both the live and the dead copy. The tell that separates them is "who dispatches this?", which neither a symbol grep nor `nm` asks.** ⇒ handed to `GOAL-DEAD-CODE-SWEEP.md` (204 ln, 2 files, 2 header decls, 2 Makefile entries); if revived, the zero-test lands WITH the revival.
**✅ SESSION'S OWN RESIDUE REMOVED:** `rspd()` went unreferenced when the tag ladder was deleted — struck in the same session per ARCH §7. `bb_binop_arith.cpp` 166 → **93** lines. Final gate after removal: **m3 280/54 · m4 276/50 SKIP=8, failure set byte-identical.**
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-BINOP-ARITH-FOUR-ARMS-COLLAPSED-TO-ONE-PER-OP-CALL.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## ⛔ PRIOR CURSOR — s205 (2026-07-29): **ORIENTATION + HOUSEKEEPING SESSION. No new RTX rung landed. Two corrections of record. NEXT: RTX-6 remainder (`rt_num_arith` int-int inline fast path) — instrument exists (RTX-0d), plan is ready, register-contract staleness is documented and corrected.**
**⭐ CORRECTION 1 — `ARCH-SNOBOL4-RTX.md §2` REGISTER TABLE WAS STALE AND LOAD-BEARING.** `r12` was listed as the conditional-assignment stack pointer. **r12 is FREE** — `ZC_FRAME_R12` was deleted outright at ZR-RSPRBP-1 (Lon directive 2026-07-27, SCRIP `da8c2347`), zero consumers. The ζ basis set is CLOSED at RSP and RBP. `rbp` was listed as the fixed ζ frame base — it is **per-graph**: `x86_fb_pinned()` selects rbp for suspended generators / pattern blobs / deep-arrival graphs and rsp for depth-static determinate graphs. Verified from live `x86_asm.h:347-361` and `zeta_choices.h:177`. **Any RTX asm spelling `[r12+off]` or a hardcoded frame base would encode the wrong thing silently.** Use `FR()`/`FRQ()` only. `ARCH-SNOBOL4-RTX.md §2` corrected this session. FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-ARCH-REGISTER-TABLE-STALE-R12-FREE-AND-FRAME-BASE-PER-GRAPH.md`.
**⭐ CORRECTION 2 — OPT-CF-COMMENT: `optimizer.c` stale DEFINE claim corrected (SCRIP `2426bac6`).** Gate comment read "ALWAYS ON for DEFINE proc graphs" — measured fold=0 for SNOBOL4 DEFINEs under default settings. CAUSE IS CORRECT, NOT A DEFECT: SN4-FLAT-PROC (s176) puts the function body as ordinary labelled statements in the one main graph; a DEFINE lowers to IR_SAVE_RESTORE + IR_CALL hook only — nothing foldable. Folding the body is governed by the expression arm (OFF) by construction. Nearly landed a vacuous one-line "fix" (copying nparams onto sno_build_call_stub's graph). ⛔ Do not re-open without re-measuring. Patterns fold correctly (measured: `LEN(2+1)` folds). Icon/Pascal/Prolog proc graphs own their bodies — the nparams arm fires correctly for them.
**⚠ INCIDENTAL BUG LOGGED, NOT CHASED (needs monitor): `'ABCDEF' ? LEN(2+1) . Z` segfaults in mode 3.** Wants the 2-way monitor per RULES, not guesswork.
**ARITH RUNG PLAN — READY TO EXECUTE (RTX-6 remainder).** The design is convergence on an in-tree pattern: `IR_COERCE_NUMERIC` + `bb_coerce_numeric.cpp` already exist; `lower_icon.c:342-343` already emits COERCE per arith operand and skips for literals; SNOBOL4 already uses the same nodes on the relop path (`sx_pred_cmp`). SNOBOL4 arithmetic is the sole outlier — it goes straight to `rt_num_arith(a,b,op)`. Work: (1) extend `lower_snobol4.c` to emit `IR_COERCE_NUMERIC` for `BINOP_CAT_ARITH` operands, mirroring `lower_icon.c:342-343` shape but with SNOBOL4 positional-errcode discipline (first position first, cf. `sx_pred_cmp`). (2) Inline fast path in `bb_binop_arith.cpp`: both `DT_I` → inline integer `add`/`sub`/etc.; both `DT_R` → inline SSE; anything else → out-of-line per-op RT entry. ⛔ NARROW INLINE PREDICATE: both-same-tag only. NOT "is either real" — Ch.17: a string parsing as real (`"1.5" + 1`) is a real operand (`operand_is_real_str`, `arithmetic.c:232`); misclassification silently changes results. (3) Per-op RT entries instead of `rt_num_arith(a,b,op)` — kills the `mov r8d, imm` + callee switch at every site. (4) Isolation arm for measurement (not family gate — `rt_cmp_d` already landed in ARITH). ⛔ Template work fires `.s` regen ×3 and touches `x86_asm.h` + `bb_*` — conflicts with RTX-11/12 concurrency warning. Lon's call on sequencing.
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-ARCH-REGISTER-TABLE-STALE-R12-FREE-AND-FRAME-BASE-PER-GRAPH.md`.**
## ⛔ LIVE CURSOR — s204 (2026-07-29): **RTX-5b LANDED — `rt_agg_alloc` in asm behind `SCRIP_RTX_ALLOC`. ⭐⭐ AND THE MEASUREMENT INSTRUMENT OVER-ATTRIBUTES: A FAMILY-GATE A/B CREDITS THE WHOLE FAMILY TO THE NEWEST RUNG.**
**⭐⭐ THE FINDING, MEASURED NOT INFERRED.** `bench_sno_rtx.sh ALLOC` read `table_churn` **1.028×** · `table_access` **1.039×** after this port. **Quoting either as RTX-5b's win would have been a FALSE CLAIM — the ALLOC gate also disables RTX-2's `rt_gcheap_alloc`/`rt_str_alloc`, live since s163.** ISOLATED (a second build with ONLY this rung routed to C, RTX-2 left live, `.so`s swapped per round, interleaved, round 1 discarded as the s201 hugepage warmup): **`table_churn` 1.022× · `table_access` 1.002×.** ⇒ the rung contributes essentially NONE of the family number. ⛔ **STANDING RULE: A RUNG LANDING INTO AN ALREADY-PORTED FAMILY CANNOT BE MEASURED BY ITS FAMILY GATE — it needs an ISOLATION ARM.** The family gate answers RTX-12's question, not the rung's. ⚠ **CANDIDATE, NOT A CAUSE — DO NOT RETRO-FIT:** RTX-3b's headline `var_access` 1.366× / `func_call` 1.080× were taken under `SCRIP_RTX_STR`, which also disables RTX-3. That does NOT make them wrong (RTX-3b's own falsification proves execution); it makes the ATTRIBUTION unproven and one build away from settled. **That isolation arm is the recommended NEXT rung.**
**✅ LANDED SCRIP `320d212a`.** Gate `SCRIP_RTX_ALLOC`; C body renamed `c_rt_agg_alloc` same commit; `HB_AGGV` anchored by `_Static_assert` in `rtx_init.c`. **TARGET SELECTED BY MEASUREMENT:** step 0(d) counts `table_churn` **8,001,201** and **SCALING PROVEN** (16,001,201 at 2× passes, constant 1,201 preserved) · `table_access` 7,506,501 · `string_manip` 0. `rt_call_arr` rejected — it needs fusion with `try_call_builtin_by_name`, already a hash + inline cache + jump table, so an asm port wins only `-O0` ceremony. **THE PORT'S SHAPE:** a six-line wrapper whose body is pure `-O0` ceremony around `rt_gcheap_alloc` (already asm since RTX-2); the asm deletes the frame and falls through into the existing armed carve. The `armed` fusion is a PROOF not a skip — armed ⇒ `g_alloc_detax==1` ⇒ the histogram guard is provably false ⇒ the call is ELIMINATED.
**✅ GATES:** watermark **m3 268/47 · m4 267/46 · DIVERGE=2** at gate ON and OFF, equal to the s202/s203 baseline and **re-proven live before any edit** · kill-switch md5 **byte-identical over 190 programs** at gate OFF **and with all six gates off** · unit 21/21 · alloc 36/36 · STR 8426/0.
**✅ FALSIFICATION TWO-SIDED — BUT THE FIRST PROBE WAS SILENT AND HAD TO BE ESCALATED.** Corrupting the block TYPE (`HB_AGGV+1`, mis-tagging every aggregate cell) moved the board by **ZERO**. Escalated to corrupting the allocation SIZE ⇒ gate ON **262/53 · 261/52 (6 movers each mode)**, same break gate OFF ⇒ **exact watermark.** ⛔⛔ **PROBE 1'S SILENCE IS A REAL COVERAGE GAP, NOT NOISE: the `HB_AGGV + clamp(kind,0,2)` field — this wrapper's entire distinctive computation — is UNVERIFIED BY ALL 315 PROGRAMS.** RTX-5b's correctness evidence covers its allocation behaviour, NOT its type computation. **A silent probe is a question, not an answer.**
**⚠ PERF — SAY IT PLAINLY: NO DEMONSTRATED SPEEDUP.** Expected board was pre-stated at 1.03–1.10× outside the ±3% floor **so that a null would be informative**; isolated result 1.022×/1.002× ⇒ **PREDICTION FALSIFIED, no-regression only.** ⭐ Because "reached" was already proven by falsification, the null means the wrapper ceremony is a smaller share of the window than estimated — NOT that the port is unreachable. That distinction exists only because it was written down first.
**⭐⭐ AND THE PHANTOM FAMILY'S GENERATOR IS FOUND: 125 OF 211 DECLARATIONS IN `rt/rt.h` ARE DEAD** (no definition anywhere; most with exactly one src ref — their own declaration). 59% of the header, and it is the natural place a rung author looks. **THREE UNSTARTED RUNGS WERE MIS-AIMED, all corrected this session:** ⭐ **RTX-9's `rt_pat_*` is dead on all 30 names — the live constructors are `pat_*`, no prefix, 32 of them in `pattern_match.c`. SEVENTH PHANTOM MEMBER AND A NEW FAILURE MODE: the prior six risked wasted work; this one risked ABANDONING A LIVE, HOT FAMILY**, because the grep hits the `rt.h` declaration and `nm` then finds nothing — the exact signature of a phantom. · **RTX-7's NV row is MIXED** (`NV_GET_fn`/`NV_SET_fn` real, `rt_nv_get`/`rt_nv_set` phantom) — **the two names most likely to be spot-checked are the two that are real**, so one check "validates" a 2/6-dead list. **Spot-checking a symbol list is not checking it.** · **RTX-10's vstack audit DISCHARGED by measurement** — zero definitions for any `rt_push_*`/`rt_pop_*`/`rt_halt_tos`; its "if dead, delete" branch is TAKEN.
**✅ s203 HOUSEKEEPING DISCHARGED (SCRIP `65ca02c3`):** the ARITH FAMSET was **annotated but not fixed** — s203 measured both its programs useless and wrote that in the comment while leaving the live set unchanged, so the default invocation still produced the false null the comment warned about. **An annotation does not disarm a default.** Corrected to `var_access func_call`. Same shape found one level down: `ALLOC`'s famset led with `string_manip`, which is `rt_agg_alloc`-BLIND (0 calls) — `table_churn` added and put first. **RTX-0d minted** for the mixed-arith instrument; `rt_num_arith` is structurally unreachable by ANY integer-only program at any loop count.
**⭐⭐ RTX-3b ISOLATION ARM RUN SAME SESSION — ATTRIBUTION DISCHARGED, AND IT CORRECTED THIS CURSOR'S OWN HEADLINE.** `.Lsc_null` routed to `.Lsc_slow` (= pre-RTX-3b behaviour), RTX-3 left live, `.so`s swapped per round: **`var_access` isolated 1.513× vs family gate 1.494× · `func_call` isolated 1.031× vs family 1.065×.** ⇒ **RTX-3b's recorded numbers HOLD and on `var_access` were UNDERSTATED — the flag is discharged BY THE ARM, not by argument.** ⛔⛔ **AND THE RULE IS STRICTLY STRONGER THAN "OVER-ATTRIBUTES": the family gate's error has NO KNOWN SIGN AND NO KNOWN BOUND** — measured −3pp to +2pp across four programs the same day on one machine (`var_access` family 1.494 < rung 1.513; `table_access` family 1.039 > rung 1.002). **"The family number bounds the rung" is FALSE.** You can only measure the rung directly. ⭐ **AND s188's LAW SURVIVES ANOTHER TEST: CALL COUNT DOES NOT PREDICT BENEFIT** — `var_access` and `func_call` make the SAME 10.0M `str_concat_d` calls and take 1.513× vs 1.031× from the identical port. Operand SHAPE decides, not call count. ⚠ `var_access` at F=1 is a **194–302ms window = BOGUS by the harness's own `MIN_MS=800` gate**; the ratio above is from an ×8 scaling. Do not quote an unscaled `var_access` number.
**NEXT:** (1) ✅ RTX-0d LANDED s204 — `arith_mixed.sno`, 40,000,001 calls/run, scaling proven, checksum pre-predicted; `rt_num_arith` measurable at last. THEN: **the rest of RTX-6** (`rt_num_arith` int-int fast path) now that it can be graded — settle the attribution on the ladder's headline speed claims, one build; (2) **RTX-0d** instrument; (3) **RTX-9 with the corrected `pat_*` spelling**; (4) delete the 125 dead `rt.h` declarations — the generator, not the symptom.
**FINDING: `FINDING-2026-07-29-CLAUDE-SN4-RTX-5B-AGG-ALLOC-LANDED-AND-THE-FAMILY-GATE-OVER-ATTRIBUTES-TO-THE-NEWEST-RUNG.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

⛔⛔ **SYMBOL OWNERSHIP IS NOT IN THIS FILE — IT IS IN `RTX-CLAIMS.md`.** The runtime is SHARED (19,962 lines, one `.so`, six languages); two RTX ladders work it from two directions. **25 symbols are called by both Icon and SNOBOL4 live artifacts.** Check a symbol OUT in that ledger — and PUSH the claim — BEFORE writing code. Run `scripts/util_rtx_claims.sh` at session start and session close; it derives symbol truth from the tree, so the ledger cannot rot silently. This ladder is `SN4-RTX`.


## ⛔ PRIOR CURSOR — s203 (2026-07-29): **RTX-6 LANDED — `rt_cmp_d` in asm behind `SCRIP_RTX_ARITH`. TARGET SELECTED BY MEASUREMENT: STEP 0(d) FALSIFIED THE RUNG'S OWN NAMED TARGET.**
**⭐⭐ STEP 0(d) RESULT: `rt_num_arith` measures ZERO calls in `arith_loop` at BOTH N=100000 and N=200000.** Integer arithmetic is fully inlined by the emitter and never reaches the C runtime. `rt_cmp_d` instead measured as the #1 hottest C runtime call in every stable benchmark: var_access/func_call **10,000,001**/run · string_manip 5,000,001 · table_access 5,016,003 · fibonacci 2,692,537 — and was entirely unported and ungated (zero references in `src/runtime/rtx/`).
**✅ LANDED SCRIP `70198a9d`.** Gate `SCRIP_RTX_ARITH`. C body renamed `c_rt_cmp_d` same commit. Gates: m3 268/47 · m4 267/46 · DIVERGE=2 at gate ON and OFF, byte-identical to s202 baseline. Two-sided falsification: corrupted DT_I result ⇒ gate ON **245/70 (23 movers)**; gate OFF ⇒ exact watermark. STR 8426/0 · RTX UNIT ALL PASS.
**⚠ PERF: var_access 1.036× · func_call 1.014× (RT_OPT=-O0, R=5 interleaved) — INSIDE the ±3% null floor. NOT a demonstrated speedup. No-regression only. No expected board was pre-stated — by s188's rule this makes the perf result uninformative either way.**
**⭐⭐ TWO COLLATERAL FINDINGS, BOTH MEASURED:**
**(1) `bench_sno_rtx.sh:51` ARITH FAMSET IS UNUSABLE** — recorded as `mixed_workload arith_loop # rt_num_arith 500K`. `arith_loop` calls it zero times (measured); `mixed_workload` segfaults on pristine main (verified rc=139 without interposer). Running ARITH rail as written is a guaranteed false null. **FAMSET MUST BE CORRECTED before quoting any ARITH speed number.** Candidate: programs that mix integer and real arithmetic; `rt_num_arith` is reached at 50,000/run by a simple `X = X + 1.5` loop.
**(2) FIVE PHANTOMS IN THE RTX-6 RUNG'S SYMBOL LIST** — `rt_incr` · `rt_decr` · `rt_exp` · `rt_neg` · `rt_coerce_num`: declaration-only in `rt/rt.h`, zero definitions, zero call sites. Same shape as RTX-3's phantoms. ARCH §7 says strike dead names in the same commit that finds them; that edit is **OWED to this file and to ARCH-SNOBOL4-RTX.md §5**.
**⚠ REGISTER NOTE FOR ζ COORDINATION:** `rtx_arith.S` touches only the RTX working set (rax rcx rdx rsi rdi r8 r9 r10 xmm0/1), never rbx/rbp/r12/r13/r14/r15. Forward-compatible with any ζ register consolidation by construction. Phase-1 RTX porting at call boundaries does NOT free registers; that requires RTX-11 (registerized ABI, phase-2, touches `x86_asm.h`, ζ-collision zone) or emitter-level inlining of the DT_I compare.
**NEXT:** (a) fix ARCH §5 phantom list + `bench_sno_rtx.sh` ARITH FAMSET (housekeeping owed this session); then (b) `rt_call_arr` (10.0M in string_manip) or `rt_agg_alloc` (7.5M in table_access) — both far above remaining ARITH targets.
**`handoff_status.sh` is the push truth — not this block.**

## ⛔ PRIOR CURSOR — s202 (2026-07-28): **RTX-3b AND RTX-5a WERE NEVER ON MAIN. THEY ARE NOW. ⭐⭐ A NEW MEMBER OF THE STALE-ORIENTATION FAMILY: BRANCH-LANDED WORK RECORDED IN MAIN-LANDED VOCABULARY.**
**⭐⭐ THE FINDING, MEASURED NOT INFERRED:** `origin/rtx-3b-s200` was **3 ahead, 9 behind** `origin/main`. Both rungs are marked `- [x]` and both cursors say LANDED — and both were true *of a branch*. **Every fresh clone — i.e. every subsequent session's starting state, including s201's — built a SCRIP with NEITHER.** Verified by tells, not prose: `rtx_str.S` on main had **0** `SNUL` refs (s200's own tell for RTX-3b's presence is 9) and `SCRIP_RTX_LEAF` appeared nowhere in `src/`. ⇒ **This is NOT the frozen push-status banner of s190/s199/s200 (rule (a)); it is a distinct shape — "landed" means origin/main everywhere else in this project, and a `[x]` rung whose commits sit on an unmerged branch reads as done to every reader while shipping nothing.** ⛔ STANDING FIX: **a rung is `[x]` only when its commits are ANCESTORS OF `origin/main`.** Cheap check: `git rev-list --count origin/main..<branch>` == 0. A branch is a place work goes to *decay* — this one was 9 behind by the time it was found, so s200's "the port is base-independent" had to be re-earned.
**✅ LANDED ON MAIN — cherry-picked, ZERO CONFLICTS:** `dc5d9eb9` (RTX-3b) · `b497034d` (RTX-3b follow-on) · `9cc782fb` (RTX-5a). Footprint is 5 files, ALL RTX-owned per the §CONCURRENCY CONTRACT (`src/runtime/rtx/*`, `src/runtime/rt/*`) — zero overlap with the ζ ladder's `emit.cpp`/`templates/`/`x86_asm.h`. **The contract predicted this exactly; it held.**
**✅ WATERMARK RE-PROVEN LIVE, TWICE — BEFORE AND AFTER THE LANDING, IDENTICAL: m3 268/47 · m4 267/46 · DIVERGE=2** (`141_pat_eval_double_fn_arbno`, `W06_tab`). ⭐ **s200's BASELINE OF RECORD CONFIRMS** — an inherited claim that HOLDS, which s201 rightly notes is as informative as voiding one. Failure set is overwhelmingly `*_pat_*` = the parallel ζ session's territory.
**✅ KILL-SWITCH, BOTH SHAPES:** all **five** gates OFF ≡ watermark; **LEAF alone OFF ≡ watermark** (the new gate's own kill-switch). Both C fallbacks confirmed live first: `rt_deref_slow` (`pattern_match.c:1173`) and `to_int_slow` (`core/core.c:1991` — needed `grep -a`; `file` reports `core.c` as `data`, so **the documented non-UTF-8 grep hazard is REAL and LIVE**).
**✅ FALSIFICATION TWO-SIDED ON *THIS* BASE, NOT INHERITED:** corrupted the `to_int` DT_I fast-path **RESULT** (`incq %rax`) ⇒ **248/67 · 247/66, TWENTY movers.** Same break with `SCRIP_RTX_LEAF=0` ⇒ **watermark.** Restored, rebuilt, re-proven, tree clean. ⭐ **PROBE CHOSEN UNDER s187's RULE:** breaking the *gate* to fall through to C would have been **vacuous** — its output equals the gate-off output, which is the watermark. The break had to corrupt a result, not a route.
**⭐ STR BATTERY IS 8426, NOT RTX-3's 8404** — the +22 are precisely RTX-3b's null-operand shape, so the port is live and exercised, not merely linked. Unit 21/21 · alloc 36/36 · STR 8426/0.
**⚠ NO-REGRESSION ONLY, SAY IT PLAINLY (§7 step 2b):** smokes 7/7×2 · Prolog 4/0 · Icon 4/0 · Snocone 8/0 are evidence for the **shared runtime edit**, NOT for the asm — they do not move under the probe. Citing them as asm gates is a FALSE CLAIM.
**⭐⭐ CONSEQUENCE FOR THE OPEN AGG FORK — THIS IS WHY THE LANDING WAS THE PREREQUISITE.** RTX-5's family is `rt_subscript_var` + `rt_deref` + `rt_field_var`. On main-before-this, **`rt_deref` was ungated asm and permanently ON in BOTH arms of any A/B** — the fork would have been ruled against an instrument blind in exactly the way s188 (cold `rt_call_arr`) and s200 (`--include=*.S`) keep warning about. With `SCRIP_RTX_LEAF` on main, an honest RTX-5 A/B is possible **for the first time.**
**⚠ REMAINING UNGATED ASM (was six, now four):** `rt_sg_scan_member` · `rt_sg_scan_nonmember` · `rt_sg_member` (`rt/rt_sg_scan.S`) · `rk_gram_enter_box` (`rt_gram_trampoline.S`). **All four are AT&T syntax, contrary to Ruling 1's "Intel, one project-wide"** — outside the contract on two axes, not one.
**⚠ s201's OWN CURSOR CONTRADICTS ITSELF ON STEP 0(d):** its line 9 says DISCHARGED with counts measured; its NEXT line and the RTX-0c rung both say still OWED. Unresolved — read the FINDING, not the cursor.
**✅ s201's PRE-STATED NOHUGE PREDICTION TESTED — CONFIRMED, HARNESS UNTOUCHED.** AGG null control (both arms identical code ⇒ pure noise floor): worst deviation **DEFAULT 2.8% → NOHUGE 0.5%**, a 5–6× tightening, inside ±3% both ways. ⛔⛔ **BUT THREE CORRECTIONS, ALL MEASURED: (1) NOHUGE is 5–10% SLOWER** — s201's "3.9×→1.62×" figures are SPREAD ratios, not throughput; read as speed they invert the sign. **The cost exceeds a typical rung win (RTX-3b `func_call` = 1.080×). (2) s201's instability MAGNITUDES DO NOT REPRODUCE** — recorded default 7.5×/3.9×, measured **1.254×/1.113×**; the bimodality is absent in this container. ⭐ The one figure that DID reproduce is the NOHUGE floor (s201 1.12× vs s202 1.116×): **the floor is stable, the ceiling is not** — so there is no correction factor to retro-fit *with*, which strengthens s201's own "do not retro-fit" order. **(3) DEFAULT IS THE SHIPPING CONFIGURATION** — nobody exports `SCRIP_NOHUGE`; measuring only there tunes against a mode no user runs.
**⇒ RECOMMENDED THIRD OPTION THE §4 FORK DID NOT CONTAIN — (c) DEFAULT STAYS THE BASIS OF RECORD; NOHUGE BECOMES A SECOND LABELLED ARM used to CONFIRM a near-floor result, never to produce one.** Gets (a)'s discriminating power without (a)'s stated cost of invalidating every recorded number. **Lon's call — I did not touch the harness.**
⭐ **METHOD LESSON PAID FOR IN THIS SESSION:** my first sampling ran the arms in BLOCKS and reported NOHUGE spread 112% — an ORDER EFFECT (prior arm's compaction decaying into the next arm's window), the exact opposite of the truth. **Hugepage state is carried ACROSS PROCESSES, so the previous arm is part of the next arm's environment.** `bench_sno_rtx.sh`'s header already says "interleaved, per-round alternation to cancel drift"; I did not apply it until the data forced me to.
**NEXT:** Lon's two rulings — the `SCRIP_NOHUGE` harness fork (now with (c) on the table and the numbers to judge it) and the AGG port-vs-layout fork — both takeable against an instrument that is no longer blind.
**`handoff_status.sh` is the push truth — not this block.**

---

## PRIOR CURSOR — s201 (2026-07-28): **RTX-0c LANDED — `table_churn.sno`. ⭐⭐ AND THE AGG INSTABILITY WAS TWO CAUSES, THE LARGER ONE ENVIRONMENTAL AND PREVIOUSLY UNDIAGNOSED: `MADV_HUGEPAGE` COMPACTION, CHARGED INTO THE MEASURED WINDOW.**
**⭐⭐ `SCRIP_NOHUGE=1` BUYS MORE ON `table_access` THAN REDESIGNING THE PROGRAM DOES.** Measured 8–10 samples each, spread excl. first run: **`table_churn` NOHUGE 1.12× · `table_churn` default 7.5% BIMODAL (367…2764ms) · `table_access` NOHUGE 1.62× · `table_access` default 3.9%** (raw 1474…10211ms). ⇒ s200's shape-based diagnosis ("5,000 fresh TABLE(512)") is REAL but SECOND-ORDER; it was **read off the source and never measured**, and it absorbed the blame for the environmental cause underneath. Same class as s200's own `rt_deref` grep-filter finding.
**⭐ MECHANISM, AND WHY SELF-TIMING DOES NOT EXCLUDE IT:** `TIME()` is `clock()` (`by_name_dispatch.c:5051`) = **CPU time INCLUDING SYSTEM time**; `gc_heap.c:149` calls `madvise(MADV_HUGEPAGE)`; kernel compaction stalls are charged to the process and land INSIDE `T2-T1`. **The BIMODALITY was the tell** — scheduler contention on `nproc=1` would be unimodal and, being CPU-timed, would largely not register at all.
**⚠ CANDIDATE, NOT A CAUSE (s199 discipline): this is a live suspect for the unexplained wrong-signed `string_manip` 1.279× under the STR gate, and for s200's four `table_access` readings.** ⛔ Do NOT retro-fit it onto recorded numbers — they are of UNKNOWN reliability, not known-bad. Re-measure.
**⛔ HARNESS FORK OPEN, DELIBERATELY NOT DECIDED — LON'S CALL.** `bench_sno_rtx.sh` does not set `SCRIP_NOHUGE`. (a) export it for both arms + label it like `RT_OPT` — apples-to-apples, but **changes the basis of every recorded number**; (b) leave it on and raise `R`. **I did not touch the harness.** Expected board stated in advance so (a) is falsifiable: the AGG null must stay inside ±3% with a visibly tighter spread.
**✅ RTX-5 STEP 0 RE-PROVEN LIVE, NOT INHERITED — ALL FIVE CHECKS PASS:** 0(a) `rt_subscript_var(DESCR_t,DESCR_t)` @ `pattern_match.c:1000` · 0(b) spelling byte-exact · 0(e) NOT already asm (grep run WITH `--include=*.S`).
**✅ 0(d) DISCHARGED — MEASURED, SCALED, AND THE INTERPOSER VALIDATED AGAINST A POSITIVE CONTROL FIRST.** `table_access` counted **sub=5,001,000 deref=2,500,500 — byte-exact to s188's recorded numbers**, so the interposer is proven AND s188's inherited counts HOLD (⭐ inherited claims are not always stale; confirming one is as informative as voiding one). **`table_churn`: sub=8,000,800 deref=4,000,400 concat=4,010,802 arith=0** — matches the design formula exactly (10000×400×2 + 800), and **SCALES LINEARLY at two loop counts** (P=1000 ⇒ 800,800; variable part ×10 exactly, constant 800 preserved). ⭐ **The direct opposite of `rt_call_arr`'s flat-8 vacuity.** **AGG:STR density 2.99:1 vs `table_access`'s 1.50:1 ⇒ 2× the AGG signal, half the loop-guard dilution.** `arith=0` in BOTH ⇒ no ARITH contamination to subtract.
**⚠ ENVIRONMENT: detached `nohup … &` builds DO NOT SURVIVE A TOOL CALL** — killed at 33/240 objects with an empty `scrip`. Independent reproduction of the s126 poisoned-tree lesson. Foreground `make` under `timeout`, incremental, converges. `-O0` throughout, no `-O1`/`-O2`.
**NEXT:** Lon's §4 harness ruling → step 0(d) counts → then the s200 AGG fork (still open, still not to be decided on opinion).
**FINDING: `FINDING-2026-07-28-CLAUDE-SN4-RTX-0C-HUGEPAGE-COMPACTION-IS-THE-LARGER-HALF-OF-AGG-INSTABILITY.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## PRIOR CURSOR — s200 (2026-07-27): **RTX-3b SURVIVED — IT IS ON ORIGIN. REVALIDATED ON CURRENT MAIN AS BRANCH `rtx-3b-s200`, ZERO REGRESSION, 1.366×/1.080× REPRODUCED. `string_pattern` DEBT DISCHARGED. NEXT: RTX-5 (AGG).**
**✅ s199's "PUSH PENDING A CREDENTIAL / NOT PUSHED" WAS FALSE WHEN READ.** `git ls-remote` shows `1f0e53cc…` = `refs/heads/rtx-3b-s199` on origin; `c5ee5f0b` is a valid object; `rtx_str.S` there has **9 `SNUL` refs** (s190's tell for lost work was ZERO). **Fifth confirmation of RULES.md (a): a push-status banner is frozen before the event it describes.** ⭐ NEW: the failure is **asymmetric in cost** — a stale "pushed" loses work (s190), a stale "not pushed" costs two minutes (s200). s199 erred safe by accident of ordering, not design.
**⛔⛔ THREE RECORDED BOARD CLAIMS, NONE MATCHING MEASUREMENT — RE-PROVE, NEVER INHERIT.** s199 cursor **221/94** · main HEAD `8d0665c8` commit msg **295/294** · **MEASURED s200: m3 268/47 · m4 267/46 · DIVERGE=2** (315 total in all three ⇒ same suite, real disagreement). ⭐ Generalizes the push-status rule *backwards*: a board number is a measurement taken in a sandbox/tree that no longer exists. **BASELINE OF RECORD = 268/47 · 267/46 · DIVERGE=2 — and re-prove it live anyway.**
**✅ RTX MECHANICALLY RULED OUT OF THE 47, RE-MEASURED NOT INHERITED.** Four gates enumerated from `rtx_init.c:10` (MISC/ALLOC/STR/CALL, all default ON); board with all four OFF is **byte-identical failure-set**. ⭐ That IS the whole-surface kill-switch proof, obtained free. Failures are `*_pat_*` — the parallel ζ session's territory.
**⭐⭐ RTX-3b REVALIDATED ON CURRENT MAIN — THE PORT IS BASE-INDEPENDENT.** Cherry-picked onto `origin/main` (was 3 behind) with **zero conflicts** ⇒ `rtx-3b-s200` (`6154217c`,`5893d173`). Crosscheck failure set **byte-identical to baseline** · unit 21/21 · alloc 36/36 · **STR battery 8426/0** · **falsification RUN not inherited: inverting the a-arm ⇒ 8426→22 mismatches, UNIT FAIL, restored green.**
**⭐ MEASURED THIRD BASE, THIRD LOAD WINDOW: `var_access` 1.366× (s199: 1.391/1.396) · `func_call` 1.080× (s199: 1.089).** Both far outside the ±3% null floor. **The point is the INVARIANCE, not the ratio: s199 proved it holds across load, s200 proves it holds across BASE.**
**⭐⭐ `string_pattern` DEBT DISCHARGED BY CLASSIFICATION.** s199 left it owed on gate-based evidence. **A KILL-SWITCH IS NOT AN ABLATION** — a gate bypasses the *body*, not the *commit* (RTX-3b also touches `rtx_abi.inc`). Rebuilt **pristine `origin/main` with the commits ABSENT**: still `rc=139`. Cannot be an RTX regression; **RTX-3b is not blocked on it.** Open pattern-family fault, ζ-owned.
**NEXT RUNG:** merge/push `rtx-3b-s200`, then **RTX-5 (AGG)** — step 0(a)(b)(c)(d) already done in its rung, including the integer-key stringify+hash+strcmp+alloc **fork that needs Lon's ruling**. Do not re-derive either. ⚠ `rt_subscript_var` is in `pattern_match.c` — ζ's active file AND the home of the 47 failures; check their cursor first.
**⭐⭐ RTX-5 STEP 0 VERIFIED (NOT re-derived) — AND IT FOUND A SIXTH PHANTOM CLASS: THE ALREADY-PORTED SYMBOL.** `rt_subscript_var`@`pattern_match.c:1000` and `rt_field_var`@`:1042` verify **byte-exact** (inherited claims are not always stale — say so when they hold). But ⛔⛔ **`rt_deref` IS ALREADY HAND-WRITTEN ASM** (`rt/rt_asm_helpers.S:8`, via `nm` over 240 objects). **ROOT CAUSE IS STEP 0's OWN GREP: no `--include=*.S`, so an already-ported symbol reads as a phantom** — s188 blamed the real-but-unrelated non-UTF-8 `core.c` next door. **FIXED IN ARCH §7: step 0(e) minted + the grep corrected at its point of use.**
**⇒ RTX-5 IS A THIRD SMALLER THAN ITS RUNG READS.** `rt_subscript_var` 5.0M = the real target · `rt_deref` 2.5M = **already done** · `rt_field_var` 0 sites in `table_access`. Static sites in `table_access.s` are 2:1, matching s188's dynamic 5,001,000:2,500,500 exactly — **step 0(d) was sound; only the target list was wrong.** ⭐ Most dangerous class yet: a re-port would have SUCCEEDED, batteries green, board unmoved, misread as "AGG asm doesn't help."
**⛔ SIX RUNTIME SYMBOLS ARE UNGATED ASM, OUTSIDE THE RTX CONTRACT ENTIRELY:** `rt_deref`·`to_int` (`rt/rt_asm_helpers.S`), `rt_sg_scan_member`·`rt_sg_scan_nonmember`·`rt_sg_member` (`rt/rt_sg_scan.S`), `rk_gram_enter_box` (Raku). No kill-switch, no battery, no gate. **⚠ THIS QUALIFIES THIS SESSION'S OWN EARLIER LINE, FLAGGED AGAINST MYSELF: "all four gates OFF ⇒ byte-identical" is TRUE AS STATED for the four RTX families, but "all `SCRIP_RTX_*` off" ≠ "pristine C runtime." Six symbols stay asm in both arms.**
**⭐⭐ THE AGG FORK IS NOW ANSWERABLE BY EXPERIMENT INSTEAD OF RULING.** The fork reduces to one empirical question — *what does asm-ceremony alone buy on an AGG leaf?* **That control already exists and we simply cannot read it: `rt_deref` is an AGG leaf, hot at 2.5M in the very benchmark RTX-5 is graded on, already ported in the identical fast/slow idiom — and ungated.** ⇒ **RECOMMENDED: RTX-5a FIRST — bring `rt_asm_helpers.S` under the contract (gate + `c_*` fallbacks + differential battery + two-sided falsification). Owed anyway per the hole above; then measure `rt_deref` ON/OFF on `table_access` (1324ms window, clears the 800ms floor) and let Lon rule with a NUMBER.** If it moves like `var_access` (1.37×) option (i) port-as-is is vindicated; if it moves like the AGG null (1.00–1.04) the ceremony is worthless and only layout/itoa-hash-fusion can pay. ⭐ **State that expected board in advance — a ~1.00 result is THE ANSWER, not a failed measurement.** ⚠ Bonus: `rt_asm_helpers.S` is the one piece of this rung **outside `pattern_match.c`**, so it cannot collide with the ζ session.
**⭐⭐ RTX-5a LANDED (SCRIP `94452610`) — `rt_asm_helpers.S` IS NOW UNDER THE CONTRACT, GATE `SCRIP_RTX_LEAF`.** `rt_deref`+`to_int` gated after VERIFYING both C fallbacks are COMPLETE (`rt_deref_slow` pattern_match.c:1173 covers all four inlined shapes + cold table/varref; `to_int_slow` core/core.c:1991 covers DT_I + coercions) — that is what makes a 2-instruction gate sufficient. ⭐ **NAMED `LEAF` NOT `AGG` DELIBERATELY: s188's null control works BECAUSE no `SCRIP_RTX_AGG` gate exists; adding one would have silently destroyed the ±3% floor it establishes.** GATES: crosscheck LEAF=1 and LEAF=0 both **268/47 · 267/46, identical to each other and byte-identical to baseline** · unit 21/21 · alloc 36/36 · STR 8426/0 · **TWO-SIDED FALSIFICATION BOTH DIRECTIONS: broken asm ⇒ LEAF=1 247/68 (21 movers, the asm executes) and LEAF=0 268/47 (exact baseline, the gate reaches C).**
**⛔⛔ THE AGG FORK IS STILL UNANSWERED — AND THE BLOCKER IS STRUCTURAL, NOT NOISE. DO NOT RE-ATTEMPT IT HERE.** Four harness runs on `table_access`: **1.340 · 2.497 · 2.606 · 0.510 — THE ARMS SWAP RANKING**, independently reproducing this cursor's own `table_access` warning. ⚠⚠ A naive single back-to-back pair beforehand read **4.3× SLOWER**; the harness then read faster. **Two fabricated numbers in opposite directions were one commit away — the R=5 interleaved-median harness is not optional on this box.** ⭐ **THE REAL FINDING: `rt_deref` has ZERO emitted call sites in EVERY reliably-measurable benchmark** (`var_access` `func_call` `string_manip` `arith_loop` `eval_fixed` `roman` all 0). It is reachable only from `table_access` (unstable) and `mixed_workload` (**segfaults on PRISTINE main — verified, pre-existing, same class as `string_pattern`**). ⇒ **The AGG-leaf measurement is unavailable in this container BY CONSTRUCTION: what exercises the symbol is broken or unstable, and what is stable never calls it.**
**⇒ NEXT RUNG RECOMMENDED — RTX-0c (INSTRUMENT, in the RTX-0b tradition): ADD A RELIABLE AGG BENCHMARK to `corpus/benchmarks/snobol4/`** — integer-keyed table churn, self-timed `ms:` window ≥800ms at N, no pattern matching (the 47 failures and both segvs are pattern-family). **Only then is Lon's fork decidable on evidence.** The gate is no longer the blocker; the corpus is. ⚠ Until then the fork should not be decided on opinion.
**`handoff_status.sh` is the push truth — not this block.**

---

## PRIOR CURSOR — s199 (2026-07-27): **⚠ ITS PUSH-STATUS AND BOARD CLAIMS ARE BOTH SUPERSEDED (see s200 above). The port itself was real, correct, and survived. Retained for the reconstruction and vacuity lessons.**
**⛔⛔ s190's RTX-3b WAS LOST — ITS CURSOR SAYING "LANDED" WAS FALSE BY THE TIME IT WAS READ.** `a5ff0a9d` is not a valid object in a fresh clone; no `rtx-3b-s190` on origin; `rtx_str.S` had zero `SNUL` refs. s190 withheld the push to avoid "burying the port in someone else's breakage" — **a false tradeoff: a BRANCH push cannot pollute `main`.** RULES.md line 54 predicted it verbatim. **The FINDING doc survived while the code did not, so the prose asserted work that no longer existed.** Recovery was cheap ONLY because this rung carried the full spec — guard order, return convention, exact `mov`s. **A well-written rung is a backup.**
**⭐⭐ RELANDED — SCRIP branch `rtx-3b-s199`: `c5ee5f0b` (port) + `1f0e53cc` (tag-drift `_Static_assert`s + measurement finding). NOT PUSHED — credential needed. DO NOT REPEAT s190: push the branch.**
**⭐ MEASURED, RELIABLE, REPRODUCED IN TWO LOAD WINDOWS:** `var_access` **1.391×** (1229/1709) then **1.396×** (1360/1899) — absolute times drifted ~10% with load, **the ratio held**. `func_call` **1.089×** (8588/9356) — **s190 could not measure this at all** (DEFINE was broken); **debt discharged**. RT_OPT=-O0, m3, N×4 to clear the 800 ms floor, ON==OFF byte-identical ex-timing.
**⛔ `string_manip`/`table_access` ARE NOT RELIABLY MEASURABLE HERE (`nproc=1`, rclone contending): 4× swings on identical config, arms swap ranking mid-series. Min-statistic ~1.098× for `string_manip`; variance exceeds effect. NO CLAIM MADE.** ⭐ The control that makes this sayable: `var_access` re-measured tight **in the same noisy window** ⇒ instability is a property of the PROGRAM, not the moment.
**⭐ CANDIDATE (NOT A CAUSE) FOR s188's OPEN ITEM:** s188's puzzling `string_manip` 1.279× may be this same instability rather than the suspected SXT mechanism. **NOT established — re-run `bench_sno_rtx.sh` in a quiet window before crediting either.**
**⭐⭐ NEW VACUITY MEMBER — THE UNREPRESENTATIVE REPRO.** The p.22 hand-repro agreed across SPITBOL oracle, RTX ON and RTX OFF **and was worthless**: inverting the port changed nothing because a source literal `''` arrives as `DT_S`/`slen==0`, NOT `DT_SNUL`. The `DT_SNUL` arm comes only from RUNTIME nulls. **Fourth member: s184 · s186 · s187 · s199.** STANDING RULE: **a hand-written repro is a HYPOTHESIS about which path runs — confirm with a counter before trusting agreement.**
**⭐ TAG CENSUS (LD_PRELOAD, §7 step 0d): `var_access` 10.0M · `func_call` 10.0M · `string_manip` 5.0M · `table_access` 5.006M — ALL `DT_SNUL + DT_I`.** The idiom is `N = LT(N,lim) N + 1`, where p.22's rule keeps `N` an INTEGER instead of degrading it to a string.
**⭐ BATTERY 8404→8426, 0 mismatches.** s190 was right that `SNUL+I` was the hole (symmetry made it look covered). Added it + guard-precedence cases. **Proven live: inverting the a-arm turns it RED.** Same for the new asserts: flipping `DT_X` to 14 fails the build. **Every instrument added was falsified before being trusted.**
**⭐ `DT_X` WAS ABSENT FROM `rtx_abi.inc` ENTIRELY** (not "documented but undefined" as s190 recorded). Added, plus the `_Static_assert`s s190 deferred — landed because this session hand-added a tag, which is exactly the drift they guard.
**⛔ BOARD 221/94 · 219/94 · DIVERGE=1 — NOT the s188 watermark (314/1), 93 pattern programs down. RTX RULED OUT MECHANICALLY: identical with every RTX gate off, and identical before/after the port.** Cause is the parallel ζ session's s198 stored-pattern segv. ✅ s190's DEFINE-null-args blocker is FIXED and is a different fault.
**⛔ STILL OWED:** `string_pattern` non-regression from 2.016× **UNDISCHARGED** — segfaults under BOTH arms including `SCRIP_RTX_STR=0`, which never enters the asm. Pre-existing, pattern-family. Not waived.
**NEXT RUNG:** push `rtx-3b-s199`, then **RTX-5 (AGG)** — step 0(a)(b)(c)(d) already done in its rung, including the integer-key stringify+hash+strcmp+alloc **fork that needs Lon's ruling**. Do not re-derive either.
**FINDING: `FINDING-2026-07-27k-CLAUDE-SN4-RTX-3B-RECONSTRUCTED-AFTER-SANDBOX-LOSS-AND-THE-UNREPRESENTATIVE-REPRO-CLASS.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## PRIOR CURSOR — s190 (2026-07-27): **⛔ ITS "RTX-3b LANDED" CLAIM IS VOID — THE COMMIT NEVER REACHED ORIGIN AND THE WORK WAS LOST. RELANDED s199. Retained below for the falsification/measurement lessons only.**
---

## PRIOR CURSOR — s188 (2026-07-27): **RTX-0b LANDED AND TWO-SIDED VALIDATED — THE LADDER CAN MEASURE ITSELF FOR THE FIRST TIME. SLICE 3's TARGET WAS COLD; RE-TARGETED. NEXT: RTX-5 (AGG), STEP 0 ALREADY DONE BELOW.**
**✅ WATERMARK RE-PROVEN LIVE BEFORE AND AFTER: m3 314/1 · m4 312/1 · DIVERGE=0**, single fail `test_case` both modes — exactly s187. **No runtime code was ported this session, so nothing could regress; SCRIP's only change is one NEW script.**
**⭐⭐ RTX-0b LANDED (SCRIP `4f634c28`, `scripts/bench_sno_rtx.sh`) — THE INSTRUMENT WAS THE BLOCKER.** The five rail demos exercise NO ported family (N=64 `claws5-match`: `rt_gcheap_alloc` 2 · `str_concat_d` 2 · AGG/ARITH 0 · `rt_call_arr` 8 and FLAT N=1→64 while compute_ms scales 2→12). New harness does `SCRIP_RTX_<FAM>`=1 vs =0 **on ONE binary**, over programs chosen by MEASURED dynamic counts, with the sub-800ms variance lesson promoted from footnote to **hard gate (ratio SUPPRESSED)**, plus free ON/OFF output-identity checking.
**⭐ VALIDATED TWO-SIDED — the RTX-1 falsification discipline applied to the INSTRUMENT: NULL (AGG, unported) 1.036 / 1.003 ⇒ ±3% floor at R=3. POSITIVE (STR, landed) `string_pattern` 2.016×.**
**⭐⭐ THE LADDER'S FIRST REAL SPEED NUMBERS** (RT_OPT=-O0, mode 3, R=3 medians) — every prior rung read "⚠ NO SPEED CLAIM": **STR** `string_pattern` **2.016×** · `string_manip` **1.279×** — **CALL (sl.1+2)** `roman` **1.491×** · `string_manip` **1.439×** · `eval_fixed` 1.006.
**⛔ RTX-4 SLICE 3 NEVER RAN — ITS TARGET IS COLD.** `rt_call_arr` passes step 0(a)(b)(c) and is called **8 times, flat across N**. 232 static refs count emitted call SITES, exactly as §5 caveat (a) warned at RTX-2 and was then not applied. **RE-TARGETED** (see rung) to `rt_call_arr` **fused with** `try_call_builtin_by_name` — 1:1 in all 16 benchmarks — gated on `string_manip`/`eval_fixed`/`roman`, never the rail.
**⭐ STEP 0(d) MINTED — PROVE THE SYMBOL EXECUTES IN THE WINDOW YOU INTEND TO MOVE.** Fourth phantom-family member, first LIVE one: RTX-2 DEAD · RTX-3 INVENTED · s165 MISRECORDED · **s188 REAL, CORRECT, COLD.** Existence is not correctness (s165); **correctness is not relevance (s188).** Rung-level form of s187's probe rule: *if the board would look identical with the port absent, the RUNG is vacuous.*
**⚠ TWO OPEN ITEMS, MEASURED BUT NOT EXPLAINED — DO NOT ASSERT A CAUSE:** (1) `func_call`/`var_access` make **10.0M `str_concat_d` calls each** yet move 1.001/1.018 under the STR gate while `string_pattern` (5.5M) moves 2.016× — **call count does not predict benefit**; likely the two-plain-strings fast path missing, and **widening it may beat any new family.** (2) The CALL differential unit battery owed since s187 is **still owed**.
**⚠ ENVIRONMENT (extends s187): `perf`, `valgrind`, `ltrace`, `strace` AND `gdb` are ALL absent.** Substitute = `LD_PRELOAD` counting interposer (`/home/claude/rtxprof.c`, session tool, NOT committed, NOT a gate).
**⭐⭐ NEXT RUNG RECOMMENDED: RTX-3b — WIDEN THE STR FAST PATH TO `SNUL ⊕ x`.** Measured, spec'd with the exact guard order derived from `c_str_concat_d`, and with its expected board stated in advance so a null result falsifies it. `SNUL+I` is the most common concat in idiomatic SNOBOL4 (the `LT(...)` loop-guard idiom) — **25M calls across four benchmarks, ALL on the slow path**, doing a full `-O0` prologue + `rt_gc_point_arr` + 6 tag compares in order to return an argument unchanged. Cheaper than any new family and likely the largest STR win left.
**THEN RTX-5 (AGG) — step 0(a)(b)(c)(d) ALREADY DONE in its rung, including a fork that needs Lon's ruling** (the integer-key stringify+hash+strcmp+alloc path: port as-is for `-O0` ceremony only, or promote the layout rung first). Do not re-derive either.
**FINDING: `FINDING-2026-07-27d-CLAUDE-SN4-RTX-4-SLICE3-CALL-ARR-IS-COLD-AND-THE-RAIL-DOES-NOT-EXERCISE-ANY-PORTED-FAMILY.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## PRIOR CURSOR — s187 (2026-07-27): **UNPARKED BY LON. SLICE 1's OWED PROBE DISCHARGED. SLICE 2 LANDED. CALL GATE NOW DEFAULT ON.**

---

## PRIOR CURSOR — s187 (2026-07-27): **UNPARKED BY LON. SLICE 1's OWED PROBE DISCHARGED. SLICE 2 LANDED. CALL GATE NOW DEFAULT ON.**
**UNPARK DIRECTIVE OF RECORD (Lon, s187): *"Replace SCRIP SNOBOL4 runtime C with asm. Continue."*** RTX was PARKED s171–s186; it is now RUNNING.
**✅ WATERMARK RE-PROVEN LIVE AT SESSION START, BEFORE ANY EDIT (21s): m3 314/1 · m4 312/1 · DIVERGE=0**, single fail `test_case` both modes. Held at EVERY gate below, at the new default AND under `SCRIP_RTX_CALL=0`.
**✅ THE s186 DEBT IS PAID.** Slice 1's falsification probe ran first, as instructed: broken asm ⇒ m3 **310/5** · m4 **308/5**; gate OFF ⇒ watermark. The asm executes, the switch switches — and the probe **measured** the coverage set (exactly the 4 EVAL/defer programs) that two sessions had only asserted.
**✅ RTX-4 SLICE 2 LANDED** (SCRIP `881ea03d`): `rt_proc_call_epilogue_slim_γ/ω` in asm, self-contained. γ **93→63** insns, ω **98→61** (SIZE proxy, `RT_OPT=-O0`, ⛔ NO speed claim — no rail run).
**✅ BOTH LANDINGS INDEPENDENTLY FALSIFICATION-PROVEN:** γ broken ⇒ 296/19 (**18 movers** — 4.5× slice 1, confirming this is where ordinary `DEFINE` calls go); ω broken ⇒ 310/5 (**4 movers**).
⚠⚠ **THE ω PROBE HAD TO INVERT, NOT MERELY CORRUPT — READ THE FINDING BEFORE WRITING ANY FUTURE PROBE.** Breaking ω to return `DT_FAIL` is a **no-op** (it is the FRETURN landing; it already returns FAILDESCR). That probe would have sat at 314/1 and been misread as "ω is unreachable," shipping an unexercised landing under a green board. **THIRD member of one family: s184 (failed compile scored as clean 0) · s186 (`expr_eval` stdin, zero bytes passing vacuously) · s187 (a break that is a no-op at its own site).** STANDING CHECK: *state what the output would look like if the thing under test did not exist at all; if that equals the passing output, the probe is vacuous.*
**✅ GATE DEFAULT FLIPPED OFF→ON** (SCRIP `f1262de7`) per ARCH §4. Kill-switch **byte-identical over all 315 programs**. ⚠ RESIDUAL RISK NAMED: the monitor arm is **untaken in every battery** (monitor dark since s158) — the green board says NOTHING about it; MON-RE must re-gate this family.
**✅ `__builtin_frame_address(0)+16` — s186's "principal risk" — IS RETIRED, AND WAS ALREADY SOLVED IN SLICE 1.** It is NOT frame-size dependent: under the standard prologue it is `rbp+16`, the caller's pre-`call` rsp, whatever `sub rsp,N` follows. `rtx_call.S:78-80` has said so since s165. **DUAL OF s186's OWN RULE: a risk recorded in a cursor is a claim about the moment it was written too — re-read the implementation before re-litigating a flagged risk.**
⭐ **NEW HAZARD FOR EVERY SELF-CONTAINED PORT:** `Σ`/`Σlen`/`g_monitor_bin` are EXPORTED (`nm` capital B) ⇒ preemptible ⇒ need `@GOTPCREL` (the RTX-2 `g_hp_fr` failure); `g_pcall`/`g_pcall_top`/`rt_k_level` are hidden ⇒ rip-direct. Slice 1 was immune only because it CALLED into C. **Add the `nm` linkage check to §7 step 0.**
⭐ **OFFSETS ARE NOW LOCKED IN THE BUILD, NOT IN PROSE.** `rt.c` `_Static_assert`s: `sizeof(rt_pcall_t)==64`, `p/save_Σ/save_Σlen/wn/fb/vtmark`, `rt_proc_t.name==0`, `is_generator==0x4c`. Layout drift fails the COMPILE. (Line numbers rotted AGAIN — slim landings are `rt.c:1156/1168`, `rt.c` is 1734 lines not 1667; `rt_nret_fix` held at `:631`. **Cite symbols, never line numbers.**)
**NEXT RUNG: RTX-4 SLICE 3 — the rest of the CALL surface**, `rt_call_arr` FIRST (**232 static refs, #1 in the whole runtime**) and worth its own slice; then `rt_proc_call_open(_slim)` · `rt_proc_open_fn` · `rt_call_named_proc` · `rt_arg_stage` + proc_set family.
**ALSO OWED BY THIS FAMILY: a CALL differential unit battery.** MISC/ALLOC/STR each have one in `test_rtx_unit.sh`; CALL has NONE, so its evidence rests entirely on corpus movement. Needs a synthetic `g_pcall` record — and it is the only way to reach the monitor branch while the monitor is dark.
**FINDING: `FINDING-2026-07-27c-CLAUDE-SN4-RTX-4-SLICE2-SLIM-LANDINGS-AND-THE-NO-OP-PROBE-CLASS.md`.**
**`handoff_status.sh` is the push truth — not this block.**

---

## ⚠ CONCURRENCY CONTRACT — RTX ‖ ζ-STORAGE (minted s186)

RTX and the ζ-storage ladder are designed to be safely parallel, but **only for phase-1 rungs.** Know which side of the line you are on.

**SAFE — RTX phase 1 (RTX-1 … RTX-10) touches ZERO templates BY CONSTRUCTION** (the ladder's own EVERY-RUNG line). Phase 1 keeps every exported symbol's EXACT current C signature; speed comes from bodies, not convention. Consequence: **no `.s` regen owed, no both-medium work, no `x86_asm.h` edit.**

| | RTX owns | ζ-storage owns |
|---|---|---|
| files | `src/runtime/rtx/*.S`, `rtx_abi.inc`, `rtx_init.c`, `src/runtime/rt/*.c`, family C sources (`gc_heap.c`, `string_ops.c`, `rt.c`, …) | `src/contracts/zeta_storage.c`, `src/emitter/emit.cpp`, `src/templates/*.cpp`, `x86_asm.h` |
| scripts | `test_rtx_unit.sh`, `util_rtx_symbol_inventory.sh`, `test_gate_rtx_inventory_live.sh` | the FC gate + `util_fc_conversion_map.py` + spine census |

**⛔ NOT SAFE — SERIALIZE WITH LON:**
- **RTX-11 (RTX-CC)** — registerized ABI changes template call sequences via `x86(...)` encoders in `x86_asm.h`, and **fires `.s` regen ×3**. That is the ζ ladder's own territory. **RTX-11 must not run concurrently with ZB-VAL/ZLS work.**
- **RTX-12 (eradication)** — deletes `c_*` bodies and gates; re-gates on the full crosscheck. Land it when the board is quiet.
- **`Makefile` link lines** — both ladders may add objects. Small, but a real rebase point.
- **THE WATERMARK ITSELF IS SHARED STATE.** Both sessions gate on the same crosscheck numbers. If one side moves the watermark mid-flight, the other's "no regression" claim is measured against a baseline that no longer exists. **Re-prove at session start; say the numbers out loud in the FINDING.**
- **RTX-7 (NV/GVAR)** — `NV_SET_fn` is DB-1's planned write-barrier choke point. Coordinate or land DB-1 first.
- **RTX-8 (MATCH SINKS)** — touches `pattern_match.c`, which the SN4-PAT / match-family rungs also reach. Check the other session's cursor before starting.

Per RULES.md s47 ⚠ CONCURRENCY: parallel sessions are safe for CODE (git rebases cleanly), and **`PLAN.md`'s Step column is stale BY DESIGN** — orientation trusts THIS file's `LIVE CURSOR`, never PLAN.md's table.

---

## Session Setup

```bash
git config --global user.name "LCherryholmes"
git config --global user.email "lcherryh@yahoo.com"
git clone https://github.com/snobol4ever/.github.git /home/claude/.github
git clone https://github.com/snobol4ever/SCRIP.git   /home/claude/SCRIP
git clone https://github.com/snobol4ever/corpus.git  /home/claude/corpus
git clone https://github.com/snobol4ever/x64         /home/claude/x64   # SPITBOL oracle
bash /home/claude/SCRIP/scripts/install_system_packages.sh
cd /home/claude/SCRIP && rm -f scrip && make -j4 scrip && make libscrip_rt
```

**READ-FIRST SET (NON-NEGOTIABLE, in this order):**
1. `RULES.md` in full — especially MONITOR-FIRST, TEMPLATE-ONLY EMISSION, and the HANDOFF-COMPLETE gate.
2. **`ARCH-SNOBOL4-RTX.md`** — the register/ABI/build contract. §7 is the per-rung checklist; §7 steps **0 / 0b / 2b** are the three hard-won ones.
3. The family's C source **in full**, then its `sbl.asm` counterpart for shapes (steal idioms, never structures).
4. **The SPITBOL manual chapter for every construct the family implements** (Lon standing directive). **CALL = Ch.8 `Program-Defined Objects`** (DEFINE, RETURN, FRETURN, the prototype string, local-variable save/restore, by-name args) **+ Ch.9 `Advanced Topics`** (NRETURN, run-time compilation). ⛔ **NOT Ch.13 — that is `Running SPITBOL`, command-line options.** s165 recorded this correction INSIDE a rung and never fixed the instruction, so three more sessions were pointed at the wrong chapter; the rung is now gone and the fix lives HERE, where it is actually read (s187). Verified against the manual's own TOC: Ch.8 p.101, Ch.9 p.121 (NRETURN p.133), Ch.13 p.161 · ARITH = mixed-mode coercion · MATCH = Ch.9 deferred evaluation + p.122 well-foundedness + p.125 FENCE.

**Verify the watermark BEFORE touching anything**, then run the ladder's first `- [ ]`.

---

## ⭐⭐ SN4-RTX — TOTAL RUNTIME REDESIGN IN OPTIMIZED x86 ASM (Lon PIVOT directive, s162 2026-07-25)
**RULING OF RECORD (promotes the s161 caveat to head rung):** the C runtime is a TRANSITIONAL ARTIFACT. The target SNOBOL4 runtime is hand-written, register-aware x86-64 assembly. We use REGISTERS freely; System V conventions bind ONLY at (a) libc call boundaries and (b) the m3 driver→blob entry edge. References: **SPITBOL x64 asm runtime** (`/home/claude/x64` — Minimal registers pinned to hard regs: IA=r12 W0=rax WA=rcx WB=rbx WC=rdx XL=rsi XR=rdi CP=r13 RA=xmm12; C-boundary = save-all-to-global-block/call/restore, `int.asm`; the runtime itself = `bootstrap/sbl.asm`, 19,064 lines) · **CSNOBOL4** C runtime (semantics reference; `git clone https://github.com/snobol4ever/csnobol4`, build via `scripts/build_csnobol4_oracle.sh`) · **SIL** macro source (algorithm reference — locate/fetch at need).
**RTX-0a MEASURED SURFACE (s162, static call sites in `claws5.s` — matches the s161 dynamic anatomy):** `str_concat_d`×38 · `rt_subscript_var`×33 (TABLE) · `rt_deref`×24 · `rt_call_arr`×22 + `rt_proc_call_epilogue_`×14 + `rt_proc_open_fn`×8 (the NRETURN/call path) · `rt_num_arith`×8 · `rt_binop_overload`×8 · `rt_defer_step`/`rt_dcap_step`×6 each · `rt_coerce_num2_d`×6. Current C runtime = **~21k LOC** (`src/runtime/`: top-level model ~12.6k, `rt/` 4.1k, `core/` 3.6k, `builtins/` 0.7k).
- [x] RTX-0 **SURVEY + CONTRACT + RULINGS — DONE s162.** Surface measured (above + full ~110-symbol inventory in `ARCH-SNOBOL4-RTX.md` §5); `int.asm` boundary protocol digested; **all four forks RULED (Lon s162): (1) Intel syntax project-wide = GNU `as` + `.intel_syntax noprefix` (what `--compile` already emits — one assembler, one syntax); (2) libc-only at the C boundary; (3) dual-build per-family kill-switch → eradicate after proof; (4) phase-1 blob pins UNCHANGED (rbx=arena mint top · r12=cond-assign stack ptr, own mmap area · r13/r14/r15=Σ/δ/Δ subject · rsp/rbp=ζ+C — note r12 correction vs REGISTER-LAYOUT.md), RTX owns the volatile nine internally.** ⭐ KEY FACT (descr.h:28): DESCR_t = 16B = two INTEGER eightbytes ⇒ **SysV already passes/returns it in a register pair (rdx:rax)** — phase-1 ports keep C signatures and get descriptor-in-registers FOR FREE; zero template churn until RTX-CC. **THE CONTRACT IS `ARCH-SNOBOL4-RTX.md` — read it FIRST, every RTX session, before the family's C source, its `sbl.asm` counterpart, and the SPITBOL-manual chapter for every construct the family implements.**
- [x] RTX-1 **SCAFFOLD + FIRST PORT — LANDED s162 (SCRIP `416190f5`), DEFAULT ON.** `src/runtime/rtx/`: `rtx_abi.inc` (contract macros: pins, DESCR ABI, `RTX_FUNC`/`RTX_GATE`/`RTX_GATE_DEF`/libc-align), `rtx_misc.S` (ports `rt_faildescr`, `rt_is_truthy`), `rtx_init.c` (`__attribute__((constructor))` reads `SCRIP_RTX_<FAM>` — ZERO edits to existing init paths), `rtx_unit_test.c`; `scripts/util_rtx_symbol_inventory.sh`.
  **⭐ ABI VERIFIED EMPIRICALLY, NOT ASSUMED (gcc -O1 probe): arg pair `rdi=(slen<<32)|v`, `rsi=value`; return pair `rax=(slen<<32)|v`, `rdx=value`. `edi`/`eax` alias the tag directly.** DESCR_t layout confirmed by offsetof: 16B, v@0, slen@4, value@8.
  **⭐ BUILD FACT THAT SIMPLIFIED EVERYTHING: `scrip` links DYNAMICALLY against `libscrip_rt.so` (Makefile:343, Lon 2026-07-25) ⇒ ONE runtime copy serves BOTH modes ⇒ add `.S` to `RT_PIC_SRCS` and both modes get it; gate bytes are `.hidden` so `[rip+sym]` is direct and interposition-proof.** Makefile already had a `%.S` rule + vpath, so `.S` (cpp-preprocessed) was a zero-friction insertion.
  **⭐ TWO IEEE TRAPS FOUND AND HANDLED IN `rt_is_truthy` — the reason a bit-test would have been WRONG: `-0.0` is bitwise NONZERO but `-0.0 != 0.0` is FALSE (so falsy), and `NaN != 0.0` is TRUE (so truthy). Implemented as `ucomisd` + `setne` OR `setp`. Both are in the unit battery.**
  **GATES (all green): differential unit 21/21 asm-vs-C incl. both IEEE traps · smokes 7/7 × 2 modes · crosscheck m3 314/1 · m4 309/4 · DIVERGE=3 = THE s161 WATERMARK EXACTLY (same three `21{4,5,6}_indirect_goto`) · `SCRIP_RTX_MISC=0` crosscheck output byte-identical to ON.**
  **⭐⭐ FALSIFICATION GATE (the proof that matters, and the pattern EVERY later rung must repeat): a passing differential test CANNOT distinguish "asm is correct" from "gate is stuck OFF and C did the work." So the asm body was deliberately broken (`mov eax,42` for DT_FAIL) and rebuilt: gate ON ⇒ test FAILS (asm=42 vs C=99) — proves the asm really executes; gate OFF ⇒ test PASSES — proves the switch really switches. Then reverted and re-gated. DO NOT land an RTX family without this two-sided proof.**
  **⚠ NEW ENVIRONMENT DEFECT: THERE IS NO `gdb` IN THIS CONTAINER** (`which gdb` empty). RULES' first ABSOLUTE RULE (monitor→bracket→gdb hit-count hunt) has NO working tool here — the monitor is dark (s158) AND now the gdb fallback is absent AND `probe.py` does not exist. Differential/falsification testing is the working substitute; record this in MON-RE.
  **⭐ MECHANICAL INVENTORY (`util_rtx_symbol_inventory.sh`, 33 artifacts, 175 runtime symbols) CONFIRMS THE LADDER ORDER — top static call sites: `rt_call_arr` 232 · `rt_proc_call_epilogue_` 178 · `str_concat_d` 152 · `rt_proc_open_fn` 138 · `NV_GET_fn` 105 · `rt_proc_call_epilogue_slim_` 98 · `rt_proc_set_frame_bytes`/`rt_proc_set_fn` 94 · `rt_proc_register` 79 · `rt_num_arith` 76. THE CALL FAMILY DOMINATES BY A WIDE MARGIN — independent static confirmation of s161's dynamic claws5 NRETURN finding.** Run this script at the start of every RTX rung and diff against ARCH §5; the doc table is a snapshot and will rot, the script is ground truth.
  ⚠ HONEST SCOPE: RTX-1 is a PLUMBING proof, not a speed rung — `rt_faildescr`/`rt_is_truthy` are nowhere near the hot path. **Do not quote a rail number off it.** First real speed evidence arrives at RTX-3/RTX-4.
- [x] RTX-2 **ALLOC — LANDED s163, DEFAULT ON.** ⛔ THE RUNG AS WRITTEN NAMED DEAD SYMBOLS: `blk_alloc`/`blk_free` have ZERO live call sites (C in `archive/backend/`, unbuilt); they appeared only because the inventory script swept 5 legacy `scrip-cc -asm` artifacts + 2 macro libraries alongside the 26 live `.intel_syntax` ones. Script fixed, ARCH §5 corrected, ladder order unaffected. **PORTED (`src/runtime/rtx/rtx_alloc.S`, gate `SCRIP_RTX_ALLOC`): `rt_gcheap_alloc` + `rt_str_alloc` DETAX FAST PATH ONLY** — armed + room at the frontier + carve at/above the HP-2 `virgin` high-water so no memset is owed; 21 instructions, no call, no memset. Init/GC/fill-window/memset-owing/histogram/exhaustion all tail-jump to `c_rt_gcheap_alloc` with edi/rsi untouched. `rt_str_alloc` ELIMINATES the histogram call (armed already encodes it off). ⭐ `virgin`+`zfull` promoted into the PL-SINK-3 exported `g_hp_fr` cell, APPENDED (offsets 0/8/16/24 are baked by `bb_call_fn.cpp`'s `sink_carve48` and must not move; new `_Static_assert`s make a layout error fail the BUILD). ⭐ `g_hp_fr` is exported⇒preemptible⇒`@GOTPCREL`, not `lea [rip+sym]` (the one link failure). GATES: differential 21/21 misc + 36/36 alloc · falsification BOTH SIDES (broken asm: ON⇒28/36 mismatch, OFF⇒0) · crosscheck 314/1 309/4 DIVERGE=3 · kill-switch ON==OFF md5 `0cc89834...` · smokes 7/7×2×both gates · Prolog 4/0 · Icon 4/0 · beauty ON==OFF. NOT RUN: Milestone-1 beauty ORACLE md5 (needs x64 clone). ⚠ NO SPEED CLAIM — see FINDING §5. ⭐ NEW: `scripts/test_rtx_unit.sh` runs every family's differential battery (RTX-1's was never wired to a script).
- [x] RTX-3 **STRINGS — LANDED s164, DEFAULT ON** (`src/runtime/rtx/rtx_str.S`, gate `SCRIP_RTX_STR`). ⛔ **THE RUNG AS WRITTEN NAMED DEAD SYMBOLS AGAIN — SECOND TIME IN TWO RUNGS:** `rt_concat`, `rt_lcomp`, `rt_acomp` are declaration-only in `rt/rt.h:69/80/81` — no definition anywhere in the tree, zero call sites. RTX-2's phantoms came from the inventory script; these came from ladder PROSE. **Common factor: the symbol list was written from a DOCUMENT, not the tree.** ARCH §7 step 0 strengthened to require striking dead names in the discovering commit. **LIVE STR SURFACE = `str_concat_d` (152 static, #2 overall behind `rt_call_arr` 232)**; `memcmp` 106 is libc and stays; `rt_gvar_assign_str` 44 is NV (RTX-7). **PORTED: the TWO-PLAIN-STRINGS FAST PATH ONLY** — both operands DT_S with authoritative nonzero slen, non-null payloads, no collection pending, SXT token not owning the left operand; all TEN helper calls ELIMINATED (`rt_gc_point_arr`, `descr_to_str`×2, `VARVAL_fn`×2, `rt_sxt_match`, `memcpy`×2, `rt_sxt_note`); exact-bounds overlapping memcpy (GPR/SSE below 32, `rep movsb` above). ⭐ **THREE TRAPS:** (a) `slen==0` means LENGTH UNKNOWN (strlen), NOT empty, and `slen==0xFFFFFFFF` means CSET — **a CSET carries tag DT_S**; (b) the manual's null-operand rule is TYPE-PRESERVING (`(20-17) ''` → integer 3, not `'3'`, v3.7 p.22); (c) ⭐⭐ **`rt_sxt_note` cannot be inlined unconditionally, and getting it wrong PASSES EVERY CORRECTNESS TEST** — under-arming leaves every result byte-correct while silently returning every `S = S CH` append loop to O(n²); title checks reproduced faithfully and the token probed via exported `rt_sxt_match()` on all 8404 cases. ⭐ GC safety: `rt_gc_point_arr` is `if(!g_gc_pending)return;` AND the allocator never collects (only SETS the flag, gc_heap.c:212/217), so raw payload pointers cannot dangle across the carve. C-side: SXT statics → hidden `g_sxt_fr` cell (owner@0 len@8 gva_n@16 off@20) with 4 `_Static_assert`s; `off` uses RTX-2's `zfull` -1-unresolved convention. GATES: differential 8404/0 · **falsification BOTH SIDES (broken tag: ON⇒8359 mismatches, OFF⇒0)** · smokes 7/7×2 · **Icon 14/14×2 · Prolog 5/5×3 (mandatory — `gc_heap.c` is shared)** · crosscheck 314/1 309/4 DIVERGE=3 both gates · kill-switch md5 `0cc898346d1ad83e1b89d87a7342147a` = RTX-2's hash exactly · board ON==OFF 13/15 · beauty ON==OFF · **`.s` regen proven NOT owed** (regen script: "No changes"). ⚠ NO SPEED CLAIM (rail not run; RT_OPT=-O0). ⚠ Milestone-1 beauty ORACLE md5 still unreproduced — now WITH A REASON: the canonical invocation is recorded nowhere and `sbl -b beauty.sno` dies ERROR 217 + segfault; `beauty.sno` reads its subject from stdin. FINDING: `FINDING-2026-07-25-CLAUDE-SN4-RTX-3-STRINGS-PHANTOM-CLASS-AND-SXT-NOTE-SILENT-TRAP.md`. NOT ported: `str_repeat_d`, SIZE/TRIM/DUPL/REPLACE builtins.
- [x] RTX-4 **CALL SLICE 1 — LANDED s165, GATE `SCRIP_RTX_CALL` DEFAULT OFF.** `src/runtime/rtx/rtx_call.S` ports `rt_proc_call_epilogue_γ` (RETURN + NRETURN) and `rt_proc_call_epilogue_ω` (FRETURN). C bodies → `c_*`; `rt_proc_epilogue_body` de-static'd + by-pointer; `g_pcall`/`g_pcall_top`/`rt_k_level` hidden. Gates: watermark BEFORE and AFTER (m3 314/1 · m4 309/4 · DIVERGE=3) · smokes 7/7×2 · kill-switch md5-identical to pristine · falsification two-sided (ON⇒310/5·305/8, OFF⇒watermark). Three-arm size split: pristine C 342 B → by-pointer C 307 B → **asm 178 B**. ⛔ NO RAIL NUMBER — code size is a proxy and is labelled as one.
- [x] **RTX-4 SLICE 2 — LANDED s187** (SCRIP `881ea03d`), and **RTX-4 CANARY DISCHARGED same session.** `rt_proc_call_epilogue_slim_γ/ω` ported behind `rtx_gate_call`, C bodies → `c_*`; `rt_nret_fix` de-`static`'d + hidden (the sole real blocker). Self-contained — no `rt_proc_epilogue_body` call, so the asm SUBSUMES the body: k_level · pop (64-byte private copy as 4 SSE moves) · tidy · Σ restore · `rt_nret_fix` · monitor event. γ **93→63** insns, ω **98→61** (SIZE proxy, `RT_OPT=-O0`, ⛔ NO speed claim). GATES: watermark held at default-ON and under `SCRIP_RTX_CALL=0` · kill-switch **byte-identical over all 315 programs** · unit 21/21+36/36+8404/0 · smokes 7/7×2 · Prolog 189/0 · Icon 4/0 · Snocone 8/0 (⚠ **no-regression evidence for the shared `rt.c` edit ONLY — they do NOT move under the probe; citing them as asm evidence is a FALSE CLAIM**). **GATE DEFAULT FLIPPED OFF→ON** (`f1262de7`). ✅ Zero template edits ⇒ `.s` regen not owed; the emitted site still calls the symbol BY NAME and the asm merely takes it over ⇒ concurrency-safe vs the ζ ladder. FINDING: `FINDING-2026-07-27c-...-NO-OP-PROBE-CLASS.md`. See LIVE CURSOR for the three lessons (no-op probe class · the frame-bound risk was already retired in slice 1 · the exported-vs-hidden linkage split).
- [x] **RTX-0b — MEASUREMENT INSTRUMENT FIXED AND VALIDATED. LANDED s188** (`scripts/bench_sno_rtx.sh`). The five rail demos exercise NO ported family (measured N=64 `claws5-match`: `rt_gcheap_alloc` **2** · `str_concat_d` **2** · `rt_subscript_var`/`rt_deref`/`rt_num_arith` **0** · `rt_call_arr` **8 and FLAT N=1→N=64** while compute_ms scales 2→12). New harness measures the A/B the ladder actually needs — **`SCRIP_RTX_<FAM>`=1 vs =0 on ONE binary**, over `corpus/benchmarks/snobol4/` programs selected by MEASURED dynamic counts. Inherits the rail's discipline (self-timed `ms:` windows so startup is excluded exactly; ×4 N auto-ranging via the dominant `LT(VAR,INT)` bound, guarded to literals ≥1000 so recursion base cases like fibonacci's `LT(N,2)` are never scaled; R interleaved rounds; medians) and **promotes the 2026-07-24 variance lesson from footnote to HARD GATE: a window <`MIN_MS` (800) is reported `BOGUS-WINDOW` and its ratio is SUPPRESSED, not printed small.** Also gates correctness for free — ON/OFF output must be byte-identical or the run is fatal.
  ⭐⭐ **VALIDATED TWO-SIDED, THE SAME DISCIPLINE RTX-1 MINTED FOR PORTS, NOW APPLIED TO THE INSTRUMENT: NULL CONTROL — AGG (unported, no gate) ⇒ 1.036 / 1.003 ≈ 1.00, establishing a ±3% noise floor at R=3. POSITIVE CONTROL — STR (RTX-3, landed) ⇒ `string_pattern` **2.016×**, `string_manip` **1.279×**.** An instrument that could not see a landed port would be worthless; this one sees RTX-3 at 2×.
  ⭐ **THE LADDER'S FIRST REAL SPEED NUMBERS (RT_OPT=-O0, mode 3, medians of R=3, gate A/B on one binary).** Every prior rung said "⚠ NO SPEED CLAIM (rail not run)":
  | family | program | off/on |
  |---|---|---|
  | **STR** (RTX-3) | string_pattern | **2.016×** |
  | **STR** (RTX-3) | string_manip | **1.279×** |
  | **CALL** (RTX-4 sl.1+2) | roman | **1.491×** |
  | **CALL** (RTX-4 sl.1+2) | string_manip | **1.439×** |
  | CALL | eval_fixed | 1.006 |
  | AGG (unported, null) | table_access / mixed_workload | 1.036 / 1.003 |
  ⚠ **OPEN, MEASURED, NOT YET EXPLAINED — DO NOT ASSERT A CAUSE:** `func_call` and `var_access` each make **10.0M `str_concat_d` calls** and move **1.001 / 1.018** under the STR gate, while `string_pattern` (5.5M calls) moves **2.016×**. Call count alone therefore does NOT predict benefit. Leading hypothesis (UNVERIFIED): RTX-3 ported only the two-plain-strings fast path (both DT_S, authoritative nonzero slen, non-null payloads, no collection pending, SXT not owning the left operand), and these two programs' operands miss it — e.g. an integer operand, or `slen==0` meaning LENGTH-UNKNOWN. **If true, widening the fast path is a larger STR win than any new family.** Verify before acting.
- [ ] **RTX-4 SLICE 3 — THE REST OF THE CALL SURFACE.** ⛔⛔ **PREMISE FALSIFIED BY MEASUREMENT, s188 — THE RUNG AS WRITTEN NAMED A COLD SYMBOL.** It read: *"`rt_call_arr` FIRST AND ALONE: 232 static refs, #1 symbol in the entire runtime … the largest single remaining lever."* **`rt_call_arr` passes step 0(a)(b)(c) — live, byte-identically spelled, linkage correctly classified — and is CALLED 5/16/8/12 TIMES on the four rail demos, FLAT ACROSS N.** The 232 is a count of emitted call SITES (the CALL BOUNDARY), exactly as ARCH §5 caveat (a) warned in writing at RTX-2 and was then not applied. **FOURTH member of the phantom family and the first LIVE one: RTX-2 DEAD names · RTX-3 INVENTED names · s165 MISRECORDED name · s188 a real, correct, COLD name.** ⇒ **STEP 0(d) MINTED: PROVE THE SYMBOL EXECUTES IN THE WINDOW YOU INTEND TO MOVE, BEFORE PORTING.** RE-TARGETED, pending Lon's ruling: port **`rt_call_arr` FUSED WITH `try_call_builtin_by_name`** — the two are called **1:1 in all 16 benchmarks**, so the 13-line setjmp wrapper alone leaves 100% of the dispatch in C — and gate it on `string_manip` (10.0M calls) · `eval_fixed` (1.0M) · `roman` (400K), **NEVER on the rail demos.** ⚠ `APPLY_fn` is **0 in all 16** (ordinary `DEFINE` calls go via `rt_proc_call_epilogue_*`, per s187's 18 movers). ⚠ Two dead ends pre-closed: `setjmp` already lowers to `_setjmp@plt` (no sigmask syscall — do not chase it) and `try_call_builtin_by_name` is **already** a `bid_of()` hash + generation-stamped `g_dtax` inline cache + `(len<<8)|fn[0]` jump table, NOT a strcmp chain — an asm port inherits that algorithm and wins only `-O0` ceremony. THEN: `rt_proc_call_open(_slim)` · `rt_proc_open_fn` · `rt_call_named_proc` · `rt_arg_stage` + proc_set family. FINDING: `FINDING-2026-07-27d-...-CALL-ARR-IS-COLD-...md`.
  ⛔ **STEP 0 IS NOW THREE CHECKS, NOT TWO** (s187): (a) live definition exists; (b) the doc's spelling round-trips byte-identically against the tree; (c) ⭐ **NEW — `nm` the LINKAGE of every global the port will touch.** Exported (`B`) ⇒ preemptible ⇒ `@GOTPCREL`; hidden ⇒ rip-direct. In the C they look identical. Slice 2 met this because a self-contained port reaches globals the previous slice only reached THROUGH a C call.
  ⛔ **AND DESIGN THE FALSIFICATION BREAK BEFORE YOU WRITE IT.** A break that is a no-op at its own site produces a green board that reads as 'unreachable' and ships an unexercised port. Ask: *what would the output look like if this code did not exist at all?* If that equals the passing output, the probe is vacuous. (s187's ω needed an INVERSION, not a corruption.)
- [ ] **RTX-4 DIFFERENTIAL UNIT BATTERY — OWED BY THIS FAMILY.** MISC/ALLOC/STR each have one under `test_rtx_unit.sh`; CALL has NONE, so all its evidence is corpus movement. Needs a synthetic `g_pcall` record — fiddly but tractable, and **the only way to exercise the monitor arm while the monitor is dark** (that branch is untaken in every battery today, so no current gate says anything about it).
- [x] ⭐⭐ **RTX-3b — ON `origin/main` s202 (`dc5d9eb9`,`b497034d`); branch-only until then. ⛔ THE s190 BRANCH `rtx-3b-s190` (`a5ff0a9d`) IS DEAD — IT NEVER REACHED ORIGIN AND `a5ff0a9d` IS NOT A VALID OBJECT IN A FRESH CLONE. Reconstructed s199 (`c5ee5f0b`,`1f0e53cc`, ON ORIGIN as `rtx-3b-s199` despite that cursor saying otherwise), then REVALIDATED ON CURRENT MAIN s200 as `rtx-3b-s200` (`6154217c`,`5893d173`) — zero regression, battery 8426/0, falsification re-run RED 22, `var_access` 1.366× · `func_call` 1.080×. `string_pattern` DEBT DISCHARGED s200 (pre-existing, proven by pristine rebuild with the commits absent). Original rung text follows.** WIDEN THE STR FAST PATH TO THE NULL-OPERAND SHAPE. MEASURED, SPEC'D, AND READY TO IMPLEMENT (s188). LIKELY THE LARGEST REMAINING STR WIN, AND CHEAPER THAN ANY NEW FAMILY.**
  **THE MEASUREMENT (operand-tag histogram via the s188 interposer, mode 3):** the correlation with the STR gate A/B is EXACT — the only program whose concats are `S+S` is the only one that wins.
  | program | dominant operand pair | count | STR gate off/on |
  |---|---|---|---|
  | func_call | **SNUL + I** | 10,000,000 | **1.001** |
  | var_access | **SNUL + I** | 10,000,000 | **1.018** |
  | string_manip | **SNUL + I** | 5,000,000 | 1.279 ⚠ see below |
  | string_pattern | **S + S** | 5,000,001 | **2.016** |
  ⭐ **`SNUL ⊕ I` IS THE MOST COMMON CONCATENATION IN IDIOMATIC SNOBOL4 AND RTX-3'S FAST PATH EXCLUDES IT BY CONSTRUCTION** (it requires BOTH operands `DT_S`). It is produced by the ubiquitous predicate-guard idiom `LOOP N = LT(N, 10000000) N + 1` — the null return of `LT`/`GT`/`LE`… concatenated with the value — which appears in essentially every SNOBOL4 loop ever written. **25 million such calls across four benchmarks, every one on the slow path.**
  **AND THE WORK IT DOES IS ALMOST NOTHING.** `c_str_concat_d` implements the manual's TYPE-PRESERVING null rule (v3.7 p.22: `(20-17) ''` → integer 3, NOT `'3'`) as simply `if (IS_NULL_fn(a)) return b; if (IS_NULL_fn(b)) return a;` — but those two lines sit BEHIND a `rt_gc_point_arr` call with a 32-byte stack shadow array, four `DT_P`/`DT_X` pattern-tag compares, and two `IS_FAIL_fn` compares, all at `-O0`. **Ten million times, to return an argument unchanged.**
  **EXACT GUARD ORDER — DERIVED FROM `c_str_concat_d`, MUST MATCH IT OR SEMANTICS CHANGE (do not reorder):**
  1. `!g_gc_pending` — else C's `rt_gc_point_arr` does real work. **Reuse the existing `@GOTPCREL` load at `rtx_str.S:148-150`; the fast path already has this guard.**
  2. neither operand `DT_P`(3) nor `DT_X`(15) — pattern concat takes precedence (`pat_cat`).
  3. neither operand `DT_FAIL`(99) — `FAILDESCR` takes precedence.
  4. THEN `a.v == DT_SNUL`(0) ⇒ **return b**; `b.v == DT_SNUL` ⇒ **return a**.
  ⛔ Implement the `DT_SNUL` arm ONLY. `IS_NULL_fn` is `v==DT_SNUL || (v==DT_S && v.slen==0 && (!v.s||!*v.s))`; the second arm is the RTX-3 `slen==0`-means-LENGTH-UNKNOWN trap and all 25M measured calls are plain `DT_SNUL`. Taking the cheap arm keeps the guard branch-free-ish and cannot mis-handle the unknown-length case.
  **RETURN CONVENTION** (RTX-1 verified empirically): args `rdi=(slen<<32)|v, rsi=value` / `rdx=…, rcx=value`; return `rax=(slen<<32)|v, rdx=value`. So **return b = `mov rax,rdx ; mov rdx,rcx ; ret`** (read `rdx` before overwriting) and **return a = `mov rax,rdi ; mov rdx,rsi ; ret`**.
  ⭐ **STATE THE EXPECTED BOARD BEFORE PORTING, SO A NULL RESULT IS INFORMATIVE (the s187/s188 vacuity discipline): `func_call` and `var_access` MUST move well outside the ±3% noise floor. If they do not, the port is not being reached and the rung is falsified — do not accept a green board as success.** Gate on `bench_sno_rtx.sh STR`; `string_pattern` must NOT regress from 2.016×.
  ⚠ **STILL OPEN, MEASURED, CAUSE NOT ASSERTED:** `string_manip` is 5.0M `SNUL+I` (slow path) yet moves **1.279** under the STR gate — that should be ~1.00, and the gated-ON arm executes MORE guard instructions than OFF for this shape, so the sign is wrong. Well outside the ±3% floor. **Something other than the `str_concat_d` fast path is moving under `SCRIP_RTX_STR` — suspect the RTX-3 SXT C-side rework (`g_sxt_fr`, `rt_sxt_note` arming, the O(n²)-append trap it names). Explain it before quoting any STR number as final.** `rt_subscript_var` + `rt_deref` + `rt_field_var` + TABLE hash/lookup/insert internals (claws5 layer C ≈ 11 subscripts/token). Keep current table LAYOUT — layout redesign is a separate later rung with its own measurement.
  ⭐ **STEP 0 GROUNDWORK DONE s188 — START HERE, DO NOT RE-DERIVE.**
  **0(a)/(b) PASS with TWO doc corrections:** `rt_subscript_var` is at **`pattern_match.c:1000`** (✅ re-verified byte-exact s200), NOT `aggregates.c`/`values.c` as §5 says. `rt_field_var` is at `pattern_match.c:1042` (✅ re-verified s200). ⛔⛔ **`rt_deref` IS ALREADY HAND-WRITTEN ASSEMBLY — `src/runtime/rt/rt_asm_helpers.S:8`, resolved by `nm`-ing all 240 objects to `out/rt_pic/rt_asm_helpers.o`. THERE IS NO PORT TO DO.** This line previously read *"exported (`T`, 100 bytes at `-O0`) but its definition is NOT grep-locatable in `src/runtime/` — `core/core.c` contains non-UTF-8 bytes and `file` reports it as `data`, so ordinary greps miss it. Locate it before porting."* **The symptom was real and the `core.c` encoding hazard is real, but it is NOT why the grep missed it: step 0's own grep filter had no `--include=*.S`.** Fixed in ARCH §7, which now carries step 0(e) and the ungated-asm inventory. ⇒ **RTX-5's addressable surface is `rt_subscript_var` + the table internals it calls — ONE of the three named symbols, and 5.0M of the 7.5M measured calls. Restate the expected board accordingly or the rung is vacuous by s188's own test.** FINDING: `FINDING-2026-07-27m-...RT-DEREF-IS-ALREADY-ASM-AND-STEP-0-GREP-CANNOT-SEE-DOT-S-FILES.md`.
  **0(c) linkage:** `rt_subscript_var` `rt_deref` `rt_gc_point_arr` all `T`. The globals the port will touch are NOT yet enumerated — do this before writing asm (`g_agg_list_ser`, `g_agg_table_ser`, the table bucket roots).
  **0(d) RELEVANCE — PASSES, and this family is measurable:** `table_access` `rt_subscript_var` **5,001,000** · `rt_deref` **2,500,500** (exactly 2:1 — two subscripts per outer iteration, only the read derefs); `mixed_workload` 1,000,020 / 500,010. Both have adequate windows (1324ms / 453ms→2589ms at ×4) and a **null control already measured at 1.036 / 1.003** on `bench_sno_rtx.sh AGG`. ⚠ The RAIL demos are ZERO for both symbols — never gate this rung on them.
  ⚠ **CONCURRENCY: `rt_subscript_var` lives in `pattern_match.c`, which the contract flags as SN4-PAT / RTX-8 territory. Check the other session's cursor before starting — this was not previously known to be an RTX-5 collision.**
  ⭐⭐ **THE REAL ALGORITHMIC FINDING — READ BEFORE PORTING, IT MAY REDIRECT THE RUNG.** Every integer-keyed table access does: `tbl_key_str` **stringifies the integer** (manual itoa into `"\001i<decimal>"`), then `_tbl_hash` **hashes that string byte-by-byte**, then `table_find_pair` **`strcmp`s** down the bucket chain — **plus a `rt_agg_alloc(VCELL_t)` allocation on EVERY subscript**, because `rt_subscript_var` returns a NAME (`NAMETRAP(vc)`), never a value. That is ~5M stringify+hash+strcmp+alloc cycles in `table_access`. **An asm port would faithfully reproduce a poor algorithm at high speed.** ⛔ But integer-keyed hashing/comparison IS a table-LAYOUT change, which this rung explicitly defers. **So decide the fork FIRST (Lon's call): (i) port the existing algorithm as-is, accepting that the win is `-O0` ceremony only; or (ii) promote the layout rung ahead of RTX-5 and fix the algorithm, which is very likely the larger win.** A legitimate middle path that preserves layout exactly: **fuse itoa+hash in one pass** so the digits are hashed as they are generated — same key bytes, same hash, one less traversal. Same spirit as RTX-8's standing "⚠⚠ DO NOT TRANSLITERATE" warning on `g_star_peek`.
- [x] ⭐⭐ **RTX-5a — ON `origin/main` s202 (`9cc782fb`); originally branch-landed s200 (`94452610` on `rtx-3b-s200`, NOT main — see s202 cursor): `rt_asm_helpers.S` UNDER THE CONTRACT, gate `SCRIP_RTX_LEAF`.** Closes the ungated-asm hole. `rt_deref`+`to_int` gated; both C fallbacks verified COMPLETE first. Two-sided falsification proven (247/68 vs 268/47). ⛔ Measurement NOT obtained and the blocker is STRUCTURAL — see LIVE CURSOR. Remaining ungated: `rt_sg_scan_member`/`rt_sg_scan_nonmember`/`rt_sg_member` (`rt/rt_sg_scan.S`), `rk_gram_enter_box` (Raku).
- [x] ⭐ **RTX-0c — LANDED s201: `corpus/benchmarks/snobol4/table_churn.sno` (+`.ref`, README row).** ONE table created once · fixed 400-entry live set (<1000 ⇒ scaler cannot touch it, so auto-ranging adds PASSES not TABLES) · 10000 passes (≥1000 ⇒ the single scalable literal) · no pattern matching · ~1.9s natural window at F=1 · deterministic checksum `80200+400*P`, predicted in advance and matched at three pass counts. ⭐⭐ **AND IT FOUND THE REAL FIRST-ORDER CAUSE: `MADV_HUGEPAGE` compaction charged into the `clock()` window — `SCRIP_NOHUGE=1` takes `table_access` 3.9×→1.62× and `table_churn` 7.5×→1.12×.** Harness fork open for Lon (§4 of the FINDING); **step 0(d) call-density OWED before quoting any ratio.** Original rung text follows.
  ↳ *(original rung text, retained — NOT an open step)* **RTX-0c — INSTRUMENT: A RELIABLE AGG BENCHMARK (prereq for RTX-5 and for Lon's fork).** `rt_deref` has ZERO emitted sites in every stable benchmark; only `table_access` (arms swap ranking: 1.340/2.497/2.606/0.510) and `mixed_workload` (pre-existing segv on pristine main) reach it. Need integer-keyed table churn, self-timed `ms:` ≥800ms, NO pattern matching. Same tradition as RTX-0b: fix the instrument before quoting the board.
- [x] ✅ **RTX-0d — LANDED s204: `corpus/benchmarks/snobol4/arith_mixed.sno` (+`.ref`, README row, wired as the ARITH famset lead).** 40,000,001 `rt_num_arith` calls/run, SCALING PROVEN (80,000,001 at 2x, constant +1 preserved), natural window 1570ms at F=1, checksum `floor(1.5*(P+1))` PREDICTED IN ADVANCE and matched at BOTH pass counts (60000001 / 120000001). **`rt_num_arith` is measurable for the first time; the rest of RTX-6 is unblocked.** Original rung text follows — INSTRUMENT: A BENCHMARK THAT ACTUALLY REACHES `rt_num_arith` (prereq for the rest of RTX-6).** Same tradition as RTX-0b/0c: fix the instrument before quoting the board. **The gap is measured, not suspected** — `arith_loop` calls `rt_num_arith` ZERO times at both N=100000 and N=200000 (s203), because the emitter inlines integer arithmetic and it never reaches the C runtime. ⇒ **integer-only programs CANNOT exercise this symbol by construction, and no amount of loop scaling will change that.** Need mixed int/real arithmetic: s203 measured a plain `X = X + 1.5` loop reaching it 50,000×/run. Requirements inherited from RTX-0c: self-timed `ms:` window ≥`MIN_MS` (800), a scalable `LT(VAR,≥1000)` bound, deterministic checksum predicted IN ADVANCE, no pattern matching, and a step-0(d) count proven to SCALE at two loop counts. Then set `ARITH) FAMSET=` to it alongside `var_access func_call`.
- [x] ⭐⭐ **RTX-6 ARITH — LANDED s210: `rt_add`/`rt_sub`/`rt_mul` in asm, int-int + REAL-REAL SSE arms, gate `SCRIP_RTX_ARITH`. `arith_mixed` ON/PRISTINE **3.710×** (3-arm, RT_OPT=-O0) — THE LADDER'S FIRST LARGE UNAMBIGUOUS WIN.** ⛔ **THIS RUNG'S OWN TEXT BELOW CONTAINED A FALSE PREMISE, STRUCK s210: it says an int-int port "grades a guaranteed 1.000×" on `arith_mixed` because s206 censused it as 100% DT_R+DT_R. MEASURED s210: 50/50, 80,000,001 calls. s207's collapse-to-one-call created the int population AFTER s206 measured — the NINTH PHANTOM SHAPE, THE STALE CENSUS.** ⛔ DIV/MOD/POW NOT ported (zero-test/`fmod`/`pow` — the setjmp-bypass argument does not cover them); `rt_num_arith` itself still `OUT:SN4-RTX:s205`, unported, and is Prolog's arithmetic path ⇒ owes its own 0(d). ⭐ The remaining inline/template work below is UNCHANGED and still DEFERRED BY LON DIRECTIVE. Original rung text follows.
- [ ] RTX-6 (residual) **ARITH — RETARGETED TWICE; READ THIS WHOLE LINE BEFORE PLANNING ANY INLINE WORK.** `rt_cmp_d` LANDED s203. ⛔ **THE INT-INT INLINE FAST PATH THIS RUNG NAMED FOR THREE SESSIONS WAS ALREADY BUILT** — `bb_binop_arith.cpp` arm 3 did it, with compile-time immediate elision on top, i.e. strictly better than this rung's own spec (s206, measured from source). **PHANTOM RUNG: planned work that is already done — the phantom family one level up from symbols, and `nm` cannot catch it; only reading the emitting template arm can.** ⛔ **AND THE INSTRUMENT POINTED AT THE OTHER ARM:** `arith_mixed`'s 40,000,001 calls are **100% DT_R+DT_R** (interposer census, s206) ⇒ an int-int port grades a guaranteed 1.000× there. ⭐ **SUPERSEDED s207 BY LON DIRECTIVE** — *"for now all these should be ONE CALL into the runtime. We'll optimize the inline later."* All four arms collapsed to one per-op call (`rt_add`/`rt_sub`/…); **inline work is DEFERRED BY DIRECTION, not abandoned.** Measured cost of the deferral, so the inline rung has an honest floor to beat: `arith_loop` **0.64×**, `arith_mixed` **0.80×** (RT_OPT=-O0). ⭐ **WHEN INLINE RESUMES, TAKE THE FREE ~11.3 ns/call FIRST** — that is one extra `-O0` frame from the `rt_<op>`→`rt_num_arith` hop, reclaimable by giving the per-op entries their own body, and it needs NO inlining and NO `x86_asm.h` change. **THEN** the real-real SSE arm (s206 spec: both-tags-`DT_R` → `movsd`/`addsd|subsd|mulsd`; ADD/SUB/MUL only — DIV needs a zero-test (`arithmetic.c:239`), MOD needs `fmod`, POW needs `pow`; ⛔ both-same-tag only, any string tag out-of-line because `'1.5'` IS a real operand per Ch.17 String→real + `operand_is_real_str`). ⛔ That arm needs NEW SSE ENCODERS in `x86_asm.h` (`addsd`/`subsd`/`mulsd`/`ucomisd`/`cvtsi2sd` do not exist; `movsd` handles only the `f64:` immediate form) ⇒ **RTX-11/12 concurrency collision is unavoidable for it and is Lon's call.** ⛔ **~~incr decr neg exp~~ ARE PHANTOMS — struck s204 per ARCH §7.**
- [x] ⭐ **RTX-5b — LANDED s204 (SCRIP `320d212a`): `rt_agg_alloc` in asm, gate `SCRIP_RTX_ALLOC`, C body `c_rt_agg_alloc`, `HB_AGGV` `_Static_assert`-anchored.** Target chosen by step-0(d) measurement (8,001,201/run in `table_churn`, scaling proven at 2x) over `rt_call_arr`. Falsification two-sided after the first probe came back SILENT (type corruption = 0 movers; size corruption = 6 movers each mode, gate OFF = watermark). ⛔ **NO DEMONSTRATED SPEEDUP — pre-stated board 1.03-1.10x, isolated result 1.022x/1.002x, PREDICTION FALSIFIED, no-regression only.** ⛔ **OPEN GAP THIS RUNG DID NOT CLOSE: the `HB_AGGV + clamp(kind,0,2)` computation is unverified by all 315 programs** — mis-tagging every aggregate cell moves nothing. Anything touching aggregate block typing has no regression net today.
- [x] ⭐⭐ **RTX-3b ISOLATION ARM — DONE s204. ATTRIBUTION HOLDS: isolated `var_access` 1.513× (family 1.494×) · `func_call` 1.031× (family 1.065×). The family gate UNDER-credited the rung on one program and over-credited on another ⇒ its error has no known sign or bound.** Original rung text follows. s204 proved a family-gate A/B credits the WHOLE family to the newest rung (ALLOC read 1.028/1.039; the rung itself was 1.022/1.002). **RTX-3b's `var_access` 1.366x / `func_call` 1.080x — the ladder's headline speed claims — were taken under `SCRIP_RTX_STR`, a gate that also disables RTX-3.** ⚠ This does NOT assert those numbers are wrong; RTX-3b's own falsification proves it executes, which was never the question. **It makes the ATTRIBUTION unproven.** Method: build with ONLY RTX-3b's arm routed to C, leave RTX-3 live, swap `.so`s per round, interleave, discard round 1 (hugepage warmup). State the expected board first.
- [ ] RTX-7 **NV/GVAR — ⛔⛔ DOWNGRADED s208: NOT A SPEED RUNG. DO NOT PORT THIS FOR PERFORMANCE.** `NV_GET_fn`/`NV_SET_fn` (✅ both verified LIVE s204) + `rt_gvar_*` + `rt_gva_island` + `rt_subject_load_nv`. ⛔ **ARCH §5's NV row also carried `rt_nv_get`/`rt_nv_set` — BOTH PHANTOMS, struck s204, re-confirmed s208 by `nm` (zero definitions).** ⛔ **STEP 0(d) FAILED, MEASURED s208:** `NV_GET_fn` = **ZERO calls** in var_access · func_call · string_manip · table_access · fibonacci · indirect_dispatch · op_dispatch, and **ZERO static call sites** in every benchmark `.s`; `rt_gvar_get_int`/`rt_gvar_assign_descr` also **0 static sites**. Only live window is **EVAL** (`eval_dynamic`/`eval_fixed`, 2,000,000 calls, scaling proven 2× with +26 constant preserved), where `rdtsc` bounds it at **0.303% + 0.276% = 0.58% UPPER BOUND** ⇒ **a perfect port wins less than the ±3% null floor.** ⛔ **NEVER grade it on the four zero-count benchmarks — guaranteed false null, the s203 ARITH-FAMSET defect class.** ⭐⭐ **CAUSE = THE EIGHTH PHANTOM SHAPE, "THE BYPASSED FAMILY": the function is live and correct, but GVA SLOTS ALREADY TAUGHT THE EMITTER NOT TO CALL IT** (`var_access.s`: `rt_gva_island`/`gva_register` once at startup, then direct slots, no runtime call) — exactly what integer inlining did to `rt_num_arith` at s203. **A RUNG'S PREMISE DECAYS WHEN AN UNRELATED RUNG SUCCEEDS; RTX-7 was correct when written.** ⇒ if ever done, it is a **correctness/eradication** rung serving RTX-12, and the FINDING must say so rather than quoting a speed number. ⚠ **THIS ROW IS THE MIXED SHAPE AND IT IS THE HARDEST TO CATCH:** the two names a rung author is most likely to spot-check (`NV_GET_fn`/`NV_SET_fn`) are exactly the two that are real, so one confirmation "validates" a list that is 2/6 dead. **Spot-checking a symbol list is not checking it — `nm` every name.** ⚠ COORDINATE: `NV_SET_fn` is DB-1's planned write-barrier choke point — port must leave room for the flag-bit check (or land DB-1 first; walking session's call, note it in the FINDING either way).
- [ ] RTX-8 **MATCH SINKS:** cap/dcap/defer families + match_enter/variant/value/replace + scan_splice_empty. ⚠⚠ DO NOT TRANSLITERATE `g_star_peek` — the s161 m4 EVAL/deferred slot collision lives in `rt_defer_get_pat_fn`'s one-entry latch (pattern_match.c:960); the port must FIX the order-dependent bleed (per-site resolution, no shared latch), and the 15-line s161 repro becomes the regression canary. This rung closes 2 of the 3 broken demos if done right.
- [ ] RTX-9 **PAT BUILD:** ⛔⛔ **THE RUNG'S OWN SYMBOL NAME WAS WRONG AND WOULD HAVE READ AS A DEAD FAMILY — CORRECTED s204 BEFORE IT COST A SESSION.** This rung read `rt_pat_*`; **all 30 `rt_pat_*` spellings are declaration-only in `rt/rt.h:38-67` with zero definitions.** The live constructors carry **NO `rt_` prefix**: 32 defined in `pattern_match.c` (`pat_lit` @ `:98`, `pat_alt` @ `:215`), listed in full in ARCH §5's corrected PAT row. ⭐ **SEVENTH PHANTOM-FAMILY MEMBER, AND A NEW FAILURE MODE: THE PRIOR SIX RISKED WASTED OR DUPLICATED WORK; THIS ONE RISKED ABANDONING A LIVE, HOT FAMILY.** A step-0 grep on `rt_pat_lit` HITS (the `rt.h` declaration) so the name reads alive; `nm` then finds no definition, which is the exact signature of a phantom — so the correct-looking conclusion is "PAT is dead, skip the rung," and it is FALSE. ⚠ Take the tree's spelling exactly: `pat_break_` has a **trailing underscore** and the ANY constructor is `pat_any_cs`, not `pat_any` (step 0b). Construction-path = size-not-time (s160a measured rule) — port LAST among hot families, quote size wins honestly, never as match-loop speedups.
- [ ] RTX-10 **MISC/IO + VSTACK AUDIT:** keywords, goto_transfer, execute_code_dyn, comm_stno, io. ✅ **THE VSTACK AUDIT IS DISCHARGED s204 — MEASURED, AHEAD OF THE RUNG.** `nm -D out/libscrip_rt.so` finds **ZERO definitions** for every `rt_push_*`, `rt_pop_*`, and `rt_halt_tos`; each is declaration-only in `rt/rt.h` with exactly one src ref (its own declaration). ⇒ **the rung's "if dead, delete instead of port" branch is TAKEN.** No construct needs naming in the FINDING because nothing reaches them — the NO-VSTACK rules already won and only the header still says otherwise. **Remaining work on this rung is the live MISC/IO surface plus deleting the dead declarations.**
- [ ] RTX-11 **RTX-CC — REGISTERIZED ABI (phase 2):** per-family register signatures beyond SysV (S/F in EFLAGS instead of DT_FAIL compares; >2-arg staging; multi-returns). EVERY template call-sequence change via `x86(...)` encoders per RULES (TEMPLATE-ONLY, BOTH-MEDIUM); `.s` regen ×3 fires; per-family, measured, rail-gated.
- [ ] RTX-12 **ERADICATION + FINAL BOARD:** delete `c_*` bodies + gates family-by-family (each deletion re-gated); rail all 16 working-set programs; before/after board vs the s161 baseline (claws5-match 0.65 · json-match 0.79 · calc-1 2.11 · treebank 2.73 · calc-2 3.54 rail; match-only ms board in s161 cursor); FINDING of record. ⛔⛔ **BLOCKED ON RTX-0b — THIS BOARD IS STRUCTURALLY BLIND TO RTX-1…RTX-7 (measured s188).** The five named demos touch ALLOC twice, STR twice, and AGG/ARITH zero times in a whole N=64 run; `rt_call_arr` is flat across N. **Running this board as written would report ~zero movement and be misread as "the asm ports did nothing" when it actually means "these programs never call the asm."** By elimination the rail demos' time is in the pattern matcher ⇒ only RTX-8/RTX-9 can move this baseline, and they are ordered LAST. Fix the instrument before quoting the board.
**EVERY RUNG:** the ARCH §7 protocol verbatim — read-first set, gates at current watermark (re-prove at session start), kill-switch md5, rail same-moment A/B labeled `RTX_<FAM>`, FINDING, LIVE CURSOR move. Phase-1 rungs (1–10) touch ZERO templates ⇒ no `.s` regen owed by construction.

