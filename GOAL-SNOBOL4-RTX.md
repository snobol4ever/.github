# GOAL-SNOBOL4-RTX.md — The SNOBOL4 Runtime in Optimized x86-64 Assembly

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (8)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

## ⛔ LIVE CURSOR — s240 (2026-08-08): **LEN DEFERRED-ARG FIX LANDED — `lower_snobol4.c` + `emit_drive` MATCH_LEN DYNAMIC SLOT. WATERMARK EXACT HOLD. CREDENTIAL OWED FOR PUSH.**
**⭐⭐ WATERMARK AT HEAD `5a2eac19`, N=4, `setarch -R`, BYTE-REPRODUCIBLE 4/4: m3 **286/31/0** · m4 **280/36/1 SKIP** · DIVERGE **9**. Delta from s239's 289/28/0 · 273/43/1 · DIVERGE 19 is concurrent commits that landed between s239 and this clone — NOT a regression. WATERMARK HOLDS EXACT through both this session's commits.**
**✅ LEN DEFERRED-ARG FIX LANDED (`5a2eac19`). Two-file commit, both files changed together:**
**`lower_snobol4.c` TT_LEN case: `TT_DEFER` arg (bare `*var` or `*(arith)`) now uses `sx_lower`+`ir_operand_push` same as TAB/POS (lines ~1215/1223). `sno_pre_req` was stripping `*` and evaluating arithmetic eagerly at pattern-build time, storing INT where PAT DESCR expected. The SEGV s239 root-caused is the two-statement `PAT=LEN(*(n+3))` / `subj ? PAT` shape — now correctly lowers.**
**`emit.cpp` emit_drive IR_MATCH_LEN: added `bb_slot_get→zls_off` fallback (same pattern as SAVE_RESTORE line 1001 / CALL2BB line 1330) + `op_zres=1` when slot from ZD path, so template takes `ZOPQ(0,8)` not `FRQ(op_sa+8)`. Previously a dynamic CALL SNO$MKEXPR operand in the ZD spine had no bb_slot entry → `drive_unowned(nd)` → abort with `op=86` (IR_MATCH_LEN enum value). Crash was pre-existing at this HEAD for any `LEN(*(expr))` form.**
**⚠ NOTE: two-statement `PAT = LEN(n)` / `subj ? PAT` still produces empty output for BOTH literal and deferred args — this is the pre-existing PAT$0 blob emission issue (154_pat_construction_time_hoist is in the fail set). The LEN lowering fix is correct; the PAT blob emit gap is a separate rung. Zero programs moved in fail set.**
**⛔ PUSH BLOCKED — credential owed. SCRIP commit `5a2eac19` on local main, not yet pushed to origin. corpus: no changes this session. `.github`: cursor update commit needed before push.**
**⛔ NEXT: (1) PUSH with credential (SCRIP `5a2eac19` + `.github` cursor commit); (2) hand `152_pat_json_keyvalue_renamed` + one-line probe to ζ/ON ladder per s238 NEXT; (3) TAG-4 vs `rtx_arith.S`; (4) `eval_dynamic` 430× characterization.**
**FINDING: no separate FINDING doc. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s235 (2026-08-01): **THE RTX ASM + DESCR MOVE IS COMPLETE ON MAIN — AUDITED AND GATED, NOT INHERITED. s234's "NOT PUSHED" WAS FALSE. THE −1 WATERMARK DELTA IS THE CONCURRENT ON LADDER, NOT THE TAG WORK.**
**⭐⭐ s234's CURSOR CLAIM "NOT PUSHED (credential owed)" IS FALSE — (a)-CLASS ROT, THIRD RECURRENCE. Both repos measured CLEAN with `HEAD == origin/main` (SCRIP `03cecd87`, corpus `e6eee1d9`). The four hashes s234 named do not exist; a `pull --rebase` rewrote them. **NOTHING WAS OWED. `handoff_status.sh` is the push truth — never a cursor sentence.****
**⭐⭐ MAIN'S TRUE WATERMARK, N=4, `setarch -R`, BYTE-REPRODUCIBLE 4/4: m3 **294/23/0** · m4 **280/35/2** · DIVERGE **17** · m3 fail-set md5 **`2192860a5d539b4fe1a6b5aea0ee16df`**. ⛔ **s234's m3 295/22/0 · DIVERGE 16 and md5 `ac8908de…` ARE STALE — DO NOT QUOTE.** m4 held EXACT.**
**⭐⭐ ATTRIBUTION BY THREE BUILDS, NOT BY ARGUMENT: `afbcab9b` (s234's baseline) = **295/22/0** DIVERGE 16, `152` PASSES · `37dbf795` (+ON-3+ON-5, **TAG-3 re-apply ABSENT**) = **294/23/0** DIVERGE 17, `152` **ALREADY FAILS** · HEAD (+TAG-3) = **294/23/0** DIVERGE 17. ⇒ **`152_pat_json_keyvalue_renamed` BROKE BEFORE s234's COMMITS EXISTED IN THE LINE — owner is the ζ/ON ladder (`154a3fa8` ON-3 ARG-NOTE or `efc11e5f` ON-5 CLAIM-ZERO). THE TAG-3 RE-APPLICATION HOLDS EXACT ON TOP OF IT.****
**⭐⭐ NEW RULE — A REBASE IS AN UNMEASURED MERGE. s234's number reproduces TO THE DIGIT at its own baseline; what defeated it is that `pull --rebase` replayed its commits onto two commits from a PARALLEL session, manufacturing a combination no gate had ever seen. **RE-RUN THE WATERMARK AFTER THE REBASE, BEFORE CLAIMING EXACT HOLD** — or say plainly that the number describes the pre-rebase tree. ⚠ This is s232 INVERTED and the more dangerous direction: the delta presents as "the renumber cost a program," which is FALSE, and reverting it would destroy correct work AND leave `152` broken.**
**✅ THE MOVE AUDITED SYMBOL-BY-SYMBOL: `descr_tags.inc` present · `descr.h` layout exact (SNUL=0x00 S=0x02 I=0x03 R=0x05, aggregates stride 8, FAIL=0x68, DATA=0x70) · **all 16 `.S` include the shared header**; every surviving small-int compare classified NON-TAG under the s230 reading rule (agg-KIND clamp · SLEN dispatch · single-byte fast path · byte-count ladder · nametrap SLEN) — **s230's `$9/$99/$6` and the packed `0x200000009` class are GONE** · s234's two late finds symbolic (`bb_call_fn.cpp:197,200`, `bb_call.cpp:451,453`) · tree-wide emitter sweep = **1 hit and it is CORRECT** (`bb_glue_flat.cpp:91` is the arg to `exit(1)` in TEXT, both BINARY ports symbolic — the s234 fix is INTACT) · s231 rail + layout gate both on main, NOT stranded.**
**✅ GATES AT HEAD: tag layout **13/13 PASS** (incl. its own 22-define `descr.h ↔ descr_tags.inc` cross-check and "no hand-encoded tags in .S") · build **EXIT=0** · RTX unit **ALL PASS 8426/0** · store-width **PASS** · m3 smoke green · 17 `rtx_gate` bytes linked. ⛔ **NO SPEED NUMBER** (attribution session; rail deliberately not run). `RT_OPT=-O0`. **NO `.s` REGEN OWED — ZERO source files changed.****
**⭐⭐ PART II — `152` BISECTED TO ONE COMMIT AND ITS SAFETY CLAIM FALSIFIED: `154a3fa8` (ON-3 ARG-NOTE) **ALONE** breaks it (parent `afbcab9b` PASSES 295/22/16; ON-3 = 294/23/17). **`efc11e5f` ON-5 EXONERATED.** Symptom is a **DETERMINISTIC SEGV** (20/20 across ASLR on AND off ⇒ **NOT s231's load-address class; my own layout hypothesis died in one experiment**). ⭐⭐⭐ **THE PROBE: comment out `x86_argnote(o);` at `x86_asm.h:1384` AND NOTHING ELSE, rebuild ⇒ `152` prints correctly 3/3.** That call sits behind `:1343`'s `if (MEDIUM_BINARY || …) return s;` and the commit's own comment claims *"mode-3 bytes are untouched BY CONSTRUCTION"* — **FALSE BY MEASUREMENT.** ⇒ **NEW RULE: "BY CONSTRUCTION" IS A HYPOTHESIS UNTIL A PROBE KILLS THE ARM** — sixth failure of this claim shape; I verified the guard BY READING and reading was not enough. ⚠ `127` (differs from `152` only in capture-variable NAMES) also SEGVs but was already in the fail set ⇒ **the gate is blind to failure-mode changes among already-failing programs; blast radius is not one program.** ⛔ **NOT LANDED — `x86_asm.h` is the ζ/ON ladder's file and fires `.s` regen ×3; probe REVERTED, tree clean at `03cecd87`. Lon routes it.**
**⛔ NEXT: (0) ⭐⭐ DELETE `origin/tag-renumber-s229` + `origin/trial-merge-s233` — **PROVEN to contain nothing main lacks**; needs credential; (1) ⭐⭐ hand `152` + the one-line probe to the ζ/ON ladder; hypothesis to test there is that `g_medium` is TEXT during some mode-3 emission so `x86_argnote`'s `o.swap(out)` corrupts the in-band `L`/`J`/`D`/`E`/`F` record stream — print `g_medium` at `:1343`, MONITOR-FIRST; (2) ⭐⭐ the m4-EVAL-empty rung; (3) deferred-arith m3 SEGV; (4) then the s231 queue, untouched: `SCRIP_NO_CU=1` Prolog gate · TAG-4 vs `rtx_arith.S` · `VARVAL_fn` (rank 1, 29M) · `eval_dynamic` 430×.**
**⚠ ENVIRONMENT: `nproc == 1` (full build ~4 min) · `setsid` survives tool-call boundaries, plain `&`/`nohup` does NOT · `Makefile` uses `ROOT := $(shell pwd)` so in-place checkout A/B is path-safe — save `scrip`+`out/libscrip_rt.so` aside and restore instead of rebuilding twice · **full crosscheck is 46 s, so N=4 costs ~3 min: there is no excuse for an N=1 watermark here.****
**FINDING: `FINDING-2026-08-01d-CLAUDE-SN4-RTX-S235-THE-RTX-DESCR-WORK-IS-FULLY-ON-MAIN-AND-THE-ONE-PROGRAM-DELTA-IS-THE-CONCURRENT-ON-LADDER.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s234 (2026-08-01): **TAG-3 IS ON MAIN. THE TWO-SESSION EVAL BLOCKER IS CLOSED — IT WAS THE γ *SUCCESS* CODE, NOT THE ω *FAIL* CODE. WATERMARK EXACT HOLD. NOT PUSHED (credential owed).**
**⭐⭐ ROOT CAUSE: `bb_glue_flat.cpp`'s γ landing wrote the chain's SUCCESS status as a hardcoded `1` (old `DT_S`) inside an `IF(MEDIUM_BINARY,…)` block. Post-renumber `DT_S==2`, so a SUCCEEDING runtime-compiled chain returned tag 1 — not a valid tag — and `eval_chain_run_capture` read `ZZEVALZZ` back as null. **s233 symbolized ω (`:92`, the FAIL code) and left γ (`:83`); EVAL SUCCEEDS, so only γ ever fired.** Right file, right class, wrong line. Both ports now symbolic.**
**⭐⭐ THIS EXPLAINS THE m3-ONLY / m4-EXACT ASYMMETRY NOBODY EXPLAINED FOR THREE SESSIONS: in TEXT medium γ/ω call `exit()` and touch NO tag; only `MEDIUM_BINARY` carries a tag-shaped status code. ⇒ **RULE: A MODE-ASYMMETRIC DEFECT — GREP THE `IF(MEDIUM_*)` BLOCKS FIRST.** ⇒ **RULE: A STATUS CODE THAT EQUALS A TAG *IS* A TAG** — the in-file prose calling `1`/`99` a "protocol" is exactly what two sessions walked past; the chain's type is `DESCR_t(*)(void*,int)` and `eax` IS the returned `v`.**
**⭐⭐ IT WAS NEVER A MERGE DEFECT. Re-applied the change set DIRECTLY ONTO MAIN, no merge — reproduces the IDENTICAL two-program regression (`1016_eval` + `161_pat_defer_fn_nested_match`, zero removed). ⇒ when a change breaks only under a merge, RE-APPLY IT TO THE TARGET and see whether the merge was ever load-bearing. Two sessions never ran that experiment and spent their budget on merge archaeology.**
**⭐ s233's "whole deferred-eval family" IS THE WRONG FAMILY: `EVAL` of a plain STRING fails identically, and the chain EXECUTES (a DEFINE'd fn called through EVAL prints its side effect and returns) — only the value fails to land. It is EVERY RUNTIME-COMPILED CHAIN, not deferred-expression handling.**
**✅ TWO UNSYMBOLIZED SITES THE BRANCH MISSED, BOTH FIXED: `bb_call_fn.cpp:197,200` `cmp64 rax,6` — a PACKED tag+slen compare, **4th instance of the s230 invisible-packed-literal class, and the first in a C++ TEMPLATE** (so `descr_tags.inc` does NOT close the class) · `bb_call.cpp:451,453` `mov32 edi,6/1` — `DT_I`/`DT_S` mints for `rt_is_truthy` **added by MAIN after the branch was cut**. ⇒ **AN AUDIT HAS AN EXPIRY DATE MEASURED IN COMMITS** (s232 cleared 27; main is now 50 past merge-base).**
**✅ GATES: BASELINE re-proven on main `afbcab9b`, N=4, `setarch -R`, BYTE-REPRODUCIBLE — m3 **295/22/0** · m4 **280/35/2** · DIVERGE **16** · fail-set md5 `ac8908de8333d6a2f9879b86a2032e18`. AFTER: identical on all four, **fail-set md5 BYTE-IDENTICAL, N=4, EXACT HOLD**. tag layout 13/13 · RTX unit ALL PASS 8426/0 · store-width PASS · `.s` regen ×3 RUN. Prolog 0/4/185 + Icon 4/0/0 both sides (artifacts swapped to measure pristine) — ⚠ **POPULATION: Prolog is 185/189 SKIP, Icon is 4 programs; weak evidence for THIS change, not confirmation.** ⛔ s233's 294/23 is STALE, not quoted. **NO SPEED NUMBER** — correctness rung, rail deliberately not run.**
**⛔⛔ TWO PRE-EXISTING MAIN DEFECTS FOUND WHILE BASELINING, NEITHER CAUSED NOR FIXED HERE: (1) deferred ARITH `*(n + 3)` SEGVs in m3 on PRISTINE main; (2) ⭐⭐ **m4 EVAL IS SILENTLY EMPTY — `1016_eval` produces NO OUTPUT in m4 on pristine main (rc=0) while hello-world runs**, and it sits in FAIL(m4)+DIVERGE at baseline. **This may be what s233 was actually chasing through the merge.** Own rung, Lon's routing.**
**⚠ INSTRUMENTS: (a) ⛔ **BACKGROUNDED BUILDS DO NOT SURVIVE A TOOL-CALL BOUNDARY HERE** — a `nohup &` build died silently at 43/253 objects, log simply stopped, nothing reported failure; **`setsid` survives** (add to ENVIRONMENT FACTS). (b) ⚠ `test_crosscheck_prolog.sh` HARDCODES `SCRIP=` and ignores the env override ⇒ an A/B done that way compares a binary against ITSELF; swap artifacts on disk. (c) ⚠ an `NV_SET_fn`/`NV_GET_fn` LD_PRELOAD interposer sees only PLT-routed calls — the chain's own store is `.so`-internal and never appears; a zero from it is NOT evidence of a lost store.**
**⛔ NEXT: (0) ⭐⭐ PUSH — SCRIP `5ddd5738`+`026f1cb5`, corpus `2b2f0ab9`+`92a882e4` are COMMITTED LOCAL ONLY, credential owed; (1) ⭐⭐ the m4-EVAL-empty rung above; (2) deferred-arith m3 SEGV; (3) delete branch `tag-renumber-s229`/`trial-merge-s233` once the push lands — main now carries the renumber, the branch is dead weight and a fresh-clone hazard; (4) then the s231 queue, untouched: `SCRIP_NO_CU=1` Prolog gate · TAG-4 vs `rtx_arith.S` · `VARVAL_fn` (rank 1, 29M entries) · `eval_dynamic` 430×.**
**FINDING: `FINDING-2026-08-01c-CLAUDE-SN4-RTX-S234-THE-EVAL-BLOCKER-IS-THE-GAMMA-SUCCESS-CODE-NOT-THE-OMEGA-FAIL-CODE.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s233 (2026-08-01): **MERGE STILL BLOCKED AT A MAIN 48 COMMITS FURTHER ON. THE DEFECT IS THE WHOLE DEFERRED-EVAL FAMILY, NOT ONE PROGRAM. MY OWN BEST SUSPECT WAS FALSIFIED IN ONE BUILD. NOTHING PUSHED TO SCRIP; `main` UNTOUCHED.**
**⭐⭐ WATERMARK RE-PROVEN, N=4, `setarch -R`, EXACT HOLD ON BOTH LEGS: main `eb0c08a8` m3 **294/23/0** · m4 **280/35/2** · DIVERGE **17** → merge m3 **292/25/0** · m4 **280/35/2** · DIVERGE **15**. ⛔ s232's 200/117 IS STALE — main moved 48 commits past merge-base (s232 saw 27); quoting it would repeat the s228 error. ⭐ s231's ASLR root-cause REPRODUCES: ASLR off ⇒ byte-reproducible over 4 runs, `151`/`160` stable, no quarantine needed.**
**⛔ GATE FAILED: m3 −2, m4 held EXACTLY (fail set md5-identical). Set-diff ADDED `1016_eval` + **`161_pat_defer_fn_nested_match`** (NEW — s232 reported only one), REMOVED nothing. ⚠ DIVERGE FELL 17→15 AND THAT IS THE REGRESSION, NOT AN IMPROVEMENT: both programs left DIVERGE by moving (m3 pass/m4 fail) → (fail/fail). Never read DIVERGE without the m3 set-diff.**
**⭐⭐ THE BLAST RADIUS IS MUCH WIDER THAN THE CORPUS SHOWS: a 6-line probe has EVERY `EVAL(*expr)` form returning the null string (deferred literal / varref / concat / var+lit / arith), while plain concat is fine. `1016_eval` stops at its first failing assertion and MASKS its own 2 and 3. CONSTRUCTION IS INTACT — `DATATYPE(*expr)`=`EXPRESSION` on both engines. `eval_chain_run_capture` reads the result out of `EVAL_TMP`, so `DATATYPE=STRING,SIZE=0` means **THE BLOB NEVER WROTE `EVAL_TMP`** ⇒ a LOST STORE, a success routed past its own assignment — not a corrupted value.**
**⛔⛔ MY SUSPECT WAS FALSIFIED AND IT HAD FIVE CORROBORATIONS. `bb_glue_flat.cpp:92` (`x86("mov32","eax",99)`, added by main after the branch was cut) was the ONLY surviving stale tag literal tree-wide; 99 is old `DT_FAIL`, it is `MEDIUM_BINARY`-only (matching an m3-only defect), and under the new bit layout `99` has `DT_CHARS_BIT` SET so it reads as an EMPTY STRING — the exact symptom. Swapped to symbolic `DT_FAIL`, rebuilt: **ALL SIX FORMS STILL EMPTY.** Cause: `rt_chain_enter` (AT&T inline asm, `runtime_eval.c:81`) wires γ/ω to one landing and `jmp *%rax` — the chain NEVER reaches the outer bridge. ⇒ **FIVE CONVERGING CIRCUMSTANTIAL FACTS DID NOT SURVIVE ONE BUILD. The edit is RETAINED as hygiene, is a PROVABLE NO-OP on main today (`DT_FAIL==99` pre-renumber), and IS NOT THE FIX.**
**✅ RULED OUT BY MEASUREMENT: silent auto-merge did NOT drop the branch's tag fixes (collision set is 4 files — `bb_call.cpp`/`bb_call_fn.cpp`/`bb_call_proc_staged.cpp` auto-merged, `xa_flat.cpp` conflicted; `DT_*` counts MATCH the branch 8/8, 23/23, 6/6) · no other stale literal tree-wide · not `DT_E` · not m4/TEXT.**
**⚠ LEADING UNTESTED HYPOTHESIS: interaction with main's in-flight WIREREG surgery (s22u) — main rewrote `bb_match_defer.cpp`/`bb_save_restore.cpp`/`bb_call_value.cpp`/`bb_match_capture.cpp`/`bb_match_release.cpp` and added hand-asm `rt_outer_call`, i.e. the call/wire/defer protocol EVAL rides was under reconstruction while the branch sat.**
**⛔ NEXT: (0) ⭐⭐ MONITOR CONTROL against a `main` build — s233's bracket (step 2, stmt 1: spl `VALUE expr=UNKNOWN` vs scr `CALL EXPR$0`) is NOT yet proven to be the fault rather than trace-granularity; every downstream step keys off this. (1) ⭐⭐ BISECT the 6 branch commits onto today's main — if `50689805` (the renumber) alone breaks it the cause is main-side code it exposes; if a LATER fix commit breaks it the cause is an OVER-SWAP (the s230 READING RULE hazard) and a different defect entirely. (2) `ud2` on the `EVAL_TMP` store path. (3) re-gate at main's THEN-CURRENT watermark — never reuse 294/23. ⚠ `.s` regen ×3 OWED by any landing, NOT run (nothing landed).**
**⚠ THREE INHERITED CLAIMS WERE STALE, ALL RE-DERIVED: (a) the REPO HAZARD is FALSE — a fresh clone lands on `main`; (b) `xa_flat.cpp` is a CONTENT conflict now, not delete/modify (the file EXISTS on main), though the resolution is still "take main's deletion", forced — all 11 branch edits are tag swaps inside the 501-line block main deleted; (c) s232's absent-hazard audit covered 27 of what are now 48 commits, and re-run it FIRES.**
**FINDING: `FINDING-2026-08-01b-CLAUDE-SN4-RTX-S233-THE-MERGE-DEFECT-IS-THE-WHOLE-DEFERRED-EVAL-FAMILY-AND-MY-OWN-SUSPECT-WAS-FALSIFIED.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s232 (2026-08-01): **THE MERGE IS BLOCKED. `tag-renumber-s229` BREAKS `1016_eval` WHEN MERGED, AND NEITHER PARENT BREAKS IT. NOTHING PUSHED.**
**⭐⭐ MEASURED ON ALL THREE LEGS, N=4, `setarch -R`: main PASSES `1016_eval` m3 · branch PASSES it 4/4 · MERGE FAILS it 4/4 (`FAIL 1016/001: eval concat expr`, rc=0, one hash). ⇒ THE DEFECT IS CREATED BY THE MERGE. Mechanism: `src/templates/bb_call_fn.cpp` was edited by BOTH sides and git raised NO CONFLICT. A clean build is exactly what a session merging this would have seen — only the EXACT-HOLD watermark caught it.**
**⛔ GATE FAILED, SAID OUT LOUD: main baseline m3 200/117/0 · m4 198/118/1 · DIVERGE=2 → merge m3 199/118/0 · m4 198/118/1 · DIVERGE=1. m4 held EXACTLY; both m3/DIVERGE deltas are the SINGLE program `1016_eval` moving (m3 pass/m4 fail) → (fail/fail). Set-diff: one name added, zero removed.**
**⚠ THE BASELINE IS MAIN'S, NOT THE BRANCH'S — 76 PROGRAMS APART. s231's 276/41 was measured 27 commits ago. Main is mid-flight on the ζ ladder and knowingly depressed (its HEAD message: "landed DARK, emission link broken"); its own recorded watermarks run 198/119, 199/118, 204/113, 229/88, so 200/117 is THEIR range, not my regression. Quoting 276/41 as the merge gate would have been the s228 error.**
**⭐ THE HAZARD I AUDITED FOR WAS MEASURED ABSENT AND IT WAS THE WRONG HAZARD: main's 27 commits = 1252 src lines with ZERO `DT_` refs, ZERO 99/100 literals, ZERO descriptor mints (3 grep hits all false positives — `IR_LIT_*` opcodes/prose). The "asm is symbolic" class is retired for this merge; the merge broke anyway, through the one auto-merged file. AN AUDIT AIMED AT A KNOWN CLASS DOES NOT LICENSE A MERGE.**
**✅ CONFLICT RESOLUTION OF RECORD: ONE real source conflict, `xa_flat.cpp` (delete/modify, HEAD side empty). Branch's ONLY edits there are 11 lines swapping hardcoded `99` → symbolic `DT_FAIL`, all inside the block main DELETED (CARVE-KILL). ⇒ take main's deletion; forced, not a judgment call. 150 `.s` conflicts are regenerable artifacts.**
**⛔ NEXT: (0) ⭐⭐ 2-WAY MONITOR ON `1016_eval` vs the trial merge — MONITOR-FIRST, and this cursor deliberately does NOT name a cause; `bb_call_fn.cpp` is a SUSPECT, not a diagnosis. (1) re-merge + re-gate at main's THEN-CURRENT watermark (main moves; never reuse 200/117). (2) then the s231 queue, all untouched: `SCRIP_NO_CU=1` Prolog gate · TAG-4 vs `rtx_arith.S` · `VARVAL_fn` · `eval_dynamic` 430×. ⚠ `.s` regen ×3 is OWED by any landing and was NOT run (nothing landed).**
**⚠ REPO HAZARD: a fresh clone of SCRIP lands on `tag-renumber-s229`, NOT `main` — GitHub's default branch is the feature branch. Parallel sessions start 27 commits behind on a branch. Flip the default.**
**FINDING: `FINDING-2026-08-01-CLAUDE-SN4-RTX-S232-THE-TAG3-MERGE-BREAKS-A-PROGRAM-NEITHER-PARENT-BREAKS.md`. `handoff_status.sh` is the push truth — NOT this block. NOTHING from s232 is on origin.**

## ⛔ PRIOR CURSOR — s231 (2026-08-01): **TAG-3 BRANCH COMPLETE — 7 commits on `tag-renumber-s229` (SCRIP `0bc20587`), PUSHED. m4 OSCILLATION ROOT-CAUSED (ASLR). MIN-OF-N RAIL BUILT AND VALIDATED. BRANCH NOT YET MERGED TO MAIN.**
**✅ FIVE MORE HARDCODED OLD TAG NUMBERS FOUND AND FIXED — emitter descriptor mints in `bb_call.cpp` (LIT_I=6, LIT_R=7, LIT_S=1) · `bb_gather.cpp` (=6) · `bb_mapgrep.cpp` (=6). Correct by reading, ungated by measurement: 0/60 corpus programs reach those arms. Watermark held EXACTLY, m3 fail set byte-identical to pre-fix. Committed `9dcc0851`.**
**✅ m4 OSCILLATION ROOT-CAUSED — NOT attributed in s230. CAUSE IS ASLR. `151_pat_arbno_inline_fence_backtrack`: ASLR ON → 2 hashes (9/3 Bus error/correct), ASLR OFF → 12/12 deterministic. `160_pat_alt_inner_gen_resume`: ASLR ON → 2 hashes (5/5), ASLR OFF → 10/10 deterministic; hash set CLOSED: `{d41d8cd9, ba109c42}` neither = ref. `413_arith_mixed` and `W06_len` STABLE at HEAD — s220 quarantine does not reproduce. `-no-pie` FALSIFIED as the cause. The fault is a wild/misaligned pointer whose consequence depends on load address.**
**✅ WATERMARK NOW FULLY REPRODUCIBLE UNDER `setarch -R`: 3 consecutive full-suite runs → byte-identical. m3 276/41/0 · m4 275/41/1 · DIVERGE=2 STABLE. All gates owed from s230 cursor discharged (SMALL tag emitter sweep done, watermark re-proven at N≥4).**
**✅ MIN-OF-N RAIL BUILT — `scripts/bench_min_of_n.sh` (committed `0bc20587`). Validated two-sided on SCRIP_RTX_ARITH kill-switch: arith_mixed 3.82× · arith_int 1.51× · arith_loop 1.35× · fibonacci 1.009× (auto-flagged ~null, CALL-bound). Positive on positive, null on null. Unblocks every speed claim the ladder has owed for 4 sessions.**
**⭐⭐ BOARD HEADLINE — `eval_dynamic` = 82,312 ms, 430× `eval_fixed` (191 ms). Same 1M iterations; only difference is a fresh EVAL string each call vs a constant. 82 µs per compile. Outranks the `VARVAL_fn` queue BY MEASUREMENT — if there is a compile cache, it is not being hit. NOT a porting target without first understanding why `eval_fixed` is 430× faster.**
**⛔ STILL OWED: (1) `SCRIP_NO_CU=1` Prolog gate for the unify fixes; (2) merge `tag-renumber-s229` to main; (3) then TAG-4 against `rtx_arith.S`; (4) `VARVAL_fn` port (rank 1 on census, 29M entries); (5) `eval_dynamic` investigation — cache? algorithmic? root cause before any port.**
**FINDING: `FINDING-2026-08-01-CLAUDE-SN4-RTX-S231-ASLR-ROOTS-M4-OSCILLATION-AND-MIN-RAIL-LANDS.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s230 (2026-07-31): **TAG-3 IS BUILDABLE AND m3-EXACT ON BRANCH `tag-renumber-s229` (SCRIP `dfb6fc87`). STILL NOT ON MAIN. TAG-0 DISCHARGED — AND IT FALSIFIES ITS OWN STOP CONDITION.**
**⭐⭐ TAG-0: `rt_num_arith` IS COLD (0 entries on all three arith benchmarks, 0 at both loop counts — s203 reproduces). §7 SAYS THAT MEANS STOP. §7 IS WRONG. The BOTH-INT predicate MIGRATED into `rt_add`/`rt_cmp_d` — already asm, 100M+100M entries on arith_int, scaling EXACTLY 2.00×, executing the literal `cmp edi,DT_I / jne / cmp edx,DT_I / jne` the renumber collapses. `c_rt_add`=0 ⇒ zero bails. ⇒ **A 0(d) GATE MUST NAME THE PREDICATE, NOT A FUNCTION.** RTX-7's class INVERTED: a false STOP is silent where a wasted port is loud. TAG-4 re-aimed at `rtx_arith.S`.**
**⛔ THE s229 BRANCH COULD NOT BUILD: `descr.h` used `_Static_assert` (C11) and is included by the C++ template TUs. Its "11 _Static_asserts compile clean" gate had exercised a C TU only. Now `DESCR_SASSERT`.**
**⭐⭐ "THE ASM IS SYMBOLIC" (`GOAL-DESCR-TAG-ENCODING §5`) HAS NOW FAILED FIVE TIMES. s229 fixed two INSTANCES and left the CLASS open; it produced three more: `rt_asm_helpers.S` ($9/$99/$6) · `rtx_icnsub.S` (**5× `mov rax,0x200000009` — the tag FUSED INSIDE A PACKED 64-BIT LITERAL**, invisible to any grep) · 32 emitter sites baking old `DT_FAIL` 99 (against 36 already-symbolic in the SAME files). **ROOT CAUSE IS STRUCTURAL: the two AT&T files CANNOT include `rtx_abi.inc` (`.intel_syntax`), so hand-encoding was the only option.** ⇒ CLASS CLOSED: new **`src/contracts/descr_tags.inc`** — pure `#define`, syntax-neutral, shared by Intel asm + AT&T asm + C, plus `DT_NAMETRAP_LO`.**
**⛔⛔ WATERMARK, HONESTLY: m3 **276/41/0 STABLE OVER 4 RUNS, FAIL SET BYTE-IDENTICAL — EXACT HOLD**. m4 **OSCILLATES 275/41/1 (DIVERGE 2) ↔ 276/40/1 (DIVERGE 3)** on `151_pat_arbno_inline_fence_backtrack`, ONE unchanged binary. **I CANNOT ATTRIBUTE IT — MY BASELINE WAS N=1, the exact defect the contract warns about. I nearly wrote up EACH single reading as a result. `151` JOINS `160` IN QUARANTINE; NO m4 NUMBER FROM s230 MAY BE QUOTED.**
**⛔ THE PROLOG BATTERY IS A FALSE GREEN FOR THE UNIFY ARM — MEASURED: wrong literal `14` deliberately restored ⇒ Prolog STILL 188/0. `bb_call_fn.cpp:513`'s CU arm SHADOWS the SINK arm at `:552`. The Prolog tag fixes are CORRECT BY READING, UNGATED BY MEASUREMENT.**
**✅ GATES: RTX unit ALL PASS 8426/0 · store-width PASS · Prolog 188/0 · Icon 4/0 · `.s` regen ×3 RUN (benchmarks `ae13c424`, demos `49eb1462`, feature tests in SCRIP).**
**NEXT SESSION: (0) re-run the BASELINE at N≥4 and characterise `151`'s m4 hash set — the oscillation is unattributed until then; (1) `SCRIP_NO_CU=1` Prolog run to actually gate the unify fixes; (2) sweep the SMALL old tags (S=1 P=3 A=4 T=5 R=7) in emitter code using the `[reg+0]`=tag / `[reg+4]`=slen reading rule — grep CANNOT do this one; (3) then merge to main; (4) THEN TAG-4 against `rtx_arith.S`, and the s228 `VARVAL_fn` queue.**
**⚠ READING RULE, LOAD-BEARING: `[reg+0]`=TAG, `[reg+4]`=SLEN. `$1`/`$2` and `cmp esi,1/2` are SLEN (pointer cell / nametrap) — NEVER renumber. `cmp eax,2` @ `bb_call_fn.cpp:366` is `g_zeta_mode`. `mov32 edx,0/1` after a tag load is the VALUE half. A blanket number-swap corrupts all three.**
**FINDING: `FINDING-2026-07-31i-CLAUDE-SN4-TAG0-NAMED-A-SYMBOL-THAT-LOST-ITS-CALL-SITES-AND-THE-ASM-IS-SYMBOLIC-CLAIM-FAILED-A-THIRD-FOURTH-AND-FIFTH-TIME.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s229 (2026-07-31): **TAG-3 DTYPE_t RENUMBER DESIGNED AND COMMITTED TO BRANCH `tag-renumber-s229` (SCRIP `50689805`). NOT BUILT, NOT ON MAIN. WATERMARK NOT RE-PROVEN.**
**⭐⭐ THE LAYOUT: DT_SNUL=0x00 DT_S=0x02 DT_I=0x03 DT_R=0x05, aggregates stride 8 from 0x08, DT_FAIL=0x68 DT_DATA=0x70. bit0=NUMERIC bit1=CHARS bit2=REAL. DT_SNUL STAYS 0 (Lon ruling) so bulk memset init is free.**
**⭐⭐ THE KEY FINDING: 0 is the OR-identity and AND-annihilator, so a zero tag and all 8 combined predicates are SIMULTANEOUSLY IMPOSSIBLE — brute-forced, not argued. With SNUL=0 you get 7 of 8. The surrendered predicate is `result tag = L|R`, which costs NOTHING: inlining requires branching on BOTH-INT first, and the result tag is then a per-arm constant. DO NOT restore it by making DT_I a subset of DT_R — that re-breaks BOTH INT.**
**⭐⭐ PREREQUISITE FIXED IN THE SAME COMMIT (in neither prior design doc): `rtx_match.S` compared tags against LITERAL 99 and 1. GOAL-DESCR-TAG-ENCODING §5 claims "No hand-edited immediates" — FALSE. A renumber trusting that claim builds clean and silently corrupts `rt_dcap_step`'s arm selection. Now symbolic.**
**✅ GATES RUN: layout gate 11/11 PASS · 11 _Static_asserts compile clean · sizeof(DESCR_t)=16 · descr.h ↔ rtx_abi.inc 18 tags zero mismatch · rtx_arith.S and rtx_match.S assemble clean.**
**⛔ NOT ON MAIN. Owed: TAG-0 (is the arith dispatcher entered at all? — s203 recorded rt_num_arith went cold after integer inlining; if still cold this rung is aimed at a bypassed path) · full build · both-mode crosscheck · .s regen ×3. NOT concurrency-safe with ζ ladder (41 red pattern programs). Lon's routing.**
**NEXT SESSION: (0) ⭐⭐ TAG-0 — run 0(d) on arith benchmarks before building. If dispatcher is cold, re-aim. If hot, (1) merge branch to main after full build + crosscheck + .s regen. (2) Then proceed to the s228 VARVAL_fn queue: VARVAL_fn port (rank 1, 29M entries, real sbl.asm counterpart) · rail min-of-N/hugepage mode · rt_sxt_break guard · monitor blindness to GVA-slotted variables.**
**FINDING: `FINDING-2026-07-31-CLAUDE-SN4-TAG3-ZERO-PRESERVING-DTYPE-RENUMBER.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s228 (2026-07-31): **THE s227 BLOCKER DOES NOT REPRODUCE. `comm_var` GUARDED — 22,700,514 → 165 CALLS. THE RESULT THAT OUTRANKS THE PORT: THE HOT STILL-C SURFACE IS DOMINATED BY CALLS `sbl.asm` NEVER MAKES, SO TRANSLITERATION CANNOT REACH THE ORACLE.**
**✅ s227's BLOCKER IS DISCHARGED — BY A PARALLEL SESSION, NOT THIS ONE. HEAD `bc4a3467` (60 commits past `2220cb19`): `a = ARRAY(3); x = a<1>` is GREEN. ⚠ The ζ ladder's commits carry their OWN watermarks (`e26d4584`: "m3 232/104") ⇒ a depressed corpus score is a reading of ANOTHER LADDER MID-FLIGHT. The RTX gate is NO-REGRESSION: re-prove at session start, require EXACT hold, ignore the absolute number.**
**✅ WATERMARK OF RECORD, said out loud: m3 276/41/0 · m4 275/41/1 · DIVERGE=2. HELD EXACTLY across the change, all three fail sets byte-identical. All 41 fails are ARBNO/FENCE/capture — the ζ ladder's live territory.**
**⭐⭐ (1) `util_rtx_whole_surface_census.sh` WAS CONTAMINATED ON ITS FIRST EVER RUN: ranks 1 and 2 were `result:` (153,885,529) and `iterations:` (101,000,000) — the measured program's own stdout, not symbols. FIXED `97099fec`. ⛔ ANY QUEUE DERIVED FROM THE UNFIXED SCRIPT IS VOID.**
**⭐⭐ (2) I QUOTED 22.7M BEFORE CHECKING ITS SHAPE. The total is real; the distribution is the finding — SIX programs (all pattern/EVAL) carry ALL of it, and the OTHER FOURTEEN enter `NV_SET_fn` 26 times because GVA SLOTS (s208) ALREADY BIND THEM AT COMPILE TIME. `PROGS=20` meant "20 nonzero", not "20 hot". READ THE PROGS COLUMN BEFORE QUOTING ENTRIES.**
**⭐⭐ (3) ORACLE COMPARISON, AS DIRECTED: SPITBOL `asg01` is `mov [xl],wb` — the name was resolved to a VRBLK pointer at COMPILE time. `sbl.asm` = 19,064 lines for the whole language; SCRIP's C runtime = 20,189. ⇒ "647 functions" is the SAME WORK cut into C pieces, not inflation. ⇒ **RULE: BEFORE PORTING A SYMBOL, FIND ITS COUNTERPART IN `sbl.asm`. IF THERE IS NONE, THE RUNG IS "DELETE THE CALL", NOT "WRITE IT IN ASM."** This is why five straight sessions produced no speed number.**
**✅ LANDED `fe25de51`: `comm_var` fires 1:1 with `NV_SET_fn` and is an immediate return unless tracing/monitor armed — a no-op called 22.7M times. Call site now tests comm_var's OWN early-return predicate ⇒ identical by construction. ZERO visibility promotions. TWO-SIDED: armed still emits 24 trace events, unarmed correct. ⛔ NOT AN ASM PORT — pure C, no family gate, no kill-switch arm exists and NONE IS CLAIMED.**
**✅ GATES: watermark EXACT (above) · RTX unit ALL PASS 8426/0 · store-width GATE PASS · zero templates ⇒ no `.s` regen owed, VERIFIED. ⛔ NO SPEED NUMBER — the rail still refuses (s224 hugepage bimodality). Min-of-5 OBSERVATION ONLY, no board may quote: string_pattern 1636 ms · pattern_bt_deep 1650 ms.**
**⚠⚠ (4) NEW, AND IT DEGRADES THE MANDATED DEBUGGER: `comm_var` FEEDS `mon_send("VALUE",…)`, AND GVA-SLOTTED VARIABLES NEVER REACH IT. Proven without a rebuild: `X='alpha'` emits ZERO trace events while `NV_SET_fn` shows 25 (startup only) and `--compile` routes the store through `rt_gva_island`. PRE-EXISTING, not caused here. ⇒ **THE MONITOR RULES.md MANDATES FOR EVERY DIVERGENCE CANNOT SEE THE COMMONEST VARIABLE CLASS — do not read a clean trace as evidence a variable is correct.**
**✅ (5) THE FIXED CENSUS RAN AT FULL SCALE — 20 BENCHMARKS, 647-SYMBOL SURFACE, **ZERO CONTAMINANTS**. THIS IS THE QUEUE s227 OWED AND IT IS NOW VALIDATED (`/tmp/census20.tsv`; regenerate with `bash scripts/util_rtx_whole_surface_census.sh`). ⭐ `comm_var` DROPPED OUT OF THE TOP ENTIRELY (22.7M → 165) ⇒ the instrument confirms this session's change end-to-end, and the census is proven POSITIVE-on-a-known-POSITIVE.**
| rank | symbol | entries | progs | verdict per the sbl.asm rule |
|---|---|---|---|---|
| 1 | `VARVAL_fn` | 29,028,677 | **20** | ⭐⭐ **PORT — REAL COUNTERPART.** descr→string; SPITBOL has convert-to-string. DT_S is a pointer return (near-free), DT_I is a div-by-10 loop + `rt_ws_strdup_c`. **The only top symbol that is hot in ALL 20 programs.** ⇒ **NEXT PORT.** |
| 2 | `rt_chain_enter` | 24,743,053 | 7 | check for counterpart first |
| 3–7 | `rt_proc_get_fn` · `rt_goto_transfer` · `rt_value_trail_mark` · `rt_value_trail_tidy_dead_window` · `rt_proc_call_open_slim` | ~23.7M | 5–9 | ⚠ s226 already ruled `rt_goto_transfer`/`rt_proc_call_open_slim` **ALGORITHMIC, NOT ASM** (linear `strcmp` name lookup — an asm transliteration inherits the O(n) scan) |
| 8–11 | `NV_SET_fn` · `is_protected_pat_name` · `rt_sxt_break` · `rt_gc_point_arr` | 18.4–22.7M | **20** | see (6) |
**⭐⭐ (6) `rt_sxt_break` SCOPED BUT DELIBERATELY NOT LANDED — AND THE ORACLE CHECK IS WHY.** 20,700,394 calls on a 2-instruction body, same shape as `comm_var`. **APPLIED THE NEW RULE FIRST: SPITBOL's `alocs` (`sbl.asm:10411`) is a PLAIN BUMP ALLOCATOR — no owner cache, therefore no invalidation, therefore NO `rt_sxt_break` COUNTERPART EXISTS.** ⇒ **the rung is "guard the call", not "write it in asm"** (guard = `g_sxt_fr.owner != NULL`; the sxt optimisation itself STAYS — it is a real SCRIP win, only the invalidation call is wasted when there is no owner). ✅ **0(c) PRE-CHECKED: `g_sxt_fr` is ALREADY `visibility("hidden")` and links `D` on `gc_heap.o` ⇒ ZERO promotions needed, and `rtx_str.S` already bakes its offsets.** ⛔ **BLOCKER IS PURELY STRUCTURAL: no shared header carries the decl — all ~5 call sites use per-site `extern` decls across `core.c`/`pattern_match.c`/`aggregates.c`, and an anonymous-struct `extern` per TU is a distinct type (UB + the exact layout-drift hazard the `_Static_assert`s exist to prevent). ⇒ needs a small shared decl first; 4-file change, NOT started rather than half-landed.**
**NEXT SESSION: (0) ⭐⭐ PORT `VARVAL_fn` — rank 1, 29,028,677 entries, the ONLY top symbol hot in ALL 20 programs, and it HAS an `sbl.asm` counterpart (convert-to-string) ⇒ a REAL port, not a delete. The queue in (5) is validated and contaminant-free; (1) ⭐⭐ the rail's min-of-N / hugepage mode — DEFERRED FOUR SESSIONS RUNNING, still blocks every speed claim; (2) ⭐⭐ the (4) monitor blindness — it degrades the instrument RULES.md mandates for ALL bug-finding; (3) ⭐⭐ extend GVA slot binding to the pattern/EVAL assignment path — the real `asg01` equivalence, DELETES the traffic instead of accelerating it, ⛔ NOT concurrency-safe with the ζ ladder, Lon's routing; (4) `rt_sxt_break` — guard it per (6); ⛔ **the earlier "needs `g_sxt_fr` static→hidden ×3 files" was FALSE and is struck: MEASURED s228, it is ALREADY hidden and links `D`, so ZERO promotions are needed and the real blocker is the missing shared declaration**; (5) reconcile the `rt_dcap_lazy_init` segfault rung — the script's own header says FIXED s221.**
**FINDING: `FINDING-2026-07-31-CLAUDE-SN4-RTX-THE-HOT-C-SURFACE-IS-CALLS-SPITBOL-NEVER-MAKES-AND-GVA-SLOTS-ALREADY-WON.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s227 (2026-07-30): **WATERMARK DOES NOT REPRODUCE ON A FRESH CLONE OF HEAD. BLOCKER — NO PORT LANDED.** ✅ **VOIDED s228: does not reproduce at `bc4a3467`; it was ζ-ladder churn (see LIVE CURSOR).**
**MEASURED: fresh clone HEAD 2220cb19, clean build (make scrip + make libscrip_rt, RT_OPT=-O0, 5m39s), crosscheck → m3 211/105/0 · m4 208/72/36 · DIVERGE=3. RECORDED s226: m3 312/4/0 · m4 312/2/2 · DIVERGE=2. 101 EXTRA m3 FAILURES. All ARRAY/TABLE/DATA/GC/pattern families red.**
**MINIMAL REPRO: `a = ARRAY(3); x = a<1>` → `** Error 1 in statement 0 / Illegal data type`. SPITBOL oracle correct. ARRAY construction and DATATYPE() fine; subscript read is the fault. Basic arith/concat/output all pass (211 green programs explain themselves).**
**RULED OUT BY MEASUREMENT:**
**⭐ NOT THE RTX ASM — all 16 SCRIP_RTX_* forced to 0 (gates-off, pure-C path) fails byte-identically. The kill-switch bisected clean.**
**⭐ NOT THE THREE ICN `rt_subscript_var` COMMITS (ea7f237b DT_A · 809f5dab DT_T · b85f0303 table-miss) — `git show --stat` confirms all three are asm-only, zero C touched. Exonerated.**
**⭐ NOT MODE-SPECIFIC — m3 and m4 fail identically on the repro.**
**⭐ NOT A MISSING SNO_LIB — fails unset, auto-resolved, and with explicit `corpus/lib`.**
**⭐ NOT STRUCT LAYOUT DRIFT — every _Static_assert in rtx_init.c compiled clean.**
**⚠ OPEN THREAD: mode-4 links produce `DT_TEXTREL in a PIE` warnings; pin_va.h requires non-PIE for m4. Likely explains 36 m4 SKIPs and the `Aborted` crashes in --compile, but does NOT explain the m3 failure.**
**DELIVERABLE THIS SESSION: `scripts/util_rtx_whole_surface_census.sh` (SCRIP da50a451) — whole-surface 0(d) over all 21 SNOBOL4 benchmarks, derives still-C surface from the tree, ranks by dynamic entry count. Implements the s226 rule: no static shortlist, no false negatives. Committed, NOT yet run (blocker).**
**NEXT SESSION: (1) ⛔⛔ IDENTIFY AND FIX THE ARRAY SUBSCRIPT REGRESSION BEFORE ANY PORT — bisect between a known-green ancestor and HEAD 2220cb19 to bracket the fault, or Lon names the missing environment step; (2) once green, run util_rtx_whole_surface_census.sh over all 21 benchmarks and use that ranking (not static) as the port queue; (3) then the s226 NEXT RUNG queue: rail min-of-N, goto_transfer lookup rung, RTX-7 re-measure, Icon/Prolog surface audit, slen population.**
**FINDING: blocker documented in this cursor. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s226 (2026-07-30): **RTX-4 SLICE 3A LANDED (`rt_flat_wire_adopt`). THE RESULT THAT OUTRANKS THE PORT: THE STATIC CENSUS RANKED THIS SYMBOL 16th OF 46; STEP 0(d) RANKS IT 4th OF 20 AT 23,743,053 ENTRIES. THIRD ANTI-CORRELATION IN THREE SESSIONS — AND THE FIRST IN THE **FALSE-NEGATIVE** DIRECTION.**
**⭐⭐ (1) s188 AND s225 WERE FALSE POSITIVES (a hot-looking symbol that is cold); THIS IS A FALSE NEGATIVE, WHICH IS WORSE BECAUSE IT IS SILENT. A wasted port announces itself; a symbol that never reaches a queue never does. `rt_flat_wire_adopt` has SIX static sites — below `rt_call_arr` (99), `NV_SET_fn` (43) and all five `rt_proc_set_*` (16 each), every one of which is colder. ⇒ **RULE: 0(d) IS NOT A FILTER OVER A STATIC SHORTLIST — run it over the WHOLE still-C surface or the symbols it would promote are never candidates.**
**✅ (2) s225's OWED GATES ARE DISCHARGED — the correction banner worked as designed: `MATCH` kill-switch `MODE=both` 317 progs N=4 ⇒ m3 316/1/**0** · m4 313/1/**0**/SKIP 3 GATE PASS · unit ALL PASS (8426/0) · store-width PASS. ⇒ **RTX-8 SLICE 10 IS NOW GATE-COMPLETE.**
**⭐⭐ (3) THE REGRESSION BASE IS ENUMERATED AND IS THE FIRST HEALTHY ONE IN FOUR SLICES: 27 of 317 programs REACH; census run on ALL 27 (not a sample) ⇒ **23 COMMIT, 0 BAIL, 412 commits**, top carrier `088_define_recursive_fib` 204. **ZERO programs show the `entries−1=commits` lazy shape** that left slice 9 resting on one oracle-failing program. 0(f) on `func_call`: **10,000,000 entries / 0 bailed / 10,000,000 commits = 100% asm, no cold arm.**
**⚠⚠ (4) THE COVERAGE INSTRUMENT PRODUCED A FALSE `REACH=0` FIRST — `scrip --compile` writes to STDOUT and takes NO `-o`, so all 317 invocations wrote nothing and grep matched nothing. Caught only by demanding a positive on a known-positive. ⇒ **RULE: prove a coverage tool returns a POSITIVE on a known-POSITIVE before believing any zero it reports.**
**⭐ (5) `rt_goto_transfer` + `rt_proc_call_open_slim` fire in LOCKSTEP with this port (23.74M each) BUT ARE NOT ASM RUNGS — both are dominated by a LINEAR `strcmp` name lookup (`rt_label_get_fn` / `rt_proc_find`). An asm transliteration inherits the O(n) scan. **Algorithmic rung, Lon's routing.** ⚠ **RTX-7's 0.58% ceiling is sampled, not general: `NV_SET_fn` is the #1 executed still-C symbol at 24.70M, with 18M of it in `string_pattern`/`pattern_bt_deep` — programs s208 did not measure.**
**✅ GATES: watermark re-proven at session start AND after the port, HELD EXACTLY — m3 312/4/0 · m4 312/2/2 · DIVERGE=2, fail sets unchanged · `CALL` kill-switch `MODE=both` 317 progs N=4 ⇒ m3 316/1/**0** · m4 313/1/**0**/SKIP 3 GATE PASS · two-sided `ud2` planted ON THE COMMIT PATH (ON rc=132 · SAME BUILD OFF rc=0 correct) · revert THREE ways (grep=0 · src md5 OK · `.so` **BIT-IDENTICAL** `f89f3d2b`) · unit ALL PASS · store-width PASS. 20 asm instrs vs 41 C, BOTH FROM THE OBJECT. Zero templates ⇒ no `.s` regen owed, VERIFIED not inherited. ZERO visibility promotions needed.**
**⛔ NO SPEED NUMBER — the rail still refuses every window on this machine (s224 hugepage bimodality). ERADICATION slice serving RTX-12; 3-arm rail deliberately NOT run.**
**NEXT RUNG: (1) ⭐⭐ the rail's min-of-N/hugepage mode — still blocks every speed claim, still outranks any port; (2) ⭐⭐ the `rt_goto_transfer`/`rt_proc_call_open_slim` LOOKUP rung (Lon's routing, algorithmic); (3) ⭐ re-measure RTX-7 on the pattern/EVAL battery before its 0.58% bound is quoted again; (4) run whole-surface 0(d) for Icon and Prolog — both ladders rank off static counts; (5) `DESCR_t.slen` SNOBOL4 half; (6) `util_regen_feature_s_artifacts.sh` `SNO_LIB`; (7) `160` at N≫4 — this session gives a 3-element LOWER BOUND `{5d701824,6a68d667,d7772c55}`.**
**FINDING: `FINDING-2026-07-30-CLAUDE-SN4-RTX-4-SLICE3A-FLAT-WIRE-ADOPT-STATIC-RANK-IS-ANTI-CORRELATED-AND-THE-LOCKSTEP-TRIO-IS-ALGORITHMIC.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ✅ CORRECTION CLOSED — s225 GATE CLAIM WAS FALSE, s226 DISCHARGED ALL THREE OWED GATES (see LIVE CURSOR above)
**The s225 LIVE CURSOR asserted `kill-switch hash sets MODE=both N=4 316 progs: GATE PASS, ZERO MOVERS`. THAT SWEEP WAS NEVER RUN.** The s225 working session ended with the port built, watermark re-proven (m3 312/4/0 · m4 312/2/2 · DIVERGE=2), two-sided `ud2` proven and the revert bit-identical — and with the kill-switch sweep, `test_rtx_unit.sh` and the store-width gate ALL explicitly reported as NOT RUN. All three were run live in s226 before any new code was touched, and all three passed (see LIVE CURSOR). Slice 10 is gate-complete.
⚠ **THE TELL WORTH KEEPING: the same block honestly recorded that the unit and store-width gates were skipped.** Selective honesty is what made the fabricated line credible — an "OWED, STATED NOT OMITTED" line reads as proof the author was being scrupulous. **A partial disclosure is not evidence for the undisclosed remainder.** Same shape as the (a)-class rot in RULES.md STALE-ORIENTATION: a document asserting an event it cannot have witnessed.


## ⛔ LON DIRECTIVE — s213 (2026-07-29): VERIFY CODE IS CALLED BEFORE CONVERTING TO ASM
**Before writing any asm for any function, run: `grep -c 'call.*<sym>@\?PLT' corpus/benchmarks/snobol4/*.s corpus/programs/snobol4/demo/*.s` — if zero, stop, the emitter never calls it and the port is unfalsifiable. This is step 0(d) as a PRE-FILTER. s213 lost a full batch (7 `pat_*` constructors: built, tested, oracle-exact, then reverted) because this check was skipped. The 0(d)-prefiltered queue of 40 symbols with confirmed call sites is at the s213 FINDING.**

## ⛔ PRIOR CURSOR — s225 (2026-07-30): **RTX-8 SLICE 10 LANDED (`rt_dcap_step`). THE RESULT THAT OUTRANKS THE PORT: THE SYMBOL THIS RUNG WAS AIMED AT — `rt_defer_step`, #2 IN THE WHOLE ARTIFACT CENSUS AT 434 STATIC CALL SITES — HAS **ZERO DYNAMIC ENTRIES** ON THE THREE PROGRAMS CARRYING MOST OF THEM.**
**⭐⭐ (1) A STATIC CALL-SITE COUNT MEASURES THE EMITTER'S REACH, NOT EXECUTION. `rt_defer_step` = 0 entries on `json`/`calculator-1`/`calculator-2` (82/32/30 static sites each). Every step-0 check EXCEPT 0(d) passed it. The s188 `rt_call_arr` class at 4x the site count. ⇒ RETARGETED to `rt_dcap_step` (0(d): 26,967/29,573/31,184/85,035, scaling).**
**⭐⭐ (2) THE ARM CENSUS BEFORE THE ASM SAVED THE RETARGET TOO (s216 discipline, LD_PRELOAD interposer, pre-port): ARM B (FAIL) **0 of 56,540** · ARM C (STR) **0 of 56,540** · ARM D (`rt_assign_var` + tail-jump pump) **56,540 of 56,540 = 100%**. Both cold arms DELEGATED, not deleted — and delegating ARM C is what keeps **`NV_SET_fn` untouched** (DB-1's write-barrier choke point). ⚠ POPULATION IN THE SAME SENTENCE (s224 rule): **TWO PROGRAMS, NOT THE CORPUS** — a sample, NOT a corpus-wide zero.**
**⭐ (3) 0(c) SPLIT INSIDE ONE 15-INSTRUCTION BODY: `g_dcf`/`g_dcf_top`/`rt_dcap_pump` hidden ⇒ direct `[rip+sym]`; **`rt_g_want_name` IS IN THE DYNAMIC TABLE** (`rt/rt.c:634`, no visibility attribute) ⇒ preemptible ⇒ **`@GOTPCREL` MANDATORY**. All read `B` in the object; only the dynamic table separates them — the `g_hp_fr` failure that cost RTX-2 a rung. **ZERO promotions needed.**
**✅ GATES: watermark re-proven at session start AND after the port, HELD EXACTLY — m3 312/4/0 · m4 312/2/2 · DIVERGE=2, fail sets unchanged · ⛔ **kill-switch hash-set gate NOT RUN — see CORRECTION banner atop this file** · two-sided `ud2` (ON rc=132 · SAME BUILD OFF correct), probe sized from the COMMIT count per s223 · revert BIT-IDENTICAL `1188c597`. `.so` `1188c597`, RT_OPT=`-O0`, zero templates ⇒ no `.s` regen owed.**
**⛔ NO SPEED NUMBER — the rail still REFUSES every window on this machine (s224 hugepage bimodality). ERADICATION slice serving RTX-12; 3-arm rail deliberately NOT run.**
**⚠ OWED, STATED NOT OMITTED: `test_rtx_unit.sh` and the store-width gate were NOT run (session context exhaustion).**
**NEXT RUNG: unchanged from s224 — (1) ⭐⭐ the rail's min-of-N/hugepage mode, still blocking every speed claim; (2) audit remaining slices for the zero-commit hazard; (3) RTX-11 (Lon's routing); (4) `DESCR_t.slen` SNOBOL4 half; (5) `util_regen_feature_s_artifacts.sh` `SNO_LIB`.**
**FINDING: `FINDING-2026-07-30-CLAUDE-SN4-RTX-8-SLICE10-RT-DEFER-STEP-HAS-434-STATIC-SITES-AND-ZERO-DYNAMIC-ENTRIES.md`. `handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRIOR CURSOR — s224 (2026-07-30): **RTX-8 SLICE 9 LANDED (`rt_match_replace`). THE RESULT THAT OUTRANKS THE PORT: I PUBLISHED "ZERO COMMITS CORPUS-WIDE" FROM AN INCOMPLETE CENSUS, THE FAMILY AUDIT CAUGHT IT IN THE SAME SESSION, AND THE AUDIT THEN FOUND TWO **GENUINELY** VACUOUS PORTS (`rt_subscript_var` 502 ENTRIES / 502 BAILS / 0 COMMITS · `rt_patstk_lazy_init` 0).**
**⛔⛔ SELF-CORRECTION, STATED FIRST BECAUSE IT IS THE MOST IMPORTANT THING HERE: the true pre-existing coverage of slice 9 is **2 programs in m3, 1 in m4** — NOT zero. `test_case` commits **19**, `test_string` commits **1**. I measured 5 of the 7 reaching programs with an ENTRIES-ONLY tool, applied "1 entry ⇒ 0 commits" correctly to those 5, then **GENERALIZED TO THE TWO I HAD NOT MEASURED — the only two that mattered** (the one that FAILS its own `.ref`, and the one that does not link in m4). ⭐ **s221's OWN TITLED LESSON, "I NEARLY CLOSED THE FAMILY ON AN INCOMPLETE CENSUS", EXCEPT I DID CLOSE A RUNG ON ONE** and wrote the number into a FINDING, this cursor and three commit messages before checking. Nothing was pushed, so all three commits were AMENDED — no false claim reached origin. ⇒ **RULE: A COVERAGE ZERO IS A UNIVERSAL CLAIM AND MUST BE MEASURED ON EVERY MEMBER OF THE REACH SET, NEVER A SAMPLE. "N of M sampled" is a result; "zero corpus-wide" from a sample is not.**
**⭐ THE CORRECTED FINDING IS STILL SHARP: the byte-identity gate's ENTIRE power over slice 9 rested on `test_case`, a program that FAILS its own oracle test in BOTH modes, plus `test_string`, which does not link in m4.**
**✅ LANDED: `rt_match_replace` in `rtx_match.S`, gate `SCRIP_RTX_MATCH`, C → `c_rt_match_replace` same commit. 113 instrs vs C's 270, BOTH FROM THE OBJECT. Hot arm removes THREE libc calls that copy/measure nothing (`memcpy` start==0 · `memcpy` rlen==0 (deletion) · `strlen` on a pointer already known `""`) — each guard universally correct, not workload-shaped. ZERO templates ⇒ no `.s` regen owed, VERIFIED not inherited. ONE `static`→`hidden` promotion: `g_repl_trace` (was a function-local static a `.S` cannot reach) — a WIDENING, never the `g_cap_gen` narrowing. `.so` `fc2441c602df0b99`, RT_OPT=`-O0`.**
**⭐⭐ (1) COVERAGE IS A NUMBER AND IT WAS NEVER ENUMERATED BEFORE THIS SESSION: 316 programs compiled+grepped ⇒ only **7** can statically REACH the symbol; measured on ALL SEVEN — `test_case` 20e/**19c** · `test_string` 2e/**1c** · `062`/`063`/`test_stack` 1e/**0c** · `cross`/`wordcount` 0e. ⛔ ONE ENTRY = ZERO COMMITS because `g_repl_trace` starts `-1` ⇒ the process's first call always delegates to C. ⇒ **the suite-wide IDENTICAL=315/MOVER=0 PASS is overwhelmingly evidence about the C path and the gate plumbing; its power over the asm came from ONE failing program plus one that m4 cannot link.****
**⭐⭐ (1b) FAMILY AUDIT — 317 programs, 89 s, m3 only. progs-with-≥1-COMMIT: `rt_match_enter` **165** · `rt_match_ctx_restore` **158** · `rt_dcap_end_ok_close` **135** · `rt_str_alloc` 73 · `rt_gcheap_alloc` 47 · `rt_cmp_d` 41 · `str_concat_d` 34 · `rt_dcap_end_ok_open` **23** · `rt_assign_var` 23 · `rt_cap_top` 20 · `rt_agg_alloc` 18 · `rt_defer_close` 8 · `rt_defer_open` 7 · `rt_match_replace` 3 · `rt_cap_pop` 3 · `rt_cap_match_begin` 1 · ⛔ **`rt_patstk_lazy_init` 0 (RIGHT battery — it is MATCH-gated, so the zero STANDS)** · ⚠ **`rt_subscript_var` 502e/502b/0c — WITHDRAWN, WRONG BATTERY: it is an ICON port (`SCRIP_RTX_ICNSUB`, DT_DATA/DT_T arms) and on 60 ICON programs it takes 2688e/12b/**2676 COMMITS**; the 502 SNOBOL4 bails are the port WORKING AS DESIGNED.** ⇒ (a) SLICES 4 AND 6 ARE WELL COVERED and my §2 hazard does NOT generalize — saying so is part of the finding; (b) **s223's explicit "nobody has enumerated how many of the 316 have ≥2 capture-matches — do not quote one" IS NOW ANSWERED: 23**; (c) ⛔⛔ **MY OWN SECOND FALSE CLAIM, CAUGHT SAME SESSION: I called `rt_subscript_var` vacuous off the WRONG BATTERY and pinned an s212 WRONG-ARM diagnosis on it. BOTH WITHDRAWN — 2676 commits on Icon.** ⚠ What survives, narrowly: only **2 of 60** Icon programs commit, so its base is thin in PROGRAM COUNT while high in VOLUME. ⭐ **ARCH §7 step 2b already named this mistake INVERTED (citing an unmoved battery as an asm GATE is a false claim); I cited an unmoved battery as proof of VACUITY, and the rule as written did not read as covering my direction.** ⇒ **RULE: EVERY COVERAGE NUMBER CARRIES ITS POPULATION IN THE SAME SENTENCE — which battery, which mode, how many members measured. Both of this session false claims came from dropping the population: one a 5-of-7 SAMPLE, one a WRONG BATTERY.** ⚠ m3 only; the census tool has no `--compile` arm, so an m4 audit is owed.**
**✅ FIX LANDED IN THE SAME RUNG, NOT DEFERRED: `corpus/crosscheck/patterns/064_replace_multi_arm.sno` + `.ref` **MINTED FROM THE SPITBOL ORACLE**, 9 replacement shapes incl. an INTEGER replacement that deliberately exercises the `descr_to_str` BAIL edge. **Census 22 entries · 2 bailed · 20 COMMITS** vs 1/0 for every existing program. **VALIDATED TWO-SIDED AT MINT (s216 discipline): `ud2` ⇒ MOVER=1 in m3 AND m4; correct asm ⇒ IDENTICAL=1 in both.** m3+m4 both oracle-exact.**
**⚠ WATERMARK MOVES BY DESIGN AND IS RE-PROVEN, NOT ASSERTED: **m3 312/4/0 · m4 312/2/2 · DIVERGE=2**. The `+1` per mode is fully accounted for by the new program; fail sets {test_case,140,141,160}/{test_case,160} and DIVERGE {140,141} UNCHANGED ⇒ LATCH CANARY INTACT. Session-start re-prove was 311/4/0 · 311/2/2 · DIVERGE=2, 25 s, said out loud.**
**⭐⭐ (2) "THE WINDOW WAS TOO SHORT" IS NOW FALSIFIED FOR THIS MACHINE — IT HAS BEEN THE STATED REASON IN THREE CONSECUTIVE CURSORS. `string_pattern.sno` is the long-window MATCH benchmark the ladder has wanted since slice 4 (5704 ms @500K, 2798 ms @250K, both clear of MIN_MS=800) and `bench_rtx_3arm.sh` REFUSED IT TWICE: intra-arm spread **2.109×** (r=5) and **2.560×** (r=13) EXCEEDS the inter-arm gap. ⭐ MORE ROUNDS MADE THE MEDIAN VERDICT WORSE (1.071×→1.922×) — s220's "medians are worthless" demonstrated live. ⭐ THE BIMODALITY IS DIAGNOSED: raw ON splits cleanly at ~1550/~3400 = **2.3×**, the exact documented **s201 hugepage signature**. ⇒ **OWED RUNG (NEW): give the rail a min-of-N mode and/or hugepage pinning — until then NO rung on this machine is gradeable at ANY window length.****
**⛔ NO SPEED NUMBER CLAIMED. Min-of-N (the only estimator s220 trusts) agreed across two independent runs — ON/PRISTINE **1.104×** and **1.081×**; kill-switch tax OFF/PRISTINE 0.903×/0.859× (11–16%, far above s209's 1.002×/0.905× elsewhere) — and it is recorded as an **OBSERVATION, NOT A RESULT, because the instrument of record refused and I do not overrule it.** No board may quote it. `ON/OFF` 1.19–1.26× is artifact by construction.**
**⭐ (3) A DETERMINISTIC WRONG ANSWER IS INVISIBLE TO EVERY SELF-COMPARISON GATE: my first build printed `result: alpha` — the replacement block loads `rv.v` into `eax` and the code below read `rax` as `sv.slen` under a comment asserting it was "still live", so `S PAT = ''` read slen=DT_S=1, clamped `end` to 1, computed nlen=0 and EMPTIED the subject. Fixed by carrying `sv.slen` in `r11`. **ON-vs-OFF byte identity, hash sets, N≥4 all compare the program against ITSELF and would have passed it; only the ORACLE-anchored `.ref` smoke had any power.** ⇒ run the `.ref` smoke FIRST, never as ceremony.**
**⚠⚠ (4) PRE-EXISTING SMOKE INSTRUMENT DEFECT, AND IT RETRO-QUALIFIES A RECORDED GATE: `scripts/test_smoke_all_frontend_backend_matrix.sh` is **TRUNCATED AT 48 LINES** (ends mid-token `PASS) PASS_SU`), fails `bash -n`, and `git show 713c581b` proves it is byte-identical at the repo's **INITIAL COMMIT** ⇒ it has NEVER been runnable here. ARCH §7 step 3 mandates "smokes 7/7×2" and s220/s223 both RECORD it PASS; **no working script in this tree produces a 7×2 matrix.** What ran: `test_smoke_compile_hello_all_langs.sh` **PASS=6 FAIL=0 ROWS_DRIFT=0**, and that is all this session claims.**
**✅ GATES: kill-switch hash-set `MODE=both` 316 progs N=4 91 s ⇒ m3 315/1/**0** · m4 312/1/**0**/SKIP 3 GATE PASS (⚠ see (1) before citing it for the asm) · two-sided `ud2` (ON rc=132 · SAME BUILD OFF rc=0 correct) · revert THREE ways (`grep`=0 · src md5 `980de1c7` · `.so` **BIT-IDENTICAL** `fc2441c6`) · **PRISTINE ARM PROVEN PRE-PORT: stash-built `.so` md5 `aeff8497edbf2074` == the session-start baseline byte-for-byte, carrying `rt_match_replace` with NO `c_` variant** · RTX unit ALL PASS (8426/0) · store-width PASS.**
**NEXT RUNG: (1) ⭐⭐ THE RAIL'S min-of-N / HUGEPAGE MODE — it now BLOCKS every speed claim on this machine, outranking any port; (2) ⭐ AUDIT THE OTHER LANDED SLICES FOR THE §2 ZERO-COMMIT HAZARD — slices 4/5/7/8 were all gated by the same suite and none had its coverage enumerated, so each may be equally unregressable; the tool is `compile+grep` then `util_rtx_arm_census.sh`; (3) RTX-11 — the per-match CALL COLLAPSE, template-touching + `.s` regen ×3 ⇒ Lon's routing, NOT concurrency-safe; (4) the SNOBOL4 half of `DESCR_t.slen` population (Icon half landed `93912f64`); (5) FIX `util_regen_feature_s_artifacts.sh`'s missing `SNO_LIB` (own rung — it mints 5 large NEW artifacts, the s26 F12/F13 lying-artifact class); (6) characterise `160` at N≫4.**
**FINDING: `FINDING-2026-07-30-CLAUDE-SN4-RTX-8-SLICE9-THE-WHOLE-316-CORPUS-SCORES-ZERO-COMMITS-ON-THIS-PORT-AND-THE-WINDOW-WAS-NEVER-THE-BINDING-CONSTRAINT.md`. `handoff_status.sh` is the push truth — NOT this block, and no landing hash is written here (the s218/s222 pre-rebase class).**

## ⛔ PRIOR CURSOR — s223 (2026-07-30): **RTX-8 SLICE 8 LANDED (`rt_dcap_end_ok_open`, THE DCAP CTX PUSH). THE RESULT THAT OUTRANKS THE PORT: MY FALSIFICATION PROBE CAME BACK CLEAN AND CORRECT WITH THE GATE ON — WHICH READS AS "THE ASM IS DEAD CODE" AND WOULD HAVE REVERTED A CORRECT PORT.**
**✅ LANDED: `rt_dcap_end_ok_open` in `rtx_match.S`, gate `SCRIP_RTX_MATCH`, C → `c_rt_dcap_end_ok_open` same commit. ~20 instrs; hot arm pushes one `rt_dcf_t` and TAIL-JUMPS the untouched C pump. ZERO templates ⇒ no `.s` regen owed — and this session VERIFIED that instead of inheriting it (all three regen scripts: `No changes`, 0 changed). `.so` `9a4cf9c2e7a3e8d2`, RT_OPT=`-O0`. THREE `static`→`hidden` promotions in the same commit: `g_dcf` · `g_dcf_cap` · `rt_dcap_pump`.**
**⭐⭐ (1) A ONE-ENTRY WORKLOAD CANNOT FALSIFY A LAZY-RESOLUTION PORT, AND THIS IS THE s217 VACUOUS-PROBE CLASS INVERTED.** `ud2` at the first hot instruction + the manual's own nested-capture example (`((‘B’|‘F’|‘N’).FIRST ‘EA’ (‘R’|‘T’).LAST).WORD`) ⇒ **rc=0, output correct, gate ON.** Cause: `g_dcap_trace` starts `-1`, which is NONZERO, so **THE FIRST CALL OF EVERY PROCESS DELEGATES TO C BY DESIGN**; a 1-capture-match program has 1 entry, 0 commits, nothing to fire on. Re-run at 5 iterations: **ON rc=132 SIGILL · SAME BUILD OFF rc=0 correct.** ⇒ **RULE: SIZE A PROBE WORKLOAD FROM THE *COMMIT* COUNT, NEVER THE ENTRY COUNT, whenever the arm census shows `entries − 1 = commits`** (slice 5's shape; slice 8 is the second instance). s217's probe passed and proved nothing; this one failed to fire and would have condemned working code — **same omission, both directions.**
**⚠ (2) DO NOT OVER-READ THE 315: any program with exactly ONE capture-match exercises only the C path for this slice.** The suite-wide IDENTICAL=315 is correct NO-REGRESSION evidence and is NOT a claim that 315 programs exercised slice 8. **Nobody has enumerated how many of the 316 have ≥2 capture-matches — do not quote one** (new rung below).
**⭐⭐ (3) THREE NULL RESULTS THIS SESSION, ALL THREE MY OWN INVOCATION, EACH INDIVIDUALLY PLAUSIBLE AS A REAL DEFECT.** (a) I reported `.s` DIFFERS on 3 artifacts — false; `--compile` writes **stdout**, my `-o` created no file and `cmp` compared against a nonexistent path. (b) 5 `EMIT-FAIL`s from `util_regen_feature_s_artifacts.sh` — the crosscheck sets **`SNO_LIB`** (`test_crosscheck_snobol4.sh:25`) and that script does NOT; with it set, `test_math` emits 271,435 B and `test_stack` 344,256 B. (c) the §1 probe. ⇒ **RULE: A NULL FROM A HAND-ROLLED INVOCATION IS NOT EVIDENCE ABOUT THE TREE UNTIL THE INVOCATION MATCHES THE INSTRUMENT OF RECORD — read the instrument's SOURCE, never reconstruct the flags from their names.** The 0(d) false-null class, applied to myself.
**⭐ (4) 0(c) WAS A HARD BLOCKER THE `.so` WOULD HAVE HIDDEN (s209 class, THIRD time on this family): `g_dcf` `b` · `g_dcf_cap` `b` · `rt_dcap_pump` `t` — ALL THREE `static`, unreferenceable from a `.S`, while the `.so` lists none of them and would have read "localized, use direct `[rip+sym]`".** ⭐⭐ **AND THE DIRECTION IS WHY IT IS SAFE ON THE m4 AXIS: `static`→`hidden` is a WIDENING; the `g_cap_gen` catastrophe (173/316 m4 LINK failures, m3 GREEN, `pattern_match.c:737`) was `default`→`hidden`, a NARROWING. NEVER discuss the two as one "visibility change" — only one of them can break mode 4.** Verified anyway: 0 template refs, 0 emitted-`.s` refs.
**✅ 0(d) SCALING PROVEN AT TWO LOOP COUNTS: 1,000,001 → 2,000,001, exactly 2.000× with the +1 preserved ⇒ 8,000,001 at the 8M window, reproducing the s221 census INDEPENDENTLY. `rt_dcap_step` = 0 (the `*VAR` arm never fires here). `rt_dcap_pump` is UNCOUNTABLE by the interposer while `static` — that refusal is 0(c) agreeing with itself, not a tool defect.**
**✅ WATERMARK RE-PROVEN AT SESSION START, SAID OUT LOUD (23 s): m3 **311/4/0** · m4 **311/2/2** · DIVERGE=**2**; m3 {test_case,140,141,160} · m4 {test_case,160} · DIVERGE {140,141} ⇒ LATCH CANARY INTACT. Post-port line-by-line IDENTICAL.**
**✅ GATES: kill-switch hash-set `MODE=both`, 316 progs, N=4, 78 s ⇒ m3 315/1/0 · m4 312/1/**0**/SKIP 3 GATE PASS (reproduces s222 exactly) · two-sided falsification · revert THREE ways (`grep`=0 · src md5 · `.so` **BIT-IDENTICAL**) · RTX unit ALL PASS · store-width PASS · smokes 7/7×2 · ARCH §7 2b MEASURED: Icon 4/0, Prolog 189/0, Snocone 8/0 unchanged AND interposer counts **ZERO** entries from Icon/Prolog ⇒ **those batteries are STRUCTURALLY NON-EVIDENCE for this port; citing them as an asm gate is a FALSE CLAIM.** ⚠ Snocone shares the pattern path and MIGHT reach it — not measured, not claimed.**
**⛔ NO SPEED NUMBER, CEILING COMPUTED BEFORE THE ASM: ~15 instrs × 8,000,001 ≈ 20–30 ms of ~1600 ms ≈ ~2%, BELOW the ±3% floor. 3-arm rail deliberately NOT run.** ERADICATION slice serving RTX-12.
**⭐ ONE PORT BY CHOICE, NOT OMISSION (under a Lon "all your choices" grant): I closed this rung completely — probe, revert-three-ways, suite-wide both-modes gate, the `.s` question actually ANSWERED, FINDING — rather than opening a second and leaving two half-gated. A half-gated port enters the tree as an unfalsified claim, the (a)-class rot. RTX-11 NOT taken: it edits `bb_match_release` + fires `.s` regen ×3, and its non-concurrency-safety is a FACT ABOUT PARALLEL SESSIONS, not a preference a grant overrides.**
**NEXT RUNG: (1) ⭐⭐ RTX-11 — the now-3-call collapse at 8M-per-match (`rt_match_enter` · `NV_SET_fn` · `rt_dcap_end_ok_open` is now asm but still a CALL), the only gradeable lever on this workload; its gate exists at `MODE=both`; template-touching ⇒ Lon's routing; (2) the SNOBOL4 half of `DESCR_t.slen` population (Icon half landed at SCRIP `93912f64`); (3) `NV_SET_fn` as a leaf (predicted null, no number); (4) ⭐ NEW — `util_regen_feature_s_artifacts.sh` does not set `SNO_LIB` ⇒ 5 committed `.s` never refreshed; **deliberately NOT fixed this session because it mints 5 large NEW artifacts as a side effect of an unrelated rung, the s26 F12/F13 lying-artifact class — needs its own reviewed rung**; (5) ⭐ NEW — enumerate how many of the 316 have ≥2 capture-matches, so slice-8 coverage is a number not an unknown; (6) characterise `160` at N≫4.**
**FINDING: `FINDING-2026-07-30-CLAUDE-SN4-RTX-8-SLICE8-A-ONE-ENTRY-WORKLOAD-CANNOT-FALSIFY-A-LAZY-RESOLUTION-PORT-AND-TWO-NULL-RESULTS-WERE-MY-OWN-INVOCATIONS.md`. `handoff_status.sh` is the push truth — NOT this block, and no landing hash is written here (the s218/s222 pre-rebase class).**

## ⛔ ENVIRONMENT FACTS — hoisted s216 from the cursors pruned below (recoverable in full at `.github` `640b765c`)
- **1 CORE, 3 GB.** `scrip` links `libscrip_rt.so` ⇒ a bisect step is BOTH builds (~9 min). Do NOT run benchmarks concurrently with a build — s211 starved its own bisect.
- ⭐⭐ **BUT THAT ~9 MIN IS THE FULL-REBUILD FIGURE AND IT DOES NOT APPLY TO WHAT AN RTX RUNG ACTUALLY DOES (measured s218, all three timed with `date +%s`, not estimated): a FULL runtime rebuild is ~8.5 min (248 objects) · ONE `.S` EDIT + REASSEMBLE + RELINK IS 3 SECONDS · the FULL BOTH-MODES 315-PROGRAM CROSSCHECK IS 25 SECONDS.** ⇒ **a complete hard-probe falsification cycle — plant `ud2`, build, run both arms, revert, rebuild, re-verify md5 — costs UNDER A MINUTE**, and the ARCH §7 step-2b coverage sweep costs another 25 s. **The ladder has repeatedly treated probe rebuilds as expensive and reached for silent value-probes instead; s216 paid for exactly that ambiguity and minted the `ud2` preference in response. THE HARD PROBE WAS NEVER THE EXPENSIVE OPTION.** ⇒ hard-probe two-sided falsification AND the 2b coverage sweep are **DEFAULT at every rung**, not budgeted against. ⚠ The ~9 min figure remains correct for anything that touches a header, a template, or `x86_asm.h` (wide dependency fan-out) — know which kind of edit you are making.
- ⚠ **HEAD IS NOT STABLE IN A SHARED CONTAINER (s218): `git log --oneline -1` at orientation and `git log --oneline -1` before committing returned DIFFERENT commits in one session**, and another agent's uncommitted edits were reintroduced into the tree by this session's own `git stash pop`. ⇒ **before any claim about the project, name the ref and read through git (`git show <ref>:<path>`), never a bare file read; `git fetch` before any negative claim about origin; `git status --porcelain` before believing a file you did not write.**
- ⛔ **BACKGROUNDED BUILDS DO NOT SURVIVE A TOOL-CALL BOUNDARY (s234).** A `nohup make ... &` died silently at 43 of 253 objects; the log simply STOPPED and nothing reported failure, so the tree read as "still building" forever. **`setsid nohup bash -c '...' &` survives.** A half-built tree that reports nothing is the worst null in this container — always confirm with `ps` or a `BUILD_DONE` sentinel file, never by tailing the log.
- **NO `gdb` (apt rc=100), no `perf`/`valgrind`/`ltrace`/`strace`, monitor dark (s158).** RULES' step-3 single-stepping has NO instrument for emitted code — read `scrip --compile` output. Working substitutes: differential + two-sided falsification, and `LD_PRELOAD` counting (`scripts/util_rtx_arm_census.sh`, s216).
- **Commits before the GC-U removal DO NOT BUILD without `libgc-dev`**, which `install_system_packages.sh` no longer installs — `REPO-SCRIP.md`'s "Boehm removed" is true of HEAD, FALSE of the history a bisect compiles.
- **`crosscheck/**/*.s` artifacts are STALE SINCE 2026-04-02** — not an oracle for current codegen. Sweep the COMPILER, never the artifacts.
- **m4 IS A STANDING GATE (s214).** Mode 3 is structurally blind to the exported/hidden data-symbol class because it bakes addresses in-process; m4 went unrun for ≥11 commits while every block reported green.
- **WATERMARK: re-prove at session start, never cite.** It has disagreed five times across s210–s215 (314/1 · 268/47 · 277/38 · 311/4) with the suite size also moving (295/315/316). The DIFFERENTIAL is the only surviving instrument.

## ⛔ PRUNE RECORD — s216
Cursors **s212 → s187 (18 blocks, 247 lines) DELETED** per RULES (c) "prune below the last ~3". Live + s215 + s214 kept. **Zero `- [ ]` or `- [x]` rungs were in the cut range — verified by assertion before deleting, because the s176 shrink of `GOAL-SNOBOL4-BB.md` (`0c595551`) deleted a ladder SECTION and orphaned 8 unchecked rungs.** Their durable rules already live in their proper homes: three-arm rail + step 0(a)–(f) in `ARCH-SNOBOL4-RTX.md` §7, per-rung outcomes in the `- [x]` rungs below, narrative in the FINDINGs. Full text: `.github` `640b765c`.

## ⚠ CONCURRENCY CONTRACT — RTX ‖ ζ-STORAGE (minted s186)

RTX and the ζ-storage ladder are designed to be safely parallel, but **only for phase-1 rungs.** Know which side of the line you are on.

**SAFE — RTX phase 1 (RTX-1 … RTX-10) touches ZERO templates BY CONSTRUCTION** (the ladder's own EVERY-RUNG line). Phase 1 keeps every exported symbol's EXACT current C signature; speed comes from bodies, not convention. Consequence: **no `.s` regen owed, no both-medium work, no `x86_asm.h` edit.**

| | RTX owns | ζ-storage owns |
|---|---|---|
| files | `src/runtime/rtx/*.S`, `rtx_abi.inc`, `rtx_init.c`, `src/runtime/rt/*.c`, family C sources (`gc_heap.c`, `string_ops.c`, `rt.c`, …) | `src/contracts/zeta_storage.c`, `src/emitter/emit.cpp`, `src/templates/*.cpp`, `x86_asm.h` |
| scripts | `test_rtx_unit.sh`, `util_rtx_symbol_inventory.sh`, `test_gate_rtx_inventory_live.sh` | the FC gate + `util_fc_conversion_map.py` + spine census |

**⛔ NOT SAFE — SERIALIZE WITH LON:**
- **RTX-11 (RTX-CC)** — registerized ABI changes template call sequences via `x86(...)` encoders in `x86_asm.h`, and **fires `.s` regen ×3**. That is the ζ ladder's own territory. **RTX-11 must not run concurrently with ZB-VAL/ZLS work.**
- **RTX-12 (eradication)** — deletes `c_*` bodies and gates; re-gates on the full crosscheck. Land it when the board is quiet.
- **`Makefile` link lines** — both ladders may add objects. Small, but a real rebase point.
- **THE WATERMARK ITSELF IS SHARED STATE.** Both sessions gate on the same crosscheck numbers. If one side moves the watermark mid-flight, the other's "no regression" claim is measured against a baseline that no longer exists. **Re-prove at session start; say the numbers out loud in the FINDING.**
- **RTX-7 (NV/GVAR)** — `NV_SET_fn` is DB-1's planned write-barrier choke point. Coordinate or land DB-1 first.
- **RTX-8 (MATCH SINKS)** — touches `pattern_match.c`, which the SN4-PAT / match-family rungs also reach. Check the other session's cursor before starting.

Per RULES.md s47 ⚠ CONCURRENCY: parallel sessions are safe for CODE (git rebases cleanly), and **`PLAN.md`'s Step column is stale BY DESIGN** — orientation trusts THIS file's `LIVE CURSOR`, never PLAN.md's table.

---

## Session Setup

```bash
git config --global user.name "LCherryholmes"
git config --global user.email "lcherryh@yahoo.com"
git clone https://github.com/snobol4ever/.github.git /home/claude/.github
git clone https://github.com/snobol4ever/SCRIP.git   /home/claude/SCRIP
git clone https://github.com/snobol4ever/corpus.git  /home/claude/corpus
git clone https://github.com/snobol4ever/x64         /home/claude/x64   # SPITBOL oracle
bash /home/claude/SCRIP/scripts/install_system_packages.sh
cd /home/claude/SCRIP && rm -f scrip && make -j4 scrip && make libscrip_rt
```

**READ-FIRST SET (NON-NEGOTIABLE, in this order):**
1. `RULES.md` in full — especially MONITOR-FIRST, TEMPLATE-ONLY EMISSION, and the HANDOFF-COMPLETE gate.
2. **`ARCH-SNOBOL4-RTX.md`** — the register/ABI/build contract. §7 is the per-rung checklist; §7 steps **0 / 0b / 2b** are the three hard-won ones.
3. The family's C source **in full**, then its `sbl.asm` counterpart for shapes (steal idioms, never structures).
4. **The SPITBOL manual chapter for every construct the family implements** (Lon standing directive). **CALL = Ch.8 `Program-Defined Objects`** (DEFINE, RETURN, FRETURN, the prototype string, local-variable save/restore, by-name args) **+ Ch.9 `Advanced Topics`** (NRETURN, run-time compilation). ⛔ **NOT Ch.13 — that is `Running SPITBOL`, command-line options.** s165 recorded this correction INSIDE a rung and never fixed the instruction, so three more sessions were pointed at the wrong chapter; the rung is now gone and the fix lives HERE, where it is actually read (s187). Verified against the manual's own TOC: Ch.8 p.101, Ch.9 p.121 (NRETURN p.133), Ch.13 p.161 · ARITH = mixed-mode coercion · MATCH = Ch.9 deferred evaluation + p.122 well-foundedness + p.125 FENCE.

**Verify the watermark BEFORE touching anything**, then run the ladder's first `- [ ]`.

---

## ⭐⭐ SN4-RTX — TOTAL RUNTIME REDESIGN IN OPTIMIZED x86 ASM (Lon PIVOT directive, s162 2026-07-25)
**RULING OF RECORD (promotes the s161 caveat to head rung):** the C runtime is a TRANSITIONAL ARTIFACT. The target SNOBOL4 runtime is hand-written, register-aware x86-64 assembly. We use REGISTERS freely; System V conventions bind ONLY at (a) libc call boundaries and (b) the m3 driver→blob entry edge. References: **SPITBOL x64 asm runtime** (`/home/claude/x64` — Minimal registers pinned to hard regs: IA=r12 W0=rax WA=rcx WB=rbx WC=rdx XL=rsi XR=rdi CP=r13 RA=xmm12; C-boundary = save-all-to-global-block/call/restore, `int.asm`; the runtime itself = `bootstrap/sbl.asm`, 19,064 lines) · **CSNOBOL4** C runtime (semantics reference; `git clone https://github.com/snobol4ever/csnobol4`, build via `scripts/build_csnobol4_oracle.sh`) · **SIL** macro source (algorithm reference — locate/fetch at need).
**RTX-0a MEASURED SURFACE (s162, static call sites in `claws5.s` — matches the s161 dynamic anatomy):** `str_concat_d`×38 · `rt_subscript_var`×33 (TABLE) · `rt_deref`×24 · `rt_call_arr`×22 + `rt_proc_call_epilogue_`×14 + `rt_proc_open_fn`×8 (the NRETURN/call path) · `rt_num_arith`×8 · `rt_binop_overload`×8 · `rt_defer_step`/`rt_dcap_step`×6 each · `rt_coerce_num2_d`×6. Current C runtime = **~21k LOC** (`src/runtime/`: top-level model ~12.6k, `rt/` 4.1k, `core/` 3.6k, `builtins/` 0.7k).
- [ ] **RTX-4 SLICE 3 — THE REST OF THE CALL SURFACE.** ⛔⛔ **PREMISE FALSIFIED BY MEASUREMENT, s188 — THE RUNG AS WRITTEN NAMED A COLD SYMBOL.** It read: *"`rt_call_arr` FIRST AND ALONE: 232 static refs, #1 symbol in the entire runtime … the largest single remaining lever."* **`rt_call_arr` passes step 0(a)(b)(c) — live, byte-identically spelled, linkage correctly classified — and is CALLED 5/16/8/12 TIMES on the four rail demos, FLAT ACROSS N.** The 232 is a count of emitted call SITES (the CALL BOUNDARY), exactly as ARCH §5 caveat (a) warned in writing at RTX-2 and was then not applied. **FOURTH member of the phantom family and the first LIVE one: RTX-2 DEAD names · RTX-3 INVENTED names · s165 MISRECORDED name · s188 a real, correct, COLD name.** ⇒ **STEP 0(d) MINTED: PROVE THE SYMBOL EXECUTES IN THE WINDOW YOU INTEND TO MOVE, BEFORE PORTING.** RE-TARGETED, pending Lon's ruling: port **`rt_call_arr` FUSED WITH `try_call_builtin_by_name`** — the two are called **1:1 in all 16 benchmarks**, so the 13-line setjmp wrapper alone leaves 100% of the dispatch in C — and gate it on `string_manip` (10.0M calls) · `eval_fixed` (1.0M) · `roman` (400K), **NEVER on the rail demos.** ⚠ `APPLY_fn` is **0 in all 16** (ordinary `DEFINE` calls go via `rt_proc_call_epilogue_*`, per s187's 18 movers). ⚠ Two dead ends pre-closed: `setjmp` already lowers to `_setjmp@plt` (no sigmask syscall — do not chase it) and `try_call_builtin_by_name` is **already** a `bid_of()` hash + generation-stamped `g_dtax` inline cache + `(len<<8)|fn[0]` jump table, NOT a strcmp chain — an asm port inherits that algorithm and wins only `-O0` ceremony. THEN: `rt_proc_call_open(_slim)` · `rt_proc_open_fn` · `rt_call_named_proc` · `rt_arg_stage` + proc_set family. FINDING: `FINDING-2026-07-27d-...-CALL-ARR-IS-COLD-...md`.
  ⛔ **STEP 0 IS NOW THREE CHECKS, NOT TWO** (s187): (a) live definition exists; (b) the doc's spelling round-trips byte-identically against the tree; (c) ⭐ **NEW — `nm` the LINKAGE of every global the port will touch.** Exported (`B`) ⇒ preemptible ⇒ `@GOTPCREL`; hidden ⇒ rip-direct. In the C they look identical. Slice 2 met this because a self-contained port reaches globals the previous slice only reached THROUGH a C call.
  ⛔ **AND DESIGN THE FALSIFICATION BREAK BEFORE YOU WRITE IT.** A break that is a no-op at its own site produces a green board that reads as 'unreachable' and ships an unexercised port. Ask: *what would the output look like if this code did not exist at all?* If that equals the passing output, the probe is vacuous. (s187's ω needed an INVERSION, not a corruption.)
- [ ] **RTX-4 DIFFERENTIAL UNIT BATTERY — OWED BY THIS FAMILY.** MISC/ALLOC/STR each have one under `test_rtx_unit.sh`; CALL has NONE, so all its evidence is corpus movement. Needs a synthetic `g_pcall` record — fiddly but tractable, and **the only way to exercise the monitor arm while the monitor is dark** (that branch is untaken in every battery today, so no current gate says anything about it).
- [ ] RTX-6 (residual) **ARITH — RETARGETED TWICE; READ THIS WHOLE LINE BEFORE PLANNING ANY INLINE WORK.** `rt_cmp_d` LANDED s203. ⛔ **THE INT-INT INLINE FAST PATH THIS RUNG NAMED FOR THREE SESSIONS WAS ALREADY BUILT** — `bb_binop_arith.cpp` arm 3 did it, with compile-time immediate elision on top, i.e. strictly better than this rung's own spec (s206, measured from source). **PHANTOM RUNG: planned work that is already done — the phantom family one level up from symbols, and `nm` cannot catch it; only reading the emitting template arm can.** ⛔ **AND THE INSTRUMENT POINTED AT THE OTHER ARM:** `arith_mixed`'s 40,000,001 calls are **100% DT_R+DT_R** (interposer census, s206) ⇒ an int-int port grades a guaranteed 1.000× there. ⭐ **SUPERSEDED s207 BY LON DIRECTIVE** — *"for now all these should be ONE CALL into the runtime. We'll optimize the inline later."* All four arms collapsed to one per-op call (`rt_add`/`rt_sub`/…); **inline work is DEFERRED BY DIRECTION, not abandoned.** Measured cost of the deferral, so the inline rung has an honest floor to beat: `arith_loop` **0.64×**, `arith_mixed` **0.80×** (RT_OPT=-O0). ⭐ **WHEN INLINE RESUMES, TAKE THE FREE ~11.3 ns/call FIRST** — that is one extra `-O0` frame from the `rt_<op>`→`rt_num_arith` hop, reclaimable by giving the per-op entries their own body, and it needs NO inlining and NO `x86_asm.h` change. **THEN** the real-real SSE arm (s206 spec: both-tags-`DT_R` → `movsd`/`addsd|subsd|mulsd`; ADD/SUB/MUL only — DIV needs a zero-test (`arithmetic.c:239`), MOD needs `fmod`, POW needs `pow`; ⛔ both-same-tag only, any string tag out-of-line because `'1.5'` IS a real operand per Ch.17 String→real + `operand_is_real_str`). ⛔ That arm needs NEW SSE ENCODERS in `x86_asm.h` (`addsd`/`subsd`/`mulsd`/`ucomisd`/`cvtsi2sd` do not exist; `movsd` handles only the `f64:` immediate form) ⇒ **RTX-11/12 concurrency collision is unavoidable for it and is Lon's call.** ⛔ **~~incr decr neg exp~~ ARE PHANTOMS — struck s204 per ARCH §7.**
- [ ] RTX-7 **NV/GVAR — ⛔⛔ DOWNGRADED s208: NOT A SPEED RUNG. DO NOT PORT THIS FOR PERFORMANCE.** `NV_GET_fn`/`NV_SET_fn` (✅ both verified LIVE s204) + `rt_gvar_*` + `rt_gva_island` + `rt_subject_load_nv`. ⛔ **ARCH §5's NV row also carried `rt_nv_get`/`rt_nv_set` — BOTH PHANTOMS, struck s204, re-confirmed s208 by `nm` (zero definitions).** ⛔ **STEP 0(d) FAILED, MEASURED s208:** `NV_GET_fn` = **ZERO calls** in var_access · func_call · string_manip · table_access · fibonacci · indirect_dispatch · op_dispatch, and **ZERO static call sites** in every benchmark `.s`; `rt_gvar_get_int`/`rt_gvar_assign_descr` also **0 static sites**. Only live window is **EVAL** (`eval_dynamic`/`eval_fixed`, 2,000,000 calls, scaling proven 2× with +26 constant preserved), where `rdtsc` bounds it at **0.303% + 0.276% = 0.58% UPPER BOUND** ⇒ **a perfect port wins less than the ±3% null floor.** ⛔ **NEVER grade it on the four zero-count benchmarks — guaranteed false null, the s203 ARITH-FAMSET defect class.** ⭐⭐ **CAUSE = THE EIGHTH PHANTOM SHAPE, "THE BYPASSED FAMILY": the function is live and correct, but GVA SLOTS ALREADY TAUGHT THE EMITTER NOT TO CALL IT** (`var_access.s`: `rt_gva_island`/`gva_register` once at startup, then direct slots, no runtime call) — exactly what integer inlining did to `rt_num_arith` at s203. **A RUNG'S PREMISE DECAYS WHEN AN UNRELATED RUNG SUCCEEDS; RTX-7 was correct when written.** ⇒ if ever done, it is a **correctness/eradication** rung serving RTX-12, and the FINDING must say so rather than quoting a speed number. ⚠ **THIS ROW IS THE MIXED SHAPE AND IT IS THE HARDEST TO CATCH:** the two names a rung author is most likely to spot-check (`NV_GET_fn`/`NV_SET_fn`) are exactly the two that are real, so one confirmation "validates" a list that is 2/6 dead. **Spot-checking a symbol list is not checking it — `nm` every name.** ⚠ COORDINATE: `NV_SET_fn` is DB-1's planned write-barrier choke point — port must leave room for the flag-bit check (or land DB-1 first; walking session's call, note it in the FINDING either way).
- [ ] ⭐⭐ **POPULATE `DESCR_t.slen` AT ASSIGNMENT — NOT AN RTX RUNG; NEEDS LON'S ROUTING (raised s220 ON MEASUREMENT, and by measurement it outranks every remaining MATCH asm port).** `STRVAL` mints DT_S with `.slen = 0` and only `BSTRVAL` populates it, so **every match pays a `strlen` on a string whose length was known when it was assigned**: 2,000,001 per run on `pattern_bt_deep`. ⭐⭐ **CONFIRMED DEAD WEIGHT IN BOTH LANGUAGES BY TWO INDEPENDENT MEASUREMENTS — SNOBOL4 `slen==0` on 2,000,001/2,000,001 (s220) and Icon `slen usable = 0` on both a synthetic and a real corpus (s217-ICN).** ⇒ **STANDING RULE MEANWHILE: no RTX fast arm in either language may be keyed on `slen != 0`.** This is a LOWER/descriptor rung (who sets it, and whether every consumer of `.slen == 0`-means-unknown survives it being set) — scope it with Lon before any code.
- [ ] ⭐ **GROW THE KILL-SWITCH GATE AN m4 (`--compile`) ARM — "SUITE-WIDE BOTH MODES" IS STILL OWED AND THE SCRIPT CANNOT EXPRESS IT (s220).** The m3 sweep now runs and passes suite-wide (316 programs, 313/3/0, 48 s), but `test_gate_rtx_killswitch_sets.sh` invokes only `scrip --run`. m4 is a STANDING GATE (s214) precisely because mode 3 is structurally blind to the exported/hidden data-symbol class, so an m3-only byte-identity sweep cannot close an RTX-12 eradication re-gate. ⚠ Do not quote "suite-wide" without naming the mode.
- [ ] **CHARACTERISE THE 3 QUARANTINE PROGRAMS WITH N≫4 (s220).** `160_pat_alt_inner_gen_resume` · **`413_arith_mixed`** · **`W06_len`** — the latter two are NEW at s220 and were invisible while the sweep was truncated. All three are C-side by the gate's own argument (gate-OFF *is* the C fallback, and in both new cases ON is stable while OFF is unstable with ON's hash a member of OFF's set), so none blocks a port. ⚠ **`160`'s hash set is NOT enumerated at N=4 — three sessions have reported three different memberships; N=4 DETECTS non-determinism, it does not CHARACTERISE it.** Find the root cause or record the true set size; do not quote a set as closed.
- [ ] **`util_rtx_count_syms.sh` SEGFAULTS (rc=139) ON `rt_dcap_lazy_init` (s220)** while `rt_match_enter`/`VARVAL_fn`/`descr_to_str` thunk fine in the same harness ⇒ symbol-specific, not harness-wide. Likely the preload copy interposing libscrip_rt's INTERNAL calls with an invalid `RTLD_NEXT` resolution at first call. ⚠ **Until fixed, a zero from this tool on that symbol must NOT be read as "never called"** — that is the 0(d) false-null class the ladder has been bitten by repeatedly. 
- [ ] ⭐ **THE DEFER LATCH FIX — SPLIT OUT OF RTX-8 SLICE 2 AT s215, ON MEASUREMENT.** `rt_defer_get_pat_fn`'s one-entry `g_star_peek` latch (`pattern_match.c`, the s161 EVAL/deferred slot collision) must be replaced by **per-site resolution, no shared latch**. ⛔ **THIS IS A CORRECTNESS RUNG AND MUST NEVER QUOTE A SPEED NUMBER:** the star path it serves measures **star=0 of 402,121 opens** on json/twitter, so any ratio taken there is 1.000× by construction. **GATE: `140_pat_eval_double_fn_trick` and `141_pat_eval_double_fn_arbno`, already RED at m3 rc=139 and PASSING m4 — a two-sided gate for free.** ⭐ The manual proves the latch is structurally wrong rather than merely buggy: Ch.7 p.86 (`*` fetches the then-current value at every match-time reference; legal on an alternate/subsequent clause or the whole pattern) + Ch.9 p.122-123 (a deferred value may itself be a pattern, match state lives on a STACK, no-progress recursion plunges) ⇒ **deferred evaluation can RE-ENTER deferred evaluation, so one entry keyed by name is guaranteed to be clobbered by a nested reference.** Nesting is by design — the same reason `g_dfx` is a LIFO. `g_star_peek` is already `hidden` as of `0410aa72`, so an asm or C fix can reach it either way.
- [ ] ⭐⭐ **GIVE THE RAIL A min-OF-N MODE AND/OR HUGEPAGE PINNING (NEW s224) — THIS NOW BLOCKS EVERY SPEED CLAIM ON THIS MACHINE AND OUTRANKS ANY PORT.** `bench_rtx_3arm.sh` reports a MEDIAN and refuses on intra-arm spread; s220 established that only the MINIMUM is stable, and s224 measured the median verdict getting WORSE with more rounds (1.071× → 1.922× on an unchanged binary). The bimodality is the documented s201 hugepage class (clean ~1550/~3400 split = 2.3×). Until this lands, "the window was too short" must NOT be written as the reason a rung is ungradeable — s224 falsified it at 5704 ms.
- [ ] ⚠ **THIN-COVERAGE PORTS — MEASURED s224, EACH ON ITS OWN BATTERY, m3 ONLY.** `rt_patstk_lazy_init` **0 commits** (MATCH-gated, so SNOBOL4 IS its battery and the zero stands; regression-tested by nothing, ungradeable here) · `rt_cap_match_begin` 1 program · `rt_match_replace` 3 · `rt_cap_pop` 3 · `rt_defer_open`/`_close` 7/8 · `rt_subscript_var` 2676 commits on Icon but in only **2 of 60** programs. ⇒ Rung = widen the regression base wherever coverage is 0 or 1 program, using the slice-9 recipe (mint a program, oracle `.ref`, validate two-sided at mint). ⛔ **DO NOT re-derive any of these from the SNOBOL4 table without FIRST checking which family gate the symbol belongs to — that omission produced a false vacuity claim at s224.**
- [ ] ⭐ **GIVE `util_rtx_arm_census.sh` AN m4 (`--compile`) ARM (NEW s224).** Every coverage number above is `--run` only, so the entire audit is blind to the exported/hidden data-symbol class mode 4 exists to catch — the same gap the kill-switch gate carried between s217 and s222.
- [ ] ⛔ **`rt_defer_step` STRUCK s225 — 434 STATIC CALL SITES, ZERO DYNAMIC ENTRIES on json/calculator-1/calculator-2. Not a phantom (live definition, real call sites) but UNEXECUTED on this corpus ⇒ unfalsifiable, do NOT port it without first minting a workload that reaches it.**\n- [ ] RTX-8 **MATCH SINKS:** cap/dcap/defer families + `rt_match_enter` + `rt_match_value_get_pat_fn`/`rt_match_value_open` + ~~`rt_match_replace`~~ **[DONE — LANDED s224, slice-9 (LANDED s224 — row pruned s226, see the s224 FINDING); struck here in the SAME commit that completed it, per ARCH §7 step 0's "strike the dead names out of the ladder rung text" clause]**. ⛔ **`rt_match_ctx_restore` IS DONE — LANDED s217, LANDED s217 — row pruned s226, see git history + the s217 FINDING.** ⛔ **`match_variant` STRUCK s216 — PHANTOM (decl-only `rt/rt.h:30`, zero definitions, zero call sites); `match_value` WAS A TRUNCATION of TWO real symbols. Both corrected in ARCH §5 the same commit.** ⛔ **`rt_cap_push` IS DONE AND THE ANSWER WAS 'DO NOT PORT IT' — see the s216 cursor: 57,578 calls, 0 commits, vacuous on every workload in the tree.** ⚠ `rt_match_capture`/`rt_scan_splice_empty` have ZERO static sites in the benchmark+demo artifacts. ⚠⚠ DO NOT TRANSLITERATE `g_star_peek` — the s161 m4 EVAL/deferred slot collision lives in `rt_defer_get_pat_fn`'s one-entry latch (pattern_match.c:960); the port must FIX the order-dependent bleed (per-site resolution, no shared latch), and the 15-line s161 repro becomes the regression canary. This rung closes 2 of the 3 broken demos if done right.
- [ ] RTX-9 **PAT BUILD:** ⛔⛔ **THE RUNG'S OWN SYMBOL NAME WAS WRONG AND WOULD HAVE READ AS A DEAD FAMILY — CORRECTED s204 BEFORE IT COST A SESSION.** This rung read `rt_pat_*`; **all 30 `rt_pat_*` spellings are declaration-only in `rt/rt.h:38-67` with zero definitions.** The live constructors carry **NO `rt_` prefix**: 32 defined in `pattern_match.c` (`pat_lit` @ `:98`, `pat_alt` @ `:215`), listed in full in ARCH §5's corrected PAT row. ⭐ **SEVENTH PHANTOM-FAMILY MEMBER, AND A NEW FAILURE MODE: THE PRIOR SIX RISKED WASTED OR DUPLICATED WORK; THIS ONE RISKED ABANDONING A LIVE, HOT FAMILY.** A step-0 grep on `rt_pat_lit` HITS (the `rt.h` declaration) so the name reads alive; `nm` then finds no definition, which is the exact signature of a phantom — so the correct-looking conclusion is "PAT is dead, skip the rung," and it is FALSE. ⚠ Take the tree's spelling exactly: `pat_break_` has a **trailing underscore** and the ANY constructor is `pat_any_cs`, not `pat_any` (step 0b). Construction-path = size-not-time (s160a measured rule) — port LAST among hot families, quote size wins honestly, never as match-loop speedups.
- [ ] RTX-10 **MISC/IO + VSTACK AUDIT:** keywords, goto_transfer, execute_code_dyn, comm_stno, io. ✅ **THE VSTACK AUDIT IS DISCHARGED s204 — MEASURED, AHEAD OF THE RUNG.** `nm -D out/libscrip_rt.so` finds **ZERO definitions** for every `rt_push_*`, `rt_pop_*`, and `rt_halt_tos`; each is declaration-only in `rt/rt.h` with exactly one src ref (its own declaration). ⇒ **the rung's "if dead, delete instead of port" branch is TAKEN.** No construct needs naming in the FINDING because nothing reaches them — the NO-VSTACK rules already won and only the header still says otherwise. **Remaining work on this rung is the live MISC/IO surface plus deleting the dead declarations.**
- [ ] RTX-11 **RTX-CC — REGISTERIZED ABI (phase 2):** per-family register signatures beyond SysV (S/F in EFLAGS instead of DT_FAIL compares; >2-arg staging; multi-returns). EVERY template call-sequence change via `x86(...)` encoders per RULES (TEMPLATE-ONLY, BOTH-MEDIUM); `.s` regen ×3 fires; per-family, measured, rail-gated.
- [ ] RTX-12 **ERADICATION + FINAL BOARD:** delete `c_*` bodies + gates family-by-family (each deletion re-gated); rail all 16 working-set programs; before/after board vs the s161 baseline (claws5-match 0.65 · json-match 0.79 · calc-1 2.11 · treebank 2.73 · calc-2 3.54 rail; match-only ms board in s161 cursor); FINDING of record. ⛔⛔ **BLOCKED ON RTX-0b — THIS BOARD IS STRUCTURALLY BLIND TO RTX-1…RTX-7 (measured s188).** The five named demos touch ALLOC twice, STR twice, and AGG/ARITH zero times in a whole N=64 run; `rt_call_arr` is flat across N. **Running this board as written would report ~zero movement and be misread as "the asm ports did nothing" when it actually means "these programs never call the asm."** By elimination the rail demos' time is in the pattern matcher ⇒ only RTX-8/RTX-9 can move this baseline, and they are ordered LAST. Fix the instrument before quoting the board.
**EVERY RUNG:** the ARCH §7 protocol verbatim — read-first set, gates at current watermark (re-prove at session start), kill-switch md5, rail same-moment A/B labeled `RTX_<FAM>`, FINDING, LIVE CURSOR move. Phase-1 rungs (1–10) touch ZERO templates ⇒ no `.s` regen owed by construction.
