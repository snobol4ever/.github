# GOAL-SNOBOL4-RTX.md — The SNOBOL4 Runtime in Optimized x86-64 Assembly

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔

**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. We do not do that here. ██**
**ENFORCEMENT: every diff is checked for new file-scope definitions; a commit adding one without a cited in-chat grant in its message is REJECTED on sight. Precedent: the g_pcall / g_pcall_wires / RT_AB_ANCHOR eradication (s55) — that entire class is what this rule forbids recreating.**
**⛔ THE ASK ITSELF MUST BE A BANNER: any session requesting this permission MUST display the request in-chat as a large unmissable ⛔ banner — the proposed global's name, type, owning file, purpose, and why registers/the stack cannot carry it — so Lon cannot miss the ask.  A quiet or inline ask does not count as asking. (Lon 2026-08-13 s55, in-chat.)**


## ⛔⭐⭐⭐ CROSS-GOAL BULLETIN — 2026-08-10 s12: LADDER WREG IS LIVE, r10/r11 ARE CLAIMED WIRE REGISTERS (owner-directed cross-post; authority = `GOAL-PASSTHRU-RBP-ERAD.md` LADDER WREG, `.github` `6de2dbff`)

**Lon runs 5 seats concurrently: PASSTHRU-RBP-ERAD + RTCC + ZETA-MECH + ZETA-CLIMB + SNOBOL4-RTX.** Charter, verbatim: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11."* Pattern blobs stop being procedures: the `proc_PAT$N_α` shim (304B carve · γ/ω wire stores · SCANBASE fills · `g_zctx` push · resume-slot store) and its CLASS-D exit ceremony are DELETED; call sites enter the blob's FIRST INTERIOR BOX directly with `lea r10,[rip+site_γ] · lea r11,[rip+site_ω] · jmp <first_box>`; exits are `jmp r10` (success) / `jmp r11` (fail after drain). **rΓ=r10 · rΩ=r11 are RESERVED product-wide, all three glue kinds, ONE authority (a per-kind split is the mixed regime ZW16 convicted).** LAW: blob-interior pending cells capture/restore the pair at push/β, else it is `g_blob_ctx`'s single-cell defect in register clothing.

**WHY THIS IS YOURS — ONE BINDING CONSTRAINT.** You write hand-optimized runtime asm. **Any RTX asm that clobbers r10 or r11 silently breaks EVERY pattern blob in flight** — the wires are live across the entire match, and a corrupted rΓ/rΩ is a wild jump, not a wrong answer. RTX may run concurrently IFF its asm either PRESERVES the pair or sits behind an RTCC veneer. ⛔ WREG-0's claim gate must sweep RTX asm sources, not just `src/templates/` — raw-byte and hand-written asm do not grep as `"r10"`. Per-family kill-switches make this cheap to bisect if a divergence appears.

**s12b MEASURED FLOORS (same container, BY SET — every seat's regressions are judged against these):** probe **33/5** m3 · **33/4** m4 · crosscheck/patterns **82/40** m3 · **78/44** m4 · broad-336 **270/66** m3 · **262/68/6** m4. Honest census Σest ≈ **263** (NOT the stale 557; crosscheck committed `.s` were pre-deletion — re-measure fresh, do not trust those artifacts). Runner semantics: `$(cat ref)` vs `$(cmd)` (ENV-TRAP (a)); namespace `/tmp` evidence per seat.

## ⛔⭐⭐⭐ LON DIRECTIVE (2026-08-09, routed in-session): THE CALL-PATH C SURFACE IS HIGHEST RTX PRIORITY — replace with asm
The DEFINE call protocol's per-activation C round-trips are the measured whole of the func-family deficit vs SPITBOL: `func_call_overhead` 0.52× ≈ `func_call` 0.49× (an overhead-isolating bench pricing identical to a working one ⇒ the cost IS the protocol), `fibonacci` 0.37× = same cost × recursion depth.  TARGET SET, in emitted-call order: `rt_proc_find` / `rt_proc_open_fn` (name-keyed dispatch — mostly DELETED by AB-3b's `jmp [fn_cell$FN]` site flip, which is the strict prerequisite: land or coordinate with GOAL-SNOBOL4-BB LADDER AB before spending asm here) · `rt_proc_call_epilogue_γ/ω` · the per-boundary `rtcc_wb/rtcc_rl` register writeback-reload tax (RTCC veneer is this ladder's own machinery — the call path is its first paying customer).  SPITBOL's own protocol (manual Ch.8: pushdown save of args/locals + direct branch) is the asm shape to hit.  FALSIFIABLE ACCEPTANCE: `perf` majority moves out of libscrip_rt on `func_call_overhead`; that bench → near-parity; `fibonacci` follows proportionally.  Evidence + table: FINDING-2026-08-09h §7 + SCRIP README bench section @ `1c53552a`.  Prior art this directive elevates, not replaces: FINDING-2026-07-31 "THE-HOT-C-SURFACE-IS-CALLS-SPITBOL-NEVER-MAKES".  ⛔ Concurrency: obeys this file's existing SHARED-FILE (merge, do not wait) markings; Lon routes the seat.

## ⚙️ CONCURRENT BY DEFAULT — AND THE REPOS MOVE UNDER YOU

**Many seats run this file's siblings at the same time. Edit any file, commit and push whenever a rung is buildable and green — mid-session, per rung. Never park work or decline an edit on concurrency grounds; stranding has cost this project far more than merging ever has.** Git merges; `git pull --rebase` and resolve normally.

**⛔ ASSUME ORIGIN MOVED SINCE YOU LAST LOOKED.** Another seat may have landed in your exact files while you were reading them.
- `git pull --rebase` before every push; **re-prove THIS file's gate/watermark after any rebase** — shared state moves under you and a watermark measured pre-rebase is void.
- `git log origin/main..HEAD` at orientation AND before handoff. **A clean `git status` is NOT a clean tree** — it hides local commits a peer seat left in a shared working copy.
- Place trees at canonical absolute paths (`/home/claude/{SCRIP,corpus,.github,x64}`) BEFORE running any gate: **many scripts grade a tree by absolute path.**
- Prefer **one clone per seat**; two seats in one working copy silently overwrite each other's uncommitted edits, and a global gitconfig scrambles attribution.
- Push **code repos before `.github`**, so no FINDING ever describes an unpushed tree.
- Push needs a credential — **ask Lon in chat and wait.** Never write push status into a doc.
- `bash scripts/handoff_status.sh` verbatim is the ONLY push truth. Not this file, not a commit message.

**Semantic collisions (two seats claiming one register) are caught MECHANICALLY by the claim gates, not by scheduling.** That is why no window is needed.

## ⛔ LIVE CURSOR — s_this+9 (2026-08-11 Opus, shared container; CONCURRENT with s_this+8, whose block is RETAINED below and NOT superseded): **MONITOR-FIRST DISCHARGED ON RTX-FUNC-11 — s+8's OWN NEXT ACTION, NOW DONE. AND TWO OF s+8's RESULTS ARE CONTRADICTED BY MEASUREMENT: THE PRODUCT LAW DOES NOT SURVIVE A DRIVER-PADDING SWEEP (A THIRD FACTOR — THE MAIN FILE'S STATEMENT COUNT — SUPPLIES HEADROOM, MONOTONICALLY), AND THE m4 INLINE ARM IS A VALID CONTROL ON THIS SEAT'S WITNESS. GVA ARENA AND TABLE GROWTH BOTH EXONERATED. TWO SELF-FALSIFICATIONS RECORDED.** Fingerprint SCRIP `565ecfa8` (3155 commits) · corpus `eb23c7ee` · `.github` `984414e3`; AB=0, `RT_OPT=-O0`, ONE binary, mtime 00:37:45 verified unmoved across every number below.
- ✅ **BASELINE RE-PROVEN AT HEAD**: 17/17 beauty drivers SIGSEGV at `565ecfa8`. The RTCC-default-on / WREG / ZCTX landings since `bce9a4b` do **not** touch it. Verdicts by **output-diff vs `.ref`**, never by exit-code test.
- ✅⭐ **MONITOR-FIRST DISCHARGED (s+8 lists this as its NEXT ACTION).** Both blockers were illusory: `x64` is one clone, and `scripts/test_monitor_2way_spitbol_vs_run.sh` already exists as a wrapper setting `PARTICIPANTS="spl scr"` — s+7's "runs csn spl" note applies to the `_bin` sibling, not this one. **DIVERGE step 48 / stno 28** at `global.sno:29` `UTF[CHAR(194) CHAR(160)] = 'NO_BREAK_SPACE'`: SPITBOL emits `@28 VALUE <lval>=STRING(14)`, SCRIP emits **no VALUE event at all**. ⚠ **Occurs BEFORE the crash; causal link UNPROVEN and not claimed** — possibly a second, unowned defect in table-element assignment tracing.
- ⭐⭐ **CONFLICT WITH s+8's PRODUCT LAW — MEASURED, NOT ARGUED.** s+8: crash needs vars × iters ≳1500. Here vars=iters=N, so product=N². **N=17 CRASHES (product 289)** with a bare 3-line driver; **N=60 is CLEAN (product 3600)** once the driver carries 60 inert statements. A pure product law predicts the reverse. Driver-padding sweep, monotonic: **pad 0→max clean N 16 · 10→20 · 30→30 · 60→60.** ⇒ **THIRD FACTOR: the MAIN FILE's statement count supplies the capacity; `-INCLUDE`d statements do not enlarge it.** That is why a 3-line driver dies at N=17 while real drivers need 161 included lines — they bring their own headroom. **Neither law is established; both are slices of one surface. A 3-D sweep (vars × iters × driver statements) is owed BEFORE any constant is quoted again** — including s+8's 1500 and s+7's 112.
- ⭐ **s+7's "entries" RE-READ**: not table capacity — the count of **runtime-created variables**. Bisect: `head -N global.sno` N≤160 clean, **N=161 crashes**; line 161 is `$UTF_Array[i,2] = UTF_Array[i,1] :S(G1)`, the indirect operator as an **lvalue** (manual Ch.7/Ch.9), creating one variable per table entry. Numerically equal to the entry count in `global.sno` only because the loop walks it one-for-one.
- ⛔ **CONFLICT WITH s+8's m4 CLAIM.** s+8: *"the inline arm is a control in m3 ONLY — m4 kills both arms."* On `corpus/probe/rtx11_dynvar_*` (N=40), same command, same binary: **m4 include rc=139, m4 inline rc=0 printing `done`.** ⚠ Not a claim that s+8 is wrong — recipes differ at least in `-L`: the `.so` is at **`SCRIP/out/libscrip_rt.so`**, and a bare `-L/home/claude/SCRIP` LINK-FAILS, a plausible way for an m4 arm to look broken for a non-defect reason. **UNRESOLVED — settle before either m4 claim is load-bearing.**
- ✅ **WITNESS (second, independent of s+8's):** `corpus/probe/rtx11_dynvar{.inc,_include.sno,_inline.sno}` + oracle `.ref` ×2. Oracle `done` both arms. Three-zone threshold reproduces: **≤15 clean · 16 PROBABILISTIC (139,0,139,139,0 over 5) · ≥17 always** — the coin-flip band independently corroborates the out-of-bounds-write reading from a different geometry.
- ⛔ **TWO MORE HYPOTHESES DEAD (neither in s+8's list of four):** **table growth/rehash** — plain inserts clean at 80/100/112/125/200/**400** entries × integer/string/`CHAR CHAR` keys, so the computed-key expression is irrelevant; **GVA arena** — exonerated by its own kill-switch, `driver/scrip.c:1537` `n_gva_m3 = getenv("MONITOR_BIN") ? 0 : gva_count()`, and the witness segfaults identically with `MONITOR_BIN` set (3/3 each arm; `fence_driver.sno` likewise).
- ⛔ **SELF-FALSIFICATION (a):** I claimed from that same line 1537 that the monitor is *structurally blind* to this crash. **My next command falsified it.** Claim withdrawn — I convicted a mechanism from one line of source while quoting the rule that forbids it.
- ⛔ **SELF-FALSIFICATION (b):** `scrip ... | head -2; echo rc=$?` reports **head's** status; it printed `rc=0` for an arm that segfaults and briefly manufactured a "fix". **Third instance of this genus in three sessions** (s+7 SIGBUS-as-clean; s+5b `grep -c "fn_cell\$"`). **Standing remedy: redirect to a file and diff — never pipe into a truncating filter and read `$?`.**
- ⛔ **NOT DONE:** no site convicted (`gva_collect.c` grows correctly; the emitted lowering is `rt_deref`→`rt_call_arr` per s+9c — `rt_indirect_assign_str` is NOT the emitted path — and NEITHER is instrumented) · **bisect NOT run** though possible at 3155 commits, so regression-vs-debt is still unknown · no `.s` regen (no templates touched) · no watermark sweep this seat · s+8's core read NOT reproduced here.
- ⭐⭐ **THE 3-D SWEEP RAN — AND FALSIFIES EVERY SCALE LAW SO FAR, THREE OF THE FOUR MINE.** Decoupling V (distinct names) from R (repeats) via an outer loop, D=0, N=3–4/point: **total assignments DEAD** (total=20: `V=20,R=1` **3/3 crash** vs `V=1,R=20` **0/4 clean**) · **distinct-vars-alone DEAD** (`V=10,R=1` clean, `V=10,R=2` 2/3 — *the same ten names* ⇒ re-assigning an EXISTING variable still consumes capacity) · **s+8's product DEAD in this geometry** (`V=16,R=1` product 16 crashes 2/3 while `V=1,R=30` product 30 is clean) · **simple linear fits DEAD** (`aV+b·VR` → predicts `V=1,R=30` crashes; measured 0/4). **Surviving, deliberately weak:** both axes reach the fault independently (V=1 crashes at R≥40; R=1 at V≥20); distinct names are ~2× the cost of repeats; **V=1 bounded R=30 clean 0/4 / R=40 crash 4/4**; D shifts thresholds monotonically; every boundary has a probabilistic band.
- ⛔⛔ **STANDING INSTRUCTION — STOP DERIVING THRESHOLD CONSTANTS.** s+7's `112`, s+8's `1500`, s+9's `16/17` are each a **1-D slice of a ≥3-D surface**, each valid on its own probe geometry, none transferable. **Three seats, three constants, three falsifications — one per geometry tried.** The consumed quantity is provably NOT {names, assignments, their product, any linear combination}, so it **cannot be inferred from black-box thresholds**. Next instrument is **instrumentation of the allocation itself** — a counter in the runtime path, or s+8's core-file technique — NOT another sweep.
- ⭐ **s+9c — THREE MORE EXONERATIONS FROM THE EMITTED ASM, recorded so nobody re-spends them:** inline `.string "SNO$NAME"` in the code path — FALSE ALARM, correctly bracketed `.rodata`/`.text` · mixed direct-vs-GOTPCREL addressing on `g_rtcc_block` — `R_X86_64_COPY` and `GLOB_DAT` both resolve `0x408080`, no split · copy-reloc size overflow — symbol 256B, max emitted offset +64. m4-side; could not explain a both-modes fault anyway. **Seat tally: seven hypotheses raised, seven dead, five mine — the mechanism is unnamed and that is the correct state.**
- 📐 **PLAN SCRUTINY (s+9, session close, owner review requested):** (a) **the scale-law chase cost three seats** — the standing instruction above is the fix; enforce it at review, not just in prose. (b) **`crosscheck/` has zero live `-INCLUDE`** — 269 green programs are structurally blind to this class; the witness pairs in `corpus/probe/` seed the missing probe family, and promoting them into the sanctioned suite is a POLICY call for the owner. (c) **the step-48 missing-VALUE divergence deserves its OWN rung** — a monitor-fidelity/modes-1:1 defect independent of the SEGV; folded under this one it will be forgotten. (d) **the m4 conflict with s+8 is likely the `-L` path** (`SCRIP/out/libscrip_rt.so`, not repo root — bare `-L SCRIP` link-fails); a five-minute check that unblocks both m4 claims — do it FIRST. (e) **two seats, one rung, non-overlapping geometries produced the decisive falsifications** — the disagreement was the yield; retain concurrent cursors rather than superseding, as done here.
- 📋 **NEXT, IN ORDER:** (1) ~~the 3-D sweep~~ **DONE — see above; instrument the allocation instead** — it is the only way to reconcile s+7/s+8/s+9 into one law, and until it runs no threshold constant should be cited; (2) settle the m4 control conflict (start with the `-L` path); (3) bisect using whichever witness is deterministic under the reconciled law; (4) own the **`-INCLUDE` coverage hole** — `crosscheck/` has zero live `-INCLUDE`, so 269 passing programs cannot catch this class; (5) settle the step-48 divergence.
- **FINDING:** `FINDING-2026-08-11-CLAUDE-OP5-SN4-RTX-FUNC-11-IS-INCLUDE-SCOPED-CAPACITY-FOR-RUNTIME-CREATED-VARIABLES-AND-THE-GVA-ARENA-IS-EXONERATED.md` (§10 is the cross-seat reconciliation).
- **`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ CONCURRENT CURSOR — s_this+8 (2026-08-11 Opus 5, fresh three-repo clone): **RTX-FUNC-11 REDUCED FROM 17 DRIVERS TO AN 8-LINE TWO-SIDED WITNESS, AND THE SCALE LAW IS RESTATED: IT IS A ⭐PRODUCT⭐ (include-defined vars × loop iterations ≳1500), NOT AN ENTRY COUNT — THE INHERITED "≤88/104/≥112 entries" IS ONE SLICE OF THAT SURFACE. FOUR HYPOTHESES FALSIFIED BY MEASUREMENT (subscript-overrun · STACK OVERFLOW · ARENA EXHAUSTION · RTCC). CRASH PC IS IN EMITTED CODE (anon JIT slab, not libscrip_rt) AND `rbx` — THE PINNED DESCR MINT POINTER — IS WILD, IN NO MAPPING. MECHANISM DELIBERATELY NOT NAMED: MONITOR-FIRST. ⭐ THE ORACLE IS NOW PRESENT (`/home/claude/x64` cloned) — s+7's stated blocker is GONE.** Fingerprint SCRIP `565ecfa8` · corpus `bea31de0` · `.github` `6bb9f32f`; AB=0, `RT_OPT=-O0`, binary mtime 00:23 verified unmoved. **ZERO CODE CHANGED — characterisation + witness only.**
- ✅ **WITNESS MINTED, TWO-SIDED, SPITBOL-VALIDATED:** `corpus/probe/rtx_func_11_{include,inline}.sno` + `rtx_func_11_inc.sno` + `.ref` ×2. Oracle runs BOTH arms identically. m3: include **rc=139**, inline **ORACLE-EXACT**.
- ⭐⭐ **PRODUCT LAW (N=8 per point):** vars×iters ≤1024 → **0/8 crash**; 1280 → **1/8** (probabilistic band); ≥1600 → **8/8 crash**. Neither factor alone fires (40×3, 0×40, 40×0 all clean). ⇒ **any probe naming only ONE factor re-derives a threshold that does not transfer.** Same three-zone shape as the inherited entry law, which is why the slice passed for the whole.
- ⭐⭐ **THE INLINE ARM IS A CONTROL IN m3 ONLY — m4 KILLS BOTH ARMS.** s+6's "perfect zero-cost control" holds for `--run` and is FALSE for `--compile`. m4 recipe validated by a green `hello` control. **AND, INDEPENDENTLY: at 8×8 — far below every threshold — m4 prints NOTHING at rc=0 where m3 prints `i=8`.** Silent wrong answer, modes-1:1 violation, invisible to exit-code probes. **Its own rung, not RTX-FUNC-11.**
- ⛔ **FOUR HYPOTHESES DEAD, each N≥4:** subscript-overrun (a BOUNDED loop crashes identically) · **stack overflow** (`ulimit -s` 2 MB→unlimited = **24/24 SEGV**) · **arena exhaustion** (`SCRIP_HEAP_MB` 64→4096 = **24/24 SEGV**; the `[ZHP]` check never fires) · **RTCC** (`SCRIP_RTCC=0` → repro 4/4 SEGV, drivers **0/17** recovered). TABLE · SORT · `$`-indirect · cross-include label+goto each exonerated in isolation.
- ⭐ **CORE READ WITHOUT gdb** (`NT_PRSTATUS` + `NT_FILE` parsed directly): `rip` in **no file-backed mapping** ⇒ anonymous JIT/RX slab ⇒ **emitted code, not the C runtime**; `rbx` (= arena heap top / DESCR mint pointer, ARCH §2) = `0x347a25d8`, **in no mapping** ⇒ clobbered pin, not exhaustion (§5.3 independently kills exhaustion).
- ⚠ **THREE SELF-FALSIFICATIONS RECORDED:** (a) nearly convicted `SUBSTR` on a probe whose loop was **unbounded** — its inline arm was rc=124 TIMEOUT, so both arms were failing differently; **a control must be CORRECT and TERMINATING, not merely non-crashing.** (b) my first 2-D scan was **N=1** and placed `40×32` in the clean column; at N=8 it is in the probabilistic band, and a `ulimit` conclusion drawn from those cells came out **backwards** and was discarded. (c) peak-RSS was flat and is **structurally blind** here (~24 KB against a 12 MB baseline) — quoted as evidence in neither direction.
- ✅⭐⭐ **MONITOR RUN — AND IT IS STRUCTURALLY BLIND TO THIS CLASS, WHICH IS NOW A MEASURED FACT.** `test_monitor_2way_spitbol_vs_run.sh` on the witness: **~283 trace events AGREE, then `scr: EOF`** while spl is still emitting inside the loop. **There is NO first divergent event — SCRIP computes CORRECTLY until the instant it dies ⇒ "wrong value propagates then crashes" is EXCLUDED.** ⭐ **AND THE DEATH POINT MOVES RUN TO RUN** (step 284/spl stno 46, then step 283/stno 45, same binary same program) ⇒ **not statement-deterministic**, independently corroborating the wild-`rbx` reading over a fixed-statement logic error. The theorem's bracket is `[last agreeing event, EOF]` = *inside the loop*, and a SEGV presents as EOF rather than as a divergent event — ARCH §7's "blind to the divergence CLASS" arriving in the crash axis.
- ⛔ **NEXT INSTRUMENT IS NOT ANOTHER MONITOR RUN.** Bisect with a **product ≥1600** probe (8/8 deterministic — the 1280 band bisects to noise); SCRIP is cloned full-depth (3148 commits). Or hit-counted breakpoint/single-step at the loop body per RULES step (2).

### ⚠ CONCURRENT-SEAT RECONCILIATION — s+8 × s+9 (written by s+8 AFTER seeing `15d8afc8`; neither supersedes the other)
**The two monitor results are NOT in conflict — THEY WERE RUN ON DIFFERENT WITNESSES.** s+9 measured a *named* first divergence (step 48 / stno 28, missing VALUE event on a table-element assign) on its 45-line `$`-indirect+TABLE witness; s+8 measured **no divergent event at all** (~283 agreeing, then EOF) on its 8-line scalar witness. Two programs, two answers, both honest. ⛔ **Do not quote one witness's monitor result as evidence about the other.**
⛔⭐ **s+9's TRIGGER CLAIM IS FALSIFIED BY s+8's WITNESS, WHICH IS STRICTLY TIGHTER.** s+9's headline is *"the trigger is runtime-created variables (`$` indirect assignment) arriving via `-INCLUDE`"*. **`corpus/probe/rtx_func_11_inc.sno` contains NO `$`, NO `TABLE`, NO `SORT`, and NO array — 40 plain scalar assignments and a loop — and it SEGVs 8/8.** `$`-indirect assignment is therefore **sufficient but NOT necessary**, and the trigger must be named over something both witnesses share.
✅ **s+9's PADDING SWEEP IS A REAL REFINEMENT, NOT A CONTRADICTION — IT ADDS A THIRD FACTOR.** s+8's product law was measured at a **FIXED driver size (3 statements)** and is stated only for that slice; s+9 shows padding the *driver* moves the threshold, i.e. main-file statement count is a third variable. **s+8's own §2 warning applies to s+8: a law that names fewer factors than the surface has re-derives a threshold that does not transfer.** ⇒ **OWED: re-run the product scan across a driver-size axis before either law is quoted load-bearing.**
✅ **INDEPENDENTLY CORROBORATED ACROSS SEATS:** 17/17 baseline; `-INCLUDE` scoping; RTCC/WREG/ZCTX landings do not touch it. **AND s+9 exonerates two more:** table growth (clean to 400 entries, three key kinds) and the GVA arena (by its own kill-switch). **Running total: SIX hypotheses dead.**
- **FINDING:** `FINDING-2026-08-11-CLAUDE-OP5-SN4-RTX-FUNC-11-IS-A-PRODUCT-LAW-NOT-AN-ENTRY-COUNT-AND-FOUR-HYPOTHESES-DIED-INCLUDING-MY-OWN.md`
- **`handoff_status.sh` is the push truth — NOT this block.**

## ⛔ PRUNE RECORD — s_this (2026-08-09, third seat)
Three PRIOR blocks (2026-08-08: CENSUS-M4-ARM · CHAIN-ENTER · EVAL-RETURN+DEFER-LATCH) DELETED per RULES (c). Kept: this LIVE + seat-A LIVE + seat-B addendum + 2026-08-08 DEFER-GET-PAT-FN = ~4 units. **Zero `- [ ]`/`- [x]` rungs in cut range — grep-verified 0 before deleting.** Full text in `.github` git history. Prior PRUNE RECORDs preserved in history.

## ⚠ CONCURRENCY CONTRACT — RTX ‖ ζ-STORAGE (minted s186)

RTX and the ζ-storage ladder are designed to be safely parallel, but **only for phase-1 rungs.** Know which side of the line you are on.

**SAFE — RTX phase 1 (RTX-1 … RTX-10) touches ZERO templates BY CONSTRUCTION** (the ladder's own EVERY-RUNG line). Phase 1 keeps every exported symbol's EXACT current C signature; speed comes from bodies, not convention. Consequence: **no `.s` regen owed, no both-medium work, no `x86_asm.h` edit.**

| | RTX owns | ζ-storage owns |
|---|---|---|
| files | `src/runtime/rtx/*.S`, `rtx_abi.inc`, `rtx_init.c`, `src/runtime/rt/*.c`, family C sources (`gc_heap.c`, `string_ops.c`, `rt.c`, …) | `src/contracts/zeta_storage.c`, `src/emitter/emit.cpp`, `src/templates/*.cpp`, `x86_asm.h` |
| scripts | `test_rtx_unit.sh`, `util_rtx_symbol_inventory.sh`, `test_gate_rtx_inventory_live.sh` | the FC gate + `util_fc_conversion_map.py` + spine census |

**⛔ NOT SAFE — SERIALIZE WITH LON:**
- **RTX-11 (RTX-CC)** — registerized ABI changes template call sequences via `x86(...)` encoders in `x86_asm.h`, and **fires `.s` regen ×3**. That is the ζ ladder's own territory. **RTX-11 must not run concurrently with ZB-VAL/ZLS work.**
- **RTX-12 (eradication)** — deletes `c_*` bodies and gates; re-gates on the full crosscheck. Land it when the board is quiet.
- **`Makefile` link lines** — both ladders may add objects. Small, but a real rebase point.
- **THE WATERMARK ITSELF IS SHARED STATE.** Both sessions gate on the same crosscheck numbers. If one side moves the watermark mid-flight, the other's "no regression" claim is measured against a baseline that no longer exists. **Re-prove at session start; say the numbers out loud in the FINDING.**
- **RTX-7 (NV/GVAR)** — `NV_SET_fn` is DB-1's planned write-barrier choke point. Coordinate or land DB-1 first.
- **RTX-8 (MATCH SINKS)** — touches `pattern_match.c`, which the SN4-PAT / match-family rungs also reach. Check the other session's cursor before starting.

Per RULES.md s47 ⚠ CONCURRENCY: parallel sessions are safe for CODE (git rebases cleanly), and **`PLAN.md`'s Step column is stale BY DESIGN** — orientation trusts THIS file's `LIVE CURSOR`, never PLAN.md's table.

---

## Session Setup

```bash
git config --global user.name "LCherryholmes"
git config --global user.email "lcherryh@yahoo.com"
git clone https://github.com/snobol4ever/.github.git /home/claude/.github
git clone https://github.com/snobol4ever/SCRIP.git   /home/claude/SCRIP
git clone https://github.com/snobol4ever/corpus.git  /home/claude/corpus
git clone https://github.com/snobol4ever/x64         /home/claude/x64   # SPITBOL oracle
bash /home/claude/SCRIP/scripts/install_system_packages.sh
cd /home/claude/SCRIP && rm -f scrip && make -j4 scrip && make libscrip_rt
```

**READ-FIRST SET (NON-NEGOTIABLE, in this order):**
1. `RULES.md` in full — especially MONITOR-FIRST, TEMPLATE-ONLY EMISSION, and the HANDOFF-COMPLETE gate.
2. **`ARCH-SNOBOL4-RTX.md`** — the register/ABI/build contract. §7 is the per-rung checklist; §7 steps **0 / 0b / 2b** are the three hard-won ones.
3. The family's C source **in full**, then its `sbl.asm` counterpart for shapes (steal idioms, never structures).
4. **The SPITBOL manual chapter for every construct the family implements** (Lon standing directive). **CALL = Ch.8 `Program-Defined Objects`** (DEFINE, RETURN, FRETURN, the prototype string, local-variable save/restore, by-name args) **+ Ch.9 `Advanced Topics`** (NRETURN, run-time compilation). ⛔ **NOT Ch.13 — that is `Running SPITBOL`, command-line options.** s165 recorded this correction INSIDE a rung and never fixed the instruction, so three more sessions were pointed at the wrong chapter; the rung is now gone and the fix lives HERE, where it is actually read (s187). Verified against the manual's own TOC: Ch.8 p.101, Ch.9 p.121 (NRETURN p.133), Ch.13 p.161 · ARITH = mixed-mode coercion · MATCH = Ch.9 deferred evaluation + p.122 well-foundedness + p.125 FENCE.

**Verify the watermark BEFORE touching anything**, then run the ladder's first `- [ ]`.

---

## ⭐⭐ SN4-RTX — TOTAL RUNTIME REDESIGN IN OPTIMIZED x86 ASM (Lon PIVOT directive, s162 2026-07-25)
**RULING OF RECORD (promotes the s161 caveat to head rung):** the C runtime is a TRANSITIONAL ARTIFACT. The target SNOBOL4 runtime is hand-written, register-aware x86-64 assembly. We use REGISTERS freely; System V conventions bind ONLY at (a) libc call boundaries and (b) the m3 driver→blob entry edge. References: **SPITBOL x64 asm runtime** (`/home/claude/x64` — Minimal registers pinned to hard regs: IA=r12 W0=rax WA=rcx WB=rbx WC=rdx XL=rsi XR=rdi CP=r13 RA=xmm12; C-boundary = save-all-to-global-block/call/restore, `int.asm`; the runtime itself = `bootstrap/sbl.asm`, 19,064 lines) · **CSNOBOL4** C runtime (semantics reference; `git clone https://github.com/snobol4ever/csnobol4`, build via `scripts/build_csnobol4_oracle.sh`) · **SIL** macro source (algorithm reference — locate/fetch at need).
**RTX-0a MEASURED SURFACE (s162, static call sites in `claws5.s` — matches the s161 dynamic anatomy):** `str_concat_d`×38 · `rt_subscript_var`×33 (TABLE) · `rt_deref`×24 · `rt_call_arr`×22 + `rt_proc_call_epilogue_`×14 + `rt_proc_open_fn`×8 (the NRETURN/call path) · `rt_num_arith`×8 · `rt_binop_overload`×8 · `rt_defer_step`/`rt_dcap_step`×6 each · `rt_coerce_num2_d`×6. Current C runtime = **~21k LOC** (`src/runtime/`: top-level model ~12.6k, `rt/` 4.1k, `core/` 3.6k, `builtins/` 0.7k).
- [ ] **RTX-4 SLICE 3 — THE REST OF THE CALL SURFACE.** ⛔⛔ **PREMISE FALSIFIED BY MEASUREMENT, s188 — THE RUNG AS WRITTEN NAMED A COLD SYMBOL.** It read: *"`rt_call_arr` FIRST AND ALONE: 232 static refs, #1 symbol in the entire runtime … the largest single remaining lever."* **`rt_call_arr` passes step 0(a)(b)(c) — live, byte-identically spelled, linkage correctly classified — and is CALLED 5/16/8/12 TIMES on the four rail demos, FLAT ACROSS N.** The 232 is a count of emitted call SITES (the CALL BOUNDARY), exactly as ARCH §5 caveat (a) warned in writing at RTX-2 and was then not applied. **FOURTH member of the phantom family and the first LIVE one: RTX-2 DEAD names · RTX-3 INVENTED names · s165 MISRECORDED name · s188 a real, correct, COLD name.** ⇒ **STEP 0(d) MINTED: PROVE THE SYMBOL EXECUTES IN THE WINDOW YOU INTEND TO MOVE, BEFORE PORTING.** RE-TARGETED, pending Lon's ruling: port **`rt_call_arr` FUSED WITH `try_call_builtin_by_name`** — the two are called **1:1 in all 16 benchmarks**, so the 13-line setjmp wrapper alone leaves 100% of the dispatch in C — and gate it on `string_manip` (10.0M calls) · `eval_fixed` (1.0M) · `roman` (400K), **NEVER on the rail demos.** ⚠ `APPLY_fn` is **0 in all 16** (ordinary `DEFINE` calls go via `rt_proc_call_epilogue_*`, per s187's 18 movers). ⚠ Two dead ends pre-closed: `setjmp` already lowers to `_setjmp@plt` (no sigmask syscall — do not chase it) and `try_call_builtin_by_name` is **already** a `bid_of()` hash + generation-stamped `g_dtax` inline cache + `(len<<8)|fn[0]` jump table, NOT a strcmp chain — an asm port inherits that algorithm and wins only `-O0` ceremony. THEN: `rt_proc_call_open(_slim)` · `rt_proc_open_fn` · `rt_call_named_proc` · `rt_arg_stage` + proc_set family. FINDING: `FINDING-2026-07-27d-...-CALL-ARR-IS-COLD-...md`.
  ⛔ **STEP 0 IS NOW THREE CHECKS, NOT TWO** (s187): (a) live definition exists; (b) the doc's spelling round-trips byte-identically against the tree; (c) ⭐ **NEW — `nm` the LINKAGE of every global the port will touch.** Exported (`B`) ⇒ preemptible ⇒ `@GOTPCREL`; hidden ⇒ rip-direct. In the C they look identical. Slice 2 met this because a self-contained port reaches globals the previous slice only reached THROUGH a C call.
  ⛔ **AND DESIGN THE FALSIFICATION BREAK BEFORE YOU WRITE IT.** A break that is a no-op at its own site produces a green board that reads as 'unreachable' and ships an unexercised port. Ask: *what would the output look like if this code did not exist at all?* If that equals the passing output, the probe is vacuous. (s187's ω needed an INVERSION, not a corruption.)
- [x] DONE — ~~ (28ef6caf)
- [ ] RTX-6 (residual) **ARITH — RETARGETED TWICE; READ THIS WHOLE LINE BEFORE PLANNING ANY INLINE WORK.** `rt_cmp_d` LANDED s203. ⛔ **THE INT-INT INLINE FAST PATH THIS RUNG NAMED FOR THREE SESSIONS WAS ALREADY BUILT** — `bb_binop_arith.cpp` arm 3 did it, with compile-time immediate elision on top, i.e. strictly better than this rung's own spec (s206, measured from source). **PHANTOM RUNG: planned work that is already done — the phantom family one level up from symbols, and `nm` cannot catch it; only reading the emitting template arm can.** ⛔ **AND THE INSTRUMENT POINTED AT THE OTHER ARM:** `arith_mixed`'s 40,000,001 calls are **100% DT_R+DT_R** (interposer census, s206) ⇒ an int-int port grades a guaranteed 1.000× there. ⭐ **SUPERSEDED s207 BY LON DIRECTIVE** — *"for now all these should be ONE CALL into the runtime. We'll optimize the inline later."* All four arms collapsed to one per-op call (`rt_add`/`rt_sub`/…); **inline work is DEFERRED BY DIRECTION, not abandoned.** Measured cost of the deferral, so the inline rung has an honest floor to beat: `arith_loop` **0.64×**, `arith_mixed` **0.80×** (RT_OPT=-O0). ⭐ **WHEN INLINE RESUMES, TAKE THE FREE ~11.3 ns/call FIRST** — that is one extra `-O0` frame from the `rt_<op>`→`rt_num_arith` hop, reclaimable by giving the per-op entries their own body, and it needs NO inlining and NO `x86_asm.h` change. **THEN** the real-real SSE arm (s206 spec: both-tags-`DT_R` → `movsd`/`addsd|subsd|mulsd`; ADD/SUB/MUL only — DIV needs a zero-test (`arithmetic.c:239`), MOD needs `fmod`, POW needs `pow`; ⛔ both-same-tag only, any string tag out-of-line because `'1.5'` IS a real operand per Ch.17 String→real + `operand_is_real_str`). ⛔ That arm needs NEW SSE ENCODERS in `x86_asm.h` (`addsd`/`subsd`/`mulsd`/`ucomisd`/`cvtsi2sd` do not exist; `movsd` handles only the `f64:` immediate form) ⇒ **RTX-11/12 concurrency collision is unavoidable for it and is Lon's call.** ⛔ **~~incr decr neg exp~~ ARE PHANTOMS — struck s204 per ARCH §7.**
- [ ] RTX-7 **NV/GVAR — ⛔⛔ DOWNGRADED s208: NOT A SPEED RUNG. DO NOT PORT THIS FOR PERFORMANCE.** `NV_GET_fn`/`NV_SET_fn` (✅ both verified LIVE s204) + `rt_gvar_*` + `rt_gva_island` + `rt_subject_load_nv`. ⛔ **ARCH §5's NV row also carried `rt_nv_get`/`rt_nv_set` — BOTH PHANTOMS, struck s204, re-confirmed s208 by `nm` (zero definitions).** ⛔ **STEP 0(d) FAILED, MEASURED s208:** `NV_GET_fn` = **ZERO calls** in var_access · func_call · string_manip · table_access · fibonacci · indirect_dispatch · op_dispatch, and **ZERO static call sites** in every benchmark `.s`; `rt_gvar_get_int`/`rt_gvar_assign_descr` also **0 static sites**. Only live window is **EVAL** (`eval_dynamic`/`eval_fixed`, 2,000,000 calls, scaling proven 2× with +26 constant preserved), where `rdtsc` bounds it at **0.303% + 0.276% = 0.58% UPPER BOUND** ⇒ **a perfect port wins less than the ±3% null floor.** ⛔ **NEVER grade it on the four zero-count benchmarks — guaranteed false null, the s203 ARITH-FAMSET defect class.** ⭐⭐ **CAUSE = THE EIGHTH PHANTOM SHAPE, "THE BYPASSED FAMILY": the function is live and correct, but GVA SLOTS ALREADY TAUGHT THE EMITTER NOT TO CALL IT** (`var_access.s`: `rt_gva_island`/`gva_register` once at startup, then direct slots, no runtime call) — exactly what integer inlining did to `rt_num_arith` at s203. **A RUNG'S PREMISE DECAYS WHEN AN UNRELATED RUNG SUCCEEDS; RTX-7 was correct when written.** ⇒ if ever done, it is a **correctness/eradication** rung serving RTX-12, and the FINDING must say so rather than quoting a speed number. ⚠ **THIS ROW IS THE MIXED SHAPE AND IT IS THE HARDEST TO CATCH:** the two names a rung author is most likely to spot-check (`NV_GET_fn`/`NV_SET_fn`) are exactly the two that are real, so one confirmation "validates" a list that is 2/6 dead. **Spot-checking a symbol list is not checking it — `nm` every name.** ⚠ COORDINATE: `NV_SET_fn` is DB-1's planned write-barrier choke point — port must leave room for the flag-bit check (or land DB-1 first; walking session's call, note it in the FINDING either way).
- [ ] **⬅ ROUTED 2026-08-08 (Lon GO): executes as RUNG M-SLEN in `GOAL-SN4-ZETA-MECH.md` — this row is a POINTER, not the queue.** ⭐⭐ **POPULATE `DESCR_t.slen` AT ASSIGNMENT — NOT AN RTX RUNG; NEEDS LON'S ROUTING (raised s220 ON MEASUREMENT, and by measurement it outranks every remaining MATCH asm port).** `STRVAL` mints DT_S with `.slen = 0` and only `BSTRVAL` populates it, so **every match pays a `strlen` on a string whose length was known when it was assigned**: 2,000,001 per run on `pattern_bt_deep`. ⭐⭐ **CONFIRMED DEAD WEIGHT IN BOTH LANGUAGES BY TWO INDEPENDENT MEASUREMENTS — SNOBOL4 `slen==0` on 2,000,001/2,000,001 (s220) and Icon `slen usable = 0` on both a synthetic and a real corpus (s217-ICN).** ⇒ **STANDING RULE MEANWHILE: no RTX fast arm in either language may be keyed on `slen != 0`.** This is a LOWER/descriptor rung (who sets it, and whether every consumer of `.slen == 0`-means-unknown survives it being set) — scope it with Lon before any code.
- [ ] ⭐ **GROW THE KILL-SWITCH GATE AN m4 (`--compile`) ARM — "SUITE-WIDE BOTH MODES" IS STILL OWED AND THE SCRIPT CANNOT EXPRESS IT (s220).** The m3 sweep now runs and passes suite-wide (316 programs, 313/3/0, 48 s), but `test_gate_rtx_killswitch_sets.sh` invokes only `scrip --run`. m4 is a STANDING GATE (s214) precisely because mode 3 is structurally blind to the exported/hidden data-symbol class, so an m3-only byte-identity sweep cannot close an RTX-12 eradication re-gate. ⚠ Do not quote "suite-wide" without naming the mode.
- [ ] **CHARACTERISE THE 3 QUARANTINE PROGRAMS WITH N≫4 (s220).** `160_pat_alt_inner_gen_resume` · **`413_arith_mixed`** · **`W06_len`** — the latter two are NEW at s220 and were invisible while the sweep was truncated. All three are C-side by the gate's own argument (gate-OFF *is* the C fallback, and in both new cases ON is stable while OFF is unstable with ON's hash a member of OFF's set), so none blocks a port. ⚠ **`160`'s hash set is NOT enumerated at N=4 — three sessions have reported three different memberships; N=4 DETECTS non-determinism, it does not CHARACTERISE it.** Find the root cause or record the true set size; do not quote a set as closed.
- [ ] **`util_rtx_count_syms.sh` SEGFAULTS (rc=139) ON `rt_dcap_lazy_init` (s220)** while `rt_match_enter`/`VARVAL_fn`/`descr_to_str` thunk fine in the same harness ⇒ symbol-specific, not harness-wide. Likely the preload copy interposing libscrip_rt's INTERNAL calls with an invalid `RTLD_NEXT` resolution at first call. ⚠ **Until fixed, a zero from this tool on that symbol must NOT be read as "never called"** — that is the 0(d) false-null class the ladder has been bitten by repeatedly. 
- [ ] ⭐ **THE DEFER LATCH FIX — SPLIT OUT OF RTX-8 SLICE 2 AT s215, ON MEASUREMENT.** `rt_defer_get_pat_fn`'s one-entry `g_star_peek` latch (`pattern_match.c`, the s161 EVAL/deferred slot collision) must be replaced by **per-site resolution, no shared latch**. ⛔ **THIS IS A CORRECTNESS RUNG AND MUST NEVER QUOTE A SPEED NUMBER:** the star path it serves measures **star=0 of 402,121 opens** on json/twitter, so any ratio taken there is 1.000× by construction. **GATE: `140_pat_eval_double_fn_trick` and `141_pat_eval_double_fn_arbno`, already RED at m3 rc=139 and PASSING m4 — a two-sided gate for free.** ⭐ The manual proves the latch is structurally wrong rather than merely buggy: Ch.7 p.86 (`*` fetches the then-current value at every match-time reference; legal on an alternate/subsequent clause or the whole pattern) + Ch.9 p.122-123 (a deferred value may itself be a pattern, match state lives on a STACK, no-progress recursion plunges) ⇒ **deferred evaluation can RE-ENTER deferred evaluation, so one entry keyed by name is guaranteed to be clobbered by a nested reference.** Nesting is by design — the same reason `g_dfx` is a LIFO. `g_star_peek` is already `hidden` as of `0410aa72`, so an asm or C fix can reach it either way.
- [ ] ⭐⭐ **GIVE THE RAIL A min-OF-N MODE AND/OR HUGEPAGE PINNING (NEW s224) — THIS NOW BLOCKS EVERY SPEED CLAIM ON THIS MACHINE AND OUTRANKS ANY PORT.** `bench_rtx_3arm.sh` reports a MEDIAN and refuses on intra-arm spread; s220 established that only the MINIMUM is stable, and s224 measured the median verdict getting WORSE with more rounds (1.071× → 1.922× on an unchanged binary). The bimodality is the documented s201 hugepage class (clean ~1550/~3400 split = 2.3×). Until this lands, "the window was too short" must NOT be written as the reason a rung is ungradeable — s224 falsified it at 5704 ms.
- [ ] ⚠ **THIN-COVERAGE PORTS — MEASURED s224, EACH ON ITS OWN BATTERY, m3 ONLY. ⬅ m4 arm now available (`util_rtx_arm_census.sh <prog> both`) — re-run these on m4 before any port.** `rt_patstk_lazy_init` **0 commits** (MATCH-gated, so SNOBOL4 IS its battery and the zero stands; regression-tested by nothing, ungradeable here) · `rt_cap_match_begin` 1 program · `rt_match_replace` 3 · `rt_cap_pop` 3 · `rt_defer_open`/`_close` 7/8 · `rt_subscript_var` 2676 commits on Icon but in only **2 of 60** programs. ⇒ Rung = widen the regression base wherever coverage is 0 or 1 program, using the slice-9 recipe (mint a program, oracle `.ref`, validate two-sided at mint). ⛔ **DO NOT re-derive any of these from the SNOBOL4 table without FIRST checking which family gate the symbol belongs to — that omission produced a false vacuity claim at s224.**
- [x] DONE — ~~⭐ (2d687dab)
- [ ] ⛔ **`rt_defer_step` STRUCK s225 — 434 STATIC CALL SITES, ZERO DYNAMIC ENTRIES on json/calculator-1/calculator-2. Not a phantom (live definition, real call sites) but UNEXECUTED on this corpus ⇒ unfalsifiable, do NOT port it without first minting a workload that reaches it.**\n- [ ] RTX-8 **MATCH SINKS:** cap/dcap/defer families + `rt_match_enter` + `rt_match_value_get_pat_fn`/`rt_match_value_open` + ~~`rt_match_replace`~~ **[DONE — LANDED s224, slice-9 (LANDED s224 — row pruned s226, see the s224 FINDING); struck here in the SAME commit that completed it, per ARCH §7 step 0's "strike the dead names out of the ladder rung text" clause]**. ⛔ **`rt_match_ctx_restore` IS DONE — LANDED s217, LANDED s217 — row pruned s226, see git history + the s217 FINDING.** ⛔ **`match_variant` STRUCK s216 — PHANTOM (decl-only `rt/rt.h:30`, zero definitions, zero call sites); `match_value` WAS A TRUNCATION of TWO real symbols. Both corrected in ARCH §5 the same commit.** ⛔ **`rt_cap_push` IS DONE AND THE ANSWER WAS 'DO NOT PORT IT' — see the s216 cursor: 57,578 calls, 0 commits, vacuous on every workload in the tree.** ⚠ `rt_match_capture`/`rt_scan_splice_empty` have ZERO static sites in the benchmark+demo artifacts. ⚠⚠ DO NOT TRANSLITERATE `g_star_peek` — the s161 m4 EVAL/deferred slot collision lives in `rt_defer_get_pat_fn`'s one-entry latch (pattern_match.c:960); the port must FIX the order-dependent bleed (per-site resolution, no shared latch), and the 15-line s161 repro becomes the regression canary. This rung closes 2 of the 3 broken demos if done right.
- [ ] RTX-9 **PAT BUILD:** ⛔⛔ **THE RUNG'S OWN SYMBOL NAME WAS WRONG AND WOULD HAVE READ AS A DEAD FAMILY — CORRECTED s204 BEFORE IT COST A SESSION.** This rung read `rt_pat_*`; **all 30 `rt_pat_*` spellings are declaration-only in `rt/rt.h:38-67` with zero definitions.** ⛔⛔ **THAT SENTENCE IS NOW FALSE IN BOTH DIRECTIONS — CORRECTED 2026-08-09.** (i) The 30 were **DELETED** at SCRIP `1b251a7d`; `rt/rt.h:38-67` today holds `rt_acomp`/`rt_lcomp`/`rt_define_entry`, so **the line-number pointer lands on unrelated text.** (ii) There is now **exactly ONE LIVE `rt_pat_*`** — `rt_pat_prim_int`, `T` in the `.so`, added by concurrent D08-FIX (`e3ef8f7d`) for `LEN(*var)` match-time integer fetch — so "`rt_pat_*` is a dead family" would now MISREAD A LIVE SYMBOL. ⭐ **A line-number pointer into a file another seat edits rots exactly like a symbol name copied from prose.** ✅ The rest of this row survives step 0(b) at HEAD: `pat_lit`/`pat_alt`/`pat_break_`/`pat_any_cs` all round-trip byte-identically and the family is 32 defined constructors, as claimed. The live constructors carry **NO `rt_` prefix**: 32 defined in `pattern_match.c` (`pat_lit` @ `:98`, `pat_alt` @ `:215`), listed in full in ARCH §5's corrected PAT row. ⭐ **SEVENTH PHANTOM-FAMILY MEMBER, AND A NEW FAILURE MODE: THE PRIOR SIX RISKED WASTED OR DUPLICATED WORK; THIS ONE RISKED ABANDONING A LIVE, HOT FAMILY.** A step-0 grep on `rt_pat_lit` HITS (the `rt.h` declaration) so the name reads alive; `nm` then finds no definition, which is the exact signature of a phantom — so the correct-looking conclusion is "PAT is dead, skip the rung," and it is FALSE. ⚠ Take the tree's spelling exactly: `pat_break_` has a **trailing underscore** and the ANY constructor is `pat_any_cs`, not `pat_any` (step 0b). Construction-path = size-not-time (s160a measured rule) — port LAST among hot families, quote size wins honestly, never as match-loop speedups.
- [ ] RTX-10 **MISC/IO + VSTACK AUDIT:** keywords, goto_transfer, execute_code_dyn, comm_stno, io. ✅ **THE VSTACK AUDIT IS DISCHARGED s204 — MEASURED, AHEAD OF THE RUNG.** `nm -D out/libscrip_rt.so` finds **ZERO definitions** for every `rt_push_*`, `rt_pop_*`, and `rt_halt_tos`; each is declaration-only in `rt/rt.h` with exactly one src ref (its own declaration). ⇒ **the rung's "if dead, delete instead of port" branch is TAKEN.** No construct needs naming in the FINDING because nothing reaches them — the NO-VSTACK rules already won and only the header still says otherwise. **Remaining work on this rung is the live MISC/IO surface ~~plus deleting the dead declarations~~.** ⛔ **THE DELETION HALF IS ALREADY DONE — STRUCK 2026-08-09.** SCRIP `1b251a7d` deleted all 12 vstack decls (with 30 `rt_pat_*`); at HEAD `nm` finds zero definitions AND the grep finds zero declarations. **PHANTOM RUNG (planned work already done) — `nm` cannot catch it, only `git log -S` can.** ⚠ But `rt/rt.h` still carries **83 OTHER declared-but-never-defined symbols** (51 with zero references; 36 non-Prolog / 15 Prolog-family) — full census + the safe list in `FINDING-2026-08-09-...-83-PHANTOM-DECLARATIONS...md`. ⛔ Deleting them was ATTEMPTED and REVERTED UNPROVEN: one program (`161_pat_defer_fn_nested_match`) moved PASS→FAIL, which an unreferenced-declaration delete cannot cause ⇒ **suspect the BASELINE was a stale `.so` (`make libscrip_rt` said "Nothing to be done" at session open).** Re-baseline against a FULL rebuild before re-attempting.
- [ ] ⛔⭐⭐⭐ **RTX-FUNC — ZERO-C FUNCTION CALL PATH (Lon directive 2026-08-09: highest RTX priority)**
  **GOAL:** `func_call` and `fibonacci` at SPITBOL parity. SPITBOL Ch.8 protocol: save-set push → direct branch → pop on return. Zero C calls per invocation. Current gap after AB-3b: two C crossings remain on every call (`rt_ab_enter_env` at entry, `rt_ab_leave_env` at exit); name lookup eliminated, pcall record eliminated, but bookkeeping boundary still costs.
  **TARGET HOT PATH (zero C calls):**
  ```
  call site:  spill save-set → install actuals → jmp [fn_cell$FN]
  α:          inline Σ/wn save → inline vtmark → inline k_level++ → body
  β:          inline k_level-- → inline vtmark-tidy (fast path) → inline Σ restore → jmp wire
  ```
  **RUNGS — in order, each its own commit, each killswitch-gated, each measured:**

- [x] DONE — ~~ (5416ed56)
- [x] DONE — ~~ (5416ed56)
- [x] DONE — ~~
- [x] DONE — ~~
  - [ ] **RTX-FUNC-5: MEASURE AND FINDING.** After RTX-FUNC-1 through RTX-FUNC-4: run `bench_rtx_3arm.sh` with min-of-N rail (ARCH §7 step 4 three-arm protocol). Report `func_call` and `fibonacci` ON/PRISTINE ratios vs s_this baseline (func_call 1882ms, fibonacci 486ms). Target: ratio ≥ 0.80× SPITBOL (directive's falsifiable acceptance). FINDING doc naming each rung's contribution. ⛔ NO SPEED NUMBER before RTX-FUNC-2 is done — the two C crossings are the whole cost; quoting partial progress misrepresents the win.

  - [ ] **RTX-FUNC-6 (NEW 2026-08-10, parallel seat): AB=0 RECURSION NON-DETERMINISM — root-cause + fix, or formal quarantine.** The LEGACY arm returns multi-element output sets on recursive DEFINEs, including a fatal `[ZHP] heap exhausted`/SIGSEGV class (sets enumerated in the 2026-08-10 ROMAN FINDING). Reproducer: `p10_gotoloop.sno` — 12 lines, deterministic at AB=1, in the FINDING; mint into corpus with `.ref` (slice-9 recipe) at rung open. MONITOR-FIRST per RULES. This is a FALLBACK-ARM fatality: a control that aborts outranks a probe that does not fire — sequence it ahead of remaining FUNC polish.

  - [ ] **RTX-FUNC-7: RE-RUN THE DEFINE-BEARING AB IDENTITY SWEEP AT N≥4 PER ARM, THREE VERDICTS (IDENTICAL/QUARANTINE/MOVER).** Until RTX-FUNC-6 closes, NO AB identity claim may be recorded from N=1 — the control is non-deterministic on recursion, so N=1 identity is a coin flip (ARCH §7 step 3's `160` class, AB axis). Re-bucket the "9 harness artifacts": `roman.sno` is self-contained (no `-INCLUDE`, no input) and was never a member of the `</dev/null` class.

  **COORDINATION:** RTX-FUNC touches `bb_func_activate.cpp` (template) — SHARED-FILE (merge, do not wait) with any other seat editing that file. Lon routes the seat. Phase-2 work (registerized ABI, RTCC) is additive on top of this and handled by RTX-11/GOAL-RTCC. The AB-6 sweep (delete C bodies) in `GOAL-SNOBOL4-BB.md` is the terminal cleanup after RTX-FUNC proves zero-C parity.
  - [ ] **RTX-FUNC-8 (NEW 2026-08-10 s_this+5): EXPRESSION-POSITION DEFINE GETS NO ACTIVATION BLOCK — THE DEFAULT-ON FLIP BLOCKER.** `DIFFER(DEFINE('f(n)','entry'))` is hoist-registered by `sno_prescan_expr` (`lower_snobol4.c` ~:2311) so LEGACY finds it, but block+bind are minted ONLY on the statement path (~:2048–2070, subject position). NEW name ⇒ undef stub ⇒ *"Undefined function called"*; REDEFINED name ⇒ stale `fn_cell` ⇒ old body under the new prototype's frame shape ⇒ **SIGSEGV**. GATE: `corpus/probe/ab_expr_define.sno` + `1011_func_redefine` green under AB=1 at N≥4 both media, with no AB=0 regression. ⛔ **NOT a prescan-site patch:** the `bb_call_proc_staged.cpp:242` guard is `ab_n > 0` (program-wide, too coarse) AND 1011's `myfunc` already HAS a block from its FIRST prototype — the block must match the LIVE definition, which runs into **compile-time vs runtime DEFINE binding** (both arms diverge from the oracle here; runtime DEFINE pending at `:740`). **LON ROUTES THE DESIGN BEFORE ANY CODE.**
  - [ ] **RTX-FUNC-8 (NEW 2026-08-10 s_this+5): EXPRESSION-POSITION DEFINE GETS NO ACTIVATION BLOCK — THE DEFAULT-ON FLIP BLOCKER.** **HATCH LANDED s_this+5b (SCRIP `8484fc7`): program-wide suppression — nested-DEFINE programs mint no blocks, call sites take the legacy fallback; `1011` AB=1 N=5 green, liveness census proves AB still fires elsewhere. RUNG STAYS OPEN for the per-fname fix behind the runtime-DEFINE routing.** `DIFFER(DEFINE('f(n)','entry'))` is hoist-registered by `sno_prescan_expr` (`lower_snobol4.c` ~:2311) so LEGACY finds it, but block+bind are minted ONLY on the statement path (~:2048–2070, subject position). NEW name ⇒ undef stub ⇒ *"Undefined function called"*; REDEFINED name ⇒ stale `fn_cell` ⇒ old body under the new prototype's frame shape ⇒ **SIGSEGV**. GATE: `corpus/probe/ab_expr_define.sno` + `1011_func_redefine` green under AB=1 at N≥4 both media, with no AB=0 regression. ⛔ **NOT a prescan-site patch:** the `bb_call_proc_staged.cpp:242` guard is `ab_n > 0` (program-wide, too coarse) AND 1011's `myfunc` already HAS a block from its FIRST prototype — the block must match the LIVE definition, which runs into **compile-time vs runtime DEFINE binding** (both arms diverge from the oracle here; runtime DEFINE pending at `:740`). **LON ROUTES THE DESIGN BEFORE ANY CODE.**
  - [ ] **RTX-FUNC-8 (NEW 2026-08-10 s_this+5): EXPRESSION-POSITION DEFINE GETS NO ACTIVATION BLOCK — THE DEFAULT-ON FLIP BLOCKER.** **HATCH LANDED s_this+5b (SCRIP `66322b3`): program-wide suppression — nested-DEFINE programs mint no blocks, call sites take the legacy fallback; `1011` AB=1 N=5 green, liveness census proves AB still fires elsewhere. RUNG STAYS OPEN for the per-fname fix behind the runtime-DEFINE routing.** `DIFFER(DEFINE('f(n)','entry'))` is hoist-registered by `sno_prescan_expr` (`lower_snobol4.c` ~:2311) so LEGACY finds it, but block+bind are minted ONLY on the statement path (~:2048–2070, subject position). NEW name ⇒ undef stub ⇒ *"Undefined function called"*; REDEFINED name ⇒ stale `fn_cell` ⇒ old body under the new prototype's frame shape ⇒ **SIGSEGV**. GATE: `corpus/probe/ab_expr_define.sno` + `1011_func_redefine` green under AB=1 at N≥4 both media, with no AB=0 regression. ⛔ **NOT a prescan-site patch:** the `bb_call_proc_staged.cpp:242` guard is `ab_n > 0` (program-wide, too coarse) AND 1011's `myfunc` already HAS a block from its FIRST prototype — the block must match the LIVE definition, which runs into **compile-time vs runtime DEFINE binding** (both arms diverge from the oracle here; runtime DEFINE pending at `:740`). **LON ROUTES THE DESIGN BEFORE ANY CODE.**
- [x] DONE — ~~ (bce9a4b)
  - [ ] **RTX-FUNC-11 (NEW 2026-08-10, clean-clone seat): ⭐ REDUCED SAME SEAT — THE DEFECT IS `-INCLUDE`-SPECIFIC: IDENTICAL BYTES RUN INLINE AND SEGFAULT WHEN INCLUDED.** Path-swap control (RULES.md's prescribed cheapest discriminating experiment): the 17-line region `global.sno:145-161` pasted INLINE prints `alive`; the SAME BYTES via `-INCLUDE` SEGV. Driver body EXONERATED — `fence_driver.sno` minus its `-INCLUDE` line is ORACLE-EXACT vs its `.ref`. EXONERATED BY MEASUREMENT: `-INCLUDE` in general (3-line include ok) - source size (23KB comment padding ok) - runtime value size (`DUPL('X',40)` ok) - key length (4-char literal keys ok) - high/non-ASCII bytes (ASCII `CHAR()` keys crash identically) - concatenation as such (literal concat ok) - entry count as such (20 short-key entries ok) - `SORT`/`$`-indirect-assign/range-exit standalone. ⛔ **MECHANISM NOT NAMED: the trigger tracks `CHAR()` calls in the included file and is threshold-like (24 ok - 36 ok - 40 SEGV - 48 SEGV), BUT 11 REAL entries x 4 CHAR = 44 calls is ok — the scalar model fits seven rows and is FALSIFIED BY THE EIGHTH, so it is recorded as a contradiction, not published as the rule.** 10-second repro (no corpus needed) + inline control in the FINDING. ⛔ **STILL MONITOR-FIRST — the inline arm is a PERFECT control (same bytes, same work, one path crashes); run the 2-way monitor against it and take the first divergent event.** ⚠ **COVERAGE BLIND SPOT WORTH ITS OWN RUNG: `crosscheck/` has ZERO live `-INCLUDE`, so the 269-program passing suite is STRUCTURALLY INCAPABLE of catching any `-INCLUDE` defect; the 17 beauty drivers are the graded set's only `-INCLUDE` coverage and all 17 are red.** ⚠ Not minted into `corpus/probe/` — a `.ref` needs the `x64` oracle, not cloned this seat. ORIGINAL RUNG TEXT: all 17 `beauty_suite/*_driver.sno` SEGFAULT IN m3 — 13 of them are FAILs in the broad sweep.** Measured at `bce9a4b`: 17/17 produce NO output, `Segmentation fault` on stderr. **THREE ENVIRONMENT HYPOTHESES RAISED AND KILLED BY MEASUREMENT:** (a) `SNO_LIB` → the absent `demo/inc` — falsified, pointing it at the drivers' own dir changes nothing and the included `.sno` files are all present beside them; (b) CWD — falsified, identical segfault from the corpus root (the natural suspect given HEAD `bce9a4b` "compile from the CORPUS root so `-include` resolves"); (c) `SNO_LIB=corpus/lib` — falsified. ⛔ **NOT ROOT-CAUSED ON PURPOSE — MONITOR-FIRST BINDS.** Next seat starts at `scripts/test_monitor_2way_sync_step_bin.sh programs/snobol4/beauty_suite/fence_driver.sno`; no cause is named here because none was measured. ⛔ **RECORDED SO IT IS NOT MISREAD AS EVIDENCE: `282−269 = 13` and `48−35 = 13` fits these drivers exactly — and means nothing yet, since they fail in every environment arm constructible here.** OPEN: regression or long-standing debt? A whole-suite segfault on the Gimpel beauty drivers is not debt-shaped — settle it by BISECT, not by reading code.
  - [ ] **RTX-FUNC-10 (PROPOSED s_this+5b): FLIP THE DEFAULT WITH THE HATCH — safe-subset AB.** GATE: full-corpus sweep no-regression vs AB=0 at the SAME binary (owed from s_this+5b, in-flight results lost with the container) · every DEFINE-bearing delta re-run N≥4 (RTX-FUNC-7) · RTX-FUNC-5 measured. Full AB incl. nested DEFINE stays behind the runtime-DEFINE routing. **LON ACCEPTS OR REJECTS THE SPLIT.**
  **COORDINATION:** RTX-FUNC touches `bb_func_activate.cpp` (template) — NOT-CONCURRENCY-SAFE with any other seat editing that file. Lon routes the seat. Phase-2 work (registerized ABI, RTCC) is additive on top of this and handled by RTX-11/GOAL-RTCC. The AB-6 sweep (delete C bodies) in `GOAL-SNOBOL4-BB.md` is the terminal cleanup after RTX-FUNC proves zero-C parity.

  - ⭐ **ADDENDUM s_this+7 (Sonnet 4.6, same container):** BOTH MODES CONFIRMED (m4 binary via `gcc -no-pie …` also rc=139 — compiler is clean, rc=0, 2.3MB asm). FAULT IS ADDRESS-LAYOUT-SENSITIVE: ASLR-ON → 17 SEGV; ASLR-OFF → 5 SEGV + 12 SIGBUS; ZERO CLEAN IN ANY ARM — changing address layout changes the *signal* not the *incidence*, diagnostic of out-of-bounds write. SCALE LAW N=8: ≤88 entries 0/8 crash; 104 entries 4/8 (probabilistic, coin-flip class); ≥112 entries 8/8 — **BISECT MUST USE ≥112 entries per workload or results are unreliable.** SCRIP UNSHALLOWED: `git fetch --unshallow` → 3148 commits, 407 since 2026-08-01 bound the range — **repeat blocker per docs (s6, PASSTHRU s12); now cleared.** SELF-FALSIFICATION: first sweep tested `[ $? -eq 139 ]` only, scoring SIGBUS(135) as clean; corrected by stdout-vs-ref diff. WORKTREE at `/home/claude/wt-probe` (`03cecd87`, Aug-1) exists, unbuilt; build must run FOREGROUND (backgrounded builds reaped). FINDING: `…SCALE-THRESHOLDED-ADDRESS-SENSITIVE.md`.
- [ ] RTX-11 **RTX-CC — REGISTERIZED ABI (phase 2): ⬅ PROMOTED + GENERALIZED 2026-08-08 (Lon strategy pivot) — pulled forward as `GOAL-RTCC.md` (universal C-boundary veneer, not per-family); the per-family registerized-signature work re-homes to RC-4/RC-6 there; this rung is a STUB pending RTCC's fold.** per-family register signatures beyond SysV (S/F in EFLAGS instead of DT_FAIL compares; >2-arg staging; multi-returns). EVERY template call-sequence change via `x86(...)` encoders per RULES (TEMPLATE-ONLY, BOTH-MEDIUM); `.s` regen ×3 fires; per-family, measured, rail-gated.
- [ ] RTX-12 **ERADICATION + FINAL BOARD:** delete `c_*` bodies + gates family-by-family (each deletion re-gated); rail all 16 working-set programs; before/after board vs the s161 baseline (claws5-match 0.65 · json-match 0.79 · calc-1 2.11 · treebank 2.73 · calc-2 3.54 rail; match-only ms board in s161 cursor); FINDING of record. ⛔⛔ **BLOCKED ON RTX-0b — THIS BOARD IS STRUCTURALLY BLIND TO RTX-1…RTX-7 (measured s188).** The five named demos touch ALLOC twice, STR twice, and AGG/ARITH zero times in a whole N=64 run; `rt_call_arr` is flat across N. **Running this board as written would report ~zero movement and be misread as "the asm ports did nothing" when it actually means "these programs never call the asm."** By elimination the rail demos' time is in the pattern matcher ⇒ only RTX-8/RTX-9 can move this baseline, and they are ordered LAST. Fix the instrument before quoting the board.
**EVERY RUNG:** the ARCH §7 protocol verbatim — read-first set, gates at current watermark (re-prove at session start), kill-switch md5, rail same-moment A/B labeled `RTX_<FAM>`, FINDING, LIVE CURSOR move. Phase-1 rungs (1–10) touch ZERO templates ⇒ no `.s` regen owed by construction.
