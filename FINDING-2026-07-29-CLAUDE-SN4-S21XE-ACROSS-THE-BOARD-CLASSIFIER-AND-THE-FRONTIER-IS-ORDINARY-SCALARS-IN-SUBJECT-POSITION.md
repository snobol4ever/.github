# FINDING 2026-07-29 (s21x-e) — STRING RUNG + GOTO LAW + ACROSS-THE-BOARD CLASSIFIER + PLANNER WIDEN; THE FRONTIER IS ORDINARY SCALARS IN SUBJECT POSITION, NOT EXOTIC KINDS

**Session directives (Lon, live):** (1) IR_GOTO: "removal + a little re-wire, yes; replacement, no" — GOTO-ERAD ladder banked (`.github` `eecaa29a`), parked behind (2). (2) "Finish RBP/RSP. Add the RSP decrement ACROSS THE BOARD, not one at a time." (3) "NO frame-relative addressing for OPERANDS. None. RBP only for housekeeping. Remove the ELIDE temporarily. Simple graph traversal with accumulated offsets — order of BBs × each BB's ζ size = stack position."

## LANDED (all opt-in under SCRIP_STMT_FRAME=1; default arm watermark held at every step; m3 311/4 · m4 311/2 · DIVERGE=2 fail-set identical throughout)
1. **STRING rung** (SCRIP `9597eb56`): IR_LIT_STRING joined whitelist+conjunct — two lines, because the planner was already string-symmetric (ZB-VAL-6a, one `bb_lit_scalar` body). Armed 7→28. Emitted shape = the design verbatim: per-lit `sub rsp,16`, tag+RO ptr, direct jmp.
2. **GOTO LAW** (same commit): slice-1's "no gotos admitted" was the string rejection MASKING optimizer-folded goto edges, never a checked invariant. Non-first heads are correct via st_x leave-then-enter (031/036/037 armed AND green). The FIRST head is st_x-exempt → an edge into ENTRY enters a bracket without leaving one (sw3 falsifier: pre-guard ADMIT, `jmp n0_lit_string_st` onto a bare enter, 16B leak/arrival). Classifier rejects entry-targeting edges until the first head grows a jumped-over st_x.
3. **ACROSS-THE-BOARD classifier** (SCRIP `35aa7cbd`): kind whitelist DELETED — admit every construct, both edges walked. Conjunct polarity INVERTED to registration-is-the-license: any reachable fc=0 node rejects unless proven TOS-consumer (BINOP/ASSIGN) or inert (GOTO). Forced by measurement: admit-all broke exactly 4 programs, ONE family (&ALPHABET/UCASE/LCASE) — KEYWORD/CALL producers slipped a LIT/VAR-only conjunct and SEGV'd m4 identically to 026 (unregistered flat-slot arm presumes the graph carve; 097 exit=139 measured). END STATE: zeta_storage fc registration is the ONE admission authority; families arm automatically as the planner widens; zero further classifier edits.
4. **PLANNER WIDEN slice 1** (SCRIP `13a0dbc4`): (a) `fc_vbinop_ok` admits DIV(3)/MOD(4)/POW(18) — the "no idiv fault path" decline was STALE vs the collapsed rtop arm (generic `rt_num_arith` call carries DT_FAIL→ω); **026/027, the original regime casualties, GREEN oracle-exact m4**. UNGATED — changes legacy arm selection too; default watermark held, bytes churn honestly at handoff sweeps. (b) `SCRIP_STMT_FRAME=1` implies `SLOT_ELIDE` off (Lon: elide off temporarily) — every result carves unconditionally, the accumulated-offset traversal (zls_build's d-simulation: position = running sum of prior ζ sizes; it ALREADY IS Lon's "simple graph traversal") is hole-free. Armed 29→31.

## ⭐ HEADLINE STRUCTURAL FACT — THE STATEMENT BRACKET DISSOLVES THE RELEASE-ARM REASON THE PLANNER WAS SELECTIVE
Registration historically mirrored per-template fallible-ω release arms (registered total-pops, op_wpop) because legacy had no statement bracket. Under the regime a fallible ω rides the statement cut: `mov rsp,rbp` normalizes ANY depth wholesale. First stale caution killed by this fact: the DIV/MOD decline. Expect the same dissolution across further families.

## ⭐ THE MEASURED FRONTIER (kind-tally of first UNREGISTERED reject per program, 316 corpus)
LIT_STRING **157** · VAR **85** · LIT_INTEGER **29** · KEYWORD_SNOBOL4 5 · CALL 5 · UNOP 2 · LIT_REAL 1. The blockers are ORDINARY scalars in POSITIONS outside the value-spine — chiefly match-statement SUBJECTS (157 tracks the 121-program pattern family). NOT exotic kinds (12 total). → **SUBJECT-CELL rung** (goal file NEXT (a)): register subject producer chains; convert IR_MATCH_HEAD's subject load from FRQ(slot) — frame-relative, banned for operands — to TOS consume into Σ/δ/Δ. Template-touching. Same shape then serves CALL/KEYWORD/relop ARG positions.

## BANKED
- GOTO-ERAD GE-0..9 ladder in goal file: verdict = removal+rewire; two holdouts by mechanism: IR_GOTO_DEFERRED (runtime dispatch, stays) and the MONITOR label taps (emit.cpp:952 is scr's ONLY monitor event source; GE-1 relocates taps to statement heads, also closing the s195 granularity gap).
- s21x-d cursor's 329/324 totals are inconsistent with its own 7/316 census and the 316-program corpus at origin — prose-rot; fail-set membership is the licensed instrument.
