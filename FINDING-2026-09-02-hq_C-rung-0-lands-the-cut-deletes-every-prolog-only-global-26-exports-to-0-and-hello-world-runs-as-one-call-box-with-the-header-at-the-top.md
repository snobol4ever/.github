# FINDING 2026-09-02 hq_C — Rung 0 lands: THE CUT deletes every Prolog-only global (`nm -D` 26 → 0, `plc_` statics 21 → 0, −5,050 lines) and hello world runs as ONE `bb_call_proc_staged` box with two compile-time leaves and the § A.1 header at the top of its frame, both modes; SNOBOL4 and Icon output byte-identical

**Row:** `prolog-rung-0-the-cut-and-hello-world-with-zero-globals` (ceo, on Lon's six rulings, CEO-149 `.github` `9fd7c67a`). **Mode:** TRIO (MODE file). **Law:** `RULES.md` § THE PROLOG REBUILD GATE; sovereign page `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § B.0 / § C ruling / § E / § E.2.
**Tree:** SCRIP `db299d41` on top of `48b09e04` (hq_B's runner `6faa3215`, hq_P's bench instruments) · corpus `9c648987` on top of `d0351a54` (hq_B's 12 ladder witnesses `1d4ff901`) · `.github` on `f8a9f9f3`. `RT_OPT=-O0`. Every verdict below was re-taken on a `make pristine` build of the pushed tree (HQ-27); the control arms were captured on the pre-cut pristine `f4532dea` build before the first edit.

## 1. The cut — what left, measured

| receipt | pre-cut (`f4532dea`) | post-cut |
|---|---|---|
| `nm -D out/libscrip_rt.so` data symbols matching `g_pl_ g_plw_ g_resolve_ g_rt_pl_ pl_wot_` | **26** | **0** |
| Prolog-named writable globals in the runtime objects (`nm -S out/rt_pic-*/*.o`, the census instrument) | 58 | 0 |
| `^static.*\bplc_` statics in `by_name_dispatch.c` (the runtime solver) | 21 | 0 |
| references to any § C symbol under `src/` (`.c .h .cpp .s .S .inc`) | hundreds | **0** (the only `g_pl_*` names left are the lowerer's five `static const char *` rung tables, compile-time RO) |
| `src/` diff | — | 50 files, +407 / −5,050; 8 files deleted |
| `by_name_dispatch.c` / `unification.c` / `lower_prolog.c` | 7,300 / 2,262 / 1,481 lines | 5,628 / 1,629 / 359 |

**Deleted, by the brief's list.** `resolution.c`/`.h` (the pred hash table moved to `lower_prolog.c` as compile-time code, its typedef to `stage2.h`); `pl_resolve.h`; `pl_area.h` + its two unbuilt tests; `bb_cell_ite.cpp` + `bb_cell_cut.cpp` (and their Makefile lines — the Makefile lists templates explicitly, a glob would not have caught it); `IR_CALL_PROLOG` (IR.h, `scrip_ir.c`, three emitter `case` sites, `zls_s4_ok`); the dormant `pl_cells_graph` graph field and its 30 reader arms (an env-gated mode, `SCRIP_PL_CELLS` default off, ZERO graphs set it — deleted, not renamed, see § 4); `pl_zf_trail_mark_off` + the `$trail_mark` slot probe in `scrip_ir.c` + three `zls_g_*trail_mark*` accessors; the seven `SCRIP_ZD_PL_*` emitter gates and `SCRIP_ZD_PL_GEN`, `SCRIP_PL_GAMMA_RETAIN`, `SCRIP_PL_ZFRAME`, `SCRIP_PL_ZA`, `SCRIP_PL_CELLS`, `SCRIP_PL_POISON_TRAP`, `SCRIP_PL_WS_RECLAIM`, `SCRIP_PL_CTERM_POISON`, `SCRIP_PL_DET`, `SCRIP_PL_BOUNDED_DUMP`, `SCRIP_NO_UL`, `SCRIP_NO_IX`, `SCRIP_NO_VVB`, `SCRIP_NO_CTR` (every one a mode; ZETA HAS NO MODES); `resolve_cs`, `resolve_choice_id/n`, the three `resolve_*_label` GROUND-ZERO stubs. **rt.c:** the retry, CP and zf3 stacks with `rt_pl_cp_push/pop/push3/pop3`, `rt_pl_retry_push/pop`, the six `g_pl_zf_pending_*` and `rt_pl_zf_resume_set/clear`; `rt_jmp_frame_lexprep2`'s Prolog arm (the memset stays); `rt_cut_set`. **Templates:** `bb_call_proc_staged.cpp`'s mailbox β re-invocation lambda and the clause-(c) bomb; `bb_suspend.cpp`'s CP push/pending poll; `xa_flat.cpp`'s epilogue-γ mailbox poll and `pl_gamma_retain_on`; `bb_move_label.cpp`'s retry-stack arm; `bb_indirect_goto.cpp` reduced to Jcon's `jmp [t]`; `bb_call_fn.cpp`'s six sink emitters (`$unify`/`$unify_lst`/`$ix_g`/`$trail_mark` inline fast paths, 410 lines) and their four extern globals; `bb_define.cpp`'s two `g_pl_trail` reads (see § 3); `bb_var.cpp`/`bb_var_ref.cpp`/`bb_lit_scalar.cpp` cells arms. **Runtime:** the `plc_*` solver + `rt_pl_call_gen` + the runtime term reader (`plc_rd_*`) + `rt_pl_between_gen`/`rt_pl_sub_atom_gen`; the **113** `$`-arms of `script_try_call_builtin_by_name` plus 5 bare-brace `$` blocks plus the 14 `$` arms of `rt_call_arr_gen` (the function itself is SHARED — `where`, `seek`, `__rk_*`, `__pas_*`, `meth_call` are Raku/Pascal/SNOBOL4's and stay); the unwind floor (`g_plw_unwind_floor`, `g_plw_floor_bypass`, `rt_plw_floor_bypass_on`, `plc_dead_cstack` + the `/proc/self/maps` parser, `dop_call_nothrow`, and the floor save/restore in `dop_call`, the four `rt_pl_dop_*` wrappers, `rt_call_arr_bl` and `rtx_plunify.s`); `g_plw_preds`+`rt_pl_pred_*`, `g_plw_poison`, `g_plw_dot_sl`, `g_plw_cwp*`, `pl_wot_*`; `rt_findall_*`; `plc_iso_*` ball builders; `dop_ix_g`, `dop_unify_lst`, `dop_trail_mark/unwind/unwind_nothrow`, `pl_builtin_is_known`, `rt_pl_det_builtin_target`, `rt_pl_ax/cmp_suffix`, the stream/read helpers; **unification.c:** `g_pl_env_area` + `rt_e_*`, the mark stack + `rt_trail_*`, `g_pl_throw_ball` + `rt_pl_throw_set/pending/match`, the copy/functor counters, `g_pl_pred_table`, the dynamic DB (`g_pl_dyn_pred_*`, `dyn_*`, `rt_pl_dyn_*`, `rt_pl_clause_gen`, `rt_pl_current_predicate_gen`), `g_rt_pl_nb` + `rt_pl_nb_*`, `g_pl_flags` + `rt_pl_set_prolog_flag`/`current_prolog_flag_gen`/`rt_pl_dialect_is_swi`, `rt_pl_current_op_gen`, `rt_pl_predicate_property_gen`, `rt_pl_current_stream_gen`, `rt_pl_stream_property_gen`, `rt_pl_bag_group_gen`, `rt_pl_can_compare_cell`; **rt_arena.c:** the cterm and cellws islands (`PL_CELL_ALLOC` is `rt_ws_alloc` now); **pl_cell.h:** `pl_trail_t`/`pl_trail_*`, `plc_dead_cstack`; `pl_bind(cell, word)` and `pl_unify(a, b)` lost their trail parameter (228 → 0 references in `unification.c`, 12 → 0 in `rt_runtime.c`). **Driver:** `g_resolve_active` + the brokered-call branch in `driver_hooks.c`, the two floor-bypass writes and the `rt_plw_floor_bypass_on` emission in `scrip.c`, the `resolution.h` include from 12 files.

**NOT deleted (the boundary, RULES clause 2):** the parser, the DESCR cell layer (`plw_cell_deref`, `plw_bind`, `plw_unify_cells`, `rt_pl_deref_val`, `rt_pl_fresh_var_ref`), the atom table, `rtx_plunify.s` (only its two floor reads went), the `dop_*` value bodies and their `rt_pl_dop_*` wrappers (`dop_call` is now `gc_point + body`), the `rt_pl_*_cell` value services, the shared boxes, the BX-0 hook, `rt_pl_dc_*` (see § 3), `rt_pl_iso_throw_*` (rewritten: an uncaught ISO error prints `Warning: goal raised exception: error(...)` and exits 1 — no ball, no global — until rung 9 lands `bb_catch`).

## 2. Hello world — the witness, both modes

`:- initialization(main). main :- write(hello_world), nl.` (`ladder__rung00_hello`, the master entry `directive_82`, `.ref` cut from swipl by hq_B).

| arm | pre-cut `f4532dea` | post-cut |
|---|---|---|
| `--compile` `.s`: Prolog-only global references | 0 | 0 |
| `call rt_call_arr_bl` / `call rt_plw_floor_bypass_on` | 3 / 1 | **0 / 0** — the leaves are `call dop_write@PLT`, `call dop_nl@PLT` |
| `main` is a box | no (a flat `pat_flat` chain of 4 boxes: `lit_string call_prolog lit_string call_prolog`) | **yes**: `FN__main$2F0` is a `zframe_pinned_base` graph; the top graph calls it through `bb_call_proc_staged` |
| BX-0 trace (`SCRIP_PL_TRACE=1`, m3) | `Call n0_lit_string … Exit n3_call_prolog $nl0 -> pat_flat_γ` | `(1) 0 Call: n0_call_proc_staged main/0` · `(2) 1 Call: n0_lit_string hello_world` · `(3) 1 Call: n1_call $write` · `(4) 1 Call: n2_call $nl` … `(4) 1 Exit … -> main/0_γ` · `(1) 0 Exit: n0_call_proc_staged main/0 -> pat_flat_γ` |
| output = swipl, m3 and m4 | — | `hello_world`, rc 0, both |
| frame addressing | `[rbp + N]` | `[rbp + N]` (33 sites) |

**The § A.1 header, measured (`main/0`: kt = 176, H = rbp + 112):** `[H+0]` F.TRMARK (0) · `[H+8]` F.CUR (0) · `[H+16]` F.RES (0) · `[H+24]` F.B0 (= r13 at α) · `[H+32]` pad · `[H+40]` γ wire · `[H+48]` ω wire · `[H+56]` caller rbp. The header is the top 64 bytes of every `zframe_pinned_base` frame: `flat_frame_bytes` grows 48 → 80 over the zls region for a pinned graph (both frame-size sites), `rt_jmp_frame_lexprep2` and the `rep stosb` seed zero `kt-64` instead of `kt-32`, the prologue stores the four words after the pin. `F.B` is `r13` itself (no slot, review C1); `F.HI` falls away. The ROOT graph (a new language-blind IR property `root_graph`, set on the top graph) seeds `r13 = 0`, `r15 = 0`, `r14 = H` before its own header store — so every β guard and ω reads a defined ball register from rung 0 on (hq_P's "r15 is a driver mmap address" is closed at the root; the trace still withholds `Exception` until C37 flips its one line). `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § A.1's table now carries these offsets.

**The lowerer (`lower_prolog.c`, 1,481 → 359 lines):** single-clause argument-free predicates, conjunction chained α→γ forward and ω→β backward (Proebsting `plus`), `write/1` and `nl/0` as `IR_CALL` leaves `$write`/`$nl` resolved at compile time through `dop_direct_fp`, user calls as `IR_CALL_PROC_STAGED` `name/arity`, `true`/`fail`; `main/0` is an ordinary predicate graph named `main/0` and the driver's `main` is the top graph that calls it (the old `$init_chain` inlined main's body, which is why main was never a box). **Every construct above rung 0 REFUSES at compile, rc=2, naming its rung:** `scrip: prolog: <what> is not on the ladder yet -- rung N lands it` (`=` and head arguments → 1, multi-clause → 2, `;` → 3, `!` → 4, `->`/`\+`/`once` → 5, det builtins → 6, generators → 7, findall → 8, catch → 9, call/N, directives, dynamic DB → 10, unknown procedure → 9). A refusal is not a wrong answer: the master board reads them as compile failures, the ladder runner as its cumulative bound.

## 3. Three boundary facts the brief's list did not know

1. **`g_pl_trail` was ALSO SNOBOL4's DEFINE value-trail.** `bb_define.cpp`'s RTX-FUNC-1/2 inline enter/leave_env read `g_pl_trail.top` as `AB_OFF_VTMARK`, `rt_ab_enter_env`/`rt_ab_leave_env` (rt.c) mark and tidy it, `gc_heap.c` tested it in the `pz` root-scan condition, and `rt_pl_dc_prep/leave_γ/ω` are the SNOBOL4/Icon DIRECT-CALL path (`rt_pl_dc_ok` admits non-generator `jmp_entry` procs of any language — misnamed with a `pl_` prefix, a rename is hygiene). The census read "26/26 · 0 · 0" because the SNOBOL4 arm is not emitted on any committed `.s`. The value-trail hooks are gone with the global (their trail was always empty for SNOBOL4: nothing but Prolog's `pl_bind` ever pushed); the SNOBOL4 board is the control, and the two control-arm `.s` are byte-identical.
2. **`script_try_call_builtin_by_name` is a shared cascade, not Prolog's.** Its head is `where/1`, `seek`, `__rk_*` (Raku), `__pas_*` (Pascal), `meth_call`; `arithmetic.c` routes `__multi_call` through it. Only the `$`-arms are Prolog's — 113 `if` arms plus 5 bare-brace blocks, removed by brace-matching; the three non-Prolog `$` names (`$accept`, `$fnc`, `$_`) have no arm there.
3. **`rt_call_arr_bl` stays** (SNOBOL4/Icon by-name dispatch and its `&ERROR` setjmp barrier); what left is the Prolog route into it — no Prolog graph emits it any more (0 on the witness; the old `IR_CALL_PROLOG` route through `pl_builtin_is_known` is gone). `rt_call_arr_gen` keeps its `find`/`upto` SNOBOL4 arms.

## 4. Deviations and judgement calls, for ceo to rule on

- **`cells_graph`.** The brief says the Prolog-distinguishing graph property is `cells_graph`. `pl_cells_graph` was an env-gated dormant mode that no graph set; renaming a dead field would have manufactured a property with no writer. The language-blind key that actually distinguishes a Prolog graph today is `zframe_pinned_base` (set only by `lower_prolog.c`, read by `emit_zframe_pinned()`/`x86_fb_pinned()` — already language-blind by name) plus `root_graph` for the top graph. If the name `cells_graph` is wanted, it is a one-line rename of `zframe_pinned_base`.
- **`rt_pl_iso_throw_*` print-and-exit(1)** until rung 9: a raising builtin (none reachable at rung 0) prints swipl's `Warning: goal raised exception: error(...)` shape to stderr and exits 1. No ball, no global, no setjmp.
- **`copy_term`/`functor` fresh variables** are numbered `-1` (address is identity); the two counter globals went. If printing needs `_G<n>` numbering, rung 8 passes a spine-cell counter to the service.
- **Trail-less `pl_unify`/`plw_unify_cells`** bind without logging until rung 1 gives them the frame log through `r13`; a mismatch mid-unification no longer undoes its partial bindings (B.11's atomicity rule is rung 1's).
- **Corpus artifacts:** `util_regen_prolog_bench_s_artifacts.sh` gained a `REFUSED` branch mirroring FENCED — a ladder refusal removes the stale `.s` and writes `<name>.s.REFUSED` naming the rung (23 of 23 bench kernels today; −263,663 lines of pre-cut assembly that referenced deleted globals). The SNOBOL4 benchmark and demo artifact sets regenerated with no change.

## 5. Instruments — three defects found by running them, two extensions

- **DONE-WHEN arm `grep -qE '\[rbp[+-]'` can never match SCRIP:** the text medium prints `[rbp + 80]` with spaces (0 matches pre- and post-cut; 33+ for `\[rbp *[+-]`). Re-cut in the baton to `\[rbp *[+-]`, ledgered.
- **DONE-WHEN `extract … ladder__rung00_hello` refuses:** the harness extracts by ENTRY name; hq_B absorbed the witness under `directive_82` with the ladder name in the ORIGIN column. The step now resolves the entry from `ALL.csv` by origin.
- **`test_gate_p7_prolog_setjmp_gone.sh` defaults its root to `/home/claude_P`** when `S4E_HOME` is unset (`R="${S4E_HOME:-/home/claude_P}"`) and reported symbols "still referenced/exported" that exist only in hq_P's checkout. Under `S4E_HOME=/home/claude_C` the symbol half is green (all seven gone from `src/` and `nm -D`); its van Roy safety half reads CLEAN 0 of 21 because every kernel is above rung 0 — REPORTED by RULES clause 4, re-pinned at rung 8. Reported to hq_P (their instrument).
- **`test_gate_pl_port_trace.sh` gained `--to N`** (the runner's cumulative bound: with `--to`, the population is the ladder origins at or below N; without it, hq_B's default `probe_plz ladder`) **and a MERGING `--cut`** (with `--to`, only the graded origins' blocks are re-cut; every other block of `ALL.trace` is kept verbatim). Before this, a cut on the cut tree would have dropped hq_B's 30 blocks for rungs 1–5 and the probe family; without `--to` the gate is UNPROVEN(2) at the first witness above the rung (`probe_plz__plz_p1_single_clause`: zero trace lines because it refuses at compile) — correct, and useless as a rung gate.

## 6. Verdicts on the pushed tree (pristine `-O0`)

- `nm -D` Prolog-only data symbols **0** (26 before) · `plc_` statics **0** · § C references under `src/` **0** · `strip_comments.py --check` **0** files · `test_gate_emit_no_lang.sh` OK · `test_gate_pl_no_new_global.sh` PASS (doomed ratchet 0 / floor 14) · `test_gate_pl_no_value_stack.sh` PASS · `test_gate_no_handencoded_bytes.sh` OK (0 sites).
- **Ladder** `test_prolog_ladder.sh --to 0`: **PASS 2/2** (witnesses=1, modes=2). **Trace** `test_gate_pl_port_trace.sh --to 0`: **PASS(0)**, examined 2, killswitch OK, perturbation OK both modes, trace m3 ok(8) m4 ok(8), answer ok; refs re-cut (merged, 42 blocks).
- **Shared-node control arms:** `corpus/benchmarks/snobol4/eval_fixed.sno` (993 lines) and `corpus/benchmarks/icon/bench_icnint_mod_isolate.icn` (231 lines) `--compile` output **byte-identical** to the pre-cut `f4532dea` build at every phase (after the emitter cut, after the runtime cut, after the header). `make test`: rc=0 on the pristine build (SNOBOL4 m3 1679/1679 · m4 1679/1679 FAIL=0 · optbypass 192/1656 ≤ 192 · every gate OK).
- **REPORTED, not gating (RULES clause 4):** Prolog master board `SUITE_BOARD total=383 m3_pass=6 m3_fail=365 m3_xfail=12 · m4_pass=6 m4_skip=365 m4_xfail=12` (pre-cut 348/371 on `f4532dea`); Prolog smoke 1/5 in all three columns (four witnesses are above rung 0); van Roy acceptance CLEAN 0 REFUSE 0 other 21 (all 21 kernels refuse at compile).

## 7. What rung 1 starts from

`pl_refuse` names rung 1 for `=`, `\=`, head arguments and `TT_UNIFY`. The lowerer's `term_e`/`term_lval_e`/`mkc_node` (compound and list building through the `$mkc` leaf → `rt_pl_dop_mkc`) are kept; `unify_pair`/`unify_lst_build` and `lower_pl_clause_into`'s head-unification loop were deleted with the control lowering and are the shape to bring back as `IR_CALL "$unify"` leaves → `rt_pl_dop_unify` in `rtx_plunify.s` (the direct table still maps `$unify`, `$mkc`, `$is_v`, `$ax_*`, `$cmp_*`); the frame slots `F.A[i]`/`F.G[j]` come from `g->nparams/pnames` + `g->nlocals/lnames` exactly as before; the log rule of § B.11 reads `F.B` through `r13` inside the service — `plw_bind` is where the push goes.
