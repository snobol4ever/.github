# ARCH-RT-CALL-PROTOCOL — design rationale recovered from `src/runtime/rt/rt.c`: the procedure call protocol

**What this is.** `src/runtime/rt/rt.c` is the C side of every procedure activation: the mode-3 outer thunk that enters a graph, the proc registry record, the open/prologue/epilogue leaves an emitted call site runs, the C-to-graph transfer shim, the by-name (dynamic) entry record, and the return-protocol discriminators. On **2026-08-20** commit `e25a5daf` stripped 6,919 comments tree-wide; this file went from 2276 to 1991 lines and lost **156 comment blocks**, most of them a measured contract with a witness program, a killswitch and an alignment argument, which is exactly the text a later reader of an `__asm__` string cannot reconstruct.

Recovered and relocated by **the cto, 2026-09-16**, row `rationale-rt-c`, per `RATIONALE-INDEX.md`. ⛔ **The comments are NOT going back into the source** (RULES.md, one comment form). This document takes the **call protocol** first, by the same criterion as `ARCH-FRAME-LAYOUT-FIXED-CELLS.md`: it is the densest cluster (about 70 of the 156 blocks carry an NCB-1 / GLOBALS-GONE / PROC-CONV / WIRE-STACK / TINY / PL-REGAIN / ICN-* tag), every block is a measured verdict, and it is the spine: the activation frame and the boundary between emitted code and C, which ARCH-ENGINE.md's CALLING-CONVENTION RULING governs. The remainder (the match-time by-name primitives, the B1c mode-4 seam, the AB-2 leaves, GENP in full, and the retired PL-FR-4 and ICN-FR-5 stacks as history) is row `rationale-rt-c-remainder`; until it lands, the raw text is `git -C SCRIP show e25a5daf^:src/runtime/rt/rt.c`.

⭐ **Everything below was re-verified against the LIVE tree (SCRIP `db4a6b1fc`, `src/runtime/rt/rt.c`, 2087 lines, 93 commits since the strip) before being written down.** The recovered text says what its author measured in July and August; the ✅/⛔/⚠ notes say what is true today, and where they disagree, today wins and the disagreement is recorded.

---

## §0 Vocabulary

- **The staged-args medium.** `g_call_args[CALL_ARGS_MAX]`, a process-lifetime DESCR array every call road writes its actuals into and every prologue reads from (the REGAIN-1 "arg medium" decision). Nothing rewrites its tail (§4, ICN-ARG-NULL), which is why the collector's root walk over all of it is live work for hq_prolog today.
- **Leaf.** A C function that calls no Byrd box: the open leaf selects the protocol and runs the prologue; the epilogue leaves close it. The emitted call site is `open → frame → transfer → epilogue`, protocol-agnostic (§3).
- **Regime.** Whether a proc's EMITTED body is a jmp-entry blob (self-allocating, exits by wire) or a call-regime body (entered by C `call` into a caller-made frame). Recorded on the proc record (`jmp_entry`), never re-derived (§2).
- **Wires.** The γ/ω landing addresses a body exits through. Today they ride a pushed pair at the callee's TOS (§5), not r10/r11 (ARCH-SNOBOL4-RTX.md: the r10/r11 product-wide wire registers are retired).
- **Floater.** The RETURN/FRETURN exit of a dynamic-scope (DEFINE) body, which reads the pair at TOS and pops it (§7).

---

## §1 `rt_outer_call` and `rt_outer_call_delta0` — the mode-3 entry thunk

**r12, three rulings in sequence.** R12-EXTERN (Lon s173): mode 3's OUTSIDE sets the environment register, the twin of the mode-4 wrapper's `mov r12,[RT_DCAP_TOP]`; the blob no longer self-seeds and every graph assumes r12 = the live pend/dcap top on entry. R12-FREE-1 (Lon 2026-07-29) DELETED that seed to vacate r12 for a frame register; ZW-3 (s23l) REVERSED it: r12 IS the live CAS/dcap top, `rt_outer_call` seeds it from the absolute slot `0x70000000` (`RT_DCAP_TOP == RT_PIN_BASE+0`, locked by a `_Static_assert` in `rtx_init.c` and again in `runtime_eval.c`) and saves/restores the CALLER's r12 across the call (SysV callee-saved).

**ONE-SHOT-BRIDGE-M3 (s22q): the alignment adjuster is 16, not 8, MEASURED not reasoned.** Mode 4's `main` jumps into α having moved 24 bytes (`sub rsp,8` + two pushes), so α arrives at rsp ≡ 0 (mod 16); mode 3 CALLs α through this thunk and the `call` pushes 8 more, so the old `sub $8` delivered α at rsp ≡ 8, an 8-byte skew inherited by every C call the graph then makes. Witness `002_output_integer_literal`: the graph-side `NV_SET_fn` entry measured rsp%16 = 8 in m3 versus 0 in m4, SIGSEGV in glibc `dl_iterate_phdr`'s `movaps` during `gc_static_segs_init`. `sub $16` puts α at ≡ 0 in both modes: the bridge's parity contract became one law.

**`rt_outer_call_delta0` (ICN-FR-5 RESTORE, s238): zero r14 once at program entry.** Icon's `&pos` compiles to r14+1 unconditionally (the `g_scan_regs_live` gate was deleted deliberately: a `?`-less scanning callee runs inline scan primitives on the caller's ambient subject, where the emit-time `in_scan` flag reads 0 but the registers are live) on the stated invariant "outside any scan r14 is 0", which nothing established: at `main` entry r14 holds libc residue and `&pos` read 4304233 (0x41B069). The two original init sites were both gated on `zframe_graph && !icn_cells_graph`, and Z-1 stamped `icn_cells_graph=1` by default, which silently disabled them on every arm. It is a SEPARATE SYMBOL rather than an edit to `rt_outer_call` because that thunk is shared with SNOBOL4/Prolog/Pascal/Raku and the SN4 match family also uses r14: the driver selects this variant for Icon only, so every other language stays byte-identical BY CONSTRUCTION. **ABI hardening (s238-b):** the first cut was `xor r14d,r14d; jmp rt_outer_call`, which RETURNS to its C caller with r14 destroyed; r14 is callee-saved and `rt_outer_call` returns normally (unlike `icn_zf_main_call`, whose `jmp` is correct because its target never returns). `main` allocated r14 zero times at the time, so it did not bite, but the ICN-FR-5 CLOBBER-FIX class had bitten once already; push/pop costs two instructions and removes the hazard permanently.

✅ **LIVE:** `rt_outer_call` is `push r12; sub $4194304,rsp; mov 0x70000000,r12; RTCC inbound load; push rcx; push rcx; jmp *rax`, landing `add $4194304,rsp; add $16,rsp; pop r12; ret`. Two changes since the strip: the `sub/add $16` adjuster became the two pushed landings (the WIRE-STACK pair of §5, both `rt_kw_return_level_zero`), and the thunk reserves **4 MB of ZLS headroom** under the graph (`9e6c187c8`, "zeta-frame-rsp-second-wild-write", matched by the mode-4 main prologue emitters). The parity arithmetic still lands α at ≡ 0: entry ≡ 8, `push r12` → 0, 4 MB is a multiple of 16, two pushes → 0. `rt_outer_call_delta0` is `push r14; xor; call rt_outer_call; pop r14; ret`, the hardened form.

---

## §2 `rt_proc_t`, the regime record, and the record's baked offsets

- **NPSPLIT (s22w): `nformals` is the FORMALS-ONLY bound** (arg-bind, excess-clamp, dc/slim admissions); `nparams` keeps the full-name-set meaning (save/restore span, pname bound, pad loop). SPITBOL Ch.8: ALL prototype names are saved and null-initialised, only formals bind, excess actuals are evaluated then IGNORED. `nformals == 0` means an unsplit registrant and consumers fall back to `nparams`. APPENDED LAST because the runtime asm bakes the earlier offsets.
- **`rest_kind` names WHAT differs about the variadic tail, never WHICH language asked:** 0 = the DT_DATA list `rt_make_list` builds; 1 = the SOH-joined flat aggregate (Raku's canonical from-slurpy-flat, `List.rakumod:271`) that `.elems`, subscripts and reductions already understand; 2 = nested aggregate.
- **`jmp_entry` (NCB-1d) is the REGIME RECORD:** 1 = the emitted body is a jmp-entry blob, armed by the driver proc loops (= `!is_generator` for table procs), in-process for mode 3 and via the printed startup registration for mode 4; blocks/rules/methods registered outside those loops keep 0 and stay on the call-regime C window. The C transfer functions select the window by THIS recorded fact, never by re-deriving the emit-side predicate.
- **`zstatic` (PS-1b, s151):** 1 iff the proc's blob graph was DEFER/VALUE-free with a known region; default 0 is conservative, so an unregistered proc reads as chain-path safe.
- **`rt_k_level_p` (RTX-FUNC-1): a hidden cell exported through a pointer, and why neither promotion works.** `bb_define`'s α does `k_level++` inline, so the m4 executable must name the cell, and a hidden symbol is absent from the `.so`'s `.dynsym` (every m4 DEFINE-bearing program would fail to link, the `g_cap_gen` class). But the hand-written `.S` reaches `rt_k_level` with a direct `R_X86_64_PC32`, legal ONLY while it is non-preemptible: promoting it fails the `.so` link outright (measured). The hidden visibility is LOAD-BEARING for the in-`.so` asm, so the cell stays hidden and the pointer carries it across the boundary; emitted code pays one extra load and both media use the identical shape.
- **ONE-REG (Lon s119): `rt_proc_register_rec` replays EXACTLY the per-proc call sequence the pre-s119 m4 startup block emitted**, same order, same skip conditions; `_Static_assert`s pin the 64-byte record layout the driver spells in `.quad`/`.long`, so a drift is a compile error, not a silent wrong-field read.

✅ **LIVE:** the asserts hold at `rt.c:428-430` (`fn` at 8, `name` at 0, `is_generator` at 0x4c, `dyn_scope` at 80, `sizeof == 128` for the `shl 7` stride) and the ONE-REG assert at 2069; `rt_k_level_p` at 420; a `gen_region_ft` field was added after the strip. ⚠ The assert messages still name `rtx_call.s`/`rtx_plcall.s`, which were deleted at s55 (§3); the offsets are now consumed by the surviving `rtx_*.s` files, and the message text is stale, not the constraint.

---

## §3 NCB-1 LEAF SPLIT, GLOBALS-GONE (s55), and the stubs it left

**The split (Lon's one-entry convention).** The dyn-scope call trampoline (V1 `rt_call_named_proc`, V2 `rt_call_proc_direct`, identical 16 steps differing only in how the record is found) was decomposed into two STRICT LEAVES with the frame and the `p->fn` transfer between them, in exactly the shape the emitted call site takes: `prologue leaf → frame (rsp bump) → call p->fn → release → epilogue leaf`. State that must survive the transfer (save base, Σ, want-name, result name, the record) rides a LIFO context stack rather than C locals, so the emitted site needs no ζ grant to hold it. **LIFO is sound BY THE LANGUAGE DEFINITION, not by luck:** SPITBOL's DEFINE saves formals AND locals on a pushdown stack and restores them on RETURN/FRETURN/NRETURN (manual Ch.8), so call nesting and save/restore nesting are the same nesting, which is precisely why an assembly call/ret may carry it.

**GLOBALS-GONE (Lon s55, in-chat: "Remove g_pcall* regardless of who uses them. We do not do that here.").** `rt_pcall_t`, `g_pcall`, `g_pcall_wires`, `rt_flat_wires_t`, `g_flat_ret_snapbuf`, `rt_pcall_grow`, the RTX-4 test accessors, `c_rt_flat_wire_adopt`, `rt_proc_epilogue_body` and the two `rtx_call.S`/`rtx_plcall.S` ports were ERADICATED; linkage rides the wires plus the stack. The leaves that used to read the record became stubs, each carrying an OWED note: `rt_proc_open_fn` returns 0 (fn now rides the open's own return, the rax channel), `rt_frame_prep` returns 0, `rt_jmp_frame_lexprep`/`lexprep2` no-op ("Icon/PL lexical prep OWED a stack-resident record"), `rt_gen_get_fb` returned 0 ("Icon FR-4 resume OWED a stack-resident carrier"), and the `[fb+0]` lift in `rt_proc_call_epilogue_ret` became a pass-through. **FN-RET OPEN (s104):** two consumers of the eradicated `rt_proc_open_fn` (the `rt_dcap_pump` COND flush and `rt_cap_open` IMM) were never migrated, so every computed-name capture transfer loaded `fbytes`, nulled it through the stub and jumped 0; `rt_proc_call_open_fnret` is the s55 rax-channel contract for them (admitted == nonzero == the transfer target).

**ALIAS LAYER COLLAPSED (s180, RT-CONSOLIDATION rung 1, Lon 2026-08-20: "We do not want two versions of the same code to maintain").** The unprefixed hot symbols had been kept alive as thin wrappers over `c_rt_*` bodies, staged for an RTX re-port "when the stack-resident record exists". The staging was retired: the C bodies carry the `rt_*` names directly (one function, one name), and a future RTX port lands as a WHOLE replacement `.S` defining `rt_*` and deleting the C body in the same commit, never a parallel twin. `rt_faildescr` is RTX asm (`rtx_misc.s`) and its C twin was deleted the same rung.

✅ **LIVE:** the stubs are still stubs (`rt_proc_open_fn` 1650, `rt_jmp_frame_lexprep` 1782, `lexprep2` 1787, `rt_frame_prep` 1792, all returning 0 or no-op); `rt_gen_get_fb` and the ICN-FR-5 state stack are gone entirely (Icon's generator resume lives in the ζ-frame regime now, ARCH-ICON-RTX.md). The `rtx/` directory holds no `rtx_call.s`.

---

## §4 The open leaves: `rt_proc_call_open`, the det family, the slim arm, and the two prologues

- **`rt_proc_call_open` (OPEN LEAF):** finds the proc, selects the protocol, runs the matching prologue, returns the 16-aligned frame byte count (0 = no body, the site's FAIL arm). Args are read from the staged medium. **Cells are resolved ONLY on the dyn path:** `NV_PTR_fn` interns a global cell per name, so resolving on the lexical path would mint spurious globals for the lexically-scoped frontends' formals.
- **`rt_proc_call_open_det` (PL-REGAIN-1 slice A, s100):** the emit-time-resolved det call's fused open: the caller lowered a LITERAL non-dyn callee, resolved its dense registry index at emission (registration order is identical in-process and in the mode-4 startup bake, so the index is stable in both media), and calls with the index: no name, no hash, no separate fn crossing. Guards precede every side effect, so a refuse is side-effect-free.
- **The FUSED DET OPEN family `rt_proc_call_open_det0..4` (PL-REGAIN-4):** the site passes CALLER-FRAME CELL POINTERS (`lea [___+slot]`) and the leaf copies them into `g_call_args`; ONE crossing replaces `{rt_arg_stage × nargs + open_det}`; nargs > 4 keeps the classic stage chain (tak/4 was the ceiling). **ICN-ARG-NULL (s239):** the classic path null-filled `[nargs, nparams)` inside `rt_frame_bind_args`; folding the staging into the open dropped it, so an UNDER-SUPPLIED call left the missing parameter holding the PREVIOUS activation's argument, because **`g_call_args` is a process-lifetime medium and nothing else rewrites the tail.** The Icon ζ-frame installer reads `g_call_args[0..np)` verbatim, so `try(p,a)` against `procedure try(p,a,b)` saw the prior call's `b`, `\b` succeeded, and `atan(x)` silently evaluated as `atan(x, 10.0)`. Each leaf now seeds the tail with NULVCL before the prologue, keyed on the callee record's arity, never on a language.
- **BP-7 SCC SLIM LEAVES (`rt_proc_call_open_slim`):** the static-save-set convention. The emitted static arm does the save-set old-value saves (GVA cell → caller rsp block) and the arg installs INLINE; the leaves carry only the per-call residue (Σ save/restore, want-name, monitor events, k_level, vtmark tidy). `open_slim` runs AFTER the inline saves and BEFORE the installs, and every guard is checked BEFORE any side effect so a 0 return falls back to the classic arm with nothing to undo. `rt_c2b_arm_trap` is the FATAL for "slim open refused at RUNTIME on an fc-armed call site".
- **`rt_proc_call_prologue` (dyn):** NPSPLIT clamps binding at the formals; the save push then null-inits every remaining name, locals included, which is the manual's locals discipline verbatim.
- **`rt_proc_call_prologue_lex` and ICN-VARARG:** a trailing `[]` parameter must receive a LIST of the surplus arguments (`icon-master/src/runtime/invoke.r:105-133`: `nparam<0` marks variable arity, short calls null-pad first). `rt_frame_bind_args` did that; the callee-carves-its-own-frame path never reached it because the two flat installers (`rt_icn_zframe_args_install`, `rt_lcl_proc_args_install`) are plain `slot[i] = g_call_args[i]` copies. Measured: `note("p0")` bound the STRING, so `!a` generated `'p','0'`; `note("x","y")` dropped `"y"`; `note()` bound `&null`. **This prologue is the ONE site holding BOTH the proc record and the true per-call-site nargs (every det open leaf funnels here)**, so normalising `g_call_args` to exactly `nparams` entries here fixes both media by construction. ⛔ **`jmp_entry` is the discriminator and it is load-bearing:** `rt_proc_call_gen_h` calls the open AND then `rt_frame_bind_args` on the same call, so an ungated collapse would run twice and nest the tail list inside itself; generators carry `jmp_entry=0`.
- **PL-DC (REGAIN-1 slice C, s108), `rt_pl_dc_ok`:** the emit-time-resolved DIRECT det call through a per-proc dc stub, ONE slim prep crossing, γ/ω ret-shims, no pcall record; the vtmark lives in a FRAME CELL the stub carved (the FACT RULE "no §10 global"; longjmp-immune for free). Eligibility is the ONE predicate both the site arm and the driver's stub arming read (table facts only; `p->fn` deliberately untested because it is unset at m4 compile time). `LBL__` main-program pseudo-procs are excluded structurally (s119): they are entered only through `rt_chain_enter`, and admitting them tripped the FATAL guard on CODE label transfer. Hatch `SCRIP_NO_DC=1`.

✅ **LIVE:** all present. `open_det0` seeds `[0, nparams)` with NULVCL (line +6 of its body); the VARARG arm is at 1662-1663 gated on `SCRIP_VARARG_TAIL` and `p->is_variadic && p->jmp_entry`; the `LBL__` exclusion at 1467. ⚠ The PL-DC vtmark frame-cell store as recovered (`region_bytes + 32`) no longer greps; the family was reshaped after the strip (`rt_pl_dc_ok` is read from five files), so cite the live `rt_pl_dc_*` bodies, not the recovered arithmetic.

---

## §5 `rt_proc_enter` — the two-landing transfer shim and the WIRE-STACK pair

**PROC-CONV (R12-FREE rung 2): γ and ω get SEPARATE landings, each statically knowing its port**, so the FAILDESCR sentinel round-trip (the proc-level LAST_OK) is deleted rather than re-derived (the s61 finding: the IR already knows the port; `lower_snobol4.c` routes RETURN to γ and FRETURN to ω; the call regime's `IS_FAIL_fn` round trip was an impedance mismatch, not information). ⚠ **EXACTLY FIVE PUSHES:** SysV 16-byte alignment is load-bearing through the jmp (rsp ≡ 8 at entry, +40 bytes ≡ 0; the blob's 16-multiple `sub rsp,K` carries it), the measured s60 correction. Under rsp frames (R12-ERAD s65) the blob's LIFO exits fully unwind BEFORE the jmp, so both landings arrive at the pre-jmp rsp and the r12 anchor is deleted.

**EXPR-THUNK RESULT (class B, s96):** the γ landing tail-calls `rt_proc_call_epilogue_γ(frame0)`, which returns what it received in rdi:rsi, but every wire-exiting body delivers its value in rax:rdx. Measured: `*(X 'b')`, `*GT(N,3)`, `*IDENT(X,'a')` reached the landing with the right value in rax:rdx and the C caller received whatever rdi:rsi held (the last operand load before the body's final C call), benign-looking null garbage for `*DIFFER(X)` shapes, whose MATCH was accidental. The landing forwards rax:rdx into rdi:rsi. **EXPR-THUNK EXITS (s96):** every emitted body exits through the wires, yet this opener delivered its landings only in rcx/rdx and the RTCC inbound load then seated the CALLER's banked wires last; `S BREAK(',') *DIFFER(X)` unwound cleanly and jumped through wire 0. Both arms seat the landings AFTER the load; a fresh C-entered activation OWNS its ports, and the caller's wires survive in `rtccb` and reload at its own return.

**WIRE-STACK rung 2 (Lon s194: "PUSH <fail_location> PUSH <succeed_location> at the CALLEE").** The C entry road is the one with no template pass site, so rung 1 converted the exits and left this entry speaking registers. ⛔ WHY THE PAIR AND NOT THE REGISTERS: `rtccb` banks r10/r11 and it is a FLAT GLOBAL with NO nesting; an inner activation entered through C overwrites the outer activation's banked wires, the measured M1 SIGSEGV (`r11 = rip = rtccb`). A pushed pair rides this activation's own frame and cannot be clobbered by a nested one. FAIL is pushed FIRST so it lands DEEPER: `[rsp+0]` = γ, `[rsp+8]` = ω, the same order `SCRIP_SLIM_PAIR` and `bb_glue_pass_wires_blob` use. The landing drops the pair (`addq $16`) BEFORE the callee-saves come back, the same 16 on both arms so γ and ω stay depth-symmetric at the epilogue join. It was BUILD-TIME rather than runtime DELIBERATELY: a runtime arm needs a flag variable in the runtime and RULES.md forbids a new global without Lon's grant, so the default build was byte-identical by construction.

✅ **LIVE (`rt.c:1510`):** five pushes, `lea 2f→rcx`, `lea 3f→rdx`, RTCC load, `push rdx; push rcx; jmp *rax`; both landings `addq $16` then five pops, then `jmp rt_proc_call_epilogue_γ` / `_ω`. No `#if` remains: the pair is the only form. `rt_chain_enter` is no longer defined in this file (it survives as the two `_Static_assert` messages in `runtime_eval.c` and as the `LBL__` route name in §4).

---

## §6 `rt_tiny_record_enter` — the by-name dynamic entry record (s104, ARG CONTRACT CORRECTED s118)

The C twin of the emitted TINY dyn call site, re-measured verbatim from the PASSING two-arg `R('a','b')` asm: the record is FIVE quads `{nargs, γ, ω, argbytes, argbase}` and the ARGS RIDE THE STACK ABOVE THE RESULT CELL IN REVERSE ORDER (last arg nearest, at `[rsp+16]`); the callee computes formal i at `caller_rsp + argbytes − 16i` and NEVER adds argbase, so the arg block is PINNED at `[rsp+16)` and cannot be relocated. s104 to s117 wrote a THREE-quad record AT `[rsp+16]`, on top of the arg block, so a by-name call published `nargs=0` and every formal bound null (the `GOT-` vs `GOT-ab` class); widening the count alone made the callee read the record's γ/ω quads as args (SIGSEGV), which is why the count and the layout had to move together. Contract in: rdi = fn (the sealed `alpha$` cell content), rsi = nargs, args from `g_call_args`. At the jmp: rsp = &result cell, `[rsp+16 .. 16+16n)` = args reversed, rcx = &record parked ABOVE the args. Back: the callee restores rsp, stores the DESCR into the cell and jmps the recorded landing, which re-seats rax:rdx and runs the same NRETURN consult the emitted landings run (`rt_nret_fix_tiny`). The frame is VARIABLE (56+16n), so the landings unwind off rbp, which is NOT in the VM register contract and which emitted bodies never touch or push/pop symmetrically. Alignment: entry ≡ 8, +8 rbp → 0, +40 five pushes → 8, −(56+16n) → 0 at the jmp.

**D-18c (s161): AN UNSEALED CELL IS NOT A TARGET.** `bb_ab_slot_for` initialises every fn cell to `rt_ab_undef_fn_stub` AND the cell allocator mints on first request, so `rt_dyn_alpha_fn`, asking for a name nobody sealed (every EVAL-fragment `EXPR$`/`PAT$` thunk: their chains carry no `<name>_α` label for the seal walk), minted a stub-holding cell and "preferred" it over the LIVE fallback: Error 22 at match time for every deferred call inside an EVAL-built pattern, both modes, beauty's grammar wall. The stub is the allocator's OWN sentinel, so testing against it is exact, not heuristic. **s108 (Lon: "if they need to be on, then turn them on"):** the NRET_CAP/DYN_ALPHA pair default ON (pair on = PASS, off = rc 134); `SCRIP_DYN_ALPHA=0` restores the prior default. **SPELLED-TWICE (s117):** `p->fn` from `rt_define_site` is the GENERIC ENTRY THUNK, the wrong protocol for an emitted body (`rip=_rtld_global`, the OPSYN `&` SIGSEGV); `rt_call_named_proc` takes the same sealed-`alpha$` arm as its sibling `rt_call_proc_descr`, verbatim, gated `SCRIP_BYNAME_ALPHA`. A THIRD by-name path (`rt_proc_enter((void *)p->fn)` at the tail of `rt_call_proc_direct`) was named a suspected twin and left verbatim because no witness reached it.

✅ **LIVE:** the entry at `rt.c:881` is six pushes (rbp + five), `sub 56+16n`, the `g_call_args` copy loop; the stub test at 979 and 1026; the s108 default and the `SCRIP_BYNAME_ALPHA` arm present. ⛔ **`rt_nret_fix_tiny` is now a plain pass-through** (`wn = rt_g_want_name; return rt_nret_fix(r, wn)`): the SN4-CAP-NAME-STRICT s178-c re-arm (want_name re-arming while a real DT_N rode up a two-landing chain, the 17-capture beauty skeleton witness) is gone from this landing; whatever holds that case now is not in this file.

---

## §7 The return protocols: γ, ω, the named twins, and the two discriminators

- **γ ENTRY is RETURN and NRETURN; ω ENTRY is FRETURN; there is no fifth port** (RULES.md: FOUR PORTS). NRETURN yields a NAME and is a γ citizen (the lowerer routes its node to the same exit as RETURN, the flag riding `rt_g_ret_by_name`). Arriving at ω IS the failure signal (s61 ruling 1); a failing lexical proc returns FAILDESCR (s62 ruling c). ⚠ The call-regime lexical arm was PORT-AGNOSTIC (read `[fb+0]` whether RETURN or FRETURN) and was preserved verbatim pending a ruling.
- **NAMED γ/ω TWINS (s112): the non-slim arm's missing return contract, MEASURED.** The classic `rt_proc_call_open` arm's landings called the bare epilogues, which did nothing but decrement the level and hand back whatever junk rode rdi:rsi, so a DEFINE'd proc reached through that arm returned NULL and NEVER restored its save set. Witness `probe/mon/mon_return_contract.sno`: oracle `R1=5 A=OUTER-A L=OUTER-L`, non-slim `R1= A=4 L=104` (the result lost AND the actual leaking over the caller's variable); manual Ch.8 pp.103-104 verbatim on both halves. Regime-independent (`SCRIP_SCC_OFF=1` reproduced it with GVA fully on). The twins are the slim arm's γ contract for the arm whose save set lives on the runtime name-save stack: `rcell ? *rcell : NV_GET_fn(rname)`, then `rt_name_restore`, then the monitor tap. **NO NEW GLOBAL:** the restore base is DERIVED as `g_name_save_top − rt_proc_save_count(p)`, recursion-safe by the same LIFO discipline; only a `dyn_scope` proc may restore (a lex proc would corrupt a stack it never wrote).
- **SN4-CAP-NAME-STRICT (s170) in `rt_nret_fix`:** under the strict arm a NAME-CONTEXT caller (`wn==1`, only the deferred-capture sites) keeps the by-name flag STANDING so it can tell NRETURN from RETURN and clears it itself; every `wn==0` caller clears as before.
- **GENP &level DANCE (s92):** a spine-resident generator's record pops at its FIRST delivery (the ONE-POP LAW: the open pushed one record, only the first landing runs an epilogue leaf, so abandonment stays free), but `&level` is the LIVE-CHAIN depth and a resumed generator runs one deeper than its resumer; the caller's β edge raises the counter before `jmp [rsp]` and the resumed-delivery pass leaves take it back down. Nested resumed generators compose, ±1 per resume span.
- **ICN-WIRE-PAIR (s244), `rt_define_returns_by_frame`: does this callee CONSUME the pair at TOS?** Two return protocols share the flat rcx/rdx-wire call arms. The RETURN/FRETURN floater (DEFINE role-1/2) reads `{γ,ω}` AT TOS and POPS 16 on the way out, which is why the sites push the pair; the plain jmp-entry protocol takes both wires in rcx/rdx, saves them INSIDE its own frame and leaves by `add rsp,kt; jmp rcx`, never reads TOS and never pops, so a pair pushed for it is never released and the CALLER resumes 16 bytes low. Witness `every p(1|2|3)` against `procedure p(a,b)`: the enclosing disjunction's arm counter aliased the argument descriptor and the loop ran once. ⛔ **THE DISCRIMINATOR IS `dyn_scope`, NOT `jmp_entry` ALONE:** a DEFINE-site callee is both dyn_scope and jmp_entry and still exits through the floater, so gating on jmp_entry alone suppressed a pair the floater went on to pop and the callee returned into freed stack (`1010_func_recursion` SEGV, both media). A callee that is jmp-entered and carries NO dynamic-scope save/restore protocol is exactly the self-unwinding class; keyed on the record's own protocol bits, never on a language.

✅ **LIVE:** `rt_proc_call_epilogue_γ` is `rt_lvl_close(); return frame0;` (the level bookkeeping moved into `rt_lvl_*`), `_ω` likewise then FAILDESCR; `rt_proc_save_count` and the twins present; `rt_define_returns_by_frame` at 1914 reads the record.

---

## §8 GENP slice-2 — generator PROCEDURES on their own stack per instance (Lon 2026-07-17)

A generator-proc activation is a `scrip_coctx_t` on its own pthread stack (GC-heap carved and registered, `rt_coexpr.c`; ARCH-ENGINE.md's CO-EXPRESSION STACK RULING). The jmp-entry body runs UNCHANGED on the instance stack; the suspend arm transmits the value via `rt_genp_yield` and resumes by falling through when the caller re-activates; real return/fail unwind through `rt_proc_enter`'s landings into `rt_genp_entry_c`, which transmits done=1 (final value) or done=2 (failed) and parks; destroy joins the parked thread. **The s94 repair:** `rt_proc_enter`'s γ landing pops five saved registers, correct for det bodies (fully unwound before the jmp) but at a RETAINED frontier it EATS the 16-byte resume record plus 24 bytes of live frame into rbx/r12..r15 and the next wire jmps frame junk (the rip=r12 class). The instance-thread shim therefore enters the blob with rcx/rdx wires and landings that POP NOTHING (callee-saved ride through the co-expression's own ABI preservation); first-vs-resumed is the instance once-flag (ONE-POP); every γ delivery parks and, on re-activation, resumes via `jmp qword [rsp]` (the record sits AT the frontier by LIFO balance on the instance's own stack); three sentinel qwords keep the 16-byte entry alignment (call 8 + 24 = 32). The caller's callee-saved registers are captured FIRST (rbx/r12 GVA and ζ bases, any live scan triad) and ride into the thread; the live-instance list membership IS the flavor discriminator (a handle or legacy frame pointer can never collide with a listed instance address).

✅ **LIVE:** `rt_genp_entry_c` and `rt_genp_lookup` present; the full per-field layout and the once-flag are the remainder row's material.

---

## §9 Routing: what this document does NOT carry

- **`rt_zeta_cstack` / `rt_zls2_push` / `rt_zls2_release_to`:** the storage-axis switch around the C-side frame push (`alloca` versus the ZLS2 bump). ⛔ Gone with the port and storage axes (`ARCH-FRAME-LAYOUT-FIXED-CELLS.md` §1, RULES.md § ZETA HAS NO MODES); the C window's frame is `alloca` only.
- **PL-FR-4 (`g_pl_retry`, `g_pl_cp_stack`, `g_pl_zf3_stack`, the `g_pl_zf_pending_*` globals, the WAM B-register argument):** ⛔ gone; the live Prolog frame design is `ARCH-PROLOG-THREE-ZETAS.md`. **ICN-FR-5 (`icn_gen_state_t`, `g_icn_gen_stk`, `rt_gen_get_fb`):** ⛔ gone; `ARCH-ICON-RTX.md`.
- **`rt_pat_prim_int` / `rt_pat_prim_str` (D08, MODE34-5b), the B1c mode-4 GVA seam (s170), the AB-2 strict leaves and `rt_ab_undef_fn_stub` (error 022), and GENP's per-field record:** ✅ **relocated below** by row `rationale-rt-c-remainder` (cto, 2026-09-18) — §10 the match-time by-name primitives, §11 the AB-2 leaves and the LADDER AB stub, §12 the B1c mode-4 GVA seam, §13 the GENP record and the two spines. Nothing of `rt.c`'s stripped text is now owed.

---

⭐ **§10–§13 were recovered and relocated by the cto on 2026-09-18, row `rationale-rt-c-remainder`, and re-verified against the LIVE tree at SCRIP `be0d338c3` (`src/runtime/rt/rt.c`, 2091 lines).** Same discipline as §1–§9: the recovered paragraph says what its author measured, the ✅/⛔/⚠ note says what is true today, and where they disagree today wins and the disagreement is written down. **Four of the notes below are disagreements, and two of them are live defects** (§11's dead slot and dead twin).

## §10 `rt_pat_prim_int` / `rt_pat_prim_str` — the match-time BY-NAME primitives

**D08 (s22 CLIMB) — the integer twin.** Match-time integer fetch for the deferred-integer primitives `LEN(*var)` / `POS(*var)`, called from `bb_match_len`'s (and its siblings') α/β when `op_sval` starts with `*`. Reads the named variable **at match time**, coerces string→int by the manual's p.86 type-conversion rules, and returns the value (≥0) or **-1 on failure — the template treats a negative return as ω→fail**, which is the whole convention: there is no separate failure channel, the sign IS the channel.

**MODE34-5b — the string twin, and the measurement that forced it.** `rt_pat_prim_str` is the by-name string fetch for the dynamic-charset primitives `SPAN(var)` / `ANY(var)` / `NOTANY(var)` / `BREAK(var)` / `BREAKX(var)`. It reads the variable **by NAME, never through a pre-chain operand slot or FRQ offset**, and that is not a style preference — it is immunity to a measured corruption. The legacy `op_sa`/`FRQ(off+8)` read is taken relative to a frame that the enclosing `MATCH_BEGIN` / `MATCH_ASSIGN_SAVE` / own-preamble sequence has since grown; **gdb-measured at the time: 5b's `SPAN(WS)` reads landed 240 bytes off the real DESCR pointer field**, pulled stack garbage as the "charset", and the membership loop therefore never matched — `SPAN` reported zero characters on every call. Writes the coerced string's pointer to `*out_ptr` and its length to `*out_len`; returns 0, or -1 if the variable does not coerce to a string, matching the integer twin's negative-is-ω convention.

⭐ **THE GENERAL LESSON, WHICH OUTLIVES BOTH FUNCTIONS: an operand offset is a claim about a frame, and a frame that a preamble may grow cannot honour it.** A name is stable under frame growth; an offset is not. That is why the by-name road exists at all, and it is the same reasoning the fixed-cell authority uses one layer down (`ARCH-FRAME-LAYOUT-FIXED-CELLS.md`).

✅ **LIVE, AND THE SIBLINGS ARRIVED.** The recovered text says "called from `bb_match_len` (and future siblings)". The future is here: ten templates call these two today — `bb_match_len`, `bb_match_pos`, `bb_match_rpos`, `bb_match_tab`, `bb_match_rtab` on the integer road; `bb_match_span`, `bb_match_any`, `bb_match_notany`, `bb_match_break`, `bb_match_breakx` on the string road.

⚠️ **DISAGREEMENT 1 — THE CONTRACT WIDENED AND THE NAME NO LONGER MEANS "VARIABLE".** Both functions now read through a shared static leaf, `rt_pat_prim_arg`, which **did not exist at the strip** (zero hits in `e25a5daf^`). It says: if the name begins with `*`, and a *procedure* of that name is registered, **call it with zero arguments** (`rt_call_proc_descr(name+1, 0)`) and use its result; otherwise read the variable. So `SPAN(*F)` where `F` is a DEFINEd function is a match-time CALL, not a match-time variable read, and the deferred-argument road is now a deferred-*evaluation* road. The recovered paragraphs describe only the variable case and would mislead anyone reading them as the current contract.

⚠️ **DISAGREEMENT 2 — the string twin gained a FAIL guard the integer twin does not have.** `rt_pat_prim_str` now tests `IS_FAIL_fn(v)` before coercing and returns -1 on a failed fetch; `rt_pat_prim_int` has no such test and falls through its type ladder to `return -1` instead. The outcomes agree today (both end at -1 = ω), so this is asymmetry rather than a bug — but it is asymmetry in a pair whose whole design is "the string twin of the integer one", and the reason for it is not recorded anywhere. **Also:** the integer twin now reads the string body through `rt_cstr_d(v)` rather than indexing `v.s` directly, which is the descriptor rule (RULES.md: no `strlen` on a descriptor) arriving after the strip.

---

## §11 The AB-2 strict leaves, and the LADDER AB undefined-function stub

**What "strict leaf" means here.** The AB (DEFINE/dynamic-scope) activation protocol has its frame setup and teardown owned by the **emitted** α/β. The two C leaves carry only the bookkeeping that must run near a C boundary — they are residue, not the protocol.

**`rt_ab_enter_env(frame)` — called from α AFTER the frame is established and the save-set is spilled.** Snapshots `Σ`/`Σlen` into the frame (mid-match protection: a DEFINE body that runs during a match must not leave the subject moved), snapshots `rt_g_want_name` into `AB_OFF_WN` and **zeroes the global** so the body starts with no inherited by-name intent, marks the value trail, and increments `rt_k_level` (which is `&FNCLEVEL`; `kw_fnclevel` is the readable keyword cell holding the same value, one less, because `&FNCLEVEL` counts program-defined depth while `rt_k_level` starts at 1 for the outermost). **Returns the vtmark so α can store it into `AB_OFF_VTMARK` without a second C crossing** — the return value exists purely to save a call.

**`rt_ab_leave_env(frame, result, is_fail)` — called from β BEFORE the frame is torn down (LEAVE not yet executed).** Performs the value-trail **dead-window tidy** — a GC-correctness act, clearing cells that lived only inside this activation — restores `Σ`/`Σlen`, decrements `rt_k_level`/`kw_fnclevel`, and returns `rt_nret_fix(result, wn)`, the caller's `rax:rdx` after restore. `result` is the candidate DESCR as it arrives at β in `rax:rdx`, pre-restore; `is_fail = 1` is FRETURN. **The dead window is `[fb, fb+16)` — the `___`-push slot — mirroring the slim epilogue's RSP-F-2 form.**

**LADDER AB (2026-08-09) — `rt_ab_undef_fn_stub`.** `fn_cell$<FN>` is a `.data` qword initialized to this address at program start; DEFINE's residual runtime action overwrites it with `&<FN>_act_α`. The AB-3 call path is `jmp [fn_cell]`, so calling a function whose DEFINE has not executed — or never will — lands here and fires **error 022, "Undefined function called"** (manual ch.10 p.140, error-table entry 22). It is `__attribute__((noreturn))` because `core_runtime_error` does not return; the attribute is load-bearing rather than decorative, since without it the compiler must preserve the AB frame's callee-save discipline across a call that can never come back.

⭐ **The stub's second life as a SENTINEL (D-18c, s161).** Because `bb_ab_slot_for` initializes every cell to this stub **and** `bb_ab_fn_cell_ptr` *allocates a cell on first request*, a reader asking for a name nobody ever sealed mints a fresh stub-holding cell and then "prefers" that stub over the live fallback (`p->fn`, the registered chain entry). That is error 022 at match time for every deferred call inside an EVAL-built pattern, in both modes — the B1b witness `probe/b1/b1_eval_pattern_defer_call`. The cure is the reader testing against the stub address itself: **an unsealed cell is not a target.** The test is exact, not heuristic, precisely because the stub is the allocator's own sentinel — no other value can appear there by accident.

✅ **LIVE:** `rt_ab_undef_fn_stub` and the `fn_cell$<FN>` `.data` qword are emitted by `bb_define.cpp` exactly as described; the D-18c sentinel test is still the unsealed-cell guard. `rt_ab_leave_env` is live, called from β at two sites in `bb_define.cpp`.

⛔ **DISAGREEMENT 3, AND IT IS A DEFECT: `rt_ab_enter_env` IS DEAD CODE, AND IT IS AN UNMAINTAINED TWIN OF THE LIVE PROLOGUE.** At `be0d338c3` it has **zero call sites in the whole tree** — declared in `rt.h`, defined in `rt.c`, called by nothing. Its work was inlined into the emitted α (`bb_define.cpp`, tagged `RTX-FUNC-1 inline enter_env`), which writes `AB_OFF_SIGMA`, `AB_OFF_SIGMALEN` and `AB_OFF_WN`, zeroes `rt_g_want_name`, and bumps `rt_k_level`/`kw_fnclevel` itself. The two are two copies of one contract with no check binding them, which is the failure shape this project keeps paying for; the C copy is the one nobody will update.

⛔ **DISAGREEMENT 4, THE SAME DEFECT SEEN FROM THE FRAME SIDE: THE VALUE TRAIL IS GONE, AND `AB_OFF_VTMARK` IS A HOLE NOTHING EVER WRITES.** `rt_value_trail_mark` and `rt_value_trail_tidy_dead_window` have **zero hits in `src/`** — the mechanism the recovered text calls a GC-correctness act no longer exists. `rt_ab_leave_env` no longer tidies; `rt_ab_enter_env` (dead anyway) returns a constant 0 where it used to return a vtmark. But **`AB_OFF_VTMARK (-0x38)` is still defined in `src/ir/ab_abi.h`, in the middle of a fixed absolute-offset layout, and is the only `AB_OFF_*` with no reader and no writer in the entire tree** — the emitted α writes GW, WW, ERSP, ANCHOR, SIGMA, SIGMALEN, WN and BADDR and skips it, and the frame is not zeroed at carve. ⭐ **WHY THIS MATTERS BEYOND TIDINESS, AND WHY IT IS NAMED HERE RATHER THAN QUIETLY DELETED:** under the 2026-09-17 collector ruling (RULES.md FACT RULE, CEO-812) *everything on the emitted stack is a DESCR and its type field is the only tag*, read through one static map per activation frame. An eight-byte hole carrying whatever the previous stack user left there is exactly the qword a static AB frame map must be built to EXCLUDE, and nothing in the tree currently records that it must. Deleting the define is an ABI edit and belongs to a codegen row, not to a rationale row; **what this row owes is the fact, in the place the frame-map author will look.**

⚠️ **One more drift, minor:** `rt_ab_leave_env`'s fail path now restores `rt_g_want_name = wn` before returning `FAILDESCR`. The recovered text returns `FAILDESCR` with the global left zeroed. The restore is the correct half of the enter/leave pair (the caller's pending by-name request must survive a failed activation, the same argument `bb_define.cpp`'s WANT-NAME NESTING note makes for the success path); it arrived after the strip and is not a disagreement so much as a repair the recovered text predates.

---

## §12 The B1c mode-4 GVA seam (s170, seat6)

**The seam, stated as a fact about two images.** The emit-side GVA registry is **EMPTY in the mode-4 runtime image.** The driver builds `gva_collect`'s name table and sets `g_gva_active` at COMPILE time, then clears the flag before exit (`src/driver/scrip.c`, the `g_gva_active = 0` on the m4 road). So when the runtime fragment compiler re-enters the emitter to build an EVAL body, it asks `bb_scc_probe` a question **the image cannot answer**: `g_gva_active == 0` and `gva_index_of()` returns -1 for every global. TINY admission (`bb_tiny_shim_ok`) therefore **REFUSES in m4 where it ADMITS in m3**, the fragment falls back to the slim/legacy call path, and that path's pushed-landing protocol is the B1c SEGV.

**The cure, and why the reconstruction is exact rather than approximate.** `gva_register` rebuilds the registry from `__gva_names` — the very table the m4 preamble already hands it. `gva_collect_var` assigns indices sequentially and the preamble emits `.Lgvan<k>` in `gva_name(k)` order, **so index k is reproduced EXACTLY as the compile-time emitter saw it.** This is the load-bearing step: a registry rebuilt in a different order would be worse than no registry, because the probe would then answer confidently and wrongly.

**The guard is a signature, not a heuristic.** `gva_count() == 0` *is* the m4-runtime condition. The m3 driver only reaches this call with the table already populated, so the block is unreachable in m3 — the guard does not "detect the mode", it tests the one state only the m4 image can be in.

**The default was FLIPPED ON deliberately, following `c6245f60`, which flipped the other three `SCRIP_B1C_PARITY` sites.** ⭐ **Leaving this one default-OFF would have made the switch mean "m3 armed, m4 half-armed" — the incoherent state this codebase punishes.** A killswitch that arms a protocol on one road and half of it on the other is not a killswitch; it is a third configuration nobody grades.

**Measured at the flip, and note what the numbers are FOR:** corpus 326/322 and crosscheck 308/306, `DIVERGE=1`, **IDENTICAL armed versus disarmed — zero count movement** — while the four m4 b1 witnesses went **SEGV(139) → a clean Error 22**. That is `c6245f60`'s own "crash → better" bar: the flip is justified not by a score it moves but by a crash it converts into a diagnosed error while moving no score at all.

✅ **LIVE and unchanged at `be0d338c3`:** the block, the `gva_count() == 0` guard and the default-ON `SCRIP_B1C_PARITY` read are byte-for-byte the recovered form, and all four parity sites still agree on the default (`bb_call_proc_staged.cpp` ×2, `runtime_eval.c`, `rt.c`).

⚠️ **Cite the statement, not the line.** The recovered text names `scrip.c:1573` and `scrip.c:1612`; at HEAD those are the `g_gva_active = 0` on the m4 road (1574) and the m3 `gva_register(...)` / `g_gva_active = 1` block (1611–1622). The drift is one and eleven lines in three weeks, which is why the citations above are by statement.

⚠️ **`gva_register` has grown two blocks AHEAD of the seam since the strip**, and a reader of the recovered paragraph will not expect them: an Icon global-declaration note (`rt_icn_global_note` per name) and a reassigned-builtin seeding pass (a global whose name is a reassigned builtin gets `rt_proc_value(nm)` if a proc of that name is registered, else a `DT_E` descriptor with `PROCVAL_SLEN`). Neither touches the seam; both run before it on every registration.

---

## §13 GENP slice-2 — the per-instance record, and the spine that had to stop popping

**Lon's directive, 2026-07-17, verbatim in the recovered text:** *"co-expressions and generator PROCEDURES get their OWN stack PER INSTANCE."* A generator-proc activation is therefore a `scrip_coctx_t` on its own pthread stack (GC-heap carved and registered per GC-U-6/7, `rt_coexpr.c`). **The jmp-entry body runs UNCHANGED on the instance stack** — it self-allocates, and recursion and depth are all natural because it is a real stack. `bb_suspend`'s `flat_gen` arm transmits a value via `rt_genp_yield` (`scrip_coret`) and resumes by falling through when the caller re-activates; a real return or fail unwinds through `rt_proc_enter`'s existing γ/ω landings into `rt_genp_entry_c`, which transmits the final value or the failure and **parks** — `scrip_coexpr_destroy` joins the parked thread.

**`done` is the three-state transmit code:** `0` = live (yielded, resumable), `1` = returned (final value, **not** resumable), `2` = failed.

**The record, `rt_genp_s`, field by field.** `next` at **offset 0** — the live-instance list, and the **resume-window discriminator** (`rt_genp_lookup`): list membership IS the flavor test, and it is exact because no handle and no legacy frame pointer can collide with the address of a listed instance. `regs[5]` at **offsets 8..47** — the caller's `rbx r12 r13 r14 r15`, restored by `rt_genp_thread_entry`; these are captured FIRST at the call site, while `-O0` still guarantees they are untouched, because they hold the emitted caller's GVA and ζ bases and any live scan triad, and they must ride into the instance thread. Then `co` (the context), `args[CALL_ARGS_MAX]` with `nargs` (the staged actuals are **copied**, and the copy is race-free because the caller blocks on the activate handshake), `fn`, `name`, `done`, and `first_done` — **the ONE-POP law on the instance thread: `rt_proc_call_open` pushed exactly ONE pcall record, so only the first γ/ω delivery runs an epilogue leaf and every resumed delivery passes straight through.** The layout is held by a `_Static_assert` on `next`==0 and `regs`==8, because `rt_genp_thread_entry` is hand-written asm that bakes those two offsets.

**The s94 repair, which is the section's real content.** The s92 GENP-SPINE made generator bodies suspend through `xa_flat`'s **RETAINING** γ epilogue: result preloaded into `rdi:rsi`, a 16-byte resume record `{res-landing, callee ___}` left AT the deep frontier, γ wire jumped, **no unwind**. But `rt_proc_enter`'s γ landing **pops five saved registers** — correct for a det body, which is fully unwound before the jmp, and catastrophic at a retained frontier, where it EATS the 16-byte resume record plus 24 bytes of live frame into `rbx`/`r12..r15` and the next wire jumps frame junk. That is the `rip = r12` crash class. ⭐ **The shape of the bug is worth more than the fix: one landing served two frontier states, and the distinguishing fact — retained versus unwound — was not in its hands.**

**So the instance-thread shim mirrors `bcps_spine_gen_arm`'s call-site contract instead:** enter the blob by `jmp rax` with `rcx`/`rdx` carrying the γ/ω wires, and **the landings POP NOTHING** — the callee-saved registers ride through `scrip_coret`'s own ABI preservation, which is the instance stack's job, not the landing's. First-versus-resumed is the instance once-flag. Every γ delivery parks in `scrip_coret` and, on re-activation, resumes the recorded suspend by `jmp qword [rsp]` — **the record sits AT the frontier by LIFO balance on this instance's OWN stack**, which is the property having a per-instance stack buys. ω, and the pushed SENTINEL record that catches post-`return` slot-poison resumption arriving fully unwound (`[rsp]` is then the sentinel's landing), park the instance failed. **Three sentinel qwords keep `rt_proc_enter`'s 16-byte entry-alignment convention: `call` 8 + 24 = 32.**

✅ **LIVE:** `rt_genp_s` still carries `next`/`regs[5]`/`first_done` at the asserted offsets with the same `_Static_assert`; `rt_genp_thread_entry`, `rt_genp_spine_enter`, `rt_genp_deliver_γ`/`rt_genp_deliver_ω`, `rt_genp_entry_c`, `rt_genp_yield`, `rt_genp_lookup`, `rt_genp_destroy` and `rt_genp_triage` are all present and the three-state `done` is unchanged.

⚠️ **A SECOND SPINE EXISTS NOW, AND THE RECOVERED TEXT PREDATES IT ENTIRELY.** `rt_genp_spine_enter_n2` / `rt_genp_deliver_n2_γ` (row `icon-generator-through-a-procedure-value-is-ungraded-at-the-intersection`, hq_B) is the N-2 ABI twin, and `rt_genp_s` gained a `region_ft` field to serve it. Its entry frame is **five words** — `[rsp+0]` γ, `[rsp+8]` ω, `[rsp+16]` REGION, `[rsp+24]` L7, `[rsp+32]` ABI word, ANCHOR at `[rsp+40]` — and it is the hand-written twin of `bcps_spine_gen_arm`'s push block, with its full parity argument carried as an emitted-asm note at the site (CEO-483, hq_U 2026-09-10: the sixth word had no reader, proven by poisoning it and getting a byte-identical parse while the same poison at `[rsp+0]` SIGSEGVd). ⛔ **Read §13's spine as the ORIGINAL spine; the N-2 arm's contract lives at its own site and the two are deliberately two hand-written copies of one ABI** — the site's own note records that the last time one grew without the other it took weeks to find.

⚠️ **The instance record is `calloc`'d.** Under the 2026-09-18 full-eradication order (Lon, CEO-842) that site is in scope like every other; it is in `src/runtime`, the cfo's lane, and its destination is **(1) collected heap with a root, not the compile-time arena** — the record is reachable from the running program by construction, since `g_genp_head` is a live-instance list the emitted program walks.

---

## §14 Closing note — what `src/runtime/rt/rt.c` still owes

**Nothing.** `e25a5daf` stripped 156 comment blocks from this file; §1–§8 took the procedure call protocol (row `rationale-rt-c`, 2026-09-16) and §10–§13 take the remainder (row `rationale-rt-c-remainder`, 2026-09-18). §9 routes the four clusters that are **gone rather than relocated** — the storage-axis switch, PL-FR-4, ICN-FR-5 — each verified at `be0d338c3` to have **zero hits in `src/`** before being written down as history. ⭐ **The recovery turned up four disagreements between the recovered text and the live tree and two of them are live defects** (`rt_ab_enter_env` dead and untwinned, `AB_OFF_VTMARK` a never-written hole in a fixed frame layout); both are named in `RATIONALE-INDEX.md` so that a `grep` on the symbol finds them, and the second is addressed to whoever builds the AB activation frame's static GC map.

## §12 THE PRICE OF THE c2bb OPEN/LAND CONTRACT, MEASURED (ceo, 2026-09-24 17:1x CDT, CEO-1243)

Lon, in-chat: *"what in the call mechanism changed for SNOBOL4?"* Callgrind on indirect_dispatch's fixed-iteration twin (2,000 repetitions × 1,000 APPLY calls, mode 4), the 09-03 tree `380cc4162` against `444e9893d`, with every trace hook and the trace-set lookup already off the path: **1.39 G Ir against 2.21 G — about 695 against 1,100 instructions per call.** The name save/restore (`rt_name_save_push` 117 Ir/call, `rt_name_restore` 37) and the prologue/epilogue split predate 09-03 (SR-1a June, NCB-1 July, s112 August); what is new is the 09-19/21 collector contract that a runtime-made call never re-enters emitted code from C: `rt_call_open_by_name` (47/call, `80e3a942e`), `rt_apply_open` (45, `ed9f7a76e`), `rt_proc_call_prologue` (75), `rt_proc_save_count` (44), `rt_proc_epilogue_named` (46), plus the by-name lookup `rt_proc_find` + `strcmp` (~100). The old tree spent its per-call budget differently (`try_call_builtin_by_name_bl` 157/call, `rt_call_proc_descr`, `rt_proc_fnv`). None of it is instrumentation; it is the contract as first landed. THE ROW (hq_snobol4, rank 0): a direct open/land fast path when the callee is a known DEFINE'd function with a resolved proc record — no name lookup, the locals' save/restore as a fixed-offset block — measured on this twin against the 695 baseline.
