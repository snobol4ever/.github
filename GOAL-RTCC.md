# GOAL-RTCC.md — GLOBAL REGISTER LIBERATION: the CRAZY save/restore at EVERY C-RT boundary

## ⛔⭐⭐⭐ CROSS-GOAL BULLETIN — 2026-08-10 s12: LADDER WREG IS LIVE, r10/r11 ARE CLAIMED WIRE REGISTERS (owner-directed cross-post; authority = `GOAL-RBP-EARN.md` LADDER WREG, `.github` `6de2dbff`)

**Lon runs 5 seats concurrently: RBP-EARN + RTCC + ZETA-MECH + ZETA-CLIMB + SNOBOL4-RTX.** Charter, verbatim: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11."* Pattern blobs stop being procedures: the `proc_PAT$N_α` shim (304B carve · γ/ω wire stores · SCANBASE fills · `g_zctx` push · resume-slot store) and its CLASS-D exit ceremony are DELETED; call sites enter the blob's FIRST INTERIOR BOX directly with `lea r10,[rip+site_γ] · lea r11,[rip+site_ω] · jmp <first_box>`; exits are `jmp r10` (success) / `jmp r11` (fail after drain). **rΓ=r10 · rΩ=r11 are RESERVED product-wide, all three glue kinds, ONE authority (a per-kind split is the mixed regime ZW16 convicted).** LAW: blob-interior pending cells capture/restore the pair at push/β, else it is `g_blob_ctx`'s single-cell defect in register clothing.

**WHY THIS IS YOURS:** WREG-0 IS an RTCC ledger entry and `test_gate_rtcc_claimed_regs.sh` is its arbiter. ⛔ **DIRECT COLLISION:** your charter claims all 9 caller-saved GPRs as VM globals — r10/r11 are inside that pool and are now SPOKEN FOR. Do not allocate them to another role; if you already have, say so in-chat and the fallback is r8+r11 (paying SCANBASE entanglement). ⭐ **AND WREG DEPENDS ON YOU:** the wires only survive `rt_*@PLT` crossings because a veneer brackets every C-RT boundary — RC-1..RC-5 veneer work is LOAD-BEARING for WREG, not adjacent to it. This ladder is the payoff of the RTCC investment. Belt-and-suspenders for r11's lazy-binding-resolver clobber: `-Wl,-z,now`.

**s12b MEASURED FLOORS (same container, BY SET — every seat's regressions are judged against these):** probe **33/5** m3 · **33/4** m4 · crosscheck/patterns **82/40** m3 · **78/44** m4 · broad-336 **270/66** m3 · **262/68/6** m4. Honest census Σest ≈ **263** (NOT the stale 557; crosscheck committed `.s` were pre-deletion — re-measure fresh, do not trust those artifacts). Runner semantics: `$(cat ref)` vs `$(cmd)` (ENV-TRAP (a)); namespace `/tmp` evidence per seat.

## ⚙️ CONCURRENT BY DEFAULT — AND THE REPOS MOVE UNDER YOU

**Many seats run this file's siblings at the same time. Edit any file, commit and push whenever a rung is buildable and green — mid-session, per rung. Never park work or decline an edit on concurrency grounds; stranding has cost this project far more than merging ever has.** Git merges; `git pull --rebase` and resolve normally.

**⛔ ASSUME ORIGIN MOVED SINCE YOU LAST LOOKED.** Another seat may have landed in your exact files while you were reading them.
- `git pull --rebase` before every push; **re-prove THIS file's gate/watermark after any rebase** — shared state moves under you and a watermark measured pre-rebase is void.
- `git log origin/main..HEAD` at orientation AND before handoff. **A clean `git status` is NOT a clean tree** — it hides local commits a peer seat left in a shared working copy.
- Place trees at canonical absolute paths (`/home/claude/{SCRIP,corpus,.github,x64}`) BEFORE running any gate: **many scripts grade a tree by absolute path.**
- Prefer **one clone per seat**; two seats in one working copy silently overwrite each other's uncommitted edits, and a global gitconfig scrambles attribution.
- Push **code repos before `.github`**, so no FINDING ever describes an unpushed tree.
- Push needs a credential — **ask Lon in chat and wait.** Never write push status into a doc.
- `bash scripts/handoff_status.sh` verbatim is the ONLY push truth. Not this file, not a commit message.

**Semantic collisions (two seats claiming one register) are caught MECHANICALLY by the claim gates, not by scheduling.** That is why no window is needed.

## 📋 PLAN SCRUTINY (s11 close, Opus 5) — corrections to the ladder + where Lon's ruling is needed
**1. CLOSE RC-5 AS "LANDED, STATICALLY GRADED" — the rail can never grade it and now doesn't need to.** Mechanism proven end-to-end this session: killswitch genuinely switches arms (116 GVA sites vs 0, md5 differ, both green), static delta 464 B on fibonacci matching s10's census to the byte. Per the s10 law, static mechanisms get static grades. ~~**RE-OPEN the ANCHOR revert** (`f1fddf55` was decided on two identical binaries).~~ ⛔ **CORRECTED s13 — THE "SWITCHES ARMS" RESULT IS GVA-ONLY AND DOES NOT TRANSFER.** `rtcc_anchor_cmp` has **ZERO CALLERS**; the ANCHOR revert was executed at the TEMPLATE CALL SITES, not the killswitch (which was born `1` and never held another value). Measured `roman.sno` m4 at maximum-ON: `test r8,r8` = **0** while the GVA positive control in the same emission = **106**. **Re-opening ANCHOR by flipping its killswitch produces two identical binaries a THIRD time.** The re-open requires restoring `bb_match_begin`/`bb_match_advance` call sites — Lon's call, see the s13 cursor.
**2. RC-6 IS READY EXCEPT FOR ONE RULING — ⛔ LON: STATE THE WORKLOAD MIX.** Instrument landed, target falsified. `rt_call_arr` is 4 on fibonacci and 100,005 on roman; there is no rank without a mix. Options: (a) the 23-benchmark suite equally weighted (defensible, cheap); (b) per-family — port the family the mix says is hot for the programs that family serves (matches RC-6's per-family veneer-bypass shape; RECOMMENDED); (c) a Lon-designated "representative" set. Also unresolved: whether mode-4 ranking transfers to mode-3 (chartered 1:1, untested; mode-3 is where the veneer runs).
**3. RAISE `RATIO_FLOOR` TO A BOX-MEASURED VALUE.** 1.10 sits BELOW this box's measured floor (~1.12). A constant floor is wrong in kind — it should be re-measured per box (unchanged-binary N=12 interleaved) and stored, or rungs graded statically. ⛔ **LON: this box cannot meet RC-0(a) (needs ≤1.05×, measures 1.075–1.116). Either provide a quiet multi-core box for RC-7's fold decision, or amend RC-0(a) to bless static-only grading here.** Every "measure on the rail" instruction in RC-5/RC-6/RC-7 is currently unexecutable as written on this hardware.
**4. STILL OPEN FROM RC-0(d), PRE-EXISTING:** the coexpr/TLS ruling (one global block vs two VM states under m3 pthreads). Not blocking until a coexpr-heavy workload joins the mix, but RC-7's DoD line 2 cannot close without it.
**5. PROCESS: the multi-seat container needs a heartbeat convention.** Two seats ran blind to each other on one filesystem (s11/s12). Suggest: each session `touch /home/claude/.seat-$$` at open and check `find /home/claude/.seat-* -mmin -30` before recovering any tree. Cheap, mechanical, prevents the s12 misread.
**6. s13b UPDATES TO ITEMS 1–5 (2026-08-10, at close):** **Item 1** — RC-5 CLOSED-as-static stands; but the peer s13 proved `RTCC_GLOBAL_R8_ANCHOR` is **DECORATIVE** (`rtcc_anchor_cmp` has zero callers — the revert lives at the template call sites), so *"re-open the ANCHOR revert"* must re-instate the CALL SITES, not flip the killswitch, or it voids a third number. **Item 2** — the mix ruling was taken **per-family** under Lon's in-chat *"all your choices"* (VETOABLE), and the m4→m3 transfer half is CLOSED structurally (veneered site set is medium-invariant; FINDING 2026-08-10e §9.2); the RC-6 rewrite that remained of item 2 is now in the rung text below. **Item 3** — MOOTED for RC-6: it grades statically (crossings × 14) and needs no rail; the quiet-box ruling still matters for RC-7's fold only. **Item 4** — unchanged, still parked until a coexpr-heavy workload joins the mix. **Item 5** — ADOPTED, and it FIRED THREE TIMES on 2026-08-10 alone (s11/s12 worktree misread · s13/s13b same-goal-file collision · the phantom `.so` s13b destroyed and rebuilt); `touch /home/claude/.seat-<goal>-$$` at open is now recorded practice in the s13b cursor.

## 🔭 s16e PLAN SCRUTINY AT HANDOFF (Opus 5) — recommendations to the PLAN itself, all ⛔ VETOABLE

**1. STANDING ITEMS MUST CARRY THEIR PROBE, NOT JUST THEIR OPERATION.** "Guard all five in ONE commit" survived s12→s16 as an *operation* and was wrong-in-kind for 3 of 5. Phrased as its *invariant* — "no RTCC constant may silently ignore a `-D`, and none may half-apply one" — any session would have derived seal-not-guard in minutes. RECOMMEND: cursor standing items state the invariant + a one-command probe; operations are proposals, not instructions.

**2. ARTIFACT FRESHNESS IS UNGATED — §4 regen is session discipline, and discipline failed twice in one day** (`c4cb8813`, `565ecfa8` both skipped it; the debt surfaced under s16's name). RECOMMEND a freshness canary in `handoff_status.sh`: emit fibonacci to /tmp, md5 vs the corpus `.s`; on mismatch, name the debtor commits (`git log <last-regen>..HEAD -- src/templates/`). One command; retires the entire "regen bills the runner" class.

**3. CHARTER ≠ HEAD.** Charter claims 9 GPRs + XMM8–15 as VM globals; the coverage gate proves ROUND-TRIP=3, XMM staged=0, and 5 dead stores per crossing. RECOMMEND a three-state vocabulary — **CLAIMED (charter) / STAGED (block+veneer) / PROVEN (round-trip + GC-visible)** — and redefine RC-7 DoD line 1 as *"gate output == charter"* so the fold becomes mechanical instead of narrative.

**4. PRIZE RE-ORDER.** RC-6's `crossings×14` sits behind per-family RTX eradication (eligible set: 1 of 54 symbols). s16b's `crossings×5` needs **no eradication** — fibonacci ~47.1M, roman ~33.0M on the same noise-free static instrument. Value-per-effort now reads **RC-8a → RC-8b > RC-6**. RECOMMEND Lon bless or veto this ordering explicitly; the cursor should not imply RC-6 is still the headline.

**5. STILL OPEN FOR LON (accumulated, none blocking s16's work):** RC-8 minting veto window · RC-5 ANCHOR call-site re-open · XMM: stage or amend charter (RC-8c) · coexpr/TLS ruling · heartbeat protocol: fix the one-directional flaw (s15) **or retire it** and rely on canonical-path occupancy + origin timestamps, which is what actually worked in s16.

**6. CREDENTIAL LOGISTICS.** Seven commits rode one container across four asks; a container death pre-push loses everything but the transcript. RECOMMEND: credential at session OPEN (or least-privilege per-repo deploy keys), and **ROTATE the token used for this handoff after it completes — it appears in the chat transcript.**

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-11 s16 (Opus 5) — ✅ **THE "GUARD ALL FIVE" ITEM IS CLOSED BY CORRECTION: 3 OF THE 5 ARE ABI, NOT KNOBS, AND GUARDING THEM MANUFACTURES THE H2 SIGSEGV CLASS. SEALED INSTEAD, WITH BOTH POSITIVE CONTROLS FIRING.**
**s16 (Opus 5) — RC-0 class.** FINDING: `FINDING-2026-08-11-CLAUDE-OP5-RTCC-S16-THE-FIVE-CONSTANTS-ARE-TWO-CLASSES-AND-GUARDING-THREE-OF-THEM-MANUFACTURES-THE-H2-SIGSEGV-CLASS.md`. SCRIP `565ecfa8` → **`d6893783`**. corpus → **`a38e551f`**. **ZERO emitted bytes (measured).**
✅ **STANDING ITEM CLOSED, BY CORRECTING IT.** Probed: `RTCC_GLOBAL_R9_GVA` (guarded) takes a `-D`; `RTCC_SLOT_R8`/`RTCC_SLOT_R9`/`RTCC_GVA_REG` (unguarded) are **silently ignored**, warning eaten by tree-wide `-w` — so the tree WAS in the one-sided state s12 warned of. But guarding them is **strictly harmful**: `src/templates/` contains **zero** uses of `RTCC_SLOT_*` and **12 raw slot literals**, so a guarded `-DRTCC_SLOT_R9=7` moves the C seed to `block[7]` while the encoders keep addressing `block+48` ⇒ `RT_GVA_VA` never reaches r9 ⇒ **H2 SIGSEGV, manufactured by the fix.** Demonstrated with a guarded copy, not argued. **NEW LAW: KNOB ⇒ GUARD (read by both halves, `-D` reaches emitted bytes). ABI ⇒ SEAL (one half is a hardcoded literal).** Landed: `static_assert` in `x86_asm.h` binding all 9 slots + `RTCC_GVA_REG`→`"r9"` + tier width; drift is now a BUILD ERROR. **Positive controls both fire (exit=2):** SLOT_R9 6→7 and GVA_REG r9→r8.
⛔ **THE `.s` ARTIFACTS WERE STALE BY TWO COMMITS AND THE REGEN BILLED 13,654 LINES TO THIS RUNG.** s16 is byte-neutral, so the diff cannot be its own. Attribution mechanical: `roman.s` had **0** `g_rtcc_block` at `82c34a01`, has **309** now, added lines dominated by veneer wb/reload pairs ⇒ **TRUE OWNER is `c4cb8813` RTCC DEFAULT-ON, pushed without the RULES §4 regen** (`565ecfa8` ZCTX same window). Corpus commit message amended to say so. **NEW LAW: A REGEN BILLS ITS DIFF TO WHOEVER RUNS IT, NOT WHOEVER CAUSED IT** — check any regen diff against your own rung's measured emission delta before accepting it.
✅ **s16d — MODE-4 END-TO-END CLOSED (verification gap I had left open).** s16 proved mode-3 RUNS and mode-4 EMITS byte-identically, but never proved mode-4 ASSEMBLES AND EXECUTES — "byte-identical emission" is not "the emission is valid". Probed: `--compile` → 2262 lines → `gcc -no-pie -lscrip_rt` → **`result: 832040`**, matching mode-3 exactly. **The 1:1 mode property holds with the ABI seal in place.** LAW: an emission-md5 equality proves the compiler unchanged, NOT that its output is runnable — run the artifact, in both modes, or the 1:1 charter is untested.
✅ **WATERMARK RE-PROVED OPEN AND CLOSE:** claim gate `--strict` **PASS** (COLLISION CLASS EMPTY, HAZARD SURFACE **19** — matches s12 to the digit) · fibonacci m3 `result: 832040` at RTCC=0, RTCC=1, and absent (s13 default-ON flip confirmed live) · emission md5 `3c63fed5…` identical across stash/unstash. **No perf number quoted** — observed 1.11× sits inside this box's ~1.12× band (s12 law).
✅ **s16b — ARG-TIER/XMM RULING TAKEN (⛔ VETOABLE, Lon in-chat "all your choices"): MINT RC-8, DO NOT RE-OPEN RC-4.** RC-4 closed correctly for what it claimed (writeback + scratch reload, arg-tier reload deferred in a comment); re-opening a correctly-closed rung to carry another scope is how a ledger stops meaning anything, and s14's instinct not to re-open a peer's rung was right. **RC-8a GC coverage (BLOCKING) → RC-8b arg tier: claim it or stop paying for it → RC-8c XMM: stage them or AMEND THE CHARTER to 9 GPRs** (at HEAD there is no rung behind the XMM claim at all).
⛔ **NEW DEFECT — THE RTCC BLOCK IS NEVER SCANNED BY THE GC.** `rtcc_gc_register` calls `rt_gc_root_pin_add` ONLY. `gc_heap.c:625` feeds pinned roots to `rt_gc_pin_ptr` (pins the POINTER VALUE); only `rt_gc_root_range_add` reaches `gc_cons_scan` (`:626`). Every other block in the tree registers BOTH (`zeta_heap.c:161`, `rt_coexpr.c:74`). ⇒ the block's 32 slots are scanned by NOTHING and rtcc_init.c's *"registering it is sufficient"* is FALSE — premise true, mechanism absent. **LATENT at HEAD** (r8=int, r9=mmap'd island, r10/r11=code addrs); **LIVE the instant the arg tier is claimed** (RAX:RDX carry DESCR_t returns ⇒ use-after-free). ⛔ **RC-7 MUST NOT FOLD** on a veneer protecting 3 of 17 whose canonical block is outside the GC's reach; **RC-8a blocks RC-8b.**
⛔ **CORRECTS s14: `rtcc_load_all` IS NOT ORPHANED — it has exactly 2 call sites (`src/driver/scrip.c:1490` m4, `:1651` m3) and BOTH are the PROCESS-ENTRY crossing**, so the arg tier reloads ONCE per process before any generated code runs. ⇒ `rax rcx rdx rsi rdi` are stored every crossing and reloaded by nothing: **5 of the veneer's 14 instructions are pure cost at HEAD.** 📌 **THIS REFRAMES RC-6:** `crossings × 5` is available with NO RTX eradication, on the same noise-free static instrument — **fibonacci 9,423,879 × 5 = 47,119,395 · roman defer trio 6,600,066 × 5 = 33,000,330**, same order as RC-6's headline prize, unblocked. ⛔ NOT COSTED AND NOT IMPLEMENTED: `wb_bin` uses the RAX store + `movabs rax,block` as its RSP-safe base-register story, so dropping the stores needs a probed (not assumed) base plan — RC-8b.
✅ **LANDED `scripts/test_gate_rtcc_block_coverage.sh` (SCRIP `1d4cbef7`) — the DoD census is now ONE COMMAND.** Independently reproduces veneer **14** (9 wb + 5 rl, s13b) and ROUND-TRIP **r8 r10 r11 = 3** (s14), plus RELOAD-ONLY `r9` (correct by H2) and WRITEBACK-ONLY `rax rcx rdx rsi rdi`. **SELF-ARMING, not permanently red:** WARNs while the GC gap is latent, turns FAIL automatically the moment an arg-tier register joins ROUND-TRIP. Controls fire (simulated claim ⇒ `USE-AFTER-FREE`; BOTH-MEDIUM fired independently on the TEXT-only half-claim). **Two instrument defects caught before trusting a digit:** v1 parsed the BINARY encoder's COMMENTS (grading documentation, s13b class) and miscounted the `RTCC_GLOBAL_R9_GVA`-guarded store (s11 class, false BOTH-MEDIUM alarm). First gate in the tree to locate its tree with `git rev-parse --show-toplevel` (s15h: 0 of 71).
⛔ **s16c — RC-8b's "RAX IS DEAD" PREMISE IS NOT RETIRED; I TRIED AND REPORT THE NULL.** Vararg census: **zero variadic `rt_*`** (control PASSED — same regex finds 20 variadic decls in `src/`: `sno_error`, `rebus_error`, `mremap`), so no `rt_*` callee reads AL — **but that is narrow, not a clearance.** ⛔ **My veneered-symbol census was a BAD INSTRUMENT (control FAILED):** I grepped `x86_rtcc_call("sym")` and read 0 — the veneer is emitted as **`x86("rtcc_wb")` / `x86("rtcc_rl")`, 12 template sites each**, with the callee symbol NOT a veneer argument. s13b `.globl` / s11 `LD_AUDIT` class. ⇒ **correct instrument for RC-8b, named for the next seat: walk the 12 `x86("rtcc_wb")` sites and identify the call each brackets.** Do not re-derive the false zero.
📌 **OPEN:** RC-5 ANCHOR call-site re-open (Lon) · RC-6 (blocked on RTX *eradication*) · RC-6b deferred · **RC-8a GC coverage — BLOCKING, and it blocks RC-8b** · RC-8b arg tier (claim or stop paying) · RC-8c XMM or charter amendment · coexpr/TLS ruling. ⛔ **RC-7 NOT FOLDABLE** until RC-8a lands.
📌 **s16 process:** no peer seat (canonical paths unoccupied; commits past s15 were ~40 min old and on origin). Trees placed at canonical paths BEFORE any gate ran, per s15h. **A killed background build leaves 0-byte objects that read as a broken HEAD** — `undefined reference to bb_assign_global[abi:cxx11]()` on a clean clone was a truncated `.o`, cured by `find out -name '*.o' -size 0 -delete`.

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s15 (Opus 5) — ✅ **s14 r10/r11 COLLISION ARBITRATED (NECESSARY-NOT-SUFFICIENT; gate guards it). ⭐ HALF THE SCRIPTS GRADE A TREE BY ABSOLUTE PATH — 191/383, 23/71 GATES, 0/71 via git rev-parse.**
**s15 (Opus 5) — RC-0 / process, two FINDINGs.** FINDINGS: `FINDING-2026-08-10g-...-RTCC-S15-THE-SEAT-HEARTBEAT-IS-ONE-DIRECTIONAL-AND-FAILED-THAT-WAY-TEN-MINUTES-AFTER-ADOPTION.md` · `FINDING-2026-08-10h-...-RTCC-S15-HALF-THE-SCRIPTS-GRADE-A-TREE-BY-ABSOLUTE-PATH-AND-MY-OWN-DARK-VERDICT-WAS-FALSIFIED-BY-IT.md`. SCRIP **unchanged**. `.github` two commits ahead of origin. **ZERO emitter bytes, zero source edits, no regen owed.**
✅ **s14 WREG ARBITRATION READ AND STANDING.** s15 opened intending to arbitrate r10/r11; discovered s14 (live peer seat) had just committed the answer. Summary: (a) NO ALLOCATION COLLISION — `LIVE_CLAIMS` is r9 only; r8+r11 fallback is NOT needed on RTCC's account. (b) MECHANISM COLLISION IS LATENT AND GATED: the veneer round-trips r10/r11 across LEAF crossings (unconditional wb+reload); it does NOT preserve them across re-entrant crossings, which are the norm — writeback overwrites the outer activation's value. Safe config = `RTCC=ON` AND WREG's own pending-cell capture/restore at push/β. Neither alone. s14 landed `test_gate_rtcc_claimed_regs.sh` RESERVED tier (SCRIP `50005578`) plus DoD coverage census (`082c876a`): **3 of 17 claimed registers round-trip at HEAD; arg tier (5 GPRs) reload orphaned since RC-4 closed; XMM8–15 never staged, no rung behind them.** ⛔ RC-7 must not fold on this — its DoD walk would certify line 1 against a veneer protecting 3 of 17. Re-open RC-4 as `[~]` with reload carry, or mint an explicit rung for arg tier + XMM. **LON'S CALL — s14 did not re-open another seat's closed rung.**
⭐ **HEARTBEAT DEFECT (s15g):** the s13b convention is **one-directional** — a check run at open measures ARRIVAL ORDER, not occupancy. The later seat always detects; the earlier seat (which holds the tree, is mid-flight) never does. s14 recorded *"no peer seat"* at 23:44; s15 arrived at 23:54 and detected the peer. **Second failure mode:** `-mmin -30` over a file written once ages a live long-running session out of its own window silently. FIX (not yet applied): re-check at mutation + push (not just at open); re-`touch` per rung. NEW LAW added below.
⭐ **PATH-PINNING FINDING (s15h) — 50% OF THE SCRIPT SUITE GRADES A FIXED ABSOLUTE PATH:** 191/383 scripts and 23/71 gates hardcode `/home/claude/…`; **0/71 gates** use `git rev-parse --show-toplevel`. A gate run in a private clone audits whatever tree sits at the canonical path — under multi-seat reality that is the live peer's worktree bearing an unpushed commit. Generalises s13b's phantom-`.so` hazard from build artifacts to the entire measurement surface; directly undermines this goal's HEAD-stamp requirement. Also: 13 scripts reference `/home/claude/work` which did not exist this session. Three live path-moving goals (SRC-REORG · RUNTIME-RENAME · SCRIP-RENAME) make every pinned reference a silent tripwire. FIX proposed (not applied: 191 sites, three goals' territory, needs sequencing). `test_gate_omega_own_k.sh` is the EXEMPLAR: it carries a `TOTAL_FILES -eq 0 → exit 1` positive control and cannot silently grade nothing; copy that pattern to every absence-asserting gate. ⛔ **NOT A LI-FENCE ALARM:** `test_gate_no_lang_names.sh` is red (1584) but is NOT the sanctioned gate; `test_gate_emit_no_lang.sh` (RULES.md §FACT-RULES) is **GREEN at origin HEAD**. No LI-FENCE violation claimed.
⛔ **SEAT HEARTBEAT FOURTH OCCURRENCE, SELF-RETRACTION DOCUMENTED.** s15 ran an empty-tree experiment and initially called `omega_own_k` DARK (passes over nothing). Retracted in-finding: the gate's positive control worked; my empty-tree scaffold was never read because CORPUS is pinned. The broken instrument was mine. Recorded to prevent the next seat re-deriving the alarm.
📌 **OPEN:** RC-5 ANCHOR call-site re-open (Lon's call per s13) · RC-6 (blocked on RTX eradication per s13b) · RC-6b (⛔ Lon rules: defer trio, roman, 92M instructions) · guard five duplicated RTCC constants in one commit · arg-tier reload + XMM8-15 rung/bookkeeping (Lon's call per s14). **No build, no timing, no watermark re-proof this session** — peer's tree was read never written; honest statement is s15 has no measurement.

---

## ⛔ DEFINITION OF DONE
1. **ALL nine caller-saved GPRs (RAX RCX RDX RSI RDI R8 R9 R10 R11) + XMM8–XMM15 are PROTECTED**: usable as SCRIP VM globals whose values survive every generated↔C crossing in both directions, both media, all languages. 2. RTCC block is CANONICAL at every boundary, GC-rooted, and coexpr/thread-safe (ruling recorded). 3. Killswitch-OFF (`SCRIP_RTCC=0`) byte-identical until the RC-7 fold. 4. Veneer coverage = 100% of registry C-symbols and 100% of inbound C→generated edges (DENOMINATOR LAW — census, not sampling). 5. xc318 BY SET ≥ the RC-0 recorded floor, both modes; 141-probe ≥ floor; bench board ≥ floor on the FIXED instrument. 6. No template speaks any save/restore — encoder-only (greps == 0). 7. regen ×4 clean.

## ⭐⭐ DESIGN OF RECORD (minted s0 — falsify by probe, not by argument)
**THE REGISTER MAP.** Callee-saved set already SCRIP-owned and untouched by this goal: RBX=heap frontier · RBP=law-4 frames · R12=CAS/DCAP top · R13=Σ · R14=δ · R15=Δ · RSP=ζ spine. THE LIBERATION TARGET is the caller-saved set, split by role: **SCRATCH TIER** {R10, R11, R8, R9, XMM8–15} — never (R10/R11) or rarely (R8/R9 = SysV args 5/6) used for C-call argument staging; claimable with ZERO template changes. **ARG TIER** {RAX, RCX, RDX, RSI, RDI} — the SysV argument/return registers; claiming them forces the arg-staging re-plumb (RC-4). DESCR_t is 16B ⇒ C returns ride RAX:RDX — the RC-4 veneer must capture the return to its destination slot BEFORE the global reload (encoder overload `x86("call_rt", sym, result_slot)` owns the post-call store; the templates' current post-call `mov FRQ,rax/rdx` idiom migrates INTO the encoder). Varargs symbols (printf-class): AL = #xmm-args must be staged post-save — registry marks them.
**BLOCK-CANONICAL, NOT STACK-SAVE (the load-bearing decision).** The boundary is BIDIRECTIONAL and re-entrant: generated→C→generated is the NORM (`rt_call_arr`→proc bodies · `rt_dcap_pump`→PAT$ blobs · `rt_chain_enter`/EVAL · `runtime_eval.c:303/308/312/379/412`). A stack save buries the VM globals in an ancestor veneer frame where inbound edges cannot reach them — DISQUALIFIED. The SPITBOL model holds instead, read as CACHE SEMANTICS: the RTCC BLOCK (one cache-aligned bss region) is the CANONICAL home of the VM globals; registers are a cache VALID ONLY INSIDE GENERATED CODE. Outbound crossing = WRITEBACK (regs→block); inbound crossing = LOAD (block→regs). Nested crossings compose by induction — the block holds the latest values at every boundary, and an inner generated region advancing the VM state is CORRECTLY seen by the outer region on its reload. C code never reads the claimed registers as meaningful; gcc may clobber them freely. **Consequences bought for free:** (a) GC ROOT SET — at any point GC can run (inside C), every heap pointer cached in a claimed register is sitting in the block; register the block with the collector, and the "GC can't see register roots" problem never exists. (b) The block is ~72–136B hot in L1.
**TWO OPEN DESIGN ITEMS (RC-0(d) closes them):** (i) COEXPR/THREADS — m3 co-expressions are pthreads (`rt_coexpr.c`); one global block races two VM states. Candidate answers: TLS block (fs-relative addressing — new encoder arm) OR block swap folded into the existing coexpr context switch. Lon rules at RC-0 close. (ii) SIGNAL WINDOWS — the two-instruction gap between call and writeback completion; likely moot (SCRIP handles no async signals that re-enter generated code) but say so from census, not assumption.
**COST HONESTY.** The boundary is HOT (string_manip ≈10M rt calls/run; SPITBOL pays its toll RARELY, SCRIP pays per-call). Writeback+reload ≈ 2×(set size) movs to one hot line. The bet is register freedom in ALL generated code + RC-6 bypasses outweigh it — a BET UNTIL THE RAIL SAYS SO, and the rail cannot say anything until RC-0(a). ⛔ NO SPEED CLAIM in any RC FINDING without the min-of-N instrument.

## RUNGS (strict order; each: read-first set, gate at current watermark, killswitch md5, FINDING, cursor move)
- [x] DONE — RC-0
- [x] DONE — RC-1 (b1aca5dc)
- [x] DONE — RC-2 (d6e136d9)
- [x] DONE — RC-3
- [x] DONE — RC-4
- [ ] **RC-5 (orig) · GLOBAL ASSIGNMENT — THE PAYOFF (concurrency-safe per assignment once RC-4 is stable; each its own measured rung on the fixed rail).** Assign VM globals into the protected set by MEASURED rank, one at a time: candidates (rank by census, do not trust this order): NV dict base · GVA base (retire ABSQ absolutes) · pat/dcap pool frontiers · keyword cache (&ANCHOR/&FULLSCAN) · subject char cache · statement base. Every assignment: rail A/B on the instrument, honest ratio, revert if ≤1.00×; extend claim-gate `LIVE_CLAIMS` in the SAME commit (s8).
- [ ] **RC-6 · VENEER BYPASS LANES — ⛔⛔ s13b: DO NOT OPEN AS WRITTEN — THE PREMISE IS FALSE AT HEAD.** The NO-VENEER eligible set is **1 of 54** ported symbols (`rt_patstk_lazy_init`, a lazy one-shot): `RTX_GATE(fam,c_fallback)` = `cmp byte [rip+rtx_gate_fam],0; je c_fallback`, so **every gated symbol branches into C whenever its family gate is OFF** — a claimed-register read/clobber that is garbage BY CONTRACT. The enabling condition is per-family RTX **ERADICATION** (migration step 3: gate folded, C fallback deleted), NOT ported (step 2). Census: `scripts/util_rtcc_escape_census.sh` — re-run after any RTX landing; eradication is the only thing that moves symbols to ELIGIBLE. Tripwire: unveneered PLT sites = **17** on x86 SNOBOL4 programs (fibonacci AND roman); any other number means the target classifier moved. When a lane does open, grade **STATIC: crossings_removed × 14** (veneer cost exact — 9 writeback + 5 reload; proven on two workloads by two methods, 784/56 and 1442/103 both = 14.000; the veneered SITE SET is medium-invariant by construction, so the m4 census transfers to m3 as an IDENTITY — FINDING 2026-08-10e §9). Alternative needing no eradication: **RC-6b below (⛔ LON RULES).**
- [ ] **RC-6 (s11) · VENEER BYPASS LANES — ⛔ s11: THE INHERITED TARGET IS FALSIFIED. `rt_call_arr` executes 4 times on fibonacci (99 static sites); `rt_arg_stage` executes 2,692,537. Order by MEASURED crossings via `scripts/util_rtcc_crossing_audit.c` on a MODE-4 binary (mode-3 is blind to crossings), against a STATED workload mix — the hot set is workload-dependent (fibonacci = proc-call path, roman = deferred-eval path, sharing almost nothing). Original text follows.**
- [ ] **RC-6 (orig) · VENEER BYPASS LANES — the resurrected RTX endgame, now OPTIONAL and hotness-ordered.** A family already in asm (`rtx_*.S`) adopts the SCRIP convention natively; the registry reclassifies it NO-VENEER; its crossings become direct jumps/calls with zero writeback. Inherits RTX step-0 discipline verbatim (four checks: live · byte-identical spelling · `nm` linkage · **executes-in-window**) — the eight phantom shapes catalogued in GOAL-SNOBOL4-RTX apply unchanged.
- [ ] **RC-6b (PROPOSED s13b — ⛔ LON RULES; do not start without his yes) · GATED BYPASS: MOVE THE VENEER FROM THE CROSSING TO THE ESCAPE EDGE.** The hot asm body becomes SCRIP-convention-native: its fast path preserves the claimed set itself (it is asm — it must simply not clobber r8–r11 and must re-stage rather than destroy the arg tier) and BRACKETS its own escape edges (writeback immediately before `je c_fallback`, reload on return from C); the registry then reclassifies the symbol NO-VENEER and every call site emits ZERO of the 14. Per-symbol work confined to `rtx_*.S` — zero emitter bytes, zero template churn, both media unaffected. ⛔ **THE TRAP THAT MAKES THE NAIVE FORM WRONG: writeback and reload must be removed TOGETHER** — a fast path that skips the writeback while generated code still reloads restores STALE block contents (silent corruption, strictly worse than the toll); this is why it is asm work, not a registry flag. First lane: **roman's defer trio** (`rt_defer_open`/`_get_pat_fn`/`_close` — one family, 6,600,066 measured crossings, **92,400,924 instructions removed**, one workload exercising all three; 2.2M commits each ⇒ a probe is structurally falsifiable per the s223 commit-count law). Per-rung: escape-bracket audit via the census · killswitch md5 · claim-gate `--strict` · static grade (crossings × 14) · FINDING + cursor move.
- [ ] **RC-7 · FOLD + FINAL BOARD.** `SCRIP_RTCC` folds to always-ON or dies (diagnostics survive); dead SysV-era spellings deleted; completion greps == 0; bench board vs the RC-0 floor on the fixed instrument, N stated; xc318 + 141-probe ≥ floor both modes; regen ×4; FINDING of record; DoD walked line-by-line.

## ⛔ LAWS & TRAPS (binding)
- **A LAW DERIVED FROM ONE REGISTER'S MECHANISM DOES NOT BIND THE OTHERS (s14):** the s8 law (*"NEVER CARRY A VALUE IN A CLAIMED REGISTER ACROSS A CROSSING"*) was generalised from r9's mechanism (seed-restore: writeback deleted, reload preserved) to the whole claimed set. It is FALSE of r8/r10/r11 at HEAD — the round-trip class does save/restore exactly. The law is true of r9 and the arg tier; amended to a convention backed by a gate, not a fact about the machine. Generalises s13's *"A MECHANISM-DERIVED LAW INHERITS THE MECHANISM'S SCOPE"* to register classes as well as rungs.
- **A PRESENCE CHECK RUN ONLY AT OPEN MEASURES ARRIVAL ORDER, NOT OCCUPANCY (s15):** any mutual-visibility signal must be re-read at the instant of the action it protects. Read once at session open it answers a different question than the one asked: the later seat always detects, the earlier seat (which holds the tree, is mid-flight) never does. Re-check before each commit and before push; re-`touch` per rung. Same class as the macro-blind assembly census and the LD_AUDIT complement — an instrument returning a clean answer to a question nobody asked, and an empty answer never checked against a known non-empty control.
- **A GATE RESOLVING ITS TARGET BY ABSOLUTE PATH GRADES WHATEVER SITS AT THAT PATH, NOT THE TREE THE SESSION IS EDITING (s15):** 23/71 gates hardcode `/home/claude/…`; 0/71 use `git rev-parse`. Under multi-seat reality a gate run in a clone audits the peer's worktree and reports the verdict as the current seat's own — directly undermining the HEAD-stamp-every-measurement requirement. Fix: `ROOT=$(git rev-parse --show-toplevel)` once per gate; copy `omega_own_k`'s `TOTAL_FILES -eq 0 → exit 1` positive control into every absence-asserting gate. Fix not yet applied (191 sites, three goals' territory).
- **A KILLSWITCH IS NOT A GATE UNTIL SOMETHING CALLS THE ARM IT GUARDS (s13):** a `#define` that is live, guarded, `-D`-overridable and correct can still be DECORATIVE if its arm has no call sites — `RTCC_GLOBAL_R8_ANCHOR` is exactly that. Before A/B-ing any killswitch, **grep the arm for callers AND confirm the ON form appears in emitted output**; the encoder arm existing is not the arm firing. Three RC-5 numbers have now been voided by three DIFFERENT mechanisms of one class: same binary (s9), noise below floor (s10/s11), unreachable arm (s13).
- **A POSITIVE CONTROL IN THE SAME EMISSION IS THE CHEAPEST GUARD AGAINST A NULL (s13):** "ANCHOR greps to zero" is ambiguous alone; GVA firing 106 times in the IDENTICAL `.s` is what converts it into a measurement. Any census reporting a zero must name the non-zero it measured alongside it. (Generalizes s11's "check the census against a quantity you already know".)
- **A MECHANISM-DERIVED LAW INHERITS THE MECHANISM'S SCOPE (s13):** "RC-5 has zero dynamic delta" is true of GVA's mechanism (same load, encoding-only) and was carried across the whole rung; ANCHOR's mechanism removes 2 instructions AND a dependent load (17B→3B). Attach mechanism laws to the mechanism, never to the rung number.
- **AN UNCOMMITTED WORKTREE IS NOT A STRANDING SIGNAL WHEN ANOTHER SEAT MAY BE LIVE (s11/s12):** two seats ran concurrently on ONE filesystem on 2026-08-10; s12 read s11's mid-flight worktree at 19:51 as an abandoned session (s11 committed at 19:59). `git status --porcelain` cannot distinguish "abandoned" from "in progress". Check commit TIMESTAMPS and whether HEAD is still moving before recovering another seat's tree; never conclude abandonment from worktree state alone. The concurrency protocol assumes one seat per tree -- when that is violated, neither seat can see the other.
- **A STATIC CALL-SITE RANK IS NOT A HOTNESS RANK — FOURTH OCCURRENCE, AND THE SHARPEST (s11):** `rt_arg_stage` and `rt_call_arr` sit **1.36x** apart statically (1031 vs 760 emitted sites) and **673,000x** apart dynamically on fibonacci. The mechanism is structural — hot code is loop-resident and needs ONE call site, cold setup code needs many — so a `grep -c` over `.s` files measures roughly the INVERSE of the quantity it is quoted as. Any rung ordered "by hotness" whose evidence is a static count is aimed backwards.
- **THERE IS NO SINGLE HOTNESS RANK (s11):** the hot crossing set is WORKLOAD-DEPENDENT and the workloads share almost nothing (`rt_call_arr`: 4 on fibonacci, 100,005 on roman). A ranking is meaningless without a STATED workload mix; a single benchmark's rank misranked by 10^5 here.
- **AN INSTRUMENT CAN RETURN THE EXACT COMPLEMENT OF WHAT YOU ASKED (s11):** `LD_AUDIT`/`la_pltenter` on mode-3 measures intra-C PLT traffic, NOT generated->C crossings, because the RX slab has no PLT — yielding an exact INVERSE correlation with the emitted call sites. The tell was the answer being TOO CLEAN (five straight zeros is a property of the instrument, not the program). **Before trusting any census, check it against a quantity you already know** — here, emitted call sites in the `.s`.
- **INTERLEAVE, OR DO NOT COMPARE (s11):** two arms measured in BLOCKS confound the arm with thermal/scheduler drift; on this 1-core box that confound INVERTED the conclusion (sequential said ASLR-pinning was 2x worse; interleaved said the arms are indistinguishable). Every two-arm comparison alternates run-by-run.
- **PINNING CONVERTS VARIANCE INTO BIAS, IT DOES NOT REMOVE IT (s11):** `setarch -R` yields ONE layout, not the average layout, and a single layout can be systematically unlucky (~5% slow here). Justified for RATIOS (shared layout cancels between arms); the "reduces spread" claim is FALSIFIED (on 1.075x vs off 1.085x, N=12 interleaved).
- **AN INSTRUMENT THAT FAILS ITS OWN EXIT CRITERION CANNOT GRADE A RUNG (s11):** RC-0(a) demands max/min <= 1.05x on an unchanged binary; this box gives 1.075-1.085x. Every rung graded on this rail below ~1.09x is UNGRADED whatever number it printed.
- **`cmd | grep -q` UNDER `pipefail` IS A DEFECT WHEN THE UPSTREAM IS EXTERNAL (s11):** the reader's early exit SIGPIPEs the upstream and pipefail propagates it, so a MATCH READS AS A MISS. Deterministic above the 64KiB pipe buffer, a scheduling race below it. Portable form is `grep -c` + a count test; `grep -q FILE` (no pipe) is always safe. The discriminator is the UPSTREAM, not the `-q`.
- **CONTENT, NOT HASH, IS THE TEST FOR "DID IT LAND" (s11):** rebase at handoff rewrites hashes, so an absent hash is not absent work -- and a present hash in a cursor is not present work. Verify by grepping for the artifact.
- **A PUSHED FINDING IS NOT A PUSHED FIX (s11):** repos push separately and can diverge; `.github` landing while SCRIP does not is silent and self-concealing, because the next session orients off the FINDING and inherits a tree state that never existed.
- **BLOCK-CANONICAL LAW**: registers are a cache valid only inside generated code; every crossing writeback/loads. A C function reading a claimed register is reading garbage BY CONTRACT.
- **RETURN-BEFORE-RELOAD**: RAX:RDX capture precedes global reload at every RC-4 site — encoder-owned, never template-owned.
- **NEVER VENEER A GENERATED TARGET**: PAT$/proc/port-label calls stay direct; veneering one deadlocks the cache semantics (generated code expects live registers). Registry is the ONE authority; an unclassified target is a BUILD ERROR, not a default.
- **THE BOUNDARY IS HOT**: no speed number without the RC-0(a) instrument, N stated, min-of-N. "The window was too short" is a falsified excuse (RTX s224).
- **CRATER ATTRIBUTION**: RC-2/RC-4 flips land on a board ZETA-MECH may still be recovering — gate against the RC-0 HEAD-STAMPED floor, diff fail sets BY SET, and name parallel-landed commits in the cursor before attributing any regression to RTCC (the s235 three-builds rule: attribution by builds, not by argument).
- **"BY CONSTRUCTION" IS A HYPOTHESIS UNTIL A PROBE KILLS THE ARM** (RTX s235, sixth failure of that claim shape) — RC-1's poison probe exists because of it.
- **NEVER CARRY A VALUE IN A CLAIMED REGISTER ACROSS A CROSSING (s8):** the veneer is NOT a callee-saved-register emulator. A site that needs a value to survive a C call uses an unclaimed scratch-tier register or an rbp/ζ slot — never a claimed one. The R9/GVA slot is no longer written back (`5b435458`): reload always restores the pristine seed, so such a dependence on r9 is now broken BY DESIGN rather than working by luck. Probe-proven both directions (FINDING Addendum 2).
- **CLAIM-GATE IS PART OF THE RUNG (s8):** `scripts/test_gate_rtcc_claimed_regs.sh` — every RC-5 assignment extends `LIVE_CLAIMS` in the SAME commit; `--strict` green joins killswitch md5 in each rung's exit. It caught a live defect on its first run (fibonacci AB=1+RTCC=1 SIGSEGV) that a human had already noticed in a commit message and forgotten.
- **A THRESHOLD AT THE NULL CENTRE IS NOT A GATE (s10):** RC-0(a) defines the no-effect distribution as centred on 1.00×; an accept criterion of `>1.00×` therefore passes a zero-effect rung on a coin flip — and did so for RC-5-GVA and RC-5-ANCHOR (opposite outcomes, same coin). **An accept threshold must lie outside the measured noise band of its own instrument** (currently: ≥1.10× on the rail at ASLR=off) **or be replaced by a noise-free instrument.** Where a rung's mechanism is static (encoding delta, site count, byte width), grade the mechanism — it has no noise band. Measured at HEAD: unchanged binary max/min=1.031× ASLR-pinned, 1.127× default; the min-of-N statistic itself moves 3.2% on ASLR alone.
- **COUNT ENCODING BYTES FROM THE ASSEMBLER, NOT FROM DESCRIPTION (s10):** the absolute GVA form was recorded as 7 bytes (s5) but is 8 bytes — REX.W + op + modrm + SIB + disp32; the SIB is required for the no-base disp32 encoding (mod=00, r/m=100, base=101). Before recording an encoding saving, assemble both forms and read address deltas from `objdump`. A one-byte undercount in a claimed saving is a falsifiable prediction that the next measurement will correct.
- Per-rung: killswitch md5 · claim-gate `--strict` green · watermark re-proved at open AND close · HEAD-stamp every measurement · `setarch -R` (`ASLR=off`) on suites · FINDING + cursor move or the rung did not happen.
