# HANDOFF 2026-06-07-G — IR-REDESIGN: IRD-3d + IRD-3e COMPLETE (Opus 4.8, Lon attending)

**State of the world:** SCRIP origin/main = `faa9b52`, .github origin = this commit. Both trees CLEAN, everything pushed. Prolog lowering (`lower_prolog.c`) and `lower_proc_gen` carry **ZERO α/β child writes**; remaining γ/ω writes there are legitimate control ports (pl_set_succ_fail, apply→ω, arm→γ). Icon-scope single-child carries (LIST_BANG/EVERY/UNTIL/REPEAT) are on operands[0]. All α/β child-write residue now lives ONLY in driver gz-synth + ring builders + SCAN — i.e. bulk stage (c).

## Commits this session (each fully gated against pristine /tmp/base_pre; gates = 5 sweeps byte-identical, 5 smokes clean, prove_lower ;PASS=68, pl smoke m2 5/5 HARD m3 3/2 m4 3/2, icon m2 12/12 HARD m3 10/2 m4 10/2, plus git-stash masked-ival structural A/B per writer flip)

1. **`6e3cb1e` IRD-3d-CHAIN-CONSUMERS-CORE** — γ-chain STRUCT/BUILTIN readers → ir_call_arg across interp (resolve STRUCT walk, throw, 9 a0/a1/a2 ladders, typetest/atom-families/write-family/sort/format/numbervars/writeq/retract/assertz/abolish + all α-presence guards), runtime/unification.c pl_build_term_gz_r (libscrip_rt rebuilt + m4 regated), emit_bb 883 marshal, driver classifiers + gz-write + findall walk + pl_rich_node_emittable.
2. **`470cdd0` IRD-3d-CHAIN-CONSUMERS-TEMPLATES** — 9 BB_templates files, strict count asserts, residue proofs per file; bnth helper DELETED; bb_is_cmp untouched (pair).
3. **`9134387` IRD-3d-CHAIN-WRITERS-FLIP** — lower_prolog 228 (TT_FNC per-child push, dead `prev` removed), 253 (MAKELIST cons pushes [hα,suffix]; cell→α AND hα→γ writes deleted), 271 (g_builtin push). **REGRESSION CAUGHT AND FIXED IN-COMMIT:** recursion.pl m3/m4 segv at `pl_gz_rule_callee_body` write arm (~697) — a SECOND parallel gz write-arg reader. Its guard names write by sval/ival only (no α mention → α-keyed census missed it); admission (`pl_gz_rule_clause`) validates name+ival only, so flipped bodies were ADMITTED and the builder deref'd NULL α. Diagnosed via `gdb -batch -ex run -ex bt`; fixed via ir_call_arg(gg,0); post-fix residue audit of driver+interp = exactly pair classes. A/B: BLTIN α→-1 ×14 rows, STRCT ×4, former-arg ATOM/LVAR γ→-1 ×6.
4. **`e7e1e22` IRD-3d-PAIR-CONSUMERS** — NEW `ir_pair_arg(nd,j)` in contracts/IR.h (operands-first, else j?β:α); all pair consumers converted (interp cmp/term_compare/succ/is-fallback; driver is/succ/gz-ok/rule_callee/build_goal/lint/mset/rich arms; emit_bb is-β marshal; bb_is_cmp 22α/39β prefix-preserving — rhs-ARITH internals stay verbatim, gz-synth regime; bb_succ_plus; bb_resolve mset). **Flip-safety banked:** post-flip guard over-admission of γ-chain 2-arg names into cmp arms is blocked by in-body is_cmp checks (519/686/953 return-0-identical) + gz admission name filter — no separate name-set gate needed.
5. **`415e465` IRD-3d-PAIR-WRITERS-FLIP** — 178/193/208 → push ×2 each. A/B: exactly 12 BLTIN lines = 6 pair rows with α AND β →-1 together. Gate carried the sno flake (see laws).
6. **`28dad0b` IRD-3d-CATCH-FLIP (IRD-3d COMPLETE)** — g_catch catcher → operands[0]. Zero α readers existed (all consumers read zc->catcher sidecar) but bare deletion forbidden: bb_walk_rec reaches the catcher subgraph THROUGH the child carry (sidecars unwalked — d20c45e .rodata class). A/B: one '?' row (CATCH lacks a kname in the harness — IRD-5 doc item) α 3→-1.
7. **`5334ead` IRD-3e-REST-ICON-FOUR-KINDS** — LIST_BANG 181 / EVERY 346 / UNTIL 423 / REPEAT 436 → operands[0]; REPEAT `rp->γ=eα` loop-back STAYS (control edge; A/B-proven γ=4 both sides). v_while precedent mirror: cond/child push only; β-body reads in those interp/emit arms have zero writers from these lowerers and stay VERBATIM (serve other producers). Writers+consumers in one commit justified by single-consumer-per-kind dual-read. interp ucnd/r0/ev0/lb0 locals; flat_drive_list_bang + flat_drive_every → bb_child0 (flat_drive_idx_get deliberately untouched — IDX_GET ≠ 3e-rest); bb_every.cpp ×3 → ir_call_arg. A/B: EVERY/UNTIL/REPEAT α 4→-1 one row each; LIST_BANG absent from prove corpus (no bang construct) — validated by byte-identical icn sweep.
8. **`3cedeea` IRD-3d-FOLLOWUP bb_term_io format a1 + INHERITED-RED TRIAGE** — the 470cdd0 hybrid `a1 = a0->γ` (fetch converted, relative hop left) → ir_call_arg(pBB,1) ×2 (141/155); latent, corpus-unreached, byte-identical gates. **TRIAGE VERDICT for BB-FIXUP b8e3a04 tracker note:** format("~w~n",[42]) m3 rc=134 PL-GZ FENCE abort + m4 rc=1 empty are **worktree-PROVEN PRE-EXISTING at `6e3cb1e~1`** (pre-session pristine build reproduces both identically; m2=42 correct both heads). Compound format/2 was NEVER admitted by gz/flat tiers; the fence (interp fallback deleted 2026-06-06) makes it abort. BB-FIXUP's "470cdd0 γ-hop × 9134387 flip interaction" attribution is wrong on causality. Owner: admission layer, not IRD. Smoke corpus has no compound-format case — add one when admission lands.
9. **`faa9b52` IRD-3e PROC_GEN self-loops DELETED** — `bb->α=bb; bb->β=bb` removed outright. Zero field readers anywhere (interp arm reads only IR_EXEC counter + γ/ω; all other mentions are kind tests/kname/enum-range). bb_walk_rec revisit-guard verified (g_visited ring, emit_core 700-708). Operands-push rejected: arity/dump pollution. A/B EMPTY — PROC_GEN graphs are runtime-synthesized per GeneratorState, never in the static prove corpus.

## Races absorbed ×3 (LAW: a clean rebase is NOT a gate — rebuild + re-smoke the merged head every time)

- `b8e3a04` BB-FIXUP bb_term_io regen (re-derived on my raced 470cdd0/9134387 heads per their PUSH-RACE ABSORPTION note; their format γ-hop "kept as theirs" = my hybrid, fixed in 3cedeea).
- `27c797f` B0 pattern_match stub-text purge — **src/runtime/** file → libscrip_rt rebuilt.
- `b7a2717` B0b AST-evaluator deletion — disjoint; merged tree rebuilt + m2 hard gates verified.

## Laws recorded this session (append to the running set)

4. Freestanding RELATIVE γ-hops on already-fetched arg locals (`a1 = a0->γ`) escape α-keyed censuses — at every flip, grep `'->γ'` in the kind's consumer files too (bb_term_io 141/155).
5. Guards naming a kind by sval/ival WITHOUT α/β mention hide parallel readers from α-keyed sweeps — grep by BUILTIN NAME too (pl_gz_rule_callee_body, 9134387).
6. prove_lower LVAR/ITE/GCONJ rows carry pointer-punned ivals — mask column 7 (awk `$7="IVAL"`) before structural diff; unmasked extras are ASLR noise.
7. `test/snobol4/keywords/100_roman_numeral.sno` runs 7.7-7.9s vs the 8s sweep timeout — flaked rc=124 once under bake load (415e465 gate), 153/153 identical on idle re-run, identical empty-output md5; pre-existing; bump the timeout when convenient (IRD-5 note).

## Remaining ladder (goal-file NEXT order)

1. **Bulk stage (c)** — start with ring BINOP/UNOP (scrip.c 89/92 stack-wires + interp tree reads ~2508/2598/2712, icon/raku scope); then gz-synth SYNTHESIZED α/β regime (writes at ~598/617-23/652-3/673-7/683-4/693-5/701/882/917-8/944/952/961/1099 — those nn->α/β assignments are the regime the dual-read emit arms expect; converting them means converting bb_det_is.cpp, bb_is_cmp rhs-ARITH internals, bterm_arith together); SCAN subject ruling (pattern-BB joint owner); flat_drive_gz_query γ-walks ~572-595; flat_drive_seq ~927; emit_bb 2045 arm-list-via-ω; bb_call.cpp:93; **operand_aux LAST and as its own sub-cluster — LOAD-BEARING, census 2026-06-07-G: get callers at emit_bb 315/360/382/689/2481/2607 + bb_alt.cpp:34 + bb_call.cpp:96 (ALT arms, CALL fallback, chain-child resolution); set callers uncounted. NOT a micro-commit.**
2. **IRD-4** (γ/ω → IR_ref_t, α/β DELETED, t→op) → **IRD-5** (fence audit, sizeof, ARCH-IR.md, prove_lower ops column + CATCH kname + sno timeout bump).
3. Residue flags: interp IF/WHILE/UNTIL/EVERY ->β body reads have zero writers (re-verified at the 3e-rest flip) — verify-then-delete candidates in bulk.

## Next-session boot

Clone/pull BOTH repos; `git remote set-url origin https://TOKEN@github.com/snobol4ever/<repo>` with Lon's token; git identity LCherryholmes/lcherryh@yahoo.com per repo; `apt-get install -y libgc-dev; make; make libscrip_rt` (MANDATORY for m4); bake `scripts/bake_ird3_baseline.sh /tmp/base_pre` FOREGROUND (~2.5 min) BEFORE touching code; then bulk stage (c) per NEXT. Concurrent sessions: BB-FIXUP (owns BB_templates + BB-REVAMP-TRACKER.md — rebase before touching bb_*.cpp; answer tracker notes in your commit messages, never edit their tracker), B0 (runtime/AST purges), pattern-BB design (SCAN ruling joint owner). ALWAYS `git pull --rebase` both repos before working; after any raced rebase, rebuild and re-smoke the merged head before claiming green.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude
