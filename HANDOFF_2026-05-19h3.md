# HANDOFF_2026-05-19h3.md — PST-SC-SWITCH-LABELS complete

**Session:** 2026-05-19 (Sonnet 4.6)
**Goal:** GOAL-PST-SNOCONE.md — PST-SC-SWITCH-LABELS

## What landed

### PST-SC-SWITCH-LABELS ✅ (one4all `648b7d24`)

**Problem:** `sc_finalize_switch_pst` still pushed `TT_QLIT(_Lend_NNNN)` as last child of TT_CASE — synthesized label in parser, V13-switch violation reopened by AUDIT-2.

**Fix — `src/frontend/snocone/snocone_parse.y`:**
- `sc_switch_head_new`: removed two `sc_label_new` calls; `end_label=NULL`, `default_label=NULL`; `sc_loop_push(NULL, NULL, 0)` — mirrors PST-SC-LABELS for while/do/for.
- `sc_finalize_switch_pst`: removed `TT_QLIT(end_label)` last-child. TT_CASE shape now `[disc, val0, body0, val1, body1, …]` only. No trailing QLIT.

**Fix — `src/lower/lower.c`:**
- `lower_case`: new `is_snocone` branch (g_lang != LANG_RAKU, no trailing QLIT). Mints `lbl_end` via `lower_fresh_label`, pushes/pops `g_loop_stack` via `loop_push`/`loop_pop` so `TT_LOOP_BREAK` inside switch resolves correctly. Registers label with `labtab_define(lbl_end, exit_pos)`. Old `has_qlit_end` backward-compat path retained.

### PST-SC-DOC-CLEANUP ✅ (same commit)
Stale docstrings in `sc_finalize_while_pst`, `sc_finalize_do_while_pst`, `sc_finalize_for_pst` updated to reflect current tree shapes. No code change.

## Gates
- smoke_snocone: 2/3 (floor — pre-existing LE segfault)
- scrip_all_modes: 2/0
- crosscheck_snobol4: 5/1
- All at floor ✅

## State after session
- GOAL-PST-SNOCONE.md: Phase 1 C **COMPLETE** (11/11 violations closed). Updated + pushed.
- one4all @ `648b7d24`
- .github @ `82c9485d`

## Phase 2 gate
PST-SC-SWITCH-LABELS was the **last gating item** for Phase 2 (per PLAN.md).
Phase 2 ordering: PRF-13 → PST-SN4-SC → PST-SC-SC → PST-RB-SC → PST-PL-SC → PST-ICN-SC.
PRF-13 is SCRIP mirror work (parser_raku.sc + raku_helpers.sc) — not C.
PST-SC-SC is the next C-adjacent work (SCRIP mirror for snocone parser).

## Next session
Update PLAN.md row for PST-Parent and PST-Snocone to reflect Phase 2 gate open.
Then begin PRF-13 (SCRIP mirror, Raku) per Phase 2 ordering.
