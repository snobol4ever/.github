# Session Handoff — 2026-08-09 s30 (Opus)

## Context at handoff: ~90%

---

## What was accomplished this session

### OS-2·SLICE-1 LANDED: DEFER-immune capture δ anchor, per-node slots, region-backed

**Problem:** `((*P $ X)(*Q $ Y)) $ Z` — nested deferred captures — was failing m3 and SEGVing m4. Two separate environ-smash bugs, both measured in gdb.

**Design approved by Lon:** per-NODE (per-activation) slot — "I see your per instance slot usage. You'll only do it when needed. All is good. Implement it."

---

### Commits pushed

**SCRIP `1c958553`** — OS-2 SLICE-1 implementation:
- `src/emitter/emit.cpp`: `cap_ok_at` + `cap_anchor_of` (THE ONE ROUTING AUTHORITY, returns 1-based slot k; SCRIP_OS_CAP=0 = byte-identical). IMM/COND resolve through operands[1] so the pair shares the SAVE's k. Declines: ARBNO body span; FENCE1/ABORT graphs (floor-whack hazard); non-pinned non-jmp-entry classes (measured L07: naked FR on rsp arm = depth-diff disease reborn). GLUE-O enter carve widened: `op_fc_bytes = 64+16N` when N>0 anchored SAVEs; N=0 byte-identical. SAVE's `op_fc_base` nulled under anchor (regime-2 cannot capture the rsp-cell spelling).
- `src/templates/bb_match_capture.cpp`: `caphome()` helper — legacy deep MAIN → `RDD("rbp", -(64+16*op_cap_anchor))`; carve-then-pin classes → `FR(op_off)` raw (per-activation, makes *P recursion correct). Anchor arms moved above zres ZD arms in cascade (ZOPD across DEFER = depth-diff disease; `_fb/_adj` deleted whole per ONE-SYSTEM ruling).
- `src/templates/bb_match_begin.cpp` + `bb_match_end.cpp`: `stfh()` widened with `oscap_l()` and `has_replace_l()` to re-home the PATCTX/old_rbp quartet from positive FRQ slots (environ-smash class) to HKQ negatives backed by its own zclaim(48). Excluded: REPLACE (bb_match_replace.cpp:33 reads FRQ(+40) raw — unify later), FENCE1/ABORT/ARBNO (all have measured split readers or floor-whack hazards; X10 ARBNO exclusion: mechanism-2 release reads old_rbp at positive [rbp+pin] — exit SEGV on stale base when HKQ moved it).

**corpus `226f904b`** — OS-2 SLICE-1 witnesses:
- `dc_nest2/dc_nest3/dc_recur` — PASS m3+m4 (dc_nest2 = Lon's exact case)
- `dc_nest_bt` — xfail (pre-existing defer-β re-yield rung; capture-free repro committed)
- `dc_sib_bt` — NEW: sibling-backtrack discriminator. `'ABCD' ? ((*P $ X)(*Q $ Y) 'D') $ Z` with P=ARB Q=LEN(1). Oracle X=AB Y=C Z=ABCD. Per-depth slots would give X=B (alias). SCRIP gives X= (empty) — convicts the re-yield wiring, not the slot design. Both xfail with cause.

**dotgithub `109c9779`** — cursors s30 prepended in GOAL-SN4-ZETA-MECH.md and GOAL-SNOBOL4-BB.md.

---

### THE FINDING (extends s23a PIN-REBASE)

LP-1 `flat_all_zd=8` "region unused" premise is **FALSIFIED** by pinned-FR region readers on carve-less mains. Two gdb convictions:

1. Capture slot offset 400 landed inside `environ[8]` — dword cursor store from the SAVE arm smashed the pointer array; `getenv("SCRIP_DCAP_TRACE")` walked it to a SEGV. m3 only "passed" because producer and consumer hit the same slack byte (LUCK — same word the s22z comment used).

2. Hardware watchpoint: `n77_match_begin_α: mov %rbp,0x138(%rbp)` — the HEAD QUARTET's old_rbp store smashing `environ[1]` on dc_recur (head ZLS off 272, environ base at 0x7fffffffeaa0 = rbp+0x138). **The quartet was the pre-existing latent; the capture slots were just the first hard casualty.**

**Next slack-rider family named:** start_δ / rsp_mark / zls2_mark at their positive FRQ offsets — these are what blocks D06 m4 (same environ-smash class, next rung).

---

### Gate results

**148-probe A/B vs SCRIP_OS_CAP=0:**
- m3: 15 → 4 fails, **0 regressions**. Fixes: D02/03/04/06/09/11 H21/31 N08/09 N16.
- m4: 13 → 7 fails, **0 regressions**. Fixes: D05 D09 H31 N08/09 N16.
- D05: PASS both modes. D06: m3 flipped per spec (m4 blocked by start_δ family).
- D09/D11 = SLICE-2's targets; N08/N09 = SLICE-3's — both flipped as a side effect.
- Residual m3 fails (4): H29 N21 fence_probe t6m — pre-existing, none introduced.

**Witnesses:** dc_nest2/nest3/recur + stitch trio (snapshot/defer/compose) + os1_runtime_k — all PASS m3+m4. os1_runtime_k oracle-identical both modes.

**dc_nest_bt + dc_sib_bt:** xfail, cause committed. Defer-β re-yield is a separate rung.

---

## Repo state at handoff

| Repo | HEAD | Clean | Pushed |
|------|------|-------|--------|
| SCRIP | `1c958553` | ✓ | ✓ |
| corpus | `226f904b` | ✓ | ✓ |
| dotgithub | `109c9779` | ✓ | ✓ |

Binaries built and current. `gdb` installed (session-local, apt-get).

---

## Terse answers owed to Lon (from the session)

**Which constructs build an RBP frame on the RSP stack?**
- Deep statement chains via GLUE-O outer bracket (`push rbp; mov rbp,rsp; sub rsp,K`) — the single whole-chain bracket for goto-bearing SNOBOL4 graphs.
- STMT-FRAME per-statement `bb_glue_framed_enter/leave` heads when the regime is on (SCRIP_STMT_FRAME; off for goto-bearing graphs — exactly the dc_* class).
- PAT$ blob α (BLOB-GRANT, always, per-activation): `sub rsp,kt; mov rbp,rsp`.
- rpin arm of MATCH_BEGIN when taken (`mov rbp,rsp` — statement claim base pin).
- proc/ζ-frame graphs (gen-proc = HEAP-frame adopt via ICN-FR-2 / flat_lcl_proc prologue).

**Does DEFER use an RBP frame?** No — DEFER emits into the BLOB-GRANT prologue of its minted PAT$ blob. The blob has its own real frame (sub rsp,kt; mov rbp,rsp). It does NOT build an RBP frame on the STATEMENT RSP stack. No cross-frame MATCH-frame access needed because match context travels via PATCTX-copy + r12/r13/r14/r15 registers; every BB's captures are graph-local pairs in their own frame.

**No IR_t depth field needed:** operands[1] linkage + ZLS grant give IMM/COND their SAVE twin's k by construction.

---

## Next rung candidates (in order per GOAL-SN4-ZETA-MECH)

1. **Defer-β re-yield** (xfail rung exposed this session): blob ARB doesn't re-yield on retreat. Repro committed. Discriminator: dc_sib_bt.
2. **D06 m4 / start_δ family**: start_δ/rsp_mark/zls2_mark positive FRQ slots on carve-less mains — same environ-smash class as the quartet, next family.
3. **SLICE-2** (D09/D11): ARBNO extent-base captures — already partially flipping from this slice as a side effect.
4. **SLICE-3** (N08/N09): already partially flipping.
5. **ARBNO stfh exclusion unify**: the mechanism-2 release reads old_rbp at the positive [rbp+pin] slot; when that restore goes through the one macro, ARBNO joins the widened stfh naturally.


---

## Addendum: GOAL-SN4-ZETA-CLIMB update (s26)

CLIMB.md updated and pushed (`d50dfb54`) with:

**Watermark corrected:** m3 136/12xf → **144/4xf** · m4 135/13xf → **141/7xf** · 0R both modes.

**Probes that flipped (verified at HEAD `1c958553`):**

| Probe | m3 | m4 |
|-------|----|----|
| D06 | ✅ PASS | ❌ still FAIL |
| D09 | ✅ | ✅ |
| D11 | ✅ | ✅ |
| N08 | ✅ | ✅ |
| N09 | ✅ | ✅ |
| N16 | ✅ | ✅ |
| H31 | ✅ | ✅ |

**Rung updates:**
- C-6: RSP-aliasing MECH cross-request (s17) marked RESOLVED — the `rbp+528 ↔ rsp+480` collision is gone; negative-rbp anchor slots cannot overlap positive RSP stores. D06 m3 closed. D09/D11 closed. Remaining = D06 m4 only (OPS-2 gated, s24 carry-forward still the entry point).
- C-7: N08/N09/N16/H31 closed as SLICE-1 collateral. Remaining = N21 (dc_sib_bt is the entry point), H29 (CRASH), H21.

**XFAIL update needed at next commit:** retire from XFAIL.run: D06 D09 D11 N08 N09 N16 H31.

**dotgithub HEAD:** `d50dfb54`
