# FINDING (2026-07-25, s153, Claude) — PS-3 slice "defer-as-KNOWN-FOOTPRINT-LEAF" LANDED (opt-in): the ε-RESUME CONVENTION is the tail arm's real contract, and a defer leaf must be made content-blind to honor it.

**Context:** The s152 cursor's named next rung — teach `fc_tail_walk`/`fct_fp_range` that a seal==2 prologue-bound defer with a registry-uniform target contributes SUSP to fp, so WINDOWED ARBNO bodies with an interior defer take the existing R12-EXIT fast tail arm. Landed this session under `SCRIP_ARBNO_LATCH=1`. Three findings of record beyond the plan.

## 1. The registry is emit-phase-ordered ⇒ the fct finalize must be TWO-PASS
`drive_slots_all` runs `zls_build` (containing the fct finalize) for ALL graphs BEFORE any proc emits — the `emit_patzeta` registry is EMPTY there, so a defer's SUSP is unknowable at the historical finalize point (first implementation: every windowed candidate declined, uni=0 across the board). Resolution: `zls_fct_finalize(g, late)` — the early pass (from `zls_build`) finalizes defer-free candidates exactly as ever and leaves defer-bearing ones PENDING; `emit_chain` calls the late pass at each graph's emission entry, where every earlier-emitted PAT$ is registered (driver loop registers-then-emits per proc, all before main). Forward/self references decline deterministically (loop order fixed, both modes).

## 2. THE CRASH AND ITS LAW — the ε-resume convention (t3, rip=0, both modes)
First armed run: T1 (multi-iteration) and T2 (ε) passed; T3 — the statement-failure/unwind on `"(1,,2)"` — SEGV'd identically m3+m4 at `rip=0`. Ground truth from the emitted `.s` (not the template comments): the tail arm's fail glue, on a real element's death, pops `op_sb` and enters **the body SEQ's β, which statically forwards to the LAST body box's β at the PREVIOUS element's yield depth**. When the previous element is the ε element, that β lands on the **phantom FPB pad** — which is NOT zeroed and NOT written: it is dirty stack. The proven defer-free arm survives because granted-leaf βs are **content-blind**: they pop their own cell footprint and ω-transit regardless of cell content, and the ε exhaust path (`L(2)`) restores r14 from the ε header — so pad garbage corrupts nothing as long as the POP ARITHMETIC is exact. **The defer's raw `jmp [rsp+0]` is the one content-SENSITIVE β**: on the pad it jumps through garbage (NULL on virgin stack — the measured rip=0; wild otherwise). This is the tail arm's real per-box contract, now named: *every body box's β must, on arbitrary cell content, pop exactly its own footprint and ω-transit.*

## 3. The fix — make the defer leaf content-blind (two template-only changes)
(a) **Zero-guarded defer β** (`bb_match_defer.cpp`, staged via new `fc_tail_defer_susp_g` → `g_emit.op_defer_leaf_susp`, s141-appended): test the γ-record at `[rsp+0]`; nonzero ⇒ `jmp rax` (the record) as ever; zero ⇒ `add rsp, SUSP` + ω — the exhausted-leaf behavior, consuming the pad share exactly as granted leaves consume theirs. (b) **Deterministic discriminator**: the tail α zeroes the pad `[0,FPB)` when the candidate carries defer leaves (`fct.dfr` → `fc_tail_dfr` → `g_emit.op_tail_dfr`); defer-free candidates keep the unzeroed pad byte-verbatim. After (a)+(b): the whole T3 unwind is arithmetic-exact — pad-pop cascade → ε header read at the exact HDRB depth → `add rsp, op_sb` lands on the ARBNO entry frontier → left defer's REAL record resumes → statement F.

## Safety structure (the finalize-decline is a NEW decline point)
A finalize-decline must strand nothing granted at LOWER: (i) defer-bearing candidates admit only with ZERO wrap/promo capture registrations (`defer-caps` lower gate — the 163-mine class prevented by construction); (ii) decline nulls head+arbno (kills the fin-blind `fc_tail_head` coherently) and `fc_seq_unregister`s the pat_entry SEQ (new API) — a half-tail statement is impossible. SUSP pricing is context-confined (`fct_pricing`) to the finalize so `zls_g_fp_total` → `pz[].fp` never becomes registration-order-dependent (a mode-divergence mine deliberately not stepped on).

## Proof set (s153)
Canary **182** (`crosscheck/patterns/182_pat_arbno_defer_windowed_leaf.sno`, 8 stressors: multi-iteration, ε, failure/unwind, commit-then-retract, left+right window defers, ALT-bearing uniform target ⇒ SUSP=176, stored-pattern path, recursive-target decline, depth-12): **oracle-identical both modes armed**, nonzero sizes; the armed compile selects 6 TAIL arms (SUSP 128/176 verified). 179/180/181 armed byte-identical (179 still declines; 181's DT arm still fires). Smokes 7/7×2 default AND armed. `.s` regen ×3 zero drift (default byte-identity). Crosscheck watermark **OFF = ON = POISON-armed = m3 313/1 · m4 308/4 · DIVERGE=3** (the s152 watermark +1/mode from the new canary; identical failing sets). **All 10 match demos** (calculator-1/2±fence w/ calculator.input, claws5±fence w/ CLAWS5inTASA.dat, treebank±fence w/ VBGinTASA.dat, json±fence w/ twitter.json): m3-OFF, m3-ON, m4-ON all byte-identical to refs.

## Honest scope + rail
Rail `bench_sno_rail.sh treebank-match` (SO_A=out/libscrip_rt.so, -O0): **OFF ratioA=1.26, ON ratioA=1.27 — unmoved, within noise, identity OK.** As predicted: treebank's `ARBNO(*group)` target is recursive ⇒ non-uniform ⇒ declines; calc's defers sit inside UNGRANTED-ALT subtrees (fc_alt_register declines DEFER arms); json's inside ALT arms + recursive. **This slice lands the MECHANISM (proven by 182); slice 2 — the record-peek cursor (`[rsp+8]→[rbp+56]`) — removes the uniformity gate, admits recursion, and is the 58% lever.** Widening residues: defers inside granted-ALT arms (teach `fc_alt_register`), defer+capture coexistence (relax the `defer-caps` gate once finalize-decline-with-captures has an unwind story).

## Container states of record (bisect-proven pre-existing, NOT this rung)
(1) `test_demo_full_3way` treebank-**list** m4 leg prints `''` — reproduced with the byte-identical checked-in `.s` against a PRISTINE stash-rebuilt `libscrip_rt`; treebank-list.sno has 2 indirect-goto sites = the same class as the watermark's expected m4 fails (214/215/216); not stack-size (ulimit probed). treebank-**match** (the demo-gate program) is fully green. (2) `test_scrip_demos.sh` is 3/7 identically OFF and ON — `.scrip`-frontend demos, unrelated to this rung.

## FACT-RULE catches earned this session
(a) Detached (`nohup`/backgrounded-subshell) builds AND clones die silently in this container — twice this session (s126 class); foreground `timeout 110 make -j4` chunks resume cleanly. (b) `extern "C"` at block scope is ill-formed C++ — twice; declare at file scope. (c) "identical output" ≠ "arm fired": the first armed run passed while every candidate silently declined — the `.s` grep (`IR_MATCH_ARBNO_TAIL` count) + first-failing-gate diag are the engagement proof, not the diff.
