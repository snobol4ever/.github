# FINDING — WS-CLASS SPLIT LANDED (HB_WSC collectable workspace class; frontier-drop restored on transient churn) + LIBGC residue swept, eradication PROVEN with libgc-dev physically uninstalled (s91, 2026-07-17, Claude)

**SCRIP commits this session: `29413cb8` (libgc residue) · `e7f7bbba` (the split) · `cf380867` + `309069ac` (200-col compliance).** Directive: "Complete the unified GC and get rid of Boehm GC library usage if not already done so. We want the ONE new GC system everywhere."

## 1. LIBGC — the eradication is now TOTAL and PROVEN, not just recorded
GC-U-4 (s67) deleted Boehm from the tree and the main link; this session swept the residue and proved the whole claim by physical absence:
- `-lgc` removed from **31 scripts** (every helper/bench/crosscheck gcc link line still carried it as a phantom dependency — `nm` shows ZERO `GC_` symbols in `scrip` or `libscrip_rt.so`, so the flag pulled nothing but forced libgc-dev to exist at link time).
- `install_system_packages.sh`: libgc-dev dropped from PKGS; its stale NOTE (claimed `core.h:8` includes `gc/gc.h` — it does not) rewritten to record the eradication.
- Dead `#define ZC_HEAP_LIBGC 0` deleted (zero references); the one prose `GC_malloc` mention in `rt_arena.h`'s D3 note de-Boehmed.
- **THE PROOF:** `apt-get remove libgc-dev`, then the FULL gate set — sno smokes 7/7×2 · crosscheck m3 303/4 · m4 293/13/1 · DIVERGE=10 identical names · icon 12/2+10/4 · prolog 3/2×2 — ALL AT WATERMARK with Boehm physically absent from the container. The ONE GC (gc_heap.c MARK→FORWARD→ADJUST→SLIDE) is the only collector anywhere in the system.

## 2. WS-CLASS SPLIT — the GC-U-7 finisher (s90's named fix), landed
**Mechanism (`gc_heap.{c,h}`, `rt_arena.h`):** new title type `HB_WSC` = the COLLECTABLE workspace class, allocated via `rt_ws_alloc_c` / `rt_ws_strdup_c`. Contract: **zero-pointer char payloads referenced ONLY through DESCRs or conservatively-scanned locations** (DESCR STRVALs, C locals/statics, ζ frames, coexpr spills). Collector treatment: (a) NOT blanket-pinned at reset (the whole point); (b) the `ws_only` conservative filters (statics scan + flavor-2 gateway stack scan) honor it — without this, raw WSC pointers in C locals/statics across gateway seams would lose their referents; (c) marked ⇒ promoted to `HBF_PIN` at forward (fwd=self, never slid — raw `char*` referents cannot be adjusted); (d) unmarked ⇒ RECLAIMED (HB_FILL / slide). Its payloads are NEVER transitively scanned: zero-pointer by contract, and conservatively scanning string bytes would mint false pins. `rt_ws_*` stays the IMMORTAL class; `rt_ws_realloc` grows into IMMORTAL regardless of source. Per-class pin telemetry added to the `[ZGC]` line (`pin-classes ws= wsc= zblk= val= wsc-dead=`), env-gated under `SCRIP_ZETA_TELEM`.

**Flips (39 sites, all in `by_name_dispatch.c`):** 35 mechanical `STRVAL(rt_ws_strdup(…))` single-expression sites + 4 gdb-attributed hot buf sites (TRIM/DUPL/REPLACE/DATE: `buf = rt_ws_alloc(n); fill; *out = STRVAL(buf)` — audited by eye, zero pointers out). Attribution was MONITOR-DISCIPLINE mechanical, not guessed: gdb breakpoint on `rt_ws_alloc` with a 3200-hit spin counter mid-churn-loop sampled the true per-iteration allocators — which also proved **mode-3 routes DUPL/TRIM/REPLACE through `by_name_dispatch.c:4746-4772`, NOT core.c's registered `_DUPL_`→`string_builtins.c` path** (that path uses `rt_str_alloc`, already precise-collectable).

## 3. MEASURED RESULTS (SCRIP_GC_STRESS=3, SCRIP_ZETA_TELEM=1)
- **Pure-transient churn (2000 iters DUPL/TRIM/REPLACE, no retention): PLATEAU.** 629 blocks / 264KB held flat across 2,002 collects; `wsc-dead≈3` reclaimed per cycle; 223KB reuse window; immortal `ws=625` CONSTANT (startup census). Pre-split this class accumulated one immortal pin per allocation forever. **Frontier-drop restored for the collectable class — the rung's stated goal.**
- **Retention churn (800-iter table probe):** immortal share **ws 6234 → 3832 (−38%)**; `wsc=802` pinned = exactly the live table-retained REPLACE results (correct); dead builtin results recycle via the fill window (win 48→112); block count −20%.
- **The 4000-iter retention probe still exceeds a 120s wall under STRESS=3 — that is the HARNESS, not the split:** every 3rd alloc walks the whole heap while the program retains 3,689 live entries ⇒ O(n²) by construction. **Normal mode: 4,000 iterations in 42ms, ZERO collections** (1.6MB of 512MB) — the s88 "collections only at exhaustion" profile exactly.
- ⚠ **The remaining accumulator is named: VCELL/TBBLK-pair aggregate cells** (`rt_subscript_var` pattern_match.c:950 ≈ VCELL per subscript; `table_set_descr` aggregates.c:127-8 ≈ TBPAIR + key strdup per entry). These are POINTER-BEARING and `rt_gc_visit_descr` traces *through* them without marking their blocks (the malloc-era "aggregate cells do not move" assumption — DT_N slen==2 visits `vc->key_d/sv/cellp` but never marks the vc block itself). They MUST stay immortal until DT_N/aggregate precision work marks+adjusts the cells properly. **Named follow-up rung: AGG-PRECISE** (make DT_N/TBPAIR blocks first-class marked citizens; then they flip collectable and the last WS growth line flattens).

## 4. Gates — ALL AT WATERMARK, regressions bisect-excluded
crosscheck EXACT m3 303/4 · m4 293/13/1 · DIVERGE=10 IDENTICAL fail names (before and after every commit) · sno smokes 7/7×2 · icon 12/2+10/4 · prolog 3/2×2 · authored lines 200-col clean (verified in CHARACTERS via python against the pre-session base; two overages found and fixed, one a pre-existing-207 line my `_c` suffix nudged to 209) · **gc stress suite fails (204_gc_recursive_frames m3+m4, 212_gc_args_in_flight m4, 214_gc_exhaustion_live_set m3+m4) BISECT-PROVEN PRE-EXISTING** — git-stash A/B under identical hand invocation produced IDENTICAL fail sets on the pre-slice and post-slice binaries. Runtime-only diff (no emitter/templates/lower files) ⇒ `.s` regen n/a per the s89 precedent.

## Open
- **WS-CLASS WIDENING:** core.c's s38-certified strdup family (75 sites, "char bodies, ZERO pointers out" per the TR-3(b) ROOT+EDGE row) is the next flip population; then per-file audits of the remaining rt_ws sites against TR-5's census.
- **AGG-PRECISE** (above) — unlocks the VCELL/TBPAIR share, the dominant remaining WS growth.
- **Window-first re-try** (withdrawn s90; its motivation returns as the collectable share grows).
- ZH-RETIRE sweep (s89) · ruling (C) rc=134 · refs/icon-master gen-proc consult — all unchanged from s90.
