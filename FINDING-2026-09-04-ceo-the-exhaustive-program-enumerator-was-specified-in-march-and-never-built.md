# FINDING 2026-09-04 (ceo, QUARTET) — the exhaustive program enumerator was specified in March 2026 and never built; the pieces exist and one of them found 9 divergence classes

**LON'S ASK (2026-09-04, verbatim):** *"I also want to explore further the grammar-based random and exhaustive language generators, like expressions.py, and using Icon generators to begin automatically generating length 1, length 2, ..., length 10 character valid programs that just happen to have an output that can be measured and thus we test every single combination up to those character lengths. Scan the ARCH files and the repos for more on this which we had explored within the last 5 months."*

**MEASURED (an Explore sweep of `.github`, `SCRIP/scripts`, `SCRIP/tools`, `harness/`, `corpus/`, `IDEAS.txt`, and the three repos' logs since 2026-04-01; every path verified on disk):**

| what | where | date | state |
|---|---|---|---|
| `Expressions.py` — Lon's arithmetic grammar over `x y z` + literals: a weighted RANDOM generator **and** a mutually-recursive **yield-based ENUMERATOR** (`gen_item/gen_element/gen_factor/gen_term`, the Icon-generator idiom in Python), self-checking through its own `evaluate()`; also a SNOBOL4-pattern engine in Python | `harness/adapters/tiny/Expressions.py` (copy `snobol4python/misc/Expressions.py`) | 2026-03-11 | the exhaustive driver is switched OFF (`for i in range(0)`); `main()` prints 100 random expressions; graded by nothing |
| THE SPEC: *"a depth-bounded exhaustive enumerator: every syntactically valid program from 0 to N tokens (target N = 25) is compiled through all backends, run against the oracle, and on any divergence the Monitor drills to the exact first diverging trace event"*; candidate grammar (b) *"Icon generator expressions — E_TO/E_TO_BY/E_SUSPEND/E_ALT_GEN/E_ITER/E_LIMIT (6 kinds, Expressions.py already seeds this)"*; decision M-G8-DEPTH asks token-count vs IR depth and *"how many programs at N=10, 20, 25 — is the count tractable?"* | `.github/archive/MISC-REORG-GENTEST.md` (G-8) | 2026-03-29 | never implemented; the four `GEN_*` decision records it mandated (HOME, DEPTH, ORACLE, GRAMMAR) were never written |
| HARNESS-GEN: grammar-driven semantic generator for SNOBOL4 arithmetic with a synchronous SPITBOL-pipe oracle; variable pools `i j k l m n` int, `p q r` pattern (Phase 2), `s t` string | `harness/HARNESS-GEN.md` | 2026-03-31 | Phase 1 = Expressions.py; Phase 2 = the August fuzzer |
| Milestone M-G10-HARNESS-RANDOM: `harness/methods/random/` = *"grammar-driven generation, depth-N enumeration"*, acceptance *"Depth-3 SNOBOL4 exhaustive: zero divergence"* | `.github/archive/GRAND_MASTER_REORG_2.md` §G-11 | 2026-03-31 | never built; `harness/methods/` does not exist |
| `util_pattern_fuzz.py` — the only LIVE grammar generator: RANDOM SNOBOL4 patterns (13 leaves, cat/alt/ARBNO/FENCE/captures/defer), oracle-differential against `sbl`, verdicts AGREE/DIFF/FALSE-ACCEPT/SEGV/HANG deduplicated to one witness per canonical shape | `SCRIP/scripts/util_pattern_fuzz.py` | 2026-08-20 (s183) | not wired into any runner; **800 generated programs → 9 distinct divergence classes, including the campaign's only FALSE ACCEPT** (`GOAL-SCRIP-HQ.md:507`); the 168-combination hand grid found nothing; four s188/s189 FINDINGs reduced 11 SEGVs to 4 mechanisms, 2 aborts to 1, 6 hangs to 3 classes |
| sub-expression extractors (random sample / exhaustive over one file's RHS), the calculator input generator, the fuzz-witness predicate ladder P1–P4 | `corpus/tests/scrip_test/{beauty_subexpr_gen,subexpr_oracle}.py`, `corpus/demos/snobol4/calculator/calculator-gen.py`, `SCRIP/scripts/util_fuzz_witness_*.sh` | 08-29 / 08-30 | tooling around witnesses, not program generators |

**NOT FOUND:** any enumerator of length-N programs (tokens or characters), any `gen/`, `harness/methods/random/`, `corpus/generated/`; no sentence anywhere saying Icon generators enumerate programs by character length; `IDEAS.txt` (856 lines) has no entry on generation. The memory's likely origin is the March candidate grammar (b) plus Expressions.py's generator-style enumerator.

**WHAT THIS MEANS.** The idea is five months old, correctly specified, and its one executed fragment (the August fuzzer) paid for itself in a day. The exhaustive arm was never run because the tractability question in M-G8-DEPTH was never measured. The cure is a row, not a study: `exhaustive-program-enumerator-to-length-n-with-oracle-divergences-as-witnesses` (hq_T rank 0, seat16 under FLEET-16) builds `util_program_enumerator.py` on the Expressions.py grammar, measures the counts at N=1..10 in tokens AND characters as the first ledger line, grades every program against the oracle, and absorbs only DIVERGENCE classes into the master as witnesses — the fuzz precedent, not a wholesale dump. Point 9 of the test standard names it.
