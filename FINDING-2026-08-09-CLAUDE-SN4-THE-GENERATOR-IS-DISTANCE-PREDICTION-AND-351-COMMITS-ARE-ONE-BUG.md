# FINDING 2026-08-09 — THE GENERATOR: cross-extent slot addressing is emit-time DISTANCE PREDICTION; 351 of 641 commits are one bug; sessions are structurally forbidden from the cure

**MEASURED (SCRIP, since 2026-07-10, src/emitter+src/templates+zeta_storage.c):** 641 fix-commits; **351 (55%) name stack-address arithmetic** (offset/disp/depth/rsp/rbp/slot/alias/frame/carve/pop). Vocabulary in live code: **24 distinct `fc_*` displacement helpers**, **362 compensation-term occurrences** (`op_fc_disp/base/wbytes/bytes`, `op_zdepth`, `op_zread[]±adj`, `op_flat_disp`, `fpr_rsp`, `flat_frame_floor/bytes/seed`, `op_tail`), 48 raw `[rsp+N]` template reads, 1142 `FR/FRQ` reads behind the per-graph `x86_fb_pinned()` selector.

**THE THEOREM.** Per-BB **allocation** landed long ago (`sub rsp,K` at α; LIFO holds — the transit guarantee). What never landed is per-BB **addressing across dynamic extents**. Wherever ARBNO element chains, runtime-minted DEFER blobs, ALT retry, or match re-entry sit between a producer and a consumer, the consumer's distance to the cell is RUNTIME data — yet the architecture predicts it at emit time, per construct-pair, via the vocabulary above. Every family pairing therefore mints a fresh mismatch (BUG-2 pop-debt, BUG-3 PAIR(2) ε-slot, BUG-4 hfc save/restore split, BUG-7 return-address alias, CARVE-ERAD ALT alias, D06 [rsp+16]-vs-[rsp+128], N08/N09 outer-capture, s23's +112). **The probe matrix is the pairing enumeration; 351 commits are the same bug 351 times.**

**WHY SESSIONS CIRCLE.** Under minimal-diff + don't-regress, a session may only add compensation term #363 — locally correct, globally accretive. Deleting the prediction FAMILY (24 helpers + 362 terms) and replacing it with one mechanism forbids working code paths tree-wide: an **architecture-authority ruling no session may make.** This is the unsolvable-by-agents problem, delivered per Lon's order.

**RULING REQUESTED (one line each):**
1. **THE LAW — CARRY, NEVER PREDICT:** across any dynamic-extent boundary, a consumer reads `[PIN + k]` or through an address the producer PUBLISHED (one `lea` at its own known depth into a fixed home: rbp quad / element header / anchor). `[rsp+computed]` legal ONLY within one box's α..ω with no dynamic extent between — an IR-graph-checkable property; gate: emitter refuses cross-extent rsp arithmetic.
2. **THE PIN:** (a) rbp pinned at every dynamic-extent α (Lon's own "RBP frames at unknown-growth spots," promoted from few to ALL such boundaries — FENCE1's depth-immune quad generalized), or (b) published-pointer headers (the N08 lea generalized), or a per-extent-kind mix. Lon names it.
3. **THE DELETION LADDER:** fold the 24 `fc_*` helpers + 362 terms into the one mechanism, killswitch per slice, probe families as the walk — with explicit license that this ladder MAY transiently regress behind the switch: minimal-diff is SUSPENDED for it, being the discipline that manufactured the month.

**RULED (Lon, same day, verbatim): "Make the independent systems that calculate BE ONE SYSTEM."** Consolidation ladder = GOAL-SN4-ZETA-MECH §LADDER ONE-SYSTEM (addr_of authority in zeta_storage.c; RSP_STATIC/PIN/PUBLISHED modes; migrate-by-family, delete-same-slice). The ruling subsumes carry-vs-predict: one calculator cannot disagree with itself.
