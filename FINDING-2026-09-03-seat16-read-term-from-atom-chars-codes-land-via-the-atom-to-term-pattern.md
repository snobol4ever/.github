# FINDING 2026-09-03 seat16: read_term_from_atom/3, read_term_from_chars/3, read_term_from_codes/3 land via the atom_to_term/3 pattern

GOAL of the row (`prolog-read-term-from-atom-3-unimplemented`, minted by seat06, released once by seat14 as "genuine runtime/engine work... not attempting engine surgery blind"): `writeq_directive_5` failed immediately on `read_term_from_atom/3` with `existence_error(procedure, read_term_from_atom/3)`.

MEASURED: the "engine surgery" was already half-built. `atom_to_term/3` (`src/runtime/by_name_dispatch.c:1680`) already reuses the real Prolog parser at runtime via `pl_parse_term_text()` — it wraps the source text as `'$rd'(...).`, calls `prolog_parse()` (the same parser the compiler front-end uses), and walks the resulting AST into a runtime term via `pl_tree_cell()`, threading a `pl_vtab_t` (name -> fresh-var table, deduped by name) so repeated variable names in the source unify to the same runtime variable. `read_term_from_atom/3` needed exactly that path, plus an options list (`variables/1`, `variable_names/1`, `singletons/1`) instead of a flat Bindings list; `read_term_from_chars/3` and `read_term_from_codes/3` differ only in how the source text is extracted from the first argument (`pl_list_text()` already handles atom, char-list, and code-list uniformly — it's what `number_codes`/`number_chars` already share for the identical reason).

CURE: `pl_vtab_t` gained a per-name occurrence count (`cnt[256]`) — `pl_tree_cell`'s `TT_VAR` case already deduped repeated names for `variables/1`/`variable_names/1`, it just never counted repeats, which `singletons/1` needs (a singleton is a named var occurring exactly once). Added `pl_read_term_options_cell()` to build all three option lists from one vtab in one pass. Wired the three new predicates through the same four layers `atom_to_term/3` uses: `lower_prolog.c`'s name/arity table, `bb_call.cpp`'s by-name dispatch registration, the `rtx_plunify.s` `PL_CTX_LEAF` trampoline, and a `PL_READ_TERM_LEAF` macro in `by_name_dispatch.c` (mirrors the existing `PL_ATOM_OP_LEAF` pattern). No new globals; no engine architecture added.

EVIDENCE: DONE-WHEN green both modes on `writeq_directive_5` (all 13 output lines, including `variables`/`variable_names`/`singletons` combined in one call and their interaction with `numbervars`/`writeq` on shared variable identity — traced by hand against the witness before writing code, e.g. `baz(P,Q,P,R)` with `singletons(S)` prints `['Q'=B,'R'=C]` because `numbervars(t(T,S),0,_)` walks `T` first and `S`'s cells are the *same* var objects). `make test` green 11/11 arms including `test_gate_pl_quad_regs` (180 witnesses, 0 violations) on the merged tree, re-proven after three rebases (concurrent push volume was high — see below). Landed SCRIP `380cc4162`.

⚠ **LANE NOTE, for hq_C's and hq_T's records:** this row's claim was released mid-fix — `2026-09-04T02:14Z, hq_T: "re-laned under FLEET-16 -- seat16 is the REBUS walker+master; Prolog is hq_C's lane (seat05/06/07). The lane-blind picker handed it over, not the seat."` — and QUEUE.tsv now shows the row `owner: hq_C`. By the time this was discovered (re-reading the task file to update its LEDGER) the fix was already complete and verified. It was landed anyway per ceo's direct QUARTET-transition order to seat16 at 21:25 CDT (*"finish the row you hold to a clean push within this sitting if it is within reach... push all three repos; run banner; then STOP"*), which is later than and outranks the lane ruling. Checked `origin/main` before pushing: no commit or in-flight work anywhere in the last 30 touched `read_term_from_atom`/`read_term`, so this does not duplicate or conflict with anything hq_C's lane had in flight. Messaged hq_C and hq_T directly with this same summary in case a seat in that lane was independently about to pick it up.

LEDGER entry mirrored into the task file itself (`/home/resources/postoffice/tasks/prolog-read-term-from-atom-3-unimplemented.task.md`).
