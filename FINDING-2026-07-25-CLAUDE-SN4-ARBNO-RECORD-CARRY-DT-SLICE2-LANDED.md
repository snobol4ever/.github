# FINDING (2026-07-25, s155, Claude) — PS-3 SLICE 2 LANDED: the RECORD-CARRY DT arm (`ARBNO(*P)` susp-free) — uniformity gate + recursion barrier DISSOLVED; treebank's recursive `*group` admits and rails 1.43→1.35 same-session.

## THE GROUND TRUTH THAT RESHAPED THE DESIGN (read before extending this arm)
The s154 design sketch (`[rsp+8]→[rbp+56]` cursor mirror) presumed the exhaust needed the recorded base restored caller-side. The emitted epilogue says otherwise: **the pat-blob's own ω-half is ALREADY the absolute pointer restore** — `xa_flat.cpp` (ZC_FRAME_RSP ∧ flat_pat arm): γ retains the activation and pushes the 16B `{res-landing, rbp}` record at the deep frontier; **ω unwinds ABSOLUTELY, `lea rsp,[rbp+kt]`, rejoining pre-entry rsp at ANY arrival depth**. The callee self-frees; no caller-side size arithmetic ever existed on the real-exhaust path. The ONLY compile-time-SUSP consumers were therefore: (a) the DT arm's σ null-progress read of the element entry cursor at `[rsp + op_arbno_dt_susp]` — reaching OVER the variable-size blob to a header pushed below it at β; (b) the TAIL arm's ε phantom-pad share (`bb_match_defer.cpp` zero-guard `add rsp,SUSP`) plus the crossing `[rsp+const]` displacement registrations (`fct_leaf_range` pfx advance, window CONDs) that bake SUSP into match-time reads. (a) is the treebank lever and is what this slice removes; (b) is safe to fixed-pad ONLY for candidates with zero crossing displacement/window registrations (pad ruling A ratified: FIXED per-leaf placeholder, 16B = one record slot = the slow path's own `{L6}` exhaust-record size — deliberately DEFERRED, no working-set demo needs it this slice).

## THE MECHANISM (template + staging, both under `SCRIP_ARBNO_LATCH=1`; `SCRIP_ARBNO_PEEK=0` = s152 byte-verbatim)
`bb_match_arbno.cpp` new arm, selected by `op_arbno_dt_susp == 0` (>0 keeps the s152 form verbatim): the element entry cursor lives in the flat quad `FR(off+4)` while the element is IN FLIGHT (β reads it and leaves it untouched; exactly ONE element is in flight per activation; recursion-safe because FR is per-activation via the pinned rbp). At yield-accept, a 16B **CARRY RECORD** `{[rsp+0]=&L(4) glue, [rsp+8]=entry cursor}` is pushed ON TOP of the blob's own frontier record — a JUMPABLE record, so the universal retreat `jmp [rsp+0]` composes: L(4) restores the cursor into `FR(off+4)`, pops itself, falls through to the blob's record. PAIR(3) needs NO header pop (nothing sits below the blob anymore; self-release lands rsp exactly on the previous element's carry record, or the ARBNO frontier at elem0 — L(2) restores `FRQ(off+24)` as ever). σ/count bookkeeping mirrors s152 move-for-move. The SLOW `rt_defer` path composes untouched: its `{L6}` record is 16B and self-popping, so the carry stacks over it identically (canary T5 proves it). Staging (`emit.cpp` IR_MATCH_ARBNO drive): peek mode drops BOTH the `emit_patzeta_lookup` uniformity gate AND the `PAT$`-operand requirement (the recursive `*group` defer carries none — `SCRIP_DT_DIAG` showed `ok=1 pn=- pro=1`; the carry arm resolves the target through the s142 cell at match time, PAT$ identity never consulted). License remaining: single seal==2 defer body + inert LIT_STRING/GOTO wiring + prologue-bound name. Diag extended with `pk=`.

## PROOF SET (all this session, tree at 9f44e739+slice2)
NEW canary **183** (`crosscheck/patterns/183_pat_arbno_defer_recursive_carry.sno`, ref sbl-generated): recursive `G = "<" SPAN("ab") ARBNO(*G) ">"` (the treebank shape reduced), depth-12 nest, T4 overall-fail unwind through suspended recursive elements to the L2 restore, T5 STRING target (slow-path compose), T6 empty-first alternation (σ null-progress je path per element), T8 β-extend after a right neighbour consumed a group. **All four legs identical to the oracle: m3/m4 × armed/default; 7 carry arms in the armed `.s`, 0 default.** Treebank-match itself: the recursive `ARBNO(*group)` — the s153 decline class — **fires the carry arm (diag + `.s` grep=1) and is byte-identical to ref in BOTH mediums on the full VBGinTASA workload.** Crosscheck ×3 lanes (default / armed / POISON+armed): **m3 314/1 · m4 309/4 · DIVERGE=3** — the s153 watermark +1/mode from 183, failing sets identical (m3 test_case; m4 +214/215/216). Smokes 7/7 ×2. **179/180/181/182 arm-for-arm identical pk=0 ↔ pk=1** (TAIL counts 0/2/6/6 both; zero regression on proven canaries — note the s153 cursor's "181 DT fires" memory is stale: 181 is TAIL-armed in the s153 state too, pk=0 confirms). All 10 match demos m3-OFF == m3-ON == m4-ON == ref. `.s` regen ×3 zero drift; **default `.s` byte-identical to a pristine stash-rebuild** (structural: every change sits inside the `_dtl` latch gate + an unreachable template arm). s154 OWED items discharged: poison lane (above), regen ×3 (above), same-moment rail A/B (below).

## RAIL (same-session A/B, `bench_sno_rail.sh`, SO_A=out/libscrip_rt.so, RT_OPT=-O0, SCRIP= exported)
treebank-match: **OFF ratioA=1.43 (1100.6us/iter) → ON 1.35 (1017.6us/iter), ~7.5% wall, identity OK.** Honest decomposition: this is the DT-arm-only bite. Treebank's OTHER two ARBNOs stay chain BY CONSTRUCTION — the outer `ARBNO(ARBNO(*group) delim)` and group's interior `ARBNO(delim (*group|word))` have multi-node/ALT bodies (the granted-ALT rung), and PAT$_α keeps its ~26% (DB-2 α-diet rung). The cursor's "58% lever" is the xchain POT for the whole ladder, not slice 2's solo take; slice 2's structural win is that the RECURSION CLASS IS UNBLOCKED AND PROVEN — every later rung now lands on admitting shapes. Owed s154 fold A/B also run: calc-1 **FOLD ON 1.92 vs OFF 2.01** (~4.5% wall from the 60→30 trampoline cut), identity OK.

## DEFAULT-FLIP RULING
`SCRIP_ARBNO_LATCH` stays OPT-IN. Lon's s152 note ("opt-in this slice until monitor-proven") governs the family; the monitor 2way sync pass has not been run on the armed path. Flip = its own small rung: monitor pass + one-line default change + the same gate battery.

## FACT-RULE catches earned this session
(a) `str_replace` payloads with JSON-style `\"` land LITERAL backslashes in the file — the display layer's own escaping makes visual checks lie in BOTH directions, and a `grep '\\\\'` check is itself wrong (that pattern matches TWO backslashes). **The compile is the only trustworthy verdict; run it to completion.** (b) `timeout 110 make` + an existence check on the `scrip` binary = silently stale build TWICE: the code lives in `out/libscrip_rt.so` (the driver merely links it), the full .so build exceeds 110s, and the binary's mtime never changes. Verify by `strings out/libscrip_rt.so | grep <new-literal>` and by make rc, never by file existence. (c) `--compile` writes the `.s` to STDOUT (redirecting stdout to /dev/null while looking for a side-file = "no .s produced").

## NEXT (priority order, the cursor's ladder)
(1) **Granted-ALT defer arms** — teach `fc_alt_register` to admit DEFER arms (calc-2's 16% `rt_defer_open/close` + the json/treebank interior shapes; the carry mechanism is the resume protocol they'll ride). (2) TAIL-arm fixed-pad (ruling A) for windowed candidates with zero crossing displacements. (3) v2 fold pre-pass (hoist the γ/ω wire-resolution, emit.cpp ~1978–2088, so forward-wire consumers fold — the other 30 trampolines). (4) DB-2 α diet (PAT$_α 26–38%). (5) Latch default-flip after a monitor pass.
