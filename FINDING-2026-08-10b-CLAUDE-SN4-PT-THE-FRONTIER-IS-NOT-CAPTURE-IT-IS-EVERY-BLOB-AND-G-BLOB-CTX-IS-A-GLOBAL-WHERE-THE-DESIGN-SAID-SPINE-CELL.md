# FINDING 2026-08-10b (session 10, Claude Opus 5) — DEL-T1 diagnosis only, ZERO code touched

**Scope: MEASUREMENT. No `src/` edit, no regen, no push. Tree at SCRIP `ef8a3052` throughout; tripwire clean at open and close.**

---

## 0. WATERMARK HASHES IN THE s9 CURSOR DO NOT EXIST (fix your orientation before trusting anything below it)

The s9 cursor and its addendum name SCRIP `33062a20` (D-3), `5e66bfa6` (D-2), `9f1436b4` (D-1).
**All three are ABSENT from origin.** Commits with those exact MESSAGES are present under rebased hashes:

| rung | cursor claims | actually landed as | time |
|---|---|---|---|
| D-1 delete | `9f1436b4` | **`1af93e3a`** | 14:52:46 |
| D-2 pass-thru | `5e66bfa6` | **`1f96143c`** | 15:24:37 |
| D-3 killswitch unconfound | `33062a20` | **`ef8a3052`** | 15:28:18 |

Cause is the one s8 already recorded — *"grep the commit MESSAGE, not the hash"* — s9 wrote pre-rebase hashes into its cursor and the push rebased them over the parallel RTX-FUNC landing `799f2e76` + regen `930539c0`. Descent is linear and all the work is present.

**Second instance, different repo:** corpus is NOT at the claimed `0f02731d` (cursor: "untouched"). Origin corpus is `73271e2e`, an RTX-FUNC demo regen pushed at 15:08 by another seat, and its commit message cites SCRIP `5416ed56` — **also absent from SCRIP**. So the question "were those artifacts generated by a D-1-broken compiler?" **cannot be answered from the commit message** and is left OPEN. The 15:08 timestamp sits between D-1 (14:52) and D-2 (15:24), so it is a live concern, not a theoretical one.

⭐ **This is now a recurring class, not an accident. Recommend: cursors record `git log -1 --format=%s` alongside any hash, or the hash is written AFTER the push completes.**

## 1. BASELINE REPRODUCES EXACTLY IN A COLD CONTAINER

Probe suite, mode-3, at `ef8a3052`: **24 PASS / 12 FAIL** — identical to the s9 table's D-1+D-2 row, and the failing set is exactly s9's 5 pre-existing (`ab_freturn`, `ab_nret_lvalue`, `ab_redefine`, `z4_arbno`, `z4_span`) plus the 7 named casualties. Unlike broad-336 absolute counts, **this measurement transfers across containers.**

Two distinct modes, and s9's cursor does not separate them: **`124` = HANG (timeout)** for the five `w_cap_*`; **`139` = SIGSEGV** for `dc_recur` and `mv_arbno_callcap`.

**D-3 independently confirmed from cold:** neither `SCRIP_PAT_INLINE=0` nor `SCRIP_SCAN_OFF=1` moves ANY of the 7 (matrix run; all 8 cells unchanged). The retry blocks and the PT-1/2/3 ladder are exonerated a second time, by a different operator, on a different container.

## 2. ⛔⭐ THE FRONTIER IS NOT "CAPTURE + RECURSION" — IT IS EVERY PAT$ BLOB

s9 names the remaining defect *"interior `op_flat_disp` for capture/recursion families in blob context"* and routes the next session to `w_cap_stored` / `dc_recur`. **The capture half is wrong.** Four scratch witnesses, each pair semantically identical and differing ONLY in whether the pattern is written inline or stored (stored ⇒ `PAT$` blob):

| witness | capture? | PAT$ blob defs | `g_blob_ctx` refs | `mov rbp, rsp` | result |
|---|---|---|---|---|---|
| `A2_inline_nocap` | none | 0 | 0 | 0 | **PASS** `S` |
| `A_stored_nocap` | **none** | 18 | 12 | 0 | **HANG 124** |
| `C2_inline_nested` | none | 0 | 0 | 0 | **PASS** `S` |
| `C_stored_nested` | **none** | 9 | 6 | 0 | **SEGV 139** |

`A_stored_nocap` contains **no `.`, no `$`, no `@`** anywhere and still hangs. Correlation with capture: **absent.** Correlation with blob presence: **perfect, 4 of 4.**

Restated frontier: **any pattern routed through a `PAT$` blob on the pass-thru path fails. Non-nested ⇒ hang. Nested ⇒ SEGV.** Capture witnesses were merely the ones already promoted to `corpus/probe/`.

⭐ **Cheaper reproducers than the ones s9 recommends** (both 5 lines, both capture-free, `C` uses one recursion level and a 5-char subject vs `dc_recur`'s 3 levels and 14):

```
A_stored_nocap.sno              C_stored_nested.sno
  VOWEL = ANY('AEIOU')            ITEM = SPAN('0123456789') | *LIST
  PAT = VOWEL SPAN('BCD...YZ')    LIST = '(' ITEM ')'
  'GRAY' ? PAT        :F(NO)      '((7))' ? LIST        :F(NO)
  OUTPUT = 'S'        :(EN)       OUTPUT = 'S'          :(EN)
NO OUTPUT = 'F'                 NO OUTPUT = 'F'
EN                              EN
END                             END
```

## 3. ⛔⭐ ROOT CAUSE IS NAMED IN D-2'S OWN COMMENT — AND THE GATE DOES NOT HONOUR IT

`src/runtime/pattern_match.c:624`:

> `uint64_t g_blob_ctx[5] = {0,0,0,0,0};` … *"**NON-NESTING assumption**: T1 invariant blobs make no interior defer calls (TT_VAR members are snapshots; TT_DEFER is excluded from invariance), **so a single cell suffices**; variant blobs with nested defers remain the deferred class pending ZD admission (PT-4 PREREQ)."*

D-2 replaced a **per-activation** RBP frame with **one process-global 5-word cell**, sound only where activations never nest. The gate that arms it, `emit.cpp:2358`:

```c
int blob_act = (x86_zc_frame() == ZC_FRAME_RSP && g_emit.flat_pat) ? 1 : 0;
```

keys on **`flat_pat` alone** — not invariance, not nesting, not defer-freedom. **The cell's licensed scope and the emission's actual scope do not match.** `C_stored_nested` reaches the arm through `*LIST`, exactly the TT_DEFER case the comment says is excluded, and gets the single-cell protocol anyway → `mov rsp, ctx.base` (emit.cpp:2740) restores a base belonging to a different activation → SEGV.

**This is a deviation from THIS GOAL FILE'S OWN DESIGN OF RECORD.** PT-4 specifies *"α becomes PASS-THRU + a box-own linkage CELL **on the spine** ({γ, ω, δ0, scanflag} — law-1 'a box a few bytes of its own')"*. A spine cell is per-activation by construction. D-2 shipped a process-global instead. **The repair direction is already written in the file; D-2 departed from it.** The `.s` evidence says the departure bought exactly what Lon asked for — `mov rbp, rsp` = **0** in every blob, both media — so D-1's charter is discharged and only the replacement anchor is wrong.

**Manual concurrence (Ch.18 p.204, Ch.9 p.123):** the matcher is specified as a **pushdown stack** — step 3 pushes (alternative, cursor) per alternative, step 6 pops — and p.123 states recursive patterns consume that stack and can overflow it. `dc_recur`'s own header says *"same static capture nodes live at several depths at once."* One global 5-word cell cannot represent a stack of activations; the semantics require depth, and depth is what the pass-thru anchor dropped.

## 4. WHAT I DID NOT DO

No `src/` edit (PT-4 is deferred-BLOCKED behind MECH; the routing question is unanswered). No regen ×3 (NOT-CONCURRENCY-SAFE, Lon routes). No broad-336. No push. Scratch witnesses live in `/tmp/ptdiag/` and are **deliberately NOT promoted** — a known-fail `.ref` would move the probe watermark other seats gate on (s5 precedent); promote them inside the rung that fixes them.

`gdb` would not install (`apt-get` rc=100) so no instruction-level trace; none was needed — the sibling-pair discriminator settled it per RULES' cheapest-discriminating-experiment clause, before any code was read.

## 5. ROUTING (unchanged items carried, one sharpened)

1. **PT-4 here-vs-MECH** — unchanged and now more urgent: the repair is "spine cell per activation", which is MECH's stack-work zone by single-authority.
2. **regen ×3 window** — still owed and still Lon's; now ALSO entangled with the unresolved corpus-artifact provenance in §0.
3. **`claws5-match`** rc=0-wrong-output — needs the monitor once interiors move.
4. **NEW — retarget the next rung's witnesses** to §2's capture-free pair; `w_cap_*` are not the cheapest and their capture framing has been misleading the diagnosis for two sessions.
5. Carried, unowned: `bb_match_end.cpp` 12×r10 TIER-2; `128_pat_recursive_grammar`.
