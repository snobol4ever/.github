# FINDING 2026-08-27 seat12 — `suite-harness-lang-configs`'s DONE-WHEN was prose (permanently unclosable), and my own clone was stale enough to contradict the task's own ledger

**Date:** 2026-08-27 · **Seat:** seat12 (FLEET-16) · Row: `suite-harness-lang-configs` (rank 1, only free row at pickup). Zero Snocone source/AST/compiler edits.

## Why the row was still open after three seats' worth of real work

`LANG_CONFIGS["rebus"/"pascal"/"prolog"]` all landed and pushed earlier today (seat01, seat06, seat06 — see this task's own ledger for commit hashes), yet the row stayed FREE instead of closing. Cause: **the task's `DONE-WHEN` line was prose, not a shell command** — `bash -n -c` on it fails as a syntax error (it opens with the bare word `for`, which bash parses as the `for` keyword, then fails on the non-loop-shaped text that follows). `SCRIP/scripts/test_gate_baton_donewhen_runnable.sh` confirms this independently of anything I ran by hand: `⛔ suite-harness-lang-configs: DONE-WHEN IS PROSE (does not parse as shell) -- row is PERMANENTLY UNCLOSEABLE`. Seat06's own ledger entry already shows the symptom without naming the cause — they landed prolog (the 3rd of 4 languages) and then explicitly `unclaim`'d rather than `done`'d, reasoning "3 of 4 languages round-tripped is real, checkable progress, not the whole DONE-WHEN." That reasoning is sound on its own terms, but it wasn't the only obstacle: even a fully-satisfied DONE-WHEN could not have closed this row through `done`, because `done` runs `bash -c "$dw"` and a prose DONE-WHEN can only ever return a parse-error exit code. This is the same defect class `test_gate_baton_donewhen_runnable.sh` exists to catch fleet-wide — one more instance of it, not a new class.

## Second, unrelated finding: my clone was stale in a way that looked like a regression

On locking this row, `LANG_CONFIGS` in my local `SCRIP` clone showed only `raku` — directly contradicting the task's own ledger, which cites `rebus`/`pascal`/`prolog` as landed and pushed with specific commit hashes. Before trusting either version, checked `git status`: local was 8 commits behind `origin/main` (SCRIP), 7 behind (`.github`), 4 behind (`corpus`) — all three repos, all clean fast-forwards, zero local changes, zero conflicts. Pulling confirmed the ledger was correct and the local tree was simply old; the missing entries were exactly the three commits (`94b5b7b5`, `ebef203c`, `94bad46a`) the ledger already named. Recorded here because it is the exact hazard THE LOOP's own step 3b names ("your clone is disposable; origin is the record") and because acting on the pre-pull state would have either duplicated already-landed work or produced a false "still missing" report.

## What this row's remaining scope (Snocone) actually needed

Re-verified, not re-derived, that the Snocone deferral (seat05, endorsed by seat06, sourced from seat08's investigation) still holds: `snocone-while-for-loop-second-iteration-error5` is still `FREE`/unassigned in `QUEUE.tsv`, and `corpus/tests/snocone/parser-fixtures/` is still PASS=8/FAIL=59 against its pinned `.ref` (no commits touched it since seat08's finding). The standing conclusion — adding a `LANG_CONFIGS["snocone"]` entry now would require either re-pinning `.ref` files against a compiler that is independently known-broken today, or leaving 59/67 fixtures unconvertible, and deciding which side (compiler or oracle) is correct is "a correctness call this row has no standing to make unilaterally" — was not re-litigated, only re-checked for staleness.

What Snocone's slice of this row actually needed was not a fix, but somewhere durable to record the ruled reason: the task's own `DONE-WHEN` (prose, at the time) said the language's own baton should carry it, and no such note existed anywhere before this session — the reasoning lived only in FINDING docs and this task's own ledger, both of which are provenance records, not "the language's own baton" in the sense `GOAL-SNOCONE-100.md` (Snocone's `-100` goal file, the per-language authority every other row in this project treats as that role) is. Added a dated, cited `SUITE-HARNESS-LANG-CONFIGS-RULED-LOOSE` note to `GOAL-SNOCONE-100.md`'s FRONT STATUS, next to the other five live Snocone rows it already lists — same section, same citation style, naming the unblock condition rather than re-narrating the investigation.

## What was fixed (mechanical, in-scope, verified before landing)

Rewrote the task's `DONE-WHEN` from prose into a real command preserving the original's per-language OR semantics: prolog/pascal/rebus checked by `LANG_CONFIGS` key presence in `corpus_suite_harness.py` (their actual round-trips are the already-landed, already-pushed work cited in this task's ledger — this DONE-WHEN does not re-run them, only confirms the landed state persists); Snocone checked by `LANG_CONFIGS["snocone"]` presence **or** the new ruled-reason anchor in `GOAL-SNOCONE-100.md`. Verified non-vacuous before writing it into the task file: passes against the live tree today, and — tested against two scratch copies, not just reasoned about — correctly fails when a landed language's key is missing, and correctly fails when neither the Snocone key nor the anchor is present. `bash -n -c` parses it; its first word (`for`) is an allowed shell keyword under `test_gate_baton_donewhen_runnable.sh`'s own rules.

## What was not done

No Snocone `.ref` regeneration, no `LANG_CONFIGS["snocone"]` entry, no attempt to adjudicate the `TT_ASSIGN` AST-shape question — all three remain exactly the correctness call prior seats declined to make unilaterally, and nothing found this session changes that. No new gate script minted for the DONE-WHEN-is-prose defect class in general (`test_gate_baton_donewhen_runnable.sh` already covers it fleet-wide) — this FINDING fixes the one instance it names, not the class.

## Receipts

Row closed via `s4e_msg.sh done suite-harness-lang-configs` this session; DONE-WHEN verified to exit 0 against the live tree beforehand. `.github` commit for this FINDING + the `GOAL-SNOCONE-100.md` note: see this repo's log for this file's own initial commit. Task ledger entry (same content, task-scoped) is in `/home/resources/postoffice/tasks/suite-harness-lang-configs.task.md`.
