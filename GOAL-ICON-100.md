# ⛔⭐⭐⭐ GOAL-ICON-100 — SCRIP ICON x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

**Opened 2026-08-15 s229 by Lon in-chat (Fable 5 seat), verbatim in substance:** *"We've been at this for 4.5 months and today is the day … Take a full accounting of every single Icon-related GOAL-*.md file. ONE GOAL file to bring us all the way home to 100% Icon Programming Language. So much of those RUNGS and STEPS are completely obsolete. We basically have one plan moving forward for SCRIP's Icon x86/x64: the THREE ZETAS — standing, activation frames, and spine cells. After the consolidation, delete those MD files which have been replaced by the new ONE GOAL. For every item verify against the source to see if the RUNG or TASK even makes sense. Mimic exactly what we did for SNOBOL4."*

**This file REPLACES (deleted this session, full text in `.github` git history at `835f4131` and before):** `GOAL-ICON-BB.md` · `GOAL-ICN-ZETA-CELLS.md` · `GOAL-ICN-ZFRAME-RESTORE.md` · `GOAL-ICN-GVA-M3.md` · `GOAL-ICN-GLOBAL-NV.md` · `GOAL-ICN-BROKER.md` · `GOAL-ICON-RTX.md` · `GOAL-ICON-BB-NATIVE.md` · `GOAL-ICON-FULL-PASS.md` · `GOAL-ICON-GATE-WIDEN.md` · `GOAL-ICON-IR-RUN.md` · `GOAL-ICON-LOWER-REDESIGN.md` · `GOAL-LANG-ICON.md` · `GOAL-PARSER-ICON.md` · `GOAL-PST-ICON.md` — plus the absorbed-era satellite notes `ICON-BB-{NOTE-2026-06-27-KEY-GEN-AND-LVALUE-GEN,PUNCH-LIST-2026-06-24,THREE-PILES-2026-06-27,HANDOFF-2026-06-27b}.md` and `ICON-AUDIT-2026-06-24.md`. Any reference to one of those names, anywhere in `.github`/SCRIP/corpus, resolves HERE. Their laws that still bind are restated below; their rungs are dispositioned in the LEDGER at the bottom (verified against SCRIP `07d6eae7`, corpus HEAD, live tree + fresh triple suite run, 2026-08-15). **NOT absorbed (architecture, contracts, platform, other languages — untouched):** `ARCH-ICON.md` · `ARCH-ICON-RTX.md` · `ARCH-ZETA-LOCAL-STORAGE.md` · `DESIGN-ICON-SUSPEND.md` · `STUDY-JCON-ICON-CONTROL-FLOW-*.md` · `PROC-ICON-BENCH-ASM.md` · `RTX-CLAIMS.md` · `GOAL-RTCC.md` · every SNOBOL4/Prolog/Snocone/Raku/Pascal/Rebus goal.

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔
**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. ██** Enforcement: every diff is checked for new file-scope definitions; a commit adding one without a cited in-chat grant is REJECTED on sight (precedent: g_pcall/g_pcall_wires/RT_AB_ANCHOR eradication, s55). **THE ASK ITSELF MUST BE A BANNER** in chat (name, type, owning file, purpose, why registers/stack cannot carry it).

---

## ⭐⭐⭐ THE MODEL — THE THREE ZETAS FOR ICON (design of record; tier glossary = `ARCH-SCRIP.md`; Lon's phrase, this charter: *"standing, activation frames, and spine cells"*; Lon confirms/overrides the Icon tier boundary at any session start)

| tier | motion | Icon embodiment | at HEAD `07d6eae7` (VERIFIED by grep + run this session) |
|---|---|---|---|
| **ζ-STANDING** | never moves for the life of its activation/process | (a) the per-graph activation base: wire header `[base+kt-24]`=γ `[kt-16]`=ω `[kt-8]`=caller-rbp + params/locals ×16B (`zframe_graph` regime ICN-FR-2/3, `flat_lcl_proc` s211) — established once at α, pinned; (b) process-lifetime storage: GVA globals OFF-STACK (`gva_register` cells; ZK-5 charter: globals are NEVER ζ-spine cells), statics, coexpr pthread stacks | `flat_lcl_proc` emit.h:617 · pin predicate `emit_jmp_pin_legacy()` emit.h:640 (deep_arrival ∥ gen ∥ lcl_proc ∥ zframe_graph ∥ patpin-probe·pat) · GVA gates `test_gate_icn_{zk5,zcells}_gva.sh` present |
| **ζ-ACTIVATION FRAME** | framed, comes and goes | suspended-generator activations (γ-retain at the deep frontier; per-activation pending state on the `g_icn_gen_stk` LIFO keyed by generator base — the s18 one-slot fix), scan environments (&subject/&pos save-restore), deep arrivals; chained off STANDING, self-pop at ω | generators-on-spine per R-ICN-B (s89 LIFO ruling; NO pthread-per-generator — pthreads are coexpr ONLY, R-ICN-C) |
| **ζ-SPINE** | slides | per-BB FORTH cells on RSP: α grants `sub rsp,K`, zero intermediate pops, ONE terminal release (`zgpop`); the cells arm (`icn_cells_graph`, ZK-1..5 LANDED) covers LVA locals/params/return + the det value family | ZD one-authority lines: `zd_wl_kind` emit.cpp:2087 · ZK-2/ZK-5 arms verified live at :2103 (locals-on-pinned + globals-decline-to-GVA) · census `SCRIP_ZD_CENSUS=1` |

**Register contract of record:** RSP = FORTH spine top · RBP = the pinned STANDING/FRAME base per `emit_jmp_pin_legacy()` (depth-static graphs stay rsp + `op_flat_disp`) · **R13/R14/R15 = RESERVED Σ/δ/Δ for the BB_SCAN family — no other Icon use, ever** · **R12 = FREE (Lon ruling 2026-08-07)** — first claimant documents its assignment here and in the gate scripts (supersedes the old no-stack gate's r12-as-TOS ban for Icon storage arms) · RBX = heap bump-frontier (dormant under ZC_PORT_FORTH) · RCX/RDX = γ/ω wires at proc enter, args via `g_call_args[]` · RAX scratch. **TWO STORAGE ARMS, BOTH KEPT until Lon picks a default** (the ARCH-ICON dual-embodiment precedent): default = zframe routing; `SCRIP_ICN_CELLS=1` = cells arm; one graph NEVER in both (R-ZK-A mutual exclusion at the `lower_icon.c` stamp site).

## ⛔ DEFINITION OF DONE — "100%" MEANS THIS AND NOTHING LESS
1. **`scripts/scorecard_icon.sh` META SCORE = 100.0** — which requires `test_icon_all_rungs.sh` = 293/0/0 on the default arm, m3 — every FAIL and every `.xfail` marker retired; oracle = Arizona `icont -s prog.icn -x` / pinned `.expected` (⛔ NEVER Java/JVM — Lon 2026-07-21; JCON real formatting where the rung36 corpus is JCON's).
2. **m3 ≡ m4:** the mode-4 runner (R-6, FR-6d recipe) lands with the SAME 293 denominator and reads 293/0/0; crash-aware grading (the stdout-only harness blind spot closed).
3. **Benchmarks honest-green:** `scripts/honest_icon_bench.sh` (oracle-diffing, never `test_icon_bench_corpus.sh`) — geddump/tgrlink/rsg divergences closed, whole-output, not 30-line heads.
4. **THE THREE ZETAS complete:** ZK-6 suspension decision measured + ZK-7 generator family walked + ZK-8 100% assertion gate green on the cells arm; **Lon picks the default (frames vs cells)** from a side-by-side decision memo; loser's numbers recorded, killswitches folded (R-7).
5. Gates strict and green: `test_gate_icn_{no_stack,one_reg_frame,semicolon_required,rbp_census_ratchet,zk5_gva,zcells_gva,local_no_nv,global_no_nv_m3,scan,var}.sh` + TEMPLATE-ONLY/BOTH-MEDIUM greps == 0 + SN4/Prolog watermark invariance (R-ICN-D) + `handoff_status.sh` COMPLETE.

## THE INSTRUMENT (one; boards are RUN, never transcribed; re-derive every count fresh — never from prose)
```
bash scripts/scorecard_icon.sh run  [--suites a,b] [--out DIR]   # ~15 min; the periodic progress reading Lon asks for
bash scripts/scorecard_icon.sh report [DIR]                      # META = 100·Σ(w·pass/total)/Σw over suites present
SCRIP_ICN_CELLS=1 / SCRIP_ICN_ZFRAME=0 bash scripts/test_icon_all_rungs.sh   # per-arm spot reads
SCRIP_ZD_CENSUS=1 / SCRIP_ZD_DIAG=1                              # arming census / planner-decline discriminator
```
Weights (Lon's knob, ONE authority in the script header): rungs_m3 25 · rungs_m4 20 · rungs_m3_cells 10 · bench_correct 15 · smoke 10 · crosscheck 5 · gates 10 (Σ95). XFAIL counts in the denominator (end state 293/0/0). Bench graded ONLY by `honest_icon_correctness.sh` IDENTICAL verdicts vs live iconx (build the oracle once per session; the rc/non-empty grader `test_icon_bench_corpus.sh` is ⛔ superseded — the s164 rsg lesson).
**SUITE ACCOUNTING (every Icon test asset, dispositioned s229b):** SCORED: `test_icon_all_rungs.sh` (m3, 293, ×2 arms) · `test_icon_x64_all_rungs.sh` (m4 ladder, 293 graded + 10 no-.expected SKIPs) · `honest_icon_correctness.sh` (8 benchmarks) + `honest_icon_bench.sh` (timing companion, never scored alone) · `test_smoke_icon.sh` (14×2 modes) · `test_crosscheck_icon.sh` (4 consistency) · the 10 `test_gate_icn_*.sh`. SUBSUMED (not scored): 36 per-rung `test_icon_ir_rung_*.sh` slices + `test_icon_mode4_rung.sh` (per-rung m4) + `test_icon_bench_rung36.sh` + `test_icon_x86_runner.sh` (helpers) · `icon_bb_probes.sh` (sourced library) · `util_icon_bb_progress.sh` · `update_icon_bench_asm.sh` (artifact regen). BROKEN-INFRA → R-6 debt: `test_corpus_icon_parser.sh` (its standalone icon_parser binary no longer compiles). STALE/DELETED-ERA: `test_icon_rung_suite.sh` + `test_icon_arith_unary_mode2.sh` (m2) · `test_icon_jvm_runner.sh` (⛔ JVM path forbidden). SOURCES: `corpus/programs/icon/` (~1200 files: 293 graded, 30 `.xfail`, plus `.expected`/repro/coverage/demo/parser subdirs) · `corpus/benchmarks/icon/` (20 `.icn`; 8 oracle-graded, rest link-libs/no-oracle → R-3(h)) · `SCRIP/test/icn_global_nv_probe.icn` · jcon-master `gtest/` (graphics, out of scope — needs X).
Bug-finding law: **MONITOR-FIRST** — for Icon: cheapest discriminating experiment first (passing sibling vs failing probe, emitted-asm diff), then `CSN_NO_SEGV_HANDLER=1` + gdb spin/ignore-count breakpoints (HW watchpoints don't work in this container). Bisect predicates PROVEN discriminating at BOTH ends before the first probe (the s203 XFAIL-grep conviction). Ir (callgrind) is the honest perf metric; wall-clock only on tgrlink/geddump; label every number with RT_OPT.

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY (`x86(...)` sole producer; missing encoder ⇒ add it in `x86_asm.h`) · ONE-AUTHORITY constants (K/nops/admission change ONE line — `zd_wl_kind`/`zd_k`/`zd_nops`/staging choke; line numbers drift daily, re-grep) · PEERS RULE (no BB_t fields; `ir_operand_push`) · NO-LANGUAGE-IDENTITY past LOWER (behavioral names only; `icn_cells_graph`/`zframe_graph`/`flat_lcl_proc` name WHAT they decide) · ZERO C BYRD BOXES (`icn_bb_dcg` sole exemption) · **ICON SEMICOLON-REQUIRED, ZERO newline processing** (prison gate, 3 locks) · CONSULT CANONICAL SOURCES every session (`refs/jcon-master/tran/irgen.icn` 43 `ir_a_*` four-port topologies + `refs/icon-master/src/runtime/*.r`; `refs/` is NOT in a fresh clone — symlink Lon's archives, verify `ls` before trusting) · killswitch per behavioral family, `=0` byte-identity is a completion criterion · **R-ICN-D: SN4 (+ Prolog when touched) watermark invariance on EVERY shared-emitter commit** · struct fields APPENDED AT END (s141 ABI law) · REGENS ×3 + `update_icon_bench_asm.sh` (DEMO+BENCHMARK only) when codegen touched · O2-DIRECTED-ONLY · timeouts 8s/30s · `git pull --rebase` before every push, re-prove watermark after rebase · push mid-session per rung; credential = ask Lon in chat and wait · LIVE CURSOR moves every handoff · "HANDOFF COMPLETE" only from `handoff_status.sh` verbatim stdout.

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config user.name LCherryholmes && git config user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh; make libscrip_rt && make scrip   # header edits are NOT dep-tracked: rm -rf out/rt_pic first
mkdir -p refs   # symlink unzipped icon-master + jcon-master (watch double-nesting); ls refs/jcon-master/tran/irgen.icn before trusting
# Oracle once: cd /path/to/icon-master && make Configure name=linux && make → bin/icont bin/iconx
printf 'procedure main(); write("hello"); end\n' > /tmp/h.icn && ./scrip --run /tmp/h.icn; echo rc=$?   # the canary, all three arms
bash scripts/test_icon_all_rungs.sh 2>/dev/null | tail -1   # fresh watermark FIRST
```

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s229 (Claude Fable 5) — **CONSOLIDATION DONE (15 goal files + 5 satellite notes → this one, every rung dispositioned against source). ICON'S DEFAULT ARM IS DEAD AT HEAD — MEASURED, NOT INHERITED: THE FOURTH RECURRENCE OF THE "ICON WAS NOT IN THE LEDGER" CLASS.**
- **FRESH TRIPLE WATERMARK at SCRIP `07d6eae7` (this session, full suite ×3):** default **1/262/30** · `SCRIP_ICN_CELLS=1` **235/28/30** · `SCRIP_ICN_ZFRAME=0` **78/185/30**. Canary: default `write("hello")` **SEGV rc=139**; CELLS=1 and ZFRAME=0 both print `hello` rc=0. So the break is in the DEFAULT ZFRAME ROUTING only; both alternate arms are alive and the cells arm is the best live configuration at HEAD.
- **The collapse window is `8487d499` (2026-08-09, ZFRAME s18, measured 248/15/30, ON ORIGIN) → `07d6eae7` — 295 commits, essentially the SNOBOL4-100 R-3/R-4 shared-emitter campaign** (mrbp, activation frames, DEL-T1 flat_pat pin removal, glue CLASS work). Predicate for R-0 is ALREADY PROVEN AT BOTH ENDS: good end = s18's recorded+pushed 248 at `8487d499`; bad end = rc=139 measured here.
- **s229b — THE SCORECARD IS MINTED AND THE BASELINE IS READ: META 19.1** at SCRIP `07d6eae7` (`scripts/scorecard_icon.sh`, SCRIP `96ed8a5b`; run dir `test-results/scorecard-icon-20260815-205112`): rungs_m3 1/293 · rungs_m3_cells 235/293 · rungs_m4 0/293 · bench_correct 0/8 (all EMITFAIL) · smoke 0/28 · crosscheck 4/4 · gates 5/10 (red five — zcells_gva, local_no_nv, global_no_nv_m3, scan, var — all run programs on the dead default arm). Falsifiability proven: injecting 290/293 into a results copy moves META 19.1→45.0. iconx oracle builds clean in-session (v9.5.25a, `/home/claude/icon-build/bin`). PREDICTION for R-0: rungs_m3+m4+bench+smoke+the five red gates are all keyed on the same default-arm corpse, so the R-0 fix alone should move META ~19 → ~75-85; report the scorecard after every rung — this is the periodic progress reading Lon asked for.
- Prior best ever: anchor `8d0665c8` **252/11/30** (die-list of 11 = the pre-existing jcon FZ clusters, R-3's inventory). Prior arm bests: cells 241/22/30 (s228 at `bd043759`), zframe 248/15/30 (s18).
- Verified DONE since the old cursors were written (do not re-derive): ICN-PROC-FRAME-2 wire-exit (= ICN-FR-3 `76287768`) · ZD-2h locals widening ⛔ RELEASED AND LANDED (emit.cpp:2103, ZK-2/ZK-5 arms) · IR_CALL_BUILTIN_ICON ZD arm (`6967f531`) · ICN-FB-0 census instrument (`test_gate_icn_rbp_census_ratchet.sh`) · the Icon excise gate is GONE (`graph_native_emittable*` is Raku-only, scrip.c:882/1593) · `--dump-sm` no longer exists in the driver (RULES' "ICON SM = ZERO OPCODES" gate line is vestigial — R-7 asks Lon to retire it).
- OWED: R-0 · the R-1 FACT-RULE ask to Lon (fourth recurrence) · push credential (asked in chat at session end).

---

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity where applicable, triple-arm watermark + SN4 invariance, cursor move, push mid-session

- [ ] **R-0 · RESURRECT THE DEFAULT ARM (bisect `8487d499..07d6eae7`).** Predicate: `./scrip --run /tmp/h.icn` prints `hello` && rc==0 — proven discriminating at both ends (above). ~295 commits ≈ 9 probes × ~25s build (`rm -rf out/rt_pic` on header-touching probes). Candidate first-looks (from the range's own commit prose, VERIFY not assume): DEL-T1 D-2 flat_pat pin removal + g_zctx CLASS-D glue reads (emit.h:640's own comment names the revert one-liner) · `bb_glue_outer_γ/ω` CLASS-O work (`76c0e95f`) · mrbp MATCH-frame arms (should be SN4-gated — if one leaks into zframe routing, that IS the bug). May be MULTIPLE events (the s206/s207 precedent). Fix at the land mine; floor = 248/15/30 re-proven; cells + ZFRAME=0 arms must not move (they are the controls).
- [ ] **R-1 · THE PROCESS RUNG — STOP THE BLEEDING (fourth recurrence: s203 ZW-1 −37 · s207 1/293 · post-s211 drift · now s229 1/293).** RULES.md's own test for promoting a lesson to a FACT RULE is met. Draft for Lon's signature: **"ICON TRIPLE-ARM CANARY REQUIRED ON EVERY `src/emitter/` + `src/templates/` + `x86_asm.h` COMMIT"** — the 8-second `write("hello")` canary ×3 arms minimum, full suite on Icon-touching rungs. ⛔ CHAT-ESCALATION: put the ask at the TOP of a reply; this file only drafts it. Also propose the sibling: cross-goal watermark table in `handoff_status.sh`.
- [ ] **R-2 · ANCHOR PARITY (ex ICN-FR-5/6).** Target **PASS ≥ 252 with FAIL ⊆ the anchor die-list** (`rung36_jcon_args endetab fncs1 kwds mathfunc mindfa scan scan1 scan2 subjpos var`). The 5 in-scope generator/proc fails from s18: `recogn` (recursive suspend, MONITOR-FIRST on `proc_s_α`) · `level` (rsp-drift-per-β, distinct mechanism) · `prepro` (&features) · `proc_lookup` (DC-stub double-execute) · `rung03_suspend_return`. Then land the gates: (a) `test_gate_icn_zframe.sh` (repro quartet f0/f1/gen/fib both modes + `SCRIP_ICN_ZFRAME=0` identity + suite floor DESCENDING RATCHET, never a zero-assert); (b) `test_icon_m4_all_rungs.sh` (FR-6d recipe: copy the m3 runner, SAME 293 denominator).
- [ ] **R-3 · FAIL-ZERO — one failure CLASS per commit, witness minted, oracle-verified (re-measure every item first; these carry from the verified inventories).** (a) the FZ-E scan family root: emitter wires SCAN_MATCH fail-edge to arm-B β not α; 2-line repro `procedure f(s); if s ? (="'") then return "THEN" else return "ELSE"; end` → iconx ELSE, SCRIP fails the procedure (7 falsified hypotheses recorded in git history at `835f4131` — do not re-try); (b) FZ-B1 `variable(...)` lvalue — needs Lon's name-table ruling (sealed RO blob vs startup table); (c) case-arm-leading disjunction selects its LAST arm (repro `corpus/programs/icon/repro/rsg_case_arm_disjunction.icn`; start in emitter dj ω-edge, NOT lower_icon.c); (d) `&pos` garbage read 4287849 (r14 never established at entry; fix = once-at-entry both-medium site, NOT the read, NOT xa_flat's shared prologue); (e) mathfunc residual = BINOP_CONCAT coercion carried in IR by LOWER (10 read sites — full-budget, BID-AT-LOWER); (f) ⛔ LIVE SILENT-CORRUPTION BUG: `rtx_icnagg.S`/`rtx_icnsub.S` still carry pre-renumber `DT_DATA=100` (live 0x70) — the stale-asm-tag class, three-for-three; fix + propose the single-source `.inc` header gate; (g) FZ-C SEGVs (endetab/fncs1/scan2) + FZ-D parse errors (htprep/prepro) + FZ-E wrong-output remainder (args/coerce ⚠241MB-use-head/mffsol/mindfa/kwds/scan1/string) — MONITOR-FIRST each; (h) benchmark residue: link-library resolution (BENCH-1) + deal/rsg seed-pin oracles; `options/post/shuffle` are LINK-DEP LIBRARIES with no main — grade accordingly.
- [ ] **R-4 · XFAIL-ZERO — 30 markers (count re-verified this session).** Per marker: remove, run, fix-or-delete. Known clusters: bignum (radix/lgint/arith/checkfpx/cxprimes — arbitrary-precision ints, own sub-rung, Lon scopes it) · `ck` generator-in-arg to Image() · `profsum` `next` inside scan-body doesn't restart enclosing while · ~24 empty markers to classify on a fresh run (some pass free — cxprimes already ran 25/25 under s18's fix).
- [ ] **R-5 · THE THREE ZETAS COMPLETION (ex ZK-6/7/8 + ICN-FB-1..5).** (a) **ZK-6 THE SUSPENSION DECISION** — Option T (pthread coexpr machinery as implicit coexprs) vs Option P (pending cells on the ONE spine, the ARB-LON-K16 mechanics + re-homing/composition laws A-B; canonical precedent Arizona `interp.r` efp/gfp mark/esusp — READ IT FIRST). Witness set: nested every-over-to, disjunction resumed across depth, recursive generator, suspend-in-arg-list; measure Ir + peak RSS + suite delta both media; DECIDE, record the loser under the graveyard. Lean P (LIFO ruling + iconx precedent + zero thread overhead) — the measurement rules, not the lean. (b) **ZK-7 WALK THE GENERATOR FAMILY** with the winner: `IR_TO_BY · IR_DISJUNCTION · IR_REPALT · IR_PROC_GEN · every/limit/bang · scan control ? and the ICN-SCAN family` (position-returners vs cursor-movers per ARCH-ICON; R13/R14/R15 discipline is the sharpest edge). One rung-let per kind, each citing its `ir_a_*` topology; land the topology table ONCE as `.github/ICON-IRGEN-TOPOLOGY.md` (this subsumes ICN-FB-1's arrival-depth classifier — STATIC/DYNAMIC verdict per kind feeds both arms). (c) **GVA-M3 residue:** default-arm m3 globals off NV via the ZK-5 pattern (heap arena + `gva_register` — the runtime already accepts a caller array; NO new global without the banner ask); gate exists (`test_gate_icn_global_no_nv_m3.sh`). (d) **ZK-8 100% ASSERTION** — `test_gate_icn_zcells.sh`: whole-graph carve 0 · .bss backtrack arenas 0 · heap-ζ for generators 0 (coexpr exempt) · locals/params = cells · globals = GVA · R13/14/15 grep 0 · `=0` byte-identity · suite floor ratchet. Then the **DEFAULT DECISION MEMO for Lon** (frames vs cells, numbers side by side).
- [ ] **R-6 · m3 ≡ m4 + INSTRUMENT DEBTS.** The m4 runner (R-2b) at parity; crash-aware CLEAN/DIRTY split replaces stdout-only grading; `.s` regen freshness discipline (the seeded>0 staleness diagnostic); sweep the COMPILER never the artifacts.
- [ ] **R-7 · DELETIONS + KILLSWITCH FOLD (last; byte-inert by construction).** After Lon's default pick: fold `SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME`/the loser's arms, one family per commit, byte-identical OFF→deleted; retire `ZC_FRAME`'s dead RBP arms (Lon's s202 call: evidence favors deletion — 17 arms in 6 files written for R12, measured 9-net-crash trap, `#error` guard landed) · ask Lon to retire RULES' vestigial `--dump-sm` gate line + the old r12-as-TOS wording superseded by the 2026-08-07 R12 release · retire `GOAL-ICN-GVA-M3`'s `g_gva_rbx_base` design formally (violates the no-new-globals FACT RULE and the RBX contract) · keep ONLY print-and-never-change-bytes diagnostics.

**PARKED — PERFORMANCE / ARCHITECTURE, AFTER 100% (history in git at `835f4131`; do not open before DoD 1-4 green unless Lon directs):** the ICON-RTX ladder open queue (RTX-21/17/22/4/5/6c/7/10/13/14/25/26-remainder-ICN + Phase-2 RTX-11/12; landed arms + REFUSED/DO-NOT-PORT rulings preserved in `RTX-CLAIMS.md` + git; RTX-23/25 remain ⛔ BLOCKED ON LON) · GN-PERF slot-vs-nv A/B (superseded in practice: GVA won; NV remains the reflective/cross-language binding via `NV_bind_gva`) · `-O2` runtime pass (O2-DIRECTED-ONLY) · JCON-compiler-modules byte-compare (data-level, never JVM).

---

## LEDGER — EVERY ICON GOAL FILE, EVERY RUNG, DISPOSITIONED (verified against SCRIP `07d6eae7` + fresh triple suite, 2026-08-15; legend: **DONE** verified in tree · **→R-n** absorbed · **OBS** obsolete/superseded · **PARK** perf-after-100% · **INH** inherited claim, re-measure before spending)

**GOAL-ICON-BB.md (799L, the suite owner, s160s–s211):** FAIL-ZERO/XFAIL-ZERO inventories → **R-3/R-4** (each item re-measure-first); ICN-PROC-FRAME + PROC-FRAME-2 wire exit → **DONE** (flat_lcl_proc emit.h:617 + ICN-FR-3 `76287768`); ICN-FB-0 census instrument → **DONE** (`test_gate_icn_rbp_census_ratchet.sh`); ICN-FB-1..5 arrival-depth classifier + RSP-only data spine → **→R-5(b)** (the topology table is the shared deliverable); ICN-CARVE-0..3 → already ABSORBED by ZETA-CELLS in-file (ZK-0/1/2/8), now **→R-5**; ZD-2h widening "⛔ BLOCKED ON LON" → **DONE** (released s210/s211, landed with ZK-2/ZK-5 arms, verified emit.cpp:2103); IR_CALL_BUILTIN_ICON arm → **DONE** (`6967f531`); s203 residual-7 bisect + SCRIP_BB_ALLOC default call → **OBS** (three collapse/restore cycles later; R-0 supersedes); ZR-RSPRBP/FLATDISP history, prefix-arming UNSOUND, ZD_TOTAL-inert, capability-registry reading, `zd_stub_ok` tension → laws + graveyard (git); perf graveyard (tolower guard +1.4% worse · always_inline wash · asm rt_subscript_var 3% slower · value-subscript fusion no-win · pointer-keyed memo UNSOUND · goto-past-chain UNSOUND · 288-arm first-char-guard trap · one-caller-generates-all check) → **PARK preamble, binding** (do not re-derive); anycmp mixed-type ordering gap → **→R-3** ruling line (deliberately unfixed, moves green output); MEASUREMENT PROTOCOL → THE INSTRUMENT; permanent notes (oracle icont/iconx NEVER Java · JCON real formatting law · harness stdout-only blind spot · 241MB coerce head-cap) → laws/R-3/R-6.
**GOAL-ICN-ZETA-CELLS.md (s228):** ZK-1/2/3/4/5 → **DONE** (in-file checkboxes + zd arms verified live; cells arm measured 235/28/30 this session); ZK-6/7/8 → **→R-5(a)(b)(d)**; register contract + R-ZK-A routing + concurrency armor → THE MODEL/laws; blocker histogram + census interpretation → R-5 instruments; the sub-rbp-carve open measurement + `main_γ` fallthrough observation → **INH**, fold into R-0/R-5 first looks.
**GOAL-ICN-ZFRAME-RESTORE.md (s18):** FR-0/1/2/3/4 → **DONE** (anchor verified, regime landed `bcf05d33`, wire exit `76287768`, generators-on-spine + one-slot→`g_icn_gen_stk` fix `8487d499`); FR-5 → **→R-2** (5 in-scope fails listed); FR-6a zframe gate + FR-6d m4 runner → **→R-2**; FR-6c watermark FACT RULE draft → **→R-1** (now fourth recurrence); FR-7 doc sync → **DONE by this consolidation**; anchor `8d0665c8` 252/11/30 + die-list + phantom-`030d6263` warning → LIVE CURSOR/R-2; ERA MECHANICS + rulings R-ICN-A..F → THE MODEL; stale-asm-tag DT_DATA live bug → **→R-3(f)**.
**GOAL-ICN-GVA-M3.md (2026-06-24):** M3-ARENA-1..4 → **OBS as designed** (the `g_gva_rbx_base` NEW GLOBAL + rbx-base addressing violate the 2026-08-13 FACT RULE and the live RBX=heap-frontier contract; ZK-5 landed GVA off-stack for the cells arm with its own gates); the still-real m3-globals-off-NV residue → **→R-5(c)** using the file's own correct observation (`gva_register` accepts a caller-supplied array — no .bss needed).
**GOAL-ICN-GLOBAL-NV.md (2026-06-03):** GN-1..5 **DONE**, GN-6 partial → **OBS era** (m2 oracle deleted; `icn_graph_native_emittable` gate now Raku-only, verified scrip.c:882/1593; `[r12+off]` frame model deleted at R12-ERAD s65); the dual-mode slot/nv directive → superseded by GVA for natives; NV remains the reflective/cross-language binding (`NV_bind_gva`) — restated in PARK; GN-PERF/GN-FENCE → **PARK/OBS**. ARCH-ICON.md's "Ladder:" pointer re-aimed here this session.
**GOAL-ICN-BROKER.md:** B-1..B-11 all **DONE** — and the entire icn_gen_t/ucontext broker it built was itself replaced by native BB emission (GROUND ZERO 3). CLOSED.
**GOAL-ICON-RTX.md (s225-ICN):** landed arms (RTX-1b/6/6b/8c/16/24/26/27/28/29/30-ICN) → **DONE**, ownership stays `RTX-CLAIMS.md`; open queue + Phase-0/2 + the DO-NOT-PORT/REFUSED rulings (DT_R/DT_DATA table keys · rt_ws_alloc · rt_make_list) → **PARK** verbatim; its permanent notes (no gdb-in-that-era note is STALE — gdb works in this container per RULES; JCON real formatting; nondeterministic micro/deal) → carried, first item corrected.
**GOAL-ICON-BB-NATIVE.md:** self-declared SUPERSEDED-ERA (Lon prune 2026-06-04); doctrine lives in `ARCH-ICON.md`; box taxonomy feeds R-5(b)'s topology table. DELETED.
**GOAL-ICON-FULL-PASS.md (2026-06-23, /283 era):** premise (widen the excise gate; scan box missing) → **OBS** (gate Raku-only; ICN-SCAN family landed per ARCH-ICON canonical-set-closed note); BENCH-0 link/invocable parse **DONE**; BENCH-1 library resolution + BENCH-ORACLE seed-pins → **→R-3(h)**; the 4 old parse-blocked benchmarks → re-measure under R-3 (grammar has moved).
**GOAL-ICON-GATE-WIDEN.md (2026-06-24):** R1-records partial + R2..R7 construct ladder → **OBS wholesale** — the gate it widens no longer applies to Icon; every construct it lists is owned by the suite counts now. Its discipline block (canonical-first, pure x86(), floor-safe) → laws.
**GOAL-ICON-IR-RUN.md / GOAL-ICON-LOWER-REDESIGN.md / GOAL-LANG-ICON.md (May-era, modes 1/2):** **OBS** — SM/interp modes DELETED; `--dump-sm` flag itself gone from the driver (verified). LANG-ICON's IC-8 jcon suite = today's rung36 corpus (live inside R-3); IC-12..15 ilib subset → whatever the suite still needs surfaces as R-3 classes; IC-10b value-typed C Byrd boxes → **forbidden** by the ZERO-C-BB law.
**GOAL-PARSER-ICON.md** 22/22 **DONE** (the Snocone shift/reduce frontend exercise) · **GOAL-PST-ICON.md** **COMPLETE** (2026-05-19, 0 violations) — both CLOSED.
**Satellite notes (ICON-BB-NOTE/PUNCH-LIST/THREE-PILES/HANDOFF-2026-06-27b, ICON-AUDIT-2026-06-24):** June-era working notes of the pre-CARVE tree; every live item they held was re-derived into the s19x-s21x cursors long ago. DELETED, text in history (mirrors the s93 SNOBOL4 accounting pass).

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet · Claude Fable
**Architecture:** `ARCH-ICON.md` · `ARCH-SCRIP.md` (ζ tier glossary) · `ARCH-ZETA-LOCAL-STORAGE.md` · `ARCH-ICON-RTX.md` · `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md`

## Session-close / push protocol
See RULES.md — `scripts/handoff_status.sh` verbatim stdout is the ONLY sanctioned completion claim.
