# ⛔⭐⭐⭐ GOAL-ICON-100 — SCRIP ICON x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

**Re-chartered 2026-08-15 s230 by Lon in-chat (Fable 5 seat), verbatim in substance:** *"We have an entirely new system. No need to bisect anything when we know what is wrong. The system has not been properly converted to the THREE ZETAS. Rethink and re-organize the entire plan to get Icon to 100%."* Every s229 rung/step was re-verified against SCRIP `ee315070` source + a fresh triple suite run this session; dispositions in LEDGER v2 at bottom. The s229 charter + 15-file consolidation ledger live in `.github` git history at `cc418744`; pre-consolidation texts at `835f4131`.

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔
**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. ██** Enforcement: every diff checked for new file-scope definitions; a commit adding one without a cited in-chat grant is REJECTED on sight (precedent: g_pcall/g_pcall_wires/RT_AB_ANCHOR eradication, s55). **THE ASK ITSELF MUST BE A BANNER** in chat (name, type, owning file, purpose, why registers/stack cannot carry it).

---

## ⭐⭐⭐ THE MODEL — ONE SYSTEM: THE THREE ZETAS (Lon's s230 ruling; tier glossary `ARCH-SCRIP.md`; ⛔ NO DUAL STORAGE ARMS IN THE END STATE)
The SN4-100 campaign built the zeta machinery in the SHARED emitter (ζ-activation slots, `emit_match_rbp` frames, defer records riding activation-frame slots, GVA, ZD staging). **Icon is NOT converted:** at `ee315070` it still routes through pre-zeta regimes — the `SCRIP_ICN_ZFRAME`/`SCRIP_ICN_CELLS` env-var arms stamped per-graph in `lower_icon.c` (~:1422, R-ZK-A site), `flat_lcl_proc`, and `icn_zframe_gen`'s `rt_gen_save_wires` hack whose own stamp comment states it supports **ONE active suspension at a time**. The conversion IS the plan; the residual FAILs decompose onto the unconverted tiers (measured s230, table below).

| tier | motion | Icon embodiment (target) | at `ee315070` (VERIFIED by grep + fresh run, s230) |
|---|---|---|---|
| **ζ-STANDING** | pinned for the life of its activation/process | per-graph activation base: wire header (γ/ω/caller-base) + params/locals ×16B, established once at α; process-lifetime: GVA globals OFF-STACK (`gva_register`), statics, coexpr pthread stacks | zframe establishment alive again post-`ee315070` (ZB-FC-1: the frame-base lea silently emitted nothing — also resurrected Prolog); GVA landed (ZK-5) and **VERIFIED WORKING ON THE DEFAULT ARM s230**: rung21/rung25 globals PASS 6/6 standalone and in-suite, gate `global_no_nv_m3` GREEN at the sanctioned 240s timeout. The earlier "globals fail on default" reading was a CONTAMINATED MEASUREMENT (concurrent suite run) — see MEASUREMENT HYGIENE. Residue: the two arms are now BEHAVIORALLY IDENTICAL (same 28-program fail set), so unification is a DELETION exercise, not a behavior change |
| **ζ-ACTIVATION FRAME** | comes and goes | per-suspension generator activations chained off STANDING, self-pop at ω; scan `?` environments (&subject/&pos save-restore); deep arrivals | **NOT CONVERTED** — `rt_gen_save_wires` one-active-suspension; witnesses FAIL: rung03_suspend_{gen,gen_compose,gen_filter,return} · recogn · level · proc_lookup · subscript_genproc · genqueen · meander (10) + scan cluster (5 suite + 2 smoke + subjpos). ⛔ NEW INSTRUMENT s230: under the rebuilt m4 runner these SEGV (14 crashes) where m3 silently prints wrong output — the faulting instruction is readable straight out of the emitted `.s`, making m4 the MONITOR-FIRST entry point for Z-3/Z-4 |
| **ζ-SPINE** | slides | per-BB FORTH cells on RSP: α grants `sub rsp,K`, zero intermediate pops, ONE terminal release; LVA locals/params/return + det value family | ZK-1..5 LANDED (`zd_wl_kind` one-authority, ZK-2/ZK-5 arms live) — the cells arm is today's best (235/28/30) and becomes THE substrate of the one system, not an arm |

**Register contract (unchanged):** RSP = FORTH spine · RBP = pinned STANDING/FRAME base per predicate · **R13/R14/R15 = Σ/δ/Δ RESERVED for scan family** · **R12 FREE** (Lon 2026-08-07; first claimant documents here + gates) · RBX = heap bump-frontier · RCX/RDX = γ/ω wires at proc enter, args via `g_call_args[]` · RAX scratch. Canonical sources law: `refs/jcon-master/tran/irgen.icn` (43 `ir_a_*` four-port topologies) + `refs/icon-master/src/runtime/*.r` (`interp.r` efp/gfp mark/esusp is THE suspension precedent) — Lon supplies the archives as chat uploads (`2-icon-master.zip`/`3-jcon-master.zip`); unzip so `ls refs/jcon-master/tran/irgen.icn` succeeds before trusting.

## ⛔ DEFINITION OF DONE — "100%" (arm-choice decision memo DELETED — there is ONE system)
1. **`scripts/scorecard_icon.sh` META = 100.0** ⟸ `test_icon_all_rungs.sh` **293/0/0** on THE system, m3; oracle = Arizona `icont -s prog.icn -x` / pinned `.expected` (⛔ NEVER Java/JVM — Lon 2026-07-21; JCON real formatting where the rung36 corpus is JCON's).
2. **m3 ≡ m4 at 293/0/0.** The m4 runner was REBUILT AND LANDED s230 (Z-0(a) DONE; the shipped one was deleted-era infra — `-icn`+nasm+`/home/scrip` — and its 0/293 measured the harness, not the compiler). FIRST HONEST m4 WATERMARK: **234/29/30**, differing from m3's 235/28/30 by **EXACTLY ONE PROGRAM** (`rung36_jcon_proto`, emit-fail; 28 fails shared). m3≡m4 is therefore ALREADY STRUCTURALLY TRUE — the parity clause reduces to closing the 28 shared classes + that one emit.
3. **Benchmarks honest-green:** `honest_icon_correctness.sh` 8/8 IDENTICAL vs live iconx (build oracle once per session; `test_icon_bench_corpus.sh` ⛔ superseded — s164 rsg lesson). RE-MEASURED s230 against a live oracle (Icon 9.5.25a built from `refs/icon-master`): **0/8, but the failure MODE changed** — no longer EMITFAIL; SCRIP now emits, links and runs, producing zero output on 7 (link-library/early-exit class, cf. BENCH-1) and a 2-line near-miss on micsum (only line 2 differs). That is a measurable baseline, not a wall.
4. **THREE ZETAS complete:** Z-1..Z-4 landed; **zero env-var storage arms remain** (`SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME` folded byte-inertly); ZK-8 100% assertion gate green.
5. **Gates strict and green:** all 10 `test_gate_icn_{no_stack,one_reg_frame,semicolon_required,rbp_census_ratchet,zk5_gva,zcells_gva,local_no_nv,global_no_nv_m3,scan,var}.sh` + TEMPLATE-ONLY/BOTH-MEDIUM greps == 0 + SN4+Prolog watermark invariance on every shared-emitter commit (R-ICN-D; `ee315070` proved the couplings run BOTH ways) + `handoff_status.sh` COMPLETE.

## THE INSTRUMENT (one; boards are RUN, never transcribed; re-derive every count fresh — never from prose)
```
bash scripts/scorecard_icon.sh run [--suites a,b] [--out DIR]    # the periodic progress reading Lon asks for
bash scripts/scorecard_icon.sh report [DIR]                      # META = 100·Σ(w·pass/total)/Σw
bash scripts/test_icon_all_rungs.sh                              # m3 suite; SCRIP_ICN_CELLS=1 / SCRIP_ICN_ZFRAME=0 spot-reads REMAIN until Z-8 folds the arms
SCRIP_ZD_CENSUS=1 / SCRIP_ZD_DIAG=1                              # arming census / planner-decline discriminator
```
Weights (Lon's knob, ONE authority in the script header): rungs_m3 25 · rungs_m4 20 · rungs_m3_cells 10 · bench_correct 15 · smoke 10 · crosscheck 5 · gates 10 (Σ95). XFAIL counts in the denominator (end state 293/0/0). rungs_m4 feeds from the Z-0 rebuilt runner; the rungs_m3_cells weight retires with the Z-8 fold (Lon's knob). ⛔⛔ **MEASUREMENT HYGIENE + CONCURRENCY (minted s230, and the reason a whole block of s230 numbers is VOID below):** **A SECOND WORKER WAS EDITING AND REBUILDING THE SAME SCRIP TREE DURING THIS SESSION'S MEASUREMENTS.** Forensics: SCRIP `8303fb8f` (22:13:30, same `LCherryholmes` identity) independently rebuilt the m4 runner — the same Z-0(a) task, ~30 min duplicated; `src/emitter/emit.cpp` + `src/contracts/IR.h` + `src/lower/lower_icon.c` were edited 22:17:50–22:18:07 to stamp `icn_cells_graph=1` BY DEFAULT with a `SCRIP_ICN_LEGACY=1` escape (Z-1 work, uncommitted); **`./scrip` was rebuilt at 22:21:38 — in the middle of this session's suite sequence**; and `test_gate_icn_local_no_nv.sh` + `test_gate_icn_global_no_nv_m3.sh` were rewritten at 22:29:46 (LVA-1 LOCK 2 changed from requiring `[r12+off]` to accepting `rsp` refs and banning `r9` — coherent with a cells-default regime, but a GATE RELAXATION that must be Lon's call, not a side effect). LAWS: (1) **PIN THE COMMIT AND THE BINARY** — record `git rev-parse HEAD`, `git status --short` (must be empty) and the `scrip` mtime with every watermark; a number without that provenance is not evidence. (2) **`pgrep -c scrip` = 0 and `git status` clean BEFORE and AFTER** a suite; if the tree moved, the run is void. (3) **Two clean agreeing runs on the same pinned binary** is the bar for a number that enters this file. (4) **ONE WORKER PER TREE** — concurrent sessions must serialize or work separate clones; this session and the other one rebuilt the same script twice and measured each other's compiler. Bug-finding law: **MONITOR-FIRST**Bug-finding law: **MONITOR-FIRST** — cheapest discriminating experiment first (passing sibling vs failing probe, emitted-asm diff), then `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` + gdb spin/ignore-count breakpoints (HW watchpoints don't work in this container). ⛔ INHERITED-CLAIM LAW, re-proven twice this session: the R-3(a) fail-edge root repro and the R-3(c) case-disjunction repro BOTH PASS at `ee315070` — never spend on a recorded diagnosis without re-running its repro first.

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY (`x86(...)` sole producer; missing encoder ⇒ add it in `x86_asm.h`) · ONE-AUTHORITY constants (line numbers drift daily — re-grep) · PEERS RULE (no BB_t fields; `ir_operand_push`) · NO-LANGUAGE-IDENTITY past LOWER (behavioral names only) · ZERO C BYRD BOXES (`icn_bb_dcg` sole exemption) · **ICON SEMICOLON-REQUIRED, ZERO newline processing** (prison gate, 3 locks) · CONSULT CANONICAL SOURCES every session (see THE MODEL) · killswitch per behavioral family, `=0` byte-identity is a completion criterion · **R-ICN-D: SN4 + Prolog watermark invariance on EVERY shared-emitter commit** · struct fields APPENDED AT END (s141 ABI law) · REGENS ×3 + `update_icon_bench_asm.sh` (DEMO+BENCHMARK only) when codegen touched · O2-DIRECTED-ONLY · timeouts 8s/30s, `< /dev/null` on scrip calls · `git pull --rebase` before every push, re-prove watermark after rebase · push mid-session per rung; credential = ask Lon in chat and wait · LIVE CURSOR moves every handoff · "HANDOFF COMPLETE" only from `handoff_status.sh` verbatim stdout.

## ⛔⛔⛔ FACT RULE — ICON+PROLOG TRIPLE-ARM CANARY ON EVERY SHARED-EMITTER COMMIT (Lon 2026-08-15 s230, in-chat: "All your choices. I'm with you on this" — RATIFIED) ⛔⛔⛔
**██ Every commit touching `src/emitter/`, `src/templates/`, or `x86_asm.h` MUST run the Icon canary `write("hello")` on every live arm AND the Prolog honest suite BEFORE commit; Icon-touching rungs run the full Icon suite. ██** Evidence: 4 recurrences of the Icon-dies-off-ledger class (s203 −37 · s207 1/293 · post-s211 drift · s229 1/293), and `ee315070` proved the couplings run BOTH ways — one frame-base lea killed Icon AND Prolog together. Sibling (open): cross-goal watermark table in `handoff_status.sh`.

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh; make libscrip_rt && make scrip   # header edits are NOT dep-tracked: rm -rf out/rt_pic first
mkdir -p refs && unzip -q <Lon's 2-icon-master.zip> -d /tmp/i && unzip -q <3-jcon-master.zip> -d /tmp/j && cp -r /tmp/i/icon-master /tmp/j/jcon-master refs/   # verify: ls refs/jcon-master/tran/irgen.icn
printf 'procedure main(); write("hello"); end\n' > /tmp/h.icn && for e in "" "SCRIP_ICN_CELLS=1" "SCRIP_ICN_ZFRAME=0"; do env $e ./scrip --run /tmp/h.icn </dev/null; echo "$e rc=$?"; done   # canary ×3 until Z-8
bash scripts/test_icon_all_rungs.sh 2>/dev/null | tail -1   # fresh watermark FIRST
```

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s233 (Claude Opus 5) — **Z-5(b) FIRST WIN LANDED (snprintf out of the coercion hot path). Z-4 "RELEASE MATCH'S CARVE" FALSIFIED BY MEASUREMENT — THE LEAK IS LOAD-BEARING FOR DIRECT CONSUMERS.**
- **Commits:** SCRIP `3b9c9db0` (descr_to_str itos) · corpus `27cc99d2` (3 isolating micro-benchmarks). ⛔ NOTHING PUSHED — credential ask OPEN, covering exactly these 2 (one per repo). `.github` commit for this cursor pending in the same ask.
- **WATERMARK at `3b9c9db0`, clean tree, pinned, `pgrep scrip`==0 before+after, two agreeing runs:** Icon m3 **235/28/30** · canary rc=0 ×3 arms · Prolog honest **PASS=108 FAIL=0 ABORT=0** · SN4 broad m3 **311/25**, m4 **310/24 SKIP=2** (m4 matches record) · oracle = Arizona Icon 9.5.25a built this session.
- **⭐⭐⭐ Z-5(b) FIRST WIN — THE COERCION COST WAS `snprintf`, AND IT IS NOW GONE.** Poor-man's gdb sampler put the time in `descr_to_str` → `snprintf`. Every integer→string coercion ran a full printf-family format parse, plus an unconditional `rt_str_alloc(31)` regardless of digit count, plus a `STRVAL` that DISCARDED the length snprintf had already returned. Replaced with canonical `itos` (`refs/icon-master/src/runtime/cnv.r:648`) — backward digit loop into a stack buffer, length as a pointer difference. **MEASURED, correctness-gated: isolating witness `"abcdefgh" || intvar` ×2M 208ms → 144ms; committed `bench_icnstr_concat_int_dispatch` 222ms → 177ms.** LLONG_MIN oracle-verified (naive `-ival` overflows; unsigned negation handles it).
  - **COMPOUNDING FACT, recorded because it is invisible from the C:** `rtx_str.S`'s `str_concat_d` asm fast path treats **`slen==0` as "unknown length, C strlens" and BAILS**. `STRVAL` sets `slen=0`. So every integer-derived string was PERMANENTLY DISQUALIFIED from the asm fast path. Now returns `BSTRVAL` so the length rides the descriptor. ⛔ HONEST: that second step showed **NO measurable win** on this benchmark (144→148ms, noise) — the payoff can only appear on RE-concatenation of an integer-derived string. Landed as strictly-more-information, **not** as a banked speedup. Do not cite it as a perf result.
- **⛔⭐⭐⭐ Z-4 NARROWING EXPERIMENT RUN TO COMPLETION AND FALSIFIED — DO NOT RETRY "JUST RELEASE MATCH'S CARVE".** s232 queued this as the cheap test before the big conversion. It is now RUN, with a clean causation A/B, and it FAILS.
  - **The misclassification is REAL and now has a SOURCE-LEVEL proof, not just an asm reading:** `bb_scan_match.cpp` emits `x86_beta_trampoline()` = **`β: jmp ω`, a det-leaf that reads NOTHING** (`x86_asm.h:745`), while `bb_scan_tab.cpp` emits a real `x86_beta()` that reads the saved cursor back out of the retained cell. So match genuinely has **no gamma-live state**, yet `emit.cpp:1236` files it in the windowed class (`op_fc_base = op_off+16`) whose entire stated rationale (`x86_asm.h:2412`) is that gamma-live state is the point.
  - **The release mechanism EXISTS and works:** `op_zgpop` rides the ONE existing X86H_JMP/GAMMA hook arm (`x86_asm.h:2419`, `add rsp,op_zgpop`) and is gated INDEPENDENTLY of `op_fc_base` at `:489` — so match can keep its window for its own α..γ needle reads AND release at γ. Implemented behind `SCRIP_NO_MATCH_GREL=1`.
  - **RESULT: the three s232 carve-leak witnesses ALL PASS with the release on, and `_var`+`_subject` FAIL with the killswitch restoring the leak — clean causation. `_lucky` passes BOTH ways, behaving exactly as s232 said its control should. BUT the suite went 235 → 234.**
  - **⛔ THE REGRESSION IS THE FINDING: `rung08_strbuiltins_match`.** `"hello" ? write(match("hel"))` must print `4`; with the release it prints EMPTY. **The consumer reads match's result through an rsp-relative address calibrated around the carve PERSISTING** — the same assumption the s231 `op_sa` compensation bakes in at `emit.cpp:1230/1231` for tab/move. So the leak is **simultaneously load-bearing for DIRECT consumers and harmful for DOWNSTREAM locals**. That is the per-box `op_zdepth` "every box is balanced at handoff" model breaking in both directions at once.
  - **CONSEQUENCE FOR THE PLAN — this is positive evidence for Z-3, not just a dead end:** a release-only fix cannot work at ANY of the three sites, because the compensations that assume the carve persists would all have to come out in the same motion. **The cell must move OFF the spine into a ζ-ACTIVATION FRAME (Z-3 core) and the `op_sa`/`op_zdepth` compensations must retire WITH it, in one commit.** Reverted; floor re-proved 235/28/30 at a clean tree.
- **⭐ THE s232 COERCION PAIR WAS UNSOUND — the signal was right, the instrument was not.** `bench_icnstr_concat_dispatch` vs `..._int_dispatch` differ by **THREE** variables (literal vs variable right operand, an added `i%97`, and the coercion), and the literal‖literal case is **loop-invariant and constant-foldable**, so its speedup may be measuring the OPTIMIZER. Also **3 of that board's 10 programs were never committed and are LOST**. Minted the single-variable set in corpus `27cc99d2`, auto-wired via the `bench_icn*` glob: `bench_icnstr_concat_strvar` (2.02x, no coercion) · `bench_icnstr_concat_intvar` (0.67x, coercion) · `bench_icnint_mod_isolate` (**5.39x — EXONERATES the modulo the old pair had confounded in**).
- **⭐ HONEST BOARD, 10/10 byte-identical vs live iconx, REPS=5** (post-coercion-fix): int loop **8.8–9.4x** · mod isolate **5.39x** · str‖strvar **2.02x** · concat table 1.25x · sub list 1.40x · concat_int_dispatch 0.72–0.90x (was 0.48–0.61x) · table-miss dispatch 0.86x · str‖intvar 0.67x · table-miss semantics 0.50x. **Headline 8 still 0/8** (concord/geddump SEGV, tgrlink abort, micsum 1-line near-miss, rest empty) — blocked by the tab/move carve leak, so NO headline speedup number is valid.
- **⭐ NEW INSTRUMENT — STARTUP FLOOR, and it changes how the small end of the board must be read.** Measured on an empty `procedure main();end`: **iconx 0.7ms vs SCRIP 1.9ms (+1.2ms fixed)**. `bench_icnsub_table_miss_semantics` totals 4ms vs 8ms, so roughly a THIRD of its "0.50x" is process startup, not semantics. ⛔ Do not read sub-10ms rows as engine speed, and do not let that row drag a geometric mean: excluding it moves the 7-program geomean from 1.25x to 1.50x.
- **ORACLE BUILD CONFIRMED (s232's simpler recipe is correct):** `make Configure name=linux` configures NoGraphics directly — no `define.h` edit, no `make X-Configure` revert trap. Only `XLIBS=` in `Makedefs` needs clearing, then `make All`. Reproduced clean this session.
- OWED: push credential (2 commits + this cursor) · Z-3 slice 2 (now the ONLY route to Z-4) · the tab/move leak remains OPEN and `_lucky` still passes by luck.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s232 (Claude Opus 5) — **Z-4 ROOT CAUSE PROVEN: THE FORTH CARVE IS NEVER RELEASED ON THE SCAN SUCCESS EDGE. THE RECORDED "Δ NOT ESTABLISHED" DIAGNOSIS IS FALSIFIED. FIRST HONEST ICON PERF BOARD LANDED.**
- **Commits:** corpus `7caceb7d` (3 witnesses) · SCRIP `9d5f324e` (`scripts/honest_icon_microbench.sh`). ⛔ NOTHING PUSHED — credential ask OPEN, covering exactly these 3 (one per repo). ⭐ CORRECTION to the s231 cursor: its "5 unpushed SCRIP commits" claim is STALE — `bb934ff3` and its predecessors are ON origin/main (verified `git branch -r --contains`); only this session's commit is ahead. Per STALE-ORIENTATION (a), do not carry a push-status claim forward from prose — re-derive it.
- **WATERMARK at `bb934ff3`, clean tree, pinned, `pgrep scrip`==0 before+after:** Icon m3 **235/28/30** (matches record) · canary rc=0 ×3 arms · oracle = Arizona Icon 9.5.25a built this session.
- **⭐⭐⭐ ONE DEFECT, FOUR SYMPTOMS — Z-4 IS NOT A Δ-ESTABLISHMENT RUNG.** Scan-family boxes carve a FORTH cell at α (`sub rsp,16`) and release it ONLY on the failure/β edges. **The SUCCESS edge has no release at all**, so the carve LEAKS, once per scan-family box in the chain. Locals are rsp-relative ⇒ every later local ref is shifted. Measured with gdb: `tab(match("-"))` = two carves ⇒ rsp **32 low** at the read ⇒ `[rsp+384]` hits zeros; `tab(2)` = one carve ⇒ rsp **16 low** ⇒ lands on a DUPLICATE copy of the same descriptor and is **ACCIDENTALLY CORRECT** (`x/2gx` showed identical DESCRs at `[rsp+352]` and `[rsp+368]`).
  - **Chain:** leak shifts rsp → next block's `scan_enter` reads its subject operand from the shifted slot → zero DESCR → `&subject` empty → Δ=0 → `pos(0)` computes `cvpos(0,0)=len+0+1=1 == &pos` → wrongly SUCCEEDS. That IS the recorded "stale Δ", and it is DOWNSTREAM. options() gates 7 of 8 headline benchmarks.
- **⛔⭐ FIX DIRECTION — CORRECTED LATE IN s232, SUPERSEDES THE "JUST ADD A TERMINAL RELEASE" READING ABOVE.** The missing γ release is **NOT an oversight**. THE EXACT SITE: `x86_asm.h:489` gates the γ-port release on `x86_fc_on() && _.op_fc_base < 0` — the "carve-only class" — while the ω port (`:488`) releases unconditionally. `emit.cpp` IR_SCAN_TAB/IR_SCAN_MOVE set `op_fc_base = op_off + 16` (≥0), so **tab/move are excluded from the γ release BY CONSTRUCTION**. The reason is visible in the emitted asm: `n6_scan_tab_β: mov r14,[rsp+0]` — **β reads the saved cursor out of that very cell**, per ARCH-ICON's "cursor-movers reversed-on-resume". The cell MUST survive γ.
  - **So the real defect is architectural, and it is exactly the ζ conversion this goal already calls for:** the suspended cursor cell **rides the SPINE (rsp)**. Retaining it past γ — which correctness of β requires — leaves rsp deep while execution continues, and every later rsp-relative local ref is shifted. Per-box `op_zdepth` ("a box compensates for exactly what IT carved") is a model that assumes each box is balanced at handoff; a box that legitimately suspends breaks that assumption. **Fix = move the retained cursor cell OFF the spine into a ζ-ACTIVATION FRAME chained off STANDING (Z-3's core), not a terminal release.** Dual of the generator bug already recorded in Z-3: generators POP at γ when they must RETAIN; cursor-movers RETAIN at γ while the spine model assumes they popped. Same root, opposite sign.
  - **⭐ BLAST RADIUS + A CHEAP TEST TO RUN FIRST (s232, last finding).** EXACTLY THREE boxes set `op_fc_base >= 0` and are thus excluded from the γ release: `IR_SCAN_TAB`, `IR_SCAN_MOVE`, `IR_SCAN_MATCH` (grep of emit.cpp; the conversion scope is that small). ⛔ BUT **MATCH LOOKS MISCLASSIFIED**: ARCH-ICON classes match as a POSITION-RETURNER (`{0,1}`, δ-untouched), not a cursor-mover, and the emitted asm agrees — `t2.s` emits `n5_scan_match_α` with two failure exits and **NO `_β` label at all**, while tab emits both `α` and `β`. A box that never resumes has no reversal-on-resume state, so its retained carve may serve NO purpose. TEST BEFORE THE BIG CONVERSION: release match's carve at γ (drop it from the ≥0 class) and re-measure — that alone halves the leak in `tab(match(..))`, which is the `=s` shape gating options(). ⛔ NOT a fix on its own: the SINGLE-carve case still leaks 16 and passes only by the duplicate-DESCR luck documented above. Treat as a cheap narrowing experiment, not a rung.
  - **⛔ HONEST STATUS OF THE HAND-PATCH:** inserting `add rsp,32` at the scan-leave DID make `tab(match("-"))` oracle-exact — but that witness never resumes β, so it does not license the release as a fix. I then tried to demonstrate the release BREAKING a β-resume program (`if tab(match("-")) & pos(0) ...`, which does reach `scan_tab_β`) and **the patched binary still passed — the demonstration was INCONCLUSIVE, not a vindication of the release.** Recorded as inconclusive deliberately: do not cite the hand-patch as evidence the release is safe, and do not cite my failed break-attempt as evidence it is safe either. The line-489 gate + the β read of `[rsp+0]` are the load-bearing facts.
  - **⛔ ACCIDENTAL PASSES ARE IN THE SUITE.** `witness_icn_carve_leak_lucky.icn` is committed passing, by luck. Some of today's 235 may be luck of the same kind — treat single-carve scan passes as unproven until the release lands.
- **⛔ FALSIFIED THIS SESSION (do not retry):** "Δ not established at scan entry" (single-block `pos(0)`/`pos(-1)`/`*&subject` all oracle-exact) · "the s231 `op_sa` carve compensation causes it" (`SCRIP_NO_CM_CARVE=1` changes nothing — **`bb934ff3` is exonerated**) · "any non-literal operand triggers it" (`tab(1+1)` clean; only a scan-family PRODUCER as operand, i.e. only when two boxes carve) · patching the cursor-mover's own failure-edge releases 16→32 (no effect — the success edge has nothing to patch).
- **⛔ NEXT RUNG (Z-4, restated):** enforce the ζ-SPINE law already written in THE MODEL — "α grants `sub rsp,K`, **ZERO intermediate pops, ONE terminal release**". Remove the per-box intermediate pops so depth at the leave box is UNIFORM across incoming edges, then emit ONE terminal release of the cumulative grant. ⛔ A naive per-edge `add` is WRONG: the failure edges have already popped, so depth is edge-dependent today — that asymmetry is the bug, not a detail. Shared emitter ⇒ R-ICN-D (SN4 + Prolog invariance) + triple-arm canary on the commit.
- **⭐ FIRST HONEST ICON PERF BOARD** (correctness-gated, two agreeing runs; `scripts/honest_icon_microbench.sh`): 10/10 byte-identical, **geometric mean 1.70x**, 6 faster / 4 slower. int loop **7.55x** · numeric isolate **5.07x** · relational isolate **3.62x** · str concat 2.48x · sub list 1.59x · concat table 1.57x · table-miss dispatch 0.97x · **numeric MIXED 0.72x** · **concat WITH int coercion 0.66x** · table-miss semantics 0.50x. The headline 8 remain **0/8** (3 now SEGV/abort: concord, geddump, tgrlink; micsum a 1-line near-miss) — blocked by the carve leak above, so no headline speedup number is valid.
- **⭐ SIGNAL — Z-5(b) IS UNDERPRICED.** The two worst results are both COERCION, and they form a matched pair across two independent paths: str concat 2.48x vs concat+int-coercion 0.66x; numeric isolate 5.07x vs numeric mixed 0.72x. One shared coercion cost, ~3.8x and ~7x swings. Recommend Z-5(b) be re-ranked against Z-3.
- **ORACLE BUILD — SIMPLER THAN RECORDED.** `make Configure name=linux` configures **NoGraphics** directly: no `define.h` edit, and the `make X-Configure` regenerate-and-silently-revert trap never arises. Only `XLIBS=` in `Makedefs` still needs clearing, then `make All`. Recommend replacing the two-edit recipe in the s231 cursor with this one-liner.
- **INSTRUMENT TRAP (cost this session ~15 min, is the Z-0 failure mode):** `--compile` writes asm to **stdout**; a harness that looks for an output binary prints a plausible full-board **0/7 EMITFAIL** and measures itself, not the compiler. Sanctioned recipe: `--compile --target=x86 > f.s` then `gcc -no-pie f.s -L$RTDIR -lscrip_rt -Wl,-rpath,$RTDIR`.
- OWED: push credential · the Z-4 release fix above · Z-3 unchanged.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s231 (Claude Sonnet 4.6) — **BENCHMARKS RUNNING vs Arizona Icon 9.5.25a; pos(0) STALE-Δ ISOLATED; WITNESSES MINTED IN corpus/probe/icn/; NEXT: Z-4 δ ESTABLISHMENT**
- **SCRIP commits this session:** none (all bench-args + =s fixes landed by concurrent seat; my independent convergence confirmed and verified). **corpus commit:** `77ae69b1` — two Z-4 witnesses under `probe/icn/`.
- **WATERMARK at `ccb7e7b9`, clean tree, pinned:** Icon m3 **235/28/30** · canary rc=0 ×3 arms · SN4+Prolog invariant held.
- **MICRO-BENCHMARKS vs Arizona Icon — ALL 7 byte-identical (first correctness-gated perf numbers):**

| Benchmark | iconx | SCRIP m4 | Speedup |
|---|---|---|---|
| bench_icnint_loop | 154ms | 22ms | **7.00x** |
| bench_icnstr_concat_dispatch | 157ms | 65ms | **2.42x** |
| bench_icnstr_concat_table | 362ms | 234ms | **1.55x** |
| bench_icnsub_list_dispatch | 275ms | 178ms | **1.54x** |
| bench_icnsub_table_miss_dispatch | 193ms | 192ms | 1.01x |
| bench_icnstr_concat_int_dispatch | 241ms | 366ms | **0.66x** |
| bench_icnsub_table_miss_semantics | 8ms | 14ms | 0.57x |

Geometric mean ~1.5x. Integer arithmetic 7x — strongest evidence for the "ten times faster" thesis. ⚠️ **Regression:** plain str concat 2.42x, str concat WITH integer coercion 0.66x — inverted by the BINOP_CONCAT coercion path (Z-5(b) material). Harness: `/home/claude/microbench.sh` (timing only on byte-identical output).
- **⛔ THE HEADLINE 8 STILL 0/8 — and why.** The chain: `main(args)` was null (fixed `c7338c61`) → `options()` now gets args → `=s` sugar set cursor to end (fixed `90618b21`) → `not pos(0)` NOW WRONG because **Δ (subject length) is stale** after a prior scan block. `pos(0)` means "at end" → non-positive conversion needs Δ → stale/zero Δ makes end = 1 → `not pos(0)` at position 1 succeeds wrongly → options() takes the else branch → `put(flist,x)` gets operands from wrong slots → "Error 5 Undefined function." Every headline benchmark parses `-` options.
- **⭐ ISOLATED: pos(0) STALE-Δ DEFECT — the remaining root cause for 7/8 benchmarks.**
  ```
  ORACLE:  B &pos=1 pos(1)=Y pos(0)=N pos(5)=N
  SCRIP:   B &pos=1 pos(1)=Y pos(0)=Y pos(5)=N   ← pos(0) only
  ```
  `&pos`, `pos(1)`, `pos(5)` all correct. Only `pos(0)`. Triggers after a prior scan block runs `=s`. Witnesses: `corpus/probe/icn/witness_icn_pos0_stale_delta.icn` + `witness_icn_options_dash_branch.icn` (both oracle-verified at `ccb7e7b9`).
  - **FALSIFIED (do not retry):** missing get/put builtins · missing `;` after `local` decl · `not` broken · `pos()` broken generally · scan-state leakage between blocks · 90618b21 `=s` cursor fix covering pos(0) — it fixes cursor (pos=2 ✓) but NOT Δ.
- **⛔ NEXT RUNG (Z-4):** ONE both-medium Σ/δ/Δ establishment site at scan entry per `ir_a_Scan` ScanSwap topology. Read `refs/icon-master/src/runtime/fscan.r` (`cvpos`/`k_pos`) and `ir_a_Scan` in irgen.icn first. Witness in `corpus/probe/icn/` confirms the predicate at both ends. Do NOT touch xa_flat's shared prologue. Gate: scan green + witness IDENTICAL.
- **CONCURRENCY NOTE:** Shared tree moved twice under me mid-session (cc626b89→c7338c61→90618b21→ccb7e7b9). My main(args) diagnosis was independently converged by the concurrent seat. Mitigation: isolated pinned clones for all measurements (LAW 4). Suggest adding "MEASURE PINNED" as a one-liner to the hygiene laws.
- OWED: push credential (Lon supplied; push pending in this handoff) · Z-4 δ establishment · Z-3 unchanged.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s231 (Claude Opus 5) — **BENCH-1 OPENED: TWO ROOT CAUSES FOUND AND FIXED. THE BENCHMARK OUTPUT-EMPTY CLASS IS NOT ONE DEFECT, IT IS A CHAIN.**
- **SCRIP commits this session:** `c7338c61` main(args) bind · `90618b21` cursor-mover carve compensation. ⛔ NOTHING PUSHED — credential ask STILL OPEN, now covering 5 SCRIP commits (`8303fb8f` `f9beec6d` `b1fc1ac4` + these two) and 1 `.github`.
- **WATERMARK at `90618b21`, clean tree, pinned:** Icon m3 **235/28/30** (held across BOTH commits — these fixes live in the benchmark corpus, not the rung suite) · canary rc=0 ×3 arms · Prolog honest FAIL=0 · SN4 broad_corpus **310/24** invariant (R-ICN-D proven on both commits, and A/B'd under killswitch on the first).
- **⭐ ORACLE IS LIVE AGAIN — build recipe, because it is NOT reproducible from the Makefile alone:** Arizona Icon 9.5.25a from `refs/icon-master` needs TWO edits in this container — comment out `#define Graphics 1` in **generated** `src/h/define.h`, and clear `XLIBS = -lXpm -lX11` in `Makedefs`. ⛔ `make X-Configure` REGENERATES define.h and silently reverts edit 1. Then `make All`. Invoke the honest scripts with `SCRIPDIR=/home/claude/SCRIP ICONM=/home/claude/icon-master CORPUS_SRC=/home/claude/corpus/benchmarks/icon` — env overrides beat the `/home/claude/work` symlink layout.
- **⛔⛔ THE 38x THAT ISN'T: `honest_icon_bench.sh` printed 7.67x/10.71x/13.07x/38.17x THIS SESSION, every one on a program that exits rc=1 having done NO work.** The only end-to-end-honest program, `version`, is **0.67x — SCRIP SLOWER than iconx**. The s164 footer warning fired again, verbatim. ⛔ NO ICON SPEEDUP NUMBER IS VALID TODAY. Do not quote one until `honest_icon_correctness.sh` says IDENTICAL for that program.
- **BENCH-1 ROOT CAUSE 1 — `main(args)` WAS NEVER BOUND (`c7338c61`).** The ICNBENCH-ARGS-RSP channel was HALF-WIRED since 2026-07-18: stage side emits, bind side never did — `rt_main_args_fetch` is declared at `xa_flat.cpp:14` and referenced NOWHERE; emitted `.s` carried stage ×1 fetch ×0. Probe: SCRIP `type=null size=0` vs oracle `type=list size=2`. options() does `while x := get(arg)` over that null → APPLY_fn → Error 5 → death immediately after the `*** Benchmarking with output ***` marker → EMPTY window. Fix: `rt_main_args_stage` also writes the list DESCR into `g_call_args[0]`; the existing installer (`rt.c:1212`, param i at `[base+(i+1)*16]`) binds it. Killswitch `SCRIP_NO_MAIN_ARGS=1`.
- **BENCH-1 ROOT CAUSE 2 — CURSOR-MOVERS DROPPED NON-LITERAL OPERANDS (`90618b21`).** `tab(v)`/`move(v)` read 0 for any non-literal v; literals were fine. The box carves its FORTH cell at α BEFORE the fetch, so a producer's pre-carve slot was read post-carve, 16 bytes off. `=s` lowers to `tab(match(s))` and match returns a non-literal ⇒ **EVERY `=s` in Icon was broken** ⇒ options() again. Fix: add the granted carve to `op_sa` at the TAB/MOVE dispatch. ⛔ DO NOT use `op_zdepth` for this — tried and measured, it bumps the box's OWN result slot too and breaks the literal arm.
- **⛔ THE NEXT RUNG, STATED PRECISELY (Z-4, and it is ONE defect not two):** a scan environment does NOT establish/reset the internal cursor δ. After a scan that moved the cursor, the next `x ? {...}` reads `&pos=1` while δ is STALE — so `not pos(0)` wrongly fails, and `move(var 3)` returns `[ab]` not `[abc]` even though its addressing is now provably correct (552 pre-carve → 568 post-carve, verified in the emitted `.s`). Repro `/tmp/bp/td.icn` + `th.icn`. This is exactly Z-4's "once-at-entry establishment" (old R-3(d): ONE both-medium site at scan entry, NOT the read, NOT xa_flat's shared prologue). ⛔ READ `refs/icon-master/src/runtime/fscan.r` (`cvpos`/`k_pos` — `tab` at :89, `move` at :9) + `ir_a_Scan` ScanSwap in irgen.icn BEFORE coding.
- **METHOD NOTE THAT PAID OFF TWICE:** the harness discards stderr. Both root causes fell out of `2>&1` on ONE benchmark binary, then a gdb breakpoint on `core_runtime_error` (`set breakpoint pending on` — `rt_*` live in the `.so`). Root cause 2 was PROVEN before any compiler edit by hand-patching one displacement in the emitted `.s` and reassembling. Do that first; it costs a minute and cannot lie.
- **SECONDARY, UNCHASED:** `write()` appears to DROP the string literal immediately preceding a cursor-mover call (`"literal: tab(3)=["` vanished from output). Possibly the `icn_tree_is_cursor_mover` backtrack handling at `lower_icon.c:848`. Witness in `/tmp/bp/tg.icn`.
- OWED: push credential · Z-4 δ establishment (above) · Z-3 core (γ-retain + spine handshake) unchanged from s230.

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity where applicable, triple-arm watermark (single-arm after Z-8) + SN4+Prolog invariance, cursor move, push mid-session

- [~] **Z-0 · GROUND TRUTH + INSTRUMENT REPAIR — INSTRUMENTS LANDED, GROUND TRUTH RE-OWED.** The instrument half is done (m4 runner rebuilt ×2 by two workers, scorecard repointed, live oracle built, MEASUREMENT HYGIENE + CONCURRENCY laws minted). The ground-truth half must be REDONE on a pinned commit with a clean tree, because the second worker's 22:21:38 rebuild voided the number board. Prior wording (now false): triple watermark (twice, agreeing) · 28-fail classification · gate board at the sanctioned timeout · smoke · crosscheck · **m4 runner rebuilt and landed** · **scorecard re-baselined META 69.1** · **live Icon 9.5.25a oracle built** · two prior-session diagnoses retired by re-run · MEASUREMENT HYGIENE law minted. Nothing owed.
- [~] **Z-1 · ONE SYSTEM — ARM UNIFICATION — ⛔ IN FLIGHT BY THE OTHER WORKER, DO NOT DOUBLE-DRIVE.** Uncommitted in the shared tree as of 22:29: `icn_cells_graph=1` stamped by default, `SCRIP_ICN_LEGACY=1` as the restore-the-fork killswitch, and the two NV gates rewritten for the cells regime. ⛔ The apparent "arms are behaviorally identical / globals closed" evidence is an ARTIFACT of measuring that in-flight change (cells default ON ⇒ the default arm IS the cells arm); the provenance-clean `ee315070` reading still shows rung21/rung25 FAILING on the true default arm. Whoever lands this owes: byte-identity of emitted `.s` under `SCRIP_ICN_LEGACY=1`, an honest pinned watermark, and Lon's ruling on the gate relaxation. Kill the `getenv` arms at the `lower_icon.c` R-ZK-A stamp site (~:1422) behind a transition killswitch (`SCRIP_ICN_LEGACY=1` restores today's routing byte-exactly until Z-8 deletes it — NO new global; the existing env-read pattern inverted). Verify by BYTE-IDENTITY of emitted `.s` over the corpus, not merely by suite parity — identical scores can hide divergent codegen. Floor: 235/28/30 m3 + 234/29/30 m4 held; 8 gates green; SN4+Prolog invariant.
- [ ] **Z-2 · ζ-STANDING COMPLETE.** Per-graph activation base on the shared mechanism: wire header (γ/ω/caller-base), params/locals install, return protocol — verified against the SN4-proven establishment (behavioral names only; no LANG sentinels). Gates local_no_nv + rbp_census_ratchet green; `test_gate_icn_var.sh` re-read (its RED overlaps the var/globals classes — re-derive after Z-1).
- [ ] **Z-3 · SUSPENSION = ζ-ACTIVATION-FRAMES (the conversion's heart; expected yield ~10 FAILs). SLICE 1 LANDED `b1fc1ac4` (opt-in `SCRIP_ICN_GENFRAME=1`): generator graphs admitted to establishment; prologue + wire-based γ/ω now emit correctly. ⛔ SLICE 2 = THE CORE, stated precisely from measurement: (i) γ must RETAIN the activation, only ω frees it (today γ does `add rsp,kt` and destroys the suspended frame); (ii) the caller handshake `.Lx14_3: mov [rsp+136],rsp` / resume `mov rsp,[rsp+136]; jmp qword ptr [rsp]` must agree with the retained base and with the generator's parked resume address (`[rsp+48]`). Then** replace `icn_zframe_gen` + `rt_gen_save_wires` (one-active-suspension) with real per-activation frames chained off STANDING, self-pop at ω. ⛔ READ FIRST: `refs/icon-master/src/runtime/interp.r` efp/gfp mark/esusp (the canonical precedent — verified present s230) + each construct's `ir_a_*` topology in `refs/jcon-master/tran/irgen.icn`. ⛔ MONITOR-FIRST ENTRY POINT (s230): run each witness under **m4** — 9 of the 10 SEGV there, so the faulting instruction and its frame arithmetic are readable directly from the emitted `.s`, instead of chasing m3's silent wrong output. Witness set: the rung03 suspend quartet · recogn (recursive suspend) · level (rsp-drift-per-β) · proc_lookup (DC-stub double-execute) · subscript_genproc · genqueen · meander · nested every-over-to · disjunction resumed across depth · suspend-in-arg-list. Then WALK THE FAMILY, one rung-let per kind citing its topology: `IR_TO_BY · IR_DISJUNCTION · IR_REPALT · IR_PROC_GEN · every/limit/bang`. Land the topology table ONCE as `.github/ICON-IRGEN-TOPOLOGY.md` (STATIC/DYNAMIC arrival-depth verdict per kind — subsumes old ICN-FB-1). Measure Ir + peak RSS on the witness set as VALIDATION (the T-vs-P decision itself is settled by Lon's s230 ruling: frames, no pthread-per-generator; pthreads remain coexpr-only per R-ICN-C).
- [ ] **Z-4 · SCAN ENVIRONMENTS ON THE FRAMES — ⛔ ROOT CAUSE PROVEN s232; THE CHEAP RELEASE-ONLY ESCAPE IS FALSIFIED s233 (see LIVE CURSOR: `rung08_strbuiltins_match` regresses because DIRECT consumers read match's result through an rsp address calibrated on the carve PERSISTING). Z-4 IS NOW STRICTLY DOWNSTREAM OF Z-3 — the cell moves off the spine and the `op_sa`/`op_zdepth` compensations retire in the SAME commit, or not at all. READ THE LIVE CURSOR BEFORE THIS BULLET.** The gating defect is **NOT** Δ establishment (falsified s232: single-block `pos(0)`/`pos(-1)`/`*&subject` are all oracle-exact). It is that **scan-family boxes never release their FORTH carve on the SUCCESS edge** — only on failure/β — so the carve leaks once per box and shifts every later rsp-relative local ref. ⛔ Fix is NOT a terminal release (corrected late s232): the γ release is excluded BY CONSTRUCTION at `x86_asm.h:489` (`op_fc_base < 0` gate) because β reads the saved cursor from that cell. The cell rides the SPINE; retaining it past γ — which β requires — shifts every later rsp-relative ref. Fix = move the retained cell OFF the spine into a ζ-ACTIVATION FRAME (Z-3 core). See LIVE CURSOR. Witnesses `corpus/probe/icn/witness_icn_carve_leak_{var,subject,lucky}.icn` (`_lucky` passes BY ACCIDENT — a single 16-byte leak that happens to land on a duplicate DESCR; it is the control). Hand-patch proof recorded in corpus `7caceb7d`. The remainder of this bullet stands as written: `?` environment = &subject/&pos save-restore as activation-frame slots — the spec is `ir_a_Scan`'s ScanSwap topology (irgen.icn, read s230) + `fscan.r`/`fstranl.r`; R13/R14/R15 Σ/δ/Δ discipline with once-at-entry establishment (old R-3(d): fix = one both-medium site at entry, NOT the read, NOT xa_flat's shared prologue); position-returners δ-untouched vs cursor-movers reversed-on-resume per ARCH-ICON (do not blur). ⛔ The old fail-edge diagnosis is DEAD (root repro passes at `ee315070`) — MONITOR-FIRST a fresh first-divergence on jcon_scan before touching anything; scan/scan1/scan2 all SEGV under m4 (readable fault site) while subjpos/wordcnt/scan_alt give wrong output — work the crashers first; the 7 falsified hypotheses in history at `835f4131` remain do-not-retry. Gate scan green; smoke 14/14.
- [ ] **Z-5 · FAIL-ZERO — the 12 independent classes, one CLASS per commit, witness minted, oracle-verified, re-measure each first.** (a) ⛔ FIRST — the DT_DATA stale-asm-tag SILENT CORRUPTION (`rtx_icnagg.S`/`rtx_icnsub.S` carry 100; live value 0x70): fix + land the single-source `.inc` header gate (third recurrence of the class); (b) mathfunc = BINOP_CONCAT coercion carried in IR by LOWER (9 files at HEAD — full-budget, BID-AT-LOWER); (c) variable() lvalue — ⛔ NEEDS LON'S name-table ruling (sealed RO blob vs startup table); (d) FZ-C SEGVs endetab/fncs1/scan2 · FZ-D parse htprep/prepro · FZ-E remainder args/coerce(⚠241MB-use-head)/mffsol/mindfa/kwds/cxprimes — MONITOR-FIRST each; anycmp mixed-type ordering stays deliberately-unfixed (ruling on record).
- [ ] **Z-6 · m3 ≡ m4 — nearly free at s230.** Measured delta is ONE program (`rung36_jcon_proto`, emit-fail under `--compile`) plus the 28 shared classes that Z-3/Z-4/Z-5 close anyway. Fix jcon_proto's emit path; hold DIRTY-PASS at 0 (the crash-aware grade is the point of the rebuilt runner); `.s` regen freshness discipline; sweep the COMPILER never the artifacts.
- [ ] **Z-7 · XFAIL-ZERO — 30 markers (count re-verified s230).** Per marker: remove, run, fix-or-delete. Clusters: bignum (radix/lgint/arith/checkfpx/cxprimes — arbitrary-precision ints, own sub-rung, ⛔ Lon scopes it) · ck (generator-in-arg to Image()) · profsum (`next` inside scan-body must restart enclosing while) · ~24 empties to classify fresh (some pass free — cxprimes ran 25/25 once under s18).
- [ ] **Z-8 · BENCH HONEST-GREEN + GATES STRICT + THE FOLD.** honest_icon_correctness 8/8 vs live iconx; BENCH-1 link-library resolution + deal/rsg seed-pin oracles (`options/post/shuffle` are LINK-DEP LIBRARIES, no main — grade accordingly); confirm rsg end-to-end retires R-3(c) for good. Then ZK-8 100% assertion gate (`test_gate_icn_zcells.sh`: whole-graph carve 0 · .bss backtrack arenas 0 · heap-ζ for generators 0 (coexpr exempt) · locals/params=cells · globals=GVA · R13/14/15 grep 0 · `=0` byte-identity · suite-floor ratchet). Then THE FOLD, one family per commit, byte-identical OFF→deleted: `SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME`/`SCRIP_ICN_LEGACY` env reads · `zframe_graph`/`icn_cells_graph` dual-stamp scaffolding · `rt_gen_save_wires` family · dead ZC_FRAME RBP arms (s202 evidence favors deletion). Ask Lon to retire RULES' vestigial `--dump-sm` gate line (verified 0 refs s230) + the old r12-as-TOS wording.

**PARKED — PERFORMANCE / ARCHITECTURE, AFTER 100% (history at `835f4131`; do not open before DoD 1-4 green unless Lon directs):** the ICON-RTX ladder open queue (RTX-21/17/22/4/5/6c/7/10/13/14/25/26-remainder-ICN + Phase-2 RTX-11/12; landed arms + REFUSED/DO-NOT-PORT rulings in `RTX-CLAIMS.md` + git; RTX-23/25 ⛔ BLOCKED ON LON) · GN-PERF slot-vs-nv A/B (GVA won; NV remains the reflective/cross-language binding via `NV_bind_gva`) · `-O2` runtime pass (O2-DIRECTED-ONLY) · JCON-compiler-modules byte-compare (data-level, never JVM) · the perf graveyard (tolower guard/always_inline/asm rt_subscript_var/value-subscript fusion/pointer-keyed memo UNSOUND/goto-past-chain UNSOUND/288-arm guard trap) — binding, do not re-derive.

---

## LEDGER v2 — EVERY s229 RUNG, RE-VERIFIED AND DISPOSITIONED (against SCRIP `ee315070` source + fresh triple suite + live repro runs, 2026-08-15 s230; legend: **DONE** verified in tree/run · **→Z-n** absorbed · **OBS** obsolete/superseded · **LIVE** re-confirmed failing/pending)
**R-0 resurrect-the-default-arm (bisect):** **DONE** at `ee315070` ("R-0 LANDED... the frame-base lea silently emitted nothing, ZB-FC-1" — also resurrected Prolog); canary rc=0 ×3 arms and default 1→234 measured s230. The bisect METHOD is retired by Lon's s230 ruling — conversion, not regression-hunting, is the plan.
**R-1 process/FACT-RULE ask:** **LIVE** → the STANDING PROCESS ASK section; surfaced in-chat s230.
**R-2 anchor parity (252 die-list) + gates:** anchor framing **OBS** (pre-new-system anchors; 252/11/30 is a pre-collapse arm-era number). Its 5 in-scope fails all re-measured FAIL s230 → **→Z-3** (recogn/level/proc_lookup/rung03_suspend_return) + Z-3/Z-5 (prepro is FZ-D parse). FR-6a per-arm zframe gate **OBS** (ONE system; floor-ratchet discipline survives in the scorecard). FR-6d m4 runner **LIVE** → **→Z-0(a)** (shipped runner verified deleted-era: `-icn`+nasm+`/home/scrip`).
**R-3 FAIL-ZERO:** (a) scan fail-edge root **FIXED-VERIFIED** at `ee315070` (repro ELSE rc=0) — cluster still fails, diagnosis re-derives fresh → **→Z-4**; (b) variable() **LIVE**, Lon ruling owed → **→Z-5(c)**; (c) case-arm disjunction **FIXED-VERIFIED** (repro iters=3 rc=0; rsg end-to-end confirm at Z-8); (d) &pos establishment → **→Z-4**; (e) mathfunc/BINOP_CONCAT **LIVE** (9 files) → **→Z-5(b)**; (f) DT_DATA stale tag **LIVE-VERIFIED** (100 vs 0x70) ⛔ silent corruption → **→Z-5(a) FIRST**; (g) endetab/fncs1/scan2/htprep/prepro/args/coerce/mffsol/mindfa/kwds all re-measured FAIL s230 → **→Z-4/Z-5(d)**; (h) bench residue → **→Z-8**, re-measure first (0/8 EMITFAIL predates resurrect).
**R-4 XFAIL-ZERO:** 30 markers re-counted s230 → **→Z-7** intact.
**R-5 THREE ZETAS completion:** ZK-6 T-vs-P suspension decision **SETTLED by Lon's s230 ruling** (ζ-ACTIVATION-FRAMES; measurements become validation) → **→Z-3**; ZK-7 family walk → **→Z-3**; ZK-8 assertion → **→Z-8**; GVA-M3 residue **CLOSED-VERIFIED s230** — rung21/rung25 PASS on the default arm (6/6 standalone + in two clean suite runs) and `global_no_nv_m3` is GREEN at 240s; the s229 "fails on default" record was a contaminated measurement.
**R-6 m3≡m4 + instrument debts:** **DONE s230** — runner rebuilt with crash-aware CLEAN/DIRTY grading and landed; first honest m4 watermark 234/29/30, parity to within ONE program; scorecard repointed to the shared parser.
**R-7 deletions + killswitch fold:** the frames-vs-cells DEFAULT DECISION MEMO is **OBS** — Lon's ruling ends the arm contest; folds/deletions → **→Z-8**; `--dump-sm` RULES vestige **VERIFIED** (0 refs) — retirement ask rides Z-8.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet · Claude Fable
**Architecture:** `ARCH-ICON.md` · `ARCH-SCRIP.md` (ζ tier glossary) · `ARCH-ZETA-LOCAL-STORAGE.md` · `ARCH-ICON-RTX.md` · `DESIGN-ICON-SUSPEND.md` · `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md`

## Session-close / push protocol
See RULES.md — `scripts/handoff_status.sh` verbatim stdout is the ONLY sanctioned completion claim.
