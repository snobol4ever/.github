# ⛔⭐⭐⭐ GOAL-ICON-100 — SCRIP ICON x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

Re-chartered 2026-08-15 s230 by Lon in-chat: *"The system has not been properly converted to the THREE ZETAS. Rethink and re-organize the entire plan to get Icon to 100%."* s229 charter + 15-file consolidation ledger: `.github` git `cc418744`; pre-consolidation texts `835f4131`. **PRUNED 2026-08-21 s247, Lon-directed: completed rungs/cursors deleted, prose compressed; every deleted word is verbatim in git history (`git log -p GOAL-ICON-100.md`). Still-binding leads re-homed into the rungs below.**

## ⛔⛔⛔ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat)
**██ No session creates ANY new global — file-scope mutable state, pinned VA slot, exported cell, parallel array, or equivalent — in ANY repo without FIRST asking Lon in-chat that session. State rides registers (r10/r11 wires) and the stack. ██** The ask MUST be a large ⛔ banner (name, type, owning file, purpose, why registers/stack cannot carry it); a quiet ask does not count. Commits adding one without a cited grant are REJECTED on sight (precedent s55).

---

## ⭐⭐⭐ THE MODEL — ONE SYSTEM: THE THREE ZETAS (Lon s230; tier glossary `ARCH-SCRIP.md`; ⛔ NO DUAL STORAGE ARMS IN THE END STATE)
| tier | motion | Icon embodiment (target) |
|---|---|---|
| **ζ-STANDING** | pinned for activation/process life | per-graph activation base: wire header (γ/ω/caller-base) + params/locals ×16B, established once at α; process-lifetime: GVA globals off-stack, statics, coexpr pthread stacks |
| **ζ-ACTIVATION-FRAME** | comes and goes | per-suspension generator activations chained off STANDING, self-pop at ω; scan `?` environments (&subject/&pos save-restore); deep arrivals |
| **ζ-SPINE** | slides | per-BB FORTH cells on RSP: α grants `sub rsp,K`, zero intermediate pops, ONE terminal release; LVA locals/params/return + det value family |

**Register contract:** RSP = FORTH spine · RBP = pinned STANDING/FRAME base per predicate · **R13/R14/R15 = Σ/δ/Δ RESERVED (scan family)** · **R12 FREE** (first claimant documents here + gates) · RBX = heap bump-frontier · RCX/RDX = γ/ω wires at proc enter, args via `g_call_args[]` · RAX scratch · R9 globals (GVA).
**Lon's architecture rule (s242):** RSP fixed offsets are the default, right up until compile-time depth is unknowable/unbounded — only then does a BB own an RBP frame, built at its own α, torn down at its own ω, LIFO. Icon's only dynamic-growth sites are **generator procedures and co-expressions**; scan, every, to-by, disjunction are depth-static → RSP.
**Canonical sources law (every session):** `refs/jcon-master/tran/irgen.icn` (43 `ir_a_*` four-port topologies) + `refs/icon-master/src/runtime/*.r` (`interp.r` efp/gfp mark/esusp = THE suspension precedent). `refs/` is gitignored and NOT auto-populated — repopulate (Lon's chat uploads `2-icon-master.zip`/`3-jcon-master.zip`; local seats: `/home/resources/`) and verify `ls refs/jcon-master/tran/irgen.icn` before trusting.

## ⛔ DEFINITION OF DONE — "100%" (there is ONE system)
1. `scripts/scorecard_icon.sh` META = 100.0 ⟸ `test_icon_all_rungs.sh` **293/0/0** m3; oracle = Arizona `icont -s prog.icn -x` / pinned `.expected` (⛔ NEVER Java/JVM — Lon 2026-07-21; JCON real formatting where the rung36 corpus is JCON's).
2. **m3 ≡ m4 at 293/0/0** (m4 runner = `scripts/test_icon_mode4_rung.sh`, rebuilt s230; first honest m4 watermark 234/29/30 differed from m3 by exactly `rung36_jcon_proto`).
3. **Benchmarks honest-green:** `honest_icon_correctness.sh` 8/8 vs live iconx built once per session from `refs/icon-master` (`test_icon_bench_corpus.sh` ⛔ superseded — s164 rsg lesson). s230 baseline 0/8: 7 zero-output (link-library/early-exit class, cf. Z-8), micsum 2-line near-miss.
4. **THREE ZETAS complete:** Z-2..Z-4 landed (Z-1 ✅); **zero env-var storage arms remain** (`SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME`/`SCRIP_ICN_LEGACY` folded byte-inertly at Z-8); ZK-8 100% assertion gate green.
5. **Gates strict and green:** all 11 `test_gate_icn_{no_stack,one_reg_frame,semicolon_required,rbp_census_ratchet,zk5_gva,zcells_gva,local_no_nv,global_no_nv_m3,scan,var,tag_single_source}.sh` (global_no_nv_m3 at the sanctioned 240s timeout) + TEMPLATE-ONLY/BOTH-MEDIUM greps == 0 + R-ICN-D SN4+Prolog watermark invariance on every shared-emitter commit + `handoff_status.sh` COMPLETE.

## THE INSTRUMENT (one; boards are RUN, never transcribed; re-derive every count fresh — never from prose)
```
bash scripts/scorecard_icon.sh run [--suites a,b] [--out DIR] | report [DIR]   # META = 100·Σ(w·pass/total)/Σw
bash scripts/test_icon_all_rungs.sh          # m3 suite; SCRIP_ICN_CELLS=1 / SCRIP_ICN_ZFRAME=0 spot-reads REMAIN until Z-8
bash scripts/test_icon_mode4_rung.sh         # m4 suite
SCRIP_ZD_CENSUS=1 / SCRIP_ZD_DIAG=1          # arming census / planner-refuse discriminator
```
Weights (Lon's knob, ONE authority in the script header): rungs_m3 25 · rungs_m4 20 · rungs_m3_cells 10 (retires at the Z-8 fold) · bench_correct 15 · smoke 10 · crosscheck 5 · gates 10 (Σ95). XFAIL counts in the denominator (end state 293/0/0).
**⛔⛔ MEASUREMENT HYGIENE (minted s230, amended s236):** (1) **PIN COMMIT + BINARY** — record `git rev-parse HEAD`, clean `git status`, and `scrip` mtime with every watermark; re-check the mtime immediately before EVERY run, not once per session. (2) `pgrep -c scrip`==0 and clean tree BEFORE and AFTER a suite; if the tree moved the run is VOID. (3) **TWO clean agreeing runs on the same pinned binary** is the bar for a number entering this file. (4) ONE WORKER PER TREE — unsatisfiable by luck (STANDING CONDITION): **separate clones are the default**. (5) A language-invariance sweep must cover `crosscheck/` and `probe/`, not just `programs/<lang>/` (s244 sampling trap). (6) `scrip` is a thin driver — **the emitter lives in `out/libscrip_rt.so`** and `LD_LIBRARY_PATH` OVERRIDES RUNPATH; a copied baseline binary still loads the patched `.so` unless pointed at a pristine one, so **always prove the baseline reproduces the defect before trusting an identity board** (s244). Bug-finding: MONITOR-FIRST = cheapest discriminating experiment (passing sibling vs failing probe, emitted-asm diff), then `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` + gdb spin/ignore-count breakpoints (HW watchpoints do not work in this container). **INHERITED-CLAIM LAW:** never spend on a recorded diagnosis without re-running its repro first.

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY (`x86(...)` sole producer; missing encoder ⇒ add it in `x86_asm.h`) · ONE-AUTHORITY constants (line numbers drift daily — re-grep) · PEERS RULE (no BB_t fields; `ir_operand_push`) · NO-LANGUAGE-IDENTITY past LOWER (behavioral names only) · ZERO C BYRD BOXES (`icn_bb_dcg` sole exemption) · **ICON SEMICOLON-REQUIRED, ZERO newline processing** (prison gate, 3 locks) · CONSULT CANONICAL SOURCES every session (THE MODEL) · killswitch per behavioral family, `=0` byte-identity is a completion criterion · **R-ICN-D: SN4 + Prolog watermark invariance on EVERY shared-emitter commit** · struct fields APPENDED AT END (s141 ABI law) · REGENS ×3 + `update_icon_bench_asm.sh` (DEMO+BENCHMARK only — ⛔ never `corpus/programs/icon/` rung tests) when codegen touched · O0-DEV / O2-BENCH-ONLY, label every perf number with RT_OPT · timeouts 8s/30s, `< /dev/null` on scrip calls · `git pull --rebase` before every push, re-prove watermark after rebase · push mid-session per rung; credential = ask Lon in-chat and wait · LIVE CURSOR moves every handoff · "HANDOFF COMPLETE" only from `handoff_status.sh` verbatim stdout.

## ⛔⛔⛔ FACT RULE — ICON+PROLOG TRIPLE-ARM CANARY ON EVERY SHARED-EMITTER COMMIT (Lon s230, RATIFIED)
**██ Every commit touching `src/emitter/`, `src/templates/`, or `x86_asm.h` MUST run the Icon canary `write("hello")` on every live arm AND the Prolog honest suite BEFORE commit; Icon-touching rungs run the full Icon suite. ██** Evidence: 4 recurrences of the Icon-dies-off-ledger class; the couplings run BOTH ways (one frame-base lea killed Icon AND Prolog). ⛔⛔ **EXTENDED s236 — BINDS ON THE PULL SIDE TOO:** canary ×3 arms after every `git pull --rebase`/rebuild — a sibling commit can flip a codegen default without naming Icon (precedent `1d98f9d2`). **A pull is a shared-emitter event.**

## ⛔⛔⛔⭐⭐⭐ STANDING CONDITION — `GOAL-SNOBOL4-100` RUNS CONCURRENTLY AND WRITES TO ALL FOUR REPOS (Lon s236)
A sibling campaign is live at the same time as you, committing to `SCRIP`, `corpus`, `.github`, `x64` under the SAME `LCherryholmes` identity. Permanent property of the environment; binding consequences:
1. **Separate clones are the DEFAULT for measurement**; treat the shared checkout as read-only except when landing a commit. Do not wait for a quiet tree — there isn't one.
2. SN4 commits can move the Icon watermark without mentioning Icon (shared emitter) — R-ICN-D + the pull-side canary exist for exactly this.
3. A number without a pinned hash is worthless within minutes — `git fetch` before trusting any recorded watermark.
4. Do NOT read an SN4/Prolog delta as an Icon regression without checking the sibling goal's cursor first (their numbers move for their own reasons).
5. NEVER `git checkout .`/`stash`/`reset --hard`/revert/stage a file you did not modify; never force-push — a dirty file is likely the sibling mid-edit.
6. `.github` is shared by both goals: append cursors at top with your session id, commit small and often, let `git pull --rebase` merge; expect mid-session rebases and re-prove your watermark after each.
7. Attribute by session id + timestamp (`git reflog --date=iso`, commit tags `[ICN-…]` vs SN4's), never by author.

## Session Setup (every session)
```bash
# STANDING CONDITION first: the SN4 sibling may be writing these repos RIGHT NOW — measure in your own clone.
git clone https://github.com/snobol4ever/.github <root>/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r <root>/$r; done
# Set identity in EVERY repo you will commit to (SCRIP, corpus AND .github — plain commit dies without it):
git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
git status --short; git reflog --date=iso | head -5     # dirty file or foreign commit = sibling live → clone elsewhere to measure
bash scripts/install_system_packages.sh; make libscrip_rt && make scrip   # header edits NOT dep-tracked: rm -rf out/rt_pic first
mkdir -p refs && unzip -q 2-icon-master.zip -d /tmp/i && unzip -q 3-jcon-master.zip -d /tmp/j && cp -r /tmp/i/icon-master /tmp/j/jcon-master refs/ && ls refs/jcon-master/tran/irgen.icn   # verify before trusting
printf 'procedure main(); write("hello"); end\n' > /tmp/h.icn && for e in "" "SCRIP_ICN_CELLS=1" "SCRIP_ICN_ZFRAME=0"; do env $e ./scrip --run /tmp/h.icn </dev/null; echo "$e rc=$?"; done   # canary ×3 until Z-8
bash scripts/test_icon_all_rungs.sh 2>/dev/null | tail -1   # fresh watermark FIRST
```

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-21 s247 (Claude Fable 5, seat1) — **ORIENTATION + LON-DIRECTED FILE PRUNE. Watermark RE-PROVEN 247/16/30 at `ebbc1c89`. Next: Z-3 slice 2.**
- **Commits:** SCRIP NONE · corpus NONE · `.github` this prune+cursor. ⛔ **PUSH STATUS: read `handoff_status.sh`, never this file.**
- **WATERMARK — TWO AGREEING RUNS:** Icon m3 **247/16/30** at SCRIP `ebbc1c89` · binary mtime 09:59 verified unmoved across both · fail sets byte-identical · `git status` clean in all four repos · `pgrep -c scrip`==0 · canary rc=0 ×3 arms · RT_OPT `-O0`. Fail set identical to s246's: **9 D2-suspend** (`rung03_suspend_{gen,gen_compose,gen_filter,return}` · `rung36_jcon_{genqueen,level,recogn}` · `rung37_{proc_lookup,subscript_genproc}`) + **3 D3-scan** (`rung36_jcon_{scan,scan1,scan2}`) + **4 Z-5** (`rung36_jcon_{args,coerce,cxprimes,var}`). The 4 days of sibling commits `743eaf6f..ebbc1c89` are Icon-invariant.
- **Verified in-tree this session:** Z-1 LANDED (`lower_icon.c:1228` stamps `icn_cells_graph=1` by default, `SCRIP_ICN_LEGACY=1` restores s229 routing) · Z-3 slice 1 opt-in present (`emit.cpp` `SCRIP_ICN_GENFRAME`, ~:3662) · Z-5(a) DONE (`test_gate_icn_tag_single_source.sh` GREEN; rtx files now live in `src/runtime/rtx/`).
- **⛔ NEXT: Z-3 slice 2** (see rung — retention + caller handshake as ONE commit).

## ⛔ PREVIOUS CURSOR — 2026-08-17 s246 (Claude Sonnet 4.6) — ICN-ENDETAB: 246/17/30 → 247/16/30 at SCRIP `743eaf6f`
Two defects in `rung36_jcon_endetab`: (1) zero-arg `entab()`/`detab()` fast-dispatch `goto L_bidjmp_*` jumped past the shared `nargs==0 → error 103` guard into APPLY_fn's error 5 — guard duplicated after each jump label; (2) `detab`'s copy loop miscounted the column after `\b`/`\n`/`\r` — ported canonical `fstr.r` three-arm handling (entab already had it). R-ICN-D verified (SN4 smoke 6/1 · Prolog smoke 3/2, byte-identical to pristine).

s229–s246 cursor blocks deleted 2026-08-21 (s247 prune) and preserved verbatim in `.github` git history (`git log -p GOAL-ICON-100.md`); still-binding leads re-homed into the rungs below. Do not re-derive claims from prose — re-run the repro.

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity where applicable, watermark (two agreeing pinned runs) + SN4+Prolog invariance, cursor move, push mid-session

**✅ LANDED (delete-log; killswitches noted):** Z-0 instrument+ground-truth · **Z-1 arm unification** (cells default; `SCRIP_ICN_LEGACY=1` transition killswitch until Z-8) · **Z-3 slice 1** `b1fc1ac4` (opt-in `SCRIP_ICN_GENFRAME=1` establishment) + γ-half no-unwind epilogue s243 (`xa_flat_zframe_epilogue_γ_str` cells-gen branch: marshal rdi:rsi, γ wire from `[rsp+kt-24]`, `gen____` in rax, no `add rsp,kt`; ω keeps the unwinding fall-through) · **Z-5(a)** DT_DATA tag single-source (+gate) · **Z-5(b)** mathfunc `BINOP_CONCAT_FRACDIGIT` at LOWER · ICN-LCL-SEED (`SCRIP_LCL_SEED=0`; NULVCL-seed `[zls_g_locals, zls_g_region)` BEFORE the install call — a seed after the bind NULVCLs just-bound params) · ICN-PAD-CONV (left/right/center real-rendering; scope held to 3 sites — widening to TRIM/REVERSE/REPL is a cross-language rung) · ICN-SCAN-RSP · ICN-WIRE-PAIR (`SCRIP_WIRE_PAIR_FRAME=0`; discriminator is `jmp_entry && !dyn_scope`, NOT `jmp_entry` alone) · ICN-M4-GCWARMUP (`core_lib_init` drives `rt_gcheap_warmup`) · ICN-VARARG (`SCRIP_VARARG_TAIL=0`; tail rule at `rt_proc_call_prologue_lex`, the one authority holding proc record + true nargs) · ICN-GEN-GAMMA-RETAIN (γ-half) · ICN-CSET-NUMREAL (`binop_is_num_real` misclassified CUNION/CDIFF/CINTER) · ICN-ENTAB-NXTTAB + ICN-ENDETAB · topology table **`ICON-IRGEN-TOPOLOGY.md`** s236 `5689e4a3` (all 43 `ir_a_*` rows, re-derive recipe embedded).

- [ ] **Z-2 · ζ-STANDING COMPLETE.** Per-graph activation base on the shared mechanism: wire header (γ/ω/caller-base), params/locals install, return protocol — verified against the SN4-proven establishment (behavioral names only). Gates local_no_nv + rbp_census_ratchet green; `test_gate_icn_var.sh` re-read (its RED overlaps the var/globals classes — re-derive post-Z-1).
- [ ] **Z-3 · SUSPENSION = ζ-ACTIVATION-FRAMES — THE RUNG (expected yield ≈10 FAILs: the 9 D2-suspend + `cxprimes`, which uses `suspend` and is NOT independent).**
  - **SLICE 2 = THE CORE, stated from measurement:** (i) γ must RETAIN the activation, only ω frees it; (ii) the caller handshake must agree with the retained base — today the caller's save/restore slot (`mov [rsp+88],rsp`) sits INSIDE the generator's retained frame, `add rsp,8` shifts into that frame, and `mov rsp,[rsp+88]` at β resume reads 8 bytes off → rsp=0, SEGV at `n31_proc_gen_β+10`. The caller must continue with rsp BELOW `gen____` so the retained frame is inviolate. Fix is in whichever template emits `n31_proc_gen_β` (`bb_proc_gen.cpp` does NOT exist — grep `proc_gen` in `src/templates/`). **Retention + handshake land as ONE commit or neither; then flip `SCRIP_ICN_GENFRAME=1` default ON.**
  - ⛔ **FALSIFIED (s245/s245b — do not re-try as a patch):** `SCRIP_ICN_GENFRAME=1` alone still SEGVs. Under γ-retain the callee arrives with rsp on its own retained frame, so caller `FRQ(H)`/`FRQ(H+8)` alias one address, and `n?_suspend_α` never materialises the DESCR into rax:rdx (contract: `src/runtime/rtx/rtx_icngen.S`). **A retained activation cannot live on the shared spine** — the off-spine move IS the rung. Then replace `icn_zframe_gen` + `rt_gen_save_wires` (one-active-suspension hack) with real per-activation frames chained off STANDING, self-pop at ω.
  - ⛔ **READ FIRST:** `refs/icon-master/src/runtime/interp.r` efp/gfp mark/esusp + each construct's `ir_a_*` topology in `refs/jcon-master/tran/irgen.icn` + **`ICON-IRGEN-TOPOLOGY.md`** — its D1/D2/D3 split: pure-D1 (runtime resume TARGET) is 39 PASS / 0 FAIL; every current failure is D2 (`ir_Succeed`) or D3 (`ir_ResumeValue`/`ir_ScanSwap`) — **state retained across γ is the defect, arrival-depth dynamism per se is not.**
  - ⛔ **INSTRUMENT:** run each witness under **m4** — most SEGV there with the faulting instruction readable straight from the emitted `.s` (m3 silently prints wrong output). Direct-consumer probe first: `rung08_strbuiltins_match` (`"hello" ? write(match("hel"))` → `4`) + the three `corpus/probe/icn/witness_icn_carve_leak_*.icn`; a correct slice turns BOTH green at once (s233: you cannot buy one with the other; `_lucky` passes BY ACCIDENT — it is the control).
  - ⛔ **RETIRE-TOGETHER SET (s233; a slice that moves the retained cell off the spine MUST retire all six in the SAME commit — each compensates a carve persisting past γ; line numbers drift, re-grep):** (1) `emit.cpp` IR_SCAN_TAB `op_sa += fck` (killswitch `SCRIP_NO_CM_CARVE=1`); (2) IR_SCAN_MOVE identical twin; (3) IR_SCAN_MATCH windowed `op_fc_base = op_off+16` (no `op_sa` bump); (4) `op_zdepth = op_fc_bytes` per-box compensation model; (5) `x86_asm.h` γ-release gate `op_fc_base < 0 || op_zgpop > 0`; (6) the three-class discipline comment (its "gamma-live state" rationale is TRUE for tab/move, FALSE for match; the split stops mattering off-spine).
  - Then WALK THE FAMILY, one rung-let per kind citing its topology: `IR_TO_BY · IR_DISJUNCTION · IR_REPALT · IR_PROC_GEN · every/limit/bang`. Validation: Ir + peak RSS on the witness set. Frames, no pthread-per-generator (Lon s230); pthreads remain coexpr-only.
- [ ] **Z-4 · SCAN ENVIRONMENTS ON THE FRAMES — ⛔ STRICTLY DOWNSTREAM OF Z-3.** Root cause (proven s232/s233): scan-family boxes never release their FORTH carve on the SUCCESS edge — β needs the saved cursor in that cell, the γ-release is excluded BY CONSTRUCTION (`x86_asm.h` `op_fc_base < 0` gate), and retaining the cell on the spine shifts every later rsp-relative ref. The release-only escape is FALSIFIED (direct consumers read match's result through an address calibrated on the carve persisting); Δ-establishment as cause is FALSIFIED (single-block `pos(0)`/`pos(-1)`/`*&subject` oracle-exact). **Fix = the cell moves OFF the spine into a ζ-ACTIVATION-FRAME (Z-3 core) with the retire-together set in the same commit, or not at all.** Spec: `ir_a_Scan` ScanSwap topology + `fscan.r`/`fstranl.r`; `?` environment = &subject/&pos save-restore as activation-frame slots; Σ/δ/Δ once-at-entry establishment (ONE both-medium site at entry — NOT the read, NOT xa_flat's shared prologue); position-returners δ-untouched vs cursor-movers reversed-on-resume (ARCH-ICON — do not blur). Witnesses: `jcon_scan`/`jcon_scan1` SEGV under m4 (readable fault site) — work the crashers first. ⛔ `jcon_scan2` is NOT a scan program (s242): its gap is **`break expr` value propagation** — `lower_icon.c` TT_LOOP_BREAK (~:534) wires expr's γ AND ω to `exit_goto`, value never reaches a result slot, β never re-pumped; fix at LOWER (loop gets a result slot, γ routes to exit, expr's β stays resumable for `every`), depth-static → RSP. Likely shared cause with D2-suspend ("value escapes the construct"). The 7 falsified scan hypotheses at `835f4131` are do-not-retry. Gate scan green; smoke 14/14.
- [ ] **Z-5 · FAIL-ZERO — remaining classes, one CLASS per commit, witness minted, oracle-verified, RE-MEASURE EACH FIRST.**
  - `args` — variable-arity APPLY `(!plist) ! alist` is the only remaining blocker (s245; ICN-VARARG closed the trailing-`[]` half). Presents as runaway: 16.7M lines → `[WSI] workspace island exhausted` abort.
  - `coerce` — ⚠ 241MB output, use `head`; MONITOR-FIRST fresh.
  - `cxprimes` — uses `suspend` → belongs to Z-3; also bignum (Z-7 cluster).
  - `var` — FATAL `emit_drive` IR_ASSIGN guard: nameless 2-operand assign (assign-through-lvalue-producer `!x`/`?x`/`s[i:j]`) = the ASSIGN-LV rung of `GOAL-IR-IMMUTABLE-EMIT.md`; plus the `variable()` name-table half (AWAITING LON).
  - **Leads banked, none started:** zero-arg BUILTIN routing — `reads()` (and the general case) raises error 5 via failed user-proc lookup instead of reaching by-name `nargs` validation; vicinity `bb_call_route_classify` (emit.cpp, high blast-radius — isolate the exact differing branch vs `write()` before patching; the entab/detab-specific case was closed s246 by bidjmp guards; re-derive whether the general case persists) · `image()` on a real renders SPITBOL-convention (`by_name_dispatch.c` ~:5710 — same class as the mathfunc fix) · `rt_pl_dc_ok` could admit under-supplied calls with a NULVCL pad instead of refusing to the legacy flat-glue arm (would retire that arm for the shape) · anycmp mixed-type ordering stays deliberately-unfixed (ruling on record).
- [ ] **Z-6 · m3 ≡ m4 — nearly free.** Measured delta is ONE program (`rung36_jcon_proto`, emit-fail under `--compile`) plus the shared classes Z-3/Z-4/Z-5 close anyway. Fix jcon_proto's emit path; hold DIRTY-PASS at 0; `.s` regen freshness discipline; sweep the COMPILER never the artifacts.
- [ ] **Z-7 · XFAIL-ZERO — 30 markers.** Per marker: remove, run, fix-or-delete. Clusters: bignum (radix/lgint/arith/checkfpx/cxprimes — arbitrary-precision ints, own sub-rung, ⛔ Lon scopes it) · ck (generator-in-arg to Image()) · profsum (`next` inside scan-body must restart enclosing while) · ~24 empties to classify fresh (cxprimes ran 25/25 once under s18).
- [ ] **Z-8 · BENCH HONEST-GREEN + GATES STRICT + THE FOLD.** `honest_icon_correctness` 8/8 vs live iconx; BENCH-1 link-library resolution + deal/rsg seed-pin oracles (`options/post/shuffle` are LINK-DEP LIBRARIES, no main — grade accordingly); confirm rsg end-to-end. Then ZK-8 100% assertion gate (`test_gate_icn_zcells.sh`: whole-graph carve 0 · .bss backtrack arenas 0 · heap-ζ for generators 0 (coexpr exempt) · locals/params=cells · globals=GVA · R13/14/15 grep 0 · `=0` byte-identity · suite-floor ratchet). Then THE FOLD, one family per commit, byte-identical OFF→deleted: `SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME`/`SCRIP_ICN_LEGACY` env reads · `zframe_graph`/`icn_cells_graph` dual-stamp scaffolding · `rt_gen_save_wires` family · dead ZC_FRAME RBP arms (s202 evidence favors deletion).

## ⛔ AWAITING LON (rulings owed to this goal)
- **Exit-status divergence (deliberately not fixed):** when `main`'s last expression FAILS, SCRIP exits 1; Arizona (`imain.r:151`) and JCON both exit 0 unconditionally, and SPITBOL agrees. Site `bb_glue_flat.cpp` `bb_glue_outer_ω` is SHARED BY EVERY LANGUAGE (5 of 45 sampled SN4 programs currently exit 1, intent unaudited) — do not flip blind; the architecturally correct fix is at LOWER (behaviorally-named per-graph flag, struct-end append). Invisible to the runner (stdout-only compare) — justify on semantics, not score.
- **`variable()` name-table:** sealed RO blob vs startup table (Z-5 `var`, second half).
- **Z-1 gate rewrite ratification:** the s230 worker rewrote `test_gate_icn_{local_no_nv,global_no_nv_m3}.sh` (LVA-1 LOCK 2: `[r12+off]` → accept rsp refs, ban r9) coherently with the cells regime — operative since, but the relaxation was flagged as Lon's call and never explicitly ratified.
- **Bignum scope** (Z-7 cluster).
- **Retire RULES' vestigial `--dump-sm` gate line** (0 refs, verified s230) + old r12-as-TOS wording.
- **R-1 (the s229 process/FACT-RULE ask):** its "STANDING PROCESS ASK" section is ABSENT from this file — dangling since consolidation (not present at `cc418744` either). Re-issue or retire.

## OWED (small, non-blocking)
- BINOP_CONCAT gate re-spell: `grep -rn '== BINOP_CONCAT\b' src/` counts its own comment + the `binop_is_concat` definition (`gen.h:28-29`) — exclude the predicate's definition site.
- Prolog harness denominator: `test_prolog_bb_honest.sh` silently skips `rc != 0` runs — 105–108 flakiness band on identical binaries. Fix before trusting its number (do not read the band as a delta).

## PARKED — PERFORMANCE / ARCHITECTURE, AFTER 100% (history at `835f4131`; do not open before DoD 1–4 green unless Lon directs)
ICON-RTX ladder open queue (landed arms + REFUSED/DO-NOT-PORT rulings in `RTX-CLAIMS.md` + git; RTX-23/25 ⛔ BLOCKED ON LON) · GN-PERF slot-vs-nv A/B (GVA won; NV remains the reflective/cross-language binding via `NV_bind_gva`) · `-O2` runtime pass (O2-BENCH-ONLY) · JCON-compiler-modules byte-compare (data-level, never JVM) · the perf graveyard (tolower guard / always_inline / asm rt_subscript_var / value-subscript fusion / pointer-keyed memo UNSOUND / goto-past-chain UNSOUND / 288-arm guard trap) — binding, do not re-derive.

## LEDGER
s229 R-0..R-7 re-verified and dispositioned s230, absorbed into the Z-rungs; per-rung text at `cc418744` (pre-consolidation `835f4131`). R-1 carried in AWAITING LON.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet · Claude Fable
**Architecture:** `ARCH-ICON.md` · `ARCH-SCRIP.md` (ζ tier glossary) · `ARCH-ZETA-LOCAL-STORAGE.md` · `ARCH-ICON-RTX.md` · `DESIGN-ICON-SUSPEND.md` · `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` · `ICON-IRGEN-TOPOLOGY.md`

## Session-close / push protocol
See RULES.md — `scripts/handoff_status.sh` verbatim stdout is the ONLY sanctioned completion claim.
