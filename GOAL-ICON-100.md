# ⛔⭐⭐⭐ GOAL-ICON-100 — SCRIP ICON x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

**Re-chartered 2026-08-15 s230 by Lon in-chat (Fable 5 seat), verbatim in substance:** *"We have an entirely new system. No need to bisect anything when we know what is wrong. The system has not been properly converted to the THREE ZETAS. Rethink and re-organize the entire plan to get Icon to 100%."* Every s229 rung/step was re-verified against SCRIP `ee315070` source + a fresh triple suite run this session; dispositions in LEDGER v2 at bottom. The s229 charter + 15-file consolidation ledger live in `.github` git history at `cc418744`; pre-consolidation texts at `835f4131`.

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔
**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. ██** Enforcement: every diff checked for new file-scope definitions; a commit adding one without a cited in-chat grant is REJECTED on sight (precedent: g_pcall/g_pcall_wires/RT_AB_ANCHOR eradication, s55). **THE ASK ITSELF MUST BE A BANNER** in chat (name, type, owning file, purpose, why registers/stack cannot carry it).

---

## ⭐⭐⭐ THE MODEL — ONE SYSTEM: THE THREE ZETAS (Lon's s230 ruling; tier glossary `ARCH-SCRIP.md`; ⛔ NO DUAL STORAGE ARMS IN THE END STATE)
The SN4-100 campaign built the zeta machinery in the SHARED emitter (ζ-activation slots, `emit_match_rbp` frames, defer records riding activation-frame slots, GVA, ZD staging). **Icon is NOT converted:** at `ee315070` it still routes through pre-zeta regimes — the `SCRIP_ICN_ZFRAME`/`SCRIP_ICN_CELLS` env-var arms stamped per-graph in `lower_icon.c` (~:1422, R-ZK-A site), `flat_lcl_proc`, and `icn_zframe_gen`'s `rt_gen_save_wires` hack whose own stamp comment states it supports **ONE active suspension at a time**. The conversion IS the plan; the residual FAILs decompose onto the unconverted tiers (measured s230, table below).

| tier | motion | Icon embodiment (target) | at `ee315070` (VERIFIED by grep + fresh run, s230) |
|---|---|---|---|
| **ζ-STANDING** | pinned for the life of its activation/process | per-graph activation base: wire header (γ/ω/caller-base) + params/locals ×16B, established once at α; process-lifetime: GVA globals OFF-STACK (`gva_register`), statics, coexpr pthread stacks | zframe establishment alive again post-`ee315070` (ZB-FC-1: the frame-base lea silently emitted nothing — also resurrected Prolog); GVA landed (ZK-5) and **VERIFIED WORKING ON THE DEFAULT ARM s230**: rung21/rung25 globals PASS 6/6 standalone and in-suite, gate `global_no_nv_m3` GREEN at the sanctioned 240s timeout. The earlier "globals fail on default" reading was a CONTAMINATED MEASUREMENT (concurrent suite run) — see MEASUREMENT HYGIENE. Residue: the two arms are now BEHAVIORALLY IDENTICAL (same 28-program fail set), so unification is a DELETION exercise, not a behavior change |
| **ζ-ACTIVATION FRAME** | comes and goes | per-suspension generator activations chained off STANDING, self-pop at ω; scan `?` environments (&subject/&pos save-restore); deep arrivals | **NOT CONVERTED** — `rt_gen_save_wires` one-active-suspension; witnesses FAIL: rung03_suspend_{gen,gen_compose,gen_filter,return} · recogn · level · proc_lookup · subscript_genproc · genqueen · meander (10) + scan cluster (5 suite + 2 smoke + subjpos). ⛔ NEW INSTRUMENT s230: under the rebuilt m4 runner these SEGV (14 crashes) where m3 silently prints wrong output — the faulting instruction is readable straight out of the emitted `.s`, making m4 the MONITOR-FIRST entry point for Z-3/Z-4 |
| **ζ-SPINE** | slides | per-BB FORTH cells on RSP: α grants `sub rsp,K`, zero intermediate pops, ONE terminal release; LVA locals/params/return + det value family | ZK-1..5 LANDED (`zd_wl_kind` one-authority, ZK-2/ZK-5 arms live) — the cells arm is today's best (235/28/30) and becomes THE substrate of the one system, not an arm |

**Register contract (unchanged):** RSP = FORTH spine · RBP = pinned STANDING/FRAME base per predicate · **R13/R14/R15 = Σ/δ/Δ RESERVED for scan family** · **R12 FREE** (Lon 2026-08-07; first claimant documents here + gates) · RBX = heap bump-frontier · RCX/RDX = γ/ω wires at proc enter, args via `g_call_args[]` · RAX scratch. Canonical sources law: `refs/jcon-master/tran/irgen.icn` (43 `ir_a_*` four-port topologies) + `refs/icon-master/src/runtime/*.r` (`interp.r` efp/gfp mark/esusp is THE suspension precedent) — Lon supplies the archives as chat uploads (`2-icon-master.zip`/`3-jcon-master.zip`); unzip so `ls refs/jcon-master/tran/irgen.icn` succeeds before trusting.

## ⛔ DEFINITION OF DONE — "100%" (arm-choice decision memo DELETED — there is ONE system)
1. **`scripts/scorecard_icon.sh` META = 100.0** ⟸ `test_icon_all_rungs.sh` **293/0/0** on THE system, m3; oracle = Arizona `icont -s prog.icn -x` / pinned `.expected` (⛔ NEVER Java/JVM — Lon 2026-07-21; JCON real formatting where the rung36 corpus is JCON's).
2. **m3 ≡ m4 at 293/0/0.** The m4 runner was REBUILT AND LANDED s230 (Z-0(a) DONE; the shipped one was deleted-era infra — `-icn`+nasm+`/home/scrip` — and its 0/293 measured the harness, not the compiler). FIRST HONEST m4 WATERMARK: **234/29/30**, differing from m3's 235/28/30 by **EXACTLY ONE PROGRAM** (`rung36_jcon_proto`, emit-fail; 28 fails shared). m3≡m4 is therefore ALREADY STRUCTURALLY TRUE — the parity clause reduces to closing the 28 shared classes + that one emit.
3. **Benchmarks honest-green:** `honest_icon_correctness.sh` 8/8 IDENTICAL vs live iconx (build oracle once per session; `test_icon_bench_corpus.sh` ⛔ superseded — s164 rsg lesson). RE-MEASURED s230 against a live oracle (Icon 9.5.25a built from `refs/icon-master`): **0/8, but the failure MODE changed** — no longer EMITFAIL; SCRIP now emits, links and runs, producing zero output on 7 (link-library/early-exit class, cf. BENCH-1) and a 2-line near-miss on micsum (only line 2 differs). That is a measurable baseline, not a wall.
4. **THREE ZETAS complete:** Z-1..Z-4 landed; **zero env-var storage arms remain** (`SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME` folded byte-inertly); ZK-8 100% assertion gate green.
5. **Gates strict and green:** all 10 `test_gate_icn_{no_stack,one_reg_frame,semicolon_required,rbp_census_ratchet,zk5_gva,zcells_gva,local_no_nv,global_no_nv_m3,scan,var}.sh` + TEMPLATE-ONLY/BOTH-MEDIUM greps == 0 + SN4+Prolog watermark invariance on every shared-emitter commit (R-ICN-D; `ee315070` proved the couplings run BOTH ways) + `handoff_status.sh` COMPLETE.

## THE INSTRUMENT (one; boards are RUN, never transcribed; re-derive every count fresh — never from prose)
```
bash scripts/scorecard_icon.sh run [--suites a,b] [--out DIR]    # the periodic progress reading Lon asks for
bash scripts/scorecard_icon.sh report [DIR]                      # META = 100·Σ(w·pass/total)/Σw
bash scripts/test_icon_all_rungs.sh                              # m3 suite; SCRIP_ICN_CELLS=1 / SCRIP_ICN_ZFRAME=0 spot-reads REMAIN until Z-8 folds the arms
SCRIP_ZD_CENSUS=1 / SCRIP_ZD_DIAG=1                              # arming census / planner-decline discriminator
```
Weights (Lon's knob, ONE authority in the script header): rungs_m3 25 · rungs_m4 20 · rungs_m3_cells 10 · bench_correct 15 · smoke 10 · crosscheck 5 · gates 10 (Σ95). XFAIL counts in the denominator (end state 293/0/0). rungs_m4 feeds from the Z-0 rebuilt runner; the rungs_m3_cells weight retires with the Z-8 fold (Lon's knob). ⛔⛔ **MEASUREMENT HYGIENE + CONCURRENCY (minted s230, and the reason a whole block of s230 numbers is VOID below):** **A SECOND WORKER WAS EDITING AND REBUILDING THE SAME SCRIP TREE DURING THIS SESSION'S MEASUREMENTS.** Forensics: SCRIP `8303fb8f` (22:13:30, same `LCherryholmes` identity) independently rebuilt the m4 runner — the same Z-0(a) task, ~30 min duplicated; `src/emitter/emit.cpp` + `src/contracts/IR.h` + `src/lower/lower_icon.c` were edited 22:17:50–22:18:07 to stamp `icn_cells_graph=1` BY DEFAULT with a `SCRIP_ICN_LEGACY=1` escape (Z-1 work, uncommitted); **`./scrip` was rebuilt at 22:21:38 — in the middle of this session's suite sequence**; and `test_gate_icn_local_no_nv.sh` + `test_gate_icn_global_no_nv_m3.sh` were rewritten at 22:29:46 (LVA-1 LOCK 2 changed from requiring `[r12+off]` to accepting `rsp` refs and banning `r9` — coherent with a cells-default regime, but a GATE RELAXATION that must be Lon's call, not a side effect). LAWS: (1) **PIN THE COMMIT AND THE BINARY** — record `git rev-parse HEAD`, `git status --short` (must be empty) and the `scrip` mtime with every watermark; a number without that provenance is not evidence. (2) **`pgrep -c scrip` = 0 and `git status` clean BEFORE and AFTER** a suite; if the tree moved, the run is void. (3) **Two clean agreeing runs on the same pinned binary** is the bar for a number that enters this file. (4) **ONE WORKER PER TREE** — concurrent sessions must serialize or work separate clones; this session and the other one rebuilt the same script twice and measured each other's compiler. ⛔⛔ **AMENDED s236 — SEE "STANDING CONDITION" BELOW: `GOAL-SNOBOL4-100` RUNS CONCURRENTLY AND WRITES TO ALL FOUR REPOS, SO THIS LAW CAN NO LONGER BE SATISFIED BY SERIALIZING. SEPARATE CLONES ARE THE DEFAULT. DO NOT WAIT FOR A QUIET TREE — THERE ISN'T ONE.** Bug-finding law: **MONITOR-FIRST**Bug-finding law: **MONITOR-FIRST** — cheapest discriminating experiment first (passing sibling vs failing probe, emitted-asm diff), then `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` + gdb spin/ignore-count breakpoints (HW watchpoints don't work in this container). ⛔ INHERITED-CLAIM LAW, re-proven twice this session: the R-3(a) fail-edge root repro and the R-3(c) case-disjunction repro BOTH PASS at `ee315070` — never spend on a recorded diagnosis without re-running its repro first.

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY (`x86(...)` sole producer; missing encoder ⇒ add it in `x86_asm.h`) · ONE-AUTHORITY constants (line numbers drift daily — re-grep) · PEERS RULE (no BB_t fields; `ir_operand_push`) · NO-LANGUAGE-IDENTITY past LOWER (behavioral names only) · ZERO C BYRD BOXES (`icn_bb_dcg` sole exemption) · **ICON SEMICOLON-REQUIRED, ZERO newline processing** (prison gate, 3 locks) · CONSULT CANONICAL SOURCES every session (see THE MODEL) · killswitch per behavioral family, `=0` byte-identity is a completion criterion · **R-ICN-D: SN4 + Prolog watermark invariance on EVERY shared-emitter commit** · struct fields APPENDED AT END (s141 ABI law) · REGENS ×3 + `update_icon_bench_asm.sh` (DEMO+BENCHMARK only) when codegen touched · O2-DIRECTED-ONLY · timeouts 8s/30s, `< /dev/null` on scrip calls · `git pull --rebase` before every push, re-prove watermark after rebase · push mid-session per rung; credential = ask Lon in chat and wait · LIVE CURSOR moves every handoff · "HANDOFF COMPLETE" only from `handoff_status.sh` verbatim stdout.

## ⛔⛔⛔ FACT RULE — ICON+PROLOG TRIPLE-ARM CANARY ON EVERY SHARED-EMITTER COMMIT (Lon 2026-08-15 s230, in-chat: "All your choices. I'm with you on this" — RATIFIED) ⛔⛔⛔
**██ Every commit touching `src/emitter/`, `src/templates/`, or `x86_asm.h` MUST run the Icon canary `write("hello")` on every live arm AND the Prolog honest suite BEFORE commit; Icon-touching rungs run the full Icon suite. ██** Evidence: 4 recurrences of the Icon-dies-off-ledger class (s203 −37 · s207 1/293 · post-s211 drift · s229 1/293), and `ee315070` proved the couplings run BOTH ways — one frame-base lea killed Icon AND Prolog together. Sibling (open): cross-goal watermark table in `handoff_status.sh`. ⛔⛔ **EXTENDED s236 — THE RULE NOW BINDS ON THE *PULL* SIDE TOO, NOT ONLY THE COMMIT SIDE.** With `GOAL-SNOBOL4-100` live in the same repos (see STANDING CONDITION), the likeliest shared-emitter change under your feet is **one you did not write**. **So: canary ×3 arms after every `git pull --rebase` / rebuild, not merely before every commit** — a sibling commit can flip a codegen default without naming Icon anywhere. **Precedent:** `1d98f9d2` "AI-3 ARITH FUSE DEFAULT ON" was SNOBOL4 arithmetic work that landed a codegen default flip with no Icon canary attached; it sat unverified for four commits and was only cleared at s236 (Icon-invariant, 235/28/30, two agreeing runs). **A pull is a shared-emitter event.**

## ⛔⛔⛔⭐⭐⭐ STANDING CONDITION — **`GOAL-SNOBOL4-100` RUNS CONCURRENTLY WITH THIS GOAL AND WRITES TO EVERY `snobol4ever` REPO** (Lon 2026-08-16 s236, in-chat) ⛔⛔⛔

**This is not an incident report. It is a permanent property of the working environment, and it retires the assumption that any Icon seat ever has a tree to itself.** A sibling campaign, **`GOAL-SNOBOL4-100`**, is live *at the same time as you* and commits to **`SCRIP`, `corpus`, `.github` and `x64`** — the same four repos this goal uses. Its seats carry the same `LCherryholmes` identity, so **you cannot tell its commits from your own by author.** Consequences, all of them binding:

1. ⛔ **LAW 4 ("one worker per tree") IS NOW UNSATISFIABLE BY LUCK — SEPARATE CLONES ARE THE DEFAULT, NOT THE REMEDY.** Do not wait for a quiet tree; there will not be one. Build and measure in your own clone (`SCRIP-clean`/`corpus-clean` or equivalent) and treat the shared checkout as read-only unless you are landing a commit. LAW 4 fired in **five consecutive sessions** (s230, s233, s234, s234b, s235/s236) and the mitigation was re-invented each time; it is now written down once, here.
2. ⛔ **THE SHARED EMITTER MAKES THIS BIDIRECTIONAL — SN4 COMMITS CAN MOVE THE ICON WATERMARK WITHOUT MENTIONING ICON.** `src/emitter/*` is common ground. **Precedent, measured s236:** `1d98f9d2` "AI-3 ARITH FUSE DEFAULT ON" was pure SNOBOL4 arithmetic work that flipped a **codegen default** under Icon's feet with no Icon canary attached. It happened to be Icon-invariant (235/28/30 held, two agreeing runs) — but that was *verified*, not lucky, and the verification was owed for four commits. **This is exactly what R-ICN-D exists for, and the sibling goal is its most likely trigger.**
3. ⛔ **A NUMBER WITHOUT A PINNED HASH IS NOW WORTHLESS WITHIN MINUTES, NOT SESSIONS.** During s236 alone, origin moved **twice**: `3b9c9db0 → 1d98f9d2 → 860a9e43`, and then the shared tree moved again to `4fc01bee` mid-session. **`git fetch` before trusting any recorded watermark**, and pin `git rev-parse HEAD` + clean `git status` + `scrip` mtime to every number you record.
4. ⛔ **DO NOT READ AN SN4 OR PROLOG DELTA AS AN ICON REGRESSION SIGNAL WITHOUT CHECKING THE SIBLING GOAL'S CURSOR FIRST.** The SN4 and Prolog figures quoted in this file drift for reasons that have nothing to do with Icon. Measured s236: SN4 broad came in **m3 314/22 · m4 311/23** against a recorded 311/25 · 310/24 — i.e. **improved, by the sibling goal's own work**, and a seat pattern-matching "the numbers moved" would have chased a phantom Icon regression. Prolog's honest count has read **108 → 107 → 105** across three seats for harness/corpus reasons, not codegen ones.
5. ⛔ **NEVER `git checkout .`, `git stash`, `git reset --hard`, revert, or stage a file you did not modify, and never force-push.** A dirty file in the shared tree is very likely the sibling goal mid-edit. **Precedent, s236:** the shared `SCRIP` carried an uncommitted `SCRIP_ICN_FCFLAT` killswitch across `IR_SCAN_TAB`/`MOVE`/`MATCH`; building in place would have compiled a half-finished change and reported it as a watermark, which is precisely how the s230 board was voided. Leave it untouched and clone.
6. **`.github` IS SHARED BY BOTH GOALS** — `GOAL-ICON-100.md` and `GOAL-SNOBOL4-100.md` sit side by side and both cursors get appended. **Append at the top of the cursor stack with your own session id, commit small and often, and let `git pull --rebase` merge** (RULES.md: commit and push freely, no concurrency gating). Expect to be rebased mid-session; re-prove your watermark afterwards rather than carrying the pre-rebase number forward.
7. **ATTRIBUTE BY SESSION ID AND TIMESTAMP, NOT BY AUTHOR.** Use `git reflog --date=iso` and commit-message tags (`[ICN-…]` vs the SN4 goal's tags) to work out who did what; the author field is identical for every seat in both campaigns.

## Session Setup (every session)
```bash
# ⛔ read the STANDING CONDITION above first: a sibling goal (GOAL-SNOBOL4-100) is writing to these
# same repos RIGHT NOW. Clone, then MEASURE IN YOUR OWN CLONE — never assume the tree is yours.
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
# ⛔ CONCURRENCY PRE-CHECK — if either line is non-zero/non-empty, do NOT build here; clone and measure there:
git status --short; git reflog --date=iso | head -5   # dirty file, or a commit you did not author = sibling goal is live
bash scripts/install_system_packages.sh; make libscrip_rt && make scrip   # header edits are NOT dep-tracked: rm -rf out/rt_pic first
mkdir -p refs && unzip -q <Lon's 2-icon-master.zip> -d /tmp/i && unzip -q <3-jcon-master.zip> -d /tmp/j && cp -r /tmp/i/icon-master /tmp/j/jcon-master refs/   # verify: ls refs/jcon-master/tran/irgen.icn
printf 'procedure main(); write("hello"); end\n' > /tmp/h.icn && for e in "" "SCRIP_ICN_CELLS=1" "SCRIP_ICN_ZFRAME=0"; do env $e ./scrip --run /tmp/h.icn </dev/null; echo "$e rc=$?"; done   # canary ×3 until Z-8
bash scripts/test_icon_all_rungs.sh 2>/dev/null | tail -1   # fresh watermark FIRST
```

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-16 s236 (Claude Opus 5) — **THE OWED R-ICN-D CANARY IS RUN: THE ARITH-FUSE DEFAULT FLIP IS ICON-INVARIANT. 235/28/30 RE-DERIVED ON A PROVENANCE-CLEAN PINNED TREE, TWO AGREEING RUNS, AND THE 28 DECOMPOSE EXACTLY 10/6/12. NO COMPILER EDIT.**
- **⛔⭐⭐ FLOOR HAS MOVED SINCE THIS CURSOR'S HEADLINE — READ THIS FIRST. s235 LANDED MID-SESSION AND THIS SEAT RE-DERIVED THEIR NUMBER INDEPENDENTLY.** Everything below is honest **at `860a9e43`** and is the *pre-fix* floor. **The live floor is `4fc01bee`: Icon m3 239/24/30**, reproduced on THIS seat's own clean clone (SCRIP `4fc01bee` / corpus `6288c09c`, tree clean, `pgrep scrip`==0, canary rc=0 ×3) — matching s235's reported number exactly, with **zero new failures** (`comm` against the pinned 28-set, not eyeballed). ⛔ Do not quote 235/28/30 as current; quote it only as the verified pre-fix baseline that made the +4 attributable.
  - **⭐ THE 4 FIXED, AND TWO OF THEM CORRECT THE LEDGER:** `jcon_wordcnt` + `rung37_scan_alt` (D3-scan, as predicted) **plus `jcon_meander` — recorded as Z-3 — and `jcon_mffsol` — recorded as Z-5.** Both were in fact **scan-blocked**. **The recorded 10/6/12 over-attributed to Z-3 and Z-5; true remaining decomposition is 9 D2-suspend / 4 D3-scan (`scan`,`scan1`,`scan2`,`subjpos`) / 11 Z-5 = 24.** Expect the Z-3 yield estimate ("~10 FAILs") to be **9**.

- **Commits:** `.github` this cursor. **SCRIP + corpus UNTOUCHED by this seat — zero source edits, no regens, no R-ICN-D exposure created.**
- **⛔⛔⭐ LAW 4 VIOLATED AGAIN — FOURTH CONSECUTIVE SESSION, AND THIS TIME CAUGHT LIVE MID-EDIT.** A second worker (**s235**) was active inside `/home/claude/SCRIP` *during this session's setup*. Forensics, all computed: clone at `03:02:46` checked out `1d98f9d2`; at `03:27:38` a commit **this seat did not make** appeared — `fa143006 [ICN-Z0 s235] bench harness: fix 3 phantom /home/claude/work/ default paths`; at `03:32:39` `src/emitter/emit.cpp` was left **dirty and uncommitted**. ⛔ The dirty diff is not incidental — it adds a **`SCRIP_ICN_FCFLAT` killswitch to exactly `IR_SCAN_TAB` / `IR_SCAN_MOVE` / `IR_SCAN_MATCH`** (emit.cpp:1230/1231/1236), i.e. **the RETIRE-TOGETHER set**. s235 is mid-flight on the Z-3/Z-4 carve conversion. corpus likewise carried `39ea20f5 [ICN-Z3 s235] witnesses: scan carve shifts LIVE PARAMETER slots`.
  - **MITIGATION TAKEN, and it is the one LAW 4 actually prescribes ("serialize or work separate clones"): this seat built and measured in fresh clones `/home/claude/SCRIP-clean` + `/home/claude/corpus-clean` and did not touch, revert, stage, build or `pull --rebase` the shared tree.** Every number below is therefore free of s235's uncommitted emitter edit. ⛔ Had I built in place I would have compiled a half-finished killswitch and reported it as a watermark — which is precisely how the s230 board was voided.
  - **⭐ PROCESS FINDING, offered to Lon:** three consecutive cursors now open with a concurrency incident, and the mitigation has been re-invented each time. **Recommend LAW 4 get a mechanical form:** a session-setup step that refuses to build when `git status --short` is non-empty at start or when the reflog shows a commit the seat did not author, rather than a prose law each seat rediscovers after the fact. Pairs with s234's proposal to let `handoff_status.sh` own every word about pushed-ness.
- **⭐⭐⭐ THE RESULT — `1d98f9d2` "AI-3 ARITH FUSE DEFAULT ON" IS ICON-INVARIANT, AND THAT WAS AN OPEN CORRECTNESS QUESTION.** s234 flagged it explicitly: a shared-emitter **codegen default flip** had landed with **no Icon canary run against it**, and warned the next seat must rebase, rebuild and re-derive before trusting any number. Done. **PINNED PROVENANCE: SCRIP `860a9e43` (which contains `1d98f9d2` plus s110 `SCRIP_SLIM_PAIR`, default OFF) · corpus `e6b95477` · both trees `git status` EMPTY before and after · `pgrep -c scrip` == 0 before and after · `scrip` built 03:44:21, mtime unmoved across both runs · RT_OPT = `-O0` (O2-DIRECTED-ONLY honoured).**
  - **Icon m3 = 235 / 28 / 30 (TOTAL 293), TWO AGREEING RUNS** on that one pinned binary — the bar the hygiene law asks for — and it **matches the recorded pre-flip watermark exactly**. Triple-arm canary rc=0 with correct output on all three arms (default · `SCRIP_ICN_CELLS=1` · `SCRIP_ICN_ZFRAME=0`).
  - **⭐ THE FAIL SET IS BYTE-FOR-BYTE THE RECORDED DECOMPOSITION, AND IT IS IDENTICAL ACROSS BOTH RUNS** (`diff` clean, not eyeballed): **10 Z-3** (`rung03_suspend_{gen,gen_compose,gen_filter,return}` · `jcon_recogn` · `jcon_level` · `jcon_genqueen` · `jcon_meander` · `rung37_proc_lookup` · `rung37_subscript_genproc`) · **6 Z-4 scan** (`jcon_{scan,scan1,scan2,subjpos,wordcnt}` · `rung37_scan_alt`) · **12 Z-5** (`jcon_{args,coerce,cxprimes,endetab,fncs1,htprep,kwds,mathfunc,mffsol,mindfa,prepro,var}`) = 28. Full list `/tmp/fails_860a9e43.txt`. **No drift, no new class, nothing shuffled between classes** — so Z-3/Z-4/Z-5 scoping survives the flip untouched.
- **⭐ CROSS-LANGUAGE INVARIANCE (the other half of R-ICN-D), same pinned tree:** SN4 broad **m3 PASS=314 FAIL=22** · **m4 PASS=311 FAIL=23 SKIP=2** (336 total) — vs the recorded 311/25 and 310/24; SN4 is **+3 m3 / +1 m4, i.e. IMPROVED, no regression**. Prolog honest **PASS=107 FAIL=0 ABORT=0**.
  - **⛔ HONEST CAVEAT, NOT PAPERED OVER:** the record says Prolog 108, this run says 107. FAIL=0/ABORT=0 both times, so nothing is failing — but `test_prolog_bb_honest.sh` grades **only programs whose `--run` exits 0** (`[ $ir_rc -ne 0 ] && continue`, :30) and then checks `--run` against `SCRIP_NO_AST_WALK=1 --run`, i.e. it is a **self-consistency harness, not an oracle harness** (185 `rung*.pl` present, 164 with a `.ref`, 107 graded). The 108→107 delta is therefore *either* one program that no longer exits 0 *or* corpus drift between commits, and **I did not run it down.** ⛔ Do not read 107/0/0 as proof of Prolog invariance at the same strength as the Icon number — treat closing this as a cheap owed item.
- **⭐ ORIENTATION NOTE FOR THE NEXT SEAT — the canonical archives are NOT auto-populated and the goal file's setup line is easy to skim past.** `refs/` is gitignored; RULES.md's CONSULT CANONICAL SOURCES law is unsatisfiable until Lon's two chat uploads are unzipped to **`SCRIP/refs/{icon-master,jcon-master}`** (verify `ls refs/jcon-master/tran/irgen.icn` succeeds). Populated this session and read against the live rungs: `ir_a_Suspend` publishes γ via a distinct **`ir_Succeed`** and resumes through an **`ir_IndirectGoto`** on a parked continuation — the exact park/resume handshake s234 measured as disagreeing by 8 bytes; `interp.r`'s `Op_Mark`/`Op_Esusp` chain **`efp`/`gfp` as two independent linked lists and COPY the live region to the stack top on suspend** rather than retaining a frame in place, which is the canonical shape ζ-ACTIVATION-FRAME is reaching for and is direct support for s234's off-spine finding; `ir_a_Scan`'s ScanSwap saves/swaps/restores &subject+&pos around the body; `fscan.r{tab,move}` are `{0,1+}` with reversal-on-resume vs `fstranl.r{any,match,many}` `{0,1}` with none — the split that makes `match`'s membership in the carve-retention class look wrong.
- **⛔ NEXT — UNCHANGED, AND NOW ON A VERIFIED FLOOR:** Z-3 slice 2 (off-spine activation + all three handshake disagreements + the retire-together set, ONE commit). ⛔ **COORDINATE BEFORE TOUCHING `emit.cpp`: s235 is mid-flight on the same three sites** (`SCRIP_ICN_FCFLAT`), and s234 claimed slice 2 as well. Three seats have now converged on one defect; the ledger cost of that is higher than the merge cost of talking first. The floor this seat leaves is a *provenance-clean, twice-agreeing* 235/28/30 at `860a9e43` — rebuild and re-derive it before trusting anything measured earlier, since every prior number predates the arith-fuse flip.
- **⭐⭐⭐ SECOND DELIVERABLE — `.github/ICON-IRGEN-TOPOLOGY.md` IS LANDED (`5689e4a3`), AND IT CARRIES A FALSIFIABLE REFINEMENT OF THE MODEL.** Z-3 has asked for this table for several sessions; it now exists with all **43** `ir_a_*` procedures, resume-edge op signatures **extracted mechanically** (one `awk` pass over `irgen.icn`, recipe embedded in the file and verified to reproduce), a STATIC/DYNAMIC arrival-depth verdict per kind, and the 16-witness Z-3/Z-4 set mapped onto its rows. Both halves re-derive in two commands — nothing in it is transcribed.
  - **⭐ THE FINDING, and it sharpens the Z-3 thesis rather than restating it: DYNAMISM IS NOT ONE THING, AND ONLY TWO OF ITS THREE FORMS ARE BROKEN.** Splitting the dynamic constructs by *which* marker makes them dynamic: **D1** = runtime-variable resume TARGET (`ir_IndirectGoto` through an `ir_MoveLabel`'d temp) · **D2** = true suspension (`ir_Succeed`) · **D3** = retained state across resume (`ir_ResumeValue` / `ir_ScanSwap`). **Measured at `860a9e43`: pure-D1 constructs (alt · case · every · while · until · repeat · if) are 39 PASS / 0 FAIL, while every single one of the 16 Z-3+Z-4 failures lands on D2 or D3.** (The lone `alt` hit in a raw grep is `rung37_scan_alt` — scan-family, i.e. D3, not a pure D1.)
  - **⛔ SO: A RUNTIME-VARIABLE RESUME TARGET IS ALREADY HANDLED CORRECTLY TODAY. WHAT BREAKS IS STATE RETAINED ACROSS γ.** That is a stronger and more useful statement than "generators are dynamic", and it is **positive, independent evidence for Z-3's off-spine activation over any addressing/compensation patch** — an addressing fix by construction leaves D2/D3 retention untouched, which is exactly why s233's release-only experiment and s234's "correct base but keep the carve" experiment each failed, from opposite sides. It also converges with s234b's cumulative-depth argument (depth is not path-invariant at merge points) by a completely different route. ⭐ It is stated so as to be falsifiable: **if a pure-D1 rung ever fails, or a conversion closes D2/D3 without touching D1, this claim is what predicted it.**
  - **⭐⭐⭐ AND IT WAS TESTED OUT-OF-SAMPLE WITHIN THE HOUR — s235's FIX IS A CLEAN TEST OF A CLAIM WRITTEN BEFORE IT LANDED.** At `4fc01bee`: **pure-D1 = 40 PASS / 0 FAIL** (up from 39/0, since the one contaminating grep hit `rung37_scan_alt` is now a PASS), and **every one of the 4 newly-fixed programs is a D3 row.** ✅ Locus claim CONFIRMED out-of-sample.
  - **⛔ BUT THE REMEDY HALF IS FALSIFIED, AND I AM RECORDING IT RATHER THAN QUIETLY EDITING IT.** This file's first draft inherited the goal file's framing — "the retained cell must move OFF the spine into a ζ-ACTIVATION FRAME". **s235's fix was a DELETION:** `bb_scan_tab.cpp` already addresses its cell as `FRQ(op_off+16)`, an ordinary flat frame quad **the allocator already grants**, so the `sub rsp,16` was a *second allocation of a slot that already existed off-spine*. Nothing needed moving; nothing needed compensating. ⛔ **THE LESSON GENERALIZES AND IS WORTH MORE THAN THE FIX: three sessions (s232 release, s233 match-release, s234 correct-base) reasoned about how to correctly compensate or relocate a carve that should never have been emitted at all. CHECK FOR DOUBLE-ALLOCATION BEFORE DESIGNING A RELOCATION.**
  - **⛔ THE D2 SET IS UNTOUCHED — 9 suspend programs remain, and they are the real test of the activation-frame thesis.** Nothing above licenses assuming they are also a double-allocation: s234's hand-patch evidence (γ destroys the suspended frame; three independent caller-handshake disagreements) is independent and still stands.
- OWED: push credential · the Prolog 108→107 one-liner above · Z-3 slice 2 (coordinate with s235/s234).

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-16 s237 (Claude Opus 5) — **Z-5(a) LANDED: THE DT_DATA SILENT CORRUPTION IS FIXED AND GATED, AND THE FIX WAS A DELETION. THE DORMANT Z-3 SLICE-1 FLAG IS MEASURED FOR THE FIRST TIME AND IS CRASH-NEGATIVE — SLICES 1 AND 2 ARE ONE COMMIT OR NEITHER. PROLOG'S 108→107→105 DRIFT IS PROVEN TO BE HARNESS FLAKE.**
- **Commits:** `.github` this cursor. **SCRIP + corpus UNTOUCHED — zero source edits, no regens, no R-ICN-D exposure created.**
- **⛔⭐ PROVENANCE DEFECT IN THE RECORD — THE s235 CURSOR'S PINNED HASHES DO NOT EXIST.** `4fc01bee` and `ee6afefd` are **ABSENT from origin** (`git cat-file -t` → fail). They were rebased onto the s111 SNOBOL4 commits and rewritten as **`9630d3c4`** (the scan-carve fix) and **`cfcea2c7`** (bench harness). Content verified intact by reading the diff. ⛔ **STANDING CONDITION #3 needs a codicil: a pinned hash is not durable — a sibling seat's rebase rewrites it. Pin the hash AND the commit subject line**; the subject survived, the hash did not. Also: the s236 cursor records the in-flight killswitch as `SCRIP_ICN_FCFLAT`; **the landed name is `SCRIP_ICN_FCCARVE`** (polarity inverted — set it to *restore* the old carve).
- **WATERMARK, re-derived not quoted, TWO AGREEING RUNS:** Icon m3 **239/24/30** at SCRIP **`9630d3c4`** · corpus `6288c09c` · `git status` empty before+after · `pgrep -c scrip`==0 before+after · scrip mtime 11:49:40 unmoved across both runs · canary rc=0 ×3 arms with correct output · RT_OPT `-O0`. **`SCRIP_ICN_CELLS=1` arm also 239/24/30 with a BYTE-IDENTICAL fail set** (`diff` clean) — the arms remain behaviourally identical, so the Z-8 fold stays a deletion exercise.
- **⭐ THE 24 DECOMPOSE EXACTLY AS s236's CORRECTED LEDGER PREDICTED, independently re-derived:** **9 D2-suspend** (`rung03_suspend_{gen,gen_compose,gen_filter,return}` · `jcon_recogn` · `jcon_level` · `jcon_genqueen` · `rung37_proc_lookup` · `rung37_subscript_genproc`) · **4 D3-scan** (`jcon_{scan,scan1,scan2,subjpos}`) · **11 Z-5**. No drift, no new class.
- **⭐⭐⭐ THE FINDING — SLICE 1 EXISTS, SHIPPED DORMANT AT s230, AND HAD NEVER BEEN SUITE-MEASURED.** `emit.cpp:3344-3345` carries `SCRIP_ICN_GENFRAME=1` (requires `icn_cells_graph`, i.e. `SCRIP_ICN_CELLS=1`) admitting generator graphs to ζ-ACTIVATION establishment. Measured this session on the pinned binary:
  - **Watermark-neutral: 239/24/30, fail set BYTE-IDENTICAL to default** (`diff` clean, not eyeballed). Zero programs flip either way. The runner does no env scrubbing (`grep 'env -i|unset|SCRIP_ICN'` == 0), and behaviour demonstrably changes per-program, so this is the compiler, not the harness.
  - **⛔ CRASH-NEGATIVE, and the PASS/FAIL metric is blind to it: D2 crashes (rc≥132) go 4/9 → 7/9.** Attribution isolated three ways: **default 4/9 · `CELLS=1` alone 4/9 · `CELLS=1 GENFRAME=1` 7/9** — so `GENFRAME` alone owns all +3.
  - **⭐ THE +3 ARE PRECISELY THE MULTI-VALUE RESUMING GENERATORS** (`rung03_suspend_{gen,gen_compose,gen_filter}` — all `while i<=n do suspend i do i:=i+1` driven by `every`). Honest limit, stated so it stays falsifiable: **this is directional, not a biconditional** — `rung37_proc_lookup` uses `every` twice and is unchanged (it fails before resumption).
- **⭐⭐ WHY THIS IS EVIDENCE *FOR* Z-3 RATHER THAN AGAINST SLICE 1, and it is the whole point.** Establishing an activation that **γ then destroys** is *strictly worse* than never establishing one, for a generator that resumes: before slice 1 there is no frame to tear down; after slice 1 there is, and β re-enters onto a dead frame. **That is s234's "γ destroys the suspended frame" reproduced from the opposite direction, on the default tree, at no cost.** ⛔ **CONSEQUENCE, and it is a scheduling ruling: slice 1 must NOT be flipped on by default as a de-risking stepping stone. Establishment and retention-across-γ are ONE commit or neither.**
- **⭐ INDEPENDENT ROOT-CAUSE READ-OUT (MONITOR-FIRST, cheapest experiment, asm never guessed).** Minimal ladder isolates it: `return` PASSES, `suspend` SEGVs with **zero output** even with no `every` and even with the value discarded (`g();`) — so the default-arm defect is **at the suspend's own γ, not in the caller handshake and not on resume**. Emitted asm, passing sibling vs failing probe: the `return` graph gets `sub rsp,80` + `[rsp+56]=rcx`/`[rsp+64]=rdx` wire save + `rt_icn_zframe_args_install` + `proc_g_γ: add rsp,80; jmp rcx`; the `suspend` graph gets **no grant at all**, no wire save, no args install, and exits `add rsp,0; mov eax,2; ret` — writing `[rsp+0..32]` over its own return address, then `ret`ting into it. `emit.cpp:3345`'s own comment names this witness and mechanism exactly; this seat re-derived it before reading the comment, so the two are independent.
- **⛔ BLOCKER FOR SLICE 2, surfaced to Lon in-chat:** the canonical archives **did not arrive** — `/mnt/user-data/uploads/` is EMPTY, so `refs/{icon-master,jcon-master}` cannot be populated and RULES' CONSULT-CANONICAL-SOURCES law is unsatisfiable. Slice 2 is the γ/β handshake, whose spec is `ir_a_Suspend` (`ir_Succeed` + `ir_IndirectGoto` park/resume) in `irgen.icn` and `interp.r`'s `Op_Mark`/`Op_Esusp` efp/gfp copy-to-stack-top. ⛔ Do not design the handshake without them — that is the inherited-claim trap.
- **⭐⭐⭐ Z-5(a) DONE — SCRIP `7d61ca64` — AND THE RECORDED DIAGNOSIS WAS TRUE BUT IMPRECISE IN A WAY THAT MATTERED.** The ledger said "`rtx_icnagg.S`/`rtx_icnsub.S` carry 100; live value 0x70". Re-derived: **`rtx_abi.inc:64` has included `descr_tags.inc` since s230** ("THE tag numbers live there and ONLY there"), so `0x70` was already in scope — the `.S` files carried a local `#define DT_DATA 100` **placed AFTER the include, shadowing it**. s230 landed the single-source header and never removed the defines it obsoleted. **PROVEN IN THE SHIPPED BYTES, not argued from preprocessor rules:** `libscrip_rt.so` held `cmp $0x64,%esi` in `dat_field_get` while every C site used `cmp $0x70` — **one binary, two values for one tag**, `rtx_gate_icnagg` defaulting on.
  - **⭐ THE TWO SITE CLASSES BEHAVE DIFFERENTLY AND THE LEDGER CONFLATED THEM.** `dat_field_get`'s guard is a **RANGE** test (`v < DT_DATA → C`); 100 was chosen as "just above DT_FAIL 99", but **DT_FAIL is really 0x68 = 104**, so the boundary sat **BELOW** it and a FAIL descriptor passed the guard and was walked as a record — **live corruption**. The `rtx_icnsub.S` arms are **EQUALITY** tests against 100 that never match a live 112 — **RTX-24's list fast path was silently DEAD**, not corrupting. The deleted comment had itself warned the omission could "admit the wrong datatype, silently"; it did, by a different mechanism than the one feared.
  - **THE FIX IS A DELETION** (cf. s235's carve — the lesson generalises a second time). Also removed 3 **LATENT** shadows that happened to agree (`rtx_icnrel.S` DT_S/DT_I · `rtx_icncall.S` DT_E) so the gate needs no exceptions, and symbolized `rt_gram_trampoline.S`'s bare `$0x68`.
  - **⭐ THE GATE EARNED ITS KEEP ON ITS FIRST RUN:** `scripts/test_gate_icn_tag_single_source.sh`, 3 locks — (1) zero `#define DT_*` outside `descr_tags.inc`; (2) every DT_-using `.S` reaches the single source; (3) `descr_tags.inc` agrees with `descr.h`. GREEN, **18 tags cross-verified**. **Lock 2 found a 4th instance nobody had named (`rt_gram_trampoline.S`)** — which is the point of a gate over a fix.
  - **VERIFIED** (clean rebuild `rm -rf out/rt_pic`, pinned, `pgrep scrip`==0): `dat_field_get` now `cmp $0x70`; canary rc=0 ×3; **Icon m3 239/24/30 TWICE, fail set IDENTICAL to the pre-fix baseline** (`diff` clean); **SN4 broad m3 314/22 · m4 311/23 SKIP=2, exact match to record**; Prolog FAIL=0 ABORT=0. ⚠ **Honest: watermark-NEUTRAL on Icon** — it kills a proven silent corruption and revives a dead fast path, but flips no program. No regen owed (no codegen touched; `update_icon_bench_asm.sh` refuses `*/programs/icon`, Lon 2026-07-11).
- **⛔⭐⭐ PROLOG'S COUNT DRIFT IS CLOSED, AND THE ANSWER IS NEITHER CANDIDATE.** s236 left it owed ("either one program no longer exits 0 *or* corpus drift; I did not run it down"). Measured **106 → 104 → 107 across three runs on ONE pinned binary and ONE corpus, this session**. `test_prolog_bb_honest.sh` grades only programs whose `--run` exits 0 (`:31 [ $ir_rc -ne 0 ] && continue`), and that status is **not stable run-to-run**. **The recorded 108→107→105 deltas across three seats were FLAKE, never codegen signal.** ⛔ Do not read the count as invariance evidence and do not chase its deltas — FAIL=0/ABORT=0 is the only meaningful part.
- **⛔ PROCESS — LONG BUILDS ARE BEING SILENTLY KILLED.** A `nohup` background build died mid-flight (no processes, objects 17 min stale, status file never written); only a static object count caught it. **`setsid` survived where plain `nohup` did not.** A watermark taken right after a silently-truncated build measures a half-built tree — the s230 void class. Recommend the Session Setup block adopt `setsid` + an explicit build-completion assertion.
- **NEXT:** Z-3 slice 2 = retention across γ + the three handshake disagreements + `SCRIP_ICN_GENFRAME` promoted in the SAME commit. Coordinate: s234/s235 both claimed slice 2. **OWED: push credential.**

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-16 s235 (Claude Opus 5) — **Z-3/Z-4 LANDED: THE SCAN CELL IS OFF THE SPINE. THE FIX WAS A DELETION, NOT A COMPENSATION. m3 235→239, bench 0/8→3/8, META 69.1→75.8, ZERO REGRESSIONS.**
- **Commits (hashes only — `handoff_status.sh` owns every word about pushed-ness):** SCRIP `ee6afefd` (bench harness paths) + `4fc01bee` (the fix) · corpus `6288c09c` (regen) + the witness commit · `.github` this cursor. Post-rebase onto origin (which moved twice mid-session: AI-3 arith fuse, then s110 SLIM_PAIR).
- **⛔⛔ LAW 4 VIOLATED AGAIN — FOURTH CONSECUTIVE SESSION. A CONCURRENT ICON SEAT (s236) IS IN THIS TREE.** `.github` `9a33037e` "[ICN-s236] cursor: R-ICN-D canary discharged against the AI-3 arith-fuse" landed under the same `LCherryholmes` identity while this seat was mid-measurement — and it is the SAME R-ICN-D canary this seat had already run. Two seats, one tree, duplicated work, three sessions running. Rebased and merged rather than waited, per RULES.md. ⛔ Every number below was taken on this seat's own binary, clean tree, `pgrep scrip`==0 before+after, and RE-PROVEN after the rebase.
- **WATERMARK, re-proven post-rebase at `4fc01bee`, clean tree, pinned:** Icon m3 **239/24/30** (was 235/28/30) · m4 **238** · canary rc=0 ×3 arms · Prolog **PASS=105 FAIL=0 ABORT=0** · SN4 broad m3 **314/22**, m4 **311/23 SKIP=2** (both slightly UP on record — the arith-fuse seat's doing, not this one). **META SCORE 69.1 → 75.8** (full 7-suite scorecard, live Arizona Icon 9.5.25a oracle built this session).
- **⭐⭐⭐ THE FIX: THERE WAS NEVER ANYTHING TO COMPENSATE — THE CARVE WAS A DOUBLE-ALLOCATION.** `bb_scan_tab.cpp` already addresses its cell as `FRQ(op_off+16)`, an ordinary flat frame quad **the allocator already grants**. The `sub rsp,16` at α was a SECOND allocation of storage that already existed — precisely the "pure double-count" pattern `zls_fc_cell` documents for the SNOBOL4 match kinds (`zeta_storage.c:270`), which was netted out there and never here. Deleting the grant at the three sites leaves the cell at a fixed frame offset: off the spine, no rsp motion, survives γ for β to read (ARCH-ICON cursor-movers reversed-on-resume), and nothing downstream shifts.
- **⭐⭐ WHY THIS BEATS THE s233 FALSIFICATION, STATED AS THE GENERAL LESSON.** s233 proved a γ release fixes downstream locals but breaks DIRECT consumers reading match's result through an address calibrated on the carve persisting — the two requirements are incompatible *under the carve model*. **With no carve there is no depth to be right or wrong about, so both hold at once.** `rung08_strbuiltins_match` STAYS GREEN alongside all three `carve_leak` witnesses. ⛔ The retire-together set (`op_sa` bumps at `emit.cpp:1230/1231`, the `op_fc_base` windowing at `:1236`) is now DEAD CODE ON THE DEFAULT ARM — it only executes under the killswitch. Removing it outright is a Z-8 fold item, not this commit.
- **⭐ ROOT CAUSE, and it runs DEEPER than s234b recorded.** `=s` desugars to `tab(match(s))` = TWO carves = 32B = two DESCRs. In `t(p,q,r,s)` with `p ? { if ="-" ... }`: **r returns p's value, s returns q's, p/q read off the end as `""`/`&null`, then SEGV.** The victims are not only operand-list slots (s234b) but **LIVE PARAMETER SLOTS of the enclosing activation**, and the shift is cumulative in carve count. Witnesses `corpus/probe/icn/witness_icn_frameslot_shift_{param,options}.icn`, oracle-verified.
- **⭐⭐⭐ THE BENCHMARK BLOCKER WAS THIS, END TO END — AND IT WAS NEVER AN EMIT FAILURE.** ipl `options()` does `x ? { if ="-" ... while c := move(1) ... find(c,optstring) }`. The carve corrupted the **`optstring` PARAMETER to `""`** before `find()` read it; because `""` is non-null the `/optstring := string(&letters)` default never fired, `find()` searched an empty string, and EVERY option was rejected → `Unrecognized option: -n` → early exit → zero output. That is why 7 of 8 headline benchmarks printed nothing. **bench_correct 0/8 → 3/8 IDENTICAL vs live iconx: `deal` (17000L), `queens` (16653L), `micsum`. `rsg` 0 → 4893/5000, `tgrlink` 0 → 2. FIRST HEADLINE ICON BENCHMARKS EVER GREEN.**
- **⛔⭐ INSTRUMENT REPAIR — THE 0/8 WAS THE HARNESS, THIRD RECURRENCE OF THE CLASS.** `honest_icon_correctness.sh` defaulted `SCRIPDIR`, `ICONM` and `CORPUS_SRC` to `/home/claude/work/...`, a path in no checkout. `scorecard_icon.sh` overrides `SCRIPDIR` but **not `ICONM`**, so `icont`/`iconx` resolved under the phantom path, the oracle never compiled, and every row scored **ORACLE L=0** — the board printed a plausible full `0/8 EMITFAIL` measuring itself. Fixed; `ICONM` now derives from `ORACLE_BIN`, and a missing oracle/corpus is a loud `exit 2` instead of a believable board. ⛔ **GENERAL LESSON FOR THE NEXT SEAT: a suite that scores the ORACLE at zero is reporting its own breakage. Check the oracle column before believing any SCRIP column.**
- **KILLSWITCH + INVARIANCE:** `SCRIP_ICN_FCCARVE=1` restores the carve, **`=0` byte-identical over 60 corpus programs** (completion criterion met). No new global — inline `getenv`, the existing `SCRIP_NO_CM_CARVE` pattern. Regens ×3 + `update_icon_bench_asm.sh` all run, artifacts committed.
- **⛔ NEXT — Z-3 SLICE 2 IS STILL OPEN AND IS NOW THE WHOLE BALLGAME.** This rung closed the *scan* half of the spine-residency defect (`tab`/`move`/`match`). The **generator** half is untouched: `rt_gen_save_wires` one-active-suspension, γ freeing what it must retain, the three caller-handshake disagreements (s234, all still valid — its hand-patch demonstrator `corpus/probe/icn/z3_offspine_handpatch.py` stands). ⭐ **STRATEGIC READ: apply THIS rung's lesson there first — before building an activation-frame mechanism, check whether the generator frame's storage is ALSO already granted and merely double-allocated.** The scan half looked architectural for three sessions and turned out to be a three-line deletion. Remaining 24 fails: **10 Z-3 generator** · **~2 scan residue** · **12 Z-5**.
- OWED: **push credential (ask is OPEN in chat — 5 commits across 3 repos)** · the 2 red gates (`scan`, `var`) — re-derive, they predate this rung and may be stale · `concord`/`geddump`/`ipxref` still 0 lines (a DIFFERENT class from options(), now isolatable) · Z-8 fold of the dead retire-together set.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s234 (Claude Opus 5) — **Z-3 SLICE 2 PROVEN BY HAND-PATCH: THE ACTIVATION MUST LEAVE THE SPINE, AND THE CALLER HANDSHAKE HAS THREE INDEPENDENT DISAGREEMENTS — ALL FOUR MEASURED, NONE INHERITED. NO COMPILER EDIT THIS SESSION.**
- **Commits:** corpus (`probe/icn/z3_offspine_handpatch.py`, the reproducible demonstrator) · `.github` this cursor. **SCRIP UNTOUCHED — zero source edits, so no regens, no R-ICN-D exposure, no watermark risk.**
- **⛔⭐ STALE-ORIENTATION (a) FIRED AGAIN — THIRD RECURRENCE, AND THE s233 CURSOR IS THE CARRIER.** s233 states "⛔ NOTHING PUSHED — credential ask OPEN, covering exactly these 2". **FALSE at this tree:** `git log origin/main..HEAD` is **0 ahead in all three repos** — SCRIP `3b9c9db0` and corpus `27cc99d2` are both ON origin. s232 corrected the same false claim inherited from s231; s231 inherited it from s230. ⛔ The class is now proven ENDEMIC to this goal file, not incidental: **a push-status sentence in prose is wrong roughly as often as it is right, and this session very nearly copied it forward verbatim before running the check.** Concrete proposal for Lon, since the rule plainly is not enough on its own: make the cursor's commit line carry ONLY hashes and let `handoff_status.sh` own every word about pushed-ness — i.e. forbid the string "unpushed"/"NOTHING PUSHED" in goal files outright, the way `--dump-sm` was retired.
  - **⛔ AND IT PROPAGATED WHILE THIS SESSION WAS WRITING: the s234b cursor below repeats it verbatim** ("now covering SCRIP `3b9c9db0` (s233)"). **PROVED FALSE TWO WAYS at this tree:** `git log origin/main..HEAD` = 0 for SCRIP, and `git branch -r --contains 3b9c9db0` prints `origin/main`. Two independent seats inherited one false sentence in the same hour without either running a one-line check — that is the strongest possible argument for the ban above. **TRUE UNPUSHED SET, re-derived 2026-08-16: SCRIP 0 · corpus 2 (`a5b1133f` s234b, `404541e0` s234) · `.github` (this file).** ⚠ Note also that s234b's prose cites its corpus commit as `9005fdb6` while the landed hash is `a5b1133f` — a hash typed rather than computed, the same failure mode one level down.
- **⛔⭐⭐ ORIGIN HAS MOVED UNDER THIS WATERMARK — READ BEFORE TRUSTING ANY NUMBER IN THIS CURSOR.** `handoff_status.sh` (which fetches) shows SCRIP origin/main at **`1d98f9d2`, four commits AHEAD of the pinned `3b9c9db0`** — `git fetch` confirms **behind=4, ahead=0**, and `git merge-base --is-ancestor` confirms `3b9c9db0` IS on origin (so the false-unpushed correction above stands, twice over). The four include **`1d98f9d2` "AI-3 ARITH FUSE DEFAULT ON: inline both-int and both-numeric arms"** — a CODEGEN DEFAULT FLIP from the SN4 s108/s109 seat, plus two `.s` regens. ⛔ **Everything in this cursor is measured at `3b9c9db0` and is honest THERE; the next seat must `git pull --rebase`, REBUILD, and RE-DERIVE 235/28/30 before trusting it** — an arith-fuse default flip is exactly the shared-emitter class R-ICN-D exists to catch, and no Icon canary has been run against it. This session deliberately did NOT rebase: rebasing mid-measurement is what voided the s230 board.
  - **METHOD, because it caught me too:** my own `git log origin/main..HEAD` = 0 was computed against the CLONE-TIME remote-tracking ref and was therefore stale-by-construction. **`origin/main` is not ground truth until you `git fetch`.** The computed check beats typed prose, but only a FETCHED computed check beats a cached one. Add the fetch to the hygiene laws.
- **WATERMARK at `3b9c9db0`, clean tree, pinned, `pgrep scrip`==0 before+after, `scrip` built 02:20:43 this session:** Icon m3 **235/28/30** — reproduced EXACTLY, and it agrees with the independently-recorded s233 number, which is the second agreeing run the hygiene law asks for. Canary rc=0 ×3 arms. Fail set decomposes exactly as recorded: **10 Z-3** (rung03 suspend quartet · recogn · level · proc_lookup · subscript_genproc · genqueen · meander) · **6 Z-4 scan** (scan/scan1/scan2/subjpos/wordcnt/scan_alt) · **12 Z-5**.
- **⭐⭐⭐ THE POSITIVE RESULT — SUSPENSION WORKS THE MOMENT THE ACTIVATION LEAVES THE FORTH SPINE.** On `rung03_suspend_gen` (`upto(n)` suspending 1..4), hand-patching the emitted m4 asm so the generator's frame lives in a private arena instead of an rsp carve takes the program from **1 γ-arrival (SEGV at the resume)** to **2 γ-arrivals with the correct values (γ¹ rax=1, γ² rax=2)** — i.e. suspend-and-resume executes. Demonstrator committed as `corpus/probe/icn/z3_offspine_handpatch.py` (6 patch sites, asserts on drift); recipe in its header. ⛔ It is a hand patch on ONE program's `.s`, evidence for the design and NOT a fix — nothing wired to a gate.
- **⭐⭐ GENERALIZED TO A SECOND LAYOUT — THIS IS THE CLAIM THAT MATTERS FOR SLICE 2.** `rung03_suspend_return` (`suspend 1; return 2;`) is structurally different — it carries a RETURN path (`proc_g_res: add rsp,8; pop rsp`) as well as a suspend, and its own five offsets (kt=112 · γ-wire 88 · ω-wire 96 · free slot 104 · resume slot 48 · caller park 136→128). **The SAME six edits take it from rc=139 SEGV with no output to rc=0 printing `2 done`** against oracle `1 2 done`: control flow, the return path and program exit all correct, the single missing line being the FIRST suspended value — i.e. the descriptor-publication defect below, not a frame defect. ⭐ Its resume slot is **48** — the exact number the s230 text quoted as though it were a constant. **Two witnesses, two layouts, one transform, both SEGV→progress.** The transform is parameterized by exactly five per-graph numbers, all already known to the emitter at prologue time.
- **⛔ FALSIFIED FIRST, WHICH IS WHY THE ABOVE IS LOAD-BEARING: fixing the ADDRESSING alone does nothing.** A patch that gave the caller a perfectly correct activation base (published in r12 at γ) AND the correct resume slot, while leaving γ's `add rsp,kt` in place, still SEGVs. Reason, and it is mechanical: γ restores rsp above the frame, then the caller's own γ-landing path (`rt_proc_call_epilogue_γ` / `rt_gen_spine_pass_γ` and everything below them) runs nested calls straight through the abandoned 304-byte region. **The frame is not merely unreachable, it is overwritten.** ⛔ DO NOT retry any variant of "retain the base but keep the carve" — this is the Z-3 twin of the s233 "just release match's carve" falsification, and it fails for the mirror-image reason.
- **⛔⭐ THE CALLER HANDSHAKE — THREE DISAGREEMENTS, EACH READ OUT OF THE EMITTED ASM, EACH INDEPENDENT.** Under `SCRIP_ICN_GENFRAME=1` (Z-3 slice 1, verified live at `emit.cpp:3344`) the witness SEGVs where the default arm merely prints nothing — the crash is the better instrument, exactly as s230 predicted.
  1. **γ FREES WHAT IT MUST RETAIN.** `proc_upto_γ: mov rcx,[rsp+280]; add rsp,304; jmp rcx` — γ tears down the activation holding the parked resume address and every local. ω's identical `add rsp,304` is CORRECT (genuinely exhausted). This is clause (i) of the rung, now witnessed.
  2. **THE PARK SLOT AND THE RESUME SLOT ARE 8 BYTES APART.** The landing is `.Lx35_3: mov [rsp+88],rsp; add rsp,8` — so the park lands at **steady+80** (the very next instruction reads it back as `[rsp+80]`, proving the address). The resume is `n31_proc_gen_β: mov rsp,[rsp+88]` — reading **steady+88**. It read 0 and `jmp *0xc0(%rsp)` faulted with rsp=0. Neither site is obviously wrong alone; they are wrong *with respect to each other*.
  3. **THE PARK SLOT DOUBLE-BOOKS AS THE "ALREADY PASSED γ" FLAG.** Same address serves both, so the park write makes the flag unconditionally non-zero and the `mov [rsp+80],1` / `rt_proc_call_epilogue_γ` first-arrival arm is DEAD on every generator call — control always takes `.Lx35_5: call rt_gen_spine_pass_γ`. ⛔ Whoever lands slice 2 must give the base its own slot; do not "fix" this by moving the park, or the flag semantics flip silently.
  - **Caller reads the resume address at `[base+0]`; the callee parks it at `[base+192]`** (`lea rax,[rip+n6_suspend_β]; mov [rsp+192],rax` at α_body). The s230 text records this offset as `[rsp+48]` — that was ONE program's layout, not a constant. Re-derive per graph; the design fact is only that the two sites must agree.
- **⭐ NEW, AND IT SURVIVES Z-3 — THE SUSPEND BOX PUBLISHES A MALFORMED DESCRIPTOR.** With the activation fixed, the run gets all the way into `write` and dies in `out_write_str` with `s=0x2`. At γ the descriptor pair is `{rax=1,rdx=1}` then `{2,2}` — **both words carry the integer, the type word is never set** (`n0_lit_integer_α` builds `{tag=3,val=1}` correctly at `[rsp+176/184]`, so the loss is in the suspend box's publication, not the literal). ⛔ MEASURED IDENTICAL IN THE UNPATCHED BASELINE — pre-existing, downstream of Z-3, and NOT introduced by the patch. Expect `rung03_suspend_gen` to need this too; it is plausibly kin to the Z-5(a) DT_DATA tag class, and cheap to re-check once slice 2 lands.
- **CARRIED FORWARD from the pruned s231 cursor so it is not lost:** `write()` appears to DROP the string literal immediately preceding a cursor-mover call (`"literal: tab(3)=["` vanished). Suspect `icn_tree_is_cursor_mover` backtrack handling at `lower_icon.c:848`. Unchased.
- **METHOD NOTE:** every finding above came from `--compile --target=x86 > f.s` + hand-patch + reassemble + gdb, per the s231 note. The hand-patch loop cost minutes and falsified one hypothesis and confirmed another without a single compiler rebuild. Do this before touching `emit.cpp`.
- OWED: push credential (SCRIP is already fully on origin — verified; the ask covers corpus `404541e0`+`a5b1133f` and this `.github` cursor) · ⛔ **COORDINATION ACK to s234b: Z-3 slice 2 is this seat's; the argument-slot collision class is yours. The two findings are the SAME defect seen from the two ends** — your carve collides with the predecessor's slot, mine is an activation destroyed at γ; both are spine-resident state outliving the box that carved it, and both retire in the one Z-3 commit. · **Z-3 slice 2 = give the activation an off-spine home, then fix all three handshake disagreements IN ONE COMMIT** (with the s233 retire-together set) · the descriptor-publication defect above · Z-4 remains strictly downstream of Z-3.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-16 s234b (Claude Opus 5) — **THE CARVE DOESN'T JUST LEAK, IT COLLIDES: THE SCAN CELL DESTROYS ITS PREDECESSOR'S SLOT. ROOT CAUSE PROVED FROM THE .s AND BY HAND-PATCH. BENCH BOARD RE-MEASURED GREEN AT 1.70x GEOMEAN.**
- **Commits:** corpus `9005fdb6` (4 argument-slot collision witnesses). ⛔ NOTHING PUSHED — credential ask OPEN, now covering SCRIP `3b9c9db0` (s233), corpus `9005fdb6`, and this `.github` cursor. ⭐ CORRECTION: s233's cursor lists corpus `27cc99d2` as unpushed; that work IS on origin as `103923b5` (rebased hash). Re-derive push status, never carry it from prose (STALE-ORIENTATION a).
- **⛔⛔ CONCURRENCY, LIVE AND MEASURED — A SECOND WORKER IS IN THIS TREE RIGHT NOW.** `corpus/probe/icn/z3_offspine_handpatch.py` appeared at 02:32 authored "s234, Claude Opus 5" (Z-3 slice-2 hand-patch work) — not written by this seat; left UNSTAGED and untouched. **The `scrip` binary was rebuilt at 02:20:43** on top of this seat's 01:50:03 build, mid-probe-series. Mitigation applied and recorded: HEAD unchanged `3b9c9db0`, tree clean, zero `src/` files modified in the window, and the rebuilt binary re-verified equivalent (canary rc=0 ×3; int_loop 16ms vs the 16/17ms recorded). **Every benchmark number, the correctness board and the 235/28/30 watermark below were taken BEFORE 02:20:43 on this seat's own binary.** LAW 4 (one worker per tree) is being violated again — two seats have now independently duplicated Z-3 work in three consecutive sessions.
- **WATERMARK at `3b9c9db0`, clean tree, pinned, `pgrep scrip`==0 before+after, two agreeing runs:** Icon m3 **235/28/30** (re-derived fresh, matches record) · canary rc=0 ×3 arms · oracle = Arizona Icon 9.5.25a built this session. RT_OPT = **-O0** (O2-DIRECTED-ONLY honoured).
- **⭐ BENCH BOARD RE-MEASURED, 10/10 byte-identical, REPS=5, two agreeing runs — geomean 1.70x** (1.95x excluding the sub-10ms row): int_loop **6.65–7.00x** · mod_isolate **4.61x** · concat_strvar **2.53–2.63x** · concat_dispatch 2.41–2.46x · concat_table 2.12–2.18x · sub_list 1.54–1.57x · table_miss_dispatch 0.99–1.00x · concat_int_dispatch 0.81–0.82x · concat_intvar 0.76–0.78x · table_miss_semantics 0.46–0.50x. **s233's `itos` win is visible and held: concat_int_dispatch 0.66 → 0.81x.** Headline 8 **still 0/8**, modes exactly as recorded (concord/geddump SEGV, tgrlink abort, micsum 1-line near-miss, rest empty) — so no headline speedup number is valid.
- **⛔ CORRECTION — THE s233 STARTUP-FLOOR NOTE IS WRONG IN DIRECTION.** Measured over 40 iterations with a `/bin/true` fork control (1.0ms) subtracted: **SCRIP 1.9ms** (matches s233 exactly) but **iconx 3.9ms, not 0.7ms.** Startup HELPS SCRIP here. So startup does NOT explain `table_miss_semantics` 0.50x — that row's true compute gap is WORSE than the wall-clock shows, and the "excluding it moves geomean 1.25→1.50x" reasoning must not be carried forward on startup grounds. (Excluding it is still right, but for small-N noise reasons, not startup.)
- **⭐⭐⭐ ROOT CAUSE, PROVED TWO WAYS — THE CARVE COLLIDES WITH THE PREDECESSOR'S SLOT.** Minimal repro `write("A", tab(4))` (166-line `.s`, m4 reproduces m3): `n2_lit_string` writes `"A"` at `[rsp+144]` **pre-carve**; `n4_scan_tab` does `sub rsp,16` then writes its result at `[rsp+160]` **post-carve**; `(rsp_pre-16)+160 == rsp_pre+144` — **the same address. tab's result physically overwrites "A".** Slots are handed out in sequential 16B units and the carve is 16B, so the victim is deterministically the box allocated immediately before the carver = the immediately preceding argument. Verified on the 3-predecessor case (`"C"`@224, tab result post-carve 240 → 224; only `"C"` dies). **HAND-PATCH PROOF:** adding the outstanding carve to the six post-carve displacements makes SCRIP oracle-exact (`112` → `A112`). ⛔ BOTH halves are required — moving tab's result alone only converts destruction into a stale read.
- **⛔ THIS CORRECTS s231's "SECONDARY, UNCHASED" NOTE ON BOTH COUNTS.** It is NOT "write() drops the string literal preceding a cursor-mover": it is **any operand list** (user procedure args and LIST CONSTRUCTORS included), **any operand kind** (a variable is lost the same as a literal), and **`match` does it too** — a position-returner, not a cursor-mover. **The affected set is EXACTLY {tab, move, match}** = the three carve-compensated sites `emit.cpp:1230/1231/1236`, i.e. precisely the RETIRE-TOGETHER list. `many/any/upto/find/bal/pos` all measured CLEAN. Damage is exactly one 16B slot per scan call and COMPOUNDS (`write("A",tab(2),"B",tab(4))` → `A1B12` becomes `12`). Witnesses: `corpus/probe/icn/witness_icn_argslot_collision_{tab,match,count,nonwrite}.icn` + `.ref`, all four FAIL at `3b9c9db0`.
- **WHY s231's FIX WAS HALF THE STORY:** `op_sa += fck` compensates the box's OWN operand fetch. Neither the box's **result slot** nor any **downstream consumer** of a pre-carve-written slot gets the same compensation. The per-box `op_zdepth = op_fc_bytes` model (`emit.cpp:1000`, "a box compensates for exactly what IT carved") is the assumption that breaks, exactly as the Z-3 bullet states.
- **⛔ WHY A CUMULATIVE-DEPTH PATCH IS THE WRONG FIX (do not attempt without Lon):** the outstanding depth is NOT path-invariant at merge points — in the same 166-line `.s`, `n6_scan_α` and `n7_scan_α` both emit `lea rdi,[rsp+16]` while reaching that displacement at rsp values 16 apart (n6 post-carve, n7 post-release). A static per-box compensation would need real dataflow. This is direct evidence FOR Z-3 (move the cell off the spine so no depth is ever outstanding) over any addressing patch.
- **THE CHEAPEST CONVERSION WITNESS IS NOW THIS SET, not the leak trio.** `witness_icn_argslot_collision_tab` is 1 line, oracle-exact, and turns green only if a Z-3 slice keeps direct consumers whole AND closes the downstream shift. Pair it with `rung08_strbuiltins_match` (must STAY green) — s233 proved you cannot buy one with the other.
- OWED: push credential (SCRIP `3b9c9db0` + corpus `9005fdb6` + this cursor) · Z-3 slice 2 ⛔ **COORDINATE — the other seat is mid-flight on it** · `micsum`'s near-miss decomposes onto this same class (`tab(many(...))` ×4), so it is NOT an independent cheap win.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s233 (Claude Opus 5) — **Z-5(b) FIRST WIN LANDED (snprintf out of the coercion hot path). Z-4 "RELEASE MATCH'S CARVE" FALSIFIED BY MEASUREMENT — THE LEAK IS LOAD-BEARING FOR DIRECT CONSUMERS.**
- **Commits:** SCRIP `3b9c9db0` (descr_to_str itos) · corpus `27cc99d2` (3 isolating micro-benchmarks). ⛔ NOTHING PUSHED — credential ask OPEN, covering exactly these 2 (one per repo). `.github` commit for this cursor pending in the same ask.
- **WATERMARK at `3b9c9db0`, clean tree, pinned, `pgrep scrip`==0 before+after, two agreeing runs:** Icon m3 **235/28/30** · canary rc=0 ×3 arms · Prolog honest **PASS=108 FAIL=0 ABORT=0** · SN4 broad m3 **311/25**, m4 **310/24 SKIP=2** (m4 matches record) · oracle = Arizona Icon 9.5.25a built this session.
- **⭐⭐⭐ Z-5(b) FIRST WIN — THE COERCION COST WAS `snprintf`, AND IT IS NOW GONE.** Poor-man's gdb sampler put the time in `descr_to_str` → `snprintf`. Every integer→string coercion ran a full printf-family format parse, plus an unconditional `rt_str_alloc(31)` regardless of digit count, plus a `STRVAL` that DISCARDED the length snprintf had already returned. Replaced with canonical `itos` (`refs/icon-master/src/runtime/cnv.r:648`) — backward digit loop into a stack buffer, length as a pointer difference. **MEASURED, correctness-gated: isolating witness `"abcdefgh" || intvar` ×2M 208ms → 144ms; committed `bench_icnstr_concat_int_dispatch` 222ms → 177ms.** LLONG_MIN oracle-verified (naive `-ival` overflows; unsigned negation handles it).
  - **COMPOUNDING FACT, recorded because it is invisible from the C:** `rtx_str.S`'s `str_concat_d` asm fast path treats **`slen==0` as "unknown length, C strlens" and BAILS**. `STRVAL` sets `slen=0`. So every integer-derived string was PERMANENTLY DISQUALIFIED from the asm fast path. Now returns `BSTRVAL` so the length rides the descriptor. ⛔ HONEST: that second step showed **NO measurable win** on this benchmark (144→148ms, noise) — the payoff can only appear on RE-concatenation of an integer-derived string. Landed as strictly-more-information, **not** as a banked speedup. Do not cite it as a perf result.
- **⛔⭐⭐⭐ Z-4 NARROWING EXPERIMENT RUN TO COMPLETION AND FALSIFIED — DO NOT RETRY "JUST RELEASE MATCH'S CARVE".** s232 queued this as the cheap test before the big conversion. It is now RUN, with a clean causation A/B, and it FAILS.
  - **The misclassification is REAL and now has a SOURCE-LEVEL proof, not just an asm reading:** `bb_scan_match.cpp` emits `x86_beta_trampoline()` = **`β: jmp ω`, a det-leaf that reads NOTHING** (`x86_asm.h:745`), while `bb_scan_tab.cpp` emits a real `x86_beta()` that reads the saved cursor back out of the retained cell. So match genuinely has **no gamma-live state**, yet `emit.cpp:1236` files it in the windowed class (`op_fc_base = op_off+16`) whose entire stated rationale (`x86_asm.h:2412`) is that gamma-live state is the point.
  - **The release mechanism EXISTS and works:** `op_zgpop` rides the ONE existing X86H_JMP/GAMMA hook arm (`x86_asm.h:2419`, `add rsp,op_zgpop`) and is gated INDEPENDENTLY of `op_fc_base` at `:489` — so match can keep its window for its own α..γ needle reads AND release at γ. Implemented behind `SCRIP_NO_MATCH_GREL=1`.
  - **RESULT: the three s232 carve-leak witnesses ALL PASS with the release on, and `_var`+`_subject` FAIL with the killswitch restoring the leak — clean causation. `_lucky` passes BOTH ways, behaving exactly as s232 said its control should. BUT the suite went 235 → 234.**
  - **⛔ THE REGRESSION IS THE FINDING: `rung08_strbuiltins_match`.** `"hello" ? write(match("hel"))` must print `4`; with the release it prints EMPTY. **The consumer reads match's result through an rsp-relative address calibrated around the carve PERSISTING** — the same assumption the s231 `op_sa` compensation bakes in at `emit.cpp:1230/1231` for tab/move. So the leak is **simultaneously load-bearing for DIRECT consumers and harmful for DOWNSTREAM locals**. That is the per-box `op_zdepth` "every box is balanced at handoff" model breaking in both directions at once.
  - **CONSEQUENCE FOR THE PLAN — this is positive evidence for Z-3, not just a dead end:** a release-only fix cannot work at ANY of the three sites, because the compensations that assume the carve persists would all have to come out in the same motion. **The cell must move OFF the spine into a ζ-ACTIVATION FRAME (Z-3 core) and the `op_sa`/`op_zdepth` compensations must retire WITH it, in one commit.** Reverted; floor re-proved 235/28/30 at a clean tree.
- **⭐ THE s232 COERCION PAIR WAS UNSOUND — the signal was right, the instrument was not.** `bench_icnstr_concat_dispatch` vs `..._int_dispatch` differ by **THREE** variables (literal vs variable right operand, an added `i%97`, and the coercion), and the literal‖literal case is **loop-invariant and constant-foldable**, so its speedup may be measuring the OPTIMIZER. Also **3 of that board's 10 programs were never committed and are LOST**. Minted the single-variable set in corpus `27cc99d2`, auto-wired via the `bench_icn*` glob: `bench_icnstr_concat_strvar` (2.02x, no coercion) · `bench_icnstr_concat_intvar` (0.67x, coercion) · `bench_icnint_mod_isolate` (**5.39x — EXONERATES the modulo the old pair had confounded in**).
- **⭐ HONEST BOARD, 10/10 byte-identical vs live iconx, REPS=5** (post-coercion-fix): int loop **8.8–9.4x** · mod isolate **5.39x** · str‖strvar **2.02x** · concat table 1.25x · sub list 1.40x · concat_int_dispatch 0.72–0.90x (was 0.48–0.61x) · table-miss dispatch 0.86x · str‖intvar 0.67x · table-miss semantics 0.50x. **Headline 8 still 0/8** (concord/geddump SEGV, tgrlink abort, micsum 1-line near-miss, rest empty) — blocked by the tab/move carve leak, so NO headline speedup number is valid.
- **⭐ NEW INSTRUMENT — STARTUP FLOOR, and it changes how the small end of the board must be read.** Measured on an empty `procedure main();end`: **iconx 0.7ms vs SCRIP 1.9ms (+1.2ms fixed)**. `bench_icnsub_table_miss_semantics` totals 4ms vs 8ms, so roughly a THIRD of its "0.50x" is process startup, not semantics. ⛔ Do not read sub-10ms rows as engine speed, and do not let that row drag a geometric mean: excluding it moves the 7-program geomean from 1.25x to 1.50x.
- **ORACLE BUILD CONFIRMED (s232's simpler recipe is correct):** `make Configure name=linux` configures NoGraphics directly — no `define.h` edit, no `make X-Configure` revert trap. Only `XLIBS=` in `Makedefs` needs clearing, then `make All`. Reproduced clean this session.
- OWED: push credential (2 commits + this cursor) · Z-3 slice 2 (now the ONLY route to Z-4) · the tab/move leak remains OPEN and `_lucky` still passes by luck.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s232 (Claude Opus 5) — **Z-4 ROOT CAUSE PROVEN: THE FORTH CARVE IS NEVER RELEASED ON THE SCAN SUCCESS EDGE. THE RECORDED "Δ NOT ESTABLISHED" DIAGNOSIS IS FALSIFIED. FIRST HONEST ICON PERF BOARD LANDED.**
- **Commits:** corpus `7caceb7d` (3 witnesses) · SCRIP `9d5f324e` (`scripts/honest_icon_microbench.sh`). ⛔ NOTHING PUSHED — credential ask OPEN, covering exactly these 3 (one per repo). ⭐ CORRECTION to the s231 cursor: its "5 unpushed SCRIP commits" claim is STALE — `bb934ff3` and its predecessors are ON origin/main (verified `git branch -r --contains`); only this session's commit is ahead. Per STALE-ORIENTATION (a), do not carry a push-status claim forward from prose — re-derive it.
- **WATERMARK at `bb934ff3`, clean tree, pinned, `pgrep scrip`==0 before+after:** Icon m3 **235/28/30** (matches record) · canary rc=0 ×3 arms · oracle = Arizona Icon 9.5.25a built this session.
- **⭐⭐⭐ ONE DEFECT, FOUR SYMPTOMS — Z-4 IS NOT A Δ-ESTABLISHMENT RUNG.** Scan-family boxes carve a FORTH cell at α (`sub rsp,16`) and release it ONLY on the failure/β edges. **The SUCCESS edge has no release at all**, so the carve LEAKS, once per scan-family box in the chain. Locals are rsp-relative ⇒ every later local ref is shifted. Measured with gdb: `tab(match("-"))` = two carves ⇒ rsp **32 low** at the read ⇒ `[rsp+384]` hits zeros; `tab(2)` = one carve ⇒ rsp **16 low** ⇒ lands on a DUPLICATE copy of the same descriptor and is **ACCIDENTALLY CORRECT** (`x/2gx` showed identical DESCRs at `[rsp+352]` and `[rsp+368]`).
  - **Chain:** leak shifts rsp → next block's `scan_enter` reads its subject operand from the shifted slot → zero DESCR → `&subject` empty → Δ=0 → `pos(0)` computes `cvpos(0,0)=len+0+1=1 == &pos` → wrongly SUCCEEDS. That IS the recorded "stale Δ", and it is DOWNSTREAM. options() gates 7 of 8 headline benchmarks.
- **⛔⭐ FIX DIRECTION — CORRECTED LATE IN s232, SUPERSEDES THE "JUST ADD A TERMINAL RELEASE" READING ABOVE.** The missing γ release is **NOT an oversight**. THE EXACT SITE: `x86_asm.h:489` gates the γ-port release on `x86_fc_on() && _.op_fc_base < 0` — the "carve-only class" — while the ω port (`:488`) releases unconditionally. `emit.cpp` IR_SCAN_TAB/IR_SCAN_MOVE set `op_fc_base = op_off + 16` (≥0), so **tab/move are excluded from the γ release BY CONSTRUCTION**. The reason is visible in the emitted asm: `n6_scan_tab_β: mov r14,[rsp+0]` — **β reads the saved cursor out of that very cell**, per ARCH-ICON's "cursor-movers reversed-on-resume". The cell MUST survive γ.
  - **So the real defect is architectural, and it is exactly the ζ conversion this goal already calls for:** the suspended cursor cell **rides the SPINE (rsp)**. Retaining it past γ — which correctness of β requires — leaves rsp deep while execution continues, and every later rsp-relative local ref is shifted. Per-box `op_zdepth` ("a box compensates for exactly what IT carved") is a model that assumes each box is balanced at handoff; a box that legitimately suspends breaks that assumption. **Fix = move the retained cursor cell OFF the spine into a ζ-ACTIVATION FRAME chained off STANDING (Z-3's core), not a terminal release.** Dual of the generator bug already recorded in Z-3: generators POP at γ when they must RETAIN; cursor-movers RETAIN at γ while the spine model assumes they popped. Same root, opposite sign.
  - **⭐ BLAST RADIUS + A CHEAP TEST TO RUN FIRST (s232, last finding).** EXACTLY THREE boxes set `op_fc_base >= 0` and are thus excluded from the γ release: `IR_SCAN_TAB`, `IR_SCAN_MOVE`, `IR_SCAN_MATCH` (grep of emit.cpp; the conversion scope is that small). ⛔ BUT **MATCH LOOKS MISCLASSIFIED**: ARCH-ICON classes match as a POSITION-RETURNER (`{0,1}`, δ-untouched), not a cursor-mover, and the emitted asm agrees — `t2.s` emits `n5_scan_match_α` with two failure exits and **NO `_β` label at all**, while tab emits both `α` and `β`. A box that never resumes has no reversal-on-resume state, so its retained carve may serve NO purpose. TEST BEFORE THE BIG CONVERSION: release match's carve at γ (drop it from the ≥0 class) and re-measure — that alone halves the leak in `tab(match(..))`, which is the `=s` shape gating options(). ⛔ NOT a fix on its own: the SINGLE-carve case still leaks 16 and passes only by the duplicate-DESCR luck documented above. Treat as a cheap narrowing experiment, not a rung.
  - **⛔ HONEST STATUS OF THE HAND-PATCH:** inserting `add rsp,32` at the scan-leave DID make `tab(match("-"))` oracle-exact — but that witness never resumes β, so it does not license the release as a fix. I then tried to demonstrate the release BREAKING a β-resume program (`if tab(match("-")) & pos(0) ...`, which does reach `scan_tab_β`) and **the patched binary still passed — the demonstration was INCONCLUSIVE, not a vindication of the release.** Recorded as inconclusive deliberately: do not cite the hand-patch as evidence the release is safe, and do not cite my failed break-attempt as evidence it is safe either. The line-489 gate + the β read of `[rsp+0]` are the load-bearing facts.
  - **⛔ ACCIDENTAL PASSES ARE IN THE SUITE.** `witness_icn_carve_leak_lucky.icn` is committed passing, by luck. Some of today's 235 may be luck of the same kind — treat single-carve scan passes as unproven until the release lands.
- **⛔ FALSIFIED THIS SESSION (do not retry):** "Δ not established at scan entry" (single-block `pos(0)`/`pos(-1)`/`*&subject` all oracle-exact) · "the s231 `op_sa` carve compensation causes it" (`SCRIP_NO_CM_CARVE=1` changes nothing — **`bb934ff3` is exonerated**) · "any non-literal operand triggers it" (`tab(1+1)` clean; only a scan-family PRODUCER as operand, i.e. only when two boxes carve) · patching the cursor-mover's own failure-edge releases 16→32 (no effect — the success edge has nothing to patch).
- **⛔ NEXT RUNG (Z-4, restated):** enforce the ζ-SPINE law already written in THE MODEL — "α grants `sub rsp,K`, **ZERO intermediate pops, ONE terminal release**". Remove the per-box intermediate pops so depth at the leave box is UNIFORM across incoming edges, then emit ONE terminal release of the cumulative grant. ⛔ A naive per-edge `add` is WRONG: the failure edges have already popped, so depth is edge-dependent today — that asymmetry is the bug, not a detail. Shared emitter ⇒ R-ICN-D (SN4 + Prolog invariance) + triple-arm canary on the commit.
- **⭐ FIRST HONEST ICON PERF BOARD** (correctness-gated, two agreeing runs; `scripts/honest_icon_microbench.sh`): 10/10 byte-identical, **geometric mean 1.70x**, 6 faster / 4 slower. int loop **7.55x** · numeric isolate **5.07x** · relational isolate **3.62x** · str concat 2.48x · sub list 1.59x · concat table 1.57x · table-miss dispatch 0.97x · **numeric MIXED 0.72x** · **concat WITH int coercion 0.66x** · table-miss semantics 0.50x. The headline 8 remain **0/8** (3 now SEGV/abort: concord, geddump, tgrlink; micsum a 1-line near-miss) — blocked by the carve leak above, so no headline speedup number is valid.
- **⭐ SIGNAL — Z-5(b) IS UNDERPRICED.** The two worst results are both COERCION, and they form a matched pair across two independent paths: str concat 2.48x vs concat+int-coercion 0.66x; numeric isolate 5.07x vs numeric mixed 0.72x. One shared coercion cost, ~3.8x and ~7x swings. Recommend Z-5(b) be re-ranked against Z-3.
- **ORACLE BUILD — SIMPLER THAN RECORDED.** `make Configure name=linux` configures **NoGraphics** directly: no `define.h` edit, and the `make X-Configure` regenerate-and-silently-revert trap never arises. Only `XLIBS=` in `Makedefs` still needs clearing, then `make All`. Recommend replacing the two-edit recipe in the s231 cursor with this one-liner.
- **INSTRUMENT TRAP (cost this session ~15 min, is the Z-0 failure mode):** `--compile` writes asm to **stdout**; a harness that looks for an output binary prints a plausible full-board **0/7 EMITFAIL** and measures itself, not the compiler. Sanctioned recipe: `--compile --target=x86 > f.s` then `gcc -no-pie f.s -L$RTDIR -lscrip_rt -Wl,-rpath,$RTDIR`.
- OWED: push credential · the Z-4 release fix above · Z-3 unchanged.

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity where applicable, triple-arm watermark (single-arm after Z-8) + SN4+Prolog invariance, cursor move, push mid-session

- [~] **Z-0 · GROUND TRUTH + INSTRUMENT REPAIR — INSTRUMENTS LANDED, GROUND TRUTH RE-OWED.** The instrument half is done (m4 runner rebuilt ×2 by two workers, scorecard repointed, live oracle built, MEASUREMENT HYGIENE + CONCURRENCY laws minted). The ground-truth half must be REDONE on a pinned commit with a clean tree, because the second worker's 22:21:38 rebuild voided the number board. Prior wording (now false): triple watermark (twice, agreeing) · 28-fail classification · gate board at the sanctioned timeout · smoke · crosscheck · **m4 runner rebuilt and landed** · **scorecard re-baselined META 69.1** · **live Icon 9.5.25a oracle built** · two prior-session diagnoses retired by re-run · MEASUREMENT HYGIENE law minted. Nothing owed.
- [~] **Z-1 · ONE SYSTEM — ARM UNIFICATION — ⛔ IN FLIGHT BY THE OTHER WORKER, DO NOT DOUBLE-DRIVE.** Uncommitted in the shared tree as of 22:29: `icn_cells_graph=1` stamped by default, `SCRIP_ICN_LEGACY=1` as the restore-the-fork killswitch, and the two NV gates rewritten for the cells regime. ⛔ The apparent "arms are behaviorally identical / globals closed" evidence is an ARTIFACT of measuring that in-flight change (cells default ON ⇒ the default arm IS the cells arm); the provenance-clean `ee315070` reading still shows rung21/rung25 FAILING on the true default arm. Whoever lands this owes: byte-identity of emitted `.s` under `SCRIP_ICN_LEGACY=1`, an honest pinned watermark, and Lon's ruling on the gate relaxation. Kill the `getenv` arms at the `lower_icon.c` R-ZK-A stamp site (~:1422) behind a transition killswitch (`SCRIP_ICN_LEGACY=1` restores today's routing byte-exactly until Z-8 deletes it — NO new global; the existing env-read pattern inverted). Verify by BYTE-IDENTITY of emitted `.s` over the corpus, not merely by suite parity — identical scores can hide divergent codegen. Floor: 235/28/30 m3 + 234/29/30 m4 held; 8 gates green; SN4+Prolog invariant.
- [ ] **Z-2 · ζ-STANDING COMPLETE.** Per-graph activation base on the shared mechanism: wire header (γ/ω/caller-base), params/locals install, return protocol — verified against the SN4-proven establishment (behavioral names only; no LANG sentinels). Gates local_no_nv + rbp_census_ratchet green; `test_gate_icn_var.sh` re-read (its RED overlaps the var/globals classes — re-derive after Z-1).
- [ ] **Z-3 · SUSPENSION = ζ-ACTIVATION-FRAMES (the conversion's heart; expected yield ~10 FAILs). SLICE 1 LANDED `b1fc1ac4` (opt-in `SCRIP_ICN_GENFRAME=1`): generator graphs admitted to establishment; prologue + wire-based γ/ω now emit correctly. ⛔ SLICE 2 = THE CORE, stated precisely from measurement: (i) γ must RETAIN the activation, only ω frees it (today γ does `add rsp,kt` and destroys the suspended frame); (ii) the caller handshake `.Lx14_3: mov [rsp+136],rsp` / resume `mov rsp,[rsp+136]; jmp qword ptr [rsp]` must agree with the retained base and with the generator's parked resume address (`[rsp+48]`). Then** replace `icn_zframe_gen` + `rt_gen_save_wires` (one-active-suspension) with real per-activation frames chained off STANDING, self-pop at ω. ⛔ READ FIRST: `refs/icon-master/src/runtime/interp.r` efp/gfp mark/esusp (the canonical precedent — verified present s230) + each construct's `ir_a_*` topology in `refs/jcon-master/tran/irgen.icn`. ⛔ MONITOR-FIRST ENTRY POINT (s230): run each witness under **m4** — 9 of the 10 SEGV there, so the faulting instruction and its frame arithmetic are readable directly from the emitted `.s`, instead of chasing m3's silent wrong output. Witness set: the rung03 suspend quartet · recogn (recursive suspend) · level (rsp-drift-per-β) · proc_lookup (DC-stub double-execute) · subscript_genproc · genqueen · meander · nested every-over-to · disjunction resumed across depth · suspend-in-arg-list. **⛔⭐ THE RETIRE-TOGETHER SET (enumerated s233 — the release-only experiment failed precisely because these were left in place; a Z-3 slice that moves the scan cell off the spine MUST retire all of them in the SAME commit, and each is a compensation that exists ONLY to model a carve that persists past γ):** (1) `emit.cpp:1230` IR_SCAN_TAB `op_sa += fck` (s231 `90618b21`, killswitch `SCRIP_NO_CM_CARVE=1`) — compensates the FOREIGN operand slot; (2) `emit.cpp:1231` IR_SCAN_MOVE, the identical twin; (3) `emit.cpp:1236` IR_SCAN_MATCH — windowed `op_fc_base = op_off+16` with NO `op_sa` bump, the asymmetry that made it look misclassified; (4) `emit.cpp:1000` `op_zdepth = op_fc_bytes` — the per-box "a box compensates for exactly what IT carved" model, which is the assumption that breaks; (5) `x86_asm.h:489` the γ-release gate `op_fc_base < 0 || op_zgpop > 0`; (6) `x86_asm.h:2412` the three-class discipline comment (carve-only / windowed / op_zres) — the windowed class's rationale is "gamma-live state is the whole point", which is TRUE for tab/move and FALSE for match, and that split stops mattering once the cell leaves the spine. ⛔ The direct-consumer read is the one to instrument first: `rung08_strbuiltins_match` (`"hello" ? write(match("hel"))` → `4`) is the cheapest witness that a conversion has kept direct consumers whole, and the three `corpus/probe/icn/witness_icn_carve_leak_*.icn` are the cheapest witnesses that it has closed the downstream leak. A correct Z-3 slice turns BOTH green at once; s233 proved you cannot buy one with the other. Then WALK THE FAMILY, one rung-let per kind citing its topology: `IR_TO_BY · IR_DISJUNCTION · IR_REPALT · IR_PROC_GEN · every/limit/bang`. Land the topology table ONCE as `.github/ICON-IRGEN-TOPOLOGY.md` (STATIC/DYNAMIC arrival-depth verdict per kind — subsumes old ICN-FB-1). **✅ LANDED s236 `5689e4a3` — all 43 `ir_a_*` rows, signatures extracted mechanically, re-derive recipe embedded and verified. ⭐ READ ITS D1/D2/D3 SPLIT BEFORE SLICE 2: pure-D1 (runtime resume TARGET) is 39 PASS / 0 FAIL at `860a9e43`, and all 16 Z-3+Z-4 failures are D2 (`ir_Succeed`) or D3 (`ir_ResumeValue`/`ir_ScanSwap`) — state retained across γ is the defect, arrival-depth dynamism per se is not.** Measure Ir + peak RSS on the witness set as VALIDATION (the T-vs-P decision itself is settled by Lon's s230 ruling: frames, no pthread-per-generator; pthreads remain coexpr-only per R-ICN-C).
- [ ] **Z-4 · SCAN ENVIRONMENTS ON THE FRAMES — ⛔ ROOT CAUSE PROVEN s232; THE CHEAP RELEASE-ONLY ESCAPE IS FALSIFIED s233 (see LIVE CURSOR: `rung08_strbuiltins_match` regresses because DIRECT consumers read match's result through an rsp address calibrated on the carve PERSISTING). Z-4 IS NOW STRICTLY DOWNSTREAM OF Z-3 — the cell moves off the spine and the `op_sa`/`op_zdepth` compensations retire in the SAME commit, or not at all. READ THE LIVE CURSOR BEFORE THIS BULLET.** The gating defect is **NOT** Δ establishment (falsified s232: single-block `pos(0)`/`pos(-1)`/`*&subject` are all oracle-exact). It is that **scan-family boxes never release their FORTH carve on the SUCCESS edge** — only on failure/β — so the carve leaks once per box and shifts every later rsp-relative local ref. ⛔ Fix is NOT a terminal release (corrected late s232): the γ release is excluded BY CONSTRUCTION at `x86_asm.h:489` (`op_fc_base < 0` gate) because β reads the saved cursor from that cell. The cell rides the SPINE; retaining it past γ — which β requires — shifts every later rsp-relative ref. Fix = move the retained cell OFF the spine into a ζ-ACTIVATION FRAME (Z-3 core). See LIVE CURSOR. Witnesses `corpus/probe/icn/witness_icn_carve_leak_{var,subject,lucky}.icn` (`_lucky` passes BY ACCIDENT — a single 16-byte leak that happens to land on a duplicate DESCR; it is the control). Hand-patch proof recorded in corpus `7caceb7d`. The remainder of this bullet stands as written: `?` environment = &subject/&pos save-restore as activation-frame slots — the spec is `ir_a_Scan`'s ScanSwap topology (irgen.icn, read s230) + `fscan.r`/`fstranl.r`; R13/R14/R15 Σ/δ/Δ discipline with once-at-entry establishment (old R-3(d): fix = one both-medium site at entry, NOT the read, NOT xa_flat's shared prologue); position-returners δ-untouched vs cursor-movers reversed-on-resume per ARCH-ICON (do not blur). ⛔ The old fail-edge diagnosis is DEAD (root repro passes at `ee315070`) — MONITOR-FIRST a fresh first-divergence on jcon_scan before touching anything; scan/scan1/scan2 all SEGV under m4 (readable fault site) while subjpos/wordcnt/scan_alt give wrong output — work the crashers first; the 7 falsified hypotheses in history at `835f4131` remain do-not-retry. Gate scan green; smoke 14/14.
- [ ] **Z-5 · FAIL-ZERO — the 12 independent classes, one CLASS per commit, witness minted, oracle-verified, re-measure each first.** (a) ⛔ FIRST — the DT_DATA stale-asm-tag SILENT CORRUPTION (`rtx_icnagg.S`/`rtx_icnsub.S` carry 100; live value 0x70): fix + land the single-source `.inc` header gate (third recurrence of the class); (b) mathfunc = BINOP_CONCAT coercion carried in IR by LOWER (9 files at HEAD — full-budget, BID-AT-LOWER); (c) variable() lvalue — ⛔ NEEDS LON'S name-table ruling (sealed RO blob vs startup table); (d) FZ-C SEGVs endetab/fncs1/scan2 · FZ-D parse htprep/prepro · FZ-E remainder args/coerce(⚠241MB-use-head)/mffsol/mindfa/kwds/cxprimes — MONITOR-FIRST each; anycmp mixed-type ordering stays deliberately-unfixed (ruling on record).
- [ ] **Z-6 · m3 ≡ m4 — nearly free at s230.** Measured delta is ONE program (`rung36_jcon_proto`, emit-fail under `--compile`) plus the 28 shared classes that Z-3/Z-4/Z-5 close anyway. Fix jcon_proto's emit path; hold DIRTY-PASS at 0 (the crash-aware grade is the point of the rebuilt runner); `.s` regen freshness discipline; sweep the COMPILER never the artifacts.
- [ ] **Z-7 · XFAIL-ZERO — 30 markers (count re-verified s230).** Per marker: remove, run, fix-or-delete. Clusters: bignum (radix/lgint/arith/checkfpx/cxprimes — arbitrary-precision ints, own sub-rung, ⛔ Lon scopes it) · ck (generator-in-arg to Image()) · profsum (`next` inside scan-body must restart enclosing while) · ~24 empties to classify fresh (some pass free — cxprimes ran 25/25 once under s18).
- [ ] **Z-8 · BENCH HONEST-GREEN + GATES STRICT + THE FOLD.** honest_icon_correctness 8/8 vs live iconx; BENCH-1 link-library resolution + deal/rsg seed-pin oracles (`options/post/shuffle` are LINK-DEP LIBRARIES, no main — grade accordingly); confirm rsg end-to-end retires R-3(c) for good. Then ZK-8 100% assertion gate (`test_gate_icn_zcells.sh`: whole-graph carve 0 · .bss backtrack arenas 0 · heap-ζ for generators 0 (coexpr exempt) · locals/params=cells · globals=GVA · R13/14/15 grep 0 · `=0` byte-identity · suite-floor ratchet). Then THE FOLD, one family per commit, byte-identical OFF→deleted: `SCRIP_ICN_CELLS`/`SCRIP_ICN_ZFRAME`/`SCRIP_ICN_LEGACY` env reads · `zframe_graph`/`icn_cells_graph` dual-stamp scaffolding · `rt_gen_save_wires` family · dead ZC_FRAME RBP arms (s202 evidence favors deletion). Ask Lon to retire RULES' vestigial `--dump-sm` gate line (verified 0 refs s230) + the old r12-as-TOS wording.

**PARKED — PERFORMANCE / ARCHITECTURE, AFTER 100% (history at `835f4131`; do not open before DoD 1-4 green unless Lon directs):** the ICON-RTX ladder open queue (RTX-21/17/22/4/5/6c/7/10/13/14/25/26-remainder-ICN + Phase-2 RTX-11/12; landed arms + REFUSED/DO-NOT-PORT rulings in `RTX-CLAIMS.md` + git; RTX-23/25 ⛔ BLOCKED ON LON) · GN-PERF slot-vs-nv A/B (GVA won; NV remains the reflective/cross-language binding via `NV_bind_gva`) · `-O2` runtime pass (O2-DIRECTED-ONLY) · JCON-compiler-modules byte-compare (data-level, never JVM) · the perf graveyard (tolower guard/always_inline/asm rt_subscript_var/value-subscript fusion/pointer-keyed memo UNSOUND/goto-past-chain UNSOUND/288-arm guard trap) — binding, do not re-derive.

---

## LEDGER v2 — EVERY s229 RUNG, RE-VERIFIED AND DISPOSITIONED (against SCRIP `ee315070` source + fresh triple suite + live repro runs, 2026-08-15 s230; legend: **DONE** verified in tree/run · **→Z-n** absorbed · **OBS** obsolete/superseded · **LIVE** re-confirmed failing/pending)
**R-0 resurrect-the-default-arm (bisect):** **DONE** at `ee315070` ("R-0 LANDED... the frame-base lea silently emitted nothing, ZB-FC-1" — also resurrected Prolog); canary rc=0 ×3 arms and default 1→234 measured s230. The bisect METHOD is retired by Lon's s230 ruling — conversion, not regression-hunting, is the plan.
**R-1 process/FACT-RULE ask:** **LIVE** → the STANDING PROCESS ASK section; surfaced in-chat s230.
**R-2 anchor parity (252 die-list) + gates:** anchor framing **OBS** (pre-new-system anchors; 252/11/30 is a pre-collapse arm-era number). Its 5 in-scope fails all re-measured FAIL s230 → **→Z-3** (recogn/level/proc_lookup/rung03_suspend_return) + Z-3/Z-5 (prepro is FZ-D parse). FR-6a per-arm zframe gate **OBS** (ONE system; floor-ratchet discipline survives in the scorecard). FR-6d m4 runner **LIVE** → **→Z-0(a)** (shipped runner verified deleted-era: `-icn`+nasm+`/home/scrip`).
**R-3 FAIL-ZERO:** (a) scan fail-edge root **FIXED-VERIFIED** at `ee315070` (repro ELSE rc=0) — cluster still fails, diagnosis re-derives fresh → **→Z-4**; (b) variable() **LIVE**, Lon ruling owed → **→Z-5(c)**; (c) case-arm disjunction **FIXED-VERIFIED** (repro iters=3 rc=0; rsg end-to-end confirm at Z-8); (d) &pos establishment → **→Z-4**; (e) mathfunc/BINOP_CONCAT **LIVE** (9 files) → **→Z-5(b)**; (f) DT_DATA stale tag **LIVE-VERIFIED** (100 vs 0x70) ⛔ silent corruption → **→Z-5(a) FIRST**; (g) endetab/fncs1/scan2/htprep/prepro/args/coerce/mffsol/mindfa/kwds all re-measured FAIL s230 → **→Z-4/Z-5(d)**; (h) bench residue → **→Z-8**, re-measure first (0/8 EMITFAIL predates resurrect).
**R-4 XFAIL-ZERO:** 30 markers re-counted s230 → **→Z-7** intact.
**R-5 THREE ZETAS completion:** ZK-6 T-vs-P suspension decision **SETTLED by Lon's s230 ruling** (ζ-ACTIVATION-FRAMES; measurements become validation) → **→Z-3**; ZK-7 family walk → **→Z-3**; ZK-8 assertion → **→Z-8**; GVA-M3 residue **CLOSED-VERIFIED s230** — rung21/rung25 PASS on the default arm (6/6 standalone + in two clean suite runs) and `global_no_nv_m3` is GREEN at 240s; the s229 "fails on default" record was a contaminated measurement.
**R-6 m3≡m4 + instrument debts:** **DONE s230** — runner rebuilt with crash-aware CLEAN/DIRTY grading and landed; first honest m4 watermark 234/29/30, parity to within ONE program; scorecard repointed to the shared parser.
**R-7 deletions + killswitch fold:** the frames-vs-cells DEFAULT DECISION MEMO is **OBS** — Lon's ruling ends the arm contest; folds/deletions → **→Z-8**; `--dump-sm` RULES vestige **VERIFIED** (0 refs) — retirement ask rides Z-8.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet · Claude Fable
**Architecture:** `ARCH-ICON.md` · `ARCH-SCRIP.md` (ζ tier glossary) · `ARCH-ZETA-LOCAL-STORAGE.md` · `ARCH-ICON-RTX.md` · `DESIGN-ICON-SUSPEND.md` · `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md`

## Session-close / push protocol
See RULES.md — `scripts/handoff_status.sh` verbatim stdout is the ONLY sanctioned completion claim.
