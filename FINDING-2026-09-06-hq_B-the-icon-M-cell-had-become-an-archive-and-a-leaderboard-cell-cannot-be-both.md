# FINDING 2026-09-06 (hq_B) — the icon M cell had become an archive: twenty sentences of analysis inside one leaderboard cell, and the cell could not be updated without deleting them

`util_score_row.py` REFUSED to rewrite the September-10 grid's icon **M** cell, in the right way and for the
right reason: *"grid M carries 20 sentence(s) no runner models -- a person wrote them; fold what is still true
into --text or hand-edit the cell."* The cell was stale (607/609 against a board reading 642/655, CEO-309) and
could not be refreshed by any runner, because refreshing it would have deleted analysis that no runner could
regenerate.

⭐ **That refusal is the correct behaviour and it is worth keeping.** A cell a runner owns must be rewritable by
that runner; the moment a person writes durable analysis into it, the two purposes collide. The tool cannot
resolve that collision — only a person can, by moving the analysis somewhere versioned. That is what this
FINDING is: the cell's full prior content, archived verbatim, so the cell can go back to being one measurement.

⛔ **What is NOT still true**, and is why the cell had to move: both reds it names — `ladder_rung26_pow_pow_negbase_real`
and `ladder_rung26_pow_pow_zero_negexp` — were CURED on 2026-09-06 (SCRIP `0719bd9bd`); the board now reads
642/655 both modes over a population of 655, and the watermark it discusses (596 → 607) has moved twice since.
The `procedure_write_image_1` and `procedure_record_every_replace_2` analysis below describes a 599/601 reading
from 2026-09-03 and was already marked superseded *inside the cell*.

⭐ **What IS still worth keeping**, and the reason this is a FINDING rather than a deletion: the shared-node
bisect (an Icon red caused by a SNOBOL4 landing, through a serial drawn by `rt_args_list_from` → `rt_make_list`),
the `map()` trailing-duplicate witness, and the author's own recorded wrong first diagnosis. Those outlive the
numbers around them — a diagnosis deleted because its symptom left the board is how the same defect gets
rediscovered from scratch.

## The cell, verbatim as it stood before the 2026-09-06 rewrite

```
 run-graded **607/609 both modes** (entries=762, floors m3 607 / m4 607) + ast-shape 153/153 (informational, not scored) — `board_icon_master.sh`, hq_B SCRIP `b812fb6d1` corpus `8972babeb` RT_OPT=-O0 2026-09-05 15:56 CDT. Reds by name: FAIL ladder_rung26_pow_pow_negbase_real · FAIL ladder_rung26_pow_pow_zero_negexp — both NEW entries (ALL.csv rows 761-762, corpus `bb5ee2b69`) that have never passed, NOT a regression, and rowed as `icon-pow-error-cases-fail-or-print-nan-instead-of-raising-204-and-206` with the committed gate `test_gate_icn_pow_error_cases.sh`. The watermark was re-pinned 596 -> 607 this sitting: pinned to what today holds, never to the population, so curing that row moves it to the full count. ⛔ EVERYTHING BELOW THIS SENTENCE IS SUPERSEDED PROVENANCE, KEPT AS ANALYSIS AND NOT AS CURRENT STATE: it describes the earlier 599/601 reading whose two reds (`procedure_write_image_1`, `procedure_record_every_replace_2`) are NOT in today's red list. ⭐ It is kept because the shared-node bisect and the `map()` witness are reusable, and deleting a diagnosis because its symptom left the board is how the same defect gets re-diagnosed from scratch — but do not read either as open today without re-measuring. ⛔ CORRECTED FROM `601/601` ON THE ceo'S AUDIT: the cell claimed FAIL=0 while the board read 599, and BOTH reds are now bisected and named rather than left as "output mismatch". ⭐ RED 1 `procedure_write_image_1` is a SHARED-NODE REGRESSION FROM A SNOBOL4 LANDING: `git bisect` over the 67 commits since `6f3d0e72c` (baseline re-proved good, not assumed) names SCRIP `1d31e2963` "HOST(2,i) and HOST(3): implement the argv contract both oracles actually have", which removed the four `nparams >= 1` gates on `rt_main_args_stage` in `src/driver/scrip.c`. Staging calls `rt_args_list_from` -> `rt_make_list`, which draws an Icon list serial from `rt_agg_serial_list()`, so every Icon program now burns serial 1 at startup and `write(image([1,2,3]))` prints `list_2(3)` where icont/iconx print `list_1(3)`. ⛔ THE GATE WAS RIGHT FOR ICON AND WRONG FOR SNOBOL4 AT THE SAME TIME, which is why removing it looked safe: measured on the oracle, Icon materializes the args list IFF the entry has a formal parameter — `procedure main(a)` gives `list_2(3)` and `procedure main()` gives `list_1(3)`, independently of whether args are actually passed — while SNOBOL4's HOST() must see argv regardless of the entry's params (`genc.sno` has a zero-param entry, and that was the defect `1d31e2963` cured). One gate, two contracts. A cure must keep HOST(3)'s staged count exact while not letting an unobservable bookkeeping list consume a program-visible serial. NOT CURED HERE: it is the HOST row's own contract decision, and Lon's SNOBOL4-ONLY order stands. ⭐ RED 2 `procedure_record_every_replace_2` is INDEPENDENT and not a regression: `map()` with a `&cset`-derived source ignores trailing duplicate mappings. One-line witness, oracle-confirmed — `map("hello", &cset CAT "e", &cset CAT "-")` (CAT = Icon's two-pipe concat, spelled out because this is a markdown table) gives `hello` in SCRIP and `h-llo` in iconx, while `map("hello", repl("z",256) CAT "e", repl("z",256) CAT "-")` is correct in both, so it is NOT a 256-length cap and NOT the last-duplicate-wins rule (`map("abc","aa","xy")` gives `ybc` in both). ⚠ My first reading of this one was WRONG and is recorded so nobody repeats it: I diagnosed a missing companion file because a hand-run in an empty dir could not `open("fncs1.dat")`. The harness already searches `config/`. The file was fine, my test dir was not. ⭐ The builder that grows this master had been REFUSING rc=2 for every seat on a duplicate entry name (`procedure_scan_tab_2` carried two different programs); cured, so this number can move again. ⛔ Was `377/381 RED by 2`: that reading is superseded twice over — the two reds were CORRECT OUTPUT FAILING A BAD `ALL.ref` (cured, corpus `bfa2adfa`), and the population has since grown 381 → 409 → 569 under several seats in one evening. Do not pin either number; `board_icon_master.sh` prints both; STRICT rung suite 266 PASS · 4 FAIL · 1 BADEXIT · 26 XFAIL · 1 XPASS of 298 (seat16 `0fca0dc3`) 
```
