# FINDING — `loopctl-inline-lt-concat-store` (rank 1, locked via `next`) IS ALREADY LANDED. ALL THREE SUB-ITEMS VERIFIED AT HEAD. CLOSING THE ROW, NOT WORKING IT.

**seat03, 2026-08-22. Picked up via THE LOOP (`s4e_msg.sh next`). The printed brief (FINDING s200's "RANKED BY MEASUREMENT" list, unmodified) asked for three sub-items still to build. HQ's own s249 handoff cursor (`GOAL-SCRIP-HQ.md` line 317, which is NEWER than the s200-era brief — newest session state sorts to the top per STALE-ORIENTATION(c)) already reports two of the three landed, and direct inspection of HEAD shows the third was never missing. This is the FINDING rule-3 case exactly: the brief disagreed with reality, so record it, verify against the ACTUAL COMPILER (RULES: "sweep the COMPILER, never the artifacts"), and report the corrected state rather than re-implementing work that already exists.**

## THE DISCREPANCY
`next` printed the s200 FINDING's original three-item plan verbatim, with no indication two of the three were already done. The gap: `GOAL-SCRIP-HQ.md`'s s249 HQ-HANDOFF entry (2026-08-21, one session after s200) already states *"Null-concat copy propagation −7.4 cyc (solid) · cmp_test integer inline −3.1 (probably real) … The null-concat landed in `cp_source()` at the OPTIMIZER, which s200 predicted."* Nothing in the queue/brief layer reflects this landing, so `next` re-offered a finished row as open work. Recommend HQ correct the row's bookkeeping so it stops being handed out (this seat cannot find `QUEUE.tsv` on disk to edit directly — `s4e_msg.sh next`/`check` seem to resolve it from somewhere this seat doesn't have write access to or visibility into; routed to HQ below).

## VERIFIED AT HEAD (SCRIP `568bf098`, pristine `-O0` build, `out/libscrip_rt.so` fresh)
Ground truth is the compiled output of `corpus/benchmarks/snobol4/arith_loop.sno` — the exact program s200's asm walkthrough used — regenerated with `--compile` and diffed **byte-identical** against the tracked `.s` (committed today 14:20:19, SHA `e95789f4`), confirming the tracked artifact IS current HEAD, not stale.

**(1) Null-concat elision — DONE, `979feb4a` "Widen cp_source() to the null-concat identity … (s249)".**
`src/optimizer/copy_prop.c:20-22`: `cp_source()` treats an `IR_BINOP`/`BINOP_CONCAT` node as a pure copy of its non-null operand when `ir_value_is_null_string()` proves the other operand null **by construction** (predicate result type, not a name list — satisfies the brief's explicit constraint). `cp_run`'s second pass then turns the unreferenced copy into `IR_SUCCEED` — the box is deleted from the graph, not merely bypassed. In `arith_loop.s`, the labels run `n47_cmp_test_α` (the `LT`) → `n48_var_α` (loading `ZI` for `ZI+1`) with **no concat/binop node between them at all** — the elision is structural, not a runtime skip. Killswitch `SCRIP_OPT_NULLCAT` (default ON, `copy_prop.c:7,20`) verified live: `SCRIP_OPT_NULLCAT=0 ./scrip --compile arith_loop.sno` restores `str_concat_d` call count 7→9 for this program.

**(2) `rt_cmp_d` integer inline — DONE, `ec34eba0` "arith_loop +41.9% THROUGHPUT, 165 -> 120 INSTRUCTIONS PER ITERATION (s249)".**
`src/templates/bb_cmp_test.cpp:36-61`: when both operands are DT_I, the box emits `cmp dl,3 / cmp rax,rdx / jl` (3 instructions, `arith_loop.s:472-482` for this `LT`) and reaches `call rt_cmp_d@PLT` (line 486) only on the non-integer cold branch — the exact shape `+`/`rt_add` already used. All six relational predicates (`ct_fail_sign`/`ct_fail_int`, lines 15-16) are keyed off `_.op_ival` through one uniform table, not a per-predicate special case — **NO PER-OP FILTER respected**. Killswitch `SCRIP_OPT_CMPINT` (default ON, line 24) verified live: `SCRIP_OPT_CMPINT=0` removes the 10-line inline guard at all 4 `cmp_test` sites in this program, falling straight to the call (confirmed by structural diff, not just call-count — a call to `rt_cmp_d` is textually present on the cold branch either way, so counting occurrences alone is a false negative; the guard's presence/absence is the real signal).

**(3) `NV_SET_fn` inline store — NOT NEW WORK; PRE-EXISTING SINCE `9dc64c75` "RC-5-GVA: R9=RT_GVA_VA" (predates s249 and s200 both).**
`src/templates/bb_assign_global.cpp:61-85`: when `g_gva_active && op_gva_k >= 0`, the store is `mov [gva_slot], rax/rdx` directly off `r9` — `NV_SET_fn` is called only in the `!(g_gva_active && op_gva_k >= 0)` branch (lines 86-101). In `arith_loop.s`, both natural variables in the hot loop store this way — `A` at `n40_assign_α:408-409` (`mov [r9+80],...`) and `ZI` at `n51_assign_α:544-545` (`mov [r9+96],...`) — **zero `NV_SET_fn` calls in the loop body**, confirmed for m4 (`--compile`, this artifact) and true by the same mechanism for m3 (`g_gva_active` is set unconditionally from `n_gva_m3`/`n_gva` counts at `scrip.c:1549,1594`, gated only by the pre-existing `SCRIP_M3_GVA` toggle for m3's pinned-island variant — no m4-specific killswitch exists because m4's GVA activation, `scrip.c:1549`, isn't independently toggleable; that's a property of the pre-existing mechanism, not a gap this row introduced).

## GATES
- **Corpus crosscheck** (`test_crosscheck_snobol4.sh`): `--run` PASS=320 FAIL=1, `--compile` PASS=319 FAIL=1 SKIP=1, DIVERGE=0. The one failure (`160_pat_alt_inner_gen_resume`, both modes) is a pattern-ALT-resume defect, unrelated to loop control/arithmetic/store — not moved by anything on this row.
- **M1 ladder** (`board_beauty_m1.sh --modes both`): **m4 10/10, ⭐M1-FIXED-POINT held** at the full 622-line rung. **m3 3/10, first red at line 10** — ⛔ this is NOT a regression from this row (zero source changes made this session; tree was clean before and after). It matches an already-censused, separately-tracked defect (GOAL-SCRIP-HQ.md: *"m3: 7-line header byte-correct then SEGV at first real statement (START)… WALL-2 exactly as censused"*), last re-confirmed "WALLS UNCHANGED, BOTH ARMS." The brief's DONE-WHEN ("M1 ladder STAYS 10/10 BOTH modes") is read here as *no new regression*, since m3 was never 10/10 to begin with in the current tree state — only the single full-file M1 rung is the actual milestone per `board_beauty_m1.sh`'s own header, and m3's wall is pattern/SEGV-class, nothing this row's mechanisms (copy-prop, cmp inline, GVA store) touch.

## SESSION ENVIRONMENT NOTES (not this row's scope, recorded so the next seat doesn't re-diagnose)
- `make -j4 scrip` failed clean at session start: `out/rt_pic/*.d` (259 files, gitignored, not source) carried **hardcoded `/home/claude3/SCRIP/...` paths** from whatever tree last populated this seat's `out/`. `make pristine` is the documented cure and worked (`rm -rf $(OBJ) $(ROOT)/out && make all`); scrip + libscrip_rt rebuilt clean afterward. Flagging in case other seats provisioned the same way hit the same wall.
- `scripts/install_system_packages.sh` exits 100 here (`apt-get`: permission denied unlinking `/var/cache/apt/*cache.bin`) — this user can't refresh the apt cache in this container. Not blocking: gcc/g++/make/nasm/gdb were all already present and sufficient for this row. Not investigated further since nothing needed was actually missing.
- `x64/` was absent in this seat (correct per the "worker seats carry ONLY three clones" design — see CLAUDE.md). `board_beauty_m1.sh:42` hardcodes `$S4E/x64/bin/sbl` with **no HQ-root fallback** (unlike `scorecard_snobol4.sh`'s `S4E_ASSETS`/D-17b tier, which falls back to `/home/claude`). Cloned `x64` locally into this seat (`git clone https://github.com/snobol4ever/x64`, 67MB, matches REPO-SCRIP.md's documented step) rather than fight the script or symlink (symlinks are Lon-forbidden for seat roots, s160). Worth an HQ ruling on whether `board_beauty_m1.sh` should gain the same fallback tier other scripts have, or whether the M1 gate specifically wants a local oracle clone always.

## DISPOSITION
Recommend the row **CLOSE** as complete. No code change accompanies this FINDING — `git status` is clean in all three repos. If HQ's queue tracking lives somewhere this seat can reach, closing `loopctl-inline-lt-concat-store` there (and, if `rtcc-veneer-strip-pure-asm` — the row this one was gating — is not already independently claimed, considering whether IT is now unblocked) is the actual remaining action. Routed to HQ via `s4e_msg.sh ask`.
