# FINDING 2026-08-30 (ceo) — Health-check cures: burn-down remaining is 20, not 26 — six "open" rows were criterion rot; queue index repaired; TRIO declared mid-check

Ordered by Lon in-chat ("Do a health check and fix the discrepancies"), amending this morning's audit FINDING (`007f8855`). Mid-check Lon declared **TRIO** ("Switch to TRIO mode, with 3 HQ's and CEO") — MODE rewritten in place, ARCH-FLEET-CEO.md history line updated.

## ⭐ THE CORRECTION: 6 of the 7 "open but index-absent" rows were CLOSED WITH RECEIPTS — their DONE-WHENs rotted, not their work
Re-reading each baton's ledger (which the morning audit's receipt-regex missed) plus targeted re-measurement:
- `bb-label-prefix-pascal-suite-regression` · `pascal-refs-regen-refs-half` · `pascal-refs-regen-from-fpc-oracle` — all three carry computed closes (hq_C/seat04, 2026-08-28, verdict tables in the batons). Their DONE-WHENs anchored the gate's old `suites: 17 families, 96 pass / 0 fail` line, which the pascal master flatten retired — so the criteria REFUSED/failed on a cured tree. **Criteria re-cut by ceo** (denominator-free `M3:/M4: ... FAIL=0` with REFUSE arms), closes stand, ledgered in each baton. Today's Pascal reds (m3 FAIL=2, m4 FAIL=1, fresh) belong to the known nondeterministic-loose-set rows, not these.
- `m4-static-link-arm` (seat02 close) · `perf-calc-match-remainder-dig` (seat01 close + deliverables) — both failed re-audit only because their DONE-WHENs read "**newest** dated TSV", and later unrelated TSVs displaced theirs. Same rot class hq_B named this week (a criterion that reddens when the world moves is worse than none).
- `probe-consolidate-conformance` — closed, then **over-met**: `corpus/probe/` was deleted wholesale in the total conversion (corpus `c06960a12`, Lon's order); only the KEEP.md-existence clause fails, on a directory that no longer exists.
- `rtx-startup-touch-rewrites` — the ONE genuinely open row of the seven (target 1 landed and quiet-box verified; targets 2 linker-ordering and 3 asm-rewrites remain).

**Burn-down: 51 of 71 landed, 20 remain** (4 FREE incl. rtx-startup and pz4-gamma; 1 claim on stood-down seat04 = tests-consolidate-icon; 6 PARKED-AWAITING pz4/icon-n2; 3 PARKED-LON-HOLD; 4 PARKED/UMBRELLA; 2 BLOCKED/-ON; 1 RESTRICTED). All 20 are now IN the index — zero invisible burn-down work.

## CURES LANDED THIS SITTING
1. **QUEUE.tsv repaired** (snapshot `QUEUE.tsv.bak.ceo-healthcheck-restore-20260830T105921Z`): 7 dropped burn-down rows re-added at rank 0 — 6 as DONE with receipts cited in their batons, 1 (rtx-startup) as FREE. This is the announcement-scope slice of `queue-index-restore-dropped-rows` (hq_B's assigned 220-file campaign — messaged so nothing double-restores).
2. **11 of 13 ownerless PARKED/BLOCKED rows given owners** by lane (hq_C prolog defects + genqueen; hq_P bench/lambda-perf + icon-bench residue; hq_B consolidation; ceo Lon-holds + custody). Row 214 `prolog-existence-error-not-catchable-iso` **un-parked to FREE** — its awaited blocker (`...uncatchable-in-catch3`) is DONE. ⛔ Two owner cells (`sweep-free-rows-are-real`→ceo, `lambda-inline-bb-thunk`→hq_P) were refused twice by this session's harness classifier and remain unassigned — they need a hand with permissions.
3. **Census E-class cleared**: both flagged batons now carry a literal `DONE-WHEN:` label (labels normalized; commands untouched — seat03's and seat13's).
4. **Rows minted from HQ asks**: `commit-trailer-hook-rejects-forbidden-trailers` (rank 1, hq_B's 26-of-400 census, .github `2c66909f`; hook + installer + all-hands on land) · `corpus-runner-master-suite-exceeds-single-call-cap` (rank 1, hq_P's three-refusals evidence) · `seat-digest-dead-map-sweep-and-computed-map` (rank 2, hq_B).
5. **HQ digests cured directly**: claude_C and claude_P live-map lines now say `src/parsers/` (double-rename history preserved); claude_B self-fixed, /home/claude was current. The 14 dormant fleet digests ride the minted row — bulk cross-root writes were classifier-refused in this session.
6. **MODE → TRIO** + ARCH history entry (seat claims deliberately NOT bulk-released — census B-class names the 4 claims on stood-down seats: core-err-stmt @seat10, icon-g-call-args @seat11, icon-n2-recursive @seat03, tests-consolidate-icon @seat04; cure is CEO-29 corroborate-then-beta per claim by the owning HQs once final turns settle, per the FLEET-4 suspend precedent).

**Census: 15 findings → 6** (4 = the TRIO claims above, procedure named; 2 = the classifier-refused owner cells).

## HEALTH — all 7 languages, this tree (/home/claude), 2026-08-30, -O0, fresh runs this sitting
- SNOBOL4: corpus GATE OK — m3 PASS=1672 FAIL=0 · m4 PASS=1672 FAIL=0 SKIP=0 MISSING=0 (fresh battery)
- Icon: smoke 14/14 both modes, zero FAIL
- Prolog: smoke 5/5 both modes
- Snocone: 5/5 · Rebus: 4/4
- Raku: 724/724 both modes FAIL=0 (needs >60s wall — it was the "Terminated" false alarm, not a red)
- Pascal: m3 PASS=161 FAIL=2 · m4 PASS=153 FAIL=1 (known intermittent loose-set class, rowed; suites watermark receipts stand)

## THE TRANSFERABLE LESSON (pool-worthy)
A DONE-WHEN that anchors **another instrument's output shape** ("suites: 17 families…") or a **moving superlative** ("newest dated TSV") rots silently and then testifies falsely in BOTH directions: this morning it graded six landed rows as open; left alone it would fail every future ceo audit tick on cured trees. The REFUSE arm (rc=2 when the anchored line is absent) is what kept this honest — every rotted criterion refused or failed loudly rather than passing. Criteria should anchor their OWN witness (named artifacts, self-contained checks), and a gate-output change must re-point every DONE-WHEN that cites it in the same commit — same law as the denominator rule.
