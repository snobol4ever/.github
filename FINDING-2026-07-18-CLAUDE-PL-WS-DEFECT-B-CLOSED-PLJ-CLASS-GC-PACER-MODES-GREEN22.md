# FINDING 2026-07-18 — PL defect (b) WS accrual CLOSED: HB_PLJ collectable class + GC byte-budget pacer; modes gate green=22 broken=0 (FIRST full-green board); queensn oracle-correct

Session: Claude (Fable 5), s99, 2026-07-18. Clean clones at SCRIP `733e9c36`; `install_system_packages.sh` + `make -j4 scrip` + `make libscrip_rt` rc=0; gprolog 1.4.5 + swipl 9.0.4 (apt); `refs/` from Lon's uploaded gprolog-master/swipl-devel-master zips. All measurement, no guessing; every mechanism below was bracketed by instrumentation before any fix was written.

## 1. THE MEASUREMENT CHAIN (how defect (b) was actually found — the three documented streams were bystanders)
New env-gated telemetry `SCRIP_ALLOC_HIST=1` (per-type counts/bytes at `rt_gcheap_alloc` + return-address histograms at the wrapper entries, s98's histogram method) on fib ×8 fail-loop:
- **HB_AGGV 1,367,553 blocks / 98.4 MB (~12 MB/iter)** — 100% from `rt_var_ref_cell` (callers = emitted blobs via `bb_var_ref`): Prolog lowers EVERY variable occurrence as `IR_VAR_REF`, one 64-byte VCELL NAMETRAP per occurrence per call (~8/predicate call).
- `rt_zls_alloc` NEVER FIRES on fib (all-DET post RSP-F-2) and `SCRIP_PL_WS_RECLAIM=1` changes nothing — streams 1–3 of the 2026-07-18 CELLWS finding are not fib's leak.
- Forced collects (`SCRIP_GC_STRESS`) drop live blocks 4.37M→672K: the VCELLs are DEAD, COLLECTABLE garbage; nothing triggers collection before absolute span exhaustion.
- Pin-source tags (`[ZGC-PIN] tag×type`, kept env-gated): with pacing alone, pinned survivors GROW +43.7K/+43.9K per collect interval — exactly the immortal HB_WS var-var JOIN cells (`by_name_dispatch.c:90`, ~11K/iter, blanket-pinned). **The newest pin always sits at the compaction top (top never retreats: `reclaimed 0`, `win=3264` bytes) and ~88K interleaved pins shatter the span into ~4.5 KB mean gaps — the fill window starves at exhaustion. That IS the storm.** crypt's version: `$mkc` kids arrays + join (`:1458/:1463`), 28.8K immortal blocks/iter, measured by the same histogram; the flag-gated cellws island does not cover them.

## 2. THE FIXES (SCRIP diff: gc_heap.{c,h}, by_name_dispatch.c, +gate allowlist)
(a) **GC byte-budget pacer.** `rt_gcheap_alloc` sets `g_gc_pending` when carved bytes since last trip exceed `SCRIP_GC_BUDGET_MB` (default 64; `0` = legacy exhaustion-only, the SCRIP_OPT=0-shaped emergency hatch). Drained at the EXISTING shielded gateway (`rt_call_arr` → `rt_gc_point_arr`), so every builtin goal is a safe point; no new collect entry.
(b) **HB_PLJ (209): the Prolog per-branch transient class.** Conservative-discoverable (added to the ws_only filter), transitive conservative payload scan (shares the HB_WS arm), **pin-on-mark and NEVER slid — address stability is LOAD-BEARING: the plc meta-call machinery keeps raw interior pointers into these blocks** — reclaimed when unmarked. NOT blanket-pinned at reset (the one line that made HB_WS immortal). `rt_plj_alloc(size_t)` in gc_heap.c.
(c) **Seven per-branch builder sites moved WS→PLJ** in by_name_dispatch.c: `:90` var-var join; `pl_cons` kids (`:891`); `pl_term_to_cell` TERM_VAR (`:919`) + kids (`:921`); `plc_iso_fresh` (`:955`); `$mkc` kids (`:1458`) + join (`:1463`). Safety: every referent path is scanned (rsp frames conservatively; ZLS/coexpr/islands via RT_SLAB_GC_ROOTS ranges; WS survivors blanket-pinned + transitively scanned; `plc_slv_t`/`plc_top_t` verified `rt_ws_alloc`-hosted). DEFERRED candidates (same shape, not hot on this rung's benches): call/N staged-arg arrays `:761/:2330/:2378`. `:2847 plc_cell_persist` is a deliberate SURVIVOR — stays WS.
(d) Gate: `test_gate_pl_no_new_global.sh` FAILED AT PRISTINE `733e9c36` (notarized by stash A/B, source-scan gate) on `g_pl_det_done/_n/_v` — s96's det-classifier lower-time memos, never allowlisted. Sanctioned with justification per the gate's own prescribed remedy; floor 14 held; rc=0. The new gc_heap.c diagnostics are file-static and outside the gate's scan set (flagged here, s98-registry-statics precedent).

## 3. MEASURED RESULTS
- **fib fail-loop ×256: COMPLETES, FLAT** — iter 250 = iter 5 (~65-70 ms; ~130 ms on collect ticks). Was: 32 iters then an 85 s wall. Pinned survivors 219,816 → ~800 (true live set). PLJ alone: fib×24 1.81 s vs 2.19 s pristine; pacer rent ~55 ms/collect at 64 MB.
- **crypt N=64→96 cliff DEAD AND INVERTED**: N=96 timeout→42 ms/iter; N=256 flat 44 ms/iter — ~2× FASTER than the old pre-cliff N=64 rate (the immortal-pin fragmentation taxed even "healthy" iterations).
- **log10 ×65536: completes, 0.19 ms/iter.**
- **queensn (the goal file's permanently-broken bench, previously >300 s DNF @2.48 GB): ORACLE-EXACT**, ~9.5-10.6 s @541 MB, 5/5 standalone + in-harness.
- **modes gate `test_bench_prolog_modes.sh`: green=22 broken=0 — rc=0, notarized on the UNTOUCHED script — the first full-green board in this goal file's history.** (Two consecutive green=22 runs.)
- Board: Prolog rung suite **138/138 ×3 modes ×3 passes**; smoke **5/5** all modes; SNOBOL4 smoke 7/7; Raku `--run PASS=26 FAIL=21`, 63 all-modes — **numerically identical to the s97/s98 recorded pristine fail-set** (21×3=63 reconciles both tracked figures); Icon 245/15/32 ×3 — inside the documented ±1 container band (s96 recorded 15 fails); FAIL-name identity NOT captured (suite prints counts at default; open command: `VERBOSE=1 test_icon_rung_suite.sh` + pristine stash A/B). no_new_global rc=0 (floor 14), no_value_stack rc=0.

## 4. BANKED (pre-existing or follow-up; none regressions of this diff)
(a) **qsort dies ~773-781 iters (SIGSEGV)** — budget-INVARIANT (identical at `SCRIP_GC_BUDGET_MB=0`), GC-stress-clean ×50, and s97's own cursor records qsort stopping at 741 in 4.9 s at that HEAD: pre-existing wall, shifted later by reduced garbage pressure. Suspect: nondet ZLS/trail accrual class (defect (c)'s family). Pristine stash A/B was interrupted by a tool timeout — open to notarize.
(b) **ham dies ~44-58 lines of ×16** — same bracketing shape, ham already the sickest bench (s95 3899×; s98 "accrual @ N=1" swings). Own rung needed.
(c) **Top-retreat + window-first (the high-water/RSS lever)**: trailing-FILL truncation was attempted, CORRUPTED queensn output (fib stayed green — det-only), REVERTED and re-verified. Root cause of the corruption: the hunk was anchored at a unique line WITHOUT reading which collector phase surrounds it (the adjust/copy pass). One live in-flight block near the top defeats trailing truncation anyway — exactly the code's own s90 warning — so the real fix is window-FIRST allocation (its s90 withdrawal reason is obsolete post-WS-class-split). Follow-up rung, with the survivor-adjust phase read FIRST.
(d) **4-way queensn "flake" mechanism CLOSED: self-inflicted background load.** Every in-harness FAIL (4way ×2, modes2) overlapped this session's own orphaned builds/apt pushing the 10.5 s run across the 30 s line; every quiet-machine run passes (incl. instrumented harness: `CELLMS queensn m3=9790`, empty m3err). Adds to s98's banked rail defect (abort-blindness): the bench harnesses have no load isolation and their timeouts are silent — a CELLMS-style per-cell timing echo is a one-line hardening.
(e) **boyer** (swi-vanroy src): rc=134 in 105 ms, empty output — immediate abort, NOT a leak death; admission to the bench set needs its own dialect/diagnosis rung.
(f) Telemetry KEPT env-gated pending Lon's call: `SCRIP_ALLOC_HIST` (found everything this session) + `[ZGC-PIN]/[ZGC-SRC]` under `SCRIP_ZETA_TELEM`. Strip trivially if unwanted.

## 5. PROCESS FAILURES (three, one family — recorded so the discipline sticks)
(1) An `&&`-chained patch was silently skipped (leading grep failed) while the trailing build+measure ran → two measurement rounds on a STALE binary (the "PLJ" numbers that were actually pacer-only — which, once recognized, RECONCILED perfectly and proved the join-ratchet prediction). (2) A patch script died mid-file-set after writing gc_heap.c but before by_name_dispatch.c → prototype mismatch (`rt_plj_alloc()` call vs `(size_t)` definition, garbage rdi as size) → rc=134 binaries, void measurements. (3) The top-retreat insertion at a unique anchor without reading the surrounding algorithm → silent survivor corruption. ADOPTED: patch+verify-landing in one call; build in the NEXT call; measure in the NEXT; never insert into an algorithm body without reading its full phase structure; artifact md5 asserts before every gate batch (which caught the orphaned-make race that transiently deleted scrip's .so mid-modes-run and manufactured a phantom times10/zebra regression).

## 6. FILES (LOCAL at session close; push pending credential)
SCRIP: `src/runtime/rt/gc_heap.h` (+HB_PLJ), `src/runtime/rt/gc_heap.c` (pacer, PLJ class, rt_plj_alloc, env-gated telemetry, all diagnostics file-static), `src/runtime/by_name_dispatch.c` (7 sites + 1 extern), `scripts/test_gate_pl_no_new_global.sh` (sanction+justification). corpus: untouched. `.github`: this FINDING + GOAL-PROLOG-BB.md cursor.
