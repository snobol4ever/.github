
---

## 6. RE-PROOF AT ORIGIN TIP (added at close — the concurrency protocol's "re-prove at close")

All of §1–§3 was measured at `ef8a3052`. The RTCC seat pushed **4 SCRIP / 2 corpus / 5 `.github`** commits during this session (RC-5 claim gate, `bb_func_activate` r9→r10 under the GVA claim with a convicted SIGSEGV fix, H2 veneer hardening, demo+benchmark regen). I rebased to **`c7e085fd`**, rebuilt `scrip` + `libscrip_rt`, and re-ran:

| measurement | at `ef8a3052` | at `c7e085fd` |
|---|---|---|
| probe m3 | 24 PASS / 12 FAIL | **24 PASS / 12 FAIL, identical set** |
| `A2_inline_nocap` | PASS `S` | **PASS `S`** |
| `A_stored_nocap` | HANG 124 | **HANG 124** |
| `C2_inline_nested` | PASS `S` | **PASS `S`** |
| `C_stored_nested` | SEGV 139 | **SEGV 139** |
| `g_blob_ctx[5]` single global | yes | **yes** |
| `emit.cpp:2358` keys on `flat_pat` alone | yes | **yes** |

**The RTCC register work does not intersect this zone. The finding stands at the current origin tip.**
