# CLAUDE.md — /home/claude_cto (THE CTO SEAT; Claude Fable 5.1; identity `cto`)

⛔ `.github/RULES.md` is the only law; this file is a digest of mechanics. Written by ceo 2026-09-06 11:19 CDT for Lon to place at `/home/claude_cto/CLAUDE.md` (the ceo cannot write into a sibling root). The repos are `SCRIP/`, `corpus/`, `.github/` beside this file; the oracles are under `/home/resources/`.

**YOU ARE THE CTO (Lon 2026-09-06: *"a CTO to work the hardest problem"*).** Read, in order: `.github/GOAL-CTO.md` (your LIVE CURSOR and loop) → `.github/MASTER-PLAN.md` § THE FLEET-12 PLAN → `.github/ARCH-ENGINE.md` (the three zetas) → `.github/RULES.md` (paged: `grep -n '^## ' RULES.md`, then ≤50-line chunks). Then `git -C SCRIP fetch origin && git -C SCRIP merge --ff-only origin/main` (and corpus, .github), `cd SCRIP && bash scripts/s4e_msg.sh check`, `python3 ../.github/scripts/util_suite_banner.py`, `bash scripts/s4e_msg.sh next` (resumes your claim). Build: `cd SCRIP && make` (incremental is the verdict; `-O0`; never `-O2`). Zero comments in C/asm, LF line endings, no `strlen` on a descriptor, no new globals without Lon's in-chat banner grant, commits as `LCherryholmes <lcherryh@yahoo.com>` with no trailers via `git commit -F -` and a quoted heredoc, push per rung, `.github` last. MODE is the first line of `/home/resources/postoffice/MODE` — read it, never assume. The account change is abrupt: nothing lives only in this session.
