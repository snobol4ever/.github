# GOAL-SN4-ZETA-CLIMB — SNOBOL4 ON ζ-CELLS: THE CLIMB, 1+1 → EXEC/CODE (sole FORTH regime)

**CHARTER (Lon 2026-08-08):** walk the ENTIRE SNOBOL4 language bottom-up at the sole ζ-cells-on-stack regime (selectors deleted, SCRIP `de837576`): stop at first failing rung, MONITOR-FIRST, fix, one FINDING per land mine, XFAIL shrinks monotonically (removed in the SAME commit as the fix), move up. Summit = EVAL/CODE/EXEC + beauty drivers + xc318 ≥ pre-flip both modes. Twin: `GOAL-SN4-ZETA-MECH.md` owns STRUCTURE; this file owns CORRECTNESS. A defect whose fix needs new protocol = MECH rung (cross-request both cursors). Protocol: RULES.md (monitor scripts §1: `test_monitor_2way_sync_step_bin.sh` / 3-way PARTICIPANTS · offline `probe.py` &STLIMIT+&DUMP replay bisect · gdb spin-counter, HW watchpoints DEAD in container, `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` clean-bt · `setarch -R`). Oracle: `/home/claude/x64/bin/sbl -b f.sno`; corpus paths CORPUS-LOCATIONS.md; suite `corpus/probe/bb/run_suite.sh` (m3) + `MODE=compile` (m4).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s4 (Sonnet 4.6)
141-probe m3 **128/14/0/0** · m4 **118/24/0/0** · roman correct 1–9 then blank rc=139 (pre-existing M-2(a)) · xc318 post-flip floor m3 **286/31** · m4 **268/48/SKIP=1/DIVERGE=21** (setarch -R). SCRIP `29f89dd5` · corpus `6f5e8102`. C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ COMPLETE. NEXT: C-4.
**Session notes:** C-3 re-proved: watermark held (128/14 m3, 118/24 m4, no drift from prior cursor). Full C-3 witness sweep: L-family 20/20 both modes; crosscheck/patterns 039–049 (ANY/NOTANY/SPAN/BREAK/BREAKX/LEN/POS/RPOS/RTAB/REM/ARB) all PASS m3; W05 W06 W07 all PASS; 045_pat_rpos PASS; 135–137/174–177 BAL (shallow/deep/mixed/manual/retry/forms/unbalanced) all PASS; 098 &ANCHOR PASS; parser/pat_any|break|len|notany|pos|span PASS live oracle. No source changes — orientation + re-prove session only. W04 ARBNO BENCH-line diff pre-existing (C-7 territory). All 44 crosscheck/patterns failures are C-4+ (ARBNO/FENCE1/ALT-commit/deferred). No regen needed (no codegen touched).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s2 (Sonnet 4.6)
141-probe m3 **124/18/0/0** · m4 **116/26/0/0** · roman correct 1–9 then blank rc=139 (pre-existing M-2(a)) · xc318 post-flip floor m3 **286/31** · m4 **268/48/SKIP=1/DIVERGE=21** (setarch -R). SCRIP `29f89dd5` · corpus `34065383`. C-0 ✅ C-1 ✅ C-2 ✅ COMPLETE. NEXT: C-3.
**Session notes:** C-1 verified green both modes (all crosscheck arith_new, A01–A04, 023_arith_add pass). C-2: control family 031–037 + indirect `:($X) + 1020_code_label_transfer all pass both modes. `-NOFAIL` defect found (lexer catch-all silently discarded it), fixed: SCRIP `29f89dd5` (6 source files + bison/flex regen). Error 35 fires correctly m3+m4, probe suite watermark held. Monitor trace-ordering divergence is artifact (SCRIP emits label entry before abort; semantic result correct). `expr_eval` uses EVAL() → C-11 territory, expected fail. `1020_code_label_transfer` passes (CODE() compiling statement blocks works; EVAL() expression evaluation does not — different code paths). Monitor built (csnobol4 + ss-monitor) this session.

## ⛔ METHOD (binding every rung)
1. Rung gate = every witness green in BOTH modes BY SET vs oracle `.ref`; crash detection is wait-status (SIGSEGV handler swallows — run_suite.sh does this; ad-hoc runs must check rc). 2. MONITOR-FIRST: divergence bracketed between last-agree and first-diverge trace events BEFORE reading code; monitor dark for the mode ⇒ MON-RE first (known defect B: stno counting — `build_stno_map.py` canonical). 3. Fix at the bracketed site; re-run monitor, divergence must MOVE. 4. XFAIL removal same commit; NEVER add to XFAIL except a Lon-ruled park. 5. Timeouts 8s unit / 30s corpus; `< /dev/null` on scrip calls; grep error: both build logs. 6. m3-only defects: suspect the m3 glue stubs first (X12 lesson: one-shot push-of-zero classes — FI8 lazy-init, PS-1b twin driver:1554, first-touch trampoline bakes; m4 clean = emission exonerated). 7. SPITBOL semantics per manual (SCRIP FOLLOWS SPITBOL; case-sensitive); manual anchors inline per rung below.

## RUNGS (stop at first fail; a rung already green just gets its checkbox and a HEAD-stamp)
- [x] **C-0 RE-PROVE + xc318 POST-FLIP FLOOR** — ✅ 2026-08-08 s1. m3 124/18/0/0 · m4 116/26/0/0 · xc318 m3 286/31 · m4 268/48/SKIP=1/DIVERGE=21 (setarch -R). Floor recorded. SCRIP c0372fec · corpus f65d8278.
- [x] **C-1 VALUE SPINE: 1+1 → arith/assign/concat** — ✅ 2026-08-08 s2. All crosscheck arith_new + A01–A04 + 023_arith_add pass m3+m4. SCRIP 29f89dd5.
- [x] **C-2 CONTROL** — ✅ 2026-08-08 s2. 031–037 + indirect :($X) + 1020_code_label_transfer pass m3+m4. -NOFAIL fixed (error 35, rc=1, no fallthrough). SCRIP 29f89dd5.
- [x] **C-3 STATIC PATTERNS** — ✅ 2026-08-07 s4. LIT ANY NOTANY SPAN BREAK BREAKX LEN POS RPOS TAB RTAB REM BAL ARB all green m3+m4. &ANCHOR (scan-retry law) green. L-family 20/20, crosscheck 039–049, W05/W06/W07, BAL 135–137/174–177, 045_pat_rpos, 098_keyword_anchor. SCRIP 29f89dd5 · corpus 6f5e8102.
- [ ] **C-4 ALTERNATION + CAPTURES** — `|` arms, `$` immediate (fires per subpattern match even on overall fail, manual p.87), `.` conditional; transit guarantee (captures POP on retreat — wrong VALUES not crashes). **Crater members: A05 A06 (ALT3-capture, both modes — M-1 suspects bb_match_capture eax/saved_delta under armed) · F03 F04 F05 (capture behind FENCE).**
- [ ] **C-5 FENCE0 / FENCE1** — FENCE0: null-match forward, ABORT on backup, anchors at head regardless of &ANCHOR (manual pp.125-126, 209); FENCE1 `FENCE(P)` p.128: matches as P, backup PASSES THROUGH, arms within P invisible, left elements stay live. **Crater members: G05 G06 G09 G17 G18 G21 G22 G23 G24 (FENCE0 crash family) · H14 H20 H23 (FENCE1-capture) — M-1/M-2 customers; this rung certifies semantics after structure lands.** Carried opens: FENCE1 under `$`/`.` inside PAT$ blob transparent (g_sno_in_patproc gate, witness 127) · FENCE1 under `$`/`.` inside ARBNO body transparent · fence_probe m3 T6 crash (pre-existing).
- [ ] **C-6 DEFERRED EVAL + RECURSION** — `*name`/`*expr` = match-time fetch (manual p.86: plain name snapshots at BUILD — the LADDER-PB stitch semantics, witnesses pb_snapshot_imm/stitch_* MUST STAY GREEN); `*LIST` recursion p.122. **Crater: D02 D03 D04 (m3-only crash, X12 signature — m4 XPASSED at flip) · roman ≥10 (sealed-DEFER, M-2(a)) · D06 D07 D08 pre-existing LEN(*N)-deferred-int family.**
- [ ] **C-7 ARBNO + NESTING** — shortest-first, retried-on-demand (manual pp.121-122, 212); frameless {Δ0,yield} cell; nested-ARBNO class **H24 H25 X02 X06 X11** (sno_seq_nary second-pass catch-all, immune to ZD switches — lower-side) · X03/X05 nesting law (legacy-chain outer requires net-zero delta — dies with M-5 chain deletion) · nested-ARBNO ZLS geometry (FINDING-08-05 s9/s10 rsp3 root cause).
- [ ] **C-8 FUNCTIONS** — DEFINE/RETURN/FRETURN/NRETURN (NRETURN+defer manual p.~130), recursion (fib), OPSYN, APPLY, ARG; value/defer-in-proc (X12 class — m3 glue); func_call benchmark `result:` correct; call2bb parked items (SCRIP_CALL2BB default 0 — folds at MECH M-5 only if measured). Witnesses: 085/086/087, uw2/uw3, test_stack, 209.
- [ ] **C-9 REPLACEMENT + SPLICE** — 5-stage stmt execution stages 4/5 (Lon ruling in HQ): build replacement, splice; TAB-binds-subject trap manual p.143 #10; capture-replacement queue item; replacement-splice PENDING LON RULING (sole-consumer fence retirement) — surface it here if the rung blocks.
- [ ] **C-10 DATA + KEYWORDS** — ARRAY/TABLE/DATA/field, ITEM, SORT/RSORT, CONVERT; &STLIMIT &DUMP &PROFILE (monitor infra), &FENCE primitive, &ANCHOR. Witnesses: data family, 131/141/183 (131 = ex-PATREF 528B UCLAIM customer — must be clean post-M-3), 066, W04_arbno_*.
- [ ] **C-11 EVAL / CODE / EXEC (the summit construct)** — EVAL(string/expr), CODE(string) → code object executed via direct goto `<code>`, SETEXIT, CONVERT to EXPRESSION; the eval-family falsification controls **1016 1019 161 expr_eval MUST HOLD** (named gatekeep class); eval_fixed rc=0 witness; S233/S234 deferred-eval-family merge-defect findings are the map (γ success-code blocker, not ω).
- [ ] **C-12 SUMMIT GATES** — beauty_suite 19 drivers vs refs (beauty.sno itself is DESTINATION not gate) · xc318 BY SET ≥ 280/260 BOTH modes · bench 18/21 hold-or-better · rc=139 tail zero · 127/152 env-pad IMPOSSIBLE (post-M-6 a flake is a LEAD) · armed roman full MDCCLXXVI · regen ×4 · FINDING + both cursors + MECH DoD cross-check.

## WITNESS SET (report every session): probe suite both modes · roman · pb_snapshot_imm + stitch trio · uw2 uw3 · 131 141 183 066 · 045_pat_rpos · eval controls 1016/1019/161 · eval_fixed · func_call · test_case (honest wedge) · 127/152.

## COORDINATION
MECH owns structural edits; this ladder NEVER lands a new frame/glue/claim protocol — it files the cross-request and blocks the rung on MECH's cursor. Both re-prove the shared watermark at open/close. Icon: after ANY shared-file edit, run the 303-emission content-hash sweep per MECH's procedure. `.github` pushes last; handoff per RULES (regen scripts if codegen touched; handoff_status.sh verbatim).
