# GOAL-SN4-ZETA-CLIMB — SNOBOL4 ON ζ-CELLS: THE CLIMB, 1+1 → EXEC/CODE (sole FORTH regime)

## ⛔⭐⭐⭐ CROSS-GOAL BULLETIN — 2026-08-10 s12: LADDER WREG IS LIVE, r10/r11 ARE CLAIMED WIRE REGISTERS (owner-directed cross-post; authority = `GOAL-PASSTHRU-RBP-ERAD.md` LADDER WREG, `.github` `6de2dbff`)

**Lon runs 5 seats concurrently: PASSTHRU-RBP-ERAD + RTCC + ZETA-MECH + ZETA-CLIMB + SNOBOL4-RTX.** Charter, verbatim: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11."* Pattern blobs stop being procedures: the `proc_PAT$N_α` shim (304B carve · γ/ω wire stores · SCANBASE fills · `g_zctx` push · resume-slot store) and its CLASS-D exit ceremony are DELETED; call sites enter the blob's FIRST INTERIOR BOX directly with `lea r10,[rip+site_γ] · lea r11,[rip+site_ω] · jmp <first_box>`; exits are `jmp r10` (success) / `jmp r11` (fail after drain). **rΓ=r10 · rΩ=r11 are RESERVED product-wide, all three glue kinds, ONE authority (a per-kind split is the mixed regime ZW16 convicted).** LAW: blob-interior pending cells capture/restore the pair at push/β, else it is `g_blob_ctx`'s single-cell defect in register clothing.

**WHY THIS IS YOURS — YOU ARE THE CLEANEST PARALLEL SEAT (near-zero collision; neither item touches registers or frames).** Two orphaned defects are routed here, both fitting your XFAIL-as-work-queue model:
1. ⛔⭐ **`151_pat_arbno_inline_fence_backtrack` — NEW s12b, REVERSE-DIRECTION MODE34.** PASSES m4, SIGSEGVs m3 (rc=139), deterministic 3/3 each way at SCRIP `bce9a4b0`. **This FALSIFIES 11-ENV's "strictly one-directional, zero in reverse" — m4's fail set is NO LONGER a superset of m3's, and any bisect predicate resting on that is invalid.** Shape = inline FENCE inside ARBNO backtrack, i.e. the licensed-keeper FENCE1 boundary. MONITOR-FIRST.
2. **`128`** — own rung since s8; s7's enclosure-leak reading is WRONG (in `cmds = *unit FENCE(*cmds | eps)` the `*unit` is a SIBLING of the FENCE, not inside its body); a separate defect wearing the family's crash signature.
⭐ Clearing either removes it from WREG-5's witness set before that gate runs.

**s12b MEASURED FLOORS (same container, BY SET — every seat's regressions are judged against these):** probe **33/5** m3 · **33/4** m4 · crosscheck/patterns **82/40** m3 · **78/44** m4 · broad-336 **270/66** m3 · **262/68/6** m4. Honest census Σest ≈ **263** (NOT the stale 557; crosscheck committed `.s` were pre-deletion — re-measure fresh, do not trust those artifacts). Runner semantics: `$(cat ref)` vs `$(cmd)` (ENV-TRAP (a)); namespace `/tmp` evidence per seat.


## ✅ CONCURRENT WORK — COMMIT AND PUSH FREELY (Lon 2026-08-10 — SUPERSEDES the 2026-08-08 CONCURRENCY PROTOCOL, which is DELETED)
**Any session may edit ANY file at ANY time, and push whenever it has something worth saving.** There is no routed window, no reserved file, no "not-concurrency-safe" rung, no concurrent set, and no waiting on another seat. Git merges; `git pull --rebase` before pushing and resolve conflicts normally. ⛔ **NEVER hold back a commit or a push on concurrency grounds, and never park work waiting for a window** — stranding has cost this project vastly more than merging ever has (s9/s10 lost two sessions of RTCC work; s19–s26 stranded eight sessions of MECH work and BUG-7 was re-derived from scratch). **Push early and often — mid-session, per rung — not only at session end.**
Three things survive because they are about CORRECTNESS, not scheduling: (1) after a `git pull --rebase`, re-prove THIS file's gate/watermark — shared state can move under you; (2) push code repos before `.github`, so a FINDING never describes a tree that was not pushed; (3) `handoff_status.sh` verbatim is the only push truth. **Semantic collisions — two seats claiming one register, the r9/GVA class — are caught MECHANICALLY by `scripts/test_gate_rtcc_claimed_regs.sh`, not by scheduling. That gate is the replacement for the window, and it is why the window is no longer needed.**

**CHARTER (Lon 2026-08-08):** walk the ENTIRE SNOBOL4 language bottom-up at the sole ζ-cells-on-stack regime (selectors deleted, SCRIP `de837576`): stop at first failing rung, MONITOR-FIRST, fix, one FINDING per land mine, XFAIL shrinks monotonically (removed in the SAME commit as the fix), move up. Summit = EVAL/CODE/EXEC + beauty drivers + xc318 ≥ pre-flip both modes. Twin: `GOAL-SN4-ZETA-MECH.md` owns STRUCTURE; this file owns CORRECTNESS. A defect whose fix needs new protocol = MECH rung (cross-request both cursors). Protocol: RULES.md (monitor scripts §1: `test_monitor_2way_sync_step_bin.sh` / 3-way PARTICIPANTS · offline `probe.py` &STLIMIT+&DUMP replay bisect · gdb spin-counter, HW watchpoints DEAD in container, `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` clean-bt · `setarch -R`). Oracle: `/home/claude/x64/bin/sbl -b f.sno`; corpus paths CORPUS-LOCATIONS.md; suite `corpus/probe/bb/run_suite.sh` (m3) + `MODE=compile` (m4).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s40 (Opus 5) [⭐ **H31 IS GREEN — the 3R is a 2R** (s39's unclaimed watermark, now measured on a verified clean build) ⇒ **MECH M-1b's acceptance set has lost its only NESTING witness** · ⭐ **NEW C-9 DEFECT: TAB/POS + replacement splices the WRONG SPAN both modes, LEN is correct** (p.143 #10, previously unprobed) · METHOD 8 gap: a 5-line "build" passes every current doorway check and is a RELINK. **ZERO code changed**; tips SCRIP `bce9a4b` · corpus `bea31de` unmodified]

**WATERMARK (re-proved at `bce9a4b`, verified clean rebuild — 258-line foreground build, exit-checked, mtime 23:21:04 predating all measurement):** m3 **134**/15xf/0/**2R** · m4 **133**/16xf/0/**2R**. REGRESSION set both modes = **D12 · D13** only. This is s39's explicitly-unclaimed number ("*unknown and is not claimed unchanged*"), now measured.

**⛔ WATERMARK IS PINNED TO `bce9a4b` — NOT CLAIMED AT `72830da`.** SCRIP fast-forwarded `bce9a4b`→**`72830da`** (SIGPIPE SWEEP) *after* this session's measurements (reflog: `pull --rebase: Fast-forward`; my binary/`.so` mtime **23:21:04**, suites ran 23:21:13–23:21:44, incoming HEAD is dated 23:21:40). Build and both suite runs are therefore validly pinned to `bce9a4b`, and the tree has moved **under an unchanged binary** — re-running the suite now would measure a stale binary against newer source, the METHOD 8 trap in reverse. **Next seat: rebuild (`rm -rf out/rt_pic`) before trusting any number at `72830da`.** The H31 result and both defect characterizations are source-independent reproducers and stand.

**⭐ H31 PASSES IN BOTH MODES.** Was red at s35/s36/s38 (three independent proofs of 3R). Repaired by a parallel seat between `66322b3c` and `bce9a4b`, **incidentally and undocumented** — no FINDING claims it; every existing mention treats it as red (FINDING-set grepped first, per s37's rule; no builds spent). The D-2/D-3 **repaired-8 guard is intact**: D06 D09 D10 D11 H20 H21 X07 X08 all ✅.

**⛔ CROSS-REQUEST → MECH M-1b — YOUR ACCEPTANCE SET IS NOW MIS-SPECIFIED.** s38 set acceptance `{D12,D13,H31}` + repaired-8, and DELTA 2 established **H31 was the SOLE NESTING witness** (recursion = D12/D13). With H31 green *before* M-1b runs, the criterion now covers **only the recursion species** — GLOBAL-EXECUTE could pass it while silently regressing nesting. Re-home H31 to the *guard* set (9th guard) or mint a new nesting witness; MECH rules. **Open, NOT claimed:** whether the parallel fix repaired nesting mechanically or merely moved H31's stack layout so the overwrite lands harmlessly — if the latter, H31 is a **false green**. Cheap for MECH from its own `g_blob_ctx[5]` root cause; expensive here (depth-1 clone + s37's void-bisect lesson).

**⭐ NEW C-9 DEFECT — TAB/POS REPLACEMENT SPLICE (rung item "TAB-binds-subject trap p.143 #10", unprobed until now).** Subject `'ABCDEFGHIJ'`, **literal** args, one statement per program: `TAB(4) LEN(1) = '*'` → oracle `*FGHIJ`, SCRIP **`*BCDEFGHIJ`** · `POS(4) LEN(1) = '*'` → oracle `ABCD*FGHIJ`, SCRIP **`*BCDEFGHIJ`** · `TAB(4) . FRONT LEN(1) = FRONT '*'` → oracle `ABCD*FGHIJ`, SCRIP **`ABCD*BCDEFGHIJ`**. All three collapse the replaced span to `[0,1)`, **both modes, rc=0 — silently wrong, not a crash.**
**DISCRIMINATORS (measured, none inherited):** (1) ⭐ **`LEN(4) LEN(1) = '*'` is CORRECT** — the failing prefixes are the **absolute-cursor** primitives, the working one is **relative-advance**; sharpest lead. (2) NOT "non-zero match start" — `'CD' = '*'` → `AB*EFGHIJ` ✅. (3) **NOT the match and NOT capture — the SPLICE only**: `TAB(4) . F LEN(1) . G` → `ABCD:E` ✅ oracle-correct without replacement, and `FRONT`=`ABCD` is right *inside* the failing statement while the splice beside it is wrong — **the same selectivity fingerprint s35 used to convict the `FRQ`/`op_zdepth` defect**. (4) NOT s39's variable-arg class — all args literal; two distinct defects (C-3 green doesn't cover this: C-3 is match-only). (5) **Context-dependent** = wrong-cell signature: isolated span `[0,1)`, but three such statements in one program give `[0,0)` **and m4 SIGSEGV rc=139** where m3 returns wrong values at rc=0.
**⚠️ LEFT UNCONVICTED ON PURPOSE** — anti-pattern §2 has killed "offset wrong by N bytes" twice. I have a fingerprint, not a mechanism; I did not read the emitter and name no cell. **NEXT INSTRUMENT: MONITOR-FIRST, and it is NOT dark here** (value divergence, live trace events). Cheapest first move: diff emitted asm of `TAB(4) LEN(1) = '*'` (fails) vs `LEN(4) LEN(1) = '*'` (passes) — one primitive apart. Reproducers `/tmp/climb_s40/`; **not** added to `probe/bb/` (red probes would register REGRESSION; RULES §4 forbids XFAIL additions absent a Lon park — **Lon: say the word and they land in the fix commit**).

**⚠️ METHOD 8 GAP (proposed addendum) — A RELINK PASSES EVERY CURRENT DOORWAY CHECK.** `make -j4 scrip` finished in **1s / 5 log lines / rc=0 / fresh mtime / green smoke test** — and had rebuilt **nothing**: `scrip` is a thin driver, the compiler is `out/libscrip_rt.so`. `make libscrip_rt` then declared up-to-date against an **untracked** `.so` (`.gitignore:32`) of unknown provenance; deleting it relinked in 3s from ~260 stale `out/rt_pic/*.o`. Only `rm -rf out/rt_pic` produced a real build (**258 lines, 2m42s**). **`out/rt_pic/` is a THIRD contamination layer beside `/tmp/si_objs`.** Proposal: a watermark requires a build log of **~258+ lines**, not merely rc=0 + fresh `scrip` mtime — `scrip`'s mtime proves only the driver relinked, and the artifact under test is the `.so` (cf. FINDING-2026-08-01-WIREREG, *"the AB artifact is the SO not the binary"*).

**⭐ THE SPLICE DEFECT OWNS `test_case` — A C-9 RESIDUAL IS CLOSED (cause named).** ⚠️ **CWD IS PART OF THE INVOCATION:** run from anywhere but the corpus root and **the ORACLE ITSELF fails rc=1** (these pull `lib/`; cf. HEAD's own subject *"compile from the CORPUS root so `-include 'lib/*.sno'` resolves"*). From `/home/claude/corpus`, oracle rc=0 and **byte-matches both `.ref`s** — refs trustworthy, and any measurement from the wrong cwd is void. **Corrected signatures:** `test_case` **rc=134** (NOT s39's 124) — `[WSI] workspace island exhausted (22366969 blocks)`; `test_string` **rc=0 with WRONG OUTPUT** (an rc-only check scores it green — it is not). **ROOT CAUSE, probe-confirmed:** `lib/case.sno:34,36` is the failing shape verbatim (`str POS(0) LEN(1) . ch =`). In a 5-line loop, oracle goes `abc`→`bc`→`c`→`[]`; SCRIP captures `C=a` correctly every pass but **never modifies the subject** (`rest=[abc]` forever) — splice is a no-op, the subject never shrinks, the ALOOP idiom (manual Ch.6 p.73 / p.142 #6) goes non-convergent and allocates until the island dies ⇒ rc=134. **`test_case` is a CUSTOMER of the §2 defect, not a separate residual.** `test_string` (`lib/string.sno:34` lstrip, `POS(0) (SPAN(ws)|'') REM . r =`): splice also a no-op **but the CAPTURE IS ALSO WRONG** (`r=[   hello]`, blanks not stripped) — that is NOT §2's *capture-correct/splice-wrong* signature, so a **second unconvicted component** (alternation arm / post-SPAN cursor) rides it; do NOT assume the §2 fix clears it. **⇒ C-9's residual board is at most two defects, not four, and TAB/POS splice is the highest-leverage single fix on it.**

**NEXT:** MONITOR-bracket the TAB/POS splice against the LEN sibling (monitor NOT dark: value divergence, live trace events); cheapest first move is the emitted-asm diff of `TAB(4) LEN(1) = '*'` vs the passing `LEN(4) LEN(1) = '*'`. Then re-measure `test_case` (expect it to clear) and `test_string` (expect a residual second component) **after** that fix. 061 remains **s39's** rung — do not duplicate. Then C-10. **FINDING:** `FINDING-2026-08-10e-CLAUDE-OP5-SN4-CLIMB-H31-IS-GREEN-SO-THE-3R-IS-2R-AND-M-1b-LOSES-ITS-ONLY-NESTING-WITNESS-AND-TAB-POS-REPLACEMENT-SPLICES-THE-WRONG-SPAN-WHILE-LEN-IS-CORRECT.md`

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s39 (Opus 5) [C-9 residual `061` ENTERED AND RE-CHARACTERIZED: it is **not** "variable-arg POS", it is the WHOLE variable-argument pattern-primitive class (POS+LEN+TAB), one choke, one owner. Mechanism measured against emitted asm; four inherited hypotheses killed. Second separate land mine: `IR_MATCH_POS` hard-aborts on the stored `POS(*N)` form. **ZERO code changed**; tips SCRIP `bce9a4b` · corpus `bea31de` unmodified]

**⛔ WATERMARK: NOT RE-PROVED — DO NOT INHERIT A NUMBER FROM THIS SESSION.** The 141-probe suite was not run. s38's m3 133/15xf/0/3R was measured at `66322b3c`; this seat opened at SCRIP **`bce9a4b`** (HEAD moved under us — parallel seats). The watermark at this tip is **unknown and is not claimed unchanged.** Build was foreground/`setsid` + exit-sentinel, exit 0, mtime 23:07 predating all measurement (METHOD 8).

**REBASE AT CLOSE (origin moved under this seat mid-session):** SCRIP `bce9a4b`→**`72830da`** · `.github` →`bee967d3` (rebased clean, 0 conflicts) · corpus `bea31de` unchanged. **Re-prove discharged BY INSPECTION, not by rebuild:** the SCRIP delta is `72830da` (SIGPIPE gate sweep) + `3a4a35b` (RTCC census) touching **four shell scripts only** — `test_gate_icn_global_no_nv_m3.sh`, `test_gate_icn_zcells_gva.sh`, `test_gate_pl_no_new_global.sh`, `util_rtcc_escape_census.sh` — and **zero** files under `src/` (`grep -c` over the diff for `bb_match_{pos,len,tab}|emit.cpp|lower_snobol4|x86_asm.h` = **0**). The binary measured here was built from `bce9a4b`; the source tip is now `72830da` and the difference is non-compiled files, so every §8 measurement stands. ⚠️ This discharges the *re-prove* rule only — it does **not** supply a watermark; the suite still has not been run (see above).

**⭐ `061` IS A FAMILY, NOT A PROBE.** Literal args pass (`POS(0)`, `POS(1)`); **every** variable-arg form fails in BOTH modes — `POS(N)`, `LEN(N)`, `TAB(N)`, `POS(N + 0)`, anchored and unanchored, with and without capture, loop and single-shot. Consistent with C-3 (STATIC patterns) being green: C-3 only ever covered literals. **Fix at the choke, not per primitive.**

**FOUR HYPOTHESES DEAD (do not re-spend a rung on any of them):** (1) NOT capture — `061` contains no ARBNO and needs no capture to reproduce; the probe name misleads twice. (2) NOT the loop. (3) **NOT the wrcount/seal split** — wrcount==1 and wrcount==2 behave identically, so the D05/D06 seal=2-vs-seal=0 lead does NOT govern here. (4) NOT a broken variable read — `DUPL('x',N)`→`xx` and `OUTPUT = N`→`2` are oracle-correct; non-pattern functions take variable args fine.

**MECHANISM (asm-measured, per anti-pattern §4 — nearest passing sibling diffed against the failing probe):** `bb_match_pos.cpp:9` documents the two arms; emission confirms the load is the entire delta — static `mov rax, 1` vs dynamic `mov rax, qword ptr [rsp + 8]` (`ZOPQ(0,8)`). `cmp r14d, eax` compares only the low 32 bits, so a cell correctly holding `N=0` would succeed; the anchored `N=0` probe still fails ⇒ **the dynamic arm reads a cell the argument never landed in.** Context-dependence is the fingerprint (`LEN(N=0)` succeeds flat, fails in a loop shape) — a wrong constant would fail uniformly, a wrong *offset* picks up whatever occupies the slot. Same CLASS as C-9's `FRQ`/`op_zdepth` splice defect (s35, `g_zd_zunder`). ⚠️ **Left deliberately unconvicted** — anti-pattern §2 has falsified "offset wrong by N bytes" twice. **MONITOR-FIRST is the next instrument and it is NOT dark for this class** (value divergence with live trace events, unlike the 3R crash class).

**SECOND LAND MINE (separate rung, do not fold into the same commit):** stored `P = POS(*N) 'a' . V` (manual p.143 #11 shape) aborts — `FATAL emit_drive: IR op=88 has no template`; **op 88 = `IR_MATCH_POS`** (confirmed against the 127-op enum). m3 rc=134, m4 does not link. Per the sink's own note POS *has* a case, so a **guard inside it** declines the deferred form. ⚠️ NOT confirmed to be `test_case`: at this tip `test_case` **hangs (rc=124)** with no op=88 abort, and `test_string` exits rc=0 — both signatures have MOVED from s35's record and need a clean re-measure before being trusted either way.

**SEMANTICS ANCHOR:** oracle reproduces `061`'s `.ref` byte-for-byte. `061` is legal SNOBOL4 **without** `*` — manual p.143 #11 scopes `*` to patterns stored in a variable; in-statement `POS(N)` re-evaluates per execution (oracle-confirmed both forms → `a a a`). **TAB-binds-subject (p.143 #10) remains unprobed**: `TAB`/`BREAK` bind every character passed over, so `S ? TAB(49) LEN(1) = '*'` replaces the first 50 chars, not the 50th — i.e. the replaced span is the full extent bound by the match, a direct spec for the splice.

**⭐ MONITOR-FIRST DISCHARGED — AND THE MONITOR IS NOT DARK HERE (verified, not assumed).** `PARTICIPANTS="spl scr"` 3-way harness brackets it: step 6 both `LABEL stno=4`, step **7 diverges** (spl→stno 5 fall-through/success, scr→stno 7 `:F` branch) ⇒ the loss is **inside statement 4's execution**. Step 5 shows BOTH engines agreeing `@3 VALUE N = INT=0` immediately before ⇒ the variable is correct on entry. ⚠️ **Trap for the next seat: `test_monitor_2way_sync_step_bin.sh` runs `csn vs spl` — TWO ORACLES — and cannot see SCRIP at all.** Use the 3-way harness with an explicit `PARTICIPANTS` pair.

**⭐ PROVEN: THE ARGUMENT NEVER ARRIVES (value-independence, identical shape, only the constant differs).** `LEN(N)` on `'abc'`: N=0 → oracle `OK[]`, m3 **NOMATCH** (LEN(0) can never fail); N=1 → oracle `OK[a]`, m3 `OK[]`; N=2 → oracle `OK[ab]`, m3 `OK[]`. **m3 does not track N** — 1 and 2 both match ZERO chars. The effective argument is uncorrelated with the source argument.

**CARVES, IN EMISSION ORDER:** arg descriptor written at `[rsp+0]` after `sub rsp,16`; then coerce box `sub rsp,16`; then match frame `sub rsp,32`; then POS reads `[rsp+8]`. At read time `[rsp+8]` sits INSIDE the match frame (between `start_δ`@+0 and `rsp_mark`@+16). Descriptor layout (type/len at +0/+4, value at +8) says the **half is right and the base is wrong**.

**⛔ CORRECTION TO THIS SEAT'S OWN FIRST WRITE-UP:** I called context-dependence "the fingerprint." **Partly wrong.** Four probes with an identical match statement and 0/1/2 extra preceding assignments fail UNIFORMLY — the displacement is the interposed **carve depth** (a property of the construct), not padding-sensitive. A next seat testing "add a statement and watch it move" gets a null result and might wrongly acquit the offset theory.

**⛔ DO NOT HARDCODE THE DELTA.** The 16+32 above is one shape. s35's trap applies verbatim: *"the delta is NOT a constant … any fix hardcoding 16 passes the literal case and FAILS concat."* A richer arg expression carves more. Correct shape = s35's: stage the depth at the planner, consume at the choke as an addend (`g_zd_zunder`/`g_zd_ztail` precedent). **Still a lead, not a conviction** — proven: arg never arrives, loss is inside the match, read is base-relative across two carves; NOT proven: that fixing the base suffices.

**NEXT:** confirm which cell the planner *believes* it wrote, then fix at the choke. Acceptance for the family fix: `q1 q2 q3 q4 q6 q7 r3 r4 r5 r6` green both modes + `s1`/`s2` sweeps returning identity `0->0 … 5->5` + `u_len_{0,1,2}` tracking the oracle (evidence at `/tmp/climb_s39/`). Then the `*N` guard arm, then `test_string`/`test_case` re-measure, then TAB-binds-subject, then C-10. Container gap: **no `sudo` ⇒ no `gdb`/`nasm`**, so RULES §1's gdb spin-counter is unavailable until resolved. FINDING: `FINDING-2026-08-10d-CLAUDE-OP5-SN4-CLIMB-061-IS-THE-WHOLE-VARIABLE-ARG-PATTERN-PRIMITIVE-CLASS-NOT-POS-AND-THE-DYNAMIC-ARM-READS-A-CELL-THE-ARG-NEVER-LANDED-IN.md` (§8 = the addendum above).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s38 (Fable 5) [CONVERGENT independent bisect (pre-s37-pull) reconfirms `1af93e3a`; DELTAS ONLY: blast radius **11→8→3** with the witness list · **H31 re-enters by NESTING (7 live blobs, zero recursion) — s37's open question ANSWERED, one class not two** · killswitch/env exoneration sweep (no rescue exists) · false-11R retraction (reaped background build — s37's container lesson, build side). Zero code landed; close tips SCRIP `66322b3c` · corpus `19a5bf9c` (session measured at `a5533659`/`f728c278`; re-proved at close tip)]

**WATERMARK (re-proved at close vs `a5533659`, VERIFIED clean rebuild — 263-line foreground build, exit-checked):** m3 **133**/15xf/0/**3R** (D12 D13 H31) — third independent proof of the shared number (s36, MECH, this). m4 not re-run (no codegen touched; MECH's m4 132/16/0/3R stands).

**REBASE RESULT (close):** `.github` pulled `2bd8f73b` (RTX hash-refresh — zero CLIMB collision; parallel s37 → `2fed63fe`) · corpus pulled `19a5bf9c` (`probe/ab_expr_define` — ZERO files under `probe/bb/`, suite basis unchanged) · SCRIP fast-forwarded `a5533659`→**`66322b3c`** (RTX-FUNC-8 hatch · alias guard · RTCC dedup). **POST-REBASE RE-PROOF at `66322b3c` per METHOD 8** (clean 263-line foreground build 20:48:18, measurement 20:48:26): m3 **133**/15xf/0/**3R** — unchanged. Housekeeping found-not-made: SCRIP's main checkout arrived DETACHED at `a5533659`; normalized to `main`==origin at close.

**CONVERGENCE, NOT NOVELTY:** this seat ran its own 5-step `git bisect` (private-OBJ clean build each step, endpoints self-verified) and landed on `1af93e3a` before pulling — same conviction as the 08-10b seats and s37. Recorded as independent confirmation only. s37's next-seat rule holds and is hereby double-underlined: **grep the FINDING set for the suspect range BEFORE spending 1-CPU builds.**

**DELTA 1 — BLAST RADIUS 11→8→3 (quantifies D-2/D-3's partial repair):** at `1af93e3a` ELEVEN witnesses crash rc=139 — **D06 D09 D10 D11 D12 D13 H20 H21 H31 X07 X08** — all eleven pass at parent `930539c0` (clean build both endpoints). D-2 `1f96143c` + D-3 `ef8a3052` restore eight; the residual 3R are exactly the re-entrant subset. **⇒ M-1b acceptance must ALSO hold the repaired-8 green** — GLOBAL-EXECUTE deletes the layer D-2 built; the eight are its regression guard.

**DELTA 2 — H31 IS THE SAME CLASS: RE-ENTRY BY NESTING.** s37 flagged H31 ("no recursion, yet fails identically… second class hiding in the same rc?"). Answer from emitted-asm census (`--compile`, grep `proc_PAT$`/`g_blob_ctx`): H31 mints **7 blobs / 42 ctx refs** — `pair`'s activation is LIVE when `value`'s blob enters, which is live when `bool`'s enters (FENCE-in-FENCE); each α overwrites the ONE global cell, the outer's scanhit/β then reads the inner's cells. Control: **D06 mints 2 blobs and PASSES** — its two activations are strictly SEQUENTIAL (two separate statements, never co-live). Discriminator, sharpened: **>1 simultaneously-live blob activation**; recursion (D12/D13 `*LIST`) and nesting (H31) are the two species of the one class. The 3R witness set therefore spans both species — good acceptance coverage for M-1b, no fourth witness needed.

**DELTA 3 — EXONERATION SWEEP (negative results M-1b's implementer should not re-run):** `SCRIP_{PAT_INLINE,RTCC,AB,ARBNO_LATCH,CALL2BB,STMT_FRAME}` × {0,1} — all 12 arms crash D12 · `ulimit -s 65536` same · `setarch -R` same · deterministic 5/5. No killswitch or environment rescue exists; the defect is structural only, consistent with Lon's delete-the-global ruling.

**REVERT IS MECHANICALLY DEAD (adds a fact to s37's policy bar):** `git revert 1af93e3a` CONFLICTS in emit.cpp — D-2 did not patch the deleted region, it REPLACED the design (interior readers now speak `rsp+op_flat_disp`; β res-stub refills ctx from the carve header). Resurrecting the pin breaks HEAD's readers even for the owner. Moot under M-1b's forward deletion; recorded so nobody tests it again.

**⛔ RETRACTION — FALSE 11R AT HEAD, MINE:** mid-session this seat reported m3 125/15/**11R** at `a5533659`. WRONG — measured against a **reaped background build** (s37's container lesson, build side): `nohup make` reaped between tool calls, empty log + vanished process misread as completion, binary mtime **20:22** postdates the ~20:0x suite run. Verified clean rebuild reproduces **133/15/3R** exactly — s35/s36/MECH watermarks were right, the tree was innocent. Second hazard while correcting: Makefile's `OBJ := /tmp/si_objs` is SHARED across checkouts/worktrees — every bisect build here used a private `OBJ=`. Both → METHOD item 8.

**CROSS-REQUEST ADDENDUM → MECH M-1b (extends s37's):** acceptance = `{D12,D13,H31}` green BOTH modes **AND** `{D06,D09,D10,D11,H20,H21,X07,X08}` remain green (the D-2/D-3 repaired set). The set spans both re-entry species by construction.

**LON (s37's park question, +1 with a reason):** park the trio as XFAIL tagged `M-1b` — three seats have now independently re-proved the same 3R; every further session pays the "is this mine?" tax against a red background. Removal in the repair commit per RULES §4. Lon rules.

**NEXT (unchanged from s37, now with one fewer open question):** C-9 residuals — 061 variable-arg POS first, then `test_string`, `test_case` rc=134, TAB-binds-subject (manual p.143 #10); 065 separate pre-existing class. Then C-10. FINDING: `FINDING-2026-08-10-CLAUDE-SN4-CLIMB-S38-DEL-T1-BLAST-RADIUS-11-8-3-AND-H31-RE-ENTERS-BY-NESTING-AND-A-REAPED-BUILD-FAKED-11R.md`.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s37 (Opus 5) [D12/D13/H31 A/B-pinned to `1af93e3a` (DEL-T1 D-1) — ⛔ BUT the bisect was VOID WORK and the mechanism/owner are SUPERSEDED by MECH s36d, pulled at close: root cause is `g_blob_ctx[5]`, owner is MECH M-1b, discriminator is RE-ENTRY. Read MECH's cursor for the mechanism. No code changed; SCRIP `a5533659`, corpus `f728c278` untouched]

**WATERMARK:** not re-run this session — **no code changed**, trees left byte-identical to s36 close (SCRIP `a5533659`, corpus `f728c278`). s36's m3 **133**/15xf/0/**3R** stands unmodified. The 3R are the three probes below.

**⭐ THE 3R ARE CONVICTED, MECHANICALLY.** `git bisect run` over the full 33-commit range `6ffa57fe..c7e085fd`, endpoints pre-verified by the driver (NOT taken on the inherited cursor's word — `6ffa57fe` measured D12 PASS rc=0, `c7e085fd` measured FAIL rc=139):

**FIRST BAD COMMIT = `1af93e3a` — "DEL-T1 D-1: DELETE the BLOB-GRANT frame establishment for PAT$ blobs (Lon directive s9)."**

All three confirmed together against the convicted commit and its parent `930539c0` — parent: D12 `=S` ✅ D13 `=F` ✅ H31 `k=age s= n=42 b=` ✅ all rc=0; convicted: **all three rc=139, empty stdout**. One commit, one cause, not three defects. Crash class, not value divergence — the 2-way monitor would be dark to it (zero trace events, zero output), so MON-CAP would have been the prerequisite had this been CLIMB's to hunt.

**⛔ IT IS NOT CLIMB'S TO HUNT — AND NOT A DEFECT.** `1af93e3a`'s own message: *"THIS COMMIT IS DELIBERATELY BREAKING (delete-first by owner direction; DEL-T1 ladder, GOAL-PASSTHRU-RBP-ERAD) … Every pattern program routing through a surviving PAT$ blob is expected broken until D-2."* D12/D13 are recursive `*LIST` patterns; PASSTHRU PT-3 states verbatim that **recursive/sealed-DEFER stays DEFER** — i.e. keeps its blob — so these are surviving-blob programs by design. Repair = PASSTHRU **PT-4 (open, `[ ]`)**, a new frame/glue/linkage protocol, which this ladder's charter forbids CLIMB from landing. **⛔ DO NOT REVERT `1af93e3a`** — owner-directed rung, revert path reserved to its owner.

**⛔⭐ POST-REBASE CORRECTION — MECH s36d (`f2b6ea0e`) LANDED DURING THIS SESSION AND DEMOTES MOST OF THE ABOVE. Read MECH's cursor, not this one, for the mechanism.**

- **THE BISECT WAS UNNECESSARY.** MECH s36 says verbatim *"DO NOT RUN s35's BISECT — IT IS VOID … the DEL-T1 trio was already convicted by two parallel seats on 08-10b."* ~50 min of 1-CPU builds re-derived a known result. Partly avoidable: FINDING-2026-08-10b's hash-correction table names the trio, and I opened it *after* launching the bisect. **Next seat: grep the FINDING set for the suspect commit range BEFORE spending builds.**
- **RETRACTED — "a fourth uncounted casualty set."** MECH's re-proved watermark m3 133/15/0/**3** · m4 132/16/0/**3** names *"REGRESSION set D12·D13·H31 both modes"* — that watermark **is** this suite. Already counted, and MECH measured **m4** where this session did not.
- **MECHANISM SUPERSEDED.** Not "recursive-DEFER keeps its blob" (PT-3 carve-out): Lon s36c/s36d rules **"nothing special about `*name` DEFER"**, and MECH named the root cause from source — the process-global mirror **`g_blob_ctx[5]` (`pattern_match.c:624`)**, every reader re-basing off the GLOBAL instead of its own already-correct per-activation cell.
- **OWNER IS MECH M-1b, NOT PASSTHRU PT-4.** GLOBAL-EXECUTE: `g_blob_ctx` **deleted, not bracketed**; header layer deleted, not rethreaded. My cross-request re-homes to **MECH M-1b**.
- **⭐ MECH ALSO FALSIFIES THE OBVIOUS NEXT GUESS:** "every blob-bearing program fails" is FALSE — D10 (18 `proc_PAT`, 6 `g_blob_ctx` refs) and D11 **pass both modes**. The discriminator is **RE-ENTRY**, not blob count or capture (`W3_selfrec` hangs on the same footprint as passing D10). D12/D13 are recursive `*LIST` ⇒ re-entrant by construction — that is the right frame for them.

**WHAT SURVIVES:** the clean same-container **parent-vs-convicted A/B on all three probes** (`930539c0` all rc=0 oracle-matching → `1af93e3a` all rc=139 empty), which pins these three specific witnesses to **one** commit and shows one cause, not three defects; plus the container method note in the FINDING (background jobs are reaped between tool calls unless `setsid`; `pgrep -f 'make|cc1'` self-matches the invoking shell and reports a dead build alive).

**s36's GUIDANCE IS STILL RETIRED (this part stands):** "the 3R are the highest-priority blocker before C-9 can be closed" is false as framed. The repair is MECH M-1b structural work; CLIMB cannot land it, and C-9's residuals never depended on it. Waiting idles this ladder behind another seat's rung.

**⛔ CROSS-REQUEST → MECH M-1b:** `corpus/probe/bb/probes/{D12,D13,H31}.sno` as acceptance witnesses. Oracle refs already present and not litigable — **D12/D13 are the SPITBOL manual's own recursive-LIST examples (p.122)**, subjects `(12,(3,45,(6)),78)`→`=S` and `(12,(34)`→`=F`, verbatim. D12/D13 are re-entrant (fits MECH's discriminator); **H31 is the interesting one — FENCE-over-ALT with captures and NO recursion, yet it fails identically**, so if re-entry is the discriminator, H31 either re-enters by a path worth naming or is a second class hiding in the same rc.

**⛔ QUESTION FOR LON (blocks nothing; needs a ruling):** RULES forbids XFAIL additions "except a Lon-ruled park." Should the DEL-T1 casualty set be a **park** — XFAIL tagged `DEL-T1/M-1b`, removed in the same commit as the repair — or stay red as visible debt? Standing red corrodes the watermark as shared state: a seat that lands a *real* regression cannot see it against a red background.

**NEXT:** C-9 residuals, unblocked — 061 (variable-arg POS, match fails outright), `test_string`, `test_case` rc=134, TAB-binds-subject (manual p.143 #10). 065 rc=139 is a separate pre-existing crash class. Then C-10.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s36 (Sonnet 4.6) [C-9 splice fix confirmed green post-rebase; watermark re-proved; D12/D13/H31 still inherited open; SCRIP `a5533659`, corpus `f728c278`]

**WATERMARK (re-proved at open AND close, post-rebase):** m3 **133**/15xf/0/**3R** · m4 not re-run (inherited 3R unchanged, no codegen touched this session).

**REBASE RESULT:** `git pull --rebase` pulled `d21b80c5` (RTX-FUNC m4 fix) onto SCRIP; corpus pulled `f728c278`. Zero semantic collision. `942ef1b1` (C-9 REPL-ZDEPTH) confirmed in history. Rebuilt, gates re-proved.

**CAPTURE FAMILY POST-REBASE:** 058 059 060 062 063 064 066 all ✅ both modes. 061 (variable-arg POS, match fails outright) and 065 (pre-existing rc=139 CRASH) unchanged — separate classes, not C-9 splice.

**INDEPENDENT ROOT-CAUSE DERIVATION (this session):** Arrived at the same root cause as s35 independently, via MONITOR-FIRST + in-tree tracer `SCRIP_REPL_TRACE=1` + cheapest-discriminating-experiment protocol. Two hypotheses died before code was read: (1) "prefix dropped" falsified by non-empty suffix probes — suffix also gone; (2) "constant +16" falsified by replacement SHAPE — concat/arith chains carve more than one cell so +16 doesn't hold. Probe reverted; tree clean. Full derivation in `FINDING-2026-08-10-CLAUDE-SN4-CLIMB-C9-SPLICE-FRQ-READS-MISS-BY-REPLACEMENT-SUBTREE-DEPTH-TWO-HYPOTHESES-DIED.md`.

**ADDITIONAL FINDING this session:** The defect is also a **regression vs. committed artifacts**: `062_capture_replacement.s` in corpus carried `[rsp+176/184/64/88]` (correct); HEAD `c7e085fd` emitted `[rsp+160/168/48/72]`. A/B of committed artifact vs current compiler exonerates the C of record without reading any code and names the emitter as the site. IMPORTANT: "sweep the compiler, not the artifacts" — committed `.s` carry PRE-regression honest output and would wrongly exonerate the compiler if used as gate. Do not wire `.s` byte-identity for this family into a gate.

**⚠️ INHERITED REGRESSIONS — NEED AN OWNER:** D12 · D13 · H31 fail at HEAD both before and after `942ef1b1` (proven by stash + pristine rebuild per s35). They were green at s34 watermark (`6ffa57fe`). Introduced between `6ffa57fe` and `c7e085fd` by a parallel seat. Not diagnosed here — left for the seat that introduced them or MECH to own.

**GUIDANCE FOR NEXT SESSION:** The inherited regressions are the highest-priority blocker before C-9 can be closed. Approach: `git bisect good 6ffa57fe bad c7e085fd` on D12 (simplest: "recursive pattern via * (manual LIST)") to identify the introducing commit, then MONITOR-FIRST on that commit's program. C-9 residuals (061 variable-arg POS, test_string, test_case rc=134, TAB-binds-subject) come after the 3R are cleared.

**NEXT:** D12/D13/H31 regression hunt (bisect from `6ffa57fe` to `c7e085fd`); then 061/test_string/test_case residuals; then C-10.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s35 (Opus 5) [⭐ C-9 SPLICE DEFECT FIXED AND LANDED; SCRIP `9b951391`, corpus regen committed]

**WATERMARK (post-session):** m3 **133**/15xf/0/**3R** · m4 **132**/16xf/0/**3R** — ⚠️ the 3R are **INHERITED, NOT THIS SESSION'S** (see below). crosscheck m3 **242**/75 (was 239/78 at open).

**C-9 ROOT CAUSE + FIX (LANDED `9b951391`):** `zd_k()` classes `IR_MATCH_REPLACE` K=0 — correct, the splice owns no result cell — so the choke set `op_zdepth=0`. But `FRQ(off)` on the RSP/FORTH arm is `[rsp+off+op_zdepth]`, and the replacement expression runs AFTER `MATCH_END` with its cells **still live** at the splice (`r9` = `lea` of rv's own cell). All four frame reads (subject lo/hi, match start/end) were short by exactly the subtree footprint → stale stack → empty subject + zero span → "replacement verbatim", the degenerate whole-subject case. `ROQ`/`ZOPQ` ignore zdepth, which is why name and replacement were always correct — **that selectivity was the fingerprint**. FIX: `g_zd_zunder`, staged for `IR_MATCH_REPLACE` only, consumed at the choke as an `op_zdepth` addend; mirrors the `g_zd_ztail`/`IR_TO` precedent. 3 insertions / 3 deletions.

**⛔ THE DELTA IS NOT A CONSTANT:** +16 for a literal replacement, **+80 for `A '-' B`** — the subtree's ζ footprint, confirmed against the planner's own `zout`. **Any fix hardcoding 16 passes the literal case and FAILS concat. Witness: `P8_concat_repl`.**

**⛔ CORRECTION — DO NOT REVIVE `op_wpop`/CONST-WPOP FOR THIS:** my own first handoff offered it. It is WRONG. CONST-WPOP pops an *orphaned* cell on the **failure** edge; these cells are live on the **success** path and `r9` points into them. Compensating the reads is the only viable shape.

**GATE (full untruncated A/B, pristine HEAD `c7e085fd` vs fixed):** crosscheck m3 239/78 → 242/75, EXACTLY 3 status changes, **ALL fail→pass** (062_capture_replacement · 063_capture_null_replace · 064_replace_multi_arm), **ZERO pass→fail**. 141-probe m3 133/15/0/3R **identical before and after** (neutral). m4 132/16/0/3R. Probes P1/P5/P7/P8 green BOTH modes. Regen ×3 done (benchmark none · feature 062/063 · demo pattern_test).

**⚠️ INHERITED BREAKAGE — NEEDS AN OWNER:** `D12` `D13` `H31` fail at HEAD `c7e085fd` **without** my change (proven by stash + pristine rebuild). They were green at the s34 watermark, so a parallel seat broke them between `6ffa57fe` and `c7e085fd`. Not diagnosed here.

**⚠️ PHANTOM RECORDED:** a first A/B appeared to show 3 pass→fail (134/135/136 balanced-parens). Artifact of a sweep script truncating the failing list (`head -40`) while FAIL was 75–78. Untruncated re-run: they fail in BOTH builds. Do not chase.

**RESIDUAL (separate classes, C-9 not yet closable):** `test_case` rc=124 hang → **rc=134** — the runaway loop WAS a consequence of the clobbered subject (canonical `ALOOP` delete-all idiom, manual Ch.6 p.73, went non-convergent); now terminates, still fails. `test_string` still fails. `061_capture_in_arbno` = `POS(N)` with a **variable** arg fails the match outright (not a splice defect). `065` pre-existing CRASH.

**NEXT:** C-9 remains OPEN on the residual set — enter at `test_string`, then 061 (variable-arg POS), then reassess `test_case` rc=134. TAB-binds-subject trap still unprobed. Then C-10.
**FINDING:** `FINDING-2026-08-10-CLAUDE-SN4-CLIMB-C9-REPLACE-IS-K0-SO-OP-ZDEPTH-IS-ZERO-AND-FRQ-READS-MISS-BY-THE-REPLACEMENT-SUBTREE-DEPTH.md`

## ⭐ CROSS-SEAT NOTE — 2026-08-09e (from GOAL-SNOBOL4-BB): the DEFINE m4 hard-block is REMOVED
Every DEFINE-bearing program failed to LINK in m4 at prior HEADs (undefined `<FN>_act_γ`, ld exit 1, rc=1 with zero output through stderr-swallowing harnesses) — a silent ceiling on any m4 rung touching user functions.  Fixed at BB `7170c539` (details FINDING-2026-08-09e-fa-ab3a §4).  MEASURED: ab_recurse/ab_freturn green BOTH modes; **X12 (defer element inside an INVOKED proc — the DEFINE × *P intersection) green BOTH modes.**  CLIMB needs no AB-3b for correctness: the legacy call path is oracle-correct; AB is BB-seat mechanism.  ⚠ The board instrument's OTHER rc=1 (m3 `hello.sno`, driver exit code on success, 09c cursor) remains UNFOUND and is unrelated.  Current watermark (150 probes): m3 146/4 {H29 N21 fence_probe t6m} · m4 144/6 {A06 D06 H29 N21 fence_probe t6m} — the attackable-first set and D06-m4-behind-OPS-2 routing below are unchanged and current.

## ⛔⛔ READ BEFORE TOUCHING D06 — S23 ANTI-PATTERN (four process failures, one session, zero fixes)
S23 spent a full session on D06 and landed NOTHING. Every failure was covered by a rule already in RULES.md or this file. Do not repeat them:
1. **MONITOR DARK ⇒ MON-RE FIRST, NOT "GO READ THE EMITTER."** D06's monitor exits 0 (all 11 steps agree) and diverges only on stdout — that means the instrument cannot see this defect. RULES.md §1 says reinstating the monitor is then the FIRST rung, not an optional preliminary. S23 read the exit code, shrugged, and went straight into `emit.cpp`. Everything downstream was the print-scatter/guess failure mode MONITOR-FIRST exists to prevent. **If you cannot bracket D06 between two trace events, your first task is making the monitor able to see a stdout-only divergence (capture-level trace events), not finding the bug.**
2. **DO NOT INHERIT THE OFFSET/SLOT-ALIASING THEORY. IT IS NOW FALSIFIED TWICE.** The C-6 rung already warned "re-diagnose from scratch, do NOT inherit the s10 `rt_defer_get_pat_fn` theory." S22 then handed forward a slot-aliasing hypothesis (`[kt-32]`/`[kt-40]`, blob frame aliasing, ival propagation) and s23 adopted it whole and spent the session hunting an emitter offset bug. **MEASURED AND EXONERATED (s23): the ZOPD read address is CORRECT.** D05 (passes) and D06 (fails) both emit the identical `mov eax, dword ptr [rsp + 128]` at the IMM α — the ZD planner already accounts for the DEFER blob's stack overhead. `op_zread[1]`, `op_fc_disp`, and `op_zdepth` are all innocent. Any new theory that ends in "the offset is wrong by N bytes" is contradicted by this measurement; check it against the emitted asm BEFORE spending a rung on it.
3. **RUN THE CHEAPEST DISCRIMINATING EXPERIMENT FIRST.** D05 and D06 are the same shape (`POS(0) *P $ OUTPUT`) and differ in EXACTLY ONE property: D05's P is written ONCE (`sno_fz_wrcount==1` → `sno_seal_pat` resolves → `seal=2` → the write-once defer cell arm in `bb_match_defer` is live), D06's P is written TWICE (`wrcount==2` → `sno_seal_pat` returns NULL → `seal=0` → raw GVA + `dtp_fn_of` arm only). That is the whole delta, it is visible at `lower_snobol4.c:1262`, and it is the lead. **First experiment next session: make the two probes swap paths** — add a second write to D05's P (expect D05 to break the same way ⇒ defect is in the seal=0 GVA arm), and/or force D06 onto the cell arm. One probe pair settles which side owns the bug before any code is read.
4. **MEASURE, THEN EDIT — NEVER THE REVERSE.** S23 made three emitter edits, none preceded by a measurement that could have predicted the outcome. The `op_zread[1] += 112` "fix" made D06 green and silently broke D05, because 112 was inferred from reading blob assembly instead of checking what `op_zread[1]` already was. The falsifying check was ONE `--compile` and ONE grep. **Before any edit: dump the emitted asm for the failing probe AND its nearest passing sibling, and diff them.** If the instruction you are about to "fix" is byte-identical in the passing probe, the bug is not there.

**S23 NET RESULT (the only durable output):** the read-offset class is exonerated; the seal=2 vs seal=0 split is the live lead; the next instrument is `saved_delta`/`cur_delta` at the `rt_cap_open` call site (one `fprintf` in `src/runtime/pattern_match.c`), which discriminates start-cursor corruption (SAVE side) from end-cursor corruption (r14d after the blob γ-returns). Tree left clean at watermark; no commits.

## ⛔ METHOD (binding every rung)
1. Rung gate = every witness green in BOTH modes BY SET vs oracle `.ref`; crash detection is wait-status (SIGSEGV handler swallows — run_suite.sh does this; ad-hoc runs must check rc). 2. MONITOR-FIRST: divergence bracketed between last-agree and first-diverge trace events BEFORE reading code; monitor dark for the mode ⇒ MON-RE first (known defect B: stno counting — `build_stno_map.py` canonical). 3. Fix at the bracketed site; re-run monitor, divergence must MOVE. 4. XFAIL removal same commit; NEVER add to XFAIL except a Lon-ruled park. 5. Timeouts 8s unit / 30s corpus; `< /dev/null` on scrip calls; grep error: both build logs. 6. m3-only defects: suspect the m3 glue stubs first (X12 lesson: one-shot push-of-zero classes — FI8 lazy-init, PS-1b twin driver:1554, first-touch trampoline bakes; m4 clean = emission exonerated). 7. SPITBOL semantics per manual (SCRIP FOLLOWS SPITBOL; case-sensitive); manual anchors inline per rung below. 8. A WATERMARK REQUIRES A VERIFIED BINARY (s38): build FOREGROUND + exit-checked; binary mtime must PREDATE the measurement — the container reaps background jobs between tool calls (a reaped `nohup make` faked 11R at HEAD; empty log + vanished process is NOT completion). Bisect/worktree builds take a PRIVATE `OBJ=` — Makefile's `/tmp/si_objs` is shared across checkouts and cross-contaminates.

## RUNGS (stop at first fail; a rung already green just gets its checkbox and a HEAD-stamp)
- [x] **C-0 RE-PROVE + xc318 POST-FLIP FLOOR** — ✅ 2026-08-08 s1. m3 124/18/0/0 · m4 116/26/0/0 · xc318 m3 286/31 · m4 268/48/SKIP=1/DIVERGE=21 (setarch -R). Floor recorded. SCRIP c0372fec · corpus f65d8278.
- [x] **C-1 VALUE SPINE: 1+1 → arith/assign/concat** — ✅ 2026-08-08 s2. All crosscheck arith_new + A01–A04 + 023_arith_add pass m3+m4. SCRIP 29f89dd5.
- [x] **C-2 CONTROL** — ✅ 2026-08-08 s2. 031–037 + indirect :($X) + 1020_code_label_transfer pass m3+m4. -NOFAIL fixed (error 35, rc=1, no fallthrough). SCRIP 29f89dd5.
- [x] **C-3 STATIC PATTERNS** — ✅ 2026-08-07 s4. LIT ANY NOTANY SPAN BREAK BREAKX LEN POS RPOS TAB RTAB REM BAL ARB all green m3+m4. &ANCHOR (scan-retry law) green. L-family 20/20, crosscheck 039–049, W05/W06/W07, BAL 135–137/174–177, 045_pat_rpos, 098_keyword_anchor. SCRIP 29f89dd5 · corpus 6f5e8102.
- [x] **C-4 ALTERNATION + CAPTURES** — ✅ 2026-08-07 s8. A-family 13/13 m3+m4. pb_snapshot_imm + stitch_defer + stitch_snapshot + stitch_compose all green m3+m4. m3 134/8 · m4 131/11, 0 REGRESSION. F02 G15 m4 xfail (UCLAIM pinned-rbp path — separate class, not C-4). SCRIP `6396a534` · corpus `82f127c7`.
- [x] **C-5 FENCE0 / FENCE1** — ✅ 2026-08-07 s9. G06/G22/G23 ✅ both modes; H14/H23 ✅ both modes; G24/H20 xfail m3 (C-6 deferred). F-family 5+1xfail m4 (F02 UCLAIM pre-existing). SCRIP `6396a534`.
- [x] **C-6 DEFERRED EVAL + RECURSION** — ✅ 2026-08-09 s34 (discovered closed — parallel OPS-2 landed D06 m4). D-family 13/13 both modes. D02/D03/D04/D06/D07/D08/D09/D10/D11/D12/D13 all ✅ m3+m4. Blob sharing PROVEN (D13 mints exactly 2 blobs). SCRIP `6ffa57fe` · corpus `f46d3ebe`.
- [x] **C-7 ARBNO + NESTING** — ✅ 2026-08-09 s34. H-family 27/4 · N-family 14/7 · X-family 10/2 · both modes · 0 REGRESSION. dc_sib_bt CLOSED (ZGPOP-BLOB fix). H21 ✅ (FENCE1+deferred-ALT). H24/H25/X02/X06/X11 ✅ (nested-ARBNO structural class already fixed by prior sessions). Remaining xfail: H09/H11/H12/H13 (CRASH, not gated) · N12/N15/N17/N19 (CRASH, not gated) · X09/X10 (CRASH, not gated) · X08 pre-existing CRASH (ARBNO+SPAN, not gated). SCRIP `6ffa57fe` · corpus `f46d3ebe`.
- [x] **C-8 FUNCTIONS** — ✅ 2026-08-09 s34. DEFINE/RETURN/FRETURN/NRETURN/recursion(fib)/OPSYN/APPLY/ARG all green m3+m4. rung10 crosscheck 13/13. crosscheck/functions 10/10. uw2/uw3 ✅ · test_stack ✅ · 209(NRETURN) ✅ · 085/086/087/088/089/090 ✅ · X12(value/defer-in-proc) ✅. CALL2BB parked (SCRIP_CALL2BB default 0 — folds at MECH M-5 only if measured). SCRIP `6ffa57fe` · corpus `f46d3ebe`.
- [ ] **C-9 REPLACEMENT + SPLICE** — 5-stage stmt execution stages 4/5 (Lon ruling in HQ): build replacement, splice; TAB-binds-subject trap manual p.143 #10; capture-replacement queue item; replacement-splice PENDING LON RULING (sole-consumer fence retirement) — surface it here if the rung blocks. **⛔ s37/s38: D12/D13/H31 = `g_blob_ctx` re-entry (two species: recursion D12/D13, nesting H31), owner MECH M-1b GLOBAL-EXECUTE — not CLIMB's to land; do NOT re-bisect (FINDING set names the trio); C-9 residuals (061 · test_string · test_case · TAB-binds-subject) proceed UNBLOCKED.**
- [ ] **C-10 DATA + KEYWORDS** — ARRAY/TABLE/DATA/field, ITEM, SORT/RSORT, CONVERT; &STLIMIT &DUMP &PROFILE (monitor infra), &FENCE primitive, &ANCHOR. Witnesses: data family, 131/141/183 (131 = ex-PATREF 528B UCLAIM customer — must be clean post-M-3), 066, W04_arbno_*.
- [ ] **C-11 EVAL / CODE / EXEC (the summit construct)** — EVAL(string/expr), CODE(string) → code object executed via direct goto `<code>`, SETEXIT, CONVERT to EXPRESSION; the eval-family falsification controls **1016 1019 161 expr_eval MUST HOLD** (named gatekeep class); eval_fixed rc=0 witness; S233/S234 deferred-eval-family merge-defect findings are the map (γ success-code blocker, not ω).
- [ ] **C-12 SUMMIT GATES** — beauty_suite 19 drivers vs refs (beauty.sno itself is DESTINATION not gate) · xc318 BY SET ≥ 280/260 BOTH modes · bench 18/21 hold-or-better · rc=139 tail zero · 127/152 env-pad IMPOSSIBLE (post-M-6 a flake is a LEAD) · armed roman full MDCCLXXVI · regen ×4 · FINDING + both cursors + MECH DoD cross-check.

## WITNESS SET (report every session): probe suite both modes · roman · pb_snapshot_imm + stitch trio · uw2 uw3 · 131 141 183 066 · 045_pat_rpos · eval controls 1016/1019/161 · eval_fixed · func_call · test_case (honest wedge) · 127/152.

## COORDINATION
MECH owns structural edits; this ladder NEVER lands a new frame/glue/claim protocol — it files the cross-request and blocks the rung on MECH's cursor. Both re-prove the shared watermark at open/close. Icon: after ANY shared-file edit, run the 303-emission content-hash sweep per MECH's procedure. `.github` pushes last; handoff per RULES (regen scripts if codegen touched; handoff_status.sh verbatim).
