# GOAL-SN4-ZETA-CLIMB — SNOBOL4 ON ζ-CELLS: THE CLIMB, 1+1 → EXEC/CODE (sole FORTH regime)

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

**CHARTER (Lon 2026-08-08):** walk the ENTIRE SNOBOL4 language bottom-up at the sole ζ-cells-on-stack regime (selectors deleted, SCRIP `de837576`): stop at first failing rung, MONITOR-FIRST, fix, one FINDING per land mine, XFAIL shrinks monotonically (removed in the SAME commit as the fix), move up. Summit = EVAL/CODE/EXEC + beauty drivers + xc318 ≥ pre-flip both modes. Twin: `GOAL-SN4-ZETA-MECH.md` owns STRUCTURE; this file owns CORRECTNESS. A defect whose fix needs new protocol = MECH rung (cross-request both cursors). Protocol: RULES.md (monitor scripts §1: `test_monitor_2way_sync_step_bin.sh` / 3-way PARTICIPANTS · offline `probe.py` &STLIMIT+&DUMP replay bisect · gdb spin-counter, HW watchpoints DEAD in container, `CSN_NO_SEGV_HANDLER=1`/`SCRIP_NO_SEGV_HANDLER` clean-bt · `setarch -R`). Oracle: `/home/claude/x64/bin/sbl -b f.sno`; corpus paths CORPUS-LOCATIONS.md; suite `corpus/probe/bb/run_suite.sh` (m3) + `MODE=compile` (m4).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s31 (Sonnet 4.6) [N21 CLOSED via parallel MECH CONST-WPOP; revert of duplicate fix; SCRIP `c586076`, corpus `decfadf`]

**WATERMARK (post-session, 0 regressions):**
- m3: **133**/17xf/0/0R
- m4: **131**/18xf + D06 XPASS/0R
- X08 = pre-existing CRASH on HEAD (confirmed `git stash` control); not gated here.

**N21 ROOT CAUSE (measured, MONITOR-FIRST):** `bb_match_arbno_frameless()` (ARBNO-LON K0 arm) missing right-spine RSP compensation. When `RPOS(0)`'s `LIT_INTEGER` arg does `sub rsp,16` and RPOS fails, β re-enters PAIR(2)/as with RSP displaced 16B below the ARBNO cell — `[rsp+4]` reads garbage, stall check bogus, yield-cursor write corrupts LIT_INTEGER's frame, ARBNO loops on same cursor until CAS mark limit fires (~3s) → `=F`. Monitor bracket: DIVERGE step 4, stno=3.

**N21 FIX — PARALLEL SEAT** (commit `7269e11`, landed before this session pushed): `ZD-5b POS/RPOS CONST-WPOP` — adds `op_wpop+=16` in POS/RPOS staging when `ZC_PORT_FORTH && g_zd_arm && !fc_geom(a0)`, arming `x86_fc_jcc_omega` to emit `je Lγ / add rsp,16 / jmp ω / Lγ:` on failure — pops the orphaned LIT_INT cell at the RPOS ω rather than compensating in each ARBNO arm. Cleaner, single-layer fix. This session independently found the same root cause and implemented a parallel fix in the ARBNO template (compensate in PAIR(2)/PAIR(3) via `op_tail_fpr_rsp`). That approach was correct in isolation but double-pops when both fixes are applied → REVERT committed at `c586076`. CONST-WPOP is the surviving fix; our FINDING documents the root cause for the record.

**XFAIL retired this session:** N21 from XFAIL.run; D06+N21 from XFAIL.compile. D06 m4 XPASS via parallel MECH SLACK-RIDER RE-HOME (`62cf516`).

**C-7 RUNG UPDATE:**
- N21 **CLOSED** (parallel MECH CONST-WPOP).
- **Remaining C-7 open:** H29 (CRASH, gdb-reachable — FENCE1+TAB/ABORT idiom), H21 (FENCE1 over deferred ALT — probe before inheriting D-gate), dc_sib_bt (defer-β re-yield; sibling ARBNO capture, witness committed `corpus/226f904b`).
- X08 (ARBNO with ITEM=SPAN pattern) crashes on HEAD — pre-existing, not this session's target.
- Nested-ARBNO class H24/H25/X02/X06/X11 remain structural (lower-side sno_seq_nary).

**CONCURRENT WATERMARK NOTE:** The parallel CONST-WPOP fix (`7269e11`) was at a different watermark baseline (133/17 m3 · 131/19 m4) than the s26 cursor's 144/4 — because parallel sessions between s26 and s31 added XFAILs (the existing 17/18 xfail count). The XFAIL baseline in run_suite now reflects the net-retired set: N21 and D06 retired; others added by intermediate sessions.

**NEXT:** C-7 entry — H29 (gdb-reachable CRASH) or dc_sib_bt (defer-β re-yield). MONITOR-FIRST per RULES.md.

## ⭐ CROSS-SEAT NOTE — 2026-08-09e (from GOAL-SNOBOL4-BB): the DEFINE m4 hard-block is REMOVED
Every DEFINE-bearing program failed to LINK in m4 at prior HEADs (undefined `<FN>_act_γ`, ld exit 1, rc=1 with zero output through stderr-swallowing harnesses) — a silent ceiling on any m4 rung touching user functions.  Fixed at BB `7170c539` (details FINDING-2026-08-09e-fa-ab3a §4).  MEASURED: ab_recurse/ab_freturn green BOTH modes; **X12 (defer element inside an INVOKED proc — the DEFINE × *P intersection) green BOTH modes.**  CLIMB needs no AB-3b for correctness: the legacy call path is oracle-correct; AB is BB-seat mechanism.  ⚠ The board instrument's OTHER rc=1 (m3 `hello.sno`, driver exit code on success, 09c cursor) remains UNFOUND and is unrelated.  Current watermark (150 probes): m3 146/4 {H29 N21 fence_probe t6m} · m4 144/6 {A06 D06 H29 N21 fence_probe t6m} — the attackable-first set and D06-m4-behind-OPS-2 routing below are unchanged and current.

## ⛔⛔ READ BEFORE TOUCHING D06 — S23 ANTI-PATTERN (four process failures, one session, zero fixes)
S23 spent a full session on D06 and landed NOTHING. Every failure was covered by a rule already in RULES.md or this file. Do not repeat them:
1. **MONITOR DARK ⇒ MON-RE FIRST, NOT "GO READ THE EMITTER."** D06's monitor exits 0 (all 11 steps agree) and diverges only on stdout — that means the instrument cannot see this defect. RULES.md §1 says reinstating the monitor is then the FIRST rung, not an optional preliminary. S23 read the exit code, shrugged, and went straight into `emit.cpp`. Everything downstream was the print-scatter/guess failure mode MONITOR-FIRST exists to prevent. **If you cannot bracket D06 between two trace events, your first task is making the monitor able to see a stdout-only divergence (capture-level trace events), not finding the bug.**
2. **DO NOT INHERIT THE OFFSET/SLOT-ALIASING THEORY. IT IS NOW FALSIFIED TWICE.** The C-6 rung already warned "re-diagnose from scratch, do NOT inherit the s10 `rt_defer_get_pat_fn` theory." S22 then handed forward a slot-aliasing hypothesis (`[kt-32]`/`[kt-40]`, blob frame aliasing, ival propagation) and s23 adopted it whole and spent the session hunting an emitter offset bug. **MEASURED AND EXONERATED (s23): the ZOPD read address is CORRECT.** D05 (passes) and D06 (fails) both emit the identical `mov eax, dword ptr [rsp + 128]` at the IMM α — the ZD planner already accounts for the DEFER blob's stack overhead. `op_zread[1]`, `op_fc_disp`, and `op_zdepth` are all innocent. Any new theory that ends in "the offset is wrong by N bytes" is contradicted by this measurement; check it against the emitted asm BEFORE spending a rung on it.
3. **RUN THE CHEAPEST DISCRIMINATING EXPERIMENT FIRST.** D05 and D06 are the same shape (`POS(0) *P $ OUTPUT`) and differ in EXACTLY ONE property: D05's P is written ONCE (`sno_fz_wrcount==1` → `sno_seal_pat` resolves → `seal=2` → the write-once defer cell arm in `bb_match_defer` is live), D06's P is written TWICE (`wrcount==2` → `sno_seal_pat` returns NULL → `seal=0` → raw GVA + `dtp_fn_of` arm only). That is the whole delta, it is visible at `lower_snobol4.c:1262`, and it is the lead. **First experiment next session: make the two probes swap paths** — add a second write to D05's P (expect D05 to break the same way ⇒ defect is in the seal=0 GVA arm), and/or force D06 onto the cell arm. One probe pair settles which side owns the bug before any code is read.
4. **MEASURE, THEN EDIT — NEVER THE REVERSE.** S23 made three emitter edits, none preceded by a measurement that could have predicted the outcome. The `op_zread[1] += 112` "fix" made D06 green and silently broke D05, because 112 was inferred from reading blob assembly instead of checking what `op_zread[1]` already was. The falsifying check was ONE `--compile` and ONE grep. **Before any edit: dump the emitted asm for the failing probe AND its nearest passing sibling, and diff them.** If the instruction you are about to "fix" is byte-identical in the passing probe, the bug is not there.

**S23 NET RESULT (the only durable output):** the read-offset class is exonerated; the seal=2 vs seal=0 split is the live lead; the next instrument is `saved_delta`/`cur_delta` at the `rt_cap_open` call site (one `fprintf` in `src/runtime/pattern_match.c`), which discriminates start-cursor corruption (SAVE side) from end-cursor corruption (r14d after the blob γ-returns). Tree left clean at watermark; no commits.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s26 (Opus — MECH SLICE-1 COLLATERAL: watermark updated, D06 m3 flipped, N08/09/16, D09/11, H31 all PASS; SCRIP `1c958553`, corpus `226f904b`)

**WATERMARK UPDATE (MECH SLICE-1 `1c958553` landed concurrently):**
- m3: **144**/4xf/0/0R (was 136/12xf) — 8 probes up, 8 xfails retired.
- m4: **141**/7xf/0/0R (was 135/13xf) — 6 probes up, 6 xfails retired.
- All three repos pushed. No CLIMB code changed this session — all movement is MECH collateral.

**WHAT FLIPPED (measured at `1c958553`, both modes where stated):**
- D06 **m3 PASS** (was XFAIL.run) — the s17 RSP-aliasing MECH cross-request is RESOLVED. The positive-rbp ζ-frame overlap with live RSP stores is gone: anchor slots now live at `[rbp−64−16k]` (negative, below rbp, inside the GLUE-O widened carve). s17's `rbp+528` ↔ `LIT_INTEGER [rsp+480]` collision cannot occur in the new regime. D06 **m4 still FAIL** — gated on the start_δ/rsp_mark/zls2_mark family (next slack-rider; same environ-smash class, names in MECH s30 cursor).
- D09, D11 **PASS both modes** — ARBNO extent-base captures resolved as SLICE-2 collateral.
- N08, N09, N16 **PASS both modes** — outer ARBNO capture resolved as SLICE-3 collateral. The s25 route-reorder prioritized these as "attackable today"; they are now closed by MECH rather than by CLIMB's own fix.
- H31 **PASS both modes** — FENCE+capture class resolved.

**C-6 RUNG UPDATE:**
- ~~RSP-aliasing MECH cross-request (s17)~~ — **RESOLVED by SLICE-1.** The `rbp+528 ↔ rsp+480` aliasing diagnosed in s17 and the `_fb/_adj` compensation blocks in emit.cpp at COND/IMM are both gone. Do NOT re-file this cross-request.
- D06 m3 **CLOSED** (falls out of XFAIL.run — remove at next XFAIL update commit).
- D09 m3+m4 **CLOSED**. D11 m3+m4 **CLOSED**.
- **REMAINING C-6 open:** D06 m4 only — still gated on OPS-2 (monitor dark for stdout-only class, S23 ANTI-PATTERN §1 still fully in force). The s24 carry-forward (read IR_LIT(_dfr).sval, drop n_operands>0 conjunct, use 48 floor, then re-measure rt_cap_open saved/cur) is still the entry point once OPS-2 lands. DO NOT inherit the s17 aliasing theory — it is resolved and exonerated.

**C-7 RUNG UPDATE:**
- N08, N09, N16 **CLOSED** by SLICE-1 collateral — remove from the "pulled-forward attackable" list.
- **Remaining C-7 open:** N21 (sibling ARBNO capture — not in the slice-1 flip set; probe it), H29 (CRASH class, gdb-reachable), H21 (FENCE1+deferred-ALT), A06-m4 bisect.

**XFAIL UPDATE NEEDED:** at next commit, retire from XFAIL.run: D06 D09 D11 N08 N09 N16 H31. From XFAIL.compile: D09 D11 N08 N09 (confirm with run_suite). Update the baseline watermark line in run_suite if hardcoded.

**dc_sib_bt NOTE for C-7:** MECH s30 added dc_sib_bt to corpus — the sibling-backtrack discriminator for the defer-β re-yield rung (N21 neighbor). Oracle X=AB; SCRIP X= (empty). The probe discriminates per-node from per-depth slots AND convicts the re-yield wiring. The re-yield rung is CLIMB C-7 territory; the committed xfail witness is the entry point.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s25 (Opus — INSTRUMENTS RESTORED + ROUTE REORDER per Lon; SCRIP `ee8707d8`, corpus `3449999a`)
**WATERMARK PROVED both modes on FRESH baselines:** run_suite m3 **136/12xf/0/0R** · m4 **135/13xf/0/0R** · matrix 136/12 · 135/13. m4 matrix runner FIXED (was exec'ing the .s; MECH OPS-1, delta + refs in corpus `3449999a`). XFAIL is now the honest open set BY NAME — run: D06 D09 D11 H21 H29 H31 N08 N09 N16 N21 fence_probe t6m · compile adds A06 D05 X12 minus D11 H21.
**⛔ ROUTE REORDER (Lon 2026-08-09 "re-arrange the order of things"):** C-6's D-family is GATED ON MECH OPS-2 (MON-CAP) — the monitor is blind to its stdout-only class (S23 ANTI-PATTERN §1). **Work the attackable set FIRST, monitor-visible today:** (1) **N08→N09→N16→N21** (C-7 territory pulled forward — ARBNO with OUTER $/. capture and sibling ARBNOs; final =S/=F diverges at statement level, the 2-way monitor CAN bracket it; check whether s28's fpr_rsp/PAIR(2) pair is sufficient once captures ride the right-spine, per s28's own NEXT). (2) **H29** — CRASH class, gdb-reachable per RULES §1 (FENCE1 TAB/ABORT idiom; new probe, never green). (3) **H21** (FENCE1 over deferred ALT — may be D-gated; probe it, route if blind). (4) **A06-m4 CRASH bisect** `6396a534..a19a0258` — suspect TRUE regression (13/13 at C-4 close). D06/D09/D11 resume ONLY behind OPS-2, then the s24 carry-forward applies unchanged.
**⚠ LIFO ruling recorded in MECH (law):** extents ARE LIFO; every stack theory must be phrased as DYNAMIC-DEPTH or POP-DEBT — "non-LIFO lifetime" is a malformed diagnosis.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s24 (Opus 4.5) [D06 ROOT CAUSE FOUND — no code landed, tree clean]
141-probe m3 **120/1/0/22** · m4 **119/4/0/20** at open, **118/4/0/21** at close on IDENTICAL BYTES (1-program m4 oscillation, flaky-test class — a single m4 run is NOT a watermark). SCRIP `d01137ac` (origin, unchanged) · corpus `8be3ef87` (origin, unchanged). Watermark IMPROVED vs s23 by the concurrent ZK-2-FIX/MECH-BUG7 pushes (m3 +9 pass, -9 REGRESSION). C-0..C-5 ✅ C-6 OPEN.
**D06 ROOT CAUSE (s24, MEASURED):** the blob-overhead adjustment at `emit.cpp:989` (IMM; `:988` COND twin) is **STRUCTURALLY UNREACHABLE for the `*VAR` family** — its guard is `_dfr->n_operands > 0 && _dfr->operands[0]`, and a `*VAR` deferred-pattern DEFER carries **ZERO operands** (`dfr_nops=0` measured; the variable name lives in the node's OWN literal, `.S0: .string "P"` in the emitted asm). The block that exists precisely for this hazard never runs. Secondary defect: `_pn` feeds `rt_proc_get_fn`, which cannot resolve at emit time (`proc_startup` has not run) and for `*VAR` is handed a GVA variable name, never a proc name — `_fb=0` on both counts; correct constant is 48 (`emit.cpp:2895`, jcon_value_region=0 floor). Forcing `_fb=48` moved the emitted offset ZERO bytes while the guard stands — measured, reverted.
**TWO THEORIES DIED THIS SESSION:** (1) my own "the cfc arm needs an op_fc_disp adjustment" — FALSIFIED, `fc_disp=-1 zres=1`, the **ZD arm** is live, not cfc; (2) "`_fb` is the blocker" — TRUE defect, NOT the blocker. Full table + stack arithmetic: `FINDING-2026-08-09-CLAUDE-SN4-CLIMB-D06-THE-BLOB-OVERHEAD-BLOCK-IS-STRUCTURALLY-UNREACHABLE-FOR-STAR-VAR-AND-TWO-INHERITED-THEORIES-DIED.md`.
**⛔ CARRY FORWARD:** 112 = 96 (proc `sub rsp,96`) + 16 (`proc_PAT$N_γ` push-pair, never unwound — `proc_PAT$N_res` is unreachable via `ret` on this arm). SAVE's cell is at `[rsp+128]`; IMM reads `[rsp+16]`. First match reads 0 there BY LUCK and passes — **a passing sibling is not evidence the offset is right.** DO NOT assume 128 fixes D06: s23 measured 128 on BOTH probes at `e3ef8f7d` and D06 still failed, so either the offset regressed across HEADs or a SECOND defect sits downstream. Emit the corrected offset, then re-measure `rt_cap_open` saved/cur BEFORE claiming the rung; if `saved=2` persists, the offset class is exonerated a THIRD time — record and STOP.
**NEXT:** (1) read the name from `IR_LIT(_dfr).sval`, drop the `n_operands>0` conjunct for the GVA form, use the 48 floor. (2) Confirm `op_zread[1]` 16→128 in the emitted asm for BOTH D05 and D06 before running either. (3) Re-measure `rt_cap_open`.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s23 (Sonnet 4.6) [D06 DIAGNOSIS IN PROGRESS — no code landed]
141-probe m3 **111/1/0/31** · m4 **118/3/0/22R** — watermark re-proved at open (parallel MECH push added 1 probe to m4 regression count vs s22). SCRIP `e3ef8f7d` (origin, unchanged) · corpus `a09ecb71` (origin, unchanged). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 OPEN. **NEXT:** D06 — re-enter with `rt_cap_open` tracing; the fix hypothesis about the IMM emit path was falsified (see notes below).
**Session notes (s23 Sonnet):** Full orientation: cloned .github + SCRIP + corpus + x64, read PLAN/RULES/GOAL-SN4-ZETA-CLIMB/GOAL-SN4-ZETA-MECH/CORPUS-LOCATIONS/REPO-SCRIP, pulled SPITBOL manual pp.85-88 (deferred eval), pp.121-122 (ARBNO/recursive patterns), pp.203-205 (scanner algorithm), pp.130-133 (EVAL/CODE). Watermark re-proved at open. Investigated D06 for the full session.
**D06 diagnosis (s23):** `SUBJ ? POS(0) *P $ OUTPUT` with P=LEN(1) then P=LEN(3); second match outputs 'c' instead of 'abc'. Monitor (spl scr): exits 0 (all 11 steps agree), stdout diverges. Direct run: `a / =S / c` (scr) vs `a / =S / abc` (oracle). Diagnostic confirmed `op_zres=1` (ZD arm active) for both MATCH_ASSIGN_IMM nodes; both emit `mov eax, dword ptr [rsp + 128]` for ZOPD(1,0) — the ZD planner already correctly accounts for DEFER's blob overhead (same 128 as D05). **The bug is NOT in the ZOPD read offset.** Three fix attempts falsified: (1) `op_fc_disp += 16` — wrong arm (op_zres=1 means ZD fires, not cfc); (2) `op_zread[1] += 112` — broke D05 which was already correct; (3) all reverted, tree clean at baseline. **True root cause not yet found.** Working hypothesis: the bug is in `r14d` (cur_delta) or `saved_delta` value at `rt_cap_open` call time — not in the read address. D05 (single-write seal=2, write-once cell) works; D06 (double-write seal=0, GVA only) fails. The difference between them is purely the seal/cell path in bb_match_defer, NOT the IMM template. Next session must trace `saved_delta` and `cur_delta` at the `rt_cap_open` call site via gdb or fprintf in rt_cap_open itself.
**NEXT SESSION ENTRY:** (1) Add `fprintf(stderr, "[CAP] rt_cap_open var=%s saved=%d cur=%d imm=%d\n", varname, saved_delta, cur_delta, is_imm);` to the top of `rt_cap_open` in `src/runtime/pattern_match.c` (or equivalent). Run D06 and D05, compare the two rt_cap_open calls per program: for D06 the second call should show `saved=0, cur=3` (correct) or something else (the bug). (2) If saved_delta is wrong, trace backward to SAVE node: the SAVE node stores r14d into `[rsp+0]` of its 16B cell; ZOPD(1,0)=[rsp+128] at IMM α — confirm SAVE wrote at [rsp+128] by checking the ZD spine layout at MATCH_BEGIN entry. (3) If cur_delta (r14d) is wrong, check whether the GVA fast-path blob entry corrupts r14d — the blob template reads r14d as start-cursor, does `add r14d, N` (LEN), then γ-returns; if the blob's frame stores/restores r14d incorrectly the second call sees a stale r14d. (4) Note: D05 uses write-once cell (seal=2) fast-path; D06 uses raw GVA path (seal=0) — if the GVA path corrupts r14d and the cell path doesn't, that localizes to the dtp_fn_of call vs cell-read path inside bb_match_defer.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s22 (Sonnet 4.6) [D08-FIX LANDED — LEN(*var) deferred-integer]
141-probe m3 **111/1/0/31** · m4 **119/3/0/21** — re-proved post-rebase at close. SCRIP `e3ef8f7d` (origin) · corpus `a09ecb71` (origin). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 OPEN — D02/D03/D04 heap-exhaust (structural MECH), D06 deferred-pattern wrong-result, ARBNO class (N/G/X families, M-2 BUG-5 in MECH). D07+D08 CLOSED this session. NEXT: D06 (`*P` with P reassigned between matches — monitor exit 0 but stdout diverges; second match `LEN(3)` blob matches 1 char instead of 3; hypothesis is dtp_rcp_tree ival propagation or baked blob frame slot aliasing between first and second match frames).
**Session notes (s22 Sonnet):** D08 root cause diagnosed and fixed. Root: `LEN(*N)` was lowered by `sno_pre_req` with TT_DEFER stripped — coerce ran at statement entry (pre-MATCH_BEGIN) with N=empty→0. Under ζ-cells-on-RSP the coerce result lived on the FORTH spine; by LEN match-time RSP had descended 64B (MATCH_BEGIN+SAVE+SPAN carves), so FRQ(slot+8) read a stale slot causing wrong cursor read at LEN. Monitor bracket step 6/7: both agree N=STRING(2)='12' but scr writes '2' to N (wrong capture from stale LEN count). Fix: in `lower_snobol4.c` TT_LEN, when arg is TT_DEFER, store `"*varname"` in `IR_LIT(nd).sval` instead of calling `sno_pre_req`; new runtime `rt_pat_prim_int(varname)` reads the variable via NV_GET at MATCH time and coerces to integer; `bb_match_len.cpp` calls it at both α and β when op_sval != NULL. D08 XFAIL removed same commit. Post-rebase watermark confirmed: m3 111/31 m4 119/21. SCRIP `e3ef8f7d` pushed.
**Session notes (s21 Sonnet):** Orientation session. Cloned .github + SCRIP (`c09012d5`) + corpus (`ba057fc4`) + x64 oracle. Read PLAN/RULES/REPO-SCRIP/CORPUS-LOCATIONS/GOAL-SN4-ZETA-CLIMB/GOAL-SN4-ZETA-MECH in full. Read SPITBOL manual Ch.7 (unevaluated expressions pp.85-87: plain-name = BUILD-time snapshot; `*name` = MATCH-time fetch) and Ch.18 (pattern-match algorithm pp.203-207: pushdown-stack scanner, cursor, backtrack-pop, &ANCHOR retry) and Ch.19 ARBNO/FENCE/CODE/EVAL formal defs. Ran PARTICIPANTS="spl scr" monitor on D06 and D08.
D08 (SPAN $ N LEN(*N) . FIELD): **clean monitor bracket** — both agree step 6 `N=STRING(2)='12'`; diverge step 7: spl `<lval>=STRING(12)='ABCDEFGHIJKL'` vs scr `N=STRING(1)='2'`. LEN(*N) deferred fetch reads N as '2' (1 char) instead of '12' (2 chars). Descriptor-read offset error in deferred-value resolution: reads slen=1 or truncated pointer at match time.
D06 (*P with P reassigned between two matches): monitor reports all 11 steps agreed (exit 0), but stdout diverges (scr outputs 'c', not 'abc'). SCRIP_DEFER_CELL=0 does not help (cache not involved). SCRIP_PSTAMP_TRACE=1 confirms TWO DTP compilations fire — one per match — so rt_defer_get_pat_fn IS called at match time and IS compiling the new LEN(3) DTP. fn returned correctly in rax. Yet LEN(3) blob matches only 1 char instead of 3. Hypothesis: scanner register (r14d cursor, or the LEN count baking) aliasing during deferred-blob jmp-entry: the SPD-2 slots ([kt-32]/[kt-40]) or the IR_MATCH_LEN ival embedding may be reading a residual from the first match's frame. D06 heap-exhaust was also observed in earlier monitor run (same D02/D03/D04 structural class when GVA cold path recurses) — but PSTAMP run shows second DTP does compile, so heap-exhaust is not the only failure mode here.
NEXT SESSION ENTRY: (1) D08 — trace descriptor layout at `c_rt_defer_open` for the `LEN(*N)` site: print `val.v / val.slen / val.s` when varname="N" at step 7 to confirm whether slen=1 or pointer is wrong. (2) D06 — add fprintf in `bb_match_len` or `IR_MATCH_LEN` template to print ival at jmp-entry time to confirm whether the count register is 1 or 3; also probe whether the issue is in `dtp_rcp_tree` TT_LEN → TT_ILIT → IR_MATCH_LEN ival propagation vs what lands in the blob frame slot `[kt-32]`/`[kt-40]`. MECH M-2 BUG-5 owns the ARBNO regression class (N/G/X) — do not touch until MECH closes it.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s20 (Sonnet 4.6) [CORRECTED POST-REBASE]
141-probe m3 **109/2/0/32** · m4 **110/3/1-flaky/29** — re-proved post-rebase. SCRIP `27a207c2` (origin) · corpus `ba057fc4` (origin). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 CRATER-PARTIALLY-CLOSED — 32 m3 / 29 m4 regressions remain. D05 m4 flaky (XPASS in some runs, REGRESSION in others — kept in XFAIL.compile). NEXT: remaining 32R are D02/D03/D04 (heap-exhaust MECH), D06/D07/D08 (COERCE sequencing MECH-blocked), plus ARBNO ZD arm / ZOPQ offsets class (BUG-2 MECH cross-requests from s19). No CLIMB code commits this session. ⚠ PUSH PENDING — credential needed.
**Session notes (s20 Sonnet):** Parallel Opus s20 landed CLIMB-RSP-FIX (`d2872e69`, β RSP restore), M-2 BUG-3 (`1bbe3370`, zdh_match broadened to all MATCH_BEGINs, 47-probe fix), M-2 BUG-4 (`2829ce8e`, fc_tail_head mirror in MATCH_END). This session: full MONITOR-FIRST diagnosis of the 80R root cause. M-2 HFC-WINDOW expanded sub rsp,32→80 inside bb_match_begin template via x86_zclaim(80), bypassing x86_alpha() — op_zdepth/op_fc_bytes never see the 80B. Every downstream pattern FRQ resolves 80B above its ZLS-allocated slot (overwriting PATCTX saves). Fix attempted: op_hfc_depth persistent field in sm_emit_t, added to x86_frame_off rsp arm. A10 ALTERNATE offset: 192→272 (+80). A10 still crashes — pre-existing 48B sibling-cell deficit (VAR+LIT_INT+ASSIGN_SAVE live cells not covered by op_zdepth, op_flat_disp=0 deleted). Fix reverted cleanly. FINDING written. Key: double-count trap — op_hfc_depth must only go into x86_frame_off (encode time), NOT also into x86_zop string construction (TEXT string is re-parsed and x86_frame_off adds it again).
**Session notes (s20 Opus):** Opened on CAPGEN-ERAD + M-2 HFC-WINDOW crater (m3 61/80, m4 106/33). Root cause: bb_match_begin β reads start_δ via FR(_.op_off)=[rsp+op_off] but pattern nodes push ζ-cells before failing β → RSP shifted → wrong slot read → start_δ never increments → infinite retry → stack overflow → SIGSEGV. Fix: restore RSP at top of β under hfc() via cas_rsp_mark snapshot. Closed 67 m3 + 21 m4 regressions (d2872e69). Then landed M-2 BUG-3 (47-probe fix) and BUG-4. Claimed watermark d2872e69 128/13 was pre-BUG-3/BUG-4 and pre-rebase; post-rebase actual: 109/32.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s19 (Sonnet 4.6)
141-probe m3 **124/1/0/17** · m4 **123/3/0/16** — re-proved at open AND close. SCRIP `0b7a7244` (local, push pending credential) · corpus `bccf1cec` base (unchanged). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 CRATER-PARTIALLY-CLOSED — 17 m3 / 16 m4 regressions remain; see below. MECH cross-requests: (1) ARBNO ZD arm with 288B-aware ZOPQ offsets; (2) post-ARBNO K>0 re-homed cells. NEXT: diagnose remaining regressions (A10, N08/N09/N13/N14/N16/N21, X01/X10, H21); D-family (*VAR deferred) are pre-existing MECH items. ⚠ PUSH PENDING — credential needed.
**Session notes:** Opened on WL-DELETE crater (b455c491, Lon-directed): m3 29/112 / m4 114/25. Two root causes diagnosed and fixed in SCRIP `0b7a7244`:
BUG-1 (MATCH_BEGIN _wzdepth over-count): WL-DELETE admitted MATCH_BEGIN into the ZD run; blob-interior K>0 nodes (LIT_INTEGER for POS etc.) accumulated into zd; STATEMENT_END zgpop=32 instead of 16; ret read type-tag 0x3 as return address → SIGSEGV at 0x3. Fix: zd_mhead/past_mend track depth at MATCH_BEGIN entry; _wzdepth capped at zd_mhead for post-MATCH_END nodes. Closed 86 m3 regressions.
BUG-2 (ARBNO infinite retry): WL-DELETE admitted MATCH_ARBNO (K=16); ARBNO template does sub rsp,288 before ZD arm reads predecessors → ZOPQ resolves inside zero-init 288B frame → r14d=0 each retry → RPOS never satisfied → stack overflow. Post-ARBNO K>0 nodes (LIT_INT-RPOS) emit sub rsp,16 every sigma pass → accumulating RSP skew. Fix: staging choke suppresses g_zd_arm for ARBNO (zon==1 kept for depth accounting); zd_plan tags post-ARBNO K>0 nodes zon[i]=2 (depth-only); choke treats zon==2 as arm-suppressed. Also added zls_g_icn_zframe_gen_by_name stub (77aa7119 reference missing from tree after M-3 merge testing). Closed 9 more m3 regressions → 124/1/0/17.

141-probe m3 **140/2/0/0** · m4 **139/3/0/0** — re-proved at open AND close. SCRIP `e0fb77e7` base + emit.cpp fix (local, push pending credential) · corpus `62393e5b` base + XFAIL.run update (local, push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 PARTIAL — D02 ✅ D03 ✅ D04 ✅ D07 ✅ CLOSED · D06 MECH-BLOCKED (ζ-frame RSP aliasing, filed s17) · D08 MECH-BLOCKED (sno_pre_req pre-chain COERCE vs in-body COERCE, filed s17). NEXT: Lon to route MECH cross-requests for D06 and D08; C-6 cannot gate until both resolved. C-7 ARBNO after C-6 gate.
**Session notes:** Watermark at open: MECH s17 had already landed (BUG-1 PATCTX saves, BUG-2 gpop over-count, D07 RTX collision) — CLIMB cursor was behind. Re-proved 140/2·139/3 from SCRIP `8f97f53d` (MECH s17 regen commit). Diagnosed and fixed D02/D03/D04 m3 empty-capture: ZD planner stages COND's `op_zread[1]` (cross-read to SAVE's cell) without the DEFER blob's runtime stack overhead. Monitor spl vs scr on D02: diverge step 8 (scr reaches END without EN-label output). RSPDIFF confirmed: RSP at COND's α is 112 bytes lower than SAVE's push point (`(48+48+15)&~15 + 16 = 112` for LEN(1) blob). Fix: `emit.cpp` lines 988–989 — add `((48+fb+15)&~15)+16` to `op_zread[1]` (ZD arm) at COND/IMM emit when predecessor is MATCH_DEFER with registered PAT$ blob. Gate: blob size > 0, DEFER with LIT_STRING operand. Byte-identical when no such predecessor. D02/D03/D04 removed from XFAIL.run same commit. FINDING written. ⚠ PUSH PENDING — credential needed.

## ⭐⭐⭐ LIVE CURSOR — [older history pruned per RULES.md §STALE-ORIENTATION — see git log]

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s17 (Sonnet 4.6)
141-probe m3 **135/7/0/0** · m4 **139/3/0/0** — re-proved at open AND close. SCRIP `e0fb77e7` (local, push pending credential) · corpus unchanged. C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 OPEN — D07 ✅ CLOSED · D06 MECH-BLOCKED (ζ-frame RSP aliasing) · D08 MECH-BLOCKED (sno_pre_req pre-chain LEN(*N) reads N before ⒸN fires; fix requires in-body COERCE sequenced after ⒸN IMM) · D02/D03/D04 xfail (heap-exhaust, structural). NEXT: MECH cross-requests for D06 (ζ-frame RSP alias) and D08 (pre-chain COERCE vs in-body COERCE); Lon to route both. C-6 otherwise exhausted for CLIMB this session.
**Session notes:** Watermark at open showed D07 REGRESSION (re-introduced by `55c045eb` SN4-RTX-LEN commit that landed after s16). Diagnosed and fixed D07: `55c045eb` routed `LEN(*N)` through `sx_lower(cx, TT_DEFER_node)` which builds `SNO$MKEXPR` instead of the correct `VAR→COERCE_INTEGER→MATCH_LEN`. Restored `sno_pre_req` path (commit `e0fb77e7`). Watermark re-proved clean. Then diagnosed D06: monitor step 10 PARTIAL-EOF confirmed scr crashes at EN label on second `*P $ OUTPUT`. Two independent defects: (1) OOM/heap-exhaust on cold GVA path (`dtp_fn_of` recursion, same D02/D03/D04 class, MECH structural); (2) SIGSEGV in `rt_cap_push` — slot at `rbp+528` contains `0x300000003` (garbage) because the second match’s LIT_INTEGER does `sub rsp,16; mov [rsp+480],3` which at RSP=`rbp+48` writes to `rbp+528`, exactly the MATCH_ASSIGN_SAVE slot. ζ-dump confirmed: second match SAVE slot at `+528`, LIT_INTEGER result at `+480`. This is a ζ-frame/RSP aliasing defect — the positive-rbp ζ frame overlaps the live RSP store range during match body execution. MECH cross-request filed (M-NEW: ζ-frame RSP aliasing). FINDING-2026-08-08-CLAUDE-SN4-CLIMB-D06-RSP-ZLS-ALIAS-AND-D07-RTX-LEN-REVERT written. ⚠ PUSH PENDING — credential needed. D08 diagnosis: s16 "cross-arm mismatch" theory falsified — `FRQ(op_sa+8)` reads the correct slot (`[rsp+408]` = COERCE result). The value is wrong, not the address. Root cause: `sno_pre_req` places COERCE_INTEGER in the PRE-CHAIN (before MATCH_BEGIN), so it reads N’s value before `$ N` fires. N is null before the match → coerces to 0 → LEN(0) matches null → FIELD=empty. Fix requires COERCE_INTEGER to be an in-body node sequenced after MATCH_ASSIGN_IMM when the `*N` arg is a capture target in the same pattern. MECH cross-request filed. FINDING updated.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s16 (Sonnet 4.6)
141-probe m3 **137/5/0/0** · m4 **132/10/0/0** — both re-proved at open AND close (+2 vs s15 watermark: G24+H20 fixed collaterally). SCRIP `faac2834` (on origin pending push) · corpus `d465cf94` (on origin pending push). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 OPEN — pb_snapshot_imm ✅ G24 ✅ H20 ✅ · D02/D03/D04 xfail (heap-exhaust, structural) · D06 m3 rc=139 SIGSEGV · D08 m3 wrong output (FIELD=empty). NEXT: monitor D06 (PARTICIPANTS="spl scr"; csnobol4 not available in container — use x64 sbl as spl); D08 ZD-arm cross-mismatch diagnosis.
**Session notes:** PB-2 ARMED-PIN COMPLETE. Discriminator: `op_zres` — the three regressions (D10/D11/H21) all had `zres=0` at MATCH_BEGIN (ARBNO body heads: udout=400 but no FRQ reads from THIS node); target `pb_snapshot_imm` had `zres=1`. Found via `SCRIP_APIN_DIAG=1` fprintf sweep comparing g_emit flags at MATCH_BEGIN: pb_snapshot_imm `zres=1 udout=144`; D10/D11/H21 `zres=0 udout=400`. Earlier hypothesis `!op_tail` (ARBNO chain-body heads via fc_tail_head) was falsified: DEFER-bearing ARBNO candidates are DECLINED at late-pass finalize (`fct[c].head=0`), so fc_tail_head returns 0 for D10 by emission time — `op_tail=0` same as target. Fix: added `&& _.op_zres` to the ARMED-PIN arm guard. Collateral: G24 (`FENCE0 *P`) and H20 (`FENCE1 *P`) also have `zres=1` and are now fixed — removed from XFAIL.run corpus commit `d465cf94`. WIP-PARK commit `6899b18f` stays in history (holds `bb_match_end.cpp` body_carve compensation which is still needed). Regen: feature .s — 053/057/cross.s changed (commit `faac2834`). Gates: emit_no_lang ✅; template_medium_invisible --strict pre-existing bb_glue_flat/xa_flat WIP (untouched). D06 monitor: step 10 PARTIAL-EOF, scr crashes at EN label — second match `*P` (P=LEN(3)) SIGSEGV. D08: monitor step 7 diverge — `SPAN $ N LEN(*N) . FIELD` gives FIELD=empty(m3) vs FIELD=ABCDEFGHIJKL(oracle). D08 mechanism: COERCE_INTEGER is ZD-armed (writes ZRES), LEN non-ZD arm reads FRQ(op_sa+8) — cross-arm mismatch when COERCE writes to a different rsp slot than FRQ reads. D06/D08 are C-6 remainder; D02/D03/D04 are structural (heap-exhaust on recursive patterns, not this session's target). ⚠ PUSH PENDING — credential needed.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s15 (Sonnet 4.6)
141-probe m3 **135/7/0/0** · m4 **132/10/0/0** — both re-proved at open AND close, exact s14 watermark held. SCRIP `69c476a0` (on origin) · corpus `e74c0a00` (on origin). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 MECH-BLOCKED (pb_snapshot_imm PB-2). NEXT: PB-2 fix — unstash SCRIP (`git stash pop`), narrow ARBNO-blob exclusion in rpin() (add third guard distinguishing ARBNO body blobs from plain armed outer matches), re-run 141-probe suite zero-regression, commit, then regen ×3.
**Session notes:** Diagnosis-and-partial-fix session for `pb_snapshot_imm` (rc=134 stack-smash, second crash source per s14 cursor). Root cause fully traced to machine level: MATCH_BEGIN is a static-extent armed match (`flat_layout_unknown=0`, Kc=144, no ZWS/ZWR). PATCTX saves use `FRQ(_.op_off + N)`; with GLUE-O pinned rbp (`emit_rec_pin=1`, `flat_deep_arrival=1`) the TEXT encoder classifies the FRQ string as `XK_FR64` and calls `x86_frame_off` which falls to the raw fallback (op_stmt_pin=0, no pin). The fallback returns the raw offset 48; but the ZD hybrid arm then adds `op_flat_disp=48` → `[rbp+96]` landing in the CRT frame above main — stomping the canary. Fix direction (PB-2, stashed): extend `rpin()` in bb_match_begin.cpp with `apin() && op_udout>0 && !flat_layout_unknown && !flat_stmt_frame && !op_zw && !op_zw2 && !flat_jmp_entry`; add body_carve `sub rsp` compensation in bb_match_end.cpp. Both fixes work for pb_snapshot_imm (rc=0 both modes) but 3 regressions remain (D10/D11 ARBNO+DEFER, H21 FENCE+ARBNO+DEFER). The !flat_jmp_entry guard was expected to exclude PAT$ blobs but ARBNO element blobs may not carry that flag. Next session: unstash, add one more exclusion (ARBNO element blobs), verify clean. ⚠ pb_snapshot_imm STILL rc=134 at HEAD — PB-2 stashed, not committed. FINDING-2026-08-08-...-SAVE-OIN-BACK-EDGE and new s15 addendum written.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s14 (Opus 4.5)
141-probe m3 **135/7/0/0** · m4 **132/10/0/0** — both ABOVE the s13 watermark (134/131). SCRIP `912c0dbf` (local; push pending credential) · corpus `bf8ec91e`+regen commits (local; push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ **C-6 UNBLOCKED — D07 CLOSED**. NEXT: `pb_snapshot_imm` PATCTX-saves-in-unframed-corner (second source, MECH territory — see FINDING-2026-08-08-SAVE-OIN) → then C-6 remainder D02/D03/D04/D05/D06/D08 → C-7 ARBNO.
**Session notes:** Opened by re-proving the watermark; **it did not hold** — 10 REGRESSIONS both modes (A05 A06 H08 H10 H14 H15 H23 H26 H28 H29) at HEAD `31a22715`. Bisect: last good `01440ed4` (s13's watermark, exact), introduced by `b491998e` (ZD-5b-LEN). That commit **named all ten in its own message** and diagnosed them as blob contexts re-entering at variable RSP depth = M-2(a)/MECH sequel. **That diagnosis was falsified in one run:** `SCRIP_ZD_DIAG=1` shows **zero `[ZD-H]` events** for A05/H08/H14, so all three parts of the commit (which live inside the `[ZD-H]` branch) never executed for them. Selective revert isolated part 3 (`_xh`) as sole carrier. TRUE ROOT CAUSE: `zd_ud[]` has **two writers with two meanings** — blob-closure arm stores `zdh` (crossing depth), UCLAIM `mem[]` arm stores `K` (claim frame size). The `_xh` site read `zd_ud[zd_uh[i]]` assuming the first; all ten take the UCLAIM arm, so it read `K`, the `>0` gate passed on a non-depth, and `_xh` fired on statements with no cross-head read. Same one-authority class the ZD-2d/2e comment on that very line already names. FIX (`8970c59e`): dedicated `zd_zdh[]`, written only by the blob-closure arm, init `-1`, so `>=0` is a real predicate not a write-order accident. **D07 KEPT, ten restored** — not a trade. XFAIL.compile D07 removed same rung (`bf8ec91e`). Regen ×3 run: `061_capture_in_arbno.s` `[rsp+72]`→`[rsp+312]` (oracle-verified), 3 demo `.s` (all oracle-clean), benchmark unchanged. Gates: `emit_no_lang` ✅, both-medium ✅; `template_medium_invisible --strict` red on `bb_glue_flat/xa_flat` (pre-existing, self-labelled WIP baseline, untouched). **C-6 was never MECH-blocked on this** — de-aliasing two slots is CLIMB-local; the MECH cross-request for D06/D07/D08 is partially void (D07 closed; D06/D08 need re-diagnosis, do not inherit the old theory). ⚠ STILL OPEN, control-built pristine HEAD confirms **pre-existing and untouched by this fix**: `pb_snapshot_imm` rc=134 stack-smash · `pb_stitch_compose` rc=139 SIGSEGV. ⭐ **`pb_snapshot_imm` ROOT CAUSE FOUND s14 (not yet fixed — next session starts here, fix should be small):** gdb `break __stack_chk_fail` → canary fires in **`main`'s own epilogue** (`scrip.c:1747`), i.e. the in-process JIT writes into the CALLER's frame. Emitted asm shows why: `n8_match_begin_α` does `sub rsp,144` and zeroes `[rsp+0..136]` (exactly 144) but then **reads `[rsp+160]`/`[rsp+168]` — 16/24B PAST its own allocation**; then on the success path `n9_match_assign_save_β` emits `add rsp,16` (its own K) **followed by `add rsp,176`**. ZD trace confirms the accounting: statement spine `zout=48` + `Kc=144` = **192**, correctly popped by MATCH_END (`wpop=192`) and STATEMENT_END (`gpop=192`) — but `IR_MATCH_ASSIGN_SAVE K=16` carries **`wpop=176`** (=192−16), a STATEMENT-LEVEL pop on a node that sits INSIDE the live match. At its β port it therefore releases the whole 192B match frame mid-match; MATCH_LIT / MATCH_ASSIGN_IMM / MATCH_DEFER then run with RSP 192B above the live region and every write lands in `main`. **This is exactly the principle ZK-34-FIX (s12) already articulated — "an intra-match ω where MATCH_END is sole release authority" — not yet extended to `MATCH_ASSIGN_SAVE`.** s12's guard suppressed wpop only on **K=0** scanner kinds in ZWS/ZWR windows; SAVE has K=16 so it slips through. Fix direction: suppress the statement-level wpop for ALL intra-match nodes regardless of K, leaving MATCH_END as sole authority. NOTE this REPLACES s13's theory (PB-1s snapshot misclassifying `$ X` capture-target): the `PATV$0` snapshot is CORRECT per manual p.86 — plain `X` in `'A' $ X X` snapshots at BUILD (X='Z'), so the pattern is `'A' $ X 'Z'` matching 'AZ', output `S A` = oracle. Output was never wrong; only the stack was. ⛔ **FOR LON:** `b491998e` landed 10 known-red witnesses without XFAILing them — correct per RULES (no unsanctioned XFAIL), but it left the gate red and the next session inheriting a dead watermark. A known-red landing needs a **park or a revert, not a message** — ruling requested. FINDING-2026-08-08-...-ZDUD-HAS-TWO-WRITERS + FINDING-2026-08-08-...-SAVE-OIN-BACK-EDGE. Second fix (912c0dbf): k>r guard on oin wrong for scan-retry back-edges — MATCH_ASSIGN_SAVE (omega=MATCH_BEGIN) got wpop=176, popped full match frame mid-match. Fix: drop k>r from oin only. Regen x3: 29 .s files changed, oracle-clean. pb_snapshot_imm SECOND crash source: PATCTX saves via FRQ to [rbp+88/96/...] when no frame pushed (Kc>0/nblob=0/zws=0/zwr=0) — rbp is main compiler rbp (0x433fd0=.data), writes hit canary. Fix direction: push minimal rbp or route PATCTX RSP-relative for this corner. MECH territory. Pre-existing at 01440ed4.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s13 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman rc=139 (pre-existing M-2(a)) · xc318 not re-run. SCRIP `01440ed4` (on origin) · corpus `b592b8ac` (on origin). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 MECH-BLOCKED. NEXT: unblock via MECH (three cross-requests below); C-7 ARBNO if MECH resolves or Lon rules C-6 can be partial-gated. ⚠ **pb_snapshot_imm REGRESSION at HEAD — MUST FIX FIRST next session before any other work.**
**Session notes:** Diagnosis-only session — no source changes, watermark re-proved exact. C-6 fully exhausted: every open XFAIL is MECH-blocked or C-7. D02/D03/D04/D05: `*P` deferred → heap-exhaust in `dtp_fn_of → bb_compile_pat_tree_sz` recursing inside live match (confirmed by IR: D02 MATCH_DEFER on PAT$0 hidden global). D06/D07/D08: `LEN(*N)` deferred integer — root cause traced to machine level: COERCE_INTEGER executes before MATCH_BEGIN, writes result at pre-match ζ-spine slot; MATCH_BEGIN allocates 320B match frame; MATCH_LEN reads `FRQ(op_sa+8)=[rsp+104]` which lands inside the zeroed PATCTX block (always 0). ZD not armed because MATCH_BEGIN has no ZD template yet (emit.cpp:1952). TAB(*N) crashes identically (op=89 SNO$MKEXPR has no template in direct-scan position). Fix requires COERCE_INTEGER to execute inside the match body — lowerer structural change, MECH territory. F02/G15: UCLAIM mech2 SAVE frame geometry (FINDING-2026-08-07h class). G24/H20: same deferred-pattern class as D02. N07/N08/N09/N16/N21: m4-only ARBNO+outer-capture crashes, C-7 territory. **NEW REGRESSION: pb_snapshot_imm crashes with stack smashing at HEAD `01440ed4`.** Monitor at step 4: scrip emits `VALUE PATV$0 = STRING(1)='Z'` (PB-1s snapshot firing on capture target) vs oracle `VALUE X = STRING(1)='A'` (correct immediate capture). PB-1s snapshot machinery is misclassifying `$ X` capture-target X as a deferred pattern variable to snapshot. Output 'S A' is correct but stack canary fires post-execution. Bisect: passes at `1e773531` (W-2a), fails at `8e46f05f` (M-1-FIX). Introducing commit is one of: `2308ac59` (X12 JE-PIN) · `4949c7e9` (W-2b: admit pat_static DEFER in zd_wl_kind) · `de837576` (REGIME DELETION) · `5144d4be` (ZK-0/ZK-1/ZK-2/ZK-3-partial) · `76287768` (ICN-FR-3) · `8e46f05f` (M-1-FIX). Best suspect: `4949c7e9` (W-2b) — widened zd_wl_kind for pat_static DEFERs, may cause sno_pat_dfree/sno_name_static to misclassify a capture-target VAR. MECH CROSS-REQUEST 3: pb_snapshot_imm stack-smash — PB-1s snapshot arm in sno_lower_match incorrectly firing on `$ X` capture-target. Watermark: m3 134/8/0/0 · m4 131/11/0/0 · 0 REGRESSION both modes (141-probe gate clean; pb_snapshot_imm is a named witness outside the gate).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s12 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman rc=139 (pre-existing M-2(a)) · xc318 not re-run. SCRIP `01440ed4` (on origin) · corpus `b592b8ac` (on origin). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 MECH-BLOCKED. NEXT: unblock via MECH (two cross-requests below); C-7 ARBNO if MECH resolves or Lon rules C-6 can be partial-gated.
**Session notes:** ZK-34-FIX landed (`484f3965`): the F03/F05/G06/G22/G23 m4 regression is fixed. Root cause: M-1-FIX-3's ZWS backward-edge oin guard (emit.cpp line 2096) was NEVER compiled — it lived inside an unclosed `/* ZD-5b (s24b):` comment from line 2095, making it dead comment text in every build since `71bda272`. This was the reason F03/F05/G06/G22/G23 never actually passed m4. The fix has two parts: (1) close the line 2095 comment properly so line 2096 code is live; (2) extend guard from `zws` to `(zws||zwr)` and add `!ot` arm for NULL ω.node (MATCH_LIT abort-fail path — template-routed, no IR node). Root cause of the null ω.node: MATCH_LIT's ω in a FENCE graph goes to the match_begin abort-fail path, not the scan-retry β; this is template-routed with ω.node=NULL. The fix correctly identifies this as an intra-match ω where MATCH_END is sole release authority. Feature regen: 25 .s files changed (wpop suppressed on K=0 scanner kinds in ZWS/ZWR windows). XFAIL.compile: F03/F05/G06/G22/G23 removed same commit. Watermark: m3 134/8/0/0 · m4 131/11/0/0 · 0 REGRESSION both modes.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s11 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **126/16/0/0** · roman rc=139 (pre-existing M-2(a)) · xc318 not re-run. SCRIP `9e169ab9` (on origin) · corpus `5fb522cc` (pushed). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 MECH-BLOCKED. NEXT: unblock via MECH (two cross-requests below); C-7 ARBNO if MECH resolves or Lon rules C-6 can be partial-gated.
**Session notes:** BUILD-FIX: SCRIP was broken at session open (emit.cpp would not compile). M-1-FIX-3 commit `71bda272` split the ZD-5b one-liner by inserting the new ZWS guard line but left ZD-5b unclosed; orphaned ZD-5b tail appeared after M-1 FIX 3's own `*/` — compiler saw unterminated string literal. Origin independently fixed it as `682016d6`; two further commits landed: ZK-3-COMPLETE (`cffe7507`) + ZK-4-SLICE-1 (`9e169ab9`). ⚠ ZK-3/ZK-4 RE-INTRODUCED m4 regressions: F03/F05/G06/G22/G23 (same FENCE+ZWS crash class as M-1-FIX-3 fixed) now FAIL m4 at SCRIP `9e169ab9`; m3 remains clean. XFAIL.compile updated to restore those 5 entries (corpus `5fb522cc`). Watermark at new HEAD: m3 134/8/0/0 · m4 126/16/0/0 · 0 REGRESSION both modes. ⚠ OPEN REGRESSION at SCRIP HEAD: F03/F05/G06/G22/G23 m4 — ZK-3/ZK-4 introduced this; it must be resolved before C-6 can gate. C-6 diagnosis deepened: `LEN(*N)` is NOT routed through MATCH_DEFER — lower emits `VAR "N"` → `COERCE_INTEGER` → `MATCH_LEN [operand]` chain directly (confirmed via --dump-ir). The `bb_match_len` dynamic arm reads `FRQ(op_sa+8)` or `ZOPQ(0,8)` for `rcx`. Killswitch sweep (20 switches all =0) showed **no killswitch changes D07 output** — failure is in base behavior, not any toggled optimization. NEXT SESSION: (1) Diagnose ZK-3/ZK-4 F03/F05/G06/G22/G23 m4 regression — MONITOR-FIRST, fix, restore watermark. (2) Then MONITOR-FIRST on D07 to bracket LEN(*N) divergence; suspect `op_sa` slot offset or COERCE_INTEGER result cell read timing. D06 SEGV (second `*P` match after P reassigned) likely `g_sno_defer_cells` write-once cache not invalidating — separate after D07/D08.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s10 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman rc=139 (pre-existing M-2(a)) · xc318 not re-run. SCRIP `6396a534` (push pending credential) · corpus `82f127c7` (push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ C-6 MECH-BLOCKED. NEXT: unblock via MECH (two cross-requests below); C-7 ARBNO if MECH resolves or Lon rules C-6 can be partial-gated.
**Session notes:** No source changes this rung — diagnosis only. C-6 assessed: D-family 7/6/0/0 — D10/D11/D12/D13 (ARBNO(*P), recursion, negative control) all green; D02/D03/D04 m3-only heap-exhaust (X12 lazy-compile: `dtp_fn_of` calls `bb_compile_pat_tree_sz` recursively inside a live pattern match → heap exhausted at 512 MB, same class the goal file names; m4 XPASS confirmed); D06/D07/D08 both-mode (LEN(*N) integer-deferred: `rt_defer_get_pat_fn` checks `val.v == DT_P` only — integer N is not DT_P, returns NULL, slow path produces no match; manual p.87 requires type conversion at match time). MECH CROSS-REQUEST 1: D02/D03/D04 — lazy `bb_compile_pat_tree_sz` during live match. MECH CROSS-REQUEST 2: D06/D07/D08 — `rt_defer_get_pat_fn` must coerce integer `*N` to call `LEN(N)` at match time. C-6 cannot gate until both resolved. D12/D13 confirm recursive `*LIST` structural path is sound (manual p.122).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s9 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman rc=139 (pre-existing M-2(a)) · xc318 not re-run. SCRIP `6396a534` (push pending credential) · corpus `82f127c7` (push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ C-5 ✅ CLOSED. NEXT: C-6 deferred-eval — D02/D03/D04 (m3-only crash, X12 signature), D06/D07/D08 (LEN(*N) deferred-int family), roman ≥10 (sealed-DEFER M-2(a)).
**Session notes:** Watermark re-proved at C-5 open: 134/8/0/0 m3 · 131/11/0/0 m4, no drift. C-5 gate sweep: G06/G22/G23 ✅ both modes (our Fix3 recovery confirmed); G24/H20 xfail m3 (C-6 deferred, m4 ✅); H14/H23 ✅ both modes; F-family 6/0/0/0 m3 · 5/1/0/0 m4 (F02 pre-existing UCLAIM xfail); H-family 29/1/0/0 m3 (H20 xfail) · 30/0/0/0 m4; 183/uw2/uw3/1016/152 ✅ both modes. Witnesses 131/141/test_case/func_call/1019/expr_eval/127 — pre-existing failures on pristine origin too (C-6/C-7/C-8 territory), not regressions from our edits (control-built pristine to confirm). C-5 certifies FENCE0/FENCE1 semantics for all non-deferred witnesses. No source changes this rung — certification-only session.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s8 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman not re-run · xc318 not re-run. SCRIP `6396a534` (push pending credential) · corpus `82f127c7` (push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 ✅ CLOSED. NEXT: C-5 — G24 H20 m3 (FENCE0/FENCE1 over `*P`, deferred), H14 H23 (FENCE1-capture); re-prove F-family and G06/G22/G23 green at open to confirm no drift.
**Session notes:** Opened to 6 REGRESSIONs (F04 G05 G09 G17 G18 G21) vs s7 watermark — origin SCRIP was missing two fixes committed but never pushed: (1) ZWS-FENCE0-UCLAIM s5 (bb_match_fence1.cpp ival=0 IF(op_zw,lea) arm — double-release of statement claim under WHACK CONTRACT clause 5); (2) MECH M-1 Fix3 (emit.cpp planner k>r forward-only guard misses backward omega edges under ZWS — oin=0, wpop fires on scan retry, use-after-free on VAR cell). Both root causes independently reconfirmed: SCRIP_FENCE_WHACK=0 killed all 6 fence regressions; ZD diagnostic on G06 showed MATCH_LIT wpop=224 at k=5>r=2. Control: stash+pristine build confirmed F03/F05/G06/G22/G23 crash without Fix2 regardless of Fix1. Re-landed both (C-4-RECOVERY commit `6396a534`). Gate closed: A-family 13/13 m3+m4 ✅ · pb_snapshot_imm + stitch_defer + stitch_snapshot + stitch_compose all ✅ m3+m4 · 0 REGRESSION both modes. Regen: benchmark 3 files, feature 4 files, demo 6 files. XFAIL baseline already correct from prior corpus commit (no changes needed). F02 G15 m4 xfail unchanged (UCLAIM pinned-rbp path, separate class).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s7 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **131/11/0/0** · roman correct 1–9 then blank rc=139 (pre-existing M-2(a)) · xc318 not re-run this session. SCRIP `7ca07e8f` (on origin `466c5334` — push pending credential) · corpus `82f127c7` (on origin `fa3973fc` — push pending credential). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 GATE-READY (A05 A06 fixed; F02 G15 m4 remain xfail — separate UCLAIM path bug, not C-4 gate). C-5 semantics green (non-deferred). NEXT: gate C-4 (A-family 13/13 m4 sweep), advance C-5 gate (G24/H20 await C-6), begin C-6 deferred-eval.
**Session notes:** Fix landed: `x86_zop` regime-4 else arm gains `_m2=(op_mech2_kc>0 && !x86_fc_on())?8:0` — mech2 blob non-own-cell nodes (ASSIGN_SAVE push path, ALTERNATE state slots) now emit `[rsp+off+8]` matching the designed blob addressing `[rsp+8+off]=[α-Kc+off]`. 1-line change `x86_asm.h`. A05 A06 XPASS m4. Regen: benchmark 3 files, feature 19 files, demo 7 files committed. XFAIL.compile: A05 A06 removed (same commit, RULES.md §4). Watermark close: m3 134/8 m4 131/11 both 0 REGRESSION. F02/G15 m4 xfail unchanged — they use UCLAIM pinned-rbp path (`[rbp+208]`), different bug class, not touched. SCRIP `7ca07e8f` · corpus `82f127c7`. FINDING-2026-08-07h and this cursor updated.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s6 (Sonnet 4.6)
141-probe m3 **134/8/0/0** · m4 **129/13/0/0** · roman correct 1–9 then blank rc=139 (pre-existing M-2(a)) · xc318 not re-run this session. SCRIP `466c5334` (on origin `466c5334`) · corpus `fa3973fc` (on origin `fa3973fc`). C-0 ✅ C-1 ✅ C-2 ✅ C-3 ✅ C-4 PARTIAL (MECH-blocked) · C-5 semantics green (non-deferred witnesses). NEXT: await MECH M-2 blob-addr fix (A05 A06 F02 G15 m4) then close C-4; C-5 gate after G24/H20 m3 clear with C-6.
**Session notes:** Watermark re-proved at open against SCRIP `466c5334` (MECH pushed PL-ZK work since s5): m3 134/8/0/0 · m4 129/13/0/0 — exact match, no drift. Full C-4 partial-gate sweep: A-family m3 13/13, m4 11/13 (A05 A06 xfail). C-5 witness sweep: G06 G22 G23 H14 H23 all ✅ both modes; G24 H20 xfail m3 (FENCE0/FENCE1 over `*P` — C-6 deferred, not C-5). C-5 semantics certified for all non-deferred witnesses. Diagnosed A05/A06/F02/G15 m4 SIGSEGV class: mech2 blob ASSIGN_SAVE (`rt_cap_push` path, `fc_bytes=0`) emits `lea rdi,[rsp+op_off]` missing +8 from `push rbp` mech2 boundary; `op_flat_disp=0` (CARVE-DATA-ERAD), `op_zdepth=0` (no own carve), `x86_fb_data()=false` (`op_fb_rbp=0` for blob-interior nodes), `zvo_resolve` skipped (mech2 suppresses UCLAIM); m3 survives by main-stack slack. Fix path: `op_fb_rbp=1` for blob-interior SAVE nodes (option 1) or `fc_geom` grant for SAVE (option 2). FINDING-2026-08-07h written. MECH cross-request filed.



## ⛔ METHOD (binding every rung)
1. Rung gate = every witness green in BOTH modes BY SET vs oracle `.ref`; crash detection is wait-status (SIGSEGV handler swallows — run_suite.sh does this; ad-hoc runs must check rc). 2. MONITOR-FIRST: divergence bracketed between last-agree and first-diverge trace events BEFORE reading code; monitor dark for the mode ⇒ MON-RE first (known defect B: stno counting — `build_stno_map.py` canonical). 3. Fix at the bracketed site; re-run monitor, divergence must MOVE. 4. XFAIL removal same commit; NEVER add to XFAIL except a Lon-ruled park. 5. Timeouts 8s unit / 30s corpus; `< /dev/null` on scrip calls; grep error: both build logs. 6. m3-only defects: suspect the m3 glue stubs first (X12 lesson: one-shot push-of-zero classes — FI8 lazy-init, PS-1b twin driver:1554, first-touch trampoline bakes; m4 clean = emission exonerated). 7. SPITBOL semantics per manual (SCRIP FOLLOWS SPITBOL; case-sensitive); manual anchors inline per rung below.

## RUNGS (stop at first fail; a rung already green just gets its checkbox and a HEAD-stamp)
- [x] **C-0 RE-PROVE + xc318 POST-FLIP FLOOR** — ✅ 2026-08-08 s1. m3 124/18/0/0 · m4 116/26/0/0 · xc318 m3 286/31 · m4 268/48/SKIP=1/DIVERGE=21 (setarch -R). Floor recorded. SCRIP c0372fec · corpus f65d8278.
- [x] **C-1 VALUE SPINE: 1+1 → arith/assign/concat** — ✅ 2026-08-08 s2. All crosscheck arith_new + A01–A04 + 023_arith_add pass m3+m4. SCRIP 29f89dd5.
- [x] **C-2 CONTROL** — ✅ 2026-08-08 s2. 031–037 + indirect :($X) + 1020_code_label_transfer pass m3+m4. -NOFAIL fixed (error 35, rc=1, no fallthrough). SCRIP 29f89dd5.
- [x] **C-3 STATIC PATTERNS** — ✅ 2026-08-07 s4. LIT ANY NOTANY SPAN BREAK BREAKX LEN POS RPOS TAB RTAB REM BAL ARB all green m3+m4. &ANCHOR (scan-retry law) green. L-family 20/20, crosscheck 039–049, W05/W06/W07, BAL 135–137/174–177, 045_pat_rpos, 098_keyword_anchor. SCRIP 29f89dd5 · corpus 6f5e8102.
- [x] **C-4 ALTERNATION + CAPTURES** — ✅ 2026-08-07 s8. A-family 13/13 m3+m4. pb_snapshot_imm + stitch_defer + stitch_snapshot + stitch_compose all green m3+m4. m3 134/8 · m4 131/11, 0 REGRESSION. F02 G15 m4 xfail (UCLAIM pinned-rbp path — separate class, not C-4). SCRIP `6396a534` · corpus `82f127c7`.
- [x] **C-5 FENCE0 / FENCE1** — ✅ 2026-08-07 s9. G06/G22/G23 ✅ both modes; H14/H23 ✅ both modes; G24/H20 xfail m3 (C-6 deferred). F-family 5+1xfail m4 (F02 UCLAIM pre-existing). SCRIP `6396a534`.
- [ ] **C-6 DEFERRED EVAL + RECURSION** — ⬅ **s25 MEASURED RESET:** D02/D03/D04 ✅ · D07 ✅ · D08 ✅ · D09 ✅ · D11 ✅ · **D06 m3 ✅ (s26 SLICE-1 collateral)** · D06 m4 OPEN (OPS-2 gated). **⭐ s26 UPDATE: RSP-aliasing MECH cross-request (s17) RESOLVED by SLICE-1 — negative-rbp anchor slots cannot overlap positive-direction RSP stores; do NOT re-file.** D09/D11 closed as SLICE-2 collateral. Open crater = D06 m4 only (stdout-only class, monitor dark, S23 ANTI-PATTERN in force). Entry: s24 carry-forward (IR_LIT sval path + 48 floor + rt_cap_open re-measure) once OPS-2 lands. Blob sharing PROVEN: D13 `*LIST` recursive mints exactly 2 blobs (rc=0, oracle output — fixed by the s19–s28 landings; this paragraph had outlived its facts). Blob sharing PROVEN: D13 (`*LIST` recursive, nested failing subject) mints **exactly 2 blobs** total (`SCRIP_PSTAMP_TRACE=1` census) — one per pattern object via the `h->fn` memo at `pattern_match.c:94`, shared at every recursion depth; ω/γ memory rides the standard unwind/whack laws (Lon model, running). D10/D11?/D12/D13 recursion ✅. **OPEN = D06 D09 D11 only.** D06 root cause (s24 FINDING) stands, with an s25 SHAPE RULING: the blob-overhead adjustment CANNOT be an emit-time constant — the `*VAR` blob is runtime-minted (`dtp_fn_of`), its frame size is runtime data, so any static `op_zread/op_zdepth += K` is malformed under MECH's LIFO-EXTENT/DYNAMIC-DEPTH law (s23's +112 conviction). **Route: depth-immune read — the FENCE1 precedent one case below (rbp-anchored watermark quad "read at the dynamic post-P depth BY DESIGN") applied to the capture-after-DEFER slot; MECH cross-request (structural), witnesses D05+D06 swap-paths first per ANTI-PATTERN §3.** `*name`/`*expr` = match-time fetch (manual p.86; stitch witnesses MUST STAY GREEN); crater line reduced accordingly: **roman ≥10 (M-2(a)) · D06/D09/D11.**
- [ ] **C-7 ARBNO + NESTING** — **⭐ s26: N08 N09 N16 H31 CLOSED (SLICE-1 collateral).** Remaining open: N21 (sibling ARBNO capture — dc_sib_bt witness committed, defer-β re-yield entry point), H29 (CRASH, gdb-reachable), H21 (FENCE1+deferred-ALT — probe before inheriting D-family gate). Nested-ARBNO class **H24 H25 X02 X06 X11** (sno_seq_nary second-pass catch-all, immune to ZD switches — lower-side) · X03/X05 nesting law (legacy-chain outer requires net-zero delta — dies with M-5 chain deletion) · nested-ARBNO ZLS geometry (FINDING-08-05 s9/s10 rsp3 root cause).
- [ ] **C-8 FUNCTIONS** — DEFINE/RETURN/FRETURN/NRETURN (NRETURN+defer manual p.~130), recursion (fib), OPSYN, APPLY, ARG; value/defer-in-proc (X12 class — m3 glue); func_call benchmark `result:` correct; call2bb parked items (SCRIP_CALL2BB default 0 — folds at MECH M-5 only if measured). Witnesses: 085/086/087, uw2/uw3, test_stack, 209.
- [ ] **C-9 REPLACEMENT + SPLICE** — 5-stage stmt execution stages 4/5 (Lon ruling in HQ): build replacement, splice; TAB-binds-subject trap manual p.143 #10; capture-replacement queue item; replacement-splice PENDING LON RULING (sole-consumer fence retirement) — surface it here if the rung blocks.
- [ ] **C-10 DATA + KEYWORDS** — ARRAY/TABLE/DATA/field, ITEM, SORT/RSORT, CONVERT; &STLIMIT &DUMP &PROFILE (monitor infra), &FENCE primitive, &ANCHOR. Witnesses: data family, 131/141/183 (131 = ex-PATREF 528B UCLAIM customer — must be clean post-M-3), 066, W04_arbno_*.
- [ ] **C-11 EVAL / CODE / EXEC (the summit construct)** — EVAL(string/expr), CODE(string) → code object executed via direct goto `<code>`, SETEXIT, CONVERT to EXPRESSION; the eval-family falsification controls **1016 1019 161 expr_eval MUST HOLD** (named gatekeep class); eval_fixed rc=0 witness; S233/S234 deferred-eval-family merge-defect findings are the map (γ success-code blocker, not ω).
- [ ] **C-12 SUMMIT GATES** — beauty_suite 19 drivers vs refs (beauty.sno itself is DESTINATION not gate) · xc318 BY SET ≥ 280/260 BOTH modes · bench 18/21 hold-or-better · rc=139 tail zero · 127/152 env-pad IMPOSSIBLE (post-M-6 a flake is a LEAD) · armed roman full MDCCLXXVI · regen ×4 · FINDING + both cursors + MECH DoD cross-check.

## WITNESS SET (report every session): probe suite both modes · roman · pb_snapshot_imm + stitch trio · uw2 uw3 · 131 141 183 066 · 045_pat_rpos · eval controls 1016/1019/161 · eval_fixed · func_call · test_case (honest wedge) · 127/152.

## COORDINATION
MECH owns structural edits; this ladder NEVER lands a new frame/glue/claim protocol — it files the cross-request and blocks the rung on MECH's cursor. Both re-prove the shared watermark at open/close. Icon: after ANY shared-file edit, run the 303-emission content-hash sweep per MECH's procedure. `.github` pushes last; handoff per RULES (regen scripts if codegen touched; handoff_status.sh verbatim).
