# GOAL-HQ-VALIDATE.md — HQ-VALIDATE (hq_V), opened 2026-09-06 20:08 CDT

## THE MANDATE
Lon 2026-09-06, in-chat to ceo, verbatim: *"Let's do open up Pascal for development. So we'll have SNOBOL4, Icon, Prolog, and Pascal."* (19:5x) · *"Let's think of one more HQ to fire up. What would be a good name/concept? HQ-*?"* (19:5x) · *"I created /home/claude_V for you to populate."* (20:00). The ceo's recommendation that sitting (GOAL-CEO CEO-369 addenda): HQ-VALIDATE owning the Pascal lane whole, because Pascal became the fourth language in Lon's order with 178 package reds (PAT 127 + FPC 51) and zero fixers while hq_P carried SNOBOL4 · Snocone · Pascal · the B cell for all seven alone. The name is the PAT suite's own: it is the ISO 7185 VALIDATION suite, and 100% means 100% of ISO 7185 as that suite states it (Lon 2026-09-03: *"It means 100% of the industry standard language."*).

## THE LANE (MASTER-PLAN § THE LANES, nine HQs)
hq_V owns PASCAL WHOLE: M the master `corpus/tests/pascal/` (PasM 248/251 at opening) · L the ISO 7185 construct ladder (`test_pascal_ladder.sh`, `config/LADDER.tsv`) · V `packages/pascal/fpc_tests` (FPC 130/181, `test_pascal_fpc_suite.sh`) + `packages/pascal/pat` (PAT 300/427, `test_pascal_pat_suite.sh`, THE Pascal denominator) — every Pascal class from `src/parsers/pascal/` through `src/lower/lower_pascal.c` to the runtime sinks Pascal reaches. The B cell (benchmarks, rivals grid, `bench_triangulate_pascal.sh`) stays hq_P's. Oracle and rival: `fpc -Miso` (FPC 3.2.2). The 44 Pascal rows in QUEUE.tsv were re-laned hq_P → hq_V at the opening (LANE REVIEW lines in each baton); the two rivals/bench rows stayed with hq_P. Seats under OCTET: none (the fleet is shut down).

## LAWS THAT BIND EVERY hq_V LANDING (compact; RULES.md is the parent)
- Under OCTET an HQ MEASURES AND CURES in its own lane: one package program (one bug) in hand at a time, cured, measured through the suite's own runner, flipped to `coo`, next. Rule 5: one board per tree, cite the suite table before you run.
- A shared-node change is co-signed by hq_U and passes THE CONTROL-ARM BAR (ceo-359) on every other frontend, same corpus, clean comparison tree named, every tolerated red named with its row.
- Every suite run rewrites its SCORE.md row and SUITES.tsv line through `util_score_row.py` in the same sitting and pushes; THERE IS NO XFAIL; a DONE-WHEN reads the printed denominator and is graded on origin/main; OUR FILES ARE LF; no `-O2`; no pristine build; zero comments in C; artifacts regenerate in the landing that moved emission.
- Drain your inbox at every prompt boundary: a ruling unread is not in effect. Asks to `ceo`, flips to `coo`, one paragraph, `--stdin` with a quoted heredoc, no backticks.

## RULINGS (ceo) — the standard governs; fpc is the output oracle and the rival

- **CEO-375 (ceo, 2026-09-06 20:41 CDT) — THE AUTHORITY RULING, on hq_V's HQV-1 ask (ceo/inbox 20:37):** ISO 7185 AS PAT STATES IT is the conformance authority for the Pascal lane over BOTH populations — what must be rejected and what must be accepted. `fpc -Miso` (FPC 3.2.2) is the OUTPUT oracle for the acceptance population and the performance rival (CEO-33), never the authority on what is an error. Where fpc is laxer than the standard (it ACCEPTS iso7185prt1805, 1806, 1808 and 1811 of the eight clause 6.8.3.9 for-statement tests), SCRIP refuses per the standard; a PAT green that is STRICTER than fpc is conformance, not a defect and never a regression, and every such divergence is named in the row's receipt as `PAT <id>: ISO rejects · fpc accepts · SCRIP refuses` so no later reader re-litigates it as an fpc mismatch. Grounds: Lon 2026-09-03 20:45 (*"It means 100% of the industry standard language."*), the postoffice ANNOUNCEMENT.md industry-standard table naming PAT as THE Pascal conformance suite, CLAUDE.md (PAT is THE Pascal denominator). Lon may overrule in chat; the ceo relays this ruling to him the same turn. TWO SMALLER RULINGS in the same telegram: (b) the one-fraction `--text` per mode the ceo asked for in HQV-0b is NOT OWED — hq_T's declared-pair mechanism (CEO-363) short-circuits the text parse, and hq_V's fraction-shaped crash diagnostic (`crash m3 N, m4 N`) stands; (c) the Pascal package pair wires to the mode the published row NAMES — both rows read m3 (the coo's hand-set), so hq_V's rewire from hq_T's m4 to m3 STANDS and m4 earns its own reading before any row carries it; a published mode flip without a board is exactly the 14-program phantom hq_V avoided; hq_T was told by hq_V. Record: `.github/GOAL-CEO.md` CEO-375; the baton `pascal-pat-for-loop-control-variable-protection` carries the ledger line.

## LIVE CURSOR
- **HQV-40 (hq_V, 2026-09-12 08:1x–09:0x CDT, SCRIP `f008879f8`, corpus `7923b3c60` → +1, MODE NONET, CEO-586 correction + CEO-547) — A BOARD SAT IN THE BLOCKING SET AND REFUSED IN ONE SECOND FOR TWO DAYS, AND THE RULING THAT FIXED IT HAD ALREADY BEEN MADE.** The cfo's 147-arm census found `test_gate_icon_master_per_entry_identity` REFUSING rc=2; the ceo handed it to me as the best hiding place of the three. **MEASURED: it exits rc=2 in 1.0 s** — it runs the 800-entry Icon master, and ONE RUNNER, ONE BOARD (CEO-523, 2026-09-10) refuses a corpus-rooted population to every seat but the coo, from `_one_runner_guard` at the top of `cmd_run` before one entry is graded. So from 09-10 to 09-12 a `WIRED` gate in `make test` asserted **nothing**, accurately reporting its own death every run. ⭐ **THE COMPARISON WAS NEVER WEAKENED, AND I PROVED IT RATHER THAN CLAIMING IT:** the one runner had already boarded THIS EXACT TREE (coo, scrip `f008879f8`, 13:13Z, 1765 rows in the live progress table); fed its own record through the gate's documented `ICON_IDENTITY_MEASURED_FROM` arm — no board re-run, no override — it grades `examined=1669 regressions=0 vanished=0 improved=6 new=96` at rc=0 in ~2 s, and `FAIL_ONCE=1` reds it at rc=1 naming `call_through_a_static_variable_reads_the_static`. ⛔ **I DID NOT RE-DECIDE THE RULING — I EXECUTED ONE THAT NEVER LANDED. CEO-547 (ceo, 2026-09-11, on the cfo's ECONOMY finding): "`make test` WIRES A BOARD, so it comes OUT of the blocking set and moves to the coo's pass."** The landing went to the cto and did not happen. Now landed: out of `make test`, into `make test-boards`; `gate_wiring.tsv` `WIRED` → `RULING` through `util_gate_wiring.py declare`, with a reason and a declarer. DONE-WHEN `SCRIP/scripts/donewhen_icon_identity_gate_measures.sh`, 12 arms, **no population literal**, asserting what the refusal SAID and not merely its code (the ONE-RUNNER refusal is also rc=2) — **proven RED on the pre-cure tree, GREEN after, and UNPROVEN(2) on a stale binary**, that third state found the hard way when a `stash pop` refreshed the Makefile mtime and arm 9 called "rebuild first" a regression. Record: `FINDING-2026-09-12-hq_V-a-board-in-the-blocking-set-refused-in-one-second-and-read-as-a-wired-gate-for-two-days.md`. ⛔ **NAMED, NOT MINE:** `util_gate_wiring.py check` is rc=1 on this tree AND byte-identical rc=1 on the clean tree (`wired +0 · on-disk +6`, measured by stashing my two files) — six gates other seats landed on disk, unwired and undeclared; and `test_gate_icn_rundir_contract.sh` REFUSES rc=2 on the clean tree because `corpus/tests/icon/ALL.argv` declares `procedure_every_suspend_replace_8` while sitting beside `ALL.icn` (a copied sidecar arming the wrong program). Both pre-existing, both told to ceo/coo. **THE GENERAL FORM: two rulings can each be right and still kill an instrument where they meet, and the casualty is silent — the cheap detector is the cost estimate, a gate whose comment says `~90s MEASURED` and returns in one second is either cured or dead, and the difference is one `time`.**
- **HQV-39½ (hq_V, 2026-09-12 08:2x CDT, corpus `7923b3c60` → +1, CEO-586 as ruled) — THE 60-BYTE `rung36_jcon_io.stdin` IS DELETED, WITH THE TWO ARMS AS THE RECEIPT.** ⛔ ARM A, cmp-proven truncation: the 60-byte sidecar beside the `.icn` is byte-for-byte the **first 60 bytes** of the 130-byte `config/rung36_jcon_io.stdin`, which is itself byte-identical to the entry's own `.fixtures/io.dat` AND to upstream `packages/icon/jcon_tests/io.dat`. ⛔ ARM B, measured through the harness's own `run_m3`/`run_m4` (not re-implemented): **both companions PASS in BOTH modes** against the entry's `.expected` — so the ref cannot choose between them, and **the ambiguity is the defect**. The entry's `.argv` names `io.dat`; the upstream contract feeds the SAME file on stdin and argv. After deletion `icn_rundir_stdin` falls through to the 130-byte `config/` copy — re-resolved live, and it is byte-identical to the `io.dat` the `.argv` names, so stdin and argv are now one file as upstream intends.

- **HQV-39 (hq_V, 2026-09-11 18:5x–19:3x CDT, SCRIP `91394217f` → `f008879f8`, corpus `5f06f3f49` → `7923b3c60`) — THE HOLD-OUT WAS ON A CAPABILITY THAT HAD ALREADY LANDED, AND CLEARING IT COST TWO CORRECTIONS I FOUND BY LOOKING AT WHAT MY OWN RULE PROPOSED TO DO.** `next` served this seat nothing (every hq_V row is Pascal, parked under ICON-ONLY; it will not hand over another seat's row), so I worked my standing duty — CEO-452, the one writer of the Icon master pair. ⭐ **(1) THE PARK NAMED A MISSING CAPABILITY THAT EXISTS.** The builder excluded EVERY stdin-bearing plain program with *"stays as files until the stdin-sections format extension lands"*. It landed: the master pair ships `ALL.in`, and **this master was already grading 20 stdin-fed entries while that park went on turning the rest away.** `corpus/tests/icon/KEEP.md` names this class in its own words — *a rationale that names a MISSING CAPABILITY needs a RE-MEASURE date, not just an author date* — and this is the second time this month it cost that file real work. ⛔ **THE GUARD THE PARK PROTECTED IS REAL AND STAYS:** absorbing a stdin-reader UNFED pins a PREFIX of it as its expectation. The cure is to CARRY the input, not to refuse the program; what still refuses is input that cannot be carried FAITHFULLY, and that refusal is `loose_stdin_companion`'s own — AMBIGUOUS and non-UTF-8 — asked through the grader's finder, never a local list. The xfail probe is FED what the entry will be fed, or its verdict is about a different run. ⭐ **(2) `rung36_jcon_others` IS ABSORBED, FED, AS `procedure_every_scan_replace_16`.** Four cells measured BEFORE the declaration was written: the shipped `.expected` IS the fed Arizona icont run byte-for-byte (183 lines), a STARVED oracle run gives 130 and loses 53, and scrip FED reproduces the ref exactly in m3 AND m4. **Then proved fed THROUGH THE MASTER'S OWN MECHANISM, both ways: graded from `ALL.in` it is PASS/PASS, and with that sidecar removed FAIL/FAIL** — so the hold-out's fear was real and carriage is its cure. Control arm body-to-body, never by name count: `ALL.icn`/`ALL.ref`/`ALL.in` each gained EXACTLY this one entry, **zero bodies changed, nothing removed**, new body and ref byte-identical to no existing entry, and the tracked build byte-identical to the scratch arm verified before it. ⭐ **(3) BLAST RADIUS MEASURED, NOT REASONED, because the builder is shared by seven languages: 17 loose sources fleet-wide have a stdin companion (icon 11 · pascal 5 · rebus 1), 2 still refuse; and pre/post builder arms on scratch copies for icon, pascal and rebus moved ZERO entries in any of the three.** Eligibility changed; absorption did not. Nothing moves in another lane until that seat retires a declaration. ⛔⭐ **(4) FIRST CORRECTION — I HAND-EDITED A GENERATED FILE AND THE RIGHT GUARD REFUSED ME.** The stale `KEEPER` row survived absorption because the sidecar merge never deletes, so I deleted the line by hand; `_excl_guard` then REFUSED the builder rc=2 (CEO-545: this file is GENERATED, a retraction is made where the exclusion is DECLARED and the file is REBUILT). **The guard was right and I was wrong about the mechanism** — the builder now performs the retraction itself, and the row rewrote itself truthfully with no hand edit at all. ⛔⭐⭐ **(5) SECOND CORRECTION, AND THE ONE WORTH KEEPING: MY FIRST RETRACTION RULE PROPOSED TO DELETE 191 ROWS INSTEAD OF 31, AND I FOUND IT BY READING WHAT IT WOULD DELETE RATHER THAN BY TRUSTING THE RULE.** *"The family is in the master"* does NOT mean *"this row is stale"*: **153 of those are `parser_*` loose pairs that are deliberate KEEPERS in `parser/KEEP.md` while same-named entries were absorbed from elsewhere** — both readings true at once, the origin-keyed-guard trap this corpus already paid for. The rule now needs TWO conditions: in the denominator AND not re-emitted by this run's freshly computed exclusions, so every live keeper re-emits itself and only a declaration that is actually GONE is retired. The 31 retired were verified one by one (26 "already in the master" notes whose loose pairs are deleted, and the 5 root copies KEEP.md itself records as deleted duplicates — confirmed absent by `ls`). ⭐ **(6) AND THE GATE I WROTE YESTERDAY WAS BOTH WRONG AND WEAK.** Its assertion was *"the builder refuses it"*, which this cure inverts, so it moved to *"the builder CARRIES it into `ALL.in` byte-for-byte"* — **a strengthening, because the old form was satisfied by a refusal for ANY reason.** Then MUTATING the spelling list (not reading the file) showed the gate draws its fixtures FROM the list it tests: deleting `.stdin` and `.input` took it from 12 checks to 4 and **it still printed a green tick**. It now carries a POPULATION FLOOR and that mutation is **rc=2 REFUSED**. ⚠ Named, not chased: `test_gate_orphaned_witnesses_do_not_grow.sh` is red on snobol4 (40 against floor 39) on a committed file this seat never touched, and SNOBOL4 is parked under ICON-ONLY.\n- **HQV-38 (hq_V, 2026-09-11 18:4x CDT) — THE io STDIN AMBIGUITY IS MEASURED AND ROUTED, NOT PICKED, BECAUSE THE MEASUREMENT CAME OUT THE WAY THAT MAKES IT A RULING.** HQV-34 item (3) named it and called it *"a decision for its author"*; I said today that was half wrong, because **which file is a program's input is answerable by the ORACLE** — and then ran the arms rather than assuming which way they would fall. ⛔ **THE OUTPUT CANNOT SEPARATE THEM.** Two scratch arms differing only in the fed file, our own m3, the entry's own `.argv` (`io.dat`): **both the 60-byte `tests/icon/rung36_jcon_io.stdin` and the 130-byte `tests/icon/config/rung36_jcon_io.stdin` reproduce `rung36_jcon_io.expected` byte for byte**, and on Arizona icont both also reproduce the package's own `io.std`. The program's reads never reach the five lines where the two files differ. **So I told the ceo it is a ruling, exactly as I had undertaken to if both reproduced** — changing that answer after the arms landed would be the shape this fleet punishes. ⭐ **WHAT I DO HAVE IS A CONTRACT AND A PROVENANCE, and I recommended on them rather than merely reporting:** the entry's own `rung36_jcon_io.fixtures/io.dat` is 130 bytes and **byte-identical to the config companion**; the entry's `.argv` declares `io.dat`; and upstream's contract, which this suite and `test_icon_arizona_suite.sh` both reproduce, runs `prog file.dat < file.dat` — **the same file on argv and on stdin**, so feeding the 60-byte file hands the program a different byte stream than the file its own argv names. `cmp` says the 60-byte file is a **TRUNCATION** of the other (EOF after byte 60, line 8), not an independent cut; the config copy arrived with the flat-end-state move and the loose copy was added later by a commit believing the ref could not be reproduced without it — a belief these arms show was already satisfied. ⛔ **THE AMBIGUITY IS ITSELF THE DEFECT:** `loose_stdin_companion` REFUSES rather than picking, correctly, so **`io` cannot be graded fed by any path today**, and one deletion gives the cell back with no expected byte moving in either direction. **I have not touched the file.**\n- **HQV-37 (hq_V, 2026-09-11 18:3x CDT, corpus `5063dcfb1` → `5f06f3f49`) — THE +1 ON hq_T's RATCHET WAS MY ABSORPTION, SO THE DECLARATION WAS MINE, AND IT IS CUT FROM THE ORACLE AND NOT FROM WHAT MAKES THE GATE GREEN.** Taken the moment the progname row closed, one bug in hand. ⭐ **hq_T WAS RIGHT TO REFUSE THE RE-PIN and I told it so:** argv 35 against floor 34 with stdin at 50 against 50 is GROWTH, and pinning upward would launder a new debt into the floor, which is the one thing the growth arm exists to stop. The +1 is icon entry 925 `procedure_every_suspend_replace_8` (origin `rung36_jcon_genqueen`), absorbed by me, and absorption is single-writer and mine (CEO-452). ⛔⭐ **IT IS THE read_argv_sidecar DOCSTRING'S OWN WITNESS, WORD FOR WORD:** the program reads `n := integer(args[1]) | 6`, so with no declaration it scored **PASS in both modes while the argument handling it exists to exercise never executed** — *a criterion that is never evaluated looks exactly like one that was satisfied.* ⭐ **MEASURED ON THE ORACLE BEFORE A LINE WAS DECLARED** (Arizona 9.5.25a, the entry's origin, three invocations): **no arguments → byte-identical to the shipped `genqueen.std`** (so the ref was cut argument-free), **the argument 6 → byte-identical to that**, **the argument 4 → 20 lines against 56**. `args[1]` IS LIVE and the default was merely being taken, so declaring `6` **changes no expected byte and makes that path actually run** — the honest declaration, not the convenient one. ⭐ **THREE ARMS THROUGH THE REAL GRADING PATH**, one entry, both modes: declared `6` → **PASS/PASS**; declared `4` → **FAIL/FAIL**, which is what proves the declaration REACHES the program rather than decorating the tree; no declaration → PASS/PASS, the silent green this closes. **The ratchet reads argv 35 → 34, at its floor, GREEN WITH NO PIN CHANGE**, re-proved on the clean pushed tree. ⛔ **AND THE FIRST DRAFT OF THE FILE WAS WRONG IN A WAY ONLY COUNTING CAUGHT:** I wrote the receipt as a trailing `# …` field on the row, and **every field after the name is an ARGUMENT** — the program would have been handed `#`-prose as `argv[2]`, silently. Found by counting the parsed fields (3, not 2) instead of re-reading my own line; the receipt now sits on its own comment line ABOVE the row and the file's header says why. ⚠ **VERIFIED, NOT ASSUMED, BEFORE LANDING:** `read_argv_sidecar` REFUSES a family graded `ast`-only, and **every caller that grades the icon master passes `--modes m3,m4`** (`board_icon_master.sh`, `test_gate_icon_master_per_entry_identity.sh`, `util_cross_language_regression_diff.sh`) — so the new sidecar cannot refuse anyone. ⚠ `test_gate_icon_master_per_entry_identity.sh` returns **rc=2 UNPROVEN, not red**: it is a master board and the one-runner guard refused it to this seat, which is the instrument working.
- **HQV-36 (hq_V, 2026-09-11 18:0x–18:4x CDT, SCRIP `d7766980e` → `91394217f`, corpus `0ad71b7b6` → `5063dcfb1`) — CEO-581 LANDED AS THE PER-MODE REF, AND IT TURNED UP THAT THE PIN IT RESTS ON WAS TRUE OF THE LINE AND FALSE OF THE RUN.** Inbox drained (3: hq_T, hq_I, ceo — read, acted on, answered, cleared). FINDING: `FINDING-2026-09-11-hq_V-a-source-level-assertion-is-not-a-measurement-of-behaviour.md`. ⭐ **(1) THE RULING EXECUTED, AND THE PAIR MEASURED BEFORE EITHER FILE WAS WRITTEN.** `<stem>.moderef` beside `<stem>.ref` — `entry<TAB>mode<TAB>ref-line<TAB>mode-line<TAB>receipt` — read by `corpus_suite_harness.py` and by the bash runners through `util_apply_moderef.py`, **a shim over that same reader and never a second implementation** (the defect this lane cured one day earlier, when one question had three written answers). On the entries themselves, Arizona 9.5.25a: `icon procedure_every_alt_replace_4.icn` → the stored ref byte-for-byte; `icont -s …; ./procedure_every_alt_replace_4` → `   &progname: ./procedure_every_alt_replace_4`; the same pair for jcon `kwds` at line 45. ⛔ **CEO-581'S GUARD IS WIRED, NOT PROMISED:** the receipt column is mandatory and an unreceipted row REFUSES rc=2, and the gate **re-runs the oracle under both invocations every time it is invoked**, refusing rc=2 if icont is unreachable. Nine refusals proven red on purpose (4 fields · wildcard · unknown mode · no-op substitution · absent ref-line · ambiguous ref-line · majority · no receipt · a named sidecar that does not exist). ⛔ **IT IS NOT THE MASK AND MUST NOT BECOME IT:** a mask rewrites BOTH streams because the oracle's value moves between RUNS; here it moves between INVOCATIONS, so nothing is hidden and the line is graded in full. ⛔⭐⭐ **(2) THE BIGGER HALF, AND ONLY A LIVE RUN FOUND IT.** Grading entry 924 through the real master path printed `&progname: /tmp/tmphdbe45rk/…/procedure_every_alt_replace_4`. **The CEO-569 pin was never in effect where entries are graded:** `run_m4` spells `./<stem>` correctly and `run_suite_entry` → `run_all_modes` opens a SECOND NESTED tempdir and passes THAT as `tmp_dir`, so `_same` was always False and **every master entry in seven languages was still invoked by its absolute mktemp path** — the exact ungradability CEO-569 named, surviving a cure checked by reading the source and by grepping for the spelling. **MY OWN GATE WENT GREEN ON IT ALL DAY**, because clause 1(a) greps for the form and the form is right there. **A SOURCE-LEVEL ASSERTION IS NOT A MEASUREMENT OF BEHAVIOUR** — the gate now builds a one-entry master suite whose ref says `./<stem>` and grades it THROUGH the harness, so it can only pass if the invocation the grader actually makes is the pinned one. The cure is `bin_dir`, **declared never inferred**: a caller that passes none keeps the absolute invocation it has always had, only the binary moves (intermediates stay in `tmp_dir`), and **no entry in any of the seven masters lists or globs its run directory** — censused before landing, because jcon `io.icn` is the program that made that hazard real. ⭐ **(3) THE CONTROL ARM, OWED BECAUSE THE HARNESS IS A SHARED INSTRUMENT: all seven masters, 25-entry slices, pre vs post on the same binary and corpus — ZERO differing verdict lines and identical SUITE_BOARD lines in icon, snobol4, pascal, prolog, raku, rebus and snocone**, with the pre arm run from INSIDE `scripts/` so `__file__` resolves the same repo root (the HQV-34 trap). **The three argv[0]-sensitive entries fleet-wide were graded EXPLICITLY in both arms**, because a 25-entry slice does not contain them and a control arm that cannot see its subject says nothing: icon 414 (prints only the TYPE and length of `&progname`) and snobol4 104 (`HOST(0)`, ref `host0=` empty) **pass identically in both**. ⭐ **(4) THE TWO FLIPS, AND A CELL GIVEN BACK.** IcnM entry 924 `procedure_every_alt_replace_4` m4 **FAIL → PASS**; jcon `kwds` m4 **FAIL → PASS** — and `MODES.tsv`'s kwds m3-only row is **RETIRED on a measurement** (with the declaration m3 PASS m4 PASS; without it m3 PASS m4 FAIL), so the entry is graded in BOTH arms again: a cell given **back** to the denominator, the opposite of the direction a declaration file usually moves. CEO-561's reason was true of the ref model as it stood; the pin made the answer stated and CEO-581 changed the model. **The gate is 13 green / 0 red, re-proved after the corpus rebase onto `af0ec3bbf`.** ⚠ **NAMED, NOT INHERITED:** `test_gate_harness_transitive_companions.sh` is RED on the CLEAN tree, byte-identically (`AFTER HANG HANG`, expecting CRASH) — measured both ways by swapping my harness for HEAD's; pre-existing, not mine. ⚠ **A SECOND LATENT DEFECT CLOSED IN PASSING, in the file already being changed:** `test_icon_jcon_suite.sh --corpus <scratch>` reached the SCORE.md write and published a JCON cell measured over the FIXTURE's population — a complete, plausible `JCON 1/1` with a clean stamp. The board line stands; calling it the package's published row does not. ⚠ **OWED NEXT (hq_T, this inbox):** corpus `b847be956` absorbed `rung36_jcon_genqueen` as icon entry 925 and it takes program arguments with **no `ALL.argv` declaration** — hq_T is right that this is a growth arm doing its job and NOT a re-pin, and the declaration is the absorber's (mine, CEO-452).
- **HQV-35 (hq_V, 2026-09-11 17:3x–18:1x CDT, SCRIP `30e8878b2` → `8b8403f67`) — `&progname` IS argv[0] VERBATIM, SO PINNING THE MODE-4 INVOCATION MAKES IT GRADABLE AND CANNOT MAKE IT EQUAL; CLAUSE 1 OF CEO-569 IS LANDED AND CLAUSE 2 IS A RULING I ROUTED RATHER THAN PICKED.** Inbox drained twice (11 messages: ceo ×4, coo ×2, hq_B, hq_I ×2, hq_R, hq_T — all acted on or answered, then cleared). ⭐ **ACCEPTED FROM THE ceo, AGAINST ITSELF:** CEO-569 corrected its own allocation — it had pulled me onto a Prolog row while my kwds work was HALF cured, *"a thing believed done because the number contradicting it was never read"* — released `wall_us`, and put me on `icon-progname-class-one-ref-cannot-be-right-for-both-modes-master-and-jcon`, claimed, **one row in hand**. ⭐⭐ **(1) I MEASURED THE ORACLE BEFORE WRITING A LINE, AND THE ANSWER RESHAPED THE ROW.** One program, three invocations, Arizona icont/iconx 9.5.25a: `./pn` → `./pn`, the absolute path → the absolute path, `iconx pn` → `pn`. **`&progname` is argv[0], verbatim** — not the source name, not the program's identity, **not something a compiler can get right or wrong**. Entry 924 is therefore **not an engine defect at all**, and there is nothing in the compiler to fix. ⛔ **(2) THE DEFECT IS UNGRADABILITY, NOT A RED — AND IT IS CURED.** The jcon runner built its m4 binary as `"$WORK/${name}_bin"`, an ABSOLUTE path under a mktemp root, and invoked it there: **the answer changed every run**, so no `.ref` was ever cuttable from it — *a criterion that can never say YES*. Same binary, two invocations, measured: **pre-pin `/tmp/.../kwds_bin` → post-pin `./kwds`**. It now builds into the rundir under its bare stem and runs as `./<stem>`, matching `run_m4` exactly (coo `413a0e0a6`) and hq_B's arizona pin (CEO-557). **Pinning converts UNGRADABLE into GRADABLE, which is the whole of what a runner can do here.** ⛔ **INTO THE RUNDIR AND NOT BESIDE THE SOURCE, AND THE BARE STEM IS LOAD-BEARING:** `io.icn` lists its own directory through `ls io.[ids][tca][dnt]` and `ls io.i?n io.d?t io.s?d`, patterns needing a dot plus three characters that a binary named `io` cannot match — and measured across every `.std` in the package, **`io` is the ONLY program that lists its directory**. ⭐ **CONTROL ARM**, pre-pin vs post-pin on the 12 progname-sensitive programs (`kwds profsum tgrlink io recent traceback cxtrace loadfunc tracing tpp link1 link2`): **m3 9/9 and m4 8/9 in BOTH arms, same single red, verdicts byte-identical including `io`** — no board, no row written, override loud and recorded. ⛔ **(3) AND THE HALF A RUNNER CANNOT REACH, WHICH IS WHY THE ROW STAYS OPEN.** m3 is handed a SOURCE and answers `<stem>.icn`; m4 IS the program and answers `./<stem>`. **Both are correct** — each is exactly what the oracle prints for that invocation — and **no invocation of a compiled binary can ever produce the string `<stem>.icn`**. Entry 924 and jcon `kwds` differ from their refs on **exactly one line each**, and it is that one. **Making them EQUAL is a decision, not a cure**, so I routed three options to the ceo with the measurement bearing on each and **picked none**: a per-mode ref (**possible for the first time, precisely because the name is now stable and stated**, and the only option where every graded line stays graded); a mask row (**the machinery is guarded and honest, but its earning test is that the ORACLE's value moves between RUNS and here it moves between INVOCATIONS** — that widens the contract and must be said, not assumed); or a permanent red (honest, visible, and it costs the Icon master its 100% on the row we quote to Lon). ⭐ **(4) THE DONE-WHEN GATE IS MINTED AND IT IS RED ON PURPOSE.** `test_gate_icon_m4_invocation_is_pinned.sh` asserts **BOTH** clauses as the row minted them. Clause 1 (the pin) is **5/5 green**: both runners' spellings, a **LIVE ROUND TRIP** that compiles its own witness and requires `./<stem>` to answer `./<stem>` — *because grep asserts a spelling and not a behaviour* — and a **FAIL-ONCE arm** that runs the SAME binary by its mktemp absolute path and requires the unpinned answer, so **the day the pin stops being observable the gate says so instead of passing vacuously**. Clause 2 (the refs) is **RED**, two entries, one line each, with the oracle measurement printed in the failure text. ⛔ **ASSERTING ONLY CLAUSE 1 WOULD BE A CRITERION A DELETION SATISFIES WHILE THE DEFECT STANDS** (CEO-553: that is FALSE, not merely weak) — so the gate is red, **`done` cannot pass on this row and I did not call it**, and that is the correct state until the ceo rules. **NOT wired into `make test`:** a red gate in the preflight is the ignored-gate shape, and this is a DONE-WHEN. ⚠ **(5) ONE FALSE RED I MANUFACTURED AND CAUGHT:** clause 1's third arm grepped the whole runner for `${name}_bin` and matched **this landing's own explanatory comment**. ⭐ *A source assertion that cannot tell code from commentary fires on the day someone documents the fix.* Code lines only now. ⭐ **(6) TAKEN FROM hq_I, WHOSE FRAMING BEAT MINE:** the icon master identity gate **goes QUIETER THE FASTER THE MASTER GROWS**, because the newest entries are both the unpinned ones and the ones most likely broken (911 origins pinned against 958 in ALL.csv, `show()` capping at 25 of 94, so the FAIL line on my own kwds entry was never printed). They minted it as a row owned by hq_T; I declined to claim it. **And their correction is taken: the origin is `rung36_all__rung36_jcon_kwds` as a SINGLE TOKEN, and the entry NAME is display-only and gets renumbered by the builder — key on origin.** ⛔ **NO BOARD RUN.**
- **HQV-34 (hq_V, 2026-09-11 16:1x–17:3x CDT, SCRIP `a7e53038c` → `30e8878b2`) — ONE QUESTION HAD THREE WRITTEN ANSWERS, AND THE NARROWEST OF THEM DECIDED WHAT GETS ABSORBED INTO A MASTER.** Session start clean (three repos ff-merged, inbox empty at the first check, banner read, incremental `make`). MODE **NONET**, order of work ICON, SNOBOL4, PROLOG. My named Icon row (jcon `kwds`) closed in HQV-33, so I took the defect HQV-32 item (3) NAMED AND DID NOT CURE. FINDING: `FINDING-2026-09-11-hq_V-one-question-three-answers-the-builder-and-the-grader-disagreed-about-what-a-stdin-companion-is.md`. ⛔⭐ **(1) THE DEFECT.** *"Does this program have stdin?"* was written down three times with three different answers — the grader (`loose_stdin_companion`) knew `.stdin/.in/.input`, the builder's generalised guard knew `.in/.input`, its snobol4 plain-program guard knew `.input` alone — and **all 8 icon stdin companions in this corpus are spelled `config/<stem>.stdin`, the one spelling the BUILDER did not know** (`rung36_jcon_{btrees,geddump,io,others,prefix,profsum,recent,recogn}`). ⛔ **IT IS SILENT, WHICH IS THE WHOLE DANGER:** a program whose input the builder cannot see is not refused — it is ABSORBED, the master grades it against `/dev/null`, it exits at EOF before reaching the behaviour it was written to test, and the auto-xfail path files the starved run as a documented red. **THERE IS NO XFAIL** — a FAIL on every board, wearing bookkeeping's clothes. ⭐ **MEASURED BOTH DIRECTIONS ONE BYTE APART** on scratch corpora, same program, same input bytes, same ref: `config/w.stdin` → **ABSORBED** (`CANNOT ABSORB: 0`, entry lands **XFAIL**), `config/w.in` → **EXCLUDED BY NAME**. ⭐ **(2) THE CURE IS ONE FINDER, NOT A FOURTH LIST.** The spellings live only in `corpus_suite_harness.STDIN_COMPANION_SUFFIXES`, behind `stdin_companion_candidates(src)`, which the grader and BOTH builder guards call — **the builder and the grader must ask the same question about a file, so they ask it through the same function rather than through two lists that agreed on the day they were written** (CEO-566's own words: a hand-maintained list is the defect, not the cure). The exclusion message now NAMES the companion **relative to the source's directory**, never `.name` alone — `w.stdin` and `config/w.stdin` both render as "w.stdin", the identical trap hq_U cured in this same function's AMBIGUOUS message on 2026-09-08. ⚠ **(3) A SECOND DEFECT THE SAME MEASUREMENT SURFACED, NAMED NOT CHASED:** `rung36_jcon_io` has TWO companions — `tests/icon/rung36_jcon_io.stdin` (60 bytes) and `tests/icon/config/rung36_jcon_io.stdin` (130 bytes) — and they **DIFFER**. The grader correctly REFUSES rather than picking one, which also means **`io` cannot be graded fed by any path today**. Which is the program's input is a decision for its author, not a precedence rule to invent here. ⭐ **(4) THE CONTROL ARM: NOTHING MOVED, AND THAT IS THE CORRECT RESULT.** All seven masters (`snobol4 icon prolog raku snocone rebus pascal`) rebuilt from the real corpus on scratch copies, pre-cure vs cured, symmetric roots, same binary: **byte-identical, every file, every language, 0 differing files** — re-proved from scratch after each of two rebases, never inherited. The 8 icon programs are held out today by a `KEEP.md` declaration checked BEFORE this guard, so the cure is a **LATCH, not a flip** — exactly what HQV-32 called it: armed, not latent. `recogn` fell through the instant its deferral row reached DONE; the other seven are one retired declaration away each. ⛔⭐ **(5) A CORRECTION TO MY OWN FIRST CONTROL ARM, AND IT IS THE MOST USEFUL THING IN THIS RUNG.** That arm reported the snobol4 master REORDERED with `data_redefinition_adds_field_to_existing_type` flipping `xfail=1 → 0`. **That was my instrument, not the code.** I ran the pre-cure builder from a copied `scripts/` directory outside the repo, and `resolve_paths()` derives `scrip_bin` as `Path(__file__).resolve().parent.parent / "scrip"` — so that arm's compiler path did not exist and **every fresh absorption auto-XFAILed because nothing could run**. The entry was flipped by hq_C's `a71a153fa` earlier the same day; only the arm WITH a compiler could see it. ⭐ **A CONTROL ARM THAT RUNS THE BUILDER FROM A COPIED `scripts/` HAS NO COMPILER AND SAYS SO NOWHERE** — it does not crash, does not warn, and produces a complete, plausible, fully-populated master in which every newly absorbed entry is stamped XFAIL. **Both halves reproduce perfectly run to run, so determinism is no defence**: I confirmed old==old2 and new==new2 and was still comparing an arm with a compiler against an arm without one. Both arms now carry a `scrip`/`out` symlink beside the copied `scripts/`. ⭐ **(6) THE INSTRUMENT.** `scripts/test_gate_builder_and_grader_agree_on_stdin_companion_spellings.sh`, wired into `make test`, **~2.2s MEASURED** (2.17/2.16), 12 arms = 3 spellings × 2 locations × icon + snobol4, each on its own synthetic corpus built and torn down in the gate — **no board, no corpus master** (CEO-547 part 2), runnable by any seat under ONE RUNNER. It **reads the spellings FROM the harness**, so a fourth copy of the list cannot be what keeps it green; it covers **both** languages because icon reaches the generalised guard and snobol4's bannerless pair reaches the plain-program guard and those held different lists; and it asserts on the exclusion being **NAMED**, never on a count — a count is also satisfied by the pair vanishing for an unrelated reason, and the lesson of this defect is that a program leaving the numerator silently is indistinguishable from one never there. **FAIL-ONCE: rc=1 naming 6 of 12 arms** (icon `.stdin` ×2, snobol4 `.stdin` ×2, snobol4 `.in` ×2) against the pre-cure builder with the cured harness — the exact drift state it polices, and the precise shape of the three-way divergence recovered by the instrument rather than asserted by me. **PASS-ONCE: rc=0, 12/12.** REFUSES rc=2 when it cannot measure. Both re-proved after each rebase. ⚠ **(7) NOT MINE, NAMED WITH ITS MEASUREMENT AND PROVED PRE-EXISTING** on a stashed clean tree with a fresh binary (identical output, same counts, same named gates): `test_gate_baton_donewhen_runnable` **92 live rows vs ceiling 89, GREW by 3** — and it is the SECOND arm of `make test` (Makefile:155), **so it stops every arm after it for every seat** (hq_S's ratchet, `e441fc7e`); `test_gate_runners_refuse_on_a_stale_binary` (6 gates execute `./scrip` with no freshness guard, none of them mine); `test_gate_orphaned_witnesses_do_not_grow` (`ORPHANED-WITNESSES.tsv` stale against a CLEAN corpus); `test_gate_master_sidecars_cover_stdin_and_argv` (1 debt above its pinned floor — the re-pin is hq_T's). Two further arms exit **rc=2 as ONE RUNNER refusals**, which is the correct answer for a non-coo seat and **not a failure**. ⚠ **(8) TWO BROKEN SYMLINKS IN THE SNOBOL4 CORPUS, SURFACED BY THE ARM:** `tests/snobol4/config/probe_loose_table_nested_claws5_l{1,2}.dat` → `../../benchmarks/snobol4/demo/claws5.dat` resolves to `corpus/tests/benchmarks/...` and does not exist; the real file is `corpus/benchmarks/snobol4/demo/claws5.dat` (66757 bytes), one `../` away. Both pairs are already excluded from the master, so no board number turns on it — hygiene, routed to hq_P rather than fixed from this seat. ⛔ **NO BOARD RUN.**
- **HQV-33 (hq_V, 2026-09-11 16:1x–16:5x CDT, SCRIP `cd8852788`, corpus `cab7d16f2`) — CEO-561 EXECUTED, AND THE RULING'S PREMISE WAS TRUE OF THE PROGRAM AND FALSE OF THE RUNNER: kwds COULD NOT BE GRADED IN *EITHER* MODE.** Inbox drained (4: hq_B ×2, ceo ×2 — all acted on or answered). ⭐ **(1) THE RULING LANDED AS GIVEN.** jcon `kwds` is DECLARED `m3` in a new `corpus/packages/icon/jcon_tests/MODES.tsv`; ref NOT re-cut, entry NOT excluded, and **the row carries the measurement, not a bare mode** (CEO-561 adopting this lane's standing practice — a wrong exclusion costs more than a wrong cure). Re-measured here rather than taken from the ask: **exactly one diff line**, m3 byte-identical, m4 printing `&progname: ./kwds_bin`. The file's own header states that a row is NOT an exclusion — a declared entry keeps its place in SHIPPED, GRADED and the published denominator — and that it is deliberately not `OUTSIDE_ARIZONA_BASELINE.tsv`, whose names leave the denominator because no ground truth exists. ⛔⭐ **(2) THE CORRECTION, AND I FOUND IT ONLY BECAUSE A VERIFICATION ARM DISAGREED WITH MY OWN HAND MEASUREMENT INSTEAD OF CONFIRMING IT.** By hand from inside the directory, m3 PASSES. Through `test_icon_jcon_suite.sh`, m3 **FAILED** — the runner handed the driver `"$icn"`, an **ABSOLUTE PATH**, while already `cd`'d into the rundir where it had just copied the source, so `&progname` echoed `/tmp/.../kwds.icn` against a ref that says `kwds.icn`. **A declaration alone would have left kwds red in the very arm that was supposed to grade it**, and the board would have read that as a compiler defect. The cure is the one `corpus_suite_harness.py`'s `run_m3` already carries (row `suite-harness-argv-echoes-a-mktemp-path-so-diagnostic-programs-cannot-be-graded`): bare name in argv, explicit cwd. ⭐ **The arizona runner never had it wrong** — it always used the bare name — **which is exactly why pinning the m4 binary name fixed arizona kwds and could never have fixed jcon's.** ⛔ **(3) THE ARGV CURE NEEDED A COMPANION, MEASURED NOT GUESSED:** `link link2` resolves relative to the MAIN PROGRAM'S path, so the bare name moved that search to `./` and `link1` died with *cannot open link2.icn* even with `link2.icn` on the command line. A linked module now travels into the rundir exactly as the program's own source already did — **the program's OWN closure only, never the package**, so `io`'s rundir (whose ref lists its directory and expects `io.dat` and `io.icn` and nothing else) is untouched because `io` links nothing. ⛔ **(4) AND MY OWN WIRING SHIPPED A BUG FOR TEN MINUTES, CAUGHT BY A SCRATCH ARM AND NOT BY READING IT:** `MODES_TSV` was bound from the DEFAULT corpus at load time, **before `--corpus` is parsed**, so a run could grade one tree while reading another tree's declarations — it reported `NOTGRADED=1` on a scratch corpus that had no MODES.tsv at all. The pre-existing `OUTSIDE=` line has the identical shape and is re-bound with it: **being right by accident at the default path is not a contract.** ⭐ **(5) SCOPE BOUNDED BEFORE THE CHANGE, NEVER ASSERTED AFTER:** the only programs whose output can turn on the spelling are those printing their own invocation or filename — sources naming `&progname`/`&file` (kwds, profsum, tgrlink) and `.std` files quoting a `.icn` name (io, kwds, recent, traceback, cxtrace, loadfunc, tracing, tpp). All nine gradable ones run BOTH ways, twice each for stability: **m3 8/10 → 9/10**, kwds FAIL → PASS, every other verdict byte-identical, `tracing` still the only red (the ceo's named Jcon red, not mine). ⚠ `io` flipped red in exactly one of five runs and was byte-identical by hand both ways — **named, not chased**: it is the program that `sed`s its own source and `ls`es its own directory. **FOUR ARMS ON THE DECLARATION ITSELF:** absent → m4 grades kwds and FAILS; present → m4 prints `NOTGRADED=1` **naming the cell** and m3 is unchanged; a row naming no shipped program **REFUSES rc=2**; a modes value that is not m3/m4/m3,m4 **REFUSES rc=2**. ⛔ **NO BOARD RUN:** the arms are a 9-program SCRATCH corpus under a loud `S4E_ONE_RUNNER_OVERRIDE` with no row written; the coo owns the jcon board and should see kwds green in m3 and not-graded in m4 on the next pass. ⭐ **(6) NOTED FROM CEO-558:** the announce date is **September 17, 2026**; the 35-hours basis is VOID; scope is UNCHANGED and a date slipping is not a scope change. Every "September 10" in my root digest is stale and is not to be quoted.
- **HQV-32 (hq_V, 2026-09-11 15:4x–16:5x CDT, SCRIP `1493214e4`→, corpus `2e98e16ad`→) — THE ICON CENSUS CANNOT REACH ZERO BECAUSE A CORRECT DECLARATION CANNOT PHYSICALLY REACH THE LEDGER, AND A ROUTINE REBUILD WOULD HAVE SHIPPED A RED DUPLICATE OF A GREEN ENTRY.** Inbox drained (2: hq_T, both verified in the tree before acknowledging and both answered in one reply). FINDING: `FINDING-2026-09-11-hq_V-a-keeper-that-cannot-be-recorded-and-a-stdin-spelling-the-builder-does-not-know.md`. ⛔⭐ **(1) A PAIRLESS KEEPER IS UNRECORDABLE, PROVED IN TWO ARMS ON SCRATCH COPIES.** `discover_pairs()` drops a source with no `.ref`/`.expected` one branch BEFORE the `_declared_in_keep()` check that would write its KEEPER line, and `ALL.excluded.txt` is the ONLY channel the census reads (and since CEO-545 the builder is its only writer). **ARM A** (tree as shipped): no ledger line for `rung16_seqexpr_gen_basic`, 959 entries. **ARM B** (same tree + one `.ref` beside it): `KEEPER, declared in KEEP.md` appears, same 959 entries, nothing new absorbed — **the accounting channel is gated on the exact property the declaration says the file deliberately lacks** (*"deliberately shipped with NO .ref, because a pairless witness is not a board member"*). **46 of icon's 54 owed are pairless**, including two I declared correctly myself on 2026-09-10. ⭐ **A number that will not move is not always a backlog; ask first which writes can physically reach the meter.** Routed to hq_T with the cure (run the keeper check BEFORE the pairless `continue`), NOT landed — they hold the builder this sitting and two seats in one file is how a merge conflict becomes a false measurement. ⛔⭐ **(2) THE REBUILD I WAS ABOUT TO RUN WOULD HAVE ADDED A RED DUPLICATE OF A GREEN ENTRY.** A plain `--lang icon --write` on a scratch copy of today's tree produces **959**, the extra being `procedure_suspend_scan_replace_2`, origin `rung36_jcon_recogn`, **xfail=1**, beside the already-green `900 procedure_suspend_scan_replace_1`, origin `rung36_jcon_recogn__rung36_jcon_recogn`, body and ref BYTE-IDENTICAL. Same origin-spelling guard miss as HQV-31's `generators`, but this one arrives stamped **xfail, and THERE IS NO XFAIL** — a FAIL on every board. ⛔ **(3) AND THE REASON IT WOULD BE RED IS A SPELLING:** the builder's stdin guard knows `.in`/`.input` beside the source and in `config/`, the harness's `loose_stdin_companion()` knows those plus **`.stdin`**, and **all 8 icon stdin companions in this tree are `config/*.stdin`** — so the program is graded against `/dev/null`, exits at EOF before any `suspend`, prints nothing against its own 8-line FED ref, and the auto-XFAIL path files the red as documented. ⚠ **Seven more are armed, not latent** (`btrees geddump io others prefix profsum recent`): each is held loose today only by a declaration, and `recogn` fell through the instant its deferral row reached DONE. ⭐ **(4) WHAT LANDED, AND WHY DELETING THE DUPLICATE WOULD HAVE BEEN WRONG.** The ordinary cure for a duplicate beside its absorbed entry is DELETE (18 of them on 2026-09-10). Here **two instruments read the file BY PATH and both go quietly smaller**: `test_gate_icn_rundir_contract.sh` DISCOVERS witnesses by scanning `tests/icon/*.icn` and refuses only on an EMPTY set (8 → 7, no message, its `contract_floor` pin dead), and `test_icon_ir_rung_36.sh`'s `run()` prints `SKIP (no .expected)` **and returns success**. So `rung36_jcon_recogn.icn` is DECLARED in KEEP.md **with its `.icn` suffix** (HQV-30's lesson) **and a stated exit condition** — re-run the scratch arm; the day it prints 958 the section is dead — and the declaration is **proved to bite**: 959 without it, **958 and no duplicate row** with it. ⭐ **(5) ONE REAL MASTER WRITE: `rung36_jcon_genqueen` ABSORBED, ICON MASTER 957 → 958.** Its deferral (`icn-recogn-genqueen-suspend-shape`) expired when that row reached DONE. Re-measured here rather than taken from the old note: harness `run_suite_entry` **m3 PASS + m4 PASS**, `.expected` == a FRESH `icont -s` cut of BOTH the upstream `packages/icon/jcon_tests/genqueen.icn` AND the corpus copy byte for byte, input-independent (no `read`, no `&input`, no `.dat`), modes declared in `config/MODES.tsv` from that evidence and never from the family name. **Pass-once: PASS m3+m4 at entries=958; fail-once: one injected ref line FAILS both modes.** Content diff **lost 0, gained exactly 1, changed 0**. Loose pair deleted by the builder's own `--delete-absorbed` after content re-verification, and its smoke line replaced by a new `moved()` that ASSERTS the master carries the origin and **REFUSES rc=2** otherwise — because a silent SKIP is how the correct end of a conversion makes an instrument smaller with no one noticing. ⚠ **(6) CORRECTION TO MY OWN RECORD, hq_T's, verified before accepted:** corpus `249f653a6`'s message says *"the four rung03 `.expected` twins went too"* and **they did not** — that commit deletes 40 files, none of them `.expected`; hq_T deleted them at `1b071fc15` after proving preservation against the master. **And the wider half: HQV-30's "icon orphans reach 0" was true of the tests-tree orphan ratchet it named and NOT of the census**, which reads icon `OWED 54` across trees; a reader could fairly have taken the wider claim. ⭐ **(7) ACCEPTED FROM hq_T, in the record because it governs every instrument I write next:** a fail-closed `rc=1` is still the WRONG one of the two honest answers when the instrument cannot measure — **a FAIL and a CANNOT-MEASURE are different facts**, and `rc=1` for "I could not look" is a FALSE RED that sends the next reader hunting a defect in the language. Both narrowings on the third-declaration-source ruling accepted: the predicate is a live BUILDER RULE only (never an echo of another declaration source, never absorbed-ness), and the gate must VERIFY the CEO-545 digest and refuse rc=2 on a mismatch. ⛔ **NO BOARD RUN** (ONE RUNNER, ONE BOARD — `rung36_all` delegation REFUSES rc=2 for every seat but the coo, correctly). Gates: `test_gate_icn_rundir_contract.sh` **PASS** (8 witnesses, 0 failures), `test_gate_suite_conversion_complete.sh icon` **GREEN** (loose-but-undeclared **0**), `test_icon_ir_rung_36.sh` loose arms unchanged but for the new MOVED line, `strip_comments.py --check` clean.
- **HQV-31 (hq_V, 2026-09-11, SCRIP `c0f6a747a`, corpus `379837dcc`→`faebbb268`) — I ALMOST SHIPPED A FIVE-ENTRY DOUBLE COUNT AND TWO SEPARATE TOOLS COULD NOT SEE IT.** Inbox drained (4: ceo, hq_B ×2, hq_C — all acted on or answered). ⛔⭐ **(1) THE NEAR MISS IS THE ENTRY.** hq_C handed me `generators.icn` as unblocked; KEEP.md declared it and seven siblings unconvertible because *“none of them has an `.expected` file, and never did”*. **Both halves were false.** An oracle exists — `capture-oracle-refs --lang icon` reports **8 of 8 GREEN, m3=AGREE and m4=AGREE** — and **SIX OF THE EIGHT WERE ALREADY IN THE MASTER**, absorbed in an earlier pass under the OLD `family__stem` origin spelling, their loose root copies never deleted. Absorbing those copies produced **five new entries (957 → 962) whose bodies AND refs are byte-identical to entries already present**. ⛔ **THE BUILDER'S “already in the master” GUARD IS KEYED ON THE ORIGIN STRING, NOT ON CONTENT**, and a builder change had re-spelled that key between the two passes, so it could not match. ⛔ **AND `util_master_content_diff.py` DOES NOT CATCH IT EITHER** — it compares by NAME, reported *gained 5, changed 0*, and is **right by its own contract**. Two instruments, both working as designed, both blind to the same thing; only an explicit body-to-body comparison found it. **REVERTED**; the five deleted as duplicates instead, each verified byte-identical in body AND ref against its existing entry first. **Master stays 957 — the denominator gains nothing, because there was nothing to gain.** ⭐ **(2) THE GREP IS A FINDING AID; THE RENAME IS THE TEST.** `generators.ref` contains the word `generators` — in the literal heading *Primes to 50 (trial division by generators):*, an English word, not a filename echo. A stem grep says *name-echoing, do not absorb* and is WRONG. Running each program under a DIFFERENT filename is the decisive arm: all 8 byte-identical under a rename, both modes. Same shape as this morning's `read`/`reads` count. ⛔ **(3) THREE STAY LOOSE AND EACH REASON IS LIVE AND RE-CHECKABLE.** `palindrome` and `generators` are read BY PATH by gates that are `[ -f ]`-guarded and **SKIP SILENTLY** — deleting either fails nothing and quietly shrinks a witness set, the false-green class. `meander` is held by the builder's own stdin-sidecar rule. ⚠ **AND NAMING `meander` COST SOMETHING EITHER WAY, MEASURED BOTH WAYS:** with the `.icn` suffix the builder re-labels it *KEEPER, declared in KEEP.md* and **overwrites its own true reason**; without it the conversion gate, which reads only KEEP.md/PENDING.md, calls it undeclared and goes red. **Neither spelling is free.** Kept the suffix plus **a stated EXIT CONDITION** — the sanctioned cure, since decay comes from an UNCONDITIONED note, not from a note. ⭐ **(4) ROUTED, NOT TAKEN:** the conversion gate could accept a reasoned `ALL.excluded.txt` line as a third declaration source, and that is **newly safe only because of CEO-545's digest** — before it, an invented line could have been hand-added to satisfy a gate. Two changes that fit together; hq_T's judgement, not my assertion. ⭐ **(5) hq_B RETRACTED THE `&progname` ASK AND MY HOLD COST NOTHING** — I had re-cut no ref, because a single-writer artifact changed on two theories at once cannot be attributed afterwards. Their one-step/two-step measurement vindicates BOTH shipped `.std` files. **Their open question has an answer in machinery I maintain:** `config/MODES.tsv` declares per family WHICH modes an entry is graded in, so an entry whose correct output genuinely differs between m3 and m4 is declared **m3-only with its evidence** rather than being wrong in one of two — smaller than a format extension. Waiting on their engine half landing green and the ceo's word before touching entry 911. ⛔ **NO BOARD RUN.** Icon orphans **0/196**, conversion gate **GREEN** (0 loose-but-undeclared), master **957**.
- **HQV-30 (hq_V, 2026-09-10 19:3x – 2026-09-11, SCRIP `a3eab74ad`→`c265bdcc1`, corpus `98c3e82ea`→`379837dcc`) — ICON ORPHANS REACH **0**, THE CONVERSION GATE IS GREEN, AND CEO-545 IS DISCHARGED WITH ITS COSTS NAMED.** FINDING: `FINDING-2026-09-11-hq_V-a-generated-sidecar-failed-two-opposite-ways-and-a-keeper-declaration-was-invisible-to-the-machine-that-enforces-it.md`. ⭐ **(1) THE "UNCONVERTED" BACKLOG WAS 18 DUPLICATES AND 2 WITNESSES, WHICH IS WHY IT HAD NOT MOVED FOR A DAY.** The conversion gate called 20 files *neither converted nor declared*; eighteen were **already IN the master** with only their loose copies left behind (the four `rung03` ones by my own 2026-09-09 landing). **A duplicate beside its absorbed entry is indistinguishable, to that gate AND the orphan census, from a witness nobody converted — and the two want OPPOSITE cures, delete versus absorb.** Reading the gate's message as its own diagnosis is what froze the number. All 18 content-verified by the builder's `--delete-absorbed` before removal; the 2 real ones absorbed. **Icon orphans 0/201 — the first language to reach that ratchet's intended terminal value** — `FLOOR_icon` 2 → 0, master **955 → 957**. ⛔⭐ **(2) A KEEPER DECLARATION WAS INVISIBLE TO THE MACHINE THAT ENFORCES IT.** hq_S's `an_ordinary_call_chain_prints_every_frame_in_the_traceback` was declared in KEEP.md WITHOUT its `.icn` suffix, and the deferral contract is a delimited substring search for the BASENAME — so it matched nothing and **the builder would have ABSORBED a file whose ref carries its own name on 4 lines**, manufacturing a red out of a rename. PROVED, two scratch arms: bare → ABSORBED (956 entries); `.icn` → REFUSED as a KEEPER. **A declaration that reads correctly to a human and to nothing else is not a declaration.** ⚠ **This corrects my own HQV-28**, which called it *bookkeeping lag, no build had run since* — it was not lag, and a build would have made it WORSE. ⭐ `rung03` spells its names bare ON PURPOSE so a RETIRED entry stops holding the block live: same mechanism, opposite intent, and only the entry's direction tells them apart. Both named in the file now. ⛔⭐ **(3) CEO-545 DISCHARGED — AND THE SIDECAR FAILED TWO OPPOSITE WAYS, NOT ONE.** Measured before any code changed: a hand **DELETION** of a computed line was **silently reverted** by the next build; a hand-**ADDED** line the builder never computes **survived forever, unchallenged**. **The second is the worse half — it is how a WRONG exclusion becomes permanent, the jcon `kwds` shape that cost four cycles.** Cure: the builder stamps `# builder-digest: <sha256>` over the sorted data lines and REFUSES rc=2 on any other writer; all three writers covered in one change. **Costs named as the ruling asked:** a hand edit now needs a rebuild (the point — and it convicts my own edit an hour earlier, so the escape hatch is documented IN the refusal message); the digest cannot tell an invention from another RUN's output, so every writer had to be covered at once; other languages MIGRATE rather than refuse, because a guard that fires on its own introduction is not a guard; a git merge conflict here now surfaces as a refusal. ⛔ **THE NON-REGRESSION THAT MATTERED MOST:** `MODES.tsv` shares the merge helper and is **DECLARED, NEVER DERIVED** — guarding it would refuse its own intended use. Opt-in per call site, with an arm proving a hand row survives and no digest leaks. **Nine arms wired (21 → 30 assertions) AND PROVED TO BITE** against the unguarded builder, the false-positive control staying green — **a detector arm that fails open reads as "there was never a bug here".** ⚠ **(4) ONE READER REPAIRED BECAUSE THIS CHANGE MADE A LATENT BUG REACHABLE:** `test_gate_snobol4_master_named_set_equality.sh` did `cut -f1` over the sidecar, turning every comment line into a phantom excluded NAME; that file could always carry comments, and now always will. ⚠ **NAMED, NOT MINE:** that gate is RED on two long-deleted SNOBOL4 pairs (**proved** pre-existing — the verdict is byte-identical with my edit reverted; SNOBOL4 is parked); `test_icon_ir_rung_03.sh` globs `/home/corpus/icon`, which does not exist, and is wired into nothing — it exits rc=1 so it fails CLOSED, a dead instrument not a false green, routed to hq_T; the port-trace gate's 22 reds belong to SCRIP `a3eab74ad`. ⛔ **NO BOARD RUN.** Icon master across the sitting: **939 → 950 → 953 → 955 → 957**.
- **HQV-29 (hq_V, 2026-09-10 19:3x–19:5x CDT, SCRIP `bce188090`→`2e7b357c5`, corpus `24dd8237d`→`98c3e82ea`) — THE CFO'S TWO PAIRS ABSORBED, ICON MASTER **953 → 955**, AND THE ORPHAN DEBT THIS LANE NAMED TWO COMMITS AGO IS PAID RATHER THAN OUTLIVED.** Inbox drained (4: cfo, ceo ×2, hq_B — all four acted on or answered). ⭐ **(1) THE TWO PAIRS ARE THE EXACT TWO I NAMED AS NOT-MINE, AND THAT IS THE POINT.** HQV-27 shipped the ratchet red against corpus `cfc826b0f` and `2e8b94fe5`; HQV-28 recorded that the count had fallen PAST them rather than resolving them. The cfo read the gate, identified both as theirs, and offered them; the master is this seat's write, so they are in — `procedure_alt_list_replace_1` and `procedure_every_to_52`. **A named debt leaving a ratchet by being PAID is the only honest exit, and naming it precisely is what made the payment findable.** Floor icon 4 → 2, gate GREEN. ⛔ **(2) THE rc ARM WAS PROVED TO BITE, NOT ASSUMED.** `stop_writes_to_a_leading_file_argument_and_ends_its_line` dies **rc=1 by design** (`stop()` exits 1), so it needs a row in `ALL.wantrc`. Measured both ways: **row removed → FAIL in BOTH modes; row restored → PASS in both.** An undeclared positive rc is exactly the vacuous-pass shape the harness's own ruling refuses, and writing the row without the arm would have been a declaration no one had tested. ⚠ The row is keyed on the ASSIGNED name, which shifts on a rebuild — the same hazard KEEP.md records for refs pinned to assigned names. Eight rows in that file share it; not changed here, named as a live trap for the next re-sort. ⭐ **(3) AN INPUT-INDEPENDENCE GREP IS A FINDING AID, NOT THE TEST.** `list_and_reads_...` trips a source-text `read`/`reads` count FOUR times and is input-independent anyway: every one reads a file it opened on `/dev/null`, and it never touches `&input`. **The question is what the program READS, and only the source answers it** — the count only says where to look. Told to the cfo so the next handed pair does not stall on it. ⛔ **(4) CEO-543 ARRIVED AND CEO-544 VOID'D IT WITHIN THE SAME INBOX** (Icon newline-ends-statement, withdrawn on Lon's word: vendored Icon sources are CONVERTED ON THE WAY IN and checked in converted). **Nothing was begun on the strength of it**, which is the whole reason a broadcast law gets read before it gets acted on. ⚠ **(5) hq_B's `&progname` MEASUREMENT FILED, NOT ACTED ON, AND IT LANDS TWICE IN THIS LANE:** `&progname` is a COMPILE-TIME constant from the source basename here (a mode-4 binary copied to `zzz` still prints `pn.icn`) where iconx's is `argv[0]`. Curing it needs master entry 911's ref re-cut — my write — and it reverses a half of CEO-524. **I am touching neither half until the ceo answers hq_B's ask**: a single-writer artifact changed on two theories at once cannot be attributed afterwards. Answered by name with the jcon `kwds` history (my HQV-20: we already agree with icont because the runner hands SCRIP the SOURCE) and with the warning that cost four cycles — **an excluded name cannot be red, so nobody looks; if the answer is `OUTSIDE_ARIZONA_BASELINE.tsv`, the row carries the measurement and not just the name.** ⛔ **NO BOARD RUN** — ONE RUNNER, ONE BOARD. Icon master today: **939 → 950 → 953 → 955**, three landings, all three-way measured at absorb time.
- **HQV-28 (hq_V, 2026-09-10 19:0x–19:3x CDT, SCRIP `669786b88`→`bce188090`, corpus `2ef0fe270`→`24dd8237d`) — THE CTO'S TWO DISPLAY WITNESSES AND THE CFO'S SECTION WITNESS ABSORBED, ICON MASTER **950 → 953**, AND THE SECOND HALF OF THE `ALL.excluded.txt` SHAPE FOUND ITSELF.** Inbox drained (4 total this sitting: ceo, cto ×2, cfo — all acted on). ⭐ **(1) RE-MEASURED, AND THE RE-MEASURE WAS NOT CEREMONY — THE TWO CTO TELEGRAMS DISAGREED WITH EACH OTHER.** The first said green m3 only (`diff=14` and `diff=3` in m4) on `3e68226ea`; the second said green in BOTH modes on `6c453e4d0`. Taking either at face value would have been wrong on the tree I actually hold. Measured on `669786b88`, which contains the cto's mode-4 procedure-name-table cure: all three are **SCRIP m3 == SCRIP m4 == a fresh `icont -s` cut here == the shipped `.ref`**, byte for byte, rc=0, input-independent, no name echo. pass-once all 3 PASS both modes; fail-once all 3 FAIL both modes; content diff **950 → 953, lost 0, gained exactly 3, changed 0**; icont gradability **953/953**. ⭐ **(2) `display_prints_the_frames_locals_and_the_globals` IS THE ENTRY HQV-26 REFUSED SIX HOURS EARLIER**, on the rule that a red absorbed is a red wearing an absorb's clothes. It is green now because the cto cured what was underneath it — a standalone binary registered NO per-procedure name metadata at all — **which is the outcome that refusal was holding out for. The witness was never the defect; withholding it is what kept it visible until the defect went.** ⛔ And `icon_display_builtin_unimplemented` no longer means what its name says: the oracle agrees with us byte for byte today, so it pins CURED behaviour and the name is stale — the same class as HQV-26's three sort/serial witnesses, absorbed on the measurement and never on the name. ⛔⭐ **(3) THE SECOND HALF OF THE EXCLUDED-SIDECAR SHAPE, AND IT RUNS THE OTHER WAY.** HQV-27 found that the merge NEVER RETRACTS an exclusion. This landing found that **NOTHING BUT A BUILD EVER ADDS ONE**: hq_S's `an_ordinary_call_chain_prints_every_frame_in_the_traceback` (corpus `bc1b1f900`) was DECLARED a permanent name-echoing keeper in KEEP.md in its own commit and still counted as an ORPHAN, because `ALL.excluded.txt` is the builder's output and no build had run since. **A declared keeper reads as an orphan until somebody runs a build, and the census cannot tell that apart from real debt.** Verified the declaration rather than assuming it — its ref carries its own file name on 4 lines, so the rename would manufacture a red — then wrote the one line the builder emits verbatim, and named it in the commit because it is another seat's file. ⚠ **(4) THE FLOOR MOVED 5 → 4 AND THE TWO REBASE-WINDOW ORPHANS ARE STILL THEIRS:** corpus `cfc826b0f` and `2e8b94fe5`. The count fell PAST them because three absorbs outweighed two additions, not because either was resolved; `ORPHANED-WITNESSES.tsv` still names both. **A floor is a ceiling on the count, never a receipt for what is under it.** The gate is GREEN at `bce188090`. ⭐ **(5) THE CFO'S OTHER HALF, FILED NOT TAKEN:** `procedure_record_every_replace_12` reads 0 differing lines in m3 under CFO-45/46 and its last m4 red — `stop(&output, ...)` at the stdout buffer boundary — is cured at SCRIP `e7e916e14` with witness corpus `cfc826b0f`. That is an existing master entry, not an absorb, and the board that grades it is the coo's. ⛔ **NO BOARD RUN** — ONE RUNNER, ONE BOARD (CEO-523). Icon master denominator today: **939 → 950 → 953**.
- **HQV-27 (hq_V, 2026-09-10 18:3x–19:0x CDT, SCRIP `2a8d0f9fc`→`669786b88`, corpus `3f0b910e4`→`2ef0fe270`) — CEO-541 DISCHARGED: THE ELEVEN ARE ABSORBED, ICON MASTER **939 → 950**, AND THE LANDING FOUND TWO BOOKKEEPING ARTIFACTS NEITHER THE BUILDER NOR A GATE CAN RETRACT ON ITS OWN.** Inbox drained at the prompt boundary (2: ceo, cto — both acted on). ⭐ **(1) THE RULING EXECUTED TO ITS CONDITIONS, AND THE CONDITION THAT MATTERED WAS *AT ABSORB TIME*.** The ask carried a measurement on SCRIP `b84976b17`; this landing is `2a8d0f9fc`, a later tree, so all eleven were RE-RUN rather than re-cited — SCRIP m3 == SCRIP m4 == a fresh `icont -s` cut here == the shipped `.expected`, byte for byte, rc=0; `&input`/`read`/`reads` count 0; own-stem count 0. **None failed, so none is named** — and the arm is recorded so a later reader can tell a re-measure from a repeated citation. Through the harness's own `read_block_suite` + `run_suite_entry`, both modes: **pass-once all 11 PASS m3+m4; fail-once (one line inserted per ref block) all 11 FAIL both modes**; content diff **939 → 950, lost 0, gained exactly 11, changed 0**; icont gradability **950/950 compiled, 0 refused**. Modes declared per row in `config/MODES.tsv` on ref-content evidence. The eleven KEEP.md declarations left in the same commit, as ruled. ⛔ **(2) `ALL.excluded.txt` CANNOT RETRACT AN EXCLUSION — IT IS WRITTEN BY A MERGE.** `_excl_existing.update(...)` means a family's line survives absorption forever: the eleven would still have read *“KEEPER, declared in KEEP.md — never absorbed”* about files that no longer exist and a declaration that no longer does. The previous batch only looked self-healing because a later pass re-classified those three while their files were still on disk; absorbing in the SAME call that deletes is what makes it stale. **This is the jcon `kwds` shape again — an excluded name cannot be red, so nobody looks** — so the eleven lines were removed by hand and the rule written into KEEP.md for the next absorb. ⛔⭐ **(3) THE ORPHAN CENSUS WAS STALE AND THE STALENESS WAS MINE.** HQV-26 absorbed 18 witnesses and never regenerated `corpus/tests/ORPHANED-WITNESSES.tsv`, so `test_gate_orphaned_witnesses_do_not_grow.sh` read STALE — **red on origin, for every seat that ran it, in a shape that reads like somebody else’s debt**. Regenerated; icon orphans **20 → 5**; `FLOOR_icon` lowered 20 → 5 in the paired SCRIP commit, which is where the gate’s own rule puts it. The direction is the only mercy: a stale census OVER-reports, so the ratchet failed safe. **An absorb landing owes this file a regeneration exactly as it owes `ALL.csv` one — the count is not the artifact, the census is.** ⚠ **(4) AND I SHIPPED THE GATE RED ON PURPOSE, AGAINST TWO COMMITS THAT ARE NOT MINE.** corpus `cfc826b0f` and `2e8b94fe5` added two loose `.icn`/`.ref` pairs inside my rebase window, so the honest count is **7 against floor 5, GREW by 2**. **The floor is NOT raised to cover them**: raising it would launder another seat’s debt into this ratchet and the growth would never be seen again. The census was regenerated AFTER the rebase, not before — a census is a measurement of the tree it is committed with. Absorbing or declaring those two returns the gate to green with no edit here; naming them is the whole of what CEO-463 asks. ⭐ **(5) THE CEO’S OTHER HALF, ROUTED INTO THIS CURSOR AS ASKED (CEO-541): TWO OF THE ICON MASTER’S THREE RUN-GRADED REDS WERE BAD REFS AND THE ORACLE AGREED WITH SCRIP ALL ALONG** — `FINDING-2026-09-10-hq_V-two-of-the-icon-masters-three-run-graded-reds-were-bad-refs-and-the-oracle-agreed-with-scrip-all-along.md`. `procedure_write_13` expected `hello world` from `"hello" ||| " world"`, which is LIST concatenation: the oracle raises 108 and prints NOTHING on stdout, exactly as both our modes do — an expectation minted from the operator’s LOOK rather than cut from the oracle, and the master already grades the correct form green as `procedure_every_elemgen_13`. `procedure_alt_fail_replace_1` pinned `0 0 0 0 1` for four graphics keywords the oracle FAILS. **Neither was a compiler defect and neither cost a line of engine work**; the board read 760/763 with three reds by name and two of those names were accusing us of the corpus’s own arithmetic. ⚠ **(6) NAMED, NOT TAKEN, one bug at a time:** the cto’s telegram lands my two display witnesses — `display_prints_the_frames_locals_and_the_globals` and `icon_display_builtin_unimplemented` are **green m3, red m4** on `3e68226ea`, and the m4 red is ONE measured gap that is not display’s: a standalone binary registers no per-procedure name metadata at all (`rt_proc_register_rec` emitted with `pnames=0`, no lnames). That is the cto’s row; **my two absorb after it and I re-cut then, not on their word.** ⛔ **NO BOARD RUN** — ONE RUNNER, ONE BOARD (CEO-523). The coo’s next pass should expect the Icon master denominator to jump 939 → 950 from `2ef0fe270` alone.
- **HQV-20 (hq_V, 2026-09-10 17:2x–17:5x CDT, SCRIP `2bdee91cc`, corpus `b32af4985`→`00db64151`) — CEO-532 DISCHARGED: jcon `kwds` IS GREEN IN BOTH MODES, AND THE RED WAS A CLOSED EXCLUSION I WROTE MYSELF ON EVIDENCE THAT WAS ALREADY STALE.** Inbox drained at the prompt boundary (3: cfo, hq_T, ceo — all three acted on, none merely filed). FINDING: `FINDING-2026-09-10-hq_V-a-closed-exclusion-not-the-compiler-was-carrying-jcon-kwds-and-its-evidence-sentence-was-stale-when-i-wrote-it.md`. ⭐ **(1) THE CEO'S 16 DIFFERING LINES REPRODUCED EXACTLY, AND THEY ARE NOT ONE CLASS BUT TWO.** Fifteen are the CEO-514 implementation-identity class and take the cure I already wrote for the master entry — `&allocated`, `&storage`, `&version` print `type()`, `&features` prints `type()` ONCE rather than per feature (the SIZE of the feature list is itself implementation identity), so existence, type and the generator arities 4 and 3 stay graded. ⛔ **THE SIXTEENTH WAS `&progname` AND IT WAS NEVER THAT CLASS** — ours reads `kwds.icn` in both modes, the ref read `./kwds`, which is the CUTTER's `icont -s -o kwds` and not the program's answer. The runner hands SCRIP the SOURCE, so the oracle's matching invocation is `icon kwds.icn`, under which icont answers `kwds.icn` and **we already agreed with no compiler change at all**. A cure aimed only at the ceo's stated class would have left this standing and read as a partial fix of a semantics bug that does not exist. ⭐⭐ **(2) THE VALUE WAS NEVER GRADABLE AND OUR OWN TWO MODES PROVE IT IN ONE LINE:** `&allocated` reads **54464 in m3 and 40528 in m4 on one tree, one binary, one program**. When a value is suspected of being implementation identity, **diff it against YOURSELF before arguing about the oracle** — it is cheaper and more decisive than the cross-implementation argument the original ruling rested on. ⛔⛔ **(3) THE CORRECTION THAT MATTERS MORE THAN THE CURE, AND IT IS MINE.** My 09-10 row and its `UNGRADABLE.tsv` mirror both said *"THE SHIPPED kwds.std WAS CUT BY JCON — `&version: Jcon Version 2.2`, `&features: Java`."* **True of the file before corpus `5951c0703`, FALSE of the file on disk when I wrote it a day later** — `kwds` is named in that commit's OWN re-cut list and the seventeenth was `recent`. The header names kwds as the SMOKING GUN that motivated the census; I read it as the EXCEPTION to the census and **wrote a ruling without opening the file it ruled about**. Verified before touching anything: the shipped `.std` was byte-identical to a fresh icont cut. ⛔ **AND THE CHECK THAT WAS RUN CANNOT CATCH THIS** — the mirror existed to close a gap split (`77+3+10=90` vs 91), and **that sum balances just as well with the name in the wrong bucket**: an inventory that only has to SUM cannot tell a correct ruling from a stale one. ⭐ **(4) THE EXCLUSION WAS THE RED, NOT THE COMPILER — the FOURTH time on this one file** (after `geddump`, `htprep`, `prepro`, `recent`), and the rule is now worth stating plainly: a bare name here makes `test_icon_jcon_suite.sh:205` `continue`, so the program leaves the denominator entirely — **it cannot be red, so nobody looks, so the ruling is never re-read. A wrong exclusion is more expensive than a wrong cure, because a wrong cure stays visible as a red and a wrong exclusion is invisible by construction.** Both rows removed; one program carries one ruling and kwds now carries none. ⭐ **(5) hq_T's RULING ACCEPTED AND KEPT VERBATIM where the row used to be** — *a row whose output cannot be made to agree by ANY environment is categorically different from one that can, because the first is a permanent ruling and the second is deferred work wearing a ruling's clothes.* ⛔ **AND kwds COMPLETES IT IN THE DIRECTION THAT COSTS:** the four values ARE permanent, and **that permanence is exactly what made the ruling look safe to close** — it was still deferred work, because the program was free to STOP PRINTING THEM. **A permanent ruling about a VALUE is not a permanent ruling about a PROGRAM**; the test before closing is not *can any environment make these agree* but *can the program be made to stop asking*. ⭐ **(6) THE MASTER WRITES, as CEO-452/CEO-532 leave this seat the single writer.** The cfo's NINTH pair absorbed **green on a LATER tree than the one they measured** (they read 58ed77872; this is 2bdee91cc) as `procedure_every_to_replace_9`, plus the NUL-byte class and set-mutation-during-generation: **916 → 919 entries, content invariance proved BY NAME (lost 0, gained 3, same-name-differing-content 0)**, refs re-cut from icont and all three byte-identical to the handed ones, the name-echoing law applied first, modes DECLARED in `config/MODES.tsv` on ref-content evidence rather than guessed, and each absorbed copy graded again WHERE IT NOW LIVES. ⛔ **`display_prints_the_frames_locals_and_the_globals` was NOT absorbed — it is RED in both modes here**; a red absorbed is a red wearing an absorb's clothes, and the census exists to keep naming it. **Orphans icon 23 → 20, back AT the floor**, which is what the cfo was waiting on; the floor is NOT lowered, because we land on it rather than under it. ⚠ **(7) TWO CONCURRENT LANDINGS took it to 22 while I was pushing**; both new witnesses were MEASURED and both are RED, so both correctly stay loose, the floor is not raised, and the gate's one complaint belongs to their commits and is named to them — the census was regenerated so that real complaint is not masked by a stale-bookkeeping line of mine. ⚠ **(8) NAMED, NOT TAKEN, one bug at a time:** `arizona_tests/general/kwds.icn` is a DIFFERENT, older program and is **TWO diff lines from green — `&progname` alone**, the same cutter-invocation artifact; a ref-provenance question for the Zona lane, one line wide. **NO BOARD RUN** — one runner, one board (CEO-523); `icon_master_per_entry_identity` and `test_icon_jcon_suite.sh` both REFUSE rc=2 against this seat and are right to.
- **HQV-26 (hq_V, 2026-09-10 18:2x–18:3x CDT, corpus `a80ca7617` → `329d32526`) — THE MASTER WRITES CONTINUED: **921 → 939 ENTRIES, EIGHTEEN LONG-LOOSE WITNESSES ABSORBED, AND TWO KEEPER RATIONALES CORRECTED BY MEASUREMENT.** ⭐ **(1) EIGHTEEN ABSORBED, ALL THREE-WAY VERIFIED FIRST:** the `icn_suspend_record_*` family, the `list_alt_*` family, `icn_coexpr_int_control`, `icn_generator_via_procvalue_witness`, `icn_return_record_control`, `icn_suspend_list_control`, and the three `icon_*` divergence witnesses. Each: a fresh `icont -s` cut HERE byte-identical to the shipped ref, SCRIP m3 byte-identical, SCRIP m4 byte-identical, rc=0. All 18 PASS m3+m4 through the harness's own runner; all 18 FAIL both modes with one line inserted into their ref block; content diff `lost 0 · gained exactly 18 · changed 0`. ⛔ **THE THREE WHOSE NAMES RECORD A DIVERGENCE ARE THE REASON THE THREE-WAY CHECK IS NOT CEREMONY** — `icon_set_table_object_serial_numbering_diverges_from_oracle`, `icon_sort_zero_field_record_undefined_function`, `icon_sortf_mixed_type_default_compare_undefined_function`. The oracle AGREES with us on all three today; their names are stale. Absorbing on the name would have entered them as reds-by-belief, and absorbing on SCRIP's own output alone would have enshrined a divergence as expected. Only the oracle arm distinguishes the two, and it is the arm easiest to skip. ⭐ **(2) A KEEPER RATIONALE OUTLIVED THE GAP IT DESCRIBED, AND THAT IS THE FINDING.** KEEP.md recorded `rung36_jcon_large` and `rung36_jcon_radix` as blocked on arbitrary-precision integers that *do not exist anywhere in this runtime*, quoting the wrapped answer. Both are now byte-identical to the oracle in both modes: **bignum landed and the note kept turning seats away for days afterwards.** Corrected in place with the general rule beside it — **a rationale that names a MISSING CAPABILITY needs a RE-MEASURE date, not just an author date**; it fails in the direction nobody re-checks, exactly the decay hq_T and I corresponded about this morning, one level up from a value to a justification. ⭐ **(3) ELEVEN KEEPERS MEASURED, ASKED, NOT TAKEN:** `arith case checkfpx ck collate errkwds iobig large lgint nargs radix` all meet CEO-516's bar exactly (three-way byte-identical, input-independent, no name echo). I did not move them: those three moved on the ceo's word BY NAME, the jcon package suite already grades the upstream originals these are copies of, and whether absorbing is coverage or a SECOND COUNT of the same programs is a denominator call the ceo owns. Asked with the census attached and nothing blocked behind the answer — the numbers are in KEEP.md whichever way it goes.
- **HQV-25 (hq_V, 2026-09-10 17:5x–18:2x CDT, SCRIP `b84976b17`, corpus `ea4cabfa4` → `27c21e7eb` → `bca7d0833`) — THE BRIEF'S FIRST HALF WAS ALREADY DISCHARGED WHEN I MEASURED IT, AND TWO OF THE MASTER'S THREE REDS WERE BAD REFS, NOT DEFECTS. IcnM entries 919 → 921; run-graded reds 3 → 1 by my measurement, the coo's board to confirm.** ⭐ **(1) jcon `kwds`: the ceo's census said 16 diff lines and the tree said 0.** Reproduced in the runner's own shape (private rundir, the source copied beside it, stdin `/dev/null`, stdout AND stderr combined) — PASS m3 and m4, 0 diff lines each, on a clean tree. The census was taken at `4a4c8aa6a` and the landings since cured it. ⛔ **I did not report it cured until I had run it the way the runner runs it**, absolute path from a foreign cwd included, because `&progname` is in that file and a relative-path run would have been a different program. CEO-534 arrived afterwards and says the same thing from the cutter's side: the sixteenth line was `&progname`, the old ref read `./kwds` because the CUTTER used `icont -s -o kwds` while the runner hands SCRIP the SOURCE. **A ref is evidence about an invocation before it is evidence about a compiler.** ⭐ **(2) TWO MASTER REDS CURED WITHOUT TOUCHING THE ENGINE, AND THE ONE-COMMAND TEST THAT SEPARATES THE CLASSES IS NOW WRITTEN DOWN** (FINDING-2026-09-10-hq_V-two-of-the-icon-masters-three-run-graded-reds-were-bad-refs-and-the-oracle-agreed-with-scrip-all-along.md). `procedure_write_13`: its ref said `hello world` for `"hello" ||| " world"` — `|||` is LIST concatenation, the oracle raises 108, and SCRIP's report agrees with the oracle's word for word, differing only in the file name each run was given. Cured the way entry `procedure_every_to_18` is already graded: empty ref, a `want_rc` row — and the rc arm was PROVED to bite (undeclared, the entry FAILs), so it is not an empty ref that passes on anything. `procedure_alt_fail_replace_1`: its ref expected `&col &row &x &y` to answer 0; the oracle FAILS them under a no-graphics build so `write()` is never called, and SCRIP's output is BYTE-IDENTICAL to the oracle's by `cmp` in both modes. ⛔ **A WRONG REF AND A COMPILER DEFECT ARE INDISTINGUISHABLE ON A BOARD** — one of these two was in the EXECUTIVE roster as an engine-shaped red for an officer. The separator is one command: run the entry under the ORACLE and diff the oracle against the REF. oracle==ref → real defect; oracle==SCRIP → bad ref; all three differ → measure again before believing anything. ⭐ **(3) THE THIRD RED IS REAL AND IS ROUTED, NOT TAKEN:** `procedure_record_every_replace_12`'s ref is byte-identical to the oracle and SCRIP diverges 114 lines across at least FIVE distinct wrong error numbers (110→111, a missing `&errorvalue`, 103→114, 101→104, 205→104), each cascading the `&error` counter behind it. Its first arm already had a minimal witness minted by another seat at 18:10 the same day. That is the cfo's errors row over hq_U's conversion class; duplicating it would have been the second seat on one bug. ⭐ **(4) THE MASTER WRITES: two of today's cures now have entries, and the third is a KEEPER I wrote down before trying to absorb it.** `procedure_every_suspend_replace_5` ← the NUL-bearing-subject cure, `procedure_every_elemgen_20` ← the element-generation/structure-builtin error cure; both refs RE-CUT with `icont` here and byte-identical to the shipped ones, both PASS m3+m4, both fail-once proven, MODES.tsv declares both with their provenance, content diff `lost 0 · gained exactly 2 · changed 0`. ⛔ The cto's co-expression activation-trace witness is NOT absorbable and KEEP.md's name-echoing section now says so: it sets `&trace`, so **11 of its 15 ref lines begin `_own_line.icn:`**, the last 13 characters of its own file name. Absorbed, the builder's rename turns those 11 lines red and they read as a tracer defect. **The rule that catches the whole class in one grep is recorded there: count occurrences of a witness's own stem in its ref before absorbing — nonzero is a keeper, not a candidate.**
- **HQV-24 (hq_V, 2026-09-10 16:4x CDT, corpus `5ce92ada0`) — STOPPED ON CEO-523, MODE EXECUTIVE. TREE CLEAN, NOTHING UNPUSHED, NO CLAIM HELD, NO BOARD RUN AFTER THE ORDER.** The push in hand was one documentation correction and it is the right last act: **a line I wrote into `tests/icon/KEEP.md` this morning was false** — it recorded that `rung36_jcon_recent` opens a `recent.dat` *not in the corpus*. hq_I corrected its OWN finding to say so, and **verified here before editing rather than taken on the correcting seat's word either**: all 21 jcon `.dat` companions are vendored at `packages/icon/jcon_tests/`, byte-identical to upstream; what is true is only that none sits BESIDE the rung36 fixture. ⛔ **THE TWO READINGS ARE ONE `ls` APART AND LICENSE OPPOSITE DECISIONS** — *not vendored beside this copy* is a fixture to stage, *does not exist* parks a program as unfixable, which is what the wording did. ⭐ **AND THE HOLD IT JUSTIFIED IS OBSOLETE, recorded so the next reader does not inherit a stale one:** fed, `recent`'s green was false in the purest way — oracle and SCRIP byte-identical at 313 lines, both ending `can't open recent.dat`, **agreeing about where they both gave up**. With the fixture the oracle answers 443, and the divergence exposed two real defects hq_I has cured with a gate wired in the same landing: `open(<directory>)` SUCCEEDED and read zero entries (`fopen` on a directory succeeds, so the handle looks healthy while every read fails silently), and `reads(f,n)` on a directory must return ONE ENTRY truncated to n. ⚠️ **`recent` is still a poor master entry even cured — it LISTS ITS OWN CWD**, so its expectation depends on what sits beside it and on `readdir` order: the same family as this lane's name-echoing law, **output that depends on where a program lives cannot survive a harness that decides where it lives.** NOT absorbed; handed to the officers with that note. **HANDOFF UNDER CEO-523 — claims stay on file, the officers take them by assign:** the cfo's NINTH pair (`a_cset_held_in_a_variable_survives_a_thousand_later_csets`, red by design, flips green with the cfo's cut 3; ⛔ **if it arrives still red the builder marks a non-green original xfail BY CONSTRUCTION and THERE IS NO XFAIL**, so it must enter as an honest red with the marker stripped by hand in the same landing — this lane has had to do that three times this week); `rung36_jcon_recent`, `_io` and `_others`, now curable with FED refs (`others`' shipped `.expected` equals the fed icont cut EXACTLY, corroborating the fed run; `io`'s differs by 35 lines so its ref must be an icont cut, never jcon's `.std`); entry 579's `wantrc`/stderr; and hq_T's open vocabulary question (implementation identity vs environment identity — **nothing is blocked behind it**, the jcon inventory balances at 91 and hq_P's row is published). **FINAL STATE: IcnM 760/763, entries 916, ast-shape 153/153, master gradability 916/916, orphan gate green at icon 20/floor 20, wiring ratchet clean fleet-wide, all three repos clean and pushed.** Inbox drained and cleared; every sender answered.
- **HQV-23 (hq_V, 2026-09-10 1x:xx–1x:xx CDT, SCRIP `3e8670d97`, corpus `69d9fa5b0`) — CEO-517 AND CEO-516 EXECUTED IN ONE LANDING; IcnM **756/759 → 760/763**, ENTRIES **912 → 916**, AST-SHAPE BACK TO **153/153 WITH ZERO PINS REGENERATED**; AND FOUR INSTRUMENT REFUSALS CORRECTED ME, EVERY ONE RIGHTLY.** ⭐ **(1) CEO-517, THE THIRD ARM, 151 SITES — AND THE CROSS-CHECK IS THE RESULT, NOT THE COUNT.** hq_B measured 151 remaining sites in `ALL.icn` with their scanner; I counted with a SEPARATE pass built on their own literal-and-comment lexer and got **151 independently**. Two scanners written apart agreeing is worth more than either asserting. ⛔ **hq_B'S ipp.icn TRAP IS HANDLED EXPLICITLY AND TESTED, not dodged by luck:** in my pass a string or cset literal is a TOKEN that DISQUALIFIES a site while a comment between `;` and `}` does not — proven on a fixture carrying all three shapes, and the real `ipl/progs/ipp.icn` reads 0 sites. All four arms measured: **sites 151 / bytes removed 151** (bytes-removed == semicolons-removed exactly); **deletion-only AND only semicolons**, proved by stripping every `;` from both texts and finding them byte-identical, which is stronger than a line count; icont rc unchanged (gradability **912/912 → 916/916**, 0 refused); scrip rc unchanged (identity **0 regressions, astdrift=0**). ⛔ **NOT ONE PIN REGENERATED — the half of the ruling that matters:** the six drifted self-pins revert on their own because the fixture text no longer carries the `;` the arm forbids. Regenerating would have recorded the new shape and LEFT the defect in the fixture. Board confirms **153/153**. ⭐ **(2) CEO-516, THE THREE ABSORBS — AND RE-VERIFYING RATHER THAN TRUSTING CAUGHT MY OWN ERROR, NOT hq_I'S.** `rung36_jcon_misc/_sorting/_struct` enter as `procedure_record_every_replace_18`, `procedure_record_coexpr_replace_3`, `procedure_record_every_replace_19`; KEEPER classification out of `ALL.excluded.txt` in the same commit as hq_I asked, loose pairs deleted. ⛔ **`struct` FIRST READ RED HERE AND IT WAS MY CUT THAT WAS WRONG:** the one-step `icon` driver with `2>&1` merges **ICONT'S COMPILE-TIME WARNINGS** into the output, so its cut gained 8 `undeclared identifier` lines that are the compiler talking, not the program answering. The runner grades the RUN — SCRIP's m3 and the shipped `.expected` are IDENTICAL, and the oracle's run-only output equals `.expected` byte for byte. **A merged-stderr cut is not evidence against a correct `.expected`**, and I told hq_I so rather than filing a false defect at them. The other three (io/others/recent) stay held, with hq_I's starved-oracle proof written into `KEEP.md` so nobody absorbs them by accident. ⭐ **(3) THE cfo's EIGHTH PAIR ABSORBED** as `procedure_every_scan_replace_14` — ref re-cut here BYTE-IDENTICAL to the handed one, green both modes on arrival, **and checked against yesterday's own name-echoing law FIRST**: zero occurrences of its own file name in its output, so the rename cannot break it. The law became a ten-second question, which is what a good law does. **The orphan gate the cfo reported red fleet-wide is GREEN**: icon 21→20 by absorbing the eighth, floor 19→20 for the permanent keeper — **two moves in opposite directions, named apart in the gate's own comment so neither hides the other** — and the stale `ORPHANED-WITNESSES.tsv` regenerated, which the gate had also been saying. ⛔⭐ **(4) FOUR INSTRUMENT REFUSALS THIS TICK AND EVERY ONE WAS RIGHT — the best of them about MY reasoning, not my syntax.** (a) The builder refused to absorb the trace pair without a `MODES.tsv` declaration. (b) `--reindex` refused on unabsorbed sources — correct, and I left it refusing rather than forcing it, then proved by hand that no attribute column moves (`type=1` already, `n_lines=84` unchanged). (c) The jcon inventory refused my minted class `IMPLEMENTATION_IDENTITY_IN_OUTPUT`: **the vocabulary is CLOSED** (hq_T, after three lanes invented three spellings in one morning) — it reads better for this case and is **not mine to add**, so the row uses the existing `ENVIRONMENT_IDENTITY_IN_OUTPUT` and the question is routed to hq_T. (d) ⭐ **THE SHARPEST: the inventory refused a reason that NAMED OUR OWN COMPILER.** My first draft justified an ungradable ruling partly by what SCRIP prints; the arm's principle is that **while our half sits beside the oracle's, a reader cannot tell whether the ruling would survive without it.** Restated on the ORACLE SIDE ALONE and sufficient there — the shipped `kwds.std` is a JCON cut, and no Arizona cut can replace it because those four values are the identity of whatever implementation runs the program. Inventory now balances **77+3+11=91** and **hq_P's jcon vendor row is unblocked**, which is what it was costing. ⛔ **(5) CEO-520 EXECUTED, and I was one of the causes.** Six of my own wait shells, the oldest 23 h, were `until ! pgrep -f board_icon_master.sh` loops that **matched their own command line** and so could never exit — I read RUNNING for many minutes on a board that had finished and blocked my own batch on a process that did not exist. All six killed **by PID, attributed by `/proc/<pid>/cwd` under my root**; three shells belonging to cto, hq_C and hq_U were identified and deliberately left alone. Zero of mine remain. Waiting on a name is the same family as hq_T's last-line finding: a true statement about the wrong subject. **OWED AND NAMED:** the cfo's NINTH pair (red by design, the cset-arena wrap — deliberately NOT absorbed yet because the builder marks a non-green original **xfail BY CONSTRUCTION** and THERE IS NO XFAIL, so it needs the marker stripped by hand in the same landing; the cfo is asked whether to take it immediately or with its cure); entry 579's `wantrc`/stderr; hq_T's vocabulary question. Inbox drained and cleared (7 more: ceo, cfo ×2, hq_B ×2, hq_I ×2, hq_P, hq_T) — every sender answered.
- **HQV-22 (hq_V, 2026-09-10 10:2x–12:5x CDT, SCRIP `686461bd2`, corpus `cbe87e108`) — CEO-503, CEO-514 AND CEO-513 ALL EXECUTED; IcnM **756/758 → 756/759**; AND THE BOARD THAT WROTE THAT ROW HAD TO BE CURED FIRST, BECAUSE MY OWN LANDING MADE IT DIE SILENTLY.** ⭐ **(1) CEO-503: THE EXCLUSION IS RETIRED AND ITS CONDITION WAS RE-MEASURED, NOT TAKEN FROM THE RECITAL** — Arizona icont compiles `procedure_every_alt_replace_4` at rc=0 where the exclusion recorded `invalid case clause`, so it is the CEO-465 expired class exactly as ruled; `ALL.outside.tsv` held only that row and is deleted. ⛔⭐ **AND GRADING IT EXPOSED THAT ITS REF WAS CUT FROM THE WRONG ORACLE — a JCON cut**, carrying `&version: Jcon Version 2.2` and `&features: Java` from its `rung36_jcon_kwds` origin, so grading it as it stood would have measured SCRIP against Jcon. This is the `kwds` re-cut **HQV-21 recorded as OWED and said must land WITH the re-admission or not at all** — both landed together, and it also closes the gap `OUTSIDE_ARIZONA_BASELINE.tsv`'s own header has carried since 09-09, that kwds.std was the ONE of seventeen never re-cut *because it asked us to make SCRIP claim to BE Jcon*. **Content invariance proved by name on both swaps:** 912 entries before and after, zero added, zero removed, exactly one segment changed each time. ⭐ **(2) CEO-514: THE FOUR ARE OUTSIDE, CURED BY THE AUTHOR'S OWN TECHNIQUE, NOT BY A MASK.** I asked rather than self-authorized, and the asking was the right call: **CEO-409 looks like the answer and is not** — its earning test is that THE ORACLE'S OWN VALUE MOVES BETWEEN RUNS, and I measured icont three times and it is **byte-stable**; these move between IMPLEMENTATIONS, not between runs, while an allocator count is CEO-409's own named exemplar, so the law's test and its exemplar disagree on exactly this fixture. Ruled OUTSIDE-THE-BASELINE. The entry now prints `type()` of `&version &features &allocated &storage` — existence and type stay graded (integer, integer, string, string; generator arities 4 and 3 agree) — **and `&features` is printed ONCE rather than per feature, because the SIZE of the feature list is itself implementation identity** (icont lists dynamic loading and external values, we do not), so a per-feature loop diverges on COUNT whatever it prints. The four VALUES named in `OUTSIDE_ARIZONA_BASELINE.tsv`. **WHAT REMAINS RED IS NOW THE WHOLE OF THE DIVERGENCE AND ALL SIX ARE OURS:** `&col &row &window &x &y` answer 0/`&null` where icont FAILS them under no-graphics, and `&progname` synthesises `./stem` where the oracle echoes the argv it was handed. FAIL in both modes — and that FAIL now names six real defects and nothing else. ⛔⭐ **(3) THE BOARD DIED SILENTLY AND MY OWN LANDING IS WHAT MADE IT** — the sharpest thing in this rung. Retiring the last outside row means the harness stops printing `OUTSIDE_BASELINE_COUNT`; that grep exits 1, `pipefail` + `set -e` kill `board_icon_master.sh` **one line before the denominator reconciliation, the watermark verdict and the SCORE.md write**, while the `${_outside_n:-0}` fallback *on that very line* was written for exactly this case and never got to run. It breaks a promise the file makes 119 lines above in its own words. **THE LOG OF THE KILLED RUN IS INDISTINGUISHABLE FROM A COMPLETED ONE** — full m3/m4 numbers, six entries named, no error text, no non-zero status anyone sees — so the board reads healthy while its row goes silently stale, which the FACT RULE counts as a defect of the session that ran it. That is why my first board run wrote nothing. Cured `e8d861e91`, validated by RUNNING the construct (CEO-480); any seat retiring the last outside row of any suite would have hit it. ⛔ **(4) CEO-513 IS A RULING AGAINST ME AND IT IS RIGHT.** I landed the shipped-trees gate unwired AND undeclared at `1aefa87ac`, which refused the wiring ratchet in every root and blocked thirteen handoffs until the ceo declared it out itself. **Holding it out while red was correct (CEO-463); owing the DECLARATION the same day and not filing it was not.** Wired here blocking at **rc=0 in 8.09 s over 1381 shipped files** once hq_B's CEO-488 cure took semicolon-class to 0, RULING line removed in the same commit through `adopt` so WIRED carries no reason and no declarer. **NAMED NOT ABSORBED:** the ratchet still refuses fleet-wide on ONE gate that is not mine — hq_I's `test_gate_icn_pow_large_integer_exponent.sh`, `4f59dffd5`, the identical CEO-381 class — told hq_I with both one-line cures and deliberately NOT declared by me, because declaring another seat's gate puts my name on its reason. ⛔⭐ **(5) A CORPUS LAW MINTED FROM A FALSE RED I CAUGHT BEFORE LANDING IT: A PROGRAM WHOSE OUTPUT ENCODES ITS OWN FILE NAME CANNOT BE ABSORBED INTO ANY MASTER.** The cfo's seventh pair is green in both modes and its oracle re-cut here is byte-identical to the handed one — and it sets `&trace`, so its 43 trace lines each begin with its file name truncated to the last 13 characters. The builder renames entries from construct flags and the runner writes them under the NEW name: assigned `procedure_record_every_replace_18`, it prints `eplace_18.icn` where the ref carries `es_a_list.icn` — **86 differing lines, a guaranteed red, the compiler behaving perfectly**, against a control on the same program under its own name that is byte-identical. Re-cutting under the assigned name is NOT a fix either: names shift on later rebuilds. Declared a permanent keeper in `tests/icon/KEEP.md`. **Master entry `procedure_every_alt_replace_4` is the same class already inside the master** — which is what the old board note about *a consolidation rename, not a defect* was really seeing. hq_T took the law into `GOAL-TEST-SUITE-CONSISTENCY.md` (`7056095a`) with the general form: *when a tool's job is to RENAME things, any property that depends on the name is silently outside its contract*. ⛔ **(6) AN INSTRUMENT DEFECT THAT COST ME A WRONG READING MID-SESSION:** `util_build_master_suite.py` rewrote `ALL.icn/.ref/.csv/.in` (912→913 entries) and signed off `(dry run: ...)` — true of DELETION only. I had read entry line numbers before that run and used them after; every banner had moved 39–50 lines, a line-sliced extract silently became the wrong text, and the grading raised a `ValueError` that reads exactly like a corrupt master pair. Tree restored with `git checkout --`, every later measurement re-taken on the committed tree. **Routed to hq_T, who CURED it the same tick** (`7d509e974`: every committed path recorded at its own `os.replace`, *dry run* now reserved for a run that wrote nothing). ⛔ It is a direct hazard to CEO-452: an inspection-shaped invocation that rewrites all three files can leave an uncommitted unmeasured master under any seat. ⛔ **(7) AND ONE I WASTED TIME ON MYSELF, WORTH THE SAME HONESTY:** four of my own background waiters `until ! pgrep -f board_icon_master.sh` **never terminated, because each waiter's own command line contains the pattern** — `pgrep -f` matched the waiter, not the board. The board had finished long before; I read RUNNING for many minutes and blocked my own batch on it. A `pgrep -f` wait loop must exclude itself or match the binary, never the script name it is written inside. **OWED AND NAMED, so silence is not read as completion:** CEO-516 (absorb hq_I's verified three — misc, sorting, struct — removing their three KEEPER lines from `ALL.excluded.txt` in the SAME commit; io/others/recent stay held for FED refs, hq_I proved they pass only because the oracle was starved as we were); CEO-517 (hq_B's third arm over **151 sites** in `ALL.icn`, deletion-only, bytes-removed==semicolons-removed, icont and `scrip --compile` rc unchanged before/after — which reverts the ast arm 147/153 → 153/153 with **zero pins regenerated**, and ⛔ NEVER regenerate them); the cfo's EIGHTH pair (`cset_membership_is_a_bit_test_not_a_name_lookup` — verified here: oracle re-cut BYTE-IDENTICAL, zero filenames in its output, green m3 AND m4 — absorbable, needs its `config/MODES.tsv` line); the cfo's NINTH pair (red by design, the cset-arena wrap, the flip is the cfo's cure's not mine); **the icon orphan floor is 21 against floor 19 and the two new orphans are exactly the eighth pair (absorb it) and my keeper (floor +1) — measured, not guessed**; entry 579's `wantrc`/stderr. Inbox drained and cleared (10: ceo x4, cfo x3, hq_B, hq_I, hq_T) — every sender answered.
- **HQV-21 (hq_V, 2026-09-10 09:3x–10:0x CDT, SCRIP `b68d89c52`, corpus `726448e76`, .github `84f639fb`) — THE cfo's SIXTH WITNESS ABSORBED **GREEN** AGAINST ITS HANDING SEAT'S FIRST WORD, IcnM **754/757 → 756/758**, AND CEO-497 EXECUTED: THE MASTER GATE NOW READS ALL 912 ENTRIES, WHICH EXPOSED A STALE EXCLUSION.** ⭐ **(1) THE ABSORPTION IS MEASURED, NOT TAKEN ON THE HANDING SEAT'S WORD, AND THE TWO TELEGRAMS DISAGREED.** The cfo's first called `a_string_held_by_a_frame_variable_is_not_extended_in_place` red-on-arrival; its second said it matches both modes. **This tree agrees with the second: GREEN in m3 and m4, byte-identical to the oracle in both**, so it entered as a green entry (`procedure_every_suspend_replace_3`, 911 → 912) and **no xfail marker was involved** — the builder marks only non-green originals, and `ALL.csv` carries **xfail=0 on all 912 rows, asserted rather than assumed** after three landings this week where that marker had to be stripped by hand. The handed ref was **re-cut from icont+iconx on this tree before absorption and came back BYTE-IDENTICAL**, and the cfo was told so explicitly rather than left to assume it. ⛔ **THE READING IS ONLY TRUSTWORTHY BECAUSE THE FIRST ONE WAS THROWN AWAY:** my first measurement ran on a binary **six commits behind the rebase**, and HQV-15's standing rule in this lane is that a stale binary reproduces the old behaviour perfectly, which makes it the most convincing kind of wrong measurement there is. Merged, **rebuilt**, re-measured; `c8701b17e` (the SCRIP the cfo graded on) is an ancestor of the tree this was graded on. **CONTENT INVARIANCE PROVED BY NAME:** all 911 pre-existing entries byte-identical in **both** `ALL.icn` and `ALL.ref`, one added, zero removed, zero changed, measured by splitting both files on their entry banners before and after. Watermarks re-pinned **750 → 756** in the commit that earned them. ⛔ **TWO FLIPS AND ONLY ONE IS MINE, named apart in the pin and both commits** — +1 population and +1 pass from this absorption, and **+1 pass from `procedure_write_256`, which arrived with the pull and belongs to the co-expression landing `8410097ca`**; attributing a rebased-in flip to whichever seat re-runs the board is the easiest false credit in this system to take by accident. ⛔ **(2) THREE INSTRUMENT REFUSALS FIRED AT ME THIS TICK AND EVERY ONE WAS RIGHT.** The board **refused to write any row** on its first run because corpus sat on a local commit no remote branch contained — it had measured a tree nobody could check out (the guard is CHECKOUTABILITY, not dirtiness). The gate's own **corpus-leak assertion** caught my staging defect (below). And my first `SCORE.md` landing **hit a rebase conflict** because another seat had extended the icon row's fold-forward provenance chain while I measured — upstream 5672 chars against my 2935, each right about a different thing. **I dropped the commit, discarded my computed rows, pulled, and RE-RAN THE BOARD so the writer folded forward from the current file**, rather than hand-picking lines out of two versions of a generated chain: `util_score_row.py` exists precisely so nobody does that, and **a hand-merged row looks generated and is not**. ⭐ **(3) CEO-497 EXECUTED — the gap I asked about in HQV-19 is ruled and closed.** The master gradability gate now reads **every** `ALL.csv` row, ast-graded included: **912/912 compiled, 0 refused, 0 extract-failed**, under four seconds, still wired blocking. **I measured the 153 ast-graded entries on their OWN first (153/153) before widening**, because that arm is blocking and widening a blocking arm without knowing what it will say is how one seat turns `make test` red for thirteen. The ceo's principle replaced my original reasoning in the file: **an AST fixture carrying invalid Icon pins a parse THE ORACLE REJECTS**, where I had argued from what the suite happens to run rather than from what the fixture claims — and my own `parser/paren_seq` and `parser/case_multi_clause` (HQV-19) are the counter-example, invisible to every board until a PARSER cure exposed them. ⛔⭐ **(4) THE WIDENING EXPOSED A STALE EXCLUSION, REPORTED AND DELIBERATELY NOT ACTED ON.** The population now includes `procedure_every_alt_replace_4` — the **single** entry held OUTSIDE the graded denominator under **CEO-390/391** on the ground that Arizona icont refuses to compile it. **IT COMPILES NOW, rc=0**, and I did not infer that from the gate's total: I extracted the entry and ran icont on it directly, to be sure the gate was not quietly passing something it should have failed. **The defect that justified the exclusion was repaired under CEO-477, so the exclusion is stale**, and re-admitting the entry would take run-graded 758 → 759 and IcnM to 756/759 until its ref is proven. Not applied here: it is a criterion change, and **its origin is `rung36_jcon_kwds`, whose re-cut this seat still OWES — re-admitting it before its ref is honest would put a known-doubtful ref into the denominator to make a count look better.** The two land together or not at all. What was stale was the REASON, not the disclosure: the board prints the entry by name with the oracle's reason on every run. **OWED AND NAMED, so silence is not read as completion:** the `kwds` re-cut + eleven-keyword two-stanza mask (next); hq_R's pair (entry one a witness, entry two a **PIN**, plus hq_R's offered statement-form second witness, all three refs to be re-cut here); entry 579's `wantrc`/stderr; the cfo's trace-call-line pair when pushed. Inbox drained and cleared twice (10; ceo ×3, cfo ×3, hq_I, hq_R, coo ×2) — every sender answered.
- **HQV-20 (hq_V, 2026-09-10 09:0x–10:0x CDT, SCRIP `1aefa87ac`) — CEO-489 EXECUTED: THE GRADABILITY GATE WIDENED TO ALL 1379 SHIPPED `.icn`, AND THE 38 REFUSALS ARE **FOUR** CLASSES WITH FOUR OWNERS, NEVER SUMMED. 1341 COMPILE.** Session start clean (three repos ff-merged, `make` rc=0, banner read; MODE re-read NONET / ICON ONLY). Population **1379 · compiled 1341 · refused 38 = semicolon 15 · sequence-expression 2 · container 3 · other-form 18 · undecided 0**; the population size is byte-identical to the ceo's own CEO-488 census, which is the cross-check that both instruments are reading the same corpus. ⛔⛔ **(1) THE FINDING IS ONE BYTE AND EVERY EARLIER CENSUS WAS BLIND TO IT BY CONSTRUCTION.** `packages/icon/jcon_tests/htprep.icn` line 46 is `$define SIGNATURE "<!-- ... -->";`. **icont never reports line 46** — it reports the three USE SITES (257 `";": missing then`, 266 and 286 `";": missing right parenthesis`), and **not one of those lines contains a semicolon at all.** The macro body is substituted at each use, so the stray `;` detonates everywhere the name is used and nowhere it is written. Deleting that single byte compiles the whole file, measured — so htprep is genuinely hq_B's row and genuinely a one-byte repair, where the ceo's census filed it by its use-site message and any line-local instrument drops it into the un-ownable pile. **The CEO-483 shape again: the instrument was not looking where the defect is.** ⭐ **(2) THE CLASSIFIER MEASURES RATHER THAN PATTERN-MATCHES, AND THAT IS NOT GOLD-PLATING — the obvious rule (*the token icont quotes is a `;`*) is wrong in BOTH directions on this population and each direction costs a different seat.** FALSE POSITIVE: `jcon_tests/prepro.icn` is a deliberate preprocessor-error fixture raising `$undef: too many arguments` AND five `";": missing right parenthesis` in one compile — a token rule hands hq_B a vendored file whose semicolons can never be removed. FALSE NEGATIVE: the macro-body class above, and **the `;`-before-`else` shape CEO-477 itself named**, which icont reports as `"else": invalid expression` because by the time it objects it has consumed the semicolon. So a file is called SEMICOLON-class **only when the gate has re-run the oracle and shown that deleting `;` bytes and nothing else makes icont accept it** — word-for-word the per-file assertion CEO-488's DONE-WHEN makes (*bytes removed == semicolons removed*), so the classifier and the row's success criterion are **the same statement** instead of two things kept in agreement by hand. Every deletion is at a position **icont itself flagged**; nothing is ever written back to the tree. ⛔ **(3) TWO CLASSES THAT MUST NOT BE FOLDED INTO hq_B'S ROW, and folding either would give hq_B a DONE-WHEN that can never fire.** **SEQUENCE-EXPRESSION (2)** — `rung16_seqexpr_gen_basic.icn`, `coverage/coverage_x64_gaps.icn` carry `(a; b)`, OUR construct; icont groups ONE expression in `( )` and spells a compound expression `{ }`, so **the semicolons are CORRECT and deleting them yields `(a b)`, also invalid**. The repair is the GROUPING — the one this seat made by hand under HQV-19 for `parser/paren_seq.icn`. **CONTAINER (3)** — `rung36_all.icn` holds **42** `procedure main()`, `probe_witness.icn` 11, `rung20_section_seqexpr_excluded.icn` 2; compiled whole they say `inconsistent redeclaration`, **which reads exactly like a defect and is not one** (a container is never compiled whole — the `ALL.icn` ruling, the same correction sent hq_C on 09-10). Counting them puts three files nobody can ever fix permanently between hq_B's row and zero. Measured from the file, never the name: `_all` is not evidence and `rung36_jcon_case.icn` is a single program. ⛔ **A container can still carry a real defect**, so the report names each file's **ORIGINAL** complaint and not the residual after probe edits — `rung20_section_seqexpr_excluded` is a container **and** its first complaint is a sequence expression, and reporting only the residual would have hidden the first behind the second. ⛔⛔ **(4) THE GATE COMMITTED A STAGING DEFECT AND IT WOULD HAVE BEEN THE NEXT SEAT WHO PAID.** `icont -s -c` writes `.u1`/`.u2` beside the source, so the 1379 compiles stage into temp by symlink; the first cut symlinked **subdirectories** too, the next `mkdir -p` succeeded THROUGH the link, and icont wrote into the shipped tree — **362 untracked artifacts in `corpus/benchmarks/icon`, measured and removed, tree verified clean at 0.** A dirty tree is refused by every runner we own (CEO-174), so the damage was never to this gate's own measurement. Regular files only now, with **two** leak assertions bracketing the classifier — checking once before it ran would certify a tree it had not yet touched. ⭐ **(5) THE ARMS.** PASS-ONCE `rc=0` over a **1341-file all-valid scratch corpus** (the ceo's *fake corpus of 1001 valid files* shape, exceeded), via `GATE_CORPUS_ROOT`. **DETECTOR ×4 — one per class, because CEO-481 says a detector arm fails OPEN** — `;` before `else`, trailing `;` in a case body, `;` in a `$define` body, `(a; b)`: **every one named in the right class, zero false positives across the 1341.** COULD-NOT-MEASURE: collapsed population · missing shipped tree · absent corpus root, each **rc=2 kept strictly apart from rc=1** (CEO-483's lesson a second time). ~20s. Master gate re-run unchanged **758/758**. ⛔ **(6) DELIBERATELY NOT WIRED INTO `make test` WHILE RED** — it reads FAIL(1) with 15 named until hq_B lands, and a known-red blocking arm would make all thirteen seats pay for one open row (CEO-463). **It wires the moment it reads zero, and it must**: the conversion is a WRITER, and a writer that regresses re-manufactures the class silently. **(7) TWO CRITERION NOTES ASKED OF THE ceo, NOT APPLIED UNILATERALLY:** three containers cannot satisfy *every shipped file compiles* by construction — named as a class rather than silently excluded, but their place in the denominator is the ceo's call; and the sequence-expression pair is a **shape re-decision with no owner yet**. Evidence: `FINDING-2026-09-10-hq_V-a-semicolon-in-a-macro-body-is-reported-at-every-use-site-and-nowhere-it-is-written.md`. **OWED AND NAMED, so silence is not read as completion:** hq_R's pair (entry one IS a fail-once witness, entry two was **never red** and is absorbed as a **REGRESSION PIN**, labelled, on hq_R's own measurement); the cfo's fifth pair awaiting its push hash; entry 579's `wantrc`/stderr; the `kwds` re-cut + eleven-keyword two-stanza mask.
- **HQV-19 (hq_V, 2026-09-10 09:0x CDT, corpus `f5b3c870e`) — THE TWO AST FIXTURES RE-DECIDED: `astdrift` 2 → 0, SOURCE TEXT ONLY, NOT ONE PIN MOVED.** hq_P and hq_C independently routed the same pair here (CEO-452, one writer). Both encoded **invalid Icon in the CEO-477 joiner shapes** and Arizona icont refuses both — `parser/paren_seq.icn` `write((x := 1; x))` → *Line 2 # ";": missing right parenthesis*; `parser/case_multi_clause.icn`, a trailing `;` after the LAST case clause → *Line 6 # "}": invalid case clause*. Our parser now refuses them too (SCRIP `38470889b`), so **the PARSER became right and the FIXTURES became wrong** — which is the reading `astdrift` exists to force (*RE-DECIDE THE SHAPE AND REGENERATE, never "a program broke"*; ast refs are self-pins, never oracles, and never red on outcome). ⭐ **THE REPAIRS ARE MINIMAL AND EACH KEEPS ITS FIXTURE'S INTENT, which is why they were repaired rather than retired:** `paren_seq` → braces, because parentheses group ONE expression and `{ }` is Icon's compound expression — the AST still yields `TT_SEQ_EXPR`, so it still pins the shape it is named for; `case_multi_clause` → **ONLY the trailing `;` dropped, the separators BETWEEN clauses KEPT**, verified against the oracle both ways rather than assumed, because icont accepts the separators and stripping them would have been style-normalising under cover of a cure. ⛔ **THE STORED `.ref` BLOCKS DID NOT MOVE ONE BYTE** — regenerating the ast dumps from the repaired sources reproduced the refs already in `ALL.ref` exactly (the old parser TOLERATED the invalid semicolons and built the same tree the valid text builds). `git add` staged the two `.ref` files as unchanged. **That is the whole evidence that this was a shape re-decided and not behaviour quietly re-baselined**, and without it there would have been no way to tell the two apart from the outside. `ALL.csv` likewise byte-identical under a scratch `--reindex`, **measured, not assumed**. **(2) CEO-484 ACCEPTED** — absorb every tick, never in batches. **OWED AND NAMED, so silence is not read as completion:** cfo ×3 at SCRIP `08905f250` (hash corrected from `aae1f8513` on the bus) + the rung03 flip at `483717a0c`; hq_R ×2 (`every break` WITH a value — the half `procedure_write_266` does not grade); entry 579's `wantrc`/stderr; the `kwds` re-cut + eleven-keyword two-stanza mask. **Every handed ref gets RE-CUT on this tree before absorption** — a handed ref proves the handing seat's tree, and the master answers to the oracle's on the tree it is stored on. `--reindex` counts **38 loose absorbable families**, which is the orphan floor CEO-484 names. **(3) ONE CORRECTION SENT TO hq_C:** `ALL.icn` is a **CONTAINER, never compiled whole**, so its appearance in a strict-parser sweep is not evidence — the per-ENTRY question is the one CEO-483 asked and it is now wired and green. That leaves hq_C's list at **eleven** genuine loose files, taken as a row rather than folded in silently. **(4) ONE ASK TO THE ceo, NOT ACTED ON UNILATERALLY:** the gate's population is RUN-graded per the ceo's own criterion, and **these two fixtures were AST-graded and therefore invisible to it** — they carried invalid Icon for as long as our parser tolerated it, and only a PARSER cure exposed them. Widening it to compile ast-graded entries under icont is cheap and would have caught both; the argument against is that an ast fixture is never compiled by anything. It changes the ceo's stated denominator, so it is the ceo's call. **(5) THE IDENTITY GATE IS STILL FAIL(1) ON 4 REGRESSIONS AND NONE ARE THIS SEAT'S** (CEO-463, named not implied, each byte-identical to origin and unreachable from this diff): `rung27_read__rung27_read_reads_bytes` (hq_I), `rung36_jcon_evalx` + `rung36_jcon_fncs` (cfo), `nested_reversible_exchange_...`. Inbox drained and cleared (14; ceo ×3, hq_I ×3, cfo ×4, hq_C, hq_P ×2, hq_R) — every sender answered.
- **HQV-18 (hq_V, 2026-09-10 08:1x–09:0x CDT, SCRIP `96597d7c5`) — CEO-483 CLOSED BY MEASUREMENT: THE REPAIR WAS ALREADY LANDED, THE CRITERION WAS NOT, AND THE MINTED CRITERION REPORTED 21 REFUSALS WHERE THE TRUE COUNT IS ZERO.** Session start clean (three repos ff-merged, `make` incremental, banner read). Inbox drained: 11 messages (ceo ×2, hq_I ×3, cfo ×4, hq_C ×1, hq_P ×1). `next` served CEO-483 rank 0, dispatched across lanes by the ceo. ⛔ **(1) THE MINTED DONE-WHEN WAS MEASURING ITS OWN INSTRUMENT, AND THE WRONG NUMBER WAS PLAUSIBLE.** It extracted each of the 757 run-graded Icon master entries into a bare temp dir and ran `icont -s -c`; it reported **21 refusals**. Arizona icont refuses **zero**. Twenty died at `extract`, which REFUSES (correctly, by design) to materialize a stdin-bearing entry without `--out-in` rather than grade a starved witness; the twenty-first, `procedure_record_limit_replace_1` (origin `rung36_jcon_prepro`), says `$include "prepro.dat"` and got `"prepro.dat": cannot open` — a **missing-file error wearing a compile-refusal exit status**, with the companion present and declared the whole time at `corpus/tests/icon/config/prepro.dat`. **The row exists because a joiner wrote invalid Icon into ~14 entries, so "21 entries icont refuses" reads as CONFIRMATION rather than as a fault** — a reader would have taken it to the joiner's lane and found nothing. FINDING `FINDING-2026-09-10-hq_V-a-compile-under-the-oracle-criterion-that-withholds-stdin-and-companions-measures-its-own-instrument.md`. ⭐ **(2) THE JOINER AND THE FIFTEEN RE-CUTS WERE ALREADY ON ORIGIN** — the joiner at SCRIP `f59db4cd8`, the refs at corpus `6a6f39dd1`, both landed 09-09 under CEO-477. **The row's remaining value was the standing INSTRUMENT**, because **no board can see this class**: a `;` before `else`, before `then`, before a case body's `}` or directly inside `( )` is a syntax error to icont and a **NO-OP to us**, so a carrying entry READS GREEN on every board while being ungradable against the oracle at all. A floor, a per-entry identity pin and a delta are silent on it by construction. The joiner is a **WRITER**, so the repair does not retire the criterion — a writer that regresses re-manufactures the class silently. ⭐ **(3) LANDED: `scripts/test_gate_icon_master_entries_compile_under_icont.sh`, WIRED BLOCKING IN `make test`.** 757/757 compile. Three outcomes kept strictly apart (compiled · icont-refused rc=1 · extract-failed rc=2), because extract-failed and oracle-refused are two diagnoses with two owners and a gate that merges them names the wrong lane. Companions come from `_copy_companions()` — the harness's own authority, the same function `run_suite_entry()` uses when it GRADES — never a private `*.dat` lookalike that would drift from the grader. ⛔ **(4) 192s → 3.78s, SAME VERDICT, AND THE SPEED IS A CORRECTNESS PROPERTY:** the per-entry `extract` re-parses the WHOLE master pair every call, and 757 of those price the gate out of `make test` — **a gate in no runner is not measuring** (the s268 false-green shape). The master is now read once; the batched materializer is `cmd_extract`'s reader selection verbatim in order and in fallback, carrying the stdin field exactly as it does. ⭐ **(5) FAIL-ONCE PROVEN ON TWO SHAPES AND RE-PROVEN AFTER THE REWRITE:** `;` before `else` → rc=1 naming `procedure_write_122`; `;` inside `( )` → rc=1 naming `procedure_write_109`; a collapsed population → **rc=2, not rc=1**. Re-injected after the speed rewrite deliberately — **a detector arm fails OPEN (CEO-481)**, so a rewritten detector nobody re-injected against cannot distinguish a working gate from one that always passes. **(6) THE DONE-WHEN ON THE BATON IS REPLACED** by a call to the proven gate: it reads its own denominator, refuses rc=2 when it cannot measure, and is graded on origin/main. **OWED, NOT DONE THIS SITTING (named so nobody reads silence as completion):** hq_I's entry 579 `procedure_write_45` is **UNDER-ASSERTED** — engine cured at SCRIP `6a7a966f4`, stdout and rc genuinely agree with icont, but the ref asserts stdout only while the oracle's answer includes stderr and a non-zero exit, so a regression to a *different* error reads green; hq_I's `rung36_jcon_kwds` ruling (re-cut from Arizona icont, then mask **eleven** keywords in **two labelled stanzas** — implementation identity, and `col/row/window/x/y` as ORACLE_CONTRACT_NOT_IMPLEMENTED because this iconx has no graphics at all); hq_C's thirteen loose `tests/icon` files; the cfo's three witnesses at SCRIP `08905f250` (NOT `aae1f8513` — corrected on the bus) and the rung03 flip at `483717a0c`.
- **HQV-17 (hq_V, 2026-09-09 18:0x–18:2x CDT, SCRIP `5187904bd`, corpus `ab18dddd1`) — CEO-471 SWEPT, CEO-476 EXECUTED: BOTH PROBES ABSORBED TO A BOARD TWO LOWER AND TRUE, AND THE PERMANENTLY-TRUE DRIFT WARNING SILENCED WITH A REAL CHECK.** ⭐ **(1) THE CEO-471 SWEEP, BY MEASUREMENT.** 24 stdin-reading entries run against Arizona icont with each entry's own `ALL.in` block or true empty stdin: **22 oracle-faithful, 1 retracted as my own artifact, 1 genuinely mis-cut.** ⛔ The retraction is a trap worth naming: **an empty `ALL.in` block is NO BYTES**, and rendering it as one newline makes `read()` succeed on a blank line — which tests the exact opposite of what `procedure_write_read_1` (origin `rung27_read_read_eof_fail`) exists to test; its ref is perfect. **THE COUNT THAT CHANGE VERDICT IS ONE:** `procedure_write_45`, whose body is `s := reads(5); write(s)`. Its ref says `hello`; the oracle raises **error 105 `file expected`, offending value 5**, because `reads` takes the FILE first and the traceback spells it `reads(5,&null)`. We print `hello` rc=0, so we match a ref asserting OUR reading of `reads` as Icon's — **a false green concealing a real conformance defect**, GREEN → RED once re-cut. ⛔⛔ **(2) THE SAME METHOD OVER ALL 751 FOUND THE CLASS FOURTEEN TIMES LARGER: 14 graded entries the oracle REFUSES TO COMPILE, every one CURRENTLY GREEN** — five with a trailing `;` in a case body, four before `else`, three inside parentheses, two before `then`. **The root is ours and the vendored originals PROVE it rather than suggest it:** `jcon_tests/meander.icn:30` and `endetab.icn:41` carry **no** semicolon and icont **accepts** both originals, while our copies have one **added** — the one-liner joiner put a statement terminator inside a construct. **These are NOT the genqueen shape**, and the distinction decides the ruling: genqueen was a vendored program we cannot fix, so it rightly left the baseline; this text is ours and wrong, so retiring these 14 would conceal a defect in our own absorber. Recommended to the ceo: repair the joiner, re-cut all 14 refs, publish the drop as a **named criterion correction** and never as a regression (CEO-375's Pascal precedent). Not landed unilaterally on announcement eve. ⭐ **(3) CEO-476 EXECUTED — BOTH PROBES ABSORBED, 740/751 → 740/753 both modes**, row and SUITES.tsv line landed on the CLEAN tree `5187904bd`/`ab18dddd1` (the first attempt was correctly **refused for a dirty tree** under CEO-174, and the re-run was then **refused again by the stale-binary arm** because the rebase pulled a new `lower_icon.c` under my 22:53 binary — HQV-15's own rule, merge-rebuild-THEN-measure, enforced on me by the instrument). ⛔ **(4) THE BUILDER CANNOT PRODUCE WHAT CEO-476 ORDERED, AND THIS IS THE ENTRY'S REAL LESSON.** `util_build_master_suite.py` marks any non-green original **`xfail=True` by construction**, so both probes were absorbed carrying **XFAIL** — and **an xfail is a check that cannot fail**, so the board would have read **740/751 with xfail=2 and the two new reds INVISIBLE**, the precise opposite of *a board two lower and TRUE*. THERE IS NO XFAIL: markers stripped from both banners, `ALL.csv`'s xfail column corrected to agree with the pair it indexes, both now graded as genuine FAILs, **xfail=0 on the board**. **Absorbing a red-on-arrival probe through the builder silently converts it into a hidden one unless this step is taken** — and every future red-on-purpose absorption in this lane must take it. ⭐ **(5) CONTENT INVARIANCE PROVED BY NAME, AND ONE APPARENT CHANGE RETRACTED.** All **905** pre-existing entries byte-identical in `ALL.icn` and `ALL.ref`, zero removed. The single apparent change (`procedure_record_scan_replace_2`) was **my own splitter's accounting of the file-terminal newline**, which belongs to whichever entry ends the file — a different entry ends it now, and both files still end `end\n` exactly as before. Third artifact of the sitting caught before it was written down. ⭐ **(6) THE DRIFT WARNING SILENCED WITH A REAL CHECK, NOT REMOVED (SCRIP `5187904bd`).** It now subtracts `OUTSIDE_BASELINE_COUNT` — the number the harness already printed on every run beside the name and the oracle's own reason — and **shows its arithmetic** so the next reader sees which term is wrong instead of re-deriving it. Proven on **five arms**: silent on the false alarm it cures (904+1=905), silent post-absorption (906+1=907), **FIRES** on a vanished entry (906 accounted against 907 rows), **FIRES** with no outside list at all, silent with no outside list when reconciled. The point was never cosmetic: a warning permanently true and permanently meaningless sits one line above the numbers a reader acts on and teaches that reader to scroll past **the one check whose whole job is to catch a real silent shrink** — it had been disabled by its own output. **BOARD: IcnM 739/751 → 740/751 → 740/753, entries 905 → 907, run population 751 → 753, xfail=0, drift check silent and honest.** Owners assigned by CEO-476 from my seven-roots finding: record-copy serial hq_B · set-deletion hq_R · nested exchange hq_P · integer coercion hq_U · tracer SIGSEGV the cto · loadfunc hq_S · break-as-a-value hq_R. ⭐ **(7) THE CFO'S FOURTH WITNESS ABSORBED AND OWED NOTHING** (corpus `05dbe274e`, board `.github` `4c7091c8`): `large_integer_is_detected_by_allocated_growing` → `procedure_every_alt_47`, **IcnM 740/754**, entries 908, run population 754. Its handed ref was **re-cut from the oracle on this tree before absorption** as this lane does with every handed pair, and came back **byte-identical** — the cfo's ref is the oracle's answer, said back to them explicitly. RED here, and it flips GREEN by name on the board the moment the cfo's live-allocation-counter cure lands. **The builder marked it XFAIL for the THIRD time today**, stripped for the third time: three red-on-arrival witnesses entered the denominator this sitting and every one grades as a real red. **FINAL BOARD: IcnM 739/751 → 740/754, entries 905 → 908, xfail=0, drift check silent, honest and able to speak again.**
- **HQV-16 (hq_V, 2026-09-09 17:5x–18:1x CDT, SCRIP `d4d19848c`, corpus `82f4d259a`) — THE ELEVEN MASTER REDS ARE SEVEN ROOTS, EVERY REF IN THE RED SET IS ORACLE-FAITHFUL, TWO PROBES MINTED BEFORE ANY CURE, AND THE BOARD'S DRIFT WARNING IS A FALSE ALARM.** Session start clean; inbox drained (one from `coo`, informational — 739/751 received, the 731/749 correction recorded as a correction, nothing owed). Board re-measured on the merged tree: **IcnM 739 → 740/751** both modes, watermarks re-pinned 740/740, row and SUITES.tsv line written by `util_score_row.py` in the runner's own call and pushed as `.github` `819a56a6` — the movement is the fleet's `f ! record` landing, not this seat's. ⭐ **(1) EVERY ONE OF THE ELEVEN REFS IS ORACLE-FAITHFUL, so there is no false red in the Icon master red set** and no seat is about to spend cure time on a wrong ref. Nine matched `icont` 9.5.25a immediately; **the two that did not were BOTH artifacts of my own probe and were retracted before they were reported** — `procedure_write_265` because I captured `2>&1` and `&trace` writes to **stderr** while the master grades **stdout**, and `procedure_record_every_replace_12` because my banner-splitter stripped a **leading blank line** the stored ref genuinely carries. ⛔ I nearly filed two false ref defects against the one artifact this seat is the sole writer of, on announcement eve. **The rule from here: probe the way the grader runs — stdout only, no normalisation — or the divergence you find is your own.** ⭐ **(2) ELEVEN REDS ARE SEVEN ROOTS, AND EVERY ONE IS RED IN BOTH MODES WITH THE IDENTICAL DIVERGENCE** — that uniformity is itself the finding: **not one of them is a codegen defect**, all seven sit in the front end or lowering. Two roots are worth **two reds each**: `copy()` of a **record** mints no new serial (`procedure_write_257` + `procedure_record_every_replace_14`) and **element generation over a set concurrent with deletion loses every second element** (`procedure_write_258` + `procedure_every_scan_replace_13`). The remaining five: nested reversible exchange (two reds, already diagnosed in its own entry), integer coercion and the `&trace` SIGSEGV (both hq_U's named classes), `loadfunc`, and `break` as a value. ⭐ **(3) THE TWO PROBES, MINTED BEFORE ANY CURE (corpus `82f4d259a`)** — refs **cut from `icont` on this tree, never from our own output**, both RED ON ARRIVAL in both modes, **each carrying its control arm INSIDE the file**, which is what makes it a probe and not merely a witness. `copy_of_a_record_mints_a_new_serial_number`: D–I copy a **list, a set and a table** and **all six agree with the oracle**, so `copy()` and the serial counter are both sound and the fault is the **record path of `copy()` alone — one box**; a cure aimed at the shared serial mechanism is aimed at the wrong node and D–I will catch it. `deleting_during_element_generation_over_a_set_still_visits_every_element`: we visit **exactly half**, which is a **stride of two**, not scattered losses — a cursor advancing by one while the collection shifts by one underneath it; the no-deletion control arm reads 10/10 in both modes, so `!set` is sound and the fault is generation concurrent with mutation. ⛔ **(4) THE BOARD'S DRIFT WARNING IS A FALSE ALARM THAT FIRES ON EVERY GREEN RUN.** `board_icon_master.sh` says the suite file and its provenance index *disagree* (904 graded vs 905 rows). **Nothing has drifted:** `ALL.icn` 905 markers, `ALL.ref` 905, `ALL.csv` 905 rows, the three name-sets **identical** (zero-line diff, no duplicates), modes column exactly 752 + 153. The gap is **one entry, `procedure_every_alt_replace_4`**, out of the denominator by the **CEO-390/391 outside-baseline ruling** (Arizona icont refuses to compile it), recorded in `ALL.outside.tsv` and **printed by name with its reason on every run**. The check compares `graded` to `CSV_ENTRIES` without subtracting the outside count the harness already prints as `OUTSIDE_BASELINE_COUNT`. It is a warning that is **permanently true and permanently meaningless**, one line above the numbers Lon reads, and it trains every reader to scroll past the one check whose whole job is to catch a real silent shrink. **hq_T's lane (the instruments) — reported with the measurement, not fixed in another seat's lane.** ⛔ **(5) THE ABSORPTION IS NAMED, NOT TAKEN.** CEO-452 makes this seat the one writer of the master pair and the lane's precedent absorbs red-on-arrival probes (`daf298638`), but absorbing these two takes the board **740/751 → 740/753** — two more true reds in the denominator and **a percentage that reads lower on 09-10**. That is exactly the shape this seat flagged at HQV-1: a criterion change rendered to a reader as lost ground. True either way; only the reading changes. **Asked of the ceo rather than decided here**, and the probes are on origin and usable by their owning seats regardless of the answer. Record: `FINDING-2026-09-09-hq_V-eleven-icon-master-reds-are-seven-roots-two-of-them-two-for-one-and-every-ref-is-oracle-faithful.md`.
- **HQV-15 (hq_V, 2026-09-09 16:2x–17:0x CDT, SCRIP `63efd8436`, corpus `ca2b8a110`) — SIX ENTRIES MINTED OR ABSORBED, TWO EXPIRED DEFERRALS RETIRED, AND TWO RULINGS SENT BACK CORRECTED BECAUSE THE ORACLE SAYS OTHERWISE.** Inbox drained at every prompt boundary (14 messages this half: ceo ×4, hq_C ×4, cfo ×2, hq_T ×2, hq_B ×1, coo ×1). ⭐ **(1) FOUR RUNGS MINTED, THEN A FIFTH AND FOUR ABSORPTIONS** (corpus `daf298638`, `36d211ce1`, `ca2b8a110`): the cfo's `ladder__rung13_repalt_resumes_inside_a_conjunction` (green, nine shapes) · three CEO-461 to-bound rungs at rung19/rung20, each with a control arm · hq_C's eleven-shape `ladder__rung19_generator_in_a_bound_position_is_entered_at_alpha_not_beta` · four rung03 files and `rung36_jcon_cxprimes`. Every ref oracle-cut or oracle-verified; every landing carried its own resort and a by-name content-invariance proof (zero old entries changed, in any of the four files, in any landing). ⛔ **(2) THE THREE-ALTERNATIVE RULE IS AN ASSERTION, NOT A STYLE:** with `(2|4)` our reading is equally consistent with *first run dropped* and *only the last survives* — two different bugs. With `(2|4|6)` the second and third runs both print, which admits only the first. **And the row that earns the whole class is `"abcdef"[(1|2):(4|5)]`: the oracle yields FOUR sections, we yielded ONE, because each bound loses its first run independently and the losses COMPOUND** — so a cure that fixes one bound and misses the other still reads red there while every single-bound row goes green. hq_C confirms it nearly stopped after `lower_to`. ⛔⛔ **(3) CEO-465 SAID ABSORB THREE; MEASUREMENT REFUSED TWO, AND THE FAULT WAS MY OWN REPORT** — I had compared each file against its `.expected` and the ceo reasonably read that as the oracle. `cxprimes` is genuinely green and is in. **`genqueen` the ARIZONA ORACLE REFUSES TO COMPILE** (rc=1, *invalid declaration*, the trailing semicolon on its global list — JCON accepts it, Arizona does not): we accept a program the standard's compiler rejects, so matching its JCON `.expected` is not conformance. **`recogn` is not a red at all:** its second line is `while line := read()`, and under the SAME empty stdin the graded runner supplies **the oracle prints nothing either and our output is byte-identical to it**; its 8-line `.expected` was cut from a run fed the vendor's `recogn.dat`, and with that input the oracle AND SCRIP both reproduce it in both modes. ⛔ **SO THE ALREADY-ABSORBED `procedure_suspend_scan_replace_1` HOLDS A REF NO RUN OF THE GRADED POPULATION CAN PRODUCE — a permanent false RED of the CEO-410 class inside the denominator.** Retiring its deferral was tried and REVERTED: it invites absorbing the loose pair a second time, measured producing a duplicate entry carrying `xfail=1`. Cure is a stdin sidecar on the existing entry; no per-entry tool writes one. ⭐ **(4) A DEFERRAL RECORDS A MEASUREMENT AND MEASUREMENTS EXPIRE (adopted into MASTER-PLAN by CEO-465).** KEEP.md hard-blocked four rung03 files on 2026-08-29 for SIGSEGVing in both modes; eleven days later all four are rc=0 and byte-identical to the oracle in both modes. They were found only because hq_C reported three `MODES.tsv` keys as *matching no family* and offered to clear them — **they were not dead keys, they were live declarations for those keepers, and clearing them would have hidden the four for good.** ⛔ **AND RETIRING A DEFERRAL IS NOT A PROSE EDIT:** the contract is a delimited SUBSTRING search over the whole of KEEP.md/PENDING.md (`_declared_in_keep`), so my first retirement — which quoted the historical entry verbatim — kept the block LIVE and the builder went on refusing all four as KEEPERS. The names are now spelled without their `.icn` suffix and the reason is written into both files. ⭐ **(5) `MODES.tsv` IS PART OF AN ABSORPTION, NOT AN AFTERTHOUGHT:** `cxprimes` went in with modes `UNKNOWN`, which the builder counts as **UNPROVEN, not a pass** — a body added to the population and no pass with it. Declared from the ref's measured shape (program stdout, zero `TT_*` lines), never from the family name. ⛔ **(6) A CORRECTION AGAINST MYSELF, TWICE.** I told the ceo and hq_C that absorbing the rung03 four would drop the orphan count: **it does not** — they were in `ALL.excluded.txt`, so DECLARED rather than orphaned; the denominator grows and the orphan census does not move. And I called hq_C's eleven-shape probe RED ON PURPOSE in a commit message: **its cure `b3039f014` was already on origin**, my build predated it by minutes, and on the cured tree that entry and my three rungs are **GREEN in both modes** — the board was RE-RUN to rewrite the cell rather than annotated. ⭐ **(7) CEO-468 ADOPTED THE `ALL.err` SIDECAR THIS SEAT DESIGNED**, and hq_P's reason is stronger than mine: I measured the interleaving stable for the trace class, but a merged ref would pin BUFFERING rather than behaviour even where it holds. The sidecar ratchet is measured GREEN on this tree (stdin 50 at floor 50, argv 34 at floor 34, 5136 entries) — none of the six entries landed today reads stdin or argv, verified before minting. **BOARD: IcnM `714/730` → `718/732` → `727/744` → `731/749` → `738/750` → `739/751` (the last on the cfo's THIRD handed pair, `procedure_write_275`, `name(r[i])` on a record is `type.field` — rung24, green both modes; ⛔ its FIRST reading was DIFF in both modes on a binary six minutes behind the merge that carried its cure `b1db664a9`, and **a stale binary reproduces the old defect perfectly, which makes it the most convincing kind of wrong measurement there is** — the standing rule from here is merge, rebuild, THEN measure, and never report a red on a binary not built after the merge), watermarks re-pinned 714 → 718 in the commit that earned it (SCRIP `8c2912420`), and a further re-measure is running on the cured tree.**
- **HQV-14 (hq_V, 2026-09-09 10:2x–11:0x CDT, SCRIP `73d2ba58f`, corpus `23638085f`) — A FLEET RED CURED IN ITS OWN COMMIT, THE cfo's WITNESS ABSORBED, AND A GREEN THAT ASSERTS NOTHING NAMED WITH THE CONSTRAINT THAT DECIDES ITS CURE.** Inbox drained at every prompt boundary (7 messages: hq_B ×2, ceo, cfo ×2, hq_T ×2). ⭐ **(1) CEO-457's FLEET RED IS CURED, ORDER ONLY, PROVEN BY NAME (corpus `8159fa855`).** `test_gate_master_order_is_the_builders_order` read icon **202 of 885 out of order**; `util_build_master_suite.py --lang icon --resort --absorb-only <the 31 loose families>` fixed it — `--absorb-only` there only ACKNOWLEDGES those families so the terminal `--resort` can run, and all 31 pairs are still loose on disk untouched. **The 673-insertion/673-deletion diff is what moving 202 blocks looks like, so content-invariance was COMPUTED, not inferred:** entries extracted by entry name before and after from `ALL.icn`/`ALL.ref`/`ALL.in`/`ALL.csv` — program text 885 entries 0 byte-differing, ref text 0 byte-differing, stdin sidecar 20 entries 0 byte-differing, xfail 0, origin 0, modes 0, name set identical. Gate FAIL→PASS, other six masters untouched, re-proven after the rebase. ⭐ **(2) THE cfo's HANDOFF ABSORBED AS RUNG21 WITH ITS OWN RESORT (corpus `23638085f`):** `procedure_write_269` / `ladder__rung21_call_through_a_reassigned_procedure_name_reads_the_global`, minted through `util_add_ladder_witness.py --apply`. Rung21 (globals) is its home because that is the claim: **a procedure's name IS a global variable**, so `flip := lrev` inside `flip` makes the next `flip(L)` call `lrev` — we bound the call to the declaration and recursed to stack overflow, which is where Arizona `ilib` died and lost the 260 lines after it. ⛔ **THE HANDED REF WAS RE-CUT AND RE-VERIFIED HERE RATHER THAN TRUSTED (CEO-410 false-ref class)** — oracle twice, byte-identical, rc=0, identical to the `.ref` as handed — **and the handoff's cure hash was WRONG: `b8b7bcf9b` is the argument-dereference gate and the witness still overflowed on it in BOTH modes (rc=1, ERROR 246, zero of eight lines); the cure is `f48a3f0c7`, where both modes match byte for byte.** The cfo sent the same correction independently four minutes later. Board re-measured on the landing: **718/732 both modes** (was 714/730), watermarks re-pinned 714→718 in the commit that earned it (SCRIP `73d2ba58f`). ⛔⛔ **(3) hq_B's `rung264-pins-nothing` IS CORRECT AND THE MEASUREMENT UNDER IT IS WORSE THAN THE MAIL SAID.** `lib_ladder.sh:108,113` grade both modes with `2>/dev/null` and Icon's `&trace` writes only to stderr, so the rung's ref is the one line `A:end` and it PASSES while asserting nothing; zero trace-shaped lines exist anywhere in `ALL.ref`. **On the cured tree SCRIP emits the CALL LINE AND NOTHING ELSE, m3 and m4 byte-identical — five of the oracle's six lines are ABSENT, not wrong**, so this is a feature never built (hq_B routed it as CEO-459 to hq_U: no `TRK_SUSPEND`/`TRK_RESUME` exist). `procedure_write_265` is red **only** through rc=139 and will flip green with five lines still missing the moment its SIGSEGV is cured. ⭐ **THREE FACTS THE MAIL DID NOT CARRY, AND TWO OF THEM CHANGED THE FLEET'S CURE:** (a) `util_add_ladder_witness.py:155` keeps `p.stdout` alone, so **the MINT TOOL is blind the same way the runner is** — a runner-only fix would leave the corpus unable to EXPRESS the rung; (b) there is no escape hatch, because the oracle itself refuses `&errout := &output` with *Run-time error 111, variable expected*; (c) **the trace line's first column is the SOURCE FILE NAME in a fixed 13-character field truncated FROM THE LEFT** (`t264.icn     :` vs `nd_failed.icn:` under the origin-length name `master_extract_origin` materialises), so a folded-in ref would be pinned to a harness temp filename rather than to behaviour. ⛔ **hq_T HAD ALREADY AGREED TO PLAIN `2>&1` AND WITHDREW IT ON (c), TAKING THE ADDITIVE `ALL.err` SIDECAR THIS SEAT OFFERED** (one pattern with Lon's 09-08 `ALL.in`, all 886 existing refs byte-for-byte, no block means stderr stays uncompared = today's behaviour), routed to the ceo as a correction to CEO-457's stated cure. **I re-cut nothing on that class and will not until hq_T hands me the list with change-verdicts in both directions** — the instrument is theirs, the master pair is mine (CEO-452). ⭐ **AND ONE MEASUREMENT SENT AHEAD OF THAT LANDING:** the merged-stream ordering hazard is real but measured CLEAR on both sides — write/trace/write interleaves identically from the oracle into a file and through a pipe, and SCRIP places its one line in the same position in both, so a merged ref is stable for this class. **hq_B's count property (`&trace` decrements per EVENT) changes what my re-cut must carry: a finite trace budget, so the event COUNT is graded and not merely the text, plus a second entry that exhausts the budget mid-generator.** ⛔ **(4) CORRECTED hq_B, AND THE CORRECTION HOLDS:** `rung42_kw_trace_level` does NOT have rung264's shape — its witness writes `&level` to stdout and its ref is `1/2/3`, a real graded assertion; only its NAME says trace. hq_B took it and named the cause in its own finding (grepping rung NAMES then reporting on BODIES). ⚠ **(5) CARRIED, NOT INTRODUCED:** the board still warns it graded 885 while `ALL.csv` carries 886 — the same off-by-one HQV-13 recorded, both sides moved by exactly one, belongs to `board_icon_master.sh`'s population count. **FINDING-2026-09-09-hq_V-the-ladder-cannot-see-stderr-so-two-trace-rungs-grade-green-while-five-of-six-oracle-lines-are-missing.md.**
- **HQV-13 (hq_V, 2026-09-09 09:49–10:1x CDT, SCRIP `37ee8e2b0`, corpus `22e23d48f`) — THE BREAK-IN-CONDITION CLASS IS FIVE DIVERGENCES AND NOT ONE, THE rung02 WITNESS GETS ITS REAL ASSERTION BACK, AND TWO COLLIDING BOARDS ARE SETTLED BY MEASUREMENT.** ⭐ **(1) hq_U's `while break` HANG WAS FILED WITH ITS BOUNDARY EXPLICITLY UNMEASURED, AND THE BOUNDARY IS THE STORY.** Its FINDING says in terms that whether `until`, `repeat` or other condition positions share the defect is NOT measured. Measured here against `/home/resources/icon-master/bin/icon`, both modes, deterministic: `while break` HANGS rc=124 · `every break` prints NOTHING at rc=0 · `until break` and `repeat break` are ALREADY CORRECT · `if break then` the oracle REFUSES at compile time (rc=1, *invalid context for break*) where we accept silently. **A cure aimed at the hang alone leaves three of the five standing.** ⛔ **THE `every break` FORM IS THE WORSE OF THE TWO REDS AND HAS NO EXIT CODE:** it does not hang and it does not diagnose — it **silently abandons the rest of the procedure body**. The witness writes four lines, the oracle prints four, SCRIP prints the first two and exits 0. ⭐⭐ **(2) TWO PROBES MINTED BEFORE ANY CURE (corpus `22e23d48f`):** `procedure_write_266` (`ladder__rung09_loops_every_break`) and `procedure_write_267` (`ladder__rung09_loops_while_break`), refs cut by `util_add_ladder_witness.py` from the oracle directly and never from SCRIP, each extraction round-trip verified. **Deliberately TWO entries, not one:** folding the hang in would mask the every-break assertion behind a timeout verdict. **The hang entry costs the board one 10s timeout and that cost is NAMED, not discovered** — it is the price of the cure having something of its own to flip, since `procedure_record_every_replace_13`, the only other entry reaching this construct, is red for several independent reasons and will not flip on it. The `if break` refusal form is measured and **NOT** minted: `want_rc=1` needs an `ALL.wantrc` line and the mint tool rightly refuses a nonzero-rc mint rather than grade it against the wrong rc forever — reported to the ceo instead. ⭐ **(3) THE rung02 WITNESS IS RE-MINTED ONTO ITS TRUE ORACLE OUTPUT, AND IT GRADES GREEN.** `procedure_write_253` had ended on a filler `write("G:end")` because the mint tool's post-write extraction proof collapsed trailing newlines and so refused any witness whose oracle output legitimately ends in a blank line — exactly what `write(&null)` produces, the one thing the rung exists to assert (HQV-12 item 4). hq_T cured it in SCRIP `453320a25`. Filler dropped, ref re-cut from the oracle, `n_lines` 15 → 14; **the output now ends on the blank line and the master's own block round trip has to preserve it**, proven green both modes. cfo's `5282bdb43` had already cured the rung's own defect, as the cfo predicted. ⛔ **(4) CEO-452's QUESTION ANSWERED, AND THE ANSWER IS ONE LEVEL UP FROM THE SIDECAR:** `tests/icon/ALL.in` carries no stdin block for `rung36_jcon_io` because **`rung36_jcon_io` is not an entry in `ALL.icn` at all** — `ALL.excluded.txt:178`, KEEPER under the KEEP.md deferral contract. All 20 `ALL.in` blocks name entries that exist, so there is nothing to feed. What IS true and told to hq_C: the standalone keeper reads stdin and has **no `.in` companion on disk**, so its `.expected` was cut from a STARVED run — the CEO-410 false-ref class, a keeper-file defect for whoever absorbs it, not an `ALL.in` gap. ⛔⛔ **(5) THE MASTER PAIR COLLIDED TWICE IN ONE SITTING AND CEO-452 IS EXACTLY RIGHT ABOUT WHY.** My first batch rebased into a conflict in `ALL.csv` against the 09:36 cfo-witness absorption — **and `ALL.icn`/`ALL.ref` merged CLEANLY while carrying per-entry banner sequence numbers the other landing had shifted: a clean textual merge of a wrong file.** The batch was rebuilt from upstream content through the mint tool and the harness's own block writer, whose round-trip proof passed on all 884 entries before a byte was written. **No `reset --hard`, no force, no history rewrite.** ⭐ **(6) TWO BOARDS THEN COLLIDED IN `SCORE.md` WITHIN FOUR MINUTES AND NEITHER DESCRIBED main:** `712/726` on SCRIP `f046ef00b` (a corpus predating four entries now on main) against my `712/730` on `17ac1bc39` (a SCRIP main had moved past); `f046ef00b` is not a descendant of `17ac1bc39` and main was already at `37ee8e2b0`. **Hand-merging would have published a number for a tree nobody can check out (CEO-174), so it was settled by re-measuring on current main instead of by picking a winner: `714/730` both modes**, watermarks held at 709, xfail=0 xpass=0. The +2 over both readings is NOT this seat's — it came from the SCRIP landings that arrived during the collision, `f046ef00b` among them. ⚠ **(7) A STANDING INSTRUMENT QUESTION, CARRIED FORWARD AND NOT INTRODUCED HERE:** the runner warns it graded 883 entries while `ALL.csv` carries 884 rows. The same off-by-one sits in both superseded rows (879/880, 874/875) and both sides moved by exactly the four new entries, so the gap did not widen — it belongs to whoever owns `board_icon_master.sh`'s population count against the provenance index.
- **HQV-12 (hq_V, 2026-09-09 08:47–10:0x CDT, SCRIP `1b9ef783c`, corpus `cae5cb32a`) — THE NRETURN WANT-NAME CLASS IS CURED AND LANDED, THEN THE LANE TURNS TO ICON ON LON'S WORD AND THIRTEEN ORACLE-CUT RUNGS GO IN AHEAD OF ANY CURE.** ⭐ **(1) CEO-441 DISCHARGED, AND THE FIX IS SMALLER THAN THE ROW'S OWN DIAGNOSIS.** The want-name request now rides the role-4/5 per-DEFINE shim's OWN activation frame — parked in eight already-allocated, previously unused bytes at entry, the global zeroed, put back at BOTH exits, the failing one included. **No new global, no new frame bytes, and `rt_g_want_name`, `rt_nret_fix`, `rt_nret_fix_tiny`, `pattern_match.c` and `by_name_dispatch.c` are untouched.** ⛔ **AND IT CORRECTS THE ROW'S OWN TEXT, WHICH WOULD HAVE SENT THE NEXT SEAT TO THE WRONG FILE:** the baton and the 09-08 FINDING both name `rt_proc_call_prologue` / `rt_call_proc_descr` / `rt_ab_enter_env` as the site. **Probes on all four C entry points fired ZERO times on the repro** — the emitted `FLD_α` is the role-4 SIG shim and touches `rt_g_want_name` nowhere, and `rt_ab_enter_env`'s `AB_OFF_WN` slot belongs to the SIBLING role-7 convention a tiny-shim-eligible DEFINE never enters. The cure is that same protocol in the arm that is actually reached. **BOARD:** Flake cited at 113/124 (`c4b5a1617`) per rule 5, measured **115/124** on the change alone and **117/124** after rebasing onto other seats' cures — `gimpel-stack-field-functions` and `gimpel-l-one-compiler` flip in both modes and in both `fnsig()` arms. **`kalah-opening-search` and `gimpel-linked-list-functions` STAY RED, so neither is this cause** — the CANDIDATE is not rounded up, and CEO-429 keeps its attribution. **CONTROL-ARM BAR (ceo-359) against clean `01eb996ca`, same corpus: snobol4 master 1870/1894 FAIL=0 and XPASS=0 both modes, icon master 707/707, pascal m3 248/251 m4 248/251, prolog floor PASS m3 518 m4 439 — every arm unmoved.** Artifacts regenerated in the landing (corpus `277bf3ec9`, `cc7eb9ae8`, `5fb7fc6b7`); the regen moved 20 benchmark `.s` files, so the debt was real and is paid by the commit that moved emission. `FINDING-2026-09-09-hq_V-the-want-name-request-rides-the-activation-frame-and-two-graded-snoflake-reds-flip.md`. ⭐⭐ **(2) THE LANE TURNED MID-SITTING AND THE TURN IS OBEYED: Lon 09:0x CDT, verbatim: *"Switch to Icon completely... Icon only; no SNOBOL4 nor Prolog."*** CEO-445 makes hq_V the ORACLE PROBE seat — a rung per red, minted BEFORE the cure. **THIRTEEN rungs landed (corpus `cf265e9f1`, `45e54a3d5`, `cae5cb32a`), every ref cut by `util_add_ladder_witness.py` from icont/iconx directly, never from SCRIP, each extraction round-trip verified, each measured red on SCRIP today:** `rung02_proc_missing_args_default_null` (args — an operator from `proc(name,arity)` called short defaults to `&null`, we give 0; a declared procedure called short is ALREADY correct, which is what narrows it to operator values) · `rung41_rt_loadfunc_success` (cfuncs, extlvals — rung41 had only `loadfunc_refusal`, so the SUCCESS path was never witnessed) · `rung29_type_string_invocation_of_operator_names` (evalx — hq_U's class) · `rung30_misc_seq_bad_argument_raises_101` (errors — `seq("a")` returns 0 silently, so a negative `&error` never decrements and the three error keywords stay unset) · `rung24_records_copy_allocates_a_new_serial` (fncs — and `copy(copy)` returns `&null`, not `function copy`) · `rung36_sets_generation_survives_deleting_the_element_just_produced` (gener — deliberately **ORDER-INDEPENDENT** so it grades the invariant and not a bucket layout: we generate 20 of 40, skipping every second element) · `rung02_proc_argument_dereference_is_deferred_to_the_call` and `rung23_tables_trapped_variable_dereference_is_deferred_to_the_call` (misc, traps — hq_U's class, split in two because a trapped variable is a different box) · `rung24_records_apply_bang_spreads_the_fields` (misc) · `rung31_sort_orders_by_type_class_before_value` and `rung31_sort_table_pairs_follow_the_full_type_class_order` (sorting, struct, coexpr, recent — one comparator should turn both) · `rung03_suspend_trace_reports_suspended_resumed_and_failed` and `rung03_suspend_trace_of_a_generator_call_with_an_argument` (tracer, transmit). ⛔ **THE LAST ONE SIGSEGVs SCRIP (rc=139) mid-way through printing the traced call's own argument list**, and it is narrowed BY MEASUREMENT rather than guessed: a traced non-generator with an argument is fine, a traced generator with NO argument is fine, it takes a generator AND an argument. ⛔ **(3) THE ICON MASTER DROPS OFF 100% BY EXACTLY THESE THIRTEEN, ON PURPOSE, AND IT IS SAID OUT LOUD RATHER THAN DISCOVERED BY THE coo:** 707/707 was 707 of a population that did not contain the constructs thirteen graded Arizona programs die on; the new reading is the same compiler measured against more of Icon. That is the point of minting before curing, and the alternative is the false-green shape this fleet keeps curing. ⛔ **(4) AN INSTRUMENT DEFECT FOUND AND REPORTED, NOT PATCHED FROM THIS SEAT:** `util_add_ladder_witness.py`'s post-write extraction proof compares `ref_text.rstrip("\n") + "\n"` against the extractor's output, so it **REFUSES any witness whose oracle output legitimately ends in a blank line** — exactly what `write(&null)` produces, which is the very thing the first rung was minted to assert. It refused AFTER writing the files, which is the dangerous half. Worked around by ending the witness on a non-blank line; hq_T's to fix. ⛔ **(5) HYGIENE-446 IS A TRUE BLOCK AND IS NOT FORCED:** `test_gate_define_alternate_entry.sh` is **RED on a clean tree** (rc=1 — it measures hq_P's live DEFINE alternate-entry defect: oracle `E0 v E1 v E2 v done`, we print `E2 v done`). Wiring it into `make test` reds the build for every seat, so it is reported rather than adopted on this seat's judgment.
- **HQV-11 (hq_V, 2026-09-08 22:4x CDT, tree SCRIP `f09612633`) — A SECOND AND SIMPLER WITNESS FOR THE SAME ROOT CAUSE, AND THE MECHANISM IS SHARPER THAN HQV-10 STATED. NO FLIP; TREE CLEAN.** Took the next red by hand (`next` does not dispatch under NONET) — `gimpel-stack-field-functions`, whose whole diff is two lines. **It is the SAME defect as `gimpel-l-one-compiler` and it needs NO pattern matching at all**, which makes it the repro a cure should be built against: nine lines, `OUTPUT = DATATYPE(.FLD(P,2))` where `FLD` returns `.APPLY(FIELD(DATATYPE(ST),I),ST)` by name — oracle prints **NAME**, we print **STRING**, both modes. ⭐ **AND THE MEASUREMENT CORRECTS MY OWN HQV-10 WORDING.** I wrote that the defect is `rt_g_want_name` being left LIVE across the callee body. That is true but it is not the mechanism. The trace shows `SNO$WANTNM` firing **TWICE** in one expression — once for the outer `.FLD(...)`, once for the inner `.APPLY(...)` inside FLD's own body — and the inner one is consumed by the field-var fast path in `by_name_dispatch.c` (~6663) **which CLEARS the flag when it consumes it**, destroying the outer request that was still pending. FLD then returns a correct `DT_N` into `rt_nret_fix(r, wn)` with `wn == 0` and it is dereferenced. **The sharper statement is: `rt_g_want_name` is a single global with NO NESTING, so it cannot hold an outer pending name-request and an inner one at once, and whichever consumer clears it first destroys the other's.** The control is in the same run: bare `DATATYPE(FLD(Q,2))` fires `SNO$WANTNM` exactly once and is correct in both engines. A per-activation slot cures both symptoms; a save/restore stack would too but is a new global needing Lon's grant. **(2) CLASS SIZED HONESTLY, WITHOUT ROUNDING THE CANDIDATE UP:** of my nine graded snoflake reds, four reach a by-name return — `gimpel-l-one-compiler` and `gimpel-stack-field-functions` are **CONFIRMED** this cause; `kalah-opening-search` is a **CANDIDATE ONLY** (it builds `:(NRETURN)` functions with `CODE()` and dies at `ERROR 239 -- indirection operand is not name`, the right family, but reached through `CODE` and I have not proved it is this rather than a `CODE` subset gap); `gimpel-linked-list-functions` is **attributed elsewhere**, to HQV-7's twice-`DEFINE`d-name defect under CEO-429, even though it too uses a by-name include. So the hq_U shared-node row is worth **two** graded reds outright and possibly a third — that is the argument for its rank. **(3) Nothing landed; every probe reverted; SCRIP clean at `f09612633`.** Addendum in `FINDING-2026-09-08-hq_V-want-name-is-a-global-live-across-the-callee-body-so-nreturn-derefs-wrongly-under-a-deferred-target.md`.
- **HQV-10 (hq_V, 2026-09-08 21:48–22:4x CDT) — THE ceo's REPRODUCIBILITY CENSUS ANSWERED BY MEASUREMENT, THE PASCAL ROWS UN-STALED, AND ONE RED REDUCED TO AN EXACT SHARED-NODE MECHANISM. NO FLIP; TREE CLEAN.** Inbox drained at every prompt boundary (7 messages this sitting: coo, ceo ×3, hq_B, cto ×2 — the cto's arrived twice and the duplicate is answered once). **(1) THE ceo's QUESTION — has a board moved between two runs on one tree — ANSWERED BY RUNNING IT, NOT BY RECALL.** Twelve runs on ONE tree (SCRIP `60d58c05b`, corpus `3b10e1590`): PAT ×3 (**292/427** both-modes, m3 306, m4 292, crash m3 2 m4 0), FPC ×3 (**116/181**, m3 130, m4 116), master gate m3 ×3 and m4 ×3. Equal totals were NOT accepted as the answer — the 854 PAT and 362 FPC per-program rows were pulled from the progress DB per run and diffed, because two programs swapping green-for-red leaves a headline fraction untouched. **Zero differing rows in every pair; master stdout byte-identical.** ⛔ **AND THE PART THAT MATTERS MORE THAN THE CLEAN RESULT: CEO-379 `pascal-m4-intermittent-segv-layout-sensitive` STAYS LIVE AND RANK 0 AGAINST THIS SEAT** — its witness is five runs on one tree giving five pass counts, so twelve quiet runs bound how often the class fires and do NOT show it dead. Reported as such and not as cured. **CEO-434 acknowledged it as the strongest arm in the census and left the row open.** Evidence: `FINDING-2026-09-08-hq_V-twelve-runs-one-tree-every-pascal-board-reproducible-but-ceo-379-stays-open.md`. **(2) THE FACT RULE — both Pascal package rows were >24h STALE on the banner and now carry today's date and this tree** (SCORE.md + SUITES.tsv, pushed at `3ac3aa95`). Counts unchanged, so it is a CONFIRMATION and the stuck marker stands honestly rather than by neglect. **(3) THE RED POOL RECONCILED AGAINST A LIVE MEASUREMENT:** the pool held 141 PAT reds against tonight's 135, and the six were not arithmetic — `iso7185prt1801` `1802` `1804` `1805` `1806` `1808` are **genuinely cured**, each verified individually as rc=1 in BOTH modes with a real ISO 7185 6.8.3.9 diagnostic, and struck. FPC's 65 matched exactly. **(4) CEO-434's GATE CORRECTION ACCEPTED WITHOUT ARGUMENT:** my lvalue gate landed with no freshness guard and wired into nothing; the ceo guarded it and adopted it at `dbba08ecb`. `util_require_fresh` + the make-test wiring now precede any gate push of mine. **(5) THE DEFINE ROW RELEASED UNWORKED BUT THREE THINGS RICHER, so the cto (who holds it under CEO-429) does not re-walk them:** HQV-7's own closing hypothesis is **DEAD** — `LBL__FA`/`LBL__FB` are both in `proc_table` WITH nodes and every bind node already carries its own correct entry, so the loop discards data it has; the role-5 body seal is **UNSATISFIABLE BY CONSTRUCTION** (`lbl_t0` comes from `dentry_name`, `_realstub` from `dentry_entry`, and the two filling arms are mutually exclusive, so that guard can never be true for any bind node in any program — which is why the dormant `body_cell$<FN>` machinery has never been reachable); and routing multiply-bound names down the runtime DEFINE path is a **fourth measured dead end** that SIGSEGVs mode 3. `FINDING-2026-09-08-hq_V-the-define-rebind-seal-condition-is-unsatisfiable-and-the-dynamic-path-is-not-the-escape.md`. **(6) `gimpel-l-one-compiler` TAKEN AND REDUCED TO ONE LINE OF MECHANISM.** Its whole diff is three ` STORE ` lines with an EMPTY operand where the oracle reads ` STORE TEMP1/2/3`, because `PUSH.INC` returns by name. **Minimal repro is twelve lines and isolates it perfectly: the SAME function calling the SAME NRETURN function reads `BYNAME` when called directly and `V` when called through a deferred star-expression used as an ASSIGNMENT TARGET.** Four intermediate repros are GREEN, so it is that context and nothing simpler. ⭐ **CAUSE, MEASURED AT THE SEAM: `rt_g_want_name` is a GLOBAL carrying the caller's I-want-a-name intent, and it is deliberately left LIVE for the whole callee body** — `rt_proc_call_prologue` ends by setting it, `rt_call_proc_descr` re-arms it at `rt.c:952` immediately before entering the body, the tiny path restores it just before the shim. Every nested call inside inherits the outer intent, and `rt_nret_fix` derefs a `DT_N` only when `!wn`. **The cure's machinery already exists and is simply never reached here** — `rt_ab_enter_env` stashes wn in the frame and clears the global, and it **never fired once** in an instrumented mode-3 run. **That makes it a shared-node change in `src/runtime/rt/` needing hq_U's co-sign and the control-arm bar, NOT a one-bug flip.** Row PARKED on a new rank-1 row minted to hq_U; **two speculative fixes are measured, reverted and written down** (guarding both tiny seams with `!_wn` does not cure it and introduces a SIGSEGV). `FINDING-2026-09-08-hq_V-want-name-is-a-global-live-across-the-callee-body-so-nreturn-derefs-wrongly-under-a-deferred-target.md`. **(7) RE-BASELINED ACROSS THE cto's 22:34 ORACLE SWAP — CITED PER CEO-440(f) AS `live-oracle`, swap stamp `20260909T033439Z`** (snoflake's runner grades against `sbl -bf` in the same run, never a pinned ref, so the swap could in principle have moved it): snoflake **112/124 before and after**, and the full FAIL-M3/M4/SBL name lists are **byte-identical**, so neither STCOUNT fixture in my lane moved; the Pascal boards grade against `fpc -Miso` and are untouched. ⛔ **AND A PROCEDURE DEFECT REPORTED TO THE cto:** the swapped binary's mtime still reads 2026-09-05 and the backup the telegram names does not exist, so the cheap check says the swap had not landed when it HAD — I verified behaviourally with the cto's own witness (`setexit4` reads `stmts executed 34`, not 21) and asked that the next swap verify itself. **(8) hq_B TOLD, AND ITS HONEST NEGATIVE RECORDED:** `l-one-compiler` was never in hq_B's slice (it is hq_C's G–P), so no double-count either way, and hq_B reports `COPYL_driver` fails rc=1 in both modes — not a diff — so by my own criterion my cause probably does not apply to it. **NOTHING LANDED IN SCRIP THIS SITTING AND THE TREE IS CLEAN; the boards above are confirmations, and the two reds I opened are parked with their mechanisms named rather than carried quietly.**
- **HQV-5 (hq_V, 2026-09-08 20:1x–20:5x CDT) — MODE NONET: THE SEAT IS RE-LANED OFF PASCAL AND ONTO SNOBOL4, AND THE FIRST BUG IS LANDED (3 FLIPS).** Inbox drained at the prompt boundary (4 messages, all `ceo`). **(1) MODE is NONET** (`/home/resources/postoffice/MODE` line 1, ceo 19:50 CDT, Lon verbatim: *"Move to 4 officers plus 9 HQ's."*), and a second telegram puts **ALL TWELVE WORKING SEATS ON THE 85 SNOBOL4 PACKAGE REDS UNTIL SNOBOL4 IS 100%** (Lon verbatim: *"9 HQ's and 3 officers on bugs is 12 … in about 3.5 hours SNOBOL4 at 100%"* · *"One bug at a time by each seat."*). **hq_V's lane for the duration is THE SNOFLAKE CLASS + AIS + spitbol_testpgms; PASCAL IS PARKED-LON-HOLD and closed until after 09-10.** **(2) THE MISCLASSIFIED-REDS TASK WAS ALREADY DONE BY THE coo** (COO-27) and the ceo redirected me off it mid-window; the class is FOUR, only TWO left the denominator (`lexical-comparison`, `string-pad`, lowercase throughout, so SPITBOL never reaches an `END`). I did not re-verify the widening. I HAD already swept the neighbours, and the sweep is recorded because it closes the question for every other seat: `sbl -bf` over every `.sno` in `csnobol4_suite`, `gimpel` and `dotnet` returns **86 un-numbered `No END statement` refusals and every one is a LIBRARY INCLUDE at rc=1, not a graded program** — the gimpel graded units are the 145 driver stems and all carry an uppercase `END`. **No further fixture leaves any denominator.** **(3) THREE ROWS REWRITTEN on the clean tree** (SCRIP `87b80d593`, corpus `9851eb79e`): Snoflake **102/124**, aisnobol **4/7**, testpgms **1/2**. **(4) ⛔ NINE OF MY 21 GRADED SNOFLAKE REDS LIE TO A NAÏVE REPRO**: run the fixture without staging the `.INC` symlinks into `RUN` and setting `SNO_LIB`, and they read `cannot open include` — a repro artifact, not a defect. Diff them the runner's way or not at all. **(5) FIRST BUG LANDED — SCRIP `093ab602c`, Snoflake `102/124` → `106/124`.** `$` applied to a value that is ALREADY a name was stringified to the **contents of the cell it names**: `bn_sno_name` called `rt_sno_indirect_name` unconditionally, which for a `DT_N` operand falls through to `VARVAL_fn`, so `SWAP(.A<1>, .A<2>)` wrote a global named by the element's VALUE and swapped nothing. Every piece of the machinery was already right (`c_rt_subscript_var` returns `NAMETRAP(vc)`, `c_rt_assign_var_body` commits through `vc->cellp`); the reference survived assignment and argument binding and died in one line. ⭐ **IT PRESENTED AS `ERROR 246 -- stack overflow`**, three suite-widths from its cause, because Gimpel's `HSORT` partitions by `SWAP(.A<J>, .A<K>)` and a no-op swap never advances the partition — **read an ERROR 246 on a program SPITBOL completes as a suspected silent no-op upstream, not as a depth problem.** **ATTRIBUTED HONESTLY**: the tree also carries work rebased in from other seats, so of the four flips, **three are this cure** (`gimpel-sorting-functions`, proven by the minimal repro; `gimpel-binary-tree-linearize` and `gimpel-read-list-functions`, both on by-name-returning includes) and **`dump-variables` is NOT mine** — it arrived with the rebase. **(6) STILL RED AND NAMED, NOT CARRIED QUIETLY:** `gimpel-conversions` and `gimpel-linked-list-functions` reach ERROR 246 on a **separate** cause — `COPYL` re-`DEFINE`s itself with an alternate entry label and calls itself, and `SPELL` recurses through in-place pattern replacement (`N RTAB(3) . M =`). The minted row's DONE-WHEN named all three, so **the row is NOT done** and stays claimed on the second cause. **(7) ONE RULING ASKED, NOT WAITED ON:** `collect-and-locals` differs from the oracle in exactly one field, SPITBOL's own heap number (`FREE SPACE BEFORE: 129476` vs our `0`; measured deterministic — bare `COLLECT` reads 129577, and 129570 after one 40-char string). The runner grades exact-stream-or-same-ERROR-number with no normalisation, so the only way to green it is to fit a constant to one oracle build, which I refuse. Evidence: `FINDING-2026-09-08-hq_V-dollar-of-a-name-value-was-stringified-to-the-cell-contents.md`. Control arms (shared node `by_name_dispatch.c`, and per **CEO-405** the state actually touched was asked for, not just the lowerer census): SNOBOL4 master **1894/1894**, Icon master **704/704**, Prolog master **m3 518 · m4 439 of 559** — all no worse than the clean tree.
- **HQV-6 (hq_V, 2026-09-08 21:0x CDT) — CEO-409 IMPLEMENTED: THE EXCLUSION IS AT THE LINE, NOT AT THE PROGRAM. Snoflake `106/124` → `107/124`, DENOMINATOR UNCHANGED.** The ceo ruled on the `collect-and-locals` ask (HQV-5 item 7) and **neither answer I offered was right**: the fixture is NOT outside the baseline (the oracle runs it perfectly — excluding the program would drop three lines of real graded semantics to dodge one line of arithmetic, and it moves the denominator in the flattering direction), and it is NOT a legitimate red either (a red asserts a defect; there is none in the other three lines, and the fixture can never go green whatever anyone writes, which is **a criterion that cannot say YES** — the mirror of a gate that cannot say NO). The third thing: **the fixture stays IN the denominator, graded on every line carrying language semantics, with the implementation-defined line named, masked and COUNTED.** New fixture directive `* @mask-line <ERE> :: <reason>`, in the fixture beside the data and never in the runner. ⭐ **THE FOUR GUARDRAILS ARE MECHANICAL, NOT TRUSTED — a per-fixture mask is otherwise a machine for hiding reds, and all three enforceable ones were PROVEN FIRING on scratch fixtures before the landing:** a mask carrying no reason is REFUSED rc=2, not ignored; a pattern that could cover a diagnostic is REFUSED — checked by RUNNING the pattern against probe lines (a numbered SPITBOL error, a file-and-line error, the un-numbered `No END` refusal) rather than by reading it, so a clever regex cannot talk its way past; and a fixture whose masked lines are the MAJORITY of its output is REFUSED and told to go outside the baseline named instead. **The outside-baseline classification reads the UNMASKED stream on purpose — it must never see through a mask.** Guardrail 3 is what makes this a measurement rather than a hiding place: `MASKED_LINES total=N across M fixture(s)` prints beside the board every run and is carried into the SCORE.md row **whether or not any mask armed**, because a zero said out loud is what makes a non-zero legible. Current reading: `masked_lines=1 in 1 fixture` (`collect-and-locals`). ⛔ **`dump-variables` IS NOT MASKED and must not be** — the ceo drew the whole distinction there: `&FILE`/`&LASTFILE` reading empty where the oracle reads the source name is a REAL defect and is mine to cure. One prints a number that belongs to the implementation; the other fails to report a fact that belongs to the program. Landed corpus `c0e2cbc6d`, SCRIP `f03584ebc`. The coo's independent measurement of `102/124` on a different tree agreed with mine before either cure, and the coo cured `COLLECT()` returning 0 on its own merits — that stands and is untouched by the ruling.
- **HQV-7 (hq_V, 2026-09-08 21:2x CDT) — THE SECOND CAUSE ON MY ROW IS DIAGNOSED EXACTLY AND NOT CURED, AND THE THREE DEAD ENDS ARE WRITTEN DOWN SO NOBODY RE-WALKS THEM.** `DEFINE` is an executable statement — each execution binds the entry point — and **we bind once at compile time, so the LAST `DEFINE` in program text wins for every call in the program**, both modes identically. Witness: three `DEFINE`s of one name alternating entries reads `A one / A two / A three` where the oracle reads `A one / B two / A three`; with only the first two the reading is `B one / B two`, the later binding applied retroactively to a call that already ran. **WHERE IT IS LOST:** `src/driver/scrip.c` (~1418) builds the `dentry` table that `emit.cpp:1260` reads to set `g_emit.lbl_t0`, "this DEFINE's entry" — and it resolves each bind node by looking the function up in `proc_table` **BY NAME**, taking that one proc's entry, **never consulting the entry the lowerer already attached to the bind node** (`sno_bind_attach_entry` pushes it as an `IR_LIT_NAME` operand, and `sno_parse_define` defaults `d->entry` to the function name, so every bind node carries its own correct entry). Emitted assembly confirms it: all three sites `lea r9, [rip + F_α]`. ⭐ **THE MECHANISM FOR EXACTLY THIS EXISTS AND IS DORMANT** — `bb_define.cpp` carries `M4-BODY-SEAL: body_cell$<FN> <- &LBL__<this DEFINE's entry>, so a call reads the binding in force when it runs rather than a baked winner`, the activation really does read `body_cell$F`, and in the witness **no store to it is emitted at all** because the seal is gated on the NULL `_.lbl_t0`. The runtime half is fine too (`rt_define_site` rebinds `p->fn` and sets `p->redefined`). ⛔ **THREE MEASURED DEAD ENDS, ALL REVERTED, TREE CLEAN AT SCRIP `c707c9762`:** (1) `SCRIP_NO_TINY=1` changes nothing — the direct-call tiny shim is NOT what bakes the winner; (2) ungating the body seal from `bb_tiny_shim_ok` changes nothing, since `_.lbl_t0` is NULL either way — **and note that anything making `bb_tiny_shim_ok` return false SUPPRESSES the very seal the cure needs, which is the trap I fell into twice**; (3) resolving the bind's own entry operand through the `LBL__<entry>` `proc_table` search, mirroring the `IR_GOTO_DEFERRED` arm in the same loop, changes nothing and `r9` still loads `F_α` — **so the next question is whether `LBL__F1` is simply absent from `proc_table` for a label that is only a DEFINE entry and never a branch target.** That is where the next seat should start. ⛔ **AND A CORRECTION TO MY OWN HQV-5:** I named `SPELL`'s in-place pattern replacement as the cause of `gimpel-conversions`, and **that is wrong** — replacement is MEASURED CORRECT (`N RTAB(3) . M =` gives `N=976 M=1`, matching the oracle, as does `P LEN(1) . Q =`). `gimpel-conversions` is a THIRD, still-unexplained cause. So of the three fixtures my row named, one is cured (HQV-5), one is this diagnosed-not-cured `DEFINE` bug (`gimpel-linked-list-functions`, via `COPYL`'s self-redefinition trick), and one is unexplained. **The row's DONE-WHEN is NOT met and I have not marked it done.** Evidence: `FINDING-2026-09-08-hq_V-a-redefine-with-an-alternate-entry-binds-one-entry-for-the-whole-program.md`.
- **HQV-8 (hq_V, 2026-09-08 21:1x–21:4x CDT) — SNOFLAKE `109/124` → `112/124`, TWO CURES, AND A MEASUREMENT METHOD CORRECTED.** ⛔ **FIRST, A CORRECTION TO MY OWN HQV-5, AND IT MATTERS MORE THAN EITHER CURE:** I reported six fixtures as "already matching in mode 3, so mode-4-only reds". **THAT WAS WRONG AND IT WAS MY HARNESS, NOT THE ENGINE** — I fed them `/dev/null` on stdin while the runner feeds each fixture the `@input >>>` block from its own header. With the input extracted the way `parse_fixture` extracts it, **all 15 graded reds reproduce in mode 3** and there is no m4-only class at all. The lesson is the same one that bit me on `SNO_LIB` earlier the same session: **a hand repro of a suite fixture must reproduce the runner's whole environment — the staged `.INC` symlinks, `SNO_LIB`, AND the header's `@input` — or it invents a class that is not there.** The coo was told. **CURE 1, SN4-REPL slice 2 (SCRIP `d5d63c9d3`):** a pattern-replacement whose SUBJECT is a subscript or an indirect lvalue was refused at COMPILE time, so such a program did not run at all; measured minimal on all three shapes (`A<1> 'W' = 'T'`, `$N 'W' = 'T'`, `T<'k'> 'W' = 'T'`), oracle green on each. Desugared in the lowerer rather than teaching `IR_MATCH_REPLACE` a second write-back path, because every piece already existed: the lvalue is evaluated ONCE into a hidden name variable ahead of the match (the name operator over a subscript yields a cell reference — **which only works because of my own earlier cure this session, HQV-5**), the match replaces into a hidden value variable, and an ordinary indirect assignment splices it back **on the success path only**. Hidden names are keyed by the graph's node count, so they are unique per site and **no new global is introduced**. ⭐ **TWO SEMANTICS A SIMPLER DESUGARING GETS WRONG, BOTH MEASURED AND BOTH AGREEING IN m3 AND m4:** a FAILED match must not write (`A<1> 'ZZZ' = 'THERE' :F(...)` leaves `A<1>` alone and takes the F branch), and **the subject lvalue is evaluated exactly ONCE** — with the index a counting function, `A<IDX()> 'HELLO' = 'BYE'` reports `calls=1` like the oracle, where re-lowering the subject for the write-back would have called it twice **and no fixture in the suite would ever have shown it**. **CURE 2, an unescaped label name (SCRIP `99f302962`):** pre-existing, m4 only, and it presented as a compile-time SKIP rather than a diagnosis — the label-name table emitted `.string "THAT'S//""` raw, which gas reads as an unterminated string, so the program compiled at rc=0 and then failed to ASSEMBLE. The cure is the escaper the same file already calls two lines away for proc and parameter names; the label line simply never called it. `eliza-duquet-original` and `eliza-modernized` are both green in both modes, each verified by direct oracle diff rather than inferred from the board. Control arms on both landings, all no worse than the clean tree: SNOBOL4 master **1894/1894**, Icon master **704/704**, Prolog master **m3 518 · m4 439 of 559**. Board write checked clause by clause against the ceo's interim silent-revert rule: only my Snoflake clause and its provenance stamp moved.
- **HQV-9 (hq_V, 2026-09-08 21:5x CDT) — THE ROW IS CLOSED ON A GATE THAT WAS PROVEN RED FIRST, AND THE REGRESSION-DEBT AUDIT CAME BACK CLEAN.** ⛔ **THE ROW'S MINTED DONE-WHEN WAS PROSE AND `done` REFUSED IT** — correctly: a DONE-WHEN is EXECUTED, so a parenthetical breaks it and a bare `scripts/...` path breaks it too (the runner's cwd is not mine; the fleet spelling is `cd "$S4E_HOME/SCRIP" && ...`). Replaced with a real command and the prose moved to an `ACCEPTANCE, IN PROSE:` LEDGER line. **THE GATE** (`test_gate_sno_repl_lvalue_subject.sh`, SCRIP `22860871f`): 18 arms, one witness × three engines × six checks, graded against `sbl -bf` **in the same run** rather than a recorded ref. **Proven FAIL-ONCE at 12/18 red on the reverted tree and PASS-ONCE at 18/18 here — and the six `sbl` arms stayed green in BOTH runs, which is what makes it a conformance measurement instead of a convention.** ⭐ **THE THREE SHAPE ARMS ARE THE OBVIOUS HALF; THE TWO THAT EARN IT ARE THE FAILED-MATCH AND SINGLE-EVALUATION ARMS, BECAUSE NO FIXTURE IN THE SUITE CATCHES EITHER** — the eliza pair passes whether or not they hold, so the desugaring could regress into a plausible-looking cure with every board still green. The single-evaluation arm counts calls to a counting function used as the subscript index; that is the only way a double evaluation is visible at all. **REGRESSION-DEBT AUDIT ON MY OWN RECUT (the ceo's rule, checked rather than assumed):** my CEO-409 mask hid the FREE SPACE line, which was **the only place on any board where the coo's COLLECT-returns-zero cure was visible**, and the SNOBOL4 master holds no COLLECT witness at all (its one hit is a comment). **The debt is nevertheless ZERO** — `test_gate_collect_reports_free_storage_and_fails_when_short.sh` exists, I ran it, and its 12 arms assert COLLECT is a NONZERO INTEGER and that `COLLECT(N)` decides on its argument, oracle-agreeing in both modes. Reported anyway, because a seat reading only the boards would conclude the opposite. **ON THE ceo's `85` CORRECTION:** nothing owed from my slice, and I confirmed hq_B's sweep from the other side — **none of my 15 graded snoflake reds diffs against a SPITBOL fatal report or its allocator counters**, every one is a program `sbl` runs clean, which is why tonight's cures actually moved the board. **ON THE coo's csnobol4 PLAN BUILT ON MY MASK:** it has the mechanism right (a mask REPLACES and never deletes, so emit-then-mask), but I flagged an interaction it had not hit yet — **guardrail 4's majority refusal is mechanical, and SPITBOL's termination report is ~6 lines**, so a short csnobol4 program would be six masked against three graded, the mask would be the majority, and my runner would REFUSE rather than grade. The 22 will therefore SPLIT, and I asked it to measure that before quoting a number. I will not weaken the guardrail to make the class pass.
- **HQV-4 (hq_V, 2026-09-08 19:38 CDT) — TWO CEO TELEGRAMS FILED, AND THE SEAT IS STOOD DOWN UNDER QUARTET.** Inbox drained at the prompt boundary (3 messages, all from `ceo`). **(1) MODE is QUARTET** (`/home/resources/postoffice/MODE` line 1, set 2026-09-08 19:09 CDT on Lon's word, verbatim: *"I'm firing up 4 HQ's: HQ-COMPLETE, HQ-PERFORM, HQ-UNIFY, and HQ-TEST."*). The four HQs QUARTET names are hq_C, hq_P, hq_U, hq_T — **hq_V is NOT among them**, so this seat stays stood down as the 07:51 EXECUTIVE telegram ordered; the order of work is SNOBOL4 and Icon to 100% for the 09-10 announcement, and **Pascal ships as it stands (demonstrable), its rows PARKED-LON-HOLD**. The two mode telegrams (`mode-ceo-on-lons-word-0713`, `mode-executive-hqs-stop-0751`) are therefore superseded on arrival and are filed here, not acted on: no row is picked, nothing is claimed. **(2) CEO RULING ON `iso7185prt1809` — THE GLOBAL IS REFUSED AND THE REGISTER NEED NOT EXIST**, and this CORRECTS what HQV-2 recorded above. HQV-2 said the control-variable-is-`real` check *"is a NEW GLOBAL, which needs Lon's grant"*; the ceo rules that **no global is asked for and none is granted**, because *the fact "this variable is a real" is a property of a DECLARATION, so it lives in the parser's symbol entry for that variable, where the other type facts already live* — i.e. `src/parsers/pascal/` symbol data, not a new registry and not a new global (RULES.md FACT RULE: a global needs Lon's in-chat grant asked as a banner naming it). `iso7185prt1809` stays RED under its own row; the bug is not to be widened to reach it. **(3) HQV-3 read and accepted:** the ceo confirms the row `pascal-m4-intermittent-segv-layout-sensitive` **correctly splits into two defects**, and that **the 14 deterministic m3-pass/m4-fail programs are the tractable half — the row's NAME must say so when it is next picked**, so the next claimant does not re-inherit the conflation. That renaming is owed to whoever picks it after the announcement; it is not done now because no row may be claimed from a stood-down seat.
- **HQV-3 (hq_V, 2026-09-07 07:12 CDT) — THE RANK-0 REOPEN'S RED PROOF DID NOT REPRODUCE, AND THE ROW CONFLATES TWO DEFECTS.** Took `pascal-m4-intermittent-segv-layout-sensitive` (CEO-379) after releasing the for-loop row, and did the required red proof FIRST. Five consecutive `test_pascal_fpc_suite.sh` runs on ONE clean tree (SCRIP `6ba558ce7`, corpus `85dcc71d4`, runs 21:22–21:31 CDT 09-06) read **m3 130 · m4 116 · m4_fail 65, identical on all five — and the m4 FAIL NAME LIST byte-identical too, md5 `4cd8ca0aac01`**, 65 m4 FAIL rows per run in the progress database. A stable count could hide two programs trading places; an identical fail set cannot. **(a) The 130-vs-116 gap is NOT the wobble** — it is 14 programs failing m4 *deterministically* while passing m3, a different defect with a different cure (ASM-DIFF-FIRST, per program), very likely a bigger and more tractable win than the flake, and it deserves its own row. **(b) The flake class is not disproved** — its witnesses (`boolmix`, `boolchain`, `pb30`, PASS → EMPTY_rc139) live in `test_gate_pascal_m4.sh`'s LOOSE population, and this baton already recorded that all 17 suite families were constant while only the loose set moved. ⛔ **So the criterion the reopen names cannot be written as asked**: "five runs read ONE m4 count" is ALREADY MET on this tree and would pass on arrival — the vacuous-green shape — so I did not write it in. Proposed to the ceo instead: N runs of the LOOSE witnesses yielding an identical PER-PROGRAM verdict set, N stated, suite arm kept as the cheap regression guard it has now been measured to be. Evidence in `FINDING-2026-09-06-hq_V-the-fpc-suite-m4-arm-is-deterministic-…md`. Not a claim that hq_T mismeasured: five identical runs at one load do not refute a reading at another, and an intermittent SIGSEGV is exactly the class that hides from whoever looks for it. Row released FREE with the work recorded on its baton rather than held across a session boundary.
- **HQV-2 (hq_V, 2026-09-06 21:2x CDT) — FIRST BUG LANDED: ISO 7185 §6.8.3.9, SIX PAT PROGRAMS, BOTH MODES.** Row `pascal-pat-for-loop-control-variable-protection`, claimed and cured. SCRIP `6ba558ce7`, corpus `85dcc71d4`. **This is Pascal's FIRST SEMANTIC CHECK** (`src/parsers/pascal/pascal_sem.c`, called from `pascal_compile` so both modes get it from one seam): every one of the 300 PAT rejections this compiler already made was a SYNTAX error, which is precisely why 122 syntactically-valid-but-illegal programs were accepted. Flipped, both modes, by the runner's own per-program PASS lines: `iso7185prt1801` `1802` `1804` (threat inside the controlled statement — assignment, var-parameter, reuse as a nested control-variable) · `1805` `1806` (threat inside a procedure DECLARED IN THE BLOCK, where a call can reach the variable mid-loop) · `1808` (control-variable not declared in the closest-containing block). Board: PAT m3 300→306, m4 286→292, **both-modes 286→292 under ceo-372**.
- **⛔ MY FIRST CUT OF THE THREAT RULE WAS WRONG AND THE CONTROL ARM CONVICTED IT — the most useful thing in this rung.** I scanned the WHOLE BLOCK for threats, which is what "threatened by the block" reads like and what HQV-1 above says. The Pascal master caught it immediately: m3 243→221, because `for i := 5 downto 1 do s := s + i; for i := 10 downto 5 do writeln(i)` is two sequential loops over one control-variable — ordinary legal Pascal — and a block-wide rule calls the second a threat to the first. THE RULE THAT SATISFIES ALL SIX TESTS AND THE CORPUS HAS TWO ARMS: a threat inside the for's own CONTROLLED STATEMENT, or a threat anywhere inside a procedure DECLARED in the block. The second arm is what makes 1805/1806 violations while sequential loops stay legal — the hazard is a CALL reaching the variable during the loop, not textual proximity. Shadowing stops the descent (a nested procedure with its own `i` is a different `i`; its whole subtree is skipped). Correcting HQV-1's "block-wide, not loop-body-wide" to this two-armed statement.
- **A MASTER FIXTURE WAS ITSELF AN ILLEGAL PROGRAM** (corpus `444719ac3`): `parser__for_loop`'s entire body was `i := i` — the very violation `iso7185prt1801` exists to catch — invisible for as long as it existed because the entry is `modes=ast`, so it was graded on its parse tree and never executed, and nothing ever asked whether it was legal Pascal. `fpc -Miso` unprompted: *"Illegal assignment to for-loop variable"*. THERE IS NO XFAIL, so it was fixed against the oracle (`j := i`, ref updated to the one node that changes), not tolerated.
- **CONTROL ARM, all on the landing tree:** Pascal master ast 5/5 + run 243/246 = **248/251 in BOTH modes, byte-identical to baseline**; FPC **130/181 m3 · 116/181 m4, unmoved** — no false positive anywhere in 181 vendored programs; `make test` **rc=0**, 34 gates. Not a shared node (`src/parsers/pascal/` + the Makefile source list only), so no hq_U co-sign owed.
- **CEO-375 NAMING, as that ruling requires** — three greens where we are STRICTER THAN THE RIVAL, which is conformance and must never later be read as a regression: `PAT iso7185prt1805: ISO rejects, fpc accepts, SCRIP refuses` · `PAT iso7185prt1806: ISO rejects, fpc accepts, SCRIP refuses` · `PAT iso7185prt1808: ISO rejects, fpc accepts, SCRIP refuses`. (1801/1802/1804: ISO rejects, fpc rejects, SCRIP refuses.)
- **NOT DONE, AND NOT SMUGGLED INTO THIS BUG:** `iso7185prt1809` (control-variable of `real` type) stays red. Nothing in this front end records that a variable is real — there is no registry to ask — and building one is a NEW GLOBAL, which needs Lon's grant. It belongs to its own row, not to a quiet widening of this one.
- **⚠ THE SUITE TABLE NOW RENDERS ceo-372 AS A REGRESSION, AND IT IS NOT MINE TO REBASELINE.** With the rows correctly carrying the AND reading, the table computes movement against a FIRST-READING column still holding pre-criterion m3-only numbers (`298/427 (09-04)`, `119/181 (08-30)`), so both Pascal rows now render **`-6 ⛔ stuck`** and **`-3 ⛔ stuck`** — a criterion change displayed to Lon as lost ground. The numbers published are the true ones (FACT RULE); the comparison is the defect. This will hit EVERY lane ceo-372 re-criterions, not just Pascal, so it is named to hq_T and the ceo rather than patched in my two rows.
- **NEXT: `pascal-m4-intermittent-segv-layout-sensitive` (rank 0, reopened to this lane by CEO-379)** — taken now that the for-loop row has landed, one bug at a time under OCTET.
- **HQV-0 (2026-09-06 20:08 CDT, written by ceo at the opening):** the root is stocked by `populate_hqv_root.sh` (three clones at origin/main, ONE-IDENTITY config, hooks, refs symlinks, `.claude/settings.json` with the banner and inbox hooks, this file's digest as `CLAUDE.md`, postoffice `hq_V/{inbox,archive}` with `HQ=ceo`). FIRST SITTING, in order: (1) the session start in CLAUDE.md, then one incremental `make`; (2) cite SCORE.md § THE SUITE TABLE (PasM 248/251 · FPC 130/181 · PAT 300/427 at the opening, all three STALE >24h except PasM), then run `test_pascal_pat_suite.sh` and `test_pascal_fpc_suite.sh` ONCE each on the clean tree, let them set their rows, push `.github`, and append every red program to `probes/package-red-pools-2026-09-06.tsv` (suite · program · outcomes) so the pool census sees the lane; (3) take the first bug by rank from the pool — the PAT classes are minted as rank-3 rows `pascal-pat-*` (18 of them, by class) under the rank-0 umbrella `pascal-pat-suite-427-to-100-percent-by-class`, the FPC classes as rank-1 rows `pascal-fpc-class-*`; re-rank by what the runner shows is behind the most programs, one bug at a time; (4) each flip to `coo` names the program, the suite, both modes, and the tree; each cure lands with a gate proven red-then-green and its receipt in the baton. Write HQV-1 here when the pool is populated: the two boards, the pool count, the first bug in hand.
- **HQV-0a (ceo, 2026-09-06 20:15 CDT):** STARTED — Lon 2026-09-06 20:1x CDT, in-chat to ceo, verbatim: *"I started HQ-VALIDATE."*; hq_V's transcript exists and its receipt reached the ceo at 20:11 (Lon's first in-chat word in that seat was `/init`, so the seat writes its own guidance file first, routes that into this file, then runs the two suites and populates the pool — the ceo confirmed that order). The ninth HQ is live.
- **HQV-0b (ceo, 2026-09-06 20:16 CDT):** FIRST ACT AMENDED — the coo had already run both boards on clean 801c0612a (FPC m3 130/181 · PAT m3 300/427; m4 116 and 286 cited as samples, the FPC runner's own header warns m4 counts are layout-sensitive); cite them, do not re-run. Neither Pascal runner appends per-program progress rows or prints a per-program verdict, so no red NAMES exist for the pool and a Pascal flip is invisible to the measure: hq_V's first landing is the two runners appending one progress row per program per mode through `lib_progress.sh`, printing PASS/FAIL per program, writing a one-fraction `--text` per mode with `--suite-pass/--suite-total` so `util_score_row.py` sets the rows itself, and PAT's `2/0 crash` diagnostic spelled so the writer does not read it as a fraction; hq_T co-signs. Then one run each, the pool, the first bug, HQV-1.
- **HQV-18 (hq_V, 2026-09-09 18:5x CDT) — CEO-477 LANDED: THE CRITERION CORRECTION IS ONE ENTRY, THE CLASS WAS SIXTEEN, AND THE JOINER IS FIXED IN THE TOOL.** Tree SCRIP `df41c4250` corpus `2e4610172`, Arizona icont/iconx 9.5.25a, incremental make. THE FINDING: `FINDING-2026-09-09-hq_V-the-ceo-477-criterion-correction-is-one-entry-not-fourteen-and-the-class-was-sixteen-not-fourteen.md`. ⭐ **I PREDICTED A DROP OF UP TO FIFTEEN AND MEASURED ONE.** All 15 refs were re-cut from the oracle after the text was repaired and **14 came back byte-identical to the ref already stored** — because the semicolons our pass inserted were invalid Icon that changed nothing observable (`;` before `else`/`then`/a case body's `}` is a syntax error to icont and a no-op to us), so the refs were always right about the OUTPUT and always wrong about the LANGUAGE. The 14 stay green and are now gradable against the oracle at all, which was the entire point. The ONE flip is `procedure_write_45`: the oracle raises error 105 `file expected` on `reads(5)` because `reads(f,i)` takes the FILE first; its ref is now empty and `ALL.wantrc` declares rc=1 from the ORACLE, and we are RED because we accept a non-file first argument and silently read it as a byte count — a real conformance defect, routed to its rung. ⛔ **TWO CORRECTIONS TO MY OWN 18:2x FINDING, both found by re-running the sweep after the cure instead of trusting the diff.** (1) The class was **16, not 14**: `procedure_every_alt_replace_4` (rung36_jcon_kwds, same case-body shape, pre-existing, my count was simply low) and `procedure_record_limit_replace_1`, which my finding had **dismissed as my own artifact** — that dismissal was wrong; the cause was in the companion, `config/prepro.dat` carrying `;` on three directives, and `$define abc 321;` expanded abc to `321;` so the INCLUDING entry failed thirty lines later at an innocent line. (2) `procedure_every_alt_replace_4`'s ref is **NOT re-cuttable and I did not re-cut it**: it is a keyword dump whose diff is entirely implementation identity (`&version: Jcon 2.2` vs `Icon 9.5`, `&allocated`, `&regions`, `&storage`, and `&progname` which would embed my temp path). Its TEXT is repaired, its REF stands; RED before and after; it needs a mask or an outside-baseline ruling from its owner — origin is a Jcon J–Z red, so **hq_I's**, telegraphed not assumed. **THE POPULATION PROOF: icont refusals over the 756 run-graded entries went 16 → 0.** **THE BOARD, RUN ON THE MATCHED TREE (SCRIP `f59db4cd8` corpus `f3121f507`, binary rebuilt after the rebase because a board whose binary predates its own HEAD is a stamp on a tree that never existed — I killed a first run for exactly that and re-ran it): entries=909 · run-graded 756 · m3 PASS=743 · m4 PASS=743, both modes equal, xfail=0 xpass=0, watermarks held, SCORE.md and SUITES.tsv rewritten by the runner in the same call.** 740/754 → 743/756: the denominator grew by the two witnesses absorbed this tick and the numerator carries my −1 criterion correction against other seats' cures landing in the same window. ⭐ AND THE cfo's FLEET RED IS CURED BY THIS LANDING, not worked around: `test_gate_master_order_is_the_builders_order.sh` read RED on corpus `05dbe274e` (49 of 908 entries out of order from index 859, an absorption that moved a sort key without a re-sort). It reads **`ok icon 910 entries in the builder's order`** here, because every absorption in this tick went through the builder, which writes in its own order. Nothing to re-sort by hand and no seat pays for it twice (CEO-463). The joiner is repaired in `util_strip_nonstatement_semicolons.py` with a string/comment-aware scanner carrying four shapes, each measured against icont first, and two guardrails that are why it was measured: the trailing-`;` rule fires ONLY for a case body (`{1;2;3;}` is VALID Icon and its trailing `;` CHANGES THE VALUE), and the paren shape is not a semicolon to delete (dropping them leaves `(1 2 3)`, also invalid) but a delimiter to change, `( )` → `{ }`. Its population had a hole that hid correction (1): it walked `*.icn`, and `prepro.dat` is `$include`d so icont preprocesses it like a source; `.dat` is now in the population and two dangling symlinks are named-and-skipped rather than refusing. ⛔ **THE 47-FILE CENSUS IS HANDED OVER, NOT SWEPT**: with the new rules the tool reports 47 files / 236 lines across jcon_tests, arizona_tests, ipl and demos — other seats' lanes, each needing its own suite re-graded, so I repaired NONE of them. Note `tests/icon/rung36_all.icn` (6 lines) is the loose source the master's meander/endetab entries came from and will re-corrupt them if re-absorbed unrepaired. **The parser half stays pinned for hq_C**: `procedure_write_85` and `procedure_case_write_7` still hold Icon icont refuses, both `ast`-graded parser fixtures from `parser/KEEP.md` — they are the EVIDENCE for the ceo's row `icon-parser-accepts-a-semicolon-icont-refuses-...`, so repairing them would delete the defect's own pin. Proof the order named: meander and endetab now read character-for-character like their vendored originals and icont accepts both.
- **HQV-19 (hq_V, 2026-09-09 19:3x CDT) — THE FIFTH WITNESS FLIPPED, AND A STALE BINARY NEARLY MADE ME REPORT A FALSE RED ON ANOTHER SEAT'S CURE.** The cfo's cure for `call_through_a_static_variable_reads_the_static` landed as SCRIP `183aee4a1`; the entry (`procedure_record_every_replace_17`) reads **PASS in m3 AND m4** on SCRIP `e2dd358b7` corpus `f3121f507`, against the ref I re-cut from Arizona icont at absorption. RED ON ARRIVAL at 19:0x, green at 19:3x — **the witness reported its own cure, which is the whole reason a red-on-arrival pair is worth absorbing instead of waiting for the cure and absorbing a green one.** ⛔ **THE NEAR-MISS, RECORDED BECAUSE IT WOULD HAVE LANDED ON SOMEONE ELSE:** the cure hit origin at 19:22 and my binary was built at 19:09. Grading on reflex would have printed the same ERROR 022 and I would have told the cfo and the coo that their cure did not work. I rebuilt to 19:31 first. **On a flip report specifically a stale binary is indistinguishable from a live defect — same diagnostic, same exit code — so the rebuild is not hygiene there, it is the measurement.** ⛔ **AND I DID NOT REPUBLISH THE cfo's FRACTION AS MINE:** the cfo reads IcnM 747/756 on its own merged tree; I graded the single entry and told the coo the number is the cfo's with the cfo's stamp, because a seat that repeats another seat's fraction as its own is exactly how one measurement becomes two boards (CEO-308). `procedure_write_45` remains FAIL both modes on the rebuilt tree — the deliberate CEO-477 correction with its row open, not drift. **Routed to hq_P:** its ordering (rung36_all behind CEO-478's two OUTSIDE lists) is RIGHT and I told it not to flip — `rung36_all.icn` is **loaded but not armed**, since those six lines can only hurt through an ABSORPTION and absorption is single-writer and mine (CEO-452), whereas the OUTSIDE lists move the announced denominator; I also undertook that if I ever absorb it I run the repaired stripper and re-cut its refs first, so the hazard stops depending on either seat remembering. hq_P's own retraction is worth keeping as fleet knowledge: a pre-rebase hash in a telegram is recoverable by content, but **a pre-rebase hash in a PUSHED COMMIT MESSAGE cannot be corrected** — its `<->` cure commit pins `2ffead18a`, which does not exist.
- **HQV-1 (hq_V, 2026-09-06 20:35 CDT) — THE TWO BOARDS, THE POOL, AND THE FIRST BUG.** Session start clean (SCRIP `e06e2c131` → the two boards on `3109935b5`, corpus `8f5c58517`, binary rebuilt 20:27, both suites run ONCE each). ⛔ RULE 5 WAS NOT VIOLATED BY RUNNING: the coo's boards are `801c0612a`/`1340f33ea`, my tree is `3109935b5`/`8f5c58517` — different SCRIP AND corpus hashes, so this is a NEW board, not a re-run, and the coo's own message asked for exactly one thing its boards could not give (the ~107 unnamed PAT reds). **BOTH BOARDS REPRODUCE THE COO'S NUMBERS EXACTLY** — FPC m3 130/181 · m4 116/181, PAT m3 300/427 · m4 286/427 — which is a clean control arm over the two commits I rebased onto (`dae129a92`, `fb2939930`) and over my own instrument landing: no Pascal movement in either direction.
- **THE THREE INSTRUMENT DEFECTS, MEASURED THEN CURED (SCRIP `3109935b5`).** The coo named them at my opening; each was proven before it was touched. (1) **The rows could not move**: `util_score_row` reads ONE N/M from `--text` or refuses, and PAT's text carries THREE fractions (m3, m4, and the fraction-shaped `(2/0 crash)` diagnostic), FPC's two — proven by calling `fraction_from_text` on the real texts. hq_T cured the mechanism concurrently in `dae129a92` and chose the m4 arm, giving its reason (the published row carried m4) and inviting the one-line change; **that premise had already inverted** — the coo set both rows BY HAND to m3 in `.github 7b24d6a1`, so m4 was now the wiring that would publish a regression that never happened (PAT 300→286, FPC 130→116), the very harm hq_T's line existed to prevent. Wired to m3, with the reasoning at the line in both files, hq_T told and the ceo asked to rule; either way it stays one line. (2) **The measure could not see a Pascal package flip**: 497 pascal rows in `/home/resources/progress/results.tsv` and every one `pascal-master` — ZERO from `pat` or `fpc`, so no Pascal package flip had ever reached the histogram OCTET is run on. Both runners now append (one bulk `rows-tsv` call each); this sitting wrote **854 + 362 rows, the first Pascal package rows that table has ever held**. The `outcome` column carries the VERDICT and `note` the DIAGNOSIS, which is PAT's own load-independence ruling applied to the database. (3) **The pool could not be built from the instrument that measures it**: PAT's red listing was capped at a literal `head -40`, hiding 87 of 127 m3 reds; `PAT_NAME_REDS_MAX=all` lifts it, default unchanged. Also fixed in passing: a crashed ACCEPTANCE test incremented PAT's crash column but not its fail column, so a crash left pass+fail one short of total — latent only because both acceptance fixtures are ungradable by the oracle.
- **THE POOL IS POPULATED — AND PASCAL IS THE LARGEST RED POOL IN THE FLEET.** `probes/package-red-pools-2026-09-06.tsv` 237 → 443 lines: **PAT 141 · FPC 65 = 206 of the 442 red package programs**, against inria 95 · csnobol4 53 · jcon 38 · ipl 32 · swi 8 · aisnobol 8 · gnu 1 · gimpel 1. Nearly half the fleet's remaining package work is Pascal, and it carried zero fixers until this seat opened.
- **THE PAT RED CENSUS (m3, 127).** 122 `accepted` (scrip runs a program ISO 7185 says it must refuse) · 3 `rejected-silently-no-diagnostic` (`iso7185prt1701`, `1744`, `1828` — already refused, only the diagnostic missing) · 2 `crash(rc=134)` (`iso7185prt1705`, `1849`). All five of those cheap ones are `accepted` in m4, so m3 holds a check m4 does not. The 122 are NOT one class — they are 122 distinct ISO error conditions, which is why this suite's ETA reads 2027-01-11 — but they cluster: for-statement index 8 · variant/tag 9 · string 7 · file/eoln/eof 6 · declaration order 5 · pointer 5 · case 4 · undefined 4 · packed 3.
- **FIRST BUG IN HAND: `pascal-pat-for-loop-control-variable-protection` (rank 3, was FREE).** ISO 7185 §6.8.3.9, **7 PAT programs from one front-end check**, and front-end means BOTH MODES at once: `iso7185prt1801` `1802` `1804` `1805` `1806` (the control variable must not be THREATENED by the block containing the for-statement) · `1808` (it must be an entire-variable declared in the closest-containing block) · `1809` (it must be of ordinal type). ⛔ THE THREAT RULE IS BLOCK-WIDE, NOT LOOP-BODY-WIDE — in `1805` and `1806` the threatening assignment lives in a sibling procedure declared in the same block and called BEFORE the loop; a checker that only walks the loop body scores 3 of these 5 and looks correct. `1811` (index undefined after the loop) is deliberately NOT in this bug: it needs definiteness tracking and already has its own row, `pascal-pat-definite-assignment-tracking`.
- **⚠ AN AUTHORITY QUESTION THE CEO SHOULD SETTLE, RAISED NOT ASSUMED.** `fpc -Miso` 3.2.2 — my CLAUDE.md's named oracle and rival — **rejects only 4 of these 8** (1801, 1802, 1804, 1809) and ACCEPTS 1805, 1806, 1808, 1811. The PAT runner is explicit that the 427 rejection tests are graded on whether SCRIP refuses them and that fpc is the oracle for the ACCEPTANCE population only, so the suite governs here and all eight are genuine §6.8.3.9 requirements that FPC's ISO mode does not implement. I am curing to the STANDARD as PAT states it, per Lon's "100% of the industry standard language". Flagged because it means a PAT green can be a place where we are STRICTER than the rival, and that must never be read as a defect.
