# HANDOFF_2026-05-19k.md — EC series complete (EC-6 + EC-7)

**Date:** 2026-05-19
**Session:** Claude Sonnet 4.6
**Goal:** GOAL-HEADQUARTERS.md — EC-6 + EC-7

---

## What was done

### EC-6 ✅ (one4all `7c33121c`)
- Deleted `src/emitter/emit_wasm.c` (747 LOC).
- Moved into `emit_core.c`: WASM string table (`wasm_strtab_*`), user-fn table (`wasm_userfns_*`), `emit_wasm_from_sm`, WASM arms in `emit_prologue` / `emit_epilogue` / `emit_program`.
- Added `EMIT_WASM = 8` and `IS_WASM` macro to `emit_core.h`.
- `scrip.c`: removed `extern emit_wasm_program`; replaced call with `emit_program(ast_prog, stdout, EMIT_WASM)`.
- `Makefile`: removed both `emit_wasm.c` references (SRCS list + scrip compile line).
- Net: **−427 LOC**. Gates: 5/0 · 23/26 · 194/36 — all at floor.

### EC-7 ✅ (.github `f9f99110`)
- Full gate run: sno 7/0, icn 5/0, pl 5/0, rk 5/0, sc 2/3 (pre-existing), rb 4/0, broker 23/26, icon rung ladder 194/36. All at floor.
- `ARCH-IR.md` updated: appended "Unified Emitter Model" section documenting mode enum, `emit_program` entry point, how to add a new backend/opcode, and post-EC file map.
- EC rung **closed**.

---

## State

```
one4all: 7c33121c  (EC-6)
corpus:  92e103f   (unchanged)
.github: f9f99110  (EC-7 + this handoff)
--interp:  194/265  (held)
broker:    23/26    (held)
smoke ×6:  7/0 5/0 5/0 5/0 2/3 4/0 (held)
```

## Architectural fact now true

Every SM opcode and BB box kind has exactly one function in `emit_core.c`.
No per-target silo files remain. Adding a new backend = adding `case IS_NEW:` arms.
Deleted in EC series total: `emit_jvm.c`, `emit_js.c`, `emit_net.c`, `emit_ir.c`,
`emit_ir_targets.c`, `emit_wasm.c`, `emit_ir.h` shim, `IR_emit_vtable_t`.

## Next

Per PLAN.md the EC goal row is closed. Lon names the next goal.
