# FINDING — sweep-free-rows-are-real: 89 free rows classified, 8 already fixed, 6 stale briefs, 2 new rows minted

**Session:** 2026-08-23 seat02 (`/home/claude02`, Claude Sonnet 5), THE LOOP row `sweep-free-rows-are-real`. Snapshot frozen at SCRIP `76ec253d`, corpus `f34ce80a`, `.github` `e6425fb5` (fresh `git pull --rebase` all three + fresh `make -j4 scrip`, so every verdict below runs against one consistent HEAD). Method used 7 parallel forked sub-agents (batches A/C/D/E/F/G/H) plus one batch (B) resolved directly from a document; see METHOD below.

## THE DELIVERABLE HQ ASKED FOR

The brief (HQ s256) said 44 free rows and asked "how many are real" as the number nobody had measured. That count is stale by growth alone — QUEUE.tsv is live and grew from 105 to 155+ data rows *during this single session* (visibly, mid-sweep, from other seats/HQ minting). **At this session's frozen snapshot (2026-08-23T20:57Z), the true free-row count — state `FREE` (not PARKED/SUPERSEDED) AND no `claims/<topic>.claim` file, which is what `s4e_msg.sh next` PASS 3 actually serves, not the queue's decorative `owner` column — was 89, not 44.**

Of those 89:
- **8 were ALREADY-FIXED** (9.0%) — retired via `claim` + `done` this session, DONE-WHEN re-verified live for each, four of them needed their DONE-WHEN itself repaired first (see below).
- **6 were STALE-BRIEF** (6.7%) — a real defect/need exists but the brief's specifics were wrong (wrong file path, wrong framing, wrong lane, or a vacuous completion test); rewritten in place, left FREE (one PARKED).
- **75 were confirmed LIVE** (84.3%) — the defect/need still reproduces as described. Of these, several had mechanical DONE-WHEN defects (hardcoded other-seat paths, missing Icon-witness semicolons) fixed in passing so they're honestly closeable once someone lands the real work; four were flagged for a rank change; two new rows were minted from gaps a fork surfaced while classifying its batch.

**So the honest answer to "how many of the free rows are real" is ~84% at this snapshot** — lower than HQ's own guess that "a meaningful fraction" would be dead (9% dead + 7% stale-but-real ≈ 16% non-actionable-as-written), but the fleet's own growth rate means this number decays continuously; treat it as a point-in-time measurement, not a standing fact.

## METHOD

1. Cross-referenced `QUEUE.tsv` against `claims/*.claim` to build the true-free set (`state==FREE` and no claim file) — 89 topics, all of which already had V2-2 task-file batons (no baton-minting needed).
2. Batched the 89 into 8 groups by family (false-green gate scripts, `rationale-*` file relocations, SNOBOL4 conformance-vs-oracle, Icon N-ladder, perf/architecture, correctness/crash bugs, misc process/ruling rows, goal-cursor/triage rows).
3. Batch B (8 `rationale-*` rows) resolved directly: `.github/RATIONALE-INDEX.md` (freshly pulled this session) already lists all 8 as its own "Remaining clusters, not yet relocated" table — no repro needed, all 8 confirmed LIVE, correctly tail-ranked, no rewrite.
4. The other 7 batches (81 topics) each ran as a parallel forked sub-agent, read-only (no claim/done/park, no file edits — that was reserved for this orchestrating session to avoid concurrent-write races across 7 simultaneous checkers on shared files), budgeted ~5 min/topic, required a receipt (command+output, git log citation, or a direct file-content citation) per verdict.
5. This session then performed all retirements, rewrites, re-ranks, and minting centrally, re-reading each shared file fresh immediately before writing it (md5-verified no concurrent change against a snapshot backup before the one multi-row QUEUE.tsv edit).

## THE 8 RETIRED (ALREADY-FIXED)

| topic | fixed by | DONE-WHEN needed repair? |
|---|---|---|
| `conform-case-keyword-mismatch` | SCRIP `f2bbeeb5` (&CASE defaulted to the folding oracle arm, contradicting the mandatory `-bf` arm) | no |
| `json-gcstress7-segv` | plausibly the no-pin rooted GC landing (s263, `c61c5610`/`927d0521`/`1257d56c`) | no |
| `bench-refs-empty-ident-call` | corpus `90dbbb89` (benchmark `.ref` minting pass) | no — but see `sweep-zero-length-refs` below, its own second ask was never done |
| `beauty-fixed-point` | M1 self-host fixed point holds, corroborated independently by hq_P s266 and seat15's `reprofile-after-byname-bake` closure | no |
| `json-m3-m4-divergence-dcap-pump` | plausibly `2037a02f` (seat09, jdec capture-after-alternation fix — same `rt_dcap_pump` subsystem) | **yes** — old DONE-WHEN grepped stdout for a `^check: ` line json.sno's output format never contains at all; rewrote to compare m3≡m4 output (mod one nondeterministic-position timing line) and confirm no corruption marker |
| `const-ordinary-compose-seam` | plausibly other const/pattern-family work landed 2026-08-17→20; `.ref` asserted semantic-truth by pinning commit corpus `2ee3f10e` | **yes** — old DONE-WHEN was the permanent PROSE-refusal stub; authored a real one (3 witnesses × 2 modes byte-identical to `.ref`) |
| `datatype-case` | its own blocker (`scorecard-oracle-case`) already resolved in favor of `-bf` as the mandated oracle; under that now-settled mode SCRIP already agrees with the oracle | **yes** — same permanent-refusal stub; authored a real one (inline witness, both engines agree) |
| `opsyn-evalpat-residue` | plausibly RT-CARRIER/RTSEQ-RESUME family work (same family credited with curing this row's own now-defunct sibling `ptw_min_compose`) | **yes** — old DONE-WHEN named a file that has never existed (`corpus/crosscheck/functions/opsyn_evalpat_residue.sno`); corrected to the real witness the brief itself named throughout (`corpus/probe/passthru/ptw_min_opsyn_evalpat.sno`) |

**Four of eight ALREADY-FIXED rows had a DONE-WHEN that could never have certified completion regardless of fix status** — two permanent-refusal stubs never replaced, one wrong/nonexistent file path, one grep for a marker string the program never emits. This is the same disease the sweep brief warned about (seat1's `bench-timed-oracle-flag` precedent) but on the *closing* side rather than the *staleness* side: a fixed defect sitting behind a broken gate is invisible to the fleet in exactly the same way a fixed defect sitting behind a stale FREE row is.

## THE 6 STALE-BRIEF (rewritten in place)

- **`bb-fixup-rank-85-dirty-templates`** — GOAL described the audit script's old "no exit statement" bug (fixed at V2-5, `e88e77db`, before this row was minted); rewrote to lead with the real remaining state (85/148 template files still need conversion, a LAP/backlog row not a bug row). Re-ranked 4→6 (non-blocking backlog per its own BRIEF).
- **`icon-r0-bisect`** — GOAL claims Icon-lane sovereignty ("GOAL-ICON-100.md LIVE CURSOR") but its NEXT is entirely about SNOBOL4/M1-lane "R-0" terminology, which appears nowhere in GOAL-ICON-100.md. GOAL-SNOBOL4-100.md's own current text suggests R-0 may no longer reproduce at all ("all 40 probe/m1/ witnesses... reach it ZERO times"). Asked HQ (`q-icon-r0-bisect-misfile`) to rule refile-vs-close; left LIVE/FREE pending that ruling rather than deciding unilaterally.
- **`perf-table-array-runtime`** — its own named smoking-gun defect (VCELL-alloc-on-read for every table subscript, including pure reads) is confirmed CURED (`ce48e3bb` + asm port `05f11c4c`, comment "RETURNS THE VALUE, AND ALLOCATES NOTHING (hq_P s264)"). Rewrote GOAL/NEXT to say so and to demand a fresh callgrind remeasurement before anyone assumes the headline 2.8x ratio has closed — explicitly NOT retired, since closing on stale pre-fix numbers would be the exact error this sweep exists to catch. Cross-referenced against the next row.
- **`table-int-keys-and-nd-subscript`** — of its three named defects, (1) integer-key stringification is FIXED (type-tagged hashing, `tbl_typed_off()`) and shares defect (3)'s VCELL-alloc fix with the row above; (2) N-D dispatch consolidation is confirmed STILL LIVE (`array_get2`/`array_set2` combined-formula functions exist in `aggregates.c` but are dead code — zero callers in lower/templates/emitter); (3) `.NAME` representation cost remains unmeasured. Rewrote GOAL to scope the real remaining work down to (2)+(3) only. Two prior seats (seat09, seat15) mis-locked and released this row without ever reading current source against the brief — this sweep is the first session that did.
- **`setjmp-per-builtin-call`** — DONE-WHEN (`test_corpus_snobol4.sh`) is vacuous relative to its own claim (proves nothing about setjmp call counts) — exactly the "decorated no-op" class the sibling row `donewhen-decorated-noop-evasion` exists to police. Did not author a replacement (needs a real callgrind/strace-based measurement, which is investigation work, not a mechanical path fix); flagged clearly in NEXT/LEDGER instead. Underlying claim likely still live (the one commit that touched a nearby setjmp, `1257d56c`, is a *different* setjmp — GC pin snapshot, not builtin-call unwind).
- **`prolog-next`** — PARKED (not just rewritten). Its `## NEXT` was a broken template placeholder ("Its cursor\s"); fixed with the real current GOAL-PROLOG-100.md cursor content. More importantly, this evergreen dispatch stub was sitting plain FREE at rank 8, pickable by the rank-sorted picker, despite Lon's s264 standing order (SCRIP `9cf506c9`) explicitly closing the Prolog lane while Icon gets live development — checked for any later reopening (none found in `.github` git log or RULES.md/PLAN.md). Parked via `s4e_msg.sh park prolog-next PARKED`; un-park with `park prolog-next FREE` once the order lifts.

## OTHER HOUSEKEEPING DONE

- **Duplicate queue row found and fixed:** `icon-n1-wire-stack-crossing` appeared twice in QUEUE.tsv (rank 0 and rank 1, same topic, same task file) — removed the rank-1 duplicate line, kept rank 0.
- **4 rank changes applied** on confirmed-LIVE rows, on fork-supplied reasoning: `core-err-stmt-never-advances` 6→2 (every runtime error fleet-wide reports `in statement 0` — this is the shared debugging instrument for every other oracle-differential bracketing session, not a narrow single-program bug); `eval-code-end-terminates-m4` 6→2 (confirmed SIGSEGV, not silent-wrong-answer — a crash outranks the correctness rows it was ranked alongside); `autobug-dead-child-false-agree` 5→3 (a bug-finder that reports false agreement when its child died undermines confidence in every other gate/board result built on it, not just this one row).
- **5 DONE-WHEN portability/mechanics fixes** on confirmed-LIVE rows so they're honestly closeable once the real work lands: `nul-in-counted-strings-class-defect` (pointed at a file that never existed; retargeted to the one real remaining rung, `n11_array_key`, and corrected a stale `owner: hq_C` header — hq_C released this at s266, no claim file exists), `icon-n6-zero-arg-builtin-routing` (hardcoded `/home/claude_C/SCRIP`; also the Icon witness didn't parse — this project's Icon frontend requires semicolons the original omitted), `csnobol4-suite-triage` + `gimpel-suite-triage` (both hardcoded `/home/claude_C/...`), `opt0-residual-two-defects` (same hardcoded-path defect). All five: replaced the absolute other-seat path with the standard seat-relative convention (`done` already `cd`s to `$S4E` first).
- **1 content-only fix:** `const-graph-marginal-over-watermark`'s GOAL cited a stale number (146 lines/site); re-measured live at 156 — the regression is worsening, not static. No rank change, gate itself already works correctly.
- **2 new rows minted** from gaps surfaced mid-classification (row-factory rule: rank and mint, do not cure inside a discovery row):
  - **`perf-by-name-builtin-dispatch`** (rank 0) — the `perf-string-runtime` batch fork found its own diagnosed 58.6%/~34%-combined by-name-builtin-dispatch cost has zero dedicated queue row, the single highest-value un-minted bucket in the whole sweep.
  - **`sweep-zero-length-refs`** (rank 3) — retiring `bench-refs-empty-ident-call` surfaced that its own brief's second half ("sweep every corpus `.ref` for zero length") was never done; re-measured directly (not trusting the fork's figure) at **31** zero-length `.ref` files, 21 of them one shape (Icon `augop_*` augmented-assignment witnesses) that may share one root cause.

## FLAGGED, NOT ACTED ON (out of this row's scope — record + rank + mint, not cure)

- **Fleet-wide gate infrastructure bug**, found incidentally by the batch-F fork while checking `c-to-bb-unledgered-scrip-c-57` and `vstack-residue-rt-h`: any `test_gate_*.sh` that sources `lib_gate.sh` via `$(dirname "$0")/lib_gate.sh` AFTER `cd`-ing into `$REPO` breaks when invoked as `bash SCRIP/scripts/X.sh` from the seat root (the sanctioned DONE-WHEN convention) — `$0` is stale relative to the new CWD, so `lib_gate.sh: No such file or directory` fires and `gate_floor`'s coverage-floor check is silently skipped, though each gate's own substantive check still ran correctly in both cases observed. Not one of this sweep's 89 topics; not independently confirmed fleet-wide-scope, but the failure mode (a `cd` before a `dirname "$0"`-relative source) is generic to however many gates share the pattern. Recommend an HQ audit, not a row-factory row, since scope is unknown until someone greps for the pattern across all ~300 `test_gate_*`/`board_*`/`bench_*` scripts.
- **Possible duplicate/overlap, not resolved:** `kw-missing-4` (batch H, LIVE — `&LINE`/`&LASTLINE`/`&FILE`/`&LASTFILE` genuinely unimplemented, confirmed via `Error 342`) and `conform-line-lastline-crash` (batch C, LIVE — same keyword family, but the fork observed a truncated-output-plus-nonzero-exit symptom, not a clean Error 342) may be the same gap described from two different angles/sessions, or may be two genuinely different defects in the same keyword family. Neither classifying fork could resolve this without reading the other's evidence; left both rows as-is with a cross-reference note rather than merging on a guess.

## CAVEAT: THIS IS A SNAPSHOT, NOT A STANDING FACT

QUEUE.tsv grew from 105 to 155+ rows *during this single session*, visibly, from other seats/HQ minting work concurrently (including, mid-sweep, several more members of the exact "false-green gate script" family Batch A just finished classifying — `rbx-quarantine-missing-src-false-pass`, `ir-field-discipline-missing-src-ratchet-lie`, `sm-dead-missing-src-ratchet-lie`, `stage2-isolation-missing-src-false-ok`, `call2bb-stub-regime-skip-as-success` — none of these are covered by this pass). The 89-row denominator and the ~84%-live figure above describe the fleet at 2026-08-23T20:57Z specifically. **A future sweep should re-run the same cross-reference (`QUEUE.tsv` state==FREE minus `claims/*.claim`) rather than trusting this count's currency.**
