# FINDING 2026-08-01 (s22v) — SN4: the exit-class ledger, the four-glue grid, and the arg install is the next carve casualty

Session: SNOBOL4-BB s22v, Lon directive "NON-POPPING FORTH RSP ζ; allocate on ALPHA, free on OMEGA; WHACK-FREE at completion (outside the BB graph) or FENCE checkpoint (inside) or other known sync point; dynamic glue templates for one-shot and pass-through access to complete BB graphs with one entry and one exit" + grant "all your choices". Commits: SCRIP `159e7875` (+ feature artifacts `ecdcaa23`) · corpus `581b87e5` (benchmark) + `cf7f39a5` (demo) · .github docs.

## 1. The RBPPAIR "second authority" is not a pin — it is the ambient C frame

s22u's falsified RBPPAIR named a second rbp-pinning authority for EVAL/CODE chains and assumed `rt_chain_enter` established it. **Measured against the source: `rt_chain_enter` never touches rbp** (its own comment: "rbp needs no save here — it is the align-save register the chains manage themselves"). What the unguarded γ/ω whack (`mov rsp,rbp; pop rbp`) actually unwinds to on a chain graph is the **ambient -O0 C frame pointer of whichever runtime function last established rbp** — and in m3 that accidental unwind, followed by `eax; ret`, returns FROM that C function, which IS the return-to-C that `1016_eval` depends on. The falsification stands explained: suppressing the whack removed a load-bearing accident. The cure is not a guard — it is naming the licensee.

## 2. The exit-class ledger (landed, measured identical-by-set)

The shared γ/ω glue at every graph emission is now selected by HOW THE GRAPH IS ENTERED (emit.cpp shared-port site):

- **CLASS O** — outer one-shot, α-pinned by the guarded `bb_glue_framed_enter`: keep `bb_glue_outer_γ/ω` (whack = the completion sync point OUTSIDE the graph, per the directive).
- **CLASS C** — chain-entered (LBL__ pseudo-procs, EVAL/CODE fragments): keep the whack, now as the LEDGERED ambient-C-frame unwind (see §1).
- **CLASS P** — wire-entered DEFINE stub blobs: **`bb_glue_wire_γ/ω`** — snap the open pcall record (`rt_flat_ret_snap`), restore rsp/rbp(/r12 island) FROM THE RECORD (the sync point is the role-3 ADOPT; no rbp assumption, no whack), jmp home through the port's wire. γ=gw (RETURN), ω=ww (FRETURN). Retires the `exit@PLT`-inside-PROC Lon observed opening s22u AND the `exit(0)` **silent success** on a fell-through-transfer path; level-0 arrival now dies loudly (error 18).

**Discriminator = the driver's own stub verdict `g_flat_frame_floor > 0`** (all four scrip.c proc loops set it from the role-3-entry predicate exactly and only around DEFINE-stub emissions; chains/PAT$ leave 0) — the same fact the CALL2BB `_stfj` discriminator already earned through two falsifications. Probed live at HEAD before use: roman's two stubs floor=1296, chains/deep graphs floor=0 (not vacuous).

**Measured** (this harness, bare container, TIMEOUT=10, crosscheck 318, both modes, set-diffed): m3 205/112 · m4 203/113/1 · DIV 2 {170_pat_abort_kills_match, 1016_eval} — **IDENTICAL BY SET base↔new, zero fixed, zero broken.** Positive controls: roman.s stub ports provably rewired (snap/restore/jmp-wire replaces whack+exit); RETURN/FRETURN floaters byte-identical after the one-authority refactor; CLASS C ports byte-identical.

## 3. The four-glue grid (Lon's count = 4, confirmed; design of record in GOAL-SNOBOL4-BB.md + ARCH-SNOBOL4.md)

ONE dynamic linkage everywhere — the wire contract `rcx=γ, rdx=ω, jmp target` — spoken identically by C (rt_chain_enter, rt_proc_call_open/_slim) and emitted sites. **ONE-SHOT = contract + pcall record** (open pushes, role-3 adopts, exits via `bb_glue_wire_exit`). **PASS-THROUGH = the bare contract** (nothing recorded; the target's exits ride the wires into the site's own continuation labels).

(ONE) MAIN→initial graph: one-shot static — complete. (TWO) BB_DEFER→blob: pass-through — **`bb_glue_pass_wires(gid,wid)` minted; canonical defer site converted, byte-identical proven** (two-arm `.so` emission diff, rc=0). (THREE) call site→SAVE_RESTORE/CALL_FUNC: one-shot dynamic — exit side landed; enter side stays C until the role-0 emitted open (CALL2BB). (FOUR) shim→first statement: pass-through THROUGH THE REGISTRY — **must stay dynamic: SPITBOL Ch.9, CODE-fragment labels override same-name main labels at run time**; a static jmp fast-path needs a label-never-redefined license. Conversion backlog (hand-rolled trios, convert on touch): bb_call_proc_staged ×5 · bb_call_value · bb_match_capture · bb_match_release · defer L7 arm.

## 4. THE HEADLINE FOR NEXT SESSION — the DEFINE-family arg install is the same carve-kill disease as WIREREG

The whole 083–090 define family (+ roman's unbound N) fails at HEAD with args unbound. Shrunk to 083 (`DEFINE('double(s)')`, ONE formal, ZERO locals — upstream of any formals/locals question): m3 `Error 1 Illegal data type` = `s` is null at `2*s`. The transport is sound (`rt_arg_stage`→`g_call_args`→`rt_proc_call_open`→`rt_proc_call_prologue(p, g_call_args, nargs, wn)`→name-save binds pcells). **The defect is the emitted call site's OPERAND READ:** the SLIM path installs the staged arg into the formal's GVA cell by reading the caller's FORTH spine at **static `[rsp+160/168]`**, while its own fallback path (post `add rsp,32`) reads the same producer cell at **`[rsp+112/120]`** — two static spellings, disagreeing by 16B even net of the 32B carve, neither resolving through the live per-BB authority. Exactly the class THE MODEL names and WIREREG cured for the wire quad; this is its arg-install sibling, and the two-path disagreement is a built-in A/B instrument. Fix class: route the reads through `x86_frame_off`/the UCLAIM resolver — making the s22t dark-emission bisect the named owner — MONITOR + zd_plan territory, deliberately NOT started on end-of-session context (a half-landed resolver is the outcome worse than the debt). Secondary, land with it: `sno_parse_define` (lower_snobol4.c:732) folds locals into `names[]`/`nnames` and line 2345 sets `nparams = nnames` (ROMAN registers nparams=2); manual Ch.8 = all names SAVE, only formals BIND — split the counts, minding that `rt_proc_call_prologue` uses nparams as the save count too (a naive nparams=formals change would stop saving locals and break recursion).

## 5. Instrument notes and small flags

(a) This container reaps background children between tool calls — `setsid nohup … < /dev/null` is the surviving detach for the crosscheck runner AND the `.so` rebuild (any x86_asm.h/bb_templates.h touch fans out ~109 template TUs past one tool call). (b) The s22u `.so`-snapshot + `LD_LIBRARY_PATH` arm-select law extends to EMISSION diffs: the emitter lives in the `.so`, so before/after `.s` for a template refactor comes from two snapshots under one driver binary. (c) `test_gate_template_medium_invisible.sh --strict` fails at HEAD on PRE-EXISTING debt only — bb_glue_flat's four `IF(MEDIUM_*)` are s22p's outer landings (genuinely different per-medium protocols: exit-syscall vs eax/ret, no PLT in the m3 slab — not the forbidden duplicate-instruction pair, but the strict grep cannot tell) + xa_flat(8); verified identical count at pre-session `2edd3497`; zero introduced. (d) FLAGGED, untouched: `proc_<name>_res` emits `add rsp,8; pop rsp` (pop into RSP) where the LBL__ twin pops rbp — likely a defect in a separate emission arm; own bracket.
