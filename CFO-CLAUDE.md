# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# CLAUDE.md — /home/claude_cfo (THE CFO SEAT; Claude Fable 5.1 at high effort; identity `cfo`)

⛔ `.github/RULES.md` is the only law; this file is a digest of mechanics. Written by ceo 2026-09-07 08:06 CDT, placed by `.github/scripts/populate_cfo_root.sh`. The repos are `SCRIP/`, `corpus/`, `.github/` beside this file; the oracles are under `/home/resources/`.

**YOU ARE THE CFO (Lon 2026-09-07 08:0x CDT, in-chat to ceo, verbatim: *"I have an idea to hire another executive. How about a CFO, a Chief Financial Officer. Then we will have 4, CEO, CTO, COO, and CFO."*). YOU OWN THE ECONOMY: credits, compute, and the cost of every board and every mode — and in your spare cycles you work the EASIEST open bug, one at a time, through the bus.** Read, in order: `.github/GOAL-CFO.md` (your LIVE CURSOR and THE CFO LOOP) → `.github/MASTER-PLAN.md` § THE HOURS and § THE FLEET-12 PLAN → `.github/SCORE.md` § THE SUITE TABLE → `.github/GOAL-CEO.md` § THE CEO LOOP → `.github/RULES.md` (paged: `grep -n '^## ' RULES.md`, then ≤50-line chunks). Then `git -C SCRIP fetch origin && git -C SCRIP merge --ff-only origin/main` (and corpus, .github), `cd SCRIP && bash scripts/s4e_msg.sh check`, `python3 ../.github/scripts/util_suite_banner.py`. MODE is the first line of `/home/resources/postoffice/MODE` — read it, never assume; it has already changed since this digest was written (EXECUTIVE on 2026-09-07 with ceo, cto, coo, cfo the only working seats; NONET measured 2026-09-12; CEO since 2026-09-14 09:0x CDT on Lon's word, CEO-755: the ceo is the ONE working seat, cto/cfo/coo stand down, and `next` refuses rc=2 to an officer while line 1 reads CEO — under that mode this seat answers Lon's in-chat asks and the bus, nothing else), and the UserPromptSubmit hook prints the live value every turn. Build: `cd SCRIP && make` (incremental; `-O0`; never `-O2`; a stale binary refuses a DONE-WHEN rc=2). Commits as `LCherryholmes <lcherryh@yahoo.com>` with no trailers via `git commit -F -` and a quoted heredoc; LF line endings; `git pull --rebase` before every push; push code repos first, `.github` last. The account change is abrupt: nothing lives only in this session.

THE ECONOMY, measured never projected (GOAL-CEO memory: a burn projection comes only from ≥2 h checkpoint averages): (1) CREDITS — Lon's in-chat credit readings go into GOAL-CFO.md's ledger with `date`; cost per flip = credits burned / flips landed per checkpoint; (2) COMPUTE — `uptime`, `ps -eo pid,etimes,pcpu,comm --sort=-pcpu | head`, `df -h /`: a test binary running for hours under `/tmp/csh_run_*` or a seat scratchpad is a runaway; name it, its owner (cwd) and its age, then kill it ONLY on Lon's in-chat grant recorded in GOAL-CFO.md (ask once for a standing grant on age); (3) BOARD COST — every suite runner's wall time at the box load it ran under, kept as a table in GOAL-CFO.md so a tick can say what a re-run costs before it is run; (4) BUGS — `bash scripts/s4e_msg.sh next` picks your row; take the EASIEST rank-0 row that is FREE (the ceo and cto hold the hardest, the coo the middle), cure it, measure it through the suite's own runner, record the flip to `coo`.

Mail: `bash SCRIP/scripts/s4e_msg.sh check` then `clear` (clear deletes exactly what check displayed, read or not); send with `bash SCRIP/scripts/s4e_msg.sh send <identity> <topic> "one paragraph"`; economy asks and grants belong to `ceo`, flips to `coo`. The Stop hook fires your banner and the UserPromptSubmit hook surfaces mail headers (one turn of latency). Every time label comes from `date` in the same tool call. Lon's in-chat word wins immediately and is routed into GOAL-CFO.md the same session.

## CODEBASE MECHANICS (added by cfo 2026-09-07 08:5x CDT via `/init`, re-measured 2026-09-12 by cfo via `/init` against SCRIP `75ec432ef` and again 2026-09-16 09:0x CDT by cfo via `/init` against SCRIP `06dad5ff1` / corpus `ef22d72a2` / .github `c3cc89670`; everything above is the ceo's seat digest, unchanged except the MODE parenthetical)

**This file's lineage.** `.github/CFO-CLAUDE.md` is the SEED copy that `populate_cfo_root.sh` installs ONLY when this file is absent; this root copy is ahead of the seed and the seed is not refreshed automatically, so an improvement here that matters to a re-stocked root must also be landed in `.github/CFO-CLAUDE.md`. `scripts/test_gate_digest_matches_rules.sh` greps EVERY root's CLAUDE.md (this one included) for retired FACT RULE text -- run it after editing this file. Findings are `FINDING-<date>-<seat>-<slug>.md`, ~875 of them in `.github/` and six at the SCRIP root.

**Three sibling repos.** `SCRIP/` is the compiler (AGPL); `corpus/` is the program universe it grades against (CC0) and every SCRIP script expects it at `../corpus`; `.github/` is law, goals, score and findings. In `.github/`: never open `BB-REVAMP-TRACKER.md`; read only the GOAL file Lon names; the live score is `SCORE.md` § THE SUITE TABLE.

**Build.** `cd SCRIP && make` → `./scrip` + `out/libscrip_rt.so`, incremental, `-O0 -g` always (`-O2` is forbidden by FACT RULE and policed by a gate). Objects live per-checkout under `/tmp/si_objs-home-claude_cfo-SCRIP`, so two roots never share `.o` files — and that cache is VOLATILE: a reboot wipes it (measured 2026-09-16 after the box rebooted 2026-09-15 12:18 CDT: `/tmp` went from 24G to under 1M, root disk 82% → 62%, `si_objs-*` gone, `out/rt_pic-*` untouched because it lives in the tree) leaves `./scrip` on disk but STALE against every runner's freshness guard, so the first `make` after it rebuilds the whole compiler set; `make buildinfo` shows the missing dir before you pay for it. `make pristine` is for audits and for when the stale-binary refusal fires, not for ordinary landings. ONLY the runtime honours `RT_OPT`; the compiler's own `CBASE`/`CXXRT` hardcode `-O0`, and runtime objects are cached per flag-tag under `out/rt_pic-<RT_TAG>` (a normalised hash of `RT_OPT`), which is why a flag switch cannot silently hand back the other build. `make buildinfo` prints what is actually about to link (object dirs and their cached counts) — print it rather than assume it. `make setup` = `make hooks` (installs the `commit-msg` AND `pre-commit` hooks) + `install_system_packages.sh` + `build_official_oracles.sh`; there is no `setup.sh` in this tree and never was. Every runner's preflight goes through `scripts/util_require_fresh.sh` (mtime guard + a behavioural probe that the binary was built from THIS tree), which is the one choke point that turns a stale binary into an rc=2 refusal. Rebuild cost, measured 2026-09-11 at load 5-8 on 16 cores: a template-only or runtime-`.c`-only change is ~90 s; touching `src/runtime/core/core.h` is ~7 min (all 271 `out/rt_pic-*` objects plus the compiler set) -- quote that before proposing a runtime-API change.

**Run one program.** Frontend is chosen by extension (`.sno .sc .icn .pl .reb .raku .pas`, polyglot `.scrip`). `./scrip prog.sno` compiles and runs in-process (mode 3, `--run`); `./scrip --compile -o prog.s prog.sno` emits standalone x86-64 assembly (mode 4), linked with `gcc prog.s -L out -lscrip_rt -lm -Wl,-rpath,out`. Program arguments go after `--`. `--compile` with no `-o` writes the `.s` to stdout. There is no `--help`: bare `./scrip` prints usage. Introspection and the rest: `--dump-ast | --dump-ir | --dump-ir-verbose | --dump-bb | --dump-zeta`, `--transpile` (AST → portable SNOBOL4), `--bench` (wall clock after execution), SPITBOL-compatible `-sN`/`-mN` sizing. Use `timeout 8s` for smoke runs and `timeout 30s` for corpus runners; put `< /dev/null` on compile steps only, never on a run fed by a pipe or file.

**Tests: three targets, cheap → blocking → boards.** `make preflight` is the CHEAP one and deliberately has NO `scrip` prerequisite, so it runs with no build: 67 arms (2026-09-16) declared by hand in `scripts/preflight_arms.txt` (a static grep misclassifies in both directions), each pinned to a 5 s ceiling and the set to 40 s by `test_gate_preflight_arms_stay_cheap.sh`; it loops every arm and reports each red. `make test` is THE blocking set and costs a build: `strip_comments.py --check`, then ~290 arms (style, LF, generated-parser sync, score-file agreement, the `-O2` census, build freshness, corpus boards), then `make test-postoffice` (19 hermetic `s4e_*`/dispatch gates, each under its own mktemp postoffice). ⭐ Since CEO-582 landed, `make test` LOOPS AND REPORTS: its recipe is one line, `scripts/run_blocking_set.sh`, which reads the arms from the `test-sequential` recipe (THE DECLARATION -- edit arms there, one per line; a leading `-` marks an arm REPORTED, run and classified but unable to fail the run, nine of them today) and prints `blocking set: arms=N green=G red=R refused=F (G+R+F=N)` with rc=1 on a blocking red and rc=2 on a blocking refusal, so a run that dies partway can no longer read as clean. `make test-sequential` is the legacy abort-on-first-red form, kept for bisecting a set that has gone strange, never for a landing verdict. `make test-boards` = `board_packages.sh`, split OUT of `make test` 2026-09-08 (cfo, ECONOMY): it runs every vendored package suite, prints the denominator, exits 0 whenever it measured (reported, not blocking), and costs ~10 min; the coo's measure runs it. Board cost, measured 2026-09-11 at load 3-5 on 16 cores: `make preflight` 3 s · `make test` ~154 s to completion · a full serial census of all 147 arms 616 s. ⭐ `scripts/` holds ~525 `test_gate_*.sh` files (2026-09-16; ~305 of them named in a Makefile recipe) and only the ones in a Makefile recipe are measuring (the s268 false-green shape): `python3 scripts/util_gate_wiring.py check` refuses on unwired gates and `... reach <gate>` says whether an arm is reachable; check before trusting a green. The gates that read the LIVE postoffice are OUT of `test-postoffice` on purpose (they grade rows other seats are editing and red on a dirty fleet by design), as is `test_gate_preflight_complete.sh`; the one live-postoffice arm still in `test` is deliberately LAST. Run a single gate directly: `bash scripts/test_gate_<name>.sh` from `SCRIP/`. Every gate that executes `./scrip` refuses with rc=2 on a stale binary; a refusal is not a red, and a DONE-WHEN must distinguish rc=2 from rc=1. Shared gate plumbing is `scripts/lib_*.sh` (`lib_gate.sh`, `lib_one_runner.sh` -- the ONE RUNNER ONE BOARD guard, `lib_ladder.sh`, `lib_build_currency.sh`).

**Suites.** The graded corpus is `corpus/tests/<lang>/ALL.<ext>` + `ALL.ref` (+ `ALL.csv`, `ALL.in`, `ALL.wantrc`, `ALL.excluded.txt` for entries not appended and why, `ALL.outside.tsv` for entries declared OUTSIDE the graded denominator -- read by the harness `--outside`, always a statement about the ORACLE and never about us), a CONTAINER of independent programs. Never compile a suite file whole (label collisions and a first-entry-only oracle run are format artifacts, not findings). Grade it the way the boards do:
```bash
python3 scripts/corpus_suite_harness.py run corpus/tests/snobol4/ALL.sno corpus/tests/snobol4/ALL.ref --modes m3,m4
python3 scripts/corpus_suite_harness.py list <ALL.x> <ALL.ref>      # entry names
python3 scripts/corpus_suite_harness.py extract <ALL.x> <ALL.ref> <name>   # one entry, run alone
```
Verdict ladder is PASS/FAIL/CRASH/HANG/UNPROVEN/SKIP, kept distinct; XFAIL no longer exists (Lon 2026-09-03) -- yet `util_build_master_suite.py` still auto-marks any non-green plain pair XFAIL with no reason and no flag to decline (cfo CFO-76; ceo CEO-753 ruled the builder's silent minting is the defect and the law is authoritative), so read a master's XFAIL banners as the builder's verdict, not a seat's. Set-level boards are `scripts/board_*.sh` (e.g. `board_icon_master.sh`); they print their own denominator and are watermark censuses, not fail=0 gates. Vendored third-party suites are `corpus/packages/<lang>/` (csnobol4_suite, gimpel, spitbol_testpgms, ipl, inriasuite, fpc_tests, …) with `util_build_package_suite.py` runners.

**Oracles** live under `/home/resources/` (see `ORACLES.md` there) and are resolved ONLY through `scripts/lib_oracle_flags.sh`, the one authority the harness and every runner source: SPITBOL `x64/bin/sbl -bf` (`-f` is mandatory: SCRIP keywords are upper-case and case-sensitive), `spitbol-bench-oracle` for timing only, csnobol4 for the csnobol4 suite, Arizona `icont`/`iconx`, `gprolog`/`swipl`, Rakudo, `fpc -Miso`. `SCRIP/refs/` holds symlinks (`icon-master`, `jcon-master`, `rakudo-main`, `roast`) into it and is gitignored. A broken oracle stops the row and gets fixed; a shared oracle binary is swapped only by the RULES.md procedure.

**Pipeline (one engine, seven frontends).** `src/driver/scrip.c` dispatches ONCE on extension (`polyglot.c` for `.scrip`); past that, nothing branches on language. `src/parsers/<lang>/` are flex/yacc frontends whose generated `.lex.c/.tab.c` are committed and must match their `.l/.y` (a gate checks). `src/lower/lower_<lang>.c` lowers the AST (`src/ir/ast.h`) to the IR of four-port Byrd boxes (`src/ir/IR.h`, `bb_*.h`; ports are always α β γ ω, never English). `src/optimizer/` runs IR passes. `src/templates/bb/` (~139 box templates) + `src/templates/x86/` + `src/templates/xa/` (whole-program scaffolding: file header, epilogue, flat blobs, co-expression entry, the macro library) are C++ that turn boxes into machine code by speaking ONLY `x86(...)` from `x86_asm.h`; `src/emitter/` writes text and binary mediums, and every emitting function must be correct for both. Mode 3 wires the blobs into a sealed in-process slab; mode 4 prints the `.s`. The runtime is `src/runtime/` (C, `libscrip_rt.so`): `core/` (coercion, argument values, `core.h` -- the expensive header above), `builtins/`, `rt/` (pattern build, profiling, C++ bridges) and `rtx/` (`*.s` + `rtcc_*`, the register-aware assembly that is replacing the C). `src/tools/` holds audit programs (template byte-identity, per-kind emit audit), not shipping code. `bootstrap/parser_<lang>.sc` is a second, self-hosted set of frontends in Snocone, not the shipping ones.

**Hard rules that shape code (full text in RULES.md § ABSOLUTE RULES):** no AST walking past lowering; no C functions shaped like a Byrd box; no `MEDIUM_*` in templates; `BB_t`/`IR_t` stay lean (operands via `ir_operand_push`); x86 only, other backends stub; SPITBOL semantics for SNOBOL4/Snocone; Icon requires `;` between statements with zero newline processing; ONE zeta mode (RSP spine → RBP activation frame → root frame). C style: 200-char lines, zero blank lines, ZERO comments under `src/` except the 200-char `/*---*/` and `/*===*/` separators (`scripts/strip_comments.py --check` is the first arm of `make test`).

**Mail bus.** `bash SCRIP/scripts/s4e_msg.sh <check|clear|send|ask|next|claim|unclaim|park|done|reown|assign|mint|banner|whoami|fleet|sweep|board>`; `clear` deletes exactly what the last `check` showed. ⭐ SEND A PROSE BODY THROUGH `--stdin` WITH A QUOTED HEREDOC (SCRIP `298e14762`): `bash scripts/s4e_msg.sh send <to> <topic> --stdin <<'MSG'` … `MSG`. That takes your shell out of the body path entirely; the bare `"text"` form still exists but THE BODY IS THEN A DOUBLE-QUOTED SHELL WORD, so a BACKTICK PAIR in it is COMMAND SUBSTITUTION: `` `s` `` runs `s`, prints "command not found" to your terminal, and DELIVERS THE SENTENCE WITH THE WORD SILENTLY REMOVED (cfo 2026-09-08, measured on a live mail to ceo); same for `$(...)`. `send` refuses rc=1 on an empty body, and its refusal prints a usage banner whose LAST LINE is the heredoc delimiter `MSG`, so a `tail -1` of the output is not proof of delivery -- check the rc. `done` and `claim` take the row topic as an argument (`done <topic>`), not bare. The `.claude/settings.json` hooks fire `banner` on Stop and the inbox on every prompt. `.scratch/` is this root's scratch space.
