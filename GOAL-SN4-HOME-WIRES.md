# GOAL-SN4-HOME-WIRES — r10/r11 wires, two glue kinds, shim deletion (HOME seat; master = GOAL-SN4-HOME.md)

## ⛔ TOOLING FIRST — `bash /home/claude/SCRIP/scripts/install_system_packages.sh` BEFORE ANY RUNG
One authority, idempotent, prints whether `gdb` is live. **`gdb` is MANDATORY** — MONITOR-FIRST step (2) *is* a gdb breakpoint with a spin/ignore counter. ⛔ **Never hand-run `apt-get install gdb`** — it pulls Recommends `libc-dbg` against a stale index and 404s; that trap cost RBP-EARN seven sessions (s33–s39). Runtime `rt_*` symbols live in `out/libscrip_rt.so` ⇒ `set breakpoint pending on`; "Function not defined" is dynamic linking, not a broken gdb. Any genuinely missing tool ⇒ ADD IT TO THAT SCRIPT in the same push. Any prior cursor claiming gdb is unavailable is VOID.

**CHARTER:** rΓ=r10 · rΩ=r11 product-wide (Lon s12: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11"*), exactly TWO glue kinds (ONE-SHOT · PASS-THRU; FRAMED IS NOT A GLUE KIND — s29), RBP never written by glue. **Mechanism authority = LADDER WREG + LADDER PT inside `GOAL-RBP-EARN.md` (absorbed by reference; corrections here supersede).** RTCC's wire half (veneer preservation) is owned HERE per the s14 arbitration: safe config = RTCC-ON **and** wire capture/restore, neither alone.

## CLOSED (ledger only — full history in the cited FINDINGs; do not reopen)
- **W-0 · CLAIM SWEEP — CLOSED s36c.** Lon's W-0b rulings executed at `wreg_claim_whitelist.txt` + `test_gate_wreg_claim_binary.sh` (gdb-dump-at-bb_seal + objdump, 19 programs / 27 slabs / 275 shape checks, all matching the two licensed mechanisms; `--quick` smoke mode exists). "Product-wide" ruled: unreachability is NOT a licensing excuse — the 30 SN4-unreachable RTX occurrences became debt, tracked at W-8. Allowlist extension = FINDING-required event.
- **W-1 · ZCTX SCRATCH ERADICATION — DONE s33 (`26c84e72`).** `g_zctx[66]` was dead exported BSS, deleted. **HOME GATE line 4 satisfied as a MEASURED side effect — `g_blob_ctx` and `rt_blob_ctx_ptr` both grep to 0.** History: `FINDING-2026-08-12b`.
- **W-2 · PUSH/POP GUARD UNIFICATION — CLOSED this session, disposition (a).** Evidence from two sessions falsified both stated premises (structurally-unreachable asymmetry; D12/D13 belong to the ARBNO dispatcher, not r10/r11) with nothing left to re-derive — closing as FALSIFIED reflects what was actually found, rather than parking a disproven hypothesis under "hygiene." The one real, smaller thing inside it — raw string/byte literals in `emit.cpp`'s push/reload pair, a genuine TEMPLATE-ONLY violation — is split out below as **W-9**, correctly scoped and NOT landed, because the site is inside the master's COLLISION PIN.

## RUNGS
- [ ] **W-3 · WREG MECHANISM — ⛔ LIVE AND UNCONTROLLED (the "DORMANT/killswitched" description was FALSE; corrected s39c, `FINDING-2026-08-12r`).** Site glue `lea r10,[rip+site_γ]` · `lea r11,[rip+site_ω]` · `jmp rax`; exits `jmp r10`/`jmp r11`. `bb_glue_pass_wires_blob()` (`bb_glue_flat.cpp:162`) has ONE live, UNCONDITIONAL caller — `bb_match_defer.cpp:83` — so r10/r11 glue is the only reachable spelling at the DEFER blob-entry site. **`SCRIP_WREG` does not exist in `src/`** (grep + empirical `SCRIP_WREG=0` A/B byte-identical); three comments claim it (`bb_glue_flat.cpp:158`, `bb_match_defer.cpp:83`, `bb_templates.h:68`) — stale safety documentation, itself a hazard.
  - **⭐ CRASH ROOT-CAUSED, NOT FIXED (`FINDING-2026-08-12s`, this session).** Witness `corpus/probe/bb/witness_wreg_s39/W01_stored_pattern_defer_len_capture.sno` (oracle `=S`, `--run` SIGSEGV 139). Cause: the DEFER blob's own `IR_MATCH_ASSIGN_SAVE` result-cell write (`ZRESD(0)` → `[rsp+64]`) lands on the SAME address as the outer graph's `zls2_mark` slot (`HKM()` = `[rbp-56]`) — confirmed by address arithmetic at the fault (`$rsp+0x40 == $rbp-56`), not inference. `bb_match_end`'s `x86_zls2_release_to_call` then does `mov rsp,[rbp-56]` on the clobbered value and the next `push` faults. **RULED OUT with evidence (do not re-check):** r10/r11 register clobber (registers never move after their `lea`); `stfh()` split-predicate drift between `bb_match_begin.cpp`/`bb_match_end.cpp` (macro text byte-identical AND runtime values identical at all 16 BEGIN / 5 END sites). **Likely NOT WREG-specific** — the collision is relative stack depth at blob entry, not register choice — but unproven (no rcx/rdx twin at this site to A/B). Fix argued to belong at W-4 (fold `zls2_mark` into the arena, depth-immune) rather than patched in place — see Q7.
- [ ] **W-4 · ARENA WIRE-PAIR SLOT (+16B) — THIS SEAT OWNS THE LAYOUT; RBP/EARN-5 CONSUMES (one authority).** Blob-interior pending records capture {r10,r11} at push, restore at β, or it is `g_blob_ctx`'s single-cell defect in register clothing (the LAW). **Now blocks FOUR real fixes: D12/D13, X01, the W-3 crash, and (if folded) `zls2_mark`.** ⛔ **THREE carry shapes the layout must cover — do NOT design against "crosses a call" alone:**
  1. **Carve/copy carry** — `rtx_str.S:165-207` (`str_concat_d`): r10=buf, r11=len live from just after ONE `rt_str_alloc` call through to `ret`, ~42 lines; recurs in `rtx_match.S`'s `rt_cap_open`/`rt_match_replace`. A save-at-every-call-boundary scheme clobbers this.
  2. **Explicit-frame preserver** — `rtx_plcall.S` cold-growth arm: r10d spilled to `[rbp-24]` and restored around `call rt_pcall_grow`, via a frame slot not push/pop. A push/pop-shaped scanner misses it.
  3. **⭐ CANDIDATE (`FINDING-2026-08-12s`, NOT YET RULED IN-SCOPE — Q7):** `zls2_mark`, today a fixed `[rbp-56]` cell (`HKM()`), the W-3 crash's actual collision target. An arena slot makes it depth-immune the same way the register wires already are.
- [ ] **W-5 · ⛔ THE FLIP — REQUIRES EARN-1 + EARN-3 LANDED (EARN-10 ordering).** PROC-shim deletion (PT-1..3); CLASS-D exit ceremony dies with it. The old WREG residual (19 SEGV + 7 HANG) was MISSING FRAMES, not glue defects — EXPECTED cured by EARN; measure BY SET, never assume. ⛔ **PREDICATE:** `grep -rn "frame_need_of" /home/claude/SCRIP/src/` non-empty AND `UNBLOCKS: WIRES W-5` on origin. Currently FALSE — `frame_need_of` does not exist under ANY spelling; it is a FORWARD REFERENCE the RBP seat must create. This seat cannot unblock it; check `GOAL-RBP-EARN.md`'s cursor rather than re-grepping hopefully each session, and skip DOWN (never wait).
  - **⛔ NAMED PREDICTIONS FOR THE FLIP (recorded deliberately — do NOT fix before W-5 opens):** (1) **Scan-family asymmetry** — 26 `push r10` / 0 `push r11` across 10 `bb_scan_*.cpp`. Every one protects γ and abandons ω the moment r11 becomes a wire. Witness at W-5. (2) **Encoder landmine** — `[r10]` in a template resolves via `XK_R10MIR` → `x86_store_cursor_mirror()` = `mov [r10],r14d`. **Any W-3/W-5 template touching `[r10]` must spell it `[r10 + 0]`.**
  - `xa_flat.cpp:238–307` IS the PROC-shim — do NOT sweep it under W-0/W-8; it dies here.
- [ ] **W-6 · RTCC RE-ENTRANT PRESERVATION + DEFAULT-ON REVALIDATION.** Leaf half PROVEN SAFE (s35: 172 veneered, 0 bare match-time) — **scope is re-entrant ONLY.** Then RTCC default-ON must hold the P0 floors with NO `SCRIP_RTCC=0` escape (kills the m4-130 class). Belt-and-suspenders: `-Wl,-z,now` for the r11 lazy-binding clobber. Witnesses: probes `140`/`141`.
- [ ] **W-7 · ARBNO DISPATCHER SOUNDNESS — ROUTED HERE (Lon s36: "You own the dispatcher bug"); ROUTING RE-ASKED, see Q1.** Witnesses D12 (SEGV) · D13 (hang) · X01 (related, different sub-class). Root cause + blast radius: `FINDING-2026-08-12k`.
  - **INTERIM GUARD LANDED s38 (`fc8b96b8b`) — NOT A FIX.** `op_arbno_body_defer_unsafe` (emit.h struct end + emit.cpp companion scan beside `op_arbno_body_k0` + per-node reset) ⇒ `bb_match_arbno()` declines with `x86_bomb` instead of the unsound plain-frameless fallback. **D12 139→134, D13 124→134.** Floor-neutral, A/B-proven against a stashed baseline (not assumed): probe/bb identical `159 pass · 1 xfail · 5 REGRESSION {D12,D13,H31,X01,X10}`; crosscheck/patterns identical `PASS=77 FAIL=45`, byte-identical failing-NAME set, only exit codes moved. **Converts silent corruption into loud refusal; flips ZERO witnesses green.** Real fix = anchor-relative ARBNO cell = W-4's layout, which this seat cannot self-serve.
  - **⛔ THE BOMB ARM MUST DEF ITS PAIR LABELS.** First version left 3–8 unresolved forward refs: the drive loop registers `β`(`PAIR(1)`, ~emit.cpp:2412) and `na_s`/`na_f`(`PAIR(2)`/`PAIR(3)`, `flat_drive_match_alt` ~:1201) UNCONDITIONALLY before the template runs. Fixed by `x86_beta()` + `def PAIR(2)` + `def PAIR(3)` after the `ud2` (dead but resolved). **Generalizes to every n-ary template.**
  - **⛔ FIX CONSTRAINT (measured):** the 16-passer set {D09 D10 D11 G19 G20 H21 H24 H25 N12 N17 X02 X03 X04 X05 X06 X11} rides the same plain-frameless path with `k0=0` and must stay green BY SET. **A blanket DEFER exclusion breaks all 16.**
  - **DISCRIMINATOR (s36b, gdb at `lower_snobol4.c:1324`):** D12 builds 3× `DEFER(sval=LIST, pat_static=0)`; passer D09 builds `DEFER(sval=P, pat_static=1)`. The fix keys on the DEFER node's own `pat_static` — no extra indirection.
  - **⛔ X01: BOTH blanket-widening routes FALSIFIED (s39a/b, `FINDING-2026-08-12p`/`q`).** `!op_arbno_body_k0` breaks 10 of 16 passers; `op_body_has_arbno` breaks the same 10. No staged field separates X01 from its passing nested-ARBNO siblings. Best-supported discriminator (oracle-verified, 9 witnesses, NOT gdb-confirmed, NOT a staged field): the outer arm needs a **character-consuming co-member** beside the nested ARBNO — `ARBNO(ARBNO(LEN(1)) 'x')` passes, bare `ARBNO(ARBNO(LEN(1)))`/null-lit/`LEN(0)` fail. Mechanism untraced (MON-CAP blocks sync-step for the wrong-answer-rc-0 class). Witnesses `corpus/probe/bb/probes_x01_witness_s39/`. **Do not retry a blanket guard without re-reading the finding.**
  - Reproduce: `SCRIP_ARBNO_DIAG=1 ./scrip --run <probe>` (arm verdict + k0) · gdb `break lower_snobol4.c:1324` + printf per node · `diff` vs `.ref` (never shell `[ ]` on multi-line output).
- [ ] **W-9 · RAW-BYTE PUSH/POP EMISSION IN `emit.cpp` — TEMPLATE-ONLY VIOLATION, ⛔ BLOCKED, DO NOT LAND BEFORE P3/EARN-11.** Split out of closed W-2 (this session); confirmed against CURRENT code, not the stale line numbers W-2 carried (this file drifts under concurrent RBP-EARN commits — verify against content, not prose, per RULES.md). Both halves of the CLASS D resume-record protocol bypass `x86()` entirely: **push** (`_blob_wire` guard) — `sub rsp,8; push r11; push r10; lea rax,[rip+lbl_res]; push rax; jmp r10`, hand-written via `emit_text_n` (TEXT) / `ef_b4·ef_b2·ef_b3·ef_b1` (BINARY) at emit.cpp's `_blob_wire` block (search `PASS-THRU SUSPENSION RECORD`, not a line number). **reload** (`flat_pat` guard at the `_res` landing) — `mov r10,[rsp+8]; mov r11,[rsp+16]; add rsp,32`, same `emit_text_n`/`ef_bN` pattern (search `W-MAP(3) res landing`). Neither uses a name RULES.md's current gate script greps for (`ef_b1..4`/`emit_text_n` aren't in the forbidden-name list) — a real gap between the LAW and the enforced LETTER, which is exactly why `--strict` reads green over a violation `git grep -n 'ef_b[0-9](' src/emitter/emit.cpp` finds directly. ⛔ **DO NOT IMPLEMENT THIS RUNG BEFORE P3/EARN-11:** this exact code is the push/pop guard pair GOAL-SN4-HOME.md's COLLISION PINS section names by address ("WIRES must NOT cut those arms before P3/EARN-11 — the s12 'highest collision surface' note is now an ORDERING LAW"); RBP-EARN's EARN-1/3/4/11 rungs establish per-activation framing in this same function and are mid-flight (EARN-1 SLICE 1 landed dormant, EARN-2 landed, EARN-3 declined pending EARN-4/7 — verified via `git log` this session, SCRIP `5547de99`). Landing a template-ify refactor here now risks exactly the collision the pin exists to prevent. Acceptance whenever it does open: both blocks re-expressed as `x86()` calls (adding TEXT+BINARY encoders to `x86_asm.h` if missing — sub rsp imm8, push r64, lea reg/[rip+label], jmp r64, mov r64/[rsp+disp8], add rsp imm8 are all standard shapes likely already covered), byte-identical objdump before/after, `test_gate_template_medium_invisible.sh --strict` green, and the two `ef_bN`-name gaps reported for the gate script itself (separate small fix, harmless either order).
- [ ] **W-8 · THE 30 SN4-UNREACHABLE RTX OCCURRENCES — PRODUCT-WIDE DEBT (minted s36c from Lon's W-0b(a) ruling).** Census + per-occurrence classification done (`FINDING-2026-08-12h`, the 30 formerly-"excused" rows — now queue). Work = reassign or veneer-cover each, per the same idiom families as the 193. Not urgent for SN4 floors (unreachable by definition) but blocks the day Icon/Prolog adopt wires; sequence after W-7/W-6 unless an Icon/Prolog wire ladder opens first, in which case it jumps.

## GATES (every rung)
Claim gate `--strict` green · probe + crosscheck BY SET vs P0 floors both modes, RTCC ON and OFF until W-6 seals · killswitch md5 discipline · FINDING + cursor move. Note: `--strict` overall is red only on W-3's glue emitters lacking their own gate entry — the emitter EXISTS and is LIVE (s39c), so this is "not yet gated," never "not yet written."

**m3 BY-SET FLOOR:** `corpus/probe/bb/run_suite.sh` — ⛔ **that path, NOT `SCRIP/scripts/`; the master's instrument map is wrong.** **160 pass · 1 xfail · 5 REGRESSION {D12, D13, H31, X01, X10}** — NOT baselined (`XFAIL.run` = `fence_probe` only). Every seat will see these 5. **Hold BY SET, never by count.** ⚠ The pass COUNT has drifted under concurrency (157 @s36 → 159 @s38 → 160 @s39d) purely from other seats adding unrelated probes — W-7's guard note below cites `159` because that was its own A/B pair, and the A/B being IDENTICAL on both sides is what made it floor-neutral. **This is exactly why the invariant is the SET, and why a transcribed count is never a gate.**

**m4 liveness is NOT assumable in either direction** — re-run the master's 30-second check (`MODE=compile bash corpus/probe/bb/run_suite.sh X12`; **at least one probe must PASS** — non-empty output is NOT liveness, per BOARD s33 / `FINDING-2026-08-12c`). BOARD's B-1 landed the r9 seed and reported m4 alive; that postdates this file's older m4-is-dark prose, which is why the prose is gone rather than carried.

**INSTRUMENT RULES EARNED HERE (offered for RULES.md, adoption unconfirmed):** (1) an instrument reports a class only after ONE member has been confirmed BY HAND — a scanner table is a claim about text, verify its units before trusting a zero. (2) Before hand-rolling a census of anything that already has a named gate script, RUN THE GATE SCRIPT FIRST — a hand grep disagreeing with a trusted number is evidence to go find the real instrument, not evidence the number is wrong (measured: a hand census "corrected" s33's tally and was itself wrong three ways — no comment-stripping, missed the majority `[r10 + N]` form, no sub-register spellings).

## ⛔⭐ OPEN QUESTIONS FOR LON (one list; renumbered and merged this session)
1. **ROUTING — ARBNO (W-7/X01) stays here, or moves?** Sixth session defaulting to ARBNO by inertia. Evidence for moving it: in under an hour on this seat's OWN charter (W-3), s39c found a dead killswitch + a live crash, and this session root-caused that crash. **Proposal:** route X01 + ARBNO dispatcher soundness to `GOAL-SN4-ZETA-MECH` or `GOAL-SNOBOL4-BB` (the template owner); point this seat at W-3/W-4/W-6. Either answer is fine — silence is what keeps costing sessions.
2. **W-4 promotion / dedicated runway.** It now blocks FOUR real fixes (D12/D13, X01, W-3 crash, `zls2_mark` fold). Getting larger, not smaller.
3. **X01 discriminator.** Blanket routes falsified (see W-7). Either (a) widen `op_arbno_body_defer_unsafe` with a narrow new "body-span is one nested ARBNO with no character-consuming sibling" scan (needs n>1 before landing), or (b) defer X01 to whoever owns the W-4/W-7 real fix — likely the same anchor-relative slot.
4. **W-2 disposition — RESOLVED this session, (a).** Closed as FALSIFIED (ledger above); the real content split out as W-9, explicitly blocked behind the COLLISION PIN rather than landed. No longer open.
4b. **NEW — W-4/W-7's "anchor-relative ARBNO cell" is EARN-4's design, not an open question (BOARD `FINDING-2026-08-13-...SAME-MECHANISM-DIFFERENT-MOTIVE.md`, this seat's follow-up `FINDING-2026-08-13b-...` adds the manual grounding it flagged as unread — Ch.9 pp.121-123 backs "one frame per recursive activation" as the language's own semantic model, not just an offset fix).** Still open: which seat implements it. Recommend RBP-EARN (EARN-4 is its rung, both findings agree) rather than WIRES opening it independently — but say so in-chat per both findings' request; this seat is not claiming it.
5. **MON-CAP / `csnobol4` build.** Has blocked four investigations (s38, s39a, s39b; s39c/this session excepted — plain gdb sufficed for the crash class). `TOKEN_SEE_LON` is the sole blocker. Recommend supplying it at session start alongside the push credential.
6. **`SCRIP_WREG` stale documentation.** Crash half is now answered (root-caused). Remaining: either implement the env-gate for real, or delete the three comments that claim it exists. Leaving them is a live hazard — a future session will reasonably try `SCRIP_WREG=0` as a bisect step and be silently ignored.
7. **W-3 fix scope — in place, or fold into W-4?** This finding argues the durable fix is moving `zls2_mark` off its fixed `[rbp-56]` home into the W-4 arena (depth-immune by construction), not widening blob-entry depth accounting in place (which risks a second collision with the `SCRIP_OS_CAP`/anchored-capture carve near the same zone). Affects two rungs' scope.
8. **⛔⭐ UNRESOLVED ESCALATION, CARRIED FORWARD — `dc_sib_bt.sno` is SILENTLY WRONG at rc=0.** m3 prints `=S / X= / Y=C / Z=CD`; oracle `.ref` is `=S / X=AB / Y=C / Z=ABCD`. **Exit 0, no crash, no stderr.** Found incidentally, deliberately not chased (MONITOR-FIRST says the monitor owns it). Three things this seat cannot decide: **(i)** is it a member of the 5-REGRESSION set {D12,D13,H31,X01,X10} or an UNCOUNTED SIXTH? (the set is named by probe ID, `dc_*` are named witnesses — if uncounted, the floor number is WRONG, not merely imprecise); **(ii)** who owns it? (output shape smells like RBP/EARN CLASS-D pending-cell restore, but that is exactly the guess MONITOR-FIRST forbids acting on); **(iii)** does MON-CAP exist yet? HOME GATE line 5 lists it as owed, and **a by-set floor that grades pass/fail cannot see this defect class at all** — every seat's "floor held BY SET" claim, including this file's, is only as honest as the instrument's ability to notice a wrong-but-exit-0 answer. Re-raised here because a prune is exactly where a loud anomaly quietly disappears.
9. **Silent-corruption sweep — after a fix, not before.** This witness happens to land exactly on the crashing `zls2_mark` cell. Other DEFER/capture body shapes could collide with a different outer-graph slot (anchored-capture δ region, PATCTX quartet) and produce a silently WRONG ANSWER instead of a SIGSEGV — worse. A fix changes the depth arithmetic the sweep must target, so sequence it after.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-13 (Claude Sonnet 5) — W-2 CLOSED (a) + W-9 SPLIT OUT BUT BLOCKED · MANUAL GROUNDING SUPPLIED FOR B-12/EARN-4 CROSS-SEAT FINDING · ZERO SOURCE BYTES CHANGED · NOT PUSHED (no credential yet this session)

**WATERMARK UNCHANGED — no rung executed against it.** SCRIP `5547de99` · `.github` `c39208cb` at session start; local-only commits since (`.github` `f6ccc909` = FINDING-2026-08-13b; this cursor + W-2/W-9 edit not yet committed). Tooling verified fresh this session (`install_system_packages.sh`, gdb 15.1 live), `scrip`/`libscrip_rt.so` build green from clean clone — both confirmed rather than inherited.

**Did:** (1) Resolved W-2's sixth-re-asked disposition myself rather than re-parking it — evidence supports (a) FALSIFIED, not (b); split the one real finding inside it into W-9 with current-content-verified locations (the file's line numbers had already drifted under RBP-EARN's EARN-1/EARN-2 commits — grepped by name, not trusted by number, per RULES.md). (2) Read BOARD's fresh cross-seat finding on W-4/W-7's ARBNO-cell design and supplied the manual grounding (Ch.9 pp.121-123) it explicitly flagged as missing — SPITBOL's own stack-based recursion model for `*name` self-reference backs "one frame per recursive activation" as the correct semantic target, not merely RBP-EARN's offset-hazard side effect. Written up as `FINDING-2026-08-13b`, local commit only.

**Did NOT do, deliberately:** did not implement W-9 — the exact code sits inside the master's COLLISION PIN (RBP-EARN's EARN-1/3/4/11 are mid-flight in the same function), and the ordering law says not before P3/EARN-11. Did not implement EARN-4/the W-7 fix — it's RBP-EARN's rung, not this seat's, and no RBP-EARN session is live to merge intent with. Did not touch W-3, W-4, W-6, W-7, or W-8. **UNBLOCKS: nothing landed; W-9 is a well-scoped item waiting on P3/EARN-11, and 4b above is a routing question for chat, not a rung either seat should open solo.**

## ⭐⭐⭐ LIVE CURSOR — 2026-08-12 (Claude Sonnet 5) — W-3 CRASH ROOT-CAUSED (not fixed) · GOAL FILE PRUNED · PUSH BLOCKED PENDING CREDENTIAL

**WATERMARK UNCHANGED: 160 pass · 1 xfail · 5 REGRESSION {D12,D13,H31,X01,X10}** (the W-3 crash is a new construction outside that suite). **SCRIP `a037b637` · corpus `1dd3ff15` — ZERO source bytes changed in both, confirmed `git diff --stat` empty.** `.github`: finding + this prune.

**Did:** carried s39c's W-3 crash from "diagnosis-in-progress" to confirmed root cause via the prescribed gdb hunt (breakpoint + spin + single-step through JIT'd code, no symbols). Result and both ruled-out hypotheses are folded into the W-3 rung above; full detail `FINDING-2026-08-12s`. Temporary `stfh()` diagnostic instrumentation was added to `bb_match_begin.cpp`/`bb_match_end.cpp` and FULLY REVERTED (tree diffs clean). Claim gate re-run green (`--quick`: 3 programs / 6 slabs / 71 shape checks, BINARY MEDIUM CLEAN).

**Pruned this file per RULES.md step 1 + STALE-ORIENTATION (c)** (Lon-directed; s39d had recorded the debt and declined to execute it unilaterally): 997 → ~70 lines. W-0/W-1 collapsed to a CLOSED ledger; W-2/W-3/W-4/W-7 made terse with every measured fact and constraint kept; the s35 CARRY SHAPES block folded INTO W-4 (it was referenced by a rung but living in a cursor slated for deletion); six Lon-questions merged with this session's two into ONE renumbered list; cursors below the last three deleted (s39b, s39, s38 + its scrutiny block, s36, and four older ones — all superseded, content preserved in the named FINDINGs). **The prune was AUDITED, not assumed:** every load-bearing token was grep-diffed old-vs-new, and four blocks that were living inside doomed cursors were RESCUED into the rungs/gates that actually own them — the s35 CARRY SHAPES (→ W-4), the "do NOT fix before W-5 opens" predictions incl. the `[r10]`/`XK_R10MIR` encoder landmine (→ W-5), the by-set floor + correct `run_suite.sh` path + the two earned instrument rules (→ GATES), and the unresolved `dc_sib_bt` silently-wrong-at-rc-0 escalation (→ Q8). Stale m4-is-dark prose was DROPPED rather than carried, since BOARD's B-1 r9 seed postdates it.

**NOT DONE / deliberately left:** no fix landed (W-3's real fix is a frame/depth-accounting scope decision — Q7). No watermark move. RULES step 4 regen NOT APPLICABLE (zero codegen bytes). **UNBLOCKS: W-4 — the layout now has its third candidate shape named and a root-caused defect motivating it.**

## ⭐ CURSOR — s39d (Claude Sonnet 5) — plan scrutiny, zero source edits
Reviewed RUNGS + the open-questions block against session evidence; applied four corrections (W-7 X01 note, W-3 status, gate wording, questions refresh). Watermark unchanged. Flagged the prune debt this session executed. Its questions block is superseded by the merged list above.
