# GOAL-SN4-HOME-WIRES — r10/r11 wires, two glue kinds, shim deletion (HOME seat; master = GOAL-SN4-HOME.md)

**CHARTER:** rΓ=r10 · rΩ=r11 product-wide (Lon s12 verbatim: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11"*), exactly TWO glue kinds (ONE-SHOT · PASS-THRU; FRAMED IS NOT A GLUE KIND — s29), RBP never written by glue. **Mechanism authority = LADDER WREG + LADDER PT inside `GOAL-RBP-EARN.md` (absorbed by reference, corrections here supersede).** RTCC's wire half (veneer preservation) is owned HERE per the s14 arbitration: safe config = RTCC-ON **and** wire capture/restore, neither alone.

## RUNGS
- [ ] **W-0 · CLAIM SWEEP, HONEST.** r10/r11 usage census across templates + emit.cpp + **RTX hand asm + raw-byte encoders** (grep is insufficient — objdump the emitted slab too). With BOARD: claim gate becomes data-driven {rbx r9 r10 r11 r12 …} + `--strict` (today: r9-only, informational — a hole MECH documented).
- [ ] **W-1 · ZCTX SCRATCH ERADICATION.** All six ZCTX sequences use r10/r11 as scratch (s37 measured) — re-allocate BEFORE any flip; live-on-arrival landmine otherwise.
- [ ] **W-2 · PUSH/POP GUARD UNIFICATION (MECH s37(B), the sole D12/D13 regression pair).** push guard `flat_jmp_entry` (emit.cpp:2373) vs pop guard `!_wire_stub && flat_jmp_entry && flat_pat` (:2806): any graph outside the intersection pushes and never pops = POP DEBT. ⛔ ONE predicate, both media — never a compensating pop on one exit (two-calculators disease). If the CLASS-O/`_wire_stub` design call is still ambiguous after the census, route both arms to Lon.
- [ ] **W-3 · WREG MECHANISM, DORMANT.** Site glue `lea r10,[rip+site_γ]` · `lea r11,[rip+site_ω]` · `jmp <first interior box>`; exits `jmp r10`/`jmp r11`. Killswitched; default emission byte-identical to HEAD. r10/r11 are caller-saved ⇒ saves are TEMPLATE-EMITTED per-activation on the spine, never an implicit choke (s18 RSP-SAFETY + the stack-arg witness).
- [ ] **W-4 · ARENA WIRE-PAIR SLOT (+16B) — THIS SEAT OWNS THE LAYOUT.** Blob-interior pending records capture {r10,r11} at push, restore at β, or it is `g_blob_ctx`'s single-cell defect in register clothing (the LAW). RBP/EARN-5 consumes this layout — one authority.
- [ ] **W-5 · ⛔ THE FLIP — REQUIRES EARN-1 + EARN-3 LANDED (EARN-10 ordering).** PROC-shim deletion (PT-1..3), CLASS-D exit ceremony dies with it. The old WREG residual (19 SEGV + 7 HANG) was MISSING FRAMES, not glue defects — EXPECTED cured by EARN; measure by set, never assume.
- [ ] **W-6 · RTCC RE-ENTRANT PRESERVATION + DEFAULT-ON REVALIDATION.** The veneer round-trips wires on leaf crossings only; fix the re-entrant case; then RTCC default-ON must hold the P0 floors with NO `SCRIP_RTCC=0` escape (kills the m4-130 class). Belt-and-suspenders: `-Wl,-z,now` for the r11 lazy-binding clobber.

## GATES (every rung)
claim gate `--strict` green · probe + crosscheck BY SET vs P0 floors both modes, RTCC ON and OFF until W-6 seals · killswitch md5 discipline · FINDING + cursor move.

## ⭐ LIVE CURSOR — 2026-08-12 s33 (Opus 5) — **W-0 OPENED AND HALF-LANDED. ⭐⭐⭐ THE CENTRAL RESULT: THE RTCC VENEER ALREADY ROUND-TRIPS *BOTH* WIRES AT EVERY LEAF CROSSING — 172 VENEERED SITES ACROSS 15 PROBES, `rt_match_enter` 15/15, r11 RESTORED LAST AS DESIGNED; THE ONLY BARE CROSSINGS ARE PROLOGUE + `exit`, WHERE NO WIRE CAN BE LIVE. THE EXPOSURE IS THE RE-ENTRANT CASE (W-6), NOT THE LEAF.** ZERO compiler bytes.

**Full chain, numbers, and two self-corrections:** `FINDING-2026-08-12d-CLAUDE-OP5-WIRES-W0-THE-VENEER-ALREADY-ROUND-TRIPS-BOTH-WIRES-AT-EVERY-LEAF-CROSSING-AND-THE-SWEEP-SIZE-IS-NOT-232.md`. Trees measured: SCRIP `52545cbf` (+ census commit `19d9ba37`) · corpus `c91d1adf` · x64 `5035571`.

- **⛔ THE SWEEP SIZE IS NOT 232.** The source gate sums four classes with opposite meanings: ~52 occ are the `bb_scan_*` `push r10`/`pop r10` **PRESERVERS** (the cure, counted as the disease — driving the number to 0 deletes the discipline W-3 mandates), plus `x86_asm.h` decoder/comment noise, plus 6 **LICENSED** occurrences, leaving `bb_call_fn.cpp` (93) + `xa_flat.cpp` (25) + `bb_var.cpp:19` as the genuine scratch. **Classify before sweeping.**
- **⛔ INSTRUMENT-TRUTH DEFECT, CHEAPEST NEXT ACTION:** the gate prints `licensed: 0` because `wreg_claim_whitelist.txt` is empty — but the glue emitters ARE in the tree at `emit.cpp:2687` (res landing reloads both wires) · `:2720-2722` (pass-thru suspension record `{res,r10,r11,pad}` 32B, per-activation on the spine) · `:2737` (CLASS D ω `jmp r11`). Populate the whitelist from those three; no compiler bytes.
- **NAMED PREDICTION FOR THE FLIP (do NOT "fix" now — r10/r11 are not yet wires):** the scan family is asymmetric — **26 `push r10`, 0 `push r11`** across 10 files. At W-5 every one of those sites protects γ and abandons ω. Witness it at the flip; never assume it.
- **ENCODER LANDMINE, LIVE:** `[r10]` in a template parses as `XK_R10MIR` → `x86_store_cursor_mirror()` emits `mov [r10], r14d` (`0x45 89 32`), a STORE, not an addressing mode. `xa_flat.cpp:249` and `bb_call_fn.cpp:372` already carry hand-written `[r10 + 0]` workarounds. Any W-3/W-5 template touching `[r10]` must use `[r10 + 0]` or the encoder must disambiguate.
- **⛔ TWO SELF-CORRECTIONS ON RECORD (both mine, both caught before publication):** (1) a name-shaped `/match|scan|pat/` filter counted `bb_match_defer[abi:cxx11]()` — a COMPILE-TIME template fn returning `std::string` — as a match-time hazard; `nm -C` settles it. (2) An intermediate table claiming `rt_match_enter` BARE in 10/15 probes is **VOID**: `for (n in calls)` yields awk keys as STRINGS, so `i < n` compared `"92" < "104"` lexicographically and the lookback never ran. **Rule earned: a scanner reports a class only after one member is confirmed BY HAND.**

**NEXT SEAT, IN ORDER:** (1) **whitelist population** (above) — 10 minutes, makes the burn-down honest. (2) **W-6's nested-crossing witness** — `g_rtcc_block` is ONE FLAT BLOCK at fixed offsets (r10→+56, r11→+64), so a re-entrant `rt_*` that re-enters emitted code (`rt_dcap_step`, `rt_proc_open_fn`, a deferred fn inside a pattern) makes the INNER writeback overwrite the OUTER wires. Compile `140_pat_eval_double_fn_trick` / `141_pat_eval_double_fn_arbno`, count writeback/reload pairs on the re-entrant path, show the clobber. **Same single-cell disease as `g_blob_ctx`/`g_zctx`/`g_star_peek`; cure is the per-activation spine shape `emit.cpp:2720` already uses.** (3) W-1 (ZCTX scratch) is untouched by all of this and runnable. (4) W-2's push/pop guard pair owns D12/D13.

⛔ **W-5 REQUIRES (predicate — CHECK, never wait):** `grep -rn "frame_need_of" /home/claude/SCRIP/src/` non-empty AND an EARN-3 anchor landing on origin (RBP cursor line `UNBLOCKS: WIRES W-5`); FALSE ⇒ W-6's RTCC half or the master POOL. **Checked this session: `frame_need_of` grep is EMPTY ⇒ W-5 correctly skipped.**

**UNBLOCKS: WIRES W-6** (the leaf half is proven correct by measurement, so W-6's scope narrows to the re-entrant case only — no need to re-audit leaf crossings).
