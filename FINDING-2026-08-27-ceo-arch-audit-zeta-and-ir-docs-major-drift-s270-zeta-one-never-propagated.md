# FINDING 2026-08-27 (ceo): ARCH audit 1 of 3 — ARCH-ZETA-LOCAL-STORAGE.md and ARCH-IR.md are MAJOR DRIFT; the s270 zeta-ONE ruling never reached the zeta doc

**Context.** Lon's standing order this date: the CEO ensures ARCH*.md stays correct against source and plan; the fleet is ordered to READ these docs before touching BB/ζ code — so a wrong ARCH doc now actively misleads people following orders. Audit by doc-vs-source verification at current HEAD (post-srcreorg moves `cf1f2961` parser→frontend, `d4312e86` contracts+machine+include→ir).

## ARCH-ZETA-LOCAL-STORAGE.md — MAJOR DRIFT

- **Dead paths (7+):** every `src/contracts/` citation → `src/ir/` (zeta_storage.{h,c}, descr.h, IR.h — struct now at :178 not :156, scrip_ir.c — :267 not :206, zeta_choices.h); two reading-list artifacts (`artifacts/asm/fixtures/arbno_alt.s`, `archive/backend/bb_boxes.s`) do not exist.
- **Deleted entities still taught:** §3's whole table is keyed on `IR_t.tmp` — **the field does not exist** (IR.h:178-188 has no tmp; re-key to the zls grant in zeta_storage.c). `g_proc_arena`/`PROC_FRAME_DEPTH×QWORDS`, `bb_callee_frame.cpp` (decl-only residue), `rt_zcol_grow`, `alt_resume_chain_fc`, `x86_zeta_selfload_*` (live accessor is `x86_selfload_mode()`, x86_asm.h:485, one and shared): **zero hits each**. `scan_stack[]` is now `scan_saved[]` (gen_runtime.c:23). `x86_scratch_off` sites are emit.cpp:1620/1628 (two, not three).
- **Stale mechanisms:** Terms table says rZ "Ratified = r12" — **rZ is rsp** (`x86_zr()` x86_asm.h:491; ZC_FRAME=ZC_FRAME_RSP). The r12 prologue (`push r12; mov r12,rdi`) does not exist in xa_flat.cpp (real prologue: `mov rsp,[anchor]` :119). The **ZC_ALLOC axis does not exist in zeta_choices.h** — replaced by ZC_STORAGE(=CELL_STACK) and ZC_ZETA(=ZLS2); D15's "flip to BUMP_LIFO" swaps nothing. ZC_SELFLOAD gained a 5th mode (ALLOC=4, the only one with live emitter code). §13 says PORT_FORTH is "NOT default" selectable via flags — **it IS the compiled default and the flags refuse rc=2**.
- **⛔ THE HEADLINE: the s270 zeta-ONE ruling is entirely absent.** `scrip.c:818-820` refuse `--zeta*` with "the four zeta configs are ONE (Lon, via CEO-11) — storage is cell-stack, port is forth, zeta is zls2, always" — yet §5j still frames every axis as a live compile-time switch. A reader planning a ζ A/B campaign from this doc designs dead arms. Suggested single highest-value edit: an AXES-CLOSED banner at §5j.
- **Law contradictions:** 6 uses of the VOID term "stackless", one (:787) listing it as a *currently standing rule*; §7a/§13/§14 choose frame flavor by BOX-KIND and by LANGUAGE ("SNOBOL4 IS ALL-STACK… heap flavor ICON-ONLY") — superseded by RULES.md § BB FRAME-PLACEMENT CRITERION; the appended criterion section (:939-947) is correct, but §7a/§13/§14 headings carry no supersession marker, so a mid-document reader takes retired law as current.
- Verified-correct spine: the gates, zls_build/zls_group_mark, --dump-zeta, GC/heap sections (rt_gc_collect, HBF_PIN, zeta_heap, region_report), fc_geom/fc_alt_register, PEERS (zls[] parallel table), §11-14 content, and the FRAME-PLACEMENT addendum.

## ARCH-IR.md — MAJOR DRIFT

- **Dead paths (10):** `src/include/ast.h`→`src/ir/ast.h`; `src/runtime/x86/{sm_prog.h,sm_interp.c,bb_box.h,bb_broker.c}` — the directory does not exist (bb_box.h→`src/ir/`; `bb_broker` zero hits); `src/lower/SM.h`→`src/ir/SM.h`; `emit_io.c` "(survives unmerged)" does not exist; `src/include/stage2.h`→`src/ir/`; `src/lower/sm_prog.c`→`src/ir/`; `src/runtime/interp/{icn,pl}_runtime.h`→`src/runtime/builtins/gen_runtime.h` / `resolution.{c,h}`.
- **⛔ Worst item: the `STMT_t.lang` / `LANG_*` section (:100-116) documents a field and macro family that were DELETED to satisfy the live `emit_no_lang` gate** (RULES.md NO LANGUAGE SENTINEL; `grep LANG_ src/` → 0, only a parked lower file). The doc teaches exactly what the gate forbids. Delete the section; resync STMT_t (real fields incl. `nofail`, `goto_s/f/u`).
- **IR-CONSOLIDATE invariant names dead API:** `g_stage2.sm.bb_table`→`g_stage2.bbp.table` (bb_program_t), `SM_seq_bb_add`→`bb_program_add`, `BB_graph_t`→`IR_graph_t`; `IcnProcEntry`/`Pl_PredEntry_BB`/`Pl_PredTable`→`ProcEntry`/`Resolve_PredTable`; `rt_pl_b_end_register` zero hits; `SM_BB_PUMP`/`SM_BB_ONCE` gone (only `SM_EXEC_STMT` survives); template count 149 not 161 (132 bb_*.cpp).
- VOID "stackless" in the :66-71 correction block (content survives as no-software-value-stack; the ARCH-x86 cross-ref it cites is itself void).
- The file's own self-flagged stale sections (:189-199, :270-277) remain accurate — the UN-flagged sections are the problem.

**Disposition:** repair row `arch-doc-repair-bundle` minted (rank 1) with this FINDING as worklist; "stackless" rewording rides the existing `stackless-eradication` row. Audits 2 (x86/Prolog) and 3 (Icon) reported in their own FINDINGs.
