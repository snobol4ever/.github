# FINDING 2026-09-02 hq_C — Rung 1 lands: head unification and `X = T` through the shared `bb_call` leaf around `rtx_plunify.s`, body conjunction as the identical Proebsting wiring, THE TRAIL on `r12` with the log rule read through `r13` inside the unify service, and ZERO `r12`–`r15` writes in emitted code (the five writers are named rtx helpers)

**Row:** `prolog-rung-1-fact-with-arguments-rule-body-conjunction-and-the-trail-log-rule` (ceo, CEO-149). **Mode:** TRIO (MODE file). **Law:** `RULES.md` § THE PROLOG REBUILD GATE; sovereign page `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § B.1 / B.2 / B.4 / B.11 / § E row 1; the CEO rulings on the rung-0 judgement calls (FINDING § 4 of the rung-0 receipt) are honoured as written.
**Tree:** started from origin SCRIP `db299d41` · corpus `9c648987` · `.github` `af99783d`; landed as SCRIP `7d0ed0f8` · corpus `e5ba308f` · `.github` `(this commit)`. `RT_OPT=-O0`. Every verdict in § 6 was re-taken on a `make pristine` build of the pushed tree (HQ-27); the control arms in § 5 were captured on the rung-0 build before the first edit.

## 1. What landed, by the brief's three letters

**(a) Facts with arguments and rule bodies.** `lower_pl_pred_graph` no longer refuses `arity > 0`; `pl_clause_graph` (`lower_prolog.c`) chains one `IR_CALL "$unify"` leaf per head argument, right to left, in front of the body — `$unify(var_ref A_i, term)` where the term is `term_lval_e` of the head argument (a variable → `IR_VAR_REF G_k`, an atom/number → a literal, a compound → the `$mkc` builder), exactly the pre-cut `lower_pl_clause_into` shape with `IR_CALL_PROLOG` replaced by the shared `IR_CALL`. `X = T` as a goal (`TT_UNIFY` and the `=/2` functor) lowers through `unify_pair`, which decomposes same-functor compounds at compile time and otherwise emits the same leaf. The parameters are `F.A[i]` (`g->pnames = A0..`, staged through Icon's `g_call_args` and installed at `[rbp+16+16i]` by the shared prologue's `rt_icn_zframe_args_install`), the clause variables are `F.G[k]` (`g->lnames = G0..`, granted frame slots by `zeta_storage.c`'s local discovery, seeded unbound by the prologue's `rep stosb`). No by-name path: the leaf routes through `dop_direct_fp` to `rt_pl_dop_unify` / `_ci` / `_cs` (the const fast path of `bb_call_fn.cpp` is kept). `\=` refuses naming rung 5 (it is `\+ =`).

**(b) Body conjunction.** Landed as the IDENTICAL WIRING, not the box: Icon's `IR_CONJUNCTION` node (`bb_conjunction.cpp`) is a value-forwarding leaf — its Byrd wiring (`A.γ → B.α`, `B.ω → A.β`, `A.ω → C.ω`) is done by the Icon lowerer's TT_CONJ arm, not by the box. Prolog has no value to forward, so `pl_lower_conj` (rung 0's Proebsting `plus`) threads γ forward and ω back to the youngest resumable goal's β and emits no node. `bb_conjunction.cpp` is untouched; the Icon control arms (§ 6) are the proof that nothing shared moved.

**(c) THE TRAIL.** `r12` is TR. `rt_pl_tr_init` (`src/runtime/rt/rt_pl_trail.c`, new) takes a 2^26-byte `rt_slab_region` and aligns a 2^25-byte arena inside it (`PL_TR_ARENA_LG2 = 25`, 32 MB, 1,048,575 entries), so `base = r12 & ~(2^25−1)` — **the arena's only handle is `r12`; there is no global** (`nm -D` names none, `test_gate_pl_no_new_global.sh` PASS). Header: 32 bytes at `base`, `[base+0]` = the live top; entries: 32 bytes `{cell, pad, old DESCR}` from `base+32` (32 not the pre-cut 24, so the old DESCR sits on the 16-byte grid the conservative scanner walks). Overflow REFUSES rc=2 naming the limit (`rt_pl_tr_refuse`), never aborts, never wraps. **`F.TRMARK`:** every pinned prologue stores `mov [rsp+kt-64], r12` at α (`xa_flat.cpp`; measured `p/2`: `[rsp+256]`, `main/0`: `[rsp+64]`). **The log rule, inside the unify service:** `pl_tr_needs_log` (`rt_pl_trail.h`) — with `B = r13` (H of the youngest choice; 0 at the root), a binding of cell `c` is logged iff `B ≠ 0` and (`c` is a heap cell, i.e. at or below the C stack probe, or `c ≥ H+64`, older than the choice frame's top); a cell inside the choice frame or younger is not logged. **Atomicity (B.11):** each unify body records its entry mark and, on mismatch, `rt_pl_tr_unwind_to` restores every cell it bound before returning FAIL. **How C sees `r12`/`r13`** (rule (1) of the grant): every binder reachable from a Prolog graph enters through an rtx gate in `rtx_plunify.s` — `rt_pl_dop_unify`, `rt_pl_dop_unify_ci`, `rt_pl_dop_unify_cs`, `rt_pl_dop_mkc`, `rt_pl_dop_is_v` — that parks `{r12, r13}` in a `pl_tr_ctx_t` on its own 24-byte frame, calls the `_c` body with the ctx as the third argument, and reloads `r12` from the ctx on return; `plw_bind` / `plw_unify_cells` / `plw_unify_vals` / `plw_mkc_kids` carry the ctx as a parameter. **The root seed is ONE rtx helper:** `rt_pl_quad_seed(H)` — `r14 := H`, `r13 := 0`, `r15 := 0`, `r12 := rt_pl_tr_init()` — called from the `root_graph`'s pinned prologue; the four inline seeds of rung 0 are gone, and the m4 veneer's shared `mov r12,[0x70000000]` / `xor r14d,r14d` are not emitted for a pinned root graph (`scrip.c`, keyed on the graph properties, not a language name). **GC:** the arena is a root range whose top is a word (`rt_gc_root_range_add_topword`, a 4-line addition to `gc_heap.c`: `hi == NULL` means read the top from `[lo]`); every gate writes the top word on exit, so the scan covers exactly the entries that were live at the last binder — a superset of the live entries at any later GC point, never a subset.

**The observable at rung 1 is nil by construction** — no choice exists, `r13` is 0 for the whole run, nothing is logged, and the witness's output does not depend on the trail. So the mechanism has its own instrument: `scripts/test_gate_pl_trail_mechanism.sh` (new) compiles a C harness against `out/libscrip_rt.so` and drives the exported `_c` bodies directly — 12 checks: B=0 binds and logs nothing · B set and cell older → exactly one 32-byte entry `{&cell, 0, old}`, the top word tracks it · `rt_pl_tr_unwind_to` restores the cell and pops · a cell younger than the choice is bound but not logged · `f(X,1) = f(2,3)` fails, unwinds its own suffix, X is unbound again · the arena limit REFUSES rc=2 in a forked child. Fail-once/pass-once: a copy with one expectation flipped reports `GATE FAIL(1) … 1 trail-mechanism checks failing (examined 12)`; the real one `PASS(0)`.

## 2. The witness and the hand witnesses

`ladder__rung01_fact_rule` (`directive_83`): `p(a,b). main :- p(X,_), write(X), nl.` → `a` in both modes (was `rc=2 … rung 1 lands it` on `db299d41`). The BX-0 trace (`ALL.trace`, re-cut with the merging `--to 1 --cut`; the rung-0 block byte-identical, 42 blocks kept) now reads as § B.1/B.2 draw it: `Call main/0 · var_ref G0 · var_ref G1 · Call p/2 · var_ref A0 · lit_string a · call $unify · var_ref A1 · lit_string b · call $unify → p/2_γ · Exit p/2 · var G0 · call $write · call $nl → main/0_γ · Exit main/0` (26 lines; the pre-cut ref had 30, its `$trail_mark` and `move_label` boxes no longer exist). Hand witnesses against `swipl -q -t halt`, m3: compound head argument `q(f(X)) :- write(X)` + `Y = g(1,Z), Z = 2, write(Y)` → identical; shared head variable `r(X,X)` three ways → identical; a partial binding inside a conjunction `X = f(A,b), X = f(a,B)` → `ab`, identical; nested `p(f(g(X)), [1,2|T], 7)` → identical modulo the unbound-variable print name (`_G0` vs swipl's `_5196`, both implementation-chosen); a failing init goal (`p(b,X)` against `p(a,b)`, and `X = f(Y), Y = 1, X = f(2)`) → empty stdout in both, swipl prints its `Initialization goal failed` warning and exits 0 under `-t halt` where SCRIP exits 1 — the directive's failure shape is rung 10's, not this rung's.

## 3. Judgement calls, for ceo to rule on

1. **Five helper names in `QUAD_HELPER_RX`, not only "TR helpers".** The brief says the allow-list gains exactly the TR helper names. The honest set of writers is five: the four binder gates (each writes `r12` once, on reload) and `rt_pl_quad_seed`, which seeds all four registers. Folding the root seed into one rtx helper is what makes emitted code write none of `r12`–`r15` (arm 1: 0 writes on 3 compiled witnesses; the rung-0 tree had 5); the alternative — leaving rung 0's inline seeds — would have needed an emitted-label exception, which is a per-site exception by another name. The default RX in hq_P's gate now names the five; with the empty default the gate still says NO (5 violations, all in the helpers), so both arms of the instrument are exercised.
2. **The m4 veneer's two seeds are suppressed for a pinned root graph** (`scrip.c`, one `_pinned_root` test). They were dead for Prolog (the root graph re-seeds all four) and were 2 of the 5 rung-0 violations. SNOBOL4's and Icon's veneer output is unchanged (the test is on `zframe_pinned_base && root_graph`, which only Prolog graphs set).
3. **The inline `pl_unify`/`pl_bind` family of `pl_cell.h` is NOT gated yet** — 43 sites in `unification.c` (functor/3, arg/3, `=..`, copy_term, …), all rung-6+ value services the rung-1 lowerer refuses. They bind with no ctx until rung 6 gives each a gate and a name in the RX. Recorded in § B.11's MEASURED paragraph as rung 6's obligation (hq_P). The alternative (threading a ctx through 43 sites that no graph reaches) would be untested code.
4. **`gc_heap.c` gained the top-word root range** (shared code, 4 lines, `hi == NULL` is the only new meaning). The alternative — re-registering `{base, top}` at every gate — is a linear table walk per unify; the alternative to that — scanning the whole 32 MB arena — commits nothing but costs ~2M word checks per GC.
5. **`test_gate_pl_no_new_global.sh` was RED on the committed rung-0 tree** — my own residue: the five compile-time rung tables in `lower_prolog.c` were named `g_pl_rung*_builtins`. The rung-0 receipt quoted only the ratchet line as PASS (a transcription defect, mine). Renamed `pl_rung*_builtins`; the gate is green on both arms now. Nothing in the allow-list changed.
6. **`\=` refuses at rung 5**, not 1: it is negation-shaped (`\+ X = Y`) and lands with § B.8.

## 4. Instruments

- **Quad gate** (hq_P's `test_gate_pl_quad_regs.sh`), before → after: emitted quad-writes **5 → 0** (`ladder__rung00_hello`: `mov r12,[0x70000000]`, `xor r14d`, `xor r13d`, `xor r15d`, `lea r14`), compiled witnesses 1 → 3 (rung00, rung01, `probe_plz__plz_p1_single_clause`), reachable rtx quad-writes **0 → 5** (all in `rtx_plunify.s`, all helper-legal); verdict under the named RX **PASS(0)**, under the empty RX FAIL(1) with 5 — the instrument says NO and YES. Its `--self-test` still passes.
- **Trace gate** `test_gate_pl_port_trace.sh --to 1`: FAIL(1) on the pre-cut ref (`total 26 != ref 30`, both modes, killswitch and perturbation OK) → `--to 1 --cut` (merged) → PASS(0). The rung-0 block did not change under the cut (the prologue change is invisible to the port hook).
- **Ladder** `test_prolog_ladder.sh --to 1`: PASS 4/4 (witnesses 2, modes 2).
- **Trail gate** `test_gate_pl_trail_mechanism.sh`: PASS(0) over 12 checks; fail-once shown above.
- **The DONE-WHEN as written in the baton** ran once under `bash -x` on the pristine build: rc=__DONEWHEN_RC__.

## 5. Control arms on the rung-0 build (before the first edit)

Icon smoke m3 14/14 · m4 14/14; Icon rung suite interp/run/compile each `PASS=264 FAIL=6 BADEXIT=1 XFAIL=27 TOTAL=298` (rc=1 is the suite's standing state, the watermark is the four numbers); quad gate 5 violations; Prolog master `SUITE_BOARD total=383 m3_pass=6 … m4_pass=6` (the rung-0 receipt); Prolog smoke 1/5; van Roy CLEAN 0 REFUSE 0 other 21.

## 6. Verdicts on the pushed tree (pristine `-O0`)

- **The DONE-WHEN as written in the baton**, `bash -x`, on the pristine tree `79e4bc3b` / `e5ba308f` (this rung over origin `d754df54`): **rc=0**. On the final tree `7d0ed0f8` (the same rung over `bd6bacb7`, an Icon-lowerer commit that landed under the push) the first five arms were re-taken green (ladder 4/4, trace PASS(0), `nm -D` 0, strip 0, Icon smoke 14/14) and `make test` was re-run twice: the first run died with an unattributed SIGTERM inside the SNOBOL4 board while five other suites and a worktree build shared the machine (`make: *** [Makefile:105: test] Terminated`, rc=143 — a signal, not a verdict), the second was still in its SNOBOL4 board when Lon ordered the wrap-up; **`s4e_msg.sh done` re-runs the whole DONE-WHEN and is the row's closing verdict, not this sentence.** The arms as measured: `test_prolog_ladder.sh --to 1` **PASS 4/4** (witnesses 2, modes 2, denominator printed) · `test_gate_pl_port_trace.sh --to 1` **PASS(0)** (examined 4; killswitch, perturbation and trace all OK, both modes) · `nm -D out/libscrip_rt.so` Prolog-only data symbols **0** · `strip_comments.py --check` **0** files · `test_smoke_icon.sh` **m3 14/14 · m4 14/14** · `make test` **rc=0** — SNOBOL4 **m3 1679/1679 · m4 1679/1679 FAIL=0** over the printed denominator, capture/stdin, Term ratchet 0/0, emit_no_lang OK, medium_invisible OK, corpus coverage OK, optbypass watermark (DEFAULT 0/1656, `SCRIP_OPT=0` 192 ≤ 192), and hq_P's quad gate last (`quad-scan: files=3 writes=0 enrolled=0 violations=0 rtx-defined=52 rtx-reachable=5 rtx-writes=5 rtx-violations=0`, canaries c1/c2 RED and c3 REFUSE(2) as required).
- **Hard shared-node arms:** Icon rung suite (`test_icon_rung_suite.sh`) interp / run / compile each `PASS=264 FAIL=6 BADEXIT=1 XFAIL=27 TOTAL=298` — byte-identical to the rung-0 reading (the watermark; its rc=1 is the suite's standing state). Regenerated artifacts: `util_regen_benchmark_s_artifacts.sh` and `util_regen_demo_s_artifacts.sh` changed **0** files (the SNOBOL4 and demo `.s` are byte-identical: the pinned-prologue and veneer changes are keyed on `zframe_pinned_base`/`root_graph`, which only Prolog graphs set); `util_regen_prolog_bench_s_artifacts.sh` refused 23/23 (every kernel needs a rung above 1).
- **Structure gates:** `test_gate_emit_no_lang.sh` OK · `test_gate_template_medium_invisible.sh` OK · `test_gate_no_handencoded_bytes.sh` OK · `test_gate_pl_no_new_global.sh` **PASS both arms** (RED on `db299d41`, § 3.5) · `test_gate_pl_no_value_stack.sh` PASS · `test_gate_pl_trail_mechanism.sh` **PASS(0), 12/12** · quad gate under the named allow-list **PASS(0)**: emitted quad-writes **0** (5 on `db299d41`), reachable rtx quad-writes **5**, all in the five named helpers; under an empty allow-list it says NO (5).
- **The rebase:** five SCRIP commits (`d754df54` … `5934802b`: hq_P's quad gate rewrite + scanner, hq_B's harness `extract --origin` and sharded SNOBOL4 board, an Icon find/upto lowering, a Pascal `bb_return` arm) and one corpus commit landed under this push; none touched a file of this rung except the Makefile (a different region). The old gate's allow-list edit was dropped and the five names went into the scanner's `TR_HELPER` default instead (hq_P's `(site, register, shape)` contract keeps emitted enrolment by shape — there is nothing to enrol, emitted code writes none of the quad). Every number above was re-taken on a `make pristine` of the rebased tree, not carried over. A second rebase followed (`bd6bacb7`, `src/lower/lower_icon.c` only — `variable("lit")` resolution, ceo/hq_P's Icon guard cure): on that merged pristine tree the Icon rung suite again reads 264/6/1/27 in all three modes, the trail gate PASS(0), the master board 50/383, the three artifact regens 0 changed files; a control build of `bd6bacb7` alone in a scratch worktree (so the Icon watermark has a same-base before/after rather than a reasoned one) was started at wrap-up — its first run refused for want of a sibling corpus (`--corpus` names it), its re-run was in flight when the session closed. Between the two rebases the prolog-bench regen auto-committed its marker refresh (corpus `e29ea5d0`, identity LCherryholmes). **A third rebase at wrap-up** (`5a49419b`: hq_P's r9-reload mask in `x86_asm.h`, ceo's Icon `name()` lowering, hq_B's rung-suite rc rule — none of this rung's files) put the commit on main as `7d0ed0f8` on ceo's wrap-up order; the DONE-WHEN was last run whole on `79e4bc3b` (rc=0) and its SNOBOL4 board again green on the pre-third-rebase tree, which is parked verbatim as origin branch `hq_C/prolog-rung-1` (= `12960ac8`); **the post-third-rebase re-proof is owed by the next session and `s4e_msg.sh done` runs it** — that run was started in the background at wrap-up.

## 7. REPORTED, not gating (RULES clause 4)

Prolog master board (`corpus_suite_harness.py run … --lang prolog --modes m3,m4`, pristine rebased tree): `SUITE_BOARD total=383 m3_pass=50 m3_fail=321 m3_xfail=11 m3_xpass=1 · m4_pass=50 m4_skip=321 m4_xfail=11 m4_xpass=1` — **50/383 both modes** (rung 0: 6; pre-cut 348/371); one entry marked XFAIL now passes (the harness summary does not name it — hq_B's marker promotion, not this rung's). Prolog smoke **2/5** in m3 and m4 (rung 0: 1/5). van Roy acceptance **CLEAN 0 · REFUSE 0 · CRASH 0 · other 21** (unchanged; every kernel is above rung 1; the pin is re-taken at rung 8). `test_prolog_rung_suite.sh` (the 15-entry rung board, a different instrument from the master) interp 3/15, compile 0/15 — reported because it prints, not because it grades this rung.

## 8. What rung 2 starts from

`F.TRMARK` (`[H+0]`) holds `r12` at α in every Prolog frame; `F.B0` (`[H+24]`) holds `r13` at α; `F.CUR` (`[H+8]`) and `F.RES` (`[H+16]`) are zeroed. The clause step needs: `r13 := H` at α when the predicate has more than one candidate (the frame becomes the youngest choice), the clause-step β chunk on `bb_call_proc_staged` (Jcon `Alt` with `F.CUR`/`F.RES` in the frame), an rtx unwind helper `rt_pl_tr_unwind(mark)` that walks `r12` down to `[H+0]` restoring cells and writes `r12` — **name it in `QUAD_HELPER_RX` in the rung-2 commit** — and the re-seed of `F.G[*]` (the choice frame's own cells are never logged, by the rule above, so the step must clear them all; `rep stosb` over the locals region is the LCL-SEED precedent). The γ/ω landings must hand `r13` per § A.1 (γ: the callee's `F.B`; ω: the callee's `F.B0`); at rung 1 every value is 0, so no landing code was touched. The rung-2 witness `p(1). p(2). p(3). main :- p(X), write(X), nl, fail. main.` is the first observable of the log: `X` lives in `main`'s frame, older than `p`'s choice, so its binding is logged and undone on each redo.
