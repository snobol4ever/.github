# ⛔⭐⭐⭐ GOAL-SNOBOL4-100 — SCRIP SNOBOL4 x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

**Opened 2026-08-15 s92 by Lon in-chat (Fable 5 seat), verbatim in substance:** *"We've been at this for 4.5 months and today is the day … reaching MILESTONE 1, the BEAUTY self host demo test. Take a full accounting of every single SNOBOL4-related GOAL-*.md file. ONE GOAL file to bring us all the way home to 100% SNOBOL4. So much of those RUNGS and STEPS are completely obsolete. We basically have one plan moving forward for SCRIP's SNOBOL4 x86/x64: the THREE ZETAS — standing, activation frames, and spine cells. After the consolidation, delete those MD files which have been replaced by the new ONE GOAL. For every item verify against the source to see if the RUNG or TASK even makes sense."*

**This file REPLACES (deleted this session, full text in `.github` git history at `1e5db2cf` and before):** `GOAL-RBP-EARN.md` · `GOAL-SN4-HOME.md` · `GOAL-SN4-HOME-{BOARD,LOWER,RBX,WIRES}.md` · `GOAL-SN4-ZETA-{MECH,CLIMB}.md` · `GOAL-SNOBOL4-BB.md` (+`-ALPHA`,`-OMEGA`) · `GOAL-SNOBOL4-RTX.md` · `GOAL-SNOBOL4-PAT-IR.md` · `GOAL-MILESTONE-1-BEAUTY.md` · `GOAL-LANG-SNOBOL4.md` · `GOAL-PARSER-SNOBOL4.md` · `GOAL-PST-SNOBOL4.md` · `GOAL-SNO-{CLAWS5,TREEBANK-ARRAY,TREEBANK-LIST}.md` · `GOAL-SCRIP-BEAUTY.md` · `GOAL-CSNOBOL4-{FENCE,HARNESS}.md` · `GOAL-CSN-FENCE-FIX.md` · **(s93, text at `1ccb3e59` and before)** `GOAL-ZETA-FOUR.md` · `GOAL-SUBEXPR-ORACLE.md` · `GOAL-DATATYPE-PORTABLE-TESTS.md`. Any reference to one of those names, anywhere in `.github`/SCRIP/corpus, resolves HERE. Their laws that still bind are restated below; their rungs are dispositioned in the LEDGER at the bottom (verified against SCRIP `2a6a76ab`, corpus `20a2387a`, live tree, 2026-08-15). **NOT absorbed (platform, all-language, untouched):** `GOAL-RTCC.md`, `GOAL-MODE34-IDENTICAL.md`, `GOAL-DESCR-TAG-ENCODING.md`, monitor/harness goals, every Icon/Prolog/Snocone/Raku/Pascal/Rebus goal.

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔
**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. ██** Enforcement: every diff is checked for new file-scope definitions; a commit adding one without a cited in-chat grant is REJECTED on sight (precedent: the g_pcall/g_pcall_wires/RT_AB_ANCHOR eradication, s55). **THE ASK ITSELF MUST BE A BANNER** in chat (name, type, owning file, purpose, why registers/stack cannot carry it).

---

## ⭐⭐⭐ THE MODEL — THE THREE ZETAS (design of record; Lon's register ruling s81, verbatim: *"RSP for ZETA SPINE, RBP for ZETA ACTIVATION FRAMES, and RBP for ZETA STANDING AREA"*)

| tier | register | what | α / ω rule | at HEAD `2a6a76ab` (VERIFIED by grep + emitted asm this session) |
|---|---|---|---|---|
| **ζ-SPINE** | RSP | ordinary per-BB storage: box operands, ζ cells, choice/resume records; compile-time-constant offsets, FORTH/LIFO | α allocates · ω self-frees on failure · WHACK frees on FENCE'd or final success | live everywhere; depth model = `zd_plan`/`zvo_resolve` (`src/contracts/zeta_storage.c` + emit.cpp staging) |
| **ζ-STANDING** | RBP | the ROOT activation, pinned for the life of the match: established once at `S ? P` (`IR_MATCH_BEGIN`), holds the CAS MARK (`[rbp-8]`), outer Σ/δ/Δ, retry cursor, reserved RESULT, and every registered SAVE/COND/IMM/ARBNO slot (`frame_slot_scan`, offset `-(64+16*idx)`) | `push rbp; mov rbp,rsp` at α (`bb_match_begin.cpp:48`) · `mov rsp,rbp; pop rbp` whack at MATCH_END/fail tail | authority `emit_match_rbp()` (emit.cpp:2118, S-85 reactivation); slots via `capture_frame_slot()`/`arbno_frame_slot()` (s86/s87 unified registry) |
| **ζ-ACTIVATION FRAME** | RBP | one RBP frame per `*PATTERN_VARIABLE` dereference (`IR_MATCH_DEFER`, `bb_match_defer.cpp:47`, `emit_defer_rbp()`) and — RULED s80(3)/s81, NOT YET LANDED — one per dynamic-K ARBNO **site** that cannot use the frameless-k16 arm; chained LIFO off STANDING (`[rbp+24]` structural parent), self-pop at ω, loop-whack home at any exit that discards suspended frames (FRAME LAW s75) | `push rbp; mov rbp,rsp` at α · self-pop at ω · `x86_arbno_rbp_unwind_at(MARK)` at discarding exits | ONLY MATCH_BEGIN and MATCH_DEFER push rbp (5 template hits, 2 live: `bb_match_arbno_rbp()` never called; `bb_save_restore.cpp:161/213` behind `fnrbp()==1`, unreachable — clamp forces `1→2`) |

**Register contract of record (rest of the file):** RBX = GC heap-top/allocation frontier · R12 = capture-pending arena TOP (mmap'd, stack discipline) · R10/R11 = γ/ω wires (both glue kinds, LIVE — never scratch) · R13/R14/R15 = Σ/δ/Δ match-state · R9 = GVA base (RTCC) · R8 + arg tier = RTCC slots. Full 16-row map + hazards: deleted `GOAL-SN4-HOME-RBX.md` §THE CONTRACT (git `1e5db2cf`), still law.

## ⛔ DEFINITION OF DONE — "100%" MEANS THIS AND NOTHING LESS
1. **`scripts/scorecard_snobol4.sh` META SCORE = 100.0** — every SNOBOL4 program in corpus/ + SCRIP/test (13 suites, 1396 programs) PASS in **BOTH** modes (m3 `--run` BINARY, m4 `--compile`→`gcc -no-pie`), stdout byte-equal to the live x64 `sbl` oracle (or its pinned `.ref`), m3 ≡ m4; XFAIL only where oracle-blessed (manual p.123 stack-overflow class).
2. **MILESTONE 1 RE-EARNED:** `cd corpus/demo/beauty; sbl -bf beauty.sno < beauty.sno` (622 lines) reproduced by SCRIP in both modes, **md5 `abfd19a7a834484a96e824851caee159`** (any delta must be Lon-blessed as an oracle-behaviour change); the 17 `beauty_suite` drivers green both modes.
3. Gates strict and green: TEMPLATE-ONLY + BOTH-MEDIUM greps == 0 · claim gate over {rbx r9 r10 r11 r12 + tiers} · zero r10/r11 scratch (incl. RTX hand asm) · `handoff_status.sh` COMPLETE.
4. Deletions complete: every dead killswitch/arm named in R-7 gone; `grep -c getenv\("SCRIP_ src/` reads only the sanctioned diagnostics list kept in R-7.
5. Re-entry edge inventory closed (R-5): every RBP/R12 restore edge witnessed — backtrack β per choice class · ω unwind · FENCE whack · scanfail · ABORT drain · unanchored retry bump · NRETURN · direct Goto `:<C>` into CODE() · SETEXIT.

## THE INSTRUMENT (one; boards are RUN, never transcribed)
```
bash scripts/scorecard_snobol4.sh run  [--suites a,b] [--jobs N] [--out DIR]   # ~1h/core all 13 suites; container restarts kill it — run detached, resume with --suites into a -part2 dir
bash scripts/scorecard_snobol4.sh report [DIR]                                 # needs gawk (apt-get install -y gawk)
```
Weights (Lon's knob): beauty_self 20 · beauty_suite 15 · demos 15 · benchmarks 10 · bb_probes 10 · patterns 10 · crosscheck 10 · feature_test 5 · probes_misc 5 · csnobol4_suite 5 · gimpel 5 · lon 5 · misc 3. **BASELINE s91 (SCRIP `2a6a76ab`, 10 of 13 suites, `test-results/scorecard-20260815-115505-combined`): META 38.0** — beauty_self 0/1 · beauty_suite 0/17 · demos 5/23 (SIG6/SIG6 ×10) · benchmarks 12/15 of 23 · bb_probes 156/163 of 188 · patterns 68/68 of 122 (SIG6 ×18, SIG11 ×17) · crosscheck 159/178 of 195 · feature_test 130/145 of 160 · probes_misc 51/60 of 112 · csnobol4_suite 21/26 of 90. gimpel/lon/misc not yet run.
Named witnesses live in `corpus/probe/` (`m1/` Milestone-1 root causes · `mrbp/` · `dv/` · `earn0/` · `bb/` BB-template embodiments · `l3/`); every one has an oracle `.ref` beside it. Bug-finding law: MONITOR-FIRST (RULES.md) — `scripts/test_monitor_2way_sync_step_bin.sh` then gdb spin-count breakpoints; cheapest discriminating experiment first (passing sibling vs failing probe, asm diff).

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
MONITOR-FIRST · TEMPLATE-ONLY (`x86(...)` is the only x86 producer) · BOTH-MEDIUM (no `MEDIUM_TEXT`/`MEDIUM_BINARY` gating in templates — the s91 tiny-site `MEDIUM_TEXT &&` conjunct is a standing violation, R-1 lifts it) · SCRIP FOLLOWS SPITBOL SEMANTICS, CASE-SENSITIVE · ONE AUTHORITY per fact (`emit_match_rbp`, `emit_arbno_rbp`, `emit_match_owns_startd`, `frame_slot_scan`… — never spell a fact twice: s68/s70 "spelled-twice disease") · KILLSWITCH BYTE-IDENTITY + COMPILE-TIME MD5 BLAST RADIUS over the corpus for every codegen change (never a single board run: the board's noise floor ≈5 flips green→red) · **A/B ON EMITTER BEHAVIOUR MUST SWAP `out/libscrip_rt.so`, NOT THE DRIVER** (the emitter lives in the `.so`; s68 vacuous-gate conviction) · GATE-BEFORE-LAND · VERIFY-INHERITED-BLOCKERS (re-test any "X is unavailable" with one command; gdb-404 cost 7 sessions) · CHAT-ESCALATION (any blocker only Lon can clear goes into chat as one question at the TOP of the reply; ≥2 zero-rung sessions ⇒ ask before code) · END-OF-CONTEXT LAW (do not open a hunt or deletion you cannot close; mint the repro, route it, stop) · REGENS ×3 after codegen touches (`util_regen_{benchmark,feature,demo}_s_artifacts.sh`) · O2-DIRECTED-ONLY · CONTAINER LAWS: backgrounded jobs die (build in foreground under `timeout`), `bash scripts/install_system_packages.sh` FIRST, clone `x64` oracle ALWAYS.

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh && rm -f scrip && timeout 1500 make -j8 scrip > /tmp/build.log 2>&1 && make libscrip_rt && ls -la scrip out/libscrip_rt.so   # ~160s
timeout 8 /home/claude/x64/bin/sbl -b corpus/probe/m1/m1_alt_arm2_cap.sno   # oracle alive?  → "b"
```

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-15 s111 (Claude Opus 5) — **THE MONITOR BLOCKER IS CLOSED. THE 2-WAY IPC SYNC-STEP MONITOR NOW SYNCS THROUGH A DEFINE AND REPORTS A REAL FIRST DIVERGENCE.** SCRIP `5ee5e112` + `33f40528`.

**⭐ WHAT LANDED (two commits, both gated, m4 TEXT byte-identical throughout):**

**(1) `5ee5e112` — THE BOTH-MEDIUM LIFT (the root cause; default-ON, no killswitch needed beyond the existing `SCRIP_DEFINE_FOLD=0`).** `bb_goto_dyn.cpp:21`'s DEFINE-fold arm carried a **`&& MEDIUM_TEXT`** conjunct — the standing BOTH-MEDIUM violation this goal file's LAWS line names. BINARY therefore fell to the `rt_goto_transfer` arm, which **resolves and RUNS the body NESTED inside a C frame**; the body never entered at its own α, never reached the RETURN floater at site depth, and the site's `{γ,ω}` pair went unconsumed and was re-read as save-set data. **MEASURED: floater arrived at `0x…9910` vs pair at `0x…9a90` = exactly 0x180 (384B) = `rt_goto_transfer` + `rt_chain_enter` callee-saves.** Fix rides the existing R-1 s94 mechanism — `x86_jmp_via_cell` is medium-complete (TEXT = `lea rax,[rip+label]; jmp rax`, *byte-identical to what it replaces*; BINARY through `body$<FN>`, which the m3 driver fills at main seal, `scrip.c:1670` LBL__ loop — **filled under GVA-off too, which is precisely why s110's `alpha$<FN>` route could not serve here**). GATES: m4 TEXT 15/15 `.s` byte-identical; **bb_probes A/B BY SET vs a stash-built baseline: 188/188 rows IDENTICAL, both modes, ZERO movers** (the 3 non-PASS are the pre-existing FENCE class `t6m`/`f6d`/`fence_probe`, m3≡m4 RC1, s95 R-4(a) residual).

**(2) `33f40528` — THE PAIR-PUSH COMPLETION (opt-in `SCRIP_SLIM_PAIR=1`; default emission byte-identical, 13/13 `.s`).** s110 patched only the two `open_slim` tails. **The arm GVA-off actually reaches is the ZD twin's NON-SLIM FALLBACK (~`:446`)** — `MONITOR_BIN=1` ⇒ `n_gva_m3=0` ⇒ the slim tail declines and the site falls through to `rt_proc_call_open` with flat rcx/rdx wires and no pair. Patched there and on the legacy flat-glue arm (~`:724`). Witness `probe/mon/mon_define_call_min` under `MONITOR_BIN=1 SCRIP_SLIM_PAIR=1`: **SIG11 → runs to completion.**

**⛔ THREE s110 CLAIMS FALSIFIED — DO NOT INHERIT ANY OF THEM (each cost this seat a measurement to disprove):** (a) *"under SLIM_PAIR=1 the m3 crash moves to BEFORE `rt_proc_call_open_slim`"* — FALSE, the breakpoint fires normally. (b) *"binary `lea` of a forward local label is the divergence; the fix starts in the encoder"* — FALSE, the pair-push emits **correctly** in the m3 slab (verified by breakpoint: γ=`0x…13b3` ω=`0x…1436`, rsp lands exactly at the pair). `x86_lea_rip_id` is sound. (c) *"m4 never gates GVA on MONITOR_BIN"* — FALSE, `scrip.c:1708` gates it identically to m3; m4 nonetheless keeps the TINY arm under MONITOR_BIN, so m3/m4 arm selection is **not** apples-to-apples in that regime.

**⭐ THE INSTRUMENT IS LIVE — `bash scripts/test_monitor_2way_spitbol_vs_run.sh <f.sno>` with `SCRIP_SLIM_PAIR=1` exported.** On the witness it now syncs through the DEFINE and prints the divergence table directly:
| step | spl | scr |
|---|---|---|
| 6 (last agreement) | `@2 VALUE F = INT=5` | `@2 VALUE F = INT=5` |
| **7 (DIVERGE)** | `@2 RETURN F (RETURN)` | `LABEL stno=INT=5` |

**⭐ NEXT SEAT STARTS HERE — step 7 is TWO DISTINCT DEFECTS, not one. ⛔ CORRECTING THIS SEAT'S OWN FIRST READING: it is NOT a wrong-continuation bug.** `scr` printed `R=`, so statement 4 **completed normally** — the γ continuation is correct. What is wrong is (a) the result is null and (b) the RETURN tap never fired. Both are now located:

**(a) RESULT NOT MARSHALLED ON THE NON-SLIM γ LANDING.** `rt_call_named_proc_sl` (rt.c ~`:2018`) is the reference embodiment for what a return must do: `DESCR_t *rcell = rt_call_fastpath_ok() ? p->rcell : 0; result = IS_FAIL_fn(fret) ? FAILDESCR : (rcell ? *rcell : NV_GET_fn(name…))` — i.e. **the result is fetched from the proc's RESULT CELL, by name under GVA-off.** The TINY landing has its own path (FRQ slots + RESTORE4); the fnrbp2 floater marshals **nothing** (`x86_alpha(); pop rcx; add rsp,8; jmp rcx`); and the ZD-twin non-slim γ landing (`bb_call_proc_staged.cpp` ~`:447`: `def L(3)` → `call rt_proc_call_epilogue_γ` → `jmp L(2)`) has **no twin of that fetch**, so it lands with a null result. Fix rides the existing cell/NV machinery — no new global.

**(b) ⛔ THE RETURN TAP EXISTS BUT THE GVA-OFF ARM NEVER REACHES IT. ⛔⛔ CORRECTING A FALSE CLAIM THIS SEAT BRIEFLY WROTE HERE (caught by re-grepping the WHOLE tree instead of just `rt.c` — do the same to anything below):** it is **NOT** true that "no compiled arm emits a RETURN tap". `grep -rn mon_emit_return_bin src/` gives FOUR hits, not two: definition (`rt.c:1175`), the interpreted call site (`rt.c:2020`), the decl (`core.h:228`), and — **the one that matters — a real compiled fire-point at `src/templates/bb_func_activate.cpp:335`**, which loads the sealed fname into rdi and the result pair from `RDQ("rsi", AB_OFF_RES0/RES1)` and calls the tap, guarded by the `je L(5)` above it. So the tap is emitted on the **AB ACTIVATION** path only. The ZD-twin non-slim arm the GVA-off site falls to bypasses that path entirely, which is why step 7 shows no RETURN event from `scr`. **This reframes (a) and (b) as very likely ONE defect, not two:** `bb_func_activate`'s tap reads the result from `AB_OFF_RES0/RES1` — the same activation-record result slots the non-slim γ landing fails to marshal from. Next seat: determine whether the GVA-off site *should* be routed through the AB activation path (making both symptoms vanish at once), or whether the non-slim γ landing needs its own result fetch + tap twin. Measure which before writing code — `bb_func_activate.cpp:335` is the working reference embodiment for both halves.

Gate ladder once both are green: witness both regimes → harness syncs to END → bb_probes by set both modes → then flip `SCRIP_SLIM_PAIR` default ON with full md5 radius + REGENS ×3 (owed only for the default flip; **not** owed for s111's two commits, whose default bytes are unchanged).

**SETUP TRAPS RE-HIT (6th+ session — someone should fix the scripts):** process substitution `<(...)` fails under `sh` (pipe to files); `sort -t$'\t'` fails under `sh` (use `TAB=$(printf '\t')`). **NEW TRAP, cost this seat a false trace:** `nexti` in the m3 slab silently mis-steps (no CFI) and reported a `push %rcx` as transferring to `_rtld_global` — **use explicit `break *ADDR` in slab code, never `nexti`.** gdb pending breakpoints need `set breakpoint pending on` before the `break` lines.

⛔ **TWO RULINGS STILL OWED BY LON, BOTH UNCHANGED AND BOTH BLOCKING R-2:** (i) default the `SCRIP_NRET_CAP=1`+`SCRIP_DYN_ALPHA=1` pair? (ii) the oracle md5 — live `sbl` gives `9cddff25…` (622 lines), NOT the pinned `abfd19a7…` in DoD 2; Milestone 1 cannot be declared either way until this is ruled. ⛔ **PUSH: SCRIP `5ee5e112`+`33f40528` COMMITTED LOCALLY; credential asked in chat at session end (s111 asking now).**

## LIVE CURSOR (superseded) — 2026-08-16 s110 (Claude Fable 5) — **THE 2-WAY IPC SYNC-STEP MONITOR'S ONE BLOCKER IS NAMED, MINIMISED TO 4 LINES, AND ITS FIX SPEC IS MEASURED FROM THE FLOATER'S OWN CONTRACT. NO CODE LANDED — one falsified patch honestly reverted; witnesses corpus `72245220` (`probe/mon/`).** ⚠ **s111 NOTE: three of this cursor's conclusions are FALSIFIED — see the s111 cursor above before acting on anything below.**

**LON'S DIRECTIVE THIS SEAT (verbatim in substance):** *"You will never get beauty.sno working without the sync-step monitor... For VARIABLE tracing flip the switch to use the NV_* functions versus the GVA optimization; for LABELS generate code at BB_STATEMENT_BEGIN. You will be implementing the 2-way IPC binary communication SYNC-STEP monitor using SPITBOL."* ⭐ **BOTH MECHANISMS ALREADY EXIST AND MATCH:** `MONITOR_BIN=1` forces `n_gva_m3=0` (scrip.c:1560/1708, the MON-RE-M3 s16 gate) so every SET rides NV_*/rt.c where `mon_emit_value_bin` taps; the LABEL tap is `bb_statement.cpp:14` at IR_STATEMENT_BEGIN, RTCC-veneered, both media (verified in emitted m4 asm). The harness (`PARTICIPANTS="spl scr" test_monitor_2way_spitbol_vs_run.sh`) **WORKS TODAY on non-DEFINE programs** — measured: 3-statement probe syncs 7 steps to END, both stdout equal. SPITBOL side (x64 fire-points b_vrs/bpf09/retrn + `monitor_ipc_bin_spl.c`) is live in the shipped `sbl`.

**⛔ THE ONE BLOCKER — witness `probe/mon/mon_define_call_min.sno` (4 lines, oracle R=5):** any DEFINE-bearing program crashes the scr participant. MEASURED CHAIN, every link gdb'd: (1) `MONITOR_BIN=1` ⇒ GVA off ⇒ the emit-time SCC gate (`bb_call_proc_staged.cpp:176` requires `g_gva_active`) AND the role-4 TINY shim (`bb_save_restore.cpp:71` scc probe; formals live in GVA cells) both decline ⇒ call sites fall to a non-TINY arm; (2) that arm enters the body having pushed NO continuation record; (3) body runs (taps fired stno 1,3,4 then 2; NV lookups reached libc); (4) `:(RETURN)` hits the fnrbp()==2 RSP-ONLY floater whose contract is **"pop {gamma,omega} pair at TOS"** (bb_save_restore.cpp role-1 s64 arm) ⇒ pops enclosing-frame bytes ⇒ `jmp rcx` junk ⇒ **rip = `_rtld_global` (ld.so DATA) — THE s103 omega_driver SIGNATURE, now with a 4-line instrument.** `SCRIP_NO_TINY=1` alone (GVA ON, no monitor) reproduces the identical rip ⇒ the class is ARM-SELECTION, regime-independent; MONITOR_BIN merely forces the broken arm. m4 unaffected (never gates GVA on MONITOR_BIN; m4+taps runs def1 clean).

**⛔ FALSIFIED THIS SEAT — DO NOT INHERIT EITHER THEORY:** (a) "rt_define_site receives a mis-lea'd slab base" — gdb: `SET_FN F fn=0x..400000` is the DRIVER's own seal value; F's chain is emitted FIRST and its entry genuinely IS slab base; rt_define_site arrives with the SAME value, `redefined` never set. (b) "prefer the sealed alpha$<FN> cell in rt_define_site" — REJECTED MEASURED: `bb_ab_slot_for` stub-initializes fresh cells NON-NULL (`bb_func_activate.cpp:69` → `rt_ab_undef_fn_stub`, so a naive non-null consult swaps m4's CORRECT fn for the Error-22 stub — observed), and under GVA-off the shim never emits so `<FN>_alpha` is never interned and `m3_seal_entry_cells` silently misses (`scrip.c:449` off<0 return) — the cell is stub in exactly the regime that needs it. Patch written, gated red, REVERTED (`git checkout rt.c`); tree is clean at SCRIP `1d98f9d2`.

**⭐ FIX SPEC (small, fully measured contract on both ends — next seat lands it):** make the non-TINY call arms speak the floater's pair protocol: before the transfer, push the site's ω landing then its γ landing (pair at TOS, `[rsp+0]=γ [rsp+8]=ω` per the s64 floater comment); γ/ω landings release their own record. The TINY record (`bb_call_proc_staged.cpp:605-614`: `{K}{L(2) at +16/+24}{actuals at +32+16i}` then `jmp [alpha$cell]`) is the working reference embodiment. TWO arms need it: the classic-SCC tail (`:648+`, the `rt_proc_call_open_slim` + `jmp rax` site — its lea'd r10/r11 wire seating stays for blob-exit spellings) and the legacy fall-through outside the lambda (UNREAD this seat — read it first). Gates: `probe/mon` witness PASS under `MONITOR_BIN=1` AND `SCRIP_NO_TINY=1`; harness def1 syncs to END; bb_probes 185/188-class census BOTH modes by set, killswitch byte-identity, md5 radius (codegen touch ⇒ REGENS ×3 owed). Then: harness on `probe/nret/nret_cond_defer_call.sno`, then semantic_driver, then beauty 1-line input — **the beauty m4 first divergence is already characterized this seat: line 8, `Parse Error` vs `START`; the whole `*Parse` grammar Error-22s at match time (bisection probe measured: grammar BUILDS as PATTERN in both, dies at match), the nret deferred-call class (3 witnesses m3-PASS/m4-FAIL, controls green).** Side finding banked: unset-variable `DATATYPE` divergence m4 `Space=NULL` vs oracle `STRING` (mint a probe before ruling).

**⭐ SECOND HALF OF s110 (Lon: "All your choices") — THE PAIR-PUSH IS WRITTEN, OPT-IN, AND BYTE-INERT (SCRIP `860a9e43`); THE RESIDUAL IS MEASURED TO ONE QUAD AND ONE SUSPECT.** Both `open_slim` tails in `bb_call_proc_staged.cpp` (the ZD twin at ~:402 — the arm def1 actually takes, proven by its `g_call_args` install spelling in the live slab disas — and the classic tail at ~:664) now push ω-landing then γ-landing before `bb_glue_pass_wires_blob(6,7)`, behind **`SCRIP_SLIM_PAIR=1` OPT-IN** (default emission byte-identical to pre-change baseline, `cmp` on m4 text — zero radius, zero REGENS debt, which is why it could land without the census). Landings L(6)/L(7) unchanged: the floater consumes the 16 so they arrive at today's post-carve depth. The shared glue itself deliberately untouched — PAT$ blobs consume wires, never the floater, and must not inherit a pair.

**⛔ MEASURED RESIDUAL — the witness is STILL RED under the pair (m3, both regimes), and the numbers say why it isn't sufficient yet:** site post-carve rsp S=`0x..9978` (open_slim entry+8); fault rsp `0x..9970` = S−8, i.e. the floater entered at **body-entry−8: one orphaned quad** (the classic shape: a `call` whose callee exits via jmp, orphaning its return address). At fault, `rcx = _rtld_global` (the popped "γ"), and the stack dump shows the pair's slots holding foreign values (`[S-16]=0x41ad68` a scrip static, `[S-8]=0`) — ⛔ **PRIME SUSPECT: a BOTH-MEDIUM divergence — the pair pushes are verified in m4 TEXT only; the m3 BINARY emission of the same site may take yet another spelling or drop the L() lea/push (x86_push has a MEDIUM_BINARY arm at x86_asm.h:269, but the L-label lea backpatch in binary is unverified).** NEXT SEAT, in order: (1) verify the pair exists in the m3 SLAB (gdb `x/6i` at the site under `SCRIP_SLIM_PAIR=1` — the site is findable by breaking `rt_proc_call_open_slim`); (2) find the +8 orphan (candidates: a `call`-spelled transfer on the m3 path — `n1_goto_deferred` computed-goto machinery, or the statement β trampoline); (3) with both closed, run the gate ladder written in the first half (witness both regimes → harness def1 → bb_probes census both modes by set → flip `SCRIP_SLIM_PAIR` default ON with full radius + REGENS ×3). ⛔ **STEP (1) EXECUTED AT 92% CONTEXT, ANSWER IN HAND: under `SCRIP_SLIM_PAIR=1` the m3 crash moves to BEFORE `rt_proc_call_open_slim` (the breakpoint that fired in every prior run never fires) — the BINARY emission of `x86("lea","rcx",L(n))` for a FORWARD LOCAL LABEL is itself the divergence: it corrupts/reroutes the site's binary emission, while the same source is correct in TEXT.  The fix therefore starts in the encoder (x86_asm.h lea local-label backpatch arm), not in the template.**  The m4-text A/B baseline is banked at the witness (`SCRIP_NO_TINY=1 --compile` of `probe/mon/mon_define_call_min.sno`).

**SETUP NOTES THIS SEAT:** scorecard beauty suites reproduce s109 exactly (16/17 m3 · 15/17 m4; beauty_self SIG11/DIFF). `view` tool served stale reads on /tmp files — trust `sed -n` output. gdb pending breakpoints need `set breakpoint pending on` BEFORE `break` lines in -x scripts. ⛔ **PUSH: corpus `72245220` + this cursor COMMITTED LOCALLY; credential asked in chat at session end.**

## LIVE CURSOR (superseded) — 2026-08-16 s109 (Claude Opus 5) — **AI-3 LANDED AND MEASURED: THE ARITH FUSE IS DEFAULT ON. `arith_int` 1.166× · `arith_mixed` 1.141× · `arith_str` 0.994× — ALL THREE INSIDE BANDS PRE-STATED BEFORE THE MEASUREMENT. ZERO MOVERS OVER 327 GATE ROWS.** SCRIP `1d98f9d2`, corpus `e5370451`.

**⭐ WHAT LANDED — AI-3 OF `DESIGN-SN4-ARITH-INLINE-AND-DT-BITS.md`, ON LON'S IN-CHAT "All your choices. I'm with you on this."** The fuse was WRITTEN s21x behind `SCRIP_DEF_ARITH_FUSE=0` as a play switch and **never measured** — this seat measured it and defaulted it ON. Define **INVERTED, NOT DELETED** (`=0` restores the prior emission, proven byte-identical). ⭐ **THE 4-WAY EXPLOSION LON ASKED ABOUT IS DODGED BY CONSTRUCTION — 2 one-bit tests, 3 arms:** `and edx,ecx ; cmp edx,DT_I` ⇒ BOTH-INT; `and edx,DT_NUMERIC_BIT ; jz` ⇒ BOTH-NUMERIC; else `L(0)` = the pre-existing rt call verbatim (it alone owns strings, blank-padded numerics, null→0, DT_DATA, left-first error identity). **i-r/r-i/r-r collapse into ONE arm** because each operand is normalized independently (`cmp DT_R / je / cvtsi2sd`) — the decision is LINEAR IN OPERANDS, never quadratic in tag pairs. Rests on the landed DT_t law (NUMERIC=bit0, REAL=bit2), exhaustively valid incl. all 64 `DT_DATA` types.

**SIZE, MEASURED FROM EMITTED ASM (Lon's "depends on how big"):** hot both-int path **12 instructions**, vs 17 + `call rt_add@PLT` + `rt_add`'s own 9. Whole block **41 instructions** across all three arms. ⭐ **A WIN THE DESIGN DOC DID NOT COUNT: the fast arms exit the first γ with NO rtcc veneer** — 3 stores + 4 reloads deleted — because they touch only rax/rcx/rdx/rsi/rdi/xmm0-2 and never the r8-r11/xmm8-15 VM claims.

**GATES (printed values).** `bb_probes` 185/188 both modes · `beauty_suite` 16/17 m3, 15/17 m4 · `patterns` 107/122 m3, 106/122 m4. ⛔ **BY-SET ROW DIFF OF ALL 327 ROWS, FUSE=1 vs FUSE=0: ZERO MOVERS** (aggregate equality alone was not accepted — offsetting movers were checked for explicitly). Re-proven post-REGENS. **Killswitch `FUSE=0` emits bytes identical to the pre-change baseline** (`cmp` on the emitted `.s`). REGENS ×3 RAN (benchmark 23 files, feature 28, demo). A/B swapped `out/libscrip_rt.so`, NOT the driver.

**FALSIFICATION, AIMED AT THE JOINT RULE.** `/tmp/ap/jr.sno` shape, 20 cases: `DATATYPE` on all four tag combinations (ii→INTEGER, ir/ri/rr→REAL) plus string, blank-padded `' 3 '`, and null-string operands. **Oracle-IDENTICAL.** The probe is output-sensitive to a mis-tagged real arm by construction, so it can fail — that is why it is the instrument. ⛔ **NOT YET BANKED INTO `corpus/probe/` — next seat should mint it as `probe/ai/` with its `.ref` before building on this rung.**

**I2D_MAGIC MEASURED AND LEFT AT 0 — REPORTED AS NULL, NOT AS A WIN.** magic splice 863ms vs `cvtsi2sd` 887ms = **1.028×**, which is BELOW this rail's own documented **1.10 trust floor** (`bench_min_of_n.sh`: no two-arm number under ~1.10× is trustworthy). Direction is OPPOSITE the doc's pre-stated prediction, but the magnitude is not quotable ⇒ **the prediction is neither confirmed nor falsified.** Kept at 0 on SIMPLICITY grounds (1 insn vs ~9, burns no labels), NOT on speed grounds.

**⛔ FINDING FOR LON — A LIVE ORACLE DIVERGENCE, PRE-EXISTING, UNFIXED, AND NOW CHEAP TO FIX.** Manual Ch.19 error list: SPITBOL raises **#3 addition / #34 subtraction / #28 multiplication / #11 negation / #14 division / #17 exponentiation caused integer overflow**. **NEITHER path tests OF** — not the inline `add`, not `rt_add`'s `lea` — so SCRIP silently WRAPS where SPITBOL errors. This is a wrong-answer defect independent of the fuse, sibling of the s207 no-zero-divisor-test defect at AI-5. ⭐ **The inline arm makes it CHEAPER to fix, not harder: a `jo` after the `add`, statically never-taken, costs ~nothing on the hot path, whereas behind a call you pay the call regardless.** Deliberately NOT folded into this rung — never grade two changes in one measurement.

**⭐ MEASUREMENT CONTEXT THE NEXT SEAT SHOULD WEIGH BEFORE EXTENDING AI-4/AI-5.** s108's SCRIP-vs-SPITBOL board already had arithmetic as SCRIP's STRONGEST area (`arith_mixed` 0.24, `arith_int`/`arith_loop` 0.25 — ~4× faster than the oracle) and tables/string-mutation as the losses (`mixed_workload` 5.56, `table_churn` 4.86, `string_manip` 3.24, `table_access` 2.56). This rung's 1.166×/1.141× is real and gated, but **it made a strong area stronger.** The measured frontier is still TABLE and STRING MUTATION.

**⭐ SIDE FINDING — s108's THREE OPEN PROBE MOVERS ARE PRE-EXISTING AT HEAD.** `f6d`, `t6m` and `fence_probe` come back **RC1/RC1 in BOTH fuse arms** on this seat's scorecard path, so they are not attributable to the s108 default flip. ⚠ This is the scorecard path, NOT s108's census path — s108's ask ("reproduce fence_probe m4 THROUGH THE CENSUS PATH before theorizing") is still open and this does not close it, but it does agree with s108's own hand-rolled rc=1.

**NEXT SEAT STARTS HERE:** (a) Lon's standing direction is still **the beauty self-host** (R-2) — untouched by this seat; note `beauty --compile` SKIPs for lack of `beauty.sc`. (b) bank the joint-rule probe into `corpus/probe/ai/`. (c) the overflow guard as its OWN rung with its OWN measurement (AI-5 territory: `jo`→error for add/sub/mul, then the div zero-test and `INT64_MIN / -1`). (d) `omega_driver`, the last board fail, R-0 port-less-bomb. (e) AI-4 real-arm and AI-6 COERCE 4×4 table remain open, but see the frontier note above. ⛔ **PUSH: SCRIP `1d98f9d2` and corpus `e5370451` COMMITTED LOCALLY; credential asked in chat at session end.**

## LIVE CURSOR (superseded) — 2026-08-15 s108 (Claude Opus 5) — **THE BOARD MOVED: beauty_suite 14/17 → 16/17.** PB-1s is FIXED at the root (per-construction snapshot borne by the `DTP_t`) and BOTH of Lon's owed rulings were given in-chat and acted on. SCRIP `2aa01198`, corpus `0ae28cca`.

**⭐ WHAT LANDED (1) — PB-1s PER-CONSTRUCTION SNAPSHOT.** s107's diagnosis was correct and is now cured. `DTP_t` gains APPENDED `snap`/`nsnap` (fn@0 and zsz@16 static asserts intact); `SNO$MKPAT` freezes that site's `$V<i>` values into the FRESH per-construction `DTP_t` (count threaded from lowering as `args[1]`, spliced `nl→ncnt→mk`); the blob preamble (`emit.cpp:2755`) stores its entry `rdx` into the frame's 16B-parity pad `[rbp-24]`; `$V` leaves slot-read **their own construction's** frozen value (`rt_patv_defer_get_pat_dtp` / `rt_patv_defer_open`). The `PAT$n$V<i>` globals degrade to stage-2 marshalling whose lifetime ends at MKPAT — exactly manual p.85-86. EVERY blob-entry site now carries the DTP in `rdx`: the GVA arm RE-DERIVES it after `dtp_fn_of` clobbers rdx, and the non-GVA + MATCH_VALUE sites use new DTP-returning twins. ⚠ **ONE TRAP, MEASURED THE HARD WAY:** `dtp_fn_of` LAZILY COMPILES recipe composites (`!fn && rcp` arm) — the first cut of the twins returned the DTP without materializing `fn`, and `pb_ctl_selfref_noalt_ok` went red instantly. All three twins now materialize-then-return. The per-site fn cache was reworked to hold the DTP (write-once ⇒ the DTP is as fixed as the fn) and is DISABLED for `$V` leaves, which are per-construction BY DEFINITION.

**⭐ ANSWERS TO s107's THREE GATING QUESTIONS (all measured, none inherited).** **(1) YES** — the blob can reach its `DTP_t`: `[rbp-24]` is FREE in blob frames (`blob_frame_bytes()` = 24 fixed + 16×candidates; the `[rbp-24]=outer_δ` in emitted asm is the DIFFERENT MATCH-RBP frame), and `$V`-bearing blobs ALWAYS have the frame (an interior DEFER is a wire-clobberer). On the GVA path `rdx` already held the DTP at `jmp rax`. **(2) GC SAFE** — `rt_ws_alloc` is the HB_WS workspace island: base-pinned, never collected, conservatively root-scanned `[g_wsi_base,g_wsi_ws)` every collect, so held DESCRs (patterns holding patterns) are roots by construction. **(3) CLEAN** — zero `sizeof(DTP_t)` consumers outside `pattern_match.c`; appending is sanctioned by the s150 comment.

**⛔ WHAT LANDED (2) — LON'S TWO RULINGS, GIVEN IN-CHAT THIS SESSION, ENDING A ~5-SESSION BLOCK.** **(i) "I have no clue of your switches. If they need to be on, then turn them on."** MEASURED FIRST (semantic_driver PASSES with the pair on, rc134 abort with it off), THEN defaulted: `SCRIP_NRET_CAP` (`bb_match_capture.cpp:57`) and `SCRIP_DYN_ALPHA` (`rt/rt.c:865`) now default **ON**; killswitches INVERTED, NOT DELETED (`=0` restores the pre-s108 default). **(ii) the oracle md5 mismatch: "I could not care less about a mismatch."** The `abfd19a7…`-vs-`9cddff25…` disagreement is CLOSED as a non-blocker — do not re-raise it, and do not let it gate Milestone 1. ⭐ **LON'S DIRECTION FOR THE NEXT SEAT, VERBATIM: "We are doing again the beauty self host."**

**GATES (printed values, per the s50 FACT RULE).** `probe/pb` **9/9** m3 with m3≡m4 (both SIG11s cured, the silent stale cured, all three convicting controls still green, plus the s106 witnesses). bb probes census **166/166 AGREE, 0 SEGV, 0 HANG both modes** (PB-1s fix alone). `crosscheck/patterns` A/B **ROW-IDENTICAL, zero movers**. beauty A/B census: **EXACTLY ONE mover — case_driver, SIG11 → PASS both modes.** Board m3 after both changes: **16/17** (only `omega_driver`, rc139, the R-0 port-less-bomb class). REGENS ×3 RAN (feature, demo, benchmark; 7 files/111 insertions).

**⛔ NOT REGRESSION-FREE — THREE PROBE MOVERS FROM THE DEFAULT FLIP, OPEN AND UNCLOSED.** Stated plainly rather than buried: after defaulting the pair, the bb probes census went 166/166 → **165/166 AGREE with one m4 SEGV**. Four-arm A/B attributes them: **`f6d`** and **`t6m`** change output under `SCRIP_DYN_ALPHA=1` ("d: FAIL"/"t6m FAIL" → EMPTY first line; whether empty is better or worse is UNJUDGED); **`fence_probe`** is m3-clean in all four arms and SEGVs in **m4 only** — ⚠ and a hand-rolled m4 build could NOT reproduce it (rc=1 in both arms, a different build path from the census's), so the attribution is the census's, not a reproduction. ⛔ **NEXT SEAT: reproduce fence_probe m4 THROUGH THE CENSUS PATH before theorizing.** These were left open deliberately at ~90% context (END-OF-CONTEXT LAW) — the killswitch `SCRIP_DYN_ALPHA=0` is the one-flag revert if Lon prefers the old default.

**⭐ BENCHMARKS: INSTRUMENT REBUILT AND RUN — SCRIP vs live SPITBOL, 19/23 GATED CLEAN.** The gate was broken (every row failed INCLUDING SBL-against-its-own-`.ref`) because `.ref` holds only the deterministic line while the programs also print a varying `ms:`. FIX: strip `^ms:` before comparing, and compare each engine's OWN self-reported `ms:` (execution only — scrip wall-clock unfairly includes COMPILE). RESULT (ratio = SCRIP/SBL, <1 = SCRIP faster): **SCRIP faster on 10, slower on 8.** Faster: `string_concat` 0.06, `arith_mixed` 0.24, `arith_int`/`arith_loop` 0.25, `op_dispatch`/`func_call`/`func_call_overhead` 0.26, `fibonacci`/`var_access` 0.27, `pattern_bt` 0.94. **SLOWER — THE HONEST HALF:** `pattern_bt_deep` 1.13, `arith_str` 1.27, `table_access` 2.56, `roman` 3.22, `string_pattern` 3.14, `string_manip` 3.24, `table_churn` 4.86, `mixed_workload` 5.56. ⛔ **THE SHAPE: SCRIP wins ~4x on arithmetic/call/dispatch/concat and loses 2.5-5.5x on TABLE and STRING-MANIPULATION workloads — tables and heavy string mutation are the optimization frontier, not patterns.** ⚠ 4 rows ungated: `eval_dynamic`/`eval_fixed` SEGV in BOTH killswitch arms (PRE-EXISTING, not this seat's), `cap_imm_nret2` moves rc134→rc139 under the pair-on default (SIGNAL CHANGED, both are failures — open), `indirect_dispatch` output≠ref with no crash. None of the four is attributed to PB-1s by measurement; that attribution is UNDONE.

**NEXT SEAT STARTS HERE:** (a) Lon's direction — the beauty self-host; note `beauty --compile` currently SKIPs for lack of `beauty.sc`. (b) `omega_driver`, the last board fail, R-0 port-less-bomb (`bb_emit_end: unresolved forward reference`). (c) the three probe movers above. (d) rebuild the benchmark gate, then compare to SPITBOL properly. ⛔ **PUSH: SCRIP `2aa01198` and corpus `0ae28cca` COMMITTED LOCALLY; credential asked in chat at session end.**

## LIVE CURSOR (superseded) — 2026-08-15 s107 (Claude Opus 5) — **case_driver's ROOT CAUSE IS NAMED, MINIMISED TO 10 LITERAL-ONLY LINES, AND FINGERPRINT-MATCHED TO THE DRIVER; THE "EAGER CALL" LABEL CARRIED SINCE s98 IS FALSIFIED; A SECOND, PREVIOUSLY-UNSEEN SILENT-WRONG-ANSWER DEFECT OF THE SAME CAUSE IS WITNESSED. NO CODE CHANGED — diagnosis + witnesses only (corpus `probe/pb/`, 5 files).**

**THE ROOT CAUSE — PB-1s PER-SITE SNAPSHOT CELL.** A pattern-valued statement that needs an MKPAT blob (**an ALT alone is enough to force that route**) has every plain variable reference snapshotted into a hidden global named per **LOWERING SITE**: `PAT$<blob>$V<i>`, minted at `lower_snobol4.c:2222` and `:2504` (`"%s$V%d"`). The built pattern DEFER-reads that global **by name at match time**. Manual **p.85-86** (verbatim: *"NPAT captures the value of variable N at the time of pattern construction"*; only unary `*` defers) requires each **construction** to freeze **its own** values. One cell per SITE cannot do that. ONE cause, **TWO failure modes**: **(1) STRUCTURAL CYCLE → SIG11** when the statement is SELF-REFERENTIAL (`mk = mk (...)`) and the site runs twice — iteration 2 stores into `PAT$1$V0` a pattern that itself defers to `PAT$1$V0`, so match-time recursion is unbounded (~67MB of stack before the guard page). **(2) SILENT WRONG ANSWER, exit 0** — same cell reused across constructions simply goes stale. Mode (2) was MASKED: the cycle crashed such programs first.

**⛔ THREE CORRECTIONS TO THE INHERITED s106 CURSOR (each measured, not argued).** (a) **WRONG MINT SITE NAMED.** s106 points at `PATV$k` / `static int g_snapctr` (~1998/2011). The witnesses use the *other* path, `%s$V%d` at `:2222`/`:2504`; the monitor named the cell directly (`PAT$1$V1`). Same per-site shape, different code. (b) **WRONG INGREDIENTS NAMED.** s106 says "an ALT arm holding a snapshot-deferred plain ref". **NO PLAIN REF IS REQUIRED** — `mk = mk ('X' | 'y')`, literals only, crashes. The real ingredients are exactly three: an **ALT** (whose ONLY role is to force the MKPAT blob route), **SELF-REFERENCE** of the assignment target, and the **SAME SITE EXECUTED TWICE**. (c) **THE UNEXPLAINED PART IS EXPLAINED.** s106 flagged "the failure is a CRASH, not merely aliasing, so there is more there than a stale value" — the "more" is that self-reference *closes a cycle* instead of merely going stale.

**⭐ case_driver IS THIS CLASS — FINGERPRINT MATCH.** `case.sno:23` is the witness shape verbatim: `icase = icase (upr(letter) | lwr(letter))   :(icase)` — self-reference + ALT + goto loop to the same site. `case_driver.sno` and the 10-line literals-only witness both die at **`rsp=0x7ffffbffeff0`** with an unwalkable backtrace — the same exhaustion depth to the byte. ⛔ **THE `upr()`/`lwr()` CALLS IN THE ARMS ARE INCIDENTAL.** s98, s105 and s106 each labelled case_driver an "eager call" defect and three sessions chased the calls; the minimal witness contains no call anywhere. (The s105 "three load-bearing ingredients" and the s106 "snapshot-deferred plain ref" were both fitted to call-bearing reductions.)

**⛔ WHY NO FIX LANDED — AND A SELF-CORRECTION, HELD TO THE SAME STANDARD APPLIED TO s106 ABOVE.** The frozen value must travel **with the constructed object** (per-construction operand cells). ⚠ **CORRECTION, MEASURED LATE IN s107:** earlier in this same session I wrote that the pattern value is *"a bare wrapped function pointer with no construction-time storage whatsoever."* **That is wrong and must not be inherited.** `SNO$MKPAT` (`by_name_dispatch.c:6879`) calls `dtp_wrap_fn_sz` → `dtp_new` (`pattern_match.c:36`), which **heap-allocates a FRESH `DTP_t` on every MKPAT call**: `DTP_t { fn, rcp, zsz, zstatic, zpad }` via `rt_ws_alloc`, where **`rcp` is a `dtp_rcp_t *` recipe-tree slot that the MKPAT path passes NULL** (recipe node constructors already exist: `rcp_node`/`rcp_lit`/`rcp_bin`, `pattern_match.c:41-43`). **A fresh per-MKPAT-call heap object with a free slot IS per-construction granularity** — the anchor the fix needs already exists, which makes this materially more tractable than the first half of this cursor implied.

**FIX SPEC (PROPOSAL — verified facts and open questions kept separate; next seat VERIFIES, does not inherit).** *Verified:* one `DTP_t` per construction; `rcp` free; `$V` cells are per-site (`lower_snobol4.c:2222`/`:2504`); the DEFER resolves the value **by name at match time**. *Proposed:* at MKPAT, snapshot the current `$V<i>` values into a per-construction vector hung off the `DTP_t` (in `rcp` or a sibling field), and have the blob's DEFER read **slot i of the DTP_t it is running under** instead of the global by name; the `$V` globals then degrade to a build-time marshalling buffer whose lifetime ends at MKPAT, which is exactly the p.85-86 semantics. *⛔ OPEN QUESTIONS THAT GATE IT — answer these FIRST, they are where the design can die:* **(1)** does the blob body have its `DTP_t` in hand at match time? The caller does (`rt_defer_get_pat_fn` returns `dtp_fn_of(pval->p)`, so `pval->p` IS the `DTP_t`) — but whether the **entered blob** can reach it, or whether it needs threading through the existing r10/r11 wire contract or the s97 blob activation frame, is UNMEASURED. **(2) GC is the real risk:** the vector holds `DESCR_t`s that can be **patterns holding patterns**; `rt_ws_alloc` tracing of a new DESCR_t vector must be confirmed before anything is written, or the fix trades a cycle for a collected-object crash. **(3)** a `DTP_t` field addition is a struct field, not a global — the NO-NEW-GLOBALS rule does not bind, but confirm no emitted code hard-codes `sizeof(DTP_t)`. ⚠ Not attempted at ~87% context (END-OF-CONTEXT LAW: do not open a hunt you cannot close) — routed with a spec, not half-landed. ⚠ **BOARD DID NOT MOVE — beauty_suite still 14/17; stated plainly rather than left to be inferred.**

**NEXT SEAT STARTS HERE:** the design above, with `probe/pb/pb_selfref_alt_cycle` (10 lines, literals only, m3≡m4 SIG11) as the instrument and **three convicting controls that must stay green** — `pb_ctl_selfref_noalt_ok` (no ALT ⇒ lowers to plain `IR_BINOP` concat, **no MKPAT and no `$V` cell at all** — this is what proves the ALT merely forces the blob route), `pb_ctl_selfref_alt_unrolled_ok` (two distinct sites ⇒ two distinct cells), `pb_ctl_selfref_alt_once_ok` (one execution). Fix mode (2) with it and re-check `pb_stale_snapshot_value` (silent wrong answer, exit 0, m3≡m4) — **do not declare the class closed on the crash alone; the silent one is the harder half.** All 6 have live-`sbl` `.ref`s and are verified m3≡m4 at SCRIP `bb934ff3`.

**THE CLASS IS WIDER THAN THE `X = X (A | B)` SPELLING — TOP-LEVEL ALTERNATION CRASHES TOO.** `pb_selfref_toplevel_alt_cycle` (9 lines): `mk = mk | 'y'` in a loop, SIG11 m3≡m4. This matters because `X = X | Y` is **the canonical SNOBOL4 idiom for building an alternation set incrementally**, so a fix here is worth more than the single driver suggests. ⚠ **CENSUS, WITH ITS LIMITS STATED:** the shape appears across 1305 corpus `.sno` files in `lib/case.sno`, `csnobol4-suite/atn.sno`, `gimpel/{LIKE,OR}.sno`, `lon/sno/{PARSER,Bouvier,TZ,transl8}.sno` — **but only `beauty_suite/case_driver` is cleanly fingerprint-confirmed to this cause.** `atn.sno` is parse-blocked at `:360` (an unrelated front-end defect, never reaches the runtime) and the `gimpel` files error in the **ORACLE** too (rc=1 — library fragments, not standalone). **No "N programs blocked" claim is made and none should be inherited**; the honest statement is that the idiom is common and one driver is proven.

**SETUP NOTES.** Backgrounded builds are REAPED between tool calls — use `setsid nohup make … &` (plain `nohup`+`&` silently loses the build; `disown` is absent under sh). `gdb` is NOT installed in the container: `apt-get update` FIRST, then `apt-get install -y gdb` (a bare install 404s on a stale index). `c_rt_defer_get_pat_fn` is **not** on this path — breakpoints there never fire for the `$V` class. ⛔ **PUSH: nothing committed at the time of writing; corpus witnesses + this cursor are the session's whole product and NO code changed (so no REGENS debt, no regression risk). Credential asked in chat.** ⛔ **TWO RULINGS STILL OWED BY LON, BOTH UNCHANGED AND BOTH BLOCKING R-2:** (i) default the `SCRIP_NRET_CAP=1`+`SCRIP_DYN_ALPHA=1` pair? (ii) the oracle md5 — live sbl gives `9cddff25…` (622 lines), NOT the pinned `abfd19a7…` in DoD 2.

## LIVE CURSOR (superseded) — 2026-08-15 s106 (Claude Opus 5) — **ONE SEMANTIC ROOT CAUSE CLOSED AND A SECOND, PRE-EXISTING ONE EXPOSED AND WITNESSED UNDERNEATH IT (SCRIP `b39a891f`+`3e676eda`, corpus `35f76d69`+`59c21822`+`e03cbbbf`).** **(1) SN4-PAT-EAGER-CALL (landed, gated, killswitch `SCRIP_PAT_EAGER_CALL=0`):** a bare value-returning call in pattern position was DEFERRED INTO THE PAT$ BLOB and run at MATCH time. `sno_pat_node`'s TT_FNC arm built `IR_MATCH_VALUE` and RETURNED THE EVALUATION CHAIN INTO THE PATTERN GRAPH, so for a pattern that is BUILT rather than matched in place that chain became blob-resident — the arm's own comment claimed "SNOBOL4 eager semantics" while routing to the deferral mechanism, code and comment disagreeing since 2026-07-21. MANUAL AUTHORITY: p.86 *"NPAT captures the value of variable N at the time of pattern construction"*; p.134 *"Without it, PUSH() is called when the pattern is first constructed. In the modified example, the calls to PUSH() are deferred"* — deferral is what unary `*` is FOR and a bare call must not receive it. FIX RIDES EXISTING MACHINERY, ONE MECHANISM, NO NEW GLOBAL: PB-1s already snapshots build-time values into a hidden `PATV$k` and its drain (~2005) already lowers `cx->pre[].arg` GENERICALLY via `sx_lower` — it never required a bare name — so the call tree registers as a `cx->pre[]` entry and the blob DEFERs the frozen result. **GATES: bb_probes 188/188 BOTH modes (s105 watermark held exactly, re-proven after the final comment rewrap); BY-SET killswitch A/B over 520 further programs (beauty_suite, patterns, crosscheck, feature_test, demos) ZERO regressions and exactly ONE mover, an improvement — `omega_driver` SIG11/SIG11 → SIG6/SIG6, now the honest named decline `bb_emit_end: unresolved forward reference n2_match_assign_imm_β`, i.e. THE R-0 PORT-LESS-BOMB SIGNATURE IN THE IMM PHASE (sibling of s91's `n8_match_assign_cond_β`) — routed, not chased.** REGENS ×3 RAN (feature `3e676eda`, demo `e03cbbbf`; benchmark clean). ⚠ **beauty_suite UNCHANGED at 14/17 — a real manual-verified defect was fixed and one crash became diagnosable, but THE BOARD DID NOT MOVE; stated plainly rather than left to be inferred from the gate numbers.** **(2) ⛔ THE s105 "eager-call" LABEL FOR case_driver IS FALSIFIED, AND WHAT REMAINS IS A DIFFERENT, PRE-EXISTING DEFECT — `corpus/probe/pb/` (3 witnesses).** s105 recorded three load-bearing ingredients ("remove ANY ONE and it passes"); measured against fresh controls NONE of them holds — not the goto loop, not the accumulation, not the user-defined helper, not variable-vs-literal argument, not the function RETURN boundary (`mv_alt_builtin_call_infn` is a 9-line SIG11 with a BUILT-IN in the arm and no helper at all). After the eager-call fix the residual reduced to a shape with **NO function call anywhere, IN MAIN, no DEFINE**: `pb_snap_alias_loop_main` (9 lines) — *a pattern-valued statement RE-EXECUTED AT THE SAME SITE (goto loop) accumulating an ALT arm that holds a snapshot-deferred plain ref* — SIG11 both modes under **BOTH killswitch arms**, so it is NOT caused by this seat's change; it was MASKED because the eager-call defect crashed the same programs first. THE CONVICTING CONTROL: `pb_ctl_snap_accum_unrolled` — the SAME two accumulations unrolled into two DISTINCT SITES — **PASSES**. THEORY, STATED AS THEORY (next seat VERIFIES, does not inherit): PB-1s mints the snapshot global with a static counter at LOWER time (`static int g_snapctr`, ~1998) so the cell is PER SITE, not per execution; a loop re-assigns that one cell while pattern objects built by earlier iterations still DEFER-read it, and manual p.86 requires each construction to freeze ITS OWN value. The unrolled control passing is exactly what that predicts — but the failure is a CRASH, not merely aliasing, so there is more there than a stale value. **NEXT SEAT STARTS HERE: MONITOR-FIRST on `probe/pb/pb_snap_alias_loop_main` — 9 lines, in main, no DEFINE, no call, the smallest instrument this class has ever had — then gdb spin-count.** MONITOR NOTE, CORRECTING AN INHERITED BLOCKER: the monitor is **NOT** dark for DEFINE-bearing programs — s102's note applies to `MONITOR_BIN`, while `scripts/test_monitor_2way_spitbol_vs_run.sh` (spl↔scr, needs no csnobol4 build) ran clean on every witness this seat used and produced the first divergence directly. Setup traps re-hit: `sort -t$'\t'` fails under sh (use `TAB=$(printf '\t')`), process substitution fails under sh (pipe to files) — both for the 5th+ session. ⛔ PUSH: credential asked in chat at session end (s106 asking now; SCRIP `b39a891f`+`3e676eda`, corpus `35f76d69`+`59c21822`+`e03cbbbf` COMMITTED LOCALLY AND UNPUSHED). ⛔ TWO RULINGS STILL OWED BY LON, BOTH UNCHANGED AND BOTH BLOCKING R-2: (i) default the `SCRIP_NRET_CAP=1`+`SCRIP_DYN_ALPHA=1` pair? (ii) the oracle md5 — live sbl gives `9cddff25…` (622 lines), NOT the pinned `abfd19a7…` in DoD 2.

## LIVE CURSOR (superseded) — 2026-08-15 s105 (Claude Opus 5) — **ONE ROOT CAUSE CLOSED + R-8 OPENED ON A MEASURED CENSUS (SCRIP `86a73598`, corpus `a93ba029`+`ce7c00c8`).** **(1) MV-BLOB-GLUE (landed, gated):** a program-defined function called DIRECTLY as the pattern (`S F()` — oracle-legal, manual p.104) SIG11'd in BOTH modes. `bb_match_value.cpp` entered the PAT$ blob with the FLAT glue (`lea rcx`,γ / `lea rdx`,ω) while the blob reads γ/ω from **r10/r11** and its ω is a bare `jmp r11` ⇒ wires=0 ⇒ `jmp 0`. `rt_match_value_get_pat_fn` returns `dtp_fn_of(pval->p)` — literally the same blob pointer as `rt_defer_get_pat_fn`, which correctly uses `bb_glue_pass_wires_blob`; s22x GLUE-SYM had converted this site to the flat spelling on the strength of the shared name. Hunt was MONITOR-FIRST: cheapest discriminating experiment (`'ABCDE' F()` red vs `P = F(); 'ABCDE' P` green, otherwise identical) → gdb (PC=0, r10=r11=0) → asm diff vs the passing DEFER sibling. GATES: **bb_probes 188/188 both modes, zero regression**; 5 repro shapes SIG11→PASS; all 4 NRETURN shapes PASS under the s104 opt-in pair *including the semantic_driver test-4 shape*. ⚠ **beauty_suite UNCHANGED at 14/17 — this seat fixed a real defect but did NOT move the board**, stated plainly: `case_driver` **CLASS NOW NAMED AND WITNESSED (corpus `ce7c00c8`, `probe/mv/`)**: the crash is stack exhaustion inside `proc_PAT$0_α` with **r10/r11 correctly seated** — so NOT the glue defect, a second ingredient stacked behind it. Reduced from the driver to `mv_icase_reduced` (18 lines, no INCLUDEs) and then to `mv_eager_call_in_alt_fnloop` (12 lines): **an EAGER function call inside an ALTERNATION appended to the accumulating result variable inside a DEFINE'd function's goto loop.** THREE GREEN CONTROLS prove every ingredient load-bearing — `mv_ctl_eager_call_concat_fnloop` (call in CONCAT not ALT) · `mv_ctl_eager_call_alt_toplevel` (ALT+call but no enclosing fn) · `mv_ctl_fnloop_call_as_pattern` (same loop, no nested call) — all PASS. That confirms the s98/s100 "eager-call" label for case_driver with a witness instead of a guess, and hands the next seat the discriminator, not just the failure. 4 call-as-pattern REGRESSION GUARDS also minted green. `omega_driver` is a separate EVAL-built-pattern class (dies at test 5, `p5 = TX(0,LEN(5),'hello')` → `TZ` with `@txOfs $ *assign(...)`), `semantic_driver` is SIG6 honest-bomb ONLY because the s104 pair is default-OFF. **(2) R-8 OPENED (see the rung): OPSYN operator identity + coercion shape reversion, Lon in-chat.** Manual pp.115-116/Ch.15 verified against live sbl: used operators CANNOT be OPSYN'd (ERROR 156, and the manual says the restriction exists *so that* the compiler may assume their identity ⇒ static arith coercion is SANCTIONED), but the eleven unused operators are runtime-rebindable and must carry ZERO static identity. `snobol4.y` hard-wires SIX of them to arithmetic (`#`→TT_MUL, `%`→TT_DIV, `!`→TT_POW, unary `/ # %`) — `'abc' # 'def'` prints **`0`**, a silent wrong answer. 9 witnesses minted. **NEXT SEAT — the cheapest path to beauty_suite 17/17 is now fully routed:** (1) `case_driver` → hunt `probe/mv/mv_eager_call_in_alt_fnloop` MONITOR-FIRST against its three green controls (the discriminator is already isolated; expect the eager call's activation to be re-entered per match attempt rather than once at build time); (2) `semantic_driver` → Lon's opt-in-pair ruling, no code; (3) `omega_driver` → EVAL class, mint tiny repros first. ⛔ **R-2 BEAUTY SELF-HOST WAS NOT ATTEMPTED THIS SEAT — stated plainly rather than left to be inferred:** it is ruling-blocked on the md5 below, and s105 ran out of context budget to do it responsibly (END-OF-CONTEXT LAW: do not open a hunt you cannot close). It is the next seat's opening move once the md5 is ruled. Then R-8(a) grammar (regen bison BEFORE editing, confirm byte-identical, then edit+regen) → R-8(b) dispatch on the s104 by-name machinery → R-8(d) unwire the SNOBOL4 relop coercion hoist ONLY (⛔ NOT `lower_icon.c:342/343/368`), preserving the positional error contract (111 before 112). ⛔ **TWO RULINGS OWED BY LON, BOTH BLOCKING:** (i) default the `SCRIP_NRET_CAP=1`+`SCRIP_DYN_ALPHA=1` pair? (semantic_driver goes green; needs a full md5 radius sweep); (ii) **the s101 oracle md5 — live sbl gives `9cddff25…` (622 lines), NOT the pinned `abfd19a7…` in DoD 2; Milestone 1 cannot be declared either way until this is ruled.** ⛔ REGENS ×3 NOT YET RUN for the codegen touch (owed before any board claim). ⛔ PUSH: credential asked in chat at session end (s105 asking now).

## LIVE CURSOR (superseded) — 2026-08-15 s104 (Claude Fable 5) — **ROAD B RULED AND THREE-QUARTERS LANDED (Lon in-chat: "All your choices"): the NAME datatype flows end-to-end and every deferred computed-name capture form PASSES under the opt-in pair (SCRIP `e2a0ef69` `5e23ce77` `40ecd003` `ba53a207` +2 regen, corpus `ff1b3fc5` +2 regen — ALL LOCAL/UNPUSHED, credential asked s104).** Gates at HEAD, defaults: bb_probes **188/188 both modes** (re-proven post-REGENS ×3 and again post-3b); beauty_suite 14/17 both modes ZERO regression; nret witnesses 3/7 default, **6/7 opt-in**; oracle matrix (probe /tmp/px shape) matches on DATATYPE/IDENT/concat/OUTPUT/$-indirection/table-distinctness.
**THE LADDER, MEASURED: (1) DT_N recognition (`5e23ce77`)** — DATATYPE had no DT_N arm (fell to "string"); `descr_identical` memcmp'd strdup'd pointers (IDENT(.d,.d)=F); `rt_coerce_str_d` + `descr_to_str` lacked the manual-p.197 NAME→name-string conversion. All four authorities fixed; `.A[2]` VCELL form still red (off the critical path — beauty:192/semantic/counter are simple-name only); NAME→STRING is case-sensitive per the standing ruling (folding sbl refs need case-neutral witnesses — `nret_name_op_return` re-minted uppercase, corpus `ff1b3fc5`). **(2) NRETURN lvalue (`5e23ce77`)** — s98's TINY consult hardcoded wn=0 into rt_nret_fix, deref'ing the NAME under WANTNM (the F()=43 dtype=0 bomb); `rt_nret_fix_tiny` consults the LIVE rt_g_want_name and consumes after (rt_nret_fix's tail RE-SETS the global — AB save/restore discipline — so the clear must follow). nret_lvalue_plain PASS both modes. **(3) THE COMPUTED-NAME TRANSFER WAS NEVER MIGRATED AT s55 (`40ecd003`)** — c_rt_proc_open_fn returns hardcoded 0 ("fn rides the OPEN return") but bb_match_end release_pump + both bb_match_capture IMM arms still called it and jumped rax=0 = THE PC-0 WILD-WIRE CLASS, broken since s55; pump + rt_cap_open '*' arms rewritten fully C-side via rt_call_proc_descr (nested-flush LIFO safe); stale open_fn calls deleted (dynamically dead pre-'*', md5-radius only). **(3b) TINY RECORD ENTER (`ba53a207`)** — by-name dyn entry: p->fn from rt_define_site is the rt_goto_transfer thunk (wrong protocol, rip=5); the sealed alpha\$<FN> cell speaks the MEASURED emitted contract (from the PASSING RV=G() site asm): `rcx=&{.quad nargs, γ-landing, ω-landing}`, `rsp=&result cell`, NO C OPEN (alpha owns its GVA-window save + level bookkeeping), result DELIVERED IN rax:rdx (the landing stores to the cell — .Lx70_29 — not the callee), γ landing runs the rt_nret_fix_tiny consult. asm shim `rt_tiny_record_enter` (stack record, five-push alignment, RTCC inbound load mirrored) + `rt_dyn_alpha_fn`; **APPLY('F') of a DEFINE'd fn — the 5-line repro of the whole class, minted this session — FIXED under the switch.**
⛔ **BOTH SWITCHES DEFAULT OFF — flip is the next seat's first decision:** `SCRIP_NRET_CAP=1` (capture '*' arms live) + `SCRIP_DYN_ALPHA=1` (record-enter routing). Under the pair: nret_cond_defer_call/nret_cond_stored_blob/nret_imm_defer_call ALL PASS (inline, stored-blob AND IMM deferred forms), counter_driver PASS, pb control (epsilon-var subject + DEFINE-body-built stored blob) PASS. **RESIDUAL, NAMED AND CONTROLLED: semantic_driver alone still SIG11 at test 4** (`'' nPush()`; the s103 record-resume signature, ret-addr ...f345, saved-r10-0 record at rsp) — the extra ingredient beyond the class is one of {two-arg DEFINE body labels, OPSYN aliases, PushCounter's \$'#N' DATA-ctor work executing at flush, direct call-result-as-pattern}; counter_driver (same idiom, none of those) is the GREEN CONTROL — bisect by adding ingredients to counter's shape one at a time, MONITOR-FIRST or gdb at the ...f345 restore site. THEN: flip both defaults ON, full killswitch A/B + compile-time md5 blast radius over the corpus, bb_probes+beauty_suite+patterns+crosscheck by-set, REGENS ×3, and expect semantic (+case? no — case = the s98 eager-call design class, untouched) movers. nret_cond_nondeferred stays a LOWERER refusal (build-time call capture target = the R-4(b) evaluated-operand-cells design, case_driver's class — same rung). (4) beauty.sno itself after 17/17: m4 -INCLUDE ERROR-220 divergence + m3 line-1 SIG11 (s101/s102 notes) + ⛔ ORACLE MD5 RULING still owed (live `9cddff25…` vs pinned `abfd19a7…`, s101). Manual read this seat: NRETURN pp.133-135 in full; Name Operator ~p.96; Datatypes/NAME ~p.196-197 (SPITBOL deviation: unary `.` on a simple variable yields NAME not STRING; auto-converts; observable via IDENT/DIFFER/DATATYPE/table-subscript — implemented to the letter). Setup traps this seat: gawk for scorecard report (again); sh process substitution (again — cmp files); C++ template externs need the C-linkage block (bb_call_proc_staged.cpp:50 pattern). ⛔ PUSH: credential asked in chat s104 (asked once mid-session, not yet supplied — asking again at this handoff; 6 SCRIP + 3 corpus commits local).
## LIVE CURSOR (superseded) — 2026-08-15 s103 (Claude Opus 5) — **MILESTONE 1's REMAINING BLOCKER IS NAMED AND WITNESSED: THE NAME DATATYPE / NRETURN / COMPUTED-NAME CAPTURE TARGET (SCRIP `f08e2178`, corpus `d70938bd`).** beauty_suite 14/17 both modes (UNCHANGED — this seat converted a crash into a diagnosable decline, it did not move the score); bb_probes **188/188** both modes; patterns 108/122 m3 · 107/122 m4; zero regressions by set; REGENS ×3 ran.
**⛔⛔ THE M1 BLOCKER, MEASURED (this is the rung, not a side quest): `beauty.sno:192` is `SGoto = ('S' | 's') . *assign(.sf, *'S')` — a CONDITIONAL ASSIGNMENT WHOSE TARGET IS A DEFERRED CALL, with NAME (`.sf`) arguments. `beauty_suite/semantic.sno` + `counter.sno` are the identical idiom (`epsilon . *PushCounter()`, body `PushCounter = .dummy :(NRETURN)`). Manual authority pp.133-134: NRETURN returns the NAME of a variable to which the caller may assign, and the verbatim idiom is `'ABCDE' ? LEN(2) . *PUSH()` — "the calls to PUSH() are deferred until assignment takes place"; error 243 = result in NRETURN is not a name. MEASURED DEPTH ORDER (witnesses `corpus/probe/nret/`, 7 files + README, every `.ref` from live `sbl`): (1) **the unary `.` name operator NEVER PRODUCES A NAME** — `DATATYPE(.dummy)` = `STRING` in SCRIP vs `NAME` in the oracle, and a function returning `.dummy` yields the NULL STRING, not `DUMMY` (`nret_name_op_return`); (2) NRETURN therefore cannot deliver a usable name — `F() = 43` BOMBs `rt_assign_var: lvalue is not a variable (dtype=0)` (`nret_lvalue_plain`); (3) computed-name capture targets decline (`nret_cond_*`, `nret_imm_*`); control `LEN(2) . x` PASSES (`nret_control_plain_capture`) — the axis is isolated. `DT_N` + `NAMETRAP()` + `IS_VARREF_fn()` EXIST in `src/contracts/descr.h` and `pattern_match.c` BUILDS NAMETRAPs (11 sites) — THE DATATYPE IS MODELLED; THE COMPILE PATH IS WHAT NEVER PRODUCES ONE. ⛔ **RULING WANTED FROM LON, TWO ROADS, DIFFERENT RUNGS — DO NOT GUESS:** (A) revive the C `*VAR` path (`rt_cap_open` ARM B / `c_rt_cap_open`, `g_capx`) to unblock M1 fast, or (B) build the NAME datatype properly through parser→lower→emitter and delete the C path — which is what `bb_match_capture.cpp`'s own header records Lon directing ("in favor of ordinary BB RESULT/operand dataflow"), blocked on this very bug since s82.**
**LANDED THIS SEAT (SCRIP `f08e2178`) — a real defect found en route:** `bb_match_capture.cpp` dispatched the `*VAR` computed-name guard AFTER the phase-1 arms, so a COND capture with a computed-name target fell into the LIVE spine arm and pend-pushed the literal name string onto the r12 CAS array as if it were a varname (PROVEN from emitted asm: `.S0: .string "*F"` + `lea rcx,[rip+.S0]; mov [r12+0],rcx`); the MATCH_END flush then dereferenced it as a variable ⇒ SIG11, while its IMM (phase-2) sibling reached the guard and declined honestly — the two siblings disagreed. Guard hoisted for PHASE 1 ONLY, mirroring the sibling phase-1 bombs and **DEFINING β** (R-0 law: never leave a port a sibling's glue jumps to undefined). ⛔ MEASURED NARROWING: SAVE (phase 0) deliberately UNTOUCHED — `nret_imm_defer_call` proves phase 0 only parks the start cursor and is harmless with a computed name. Witnesses `nret_cond_defer_call` + `nret_cond_stored_blob` were SIG11 before, honest BOMB after.
**⛔ THE 3 REMAINING beauty_suite DRIVERS ARE *NOT* THIS CLASS — RE-BACKTRACED, ROUTED, UNHUNTED:** `case` / `semantic` / `omega` all still SIG11/SIG11. s102's lead (b) was executed: the ZD-5b class is genuinely fixed and these are something else. Clean gdb backtraces (`SCRIP_NO_SEGV_HANDLER=1`, `set breakpoint pending on`): **semantic → PC `0x0000000000000000`, frame #1 in the RX slab (`0x7fffee40f345`) = a NULL INDIRECT CALL**; **case → PC `0x7fffee404000`, an RX-slab PAGE BOUNDARY (ran off into a page with no code)**; **omega → PC `0x7ffff7ffd000` = `_rtld_global`, i.e. INSIDE ld.so DATA**. THREE DIFFERENT PAYLOADS, ONE MECHANISM: an indirect transfer through a value that is not a code address — s102's named wild-`jmp rax`-through-a-descr-tagword / wild-wire class (`rt_coerce_str_d in=0xbc00000000`, TDump_driver's old signature; demos json/treebank/claws5/calculator share it). **NEXT SEAT STARTS HERE:** MONITOR-FIRST on ONE of them (semantic is smallest: 66-line driver, 27-line subsystem, dies at test 4 `'' nPush()` after 3 clean PASSes — a tight window), find the FIRST divergent trace event, then gdb spin-count on the indirect-transfer site; a null cell strongly suggests an entry cell never filled by `m3_seal_entry_cells()` (R-1's `bb_ab_fn_cell_ptr` machinery) — VERIFY, do not assume. ⛔ PUSH: credential asked in chat at session end (s103 asked; not yet supplied — SCRIP `f08e2178` + corpus `d70938bd` + this file are COMMITTED LOCALLY AND UNPUSHED).
## LIVE CURSOR (superseded) — 2026-08-15 s102 (Claude Fable 5) — **ZD-5b LANDED, LEAF SLICE (SCRIP `68d38973`, corpus `017bcb41`): beauty_suite 13→14/17 m3≡m4 (TDump flipped, ORACLE-EQUAL); bb_probe_matrix 166/166 both regimes; crosscheck 188/195 both modes; witnesses `probe/zd5b/` 6/6 both modes.** THE CLASS: arm-interior evaluated-arg charset primitives (`(SPAN(digits &UCASE '_' &LCASE) | eps) RPOS(0)` — TLump's exact shape, beauty-dense) were closure-only (zd_plan's linear γ-walk never descends ALT operand pairs, DESIGN-SN4-ZD5B §1), fell to the FRQ flat read, mis-resolved by EXACTLY frame64+ALT32 (asm-measured [rsp+216] vs truth [rsp+120]) → silent-NOMATCH or the wild-`jmp rax`-through-a-descr-tagword SIG11 (rt_coerce_str_d in=0xbc00000000 — TDump_driver's verbatim signature; strong suspect for omega/semantic + demos wild-wire). FIX: descent rooted ONLY at ALTs already claimed into THIS run (s24a steal impossible by the closure definition) + `+32` ALT-record conjunct in the op_zread crossing sum beside the s99 +64 mrbp arm (same loop, gated `zd_arm[i]==_zm`); arm zout = zout[ALT]+intra-arm K per chunk (cells-only ledger). ⛔ MEASURED NARROWING, DO NOT WIDEN BLIND: claiming const-form leaves (n_operands==0) flipped β-resume and CRASHED bb A07/A08/A09/A13/G00/H07/fence_probe (166→159) — they stay closure-only; arms containing DEFER/nested-ALT/captures unclaim WHOLE (per-arm). RESIDUAL NAMED: β-resume INTO an armed evaluated-arg leaf is witness-unexercised — first suspect if a backtrack witness reds. Killswitch SCRIP_ZD_5B=0 byte-identical (t7/u1 cmp). NO new globals (all planner scratch caller/function stack). REGENS ×3 ran. **NEXT SEAT: (a) re-run demos/patterns suites — the wild-wire block (json/treebank/claws5/calculator) shares this signature, expect movers; (b) omega/semantic drivers still SIG11 — re-backtrace against the now-fixed class first (cheap discriminator), then mint; (c) case = the s98 eager-call design class (R-4(b)-adjacent), untouched; (d) beauty.sno itself: m4 the `-INCLUDE` self-parse ERROR-220 divergence (manual: control statements are COMPILER directives, col-1 `-`, quotes required, p.171-173; ERROR 220 = compile-time \"missing operator\" — the divergence is in how sbl tokenizes beauty's INCLUDE-format data lines at COMPILE of beauty itself, not in ANY/POS semantics), m3 = line-1 SIG11 class. (e) s102 MONITOR NOTE for a monitor seat: `MONITOR_BIN=1` ALONE (no pipes) SEGVs scrip on DEFINE-bearing programs (mdiv.sno rc139; trivial programs fine) — the tap path crashes pre-wire; harness spl↔scr syncs 7 steps on trivial then scr EOFs = that crash. Setup traps re-hit this seat: process substitution under sh (pipe to files); gawk needed for scorecard report.** ⛔ ORACLE MD5 + expression.sno ORACLE_FAIL rulings still owed (s101). ⛔ PUSH: credential asked in chat at session end (s102 asking now).

## LIVE CURSOR (superseded) — 2026-08-15 s101 (Claude Fable 5) — **TWO ROOT CAUSES CLOSED (SCRIP `04d29ceb` `966cff70`, corpus `22340e26`): beauty_suite 12→13/17 m3≡m4 (XDump flipped); dv witnesses 4/4 m3+m4 (were RED s100); mrbp trio 3/3 (RED since s95). bb_probes 188/188 both modes zero movers across both commits.** **(1) R9/GVA CLAIM VIOLATION (`04d29ceb`)** — charset-loop match templates (ANY/NOTANY/BREAK/SPAN/SPAN_VAR/BREAKX) carried needle-len in `r9d`, killing the RC-5 GVA base at every INLINE guts invocation; the next GVA read with no intervening veneer reload crashed (json/treebank/claws5/calculator SIG11 demos, string_pattern/mixed_workload m4 benchmarks, beauty XDump/TDump). Fix: ecx (rcx dead post subject-load) for any/notany; eax-per-outer-pass for span/break/breakx/span_var — zero new encoders. Gate `test_gate_rtcc_claimed_regs.sh` extended: now catches `r9[dwb]` sub-register spellings + `mov32/movabs` (the hole that hid this for sessions). Witness `probe/rtcc/rtcc_span_gva_defer` SEGV→MATCHED both modes. **(2) DEFER HAZARD WIDENING (`966cff70`)** — `earn_hazard_in`'s `pat_static` exemption was NOT sound: even static defers exit gamma with a 16B `{pad,resume}` record retained on the spine (measured from dv_cap_defer_bare asm, `.Lx53_6`), so a SAVE..COND span crossing ANY defer has no compile-time-constant RSP distance. s100's revert evidence ("widening only shuffles shapes") was gathered while the r9 bug was live — FALSIFIED on re-measure. SCRIP_DEFER_HAZ_STATIC=1 killswitch byte-identical over probe sweep. Blast radius: 5 movers (121/122/123/125 + 066), 4 fail→PASS, 0 regressions. **ORACLE MD5 DISCREPANCY: live sbl produces `9cddff25...` (622 lines), not the pinned `abfd19a7...` — ⛔ LON RULING NEEDED before M1 can be re-earned.** **NEXT SEAT — beauty m4 runs to exit 0 and produces correct-looking output through the header (10 lines), then hits "Parse Error" on `-INCLUDE 'global.inc'` control lines (line 611: `Line POS(0) ANY('*-')` — the `-` branch). Minimal probe `/tmp/p1.sno` (NOT committed — mint and bank before hunting): `L '-INCLUDE ...' POS(0) ANY('*-')` → SCRIP prints DASH-MATCH (correct), but sbl rejects the literal with ERROR 220 (missing operator). The discrepancy is in beauty's self-parse of the INCLUDE control-line format, NOT in ANY itself. Characterize with MONITOR-FIRST on `beauty_suite/global_driver.sno` (it INCLUDEs via the same path and is now PASS — so the self-parse route is different). m3 crashes at line 1 (raw SIG11 before the header, separate class). demos remaining SIG11 block = json/treebank×2/claws5×3/calculator-1: all ARBNO+DEFINE+GVA-reader chains — the r9 fix exposed the next stacked defect (wild-wire at `0x400000000`, a descr-tagword jumped through, traced in corpus `0fd4c11d`+`dbb30c29`); demos TIMEOUT block = calculator-2*/json-match* (6 programs, one class — corpus witnesses minted, cure direction = ALT reading a CLASS D activation record as its carve, `0fd4c11d`). benchmarks residue: cap_imm_nret×2 (s68), eval×2 (R-5), indirect_dispatch. porter = dup-label ASM_FAIL (m4). expression = ORACLE_FAIL (sbl rc=139 — ⛔ LON RULING NEEDED: pin a .ref or XFAIL). Setup traps this seat: process substitution `<(...)` fails under sh (pipe to files); git config --local needed on corpus+x64; `--compile` emits `.s` text (rename to `.s` before feeding gcc, or use scorecard recipe).** ⛔ PUSH: credential supplied by Lon in-chat (s101); push in progress.

## LIVE CURSOR (superseded) — 2026-08-15 s100 (Claude Fable 5) — **THE FOURTH SEAT CLOSED IN THREE COMMITS (SCRIP `0975aa59` `b24ebbf9` `691db5c9`): l3 ladder 0/14 → 14/14 BOTH MODES BOTH REGIMES; beauty_suite 8→12/17 m3≡m4 (Qize/ReadWrite/ShiftReduce/trace flipped); benchmarks 12→18/23 (string_pattern/mixed_workload/roman/pattern_bt — the filed REPLACE-form class, confirmed); demos 5→6.** The seat was THREE stacked defects on one splice path: **(1) SUBJECT SEAT (regime-independent, `0975aa59`)** — s97's DEAD-SPILL deletion removed the head's op_sa mirror from BOTH arms while bb_match_replace still read FRQ(op_sa), a slot nobody writes in the armed world (slen=0, whole subject replaced); fix per the written ZD-5 ZOPQ(2) design: staged `op_zread[2] = zunder + (zd_out[head]−zd_out[subj])` at the REPLACE choke — physically exact POST-WHACK (NO +64 crossing term: the frame is dead at the splice; NO pattern-carve term: whacked with it — why the generic staging loop cannot serve this seat), −1 sentinel falls back to legacy FRQ byte-identical; ⚠ staging MUST run AFTER the generic loop's 6-slot clear (measured: staged 16 printed, [rsp+0] emitted). Also fixed en route: the mrbp start-cursor SOURCE (fourth instance of the geometry hole; A/B legacy raw_start=1 vs mrbp 0). **(2) CURSOR ARENA-RIDE (`b24ebbf9`)** — the MEND rfc stash wrote {start,end} at raw-rsp op_off coordinates that are claim-backed ONLY in main-shaped graphs; in a DEFINE body they alias the save_restore record (gdb rd_min: R_gamma jumped through 0x300000002 = the subject descr tagword; s99's op_off=42480 un-rebased flat coordinate = same family). Fix: bank the pair at MEND α in the STANDING frame's reserved [rbp-48]/[rbp-56] RESULT pair (r14 live; the pump clobbers it), push 16B onto the R12 arena AFTER the pump's r12 reload (post-pump mandatory or the pump misparses it as a capture record), pop LIFO in bb_match_replace; the op_off/op_zpat/op_zfc reader formula ladder RETIRES on the mrbp arm; legacy byte-identical. Accepted in-code leak: a FAILING replacement chain orphans one dead 16B pair (marks are saved values; drain edge = R-5). **(3) SPLICE CONSUMES ITS SUBTREE (`691db5c9`)** — the replacement subtree's zunder bytes were released by NOBODY (MEND whacks only the frame; statement_end's +16 is the subject cell), and the depth-exact RETURN floater's pop then loaded a statement cell as the return target; replace γ now `add rsp, op_zdepth`. This unblocked repldef1/Qize and the whole replace-in-DEFINE family. GATES all three: bb probes 166/166 BOTH modes 0 regression (re-proven post-REGENS); md5 radius 29/644 = REPLACE-family only, by-set vs stashed baseline ZERO PASS→fail; l3 14/14 under SCRIP_MATCH_RBP=0 too. REGENS ×3 ran (SCRIP `19652906`+`07d6eae7`, corpus `f9caba9e`). **NEXT SEAT — the remaining 5 beauty drivers, classified this session: (a) XDump/TDump = the `.`-ACROSS-DEFER class, four witnesses MINTED RED with oracle refs (corpus `d458eeb6`: `probe/dv/dv_cap_defer_{bare,alt_var,xdump_proto,alt_var_span}`; gdb+asm on `S (E) . X`: COND reads a DEFER slow-path resume record's bytes as the start cursor → a SINGLE >512MB rt_substr → ZHP 512MB/0 blocks; ALT flavor binds null silently). ⛔ TRIED AND REVERTED (do not redo): extending earn_hazard_in:690 + CAPNEST:763 to PATV$-only (only PATV$ snapshot DEFERs are record-free — generic pat_static misses at runtime on plain strings and takes rt_defer_open) only SHUFFLES shapes (v1 ZHP→null, v3 null→ZHP) because the STANDING frame-slot dance ITSELF binds null across a DEFER activation — the s95 DOUBLE-DUTY defect verbatim; the cure is R-4(a)'s per-iteration ACTIVATION slot, land the predicate widening TOGETHER WITH the activation frame, not before. (b) case = eager-call (R-4(b)-adjacent, per s98 map, do NOT chase r12). (c) omega/semantic = wild-wire, mint tiny repros. Demos block = R-4(a)'s roll call verbatim (json/treebank/calculator/claws5 dynamic-K ARBNO) + porter dup-label + expression COMPILE. benchmarks residue: cap_imm_nret×2 (s68), eval×2 (R-5), indirect_dispatch (uncharacterized). test_stack = oracle-unstable (sbl banner+timestamp when it errors under empty stdin) — needs a deliberately pinned .ref, classify in R-3(d) style.** Setup traps this seat: corpus/x64 need `git config --local` (s98 trap, hit again); process substitution fails under sh (s99 trap, hit again — pipe to files). ⛔ PUSH: credential asked in chat at session end (s100 asking now).

## LIVE CURSOR (superseded) — 2026-08-15 s99 (Claude Fable 5) — **R-3(a) LANDED IN FULL (SCRIP `4926f81b`): THE rt_sg_scan class was TWO STACKED DEFECTS, both fixed — (1) MRBP CROSS-HEAD FRAME missing from ZD op_zread staging: under emit_match_rbp() MATCH_BEGIN moves RSP 64B (push rbp+4 pushes+sub 24) that zd_k=0 deliberately hides (correct for RELEASE — frame self-releases via mov rsp,rbp/pop rbp — wrong for in-match ZOPQ reads of PRE-FRAME cells; Gen n2252 needle read [rsp+40] vs actual [rsp+104]); fix rides the L-4/061 precedent at THE ONE staging site (emit.cpp ~3050): emit_match_rbp() ⇒ +64 per crossing, PRECEDENCE over the legacy hfc +32 (mutually exclusive branches of bb_match_begin:34); (2) BREAK ZD arm called rt_sg_scan_nonmember (SPAN's scanner) — UNREACHABLE until (1) stopped the crash; now rt_sg_scan_member per the legacy arm at :84 (gdb: Σ/needle/Δ all correct, scan returned 0 because l∉{nl} — routine right, call wrong). GATES: witness asm 40→104 exact; beauty_suite 7→8 both modes (Gen PASS/PASS); bb_probes 188/188 BOTH MODES FULL GREEN; patterns 102/122 (+2); crosscheck 183/196; stash A/B BY SET: ZERO PASS→fail. REGENS ×3 ran. Witnesses corpus `e53c73b1`: m1_gen_flush_shape (red→green) + m1_indir_name_output{,_fn} controls. — **R-3(c)/L-3b PARTIAL (SCRIP `2afe946c`): MEND rfc write-side fixed, THIRD instance of the 64B mrbp hole** — the cursor-stash formula's +32 constant IS the legacy hfc head carve; under mrbp it must be 64 (never additive — the geometries are exclusive); gdb-measured writer/reader delta exactly 32 both slots on l3_spl_break_nonterm; post-fix SCRIP_REPL_TRACE: raw_start=0 raw_end=14 CORRECT (was garbage). **THE FOURTH SEAT IS OPEN AND LOCALIZED — NEXT SEAT START HERE:** the REPLACE reader's SUBJECT descr (bb_match_replace.cpp:19-20, FRQ(op_sa)/FRQ(op_sa+8)) still arrives slen=0; gdb at n12_match_replace_α on l3_spl_break_nonterm: read targets [rsp+176]/[rsp+184] hold {0,1}; the actual subject descr HI (slen=18, value 0x1200000002) sits at [rsp+16] — measure the lo/ptr seat beside it and re-derive op_sa's mrbp compensation (likely the same 32-vs-64 substitution on the READ side, or op_sa needs the head-geometry term removed; honor the s38 formula war — the l3 ladder is THE instrument, 0/14 at both A/B points so any PASS is a strict win). ⛔ s99 FINAL MEASUREMENT: l3 = 0/14 under BOTH SCRIP_MATCH_RBP=0 and =1, and Qize fails under =0 too — the fourth seat is REGIME-INDEPENDENT, the pre-existing L-3b marshalling war proper (the ledger line "the legacy-arm failure proves the marshalling itself is wrong first" is confirmed by direct killswitch A/B); the mrbp write-side fix stands for ITS seat (trace-proven cursors 0/14→correct) but do not look for the subject defect in mrbp geometry. Qize/XDump = the SAME family's FLAT-COORD variant: MEND rfc stores at op_off=42480 (raw zls coordinate, un-rebased — statement too big for the claim? measure why rfc()=1 with flat op_off before touching the formula). ReadWrite DIFF→SIG11 = likely same family unmasked. case = eager-call (R-4(b)-adjacent, do NOT chase the r12 symptom); ShiftReduce/TDump/trace uncharacterized; omega/semantic wild-wire (mint tiny repros). Setup traps this seat: process substitution <(...) fails under sh — pipe to files; gdb batch finish-in-commands flaky — break at labels (*&nXX_box_α) and print registers directly. ⛔ PUSH: credential asked in chat at session end (s99 asking now).**

## LIVE CURSOR (superseded) — 2026-08-15 s98 (Claude Fable 5) — **THE NRETURN CLASS LANDED (SCRIP `24e7ab36`): beauty_suite 3→7 BOTH modes m3≡m4 (counter/stack/match/tree flipped); bb_probes RE-MEASURED 166/166 both modes at every step incl. post-REGENS; crosscheck+patterns BY SET vs s91 combined: ZERO PASS→fail, 23+33 repaired.** TWO DEFECTS, one commit: **(1) the NRETURN FLOATER was not depth-agnostic** — its lit+SNO$NRET body used zd_plan [rsp+K] offsets valid at ONE depth but floaters are jumped-to from ARBITRARY depths; from a TINY body it clobbered the α carve (saved wires + call record → `F_gamma: mov rdx,[rcx]` rcx=0, the s97 cursor's named class). SNO$NRET's whole C body = `rt_g_ret_by_name=1`, so the floater is now `bb_nreturn_mark()` (bb_save_restore.cpp): x86_alpha + GOT/abs flag store + x86_gamma, glue jmp RETURN (γ redirect emit.cpp ~2870: head's γ = the never-collected SNO$NRET member → emit_floater_label(1)); RPO_PUSH refuses emit_floater_member. **(2) the TINY det γ landings never CONSUMED the flag** (the AB activation slow-arm and rt_proc_enter:558 already did): `bcps_nret_consult(r0,r8)` (bb_call_proc_staged.cpp, ONE authority, 3 call-site inserts at the L(2) landings) = flag-guarded veneer call `rt_nret_fix(result, wn=0)` through NEW `x86_rtcc_call_descr_ops` (x86_asm.h — the FRQ-slot descr veneer's parameterized-capture twin); `rt_nret_fix` EXPORTED (rt.c hidden attrs dropped). Manual read: pp.133-135 NRETURN in full (+ subject-field p.102-ish NRETURN-as-lvalue note). **WITNESSES corpus `b9cb503c`+`cbf670b5`:** `m1_nret_value` PASSES both modes (the fix's control); `m1_nret_lval` rc134 / `m1_nret_cap` rc1 / `m1_nret_defer` rc139 minted RED = the manual's OTHER two usage forms (lvalue `F()=x`, capture `. F()`, deferred `*F()`) — R-3(f) classes, different lowerings, NOT on the M1 critical path (beauty.sno's only NRETURN is a string literal, verified). **NEXT-SEAT MAP — the 10 remaining beauty_suite SIG11s, first-divergence classified this session:** (a) **rt_sg_scan_nonmember ×3 = Gen/Qize/XDump — THE BIGGEST CLASS, START HERE, MECHANICS DECODED THIS SESSION:** crash = the NEEDLE read (rt_sg_scan.S:41, rsi=stack addr, edx=0x7fff); witness Gen_driver m4 box n2252 (`$'$B' BREAK(nl) . outline nl REM . $'$B'`, indirect subject): the pre-frame producer n2249_coerce_string writes its descr at its own depth [rsp+0..15]; the in-match consumer reads planned [rsp+32/36/40] = carves-only (16 own + 16 coerce), but actual = carves + THE MATCH-RBP FRAME'S 64 (push rbp + push×4 + sub 24) ⇒ real cell at [rsp+80+] — **the in-match FRQ spelling of PRE-FRAME statement cells omits the frame's 64 bytes** (bb_match_begin's own comment: planner carve accounting "superseded" by the whack — superseded for release, NOT for in-match reads of pre-frame cells). Fix shape: +64 (or rbp+8+off — the coerce cell IS [rbp+8..]) on in-match FRQ reads of cells produced before MATCH_BEGIN when emit_match_rbp(); g1/g2 minimal slices PASS because their needle path skips the coerce box (different, compensated slot family) — the coerce-fed-needle-inside-frame shape is the discriminator. (b) **stored-pattern EAGER-CALL arm** (case_driver; witnesses `m1_alt_arm_eager_call{,_order,_inline_control}`): `P = 'x' | u('b')` — SPITBOL evaluates u at BUILD (oracle prints CALLED before built, manual p.134 refinement note verbatim), SCRIP's SN4-PAT-FNC "eager" lands INSIDE the PAT$ blob = runs at MATCH time (semantic divergence) then crashes rt_dcap_pump mark=0 (r12 was 0 at that match's α); INLINE form `'B' ? 'x'|u('b')` PASSES (control). Fix = build-time evaluation of value-returning call arms in STORED builds — pattern objects need evaluated-operand cells, R-4(b)-adjacent design; DO NOT chase the r12 symptom. (c) **simple REPLACE-form mis-marshalling** (witnesses `m1_repl_r1..r4`, `m1_replace_pos_capture`; `S 'b' = 'X'` → 'X' not 'aXc', BOTH regimes SCRIP_MATCH_RBP=0/1, BOTH media, PRE-EXISTING verified at `fabd120d` worktree): c_rt_match_replace receives slen=0 + garbage cursors (SCRIP_REPL_TRACE=1); MEND diag op_off=48 op_fc_disp=0 rfc=1 zc_frame=2 → this is the L-3b op_zpat/op_zfc compensation family (bb_match_replace.cpp:21 STEP-0' + FINDING-2026-08-12j triangulation, `probe/l3/l3_spl_*` ladder is ITS instrument — run l3 first, honor the s38 formula war; the ledger's "frame widening 64→80 + whack-to-replace-tail" note is the RBP-era design IF the formula war reopens, but the legacy-arm failure proves the marshalling itself is wrong first). ReadWrite = silent-DIFF family (LineMap[6] empty — TABLE-under-replace suspicion, mint before ruling); ShiftReduce/TDump/trace in-slab uncharacterized; omega died in ld.so / semantic pc=0 = wild-wire class (mint tiny repros). REGENS ×3 ran (benchmark 7 files / feature 20 / demo committed); bb gate re-proven green after. Setup traps this seat: sbl -I is not a flag (run oracle from the include dir); corpus/x64 need `git config --local` too. ⛔ PUSH: credential asked in chat at session end (s98 asking now).**
## LIVE CURSOR (superseded) — 2026-08-15 s97 (Claude Fable 5) — R-4(b) LANDED + TWO R-2 PREREQ CLASSES: bb_probes 163→166/166 BOTH modes; patterns 73→101 m3 / 100 m4; demos 5→8; beauty_suite 0→3 m3≡m4. SCRIP `be18fcb6` `e7ce8afd`; corpus `c5b16c7d` `4bff890d`. THE THREE LANDINGS: **(1) R-4(b)** — the stored-pattern blob IS its own ζ-ACTIVATION: `frame_slot_scan` blob-scope arm (rc 2, whole run, base 32 = {saved rbp, entry-wire r10, entry-wire r11, pad}); `blob_frame_scope()`/`blob_frame_bytes()` THE ONE AUTHORITY read by the α_body prologue (push rbp; carve; wires→[rbp-8]/[rbp-16]), CLASS D γ record ({res,r10,r11,**rbp**} then `mov rbp,[rbp]`), res (re-seats rbp from record[+24]), ω (reload wires; whack; jmp r11). Per-activation = recursion-correct (D12 re-enters the same ARBNO node). SECOND defect closed inside it: blob-interior DEFER wires were never restored (s44 WIRE-SAVE dead, `x86_fb_pinned()==0`) — a blob ending in a DEFER jumped its own γ through the interior L(4) forever = the demos `[ZHP]`/SIGSEGV class; entry wires in the frame cure every wire-clobbering interior (DEFER/CALL/CALL_VALUE/VALUE/CALLOUT). ⚠ TRAP: `x86("mov","r10",RDQ("rsp",8))` parses as the FR64 prefix and adds op_zdepth — glue must use `x86_rsp_load64/store64` (killswitch A/B caught +16). **(2) INCLUDE SEGMENTATION (R-3(f) rtx_func_11_include ROOT CAUSE)**: `stmt_src_slice` indexes MAIN-file lines by s->lineno so INCLUDEd statements got no `:src`, and zd_plan roots statement runs on bb_src_of ⇒ whole include = ONE run, statement_end released nothing, per-iteration RSP leak (beauty_suite 17/17 died in global.sno's SORT+loop tail). Every non-END statement now carries a note (placeholder when the slice can't serve it). Witness `probe/m1/m1_include_sort_loop{,_inline}`. **(3) MATCH_BEGIN DEAD SUBJECT SPILL** (beauty m4 SIG11 root cause): under op_zres every FRQ(op_sa) reader is gated !op_zres — the α spill had NO reader and wrote an unbacked flat coordinate ([rsp+1760] past m4 envp; m3 survived on chain-enter slack, s23a class). Deleted both arms; witness `probe/m1/m1_kw_subject_zres_spill`. **NEXT = the DEFINE'd-function RETURN-linkage class**: counter_driver + the 14 remaining drivers die at `<Fn>_gamma: mov rdx,[rcx]` with rcx garbage (gdb, counter m4) — the R-3(f) fencedef1/1013 family; hunt it MONITOR-FIRST from counter_driver (smallest: 48 lines + counter.sno), then re-run beauty_suite, then R-2 beauty.sno first divergence (`sbl -bf beauty.sno < beauty.sno`, md5 `abfd19a7…`). ⛔ PUSH: credential asked in chat at session end (s97 asked; not yet supplied).**
- **s96 (Fable 5, 2026-08-15) TWO CLASSES CLOSED, both gated by set (bb suite raw both modes; crosscheck+patterns scorecard old-vs-new per program: zero PASS→fail movers; the only movers are the flappy m3 rows 121/122/123/125, each re-run 3× and shown nondeterministic).**
  - **CLASS B — `*EXPR` PATTERN ELEMENTS (f6d t6m fence_probe; the s95 cursor filed them under the FENCE watermark — they are NOT that).** Discriminating siblings: `S BREAK(',') *DIFFER(X)` (no FENCE) SEGV m3 / `[GZ-10] 'EXPR$0' has no stackless slab` m4; `*(X 'b')` (no call) same; `LEN(*SIZE(X))` (same EXPR$0, value position) PASS ⇒ ANY non-variable `*EXPR` used as a pattern element, both media (m4 green on f6d/t6m was COINCIDENTAL: FENCE fails before the DEFER and the oracle also says FAIL). beauty.sno has 16 such lines (`*match(...)`, `*upr(tx)`, `*(GT(nTop(),1) nTop())`) — Milestone-1 critical path. THREE stacked defects on ONE path (`c_rt_defer_get_pat_fn → rt_call_proc_descr → rt_proc_enter → EXPR$ thunk`), one authority each: (1) scrip.c `proc_role3_kind()` — the s62 `_bare` predicate keyed on the bare role-3 shape, so the EXPR$ thunk (role-3 → body → SUCCEED/FAIL, lower_snobol4.c:2458) was emitted BARE in m4 with γ AND ω aliased to `jmp main_ω` (emit.cpp:2479), and the s57 `if (pe->dyn_scope) continue;` left it unregistered because it has NO DEFINE site; kind 1 = DEFINE stub blob (role-3 → IR_GOTO_DEFERRED, :2048: bare, site-registered), kind 2 = thunk (owns wire exits, startup-registered); (2) rt.c `rt_proc_enter` still delivered its landings only in rcx/rdx (pre-s55) while every body exits `jmp r10/r11`, and RTCC's inbound load seated the caller's stale wires (or 0) last — gdb: thunk unwound cleanly to rt_proc_enter's frame, jumped through wire 0; now seats r10=2f/r11=3f after the load, both arms; (3) rt.c γ landing: `c_rt_proc_call_epilogue_γ(DESCR_t frame0)` returns the DESCR in rdi:rsi but wire-exiting bodies deliver rax:rdx — single-operand thunks "worked" only because rdi:rsi held benign garbage (`*DIFFER(X)`), two-operand ones (`*(X 'b')`, `*GT(N,3)`, `*IDENT(X,'a')`) failed; the landing now forwards rax:rdx→rdi:rsi. 12/12 minted witnesses oracle-equal both modes (defer of call/concat/2-arg predicate/UDF, succeed+fail, with/without context/FENCE). Manual: Ch.9 p.130 (`*EXPR` evaluated at match time with current values).
  - **R-4(f) SLICE 3 RE-LANDED AS RULED (H08 H29 m3; +142_pat_arbno_fence_arbno PASS/PASS).** `fence_frame_candidate()` (container-in-body = the SAME list fence_body_kk declines on ⇒ K=0/static-K bodies byte-identical) → `fence_frame_slot()` (third customer of `frame_slot_scan`; `frame_slot_is_candidate` gains the FENCE1 arm; the MATCH_BEGIN carve widens through the same scan) → `op_fence_frame_off` (struct end, -1 at the choke); template: α `mov [rbp+off],rsp`, σ whack + its unwind MARK + PAIR(3) + β all `mov rsp,[rbp+off]`. Blob-resident FENCEs decline (no MATCH_BEGIN) = R-4(b). Manual: Ch.9 pp.125-127, Ch.18 pp.203-208.
  - Setup this seat: repo layout `/home/claude/{.github,SCRIP,corpus,x64}` (an early mis-clone of `.github` AS `corpus` was caught and corrected); gawk installed for the scorecard report; REGENS ×3 ran (no byte changes). Scorecard dirs `test-results/scorecard-s96-{classB,classB2,classA}` committed.
## LIVE CURSOR (superseded) — 2026-08-15 s95 (Claude Fable 5) — bb_probes CLIMB: m3 143→158, m4 146→162 of 166 (SCRIP `0053928b` R-4(a) SLICE 1 body-interior capture home · `683ac8f4` SLICE 2 FENCE1 σ-commit reads the watermark at the committed depth; corpus `e50f176f` XFAIL re-cut, gate green both modes). H10 — this file's named R-3(f) FENCE(ARBNO(LEN(1)) \$ OUTPUT) RPOS(0) regression — CLOSED (SLICE 2 class). ⛔ LON RULING s95 (in-chat, verbatim in substance): *"problematic BB RESULT values are allocated as a SLOT in the ACTIVATION FRAME, NOT the STANDING FRAME; we do not want to pre-allocate hardly any data in the STANDING FRAME; it is most likely a bug when recursion sets in — unless the STANDING FRAME is doing DOUBLE DUTY as ROOT+ACTIVATION, which is fine."* SLICE 1's slots (the shared s87 `frame_slot_scan` registry) are the DOUBLE-DUTY case: correct while RBP==STANDING, WRONG once a `*` activation is live between writer and reader — MEASURED s95: `probe/mrbp/mrbp_result_capture` X=[] (oracle [ABC]) both modes, `mrbp_result_value` V=[] m4 / silent m3; the trivial DEFER-adjacent captures pass. NEXT = R-4(a) SLICE 3 = the FENCE dynamic-depth-body watermark class (`H08 H29 t6m f6d`, m3-only: FENCE(ARBNO(...)) / FENCE(... | ABORT) — MEASURED in-slab gdb after `rt_match_enter`: α stores the quad at rsp=..9aa0, the σ load runs 16B deeper (ARBNO's committed cell), reads 0, RSP:=0) — DESIGN OF RECORD (Lon s95 correction in-chat: only `S ? P` and `*P` establish frames — FENCE1 does NOT push a frame): the watermark is a BB RESULT value whose committed frontier at σ is DYNAMIC (K16 ARBNO commits kk+16/instance, ALT carves 32B on the taken arm), so no RSP-relative offset is correct; it rides a SLOT IN THE CURRENT ACTIVATION FRAME `[rbp+off]` = the shared registry (`fence_frame_candidate()` container-in-body → `fence_frame_slot()` third customer of `frame_slot_scan` → `op_fence_frame_off`; α `mov [rbp+off],rsp`, every release `mov rsp,[rbp+off]`, K=0 bodies keep the RSP quad byte-identical) — the σ edge is a sync point that discards suspended inner frames first (FRAME LAW), so RBP is the α frame on all three release edges. That code was written s95, then REVERTED unbuilt on a MISREADING of the ruling (the registry slot IS the activation slot under double duty) — RE-LAND IT AS-IS; the only limit is R-4(b): a FENCE inside a stored blob has no MATCH_BEGIN in its graph and must be sized into the DEFER activation. Then R-4(a) per-iteration ARBNO activation absorbs SLICE 1's body-interior captures as ACTIVATION slots (`D12 D13`), R-4(b) `H31`/`fence_probe`. ⛔ PUSH: credential asked in chat at session end.**
- **s95 (Fable 5, 2026-08-15) bb_probes CLIMB, MONITOR-FIRST throughout.** Setup: x64 oracle cloned + alive, gdb 15.1, goldens `mkrefs.sh --verify` 166/166 current. Raw baseline both modes: m3 143/166 · m4 146/166, every failure a crash/bomb (no silent DIFF); 15/20 m4 failures ONE class = `IR_MATCH_CAPTURE_SAVE: no home` for a capture INSIDE an ARBNO/FENCE body (adjacent-sibling discrimination: `ARBNO(LEN(1)) . W` passes, `ARBNO(LEN(1) . W)` bombs; `.` vs `$` irrelevant); asm: the K16 arm counts the body SAVE's 16B into kk, the SAVE box carves it then bombs — planner denies the body interior like the ALT interior. **SLICE 1 (`0053928b`)**: `cap_in_repeat_body()` beside `cap_in_alt_arm()` at THE ONE AUTHORITY `frame_need_of()` (ARBNO span `[operands[1]..operands[2]]`, FENCE1 span `[operands[0]..operands[1]]`, the containment idiom) → +9 m3 / +10 m4, ZERO regressions by set; REAL `.so`-swap md5 radius 21/513 (crosscheck+probe/bb+probe/m1), every mover an ARBNO/FENCE-body capture, crosscheck movers `066/158/163/166` FF→PP both modes (163 = inner ALT in the body; X03/X05 nested ARBNO — oracle byte-exact). Semantics = SPITBOL conditional-assignment law (manual pp.62-63; Ch.18 p.207 each ARBNO instance a distinct stacked choice point). ⚠ HOME TIER: the shared registry = STANDING under DOUBLE DUTY (see cursor ruling); the true home is R-4(a)'s per-iteration ARBNO activation. **SLICE 2 (`683ac8f4`)**: FENCE-interior probes went bomb→SEGV; gdb + normalized asm diff H14 vs H16: FENCE1 α stores the watermark RSP-relative, the body's committed 16B is still live at the σ commit, `mov rsp,[rsp+176]` read 16B under the quad → RSP garbage → r12/r13 trashed at MATCH_END (subj=code addr) → movaps fault in libc under `rt_dcap_end_ok_open`; MATCH_END drain byte-identical to the passing sibling (exonerated). Fix: `op_fence_body_kk` (ΣK via `zd_k`, struct-end per s141 ABI law), `fence_release(off,kk)` on the σ edge only (+ its rbp-unwind twin), PAIR(3)/β unchanged (unwind law) → +6 m3 / +6 m4 incl. H10 and f6d(m4); radius 8/513, all FENCE-body, zero crosscheck movers. Retracted en route: a `break *addr` on the not-yet-mapped RX slab silently hit ld.so (all readings before anchoring on `rt_match_enter` were artifacts) — anchor in-slab breakpoints on a runtime symbol first. corpus `e50f176f`: XFAIL baseline re-cut deliberately (old list = fence_probe/A06 while 23 failed): gate exit 0 both modes, XPASS loud. REGENS ×3 ran (no byte changes). Manual read for this session: Ch.6 pp.55-79 in full (alternation, `.` vs `@`, REPLACE pp.72-73), Ch.18 pp.203-208 (the algorithm's push = alternative+cursor; ARB = implicit LEN ladder). OWED: push (credential asked in chat).
- **s94 (Fable 5, 2026-08-15) R-1 LANDED (SCRIP `03deed84`): THE TINY CROSS-CHAIN CROSSING, BOTH MEDIA.** R-1(a) merged `origin/s91-m3-unify` (`cc5711fc`); R-1(b) lifted all six medium-broken reference classes in the TINY world (not two as the goal file named — the shim itself was TEXT-gated, shim→body and site→alpha cross chains in opposite directions so one was always a forward ref in binary, shim's own γ/ω faces, SIG blob lea, `.quad <label>` emitted the name-string address silently). New `[rip@cell + __]` operand kind (`x86_jmp_via_cell`): TEXT byte-identical to before (m4 A/B 59/59 same), BINARY through a stable cell of the ONE allocator (`bb_ab_fn_cell_ptr`, now `extern "C"`), filled at each stub seal and post-main by `m3_seal_entry_cells()` (ONE authority, no new global). `x86_lea_ext` for same-box refs; `x86_quad_ilbl` + `PATCH_ABS64` + `bb_emit_patch_abs64` for `.quad L(n)`. `AB_FNCELL_MAX` 256→1024 with loud abort (old arm aliased slot 0 silently). Gates: `m1_define_return_m3` PASS m3; m1 witnesses 6/7 both modes; crosscheck full 318 both modes: m3 PASS 233 vs m4 232 (baseline s93: 159/178 of 195); beauty_suite 17/17 m3≡m4 SIG11/SIG11 (m3 could not link before). R-2 first divergence minted (see LIVE CURSOR). Oracle md5 discrepancy flagged for Lon's ruling. Manual Ch.8 pp.102-104 read. Push credential supplied by Lon in-chat.
- **s93 R-0 (landed, gated):** `frame_need_of()` (THE ONE AUTHORITY, emit.cpp) returns 1 for a COND/IMM whose SAVE..COND span lies on an ALT arm γ-chain — new pure `cap_in_alt_arm()` walks every `IR_MATCH_ALTERNATE`'s `(entry_j,resume_j)` operand pairs (`zd_chase` γ steps, plan-independent, no new global); SAVE propagation + `capture_frame_slot()` follow, so the s87 frame-slot arms carry it. The three port-less capture bombs (SAVE/COND/IMM `neither op_zres nor op_cap_anchor`) now DEFINE β. Why structural, not `!havehome()`: `zd_plan` grants per RUN (emit.cpp ~2323), so "would the plan grant this node" is not a per-node pure question; the arm interior is the whole denied class (movers = every ALT-interior capture, as predicted). **GATES:** m1 witnesses `m1_alt_{arm1,arm2,arm2_capfn_var}` PASS m3+m4 (were `n8_match_assign_cond_β` link failures both modes), controls PASS; blast radius m4 md5 41/626 over crosscheck+probe; BY-SET old-vs-new both modes over all 41 movers: **0 regressions, 11 m3 / 12 m4 repaired**, 18 now LINK but fail at runtime — censused: 15 `IR_MATCH_CAPTURE_SAVE: no home` = the **ARBNO-body** capture class (ARBNO uses the same (entry,resume) pairs; a single STANDING slot is UNSOUND there — a body that backtracks internally after a later iteration's SAVE overwrote the slot re-reads the wrong start — so R-4(a)'s ARBNO activation must be pushed PER ITERATION (LIFO), not per site: 158/163/166_pat_arbno_*, 066_capture_then_fenced_arbno, bb probes N03/N04/N13/N14/N17/N20/X03/X05/D10/c4/c4b/dc_recur/fence_probe), 2 `hazard crosses a DEFER … no enclosing MATCH_BEGIN` = R-4(b) stored blob (`m1_alt_cap_stored` re-routed there: honest runtime bomb, links), 1 silent stdout diff. Scorecard (`test-results/scorecard-s93-r0`, 4 suites): beauty_self 0/1 **COMPILE_FAIL/SIG11** (m4 NOW LINKS and runs → SIGSEGV = R-2's first divergence; m3 = R-1 RETURN class) · beauty_suite 0/17 (15 COMPILE_FAIL/SIG11 + 2 SIG11/SIG11 — every driver links in m4 now, was ASM_FAIL) · demos 5/23 unchanged · patterns 69/69 of 122 (+1 both modes; SIG6 18→20 / SIG11 17→18 are ex-link-failures now bombing honestly). Manual read for this rung: alternation pp.57-58, conditional assignment pp.62-63.
- **s93 (Fable 5, 2026-08-15) ACCOUNTING PASS over the 122 surviving `GOAL-*.md` (grep census + source check):** 3 more SNOBOL4-x86 files absorbed + deleted (ledger): `GOAL-ZETA-FOUR.md` (OBS — THREE ZETAS is the one config; live `SCRIP_ZETA_STORAGE`/`--zeta-storage=` selector → R-7), `GOAL-SUBEXPR-ORACLE.md` (tool exists: `test/beauty_subexpr_gen.py`+`subexpr_oracle.py`; scorecard is THE INSTRUMENT), `GOAL-DATATYPE-PORTABLE-TESTS.md` (OBS — sbl is the oracle; SPITBOL DATATYPE spellings are truth). **ALL `FINDING-*.md` (314 .github + 2 SCRIP) and `HANDOFF-*.md` (3 .github + 9 SCRIP) DELETED per Lon** — text in git history at `.github 1ccb3e59` / `SCRIP 31b5b17b` and before (`GOAL-RTCC.md`/`GOAL-MODE34-IDENTICAL.md` still name a few; those names now resolve to history). NOT absorbed, Lon to rule on deletion: `GOAL-NET-*` ×6 (snobol4dotnet repo), `GOAL-README-SNOBOL4*` ×5, `GOAL-SILLY-*` ×4 (monitor participant), `GOAL-STCOUNT-ALL-LANGS.md` (all-language; SNOBOL4 side = R-5); near-miss logs kept (`HANDOFF.md`, `SESSION-*-HANDOFF*.md`, `ICON-BB-HANDOFF-*`, `NOTE-2026-08-13-s6x-*`, `SB-*`, `HQ-*`). Manual read for R-0: alternation pp.57-58 (arms left-to-right, backtrack into the next), conditional assignment pp.62-63 (assigns ONLY when the WHOLE match succeeds; any nesting depth) ⇒ an ALT-arm-interior `.` needs storage that outlives the arm's β and is dropped on backtrack — the STANDING slot R-0 prescribes.
- **s91's `n7498_match_assign_cond_α` never-emitted class REDUCED (verified m3+m4 at `2a6a76ab`):** `'b' ? 'a' | ('b' . Y)` → m3 `bb_emit_end: unresolved forward reference n8_match_assign_cond_β` (rc134) · m4 `undefined reference to n8_match_assign_cond_β`. Arm position, literal-vs-variable subject, stored-vs-inline, `LEN(1)` body: all irrelevant. Controls PASS: `(A|B) . Y` and `(A . X) B`. Witnesses + oracle refs banked `corpus/probe/m1/` (7 files + README). Emitted asm: `n8_match_assign_cond_α: lea rdi,[rip+.S1]; call rt_bomb` with `.S1 = "IR_MATCH_CAPTURE_COND: neither op_zres nor op_cap_anchor granted a home -- the legacy C rt_cap_top array this used to fall back to is deliberately not rebuilt"` (`bb_match_capture.cpp` COND phase-1 fall-through after `havehome()` fails; the arm emits α+bomb only, so β/ω are never defined, and `bb_match_alternate.cpp`'s arm glue jumps to `_β`). Provenance: s83 (`d814a826`) gutted the six capture arms + the legacy `rt_cap_top` fallback, s83b (`3232ece9`) rebuilt them for the spine/frame homes only; the ALT-arm-interior capture is a DENIED walk (`fc_walk_range` — s66 ALT-CAP / s71 (7) "ungranted ALT arm" denial class) that used to ride the deleted C fallback. **THE beauty family (16/17 beauty_suite, beauty.sno itself) fails on this ONE class in both modes.**
- **s90's RETURN class re-verified:** six-line `DEFINE`+`:(RETURN)` → m3 COMPILE_FAIL (rc134, 2 unresolved `RETURN`), m4 PASS. Fix is on branch `origin/s91-m3-unify` (`fd339df4`, one crossing short — see R-1).
- **THREE ZETAS invariant re-verified at HEAD** (table above). Killswitch census: **~230 distinct `getenv("SCRIP_*")` knobs in `src/`** (R-7). Legacy: `g_blob_ctx`/`zvo_uclaim_k`/`g_vstack` = 0 hits (M-1b/M-3 DONE); `g_star_peek` 1 hit (DEFER LATCH still live); `rt_keyword_read_snobol4` 6 hits (keywords still C); `rt_match_enter` 33 hits (M-SLEN not done); `SCRIP_AB`/`SCRIP_CALL2BB` present (LADDER AB not flipped).
- OWED: R-0 fix + blast radius · push credential (asked in chat at session end).

---

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity, md5 blast radius, named witnesses vs live oracle, cursor move, push mid-session

- [x] **R-0 · M1 ROOT CAUSE (LANDED s93, SCRIP `1aaf1a11`; ARBNO-body + stored-blob residue → R-4(a)/(b)) — GIVE THE ALT-ARM-INTERIOR CAPTURE A HOME (THREE ZETAS: the STANDING frame slot).** Witnesses `corpus/probe/m1/m1_alt_*` (4 red, 2 controls). Under the model a capture with no spine home (`!havehome()`) does not bomb — it registers a slot in the CURRENT live RBP frame exactly like the s87 SAVE arm does (`capture_frame_slot()` → `op_cap_frame_off`, currently registered ONLY when `op_frame_need`; extend registration to every COND/IMM/SAVE whose walk was denied — `op_frame_need || !havehome()` — one authority, no new global) and reads/writes `[rbp+op_cap_frame_off]`; ALT keeps its own 32B α carve (s61) untouched. Then delete the α-only bomb arm (a bomb that leaves β undefined is a LINK BUG, not a decline — never emit a port-less bomb where a sibling references the port; if a decline is ever legitimate it must still define every referenced port). GATES: `SCRIP_MATCH_RBP=0`… byte-identity is N/A (no killswitch for a bug fix); md5 blast radius over probe+patterns+crosscheck (expect movers = every ALT-interior capture); m1 witnesses PASS both modes; bb_probes ≥156/163 by set; then re-run scorecard beauty_suite/patterns/demos. **THIS RUNG ALONE IS PREDICTED TO MOVE beauty_suite m4 from ASM_FAIL and beauty.sno m4 to a runnable link.**
- [ ] **R-1 · m3 UNIFICATION (the s62-owed rung, s91 built 90%): merge `origin/s91-m3-unify` + the tiny call-site BINARY crossing.** Branch delta (verified): `scrip.c` −18/+, `emit.cpp` −14/+, `bb_pool.h` (FLAT_BUF_MAX 1→4MB, pool 16→64MB); floater labels reset with the label pool; `_stmt_seed` `g_is_text` gate lifted (m3 mains statement-order like m4); m3 driver stops emitting LBL__ pseudo-procs standalone, registers LBL__ rows from main's label pool. GATE at branch: 60 non-DEFINE crosscheck m3 same/0 broke; RETURN class gone. **THE MISSING CROSSING:** `bb_call_proc_staged.cpp:280/549` TINY arm is `MEDIUM_TEXT &&` (BOTH-MEDIUM violation) — binary sites fall to the SLIM by-name arm and enter the inline body with a protocol the RETURN floater's `pop rcx; jmp rcx` does not match ⇒ SEGV. Fix WITHOUT a new global: record `<FN>_alpha` (and γ/ω) absolute addresses at each binary `emit_chain` seal via the existing runtime proc registry (`rt_proc_set_fn("<FN>_alpha", addr)`), consult at the site (`movabs rax,<abs>; jmp rax`), lift the two `MEDIUM_TEXT` conjuncts; `.quad <label>` data under SIG mode must be binary-capable. Ordering is fine (stub chains emit before main). GATES: `m1_define_return_m3` PASS m3; crosscheck m3 COMPILE_FAIL 23→~0 BY SET; every DEFINE-bearing probe m3 old-vs-new. **This rung alone collapses the m3 COMPILE_FAIL column across beauty_suite/demos/feature_test/crosscheck/benchmarks.**
- [ ] **R-2 · MILESTONE 1 — beauty.sno self-host both modes, md5 `abfd19a7…`; beauty_suite 17/17.** After R-0+R-1: m4 emits 168k lines (`.text` 935KB) — link, run `< beauty.sno`, diff vs oracle 622 lines; m3 needs FLAT_BUF_MAX 4MB (R-1). Characterize the first divergence with MONITOR-FIRST; each defect found here becomes its own R-3 line with a minted `probe/m1/` witness. Update PLAN.md Milestone 1 line with the date + hash when green.
- [ ] **R-3 · SCORECARD CLIMB TO 100 — one failure CLASS per commit, class named by mode-pair, witness minted, oracle-verified.** Known classes at baseline (re-measure, never inherit): **(a)** demos SIG6/SIG6 ×10 (uncharacterized — candidates: `[ZHP] heap exhausted` rc134 = deleted RBX X-4 / LOWER L-2 hang-masking class; `FLAT_BUF_MAX`); **(b)** patterns SIG6 ×18 / SIG11 ×17 both modes; **(c)** probes_misc DIFF ×18 / TIMEOUT ×8; **(d)** csnobol4_suite RC1 ×18 / COMPILE_FAIL ×17 / DIFF ×11 (SPITBOL-vs-CSNOBOL4 semantic differences must be classified oracle-blessed-XFAIL vs real); **(e)** benchmarks SIG11 ×3 both modes + REPLACE-form (string_pattern/mixed_workload/roman), EVAL (eval_dynamic/eval_fixed), cap_imm_nret×2 (s68 shape classes); **(f)** carry-forward defects with banked witnesses, each re-measured first: `.` capture crossing a DEFER binds NULL silently (`probe/mrbp/mrbp_result_{control,value,capture}` — s71 root cause `fc_walk_range` has no `IR_MATCH_DEFER` case; RULING WANTED: RESULT rides a STANDING slot with a LOWERER-side mark, `IR_MATCH_VALUE` is DEFER's sibling and must be covered too — never a summing whitelist admit) · H10 regression `FENCE(ARBNO(LEN(1)) $ OUTPUT) RPOS(0)` SIGSEGV since `64cf0a7c` (s89 bisect) · `probe/rtx_func_11_include` (−INCLUDE'd loop body leaks 0xCC0 RSP/iteration; identical bytes inline are clean — X-2/RTX-FUNC-11) · `probe/dv/dv_defer_nested` (s81 candidate fix banked as WIP patch `635b5685`, unproven) · `140/141_pat_eval_double_fn_*` (DEFER LATCH `g_star_peek` one-entry latch, `pattern_match.c`; m3 rc139/m4 PASS) · `test_string` second component (LOWER L-5) · L-1 producer elision (`SCRIP_PAT_INLINE=0` convicts) + L-2 the hang it masks · `1016_eval` compiler SEGV (EVAL×DEFINE×splice) · `fencedef1`/`repldef1` (fence/replace + DEFINE) · `arbdef1` ARBNO-in-DEFINE · `disc_positive` (`*cmd` fence family, `osv=1 mb=-1`) · `1013` NRETURN site deref · `expression.sno` COMPILE · `porter` dup-label LINK. **(g)** gimpel/lon/misc suites: run once, classify.
- [ ] **R-4 · THREE ZETAS COMPLETION — (b) LANDED s97 SCRIP `be18fcb6` (blob = its own activation; entry wires + registry slots in the blob frame; D12/D13/H31 + 27 patterns + 3 demos repaired, 0 regressions) (the design of record, landed to the last construct).** (a) **dynamic-K ARBNO ACTIVATION FRAME** — the `bomb-defer-unsafe` decline (`bb_match_arbno.cpp:312`, bodies containing `IR_MATCH_DEFER`/`IR_MATCH_ALTERNATE`) and the `slot not granted`/`geometry not staged` bombs (`:297/:299/:415/:417`) become one RBP activation per ARBNO **site** (one RESULT + one δ-cursor LOCAL, reused across that site's retries — s80(3)), chained off STANDING per the FRAME LAW; static-K keeps `frameless_k16`. This unblocks json/treebank/calculator/claws5/183/165/178/179/182/145 (s75 count: 46 of 95 sites bombed). (b) **PAT$N stored-pattern-blob class** — captures/ARBNO inside a separately-compiled pattern blob (no enclosing MATCH_BEGIN in that graph, `op_*_frame_off==-1`) currently `x86_bomb`: the blob's activation must receive STANDING (copied-down pointer at α — s81 "keep passing it down", no register); (c) Defect C rebase — the s137 defer save/restore onto the CALLING activation's RBP slot (s80(4)); (d) start-δ carve-less divergence (witness-free — mint before ruling); (e) `emit_match_rbp()`/`emit_arbno_rbp()` comment bodies updated in the same commits (they still narrate retirement). (f) **[LANDED s96 SCRIP `6c22cd46`] FENCE1 watermark = a SLOT in the CURRENT ACTIVATION FRAME** (Lon s95: FENCE1 pushes NO frame — only `S ? P` and `*P` do; witnesses `bb/probes/{H08,H29,f6d}`, `bb/t6m`): the dynamic-depth-body class (ARBNO/ALT/container in the body) cannot read the watermark RSP-relative at any static offset; `fence_frame_candidate()` → `fence_frame_slot()` (third customer of the shared `frame_slot_scan` registry) → `op_fence_frame_off`; α `mov [rbp+off],rsp`, releases `mov rsp,[rbp+off]` (σ discards suspended inner frames first); K=0 bodies byte-identical. Written s95 and reverted unbuilt on a misreading — RE-LAND AS-IS. (g) SLICE 1's body-interior captures move from the double-duty STANDING slot to the per-iteration ARBNO activation slot when (a) lands (their `frame_need_of` verdict is the entry ticket; only the frame changes). GATES per FRAME LAW: killswitch byte-identity, md5 radius, `probe/arb1 arbdef1 fence_arbno_top dv/* mrbp/*` both modes, nested witnesses `probe/bb/probes_x01_witness_s39/X01W*`.
- [ ] **R-5 · RE-ENTRY EDGE INVENTORY + THE SUMMIT (EVAL/CODE/EXEC, NRETURN, `:<C>`, SETEXIT, &FENCE, data + keywords).** Manual Ch.9 pp.130-135, Ch.19: each non-local transfer gets a witness that crosses STANDING/ACTIVATION frames and the R12 arena and must restore both (backtrack β per choice class · ω unwind · FENCE whack · scanfail · ABORT drain · unanchored retry bump). Data types (ARRAY/TABLE/DATA/field, ITEM, SORT/RSORT, CONVERT), keyword reads/writes (`&STLIMIT &DUMP &PROFILE &ANCHOR &TRIM &FENCE`), runtime DEFINE/APPLY/OPSYN — correctness only (native/zero-C keyword and call ladders are PARKED below); the scorecard suites decide what is missing.
- [ ] **R-6 · m3 ≡ m4 + INSTRUMENT DEBTS THAT BLOCK VERDICTS.** Every scorecard row m3 ≡ m4 by construction (`GOAL-MODE34-IDENTICAL.md` M34-5 "LOUD-IN-M4/SILENT-IN-M3" class is this file's concern for SNOBOL4: any m4 assembler/link failure is a missing m3 binary arm or vice-versa). MON-CAP (monitor sees stdout-only divergence + table-element-assign VALUE events; `TOKEN_SEE_LON` credential for csnobol4 was the blocker — ask in chat). Regen freshness canary in `handoff_status.sh`.
- [ ] **R-7 · DELETIONS + KILLSWITCH FOLD (last; hygiene, byte-inert by construction).** Delete: `bb_match_arbno_rbp()` (0 callers) · `bb_save_restore.cpp` `fnrbp()==1` arms + `SCRIP_FN_RBP`/`SCRIP_FN_SIG` knobs (unreachable) · the α-only bomb arms retired by R-0/R-4 · dead `[rsp# + 0]` ternary in `bb_match_defer.cpp` · raw-byte push/pop in `emit.cpp:2688-2724` (TEMPLATE-ONLY violation, ex-WIRES W-9) · `.Lbynamefn*` duplicate-label emitter · every killswitch whose OFF arm is legacy (`SCRIP_MATCH_RBP`, `SCRIP_ARBNO_RBP`, `SCRIP_ARBNO_FRAMELESS`, `SCRIP_ZD_*` family, `SCRIP_ZW*`, `SCRIP_CALL2BB`, `SCRIP_AB` when AB flips, `SCRIP_ZETA_STORAGE`/`--zeta-storage=` 4-way selector + its non-default arms (`zeta_alloc.c:269/283`, `zeta_choices.h:129`, `scrip.c:470`; FRAME_R12 retired s23k, CELL_HEAP parked Z4-8; ex-`GOAL-ZETA-FOUR.md`)…): fold-or-die, one family per commit, byte-identical OFF→deleted. Keep ONLY diagnostics that print and never change bytes (`*_DIAG`, `*_TRACE`, `SCRIP_OPT` emergency). Target: `getenv("SCRIP_*")` census 230 → the sanctioned list written here at fold time.

- [ ] **⛔⭐⭐ R-8 · OPSYN OPERATOR IDENTITY + THE COERCION SHAPE REVERSION (Lon in-chat 2026-08-15 s105; two halves of ONE fact: an operator's meaning and its coercions belong to the OPERATION, never to the call site).** Opened on a measured census, not a hypothesis. **THE LAW (manual pp.115-116, Ch.15 pp.181-183, VERIFIED against live `sbl` this session):** SPITBOL splits the operator space in two and the halves get OPPOSITE treatment. (i) The USED operators (`+ - * / ** | . $ @ ? =` …) **CANNOT be OPSYN'd** — `OPSYN('+','PLUS',2)` is **ERROR 156** *"OPSYN first arg is not correct operator name"* — and the manual states the reason verbatim: *"by not allowing basic system functions and operators to be redefined, SPITBOL is able to optimize the code it generates."* Static arithmetic identity for these is therefore SANCTIONED BY THE LANGUAGE, not a shortcut. (ii) The UNUSED operators — binary `& @ # % ~`, unary `! % / # = |` — are OPSYN-able **at runtime, repeatedly**, to any built-in or program-defined function. They therefore carry **ZERO** static identity: no arithmetic kind, no coercion, no precedence-implied meaning. ⛔ **MEASURED DEFECT TABLE (s105, oracle-diffed, repros in `/tmp` → mint to `corpus/probe/opsyn/`):** with `OPSYN(<op>,'cat',2)` and `cat(a,b) = a b`, oracle prints `abcdef` / `x5` for every row. MEASURED against the minted witnesses (`corpus/probe/opsyn/`, all 8 RED at SCRIP `ba53a207`+MV-glue): `&`→**SIG11** · `@`→**SIG11** · `~`→**SIG11** (these three DO reach the correct `TT_OPSYN` arm, `snobol4.y:129/138/164` — the dispatch itself is broken) · `#`→**exit 0, prints `0`** · `%`→**exit 0, prints `0`** (silent WRONG ANSWER, the worst class) · `#`→`DIFFER` builtin target→**rc=1** · unary `!`→`ANY`→**rc=1** · rebind-twice→**SIG11**. `#`/`%` are wrong because **the grammar hard-wires them to ARITHMETIC**: `snobol4.y:145` `T_2POUND → expr_binary(TT_MUL)`, `:154` `T_2PERCENT → TT_DIV`, `:176` `T_1BANG → TT_POW`, `:177` `T_1PERCENT → TT_DIV`, `:178` `T_1SLASH → TT_DIV`, `:179` `T_1POUND → TT_MUL`. Six arms, filled in from the precedence table (unused `#` sits at priority 7 beside `*`, `%` at 10 beside `/`), mistaking SAME PRIORITY SLOT for SAME OPERATION. The `0` is the coercion signature: both strings coerced to numeric 0 and multiplied. Conversely SCRIP **silently accepts** the illegal `OPSYN('+','PLUS',2)` and prints `3` where the oracle raises 156 — permissive where it must be strict, static where it must be dynamic. **Blocks `semantic.sno` (`OPSYN('~','shift',2)`, `OPSYN('&','reduce',2)`) ⇒ on the beauty path, R-2-adjacent.**
  - **(a) GRAMMAR — the eleven OPSYN-able operators lower to `TT_OPSYN` and nothing else.** Delete all six mis-mapped arms above; every unused unary/binary symbol becomes `expr_unary/expr_binary(TT_OPSYN, …)` carrying its symbol in `v.sval`, exactly as `&`/`@`/`~` already do. Precedence/associativity rows stay as the manual pins them (binary `&` 2 left · `@` 5 right · `#` 7 left · `%` 10 left · `~` 13 right; unary all equal, right-to-left) — precedence is SYNTAX and is fixed; only the OPERATION is dynamic. **Regen law: `bison`/`flex` output is the committed artifact — regen and confirm byte-identical BEFORE the grammar edit, then edit and regen (REPO-SCRIP.md).** GATE: `--dump-ir` on each of the eleven shows a dynamic dispatch node, ZERO `IR_BINOP`/`IR_COERCE_*`.
  - **(b) DISPATCH — fix the `TT_OPSYN` runtime path that SIG11s even where correctly wired** (`lower_snobol4.c:386`). An OPSYN'd operator is a by-name call to a target that may be a built-in, a program-defined function, or another operator, rebindable between any two statements: it must ride the SAME by-name machinery s104 built for computed targets (`rt_dyn_alpha_fn`, sealed-alpha cells, `rt_call_proc_descr` dyn arm) — ONE mechanism, not a second. Operand count comes from the OPSYN third arg (1/2), NOT from the symbol. No new global (registry rides the existing proc registry).
  - **(c) ERROR 156 — reject OPSYN of a used operator, first-arg check, at the `OPSYN` builtin** (`lower_snobol4.c:2410` is the compile-time arm; the check is RUNTIME because the prototype string can be computed). Oracle-pinned text; witness both modes.
  - **(d) ⛔ COERCION SHAPE REVERSION (Lon ruling, verbatim in substance: *"revert IR_COERCE_NUMERIC anyway, it produces code bloat for not much gain … we're back to just a straight RT call for each"*).** The s211 design (`DESIGN-SN4-NUMERIC-COERCION.md`) hoisted coercion OUT of the operation into per-operand-edge `IR_COERCE_NUMERIC` nodes; SNOBOL4 shipped that hoist ONLY on the relop/predicate path (`sx_pred_cmp`, `lower_snobol4.c:181-182` — two coerce nodes per `GT/LT/EQ/NE/GE/LE`). **UNWIRE IT: one RT call per operation, coercion folded inside.** The RT call does its own tag test with the bit trick the design doc already proved free (§3.1): `DT_I=6=0b110`, `DT_R=7=0b111` differ in exactly bit 0 and are the only numeric tags, so `mov eax,[a_tag]; or eax,[b_tag]; test eax,1` decides int-vs-real in two instructions — **no conversion call when none is needed, and NO four-way INT/REAL combinatorial inlining (Lon: "too much")**. ⛔ **DO NOT touch `lower_icon.c:342/343/368`** — Icon's arithmetic legitimately uses these nodes and is a different language with a different goal file. `IR_COERCE_NUMERIC` + `bb_coerce_numeric.cpp` (65 lines) survive for Icon; only the SNOBOL4 edges unwire. ⛔ **PRESERVE THE POSITIONAL ERROR CONTRACT** — the hoist's whole correctness payload: each operand validates SELF and raises ITS OWN oracle-pinned code, **first position first** (`GT('x','y')` → **111** *"GT first argument is not numeric"*, never 112; manual p.271 error list, one code pair per relop). The folded RT call must test operand 0 and raise before it looks at operand 1; a witness per relop × per position, both modes, or this step is not done. GATE: killswitch byte-identity is N/A (shape change) ⇒ md5 blast radius over crosscheck+patterns+probe with a BY-SET old-vs-new, ZERO PASS→fail; `.text` size delta reported (the bloat claim is the rung's premise — MEASURE it, do not assert it); relop witnesses green both modes.
  - **(e) WITNESSES + GATE SCRIPT.** Mint `corpus/probe/opsyn/` with a `.ref` beside each from live `sbl`: the eleven operators × {program-defined target, built-in target}, rebinding mid-program (same operator OPSYN'd twice), the illegal-`+` 156 case, arity mismatch, and an OPSYN'd operator whose operands are non-numeric strings (the coercion trap — this is the `#`→`0` witness). Add `scripts/test_gate_opsyn_no_static_identity.sh`: greps `snobol4.y` for any unused-operator symbol mapped to a `TT_` arithmetic kind == 0, and runs the probe set both modes.

**PARKED — PERFORMANCE / ARCHITECTURE, AFTER 100% (law + history in git; do not open before DoD 1-2 are green unless Lon directs):** RTX asm runtime ladders (RTX-4/6/7/9/10/12, RTX-FUNC-1..11 zero-C call path, `bench_rtx_3arm.sh` min-of-N rail) · RTCC RC-5/6/7 (`GOAL-RTCC.md`, kept) · LADDER AB flip (AB-3..6: `jmp [fn_cell]` call sites, runtime-DEFINE generic twin, static-direct fold; the RTX-FUNC-8 expression-position-DEFINE hatch) · LADDER KW native keywords (KW-0..6, `rt_keyword_read_snobol4` → emitted statics) · M-SLEN (populate `DESCR_t.slen`, `rt_match_enter` deletion, register-only MATCH_BEGIN α) · RBX inline bump-alloc arms (X-3) · OS-0..OS-FINAL ONE-SYSTEM addressing consolidation (24 `fc_*` + compensation terms — collapses naturally as R-4 re-homes storage; finish as hygiene) · MECH M-4 glue unification · `-O2` runtime pass (O2-DIRECTED-ONLY).

---

## LEDGER — EVERY SNOBOL4 GOAL FILE, EVERY RUNG, DISPOSITIONED (verified against SCRIP `2a6a76ab` unless marked INHERITED)
Legend: **DONE** (verified in tree) · **→R-n** (absorbed into rung n above) · **OBS** (obsolete — superseded by the THREE ZETAS ruling, by a later landing, or by the scorecard) · **PARK** (perf/arch, listed above) · **INH** (inherited claim not re-verified this session, re-measure before spending).

**GOAL-RBP-EARN.md (981 lines, s29–s91):** EARN ladder 0–11 → superseded by the s85–s88 THREE ZETAS landing (DONE: STANDING `emit_match_rbp`, FRAME `emit_defer_rbp`, unified slot registry `frame_slot_scan`, capture transient-rbp fallback deleted, ARBNO-RBP epilogue E-1/E-5 unwind, function-linkage RBP deleted s78, statement goto-arm whack S-1 landed s78, start-δ home `emit_match_owns_startd` s70, MATCH_SPAN eax clobber, wire-save, bare-TT_VAR seal); open: RETURN class → **R-1**; `match_assign_cond` never-emitted → **R-0**; H10 regression, `.`-across-DEFER null bind, dv_defer_nested, 181-m4/OS-2 environ-smash quartet, disc_positive, fencedef1/repldef1/arbdef1, 1013, 1016 → **R-3(f)**; PAT$N blob class, dynamic-K ARBNO, Defect C rebase, start-δ witness → **R-4**; mrbp shallow-main widening → OBS (STANDING push is unconditional at `bb_match_begin.cpp:48`); replace-family adoption / orphaned head.capgen_save/zls2/cas slot reclaim / dead defer ternary → **R-7**; s77 per-retry-chain design, s80(2)-(4) RBP ladder → OBS (retracted in-file); crosscheck `.s` regen debt → REGENS law; RESULT/LOCAL frame widening 64→80 → **R-3(f)** ruling line.
**GOAL-SN4-HOME.md (master):** register contract → THE MODEL; HOME GATE 1–7 → DoD 1–5; five-seat execution model / collision pins / THE POOL / LON QUEUE → OBS (SOLO: one file, one seat, Lon fires it); ADOPTED ledger (KW→PARK, DEFER LATCH→R-3(f), M-SLEN→PARK, C-10/C-11→R-5, RTCC residue→PARK, RTX perf→PARK, PB CLOSED, bisect fallback→OBS, Milestone-2 OUT OF SCOPE) — restated above.
**GOAL-SN4-HOME-LOWER.md:** L-3b, L-4/061 DONE (in ledger text); L-1 Defect A, L-2 Defect B, L-5 test_string, L-6 `fc_walk_range` second-instance audit → **R-3(f)** (and R-0 IS an `fc_walk_range` denial instance).
**GOAL-SN4-HOME-WIRES.md:** W-0/W-1/W-2 DONE (`g_blob_ctx` 0 hits verified); W-3 WREG live-uncontrolled → **R-4/R-3** (defer crash witness `witness_wreg_s39/`); W-4 arena wire-pair slot → **R-4(c)**; W-5 the flip (PROC-shim deletion) → PARK (LADDER AB); W-6 RTCC re-entrant revalidation → PARK/R-6; W-7 ARBNO dispatcher D12/D13 → INH (s86 "TWO BOMBS CLEARED (W-7…)" — re-measure D12/D13 in R-3); W-8 30 RTX occurrences → PARK; W-9 raw-byte push/pop → **R-7**.
**GOAL-SN4-HOME-RBX.md:** X-0 contract DONE (kept as law); X-1 GC coverage DONE (INH s33); X-2 FUNC-11 → **R-3(f)**; X-3 → PARK; X-4 ZHP class → **R-3(a)**; X-5 RTCC residue → PARK; hazards 1-3 → INH, R-4/R-7 audit.
**GOAL-SN4-HOME-BOARD.md:** B-0/B-1/B-13 DONE; B-2 EARN-2 census (`frame_need_of` exists, 14 refs) → OBS (scorecard is the census); B-3 discriminating refs, B-9 mute-instrument sweep, B-11 digits→commands, B-6 canary → **R-6**; B-4 claim gate → DoD 3; B-5 MON-CAP → **R-6**; B-7(iv) 140/141/BREAKX → **R-3(f)**; B-8 RTX instrument debts → OBS (both-mode gate = scorecard); B-12 resumable generator through a DEFER → **R-4(b)/(c)**.
**GOAL-SN4-ZETA-MECH.md:** M-1b `g_blob_ctx` DONE (0 hits) · M-3 UCLAIM DONE (`zvo_uclaim_k`/`g_zd_uclaim` 0 hits) · N21/H31/ZCTX DONE (INH s37) · ARBNO legacy-route defect (s38) → INH, **R-4(a)**; M-2 route the homeless → **R-4(b)**; M-4 glue unification → PARK; M-5 killswitch fold → **R-7**; M-6 frame-minimality/ω audit → **R-7**; M-7 finish → DoD; OS-0..OS-FINAL → PARK (hygiene); OPS-2 MON-CAP → **R-6**; OPS-3 credential ask → RULES (DONE); OPS-4 batching → OBS; S0–S5 M-SLEN → PARK (`rt_match_enter` 33 hits: not done); LADDER ONE-SYSTEM/LIFO laws → THE MODEL.
**GOAL-SN4-ZETA-CLIMB.md:** C-0..C-8 + C-9 TAB/RTAB splice DONE (INH s41, L-3b/L-4 confirm); C-9 residual replacement+splice → **R-3(e)** REPLACE-form class; C-10 → **R-5**; C-11 → **R-5**; C-12 summit gates → DoD 1-2.
**GOAL-SNOBOL4-BB.md (+ALPHA/OMEGA closed pointers):** LADDER PB CLOSED (DONE) · ALPHA A-1..A-9 / OMEGA O-1..O-8 DONE · LADDER W (wholesale α-alloc/ω-free, arm-all flip, frames at framed constructs) → OBS, SUPERSEDED BY THE THREE ZETAS RULING (its intent = R-4; W-1 bugs 1-5 landed; W-2/W-3/W-5/W-6/W-7 not to be executed as written) · LADDER AB AB-0..AB-2 DONE (INH), AB-3..AB-6 → PARK · LADDER KW KW-0..KW-6 → PARK · SN4-UNROLL-DELETE DONE · demo-board 2/15 regression bisect → OBS (scorecard demos suite = 5/23, R-3(a)) · pending Lon rulings → carried into R-3/R-4 ruling lines · MODEL/LAWS (Whack Contract, Unwind, Statement-Port Laws) → THE MODEL + FRAME LAW.
**GOAL-SNOBOL4-RTX.md:** RTX-1..3, 5, 8 DONE (INH); RTX-4/6/7/9/10/12, RTX-FUNC-1..10, rail/hugepage, thin-coverage ports, `rt_defer_step` strike, m4 killswitch-gate arm → PARK/OBS (scorecard runs both modes); DEFER LATCH → **R-3(f)**; RTX-FUNC-11 → **R-3(f)**; RTX-11 → `GOAL-RTCC.md` (kept).
**GOAL-MILESTONE-1-BEAUTY.md:** instrument → THE INSTRUMENT; cursor (1)-(4) → **R-0/R-1/R-2/R-3(a)**; fully absorbed.
**GOAL-SNOBOL4-PAT-IR.md (2026-05-31, pre-BB):** pattern primitives as IR nodes — DONE (40 `IR_MATCH_*` kinds in `src/contracts/IR.h`); S-1..S-8 OBS.
**GOAL-LANG-SNOBOL4.md (3042 lines, 2026-06-27, SM/mode-2 era):** OBS — modes 1/2 DELETED (PLAN.md Architecture); open SN-33d/e (mode-4 baseline/correctness) and SN-26-bridge-coverage-h/r (beauty 2-way, pattern-block type discrimination) → subsumed by the scorecard + **R-2**.
**GOAL-PARSER-SNOBOL4.md** 10/10 DONE · **GOAL-PST-SNOBOL4.md** COMPLETE (2026-05-19) · **GOAL-SNO-CLAWS5.md / -TREEBANK-ARRAY / -TREEBANK-LIST** all closed (their programs are the `demos` suite → **R-3(a)**) · **GOAL-SCRIP-BEAUTY.md** (S-6..S-14 beauty-driver fixes, 2026-06-27) → **R-2** (the 17 drivers are the `beauty_suite` suite; the specific defect names are stale — re-derive from the scorecard).
**GOAL-CSNOBOL4-FENCE.md / GOAL-CSNOBOL4-HARNESS.md** all closed · **GOAL-CSN-FENCE-FIX.md** (8 open, 2026-05-31): CSNOBOL4 is a secondary monitor participant, not the oracle (`x64 sbl` is); its FENCE(P) segfault is out of scope for SCRIP 100% → OBS (csnobol4_suite differences are scored as R-3(d) XFAIL-or-real).
**GOAL-ZETA-FOUR.md (s93 absorbed, verified in source):** Z4-1..Z4-7 DONE (INH; FRAME-R12 5/5 landed then RETIRED by Lon s23k ruling *"R12 all the way for COND ASSIGN"*); Z4-8 CELL_HEAP PARKED (Lon s8); *four configs, one selector* → OBS — SUPERSEDED BY THE THREE ZETAS RULING (one config: ζ-SPINE on RSP, ζ-STANDING/ζ-ACTIVATION on RBP); the surviving selector `SCRIP_ZETA_STORAGE`/`--zeta-storage=` (`src/runtime/rt/zeta_alloc.c:269/283`, `src/contracts/zeta_choices.h:129`, `src/driver/scrip.c:470`; default CELL_STACK == ZC_PORT_FORTH+ZC_FRAME_RSP, byte-identical by construction) + non-default arms → **R-7**; Z4-9 CUT / M2 SAVE-COND side path → OBS (R-0/R-4 own the SAVE/COND homes).
**GOAL-SUBEXPR-ORACLE.md (s93 absorbed):** generator/oracle pair EXISTS (`SCRIP/test/beauty_subexpr_gen.py`, `SCRIP/test/subexpr_oracle.py`; S-1 55-test generator superseded in-file); the ≥200-snippet target → OBS as a goal (scorecard is THE INSTRUMENT); the tool is a MONITOR-FIRST aid for **R-2** first-divergence hunting (isolated beauty sub-expressions vs `sbl`) — use on demand.
**GOAL-DATATYPE-PORTABLE-TESTS.md (s93 absorbed):** OBS for SCRIP-100 — `x64 sbl` is the oracle and SPITBOL's DATATYPE spellings are the truth (RULES: SCRIP FOLLOWS SPITBOL SEMANTICS); 19 corpus `.sno/.inc` files still write `IDENT(DATATYPE(x),'…')` literally and are CORRECT as written; the CSNOBOL4-side mismatch is scored in **R-3(d)** (oracle-blessed XFAIL vs real).
**Kept, cross-referenced — NOT absorbed (platform / other repo / monitor participant; Lon to rule on deletion):** `GOAL-STCOUNT-ALL-LANGS.md` (all-language `&STCOUNT/&STLIMIT`; SNOBOL4 correctness side = **R-5**) · `GOAL-NET-{BEAUTY-SELF,BEAUTY-19,OPSYN-248,DATATYPE-LOWERCASE,SNIPPETS,OPTIMIZE}.md` (snobol4dotnet repo; dormant under X86-ONLY) · `GOAL-README-SNOBOL4{DOTNET,JVM,PYTHON,CSHARP,ARTIFACT}.md` (other repos' READMEs) · `GOAL-SILLY-{COMPLETE,SWEEP-FORWARD,SWEEP-BACKWARD,SYNC-MONITOR}.md` (SILLY = C SNOBOL4 monitor participant, not the oracle) · monitor/harness goals (`GOAL-INPROC-MONITOR`, `GOAL-MONITOR-REINSTATE`, `GOAL-TWO-STEP-HUNT`).
