# FINDING seat04 — json-alternate-af-spin: ROOT CAUSE NAILED (gdb-verified, exact byte offsets) — FLAT-mode choice records assume static `rsp`, but a deferred alternation inside ARBNO leaves 1,232 bytes of un-unwound backtrack state before re-entering them. ⛔ SESSION 3 (§9): "widen the RBP-mode eligibility" (Option 1) is now a CLOSED, DEAD-END direction, not merely risky — jarray/jobject's own alternations are PROVABLY recursively re-entrant through the SAME compiled code, so NO fixed/shared-slot storage (RBP or otherwise) can ever be safe for them, regardless of eligibility conditions. Fix still NOT implemented — architectural, needs a dedicated pass, now precisely scoped.

**Session:** 2026-08-22 seat04, THE LOOP queue row `json-alternate-af-spin` (rank 0). **THIRD session on this row, zero code landed across all three** — per RULES.md/GOAL-SNOBOL4-100.md's CHAT-ESCALATION law ("≥2 zero-rung sessions ⇒ ask before code"), this session ends with an `s4e_msg.sh ask` rather than a code attempt (§10).
**Status:** DIAGNOSIS COMPLETE AND NOW MECHANISTICALLY EXPLAINED END-TO-END (§9), not just gdb-observed. Two follow-up experiments run last session (§7): siblings `json-match.sno`/`json-match-fence.sno` don't reproduce the hang (surprise, blast-radius-narrowing), and the tempting grammar-level workaround it suggests (relocate `FENCE` to use-site) does **not** safely fix it — trades the hang for a `SIGSEGV` with a corrupted-stack backtrace on 3 of 4 cases. **This session (§9) explains why that workaround was doomed, from source, not just from the crash**: it independently re-verifies (fresh gdb trace, today's SCRIP HEAD `2659558e`, byte-identical 16,599-line recompile) that RBP — unlike RSP — stays PERFECTLY stable across the entire hang in the flat (non-recursive) case, which looks like it opens an RBP-relative fix path; but tracing json.sno's own bare-name-vs-`*name` reference structure proves jarray's alternation box is the SAME compiled code recursively re-entered on nested arrays/objects, which makes ANY fixed-slot choice record (RBP-relative included) categorically unsafe for it — closing that path for good rather than leaving it "risky." **Fix NOT implemented — claim left OPEN, not `done`.** All experimentation confined to scratch copies; `SCRIP`, `corpus`, and `.github` trees confirmed clean (`git status`) before every commit this session, consistent with RULES.md ASM-DIFF-FIRST discipline ("before changing one byte").
**Builds on:** `FINDING-2026-08-21-s251-json-deserializer-hangs-on-every-comma-and-has-no-corpus-coverage.md` §7 ("MECHANISM FOUND"), which located the spin to `n241_match_alternate_af`/`n249_match_defer_α` ping-ponging via asm inspection but did not explain *why*. §§1–8 (prior session) answered that "why" with a live gdb process. §9 (this session) answers the FOLLOW-UP question §§1–8 left open — "is there a cheap, safe fix hiding in the RBP-relative mode that already exists?" — with a definitive, source-traced NO, and narrows the real fix to two concrete sub-directions (§9.4).

---

## 1. A faithful minimal repro, built per the brief's own instruction ("do NOT start from json.sno")

Neither the ablation family (`arb1`–`arb6` in the s251 FINDING) nor a from-scratch hand-rolled `ARBNO`+deferred-ref witness reproduced the hang — `arb1`–`arb5` all MATCH (ARBNO/deferred-ref/recursion are all individually innocent, as s251 already showed), and my own simplified `jelem = *jvalue` (single-arm, non-recursive) witness returned a *different*, *also-wrong* result (silent NOMATCH on input SPITBOL matches) rather than hanging. **The missing ingredient in every prior minimization: a genuine multi-way `|` alternation (`jvalue`'s 7 arms) reached via the deferred reference, not just a bare deferred pattern.**

**Working repro:** copied `json.sno`'s definitions verbatim (lines 1–257, unmodified — all `DEFINE`d actions and the full grammar including `jvalue`'s 7-arm alternation) and appended a 4-line driver with the subject as a literal instead of read from stdin:
```
src = '[1,2]'   (or '[1]')
src json                    :F(fail)
OUTPUT = 'MATCH'            :(END)
fail  OUTPUT = 'NOMATCH'
```
- `[1]` (no comma): **MATCH**, fast, both engines.
- `[1,2]` (comma): SCRIP **hangs** (`timeout 8` → exit 124); clean SPITBOL: **MATCH**, fast. Matches the original table exactly.

**ASM-DIFF, per RULES.md ("mint the smallest repro" → "diff the emitted `.s`" before gdb):** `--compile`d both witnesses (16,599 lines each). `diff` shows **exactly 3 line-pairs different — the embedded literal `"[1]"` vs `"[1,2]"` and its length**. Every instruction in the entire pattern-matching machinery, including every box implicated below, is **byte-identical** between the passing and hanging witness. Per RULES.md this **exonerates the codegen as a source-difference bug** — the two programs run the *same code*; the divergence is purely a runtime choice-record state issue triggered by what the input data does to that one shared code path. This is what justified moving to gdb next, per the mandated order.

---

## 2. The exact box graph (gdb + `nm`, addresses from a `-g` linked standalone binary)

`jarray = '[' (epsilon . *parr()) ( *jelement ARBNO(',' *jelement) | ws ) ']' (epsilon . *earr()) FENCE` compiles (this witness's numbering, confirmed to match the box **names** `n241_match_alternate_af` / `n249_match_defer_α` in the original full `json.sno` exactly, since both programs share the identical preceding `DEFINE`d-procedure prologue) to:

- **`n241_match_alternate`** = the outer `( X | ws )` choice. Its `α` does `sub rsp,32` and lays down a **4-slot, 32-byte FLAT (stack-carved) choice record**: `[rsp+0]`=cursor, `[rsp+8]`=arm-selector/β-continuation (written later by `s0`/`s1`), `[rsp+16]`=the **"af" continuation** (rewritten twice — first to `.Lx261_21`, then, once the *second* deferred call is dispatched, to `.Lx261_19`, the box's own correct unwind-and-propagate-failure exit), `[rsp+24]` unused padding.
- `α` immediately tail-jumps (no `s0`/`s1` yet) into **`n249_match_defer_α`** = the *first* `*jelement` (matching `[1,2]`'s `"1"`).
- `n249_match_defer_α` resolves `jelement`'s pattern pointer and, on the fast path, **pushes two return-style continuations** (`.Lx273_5`→`af`'s fallback if the whole deferred match ever fails outright, `.Lx273_4`→`n250_match_arbno_α` on success) and does `jmp rax` into `jvalue`'s own compiled matcher (the 7-arm `jstring | jnumber+action | jobject | jarray | true+action | false+action | null+action` alternation) — itself a nested chain of `match_alternate`/`match_defer`/`FENCE`-guarded-optional boxes (`jnumber` alone has 4 more `FENCE(X|'')` optional pieces).
- `jvalue` succeeds (matches `"1"` as a `jnumber`), lands back at `.Lx273_4` → **`n250_match_arbno_α`**. This box's *own* loop-state (`[rbp-80]`=start cursor, `[rbp-76]`=last cursor) is correctly **RBP-relative/stable** — but its `α`, having recorded that state, **jumps straight back into `n241_match_alternate_s0`** to (re-)select an arm and dispatch onward to `n242_match_lit_α` (the closing `']'` check). `n250_match_arbno_β`/`as`/`af` similarly jump in and out of `n241`'s ports and `n252_match_defer` (the comma-then-second-`*jelement` iteration body) as the loop progresses.

## 3. gdb-verified: the drift, exactly

```
break n241_match_alternate_α   → rsp = 0x7fffffffdc80   (the record's true base, R)
break n250_match_arbno_α       → rsp = 0x7fffffffd7b0   (R − 1,232 bytes)
break n241_match_alternate_s0  → rsp = 0x7fffffffd7b0   (same — re-entered with NO correction)
break n241_match_alternate_af  → rsp = 0x7fffffffd7b0, forever, [rsp+0]=4213615, [rsp+16]=0x403bde
```

**`R − 1,232` is the whole bug.** Between `α`'s `sub rsp,32` and `n250`'s first re-entry into `n241`'s ports, `jvalue`'s own internal matching of `"1"` — trying and (mostly) rejecting 6 of its 7 arms before landing on `jnumber`, each rejection/commitment itself leaving its own pending backtrack state on the shared stack by the exact same "push-two-continuations, pop-only-one-on-success" convention `n249` used — accumulates 1,232 bytes of **legitimate, by-design backtrack state that is never unwound on success**, because nothing downstream has failed yet to need it. `n241`'s ports address their choice record as `[rsp+N]` — bare current-`rsp`-relative, no adjustment for anything pushed since `α`. Every later re-entry (from `n250_match_arbno_α`/`β`/`as`/`af`, from `n248_goto`, from `n252_match_defer`'s own continuations) is silently reading/writing **1,232 bytes into unrelated memory** — specifically, into the leftover choice-point state of arms of `jvalue`'s alternation that were *tried and abandoned* while matching `"1"`.

**Why it spins, specifically:** `0x403bde` (the fixed, garbage-derived value `af` keeps re-reading from `[rsp+16]`) resolves — confirmed via `objdump`, `nm` — to an instruction *inside `n249_match_defer_α`'s own body*: `jmp n241_match_alternate_af`. That address happens to sit at the drifted `[rsp+16]` location by coincidence of what's left over in that stale memory, not by any correct choice-record write. So `af` reads it, jumps to it, lands right back in `n249`'s body at the instruction that jumps straight back to `af` — a true two-instruction stationary loop, `rsp` never moving, matching the original FINDING's 50/50 perf split exactly. **This is why `[rsp+8]`/`[rsp+24]` (the brief's own open question) don't matter to the spin itself** — the loop never reaches code that reads them; it's fully contained in the `af` ↔ `.Lx273_5`(inside `n249`) pair. `[rsp+8]` is real (the `s0`/`s1` arm-selector, confirmed in §2); `[rsp+24]` is unused 32-byte-alignment padding, confirmed never written or read anywhere in this box.

**Why `[1]` (no comma) doesn't hang:** with no comma, `n250_match_arbno_as`'s single check fails immediately (no `,` to extend the loop) and the whole thing proceeds straight through to `']'` without ever *receding* back into `n241`'s ports from a failure — the drifted-but-unread record never gets touched. The bug is dormant until something forces a *recede* into the alternation's ports after the drift has already happened, which a trailing comma (forcing an ARBNO iteration, hence an arbno-to-alternate re-entry) does unconditionally.

---

## 4. Why this specific grammar shape, and why RBP-mode (which would have been immune) doesn't fire

`sn4_choice_rbp_off()` (`src/emitter/emit.cpp:2328`) is a **live, default-on** (`sn4_pt_frame()` defaults to 1 absent `SCRIP_PT_FRAME=0`) mechanism that puts a `match_alternate`'s choice record on a **stable RBP-relative frame slot** instead of the raw stack — which would be **immune to this exact drift**, since it doesn't move when nested calls push/pop below it. It didn't activate here because `blob_choice_rbp_scan()` (`emit.cpp:2305`) requires **exactly one choice point in the blob AND no `FENCE`** (`if (_nc != 1 || _fn) return 0;`, where `_fn` comes from `sn4_blob_choice_scan` scanning for `IR_MATCH_FENCE0`/`IR_MATCH_FENCE1`). `jarray`'s second arm is `ws = FENCE(SPAN(...) | '')` — **the very presence of a `FENCE` in the alternation's *other*, entirely unrelated arm disqualifies the whole box from the safe mode**, falling back to the FLAT/stack-carved mode that this FINDING shows is unsound whenever the *chosen* arm contains a deferred reference into a further multi-arm alternation. `jobject`'s `( jmember ARBNO(',' jmember) | ws )` is the textually identical shape (same `ws` fallback arm), which is exactly why the original FINDING's table shows `{"a":1,"b":2}` hanging too — **one mechanism, two grammar sites, matching the reported symptom set exactly.**

---

## 5. Fix directions considered, NOT attempted — why, and what each needs

I stopped short of implementing a fix this session. `emit.cpp`'s choice-record machinery (`blob_choice_rbp_scan`, `sn4_blob_choice_scan`, `frame_slot_scan`, `blob_frame_bytes`, `zdp_seam_tier`) is dense, actively tuned, gates *every* `match_alternate` box project-wide, and is a live file several other seats are concurrently editing for the R10/R11 eradication ladder — this is not a "flip one condition" change I'm confident is safe without dedicated verification against the full corpus (355 m3 / 353 m4 standing programs) and the M1 fixed-point gate. Two directions, in rough order of how promising they look:

1. **Widen `blob_choice_rbp_scan`'s FENCE exclusion.** The RBP-relative mode already exists and would structurally eliminate the whole bug class (stable storage can't drift). But the FENCE exclusion looks deliberate, not an oversight — FENCE performs a global cut that discards *older* choice points, and whether that interacts safely with a single reserved RBP slot (as opposed to an arbitrary-depth stack of them) is exactly the kind of thing that needs to be verified, not assumed. Whoever picks this up should find out *why* `_fn` disqualifies before touching it — there may be a real hazard, or it may just never have been exercised against this shape.
2. **Make FLAT mode itself safe under this combination** — e.g., have `n249`/`n252`'s deferred-success path communicate how much it left pushed (so `n241`'s ports can compute the *correct* current offset instead of assuming zero drift), or have ARBNO's re-entry into an outer alternation's ports go through a proper stable-pointer handoff instead of a bare label jump. Avoids touching the RBP eligibility heuristic at all, but touches the hotter, default-active code path, so the correctness bar for verification is higher.

Neither is a same-session change I'd stand behind without the kind of corpus-wide verification RULES.md's own gate discipline demands (`make pristine` + full corpus + M1 fixed point, both modes, per HQ-27) — and this box shape (alternation-with-a-deferred-alternation-in-one-arm-and-a-fence-in-another, re-entered through ARBNO) is exactly the kind of narrow, easy-to-get-subtly-wrong interaction where a rushed fix risks trading a loud hang for a silent wrong-answer regression elsewhere in the 355-program corpus.

---

## 6. What's confirmed clean / unaffected

- **Zero code changes.** No killswitch flipped, no `.s` artifacts touched, nothing owed at regen per CLAUDE.md's codegen-touch rule (I read `emit.cpp`/`bb_match_alternate.cpp`, did not edit either).
- **`arb1`–`arb6`'s conclusions still stand** — ARBNO alone, deferred refs alone, and recursive deferred refs alone are all innocent, confirmed independently by this session's own from-scratch witness attempts hitting *different* (non-hang) failure modes when the 7-arm alternation ingredient was missing.
- **`cond-assign-double-fire`** (the secondary defect s251 found via `arb6`, unrelated to this spin) was fixed mid-session by seat11 (`SCRIP b007a116`) — not this row's concern, noted for completeness only.
- Both `jbig_comma.sno`/`jbig_nocomma.sno` witnesses and all gdb scripts live under this session's scratchpad, not the repo — nothing to clean up in `corpus/` or `SCRIP/`.

## 7. Two follow-up experiments run after §6 — one surprise, one important negative result

**7a. The siblings do NOT hang — contradicting my own prediction, and worth recording precisely.** Checked `json-match.sno`/`json-match-fence.sno` (same header claim: "recognize-only... same grammar, zero side effects") on `[1,2]`: **both MATCH cleanly, no hang.** Both differ from `json.sno` in exactly one structural way relevant here: `jobject`/`jarray` do **not** carry their own trailing `FENCE` after the closing bracket — `json-match.sno` has no equivalent `FENCE` at all in that position, `json-match-fence.sno` moved it to **use-site** inside `jvalue` (`| jobject FENCE | jarray FENCE`), exactly the restructuring the original s251 FINDING §4 recommended for independent correctness reasons ("definition-site rests on an invariant nothing checks... use-site is correct-by-construction"). This is consistent with — though not fully box-traced and confirmed for — the `_fn` (FENCE-presence) condition in `blob_choice_rbp_scan` (§4): if `json.sno`'s *definition-site* placement puts the trailing `FENCE` in the *same blob* as `jarray`'s own outer alternation while use-site placement does not, that alone would explain why the siblings' equivalent alternate box gets to use the safe RBP-relative mode while `json.sno`'s doesn't. **Not fully verified at the box level for the fence sibling** (its `']'`-matching box wasn't locatable by the same `cmp eax,93` signature in a quick pass — time-boxed, not chased further) — reporting as a well-supported but not fully closed hypothesis, not a proven mechanism.

**7b. ⛔ Tested the obvious grammar-level workaround this hypothesis suggests — it does NOT safely fix the hang, and this is important.** Relocated `json.sno`'s own `FENCE` from definition-site (`'}' (epsilon . *eobj()) FENCE`) to use-site (`| jobject FENCE`), mirroring `json-match-fence.sno` exactly, on a scratch copy (repo `json.sno` untouched). Result on the reported hanging inputs:

| input | before (this FINDING's own repro) | after fence relocation |
|---|---|---|
| `[1,2]` | hangs (timeout 124) | **SIGSEGV** (rc 139, `timeout: dumped core`) |
| `{"a":1,"b":2}` | hangs | **SIGSEGV** |
| `{"a":[1,2],"b":{"c":3}}` | hangs | **SIGSEGV** |
| `[{"a":1},2]` | hangs | still hangs (rc 124) |
| all comma-free inputs | pass | still pass |

**This is not a fix — it trades an infinite loop for a crash**, worse in one respect (a `SIGSEGV` risks having already corrupted memory before the fault, where a spin at least fails loud and safe). `gdb bt` on the crash shows a **fully unwindable-garbage stack** (every frame `#N 0x<raw-stack-bytes> in ?? ()`) — the classic signature of a corrupted/misaligned `rsp`, not a clean null-deref. **Reading this together with §4's `_nc == 1` requirement** (not just `!_fn`): `blob_choice_rbp_scan` restricts RBP-mode to blobs with *exactly one* choice point, and `jvalue`/`jobject`/`jarray` are *mutually recursive* (an array can contain another array) — a single fixed RBP-relative slot is not naturally reentrant the way a stack-carved region is, so a **nested** invocation of the same alternate box (recursing into a nested array/object, exactly what `{"a":[1,2],...}` does) would clobber an outer, still-live invocation's slot if RBP-mode activated for a recursive box. **This crash is a plausible instance of exactly that hazard**, though not conclusively distinguished here from a residual instance of §3's original drift bug manifesting differently under a shifted box graph — either reading points the same direction: **the `_fn`/`_nc==1` eligibility conditions in `blob_choice_rbp_scan` look load-bearing, not incidental**, and Option 1 in §5 (widen the FENCE exclusion) needs to reckon with recursion-safety specifically, not just re-verify FENCE's own global-cut semantics. Revising §5's framing accordingly: **Option 1 is riskier than it looked before this experiment; Option 2 (make FLAT mode itself drift-safe) no longer has a tempting cheap alternative competing with it.**

## 8. Items for HQ / whoever continues this row

1. **This claim is left OPEN, not `done`** — the row's DONE-WHEN explicitly wants the hang fixed, corpus-covered, and verified; only the root-cause half is complete, and §7b shows the tempting shortcut doesn't work either. Whoever continues (myself next session, or another seat) can start directly at §5+§7b without re-deriving §§1–4.
2. **`json-match.sno`/`json-match-fence.sno` do NOT reproduce the hang** (§7a) — good news for blast radius (only the WORK member, `json.sno`, is affected), but means they're not useful as a smaller/simpler repro vehicle for continued debugging; `jbig_comma.sno`/`jbig_nocomma.sno` (§1) remain the right witnesses.
3. **Do not relocate `json.sno`'s FENCE placement as a fix** — §7b demonstrated this trades a hang for a `SIGSEGV` on 3 of 4 previously-hanging cases. If someone independently rediscovers this idea, this FINDING already has the receipt against it.
4. **The missing corpus row (s251 §3, still true)** should use an input that actually exercises this box shape (comma inside an array or object) once the fix lands — the original FINDING already specified this; not repeated in more detail here.
5. If a fix attempt lands, re-run this session's exact two witnesses (`jbig_comma.sno`/`jbig_nocomma.sno`, reproducible from `json.sno` lines 1–257 verbatim + the 4-line driver in §1) as the fastest possible regression check before touching `citm_catalog.json`.

---

## 9. SESSION 3 — why "just widen RBP-mode eligibility" is a dead end, proven from source, not just from the §7b crash

Re-verified the whole mechanism is unchanged at today's HEAD before adding anything: rebuilt `jbig_comma.sno`/`jbig_nocomma.sno` verbatim per §1 against SCRIP `2659558e` (clean tree), same repro (nocomma MATCH fast, comma `timeout 8` → rc=124, oracle `sbl -bf` MATCHes both), same 16,599-line `--compile` output, same instruction bytes at `n241_match_alternate_af` (`mov r14d,[rsp+0]; mov rax,[rsp+16]; jmp rax`, `jbig_comma.s:2267-2269`) — nothing any concurrent seat touched this session moved this mechanism.

### 9.1 The addressing mechanism, confirmed from source (not inferred from asm alone)

`CROQ`/`CROD` (`src/templates/x86_asm.h:938-939`) both route through `ZREFS` (`x86_asm.h:915-926`):
```
inline const char * ZREFS(int reg_off, int d, int w, int customer, int spine) {
    ...
    if (off != -1) return (w==8) ? RDQ("rbp", off+d) : RDD("rbp", off+d);   // RBP-mode: STABLE
    if (spine == ZSP_RAW) return (w==8) ? RDQ("rsp", d) : RDD("rsp", d);    // FLAT-mode: raw LIVE rsp
    ...
}
```
`bb_match_alternate.cpp:36` (`const int cro = sn4_choice_rbp_off();`) decides which arm every read in the box takes, uniformly. When `cro==0` (json's `jarray`/`jobject` today), **every later port — `β` (`:48`), `af`/`PAIR(2N+1)` (`:51-52`), every `alt_entry_stubs` retry stub (`:39,41` inside the loop) — computes its address off WHATEVER `rsp` happens to be live at that exact instant**, with no correction for anything pushed since `α`'s own `sub rsp,32` (`:39`). This is the FLAT-mode drift, now pinned to the exact macro and the exact template, not just to the emitted bytes.

### 9.2 NEW: RBP, unlike RSP, is measured PERFECTLY STABLE across the entire hang (not checked in §§1-8)

`§§1-8` gdb-traced RSP drift (1,232 bytes) but never looked at RBP. This session added `rbp` to every breakpoint in the SAME `jbig_comma_bin` (`-g`, `--compile` + `gcc -g`, matching §2's build recipe) across `n241_match_alternate_α` → `n249_match_defer_α` → `n250_match_arbno_α` → `n241_match_alternate_s0` → `n241_match_alternate_af` (spin, 4 more hits):

| breakpoint | rsp | rbp |
|---|---|---|
| `n241_match_alternate_α` | `0x7fffffffdca0` | `0x7fffffffdd68` |
| `n249_match_defer_α` | `0x7fffffffdc80` (α's own `sub rsp,32`) | `0x7fffffffdd68` |
| `n250_match_arbno_α` | `0x7fffffffd7b0` (drifted −1,232 vs α, matching §3) | `0x7fffffffdd68` |
| `n241_match_alternate_s0` | `0x7fffffffd7b0` | `0x7fffffffdd68` |
| `n241_match_alternate_af` × 4 more hits (the spin) | `0x7fffffffd7b0`, unmoving | `0x7fffffffdd68`, unmoving |

**RBP is bit-for-bit identical across all 8 hits.** Traced why: `n249_match_defer_α` here is `jelement`'s GVA-fast-path body (`jbig_comma.s:2409-2437`; matches `bb_match_defer.cpp`'s `vslot<0 && g_gva_active` arm, `:103-132`, landing at `.Lx273_10`/local `L(10)`), and the FAST inline dispatch it falls into (`bb_glue_pass_wires_blob(4,5)`, emitted as `jbig_comma.s:2431-2435`: push `af`-label, push `arbno_α`-label, `jmp rax` straight into `jvalue`'s compiled matcher) is **not gated by `emit_defer_rbp()` at all** — the success-landing label `.Lx273_4` (`jbig_comma.s:2436`) is a bare `jmp n250_match_arbno_α` with **zero instructions before it**, meaning `bb_match_defer.cpp:158-161`'s `IF(dfrm() && ... && emit_defer_rbp(), mov rsp,rbp + pop rbp)` compiled to nothing here — this specific call site never pushed an RBP frame to begin with, so there was nothing to drift.

**This looked, briefly, like reuse-bait: if `rbp` never moves here, why not address the choice record `[rbp − K]` for a compile-time-constant `K` instead of `[rsp + d]`?** That is *exactly* what the existing `cro`/RBP-mode arm already does (§9.1) — so the real question was never "is there a new mechanism to build," it was "why doesn't `blob_choice_rbp_scan` just admit this box already." §9.3 answers that, and the answer forecloses the idea rather than validating it.

### 9.3 ⛔ SELF-CORRECTION, SAME SESSION: the "recursion makes any fixed slot categorically unsound" claim below was WRONG — measured, not just re-reasoned

*(This subsection originally argued that `jvalue` statically inlines `jobject`'s and `jarray`'s entire box graphs into one blob, making `n241` recursively re-entrant with no frame of its own, and concluded no fixed-slot choice record could ever be safe for it. That argument is preserved in spirit below because the correction is more valuable pinned next to the mistake than silently replacing it — RULES.md's "measured not assumed" cuts against the seat that made the error too.)*

**The claim was falsified by the same `PAT$9_α_body` prologue already visible in `jbig_comma.s:2157-2168`** (found while chasing a different question, §9.2's "why not just address `[rbp−K]`" tangent) — `jarray` is compiled as its OWN separately-invoked "stored pattern" object (`push rbp; mov rbp,rsp; sub rsp,152; ...`, the exact `blob_frame_bytes()>0` shape documented at `emit.cpp:2743`, "must survive... and be PER-ACTIVATION under recursion"), not inlined into `jvalue`'s blob — `n237_match_lit_α`'s `cmp eax,91` (`'['`) sits *inside* `PAT$9`, confirming this stored-pattern object is `jarray` itself, bare-name reference or not. **Built a nested-array witness (`jbig_nested.sno`, `src = '[[9]]'`, comma-free so it doesn't hang) and broke on `PAT$9_α_body` twice:**

| entry | rsp (before `push rbp`) | rbp becomes |
|---|---|---|
| outer `jarray` | `0x7fffffffdd70` | `0x7fffffffdd68` |
| inner (recursive) `jarray` | `0x7fffffffda20` | `0x7fffffffda18` |

**Different rbp on the recursive entry — 848 bytes apart.** Each invocation of `jarray`'s blob gets its own fresh, properly call-isolated activation frame; a recursive invocation cannot alias an outer one's `[rbp−K]` slot. The "categorically unsound, no fixed slot can ever work" conclusion below is **retracted** — it was built on an unverified assumption about box-graph inlining that turned out to be backwards.

### 9.3-original (retracted, kept for the record — see the correction above)

<details><summary>original text</summary>

Checked `json.sno`'s own grammar (`corpus/programs/snobol4/demo/json.sno:240-254`) for which references are bare-name (statically embedded into the referencing pattern's own compiled graph at the moment `name = ...` executes) versus `*name` (truly deferred). **`*jelement` and `*jvalue` are the ONLY two deferred references in the whole grammar — every other cross-reference is a bare-name STATIC embed.** *(Retracted premise:)* That means `jvalue`'s single compile inlines jobject's and jarray's entire box graphs directly into its own `g_emit_cfg` — `n241` is textually and physically part of `jvalue`'s ONE compiled blob, not a separately-invoked unit. *(§9.3 above shows bare-name reference does not imply inlining — `jarray` gets its own `PAT$9` stored-pattern object regardless.)* Recursion realized through the two deferred hops (`jarray`'s `*jelement` → `jelement` → `*jvalue` → `jvalue` → bare `jarray`) was reasoned to re-enter "the exact same compiled `n241`" with no frame of its own — refuted directly by the nested-witness measurement above.

</details>

### 9.4 What §9.3's correction actually opens up: the real blocking conjunct, isolated

With recursion-unsafety off the table for `jarray`/`jobject` specifically (their own stored-pattern call convention already isolates each invocation), re-examined exactly what still refuses `n241` in `blob_choice_rbp_scan()` (`emit.cpp:2305-2317`) at TODAY's default flags (`sn4_pt_frame()` defaults **1**, confirmed `emit.cpp:2185-2187` — `getenv("SCRIP_PT_FRAME")`, `0` only on explicit `=0`):
```c
if (!sn4_choice_rbp() || !sn4_alt_carrier() || !blob_frame_scope() || !g_emit_cfg) return 0;
int _nc=0,_lf=0,_fn=0; sn4_blob_choice_scan(&_nc,&_lf,&_fn);
if (_nc != 1 || _fn) return 0;                 // <-- the live gate at default flags
{ if (!sn4_pt_frame()) { ... } }               // <-- DEAD by default (sn4_pt_frame()==1)
if (frame_slot_scan(...) != 2) return 0;
return 1;
```
At default flags the `!sn4_pt_frame()` branch (which also checks `_lf` and `blob_wire_clobber_scan()` — the latter refuses ANY blob containing an `IR_MATCH_DEFER`/`IR_CALL`, which would ALSO have refused `jarray` for an unrelated reason, register-wire safety around a nested call, not stack depth) **never runs today** — so `_nc==1 && !_fn` is the *entire* live gate for `n241` beyond `blob_frame_scope()`/`frame_slot_scan()`. `_nc==1` already holds for `jarray` (one choice point, `n241`, in its own blob) and is the RIGHT guard for the real intra-blob hazard (two *simultaneously live, statically-nested* choice points in ONE blob sharing the SAME single `[rbp−K..+31]` slot `blob_frame_bytes()` hands out — confirmed by HQ's cited `ptw_min_alttop_nofence_ctl` witness, `_nc==2`, SIGSEGV). **`_fn` (any FENCE anywhere in the blob — here, `ws`'s own `FENCE(SPAN(...)|'')`, the OTHER/sibling arm of `n241`'s own alternation) is the one remaining blocker**, and per HQ's answer (§10, this session) it is empirically NOT what discriminates cure from crash in the sibling `resume_carrier_ok` tier-3 mechanism (`lf=0 fn=1` on BOTH the cures AND — by the recovered s128 admission comment's own words — jarray's is "the captarm/beauty-Command class" `blob_choice_rbp_scan` was written FOR). This narrows §9's whole investigation to one measurable question: **does relaxing `_fn` alone (holding `_nc==1` fixed) fix json without regressing anything else?** — answered by direct A/B measurement in §11, not by further static reasoning.

## 10. HQ answered the ask this session's predecessor filed — and named the exact prior art

Checking `s4e_msg.sh check` mid-session surfaced `hq-answer-fence-exclusion-blob-choice-rbp`, answering a `q-json-alternate-af-fix-direction` ask an earlier session on this row filed (before this session's own transcript). **Recovered per HQ's own instruction** (`git show e25a5daf^:src/emitter/emit.cpp`, a "style pass" that stripped the explaining comments off `sn4_blob_choice_scan`/`resume_carrier_ok`/`blob_choice_rbp_scan` without changing behavior): the FULL history is now in that recovered file (saved this session, not re-quoted verbatim here for length — see `resume_carrier_ok`'s TIER 3 comment, `s128`'s `blob_choice_rbp_scan` comment, and `s126`'s "one-deep records compose by contiguity, two-deep do not yet"). **HQ's headline: `!_fn` was an EMPIRICAL narrowing (s128, "wide cut retracted same-session, 150/beauty movers measured"), never a proven FENCE/RBP semantic interaction; the REAL discriminator, independently measured by a SIBLING mechanism (`resume_carrier_ok` tier 3, s189, three witnesses), is choice-node COUNT (`_nc`), not FENCE presence.** HQ recommends replacing `!_fn` with a real depth-invariance question rather than deleting the conjunct outright, and explicitly warns the s127/s128 "second admission" (a *different* function, gating `resume_tgt` selection, not `blob_choice_rbp_scan`) must NOT be unified with this one — they answer different questions. §9.4 above is this session's independent narrowing of the SAME conclusion, arrived at from `blob_choice_rbp_scan`'s own code before this message was read, and the recursion-safety measurement in §9.3 is the piece HQ's answer doesn't cover (tier 3's evidence is about intra-blob `_nc`, silent on cross-invocation recursion) — now closed by direct measurement rather than left as a caveat.

## 11. The experiment run — killswitch A/B, not a blind default flip

Per HQ's explicit warning ("a conjunct removed without replacing the question it was standing in for is how the wide cut moved 150 the first time") and RULES.md's KILLSWITCH-BYTE-IDENTITY discipline, did **not** delete `_fn` outright. Added one gated relaxation, same idiom as every other `SCRIP_*` flag already in this file (`sn4_pt_frame`, `sn4_alt_carrier`, …): `sn4_alt_fence_relax()` (`emit.cpp:2305`, `getenv("SCRIP_CHOICE_RBP_FENCE")`, default **0** — off, byte-identical to today unless armed), consumed as `if (_nc != 1 || (_fn && !sn4_alt_fence_relax())) return 0;` in `blob_choice_rbp_scan`.

## 12. ⛔ MEASURED: the relaxation cures the hang but produces a WRONG ANSWER — worse than the bug, not fixed

**Armed (`SCRIP_CHOICE_RBP_FENCE=1`), `jbig_comma.sno` (`[1,2]`) no longer hangs — `rc 124 → rc 0`** (confirmed `n241`'s `cro` is live, `jbig_comma_on.s:2249-2267` addresses `[rbp-184]`/`[rbp-176]`/`[rbp-168]` throughout, not `[rsp+…]`). **But the answer is wrong: `NOMATCH`, where clean SPITBOL (`sbl -bf`) reads `MATCH`.** This is a *silent* wrong answer, not a crash — strictly worse than the hang it replaces, and exactly the failure mode HQ's answer (§10) warned a rushed conjunct-deletion risks. `jbig_nocomma.sno` (`[1]`) and `jbig_nested.sno` (`[[9]]`, comma-free) both still MATCH correctly armed — the regression is specific to the ARBNO-extends-then-declines path (2+ elements), not to RBP-residency in general.

**Two concrete hypotheses checked and RULED OUT, not just considered:**
1. **Frame-slot collision.** `n199` (a second, structurally identical alternation — almost certainly `jobject`'s own `(jmember ARBNO(...)|ws)`, same shape as `jarray`'s) uses the SAME offsets (`[rbp-184/-176/-168]`) as `n241`. Grepped `jbig_comma_on.s` for every reference to these three offsets and traced each hit's enclosing `PAT$N` block (the nearest preceding `PAT$N_α_body:` label): `n199` is inside `PAT$8`, `n241` is inside `PAT$9`, `n288`'s unrelated use of `[rbp-176]` is inside `PAT$10` — three separate stored-pattern objects, three separate physical stack frames (each with its own `push rbp`). Same offset, different memory. Not a collision.
2. **Recursion re-entrancy of the fixed slot itself** — the hypothesis §9.3-original wrongly convicted this on. Already measured innocent in §9.3: distinct `rbp` per `PAT$9` invocation, nested or not.

**Working hypothesis, not chased further this session:** gdb (`n241_match_alternate_af`/`n242_match_lit_{α,β}`/`n250_match_arbno_{β,as}`/`n240_match_assign_cond_β`, `jbig_comma_on_bin`) shows the cursor (`r14d`) go **backward** — `2 → 1` — on what should be ARBNO's first greedy-extension attempt (after `]` first correctly fails to match at cursor 2, the un-anchored recede should retry with ONE more `,jelement` rep, advancing the cursor toward 4, not retreating past where the FIRST `jelement` even started). This is consistent with `n250_match_arbno`'s own "interior choice point af back-out edge" (`op_arbno_body_actframe`, the s125-127 mechanism recovered in §10 — ARBNO's `af` jumping directly into the sibling alternate's `s0`/`as` ports, bypassing a generic recede) carrying an assumption about the sibling record's shape/location that was written and measured **only against a FLAT (rsp-relative) alternate** — never against an RBP-resident one, since until this session's experiment no witness had BOTH conditions simultaneously. **This reads as a fresh instance of s126's own already-named unsolved class, quoted verbatim in the recovered comment (§10): "one-deep records compose by contiguity, two-deep do not yet."** `n241` going RBP-resident is one "deep" (immune to FLAT drift, §9); `n250`'s own interior-choice-point fast path into `n241`'s ports is a second, and the two have not been designed to compose — matching, not contradicting, the caution both HQ's answer and the prior session's §5/§7b already carried.

**Verification the killswitch is inert at default (OFF), all three re-run against this session's own build:** `test_smoke_snobol4.sh` **7/7 both modes**; `test_corpus_snobol4.sh` **m3 355/2 · m4 353/2/2skip** — byte-identical BY NAME to the standing baseline this file's own `GOAL-SNOBOL4-100.md` LIVE CURSOR entries already carry (`FAIL-M3 160_pat_alt_inner_gen_resume`/`demo_treebank`, `SKIP 132_pat_fence_eps_recur_shallow`/`demo_porter`); `board_beauty_m1.sh --modes both` **m3 3/10 (first red 10) · m4 10/10 ⭐M1-FIXED-POINT**, also byte-identical to the standing watermark. **Code stays in the tree, default OFF, with a `⛔⛔ MEASURED-BROKEN, DO NOT PROMOTE` comment directly on `sn4_alt_fence_relax()`** (`emit.cpp:2305`) naming both ruled-out hypotheses and the working one, so the next session that reaches for "just relax `_fn`" starts from this measurement instead of re-discovering it blind.

## 13. Where this leaves the row

Diagnosis is now two layers deep and both are measured, not assumed: (1) FLAT-mode drift, gdb-nailed to the byte (§§1-8); (2) the tempting RBP-mode cure needs `n250`'s interior-choice-point fast path taught to compose with an RBP-resident sibling record, or disabled when one is present, and neither was attempted this session — it needs `bb_match_arbno.cpp`'s `op_arbno_body_actframe` machinery read with the SAME care §9 gave `bb_match_alternate.cpp`, which this session's budget didn't reach. **This is now a real design/implementation task with a precise starting point** (`bb_match_arbno.cpp`'s interior-choice-point admission, `emit.cpp`'s `op_arbno_body_actframe` field, cross-referenced against `sn4_alt_fence_relax()`'s own comment) rather than an open-ended architecture question — the sharpest handoff this row has had across all three sessions. Replying to HQ's answer (§10) with this session's two corrections — the recursion-safety retraction (§9.3) and the new measured regression (§12) — via `s4e_msg.sh` before ending. Claim stays OPEN, not `done`; corpus row still deliberately not added, same reasoning as before (no XFAIL mechanism, fix and coverage land together).

---

## 14. SESSION 4 (2026-08-23) — the hang stopped reproducing from an UNRELATED commit; MEASURED that this is a symptom change, not a cure; the row's REAL remaining blocker is a THIRD, independent bug

**Landing job per the task baton — not an investigation.** Session start found two same-day developments already in the tree that directly bear on this row: `SCRIP 3a644af1` (hq_P, "ARRAY(n) no longer round-trips through text") incidentally stopped `json.sno`'s comma-hang from reproducing on real input, and `SCRIP 2037a02f` (seat09, landed mid-*this* session, "jdec capture-after-alternation") fixed the row's anticipated second blocker (`jstring-escape-dcap-pump-segv`) for real. Both are genuine, well-verified, unrelated fixes — neither touches `bb_match_alternate.cpp`/`blob_choice_rbp_scan`/anything this row's own mechanism lives in.

### 14.1 The hang is NOT reproducible — but the drift IS still there, measured directly

`3a644af1`'s own author flagged this explicitly: *"I CANNOT EXPLAIN THE MECHANISM and am not taking credit for it... the row belongs to hq_C/seat01 and the mechanism is theirs to confirm before anyone calls it closed."* This session did that confirmation, and the honest answer is **LATENT, NOT CURED**:

- Built a fresh `-g` mode-4 binary of today's `json.sno` (`--compile` + `gcc -g`, linking `out/libscrip_rt.so`, at SCRIP `2037a02f`). `json.s`'s own `n241_match_alternate_α` (same box number as this FINDING's §§1-9 analyzed — box numbering is stable because `json.sno`'s own grammar hasn't moved since a prior session's `fence-jstrbody-cas` edit, which touched `jkey`/`jstring`'s own FENCE, not `jarray`/`jobject`'s) is **still `[rsp+0]`/`[rsp+16]`-addressed** — FLAT mode, unchanged. `sn4_choice_rbp_off()`/`blob_choice_rbp_scan()` still refuse it for exactly the reasons §§4/9 established.
- gdb-traced `[1,2]` against this binary: `n248_match_defer_α` (the box graph shifted numbering slightly vs. this FINDING's §2, but is the same shape) hits at `rsp=0x7fffffffdc80`; the next `n241_match_alternate_s0` hit is at `rsp=0x7fffffffd7f0` — **drifted 1,168 bytes**, same order of magnitude as §3's original 1,232-byte measurement, same mechanism, same direction. A second ARBNO iteration drifts a further identical 1,168 bytes. **The choice record's addressing bug is structurally unchanged.**
- `n241_match_alternate_af` — the port that spins — **is reachable** (confirmed by breakpoint on a backtrack-forcing malformed input, `[1,2,]`) and does not spin when reached: it read a plausible-looking, apparently-correct return address and the program exited normally. A 102-input sweep (trailing/leading/doubled commas, truncated arrays/objects, 60 random malformed fragments, up to and including inputs specifically chosen to force a recede through this exact alternation) found **zero** reproductions of the original hang against current HEAD.

**Best-supported reading, not fully proven:** `3a644af1`'s ARRAY(n) change altered heap/workspace allocation timing enough that the *stale garbage value* the buggy `af` port reads from the drifted stack slot no longer happens to be a self-referencing jump target (which is what made the original bug a *deterministic infinite* 2-instruction loop rather than a one-off bad jump). The underlying defect — a FLAT-mode choice record computing its address off live, undrifted-assumed `rsp` after a deferred multi-arm alternation has legitimately left backtrack state behind — is unchanged and unfixed. It is plausible, not confirmed, that a different `SCRIP_GC_STRESS` arm, a different malloc/ASLR layout, or a grammar shape elsewhere in the corpus sharing this exact box shape could still trip it. **This row should stay open, tagged LATENT rather than closed, until §5/§9's actual architectural fix (or an equivalent) lands.** Do not let the disappearance of the *symptom* be read as the disappearance of the *defect* — that is precisely the "presence is not proof" failure class RULES.md names repeatedly elsewhere in this project's history.

### 14.2 DONE-WHEN still does not pass — but not for either anticipated reason

With both `3a644af1` and `2037a02f` in the tree, `citm_catalog.json` no longer hangs and no longer SIGSEGVs on the escape path. It **still SIGSEGVs**, in 1.2s, deterministically — a C-stack exhaustion in the deferred-pattern-value machinery (`patv_slot`/`rt_dfx_push`/`c_rt_defer_close`, `pattern_match.c`), root-caused to bare `FENCE`'s static stack-release computation silently billing 0 whenever it's preceded by an ALTERNATE/ARBNO (which `jarray`/`jobject`'s trailing `FENCE` always is), so every array/object's own backtrack state accumulates, unreleased, for the rest of the top-level match. **This is a third, independent bug — not this row's mechanism, not the escape bug.** Full root cause, a synthetic 234-record witness (no dependency on `citm_catalog.json`'s content), and why a fix needs its own dedicated session (it touches the same shared frame-slot allocator `blob_choice_rbp_scan` depends on — this row's own HARD CONSTRAINT territory) are in `FINDING-2026-08-23-seat04-json-fence0-static-release-cant-see-past-alternation-unbounded-stack-leak.md`.

### 14.3 Per the task baton's own item 4: ω-CONCEDE, not force

The baton's own NEXT block anticipated exactly this shape ("DONE-WHEN blocked by a second, separate bug... prove the af-spin is gone on an escape-free witness, write it up, and ω-CONCEDE with a rewritten NEXT rather than forcing the criterion") — it named the wrong second bug (the escape bug, since fixed) but got the *shape* of the outcome right: this row's own mechanism cannot be honestly marked CURED (§14.1), and the DONE-WHEN is now blocked by a bug outside this row's scope (§14.2, §4 of the new FINDING). **Not weakening the DONE-WHEN.** Task baton's `## NEXT` rewritten accordingly; claim stays OPEN under seat04, not `done`.
