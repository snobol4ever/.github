# FINDING — s219 (2026-07-30): RTX-8 SLICE 5 LANDED (`rt_patstk_lazy_init`). RTX-0f IS SOLVED BY LITERAL SCALING AND s216's JSON ROUTE IS STRUCTURALLY UNFIT. AND THE MATCH LEAF FAMILY IS UNGRADEABLE FOR SPEED ON THIS CONTAINER — MEASURED AT FAMILY LEVEL, NOT INFERRED.

**Session ask (Lon): "Replace SCRIP's C runtime with ASM code. Do ones that will help speed."** The honest answer to the second half is the largest result here, and it is a NEGATIVE measured at the level of the whole family gate rather than argued from instruction counts.

## 1. ✅ LANDED — `rt_patstk_lazy_init`, gate `SCRIP_RTX_MATCH`, C → `c_rt_patstk_lazy_init` same commit
5 instructions on the hot path; ZERO templates touched ⇒ no `.s` regen owed (phase-1 by construction).
**THE SHAPE IS THE POINT — LAZY INIT IS THE INVERSE OF `rt_cap_push`.** Arms read from the C body before any asm existed (0(f-pre)): `g_patstk_sp == 0` ⇒ COLD, reserves the 8 MB pend island through `rt_slab_region`, runs EXACTLY ONCE per process, delegated whole to C; `g_patstk_sp != 0` ⇒ HOT, return. Predicted from the source: **entries − 1 = commits**. Measured post-port: **entries 1,000,001 · bailed 1 · COMMITS 1,000,000.** `rt_cap_push` was 57,578 / 57,578 / **0**. Same tool, opposite verdict, and both were knowable in advance from the arm structure.
⭐ **BAIL-BEFORE-MUTATE IS FREE IN THIS SHAPE AND THAT IS WHY IT IS SAFE:** the asm mutates NOTHING — one GOT load, one compare, then `ret` or tail-delegate to a C body that re-runs the test itself. s215's double-push/double-pop hazard cannot arise; the cold path is bit-identical to pristine C by construction. **PREFER TEST-AND-RETURN TARGETS: they are the only shape where BOTH 0(f) and the mutation hazard are discharged from the source.**
⛔ **0(c) WAS LOAD-BEARING AGAIN, THIRD RUNG RUNNING.** `g_patstk_sp` is capital **`B`** in `out/rt_pic/pattern_match.o` AND present in the `.so` dynamic table — because `bb_match_head`/`bb_match_fence1`/`bb_match_release` all read it from EMITTED code. ⇒ `[rip+g_patstk_sp@GOTPCREL]` mandatory; a direct `[rip+sym]` binds A DIFFERENT VARIABLE via copy relocation under `-no-pie`, silent island divergence, no diagnostic at any stage (s214). Sibling `g_patstk_base` is lowercase **`b`** (static) and UNREFERENCEABLE from a `.S`; this port never touches it, only the C cold path does. **Read on the OBJECT, never the `.so` — the `.so` cannot separate `b` from `B`.**
**GATES:** two-sided HARD `ud2` on the commit path — gate ON ⇒ **rc=132 SIGILL**, SAME BUILD gate OFF ⇒ **rc=0 correct** ⇒ asm provably executes and the switch provably routes to C. Revert verified THREE ways: grep 0 · source md5 back to `e45032e6` · **`.so` relinked bit-identical to `4f511246`**. Crosscheck **m3 311/4/0 · m4 311/2/2 · DIVERGE=2**, failure sets line-by-line identical to the session-start baseline, **zero movers**. RTX unit **ALL PASS (8426 cases, 0 mismatches)** · smokes **7/7 ×2 (m4 hard gate)** · hidden-global gate **CLEAN** (`g_pcall_top` = the known comment-only near-miss, s214). `.so` md5 `4f511246a2b56c54725acd157562ccc3`, **RT_OPT=`-O0`** (no `-O2` directive this session).

## 2. ⭐⭐ RTX-0f IS SOLVED — AND s216's PRESCRIBED ROUTE IS STRUCTURALLY UNFIT, NOT MERELY NOISY
**THE INSTRUMENT: `pattern_bt.sno` with its one literal scaled to `LT(N,4000000)`.** Self-times **~870 ms** (clears `MIN_MS=800`); checksum **PREDICTED IN ADVANCE** and matched at FOUR counts — `result:` == K exactly (the `LT` assignment fails at N==K, leaving N==K) and `W` == `ccccddddaaaa` invariant across all K because it is input-determined; 0(d) count scales **1,000,001 → 2,000,001 = exactly 2.000×**. This is s217's named shortcut ("scale the literal"), and it cost one `sed`.
⛔⛔ **s216 RECORDED RTX-0f AS "EFFECTIVELY SOLVED" VIA SCALED `twitter.json` COPIES AT `match_ms` 4×=1172 ms. THAT ROUTE CANNOT WORK AND THE REASON IS STRUCTURAL, NOT A BAD DRAW.** Re-measured on well-formed `[doc,…,doc]` inputs (naive `cat` produces malformed JSON — s216 was right about that): 1×=165 · 2×=366 · 4×=696 · **6× = 915/973/966/673/741 ms**, a **±19% swing**, while 8× came back **LOWER than 6×** (613–673, and stable). `json.sno` BUILDS TABLES AND ARRAYS as deferred side effects, so its wall time is **allocation-and-GC-bound, not match-bound** ⇒ **lengthening the window cannot fix its noise, which is why RTX-0f kept failing to produce a gradeable number.** ⭐ **RULE: A LONGER WINDOW ONLY HELPS WHEN THE NOISE IS WARMUP. IF THE NOISE IS ALLOCATION, SCALING THE INPUT AMPLIFIES THE NOISE ALONGSIDE THE SIGNAL.** `pattern_bt` works precisely because it allocates nothing per iteration. ⚠ The recognize-only siblings (`json-match.sno`) are NOT the fix either: they FAIL on array-wrapped input and carry no `TIME()` instrumentation at all.
⚠ **AND MY OWN FIRST STABILITY READ WAS LUCK — SAID PLAINLY BECAUSE IT NEARLY BECAME A QUOTED FACT.** Three runs at 4M read 1232/877/868 and I wrote "±0.5% stable." Twelve further runs exposed intermittent **~1300 ms fliers in BOTH arms** on this 1-core container. ⇒ **NO MEDIAN-OF-3 IS TRUSTWORTHY HERE; USE MIN-OF-N AND DISCARD RUN 1.** Same class as s209's "instability is worse than bias because it cannot be subtracted out."

## 3. ⛔⛔ THE ANSWER TO "DO ONES THAT WILL HELP SPEED": NOT IN THIS FAMILY, AND THE PROOF IS FREE
The ladder has refused a speed number for four consecutive rungs on WINDOW grounds (176/205 ms vs an 800 ms floor). With a gradeable window finally in hand, the refusal survives for a DIFFERENT and stronger reason. **`rt_match_ctx_restore` is already asm behind this same gate, so the family's speed sensitivity is measurable at zero cost by toggling it:** `SCRIP_RTX_MATCH=1` → 908/859/1300/898 ms; `=0` → 1308/893/887/1334 ms. **Medians 903 vs 890 — ZERO, with the fliers straddling both arms.** ⇒ a 10-instructions-of-20 saving over 4,000,000 calls is invisible, and this port's ~5 instructions against **~650 cycles per match** (870 ms / 4M) ceilings near **0.8%**. **NO SPEED NUMBER IS CLAIMED FOR SLICE 5 AND NONE SHOULD EVER BE QUOTED OFF IT.**
⭐⭐ **WHERE THE TIME ACTUALLY IS, MEASURED NOT GUESSED — THE FIXED PER-MATCH CALL CHAIN.** Interposer counts at K=500,000 (500,001 matches), one call per match each: `rt_match_enter` · `rt_cap_match_begin` [asm] · `rt_match_ctx_restore` [asm] · `rt_patstk_lazy_init` [asm, this rung] · `rt_dcap_lazy_init` [C] · `VARVAL_fn` 500,011 [C]. **`descr_to_str` = 6 calls TOTAL ⇒ the DT_S fast path holds and coercion is NOT the cost.** ⇒ **THE LEVER IS NOT ANY SINGLE LEAF; IT IS THE CHAIN.** Three of six links are now asm and the board did not move, which is itself the evidence: **eliminating links one at a time wins <1% each, so the rung that can move this benchmark is `rt_match_enter`'s HOT PATH — collapsing the three lazy-init/begin calls that are no-ops after the first match — not a fifth leaf.** ⚠ That is the target ARCH §5 and the s216 cursor both warn against as multi-arm and call-heavy. **Both facts are true: it is the hard target AND the only one large enough to grade. The warning was about difficulty; it was never a claim that some easier symbol would do.**

## 4. ⭐⭐ THE s217-OWED KILL-SWITCH GATE NOW HAS AN EXECUTABLE FORM — AND IT REPRODUCED s217's DISCOVERY INDEPENDENTLY, SHARPER
**LANDED `scripts/test_gate_rtx_killswitch_sets.sh`**: N≥4 per arm, compares hash **SETS**, prints **THREE** verdicts — `IDENTICAL` / `QUARANTINE` / `MOVER`. ARCH §7 step 3 has mandated this since s217 and it had **no executable form until now**, so every intervening rung's headline gate was the coin-flip instrument s217 named.
**MEASURED over `corpus/crosscheck/patterns` (121 programs, the family this port touches, N=4): IDENTICAL 120 · QUARANTINE 1 · MOVER 0 ⇒ GATE PASS.** The quarantine member is `160_pat_alt_inner_gen_resume`, exactly as s217 predicted — and the new detail is decisive: **its ON and OFF hash sets are IDENTICAL two-element sets**, `ON{6a68d667,d7772c55}` == `OFF{6a68d667,d7772c55}`. ⇒ under the old one-run-per-arm gate this program reports **MOVER on roughly half of all draws**, and since gate OFF *is* the C fallback, the instability provably cannot be caused by the asm. (s217 drew 3 distinct hashes in 4 OFF runs; I drew 2 in 4 per arm — consistent, different draw, which is the whole point.)
⚠ **SCOPE STATED PLAINLY, NOT DRESSED UP: this ran on 121 pattern programs, NOT the full 316.** The full-suite N≥4 sweep is ~2,500 runs and is OWED. The rung is not closed until it runs suite-wide and both modes.

## 5. INHERITED ITEMS, STATUS
- ⚠ **`rtx_abi.inc` lines 10-15 STILL carry the stale r12 pin** (ARCH §2 corrected it at s205). Untouched a fourth session; this port uses r10 only, so the trigger condition ("fix it in the commit that first uses r12") never fired.
- ⚠ **`_Static_assert` for the `g_cap_gen`/`g_cap_gen_next` and `Σ`/`Σlen` adjacencies (s218 item 5) NOT DONE** — still nothing in the tree defends those offsets.
- ✅ **ENVIRONMENT COST MODEL RE-CONFIRMED, AND s218's CORRECTION HELD:** full runtime build **152 s** with `-j2` (not ~8.5 min) · **ONE `.S` EDIT + RELINK = 1 SECOND** · full both-modes 315-program crosscheck **25–28 s**. The complete `ud2` falsification cycle cost well under a minute and was run as DEFAULT, not budgeted.
- ✅ **THE TREE WAS STABLE THIS SESSION** — unlike s218: `.github`, SCRIP and corpus were all exactly at origin at orientation, HEAD did not move mid-session, and the independent rebuild reproduced s218's recorded `.so` md5 `db2d87ab` bit-for-bit before any edit, which is what licensed trusting the baseline.

**`handoff_status.sh` is the push truth — not this document.**
