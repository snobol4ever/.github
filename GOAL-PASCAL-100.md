# ⛔⭐⭐⭐ GOAL-PASCAL-100 — PASCAL, 100% BYRD BOXES, ONE PLAN, ONE FILE

**Minted 2026-08-27 by ceo on Lon's order (GOAL-CEO.md CEO-30), consolidating this session per THE LOOP task `goal-consolidate-pascal`.** Lon, in-chat to ceo, verbatim in substance: *"We want one GOAL file for each language at this point."* Pattern authority: the three existing `-100` files (`GOAL-SNOBOL4-100.md`, `GOAL-ICON-100.md`, `GOAL-PROLOG-100.md`) — LIVE CURSOR at top, FRONT STATUS, standing laws with provenance, rungs.

**This file REPLACES (renamed this session; full pre-rename text in `.github` git history):** `GOAL-PASCAL-BB.md`. Any reference to that name, anywhere in `.github`/SCRIP/corpus, resolves HERE. **RETIRED NAMES: `GOAL-PASCAL-BB.md`.**

⛔ **CONSOLIDATION DISCLOSURE — read before trusting anything below as current.** The prior file's content (2026-06-27 through 2026-08-08, PAS-BOOL-RELOP-ZD its newest cursor) is real, substantial technical history and is carried forward BELOW **UNCHANGED — this is a MOVE, not a rewrite; no law text was altered**, and where the old file's own later finding conflicts with a fact established today, both are kept, dated, rather than one silently overwriting the other. But **the single most important fact about Pascal's current state exists in NONE of the text below**: see FRONT STATUS immediately following. Per this project's own standing lesson (root `CLAUDE.md`'s Lon-path finding, and `GOAL-SNOCONE-100.md`'s identical disclosure the same day): a durable file must not inline a volatile fact, and the live task queue is where Pascal's truth is moving fastest right now.

## ⭐⭐⭐ FRONT STATUS — READ THIS FIRST: RESTORATION + DISPLAY REVIVAL ARE THE ACTIVE FRONT, NOT ANYTHING BELOW (as of 2026-08-27)

⭐ **Lon extended scope 2026-08-24 (s272, in-chat to ceo), verbatim in substance:** *"We are doing well enough to extend scope to Snocone, Raku, and Pascal — at least get them to where they were before the huge month-long ZETA development phase."* Routed via GOAL-CEO.md CEO-20 (`mint-restoration-rows`), minted by hq_C. Same reading as Snocone's: the month of SNOBOL4/Icon-focused THREE-ZETAS conversion work landed on the SAME shared emitter/lowering pipeline Pascal rides, and regressed working Pascal behavior along the way. The prior file's newest cursor (PAS-BOOL-RELOP-ZD, 2026-08-08) predates this and does not know it happened.

**Live rows in the postoffice task queue right now** (`/home/resources/postoffice/tasks/*.task.md`, `QUEUE.tsv`) — **these, not the ladders below, are Pascal's actual current front. Check `s4e_msg.sh next` / `QUEUE.tsv` for live state before acting on anything in this file's older sections:**
- **`pascal-restore-prezeta`** — the umbrella restoration row. Candidate watermark **152/152 both modes** — **PROVENANCE NOW CITED** (this row's own STEP 1 demanded it before trusting the number): this file's own § "WATERMARK — PAS-PINT-7 CLOSED" below, SCRIP `1950edde` · corpus `5f85b8b8`, 2026-07-23 s133. Today: **98/153 m3 · 86/153 m4, +23 programs with no `.ref` at all** (corpus grew 152→153 programs, so the gap is NOT simply "54 regressions" — split into regressed / new-since-watermark / ungraded before bisecting, per the row's own STEP 1). Sequencing interlock: `tests-consolidate-pascal` (343-file suite conversion) must land wholly before or after this row's bisection, never during.
- **`pas-display-revival`** ⭐⭐ — **directly reverses a "dead architecture" verdict recorded in this file's own history (§ "WATERMARK — CHARACTERIZATION SESSION", 2026-07-13, "FINDING 1", preserved unchanged below).** Two FINDINGs dated today (`FINDING-2026-08-27-ceo-pas-display-was-ruled-landed-2026-06-27-then-silently-regressed-and-mislabeled-dead-architecture.md`, `FINDING-2026-08-27-ceo-pascal-scoping-status-uplevel-cells-shared-across-recursion-no-save-restore.md`) establish: PAS-DISPLAY (r13/r14/r15 lexical display, Lon 2026-06-27, TOP PRIORITY) LANDED all five rungs the same day, then silently regressed with no ruling; a later audit (the "FINDING 1" preserved below) measured the regression's absence and mislabeled it "DEAD ARCHITECTURE" rather than catching the regression. Its replacement, today's `__up_*` arena cells (one cell per (proc,var)), is **semantically broken under recursion — zero save/restore, confirmed by grep** — exactly what the display design solved by construction. Lon reaffirmed the display ruling in-chat 2026-08-27: *"We were supposed to already be using R13 through R15."* This row subsumes `pascal-uplevel-nested-proc-hang` (below), since display save/restore is the cure class. ⛔ **Do not trust this file's old "PAS-DISPLAY is DEAD ARCHITECTURE" finding as a design verdict — it was a regression reading, now corrected.**
- **`pascal-m4-alpha-undefined-link`** — m3≡m4 CLASS DEFECT: mode-4 Pascal emits calls to per-proc α labels it never exports (`ld: undefined reference to swap_α / permute_α`) for nested/recursive procedure shapes. Witnesses: `corpus/benchmarks/pascal/{perm,queens,quick,towers}.pas` fail m4 link; `{bubble,intmm,sieve}.pas` are the passing control group (m3≡m4≡fpc). m3 is correct on all seven.
- **`pascal-m4-registered-dispatch-segv`** — newly VISIBLE once the row above cured at SCRIP `81b50c3b`: `corpus/tests/pascal/arrparam.pas` m3 prints the correct answer, m4 SIGSEGVs in `rt_proc_call_prologue_lex`'s `getenv("SCRIP_VARARG_TAIL")` — a trashed `environ`, i.e. the real fault is an earlier frame/stack overwrite, not the getenv itself.
- **`pascal-uplevel-nested-proc-hang`** — `corpus/programs/pascal/bench/uplevel2.pas`/`uplevel3.pas` hang/crash under `scrip --run` (nested-procedure access to an enclosing-scope variable at 2-3 levels). Subsumed by `pas-display-revival` per that row.
- **`pascal-m3-empty-corpus-false-pass`** / **`pascal-m4-empty-corpus-false-pass`** — `test_gate_pascal_m3.sh`/`test_gate_pascal_m4.sh` report a clean `PASS=0 FAIL=0` (exit 0) when the corpus glob matches nothing, instead of refusing loudly — the absent-corpus false-pass class. Needs a floor.
- **`bench-rivals-raku-pascal`** — benchmark SCRIP Pascal against Free Pascal/FPC (the open-standard rival). BLOCKED ARM: neither `fpc` nor `rakudo` installed as of 2026-08-27 — needs Lon (`sudo apt install fpc rakudo`, or build rakudo sudo-free from `/home/resources/rakudo-main`). Install-independent prep (accessors, kernel prep, grid harness) can proceed now.
- **`tests-consolidate-pascal`** — separate house-cleaning row: fold `corpus/tests/pascal`'s 343 loose files into the suite format `corpus-suites-consolidation` established for the other languages. Interlocked with `pascal-restore-prezeta` per that row's own note (above).

⛔ **STALENESS HAZARD carried from the prior file, now doubly confirmed:** the WATERMARK entries below top out at **PAS-PINT-8 (2026-07-23) / PAS-BOOL-RELOP-ZD (2026-08-08)** — all measured **before** the s272 scope extension and before the PAS-DISPLAY regression was caught. Read everything below as a historical record of what was true on its own date, never as Pascal's state today.

---

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔

**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. We do not do that here. ██**
**ENFORCEMENT: every diff is checked for new file-scope definitions; a commit adding one without a cited in-chat grant in its message is REJECTED on sight. Precedent: the g_pcall / g_pcall_wires / RT_AB_ANCHOR eradication (s55) — that entire class is what this rule forbids recreating.**
**⛔ THE ASK ITSELF MUST BE A BANNER: any session requesting this permission MUST display the request in-chat as a large unmissable ⛔ banner — the proposed global's name, type, owning file, purpose, and why registers/the stack cannot carry it — so Lon cannot miss the ask.  A quiet or inline ask does not count as asking. (Lon 2026-08-13 s55, in-chat.)**


## ⚙️ CONCURRENT BY DEFAULT — AND THE REPOS MOVE UNDER YOU

**Many seats run this file's siblings at the same time. Edit any file, commit and push whenever a rung is buildable and green — mid-session, per rung. Never park work or refuse an edit on concurrency grounds; stranding has cost this project far more than merging ever has.** Git merges; `git pull --rebase` and resolve normally.

**⛔ ASSUME ORIGIN MOVED SINCE YOU LAST LOOKED.** Another seat may have landed in your exact files while you were reading them.
- `git pull --rebase` before every push; **re-prove THIS file's gate/watermark after any rebase** — shared state moves under you and a watermark measured pre-rebase is void.
- `git log origin/main..HEAD` at orientation AND before handoff. **A clean `git status` is NOT a clean tree** — it hides local commits a peer seat left in a shared working copy.
- Place trees at canonical absolute paths (`/home/claude/{SCRIP,corpus,.github,x64}`) BEFORE running any gate: **many scripts grade a tree by absolute path.**
- Prefer **one clone per seat**; two seats in one working copy silently overwrite each other's uncommitted edits, and a global gitconfig scrambles attribution.
- Push **code repos before `.github`**, so no FINDING ever describes an unpushed tree.
- Push needs a credential — **ask Lon in chat and wait.** Never write push status into a doc.
- `bash scripts/handoff_status.sh` verbatim is the ONLY push truth. Not this file, not a commit message.

**Semantic collisions (two seats claiming one register) are caught MECHANICALLY by the claim gates, not by scheduling.** That is why no window is needed.

## ⛔ FACT RULE — O0-DEV: FEATURE BUILDS ARE `-O0`; `-O1`/`-O2` ARE PERF-ONLY (Lon directive, 2026-07-21 s119)

**While developing, debugging, or iterating on any FEATURE, EVERY build is `-O0`. `-O1` and `-O2` are FORBIDDEN during feature work and are reserved EXCLUSIVELY for perf/benchmark/release measurement.** The runtime `libscrip_rt.so` at `-O2` takes MINUTES (heavy template TUs), which is intolerable in a compile→test→fix loop and burned real session time repeatedly. `scrip` itself already builds `-O0` (Makefile `CBASE`/`CXXRT`); the offender was the runtime `.so`, whose `RT_OPT` default was `-O2`.

**THE MECHANICAL ANCHOR (why this is a FACT RULE, not a convention):** the Makefile default is now `RT_OPT ?= -O0 …` (SCRIP `Makefile` lines ~33 + ~281), so a bare `make libscrip_rt` / `make scrip` / `build_scrip.sh` is `-O0` by DEFAULT — the fast path is the path you get for free. `-O2` is now EXPLICIT opt-in, used ONLY for measurement:
```
make RT_OPT="-O2 -g -fno-strict-aliasing -fwrapv -fno-omit-frame-pointer" libscrip_rt   # perf/bench ONLY
PERF=1 bash scripts/jcon_selfhost_build.sh                                               # perf .so via the selfhost builder
```
Benchmark builders that need `-O2` already pass it explicitly (`jcon_selfhost_build.sh PERF=1`; the official-oracle trees build their own way), so the default flip does NOT silently corrupt any perf number — a perf run that forgets `-O2` is a mis-measurement the operator owns, not a default that lies.

**COMPLETION TEST:** (a) `grep -nE 'RT_OPT *[?:]?= *-O0' Makefile` matches (default is `-O0`) and no un-opted `RT_OPT ?= -O2` remains; (b) session-setup / feature-dev build scripts (`build_scrip.sh`, smoke/crosscheck runners) invoke `make` with NO `RT_OPT` override (so they inherit `-O0`); (c) any `-O2` in a script is either a monitor/oracle-side helper (separate lib) or gated behind an explicit perf flag (`PERF=1`); (d) this FACT RULE body is byte-identical across the six GOAL-*-BB files (md5-locked, per the Prolog file's sibling-verbatim note).

**LIMITATION (do not oversell — same honest shape as the other rules here):** a Makefile default and a markdown rule cannot COERCE a session to avoid typing `RT_OPT=-O2` during feature work; they make the fast path the default and the slow path a deliberate, visible choice. The human reviewer remains the real enforcer — **reject any feature-work handoff whose build log shows `-O2` on the runtime `.so`.**

## ⛔ LIVE CURSOR
- **NEXT RUNG: PAS-BOOL-RELOP-ZD — `bb_binop_relop_val()` ZD arm partially fixes multi-statement booleans. M3 81/72/23 · M4 80/73/23. SCRIP HEAD `709d5f19` (prior Pascal pushes never landed — all prior `c4ccda4c`/`1950edde` etc. hashes are local-only; this HEAD predates them). OPEN: boolassign.pas and other multi-statement bool programs still fail (WRONG+SIGSEGV). Single-bool correct. Root cause diagnosed: with `op_zres=1` (ZD-armed), RELOP_VAL was writing result to `FRQ(op_off+zdepth)` = flat slot displaced by own carve — consumers read via UCLAIM-resolved `FRQ(op_sb)` = `[rsp+0]` (the ZD cell). Mismatch → wrong values + stack drift across statements. ZD arm added to template (writes `ZRES(0)/ZRES(8)`) fixes single-bool. Multi-stmt still wrong: UCLAIM resolution of `op_sb=op_off` for large `op_off` values (1696+) in long programs may not resolve to `[rsp+0]` — must verify UCLAIM mapping for each statement's relop node before calling this closed. Also: `zeta_storage.c` `_spine` change was tried and reverted (it broke `op_wpop` accounting for the relop cell). Next action: instrument UCLAIM resolution of `op_sb` in n7_assign for the two-statement case; confirm whether `FRQ(op_sb)` = `[rsp+0]` in TEXT mode for boolassign statement 1 vs statement 2; if not, determine why UCLAIM resolves differently and fix the owner-table mapping. SCRIP commit: see below.**
- **✅ DONE (partial): PAS-BOOL-RELOP-ZD — ZD arm added to `bb_binop_relop_val()` (2026-08-08, Claude Sonnet s5). SCRIP HEAD after commit. M3 81/72/23 (was 79/74/23 at this HEAD; baseline before this session). FIX: `bb_binop_relop.cpp` — new early-return ZD arm (`if PLATFORM_X86 && op_zres && !op_num_real && op_ival in [LT..NE] && op_sa>=0 && op_sb>=0`): reads operands via `FR(op_sa)`/`FRQ(op_sb+8)` (flat, zr0=zr1=0 for Pascal integer vars), writes result to `ZRES(0)/ZRES(8)` (own ZD cell, `[rsp+0]`/`[rsp+8]`), exits via `x86_gamma()`. Identical to `bb_binop_arith()`'s ZD arm shape. Programs newly passing: `enumsubarr` (+1 M3); total +2 M3 / +3 M4 vs baseline. REMAINING: boolassign and multi-bool programs still WRONG+SIGSEGV — UCLAIM issue in multi-statement context, see NEXT RUNG above.**
- **✅ DONE: PAS-BOOL-RELOP — boolean relop port-wiring crash CLOSED (2026-08-08, Claude Sonnet s4). SCRIP `c4ccda4c` · M3 153/0/23 (was 147/6/23). REMAINING M3 FAILURES: char3 (case-on-char, feature gap) · goto2 (numeric label, feature gap). M4 145/8/23 (8 remain, all pre-existing). ROOT CAUSE: `lower_pascal.c` emitted `IR_BINOP_TEST` (branching template) in value-producing context (bool-assign: `b:=i<j`, `a[0]:=i>j`, `p^.f:=i>3`). Under `zframe_graph=1`, `IR_BINOP_TEST`'s ω port emits `add rsp,K; jmp ω_target`, tearing down the FORTH frame mid-expression when both γ and ω need to converge for an assignment. Identical to Raku's RK-ZC-6 bug (same comment in `lower_raku.c:216`). FIX (lower_pascal.c only): new `pas_mat_rv()` emits `IR_BINOP_RELOP_VAL` for relop trees, falls through to `pas_mat` for TT_NOT; wired at `lower_binop()` (covers all value-position relops including arr_set_pure args), `lower_unop()`, and two `is_condish(rhs)` sites in `lower_assign()`. Pascal.y grammar-level `b:=relop → TT_IF` rewrite also removed (was bypassing `lower_assign` entirely). Additionally: `ir_node_produces_value()` in `scrip_ir.c` now includes `IR_BINOP_RELOP_VAL` alongside `IR_BINOP_TEST`. Cross-lang smoke: SN4 7/0 · ICN 13/1(pre-existing) · PL 4/1(pre-existing) · RK 719/0 — unchanged.** PREV CURSOR: THREE FIXES LANDED (2026-08-07, Claude Sonnet s3). M3 147/6/23 · M4 141-143/10-12/23.
- **NEXT RUNG: PAS-PINT-8 — pcom now self-hosts arrays; next blocker is pointer-type chains in pcom's selector/comptypes/genlabel. SCRIP `1950edde` · corpus `5f85b8b8` · M3 152/0 · M4 152/0 (s133).** The prior finding's "aggregate-local alias under recursion" diagnosis is **wrong** — `ov6.pas` (its own repro) now passes correctly, and the proposed `lower_pascal.c` frame-slot fix would not touch the real fault. Real root cause: **chained deref of a pointer-valued record field** (`arr^.inx^.form` reads blank; `cpy:=arr^.inx; cpy^.form` reads correctly; `arr^.inx = sub` is TRUE). Reduced to clean 20-line standalone repro `ptr2.pas` — verified on clean HEAD. This is a `pascal.y` frontend type-resolution bug: `pas_selector_rectype → pas_ptrexpr_target → pas_rectype_field_ptrto_by_index` returns NULL for a `__pas_deref(TT_IDX(recvar, fieldidx))` base when the pointer-valued field is declared in a **variant arm** and/or has a **forward-declared** pointer target type — exactly pcom's `structure` record shape (`arrays:(aeltype,inxtype:stp)`). `fldptrto[]` is likely not populated for variant-arm pointer fields at `pas_pend_add` time. FIX: ensure `fldptrto[]` is recorded for variant-arm pointer fields; instrument `pas_rectype_field_ptrto_by_index` for `ptr2.pas` to find the NULL link; fix the one gap; add `ptr2.pas`+`.ref` as gated probe; bison regen + `make libscrip_rt` + full M3+M4 gate. Provenance: FINDING-2026-07-23-CLAUDE-PAS-PINT-7-CORRECTED-CHAINED-PTRFIELD-DEREF.md (supersedes the -AGG-LOCAL-ALIAS- finding).
- **✅ DONE: PAS-PINT-6 — WHOLE-RECORD ARRAY-ELEMENT COPY + CHAIN-TRAVERSAL LIMIT → pcom→pint ROUND-TRIP RUNS ON SCRIP (Claude 2026-07-23, LOCAL). M3 149/0 · M4 149/0 · 4-lang sweep clean (stash-rebuild-verified).** `hi.pas`→`HI` and `calc.pas` (`for i:=1 to 5 do s:=s+i` — old `06_for` blocker)→`15` both run SCRIP-pcom→P-code→SCRIP-pint, matching native fpc. TWO fixes (bracketed): (1) `arr[k]:=arr[j]` for `array of record` was a single-slot scalar copy losing all `nf` fields (ANY ≥2-field record, plain OR variant — corrects prior variant-only/nrec framing); pint's `store` P-stack is `array of record` and every value move is a whole-element copy. Fix pascal.y-only: `pas_recspan_nf`/`pas_recspan_slot` + `mk_assign` clause → `nf`-slot span copy when ≥1 side is TT_IDX (plain recvar:=recvar untouched); matches P4 `gen1(40 mov,size)`. (2) compiling pint aborted `[GZ-7] qt=8559>=CH_MAX=8192`; emit.cpp `codegen_flat_chain_body` CH_MAX 8192→65536, `nodes[]`→static, guard→`qt>=Q_MAX`. New probe `recspan_copy.pas`+`.ref` (+1→149). PUSH PENDING (awaiting credential).
- **✅ DONE: PAS-PINT-5b — SCRIP-pint PRINTED CHAR ORDINALS AS INTEGERS (not a mnemonic failure). CORRECTS THE PRIOR CURSOR (Claude 2026-07-22, SCRIP `ea0bac56`). M3 148/0 · M4 148/0 · SN4/ICN/PL/RK smokes green · bison 5 s/r pristine.** The 5b cursor's diagnosis was WRONG on every point — verified by the two-oracle 2×2 (native/SCRIP pint × native/SCRIP prr, all with `< /dev/null`): SCRIP-pcom's P-code is CORRECT (native pint runs it → `HI`), and SCRIP-pint LOADS+EXECUTES fine (no mnemonic-lookup failure, no `duminst`, no `halt`, no hang — the "hang" was an uncontrolled stdin artifact). REAL BUG: SCRIP-pint reached pint's char-write and printed `        72        73` — the width-10 ORDINALS of `H`,`I` — instead of the characters. LOCUS `pint.pas:525` `write(f, store[ad+i].vc)` where `store` is a variant record (`car:(vc:char)`); the char-typed variant-record-field READ in write position was formatted as int (same family as PAS-SELFHOST-3c `chr` char-ness + PAS-PINT-3 `pas_is_cafield`). ROOT: `pas_is_charexpr` recognized char vars / `__pas_chr` / char-ARRAY element+field reads, but NOT a scalar char-typed record-field read; the field-type registries (`g_pas_rectypes`, `g_pas_arrrecs`) tracked `fldca` (char-array) but had no scalar-char-field flag. FIX (pascal.y-only, ~13 additive edits mirroring the `pas_cafield` machinery, NO shared/runtime files): new `fldchar[]` dimension on both registries fed from a `g_pas_pend_fldchar[]` staging flag (set from the existing `g_pas_pend_ischar` in `pas_pend_add`, persisted in `pas_rectype_add`/restored in `pas_rectype_to_pend`, captured in `pas_arrrec_add`); predicates `pas_rectype_field_is_char`/`pas_arrrec_field_is_char`; a `pas_cvfield` mark registry (mark/is/reset) parallel to `pas_cafield`; marking at the three field-read reductions (named-rectype selector `:787`, anonymous inline-record arrrec `:786` — pint's case, with-selector `:505`); one line in `pas_is_charexpr`. Covers BOTH named element types (repro `vrec_charwrite.pas` / `vcw.pas`) and pint's anonymous inline `array of record`. Isolated repro `72 73`→`Hello`; pint round-trip → `HI`. New gated corpus probe `vrec_charwrite.pas`+`.ref` (M3+M4 148/0, native-fpc-cross-checked). Backup at `/tmp/pascal.y.bak`. `halt`-unimplemented (the 5b cursor's sub-bug b) NOT reached by `hi.pas` — defer to PAS-PINT-6 if a program hits it.
- **✅ DONE: PAS-PINT-4 — VARIANT-RECORD WHOLE-VALUE STORE INTO A NESTED FIELD (Claude 2026-07-22, LOCAL — not committed/pushed). M3 147/0 · M4 147/0 · zero regression.** `outer.field := variantval` dropped every slot but the first. ROOT (corrects prior FINDING's guess — instrumented the runtime sinks, not assumed): a WRITE/READ representation mismatch. Outer record fields = SOH(`\x01`)-separated; a NESTED sub-record = one field with elements `\x05`-separated (`__pas_nrec_get`/`update` convention). `g.cval:=v` (variant, 3 SOH slots) lowered to generic `arr_set_pure`, which spliced v's value in RAW keeping its SOH separators → field 0 became `0|SOH|1|SOH|0` where a `\x05`-subrecord was required → `__pas_nrec_get fi=0 ei=1` found nothing → 0. FIX (2 files, Pascal-only, no shared): (1) runtime `__pas_nrec_field_set(outer,fi,whole_rec)` converts RHS's inner SOH→`\x05` (by_name_dispatch.c, after `__pas_nrec_update`); (2) `pascal.y` `mk_assign` new clause routes `TT_IDX(VAR base, ILIT fi):=rhs` to it when field `fi` is a record type AND rhs resolves to a record type — else generic path (plain `a[i]:=scalar` unaffected). bison regen: 5 s/r conflicts == pristine. New gated probe `vrecfield.pas`+`.ref`. Provenance: FINDING-2026-07-22-CLAUDE-PAS-PINT-4-VARIANT-WHOLE-VALUE-INTO-NESTED-FIELD.md. PUSH PENDING (awaiting credential).
- **✅ DONE: PAS-PINT-3 (bracket + TWO enabling fixes, Claude 2026-07-22, LOCAL — not committed/pushed). M3 146/0, zero regression.** pascal.y-only (14 lines + regen). FIX 1: `pas_is_charexpr` recognizes char-array-FIELD element reads (`recfield^.arr[i]` in write position — was formatting the ordinal as int) via a `pas_is_cafield(e->c[0])` branch. FIX 2: array-of-pointers element field-store (`a[i]^.f[j]:=x`, `a:array[..] of ^Rec`) was ABORTING the compiler (nameless IR_ASSIGN guard) because the element ptr-target was discarded at decl — added `arrptr` registry (array-var→element rec type), captured in the `array..of type` production, registered in `var_decl`, consumed by `pas_ptrexpr_target`. Repros r1/r8 pass; zero new grammar conflicts (5 == pristine). Provenance: FINDING-2026-07-22.
- **✅ DONE: PAS-PINT-2 — pcom EMITS WRITE/WRITELN P-CODE (Claude, 2026-07-22, SCRIP `1f50093f`). M3 146/0 · M4 146/0 · SN4 306/4 (same pre-existing 4).** ROOT (bracketed via traced pcom copies, MONITOR-FIRST): pcom's nested proc `write` (the write-statement P-code compiler) was intercepted by SCRIP's `write` builtin and executed as a no-op. Traced: `entstdnames` registers `write` key=6/`writeln` key=12 correctly → `searchid` finds `writeln` as `klass=proc` → `call` reaches `6,12: write` arm → but pcom's nested `write` procedure body never executed (first-line trace silent). Isolated repro: `procedure write; begin writeln('CUSTOM') end; begin write end.` → no output (should print CUSTOM). FIX (pascal.y only): `call: IDENT` rule checks `pas_is_proc(name)` before `mk_call`; user-declared proc → emit direct call node bypassing all builtin interception. Limited to zero-arg IDENT calls so `write(f,...)` I/O with args is unaffected. Proc registry: `pas_proc_add`/`pas_is_proc` + forward-decl, registered at forward+full decl sites. Guard refined from an initial too-broad `mk_call`-level bypass (which broke all pcom I/O) to the correct zero-arg-only grammar-rule guard. VERIFIED: `writeln('HI')` pcom prr contains `lca`+`csp wrs`+`csp wln`; pint rc=0; shadow repro prints CUSTOM. PUSH PENDING (awaiting credential).
- **✅ DONE: PAS-PINT-1 — pint COMPILES + RUNS; pcom→pint ROUND-TRIP CLEAN (Claude, 2026-07-22, SCRIP `46a475d8`). M3 146/0 · M4 146/0 · all 5 langs regression-free.** TWO bugs: (1) emit.cpp `binop_is_num_real` exponential blowup (145× store[] assignments → ~145^depth re-exploration; fix: node/name visited-set → linear). (2) `pascal.y` `ReadInt(f,i)` unhandled → pint load loop spun. RESULT: pint.pas compiles fully (118K asm, 0 bomb stubs); round-trip rc=0 (was hang).
- **✅ DONE: PAS-PINT-1 — pint COMPILES + RUNS; pcom→pint ROUND-TRIP CLEAN (Claude, 2026-07-22, SCRIP `46a475d8`). M3 146/0 · M4 146/0 · all 5 langs regression-free.** TWO bugs, both blocking pint. (1) **EMIT EXPONENTIAL BLOWUP (the real blocker) — `scrip --compile pint.pas` HUNG AT COMPILE TIME, not runtime.** Bracketed via env-gated per-node emit probes (`SCRIP_EMIT_NODE_DBG`) to one `IR_BINOP` node inside `binop_is_num_real` (emit.cpp real-type static analysis). ROOT: pint assigns `store[]` **145×**, so `var_assigned_real_static(g,"store",…)` matched ~145 IR_ASSIGN nodes, each recursing into RHSs that reference `store` again → ~145^depth re-exploration of shared subgraphs (depth-8-bounded so NOT infinite, but astronomically slow). FIX (emit.cpp, shared): node-pointer + variable-name visited-set in `var_assigned_real_static`/`binop_operand_real_static`, reset per top-level `binop_is_num_real` query → exponential collapses to linear. Boolean result provably unchanged (a node's real-ness is path-independent). (2) **`ReadInt` UNHANDLED in `pascal.y`** — pint's P-code load loop calls `ReadInt(prd,x)` (pint.pas:211,365) to read label numbers; parser had NO handler → calls dropped → load loop spun. FIX: `ReadInt(f,i)` → `i := __pas_read_i_f(f)` (ISO digit-only read; `fscanf(" %lld")`). Zero new grammar conflicts. **VERIFIED:** pint.pas compiles fully (118K asm lines, 0 bomb stubs); `pcom < empty.pas → prr`, `cp prr prd`, `pint` → rc=0 clean exit (was rc=124 hang). REGRESSION PROOF: SN4 306/4 (4 pre-existing, byte-identical to pristine HEAD), ICN 4/0 + full-corpus 241/20/32, PL 159/0, RK 51/0, PAS M3+M4 146/0. Two scary artifact diffs (eval `.s`, 6 icon-bench port-flips) proven pre-existing-stale via pristine-HEAD regen (identical diff without my change). One residual soft note: pint emits a non-fatal `[SBB] mode-3 … soft fall` for one box shape (no output for that shape; did not block round-trip) — investigate if PAS-PINT-2 needs it. Provenance: FINDING not yet written (small session). PUSH PENDING (awaiting credential).
- **✅ DONE: PAS-PINT-0 — FILE-AWARE I/O LAYER BUILT (Claude, 2026-07-21, SCRIP `625f80ca`). M3 146/0 · zero regressions.** ROOT: pint hangs because `pinthelper.inc` `GetBufCh`/`ReadInt` use FPC-only `TTextRec` machinery — SCRIP parser drops both definitions but keeps calls → "Undefined function or operation" swallowed, load loop spins. Deeper gap: ALL Pascal file I/O was stdin/stdout-only (`read(f,x)` dropped `f`, `eof(f)` ignored `f`). FIX (4 files, Pascal-only): (a) `pascal.y` — file-var registry (`pas_filevar_add`/`pas_is_filevar`/`pas_is_stdstream`), populated from `var x:text` decls and program-header non-std params; (b) `read`/`readln` detect leading filevar, skip it as stream, route to `__pas_read_c_f`/`__pas_read_i_f`/`__pas_readln_f`; (c) `eof(f)`/`eoln(f)` → `__pas_eof_f`/`__pas_eoln_f`; (d) `GetBufCh(f)` → `__pas_getbufch_f`; (e) `write(f,…)`/`writeln(f,…)` pass filevar as leading operand (runtime write handler already routes FHVAL `args[0]` to that file); (f) bison regen zero new conflicts; (g) runtime `by_name_dispatch.c` — `__pas_read_{i,c}_f`, `__pas_readln_f`, `__pas_eof_f`, `__pas_eoln_f`, `__pas_getbufch_f`, `__pas_getbufch` all using `fh_get`/`ungetc` (mirrors Prolog `$get_char`/`$peek_char` path). GATE: M3 146/0 (NOREF=1, baseline unchanged). Open: header-file auto-open — RESOLVED as moot by PAS-PINT-1 (pint's `assign(prd,'prd');reset(prd)` opens explicitly; the real blocker was ReadInt + emit blowup, not header binding).
- **✅ DONE (stale cursor corrected): PAS-SELFHOST-3d RUNG 3 `sroi 9` — ALREADY FIXED by RUNG 2 (`bc79bb87`).** Cursor claimed "recover dropped `sroi 9`" as next rung — MEASURED first: SCRIP-pcom on `l_var1.pas` already emits `ldci 5 / sroi 9 / retp` correctly. Stale prose per RULES.md. New divergences: (1) pcom drops `writeln` statement (statement-emission gap, separate from sroi); (2) pint hangs (→ PAS-PINT-0 above).
- **✅ DONE: PAS-SELFHOST-3d RUNG 2 — RECORD/ARRAY LOCALS COLLISION FIXED (Claude, 2026-07-21, SCRIP `bc79bb87`). M3 146/0 · M4 146/0.**
- **✅ DONE: PAS-SELFHOST-3c — `chr(x)` NOW CHAR-TYPED SO writeln FORMATS IT AS A CHARACTER — FIXED (Claude, 2026-07-19).** ROOT (measured, distinct from the bracket's three guesses — `fop` was correctly 42 and the `fop=42` branch WAS taken, verified by instrumenting pcom `gen1`): `gen1` emits `retp` via `writeln(prr,chr(fp2))` (`pcom.pas:1858`, fp2=ord('p')=112). SCRIP printed `ret       112` because `chr(x)` was folded in `mk_call` (`pascal.y:91`) to its **bare integer ordinal**, losing char-ness, so `writeln(chr(x))` (and the `chr(fp2)` here) formatted the number with SCRIP's default integer width instead of emitting the character. `__pas_chr` (the write-path char signal) converts int→1-char-STRING (`by_name_dispatch.c:3923`) and only works on an int arg (double-wrapping or value-context use breaks it). FIX (1 line): `chr(x)` → `__pas_chrlit(x)` — reuse the existing char-literal marker, which `pas_is_charexpr` recognizes (so writeln wraps it in `__pas_chr` + width -2 → character), whose runtime handler returns the **int** (identity, so arithmetic/assignment stay int-valued), and which `ord` already unwraps. No lowerer/runtime change. **VERIFIED: Pascal M3 145/0 · M4 145/0** (+1 = new `chr_writeln` probe, corpus `.ref` added); `writeln(chr(112))`→`p`, `chr(65,66,67)`→`ABC`, `c:=chr(88);writeln(c)`→`XX`, `ord(chr(90))`→`90`, all M3+M4. **EMPTY PROGRAM NOW SELF-HOSTS** (`diff -w` clean vs native fpc-pcom). Original bracket (guessed operand-branch/`fop`/`mn[]`-truncation — all wrong):
- **✅ DONE: PAS-SELFHOST-3c(bracket) — SCRIP-pcom emitted `ret       112` where native emits `retp`.** BRACKETED (Claude, 2026-07-19). After the 3b fix, SCRIP-pcom on the empty program produces P-code structurally identical to native EXCEPT the program-exit op: native `retp` vs SCRIP `ret       112`.
- **✅ DONE: PAS-SELFHOST-3b — VAR-PARAM WRITE-BACK TO NESTED-PROC LOCAL — FIXED (Claude, 2026-07-19).** Fix landed in `emit.cpp` (NOT `lower_pascal.c` as the bracket guessed, and NOT via a new `IR_VAR_FRAME_REF` IR kind — that kind does not exist). ROOT (measured): the `IR_VAR_REF` case in `walk_bb_node` (`emit.cpp:784`) was GVA-only and never set `op_sa`; `bb_var_ref()` (`src/templates/bb_var_ref.cpp`) picks GVA-absolute (`mov rdx, RT_GVA_VA+k*16`) when `op_gva_k>=0` else frame-relative (`lea rdx,[rbp+op_sa]`), and the common prologue (`emit.cpp:752`) set `op_gva_k` keyed on `!graph_has_local(cfg,name)` ALONE — the lone outlier vs the four sibling predicates (IR_VAR read :782, IR_ASSIGN :794, drive :1074/:1081) which all require `is_global(name) && !graph_has_local`. For a nested-proc frame local (`seg`): `is_global=0`, `graph_has_local=0` (Pascal frame locals are in NEITHER registry — they just get a varslot; `bb_varslot_peek("seg")=528`), so the IR_VAR *read* correctly went frame (`[rbp+528]`) but the IR_VAR_REF went GVA (`0x70000020`) → writer and reader hit different cells → blank. FIX (4 lines, narrow — touches only the IR_VAR_REF case, NOT the shared prologue): resolve GVA-vs-frame inline mirroring the IR_VAR read — `is_global && !graph_has_local` → GVA (`op_gva_k=gva_index_of`), else frame (`op_sa=bb_varslot_peek(name); op_gva_k=-1`). A first attempt broadening the prologue predicate itself SEGFAULTED at compile time (too broad — exposed unset `op_sa` in other GVA-only paths); reverted. Now asm emits `lea rdx,[rbp+528]`. **VERIFIED: Pascal M3 144/0 · M4 144/0** (was 143/0; +1 = new `nested_vp_writeback` probe now gated with `.ref`); **all 4 shared-emit languages green — SNOBOL4 7/0, Icon 14/0, Prolog 5/0, Raku 341/0 (M3+M4)**. corpus `nested_vp_writeback.ref` added (`seg=1 glob=1`). PCOM IMPACT: P-code label numbers now LAND (empty program: `l 4=9 l 5=5`, `cup 0 l 3` — previously blank `ent 1 l    `). Original bracket kept below for provenance: Root: `var`-parameter write-back fails when the actual argument is a local of a nested procedure and the callee is at outer/sibling lexical level. Global side-effects of the callee execute correctly; only the by-reference write to the nested frame cell is lost. Reproduces in **both M3 and M4** → lowering bug in `lower_pascal.c`, not emit/templates. MINIMAL REPRO (`corpus/programs/pascal/nested_vp_writeback.pas`): `procedure setit(var nxt: integer)` at program level; `procedure body` nested one level has local `var seg: integer`; `setit(seg)` → native `seg=1 glob=1`, SCRIP `seg=  glob=1`. LOCUS: `lower_pascal.c:213-215` (`pas_call_args_brm`) builds `IR_VAR_REF` with the plain unmangled name — suspected to resolve via GVA path rather than frame-relative address of the nested activation. `IR_VAR_REF` emit: `emit.cpp:735/1030`, `bb_var_ref.cpp:18`, `zeta_storage.c:326`. FIX DIRECTION: when the var-param actual resolves to a nested-proc slot (slot >= 0 in innermost `pas_scope_t`), emit `IR_VAR_FRAME_REF` (frame-relative) not plain `IR_VAR_REF` (GVA path). Full bracket in `FINDING-2026-07-19-CLAUDE-PAS-SELFHOST-3B-NESTED-VP-WRITEBACK.md`. HOW THIS MANIFESTS IN PCOM: `genlabel(var nxtlab: integer)` writes to `body`'s locals `segsize`/`stacktop`/`entname` — inside `genlabel` the value is correct (`nxtlab=3,4,5` measured), but after return `body` reads blank → P-code label numbers blank, `ent   1   l    ` vs native `ent   1   l   4`.
- **PREVIOUS: PAS-SELFHOST-3 (bug 1 of 2) — LITERAL→VALUE-PARAM CHAR-ARRAY MARSHALLING FIXED (Claude, 2026-07-19, SCRIP `8f7d27b5` → now at HEAD `2a7f718b` via parallel sessions).** A string literal passed to a by-value `alpha` (packed array[1..N] of char) parameter reached the callee as raw text, but the callee decodes char-array params from the SOH-ordinal encoding → field read back empty. FIX (pascal.y-only). Recert M3 143/0 · M4 143/0; SNOBOL4/Icon/Prolog smokes clean.
- **PAS-SELFHOST-2 — FIVE STRARR/WITH BUGS LANDED (Claude, 2026-07-19, SCRIP HEAD `c313580d`).** All `pascal.y`-only. Recert **M3 143/0 · M4 143/0**.
- **FORTH RSP/RBP SWITCH — ✅ DONE-BY-INHERITANCE (2026-07-17).** See watermark below.
- **LAST SESSION (2026-07-21):** RUNG 2 bracket INDEPENDENTLY CONFIRMED — NO CODE LANDED, NO FPC ORACLE NEEDED. SCRIP HEAD `2300678f` (no Pascal commits this session). Build: `scrip` clean from HEAD; `libscrip_rt.so` (176 MB ELF) built fresh (all 91 PIC TUs + link). M3 gate VERIFIED **145/0**. Repros written standalone under SCRIP alone: `rc.pas` (same-named record locals in two procs → prints 99, should be 5 — collision confirmed); `sc.pas` (same with scalars → prints 5, no collision — isolation confirmed). Fix surface pinned: three parser-side sites in `pascal.y` — (a) `pas_array_add`/`pas_recvar_add` at decl (add proc-scope key, mirrors `pas_cap_mangle` pattern for scalars), (b) main-hoist at `:533` (must stop hoisting proc-locals — only true globals need one-time init; `mk_proc:200` already emits per-proc init in each proc body), (c) `pas_resolve_name` (qualify record/array names per-proc, mirroring the scalar `pas_cap_find`/`pas_cap_mangle` branch). Requires `pascal.y` edit → `bison -d -o pascal.tab.c pascal.y` regen → `rm -f scrip && make` → M3 145/0 + M4 145/0 + 4-lang gate (SN4/ICN/PL/RK smokes). Gate scripts: recreate `/tmp/run_gate_m3.sh` + `/tmp/run_gate_m4.sh` each session (in /tmp). M4 links `-lscrip_rt -lm` (NO `-lgc` — Boehm deleted); input files use `.in` extension. fpc not installed in this sandbox; fpc oracle NOT required for RUNG 2 (repro/verify with SCRIP alone via `rc.pas` → `5` = fixed).

## ▶ ACTIVE RUNG (TOP / FIRST, PIVOT per Lon 2026-08-07) — PAS-ZFRAME: Pascal fully working under ζ-FRAMES ON THE STACK

**THE ASK (Lon, verbatim intent):** get Pascal fully working again, and get it there *under the ζ-frames-on-the-stack mode* — not by re-propping the GLUE enter/whack symmetry that just collapsed under it.

**WHY THIS IS THE RIGHT PIVOT AND NOT A DETOUR.** Pascal's activation ζ was ALREADY frames-on-the-stack and was measured so twice: ZB-PAS-RSP-1 ("Pascal activation ζ is already `sub rsp,K`+`mov rbp,rsp`+`[rbp+off]` corpus-wide, 143/143") and ZB-PAS-FC ("PASCAL ζ IS ALREADY ON THE RSP/C-STACK UNDER THE FORTH DEFAULT"). Both of those readings were true of a world where `xa_flat_prologue` seeded rbp. CARVE-KILL (`ef9a7d2c`/`1ba33ea6`) deleted that prologue; the enter that replaced it inherited a guard written when the prologue still covered the jmp-entry class, and the surviving whack inherited a `true` from the same era. Pascal therefore did not *choose* to leave frames-on-stack — it was **evicted from the regime it already lived in, silently, by a shared-emitter rung that had no Pascal gate to trip.** PAS-ZFRAME is a RESTORATION to a regime whose emitter arm already exists, is already wired, and already carries a killswitch — not a new storage model.

**THE ONE-LINE SHAPE OF THE FIX (do not let it grow without a measurement forcing it).** `lower_pascal.c` sets `zframe_graph = 1` on the graphs it produces, mirroring `lower_icon.c:1422-1423`, behind killswitch `SCRIP_PAS_ZFRAME=0`. Everything downstream is per-graph flag routing that already exists.

### Steps

- **PAS-ZF-0 — BASELINE + INSTRUMENT. ✅ DONE (2026-08-07, this session). NO CODE LANDED.** Crater frozen from a clean clone + clean `-O0` build: M3 87/65/23/1, M4 72/80/23. Root cause bracketed to the instruction (unpaired `bb_glue_framed_leave`). Discriminator measured (`jmp=1`). Correlation measured (52/54 proc-bearing programs fail). Two candidate fixes falsified BEFORE writing any (see LIVE CURSOR (a)/(b) — do not re-spend them). Gate scripts recreated per § Session Setup; classification runner writes `/tmp/m3_results.tsv` / `/tmp/m4_results.tsv` (per-probe class: PASS · EMPTY_rcN · CRASH134/139 · WRONG_rcN · TIMEOUT · NOREF) so the next session can diff probe-sets, not just counts.

- **PAS-ZF-1 — CHOOSE THE ARM BY MEASUREMENT, NEVER BY READING.** ⭐ **START FROM THIS MEASUREMENT (PAS-ZF-10's gate, run at PAS-ZF-0): THE BREAKAGE IS TWO SHAPES, NOT ONE — a fix that closes only the first will look 80% right and still leave a live crater.** `test_gate_pas_frame_pairing.sh` on the 4-probe witness set reports 6/6 regions unpaired in exactly two flavours: **SHAPE A** `push_rbp=0 pop_rbp=2 mov_rbp_rsp=0 mov_rsp_rbp=2` (`proc_noop`, `proc_another`, `proc_five`, `proc_inner`) = pure unpaired teardown at BOTH γ and ω, no pin at all; **SHAPE B** `push_rbp=0 pop_rbp=1 mov_rbp_rsp=1 mov_rsp_rbp=0` (`proc_outer` in `nestvar.pas`, `proc_bump` in `varparam.pas`) = the graph DOES pin `mov rbp,rsp` but never pushed the caller's rbp, and then pops it once. SHAPE B is the more dangerous of the two because it *destroys the caller's rbp* rather than merely reading a stale one, and it is the shape that appears on the probes with NESTED procs and VAR params — i.e. exactly PAS-ZF-3 and PAS-ZF-4's territory. Establish which emitter arm produces each shape BEFORE choosing; an arm that fixes A but leaves B standing will pass `emptyproc`/`flatnoarg` and still fail the nest*/varparam families. `emit.cpp`'s prologue choke has THREE arms a Pascal proc graph could route to; pick by emitting each and running, not by reasoning about which reads best: **(a)** `zframe_graph` → `xa_flat_zframe_prologue` (whole-graph ζ frame on rsp, wire header, rbp pinned); **(b)** `flat_lcl_proc` → ICN-PROC-FRAME s211 (jmp-entry lexical proc WITH params/locals: carves wire-header+param+local slots, pins rbp, calls `rt_lcl_proc_args_install`) — note this arm's own comment describes Pascal's exact shape; **(c)** repair the GLUE enter/whack symmetry for the jmp-entry class. METHOD: temporary env-gated force at the `lower_pascal_proc` choke, one arm at a time, on `emptyproc.pas` (empty proc — the minimal witness) then `p2.pas`-class (proc with a body) then `nestvar.pas` (nested + locals). ACCEPTANCE per arm: `push rbp` count == `pop rbp` count inside every `proc_*` region, AND correct output. ⛔ The comment at `bb_glue_flat.cpp:67` records that blanket-forcing the enter for the jmp-entry class cost **Icon 3 programs** (184/79/30 → 181/82/30) — so arm (c) carries a known cross-language debt that arms (a)/(b) do not. Prefer the arm that touches ONLY graphs Pascal's lowerer produces.

- **PAS-ZF-2 — SET THE FLAG IN THE LOWERER + KILLSWITCH.** One edit at `lower_pascal.c:661 lower_pascal_proc` / `:711-713 bb_program_add`, mirroring `lower_icon.c` verbatim in shape. Killswitch `SCRIP_PAS_ZFRAME=0` MUST reproduce pre-rung HEAD **byte-exactly** (the mechanical anchor; same contract `SCRIP_ICN_ZFRAME=0` carries). ⛔ **`src/contracts/IR.h:256` currently asserts `zframe_graph` is "set ONLY by lower_icon.c … never set by any other lowerer (SN4/Prolog/Raku/Pascal invariance guaranteed by construction)". That sentence becomes FALSE the instant this rung lands and MUST be corrected IN THE SAME COMMIT** — a stale invariant comment is exactly how the next session inherits a false premise. Rewrite it to name the two lowerers and keep the SN4/Prolog/Raku invariance claim, which remains true.

- **✅ PAS-ZF-9 — PARTIAL (2026-08-07, Claude Sonnet s2): SCRIP-direct hi.pas→HI, calc.pas→15 confirmed. SCRIP-pcom→prr round-trip BLOCKED: pcom.pas itself crashes (SIGSEGV) before any write(prr,...); same crash with SCRIP_PAS_ZFRAME=0 — pre-existing, not a PAS-ZF regression. Round-trip unblocks when pcom's own crash is fixed (PAS-PINT-8 territory). recursion.ref created in corpus (fact/fib output verified numerically).**
- **✅ PAS-ZF-10 — DONE (2026-08-07, s1): test_gate_pas_frame_pairing.sh committed; PASS=3/3.**

## ⛔ FACT RULE — PASCAL IS ABSENT FROM EVERY SHARED-EMITTER GATE, AND THAT IS THE ACTUAL DEFECT (2026-08-07, this session)

**RULE:** a rung that edits `src/emitter/`, `src/templates/`, or `src/lower/lower_common.c` must run a Pascal smoke before it lands, exactly as it runs SN4/Icon/Prolog/Raku smokes.

**THE MECHANICAL FACT THAT MAKES THIS A RULE AND NOT A WISH:** `scripts/` contains **zero** Pascal test scripts (`ls scripts/ | grep -i pas` → empty). § Session Setup tells each session to *recreate the gate scripts in `/tmp`* — so the Pascal gate exists only inside whichever session last typed it, and vanishes with that sandbox. Every other language has committed runners under `scripts/`; Pascal's live in `/tmp`. That is the whole explanation for a 15-day, 625-commit silent crater, and no amount of care by the SNOBOL4/Icon sessions could have caught it: **they had nothing to run.**

**COMPLETION TEST:** (a) a committed `scripts/test_gate_pascal_m3.sh` + `test_gate_pascal_m4.sh` exist and are referenced from § Session Setup INSTEAD of the `/tmp` recreate-it-yourself recipe; (b) they are added to whatever shared pre-push sweep the other four languages already use; (c) PAS-ZF-10's pairing gate sits beside them.

**LIMITATION (honest, per this file's house style):** committing the scripts does not force a SNOBOL4 session to run them, and a markdown rule cannot either. It removes the ONE excuse that currently exonerates every past offender — that the gate was not runnable — and makes the omission visible and attributable. The human reviewer stays the enforcer.

## ⚠ PAS-PINT-8 PROVENANCE WARNING (2026-08-07) — THE NAMED REPRO IS NOT IN THE CORPUS

The PAS-PINT-8 cursor names a "clean 20-line standalone repro `ptr2.pas`, verified on clean HEAD" for chained deref of a pointer-valued variant-arm record field. **The `ptr2.pas` at `corpus/programs/pascal/ptr2.pas` is a DIFFERENT, unrelated 10-line file** (`type ip = ^integer` — commit `58a71745`, 2026-06-03, the `ptr1..ptr8` batch), and it PASSES. The described repro was local-only work that was never pushed and is gone with its sandbox — consistent with the many `PUSH PENDING (awaiting credential)` notes throughout this file's history. **Whoever resumes PAS-PINT-8 must reconstruct the repro from the diagnosis prose (`arr^.inx^.form` reads blank; `cpy:=arr^.inx; cpy^.form` reads correctly; `arr^.inx = sub` is TRUE; pcom's `structure` record shape `arrays:(aeltype,inxtype:stp)`) and MUST commit it under a NON-COLLIDING name before spending a session on it.** Do not trust the cursor's "verified on clean HEAD" for a file that does not exist.

## ⛔ WATERMARK — PAS-PINT-7 CLOSED: THREE BUGS FIXED, ARRAY SELF-HOSTING ACHIEVED (2026-07-23 s133, Claude) — SCRIP `1950edde` · corpus `5f85b8b8` · M3 152/0 · M4 152/0. BUG 1: multi-id record field group (`ael, inx: pp`) lost ptr-target for 2nd+ fields — `pas_pend_add` cleared pend flags per-field; fix: save/restore across id-loop (`pascal.y`). BUG 2: whole-record store into deref-base nested variant field (`lsp^.min := lvalu`) clobbered variant tag via SOH splice — new `__pas_nrec_pfield_set` runtime helper (SOH→`\x05`) + route in `mk_assign` deref clause (`pascal.y` + `by_name_dispatch.c`). BUG 3: aggregate locals aliased one cell across recursive activations — over-broad `lower_pascal.c:81` promotion to `__up_<proc>_<var>` globals removed (1 line); frame slots now handle per-activation storage correctly. Prior CORRECTED-CHAINED-PTRFIELD-DEREF finding's diagnosis was partially right (Bug 1 real) but missed Bugs 2 and 3; the original AGG-LOCAL-ALIAS finding was also real (Bug 3). All three had to fall. CONCRETE PAYOFF: pcom now compiles `array[1..5] of integer` correctly (5 units; `chki`/`deci`/`ixa`/`stoi` emitted); SCRIP-pcom→P-code→SCRIP-pint round-trip on array program → `42` verified. Three new gated corpus probes: `disc.pas` (multi-id ptr field), `mir3.pas` (nested-field clobber), `aggloc2.pas` (agg-local recursion). NOTE: the prior cursor's claim that AGG-LOCAL-ALIAS was overturned was incorrect — that finding described a real bug (Bug 3 here). The CORRECTED finding introduced the chained-deref story which was valid for Bug 1 but wrongly dismissed Bug 3.

## ⛔ WATERMARK — PAS-PINT-7 PRIOR DIAGNOSIS OVERTURNED (2026-07-23 session 2, Claude) — NO CODE CHANGED · M3 149/0 · M4 149/0 · .github `28277dde`. Prior "aggregate-local alias" root cause falsified: `ov6.pas` passes on clean HEAD; lower_pascal.c fix would not touch real bug. REAL BUG: chained deref `arr^.ptrfield^.g` reads blank; copy-to-temp-then-deref reads correctly; ptr equality holds. Reduced standalone repro `ptr2.pas` (20 lines). Fault: `pascal.y` `pas_selector_rectype`/`pas_ptrexpr_target`/`pas_rectype_field_ptrto_by_index` chain returns NULL for deref-of-variant-arm pointer field → field index not found → wrong slot read. Also flagged: unexplained working-tree deletion of `lower_pascal.c:81` (`pas_is_agg_local` line, mtime during session, HEAD unchanged); restored to HEAD; all findings re-verified on clean HEAD. FINDING-2026-07-23-CLAUDE-PAS-PINT-7-CORRECTED-CHAINED-PTRFIELD-DEREF.md.

## ⛔ WATERMARK — PAS-PINT-7 DIAGNOSIS: AGG-LOCAL ALIAS UNDER RECURSION (2026-07-23, Claude) — NO CODE CHANGED · M3 149/0 · M4 149/0. Root cause of `ixa`-drop fully bracketed. Bug: `pas_resolve_name` (lower_pascal.c:81) maps aggregate locals to `__up_<proc>_<var>` — one global cell per (proc,var), not per activation. Recursive `selector()` calls alias `lattr`; inner clobbers outer's `lattr.typtr` → `ixa` guard skipped silently. Instrumentation proof: `#DBG-COPY-NONNIL#` both, `#DBG-LATTR-TYPTR-NIL#` SCRIP only after recursive `expression()`. Standalone repro `ov6.pas` (record local under 3-deep recursion): native `300 200 100`, SCRIP `300 300 300`. Scalar locals unaffected. Fix: give agg locals real per-activation frame slots in `lower_pascal.c`. FINDING-2026-07-23-CLAUDE-PAS-PINT-7-AGG-LOCAL-ALIAS-UNDER-RECURSION.md. ← SUPERSEDED; diagnosis was wrong per session-2 measurement.

## ⛔ WATERMARK — PAS-PINT-6: WHOLE-RECORD ARRAY-ELEM COPY + CHAIN LIMIT → pcom→pint ROUND-TRIP RUNS ON SCRIP (2026-07-23, Claude) — SCRIP `e0c0484e` · corpus `b7695121` · M3 149/0 · M4 149/0. `hi.pas`→`HI`, `calc.pas`→`15`, both SCRIP-pcom→P-code→SCRIP-pint == native fpc. TWO fixes: (1) pascal.y `arr[k]:=arr[j]` for array-of-record was single-slot scalar (lost all nf fields) → `pas_recspan_nf`/`pas_recspan_slot`+`mk_assign` span copy; (2) emit.cpp CH_MAX 8192→65536 + `nodes[]`→static. New probe `recspan_copy.pas`+`.ref`(+1→149). FINDING-2026-07-23-CLAUDE-PAS-PINT-6-…

## ⛔ WATERMARK — PAS-PINT-5b: SCRIP-pint CHAR-FIELD WRITE FORMATTED AS INT (2026-07-22, Claude) — SCRIP `ea0bac56` · M3 148/0 · M4 148/0. Real bug: `store[ad+i].vc` (char-typed variant-record-field) formatted as width-10 int ordinal. Fix: pascal.y `fldchar[]` registry dimension + `pas_cvfield` mark machinery + `pas_is_charexpr` clause. New probe `vrec_charwrite.pas`+`.ref` (+1→148).

## ⛔ WATERMARK — PAS-SELFHOST-3d RUNG 2 FIXED (2026-07-21, Claude) — M3 146/0 · M4 146/0 · 4-lang smokes green (SN4/ICN/PL/RK). Record/array-local collision closed. Fix (Pascal-only, 3 sites): `pascal.y` `is_local` flag on `g_pas_arrays[]` + exported `pas_is_agg_local` predicate + main-hoist :533 skips proc-locals; `lower_pascal.c` `pas_resolve_name` mangles aggregate locals per-proc via `pas_cap_mangle` keyed on declaring scope's proc_name (correct for sibling-collision AND capture). `rc.pas` 99→5; `sc.pas`/`rcap.pas` unchanged. New gated probe `rec_local_collision.pas` (+.ref). bison regen + recompile + libscrip_rt rebuild done.

## ⛔ WATERMARK — PAS-SELFHOST-3d RUNG 2 BRACKET CONFIRMED (2026-07-21, Claude) — M3 145/0 · SCRIP `2300678f` (no Pascal commits). libscrip_rt.so built (176 MB). Bug independently reproduced SCRIP-only: `rc.pas` same-named record locals → 99 (wrong); `sc.pas` same scalars → 5 (correct). Fix surface: 3 sites in `pascal.y` — `pas_array_add`/`pas_recvar_add` (add proc key), main-hoist :533 (stop hoisting proc-locals), `pas_resolve_name` (qualify record/array names per-proc mirroring scalar `pas_cap_mangle`). Next session: make the edit, regen, full gate.

## ⛔ WATERMARK — PAS-SELFHOST-3d RUNG 1 FIXED (2026-07-21, Claude) — M3 145/0 · SCRIP `21d7fa17`. Fix: `pas_call_args_brm` routes byref-actual `IR_VAR_REF` name through `pas_resolve_name` so a captured variable's byref references its mangled capture-global cell. Root: `lcp` in `statement` is captured by `forstatement`; searchid wrote handle 55 to the dead frame slot while the caller read the capture-global → blank. Minimal repro: `cap.pas`. pcom(i:=5): 40-byte stall → 147B; ldci+retp+labels+trailer now correct; `sroi 9` still missing = record-local-collision (RUNG 2, next). Prior diagnosis in FINDING-2026-07-20 was wrong (mis-attributed to "field-layout decode"). Corrected in FINDING-2026-07-21.

## ⛔ WATERMARK — PAS-SELFHOST-3c FIXED + EMPTY PROGRAM SELF-HOSTS (2026-07-19, Claude) — M3 145/0 · M4 145/0. `chr(x)`→`__pas_chrlit(x)` (1-line parser fix) so writeln formats it as a character; pcom's `retp` now emits correctly. Empty program (`program t; begin end.`) SCRIP-pcom P-code == native fpc-pcom under `diff -w`. New `chr_writeln` gated probe. Two rungs landed this session (3b emit.cpp IR_VAR_REF + 3c chr). Next rung 3d = climb to real declarations. PUSH PENDING (awaiting credential). Known separate sub-bug: `writeln(ord(charvar))` prints char not ordinal (deferred, likely not a pcom blocker).

## ⛔ WATERMARK — PAS-SELFHOST-3b BRACKETED (2026-07-19, SCRIP HEAD `2a7f718b`, NO CODE LANDED, corpus `nested_vp_writeback.pas` added) — M3 143/0 · M4 143/0; nested-proc var-param write-back bug isolated to `lower_pascal.c:213-215` IR_VAR_REF path; minimal repro in corpus; full bracket in FINDING-2026-07-19-CLAUDE-PAS-SELFHOST-3B-NESTED-VP-WRITEBACK.md

**Session directive (Lon/Jeffrey):** "Switch over ALL ZETA storage over to the FORTH-style RSP/RBP stack. Look at what SNOBOL4 has done recently for guidance with the new ways. Continue."

**VERDICT (MEASURED, MEASURE-FIRST — no code change is the correct outcome, same class as the ZB-PAS-FC no-op watermark below):** Pascal *activation* ζ is ALREADY 100% on the FORTH-style RSP/RBP stack. The switch was accomplished by SNOBOL4's **language-blind REG-MAP ladder** (the "new ways" the directive names) — nothing Pascal-specific was needed because the emitter/templates dispatch on IR shape, not language (the `LANGUAGE-BLIND BB/XA TEMPLATES` FACT RULE).

**WHAT SNOBOL4 DID (the guidance, read this session):** the **REG-MAP SIX-REGISTER RULING** (Lon s73, `GOAL-SNOBOL4-BB.md`): RSP = ζ FORTH top + C stack · **RBP = ζ activation FRAME BASE** (saved-rbp linkage chain) · R12 = pattern-assignment pend/dcap top · RBX = GC top · R13/R14/R15 = Σ/δ/Δ subject-scan · GVA = register-free (pinned-VA absolute-disp32). Ladder REG-0…REG-7 landed s73→s86; **REG-3 FRAME-RBP** is exactly "frame on rbp, stack on rsp," and **REG-7 U1** seeds `mov rbp,rsp` in the shared `flat_α` outer wrapper. SNOBOL4's *extra* tenants (r12 pend, r13/14/15 scan, rbx GC) are **pattern/scan machinery** — Pascal has none of it, so those registers are simply unused by Pascal.

**MEASURED corpus-wide (143 `.s`, M3 `--run` + M4 `--compile`, fresh plain build — no `ZCFLAGS`):**
- Every activation frame: `sub rsp,<compile-time K>` (FORTH bump, no dynamic-size block) + `mov rbp,rsp` (frame base) + locals `[rbp+off]`. deep5 (5-level nest) + corpus sweep confirm.
- **`and rsp,-16` (align-dance) = 0 files** (the old ZB-PAS-RSP-1 target — already gone).
- **`rt_zls2_push` (arena frame push) = 0 files.**
- **MAIN frame (`flat_α`) is on the stack too** (`sub rsp,65544` + `mov rbp,rsp`) — the old "MAIN stays arena" prose is stale; even ZB-PAS-RSP-6 is effectively satisfied.
- **r12 = exactly 1 ref/program, byte-identical in all 143** (`mov r12,[RT_CAS_TOP]`): the SHARED **REG-6 pend/dcap outer seed** (`xa_flat.cpp:199`/`:266`, byte-verified vs `as` s80, UNGATED). **0 `[r12+…]` dereferences anywhere in Pascal code** — loaded-but-never-used because Pascal has no patterns. **NOT REMOVABLE:** it lives in language-blind shared `xa_flat`; gating it out for Pascal needs a language conditional (violates the FACT RULE) and breaks SNOBOL's pend stack. The old ZB-PAS-RSP-4 gate `grep r12==0` is OBSOLETE — under REG-MAP r12 is a legitimate global register tenant, and Pascal's *frames* are r12-free (the real point).

**ESCAPERS stay off-spine BY NECESSITY (the model, not a gap):** program globals → pinned-VA absolute-disp32 (`[0x70000000+off]`, REG-1 GVA-DEREG — program lifetime, cannot be LIFO-stacked); `new`/`p^` → GC heap cells (`__pas_alloc`/`pas_heap_cell` — dynamic lifetime, explicitly outlives the box); uplevel `__up_*` → arena-promoted (shared across recursive activations). This is the SAME escaper partition SNOBOL4 uses and matches the directive's own "escapers to the heap" framing from the prior FC session.

**STALE RSP-REGRESSION FLAG RETIRED:** the prior cursor claimed the `ZC_FRAME_RSP` default (`f7de3863`) breaks Pascal to M3 109/34 at `96fb698c`, needing a `-DZC_FRAME=0` R12 rebuild. It NO LONGER REPRODUCES — plain default build at `409f3508` = **M3 143/0/1 · M4 143/0/1**. The shared REG-MAP work between the two hashes fixed the byref/var-param SIGABRTs; no `zeta_choices.h` edit needed.

**Context: `409f3508` is ahead of `96fb698c` (LAST SESSION's push) — parallel sessions (Prolog/GC/coexpr, s65→s90) advanced HEAD; no Pascal-lowerer commits between, so the SELFHOST-2 correctness state is unchanged. No credentials needed until session-close push; SCRIP/corpus untouched, so no `.s` regen.**

---

## ⛔ WATERMARK — PAS-SELFHOST-2 NARROWING (2026-07-15, SCRIP HEAD 96fb698c R12 build, corpus untouched, NO CODE COMMITTED) — record-field char-array store bug isolated; pcom-independent 13-line repro in hand; RSP regression measured and flagged

**Session directive (Lon/Jeffrey):** "Continue to climb toward getting PCOM and PINT Pascal programs working as self hosting. Continue."

**RSP REGRESSION (measured, not fixed — ⚠ flag for Lon):** A fresh clean build of `96fb698c` under the committed `ZC_FRAME_RSP` default gives Pascal **M3 109/34** (34 rc=134 SIGABRT in byref/var-param probes). Rebuild `ZCFLAGS="-DZC_FRAME=0"` (both `scrip` + `rm -f out/libscrip_rt.so && make libscrip_rt ZCFLAGS="-DZC_FRAME=0"`) → **M3 143/0/1 + M4 143/0/1**. The RSP default landed in a Prolog session (`f7de3863`) before ZB-PAS-RSP-1..6 (Pascal-under-RSP correctness) were completed. Decision: work on R12 this session; RSP-default-for-Pascal is Lon's policy call.

**FAULT ISOLATED (two-oracle bracket, pcom-independent):** SCRIP-pcom stall on `var i:integer;` reduced to a **13-line repro** with no pcom involved:
- `rec_store.pas` (`id:='integer '; r.name:=id; writeln(r.name)`) → SCRIP `         0` (integer zero), native `integer ` → **record-field char-array store writes integer 0**
- `cmp_plain.pas` (plain char-array assign+compare, no record) → SCRIP `1` == native `1` → plain assign is fine
- `rec_name.pas` (store then compare `r.name=id`) → SCRIP `2` (unequal), native `1` → consistent with corrupted field
- All three independently confirmed with fpc-pcom+pint oracle (built live `/tmp/p4boot/`, fpc 3.2.2)

**FIX TARGET:** `lower_pascal.c` `lower_assign`, TT_IDX/`__pas_field_set` path when LHS is a record field and RHS is a whole char-array variable — it defaults to integer 0 instead of routing the SOH char-array into the field. Sibling of the byref-field-write fix that landed in 96fb698c (same file/function, different branch). **After fix: full 4-language gate + M3/M4 143/0 recert on R12 build.**

---

## ⛔ WATERMARK — PAS-SELFHOST CREATED + MADE #1 (2026-07-15, SCRIP HEAD clean build, corpus untouched) — fpc bootstrap self-hosts (empty diff); SCRIP-pcom stalls on `var`-decl, bisected; NO FIX CODE LANDED; tree clean
**Session directive (Lon):** "Does PCOM.pas and PINT.pas work now? Can they self host? If not make a RUNG and steps and make it so number one. Continue."
**MEASURED — TWO senses of "self-host", both tested, not inferred:**
1. **Classic P4 bootstrap under `fpc` (the corpus `.ref` oracle): ✅ SELF-HOSTS byte-for-byte.** Built `pcom`+`pint` with fpc 3.2.2 (`fpc -Ci -Co -Cr -gl`). Ran the README fixpoint: `pcom` compiled itself to **21044 lines** of P-code (`pcom.pc`), then `pcom`-as-P-code running on `pint` recompiled its own preprocessed source; `diff -w pcom.pc prr` is **EMPTY**. The 50-year-old compiler regenerates its own object code exactly. This is the provenance basis for the corpus refs — confirmed live.
2. **SCRIP running pcom.pas (the actual GOAL-PASCAL-BB frontier): ❌ DOES NOT SELF-HOST.** SCRIP runs pcom.pas, pcom starts (emits its source listing) but **stalls SILENTLY** — rc=0, **0-byte `prr`**, no error, no crash. Reproduces on a **3-line** input. Native fpc-pcom compiles the same input fully.
**BISECTION (the first-divergence bracket, per RULES.md MONITOR-FIRST):**
- `program t; begin end.` → SCRIP-run pcom **WORKS** (139-byte prr; native = 12 P-code lines). pcom's basic parse→emit loop executes under SCRIP.
- `program t; var i:integer; begin end.` → SCRIP-run pcom **STALLS** after echoing the `var` line (0-byte prr). Adding the single `var` declaration is the trigger.
- ⟹ **The bug is inside pcom's OWN variable/type-declaration control flow as executed by SCRIP** (pcom's `block`/`vardeclaration`/`typ`/`simpletype` machinery — a `while`/`case`/`repeat` scan that never terminates, or a value-returning call SCRIP fails to make pcom branch on). This is MORE UPSTREAM than the retired watermarks claimed (they reported pcom reaching `inittables/instrmnemonics`) — either a regression since 6d86723a, or those measured a different pcom variant. PAS-SELFHOST-1 brackets it precisely; do not guess.
**PROVENANCE NOTE (uploaded vs corpus P4):** the session's uploaded `comp.p`/`int.p` are the ORIGINAL ETH/CDC-era P4 (Ammann/Nori/Jacobi compiler; Jensen/Wirth/Jacobi interpreter); the corpus `pcom.pas`/`pint.pas` are Christophe Staiesse's 2013 FPC/Delphi port of the SAME lineage. Minor text differences (e.g. `realsize=1` vs `2`, CDC directive lines). The **corpus `pcom.pas`/`pint.pas` are the oracle** for this rung (they generate the `.ref` files); `comp.p`/`int.p` are a cross-reference for semantics.
**ENV NOTE:** this sandbox checks the repos out under `/home/claude/work/{SCRIP,corpus,.github}`; symlinked to `/home/claude/{SCRIP,corpus,.github}` so the goal-file scripts' absolute paths resolve. `libgc-dev` was the missing build dep (fresh container) — `scripts/install_system_packages.sh` must run before `make`.
**PAS-SELFHOST-1 BRACKETING STARTED (this session, no fix yet) — the decl ladder revealed TWO DISTINCT failures, not one:**
- **`const k=5;` → CRASH rc=134 (SIGABRT), NAMED:** `[IDX] BOMB rt_assign_var: lvalue is not a variable (dtype=1) — string/record subscript assignment is the tvsubs rung (GOAL-IR-IMMUTABLE-EMIT IDX-UNIFY)`. pcom's constant-table build (`constant`/`enterid`/the const-declaration path) emits a STRUCTURED assignment (`something[idx] := val`, dtype=1 = string/record subscript) that SCRIP's `rt_assign_var` sink cannot handle — it aborts. This is the IDX-UNIFY / tvsubs class. Fires at line-1 echo (during pcom init, before the const line is even echoed).
- **`var i:integer;` → SILENT ω-EXIT (NOT a hang):** rc=0, 0-byte prr, exits in ~1.7s (timed — not an infinite loop). pcom echoes the `var` line WITHOUT its trailing newline then unwinds to program end. This is the Byrd-box IR_SUCCEED/IR_FAIL-invisibility class (a label landing / empty body that must be an IR_GOTO box but ω's out instead). pcom's `vardeclaration` (offset/display-level allocation via `alignquot`/`aligned`) is the locus.
- **`type ti=integer;` WORKS** (139-byte prr) — so it is specifically pcom's *variable* and *constant* declaration paths, not its type machinery. Two separate brackets → likely two separate SELFHOST-2 fixes (const-crash = IDX-UNIFY sink; var-stall = port-wiring/ω).
- Repro fixtures live in `/tmp/p4boot` (`l_const.pas`, `l_var1.pas`, `l_type.pas`, etc.); NOT committed to corpus (they are pcom-INPUTs, not SCRIP gate probes — corpus `.pas` are run directly by SCRIP, a category mismatch). gdb NOT yet installed; the rc=134 crash gives a backtrace target for the next session (`apt-get install -y gdb`, break on the `rt_assign_var` bomb, backtrace to the pcom statement).
**No credentials needed until session-close push. No codegen touched — no `.s` regen.**

## ▶ ACTIVE RUNG (TOP / FIRST, #1 per Lon 2026-07-15) — PAS-SELFHOST: SCRIP self-hosts pcom.pas → pint fixpoint
**Goal:** Make SCRIP execute the canonical Pascal-P4 compiler **`pcom.pas`** correctly enough to compile real Pascal programs to P-code, culminating in the **self-hosting fixpoint on SCRIP**: SCRIP runs pcom to compile pcom's own (ppp-preprocessed) source, the emitted P-code runs on `pint`, and the result matches the fpc-oracle P-code. This is the Pascal analogue of the SCRIP stage-2 milestone (PLAN.md Milestone 2) and the single most demanding correctness test the Pascal frontend can face — pcom exercises records, variant records, pointers, sets, nested procedures, `with`, `case`, goto, forward decls, and deep recursion all at once.

**Why this is the right #1 (and what it subsumes):** pcom is the ultimate Pascal corpus program. A SCRIP that runs pcom correctly has, by construction, correct var/type declarations, statement lowering, procedure activation, and aggregate handling at scale — so PAS-SELFHOST is a super-gate over PAS-VAR / PAS-GVA / PAS-UNBOX correctness. The prior #1 (ZB-PAS-RSP, free r12) is a PERFORMANCE/register-hygiene rung; it is DEMOTED to run after SELFHOST (a correct compiler first, then a leaner one). ZB-PAS-RSP is NOT abandoned — its census stands and it resumes once pcom self-hosts (or in parallel if a second session takes it, since its files barely overlap SELFHOST's lowerer work).

**The oracle chain (all live, this session):**
- `fpc`-built `pcom`/`pint` (in `/tmp/p4boot`, rebuildable via corpus `programs/pascal/Makefile`). `pcom < prog.pas` → `prr` (P-code); the fixpoint diff is empty.
- Every intermediate: for any `prog.pas`, `native_prr = (./pcom < prog.pas)` is the exact P-code SCRIP-run pcom must eventually emit. SCRIP-run pcom's `prr` is diffed against `native_prr` — the FIRST LINE that differs (or the point SCRIP stalls) is the bracket.

**Debug methodology (RULES.md MONITOR-FIRST — NON-NEGOTIABLE):** the stall is FOUND, not guessed. Because pcom is a deterministic Pascal program, the bracket is: run SCRIP-pcom under gdb with the Pascal source line pcom is echoing as the coordinate; the stall is between the last emitted P-code op and the next expected one. The two-step gdb hit-count practice applies (the fault is inside a hot loop — pcom's insymbol/block scanner). SCRIP-pcom and native-pcom are the two oracles; their divergence brackets the bug to one pcom statement → one IR construct SCRIP mis-executes.

### Steps (top-to-bottom; first `- [ ]` is the cursor; each ends at its gate)
- [x] DONE — PAS-SELFHOST-1
- [ ] **PAS-SELFHOST-3 — CLIMB THE DECLARATION SECTION.** Extend the repro toward pcom's real declaration complexity: multiple vars, `type` aliases, enumerated types, subranges, arrays, records, variant records, pointers, sets, `forward` decls. Each new form that stalls/diverges is a sub-bracket → fix → re-diff. Drive until `program t; <full pcom-style declaration block>; begin end.` compiles under SCRIP-pcom byte-matching native. Gate: a growing `selfhost_decls*.pas` set, each SCRIP-pcom-compiles to native-matching P-code; Pascal M3+M4 green throughout.
- [ ] **PAS-SELFHOST-4 — CLIMB THE STATEMENT/PROCEDURE BODY.** Now bodies: assignment, if/while/for/repeat/case/with/goto, nested procedure decls + calls, value/var params, recursion. Same bracket→fix→diff discipline against native-pcom P-code. Milestone: SCRIP-pcom compiles the corpus's own small programs (`sieve.pas`, `recursion.pas`) to native-matching P-code, and that P-code runs correctly on `pint`. Gate: `sieve`/`recursion` round-trip (SCRIP-pcom → prr → pint → correct output) matches the fpc-pcom round-trip.
- [ ] **PAS-SELFHOST-5 — pcom AT SCALE (inittables/instrmnemonics and beyond).** Drive SCRIP-pcom through pcom's own initialization tables (the retired watermarks' `inittables`/`instrmnemonics` frontier) and the full compiler body. Expect slot-base / ζ-region-sizing issues at pcom scale (the retired PCOM-NEXT-BLOCKER hypotheses: per-proc slot-base arithmetic at `[r12+8352]`-class offsets, proc-context global resolution). Bracket each with the monitor/gdb. Gate: SCRIP-pcom compiles a substantial pcom subset (e.g. the whole declaration + init section) matching native P-code.
- [ ] **PAS-SELFHOST-6 — THE FIXPOINT ON SCRIP (the milestone).** SCRIP-pcom compiles pcom's own ppp-preprocessed source (`pcom.pp`) to P-code; that P-code equals the fpc-oracle `pcom.pc` (via `diff -w`); running it on `pint` against `pcom.pp` reproduces the fixpoint. Equivalently: replace `./pint < pcom.pas | ./pcom` (native) with SCRIP-run pcom and get the identical 21044-line result. Gate: `diff -w <SCRIP-pcom output> pcom.pc` empty; the P-code self-compiles on pint. Record in `corpus/BENCHMARKS-PASCAL.md` and a `FINDING-*` doc. This is the Pascal self-hosting fixpoint on SCRIP — the rung's terminal success condition.

### PAS-SELFHOST landmines
- **MEASURE-FIRST, MONITOR-FIRST:** never fix by reading/guessing. The two-oracle bracket (SCRIP-pcom vs native-pcom P-code diff) + gdb hit-count is the ONLY sanctioned hunt. A markdown rung cannot make pcom self-host; the mechanical bracket makes each bug findable.
- **CORPUS `pcom.pas`/`pint.pas` ARE THE ORACLE — not the uploaded `comp.p`/`int.p`.** They differ (Staiesse port vs ETH original). Refs derive from the corpus pair. Cross-reference `comp.p`/`int.p` for semantics only.
- **SILENT STALL ≠ CRASH.** The failure mode is rc=0 with empty output — a Byrd-box ω propagating to program end (IR_SUCCEED/IR_FAIL invisibility is a documented Pascal hazard: label landings + empty bodies must be IR_GOTO boxes). Watch for this class specifically.
- **SHARED FILES:** any fix in shared lowerer helpers / emitter templates / runtime sinks → ALL FOUR language gates + template byte-identity before landing (landmines #4/#6 below). Pascal-only lowerer arms (`lower_pascal.c`) are lower-risk.
- **pcom-SCALE ζ SIZING:** at full-pcom scale the retired PCOM-NEXT-BLOCKER (per-proc slot-base / `rt_zls_alloc` region sizing) may resurface; that investigation stopped at `src/runtime/rt/zeta_alloc.c` (located, not read). Read it there when SELFHOST-5 reaches scale.
- **BUILD DEPS + PATHS:** `scripts/install_system_packages.sh` (libgc-dev mandatory) before `make`; this sandbox needs the `/home/claude/work/*` → `/home/claude/*` symlinks for embedded-path scripts.

## ⛔ WATERMARK — ZB-PAS-RSP-1 CENSUS (2026-07-14, SCRIP HEAD clean build, corpus untouched): M3 137/4-oos · M4 143 compile-clean — NO CODE LANDED; align-dance load-bearing confirmed; root cause measured; tree clean
**Session directive (Lon):** "Move all BB's ZETA to RSP-topped stack and free up R12. Does Pascal have any escapee-type BBs like Icon does? If so those go on the heap. So then R12 will be free. Continue."
**ESCAPER ANSWER (measured, not asserted):** No. Verified from P4 oracle (`comp.p`/`int.p` — no coroutine/generator/coexpr/resume/suspend keyword exists in the language) and SCRIP Pascal frontend + lowerer (`lower_pascal.c` — zero IR_SUSPEND/IR_COEXPR/IR_RESUME machinery; the only `fail`/ω edges are ordinary Byrd-box goto/return ports). The one real escaper — heap data via `new(p)` — already routes to `__pas_alloc`/`pas_heap_cell`/`g_pas_heap` (runtime GC cell array), off-spine. Every Pascal activation is strict LIFO → rsp-eligible ↔ no escapers: same fact from two directions. R12 CAN be freed.
**ALIGN-DANCE CENSUS (env-gated SCRIP_ALIGN_NOOP=1 + SCRIP_ALIGN_ASSERT=1, reverted clean after):** 137/137 baseline-passing Pascal probes SIGILL when the `x86_align_enter`/`x86_align_leave` dance is no-op'd. Zero survive. The dance is genuinely load-bearing on every program — "maybe dead weight" hypothesis falsified.
**ROOT CAUSE (measured from aa1.s with dance off):** Structural, universal, single shape. Every `flat_α` prologue: `push r12` (rsp: 8→0 mod 16, aligned) → `push rsi` (rsp: 0→8 mod 16, misaligned scratch-save) → `call rt_zls_mark` (executes misaligned → SIGILL). Symmetric at `rt_zls_release_to`. The `and rsp,-16` was masking this on every program. Fix target: the ζ-lifecycle call emitter — replace `push rsi / call / pop rsi` with either `sub rsp,8 / push rsi / call / pop rsi / add rsp,8` (pad) or `mov [r12+slot],rsi / call / mov rsi,[r12+slot]` (slot-spill, keeps push-count even without a sub). By-construction alignment invariant: even number of 8-byte pushes between entry `push r12` and every C call.
**ORACLE:** `SCRIP_ALIGN_NOOP=1 SCRIP_ALIGN_ASSERT=1 ./scrip --run <prog>` — 0 SIGILL is the RSP-1 done condition. The env-gate hook was wired in `x86_align_enter`/`x86_align_leave` (x86_asm.h) and REVERTED after census; it should be re-landed as sanctioned instrumentation at RSP-1 start.
**No credentials or `.s` regen needed this session (no codegen touched).**

## ✅ RUNG #2 — ZB-PAS-RSP: rsp-as-ζ, free R12 — **SUPERSEDED-BY-REG-MAP / DONE-BY-INHERITANCE (2026-07-17, see top watermark).** Do NOT build the RSP-0..6 ladder below: it targets a pre-REG-MAP world (Pascal on `[r12+off]`, 49,291 r12 refs, 2,068 `and rsp,-16` sites, MAIN-on-arena) that no longer exists. Measured at HEAD `409f3508`: Pascal activation ζ is already `sub rsp,K`+`mov rbp,rsp`+`[rbp+off]` corpus-wide (143/143, M3 143/0 · M4 143/0); align-dance = 0; the lone r12 is the shared REG-6 pend seed (inert for Pascal, not removable). SNOBOL4's language-blind REG-MAP (REG-3 FRAME-RBP + REG-7 U1) carried Pascal here with zero Pascal code. The RSP-0/RSP-0-census prose below is retained ONLY as the empirical record of how the migration was analyzed; the step checklist is closed.
### (original header, historical) — ZB-PAS-RSP: rsp-as-ζ, free R12 (Lon directive — "ALL ZETA on RSP, one zeta register")
**Goal:** Retire r12 as the Pascal ζ frame-base register. Address ALL activation ζ **rsp-relative** (`[rsp + Δ + off]`) instead of `[r12 + off]`, so r12 becomes a free callee-saved register. This is the documented `ZC_FRAME_RSP` end-state (`zeta_choices.h:135`) — "the register-cursor end state" the INLINE/OWNED notes park as a follow-on. Pascal is the correct FIRST language to reach it (feasibility measured below).

**Why Pascal first + feasibility (MEASURED this session, 143 probes):**
- **rsp-ELIGIBLE — zero dynamic-size stack blocks.** 0 ARB/ARBNO; all 726 `sub rsp,<reg>` sites are `sub rsp,rax` proc-frame allocs where rax is a COMPILE-TIME-known per-proc frame size (→ becomes `sub rsp,<const>`). No variable-size allocation exists, so the emit-time rsp displacement Δ is STATICALLY determinable at every site. (SNOBOL/Icon CANNOT do this yet — their ARB/ARBNO push runtime-variable amounts below the mark; this is the `ZC_PORT_OWNED` "rsp becomes reachable at ZB-OWN-1+ once region coalescing resolves static/dynamic coexistence" blocker. Pascal has no such coexistence.)
- **C-ABI CLEAN — r12 is not read by the runtime.** All 15 runtime r12 mentions are in `rt_coexpr.c` (Icon coexpr register marshalling, Pascal never hits it) + doc comments. The frame is passed as a C pointer (rdi); r12 is merely callee-saved. Freeing r12 touches ZERO runtime ABI.
- **UPLEVEL ALREADY OFF-FRAME — scoping does not break.** `[r12+0]` (static-link slot) appears 326× but is READ 0× for uplevel; the actual uplevel mechanism is the 16 `__up_*` arena cells (`[rbx+k*16]`). So dropping r12 does NOT break lexical scoping — the SL slot is dead weight.

**THE ONE HARD BLOCKER (MEASURED — must be cleared FIRST): 2,068 `and rsp,-16` sites.** The pre-C-call alignment dance rounds rsp DOWN by a RUNTIME-VARIABLE amount (0–15), which desyncs static rsp-relative addressing (a slot at `[rsp+Δ+off]` is wrong the instant rsp was `and`-aligned by an unknown amount). rsp-as-ζ REQUIRES replacing `and rsp,-16` with BY-CONSTRUCTION 16-alignment (every sub/add a 16-multiple; frame origin 16-aligned; the same discipline the FORTH cells already use — "bumps round up to 16 so the ambient call-site alignment mod 16 is preserved"). This is a MANDATORY prerequisite step, not optional.

**Design — the emit-time rsp-Δ tracker (the "activation-local-offsets contract"):** add an emit-time counter `g_emit.rsp_delta` = how far rsp currently sits BELOW the frame origin. EVERY rsp-mutating emit primitive (`x86_sub`/`x86_add` on rsp, `x86_push`/`x86_pop`) updates it. Frame-slot addressing (`FR`/`FRQ` + the frame lea/store/load helpers) gains an rsp-mode arm: emit `[rsp + rsp_delta + off]`. **#1 correctness hazard: EVERY rsp mutation must route through a tracked primitive — any raw `sub rsp`/`push` in a template that bypasses the tracker silently desyncs all offsets** (the monitor/gdb bracket methodology from RULES.md is the debug path when it does). **LANGUAGE-BLIND:** frame-mode is a per-graph PROPERTY — a graph with zero dynamic-size stack blocks is rsp-eligible; one with ARB/ARBNO is not. Dispatch on that property (IR shape), NEVER `LANG_PASCAL`. Pascal graphs always qualify; SNOBOL graphs with ARBNO stay on R12. `ZC_FRAME_RSP` (build-const today) becomes the rsp-eligible arm.

### Steps (top-to-bottom; first `- [ ]` is the cursor; each ends at its gate)
- [x] DONE — ZB-PAS-RSP-0
- [ ] **ZB-PAS-RSP-1 — DELETE THE ALIGN-SAVE/RESTORE WRAPPER; by-construction 16-alignment (mandatory prerequisite; scope refined by RSP-0 census).** NOT merely "replace `and rsp,-16`" — the census proved it never stands alone. RSP-1 DELETES the entire ~5-instruction wrapper `push rsp; push qword ptr[rsp]; and rsp,-16; call …; mov rsp,[rsp+8]` at all 2068 sites (≈8K instructions removed) and instead keeps rsp 16-aligned BY CONSTRUCTION: frame origin 16-aligned, every sub/add a 16-multiple, alignment pads static. PAYOFF: once the wrapper is gone, the `mov rsp,[...]` restores vanish WITH the `and`, so the RSP-2 tracker never models a restore — remaining rsp mutations reduce to the clean statically-trackable set (`push`/`pop` ±8, `sub/add rsp,imm`, one `sub rsp,<framesize>`). Gate: 0 `and rsp,-16` AND 0 `mov rsp,[rsp+…]` restore in Pascal `.s`; full M3+M4 143/0; rsp net-balanced at every graph exit; four-language byte-identical (the wrapper is emitted by a SHARED call-alignment helper — non-Pascal must stay byte-identical or the helper gates on rsp-eligibility).
- [ ] **ZB-PAS-RSP-2 — EMIT-TIME rsp-Δ TRACKER + rsp-mode slot addressing (keystone).** Add `g_emit.rsp_delta`; route all rsp-mutating primitives through it; give `FR`/`FRQ` + frame lea/store/load an rsp-mode arm (`[rsp+Δ+off]`). Gate FIRST with frame-mode still R12 → output byte-identical (tracker runs, addressing unchanged). Then flip ONE probe to rsp-mode and verify slot addresses by inspection + run. Gate: byte-identical in R12 mode (all languages); one Pascal probe correct in rsp-mode.
- [ ] **ZB-PAS-RSP-3 — CALLEE-SELF-ALLOC frame (retire the caller-alloc trampoline for Pascal).** Proc entry: `sub rsp,<compile-time framesize>` (from zls hiwater) instead of caller `rt_proc_call_open`→`sub rsp,rax`→`rt_frame_prep`→rdi; frame_origin = rsp after; Δ:=0. Proc exit: `add rsp,framesize` before ret. Recursion nests naturally (each activation subtracts its own framesize). SHARED rt.c bracket sites → keep non-rsp-eligible graphs on the caller-alloc path (dispatch on the predicate). Gate: full M3+M4 143/0; nest*/recursion probes green; four-language gate byte-identical.
- [ ] **ZB-PAS-RSP-4 — DROP r12 (the payoff).** Remove `push r12; mov r12,rdi` prologue + `pop r12` epilogue for rsp-mode graphs; r12 now unreferenced → free callee-saved register. Gate: `grep -c '\br12\b'` == 0 across ALL Pascal `.s` (from 49,291); full M3+M4 143/0.
- [ ] **ZB-PAS-RSP-5 — byref / var-param address correctness under rsp.** `IR_VAR_FRAME_REF`/`IR_ASSIGN_FRAME_REF` lea of a frame slot → `[rsp+Δ+off]`, valid while the callee holds it (caller frame stays live below callee). Gate: varparam/vparr/vpfld/nestpv* green M3+M4.
- [ ] **ZB-PAS-RSP-6 — MAIN frame onto the C stack (strict "zero arena ζ").** Move the memoized-arena MAIN frame (`rt_frame`, `main:` calls it at the driver entry) to a program-entry `sub rsp`; handle the driver re-entry model (`scrip.c :1290-1302/:1307-1379`, the documented follow-on). After this, ALL Pascal ζ is on rsp with zero arena-resident ζ. Gate: full M3+M4 143/0; MAIN-body temps correct; driver re-entry (multiple --run in one process) still clean.

### ZB-PAS-RSP landmines
- **EVERY rsp mutation through the tracked primitive** — a single raw `sub rsp`/`push`/`pop` in any template bypassing `g_emit.rsp_delta` silently desyncs all slot offsets downstream. This is THE bug class; hunt it with the monitor bracket + gdb hit-count, not by reading.
- **`and rsp,-16` must be GONE before RSP-2** — attempting rsp-relative addressing while any data-dependent rounding remains guarantees wrong reads. RSP-1 is a hard prerequisite, not a nicety.
- **Alignment after dropping `push r12`** — entry rsp is 8 mod 16 (return addr); `push r12` used to realign to 0. Without it, `sub rsp,framesize` must include the +8 realign so C calls stay 16-aligned. Static, but fiddly.
- **SHARED emitter + rt.c bracket sites** (RSP-2/3/6) → run ALL FOUR language gates + template byte-identity before landing; non-rsp-eligible graphs (SNOBOL/Icon ARB/ARBNO) MUST stay byte-identical on the R12 path.
- **`ZC_FRAME_RSP` is build-const today** — the per-graph rsp-eligibility dispatch is the new mechanism; do NOT gate on `LANG_PASCAL` (fails `BB-TEMPLATES-LANG-AUDIT.md`).

## ⛔ WATERMARK — ZB-PAS-FC IS A NO-OP: PASCAL ζ IS ALREADY ON THE RSP/C-STACK UNDER THE FORTH DEFAULT (this session, SCRIP clean build from HEAD, corpus untouched): M3 6/6 smoke + M4 143/143 compile-clean — NO CODE CHANGED; the "move all BB ζ to the RSP FORTH stack, escapers to heap" directive was measured already-satisfied for Pascal
**Directive this session (Lon):** "Move all BB's ZETA to the RSP-topped FORTH-like stack, except escapee-type BB's which go on the heap." Applied SNOBOL4's MEASURE-FIRST law before widening anything. GROUND TRUTH (the goal-file prose below predates the FORTH/CSTACK default and never recorded this):
1. **The compiled default is already `ZC_PORT_FORTH` (=6)** (`src/contracts/zeta_choices.h:97`), the CSTACK-superset RSP model. The four `rt.c` proc-frame bracket sites (`rt_call_proc_descr/named/direct/sl`) are LANGUAGE-BLIND, so the SNOBOL4/Icon FORTH work already carried Pascal onto the rsp stack with zero Pascal-specific code.
2. **MEASURED, corpus-wide (143 probes, 0 compile-fail):** every Pascal proc frame that needs stack storage uses C-stack `sub rsp,rax` alloca (50 probes with nested procs); **`rt_zls2_push` (arena frame push) == 0 across ALL 143 `.s`.** Verified frame mechanism in `deep5.s`: caller `rt_proc_call_open`→`sub rsp,rax`→`rt_frame_prep`; callee `push r12; mov r12,rdi` so `[r12+off]` locals live on rsp. MAIN frame stays arena (`g_gva_base`, all 143) as the deliberate program-lifetime escaper; `new`/`p^` ride the GC heap cell array (46 probes, `pas_heap_cell`/`__pas_alloc`). This IS the pivot's partition: control-flow-lifetime ζ on rsp, escapers off-spine.
3. **No Pascal box qualifies for a per-box `fc_geom` FORTH cell** (the finer granularity SNOBOL4 added on top of CSTACK). Pascal emits only `IR_ASSIGN`/`IR_VAR`/`IR_LIT_*`/`IR_BINOP_ARITH`/`IR_BINOP_TEST`/`IR_DEREF`/`IR_VAR_REF`/`IR_ASSIGN_VAR`/`IR_RETURN`/`IR_BINOP_ARITH_REAL`. In `zeta_storage.c zls_layout`: `IR_ASSIGN`/`IR_DEREF`/`IR_ASSIGN_VAR` → `return 0` (NO private scratch); the value-producers fall to `default`→`return 1` = a SINGLE RESULT DESCR, which is CROSS-BOX-READ (a consumer reads the producer's slot) and therefore MUST stay in the durable per-activation frame, never a pop-at-ω cell. Under the FORTH fixed-size law "a box with no locals has a ZERO cell = no rsp motion, so `fc_geom` returning 0 IS its FORTH form" (`zeta_storage.c:401`, the ANY/POS/LEN precedent). Pascal boxes are all in that zero-cell class. Every box that DOES carry private scratch in `zls_layout` (`IR_CREATE` coexpr, `IR_PROC_GEN`/`IR_CALL_VALUE` suspend-activation handle, `IR_REV_ASSIGN` "LIVE across suspension") is an Icon/Prolog escaper already routed to the heap provider — none are Pascal.
**ESCAPER DECISION (mine, following SNOBOL4 degrade-never-die):** the escaper set is FIXED and the working durable paths are NOT disturbed — MAIN→arena, program-globals→arena, `new`/`p^`→heap cells, uplevel `__up_*`→arena-promoted (NOT moved to heap: it works, and rewriting a proven escaper path with four-language blast radius violates the SNOBOL4 discipline; the arena is a legitimate program-lifetime escaper home). Recursion is rsp-nested → recursive procs are NOT escapers. **NET: the directive is already satisfied for Pascal at proc granularity, and the per-box-cell delta is empty by the cross-box-read invariant. No code change is the correct outcome.** NEXT: PAS-UNBOX-5 (SSE) or PAS-BENCH-0 (triage). No credentials needed until session-close push.

## ⛔ WATERMARK — CHARACTERIZATION SESSION (2026-07-13, SCRIP clean build from HEAD, corpus untouched): M3 141/0/0/2 · M4 141/0/0/2 (full recipe, fresh gate scripts) — NO CODE CHANGED; findings reconcile plan with current build reality

**Session directive:** Read GOAL-PASCAL-BB.md and associated docs, orient, characterize open fronts against clean build, then close.

**Gate scripts reconstructed in /tmp** (lost on sandbox evaporation): loop `corpus/programs/pascal/*.pas`, skip `pcom*|pint*|ppp*|recursion`, NOREF=`chararr_probe`+`recursion`, stdin from `$name.in` (not `.inp`). Both gates reproduced watermark exactly: M3 141/0/0/2 · M4 141/0/0/2 in 14s.

**FINDING 1 — PAS-DISPLAY is DEAD ARCHITECTURE.** The plan marks PAS-DISPLAY as "▶ ACTIVE RUNG (TOP/FIRST) — TOP PRIORITY FOR PASCAL" with r13/r14/r15 as the uplevel-access vehicle. Current build: zero r13/r14/r15 refs in ANY of the 141 `.s` files. Verified via `deep5.pas` (the 5-level nest from PAS-DISPLAY-4): p5's reads of a/b/c/d emit `mov rax,[rbx+16]` etc. — pure arena access. The __gva_names for deep5 registers `__up_p1_a`, `__up_p2_b`, `__up_p3_c`, `__up_p4_d`, `__upsv_*` — the capture-promotion scheme from the PASCAL-100% watermark (6d86723a) promoted uplevel vars to arena globals, and GVA-3 then moved those `__up_*` cells from NV into the arena. So uplevel access is now O(1) arena `[rbx+k*16]` with zero display registers, zero SL-walk, zero NV — cleaner than any watermark states. The PAS-DISPLAY sub-steps (0–5, all marked `[x]`) are HISTORICAL: they landed and were then superseded by the uplevel-promotion + GVA-arena path. The `bb_var_frame*.cpp`/`bb_assign_frame*.cpp`/`xa_flat.cpp` display-push/pop code remains committed but emits zero display instructions for Pascal because no `IR_VAR_FRAME`/`IR_ASSIGN_FRAME` nodes are generated by the current lowering (6d86723a "PASCAL 100%" lowering uses `__up_*` arena cells instead). PAS-DISPLAY is a dead rung — neither wrong nor active.

**FINDING 2 — PAS-UNBOX-4 (real correctness) is DONE.** Plan says "root located, unstarted — reals truncated to int, realparam.ref is 1-byte stub, rc=134 crash." Current build: `realparam.ref` is correctly `       3.0` (11 bytes); M3+M4 both produce `       3.0`. Tested `z := x*y+x` on real variables (the plan's specific case) → `4.5000` correct. Tested `i := 7; r := i` (int→real) → `7.00`; `7.0/2.0` → `3.50`; byref real mutation → `50.00`; real array sum → `8.0000`. All correct both modes, matching P4 oracle. No fix needed. Mark DONE (verified this session, fix in a prior session).

**FINDING 3 — PAS-GVA-4 (aggregate globals) passes.** New probe `gagg.pas` (global array + global record, value-arg to function): M3+M4 both `14`/`30`. Emitted `.s`: `__gva` registers `p` and `v` by name, accessed at `[rbx+48]/[rbx+56]`, zero `NV_GET`/`rt_gvar_*`. Global-aggregate arena routing is correct and complete. Frontier-1 trio (`arr2dtype`/`arr2dtype2`/`arrparam`) all pass — no residual local-temp mismatch. PAS-GVA-4 is a verification rung that comes up green with no code change required.

**FINDING 4 — PAS-VAR-4 (leakage sweep) passes.** M4 emit-side sweep of all 141 gate probes: `grep -c "NV_GET_fn|NV_SET_fn|rt_gvar_"` = **0** across every `.s`. Additionally: 139/141 probes emit `[rbx+...]` arena access (positive); 141/141 emit `[r12...]` frame access (positive); zero probes even *reference* the `rt_gvar_get_int` symbol — the dead read-fallback the plan wanted removed is absent entirely. The three-tier model (arena globals, ζ-frame locals, `__up_*` promoted uplevels also via arena) is clean and complete. PAS-VAR-4 is a verification rung that passes with no code change.

**FINDING 5 — PAS-UNBOX-5 (inline-SSE) and PAS-BENCH are the only genuine forward rungs.** UNBOX-5 is a pure speed rung (`movsd`/`addsd` instead of `rt_num_arith`). PAS-BENCH (Stanford/Dhrystone/Whetstone import, timing harness, fpc comparison) is unstarted and measurable. All correctness work is done.

**NEXT SESSION:** (a) Update checklist: PAS-DISPLAY re-classify as "HISTORICAL/SUPERSEDED"; PAS-UNBOX-4 mark done; PAS-GVA-4/GVA-5/VAR-4 mark verified. (b) Choose between PAS-UNBOX-5 (SSE speed) or PAS-BENCH-0 (scope vs P4 dialect triage). Either is a clean rung with a well-defined gate. PAS-BENCH-0 is documentation-only, low risk, clears the path for BENCH-1 import. Recommend BENCH-0 first as it requires no code change and anchors the benchmark strategy. **No credentials needed for next session until session-close push.**

## ⛔ WATERMARK — PAS-GVA-3 COMPLETE: PASCAL NEVER USES THE NV SYSTEM FOR ANY VARIABLE (2026-07-10 s3, SCRIP <pending push>, corpus untouched): M3 141/0/0/2 · M4 141/0/0/2 (full recipe, final rebuild) — provably ZERO NV_GET_fn/NV_SET_fn/rt_gvar_* for any Pascal variable, both modes, corpus + pcom scale
Lon directive this session: "Ensure that Pascal does not use the NV system for any of its variables." DONE — two clean edits, no template touched (so LANG-BLINDNESS is trivially preserved; emit_no_lang gate green):
1. **Driver ordering (`src/driver/scrip.c`, M4 `--compile` arm, is_pascal-gated).** The arena was collected+registered AFTER proc-body emission, so a TRUE PROGRAM GLOBAL referenced only inside a nested proc had `g_gva_active=0` at the moment its `IR_VAR`/`IR_ASSIGN`/`IR_VAR_REF` box emitted → `op_gva_k=-1` → the box fell to `bb_var_global`/`bb_assign_global`'s by-name `NV_GET_fn`/`NV_SET_fn` hash arm. FIX: hoist `gva_collect_reset()`+`gva_collect_graph(sbbg)`+all-proc-graph sweep+`g_gva_active=(gva_count()>0)` to BEFORE the proc-emission loop (exactly the ordering the M3 `--run` arm already had since PAS-GVA-2 — this makes M4 match M3). The late block becomes `if(!is_pascal){...}` so non-Pascal keeps its original single-graph collection byte-identical. RESULT: proc-context globals (`g` read inside a proc, `__up_*` uplevel-promoted cells, byref-of-global align cells) all resolve `op_gva_k>=0` and emit `[rbx+k*16]`. This was the LAST emit-side NV leak — every corpus `.s` (was 36 files w/ NV) + pcom.s now emit **0** `NV_*`/`rt_gvar_*` calls.
2. **Runtime pointer/heap handlers (`src/runtime/by_name_dispatch.c`).** The `__pas_alloc`/`__pas_alloc_rec`/`__pas_field_set`/`__pas_field_idx_set`/`__pas_deref`/`__pas_deref_set`/`__pas_nrec_deref_set` handlers (Pascal `new`/`p^`/`p^.f`/pointer-to-record) stored heap cells as `NV_SET_fn("__heap_%ld", …)` string-keyed dictionary entries and read them back with `NV_GET_fn` — i.e. Pascal's HEAP (dynamically-allocated `new` vars) rode the NV hash at RUNTIME even though nothing emitted an NV *call* (the dispatch itself used them internally). FIX: a GC-backed `DESCR_t *g_pas_heap` cell array + `pas_heap_cell(long n, int grow)` accessor (doubles from 1024, GC_realloc, zero-fills). Integer handles unchanged (still `++g_pas_heap_ctr`, nil=0 sentinel per P4 int.p:1082 `load nil → maxstr`, new→distinct `>=1`); cells now hold full `DESCR_t` directly (a correctness improvement — no VARVAL_fn string round-trip). `by_name_dispatch.c` NV refs 12→5 (remaining 5 are SNOBOL `ASGN`/`variable`/`VALUE`/`write` handlers — NOT Pascal).
**PROOF (both modes, exhaustive):** (a) M4 emit-side: `grep -c "call NV_GET_fn\|call NV_SET_fn\|call rt_gvar_"` == 0 across ALL 141 corpus `.s` AND pcom.s (286k lines). (b) M3 runtime-side: gdb sweep with break-on-`NV_GET_fn`/`NV_SET_fn` across ALL 141 probes → the ONLY names that fire are the 24 SNOBOL core-lib seed constants (ALPHABET/ARB/BAL/FENCE/&digits/&ucase/…), set ONCE by `core_lib_init` at startup, language-independent (a SNOBOL hello seeds the identical 24) — pre-main constants, not Pascal variables; ZERO `NV_GET` fires anywhere; zero Pascal variable name (`g`/`tab`/`sy`/`__up_*`/`__heap_*`) ever hits NV. (c) Pascal never sets `dyn_scope` (grep lower_pascal.c/lower_program.c == 0) so `rt_call_proc_descr`'s `rt_call_named_proc` NV fallback is unreachable for Pascal; no Pascal `.s` calls `rt_call_named_proc`. **REGRESSION-NEUTRAL (landmines #4/#6, shared scrip.c + by_name_dispatch.c):** SNOBOL crosscheck BYTE-IDENTICAL by stash+rebuild — pre-edit and post-edit both m3 278/7 m4 277/6 DIVERGE=1 (1017_arg_local), SAME named fail sets; all three `.s` regen scripts (feature/benchmark/demo) zero-drift; Icon 12/12×2, Prolog 5/5×3, SNOBOL4 7/7; emit_no_lang gate green. NOTE the 24 seeds are SNOBOL keyword/pattern constants owned by the runtime MODEL, seeded for every language's core lib; they are not variables of any Pascal program and a Pascal program neither reads nor writes them — "Pascal does not use NV for any of its variables" holds without qualification.

## ⛔ WATERMARK — ZETA PORT RUNTIME SELECTOR + PASCAL A/B (2026-07-10 s2, SCRIP c9a0f6af (final pushed hash, rebased over b5f13a17); corpus untouched by this session; RECERTIFIED 141/0 both modes both ports: M3 128/0 + M4 128/0 at BOTH --zeta-port=cstack AND --zeta-port=inline; pcom stall proven PROVIDER-INDEPENDENT
Lon directive this session: "Try Pascal with the new ZETA Local Storage on the C stack where each BB is in charge of its own memory management. We want to be able to switch from the current mode back and forth with the old mode at runtime for testing sake." Ground truth first: Pascal ALREADY rides the C-stack mode — ZC_PORT_CSTACK has been the compiled default since 879a0d37, and Pascal's proc frames flow through the four rt.c bracket sites (rt_call_proc_descr/named/direct/sl :402/:573/:605/:700) which alloca under CSTACK vs rt_zls2_push under the arena; Pascal has zero per-BB grants in zls2_geom (its activation storage is proc-frame-shaped, so those sites + the xa_flat anchor ARE its whole zeta surface); the memoized MAIN frame stays arena by the recorded deliberate boundary. What was MISSING was the runtime switch — port mode was env-only (SCRIP_ZETA_PORT), read+memoized independently at TWO points (x86_port_mode emit-side, rt_zeta_cstack runtime-side), and an M4 binary compiled under one env silently ran mixed-mode under another (the .s is port-mode-COMMITTED: CSTACK emits rsp arithmetic, INLINE arena-cursor arithmetic — deep5.s differs 37034 vs 36206 bytes between modes).
**LANDED — `--zeta-port=plain|instrumented|alloc|inline|cstack` (SCRIP ac626323), the ZC_PORT-axis twin of s11's --zeta= rung-0, same pattern verbatim:** ONE variable both worlds (zeta_alloc.c `rt_zeta_port_set_mode`/`rt_zeta_port_mode`, precedence flag > env > ZC_PORT default; both former getenv memos deleted, x86_port_mode() and rt_zeta_cstack() reroute through it); M4 bake at BOTH main-wrapper variants (`mov edi,N; call rt_zeta_port_set_mode@PLT` before proc_startup/core_lib_init, only when the resolved mode differs from ZC_PORT — no override, no bake) so a linked binary self-selects its emit-time mode env-free. PROOFS (s11 discipline): default .s byte-identical pre/post (deep5/pb31/hello.pas + icon hello + snobol4 hello) AND all four .s regen scripts zero-drift corpus-wide; explicit =cstack = no bake, byte-identical; =inline diff vs env-3 baseline = EXACTLY the 2 baked lines; bad value loud rc=2; TELEM `[ZETA] port=N`; end-to-end self-select proven (inline-compiled deep5 binary, env UNSET, prints port=3 from the .so; default-compiled control silent). GATES: Pascal M3 128/0 + M4 128/0 at =cstack AND =inline (M4 executed env-free = bake-proven); icon smoke 12/12 m3+m4; prolog 5/5×3; snobol4 7/7.
**PCOM FINDING (the switch's first payoff — the prior watermark's open hypothesis NARROWED):** the instrmnemonics silent ω-stall is byte-identical (rc=0, zero stdout/stderr) under =cstack, =inline, AND =alloc (the proving flavor: no poison trip, no LIFO abort, TELEM confirms port=2 live). The fault is PROVIDER-INDEPENDENT — the "ζ region sizing / rt_zls_alloc provider" half of the prior hypothesis is DEAD. Remaining suspects, in order: (1) emit-time per-proc slot-base / zls_build hiwater arithmetic at pcom scale (the [r12+8352] slots — a base computed wrong is wrong identically under every provider); (2) the recorded proc-context global-resolution divergence (`mn[5]:=9` green at main entry, dead at inittables entry — same global, different resolution per context). Next session: dump the emitted slot bases for instrmnemonics vs main (--dump-zeta / .s inspection) and diff the two contexts' resolution of `mn`; the provider layer needs no further reading.

## ⛔ WATERMARK — PCOM RESTORE ADVANCE (2026-07-10, SCRIP 029f1091, corpus d5d2de82): M3 gate 141/0/0/2 · M4 gate 141/0/0/2 (128 legacy + 13 new pint-provenance probes) (both full-recipe, re-verified after EACH of three landed fixes; RECERTIFIED post push-race rebase over a27fb39c) — pcom.pas now RUNS to inittables/instrmnemonics (was: FATAL op=4 during compile of pcom IR)
Lon directive this session: "Continue restoring Pascal from the gutting (e1ec0679, non-Icon lowerers out of Makefile) — bring old code forward into the new system, the one emitter." Restoration timeline confirmed from history: e1ec0679 gut → 667b9cd6 stubs → f52c9ea3 rewire (30→113) → d95d6fe3/6d86723a (128/0). **Corpus-level restore is COMPLETE** (old vs new `lower()` TT-dispatch identical, gates identical); the unreclaimed territory is the **pcom.pas scale frontier** (pre-gut pcom ran to spurious error(103); post-gut it FATALed in the compiler). Three parser-registry fixes landed (all pascal.y, commit f18195a1, full detail in its message):
1. **RECORD-BODY SCALAR LEAK** — array-field index subranges leaked `g_pas_pend_sub_high` out of record bodies; the record type double-registered as a subrange, so record vars classified as plain arrays and every `rec.field` → TT_FIELD → lower() default GOTO (valueless) → FATAL op=4 when fed to a condition. RECORDSY-close now clears the field-type scalar pends (sub_low/high, enum_max, arr_ncols, ischar, arr_ischar) exactly as it already cleared ptrtarget. This unblocked ALL of pcom's records (valu/constant/structure).
2. **SCOPED PTRVAR REGISTRY** — pcom's markctp/markstp/followctp(forward)/followstp all name their param `fp` with DIFFERENT pointed-to types; the flat first-match registry resolved every `fp` to the first registration, so `with fp^` in followstp silently mis-typed (plain fields became bare NV-global reads — wrong storage, NO error; valu-typed fields FATALed). Now: `pv_mark` shared nonterminal (mid-rule factored — identical inline mid-rules would reduce/reduce) marks at proc-header start; release at proc end; lookup scans BACKWARD (innermost shadowing); forward decls `pas_fwd_save` their param slice by name and `pas_fwd_restore` re-registers it when the param-less resolution body arrives. Grammar conflicts 5 s/r before AND after (0 added, verified vs HEAD).
3. **STRARR (array-of-chararr rows)** — `rw: array[1..35] of alpha` / `mn: array[0..60] of packed array[1..4] of char` were indistinguishable from `s: alpha` at var_decl → all flat chararr → whole-row ops (`rw[1]:='if      '`, `writeln(rw[i])`, `id <> rw[i]`) were SILENT no-ops. P4's name-table idiom, load-bearing for pcom's inittables. Now: 1-D ARRAYSY action detects inner-was-chararr (`g_pas_pend_arr_wrap`), var_decl routes to new strarr registry — rows are plain STRING CELLS on the generic arr_set_pure/arr_get rail; `pas_is_strtyped` accepts strarr TT_IDX so chararr-vs-row relops ride the existing `__pas_alpha_str` strcmp arm. `array[1..80] of char` correctly stays chararr. NOTE: alphacmp was green BEFORE only because rows-as-string-cells already worked under the generic rail; the chararr misregistration broke row PRINTING and whole-var ops, which pcom needs and alphacmp never exercised.

**PCOM NEXT BLOCKER (diagnosed, NOT fixed):** pcom runs main entry → all 36 global-array fills live (`mn[5]:=9` GREEN at main begin) → initscalars ✓ initsets ✓ → inittables: reswords ✓ symbols ✓ rators ✓ → **instrmnemonics first statement fails SILENTLY (ω to program end, rc=0)**. Eliminated by experiment: registration (gdb: rw/na/mn/sna all strarr), fill-chain skip (all arrays writable at main entry), capture promotion (pas_cap_add trace = insymbol.test/printtables.lim/block.test/typ.displ only), M4 no different. THE TELL: `mn[5]:=9` works in MAIN but fails at inittables ENTRY — proc-context resolution of the same global diverges. Emitted instrmnemonics ZLS slots sit at `[r12 + 8352]`. **HYPOTHESIS for next session: ζ region sizing or per-proc slot-base at pcom scale — read `src/runtime/rt/zeta_alloc.c` (rt_zls_alloc) vs the emitted slot hiwater; the investigation stopped exactly here (file located, not yet read).** Repro probes lived in /tmp (evaporates): the getbounds shape (forward `stp = ^structure`, variant record, `with fsp^`, `min.ival`) and the row probes are reconstructible from the 029f1091 commit message; the bisect probe = pcom.pas with writeln milestones between main's initializer calls and inside inittables.

**KNOWN GAP (recorded, deferred):** whole-chararr literal assign `id := 'do      '` stores a plain string where `__pas_alpha_str` expects per-char ordinals → self-compare NE. pcom's insymbol builds identifiers PER-CHAR (alphacmp pattern) so this does not block pcom. Fix direction when needed: whole-chararr assign should either store ordinal form or the wrap should pass plain strings through.

**CORPUS IMPORTED (session close, corpus d5d2de82):** 13 probes landed — vrec1-9 (variant records + record-array-field leak shapes), aa1/aa2/aa6 (strarr rows: named alpha, anonymous, nested-proc writer), withfwd1 (getbounds shape: forward stp param + with fsp^ + variant min.ival). Refs are BYTE-IDENTICAL to the pcom→pint pipeline (fpc-built pcom+pint from this corpus, prr→prd) — P4-oracle provenance proper; the width question resolved itself (pint prints the width-10 default, scrip matches byte-for-byte). Gate is now **M3 141/0/0/2 · M4 141/0/0/2**, re-verified on a FRESH build of the combined a27fb39c+029f1091 head (the RECERTIFIED claim independently confirmed).

**STALENESS FLAGS (this session's audit, still true):** the PAS-DISPLAY deliverables (`bb_var_frame*.cpp`/`bb_assign_frame*.cpp` frame templates) are ORPHANED — zero callers, `IR_VAR_FRAME`/`IR_ASSIGN_FRAME` absent from the post-GZ#5 IR enum; `g_emit_frame_caller_dl` unused. PAS-VAR-2 targets a mechanism that no longer exists. PAS-VAR-4 / PAS-GVA-3 / PAS-UNBOX-6 ("grep==0 by-name") are unreachable as written: the 6d86723a uplevel rework routes every `__up_*`/`__upsv_*` access through NV_GET_fn/NV_SET_fn (deep5.pas M4: 16 NV_GET + 17 NV_SET calls, zero r13/r14/r15). The real storage model is arena / ζ-frame / mangled-global-NV-hash. ⚠ LANDMINE RECONFIRMED THE HARD WAY: bison piped through `head` DID leave pascal.tab.c stale (SIGPIPE) — mtime+grep proved it; regen bare.

## ⛔ WATERMARK — PASCAL 100% (2026-07-09 s2, SCRIP 6d86723a, corpus c4e1ee42): M3 gate 128 PASS / 0 FAIL / 0 XFAIL / 2 NOREF · M4 gate 128 PASS / 0 FAIL / 0 ASMFAIL / 2 NOREF (both full-recipe)
Pascal rides the post-GZ#5 reduced IR + ZLS. `lower_pascal.c` uses the Icon `(cx,t,γ,ω,res)` convention: call args threaded IN control flow, consumers `ir_operand_push` their producers. Locals are ZLS vslots (`zls_build` auto-names from IR_VAR/IR_ASSIGN svals; params via graph `nparams/pnames`), globals ride the GVA rbx-arena. IF/WHILE/FOR/REPEAT are pure port rewiring on IR_BINOP_TEST. BYREF = IR_VAR_REF (caller) + IR_DEREF/IR_ASSIGN_VAR (callee). UPLEVEL = capture promotion to mangled globals `__up_<proc>_<var>` with a proc-prologue copy PLUS per-activation save/restore (`__upsv_<proc>_<var>` ZLS local saved on entry, cell restored on exit-γ) — recursive enclosing procs no longer share the cell (nestrec green; old KNOWN LIMIT retired). Relops in VALUE positions materialize via `pas_mat` __pbt diamonds (parser `pas_bool` is identity — the old TT_IF(rel,1,0) wrap returned the valueless BINOP_TEST as res and is DEAD). Aggregate VALUE params (records, 1-D arrays) register via `pas_array_add2d_param` — the plain add caused main to emit GLOBAL fills for callee param names, gva-shadowing the ZLS param slot. Char-array relops: `pas_alpha_wrap` (both sides string-typed) wraps whole-chararr TT_VAR operands in by-name builtin `__pas_alpha_str(descr, lo)` (by_name_dispatch.c) decoding SOH-ordinal storage to a plain string skipping below-lo dead slots (storage spans 0..hi; `array[1..8]` slot 0 is dead — its chr(0) truncated C-string compares); `lo` rides new arrtype.lo/chararr.lo registry fields captured where `g_pas_pend_sub_low` is live; `rt_jct_relop` strcmp arm ≡ P4 word-compare on equal-length blank-padded strings. `writeln x:w:p` fixed-point emits (value,-3,w,p); `__pas_writeln`/`__pas_write` print `%*.*f` per int.p wrr (grammar previously discarded precision and the -3 sentinel was skipped → blank; realparam.ref in corpus was a stale 1-byte newline masking this as a false pass — ref corrected to `       3.0`). ⚠ IR_t payload is a UNION {sval,ival,dval} — dval routing is DEAD for calls. ⚠ IR_SUCCEED/IR_FAIL are INVISIBLE to the chain emitter — label landings and empty bodies must be IR_GOTO boxes. ⚠ bison regen: NEVER pipe bison through `head` — SIGPIPE kills it mid-write leaving pascal.tab.c stale. NOREF=2 = chararr_probe + recursion (no .ref). Cross-language: SNOBOL crosscheck fail set byte-identical to baseline f42c5953 (stash-A/B proven); RECERTIFIED on the push-race combined head (rebased over 241bb093 Icon sets/tables, which also touched by_name_dispatch.c — hunks disjoint, auto-merged clean): Pascal gates 128/0 both modes, smokes green, crosscheck = the NRETURN-era recorded set exactly (m3 minus 1013+213, DIVERGE=1 1017), all three .s regens zero drift. Everything below this watermark describing frame slots/static links/display registers is HISTORICAL (pre-gut architecture).

## ⛔ FACT RULES POINTER
ONE MEDIUM, INVISIBLE + TEMPLATE-ONLY EMISSION: canonical text in GOAL-SNOBOL4-BB.md / GOAL-ICON-BB.md / RULES.md. ZERO BINARY in any `bb_*.cpp`; `x86()` internals are the ONLY binary+text emitter.

## ⛔ LANGUAGE-BLIND BB/XA TEMPLATES
No language enums/guards in `src/emitter/BB_templates/` or `XA_templates/`; dispatch on IR shape only. Completion test: `SCRIP/BB-TEMPLATES-LANG-AUDIT.md` grep == 0.

**Repo:** SCRIP · corpus (`programs/pascal/`). **The 7th frontend.**

---

## ▶ ACTIVE RUNG (TOP / FIRST) — PAS-DISPLAY: lexical display in r13/r14/r15 — O(1) enclosing-scope access (Lon directive, 2026-06-27 — TOP PRIORITY FOR PASCAL)

**Goal:** Replace the Tier-2 (enclosing-routine local) **static-link chain-walk** with a **register-resident lexical display**. Map the three SNOBOL pattern registers — idle for the whole of any Pascal program — to absolute lexical levels: `rbx`=display[0] (globals, already done via PAS-GVA), `r13`=display[1], `r14`=display[2], `r15`=display[3], `r12`=current activation's frame. Then a read/write of a variable at lexical level L is a SINGLE register-relative load/store `[display_reg(L)+off]` — O(1) — instead of `op_dval` dependent `mov rax,[rax+0]` pointer-chases. This is the SPEED half of "clean hierarchical scoped variable access" (PAS-VAR audits the walk for *correctness*; this rung makes it *fast* and retires the walk for the common depth).

**Why r13/r14/r15 are free (verified this session):** `src/emitter/bb_regs.h` assigns `r13`=Σ subject, `r14`=δ cursor, `r15`=Δ limit — the Byrd-box pattern matcher's working set. A **Pascal program emits ZERO pattern-match boxes**, so all three are dead for the entire run (`grep` of r13/r14/r15 across `bb_var_frame*.cpp`/`bb_assign_frame*.cpp`/`bb_callee_frame.cpp` == 0 refs). They are three callee-saved 64-bit registers sitting idle — exactly the home a display wants.

**Why a display beats static links here:** each call touches EXACTLY ONE display register (the callee's own lexical level): `push display[L]; mov display[L],<my frame>` on entry, `pop display[L]` on exit — O(1) per call regardless of nesting depth. Access is then O(1). Static links are O(hops) *per access*; a display is O(1) per call + O(1) per access. Any nested code that reads an uplevel variable more than ~once per call (i.e. essentially all of it, and emphatically loops) wins. The current cost lives at `bb_var_frame.cpp:18` — `FOR(0,op_dval,[]{ mov rax,[rax+0]; })` is a serial dependency chain the CPU cannot pipeline.

**Depth budget (measured):** `pcom.pas` proc/function decls cluster at lexical levels ~1–5 (bulk ≤4, a couple deeper). Three display registers cover levels 1–3; level ≥4 needs a fallback (spilled memory display, OR retain the existing chain-walk for the overflow tail only). Common case is fully register-served.

### Steps (top-to-bottom; first `- [ ]` is the cursor)

- [x] DONE — PAS-DISPLAY-0
- [x] DONE — PAS-DISPLAY-1
- [x] DONE — PAS-DISPLAY-2
- [x] DONE — PAS-DISPLAY-3
- [x] DONE — PAS-DISPLAY-4
- [x] DONE — PAS-DISPLAY-5

## ▶ PAS-UNBOX: unbox Pascal aggregates + fix/inline reals (2026-06-27) — THE "ten times faster" array win, NEW TOP-PRIORITY OPTIMIZATION RUNG

**Goal:** Replace the SOH-delimited decimal-text aggregate representation (a Pascal `array` is
stored as a C string of `\x01`-separated decimal-ASCII elements: O(idx) scan per read +
`GC_malloc`+O(n) reserialize per write) with a flat typed `ARBLK_t` block (`runtime/core/core.h:70`
— native `DESCR_t *data` + `lo/hi` bounds), reached by an O(1) indexed load/store. Then fix reals
(currently TRUNCATED TO INT — a correctness bug) and optionally inline real arith in SSE.
Full asm-grounded characterization + evidence: **`.github/PAS-UNBOX-ANALYSIS-2026-06-27.md`**.

**Why (asm-verified — and it CONTRADICTS the committed `corpus/BENCHMARKS-PASCAL.md` root cause):**
no `-lgmp` is linked (`Makefile:36` = `-lgc -lm`); scalar integer arithmetic is ALREADY inline
(`bb_binop_arith.cpp`: register `add`/`imul`/`cqo;idiv`, `DT_I` tag, no call, no allocation). The
5,000–38,000× array-kernel slowdown is the aggregate-as-text representation: `a[i]`→`IR_CALL arr_get`,
`a[i]:=v`→`IR_CALL arr_set_pure` (both via `rt_call_arr@PLT`) into `by_name_dispatch.c` (`:802`/`:1416`),
the write doing `GC_malloc(slen*5+…)` + a full `sprintf` reserialize **per element store** (iarr.s:211–232).

**Strategy — DT_A/DT_S coexistence (language-blind, gate-safe — NON-NEGOTIABLE):** the SOH-string
aggregate substrate is SHARED across languages (Raku/SNOBOL lists use `split`/`join`/`elems`/`words`
in the SAME `by_name_dispatch.c`). DO NOT replace it. Make a Pascal array a `DT_A` `ARBLK` descriptor;
add a `DT_A` branch to each aggregate sink that does the O(1) indexed load/store; leave the `DT_S`
string branch BYTE-IDENTICAL for the other languages. Dispatch on `args[0].v == DT_A`, NEVER on
`LANG_PASCAL` (so it passes `BB-TEMPLATES-LANG-AUDIT.md` and keeps the four-language gate green by
construction — non-`DT_A` callers hit unchanged code). After the array rungs land, correct the
`BENCHMARKS-PASCAL.md` "Interpretation" section with measured before/after.

### Steps (top-to-bottom; first `- [ ]` is the cursor)

- [x] DONE — PAS-UNBOX-0
- [x] DONE — PAS-UNBOX-1
- [x] DONE — PAS-UNBOX-2
- [ ] **PAS-UNBOX-4 — real correctness (multi-part; correctness > speed).** Root located:
  `lower_pascal.c` tracks NO declared types, so a real *variable* read lowers to a bare `IR_VAR` with
  no real tag; the emit-time detector `binop_is_num_real` (`emit_bb.c:1377`) classifies integer-vs-real
  *statically* from operand shape, so `z := x*y+x` on real variables is mis-run through `imul` and
  `x := 1.5` truncates (a real *literal* alone is fine: `TT_FLIT`→`IR_LIT_F`). Fix spans: (a) propagate
  declared real type in `lower_pascal.c`, tag real variable reads; (b) all assign arms (frame + gvar)
  store `DT_R` for a real RHS uniformly; (c) `binop_operand_real_static` sees real-typed variables;
  (d) real arrays use a native `double` `ARBLK` element. Gate: real probes correct both modes;
  `realparam.ref` fixed from the 1-byte stub to `       3.0`; full M3+M4.
- [ ] **PAS-UNBOX-5 — inline-SSE real arith arm (speed).** `movsd`/`addsd`/`mulsd`/`divsd` on the
  descriptor value-halves (value half = exactly 8 bytes = one IEEE double), eliminating the
  `rt_num_arith` call on the real path. Net-new template dispatched on a real-type IR flag,
  language-blind (no `LANG_PASCAL` guard); reusable by Icon. Gate: full M3+M4 + cross-lang + template
  byte-identity; real probes still correct.
- [ ] **PAS-UNBOX-6 — retire by-name read fallback for Pascal globals (≡ PAS-GVA-3).** Asm shows
  global scalar reads still `call rt_gvar_get_int@PLT` on the hot path (iarr.s:224–227) despite the
  arena. With arena writes complete, route reads purely to `[rbx+k*16]` and remove the now-dead tag-guard
  fallback for Pascal globals (do NOT disturb SNOBOL's legitimate NV use). Gate: asm grep — Pascal global
  access emits zero `rt_gvar_*`/`NV_GET`; full M3+M4. (Coordinate with PAS-GVA-3/PAS-VAR-4.)
- [ ] **PAS-UNBOX-7 — re-bench + correct the doc.** Re-run PAS-BENCH array kernels; expect the
  5,000–38,000× cluster to collapse toward fpc once the per-write `GC_malloc`+reserialize is gone.
  Rewrite the `corpus/BENCHMARKS-PASCAL.md` "Interpretation" section (the boxed/gmp claim is false)
  with measured before/after numbers. Gate: report committed; numbers reproducible.

### PAS-UNBOX landmines
- **CREATION-FLIP MUST BE SELECTIVE (the trap that breaks records/strings):** `mk_array_fill`
  (pascal.y:410) is called for EVERY entry in `g_pas_arrays`, which mixes pure numeric arrays
  with **records** (`pas_recvar_add` + `pas_array_add(name, nf-1)`), **char-arrays**
  (`pas_chararr_add`), **2-D arrays** (`pas_array_add2d`, flattened `high=(rows+1)*ncols-1`), and
  **array-of-records**. Flipping `mk_array_fill` blanket → `arr_make` turns records and char-arrays
  into `DT_A` and breaks the `\x05`-nested-record-in-SOH-slot and string-comparison (`rt_relop_descr2`)
  mechanisms. The flip MUST cross-reference the companion registries and fire ONLY for a
  pure-numeric 1-D array (`ncols<0` ∧ not char-array ∧ not recvar ∧ not arr-rec ∧ not enum-array).
  Record/char/2-D paths keep the SOH-string. Validate with `iarr` (flips) AND a record probe +
  a char-array probe + a 2-D probe (all must STAY on SOH and stay green).
- **Migrate the array atomically per dimension/type but additively in the sinks:** create+read+write
  must all speak `DT_A` in one rung (else `arr_set_pure` on a `DT_A` that `arr_get` reads as `DT_S`
  diverges), but each sink edit is a *guarded `DT_A` branch* leaving `DT_S` untouched. Never leave the
  corpus half-converted between gate runs.
- **`by_name_dispatch.c` is shared runtime:** after ANY edit run `make libscrip_rt` and ALL FOUR
  language gates + template byte-identity before landing (landmines #4, #6).
- **`ARBLK_t` is the language-blind vehicle:** any `LANG_PASCAL` guard fails `BB-TEMPLATES-LANG-AUDIT.md`.
  Dispatch on `DT_A` vs `DT_S` representation / IR shape only.
- **Reals are a known-fragile area** (`realparam` crashes rc=134); PAS-UNBOX-4 has a wider blast radius —
  do it after the array rungs, with its own full gate.

---

## ▶ PAS-GVA: Pascal globals via `[rbx+k*16]` arena (Lon directive, 2026-06-27) — PAS-GVA-0/1/2 landed; 1b/3/4/5 remain

**Goal:** Make ALL Pascal variable access optimal by routing the program's top-level `var`s through the **slotted global arena** `[rbx+k*16]` (rbx = DESCR base, program-lifetime BSS), exactly as **Icon** and **SNOBOL4** do — retiring the NV by-name hash (`rt_gvar_get_int` / `rt_gvar_assign_int` / `rt_gvar_assign_descr` / `NV_GET_fn`, rbp-hash) for Pascal program globals. Direct memory replaces per-access hash lookup ("ten times faster"), and producer/consumer share ONE 16-byte descriptor representation (no `int`-vs-`descr` sink split).

**Why this is the right model (verified this session):** the arena is program-lifetime + single-representation; it is the correct home for Pascal top-level `var`s (which are visible to every nested routine across all activations). ζ frame `[r12+off]` is **per-activation** storage — correct for locals/params (Pascal already uses it for `decl_level>1` / byref / has-children), **wrong** for program-global lifetime. So globals → arena, locals → ζ frame. That is "optimal" for both.

**Verified current state (the bug this dissolves):**
- The slotted-global table (`global_names[]` / `is_global` / arena) is fed by exactly ONE site: `src/driver/polyglot.c:89-94`, which calls `global_register()` from a `TT_GLOBAL` node — already enabled for `LANG_PASCAL`. **But Pascal never emits `TT_GLOBAL`:** its top-level `var`s are `TT_VAR` children of `TT_PROGRAM` (`src/parser/pascal/pascal.y`), and `build_scope` (`src/lower/lower_pascal.c:515`) populates a `pas_scope_t` **only for `TT_PROC_DECL`** — the main program's vars enter NO scope. So `lower_var`/`lower_assign_var` (`lower_pascal.c:60/91`) hit the `slot < 0` branch and emit bare `IR_VAR`/`IR_ASSIGN` → NV by-name.
- M4 generic path (`src/driver/scrip.c:3150`) does call `gva_collect_graph(sbbg)` and set `g_gva_active=(n_gva>0)` (`:3178`); `gva_name_eligible` (`emit_bb.c:118`) rejects only SNOBOL keyword names, so a plainly-named Pascal global that appears as `IR_VAR`/`IR_ASSIGN` in the **main** graph IS collectible — but only if lowering does not need a frame for it and the name actually surfaces. **M3 generic `--run` path (`scrip.c:~3354`) never calls `gva_collect_*` at all** → `g_gva_active` stays 0 → Pascal globals are by-name in M3 unconditionally. Two-sided fix required.
- Consumers already have the arena arm: `bb_gvar_assign.cpp` / `bb_gvar_assign_call.cpp` `op_gva_k >= 0` branches write the FULL rax/rcx descriptor pair to `[rbx+k*16]` / `[rbx+k*16+8]`; `op_gva_k` is set at `emit_core.c:354` via `gva_index_of` when `g_gva_active`. Producer `bb_binop_gvar_arith_slot.cpp` and these consumers then share representation.

**Scope honesty:** Frontier #1's three probes (`arr2dtype`/`arr2dtype2`/`arrparam`) are dominated by **local/param** arith (`s := s + a[j]` where `s,j` are function locals and `a` is a value-param array), NOT program globals — so this rung primarily makes TRUE globals (top-level `var`s like `r`, `m`, `v`, `i`) optimal. Whether it closes those three depends on PAS-GVA-0 findings (the residual fault may be global-aggregate marshalling, which this fixes, OR the local-temp `int`-vs-`descr` mismatch, which is a separate rung). Do not claim those three closed off this rung without re-running them under PAS-GVA-4.

### Steps (do top-to-bottom; each ends at its gate; first `- [ ]` is the cursor)

- [x] DONE — PAS-GVA-0
- [ ] **PAS-GVA-1b — (optional) `IR_VAR`/`IR_LIT_S` global-assign arena arms.** Only if a Pascal probe assigns a string literal or copies global←global. `IR_VAR` arena form: read src (arena or by-name) → write dst `[rbx+k*16]` pair. Guard `op_gva_k>=0`; preserve by-name otherwise. Gate: full M3+M4 + cross-lang smoke.

- [x] DONE — PAS-GVA-2
- [x] DONE — PAS-GVA-3
- [ ] **PAS-GVA-4 — AGGREGATE GLOBALS + FRONTIER-1 RE-CHECK.** Ensure global arrays/records/sets (`r: row`, `m: mat`, `v: vec`) route through the arena correctly (slot holds the aggregate descriptor/pointer); verify `r[i]:=i+1` write and `writeln(sumvec(v))` arg marshalling of a global aggregate. Then RE-RUN `arr2dtype`/`arr2dtype2`/`arrparam` and record whether they close or whether the residual local-temp `int`-vs-`descr` mismatch remains (→ spin off as a separate Frontier-1 rung; do not fold its claim here). Gate: full M3+M4; updated probe tally recorded.

- [ ] **PAS-GVA-5 — OPTIMAL LOCALS CLOSURE.** Confirm non-global locals/params use direct `[r12+off]` descriptor storage uniformly with no by-name leakage, completing "all Pascal variable access optimal" (globals=arena, locals=ζ frame, params=ζ frame/cell). Gate: full M3+M4; brief invariant note added to Mechanism inventory.

### PAS-VAR — proper variable processing in general (the layered nested model)

The arena (globals) + ζ frame (locals) split is only complete when the THREE-TIER nested-access model is uniform and audited end to end. Tiers: **(0)** program globals → `[rbx+k*16]` arena, reachable with the same `rbx` from ANY nesting depth (zero static-link walk — the arena removes the *longest* walks); **(1)** own locals/params of the current activation → `[r12+off]` ζ frame, zero hops; **(2)** an enclosing routine's non-global local at lexical level 1..N-1 → static-link chain walk of exactly `hops` frames (`IR_VAR_FRAME`/`IR_ASSIGN_FRAME` carry `dval=hops`). Existing hooks: `lower_pascal.c` `lower_var`/`lower_assign_var` compute `hops` (`:78`) and `use_frame`; `build_scope`/`build_scope_chain` set `byref`/`has_children`. This family makes the tiers correct and provably non-overlapping.

- [ ] **PAS-VAR-1 — TIER AUDIT MATRIX.** For a deliberately deep probe (3+ nesting levels: `main` → `outer` → `inner`, with `inner` reading a main global, an `outer` local, and its own local), dump M3+M4 and record which tier each of the three reads resolves to (arena vs own-frame vs N-hop SL walk) and that `hops` is exactly right at each site. Catalogue every (tier × read/write × scalar/aggregate) cell that is exercised by the corpus. Gate: documentation; matrix recorded here.
- [ ] **PAS-VAR-2 — STATIC-LINK WALK CORRECTNESS (tier 2).** Verify the `hops`-deep frame walk reads/writes the correct enclosing slot for every depth the corpus exercises (nestvar*/nestpv*/nestcount/nestshadow/nest2). Fix any off-by-one in the SL chain (the `decl_level`/`hops` pairing in `lower_pascal.c` + the frame-load helper). Gate: full M3+M4; the nest* probe family green.
- [ ] **PAS-VAR-3 — VAR-PARAM (byref) CELL UNIFORMITY.** Confirm var-params resolve to cell addresses uniformly (`IR_VAR_FRAME_REF`/`IR_ASSIGN_FRAME_REF`), including a global passed byref into a nested routine (the callee must reach the GLOBAL's arena cell, not a stale copy) and an enclosing local passed byref further down. Gate: full M3+M4; varparam/vparr/vpfld/nestpv* green.
- [ ] **PAS-VAR-4 — NO BY-NAME LEAKAGE SWEEP.** Final asm grep across ALL Pascal probes in BOTH modes: every variable access is arena (`[rbx+k*16]`), own-frame (`[r12+off]`), or SL-walk — **zero** `rt_gvar_*`/`NV_GET_fn` remaining for Pascal. Then the gva READ-arm by-name tag-guard fallback (still emitted today) is provably dead → remove it (coordinate with PAS-GVA-3). Gate: grep==0; full M3+M4; cross-lang gates before pushing shared files.

---

## ▶ PAS-BENCH — official Pascal benchmarks into corpus + x86-64 speed comparison (Lon directive, 2026-06-27)

**Goal:** put the canonical Pascal benchmark programs into `corpus/programs/pascal/bench/` (or `corpus/benchmarks/pascal/`) and run SCRIP M3/M4 vs available native x86-64 Pascal systems (fpc is the obvious peer; gpc if installable), reporting speed the same way `corpus/BENCHMARKS.md` already does for SNOBOL (in-program `TIME()`, startup excluded, normalized, same machine).

**Research findings (web, 2026-06-27 — recorded so they are not re-derived):**
- **Todd Proebsting** (PhD Wisconsin 1992; Arizona; later MSR) is a compiler-codegen researcher: BURG/iburg/wburg/gburg (tree-pattern-matching instruction selection), "Optimizing an ANSI C Interpreter with Superoperators" (POPL'95), DCG dynamic code generation, demand-driven register allocation. **No dedicated Proebsting *Pascal* compiler exists** in his record — the "Pascal" hits in search are Anders Hejlsberg (Turbo Pascal), a different person. So "Todd's Pascal" is a non-entity; do not chase it.
- **Did Todd use BB techniques?** YES — but for **Icon**, not Pascal, and it is the DIRECT ANCESTOR of THIS repo's model. Proebsting & Townsend, *"A new implementation of the Icon language"* (Softw. Pract. Exper. 30(8):925-972, 2000) = **Jcon**, whose **"four-chunk control-flow model handles goal-directed evaluation and produces constructs not expressible as Java code"** — that four-chunk/four-port control-flow model is exactly our four-port Byrd-box (α β γ ω) generator model. Companion: Proebsting, *"Simple Translation of Goal-Directed Evaluation"* (PLDI'97). This repo already carries `jcon_irgen.icn` and `LOWER-REWRITE-FROM-JCON.md` — the lineage is acknowledged. (Aside: superoperators ≈ our composite/fused BB templates in spirit.) **Takeaway:** the BB technique is Proebsting-Icon heritage applied across all 7 frontends here; Pascal reuses it but Proebsting himself never wrote a BB Pascal.
- **Canonical Pascal benchmarks (the "official" set):** **(a) Whetstone** (Curnow & Wichmann, NPL, 1972 — synthetic FP, originally Algol 60, classic Pascal port); **(b) Dhrystone** (Weicker, Siemens, 1984 — synthetic integer/string/proc-call; v2 is the optimizer-resistant one). **The original Pascal Dhrystone v2 source is fetchable and confirmed:** `github.com/Keith-S-Thompson/dhrystone` → `original-sources/dhry-pascal` (a `sh`-unbundle shar containing Weicker's "Dhrystone Benchmark (Pascal Version 2)" RATIONALE + source; 1453 lines). **(c) Hennessy's Stanford integer suite** (perm, towers, queens, intmm, mm, puzzle, quick, bubble, tree(sort), FFT) — Pascal-origin, the small-but-control-flow-rich set used throughout the Icon/Unicon world. **(d) Byte Sieve** (Sieve of Eratosthenes) — already have `sieve.pas` in the corpus.

### Steps

- [ ] **PAS-BENCH-0 — SCOPE vs P4 DIALECT.** Whetstone/Dhrystone use reals, records, pointers, and (Dhrystone) strings/enotation; check each against the P4 subset (`pcom.pas` acceptance — integer=16-bit, `array/record/set(0..58)/pointer`, value+var params). Triage which benchmarks run as-is, which need a P4-faithful trim, which are out of scope. Record the triage. Gate: documentation.
- [ ] **PAS-BENCH-1 — IMPORT + PROVENANCE.** Add the in-scope sources under `corpus/` with a `PROVENANCE`/`NOTICE` line per file (author, origin URL, license/PD status — Dhrystone is widely treated PD/permissive; Whetstone/Stanford likewise long-circulated, but record the source). Smaller hand-portable ones (Sieve already present; Stanford perm/towers/queens/bubble/quick) first; Dhrystone/Whetstone after PAS-BENCH-0 trim. Use the existing `corpus/benchmarks/` layout convention (`LAYOUT.md`). Gate: files committed to corpus with provenance.
- [ ] **PAS-BENCH-2 — SCRIP CORRECTNESS GATE.** Each imported benchmark gets a `.ref` (value-cross-checked vs fpc per the ⚖ Provenance rule — SCRIP width-10 ints, NOT fpc's unpadded) and must pass the Pascal M3+M4 gate before it counts. A benchmark that bombs an unimplemented shape is logged as a frontier, not silently skipped. Gate: imported set green in both modes (or each failure ticketed).
- [ ] **PAS-BENCH-3 — TIMING HARNESS + PEER INSTALL.** Mirror `BENCHMARKS.md` methodology: in-program timing where the source allows (Dhrystone already self-times; add a loop-count knob), startup excluded, normalized per-iteration, same machine. Install peers: `fpc` (Free Pascal, native x86-64 — `apt-get install fpc`), optionally `gpc`. Peer compile flags recorded (e.g. `fpc -O2`). Gate: harness script in `SCRIP/scripts/` or `corpus/run/`.
- [ ] **PAS-BENCH-4 — RUN + REPORT.** Produce a `corpus/BENCHMARKS-PASCAL.md` table: SCRIP-M3 vs SCRIP-M4 vs fpc(-O2) [vs gpc], ms/iteration normalized, ratio columns. Honest reporting: note where SCRIP is a tree-walking-free flat-BB native vs fpc's mature optimizer; do not cherry-pick. Gate: report committed; numbers reproducible by the harness.

**Bench landmines:** keep benchmark sources OUT of the M3/M4 *correctness* gate loop if they are long-running (use the `timeout 30s` corpus-runner budget, not the 8s smoke); never wire benchmark `.s` byte-identity into a gate; record peer versions/flags so numbers are reproducible.

**2026-08-27 (seat12, side note from corpus-import-iso7185pat-p5, PAS-BENCH-0/1 still NOT started by this note):** `corpus/packages/pascal/p5/sample_programs/drystone.pas` landed as part of the Pascal-P5 ISO 7185 suite import — it is S. A. Moore's own small benchmark, **not** the canonical Weicker Dhrystone v2 this rung targets (that's still `github.com/Keith-S-Thompson/dhrystone`, still unfetched here). Do not let the similar filename stand in for PAS-BENCH-0/1. Whetstone remains completely unsourced. No triage or import work against this rung was done this session — this is a pointer, not progress.

---

## ▶ CURRENT STATE (Session 68, 2026-06-27)

**Gate: M3 128/0 XFAIL=0 NOREF=2. M4 128/0 ASMFAIL=0.** (NOREF=2: `chararr_probe` has no `.ref`; `recursion` has no `.ref`. `realparam` false-passes — its `.ref` is 1 byte `\n`; correct value `       3.0`; probe crashes rc=134.)

**What landed this session (Session 68, Sonnet 4.6):**

PAS-DISPLAY-3/4/5 COMPLETE. `frame_reach(reg,hops)` helper added to `bb_assign_frame.cpp`, `bb_assign_frame_ref.cpp`, `bb_var_frame_ref.cpp`; symmetric with the read-side PAS-DISPLAY-2. Byref-of-global confirmed safe (hops=0 own-frame untouched by display). Deep-nest fallback verified via `deep5.pas` (corpus, 128th probe): L1–3 display, L4+ walk; in-budget asm audit clean. Bench: `uplevel2.pas`/`uplevel3.pas` in `bench/`; **display is 7–20% SLOWER than the walk at 1–3 hops** on this machine — static-link slot is L1-hot, walk is never the bottleneck; per-call push/pop overhead dominates. No speedup. Full analysis appended to `corpus/BENCHMARKS-PASCAL.md`.

Cross-language gate: SNOBOL 179/76 identical, Prolog parity 115/0, Icon M3+M4 clean; 6 asm fingerprints byte-identical via stash+rebuild. Template-only; no `LANG_PASCAL` guard added.

**NEXT FRONTIERS (priority order, per goal file):**

1. **PAS-VAR-1..4** — Tier audit matrix, static-link walk correctness, var-param uniformity, no-by-name leakage sweep.
2. **PAS-GVA-3** — Retire NV by-name for Pascal globals (now that arena is wired both modes).
3. **PAS-GVA-4** — Aggregate globals + Frontier-1 re-check (`arr2dtype`/`arr2dtype2`/`arrparam`).

**What landed this session (Session 67, SCRIP `56b55773`):**

PAS-GVA-2: Pascal M3 `--run` global arena setup (`scrip.c`, `is_pascal`-gated). Globals move from NV by-name hash to `[rbx+k*16]` in-process arena — M3 now byte-identical in storage model to M4. Proven regression-neutral by stash+rebuild (SNOBOL M3 155/106 identical). Commits: `cc1d1ef7`.

PAS-DISPLAY promoted to TOP/ACTIVE rung (Lon directive). PAS-DISPLAY-0 characterized (walk quantified, op_dval=relative-hops finding, absolute-level mapping frozen). PAS-DISPLAY-1: display save/set/restore at proc entry/exit (`xa_flat.cpp`, shared spine). A Pascal proc at lexical level L∈{1,2,3} pushes/loads r13/r14/r15 on entry and restores on exit. CRITICAL: the extra push broke 16-byte SysV stack alignment → SIGSEGV on nested probes that call runtime with the push live; fixed with `sub rsp,8`/`add rsp,8` padding. Gated on `g_emit_frame_caller_dl`∈{1,2,3} (no language enum; SNOBOL/Icon/Prolog procs have decl_level=0, never trigger it, their r13/r14/r15 stay Σ/δ/Δ). PAS-DISPLAY-2: Tier-2 reads via display (`bb_var_frame.cpp`). Computes `target=current_level−hops`; when target∈{1,2,3} and hops≥1, replaces `lea r12;FOR hops{mov rax,[rax+0]}` with single `mov rax,r13|r14|r15`. Deep proof: `deep3.pas` l3 reading a (hops=2)→`mov rax,r13`, b (hops=1)→`mov rax,r14` — 2-hop dependent chain → 1 load. Commits: `5160be41`, `56b55773`.

DECISION (Lon 2026-06-27): absolute-level display (NOT sliding). rbx=L0, r13=L1, r14=L2, r15=L3, r12=current; level≥4 keeps chain-walk as fallback.

**NEXT FRONTIERS (priority order):**

1. **PAS-DISPLAY-3 — Tier-2 WRITE + byref via display.** Symmetric substitution in `bb_assign_frame.cpp`, `bb_assign_frame_ref.cpp`, `bb_var_frame_ref.cpp`. Byref-of-global must still hit `rbx` arena (not a display slot). Gate: full M3+M4; nest*/nestpv*/varparam green; cross-lang.
2. **PAS-DISPLAY-4 — Deep-nest fallback + walk retirement.** Confirm FOR-loop walk emitted ONLY for target≥4; asm audit clean.
3. **PAS-DISPLAY-5 — Bench the win.** Add uplevel-in-loop benchmark; record speedup vs pre-rung chain-walk baseline in `corpus/BENCHMARKS-PASCAL.md`.
4. **PAS-GVA-3 — Retire NV by-name for Pascal globals.** Remove now-dead read-path tag-guard fallback (`rt_gvar_get_int`) for Pascal globals.

**What landed in previous session (SCRIP `cd4672a`):** `emit_bb.c` — fixed nested descriptor-arith representation mismatch. When a `IR_BINOP_GVAR_ARITH_SLOT` Arm-2 arith node has an operand that itself is a descriptor-producing nested arith BINOP (e.g. the `a.x*b.x` sub-product in `dot := a.x*b.x + a.y*b.y`), that operand's result lives in a 16-byte descriptor slot (tag@+0, value@+8 via `rt_num_arith`), but the Arm-2 read was using offset +0 (the tag field), yielding `6+6=12` instead of `3+8=11`. New `arith_emits_descr()` helper mirrors the dispatcher's Arm-1-vs-Arm-2 choice exactly: a BINOP emits a descriptor iff `bb_arith_is_dynamic` is true and *both* of its operands are call/idx-kind (or are themselves descriptor-emitting). When an Arm-2 operand satisfies this, its kind is normalized to `IR_CALL` so the existing `+8` value-read fires. Routing unchanged; bare-int chain (gvar/literal operands) untouched. Validated: SNOBOL feature `.s` 0/153 changed, SNOBOL bench OK=15/FAIL=0 identical, Prolog parity 115/0 identical, no Icon path hit, template byte-identity unchanged. M3/M4 123/4→124/3. `recparam3` CLOSED.

**Closed this session:** `recparam3` (`dot := a.x*b.x + a.y*b.y` — nested product sum over record fields).

**NEXT FRONTIERS (priority order):**

1. **Flat assign-RHS arith: gvar + call operand (Arm-2 bare-int vs IR_ASSIGN descriptor consumer).** `s := s + a[j]` → blank/"TABLE"/segfault. Affects `arr2dtype`, `arr2dtype2`, `arrparam`. Root cause fully diagnosed: the `IR_BINOP_GVAR_ARITH_SLOT` Arm-2 writes a **bare int** to slot+0, but the consuming `IR_ASSIGN` (via `flat_drive_gvar_assign_binop`, `emit_bb.c:3327`) reads that slot as a full **tagged descriptor** (`+0`=tag→rbx+0, `+8`=value→rbx+8), so the tag field receives the numeric result and the value field is garbage. This differs from the nested-product case (which was an Arm-2 *read* offset bug, not an Arm-2 *write* representation bug). The `t := t + f + 2` case works because its terminal assign hardcodes `mov [rbx+0],6` (DT_I) — but the gvar-assign-binop path does not. Fix shape: make the gvar-assign-binop path apply the DT_I tag when the RHS BINOP took Arm-2 (bare int), OR make Arm-2 write a full descriptor. The latter is principled but touches Arm-2's shared representation; beware cross-language regression — the fix landed in this session's early attempt regressed `alphacmp/enum2/pb33/pb34/pb35` until the correct `arith_emits_descr` predicate was applied. The flat case needs an analogous "Arm-2 output is bare int, not descriptor" signal at the assign consumer.

2. **`realparam.ref` gate-integrity fix.** Ref is 1 byte `\n`; correct value `       3.0`. Probe crashes rc=134 (`flat_drive_assign` missing α, real-valued fn param). Fix: update ref and treat as genuine crash.

3. **pcom self-compilation (deeper, after floor is real).** `var x: integer;` raises spurious `error(103)` from `searchid` — pointer-BST symbol table corrupts at scale. `procedure` decl segfaults rc=139 M3+M4.

No mode-2 `--run` (DE-INTERP done); only `--run` (M3) and `--compile` (M4).

---

## Session Setup
```bash
bash /home/claude/SCRIP/scripts/install_system_packages.sh
cd /home/claude/SCRIP && rm -f scrip && make -j4 scrip > /tmp/build_full.log 2>&1
[ -x scrip ] || { grep "error:" /tmp/build_full.log | head -5; exit 1; }
make libscrip_rt
for r in /home/claude/SCRIP /home/claude/corpus /home/claude/.github; do ( cd "$r" && git config user.name "LCherryholmes" && git config user.email "lcherryh@yahoo.com" ); done
bash scripts/test_gate_pascal_m3.sh          # --run vs .ref
bash scripts/test_gate_pascal_m4.sh          # --compile -> gcc -> link -> RUN vs .ref
bash scripts/test_gate_pas_frame_pairing.sh  # PAS-ZF-10 structural prison
```
⛔ **THE GATES ARE NOW COMMITTED SCRIPTS, NOT A /tmp RECIPE (2026-08-07).** The former instruction here was "recreate the gate scripts in /tmp" — which is precisely why Pascal could crater for 15 days and 625 commits without anyone noticing: `ls scripts/ | grep -i pas` returned NOTHING, so no other language's session had a Pascal gate to run even if it wanted one. See § FACT RULE — PASCAL IS ABSENT FROM EVERY SHARED-EMITTER GATE. All three take `SCRIP=`/`CORPUS=`/`EXPECT_FAIL=`/`RESULTS=`; M3 and M4 write a per-probe TSV so a session can diff the FAILING PROBE SET, not just the count (87/65 → 87/65 with a different 65 is a different world, and a bare count hides it). NEVER trust an M4 watermark not produced by `test_gate_pascal_m4.sh` (compile-clean is not an M4 number — it must assemble, link, and RUN).

---

## Mechanism inventory
- **Rail:** `LANG_PASCAL`=6, `IR_LANG_PAS`=7, body walker `lower_pascal_body`.
- **Frame-as-BB:** `[fb+0]`=SL, `[fb+16+k*16]`=DESCR slot k. VAR params = cell addresses (IR_VAR_FRAME_REF/IR_ASSIGN_FRAME_REF). Migration: `decl_level>1 || byref_mask`; main stays NV. var-param-of-aggregate-element uses copy-in/out (`lower_pascal.c` rewrites `genlabel(agg[i])`→`tmp:=agg[i]; genlabel(tmp); agg[i]:=tmp`).
- **2D arrays:** rule `selector '[' expr_list ']'` (2 elts) → flat `BINOP_ADD(BINOP_MUL(i,ncols),j)`, `TT_IDX(a,flat)`. PEERS aux (`bb_operand_aux_set` in lower_binop) records both operands; `walk_bb_flat case IR_BINOP` wires them when `bb_child0==NULL` using `g_emit_cfg`. `lc_arg_block` = isolated subgraph; save/restore `g_emit_cfg` around `flat_emit_arg_subchain`.
- **Calls:** dval=2.0 → `rt_call_arr`; dval=3.0 → `rt_call_named_proc(_sl)`. `marshal_call_arg` (gvar) dispatch: pre-computed DESCR slot → inline-arith(+DT_I) → relop → nested slot → terminal CALL → LIT → varslot. Known issue: `marshal_single_call` allocs fresh slots bypassing `gvar_drive_call_arg_slots` → zeros.
- **Booleans:** INTVAL(1/0); `pas_cond`=`expr≠0`; and/or=MUL/ADD; not=`pas_flip_rel`. **Polymorphic relop (all 6, char-array/string-aware):** `IR_BINOP_GVAR_RELOP` arm (`bb_binop_gvar_relop.cpp`) → `rt_relop_descr2`→`binop_apply`; DT_I falls to int cmp. String-type hint set by `pas_rel`/`pas_is_strtyped` (`IR_LIT.dval=1.0`) for whole char-arrays, string lits, or `TT_IDX(TT_VAR,…)` of a char-array.
- **goto/label:** pass-1 pre-registers IR_SUCCEED landings; intra-proc only.
- **I/O:** `__pas_writeln/__pas_write` interleaved (value,width). Default int=10, real=20, char=-2.
- **Char:** ordinals; charvar args wrapped `__pas_chr`. **Arrays/records/ptrs:** TT_IDX→`arr_get`; `a[i]:=v`→`arr_set_pure`; records=field-index arrays; sets=`__pas_set*/in`; nested record-in-record stored as `\x05`-separated string in parent SOH slot (`__pas_nrec_*`). **Functions:** body ends `IR_RETURN(dval 0)` reading `IR_VAR(funcname)`.

## Target dialect — P4 subset
`pcom.pas` + `grammar/pascalp.y`. integer=16-bit; array, record, set(0..58), pointer; value+var params; nested routines; goto intra-proc only. If pcom rejects a probe, out of scope.

## ⚖ Provenance
`pcom.pas`/`pint.pas` = behavioral oracle only — never transliterated. `.ref` = SCRIP width-10 integers (from M3, value-cross-checked vs fpc), NOT fpc's unpadded ints.

## Invariants
No AST walking in modes 3/4. Zero C Byrd-box functions. Four ports (α β γ ω). Modes 3/4 emit no byte outside `bb_emit_x86`.

## Where things live
| Thing | Path |
|-------|------|
| Oracle | `corpus/programs/pascal/pcom.pas` / `pint.pas` |
| Grammar | `corpus/programs/pascal/grammar/pascalp.{l,y}` |
| Probes | `corpus/programs/pascal/*.pas` |
| Frontend | `src/parser/pascal/pascal.{l,y}` |
| Lowering | `src/lower/lower_pascal.c` + `lower_program.c` |
| BB templates | `src/emitter/BB_templates/` |

## ⚠ Landmines
1. `rm -f scrip` before `make scrip` — no prerequisites.
2. Pascal regen ONLY: `cd src/parser/pascal && bison -d -o pascal.tab.c pascal.y`. NEVER full regen. After editing `pascal.y` you MUST regen AND recompile `pascal.tab.o` (`rm -f scrip` alone leaves it stale).
3. `touch` templates before `make scrip` after any template edit.
4. After ANY runtime (`by_name_dispatch.c`/`rt/`) edit: `make libscrip_rt` (M4 links the external `.so`; M2/M3 use in-process).
5. Every `git pull --rebase` → `rm -f scrip && make` → full gate re-run.
6. Run ALL four language gates before pushing shared `emit_bb.c`/`emit_core.c`/`scrip.c`.
7. `lc_arg_block` = ISOLATED subgraph; save/restore `g_emit_cfg` around `flat_emit_arg_subchain`.

## ⛔ FACT RULE — SESSION CLOSE REQUIRES CONFIRMED PUSH
"HANDOFF COMPLETE" (or any doneness claim) MUST NOT be spoken until `git push` has SUCCEEDED and `git log origin/main --oneline -1` shows this session's hash on origin for every touched repo. A local commit is NOT a session close — the bytes vanish with the sandbox. If push fails or credential is missing, report BLOCKED plainly and stop. The ONLY sanctioned source of a completion claim is the verbatim stdout of `bash scripts/handoff_status.sh` (auto-discovers all repos with an `origin` remote; prints `HANDOFF COMPLETE` exit-0 or `HANDOFF BLOCKED` exit-1). Paste it verbatim — never type the claim yourself.

## ⛔ FACT RULE — THE WORD "HANDOFF" IS FORBIDDEN IN ASSISTANT PROSE AT SESSION CLOSE
At session close the assistant MUST NOT write "HANDOFF" in any self-authored sentence. It may only appear inside the pasted, unedited `handoff_status.sh` stdout. Use "session close", "session end", or "wrap-up" instead.
