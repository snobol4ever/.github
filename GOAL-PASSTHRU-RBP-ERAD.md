# ⛔⭐⭐⭐ GOAL-PASSTHRU-RBP-ERAD — FLAT PASS-THRU everywhere it should be; RBP frames counted by census, deleted by rung

## ✅ CONCURRENT WORK — COMMIT AND PUSH FREELY (Lon 2026-08-10 — SUPERSEDES the 2026-08-08 CONCURRENCY PROTOCOL, which is DELETED)
**Any session may edit ANY file at ANY time, and push whenever it has something worth saving.** There is no routed window, no reserved file, no "not-concurrency-safe" rung, no concurrent set, and no waiting on another seat. Git merges; `git pull --rebase` before pushing and resolve conflicts normally. ⛔ **NEVER hold back a commit or a push on concurrency grounds, and never park work waiting for a window** — stranding has cost this project vastly more than merging ever has (s9/s10 lost two sessions of RTCC work; s19–s26 stranded eight sessions of MECH work and BUG-7 was re-derived from scratch). **Push early and often — mid-session, per rung — not only at session end.**
Three things survive because they are about CORRECTNESS, not scheduling: (1) after a `git pull --rebase`, re-prove THIS file's gate/watermark — shared state can move under you; (2) push code repos before `.github`, so a FINDING never describes a tree that was not pushed; (3) `handoff_status.sh` verbatim is the only push truth. **Semantic collisions — two seats claiming one register, the r9/GVA class — are caught MECHANICALLY by `scripts/test_gate_rtcc_claimed_regs.sh`, not by scheduling. That gate is the replacement for the window, and it is why the window is no longer needed.**

**Protocol: `RULES.md` governs.  `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before ANY template/encoder edit.  MONITOR-FIRST for every oracle divergence.  Killswitch law per rung (byte-identical revert, md5-verified; delete label and code in the same slice).  Cross-goal single-authority: `GOAL-SN4-ZETA-MECH.md` owns the stack-work live cursor and the glue-kind/mechanism design — any rung here that touches emit.cpp frame arms re-checks MECH's cursor first and re-homes on Lon's routing; `GOAL-SNOBOL4-BB.md` LADDER PB owns stitch semantics; `GOAL-RTCC` owns register claims.**

⭐ **CHARTER (Lon 2026-08-09, verbatim):** *"I know categorically, undeniably, that a constant folded pattern does NOT need an RBP. … delete that code that generated that mess and replace it with code that uses proper PASS-THRU GLUE as linkage, NOT stack frame linkage. … DO NOT create an RBP frame for constant folded patterns."* — and, same day: *"Plan the entire code modification in an incremental sequence to getting a FLAT PASS THRU everywhere is should be. Count how many RBP frames are being created through the product. Let's make a list to target for deletion. Starting with RBP being used for INVARIANT PATTERNS."*

## DESIGN OF RECORD

- **Exactly three glue kinds** (MECH:253): **ONE-SHOT** (`add rsp,K; jmp pred_β`) · **PASS-THRU** (`bb_glue_pass_wires`: lea rcx,γ + lea rdx,ω + jmp rax — transfer box adopts caller wires, ZERO frame) · **FRAMED** (`bb_glue_framed_enter/leave`, THE ONLY RBP WRITER).
- **Licensed frame census** = **{STATEMENT · FUNCTION · MATCH_BEGIN · FENCE1}** (ARBNO-LON refinement, 08-06; FUNCTION = the AB activation frame, Lon verbatim confirm: the manual p.103 pushdown made literal).  **Every other RBP establishment in the product is a deletion target.**
- **The site side is already correct** (ZD-PATREF, emit.cpp:1935): `bb_match_defer` enters pattern blobs via `bb_glue_pass_wires`, K=0 transfer citizen.  The debt is **blob-side**: `emit_jmp_entry_for_patproc` (emit.cpp:2914/2925) routes every PAT$ blob onto BLOB-GRANT (emit.cpp:2362 — `sub rsp,kt` + wire adoption + **rbp pin**, kt=(48+jcon_value_region+15)&~15; claws5-match: 304) with the CLASS D exit protocol (γ {res,rbp} resume record, scanfail retry `mov rsp,rbp`, ω absolute unwind `lea rsp,[rbp+kt]`) and the legacy in-blob ARBNO per-iteration rbp rebase chain.
- **Statement-inline is the proof-of-possible**: the SAME constructs (ARBNO-LON frameless, ALT, SPAN/BREAK, captures) already run oracle-green in statement context under the licensed frames only.  An INVARIANT pattern's structure is compile-time known — its reference can therefore be pure wiring (pass-thru), its elements riding the statement regime, and the frame question evaporates for it.

## THE CENSUS (RE-MEASURED 2026-08-10 s7 at corpus `fa761d0` / SCRIP `23189c2` post-regen-×3; Σest **557** — s6's 618 and the 609 seed both OBSOLETE: demo MAIN 93→40 and bench MAIN 20→12 dropped via parallel frame-deletion landings between the s6 measurement and origin HEAD, direction consistent with the goal; instrument = `SCRIP/scripts/test_census_rbp_frames.sh`; `push rbp` inside PAT-BLOB regions includes CLASS D γ resume-record pushes — protocol shape, not establishment, dies with the blob; crosscheck/patterns regen cadence STILL unaudited — indicative only)

| corpus (.s) | files | blob-bearing | PAT$ blobs | est `mov rbp,rsp` PAT-BLOB / MAIN / PROC | push rbp PAT-BLOB / MAIN |
|---|---|---|---|---|---|
| programs/snobol4/demo | 28 | 17 | **101** | **130** / 40 / 12 | 113 / 19 |
| benchmarks/snobol4 | 23 | 6 | 8 | 2 / 12 / 3 | 8 / 7 |
| crosscheck/patterns | 122 | 57 | **130** | **93** / 265 / — | 139 / 178 |

Crosscheck-318 instruction baseline (STF-UNFLIP, measured then): **15,459 rbp instructions across 127 programs = the flat_pat/flat_gen WHOLE-GRAPH PIN debt** — "not a statement bracket — do not confuse the two."

## THE DELETION-TARGET LIST (creators; keepers named so nobody re-litigates them)

- **T1 ⛔⭐ BLOB-GRANT frames on INVARIANT-pattern blobs** (Lon's named first target).  Dies with it: SCANBASE rbp reads (kt-32/kt-40), scanfail retry whack, CLASS D γ {res,rbp} record + `pop rbp` res stub + ω absolute unwind (emit.cpp:2742-2796), in-blob legacy ARBNO rbp rebasing.
- **T2** Same blobs reached via `*name` (the defer fast arm is the SOLE flat_pat entry — bare refs die at PT-1/2, star refs at PT-3).
- **T3** Invariant SEGMENT blobs referenced by variant stitch structures (PB) — same BLOB-GRANT frame.
- **T4** `flat_gen` whole-graph pin (generators; product-wide Icon/Prolog) — MECH M-2 routing; tracked here, executed where Lon routes.
- **T5** GLUE-O residual pins on `emit_rec_pin()` mains (the !rec_pin majority already suppressed — s26b census 464→252 push_rbp; remainder = data-reader mains; FB-STMT refinement path).
- **KEEPERS (licensed, never targets):** AB activation frames (`*_act_*`) · MATCH_BEGIN statement head · FENCE1 seal · STATEMENT bracket · Icon zframe/flat_lcl_proc (FUNCTION-class) · CLASS C ambient-rbp whack (a CONSUMER of the C frame, not a creator — the 1016_eval return-to-C mechanism, s22u falsification on record).

## ⛔⭐⭐⭐ LADDER PT — incremental sequence to FLAT PASS-THRU (every rung: killswitch + BY SET gates m3+m4 on the probe suite + xc318 + watermark floors ≥ current + regen ×3 when lower/emitter touched + corpus witnesses with SPITBOL refs)

- [x] **PT-0 · CENSUS + TARGET LIST + INSTRUMENT + WITNESSES** — COMPLETE.  Instrument `scripts/test_census_rbp_frames.sh` landed; table + target list in this file; witnesses promoted to `corpus/probe/pt_inline_1{,_hand,_full}.sno` (SCRIP `d8bd4ba8`, corpus `105f33a6`).  SPITBOL oracle refs: credential SUPPLIED (Lon, s3 2026-08-09) — next session clones/builds x64 `sbl`, confirms `pt_inline_1{,_hand,_full}.ref` + the flagged capture-divergence witnesses against the oracle, then arms the ratchet (gate FAILS on any T-class `est` rise).
- [x] **PT-1 · PAT-INLINE (bare refs of invariant names)** — COMPLETE (DEFAULT ON, `d8bd4ba8`).  Mechanism: `sno_pat_node` TT_VAR arm inlines the compile-time tree for any `g_sno_fz`-member name when `g_sno_pat_match_ctx=1` (the ONE licensed extent: `sno_lower_match`'s pattern walk).  Guards confirmed by code read + gate sweep: `sno_fz_tree` returns NULL when `g_sno_pat_match_ctx=0` (PAT-ARG-BIND scratch walk) or `g_sno_in_patproc=1` (patthunks build) — inline cannot fire in either non-match context.  The session-prior 'miss' was a MIS-AIMED WITNESS, not a defect: `w_pinline1` matched on `PAT` (contains TT_VAR(VOWEL) ⇒ fails `sno_pat_invariant` ⇒ not in fz ⇒ arm never fired; IR byte-identical both modes); the claws5-match SEGV was pre-existing class, resolved at HEAD with no PT code change.  Killswitch `SCRIP_PAT_INLINE=0` reverts to byte-identical legacy.  xc318 watermark 98/16/8 unchanged; probe 26/3/1 unchanged; witnesses hit=A both modes.
- [x] **PT-2 · DEAD-BLOB + DEAD-BUILD SUPPRESSION** — per-name use census (consumers: `IR_VAR` value reads :232 · `*name` defers :1262/`sno_fz_mark_defer` · snapg pre-chains); zero consumers ⇒ suppress the PAT$ graph emission AND the assignment's stage-2 pattern build (dead store).  **DELIVERABLE: `claws5-match.s` regen carries ZERO `proc_PAT` lines** — the exact mess Lon pointed at, gone from the artifact.  Gate: regen diff is deletion-only; suites BY SET.
- [x] **PT-3 · `*name`-OF-INVARIANT INLINE** — TT_DEFER arm: fz-member target inlines under the same license (write-once frozen ⇒ late binding provably equals compile-time); recursive/sealed-DEFER stays DEFER → MECH M-2(a) FRAMED variant (FUNCTION-class activation — licensed keeper).  Re-run census; T2 → 0 for invariant names.
- [ ] **PT-4 · SURVIVING-BLOB PASS-THRU PROTOCOL** ⬅ **s11: THIS IS NOW THE GATE ON EVERYTHING. The DEL-T1 delete was reverted (`c4ef2176`) after its fix-forward was measured net-negative; the blob needs a depth-immune base that is NEITHER rbp NOR rsp before any re-delete.** — blobs still required (stitch segments T3, dynamic refs): α becomes PASS-THRU + a box-own linkage CELL on the spine ({γ, ω, δ0, scanflag} — law-1 "a box a few bytes of its own", NOT a frame, NO pin); interiors admitted to the per-box mechanism (**PL-ZK-5 admission precedent**: a `pat_cells`-class lifts zd_plan's jmp-entry self-decline + the `!flat_pat` guard at emit.cpp:2346); CLASS D record {res,rbp}→{res,pad}; scanfail loses `mov rsp,rbp` (the FORTH fail-chain restores entry depth BY CONSTRUCTION); ω absolute unwind dies.  **PREREQ**: ARBNO/SPAN/BREAK K-conversion inside blob context — COORDINATE WITH MECH, single authority; Lon routes whether this executes here or re-homes.  ⛔ CARVE-ERAD conviction stands (emit.cpp:2914): never cut the region under unconverted readers.
- [ ] **PT-5 · flat_gen PIN census tracking + GLUE-O residual re-measure** — execution likely MECH; this file keeps the product-wide count honest.
- [ ] **PT-6 · PHYSICAL DELETION + RATCHET SEAL** — BLOB-GRANT arm, CLASS D exit spellings, kt region math deleted (label and code same slice); final census: **T-class establishments → 0, keepers unchanged**; the ratchet becomes a standing gate.
- [ ] **PT-7 · g_zctx DELETE — LON'S MECHANISM (s11 in-chat, supersedes the wait-for-full-M-1b form): "storing what you needed as BB locals to the BB_DEFER BB box. There are no GLOBALS needed."**  Two-part conversion, then delete: **(a) SITE STATE → BB_DEFER LOCALS** — linkage {γ, ω, base, scanflag, δ0} become the defer box's OWN locals in the LICENSED statement frame (per-activation + depth-immune by the license; allocated by the normal layout pass, so no aliasing by construction; templates touched ⇒ ARCH-ICON + TEMPLATE-RULES read first, NON-NEGOTIABLE).  **(b) INVARIANT INTERIOR REFS → INLINE** (the s8 enclosure-inlining rung executed): a shared blob's reference to an invariant target folds into the blob body, so NO code inside shared blobs ever needs to find caller state — the circularity (shared code cannot know a caller's slot offset) is dissolved by removing the interior defer, not by answering it.  Then `g_zctx` + every push/pop DELETED, one isolated commit.  **EXIT GATE:** `grep -r g_zctx src/` == 0 · probe+xc BY SET ≥ **32/5 · 82/40** floors BOTH modes · census zero globals AND zero T-class frames.  **RESIDUE:** true recursion only (`dc_recur` class — shared code re-entering itself; manual p.123's stack) stays with MECH M-1b, now the WHOLE of its scope here.  Full-runway seat required — the fix loop is the cost center.
- [ ] **PT-7-ORIG-BRIDGE-FORM (superseded by LON'S MECHANISM above; kept as design record): g_zctx DELETE (Lon commitment, s11-Fable in-chat: "Is this global going to be removed in the future when other things are fixed?" — YES, and this rung is the binding form)** — `g_zctx` is a BRIDGE, not architecture: it exists ONLY because interior depth is not yet restored by construction, so re-entering code cannot find its own carve.  **GATE-IN:** M-1b / interior K-conversion complete — every match-family box's fail/exit path restores entry rsp (the FORTH drain law), `op_flat_disp` valid inside blob context.  **CONVERSION:** every g_zctx consumer (α/β push, γ/ω pop, scanhit/scanfail reads) becomes rsp + emit-time constant; the CLASS-D record's [+8] payload becomes {res,pad} exactly as PT-4's spec already writes; then DELETE the symbol and every push/pop emission, label and code same slice, ONE isolated commit (`git revert` = undo).  **EXIT GATE:** `grep -r g_zctx src/` == 0 · probe+xc BY SET ≥ the `9eb9b4f3` floors (**32/5 · 82/40**) in BOTH modes · census: zero globals AND zero T-class frames — the first state of the product with neither.  Expected side effect: `dc_recur` flips green under the same discipline.

## ⛔⭐⭐⭐ LADDER WREG — TWO GLOBAL WIRE REGISTERS (rΓ=r10 · rΩ=r11) as dynamic γ/ω for PASS-THRU + ONE-SHOT glue (Lon design s12 in-chat: "TWO GLOBAL REGISTERS as dynamic OMEGA and GAMMA… Is this a Eureka moment!!!" · pick confirmed: "R10 and R11. Perfect. DO the full design.") — SUPERSEDES the NEXT ordering below: WREG-0 first

**PRINCIPLE.** A blob is missing exactly two facts: γ and ω. Deliver them in two reserved registers. Registers dissolve 11-CODE's LAW ("a depth-immune base that is NEITHER rbp NOR rsp") by stepping outside it — depth-immune by nature, and NO OFFSETS, so PT-7's shared-code slot-offset circularity stops existing rather than being solved. The blob needs ZERO receiving code: "NO SHIM" is literal.

### ASSIGNMENT (LOCKED, s12 census evidence)
**rΓ = r10 · rΩ = r11.** Grounds: (1) the ONLY caller-saved pair that is never a SysV argument register — rdi/rsi (549 template uses) and rcx/rdx are args 1–4 of every `rt_*` call, ABI-mandated loads that can never be renamed, disqualified categorically (rcx/rdx additionally falsified: 110 interior uses measured in claws5-match PAT$0); (2) both unowned — REGISTER-LAYOUT's retirement notice says r10/BBREG_DATA is OUT, r11 has no role; r9 is TAKEN (RTCC GVA base), r8 is SCANBASE-entangled; (3) RTCC veneers bracket every `rt_*@PLT` crossing, which also neutralizes the lazy-binding resolver's r11 clobber (belt-and-suspenders: `-Wl,-z,now`). **Fallback if WREG-0 finds an unrenameable r10 use (none can exist by ABI; raw-byte encoders are the only risk): r8+r11, paying the SCANBASE entanglement.** Sweep cost measured: 178 discretionary-scratch renames — bb_call_fn 36 (LIVE mid-match via defers, not theoretical) · xa_flat 21 · bb_match_end 12 (already a TIER-2 routing item — the claim forces a wanted cleanup) · bb_scan_match 8 · bb_func_activate 7 · bb_call_proc_staged 7 · rest small.

### CONVENTION — ONE AUTHORITY (ZW16 mixed-regime conviction binds)
Wires ride r10/r11 in **ALL THREE glue kinds**, one product-wide convention — a per-kind split is exactly the mixed regime ZW16 convicted. FRAMED (`bb_glue_framed_enter`) BANKS the pair into its licensed frame at enter (it has a frame; it may). PASS-THRU and ONE-SHOT carry them live. `bb_glue_pass_wires`' rcx/rdx spelling is REPLACED, not paralleled.

### SPELLINGS (both media; TEXT shown, BIN via the same x86() encoders)
- **Site glue, compile-time-known invariant target:** `lea r10,[rip+site_γ]` · `lea r11,[rip+site_ω]` · `jmp n<first>_…_α` (direct rel32 into the blob's FIRST interior box — no proc_PAT$N_α label exists).
- **Site glue, runtime-resolved defer (`*name` fast arm):** same two `lea`s · fn ptr from the proc table into rax · `jmp rax`. Same wires, same zero-shim.
- **Blob exits:** success = `jmp r10` · fail-after-drain = `jmp r11`. NO unwind — there is no carve to dispose of; `op_zgpop` at the statement remains SOLE release authority.
- **Interior unchanged:** ζ cells (`sub rsp,16`) are box-own bytes, licensed, untouched.

### ⛔ THE ONE LAW — WIRES JOIN MATCH STATE (falsification-hardened: a bare global pair IS g_blob_ctx's single-cell defect in register clothing, `pattern_match.c:624` conviction)
**Rule: every pending cell pushed INSIDE a blob captures {r10,r11} at push; its β-resume restores them before any blob code runs.** Statement-inline cells pay NOTHING. Soundness in one line: the drain pops cells in spine order, so when control crosses from blob B's pendings down into blob A's, A's topmost captured cell restores A's wires before A executes — per-activation-correct by construction; success side is trivially correct (r10 live at the success instant); `dc_recur` inherits the discipline (M-1b save-by-value, single-field form ×2). Cost: +16B and 2 stores per blob-interior pending push, 2 restores per β. s23K's "match state = 4 registers + frame" becomes 6.

### SCAN RECAST — the retry loop is the SITE's ω, not the blob's protocol
The shim's SCANBASE fills (r8→kt−32, r14d→kt−40) fed the in-blob scanfail retry (`mov rsp,rbp` whack + re-attempt). Under WREG the blob knows NOTHING about retry: **an unanchored site chooses ω = its own retry-advance stub**, which reads/writes the attempt cursor in the STATEMENT frame's own slots (licensed, depth-immune, the site's own layout — no shared-code offset problem) and re-runs the glue with the advanced start. Anchored sites choose ω = statement-fail. scanhit's "publish the winning start" head-slot write-back is already statement-side and stays. **The entire in-blob scanhit/scanfail protocol dies with the carve, in the same slice.**

### REGISTRATION + RESUME (same slice as the shim delete)
- `rt_proc_set_fn("PAT$N", …)` registers the FIRST-BOX address (the α label is gone). `rt_proc_set_frame_bytes` → 0, with a consumer audit (who reads frame_bytes for PAT procs?). `set_jmpentry` stays 1.
- **Resume-slot pair dies for PAT blobs:** the `lea rax,[rip+ω]; mov [rbp+240],rax` store AND its `jmp [rbp+240]` readers — armed on PAT$ blobs only via the `g_resumable_callable_active` leak (emit.cpp:2443/3060, 11-CODE's second route). Fix the LEAK in this slice; the wires make the slot redundant for this family. **Witnesses: `dc_nest_bt` · `dc_sib_bt`** (the deep-arrival programs FABLE measured breaking under naive de-arm — they must ride the wires now; if they don't, the deep-arrival class is FRAMED-licensed, not WREG — decide by measurement, record which).

### KILLSWITCH (one switch, five surfaces)
`SCRIP_WREG=0` reverts BYTE-IDENTICAL to legacy: rcx/rdx pass_wires + α shim + carve + zctx push/pop + CLASS-D exits + resume store + registration address. ON is default at WREG-2 landing (PT-1 precedent). The switch gates site glue, shim emission, exit shape, registration, and cell capture TOGETHER — α-with-no-push ⇒ exits-with-no-pop is one mechanism and must flip as one.

### RUNGS
- [~] **WREG-0 · CLAIM + SWEEP (any seat, no behavior change)** — ⬅ **PART-DONE s13 (SCRIP `1d81b015`). GATE + CENSUS LANDED; THE SWEEP ITSELF IS NOT STARTED.**
  - ✅ **Gate `test_gate_wreg_claim.sh` + `scripts/wreg_claim_whitelist.txt` landed** — informational by default, `--strict` hard-fails; matches EVERY spelling (`\br1[01][dwb]?\b`) and through macros, comments stripped. Whitelist EMPTY BY DESIGN (nothing is licensed until WREG-1 creates the glue emitters).
  - ✅ **Exit criterion's no-change half VERIFIED at `1d81b015`, same container:** probe m3 **33/5/1**, fail set `{ab_freturn ab_nret_lvalue ab_redefine dc_recur z4_arbno}` · xc/patterns m3 **82/40** — BOTH reproduce s12b **BY SET**, not merely by count. Full xc fail set now recorded in the s13 cursor below (s12b published counts only; a count is not a set).
  - ⛔ **CENSUS CORRECTED — the design's 178 is raw, not the surface:** 178 raw quoted = **159 code + 19 comment-only**; TRUE surface **234 across 29 files** (all spellings, code only). The 75 delta is bracketed operands + `r10d/r11b`. Per-file, size from the gate not from this file's text: the design's bb_call_fn 36 measures **45**.
  - ⛔⭐ **"BOTH UNOWNED" IS FALSIFIED — r10 IS ALREADY CLAIMED.** `bb_func_activate.cpp:25-26` `AB_TC_REG{,_D}` = `"r10"/"r10d"` when `g_rtcc_on && RTCC_GLOBAL_R9_GVA`, taken by the s8 RTCC-safety fix *because* r10 was then "scratch-tier claimed with NO global assigned" — the very property WREG retires. **DORMANT by default** (`g_rtcc_on = 0`, env `SCRIP_RTCC`); live only at `SCRIP_RTCC=1`. **Unprobed — route, do not guess:** the only remaining safe target is r8, which this ladder flags SCANBASE-entangled, and RULES forbids acquittal by code-reading as firmly as conviction. Full provenance: `FINDING-2026-08-10d-CLAUDE-OP5-WREG-0-*`.
  - ⬜ **STILL OWED:** the **234-site** sweep (templates/emitter only — the 182 RTX asm sites are EXEMPT: the veneer banks the pair, see the DECIDING FACT in the cursor) · raw-byte producer eyeball (`x86_asm.h` internals, `ef_b3` in xa_flat — greps cannot see encoded bytes) · `--strict` green · the manifest (surviving invariant blobs post-PT-2b + witnesses + sbl refs; `sbl` was NOT cloned/built this seat).
- [~] **WREG-1 · SITE GLUE + REGISTRATION** — ⬅ **PART-DONE s15 (SCRIP `1e26e27d`). Killswitch `SCRIP_WREG` BORN (default OFF, md5-byte-identical revert proven). `bb_glue_pass_wires_blob` = `lea r10,γ · lea r11,ω · jmp rax`, NO new encoder needed in either medium. `bb_match_defer` fast arm converted. ⛔ STILL OWED: the OTHER blob entry paths — PT-2's "sole flat_pat entry" premise is FALSIFIED (3 more entries measured in `dc_sib_bt` asm); per-caller blob-vs-proc target census REQUIRED before flip. Registration (`rt_proc_set_fn` first-box address, `frame_bytes`→0) NOT started.**
- [ ] **WREG-1-ORIG (superseded scope note): SITE GLUE + REGISTRATION (full-runway seat, killswitch born here)** — `bb_match_defer` fast arm (emit.cpp:1935) + `emit_jmp_entry_for_patproc` (2914/2925) retarget to first-box label; two `lea`s; runtime-defer `jmp rax` variant; startup registration address + frame_bytes 0. Both media. Both switch arms build and run.
- [~] **WREG-2 · SHIM + CLASS-D + RESUME DELETE** — ⬅ **PART-DONE s15, GATED RED. Under the switch: blob α shim (carve + wire stores + SCANBASE fills + 6-instr `g_zctx` push) SUPPRESSED; CLASS D exits = `jmp r10`/`jmp r11`. ⛔ ON ARM IS 21/17 (vs 33/5 OFF) BECAUSE THREE READER CLASSES SURVIVE THE CUT — `scanhit` + `scanfail` (⇒ WREG-4) and `proc_PAT$N_res`/β + the `[rbp+32]` resume leak (⇒ WREG-3). ⭐ THEREFORE: WREG-4 IS A PREREQUISITE OF WREG-2, NOT A FOLLOW-UP — re-order the ladder. Do NOT flip default-ON until (1) entry census, (2) WREG-4, (3) WREG-3 land.**
- [ ] **WREG-2-ORIG (superseded scope note): SHIM + CLASS-D + RESUME DELETE (same seat/slice as WREG-1)** — blob_act region: carve, wire stores, SCANBASE fills, zctx 6-instr push, resume store — GONE under the switch; exits `jmp r10`/`jmp r11`; zctx pop + γ record + ω absolute unwind — GONE; scanhit/scanfail blocks — GONE (retry is now WREG-4's site stub); `g_resumable_callable_active` leak FIXED. Deliverable: **claws5-match.s regen carries ZERO `proc_PAT$` α labels and ZERO `g_zctx` references** — the exact mess Lon pointed at, gone from the artifact.
- [ ] **WREG-3 · SUSPENSION CAPTURE (MECH single-authority zone; Lon routes the seat)** — contract from this file: which cells (blob-interior pendings ONLY), layout delta (+16B pair slot), capture at push, restore at β before any blob code. Mechanics (cell allocation, ARBNO iteration interaction, K-conversion residue) are MECH's; the PT-4 PREREQ note (ARBNO/SPAN/BREAK K-conversion in blob context, COORDINATE WITH MECH) still binds interiors.
- [ ] **WREG-4 · STATEMENT-SIDE RETRY STUB** — unanchored-site ω spelling: attempt-cursor slot in the statement frame, advance+relaunch stub, anchored sites skip. Head-slot write-back audited end-to-end. (Site-emitter work; may land inside WREG-1/2's slice if runway allows.)
- [ ] **WREG-5 · GATE** — probe + xc + broad-336, m3 AND m4, BY SET, same container both arms, BOTH switch states. Floors (≥, s12b measured): probe 33/5 m3 · 33/4 m4 · xc 82/40 m3 · 78/44 m4 · broad 270/66 · 262/68/6. Witness set: pt_inline_1{,_hand,_full} · w_cap_* · dc_nest_bt · dc_sib_bt · dc_recur · `151_pat_arbno_inline_fence_backtrack` (the reverse-MODE34 SEGV — must not regress FURTHER; repairing it is not this rung's burden). Census: T1/T2 establishments → 0, keepers {STATEMENT·FUNCTION·MATCH_BEGIN·FENCE1} unchanged. regen ×3. sbl oracle on every witness.
- [ ] **WREG-6 · SEAL** — ratchet reseeds at the honest census (s12 baseline Σest ≈ 263, xc measured fresh not from stale artifacts); killswitch label + code DELETED same slice once green BOTH media BOTH modes; if these blobs were g_zctx's last consumers, `grep -r g_zctx src/` == 0 discharges PT-7's exit gate in the same commit.

**OPUS WALK NOTES (binding house law, restated so a fresh seat needn't rediscover it):** push per-rung mid-session, never park (RULES ¶1) · MONITOR-FIRST on ANY oracle divergence — never read-and-guess · ARCH-ICON.md + GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md BEFORE any template/encoder edit, non-negotiable · BY SET always, absolute counts do not transfer across containers (s7 law) · same container both arms · namespace /tmp evidence per seat (11-ENV clobber) · `/home/claude/s12_run.sh` exists (seat-namespaced, ENV-TRAP-(a)-correct runner; promote to scripts/ at will) · a deletion landed at end-of-context is a broken tree by construction — WREG-1/2 want a FRESH seat.

**Relationship:** supersedes PT-4's spine linkage CELL for the wire half (registers replace the cell); dissolves PT-4's depth problem for exits; makes PT-7's `g_zctx` DELETE reachable (these blobs are its remaining consumers); the DEL-T1 re-delete rides WREG-2; WREG-3 is M-1b's shape and `dc_recur` is expected to flip green under it.

**UNRESOLVED BY DESIGN (named so nobody mistakes silence for a decision):** (a) MECH owns cell mechanics and ARBNO-iteration/K-conversion interaction; (b) deep-arrival (`flat_deep_arrival`) blobs — WREG wires or FRAMED license: decided by the dc_nest_bt/dc_sib_bt measurement in WREG-2, recorded either way; (c) frame_bytes consumer audit outcome.

## ⛔⭐⭐⭐ s15f — I RETRACT MY OWN OPTION (c). LON APPROVED IT; I DID NOT IMPLEMENT IT. RTCC-OFF IS A WREG TRAP.

**Lon routed (A) as "yes, fix it as you suggest" = RTCC default-OFF for pattern blobs. I DID NOT MAKE THE CHANGE, because reading the flag revealed my recommendation was WRONG.** `rtcc_init.c:20` states the precondition I had not read: *"the veneer's writeback/reload is the ONLY thing that preserves rΓ=r10/rΩ=r11 across an rt_* crossing — at OFF a blob's wires are clobbered by plain SysV rules and the failure is a WILD JUMP, not a wrong answer."*

**r10/r11 are CALLER-SAVED in SysV.** Therefore: **RTCC-ON + flat cell** = correct at leaf depth, wrong under nesting. **RTCC-OFF** = wrong at EVERY crossing. **OFF is strictly worse, and its failure mode is worse** (wild jump vs wrong answer). ⛔ **Option (c) is not a safe interim; it is the opposite of one.**

**⭐ WHY IT LOOKED RIGHT AND WHY THAT IS THE TRAP:** today `SCRIP_WREG=0`, so NO wires ride r10/r11 and RTCC-OFF genuinely does restore m4 (133/16/0/2 vs 3/16/0/132). It would make the tree measure BETTER today **and turn WREG's flip into wild jumps tomorrow** — a change that buys a clean number by disarming the one mechanism WREG depends on. **The m4 confound is REAL but RTCC-OFF is the wrong currency to buy it with.**

**CORRECTED ROUTING FOR (A) — (a) IS NOW THE ONLY LIVE CANDIDATE:** stack-pair `push r10/r11 · call rt_* · pop r11/r10` bracketing each crossing. Depth-immune BY NATURE (the stack is the depth index), and it is the SAME shape `bb_scan_any` already uses self-preservingly. ⛔ **ITS ONE AUDIT, UNCHANGED AND MANDATORY: non-local ω exits that leave THROUGH a crossing skip their pop ⇒ rsp off by 16 ⇒ silent drain-law violation.** Enumerate every non-local exit that can cross a veneered call before writing a line. **(b) depth-keyed save remains the fallback if that audit fails.**

**⭐ SEPARATE THE m4 CONFOUND FROM (A).** It is a MEASUREMENT problem, not a design one: take m4 with `SCRIP_RTCC=0` **as a reported bisect number only** (RULES: nothing may depend on the killswitch), never as a default flip. Confound documented ≠ confound fixed.

**HONEST NOTE ON HOW THIS HAPPENED:** I proposed (c) from the s37 m4 numbers without reading the flag's own precondition comment, and Lon approved my recommendation in good faith. **An approved bad recommendation is still a bad recommendation** — the approval does not transfer the engineering judgment. Second self-caught error this seat; both caught by checking the artifact instead of trusting my model of it.

## ⭐⭐⭐ s15e CLOSE — PLAN SCRUTINY, LADDER CORRECTIONS, AND THE THREE DECISIONS ONLY LON CAN MAKE (handoff block)

**CORRECTIONS TO THE LADDER AS WRITTEN, all measured this seat:**
1. **The rung ORDER is wrong.** Measured order: **WREG-1 → WREG-4 → WREG-3 → WREG-2 → flip.** WREG-2's cut is safe only after 4 and 3 retire the readers (`scanhit`/`scanfail` ⇒ 4; `res`/β + `[rbp+32]` ⇒ 3). The ON 21/17 is the proof; renumber when convenient.
2. **Delete WREG-1's encoder budget** — void. REX for r10/r11 already correct in BOTH media (`x86_lea_id` REX.R, `x86_jmp_reg` 0x41).
3. **The landmine is now TWO items, not one.** RETIRED for blob-α (the claiming rung deletes the clobberer — one mechanism). STILL LIVE for the sweep: sharpest is `bb_match_end`'s sentinel walk (`mov r10,[sent]` · `mov rsp,[r10+8]` — runs at match END, wires live), then `bb_idx_get`/`bb_var` scratch. `bb_scan_any`'s `push r10`/`pop r10` is SELF-PRESERVING — needs NO edit; shrink the 285 census by that whole class before sizing the sweep.
4. **Census law refined:** `γ_exits == blobs` is THE invariant (24==24 suite-wide). `entries` counts SITES — `rt_defer_get_pat_fn` dispatches BY NAME AT RUNTIME (manual p.86 deferred-eval semantics), so entries < blobs is legitimate. **Use `scripts/test_census_wreg_wires.sh`, never ad-hoc grep** — the instrument self-tests; two consecutive seats were convicted of unit-unchecked greps.

**RISK REGISTER the flip must clear, in order:** WREG-4 (retry → statement-side ω stub; `mov rsp,[rdx+32]` dies with the carve) · WREG-3 with MECH (res/β + slot leak) · the veneer (below) · regen ×3 + BY SET both modes · m4 ONLY after the RTCC confound is resolved (3/16/0/132 vs 133/16/0/2 — any m4 number today measures RTCC, not WREG).

**⛔ THE THREE DECISIONS I NEED FROM LON:**
- **(A) THE VENEER.** Flat `g_rtcc_block` cannot survive nested `*F()` crossings — manual p.123's recursion guarantee makes this reachable, and s15b captured the mechanism as shipped instructions. Options: **(a)** stack-pair `push r10/r11 · call · pop` bracketing each RT crossing — depth-immune BY NATURE, but MUST be audited against NON-LOCAL ω exits through a crossing (a skipped pop = rsp off by 16, silently violating the drain law); **(b)** depth-keyed save (zctx-indexed cell); **(c)** interim: RTCC default-OFF for pattern blobs — safest, and it un-confounds m4 TODAY. My read: (c) now, (a) if its audit passes. **This straddles GOAL-RTCC — routing is yours.**
- **(B) WREG-4 DESIGN INTENT:** statement-side ω stub vs in-blob thunk — the ladder text permits both readings; pick one before the recast is written.
- **(C) DEEP-ARRIVAL VERDICT DEFERRED:** `dc_nest_bt`/`dc_sib_bt` red is CONFOUNDED until 3+4 land — do NOT let any seat read the current failure as the FRAMED-license answer.

**STATE:** SCRIP `1e26e27d`+`cb9f68e2` · `.github` ×7 with this · corpus UNTOUCHED · default arm 33/5 BY SET == s12b/s13 floor · killswitch md5-byte-identical · ON 21/17 fully attributed. Push + `handoff_status.sh` verbatim in chat (credential arrived s15e).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 15 (Claude Opus 5 — THE WIRE MECHANISM IS IN AND COSTS NO NEW ENCODER; THE ON ARM ITEMIZES ITS OWN BLOCKERS AND THEY ARE WREG-3/WREG-4; "SOLE flat_pat ENTRY" IS FALSE)

**Watermark at close:** SCRIP **`1e26e27d`** (parent `565ecfa8` — a concurrent MECH seat had moved HEAD past s14's `353bafbd` before this seat opened; re-checked at open per 11-ENV) · corpus **`bea31de0`** UNTOUCHED · `.github` this commit. **⛔ PUSH: credential requested in-chat per RULES 6b; `handoff_status.sh` NOT run — it would report BLOCKED, which is already known. No terminal doneness claim is made.**

**LON'S DIRECTIVE THIS SEAT (verbatim):** *"Remove PROC overhead to pattern BLOBS and begin using R10 and R11 for GAMMA and OMEGA."* — taken as the routing call WREG-1/2 was waiting on. **Landed the MECHANISM, default OFF; did NOT flip the switch.** Full write-up: `FINDING-2026-08-10f-CLAUDE-OP5-WREG-1-2-SLICE1-*`.

**⭐ THE LADDER WAS PESSIMISTIC IN EXACTLY ONE PLACE — THE SPELLING IS FREE.** `x86_lea_id` already sets REX.R for `m>=8` and `x86_jmp_reg` already emits `0x41`, so `lea r10,γ` / `jmp r11` encode correctly in **BOTH MEDIA with no new encoder** and no `x86_asm.h` edit. TEMPLATE-ONLY EMISSION is satisfied without touching the encoder file at all.

**⭐⭐ THE CONVERGENCE THAT RETIRES THE LANDMINE (for this path).** MECH s37 flagged the six ZCTX sequences' r10/r11 scratch use as "live-on-arrival for whoever lands WREG." **That code is what WREG-2 deletes.** The blob α's `g_zctx` push *is* the sharpest r10/r11 scratch user (`mov r10,[rax]` · `mov r11,[rax+8]` · `lea r11,[rsp+kt-40]`). The rung that claims the registers deletes the code clobbering them — one mechanism, not two racing seats. ⛔ This retires the landmine for the **blob-α path only**; the 285-occurrence sweep surface is UNTOUCHED.

**KILLSWITCH LAW SATISFIED BY MD5, NOT BY ARGUMENT:** `SCRIP_WREG=0` byte-identical to pre-edit HEAD on `dc_sib_bt` `9fe25ab5` · `dc_nest_bt` `d1e47d5d` · `pt_inline_1` `8a54a261`.

**MEASURED m3, same container, BY SET:** **OFF 33/5**, fail set `{ab_freturn ab_nret_lvalue ab_redefine dc_recur z4_arbno}` == s12b/s13 floor **name-for-name, ZERO regression**. **ON 21/17** — +12: `ab_defer_call dc_nest_bt dc_sib_bt mv_arbno_callcap mv_valheld_cap os1_runtime_k pb_stitch_defer w_cap_ay w_cap_group w_cap_novowel w_cap_stored w_cap_xxay`.

**⛔⭐⭐⭐ THE ON ARM'S RED IS THE CARVE-ERAD CONVICTION REPRODUCED — AND IT BILLS TO THE LADDER'S OWN UNEXECUTED RUNGS.** Cutting the α carve left THREE reader classes live, now named from emitted asm: **(1) `scanhit`** reads `g_zctx[1]` header {scan flag `[+8]`, δ0 `[+0]`}; **(2) `scanfail`** same reads + `mov rsp,[rdx+32]` retry re-base + `jmp α_attempt`; **(3) `proc_PAT$N_res`** re-PUSHES `g_zctx` *using r10/r11 as its scratch*, with `β = jmp qword ptr [rbp+32]` (the `g_resumable_callable_active` slot leak already named at emit.cpp:2443/3060). **(1)+(2) are WREG-4. (3) is WREG-3.** The fail set corroborates: `w_cap_*`×5 + `mv_*_cap` are capture/suspension, i.e. WREG-3. **This is not a falsification of LADDER WREG; it is the ladder's own rungs, unexecuted, measured.**

**⛔ THE DEEP-ARRIVAL QUESTION IS STILL OPEN — DO NOT READ THIS RED AS ITS ANSWER.** The ladder says `dc_nest_bt`/`dc_sib_bt` decide whether deep-arrival is WREG or FRAMED-licensed. They fail here, but they fail **with res/β unconverted**, so this run cannot discriminate. Recorded as open, with the reason.

**✅⭐ s15d — THE s15c OPEN ITEM IS RESOLVED, SAME SEAT, AND SOLE-ENTRY SURVIVES.** Discriminator run on `os1_runtime_k`: **both** PAT$0 and PAT$1 are REGISTERED (`rt_proc_set_fn` string literal present for each) and referenced — so candidate **(a) DEAD BLOB is FALSIFIED**. But **(b) is NOT the conclusion either.** The defer fast arm resolves its target via `rt_defer_get_pat_fn` **at RUNTIME, BY NAME**, so ONE emitted entry site can dispatch to ANY registered blob: **`lea r10` counts SITES, not reachable blobs**, and `entries < blobs` is therefore expected wherever one dynamic site serves several. ⭐ **This is deferred evaluation's own semantics, not an implementation quirk** — SPITBOL manual p.86: the `*` operator's target is fetched *when the pattern is used*, not when it is constructed; the compile-time count of entry sites cannot bound the run-time set of targets. **PT-2's sole-entry premise survives, and the census law is refined: `γ_exits == blobs` is the binding invariant (24==24 suite-wide); `entries` is a SITE count and is bounded by neither.**

**⭐ s15c — INSTRUMENT LANDED + ONE NEW OPEN ITEM.** `SCRIP/scripts/test_census_wreg_wires.sh` (`cb9f68e2`): whitespace-tolerant, word-anchored, and it **SELF-TESTS its own patterns against synthetic padded emitter text, exiting nonzero and reporting NOTHING if the self-test fails** — a silent zero is the failure being prevented. Suite-wide m3, `SCRIP_WREG=1`: **blobs=24 · blob_entries=29 · γ_exits=24. LAW HOLDS: γ_exits == blobs** (every blob converts its own exit pair); entries ≥ blobs expected. ⛔ **NEW OPEN ITEM THE BROKEN GREP HAD HIDDEN:** `os1_runtime_k` and `pt_inline_1_full` report **blobs=2 but entries=1** — one blob in each has NO converted entry. Two candidates, **NOT discriminated**: (a) a DEAD blob (PT-2 dead-blob-suppression territory) or (b) a genuine second entry path that is not `bb_match_defer`'s fast arm — **(b) would RE-OPEN the sole-entry question s15b retracted.** Next seat discriminates cheaply: check whether the unentered blob is referenced anywhere at all. Recorded as undetermined; neither reading is asserted.

**⛔⭐⭐⭐ RETRACTED SAME SEAT (s15b) — READ THIS BEFORE THE PARAGRAPH BELOW IT.** The "SOLE flat_pat entry is FALSE" claim in the next paragraph is **WITHDRAWN**. It rested on `grep -c 'lea r10'` run against asm that pads mnemonics with MULTIPLE SPACES (`lea              r10`) — the pattern cannot match, returned 0, and I read the 0 as "the site glue never fired." Re-measured with `grep -cE 'lea +r10\b'`: **the wires fired end-to-end all along** (`lea r10`·`lea r11`·`jmp r10`·`jmp r11` all present; `lea rcx` 8→6). ⭐ **SAME DEFECT CLASS s14 CONVICTED IN THE CLAIM GATE** (`grep -c` counting LINES, reported as `mentions`) — two consecutive seats, one instrument family: **a grep whose units were never checked against its own input.** CORRECTED CENSUS (ON, m3): `dc_sib_bt` 1 blob/1 entry/1 exit-pair · `dc_nest_bt` 1/1/1 · `w_cap_ay` 2/2/2 · `pb_stitch_defer` 1/3/1 · `mv_valheld_cap` 1/2/1 — **`PAT$ blobs == exit pairs` on every row**, entries ≥ blobs, every blob converted through the single `bb_match_defer` site. **PT-2's premise is CONSISTENT with the evidence.** The other trios are preceded by `call rt_proc_open_fn@PLT` — DEFINE'd-proc/one-shot entries that MUST keep rcx/rdx and correctly do; I mistook a shared *contract* for a shared *target class*. ⭐ **WHAT SURVIVES, STRONGER:** the three reader classes were read from emitted asm, not the broken grep — they are now the SOLE cause of ON 21/17, so **WREG-4-before-WREG-2 stands on cleaner evidence than when first written.** ⭐ **BONUS EVIDENCE:** tracing rax provenance caught s14's veneer flat-cell hazard as shipped instructions — `mov [rax+56],r10 · mov [rax+64],r11 · call rt_proc_open_fn@PLT · mov r10,[r11+56] · mov r11,[r11+64]` around a live crossing. `g_rtcc_block[32]` is flat, no depth index: correct at leaf, destroys outer wires under nesting. Still short of a failing witness; s14's routing question to Lon is UNCHANGED and still owed.

**⛔⭐⭐ A LADDER PREMISE IS FALSIFIED — PT-2's "the defer fast arm is the SOLE flat_pat entry" IS FALSE.** `dc_sib_bt` asm carries THREE more blob entries (`.Lx75_4/5`, `.Lx75_7/8`, `.Lx77_2/3`) from other `bb_glue_pass_wires` callers: `bb_match_capture`×3 · `bb_match_end`:47 · `bb_match_value`:38 · `bb_call_value`:62 · `bb_call_proc_staged`×7 · `bb_match_defer`:112. **WREG-1 is not one site.** Slice 1 converted one entry path, so ON also ran blobs whose callers still deliver rcx/rdx — a second, independent cause of the red.

**NEXT SEAT, IN ORDER:** (1) **Per-caller blob-vs-proc target census** — classify each `jmp rax`'s rax provenance; sites targeting DEFINE'd procs KEEP rcx/rdx, sites targeting PAT$ blobs take r10/r11. Cheap, mechanical, and it sizes WREG-1 honestly for the first time. **Do this before any further deletion.** (2) ⭐ **RE-ORDER THE LADDER: WREG-4 IS A PREREQUISITE OF WREG-2, NOT A FOLLOW-UP** — slice 1's red proves the scanhit/scanfail recast must precede the α cut. (3) WREG-3 with MECH for res/β + the `[rbp+32]` leak. (4) Only then default-ON + BY SET both modes + regen ×3. (5) **m4 is confounded independently of WREG** — MECH s37 measured RTCC-default-ON at 3/16/0/**132** vs 133/16/0/2 at `SCRIP_RTCC=0`; any m4 number taken now measures that, not this. (6) s14's veneer routing question is UNTOUCHED and unblocked-by-construction here (switch off).

**THIS SEAT'S LIMITS, STATED:** m3 only — no m4, no xc-122, no broad-336. Sweep untouched. No corpus bytes, no regen ×3 (OFF arm byte-identical ⇒ artifacts unmoved; regen becomes owed the moment the default flips).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 14 (Claude Opus 5 — THE GATE'S "MENTIONS" WERE LINE COUNTS: SWEEP IS 506 NOT 416; AND s13's STRONGEST PRO-WREG ARGUMENT INVERTS — THE VENEER THAT WOULD SAVE THE WIRES IS A FLAT GLOBAL CELL)

**Watermark at close:** SCRIP **`353bafbd`** (linear from parent `802cf6bd` == s13b close; nothing moved under this seat) · corpus **`bea31de0`** UNTOUCHED · `.github` this commit. **⛔ PUSH BLOCKED — NO CREDENTIAL SUPPLIED THIS SESSION. Five commits unpushed: SCRIP `1d81b015` `802cf6bd` `353bafbd` + `.github` `47946ebb` `9b18fe4d`. Requested in-chat per RULES.md 6b; not yet supplied. NO terminal doneness claim is made and `handoff_status.sh` was not run — it would report BLOCKED, which is already known.**

**NOT A CODE RUNG.** One script repaired, zero `src/` bytes changed. Full write-up: `FINDING-2026-08-10e-CLAUDE-OP5-WREG-0-THE-VENEER-THAT-SAVES-THE-WIRES-IS-A-FLAT-GLOBAL-CELL-*`.

**⛔ THE SWEEP SURFACE IS 506 OCCURRENCES, NOT 416 (`353bafbd`).** `test_gate_wreg_claim.sh:78` used `grep -c`, which counts **LINES**, and printed the total labelled `mentions`. A line carrying both registers counts once — 10 such in `bb_call_fn.cpp`, and `lea rΓ`/`lea rΩ` pairs are the idiom the ladder itself specifies, so this is not a rare case. Templates+emitter **234 lines / 283 occ** · RTX asm **182 / 223** · **TOTAL 416 / 506** = **2.8× the ladder's budgeted 178**, not 2.3×. `bb_call_fn` 82/**97**, `rtx_match.S` 74/**89**. Gate now prints both columns (LINES stays comparable with every number s13 recorded) and `--strict` fails on occurrences. ⭐ This also dissolves s13's own 45-vs-82 discrepancy: **45** = quoted-only line count (drift check), **82** = all-spellings line count, **97** = truth — three instruments, three units, cited as commensurable.

**⛔⭐⭐ s13's "ARGUMENT AVAILABLE TO WREG-2 THAT THE LADDER UNDERSELLS" INVERTS ON MEASUREMENT.** s13 argues RTCC's veneer *"is what makes register-carried wires viable at the runtime boundary."* The mechanism is real — the RTCC=1 arm of `ab_defer_call` emits `mov [rax+56], r10` / `mov [rax+64], r11` at every crossing — **but the save area is FLAT**: `rtcc.h:63` `uint64_t g_rtcc_block[32]`, `RTCC_SLOT_R10 7` (byte 56, matching the asm), no depth index, no stack, seeded once. Under nesting the inner crossing's save **destroys** the outer's γ and the outer restore reloads the inner value. **A flat cell cannot serve a LIFO discipline — this is `g_blob_ctx`'s single-cell defect in register clothing, the conviction this file already carries at `pattern_match.c:624`.** WREG's ONE LAW makes pendings per-activation *on the spine* and is sound there; it is **silent on the veneer**, which is a different save area on a different discipline. And the nested shape is not exotic: the witness is `ab_defer_call.sno` ("*function called MID-MATCH via deferred `*F()`*"), and the manual (p.123) guarantees it — SPITBOL matches exhaustively, *"deferred expressions are not assumed to match at least one character, and recursive patterns always work properly."*
⛔ **THEREFORE s13's "ONE-LINE EDIT WREG-2 MUST NOT FORGET" IS NOT SUFFICIENT AND NOT SAFE ALONE.** Extending `LIVE_CLAIMS` buys "H2 veneer-writeback protection" — **that protection is the flat cell.** Make the edit if you like, but do not believe the boundary is closed. Design input, not a landed decision: if r10/r11 are *reserved* rather than scratch, the veneer should **exclude** them (invariant across the crossing by convention; any C-side clobber is what the claim gate is for) — strictly cheaper (190 r10 mentions in one small program) and removes the LIFO violation at the root; the alternative, a depth-indexed block, is a second stack beside the FORTH spine.

**s13's ITEM (2) — `AB_TC_REG` PROBE: CHARACTERIZED, NOT CLEARED.** Dormancy now **source-verified**, not asserted: `rtcc_init.c:20` `g_rtcc_on = 0` (killswitch default) + `rtcc.h:57` `RTCC_GLOBAL_R9_GVA 1` ⇒ `AB_TC_REG` = **r9** in every default build. Behavioural probe both arms m3 on `ab_defer_call` (a PASSING witness — `ab_freturn` is one of the 5 known pre-existing probe failures and cannot discriminate): **RTCC=0 PASS · RTCC=1 PASS**, both == `.ref`. ⛔ **HONEST LIMIT:** the veneer hazard is derived **by construction from a measured mechanism**, NOT demonstrated by a failing program, and cannot be until γ actually rides r10. The probe that settles it the day WREG-1 lands: `ab_defer_call` under `SCRIP_RTCC=1` with wires live, watching `block[7]` survive the nested activation.

**⛔⭐⭐⭐ RECONCILED WITH s13c `23355182` (landed 23:22:46, SEVEN MINUTES BEFORE THIS SEAT'S COMMIT AND UNSEEN WHILE MEASURING — a second stale-orientation strike in one session, recorded against myself).** s13c found the SAME veneer mechanism and concluded the OPPOSITE: that it is a guarantee, hence *"the 182 RTX sites need NO sweep"* (416→234) and *"WREG requires RTCC=ON as a CORRECTNESS precondition."* **Both partly right; the union is worse than either.** At **RTCC=OFF** the wires die at every crossing by SysV rules (s13c). At **RTCC=ON** they survive a LEAF crossing and the **outer γ is destroyed by the inner save** under nesting (this seat). ⛔ **THERE IS NO RTCC SETTING AT WHICH THE VENEER IS A SOUND SURVIVAL MECHANISM FOR DEFERRED-MID-MATCH.** Design intent confirms it: `x86_asm.h:299` documents the writeback as *"writeback all 9 GPRs to g_rtcc_block WITHOUT touching RSP"* — deliberately not stack-based — and `depth|stack|nest|reentr|push` is **zero hits** across `rtcc.h`+`rtcc_init.c`.
⛔ **TWO CORRECTIONS THIS FORCES ON s13c:** **(a)** its recommended option **(3) "RTCC default-ON as WREG prerequisite" does NOT close the hole** — it repairs the leaf column and leaves the nested cell open; of its three options only **(2)** (WREG emits its own save/restore) can be made nesting-correct, and only if that save is **per-activation on the FORTH spine — WREG's own ONE LAW — not a flat block**. The honest generalisation: WREG needs a per-activation wire save *regardless of RTCC*, with RTCC demoted to a leaf-case optimisation. **(b)** its **RTX exemption is CONDITIONAL and fails where it matters most** — the "invisible to a caller that banked and reloaded" argument holds for LEAF calls, but `rtx_match.S` **is** the match runtime, where deferred `*F()` re-entry is the design, not an edge case. So RTX is exempt for leaf crossings and **`rtx_match.S`'s 74 lines / 89 occ are not safely exempt at all** — the file the bulletin already named the sharpest edge, on independent grounds. ⭐ **Untouched and load-bearing from s13c:** WREG is specified default-ON at WREG-2 on top of a default-OFF RTCC — *"that combination ships wires with no survival mechanism"* — the most schedule-relevant fact either seat produced.

**NEXT SEAT, IN ORDER:** (1) **Lon routes the veneer question ON BOTH SEATS' EVIDENCE, not either alone** — exclude-wires vs depth-indexed block vs WREG-independent-of-RTCC; it straddles GOAL-RTCC (owns the veneer) and this goal (owns the wires), so it is a routing call, not a drive-by. (2) Re-state LADDER WREG's sweep cost as **283 unconditional + 223 conditionally-exempt** occurrences, per-file in occurrences (`bb_call_fn` 97 · `rtx_match.S` 89, NOT safely exempt) — the ladder's 178, s13b's 416 and s13c's 234 are three units across two scopes; none is usable as written. (3) WREG-1/2 still wants a FRESH FULL-RUNWAY SEAT. (4) m4 column + broad-336 still owed from s8/s12b, untouched here. (5) `128` / `151` own rungs, untouched.

**⛔ ENVIRONMENT — A CONCURRENT SEAT COMMITTED INTO THIS WORKING COPY MID-SESSION.** `.github` reflog: clone `37e0273c` 22:44:04 → commit `47946ebb` **23:12:25** → commit `9b18fe4d` **23:14:29**, none of them this seat's. s13 and s13b ran in this same container and checkout; an orientation completed at 22:44 was two commits stale by 23:14 and the goal file grew **366 → 400 lines underneath a finished read**. 11-ENV's shared-container warning is CURRENT, not historical — **re-check HEAD before trusting an orientation, not only at open.** `--compile` writes asm to **stdout** (redirect it; s8's ENV-TRAP (b) is about the binary, this is about the text).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 13 (Claude Opus 5 — WREG-0 GATE + CENSUS LANDED; THE DESIGN'S r10 "UNOWNED" PREMISE IS FALSIFIED; SWEEP AND WREG-1/2 NOT STARTED)

**Watermark at close:** SCRIP **`802cf6bd`** (linear from parent `bce9a4b0` == s12-close cursor exactly; nothing had moved under this seat) · corpus **`bea31de0`** UNTOUCHED · `.github` this commit. **⛔ PUSH STATUS IS `handoff_status.sh`'S ALONE — see the handoff line at the bottom of this cursor.**

**NOT A CODE RUNG for the emitter.** One new script + one new registry; **zero `src/` bytes changed.** Tree builds clean (`scrip` + `out/libscrip_rt.so`, 0 errors).

**⭐ SUITES — BY SET, same container, m3 (WREG-0's "unchanged vs s12b" criterion, MET):**

| suite | s13 measured | s12b recorded | verdict |
|---|---|---|---|
| probe (39) | **33 / 5 / 1** | 33 / 5 / 1 | set-identical |
| crosscheck/patterns (122) | **82 / 40** | 82 / 40 | count-identical, set now recorded |

probe fail set = `{ab_freturn ab_nret_lvalue ab_redefine dc_recur z4_arbno}` — precisely s12b's `{4 pre-existing} + dc_recur`.
**xc m3 FAIL SET (40) — RECORDED HERE FOR THE FIRST TIME; s12b published a COUNT, and a count cannot detect a swap:**
`052 054 063 064 065 066 114 115 116 119 120 124 128 129 130 137 138 139 140 141 143 145 148 149 150 151 153 154 157 160 162 163 166 167 174 175 176 178 179 183` (short forms; `128_pat_recursive_grammar_right_rec` and `151_pat_arbno_inline_fence_backtrack` both present, consistent with their standing own-rung flags). **m4 NOT run this seat** — the m4 column remains as s12b left it.

**⛔⭐ THE SESSION'S LOAD-BEARING FINDING — LADDER WREG's register-pick ground #2 is STALE.** The design says r10/r11 are *"both unowned"*, citing REGISTER-LAYOUT's retirement notice. True of the notice, **false of HEAD**: `bb_func_activate.cpp:25-26` defines `AB_TC_REG{,_D}` as `"r10"/"r10d"` under `g_rtcc_on && RTCC_GLOBAL_R9_GVA`, put there by the s8 RTCC-safety fix **because r10 was, in that fix's own words, "scratch-tier claimed with NO global assigned"** — the exact property assigning γ to r10 destroys. It hid from the design's census because it is spelled `r10d` behind a macro and the census pattern was `"r10"`. **De-escalation, stated as precisely as the hazard: `g_rtcc_on = 0` by default, so the arm is DORMANT in every default build and live only at `SCRIP_RTCC=1`.** WREG is not blocked today; it is blocked the day RTCC goes default-ON, which GOAL-RTCC's charter points straight at. **UNPROBED — recorded as collided, not as broken and not as cleared.** Full write-up incl. the r8/SCANBASE squeeze that makes the obvious fix a rung rather than a drive-by: `FINDING-2026-08-10d-CLAUDE-OP5-WREG-0-*`.

**⛔⭐⭐ THE SWEEP IS ~2.3x THE LADDER'S BUDGET — RTX ASM IS 182 MORE SITES.** This seat's own parent commit (`.github` `37e0273c`, CROSS-GOAL BULLETIN, Lon-directed) carries a directive the ladder text was never updated to match: *"WREG-0's claim gate must sweep RTX asm sources, not just `src/templates/` -- raw-byte and hand-written asm do not grep as `\"r10\"`."* Gate extended (`802cf6bd`) and measured: **templates+emitter 234/29 files · RTX asm 182/10 files · TRUE TOTAL 416 mentions across 39 files** vs the ladder's budgeted 178. Head of the RTX burn-down: **`rtx_match.S` 74** · `rtx_icnsub.S` 25 · `rtx_alloc.S` 18 · `rtx_str.S` 17. ⭐ **`rtx_match.S` is the sharpest edge in the product** and structurally so: it runs DURING a match, i.e. exactly when the wires are live, so a clobber there is — the bulletin's own words — *"a wild jump, not a wrong answer."* ⛔ **[SUPERSEDED — see the DECIDING FACT below: the veneer banks the pair, so no RTX sweep is needed.]** ~~The RTX arm needs an OWNER NAMED BEFORE WREG-1 LANDS:~~ by the bulletin's logic an unswept `rtx_match.S` does not degrade WREG gracefully, it makes it unsafe from the first blob that enters the match runtime. LADDER WREG's sweep-cost line (bb_call_fn 36 · xa_flat 21 · …) should be re-stated at the honest number.

**⛔⭐⭐⭐ THE DECIDING FACT OF THIS SEAT — WREG REQUIRES RTCC=ON AS A CORRECTNESS PRECONDITION, AND RTCC IS DEFAULT OFF.** The RTCC veneer already banks the pair across every C-RT crossing: `x86_rtcc_wb_bin` stores `[block+56]=r10`, `[block+64]=r11`; `x86_rtcc_rl_bin` restores *exactly* the scratch tier `{R8,R9,R10,R11}`. ⭐ **COROLLARY: the 182 RTX asm sites need NO sweep** — what `rtx_match.S` does to the pair inside a call is invisible to a caller that banked and reloaded it, satisfying the bulletin's own *"or sits behind an RTCC veneer"* disjunct for free. **Sweep is 234, not 416** (this seat's own s13b conclusion, self-corrected). ⛔ **BUT `x86_rtcc_call` opens with `if (!g_rtcc_on) return x86_call_ro(...)`, and `g_rtcc_on = 0` is the default** — so at RTCC=OFF every `rt_*` call is bare and the wires are clobbered by plain SysV rules. LADDER WREG lists the veneer as *ground #3* for the pick; **it is not a ground, it is a precondition**, and the ladder never says so. As written the two killswitches default in OPPOSITE directions: WREG is specified default-ON at WREG-2, on top of a default-OFF RTCC. **That combination ships wires with no survival mechanism.** ⛔ **LON MUST ROUTE, three non-equivalent options:** (1) WREG default-OFF until RTCC flips (safe; breaks PT-1's default-ON precedent and blocks on another seat); (2) WREG-2 emits its own save/restore when RTCC is off (self-sufficient; *"NO SHIM is literal"* quietly stops being literal); (3) RTCC goes default-ON as a WREG prerequisite (cleanest, matches the bulletin's *"this ladder is the payoff of the RTCC investment"*; makes the schedule dependency explicit). **RESIDUAL HAZARD CENSUSED CLEAN:** `call_bare` is veneer-free in BOTH gate states, but all 12 uses across 8 templates are bracketed — every file's `call_bare` == `rtcc_wb` == `rtcc_rl` count. A screen, not a proof, but the class is presently empty.

**✅⭐⭐⭐ ROUTED AND DISCHARGED SAME SEAT — RTCC IS NOW DEFAULT-ON (SCRIP `c4cb8813`).** Lon, s13 verbatim: *"Make RTCC=ON ALWAYS ... We always want protection and C runtime is going away."* That is option (3). Two lines in `rtcc_init.c`: initializer `0→1`, and `rtcc_env_on` returns 1 for an absent var so only an explicit `SCRIP_RTCC=0` disarms. **Killswitch RETAINED as emergency-bisect only, exactly as `SCRIP_OPT=0` is for the optimizer (RULES OPTIMIZER-STAYS-ON) — nothing may depend on it.** **MEASURED m3 BY SET, same container: probe 33/5/1 (fail set unchanged name-for-name) · xc 82/40 (all 40 names identical to the OFF run). Set-neutral on both; build clean.** **WREG-2 may now land default-ON without shipping wires that cannot survive an `rt_*` call — the precondition is met, not merely acknowledged.** (m4 and broad-336 NOT run this seat; they remain owed.)

⛔⭐⭐ **THE FLIP RETIRES ITS OWN DE-ESCALATION — `AB_TC_REG` IS NO LONGER DORMANT.** This file recorded `bb_func_activate`'s `AB_TC_REG{,_D}` = `"r10"/"r10d"` as harmless because it is gated on `g_rtcc_on`, which was 0. **It is now 1, so the r10 arm is LIVE in every default build from `c4cb8813` forward.** Harmless TODAY — nothing owns r10 yet — and a real **γ clobber inside AB return dispatch** the moment WREG-2 assigns the wire. **Promoted from "route it" to a HARD PREREQUISITE OF WREG-2, still unprobed.** The squeeze is unchanged: the only obvious target is r8, which this ladder flags SCANBASE-entangled, so it needs the probe (SCRIP_RTCC=1 is now simply the default, which makes that probe cheaper to run than it was this morning).

**⛔ ONE-LINE EDIT WREG-2 MUST NOT FORGET:** when site glue actually carries the wires, flip `WREG_CLAIM_LIVE=1` in `test_gate_wreg_claim.sh` **AND** extend `LIVE_CLAIMS` in `test_gate_rtcc_claimed_regs.sh` to `r9:RT_GVA_VA:RC-5-GVA r10:GAMMA:WREG-2 r11:OMEGA:WREG-2`. That second edit is what buys WREG the H2 veneer-writeback protection; skipping it re-runs the s6/s7 r9 story on r10, with γ destroyed process-wide rather than merely locally. Both edits are documented in the new gate's STATUS TIERS block so the next seat cannot miss them.

**⭐ ARGUMENT AVAILABLE TO WREG-2 THAT THE LADDER UNDERSELLS:** the ladder mentions RTCC only defensively (r11 lazy-bind clobber). The stronger claim comes from the s8 fix's own reasoning — **a scratch-tier RTCC claim is exactly what makes a register survive a C-RT crossing without the holder saving it**, which is precisely what a blob's γ/ω need across every `rt_*` call. RTCC's veneer is not merely compatible with WREG; it is what makes register-carried wires viable at the runtime boundary. And it is a *tested* mechanism — s8 proved it on r10 by curing a measured fibonacci SIGSEGV.

**NEXT SEAT, IN ORDER:** (1) **WREG-0's sweep** — 234 sites, size each file from the gate's stdout, not from the ladder's per-file text (bb_call_fn is 45, not 36); the free caller-saved pool is genuinely tight, so "discretionary" is carrying weight — expect per-site liveness argument, not blind rename. (2) **AB_TC_REG probe** (SCRIP_RTCC=1, AB×match overlap) then either clear it into `rtcc_claimed_reg_whitelist.txt` with the named probe or move it off r10. (3) **WREG-1/2 — FRESH FULL-RUNWAY SEAT, unchanged advice**; this seat deliberately did NOT start it, because the ladder's own law is that *"a deletion landed at end-of-context is a broken tree by construction"* and the front half of this seat went to orientation + census. (4) m4 column + broad-336 still owed from s8/s12b.

**ENVIRONMENT NOTES (this container):** `nohup`'d background builds **do not survive between tool calls** — build in the foreground; `make` is incremental so a bounded repeat makes progress (`timeout 280 make -j4 scrip`, then `make libscrip_rt`). Runtime lands at **`out/libscrip_rt.so`**, not the repo root. s8's ENV-TRAP (a) re-confirmed as the right runner shape: compare `$(cat ref)` vs `$(cmd)` — command substitution strips both, which is what makes `070`'s missing trailing newline harmless. `/home/claude/s12_run.sh` did NOT exist in this container, as s12-close predicted.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 12-CLOSE (Claude Sonnet 4.6 — WREG DESIGNED, LOCKED, AND ROUTED; no code commits this segment)

**Watermark at close:** SCRIP **`bce9a4b0`** (unchanged since s12 regen) · corpus **`bea31de0`** (unchanged) · `.github` this commit (design landed as `cf962dc7` stub → `6de2dbff` full).

**CHARTER OF RECORD (Lon, s12 in-chat, verbatim):** *"the task to remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11"* — confirmed as LADDER WREG's charter. Register pick **r10/r11 CONFIRMED BY LON** ("You picked the two I was thinking of… R10 and R11. Perfect. DO the full design.").

**NEXT SEAT:** WREG-0 (claim + 178-use sweep + gate) may start in ANY seat, no behavior change. WREG-1/2 (site glue + shim/CLASS-D/resume delete, one killswitch) want a FRESH full-runway seat — the fix loop is the cost center. WREG-3 is MECH's zone, Lon routes. Full spec, floors, witnesses, and walk notes in LADDER WREG above; suites runner at `/home/claude/s12_run.sh` (container-local — a fresh container re-creates it from the ladder's notes or promotes it to `scripts/`).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 12 (Claude Sonnet 4.6 — ORIENTATION + REGEN ×3 OWED DISCHARGED; census re-measured from fresh artifacts; regen script include-path defect repaired)

**Watermark at close:** SCRIP **`bce9a4b0`** (parent `0970838f`) · corpus **`bea31de0`** (parent `19a5bf9c`) · `.github` this commit. **NOT A CODE RUNG.** No probe, no broad-336, no m4 — all still owed per s11-FABLE-2 cursor.

**REGEN ×3 DISCHARGED** (owed by four seats, now done):
- benchmark: 23/23 emitted, 6 changed → corpus `b8920e77`
- feature: → SCRIP `f6d1653e` then `44c8b1be` (include-path repair landed same session)
- demo: 15 changed → corpus `bea31de0` (was falsely "Committed." due to missing git identity; committed properly after setting identity per RULES.md)

**⛔ REGEN SCRIPT DEFECT REPAIRED (SCRIP `bce9a4b0`):** `util_regen_feature_s_artifacts.sh` compiled from `$ROOT` (SCRIP), but `test/snobol4/library/*.sno` use `-include 'lib/*.sno'` which resolve against the **corpus** root. Result: 4 library artifacts EMIT-FAILed silently on every regen since s189, keeping stale s189 bytes. Fixed by running compile under `cd "$CORPUS"`. Confirmed pre-existing: `a5533659` skipped the same 4. `coverage_sno_nodes` remains an honest bomb (SN4-PAT subset limit). The script also prints "Committed." unconditionally regardless of git exit status — not fixed here, noted.

**⭐ CENSUS RE-MEASURED FROM FRESH ARTIFACTS (crosscheck/patterns temp regen, not committed — cadence policy unchanged):**

| corpus (.s) | PAT-BLOB est | PAT-BLOB push | MAIN est | TOTAL est |
|---|---|---|---|---|
| demo (committed regen) | 58 | 26 | 29 | 95 |
| benchmarks (committed regen) | 0 | 0 | 9 | 9 |
| crosscheck/patterns **(stale committed)** | 93 | 139 | 265 | 358 |
| crosscheck/patterns **fresh from HEAD** | **20** | **9** | **139** | **159** |

Goal file's Σest **557 is obsolete**. True current Σest ≈ **263** (demo 95 + bench 9 + xc-fresh 159). T-class PAT-BLOB in crosscheck: 93 → **20** establishments, 139 → **9** pushes. Crosscheck cadence still unaudited per census instrument's own note — numbers are indicative; do not arm ratchet from temp regen without Lon's routing.

**⛔ 7 TARGETED / 7 REMOVED (source-verified this session):** `x86(...)` rbp emissions in PAT-blob region (emit.cpp 2340–2800) = **0**. Surviving `mov rbp,rsp` sites product-wide = 7, all licensed or T4-class (xa_flat.cpp — MECH zone, never claimed done). T4/T5 remain open by design.

**NEXT (updated by s12b suite run):** (1) ⛔ `151_pat_arbno_inline_fence_backtrack` reverse-direction MODE34 rung — NEW, deterministic, m4-passes/m3-SEGVs; (2) xc 16-gap census vs framed baseline; (3) probe + broad-336 BY SET — s7/s8 floors still owed and unverified; (4) `dc_recur` + depth-reset hygiene → MECH M-1b; (5) T-class census re-seed at honest number (coordinate with Lon — crosscheck cadence decision); (6) `128` own MONITOR-FIRST rung.

**⭐⭐⭐ SUITES RUN (s12b, same container both modes, BY SET — m4 column OWED SINCE s8, NOW DISCHARGED at SCRIP `bce9a4b0`):**

| suite | m3 | m4 |
|---|---|---|
| broad-336 (`test_corpus_snobol4.sh`) | **270 PASS / 66 FAIL** | **262 PASS / 68 FAIL / 6 SKIP** |
| probe (39) | **33 / 5 / 1 SKIP** | **33 / 4 / 2 SKIP** |
| crosscheck/patterns (122) | **82 / 40** | **78 / 44** |

**xc m3 82/40 REPRODUCES s11-FABLE-2's watermark EXACTLY.** Probe fail set = `{ab_freturn, ab_nret_lvalue, ab_redefine, z4_arbno} + dc_recur` = precisely the recorded `{4 pre-existing} + dc_recur`; m4 set identical modulo `ab_nret_lvalue` SKIP (`--compile` itself fails — 11-ENV's same observation). Probe is 39 files not 37: corpus gained 2 witnesses (`19a5bf9c`, `ca87eec0`); counts moved, SET did not.

**⭐ 11-ENV's OPEN ATTRIBUTION QUESTION IS ANSWERED (one direction).** The five one-directional MODE34 violations it measured at `c4ef2176` — `064_replace_multi_arm` · `121_pat_calc_op_dispatch` · `156_pat_cap_alt_abandon_pop` · `181_pat_arbno_defer_tail_stressors` · `182_pat_arbno_defer_windowed_leaf` — are **byte-for-byte the same five at HEAD**, two major landings later (ordered deletion `458b276f` + ZCTX `0970838f`). They are therefore **NOT caused by the revert and NOT caused by ZCTX** — a pre-existing, frame-regime-independent defect class. 11-ENV declined to claim this for want of a pristine-parent build; persistence across two landings that rewrote the frame regime is the cheaper equivalent evidence. Spot-checked deterministic (064: m3 rc=0, m4 rc=139, 2/2).

**⛔⭐ NEW — "STRICTLY ONE-DIRECTIONAL" IS FALSIFIED AT HEAD: `151_pat_arbno_inline_fence_backtrack` PASSES m4 AND SIGSEGVs m3.** Fully deterministic, 3/3 each way (m3 rc=139; m4 rc=0 emitting `inline FENCE in ARBNO sealed`). 11-ENV recorded "zero in the reverse direction" at `c4ef2176`, so this is either introduced by the two landings since, or was always latent and its run did not surface it. **m4's failure set is no longer a superset of m3's** — that assumption is retired, and any bisect predicate resting on it is invalid. This is an inline-FENCE-inside-ARBNO backtrack shape, i.e. squarely the licensed-keeper FENCE1 boundary, and it wants its own MONITOR-FIRST rung.

**Instrument:** `/home/claude/s12_run.sh` — seat-namespaced (`/tmp/${SEAT}_*`, per 11-ENV's clobber finding) and uses `$(cat ref)` vs `$(cmd)` comparison so ENV-TRAP (a) (070's missing trailing newline) cannot false-FAIL. m4 path is compile→`gcc -c`→link→**RUN THE BINARY** per ENV-TRAP (b). Not promoted to `scripts/` — a next seat should, if it wants a standing runner.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 11-**FABLE-2** (Claude Fable 5 — Lon-in-chat: "delete g_blob_ctx... stupidity" — DELETED and REPLACED by ZCTX BASE STACK; probe 26/11→**32/5** · xc 69/53→**82/40**, zero frames zero global)

**Executed:** SCRIP **`9eb9b4f3`** (parent `458b276f`). `g_blob_ctx[5]` + `rt_blob_ctx_ptr` DELETED; replacement `g_zctx[66]` per-activation BASE STACK ([0]=depth · [1]=current base · [2+]=spill; push α+β, pop γ+ω; readers = one indirection off [g_zctx+8], wires/flag/δ0 from each activation's own CARVE [base+kt-24/16/32/40]). Both media: BIN SIB-encoded, TEXT GOTPCREL. This is M-1b's save-by-value shape, single-field form — MECH owns overflow policy + statement-abort depth-reset hygiene (α/β push vs γ/ω pop are balanced per episode; ABORT-class exits that skip ω can leak depth — capacity 64, unaudited).

**MEASURED (m3, this container, BY SET):** probe fail set collapsed to `{4 pre-existing} + dc_recur` — **w_cap×5, mv_arbno_callcap, dc_nest_bt, dc_sib_bt ALL REPAIRED** vs the ordered-deletion state. xc 82/40. Vs the FRAMED pre-order baseline (33/4 · 98/24): probe within ONE (`dc_recur` = recursion depth, M-1b proper); xc gap 16 uncharacterized. **m4 NOT measured this rung; broad-336 not run; regen ×3 owed ×4 seats. ⚠ charter re-grep post-patch used a sloppy bare-`rbp` range (33 hits, range artifact suspected) — establishments were 0 pre-patch and the patch introduces no rbp by construction; re-verify with the establishment grep next seat.**

**NEXT:** (1) m4 both suites at `9eb9b4f3`; (2) xc 16-gap census vs framed baseline; (3) `dc_recur` + depth-reset hygiene → MECH M-1b; (4) re-delete audit: does any T-class site survive? census re-run + ratchet reseed.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 11-**FABLE** (Claude Fable 5 — LON ROUTED IN-CHAT: "DELETE ALL seven... now" — EXECUTED as SCRIP `458b276f`; charter number 0 both media; two de-arm variants of the LAST reader pair built, measured, reverted)

**⛔ ROUTING OF RECORD:** Lon, in-session, verbatim: *"You will go now and DELETE ALL seven RBP code generations spot that have been identified as NOT NEEDED. Keep the ones that are needed. DELETE ALL of them now."* — with the s10/s11 costs already reported to him in the same conversation. That is the owner routing the NOT-CONCURRENCY-SAFE window; MECH seat: the crater is re-opened BY DIRECTION, not by a seat freelancing. Exit remains `git revert 458b276f`.

**EXECUTED:** SCRIP **`458b276f`** = revert of `c4ef2176` = D-1+D-2+D-3 re-applied wholesale, one isolated commit, 73(+)/32(−), keepers (STATEMENT · FUNCTION · MATCH_BEGIN · FENCE1) untouched by construction. **CHARTER CHECK: blob-region `mov rbp,rsp` = 0, `[rbp` refs = 2 per blob → then the pair examined below.** Cost as twice-measured, reproduced this container: probe **26/11**, xc **69/53** (baseline before order: 33/4 · 98/24). corpus untouched.

**⭐ THE LAST BLOB rbp PAIR IS NAMED AND ITS DE-ARM IS MEASURED (new, both variants falsified):** the surviving `mov [rbp+32],rax` / `jmp [rbp+32]` per blob is **the IR_SUSPEND resume slot (`g_suspend_resume_slot`), armed on PAT$ blobs ONLY via the `g_resumable_callable_active` leak at emit.cpp:2443** — 11-CODE's "second defect", now tied to this family. (1) Blanket `!flat_pat` guard: xc 69→**73** but probe 26→**24** — breaks `dc_nest_bt`/`dc_sib_bt`, the deep-arrival blobs where the slot is load-bearing under their accidental pin. (2) Pin-aware guard `!(flat_pat && !emit_jmp_pin_rbp())`: probe stays 24 AND xc falls back to 69 — **the second pin route (emit_rec_pin/heap_fb_adopt) is invisible to emit_jmp_pin_rbp, so any emit-side pin predicate here is half-blind** — 11-CODE's two-routes finding, confirmed from the consumer side. Neither dominates by set → both reverted uncommitted; HEAD is exactly the ordered deletion. **LAW sharpened: de-arming the resume slot for blobs requires a pin oracle that sees BOTH routes, or (better) the slot rebased to `g_blob_ctx[0]+off` so pin state stops mattering — that is the smallest remaining edit between this state and green, and it is one slot, not the whole interior.**

**NEXT:** (1) rebase the resume-slot pair onto ctx[0] (store `[ctx0+32]`, dispatch `jmp [ctx0+32]`, both media, ZW-4 idiom) — single-cell nesting limit inherited, M-1b lifts it. (2) m4 + broad-336 at `458b276f`. (3) MECH M-1b unchanged as the depth repair. **Floors: this cursor sets NONE — the ordered state is the deliberate baseline.**

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 11-**ENV** (Claude Opus 5 — ⚠ TWO SEATS BOTH SELF-LABELLED s11 RAN CONCURRENTLY IN ONE CONTAINER; this is the environment/measurement seat, the code-bearing one is 11-CODE below — READ BOTH) — **m4 MEASURED AT LAST: 5 ONE-DIRECTIONAL MODE34 VIOLATIONS, AND THE RESTORATION CLAIM IS m3-ONLY FOR THREE PROGRAMS**

**⛔⭐ DISAMBIGUATION — DO NOT READ EITHER s11 ALONE.** Two Opus 5 seats ran against this one container/tree simultaneously and both wrote a cursor called "session 11". **11-CODE** (below, SCRIP `c4ef2176`) built s10's fix spec, falsified it, found the pin's second route, and reverted DEL-T1. **11-ENV** (this one) touched zero code and owns: the shared-container hazards, the independent restoration measurement, the DEPTH collision resolution, and the m4 column. Their numbers and mine were produced independently and **agree exactly** (probe 33/4 · crosscheck 98/24) — that agreement is a real cross-check, not a copy. Neither cursor supersedes the other; the next seat needs both.

**⭐⭐⭐ m4 MEASURED — OWED SINCE s8, NOW DISCHARGED (recipe copied verbatim from `test_broad_corpus_snobol4.sh` `compile_mode4`: `--compile` → `gcc -c` → link → **RUN THE BINARY**, per ENV-TRAP (b); runner `/home/claude/pt_probe_m4.sh`).** At SCRIP `c4ef2176`:
| suite | m3 | m4 |
|---|---|---|
| probe (37) | 33 PASS / 4 FAIL | **33 PASS / 3 FAIL / 1 SKIP** — failing SET identical to m3; `ab_nret_lvalue` SKIPs because `--compile` itself fails (m3 runs it and fails rc=1) |
| crosscheck/patterns (122) | 98 PASS / 24 FAIL | **93 PASS / 29 FAIL / 0 SKIP** |

**⛔⭐ FIVE MODE34-IDENTICAL VIOLATIONS, STRICTLY ONE-DIRECTIONAL — pass in m3, SIGSEGV(139) in m4, zero in the reverse direction:** `064_replace_multi_arm` · `121_pat_calc_op_dispatch` · `156_pat_cap_alt_abandon_pop` · `181_pat_arbno_defer_tail_stressors` · `182_pat_arbno_defer_windowed_leaf`. m4's failure set is a strict superset of m3's. ⭐ **CONSEQUENCE FOR 11-CODE's RESTORATION CLAIM: `121`, `181`, `182` are three of the six s7 net-negative programs that cursor lists as restored — they are restored IN m3 ONLY and still SEGV in m4.** (The other three, `070` `180` `183`, are clean in both.) The restoration is real and worth having; it is **not** two-medium green, and no watermark should be written as though it were. ⚠ **ATTRIBUTION UNMEASURED:** whether these five are pre-existing or introduced by the revert needs a pristine-parent (`a5533659`) build, which I did not take — a build contends for the single CPU with a live seat. Do not read this as caused by `c4ef2176`.

**⛔⭐ SECOND SHARED-CONTAINER HAZARD, MEASURED THE HARD WAY: `/tmp` IS SHARED AND THE GOAL'S OWN RUNNER HARDCODES IT.** `pt_probe.sh` writes `/tmp/pt_pass.txt` + `/tmp/pt_fail.txt` with no seat namespace. Mid-session the co-seat re-ran it and **overwrote my crosscheck m3 result set** (proven: file mtime `20:14:57`, content = the 4-line probe set, while my crosscheck run had written 24). I caught it because a by-set diff produced nonsense — probe programs on one side, crosscheck on the other — and the corrupted comparison was discarded, not published; the m3 set was re-measured into `/tmp/s11_*` and reproduced 98/24 exactly. **Had the two sets been from the same corpus the clobber would have been invisible and the diff plausible.** Recommend: seat-namespaced result paths in `pt_probe.sh` (`${SEAT:-$$}`), and treat any `/tmp` evidence file as volatile under a shared container — copy it the moment it is produced.

**⛔ SELF-CORRECTION TO MY OWN COMMIT `a0977e30` (11-CODE is right and I was wrong):** I wrote *"restores the 29 and lands one better than pre-deletion"*, comparing **absolute counts across containers** — 98/24 here against s10's 68/54/6 and 97/25/1. That is precisely what s7's law forbids (`599601e8` scored 258/253 in one container and 278/273 in another). **The absolute delta is INDICATIVE ONLY.** What transfers is the set-level claim, and 11-CODE states it correctly: every family s10 named broken at HEAD (`114` `115` `119` `124` `126` `127` `129` `130`) now passes, `128` still fails as its own separate defect. My §2 measurement stands as corroboration; my phrasing of it does not.

**⭐ DEPTH COLLISION RESOLVED (protocol requires the rebasing seat resolve and note it) — AND 11-CODE's MEASUREMENT INDEPENDENTLY CONFIRMS THE MANUAL'S READING.** MECH s36's *"DEPTH not blob-presence"* **subsumes** s10's *"every blob fails"*, which closes s10's *"second, unexplained defect"*: `A_stored_nocap` does not nest, D-2's NON-NESTING precondition holds, and it hangs anyway — that stops being a second defect once **depth means run-time backtrack depth, not syntactic nesting**. Manual, Ch.18 p.204–207: the matcher **pushes {alternative, cursor} on reaching any element that has an alternative** — the bead diagram stacks two entries for a flat, unnested two-element pattern — and Ch.9 p.123 records that SPITBOL applies **no Quickscan heuristics**, so alternatives are never pruned away. A stored pattern carrying `SPAN`/`BREAK`/alternation therefore reaches **depth > 1 with zero nesting**, exactly the witness split s10 measured (`VOWEL 'Y'` → 0 blobs, passes; every SPAN/BREAK/ARBNO/ANY/LEN variant → fails). ⭐ **THE SYNTHESIS THE TWO SEATS ADD UP TO:** 11-CODE measured that the rsp spelling fails because *"the res stub leaves rsp at the SUSPENSION FRONTIER, so `jmp [rsp+32]` reads at the wrong depth"*, and concluded a blob needs a base that is **neither rbp (charter) nor rsp (it moves)**. That is the same phenomenon the manual predicts from the language definition: **SPITBOL pattern matching IS a pushdown machine, so blob linkage must live at a depth-independent address by construction — no fixed offset from any depth-varying register can be correct, in either medium.** This is not a spelling preference; it is what Ch.18's algorithm requires. It is also precisely PT-4's *"a box a few bytes of its own"* and MECH M-1b's save-by-value. ⚠ Grounded analysis, not a runtime depth measurement — a strong hypothesis until M-1b's POP-MODEL check confirms or falsifies it.

**Watermark at close:** SCRIP `c4ef2176` **UNTOUCHED BY ME** (11-CODE's commit; I built from it and measured it) · corpus `f728c278` UNTOUCHED · `.github` this commit + `FINDING-2026-08-10c`. **NOT A GREEN RUNG. No ratchet move.** Census not re-run — and when it is, **T1 est. will RISE because the frames are back by design: reseed deliberately, do not read it as a ratchet trip.** broad-336 NOT run. regen ×3 NOT run (NOT-CONCURRENCY-SAFE, owed by three seats now).

**NEXT, in order:** (1) **the 5 m4 SEGVs need attribution** — build pristine `a5533659`, re-run m4 crosscheck, decide pre-existing vs introduced; cheap and it is the only thing standing between the revert and a two-medium green claim. (2) PT-4 PREREQ = the gate on everything (11-CODE's ranking, and I concur) — MECH's zone, **Lon routes**. (3) `128` own rung, unchanged since s8. (4) `g_resumable_callable_active` leak (11-CODE's second defect), routed. (5) broad-336 + regen ×3.

**ROUTING FOR LON:** PLAN.md still does not list this goal (`RULES.md` step 3 forbids me editing the table on routine handoff — **add it or retract it**) · the shared-tree amendments: **`git add -A` is unsafe**, use named paths + a `git status` tripwire at session OPEN, and **namespace `/tmp` evidence per seat** (`FINDING-2026-08-10c` §1a, §5) · PT-4 here-vs-MECH — evidence now says MECH, they are executing it · `bb_match_end.cpp` 12×r10 TIER-2 (unchanged) · the dead `:pat` line from s8 (unchanged).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 11-**CODE** (Claude Opus 5 — DEL-T1 REVERTED ON MEASUREMENT; s10's FIX SPEC BUILT AND FALSIFIED; the pin survived D-2 by TWO routes, not one)

**Watermark at close:** SCRIP **`c4ef2176`** (the revert; parent `a5533659`) · corpus **`f728c278`** (UNTOUCHED) · `.github` this commit. Probe m3 **33 PASS / 4 FAIL** · crosscheck/patterns m3 **98 / 24 / 1 hang**. **m4 NOT measured; broad-336 NOT run; regen ×3 NOT run** (NOT-CONCURRENCY-SAFE, still owed by two other seats — Lon routes). ⛔ **NOT A GREEN RUNG:** this is a restoration to a working compiler plus two findings, not a ladder advance. **Lon's charter is NOT discharged and is not claimed to be** — the frame code is back in the tree.

**⛔⭐ ROOT CAUSE CONFIRMED — AND s10's MECHANISM IS HALF RIGHT.** s10 inferred the defect from asm + predicate reads and marked the confirming experiment OWED; it is now run. `w_cap_stored` at HEAD: both PAT$ blobs emit `mov [rbp+32],rax` at α_attempt and `jmp [rbp+32]` at res — **the same slot, aliased** — with `mov rbp,rsp` = 0 in every blob, so `[rbp+32]` lands 32 bytes ABOVE main's frame base in CRT territory. **The pin survives D-2 by TWO INDEPENDENT ROUTES:** `proc_PAT$1` (`deep=1 pat=1`) via `flat_deep_arrival` — s10 predicted this one; `proc_PAT$0` (`deep=0 pat=1 rescall=1`) via **`emit_heap_fb_adopt()` reading `g_resumable_callable_active`**, which s10 never named and which **bypasses `emit_jmp_pin_rbp()` entirely** — `x86_fb_pinned()` is `emit_rec_pin()`, not `emit_jmp_pin_rbp()`. ⭐ **D-2's removal of `flat_pat` from the disjunction was an ABSTENTION where a SUPPRESSION was needed:** it stopped `flat_pat` asserting the pin but left the other disjuncts free to assert it FOR a pattern blob.

**⛔⭐ THE GOAL FILE'S OWN FIX SPEC IS FALSIFIED — BUILT, MEASURED WORSE, REVERTED (do not re-try this shape).** I built the total suppression (`flat_pat` vetoes at BOTH authorities; killswitch `SCRIP_PAT_UNPIN=0`). It did exactly what it was designed to do — **blob-region rbp refs 4 → 0**, aliasing gone, converted to `[rsp+32]` — and scored WORSE. Probe, same binary both arms, killswitch A/B: **unpin ON 23/14 · unpin OFF (= D-2/HEAD by the killswitch law) 26/11 — BY SET 3 BROKEN (`dc_nest_bt`, `dc_sib_bt`, `mv_valheld_cap`, all newly 139), 0 REPAIRED.** ⭐ **WHY, visible in the emitted code:** the α_body STORE is correct (`rsp==base` there), but the **β READ is not** — the res stub pops the cell base into `rax` and leaves **rsp at the SUSPENSION FRONTIER**, so `jmp [rsp+32]` reads at the wrong depth. Under the pin `[rbp+32]` was depth-immune and this could not bite. **The rbp path was ACCIDENTALLY surviving for some programs; the rsp path is SYSTEMATICALLY wrong.** That is the PT-4 PREREQ biting exactly where this file forecast it. ⭐ **LAW: a pattern blob needs a depth-immune base that is NEITHER rbp (Lon's directive) NOR rsp (it moves). Until one exists, both spellings are wrong somewhere — s10's "the defect is localized and small, so fix-forward is now viable" is retracted on measurement.**

**RESTORATION MEASURED (this container; runner semantics per ENV-TRAP (a)):** probe **26/11 → 33/4**, BY SET **7 repaired, 0 broken**. crosscheck/patterns **98/24/1**. ⚠ s10 recorded HEAD 68/54/6 and PRE-D-1 97/25/1 *in its own container* — cross-container absolutes do not transfer, so the ~30-program delta is INDICATIVE; **the SET-level claim is the evidence that does transfer: every family s10 named as broken at HEAD now PASSES** — `114` `115` `119` `124` `126` `127` `129` `130` — **plus s7's net-negative six** `070` `121` `180` `181` `182` `183`. **`128` still FAILS (139)** — exactly as s8 established, a SEPARATE defect wearing the family's crash signature; neither regressed nor repaired here.

**⛔ SECOND DEFECT FOUND, INDEPENDENT OF THIS GOAL, NOT FIXED:** `g_resumable_callable_active` leaks across graph emissions (`emit.cpp:3060`'s own comment already describes the leak; this session MEASURED it selecting a pinned base for a blob that is not deep-arrival). Needs its own rung and probably its own owner — it is a frame-base correctness bug reachable by any graph emitted after a resumable one.

**NEXT RUNG, in order:** (1) **the PT-4 PREREQ is now the gate on everything** — a per-blob depth-immune base (PT-4's own "a box a few bytes of its own"), i.e. interior readers addressing through `g_blob_ctx[0]` / a spine cell rather than either frame register; this is MECH's zone, **Lon routes before any edit**. (2) re-delete D-1..D-3 only AFTER (1) lands — the revert is on record and re-deleting is cheap. (3) `128` own MONITOR-FIRST rung (unchanged since s8). (4) the `g_resumable_callable_active` leak, routed. (5) m4 + broad-336 + regen ×3 all still OWED and unmeasured.



**Watermark at close:** SCRIP HEAD **`c7e085fd`** · corpus **`9fb2e019`** · `.github` this commit + `FINDING-2026-08-10b`. **RE-PROVEN POST-REBASE (protocol satisfied):** the RTCC seat landed 4 SCRIP / 2 corpus / 5 `.github` commits DURING this session (r9→r10 GVA claim, veneer hardening, a convicted SIGSEGV fix, artifact regen). I rebased onto `c7e085fd`, rebuilt, and re-ran everything: probe **24 PASS / 12 FAIL with the identical failing set**, all four scratch witnesses reproduce byte-identically, and both root-cause sites are unchanged (`g_blob_ctx[5]` still one global; `emit.cpp:2358` still keys on `flat_pat` alone). **The RTCC work does not touch this zone and the finding below stands at the current origin tip.** No code edit by me; SCRIP/corpus carry zero commits of mine. Tripwire (s7 protocol) clean at open and close: HEAD unmoved, zero dirty files across the build. **No push attempted — awaiting Lon's credential and the §5 routing.**

**⛔ READ FIRST — THE s9 WATERMARK HASHES DO NOT EXIST.** `33062a20`/`5e66bfa6`/`9f1436b4` are ABSENT from origin; the work landed rebased as **`ef8a3052`(D-3) / `1f96143c`(D-2) / `1af93e3a`(D-1)** over the parallel RTX-FUNC `799f2e76`. Second instance same day: corpus is at `73271e2e` (NOT the claimed `0f02731d`, and NOT untouched — another seat pushed a demo regen at 15:08), whose message cites SCRIP `5416ed56`, **also absent**. Consequence: the question "were the 15:08 demo artifacts generated by a D-1-broken compiler?" (D-1 landed 14:52, D-2 15:24) **is unanswerable from the commit record and stays OPEN**. This is now a recurring class — recommend cursors record the commit SUBJECT beside any hash, or write the hash only after the push returns.

**BASELINE REPRODUCES EXACTLY, COLD CONTAINER:** probe m3 **24 PASS / 12 FAIL** at `ef8a3052`, failing set == s9's 5 pre-existing + 7 casualties. Modes are two, and s9 does not separate them: **124 = HANG** (the five `w_cap_*`), **139 = SEGV** (`dc_recur`, `mv_arbno_callcap`). **D-3 re-confirmed independently:** neither `SCRIP_PAT_INLINE=0` nor `SCRIP_SCAN_OFF=1` moves any of the 7.

**⛔⭐ THE s9 FRONTIER LABEL IS WRONG ON THE CAPTURE HALF.** Four scratch witnesses, each pair identical in semantics and differing only inline-vs-stored: `A2_inline_nocap` 0 blobs → **PASS**; `A_stored_nocap` 18 blobs → **HANG**; `C2_inline_nested` 0 blobs → **PASS**; `C_stored_nested` 9 blobs → **SEGV**. `A_stored_nocap` carries **no `.`, no `$`, no `@`** and still hangs. Correlation with capture: none. With blob presence: 4/4. **Frontier restated: every PAT$ blob on the pass-thru path fails — non-nested ⇒ hang, nested ⇒ SEGV.** Capture witnesses were simply the ones already in `corpus/probe/`. Cheaper reproducers now exist (both capture-free, `C` = one recursion level, 5-char subject) — see FINDING §2; witnesses in `/tmp/ptdiag/`, deliberately NOT promoted (a known-fail `.ref` moves other seats' watermark — s5 precedent).

**⛔⭐ ROOT CAUSE, from D-2's own comment at `pattern_match.c:624`:** `g_blob_ctx[5]` is **one process-global cell** replacing a **per-activation** frame, and its stated soundness condition is *"NON-NESTING assumption … so a single cell suffices"*. The arming gate `emit.cpp:2358` keys on **`flat_pat` alone** — not invariance, not nesting, not defer-freedom — so `*LIST` (the TT_DEFER case the comment explicitly excludes) gets the single-cell protocol anyway and `mov rsp, ctx.base` (`emit.cpp:2740`) restores another activation's base. **Licensed scope ≠ emitted scope.** ⭐ This DEVIATES FROM THIS FILE'S OWN PT-4 DESIGN OF RECORD, which specifies *"a box-own linkage CELL **on the spine**"* — per-activation by construction. The repair direction was already written here; D-2 shipped a global instead. Manual concurrence: Ch.18 p.204 specifies a **pushdown stack** (push alternative+cursor per alternative, pop on fail) and Ch.9 p.123 says recursive patterns consume it — depth is semantically required, and depth is exactly what the global anchor dropped. **Lon's charter IS discharged regardless: `mov rbp, rsp` = 0 in every blob, both media, measured.**

**⛔ SELF-CORRECTION at close (see FINDING §7):** the single-global-cell mechanism explains the **139/SEGV** class only. `A_stored_nocap` does NOT nest, so D-2's own NON-NESTING precondition HOLDS for it, and it hangs regardless — **the 124/HANG class is a second, unexplained defect.** §2's blob⇔failure correlation is observation and stands; the §3 mechanism over-reached. Also unfinished: every simplification of `A_stored_nocap` I tried (one-level stored, two-level ref, two-level+literal) gets INLINED AWAY by PT-1/PT-3 to **0 blobs and passes** — so the blob's survival condition is narrower than "stored pattern" and I did not isolate it. **Open question for the next seat: what makes `A_stored_nocap` keep 18 blob defs when `PAT = V 'Y'` keeps zero?** (SPAN-vs-literal second element? the s6 `g_sno_snapref` / s7 `g_sno_encl` verdicts?) The D/E ladder is exhausted — do not re-run it as written.

**NEXT RUNG, in order:** (1) answer the §7 open question to get a minimal HANG witness, then treat hang and SEGV as two defects; (2) convert `g_blob_ctx` from process-global to a per-activation spine cell (PT-4's own wording) — **but this is MECH's stack-work zone; Lon routes before any edit**; (2) retarget witnesses to the capture-free pair — `w_cap_*` are not cheapest and their framing misled two sessions; (3) probe + broad-336 BY SET, m3 AND m4, same container both arms — the s7/s8 floors remain OWED and unverified; (4) regen ×3 still owed AND still entangled with the §0 provenance question.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 10 (Claude Opus 5 — S9'S DIAGNOSIS FALSIFIED: DEL-T1 broke EVERY ENTERED BLOB, not the capture family; BY SET 29 BROKEN / 0 REPAIRED; NO CODE CHANGED)

**Watermark at close:** SCRIP `c7e085fd` (UNTOUCHED — no code changed this session) · corpus `9fb2e019` (UNTOUCHED) · `.github` this commit + `FINDING-2026-08-10b`. Probe suite m3 **24/12 BY SET**, reproducing s9 exactly (RTCC's parallel landings do not perturb it). m4 NOT measured. broad-336 NOT run.

**⛔ HASH CORRECTION — the s9 cursor's SHAs DO NOT EXIST at origin.** Real: D-1 `1af93e3a` · D-2 `1f96143c` · D-3 `ef8a3052`. s9 recorded pre-rebase hashes; the s8 lesson recurred verbatim. Grep the MESSAGE. Three RTCC commits + a feature regen landed between D-3 and my open — **that seat is live; regen ×3 window is still not mine to take.**

**⛔⭐ THE INHERITED DIAGNOSIS IS FALSIFIED (measured, minimal pair, same container).** s9 names the frontier "capture + recursion families" and sends the next seat at interior `op_flat_disp` **for capture**. Wrong target: `w_nocap` — the `w_cap_stored` program with EVERY capture operator removed — HANGS identically (124). And the "passing" blob-bearing witness `pt_inline_1_full` passes only because it matches on `VOWEL`, not `PAT`: its 18 `proc_PAT` defs are **emitted but never entered**.

**Construct bisect, blob presence held constant (`PAT = VOWEL <X>`, subject `'AY'`), HEAD vs PRE-D-1 `930539c0`:** `VOWEL 'Y'` → 0 blobs, ok/ok · `LEN(1)` SEGV/ok · `ANY('Y')` SEGV/ok · `SPAN('Y')` HANG/ok · `BREAK('Z')` HANG/ok · `ARBNO('Y')` SEGV/ok · bare `PAT` HANG/ok · `PAT . N` HANG/**ok=`AY` (the sbl answer)**. **Every blob-bearing program fails at HEAD; every one was correct pre-D-1; the only survivor emits zero blobs.** ⭐ **The D-2/D-3 pass-thru glue does not work for ANY blob entry.** s9's "6 repaired" were repaired by no longer ENTERING blobs. `PT-4 PREREQ` (ARBNO/SPAN/BREAK K-conversion in blob context) was never discharged — DEL-T1 was executed ahead of its own stated prerequisite. Note `PAT . N` was ORACLE-GREEN pre-D-1: DEL-T1 regressed the exact behaviour s6+s7 spent two sessions establishing and promoting to `w_cap_stored.ref`.

**BLAST RADIUS — `crosscheck/patterns` (122), BY SET, both arms built here:** PRE-D-1 **97 PASS / 25 FAIL / 1 HANG** → HEAD **68 / 54 / 6**. **29 BROKEN, 0 REPAIRED.** Zero. Broken set dominated by stored-pattern / FENCE-via-var / JSON families (114, 115, 119, 124, 126, 127, 129, 130, …).

**Killswitches (both confirm D-3's bisect):** `SCRIP_PAT_INLINE=0` and `SCRIP_SCAN_OFF=1` each leave the hangs unchanged — scan machinery exonerated. DEL-T1 has NO killswitch; `git revert 1af93e3a` is the only undo and D-2/D-3 sit on it.

**⛔ ROUTING — LON'S CALL, and the evidence has changed it materially.** Fix-forward already ran a full session (s9) for 0 real repairs at 29-program cost. **(1) REVERT D-1+D-2+D-3, land the PT-4 PREREQ, then re-delete** — restores 29 programs now; interim cost is the blob prologue returns and the "never want to see that code again" directive is un-discharged. **(2) HOLD and fix forward** under a universally-broken blob path. ⭐ **Recommend (1):** the deletion is right as a destination but landed before the interior could receive it; the frame code is safe in git, re-deleting after the prereq is cheap, and nothing measured on a broken blob path is trustworthy.

**⭐⭐⭐ ROOT CAUSE FOUND (same session, MEASURED in emitted code) — it is an UNCONVERTED RBP READER PAIR, not `g_blob_ctx`.** Every PAT$ blob still emits its ω continuation as a store/load pair through `rbp`: `α_body: mov [rbp+K], rax` (rax = `lea ω`) and `β: jmp [rbp+K]`. Counted at HEAD: `b_len.s` 4 refs (PAT$0 @+32, PAT$1 @+48); `w_cap_stored.s` 4 refs with **both blobs on +32 — aliasing the same caller slot**. Pre-D-1 `rbp` was the blob's OWN frame so the spelling was legal; D-1 deleted that frame and D-2 made the blob adopt the caller's, so `[rbp+K]` now **smashes a live slot in the invoking MATCH_BEGIN frame** (a licensed keeper). Symptom follows what occupied the slot: SEGV (`LEN`/`ANY`/`ARBNO`) or HANG (`SPAN`/`BREAK`). ⛔ **This is this file's own CARVE-ERAD conviction realised verbatim — "never cut the region under unconverted readers."** Explains every observation at once: universal, capture-independent, fires on a single non-nested entry, correct pre-D-1.

**`g_blob_ctx` — verified as fact, REJECTED as cause.** It is confirmed a process-global (`pattern_match.c:624`) and α **does** publish correctly (rsp/γ/ω/scanflag/δ0 all stored — read in the asm). The conversion is INCOMPLETE, not misdesigned: wires are published into ctx but **β never reads them**, still jumping via `[rbp+K]`. Global-vs-per-activation is real debt that will bite on nesting, but cannot kill a single non-nested entry. ⚠ **MECHANISM IS INFERENCE, not runtime-measured:** `emit_jmp_pin_rbp()` (emit.h:630) is a four-way OR and D-2 removed only `flat_pat` — **necessary but not sufficient**, since `flat_deep_arrival` is classified independently at `emit.cpp:3059` and still fires. CONFIRMING EXPERIMENT OWED (instrument the predicate per-graph; I read asm + predicate, not the runtime flag).

**FIX SPEC:** `α_body` stops writing through `rbp`; `β` takes ω from the published ctx / a per-blob spine cell. Both media together (BOTH-MEDIUM MANDATORY); `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before the edit. The +32 aliasing proves a **per-blob** cell is required, not one shared slot — exactly PT-4's "a box a few bytes of its own." **This narrows the decision: the defect is localized and small, so fix-forward is now viable where s9's blind interior hunt was not; revert still restores 29 programs immediately and remains the safe default.**

**NEXT SESSION:** take Lon's routing FIRST — do not resume s9's "attack interior op_flat_disp for capture", it is aimed at a falsified target. If FIX-FORWARD: convert the ω-wire linkage per the FIX SPEC above, cheapest witness `b_len` (`PAT = VOWEL LEN(1)`, `'AY' ? PAT` — SEGV, no capture, no recursion, 4 rbp refs), then `b_span` for the HANG arm; confirm the `flat_deep_arrival` inference before editing. If REVERT: revert `ef8a3052`+`1f96143c`+`1af93e3a`, re-prove 97/25/1 on crosscheck/patterns BY SET, then land the PREREQ. Either way m4 is UNMEASURED for this family and gdb is absent in-container (no sudo; `apt-get install gdb` failed — arrange it at open).

**s10 HANDOFF ADDENDUM — ladder state + protocol corrections (⭐ = proposal, needs Lon's confirm):**
- **DEL-T1 ladder state (correcting the unchecked boxes above):** D-0 manifest NEVER RUN · D-1 `1af93e3a` / D-2 `1f96143c` / D-3 `ef8a3052` LANDED AND MEASURED BROKEN (29 BROKEN / 0 REPAIRED by set) · D-2 FIX-FORWARD is now concretely the ω-wire conversion per FIX SPEC above (small, both media, full-runway seat only) · D-3 gate + D-4 seal blocked behind it · revert path `git revert ef8a3052 1f96143c 1af93e3a`.
- ⭐ **PROPOSED BINDING RULE — PREREQ-GATES-DELETE:** a deletion rung whose own goal file names a PREREQ may not land D-1 until the prereq is discharged, or Lon waives it EXPLICITLY in the D-1 commit message. Root: DEL-T1 landed ahead of the PT-4 PREREQ this file had already written down, at 29-program cost. A delete-first charter inverts suppress-first, not prereq ordering.
- ⭐ **PROPOSED — POST-REBASE HASH DISCIPLINE:** cursor watermarks are written AFTER the end-of-session rebase, or record commit MESSAGES, never bare pre-rebase SHAs. s8 and s9 both shipped dead SHAs; s10 burned time re-deriving D-1/D-2/D-3 identities.
- **⚠⚠ PHANTOM RECURRENCE (3rd instance, s7 class):** `2f5e087d` · `34cf71af` · `164aae5f` self-minted on local main under the session identity during s10 — the third MID-SESSION, its content SELF-CORRECTING toward my in-flight measurement (convergent with, not identical to, the measured root cause; its verification claims UNVERIFIED). Treat all three as UNVERIFIED CONTENT per s7 law. Pushed WITH this marker rather than dropped: my verified commits (`a1a9a111`, `fa8b38fe`, this one) stack on them and end-of-context extraction risked a stranded handoff (the s19–s26 conviction); Lon may revert any of the three. ⭐ The mid-session reactivity strongly suggests an agentic process with repo access is live IN-CONTAINER during sessions — needs Lon-level environment investigation; s7 already exonerated hooks and Makefile.
- **Probe-suite blind spot:** the 36-probe suite barely exercises blob ENTRY (KS-OFF moved it by 1 while crosscheck/patterns moved by 29). The b_* constructions named in this cursor are the cheap entry witnesses; promotion is gen_probes/Lon territory.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 9 (Claude Opus 4.5 — DEL-T1 EXECUTED: D-1 delete landed FIRST as ordered, D-2 pass-thru fix-forward PARTIAL; the PT-4 PREREQ frontier is now MEASURED, not predicted)

**s9 ADDENDUM — D-3 `33062a20` (post-cursor, supersedes the killswitch paragraph below):** the α carve was LIFTED OUT of `scan_live` onto its own `blob_act` gate — SCRIP_SCAN_OFF again means only the retry blocks, and the FIRST HONEST BISECT ran: dc_recur 139/139, w_cap_stored 124/124, w_cap_ay 124/124 scan-on/scan-off — IDENTICAL failure with the retry blocks not emitted at all.  The D-2 scanhit/scanfail rewrite and the scan machinery are EXONERATED.  The remaining defect is INTERIOR: op_flat_disp for capture/recursion families in blob context (the PT-4 PREREQ proper).  Default-ON unchanged 24/12.  NEXT SESSION STARTS AT THE INTERIOR, NOT THE GLUE — cheapest witness w_cap_stored, deepest dc_recur.  ROUTING NEEDED FROM LON: (a) does the interior K-conversion execute under THIS goal or re-home to MECH (the goal file's original PT-4 coordination question, now live); (b) regen ×3 window — artifacts are stale by construction until the 7 go green or Lon routes an honest-broken regen; (c) claws5-match rc=0-wrong-output needs the monitor once interiors move.

**Watermark at close:** SCRIP HEAD `5e66bfa6` (D-2) on top of `9f1436b4` (D-1); corpus HEAD `0f02731d` (untouched — NO regen this session, see DEBT).  Lon directive verbatim: *"NO RBP frame for CONSTANT FOLDED patterns. Use flat pass-thru glue. I never want to see that code again."* — discharged: the compiler can no longer emit the `sub rsp,304 / mov [rsp+296],rbp / mov rbp,rsp` blob prologue in either medium.

**D-1 `9f1436b4` — THE DELETE (landed first, alone, breaking by design as ordered).**  Removed from the BLOB-GRANT arm in `emit.cpp`: `sub rsp,kt`, γ/ω wire stores `[kt-24]/[kt-16]`, caller-rbp save `[kt-8]`, `mov rbp,rsp` pin, SCANBASE fills `[kt-32]/[kt-40]` — TEXT snprintf and BINARY `ef_b*` twins together.  Verified: fresh `claws5-match` `proc_PAT$0_α` carries ZERO frame instructions.  One correction inside the same commit (amended): the first cut over-swallowed `emit_label_define_bb(&lbl_attempt)` → emit-time "unresolved forward reference"; restored.  Revert path is this commit alone.

**D-2 `5e66bfa6` — PASS-THRU GLUE (partial repair, honest numbers).**  Blob ADOPTS the caller's frame (rbp = invoking statement's MATCH_BEGIN frame, a licensed keeper, NEVER written in the blob).  Activation memory = pin-free cell carve; the depth-immune anchor is a new runtime cell `g_blob_ctx[5]` {base, γ wire, ω wire, scan flag, δ0} in `src/runtime/pattern_match.c` (+ `rt_blob_ctx_ptr()`, rt_anchor precedent).  α publishes it; scanhit/scanfail, CLASS D γ (record is now `{res, CTX.BASE}`), CLASS D ω (`rsp = ctx.base + kt`) and the β res-stub all read it; the res-stub REFILLS ctx from the suspended activation's carve header, which is the suspend/resume interleaving repair.  `flat_pat` removed from `emit_jmp_pin_rbp()` (emit.h:630) — **mandatory, not a tuning knob**: with the α pin gone, a pinned spelling addresses the INVOKER's frame.

**MEASURED, BY SET, same container, 36 probe witnesses m3:**
| rung | PASS | FAIL |
|---|---|---|
| pre-delete `7b4d310d` | 31 | 5 (pre-existing: ab_freturn, ab_nret_lvalue, ab_redefine, z4_arbno, z4_span) |
| D-1 only `9f1436b4` | 18 | 18 (13 casualties) |
| D-1+D-2 `5e66bfa6` | 24 | 12 (7 casualties; **6 repaired**) |

`claws5-match`: SIGABRT(134) under D-1 → **rc=0 under D-2 but output STILL DIFFERS from ref** (not a pass; counted as broken).

**THE 7 REMAINING CASUALTIES — the frontier, named:** `dc_recur`(139), `mv_arbno_callcap`(139), `w_cap_ay`/`w_cap_group`/`w_cap_novowel`/`w_cap_stored`/`w_cap_xxay`(124 hang).  Capture + recursion families.  This is exactly the **PT-4 PREREQ this goal file already predicted**: interior `op_flat_disp` correctness for the match family in blob context (ARBNO/SPAN/BREAK K-conversion).  It is now measured rather than forecast.  NOT closed, NOT claimed closed.

**DEFECT FOUND IN D-2's OWN PLACEMENT (first item of the next rung):** the pass-thru α carve sits INSIDE the `scan_live` block, so `SCRIP_SCAN_OFF=1` now suppresses ACTIVATION ESTABLISHMENT too — all 7 casualties report 139 under the killswitch, which is a CONFOUNDED signal and localizes nothing.  Activation establishment is not scan machinery.  Lift the carve out of that gate before using the killswitch to bisect anything.

**NEXT RUNG, in order:** (1) lift the α carve out of `scan_live` and re-run the killswitch bisect on the 7; (2) attack interior `op_flat_disp` for the capture/ARBNO family in blob context (the PT-4 PREREQ proper) — `w_cap_stored` is the cheapest witness, `dc_recur` the deepest; (3) re-measure BY SET against the table above; (4) broad-336 + regen ×3 once witnesses move.

**DEBT CARRIED (disclosed, not hidden):** broad-336 NOT run this session; regen ×3 NOT performed (corpus untouched) — artifacts on disk still show the pre-delete prologue and are now stale by construction; the census instrument was not re-run.  Deliberate: context ran out and regenerating 300+ artifacts from a knowingly-broken emitter would bury the measured signal above under noise.  Do regen ONLY after the 7 are green or Lon routes otherwise.  ⚠ carried forward, still unowned: `bb_match_end.cpp` 12×r10 TIER-2 gate fail; `128_pat_recursive_grammar` separate defect.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 5 (Claude Sonnet 4.6 — PT-0 oracle-confirm done; PT-4 is next; Lon routes PREREQ)

**Watermark at close:** xc318 269m3/48m3/3div (unchanged); RBP ratchet seeded=609 unseeded=0 GATE OK; SCRIP HEAD `87dfb964`; corpus HEAD `dee11d0e`.  **PT-0 + PT-1 + PT-2 + PT-3 complete.  PT-0 oracle-confirm DONE.**  (s5 correction: an earlier s5 cursor write recorded stale hashes `379eb630`/`e3ccea53` — s4 pushed in parallel mid-s5; STALE-ORIENTATION protocol absorbed it; hashes above are ground truth from handoff_status.)

**PT-0 oracle-confirm (s5):** x64 `sbl` cloned `/home/claude/x64/bin/sbl`. `pt_inline_1`, `pt_inline_1_hand`, `pt_inline_1_full` — sbl output matches committed `.ref` exactly; scrip m3 witnesses hit=A (m3+m4 both verified s4).  **PT-4 is first incomplete rung** — PREREQ: ARBNO/SPAN/BREAK K-conversion inside blob context; Lon routes whether PT-4 executes here or re-homes to MECH.  Read `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before any emit.cpp frame-arm edit.  ⚠ OBSERVED s5, pre-existing, NOT from PT (PT-2/3 touched only lower_snobol4.c): `test_gate_sno_pat_reg.sh` TIER-2 HARD FAIL — 12 r10 references in `src/templates/bb_match_end.cpp` (gate demands 0; REG-RO/r14-cursor law). Needs Lon routing to its owning goal.

**PT-2 session findings (this session):**  Implementation: (1) `int defer_cnt` added to `g_sno_fz` struct; (2) incremented in `sno_fz_mark_defer` for non-patproc *name consumers; (3) `sno_fz_is_dead_build(var_nm)` predicate (lookup by var name — for MKPAT chain gate); (4) `sno_fz_procname_is_dead(procnm)` predicate (lookup by PAT$N name — for patproc builder, which indexes by g_sno_pats[].name not var); (5) MKPAT chain gate at ~2065 skips to sJ for dead-build names; (6) patproc builder `continue`s for dead-build blobs.  Key discovery: MKPAT and patproc use different keys (var name vs PAT$N procname) — two separate lookup predicates required.  Verification: `pt_inline_1` and `pt_inline_1_hand` produce zero `proc_PAT` lines in mode-4; `pt_inline_1_full` suppresses PAT$0(VOWEL) but retains PAT$1(PAT) correctly (PAT's tree contains TT_VAR(VOWEL) → not inline-ok → kept); `claws5-match` proc_PAT count unchanged (ARBNO-bearing pattern, inline-ok gate fails → blob retained, correct per PT-2 scope); killswitch SCRIP_PAT_INLINE=0 reverts PT-1 inline → defer_cnt becomes 1 → PT-2 also does not suppress → blob retained → correct.  All three PT witnesses m3+m4: hit=A.  RBP ratchet GATE OK.  BY SET xc318 unchanged.  161_pat_defer_fn_nested_match --compile SEGV pre-existing (confirmed by killswitch-off same result).

**PT-3 session findings (this session):**  Implementation: in TT_DEFER arm, before calling `sno_fz_mark_defer`, check `sno_fz_tree(nm)` + `sno_pat_inline_ok` — same killswitch `SCRIP_PAT_INLINE`. Inline fires first so `defer_cnt` stays 0, PT-2 blob suppression also fires for `*name` of inline-ok invariant.  Same guards as PT-1: `!g_sno_in_patproc && g_sno_pat_match_ctx` (inside `sno_fz_tree`).  Recursive `*LIST` (ARBNO-bearing, not inline-ok) correctly stays DEFER.  Result: xc318 PASS 264→269 (5 new passes from deferred-invariant programs now resolving inline); FAIL 53→48; DIVERGE unchanged at 3.  Killswitch-off: `*VOWEL` falls back to DEFER path (blob path, pre-existing capture divergence produces `hit=`).  Recursive case `(42)` → `ok` correct.  T2 (live): demo corpus proc_PAT blobs 101→96 (5 eliminated by PT-2+PT-3); artifact census numbers still show baseline because census reads pre-regen artifacts — regen diff deletion-only confirmed (zero new proc_PAT lines).  Regen: benchmark/feature/demo artifacts updated and committed.

**PT-4 design (next session implements):** Surviving-blob pass-thru protocol — blobs still required (stitch segments T3, dynamic refs): α becomes PASS-THRU + a box-own linkage CELL on the spine; CLASS D record {res,rbp}→{res,pad}; scanfail loses `mov rsp,rbp`; ω absolute unwind dies.  PREREQ: ARBNO/SPAN/BREAK K-conversion inside blob context — COORDINATE WITH MECH, single authority; Lon routes whether this executes here or re-homes.

⚠ **FLAGGED ORACLE-DIVERGENCE (found s3, OUTSIDE PT scope, needs its own MONITOR-FIRST rung + sbl confirm):** stored-PAT whole-capture.  `PAT = VOWEL SPAN(CONS)`; `'GRAY' ? PAT . N` — the `.` attaches to the element it follows (manual pp.66-67), here the whole stored pattern, so N should be PAT's full match `AY`; scrip yields `A` (canon witness) or `` (w_pinline1 variant whose SPAN set includes vowels).  Direct-inline sibling `VOWEL SPAN(CONS) . N` (dot binds to SPAN only, p.78 — a DIFFERENT program) correctly yields `Y` both modes.  CLASS D blob capture path — dies structurally with PT-4/PT-6, but the divergence exists TODAY at killswitch-off, on the licensed-keeper side.  **sbl-CONFIRMED s5** — program `VOWEL = ANY('AEIOU'); PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ'); 'GRAY' ? PAT . N; OUTPUT = N`: sbl → `AY` (manual-derived expectation confirmed); scrip m3 at HEAD `87dfb964` → `GR` — note the scrip-side value has SHIFTED since the s3 flag (was `A`), so whatever moved between s3 and s4 HEADs changed the wrong answer without fixing it.  Divergence real; MONITOR-FIRST rung still owed; witness deliberately NOT promoted to corpus/probe (a known-fail .ref would move the probe-suite watermark other goals gate on — promote it inside the future rung that fixes it).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 6 (Claude Fable 5 — the FLAGGED stored-PAT divergence RESOLVED: it was Defect B, PT-2's own census hole, not a capture bug)

**Watermark at close:** SCRIP HEAD `599601e8` (post-rebase onto parallel RTX-FUNC-0 `029387d2`; pre-rebase `a53d3c6`) · corpus HEAD `10f5316` · `.github` this commit.  Broad-corpus parity (`test_broad_corpus_snobol4.sh`, 336-program set — supersedes the 318-era xc watermark): m3 PASS 249→**258**, m4 244→**253** (pristine-HEAD stash build vs fixed; only-in-FIXED fail-set **EMPTY** = 0 broken by set; the 9 repaired per mode are the pat_json/pat_regex/pat_calc/fence_alt_with_capture snapshot-consumer class).  Probe suites: m3 132·15xf·0xp·**4reg pre-rebase → 3reg post-rebase**, m4 131·16xf·0xp·**4reg pre-rebase → 3reg post-rebase** — the surviving trio is PRE-EXISTING (below; X12 healed by 029387d2).  RBP census Σest **609→618**: +9 = the 9 resurrected PAT$ blobs, one per repaired program — a coherent ledger; reseed is Lon's call (correctness reversal of wrong deletions); NOTE census crosscheck section is STALE VINTAGE (crosscheck regen not in the mandated ×3).  Scratch witnesses 6/6 oracle-clean m3+m4.

**RESOLVED — the s3-flagged divergence was TWO stacked defects; the live one (Defect B) is PT-2's, landed s4, and it is a MATCHING bug, not capture.**  Measured chain (gdb, m4): a bare TT_VAR member of a KEPT patproc can never inline (1293 `in_patproc` guard) ⇒ always lowers to a `$V<i>` snapshot that COPIES the source var's stage-2 value at MKPAT time — a live read PT-2's census cannot see: `defer_cnt` (mark_defer:962, itself in_patproc-guarded) counts only `*name` consumers, and the snapg arm (2351) credits the synthetic `$V` name, never the SOURCE var.  `is_dead_build(VOWEL)` fired at 2084 ⇒ stage-2 build suppressed ⇒ GVA slot `v=0x0` at match time (measured) ⇒ snapshot tag 0 ⇒ `c_rt_defer_close` literal arm matches the NULL STRING, returns cur_delta ⇒ element vacuous, SPAN alone matches from the raw entry cursor, capture window [site-δ : r14] yields GR/Y/XX.  KILLER: `'BCD' ? PAT` — sbl FAILED, pristine-HEAD scrip MATCHED:[BCD].  **KILLSWITCH-LAW GAP (routing item):** `SCRIP_PAT_INLINE=0` does NOT revert (measured invariant) — the verdict flows from the census, not the gated inline; PT-2 currently has no byte-identical revert switch.

**FIX (SCRIP `a53d3c6`, lower_snobol4.c only):** `g_sno_snapref` set + walker + pre-scan piggybacked on `sno_fz_build_table`'s stored-pattern arm (same guard as `seal_note`, BEFORE the invariance gate — the 2084 consult runs MID-LOOP, a `g_sno_pats` scan would race statement order); both verdicts (1204/1210) gain `&& !sno_fz_snapref(var)`; resets at 979 and the 2606 fragment path.  Walker skips capture TARGETS (`. N`/`$ N` c[1], `@N`) and TT_DEFER — `*name` is late-bound BY NAME with its own 1919 registration + g_spk latch; out of scope by design.  Sound over-approx: a snapref'd name stays live even if every referencing blob later dies.  **Measured-vs-inferred lesson on record:** v1 walked TT_DEFER, the suite showed 4 REGRESSION, and I attributed causation WITHOUT a pre-fix baseline run — the stash build then proved all four fail identically at pristine `87dfb964`.  The TT_DEFER skip stands on scope grounds alone; the false attribution is corrected in the code comment.

**Defect A (the s3 `A` answer) — reconstruction, labeled INFERENCE (depth-1 clone blocked bisect):** not reproducible at HEAD once builds live; MECH s34's STFH-DEFER-FIX most plausibly repaired the old γ-continuation capture defect the same day PT-2 introduced B — the s5 note "changed the wrong answer without fixing it" was two ships passing.  Its own MONITOR-FIRST rung is MOOT unless a bisect disagrees.

**Monitor finding → OPS-2 MON-CAP feed:** PARTICIPANTS="spl scr" diverges at step 2 on trace SHAPE (spl emits `VALUE VOWEL=UNKNOWN` for the pattern assignment; scr doesn't) — the instrument is blind to this whole class; diagnosis proceeded per RULES via sibling-asm-diff + gdb (w_cap_group vs w_cap_stored was the discriminating pair: grouped capture inline is CORRECT, the defect needed the blob).

**NEW FLAG — `*P`-family runaway, PRE-EXISTING at `87dfb964` (stash-proof):** D09/D11/H21 rc=139 (X12 HEALED by the parallel RTX-FUNC-0 `029387d2` — its cross-session body-jmp fix was exactly the NULL-jump; post-rebase suites 133·15·3 m3 / 132·16·3 m4) — ARBNO(`*P`) at statement level loops null-width (infinite `$` fire) then SIGSEGV; X12's header claims m4-clean but it now fails in m4 too (widened).  Baseline drift from an earlier 2026-08-09 landing (s33/s34-adjacent suspects); needs its own MONITOR-FIRST rung; suite baselines will show 4 REGRESSION until it lands.

**Witnesses (scratch, NOT promoted — probe-ID allocation is gen_probes territory; Lon routes promotion).  Verbatim for survival, 8-space indent, `END` col-1:**
```
w_cap_stored.sno:    VOWEL = ANY('AEIOU') / PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ') / 'GRAY' ? PAT . N / OUTPUT = N / END          → sbl AY
w_cap_novowel.sno:   VOWEL = ANY('AEIOU') / PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ') / 'BCD' ? PAT . N :F(NOPE) / OUTPUT = 'MATCHED:[' N ']' :(END) / NOPE OUTPUT = 'FAILED' / END   → sbl FAILED
variants: subject AY→AY · XXAY→AY · inline dot-on-SPAN→Y · parenthesized group→AY   (all six now oracle-clean m3+m4)
```

**Routing items for Lon (superset of s5):** PT-4 here-vs-MECH (unchanged) · bb_match_end.cpp 12×r10 TIER-2 fail (unchanged) · PT-2 killswitch-law gap (new) · `*P` runaway rung ownership (new) · ratchet reseed 618 (new) · witness promotion (new).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 7 (Claude Fable 5 — PT-2 killswitch-law CLOSED; the switch then measured the LADDER NET-NEGATIVE (trio + 11 broad = PT-owned, not MECH); naive repair tried+REVERTED, PT-2b specced; PHANTOM AUTO-COMMIT INCIDENT contained + disclosed; PT-4 deferred behind MECH)

**Watermark at close:** SCRIP HEAD `9d451c6` (23189c2 gate + feature regen) · corpus HEAD `0f02731` (bench/demo regen + witness promotion) · probe suites default-ON m3 **133·15xf·0xp·3reg** / m4 **132·16xf·0xp·3reg** BY SET (trio = D09/D11/H21 — **LADDER-CAUSED, not pre-existing**; see ROUTING SELF-CORRECTION below) · KS-OFF m3 **136·15xf·0reg** / m4 **135·16xf·0reg** (the honest floor) · RBP ratchet **GATE OK seeded=383 unseeded=0** · census Σest **557** (table above refreshed, dated, at named HEADs).

**⚠⚠ INCIDENT — PHANTOM AUTO-COMMITS DURING FIRST BUILD (mechanism NOT located; read before trusting any local commit):** at fresh clone `599601e8`, during the first `make` window (reflog: reset 01:38:46 · reset 01:42:21 · commit 01:44:26 · reset · commit 01:45:07), two commits self-minted on local main under the session-configured identity: `e38a9ec` "feature x86 .s artifacts: regen" (11 files 5607+/5713−) and `c8e98ce` "PT-2 killswitch-law (s7)" — the latter carrying the correct two-line gate **plus a commit message claiming stash-build/broad-336/md5/regen-×3 verification that NEVER RAN in this container** (fabricated verification record; FACT-RULE violation as content).  Forensics: no git hooks, no Makefile git verbs (incl. `make -n` dry-run), message strings absent from the entire tree, `stable_semi` = harmless Icon binary; a PATH-shim rebuild logged ZERO git invocations and no recurrence (shim bypassable by absolute-path exec — weak negative).  RESPONSE: `git reset --hard 599601e8` (dropped SHAs above, reflog-recoverable for Lon's inspection), gate re-implemented as a reviewed edit, every claim re-measured in-container, and a TRIPWIRE (`rev-parse HEAD` + `status --short|wc -l` after every build/script) held clean for the rest of the session.  `e38a9ec` was later CONTENT-VALIDATED as this compiler's legitimate owed refresh — my deliberate `util_regen_feature` run produced tree-identical artifacts (`git diff e38a9ec 9d451c6` = the 2-line gate only).  The code payload of `c8e98ce` matched the fix this cursor's own s6 text specified — context-aware content, unprompted landing.  **Next sessions: adopt the tripwire; treat any commit you did not knowingly author as UNVERIFIED CONTENT regardless of how plausible its message reads; nothing pushes without Lon regardless.**

**PT-2 KILLSWITCH-LAW CLOSED (SCRIP `23189c2`, my authorship, re-verified from pristine):** measured at clean `599601e8` first — KS-OFF was a THIRD BROKEN CONFIGURATION, not a revert: `pt_inline_1` KS-OFF emitted **proc_PAT defs=0 AND m3 output DIFF vs its own committed .ref** (inline off + blob suppressed = reference falls to a blob that does not exist; strictly worse than s6's structural framing).  Fix = cached-static `SCRIP_PAT_INLINE` guard (PT-1/PT-3 idiom) as first line of BOTH verdict predicates `sno_fz_is_dead_build`/`sno_fz_procname_is_dead` — KS-OFF ⇒ verdicts always false ⇒ suppression fully reverts; both consult sites (MKPAT skip, builder continue) covered.  Verified: KS-OFF defs 0→**9**, m3 AND m4 end-to-end == ref; default-ON no-op PROVEN — `pt_inline_1{,_hand,_full}` mode-4 `.s` byte-identical (`cmp`) to pristine-HEAD anchors; w_cap six sbl-parity BOTH switch states; suites BY SET unchanged (above); 200-col compliant.

**RESEED + PROMOTION DONE:** census re-measured Σest **557** (both 609 seed and s6's 618 obsolete — see table header).  Six w_cap witnesses PROMOTED `corpus/probe/` (`0f02731`) with sbl `.ref`s, plain-named beside `pt_inline_1` precedent — suite-ID allocation stays gen_probes/Lon.  Values as s6 predicted: stored→AY · novowel→FAILED · AY→AY · XXAY→AY · inline-dot→Y · group→AY, all m3+m4.

**⛔ ROUTING SELF-CORRECTION (same session, measured after the fact — the earlier s7 call below was WRONG):** I first routed D09/D11/H21 to MECH as their Bug 2 on the strength of s6's "pre-existing" label. Then the working killswitch — which THIS rung created — made the ladder-vs-legacy experiment possible for the first time, and it says otherwise. **On one binary (`9d451c6`), `SCRIP_PAT_INLINE=0` clears the trio outright: probe suite m3 136·15xf·0xp·**0 REGRESSION** / m4 135·16xf·0xp·**0 REGRESSION**, trio outputs == refs.** Why s6 could not have seen this: its stash comparison was fix-vs-no-fix with the ladder ON IN BOTH ARMS, so "pre-existing at `87dfb964`" only ever meant "not introduced by the s6 snapref fix" — never "not caused by PT-1/2/3". Distinct claims; the stronger one was never tested because before this rung KS-OFF did not revert PT-2. **MECH's Bug 2 (r12 leak, ~699K-iter threshold, gdb-measured) is not thereby refuted — it is a real defect on a different scale, and these programs do not reach 699K iterations. Treat as two defects; PT owns this one.**

**⛔⭐ FINDING — THE LADDER IS NET-NEGATIVE ON THE BROAD CORPUS (measured, oracle-anchored, same binary both arms):** broad-336 `test_broad_corpus_snobol4.sh` at `9d451c6`: default-ON **m3 278/58 · m4 273/57+6skip**; KS-OFF **m3 284/52 · m4 278/52**. BY SET, default-ON vs legacy: **6 programs fail in m3 and 5 in m4 that legacy passes; ZERO that legacy fails and the ladder passes.** Family is uniform — `070_pat_arbno_star_var_digits` · `121_pat_calc_op_dispatch` · `128_pat_recursive_grammar_right_rec` · `180/181/182/183_pat_arbno_defer_*`. Conviction on two, oracle-anchored: `180` sbl+ref = "even ok / odd rejected ok", ladder-ON = **empty output + SIGSEGV**, KS-OFF = correct; `070` sbl+ref = `123`, ladder-ON = **empty + SIGSEGV**, KS-OFF = correct. Same crash signature as the trio. **Lead for the repair rung:** the family is ARBNO/DEFER, which `sno_pat_inline_ok` is supposed to EXCLUDE (PT-1 charter) — so either the shape gate leaks, or PT-3's `*name` arm admits a graph whose interior is ARBNO-bearing. **⛔ LON'S DECISION, NOT MINE:** whether to flip the ladder default OFF pending repair (safe — KS-OFF is now a true byte-level revert and scores strictly better on every instrument I have) or hold default-ON and open a PT-2b/PT-3b repair rung. I did NOT flip it: other goals gate their watermarks on this default, and moving it silently would shift every seat's baseline.

**ROUTING DECISIONS (Lon: "all your choices," s7):** **PT-4 DEFERRED behind MECH** — their s30–s34 cursors show active landings in the exact frames PT-4 deletes (capture-δ anchors `[rbp−64−16k]`, stfh carve, MATCH protocol churn; next = M-2(a) sealed-DEFER FRAMED); re-open when MECH lands M-2(a) + ARBNO/SPAN/BREAK K-conversion, reading ARCH-ICON + TEMPLATE-RULES first.  ~~**`*P` trio D09/D11/H21 = MECH Bug 2** — NOT a separate rung here.~~ **SUPERSEDED — see ROUTING SELF-CORRECTION above; PT owns the trio.**  **bb_match_end 12×r10 TIER-2:** MECH's named next edit USES r10 (sentinel walk) — gate-vs-mechanism reconciliation routed to MECH + the REG-law goal jointly; PT does not touch it.  **REGISTER-LAYOUT.md rbp row** stale vs ARCH-ICON — still open, doc-only.

**BROAD-336 A/B — THE PROOF THE PHANTOM COMMIT ONLY CLAIMED (built for real this session):** worktree at pristine `599601e8`, full build, same container, same corpus. Default-ON vs pristine: **m3 278/58 and m4 273/57+6skip on BOTH**, fail sets symmetric-difference **EMPTY — 0 broken, 0 repaired**. So the gate is a proven no-op at 336-program scale, not merely on three witnesses (consistent with inspection: the guard falls through whenever `SCRIP_PAT_INLINE` is unset). KS-OFF vs pristine KS-OFF: **11 programs repaired by set, 0 broken** — the revert path works at scale, where before this rung it was a third broken configuration. **⚠ CROSS-CONTAINER WATERMARK CAVEAT (MECH's law, now demonstrated on PT's own instrument):** the SAME commit `599601e8` scores 258/253 in s6's container and 278/273 in mine. Broad-336 absolute counts DO NOT TRANSFER between containers — only same-container A/B by set is evidence. Any future cursor comparing raw broad counts across sessions is comparing noise.

**⛔⭐ THE REPAIR IS NOT A ONE-LINER — ATTEMPTED, MEASURED WORSE, REVERTED (do not re-try this shape):** the obvious fix is to gate both inline arms on the EXISTING `sno_in_arbno` counter (line 1050; it wraps the ARBNO body walk exactly — `sno_in_arbno++` → `sno_pat_node(t->c[0])` → `--` at 1382/1384 — and there is direct precedent for gating on it at 1296 FENCE-NONSPINE and 1542/1561). I built it (`_p3 && !sno_in_arbno`, `_pi && !sno_in_arbno`) and measured: **probe suite 131·15xf·0xp·5 REGRESSION m3 / 130·16xf·5 REGRESSION m4 — WORSE than the 133/132·3 it was meant to repair**, and `070` went from SIGSEGV to `fail` (ref `123`), `180` to `even FAIL` (ref `even ok`). **It converts loud crashes into SILENT WRONG ANSWERS. Reverted; tree is at the measured `9d451c6` state.** ⭐ **WHY — THE LOCKSTEP INVARIANT (this is the real finding):** `sno_pat_inline_ok` is consulted from TWO sites with DIFFERENT context available — the statement-level suppression verdicts (`sno_fz_is_dead_build` / `sno_fz_procname_is_dead`, which have NO enclosure context) and the pattern-walk inline arms (which do). Making only the walk context-aware breaks their agreement and RE-CREATES DEFECT B exactly: the verdict still sees a foldable `ANY(...)`/`LEN(2)`, judges the blob a dead store and deletes it; the guard then declines the inline; the reference falls to a blob that no longer exists. **LAW for this goal: the suppression verdict and the inline decision must return the same answer for the same name, always. Any new exclusion must be a NAME-LEVEL property both sites can see.**

**⭐ DESIGNED REPAIR FOR PT-2b (next session; shape follows the s6 `g_sno_snapref` precedent one-to-one):** pre-scan for enclosure-hostile names. Piggyback `sno_fz_build_table`'s existing pre-pass (the same hook s6 used, and for the same reason — a mid-loop consult races statement order); walk every match statement's pattern tree; record into a `g_sno_encl_hostile` set every `TT_VAR`/`TT_DEFER` name occurring inside a `TT_ARBNO` body — and inside `TT_FENCE`, since **121/128 are the FENCE variant of the identical leak** (`128`: `cmds = *unit FENCE(*cmds | eps)` — `unit` is inline-ok, gets inlined into a FENCE body, an open class). Then ALL FOUR consult sites (2 verdicts + 2 inline arms) test `&& !sno_encl_hostile(nm)` and stay in lockstep. Over-approximate by design: hostile at any one site ⇒ hostile everywhere (sound, and exactly the snapref ruling). **EXIT GATE — the crash class is loud but its naive repair is silent, so rc==0 is NOT a pass: every witness must be diffed against its `.ref`/sbl output, and the KS-OFF floor (136/135, 0 REGRESSION) is the target the default-ON numbers must MEET, not merely approach.**

**NEXT (re-ordered by this session's findings):** **(1) PT-2b ENCLOSURE-HOSTILE PRE-SCAN is now the top rung** — spec above, diagnosis complete: the shape gate walks the STORED tree while the hazard lives in the USE-SITE enclosure (ARBNO body, FENCE body). Do NOT re-attempt the `sno_in_arbno` one-liner (measured worse, reverted). Oracle refs exist for every witness named above. **(2)** PT-4 remains blocked on MECH (check their cursor at open). **(3)** PT-5 likely MECH-executed. **Floors from here:** probe suite default-ON ≥ 133/15/0 · 132/16/0 BY SET *and* KS-OFF ≥ 136/15/0 · 135/16/0 with 0 REGRESSION (the KS-OFF floor is the more honest one and should ratchet the default-ON floor up to meet it as the repair lands); Σest ratchets DOWN from 557; the killswitch matrix (ON no-op by md5 + broad-336 empty symmetric difference · OFF full revert) is part of every PT rung's exit gate from now on.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 8 (Claude Opus 5 — PT-2b IMPLEMENTED AND 9-OF-10 GREEN; the first attempt failed on a DEAD PRE-EXISTING LINE, not on the design; 128 is a DIFFERENT DEFECT and s7's reading of it is wrong)

**Watermark at close:** SCRIP `7b4d310d` (rebased over 1 parallel commit at push; pre-rebase hash was a0db98cd — grep the commit MESSAGE, not the hash) **push authorized s8-close (Lon); exit-gate floors still OWED** (parent `45de80bf` = origin at open, 3 RTX-FUNC commits past s7's `9d451c6`, linear descent) · corpus `0f02731d` (untouched) · `.github` this commit. **⛔ NOT A GREEN RUNG — the exit gate was NOT run:** probe suite and broad-336 A/B were never executed (context exhausted), so the s7 floors (default-ON ≥133/15/0 · 132/16/0; KS-OFF ≥136/15/0 · 135/16/0) are UNVERIFIED. m4 not measured for the family. Treat `a0db98cd` as a proven mechanism with an unproven blast radius.

**PT-2b LANDED (m3, oracle-anchored, this container):** repaired `070` `121` `180` `181` `182` `183` `D09` `D11` `H21` — all were rc139 SIGSEGV under the ladder, all now byte-equal to sbl. **STILL BROKEN: `128`.** Killswitch law intact: `SCRIP_PAT_INLINE=0` still fully reverts — `pt_inline_1{,_hand,_full}` + `w_cap_{stored,novowel,group}` all == `.ref` in BOTH switch states. Mechanism is the s6 `g_sno_snapref` shape one-to-one: `g_sno_encl` name set + `sno_encl_scan` + `sno_encl_hostile`, consulted at ALL FOUR sites (both dead-build verdicts, both inline arms) — s7's LOCKSTEP LAW holds by construction, name-level at every site.

**⛔⭐ ROOT CAUSE OF THE FIRST FAILED ATTEMPT — A DEAD LINE THAT PREDATES PT (file-trace MEASURED, not inferred; this is the session's most reusable finding):** PT-2b built exactly to s7's spec repaired NOTHING. Trace (`/tmp/pt2b.log`, fopen-per-call so it survives redirection and the SIGSEGV) showed `sno_encl_add` never once called. Cause: **`sfind_expr(s, ":pat")` returns NULL for EVERY statement** — the match pattern does not hang from that key. Therefore the PRE-EXISTING line beside my hook, `if (pat) sno_fz_scan(pat);` in `sno_fz_build_table`, **IS ALSO DEAD ON THIS PATH** and has been. Not introduced by PT; inherited. Anything that ever believed fz_scan saw match patterns should be re-checked. FIX: hook moved to `sno_encl_scan(s)` over the WHOLE statement tree — sound over-approximation, reaches the pattern whatever key it hangs from. ⭐ LESSON: an inline arm that "should" fire and doesn't is a REACHABILITY question first; instrument the mark site before touching the predicate.

**⛔ CORRECTION TO S7 ON `128` — IT IS NOT AN ENCLOSURE LEAK:** s7 records `128` as `unit` being "inlined into a FENCE body". Read the source: in `cmds = *unit FENCE(*cmds | eps)` the `*unit` is a **SIBLING of the FENCE, not inside its body** — enclosure-marking cannot reach it and no tuning of the hostile set will. Confirmed empirically: I wired the `sno_is_fence()` UMBRELLA (line 848 — `sno_pat_eff_kind(t)==TT_FENCE`, covers both fence forms; a raw `t->t==TT_FENCE` test misses `FENCE(P)`, worth knowing independently) and `128` still fails. **`128` is a SEPARATE DEFECT wearing the family's crash signature. It needs its own MONITOR-FIRST rung — do not spend another session tuning PT-2b at it.**

**NEXT SESSION, IN ORDER:** (1) run probe suite + broad-336 A/B against `a0db98cd` BY SET, same container both arms (cross-container absolute counts are noise — s7's law, re-confirmed: `599601e8` scored 258/253 in s6 and 278/273 in s7); the KS-OFF floor is the target to MEET, not approach. (2) regen ×3 OWED (lower touched) — SHARED-FILE (merge, do not wait), Lon routes; note `22a61ccf` from the RTX-FUNC seat ALSO declares regen ×3 owed, so the window covers both. (3) `128` own rung. (4) PT-4 still blocked behind MECH — check their cursor at open.

**ENVIRONMENT TRAPS COSTING TIME THIS SESSION (fix once, benefit every seat):** (a) **`070_pat_arbno_star_var_digits.ref` is MISSING ITS TRAILING NEWLINE** — sbl and scrip both emit `123\n`, the ref holds `123`; a naive `diff` gives a FALSE FAIL. The runner is immune because it compares `$(cat ref)` vs `$(cmd)` (command substitution strips both). Any ad-hoc sweep MUST use the runner's semantics or it will convict correct output. (b) **mode-4 is compile→gcc-link→RUN THE BINARY**; `./scrip --compile f.sno` alone captures COMPILER output — an ad-hoc m4 column built that way is void (mine was; discarded). (c) clone `--unshallow` at session start — depth-1 blocked s6's bisect and would have blocked mine. (d) `corpus/snobol4corpus` is a symlink to a nonexistent `/home/claude/snobol4corpus`.

**ROUTING ITEMS FOR LON (carried + new):** PT-4 here-vs-MECH (unchanged) · `bb_match_end.cpp` 12×r10 TIER-2 (unchanged, MECH+REG-law) · ladder default ON-vs-OFF — **s7's decision is now materially different: with PT-2b the default-ON arm repairs 9 of the 10, so "flip default OFF" is no longer clearly the safe choice; recommend deciding AFTER the suites run** · `128` rung ownership (new) · the dead `:pat` line above (new, may belong to a lowering goal) · ratchet reseed (Σest 557 unchanged this session; no census re-run).

## LADDER DEL-T5 — DELETE-FIRST eradication (Lon-directed, s8 close): pick ONE class, DELETE the emitter, fix forward
**Charter (Lon, s8):** "Pick one and delete it. Then go fix the code." This ladder INVERTS suppress-first BY OWNER DIRECTION, for T5 only. ⛔ Revert path is `git revert` of the D-1 commit — the killswitch does NOT cover deletions, so D-1 must be ONE isolated commit touching nothing else.
**WHY T5 and not T1/T2 (binding):** T1/T2 blob frames are the LIVE landing path for PT-2b hostile names and for `128` — deleting them un-repairs 9 oracle-green programs and orphans the 10th. T5 (`emit_rec_pin()` mains) is the narrowest surface: one emitter, remainder characterized as data-reader mains, prior art s26b 464→252, instrument `test_census_rbp_frames.sh` already counts it.
- [ ] **D-0 MONITOR-FIRST** — census baseline at HEAD; MANIFEST of every corpus program still emitting a rec_pin frame (expect: data-reader mains); commit manifest + stdin fixtures + sbl oracle refs. No code.
- [ ] **D-1 DELETE** — physically remove the `emit_rec_pin()` frame arm (push rbp / mov rbp,rsp emission). One commit, no predicate, no env gate. Expect manifest breakage; rc==0 is not a pass.
- [ ] **D-2 FIX-FORWARD** — make data-reader mains correct FRAMELESS: rsp-relative locals, call-alignment discipline, no rbp anchor. Loop until manifest == sbl byte-for-byte.
- [ ] **D-3 GATE** — probe + broad-336, m3 AND m4 (m4 = compile→link→RUN), BY SET, both arms, same container. Census: T5 establishments → 0, keepers unchanged. This gate ALSO discharges the still-owed PT-2b floors (debts stack; one run settles both).
- [ ] **D-4 SEAL** — ratchet becomes standing gate `T5==0`; regen ×3 (SHARED-FILE (merge, do not wait); stacks with regen already owed by s8 + RTX-FUNC); cursor move.
**AMENDMENT (s8, binding on T1's future ladder):** PT-2b made hostile-name invariant blobs KEEPERS-BY-NECESSITY — "T-class → 0" is unreachable for T1 as currently scoped. Before any T1 deletion ladder: either a safe-enclosure-inlining rung lands, or the license set grows to cover hostile-name blobs. Re-scope first, delete second.
**Ordering:** D-0 may start immediately in a fresh session. D-1 only with FULL runway — the fix loop (D-2) is the cost center, and a deletion landed at end-of-context is a broken tree by construction.

### DEL-T5 → DEL-T1 CORRECTION (Lon, s8 final word): the pick was already made — INVARIANT patterns
**Lon:** "Remove the RBP processing for CONSTANT FOLDED patterns, i.e. invariant patterns." Target = **T1∪T2** (same blobs, two access paths). DEL-T5 rung structure (D-0..D-4) carries over, retargeted.
**Scope precision (what makes this survivable):** DELETE the FRAME EMISSION for invariant-pattern blobs — the BLOBS THEMSELVES survive wherever suppression can't reach (PT-2b hostile names, snapref'd stores, variant-referenced segments) and become **PASS-THRU glue**: adopt the caller's frame and γ/ω wires, flat jumps, zero rbp writes. **D-2 fix-forward = PT-4 executed under fire.** The MATCH_BEGIN statement frame (licensed) is the anchor those blobs inherit.
**Retraction of the s8 T1 amendment's strong form:** hostile-name BLOBS are keepers-by-necessity; their FRAMES are not. "T1 → 0" is reachable again — via linkage change, not exemption.
**⛔ ROUTING (Lon only, BEFORE D-1):** PT-4 is deferred-BLOCKED behind GOAL-SN4-ZETA-MECH, which owns the stack-work cursor and is actively landing in these exact frames. Delete-first T1 lands in MECH's zone. Coordinate or accept the collision explicitly — do not let a fresh seat discover it mid-D-2.
**D-0 manifest (retargeted):** every invariant blob still EMITTED post-PT-2b (hostile/snapref/variant-referenced) + every corpus program exercising one; sbl refs; census split before/after. D-1 remains ONE isolated commit (git revert is the only undo). D-1/D-2 require a full-runway seat — the fix loop is the cost center.
