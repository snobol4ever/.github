# ⛔⭐⭐⭐ GOAL-PASSTHRU-RBP-ERAD — FLAT PASS-THRU everywhere it should be; RBP frames counted by census, deleted by rung

**Protocol: `RULES.md` governs.  `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before ANY template/encoder edit.  MONITOR-FIRST for every oracle divergence.  Killswitch law per rung (byte-identical revert, md5-verified; delete label and code in the same slice).  Cross-goal single-authority: `GOAL-SN4-ZETA-MECH.md` owns the stack-work live cursor and the glue-kind/mechanism design — any rung here that touches emit.cpp frame arms re-checks MECH's cursor first and re-homes on Lon's routing; `GOAL-SNOBOL4-BB.md` LADDER PB owns stitch semantics; `GOAL-RTCC` owns register claims.**

⭐ **CHARTER (Lon 2026-08-09, verbatim):** *"I know categorically, undeniably, that a constant folded pattern does NOT need an RBP. … delete that code that generated that mess and replace it with code that uses proper PASS-THRU GLUE as linkage, NOT stack frame linkage. … DO NOT create an RBP frame for constant folded patterns."* — and, same day: *"Plan the entire code modification in an incremental sequence to getting a FLAT PASS THRU everywhere is should be. Count how many RBP frames are being created through the product. Let's make a list to target for deletion. Starting with RBP being used for INVARIANT PATTERNS."*

## DESIGN OF RECORD

- **Exactly three glue kinds** (MECH:253): **ONE-SHOT** (`add rsp,K; jmp pred_β`) · **PASS-THRU** (`bb_glue_pass_wires`: lea rcx,γ + lea rdx,ω + jmp rax — transfer box adopts caller wires, ZERO frame) · **FRAMED** (`bb_glue_framed_enter/leave`, THE ONLY RBP WRITER).
- **Licensed frame census** = **{STATEMENT · FUNCTION · MATCH_BEGIN · FENCE1}** (ARBNO-LON refinement, 08-06; FUNCTION = the AB activation frame, Lon verbatim confirm: the manual p.103 pushdown made literal).  **Every other RBP establishment in the product is a deletion target.**
- **The site side is already correct** (ZD-PATREF, emit.cpp:1935): `bb_match_defer` enters pattern blobs via `bb_glue_pass_wires`, K=0 transfer citizen.  The debt is **blob-side**: `emit_jmp_entry_for_patproc` (emit.cpp:2914/2925) routes every PAT$ blob onto BLOB-GRANT (emit.cpp:2362 — `sub rsp,kt` + wire adoption + **rbp pin**, kt=(48+jcon_value_region+15)&~15; claws5-match: 304) with the CLASS D exit protocol (γ {res,rbp} resume record, scanfail retry `mov rsp,rbp`, ω absolute unwind `lea rsp,[rbp+kt]`) and the legacy in-blob ARBNO per-iteration rbp rebase chain.
- **Statement-inline is the proof-of-possible**: the SAME constructs (ARBNO-LON frameless, ALT, SPAN/BREAK, captures) already run oracle-green in statement context under the licensed frames only.  An INVARIANT pattern's structure is compile-time known — its reference can therefore be pure wiring (pass-thru), its elements riding the statement regime, and the frame question evaporates for it.

## THE CENSUS (measured 2026-08-09 at corpus HEAD `f46d3ebe`, demo/benchmarks regen'd same day; instrument = `SCRIP/scripts/test_census_rbp_frames.sh`; `push rbp` inside PAT-BLOB regions includes CLASS D γ resume-record pushes — protocol shape, not establishment, dies with the blob; crosscheck/patterns regen cadence unaudited — indicative only)

| corpus (.s) | files | blob-bearing | PAT$ blobs | est `mov rbp,rsp` PAT-BLOB / MAIN / PROC | push rbp PAT-BLOB / MAIN |
|---|---|---|---|---|---|
| programs/snobol4/demo | 28 | 17 | **101** | **130** / 93 / 12 | 113 / 72 |
| benchmarks/snobol4 | 23 | 6 | 8 | 2 / 20 / 3 | 8 / 15 |
| crosscheck/patterns | 122 | 57 | **130** | **93** / 265 / — | 139 / 178 |

Crosscheck-318 instruction baseline (STF-UNFLIP, measured then): **15,459 rbp instructions across 127 programs = the flat_pat/flat_gen WHOLE-GRAPH PIN debt** — "not a statement bracket — do not confuse the two."

## THE DELETION-TARGET LIST (creators; keepers named so nobody re-litigates them)

- **T1 ⛔⭐ BLOB-GRANT frames on INVARIANT-pattern blobs** (Lon's named first target).  Dies with it: SCANBASE rbp reads (kt-32/kt-40), scanfail retry whack, CLASS D γ {res,rbp} record + `pop rbp` res stub + ω absolute unwind (emit.cpp:2742-2796), in-blob legacy ARBNO rbp rebasing.
- **T2** Same blobs reached via `*name` (the defer fast arm is the SOLE flat_pat entry — bare refs die at PT-1/2, star refs at PT-3).
- **T3** Invariant SEGMENT blobs referenced by variant stitch structures (PB) — same BLOB-GRANT frame.
- **T4** `flat_gen` whole-graph pin (generators; product-wide Icon/Prolog) — MECH M-2 routing; tracked here, executed where Lon routes.
- **T5** GLUE-O residual pins on `emit_rec_pin()` mains (the !rec_pin majority already suppressed — s26b census 464→252 push_rbp; remainder = data-reader mains; FB-STMT refinement path).
- **KEEPERS (licensed, never targets):** AB activation frames (`*_act_*`) · MATCH_BEGIN statement head · FENCE1 seal · STATEMENT bracket · Icon zframe/flat_lcl_proc (FUNCTION-class) · CLASS C ambient-rbp whack (a CONSUMER of the C frame, not a creator — the 1016_eval return-to-C mechanism, s22u falsification on record).

## ⛔⭐⭐⭐ LADDER PT — incremental sequence to FLAT PASS-THRU (every rung: killswitch + BY SET gates m3+m4 on the probe suite + xc318 + watermark floors ≥ current + regen ×3 when lower/emitter touched + corpus witnesses with SPITBOL refs)

- [x] **PT-0 · CENSUS + TARGET LIST + INSTRUMENT + WITNESSES** — COMPLETE.  Instrument `scripts/test_census_rbp_frames.sh` landed; table + target list in this file; witnesses promoted to `corpus/probe/pt_inline_1{,_hand,_full}.sno` (SCRIP `d8bd4ba8`, corpus `105f33a6`).  SPITBOL oracle refs: credential SUPPLIED (Lon, s3 2026-08-09) — next session clones/builds x64 `sbl`, confirms `pt_inline_1{,_hand,_full}.ref` + the flagged capture-divergence witnesses against the oracle, then arms the ratchet (gate FAILS on any T-class `est` rise).
- [x] **PT-1 · PAT-INLINE (bare refs of invariant names)** — COMPLETE (DEFAULT ON, `d8bd4ba8`).  Mechanism: `sno_pat_node` TT_VAR arm inlines the compile-time tree for any `g_sno_fz`-member name when `g_sno_pat_match_ctx=1` (the ONE licensed extent: `sno_lower_match`'s pattern walk).  Guards confirmed by code read + gate sweep: `sno_fz_tree` returns NULL when `g_sno_pat_match_ctx=0` (PAT-ARG-BIND scratch walk) or `g_sno_in_patproc=1` (patthunks build) — inline cannot fire in either non-match context.  The session-prior 'miss' was a MIS-AIMED WITNESS, not a defect: `w_pinline1` matched on `PAT` (contains TT_VAR(VOWEL) ⇒ fails `sno_pat_invariant` ⇒ not in fz ⇒ arm never fired; IR byte-identical both modes); the claws5-match SEGV was pre-existing class, resolved at HEAD with no PT code change.  Killswitch `SCRIP_PAT_INLINE=0` reverts to byte-identical legacy.  xc318 watermark 98/16/8 unchanged; probe 26/3/1 unchanged; witnesses hit=A both modes.
- [ ] **PT-2 · DEAD-BLOB + DEAD-BUILD SUPPRESSION** — per-name use census (consumers: `IR_VAR` value reads :232 · `*name` defers :1262/`sno_fz_mark_defer` · snapg pre-chains); zero consumers ⇒ suppress the PAT$ graph emission AND the assignment's stage-2 pattern build (dead store).  **DELIVERABLE: `claws5-match.s` regen carries ZERO `proc_PAT` lines** — the exact mess Lon pointed at, gone from the artifact.  Gate: regen diff is deletion-only; suites BY SET.
- [ ] **PT-3 · `*name`-OF-INVARIANT INLINE** — TT_DEFER arm: fz-member target inlines under the same license (write-once frozen ⇒ late binding provably equals compile-time); recursive/sealed-DEFER stays DEFER → MECH M-2(a) FRAMED variant (FUNCTION-class activation — licensed keeper).  Re-run census; T2 → 0 for invariant names.
- [ ] **PT-4 · SURVIVING-BLOB PASS-THRU PROTOCOL** — blobs still required (stitch segments T3, dynamic refs): α becomes PASS-THRU + a box-own linkage CELL on the spine ({γ, ω, δ0, scanflag} — law-1 "a box a few bytes of its own", NOT a frame, NO pin); interiors admitted to the per-box mechanism (**PL-ZK-5 admission precedent**: a `pat_cells`-class lifts zd_plan's jmp-entry self-decline + the `!flat_pat` guard at emit.cpp:2346); CLASS D record {res,rbp}→{res,pad}; scanfail loses `mov rsp,rbp` (the FORTH fail-chain restores entry depth BY CONSTRUCTION); ω absolute unwind dies.  **PREREQ**: ARBNO/SPAN/BREAK K-conversion inside blob context — COORDINATE WITH MECH, single authority; Lon routes whether this executes here or re-homes.  ⛔ CARVE-ERAD conviction stands (emit.cpp:2914): never cut the region under unconverted readers.
- [ ] **PT-5 · flat_gen PIN census tracking + GLUE-O residual re-measure** — execution likely MECH; this file keeps the product-wide count honest.
- [ ] **PT-6 · PHYSICAL DELETION + RATCHET SEAL** — BLOB-GRANT arm, CLASS D exit spellings, kt region math deleted (label and code same slice); final census: **T-class establishments → 0, keepers unchanged**; the ratchet becomes a standing gate.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 3 (Claude Sonnet 4.6 — PT-1 fully verified; PT-2 design complete; next session implements PT-2)

**Watermark at close:** xc318 98/16/8 (unchanged); probe 26/3/1 (unchanged); SCRIP HEAD `d8bd4ba8` (PT-1 DEFAULT ON); corpus HEAD `105f33a6` (probe witnesses).  **PT-0 + PT-1 complete and pushed.**

**PT-1 session findings (this session):**  Both PT-1 cursor defects confirmed resolved at HEAD.  (a) 'miss' defect: `w_pinline1.sno` tests `'GRAY' ? PAT . N` — PAT is NOT in fz (`sno_pat_invariant(PAT)=0`, PAT tree contains TT_VAR(VOWEL) which fails the gate); inline arm never fires; both modes produce `hit=` (empty, pre-existing blob-capture defect unrelated to PT-1). Correct witness is VOWEL directly in match position — IR under INLINE ON shows bare `MATCH_ANY` on spine, no snapshot, no PAT$0; output `hit=A` correct both modes.  (b) Guard against inline firing during RHS build (feared 1a contamination): structurally impossible — `sno_fz_tree()` returns NULL when `g_sno_pat_match_ctx==0`; PAT-ARG-BIND scratch walk at line 2087 never sets it; IR byte-identical legacy vs inline when build walk runs.  (c) claws5-match SEGV: gone at HEAD; rc=0 both modes; flag has zero effect (claws contains ARBNO, `sno_pat_inline_ok` declines it).  Gate sweep: 3 witnesses OK both modes; RBP ratchet GATE OK both modes; BY SET xc318 PASS/FAIL/DIVERGE byte-identical legacy vs inline; killswitch md5 byte-identical.

**PT-2 design (next session implements):** After PT-1 inline, PAT$ assignment builds (`SNO$MKPAT` call + `proc_PAT$n` blob) remain emitted as dead stores for names whose only consumers were bare-ref match positions (now inlined). Suppression criterion: name in fz AND `sno_pat_inline_ok(fz_tree)` AND `defer_cnt==0` (no `*name` TT_DEFER consumers). Implementation: (1) add `int defer_cnt` to `g_sno_fz` struct; (2) increment in `sno_fz_mark_defer` per name; (3) predicate `sno_fz_is_dead_build(name)`; (4) gate MKPAT chain at line 2064 — dead-build skips to `sJ`; (5) suppress the `proc_PAT` blob graph emission in the patproc builder. Safe: inline-ok trees have no runtime-arg primitives, so `tx.npre==0` in PAT-ARG-BIND — nothing to harvest, skip is clean. Deliverable: `claws5-match.s` regen zero `proc_PAT` lines blocked on ARBNO inline (later rung); PT-2 clears non-ARBNO dead builds including pt_inline_1 witness family. Gate: regen diff deletion-only; BY SET unchanged; ratchet unchanged.  ⛔ PT-0 ratchet still unfinished (SPITBOL oracle refs need credential; clone is public but push-gated).

⚠ **FLAGGED ORACLE-DIVERGENCE (found s3, OUTSIDE PT scope, needs its own MONITOR-FIRST rung + sbl confirm):** stored-PAT whole-capture.  `PAT = VOWEL SPAN(CONS)`; `'GRAY' ? PAT . N` — the `.` attaches to the element it follows (manual pp.66-67), here the whole stored pattern, so N should be PAT's full match `AY`; scrip yields `A` (canon witness) or `` (w_pinline1 variant whose SPAN set includes vowels).  Direct-inline sibling `VOWEL SPAN(CONS) . N` (dot binds to SPAN only, p.78 — a DIFFERENT program) correctly yields `Y` both modes.  CLASS D blob capture path — dies structurally with PT-4/PT-6, but the divergence exists TODAY at killswitch-off, on the licensed-keeper side.  Expected values are manual-derived, not yet sbl-confirmed (oracle unbuilt this session).
