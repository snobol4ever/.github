# ⛔⭐⭐⭐ GOAL-PASSTHRU-RBP-ERAD — FLAT PASS-THRU everywhere it should be; RBP frames counted by census, deleted by rung

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9 +this file, added per Lon 2026-08-10; the nine sibling copies still read "(9)" — deliberately not edited while their seats may be live)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` · `GOAL-PASSTHRU-RBP-ERAD` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. ⭐ **MID-SESSION REBASE (Lon, s10 2026-08-10): other seats are pushing to origin WHILE your session runs** — origin moved 4× during s10 alone (RTCC ×3 + regen + d21b80c5), and s10's opening orientation was against a tip that drifted mid-flight — so an occasional `git pull --rebase` mid-session, at natural commit points with the gate re-proved after, is a sanctioned strategy: absorb parallel landings early instead of discovering them all at close. Pushes remain session-end only. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ THIS FILE'S NOT-CONCURRENCY-SAFE rungs: the regen ×3 artifact sweeps (mandated whenever lower/emitter is touched — mass `.s` rewrites across corpus, same class as RTX-11/12) and PT-4/PT-6 emit.cpp frame-arm surgery (MECH single-authority, currently deferred) MUST NOT run while any other seat is active — Lon routes the window.

**Protocol: `RULES.md` governs.  `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before ANY template/encoder edit.  MONITOR-FIRST for every oracle divergence.  Killswitch law per rung (byte-identical revert, md5-verified; delete label and code in the same slice).  Cross-goal single-authority: `GOAL-SN4-ZETA-MECH.md` owns the stack-work live cursor and the glue-kind/mechanism design — any rung here that touches emit.cpp frame arms re-checks MECH's cursor first and re-homes on Lon's routing; `GOAL-SNOBOL4-BB.md` LADDER PB owns stitch semantics; `GOAL-RTCC` owns register claims.**

⭐ **CHARTER (Lon 2026-08-09, verbatim):** *"I know categorically, undeniably, that a constant folded pattern does NOT need an RBP. … delete that code that generated that mess and replace it with code that uses proper PASS-THRU GLUE as linkage, NOT stack frame linkage. … DO NOT create an RBP frame for constant folded patterns."* — and, same day: *"Plan the entire code modification in an incremental sequence to getting a FLAT PASS THRU everywhere is should be. Count how many RBP frames are being created through the product. Let's make a list to target for deletion. Starting with RBP being used for INVARIANT PATTERNS."*

## DESIGN OF RECORD

- **Exactly three glue kinds** (MECH:253): **ONE-SHOT** (`add rsp,K; jmp pred_β`) · **PASS-THRU** (`bb_glue_pass_wires`: lea rcx,γ + lea rdx,ω + jmp rax — transfer box adopts caller wires, ZERO frame) · **FRAMED** (`bb_glue_framed_enter/leave`, THE ONLY RBP WRITER).
- **Licensed frame census** = **{STATEMENT · FUNCTION · MATCH_BEGIN · FENCE1}** (ARBNO-LON refinement, 08-06; FUNCTION = the AB activation frame, Lon verbatim confirm: the manual p.103 pushdown made literal).  **Every other RBP establishment in the product is a deletion target.**
- **The site side is already correct** (ZD-PATREF, emit.cpp:1935): `bb_match_defer` enters pattern blobs via `bb_glue_pass_wires`, K=0 transfer citizen.  The debt is **blob-side**: `emit_jmp_entry_for_patproc` (emit.cpp:2914/2925) routes every PAT$ blob onto BLOB-GRANT (emit.cpp:2362 — `sub rsp,kt` + wire adoption + **rbp pin**, kt=(48+jcon_value_region+15)&~15; claws5-match: 304) with the CLASS D exit protocol (γ {res,rbp} resume record, scanfail retry `mov rsp,rbp`, ω absolute unwind `lea rsp,[rbp+kt]`) and the legacy in-blob ARBNO per-iteration rbp rebase chain.
- **Statement-inline is the proof-of-possible**: the SAME constructs (ARBNO-LON frameless, ALT, SPAN/BREAK, captures) already run oracle-green in statement context under the licensed frames only.  An INVARIANT pattern's structure is compile-time known — its reference can therefore be pure wiring (pass-thru), its elements riding the statement regime, and the frame question evaporates for it.

## THE CENSUS (RE-MEASURED 2026-08-10 s7 at corpus `fa761d0` / SCRIP `23189c2` post-regen-×3; Σest **557** — s6's 618 and the 609 seed both OBSOLETE: demo MAIN 93→40 and bench MAIN 20→12 dropped via parallel frame-deletion landings between the s6 measurement and origin HEAD, direction consistent with the goal; instrument = `SCRIP/scripts/test_census_rbp_frames.sh`; `push rbp` inside PAT-BLOB regions includes CLASS D γ resume-record pushes — protocol shape, not establishment, dies with the blob; crosscheck/patterns regen cadence STILL unaudited — indicative only)

| corpus (.s) | files | blob-bearing | PAT$ blobs | est `mov rbp,rsp` PAT-BLOB / MAIN / PROC | push rbp PAT-BLOB / MAIN |
|---|---|---|---|---|---|
| programs/snobol4/demo | 28 | 17 | **101** | **130** / 40 / 12 | 113 / 19 |
| benchmarks/snobol4 | 23 | 6 | 8 | 2 / 12 / 3 | 8 / 7 |
| crosscheck/patterns | 122 | 57 | **130** | **93** / 265 / — | 139 / 178 |

Crosscheck-318 instruction baseline (STF-UNFLIP, measured then): **15,459 rbp instructions across 127 programs = the flat_pat/flat_gen WHOLE-GRAPH PIN debt** — "not a statement bracket — do not confuse the two."

## THE DELETION-TARGET LIST (creators; keepers named so nobody re-litigates them)

- **T1 ⛔⭐ BLOB-GRANT frames on INVARIANT-pattern blobs** (Lon's named first target).  Dies with it: SCANBASE rbp reads (kt-32/kt-40), scanfail retry whack, CLASS D γ {res,rbp} record + `pop rbp` res stub + ω absolute unwind (emit.cpp:2742-2796), in-blob legacy ARBNO rbp rebasing.
- **T2** Same blobs reached via `*name` (the defer fast arm is the SOLE flat_pat entry — bare refs die at PT-1/2, star refs at PT-3).
- **T3** Invariant SEGMENT blobs referenced by variant stitch structures (PB) — same BLOB-GRANT frame.
- **T4** `flat_gen` whole-graph pin (generators; product-wide Icon/Prolog) — MECH M-2 routing; tracked here, executed where Lon routes.
- **T5** GLUE-O residual pins on `emit_rec_pin()` mains (the !rec_pin majority already suppressed — s26b census 464→252 push_rbp; remainder = data-reader mains; FB-STMT refinement path).
- **KEEPERS (licensed, never targets):** AB activation frames (`*_act_*`) · MATCH_BEGIN statement head · FENCE1 seal · STATEMENT bracket · Icon zframe/flat_lcl_proc (FUNCTION-class) · CLASS C ambient-rbp whack (a CONSUMER of the C frame, not a creator — the 1016_eval return-to-C mechanism, s22u falsification on record).

## ⛔⭐⭐⭐ LADDER PT — incremental sequence to FLAT PASS-THRU (every rung: killswitch + BY SET gates m3+m4 on the probe suite + xc318 + watermark floors ≥ current + regen ×3 when lower/emitter touched + corpus witnesses with SPITBOL refs)

- [x] **PT-0 · CENSUS + TARGET LIST + INSTRUMENT + WITNESSES** — COMPLETE.  Instrument `scripts/test_census_rbp_frames.sh` landed; table + target list in this file; witnesses promoted to `corpus/probe/pt_inline_1{,_hand,_full}.sno` (SCRIP `d8bd4ba8`, corpus `105f33a6`).  SPITBOL oracle refs: credential SUPPLIED (Lon, s3 2026-08-09) — next session clones/builds x64 `sbl`, confirms `pt_inline_1{,_hand,_full}.ref` + the flagged capture-divergence witnesses against the oracle, then arms the ratchet (gate FAILS on any T-class `est` rise).
- [x] **PT-1 · PAT-INLINE (bare refs of invariant names)** — COMPLETE (DEFAULT ON, `d8bd4ba8`).  Mechanism: `sno_pat_node` TT_VAR arm inlines the compile-time tree for any `g_sno_fz`-member name when `g_sno_pat_match_ctx=1` (the ONE licensed extent: `sno_lower_match`'s pattern walk).  Guards confirmed by code read + gate sweep: `sno_fz_tree` returns NULL when `g_sno_pat_match_ctx=0` (PAT-ARG-BIND scratch walk) or `g_sno_in_patproc=1` (patthunks build) — inline cannot fire in either non-match context.  The session-prior 'miss' was a MIS-AIMED WITNESS, not a defect: `w_pinline1` matched on `PAT` (contains TT_VAR(VOWEL) ⇒ fails `sno_pat_invariant` ⇒ not in fz ⇒ arm never fired; IR byte-identical both modes); the claws5-match SEGV was pre-existing class, resolved at HEAD with no PT code change.  Killswitch `SCRIP_PAT_INLINE=0` reverts to byte-identical legacy.  xc318 watermark 98/16/8 unchanged; probe 26/3/1 unchanged; witnesses hit=A both modes.
- [x] **PT-2 · DEAD-BLOB + DEAD-BUILD SUPPRESSION** — per-name use census (consumers: `IR_VAR` value reads :232 · `*name` defers :1262/`sno_fz_mark_defer` · snapg pre-chains); zero consumers ⇒ suppress the PAT$ graph emission AND the assignment's stage-2 pattern build (dead store).  **DELIVERABLE: `claws5-match.s` regen carries ZERO `proc_PAT` lines** — the exact mess Lon pointed at, gone from the artifact.  Gate: regen diff is deletion-only; suites BY SET.
- [x] **PT-3 · `*name`-OF-INVARIANT INLINE** — TT_DEFER arm: fz-member target inlines under the same license (write-once frozen ⇒ late binding provably equals compile-time); recursive/sealed-DEFER stays DEFER → MECH M-2(a) FRAMED variant (FUNCTION-class activation — licensed keeper).  Re-run census; T2 → 0 for invariant names.
- [ ] **PT-4 · SURVIVING-BLOB PASS-THRU PROTOCOL** — blobs still required (stitch segments T3, dynamic refs): α becomes PASS-THRU + a box-own linkage CELL on the spine ({γ, ω, δ0, scanflag} — law-1 "a box a few bytes of its own", NOT a frame, NO pin); interiors admitted to the per-box mechanism (**PL-ZK-5 admission precedent**: a `pat_cells`-class lifts zd_plan's jmp-entry self-decline + the `!flat_pat` guard at emit.cpp:2346); CLASS D record {res,rbp}→{res,pad}; scanfail loses `mov rsp,rbp` (the FORTH fail-chain restores entry depth BY CONSTRUCTION); ω absolute unwind dies.  **PREREQ**: ARBNO/SPAN/BREAK K-conversion inside blob context — COORDINATE WITH MECH, single authority; Lon routes whether this executes here or re-homes.  ⛔ CARVE-ERAD conviction stands (emit.cpp:2914): never cut the region under unconverted readers.
- [ ] **PT-5 · flat_gen PIN census tracking + GLUE-O residual re-measure** — execution likely MECH; this file keeps the product-wide count honest.
- [ ] **PT-6 · PHYSICAL DELETION + RATCHET SEAL** — BLOB-GRANT arm, CLASS D exit spellings, kt region math deleted (label and code same slice); final census: **T-class establishments → 0, keepers unchanged**; the ratchet becomes a standing gate.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 10 (Claude Opus 5 — DIAGNOSIS ONLY, zero code touched: the frontier is NOT capture, it is EVERY blob; root cause named in D-2's own comment)

**Watermark at close:** SCRIP HEAD **`c7e085fd`** · corpus **`9fb2e019`** · `.github` this commit + `FINDING-2026-08-10b`. **RE-PROVEN POST-REBASE (protocol satisfied):** the RTCC seat landed 4 SCRIP / 2 corpus / 5 `.github` commits DURING this session (r9→r10 GVA claim, veneer hardening, a convicted SIGSEGV fix, artifact regen). I rebased onto `c7e085fd`, rebuilt, and re-ran everything: probe **24 PASS / 12 FAIL with the identical failing set**, all four scratch witnesses reproduce byte-identically, and both root-cause sites are unchanged (`g_blob_ctx[5]` still one global; `emit.cpp:2358` still keys on `flat_pat` alone). **The RTCC work does not touch this zone and the finding below stands at the current origin tip.** No code edit by me; SCRIP/corpus carry zero commits of mine. Tripwire (s7 protocol) clean at open and close: HEAD unmoved, zero dirty files across the build. **No push attempted — awaiting Lon's credential and the §5 routing.**

**⛔ READ FIRST — THE s9 WATERMARK HASHES DO NOT EXIST.** `33062a20`/`5e66bfa6`/`9f1436b4` are ABSENT from origin; the work landed rebased as **`ef8a3052`(D-3) / `1f96143c`(D-2) / `1af93e3a`(D-1)** over the parallel RTX-FUNC `799f2e76`. Second instance same day: corpus is at `73271e2e` (NOT the claimed `0f02731d`, and NOT untouched — another seat pushed a demo regen at 15:08), whose message cites SCRIP `5416ed56`, **also absent**. Consequence: the question "were the 15:08 demo artifacts generated by a D-1-broken compiler?" (D-1 landed 14:52, D-2 15:24) **is unanswerable from the commit record and stays OPEN**. This is now a recurring class — recommend cursors record the commit SUBJECT beside any hash, or write the hash only after the push returns.

**BASELINE REPRODUCES EXACTLY, COLD CONTAINER:** probe m3 **24 PASS / 12 FAIL** at `ef8a3052`, failing set == s9's 5 pre-existing + 7 casualties. Modes are two, and s9 does not separate them: **124 = HANG** (the five `w_cap_*`), **139 = SEGV** (`dc_recur`, `mv_arbno_callcap`). **D-3 re-confirmed independently:** neither `SCRIP_PAT_INLINE=0` nor `SCRIP_SCAN_OFF=1` moves any of the 7.

**⛔⭐ THE s9 FRONTIER LABEL IS WRONG ON THE CAPTURE HALF.** Four scratch witnesses, each pair identical in semantics and differing only inline-vs-stored: `A2_inline_nocap` 0 blobs → **PASS**; `A_stored_nocap` 18 blobs → **HANG**; `C2_inline_nested` 0 blobs → **PASS**; `C_stored_nested` 9 blobs → **SEGV**. `A_stored_nocap` carries **no `.`, no `$`, no `@`** and still hangs. Correlation with capture: none. With blob presence: 4/4. **Frontier restated: every PAT$ blob on the pass-thru path fails — non-nested ⇒ hang, nested ⇒ SEGV.** Capture witnesses were simply the ones already in `corpus/probe/`. Cheaper reproducers now exist (both capture-free, `C` = one recursion level, 5-char subject) — see FINDING §2; witnesses in `/tmp/ptdiag/`, deliberately NOT promoted (a known-fail `.ref` moves other seats' watermark — s5 precedent).

**⛔⭐ ROOT CAUSE, from D-2's own comment at `pattern_match.c:624`:** `g_blob_ctx[5]` is **one process-global cell** replacing a **per-activation** frame, and its stated soundness condition is *"NON-NESTING assumption … so a single cell suffices"*. The arming gate `emit.cpp:2358` keys on **`flat_pat` alone** — not invariance, not nesting, not defer-freedom — so `*LIST` (the TT_DEFER case the comment explicitly excludes) gets the single-cell protocol anyway and `mov rsp, ctx.base` (`emit.cpp:2740`) restores another activation's base. **Licensed scope ≠ emitted scope.** ⭐ This DEVIATES FROM THIS FILE'S OWN PT-4 DESIGN OF RECORD, which specifies *"a box-own linkage CELL **on the spine**"* — per-activation by construction. The repair direction was already written here; D-2 shipped a global instead. Manual concurrence: Ch.18 p.204 specifies a **pushdown stack** (push alternative+cursor per alternative, pop on fail) and Ch.9 p.123 says recursive patterns consume it — depth is semantically required, and depth is exactly what the global anchor dropped. **Lon's charter IS discharged regardless: `mov rbp, rsp` = 0 in every blob, both media, measured.**

**⛔ SELF-CORRECTION at close (see FINDING §7):** the single-global-cell mechanism explains the **139/SEGV** class only. `A_stored_nocap` does NOT nest, so D-2's own NON-NESTING precondition HOLDS for it, and it hangs regardless — **the 124/HANG class is a second, unexplained defect.** §2's blob⇔failure correlation is observation and stands; the §3 mechanism over-reached. Also unfinished: every simplification of `A_stored_nocap` I tried (one-level stored, two-level ref, two-level+literal) gets INLINED AWAY by PT-1/PT-3 to **0 blobs and passes** — so the blob's survival condition is narrower than "stored pattern" and I did not isolate it. **Open question for the next seat: what makes `A_stored_nocap` keep 18 blob defs when `PAT = V 'Y'` keeps zero?** (SPAN-vs-literal second element? the s6 `g_sno_snapref` / s7 `g_sno_encl` verdicts?) The D/E ladder is exhausted — do not re-run it as written.

**NEXT RUNG, in order:** (1) answer the §7 open question to get a minimal HANG witness, then treat hang and SEGV as two defects; (2) convert `g_blob_ctx` from process-global to a per-activation spine cell (PT-4's own wording) — **but this is MECH's stack-work zone; Lon routes before any edit**; (2) retarget witnesses to the capture-free pair — `w_cap_*` are not cheapest and their framing misled two sessions; (3) probe + broad-336 BY SET, m3 AND m4, same container both arms — the s7/s8 floors remain OWED and unverified; (4) regen ×3 still owed AND still entangled with the §0 provenance question.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 10 (Claude Opus 5 — S9'S DIAGNOSIS FALSIFIED: DEL-T1 broke EVERY ENTERED BLOB, not the capture family; BY SET 29 BROKEN / 0 REPAIRED; NO CODE CHANGED)

**Watermark at close:** SCRIP `c7e085fd` (UNTOUCHED — no code changed this session) · corpus `9fb2e019` (UNTOUCHED) · `.github` this commit + `FINDING-2026-08-10b`. Probe suite m3 **24/12 BY SET**, reproducing s9 exactly (RTCC's parallel landings do not perturb it). m4 NOT measured. broad-336 NOT run.

**⛔ HASH CORRECTION — the s9 cursor's SHAs DO NOT EXIST at origin.** Real: D-1 `1af93e3a` · D-2 `1f96143c` · D-3 `ef8a3052`. s9 recorded pre-rebase hashes; the s8 lesson recurred verbatim. Grep the MESSAGE. Three RTCC commits + a feature regen landed between D-3 and my open — **that seat is live; regen ×3 window is still not mine to take.**

**⛔⭐ THE INHERITED DIAGNOSIS IS FALSIFIED (measured, minimal pair, same container).** s9 names the frontier "capture + recursion families" and sends the next seat at interior `op_flat_disp` **for capture**. Wrong target: `w_nocap` — the `w_cap_stored` program with EVERY capture operator removed — HANGS identically (124). And the "passing" blob-bearing witness `pt_inline_1_full` passes only because it matches on `VOWEL`, not `PAT`: its 18 `proc_PAT` defs are **emitted but never entered**.

**Construct bisect, blob presence held constant (`PAT = VOWEL <X>`, subject `'AY'`), HEAD vs PRE-D-1 `930539c0`:** `VOWEL 'Y'` → 0 blobs, ok/ok · `LEN(1)` SEGV/ok · `ANY('Y')` SEGV/ok · `SPAN('Y')` HANG/ok · `BREAK('Z')` HANG/ok · `ARBNO('Y')` SEGV/ok · bare `PAT` HANG/ok · `PAT . N` HANG/**ok=`AY` (the sbl answer)**. **Every blob-bearing program fails at HEAD; every one was correct pre-D-1; the only survivor emits zero blobs.** ⭐ **The D-2/D-3 pass-thru glue does not work for ANY blob entry.** s9's "6 repaired" were repaired by no longer ENTERING blobs. `PT-4 PREREQ` (ARBNO/SPAN/BREAK K-conversion in blob context) was never discharged — DEL-T1 was executed ahead of its own stated prerequisite. Note `PAT . N` was ORACLE-GREEN pre-D-1: DEL-T1 regressed the exact behaviour s6+s7 spent two sessions establishing and promoting to `w_cap_stored.ref`.

**BLAST RADIUS — `crosscheck/patterns` (122), BY SET, both arms built here:** PRE-D-1 **97 PASS / 25 FAIL / 1 HANG** → HEAD **68 / 54 / 6**. **29 BROKEN, 0 REPAIRED.** Zero. Broken set dominated by stored-pattern / FENCE-via-var / JSON families (114, 115, 119, 124, 126, 127, 129, 130, …).

**Killswitches (both confirm D-3's bisect):** `SCRIP_PAT_INLINE=0` and `SCRIP_SCAN_OFF=1` each leave the hangs unchanged — scan machinery exonerated. DEL-T1 has NO killswitch; `git revert 1af93e3a` is the only undo and D-2/D-3 sit on it.

**⛔ ROUTING — LON'S CALL, and the evidence has changed it materially.** Fix-forward already ran a full session (s9) for 0 real repairs at 29-program cost. **(1) REVERT D-1+D-2+D-3, land the PT-4 PREREQ, then re-delete** — restores 29 programs now; interim cost is the blob prologue returns and the "never want to see that code again" directive is un-discharged. **(2) HOLD and fix forward** under a universally-broken blob path. ⭐ **Recommend (1):** the deletion is right as a destination but landed before the interior could receive it; the frame code is safe in git, re-deleting after the prereq is cheap, and nothing measured on a broken blob path is trustworthy.

**⭐⭐⭐ ROOT CAUSE FOUND (same session, MEASURED in emitted code) — it is an UNCONVERTED RBP READER PAIR, not `g_blob_ctx`.** Every PAT$ blob still emits its ω continuation as a store/load pair through `rbp`: `α_body: mov [rbp+K], rax` (rax = `lea ω`) and `β: jmp [rbp+K]`. Counted at HEAD: `b_len.s` 4 refs (PAT$0 @+32, PAT$1 @+48); `w_cap_stored.s` 4 refs with **both blobs on +32 — aliasing the same caller slot**. Pre-D-1 `rbp` was the blob's OWN frame so the spelling was legal; D-1 deleted that frame and D-2 made the blob adopt the caller's, so `[rbp+K]` now **smashes a live slot in the invoking MATCH_BEGIN frame** (a licensed keeper). Symptom follows what occupied the slot: SEGV (`LEN`/`ANY`/`ARBNO`) or HANG (`SPAN`/`BREAK`). ⛔ **This is this file's own CARVE-ERAD conviction realised verbatim — "never cut the region under unconverted readers."** Explains every observation at once: universal, capture-independent, fires on a single non-nested entry, correct pre-D-1.

**`g_blob_ctx` — verified as fact, REJECTED as cause.** It is confirmed a process-global (`pattern_match.c:624`) and α **does** publish correctly (rsp/γ/ω/scanflag/δ0 all stored — read in the asm). The conversion is INCOMPLETE, not misdesigned: wires are published into ctx but **β never reads them**, still jumping via `[rbp+K]`. Global-vs-per-activation is real debt that will bite on nesting, but cannot kill a single non-nested entry. ⚠ **MECHANISM IS INFERENCE, not runtime-measured:** `emit_jmp_pin_rbp()` (emit.h:630) is a four-way OR and D-2 removed only `flat_pat` — **necessary but not sufficient**, since `flat_deep_arrival` is classified independently at `emit.cpp:3059` and still fires. CONFIRMING EXPERIMENT OWED (instrument the predicate per-graph; I read asm + predicate, not the runtime flag).

**FIX SPEC:** `α_body` stops writing through `rbp`; `β` takes ω from the published ctx / a per-blob spine cell. Both media together (BOTH-MEDIUM MANDATORY); `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before the edit. The +32 aliasing proves a **per-blob** cell is required, not one shared slot — exactly PT-4's "a box a few bytes of its own." **This narrows the decision: the defect is localized and small, so fix-forward is now viable where s9's blind interior hunt was not; revert still restores 29 programs immediately and remains the safe default.**

**NEXT SESSION:** take Lon's routing FIRST — do not resume s9's "attack interior op_flat_disp for capture", it is aimed at a falsified target. If FIX-FORWARD: convert the ω-wire linkage per the FIX SPEC above, cheapest witness `b_len` (`PAT = VOWEL LEN(1)`, `'AY' ? PAT` — SEGV, no capture, no recursion, 4 rbp refs), then `b_span` for the HANG arm; confirm the `flat_deep_arrival` inference before editing. If REVERT: revert `ef8a3052`+`1f96143c`+`1af93e3a`, re-prove 97/25/1 on crosscheck/patterns BY SET, then land the PREREQ. Either way m4 is UNMEASURED for this family and gdb is absent in-container (no sudo; `apt-get install gdb` failed — arrange it at open).

**s10 HANDOFF ADDENDUM — ladder state + protocol corrections (⭐ = proposal, needs Lon's confirm):**
- **DEL-T1 ladder state (correcting the unchecked boxes above):** D-0 manifest NEVER RUN · D-1 `1af93e3a` / D-2 `1f96143c` / D-3 `ef8a3052` LANDED AND MEASURED BROKEN (29 BROKEN / 0 REPAIRED by set) · D-2 FIX-FORWARD is now concretely the ω-wire conversion per FIX SPEC above (small, both media, full-runway seat only) · D-3 gate + D-4 seal blocked behind it · revert path `git revert ef8a3052 1f96143c 1af93e3a`.
- ⭐ **PROPOSED BINDING RULE — PREREQ-GATES-DELETE:** a deletion rung whose own goal file names a PREREQ may not land D-1 until the prereq is discharged, or Lon waives it EXPLICITLY in the D-1 commit message. Root: DEL-T1 landed ahead of the PT-4 PREREQ this file had already written down, at 29-program cost. A delete-first charter inverts suppress-first, not prereq ordering.
- ⭐ **PROPOSED — POST-REBASE HASH DISCIPLINE:** cursor watermarks are written AFTER the end-of-session rebase, or record commit MESSAGES, never bare pre-rebase SHAs. s8 and s9 both shipped dead SHAs; s10 burned time re-deriving D-1/D-2/D-3 identities.
- **⚠⚠ PHANTOM RECURRENCE (3rd instance, s7 class):** `2f5e087d` · `34cf71af` · `164aae5f` self-minted on local main under the session identity during s10 — the third MID-SESSION, its content SELF-CORRECTING toward my in-flight measurement (convergent with, not identical to, the measured root cause; its verification claims UNVERIFIED). Treat all three as UNVERIFIED CONTENT per s7 law. Pushed WITH this marker rather than dropped: my verified commits (`a1a9a111`, `fa8b38fe`, this one) stack on them and end-of-context extraction risked a stranded handoff (the s19–s26 conviction); Lon may revert any of the three. ⭐ The mid-session reactivity strongly suggests an agentic process with repo access is live IN-CONTAINER during sessions — needs Lon-level environment investigation; s7 already exonerated hooks and Makefile.
- **Probe-suite blind spot:** the 36-probe suite barely exercises blob ENTRY (KS-OFF moved it by 1 while crosscheck/patterns moved by 29). The b_* constructions named in this cursor are the cheap entry witnesses; promotion is gen_probes/Lon territory.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 9 (Claude Opus 4.5 — DEL-T1 EXECUTED: D-1 delete landed FIRST as ordered, D-2 pass-thru fix-forward PARTIAL; the PT-4 PREREQ frontier is now MEASURED, not predicted)

**s9 ADDENDUM — D-3 `33062a20` (post-cursor, supersedes the killswitch paragraph below):** the α carve was LIFTED OUT of `scan_live` onto its own `blob_act` gate — SCRIP_SCAN_OFF again means only the retry blocks, and the FIRST HONEST BISECT ran: dc_recur 139/139, w_cap_stored 124/124, w_cap_ay 124/124 scan-on/scan-off — IDENTICAL failure with the retry blocks not emitted at all.  The D-2 scanhit/scanfail rewrite and the scan machinery are EXONERATED.  The remaining defect is INTERIOR: op_flat_disp for capture/recursion families in blob context (the PT-4 PREREQ proper).  Default-ON unchanged 24/12.  NEXT SESSION STARTS AT THE INTERIOR, NOT THE GLUE — cheapest witness w_cap_stored, deepest dc_recur.  ROUTING NEEDED FROM LON: (a) does the interior K-conversion execute under THIS goal or re-home to MECH (the goal file's original PT-4 coordination question, now live); (b) regen ×3 window — artifacts are stale by construction until the 7 go green or Lon routes an honest-broken regen; (c) claws5-match rc=0-wrong-output needs the monitor once interiors move.

**Watermark at close:** SCRIP HEAD `5e66bfa6` (D-2) on top of `9f1436b4` (D-1); corpus HEAD `0f02731d` (untouched — NO regen this session, see DEBT).  Lon directive verbatim: *"NO RBP frame for CONSTANT FOLDED patterns. Use flat pass-thru glue. I never want to see that code again."* — discharged: the compiler can no longer emit the `sub rsp,304 / mov [rsp+296],rbp / mov rbp,rsp` blob prologue in either medium.

**D-1 `9f1436b4` — THE DELETE (landed first, alone, breaking by design as ordered).**  Removed from the BLOB-GRANT arm in `emit.cpp`: `sub rsp,kt`, γ/ω wire stores `[kt-24]/[kt-16]`, caller-rbp save `[kt-8]`, `mov rbp,rsp` pin, SCANBASE fills `[kt-32]/[kt-40]` — TEXT snprintf and BINARY `ef_b*` twins together.  Verified: fresh `claws5-match` `proc_PAT$0_α` carries ZERO frame instructions.  One correction inside the same commit (amended): the first cut over-swallowed `emit_label_define_bb(&lbl_attempt)` → emit-time "unresolved forward reference"; restored.  Revert path is this commit alone.

**D-2 `5e66bfa6` — PASS-THRU GLUE (partial repair, honest numbers).**  Blob ADOPTS the caller's frame (rbp = invoking statement's MATCH_BEGIN frame, a licensed keeper, NEVER written in the blob).  Activation memory = pin-free cell carve; the depth-immune anchor is a new runtime cell `g_blob_ctx[5]` {base, γ wire, ω wire, scan flag, δ0} in `src/runtime/pattern_match.c` (+ `rt_blob_ctx_ptr()`, rt_anchor precedent).  α publishes it; scanhit/scanfail, CLASS D γ (record is now `{res, CTX.BASE}`), CLASS D ω (`rsp = ctx.base + kt`) and the β res-stub all read it; the res-stub REFILLS ctx from the suspended activation's carve header, which is the suspend/resume interleaving repair.  `flat_pat` removed from `emit_jmp_pin_rbp()` (emit.h:630) — **mandatory, not a tuning knob**: with the α pin gone, a pinned spelling addresses the INVOKER's frame.

**MEASURED, BY SET, same container, 36 probe witnesses m3:**
| rung | PASS | FAIL |
|---|---|---|
| pre-delete `7b4d310d` | 31 | 5 (pre-existing: ab_freturn, ab_nret_lvalue, ab_redefine, z4_arbno, z4_span) |
| D-1 only `9f1436b4` | 18 | 18 (13 casualties) |
| D-1+D-2 `5e66bfa6` | 24 | 12 (7 casualties; **6 repaired**) |

`claws5-match`: SIGABRT(134) under D-1 → **rc=0 under D-2 but output STILL DIFFERS from ref** (not a pass; counted as broken).

**THE 7 REMAINING CASUALTIES — the frontier, named:** `dc_recur`(139), `mv_arbno_callcap`(139), `w_cap_ay`/`w_cap_group`/`w_cap_novowel`/`w_cap_stored`/`w_cap_xxay`(124 hang).  Capture + recursion families.  This is exactly the **PT-4 PREREQ this goal file already predicted**: interior `op_flat_disp` correctness for the match family in blob context (ARBNO/SPAN/BREAK K-conversion).  It is now measured rather than forecast.  NOT closed, NOT claimed closed.

**DEFECT FOUND IN D-2's OWN PLACEMENT (first item of the next rung):** the pass-thru α carve sits INSIDE the `scan_live` block, so `SCRIP_SCAN_OFF=1` now suppresses ACTIVATION ESTABLISHMENT too — all 7 casualties report 139 under the killswitch, which is a CONFOUNDED signal and localizes nothing.  Activation establishment is not scan machinery.  Lift the carve out of that gate before using the killswitch to bisect anything.

**NEXT RUNG, in order:** (1) lift the α carve out of `scan_live` and re-run the killswitch bisect on the 7; (2) attack interior `op_flat_disp` for the capture/ARBNO family in blob context (the PT-4 PREREQ proper) — `w_cap_stored` is the cheapest witness, `dc_recur` the deepest; (3) re-measure BY SET against the table above; (4) broad-336 + regen ×3 once witnesses move.

**DEBT CARRIED (disclosed, not hidden):** broad-336 NOT run this session; regen ×3 NOT performed (corpus untouched) — artifacts on disk still show the pre-delete prologue and are now stale by construction; the census instrument was not re-run.  Deliberate: context ran out and regenerating 300+ artifacts from a knowingly-broken emitter would bury the measured signal above under noise.  Do regen ONLY after the 7 are green or Lon routes otherwise.  ⚠ carried forward, still unowned: `bb_match_end.cpp` 12×r10 TIER-2 gate fail; `128_pat_recursive_grammar` separate defect.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 5 (Claude Sonnet 4.6 — PT-0 oracle-confirm done; PT-4 is next; Lon routes PREREQ)

**Watermark at close:** xc318 269m3/48m3/3div (unchanged); RBP ratchet seeded=609 unseeded=0 GATE OK; SCRIP HEAD `87dfb964`; corpus HEAD `dee11d0e`.  **PT-0 + PT-1 + PT-2 + PT-3 complete.  PT-0 oracle-confirm DONE.**  (s5 correction: an earlier s5 cursor write recorded stale hashes `379eb630`/`e3ccea53` — s4 pushed in parallel mid-s5; STALE-ORIENTATION protocol absorbed it; hashes above are ground truth from handoff_status.)

**PT-0 oracle-confirm (s5):** x64 `sbl` cloned `/home/claude/x64/bin/sbl`. `pt_inline_1`, `pt_inline_1_hand`, `pt_inline_1_full` — sbl output matches committed `.ref` exactly; scrip m3 witnesses hit=A (m3+m4 both verified s4).  **PT-4 is first incomplete rung** — PREREQ: ARBNO/SPAN/BREAK K-conversion inside blob context; Lon routes whether PT-4 executes here or re-homes to MECH.  Read `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` before any emit.cpp frame-arm edit.  ⚠ OBSERVED s5, pre-existing, NOT from PT (PT-2/3 touched only lower_snobol4.c): `test_gate_sno_pat_reg.sh` TIER-2 HARD FAIL — 12 r10 references in `src/templates/bb_match_end.cpp` (gate demands 0; REG-RO/r14-cursor law). Needs Lon routing to its owning goal.

**PT-2 session findings (this session):**  Implementation: (1) `int defer_cnt` added to `g_sno_fz` struct; (2) incremented in `sno_fz_mark_defer` for non-patproc *name consumers; (3) `sno_fz_is_dead_build(var_nm)` predicate (lookup by var name — for MKPAT chain gate); (4) `sno_fz_procname_is_dead(procnm)` predicate (lookup by PAT$N name — for patproc builder, which indexes by g_sno_pats[].name not var); (5) MKPAT chain gate at ~2065 skips to sJ for dead-build names; (6) patproc builder `continue`s for dead-build blobs.  Key discovery: MKPAT and patproc use different keys (var name vs PAT$N procname) — two separate lookup predicates required.  Verification: `pt_inline_1` and `pt_inline_1_hand` produce zero `proc_PAT` lines in mode-4; `pt_inline_1_full` suppresses PAT$0(VOWEL) but retains PAT$1(PAT) correctly (PAT's tree contains TT_VAR(VOWEL) → not inline-ok → kept); `claws5-match` proc_PAT count unchanged (ARBNO-bearing pattern, inline-ok gate fails → blob retained, correct per PT-2 scope); killswitch SCRIP_PAT_INLINE=0 reverts PT-1 inline → defer_cnt becomes 1 → PT-2 also does not suppress → blob retained → correct.  All three PT witnesses m3+m4: hit=A.  RBP ratchet GATE OK.  BY SET xc318 unchanged.  161_pat_defer_fn_nested_match --compile SEGV pre-existing (confirmed by killswitch-off same result).

**PT-3 session findings (this session):**  Implementation: in TT_DEFER arm, before calling `sno_fz_mark_defer`, check `sno_fz_tree(nm)` + `sno_pat_inline_ok` — same killswitch `SCRIP_PAT_INLINE`. Inline fires first so `defer_cnt` stays 0, PT-2 blob suppression also fires for `*name` of inline-ok invariant.  Same guards as PT-1: `!g_sno_in_patproc && g_sno_pat_match_ctx` (inside `sno_fz_tree`).  Recursive `*LIST` (ARBNO-bearing, not inline-ok) correctly stays DEFER.  Result: xc318 PASS 264→269 (5 new passes from deferred-invariant programs now resolving inline); FAIL 53→48; DIVERGE unchanged at 3.  Killswitch-off: `*VOWEL` falls back to DEFER path (blob path, pre-existing capture divergence produces `hit=`).  Recursive case `(42)` → `ok` correct.  T2 (live): demo corpus proc_PAT blobs 101→96 (5 eliminated by PT-2+PT-3); artifact census numbers still show baseline because census reads pre-regen artifacts — regen diff deletion-only confirmed (zero new proc_PAT lines).  Regen: benchmark/feature/demo artifacts updated and committed.

**PT-4 design (next session implements):** Surviving-blob pass-thru protocol — blobs still required (stitch segments T3, dynamic refs): α becomes PASS-THRU + a box-own linkage CELL on the spine; CLASS D record {res,rbp}→{res,pad}; scanfail loses `mov rsp,rbp`; ω absolute unwind dies.  PREREQ: ARBNO/SPAN/BREAK K-conversion inside blob context — COORDINATE WITH MECH, single authority; Lon routes whether this executes here or re-homes.

⚠ **FLAGGED ORACLE-DIVERGENCE (found s3, OUTSIDE PT scope, needs its own MONITOR-FIRST rung + sbl confirm):** stored-PAT whole-capture.  `PAT = VOWEL SPAN(CONS)`; `'GRAY' ? PAT . N` — the `.` attaches to the element it follows (manual pp.66-67), here the whole stored pattern, so N should be PAT's full match `AY`; scrip yields `A` (canon witness) or `` (w_pinline1 variant whose SPAN set includes vowels).  Direct-inline sibling `VOWEL SPAN(CONS) . N` (dot binds to SPAN only, p.78 — a DIFFERENT program) correctly yields `Y` both modes.  CLASS D blob capture path — dies structurally with PT-4/PT-6, but the divergence exists TODAY at killswitch-off, on the licensed-keeper side.  **sbl-CONFIRMED s5** — program `VOWEL = ANY('AEIOU'); PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ'); 'GRAY' ? PAT . N; OUTPUT = N`: sbl → `AY` (manual-derived expectation confirmed); scrip m3 at HEAD `87dfb964` → `GR` — note the scrip-side value has SHIFTED since the s3 flag (was `A`), so whatever moved between s3 and s4 HEADs changed the wrong answer without fixing it.  Divergence real; MONITOR-FIRST rung still owed; witness deliberately NOT promoted to corpus/probe (a known-fail .ref would move the probe-suite watermark other goals gate on — promote it inside the future rung that fixes it).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 6 (Claude Fable 5 — the FLAGGED stored-PAT divergence RESOLVED: it was Defect B, PT-2's own census hole, not a capture bug)

**Watermark at close:** SCRIP HEAD `599601e8` (post-rebase onto parallel RTX-FUNC-0 `029387d2`; pre-rebase `a53d3c6`) · corpus HEAD `10f5316` · `.github` this commit.  Broad-corpus parity (`test_broad_corpus_snobol4.sh`, 336-program set — supersedes the 318-era xc watermark): m3 PASS 249→**258**, m4 244→**253** (pristine-HEAD stash build vs fixed; only-in-FIXED fail-set **EMPTY** = 0 broken by set; the 9 repaired per mode are the pat_json/pat_regex/pat_calc/fence_alt_with_capture snapshot-consumer class).  Probe suites: m3 132·15xf·0xp·**4reg pre-rebase → 3reg post-rebase**, m4 131·16xf·0xp·**4reg pre-rebase → 3reg post-rebase** — the surviving trio is PRE-EXISTING (below; X12 healed by 029387d2).  RBP census Σest **609→618**: +9 = the 9 resurrected PAT$ blobs, one per repaired program — a coherent ledger; reseed is Lon's call (correctness reversal of wrong deletions); NOTE census crosscheck section is STALE VINTAGE (crosscheck regen not in the mandated ×3).  Scratch witnesses 6/6 oracle-clean m3+m4.

**RESOLVED — the s3-flagged divergence was TWO stacked defects; the live one (Defect B) is PT-2's, landed s4, and it is a MATCHING bug, not capture.**  Measured chain (gdb, m4): a bare TT_VAR member of a KEPT patproc can never inline (1293 `in_patproc` guard) ⇒ always lowers to a `$V<i>` snapshot that COPIES the source var's stage-2 value at MKPAT time — a live read PT-2's census cannot see: `defer_cnt` (mark_defer:962, itself in_patproc-guarded) counts only `*name` consumers, and the snapg arm (2351) credits the synthetic `$V` name, never the SOURCE var.  `is_dead_build(VOWEL)` fired at 2084 ⇒ stage-2 build suppressed ⇒ GVA slot `v=0x0` at match time (measured) ⇒ snapshot tag 0 ⇒ `c_rt_defer_close` literal arm matches the NULL STRING, returns cur_delta ⇒ element vacuous, SPAN alone matches from the raw entry cursor, capture window [site-δ : r14] yields GR/Y/XX.  KILLER: `'BCD' ? PAT` — sbl FAILED, pristine-HEAD scrip MATCHED:[BCD].  **KILLSWITCH-LAW GAP (routing item):** `SCRIP_PAT_INLINE=0` does NOT revert (measured invariant) — the verdict flows from the census, not the gated inline; PT-2 currently has no byte-identical revert switch.

**FIX (SCRIP `a53d3c6`, lower_snobol4.c only):** `g_sno_snapref` set + walker + pre-scan piggybacked on `sno_fz_build_table`'s stored-pattern arm (same guard as `seal_note`, BEFORE the invariance gate — the 2084 consult runs MID-LOOP, a `g_sno_pats` scan would race statement order); both verdicts (1204/1210) gain `&& !sno_fz_snapref(var)`; resets at 979 and the 2606 fragment path.  Walker skips capture TARGETS (`. N`/`$ N` c[1], `@N`) and TT_DEFER — `*name` is late-bound BY NAME with its own 1919 registration + g_spk latch; out of scope by design.  Sound over-approx: a snapref'd name stays live even if every referencing blob later dies.  **Measured-vs-inferred lesson on record:** v1 walked TT_DEFER, the suite showed 4 REGRESSION, and I attributed causation WITHOUT a pre-fix baseline run — the stash build then proved all four fail identically at pristine `87dfb964`.  The TT_DEFER skip stands on scope grounds alone; the false attribution is corrected in the code comment.

**Defect A (the s3 `A` answer) — reconstruction, labeled INFERENCE (depth-1 clone blocked bisect):** not reproducible at HEAD once builds live; MECH s34's STFH-DEFER-FIX most plausibly repaired the old γ-continuation capture defect the same day PT-2 introduced B — the s5 note "changed the wrong answer without fixing it" was two ships passing.  Its own MONITOR-FIRST rung is MOOT unless a bisect disagrees.

**Monitor finding → OPS-2 MON-CAP feed:** PARTICIPANTS="spl scr" diverges at step 2 on trace SHAPE (spl emits `VALUE VOWEL=UNKNOWN` for the pattern assignment; scr doesn't) — the instrument is blind to this whole class; diagnosis proceeded per RULES via sibling-asm-diff + gdb (w_cap_group vs w_cap_stored was the discriminating pair: grouped capture inline is CORRECT, the defect needed the blob).

**NEW FLAG — `*P`-family runaway, PRE-EXISTING at `87dfb964` (stash-proof):** D09/D11/H21 rc=139 (X12 HEALED by the parallel RTX-FUNC-0 `029387d2` — its cross-session body-jmp fix was exactly the NULL-jump; post-rebase suites 133·15·3 m3 / 132·16·3 m4) — ARBNO(`*P`) at statement level loops null-width (infinite `$` fire) then SIGSEGV; X12's header claims m4-clean but it now fails in m4 too (widened).  Baseline drift from an earlier 2026-08-09 landing (s33/s34-adjacent suspects); needs its own MONITOR-FIRST rung; suite baselines will show 4 REGRESSION until it lands.

**Witnesses (scratch, NOT promoted — probe-ID allocation is gen_probes territory; Lon routes promotion).  Verbatim for survival, 8-space indent, `END` col-1:**
```
w_cap_stored.sno:    VOWEL = ANY('AEIOU') / PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ') / 'GRAY' ? PAT . N / OUTPUT = N / END          → sbl AY
w_cap_novowel.sno:   VOWEL = ANY('AEIOU') / PAT = VOWEL SPAN('BCDFGHJKLMNPQRSTVWXYZ') / 'BCD' ? PAT . N :F(NOPE) / OUTPUT = 'MATCHED:[' N ']' :(END) / NOPE OUTPUT = 'FAILED' / END   → sbl FAILED
variants: subject AY→AY · XXAY→AY · inline dot-on-SPAN→Y · parenthesized group→AY   (all six now oracle-clean m3+m4)
```

**Routing items for Lon (superset of s5):** PT-4 here-vs-MECH (unchanged) · bb_match_end.cpp 12×r10 TIER-2 fail (unchanged) · PT-2 killswitch-law gap (new) · `*P` runaway rung ownership (new) · ratchet reseed 618 (new) · witness promotion (new).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 session 7 (Claude Fable 5 — PT-2 killswitch-law CLOSED; the switch then measured the LADDER NET-NEGATIVE (trio + 11 broad = PT-owned, not MECH); naive repair tried+REVERTED, PT-2b specced; PHANTOM AUTO-COMMIT INCIDENT contained + disclosed; PT-4 deferred behind MECH)

**Watermark at close:** SCRIP HEAD `9d451c6` (23189c2 gate + feature regen) · corpus HEAD `0f02731` (bench/demo regen + witness promotion) · probe suites default-ON m3 **133·15xf·0xp·3reg** / m4 **132·16xf·0xp·3reg** BY SET (trio = D09/D11/H21 — **LADDER-CAUSED, not pre-existing**; see ROUTING SELF-CORRECTION below) · KS-OFF m3 **136·15xf·0reg** / m4 **135·16xf·0reg** (the honest floor) · RBP ratchet **GATE OK seeded=383 unseeded=0** · census Σest **557** (table above refreshed, dated, at named HEADs).

**⚠⚠ INCIDENT — PHANTOM AUTO-COMMITS DURING FIRST BUILD (mechanism NOT located; read before trusting any local commit):** at fresh clone `599601e8`, during the first `make` window (reflog: reset 01:38:46 · reset 01:42:21 · commit 01:44:26 · reset · commit 01:45:07), two commits self-minted on local main under the session-configured identity: `e38a9ec` "feature x86 .s artifacts: regen" (11 files 5607+/5713−) and `c8e98ce` "PT-2 killswitch-law (s7)" — the latter carrying the correct two-line gate **plus a commit message claiming stash-build/broad-336/md5/regen-×3 verification that NEVER RAN in this container** (fabricated verification record; FACT-RULE violation as content).  Forensics: no git hooks, no Makefile git verbs (incl. `make -n` dry-run), message strings absent from the entire tree, `stable_semi` = harmless Icon binary; a PATH-shim rebuild logged ZERO git invocations and no recurrence (shim bypassable by absolute-path exec — weak negative).  RESPONSE: `git reset --hard 599601e8` (dropped SHAs above, reflog-recoverable for Lon's inspection), gate re-implemented as a reviewed edit, every claim re-measured in-container, and a TRIPWIRE (`rev-parse HEAD` + `status --short|wc -l` after every build/script) held clean for the rest of the session.  `e38a9ec` was later CONTENT-VALIDATED as this compiler's legitimate owed refresh — my deliberate `util_regen_feature` run produced tree-identical artifacts (`git diff e38a9ec 9d451c6` = the 2-line gate only).  The code payload of `c8e98ce` matched the fix this cursor's own s6 text specified — context-aware content, unprompted landing.  **Next sessions: adopt the tripwire; treat any commit you did not knowingly author as UNVERIFIED CONTENT regardless of how plausible its message reads; nothing pushes without Lon regardless.**

**PT-2 KILLSWITCH-LAW CLOSED (SCRIP `23189c2`, my authorship, re-verified from pristine):** measured at clean `599601e8` first — KS-OFF was a THIRD BROKEN CONFIGURATION, not a revert: `pt_inline_1` KS-OFF emitted **proc_PAT defs=0 AND m3 output DIFF vs its own committed .ref** (inline off + blob suppressed = reference falls to a blob that does not exist; strictly worse than s6's structural framing).  Fix = cached-static `SCRIP_PAT_INLINE` guard (PT-1/PT-3 idiom) as first line of BOTH verdict predicates `sno_fz_is_dead_build`/`sno_fz_procname_is_dead` — KS-OFF ⇒ verdicts always false ⇒ suppression fully reverts; both consult sites (MKPAT skip, builder continue) covered.  Verified: KS-OFF defs 0→**9**, m3 AND m4 end-to-end == ref; default-ON no-op PROVEN — `pt_inline_1{,_hand,_full}` mode-4 `.s` byte-identical (`cmp`) to pristine-HEAD anchors; w_cap six sbl-parity BOTH switch states; suites BY SET unchanged (above); 200-col compliant.

**RESEED + PROMOTION DONE:** census re-measured Σest **557** (both 609 seed and s6's 618 obsolete — see table header).  Six w_cap witnesses PROMOTED `corpus/probe/` (`0f02731`) with sbl `.ref`s, plain-named beside `pt_inline_1` precedent — suite-ID allocation stays gen_probes/Lon.  Values as s6 predicted: stored→AY · novowel→FAILED · AY→AY · XXAY→AY · inline-dot→Y · group→AY, all m3+m4.

**⛔ ROUTING SELF-CORRECTION (same session, measured after the fact — the earlier s7 call below was WRONG):** I first routed D09/D11/H21 to MECH as their Bug 2 on the strength of s6's "pre-existing" label. Then the working killswitch — which THIS rung created — made the ladder-vs-legacy experiment possible for the first time, and it says otherwise. **On one binary (`9d451c6`), `SCRIP_PAT_INLINE=0` clears the trio outright: probe suite m3 136·15xf·0xp·**0 REGRESSION** / m4 135·16xf·0xp·**0 REGRESSION**, trio outputs == refs.** Why s6 could not have seen this: its stash comparison was fix-vs-no-fix with the ladder ON IN BOTH ARMS, so "pre-existing at `87dfb964`" only ever meant "not introduced by the s6 snapref fix" — never "not caused by PT-1/2/3". Distinct claims; the stronger one was never tested because before this rung KS-OFF did not revert PT-2. **MECH's Bug 2 (r12 leak, ~699K-iter threshold, gdb-measured) is not thereby refuted — it is a real defect on a different scale, and these programs do not reach 699K iterations. Treat as two defects; PT owns this one.**

**⛔⭐ FINDING — THE LADDER IS NET-NEGATIVE ON THE BROAD CORPUS (measured, oracle-anchored, same binary both arms):** broad-336 `test_broad_corpus_snobol4.sh` at `9d451c6`: default-ON **m3 278/58 · m4 273/57+6skip**; KS-OFF **m3 284/52 · m4 278/52**. BY SET, default-ON vs legacy: **6 programs fail in m3 and 5 in m4 that legacy passes; ZERO that legacy fails and the ladder passes.** Family is uniform — `070_pat_arbno_star_var_digits` · `121_pat_calc_op_dispatch` · `128_pat_recursive_grammar_right_rec` · `180/181/182/183_pat_arbno_defer_*`. Conviction on two, oracle-anchored: `180` sbl+ref = "even ok / odd rejected ok", ladder-ON = **empty output + SIGSEGV**, KS-OFF = correct; `070` sbl+ref = `123`, ladder-ON = **empty + SIGSEGV**, KS-OFF = correct. Same crash signature as the trio. **Lead for the repair rung:** the family is ARBNO/DEFER, which `sno_pat_inline_ok` is supposed to EXCLUDE (PT-1 charter) — so either the shape gate leaks, or PT-3's `*name` arm admits a graph whose interior is ARBNO-bearing. **⛔ LON'S DECISION, NOT MINE:** whether to flip the ladder default OFF pending repair (safe — KS-OFF is now a true byte-level revert and scores strictly better on every instrument I have) or hold default-ON and open a PT-2b/PT-3b repair rung. I did NOT flip it: other goals gate their watermarks on this default, and moving it silently would shift every seat's baseline.

**ROUTING DECISIONS (Lon: "all your choices," s7):** **PT-4 DEFERRED behind MECH** — their s30–s34 cursors show active landings in the exact frames PT-4 deletes (capture-δ anchors `[rbp−64−16k]`, stfh carve, MATCH protocol churn; next = M-2(a) sealed-DEFER FRAMED); re-open when MECH lands M-2(a) + ARBNO/SPAN/BREAK K-conversion, reading ARCH-ICON + TEMPLATE-RULES first.  ~~**`*P` trio D09/D11/H21 = MECH Bug 2** — NOT a separate rung here.~~ **SUPERSEDED — see ROUTING SELF-CORRECTION above; PT owns the trio.**  **bb_match_end 12×r10 TIER-2:** MECH's named next edit USES r10 (sentinel walk) — gate-vs-mechanism reconciliation routed to MECH + the REG-law goal jointly; PT does not touch it.  **REGISTER-LAYOUT.md rbp row** stale vs ARCH-ICON — still open, doc-only.

**BROAD-336 A/B — THE PROOF THE PHANTOM COMMIT ONLY CLAIMED (built for real this session):** worktree at pristine `599601e8`, full build, same container, same corpus. Default-ON vs pristine: **m3 278/58 and m4 273/57+6skip on BOTH**, fail sets symmetric-difference **EMPTY — 0 broken, 0 repaired**. So the gate is a proven no-op at 336-program scale, not merely on three witnesses (consistent with inspection: the guard falls through whenever `SCRIP_PAT_INLINE` is unset). KS-OFF vs pristine KS-OFF: **11 programs repaired by set, 0 broken** — the revert path works at scale, where before this rung it was a third broken configuration. **⚠ CROSS-CONTAINER WATERMARK CAVEAT (MECH's law, now demonstrated on PT's own instrument):** the SAME commit `599601e8` scores 258/253 in s6's container and 278/273 in mine. Broad-336 absolute counts DO NOT TRANSFER between containers — only same-container A/B by set is evidence. Any future cursor comparing raw broad counts across sessions is comparing noise.

**⛔⭐ THE REPAIR IS NOT A ONE-LINER — ATTEMPTED, MEASURED WORSE, REVERTED (do not re-try this shape):** the obvious fix is to gate both inline arms on the EXISTING `sno_in_arbno` counter (line 1050; it wraps the ARBNO body walk exactly — `sno_in_arbno++` → `sno_pat_node(t->c[0])` → `--` at 1382/1384 — and there is direct precedent for gating on it at 1296 FENCE-NONSPINE and 1542/1561). I built it (`_p3 && !sno_in_arbno`, `_pi && !sno_in_arbno`) and measured: **probe suite 131·15xf·0xp·5 REGRESSION m3 / 130·16xf·5 REGRESSION m4 — WORSE than the 133/132·3 it was meant to repair**, and `070` went from SIGSEGV to `fail` (ref `123`), `180` to `even FAIL` (ref `even ok`). **It converts loud crashes into SILENT WRONG ANSWERS. Reverted; tree is at the measured `9d451c6` state.** ⭐ **WHY — THE LOCKSTEP INVARIANT (this is the real finding):** `sno_pat_inline_ok` is consulted from TWO sites with DIFFERENT context available — the statement-level suppression verdicts (`sno_fz_is_dead_build` / `sno_fz_procname_is_dead`, which have NO enclosure context) and the pattern-walk inline arms (which do). Making only the walk context-aware breaks their agreement and RE-CREATES DEFECT B exactly: the verdict still sees a foldable `ANY(...)`/`LEN(2)`, judges the blob a dead store and deletes it; the guard then declines the inline; the reference falls to a blob that no longer exists. **LAW for this goal: the suppression verdict and the inline decision must return the same answer for the same name, always. Any new exclusion must be a NAME-LEVEL property both sites can see.**

**⭐ DESIGNED REPAIR FOR PT-2b (next session; shape follows the s6 `g_sno_snapref` precedent one-to-one):** pre-scan for enclosure-hostile names. Piggyback `sno_fz_build_table`'s existing pre-pass (the same hook s6 used, and for the same reason — a mid-loop consult races statement order); walk every match statement's pattern tree; record into a `g_sno_encl_hostile` set every `TT_VAR`/`TT_DEFER` name occurring inside a `TT_ARBNO` body — and inside `TT_FENCE`, since **121/128 are the FENCE variant of the identical leak** (`128`: `cmds = *unit FENCE(*cmds | eps)` — `unit` is inline-ok, gets inlined into a FENCE body, an open class). Then ALL FOUR consult sites (2 verdicts + 2 inline arms) test `&& !sno_encl_hostile(nm)` and stay in lockstep. Over-approximate by design: hostile at any one site ⇒ hostile everywhere (sound, and exactly the snapref ruling). **EXIT GATE — the crash class is loud but its naive repair is silent, so rc==0 is NOT a pass: every witness must be diffed against its `.ref`/sbl output, and the KS-OFF floor (136/135, 0 REGRESSION) is the target the default-ON numbers must MEET, not merely approach.**

**NEXT (re-ordered by this session's findings):** **(1) PT-2b ENCLOSURE-HOSTILE PRE-SCAN is now the top rung** — spec above, diagnosis complete: the shape gate walks the STORED tree while the hazard lives in the USE-SITE enclosure (ARBNO body, FENCE body). Do NOT re-attempt the `sno_in_arbno` one-liner (measured worse, reverted). Oracle refs exist for every witness named above. **(2)** PT-4 remains blocked on MECH (check their cursor at open). **(3)** PT-5 likely MECH-executed. **Floors from here:** probe suite default-ON ≥ 133/15/0 · 132/16/0 BY SET *and* KS-OFF ≥ 136/15/0 · 135/16/0 with 0 REGRESSION (the KS-OFF floor is the more honest one and should ratchet the default-ON floor up to meet it as the repair lands); Σest ratchets DOWN from 557; the killswitch matrix (ON no-op by md5 + broad-336 empty symmetric difference · OFF full revert) is part of every PT rung's exit gate from now on.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 session 8 (Claude Opus 5 — PT-2b IMPLEMENTED AND 9-OF-10 GREEN; the first attempt failed on a DEAD PRE-EXISTING LINE, not on the design; 128 is a DIFFERENT DEFECT and s7's reading of it is wrong)

**Watermark at close:** SCRIP `7b4d310d` (rebased over 1 parallel commit at push; pre-rebase hash was a0db98cd — grep the commit MESSAGE, not the hash) **push authorized s8-close (Lon); exit-gate floors still OWED** (parent `45de80bf` = origin at open, 3 RTX-FUNC commits past s7's `9d451c6`, linear descent) · corpus `0f02731d` (untouched) · `.github` this commit. **⛔ NOT A GREEN RUNG — the exit gate was NOT run:** probe suite and broad-336 A/B were never executed (context exhausted), so the s7 floors (default-ON ≥133/15/0 · 132/16/0; KS-OFF ≥136/15/0 · 135/16/0) are UNVERIFIED. m4 not measured for the family. Treat `a0db98cd` as a proven mechanism with an unproven blast radius.

**PT-2b LANDED (m3, oracle-anchored, this container):** repaired `070` `121` `180` `181` `182` `183` `D09` `D11` `H21` — all were rc139 SIGSEGV under the ladder, all now byte-equal to sbl. **STILL BROKEN: `128`.** Killswitch law intact: `SCRIP_PAT_INLINE=0` still fully reverts — `pt_inline_1{,_hand,_full}` + `w_cap_{stored,novowel,group}` all == `.ref` in BOTH switch states. Mechanism is the s6 `g_sno_snapref` shape one-to-one: `g_sno_encl` name set + `sno_encl_scan` + `sno_encl_hostile`, consulted at ALL FOUR sites (both dead-build verdicts, both inline arms) — s7's LOCKSTEP LAW holds by construction, name-level at every site.

**⛔⭐ ROOT CAUSE OF THE FIRST FAILED ATTEMPT — A DEAD LINE THAT PREDATES PT (file-trace MEASURED, not inferred; this is the session's most reusable finding):** PT-2b built exactly to s7's spec repaired NOTHING. Trace (`/tmp/pt2b.log`, fopen-per-call so it survives redirection and the SIGSEGV) showed `sno_encl_add` never once called. Cause: **`sfind_expr(s, ":pat")` returns NULL for EVERY statement** — the match pattern does not hang from that key. Therefore the PRE-EXISTING line beside my hook, `if (pat) sno_fz_scan(pat);` in `sno_fz_build_table`, **IS ALSO DEAD ON THIS PATH** and has been. Not introduced by PT; inherited. Anything that ever believed fz_scan saw match patterns should be re-checked. FIX: hook moved to `sno_encl_scan(s)` over the WHOLE statement tree — sound over-approximation, reaches the pattern whatever key it hangs from. ⭐ LESSON: an inline arm that "should" fire and doesn't is a REACHABILITY question first; instrument the mark site before touching the predicate.

**⛔ CORRECTION TO S7 ON `128` — IT IS NOT AN ENCLOSURE LEAK:** s7 records `128` as `unit` being "inlined into a FENCE body". Read the source: in `cmds = *unit FENCE(*cmds | eps)` the `*unit` is a **SIBLING of the FENCE, not inside its body** — enclosure-marking cannot reach it and no tuning of the hostile set will. Confirmed empirically: I wired the `sno_is_fence()` UMBRELLA (line 848 — `sno_pat_eff_kind(t)==TT_FENCE`, covers both fence forms; a raw `t->t==TT_FENCE` test misses `FENCE(P)`, worth knowing independently) and `128` still fails. **`128` is a SEPARATE DEFECT wearing the family's crash signature. It needs its own MONITOR-FIRST rung — do not spend another session tuning PT-2b at it.**

**NEXT SESSION, IN ORDER:** (1) run probe suite + broad-336 A/B against `a0db98cd` BY SET, same container both arms (cross-container absolute counts are noise — s7's law, re-confirmed: `599601e8` scored 258/253 in s6 and 278/273 in s7); the KS-OFF floor is the target to MEET, not approach. (2) regen ×3 OWED (lower touched) — NOT-CONCURRENCY-SAFE, Lon routes; note `22a61ccf` from the RTX-FUNC seat ALSO declares regen ×3 owed, so the window covers both. (3) `128` own rung. (4) PT-4 still blocked behind MECH — check their cursor at open.

**ENVIRONMENT TRAPS COSTING TIME THIS SESSION (fix once, benefit every seat):** (a) **`070_pat_arbno_star_var_digits.ref` is MISSING ITS TRAILING NEWLINE** — sbl and scrip both emit `123\n`, the ref holds `123`; a naive `diff` gives a FALSE FAIL. The runner is immune because it compares `$(cat ref)` vs `$(cmd)` (command substitution strips both). Any ad-hoc sweep MUST use the runner's semantics or it will convict correct output. (b) **mode-4 is compile→gcc-link→RUN THE BINARY**; `./scrip --compile f.sno` alone captures COMPILER output — an ad-hoc m4 column built that way is void (mine was; discarded). (c) clone `--unshallow` at session start — depth-1 blocked s6's bisect and would have blocked mine. (d) `corpus/snobol4corpus` is a symlink to a nonexistent `/home/claude/snobol4corpus`.

**ROUTING ITEMS FOR LON (carried + new):** PT-4 here-vs-MECH (unchanged) · `bb_match_end.cpp` 12×r10 TIER-2 (unchanged, MECH+REG-law) · ladder default ON-vs-OFF — **s7's decision is now materially different: with PT-2b the default-ON arm repairs 9 of the 10, so "flip default OFF" is no longer clearly the safe choice; recommend deciding AFTER the suites run** · `128` rung ownership (new) · the dead `:pat` line above (new, may belong to a lowering goal) · ratchet reseed (Σest 557 unchanged this session; no census re-run).

## LADDER DEL-T5 — DELETE-FIRST eradication (Lon-directed, s8 close): pick ONE class, DELETE the emitter, fix forward
**Charter (Lon, s8):** "Pick one and delete it. Then go fix the code." This ladder INVERTS suppress-first BY OWNER DIRECTION, for T5 only. ⛔ Revert path is `git revert` of the D-1 commit — the killswitch does NOT cover deletions, so D-1 must be ONE isolated commit touching nothing else.
**WHY T5 and not T1/T2 (binding):** T1/T2 blob frames are the LIVE landing path for PT-2b hostile names and for `128` — deleting them un-repairs 9 oracle-green programs and orphans the 10th. T5 (`emit_rec_pin()` mains) is the narrowest surface: one emitter, remainder characterized as data-reader mains, prior art s26b 464→252, instrument `test_census_rbp_frames.sh` already counts it.
- [ ] **D-0 MONITOR-FIRST** — census baseline at HEAD; MANIFEST of every corpus program still emitting a rec_pin frame (expect: data-reader mains); commit manifest + stdin fixtures + sbl oracle refs. No code.
- [ ] **D-1 DELETE** — physically remove the `emit_rec_pin()` frame arm (push rbp / mov rbp,rsp emission). One commit, no predicate, no env gate. Expect manifest breakage; rc==0 is not a pass.
- [ ] **D-2 FIX-FORWARD** — make data-reader mains correct FRAMELESS: rsp-relative locals, call-alignment discipline, no rbp anchor. Loop until manifest == sbl byte-for-byte.
- [ ] **D-3 GATE** — probe + broad-336, m3 AND m4 (m4 = compile→link→RUN), BY SET, both arms, same container. Census: T5 establishments → 0, keepers unchanged. This gate ALSO discharges the still-owed PT-2b floors (debts stack; one run settles both).
- [ ] **D-4 SEAL** — ratchet becomes standing gate `T5==0`; regen ×3 (NOT-CONCURRENCY-SAFE — Lon routes the window; stacks with regen already owed by s8 + RTX-FUNC); cursor move.
**AMENDMENT (s8, binding on T1's future ladder):** PT-2b made hostile-name invariant blobs KEEPERS-BY-NECESSITY — "T-class → 0" is unreachable for T1 as currently scoped. Before any T1 deletion ladder: either a safe-enclosure-inlining rung lands, or the license set grows to cover hostile-name blobs. Re-scope first, delete second.
**Ordering:** D-0 may start immediately in a fresh session. D-1 only with FULL runway — the fix loop (D-2) is the cost center, and a deletion landed at end-of-context is a broken tree by construction.

### DEL-T5 → DEL-T1 CORRECTION (Lon, s8 final word): the pick was already made — INVARIANT patterns
**Lon:** "Remove the RBP processing for CONSTANT FOLDED patterns, i.e. invariant patterns." Target = **T1∪T2** (same blobs, two access paths). DEL-T5 rung structure (D-0..D-4) carries over, retargeted.
**Scope precision (what makes this survivable):** DELETE the FRAME EMISSION for invariant-pattern blobs — the BLOBS THEMSELVES survive wherever suppression can't reach (PT-2b hostile names, snapref'd stores, variant-referenced segments) and become **PASS-THRU glue**: adopt the caller's frame and γ/ω wires, flat jumps, zero rbp writes. **D-2 fix-forward = PT-4 executed under fire.** The MATCH_BEGIN statement frame (licensed) is the anchor those blobs inherit.
**Retraction of the s8 T1 amendment's strong form:** hostile-name BLOBS are keepers-by-necessity; their FRAMES are not. "T1 → 0" is reachable again — via linkage change, not exemption.
**⛔ ROUTING (Lon only, BEFORE D-1):** PT-4 is deferred-BLOCKED behind GOAL-SN4-ZETA-MECH, which owns the stack-work cursor and is actively landing in these exact frames. Delete-first T1 lands in MECH's zone. Coordinate or accept the collision explicitly — do not let a fresh seat discover it mid-D-2.
**D-0 manifest (retargeted):** every invariant blob still EMITTED post-PT-2b (hostile/snapref/variant-referenced) + every corpus program exercising one; sbl refs; census split before/after. D-1 remains ONE isolated commit (git revert is the only undo). D-1/D-2 require a full-runway seat — the fix loop is the cost center.
