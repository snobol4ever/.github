# ⛔⭐⭐⭐ ARCH — THE COLLECTOR GUESSES NOTHING: COMPILE-TIME FRAME MAPS AND SAFE POINTS (Lon 2026-09-17, in-chat to ceo; CEO-812)

**Lon's word, verbatim, the morning of 2026-09-17 (landed 09:4x CDT by the ceo's clock), after the ceo confirmed that `gc_heap.c` scans stack words Boehm-style and rewrites the ones that fall inside a heap block:** *"Nope. Can not do that. You must find another way. We must know what each memory structure you are looking at. I thought they were all DESCR. But if they are varying types, then the compiler should be making maps. What do you think? Can you do it?"* — then: *"Can you do it with a compile time map versus a runtime map. If so, do so."* — then: *"If you are choosing safe points, then you can not just say at MATCH_BEGIN and MATCH_END for instance. How will you choose the safe points?"*

This page is the design. It supersedes the conservative arm of `ARCH-GC-PINNED-ALLOCATOR-LIFETIME-CLASSES.md` and the per-cycle no-move alternative the ceo put to Lon in CEO-809, which is REFUSED: no pinning in any form, per allocation or per cycle.

## 0. ⛔⭐⭐⭐⭐ WHAT IS SETTLED AND WHAT IS BEING DISCOVERED — READ THIS BEFORE THE REST OF THE PAGE (ceo, 2026-09-18 20:0x CDT, CEO-900)

**Lon, in-chat to ceo, verbatim: *“Question, how can CTO and CFO be working a design that you have no clue what it will be?”*** **THE ANSWER IS THAT THE DESIGN IS SETTLED AND THE MECHANISMS ARE NOT, AND THIS PAGE DID NOT SAY WHICH WAS WHICH — 188 lines carrying five in-place amendments and three retractions, with no way for a seat to tell in ten seconds what it may build on.** That is the ceo's defect, not the officers', and this block is the cure.

**FROZEN — settled by Lon 2026-09-17, no seat reopens any of it, a landing that contradicts one is reverted on sight:**
1. **The collector guesses nothing.** No conservative scan, no sniff, no word-rewrite on a guess. (§ 2 Rule 2)
2. **Everything on the emitted stack is a DESCR and the only tag is the DESCR's own type field** (§ 7 F1) — ⛔⭐ **RELAXED BY LON 2026-09-19 (CEO-905), verbatim, said three times in one sitting: *“You should [use] static layouts which have been generated by the compiler to solve your problems.”* then *“You do not have to use DESCR for everything on the stack, if static maps which are small can be generated by the compiler.”*** **THE RULE AS IT NOW STANDS: a cell on the emitted stack is EITHER a DESCR read by its tag, OR a raw word whose kind is named by a SMALL STATIC LAYOUT the compiler generated for that frame regime.** The collector still guesses nothing — it reads a tag or it reads a table, never a word's value. The first regime to take a static layout is the SNOBOL4 stored-pattern blob frame of § 6.2b, whose carve cannot grow (§ 6.2c) and whose interior (ARBNO cell, capture SAVE, FENCE1 watermark, the `ZK_PTR_GC` capture stack at +48) is raw words at fixed `rbp` offsets: one static table for that one regime, recognised by shape as § 6.2b already prescribes, unblocks the main-stack and match-span sweeps (5 of the 24 sites) without touching the carve. § 8.5's option is therefore GRANTED, with the counter-argument it carried still true: a static layout is only path-independent where the frame shape is — per regime, per graph — never per path, which is why § 2b's spine cells stay DESCRs unless a box kind's ζ is itself fixed-shape.
3. **No pinning in any form or under any name** — not a flag, not an exempt type, not a skipped block, not an “immortal”. (§ 7 F0, Lon's own word)
4. **An unrooted static holder gets a ROOT, never a different allocator**; and no `malloc`/`calloc`/`realloc`/`free` anywhere in SCRIP. (§ 7 F5 retraction, CEO-832)
5. **The allocator never collects** — it sets `g_gc_pending` and grows the window; collection happens at a safe point. (§ 2 Rule 3)
6. **A safe point is the return of an emitted call into an allocating runtime entry, after the result is stored.** (§ 3)
7. **Maps are per GRAPH and compile-time, found by the frame's CELL, never by a return-PC lookup table.** (§ 6.2, § 6.2d)
8. **THE ACCEPTANCE TEST IS LON'S FIVE CLAUSES AND ALL FIVE MUST READ GREEN ON ONE TREE: all sliding, no pinning, all rooted, all mapped, all working.** (CEO-895)

⛔⭐ **9. WHAT E ACTUALLY WAITS FOR, NARROWED ON LON'S TWO QUESTIONS 2026-09-19 18:0x (ceo CEO-940).** (i) THE SPINE IS NEVER MAPPED. Lon: *"You mention 153 spine sites, but you are not mapping the SPINE since it is not possible."* § 2b settles it in his own earlier words — the spine's shape at a safe point is a path through the graph and no table keyed by PC can name it — so E's precondition on the spine is that every spine word which can hold a heap pointer at a poll IS A TAGGED CELL and every other is proven not to; the § 0 item 2 static-layout relaxation belongs to a fixed-shape REGIME found from a frame's cell (today the blob frame alone), never to the spine at large. The ceo's "classify 153 sites, table or cell" wording is withdrawn. Measured size of the real job (cto's walker): 2–5 live raw-heap spine words per collection on Icon at 4–7 depths, 4–5 on SNOBOL4. (ii) ZERO C→BB IS NOT A COLLECTOR PREREQUISITE. RULES.md line 87's reason is CORRECTNESS — a box addresses its slots as if rsp were its graph's and a C frame under it moves every slot (cto-83's SIGSEGV) — while the COLLECTOR's requirement is only that NO LIVE HEAP REFERENCE EXISTS ONLY IN AN UNMAPPED C FRAME AT A COLLECTION POINT. Zero entries buys that globally; a C frame that parks its references on a rooted record and re-reads them after buys it per site (the cfo's `c_rt_cap_open` is the standing precedent). So E is decoupled from the last entry, the Raku road's 8,000,486 included; what E needs from the twelve remaining sites is the property, and the ceo owes the list with each site's answer. The ZERO target stands on its own reason. ⭐ **(iii) THE PROPERTY IS MEASURED AND IT HOLDS — E MAY CUT (ceo CEO-942, source reading on `f0368fb08`+, 2026-09-19 18:4x).** The test: a frame is exposed only if it USES, after the entry returns, a heap pointer it held before it. All thirteen declared caller sites plus the three riders pass — nine tail-return or use only the return value; the three `core.c` trace callbacks restore only scalars after the entry, with no safe point between their `rt_heap_strdup_c` and APPLY's staging because the allocator never collects; `runtime_eval.c`'s five pass CODE pointers, and `DT_C` code blocks live in the ct arena, which `ct_alloc` maps for itself and `gc_heap.c` never mentions, so they never move; the resume rider's `g` is a ct block and its `hslot` points into a mapped emitted frame. `core.c:2382` is a census FALSE POSITIVE (`g_user_call_hook = _usercall_hook` takes an address, not a call), so the honest outbound count is 12. ⛔ THE BOUND: a DECLARED set is a measurement with a stated bound, never a population, so a fifth way out would be unmeasured — the walker is the empirical check over whatever set exists, since a C frame's words lie between cells and land in its `s_raw_heap`/`a_heap` counts (2–5 per collection on Icon, 4–5 on SNOBOL4, each named by a per-site line). Source reading + per-site lines = the licence of § 6.2h. E therefore waits on the spine's tagged cells and on nothing else.

**BEING DISCOVERED BY MEASUREMENT — expect these to move, and expect the seat doing the work to move them:** the register facts at a poll (§ 6.5a superseded § 6.5's route, § 6.5c superseded § 6.5's floor); the denominator itself (CTO-79 took `unpolled` 97 → 123 by finding a TAIL JUMP is an edge); the blob-frame carve contract (§ 6.2b/6.2c, cure built and withdrawn); the `HB_WS` split by allocation site (§ 7 F5); what the spine actually costs (F2 has a ceiling, not a reading).

⛔ **THE REAL DEFECT LON'S QUESTION IS POINTING AT, NAMED RATHER THAN DEFENDED: EVERY CORRECTION TODAY WAS FOUND BY THE SEAT DOING THE WORK, NOT BY THE SEAT THAT OWNS THE PAGE.** § 6.5a, § 6.5b, § 6.5c, CTO-79's denominator, and the ceo's own inline-allocation claim in § 8.1 — five corrections in one day to a page marked FROZEN, four of them from the cto and one from the ceo correcting itself an hour after writing it. **That is the NO SECOND READER hazard MODE line 2 already names, showing up exactly where it was predicted.** The officers are not building blind: items 1–8 held all day and every correction was INSIDE a mechanism, never to the design. But the ceo was ratifying after the fact instead of reading ahead of it, and ⭐ **the cto's own lesson from CTO-79 is the general cure and it applies to this page too: *a derivation with one reader is never cross-checked.*** An architecture page with one author is the same object.

## 1. What is on the stack today, and why guessing became corruption

Three kinds of memory, measured at SCRIP `db4a6b1fc`:

1. **Zeta slots in emitted frames** — ζ-ACTIVATION-FRAME on RBP, ζ-SPINE on RSP. The compiler lays every slot out in `src/ir/frame_layout.c` and records its kind in `zls_field_t.kind`: `ZK_DESCR` (a 16-byte descriptor), `ZK_PTR_GC` (a raw 8-byte pointer into the collected heap: bucket vectors, field arrays, array data), `ZK_RAW` (counters, cursors, watermarks, saved rsp — never a pointer). 64 registrations today. The compiler knows every slot's kind and never tells the collector.
2. **The runtime's own C frames** — locals of `rt_*` functions holding a raw heap pointer across a call that can collect. The 24-site register-spill class (CEO-803) and hq_prolog's twelve stress SIGSEGVs are this kind.
3. **Registered slots** — coexpressions (`rt_coexpr.c`), the Prolog trail (`rt_pl_trail.c`), `rtcc_init.c`: precise, registered through `rt_gc_slot_reg`/`rt_gc_root_range_add`. This path is correct and is the model.

`gc_zeta_frame` (gc_heap.c:557–563) walks every registered range word by word: 16 bytes that parse as a valid descriptor are visited precisely; otherwise each 8-byte word is handed to `gc_blk_of`, and any word inside a heap block is MARKED and its slot REGISTERED FOR REWRITE when the block slides. That is the Boehm guess, and worse than Boehm: Boehm never moves, so a wrong guess leaks; here a wrong guess moves the block and overwrites a word that was an integer or the length-and-tag half of a descriptor. Under the pinned class the guessed blocks did not move, so the guess cost nothing; deleting pinning (CEO-803, `3b79683b5`) turned the leak into gc2. hq_icon measured it: 100–270 guessed words rewritten per collection on gc2; under `setarch -R env -i` with equal-length knobs the verdict is a function of the stack offset alone (BAD over a band of 32 of 64 offsets; a mark-only arm halves the band to 34..49 and cannot close it, because mark-only stops relocating genuine roots too). The poison detector does not reach this defect (0 of 36): a block that slid DOWN leaves its old bytes inside live data.

## 2. The design: compile-time maps, and the allocator never collects

**Rule 1 — the map is compile-time data.** For every safe point (§ 3) the emitter writes a static table entry beside the code, in BOTH media (in the sealed mode-3 slab; in `.rodata` under a label in mode 4): `{ return-PC, spine depth at that PC, [ (offset, kind) for every live slot of the RBP frame and the RSP spine at that PC ], [ callee-saved registers that hold a ZK_DESCR/ZK_PTR_GC at that PC ] }`, generated from `zls_field_t` and the emitter's own knowledge of which registers it assigned. Nothing is built at run time; there is no registration call per activation. Keyed by return PC, the collector finds each frame by walking the RBP chain (the runtime is `-fno-omit-frame-pointer`; emitted activation frames are RBP frames by the BB FRAME-PLACEMENT CRITERION) and binary-searches the table.

**Rule 2 — the collector walks maps, not words.** `gc_collect_ex` visits exactly the slots the map names: `ZK_DESCR` through `rt_gc_visit_descr`, `ZK_PTR_GC` through `rt_gc_visit_raw`, `ZK_RAW` never. The word scan, the 16-byte descriptor sniff, `cons_stack` and the `rt_cas_live_span` byte scan are DELETED, not gated. Coexpression stacks are chains of mapped frames walked from the coexpression's saved RBP. Registered slots (kind 3 above) stay precise roots.

**Rule 3 — the allocator never collects.** Today `rt_gcheap_alloc` (gc_heap.c:206–207) calls `rt_gc_collect` from inside the runtime, with every runtime C frame live and unmapped — which is the only reason kind 2 was ever scanned. Under this design the allocator sets `g_gc_pending` (it already exists and is already set by the stress and budget pacers) and, when the line is crossed, GROWS the arena window rather than collecting; the collection runs at the next safe point, where the only live C frame is the poll's own call into the collector. Therefore no runtime C frame is ever scanned, and the 24-site spill class and the twelve SIGSEGVs are cured by construction rather than site by site. The arena must be growable to the next safe point; the bound is one runtime call's own allocation (a `DUPL` of a gigabyte grows the window by a gigabyte and the next safe point compacts).

**Rule 4 — the one residual, enumerable.** A runtime C frame that CALLS BACK into emitted code (`by_name_dispatch.c`, `gen_runtime.c`: APPLY, EVAL, by-name calls, sort comparators, generator resumption) has a safe point BELOW it on the stack. Such a frame may not hold a raw heap pointer across the callback: it registers the slot precisely (the coexpression way) or re-reads through a handle after the callback returns. These sites are found by grep of the callback entry points, not by scanning; a gate censuses them.

## 2b. AMENDED THE SAME MORNING — THE SPINE IS PATH-DEPENDENT, SO THE SPINE CARRIES ITS OWN TYPES; AND THE HEAP INTERIORS ARE THE SAME PROBLEM ONE LEVEL DOWN (Lon + the cfo, 2026-09-17 ~10:00 CDT; CEO-815)

**Lon, verbatim:** *"I do not suspect that you will be able to create a map since the BB that paint the top of the stack goes back and forth via multiple graph traversals of the BB box. Do you see the problem in which I refer? The alpha/omega transitions would have to be known, i.e. the path would have to be known at the time GC began and you would have a graph not just a linear map."* He is right, and Rule 1 as written above is WRONG for the ζ-SPINE: a per-PC map describes only the box that is executing. What lies below it on the RSP spine is the residue of every box that suspended on the way — after an alternation the spine holds arm A's records or arm B's, at the same PC; ARBNO leaves one record per iteration; a recursive procedure interleaves activation frames. The spine's shape at a safe point is a function of the α/β/γ/ω history, a path through the graph, and no static table keyed by PC can name it.

**The cure is not to know the path; it is to make the spine describe itself, cell by cell — Lon's "I thought they were all DESCR" made true.** Rule 1 is replaced:

- **Rule 1a — THE ζ-SPINE IS TAGGED CELLS ONLY.** Every spine cell is a 16-byte descriptor-shaped cell whose tag word (`d->v`) is always valid: `DT_S`/`DT_N`… for values (the collector visits `d->s` precisely, exactly as `rt_gc_visit_descr` does today), and a `DT_RAW` tag for every counter, cursor, watermark, saved RSP and code pointer a box keeps (`ZK_RAW`, `ZK_PTR_CODE` — 95 of the 120 `zls_field` registrations today; the payload rides in the cell's `s`/`slen` words and is never visited). A box writes a cell's tag when it pushes the cell, before anything can collect. The collector walks the spine from RSP to its base as an array of cells, reading tags: precise, path-independent, no map, no sniff, no guess. `ZK_PTR_GC` raw pointers on the spine (7 registrations: bucket vectors, field arrays) become `DT_A`/`DT_T`-tagged cells or move to the RBP frame.
- **Rule 1b — THE RBP ACTIVATION FRAMES ARE MAPPED PER CALL SITE** as § 2 Rule 1 says: a call frame is path-independent within itself (one layout per graph, one return PC per call), so the emitter's compile-time table keyed by return PC over the RBP chain is exactly right THERE, and only there. Where a frame slot is itself a tagged cell, the map may say so and the collector reads the tag.
- **Rule 2b — THE HEAP IS TYPED VISITORS, NEVER A WORD SCAN (the cfo's gap, measured at gc_heap.c:619–621):** the mark worklist hands every `HB_WS`, `HB_DINST`, `HB_ARR` and `HB_PLJ` payload to the same `gc_zeta_frame` word walker — a heap-interior sniff with the same raw-word rewrite (hq_raku's `DATINST_t.type` reading 0xdbdbdbdb after a slide is that scan skipping a slot inside an `HB_DINST` block, not a stack root; `SCRIP_GC_COVERAGE` counts ranges, not worklist payloads, so it would read 0 while this remains). Every block kind gets a TYPED visitor: `HB_DINST` = one type pointer plus n descriptors (already the layout), `HB_ARR` = proto + data (precise through `DT_A`), `HB_PLJ` = the trail's registered layout; `HB_WS` — the raw workspace `rt_ws_alloc` hands to ~336 sites for mixed use — has no layout and is SPLIT BY ALLOCATION SITE into kinds that do (a per-site kind at the allocator is the heap twin of the frame map); `hb_scan_interior` is deleted with the stack scan.

**What this does to the rows:** the cto's load-bearing row becomes the SPINE-CELL DISCIPLINE (every spine cell pushed with a valid tag; `ZK_RAW`/`ZK_PTR_CODE` payloads in `DT_RAW` cells; `ZK_PTR_GC` re-shaped; the per-PC table kept for RBP frames only); the cfo's collector row walks the spine by tags and the RBP chain by map and the heap by typed visitors; the `HB_WS` split is a new row (the cfo's, the runtime side); the coo's coverage counts worklist payloads too. Cost to measure, not assume: a tag store per pushed cell on the pattern-match hot path, and spine growth where an 8-byte raw word becomes a 16-byte cell (two raw words share one cell where they are pushed together, as `span.cnt/cur`, `bal.n/start/depth`, `to.I counter/limit` already are).

## 2c. ⛔⭐⭐ A STABLE ROOT HOLDING MOVABLE POINTERS IS NOT AN EVASION — THE PASCAL HEAP TABLE, WRITTEN DOWN SO IT IS NOT REOPENED A THIRD TIME (hq_pascal, 2026-09-20; ceo CEO-1021 on the retraction of CEO-1017 by CEO-1018)

⛔ **THE TABLE LIVES IN `ct_arena`, A BUMP ALLOCATOR THAT IS NEVER COLLECTED AND NEVER MOVED, WHILE THE PAYLOADS ARE ORDINARY MOVABLE COLLECTED BLOCKS.** `new(p)` is rewritten by the parser at `pascal.y:264` into `p := __pas_alloc_rec(nfields)`; the runtime takes an integer index from `pas_heap_take`, and the record's field payload is a `rt_str_alloc` string — `c_rt_str_alloc` at `gc_heap.c:267`, i.e. `rt_gcheap_alloc` with `DT_S`, **the collected sliding heap** — parked in a `DESCR_t` cell of `g_pas_heap` (`by_name_dispatch.c:729`).

⭐ **THE SPLIT IS FORCED BY THE DESIGN AND IS THE OPPOSITE OF PINNING: a Pascal pointer VALUE is an integer handle INTO the table, so the table must be stable and the collector must fix up THE CELLS rather than the handles.** Moving the table into the arena would invalidate every live Pascal pointer in the program; leaving the payloads unmoved would be pinning. A stable root holding movable pointers is the only shape that is neither. ⛔ **And `ct_grow` is NOT `malloc` — `by_name_dispatch.c` contains zero `malloc`/`calloc`/`realloc`/`free`, so this is CEO-832 clean.** (`test_gate_pas_heap_table_is_a_movable_root.sh`'s header said "plain malloc" until 2026-09-20 and was corrected at SCRIP `88af2305d`.)

⛔ **THE ROOT IS REGISTERED AND COMPLETE: `pas_gc_roots` (`by_name_dispatch.c:776`) walks `n = 1..top` over `g_pas_heap` calling `rt_gc_visit_descr` on every cell, and visits all 512 `g_pas_tf` text-file buffers in the same pass; the collector calls it at `gc_heap.c:1085` beside `core_gc_roots`, `gen_gc_roots` and `pl_gc_roots`.** Those two statics are the ONLY Pascal runtime `DESCR` holders. Evidence is the gate, run and not read: **m3 and m4, plain and forced, byte-identical to `fpc -Miso`, 51 regenerations reclaiming 3232016 bytes, high-water pinned 0 blocks.**

⭐⭐ **WHY THIS SECTION EXISTS AT ALL, AND IT IS THE GENERAL LESSON: TWO SEATS INDEPENDENTLY READ THIS TABLE AS UNROOTED FROM A TRUE GREP.** `grep pas_heap gc_heap.c` returns **0**, because Pascal's root walk lives in `by_name_dispatch.c` — **the same file as the declaration** — so the collector never had to name it in `gc_heap.c`. A widened search that excluded the declaring file (so as not to match the declaration) **deleted the visitor with it**. ⛔ **THE INSTRUMENT THAT ANSWERED CORRECTLY WAS THE GATE INDEX, NOT THE SOURCE: `test_gate_pas_heap_table_is_a_movable_root.sh` names the invariant in its FILENAME.** A grep for a symbol cannot find a visitor spelled differently from the thing it visits; a gate whose name states the invariant can. **Check the gate index before concluding an invariant is unheld.**

## 3. Lon's question: how are the safe points chosen?

Not by box. `MATCH_BEGIN`, `MATCH_END` and every other box kind are irrelevant to the choice. **A safe point is exactly the return point of an emitted call into a runtime entry that can allocate, placed AFTER the call's result has been stored to its mapped slot** — and nowhere else. The reasons:

- **It is where collection is needed.** Memory is consumed only inside allocating runtime calls; a box that never allocates never needs a safe point; a program that allocates nothing never collects. Loop back-edges need no poll because a loop that allocates passes a safe point every iteration by construction.
- **It is where the map is knowable.** At a call's return PC the spine depth is a compile-time constant (RESULT/LOCALS live on the RSP spine only where every consumer reaches them at a fixed compile-time offset — the FRAME-PLACEMENT CRITERION already guarantees it), the RBP frame's layout is `zls_field_t`, caller-saved registers are dead across the call, and the callee-saved registers the emitter assigned are known. Storing the result before the poll means the fresh pointer is in a mapped slot, never only in `rax`.
- **It is cheap.** The poll is `cmp dword [rip+g_gc_pending], 0 ; jne slow` — the shape `bb_call_proc_staged.cpp` already emits at the staged call boundary — beside a call that just allocated. The slow path spills the callee-saved registers to a known frame and calls the collector with the map key.
- **It is complete.** Because Rule 3 removes every other collection site, the set of safe points IS the set of allocating call returns; a runtime entry that allocates and has no poll at its return is the gate's red (a census over the runtime's allocating entry points against the emitter's poll sites), and the residual of Rule 4 is the only other place a collection can begin.

The compiler already annotates one field *"dead at safe points"* (frame_layout.c:107): the notion exists; this design makes it the whole root policy.

## 3b. ⛔⭐⭐⭐ THE "INTO A MAPPED SLOT" CLAUSE IS ALREADY IN § 3 AND NOTHING HAS EVER ENFORCED IT — ONE WITNESS, ONE CENSUS AND ONE LAW READING CONVERGED ON THE SAME POPULATION IN ONE HOUR (hq_snobol4 + the cfo + hq_raku, 2026-09-20 12:3x–13:3x CDT; ceo CEO-996, landed the same sitting)

⛔ **THE CORRECTION TO THE ASK ITSELF, AND IT IS THE ceo's, because the cfo routed this as a MISSING clause and it is not missing.** § 3 above already reads *"placed AFTER the call's result has been stored to its mapped slot"* and its third bullet already says *"the fresh pointer is in a mapped slot, never only in `rax`."* **The clause is present, correct, and load-bearing; what does not exist is anything that makes it true.** That distinction is the whole finding: a design sentence stating a property the emitter is never checked against is indistinguishable, to every reader and every instrument, from a property the emitter has. This is § 6.2j's lesson one level up — *a register plane is documented in a table and enforced nowhere* — and it cost the same way.

⭐ **THE WITNESS THAT PROVED IT (hq_snobol4, `FINDING-2026-09-20-hq_snobol4-the-residual-is-a-spine-slot-24-bytes-below-its-frame-maps-region…`), which OBEYS § 3 TO THE LETTER AND STILL LOSES ITS VALUE.** `SNO$MKEXPR` (`by_name_dispatch.c:7958`) mints a `DT_X` whose `xd.s` is `rt_heap_strdup_c`'d onto the GC heap. The emitted window at the call site is `call rt_call_arr_bl` / `mov [rsp+0],rax` / `mov [rsp+8],rdx` / `call rt_gc_poll` — **the store precedes the poll exactly as § 3 requires.** The cfo compiled it and measured where the store lands: a scratch cell at the bottom of the live spine, **32 bytes below the value region's base**, with `emit.cpp:3195` emitting main's cell as `emit_gc_map_cell(_rg, _rg+16, 0, ROOT, 1)` and the dump reading `frame_bytes=432 map_off=416`. **THE MAP IS CORRECT ABOUT THE REGION IT DESCRIBES. The planner never gave the call result a value-region slot at all**, so no map could cover it and growing the region would not help. The collector sweeps the block, the arena re-issues it, the defer road resolves a garbage name, `NV_GET_fn` returns the null string, the null pattern matches empty, and **the program prints a plausible wrong answer with no crash and no diagnostic.** `SCRIP_GC_MAPS=1` prints the lost word itself: `off=-24 word=0x74ee2fe0c7d0 type=215`.

⛔ **THE ANSWER TO hq_snobol4's QUESTION — "map defect or missing region?" — IS NEITHER, AND THE THIRD ANSWER IS THE ONE THAT GENERALISES: A STORE INTO A SLOT THE PLANNER NEVER MAPPED.** The cure is not in the map and not in the region bound; it is in the PLANNER, which must give every call result whose value can be live across the poll a mapped slot, or the emitter must refuse to place a safe point there. ⛔ **§ 7 IS FROZEN AND THIS AMENDS § 3, NOT § 7**: it adds no mechanism and changes no design decision, it states the enforcement obligation the existing clause always implied.

⭐⭐ **AND hq_raku GENERALISED IT FURTHER THAN THE WITNESS DID, WHICH IS WHY IT IS A POPULATION AND NOT A SITE: a graph the compiler REFUSES A FRAME LAYOUT FOR HAS NO FRAME MAP AT ALL, so EVERY leaf boundary inside it stores into an unmapped slot BY CONSTRUCTION.** The `no_layout` census is therefore **a CORRECTNESS population and not only the denominator hole the ceo called it at CEO-993** — first side twice over. ⛔ **THE NUMBER IS 42, NOT 72, AND THE CORRECTION IS THE CTO'S OWN INSTRUMENT (SCRIP `83fb80ee7`):** thirty of the seventy-two were the census writing each entry into a bare temp dir without the companion files the harness copies, so every entry with an include directive read as a compile refusal. 72 → 42, graded 4851 → 4881, and the consumer that had printed *"NAMED AND UNCOUNTED"* since CEO-749 **while printing no name** now prints every one or refuses. The 42 are **raku 31** (nineteen share the statement-prefix stem — likely ONE construct, not nineteen defects), **prolog 10** (honest ladder refusals for rungs 6/8/10, measured by hq_prolog and confirmed by the cto from the other side), **snobol4 1**.

⛔⭐ **THE CLASS IS WIDER THAN `no_layout` AND THE WITNESS IS THE PROOF: `main` HAS a layout and still stored into an unmapped slot.** So the census that grades this is *call results stored into unmapped slots*, per language, **naming every member** — never `no_layout` alone, and never a count. Routed 2026-09-20: hq_raku the 31, hq_prolog the 10, hq_snobol4 the SNOBOL4 share including the wider class their witness opens.

⛔ **WHAT THIS POPULATION IS NOT, measured and closed the same afternoon so nobody re-derives it.** (a) It is **not** the fifth-return-class gap: the cfo raised `GC_RET_OUTPARAM` (CFO-123) and **withdrew it themselves** on the correct ground — `c_rt_gcheap_alloc` NEVER COLLECTS, so no collection can run while a C chain holds a descriptor in a C local; at the instant of any collection the out-argument target is either in an emitted activation frame covered by its map, or dead. hq_snobol4 independently refuted it from the evidence side: **eight sibling witnesses over four other out-parameter writers (`bn_dupl`, `bn_trim`, `bn_substr`, `bn_replace`), 136 arms over a 17-point stress band, ZERO RED**, sharing the same unrooted `out` local, the same allocator and a byte-identical emitted window — the only difference being out-of-map spine words, 8 for the witness and 0 for all four siblings. (b) It is **not** readable as *"a negative `off` carrying a heap word"*: the coo proposed that as a gate red and **withdrew it within the hour** when the cfo measured that `gc_walk_range` clamps `base` to at least `p`, so **negative is where spine words live by construction** — that rule would have reddened `test_gate_gc_maps_reporter_runs_beside_the_scan.sh` on origin over its own ratcheted residual, a word the cto has since proven DEAD by setting a read watchpoint and a write watchpoint together at the collection (first touch afterwards is a WRITE, never a READ; SCRIP `e067b9ce9`).

⭐ **THE GRADING RULE THAT FALLS OUT, ruled the same sitting (CEO-997, on the coo's ask): the GC census arms on the by-name and defer roads grade by ORACLE DIFF, never by `rc`.** CEO-556 already holds that an rc-based predicate cannot tell a cure from a silencing; this road proves the sharper case — **it cannot tell a right answer from a plausible wrong one**, because the failure mode is a quiet whole-match failure that exits 0.

## 3c. ⛔⭐⭐⭐ THE FOURTH STORAGE CLASS: A RAW HEAP POINTER GOES HOME IN A REGISTER PAIR NO MAP COVERS — AND IT REVERSES A ceo ROUTING (the cfo, CFO-134, 2026-09-20 13:4x CDT; ceo CEO-1006, landed the same sitting)
## 9. ⛔⭐⭐⭐⭐ THE AUTOMATIC FLUSH: TURN EVERY SILENT WRONG ANSWER INTO A LOCATED CRASH (Lon 2026-09-21, in-chat to the ceo, verbatim: *“Now you must discover a technique to automatically flush out the GC … We do not want to flush out anything. We want things like our sync-step IPC monitor which is iron-clad and guaranteed to self measure and self correct.”*; ceo CEO-1027)

⛔ **THE PROBLEM STATED EXACTLY: EVERY DEFECT WE FOUND IN THREE DAYS WAS FOUND BY HAND.** A seat writes a witness, runs it at stress, diffs it against an oracle. That is flushing, and Lon is right that it does not scale: a lost root produces a PLAUSIBLE WRONG ANSWER with exit 0, so no `rc`-shaped check sees it, and the only reason we know about `g_fh`, `SNO$MKEXPR` or the 36 Raku programs is that a seat went looking. **THE COLLECTOR ALREADY KNOWS EVERYTHING NEEDED TO CATCH ALL OF THEM AT THE MOMENT OF COLLECTION. IT SIMPLY DOES NOT CHECK.**

⭐⭐⭐ **RUNG 1 — POISON MUST TRAP, NOT MERELY LIE. THIS IS THE WHOLE GAME AND IT IS NEARLY FREE.** `SCRIP_GC_POISON` ALREADY EXISTS AND IS ALREADY ON BY DEFAULT (`gc_heap.c:1156`): after a slide it fills the vacated tail with `0xDB`. ⛔ **AND `0xDB`-FILLED MEMORY IS STILL MAPPED, READABLE AND WRITABLE, SO A STALE POINTER READ RETURNS PLAUSIBLE GARBAGE INSTEAD OF FAULTING** — a garbage string is still a string, a garbage tag is still a tag, and the program prints a wrong answer and exits 0. `0xDB` appears exactly ONCE in the whole tree, at the `memset` that writes it: **NOTHING EVER READ-CHECKS THE POISON.** We have been writing a tripwire and never attaching it to anything. **THE CURE: `mprotect(PROT_NONE)` THE VACATED PAGES INSTEAD OF TRUSTING A BYTE PATTERN.** The machinery is already in this file — the arena is reserved `PROT_NONE` at `gc_heap.c:164` and committed as needed, so protect-and-unprotect is a technique the collector already uses, just never pointed at vacated space. **A STALE READ THEN SIGSEGVs AT THE EXACT INSTRUCTION THAT USED THE STALE POINTER**, and `libscrip_rt.so` already installs a real SIGSEGV handler (`rt/rt_stack_overflow.c`). ⛔ HONEST LIMIT, STATED SO NOBODY OVERSELLS IT: `mprotect` granularity is a PAGE, so only pages ENTIRELY vacated can be protected and a sub-page tail keeps `0xDB`. That still converts the large majority of this class, and the quarantine can be widened by declining to re-commit vacated pages for N collections. **hq_snobol4's witness would stop printing `NOMATCH` and start naming the instruction.**

⭐⭐ **RUNG 2 — THE BLOCK BIRTH LEDGER, SO THE CRASH NAMES THE DEFECT RATHER THAN THE ADDRESS.** The header is sixteen bytes and must not grow: `rt_hblk_t { uint64_t fwd; uint32_t size; uint16_t type; uint16_t flags; }` — **CHANGING THE BLOCK LAYOUT CHANGES THE THING WE ARE TESTING**, which is the instrument-observes-itself trap. So the ledger is a SIDE TABLE keyed by serial, populated only under the debug knob: allocation serial, the allocating return address (`rt_alloc_hist_ra` already captures one), the type at birth, and the address the block occupied after each collection. The SIGSEGV handler then prints **“block #12345, DT_X, allocated by `rt_heap_strdup_c` from `SNO$MKEXPR`, vacated during collection #67, was at 0x…, now at 0x…”** — which is the finding hq_snobol4 spent a sitting deriving, produced automatically at the instant of the fault.

⛔⭐⭐ **RUNG 3 — THE DIFFERENTIAL ROOT AUDITOR, AND IT IS THE ONE THAT NEEDS LON'S WORD BECAUSE IT BRUSHES § 7.** Pass A is the exact typed walk that ships and DECIDES. Pass B is a deliberately over-approximate conservative scan that finds every word which LOOKS LIKE a heap pointer. **A block that B finds referenced and A did not mark is a CANDIDATE LOST ROOT, named with the referencing address.** Run the corpus under it and the unrooted set falls out without anyone writing a witness — `g_fh` and its thirteen siblings would have printed themselves. ⛔ **§ 7 FORBIDS A CONSERVATIVE SCAN, AND THIS IS AN AUDITOR THAT CANNOT CHANGE A COLLECTION DECISION — pass A still decides, B only reports. THE ceo READS THAT AS A DIFFERENT THING FROM CONSERVATIVE COLLECTION AND WILL NOT BUILD IT ON THAT READING ALONE: § 7 IS FROZEN AND THIS IS ASKED OF LON, NOT ASSUMED.** Its false-positive rate is real (a dead word that looks like a pointer) and is itself informative: the filter is whether the referencing word lives somewhere that SHOULD have been walked, which is exactly the unmapped-slot class.

⭐ **RUNG 4 — THE STORE-SITE LEDGER, WHICH NAMES THE ORIGIN RATHER THAN THE SYMPTOM.** Under the debug knob every store of a heap pointer records *this address now holds block B*. At each collection **every recorded holder must be VISITED or PROVABLY DEAD; one that is neither is a lost root named with the STORE SITE that created it.** That is strictly stronger than rungs 1–3, which name where a defect was felt; this names where it was made. It is also the most invasive, so it lands last and only if the earlier rungs leave a residue.

⭐⭐ **RUNG 5 — THE REFEREE, WHICH IS LON'S SYNC-STEP ANALOGY MADE LITERAL.** The IPC sync-step monitor is iron-clad because an INDEPENDENT OBSERVER refuses when a participant does not behave (`test_gate_monitor_refuses_when_a_participant_never_starts.sh`). Apply the same shape: in debug mode every collection is checked by an independent pass, and **DISAGREEMENT IS rc=2 REFUSE, never a silent continue.** ⛔ **AND THE “SELF-CORRECT” HALF IS NOT WHAT IT SOUNDS LIKE: THE INSTRUMENT MUST NEVER REPAIR THE HEAP**, because a repaired heap hides the defect and ships it. **WHAT IT CORRECTS IS THE VERDICT** — a run that would have printed a plausible wrong answer instead REFUSES with the block named. **TURNING DARK INTO RED IS THE CORRECTION** (CEO-582: DARK is worse than RED).

⛔ **WHAT ALREADY EXISTS, SO NOBODY REBUILDS IT: `SCRIP_GC_POISON` (on by default, unchecked), `SCRIP_GC_AUDIT_SLOTS` (scan-save heap/unrooted counts), `SCRIP_GC_COVERAGE`, `SCRIP_GC_MAPS`/`_DUMP`/`_LOG`, `SCRIP_GC_STRESS`, the plant knobs, `rt_alloc_hist_ra`, `[GC-SPINE-LOST]` (the cfo, re-reads raw spine words after the mark drain), and `rt_gcheap_verify()` which already runs every collection.** The pieces are there and scattered; **not one of them turns a silent wrong answer into a loud located one**, which is the single property the whole class needs and the only one this section adds.


⛔ **CEO-996 POINT FOUR ASKED THE cfo WHETHER THE ROAD REACHING `gen_runtime.c:69`'s in-C `rt_gc_point` STILL REACHES AN EMITTED SAFE POINT, AND INSTRUCTED THEM TO DELETE THE POLL IF IT DID. THE ANSWER IS NO, THE POLL IS LOAD-BEARING, AND THEY STOPPED AND REPORTED RATHER THAN CUTTING — WHICH IS THE INSTRUCTION WORKING, NOT FAILING.** **A/B/A ON ONE LINE IN ONE FILE** at `SCRIP_HEAP_MB=1`, stress 0/1/3/7, probe-before-belief first (gdb pending breakpoints, hits counted without stopping over a 3-iteration integer-scan witness: `descr_to_str_fracdigit` 3, `rt_scan_enter` 3, `rt_gc_point` 3 — the site is genuinely reached): **POLL PRESENT 217876 / 217876 / 217876 / 217876. POLL DELETED 217863 / 37904 / 132636 / 174312. REVERTED 217876 × 4.** ⛔ **STRESS 0 CHANGES TOO, so a natural collection at the mandatory arena loses the data and this is not a stress-plant artifact.**

⭐⭐ **THE MECHANISM IS § 3b's CLAUSE ARRIVING FROM THE REGISTER SIDE ON A SECOND INDEPENDENT ROAD.** `rt_scan_enter` rebuilds a DESCR from two register words, converts an integer subject with `descr_to_str_fracdigit` — **which allocates** — polls, parks the pointer in `scan_subj`, and then **RETURNS `ScanSubjRegs { ptr, len }`: A RAW HEAP POINTER IN A REGISTER PAIR.** The allocator never collects, so no collection can run between the allocation and `scan_subj = s`; deleting the poll therefore MOVES that collection to after the return, and at that instant `scan_subj` IS a root and IS updated while **the returned register pair is not**, so the emitted scan machinery reads the pre-collection pointer. **So the poll is not a stray C-side safe point — it is the only thing forcing the collection to happen while the pointer still has a shielded home** (`rt_gc_point_arr` copies `sv` into the shield, the collector visits it, the updated pointer is written back before `VARVAL_fn` reads it).

⛔⭐ **SO THE POPULATION NOW HAS FOUR STORAGE CLASSES AND ONLY THE FIRST TWO ARE CURED BY GIVING THE PLANNER A MAPPED SLOT.** (1) a call result in a spine scratch cell BELOW the region base (hq_snobol4's `SNO$MKEXPR`); (2) a graph with NO layout and therefore no map at all, every leaf boundary unmapped by construction (hq_raku's 31, now 9); (3) **a value returned in a REGISTER PAIR, which no frame map can ever cover because a frame map describes memory**; (4) the general case § 3b names, a store into a slot the planner never mapped. **WHAT CLASS 3 OWES IS A CALLING-CONVENTION DECISION AND NOT A POLL IN C: either the scan subject stops travelling as a raw register pair and rides a slot the caller's frame map covers, or the emitted caller RE-READS the rooted `scan_subj` after any safe point instead of trusting the register it was handed.** Both are the emitter's and the planner's region — the cfo reported rather than cut, correctly. ⛔ **UNTIL ONE OF THE TWO LANDS, THE IN-C POLL AT `gen_runtime.c:69` STAYS, AND ITS COMMENT-FREE JUSTIFICATION IS THIS SECTION**; deleting it on the strength of *no collection point lives in C* is a correctness regression with a measured witness.

⭐ **TWO SELF-CORRECTIONS THE cfo FILED AGAINST THEIR OWN WORK, AND BOTH ARE WORTH MORE THAN THE SITE.** (a) **CFO-111 PUBLISHED “THE COMPLETE COLLECTION-POINT CENSUS, ALL OF `src/`” AND THIS SITE WAS NOT IN IT**: the census enumerated `rt_gc_point_arr` and this call is `rt_gc_point`, the SINGULAR wrapper, **the only call of that spelling under `src/`** — complete for the spelling it grepped, not for the thing it named. That is THE INSTRUMENT LAWS, thirtieth batch, clause 2 in miniature: a bucket keyed on one spelling cannot name the member it never looked for. (b) ⛔ **THEIR OWN NEW `[GC-SPINE-LOST]` INSTRUMENT PRINTS ZERO LINES ON THE BROKEN BUILD** while the witness answers 9 instead of 29 — correct behaviour, and exactly the limit written into its arm 3, because it cannot see a pointer that is only in a register or only in a C frame at the moment of collection. **A SILENT `[GC-SPINE-LOST]` IS NEVER EVIDENCE OF HEALTH**, and the seat that built it said so first.

## 4. Proof and gates

- gc2 under `setarch -R env -i` with equal-length A/B knobs reads GOOD over the whole offset band in mode 4 (hq_icon's hardened recipe: never a single PAD number, always a band under an empty ambient environment); the gc2 gate reads 5 of 5 both modes and its Makefile arm loses its leading dash in the same landing (CEO-807's clause).
- hq_prolog's twelve master entries under `SCRIP_GC_STRESS=1` read PASS; the Prolog master under stress 1 reads FAIL=0 over its printed denominator.
- Census gates: zero conservative visits in `gc_heap.c` (no `gc_blk_of` on a stack word, no `cons_stack`); zero `rt_gc_collect` calls inside `rt_gcheap_alloc`; every allocating runtime entry has a poll at its emitted return (the safe-point census); every callback site of Rule 4 holds no raw heap pointer across the callback (the residual census). `SCRIP_GC_COVERAGE=1` reports words-scanned = 0.
- The control arm is base-vs-head on every frontend under CEO-757's batch; the seven masters and the benchmark grids read no worse.
- ⛔ LON 2026-09-19 15:2x (CEO-931; RULES.md THE INSTRUMENT LAWS, TWENTY-EIGHTH BATCH), verbatim: *"Remind all the seats that in order to really test the GC it should reduce the size of the arena to exasperate the problem while even running the tests suites or demos."* Every collector landing's masters, package suites and demos run once more under `SCRIP_HEAP_MB=1` (the committed-window knob; the reserve grows to 8× it): the small arena is the exerciser, `SCRIP_GC_STRESS` the deterministic plant; a board under a reduced arena names the arena; a small-arena-only red is a collector defect, rowed. Measured: `SCRIP_HEAP_MB=1` gave 1614 collections where the default gave 6 on `bench_icnstr_concat_table.icn`, output identical. ⛔ AMENDED 16:1x ON LON'S SECOND WORD (CEO-934, RULES.md batch 28 CLAUSE 2): the tiny arena is the DEFAULT of ALL GC testing, all seats, all the time — witnesses, gates, masters, packages, demos — and the default arena is one control arm per batch; the reserve cap no longer shrinks with the window (three of the first nine SnoM small-arena reds were the 8 MB cap aborting, not the collector).

## 5. Rows — THE OFFICERS' SPLIT (Lon 2026-09-17, verbatim: *"In general we are at a reset point an EMERGENCY fork so quit any existing work and concentrate solely on the mess you made with the GC HEAP design."* then *"Just so you know all HQ's are paused until we solve this GC mess. We'll keep the officers, CTO, CFO, and COO if you need them."*; CEO-813/814; MODE line 2 carries both; the six HQs are PAUSED)

All rank 0, DONE-WHENs red on origin `96c80d49d` (each anchored on the census gate the row must write), assigned:

| seat | rows | piece | order |
|---|---|---|---|
| cto | `gc-frame-placement-proof-...` | LOAD-BEARING: every spine/frame slot at a fixed compile-time offset at every allocating call return; the callee-saved registers that can hold a DESCR/PTR_GC there (Prolog r12–r15 first); where the criterion fails the slot moves to the RBP frame | FIRST |
| cto | `gc-compile-time-frame-maps-...` | the map table generated from `zls_field_t` per safe point (format § 6) | second |
| cto | `gc-the-compile-time-map-table-is-emitted-beside-the-code-in-both-media-...` | the table as static data through `x86_asm.h`/xa: sealed slab (mode 3), labelled `.rodata` (mode 4), one runtime symbol to find it | with the table |
| cto | `gc-every-snobol4-and-snocone-allocating-box-stores-its-result-then-polls-...` | READ AS EVERY LANGUAGE: each allocating box stores its result to its mapped slot, then polls `g_gc_pending` at the return | with the table |
| cfo | `gc-safe-points-the-allocator-never-collects-...` | the allocator sets `g_gc_pending` and grows the window, never collects; the callback residual (`by_name_dispatch` 208 sites, `gen_runtime`, the coexpression entry) registers what it holds | second — alone cures the C-frame class |
| cfo | `gc-the-collector-walks-the-rbp-chain-keyed-by-return-pc-...` | `gc_collect_ex` walks main and coexpression stacks by RBP chain keyed by return PC; visits only mapped slots; the scan, the sniff, `cons_stack`, `rt_cas_live_span` DELETED; coverage words-scanned 0 | after the table exists |
| coo | `gc-instruments-the-safe-point-census-...` | the census gates and the rate-graded acceptance arms with the poison fill ON (the cfo's clause: a read of vacated ground announcing itself is what proves a map COMPLETE rather than lucky), each tripping under a planted violation | parallel |
| ceo | `gc-every-zeta-slot-of-a-pascal-graph-...`, `gc-every-zeta-slot-of-a-raku-and-rebus-graph-...` (and Icon/Prolog/SNOBOL4 the same way) | no frame slot without a `ZK_` kind; § 6 with the cto; the gc2 (`setarch -R env -i`, a band) and twelve-entry witnesses; the daily integration audit; lifts the pause on Lon's word | parallel |

**The cfo's acceptance arms, adopted verbatim (CEO-814):** (1) no new conservative visit, no pinned block in any form, no collection inside a runtime call — reverted on sight; (2) a runtime function that calls back into emitted code registers every raw heap pointer it holds across the callback, or the landing is refused; (3) a map row's DONE-WHEN is graded by RATE with the poison fill ON; (4) hq_icon's mark-only halving of the bad gc2 offsets (32/64 → 16/64) is the conservative-rewrite half and hq_raku's unfixed `DATINST_t.type` slot (0xdbdbdbdb) is a map-shaped defect — the DINST layout is one of the maps, re-laned under the maps row, never another hand-written visitor. Control arms while the HQs are paused: the seven smokes plus the SNOBOL4 and Icon masters, run by the landing officer under `S4E_ONE_RUNNER_OVERRIDE` naming MODE line 2, batched per CEO-757.

## 6. The map format — FIXED 2026-09-17 10:5x CDT by the cto under § 7 F1 (hq_icon is paused, CEO-814; CTO-65), frozen with the page

§ 7 F1 replaced "a table keyed by return PC" with "everything on the emitted stack is a DESCR", so the map is no longer a per-safe-point slot list: the cells describe themselves, and the map carries only what a cell cannot — where the frame's one non-DESCR (the return address `call` pushed) sits, and which graph the frame belongs to. Measured on SCRIP `162e8a172` before anything was written: a procedure frame already reserves `[+0,+16)` for the RETURN/SUSPEND value (`frame_layout.c:454`, `bb_return.cpp:22`), params start at `+16`, and every activation frame carries a raw WIRE HEADER at its top (`[kt-24]=γ`, `[kt-16]=ω`, `[kt-8]=caller base`; `xa_flat.cpp` zframe prologue, `emit.cpp:3158` the lcl_proc prologue) directly under the return address; `main` is `sub rsp, 65544` with no header and two raw words (`push rdi; push rsi` = argv/argc) below it. So the map cell goes at the frame's TOP, not its bottom — no existing offset moves.

**6.1 Two type codes, no more (`descr.h`, `descr_tags.inc`, verified by `util_tag_layout_verify.py` like every other tag):** `DT_RAW = 0x98` — a cell whose `slen` and payload word are a value-never-a-pointer (a counter, a cursor, a saved rsp/rbp, a code pointer, a watermark); the collector never visits it. `DT_MAP = 0xA0` — the map cell; the collector never visits it as a value but reads it as a frame boundary. Both carry `DT_NOTSTR` and no `DT_NUMERIC_BIT`; a static assert pins them above `DT_ORDER`; the three open-ended `v >= DT_DATA` tests in the tree (`driver_data.c:440`, `core.c:3798`, `by_name_dispatch.c:6149` — each already admits `DT_BIG`..`DT_ORDER` and survives only by a second guard) take an explicit `v < DT_RAW` bound in the same landing, and the assert names them. Nothing else is added: a counter a box already mints as `DT_I` stays `DT_I`; an outer Σ/Δ pair a pattern box saves is a `DT_S` cell `{ v=DT_S, slen=Δ, s=Σ }`, exactly the string descriptor it is, so the collector visits and relocates the suspended subject precisely (today it is `push r13` — an 8-byte raw word that only the conservative sniff ever found).

**6.2 The map cell (one per activation frame, written by the prologue, never rewritten):** a `DESCR_t` at frame offset `map_off = frame_bytes - header_bytes - 16`, i.e. the highest cell of the frame, immediately under the raw wire header and the return address:

```
{ v = DT_MAP, mint_op = 0, src_node = 0, slen = frame_bytes, p = &<graph's static map> }
```

`frame_bytes` is the prologue's `sub rsp, N` (so `map_cell + 16 + header_bytes` is the return address slot and `map_cell + slen` is the caller's RSP after the return address is popped — the walker needs no division and no lookup to step to the next segment cell); `header_bytes` is 24 for a wired frame, 0 for `main`. Because the walker reads cells UPWARD from the safe point's RSP and every cell below the map cell is a DESCR (F1), it needs no frame size in advance: it visits cells until it meets `DT_MAP`, then skips `16 + header_bytes + 8` bytes and continues in the caller's segment. `main`'s frame grows by 16 (65544 → 65560, still ≡ 8 mod 16) to hold its cell, and its `push rdi; push rsi` becomes one `DT_RAW` cell `{ slen = argc, p = argv }` (the two readers at `rt_main_progname_stage`/`rt_main_args_stage` move by a word).

**6.2b THE ONE NAMED EXCLUSION, AND THE WALKER'S TERMINATION RULE FOR IT (cto, CEO-823's second option, measured; SCRIP `a861648ab`).** One emitted frame regime cannot take its cell at the top: the SNOBOL4 stored-pattern blob (`emit.cpp` R-4(b)) is `push rbp; mov rbp,rsp; sub rsp,N` with its wire header at `[rbp-8]`γ `[rbp-16]`ω `[rbp-24]`rdx `[rbp-32]`r12 and its registry slots (ARBNO cell, capture SAVE, FENCE1 watermark) below, every one of them addressed from `rbp` at an offset the templates bake — so a cell inserted under the header would move all of them. THE CURE WAS BUILT AND WITHDRAWN, and the withdrawal is the finding: carving 16 more and writing the cell at `[rsp+0]` flagged `GC_FRAME_MAP_BASE_CELL` gave every graph a cell and read GREEN on the row's gate in both media, on the seven smokes, on gc2 and on preflight — and cost the SNOBOL4 master ONE entry, `user_function_eval_pos_replace_branch_1`, SIGSEGV in BOTH modes at an `IR_MATCH_ASSIGN_COND` writing through `r12` (the dcap pend pointer) with that box's own emitted code BYTE-IDENTICAL between the two trees. So the blob regime couples to its carve SIZE somewhere that is neither the rbp-relative slot offsets (unchanged; the deepest this program addresses is `rbp-48` inside a 56-byte carve) nor the `fence0` floor (which was made to read one carve function and did not close it). Under CEO-589 a cure that trades one program for another never lands, so the frame REMAINS UNCELLED, as a NAMED exclusion whose walk rule is stated here, held at its measured count (4) by arm 6 of the row's gate. **THE OPEN DEFECT, for whoever takes it:** the R-4(b) blob frame's correctness depends on its carve being exactly `blob_frame_bytes()` for a reason no reader in `emit.cpp` states, and that is worth a row of its own before rung 2 re-shapes its interior. **THE WALK RULE THE cfo's REPORTER MUST CARRY, since the frame presents no cell of its own:** an emitted segment is walked upward from RSP, and a stored-pattern blob frame is recognised NOT by a `DT_MAP` cell but by its own shape — the walker is inside one when the segment's cells are bounded above by a saved `rbp` whose target frame belongs to a `PAT$N` graph. Concretely: **the reporter must stop the upward walk at the blob's `rbp` and resume in the caller's segment above the pushed `rbp`, WITHOUT visiting the blob's interior.** That interior is not DESCR-typed in rung 1 — the registry slots are raw `ZK_PTR_GC`/`ZK_RAW`, and the `ZK_PTR_GC` capture stack at `+48` is exactly the word a conservative read would corrupt — so visiting it is worse than skipping it. A blob is JMP-ENTERED, not called, so it has no return address of its own: the γ/ω pair its caller pushed belongs to the caller's segment and is walked there. ⛔ **Until rung 2 types the interior, a blob frame's live roots are NOT reachable by the map walk, so the scan cannot be deleted for a program containing a pattern on the strength of a zero-divergence census that never walked one — the census must EXCLUDE these frames explicitly and say so, rather than read zero because it never looked.** Rung 2 types the interior and the exclusion lifts.

**6.2c THE WITHDRAWAL, MEASURED TO A CONTRACT (cto, 2026-09-18, row gc-the-blob-frame-carve-..., gate `test_gate_gc_blob_carve_invariant_is_named_and_held.sh`, CEO-824(2)/CEO-829).** 6.2b said the blob regime "couples to its carve SIZE somewhere". It does, and the coupling is now bounded by ablation rather than guessed at. **THE CARVE IS A DEPTH CONTRACT, NOT A SIZE:** growing it by 16, 32 or 48 bytes — with the emitted program otherwise byte-identical, the asm diff being the two `sub rsp` immediates and nothing else — kills `user_function_eval_pos_replace_branch_1` in mode 3 with a store through `r12 = 0` inside an `IR_MATCH_ASSIGN_COND`, which is control arriving at a box whose registers the regime never established; growing it by 64 or more runs clean; mode 4 is unaffected at every size; and an environment-size sweep shows it is deterministic, not an alignment lottery. **RULED OUT, so no one pays for them twice:** a stale `blob_frame_bytes()` reader (moving the pad inside the formula, so the carve, `sn4_choice_rbp_off`, `fence0_dyn_floor` and the suspend arms all move together, still dies), `bb_match_begin`'s `lea rsp, [rbp - 56 - op_frame_extra]` retry_whack literal (following the pad, still dies), and both together (still dies). The site that bakes the depth is therefore NOT one of the named formulas and is still unnamed; the remaining suspects are the regime's own dispatch through stack cells (the DEFER β `jmp qword ptr [rsp]`, the `PAT$n_res` landing, the wire-pair release). **WHAT RUNG 2 MUST TAKE FROM THIS:** the withdrawn cure did two things and only one was fatal — writing 16 bytes at `[rsp+0]` of the UNCHANGED carve leaves the witness green, and growing the carve is what kills it. **A map cell for this regime must come out of the existing carve, never out of a bigger one.** The instrument gap the row closed is worth stating too: the SNOBOL4 smoke PASSES under the planted perturbation, so before this gate the only thing in the tree that could see the defect was a ten-minute master over 1974 entries with exactly one failing; the gate sees it in ~25 s.

**6.2d RUNG 2 AS LANDED — THE TABLE IS READ BACK, AND THE TWO MEDIA DISAGREED ABOUT IDENTITY (cto, 2026-09-18, SCRIP `fc36ea7b9`, gate `test_gate_gc_map_table_present_in_both_media.sh`).** Rung 1 gave both media an install path and NOTHING READ THE TABLE BACK: the only reporter was `SCRIP_GC_MAPS_REPORT`, which prints what the EMITTER intended at compile time and therefore cannot see a table that was emitted and never installed, nor the two media disagreeing. `rt_gc_frame_maps_dump()` (`SCRIP_GC_MAPS_DUMP=1`) reads the INSTALLED table back through the one runtime symbol. **The first run found two cross-media identity divergences.** (1) **The entry graph had two names:** the driver emits main's flat chain as the literal `"pat_flat"` in mode 3 and `"main"` in mode 4. The fact was already known and PAPERED OVER in two other places instead of cured — `ENTRY_ALIAS = ("pat_flat", "main")` in `util_gc_census.py` and a dual `strcmp` in `emit.cpp` — three copies of one fact held by memory rather than by a check. (2) **The assembler mangling leaked into `graph_name`:** mode 4 must pass an assembler-legal prefix (`asm_sym_name(pname)`) and mode 3 passes the raw name, so a Prolog program named all 159 of its graphs `app/3`, `write/1`, `=../2` in mode 3 and `app$2F3`, `write$2F1`, `=..$2F2` in mode 4. **Geometry agreed in both cases; identity did not.** Neither corrupts memory today, because the walker reaches a map through the frame's CELL and never by name — but a collector that guesses nothing cannot be built on metadata that lies about which graph a frame belongs to. **Cured at the source:** the entry graph is normalised in one named function, and the map carries the graph's TRUE name (`zls_graph_name_get`) in both media while labels keep their mangling. ⚠ **THE TRAP, MEASURED:** taking the true name unconditionally is WRONG — `LBL__` procs share main's graph object, so the entry graph came back named `g2` (Icon) and `LBL__F` (SNOBOL4) and BOTH MEDIA AGREED ON THE WRONG NAME, which a naive cross-media gate would have passed. The entry graph is guarded explicitly. **The change is data-only:** 4546 regenerated `.s` lines across 27 files, every one an `Lgcmap` name string. ⭐ **THE GATE'S POPULATION IS NOW ONE WITNESS PER FRONTEND, ALL SEVEN (cto, 2026-09-18, CEO-845).** It shipped with three (Icon, SNOBOL4, Prolog), and three witnesses cannot see a divergence in Snocone, Raku, Pascal or Rebus — a blindfold, not a scope, because both divergences above lived in the DRIVER and the EMITTER, which every frontend passes through. Under ONE RUNNER no seat may reach for a board to cover that gap, so it is covered in the hermetic gate: 2.0 s, no build, no corpus. **Populations measured at the widening (m3/m4, identical):** Icon 3, SNOBOL4 1, Prolog 159, Snocone 1, Raku 2, Pascal 2, Rebus 1 — the flat-chain frontends (SNOBOL4, Snocone, Rebus) carry exactly ONE entry because a DEFINE'd function, a Snocone `function` and a Rebus `function` all lower INTO the program's flat chain rather than into a graph of their own, which is why arm 1 PRINTS each population rather than asserting a floor it would have to invent. **Arm 4 (PLANTED) now fires in all seven too** — lines moved: Icon 6, SNOBOL4 2, Prolog 318, Snocone 2, Raku 4, Pascal 4, Rebus 2 — because a fail-once demonstrated in Icon says nothing about whether the Pascal or Rebus read-back discriminates, and an arm never seen to fire for a frontend has not measured it. **No divergence was found in the four new frontends**; the widening's product is that a future one cannot hide there. ⛔ **AND THE PAGE'S OWN STALENESS, recorded so the next reader does not build on it:** § 7 F3 still says the walker "steps frame by frame from a return-PC key", which § 6 already records as REPLACED by F1 — the map is found by cell, and there are no runtime maps.

**6.2e AS LANDED — THE BLOB INTERIOR IS ONE COMPILER-GENERATED STATIC LAYOUT, THE 6.2b EXCLUSION IS RETIRED (cto, 2026-09-19, row `gc-the-blob-frame-interior-takes-one-static-layout-and-its-cross-frame-dependency-is-named`; Lon CEO-905, § 0 item 2; gate `test_gate_gc_the_blob_frame_interior_is_a_static_layout.sh`).** The dependency 6.2c could not name was named and cured first (the box-driven capture pump, CTO-83: no C frame between an emitted box and an emitted body, so the carve is a size again) and the layout followed. **THE CELL:** the R-4(b) prologue carves `bfb + 16` and the extra 16 bytes are the frame's `DT_MAP` cell at the BOTTOM, `[rbp-(bfb+16)]`, `{ slen = bfb+16, p = &map }`, stored on the line after the carve (the safe-point gate's arm 6 reads that line; `BLOB_CEILING` 4 → 0); `frame_bytes = bfb+16`, `header_bytes = 24` (saved rbp, γ, ω above rbp), flag `GC_FRAME_MAP_BLOB`. Every rbp-relative slot offset the templates bake is UNCHANGED (the cell is below them all), `sn4_choice_rbp_off` stays `-bfb`, and the two readers of the carve size — the prologue and FENCE0's dynamic floor — read one name, `blob_carve_bytes()`. After the cell store the prologue zeroes `[rbp-bfb, rbp)` (`rep stosb`) so a collection before a box has written its slot reads a null, never the previous activation's word. **THE LAYOUT:** sealed IMMEDIATELY AFTER the map's four quads in both media (`.quad n` then `n` quads in mode 4; `bb_emit_u64` in mode 3), one quad per entry `off | kind<<32 | size<<40` (`gc_frame_map.h` `GC_LAY_Q/OFF/KIND/SIZE`, kinds `DESCR 0 RAW 1 PTR_GC 2 PTR_CODE 3` mirroring `ZK_`), read by the collector through `(const uint64_t *)(map + 1)`; no relocation in either medium, `reserved` stays 0, `sizeof == 32` holds, the maps census decodes the first four quads as before (its label regexes now admit the `$` of `PAT$n`). **THE ENTRIES, generated by the SAME `frame_slot_scan` walk that hands the boxes their slots** (one hook, `g_frame_slot_visitor`, so there is one producer): `[rbp+0]` saved rbp RAW · `+8` γ · `+16` ω CODE · `-8` γ · `-16` ω CODE · **`-24` the DTP pointer PTR_GC — the one heap pointer in the head (HB_DTP, a moving type; the defer box enters with rdx = the pattern's DTP)** · `-32` r12 RAW (the dcap pend top, into the rt_cas island, a `rt_slab_region` block that never moves) · the 8-byte slack the head formula leaves under the lowest slot RAW · per registry slot: ARBNO saved-rsp RAW 16, ASSIGN_SAVE cursor RAW 16, FENCE1 saved-rsp RAW 16, ALTERNATE's 32-byte choice record at `off(idx+1)` = RAW 8 · CODE 8 · CODE 8 · RAW 8 (and the bottom record at `-bfb` when `blob_choice_rbp_scan()` places one), a scratch-cell leaf (SPAN/BREAK/BREAKX/TAB/RTAB/REM/ARB/BAL) TWO slots — `off(idx)` = `{PTR_GC 8, RAW 8}` its pointer slot, `off(idx+1)` = RAW 16 its cnt/cursor words — any other `xop_frame_member` result cell DESCR 16. ⛔ **THE ONE TEMPLATE CHANGE THE LAYOUT FORCED:** the deferred-string forms `SPAN(*C)`, `ANY(*C)`, `NOTANY(*C)`, `BREAK(*C)` used to hand `rt_pat_prim_str` the leaf's cnt/cursor words as the home of the string pointer and its length (`LFC(0)/LFC(8)`, BREAK at `+4/+12`, an unaligned qword) — a word that is an int on one path and a heap pointer on another cannot be given ONE kind, so the pointer and length now live in the leaf's pointer slot (`LFDQ(0,·)`/`LFD(8,·)` in `x86_asm.h`: the pointer slot in the frame regime, the old spine words otherwise). **COVERAGE IS THE INVARIANT:** the generator sorts the entries and ABORTS the emitter unless they tile `[rbp-bfb, rbp+24)` exactly, in both media (`SCRIP_GC_BLOB_LAYOUT_PLANT=1` drops the last entry and reads rc 134 both media, "does not tile"). **THE READER (`gc_heap.c`):** `gc_zeta_frame` tests each word for a `DT_MAP` cell whose map is REGISTERED — an address comparison against the installed table, never a dereference of a stack word (the first cut dereferenced `d->p` on a stack word that happened to carry the tag byte and died in `gc_blob_cell`; the order is the design) — then magic, `GC_FRAME_MAP_BLOB`, `slen == frame_bytes`; on a hit `gc_blob_layout_visit` walks the entries: DESCR → `rt_gc_visit_descr`, PTR_GC → `rt_gc_visit_raw`, RAW/CODE skipped, and the walk resumes at `cell + frame_bytes + header_bytes` = `rbp+24`, § 6.2b's rule made concrete: **the interior is never word-swept again**. Reporter under `SCRIP_GC_MAPS=1`: `[GC-BLOB] frames= entries= descr= ptr_gc= raw= code= raw_in_heap=` per collection, where `raw_in_heap` counts a RAW/CODE word that resolves to a MOVING-type block (`gc_type_moves`, the one predicate `gc_mark_blk` uses; a pointer into the non-moving rt_cas island is not a lie) and `[GC-BLOB-RAW] graph= off= word= blk= type=` names each; `rt_gc_frame_maps_dump` prints `[GC-MAPTAB-LAYOUT] graph= n= off:kind:size …` per BLOB map so the gate compares the two media entry for entry through the INSTALLED table. **MEASURED (witness `scripts/gc_witnesses/hb_blob_span_defer.sno`, ref cut from `sbl -bf`):** matches at `SCRIP_GC_STRESS=1,3,5` in both modes; the kind plant (`PLANT=2`, every DESCR/PTR_GC entry retagged RAW) is counted by the reporter in both media (`raw_in_heap` 0 → 1, the word is `[rbp-24]`, type 221 = HB_DTP). Two instrument facts the row paid for: `SCRIP_GC_STRESS` arms on ALLOCATION and `rt_pat_prim_str` allocates nothing, so a witness without an allocation inside the frame never collects there in mode 4 (7 collections, `frames=0` at every one) — the witness carries `*ALLOC()` for that reason; and an output-level fail-once for the pointer slot did not reproduce (`SCRIP_ZETA_TELEM`: `moved=0B` at the collections that see the frame; a stale pointer at an unmoved block reads the right bytes), so the reporter's column is the discriminator the gate holds. **STEP 3 LANDED WITH IT — `rt_cas_live_span` IS DELETED:** the four cas arrays live in the rt_cas island and only their CONTENTS are visited, typed by their structs (`rt_cas_gc_roots` in `pattern_match.c`: `g_capx[]` DESCR, `g_dfx[].val` DESCR, `g_dcf[].pending` DESCR + `cur/top/subj/star` raw, `g_spk[].nm` raw + `.val` DESCR); `[GC-COV] cas_scanned_bytes=` now reports the bytes of the fields visited. **THE ONE NUMBER: `conservative total` 24 → 21** (the two `rt_cas_live_span` uses and the `gc_zeta_frame` call that walked them). ⛔ **FOUND, NOT THIS ROW'S, HANDED TO THE ceo:** `user_function_eval_pos_replace_branch_1` (the SNOBOL4 master entry that was this row's dependency witness) reads `error 41: field function argument is wrong datatype` under `SCRIP_GC_STRESS=1/3/5` in BOTH modes on origin `f81b1cc4d` before this landing and after it — a DT_DATA argument staged through the deferred-capture user-function entry is lost across a forced collection; the call path is the ceo's.

**6.2f AS LANDED — THE COEXPRESSION ROOTS ARE TYPED, THE PARKED STACKS ARE ONE SEGMENT POPULATION, AND THE SUBJECT REGISTER CROSSES A SWITCH AS A TAGGED CELL (cto, 2026-09-19, row `gc-the-coexpression-stacks-take-typed-visits-from-their-structs-and-the-seven-gc-zeta-frame-calls-in-gc-coexpr-roots-go`; gate `test_gate_gc_the_coexpression_roots_are_typed_and_the_parked_stacks_are_segments.sh`, 7 arms, three planted; Lon 2026-09-19 in-chat: *"Yes R13 for SNOBOL4 and Icon points to the subject string, &subject."* and *"Do not let optimization stand your way of getting a working GC."*).** `gc_coexpr_roots` swept seven regions word by word; `gc_zeta_frame_calls` 11 → 4, `conservative total` 21 → 13. **Measured from the code, correcting the reading that stood in the baton:** `gc_spill[6]` was NOT a DESCR array — `scrip_coswitch`'s asm stored rbx, rbp, r12, r13, r14, r15, the raw register file at the switch — and nothing ever reloaded a register from it, so the collector's rewrite of those words never reached a register (a conservative visit that could retain a dead block and could not keep a live pointer right): DELETED, the switch records `park_sp` (the parked thread's rsp) instead. `xmit` IS one DESCR (bb_activate stages rsi:rdx, bb_coret rdi:rsi) and is now a `DESCR_t` field visited by tag. `entry_arg` is TWO records — the Icon create package (r12,r13,r14,r15,rbx,csav5 at +8..+56) and rt.c's generator-thread record (rbx,r12,r13,r14,r15 at +8..+48) — and by Lon's word only r13 can be a collected-heap pointer in either: each record is tagged `sigma_live` at creation (`scan_depth > 0`, the creator was inside a scan) and only then is its r13 word visited raw (the package's `r13`; the generator record's word at +24, pinned by rt.c's own `_Static_assert(offsetof(regs)==8)` and `movq 24(%rdi), %r13`, read by the gate because rt.c is the cfo's file). The parked stacks — main when a coexpression runs, every parked coexpression — are SEGMENTS of the running stack's population: one iterator hands each segment to the ONE remaining walker call (pop=`parked`), and rt_coexpr.c no longer registers a coexpression stack as a root range (it was swept twice). **THE SWITCH RECORD (bb_activate, bb_coret):** r13 is spilled as ONE tagged DESCR cell `{DT_S, slen=r15d, s=r13}` on the thread's own spine before the switch and reloaded after it, because a collection on the OTHER thread cannot rewrite a parked thread's register — § 6.5's rule (registers are made visible as tagged cells) at the one site where the poll's shield array cannot help. **THE ACCIDENT IT REPLACES, measured with gdb and objdump:** the subject block of the sigma witness moved 0x…b200 → 0x…b1b0 under stress and r13 FOLLOWED it although no frame of ours saves r13 on the switch path (scrip_coswitch, scrip_coexpr_activate, scrip_coret push rbp only): glibc's `sem_wait` slow path pushes rbp, r13, r12, rbx, the whole-stack sweep rewrote that libc word, and `pop %r13` on wake restored the relocated pointer. Every co-expression program that survived a collection across a switch survived on glibc's register allocation. **THE INSTRUMENT the fail-onces needed:** `SCRIP_GC_PLANT_SHIFT=N` forwards every live block N bytes up at every collection over a kept fill at the arena start (two-pass slide: down-movers ascending, up-movers descending), so a stale copy of any heap address is wrong after every collection — six witness shapes and one 80-byte slide in one cadence had shown that a conservative regime keeps dead blocks alive through stale frame slots and the subject rarely slides. Under it: the record present → the sigma witness matches iconx at stress 1,3,5,8 both modes with the parked sweep bounded at `park_sp` (glibc's words excluded); the record omitted → red at every stress; the create package's typed visit → the create witness matches; skipped → red at every stress. **NOT DONE, NAMED (Lon: working GC before optimization):** the parked segments are still swept over their whole 8 MB mappings (617 MB over 98 ranges on the sigma witness) because the by-name generator call (`rt_call_value_gen_h` → `rt_proc_call_gen_h`) still parks its caller through C with the caller's r13 relying on glibc's words; the bound at `park_sp` is the polls row's step once every parking site carries a record. The frame SNAPSHOT of the create path (an ACTIVATION frame's bytes copied into the ct arena and registered as a root range, kept for `^e` refresh) is the one place frame bytes leave the RSP/RBP stack; on Lon's word it goes in the next rung (stack-to-stack copy at create, the refresh image on the coexpression's own stack). Pre-existing and handed to the Icon seat: Arizona `transmit.icn` and `coexpr.icn` lose their &errout trace output under `SCRIP_GC_STRESS=5` on `acbb36b00` (12 → 2 and 18 → 0 lines, rc 0), the same on a clean base build and on this tree.

**6.2g AS LANDED — NO FRAME IMAGE LEAVES THE STACK: THE CO-EXPRESSION CREATE PATH COPIES THE CREATOR'S FRAME ONTO THE NEW THREAD'S OWN STACK (cto, 2026-09-19, rung 2, the first cut of the walker row; gate `test_gate_gc_a_coexpression_frame_image_lives_on_its_own_stack.sh`, 5 arms; Lon 2026-09-19 in-chat: *"When I say frame, I am referring to STANDING-ACTIVATION (root RBP frame), ACTIVATION frames (RBP frames nested under STANDING or other ACTIVATION frames), and the SPINE (RSP)."* and *"If frames are being allocated on the HEAP then delete that code, because we do not do that here."*).** `scrip_coexpr_create` copied the creator's ACTIVATION frame image (`[regs[5]-below, +frame_bytes+below)`) into a `ct_alloc` block (`frame_copy`), registered that block as a GC root range that the word sweep walked every collection, and the trampoline copied it onto the co-expression's thread stack at first activation; the block lived on only so that `^e` could re-create the co-expression from its creation-time image. Now the thread is created EAGERLY at `create`: its trampoline reserves the image on ITS OWN stack (the pristine image), copies the creator's live frame into it while the creator blocks on a `created` semaphore, then waits for activation; the entry asm carves the working frame below and copies from the on-stack image exactly as it copied from the block; `^e` reads the old co-expression's on-stack image (Icon co-expressions are never destroyed; rt.c's lazily started generator contexts keep the lazy path, untouched). `frame_copy`, its size and below fields, the root-range registration and the `ct_drop` are gone; `scrip_co_init_once`/`scrip_co_thread_start` are the one thread-start path for both. The pristine image is a frame-shaped root on the thread's stack — it carries the creator's map cell — so the segment walk of § 6.2f reads it like any frame and the refresh values stay alive and relocated by construction. **Measured:** the refresh witness (two activations, `^e`, three more) matches iconx at stress 1,3,5,8 under the shift plant in both media; Arizona `coexpr.icn` (`image(^e)` among 58 stdout lines) prints icont's stdout at stress 0 and 5 (its &errout trace differs from icont's in format on the base build too — the Icon seat's); the reporter's `images_on_stack=2 images_off_stack=0` on the refresh witness; the uncreated-thread gate now holds BOTH contracts (a lazy context without a thread is refused by `scrip_co_stack_of`; an eager one has its stack before its first activation). Cost named: one thread per `create` instead of per first activation, and the thread's stack must hold two images (`stk_need = 2 × span`). ⛔ **THE FIRST-ACTIVATION SCAN RESET IS A PROPERTY OF THE CONTEXT BEING ENTERED, NOT OF THE SWITCH:** the first cut keyed it on a per-context `started` flag and left the root context unstarted, so a generator thread's first yield back to main reset MAIN's scan environment (`procedure_suspend_scan_replace_1`, IcnM 825/826, both modes) -- the root is started at init and only a context with an entry function (a body or a generator thread) that does not inherit the scan resets; arm 6 of the gate holds the by-value-generator-in-a-scan shape.

**6.2h AS LANDED — THE FRAME WALKER AS A REPORTER: EVERY GRAPH'S MAP SEALS ITS INTERIOR KIND TABLE, THE FOURTH QUAD IS `map_off`, AND THE COLLECTOR WALKS EVERY SWEPT SEGMENT FRAME BY FRAME BESIDE THE SWEEP (cto, 2026-09-19, the walker row's step 3, CTO-88; gate `test_gate_gc_maps_reporter_runs_beside_the_scan.sh` arms 4–6; witness `scripts/gc_witnesses/hb_scan_nested.icn`, ref from icont).** **THE TABLE:** `emit_gc_map_data` seals, after the map's four quads and in BOTH media, `n` then `n` quads `off | kind<<32 | size<<40` for every graph the frame allocator laid out (flag `GC_FRAME_MAP_LAYOUT` = 8; the blob keeps its own builder under `GC_FRAME_MAP_BLOB` in the same format), generated by ONE producer, `zls_g_layout_q` in `frame_layout.c`, from the `zls_field` registrations: sorted by offset, adjacent same-kind fields merged into runs of at most 65520 bytes, a same-kind overlap deduplicated, a different-kind overlap counted as a conflict, a byte no field covers counted as a gap (`[GC-MAP-LAYOUT] graph= n= … gaps= conflicts=` under `SCRIP_GC_MAPS_REPORT`; 0 and 0 on every witness, and the seven-language slot-kind census reads `holes=0 unkinded=0` on all 7 masters at this tree, so the tables are complete). `GC_LAY_*` equals `ZK_*` by value, pinned by a `_Static_assert` beside the producer. **THE FOURTH QUAD IS NO LONGER 0:** it is `map_off`, the cell's offset from the frame's region base (`jcon_value_region`), so a walker finds the region as `cell − map_off` in every regime without knowing the regime's header shape — 6.2's `frame_bytes − header_bytes − 16` is broken by the generator regime, whose `header_bytes` carries a `+32` (6.7) the formula does not know. `gc_frame_map_t.reserved` is renamed `map_off`, the size stays 32, and the census reads four quads and ignores the fourth's value. **THE WALKER** (`gc_walk_range`, `gc_heap.c`, behind `SCRIP_GC_MAPS`, called beside `gc_maps_report_range` for every range the sweep walks — the running segment, the pz seam and the parked co-expression stacks; registered ranges and heap blocks are skipped): scan upward for a `DT_MAP` cell whose map is registered, carries the magic and whose `slen` equals `frame_bytes` (the validation `gc_blob_cell` already used, so a false cell is astronomically unlikely and a plant fails loudly); the words between the scan start and the cell's region base are SPINE; the interior `[cell − map_off, cell)` is read by the table — DESCR cells by tag, PTR_GC words, RAW and CODE words checked for a heap pointer (the lie class); the header `[cell + 16, cell + 16 + header_bytes)` word by word; after a ROOT frame the words up to the next cell are ABOVE-ROOT, and the walk continues so a parked co-expression's pristine image (6.2g) is read as the frame it is; a blob frame is walked from its bottom cell over `[cell, cell + frame_bytes + header_bytes)`. A heap-pointing word outside an interior counts as a TAGGED CELL if the word before it carries a pointer-bearing tag (`gc_tag_bears_ptr`, exactly the set `gc_visit_one` visits), else as a RAW word. One `[GC-WALK]` line per population per collection (`frames roots nomap notab i_descr i_heap i_badtag i_ptr i_ptr_heap i_raw i_raw_heap i_gap h_words h_cell_heap h_raw_heap s_words s_cell_heap s_raw_heap a_words a_heap divergence`, the counters reset after printing — unlike `[GC-MAPS]`, which is cumulative), and per-site lines `[GC-WALK-RAW|SPINE|HEADER|ABOVE|BADTAG]` capped at 48 per collection, each with the graph, the offset from the frame's base, the block's type and 24 bytes of its payload. ⛔ **THE WALKER VISITS NOTHING** — it reports beside the sweep, so a divergence is a red on a table, on a tag or on the sweep, and with the knob off the run is byte-identical (arm 1). **MEASURED:** (1) ⭐ **THE LIE, CURED:** `frame_layout.c` IR_SCAN_ENTER registered `scan.leave out3 sigma` as `ZK_RAW` "dead at safe points"; `bb_gen_scan` saves r13 — the OUTER subject pointer — there at enter and reloads it at leave, across the whole inner scan body. The walker read `i_raw_heap=1` at every one of 15101 collections of the nested-scan witness (`SCRIP_GC_STRESS=1`), at main+816, an HB_WSB block, the outer subject; only the sweep's raw arm had been relocating it. Retagged `ZK_PTR_GC`: `i_raw_heap=0`, `i_ptr_heap=1` at every collection, answer = icont (6300), both media, at stress 1, 3 and 5 and under `SCRIP_HEAP_MB=1`; delta and Delta stay RAW (a cursor and a length). The SNOBOL4 twin, `head.sigma_save` (+48, `ZK_RAW`, whose own note says "retag interior-PTR_GC in that rung"), is NOT reached today: `stfh()` keeps the outer subject in the match head's rbp-relative push record at `[rbp−16]` instead, which the walker sees on a nested `*F()` match as `s_raw_heap=1` (the subject string itself) — a fixed-shape push record, the § 0 item 2 case, but at a PATH-DEPENDENT depth below main's frame (the ceo's CEO-930 reading: a tagged cell, not a per-graph table). (2) **THE LIVE SPINE POPULATION, which is what E stops finding:** raw heap words below any cell — Icon 2 to 5 per collection at 4 to 7 distinct depths below main (string and HB_WSB blocks, the scan and call boxes' pushes), SNOBOL4 4 to 5 per collection (the match head's r12..r15 record, the table witness's AGGV cells); tagged spine cells 0 to 17 per collection. The header region above every cell: `h_raw_heap=0` on every witness. (3) ⭐ **ABOVE THE ROOT FRAME IS DEAD RESIDUE:** exactly 4 heap-pointing words per collection on every mode-3 witness, constant across the run, at 4.11–4.18 MB above main's cell — INSIDE `rt_outer_call`'s 4 MB reserve (`scrip.c`: `push %r12; sub $4194304, %rsp; … jmp *%rax`), the leftovers of the compile-time C frames (the lowerer and the runtime tables allocate on the collected heap before the program starts, F5). The sweep's seam range crosses that reserve at every collection, and the ceo's raw-hit meter (CEO-932: 696,012 hits over the Icon master at stress 5) attributes those hits to `main` because main's cell is the last cell below them; E stops visiting them, correctly, and the live driver frames above the reserve held no heap pointer on any witness. (4) **THE OPPOSITE LIE, NAMED NOT CURED:** `i_badtag=1` on every SNOBOL4 witness (main+0, the SLOT-ELIDE dead-result scratch of IR_ASSIGN, `v=0x1c`, a raw pointer pair) and on the co-expression witness's parked frame (main+480, IR_BOUND's result slot holding a `0x7fff…` stack address): a DESCR-kinded slot written with raw words. Benign for a typed walk while the low byte is not a pointer-bearing tag, and exactly the case § 6.6's zeroing and the writing box must close before E. (5) `i_gap=0 notab=0 nomap=0` on every witness: every segment finds its frames through their cells and every interior word is tabled. (6) **Named for E, not cured here:** mode 4's `main` map says `frame_bytes=384` while the driver carves 65544+16 (harmless, ROOT frames are never stepped through, and false); the `(np+nl)*16` cells above an Icon lcl_proc's map cell are untabled (measured heap-free; to be tabled as DESCR); the poll's C frame and `rt_gc_point_arr`'s six pushed callee-saved registers sit inside the walked range below the first frame and read as spine words. **THE READING FOR E:** the interior is readable by table in every regime and the frames are findable by cell; the exposure that remains is the spine (the 126 template push sites and 27 helper sites, each to be classified fixed-shape-per-graph — a small static layout reached from the frame's cell — or path-dependent — a tagged cell per § 2b; the walker's site lines say which of them hold heap pointers LIVE), the parking sites (§ 6.2f), and the badtag class. The walker's `divergence` per collection is the E licence number: it reads 0 only when `s_raw_heap`, `i_raw_heap`, `h_raw_heap` and the live part of `a_heap` all read 0 over the masters, under `SCRIP_HEAP_MB=1` and the stress plant (CEO-931).

**6.3 The static map (32 bytes, one per graph, `_Static_assert(sizeof(gc_frame_map_t) == 32)`):**

```
typedef struct gc_frame_map_t {
    uint32_t magic;          /* 0x50414D5A 'ZMAP' — the reporter refuses a cell whose map lacks it */
    uint32_t frame_bytes;    /* == the map cell's slen; the double-entry the census compares */
    uint32_t header_bytes;   /* 0 (main) or 24 (γ, ω, caller base) */
    uint32_t flags;          /* bit 0 ROOT: this frame is the base of a segment (main, a coexpression entry, the mode-3 outer thunk's callee) — the walker stops here instead of stepping through the return address */
    const char * graph_name; /* the frame_layout graph name, for the reporter and --dump-zeta */
    uint64_t reserved;       /* 0, AND IT STAYS 0 — see 6.5a: it is NOT the register mask. A later field lands here without moving the assert, but not that one. */
} gc_frame_map_t;
```

It is emitted in BOTH media through the ONE mechanism the emitter already has for static quads beside the code — the internal-slot pool (`x86_ro_seal_q` → a `D` record sealed in the slab in mode 3, `.Lx<uid>_<n>: .quad` in mode 4; `x86_lea_rip_id` takes its address either way) — four sealed quads at the graph's prologue node; the prologue does `lea rax, [rip + map]` and stores the cell. No `.rodata` section and no runtime-built structure: mode 4 shows the map as `.quad` lines directly under the prologue label, mode 3 seals it into the slab between the same two boxes.

**6.4 The program table and the one exported symbol:** the driver emits `__gc_frame_maps` (a `.quad` list of every graph's map address, `.align 8`, after `__label_names`, mode 4) and installs it from `main`'s prologue exactly as `__label_names` is installed: `lea rdi, [rip + __gc_frame_maps]; mov esi, <n>; call rt_gc_frame_maps_install@PLT`; in mode 3 the driver calls `rt_gc_frame_maps_install` itself after `bb_seal` with the sealed addresses. **The one symbol is `rt_gc_frame_maps_install(const gc_frame_map_t *const *maps, int n)` (`src/runtime/rt/gc_heap.c`), and its reader `const gc_frame_map_t *const *rt_gc_frame_maps(int *n)`**; storage is file-static beside the collector, as the label table's is beside the driver (no new global). The collector's WALK never consults the table — it finds every frame through its cell — the table exists for the coo's maps census (every emitted prologue's cell names a map in the table and every map is named by exactly one prologue) and for `--dump-zeta`, which prints one `MAP` line per graph (`frame_bytes header_bytes map_off flags`) so the mode-3 reading and the mode-4 `.quad` reading are compared line for line by the census. A mode-3 dump flag beyond `--dump-zeta` is not needed and not added.

**6.5 What the map does NOT carry, and why:** no slot list (every slot is a DESCR — F1), no spine depth (path-dependent — § 2b, Lon's objection), no return-PC key (there is no per-PC anything), and NO REGISTER MASK: the callee-saved registers are made visible at the poll itself, not in a map. At every allocating return the poll's slow path (§ 3, F6 step 4) pushes a SPILL RECORD of tagged cells below the current RSP before calling the collector — one `DESCR_t` per callee-saved register the emitter knows to be live there, tagged by what the register holds AT THAT SITE (inside a pattern blob: `{DT_S, slen=r15d, s=r13}` for the subject, `DT_RAW` for `r12`/`r14`/`rbp`; in a Prolog frame `r12`=trail top, `r14`=the quad context address, `r13`/`r15`=choice/ball — all `DT_RAW`, none a collected-heap pointer; `rbx` is transient in the scan boxes and dead at every poll) — then reloads them from the record after the collector returns, so a relocated subject re-enters `r13` fixed up. The record is self-describing and the collector walks it like any other cells; the `floor` argument `rt_gc_collect_c` already takes is the record's lowest cell. The emitter knows the register facts per site at compile time (it assigned them); the collector learns them from the tags. Measured basis for the register facts (SCRIP `162e8a172`): `rtx_abi.inc` pins `r13`=Σ, `r14`=δ, `r15`=Δ; `rt_match_enter` returns Σ in `rax`→`r13` and Δ in `rdx`→`r15` (`bb_match_begin.cpp:136-137`), so `r13` is the ONE callee-saved register that holds a collected-heap pointer at an allocating return, and only inside a match; ⛔ **`rbx` = "arena heap top" in the same header IS NOT DEAD HISTORY AND THE ceo WAS WRONG TO WRITE THAT HERE (corrected 2026-09-19 20:0x, CEO-959, on Lon's question):** it is DESIGN INTENT the implementation never honoured — `ARCH-SNOBOL4-RTX.md` § 2, which declares itself the single source for the blob pins, reads *"rbx | arena heap top — DESCR mint pointer"*, and `ARCH-ICON-RTX.md` reads *"rbx | arena heap top / WS-GC bump frontier (HEAP arm; dormant under `ZC_PORT_FORTH` default)"*. The drift has a mechanism and it is on the page: the frontier lived in the HEAP arm of the `ZC_PORT_*` selector, FORTH was the live protocol and HEAP was kept "granted-but-broken" (`ARCH-FRAME-LAYOUT-FIXED-CELLS.md` § on the allowlist), so when the no-modes law deleted the `ZC_*` arms the HEAP arm's design went with them and left rbx a scratch register in `bb_scan_{many,any,upto,bal}` and an rsp save in `bb_return`/`bb_suspend`. **What the tree does today is measured correctly above; what it was SUPPOSED to do was recorded in two pages and this line ratified the drift instead of rowing it.** Restored by Lon 2026-09-19, verbatim: *"RBX was always meant to be the GC bump frontier. If it is not so, make it so."* — row `gc-rbx-is-the-bump-frontier-every-scratch-use-is-retargeted-and-every-save-becomes-a-reload`, § 6.2j.

**6.5a ⭐ 6.5 IS NOW MEASURED INSTEAD OF ASSERTED, AND § 7 F1's REGISTER CLAUSE IS RETIRED BY THE MEASUREMENT (cto, 2026-09-18, the ceo's row (1) of CEO-845; instrument `scripts/util_gc_callee_saved_census.py`, gate `test_gate_gc_a_callee_saved_register_at_an_allocating_return_is_named_not_assumed.sh`, WIRED 406).** ⛔ **THIS PAGE CONTRADICTED ITSELF AND I WROTE BOTH HALVES ON THE SAME DAY.** § 6.5 said NO REGISTER MASK, the registers are tagged at the poll; § 7 F1 said the map carries the pointer-bearing callee-saved registers; `gc_frame_map_t.reserved` sat at zero with no reader while the row's title said the registers *are mapped*. Neither half was held by a check — the fourth fact in two days found living in a paragraph. It is settled by measurement, not by argument. **THE INSTRUMENT:** the allocating set is derived from the runtime binary's own call graph (the coo's derivation, imported not copied); a real backward liveness over the emitted `.s` CFG says which callee-saved registers are live across each allocating return; a reaching-definition classifier then splits every OWNED reading into *provably not a pointer* (the form settles it — `mov r, rsp`, an immediate, a 32-bit define which zero-extends, a rip-relative `lea`), *a copy of something else* (a cell reload, a `pop`, a register copy, arithmetic on one of those), or *unclassified*, which it names by site rather than guessing. **THE RULING — THE REGISTER FACTS ARE A PROPERTY OF THE SITE, NOT OF THE GRAPH, so they cannot live in a map that is one per graph.** Two independent measurements say so. (a) **SITE-DEPENDENCE:** there are graphs in which the same register is live across one allocating return and dead across another — 5 such (graph, register) pairs over the 267 graphs of the seven hermetic witnesses, and **90 over the 5268 graphs of the benchmark corpus, `PAT$0` in `pattern_bt`, `string_pattern` and `mixed_workload` among them**. A per-graph mask would have to be the union over that graph's sites, and at the site where the register is dead the union tells the collector to visit whatever the last box left in the register. That is a guess in the one place Lon's rule forbids one, and it is worse than no mask: a missing root loses an object, a spurious root reads an arbitrary word as a pointer. (b) **PASS-THROUGH:** at most allocating returns most callee-saved registers were never written by the graph at all (of 514 sites × 6 registers on the seven witnesses, 2726 live-across readings are pass-throughs against 332 owned). The value belongs to an ANCESTOR frame; no map of *this* graph can name a fact that is not this graph's. ⭐ **AND THE ROUTE IN § 6.5 WAS WRONG EVEN WHERE THE REGISTER WAS RIGHT.** § 6.5 names r13 (the subject base) as the one register that can hold a collected-heap pointer at an allocating return, and the register half is confirmed — r14 and r15 are seeded 32-bit (`mov r14d, dword ptr [...]`; `cmp eax, r15d`) and a 32-bit define zero-extends, so a 64-bit pointer cannot survive in either, which is the measurement behind § 6.5's "cursor and length". But the pointer does NOT arrive by the seeding `mov r13, rax` after `rt_match_enter`: that definition is dead across every allocating call in the population. It arrives by a **reload out of the frame cell the match stashed it in**, and by the `push r13  # outer_Σ` that saves the CALLER's subject. ⛔ **So "it came from a tagged cell" is NOT an argument that a register is raw.** A cell's DESCR tag is the truth about the CELL and it does not travel to the register: the collector can relocate the block, fix up the cell, and leave the register pointing at the old address. The census therefore refuses to call a copy raw, and the gate holds the copy count at its measured ceiling (83 owned readings on the seven witnesses) so the class cannot grow in silence. **That residual is exactly what § 6.5's spill record must carry, and it is the polls row's build, not this row's.** **Nothing collects through a map yet, so none of this can bite today.** The state as landed: `reserved` is zero in every emitted map in both media, `gc_frame_map_t.reserved` has zero readers in `src/`, and arm 6 of the gate reds if a mask is ever smuggled into it.

**6.5b ⭐ THE REGISTER-RESIDENT RESULT — AND THE STEP-2 BLOCK THAT WAS NEVER A STEP-2 BLOCK (cto, 2026-09-18, F6 step 4; SCRIP this landing, gate `test_gate_gc_a_register_resident_box_result_is_spilled_as_a_descr_across_its_poll.sh`, WIRED 412; answers CEO-883/884).** ⛔ **THE DIAGNOSIS THIS CORRECTS WAS MINE AND THE ceo's BOTH.** F6 step 4's admission predicate is "the call's result is stored to a mapped frame slot before the poll" (CTO-75/76), 131 of the 210 allocating call sites cannot satisfy it, and the standing reading — the ceo's CEO-884 (*"the remaining 132 need the map to say which slots are live DESCRs at this frame"*) and my own CTO-76 bucket line (*"107 need a slot (step 2)"*) — was that they wait on a slot vector in `gc_frame_map_t`. **A SLOT VECTOR WOULD NOT HAVE MOVED ONE OF THEM.** The blocked sites are `op_zres` boxes: the call's result IS the box's result and it stays in `rax:rdx`, so it is not in the frame at all, and **a map that describes the frame perfectly still cannot see a value that never reached the frame.** ⛔ **CORRECTED IN PLACE 2026-09-18 ON THE ceo's CEO-898, AND THE CORRECTION IS MINE TO MAKE BECAUSE THE SENTENCE WAS OVER-GENERAL: that is true FOR THIS CONSUMER AND ONLY FOR IT.** The map has TWO consumers and I collapsed them exactly as CEO-884 did, in the opposite direction — step 4's consumer is the POLL, and the value it needs is in a register, so no slot list helps it; **step 3's consumer is the WALK, and the walk cannot be map-driven while the map has no slot list at all.** "The slot vector is dead" is false as written; "the slot vector is dead for the poll" is what was measured. Step 2 remains the precondition for the walker's half. The ceo's own measurement already said so without being read that way — `util_zls_frame_map_census.py` reads `unkinded=0 holes=0` over every graph of three languages, so **the slot-kind data was never the missing thing.**

**THE MECHANISM, AND IT NEEDED NO MAP CHANGE, NO NEW RUNTIME SYMBOL AND NO SECTION-6 AMENDMENT.** `x86_rt_gc_poll_res()` (`x86_asm.h`) spills the pair as a 16-byte `DESCR_t` under `rsp` across the poll and reloads it: `sub rsp,16 · mov [rsp+0],rax · mov [rsp+8],rdx · call rt_gc_poll · mov rax,[rsp+0] · mov rdx,[rsp+8] · add rsp,16`. `rsp` stays 16-aligned across the `sub`, so the poll's `call` is ABI-correct. ⭐ **THE HALF THAT MAKES IT A CURE RATHER THAN A SAVE/RESTORE: `gc_collect_ex`'s precise root phase ALREADY walks `[poll floor, stack top)` as DESCR cells WITH RELOCATION** (`gc_heap.c`, the `pz && g_gc_seam_sp` arm; `rt_gc_poll` passes its own frame as the floor), so the spilled cell is visited and **updated** by construction — the value survives a *moving* collection, not merely a clobbering call. Measured, not read off the source: `SCRIP_GC_MAPS=1` on a forced collection reports `pop=seam ranges=1 bytes=4218584`.

**THE FAIL-ONCE, RE-MEASURED LIVE RATHER THAN CITED.** The site is `bb_assign_global`'s `NV_SET_fn` path — the one whose BARE poll made `140_pat_eval_double_fn_trick` print `stk=` against `stk=B` in CTO-76, **a WRONG ANSWER and not a crash**. With the cure backed out and the tree rebuilt, it prints `stk=` again and the gate reds on arms 1 and 2; with it in, `stk=B`. `safe-points.unpolled 132 → 131`, baseline lowered in the same landing.

⛔ **WHAT THIS DOES NOT BUY, STATED HERE SO THE CURE IS NOT OVER-READ THE WAY THE BLOCK WAS.** It makes **the result** visible. It does **not** make a poll safe "anywhere": any OTHER live collected-heap pointer sitting in a caller-saved register at the poll is still lost, and a callee-saved register holding one — § 6.5a measured `r13` as that register — is still owed the SPILL RECORD of § 6.5. The predicate is relaxed from *"the result reaches a mapped slot"* to *"the result is in `rax:rdx` and nothing else live is in a register"*: **a wider door, not an open one.**

**THE SIZING, MEASURED OVER ALL 131 RATHER THAN ESTIMATED (classifier: the call's result flows to `x86_gamma()` with no redefinition of `rax` in between).** **33 sites are this class** — `bb_match_*` ×11, `bb_goto_deferred` ×3, `bb_match_capture` ×4, `bb_binop_gvar_arith` ×2, `bb_call_write_slot` ×2, and 11 singletons. 23 more store their result and are the ordinary γ idiom; 73 are neither and keep their own reasons. ⛔ **THE 33 ARE A SIZING, NOT AN ADMISSION** — twice now a batch admitted on a classifier's word cost a landing (CTO-75's `op_zres` bisect, CTO-76's over-published one-liner bucket), so each lands on its own asm evidence. `bb_return` at 47 is in the 33 and is separately excluded by CTO-76.

**6.5c ⭐⭐ THE SPILL RECORD OF § 6.5, BUILT — AND IT NEEDED NO RUNTIME SYMBOL, NO MAP CHANGE AND NO NEW DESCR CODE (cto, 2026-09-18, F6 step 4; SCRIP this landing; gate `test_gate_gc_the_spill_record_carries_a_callee_saved_heap_pointer_across_an_allocating_return.sh`, WIRED 414; answers the ceo's CEO-890).** § 6.5 promised "a SPILL RECORD of tagged cells below the current RSP before calling the collector … then reloads them from the record after the collector returns", § 6.5a measured the residual it must carry, and § 6.5b said in as many words that a callee-saved register holding a collected-heap pointer "is still owed the SPILL RECORD of § 6.5". This is that record.

**THE TWO FACTS IT TURNS ON, BOTH READ OFF `gc_heap.c` RATHER THAN OFF THIS PAGE.** (1) **`rt_gc_point_arr_c(arr, n, r0, floor)` ALREADY TAKES A SHIELD ARRAY AND A FLOOR** — the cells of `arr` are visited by `rt_gc_visit_descr`, which relocates by the cell's own type field and handles an INTERIOR pointer (`gc_visit_one`'s `DT_S` arm counts interiors and registers `&d->s` for rewrite). So the record is `arr[0]` and nothing new has to be agreed. (2) ⛔ **THE FLOOR IS WHAT KEEPS THE ceo's HAZARD OUT, AND IT IS THE HALF § 6.5 DID NOT KNOW IT NEEDED.** `gc_zeta_frame` walks `[floor, stack top)` EIGHT BYTES AT A TIME and its fallback arm is `if (gc_blk_of(*loc)) rt_gc_visit_raw(loc)` — **any word inside a heap block is visited raw AND registered for rewrite when the block slides**, so an untagged integer left in that range comes back a relocated address (CEO-890's measurement, filed here rather than in a FINDING because it is a constraint on this design and not an event). The record therefore hands the poll **the CALLER's own rsp as the floor**: the tagged cell at `+0` and the untagged scratch at `+16` are both BELOW it, the cell is visited precisely through `arr`, and the untagged word is never swept at all. That is why § 6.5 says TAGGED CELLS and why it cannot be relaxed — **this class of site returns a `long`, not a DESCR, and spilling that `long` as a descriptor pair is the corruption, not the cure.**

**AS EMITTED** (`x86_rt_gc_poll_rec1` / `x86_rt_gc_poll_rec_sigma` in `x86_asm.h`): `sub rsp,32 · mov dword[rsp+0],DT_S · mov dword[rsp+4],r15d · mov qword[rsp+8],r13 · mov qword[rsp+16],rax · lea rdi,[rsp+0] · mov esi,1 · mov edx,0 · lea rcx,[rsp+32] · call rt_gc_point_arr_c · mov r13,[rsp+8] · mov rax,[rsp+16] · add rsp,32`. `rsp` stays 16-aligned across the `sub`, so the poll's `call` is ABI-correct; the `slen=r15d` half follows § 6.5's own prescription and keeps the cell a well-formed descriptor should the floor ever be lowered.

**THE TWELVE SITES, AND WHY THEY ARE THE RIGHT FIRST BATCH.** The DEFERRED pattern primitives — `rt_pat_prim_str` in `bb_match_any` / `notany` / `break` / `breakx` (×2) / `span`, and `rt_pat_prim_int` in `bb_match_len` (×2) / `pos` / `rpos` / `tab` / `rtab` — are the homogeneous class § 6.5 was written for: inside a pattern blob, `r13`=Σ, `r14`=δ, `r15`=Δ, and the call returns a `long` status. `safe-points.unpolled 109 → 97` on the tree as pushed (the row was cut against 117 and the ceo's CEO-892 batches 3 and 4 took 117 → 109 under it; the twelve sites are the same twelve either way), baseline lowered in the same landing. SCRIP `939bbc487`.

**THE FAIL-ONCE, MEASURED LIVE RATHER THAN CITED.** With the same sites taking § 6.5b's spill-PAIR poll instead of the record, a deferred `LEN(*N)` match over a subject whose block slides prints its captures as **0xDB poison bytes** under `SCRIP_GC_STRESS=1` — `X=\xdb\xdb\xdb` against `X=AAA` — **a WRONG ANSWER, not a crash**, the same signature class as § 6.5b's `stk=`. With the record: `X=AAA Y=AABBBBBCCCCC` in both media. Only `SCRIP_GC_STRESS=1` discriminates (the collection has to land at that poll while the subject is live), which is the witness the gate pins.

⭐ **THE ceo's QUESTION (CEO-896), ANSWERED BY CONSTRUCTION RATHER THAN BY A RULE: "how does the walker tell a NON-DESCR register apart from a DESCR cell, when the swept-range validator keys on the `v` byte and a raw pointer's low byte is arbitrary?"** **IT NEVER HAS TO, BECAUSE A NON-DESCR NEVER ENTERS THE RECORD.** Two halves, and both are structural:
1. **A register that is not a collected-heap pointer is simply NOT IN THE RECORD.** It needs no relocation, and the SysV contract already preserves every callee-saved register across the poll's own `call` — `r12` (trail top), `r14` (quad context), `rbx` (an rsp save), `rbp` (a frame base) come back unchanged without anybody looking at them. Putting a `DT_RAW` cell in the record for them would buy nothing and would hand the validator the very ambiguity the question names. The record's size is the count of heap-bearing registers at that site, which is ONE here and is measured, not assumed — the census's `heap` class names them and the gate's arm 3 is coverage over exactly that list.
2. **The untagged value that DOES have to survive — this class's `long` result — is below the floor, not in the swept range at all.** So the walker's `v`-byte keying never meets it either.
⛔ **The corollary is a rule on the record, and it is the one to hold: THE RECORD IS TAGGED CELLS OR IT IS EMPTY. A `DT_RAW` cell in a swept range is not a safe way to carry a raw word — it is the ambiguity the ceo names — so the cure for a raw word is the FLOOR, never a tag.** (§ 6.1's own rule says the collector never visits a code above `DT_ORDER` as a value; `gc_zeta_frame` does not implement that yet, which is item (ii) below.)

⛔⭐⭐ **AND THE RECORD FORM IS NOW ALSO WHAT § 6.5b's FIVE SITES TAKE, BECAUSE THE ceo's RANK-0 ROW WOULD OTHERWISE BREAK THEM (cto, 2026-09-18, SCRIP `0c9e29325`, answering CEO-898).** CEO-898 measured that **every** stack walk in `gc_heap.c` is `gc_zeta_frame`, and took the row to delete it (`conservative total = 0`). ⛔ **§ 6.5b's spill-PAIR depends on exactly that sweep**: `x86_rt_gc_poll_res` lets `rt_gc_poll` pass its OWN frame as the floor, so the cell lands INSIDE the swept range and `gc_zeta_frame` is what relocates it — **the moment the sweep goes, that cell is visited by nothing and keeps its pre-collection address through a moving collection.** Five sites were on that footing (`bb_assign_global`, `bb_assign_var_sub`, `bb_idx_get`, `bb_to_by`, `bb_rev_assign_var`) and all five now take `x86_rt_gc_poll_rec_res()`: the pair is written as the record's ONE cell — **it already IS a `DESCR`, so it is tagged by its own type field and needs no invented tag** — handed to the poll as the shield array and visited by `rt_gc_visit_descr`, with the floor raised to the caller's rsp so the cell is deliberately outside the sweep. ⭐ **THE GATE'S ARM WAS INVERTED AND THE INVERSION IS THE PROOF:** it used to assert the walker SWEEPS the range the cell lands in (`pop=seam`, non-zero bytes); it now asserts the cell lies BELOW the floor. Arm 1 measures the value surviving a MOVING collection; the new arm says **the sweep cannot be what relocated it**, because the sweep walks `[floor, stack top)` and the cell is not in that range — so the relocation came through the typed visit, and the cure survives F6 step 3. The planted violation lowers every floor back into the cell, which is precisely § 6.5b, and all of them read RED. Counts unchanged (`polled=114 unpolled=123 of 237`): the same sites, a different poll SHAPE.

⛔ **WHAT THIS DOES NOT BUY.** It carries **one named register per site**. It does not make a poll safe anywhere: a caller-saved register holding a live collected-heap pointer at the poll is still lost, and a SECOND callee-saved register holding one at the same site needs a second cell — `x86_rt_gc_poll_rec1` takes the register by name for exactly that reason. The predicate for this class is *"the site's one heap-bearing callee-saved register is named and the untagged result goes below the floor"*, not *"polls are now free"*.

⭐ **AND THE INSTRUMENT HALF, WHICH IS WHERE THE REAL FINDING WAS.** ⛔ **THE CALLEE-SAVED GATE'S POPULATION READ `heap=0` — IT HAD NEVER GRADED ONE SINGLE INSTANCE OF THE CLASS IT EXISTS TO HOLD.** The seven hermetic witnesses of § 6.5a carry no DEFERRED pattern, so no `SUBJECT` reading ever appeared in them and arms 3 and 5 ratcheted a residual that was entirely `CELL`-form copies. A deferred-pattern witness reads `heap=5` at once — and reads the same `heap=5` on the ORIGIN tree, so this is a population gap of two days' standing, not something the cure introduced. Three changes land with it: (a) the witness joins the gate's population (8 files, 528 sites) and a new **arm 2b REDS IF `heap` IS ZERO** — a ratchet over a class the population never exercises grades nothing; (b) the census's site list was **TRUNCATED AT FOUR** (`heaps[:4]`), so the new gate's coverage arm would have reported full coverage over four of five sites — it prints every one now; (c) `mov <r>, rdx` immediately after `call rt_match_enter` gets its own class, **LENGTH**, provably-not-a-pointer by the same declaration that makes the `rax` half `SUBJECT` (`gen_runtime.h`: `ScanSubjRegs {uint64_t ptr; uint64_t len;}`), with a planted arm that refuses the class after any OTHER call. ⛔ **That third one corrects § 6.5a in this population:** § 6.5a measured that `r14`/`r15` are "seeded 32-bit … so a 64-bit pointer cannot survive in either" — **the deferred blobs seed `r15` with a 64-bit `mov r15, rdx` and that seeding was simply not in the population that was measured.** The register half of the conclusion survives because the declaration settles it; the *route* did not. Copy ceiling re-based 83 → 108, `unclassified` 0 on both populations.

⛔ **TWO THINGS FOUND BESIDE THE ROW AND HANDED ON RATHER THAN CURED HERE.** (i) **`rt_gc_point_arr` (the asm shim in `rt_asm_helpers.S`) ALREADY SPILLS ALL SIX CALLEE-SAVED REGISTERS AND PASSES A FLOOR BELOW THEM** (`push rbx…r15; sub rsp,8; lea rcx,8(rsp)`), so those six raw words ARE inside the word-swept range at every shielded point — an UNTAGGED spill record, and the ceo's hazard live in the tree today for any of the six holding a non-pointer whose bit pattern lands in a block. It is the cfo's file and F6 step 4 retires that entry anyway; telegrammed, not edited across the line. (ii) **`gc_zeta_frame` does not honour `DT_RAW`/`DT_MAP`** — § 6.1 says every code above `DT_ORDER` is a cell the collector never visits as a value, and the word walker ignores the tag and raw-checks both halves of such a cell. Curing that would let a record carry an untagged value safely; the floor makes it unnecessary here, and the walker is the cfo's (F6 step 3).


**6.5d ⛔⭐⭐⭐ A TAIL JUMP IS A CALL-GRAPH EDGE — THE ALLOCATING SET WAS UNDERCOUNTED BY 99 FUNCTIONS AND 27 CALL SITES, IN THE NUMBER THIS EMERGENCY IS STEERED BY (cto, 2026-09-18, SCRIP `9f7dec1b3`; row `gc-the-emitter-emits-the-poll-not-the-template` rung 1, the ceo's CEO-898 proposal; gate `test_gate_gc_the_allocating_set_is_a_generated_table_gated_against_the_call_graph.sh`, WIRED 415).** Found on the FIRST generation of the emitter's allocating table, which is the whole argument for generating it.

**THE DEFECT.** `util_gc_census.allocating_entries_from_binary` walked `call` edges only. The hand-written asm allocators in `src/runtime/rtx/rtx_alloc.s` — `rt_str_alloc` and `rt_agg_alloc` — reach the carve by a **TAIL JUMP** (`jmp .Lga_armed` on the armed path, `je c_rt_str_alloc` to the C fallback) and never execute `call rt_gcheap_alloc`. So they, **and the 99 functions that reach the heap only through them — the SNOBOL4 string builtins among them: `DUPL_fn`, `REVERS_fn`, `SUBSTR_fn`, `TRIM_fn`, `BCHAR_fn`, `_CHAR_`, `_COLLECT_`, `_ITEM_`** — read NON-ALLOCATING. Downstream, **27 emitter call sites were outside the denominator entirely: neither polled nor counted unpolled.**

| | before | after |
|---|---|---|
| `allocating_entries` | 1584 | **1683** |
| `allocating_call_sites` | 210 | **237** |
| `safe-points.unpolled` | 97 | **123** |

**WHY A TAIL JUMP IS AN EDGE FOR THIS QUESTION, stated so the widening is not read as over-approximation:** control reaches the target, the target may allocate, and it returns **past us to OUR caller** with the caller's frame live — which is exactly the condition a safe point exists for. `objdump` prints an intra-function jump as `<fn+0xNN>` and the symbol pattern refuses a `+`, so only true inter-function transfers are recorded and a self-edge is dropped; **two new planted selftest arms hold both directions** (a tail-jump-only allocator must enter the set; a looping function must not), census selftest 39 → 43 arms. ⭐ **THIS IS THE INSTRUMENT GETTING HONEST, NOT A LANDING GOING BACKWARDS** — the same tree, 27 sites that were never counted before, and every one genuinely unpolled.

**THE TABLE (rung 1), `src/templates/x86/gc_allocating_table.inc`, 1683 symbols, generated by `scripts/util_gen_gc_allocating_table.py`.** The emitter cannot derive the allocating set at its own compile time, because it is compiled INTO the `.so` that set comes from; the alternative to a generated, gated table is a hand list, which is the one thing the FACT RULE forbids. Both columns are derived:
- **ALLOCATES** — from the SAME derivation the coo's safe-point census uses, **imported rather than copied**, so the two instruments cannot drift apart (arm 5 proves they grade exactly the same population: `ONLY_TABLE=0 ONLY_CENSUS=0`).
- **RETURN CLASS** — out of the binary's own DWARF, because **the poll's SHAPE depends on it**: `VOID`=190 takes the bare poll; `DESCR`=925 may take § 6.5b's spill-PAIR; `OTHER`=568 **must go BELOW the floor** (§ 6.5c — a `long`, an `int`, a pointer, or a 16-byte struct that is not a `DESCR_t`, and spilling one as a descriptor cell is the corruption); `UNKNOWN`=0 would mean the emitter emits NO poll there. ⛔ A symbol defined in hand-written asm carries `DW_TAG_unspecified_type` in its own unit (`rt_gcheap_alloc` is one), so the prototype is taken from the DECLARATION DIEs — and **if two units give two different resolvable classes the row is `UNKNOWN` and the symbol is named**, because that is an inconsistency in the tree and not a coin to flip. Spot-checked against the C declarations: `rt_pat_prim_int` `long`→OTHER, `rt_cap_finish` `void`→VOID, `rt_scan_needle` `ScanSubjRegs`→OTHER (16 bytes and NOT a `DESCR_t`), `rt_gcheap_alloc` `void *`→OTHER, `NV_SET_fn`→DESCR.

⭐ **THE TABLE IS A FIXPOINT:** regenerating after a rebuild that included it produces byte-identical output. That is the convergence the whole route needed and it is measured, not argued.

⛔ **RUNG 1 LANDS THE TABLE AND ITS GATE, NOT YET A CONSUMER**, stated so the absent consumer is a declared rung and not an oversight. Rung 2 is the regime switch and the poll shapes; rung 3 flips ONE family through `x86_rtcc_call` with **no template edit**, which is the proof the route works. The hook is not hypothetical: `x86_rtcc_call(sym, ptr)` is the one function every named runtime call routes through (303 of 303 named-symbol call literals carry the callee address and take it; 0 take the bare path; `call_bare`×30 → `x86_call_ro`, `call_rt`×6 → `x86_rtcc_call_descr`), and **its first act is already a symbol-keyed table lookup**, `x86_rtcc_clob(sym)`, deciding which caller-saved registers to save around that specific call. Same hook, same key, a different table.

⛔ **TWO CEILINGS RE-BASED IN THIS LANDING AND NEITHER IS THE CLASS GROWING** — a count is not comparable across a change of its criterion, and both moves are the population getting honest: `safe-points.unpolled` 97 → 123 (recorded in the baseline header) and the callee-saved gate's copy ceiling 108 → 133 (recorded in its own header — the SAME eight witnesses, 528 → 582 allocating call sites, 54 sites that were always allocating and were never graded).


**6.7 AS LANDED — rung 1, SCRIP `0ce44acfb` (cto, 2026-09-17 11:3x CDT, CTO-65), corrections to 6.2–6.4 measured against the tree, not the page:** (a) the cell sits at `map_off = jcon_value_region` — the first 16 bytes ABOVE the value region — and every frame graph grows by exactly that cell: the two prologue formulas in `emit.cpp` and the GENERATOR RESERVE in `frame_layout.c` (`emit_patzeta_frame_reserve`, a second copy of the frame formula the page did not know about) read ONE named allowance, `ZLS_FRAME_ALLOWANCE` 64 / `_PINNED` 96 in `gc_frame_map.h`; with the reserve left at its own literal, gc2 read BAD at 8 of 8 stack offsets under `setarch -R env -i` where the base read GOOD at 8 of 8, because `rt_genp_deliver_n2_γ` reads a generator's result at `H - ftc` from that reserve — cured, head reads 8 of 8 and the gc2 gate 5 of 5 in both modes. `header_bytes` is computed as `frame_bytes - map_off - 16` at each prologue (48 for a plain zframe, 80 for the pinned Prolog quad plus 8 per standing cell, `48 + (np+nl)*16` for an Icon lcl_proc, `+32` for the generator regime's header above the frame with flag `GC_FRAME_MAP_GEN_ANCHOR`), never assumed. (b) The zframe prologue stores its cell AFTER its own zeroing (`rt_jmp_frame_lexprep2` / `rep stosb` cover `[0, kt-32)`, which includes the cell). (c) The map's magic is `0x50414D5A` and the four quads are `magic|frame_bytes<<32`, `header_bytes|flags<<32`, `graph_name`, `0`; a `bb_label_t` per chain names them `.Lgcmap_<graph>` and `x86_lea_ext` reaches them from the prologue as a forward reference in both media. (d) Mode 3 registers each map by `rt_gc_frame_maps_add(chain + label.offset)` after `bb_seal`; mode 4 emits a COUNTED table (`__gc_frame_maps: .quad n` then the entries, after `module_init`) and installs it from `main`'s prologue through `rt_gc_frame_maps_install_counted`, because main's own map is emitted after the prologue text. (e) There is no `--dump-zeta` MAP line: the dump runs without the emitter and the regime (zframe / generator / lcl_proc / main) is emitter state, so the mode-3 census reads `SCRIP_GC_MAPS_REPORT=1`, one `[GC-MAP] graph= frame_bytes= header_bytes= map_off= flags=` line per graph from the emitter itself, and compares it with the `.quad` lines of mode 4. (f) `SCRIP_GC_MAPS_CHECK=1` is a COMPILE-TIME knob: every prologue then verifies its own cell through `rt_gc_frame_map_check` (tag, magic, slen = frame_bytes, registered) and the runtime prints `frames_checked=` at exit; `SCRIP_GC_MAPS_PLANT=1` writes a wrong slen so the gate can prove the check trips (rc 134 in both media). (g) NOT MAPPED, named: the by-name SNOBOL4 function frame alloca'd inside `rt_proc_call` (`rt.c:1212`, zero-filled, no prologue) is a seam-side frame in a C segment — the walker never reaches it and the cfo's residual registration owns it; `main`'s value region is NOT yet zeroed (its base convention — the level-zero continuation pair under slot 0 — is settled by asm diff in rung 2 before any memset lands there); `SCRIP_TOP_PORTS=1` (an off-by-default emitter knob) enters main through a bridge that skips the cell store. (i) **`main`'s mode-4 frame was a literal, and it was too small before this row existed:** the driver emitted `sub rsp, 65544` for every program while three SNOBOL4 master entries carry a value region of 67888 to 83648 bytes, so their slots were addressed ABOVE main's reserved frame, into the caller's stack — silently, because mode 3 enters through `rt_outer_call`'s 4 MB reserve and never noticed. The map cell's own bounds check turned that latent overflow into a compile abort (m4 `SKIP=3`, entries that pass in m3), which is how it was found. The driver now sizes main's frame from the same `jcon_value_region` the cell is placed at — `max(65544, (region + 64 rounded up to 16) | 8)`, preserving the `≡ 8 (mod 16)` parity 65544 carries — an ordinary program is unchanged at 65544, and the emitter's check reads that number through `g_m4_main_frame_bytes` rather than a second literal (SCRIP `0567ad829`). (j) ⛔ **THE STORED-PATTERN BLOB FRAME — FOUND OPEN, CURE BUILT AND WITHDRAWN THE SAME DAY, NOW A NAMED EXCLUSION (see § 6.2b)** (found by the coo's maps census, SCRIP `f57bfb05c`, not by the cto's own gate): a SNOBOL4 `PAT$N` graph gets a real emitted activation frame from `emit.cpp`'s R-4(b) blob path — `push rbp; mov rbp,rsp; sub rsp,N`, header at `[rbp-8]`γ `[rbp-16]`ω `[rbp-24]`rdx `[rbp-32]`r12, registry slots (ARBNO cell, capture SAVE, FENCE1 watermark) below — and no `DT_MAP` cell in either medium, so by § 6.2's rule the walk never terminates on it and runs off into the caller's segment. Measured: 3 such frames across the 74 compiling benchmark programs and 1 at the coo's fixture. The ceo ruled it a RUNG-1 GAP and gave two ways out (CEO-823): cure it before the polls, or name it with the walker's termination rule stated. The cure was built and measured first; it traded a SNOBOL4 master entry and was withdrawn under CEO-589, so the SECOND option is what stands — § 6.2b is the named exclusion and its walk rule, arm 6's ratchet holds the count at 4, and the carve-size coupling the attempt exposed is written there as a row for whoever takes it. ⛔ **The cfo's walker (F6 step 3) must read § 6.2b before it builds a deletion criterion: these frames present no cell, and a census that never walked a pattern cannot license deleting the scan.** (k) THE INSTRUMENT LESSON, recorded because it is the recurring one: arms 2 and 3 of the cto's gate passed over (j) for two structural reasons — arm 2 compares maps to cell stores to table entries, which stay SELF-CONSISTENT when a whole graph is missing from all three producers, and arm 3's seven witnesses carry no stored pattern. A census that reads one producer against another catches only disagreement, never a shared blind spot; the coo's census found it by reading the frame allocator's OWN graph list as a third, independent producer. (h) A runtime-compiled EVAL/CODE fragment (`runtime_eval.c`, emitted with main's `pat_flat` prefix under `g_rt_fragment_emit`) gets NO cell: it runs on its caller's spine and the first cut wrote main's cell into the caller's frame (SNOBOL4 master 1963 → 1958, eval_2 SIGSEGV; cured SCRIP `b12714737`, caught by the batch arm). Cost measured: mode-4 Ir queens +0.117 %, pattern_bt +0.033 % (callgrind, base artifact vs head); static frame growth 0.12 % over 86 benchmark programs (`util_gc_descr_cell_census.py`).

**6.6 The frame's interior at a safe point:** every slot of a frame parses as a DESCR at every poll only if unwritten slots are not stack residue, so the prologue ZEROES the whole value region (`DT_SNUL = 0` is the null descriptor by the `descr.h` assert), extending the LCL-SEED `rep stosb` that already null-fills the named-local suffix to the full `[0, region_end)`; the cost is part of F2's measurement (a 64-byte frame is four stores; `main`'s 64 KB is once per run). Every spine claim (`x86_zclaim`, `sub rsp, 16` in the literal boxes, the pattern boxes' register pushes) writes its tag before the box's next call; an alignment pad (`sub rsp, 8` around a C call) is released before the poll, so the poll always sees RSP on a cell boundary — the gate asserts it.

## 6.8 AS LANDED — F6 step 1b, THE ICON ALLOCATING-BOX POLLS, and the fact the design did not know (cto, 2026-09-17, SCRIP `5dd2d79f0`; CEO-821)

**(a) The poll as emitted.** `IR_MAKE_LIST` stores its result to its mapped frame cell and then emits one `call rt_gc_poll` through the emitter's existing call wrapper, so the rtccb save/restore covers r8/r9/r10/r11 and the poll clobbers nothing; rax/rdx are dead by then *because* the store came first, which is the discipline § 3 asks for and not an accident of this box. Measured on `every i := 1 to 4000000 do L := [i, i+1, i+2]`: origin ONE collection at 861956 KB RSS, cured FIVE at 161212 KB (m4 156448 KB), `done 3` both media, wall 0.49–0.50 s → 0.41–0.42 s over five runs each.

**(b) ⛔ THE CONSERVATIVE SCAN WAS NOT SCANNING WHEREVER IT WAS ENTERED WITHOUT A FLOOR — SCOPE CORRECTED BY THE cto 2026-09-18, BEFORE ANYONE BUILT ON THE BROADER SENTENCE.** `gc_zeta_frame` walks from the `lo` it is handed: the FLOOR when one is passed, and the address of the `char anchor` local only when the floor is NULL. `rt_gc_point_arr` and `rt_gc_collect` (asm shims in `rt_asm_helpers.S`) push the six callee-saved registers and pass `8(%rsp)`, which is 8-aligned by construction — **so the shielded points and `COLLECT()` did see the stack.** What was blind is every collection entered with NO floor: the allocator-internal collections, which were the dominant path until F6 step 1 removed them, and the first poll implementation, which is how it surfaced. `gc_zeta_frame` walks 8 bytes at a time from the `lo` it is handed, and for the main stack that `lo` was `&anchor` — the address of a `char` local in `gc_collect_ex`, which the compiler places 3 bytes off a word boundary at `-O0`. Every word it examined was therefore three bytes shifted, and it found **zero** arena-pointing words on the main stack: measured 0 from the unaligned start against 3, 7, 11, 12 from an aligned start on the same four collections. Everything that survived a conservative collection survived on the precise root sets (§ 2b's registered ranges, the language root walkers, `rt_gc_root_args`, the shields) — the sniff contributed nothing. The cure is to round `lo` up to 8, one expression, and it is load-bearing in both directions: with it removed, a value live ONLY in an emitted frame dies at the poll and the witness SIGSEGVs on poisoned memory under both `SCRIP_GC_STRESS=1` and an 8 MB window. **Read § 2b, § 6.6 and F6 step 3 with this in hand:** the census that clears the scan for deletion must be run on a tree where the scan actually scans, or it grades a blindfold.

**(b2) And one level deeper, found by the cfo the same day:** because the poll collects on the precise path, `cons_stack` is not entered at all on programs like the Icon churn witness (`ranges=1 cas_scanned_bytes=0 pz=1 cons_stack=0` on every collection). A divergence census run over such programs reads zero because the conservative path never ran. The four populations that DO force it are readable from `gc_collect_ex` rather than guessable: foreign blocks (`HB_ZBLK`/`HB_PLJ`), a collection while a match is active (`rt_scan_active`), a live coexpression (`g_scrip_coexpr_live`), and an unsteady registered-root-range set (`g_gc_rrng_n != g_gc_rrng_ss`) — `pz` false forces `cons_stack = 1`. A census must print the population it exercised, and which of the four put each collection there.

**(c) The poll collects on the PRECISE path.** `rt_gc_poll` calls `rt_gc_point_arr_c` with its own frame as the floor, so `pz` stays 1 and the emitted stack above the poll is swept by the (now aligned) sniff between the poll and the stack top. The conservative entry (`rt_gc_collect_c`, `cons_stack=1`) was built first and rejected on measurement: it turns `pz` off, and with the misalignment cured it still scans far more than the poll needs.

**(d) The scope, measured rather than assumed.** Six Icon allocation shapes were run as loops before any poll was added: `table()`, `set()`, `repl()`, `s1 || s2`, `s[i:j]` and a record constructor are ALL bounded already (120–185 MB RSS), because they re-enter by-name dispatch or `string_ops`, where the shielded `rt_gc_point_arr` points live. Only the list constructor reaches none. So F6 step 1b is ONE box, and the rest of the polls row follows its own readings — the 208 unpolled sites the census now counts are a denominator, not a work list.

**(e) What this landing moved in the instruments.** `test_gate_gc_aggregates_are_collectable_not_immortal` loses the REPORTED paragraph and its 800 MB bound blocks again (this landing is the lift it named). The gc2 gate reads byte-identical 20 of 20 in both modes. `test_gate_gc_every_allocating_runtime_entry_returns_to_a_safe_point` keeps four arms but its reserve/cap witness is now 400000 LIVE lists: the dead-list witness stopped growing *because* the polls make it collect, which is the row's own point and not a regression.

## 6.2i AS LANDED — THE SPINE MEASURED TO THE WORD: nothing on the spine is mapped, the register-save records are the residual, and the first two cures (cto, 2026-09-19, SCRIP `a1ca80311`; CTO-89)

Lon, in-chat to the cto, 2026-09-19 (in substance): *are you attempting to map the RESULTS and LOCALS on the RSP spine? It changes completely with the path through α, γ, β and ω.* **No -- and the walker's numbers say why nothing needs to be:** every result and local that lives on the spine is already a 16-byte DESCR the walker reads by its tag (`s_cell_heap` on every witness, both media); the untagged heap words it names are REGISTER-SAVE RECORDS pushed around a C call or a deferred entry, and C frames. § 2b Rule 1a is therefore not a refactor of results and locals; it is a handful of records. The instrument: `SCRIP_GC_MAPS=3` dumps, for the first six collections whose spine below the first frame cell holds a raw heap word, every word between the poll floor and the frame base classified heap-cell / heap-RAW / code / stack / tag / nil / int; in mode 4 a code word resolves to its box label with `nm`. What it found: (1) the Icon "push sites" of CTO-88's step 1(b) were the C frames of the `string_ops.c` C-entry polls (removed by the ceo, `066e678b8`); (2) `x86_xfer_enter`'s raw `push r13` around a deferred `*F()` entry (−152 on `hb_defer_subject.sno`) -- CURED: the record is two tagged cells `{DT_S, r15d, r13}` + `{DT_I, 0, r14}`, same 32 bytes; (3) the match head's stfh record `HKQ(1)` (−120, mode 3) -- next, a tagged cell, never a table (its depth is path-dependent); (4) the cfo's landing-3 entry glue `bb_glue_enter_c2bb` pushing rbx/r12/r13/r14/r15/rdx raw around every C-to-BB entry (−184; the xfer class again, the cfo's file); (5) the C-side `rt_gc_point_arr` trampoline pushing the emitted caller's r13 above the floor at every by-name poll inside a scan (−504 on `hb_scan_nested.icn`, −856 on the defer witness) -- step 4. The defect behind the defer witness was not on the spine at all: `rt_match_enter` parks the subject base in the C global `Σ`, `c_rt_defer_close` matches the deferred value with `strncmp(Σ + delta, …)` after the callee's collection slid the string, and `Σ` had no root while Icon's `scan_subj` has been one in `gen_gc_roots` all along; `gc_collect_ex` visits `&Σ` as one typed field. The reporter gate's arm 7 ratchets the residual at 2 untagged heap words per collection below an emitted return poll on the defer witness under the tiny arena, both media, sites named by offset; each cure lowers it, and 0 is the E switch's licence on the spine. The census of the 135 `x86("push"` sites by class and clause is in the row's baton (W code wires 19 · T transient across a non-polling C call 92, E-inert because a collection happens only at a poll · R register-save records 16 · H the stfh head · C the trampoline).

⛔⭐⭐ **6.2i CORRECTED BY ITS OWN INSTRUMENT, AND THE CORRECTION IS MINE BECAUSE BOTH WRONG SENTENCES ARE (cto, 2026-09-20, CTO-95; SCRIP this landing).** Item (3) above names *the match head's stfh record storing r13 raw at `HKQ(1)`* as the residual at −120 on `hb_defer_subject.sno`, and the reporter gate's arm-7 ratchet of 2 named the same site. **THAT CODE CANNOT EXECUTE.** `bb_match_begin()` read `x86_begin(); { return <the push-based head>; } return <the stfh head>;` — a bare compound block ending in `return`, so the second return and its `stfh()`/`HKQ`/`HKM`/`HKD`/`rpin`/`hfc` helpers were 92 lines of unreachable emitter code. Deleted this landing; **the proof is mechanical, not a reading: every benchmark, demo, prolog and icon `.s` artifact is byte-identical afterwards (`util_verify_s_artifacts_owed.sh`, owed 0, all four lane checks ran), and the live head is the one with `push rbp; push r12; push r13 (outer Σ); push r14; push r15`.**

⭐ **AND THE WORD AT −120 IS NOT A LIVE SAVE AT ALL — IT IS A CARVED-BUT-UNWRITTEN SPINE SLOT, MEASURED DEAD RATHER THAN ARGUED.** A watchpoint on that address at the moment of the collection names its writer: `push rdx` inside `bb_assign_global`'s mon-var tap (`push rax/rax/rdi/rsi/rdx/rcx/r8/r9/r10/r11` around `comm_var`), where `rdx` is the assigned value's pointer half — **already popped** by the time the poll runs, and still sitting in a slot a later, shallower box carved without writing. A read watchpoint and a write watchpoint set together at the collection answer the only question that matters: **the first touch afterwards is a WRITE (the runtime, then emitted code) and there is never a READ**; on the nested witness the deepest site (−784) is never touched again at all. A dead word is not visited and is not read, so the typed walk is correct over it.

⛔ **CONSEQUENCE FOR THE E SWITCH'S LICENCE, STATED SO NOBODY CHASES A ZERO THAT CANNOT ARRIVE: `s_raw_heap = 0` ON THE SPINE IS NOT REACHABLE BY CURING SAVE RECORDS.** The walk range is `[poll floor, frame base)` and every word in it is live *by address*; whether it is live *by data* is something only the emitted code knows, and the instrument cannot tell. The ratchet therefore holds **no NEW raw push of a heap pointer**, not "zero words", and it now reads **1** (m3; m4 reads 0) on two witnesses instead of 2 on one — the two words the old number named are both cured (`x86_xfer_enter`, and the cfo's `bb_glue_enter_c2bb`). ⛔ **THE RULE THAT REPLACES THE GUESS: a cure lowers this ratchet only after the same read-watchpoint experiment shows the word it removes was LIVE.** Zeroing every carve would make the instrument's zero meaningful and is a row, not a footnote; it is not taken here because no measured word is a defect. Second witness added with the ratchet: `hb_nested_match_outer_subject.sno` (an inner match inside the deferred `*GC()` while the outer match is live; `spitbol` and `csnobol4` agree on `OK-3-y2y`), 25 collections, both media, 7 distinct sites, and it is also what prints the step-4 C-entry residual under the by-name trampoline (18 words m3, 13 m4, reported not graded).

## 6.2j THE REGISTER PLANE, ASKED BY LON AND MEASURED OVER THE WHOLE TREE — rbx IS NOT THE ARENA TOP IN THE TREE, IT WAS ALWAYS MEANT TO BE, AND IT IS NOW ORDERED BACK (ceo, 2026-09-19 19:5x–20:1x CDT, CEO-958/959)

Lon, in-chat to the ceo, three messages inside five minutes while the ceo was auditing `41323bc8e`: *"R12 is reserved globally and never used by anyone. RBX is used by the GC as the top of the bump arena."* then *"R12 is used by the GVA, rather."* then *"R12 is CAS. R9 is GVA. Sorry for confusion."* then *"RBX is the GC top. Correct?"* **THE ANSWER IS: r12 and r9 are exactly as he finally stated them and the tree agrees; rbx is NOT the arena top in this tree, and the census is exhaustive rather than a sample.** r12 = the CAS pointer — `bb_match_begin.cpp:140–144` writes the tag-0 sentinel at `[r12+0]`, the rsp mark at `[r12+8]` and advances by 24, over the 8 MB island `pattern_match.c:653` carves with `rt_slab_region`, which `gc_root_cas` visits through `rt_cas_gc_roots` (`gc_heap.c:1014–1017/1065`) and which is not collected-heap. r9 = the GVA, pinned by a `_Static_assert` that fires if anyone renames it (`x86_asm.h:351`, `RTCC_GVA_REG`), with `RT_GVA_VA`'s pinned island as the non-register road. **rbx, every occurrence in `src/` outside the generated parsers, is one of four things and never a heap word:** (i) a scratch save of `rsp` around a 16-byte-aligned C call — `push rbx; mov rbx,rsp; and rsp,-16; call; mov rsp,rbx; pop rbx` at `emit.cpp:278` and `2911–2922`, `xa_flat.cpp:428–437`, `bb_return.cpp:43–48`, `bb_suspend.cpp:31–44`; (ii) a scratch holder of a returned value in the four scan boxes (`bb_scan_{any,many,upto,bal}.cpp`, `mov rbx,rax` then `mov rdi,rbx`); (iii) ABI preservation in the C inline-asm shims (`rt.c` ×6 pairs, `runtime_eval.c` ×2, and the `rt.c:1238` callee-saved capture); (iv) a member of the box-entry contract set `{r12,r13,r14,r15,rbx,rbp|rsp,r9}` (`bb_create.cpp:21`) and of the co-expression package saved beside a SEPARATE `gva` field (`rt_coexpr.c:27/34/182/219/259`, `frame_layout.c:182`). **The arena frontier lives in memory, not in any register: `g_hp_fr.top` (`gc_heap.c:19/30`, offset 0 pinned by `_Static_assert` for PL-SINK-3), and the hand-written bump allocator reloads the record's address into r10 from the GOT at every single use — `rtx_alloc.s:7`, `:46`, `:60`, and `rtx_str.s:100` into rdx.** So nothing pins the frontier across a call, which is why no landing has ever had to think about it.

⛔⭐ **THE CONSEQUENCE, AND IT IS WHY THIS QUESTION WAS WORTH THE HOUR: tonight's step-0 glue (`41323bc8e`, the cfo) saves rbx and r12 RAW across every C-to-BB entry, and its licence is exactly the sentence above.** Its own commit names the reason correctly for both — a stack word and the CAS pointer. **But the licence is contingent, not structural: the moment any register is made to hold the allocation frontier, or any other quantity the collector itself rewrites, a raw save/restore around an entry that can collect ROLLS THAT QUANTITY BACK.** For a frontier that is not a stale pointer, which the E walker would at least see; it is allocation resuming at a pre-collection top and handing out addresses over live objects — silent, unbounded, and invisible to every instrument we own because no heap block is wrong, only the next one to be carved. **THE STANDING RULE, added to § 0's discipline and binding on every seat: a register whose content the collector rewrites — the frontier, a heap pointer, a window bound — is NEVER saved raw across an entry that can collect; it is either a tagged cell the collector fixes in place, or it is RELOADED from its authority after the entry rather than restored from the save.** A pinned-frontier register is a legitimate optimisation (it removes a GOT load from every allocation) and this rule is its precondition, not an argument against it; if Lon wants `rbx` to become the GC top, the glue's `x86_rsp_load64("rbx", 48)` and the co-expression package's `pkg->rbx` are the two sites that must become reloads in the same landing. ⭐ **AND THE AUDIT LESSON: the cfo's claim was right and the ceo confirmed it at source, but neither of us had checked that it would STAY right — a landing whose safety rests on what a register happens to hold today owes the sentence that says what would break it.**

⛔⭐⭐⭐ **THE VERIFICATION LON ASKED FOR, AND IT REVERSES THE HALF OF § 6.2j ABOVE THAT WAS A RULING RATHER THAN A MEASUREMENT (2026-09-19 20:0x CDT, CEO-959).** Lon, in-chat: *"Will you verify in the ARCH*.md files that RBX was meant to be GC bump top?"* then, on the answer: *"RBX was always meant to be the GC bump frontier. If it is not so, make it so."* **IT IS IN TWO PAGES, IN THE REGISTER-CONTRACT TABLE OF EACH.** `ARCH-SNOBOL4-RTX.md` § 2 REGISTER CONTRACT — the table `ARCH-ICON-RTX.md` names as *"the single source"* for the blob pins — reads **`| rbx | arena heap top — DESCR mint pointer (16-byte DESCR in 64-bit) |`**, under a heading that says RTX must PRESERVE every blob pin across every entry and exit. `ARCH-ICON-RTX.md` reads **`| rbx | arena heap top / WS-GC bump frontier (HEAP arm; dormant under ZC_PORT_FORTH default) |`**. So the measurement in this section stands — nothing in `src/` puts the frontier in rbx today — but the sentence that called it *dead history* was a ceo ruling made against two pages that say otherwise, and it is retracted at § 6.5 where it was written. ⭐ **THE DRIFT HAS A MECHANISM AND THE PAGES CARRY IT, which is what makes this a restoration rather than a new design:** the frontier lived in the **HEAP arm** of the `ZC_PORT_*` selector; FORTH was the live protocol and HEAP was kept *"granted-but-broken"* on purpose (`ARCH-FRAME-LAYOUT-FIXED-CELLS.md`, the `{FORTH, HEAP}` allowlist). When the no-modes law (RULES.md § ZETA HAS NO MODES) deleted the `ZC_*` arms, the HEAP arm's register plane went out with the switch, and rbx — preserved by SysV and therefore free-looking — was picked up as a scratch rsp-saver and a scan-box transient. **Nobody decided rbx should stop being the frontier; the decision was a casualty of a correct deletion, and no instrument existed to notice, because a register plane is documented in a table and enforced nowhere.** ⛔ **THE INSTRUMENT THAT SHOULD EXIST AND DOES NOT, named here as the row's second half:** `RTCC_GVA_REG` is pinned by a `_Static_assert` that fires the moment anyone renames it (`x86_asm.h:351`) and r9 therefore never drifted in the same years rbx did. The blob-pin table has no such assert for any of its six rows. **The row lands one per pinned register, so the next plane change fails a build instead of a page.** ⭐ AND THE GENERAL LESSON, mine to carry: **I measured the tree, found it disagreed with the seat's memory, and ruled for the tree without reading the design pages the tree was supposed to implement — twice, since § 6.5's "dead history" was the same error made silently months earlier.** A measurement answers *what is*; it never answers *what was meant*, and where those differ the divergence is a row, never a ratification.

## 7. FROZEN 2026-09-17 10:1x CDT (Lon: *"If you think now considering everything that you can salvage this GC mess, then I'll give you some rope to hang yourself. Meaning, I'll trust you and let's try it. We should know soon if things are working."*; CEO-818) — THE DESIGN AS THE OFFICERS BUILD IT

**F1 — EVERYTHING ON THE EMITTED STACK IS A DESCR, AND THE ONLY TAG IS THE DESCR's OWN TYPE FIELD** (Lon: *"everything IS a DESCR and the only tag is INSIDE a DESCR"* — yes). § 2b's "tagged cells" is this and nothing more: every spine cell and every activation-frame slot is a 16-byte `DESCR_t` whose type field (`DT_S`, `DT_I`, `DT_A`, `DT_T`, `DT_DATA`, `DT_CO`, … 22 codes today; `DT_SNUL` = 0 so a memset region is valid null descriptors) tells the collector whether the value word points into the heap. A counter is a `DT_I`; a saved RSP, a watermark, a resume address is a descriptor of a value-never-a-pointer type (at most one or two new codes in the same enum). A box writes the type when it pushes the cell. The collector walks a segment cell by cell reading types — precise, path-free, no map. **The one non-DESCR is the 8-byte return address `call` pushes**: each activation frame's first cell is a DESCR whose value word names the frame's compile-time map (static data beside the code, both media), from which the walker reads the frame size and the return address position (⛔ **and NOT the pointer-bearing callee-saved registers — that clause is STALE and is superseded by § 6.5/6.5a, which measured that the register facts are a property of the SITE and not of the graph**). Maps exist per frame only, never per path, and there are no runtime maps.

**F2 — COST, MEASURED NOT ASSUMED (ceo, from `frame_layout.c`'s 54 registration lines):** all registered box shapes summed read 896 bytes today and 1120 as DESCR cells — 25 % is the CEILING; 24 of 54 shapes grow, and half of those are 8-byte pairs already laid side by side (index+pad, resume+pad, act+0/+8) which pack into one cell with no growth, so the packed figure lands nearer 10 %. Activation frames do not grow (their slots are DESCRs; one 16-byte map cell per frame against frames of 64–20000 bytes). The cto measures the real spine depth on the benchmark programs and the tag-store cost on the match hot path in the first landing; a reading above the ceiling reopens the realloc-heap question with the number in hand, and not before.

**F3 — SEGMENTS AND SEAMS (the cfo's and the coo's facts):** activation frames are RSP-addressed (main `sub rsp 65544`, a user function `sub rsp 64`; `rbp` is pushed only inside pattern blobs), so there is no rbp chain: the walker steps frame by frame from a return-PC key and the frame size. Runtime C frames sit BETWEEN emitted frames, so the emitted stack is a chain of SEGMENTS bounded by seams, and every runtime-to-emitted re-entry pushes a seam. The coo's census (SCRIP `162e8a172`) is the seam list: the by-name procedure call (`rt/rt.c:1218`, `:1969`, reached through `rt_call_proc_descr :987` and the ~60 `rt_call_*` sites of `by_name_dispatch.c`), the user-function hook (`core/core.c:3716/:3805/:3807` when the table entry is the hook), EVAL (`pattern_match.c:20 g_eval_str_hook`, `runtime_eval.c:18 eval_chain_fn`), the deferred-pattern thunk (`pattern_match.c:735`), the coexpression entry and resumption (`rt_coexpr.c:35/:124/:162/:197/:272`); Icon generator resumption re-enters through emitted box code and is enumerated from the emitter side. A C frame above a seam holds no raw heap pointer across the callback or registers it (Rule 4).

**F4 — THE ALLOCATOR NEVER COLLECTS, AND THE ARENA IS A VIRTUAL RESERVE:** the arena today is ONE malloc of `SCRIP_HEAP_MB` (512 MB default) with no reserve behind it and cannot grow in place (the cfo's reading: `g_hp_end` is read at seven sites in gc_heap.c and nowhere in rtx assembly; `rtx_alloc.s` compares against the pacing line, never the end). DECIDED: the arena becomes a contiguous VIRTUAL reserve (`mmap PROT_NONE MAP_NORESERVE`, cap `SCRIP_HEAP_MAX_MB`, default 8× `SCRIP_HEAP_MB`) with `g_hp_end` the soft end that advances toward the cap when the allocator must not collect; the page map is sized by the cap. Growth happens only between safe points, bounded by one runtime call's allocation. The three unshielded collect sites (`:192`, `:206`, `:207`) go; the shielded `rt_gc_point_arr` sites stay until the emitted polls make them redundant. **RETRACTED 2026-09-17 BY CEO-821 ON THE cfo's MEASUREMENT, THE ONE SENTENCE OF F4 THAT WAS WRONG:** this row was written as though step 1 cured the C-frame class on its own. IT DOES NOT. Step 1 removes the UNSHIELDED collections and nothing more; the shielded `rt_gc_point_arr` sites still collect over a conservative scan of live C frames, and the three loose SNOBOL4 callout witnesses (`probe_loose_callout_claws5_call`, `_cap`, `rtdef_dexp_idiom`) read rc=139 at `SCRIP_GC_STRESS=30` on the cure AND on origin `162e8a172`, identical both ways -- they are now the fail-once of the POLLS row, not of this one. The class closes when collections happen only at emitted polls with no C frame live, or when the scan goes, whichever lands first. **AND THE SECOND READING, WHICH PULLED STEP 4 AHEAD:** "growth happens only between safe points, bounded by one runtime call's allocation" is true of the design but NOT of the tree, because AN ICON ALLOCATION LOOP CONTAINS NO SAFE POINT AT ALL -- the shielded sites are SNOBOL4 and Prolog by-name dispatch points and an `every`-`do` loop reaches none. Measured on 4M dead lists: origin 5 collections at 157144 KB RSS, step 1 ONE collection at 861932 KB, answer correct and rc=0 both ways, and a 2 MB growth step versus a full window moves RSS by 84 KB, so the growth policy is not the cause. `test_gate_gc_aggregates_are_collectable_not_immortal`'s memory arm is REPORTED (its correctness and control arms still block) with that number and this lift in its header until the cto's Icon allocating-box polls land. **COLLECT() (and Icon `collect()`) is a safe point by construction:** the emitter lowers it as set-pending, poll, then a read-only runtime entry for the free-storage figure — exact, no staleness, no census carve-out.

**F0 — LON'S ORDER, 2026-09-17, in-chat to ceo, verbatim: *“Get rid of any code that pins blocks in the GC.”* (CEO-831). THE ANSWER IS THAT IT IS ALREADY GONE, VERIFIED FOUR WAYS ON SCRIP `d8c08fe19` RATHER THAN QUOTED FROM CEO-799:** (1) **name census** — `rt_pinned_alloc`, `hb_pinned`, `hb_no_move`, `HB_WSS`, `hb_root_blanket` each ZERO hits under `src/` (458 in 27 files before CEO-799); (2) **the allocator** — `rt_ws_alloc` is an ordinary `rt_gcheap_alloc` of kind `HB_WS` and marks nothing immovable; (3) **the block header** — `rt_hblk_t` is `{fwd, size, type, flags}` and the only flags in the tree are `HBF_TTL` (a validity title) and `HBF_MARK`, so **there is no immovability bit to set**; (4) **the compactor** — `gc_heap.c:714` gives EVERY marked block a forwarding address with no exemption by type or flag. `test_gate_gc_no_pinned_lifetime_class` reads GATE PASS with census 0, structural 0, and a live table, array and record surviving the slide in both media. ⛔ **THE STANDING ORDER: no pinning mechanism returns IN ANY FORM OR UNDER ANY NAME** — not a flag, not a type exempted from the slide, not a block skipped in the forwarding pass, not an “immortal”, “no-move”, “stable” or “immovable” spelling. A landing that adds one is reverted on sight; that was already the cfo's acceptance arm (1) and now has Lon's own word behind it. ⭐ **WHAT IS NOT PINNING, said so the order is not over-read:** moving METADATA OFF the collected heap to `malloc` is the OPPOSITE of pinning — it removes the object from the GC heap rather than making a GC block immovable — and it is exactly F5's split line. The cfo's holder-class landing does that and the ceo reads it as compatible, with Lon told so directly. ⛔ **AND THE GUARD HAS A GAP, ROWED RANK 0 TO THE cfo** (`gc-a-marked-block-skipped-in-the-forwarding-pass-pins-it-without-ever-self-forwarding-...`): the structural arm asserts only that no title SELF-FORWARDS, so a block left marked and SKIPPED in the forwarding pass keeps its address, never self-forwards, and wears a name the census does not know. The cure is to assert the PROPERTY at the collector — every marked block is forwarded, so blocks-assigned-fwd equals blocks-marked — with a planted skip proving the arm reds. **The ceo did not land it itself because the cfo's tree was dirty with `gc_heap.c` when the order arrived, and editing a file under another seat's uncommitted work is how two cures become one conflict.**

⛔⛔⛔ **F5's AMENDMENT OF 14:4x IS WRONG AND IS RETRACTED BY THE ceo THAT WROTE IT (CEO-832, on Lon's order 2026-09-17, verbatim: *“Ensure that we never use malloc/free since we are doing GC instead. … Get rid of all malloc, calloc, free, and realloc. … Make a FACT RULE which disallows calling those 4 functions anywhere in SCRIP.”*).** The split line below is stated as METADATA THAT MUST NOT MOVE versus VALUES THAT MUST BE COLLECTED, and the cure it licensed was moving metadata OFF the collected heap to `malloc`. **THAT CURE IS FORBIDDEN AND ITS LANDING (SCRIP `f62a33aed`) IS REVERTED.** **THE CORRECT RULE: AN UNROOTED STATIC HOLDER GETS A ROOT, NEVER A DIFFERENT ALLOCATOR.** The collector walks `g_bb_labels`, `g_eval_cache` and its keys, `g_lbl_tab` and its keys, the NV memo and the fifth holder in `pattern_match.c` as REGISTERED ROOTS, like everything else — which is what THE COLLECTOR GUESSES NOTHING meant from the start. **Moving an object out of the collector's sight to avoid rooting it is the same evasion as pinning it in place, one indirection further out**, and the ceo failed to see that while writing the amendment below and told Lon the opposite. ⭐ **WHAT SURVIVES THE RETRACTION, because it is measurement and not reasoning:** the watchpoint method, the holder chain and its named holders, the identity-change tell (`LBL__shf9` → `RETURN` between two reads, which no append-only table can do), and cure shape (b) — a generation-guarded cache MUST be invalidated by the collector, `rt_nv_memo_invalidate` after the slide, which never involved `malloc` and stands. Read everything below with this correction attached.

**F5 AMENDED 2026-09-17 13:4x CDT (ceo CEO-830, on the cfo's watchpoint measurement; the amendment is ruled IN, not proposed) — THE SPLIT LINE HAS A NAME: METADATA THAT MUST NOT MOVE versus VALUES THAT MUST BE COLLECTED.** ⛔ **THE STRUCTURAL SENTENCE, and it reframes this whole rule: `rt_ws_alloc` IS A RENAME OF `rt_pinned_alloc`, so every object that needed to be immovable is still sitting in the sliding heap under a new name WITH NO ROOT.** The CEO-799 deletion retired the pinning MECHANISM and left the pinning POPULATION where it was. **The first named population of the split, each measured by hardware watchpoint under `setarch -R env -i` on a deterministic address, each cure exposing the next holder — an unrooted-holder class AT LEAST FOUR DEEP:** `g_bb_labels` (the lowerer's label registry, `lower_common.c:17`, an `lc_vec` on `rt_ws_alloc` storage in a C static no root walk visits) → `g_eval_cache` and its keys (`runtime_eval.c`) → `g_lbl_tab` and its strdup keys (**which was a SEPARATE ROW and is the SAME DEFECT at another holder; marked a duplicate under the LANE-BY-CURE rule**) → the NV memo (`core.c`) → and an open fifth in `pattern_match.c`'s deferred-pattern walk. **The mechanism, for the seat that takes holder five:** a collection compacts a rooted table correctly and rewrites its rooted slot; an UNROOTED holder's `.data` is not relocated; the next push through the stale pointer writes straight into the relocated block. **The tell was an identity change** — entry 1's NAME went from `LBL__shf9` to `RETURN` between two reads, which no append-only table can do. **TWO CURE SHAPES:** (a) metadata off the collected heap (malloc/realloc/strdup — none of these is a program value, none was ever traced, none is freed today, so you cannot lose a root that never existed); (b) a generation-guarded cache MUST be invalidated by the collector (`g_nv_memo_gen` was bumped only on inserts, never by a collection, so a memo survived compaction with a stale pointer and a matching generation). ⛔ **A BLANKET CONVERSION IS REFUSED BEFORE IT IS PROPOSED, and the counter-example is on the record: `by_name_dispatch`'s map buffer is ws-allocated and IS an Icon string VALUE; moving it off-heap leaks 32000 per deal run.** That is exactly why the split is BY ALLOCATION SITE and not by allocator.

**F5b — THE CONSERVATIVE STACK SCAN WAS NOT SCANNING (the cto, 2026-09-17, SCRIP `5dd2d79f0`; ceo-ratified CEO-830).** `gc_zeta_frame` began its walk at the address of a `char` local in `gc_collect_ex`, **three bytes off a word boundary**, so the scan found **ZERO** arena-pointing words on the main stack — measured 0 from the unaligned start against 3, 7, 11 and 12 from an aligned start on the same four collections. **The scan this emergency argues about had not been scanning the stack, and every program that survived a conservative collection survived on the precise root sets alone.** Rounding the start to 8 is one expression and is load-bearing in BOTH directions: removed, a value live only in an emitted frame dies at the poll and the witness SIGSEGVs on poisoned memory under stress. ⛔ **CONSEQUENCE, RATIFIED AS A CONDITION ON F6 STEP 3: the divergence census that licenses deleting the scan MUST run on `5dd2d79f0` or later, or it grades a BLINDFOLD and reads zero by never looking** — the same shape as the pattern-frame condition of CEO-824, and the THIRD instrument this day to report success while doing nothing.

**F5 — THE HEAP IS TYPED VISITORS** (Rule 2b): `HB_DINST`, `HB_ARR`, `HB_PLJ` by their layouts; `HB_WS` split by allocation site into kinds with a layout; `hb_scan_interior` deleted with the stack scan; coverage counts worklist payloads. hq_pascal's open row (Pascal heap records reached through an immortal block) and hq_raku's `DATINST_t.type` witness are this row's first witnesses: a witness that DELIBERATELY holds a Pascal record across an allocation and is proven RED before any cure, built once for both.

⛔⭐ **F5's SPLIT LINE, NAMED BY THE cfo's MEASUREMENT 2026-09-17 AND RULED IN BY CEO-830(2): `rt_ws_alloc` IS A RENAME OF `rt_pinned_alloc`, SO EVERY OBJECT THAT NEEDED TO BE IMMOVABLE IS STILL SITTING IN THE SLIDING HEAP UNDER A NEW NAME WITH NO ROOT.** That sentence was already in the gc2 baton and nobody connected it to F5 until a hardware watchpoint made it concrete. **THE LINE THE HB_WS SPLIT DRAWS IS THEREFORE: METADATA THAT MUST NOT MOVE versus VALUES THAT MUST BE COLLECTED**, and the split's first named population is the UNROOTED-STATIC-HOLDER class -- a C static holding a pointer into the collected heap that no root walk visits, so a compaction leaves it reading, or WRITING, through a stale pointer. Four holders measured, three cured at SCRIP `f62a33aed` (the lowerer's `lc_vec` storage, the eval cache and its keys, the runtime label table and its keys -- all off-heap; plus `g_nv_memo_gen`, a generation-guarded cache the collector never invalidated); holder five is open in `pattern_match.c`'s deferred-pattern walk. The corrupting shape is the dangerous one and the one the split must kill: the lowerer's label registry wrote label entries THROUGH its stale buffer pointer into the relocated generator procedure table, so the row that found it had blamed its victim. ⛔ **A BLANKET CONVERSION OF `rt_ws_alloc` IS REFUSED BEFORE IT IS PROPOSED**, which is the right place to refuse it: `by_name_dispatch`'s `map` buffer is ws-allocated and IS an Icon string VALUE -- moving it off-heap leaks 32000 per `deal` run. That single counter-example is why the split must be BY ALLOCATION SITE INTO KINDS and can never be by allocator name. ⛔ **AND IT CANNOT BE FOUND BY GREP:** a census keyed on "collected-heap allocation assigned to a file-scope static" scanned 642 allocation sites and returned SEVEN, missing every holder actually cured, because the pointer reaches the static through a temporary or a struct field. The kind split is what makes the question unaskable; until it lands the class needs a runtime instrument.

**F6 — THE ORDER, EVERY SITTING MEASURABLE (the cfo's shape; AMENDED IN PLACE 2026-09-17 10:5x CDT, CEO-821, on the cfo's step-1 measurement; AMENDED IN PLACE AGAIN 2026-09-17 20:5x CDT, CEO-838, on the ceo's measured 42-file sweep):** **(0) ⛔⭐ ROOT THE HOLDERS, *THEN* SWEEP THE C ALLOCATOR — cfo, AND IT IS THE CRITICAL PATH OF THE EMERGENCY (CEO-838).** REMOVING `malloc` AND ROOTING THE HOLDERS ARE THE SAME JOB: `malloc`'d memory never moves, so an unrooted holder of it is ACCIDENTALLY SAFE, and converting that holder to the collected heap without rooting it MANUFACTURES the corruption class this emergency exists to cure. The evidence is the ceo's own sweep, written and measured and then PARKED rather than landed (`/home/resources/postoffice/tasks/PARKED-ceo-malloc-sweep-ceo-832/`): all 341 `malloc`/`calloc`/`realloc`/`free` sites outside `src/parsers` removed across 42 files, builds clean, all seven language smokes green, `make preflight` 53 arms 0 red — AND EIGHT OF THE FIFTEEN COLLECTOR GATES RED, cause by gdb on the `no_pinned_lifetime_class` witness: a by-name function NAME STRING in the collected heap, MOVED by a collection while raw C pointers hold it across `rt_call_arr_bl_sn4` → `APPLY_fn` → `_usercall_hook` → `call_user_function` → `label_lookup`, `strcmp` reading `"\003"`; m3 fails, m4 passes. So step (0) is the holder class cured AS ROOTS — `g_bb_labels`, `g_eval_cache` and its keys, `g_lbl_tab` and its `strdup` keys, the NV memo, and the fifth holder in `pattern_match.c`'s deferred-pattern walk — each measured under `SCRIP_GC_STRESS` with its witness, each landing telegrammed per file so the sweep can take that file; the sweep then lands behind it, file by file, and the CEO-832 FACT RULE is UNCHANGED (the 341 sites are DEBT, deferred behind the rooting, never a grandfathered exemption). ⛔ A STATIC GREP IS NOT A CENSUS OF THIS CLASS (the cfo's measurement: 642 allocation sites grepped, SEVEN returned, and it missed every holder they had already cured, because the pointer reaches the static through a temporary or a struct field). ⛔ `gc_heap.c`'s OWN bookkeeping (page map, mark worklist, live-block array, slot registry, segments, frame maps) is used WHILE collecting and cannot live in the heap it manages — reverting that one file took the sweep's gate reds from 13 to 8; it is an OPEN ASK TO LON and no seat writes itself the exemption. (1) cfo: the allocator never collects + the virtual reserve (lands alone). ⛔ RETRACTED CEO-821: step 1 does NOT cure the C-frame class by itself — the three SNOBOL4 callout witnesses (`probe_loose_callout_claws5_call`, `probe_loose_callout_claws5_cap`, `rtdef_dexp_idiom`) read rc=139 under `SCRIP_GC_STRESS=30` on origin `162e8a172` and on step 1 alike, at the SHIELDED `rt_gc_point_arr` sites, which still collect over live C frames; the class closes when collections happen only at emitted polls (no C frame live) or when the scan goes, whichever lands first. AND step 1 alone leaves an Icon `every … do` loop with NO safe point at all: 4M dead lists read 5 collections and 157144 KB RSS on origin, 1 collection and 861932 KB on step 1 (answer correct both, bounded by the reserve cap), so `test_gate_gc_aggregates_are_collectable_not_immortal` goes REPORTED by name with that number in its header until its lift, which is (1b). (1b) cto, PULLED AHEAD OF (2) AND (3): the ICON ALLOCATING-BOX POLLS from the step-4 row — list/record/set/table constructors and every allocating runtime-call return in an Icon graph, result stored to its mapped slot first, then the `g_gc_pending` poll — the 157144→861932 KB reading is the fail-once and the cfo's three rc=139 witnesses are the C-frame acceptance; the coo's safe-point census (SCRIP `ad0f85fae`: 202 allocating emitter call sites UNPOLLED, 39 UNRESOLVED) is the denominator and its baseline is rewritten in the landing. (2) cto: the DESCR discipline on the spine and frames — every pushed cell typed, raw pairs packed, `ZK_PTR_GC` re-shaped, the per-frame map cell and its static table in both media — with the cost readings. (3) cfo: the walker lands FIRST AS A REPORTER behind `SCRIP_GC_MAPS=1`, walking segments by type beside the old scan and printing, per safe point, the cells it visits against the words the sniff rewrote; a divergence is a red on the discipline or on the sniff; the scan, the sniff, `cons_stack` and `rt_cas_live_span` are DELETED only when that census reads zero over gc2's band under `setarch -R env -i`, the collector gates, and the SNOBOL4 and Icon masters. (4) cto: polls at every allocating return in every language, result stored first; `rt_gc_point_arr` retired. (5) cfo: typed heap visitors and the `HB_WS` split. (6) coo throughout: the censuses (five, with planted-violation arms — already drafted), coverage words-scanned = 0 and interior-words = 0, and THE DECIDABLE TEST hq_snocone asked for — not "does the collector find the roots" but "can a suite be green while it does not": the poison fill ON plus the reporter's divergence census plus a witness class that holds a value across a forced collection at every registered kind. **Control arms while the HQs are paused:** the seven smokes plus the SNOBOL4 and Icon masters by the landing officer under `S4E_ONE_RUNNER_OVERRIDE` naming MODE line 2, batched per CEO-757. **The slot-kind half is MEASURED (ceo, CEO-820, SCRIP `9c35971c1`):** `util_zls_frame_map_census.py` over `--dump-zeta` reads every word of the zls region of every graph of all seven masters with a `zls_field` kind — after registering the return-value cell at frame +0 of every function graph (318 Raku, 514 Pascal, 304 Icon and 88364 Prolog graphs had it unregistered on `162e8a172`) and two Icon pad quads (`scanseq` +8, `revswap` +56); gates `test_gate_gc_{raku,pas}_every_frame_slot_has_a_kind.sh` are BLOCKING with a planted-hole detector arm; the machine line `ZLS-MAP lang= graphs= words= unkinded= holes= graded= no_layout=` feeds the coo's maps census; NOT censused there and named so: the wire header past region_end (the cto's map cell) and the spine (the tag census). **Lift:** the ceo lifts the HQ pause on Lon's word when gc2 reads GOOD over the band, the twelve Prolog entries PASS under stress, and the scan is deleted.

## 8. ⭐⭐⭐ THE LITERATURE READ AGAINST THIS DESIGN — AND THE BB ASSETS THE LITERATURE COULD NOT HAVE (ceo, 2026-09-18 19:4x CDT, CEO-897/898/899; the eleven papers of `/home/resources/gc-papers/`, CEO-882)

**Lon, in-chat to ceo after the reading, verbatim:** *"But our BB's give a completely other level of possibilities than these GC white papers you just read. Is that not correct? The BB's ALPHA and OMEGA traversals, and the THREE ZETAS, and entire graphs which are given ONE GAMMA and ONE OMEGA. Do you not have your checkpoints all over the place. Check that you are utilizing the fact that we are BB that have stamped DESCR's with mint_op and mod_id. And that we are BB with very clear BB FOUR PORTS. Think out of the box."* **He is right, the largest of the assets he names is MEASURABLY UNUSED, and § 8.2 is the ruling that follows from it.**

**8.1 WHERE THE DESIGN IS ORTHODOX, SO NOBODY RE-ARGUES IT.** Safe points at allocating call returns is the field's standard answer — Diwan/Moss/Hudson (PLDI '92) § 5.3, *"all calls can be considered gc-points since all allocation is done via a call"*; Wilson (1992) § 6.4.1 names it **safe-points** against **gc-anytime** and gives § 3's own argument for it. ⛔ **Diwan's sentence carries footnote 8, which is a PRECONDITION on § 3's completeness clause: *"this is true only if allocation is done via a call, in which case we must include inline allocations as gc-points."*** An inline bump-pointer allocation is an allocation that is not a call return, and § 3's *"the set of safe points IS the set of allocating call returns"* would be FALSE in its presence. **MEASURED, SCRIP `4e1683fa9`: we satisfy it — `grep -rn 'hp_cur|g_hp_cur|heap_top|alloc_ptr' src/templates/ src/emitter/` returns ZERO and the single `alloc` hit under `src/templates/` (`bb_call_bool.cpp:24`) is a frame-layout diagnostic.** Every allocation in emitted code is a runtime call. **The clause holds, and it holds CONDITIONALLY: emitting an inline allocation fast path makes that site a safe point or breaks the argument.** ⛔⛔ **CORRECTED WITHIN THE HOUR BY THE cto's CTO-79 (SCRIP `9f7dec1b3`), AND THE CORRECTION IS THE ceo's OWN INSTANCE OF THE SHAPE THE cto HAS NOW NAMED FOUR TIMES.** There IS an inline bump allocation in this tree: `rtx_alloc.s`'s armed inline carve, which `rt_str_alloc` and `rt_agg_alloc` reach **by TAIL JUMP** and which therefore never executes a `call rt_gcheap_alloc`. **The conclusion above survives — from EMITTED code the carve is still behind a `call`, so the emitted return is still the safe point — but the ceo's grep could not have seen it, because it swept `src/templates/` and `src/emitter/` and the bump lives in `src/runtime/rtx/`.** The right statement of the precondition is therefore NOT *“nothing bumps inline”* but ***“nothing in the MAPPED code bumps inline”***, and the population that must be swept to say so is the emitted code PLUS every asm entry it can reach. ⛔ **AND THE SAME TAIL JUMP UNDERCOUNTED THE DENOMINATOR EVERY NUMBER ON THIS PAGE WAS STEERING BY:** the census derived its allocating set from CALL EDGES ONLY, so the two asm allocators and the 99 functions reaching the heap only through them — `DUPL_fn`, `REVERS_fn`, `SUBSTR_fn`, `TRIM_fn`, `BCHAR_fn`, `_CHAR_`, `_COLLECT_`, `_ITEM_`, `_INTEGER_`, `_REAL_`, the SNOBOL4 string builtins — read NON-ALLOCATING. **allocating_entries 1584 → 1683, allocating_call_sites 210 → 237, unpolled 97 → 123: twenty-seven emitter call sites were outside the denominator entirely, neither polled nor counted unpolled.** A tail jump IS an edge for the question this census asks — control reaches the target, the target may allocate, and it returns PAST the jumper to the jumper's caller with that caller's frame live. ⭐ **WHICH MAKES § 8.3(a)'s PORT-GRAPH GATE MORE VALUABLE, NOT LESS, AND FOR THE cto's OWN STATED REASON — *“a derivation with one reader is never cross-checked.”* The port wiring is a SECOND, INDEPENDENT derivation of reachability (emitter control flow) against the census's first (binary call edges), and a disagreement between them is a red neither can produce alone.** Also orthodox and worth not re-deciding: stop-the-world is the modern answer, not a compromise (Zhao/Blackburn/McKinley PLDI '22 — *"regular, brief stop-the-world collections deliver sufficient responsiveness at greater efficiency than concurrent evacuation"*, against Pizlo's ~20 % for the best concurrent copier); and sliding preserves allocation order, which is a locality win beyond the fragmentation cure (Wilson § 2.3).

**8.1b WHERE IT IS UNORTHODOX AND WHAT THAT COSTS.** (a) ⭐ **Our maps are 0.12 % where the literature's are 16 %.** Diwan emits three tables per gc-point keyed by return address and measures them at **45 % of optimized code size naive, 16 % after δ-main plus byte packing** (§ 5.1–5.2); Agesen measured JVM stack maps at **57 % of bytecode** uncompressed. F1 moves that information out of the table and into the cells, so the map is 32 bytes PER GRAPH and `util_gc_descr_cell_census.py` read **0.12 % static frame growth** over 86 programs (§ 6.7(h)). **The exchange rate is table space for cell space and F2 already priced the other side: 25 % ceiling, ~10 % packed.** (b) ⛔⛔ **MANDATORY SLIDING HAS NO GRACEFUL DEGRADATION AND EVERY OTHER MOVING COLLECTOR IN THESE PAPERS HAS ONE.** Immix evacuates *opportunistically* — *"if immix runs out of space, it stops evacuating … if an object is pinned … immix simply marks the object, leaving it in place"* — and Nofl names why that matters: *"because Immix does not rely on evacuation of any individual object for collection to succeed, it is well-adapted to run-times that require the ability to pin some objects in place."* Rung 2 is *every live aggregate SLIDES* and F0 forbids the fallback under any name. **The order is not wrong; the consequence is that we have no slack, which is exactly why § 6.2b's blob frame is a HARD blocker and not a soft one** — an Immix-shaped collector pins that frame's referents and ships; we cannot, so the scan cannot be deleted for any program containing a pattern until rung 2 types that interior. (c) **The callee-saved register problem is old and has four answers, not two.** Diwan § 3 needs *"additional information about which registers were saved at each call point"*; Agesen § 2 confirms *"we also encountered that issue in implementing the scheme laid out by Diwan et al."*; Wilson § 6.4.2 names the space as **partitioned register sets** versus **variable representation recording**. § 6.5c's spill record is a THIRD point — a dynamic partitioned convention enforced at the poll boundary only — and § 6.5a's site-vs-graph measurement is what justifies the divergence. ⛔ **The fourth answer dissolves the class: OCaml has NO callee-saved registers in its calling convention at all** (Sivaramakrishnan PLDI '21 § 2.1), which is why their stack maps carry no register half. Not a proposal — our templates are built on r12–r15 as named planes — but read before anyone copies their simplicity.

**8.1c WHAT THE PAPERS EXPLAIN ABOUT READINGS WE ALREADY TOOK.** ⭐ **The cfo's 400 MB → 290 MB (CFO-872) is Boehm/Weiser's central sentence:** *"In contrast to a copying collector, such an error cannot result in incorrect program execution. It can however result in unnecessary memory consumption."* A conservative sniff's misidentification is invisible to every correctness witness and visible only in RETENTION — the cfo's own self-correction restated by the 1988 paper. ⛔ **And the mirror half is the argument for the 2026-09-17 reset, written 38 years early: the sentence holds ONLY BECAUSE BOEHM NEVER MOVES.** A collector that moves on a conservatively-identified word does not leak, it corrupts. Boehm measured his own near-miss rate at *"4 to 5 per garbage collection of a 2 megabyte address space"* in C and Russell; **our setting has no reason to resemble it — a SNOBOL4 descriptor's `slen` and payload halves are arbitrary integers by construction — so the sniff retaining half a heap is the EXPECTED reading, not an anomaly.** **Heap sizing has a known curve and pacing should stop being tuned without it:** Appel defines γ = memory / live data and measures **γ=3 → ~11 % overhead, γ=7 → ~6 %**, unrunnable below γ=2; Nofl restates it as *"giving a process 10 % more heap can cut its CPU overhead in half, at the tight end of the curve"*; Wilson § 8 estimates ~10 % of running time at ~2× data memory. **Our arena is a 512 MB default with an 8× virtual reserve (F4) and γ appears nowhere in the design.** **The generational park is priced:** Hosking/Moss/Stefanović (OOPSLA '92) — card marking at **256–512 bytes** optimal, remembered sets best overall, page trapping too coarse, generation filtering ineffective for cards; overhead 1.5–6.9 % typical and 16–37 % on a mutation-hostile benchmark; LXR's modern field-logging barrier is **1.6 %**. When Lon unparks it, the barrier is the decision and 256–512 B cards are the default, not a thing to re-derive.

⛔⛔ **§ 8.2 BELOW IS VOID AS A RULING (ceo, 2026-09-19 0x:xx CDT, CEO-904): `mint_op` NO LONGER EXISTS.** Lon, in-chat to the cfo, verbatim: *“Remove mint_op since it is derivative.”* — landed SCRIP `0a6f75394`, CFO-100: `src_node` is 24 bits and subsumes the op (the op is derivable from the node id, the node id is not derivable from the op; the 16-bit field saturated at ~3170 lines of beauty-density SNOBOL4). **The `DT_DATA` overload was cured by the cfo at SCRIP `6e9d024e4` (CFO-99) on `slen` — `DATA_INST_SLEN 0` / `DATA_ELEMS_SLEN 1`, pinned by a `DESCR_SASSERT`, 77 readers through two inline accessors — before CEO-898 arrived, and that cure stands.** What survives of § 8.2 is the MEASUREMENT (three bytes of provenance in every value, unread) and the literature parallel; the byte it named is now the upper byte of `src_node`. Read the section as history.

**8.2 ⛔⭐⭐⭐ THE ASSET LON NAMED IS REAL, IT IS THREE BYTES IN EVERY VALUE IN THE SYSTEM, AND IT IS DEAD. MEASURED, SCRIP `4e1683fa9`.** `DESCR_t` is `{ uint8_t v; uint8_t mint_op; uint16_t src_node; uint32_t slen; union {...}; }` (`src/ir/descr.h`:64–79) — **the tag byte is followed by a one-byte MINT OP and a two-byte SOURCE NODE, in every descriptor, at no cost, inside the 16 bytes the SysV register-pair return already requires.** The sentinels exist and are named: `DESCR_MINT_OP_UNSTAMPED 0`, `DESCR_SRC_NODE_UNSTAMPED 0`, `DESCR_SRC_NODE_OVERFLOW 0xFFFF` — **a provenance field that already knows how to say "I do not know", which is the instrument law at the value level.** **AND NOTHING USES THEM: 3 `.mint_op =` writers and 2 `.src_node =` writers across all of `src/`, and the only readers in the tree are a unit test's mismatch `printf` (`rtx_unit_test.c:39`) and a template COMMENT (`bb_keyword_icon.cpp:34`).** ⭐ **THIS IS EXACTLY WHAT THE PAPERS CANNOT HAVE AND SPEND THEIR HARDEST MACHINERY TRYING TO RECOVER.** Diwan § 3–4 — half his paper — builds a **derivations table** keyed by PC, with base values, invertible operations, and **path variables** for ambiguous derivations, for one reason: a C value has nowhere to carry where it came from. Agesen resolves the **jsr problem** by REWRITING BYTECODE to split a variable whose type is path-dependent, for the same reason: a JVM slot has nowhere to carry it. The README's two most-wanted missing papers — Goldberg *Tag-free garbage collection* (PLDI '91) and Stichnoth *Support for garbage collection at every instruction* (PLDI '99), both indexed in the Jones/Hosking/Moss bibliography at lines 2955 and 7824 — **are both attempts to reconstruct provenance from code that never carried it. We carry it and throw it away.**

⛔⭐⭐ **THE RULING THAT FOLLOWS, AND IT UNBLOCKS THE cfo's F6 STEP 5 TODAY (CEO-898, answering CFO-873 and `findings/FINDING-2026-09-18-cfo-dt-data-is-two-tags-wearing-one-code.md`): THE `DT_DATA` OVERLOAD IS CURED BY STAMPING `mint_op`, NOT BY MINTING A TAG CODE.** The cfo measured that `DT_DATA` carries two unrelated things — a `DATINST_t *` instance and a raw `DESCR_t *` element array (an Icon list's `frame_elems`) — with **identical `v` and identical `slen = 0`**, so *"the two are indistinguishable from the descriptor alone"*. **They are not: `mint_op` is in the descriptor, it is free, and it is zero at both forms today.** The cure: the three writers of the carrier form (`pattern_match.c:414`, `by_name_dispatch.c:5293`, `:7111`) stamp a NAMED constant, `gc_visit_one`'s `DT_DATA` arm branches on it, and the readers that spell `(ea.v == DT_DATA) ? (DESCR_t *)ea.ptr : NULL` (`rt_runtime.c:235`, `pattern_match.c:32`, `by_name_dispatch.c:1167`, `:5956`) tighten to test both fields. **FOUR REASONS IT BEATS A NEW TAG CODE, AND THE FOURTH IS THE ONE THAT DECIDES IT.** (1) **Nothing in the tag ladder moves.** The stride-8 ladder is fully occupied from `DT_P` 0x08 to `DT_MAP` 0xA0 and the only free slot is ABOVE `DT_ORDER`, which § 6.1 reserves for value-never-a-pointer codes — so an honest pointer-bearing code has nowhere to go without renumbering. `IS_DATA_TAG_fn` (`v >= DT_DATA && v < DT_RAW`), the three open-ended range tests and the whole `DESCR_SASSERT` web are untouched. (2) **It is 3 writers, not 49 sites.** The cfo's block-side alternative (`frame_elems` becomes a first-class `HB_DVEC`) is the same fact seen from the allocator and costs 49 conversions; this costs 3. (3) **It is the literature's own answer, in the shape only we can afford.** Agesen's cure for one name meaning two things is to SPLIT it; Diwan's is a derivation record. `mint_op` is a split carried in the value instead of in the code, which is Agesen's semantics at Diwan's precision and neither one's cost. (4) ⛔⭐ **A MISSED SITE FAILS LOUD INSTEAD OF SILENT.** With a new tag code, a writer you forgot emits the OLD tag and the typed walk dereferences it wrongly — the cfo's crash, again, quietly. With `mint_op`, a writer you forgot reads `DESCR_MINT_OP_UNSTAMPED` and **the visitor can REFUSE rather than guess** — rc=2 "could not measure", not rc=139. **That is the instrument law reaching inside the value, and it is available only because the sentinel already exists.** ⛔ **THE HAZARD THE cfo MUST MEASURE FIRST, AND IT IS NAMED IN OUR OWN TREE:** `bb_keyword_icon.cpp:34` records that word 0 is `{v:1,mint_op:1,src_node:2,slen:4}` packed and that *"a bare `mov ZRES(0),DT_S` zeroes slen along with it"* — **so there is emitted code that writes the tag with a sub-word store and collaterally clobbers `mint_op`/`src_node`.** The census of those sites is the fail-once and the precondition; a stamp that a template silently zeroes is worse than no stamp. **Row is the cfo's, on their own measurement; the ceo rules the shape, not the landing.**

**8.3 ⭐⭐ THE FOUR PORTS ARE A COMPILE-TIME CONTROL-FLOW GRAPH, SO TWO THINGS § 3 ASSERTS BECOME THINGS A GATE CAN COMPUTE (CEO-899).** Measured: the ports are labels and jumps emitted through `x86_alpha/beta/gamma/omega` (`x86_asm.h`:510–516, string port operands RETIRED by Lon s5), used by **130 / 50 / 124 / 93** of the 136 boxes. **The wiring IS the control flow, it exists in the emitter, and it is available before a single instruction runs — which is precisely the object Diwan and Agesen have to RECOVER by dataflow over optimized code.** Two consequences. (a) ⛔ **§ 3 says *"loop back-edges need no poll because a loop that allocates passes a safe point every iteration by construction"* — that is an ASSERTION, and both papers treat it as the thing that must be PROVED.** Diwan § 5.3 inserts a gc-point *"in all loops that do not have a guaranteed gc-point in them"*; Agesen § 3 makes *"backward branches gc points"* for the same reason. **We can do better than either, because we can decide it: walk the α/β wiring for cycles, and red any cycle that contains an allocating call and no poll.** That is a hermetic compile-time gate over a graph we already build, and it converts § 3's completeness clause from prose into a measurement — the same move that made `util_gc_census.py`'s unpolled count real. **It is also the honest check on `unpolled` — 97 when this section was drafted and 123 four hours later on CTO-79's tail-jump finding: a denominator of call sites does not tell you whether an unpolled site is REACHABLE without passing a polled one, and the port graph does.** (b) ⭐ **ω AND A GRAPH'S SINGLE γ ARE SAFE POINTS WITH A STRUCTURALLY EMPTY LIVE SET, AND THE PAPERS HAVE NO SUCH THING.** At a box's ω the box has conceded and its ζ contribution is dead; at a graph's one γ / one ω the whole activation is finished. **A poll at such a port needs no spill record at all** — § 6.5c's record exists to carry a callee-saved register that is live ACROSS the poll, and at a concede there is, by the port's own meaning, nothing of that box left to carry. Diwan and Agesen place polls where memory is CONSUMED; the ports let us also place them where liveness is provably MINIMAL, and we know those points by construction rather than by a backward liveness pass. ⚠ **STATED WITH ITS LIMIT SO IT IS NOT OVER-READ: a port poll is not a substitute for an allocating-return poll, because the allocation has already happened.** It is a poll with a BOUNDED DELAY — one box's worth of allocation — and that is exactly the latitude Rule 3 already bought by making the allocator grow the window instead of collecting. **So the step-4 worklist is not necessarily 97 sites: it is 97 sites MINUS those dominated by a port transition that is cheaper to poll, and the port graph is what makes "dominated by" computable.** This is a SIZING to be measured by the cto against the worklist, not an admission — twice a batch admitted on a classifier's word has cost a landing (§ 6.5b).

**8.4 ⭐ WHAT THE GRAPH-LEVEL SHAPE ALREADY BOUGHT US, NAMED SO IT IS NOT LOST.** Lon's *"entire graphs which are given ONE GAMMA and ONE OMEGA"* is the structural reason § 8.1b(a)'s number is 0.12 % and not 16 %: **the literature's unit is the FRAME and ours is the GRAPH**, and `gc_frame_map_t` is already one per graph (§ 6.3). A frame-unit collector needs a table entry per safe point because a frame's live set changes at every call; a graph with one entry, one γ and one ω has **three** boundaries at which its activation changes shape discontinuously. ⛔ **And it is why the map had to be found by CELL and never by return-PC (§ 6.2d): ports are JUMPS, not calls, so a port transition pushes no return address and the papers' entire keying mechanism is unavailable to us at port granularity.** That was not a deficiency to work around — it forced the cell-based design, which is the cheaper one.

**8.5 ⛔ THE OPTION THIS OPENS AGAINST A FROZEN RULE — NAMED FOR LON, NOT TAKEN.** F1 (*everything on the emitted stack is a DESCR*) is frozen on Lon's word and no seat reopens it. But the reading names a cheaper shape and the ceo states it rather than sitting on it: **the 136 box kinds are a CLOSED, compile-time set with known ζ layouts (`src/ir/zeta_depth.h`), so the spine could be walked by BOX RECORD instead of cell by cell** — one marker cell per record plus a static per-box-kind layout table, which is Diwan's δ-main scheme (§ 5.1: one main table per procedure, a 1-bit-per-entry delta per gc-point, 45 % → 16 %) applied to a FINITE set where his was unbounded. It would recover most of F2's 25 % ceiling. ⭐ **The honest counter-argument, so this is a decision and not a pitch: F1's whole virtue is that it needs no table and no lookup, and § 2b's reason for it (the spine is path-dependent) is unchanged — a box-record marker restores path-independence only if EVERY box writes its marker, which is the same discipline as tagging every cell, merely cheaper per cell.** The trade is a lookup per record against a tag per cell. **No seat acts on this; it is Lon's call whether F2's measured cost is worth reopening a frozen rule, and it should be re-asked only when F2's real spine number is in hand rather than its ceiling.**
