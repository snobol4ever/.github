# FINDING 2026-07-30 — SN4 CALL2BB 3b v1: ARG/RESULT-cell machinery LANDED (dormant behind SCRIP_CALL2BB_FC); the sr0 prefix rides the gamma spine; two named defects gate re-arming

Session: s21x-i (Claude).  SCRIP `8437c3d7`.  Scope: zeta_storage.c (planner) + emit.cpp (two dispatch grants) + bb_call_proc_staged.cpp (armed arms) + rt.c/rt.h (trap leaf).

## What landed
The full 3b machinery per the s21x-h spec: (1) PLANNER — `fc_call_ok` admits one-direct-leaf-arg registered non-generator user-proc calls into the value-spine walk (double env gate + `SCRIP_CALL2BB_FC` third gate), `fvcl` node-ptr table, call = unop-shaped in the d-simulation (consume 1 arg cell, produce 1 result cell IN PLACE, net-zero rsp, wpop = binop total-pop contract); (2) DISPATCH — the call gets the RESULT window (`op_fc_wbytes=16, base=zls_off(nd)` — wbytes NEVER bytes, the alpha-sub hook would shift the c2 landings' 16*k restores), the paired sr0 gets the ARG window (base = the arg's quad) so its existing `FRQB(slot, sb)` staging read self-rebases to `[rsp+sb]` with ZERO sr0 template edits — the s21x-h "fc arm inside FRQB" window-readiness, armed; (3) TEMPLATE — `c2farm()` armed arms: scc staging reads the arg cell by position `[rsp+scc_sb]`, emit-time bomb on armed-without-SCC-1-arg shape, `rt_c2b_arm_trap` loud abort at both L(5) slim-decline landings (the flat fallback does not exist as storage on an armed statement — correct-or-loud); L(2) result stores need NO edit (the window rebases FRQ(off)/FRQ(off+8) into the in-place cell).

## LAW — the sr0 role-0 prefix is threaded ON THE GAMMA SPINE (measured, the session's land mine)
Under CALL2BB lowering the true topology is `arg.γ → IR_SAVE_RESTORE(role 0) → CALL` — the gate-off dump's `LIT.γ=CALL` was the un-interposed world and cost one full bracket hunt (probes: `p0g_op=14`).  The value-spine gamma-adjacency fence therefore treats exactly this shape as transparent: gamma-chained + role-0 by UNION-TAG + next-is-the-registered-call-kind — the same pairing predicate the emitter's sr0 dispatch uses (`nd->γ` keying), planner and emitter in structural agreement.

## Mid-session proof of the design (before the fence widened)
With registration live, `OUTPUT = DOUBLE(21)` and `OUTPUT = DOUBLE(2) + DOUBLE(3)` both ARMED (STF `ADMIT reachable=4`, `live=1`) and printed oracle-exact m3+m4 — the arg cell consumed by sr0/scc staging through the windows, the result replacing it in place, the BINOP consuming two call-result cells top-two.  The d-simulation executed end-to-end on real calls.

## Two named defects (the re-arming blockers), then the quarantine
(D1) **FENCE-FLOATERS**: the v1 graph fence declines any graph containing role-1/2 SAVE_RESTORE — but every SNOBOL4 main carries the UNREACHED NRETURN cluster's floaters, so the fence suppresses main-statement arming wholesale (probed: REGISTERED count drops to the body X+X tree only).  Fix = reachability-scope the fence (the classifier's own `bb_src_of` discipline; name-testing unreached nodes is exactly what emit_stmt_frame_scan already refuses to do).  (D2) **LIB-COMPOSED CLASS**: with registration live, the crosscheck's SNO_LIB-composed 140/141 fail m4 (and pre-fence, 088/1010 recursion SEGV'd — the binop-arg and body-call classes, since fenced) while the same programs compiled standalone are REF-EXACT — the harness composition registers something the fences miss; MONITOR-FIRST bracket next session, nothing else (RULES.md).  QUARANTINE PROVEN THREE WAYS at `8437c3d7`: gate-off held EXACTLY (m3 311/4 · m4 311/2 S2 · DIV=2, sets verbatim); two-gate world byte-restored to the slice-3a gate-ON baseline EXACTLY (m3 313/2 · m4 311/2 · DIV=0); regen ×3 zero artifacts changed.

## Next rungs in order
(3b-2) reachable-only fence → main arming returns; (3b-3) monitor bracket on lib-composed 140 m4 with registration live; then N≥2 args (order inversion vs the affine window — position-based staging generalizes), binop-arg trees (the 088 class), dc pointer-arm, generator callees.  SCRIP_FCC_DEBUG probes are landed (guarded) for the next reader.
