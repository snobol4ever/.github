# FINDING s133 (2026-07-23, Claude) — FENCE0/FENCE1 SPLIT + SYNC-2 ζ RELEASE + SLOT-ELIDE S1

## 1. SYNC-POINT ζ RELEASE, sync point 2 — LANDED
STEP 0 answered: the s71 fence seal was PURE EDGE REWIRING (lower_snobol4.c spine walk) — right-of-fence ω edges aimed at pat_seal, resume repoints skipped, NO rsp restore at FENCE(P) commit. Bracket ends were already clean (HEAD-ω `mov rsp,[rsp+16]`; RELEASE rfc arm `x86_zls2_release_to_rspd` = the one-mov unwind). Fix: IR_MATCH_FENCE1 — one physical box per FENCE(P) in the ALT σ/φ-glue mold. α saves the watermark into its granted [rbp+off] quad (NO fc_geom BY DESIGN: the σ glue reads at dynamic post-P depth; rbp is per-activation so DEFER recursion is safe); na_s = one-mov bulk restore + γ; na_f = same restore (LIFO identity) + ω; β ≡ abandon (ARBNO L2 precedent). First-build bug worth remembering: the dup-walk queue sites (emit.cpp ~1785/1811) and the liveness pass (~1694) enumerate construct kinds — a new pair kind absent there makes its operand subgraph VANISH and PAIR(0) silently resolves to ω.

## 2. FENCE0/FENCE1 — Lon's naming ruling, and the manual split
FENCE0 = bare variable: backup ABORTS the attempt (manual: "stopped by the FENCE and fails"; fence-first anchors). As a box its body is α→γ, β→ω — two jmps — so it stays node-free (erased to edges). FENCE1 = FENCE(P): manual p.223 — backup PASSES THROUGH ("does not cause the match to abort, as does the &FENCE pattern"); only alternatives WITHIN P are hidden. SCRIP's spine currently applies FENCE0's abort-seal to BOTH forms (right-of-any-fence → pat_seal) — corpus-green, stricter than manual. RULING PARKED (cursor): over-seal legalizes the left-of-FENCE1 ζ release (collapses one-match parser retention to O(depth)); manual-strict keeps that mass live-by-spec (sbl's own json-match header provisions -s256m). CHECKPOINT = FENCE0 + dcap flush = cut-and-commit; sound because abort-on-backup legalizes both halves.

## 3. Why sync-2 didn't move the ladder — the rent model
json-match ladder unchanged (SEGV ≤32m, pass 64m) with 5 FENCE1 boxes verifiably emitting: P = SPAN|'' interiors are tens of bytes. The mass = EAGER PER-ACTIVATION FRAMES: every non-wiring node owns a 16B SU-A result quad; blob prologues carve Σ(all quads)+header (measured 208-736B) and rep-stosb zero it EVERY entry; ~40-80K *jvalue/*jelement activations = the 32-64MB floor and the s132 store storm. SPITBOL's rent for the same semantic state: (node*,cursor) per REACHED choice point, lazy, ~2-3 words; deterministic progress and recursion entries cost 0 (graph position IS the state; snapshot-cursor undo vs SCRIP's distributed per-box undo). Same information, ~30× the rent.

## 4. SLOT-ELIDE S0+S1 — LANDED
S0 census (SCRIP_SLOT_CENSUS=1): json-match 156 result quads / 43 value-refd / 113 dead (72%); pattern blobs 100% dead. S1 scratch-alias (zls_grant_elide): dead results of the positively-known zero-locals leaf class {ANY NOTANY POS RPOS LEN MATCH_LIT LIT_INT LIT_STR} alias one shared per-graph scratch quad (first dead leaf's own slot); templates untouched; SCRIP_SLOT_ELIDE=0 kill-switch. REGRESSION CAUGHT BY CROSSCHECK: ARBNO's operands[2] geometry bracket is a REAL slot read — treating ARBNO operands as wiring elided its LIT and broke 075/164/167/W04; fix = ARBNO stays in the reader class; watermark restored exactly. MEASURED: blob carves −20-25% (736→576, 400→368/304); SCRIP_STACK floor (32,64]→(32,40]; warm m3 36→37ms FLAT. Conclusion: frame zeroing/size is not the binding TIME term at this scale; Lon's 3× needs (i) dead result halves of locals-bearing kinds (decouple result from locals@+16 — S4 core), (ii) per-*DEFER activation overhead, (iii) possibly the FENCE1 ruling's left-release.

## 5. Gates (all green, RT_OPT=-O0)
sno smokes 7/7×2 · icon 14/14×2 · prolog 5/5×2 · crosscheck m3 302/8 · m4 302/6 · DIVERGE=0, fail-set name-identical to s132 watermark (test_case 140 141 [1020 1021 m3-only] 214 215 216) · fence variants 4/4 · json-match output byte-correct at 40m+. Artifacts regenerated ×3 (benchmark/feature/demo) under "s133 SLOT-ELIDE S1 + FENCE1 sync-2".
