# ⛔⭐⭐⭐ GOAL-PROLOG-100 — SCRIP PROLOG x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

## ⛔⭐⭐⭐ LON DESIGN RULING, 2026-08-24 s273 — **PROLOG USES `DESCR`, NOT `Term`. ALLOCATIONS RIDE THE THREE ZETAS, IN ORDER. NOT THE HEAP.**

**Lon, in-chat to hq_C, verbatim in substance:** *Prolog should not use `Term` at all, it should be using `DESCR`. It is out of whack design-wise. Things do not really need to live on the heap for our GDE stuff — look at Icon and SNOBOL4. The values for strings, yes. But we are using Byrd Boxes. **Any allocations better live on (1) the SPINE, (2) the ACTIVATION FRAME, (3) the STANDING (ROOT) ACTIVATION FRAME — IN THAT ORDER.** We use BB with RESULT and BB LOCALS. You can have as many RESULTS and LOCALS as needed at ALL THREE ZETAS. Tell Prolog to get with the program.*

⭐ **THIS ANSWERS OPEN QUESTION 8 (PZ-1(a)). IT IS NO LONGER THE IMPLEMENTER'S CHOICE.** s273 retired the seed *gate* and briefly left the shape free; **this ruling closes the shape.** The three-way menu (retained spine cell / activation-frame slot / heap cell with a spine handle) is now **ordered, not optional**: **SPINE first, ACTIVATION FRAME second, STANDING (ROOT) ACTIVATION FRAME third.** ⛔ **The heap arm is struck** — it was never an equal option, and choosing it is now a defect, not a trade-off.

⛔ **THE MEASURED STATE, verified by hq_C at s273 before routing this — Lon's read is exactly right:**
- `src/parser/prolog/term.h` defines a **complete second value representation** parallel to `DESCR_t`: its own `TermTag` enum (`TERM_ATOM`/`VAR`/`COMPOUND`/`INT`/`FLOAT`/`REF`) and a tagged union carrying `atom_id`/`var_slot`/`{functor,arity,Term **args}`/`ival`/`fval`/`Term *ref`.
- **448** `Term`/`Term_t`/`Term *` references across the Prolog frontend, `unification.c`, and `sync_monitor.c`.
- It is **heap-allocated**: `Term **args = malloc(...)` at multiple sites; **23** malloc/free sites in `prolog_parse.c` alone.
- **Only 2** Prolog-reachable files touch `DESCR_t` at all (`pl_cell.h`, `unification.c`).

⭐ **WHY THIS IS THE WHOLE POINT AND NOT A CLEANUP:** SNOBOL4 patterns, Icon generators and Prolog backtracking are **three syntaxes over one machine** — the four-port Byrd box. A box carries its values in **BB RESULT and BB LOCALS**, and *"you can have as many RESULTS and LOCALS as needed at ALL THREE ZETAS."* A language that invents its own heap-allocated node type is **not on that machine** — it is a second machine wearing the first one's ports, which is why Prolog keeps needing bespoke frame work (`rt_jmp_frame_lexprep2`) that the other two never needed. ⛔ **Only string VALUES belong on the heap.** Structure does not.

⛔ **STANDING INSTRUCTION TO hq_C (Lon: *"and you keep on him about it"*):** this is not a one-time telegram. **Every Prolog row this seat touches gets checked against this ruling**, and a Prolog change that adds `Term` surface or a new heap allocation is refused review until it is re-shaped onto the zetas. Recorded here so the obligation outlives any one session.

⭐ **RE-AFFIRMED 2026-08-31 ON THE TRAIL-RESIDENCY ESCALATION (Lon in-chat to ceo: *"The answer is (b). We have moved to the THREE ZETAS."*):** hq_P measured trail composition at the fatal stop across all 21 van Roy kernels — stack-resident 1951, heap-resident **0** — refuting commit 77925561's "all trail entries are PLJ-heap-resident" guarantee; hq_C escalated that *enforcing* the guarantee would relocate Prolog variable storage into the heap, a redesign contradicting the s273 ruling above. Lon refused it: **variables stay on the RSP/RBP zetas; the trail-unwind machinery is what gets fixed** (depth-tracked mark transport + refuse-loudly bounds tripwire, row `calling-convention-depth-tracked`, hq_P). The 77925561 guarantee is dead letter **by ruling**, not only by measurement — any code arm still predicated on trail heap-residency is wrong by law. `tasks/prolog-trail-heap-residency-storage-redesign.task.md` is CLOSED-REFUSED, never live.

⭐⭐⭐ **RE-AFFIRMED AND MADE THE FLEET'S TASK 2026-09-01 (Lon in-chat to ceo, verbatim in substance: *"I have the hard task and we'll get mode FLEET-8 to help. Let's eradicate the usage of Term structure in Prolog and replace with DESCR. Am I correct that Term is not strictly necessary … No need for Prolog to be special."* then *"We'll run in FLEET-8 mode."*):** MODE rewritten `FLEET-8` the same minute (ceo + hq_C + hq_P + hq_B + seat01..08). ceo's answer to the question, measured at SCRIP `8eac17da`: **yes — `Term` is a second value representation, not a necessity.** Emitted Prolog already runs on `DESCR` cells (`DT_PLREF`/`DT_PLVAR`; `by_name_dispatch.c` 1334 DESCR mentions against 8 `Term`); `Term` survives only (i) inside the legacy C services — `unification.c` 208 word-refs, `rt_runtime.c` 38 (env/copy/findall as `Term **`), `builtins/resolution.{c,h}` 16 — which RE-MATERIALISE a heap `Term` from the cells through `pl_cell_conv.h` (`pl_cell_to_term`, `rt_pl_cell_to_term_named`) on every unify/write/copy/findall, the converter being the proof of redundancy; and (ii) in the parser, which builds `Term` trees (`prolog_parse.c` 85 · `prolog_lower.c` 26 · `prolog_builtin.c` 24 · `prolog_atom.c` 17) and converts them to the common `tree_t` (`pl_assert_term` at `lower_prolog.c:1229`) where every other frontend parses into `tree_t` directly; plus `driver/{scrip.c,driver_private.h,sync_monitor.c}` includes. 490 word-refs total (s273 baseline 448; 469 at the 08-27 mint — still growing). **ROUTING:** row `prolog-term-to-descr-eradication` (hq_C, minted 2026-08-27 on this ruling) is UN-PARKED from its PZ-4 wait by Lon's word and re-stated `PARKED-UMBRELLA:hq_C-slices-FLEET-8`: hq_C sequences and mints the one-row-one-seat slices (ratchet slices with the PRINTED Term-ref count per commit; SHARED-NODE VERDICT SCOPE on every landing; the baton's original guidance stands). Natural cut, for hq_C to confirm or refute by measurement: runtime services onto cells first (unify/write/copy/findall/resolution read `DESCR` directly, converters deleted), parser to `tree_t` second, `term.h` deleted last; slices that need a γ-surviving frame for WHERE a logic variable lives queue behind PZ-4 `prolog-pz4-gamma-retain-activation-frames` (hq_C-owned, `BLOCKED-ON:calling-convention-depth-tracked`, hq_P) — the keystone is unchanged, the rest of the conversion is not gated on it. ⛔ **INSTRUMENT DEFECT FOUND AT ROUTING, ceo's own mint (RULES.md § THE INSTRUMENT LAWS — fail once and pass once):** the row's DONE-WHEN is FALSE-GREEN today — run verbatim under `S4E_HOME=/home/claude` it exits 0 (`RESULT: PASS`) with `src/parsers/prolog/term.h` present and 490 refs live, because clause 1 tests the RETIRED path `src/parser/prolog/term.h` (the `src/parsers/` rename silenced it) and clause 2's regex `\bTerm_t?\b` matches only `Term_`/`Term_t` (0 hits), never `Term`. **hq_C re-cuts it before the first slice is minted** and every slice row inherits the corrected count. Also from the same ruling's fallout, hq_P (2026-09-01): the 77925561 residency guarantee — dead letter by ruling — was the SAFETY ARGUMENT for a live bypass, `scrip.c:1904`/`:1907` `g_plw_floor_bypass=1` for Prolog zframe graphs (switches `plc_dead_cstack` off for every trail entry); the bypass has a second, still-true cause (`-O0` `dop_unwind_nothrow` leaves RSP 8 mod 16, so `plc_dead_cstack`'s own sscanf `movaps` SEGVs; bench m3 6/22→12/22) but nothing now asserts a bypassed entry is SAFE. ceo ruling: minted `prolog-plw-floor-bypass-safety-unproven` at rank 2 (not the frozen burn-down; not blocking), hq_P's lane, and it is IN SCOPE of this umbrella — when trail entries ride cells on the zetas the bypass's premise is re-examined, not inherited.

⭐⭐ **AMENDED 2026-09-01, LATER THE SAME DAY — LON: *"We are going to mode FLEET-16. Prolog is the #1 priority. All around test, performance, Term struct eradication."*** MODE rewritten `FLEET-16`; the priority rows of GOAL-HQ-COMPLETE.md and GOAL-HQ-PERFORM.md now read **PROLOG #1 · SNOBOL4 #2 · Icon #3** (SNOBOL4's blocking floor `make test` remains the control arm on every landing — the order names where the fleet's hands go, not what may regress). **THE SLICES ARE hq_C's** (FINDING `.github 95c50446`; table in the umbrella's `## NEXT`): s1 write/format/printers · s2 compare/sort/pairs · s3 copy/findall/nb/aggregate · s4 typetest/functor/univ/succ (+ deletes `prolog_unify.c`) · s5 dynamic-db/flags/streams · s6 parser builds `tree_t` directly · s7 dead resolution env layer deleted · s8 `term.h` + converters deleted (carries the umbrella's DONE-WHEN; BLOCKED-ON s1–s7); s1–s7 at rank 0 by Lon's word (hq_C re-ranked after measuring 40 minutes of starvation behind fifteen older rank-1 rows — dispatcher row `next-tiebreak-by-mint-time-not-file-order`); a ceo-drafted duplicate set was withdrawn unminted. **THREE CORRECTIONS TO THE BLOCK ABOVE, measured by hq_C (accepted as written):** (a) the service ABI is ALREADY cells — 75 exported `rt_pl_*_cell` in `unification.c` manufacture a `Term` internally at 106 converter sites and emitted code reaches them only via `by_name_dispatch` — so runtime-first is not merely first, it is independent of everything else; (b) `pl_assert_term` is declared three times and DEFINED NOWHERE, zero calls — the parser→`tree_t` boundary is `prolog_lower.c:lower_term` (static), and the sentence above naming `pl_assert_term` as the bridge is wrong; (c) the resolution-engine `Term **env` layer (`resolve_env_new`, `rt_env_current`, `resolve_catch_push`, `resolve_throw_term`, `resolve_bb_env_install`) has ZERO callers under `src/` — live variables already ride `DT_PLVAR` cells — so **PZ-4 gates no slice**; that layer is a deletion (s7). The "slices needing a γ-surviving frame queue behind PZ-4" sentence above is therefore VOID for this umbrella; PZ-4 stays hq_C's own keystone for the multi-clause/resumable-predicate ladder. Also measured by hq_C: `test_gate_pl_gz2/3/4` are dead instruments (0 `gzq` refs in src; row `test-gate-pl-gz-dead-instruments-measure-a-retired-path`; pre-existing red recorded on the umbrella ledger so no slice is blamed). **FOR SEATS 09–16:** all 17 FREE Prolog rows re-ranked to rank 1 by ceo on Lon's priority (QUEUE.tsv snapshot `.bak.ceo-prolog-priority-*`), so `next` serves Prolog before anything else; hq_C mints the Prolog TEST rows (the rung suite reads `PASS=4 FAIL=11 TOTAL=15` in BOTH modes at SCRIP 8eac17da — eleven named reds — plus the vendored GNU/SWI conformance suites and the ISO coverage audits), hq_P the Prolog PERFORMANCE rows (the vanroy 21-kernel board on the two-number basis vs swipl/gprolog; `prolog-plw-floor-bypass-safety-unproven` is already in that lane).

⭐ **INTERPRETATION 2026-09-01 (hq_C's reading on seat10's slice-5 question, confirmed by ceo; Lon may correct it in one word):** the s273 order — spine → activation frame → standing/root frame, heap only for string values — governs VALUES during matching. A dynamic-database clause (`assert`/`retract`) is PERSISTENT program state that outlives every activation and every backtrack and GROWS without bound at run time, so no zeta can hold it: it is a genuine escaper in the sense of RULES.md § BB FRAME-PLACEMENT CRITERION (the same clause that sends strings, heap cells and Raku's escaping closures to the heap). RULING: stored clauses are DESCR cells in the runtime workspace arena (`rt_ws_alloc`) — the SHAPE law (DESCR, never Term) is what Lon ruled and it is kept; the predicate table that indexes them is fixed-size root-level state and lives in the STANDING frame. Slice s5 proceeds on this; if Lon corrects the residency, the cells move and the shape does not. Routed also to the umbrella ledger.

## ⛔⭐⭐⭐⭐ LON ORDER, 2026-09-02 09:55 (in-chat to ceo) — **GIVE PROLOG ITS ACTIVATION ζ NOW. PZ-4 FIRST.**

**Lon, verbatim:** *"So what are we waiting for, give Prolog its activation ZETA like all the rest. OMG! Go fix it."*

**Trigger (ceo assessment, same minute):** the design is sound at the front — Prolog lowers to the same 136 shared boxes as Icon and SNOBOL4, no interpreter, DESCR law finishing — and broken at β: hq_B measured a two-clause `fact/2` with ZERO rbp-relative instructions and zero `push rbp`; Prolog boxes have no activation frame, so nothing survives γ for β to resume. The shallow board reads 351/371 both modes; the deep rungs read 3/15 and every red is a backtracking shape; 8 of 21 van Roy kernels crash. Control that belongs in ports lives in C (`by_name_dispatch.c` 7,528 lines: string dispatch, ten setjmp/longjmp sites, a call/N solver, a polled exception flag).

**The order as law for this lane:** PZ-4 (`prolog-pz4-gamma-retain-activation-frames`, C21, ASSIGNED hq_C) is FIRST — before T9's tail and before C36. The design is Lon's word, not a sitting: Prolog gets the **ζ-ACTIVATION-FRAME on RBP exactly as Icon has it** under the FRAME-PLACEMENT CRITERION — every predicate call is a box whose frame survives γ so β can resume it; clause alternatives ride the choice box's β path; cut is the choice box's ω; the trail is β's undo and lives in the frame (P10 folds in); `x86_fb()`/`x86_fb_pinned()` answer rbp for Prolog procs instead of the stub's rsp/0. Then the crutches leave the C runtime as one program, *Prolog control through the ports*: C36 (call/N compiled through EVAL's path, solver deleted), P6 (builtins wired at compile time), P7 (no setjmp failure), C37 (exceptions propagate through ports, not a polled flag). Acceptance is measured: PZ-4's clause (c) bomb silent; rbp frames present in the `fact/2` witness; C22/C23/C24/C26 witnesses green; the rung-suite backtracking reds and the van Roy REFUSE kernels re-run; master not worse than 351/351 both modes per milestone.

**LON ESCALATION, 10:00, verbatim:** *"The answer to how Prolog is doing is TERRIBLE. If we did not do proper THREE ZETAS for Prolog then go do a complete re design. I want that bad code deleted now. It has been too long coming."* → **THIS LANE IS NOW ONE PROGRAM: PROLOG REDESIGN ON THE THREE ZETAS.** hq_C leads the mechanism, hq_P the perf shape, the ceo writes the design page `ARCH-PROLOG-THREE-ZETAS.md` (the map of every Prolog construct onto boxes and zetas, the deletion list, the acceptance) and the HQs fill its mechanism sections; seats take the rungs. THE DELETION LIST, each a rung whose DONE-WHEN is the symbol gone from `src/` and from `nm -D libscrip_rt.so` with the boards not worse: the `plc_*` call/N solver and `rt_pl_call_gen` (C36) · every setjmp/longjmp implementing Prolog failure or cut (P7; ten sites in `by_name_dispatch.c`) · the by-name string cascade for Prolog builtins (P6) · the polled `rt_pl_throw_pending` flag (C37; one checker) · Term to zero with both headers gone (T9; 75 at `5ebc7961`). THE BUILD: PZ-4 first (the ζ-ACTIVATION-FRAME on RBP for every proc, Icon's model), then PZ-5, with clause alternatives on the choice box's β, cut on its ω, catch/3 a box with its handler on β, the trail in the frame. Language identity stops at lower: the emitter gates the frame on an IR property of the graph, never on a language flag (`icn_cells_graph` is misnamed for what it will now mean). GATE of the program: rung suite 15/15 both modes · van Roy REFUSE 0 · master ≥ 351/351 · SNOBOL4 floor green.

**⭐⭐⭐ struct Term IS DELETED ON ORIGIN — SCRIP `46db4457` (ceo, 2026-09-02 10:40, on Lon's 10:35 order):** `term.h`, `pl_cell_conv.h`, `prolog_unify.c`, `prolog_runtime.h` gone; the five constructors, the dead second choice-point/catch/env machine in `resolution.{c,h}`, the struct-tree trail, `g_resolve_nb`, `PlCallSt` gone; `grep -rcw Term src` = 0 (490 → 0 in 27 hours); the ratchet gate passes at zero. Gates on the pushed tree: smoke 5/5 both modes, parser PASS, rung38 10/10, SNOBOL4 floor 1679/0 both modes, Prolog master **348/348 of 371** = origin's identical entry set (PZ-4 clause (a) moved 351 → 348). The umbrella row `prolog-term-to-descr-eradication` stays OPEN on its own last clause — the board at fail=0 both modes, which is § 6's program gate — released under CEO mode with the receipt on its ledger.

**THE DESIGN PAGE: `ARCH-PROLOG-THREE-ZETAS.md` (landed 2026-09-02 10:25, `8426dc57`) — measurements, the three zetas applied, the box map, the mechanism seams with their lines, the deletion list with rows, the value services that stay, order and gates. It is the order of the work for this lane; hq_C fills its mechanism sections as PZ-4 lands.**

## ⛔⭐⭐⭐⭐ LON ORDER, 2026-09-01 ~21:15 (in-chat to ceo, on the Term report *353 of 490 remaining*) — **WHACK THE Term STRUCT NOW. THE SLICES ARE RETIRED.**

**Lon, verbatim:** *"So then it is time to whack it. Completely remove the Term struct and all it's references. I do not want to see it every again. Whatever process is being done is too slow. So whack it now!"*

**Execution (ceo ruling for speed, same minute):** ONE OWNER — hq_C — big-bang in its own root, compile-driven: delete `src/parsers/prolog/term.h`, `pl_cell_conv.h`, `pl_cell_conv_test.c` first, then follow the compiler to zero across the 19 files carrying the 353 word-refs at `e182a71a` (`unification.c` 116 · `prolog_parse.c` 85 · `prolog_lower.c` 26 · `prolog_builtin.c` 24 · `prolog_atom.c` 17 · `rt_runtime.c` 13 · `resolution.h` 12 · `prolog_builtin.h` 9 · `by_name_dispatch.c` 4 · the rest single digits), every consumer rewritten onto DESCR cells per the s273 law. Finish line `grep -rcw Term src --include='*.c' --include='*.h' --include='*.cpp'` sums to 0 and the two headers do not exist; on the way and at the end: Prolog smoke 5/5 both modes, parser suite PASS, master board not worse than 217/371, SNOBOL4 floor FAIL=0 (shared-node control arm), the consume-the-value witnesses (write/1 + compare on every converted class, five runs). Push per compile-clean milestone. The five slice seats (T1 seat06 · T2 seat07 · T4 seat15 · T5 seat03 · T6 seat02) push what compiles or save the diff to `postoffice/salvage/` and release within ten minutes; T7 parked behind the umbrella; T9 `prolog-term-to-descr-eradication` FREE for hq_C's claim; the slice rows close on their own DONE-WHENs when the umbrella lands. The two-encodings trap (atoms as DT_A vs DT_S, readers accepting both) and the functor/3 lesson (a representation change is validated only by consuming the value) are the two hazards named on the umbrella baton.

## ⛔⭐⭐⭐ LON RULING, 2026-09-01 ~20:53 (in-chat to ceo, routed the same minute) — **NO PROLOG RUNTIME INTERPRETER. `call/N` RE-USES THE EVAL/CODE RUNTIME-COMPILATION PATH.**

**Lon, verbatim:** *"Remove that Prolog runtime interpreter, we already decided that Prolog should be re-using EVAL and CODE."* — and, a minute later, *"Remove the dead AST walkers."*

**Trigger (ceo audit on SCRIP `6ede7c31`, FINDING `FINDING-2026-09-01-ceo-modes-3-4-no-ast-or-ir-walking-…`):** modes 3/4 walk no AST and no IR; the ONE live runtime interpreter left in the tree is Prolog `call/N`'s solver in `src/runtime/by_name_dispatch.c` — `plc_slv_t` (:4458), `plc_build`/`plc_build_resolved` building a solver tree from the goal term by `strcmp` on functor names (:4508-4557), `plc_det_exec`'s `switch (s->op)` (:4493), `plc_next` (:4570), a runtime term reader (:4690-4749), entry `rt_pl_call_gen` (:4752); 136 `plc_` references, witnessed live by gdb on `G = write(hi), call(G)`.

**The ruling, as law for this lane:** every goal that is not known at compile time — `call/1..8`, the goal arm of `catch/3`, `findall`/`bagof`/`setof`/`forall`/`\+` over a variable goal, `assert`ed clause bodies invoked later — is COMPILED at run time through the same path EVAL and CODE use for SNOBOL4 (`src/runtime/runtime_eval.c`: build the graph → `optimizer_run` → `emit_chain` → jump; the pattern JIT at `src/runtime/rt/bb_pat_build.cpp:84-110` is the same shape, compiled once at construction, blob cached) and then EXECUTED as wired boxes. The `plc_*` family is DELETED, not bypassed; no runtime dispatch loop over goal terms may exist anywhere in `src/runtime`. The unknown-functor→user-predicate class (C10, cured by seat15 in SCRIP `fb98a05eb` inside the solver) is preserved by making its witnesses the acceptance tests of the replacement, never by keeping the solver. Rung: MASTER-PLAN ⭐C36 (hq_C mints, rank 0). The dead AST walkers named in the same FINDING (`scope_patch`, `static_get/set`, `driver_call.c:139-380` over a never-assigned `g_exec_prog`, the stubs `eval_ast`/`eval_ast_pat`/`eval_node`/`exec_stmt`) are deleted under the second order — rung ⭐I14 (hq_C mints, rank 0).

## ⛔⭐⭐ LON OVERRIDE, 2026-08-24 s273 — **SEED-FIRST IS RETIRED. PROLOG HAS LIBERTY.**

**Lon, in-chat to hq_C, verbatim in substance:** *that is a silly standing rule — it came from when we were at ground zero. Give the Prolog seats liberty to get the work done.*

⛔ **WHAT THIS RETIRES, effective immediately:**
- The **seed-first law** (§ "Across-the-board admission…", struck below): *prove a new machine shape as hand-written `.s` against the oracle BEFORE compiler changes*. **No longer a precondition for anything.** A seat may change the lowerer/templates directly.
- **Open question 8's gating clause** — *"Lon rules on the hand-written seed before lowering changes"* (PZ-1(a), the ζ-spine logic-variable + choice-record shape). ⭐ **The QUESTION stays interesting; it stops being a GATE.** A seat picks the shape, implements it, and grades it. If measurement says the choice was wrong, that is a finding and a follow-up row — not a reason nobody was allowed to start.
- Any row held `BLOCKED` on either. **`prolog-multiclause-uninit-lexprep-frame` is unblocked to `FREE`, rank 0**, as of this ruling.

⭐ **WHY, recorded so it is not re-litigated:** the rule was written at ground zero, when no Prolog machine shape existed and a wrong guess cost the whole substrate. That is no longer where this project is — the shape is largely built, the failures are concrete, and the rule had stopped protecting anything. Its measured cost this session: `rt_jmp_frame_lexprep2` is a **no-op called by every 2+-clause predicate's prologue**, root-caused with gdb by seat01, who then correctly stopped because of this rule — leaving **every multi-clause Prolog predicate** riding an uninitialized frame while the fix waited on a ruling. ⛔ **A rule whose only remaining effect is to make correct engineers stop is not a safeguard.**

✅ **WHAT STILL BINDS — and this is NOT the seed rule wearing a new hat:**
`src/templates/xa_flat.cpp` is **shared with Icon**. Any change there is a cross-language change, so it is graded on all three frontends with a same-tree control arm: **SNOBOL4 holds 365/365 both modes SKIP=0**, and the **Icon rung suite does not move from its pinned number**. ⭐ That is the standing SHARED-NODE rule and it is about not silently breaking two other languages — it gates **landing**, never **starting**. Lon's ruling removes the permission step, not the measurement.

**Opened 2026-08-16 s165 (Lon). REPLANNED 2026-08-21 (Lon in-chat, seat2, verbatim in substance: *"This is a new world of code, and we will be doing things EXACTLY like SNOBOL4 has been doing: THREE ZETAS, FORTH-style stack, all of it. … Make a plan, rungs, and steps based on your discovery of the gaps in Prolog that SNOBOL4 has closed."*).** The pre-conversion ladder (R-0h..R-6, the zframe-repair track) is RETIRED — full text in git (`85542ed6` and before); every deleted Prolog GOAL-* name still resolves HERE (dispositions git `94fa9093`). **Discovery record: `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md`** — the SN4 campaign arc C1–C16, the TEN GAPS with file:line, Lon's ruling SHAs, the twelve facts. Read it before touching any rung.

## ⛔ FACT RULES (RULES.md is the authority; two locally critical)
- **⛔⛔⛔ NO NEW GLOBAL VARIABLES without Lon's explicit in-chat permission that session; the ask must be a big unmissable banner** (name, type, owning file, purpose, why registers/stack cannot carry it). State rides registers (r10/r11 wires) and the stack. This conversion DELETES globals (census: ~40 `g_pl_*`; the 15 CP/resume ones die at PZ-2..PZ-5) — it never adds one.
- ⛔⛔ **NO `-O2` BUILDS. EVER (Lon, 2026-08-23 s262 FACT RULE — this SUPERSEDES the O0-DEV-O2-BENCH text that stood here, which is DEAD and must not be followed).** `RT_OPT` is `-O0` for development AND benchmarks AND demos. Never pass `RT_OPT="-O2 …"`, never build an `-O2` RT_TAG, never quote an `-O2` number as the current state. TWO REASONS, the second the stronger: cost (~9m30 vs ~1m40 per template-touching rebuild, paid on every arm of a measure-and-cure loop), and ⭐ **it measures a compiler we are deleting** — the RT is moving to register-aware ASM (`GOAL-RTCC.md`, `src/runtime/rtx/*.S`) with C kept for at most a few large algorithms, so an `-O2` figure grades gcc's optimizer over code that will not exist. The LABELING duty survives and now reads `-O0`. Authority: `RULES.md` § NO `-O2` BUILDS. ⚠ `make` skips recompiles when only `RT_OPT` changes — `rm -f out/rt_pic/*.o out/libscrip_rt.so` first. Label every perf number with its `RT_OPT`; it reads `-O0`.

## ⭐⭐⭐ THE MODEL — THE THREE ZETAS: THE SN4 MACHINE, PROLOG EMBODIMENT
Lon's s81 register ruling (verbatim: *"RSP for ZETA SPINE, RBP for ZETA ACTIVATION FRAMES, and RBP for ZETA STANDING AREA"*); cells substrate is THE routing, one system (`db728001`). Tier verdict comes from ONE authority — `zdp_tier` (`zeta_depth.c:131`): STANDING if bracket-lifetime, ACTIVATION if α-depth unknown or resume unsound, SPINE otherwise; **refusal to SPINE is the safety property**, never a per-op filter.

| tier | register | SN4 embodiment (LANDED) | Prolog embodiment (TARGET) |
|---|---|---|---|
| **ζ-SPINE** | RSP | per-BB FORTH cells: α carves own K (`zd_k`, one authority), result at cell+0, consumers read staged depth-differences (`ZOPQ`/`op_zread`) NON-POPPING, γ SUSPENDS the cell, β free (LIFO home), ω frees own K; ONE statement-terminal release (`op_zgpop` at the choke); choice/resume records ride the spine | goal operands/results as cells (PL-ZK arms exist, emit.cpp:2237-2245); **the CLAUSE-CHOICE RECORD as a 32B record** — {resume · δ/TAG · prev_CP · saved_MH}, encoders at x86_asm.h:2125 (Lon 2026-07-28): TAG 0=CHOICE (clause alt) · 1=BASE (predicate entry barrier) · 2=FENCE (cut) |
| **ζ-STANDING** | RBP | ROOT activation per match statement (`emit_match_rbp`, `frame_slot_scan` slot registry), whacked at END/fail | per top-level goal-statement bracket; registered slots for cross-goal housekeeping |
| **ζ-ACTIVATION** | RBP chained | per `*P` defer / dynamic-K site; **heap-fb ADOPT** for β-resumable graphs (`emit_heap_fb_adopt`, emit.h:654 — "a β-resumable activation cannot ride an unwinding stack") | per PREDICATE activation: multi-clause/resumable predicate = ACTIVATION tier; **RETAINED while its choice record lives, freed LIFO by backtrack-ω / cut** — gprolog's law `Local_Top = ((B >= E) ? B : E)` (wam_archi.def:120) in three-zetas form |

**WAM correspondence (SN4 built a WAM; the canon confirms):** spine ≡ local stack · choice record ≡ CP frame (resume≡ALTB · prev_CP≡BB · saved_MH≡TRB; HB/CPB/BCIB/CSB deferred as before, PL-TRAIL-COND stays a feature rung) · cut ≡ `Assign_B(BB(B))` ≡ SWI `discardChoicesAfter` ≡ TAG-2 watermark restore · SWI CHP_CLAUSE/CHP_JUMP/CHP_CATCH ≡ the TAG taxonomy · trail ≡ the base-pinned undo arena (RULING 4: undo survives forward whacks, does not commit on failure; COMMIT-THEN-WHACK).
**Register contract of record (binds Prolog at conversion):** R10/R11 = γ/ω wires, LIVE never scratch, WIRE-ORDER γ@+0/ω@+8, `push r11; push r10`; FUNCTION LINKAGE = BLOB INTERFACE (Lon `433dc27e`) — Prolog's current rcx/rdx proc-enter wires CONVERT (PZ-4). RBX = heap frontier · R12 = undo-arena top (SN4 CAS; Prolog trail marks ride records, the trail data structure stays runtime) · R13/R14 = Σ/δ scan-reserved · ⭐ **R15 = CP (the choice-point register) IN PROLOG GRAPHS — LON RULING, in-chat 2026-08-21 (seat2), AMENDING THIS CONTRACT:** the seed parks CP in r15 and Lon ruled *keep r15, amend the contract* — Σ/δ/Δ are **scan-only** reservations, and a Prolog graph does no string scanning, so R15 is free there and carries CP. `fail` is therefore uniformly `jmp [r15]` and cut is `r15 = [r15+16]`. ⛔ The reservation still binds any graph that scans; this releases R15 for Prolog graphs ONLY, and PZ-2 must not assume it in shared code. · R9 = RTCC/GVA base, R8+tier = RTCC slots. ⛔ **THE RTCC BANK DOES NOT NEST** — a C-entered blob without its register plane jumps residue (SN4's M1 wall, s194); Prolog adds NO C→BB entry that skips the wire push.
**Canonical sources (RULES.md law):** primary source MANDATORY for semantics rungs — local seat: `/home/resources/gprolog-master` + `/home/resources/swipl-devel-master` (unpacked); web seats ask Lon. Consult per construct as you touch it. `SCRIP/doc/*STUDY*.md` are notes, not a substitute.

## ⛔ DEFINITION OF DONE — "100%" MEANS THIS AND NOTHING LESS
1. Rung suite 164/164 BOTH modes · smoke 5/5 · honest board green with ORACLE_CRASH=0 (rc≠0 counted, R-0h landed s166).
2. bench-22 green=22 m3+m4, oracle-correct vs gprolog/`.ref`; van Roy classes closed (findall/forall serialize · assert/retract mid-computation · `is` over runtime-built terms · arity>8).
3. **ONE MACHINE** — the SN4 ζ substrate (Lon 2026-08-21 directive resolves the old cells-vs-zframe A/B): zframe arm, retry/cp/zf3 stacks, pending-cursor apparatus, and their killswitches DELETED; `grep -c 'SCRIP_PL_CELLS\|SCRIP_PL_ZFRAME\|SCRIP_ZD_PL_\|SCRIP_PL_GAMMA_RETAIN' src/` reads 0; the 15 CP/resume globals gone from the census.
4. Gates strict + green: `test_gate_pl_no_new_global.sh` ratchet 14→0 · `test_gate_emit_no_lang.sh` OK · `test_gate_template_medium_invisible.sh --strict` GREEN (remaining: `bb_glue_flat.cpp(4)` + `xa_flat.cpp(8)`) · TEMPLATE-ONLY/BOTH-MEDIUM greps 0 · `handoff_status.sh` COMPLETE.
5. Perf LAST, only under THE INSTRUMENT: emitted-instruction share ≥90% (s154 baseline 55.0%) · ≤1.2× then ≤1.0× GNU on the rail. **Wall time DISQUALIFIED in this container class (s164).**

## THE INSTRUMENT (boards are RUN, never transcribed; re-derive every count fresh)

⭐ **MASTER BOARD BASELINE 2026-09-01 (ceo, SCRIP `8eac17da`, `python3 scripts/corpus_suite_harness.py run corpus/tests/prolog/ALL.pl corpus/tests/prolog/ALL.ref --lang prolog --modes m3,m4`, RT_OPT=-O0, oracle refs = ALL.ref):** `total=371 · m3 pass=218 fail=5 crash=139 xfail=1 xpass=8 · m4 pass=218 fail=5 crash=7 skip=132 xfail=1 xpass=8`. The 139 m3 crashes are ONE class — `signal 6`, stderr `[IBB] FATAL: mode-3 driver: main BB graph not found` on clause-only entries with no main (witness `foo(X,Y) :- X @>= Y.`); the 132 m4 `--compile failed` skips are the same population from the other mode. Row `prolog-directive-only-file-fatals-no-main-bb-graph` re-ranked to 0 on this measurement (37% of the board behind one driver defect). ⛔ SCORE.md's Prolog section at this writing shows smoke/parser/crosscheck only — the master board number was not on the scoreboard (routed to hq_B).
⭐ **MASTER BOARD RE-RUN 2026-09-01 (hq_C, SCRIP `54cf54fd`, binary rebuilt at `43e30f0d` 18:16 after seat02's stale-binary finding, machine load 15–33 with six slice seats' boards concurrent):** `SUITE_BOARD family=ALL total=371 m3_pass=218 m3_fail=5 m3_crash=129 m3_hang=10 m3_unproven=0 m3_skip=0 m3_xfail=1 m3_xpass=8 m4_pass=218 m4_fail=5 m4_crash=7 m4_hang=0 m4_unproven=0 m4_skip=132 m4_xfail=1 m4_xpass=8` (rc=1). Same pass/fail/skip as ceo's baseline; **10 m3 entries moved CRASH→HANG** — under that load a timeout is not evidence of a hang, so those 10 are re-tested individually at low load before anyone rows them; **xpass=8 in BOTH modes** — eight XFAIL markers now pass and are promotion candidates (the converter's hold-out rule applies: promote only a distribution with NO non-PASS arm). Two earlier detached runs of this board died of cross-seat `pkill -f corpus_suite_harness` (seat13 18:19 local, hq_P 18:28; both self-reported) — a board that dies without a SUITE_BOARD line was killed, not red. The per-entry log is the C2 input (`FINDING` to follow when C1 lands).
```bash
bash scripts/test_smoke_prolog.sh                # 5-program tripwire, 3 columns
bash scripts/test_prolog_bb_honest.sh            # counts + exit-gates ORACLE_CRASH
bash scripts/test_prolog_rung_suite.sh           # 164 × interp+compile
bash scripts/test_bench_prolog_modes.sh          # bench-22 board
LD_PRELOAD counts: scripts/util_rtx_count_syms.sh  # THE perf instrument; wall time disqualified
# conversion instruments: SCRIP_ZD_DIAG=1 (authoritative arm/refuse attribution) · SCRIP_ZSM=1 (per-port ring) · the s196 autobug harness · SCRIP_SEAL_DIAG
```
Twin-arm boards ×4 = {default, `SCRIP_PL_CELLS=1`} × {`SCRIP_PL_ZFRAME=0/1`} locate a regression's arm UNTIL PZ-6 deletes the arms. Falsify every zero by injection before believing it.

## ⛔ LAWS THAT BIND EVERY RUNG — INHERITED FROM THE SN4 CONVERSION (ruling SHAs; full list in the FINDING)
Across-the-board admission, never construct-at-a-time (`158738e8`) · failure never whacks — unwind = own-K frees; whack = success/commit brackets only (`c0c77585`); GLUE NEVER WHACKS · one statement at a time, source order (`396d62c7`) · NO per-op filter (`44b8b82c`); no declaration tables — semantics live in THE LATTICE (`18d5bfe1`) · a choice record is read at the depth it was written: depth-immune home or record-relative, never live-rsp (s189) · resume surface = RIGHTMOST box (s190; the `g->all[before]` lowering trap) · tier admission is semantic: β-extends / leftward self-undo / re-yields-same-cursor (s124/s180/HQ-73 `28e2122a`) · derive-don't-accumulate (γ re-enters) · COMMIT-THEN-WHACK · ONE AUTHORITY: offsets only through `x86_frame_off`/`zone_ref`, K only `zd_k`, releases only at the planner choke (s22k spelled-twice disease) · FOUR GREEK PORTS ONLY (`fd16fbea`); γ→β cross-box = 0 (s133, measured over 16,082 edges) · m3 proves NOTHING about m4 — every compiler-process fact needs an emitted-image twin (the seal law, s193/s194) · ⛔ **SEED-FIRST IS RETIRED — LON, 2026-08-24 s273 (see the OVERRIDE banner at the top of this file). Do not gate Prolog work on it.** ~~seed-first: prove a new machine shape as hand-written `.s` against the oracle BEFORE compiler changes~~ (`d5c7a72b`/`1e881ce5`) · ASM-DIFF-FIRST (RULES.md s132) · TEMPLATE-ONLY · BOTH-MEDIUM · TRIPLE-ARM CANARY (every emitter/templates/x86_asm.h commit runs the Icon canary AND this file's smoke+honest BEFORE commit) · killswitch byte-identity + compile-time md5 blast radius; A/B swaps `out/libscrip_rt.so`, not the driver.

## ⛔ STANDING CONDITION — SN4 + ICON GOALS RUN CONCURRENTLY, SAME REPOS
Full text GOAL-ICON-100 §STANDING CONDITION, binding verbatim: separate clones default · `git fetch` before trusting a watermark; pin `git rev-parse HEAD` + clean status to every number · sibling deltas are not Prolog signals until the sibling cursor is checked · shared-emitter commits can move THIS watermark silently (what the TRIPLE-ARM CANARY catches) · verify HEAD + 0-tracked-modified AT MEASUREMENT.

---

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 hq_C (TRIO, s286) — **RUNG 0 LANDED: THE CUT (26 → 0 exported Prolog-only globals, −5,050 lines) + HELLO WORLD AS ONE CALL BOX WITH THE § A.1 HEADER; NEXT IS RUNG 1**

**Landed and pushed, one push from origin:** SCRIP `db299d41` · corpus `9c648987` (rung-0 trace refs re-cut with hq_B's other blocks kept; 23 `.s.REFUSED` markers replace the pre-cut Prolog bench assembly) · receipt `FINDING-2026-09-02-hq_C-rung-0-lands-the-cut-deletes-every-prolog-only-global-26-exports-to-0-and-hello-world-runs-as-one-call-box-with-the-header-at-the-top.md` · § A.1's table now carries the measured H-relative header (`H = rbp + kt - 64`: TRMARK CUR RES B0 pad γ ω caller-rbp; the root graph seeds `r13=0 r15=0 r14=H`). Verdicts (pristine `-O0`): ladder `--to 0` PASS 2/2 · trace gate `--to 0` PASS(0)/2 · `nm -D` 0 · SNOBOL4 + Icon control-arm `.s` byte-identical · `make test` green · master board REPORTED 6/383 both modes (pre-cut 348/371) — every construct above rung 0 REFUSES at compile naming its rung.

**Whoever resumes starts HERE:** rung 1 (`ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E: fact with arguments + rule body, `p(a,b). main :- p(X,_), write(X), nl.`; the witness is `ladder__rung01_*` in the master, refs and traces already cut by hq_B) — head unification binds `F.A[i]` on the spine through `IR_CALL "$unify"` → `rt_pl_dop_unify` (`rtx_plunify.s`), the log rule of § B.11 via `r13` inside `plw_bind`; `pl_refuse` names rung 1 for `=` and head arguments today. Gates per push: `test_prolog_ladder.sh --to 1`, `test_gate_pl_port_trace.sh --to 1 --cut` then `--to 1`, `nm -D` 0, SNOBOL4 FAIL=0 + Icon watermark, `strip_comments.py --check`, `make test`. ⛔ Not for this seat: rungs 6–8 (hq_P), 13 (hq_B), Icon, the SNOBOL4 `SCRIP_OPT=0` crash of `5839cf13` (ceo).

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (TRIO, second account, Lon 16:55–17:05 CDT box clock: *"Let's get Prolog to 100%."* · *"Just start climbing the rungs from simple syntax to more complex syntax as you go along."* · *"Get back on Prolog now that we are cooking with it."*) — THE CEO CLIMBS RUNG 6 WHILE hq_C CLIMBS 2–5

**State at this line:** rung 0 `db299d41` and rung 1 `7d0ed0f8` on origin, rung 1 AUDITED GREEN by the ceo on a pristine build (DONE-WHEN rc=0, ladder `--to 1` 4/4, trace gate 0 failed, quad gate 0 unenrolled, `make test` rc=0, 17:00–17:09). REFUSAL CENSUS (ceo, `7d0ed0f8` · corpus `607bcf92a`, every master entry `--compile`d, FIRST refusal per entry, 383): 51 compile and pass m3; 332 refuse — rung 2: 78 · 3: 9 · 4: 3 · 5: 44 · **6: 132** (`is/2` alone 42, halt 8, format 8, term_string 6, writeq 5, numbervars 5) · 7: 2 · 8: 4 · 9: 35 · 10: 25. A first refusal is a lower bound: an entry naming rung 6 may also need 2–5; the census is re-run after every landing and the delta is the receipt's number. **Assignment (ceo ruling on Lon's order):** hq_C rung 2 (ASSIGNED, design settled, no `src/` edit at 17:10) then 3, 4, 5 in order WITHOUT waiting for a ceo audit between rungs (audits run on the pushed tree; a red sample reopens by telegram); **ceo rung 6** (`prolog-rung-6-det-builtin-leaves-is-compare-type-tests-write-family-conversions`, rank 0, ASSIGNED ceo; § E row 6 owner moved hq_P → ceo) in the order inside the rung: (1) `is/2` + arithmetic comparisons → (2) `==`/`@<` family, `compare/3`, type tests → (3) write family + `format/1,2` → (4) atom/string/number conversions → (5) `functor arg =.. copy_term numbervars succ plus sort msort` → (6) `read get_char peek_char`; each a compile-time-wired det leaf through the shared `bb_call` box (`dop_direct_fp` table), binding leaves through one rtx trampoline each (`rt_pl_dop_<name>` → `_c` with the trail ctx on `r12`/`r13`), enrolled by NAME in the quad scanner; the surviving `rt_pl_*_cell` bodies of `unification.c` bind through `plc_unify_into_cell_cx`/`plc_unify_cells_cx`/`plc_bind_cx` (ctx NULL = the old untrailed path for the rung-7/8 machinery still to be re-derived); hq_P: `--only N` landed `b8cbe837`, rungs 7–8 the moment rung 2 is on origin, reviews rung 6 on the rail; hq_B: the six rung-6 witnesses landed corpus `6936651c` (its group (d) carries `sub_atom/5`, a rung-7 generator by § E — routed back to hq_B), rungs 7–13 witnesses next, then B-5/B-4. **RUNG 6 LANDED (ceo, ~18:15 CDT): SCRIP `943c1b8c` (step 1) + `81b40ceb` (the rung), corpus `bbf299d4` (the six witnesses leave XFAIL, proven by the board in the same commit), receipt `FINDING-2026-09-02-ceo-rung-6-lands-every-deterministic-builtin-as-a-direct-leaf-binding-through-one-rtx-trampoline-each-the-master-board-moves-50-to-169-of-400-both-modes.md`.** Ladder `--only 6` PASS 12/12; fresh `--compile` of the six witnesses rc=0 with 0 `rt_call_arr_bl`, 0 `_rkfn`, 0 Prolog-only globals; REPORTED master board **169/400 both modes** (from 50/383); census first refusals now rung 2: 83 · 3: 9 · 4: 3 · 5: 52 · 6: 1 · 7: 2 · 8: 5 · 9: 43 · 10: 30 (172 of 400 compile, 171 pass m3). The row's DONE-WHEN (`--to 6`) closes when hq_C's 2–5 land; the rung-6 `ALL.trace` blocks wait on a `--only N` arm for the trace gate (hq_P). **Rulings for hq_B, routed here:** (1) the master's oracle is swipl 9.0.4 with default flags — `**` and `^` on integers yield an integer, `/` on integers yields an integer when exact; ISO/gprolog `1024.0` is not the target of the master (GNU Prolog remains a rival on the benchmark grid, not the ref oracle); (2) a ref the oracle recipe cannot produce is not a ref: `simple_program_97` (no initialization directive, 4-line ref) is re-cut under the recipe (empty output, rc 0) by hq_B, and if the origin family's intent was to run `main`, adding the directive is a corpus change hq_B may make where the family's siblings carry it — never a second run mode invented for one entry. **Corpus pin datum for hq_B:** the three `*power*` master entries pin `2 ** 10 = 1024.0` (gprolog-era) while swipl 9.0.4 prints `1024`; no ref edited by the ceo.
**RULINGS 2026-09-02 18:50 CDT (ceo, on hq_B's wrap correction; the record is CEO-151):** (a) ruling (2)'s population is 59 directive-less entries with non-empty refs, not 1; the 55 that reproduce their ref byte-exactly under `-g main` TAKE `:- initialization(main).` in one board-proven commit (hq_B); the recipe `-q -g halt` STANDS — `-g main` would double-run the 203 directive carriers. (b) `simple_program_97` is the third `**` float pin → re-pin like corpus `62dc7995`. (c) `simple_program_103` → re-cut under the recipe (swipl 9.0.4 IS the oracle; gprolog-permissive `atom_to_term/3` is not a target). `functor_2` and `copy_term_ite_list_replace_1` print fresh variable names, implementation-defined by construction → THE ENTRY is rewritten to print a stable fact (`numbervars/3` before printing, or `var/1`) and re-cut; XFAIL only if the rewrite would change what the entry tests, named in the commit. (d) RUNG 2 AUDITED GREEN by the ceo on pristine `81b40ceb` (18:35–18:46, DONE-WHEN rc=0). (e) THE CEO'S CLIMB is the rung-6 TAIL — the stream-arity leaf family over the driver `fh_` registry, `read_term/2` with `[]`, `keysort/2`, declaration directives as no-ops; plain directive goals are RE-LABELLED rung 5 (`ignore/1` at the root, file order before initialization goals, swipl's `Goal (directive) failed` warning — hq_C); `nb_setval/b_setval/flags` are ζ-STANDING slots keyed at compile time → rung 10 with the DB. Scope and witnesses at `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E rows 5/6/10. **(f) THE TAIL LANDED 19:45:** SCRIP `5d12c898` · corpus `16fb9da07` (on hq_C's rung 3 `9af884bd`), `--only 6` 20/20 over ten witnesses, trace gate `--only 6` PASS(0)/20, census 198 pass m3 of 400 (from 194), board REPORTED 194/404 both modes; receipt `FINDING-2026-09-02-ceo-rung-6-tail-lands-*.md`. Rung 3 is on origin and is the ceo's next audit. **(g) 19:55, on hq_C's rung-3 question:** the `ninit==0` run-`main/0` fallback in `lower_pl_stage2` is DELETED — SCRIP runs exactly what the master's recipe runs (`swipl -q -g halt`: directives in order, then initialization goals), nothing synthesized; the deletion lands as one board-proven pair with hq_B's 55 directive additions and the four stale `want_rc` pins (rows `prolog-directive-less-entries-run-main-but-the-oracle-does-not`, `prolog-master-seven-stale-xfail-markers-xpass-on-the-rung-3-tree`, hq_B). Rung 3 landed (hq_C `9af884bd`), F.HI restored at `[H+32]` as the log threshold word (§ A.1/B.5, hq_C's ruling, accepted).


## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (TRIO, LON ~13:40–13:55, box clock) — THE REBUILD FROM RUNG 0: THE CUT + HELLO WORLD WITH ZERO PROLOG-ONLY GLOBALS (hq_C), THE CONSTRUCT LADDER IS `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E, SHARED BOXES FIRST (§ B.0)

**Landed and pushed:** the zero-risk prep rung `prolog-dead-globals-and-bb-table-out-of-runtime-image` — SCRIP `d42d2918` (nine dead globals, the `g_zeta_mode` cmp out of the `$trail_mark` sink, `g_resolve_bb_table` 268 KB out of the runtime image), corpus `a0cca818` (prolog-bench regen), row DONE on the computed verdict; SCRIP `2748100d` (optbypass watermark 191→192 bisected to `5839cf13`, the gate prints its regressing list + wall clock). The translation page is REVIEWED and ceo has RULED on it (`.github` `59b37a74`, `e9b5d894`; review FINDING `FINDING-2026-09-02-hq_C-translation-page-review-the-base-header-lands-on-the-lexprep2-region-and-r12-is-the-live-cas-cursor-inside-every-snobol4-match-blob.md`): header at the frame TOP, `F.B`/r13 = the header address, r12 TR / r13 B / r14 ROOT / r15 BALL, r10/r11 untouchable, the trail in Prolog's own `rt_slab_region` (never the carve-only C.A.S. island), every retained-frame β tests r15 first (C9).

**RUNG 0 LANDED — SCRIP `db299d41` · corpus `9c648987` · .github `1f7e094f` (hq_C, 2026-09-02 ~15:55): `nm -D` 26 → 0, `plc_` 21 → 0, ladder `--to 0` PASS 2/2, REPORTED master 6/383 both modes, SNOBOL4 1679/1679, Icon `.s` byte-identical. NOW: **hq_C on `prolog-rung-1-fact-with-arguments-rule-body-conjunction-and-the-trail-log-rule`** (rank 0, ASSIGNED; the trail on `r12`, `F.TRMARK`, the log rule through `r13`, `bb_conjunction` shared with Icon; ceo rulings on the rung-0 judgement calls are in its baton and § B.0: the graph key is `zframe_pinned_base` + `root_graph`). The rung-0 order below stands as history. **Whoever resumes starts HERE, in this order (Lon's five rulings routed at § E; law `RULES.md` § THE PROLOG REBUILD GATE):** (1) **hq_C: `prolog-rung-0-the-cut-and-hello-world-with-zero-globals`** (rank 0, ASSIGNED hq_C) — step (a) THE CUT in one push: every § C global and the control machine that reads it deleted (list in § E; boundary: parser, DESCR cells, atom table, `rtx_plunify.s`, `dop_*`, shared boxes, BX-0 hook stay); step (b) hello world as ONE `bb_call_proc_staged` box with the § A.1 frame (header at the TOP, `F.B`=`r13`=H) and two `bb_call` leaves wired to `dop_write`/`dop_nl` at compile time, `IR_CALL_PROLOG` collapsed into `IR_CALL_PROC_STAGED`; step (c) the ladder runner `test_prolog_ladder.sh` if hq_B's has not landed first (both modes, REFUSE rc=2, denominator printed, failed-once-passed-once). DONE-WHEN in the baton. Then rungs 1–5 in § E order, one push each, from origin. (2) **hq_P:** the quad gate (no `r12`–`r15` write reachable from a Prolog graph outside the TR/B/ROOT/BALL helpers, emitted `.s` + `rtx_*.s`), then rungs 6–8 once rung 2 is on origin; the van Roy pin waits for rung 8. Also hq_P's after the cut: row `rtcc-veneer-drops-the-r9-gva-reload-when-the-gva-table-is-empty` (minted by hq_B, BLOCKED-ON rung 0; ceo ruling 2026-09-02 ~14:35) — hq_B's language-blind patch, saved outside the root, drops the `r9` GVA reload from the RTCC veneer whenever the compile's GVA table is empty (`gva_count()==0`; per-graph is unsafe because `rt_gen_spine_*` are mask-0 leaf calls), measured on `f4532dea`: nreverse `rtccb+48` reloads 168 → 0, SNOBOL4 1679/1679 both modes, Prolog smoke 5/5/5, Icon 14/14, programs with globals keep every reload; it is RE-PROVEN on the post-cut tree before landing, never merged from the pre-cut measurement. (3) **hq_B:** `test_prolog_ladder.sh` + the swipl `.ref`/`.trace` for the rung 0–5 witnesses into `ALL.pl`/`ALL.trace` (instrument lane, never `src/`), then rung 13. **Re-scoped rows:** `prolog-rung-2-0-rehome-scratch-r9-and-r12-off-prolog-reachable-templates` SUPERSEDED (the sink sites die in the cut; hq_B's two brief corrections kept at § E.2); `prolog-pz4-gamma-retain-activation-frames` and `prolog-pz5-in-frame-resume-delete-pending-mailbox` SUPERSEDED, folded into rungs 0/2; every row PARKED-AWAITING PZ-4 is closed by the rung receipt that cures it (the ladder names the row in its DONE-WHEN column), never auto-unparked. **While the cut is in flight (ceo ruling ~15:35):** hq_P and hq_B land nothing under `src/` until rung 0 is on origin — measure, park BLOCKED-ON rung 0, land scripts/corpus refs/.github; a proven `src/` patch becomes a row with the patch beside the baton, landed and re-proven from the post-cut origin (rows so parked: `rtcc-veneer-drops-the-r9-gva-reload-when-the-gva-table-is-empty`, `port-exit-value-contract-untagged-rax-forges-dt-fail` — hq_B measured 223 raw sites in 31 programs, 140 of them Prolog continuation `lea`s the cut deletes, FINDING `.github` `bb7caece`; `pascal-quick-m4-wrong-checksum-crash-masked` FREE for hq_C after rung 0). **The gate while climbing:** the Prolog master board (348/371 both modes on `f4532dea`) is REPORTED with every receipt and is not a landing gate until rung 10; SNOBOL4 FAIL=0 over the printed denominator + Icon watermark + `nm -D` names none of § C + `strip_comments.py --check` 0 are hard on every push. ⛔ Not for any seat: the `SCRIP_OPT=0` crash of `eval_convert_branch_1` (SNOBOL4, no cure owed).

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (DUO: ceo + HQ-COMPLETE/hq_C) — THE REDESIGN HAS ITS TRANSLATION PAGE: `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` (draft, HQ review owed); rung 1 = BX-0 the port-trace instrument, rung 2 = PZ-4 (b)-(f) the frame header + B wire (its § E); the zero-risk prep rung (nine dead globals of the census § C, the `g_zeta_mode` cmp in `$trail_mark`, `g_resolve_bb_table` out of the runtime image) is hq_C's first landing. Denominator of the whole redesign: `FINDING-2026-09-02-ceo-prolog-only-globals-census-56-live-9-dead-and-what-the-term-deletion-removed.md`.

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 hq_P (TRIO, second sitting) — **THE VAN ROY THREE-ANGLE INSTRUMENT FABRICATED ITS SCRIP COLUMN AND SKIPPED THE RIVALS ON THE SELF-TIMED KERNELS — BOTH CURED ON `scripts/`; THE BASELINE PIN STAYS PARKED (ceo: until rung 8) AND NO NUMBER IS QUOTED**

**SCRIP `4253dd88` + `48b09e04` · corpus `d0351a546` · pristine `-O0`.** Receipt: `FINDING-2026-09-02-hq_P-vanroy-loop-runs-its-body-exactly-twice-for-every-n-both-angles-divided-n-by-it-and-the-rivals-never-had-their-preludes.md` (BX-0's own receipt, one sitting earlier: `FINDING-2026-09-02-hq_P-bx0-port-trace-lands-r15-is-a-driver-mmap-address-at-every-omega-and-the-baseline-pin-is-parked.md`).
- ⛔ **The loop wrapper `l__(N) :- between(1,N,_), bench__main, fail.` runs `bench__main` EXACTLY TWICE for every N on m3 and m4** (N=1, 3, 64 all print two answers, each with its own fresh work bracket; `nrev` prints none) and exits 0 — a β-path datum for the rung ladder, not a harness quirk. Angle 1/2 gated on `exit=` alone and divided N=65536 by two iterations' CPU: `fib` m3 3,102,442 iter/s beside gprolog 485. ✅ `scripts/lib_prolog_bench.sh:loop_check` grades every engine's loop stdout against N × `.expected`; a cell that did not loop reads `LOOP-OUTPUT-MISMATCH(lines=seen/wanted)` and never a rate. Angle 1/2 also now consult `prelude_gplc.pl`/`prelude_swipl.pl` (they never had; the 10 self-timed kernels read `correctness-fail` on the rivals for `wall_us/1`). Both REFUSE rc=2 on a missing lib or prelude.
- ⛔ **hq_C:** `test_gate_vanroy_prolog_acceptance.sh`'s CLEAN (rc 0, no refusal text) admits `fib` at 2 of 64 answers and `nrev` at 0 of 65536; its `CLEAN_FLOOR=3` counts programs that do not run. Routed (`vanroy-clean-counts-two-of-n`).
-  ✅ **THE QUAD GATE IS MINTED — `scripts/test_gate_pl_quad_regs.sh` (ceo rung-0 brief, instrument lane):** no `r12`–`r15` write reachable from a Prolog graph outside the TR/B/ROOT/BALL helpers, ARM 1 over every Prolog witness compiled fresh (35 today: probe_plz 9 + bench 23 + demos 3 + ladder 0), ARM 2 over the rtx functions reachable from them (transitive, balanced push/pop = callee-saved-clean, unreached = INFO). **Born red on `f4532dea`: 5303 writes / 5303 violations** (4606 `lea r12,[rip+g_pl_*]` sink scratch · 417 `mov r12` · 245 `pop r12` dcα continuations · 35 `xor r14d` m4 prologues; `r13`/`r15` zero), rtx 47 functions / 2 reachable / 0 writes; `QUAD_HELPER_RX='.'` → PASS over the same 5303 (can say YES); `--self-test` proves NO and YES on both arms on fixtures. `QUAD_HELPER_RX` is the one allow-list — hq_C names the helpers there. ⭐ **s284 CORRECTION (hq_P, SCRIP `9623ff55`): that label-keyed design is SUPERSEDED — at rung 0 it is RED on the five § A.1 ROOT seeds and can only go green by naming `main`/`main_α`, which admits ANY write under those labels. The gate now grades emitted writes by (site, register, shape) against an explicit table and is GREEN on rung 0 (5/5 enrolled) and in `make test`; `QUAD_HELPER_RX` survives as the rtx ROUTINE-name allow-list (the TR helpers by name). Enrol a shape row, never a label — `FINDING-2026-09-02-hq_P-quad-gate-lands-green-at-rung-0-by-site-and-shape-superseding-the-born-red-label-allow-list.md`.** ⛔ Third harness defect, same sitting: swipl runs through `timeout`'s SIGTERM (648 s on `vanroy/queens.pl` under `timeout 60`); every angle-harness `timeout` now carries `-k 5`. **Triangulation on the cured instrument:** run 1 exposed Defect D (angle-2 drivers carried pre-bracket bodies: 8 kernels DISAGREE 1.2x–2.75x), run 2 on refreshed drivers (corpus `d0351a546`, TSV `triangulation-20260902T190902Z.tsv`) reads cal/derive/divide10/ops8 AGREE on both rivals (bucket MEASURED=4), four more AGREE on one rival, log10/tak DISAGREE on both at 0.83–0.90x (calibration, declared with a discharge condition), SCRIP cells UNPROVEN everywhere by the loop check; Defect E, the coverage gate counted 23 not 21 and had been red since 09-01 — cured SCRIP `48b09e04`, now `total=21 covered=21 rc=0`; `--two-number` bucket plumbing `MEASURED=4 DECLARED=5 REFUSE=12` rc=0, multiples unquoted. **Acceptance gate, pin tree, worst-of-3:** `CLEAN=3 (floor 3) · REFUSE=3 · CRASH=13 · other=2`, 11 of 21 flip, `GATE FAIL(1)`; the three CLEAN (`fib nrev queens_8`) include the 2-of-64 and 0-of-65536 programs.
- **r9 datum on the pin tree** (`./scrip --compile --target=x86 bench/<k>.pl | grep -c 'rtccb+48'`, SCRIP `f4532dea`, `-O0`): `nrev` **225**, `nreverse` **168**. Rung 2.0's before-number is this line, not ceo's 244 (taken on `2748100d`).
- **Single-shot m3 at `f4532dea`:** 10/21 answer correctly (`cal derive deriv divide10 fib log10 ops8 sendmore tak times10` — exactly the self-timed ten); `crypt ham meta_qsort mu nreverse nrev qsort zebra` dump core; `queens_8 queens query` fail `main/0`. gnu and swi 21/21 with their preludes.
- **NEXT for hq_P:** ceo's rung-0 brief (Lon 13:40–13:50: Prolog restarts at hello world with zero Prolog-only globals, climbing by construct; BX-0 is the ladder's grading arm) — (1) quad-clean gate over r12–r15 writes in emitted `.s` + rtx asm; (2) rungs 6–8 (`pl_leaf`, `pl_gen`, `pl_drive`) once hq_C's rung 0–2 frame is on origin; (3) the two-number pin AFTER rung 8, taken with this instrument, three runs for the spread. ⛔ `src/` is held until ceo's "rung-0-landed". The C9 witness and the optbypass durations from the previous cursor are unchanged and still parked.

## ⭐⭐ LIVE CURSOR RE-POINT (hq_C 2026-09-01, on seat15's report via ceo): THE WALKABLE RUNG IS LADDER C, NOT PZ-4
The cursor below points at PZ-4 (`prolog-pz4-gamma-retain-activation-frames`), which is **BLOCKED-ON `calling-convention-depth-tracked` (hq_P)** — a seat following it lands on a blocked row. Under FLEET-16 (Lon: Prolog #1) the walkable work is **`.github/MASTER-PLAN.md` ladder C**: ⭐ **C1** `prolog-directive-only-file-fatals-no-main-bb-graph` (rank 0, the 139-entry driver class), then C4–C20 (every FREE Prolog correctness row), with C2/C3/C27/C28/C29/I4/I5 minted and refusing until their blockers land. Run `python3 SCRIP/scripts/util_ladder_walk.py` for the live state; `prolog-next` (C20) means "the first incomplete C rung", never PZ-4 while it is blocked. The PZ-4 cursor remains correct as the *keystone* record and stays below; it is not where a seat starts.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-01 seat15 (FLEET-16, Prolog #1) — **BOTH PROLOG MODE INSTRUMENTS WERE BLIND. CURED, NOW HONESTLY RED. `prolog-next` HAS A COMPUTABLE DONE-WHEN AT LAST.**

**SCRIP `f85e1fdc`** (pristine `-O0`) · corpus `04177c4b3` · row `prolog-next` · full receipts `.github/FINDING-2026-09-01-seat15-prolog-both-mode-instruments-were-blind.md`.

**Found, verified on a pristine tree, not read:** Prolog — the fleet's #1 correctness priority — had two automated m3/m4 instruments and **neither compared anything**, in opposite ways that hid each other.

| Instrument | Before | Exit | After cure |
|---|---|---|---|
| `test_gate_pl_m34_parity.sh` | examined **0** programs | 2 UNPROVEN | **PASS=5 FAIL=7** |
| `test_crosscheck_prolog.sh` | `PASS=13 FAIL=0 SKIP=25 ORACLE_MISS=0` | **0 (green)** | **m3 3/15 · m4 3/15**, exit 1 |

- **Parity gate:** floored on `*.sno` inside `corpus/tests/prolog` (which holds **0** `.sno`, **45** `.pl`) — a SNOBOL4 copy-paste — so `gate_floor` exited UNPROVEN(2) before the first comparison. One-word cure surfaced **7 real divergences**: `findall`×2 (m4 SIGSEGV `rc=139`), `retract`×2 / `abolish`×2 / `setof`×1 (m4 `rc=1` where m3 answers correctly).
- **Crosscheck:** compared `--run` against `--run` (**mode 4 never invoked** — the variable name `ir` is the fossil of deleted mode 2), read `.ref` where the corpus stores `.expected` (**0** vs **26** on disk, so the oracle was never consulted), and pre-skipped crashing programs (**25 of 38**) — hiding exactly the PZ-4 crash class. Cured to per-mode independent oracle grading per § MODES MAY DIVERGE.

⭐ **Verified both directions before trusting the new instrument:** a reported FAIL (`rung66_current_stream`) is a genuine **silent wrong answer at rc=0** (`in_enum`→`not_in_enum`); a reported PASS hand-diffs identical. A third "disagreement" was my own ad-hoc `diff` mishandling `.expected`'s missing trailing newline — the instrument was right, I was wrong, recorded in the FINDING because that reflex is how a correct gate gets reverted.

**`prolog-next`'s DONE-WHEN is now a command that can exit non-zero** — `test_crosscheck_prolog.sh && test_gate_pl_m34_parity.sh` — discharging the standing instruction hq_C left in that row on 2026-08-22. It is RED today, for a measured reason rather than because it was prose.

⛔ **This row's own cursor chain is blocked, which is why this pass went to the instruments:** the PZ-4 cursor below names `prolog-pz4-gamma-retain-activation-frames`, which is `hq_C`-owned and `BLOCKED-ON:calling-convention-depth-tracked` (`ASSIGNED:hq_P`, rank 0, open). A seat following this file's cursor literally lands on a row it must not work — flagged to HQ, not resolved here.

**NEXT:** root-cause the dynamic-database m4 cluster (retract/abolish/setof — one mechanism, five witnesses; minimal witness pair already minted: `rung15_abolish_abolish_existing` passes both modes, `rung15_abolish_abolish_one_of_two` fails m4). Then the `findall` SIGSEGV pair. Then close the **`NO-ORACLE=23`** gap — two thirds of this corpus has no `.expected`, and no instrument can grade what has no oracle.

---

## ✅⭐⭐ LIVE CURSOR — 2026-08-27 seat05 (FLEET-8) — PZ-4 row: omega-wire caller/callee mismatch found+fixed, row still open (new SIGILL witness)

**SCRIP `0c800c86`** (on `d4e6e971`+hq_C's `0e8cf4a4`) · corpus `49b4779f0`. Row `prolog-pz4-gamma-retain-activation-frames`, task file at `/home/resources/postoffice/tasks/`, full receipts `.github/FINDING-2026-08-27-seat05-pz4-omega-wire-never-loaded-bcps-spine-gen-arm.md`.

**Found (gdb-confirmed, not just read):** `bcps_spine_gen_arm`'s wire-cross to every zframe-graph (generator) callee never loaded the omega continuation into `rdx` — `xa_flat_zframe_prologue_str` captures {γ,ω} unconditionally from rcx/rdx, but the default `SCRIP_ICN_WIRE_STACK=1` path (`bb_glue_pass_wires_blob`) only pushes them onto the stack, leaving rdx as whatever the last intervening call happened to clobber it with. γ survived by an accidental coincidence in which register the push sequence's `lea` scratch lands in; ω did not. This predates and is independent of the `SCRIP_PL_GAMMA_RETAIN` question this row was minted around — both retain=0 and retain=1 hit the same garbage-omega crash in hq_C's own matrix.

**Fixed:** `bcps_wire_cross_gen` (bb_call_proc_staged.cpp) — adds the one missing `lea rdx,<omega>` before the jmp, scoped to the two generator-call sites only.

**Measured name-for-name:** SNOBOL4 365/365 unchanged · Icon `246/16/1/30 · 246/16/1/30 · 244/18/1/30` identical to pre-change baseline · rung13 `0/5`/rung14 `2/5`/smoke m4 `4/5` unchanged (different mechanisms). ⛔ **CORRECTION, same pass:** rung15 `1/5→2/5` is true but incomplete — controlled A/B (5 runs each side) shows pre-fix perfectly deterministic (`1/5` every run) but post-fix genuinely flaky (`1–3/5` across runs). The fix is real; it converts a stable wrong-jump crash into an intermittent one on 2 of the 5 witnesses, exposing rather than closing a second pre-existing defect. Full data: the FINDING's own ADDENDUM.

**Row NOT closed.** Same repro now fails one layer deeper: fact's own omega resolves correctly now, but crashes shortly after (SIGILL, a stack address used as a jump target, inside/after the `rt_proc_call_epilogue_ω` call chain) — new mechanism, not yet root-caused. See the task file's `## NEXT` for the exact next step. This is this row's THIRD real, verified, non-regressing "worked, not done" pass in one day (hq_C's stack-depth fix, then this) — the wall keeps moving, has not yet fallen.

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-21 HANDOFF (seat2 `/home/claude2`, Claude Fable 5 → Opus 5; Lon in-chat: prune → REPLAN to the SN4 machine → deep scan + work list) — **THE CHARTER, THE CENSUS, THE SEED AND THE WORK LIST ARE ALL CUT. NO COMPILER CODE WAS CONVERTED YET — PZ-1(b) IS THE FIRST CODE RUNG.**
**HANDOFF SUMMARY.** This session replaced the whole pre-conversion ladder with the SN4 THREE-ZETAS conversion (Lon in-chat: *"a new world of code … EXACTLY like SNOBOL4"*), then did the discovery to back it: a 1,603-commit history scan of the SN4 campaign (16 campaigns, its dependency order, Lon's ruling SHAs), a full per-template scrutiny of every Prolog-reachable file, a live-measured admission census, an oracle-green hand-written seed, and the per-file work list. **⛔ ZERO codegen files were touched** (`src/emitter`, `src/templates`, `src/lower`, `src/runtime` all untouched) — so RULES step-4 `.s` artifact regen does NOT apply to this handoff, by construction rather than by omission. Deliverables: `WORKLIST-PROLOG-ZETA.md` (32-row inventory + steps + deletion ledger) · `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md` (the arc + ten gaps + twelve laws) · `FINDING-2026-08-21-seat2-PZ-0-...-86-percent.md` (the census) · `corpus/probe/plz/test_pl_zeta_1.s` (`2ef8bb3e`, oracle-green) · `SCRIP/scripts/util_pl_zd_arm_census.sh` (`cc2675cd`) · `corpus/probe/plz/` 9 witnesses + gprolog `.ref` oracles.
**⛔ FOR THE NEXT SEAT — READ IN THIS ORDER:** this cursor → `WORKLIST-PROLOG-ZETA.md` §0 (the four undocumented facts) → §1 (inventory) → §2 (steps). Re-grep every anchor: the 2026-08-20 style-200col pass moved lines tree-wide and three anchors in older Prolog docs were already stale when the work list was cut.
**PZ-0 LANDED: PROLOG HAS *ZERO* ζ SPINE AT THE SHIPPED DEFAULT, AND ONE DELIBERATE TAUTOLOGY GATES 86% OF ITS RUNS.**
Measured at SCRIP `d2ade229` (`make pristine` rc=0, `-O0`), corpus `f34388fd`, every board run live this session. ⛔ Numbers are SHA-PINNED to that measurement point; at handoff HEAD had moved to `cfff4530` (sibling Icon/HQ landings) and the cheap tripwire was RE-RUN there after rebuild: **smoke still 3/5, unchanged** — the non-smoke boards were not re-derived at `cfff4530` and PZ-1 re-derives them. **WATERMARK / THE CONVERSION'S FLOOR: smoke 3/5 · honest PASS=107 FAIL=0 ABORT=0 ORACLE_CRASH=78 · rung interp 110/164 compile 109/164.** ⚠ interp REGRESSED 113→110 since s166 (28 sessions of sibling shared-emitter landings); compile unchanged. Recorded, not chased (PZ-0 is a census) — bisect candidate against sibling landings.
**⭐⭐⭐ THE CENSUS (new instrument `scripts/util_pl_zd_arm_census.sh`, 214 programs, 736 ZD runs):** shipped default **ARMED=0 / REFUSED=736, every one at its FIRST node** — the eight PL-ZK admission arms are all gated on `pl_cells_graph`, which only opt-in `SCRIP_PL_CELLS=1` ever sets, so **the shipped compiler has no ζ for Prolog at all** and the "twin-arm" framing in every Prolog doc is misleading. Cells arm: ARMED=100, and **635 of the 636 refusals are ONE KIND — `IR_VAR_REF`** (494 of them at i=1, the node right after the head).
**⭐⭐⭐ THE GATE IS A TAUTOLOGY, DELIBERATE AND DOCUMENTED:** `emit.cpp:2064` returns `… pl_cells_graph && !pl_cells_graph …` = never 1, put there by `069c2fd8` (PL-ZK-5B s13) whose own message says *"gated off via tautology pending correct TERM_VAR materialization"*, because **Prolog variables are heap `Term*` cells needing unification+trailing** — a frame-slot copy gives NULVCL for fresh vars. Three consequences: (a) **`SCRIP_ZD_PL_VR` IS A VACUOUS KILLSWITCH** — A/B it and you measure nothing (s68 class); (b) its cost was never measured until now: **86% of all runs**; (c) ⭐ **VAR_REF ADMISSION AND γ-RETAIN ARE ONE PROBLEM** — a logic variable cannot live in a spine cell that γ RELEASES, which is exactly why s13 could find no correct materialization; under the SN4 law (α carves, ω tears down, γ RETAINS) a retained spine cell IS a WAM environment slot, made sound by conditional trailing (gprolog `Word_Needs_Trailing`, SWI `GTrail`).
**⛔ INSTRUMENT TRAP, CAUGHT BEFORE PUBLICATION:** `SCRIP_ZD_DIAG` prints ONLY refusals (armed runs print per-NODE `[ZD] h=..r=..i=..` lines instead), so counting `^\[ZD\] run` and subtracting yields **ARMED=0 for every language, always** — a structural zero that reads like a measurement. Falsified both directions (SNOBOL4 ARMED=2/REFUSED=0; Prolog probe ARMED=0/REFUSED=2); the trap is documented in the script header.
**⭐ FIRST LIVE PROLOG ORACLE OF THE CAMPAIGN:** GNU Prolog 1.6.0 built from `/home/resources/gprolog-master` (scratchpad prefix; `gplc` needs its `bin/` on PATH for `pl2wam`). Nine witnesses minted at `corpus/probe/plz/` with `.ref` oracles, run both modes: only p1 (single clause) passes. **p8 is an m3≢m4 divergence AND a silent wrong answer** — m3 SIGILL, m4 exits 0 printing `yes` where the oracle prints `no` (`t(X), !, X > 2` committed to the wrong clause and reported success); p5 splits the modes 139 vs 132. The cut/barrier class has no correct arm in either mode.
**PZ-1(a) SEED: DONE AND ORACLE-GREEN (this session)** (`corpus/probe/plz/test_pl_zeta_1.s`, SCRIP `2ef8bb3e`) — the THREE-ZETAS Prolog machine hand-written and byte-identical to live gprolog 1.6.0 on two-clause backtrack-with-binding plus cut: retained spine-cell variable · activation-homed 32B record with **E DERIVED from the record address** (record+56, so no EB field is needed) · trail `saved_MH` unwind · γ-RETAIN · cut = CP-chain pop reclaiming nothing (WAM law) · statement failure barrier = the statement's own BASE record, making `fail` uniformly `jmp [r15]` · wires-only linkage. **The shape is proven before any compiler change; Lon may veto it, and the ladder's steps are written against it.**
Full evidence: `FINDING-2026-08-21-seat2-PZ-0-prolog-has-no-zeta-spine-and-one-tautology-gates-86-percent.md`; charter + gap census: `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md`.

## ⭐ LIVE CURSOR — 2026-08-24 seat01, cross-goal beneficiary note (Icon N-1, `GOAL-ICON-100.md` row `icon-n1-wire-stack-crossing`) — CALLER SIDE CROSSED, CALLEE SIDE NOT DONE, ruled ATTRIBUTED PARTIAL PZ-LADDER PROGRESS (hq_C s269)
**Icon N-1 (SCRIP `15738e4a`):** the caller side (`bcps_spine_gen_arm`, one of the 4 `bb_glue_pass_wires` sites in `bb_call_proc_staged.cpp`) crossed for Prolog's generator-style calls — verified: `wordcount.pl --run` byte-identical old vs new, `zebra.pl` core-dumps identically both modes (pre-existing).
The callee side (the `xa_flat.cpp` epilogues) is explicitly NOT done — N-1's scope was `icn_cells_graph`; pure `zframe_graph` (Prolog's own graphs) was excluded. Cite: `FINDING-2026-08-23-seat01-icon-n1-wire-stack-crossing-landed-plus-a-stack-parity-bug.md`.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-21 s248 (seat2 `/home/claude2`, Opus 5; Lon in-chat: *"Get the THREE ZETA's working for SCRIP Prolog"*) — **PZ-1(d) IS LANDED AND PUSHED. THE FIRST COMPILER CODE OF THE CONVERSION IS IN.**
**SCRIP `e92aebfe`** (commit `9c54afbf`) · FINDING: `FINDING-2026-08-21-s248-seat2-the-2065-tautology-is-retired-and-retention-is-not-sufficient.md`.
**LON'S TWO RULINGS THIS SESSION (in-chat, supersede):** (1) **PZ-1 order = (d) BEFORE (b)/(c)** — retire the tautology first so `zdp_tier` supplies the admission signal rather than a hand predicate. (2) **CP = R15 in Prolog graphs, contract AMENDED** (Σ/δ/Δ are scan-only; a Prolog graph does not scan) — recorded in §register contract above.
**⭐ PZ-1(d) LANDED.** `emit.cpp:2065`'s `pl_cells_graph && !pl_cells_graph` is GONE; the guard is now `pl_cells_graph && emit_rec_pin() && emit_pl_gamma_retain()` — it names the real precondition (a logic variable cannot live in a cell γ RELEASES) and turns itself on when PZ-1(c) lands. `SCRIP_ZD_PL_VR` **stops being vacuous**: 240 lines of byte-motion on `plz_p2`. **Census cells arm: ARMED 100 → 534 / 736 · `IR_VAR_REF` refusals 635 → 0** (the kind vanishes). Next refusers = **`IR_VAR` (162) + `IR_CALL_BUILTIN_GEN` (40)** — that is the next admission arm. ONE AUTHORITY: `emit_pl_gamma_retain()` in `emit.h` beside `emit_rec_pin()`; `xa_flat`'s copy now delegates. No new globals (ratchet 14/14).
**⭐ WORKLIST §0(3) PROVEN BY INJECTION:** new `SCRIP_PL_ZANCHOR_POISON` drops the `[kt-8]` store; **356 program-runs** (Prolog rung 164×2 + smoke 5×3 + Icon 14×2) byte-for-byte identical with it present or absent. The slot is **dead on both the Prolog and Icon paths** — PZ-1(b)'s caller-base save has a proven-free home.
**⛔⛔ THE FINDING THAT SHAPES PZ-1(c): RETENTION AS THE EXISTING FLAG IMPLEMENTS IT IS NOT SUFFICIENT.** Measured 3 arms: cells+retain OFF = interp **103**; cells+retain **ON** + VAR_REF admitted = **91**; pin-only guard = **91**. Flipping `SCRIP_PL_GAMMA_RETAIN=1` buys **nothing** — because that flag implements **ONE** of PZ-1(c)'s **five** enabling sites (the γ no-unwind arm in `xa_flat`). ⛔ A seat trusting the flag's NAME would wrongly conclude the γ-retain thesis is falsified; **it has not been tested yet**. The −12 is the expected reading until all five sites land.
**⛔ NOT A REGRESSION:** the cells arm's `compile 2/164` is **PRE-EXISTING**, measured on both sides (before 103/2, after 103/2).
**⭐ A FIFTH DEAD GUARD, NOT IN ANY DOC:** `x86_fb_pinned()` (`x86_asm.h:456`) is a hardcoded `return 0`, ONE definition, gating **six** sites — it kills the ICON half of `:2065` (so VAR_REF was unreachable in **both** languages), all of `:2072`, two of three `:2025` clauses, and three `bb_match_defer` arms. ⛔ `rtx_abi.inc:26` asserts `x86_fb_pinned() == emit_rec_pin()` — **FALSE today**; `zeta_choices.h:124` calls that duality *"already complete"* — **it does not run**. Same disease as the tautology. Recorded, not chased; deserves its own rung before anything is built on it.
**WATERMARK (re-derived live at `cfff4530`; sibling `425c5d06` touched README + one bench script only, ZERO compiler code, so it stands at `e92aebfe`):** smoke **3/5** all modes · rung interp **110/164** compile **109/164** · shipped-default `.s` md5-identical to HEAD · Icon canary **14/14** both modes · gates `emit_no_lang` OK + `pl_no_new_global` 14/14.
**NEXT: PZ-1(c) — land the FIVE-SITE enabling pair** through the shared pin machinery (`emit_rec_pin`/`x86_fb`/heap-fb adopt), never bespoke arms, then **re-run arm B: its DONE-WHEN is simply that the −12 disappears.** Then PZ-1(b) (caller-base save into the proven-free `[kt-8]`), then PZ-1(e). Per `WORKLIST-PROLOG-ZETA.md` §2.

## ⛔⭐⭐⭐ THE WORK LIST — `WORKLIST-PROLOG-ZETA.md` (companion; cut 2026-08-21 from a full per-template scrutiny pass)
This file owns the LADDER and the LAWS; **`WORKLIST-PROLOG-ZETA.md` owns the INVENTORY (32 rows: every Prolog-reachable template/emitter site/runtime symbol with KEEP·CONVERT·DELETE and its owning rung), the per-rung STEPS with DONE-WHENs, the DELETION LEDGER (~21 globals + 2 stacks + 2 dead templates + a /proc parser), and the STANDING HAZARDS.** An implementing seat walks THAT file; this one tells it which rung it is on. ⛔ Re-grep every anchor at session start — the 2026-08-20 style-200col pass moved lines tree-wide.
**Four scrutiny findings that were in no doc (WORKLIST §0):** ⭐⭐⭐ **the default-arm retry channel is SEVERED** — `rt_pl_cp_push` has ZERO call sites while its consumer `rt_pl_cp_pop` is live in the arm every Prolog graph takes, and the producer writes a frame slot the shadowed legacy arm alone reads, so the retry dispatcher pops an always-empty stack and CONCEDES (scope it by delivery census first: MOVE_LABEL-delivered predicates only, SUSPEND delivery has a live push; ⛔ do NOT cure it by wiring the dead push back in — PZ-2 deletes that stack) · `bb_cell_cut.cpp`/`bb_cell_ite.cpp` and `IR_INDIRECT_GOTO` are **DEAD** (zero callers / no emit case), so earlier docs calling them the Prolog cut/ITE relays are wrong · **the ζ-ACTIVATION anchor does not exist yet and its slot is free** — `[kt-8]` is written with own rsp and has ZERO readers · `rt_jmp_frame_lexprep2` is an **empty stub still called from every prologue**, `resolve_cp_*` are declared-never-defined, and the trail's **/proc/self/maps** dead-frame filter exists only because frames die under live trail entries — γ-RETAIN deletes it.

## RUNGS — THE CONVERSION LADDER (walk in order; each rung: own commit, killswitch byte-identity + md5 blast radius, twin-arm boards until PZ-6, TRIPLE-ARM CANARY, boards ratchet, cursor move, push mid-session)

- [x] **PZ-0 OBSERVABLE FIRST — LANDED 2026-08-21 (a,b,c,d all measured).** (d) answered by measurement: **ZSM sees ZERO Prolog boxes** (0 events on two probes vs 11 on the SNOBOL4 control, identical flags) — the bug finder cannot hunt Prolog at all today. Master gate is env-only, so the blindness is in the emission path, not the gate; hypothesis (to be measured at PZ-1, not assumed): no ζ ⇒ no port hooks ⇒ no ZSM, in which case the gap closes as a side effect of PZ-1 and PZ-7(d) shrinks to a census. (a) Re-derive all five boards at `7153ccd7`+ pristine, say the numbers aloud, cursor them — they are the conversion's floor. (b) Mint the probe matrix as committed corpus witnesses with `.ref` oracles (the p1–p6 shapes from the s166 FINDING + the two smoke reds): two-clause first-solution, fail-driven backtrack, recursion, member/2, inline disjunction, cut-commit, cut-under-backtrack. (c) Arm census: `SCRIP_ZD_DIAG=1` + `SCRIP_ZSM=1` sweep of the 164-corpus — per-graph armed/refused counts, which xa_flat arms Prolog actually takes, per-kind refusal attribution; falsify zeros by injection. (d) Wire Prolog ports into the ZSM ring + autobug harness (law 0b: every box's four ports emit events) and record the β-vs-α RSP exceptions list for Prolog kinds. Deliverable: a census FINDING — the conversion's denominator.
- [~] **PZ-1 THE GATE PAIR — (a) ✅ SEED · (d) ✅ LANDED s248 `e92aebfe` · (b)(c)(e) OPEN.** ⛔ **LON RE-ORDERED IT IN-CHAT s248: (d) BEFORE (b)/(c)** — retire the tautology first so `zdp_tier` supplies the admission signal rather than a hand predicate; (d) is therefore done and its guard is retention-gated, so it arms itself the moment (c) lands. **(c) IS NOW THE CRITICAL PATH AND ITS SCOPE IS LARGER THAN THE FLAG SUGGESTS:** measured s248, `SCRIP_PL_GAMMA_RETAIN=1` implements **ONE of the five enabling sites** (the γ no-unwind arm in `xa_flat`), so flipping it buys nothing (interp 103→91, same as no retention at all) — ⛔ that is **NOT** a falsification of the γ-retain thesis, which has not been tested yet. DONE-WHEN for (c): the −12 disappears. **TERM_VAR MATERIALIZATION + γ-RETAIN, PLANNED AS ONE RUNG (re-ordered by PZ-0's measurement: this is the 86% gate, not a late cleanup).** They are one problem: a logic variable cannot live in a cell the γ-exit releases. (a) **SEED FIRST** (C5 discipline, `d5c7a72b`/`1e881ce5`): hand-write the `.s` for a two-clause predicate with a variable argument, binding, backtrack and trail-unwind on retained frames; run it against live gprolog; ⛔ **LON RULES THE SEED BEFORE ANY LOWERING CHANGE.** (b) γ-RETAIN through the SHARED pin machinery (`emit_rec_pin`/`x86_fb`/heap-fb adopt), never bespoke arms — the five-site enabling pair from `FINDING-2026-08-16-CLAUDE-PL-Z1A` §3: prologue saves caller base + pins own · γ hands base in rax, NO unwind · caller staged-call γ/β landings re-anchor off their own base · backtrack restores own base · ω restores caller base then releases · terminal top-graph exclusion. Flip `SCRIP_PL_GAMMA_RETAIN` ON, then DELETE the release arm. (c) **Retire the tautology at `emit.cpp:2064` and give `IR_VAR_REF` a REAL Prolog guard** — the cell holds the `Term*`/`TERM_VAR`, the retained frame is its home, `zdp_tier` decides SPINE vs ACTIVATION. ⛔ The vacuous `SCRIP_ZD_PL_VR` is replaced by a killswitch that MEASURES (prove it byte-moves both ways before believing any A/B). (d) Conditional trailing (PL-TRAIL-COND: trail only bindings older than the newest CP — gprolog `Word_Needs_Trailing`, SWI `GTrail`) is this pair's third member — spec it here even if it lands at PZ-2 with the record. (e) Discriminating witnesses `plz_p2`/`plz_p6` (⛔ `app/3`/`two.pl` do NOT discriminate — s14 correction). Re-derive ZK-5B from measurement; the s14 falsification was against a γ-RELEASING frame and does not bind the retained substrate. Target: the census ARMED count moves off 100/736 and the shipped default stops being 0.
- [ ] **PZ-1b DISPATCHER RE-LOWER (was PZ-1).** `lower_prolog.c`: predicate clause dispatch moves from {IR_MOVE_LABEL pushes + 0-operand IR_DISJUNCTION pop-jmp} to the **nary IR_DISJUNCTION** (entry,resume)-pairs form the Icon path already emits (`zdp_has_pairs` classifies it); `$ix_g` first-arg indexing stays as guard arms; measure whether `dj_expand`'s synthetic `$disjN/k` predicates still earn their keep. Resume-surface audit: every arm publishes its RIGHTMOST box (s190 trap: `g->all[before]`).
- [ ] **PZ-2 THE CHOICE RECORD (WAM CP as a ζ record).** bb_disjunction upgrades from alt_i/cmp-chain to address-dispatch on the 32B record {resume · δ/TAG · prev_CP · saved_MH}: TAG 0=CHOICE per clause alt, 1=BASE at predicate entry, 2=FENCE for cut; **homed per PF-1b** (`75e3170e`): in the nearest enclosing activation frame — a disjunction pushes NO frame of its own; record-relative addressing only (s189). saved_MH carries the trail mark — the `$trail_mark`/`$unwind_nothrow` node triples between clauses fold into the record's α/β protocol (gap 4). DISJUNCTION gets its OWN tier admission (tier-3-shaped per HQ-73's motion: β re-yields at the same cursor; a different family from IR_MATCH_ALTERNATE — per-family, not per-op). DELETES: bb_move_label PL arms, bb_indirect_goto PL arms, `rt_pl_retry_*`, `rt_pl_cp_*`, `g_pl_retry*` + `g_pl_cp_stack*` (6 globals). Canon per construct: gprolog wam_inst.h CP fields, SWI CHP_* + clause indexing.
- [ ] **PZ-3 CUT = TAG-2 WATERMARK.** IR_CUT lowers to restore-to-barrier: walk/restore rsp + prev_CP + saved_MH TOGETHER to the predicate's BASE record (the REGISTER-PLANES ideal — one cut restores every plane; trail entries below the mark stay COMMITTED, records above die). Keep the compile-time ωeff rewiring (free, correct); the stale-CP structural leak dies. Canon: `Assign_B(BB(B))` (gprolog), `discardChoicesAfter` (SWI pl-wam.c:2604). FENCE-family laws bind: commit whack mandatory (stack "COLOSSAL" otherwise), FENCE1-DEPTH class (a barrier inside an iteration is entered one level deeper each time — witness class already cured for SN4 at `c43d8f51`).
- [ ] **PZ-4 γ-RETAINS: PREDICATE ACTIVATIONS UNDER THE TIER LAW (the keystone, gap 1).** Multi-clause/resumable predicates take the ζ-ACTIVATION tier via `zdp_tier`/heap-fb ADOPT — never a hand predicate. Land the PL-Z-2 enabling pair from `FINDING-2026-08-16-CLAUDE-PL-Z1A` §3 expressed through the SHARED pin machinery (`emit_rec_pin`/`x86_fb`/adopt), not bespoke arms: (a) prologue saves caller base + pins own; (b) γ hands base in rax, NO unwind; (c) caller staged-call γ/β landings re-anchor off their own pinned base; (d) backtrack path restores own base; (e) ω restores caller base then releases; (f) terminal top-graph exclusion. Flip `SCRIP_PL_GAMMA_RETAIN` default ON, then DELETE the release arm for gen graphs. Wire conversion rides this rung: proc-enter γ/ω move rcx/rdx → R10/R11 WIRE-ORDER (`433dc27e` FUNCTION LINKAGE = BLOB INTERFACE). Frame seed/zero-fill decided by measurement (NULVCL heritage, EXTRACT-PL-FRAME).
- [ ] **PZ-5 IN-FRAME RESUME — DELETE THE PENDING-CURSOR MACHINE (gap 3, the largest deletion).** With frames retained, suspend writes the continuation to `[fb+resume_slot]` of the RETAINED frame; caller β re-enters through the frame-resident wire (or the 16B GENP-SPINE resume record). DELETES: `g_pl_zf_pending_*` (6 globals), `rt_pl_zf_resume_set/clear`, the lexprep2 override, the xa_flat γ-intercept, the `$trail_mark` N0-SUPPRESS, `g_pl_zf3_stack*`, and bcps's fresh-frame re-open (the three-abandoned-designs block, bb_call_proc_staged.cpp:813-900).
- [ ] **PZ-6 ONE MACHINE — ARM DELETION (gaps 6+7).** ζ admission becomes the Prolog default (delete `pl_cells_stamp`'s env gate); zframe stamp for Prolog + its epilogue arms + `g_flat_dc_np` plumbing die; ZRES→FRQ DUAL-WRITE dies; root-cause the BINARY `bused[]` node-index shift instead of the unconditional-β workaround; fold the 8 `SCRIP_ZD_PL_*` killswitches. ZK-5B (VAR_REF materialization) RE-DERIVED on the new substrate — discriminating witness first (`app/3`/`two.pl` do NOT discriminate); only if still red does the Option A-vs-B Lon ruling fire. Completion = DoD-3's grep and the global census.
- [ ] **PZ-7 LATTICE COVERAGE + STATEMENT BRACKETS + ZSM CHECKS (gaps 8+9).** Prolog kinds enter the `zdp_*` switches per the one-line law; `lower_prolog` mints the statement-terminal structure `op_zgpop` keys on (the choke is language-blind and waiting); UNWIND law verified per kind (failure never whacks); ZSM full-featured on the Prolog boards — RSP expected-value per port, the β-vs-α exceptions list published (retry/extend boxes are the lawful exceptions), autobug hunts Prolog the way it hunted patterns.
- [ ] **PZ-8 RECONQUEST RATCHET (continuous; this rung is the wall-clock gate).** smoke 3/5→5/5 (the γ-release class dies at PZ-4) · honest ORACLE_CRASH 79→0 · rung 113/109→164/164 both modes · bench-22 8→11 (the s16 waterline)→22 (⚠ the 11→8 drop post-dates `9313935` — bisect against sibling shared-emitter landings if it survives PZ-4). Floor = last cursor's numbers, every session.
- [ ] **PZ-9 FEATURES & CORPUS TO 100% (carried).** (a) Banked engine defects: silent-fail on undefined predicates (→ existence_error) · NO-LCO recursion cap (16-wide body dies at depth 10 — likely the largest bench-22 unlock) · nested-`\+` binding leak · retractall/1 gaps · int/float standard-order conflation (`rt_pl_term_compare`) · lexer escape three-site/two-behaviour · `:` at 200 vs 600 + `|` missing as 1105 xfy · double_quotes-under-dialect (⛔ LON RULING WANTED; + PL-STRICT-1). (b) Feature rungs: call/1..7 goal-as-Var · string type (dialect-gated) · list-ISO · meta-predicates · streams §7.9 · format §7.5 · DCG full · cross-clause catch/throw · PL-TRAIL-COND (conditional trailing = the HB field, stamps exist) · PL-INDEX-L2. (c) Suite reconquest (full text git `94fa9093`): SWI extractor → core → library → db → ISO (gprolog `tests/`); ≥80% each, ×2 modes + oracle.
- [ ] **PZ-10 PERF FENCE (LAST; THE INSTRUMENT only).** (a) medium-invisible `xa_flat.cpp(8)` + `bb_glue_flat.cpp(4)` → `--strict` GREEN (⛔ LON RULING WANTED: 4 dead xa_flat arms convert vs DELETE — several die with PZ-6's arm deletion; recount first); (b) surviving SINK rungs: SINK-5 `$is_v` · SINK-9 `$trail_unwind` (design-heavy, LAST — largely superseded by PZ-2/3's record protocol; recount) · SINK-10 sweep · SINK-4 intern cache · SINK-2 enrich; (c) RTX residue per `ARCH-PROLOG-RTX.md` (SINK owns hot symbols; `queensn`/`queens` widening; proven-cold `c_*` eradication per RTX-CLAIMS); (d) emitted share ≥90% (`gc_collect_ex` largest unowned leaf — ⛔ LON RULING WANTED: GC carve-out vs GC rung); (e) register residency → ≤1.2× → ≤1.0× GNU on the van Roy rail.

## ⛔ FALSIFIED / DO-NOT-REDO (retire only by RE-measurement on a moved substrate)
· **SINK-6/7 arith/cmp strcmp premise** — s147: the dispatch chain is COLD; the cost is the CALL. Double-compare trap: C compares ints VIA DOUBLE — naive int-cmp diverges above 2⁵³.
· **ZK-5B Option A as implemented** — s14 green 6→0 ON THE OLD SUBSTRATE; re-derive at PZ-6 from a discriminating witness, never from memory.
· **Single-kind ZD admission** — s163: the unlock set is FIVE kinds, all-or-nothing per run.
· **Blind-widening `emit_fb_stmt_scan` to IR_DISJUNCTION** — s158: the bail is principled; the whole fb-scan question dies with the zframe arm at PZ-5/6 — do not repair what will be deleted.
· **The family switch as a rung instrument** — s146: per-rung killswitches only; output identity ≠ work identity.
· **Wall time in this container** — s164: bimodal on ONE binary; same-binary-twice control + spread, or no claim.
· **Exit 0 is not exoneration** for stdout-only divergence — falsify by injection, both directions.
· **SN4-inherited bans that bind here** (FINDING §4): α:β global-LIFO pairing instruments (α runs once, β re-fires) · "passing sibling proves the offset" reasoning · per-op filters / declaration tables · undo state in whacked ζ (RULING 4) · per-statement rbp brackets (STF-UNFLIP) · whole-graph carve re-lands ("we scope at a different level now").

## ⛔ LON RULINGS WANTED (strike when ruled)
(1) ~~Storage-arm default cells vs zframe~~ **RESOLVED 2026-08-21 by the replan directive: the SN4 ζ substrate is the machine; zframe arm dies at PZ-6.** (2) ZK-5B VAR_REF Option A vs B — only if the witness is still red on the new substrate (PZ-6). (3) xa_flat dead arms convert vs DELETE — recount after PZ-6 (R-6a heritage). (4) ~24 cross-language `x86("mov",<r32>,imm)` REX.W sites. (5) GC carve-out vs GC rung for the ≥90% KPI (PZ-10d). (6) double_quotes target dialect + PL-STRICT-1 (PZ-9a). (7) s163b/s164(10) provenance — SHA-keyed baselines; the STANDING CONDITION names the mechanism, ruling may be "verify-at-measurement suffices". (NEW 8) **PZ-1(a) SEED RULING — the live one:** how a Prolog logic variable lives on the ζ spine (retained spine cell holding the `Term*` vs activation-frame slot vs heap cell with a spine handle), and the choice-record embodiment beside it (spine-carved ALT-FLAT shape vs frame-homed slot vs the 32B scan-blob cell). ⛔ ~~Lon rules on the hand-written seed before lowering changes.~~ **RETIRED — LON s273: this is a QUESTION, not a GATE. Pick a shape, implement it, grade it; a wrong choice is a finding and a follow-up row, never a reason to wait.** PZ-0 measured why it matters: this is the 86% gate.

## LEDGER — absorbed files + retired ladders (full text in git)
Ten absorbed Prolog GOAL-* files: dispositions at git `94fa9093` (PROLOG-BB · PL-ZETA-CELLS · PL-ZFRAME-RESTORE · PROLOG-RTX · PROLOG-IR-RUN · PROLOG-BB-BYRD · PST-PROLOG · PARSER-PROLOG · LANG-PROLOG · PROLOG-100-SWI; carried lesson: an atom is `DT_A` OR `DT_S` depending on entry — check both tags). The 2026-08-16..21 pre-conversion ladder (R-0h..R-6, PL-Z-1a/Z-2 zframe repair): retired by the replan; text at git `85542ed6`; PL-Z-1a's arm and FINDING remain LIVE inputs to PZ-4.

## Architecture reference (the living contract)
Pipeline: Prolog AST → `lower_prolog` (four-port IR) → m3 `--run` (EMIT BINARY → RX slab) · m4 `--compile` (EMIT TEXT → as+gcc). GZ ports: δ = callee α, ε = callee β.
| Construct | α | β | γ | ω |
|---|---|---|---|---|
| `IR_GCONJ` (seq) | first goal's α | last goal's β | `goal[i].γ = goal[i+1].α` | `goal[i+1].ω = goal[i].β`; first → ω_in |
| `IR_CHOICE`/nary `IR_DISJUNCTION` | first clause α | record resume (rightmost box of the live arm) | each `.γ = γ_in` | `clause[i].ω = clause[i+1].α`; last → ω_in |
| `IR_GOAL` (call) | callee α | callee β | callee success → γ_in | callee exhausted → ω_in |
| `IR_ITE` | cond.α | ω_in (semidet) | cond.γ→Then, Then.γ→γ_in | cond.ω→Else, Else.ω→ω_in |
| `IR_UNIFY` | self | — | bind/match → γ_in | mismatch → ω_in |
| `IR_CUT` | self | — | γ_in (after TAG-2 restore) | cut barrier → ω_in |
| leaf | self | — | γ_in | ω_in |

Admission recipe (new deterministic builtin): (1) `IR_DET_FOO` in `IR.h` + name table `scrip_ir.c`; (2) `rt_pl_foo_cell(...)` — cell-based, trail-mark/unwind, no `g_resolve_env`, `prolog_atom_intern()` not `ATOM_*`; (3) `bb_det_foo.cpp` — FRQ per slot, one call, `test eax,eax; jne γ; jmp ω; def β; jmp ω`; (4) `bb_prepare` block in `emit_bb.c`; (5) `emit_core.c` dispatch case; (6) Makefile `RT_PIC_SRCS` + rule; (7) four `scrip.c` sites (`pl_gz_rule_body_goal_ok` · `pl_gz_rule_clause` op-filter · `pl_gz_count_synth_goal` · `pl_gz_build_goal`, named arm BEFORE the generic pair-comparator arm). `ir_call_arg` for `is_builtin_exec` builtins; `ir_pair_arg` for arity-2 pairs. New ζ kinds additionally: one edit each in `zd_k` + `zd_nops` + the grant authority — nothing re-spelled (ONE AUTHORITY).

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh && rm -f scrip && timeout 1500 make -j8 scrip > /tmp/build.log 2>&1 && make libscrip_rt && ls -la scrip out/libscrip_rt.so
# ⛔ CONCURRENCY PRE-CHECK (STANDING CONDITION): git -C . log origin/main..HEAD ; git status --porcelain — non-empty ⇒ clone fresh and measure THERE
bash scripts/test_smoke_prolog.sh && bash scripts/test_prolog_bb_honest.sh && bash scripts/test_prolog_rung_suite.sh && bash scripts/test_bench_prolog_modes.sh
bash scripts/test_gate_pl_no_new_global.sh && bash scripts/test_gate_emit_no_lang.sh
# primary sources (semantics rungs): local seat has /home/resources/{gprolog-master,swipl-devel-master} unpacked; web seats ask Lon (refs/ gitignored, per-session)
```

## ⛔ SESSION-CLOSE RULES LIVE IN `RULES.md` — NOT DUPLICATED HERE
"HANDOFF COMPLETE" requires a confirmed push; `scripts/handoff_status.sh` verbatim is the only truth; credential missing ⇒ BLOCKED, ask Lon in-chat and WAIT. Move THIS file's LIVE CURSOR every session — no cursor move, no close.
