# ⛔⭐⭐⭐ GOAL-PROLOG-100 — SCRIP PROLOG x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

## ⛔⭐⭐⭐ LON RULING, 2026-09-08 17:34 (in-chat to ceo) — **NO PROLOG COMPATIBILITY SWITCH: THE SUPERSET — ISO IS THE CORE AND DEFAULT, EVERY NON-CONFLICTING GNU AND SWI BUILTIN IS ADDED, CONFLICTS GO THROUGH ISO PROLOG FLAGS** (RULES § Oracles FACT RULE NO COMPATIBILITY SWITCH FOR ANY SCRIP LANGUAGE; CEO-391)

Lon, verbatim: *"Okay make it so then. So no compat switch for any of our SCRIP language. Right?"* — on the ceo's recommendation: *implement the superset, no switch; ISO defaults; conflicts through ISO's own `set_prolog_flag/2`, set by the program or a suite runner's one-line directive.* Supersedes the 2026-09-04 17:03 two-switch ruling. Consequences for the lane: (1) no `--compat`, no dialect global, no flag in the driver — a GNU or SWI program runs unchanged when its surface is additive; (2) the double_quotes flag is the first conflict to carry (ISO/GNU `codes`, SWI `string`), the SWI runner sets it by directive where swipl's default differs; (3) a program that needs surface neither ISO nor the superset carries is OUTSIDE THE BASELINE, named in an inventory beside `swi_tests` / `gnu_prolog` / `gnu_examples`, out of the denominator, never hidden; (4) INRIA (396/445 at 09-07) grades the core; the SWI and GNU boards are the superset's measure. Prolog ships as it stands for 09-10 (EXECUTIVE order of work); this ruling shapes the rows, not the 09-10 cut.

## ⛔⭐⭐⭐ LON DESIGN RULING, 2026-08-24 s273 — **PROLOG USES `DESCR`, NOT `Term`. ALLOCATIONS RIDE THE THREE ZETAS, IN ORDER. NOT THE HEAP.**

**Lon, in-chat to hq_C, verbatim in substance:** *Prolog should not use `Term` at all, it should be using `DESCR`. It is out of whack design-wise. Things do not really need to live on the heap for our GDE stuff — look at Icon and SNOBOL4. The values for strings, yes. But we are using Byrd Boxes. **Any allocations better live on (1) the SPINE, (2) the ACTIVATION FRAME, (3) the STANDING (ROOT) ACTIVATION FRAME — IN THAT ORDER.** We use BB with RESULT and BB LOCALS. You can have as many RESULTS and LOCALS as needed at ALL THREE ZETAS. Tell Prolog to get with the program.*

⭐ **THIS ANSWERS OPEN QUESTION 8 (PZ-1(a)). IT IS NO LONGER THE IMPLEMENTER'S CHOICE.** s273 retired the seed *gate* and briefly left the shape free; **this ruling closes the shape.** The three-way menu (retained spine cell / activation-frame slot / heap cell with a spine handle) is now **ordered, not optional**: **SPINE first, ACTIVATION FRAME second, STANDING (ROOT) ACTIVATION FRAME third.** ⛔ **The heap arm is struck** — it was never an equal option, and choosing it is now a defect, not a trade-off.

⛔ **THE MEASURED STATE, verified by hq_C at s273 before routing this — Lon's read is exactly right:**
- `src/parser/prolog/term.h` defines a **complete second value representation** parallel to `DESCR_t`: its own `TermTag` enum (`TERM_ATOM`/`VAR`/`COMPOUND`/`INT`/`FLOAT`/`REF`) and a tagged union carrying `atom_id`/`var_slot`/`{functor,arity,Term **args}`/`ival`/`fval`/`Term *ref`.
- **448** `Term`/`Term_t`/`Term *` references across the Prolog frontend, `unification.c`, and `sync_monitor.c`.
- It is **heap-allocated**: `Term **args = malloc(...)` at multiple sites; **23** malloc/free sites in `prolog_parse.c` alone.
- **Only 2** Prolog-reachable files touch `DESCR_t` at all (`pl_cell.h`, `unification.c`).

⭐ **WHY THIS IS THE WHOLE POINT AND NOT A CLEANUP:** SNOBOL4 patterns, Icon generators and Prolog backtracking are **three syntaxes over one machine** — the four-port Byrd box. A box carries its values in **BB RESULT and BB LOCALS**, and *"you can have as many RESULTS and LOCALS as needed at ALL THREE ZETAS."* A language that invents its own heap-allocated node type is **not on that machine** — it is a second machine wearing the first one's ports, which is why Prolog keeps needing bespoke frame work (`rt_jmp_frame_lexprep2`) that the other two never needed. ⛔ **Only string VALUES belong on the heap.** Structure does not.

⛔ **STANDING INSTRUCTION TO hq_C (Lon: *"and you keep on him about it"*):** this is not a one-time telegram. **Every Prolog row this seat touches gets checked against this ruling**, and a Prolog change that adds `Term` surface or a new heap allocation is refused review until it is re-shaped onto the zetas. Recorded here so the obligation outlives any one session.

⭐ **RE-AFFIRMED 2026-08-31 ON THE TRAIL-RESIDENCY ESCALATION (Lon in-chat to ceo: *"The answer is (b). We have moved to the THREE ZETAS."*):** hq_P measured trail composition at the fatal stop across all 21 van Roy kernels — stack-resident 1951, heap-resident **0** — refuting commit 77925561's "all trail entries are PLJ-heap-resident" guarantee; hq_C escalated that *enforcing* the guarantee would relocate Prolog variable storage into the heap, a redesign contradicting the s273 ruling above. Lon refused it: **variables stay on the RSP/RBP zetas; the trail-unwind machinery is what gets fixed** (depth-tracked mark transport + refuse-loudly bounds tripwire, row `calling-convention-depth-tracked`, hq_P). The 77925561 guarantee is dead letter **by ruling**, not only by measurement — any code arm still predicated on trail heap-residency is wrong by law. `tasks/prolog-trail-heap-residency-storage-redesign.task.md` is CLOSED-REFUSED, never live.

⭐⭐⭐ **RE-AFFIRMED AND MADE THE FLEET'S TASK 2026-09-01 (Lon in-chat to ceo, verbatim in substance: *"I have the hard task and we'll get mode FLEET-8 to help. Let's eradicate the usage of Term structure in Prolog and replace with DESCR. Am I correct that Term is not strictly necessary … No need for Prolog to be special."* then *"We'll run in FLEET-8 mode."*):** MODE rewritten `FLEET-8` the same minute (ceo + hq_C + hq_P + hq_B + seat01..08). ceo's answer to the question, measured at SCRIP `8eac17da`: **yes — `Term` is a second value representation, not a necessity.** Emitted Prolog already runs on `DESCR` cells (`DT_PLREF`/`DT_PLVAR`; `by_name_dispatch.c` 1334 DESCR mentions against 8 `Term`); `Term` survives only (i) inside the legacy C services — `unification.c` 208 word-refs, `rt_runtime.c` 38 (env/copy/findall as `Term **`), `builtins/resolution.{c,h}` 16 — which RE-MATERIALISE a heap `Term` from the cells through `pl_cell_conv.h` (`pl_cell_to_term`, `rt_pl_cell_to_term_named`) on every unify/write/copy/findall, the converter being the proof of redundancy; and (ii) in the parser, which builds `Term` trees (`prolog_parse.c` 85 · `prolog_lower.c` 26 · `prolog_builtin.c` 24 · `prolog_atom.c` 17) and converts them to the common `tree_t` (`pl_assert_term` at `lower_prolog.c:1229`) where every other frontend parses into `tree_t` directly; plus `driver/{scrip.c,driver_private.h,sync_monitor.c}` includes. 490 word-refs total (s273 baseline 448; 469 at the 08-27 mint — still growing). **ROUTING:** row `prolog-term-to-descr-eradication` (hq_C, minted 2026-08-27 on this ruling) is UN-PARKED from its PZ-4 wait by Lon's word and re-stated `PARKED-UMBRELLA:hq_C-slices-FLEET-8`: hq_C sequences and mints the one-row-one-seat slices (ratchet slices with the PRINTED Term-ref count per commit; SHARED-NODE VERDICT SCOPE on every landing; the baton's original guidance stands). Natural cut, for hq_C to confirm or refute by measurement: runtime services onto cells first (unify/write/copy/findall/resolution read `DESCR` directly, converters deleted), parser to `tree_t` second, `term.h` deleted last; slices that need a γ-surviving frame for WHERE a logic variable lives queue behind PZ-4 `prolog-pz4-gamma-retain-activation-frames` (hq_C-owned, `BLOCKED-ON:calling-convention-depth-tracked`, hq_P) — the keystone is unchanged, the rest of the conversion is not gated on it. ⛔ **INSTRUMENT DEFECT FOUND AT ROUTING, ceo's own mint (RULES.md § THE INSTRUMENT LAWS — fail once and pass once):** the row's DONE-WHEN is FALSE-GREEN today — run verbatim under `S4E_HOME=/home/claude` it exits 0 (`RESULT: PASS`) with `src/parsers/prolog/term.h` present and 490 refs live, because clause 1 tests the RETIRED path `src/parser/prolog/term.h` (the `src/parsers/` rename silenced it) and clause 2's regex `\bTerm_t?\b` matches only `Term_`/`Term_t` (0 hits), never `Term`. **hq_C re-cuts it before the first slice is minted** and every slice row inherits the corrected count. Also from the same ruling's fallout, hq_P (2026-09-01): the 77925561 residency guarantee — dead letter by ruling — was the SAFETY ARGUMENT for a live bypass, `scrip.c:1904`/`:1907` `g_plw_floor_bypass=1` for Prolog zframe graphs (switches `plc_dead_cstack` off for every trail entry); the bypass has a second, still-true cause (`-O0` `dop_unwind_nothrow` leaves RSP 8 mod 16, so `plc_dead_cstack`'s own sscanf `movaps` SEGVs; bench m3 6/22→12/22) but nothing now asserts a bypassed entry is SAFE. ceo ruling: minted `prolog-plw-floor-bypass-safety-unproven` at rank 2 (not the frozen burn-down; not blocking), hq_P's lane, and it is IN SCOPE of this umbrella — when trail entries ride cells on the zetas the bypass's premise is re-examined, not inherited.

⭐⭐ **AMENDED 2026-09-01, LATER THE SAME DAY — LON: *"We are going to mode FLEET-16. Prolog is the #1 priority. All around test, performance, Term struct eradication."*** MODE rewritten `FLEET-16`; the priority rows of GOAL-HQ-COMPLETE.md and GOAL-HQ-PERFORM.md now read **PROLOG #1 · SNOBOL4 #2 · Icon #3** (SNOBOL4's blocking floor `make test` remains the control arm on every landing — the order names where the fleet's hands go, not what may regress). **THE SLICES ARE hq_C's** (FINDING `.github 95c50446`; table in the umbrella's `## NEXT`): s1 write/format/printers · s2 compare/sort/pairs · s3 copy/findall/nb/aggregate · s4 typetest/functor/univ/succ (+ deletes `prolog_unify.c`) · s5 dynamic-db/flags/streams · s6 parser builds `tree_t` directly · s7 dead resolution env layer deleted · s8 `term.h` + converters deleted (carries the umbrella's DONE-WHEN; BLOCKED-ON s1–s7); s1–s7 at rank 0 by Lon's word (hq_C re-ranked after measuring 40 minutes of starvation behind fifteen older rank-1 rows — dispatcher row `next-tiebreak-by-mint-time-not-file-order`); a ceo-drafted duplicate set was withdrawn unminted. **THREE CORRECTIONS TO THE BLOCK ABOVE, measured by hq_C (accepted as written):** (a) the service ABI is ALREADY cells — 75 exported `rt_pl_*_cell` in `unification.c` manufacture a `Term` internally at 106 converter sites and emitted code reaches them only via `by_name_dispatch` — so runtime-first is not merely first, it is independent of everything else; (b) `pl_assert_term` is declared three times and DEFINED NOWHERE, zero calls — the parser→`tree_t` boundary is `prolog_lower.c:lower_term` (static), and the sentence above naming `pl_assert_term` as the bridge is wrong; (c) the resolution-engine `Term **env` layer (`resolve_env_new`, `rt_env_current`, `resolve_catch_push`, `resolve_throw_term`, `resolve_bb_env_install`) has ZERO callers under `src/` — live variables already ride `DT_PLVAR` cells — so **PZ-4 gates no slice**; that layer is a deletion (s7). The "slices needing a γ-surviving frame queue behind PZ-4" sentence above is therefore VOID for this umbrella; PZ-4 stays hq_C's own keystone for the multi-clause/resumable-predicate ladder. Also measured by hq_C: `test_gate_pl_gz2/3/4` are dead instruments (0 `gzq` refs in src; row `test-gate-pl-gz-dead-instruments-measure-a-retired-path`; pre-existing red recorded on the umbrella ledger so no slice is blamed). **FOR SEATS 09–16:** all 17 FREE Prolog rows re-ranked to rank 1 by ceo on Lon's priority (QUEUE.tsv snapshot `.bak.ceo-prolog-priority-*`), so `next` serves Prolog before anything else; hq_C mints the Prolog TEST rows (the rung suite reads `PASS=4 FAIL=11 TOTAL=15` in BOTH modes at SCRIP 8eac17da — eleven named reds — plus the vendored GNU/SWI conformance suites and the ISO coverage audits), hq_P the Prolog PERFORMANCE rows (the vanroy 21-kernel board on the two-number basis vs swipl/gprolog; `prolog-plw-floor-bypass-safety-unproven` is already in that lane).

⭐ **INTERPRETATION 2026-09-01 (hq_C's reading on seat10's slice-5 question, confirmed by ceo; Lon may correct it in one word):** the s273 order — spine → activation frame → standing/root frame, heap only for string values — governs VALUES during matching. A dynamic-database clause (`assert`/`retract`) is PERSISTENT program state that outlives every activation and every backtrack and GROWS without bound at run time, so no zeta can hold it: it is a genuine escaper in the sense of RULES.md § BB FRAME-PLACEMENT CRITERION (the same clause that sends strings, heap cells and Raku's escaping closures to the heap). RULING: stored clauses are DESCR cells in the runtime workspace arena (`rt_ws_alloc`) — the SHAPE law (DESCR, never Term) is what Lon ruled and it is kept; the predicate table that indexes them is fixed-size root-level state and lives in the STANDING frame. Slice s5 proceeds on this; if Lon corrects the residency, the cells move and the shape does not. Routed also to the umbrella ledger.

## ⛔⭐⭐⭐⭐ LON ORDER, 2026-09-02 09:55 (in-chat to ceo) — **GIVE PROLOG ITS ACTIVATION ζ NOW. PZ-4 FIRST.**

**Lon, verbatim:** *"So what are we waiting for, give Prolog its activation ZETA like all the rest. OMG! Go fix it."*

**Trigger (ceo assessment, same minute):** the design is sound at the front — Prolog lowers to the same 136 shared boxes as Icon and SNOBOL4, no interpreter, DESCR law finishing — and broken at β: hq_B measured a two-clause `fact/2` with ZERO rbp-relative instructions and zero `push rbp`; Prolog boxes have no activation frame, so nothing survives γ for β to resume. The shallow board reads 351/371 both modes; the deep rungs read 3/15 and every red is a backtracking shape; 8 of 21 van Roy kernels crash. Control that belongs in ports lives in C (`by_name_dispatch.c` 7,528 lines: string dispatch, ten setjmp/longjmp sites, a call/N solver, a polled exception flag).

**The order as law for this lane:** PZ-4 (`prolog-pz4-gamma-retain-activation-frames`, C21, ASSIGNED hq_C) is FIRST — before T9's tail and before C36. The design is Lon's word, not a sitting: Prolog gets the **ζ-ACTIVATION-FRAME on RBP exactly as Icon has it** under the FRAME-PLACEMENT CRITERION — every predicate call is a box whose frame survives γ so β can resume it; clause alternatives ride the choice box's β path; cut is the choice box's ω; the trail is β's undo and lives in the frame (P10 folds in); `x86_fb()`/`x86_fb_pinned()` answer rbp for Prolog procs instead of the stub's rsp/0. Then the crutches leave the C runtime as one program, *Prolog control through the ports*: C36 (call/N compiled through EVAL's path, solver deleted), P6 (builtins wired at compile time), P7 (no setjmp failure), C37 (exceptions propagate through ports, not a polled flag). Acceptance is measured: PZ-4's clause (c) bomb silent; rbp frames present in the `fact/2` witness; C22/C23/C24/C26 witnesses green; the rung-suite backtracking reds and the van Roy REFUSE kernels re-run; master not worse than 351/351 both modes per milestone.

**LON ESCALATION, 10:00, verbatim:** *"The answer to how Prolog is doing is TERRIBLE. If we did not do proper THREE ZETAS for Prolog then go do a complete re design. I want that bad code deleted now. It has been too long coming."* → **THIS LANE IS NOW ONE PROGRAM: PROLOG REDESIGN ON THE THREE ZETAS.** hq_C leads the mechanism, hq_P the perf shape, the ceo writes the design page `ARCH-PROLOG-THREE-ZETAS.md` (the map of every Prolog construct onto boxes and zetas, the deletion list, the acceptance) and the HQs fill its mechanism sections; seats take the rungs. THE DELETION LIST, each a rung whose DONE-WHEN is the symbol gone from `src/` and from `nm -D libscrip_rt.so` with the boards not worse: the `plc_*` call/N solver and `rt_pl_call_gen` (C36) · every setjmp/longjmp implementing Prolog failure or cut (P7; ten sites in `by_name_dispatch.c`) · the by-name string cascade for Prolog builtins (P6) · the polled `rt_pl_throw_pending` flag (C37; one checker) · Term to zero with both headers gone (T9; 75 at `5ebc7961`). THE BUILD: PZ-4 first (the ζ-ACTIVATION-FRAME on RBP for every proc, Icon's model), then PZ-5, with clause alternatives on the choice box's β, cut on its ω, catch/3 a box with its handler on β, the trail in the frame. Language identity stops at lower: the emitter gates the frame on an IR property of the graph, never on a language flag (`icn_cells_graph` is misnamed for what it will now mean). GATE of the program: rung suite 15/15 both modes · van Roy REFUSE 0 · master ≥ 351/351 · SNOBOL4 floor green.

**⭐⭐⭐ struct Term IS DELETED ON ORIGIN — SCRIP `46db4457` (ceo, 2026-09-02 10:40, on Lon's 10:35 order):** `term.h`, `pl_cell_conv.h`, `prolog_unify.c`, `prolog_runtime.h` gone; the five constructors, the dead second choice-point/catch/env machine in `resolution.{c,h}`, the struct-tree trail, `g_resolve_nb`, `PlCallSt` gone; `grep -rcw Term src` = 0 (490 → 0 in 27 hours); the ratchet gate passes at zero. Gates on the pushed tree: smoke 5/5 both modes, parser PASS, rung38 10/10, SNOBOL4 floor 1679/0 both modes, Prolog master **348/348 of 371** = origin's identical entry set (PZ-4 clause (a) moved 351 → 348). The umbrella row `prolog-term-to-descr-eradication` stays OPEN on its own last clause — the board at fail=0 both modes, which is § 6's program gate — released under CEO mode with the receipt on its ledger.

**THE DESIGN PAGE: `ARCH-PROLOG-THREE-ZETAS.md` (landed 2026-09-02 10:25, `8426dc57`) — measurements, the three zetas applied, the box map, the mechanism seams with their lines, the deletion list with rows, the value services that stay, order and gates. It is the order of the work for this lane; hq_C fills its mechanism sections as PZ-4 lands.**

## ⛔⭐⭐⭐⭐ LON ORDER, 2026-09-01 ~21:15 (in-chat to ceo, on the Term report *353 of 490 remaining*) — **WHACK THE Term STRUCT NOW. THE SLICES ARE RETIRED.**

**Lon, verbatim:** *"So then it is time to whack it. Completely remove the Term struct and all it's references. I do not want to see it every again. Whatever process is being done is too slow. So whack it now!"*

**Execution (ceo ruling for speed, same minute):** ONE OWNER — hq_C — big-bang in its own root, compile-driven: delete `src/parsers/prolog/term.h`, `pl_cell_conv.h`, `pl_cell_conv_test.c` first, then follow the compiler to zero across the 19 files carrying the 353 word-refs at `e182a71a` (`unification.c` 116 · `prolog_parse.c` 85 · `prolog_lower.c` 26 · `prolog_builtin.c` 24 · `prolog_atom.c` 17 · `rt_runtime.c` 13 · `resolution.h` 12 · `prolog_builtin.h` 9 · `by_name_dispatch.c` 4 · the rest single digits), every consumer rewritten onto DESCR cells per the s273 law. Finish line `grep -rcw Term src --include='*.c' --include='*.h' --include='*.cpp'` sums to 0 and the two headers do not exist; on the way and at the end: Prolog smoke 5/5 both modes, parser suite PASS, master board not worse than 217/371, SNOBOL4 floor FAIL=0 (shared-node control arm), the consume-the-value witnesses (write/1 + compare on every converted class, five runs). Push per compile-clean milestone. The five slice seats (T1 seat06 · T2 seat07 · T4 seat15 · T5 seat03 · T6 seat02) push what compiles or save the diff to `postoffice/salvage/` and release within ten minutes; T7 parked behind the umbrella; T9 `prolog-term-to-descr-eradication` FREE for hq_C's claim; the slice rows close on their own DONE-WHENs when the umbrella lands. The two-encodings trap (atoms as DT_A vs DT_S, readers accepting both) and the functor/3 lesson (a representation change is validated only by consuming the value) are the two hazards named on the umbrella baton.

## ⛔⭐⭐⭐ LON RULING, 2026-09-01 ~20:53 (in-chat to ceo, routed the same minute) — **NO PROLOG RUNTIME INTERPRETER. `call/N` RE-USES THE EVAL/CODE RUNTIME-COMPILATION PATH.**

**Lon, verbatim:** *"Remove that Prolog runtime interpreter, we already decided that Prolog should be re-using EVAL and CODE."* — and, a minute later, *"Remove the dead AST walkers."*

**Trigger (ceo audit on SCRIP `6ede7c31`, FINDING `FINDING-2026-09-01-ceo-modes-3-4-no-ast-or-ir-walking-…`):** modes 3/4 walk no AST and no IR; the ONE live runtime interpreter left in the tree is Prolog `call/N`'s solver in `src/runtime/by_name_dispatch.c` — `plc_slv_t` (:4458), `plc_build`/`plc_build_resolved` building a solver tree from the goal term by `strcmp` on functor names (:4508-4557), `plc_det_exec`'s `switch (s->op)` (:4493), `plc_next` (:4570), a runtime term reader (:4690-4749), entry `rt_pl_call_gen` (:4752); 136 `plc_` references, witnessed live by gdb on `G = write(hi), call(G)`.

**The ruling, as law for this lane:** every goal that is not known at compile time — `call/1..8`, the goal arm of `catch/3`, `findall`/`bagof`/`setof`/`forall`/`\+` over a variable goal, `assert`ed clause bodies invoked later — is COMPILED at run time through the same path EVAL and CODE use for SNOBOL4 (`src/runtime/runtime_eval.c`: build the graph → `optimizer_run` → `emit_chain` → jump; the pattern JIT at `src/runtime/rt/bb_pat_build.cpp:84-110` is the same shape, compiled once at construction, blob cached) and then EXECUTED as wired boxes. The `plc_*` family is DELETED, not bypassed; no runtime dispatch loop over goal terms may exist anywhere in `src/runtime`. The unknown-functor→user-predicate class (C10, cured by seat15 in SCRIP `fb98a05eb` inside the solver) is preserved by making its witnesses the acceptance tests of the replacement, never by keeping the solver. Rung: MASTER-PLAN ⭐C36 (hq_C mints, rank 0). The dead AST walkers named in the same FINDING (`scope_patch`, `static_get/set`, `driver_call.c:139-380` over a never-assigned `g_exec_prog`, the stubs `eval_ast`/`eval_ast_pat`/`eval_node`/`exec_stmt`) are deleted under the second order — rung ⭐I14 (hq_C mints, rank 0).

## ⛔⭐⭐ LON OVERRIDE, 2026-08-24 s273 — **SEED-FIRST IS RETIRED. PROLOG HAS LIBERTY.**

**Lon, in-chat to hq_C, verbatim in substance:** *that is a silly standing rule — it came from when we were at ground zero. Give the Prolog seats liberty to get the work done.*

⛔ **WHAT THIS RETIRES, effective immediately:**
- The **seed-first law** (§ "Across-the-board admission…", struck below): *prove a new machine shape as hand-written `.s` against the oracle BEFORE compiler changes*. **No longer a precondition for anything.** A seat may change the lowerer/templates directly.
- **Open question 8's gating clause** — *"Lon rules on the hand-written seed before lowering changes"* (PZ-1(a), the ζ-spine logic-variable + choice-record shape). ⭐ **The QUESTION stays interesting; it stops being a GATE.** A seat picks the shape, implements it, and grades it. If measurement says the choice was wrong, that is a finding and a follow-up row — not a reason nobody was allowed to start.
- Any row held `BLOCKED` on either. **`prolog-multiclause-uninit-lexprep-frame` is unblocked to `FREE`, rank 0**, as of this ruling.

⭐ **WHY, recorded so it is not re-litigated:** the rule was written at ground zero, when no Prolog machine shape existed and a wrong guess cost the whole substrate. That is no longer where this project is — the shape is largely built, the failures are concrete, and the rule had stopped protecting anything. Its measured cost this session: `rt_jmp_frame_lexprep2` is a **no-op called by every 2+-clause predicate's prologue**, root-caused with gdb by seat01, who then correctly stopped because of this rule — leaving **every multi-clause Prolog predicate** riding an uninitialized frame while the fix waited on a ruling. ⛔ **A rule whose only remaining effect is to make correct engineers stop is not a safeguard.**

✅ **WHAT STILL BINDS — and this is NOT the seed rule wearing a new hat:**
`src/templates/xa_flat.cpp` is **shared with Icon**. Any change there is a cross-language change, so it is graded on all three frontends with a same-tree control arm: **SNOBOL4 holds 365/365 both modes SKIP=0**, and the **Icon rung suite does not move from its pinned number**. ⭐ That is the standing SHARED-NODE rule and it is about not silently breaking two other languages — it gates **landing**, never **starting**. Lon's ruling removes the permission step, not the measurement.

**Opened 2026-08-16 s165 (Lon). REPLANNED 2026-08-21 (Lon in-chat, seat2, verbatim in substance: *"This is a new world of code, and we will be doing things EXACTLY like SNOBOL4 has been doing: THREE ZETAS, FORTH-style stack, all of it. … Make a plan, rungs, and steps based on your discovery of the gaps in Prolog that SNOBOL4 has closed."*).** The pre-conversion ladder (R-0h..R-6, the zframe-repair track) is RETIRED — full text in git (`85542ed6` and before); every deleted Prolog GOAL-* name still resolves HERE (dispositions git `94fa9093`). **Discovery record: `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md`** — the SN4 campaign arc C1–C16, the TEN GAPS with file:line, Lon's ruling SHAs, the twelve facts. Read it before touching any rung.

## ⛔ FACT RULES (RULES.md is the authority; two locally critical)
- **⛔⛔⛔ NO NEW GLOBAL VARIABLES without Lon's explicit in-chat permission that session; the ask must be a big unmissable banner** (name, type, owning file, purpose, why registers/stack cannot carry it). State rides registers (r10/r11 wires) and the stack. This conversion DELETES globals (census: ~40 `g_pl_*`; the 15 CP/resume ones die at PZ-2..PZ-5) — it never adds one.
- ⛔⛔ **NO `-O2` BUILDS. EVER (Lon, 2026-08-23 s262 FACT RULE — this SUPERSEDES the O0-DEV-O2-BENCH text that stood here, which is DEAD and must not be followed).** `RT_OPT` is `-O0` for development AND benchmarks AND demos. Never pass `RT_OPT="-O2 …"`, never build an `-O2` RT_TAG, never quote an `-O2` number as the current state. TWO REASONS, the second the stronger: cost (~9m30 vs ~1m40 per template-touching rebuild, paid on every arm of a measure-and-cure loop), and ⭐ **it measures a compiler we are deleting** — the RT is moving to register-aware ASM (`GOAL-RTCC.md`, `src/runtime/rtx/*.S`) with C kept for at most a few large algorithms, so an `-O2` figure grades gcc's optimizer over code that will not exist. The LABELING duty survives and now reads `-O0`. Authority: `RULES.md` § NO `-O2` BUILDS. ⚠ `make` skips recompiles when only `RT_OPT` changes — `rm -f out/rt_pic/*.o out/libscrip_rt.so` first. Label every perf number with its `RT_OPT`; it reads `-O0`.

## ⭐⭐⭐ THE MODEL — THE THREE ZETAS: THE SN4 MACHINE, PROLOG EMBODIMENT
Lon's s81 register ruling (verbatim: *"RSP for ZETA SPINE, RBP for ZETA ACTIVATION FRAMES, and RBP for ZETA STANDING AREA"*); cells substrate is THE routing, one system (`db728001`). Tier verdict comes from ONE authority — `zdp_tier` (`zeta_depth.c:131`): STANDING if bracket-lifetime, ACTIVATION if α-depth unknown or resume unsound, SPINE otherwise; **refusal to SPINE is the safety property**, never a per-op filter.

| tier | register | SN4 embodiment (LANDED) | Prolog embodiment (TARGET) |
|---|---|---|---|
| **ζ-SPINE** | RSP | per-BB FORTH cells: α carves own K (`zd_k`, one authority), result at cell+0, consumers read staged depth-differences (`ZOPQ`/`op_zread`) NON-POPPING, γ SUSPENDS the cell, β free (LIFO home), ω frees own K; ONE statement-terminal release (`op_zgpop` at the choke); choice/resume records ride the spine | goal operands/results as cells (PL-ZK arms exist, emit.cpp:2237-2245); **the CLAUSE-CHOICE RECORD as a 32B record** — {resume · δ/TAG · prev_CP · saved_MH}, encoders at x86_asm.h:2125 (Lon 2026-07-28): TAG 0=CHOICE (clause alt) · 1=BASE (predicate entry barrier) · 2=FENCE (cut) |
| **ζ-STANDING** | RBP | ROOT activation per match statement (`emit_match_rbp`, `frame_slot_scan` slot registry), whacked at END/fail | per top-level goal-statement bracket; registered slots for cross-goal housekeeping |
| **ζ-ACTIVATION** | RBP chained | per `*P` defer / dynamic-K site; **heap-fb ADOPT** for β-resumable graphs (`emit_heap_fb_adopt`, emit.h:654 — "a β-resumable activation cannot ride an unwinding stack") | per PREDICATE activation: multi-clause/resumable predicate = ACTIVATION tier; **RETAINED while its choice record lives, freed LIFO by backtrack-ω / cut** — gprolog's law `Local_Top = ((B >= E) ? B : E)` (wam_archi.def:120) in three-zetas form |

**WAM correspondence (SN4 built a WAM; the canon confirms):** spine ≡ local stack · choice record ≡ CP frame (resume≡ALTB · prev_CP≡BB · saved_MH≡TRB; HB/CPB/BCIB/CSB deferred as before, PL-TRAIL-COND stays a feature rung) · cut ≡ `Assign_B(BB(B))` ≡ SWI `discardChoicesAfter` ≡ TAG-2 watermark restore · SWI CHP_CLAUSE/CHP_JUMP/CHP_CATCH ≡ the TAG taxonomy · trail ≡ the base-pinned undo arena (RULING 4: undo survives forward whacks, does not commit on failure; COMMIT-THEN-WHACK).
**Register contract of record (binds Prolog at conversion):** R10/R11 = γ/ω wires, LIVE never scratch, WIRE-ORDER γ@+0/ω@+8, `push r11; push r10`; FUNCTION LINKAGE = BLOB INTERFACE (Lon `433dc27e`) — Prolog's current rcx/rdx proc-enter wires CONVERT (PZ-4). RBX = heap frontier · R12 = undo-arena top (SN4 CAS; Prolog trail marks ride records, the trail data structure stays runtime) · R13/R14 = Σ/δ scan-reserved · ⭐ **R15 = CP (the choice-point register) IN PROLOG GRAPHS — LON RULING, in-chat 2026-08-21 (seat2), AMENDING THIS CONTRACT:** the seed parks CP in r15 and Lon ruled *keep r15, amend the contract* — Σ/δ/Δ are **scan-only** reservations, and a Prolog graph does no string scanning, so R15 is free there and carries CP. `fail` is therefore uniformly `jmp [r15]` and cut is `r15 = [r15+16]`. ⛔ The reservation still binds any graph that scans; this releases R15 for Prolog graphs ONLY, and PZ-2 must not assume it in shared code. · R9 = RTCC/GVA base, R8+tier = RTCC slots. ⛔ **THE RTCC BANK DOES NOT NEST** — a C-entered blob without its register plane jumps residue (SN4's M1 wall, s194); Prolog adds NO C→BB entry that skips the wire push.
**Canonical sources (RULES.md law):** primary source MANDATORY for semantics rungs — local seat: `/home/resources/gprolog-master` + `/home/resources/swipl-devel-master` (unpacked); web seats ask Lon. Consult per construct as you touch it. `SCRIP/doc/*STUDY*.md` are notes, not a substitute.

## ⛔ DEFINITION OF DONE — "100%" MEANS THIS AND NOTHING LESS
1. Rung suite 164/164 BOTH modes · smoke 5/5 · honest board green with ORACLE_CRASH=0 (rc≠0 counted, R-0h landed s166).
2. bench-22 green=22 m3+m4, oracle-correct vs gprolog/`.ref`; van Roy classes closed (findall/forall serialize · assert/retract mid-computation · `is` over runtime-built terms · arity>8).
3. **ONE MACHINE** — the SN4 ζ substrate (Lon 2026-08-21 directive resolves the old cells-vs-zframe A/B): zframe arm, retry/cp/zf3 stacks, pending-cursor apparatus, and their killswitches DELETED; `grep -c 'SCRIP_PL_CELLS\|SCRIP_PL_ZFRAME\|SCRIP_ZD_PL_\|SCRIP_PL_GAMMA_RETAIN' src/` reads 0; the 15 CP/resume globals gone from the census.
4. Gates strict + green: `test_gate_pl_no_new_global.sh` ratchet 14→0 · `test_gate_emit_no_lang.sh` OK · `test_gate_template_medium_invisible.sh --strict` GREEN (remaining: `bb_glue_flat.cpp(4)` + `xa_flat.cpp(8)`) · TEMPLATE-ONLY/BOTH-MEDIUM greps 0 · `handoff_status.sh` COMPLETE.
5. Perf LAST, only under THE INSTRUMENT: emitted-instruction share ≥90% (s154 baseline 55.0%) · ≤1.2× then ≤1.0× GNU on the rail. **Wall time DISQUALIFIED in this container class (s164).**

## THE INSTRUMENT (boards are RUN, never transcribed; re-derive every count fresh)

⭐ **MASTER BOARD BASELINE 2026-09-01 (ceo, SCRIP `8eac17da`, `python3 scripts/corpus_suite_harness.py run corpus/tests/prolog/ALL.pl corpus/tests/prolog/ALL.ref --lang prolog --modes m3,m4`, RT_OPT=-O0, oracle refs = ALL.ref):** `total=371 · m3 pass=218 fail=5 crash=139 xfail=1 xpass=8 · m4 pass=218 fail=5 crash=7 skip=132 xfail=1 xpass=8`. The 139 m3 crashes are ONE class — `signal 6`, stderr `[IBB] FATAL: mode-3 driver: main BB graph not found` on clause-only entries with no main (witness `foo(X,Y) :- X @>= Y.`); the 132 m4 `--compile failed` skips are the same population from the other mode. Row `prolog-directive-only-file-fatals-no-main-bb-graph` re-ranked to 0 on this measurement (37% of the board behind one driver defect). ⛔ SCORE.md's Prolog section at this writing shows smoke/parser/crosscheck only — the master board number was not on the scoreboard (routed to hq_B).
⭐ **MASTER BOARD RE-RUN 2026-09-01 (hq_C, SCRIP `54cf54fd`, binary rebuilt at `43e30f0d` 18:16 after seat02's stale-binary finding, machine load 15–33 with six slice seats' boards concurrent):** `SUITE_BOARD family=ALL total=371 m3_pass=218 m3_fail=5 m3_crash=129 m3_hang=10 m3_unproven=0 m3_skip=0 m3_xfail=1 m3_xpass=8 m4_pass=218 m4_fail=5 m4_crash=7 m4_hang=0 m4_unproven=0 m4_skip=132 m4_xfail=1 m4_xpass=8` (rc=1). Same pass/fail/skip as ceo's baseline; **10 m3 entries moved CRASH→HANG** — under that load a timeout is not evidence of a hang, so those 10 are re-tested individually at low load before anyone rows them; **xpass=8 in BOTH modes** — eight XFAIL markers now pass and are promotion candidates (the converter's hold-out rule applies: promote only a distribution with NO non-PASS arm). Two earlier detached runs of this board died of cross-seat `pkill -f corpus_suite_harness` (seat13 18:19 local, hq_P 18:28; both self-reported) — a board that dies without a SUITE_BOARD line was killed, not red. The per-entry log is the C2 input (`FINDING` to follow when C1 lands).
```bash
bash scripts/test_smoke_prolog.sh                # 5-program tripwire, 3 columns
bash scripts/test_prolog_bb_honest.sh            # counts + exit-gates ORACLE_CRASH
bash scripts/test_prolog_rung_suite.sh           # 164 × interp+compile
bash scripts/test_bench_prolog_modes.sh          # bench-22 board
LD_PRELOAD counts: scripts/util_rtx_count_syms.sh  # THE perf instrument; wall time disqualified
# conversion instruments: SCRIP_ZD_DIAG=1 (authoritative arm/refuse attribution) · SCRIP_ZSM=1 (per-port ring) · the s196 autobug harness · SCRIP_SEAL_DIAG
```
Twin-arm boards ×4 = {default, `SCRIP_PL_CELLS=1`} × {`SCRIP_PL_ZFRAME=0/1`} locate a regression's arm UNTIL PZ-6 deletes the arms. Falsify every zero by injection before believing it.

## ⛔ LAWS THAT BIND EVERY RUNG — INHERITED FROM THE SN4 CONVERSION (ruling SHAs; full list in the FINDING)
Across-the-board admission, never construct-at-a-time (`158738e8`) · failure never whacks — unwind = own-K frees; whack = success/commit brackets only (`c0c77585`); GLUE NEVER WHACKS · one statement at a time, source order (`396d62c7`) · NO per-op filter (`44b8b82c`); no declaration tables — semantics live in THE LATTICE (`18d5bfe1`) · a choice record is read at the depth it was written: depth-immune home or record-relative, never live-rsp (s189) · resume surface = RIGHTMOST box (s190; the `g->all[before]` lowering trap) · tier admission is semantic: β-extends / leftward self-undo / re-yields-same-cursor (s124/s180/HQ-73 `28e2122a`) · derive-don't-accumulate (γ re-enters) · COMMIT-THEN-WHACK · ONE AUTHORITY: offsets only through `x86_frame_off`/`zone_ref`, K only `zd_k`, releases only at the planner choke (s22k spelled-twice disease) · FOUR GREEK PORTS ONLY (`fd16fbea`); γ→β cross-box = 0 (s133, measured over 16,082 edges) · m3 proves NOTHING about m4 — every compiler-process fact needs an emitted-image twin (the seal law, s193/s194) · ⛔ **SEED-FIRST IS RETIRED — LON, 2026-08-24 s273 (see the OVERRIDE banner at the top of this file). Do not gate Prolog work on it.** ~~seed-first: prove a new machine shape as hand-written `.s` against the oracle BEFORE compiler changes~~ (`d5c7a72b`/`1e881ce5`) · ASM-DIFF-FIRST (RULES.md s132) · TEMPLATE-ONLY · BOTH-MEDIUM · TRIPLE-ARM CANARY (every emitter/templates/x86_asm.h commit runs the Icon canary AND this file's smoke+honest BEFORE commit) · killswitch byte-identity + compile-time md5 blast radius; A/B swaps `out/libscrip_rt.so`, not the driver.

## ⛔ STANDING CONDITION — SN4 + ICON GOALS RUN CONCURRENTLY, SAME REPOS
Full text GOAL-ICON-100 §STANDING CONDITION, binding verbatim: separate clones default · `git fetch` before trusting a watermark; pin `git rev-parse HEAD` + clean status to every number · sibling deltas are not Prolog signals until the sibling cursor is checked · shared-emitter commits can move THIS watermark silently (what the TRIPLE-ARM CANARY catches) · verify HEAD + 0-tracked-modified AT MEASUREMENT.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-24 hq_prolog (DECTET) — **LON, IN-CHAT TO THIS SEAT: "Use IPC sync-step monitoring to get Prolog test suites to 100%." FOUR OF SIX PROLOG SUITES NOW READ GREEN ON THE TABLE (INRIA 442/442, GNU 11/11, ProM 563/563, ProBench 23/23); LOGTALK IS THE WORK, SWI STAYS PARKED ON LON'S WORD**

**LANDED THIS SITTING (SCRIP, all pushed; every batch re-proved after its last rebase: ProM 563/563 m3 AND m4, INRIA 442/442 both boards, GNU 11/11, preflight 0 red, the c-allocator and fixed-caps ratchets, the generated GC table FRESH):**
- the ceo's three rank-0 gate reds DONE (cae6bf434 write/print pins re-cut to gprolog 1.6.0; 202c63b0c the atom-name gate's 60 s kill no longer reads as a divergence -- the stressed collector's ~2x CPU went to the cto, who rowed it) and the ceo's format ~Nd overflow row DONE (f765a6fcc).
- INRIA to 442/442 (466e2239e: a double_quotes directive changes how later terms read; per-Parser dq, no global).
- Logtalk runner (825d2ca80): a name imported by `:- uses(lgtunit, [...])` is an alias clause.
- ISO cures, each graded against gprolog 1.6.0 and/or swipl in both modes: round/1 = floor(X+1/2); reader operator priorities (fx fx 1, a = b = c, X = \+ b ... now syntax errors; ** xfx); sort/msort/keysort argument errors and compiled bagof/setof's list guard; op/3 and current_op/3 validation, op(0) removal, user ops shadow built-ins; subsumes_term/2; evaluable_property/2; length/2 errors and termination; =../2 errors and no 64-element truncation; retract/clause logical update view (FOUND WITH THE IPC MONITOR: gpx vs scr diverged at step 8 -- gprolog left clause 1, SCRIP re-entered retract; the inclusive IR_TO bound read one index past the snapshot); call_nth/2; ISO standard order (float before integer) and compare/3's order errors; close/2 options; predicate_property/2 meta templates; stream_property/2 errors, reposition/1, eof_action; current_prolog_flag enumeration, dialect, version_data; big-integer rounding, ^, and number/text conversion (number_codes had silently clamped to 9223372036854775807); ISO ^ rules; the six char I/O builtins' argument checks.
**IN THE WORKTREE, NOT LANDED (batch four): read/read_term keep named _X variables and check options (read_term_3 14 reds -> 0); setup_call_cleanup cleanup checks (9 -> 5); writeq's quoted comma (writeq((a,b)) printed a','b everywhere) and operator spacing (1- -1), {}(X) reads.**
**OPEN, NAMED, EACH A ROW OR AN ASK:** cyclic terms (~25 Logtalk cases; ASKED the ceo whether the runner decides coinduction=unsupported, as gprolog's adapter does); subterm portray for print/write_term portrayed(true) (~15; needs the C writer to call back into Prolog); setup_call_cleanup determinism (5); the runner's brace rewriting inside {Goal} escapes (curly_terms 5); a fast big-integer power (unbounded's four 7^7^7 cases time out); valgrind kills every SCRIP binary in the GC stack walk (routed to the cto). SWI (1051/2935) stays parked until swipl has its own IPC instrumentation (Lon 2026-09-23).

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog latest-of-latest (DECTET) — **THE RECOMPILE TRACE-LITERAL BUG IS CURED (CEO-1208's landing), AND IT WAS NEVER SHARED CODE: BOTH ESCALATED `src/ir/` CANDIDATES ARE GDB-EXONERATED, THE REAL DEFECT WAS A ONE-LINE PROLOG-OWNED DANGLING-POINTER ALIAS**

Row `prolog-monitor-...`, closing the divergence chased across the last three entries below. GDB, not
static reading, settled it: `pl_pred_graph` (`src/lower/lower_prolog.c:1691`, Prolog-owned) did
`const char * trace_key = key;` — an ALIAS of the caller's transient stack string, not a copy. Harmless
for static (`.s`-text) compilation, where the emitter dereferences it synchronously; fatal for a
runtime-JIT-recompiled predicate (any `assertz`/`retract`), whose box embeds the raw pointer for later use
— a dangling stack address once the defining call's frame is popped, read back by the NEXT `rt_trace_call`.
GDB proof: `args[0]`'s DESCR at the `rt_trace_call` breakpoint carried a correct tag/length (`v=2`,
`slen=9`) but a data pointer identical to the exact stack address that had held
`pl_runtime_define_pred_g`'s own `key` parameter moments earlier in the same call. **Both originally-escalated
candidates were checked and cleared by gdb, not assumed clean**: `zls_forget_graph_nodes`'s tables held no
stale entries at the point of the one runtime recompile in the repro, and the JIT box's literal-pool
addressing showed nothing wrong either — this diagnostic sitting's own hypothesis (an incomplete purge
across `frame_layout.c`'s several node-keyed tables) is now RULED OUT, not merely unconfirmed. **Fix**
(one line): alias → `key ? ct_strdup(key) : NULL`, the same durable-arena-copy idiom the file already uses
two lines later for the identical string. No `src/ir/` file touched; CEO-1208's permission grant was never
exercised because the defect turned out to be entirely Prolog's own. Verified: the 3-line repro
(`assertz(counter(1)), counter(C)`) now traces clean in both modes; `make preflight` 59/60 (only the known
cfo allocator gate, unrelated, pre-existing); Prolog master floor unchanged 563/480 (this bug cost no
master-board points, so no SCORE.md rewrite is owed). `.s` artifacts regenerated per handoff policy.
Landed SCRIP `768c896a3` (rebased onto origin as `07efdf8da`), corpus `18db81c9c` (regen), `.github
20b6302e` (rebased as `756af136`, FINDING closed with the full gdb evidence). ⛔ **STILL OPEN, NOT THIS
CURE'S SCOPE:** monitor witness 4 (`scripts/monitor/witnesses/sync_step_prolog_4.pl`) still DIVERGEs at
step 35, but now on an UNRELATED event-kind mismatch (SCRIP emits `CALL counter/1` where the oracle expects
`LABEL`) — no corrupted text anymore, needs its own isolation next sitting. `prolog-assertz-beyond-64-clauses-...`
is untouched (a different defect sharing the same recompile trigger, not this one). **HONEST GAP,
disclosed rather than claimed:** the CEO-757 SNOBOL4-master "owed arm" was conditioned on the cure landing
in shared code; since it didn't, the obligation likely does not apply, but a courtesy check was attempted
and picked the wrong gate (a provenance/deletion-accounting script, an unrelated pre-existing red) — no
real SNOBOL4 correctness board was run this sitting. Receipts sent to cto (reviewer-after-the-fact) and
ceo (closing the CEO-1208 ask), both disclosing this gap explicitly. **NEXT:** isolate witness-4's new
event-kind divergence; otherwise return to mining SWI (1051/2935, the largest remaining home-stretch gap)
and INRIA's last named gap (`double_quotes`-as-directive-form) via the sync-step monitor.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog latest (DECTET) — **THE HOME-STRETCH ROW (Lon 14:1x, "the one task is all test suites to 100%"): LOGTALK RE-RUN, INRIA'S SECOND NAMED GAP CURED, GNU-FD EXCLUDED BY LON'S WORD (CEO-1209) — FIVE SUITES REMAIN, ONE (SWI) FAR FROM DONE**

Row `prolog-all-test-suites-of-the-language-read-100-percent-on-the-suite-table-the-home-stretch`.
**Logtalk** re-run in full on Lon's word (ceo doorbell `lons-word-1350-...`): 2891/3600 → **2935/3600**
at SCRIP `6ab620788` (.github `96dde3452`); identity 2935 pass + 487 fail + 40 outside + 138 ungraded =
3600. **INRIA**'s second named gap (bare-variable clause body via `clause/2` returning the raw variable
instead of `call/1`-wrapped, per ISO/GNU/SWI) is cured: new `$clause_unify` runtime op
(`by_name_dispatch.c` + `rtx_plunify.s` trampoline + `bb_call.cpp` table entry), wired only into
`pl_db_enum`'s `erase==0` (clause/2) path in `lower_prolog.c` — `retract/1`'s `erase==1` path and ordinary
CALL dispatch of bare-var-body dynamic predicates are untouched and verified unbroken. **439/442 → 440/442**
at SCRIP `9f4da0b64` (.github `8ff5c5a8`); the one remaining named gap is `double_quotes`-as-directive-form,
real compiler work, untouched this landing. **GnuFD is OUT of the home-stretch criterion** by Lon's word
14:4x CDT (CEO-1209): its row is removed from SCORE.md/SUITES.tsv, `ANNOUNCEMENT.md` carries the exclusion,
`prolog-gnu-fd-thirty-...` parks on it. **THE CRITERION NOW GRADES FIVE SUITES: ProM 563/563 (done) ·
GNU 11/11 (done) · INRIA 440/442 · Logtalk 2935/3600 · SWI 1051/2935 (untouched this sitting, by far the
largest remaining gap).** DONE-WHEN is `scripts/util_suite_table_language_verdict.py prolog 0f6724c5b`
(confirmed on origin, SCRIP `202b61814` — an earlier search this sitting ran unfetched and wrongly reported
it missing; withdrawn). ⛔ **PROVENANCE HAZARD, worth every seat knowing (coo's row
`instruments-a-board-does-not-refuse-when-its-own-seat-rebuilds-the-binary-under-it` owns the cure, cite
there, no new row):** a concurrent `git pull --rebase` in this SAME root moved HEAD mid-board during the
Logtalk run, before the first pass's provenance stamp was taken; the board did NOT refuse. Re-run standalone
on the clean, final HEAD independently reproduced the identical 2935/3600, so the number on record is
correct, but the class of hazard is real and silent. **Per CEO-1209: no pull, no make, no dispatch verb in
a root while its own board runs (CLAUDE.md THE LOOP 1).** Separately, this sitting also diagnosed and
escalated the assertz/retract recompile-trace-literal corruption (entry below) — CEO-1208 has since
authorized landing it wherever it falls, permission-step retired (CEO-801); isolation of the shared
`src/ir/` candidates (`zls_forget_graph_nodes` and neighboring node-keyed tables in `frame_layout.c`) is
in progress, next sitting's `## NEXT`. The 11-site allocator/preflight-red in `by_name_dispatch.c` is the
cfo's cure row (CEO-1208); this seat reviews after, does not touch `wot_open`/`wot_close`/`wot_discard`.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog later (DECTET) — **LON'S WORD ROUTED CEO-1200 ("bring your language to 100% through the IPC sync-step monitor, every suite"): SAME ROW, PICKED UP THE OPEN DIVERGENCE — THE PRIOR SITTING'S ζ-SPINE-DEPTH HYPOTHESIS IS DISPROVEN, THE REAL TRIGGER IS RUNTIME JIT RECOMPILATION OF A DYNAMIC PREDICATE CORRUPTING ITS OWN NEXT TRACE LITERAL, AND TWO OF THE THREE REMAINING CANDIDATES ARE SHARED `src/ir/` MACHINERY — ESCALATED TO ceo, NOT LANDED BLIND**

Row `prolog-monitor-the-instrumented-gprolog-oracle-is-completed-and-used-...`, continuing from the entry
below. ASM-diff-first + gdb this sitting (SCRIP `b7b923d1c` throughout, **no source touched** — a pure
diagnostic landing). Disproved the prior sitting's guess that the divergence needed BOTH `retract` and
`assertz` present with a caller-side ζ-SPINE depth mismatch: `assertz` ALONE, immediately followed by a call
to the SAME predicate in the same clause body, reproduces it on a new 3-line repro; asserting into an
UNRELATED predicate then calling a different, never-recompiled one is clean; a plain `between/3` generator
before the call is clean. **gdb on `rt_trace_call`/`rt_trace_stmt` (mode-4 standalone binary) shows the
corrupted trace-name argument is a live STACK ADDRESS, not the static literal address it should carry** —
the freshly RUNTIME-JIT-RECOMPILED predicate's own box (`pl_db_store` → `rt_pl_db_recompile`
`unification.c:2297` → `pl_runtime_define_pred_g` `lower_prolog.c:1747`, the same recompile-on-every-assertz
path already named by `prolog-assertz-beyond-64-clauses-...`) corrupts ITS OWN next trace literal, not the
caller's. Exonerated with the evidence named: `CALL_PROC_STAGED` emission (byte-identical asm between
working/broken compiles); the generator-flag registration (`lower_prolog.c:1635` and `:1773` both hardcode
generator=1 at load time AND at recompile time — no mismatch); `bb_pool_init()` reset (a lazy, idempotent
no-op on a second call, `src/ir/bb_pool.c` read in full); `bb_pool_mark`/`bb_pool_release` rollback (Prolog's
recompile path calls neither). **Three candidates remain unisolated** — a save/restore bug in
`pl_runtime_define_pred_g` itself (Prolog-owned, landable directly) vs. `zls_forget_graph_nodes`
(`src/ir/frame_layout.c:818`, shared, also called from SNOBOL4's `runtime_eval.c:221`) vs. something specific
to how a JIT-emitted box addresses its own literal pool (also `src/ir/` territory). **Two of the three cross
into shared `src/ir/` JIT/frame-map machinery this seat does not land solo** per CLAUDE.md's shared-node rule
("a shared box ... is an ASK to your officer with the measurement, base-vs-head gate by gate — never a
landing"). Full writeup, the new minimal repro, and the isolation plan for next sitting:
`.github/findings/FINDING-2026-09-23-hq_prolog-a-call-immediately-after-retract-assertz-on-the-same-dynamic-predicate-corrupts-the-next-traced-call-name-and-the-following-stmt-line.md`.
**ASK sent to ceo** (topic `ask-prolog-assertz-retract-recompile-corrupts-own-trace-literal`) with this
measurement, per the standing non-blocking rule this seat continues other Prolog-lane work meanwhile rather
than holding the row idle on the answer.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog (DECTET) — **THE GNU-PROLOG MONITOR ROW (HANDED OFF BY THE ceo, CEO-1190): WITNESS 4 MOVED DIVERGE 20 → 35 (double_quotes NOW DEFAULTS TO codes, ISO/GNU-CORRECT, NOT atom) — AND THE NEW DIVERGENCE POINT IS A REAL, PREVIOUSLY-UNKNOWN SCRIP BUG, INDEPENDENT OF THE MONITOR, NOT THE CONTRACT GAP THE BATON GUESSED**

Row `prolog-monitor-the-instrumented-gprolog-oracle-is-completed-and-used-...`. The ceo built and handed off
the instrumented GNU Prolog 1.6.0 fork (SCRIP `4a9d5a698`) with three witnesses AGREEing and a fourth
DIVERGEing on a real SCRIP-side gap, per its own baton. This sitting cured the baton's item 1:
**`double_quotes` compiled DEFAULT was `atom`; ISO and real GNU Prolog both default it to `codes`.** Fixed
in `by_name_dispatch.c`'s `pl_flags` table (the `set_prolog_flag(double_quotes,...)` DIRECTIVE form stays
refused exactly as before, `lower_prolog.c:1862`, rung 10 — untouched, a separate and harder gap). Flipping
the default exposed that several SWI-style text builtins (`string_concat`, `atom_string`, `number_string`,
`string_length`, `atomic_concat`/`atom_concat`/`upcase_atom`/`downcase_atom`/`atomic_list_concat`) only
accepted atoms in `unification.c`'s `plc_atom_op_text` and had no codes/chars-list fallback — added one
(`plc_atom_op_list_text`, mirrors the existing atom_chars/atom_codes list-decode already in
`rt_pl_atom_op_cell`), a pure widening, no output-representation change. SCRIP `ceb95d59d`.

**REGRESSION CAUGHT AND CURED BEFORE LANDING, NOT AFTER (CEO-589 discipline):** the default flip cost the
master board one entry in both modes (563→562, 480→479) — `fact_string_arg_1` (`label("hello").` /
`write(S)`). Diagnosed as a STALE REF, not a new bug: it was cut from SCRIP's own prior (non-ISO)
atom-default output, never from the oracle. Real GNU Prolog 1.6.0, run directly this sitting
(`gprolog --consult-file`, banner/consult noise stripped), prints `[104,101,108,108,111]` for the identical
3-line program — byte-identical to SCRIP's corrected output. Re-cut the ref (corpus `9cafa02ed`) rather than
reverting the compiler fix, per "a ref is cut from the oracle, never from our output."

**FULL VERIFICATION, ON THE POST-REBASE MERGED TREE** (SCRIP `39c36da57`, corpus `a1da90fc8`, after
`git pull --rebase` moved both repos 7 commits and the gate was re-proved, not just re-quoted): master board
**563/563 m3, 480/480 m4** (back to full green); `test_prolog_ladder.sh --to 18` **568/568** unchanged;
GNU suite **11/11** unchanged; INRIA outcome-class **442/442**, bindings **439/442** unchanged (same two
pre-existing named gaps — the double_quotes DIRECTIVE and the `clause/2` body `call/1`-wrap — neither
touched by this landing).

**THE NEW DIVERGENCE (witness 4, step 35) IS NOT THE BATON'S GUESSED CONTRACT GAP.** The baton's own
`## NEXT` (before this sitting) predicted the next divergence was "the oracle side does not instrument
dynamic/multifile/public predicates" — a real, separate, still-open gap, but not what fires here. **Measured
instead: a CALL into a dynamic predicate immediately after `retract`/`assertz` on that SAME predicate,
within the same clause body, corrupts the next traced CALL's name and the following STMT's line number** —
reproduces in bare `--trace --run`, zero monitor/oracle involvement, on a 4-line witness
(`:- dynamic(counter/1). counter(0). main :- retract(counter(N)), M is N+1, assertz(counter(M)),
counter(C), write(C), nl.`). Calling `counter/1` WITHOUT a preceding retract/assertz in the same body traces
correctly. Full repro, what was ruled out (the trace-name literal mechanism, the double_quotes/string-ops
changes just landed, `intern_name_bin`), and a working but UNPROVEN hypothesis (a ζ-SPINE depth/slot
mismatch around the `$db_erase`/`$db_assertz_r` `IR_CALL` nodes) are in
`.github/findings/FINDING-2026-09-23-hq_prolog-a-call-immediately-after-retract-assertz-on-the-same-dynamic-predicate-corrupts-the-next-traced-call-name-and-the-following-stmt-line.md`.
**NOT attempted as a cure this sitting** — ASM-DIFF-FIRST needs the `.s` diff between a direct dynamic-call
witness and the retract/assertz-then-call witness before touching codegen; a guessed frame-slot fix is
exactly the class CEO-589 warns against landing blind. **Next sitting: do that ASM diff, find the actual
slot/depth mismatch, cure it, re-run `monitor_run.sh` on `sync_step_prolog_4.pl` and expect the divergence
to move past line 12** (then item 3, `safe_div`/`catch/3`, then item 4, sweeping the Prolog reds through the
monitor per the row's own USE section — none of that reached yet).

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog (DECTET) — **LON'S WORD: PROLOG USES THE IPC SYNC-STEP MONITOR TECHNIQUE GOING FORWARD — BUT THE MEASURED STATE IS IT DOES NOT EXIST FOR THIS LANGUAGE YET, AND THE SHARED LAYERS ARE hq_icon's ROW TO LAND FIRST; INRIA MEANWHILE MOVED 432/445 → 439/442 (harness bugs fixed, not compiler bugs) WITH TWO REAL GAPS LEFT NAMED**

**LON'S WORD, routed by ceo (CEO-1174), 2026-09-23 02:0x CDT, verbatim: "I've directed Prolog, Pascal, and Raku to begin using IPC sync-step monitor technique moving forward to find and fix bugs."** Recorded here per the standing law that I am the only witness to what Lon told me directly and the chat is not the record.

**THE MEASURED STATE (ceo CEO-1175/1176/1177/1178, same session) NARROWS THAT SENTENCE:** the IPC sync-step monitor exists for SNOBOL4 ALONE today — its wire event (`bb_stmt_mark` → `IR_STMT_MARK`) is emitted by `lower_snobol4.c` only, and Prolog (like Icon/Pascal/Raku) reads zero statement-mark nodes on `--dump-ir`. Lon then directed (CEO-1176) **ONE shared sync-step IPC design for all seven languages** (`MONITOR-BINARY-DESIGN.md`, on origin) — six layers, five of them ONE implementation shared by every language (statement/line/call event emission, the wire, the controller, the map-builder-with-a-per-language-plug, the harness); only the sixth (the peer arm: m3-vs-m4 self-comparison first, an oracle bridge such as swipl only if it earns its cost) is per-language. **"A per-language copy of layers 1 to 5 is reverted on sight."** The officers (cto/cfo/coo) are stood down (CEO-1178); the shared layers — the three runtime hooks (LINE/ASSIGNMENT/ENTER-EXIT per CEO-1177), the map builder, the harness — are now **hq_icon's row**, not mine. **STANDING INSTRUCTION TO ME: do NOT mint my own monitor row (plug + peer arm) until hq_icon's plug interface lands on origin** (CEO-1176/1178, explicit to all four remaining HQs). Until then, **ASM-DIFF-FIRST is the order and a monitor-only verdict does not exist for Prolog** — unchanged from RULES.md. Next sitting: check whether hq_icon's plug interface is on origin before minting anything; if it is, my row is the three hooks fired from Prolog's own boxes (naming any Prolog construct that resists hooking, never skipping it silently) plus the peer arm, referencing `MONITOR-BINARY-DESIGN.md`.

**INRIA, WORKED WHILE WAITING ON THE ABOVE:** SCORE.md read 432/445 (bindings board), graded 2026-09-21. Investigated all 13 reds by reading the actual vendored suite text + `inriasuite.pl`'s own driver, not guessing. **9 of 13 were this runner's own bugs, not scrip's:** the single-first-comma split silently folded a 3-element `[Goal, AuxFile, Expected]` entry's aux hint into `Expected`, minting a fabricated expected-error-functor out of thin air (`t_foo`, `in`) for the `t` family and all 3 `file_manip` entries; `current_input`/`current_output`/`current_predicate(run_tests/1)` needed `inriasuite.pl`'s own `exists/1`/`reset_flags/0`/`make_list/2` helpers, never consulted by this standalone-per-goal runner. Fixed all of it in the runner (`scripts/test_prolog_inria_suite.sh`, SCRIP `2847fa498`) — parses 3-element entries correctly now (inlines `t_foo.pl`'s 4 facts as `assertz/1`, since `consult/1` itself is a separate known gap), carries the suite's own harness prelude verbatim. **2 more (atom_codes/number_codes, code 1000) and 1 more (functor-bis Y<--2-vs-3) are proven vendored-suite errata** — live swipl+gprolog oracle runs this session (not argued) show gprolog's Latin-1-era 255-code cap disagreeing with swipl's full-Unicode reading on the exact witness goals; scrip already sides with swipl/Unicode (consistent with its own existing `pl_anum_code_ok` 0x10FFFF bound), and the functor-bis cell contradicts its own `%` comment exactly like the three ceo-370 entries already on this row. Added to `OUTCOME_ERRATA`/`KNOWN_SUITE_ERRATA` with the measured oracle split named per entry. **file_manip's 3 entries stay OUTSIDE THE BASELINE (CEO-749)**, not forced to a fabricated pass: they need `run_forest`/`bips-ex`, from `prologsuite.tar.gz`, which `PROVENANCE.txt` shows was never fetched (only `inriasuite.tar.gz` was) — denominator 445 → 442, stamped via a new `--criterion-changed` passthrough added to `gate_score_row` (`lib_gate.sh`, additive/backward-compatible, a shared-node touch landed directly per CEO-1178's after-the-fact-review standing).

**TWO GENUINE GAPS LEFT, NAMED, NOT PATCHED BLIND:**
- `set_prolog_flag(double_quotes, codes|chars)` as a directive is REFUSED outright — `lower_prolog.c:1862`'s own message calls it "rung 10", an explicitly-scoped, not-yet-built ladder feature (double-quoted literals are compiled to an atom unconditionally today; codes/chars needs the lowering pass to track directive-ordered flag state across top-level clauses and reinterpret each `"..."` literal's already-parsed text accordingly — real compiler work, not a bug fix). Confirmed via live swipl oracle (structured as separate sequential directives, matching how `inriasuite.pl`'s own `read_test`/`loop_through` reads one term at a time) that the suite's expectation is ISO-correct, not suite error.
- `asserta((bar(X):-X)), clause(bar(X),B)` should give `B <-- call(X)` (confirmed: swipl AND gprolog both wrap a variable clause body in `call/1` on retrieval; scrip currently returns the bare unbound variable). **ATTEMPTED AND REVERTED THIS SESSION** — wrapping at `pl_db_store` (`unification.c`) looked like the obvious fix but that stored representation is the SAME one `rt_pl_db_clause_at` feeds to ordinary dynamic-predicate CALL dispatch, not just `clause/2`'s introspection: the wrap broke `bar2(true)`-style calls to asserted clauses in m3 (`existence_error(procedure,?/0)`, a corrupted atom name) and **crashed m4 outright** (`zls: vslot table overflow`, aborted). Reverted clean (`git diff` empty before rebuild). CEO-589 held: never trade a working program class for one INRIA point. The real fix is retrieval-time-only, inside `clause/2`'s own lowering (`pl_clause_target`/`pl_db_enum` in `lower_prolog.c`) or a dedicated leaf, never touching the shared clause-storage/dispatch path — next sitting's task, or a good candidate for ASM-DIFF-FIRST once picked back up.

**BOARD:** outcome-class 442/442 (0 fail, 3 OUTSIDE), bindings 439/442 (3 fail: the two gaps above, double_quotes contributing 2 of the 3). SCRIP `292e38dfc`, corpus unchanged. Re-run and SCORE.md row confirmed this sitting.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-23 hq_prolog (DECTET) — **TWO OF THE THREE MASTER-BOARD REDS CURED: m3 562/563, m4 479/480 (denominator unchanged -- ALL.csv's own declared modes) — ONE NAMED RED LEFT, THE bagof/setof GROUPING GENERATOR**

Landed SCRIP `af74eb56e` / corpus `fc07b7113`, pushed, both re-proved on the merged tree after two rebases (other seats' concurrent GC-chunk landings in flight throughout, none touching either file).

**catch_throw_directive_replace_1 CURED.** The Prolog clause-retry trampoline (`emit.cpp`'s `pl_step_lbl`, one emission shared by m3 and m4) unwound the trail and jumped straight to the next clause alternative on ANY `DT_FAIL`, with no check for an armed exception ball (`r15`) — so a `throw/1` from a non-last clause was silently treated as an ordinary clause failure and the next clause ran instead of the exception reaching `catch/3`. The box's OWN external-redo entry point (`lbl_β`) already carried the right guard (`test r15,r15; jne ω`) forty lines below — it just never got copied to this path. Fix: the identical guard, added to `pl_step_lbl`; on a pending ball, restore the outer choice barrier (mirroring the last-alternative trampoline's own r13 restore) and concede straight to ω instead of retrying. Confirmed via ASM dump the branch is reachable ONLY from Prolog graphs (`n_alts` is set by no other lowerer) — no SHARED-NODE re-grade owed, though the full master board was re-proved anyway.

**list_directive_4 CURED, and it was genuinely ORACLE-DIVERGENT, not a bug — confirmed against BOTH real oracles, not argued.** `list_directive_4` and `write_family_print_1` want opposite `print/1` quoting with no flag in either source to distinguish them. Measured directly: GNU Prolog 1.4.5 prints UNQUOTED (byte-identical to `list_directive_4.ref`), SWI 9.0.4 prints QUOTED (byte-identical to `write_family_print_1.ref`). Per Lon's word this session, GNU is canonical for `print/1` here. Retargeted `print/1`→`$write`, `print/2`→`$write_s` (was `$writeq`/`$writeq_s`) in `lower_prolog.c`. Surveyed every OTHER `print(/1,2)` call across master/SWI/GNU/Logtalk (INRIA has none) before touching it: all plain lowercase atoms/numbers/compounds, none quote-sensitive — nothing else traded (CEO-589 held, empirically, not just by intent). `write_family_print_1.ref` re-cut (corpus `fc07b7113`) from GNU's own clean unquoted output (`gprolog --consult-file <file> < /dev/null`, banner/consult noise stripped, verified byte-identical to the paired compiler change's own output).

**bagof_setof_free_variable_grouping_1 NOT ATTEMPTED THIS LANDING — genuine feature gap, not a bug.** `bagof/setof` over a free variable need to group solutions by that variable's binding and backtrack across each group as a real nondeterministic generator; `prolog_lower.c`'s tree-rewrite already calls `$bag_group`, nothing implements it (`existence_error(procedure,$bag_prep_b/2)`). Needs new choice-point-machinery wiring, not a leaf function — next row, not yet designed.

**ALSO CLOSED THIS SESSION, SAME BATON (`prolog-gc-the-prolog-share-of-the-unmapped-slot-population-censused-by-name-and-the-master-clean-at-one-megabyte`):** its own DONE-WHEN re-run VERBATIM, clean, on SCRIP `4a1cecc84`/corpus `6e6e2e662` (pre-dates this landing's two cures): census `unkinded=0 holes=0`, `split NEVER-EMITTED=0 EMITTED-NO-LAYOUT=0 UNMEASURED=0` (no_layout is now 0, not 10 — the five ladder surfaces from `6a6983d9e` closed that population entirely), `test_gate_pl_master_is_clean_at_the_tiny_arena.sh` GREEN — divergent=0 on every axis (arena + stress {2,5,16,25}), 1043 pairs each arm, zero inconclusive, zero vanished. ⛔ Formal `s4e_msg.sh done` closure on the postoffice side still OWED, deliberately deferred: `done` re-runs this same ~70-minute gate a second time to verify, and this seat already had the identical receipt in hand from the run above on the same pinned tree — paying for it twice in one sitting before landing the two cures this block describes was the wrong order of work. The measurement above is real and reproducible (rerun it: `test_gate_pl_master_is_clean_at_the_tiny_arena.sh` on SCRIP `4a1cecc84`), not a placeholder; next hq_prolog sitting closes the postoffice row from it rather than re-measuring cold.

**BOARD, PINNED: SCRIP `5b6ebef7c` / corpus `411816b16`**, both modes, re-proved identically across two post-rebase rebuilds (16-shard `corpus_suite_harness.py run`, both times). `m3: 562/563` (one red: bagof_setof). `m4: 479/480` — 480 is m4's OWN declared-modes population per `ALL.csv` column 8, not a regression: 362 entries declare `m3,m4`, 118 `UNKNOWN` (parse-only, defaulted to both), 118+362=480; 83 declare `m3`-only and are excluded from m4's denominator by the corpus's own column, unrelated to and unmoved by this landing.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-22/23 hq_prolog (DECTET) — **THE EXCEPTION-BALL ROW IS CLOSED (cfo's cure verified, 0/216 divergent) AND THE MASTER BOARD MOVED 543 → 560 OF 563, BOTH MODES, ZERO REGRESSIONS — THREE NAMED REDS REMAIN, EACH DELIBERATELY LEFT, NOT MISSED**

**HALF TWO OF THE GC-CENSUS ROW IS NOW FULLY CLOSED.** cfo's exception-ball cure (SCRIP `5fc6ae1bc`, trail-header slot at `PL_TR_BALL_OFF`) verified 0/216 divergent across the 27 exception-shaped master entries; that row is DONE. This seat's OWN re-measurement of it caught three of its own instruments gone stale in one sitting — the tiny-arena gate's `SCRIP_HEAP_MB=1` pin had gone stale under CEO-1095 (128 KB compiled default; 1 MB is now 8x looser, collecting 13x less), its `STRESS_TIMEOUT` had no headroom for `benchmark_queensn` under real machine contention, and the census format changed under CEO-1025's own discriminator without the baton's grep keeping up. All three fixed and landed (SCRIP `1e6569c34`), and the corrected 6-arm sweep reads **divergent=0 on every axis** — arena, stress:{2,5,16,25} — control now an explicit loose `SCRIP_HEAP_MB=512` pin, arena/stress left UNSET so they track whatever "tiny" means going forward with no maintenance owed here.

**THE MASTER BOARD ITSELF, ON LON'S DIRECT WORD (CEO-1129, "each HQ owns its own language," per-HQ order of work is "their own suites to 100%"):** started at 543/563 both modes. Landed (SCRIP `41631ffa1`), verified individually and on the full board, in BOTH modes:
- `truncate`/`round`/`ceiling`/`floor` on a plain integer wrongly required a float (bignums already treated them as identity)
- `bounded` flag defaulted false; GNU Prolog's real default is true (verified against `flag_c.c`, not guessed)
- `at_end_of_stream/1` used the existence-only stream resolver instead of the permission-checking one `get_char` etc. already use
- `atom_to_term/3` silently accepted an unbound Atom instead of `instantiation_error`
- `current_op/3` had no prefix-operator table at all — added the standard ISO set, introspection-only
- `unify_with_occurs_check/2`, `display/1,2` did not exist — wired end-to-end (new dispatch leaves, the `bb_call.cpp` symbol table, the `rtx_plunify.s` `PL_CTX_LEAF` trampolines)
- DCG pushback (`Head, Push --> Body`) unified the wrong pair (inverted), so a nonempty pushback could never surface
- `aggregate_all/3` (count/max/min/sum/bag/set) — findall + a new `$aggregate_reduce` leaf, `set` reuses existing `sort/2`
- `b_setval/2`/`b_getval/2` added — `b_setval` reuses the existing `pl_tr_push`/trail-unwind machinery generically, no new undo mechanism
- `set_prolog_flag/2` as a directive was refused for any non-default flag name at all — narrowed the refusal to the one case that's genuinely unimplemented (`double_quotes` → codes/chars, a real parse-time effect); directives now route through a new auto-vivifying `$set_prolog_flag_declare`, the ordinary runtime goal stays strict
- `read_term/2,3` only handled a literal `[]` options list — reused the options-processing already proven for `read_term_from_atom/chars/codes`
- `with_output_to/2` added via tree rewrite (memstream-backed capture, `call(Goal)` once) plus `write_to_atom/2`, `format_to_atom/3`, `read_from_atom/2`
- `rt_pl_root_omega` exited 1 on an ordinary top-level goal failure; a sibling landing had already fixed the exception case (0→2, matching driven-to-completion swipl) and named the failure case as a known follow-up. Census of `ALL.wantrc`: zero entries declare rc=1 anywhere in this suite. `exit(1)`→`exit(0)`, full board re-proved with zero regressions from this one change.

⛔ **print/1,2 WAS TOUCHED AND REVERTED IN THE SAME SITTING — RECORDED BECAUSE THE TRAP IS WORTH NAMING.** `list_directive_4` wants `print('hello world')` unquoted; a SEPARATE, ALREADY-PASSING witness (`write_family_print_1`) wants `print('needs quotes')` QUOTED — two structurally identical cases, this corpus's own refs disagreeing with each other. Repointing `print` to unquoted fixed the first and broke the second. **Reverted: never trade a working entry for a broken one (CEO-589).** `list_directive_4` is left with that one named, irreconcilable line — not a miss, a corpus contradiction with no single-implementation resolution.

⛔⭐⭐ **TWO REDS NAMED AND DELIBERATELY NOT TOUCHED, BOTH FOR THE SAME CLASS OF REASON — TOO SHARED, TOO ARCHITECTURAL FOR ONE SITTING WITHOUT ASM-DIFF-FIRST + gdb.** `bagof_setof_free_variable_grouping_1`: `prolog_lower.c`'s tree-rewrite path (`pb_expand_bagof`) already calls `$bag_prep_b`/`$bag_prep_s`/`$bag_group` — the SORT/DEDUP half (`rt_pl_bag_prep_cell`) exists and works, but `$bag_group`'s job is genuine NONDETERMINISTIC backtracking over distinct key-groups, which needs a real generator in the shared Byrd-box choice-point machinery, not a leaf function. `catch_throw_directive_replace_1`: a thrown exception from a NON-LAST clause of a multi-clause predicate backtracks into the next clause instead of propagating — confirmed NOT specific to variable-goal dispatch (reproduces on a direct `catch(risky(99), _, ...)` call too). Root cause: **r15 is BOTH the choice-point register and the exception-ball carrier** in the Prolog register plane (the same finding as the exception-ball row above, now shown to have a SECOND consumer than just GC-rooting) — the ordinary clause-alternative retry path has no `$ball_pending`-style check before trying the next clause, and everything that WOULD need to check it is the most load-bearing, most-executed code in the whole language.

**BOARD, PINNED: SCRIP `6a6983d9e` / corpus `cb2e2d49e`, pushed. `SUITE_BOARD family=ALL total=563 m3_pass=560 m3_fail=3 m4_pass=560 m4_fail=3`, re-proved three times across three rebases before landing, identical every time.**

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog (TENET, latest) — **THE BALL LIVES IN r15 AND NOTHING IN THIS TREE ROOTS A REGISTER — I CORRECTED MY OWN DIAGNOSIS IN THE BLOCK BELOW, AND THE REAL CAUSE IS A REGISTER THAT MEANS A POINTER IN ONE LANGUAGE PLANE AND AN INTEGER IN ANOTHER** (row `prolog-gc-the-exception-ball-is-not-rooted-...`, rank 0, handed to the cfo with the measurement)

⛔⭐⭐ **THE DIAGNOSIS IN THE BLOCK BELOW IS SUPERSEDED BY THIS ONE AND A READER MUST NOT CHASE IT.** I had written that the cause was `rt_pl_compound_cell` holding `blk` in a C local across `prolog_atom_intern` and a second `PL_CELL_ALLOC`. **That hazard is real, but it is not this defect and it is far too narrow to explain the population** — it is a window inside ONE C function, while the actual window spans the ENTIRE throw-to-catch recession. That is what makes the whole exception family move together while nothing else in the master moves at all.

**THE CHAIN, EVERY LINK CITED, NONE INFERRED, ON SCRIP `81c00a5be`:** `rt_pl_ball_make` (`src/runtime/unification.c:1743`) allocates the ball with `PL_CELL_ALLOC` and deep-copies the thrown term into it → `PL_CELL_ALLOC(n)` is `rt_ws_alloc_descr(...)` (`unification.c:19`), **THE COLLECTED ARENA**, so the ball is a heap block and, being a COPY, a term the program still holds in a frame slot is a DIFFERENT block that does not protect it → `rt_pl_throw_raise` (`src/runtime/rtx/rtx_plunify.s:37`) is `call rt_pl_ball_make; mov r15, rax` and returns `DT_FAIL` → the machine RECEDES through arbitrarily many boxes, each free to allocate and to poll → `rt_pl_catch_handle` (`rtx_plunify.s:78`) reads `r15` back, and `rt_pl_ball_take` (`:102`) is `mov rax,r15; xor r15d,r15d`. ⛔ **BETWEEN THE THROW AND THE CATCH THE BALL HAS NO ROOT.** `pl_gc_roots` (`src/parsers/prolog/prolog_atom.c:97`) visits exactly the atom hash table and the atom name vector; the Prolog trail's topword root range is walked at `gc_heap.c:1081-1082` starting at `lo + 32` over 32-byte trail entries, visiting `cell` raw and `old` typed. **Neither sees a register, and a grep of the tree for anything that roots r15 returns nothing. It is a lost root BY CONSTRUCTION, not by accident.**

✅ **MEASURED, NOT ARGUED — THE ROW'S OWN DONE-WHEN ON `81c00a5be`, TREE CLEAN: 22 OF 34 GRADINGS WRONG.** WRONG at stress 1,2,3,5,10,25,26,30,35,45,48; CORRECT at 0,20,40,50,60,100. ⛔ **1 MB AND 512 MB FAIL IDENTICALLY AT EVERY ONE OF THOSE POINTS** — the arena column is not a weak discriminator here, it is **CONSTANT across the entire band**. That is arena-insensitivity measured in this lane rather than quoted from hq_snobol4's, and it is the second independent confirmation that an arena-only arm reads green over the whole class.

⛔⭐⭐⭐ **THE FINDING THAT OUTRANKS THE ROW: A REGISTER MEANS DIFFERENT KINDS IN DIFFERENT LANGUAGE PLANES AND THE SHARED GC MECHANISM HAS NO WAY TO SAY SO.** In the Prolog plane `r12`=trail, `r13`=choice/barrier, **`r15`=the exception ball, a collected-heap POINTER**. In the SNOBOL4/Icon plane `r13`=subject pointer and **`r15`=subject LENGTH, an integer** — pinned by `PIN_SUBJLEN_REG` with a `static_assert` at `x86_asm.h:357-362`. The tree's ONLY register shield, ARCH-GC 6.5's `x86_rt_gc_poll_rec_sigma` (`x86_asm.h:2237`), is spelled `x86_rt_gc_poll_rec1("r13", "r15d", keep_rax)`: **r13 the pointer, r15 a 32-bit LENGTH.** So the one mechanism that could shield a callee-saved register across an allocating return **hard-codes the SNOBOL4 reading**, and using it in a Prolog graph would hand the collector the ball **truncated to 32 bits and tagged as a length.** **There is no per-language register kind anywhere in the GC's vocabulary.** The Prolog symptom is one instance of that hole, which is why this is an ASK and not a landing.

**THREE CANDIDATE CURES, NAMED WITH THEIR LANE SO THE OWNER PICKS RATHER THAN INHERITS A PREFERENCE.** **(A) ζ-STANDING SLOT** — the ball's lifetime spans the recession, which **is** ζ-STANDING under the s273 ruling at the head of this file; a ζ slot is a DESCR on RBP covered by the per-frame static map, so the collector visits it **typed** with no new mechanism at all. Architecturally the right answer; cost is a real Prolog-machine change that touches emission. **(B) A PER-LANGUAGE REGISTER KIND IN THE SHIELD RECORD** — smallest diff, but `x86_asm.h` is a shared node and SHARED-NODE VERDICT SCOPE means grading every frontend that reaches it. **(C) A ROOTED PROLOG-OWNED CELL** visited by the existing `pl_gc_roots` hook with r15 demoted to a pending flag — entirely inside Prolog's files and needing no collector change, **but it requires C-visible storage, i.e. a NEW GLOBAL, forbidden without Lon's explicit in-chat permission that session.** None was added and none is being asked for until an officer says (C) is the shape. ⛔ **Nothing shared was touched: not `unification.c`, not `rtx_plunify.s`, not `gc_heap.c`, not `x86_asm.h`.**

⛔⭐ **THREE PROBES THAT LOOK LIKE EVIDENCE AND ARE NOT — RECORDED BECAUSE THE NEXT SEAT WILL BUILD THE SAME ONES.** An **atom ball vs compound ball** pair (`throw(foo)` green at 0/1/2/5, `throw(f(1))` red at 1/2/5) reads as "the heap block is reclaimed, the atom is not" — **but the two programs differ in ALLOCATION COUNT, and `SCRIP_GC_STRESS` counts allocations, so they do not even collect at the same points.** A **control holding the same term in a frame slot** (`X = f(1), catch(throw(X), f(1), ...)`) fails identically, which reads as "a mapped slot does not protect it" and is really just the ball being a **deep copy**. And the **bare-variable catcher prints `got(f(1))` correctly at stress 1 and 5**, which shows only that no collection landed in that program's window. **All three are consistent with the diagnosis; none establishes it.** What establishes it is the code chain above, which needs nothing run at all. ⭐ **The general form: when the knob you vary also changes the thing that decides where the effect lands, a clean-looking A/B is not a controlled experiment.**

✅ **AND HALF ONE OF THE CENSUS ROW IS CLOSED IN THIS LANE'S FAVOUR BY CEO-1025**, which took this seat's fork and made it bind on every language: `no_layout` **conflates two populations with opposite owners** — (a) the compiler refused the entry, nothing emitted, no leaf boundary and no store, a **COMPLETENESS DEBT** declared with its rung, inside the printed denominator per CEO-749, **not gating**; (b) the graph emitted and the planner gave it no layout, which is the cfo's STORED-INTO-A-MAPPED-SLOT clause and **does** gate. **Prolog's ten are all cause (a), all one cause** — `b_getval` x2 (rung 10), `set_prolog_flag` x2 (rung 10), `aggregate_all` x3 (rung 8), `read_term` x2 (rung 6), `with_output_to` x1 (rung 10) — so they belong **on the ladder, never in the collector**. ⛔ **Half two is red and it gates: COMPLETENESS DOES NOT OPEN IN PROLOG**, and this lane's earlier `divergent=0` over 1043 pairs **must never be entered in a condition-1 tally as a clean** — both its arms sat at stress 0, where the collector does not run.

---

## ⛔⭐⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog (TENET, SUPERSEDED DIAGNOSIS — see the block above) — **PROLOG IS NOT CLEAN: THE EXCEPTION BALL IS NOT ROOTED ACROSS A COLLECTION, 27 MASTER ENTRIES SILENTLY RETURN A WRONG ANSWER AT EXIT 0, AND THE DEFECT IS NEITHER ARENA-DEPENDENT NOR ON A THRESHOLD** (row `prolog-gc-the-exception-ball-is-not-rooted-...`, rank 0, escalated to the cfo)

⛔⭐⭐⭐ **CONDITION 1 IS ANSWERED AND THE ANSWER IS NO. COMPLETENESS DOES NOT OPEN IN PROLOG.** Measured on SCRIP `3030deb2a` with `scripts/test_gate_pl_master_is_clean_at_the_tiny_arena.sh` — four arms, one pinned binary, 16 shards each, all four recording **1043 pairs, vanished=0 inconclusive=0**: `arena` divergent **0** · `stress:5` divergent **54** · `stress:25` divergent **20**. **74 (entry,mode) pairs over 27 distinct entries; the 17 at stress 25 are a strict SUBSET of the 27 at stress 5; EVERY divergence is a FAIL — not one CRASH, not one HANG.** Every entry is exception-shaped and **nothing else in the master moved**: one unrooted value, one family.

**THE WITNESS IS MASTER ENTRY `catch_1`, UNMODIFIED, SEVEN LINES:** `main :- catch(( X is foo + 1, write(X) ), error(type_error(evaluable, foo/0), _), write(caught_type_error)), nl.` Oracle and stress-0 print `caught_type_error`; stress 1,2,3,5,10,25 print **NOTHING on stdout** and one line on stderr — `Warning: goal raised exception: _G0`. ⛔ **READ THE BALL: `error(type_error(evaluable, foo/0), _)` COMES BACK AS `_G0`, AN UNBOUND VARIABLE.** It no longer unifies with the catcher pattern, `catch/3` does not catch, the exception escapes, and the process **EXITS 0 having printed the wrong thing.**

⭐ **THE DIAGNOSIS IS TO THE LINE AND IT IS NOT IN `catch/3`.** `src/runtime/unification.c` `rt_pl_compound_cell` allocates `blk` with `PL_CELL_ALLOC` (= `rt_ws_alloc_descr`, the collected arena) and then holds it **in a C LOCAL across `prolog_atom_intern` AND across a second `PL_CELL_ALLOC`**, building the compound around it afterwards. A collection at either point leaves `blk` stale. The `rt_pl_ball_*` constructors **nest that call three and four deep** with every intermediate (`fec`, `eec`, `pic`, `ctc`) in a C local too — the deepest nest of it in the runtime, which is exactly why the exception family is the visible population and nothing else is. **The defect is in the callee; the ball family is where the probability concentrates.** This is the cto/cfo known class — a heap pointer live in a C local across an allocating call — and no root walk can see a C local. Escalated to the cfo under Lon's order with the witness and the ruled-out properties; **`unification.c` NOT touched.**

⛔⛔ **TWO PROPERTIES THAT INVALIDATE THE OBVIOUS INSTRUMENTS, BOTH MEASURED, AND EVERY LANE NEEDS THEM.** **(1) IT IS NOT ARENA-DEPENDENT** — it reproduces at the **SHIPPED 512 MB arena** at stress 5, and **MY OWN ARENA ARM READ `divergent=0` OVER 1043 PAIRS ON THESE VERY ENTRIES**, because both its arms sit at stress 0 where almost nothing collects (measured: **4 of 23** Prolog benchmark kernels collect at 1 MB with no plant). ⛔ **AN ARENA-ONLY ARM CANNOT FIND THIS CLASS, AND ITS BOARD WILL PRINT `arena_mb=1` WHILE IT FAILS TO.** **(2) THE WINDOW IS RAGGED, NOT A THRESHOLD** — WRONG at 1,2,3,5,10,25,26,30,35,45,48 and **CORRECT at 0,20,40,50,60,100**. Non-monotonic: wherever the forced collection lands inside the throw-to-catch window. **A seat sampling {20,40,50} calls this GREEN; a band of {50} finds ZERO of the 27.** hq_raku's LOWER-BOUND relabelling is arithmetic, not caution.

⭐ **A MINIMISATION TRAP THAT COST FIFTEEN MINUTES AND WILL COST THE NEXT READER THE SAME:** `catch(throw(my_ball(42)), B, ...)` — a **bare-variable catcher** — **PASSES AT EVERY STRESS POINT**, because `B` unifies with the corpse too. The defect is visible only through a **STRUCTURED** catcher that must match the ball's shape. **A witness that catches into a fresh variable is not a witness for this class.** ⛔ And the cure must not be `catch/3` accepting a corpse: that turns 27 wrong answers into 27 different wrong answers and closes the row while the lost root stays live for every other consumer of a thrown term.

⛔ **THE INSTRUMENT COST FOUR CORRECTIONS TO GET HERE AND THREE OF THEM WERE OTHER SEATS'.** hq_snobol4: the class is arena-INSENSITIVE, so a one-axis gate reads green over it. CEO-1024: a stress POINT chosen at the top of the known red band is a band that ends where the defects start. hq_raku + cto: `{16,32}` is that error MIRRORED (their map family lives at 1–6, invisible at 8+; hq_snobol4 red at 25 while green at 10,12,16,20,35,50) — **the band must not be a straight line through known points in EITHER direction.** hq_snocone: **a cell that names the arena is not thereby a reading of the collector** — now a DECIDABILITY arm with a per-language positive control, because the `[ZGC]` counter reads **zero for Prolog even where `[GC-WALK]` shows 8001 collections.** ⭐ **Without those four the gate would have shipped with my name on it reading GREEN over its own subject.**

⛔ **AND THREE FAULTS WERE MINE, ALL IN THE INSTRUMENT.** `run_arm` **discarded each shard's rc**; the refusal **deleted its own mktemp workdir**, so the harness's one-line explanation was gone before I read it (I recovered 14 of 68 names from a truncated `head -40` log **by luck** and sent the coo a confident wrong cause); and the first fix **refused on any non-zero rc**, which is wrong because the harness exits 1 whenever a board merely CONTAINS REDS. ⭐ **`rc` conflates "this board has failing entries" with "this board did not happen" and no better rc predicate separates them** — the honest question is **did this shard RECORD anything**, answered by a row-count delta. ⛔ **The 68 "vanished" pairs were never a collector effect: they were exactly shard 1 (every one ≡ 1 mod 16), because I COMMITTED UNDER A RUNNING BOARD — the pinned ground is the REPO SHA, not only the binary, and `assert_ground_unmoved` re-reads it per PROCESS.** hq_pascal's sentence, in its third variant: **A VERDICT THAT DESCRIBES A TREE OTHER THAN THE TREE YOU SHIPPED IS NOT A VERDICT.** Standing rule for this lane now: **settle the tree completely — pull, rebuild, push, clean — before starting a board, and touch git NOTHING until it lands.**

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog (later, MODE TENET) — **THE STANDING GAP THE BLOCK BELOW NAMED IS FILLED; PROLOG'S CENSUS HALF IS CLEAN AND PROLOG IS NOT; I CORRECTED MY OWN GC GATE TWICE, THE SECOND TIME AGAINST A NUMBER I HAD REASONED MY WAY TO; AND A REF CUT UNDER LON'S ORDER FOUND A SILENT EXIT-0 CLASS THAT HAS NOTHING TO DO WITH THE COLLECTOR** (row `prolog-gc-the-prolog-share-of-the-unmapped-slot-population-...`, minted and held)

✅ **THE GAP THE PREVIOUS BLOCK ENDED ON IS CLOSED:** raku and snobol4 had a no-layout GC row and Prolog had none. Minted `prolog-gc-the-prolog-share-of-the-unmapped-slot-population-censused-by-name-and-the-master-clean-at-one-megabyte` (rank 0), **DONE-WHEN proven RED and PREMISE-WHEN proven GREEN, both run as the baton carries them rather than as drafted.** Also minted `prolog-an-uncaught-error-in-an-initialization-goal-exits-0-with-empty-stdout-where-swipl-exits-2`, DONE-WHEN proven red.

⛔⭐⭐ **PROLOG IS NOT CLEAN AND I WITHHELD THE GREEN RATHER THAN PUBLISH AND REVERSE IT.** Census half clean on SCRIP `5418432bb`: `unkinded=0 holes=0` over `graphs=88364 words=1382288`, `graded=553 no_layout=10`. Master half on the **WINDOW axis only**: control (shipped 512 MB) vs subject (`SCRIP_HEAP_MB=1`), one binary, **1043 (entry,mode) pairs per arm, entry KEY SETS IDENTICAL, DIVERGENT=0** — and the summed boards were identical too, which is worth nothing on its own and is why the per-entry step is not ceremony (a total cannot tell *the same 543 pass* from *543 pass, but not the same 543*). ⛔ **CEO-1024 then established that the window axis at stress 0 is EXACTLY the arm that reads green over this class**, so that DIVERGENT=0 is **true, narrow, and not evidence Prolog's GC share is clean.** Reported to the ceo in those words with Prolog's condition-1 cell as **UNMEASURED**, not green.

⛔⭐⭐⭐ **I CORRECTED MY OWN GATE TWICE AND THE SECOND CORRECTION IS THE ONE WORTH KEEPING, BECAUSE I REASONED MY WAY INTO IT.** (1) The first cut varied **the arena alone**; hq_snobol4 measured that their class is arena-INSENSITIVE and stress-SENSITIVE (byte-equal at stress 0, wrong from stress≥1, both modes, both arenas), so it **would have read green over exactly the defect it was built to catch.** Verified the mechanism rather than trusting the summary: `gc_heap.c c_rt_gcheap_alloc` reads `SCRIP_GC_STRESS` once into a static and raises `g_gc_pending` every N allocations — orthogonal to window size, and **unset means zero forced collections.** (2) I then set the plant to **8, chosen as the top of hq_snobol4's measured red band {1,2,4,8}** — and CEO-1024 (hq_raku at stress 16: m3 853→817, m4 853→824, **65 gradings lost, 0 gained, 36 distinct programs**, all matching at stress 0) showed that is the same error one rung up. ⭐ **CHOOSING THE PLANT FROM WHERE THE KNOWN DEFECTS WERE IS CHOOSING A BAND THAT ENDS WHERE THE DEFECTS START — evidence-based in feel, circular in fact.** A point cannot answer *is there a divergence point above this one*, which is the only question. The gate now runs **one arm per value of an ASCENDING band** and reports **where** the divergence point is, name set per arm, never a count (a same-count comparison hides a **moved** divergence point — the swap in the stress dimension). `scripts/test_gate_pl_master_is_clean_at_the_tiny_arena.sh`, SCRIP `9e09c42a5`. **Deliberately NOT in `make test`** (TENET condition 2: the blocking set is the shared resource that does not scale with seats).

⭐ **A TIMEOUT IS NOT A DIVERGENCE, AND IT MATTERED ON THE FIRST SHARD.** At stress=1 the only movement against control on shard 1/16 was `benchmark_queens` m3+m4 into HANG — and stress=1 collects on **every** allocation, so a correct program runs orders of magnitude slower. An `rc=124` cannot tell *needs 8.1s* from *never finishes*. The stress arms raise the harness `TIMEOUT` and a pair whose **only** movement is into HANG **REFUSES rc=2 as INCONCLUSIVE** — never a defect, never dropped to keep the gate green. ⛔ stress=1 over the full 563-entry master is a **multi-hour** job (shard 1 alone exceeded 15 minutes); that is a row, not a silent omission.

⛔ **THE FORK I ASKED RATHER THAN DECIDED.** The ten `no_layout` are **ladder refusals** — the compiler refuses before emitting, so there is **no emitted code, no leaf boundary and no store anywhere.** That is NOT the same as raku's parse refusals under the cfo's *stored-into-a-mapped-slot* clause (CEO-999), which makes their nine a **correctness** population. If my reading holds these ten are a **completeness** debt wearing a GC census's clothes; if not they gate Prolog's completeness. `util_zls_frame_map_census.py` is the ceo's with its token frozen at CEO-821 and the DECLARED mechanism is the cto's under the raku precedent — **widening another seat's mechanism to make my own cell read better is CEO-993's false criterion**, so it is an ASK.

⛔⭐⭐ **LON'S REF ORDER: THE FLEET DENOMINATOR IS OVER-COUNTED, AND THE FIRST REAL REF FOUND A DEFECT WITHIN MINUTES.** A `.ref` grep says Prolog is **127 of 127 refless**; the honest number was **4**. (a) `benchmarks/prolog/bench` carries **23 kernels and 23 `.expected`**, and `test_bench_prolog_timed.sh` verifies each kernel's single-shot output against its `.expected` **on gnu, swi AND m3** — so every Prolog benchmark that is actually *timed* is already oracle-verified on three engines; `demos/scrip` uses `.expected` too, so this is not a Prolog quirk. (b) **94 are declared never-run** — the corpus README's own words: *"The pristine `src/` trees are reference only — never run by the SCRIP gate."* (c) 6 are engine shims (`prelude_*`, `epilogue_gplc`). The real gap was **4 demos and is now 0** (corpus `66ec59433`), with `.in` stdin fixtures for the two that read input. Two of the four are **library blocks** of the cross-language family demo (`:- export(...)`, `main :- true.` by design) — their empty refs certify *loads clean, prints nothing* and are named so nobody reads them as coverage.
⭐ **THE TRAP THAT WOULD HAVE POISONED EVERY REF CUT TODAY, AND I NEARLY WALKED INTO IT:** plain `swipl -q file.pl` **enters the toplevel**, which emits its own newline at EOF. My first cut of `family_prolog.ref` was **that stray newline** — byte-stable, reproducible, and an artefact of *how I invoked the oracle* rather than of the program. Use `-g main -t halt`. **A ref is cut from the oracle, never from our output — and the clause that law is missing is AND THE ORACLE IS INVOKED THE WAY THE PROGRAM IS MEANT TO RUN.** hq_raku hit the neighbouring form the same hour (4 raku benchmarks read UNMEASURED because rakudo needed a prelude named on the command line). ⭐ And a companion clause for the fleet census: **a program that reads STDIN needs a declared INPUT before a ref means anything** — cut `prolog_parser.pl`'s ref with `/dev/null`, which is what a census-driven batch naturally does, and you get an empty ref that is valid, stable, reproducible and certifies a program that was **never exercised**; with a real `.in` it reads 4 lines.

⛔⭐⭐ **WHAT THE REFS FOUND, AND IT IS THE SILENT EXIT-0 SIGNATURE THROUGH THE COMPLETENESS DOOR.** `demos/prolog/prolog_parser.pl` and `prolog_recognizer.pl` produce **ZERO BYTES under scrip, BOTH modes, AT rc=0**, against 4 and 58 oracle lines. Minimal witness: `:- initialization(main).` + `main :- no_such_builtin_xyz(1).` — **swipl exits 2, SCRIP exits 0** with empty stdout and one `Warning:` line on stderr. **So ANY Prolog program calling a predicate we have not implemented reads, to every rc-shaped instrument in the tree, as a clean run that chose to print nothing.** ⛔ The row carries its own trap on its face: `prolog-failed-initialization-goal-exits-1-where-swipl-exits-0` is the **opposite** direction, and a goal that **FAILS** and a goal that **THROWS** are different ISO cases with different exit codes — a cure that collapses them closes one row by opening the other. **There are now three known non-collector sources of the exit-0 silent-wrong-answer signature: this one, hq_pascal's whole-array-into-a-record-field representation disagreement (CEO-1021), and the collector itself — and the cheap discriminator is the ceo's: same wrong answer at stress 0 and at the shipped arena means it is not the collector.** My gate's control arm enforces that by construction: a pair red identically in EVERY arm prints under PRE-EXISTING-RED and is out of the verdict.

✅ **CEO-1019, PROLOG'S CANDIDATES, ANSWERED:** `pl_flags` (`by_name_dispatch.c:2935`) is **CLOSED — it cannot hold a collected-heap pointer.** `{ const char *nm; char val[48]; int mod; const char *ok[6]; }`: `nm`/`ok` are `.rodata` literals and **`val` is an inline 48-byte buffer, not a pointer**; the only writer is `snprintf(fl->val, sizeof fl->val, "%s", vn)` at `:2990` (a copy *into* it) and the reader at `:2975` calls `pl_mk_atom_dup` rather than aliasing. ⭐ **I read the TYPE — grepping for a pointer store and finding none would have matched a SHAPE, not answered the question**, which is the ceo's own retraction shape. The trail is already one of the two `rt_gc_root_range_add` ranges.

⛔ **THE ROW I WAS HOLDING IS REWRITE-NOT-CURE AND I HANDED IT BACK RATHER THAN NARROWING IT.** `instrument-the-tiny-arena-is-the-default-of-every-gc-test`: its DONE-WHEN runs `corpus_suite_harness.py` with `SCRIP_HEAP_MB` **unset** and greps for `ARENA SCRIP_HEAP_MB=1`, but the harness reads the knob from the environment with a hard default of `512` **and its own comment states the decision and the reason** — *reported, never defaulted here, because a harness that silently changed every existing board's arena would change what every published number MEANS with no line of evidence anywhere.* **A criterion that can only pass by undoing a considered design is not a criterion; it is a disagreement with the design, and it belongs in front of the owner.** ⭐ **Three lanes filed the same overlap independently within minutes and none of them knew I had already concluded it:** hq_pascal and hq_raku on the control arm (*an absent variable cannot distinguish "I asked for the default" from "I asked for the shipped arena"* — the `command -v` trap in another costume), and hq_snobol4 with a hazard that **survives the fix those two asked for** (their band is arena-insensitive, so pinning the arena and leaving the poll set unset reads green while the suite table certifies *tiny arena* — worse than silence, because the table then looks like coverage). The cto added a fourth: **ARENA-INDEPENDENT must be a legal answer beside PINNED and DEFAULT**, because their grid gate is a static read of emitted text and pinning it would force a false declaration. **PREMISE-WHEN written RED BY DESIGN** so the next picker is refused the lock and sent to the ledger instead of building it.

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog — **PROLOG'S ZLS REGION IS CLEAN AND THAT DOES NOT CLEAR PROLOG; MODE WENT SEXTET→SEPTET→TENET UNDER ME IN ONE SITTING; AND I CORRECTED A CLAIM IN MY OWN OUTBOX BEFORE IT REACHED THE cto**

✅ **THE PROLOG FRAME-MAP CENSUS, measured on SCRIP `d6b8a02c4` + corpus `86574b2bf`** (`python3 scripts/util_zls_frame_map_census.py --lang prolog`): `graphs=88364 words=1382288 unkinded=0 holes=0 graded=553 no_layout=10 fields=806555`, holes=0 across all six classes. Handed to the cto as the base arm for their planner/emitter row and **accepted into their baton with this seat's name on it**.

⛔⭐ **A CLEAN ZLS-MAP DOES NOT CLEAR PROLOG OF THE UNMAPPED-SLOT CLASS, AND SAYING SO IS THE POINT.** That census's population is **the zls region only** — the spine and the wire header past `region_end` are outside it, by the script's own header. § 3b's witness was a spine cell **32 bytes BELOW the region base**. A green census answers a narrower question than the one it looks like it answers.

⛔⛔ **I STAGED A FALSE CLAIM AND CAUGHT IT IN MY OWN OUTBOX, WHICH IS THE ONLY REASON IT IS A NOTE AND NOT A ceo CORRECTION.** I had written to the cto that classes 1 and 3 give Prolog *no instrument*. **False for class 1:** the cfo's `[GC-SPINE-LOST]` in `gc_heap.c` is language-agnostic and fires for Prolog — measured with a **positive control first** so silence could not be read as health: two rung witnesses under `SCRIP_GC_MAPS=1 SCRIP_GC_STRESS=1` print 14 reporter lines (`[GC-MAPS] pop=cstack ranges=3 bytes=62552`, `[GC-WALK] frames=2 roots=1 nomap=0 notab=0`, `sniff_only=0`), and `[GC-SPINE-LOST]` prints **zero candidates**. ⭐ **So the Prolog gap is A SWEEP NOBODY HAS RUN, not a missing instrument** — a row, not a blocker. ⛔ And it is an honest zero **on a sample of two**, not a language verdict; the cto is carrying it at that width beside their own `242 undecidable sites, UNMEASURED`. Only **class 3 (a register pair)** is instrument-less, and by construction: a frame map describes memory.

⭐ **THE TEN `no_layout` ARE FIVE SURFACES ACROSS THREE RUNGS — AND FIVE SURFACES IS AN UNKNOWN NUMBER OF CURES.** `b_getval` (2, rung 10), `set_prolog_flag` directive (2, rung 10), `aggregate_all` (3, rung 8), `read_term` (2, rung 6), `with_output_to` (1, rung 10). These are read off **the compiler's own refusal text**, so they are a measurement and not an inference — hq_raku withdrew the contrary implication — but their challenge lands one level down and is kept: **two of their six benchmarks collapsed into ONE construct across what their GOAL called two unrelated families**, so `b_getval` and `set_prolog_flag` may share one rung-10 backing store. **Never quote five as the cure count.**

⛔ **THE XFAIL TRAP, CHECKED AND NOT WALKED INTO.** The census reads `declared=0 defect=10`. `ALL.wantrc` exists beside the master and none of the ten appear in it, so the cheap way to clear the bucket is to declare them non-zero — **that is minting an XFAIL and CEO-753 forbids it.** The bucket is correct; it closes when rungs 6, 8 and 10 land. ⭐ The census **refuses by name** when that sidecar is absent, which is how I know `declared=0` is a *measured* zero rather than a defaulted one — the discriminator that proved my census was not hq_raku's twelve-program sidecar defect.

⛔⭐ **THE STALE-BINARY GUARD IS INDISTINGUISHABLE FROM FOUR RED ARMS AND COSTS ONE SECOND EACH.** Running the four GC blocking arms returned `rc=2` in ~1s apiece; the cause was a pulled `Makefile` (19:55Z) newer than my build (19:51Z). **Rebuilt, all four PASS in 23s total (2s/1s/8s/12s).** Any premise-style or sharded run that does not build first harvests a field of one-second `rc=2`s — and `run_blocking_set.sh` **invoked directly does not do the build `make test` does** (the cfo's sharper form). **Cheap + rc=2 is the signature.**

⛔⭐ **A THIRD BLOCKING-SET DEATH THAT REFUTED THE cfo'S MODEL, WHICH THEY WITHDREW WITHIN THE HOUR.** They had two deaths at arms 103 and 166 at ~15 and ~14 minutes and concluded *the arm is not the variable, the wall clock is*. **Mine died at arm 6 of 368 after THIRTY SECONDS** (start 19:51:50Z, last byte 19:52:20Z, recorded arm durations summing to 29s, and the `rc` echo on the following line never ran — so the shell was killed). Thirty seconds and fourteen minutes cannot be one ceiling. What the three share is that **something outside the run reached in**; ten seats now share one Unix user, so a `pkill` by script name reaches every seat — **the only candidate standing after three points, and still a candidate.** ⛔ I ruled myself out **by the clock**: my pull landed 3.5 minutes after the death.

⭐ **MODE MOVED TWICE UNDER ME IN ONE SITTING — `SEXTET` (my CLAUDE.md banner) → `SEPTET` → `TENET`** — and the banner was wrong about my assignment entirely, naming a `by_name_dispatch.c` region that is the ceo's under both later modes. **The file was right and the digest was stale, exactly as the CONFLICT RULE says.** Under TENET **Condition 1 a GC defect in an HQ's own language is that HQ's row and outranks its completeness work**, with classes 1 and 4 on the language HQ and class 3 the cfo's. ⛔ **THE STANDING GAP: raku and snobol4 each have a `-the-31-no-layout-…-gc-slot-kind-census` row and PROLOG HAS NONE.** That is this seat's next work and it wants minting.

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog — **THE CALL-ARGS SIGSEGV WAS CURED BY THE CURE I BUILT AND REFUSED, LANDED BY SOMEONE ELSE ONCE THE COLLECTOR CHANGED UNDER IT; AND THE ATOM-NAME GATE'S CLAUSE 1 WAS STILL THE DENYLIST ITS OWN LEDGER HAD CONVICTED** (rows `prolog-twelve-master-entries-sigsegv-...`, `prolog-atom-names-live-in-71-c-locals-...`)

✅ **BOTH ROWS MEASURED GREEN TODAY.** The call-args DONE-WHEN, run verbatim on SCRIP `ac1970678` with a fresh incremental build: `queens: stress=1 crashes 0/20 | stress=0 crashes 0/10`, rc=0 — and the unstressed control held at 0/10, so the witness was still measuring its own row rather than refusing. The atom-name gate then went **3 of 3**, clause 2 at **20/20 in both modes** under `SCRIP_GC_STRESS=1 SCRIP_GC_POISON=1`, with the positive control proving the collector really moved (`moved=233344B`) and really poisoned what it vacated (`vacated=64064B`).

⛔⭐ **THE CURE IS CURE 1 FROM MY OWN LEDGER, VERBATIM, AND THE cto LANDED IT** (SCRIP `cda82fa66`, CTO-90): `gc_visit_one`'s `DT_N` `slen==2` arm now requires `gc_block_exact(vc, HB_AGGV)` and its `slen==1` arm requires the target cell to lie inside a heap block. That is the walker-side stack exclusion at both `DT_N` branches which this seat built, measured at **20/20 → 0/20**, and **refused to land** — because on that tree a `DT_N slen==1` frame reference was the only root reaching that cell whenever the `pz` fast path skipped the machine-stack walk, so excluding it would have traded a crash for a premature free.

⭐⭐ **THE REFUSAL WAS RIGHT AND IT WAS NEVER ANSWERED — IT WAS DISSOLVED.** SCRIP `c6bf8e789`, THE E SWITCH, deleted `cons_stack`, `pz` and the seam sweep outright. `gc_collect_ex` now calls `gc_stack_segments` **unconditionally**, so the machine stack is always walked and the excluded cell is always reached by the stack walk itself — which is the cto's own stated premise: *“a name into the stack is visited by the stack walk itself and never followed from here.”* Verified in the source rather than inferred: `gc_zeta_frame` and `cons_stack` no longer exist in `gc_heap.c`.

⛔⭐⭐ **THIRD TIME THIS ROW FAMILY WAS OVERTAKEN RATHER THAN ANSWERED, AND NOTHING ON THE BUS REPORTS IT.** (1) The atom-name row's HB_WS unpin ask — made moot by CEO-799 deleting pinning. (2) That row's DONE-WHEN clause 1 — went false-green when storage moved allocator names. (3) This row — its blocking safety question dissolved by the E switch. **A seat that re-reads a blocked row and asks “has my question been answered” gets NO all three times.** The question to ask is *“is my question still the question”*, and the cheap form is to **re-run the DONE-WHEN before re-reading any of the analysis above it** — the analysis is what rots, the criterion is what keeps.

⛔ **AND THE GATE'S CLAUSE 1 WAS STILL THE VERY DENYLIST THIS ROW'S LEDGER HAD ALREADY CONVICTED.** The baton says clause 1 was *“re-cut to name the property”* after it read CLEAN on a tree where storage had moved from `rt_pinned_*` to `rt_heap_strdup_c` — the same heap under a different allocator name. **The prose was re-cut; the code was not.** It still grepped a four-spelling denylist. Rewritten this landing as a **fail-closed allowlist** that resolves the allocator actually producing the name pointer, and **mutation-proved five ways**: real file ok · collected-heap FAIL · **brand-new allocator name FAIL where the old denylist reads 0 hits and PASSES** · key/entry split FAIL · assignment deleted → **REFUSE rc=2, never a pass**. ⭐ Scoped to the NAME and not the file, deliberately: `prolog_atom.c`'s tables (`ht`, `atom_names`) come from `rt_wsb_alloc`, which **is** the collected heap and is **correct** — `pl_gc_roots` visits both blocks and registers their slots, so a slide fixes them up. Only the name may not be collected, because a raw C local holds it across operations and no root scan sees that local. A file-wide allowlist would red the two table allocations, which are not the bug.

⛔ **NOT CURED, NAMED BY THE cto IN THE SAME COMMIT AND NOT BY ME:** *“The staged-argument walk visiting 64 slots regardless of how many are staged is named here as a latent leak class for the rt.c seat.”* `rt_gc_root_args` still walks all `CALL_ARGS_MAX` slots unconditionally. A LEAK class, never a crash; `rt.c` is the cfo's node under MODE line 2; nearest existing home is row `emit-argument-staging-leaves-g-call-args-for-the-callee-frame-or-the-caller-spine`. Routed to the officers rather than minted as a duplicate.

⛔⭐⭐ **THE GATE'S 20-RUN REPETITION WAS DEAD WEIGHT THAT MY OWN POISON KNOB MADE REDUNDANT, AND NOBODY RE-DERIVED THE COUNT.** Wiring it blocking ran into cost: **778s** in the shipped arena, **843s** under the tiny arena, against a blocking set whose **most expensive arm is 90s** — 8.6x the maximum. Three candidate fixes died on inspection, each for a measured reason: baking in the tiny arena (CEO-939 already measured that a blanket export takes the 360-arm set to **32 red**, and the positive control is **identical** at 1 MB — `moved=233344B`, `vacated=64064B` — so it is **inert for this witness** and would have been a dead switch); renaming into `test-arena` (that pass is **not scanned** by `util_gate_wiring.py`, which reads only `test`/`test-postoffice`/`preflight`, and it re-runs gates already wired elsewhere, so it is a second pass and not a home); and trimming the m4 rebuild (the gate already builds **once**, outside the loop). ✅ **THE ANSWER CAME FROM REVERTING THE CURE:** on a build with atom-name storage put back on the collected heap the gate goes **RED at ITERS=1 — 1 of 1 runs diverged, BOTH modes** — and identically at 2 and 4. **Detection is deterministic, not probabilistic.** The 20-run design was right *for its world*: before `SCRIP_GC_POISON` a dangling read returned the OLD BYTES, still the correct string, so only repetition plus luck could catch it. **Poison did not make the gate faster — it made the repetition redundant**, and the count was landed in the same sitting as the knob, by this seat, and never revisited. The cost is paid by the **green** runs (~19.4s each under stress) while a diverging run fails fast (the whole mutated study: 2s, 1s, 2s). Wired as `--quick` = **ITERS=2** at a measured **~95s** (102/91/92) — two and not one because one run has no margin and this row's history includes a witness that read green in every configuration because its switch was dead. ⛔ **MY EXTRAPOLATION WAS WRONG AND THE HEADER CARRIES THE CORRECTION:** I predicted ~80s from 19.4s/run plus ~2s fixed; the fixed cost is **~13s**. Landed SCRIP `eaf756a82`, re-proved 4/4 green on the merged tree after the rebase. **GENERAL FORM, filed as a FINDING:** *when you land a stronger instrument, every older instrument's parameters that existed to compensate for its absence go stale — and they stay stale because they still work.* A redundant parameter never fails, never reds, never announces itself; it only costs.

⛔ **AND A MISTAKE OF MINE, RECORDED BECAUSE IT IS THE SITTING'S OWN CLASS:** my first fail-once study printed five clean-looking rows of `seconds=0`. The fallback was guarded by *“is the output file non-empty”* when I meant *“did the gate run”*, and bash's `No such file or directory` satisfied that guard, so the fallback never fired and the study measured nothing. Caught only because five identical zeros cannot be real. **That is the same narrower-question-than-you-asked failure as the denylist, the glob and the swallowed names — committed inside the script written to investigate it.**

⭐ **THE TEN no_layout ENTRIES ARE ONE CLASS AND THE DUMP PATH IS EXONERATED** (cto's ask, answered same sitting). `--dump-zeta` and a plain run give the **same** rc=2 and a byte-identical message, so the refusal is not in the dump path. All ten refuse with the ladder's own deliberate *“not on the ladder yet — rung N lands it”*: **five surfaces across three rungs** — `b_getval` (10, 2 entries), `set_prolog_flag` directive (10, 2), `with_output_to` (10, 1), `aggregate_all` (8, 3), `read_term` (6, 2). **Not ten defects, and no collector change will move them:** they close when rungs 6, 8 and 10 land those five surfaces. First-side by CEO-993/994 as a denominator hole, but they belong to the ladder rows, not the GC road.

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-20 hq_prolog — **findall LEFT ITS GOAL'S LAST SOLUTION BOUND, AND THE RULE THAT FIXES IT WAS ALREADY WRITTEN IN § B.7 — findall WAS NEVER BROUGHT UNDER IT** (row `prolog-findall-over-a-dynamic-predicate-...`, CURED AND LANDED, SCRIP `ee77a8e50`)

⛔ **THE DEFECT WAS NOT THE DATABASE, NOT THE COLLECTOR AND NOT THE ENUMERATION, and the row's own GOAL says all three.** `findall/3` left every variable its goal bound **still bound, at the LAST solution**. `findall(X, s(X), L)` over `s(a). s(b). s(c).` returned `[a,b,c]` correctly **and left `X` bound to `c`**; swipl leaves it unbound. Everything else follows: a second findall in the same clause body reusing that template variable enumerated an **already-bound** goal — `3-1` where the value still matched and **`3-0`** against a different predicate. `hb_pl_root_cells` read `3-1-1-1-1` because all five of its findalls use the template variable `X`. Identical at stress 0/1/3/5 and at the shipped arena; **a STATIC predicate reproduces it** (`3-1-1`), so the dynamic db is not involved; and the survivor is the **LAST** solution, not the first, so the enumeration was never short.

⭐ **THE DISCRIMINATOR IS THE TEMPLATE VARIABLE, NOT THE PREDICATE**, which is why nobody had isolated it: two findalls over the **same** predicate with **distinct** template variables are green, two over **different** predicates with the **same** template variable are red. It is also why `hb_pl_findall.pl` was green throughout — its three findalls use `X`, `Y-Z` and `Q`.

**ROOT CAUSE, and the predictive test that decided it.** The rung-2 clause step drops the choice on the **trust-me** clause, and with no live choice `pl_tr_needs_log` returns 0 for every cell, so the last clause's binding of the caller's variable is **never logged**. Prediction: keep a choice alive past the last solution with a trailing always-failing clause and the defect must vanish. It does — the same program reads `unbound 3-3` with the extra clause and `bound(c) 3-1` without it.

⛔⭐ **THE RULE ALREADY EXISTED AND THIS IS THE LESSON WORTH KEEPING.** § B.14 already specified the cure — *"`Goal.ω → finish`: undo `F.LOG` … so the template's bindings never leak"* — and § B.7 already states it in general, discovered on the if-then-else: ***a construct that must undo must first be a choice.*** findall was simply never brought under it. **A rule recorded beside the construct where it was DISCOVERED is not a rule ENFORCED across the constructs it governs.** The three constructs that call `rt_pl_disj_open` (rung-3 disjunction, rung-7 generator, rung-5 bound) were each fixed only when their **own** witness went red — and findall had no witness that could go red, because its gate graded the one program whose template variables are all distinct. ⭐ **I walked into the documented trap myself**: my first cure banked a mark in the findall accumulator and unwound to it, changed **nothing**, and an instrument measured `delta_entries=0` — the trail was EMPTY. Reverted, and recorded so nobody re-derives it.

**CURE:** the goal runs inside `IR_BOUND`/`IR_UNMARK` with the flags `pl_lower_ite` uses, which is why `\+`, `once/1`, `->`, `forall` and `ignore` were never affected. Eight lines in `lower_prolog.c`. No new box, no new IR kind, no new global, no runtime allocation, no safe-point change. Covers `findall/3`, `findall/4`, `bagof/3`, `setof/3` on the no-free-variable road.

**MEASURED, `SCRIP_HEAP_MB=1` throughout.** Base vs head on tree `76ef697a9`, one change and the same tree: ProM **m3 542 → 543, m4 459 → 460**, nothing traded. Merged tree: **m3 543/563, m4 460/480**, `arena_mb=1`. Ladder `--to 18` **both arms on the merged tree**: base PASS=550 FAIL=18, head PASS=550 FAIL=18, failing sets **byte-identical** (9 witnesses). `hb_pl_root_cells` **3-3-3-3-3 both modes, stress 0/1/3/5**, so its gate **flips REPORTED → BLOCKING** in this landing as its Makefile comment required. `make test-arena` 44 arms, 1 red — the mark-walk scaling arm, **controlled to origin and red there too**, already the cfo's row; all 7 smokes green. 24 oracle probes + 7 edge cases green against swipl.

⛔ **STILL OPEN, NOT CHASED (order of work is GC alone):** `bagof/setof` with **free variables** raises `existence_error(procedure, $bag_prep_b/2)`. `$bag_prep_b` is emitted at `prolog_lower.c:191` and **implemented nowhere** — one reference in the whole tree, the site that emits it. Same road as the pre-existing ladder red `ladder__rung08_bagof_setof_free_variable_grouping`, red on base and head alike. Owed a row.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-17 hq_prolog — **THE POISON DETECTOR DEFAULTS ON FLEET-WIDE (CEO-809/811), AND THE CALL-ARGS SIGSEGV IS DIAGNOSED TO THE LINE WITH THE ONLY WORKING CURE REFUSED AS UNSAFE** (rows `prolog-twelve-master-entries-sigsegv-...`, `prolog-atom-names-live-in-71-c-locals-...`)

⛔ **THE BOUND ON THE POISON DETECTOR IS NOT A SMALL CORNER, AND IT IS WRITTEN HERE BECAUSE hq_snocone ASKED FOR IT TO LIVE IN A GOAL FILE RATHER THAN A MAILBOX.** `SCRIP_GC_POISON` defaults ON at SCRIP `ed1c5760d` (CEO-809; `=0` is the control arm; cost ~20 ns per collection on a 186 B mean vacated tail, controlled interleaved arms show −0.5%, i.e. noise). It fills the **vacated tail** after the slide, so it catches a read of space the collector gave back. ⛔ **It is silent on the other half of the class by construction:** a block that slid **DOWN** leaves its old address inside *live data*, where a stale read still finds something plausible. **hq_icon measured that bound rather than taking my word for it** — with the environment held at equal size, `SCRIP_GC_POISON=0` vs `=1` is identical at all 36 stack offsets of their gc2 witness and no `0xDB` byte ever reaches the output, because their defect is exactly the slid-down shape. ⭐ **So poison-on is not "the memset class closed"**, and the cfo's carve cure is the same class from the other side — theirs stops a **write** onto ground the allocator wrongly believed clean, mine makes a **read** of vacated ground announce itself, and **neither would have caught the other**.

⭐ **IT EARNED ITS KEEP IN ANOTHER LANE THE SAME HOUR.** hq_raku's `benchmark_point_class_add` went PASS → CRASH on the first master pass with the fill on: `cls=0xdbdbdbdbdbdbdbdb`, graded 3/3 with the fill and rc=0 3/3 with `=0`, and the same split on a probe build of `55f47d143`, so the defect predates both collector commits. ⛔ **And their reading of it corrects a natural misreading of my own instrument:** it was a **missed FIXUP, not a missed mark** — the `DATBLK_t` moved, the table's copy of the pointer was fixed up by `dat_gc_roots`, the copy inside the `HB_DINST` instance block was not, and the instance is live and current. **`0xDB` in an argument does not tell you the object died — only that something pointing at it was not updated**, and the question is always *which copy* of the pointer got fixed.

**THE CALL-ARGS SIGSEGV: DIAGNOSED TO THE LINE, THREE CURES BUILT, TWO REFUTED, AND THE ONE THAT WORKS IS REFUSED.** Chain on `db4a6b1fc`, witness `queens.pl` at `SCRIP_GC_STRESS=1`: `rt_gc_point_arr` → `rt_gc_point_arr_c` → `gc_collect_ex(cons_stack=1)` → `rt_gc_root_args` (`rt.c:2051`) → `rt_gc_visit_descr(&g_call_args[0])` — a `DT_N slen==1` whose ptr is a **machine-stack address** → the `slen==2` branch on the caller's own `arr` → `gc_visit_vcell` → `vc->tbl` (stack) → `gc_visit_tbblk` → SIGSEGV.
- **CURE 1 (walker-side stack exclusion):** queens **20/20 → 0/20** stressed, 0/10 unstressed, DONE-WHEN green. ⛔ **NOT LANDED.** Split measured: the `slen==2` exclusion **alone leaves it at 20/20**, so the whole cure rests on the `slen==1` exclusion — and a `DT_N slen==1` pointing at a frame cell is **legitimate by design** (an unbound variable passed by reference to a live caller frame), which rung 3(f) made *more* common.
- ⛔ **WHY IT IS UNSAFE, WHICH IS THE FINDING:** `gc_zeta_frame` — the only walker of the machine stack — runs under `if (cons_stack)` at `gc_heap.c:609`, and the `pz` fast path leaves `cons_stack == 0`. **On that path the machine stack is not walked at all**, so the `slen==1` reference is the *only* root reaching the cell, and excluding it trades a crash for a premature free. In the crashing run `cons_stack == 1`, so the exclusion loses nothing *there* — the exposure is a different run, which is exactly the kind of thing not to ship.
- **CURE 2 (writer-side, the cto's recovered ICN-ARG-NULL precedent extended from `nparams` to `CALL_ARGS_MAX`):** queens **unchanged at 20/20 — REFUTED**, and it refutes the sub-hypothesis with it. The faulting slot is **index 0**, and the seed runs at the *next* call's prologue, so it cannot clear a dangling reference in the window between a callee's **return** and the next call. That window is where the collection lands.
- **WHAT IS LEFT IS A SPINE QUESTION AND THE cto NAMED IT AS THEIRS IN ADVANCE.** The `pz` predicate predates frame references being a common value in `g_call_args`. Making `pz` conditional on no slot holding a stack-pointing `DT_N` would make cure 1 strictly safe. ⛔ **Open and unmeasured:** whether `pz` is ever true while a frame reference is live — Prolog zframes make `nforeign > 0`, which may exclude `pz` for this lane entirely **while leaving it reachable for the other five**, in which case it is a defect the Prolog board can never show and another lane's eventually will.

⛔⭐ **AND THE INSTRUMENT LESSON OF THE SITTING, FROM hq_snobol4, WHICH I HAVE ADOPTED: A RED IS NOT A VERDICT, IT IS A BUCKET.** Their witness: `spitbol_testpgms test1` red on base and head, count 1/2 both times, name identical both times — and the only thing that moved was the first diff hunk inside the runner's own RED line (ERROR 22 at statement 137 → ERROR 29 at statement 72, divergence 19 lines → 65 → 19). **Every flat instrument was true and the cure was broken.** My per-case name sets have that hole: they are the strong form *across* cases and say nothing *within* one. The fix is a per-entry **fingerprint** (md5 of our own output, or the `(first-diff offset, rc)` pair) so a red that MOVED is a diff rather than a silence — taken as a row for this lane, in whatever shape hq_snobol4 builds first, one fleet one format. ⭐ Their independent version of my truncation bug in the same sitting: three of seven boards compared **empty against empty** and reported IDENTICAL on `d41d8cd98f00`, the md5 of nothing. **Both instruments announced a comparison they had not made, in the vocabulary of success.**


## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-17 hq_prolog — **ATOM NAMES LEAVE THE COLLECTED HEAP; THE COLLECTOR GETS A POISON KNOB BECAUSE A SLIDING COMPACTOR MAKES THIS WHOLE BUG CLASS UNFALSIFIABLE; AND THE STRESS SWEEP FOUND A TWELVE-ENTRY SIGSEGV THAT IS OLDER THAN THE ROW THAT FOUND IT** (row `prolog-atom-names-live-in-71-c-locals-...`, cured but NOT closed)

**THE ROW'S BLOCKER WAS OVERTAKEN, NOT ANSWERED.** Every `## NEXT` on that baton waited on the cfo for an HB_WS arm of the unpin knob. That ask is moot: **pinning is DELETED** (CEO-799, `3b79683b5`; `rt_pinned_|hb_pinned|hb_no_move` under `src/` reads zero). The shipped build is a stronger arm than the knob could ever have been. ⭐ **A row can be unblocked by the world moving rather than by the answer arriving, and nothing on the bus reports that** — the ask sat open and correct-looking while its premise expired.

⛔ **AND THE ROW'S OWN DONE-WHEN HAD GONE FALSE-GREEN IN THE SAME DIRECTION.** Clause 1 grepped `rt_pinned_(alloc|realloc|strdup)` in `prolog_atom.c` to prove the storage was still collectable. The storage had moved to **`rt_heap_strdup_c` — the same heap under a different allocator's name** — so the clause read CLEAN on a tree where the exposure was strictly *worse* than when it was written. **It was measuring the allocator's name, not the property.** Re-cut to name the property. This is the third instrument on this row to answer a narrower question than it appeared to.

**LANDED AT SCRIP `119d66c52`** (⛔ the commit message inside it cites `26083dd1a`, the pre-rebase hash of itself — `git pull --rebase` between commit and push rewrote it, and a hash a commit quotes for itself is the one hash it can never get right; the pushed object is `119d66c52`).

**THE CURE (the cto's option one, verbatim):** atom-name storage leaves the collected heap onto the C heap — the atom table is program-lifetime by construction, so it never wanted collecting — and `pl_gc_roots` stops visiting the 2×N name pointers it no longer owns. Option two, re-reading through `prolog_atom_name` after every point that can collect, was never a cure: **a claim spanning two sites can be held by a check; a claim spanning fifty-six cannot be held at all.** Gate clause 1 **RED → GREEN** on exactly this change. Master **542/459 of 563**, per-case non-PASS name set **byte-identical** to the `987a8ea74` arm head; smokes green on all eight frontends; preflight **52 arms, 0 red**; `strip_comments --check` 0; gate wiring CLEAN.

⛔⭐ **WHY A POISON KNOB HAD TO EXIST BEFORE ANY OF THIS COULD BE GRADED.** The collector is a **sliding compactor that never overwrites what it vacates** (`gc_heap.c`, `memmove` then nothing). So a pointer left behind by a move **keeps reading the old bytes, which are still the right string** until something is allocated over them. A dangling atom-name read therefore returns a *plausible* answer, and an output comparison — at any number of runs — cannot tell that class from a correct one. **That is why every witness this row ever ran was green, and why none of those greens meant what they looked like.** `SCRIP_GC_POISON=1` fills the vacated tail with `0xDB` after the slide (getenv-memo, off by default, the form the cto sanctioned; the `SCRIP_GC_` prefix is outside the zeta-switch census by the cfo's 2026-09-16 ruling). ⛔ **Proven live before being trusted** — `moved=253872B`, `vacated=64064B` — and the gate **REFUSES rc=2 if either reads zero**, because this row's first witness died of a dead switch that read exactly like a clean result. ⛔ **Honest bound: it covers the vacated TAIL only.** A block that slid *down* leaves its old address inside live data, where a stale read still finds something plausible.

⛔⛔ **THE SWEEP FOUND A BIGGER RED THAN THE ROW, AND IT IS NOT THE ROW'S CLASS.** Grading the master across three collector configurations (stress 0 / stress 1 / stress 1 + poison, all 16 shards, m3): **21 non-PASS → 33**. Twelve entries SIGSEGV under `SCRIP_GC_STRESS=1` and are clean without it — `benchmark_{crypt,ham,meta_qsort,mu,qsort,queens_8,queensn,queens,zebra}`, `forall_ite_directive_1`, `test_rung10_programs_puzzle_{14,15}`. **Graded by rate, not by one run**, because two entries in that same sweep passed under one config and crashed under another: `queens.pl` is **20/20 crashes stressed, 0/20 unstressed** — deterministic, and the witness of record. gdb: SIGSEGV in `rt_gc_visit_descr` (`gc_heap.c:419`) on a non-pointer `d`, reached from `rt_gc_root_args` (`rt.c:2050`) walking **`g_call_args`**, from a collection point in emitted code (`rt_asm_helpers.S:88`).

⭐ **THAT IT IS A SECOND DEFECT WAS MEASURED, NOT INFERRED FROM THE SHAPE.** The atom-name cure left the rate **unchanged** — queens 20/20 before and after, the atom witness 4/30 before and 3/30 after, which is noise and must not be read as an improvement. ⛔ **And the attribution refutes the reading I minted it with:** `3b79683b5^` (BEFORE the pinning deletion) is already **20/20**, `3b79683b5` is 20/20, head is 20/20 — three distinct `libscrip_rt.so` content hashes, so three real compilers. **This is not the rung-2 regression this lane spent days predicting; it is older than the deletion and nobody had swept for it, because no board runs under stress.** ⭐ **A configuration no board runs is a configuration no board defends** — it surfaced only because a different row needed stress to grade its own gate. Row `prolog-twelve-master-entries-sigsegv-under-gc-stress-in-the-call-args-root-walk`, rank 0, DONE-WHEN proven red as written.

**THE ROW STAYS OPEN AND THE GATE SHIPS UNWIRED, BOTH DELIBERATELY.** Gate clause 2 (20 runs per mode under stress, byte-equal to a swipl ref cut at run time) is red on that crash, for a reason that has nothing to do with atom names — so wiring it would red the blocking set over another row's defect, and **red-on-arrival is not a gate**. The `gate_wiring.tsv` RULING is signed and says in writing that it **expires with that row**.

⛔ **ONE MISTAKE OF MINE, RECORDED BECAUSE IT NEARLY COST THE ATTRIBUTION:** the first attribution run was piped through `tail -10`, which discarded the `pre` and `post` lines and left only `head` — the truncated-listing failure, committed *while measuring*. Re-run without the pipe. **A measurement that scrolls off is a measurement that did not happen.**


## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-16 hq_prolog — **THE cto'S FRAME RUNGS 3(e) AND 3(d): THE PROLOG ARMS** (CTO-59 / CTO-60; per-case red NAME sets, both modes, master + SWI)

**RUNG 3(e) — DISCHARGED, FLAT, ZERO FLIPS.** Base `3a530b12e` vs head `d5f59c9ad` (*a straight temp read only by one sealed det leaf's operand j is granted the argv block's slot j; r/1 272 -> 224, prelude 30208 -> 25424 B*), one scratch clone, each arm built with its own `make` and graded before the next checkout.
- **Prolog MASTER**, 16 shards, m3+m4: base `total=563 m3_pass=542 m3_fail=21 m4_pass=459 m4_fail=11 all_pass=542` — head **identical in every bucket**, crash=0 hang=0 both sides.
- **SWI package board**, m3+m4, per case: base and head both `declared=2935 graded=2858 m3_pass=1090 m3_fail=1768 m4_pass=1046 m4_fail=1812 ungraded=77`; **red NAME sets 2000 = 2000 with a diff of 0 lines** — the same ones red, not merely the same count. ⭐ This is the form the cto asked for and the only form that can see a SWAP; a scalar floor cannot.

**RUNG 3(d) — DISCHARGED, FLAT, ZERO FLIPS, FOLDED WITH 3(e) ON THE cto'S OFFER.** Base `3a530b12e` vs head `987a8ea74` (*the dead-result scratch overlays a pool slot when no dead write lands inside a pooled interval; r/1 224 -> 208*), same clone, same instrument, each arm built by its own `make` and graded before the next checkout.
- **Prolog MASTER**, 16 shards, m3+m4: `total=563 m3_pass=542 m3_fail=21 m4_pass=459 m4_fail=11 all_pass=542` on both sides, crash=0 hang=0, **and this time the per-case NON-PASS NAME SET was diffed too — 42 = 42, diff 0 lines** (21 m3 FAIL, 11 m4 FAIL, 10 m4 SKIP where `--compile` refuses; the 3(e) arm graded master by totals alone, which is the weaker instrument and is now upgraded).
- **SWI package board**, m3+m4, per case: `declared=2935 graded=2858 m3_pass=1090 m3_fail=1768 m4_pass=1046 m4_fail=1812 ungraded=77` both sides; **red NAME sets 2000 = 2000, diff 0 lines**. Concentration unchanged either side: `core/test_arith.pl` 320 · `core/test_dict.pl` 176 · `core/test_locale.pl` 170 · `core/test_write.pl` 116 · `core/test_acyclic.pl` 85.
- **VERDICT TO THE cto: the batch `3a530b12e..987a8ea74` costs the Prolog lane nothing on either board, in either mode, per case.** Both frame rungs, the collector's eight-arm deletion and five other landings clear together.

⛔⭐ **THE INSTRUMENT DEFECT I FOUND IN MY OWN ARM, AND IT WOULD HAVE BITTEN ANY SEAT WHO COPIED IT.** I added an md5 stamp of the built binary to this arm precisely to catch the cto's 2026-09-14 class (*a stale artifact under a refusing compile is indistinguishable from success*) — and it **printed the same hash on both arms**, which reads exactly like the staleness it was installed to detect. It was not staleness: I stamped **`scrip`**, which is a thin driver (`src/driver/scrip.c` + `-lscrip_rt`), and `scrip.c` is untouched across every SHA in this batch. ⛔ **The compiler is `out/libscrip_rt.so`, not `scrip`** — `frame_layout.c` (49 insertions / 21 deletions across this batch) is linked into the `.so`, so on ANY arm that does not touch the driver, an md5 of `scrip` is identical by construction and answers a question nobody asked. ⭐ **This is the `command -v` shape again**: an instrument that answers something narrower than you think you asked, and never says so. The stamp that means what it looks like:

```bash
md5sum out/libscrip_rt.so        # the compiler; a stale arm shows up HERE
ls -lt out/attic/*.so            # the build already content-hashes it: libscrip_rt-<cfg>-<content>.so, 8 newest BY CONTENT
```

⭐ **MEASURED RATHER THAN ARGUED, ALL THREE TREES REBUILT FROM THE SAME CLONE:** base `3a530b12e` → `f49522a6eff9`, rung 3(e) head `d5f59c9ad` → `20690caba1bf`, rung 3(d) head `987a8ea74` → `138e90dfb3b0` — **three distinct compilers, so both arms really did grade two different trees**, which also closes the same hole in the 3(e) arm after the fact. And the base rebuilt **byte-identically to an attic entry from six hours earlier**, so the hash is a sound identity and not a per-build nonce; `3a530b12e` is itself source-identical to rung 3(a)'s head `946630f40` but for one Makefile line, which is why its content was already in the attic.

⭐ **WHY THE BASE ARM IS RE-MEASURED RATHER THAN REUSED.** `3a530b12e` was already graded 35 minutes earlier in the 3(e) arm, on the same clone and the same SHA. `corpus` was pulled (`fe66993c8` -> `688d9a93f`) between the two, and the REBASE-BASELINE COROLLARY voids an arm across a pull whether or not the pulled files look relevant — they were all `.s` artifacts, which is exactly the kind of "obviously harmless" that the rule exists to stop being argued. Seven minutes of build is cheaper than a delta nobody can quote.


## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-16 hq_prolog — **AN ASSERTED CLAUSE'S ATOM NAMES LEAVE THE COLLECTED HEAP; AND A CURE THAT WAS GREEN ON ITS OWN TREE WAS WRONG ON THE NEXT ONE** (row `prolog-an-asserted-clauses-atom-argument-comes-back-empty-or-as-another-atoms-name-after-a-collection`, CLOSED; SCRIP `1801b547e`)

**THE DEFECT WAS A WRONG ANSWER, NEVER A CRASH.** Under `SCRIP_GC_STRESS`, `call(p_N(X))` SUCCEEDED and `X` came back as the **empty atom** for nine of ten clauses and as the atom `p_10` — **another atom's name** — for the tenth. Nothing in the tree noticed it for as long as it existed.

**THE MECHANISM, AND IT IS NOT THE CLASS THE ROW WAS FILED UNDER.** An asserted clause is **recompiled**: `rt_pl_db_recompile` → `rt_pl_clause_tree` → `pl_cell_tree` walks the stored cell into an AST, and every name in that AST was an `rt_heap_strdup_c` copy — **the collected heap**. Those blocks hang off `calloc`'d `tree_t` nodes and off the code the recompiler emits; `pl_gc_roots` walks the atom table and **nothing else**, so no root scan reaches them. gdb on `rt_heap_strdup_c` shows **ten separate copies of the one argument atom, one per clause**, which is why nine read empty and one read a reused block — a single shared pointer cannot produce two different wrong answers. ⭐ The mechanism also **predicts all three ingredients** that were ablated days before the diagnosis existed (run-time-interned functor → only that triggers a recompile; bound **atom** argument → an integer lowers to `TT_ILIT` and stores its value inline, with no pointer to dangle; two passes → interleaving leaves no collection in between). **The pinned atom table was never the victim**, so the neighbouring 71-C-locals row's premise is untouched.

⛔⛔ **THE ENTRY WORTH MORE THAN THE PATCH: MY FIRST CURE WAS GREEN AND WRONG.** I interned the names through the atom table. Gate 20/20 green in both modes, master board flat at 542/459, eight smokes green, preflight 50/0 — on `3a530b12e`. I rebased onto the tree carrying the **CEO-799 pinning deletion** and **the same cure read RED again**, 1 of 10 instead of 10 of 10: it had traded a *reclaimable* block for a **movable** one, and a raw `char *` into a movable block is not a registered slot. ⭐ **A cure graded only on the tree it was written on cannot see the assumption it inherited** — here, "pinned means does not move", which another seat's landing had already deleted. **Every instrument I had said green; only the rebase-baseline rule caught it.**

**THE CURE THAT HOLDS:** the AST is `calloc`'d, so its names are `strdup`'d beside it — same allocator, same lifetime, no collected-heap pointer, no dependence on pinning, so a future collector change cannot undo it. Six sites through one helper. **Measured on the merged tree** (base `3f7e85b9d`), re-proved again after a second rebase before push: gate RED 20/20 → GREEN 20/20 both modes against a ref cut from **swipl at run time**; master **542/459 of 563 base and head**, every bucket, both modes, nothing traded; preflight **50 arms, 0 red** (it read 49/50 fleet-wide while this gate was unwired); smokes green on all eight frontends. **The gate is WIRED BLOCKING in the same commit** (CEO-803) — its own `gate_wiring.tsv` RULING said *"THIS RULING EXPIRES WITH THAT ROW"*, and it did.

**CONTROL ARM OWED TO THE cto, DISCHARGED:** frame rung 3(a), base `de6107c87` vs head `946630f40`, **flat and zero flips** — master identical in every bucket both modes, SWI board per-case identical (**2000 red names each side, diff 0 lines**: not the same *number* passing, the same *ones*). Rung 3(e) arm in flight.

**TWO REDS MINTED FROM THE coo'S LOST LIST**, both pre-existing defects a correct cure uncovered: `arg/3`'s ISO guard tests `k.v != DT_I` so an unbounded index raises `type_error(integer)` for a term `integer/1` accepts (`by_name_dispatch.c:8810`, same shape at `functor/3` 8818 and `sub_atom/5` 8804) — and ⭐ its old PASS **was never real**: the negative bignum literal used to clamp to `-LLONG_MAX`, taking the sign branch and throwing the right error *functor* on the wrong value, which a kind-and-formal grader scored PASS. And `copy_term/2` does not terminate on a rational tree (`X = f(X), copy_term(X,Y)` → ERROR 246, both modes) while `acyclic_term/1` answers `cyc` correctly on the same term.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-16 hq_prolog — **LON'S RULING ON THE FENCED GAMMA: WHACK FREE, AND LEAVE A SURROGATE SPINE CELL BEHIND FOR ANY BETA LANDING IN THAT BB** (row `prolog-rung-9-an-opaque-goal-barrier-retains-the-frame-its-goal-carved-whack-it-at-gamma`)

**Lon, 2026-09-16, in-chat to hq_prolog, VERBATIM:** *"Right. Treat a FENCE'd GAMMA as opportunity to WHACK FREE. You can also place a surrogate SPINE cell in place of the whacked part to keep consistency on any BETA landing in that BB."*

⛔ **THIS RATIFIES THE RUNG-9 CURE'S DIRECTION AND ADDS A SECOND HALF TO IT.** The first sentence confirms CEO-690: a fenced gamma is licence to release, not merely permission to stop retaining. The second sentence is NEW LAW for this lane and it changes the cure's safety argument at the root.

⭐ **WHAT CHANGES, AND WHY IT IS NOT A DETAIL.** The cure as it stands rests on an ABSENCE: for an opaque barrier, beta into the goal is unreachable *by the lowerer's own wiring*, so releasing at gamma is safe because nothing can recede into the released region. That argument is correct and it is also FRAGILE IN EXACTLY THE WAY THIS REPO KEEPS GETTING BITTEN — it is a claim about what cannot happen, held in one lowerer, and it already failed once in flight: hq_U shipped an unconditional release, the SOFT CUT `(*->)/2` turned out to be backtrack-TRANSPARENT into its condition, and the release whacked frames a redo was about to use (cured at `4add22403` by making opacity travel as a bitmask bit). **Lon's surrogate cell replaces the absence with a PRESENCE:** the whacked region leaves a well-formed spine cell standing in its place, so a beta landing in that BB meets a consistent spine instead of released ground. The invariant stops depending on nobody ever wiring a beta edge there.

⛔ **THE DESIGN CONSTRAINT THIS PUTS ON THE RUNG.** The release is `mov rsp, <banked frontier>`; the surrogate must be carved at that same site so the BB's beta landing is structurally valid whether or not the lowerer believes the edge is reachable. It rides the SPINE (ζ-SPINE, RSP) like every other cell in that BB — **it is not a new heap allocation and not a `Term`**, which the standing design law for this lane forbids outright (Lon s273: Prolog uses `DESCR`, the heap arm is struck, only string values go on the heap).

⛔⛔ **BUILT, MEASURED, AND THE LITERAL READING IS REFUTED — 2026-09-16, same session.** The second sentence has two readings and they differ by the entire cure. **READING A (literal, BUILT AND MEASURED):** after the release, carve a 64-byte cell in the frame-header shape `xa_flat` seeds, `F.CUR`/`F.RES` zeroed so it can only be conceded through. It emits correctly (8 well-formed cells on the probe; killswitch `SCRIP_PL_SURROGATE` total) **and it costs the fence almost everything** — on the frozen witness set, m3, stack pinned 8192 KB: `once(q(_))` 400000+ reclaimed → **9153 / 916 B**, the if-then condition the same, `forall/2` → **7567 / 1108 B**, `\+` → **9153 / 916 B**, against **7348 / 1141 B before any cure at all**. The DONE-WHEN gate reads **10 of 11 arms FAILED**. ⭐ **The cost is NOT the 64 bytes the cell occupies.** Leaving `rsp` 64 below the banked frontier means each recursion level carves below the previous level's surrogate, so the release stops being a release. **A surrogate that PERSISTS on the spine cannot coexist with a fence whose whole purpose is to leave the spine empty** — the two instructions are in direct tension and the tension is arithmetic, not stylistic. Kept on branch `hq_prolog-pl-rung9-surrogate-experiment` `7a29b0050`, **marked DO NOT LAND**, because the measurement is the deliverable.
**READING B (not built, and the one that survives the arithmetic):** the surrogate lives at a FIXED offset in the BB's own activation frame — the widened `IR_BOUND` unit already carries a spare slot at `off+24` — so the BB's beta landing reads a well-formed cell at a compile-time offset and the spine stays empty. Costs zero additional spine per level, which is the only way "keep consistency on any BETA landing" and "whack free" can both hold. ⛔ **UNRESOLVED AND REFERRED UP, not guessed at:** which reading Lon means decides a rung, and this seat does not get to pick by preference. Referred to Lon and routed to the cto the same session.

**STATE AT THE TIME OF THE RULING:** the whack-free half is cured, rebased onto `a4e800207`, narrowed so Icon emission is byte-identical (242/242), pushed as `origin/hq_prolog-pl-gamma-fence-narrow` `6f2dd2ba6`, and sitting with the cto as a SHARED-NODE ASK because `frame_layout.c`/`emit.h`/`bb_bound.cpp` are the spine and a shared box. **The surrogate-cell half is NOT built.** It is the rung's next piece of work and it is recorded here before it is started, so the ruling survives this seat.

⛔ **ROUTED TO THE cto THE SAME SESSION**, per the override law: a Lon ruling the officer never hears about leaves dispatch running against a world that no longer exists, and this seat is the only witness.

## ⭐⭐ LIVE CURSOR — 2026-09-06 seat10 (FLEET-12, hq_R lane) — **$ax_eguard: arithmetic-evaluation exceptions (is/2 + all six comparisons) are now catchable by catch/3**

- **ceo `ceo-370` (2026-09-06 19:38 CDT):** the three INRIA `functor-bis` cells whose percent-comment contradicts their machine-readable expectation (hq_R FINDING 2026-09-06; SCRIP correct on all three, one is the ISO `'.'/2` vs SWI `'[|]'` split) are graded on ISO/IEC 13211-1 through a NAMED `OUTCOME_ERRATA` sibling of `KNOWN_SUITE_ERRATA` in `test_prolog_inria_suite.sh`, printed beside the PASS count as ISO-graded errata; the vendored file is never edited; the denominator stays the printed 445 — row `inria-three-functor-bis-cells-graded-on-iso-through-a-named-outcome-erratum-sibling-of-known-suite-errata`, hq_R rank 1, hq_T co-signs. 445/445 is reachable.

Row `prolog-inria-arithmetic-errors-escape-catch3` (hq_R dispatch, minted by seat05 2026-09-04, re-laned
hq_C→hq_R 2026-09-05). SCRIP `79c3738cc`, corpus `ef068af9f`. ROOT CAUSE: `pl_iso_evaluable()` /
`rt_pl_iso_throw_*()` (`by_name_dispatch.c`) raised `type_error(evaluable,...)` and `instantiation_error` via
`pl_iso_uncaught()`, which does `fprintf`+`exit(1)` directly — a completely separate mechanism from the real
Byrd-box ball/catch machinery (`rt_pl_throw_raise`/`rt_pl_exist_raise` in `rtx_plunify.s`: build a ball, stash
it in r15, return a MOD_OP-tagged `DT_FAIL` for `rt_pl_catch_handle` to intercept — confirmed via gdb that
`undef_pred`'s `existence_error` genuinely takes that path, never `pl_iso_uncaught`, which is why the task's
own contrast witness always passed while the arithmetic one never did). The six comparison operators
(`>,>=,<,=<,=:=,=\=`) with a non-numeric operand raised **no error at all** — a distinct, ISO-incorrect
silent-fail bug, not merely uncatchable.

FIX: new guard box `$ax_eguard` (`rt_pl_dop_ax_eguard`/`_c`, `MOD_OP_RT_PL_AX_EGUARD=156` in
`descr_tags.inc`, registered 1-arity in `bb_call.cpp`) follows the EXISTING `$ax_zguard`/`$pl_anum_guard5`
idiom byte-for-byte (already used ~9 times in this file) rather than inventing new machinery: a `void*`
C guard returns a ball or NULL, the asm wrapper sets r15 and returns a tagged FAIL on non-null. Balls built
via a new `rt_pl_ball_evaluable()` (mirrors `rt_pl_ball_existence`'s PI-building) and the **already-existing
but previously dead** `rt_pl_ball_instantiation()` — nothing called it before this row. Spliced as a pure
control-flow gate (operand lists untouched, exactly how `$ax_zguard` never disturbs its enclosing op's
operand list) into `is/2` and all six `$cmp_*` lowerings in `lower_prolog.c`, guarding each operator's
fully-reduced top-level value before the real op runs. Zero new globals, zero AST/SM walking.

VERIFIED: task DONE-WHEN (as minted) passes; INRIA suite (`test_prolog_inria_suite.sh`) m3/m4 outcome-class
332→342, bindings 326→336 (**+10/+10, both modes**, measured via `git stash` before/after on one tree, zero
regressions either direction); 13-witness manual battery (contrast existence_error, zero_divisor, normal
arithmetic/comparisons, compound-term type_error) all matched expectations; `nm -D` clean of
`g_pl_*`/`g_plw_*`/`g_resolve_*`/`g_rt_pl_*`/`pl_wot_*`; `strip_comments.py --check` rc=0; owed `.s` artifacts
regenerated (`bb_call.cpp` is codegen) — `benchmarks/prolog/bench/*.s` (20 changed) +
`demos/prolog/{family_prolog,family_net/family_prolog,prolog_recognizer}.s` — with SNOBOL4/Icon demo `.s`
independently reporting "same" for every entry (zero cross-language blast radius, confirmed twice: by file
scope and by the regen diff itself).

⛔ **NOT CLOSED — nested arithmetic operands still bypass catch/3** (`X is Y+1` with `Y` unbound, `X is
1+foo`): `dop_ax`'s internal per-operand checks (`by_name_dispatch.c:1252,1274,1279`) still call
`pl_iso_evaluable()`→`exit(1)` directly, and those `rt_pl_dop_ax_*` leaves (add/sub/mul/…/bnot) are called
DIRECTLY from generated code with no asm wrapper (unlike `$is_v`/`$cmp_*`) — safely setting r15 from inside
them needs either new asm wrappers for all ~30 `$ax_*` leaves, or a per-child `$ax_eguard` splice inside
`lower_arith_val`'s FNC-branch loop (same proven technique, not attempted this row to keep the diff reviewable
rather than guessing at r15 safety inside arbitrary `-O0` C). **This exact case is ladder rung18's own
`erriso_instantiation_error_1` witness** (`corpus/tests/prolog/ALL.pl` #368) — still RED, rung18 unchanged at
PASS=6 FAIL=8 (matches seat08's pre-this-row measurement in `LADDER.tsv` row 69) — independently documented in
`FINDING-2026-09-05-seat08-prolog-rung18-three-iso-error-compliance-gaps.md` (routed to hq_R). That finding's
other two causes (domain_error via `open/3` silently failing; `syntax_error_on_read` via `read/2` silently
returning a wrong term) are separate root causes, not arithmetic-shaped, untouched by this row. Task file's
own `## NEXT` carries the full handoff; its `DONE-WHEN` was raised to the nested-operand witness so the row
cannot auto-close via ALREADY-SATISFIED-AT-DISPATCH before the broader GOAL is met.

⚠ **Unrelated, pre-existing, out-of-lane reds noticed while gating** (not fixed here, not this session's
regression — confirmed by file-scope: this diff touches zero SNOBOL4/Icon files): `make test` currently reds
on `test_gate_sno_pos_rpos_dynamic_operand` (`pos_dyn_inline`, wired red-not-cured 2026-09-05 per that gate's
own header) before reaching any Prolog-relevant arm in the sequence; `test_corpus_snobol4.sh` shows m3/m4
FAIL=1, matching the ceo-documented `code_eval_len_table_replace_1` XDump/`-INCLUDE` defect named in that same
gate's header. Flagged for hq_S/hq_P/hq_T's lane.

## ⭐⭐ LIVE CURSOR — 2026-09-06 seat09 (FLEET-12, hq_C lane) — **term_variables/2 + current_predicate/1 LAND (rung17, 3/5 -> 5/5 declared FORMS)**

Row `prolog-rung-17-term-variables-current-predicate-number-chars-halt` (hq_C dispatch). SCRIP `8012d886a` (rebased onto
`1f5f20a70`; corpus unchanged `db275702f`): the two forms seat08 left RED 2026-09-05 (`FINDING-2026-09-05-seat08-prolog-
rung17-term-variables-and-current-predicate-missing.md`) are now GREEN both modes — `test_prolog_ladder.sh --only 17`
PASS **10/10** (witness×mode), up from PASS 6/10; all 5 declared FORMS witnessed. **Design note, since the baton asked
for one before coding:** `term_variables/2` wires an existing but never-called helper (`rt_pl_term_variables_cell`,
`unification.c:1080`, zero callers before this) through the ordinary `dop_direct_fp`/`PL_CTX_LEAF` det-leaf path
`copy_term/2` already uses — fixed its unify to go through `plc_unify_cells_cx` (trail-aware) instead of bare
`pl_unify`, since it was about to get its first real caller. **`current_predicate/1` is NOT the generator
(`bb_call_builtin_gen`) the baton's GOAL line speculated** — that line's "rung-13-shaped" citation was itself off (rung
13 is BX-DET/determinism, not a generator rung; the real generator precedent is rung 7's `bb_to`/`IR_TO`, confirmed by
two independent forks). Cheaper and exactly on the codebase's own established pattern instead: since this compiler
requires every predicate head to be compile-time-literal already (`assert`/`retract`/`abolish`/`clause`/`nb_setval` all
refuse a non-literal head today), the FULL set of name/arity pairs that can ever exist is already known at lower time —
so for a literal `Name/Arity` argument (`pl_spec_key`, `abolish/1`'s own helper) the call collapses to an unconditional
`IR_SUCCEED`/`IR_GOTO ω` from `pl_file_defines`/`pl_dyn_index`, zero new box, zero runtime table walk. A non-literal
argument still refuses citing rung 7, matching `clause/2`/`retract/1`'s existing convention; ISO 8.8.2's general
backtracking-over-every-predicate mode stays unimplemented. Both graded witnesses (`misc17_term_variables_1`,
`misc17_current_predicate_1`) match their swipl-cut refs byte-for-byte, both modes, checked by direct execution before
trusting the ladder runner's verdict. ⛔ **DONE-WHEN arm 3 (`test_gate_pl_port_trace.sh --to 17`) still reads GATE
UNPROVEN(2) at rung10's `global_vars_b_setval_getval`** — reproduced on a clean pre-this-row tree too (`git stash`),
so it is not this row's regression. This is the SAME defect hq_C ruled on today for the twin rung15 case (`FINDING-
2026-09-06-seat09-prolog-port-trace-gate-misreports-unproven-on-compile-time-refuse-witnesses.md`, row `port-trace-
zero-lines-check-cannot-tell-a-compile-time-refusal-from-a-tracer-that-is-not-firing` minted to hq_T): "YOUR ROW STAYS
PARKED AND THAT IS CORRECT." Corroborating, not re-diagnosing. Row parked `BLOCKED-ON` that same topic (self-clears
when hq_T lands it); claim released. **Whoever resumes rung17 fully:** nothing left in this lane's own scope — the
only remaining gap is the shared port-trace instrument, hq_T's. number_chars/2 and halt/0,1 were already landed
2026-09-05 by seat08 and untouched here.

Row `prolog-char-conversion-2-and-current-char-conversion-2-entirely-unimplemented` (hq_C dispatch, OWNER hq_R per the baton), from seat08's rung16 term-I/O FINDING (`FINDING-2026-09-05-seat08-prolog-rung16-term-io-three-missing-builtin-groups.md`, group 3). **SCRIP `af8712f3d`** (on `36e277d17`): both builtins lowered in `lower_prolog.c`'s `goal()` as a compile-time rewrite into existing primitives (`var/1 atom/1 atom_length/2 asserta/1 throw/1 ->/; =/2` + a plain call) over a compiler-internal dynamic predicate `$pl_cconv/2` — reuses the already-landed rung-10 assert/retract machinery, zero new C globals, zero new runtime functions or box/template code. Task DONE-WHEN GREEN both modes; real ladder witness `termio_current_char_conversion_1` PASS/PASS (was FAIL). **Two explicit NOT-done items, in the task baton's `## NEXT`, for whoever wants full depth here (new row, not a reopen):** (1) char_conversion/2 does not hook the runtime term-reader yet (reflection table only) — the real ladder witness `termio_char_conversion_1` exercises exactly that AND is separately blocked by the pre-existing, out-of-scope `read_term/3` gap (rung 6, seat08's item #4, cross-referenced not re-filed); (2) `current_char_conversion(X,Y)` fully-unbound enumeration can show a stale duplicate after re-converting the same character (direct lookup is always correct, newest-first via `asserta`) — traded deliberately against adding a new C global or generator/box plumbing for a two-builtin reflection feature. rung16 term-I/O is now 2 of 3 groups landed (`write_term/2` and `current_op/3` remain, per seat08's groups 1/2, not this row).

## ⭐⭐⭐ LIVE CURSOR — 2026-09-04 17:08 CDT ceo (FLEET-16) — **LON'S DIALECT RULINGS: THE DEFAULT IS ISO + THE GNU/SWI-SHARED CORE; SWI-ONLY AND GNU-ONLY SURFACE EACH LIVE BEHIND A COMPATIBILITY SWITCH** (16:59 verbatim: *"ISO plus the GNU/SWI-shared core only."* · 17:03 verbatim: *"We'll want a compatability switch, one for SWI and one for GNU."*; GOAL-CEO CEO-235/236)

THE DEFAULT dialect is ISO 13211-1 + the GNU/SWI-shared core. SWI-only surface (dicts + dot-access, SSU `=>` rules, quasi-quotation, CLP(FD), rational literals) is accepted only under the SWI switch; GNU-only surface (`g_assign`/`g_read` globals, `fd_*` constraints, GNU-specific builtins) only under the GNU switch; the default keeps refusing both exactly as today — the switch ADDS, the default never widens. ROW `prolog-dialect-compat-switch-swi-and-gnu` (hq_C, rank 1, DONE-WHEN computed with inline witnesses cut from swipl and gprolog at run time, RED at mint 17:08 on all six arms): (a) a driver option `--compat=swi|gnu` (spelling hq_C's, the DONE-WHEN drives it) selecting grammar productions and the builtin table at PARSE/LOWER time — language identity stops at lower (`test_gate_emit_no_lang.sh`), no dialect test past lower, no dialect global (the value rides the parser configuration the driver already hands the frontend); (b) `test_prolog_swi_suite.sh` runs under `--compat=swi`, `test_prolog_gnu_suite.sh` under `--compat=gnu`, the INRIA suite and the ladder under the default; (c) the SWI-only class rows (`prolog-swi-class-{dicts-dotaccess-quasiquote,clpfd-infix-operators,ssu-arrow-rules,rational-number-literals}`) hang BEHIND it and are cured under the switch; (d) denominators unchanged — swi_tests 249 under the SWI switch, gnu_prolog 62 under the GNU switch. THE 16:59 FORM IS WITHDRAWN (it stood four minutes; hq_C was told to HOLD at 17:06 before acting): no per-file dialect exclusion, no retirement of dialect class rows, no named exclusion line. Unchanged: OUT-OF-SCOPE-MEANS-NOT-OWED applied for those four minutes only to what was never torn out — the module export-list `op/3` class landed at 16:56 (SCRIP `a60908330`) and stays.

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 hq_C (TRIO, s286) — **RUNG 0 LANDED: THE CUT (26 → 0 exported Prolog-only globals, −5,050 lines) + HELLO WORLD AS ONE CALL BOX WITH THE § A.1 HEADER; NEXT IS RUNG 1**

**Landed and pushed, one push from origin:** SCRIP `db299d41` · corpus `9c648987` (rung-0 trace refs re-cut with hq_B's other blocks kept; 23 `.s.REFUSED` markers replace the pre-cut Prolog bench assembly) · receipt `FINDING-2026-09-02-hq_C-rung-0-lands-the-cut-deletes-every-prolog-only-global-26-exports-to-0-and-hello-world-runs-as-one-call-box-with-the-header-at-the-top.md` · § A.1's table now carries the measured H-relative header (`H = rbp + kt - 64`: TRMARK CUR RES B0 pad γ ω caller-rbp; the root graph seeds `r13=0 r15=0 r14=H`). Verdicts (pristine `-O0`): ladder `--to 0` PASS 2/2 · trace gate `--to 0` PASS(0)/2 · `nm -D` 0 · SNOBOL4 + Icon control-arm `.s` byte-identical · `make test` green · master board REPORTED 6/383 both modes (pre-cut 348/371) — every construct above rung 0 REFUSES at compile naming its rung.

**Whoever resumes starts HERE:** rung 1 (`ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E: fact with arguments + rule body, `p(a,b). main :- p(X,_), write(X), nl.`; the witness is `ladder__rung01_*` in the master, refs and traces already cut by hq_B) — head unification binds `F.A[i]` on the spine through `IR_CALL "$unify"` → `rt_pl_dop_unify` (`rtx_plunify.s`), the log rule of § B.11 via `r13` inside `plw_bind`; `pl_refuse` names rung 1 for `=` and head arguments today. Gates per push: `test_prolog_ladder.sh --to 1`, `test_gate_pl_port_trace.sh --to 1 --cut` then `--to 1`, `nm -D` 0, SNOBOL4 FAIL=0 + Icon watermark, `strip_comments.py --check`, `make test`. ⛔ Not for this seat: rungs 6–8 (hq_P), 13 (hq_B), Icon, the SNOBOL4 `SCRIP_OPT=0` crash of `5839cf13` (ceo).

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (TRIO, second account, Lon 16:55–17:05 CDT box clock: *"Let's get Prolog to 100%."* · *"Just start climbing the rungs from simple syntax to more complex syntax as you go along."* · *"Get back on Prolog now that we are cooking with it."*) — THE CEO CLIMBS RUNG 6 WHILE hq_C CLIMBS 2–5

**State at this line:** rung 0 `db299d41` and rung 1 `7d0ed0f8` on origin, rung 1 AUDITED GREEN by the ceo on a pristine build (DONE-WHEN rc=0, ladder `--to 1` 4/4, trace gate 0 failed, quad gate 0 unenrolled, `make test` rc=0, 17:00–17:09). REFUSAL CENSUS (ceo, `7d0ed0f8` · corpus `607bcf92a`, every master entry `--compile`d, FIRST refusal per entry, 383): 51 compile and pass m3; 332 refuse — rung 2: 78 · 3: 9 · 4: 3 · 5: 44 · **6: 132** (`is/2` alone 42, halt 8, format 8, term_string 6, writeq 5, numbervars 5) · 7: 2 · 8: 4 · 9: 35 · 10: 25. A first refusal is a lower bound: an entry naming rung 6 may also need 2–5; the census is re-run after every landing and the delta is the receipt's number. **Assignment (ceo ruling on Lon's order):** hq_C rung 2 (ASSIGNED, design settled, no `src/` edit at 17:10) then 3, 4, 5 in order WITHOUT waiting for a ceo audit between rungs (audits run on the pushed tree; a red sample reopens by telegram); **ceo rung 6** (`prolog-rung-6-det-builtin-leaves-is-compare-type-tests-write-family-conversions`, rank 0, ASSIGNED ceo; § E row 6 owner moved hq_P → ceo) in the order inside the rung: (1) `is/2` + arithmetic comparisons → (2) `==`/`@<` family, `compare/3`, type tests → (3) write family + `format/1,2` → (4) atom/string/number conversions → (5) `functor arg =.. copy_term numbervars succ plus sort msort` → (6) `read get_char peek_char`; each a compile-time-wired det leaf through the shared `bb_call` box (`dop_direct_fp` table), binding leaves through one rtx trampoline each (`rt_pl_dop_<name>` → `_c` with the trail ctx on `r12`/`r13`), enrolled by NAME in the quad scanner; the surviving `rt_pl_*_cell` bodies of `unification.c` bind through `plc_unify_into_cell_cx`/`plc_unify_cells_cx`/`plc_bind_cx` (ctx NULL = the old untrailed path for the rung-7/8 machinery still to be re-derived); hq_P: `--only N` landed `b8cbe837`, rungs 7–8 the moment rung 2 is on origin, reviews rung 6 on the rail; hq_B: the six rung-6 witnesses landed corpus `6936651c` (its group (d) carries `sub_atom/5`, a rung-7 generator by § E — routed back to hq_B), rungs 7–13 witnesses next, then B-5/B-4. **RUNG 6 LANDED (ceo, ~18:15 CDT): SCRIP `943c1b8c` (step 1) + `81b40ceb` (the rung), corpus `bbf299d4` (the six witnesses leave XFAIL, proven by the board in the same commit), receipt `FINDING-2026-09-02-ceo-rung-6-lands-every-deterministic-builtin-as-a-direct-leaf-binding-through-one-rtx-trampoline-each-the-master-board-moves-50-to-169-of-400-both-modes.md`.** Ladder `--only 6` PASS 12/12; fresh `--compile` of the six witnesses rc=0 with 0 `rt_call_arr_bl`, 0 `_rkfn`, 0 Prolog-only globals; REPORTED master board **169/400 both modes** (from 50/383); census first refusals now rung 2: 83 · 3: 9 · 4: 3 · 5: 52 · 6: 1 · 7: 2 · 8: 5 · 9: 43 · 10: 30 (172 of 400 compile, 171 pass m3). The row's DONE-WHEN (`--to 6`) closes when hq_C's 2–5 land; the rung-6 `ALL.trace` blocks wait on a `--only N` arm for the trace gate (hq_P). **Rulings for hq_B, routed here:** (1) the master's oracle is swipl 9.0.4 with default flags — `**` and `^` on integers yield an integer, `/` on integers yields an integer when exact; ISO/gprolog `1024.0` is not the target of the master (GNU Prolog remains a rival on the benchmark grid, not the ref oracle); (2) a ref the oracle recipe cannot produce is not a ref: `simple_program_97` (no initialization directive, 4-line ref) is re-cut under the recipe (empty output, rc 0) by hq_B, and if the origin family's intent was to run `main`, adding the directive is a corpus change hq_B may make where the family's siblings carry it — never a second run mode invented for one entry. **Corpus pin datum for hq_B:** the three `*power*` master entries pin `2 ** 10 = 1024.0` (gprolog-era) while swipl 9.0.4 prints `1024`; no ref edited by the ceo.
**RULINGS 2026-09-02 18:50 CDT (ceo, on hq_B's wrap correction; the record is CEO-151):** (a) ruling (2)'s population is 59 directive-less entries with non-empty refs, not 1; the 55 that reproduce their ref byte-exactly under `-g main` TAKE `:- initialization(main).` in one board-proven commit (hq_B); the recipe `-q -g halt` STANDS — `-g main` would double-run the 203 directive carriers. (b) `simple_program_97` is the third `**` float pin → re-pin like corpus `62dc7995`. (c) `simple_program_103` → re-cut under the recipe (swipl 9.0.4 IS the oracle; gprolog-permissive `atom_to_term/3` is not a target). `functor_2` and `copy_term_ite_list_replace_1` print fresh variable names, implementation-defined by construction → THE ENTRY is rewritten to print a stable fact (`numbervars/3` before printing, or `var/1`) and re-cut; XFAIL only if the rewrite would change what the entry tests, named in the commit. (d) RUNG 2 AUDITED GREEN by the ceo on pristine `81b40ceb` (18:35–18:46, DONE-WHEN rc=0). (e) THE CEO'S CLIMB is the rung-6 TAIL — the stream-arity leaf family over the driver `fh_` registry, `read_term/2` with `[]`, `keysort/2`, declaration directives as no-ops; plain directive goals are RE-LABELLED rung 5 (`ignore/1` at the root, file order before initialization goals, swipl's `Goal (directive) failed` warning — hq_C); `nb_setval/b_setval/flags` are ζ-STANDING slots keyed at compile time → rung 10 with the DB. Scope and witnesses at `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E rows 5/6/10. **(f) THE TAIL LANDED 19:45:** SCRIP `5d12c898` · corpus `16fb9da07` (on hq_C's rung 3 `9af884bd`), `--only 6` 20/20 over ten witnesses, trace gate `--only 6` PASS(0)/20, census 198 pass m3 of 400 (from 194), board REPORTED 194/404 both modes; receipt `FINDING-2026-09-02-ceo-rung-6-tail-lands-*.md`. Rung 3 is on origin and is the ceo's next audit. **(g) 19:55, on hq_C's rung-3 question:** the `ninit==0` run-`main/0` fallback in `lower_pl_stage2` is DELETED — SCRIP runs exactly what the master's recipe runs (`swipl -q -g halt`: directives in order, then initialization goals), nothing synthesized; the deletion lands as one board-proven pair with hq_B's 55 directive additions and the four stale `want_rc` pins (rows `prolog-directive-less-entries-run-main-but-the-oracle-does-not`, `prolog-master-seven-stale-xfail-markers-xpass-on-the-rung-3-tree`, hq_B). Rung 3 landed (hq_C `9af884bd`), F.HI restored at `[H+32]` as the log threshold word (§ A.1/B.5, hq_C's ruling, accepted). **(h) 2026-09-03 ~14:30, LON'S STANDING ORDER (verbatim at `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` head of § A): the wiring is the literature's, no WAM residue, the data in the three zetas — every rung receipt carries the WAM-residue census line; measured YES on `54536fbf`: zero Prolog-only globals, choice state in frame words, `r14` the ζ-STANDING root, `r12` the C.A.S. log under Lon's grant, heap only for compound cells and strings.**


## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (TRIO, LON ~13:40–13:55, box clock) — THE REBUILD FROM RUNG 0: THE CUT + HELLO WORLD WITH ZERO PROLOG-ONLY GLOBALS (hq_C), THE CONSTRUCT LADDER IS `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` § E, SHARED BOXES FIRST (§ B.0)

**Landed and pushed:** the zero-risk prep rung `prolog-dead-globals-and-bb-table-out-of-runtime-image` — SCRIP `d42d2918` (nine dead globals, the `g_zeta_mode` cmp out of the `$trail_mark` sink, `g_resolve_bb_table` 268 KB out of the runtime image), corpus `a0cca818` (prolog-bench regen), row DONE on the computed verdict; SCRIP `2748100d` (optbypass watermark 191→192 bisected to `5839cf13`, the gate prints its regressing list + wall clock). The translation page is REVIEWED and ceo has RULED on it (`.github` `59b37a74`, `e9b5d894`; review FINDING `FINDING-2026-09-02-hq_C-translation-page-review-the-base-header-lands-on-the-lexprep2-region-and-r12-is-the-live-cas-cursor-inside-every-snobol4-match-blob.md`): header at the frame TOP, `F.B`/r13 = the header address, r12 TR / r13 B / r14 ROOT / r15 BALL, r10/r11 untouchable, the trail in Prolog's own `rt_slab_region` (never the carve-only C.A.S. island), every retained-frame β tests r15 first (C9).

**RUNG 0 LANDED — SCRIP `db299d41` · corpus `9c648987` · .github `1f7e094f` (hq_C, 2026-09-02 ~15:55): `nm -D` 26 → 0, `plc_` 21 → 0, ladder `--to 0` PASS 2/2, REPORTED master 6/383 both modes, SNOBOL4 1679/1679, Icon `.s` byte-identical. NOW: **hq_C on `prolog-rung-1-fact-with-arguments-rule-body-conjunction-and-the-trail-log-rule`** (rank 0, ASSIGNED; the trail on `r12`, `F.TRMARK`, the log rule through `r13`, `bb_conjunction` shared with Icon; ceo rulings on the rung-0 judgement calls are in its baton and § B.0: the graph key is `zframe_pinned_base` + `root_graph`). The rung-0 order below stands as history. **Whoever resumes starts HERE, in this order (Lon's five rulings routed at § E; law `RULES.md` § THE PROLOG REBUILD GATE):** (1) **hq_C: `prolog-rung-0-the-cut-and-hello-world-with-zero-globals`** (rank 0, ASSIGNED hq_C) — step (a) THE CUT in one push: every § C global and the control machine that reads it deleted (list in § E; boundary: parser, DESCR cells, atom table, `rtx_plunify.s`, `dop_*`, shared boxes, BX-0 hook stay); step (b) hello world as ONE `bb_call_proc_staged` box with the § A.1 frame (header at the TOP, `F.B`=`r13`=H) and two `bb_call` leaves wired to `dop_write`/`dop_nl` at compile time, `IR_CALL_PROLOG` collapsed into `IR_CALL_PROC_STAGED`; step (c) the ladder runner `test_prolog_ladder.sh` if hq_B's has not landed first (both modes, REFUSE rc=2, denominator printed, failed-once-passed-once). DONE-WHEN in the baton. Then rungs 1–5 in § E order, one push each, from origin. (2) **hq_P:** the quad gate (no `r12`–`r15` write reachable from a Prolog graph outside the TR/B/ROOT/BALL helpers, emitted `.s` + `rtx_*.s`), then rungs 6–8 once rung 2 is on origin; the van Roy pin waits for rung 8. Also hq_P's after the cut: row `rtcc-veneer-drops-the-r9-gva-reload-when-the-gva-table-is-empty` (minted by hq_B, BLOCKED-ON rung 0; ceo ruling 2026-09-02 ~14:35) — hq_B's language-blind patch, saved outside the root, drops the `r9` GVA reload from the RTCC veneer whenever the compile's GVA table is empty (`gva_count()==0`; per-graph is unsafe because `rt_gen_spine_*` are mask-0 leaf calls), measured on `f4532dea`: nreverse `rtccb+48` reloads 168 → 0, SNOBOL4 1679/1679 both modes, Prolog smoke 5/5/5, Icon 14/14, programs with globals keep every reload; it is RE-PROVEN on the post-cut tree before landing, never merged from the pre-cut measurement. (3) **hq_B:** `test_prolog_ladder.sh` + the swipl `.ref`/`.trace` for the rung 0–5 witnesses into `ALL.pl`/`ALL.trace` (instrument lane, never `src/`), then rung 13. **Re-scoped rows:** `prolog-rung-2-0-rehome-scratch-r9-and-r12-off-prolog-reachable-templates` SUPERSEDED (the sink sites die in the cut; hq_B's two brief corrections kept at § E.2); `prolog-pz4-gamma-retain-activation-frames` and `prolog-pz5-in-frame-resume-delete-pending-mailbox` SUPERSEDED, folded into rungs 0/2; every row PARKED-AWAITING PZ-4 is closed by the rung receipt that cures it (the ladder names the row in its DONE-WHEN column), never auto-unparked. **While the cut is in flight (ceo ruling ~15:35):** hq_P and hq_B land nothing under `src/` until rung 0 is on origin — measure, park BLOCKED-ON rung 0, land scripts/corpus refs/.github; a proven `src/` patch becomes a row with the patch beside the baton, landed and re-proven from the post-cut origin (rows so parked: `rtcc-veneer-drops-the-r9-gva-reload-when-the-gva-table-is-empty`, `port-exit-value-contract-untagged-rax-forges-dt-fail` — hq_B measured 223 raw sites in 31 programs, 140 of them Prolog continuation `lea`s the cut deletes, FINDING `.github` `bb7caece`; `pascal-quick-m4-wrong-checksum-crash-masked` FREE for hq_C after rung 0). **The gate while climbing:** the Prolog master board (348/371 both modes on `f4532dea`) is REPORTED with every receipt and is not a landing gate until rung 10; SNOBOL4 FAIL=0 over the printed denominator + Icon watermark + `nm -D` names none of § C + `strip_comments.py --check` 0 are hard on every push. ⛔ Not for any seat: the `SCRIP_OPT=0` crash of `eval_convert_branch_1` (SNOBOL4, no cure owed).

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 ceo (DUO: ceo + HQ-COMPLETE/hq_C) — THE REDESIGN HAS ITS TRANSLATION PAGE: `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` (draft, HQ review owed); rung 1 = BX-0 the port-trace instrument, rung 2 = PZ-4 (b)-(f) the frame header + B wire (its § E); the zero-risk prep rung (nine dead globals of the census § C, the `g_zeta_mode` cmp in `$trail_mark`, `g_resolve_bb_table` out of the runtime image) is hq_C's first landing. Denominator of the whole redesign: `FINDING-2026-09-02-ceo-prolog-only-globals-census-56-live-9-dead-and-what-the-term-deletion-removed.md`.

## ⭐⭐⭐ LIVE CURSOR — 2026-09-02 hq_P (TRIO, second sitting) — **THE VAN ROY THREE-ANGLE INSTRUMENT FABRICATED ITS SCRIP COLUMN AND SKIPPED THE RIVALS ON THE SELF-TIMED KERNELS — BOTH CURED ON `scripts/`; THE BASELINE PIN STAYS PARKED (ceo: until rung 8) AND NO NUMBER IS QUOTED**

**SCRIP `4253dd88` + `48b09e04` · corpus `d0351a546` · pristine `-O0`.** Receipt: `FINDING-2026-09-02-hq_P-vanroy-loop-runs-its-body-exactly-twice-for-every-n-both-angles-divided-n-by-it-and-the-rivals-never-had-their-preludes.md` (BX-0's own receipt, one sitting earlier: `FINDING-2026-09-02-hq_P-bx0-port-trace-lands-r15-is-a-driver-mmap-address-at-every-omega-and-the-baseline-pin-is-parked.md`).
- ⛔ **The loop wrapper `l__(N) :- between(1,N,_), bench__main, fail.` runs `bench__main` EXACTLY TWICE for every N on m3 and m4** (N=1, 3, 64 all print two answers, each with its own fresh work bracket; `nrev` prints none) and exits 0 — a β-path datum for the rung ladder, not a harness quirk. Angle 1/2 gated on `exit=` alone and divided N=65536 by two iterations' CPU: `fib` m3 3,102,442 iter/s beside gprolog 485. ✅ `scripts/lib_prolog_bench.sh:loop_check` grades every engine's loop stdout against N × `.expected`; a cell that did not loop reads `LOOP-OUTPUT-MISMATCH(lines=seen/wanted)` and never a rate. Angle 1/2 also now consult `prelude_gplc.pl`/`prelude_swipl.pl` (they never had; the 10 self-timed kernels read `correctness-fail` on the rivals for `wall_us/1`). Both REFUSE rc=2 on a missing lib or prelude.
- ⛔ **hq_C:** `test_gate_vanroy_prolog_acceptance.sh`'s CLEAN (rc 0, no refusal text) admits `fib` at 2 of 64 answers and `nrev` at 0 of 65536; its `CLEAN_FLOOR=3` counts programs that do not run. Routed (`vanroy-clean-counts-two-of-n`).
-  ✅ **THE QUAD GATE IS MINTED — `scripts/test_gate_pl_quad_regs.sh` (ceo rung-0 brief, instrument lane):** no `r12`–`r15` write reachable from a Prolog graph outside the TR/B/ROOT/BALL helpers, ARM 1 over every Prolog witness compiled fresh (35 today: probe_plz 9 + bench 23 + demos 3 + ladder 0), ARM 2 over the rtx functions reachable from them (transitive, balanced push/pop = callee-saved-clean, unreached = INFO). **Born red on `f4532dea`: 5303 writes / 5303 violations** (4606 `lea r12,[rip+g_pl_*]` sink scratch · 417 `mov r12` · 245 `pop r12` dcα continuations · 35 `xor r14d` m4 prologues; `r13`/`r15` zero), rtx 47 functions / 2 reachable / 0 writes; `QUAD_HELPER_RX='.'` → PASS over the same 5303 (can say YES); `--self-test` proves NO and YES on both arms on fixtures. `QUAD_HELPER_RX` is the one allow-list — hq_C names the helpers there. ⭐ **s284 CORRECTION (hq_P, SCRIP `9623ff55`): that label-keyed design is SUPERSEDED — at rung 0 it is RED on the five § A.1 ROOT seeds and can only go green by naming `main`/`main_α`, which admits ANY write under those labels. The gate now grades emitted writes by (site, register, shape) against an explicit table and is GREEN on rung 0 (5/5 enrolled) and in `make test`; `QUAD_HELPER_RX` survives as the rtx ROUTINE-name allow-list (the TR helpers by name). Enrol a shape row, never a label — `FINDING-2026-09-02-hq_P-quad-gate-lands-green-at-rung-0-by-site-and-shape-superseding-the-born-red-label-allow-list.md`.** ⛔ Third harness defect, same sitting: swipl runs through `timeout`'s SIGTERM (648 s on `vanroy/queens.pl` under `timeout 60`); every angle-harness `timeout` now carries `-k 5`. **Triangulation on the cured instrument:** run 1 exposed Defect D (angle-2 drivers carried pre-bracket bodies: 8 kernels DISAGREE 1.2x–2.75x), run 2 on refreshed drivers (corpus `d0351a546`, TSV `triangulation-20260902T190902Z.tsv`) reads cal/derive/divide10/ops8 AGREE on both rivals (bucket MEASURED=4), four more AGREE on one rival, log10/tak DISAGREE on both at 0.83–0.90x (calibration, declared with a discharge condition), SCRIP cells UNPROVEN everywhere by the loop check; Defect E, the coverage gate counted 23 not 21 and had been red since 09-01 — cured SCRIP `48b09e04`, now `total=21 covered=21 rc=0`; `--two-number` bucket plumbing `MEASURED=4 DECLARED=5 REFUSE=12` rc=0, multiples unquoted. **Acceptance gate, pin tree, worst-of-3:** `CLEAN=3 (floor 3) · REFUSE=3 · CRASH=13 · other=2`, 11 of 21 flip, `GATE FAIL(1)`; the three CLEAN (`fib nrev queens_8`) include the 2-of-64 and 0-of-65536 programs.
- **r9 datum on the pin tree** (`./scrip --compile --target=x86 bench/<k>.pl | grep -c 'rtccb+48'`, SCRIP `f4532dea`, `-O0`): `nrev` **225**, `nreverse` **168**. Rung 2.0's before-number is this line, not ceo's 244 (taken on `2748100d`).
- **Single-shot m3 at `f4532dea`:** 10/21 answer correctly (`cal derive deriv divide10 fib log10 ops8 sendmore tak times10` — exactly the self-timed ten); `crypt ham meta_qsort mu nreverse nrev qsort zebra` dump core; `queens_8 queens query` fail `main/0`. gnu and swi 21/21 with their preludes.
- **NEXT for hq_P:** ceo's rung-0 brief (Lon 13:40–13:50: Prolog restarts at hello world with zero Prolog-only globals, climbing by construct; BX-0 is the ladder's grading arm) — (1) quad-clean gate over r12–r15 writes in emitted `.s` + rtx asm; (2) rungs 6–8 (`pl_leaf`, `pl_gen`, `pl_drive`) once hq_C's rung 0–2 frame is on origin; (3) the two-number pin AFTER rung 8, taken with this instrument, three runs for the spread. ⛔ `src/` is held until ceo's "rung-0-landed". The C9 witness and the optbypass durations from the previous cursor are unchanged and still parked.

## ⭐⭐ LIVE CURSOR RE-POINT (hq_C 2026-09-01, on seat15's report via ceo): THE WALKABLE RUNG IS LADDER C, NOT PZ-4
The cursor below points at PZ-4 (`prolog-pz4-gamma-retain-activation-frames`), which is **BLOCKED-ON `calling-convention-depth-tracked` (hq_P)** — a seat following it lands on a blocked row. Under FLEET-16 (Lon: Prolog #1) the walkable work is **`.github/MASTER-PLAN.md` ladder C**: ⭐ **C1** `prolog-directive-only-file-fatals-no-main-bb-graph` (rank 0, the 139-entry driver class), then C4–C20 (every FREE Prolog correctness row), with C2/C3/C27/C28/C29/I4/I5 minted and refusing until their blockers land. Run `python3 SCRIP/scripts/util_ladder_walk.py` for the live state; `prolog-next` (C20) means "the first incomplete C rung", never PZ-4 while it is blocked. The PZ-4 cursor remains correct as the *keystone* record and stays below; it is not where a seat starts.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-09-01 seat15 (FLEET-16, Prolog #1) — **BOTH PROLOG MODE INSTRUMENTS WERE BLIND. CURED, NOW HONESTLY RED. `prolog-next` HAS A COMPUTABLE DONE-WHEN AT LAST.**

**SCRIP `f85e1fdc`** (pristine `-O0`) · corpus `04177c4b3` · row `prolog-next` · full receipts `.github/FINDING-2026-09-01-seat15-prolog-both-mode-instruments-were-blind.md`.

**Found, verified on a pristine tree, not read:** Prolog — the fleet's #1 correctness priority — had two automated m3/m4 instruments and **neither compared anything**, in opposite ways that hid each other.

| Instrument | Before | Exit | After cure |
|---|---|---|---|
| `test_gate_pl_m34_parity.sh` | examined **0** programs | 2 UNPROVEN | **PASS=5 FAIL=7** |
| `test_crosscheck_prolog.sh` | `PASS=13 FAIL=0 SKIP=25 ORACLE_MISS=0` | **0 (green)** | **m3 3/15 · m4 3/15**, exit 1 |

- **Parity gate:** floored on `*.sno` inside `corpus/tests/prolog` (which holds **0** `.sno`, **45** `.pl`) — a SNOBOL4 copy-paste — so `gate_floor` exited UNPROVEN(2) before the first comparison. One-word cure surfaced **7 real divergences**: `findall`×2 (m4 SIGSEGV `rc=139`), `retract`×2 / `abolish`×2 / `setof`×1 (m4 `rc=1` where m3 answers correctly).
- **Crosscheck:** compared `--run` against `--run` (**mode 4 never invoked** — the variable name `ir` is the fossil of deleted mode 2), read `.ref` where the corpus stores `.expected` (**0** vs **26** on disk, so the oracle was never consulted), and pre-skipped crashing programs (**25 of 38**) — hiding exactly the PZ-4 crash class. Cured to per-mode independent oracle grading per § MODES MAY DIVERGE.

⭐ **Verified both directions before trusting the new instrument:** a reported FAIL (`rung66_current_stream`) is a genuine **silent wrong answer at rc=0** (`in_enum`→`not_in_enum`); a reported PASS hand-diffs identical. A third "disagreement" was my own ad-hoc `diff` mishandling `.expected`'s missing trailing newline — the instrument was right, I was wrong, recorded in the FINDING because that reflex is how a correct gate gets reverted.

**`prolog-next`'s DONE-WHEN is now a command that can exit non-zero** — `test_crosscheck_prolog.sh && test_gate_pl_m34_parity.sh` — discharging the standing instruction hq_C left in that row on 2026-08-22. It is RED today, for a measured reason rather than because it was prose.

⛔ **This row's own cursor chain is blocked, which is why this pass went to the instruments:** the PZ-4 cursor below names `prolog-pz4-gamma-retain-activation-frames`, which is `hq_C`-owned and `BLOCKED-ON:calling-convention-depth-tracked` (`ASSIGNED:hq_P`, rank 0, open). A seat following this file's cursor literally lands on a row it must not work — flagged to HQ, not resolved here.

**NEXT:** root-cause the dynamic-database m4 cluster (retract/abolish/setof — one mechanism, five witnesses; minimal witness pair already minted: `rung15_abolish_abolish_existing` passes both modes, `rung15_abolish_abolish_one_of_two` fails m4). Then the `findall` SIGSEGV pair. Then close the **`NO-ORACLE=23`** gap — two thirds of this corpus has no `.expected`, and no instrument can grade what has no oracle.

---

## ✅⭐⭐ LIVE CURSOR — 2026-08-27 seat05 (FLEET-8) — PZ-4 row: omega-wire caller/callee mismatch found+fixed, row still open (new SIGILL witness)

**SCRIP `0c800c86`** (on `d4e6e971`+hq_C's `0e8cf4a4`) · corpus `49b4779f0`. Row `prolog-pz4-gamma-retain-activation-frames`, task file at `/home/resources/postoffice/tasks/`, full receipts `.github/FINDING-2026-08-27-seat05-pz4-omega-wire-never-loaded-bcps-spine-gen-arm.md`.

**Found (gdb-confirmed, not just read):** `bcps_spine_gen_arm`'s wire-cross to every zframe-graph (generator) callee never loaded the omega continuation into `rdx` — `xa_flat_zframe_prologue_str` captures {γ,ω} unconditionally from rcx/rdx, but the default `SCRIP_ICN_WIRE_STACK=1` path (`bb_glue_pass_wires_blob`) only pushes them onto the stack, leaving rdx as whatever the last intervening call happened to clobber it with. γ survived by an accidental coincidence in which register the push sequence's `lea` scratch lands in; ω did not. This predates and is independent of the `SCRIP_PL_GAMMA_RETAIN` question this row was minted around — both retain=0 and retain=1 hit the same garbage-omega crash in hq_C's own matrix.

**Fixed:** `bcps_wire_cross_gen` (bb_call_proc_staged.cpp) — adds the one missing `lea rdx,<omega>` before the jmp, scoped to the two generator-call sites only.

**Measured name-for-name:** SNOBOL4 365/365 unchanged · Icon `246/16/1/30 · 246/16/1/30 · 244/18/1/30` identical to pre-change baseline · rung13 `0/5`/rung14 `2/5`/smoke m4 `4/5` unchanged (different mechanisms). ⛔ **CORRECTION, same pass:** rung15 `1/5→2/5` is true but incomplete — controlled A/B (5 runs each side) shows pre-fix perfectly deterministic (`1/5` every run) but post-fix genuinely flaky (`1–3/5` across runs). The fix is real; it converts a stable wrong-jump crash into an intermittent one on 2 of the 5 witnesses, exposing rather than closing a second pre-existing defect. Full data: the FINDING's own ADDENDUM.

**Row NOT closed.** Same repro now fails one layer deeper: fact's own omega resolves correctly now, but crashes shortly after (SIGILL, a stack address used as a jump target, inside/after the `rt_proc_call_epilogue_ω` call chain) — new mechanism, not yet root-caused. See the task file's `## NEXT` for the exact next step. This is this row's THIRD real, verified, non-regressing "worked, not done" pass in one day (hq_C's stack-depth fix, then this) — the wall keeps moving, has not yet fallen.

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-21 HANDOFF (seat2 `/home/claude2`, Claude Fable 5 → Opus 5; Lon in-chat: prune → REPLAN to the SN4 machine → deep scan + work list) — **THE CHARTER, THE CENSUS, THE SEED AND THE WORK LIST ARE ALL CUT. NO COMPILER CODE WAS CONVERTED YET — PZ-1(b) IS THE FIRST CODE RUNG.**
**HANDOFF SUMMARY.** This session replaced the whole pre-conversion ladder with the SN4 THREE-ZETAS conversion (Lon in-chat: *"a new world of code … EXACTLY like SNOBOL4"*), then did the discovery to back it: a 1,603-commit history scan of the SN4 campaign (16 campaigns, its dependency order, Lon's ruling SHAs), a full per-template scrutiny of every Prolog-reachable file, a live-measured admission census, an oracle-green hand-written seed, and the per-file work list. **⛔ ZERO codegen files were touched** (`src/emitter`, `src/templates`, `src/lower`, `src/runtime` all untouched) — so RULES step-4 `.s` artifact regen does NOT apply to this handoff, by construction rather than by omission. Deliverables: `WORKLIST-PROLOG-ZETA.md` (32-row inventory + steps + deletion ledger) · `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md` (the arc + ten gaps + twelve laws) · `FINDING-2026-08-21-seat2-PZ-0-...-86-percent.md` (the census) · `corpus/probe/plz/test_pl_zeta_1.s` (`2ef8bb3e`, oracle-green) · `SCRIP/scripts/util_pl_zd_arm_census.sh` (`cc2675cd`) · `corpus/probe/plz/` 9 witnesses + gprolog `.ref` oracles.
**⛔ FOR THE NEXT SEAT — READ IN THIS ORDER:** this cursor → `WORKLIST-PROLOG-ZETA.md` §0 (the four undocumented facts) → §1 (inventory) → §2 (steps). Re-grep every anchor: the 2026-08-20 style-200col pass moved lines tree-wide and three anchors in older Prolog docs were already stale when the work list was cut.
**PZ-0 LANDED: PROLOG HAS *ZERO* ζ SPINE AT THE SHIPPED DEFAULT, AND ONE DELIBERATE TAUTOLOGY GATES 86% OF ITS RUNS.**
Measured at SCRIP `d2ade229` (`make pristine` rc=0, `-O0`), corpus `f34388fd`, every board run live this session. ⛔ Numbers are SHA-PINNED to that measurement point; at handoff HEAD had moved to `cfff4530` (sibling Icon/HQ landings) and the cheap tripwire was RE-RUN there after rebuild: **smoke still 3/5, unchanged** — the non-smoke boards were not re-derived at `cfff4530` and PZ-1 re-derives them. **WATERMARK / THE CONVERSION'S FLOOR: smoke 3/5 · honest PASS=107 FAIL=0 ABORT=0 ORACLE_CRASH=78 · rung interp 110/164 compile 109/164.** ⚠ interp REGRESSED 113→110 since s166 (28 sessions of sibling shared-emitter landings); compile unchanged. Recorded, not chased (PZ-0 is a census) — bisect candidate against sibling landings.
**⭐⭐⭐ THE CENSUS (new instrument `scripts/util_pl_zd_arm_census.sh`, 214 programs, 736 ZD runs):** shipped default **ARMED=0 / REFUSED=736, every one at its FIRST node** — the eight PL-ZK admission arms are all gated on `pl_cells_graph`, which only opt-in `SCRIP_PL_CELLS=1` ever sets, so **the shipped compiler has no ζ for Prolog at all** and the "twin-arm" framing in every Prolog doc is misleading. Cells arm: ARMED=100, and **635 of the 636 refusals are ONE KIND — `IR_VAR_REF`** (494 of them at i=1, the node right after the head).
**⭐⭐⭐ THE GATE IS A TAUTOLOGY, DELIBERATE AND DOCUMENTED:** `emit.cpp:2064` returns `… pl_cells_graph && !pl_cells_graph …` = never 1, put there by `069c2fd8` (PL-ZK-5B s13) whose own message says *"gated off via tautology pending correct TERM_VAR materialization"*, because **Prolog variables are heap `Term*` cells needing unification+trailing** — a frame-slot copy gives NULVCL for fresh vars. Three consequences: (a) **`SCRIP_ZD_PL_VR` IS A VACUOUS KILLSWITCH** — A/B it and you measure nothing (s68 class); (b) its cost was never measured until now: **86% of all runs**; (c) ⭐ **VAR_REF ADMISSION AND γ-RETAIN ARE ONE PROBLEM** — a logic variable cannot live in a spine cell that γ RELEASES, which is exactly why s13 could find no correct materialization; under the SN4 law (α carves, ω tears down, γ RETAINS) a retained spine cell IS a WAM environment slot, made sound by conditional trailing (gprolog `Word_Needs_Trailing`, SWI `GTrail`).
**⛔ INSTRUMENT TRAP, CAUGHT BEFORE PUBLICATION:** `SCRIP_ZD_DIAG` prints ONLY refusals (armed runs print per-NODE `[ZD] h=..r=..i=..` lines instead), so counting `^\[ZD\] run` and subtracting yields **ARMED=0 for every language, always** — a structural zero that reads like a measurement. Falsified both directions (SNOBOL4 ARMED=2/REFUSED=0; Prolog probe ARMED=0/REFUSED=2); the trap is documented in the script header.
**⭐ FIRST LIVE PROLOG ORACLE OF THE CAMPAIGN:** GNU Prolog 1.6.0 built from `/home/resources/gprolog-master` (scratchpad prefix; `gplc` needs its `bin/` on PATH for `pl2wam`). Nine witnesses minted at `corpus/probe/plz/` with `.ref` oracles, run both modes: only p1 (single clause) passes. **p8 is an m3≢m4 divergence AND a silent wrong answer** — m3 SIGILL, m4 exits 0 printing `yes` where the oracle prints `no` (`t(X), !, X > 2` committed to the wrong clause and reported success); p5 splits the modes 139 vs 132. The cut/barrier class has no correct arm in either mode.
**PZ-1(a) SEED: DONE AND ORACLE-GREEN (this session)** (`corpus/probe/plz/test_pl_zeta_1.s`, SCRIP `2ef8bb3e`) — the THREE-ZETAS Prolog machine hand-written and byte-identical to live gprolog 1.6.0 on two-clause backtrack-with-binding plus cut: retained spine-cell variable · activation-homed 32B record with **E DERIVED from the record address** (record+56, so no EB field is needed) · trail `saved_MH` unwind · γ-RETAIN · cut = CP-chain pop reclaiming nothing (WAM law) · statement failure barrier = the statement's own BASE record, making `fail` uniformly `jmp [r15]` · wires-only linkage. **The shape is proven before any compiler change; Lon may veto it, and the ladder's steps are written against it.**
Full evidence: `FINDING-2026-08-21-seat2-PZ-0-prolog-has-no-zeta-spine-and-one-tautology-gates-86-percent.md`; charter + gap census: `FINDING-2026-08-21-seat2-prolog-zeta-gap-census.md`.

## ⭐ LIVE CURSOR — 2026-08-24 seat01, cross-goal beneficiary note (Icon N-1, `GOAL-ICON-100.md` row `icon-n1-wire-stack-crossing`) — CALLER SIDE CROSSED, CALLEE SIDE NOT DONE, ruled ATTRIBUTED PARTIAL PZ-LADDER PROGRESS (hq_C s269)
**Icon N-1 (SCRIP `15738e4a`):** the caller side (`bcps_spine_gen_arm`, one of the 4 `bb_glue_pass_wires` sites in `bb_call_proc_staged.cpp`) crossed for Prolog's generator-style calls — verified: `wordcount.pl --run` byte-identical old vs new, `zebra.pl` core-dumps identically both modes (pre-existing).
The callee side (the `xa_flat.cpp` epilogues) is explicitly NOT done — N-1's scope was `icn_cells_graph`; pure `zframe_graph` (Prolog's own graphs) was excluded. Cite: `FINDING-2026-08-23-seat01-icon-n1-wire-stack-crossing-landed-plus-a-stack-parity-bug.md`.

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-21 s248 (seat2 `/home/claude2`, Opus 5; Lon in-chat: *"Get the THREE ZETA's working for SCRIP Prolog"*) — **PZ-1(d) IS LANDED AND PUSHED. THE FIRST COMPILER CODE OF THE CONVERSION IS IN.**
**SCRIP `e92aebfe`** (commit `9c54afbf`) · FINDING: `FINDING-2026-08-21-s248-seat2-the-2065-tautology-is-retired-and-retention-is-not-sufficient.md`.
**LON'S TWO RULINGS THIS SESSION (in-chat, supersede):** (1) **PZ-1 order = (d) BEFORE (b)/(c)** — retire the tautology first so `zdp_tier` supplies the admission signal rather than a hand predicate. (2) **CP = R15 in Prolog graphs, contract AMENDED** (Σ/δ/Δ are scan-only; a Prolog graph does not scan) — recorded in §register contract above.
**⭐ PZ-1(d) LANDED.** `emit.cpp:2065`'s `pl_cells_graph && !pl_cells_graph` is GONE; the guard is now `pl_cells_graph && emit_rec_pin() && emit_pl_gamma_retain()` — it names the real precondition (a logic variable cannot live in a cell γ RELEASES) and turns itself on when PZ-1(c) lands. `SCRIP_ZD_PL_VR` **stops being vacuous**: 240 lines of byte-motion on `plz_p2`. **Census cells arm: ARMED 100 → 534 / 736 · `IR_VAR_REF` refusals 635 → 0** (the kind vanishes). Next refusers = **`IR_VAR` (162) + `IR_CALL_BUILTIN_GEN` (40)** — that is the next admission arm. ONE AUTHORITY: `emit_pl_gamma_retain()` in `emit.h` beside `emit_rec_pin()`; `xa_flat`'s copy now delegates. No new globals (ratchet 14/14).
**⭐ WORKLIST §0(3) PROVEN BY INJECTION:** new `SCRIP_PL_ZANCHOR_POISON` drops the `[kt-8]` store; **356 program-runs** (Prolog rung 164×2 + smoke 5×3 + Icon 14×2) byte-for-byte identical with it present or absent. The slot is **dead on both the Prolog and Icon paths** — PZ-1(b)'s caller-base save has a proven-free home.
**⛔⛔ THE FINDING THAT SHAPES PZ-1(c): RETENTION AS THE EXISTING FLAG IMPLEMENTS IT IS NOT SUFFICIENT.** Measured 3 arms: cells+retain OFF = interp **103**; cells+retain **ON** + VAR_REF admitted = **91**; pin-only guard = **91**. Flipping `SCRIP_PL_GAMMA_RETAIN=1` buys **nothing** — because that flag implements **ONE** of PZ-1(c)'s **five** enabling sites (the γ no-unwind arm in `xa_flat`). ⛔ A seat trusting the flag's NAME would wrongly conclude the γ-retain thesis is falsified; **it has not been tested yet**. The −12 is the expected reading until all five sites land.
**⛔ NOT A REGRESSION:** the cells arm's `compile 2/164` is **PRE-EXISTING**, measured on both sides (before 103/2, after 103/2).
**⭐ A FIFTH DEAD GUARD, NOT IN ANY DOC:** `x86_fb_pinned()` (`x86_asm.h:456`) is a hardcoded `return 0`, ONE definition, gating **six** sites — it kills the ICON half of `:2065` (so VAR_REF was unreachable in **both** languages), all of `:2072`, two of three `:2025` clauses, and three `bb_match_defer` arms. ⛔ `rtx_abi.inc:26` asserts `x86_fb_pinned() == emit_rec_pin()` — **FALSE today**; `zeta_choices.h:124` calls that duality *"already complete"* — **it does not run**. Same disease as the tautology. Recorded, not chased; deserves its own rung before anything is built on it.
**WATERMARK (re-derived live at `cfff4530`; sibling `425c5d06` touched README + one bench script only, ZERO compiler code, so it stands at `e92aebfe`):** smoke **3/5** all modes · rung interp **110/164** compile **109/164** · shipped-default `.s` md5-identical to HEAD · Icon canary **14/14** both modes · gates `emit_no_lang` OK + `pl_no_new_global` 14/14.
**NEXT: PZ-1(c) — land the FIVE-SITE enabling pair** through the shared pin machinery (`emit_rec_pin`/`x86_fb`/heap-fb adopt), never bespoke arms, then **re-run arm B: its DONE-WHEN is simply that the −12 disappears.** Then PZ-1(b) (caller-base save into the proven-free `[kt-8]`), then PZ-1(e). Per `WORKLIST-PROLOG-ZETA.md` §2.

## ⛔⭐⭐⭐ THE WORK LIST — `WORKLIST-PROLOG-ZETA.md` (companion; cut 2026-08-21 from a full per-template scrutiny pass)
This file owns the LADDER and the LAWS; **`WORKLIST-PROLOG-ZETA.md` owns the INVENTORY (32 rows: every Prolog-reachable template/emitter site/runtime symbol with KEEP·CONVERT·DELETE and its owning rung), the per-rung STEPS with DONE-WHENs, the DELETION LEDGER (~21 globals + 2 stacks + 2 dead templates + a /proc parser), and the STANDING HAZARDS.** An implementing seat walks THAT file; this one tells it which rung it is on. ⛔ Re-grep every anchor at session start — the 2026-08-20 style-200col pass moved lines tree-wide.
**Four scrutiny findings that were in no doc (WORKLIST §0):** ⭐⭐⭐ **the default-arm retry channel is SEVERED** — `rt_pl_cp_push` has ZERO call sites while its consumer `rt_pl_cp_pop` is live in the arm every Prolog graph takes, and the producer writes a frame slot the shadowed legacy arm alone reads, so the retry dispatcher pops an always-empty stack and CONCEDES (scope it by delivery census first: MOVE_LABEL-delivered predicates only, SUSPEND delivery has a live push; ⛔ do NOT cure it by wiring the dead push back in — PZ-2 deletes that stack) · `bb_cell_cut.cpp`/`bb_cell_ite.cpp` and `IR_INDIRECT_GOTO` are **DEAD** (zero callers / no emit case), so earlier docs calling them the Prolog cut/ITE relays are wrong · **the ζ-ACTIVATION anchor does not exist yet and its slot is free** — `[kt-8]` is written with own rsp and has ZERO readers · `rt_jmp_frame_lexprep2` is an **empty stub still called from every prologue**, `resolve_cp_*` are declared-never-defined, and the trail's **/proc/self/maps** dead-frame filter exists only because frames die under live trail entries — γ-RETAIN deletes it.

## RUNGS — THE CONVERSION LADDER (walk in order; each rung: own commit, killswitch byte-identity + md5 blast radius, twin-arm boards until PZ-6, TRIPLE-ARM CANARY, boards ratchet, cursor move, push mid-session)

- [x] **PZ-0 OBSERVABLE FIRST — LANDED 2026-08-21 (a,b,c,d all measured).** (d) answered by measurement: **ZSM sees ZERO Prolog boxes** (0 events on two probes vs 11 on the SNOBOL4 control, identical flags) — the bug finder cannot hunt Prolog at all today. Master gate is env-only, so the blindness is in the emission path, not the gate; hypothesis (to be measured at PZ-1, not assumed): no ζ ⇒ no port hooks ⇒ no ZSM, in which case the gap closes as a side effect of PZ-1 and PZ-7(d) shrinks to a census. (a) Re-derive all five boards at `7153ccd7`+ pristine, say the numbers aloud, cursor them — they are the conversion's floor. (b) Mint the probe matrix as committed corpus witnesses with `.ref` oracles (the p1–p6 shapes from the s166 FINDING + the two smoke reds): two-clause first-solution, fail-driven backtrack, recursion, member/2, inline disjunction, cut-commit, cut-under-backtrack. (c) Arm census: `SCRIP_ZD_DIAG=1` + `SCRIP_ZSM=1` sweep of the 164-corpus — per-graph armed/refused counts, which xa_flat arms Prolog actually takes, per-kind refusal attribution; falsify zeros by injection. (d) Wire Prolog ports into the ZSM ring + autobug harness (law 0b: every box's four ports emit events) and record the β-vs-α RSP exceptions list for Prolog kinds. Deliverable: a census FINDING — the conversion's denominator.
- [~] **PZ-1 THE GATE PAIR — (a) ✅ SEED · (d) ✅ LANDED s248 `e92aebfe` · (b)(c)(e) OPEN.** ⛔ **LON RE-ORDERED IT IN-CHAT s248: (d) BEFORE (b)/(c)** — retire the tautology first so `zdp_tier` supplies the admission signal rather than a hand predicate; (d) is therefore done and its guard is retention-gated, so it arms itself the moment (c) lands. **(c) IS NOW THE CRITICAL PATH AND ITS SCOPE IS LARGER THAN THE FLAG SUGGESTS:** measured s248, `SCRIP_PL_GAMMA_RETAIN=1` implements **ONE of the five enabling sites** (the γ no-unwind arm in `xa_flat`), so flipping it buys nothing (interp 103→91, same as no retention at all) — ⛔ that is **NOT** a falsification of the γ-retain thesis, which has not been tested yet. DONE-WHEN for (c): the −12 disappears. **TERM_VAR MATERIALIZATION + γ-RETAIN, PLANNED AS ONE RUNG (re-ordered by PZ-0's measurement: this is the 86% gate, not a late cleanup).** They are one problem: a logic variable cannot live in a cell the γ-exit releases. (a) **SEED FIRST** (C5 discipline, `d5c7a72b`/`1e881ce5`): hand-write the `.s` for a two-clause predicate with a variable argument, binding, backtrack and trail-unwind on retained frames; run it against live gprolog; ⛔ **LON RULES THE SEED BEFORE ANY LOWERING CHANGE.** (b) γ-RETAIN through the SHARED pin machinery (`emit_rec_pin`/`x86_fb`/heap-fb adopt), never bespoke arms — the five-site enabling pair from `FINDING-2026-08-16-CLAUDE-PL-Z1A` §3: prologue saves caller base + pins own · γ hands base in rax, NO unwind · caller staged-call γ/β landings re-anchor off their own base · backtrack restores own base · ω restores caller base then releases · terminal top-graph exclusion. Flip `SCRIP_PL_GAMMA_RETAIN` ON, then DELETE the release arm. (c) **Retire the tautology at `emit.cpp:2064` and give `IR_VAR_REF` a REAL Prolog guard** — the cell holds the `Term*`/`TERM_VAR`, the retained frame is its home, `zdp_tier` decides SPINE vs ACTIVATION. ⛔ The vacuous `SCRIP_ZD_PL_VR` is replaced by a killswitch that MEASURES (prove it byte-moves both ways before believing any A/B). (d) Conditional trailing (PL-TRAIL-COND: trail only bindings older than the newest CP — gprolog `Word_Needs_Trailing`, SWI `GTrail`) is this pair's third member — spec it here even if it lands at PZ-2 with the record. (e) Discriminating witnesses `plz_p2`/`plz_p6` (⛔ `app/3`/`two.pl` do NOT discriminate — s14 correction). Re-derive ZK-5B from measurement; the s14 falsification was against a γ-RELEASING frame and does not bind the retained substrate. Target: the census ARMED count moves off 100/736 and the shipped default stops being 0.
- [ ] **PZ-1b DISPATCHER RE-LOWER (was PZ-1).** `lower_prolog.c`: predicate clause dispatch moves from {IR_MOVE_LABEL pushes + 0-operand IR_DISJUNCTION pop-jmp} to the **nary IR_DISJUNCTION** (entry,resume)-pairs form the Icon path already emits (`zdp_has_pairs` classifies it); `$ix_g` first-arg indexing stays as guard arms; measure whether `dj_expand`'s synthetic `$disjN/k` predicates still earn their keep. Resume-surface audit: every arm publishes its RIGHTMOST box (s190 trap: `g->all[before]`).
- [ ] **PZ-2 THE CHOICE RECORD (WAM CP as a ζ record).** bb_disjunction upgrades from alt_i/cmp-chain to address-dispatch on the 32B record {resume · δ/TAG · prev_CP · saved_MH}: TAG 0=CHOICE per clause alt, 1=BASE at predicate entry, 2=FENCE for cut; **homed per PF-1b** (`75e3170e`): in the nearest enclosing activation frame — a disjunction pushes NO frame of its own; record-relative addressing only (s189). saved_MH carries the trail mark — the `$trail_mark`/`$unwind_nothrow` node triples between clauses fold into the record's α/β protocol (gap 4). DISJUNCTION gets its OWN tier admission (tier-3-shaped per HQ-73's motion: β re-yields at the same cursor; a different family from IR_MATCH_ALTERNATE — per-family, not per-op). DELETES: bb_move_label PL arms, bb_indirect_goto PL arms, `rt_pl_retry_*`, `rt_pl_cp_*`, `g_pl_retry*` + `g_pl_cp_stack*` (6 globals). Canon per construct: gprolog wam_inst.h CP fields, SWI CHP_* + clause indexing.
- [ ] **PZ-3 CUT = TAG-2 WATERMARK.** IR_CUT lowers to restore-to-barrier: walk/restore rsp + prev_CP + saved_MH TOGETHER to the predicate's BASE record (the REGISTER-PLANES ideal — one cut restores every plane; trail entries below the mark stay COMMITTED, records above die). Keep the compile-time ωeff rewiring (free, correct); the stale-CP structural leak dies. Canon: `Assign_B(BB(B))` (gprolog), `discardChoicesAfter` (SWI pl-wam.c:2604). FENCE-family laws bind: commit whack mandatory (stack "COLOSSAL" otherwise), FENCE1-DEPTH class (a barrier inside an iteration is entered one level deeper each time — witness class already cured for SN4 at `c43d8f51`).
- [ ] **PZ-4 γ-RETAINS: PREDICATE ACTIVATIONS UNDER THE TIER LAW (the keystone, gap 1).** Multi-clause/resumable predicates take the ζ-ACTIVATION tier via `zdp_tier`/heap-fb ADOPT — never a hand predicate. Land the PL-Z-2 enabling pair from `FINDING-2026-08-16-CLAUDE-PL-Z1A` §3 expressed through the SHARED pin machinery (`emit_rec_pin`/`x86_fb`/adopt), not bespoke arms: (a) prologue saves caller base + pins own; (b) γ hands base in rax, NO unwind; (c) caller staged-call γ/β landings re-anchor off their own pinned base; (d) backtrack path restores own base; (e) ω restores caller base then releases; (f) terminal top-graph exclusion. Flip `SCRIP_PL_GAMMA_RETAIN` default ON, then DELETE the release arm for gen graphs. Wire conversion rides this rung: proc-enter γ/ω move rcx/rdx → R10/R11 WIRE-ORDER (`433dc27e` FUNCTION LINKAGE = BLOB INTERFACE). Frame seed/zero-fill decided by measurement (NULVCL heritage, EXTRACT-PL-FRAME).
- [ ] **PZ-5 IN-FRAME RESUME — DELETE THE PENDING-CURSOR MACHINE (gap 3, the largest deletion).** With frames retained, suspend writes the continuation to `[fb+resume_slot]` of the RETAINED frame; caller β re-enters through the frame-resident wire (or the 16B GENP-SPINE resume record). DELETES: `g_pl_zf_pending_*` (6 globals), `rt_pl_zf_resume_set/clear`, the lexprep2 override, the xa_flat γ-intercept, the `$trail_mark` N0-SUPPRESS, `g_pl_zf3_stack*`, and bcps's fresh-frame re-open (the three-abandoned-designs block, bb_call_proc_staged.cpp:813-900).
- [ ] **PZ-6 ONE MACHINE — ARM DELETION (gaps 6+7).** ζ admission becomes the Prolog default (delete `pl_cells_stamp`'s env gate); zframe stamp for Prolog + its epilogue arms + `g_flat_dc_np` plumbing die; ZRES→FRQ DUAL-WRITE dies; root-cause the BINARY `bused[]` node-index shift instead of the unconditional-β workaround; fold the 8 `SCRIP_ZD_PL_*` killswitches. ZK-5B (VAR_REF materialization) RE-DERIVED on the new substrate — discriminating witness first (`app/3`/`two.pl` do NOT discriminate); only if still red does the Option A-vs-B Lon ruling fire. Completion = DoD-3's grep and the global census.
- [ ] **PZ-7 LATTICE COVERAGE + STATEMENT BRACKETS + ZSM CHECKS (gaps 8+9).** Prolog kinds enter the `zdp_*` switches per the one-line law; `lower_prolog` mints the statement-terminal structure `op_zgpop` keys on (the choke is language-blind and waiting); UNWIND law verified per kind (failure never whacks); ZSM full-featured on the Prolog boards — RSP expected-value per port, the β-vs-α exceptions list published (retry/extend boxes are the lawful exceptions), autobug hunts Prolog the way it hunted patterns.
- [ ] **PZ-8 RECONQUEST RATCHET (continuous; this rung is the wall-clock gate).** smoke 3/5→5/5 (the γ-release class dies at PZ-4) · honest ORACLE_CRASH 79→0 · rung 113/109→164/164 both modes · bench-22 8→11 (the s16 waterline)→22 (⚠ the 11→8 drop post-dates `9313935` — bisect against sibling shared-emitter landings if it survives PZ-4). Floor = last cursor's numbers, every session.
- [ ] **PZ-9 FEATURES & CORPUS TO 100% (carried).** (a) Banked engine defects: silent-fail on undefined predicates (→ existence_error) · NO-LCO recursion cap (16-wide body dies at depth 10 — likely the largest bench-22 unlock) · nested-`\+` binding leak · retractall/1 gaps · int/float standard-order conflation (`rt_pl_term_compare`) · lexer escape three-site/two-behaviour · `:` at 200 vs 600 + `|` missing as 1105 xfy · double_quotes-under-dialect (⛔ LON RULING WANTED; + PL-STRICT-1). (b) Feature rungs: call/1..7 goal-as-Var · string type (dialect-gated) · list-ISO · meta-predicates · streams §7.9 · format §7.5 · DCG full · cross-clause catch/throw · PL-TRAIL-COND (conditional trailing = the HB field, stamps exist) · PL-INDEX-L2. (c) Suite reconquest (full text git `94fa9093`): SWI extractor → core → library → db → ISO (gprolog `tests/`); ≥80% each, ×2 modes + oracle.
- [ ] **PZ-10 PERF FENCE (LAST; THE INSTRUMENT only).** (a) medium-invisible `xa_flat.cpp(8)` + `bb_glue_flat.cpp(4)` → `--strict` GREEN (⛔ LON RULING WANTED: 4 dead xa_flat arms convert vs DELETE — several die with PZ-6's arm deletion; recount first); (b) surviving SINK rungs: SINK-5 `$is_v` · SINK-9 `$trail_unwind` (design-heavy, LAST — largely superseded by PZ-2/3's record protocol; recount) · SINK-10 sweep · SINK-4 intern cache · SINK-2 enrich; (c) RTX residue per `ARCH-PROLOG-RTX.md` (SINK owns hot symbols; `queensn`/`queens` widening; proven-cold `c_*` eradication per RTX-CLAIMS); (d) emitted share ≥90% (`gc_collect_ex` largest unowned leaf — ⛔ LON RULING WANTED: GC carve-out vs GC rung); (e) register residency → ≤1.2× → ≤1.0× GNU on the van Roy rail.

## ⛔ FALSIFIED / DO-NOT-REDO (retire only by RE-measurement on a moved substrate)
· **SINK-6/7 arith/cmp strcmp premise** — s147: the dispatch chain is COLD; the cost is the CALL. Double-compare trap: C compares ints VIA DOUBLE — naive int-cmp diverges above 2⁵³.
· **ZK-5B Option A as implemented** — s14 green 6→0 ON THE OLD SUBSTRATE; re-derive at PZ-6 from a discriminating witness, never from memory.
· **Single-kind ZD admission** — s163: the unlock set is FIVE kinds, all-or-nothing per run.
· **Blind-widening `emit_fb_stmt_scan` to IR_DISJUNCTION** — s158: the bail is principled; the whole fb-scan question dies with the zframe arm at PZ-5/6 — do not repair what will be deleted.
· **The family switch as a rung instrument** — s146: per-rung killswitches only; output identity ≠ work identity.
· **Wall time in this container** — s164: bimodal on ONE binary; same-binary-twice control + spread, or no claim.
· **Exit 0 is not exoneration** for stdout-only divergence — falsify by injection, both directions.
· **SN4-inherited bans that bind here** (FINDING §4): α:β global-LIFO pairing instruments (α runs once, β re-fires) · "passing sibling proves the offset" reasoning · per-op filters / declaration tables · undo state in whacked ζ (RULING 4) · per-statement rbp brackets (STF-UNFLIP) · whole-graph carve re-lands ("we scope at a different level now").

## ⛔ LON RULINGS WANTED (strike when ruled)
(1) ~~Storage-arm default cells vs zframe~~ **RESOLVED 2026-08-21 by the replan directive: the SN4 ζ substrate is the machine; zframe arm dies at PZ-6.** (2) ZK-5B VAR_REF Option A vs B — only if the witness is still red on the new substrate (PZ-6). (3) xa_flat dead arms convert vs DELETE — recount after PZ-6 (R-6a heritage). (4) ~24 cross-language `x86("mov",<r32>,imm)` REX.W sites. (5) GC carve-out vs GC rung for the ≥90% KPI (PZ-10d). (6) double_quotes target dialect + PL-STRICT-1 (PZ-9a). (7) s163b/s164(10) provenance — SHA-keyed baselines; the STANDING CONDITION names the mechanism, ruling may be "verify-at-measurement suffices". (NEW 8) **PZ-1(a) SEED RULING — the live one:** how a Prolog logic variable lives on the ζ spine (retained spine cell holding the `Term*` vs activation-frame slot vs heap cell with a spine handle), and the choice-record embodiment beside it (spine-carved ALT-FLAT shape vs frame-homed slot vs the 32B scan-blob cell). ⛔ ~~Lon rules on the hand-written seed before lowering changes.~~ **RETIRED — LON s273: this is a QUESTION, not a GATE. Pick a shape, implement it, grade it; a wrong choice is a finding and a follow-up row, never a reason to wait.** PZ-0 measured why it matters: this is the 86% gate.

## LEDGER — absorbed files + retired ladders (full text in git)
Ten absorbed Prolog GOAL-* files: dispositions at git `94fa9093` (PROLOG-BB · PL-ZETA-CELLS · PL-ZFRAME-RESTORE · PROLOG-RTX · PROLOG-IR-RUN · PROLOG-BB-BYRD · PST-PROLOG · PARSER-PROLOG · LANG-PROLOG · PROLOG-100-SWI; carried lesson: an atom is `DT_A` OR `DT_S` depending on entry — check both tags). The 2026-08-16..21 pre-conversion ladder (R-0h..R-6, PL-Z-1a/Z-2 zframe repair): retired by the replan; text at git `85542ed6`; PL-Z-1a's arm and FINDING remain LIVE inputs to PZ-4.

## Architecture reference (the living contract)
Pipeline: Prolog AST → `lower_prolog` (four-port IR) → m3 `--run` (EMIT BINARY → RX slab) · m4 `--compile` (EMIT TEXT → as+gcc). GZ ports: δ = callee α, ε = callee β.
| Construct | α | β | γ | ω |
|---|---|---|---|---|
| `IR_GCONJ` (seq) | first goal's α | last goal's β | `goal[i].γ = goal[i+1].α` | `goal[i+1].ω = goal[i].β`; first → ω_in |
| `IR_CHOICE`/nary `IR_DISJUNCTION` | first clause α | record resume (rightmost box of the live arm) | each `.γ = γ_in` | `clause[i].ω = clause[i+1].α`; last → ω_in |
| `IR_GOAL` (call) | callee α | callee β | callee success → γ_in | callee exhausted → ω_in |
| `IR_ITE` | cond.α | ω_in (semidet) | cond.γ→Then, Then.γ→γ_in | cond.ω→Else, Else.ω→ω_in |
| `IR_UNIFY` | self | — | bind/match → γ_in | mismatch → ω_in |
| `IR_CUT` | self | — | γ_in (after TAG-2 restore) | cut barrier → ω_in |
| leaf | self | — | γ_in | ω_in |

Admission recipe (new deterministic builtin): (1) `IR_DET_FOO` in `IR.h` + name table `scrip_ir.c`; (2) `rt_pl_foo_cell(...)` — cell-based, trail-mark/unwind, no `g_resolve_env`, `prolog_atom_intern()` not `ATOM_*`; (3) `bb_det_foo.cpp` — FRQ per slot, one call, `test eax,eax; jne γ; jmp ω; def β; jmp ω`; (4) `bb_prepare` block in `emit_bb.c`; (5) `emit_core.c` dispatch case; (6) Makefile `RT_PIC_SRCS` + rule; (7) four `scrip.c` sites (`pl_gz_rule_body_goal_ok` · `pl_gz_rule_clause` op-filter · `pl_gz_count_synth_goal` · `pl_gz_build_goal`, named arm BEFORE the generic pair-comparator arm). `ir_call_arg` for `is_builtin_exec` builtins; `ir_pair_arg` for arity-2 pairs. New ζ kinds additionally: one edit each in `zd_k` + `zd_nops` + the grant authority — nothing re-spelled (ONE AUTHORITY).

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh && rm -f scrip && timeout 1500 make -j8 scrip > /tmp/build.log 2>&1 && make libscrip_rt && ls -la scrip out/libscrip_rt.so
# ⛔ CONCURRENCY PRE-CHECK (STANDING CONDITION): git -C . log origin/main..HEAD ; git status --porcelain — non-empty ⇒ clone fresh and measure THERE
bash scripts/test_smoke_prolog.sh && bash scripts/test_prolog_bb_honest.sh && bash scripts/test_prolog_rung_suite.sh && bash scripts/test_bench_prolog_modes.sh
bash scripts/test_gate_pl_no_new_global.sh && bash scripts/test_gate_emit_no_lang.sh
# primary sources (semantics rungs): local seat has /home/resources/{gprolog-master,swipl-devel-master} unpacked; web seats ask Lon (refs/ gitignored, per-session)
```

## ⛔ SESSION-CLOSE RULES LIVE IN `RULES.md` — NOT DUPLICATED HERE
"HANDOFF COMPLETE" requires a confirmed push; `scripts/handoff_status.sh` verbatim is the only truth; credential missing ⇒ BLOCKED, ask Lon in-chat and WAIT. Move THIS file's LIVE CURSOR every session — no cursor move, no close.
