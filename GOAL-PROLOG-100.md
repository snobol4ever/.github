# ⛔⭐⭐⭐ GOAL-PROLOG-100 — SCRIP PROLOG x86/x64 ALL THE WAY HOME: 100%, ONE PLAN, ONE FILE

**Opened 2026-08-16 s165 by Lon in-chat (Fable 5 seat), verbatim in substance:** *"We've been at this for 4.5 months and today is the day … Take a full accounting of every single Prolog related GOAL-*.md file. At this point we want ONE GOAL file to bring us all the way home to 100% Prolog … So much of those RUNGS and STEPS are completely obsolete. We basically have one plan moving forward: the THREE ZETAS — standing, activation frames, and spine cells. For every item you must verify against the source."*

**This file REPLACES (deleted this session, full text in `.github` git history at `94fa9093` and before):** `GOAL-PROLOG-BB.md` · `GOAL-PROLOG-BB-BYRD.md` · `GOAL-PROLOG-RTX.md` · `GOAL-PROLOG-IR-RUN.md` · `GOAL-PROLOG-100-SWI.md` · `GOAL-LANG-PROLOG.md` · `GOAL-PARSER-PROLOG.md` · `GOAL-PST-PROLOG.md` · `GOAL-PL-ZETA-CELLS.md` · `GOAL-PL-ZFRAME-RESTORE.md`. Every deleted Prolog GOAL-* name resolves HERE. Dispositions: §LEDGER.

## ⛔⛔⛔⭐⭐⭐ FACT RULE — NO NEW GLOBAL VARIABLES WITHOUT LON'S EXPLICIT PERMISSION (Lon 2026-08-13, in-chat) ⛔⛔⛔
**██ NO SESSION CREATES ANY NEW GLOBAL VARIABLE — file-scope mutable state, pinned VA slot, exported cell, parallel array, or any equivalent — in ANY repo, for ANY reason, without FIRST obtaining Lon's explicit in-chat permission in that same session. Linkage and state ride registers (r10/r11 wires) and the stack. ██** Enforcement: every diff checked for new file-scope definitions; a commit adding one without a cited in-chat grant is REJECTED on sight (precedent: g_pcall/g_pcall_wires/RT_AB_ANCHOR eradication, s55). **THE ASK ITSELF MUST BE A BANNER** in chat (name, type, owning file, purpose, why registers/stack cannot carry it). A quiet or inline ask does not count.

## ⛔ FACT RULE — `-O0`/`-O1` ONLY; `-O2`+ NEEDS AN EXPLICIT PER-SESSION LON DIRECTIVE (Lon s119/s126). Makefile default is `-O0`. ⚠ `make` skips recompilation when only `RT_OPT` changes — `rm -f out/rt_pic/*.o out/libscrip_rt.so` first or you relink stale objects. Label every perf number with its `-Ox`.

---

## ⭐⭐⭐ THE MODEL — ONE SYSTEM: THE THREE ZETAS (Lon's register ruling, same design of record as GOAL-SNOBOL4-100 / GOAL-ICON-100; ⛔ NO DUAL STORAGE ARMS IN THE END STATE)

| tier | motion | Prolog embodiment (target) | at SCRIP `c2ce75b1` (VERIFIED by grep + fresh build + live gates, s165) |
|---|---|---|---|
| **ζ-SPINE** | slides | per-BB FORTH cells on RSP: α grants, zero intermediate pops, ONE terminal release; goal operands via ZOPQ, results via ZRES; NON-POPPING (Lon s163/s164 directive) | **NOFC DEFAULT-ON tree-wide** (`zc_nofc`, `zeta_storage.c:741`, killswitch `SCRIP_NOFC=0`) — s164's headline NEXT landed by the SN4 campaign. Cells arm opt-IN `SCRIP_PL_CELLS=1` (`pl_cells_stamp`, `lower_prolog.c:13`, ZK-0..3 landed a5597f65/a2386c3f/a7c80b4b/d32f4dd2). **ZD-PL-A + slice 2a + slice 2b ALL LANDED**: ZD arm consults the same `dop_direct_fp` (`bb_call_fn.cpp:512`), `$trail_mark` nargs==0 intercepted (`:506`), `sink_unify2_str` called from the ZD arm with `zd_rsp=0` (`:515`) — the s163c NEXT ladder is DONE. |
| **ζ-ACTIVATION FRAME** | comes and goes | per-suspension predicate/generator activations; heap-fb ADOPT for resumable graphs (frame arrives in rdi, survives β-resume — carves NO rsp frame); chained, self-pop at ω | `emit_heap_fb_adopt()` named (`emit.h:644`, ζ-FB-3 s161); consumers unified `emit_rec_pin = emit_jmp_pin_legacy || emit_heap_fb_adopt` (`:645`); record payload = CELL BASE, `g_zctx` depth-immune runtime cell (DEL-T1). **The suspend class is the 70.3% wall (32,173 refs behind FLATDISP-5) — PL-ZK-5 OPEN.** RBP nearly eradicated tree-wide (`"rbp"`: 1 hit x86_asm.h, 4 emit.cpp; comments scrubbed `___`) — the s164 "RBP is vestigial on Prolog" finding is now the shipped direction. |
| **ζ-STANDING** | pinned for the life of its activation | per-graph activation base: wire header + params/locals, established once at α; zframe arm | zframe stamp **DEFAULT-ON** per-graph (PL-FR-2, `lower_prolog.c:1394`, killswitch `SCRIP_PL_ZFRAME=0`); mutual exclusion with cells arm structural (one graph never in both). **Proc-entry protocol (781 refs, 17.3%, FINDING-2026-08-01-PL §11) has NO BB to own it — the MECH-shaped decision, R-2.** |

**Register contract:** RSP = FORTH spine · pin is a LAYOUT CONTRACT, not a spelling switch (emit.h:643 law) · R13/R14/R15 scan-reserved · RBX = heap bump-frontier · rcx/rdx = γ/ω wires at proc enter · R10/R11 = glue wires, never scratch · GZ ports: δ = callee α, ε = callee β. **Canonical sources law (RULES.md): `refs/` is gitignored and NOT auto-populated — unzip Lon's supplied `gprolog-master` + `swipl-devel-master` archives into `SCRIP/refs/` each session that touches semantics (s165: the upload arrived EMPTY — ask Lon to re-supply when a rung needs them; `SCRIP/doc/{GPROLOG,SWIPL}-STUDY-2026-05-28-OPUS.md` are distilled notes, NOT a substitute for primary source).**

## ⛔ DEFINITION OF DONE — "100%" MEANS THIS AND NOTHING LESS
1. **Rung suite 164/164 BOTH modes** (`scripts/test_prolog_rung_suite.sh`) · smoke 5/5 (`test_smoke_prolog.sh`) · honest suite green **with rc≠0 COUNTED** (R-0h fixes the harness first).
2. **bench-22 green=22 m3+m4** (`test_bench_prolog_modes.sh`), oracle-correct vs gprolog/`.ref`; van Roy program classes closed (findall/forall serialize · assert/retract mid-computation · `is` over runtime-built terms · arity>8).
3. **ONE storage arm** — Lon's default ruling from the R-4 A/B table; the losing arm and its killswitches DELETED (no dual arms in the end state).
4. Gates strict + green: `test_gate_pl_no_new_global.sh` doomed-ratchet **14 → 0** · `test_gate_emit_no_lang.sh` OK · `test_gate_template_medium_invisible.sh --strict` GREEN (today: `bb_glue_flat.cpp(4)` + `xa_flat.cpp(8)` remaining) · TEMPLATE-ONLY/BOTH-MEDIUM greps 0 · `handoff_status.sh` COMPLETE.
5. Perf (LAST, and only under THE INSTRUMENT): emitted-instruction share ≥90% (s154 baseline 55.0%; s141 14%) · ≤1.2× then ≤1.0× GNU on the rail. **Wall time is DISQUALIFIED in this container class (s164) — no perf claim is admissible without the deterministic count instrument.**

## THE INSTRUMENT (boards are RUN, never transcribed; re-derive every count fresh)
```bash
bash scripts/test_smoke_prolog.sh                # 5-program tripwire, 3 columns
bash scripts/test_prolog_bb_honest.sh            # ⛔ KNOWN DEFECT R-0h: silently skips rc≠0 — a SEGV scrolls past as FAIL=0 (WITNESSED LIVE s165: rung10_programs_puzzles.pl SIGSEGV, tally 107/0/0)
bash scripts/test_prolog_rung_suite.sh           # 164 × interp+compile
bash scripts/test_bench_prolog_modes.sh          # bench-22 board (green/frontier/broken)
LD_PRELOAD counts: scripts/util_rtx_count_syms.sh  # the s148 PLT-interposition method — THE perf instrument; wall time disqualified
```
Twin-arm boards: run any board ×4 = {default, `SCRIP_PL_CELLS=1`} × {`SCRIP_PL_ZFRAME=0/1`} to locate which arm a regression lives in. Falsify every zero by injection before believing it.

## ⛔ LAWS THAT BIND EVERY RUNG (compact; RULES.md is the parent)
MONITOR-FIRST (2-way sync-step, then gdb spin-count; cheapest discriminating experiment first) · TEMPLATE-ONLY (`x86(...)` sole producer; missing encoder ⇒ add it) · BOTH-MEDIUM · **ICON+PROLOG TRIPLE-ARM CANARY (Lon s230, RATIFIED): every commit touching `src/emitter/`, `src/templates/`, or `x86_asm.h` runs the Icon canary AND this file's smoke+honest BEFORE commit — the couplings run BOTH ways (`ee315070` killed Icon and Prolog with one lea)** · ONE-AUTHORITY (grep the rule's shape, not the variable's name) · killswitch byte-identity + compile-time md5 blast radius over the corpus for every codegen change · NO-LANGUAGE-SENTINEL past LOWER · PEERS RULE (no BB_t fields; `ir_operand_push`) · ZERO C Byrd boxes · SINK CONTRACT survives (whole-arm-or-defer · faithful `sink_deref` · runtime-interned IDs un-bakeable · GC/floor skip for non-allocating arms only · width traps: `cmp` is 32-bit always, `x86("mov",<r32>,imm)` REX.W trap ~24 sites awaiting ruling · `_Static_assert` beside every baked layout · per-rung label decades · per-rung `SCRIP_NO_SINK<N>` — the family switch CANNOT measure a rung).

## ⛔⛔⛔ STANDING CONDITION — `GOAL-SNOBOL4-100` AND `GOAL-ICON-100` RUN CONCURRENTLY AND WRITE TO EVERY snobol4ever REPO
Same author identity, same four repos. Consequences (full text in GOAL-ICON-100 §STANDING CONDITION, binding here verbatim): separate clones are the DEFAULT · `git fetch` before trusting any recorded watermark; pin `git rev-parse HEAD` + clean status + `scrip` mtime to every number · a delta in SN4/Icon figures is NOT a Prolog signal (and vice versa) until the sibling cursor is checked · shared-emitter commits from siblings can move THIS file's watermark without mentioning Prolog — that is what the TRIPLE-ARM CANARY exists to catch · verify HEAD + 0-tracked-modified AT THE POINT OF MEASUREMENT (s164(10) parallel-writer precedent).

---

## ⛔⭐⭐⭐ LIVE CURSOR — 2026-08-16 s165 (Claude Fable 5, Lon in-chat: consolidation + "bring us all the way home") — **TEN FILES → ONE; EVERY CARRIED RUNG RE-VERIFIED AGAINST SOURCE AT HEAD; THE WATERMARK IS RE-MEASURED, NOT COPIED.**
Measured at SCRIP `c2ce75b1` · corpus `9c3aa509` · .github `94fa9093`, fresh clones, full clean build `-O0`, all boards run live this session:
**smoke 3/5** (clause, recursion FAIL ×3 modes) · **honest m3 107/0/0** ⚠ with the rc≠0 harness defect FIRING LIVE (rung10_programs_puzzles SIGSEGV scrolled past, tally unchanged — R-0h) · **rung suite interp 113/51 · compile 109/55 / 164** · **bench-22 green=8 frontier=0 broken=14** · gates: no_new_global PASS ratchet 14/14 · emit_no_lang OK · medium-invisible `--strict` FAIL, REMAINING `bb_glue_flat.cpp(4)` + `xa_flat.cpp(8)` (xa_flat was 128 → 8; nearly converted, plus one NEW small violator).
**VERIFIED LANDED BY PARALLEL SESSIONS (the old files' NEXT items, closed):** NOFC default-ON (`zeta_storage.c:741`) · ZD-PL-A slices 2a AND 2b (`bb_call_fn.cpp:506/512/515`) · sink addressing parameterized (`zd_rsp`/`resoff` params on `sink_*_str`) · heap-fb adopt arm named + consumers unified (`emit.h:644-646`) · RBP eradication shipped as direction (1 `"rbp"` hit in x86_asm.h).
**VERIFIED OBSOLETE (architecture no longer exists):** modes 1/2 deleted, `sm_interp.c` GONE, driver = `--run`/`--compile` only ⇒ GOAL-PROLOG-IR-RUN's 24 opens moot · PST-PROLOG Phase 1 COMPLETE, 0 opens · BB-BYRD 0 opens, broker landed · RTX ladder's own §SCOPE finding: asm fast arms vacuous where SINK inlines (the arbitration Lon was asked for is RESOLVED by consolidation: **SINK owns the hot symbols; RTX-3/0d/12 fold into R-6**).
**THE REGRESSION IS THE ROAD:** s164's 164/164 + 22/22 world predates the zeta conversion; the twin-arm substrate (2026-08-07 Lon directive) is mid-flight and today's 113/109 + 8/22 is its honest price. R-0 reconquers on the NEW substrate — never by reverting it.
**NEXT: R-0 (reconquest, MONITOR-FIRST, twin-arm boards locate the arm) · R-0h (harness honesty — cheapest, do first) · then R-1 ZK-5B per its restart protocol.**

## RUNGS — walk in order; each: own commit, buildable, killswitch byte-identity, twin-arm + SN4+Icon invariance (TRIPLE-ARM CANARY), cursor move, push mid-session

- [ ] **R-0h HARNESS HONESTY (do FIRST — it grades everything after).** `test_prolog_bb_honest.sh` must count rc≠0 as ABORT/FAIL, never skip. Witness: rung10_programs_puzzles.pl (SIGSEGV, s165). Falsify the fix by injection (a deliberate rc=139 program must move the tally).
- [ ] **R-0 RECONQUEST TO THE WATERLINE.** smoke 3/5→5/5 (clause, recursion witnesses first) · rung 113/109 ratchets upward every session, floor = last cursor's numbers · bench-22 8→11 (the 2026-08-09 s16 waterline)→22. MONITOR-FIRST per failure; run the ×4 twin-arm board to locate the owning arm before reading any code. ⚠ bench-22 was green=11 BOTH arms at `9313935` (s16) — the drop to 8 post-dates it; bisect against sibling shared-emitter landings, canary-style.
- [ ] **R-1 ζ-SPINE: ZK-5B VAR_REF MATERIALIZATION** (the live blocked rung; Option A AS IMPLEMENTED FALSIFIED s14, green 6→0). Restart protocol (carried verbatim from ZETA-CELLS s16): (1) refuse dirty tree; (2) re-derive both bench boards at HEAD; (3) **build a discriminating witness FIRST** — red on cells=1 today, green under correct materialization (`app/3`/`two.pl` do NOT discriminate — s14 correction of record); (4) re-apply `.github/WIP-2026-08-09-PL-ZK-5B-option-A-FALSIFIED.patch`, 2-way monitor first, test the `[nargs, nparams)` bound repair; (5) killswitch must cover the RUNTIME site (`rt.c`), not just the emitter (s14 killswitch-completeness defect); (6) ⛔ LON RULING WANTED: Option A (pre-allocate) vs B for VAR_REF materialization. Then **ZK-6 RATCHET** (bench 22/22 m3+m4 cells arm · rung 164/164 · smoke 5/5 · sink census ≥ legacy arm) and **ZK-7 GATES + A/B TABLE** feeding R-4.
- [ ] **R-2 ζ-STANDING: PROC-ENTRY MECH DECISION.** 781 frame refs (17.3%) with no BB to own them — the entry protocol itself (wire header, saved rcx/rdx/base). FUNCTION-class frame vs wire cells at fixed spine offsets; this is a MECH-shaped decision coordinated with GOAL-SNOBOL4-100's frame census — never land a new frame/glue protocol from this file alone.
- [ ] **R-3 ζ-ACTIVATION: THE SUSPEND/RESUME CLASS (the 70.3% wall).** PL-ZK-5: predicate/generator graphs — pending-cells vs pthread decided by MEASUREMENT (the ICN suspension-decision shape; watch Icon's ICN-GEN-GAMMA-RETAIN track, same class). Includes ζ-FB-4: instrument for any graph with `emit_jmp_pin_legacy() && !emit_heap_fb_adopt() && suspend-slot ≥ 0` — empty ⇒ close the β narrow/wide asymmetry; non-empty ⇒ it is a real measured decision. FALSIFY THE PROBE BY INJECTION before believing its zero. ⛔ Do NOT blind-widen `emit_fb_stmt_scan` to `IR_DISJUNCTION` — the bail is PRINCIPLED (choice-point arrivals are not rebalanced by the HEAD..RELEASE bracket); it needs a rebalancing proof (s164(5), s158's land mine).
- [ ] **R-4 ONE ARM.** A/B table cells-vs-zframe over the full board set (two-backends precedent) → **Lon's default ruling** → flip default → DELETE the losing arm, its stamps, and its killswitches. Completion: `grep -c 'SCRIP_PL_CELLS\|SCRIP_PL_ZFRAME' src/` reads only the sanctioned survivor.
- [ ] **R-5 FEATURES & CORPUS TO 100%.** (a) Banked engine defects, each MONITOR-FIRST: engine-wide silent-fail on undefined predicates (→ existence_error) · **NO-LCO recursion cap** (16-wide body dies at depth 10; the depth envelope every bench respects — likely the largest single unlock for bench-22) · nested-`\+` binding leak · retractall/1 gaps · int/float standard-order conflation (`rt_pl_term_compare`, two-oracle) · lexer escape three-site/two-behaviour · `:` at 200 vs 600 + `|` missing as 1105 xfy · double_quotes-under-dialect (⛔ LON RULING WANTED: target dialect; + PL-STRICT-1 silent-acceptance class). (b) Feature rungs carried from LANG-PROLOG: rung33 call/1..7 goal-as-Var · rung40 string type (dialect-gated) · rung41 list-ISO · rung42 meta-predicates · rung43 streams §7.9 · rung44 format §7.5 · rung45 DCG full · rung46 cross-clause catch/throw. (c) Suite reconquest (compressed from 100-SWI; full step text in git `94fa9093`): extractor (`swi_extract_tests.py` + `test_swi_suite.sh`) → SWI core files (term/arith/sort/call/exception/write/read/op/copy_term/syntax/bags/format) → library (lists/apply/aggregate) → db → ISO conformance (gprolog `tests/`); targets ≥80% each, ×2 modes + gprolog oracle. **Requires refs/ archives — ask Lon.**
- [ ] **R-6 PERF FENCE (LAST; every claim under THE INSTRUMENT, never wall time).** (a) finish medium-invisible: `xa_flat.cpp(8)` + `bb_glue_flat.cpp(4)` → `--strict` GREEN (⛔ LON RULING WANTED: 4 dead xa_flat arms convert vs DELETE); (b) surviving SINK rungs: SINK-5 `$is_v` delivery · SINK-9 `$trail_unwind`/`$unwind_nothrow` (design-heavy, LAST of the leaves) · SINK-10 sweep (const-unify, deref-only sites, route audit) · SINK-4 kk==4 per-site intern cache · SINK-2 enrich (int-slen0/bit-identical/recursive sub-arms); (c) RTX residue folded here: `queensn`+`queens` workload widening, eradication of proven-cold `c_*` bodies per RTX-CLAIMS ledger rules; (d) SINK-FENCE: gdb-sampled emitted share ≥90% (s154: 55.0%; `gc_collect_ex` is the largest unowned leaf — ⛔ LON RULING WANTED: GC carve-out or GC rung); (e) register residency H+TR (old RSP-F-4) → ≤1.2× → ≤1.0× GNU on the van Roy rail, fail-driven shapes, two mode-4 binaries.

## ⛔ FALSIFIED / DO-NOT-REDO (each cost a seat a measurement; retire only by RE-measurement on a moved substrate)
· **SINK-6/7 arith/cmp strcmp premise** — s147: `rt_pl_dop_ax_*`/`dop_cmp_fast` fast paths pre-empt the dispatch; the chain is COLD; the cost is the CALL. Re-verify at the post-rename runtime layout before any retry. Double-compare trap: C compares ints VIA DOUBLE — naive int-cmp diverges above 2⁵³.
· **ZK-5B Option A as implemented** — s14, green 6→0; restart from the WIP patch per R-1's protocol, never from memory.
· **Single-kind ZD admission** — s163 measured: unlocks ZERO runs; the unlock set is FIVE kinds, all-or-nothing per run, CBP in every blocking set.
· **Blind-widening `emit_fb_stmt_scan` to IR_DISJUNCTION** — s158 signature: 107/164 + SIGSEGV; the bail is principled.
· **The family switch as a rung instrument** — s146: it compares {all}vs{}; per-rung `SCRIP_NO_SINK<N>` only; ~5% effects need ~60 runs/arm; output identity ≠ work identity.
· **Wall time in this container** — s164: bimodal 649–1246ms on ONE binary; same-binary-twice control first, `^ms:` filtered, spread reported — or no claim.
· **`IR_VAR` "retired as vacuous — do not widen"** was ruled for SN4 GLOBALS; Prolog vars ARE graph locals — a reasoned re-open with Lon visibility, never a blind widen.
· Exit 0 is not exoneration when the divergence class is stdout-only; a probe that never enters the class produces a zero that looks like a pass — falsify by injection, both directions.

## ⛔ LON RULINGS WANTED (consolidated; strike when ruled)
(1) ZK-5B VAR_REF materialization Option A vs B (R-1). (2) Storage-arm default cells vs zframe (R-4, after the A/B table). (3) xa_flat 4 dead arms: convert vs DELETE (R-6a). (4) ~24 cross-language `x86("mov",<r32>,imm)` REX.W sites. (5) GC carve-out vs GC rung for the ≥90% KPI (R-6d). (6) double_quotes target dialect + PL-STRICT-1 (R-5a). (7) s163b/s164(10) provenance: parallel-writer contamination + rewritten pushed history (`1cc10758`→`548c5637`) — SHA-keyed baselines unsound until ruled (the STANDING CONDITION now names the mechanism; the ruling can simply be "expected, verify-at-measurement suffices").

## LEDGER — the 10 absorbed Prolog goal files (full text: `.github` git `94fa9093`)
`GOAL-PROLOG-BB.md` — the main ζ/SINK ladder; cursors s142–s164; SINK-1/2/3/4/8 landed; port-wiring + admission recipe carried below; open rungs → R-3/R-5/R-6. · `GOAL-PL-ZETA-CELLS.md` — ZK-0..3 landed; ZK-5B blocked (→R-1); ZK-5/6/7 → R-1/R-3. · `GOAL-PL-ZFRAME-RESTORE.md` — FR-2 default-ON at HEAD; vslot-override deletion gained +85/+79; twin-track rules → R-4. · `GOAL-PROLOG-RTX.md` — SINK-vs-RTX arbitration resolved: SINK owns; residue → R-6c. · `GOAL-PROLOG-IR-RUN.md` — modes 1/2 deleted, `sm_interp.c` gone: OBSOLETE wholesale; NO-AST law lives in RULES.md. · `GOAL-PROLOG-BB-BYRD.md` — broker landed, 0 opens: HISTORICAL. · `GOAL-PST-PROLOG.md` — Phase 1 COMPLETE, 0 opens: DONE. · `GOAL-PARSER-PROLOG.md` — Snocone self-host frontend experiment, unwired in the driver; PR-17 smoke/triage/recovery parked until self-host resumes (GOAL-PARSER-SC-TRANSPILE owns that thread). · `GOAL-LANG-PROLOG.md` — landed features s118–s155 in git; survivors → R-5b; PR-40..52 mode-split rechecks moot (2 modes, rung suite covers both). · `GOAL-PROLOG-100-SWI.md` — suite-reconquest arm → R-5c compressed; term_variables/2,/3 landed; DT_A-vs-DT_S atom lesson carried: **an atom is `DT_A` OR `DT_S` depending on entry — check both tags.**

## Architecture reference (carried verbatim — the living contract)
Pipeline: Prolog AST → `lower_prolog` (four-port IR) → m3 `--run` (EMIT BINARY → RX slab) · m4 `--compile` (EMIT TEXT → as+gcc). GZ ports: δ = callee α, ε = callee β.
| Construct | α | β | γ | ω |
|---|---|---|---|---|
| `IR_GCONJ` (seq) | first goal's α | last goal's β | `goal[i].γ = goal[i+1].α` | `goal[i+1].ω = goal[i].β`; first → ω_in |
| `IR_CHOICE` | first clause α | next clause α | each `.γ = γ_in` | `clause[i].ω = clause[i+1].α`; last → ω_in |
| `IR_GOAL` (call) | callee α | callee β | callee success → γ_in | callee exhausted → ω_in |
| `IR_ITE` | cond.α | ω_in (semidet) | cond.γ→Then, Then.γ→γ_in | cond.ω→Else, Else.ω→ω_in |
| `IR_UNIFY` | self | — | bind/match → γ_in | mismatch → ω_in |
| `IR_CUT` | self | — | γ_in | cut barrier → ω_in |
| leaf | self | — | γ_in | ω_in |

Admission recipe (new deterministic builtin): (1) `IR_DET_FOO` in `IR.h` + name table `scrip_ir.c`; (2) `rt_pl_foo_cell(...)` — cell-based, trail-mark/unwind, no `g_resolve_env`, `prolog_atom_intern()` not `ATOM_*`; (3) `bb_det_foo.cpp` — FRQ per slot, one call, `test eax,eax; jne γ; jmp ω; def β; jmp ω`; (4) `bb_prepare` block in `emit_bb.c`; (5) `emit_core.c` dispatch case; (6) Makefile `RT_PIC_SRCS` + rule; (7) four `scrip.c` sites (`pl_gz_rule_body_goal_ok` · `pl_gz_rule_clause` whitelist · `pl_gz_count_synth_goal` · `pl_gz_build_goal`, named arm BEFORE the generic pair-comparator arm). Key rule: `ir_call_arg` for `is_builtin_exec` builtins; `ir_pair_arg` for arity-2 pairs.

## Session Setup (every session)
```bash
git clone https://github.com/snobol4ever/.github /home/claude/.github; for r in SCRIP corpus x64; do git clone https://github.com/snobol4ever/$r /home/claude/$r; done
cd /home/claude/SCRIP && git config --local user.name LCherryholmes && git config --local user.email lcherryh@yahoo.com
bash scripts/install_system_packages.sh && rm -f scrip && timeout 1500 make -j8 scrip > /tmp/build.log 2>&1 && make libscrip_rt && ls -la scrip out/libscrip_rt.so
# ⛔ CONCURRENCY PRE-CHECK (STANDING CONDITION): git -C . log origin/main..HEAD ; git status --porcelain — non-empty ⇒ clone fresh and measure THERE
bash scripts/test_smoke_prolog.sh && bash scripts/test_prolog_bb_honest.sh && bash scripts/test_prolog_rung_suite.sh && bash scripts/test_bench_prolog_modes.sh
bash scripts/test_gate_pl_no_new_global.sh && bash scripts/test_gate_emit_no_lang.sh
# refs/ (semantics rungs): unzip Lon-supplied gprolog-master + swipl-devel-master into SCRIP/refs/ — gitignored, per-session, ask Lon when absent
```

## ⛔ SESSION-CLOSE RULES LIVE IN `RULES.md` — NOT DUPLICATED HERE
"HANDOFF COMPLETE" requires a confirmed push; `scripts/handoff_status.sh` verbatim is the only truth; credential missing ⇒ BLOCKED, ask Lon in-chat and WAIT. Move THIS file's LIVE CURSOR every session — no cursor move, no close.
