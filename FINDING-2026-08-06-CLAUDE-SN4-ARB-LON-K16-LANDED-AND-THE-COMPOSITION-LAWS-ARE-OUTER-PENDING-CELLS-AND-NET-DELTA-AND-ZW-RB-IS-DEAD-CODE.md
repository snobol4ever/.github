# FINDING 2026-08-06 (Claude, third dev; Lon directing) — ARB-LON-K16 LANDED; THE COMPOSITION LAWS ARE OUTER-PENDING-CELLS AND NET-DELTA-UNDER-A-CHAIN-OUTER; SCRIP_ZW_RB IS DEAD CODE AT HEAD

## 1. ARB-LON-K16 landed: the re-homed 16B cell extends the frameless ARBNO past body-K0

The frameless arm (cursor 2026-08-06, `bb_match_arbno_frameless`) required body-K0 because the one `{Δ0, yield}` cell had to sit at a static rsp offset for σ and exhaust. The widening keeps EVERY site static by **re-homing the cell to the frontier at each σ commit**: σ reads the previous cell one level in at `[rsp + kk]` (kk = Σ `zd_k` over the body span — THE ONE K AUTHORITY, never a second K list), carves a fresh 16 and copies `Δ0` forward; exhaust reads `[rsp + 0]`; the retract path frees 16 and lands exactly on the body frontier for PAIR(1) (body resume β). Growth is `kk+16` per committed instance, NON-POPPING, released only by the bracket whacks per THE MODEL. `bb_match_arbno_frameless_k` is ~14 insns; no counter, no links, no view, no chain — the Lon rulings hold at K>0.

Class admission (ONE routing authority, the emit.cpp `IR_MATCH_ARBNO` prelude; env killswitches `SCRIP_ARBNO_K16`/`SCRIP_ARBNO_FRAMELESS` read THERE ONLY; the template keys purely on staged `op_arbno_body_kk>0`, placed ABOVE the slot/geometry bombs since the arm reads nothing else): sequence-only bodies (`ALTERNATE/ARBNO/FENCE1/DEFER/PATREF/VALUE/CALL/CALL_VALUE/DISJUNCTION/ABORT` in span decline), `kk>0`, **not framed**, **no outer pending SAVE** (laws below). Statement release goes mech-2 when the arm fired in the statement: `g_arbk16_stmt` latch → `op_stmt_dyn` → `lea rsp,[rbp+op_zgpop]` before the old_rbp reload (the static `add rsp,ΣK` under-frees by 16×commits — N03/N04 witnessed as correct-output-then-SEGV in the successor statement). The FAIL-side static release is LAWFUL and untouched: the exhaust cascade retracts every committed cell during unwind, so fail exits carry zero dynamic growth (FOUR-CLAUSE LAW; witnessed by wF = fail-with-growth `:F(L2)` then a second match statement, oracle-exact).

Gate: probe suite **131/10/0/0** (N14 — ARBNO exhaust-retract — promoted out of XFAIL.run deliberately; the K16=0 arm reports N14 as its single expected delta, i.e. HEAD behavior reproduced). xc318 default: m3 **293/24**, m4 **287/28**, movement = `158_pat_cap_arbno_each_iter` F→P in BOTH modes, zero new fails, DIVERGE 7 = baseline. K16=0: m3 set EXACTLY = baseline; m4 delta = the documented 127/152 env-pad flake pair only. Killswitch-OFF `.s` byte-identical to `c7a276f6` on wB/158/052/164 via stash A/B.

## 2. Composition law A (X03/X05): an inner activation under a legacy chain outer must leave NET-ZERO stack delta

First suite run regressed exactly the nested probes X03/X05 (inner ARBNO with capture body under a chain outer). The chain outer's per-iteration block arithmetic assumes the K between its σ points is the STATIC span sum; frameless-K0 inners composed because zero cells = zero delta; K16's surviving committed cells are a VARIABLE delta by design. Framed (`_fr`, contained in any other ARBNO's span) therefore **declines** — depth>0 keeps legacy until the chain itself dies (W-5). Composition inside K16 outers is already consistent: the span blocklist routes any ARBNO-containing body to legacy.

## 3. Composition law B (166): a pending SAVE cell riding the frontier ACROSS the ARBNO forbids interposition

`(ARBNO(LEN(2) . C '-')) . W` broke as `W=[]`: W's SAVE parks its cursor cell on the frontier BEFORE the ARBNO and its COND reads it at a static Σ-K distance AFTER — K16's committed cells interpose. This is precisely what the carry-the-tail arm (R12-EXIT-1, `op_tail_fpb`) exists to relocate. **Trap discovered en route:** gating on `_fpb==0` killed the whole class — fc_tail's fpb counts BODY-INTERIOR pending cells too (it carries them across iterations), and body-interior cells are exactly what K16 re-homes. The sound discriminator is an **outer-pending scan**: any `IR_MATCH_ASSIGN_SAVE` inside the match bracket but OUTSIDE the body span → decline to tail. `fc_tail_arbno` is const-pure and now hoisted, called ONCE.

## 4. IR creation-order trap: MATCH_END's index PRECEDES the body nodes

The outer-pending scan first searched `(MATCH_BEGIN idx, MATCH_END idx)` and got `me=-1`: bracket nodes are created at statement-head parse, so MATCH_END's `g_emit_cfg->all[]` index is LOWER than the body nodes'. Index-interval search versus MATCH_END is unsound; body nodes ARE index-contiguous (the kk span proves it), so the sound window is **(this MATCH_BEGIN, next MATCH_BEGIN)**. Any future cfg scan bracketing "this statement" by kind+index must use the next-BEGIN form or pointer pairing via MATCH_END.operands[0].

## 5. SCRIP_ZW_RB=1 is dead code at HEAD, and the W-1b regression list is stale

Positive control falsified the wholesale-RB A/B: `.s` byte-identical both arms across the corpus (the lone m4 flip is the 127/152 flake). Root cause: the mech-2 predicate (emit.cpp ~2051) requires `Kc>0 && hpos>=0` from the UCLAIM owner table, which only `SCRIP_ZD_MATCH` populates — default OFF since s9 with measured justification. The gate is parasitic on disabled machinery; the 11 ZWR regressions recorded at W-1b describe a dead branch. The emitted-shape census shows the DEFAULT regime already IS the model trunk (α-carve, own-K unwind, ΣK/mech-2 statement whack, MATCH_END absolute whack, frames only {main · STATEMENT · FUNCTION · MATCH_BEGIN · FENCE1}). W-5 should delete the ZWR/UCLAIM forest rather than debug it.

## Mechanics worth keeping

The routing bug class repeated twice in one session: an arm that "doesn't fire" is a ROUTING question before it is a template question — the fc_tail claim silently owned the class, and later the else-branch guard re-staged kk on the killswitch-off path until the in-else scan was reverted to k0-only. One authority means the OTHER spellings must be deleted, not just outranked. Also: `cmd | tr; echo rc=$?` reports tr's rc — two witnesses "passed" while segfaulting until rc was captured before any pipe.
