# FINDING — 143 has converged into 145: the one remaining pattern-class wall is blob β-resume
2026-07-10, Claude (s20). gdb-traced, oracle-pinned. Two rungs landed en route (SCRIP `cefb4421`, `cde0c38b`); this doc scopes the NEXT rung, not a landed one.

## SUPERSESSION — corrects FINDING-2026-07-10-CLAUDE-SN4-NESTED-BLOB-DEFER-CALLOUT.md (do not resurrect its suspects)
That FINDING's law ("stored blob containing a DEFER callout fails") was correct; its three mechanism suspects (a) callout re-entrancy (b) blob exit wiring (c) px-compile mis-threading were ALL wrong — no blob-nesting bug exists at match time. Root cause: `sno_is_pattern_rhs` had no TT_DEFER awareness, so `OUTER = (*INNER ' ' *INNER)` never took the SNO$MKPAT route and silently degraded to STRING CONCAT (gdb: OUTER stored DT_S " ", one space). Fixed `cefb4421`: a TT_DEFER child under SEQ/CAT classifies as pattern-RHS; bare `X = *V` stays EXPRESSION (oracle pin: EXPRESSION/PATTERN/PATTERN on bare/concat/alt probes). 147 green both modes, watermark m3 279/6 m4 278/5/2. Its 124/143/147 grouping splits: 147 FIXED · 143 reclassified below · 124 residual open (capture/ALT inside an admitted blob; was never storage-blocked — its `token = (kw . K | ident . I)` top-ALT admitted pre-fix).
Bonus landed `cde0c38b`: DATATYPE had no DT_P arm (patterns printed STRING); the earlier "X2/X3 still degrade" reading was this label bug — they were DT_P all along. Functional discriminator before further classifier surgery saved a wrong fix.

## THE LAW (143's true shape, gdb-traced on `R = ARBNO(*LP)` stored, `'abc' POS(0) R RPOS(0)`)
Gate live-interrogated at lower_snobol4.c:1308: is_rhs=1 supported=1 (body det=1 → v1). Storage correct: DATATYPE(R)=PATTERN, blob fn set. Runtime trace: GETFN(R)×1, GETFN(LP)×0, no DEFMATCH → the blob runs, ARBNO (shy) null-matches, returns SUCCESS with δ unmoved; RPOS(0) fails; the draw-out must re-enter the RETURNED blob to extend ARBNO — and `bb_match_defer`'s β falls to ω by construction (template lines 62-63). 143 ≡ 145 (074/W07) ≡ DP-list item (3): β-resume across the blob boundary. One protocol flips both rungs (and possibly 124's ALT-resume residual).

## DESIGN SKETCH (recon done, NOT implemented)
Caller side (`bb_match_defer.cpp`): call convention is already `fn(rdi=ζframe, esi=entry)` with esi=0 hardcoded — a selector with one caller value. On γ: stash {fn, frame} in a `bb_slot_claim(16)`-claimed FR slot (grant the slot in `ir_drive_slot_assign` FIRST — the `_.x86_scratch_off` written-nowhere landmine, goal file "load-bearing findings"). On β: load both, call fn(frame, 1); eax==1 → γ (δ threads in R14 per the ratified register contract, ARCH-ICON) ; else `rt_zls_release(frame)` → ω. Success path already deliberately keeps the frame alive — the door was built for this key.
Blob side: PAT$ procs are ordinary compiled chains; head address reaches DT_P via `rt_gvar_assign_pat` (rt.c:128). Resume entry = prologue `test esi,esi; jnz .Lresume` with `.Lresume` planted at the chain's LAST top-level element's β edge (statically known at emit; normal right-to-left β wiring cascades correctly from there — a deterministic tail's β falls through its ω leftward, a generator tail (ARBNO/ALT) extends). ALTERNATIVE rail: mint a SNOBOL twin of the Icon handle-slot seam `rt_call_value_gen_h`/`rt_call_value_resume_h` (by_name_dispatch.c:739/753) — caller-held `void **hslot`, paired call/resume — if the direct esi dispatch can't be emitted language-blind. Either way the emitter conditions only on IR kind (FACT RULE: no language identity past LOWER).

## PRE-EMPTED LANDMINE (s18-b's exact class)
`IR_MATCH_DEFER` is NOT in `ir_is_generator_kind`. When β lands it MUST join, or the SEQ resumable-leaf re-point and capture backtrack-in skip defer boxes exactly as they skipped BREAKX (s18-b). Do NOT add it before β exists (today's β→ω makes non-generator classification correct; s18 also excluded defer from `sno_pat_deterministic`-adjacent v1 admission for primitives — but note `sno_pat_deterministic(TT_DEFER)=1` today by omission, which is DE FACTO right while callouts are atomic and must flip to excluded when β lands, or v1 ARBNO bookkeeping under-accounts resumable bodies).

## CONSTRAINTS (from the NON-NEGOTIABLE set, read this session)
Register contract R12/R13/R14/R15/RBX per ARCH-ICON ratified table (blob shares it; nothing to marshal). No C Byrd-box functions — resume is a port jump in emitted code. Template edits: ONE x86(...) concatenation, medium-invisible, encoders only in x86_asm.h (TEMPLATE-REVAMP R2/R7/R9/R10; the prologue dispatch likely needs no new encoder — test/jnz/def exist). Both media mandatory; internal label via `L(n)` if the prologue is template-emitted.

## OPEN QUESTIONS for the implementing session
Q1: WHERE is the PAT$ prologue emitted (suspected `bb_callee_frame.cpp` — sets esi at call sites; does any callee READ it)? Locate before designing the dispatch point. Q2: the g_sno_pats consumption site (the patproc compile loop) — confirm last-top-level-element β label is in hand at emit time. Q3: frame lifetime on repeated β (γ…β…γ chains) — slot persists, release only on β-exhaust; statement RELEASE-to-mark backstop already covers success-exit leaks (goal file, ARBNO section). Q4: recursion (`*group`, DP-7) — per-box FR slot is depth-1/non-recursive only; recursive same-node activation needs the frame-carried slot (ZLS2 pattern), which is DP-7's prerequisite ordering already recorded in the superseded FINDING.

## STATE AT WRITING
Watermark m3 279/6 m4 278/5/2 DIVERGE=1(1017), smoke 7/7×2, at SCRIP `cde0c38b` (two commits LOCAL, PUSH PENDING CREDENTIAL). Fail ladder: 143+145 (this rung) · 124 (blob-runtime capture/ALT residual) · expr_eval/140/141 (EVAL cluster, DP-1/DP-7) · 1017 (m4 diverge). Artifact regen (`util_regen_{benchmark,feature,demo}_s_artifacts.sh`) OWED at handoff — lower_snobol4.c touched.
