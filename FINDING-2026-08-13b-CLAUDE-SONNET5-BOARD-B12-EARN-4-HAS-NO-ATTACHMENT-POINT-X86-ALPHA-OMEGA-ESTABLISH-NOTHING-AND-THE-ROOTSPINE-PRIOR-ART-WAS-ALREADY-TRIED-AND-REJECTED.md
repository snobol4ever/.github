# FINDING — B-12: EARN-4 has no attachment point yet (x86_alpha/x86_omega establish nothing); the obvious "just give ARBNO an rbp frame" path was already tried, gdb-convicted, and deliberately deleted

**Session:** BOARD seat, SOLO (chat continues SOLO by Lon's own override, per GOAL-SN4-HOME.md). SCRIP HEAD `5547de99`, corpus HEAD `1dd3ff15`, .github HEAD `c39208cb` at session start. No code touched in SCRIP or corpus this session — read-only investigation plus this .github write-up.

## WHAT WAS DONE

1. **Re-verified current-HEAD state, not transcribed.** `install_system_packages.sh` run first (gdb 15.1 confirmed live). Built `scrip` clean. probe/bb: **160 pass · 5 REG (m3)**, **158 pass · 6 REG (m4)** — both +1 over the master file's stamped floor table, consistent with B-13 having landed since that table was cut. D12/D13 still in both regression sets. `X12` m4-liveness sanity check passes (mode genuinely live, not a quiet suite).
2. **Confirmed the three named witnesses at HEAD, not from the cursor's memory.** 178, 179, 182 (`crosscheck/patterns/`) all **BOMB now** (`rc=134`, `IR_MATCH_ARBNO: ... W-4`), not the "182 still HANGs (rc=124)" split s46 recorded — the guard's coverage looks tighter than it was at s46's hash. calculator-1/2/treebank-match/json-match(-fence) unchanged (BOMB). Source-grepped: all five are the manual's own `ITEM = SPAN(...) | *LIST` / `LIST = "(" ITEM ARBNO("," ITEM) ")"` idiom, D12.sno being close to verbatim.
3. **Read the BB-CODEGEN design set** (`ARCH-ICON.md`, `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md`) per RULES.md's non-negotiable gate for any template/`x86_asm.h` work, since B-12's fix necessarily touches both.
4. **Read the manual** (Ch.7 pp.85-86 Unevaluated Expressions; Ch.9 pp.121-123 ARBNO + Recursive Patterns) — see prior session's B-12 finding for the semantic grounding; this session adds nothing new there, just confirms it was actually available and read (s49's blocker).
5. **Traced the actual live ARBNO codegen, not the dispatcher comments.** `bb_match_arbno()`'s W-4/W-7-declined path falls through to `bb_match_arbno_frameless()` (`bb_match_arbno.cpp:85`). Read it in full. It is Lon's own 2026-08-06 ruling, implemented exactly as specified: ONE 16B **rsp-relative** cell `[rsp+0]`/`[rsp+4]` (DELTA0 + yield cursor), zero counter, zero chain, zero view register. The comment is explicit about the precondition: **"Gated to the nested-K0 class ... the frontier never moves inside the activation, so the cell is [rsp+0]/[rsp+4] from EVERY site."** A body containing a suspend-capable DEFER is exactly the case where that precondition fails — W-4/W-7 declines correctly, by design, not by omission.
6. **Found and read the code this simple design REPLACED.** `bb_match_arbno_DELETED_ARMS()` (same file, line 240) is dead — confirmed by grep, zero call sites anywhere in `src/`. It still contains a **complete prior attempt** at exactly what B-12 needs: an `_.op_arbno_chain` arm with an `SCRIP_ARBNO_ROOTSPINE` scaffold (default off) — a 48-byte alpha-carved per-activation root record `{anchor, yield, count, chainhead, exhaust-rsp, saved-rbp}`, with an `rbp`-borrowed "element view" register (`zv()`) repointed per iteration. **Its own comment records why it's dead:** *"MEASURED INSUFFICIENT ALONE ... gdb conviction: treebank-match 327B witness, inner `*group` re-entry aliased its header writes onto the outer element window (op_off+24 == op_sa-24 == +88 collide)."* And the frameless design that replaced it says why the whole chain-arm family was abandoned, not just patched: *"the counter machinery is what MEASURABLY aliased and killed the 5 nested probes (X02 gdb 2026-08-06)."*
   **Consequence for B-12/EARN-4:** reviving a per-element moving-view chain mechanism is a KNOWN-BAD path, already tried, already gdb-convicted on the exact demo (treebank) B-12 needs to fix. EARN-4's design (ONE frame at α, fixed `[rbp+K]`, no per-element re-establishment) is meaningfully different from ROOTSPINE's moving element-view — worth stating precisely so nobody "helpfully" resurrects ROOTSPINE thinking it's unfinished-but-on-the-right-track. It is finished, and it was the wrong track.
7. **Traced whether EARN-4's actual mechanism exists to attach to. It does not — verified at the two specific call sites, not inferred.**
   - `frame_need_of()` (`emit.cpp:662`) — its own comment: **"THE ONE AUTHORITY, DORMANT (no reader)."** Staging line 680: **"DORMANT -- nothing reads it yet."** Grepped every call site of `op_frame_need`: the only consumer anywhere is an opt-in `SCRIP_EARN_DIAG` stderr print. Zero behavioral readers. EARN-1's `ARBNO=1 ALWAYS` verdict is real and computed every compile — and changes nothing.
   - `x86_alpha()` / `x86_omega()` (`x86_asm.h:681,686-687`) — read the literal bodies: `x86_alpha()` is `x86_deflabel(X86P_ALPHA)` (defines a label, nothing else); `x86_omega()` is `x86_jmp(X86P_OMEGA)` (an unconditional jump). Neither pushes, saves, or restores `rbp` in any form, for any box, in any language. GOAL-RBP-EARN.md's own claim ("today's ZW-1 carve label/jump primitives, not a frame establisher") is exactly right, and this session re-verified it at the source rather than trusting the prose.

## THE ACTUAL BLOCKER, STATED PRECISELY

B-12 is not blocked on a BOARD-vs-RBP-EARN turf question. It is blocked on **EARN-3 not existing as a buildable mechanism yet** — RBP-EARN's own s49 cursor already reached this ("EARN-3 DECLINED (no attachment point until EARN-4/7 build a real frame)") but framed it as EARN-3 depending on EARN-4/7, which reads circularly. Restated from what this session verified directly: **EARN-4 (ARBNO's specific cell) cannot attach anywhere until *something* makes `x86_alpha()`/`x86_omega()` conditionally establish and release a per-box RBP frame, gated on `frame_need_of`'s already-computed verdict.** That mechanism is presently zero lines of code. It is product-wide by construction (α/ω are shared by every box in every frontend — SNOBOL4, Icon, Prolog, Snocone all call the same two functions), which is exactly why RULES.md's "ONE authority for the bytes, ONE for the decision" language exists and why this is a bigger blast radius than "fix ARBNO."

## NOT DONE / DELIBERATELY STOPPED HERE

No code touched. I traced this far specifically to avoid the RULES.md GATE-BEFORE-LAND failure mode named in this same master plan (RBX s40b: a fix landed without its own mandated sweep, reverted a session later at real cost) — building a product-wide frame-establishment primitive inside `x86_alpha`/`x86_omega`, unreviewed, on the plan's own named highest-collision surface, with no monitor witness minted yet for the new mechanism, is not a rung to open solo at the tail of an already-long session. The investigation is complete and load-bearing; the implementation is not attempted.

## RECOMMENDATION

This is a design decision bigger than either seat, and per the CHAT-ESCALATION/STREAK-BREAKER rule (two consecutive sessions — s46, s49 — have now closed on this same rung without landing code) it belongs in chat, not another solo attempt: **build the α/ω frame-establish/release primitive as its own rung — gated dormant-first behind `frame_need_of`'s existing verdict, byte-identical-by-default exactly as EARN-1 itself was proved, one box (ARBNO) as the first live consumer — before either seat opens `bb_match_arbno.cpp` again.** Whoever takes this: `frame_need_of` and its ARBNO=1 verdict are already correct and already tested byte-identical; the work is entirely in `x86_alpha`/`x86_omega`, not in re-deriving what needs a frame.
