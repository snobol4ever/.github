# ARCH — the ALT/DEFER/ARBNO choice-carrier depth story (recovered rationale, `src/emitter/emit.cpp` + `src/contracts/zeta_depth.{c,h}`)

**Recovered** 2026-08-23 (seat05) from `git show e25a5daf^:...` — the pre-strip blobs of `e25a5daf` ("GOAL-STYLE-200COL REACTIVATION 4", 2026-08-20, 6,919 comments deleted fleet-wide for the 200-col/zero-comment C style). This is the flagship relocation for `recover-stripped-design-rationale`; see `.github/FINDING-2026-08-22-recover-stripped-design-rationale-classification.md` for the classification methodology and why this cluster was chosen first (it is the one seat04 needed and did not have — see that FINDING's opening section). Comment text below is copied verbatim from the pre-strip source, not paraphrased; session tags (`sNNN`), witness program names, and measured numbers are load-bearing and must not be "cleaned up" by a future editor.

⚠️ **This is a recovered historical record, not a live spec.** The functions and call sites named below were read at `e25a5daf^` and re-verified present (by name) at the commit landing this doc (`git grep` receipts in the LEDGER-equivalent note at the bottom). Architecture drifts — before trusting any claim of "what currently calls what," re-run the verification grep yourself; do not assume this doc stays current forever.

## 1. THE PROBLEM

A SNOBOL4 pattern match suspends and resumes constantly — `ALTERNATE`/`DISJUNCTION` choice points, `ARBNO` retry, `*P` deferred/recursive pattern calls — and every suspension pushes a resume record that a later `β` (backtrack) port has to read back at the **same stack depth** it was written at. If anything between the push and the read moves the stack (an arm that carves its own scratch space, a nested choice node, a recursive re-entry), the read lands on the wrong bytes — silently wrong on a good day, `rip=0` / SIGSEGV on a bad one. Nearly every rung below is a different corner of "how do we know, at compile time, that the depth is still what it was when the record was written."

## 2. THE HISTORICAL ARC — READ THIS BEFORE THE PER-FUNCTION SECTIONS

**s121–s131: hand-written admission estimators, one per corner.** Each rung convicted a specific unsafe case and wrote a specific predicate to refuse it, then discovered the *next* corner the previous predicate didn't cover:
- s121: board conviction that a bare ALT/DISJUNCTION carrier is unsafe (an arm with interior carves shifts the 32B arm record under a fixed `res` offset). `resume_carrier_ok` REFUSES it into `sn4_seam_walk`'s tier-1/2 leaf-generator path.
- s124: `resume_carrier_ok`'s tier-1/tier-2 CARRIER OP-FILTER stated *behaviourally* (FACT RULE NO-LANGUAGE-IDENTITY) — leaf generators admitted unconditionally, deterministic seam elements admitted by walking a leftward beta chain the element templates already build. Everything else refused, so an unlisted op is safe by construction (never a blacklist that can miss a case).
- s125–s127: `sn4_alt_carrier` / `sn4_blob_choice_scan` — the **ALT-carrier depth story**: a stored blob whose body is a single unsealed ALTERNATE with all-leaf arms is admitted because leaf arms carve nothing, so `res`'s fixed `-32` lands exactly on the arm record. Proven by measurement (s121's 150-program conviction was specifically arms *with* interior carves), shipped behind `SCRIP_ALT_CARRIER` (default ON since s127, killswitch never deleted, per rule R-7).
- s128: `blob_choice_rbp_scan` / `sn4_choice_rbp_off` — widens admission to the **non-leaf single-choice** case by moving the arm record off the volatile `rsp` frontier onto the blob's own `rbp` activation frame (`blob_frame_bytes() += 32`), making the read depth-immune by construction instead of by leaf-only restriction. Guarded so it never frames a zero-demand (ZD-priced) blob — s127's own conviction, stated as a warning (§4).
- s136 (see below): the lattice that was meant to retire all of the above into one predicate.
- s177 (PF-1b): the pass-thru-frame law retires *most* of s128's tightenings (every scratch-cell leaf now homes in the blob's own frame), leaving only the genuinely semantic refusals: multi-record composition (`_nc>1`) and FENCE.
- s189 (ruling HQ-73): `resume_carrier_ok`'s **tier 3** — a disjunction whose beta re-yields at the same cursor — admitted on a class predicate (choice-node COUNT, not leaf/fence shape; FACT RULE NO-PER-OP-FILTER). This is the rung with the exact sentence seat04 needed (§3.3).

**s136: the ZETA-DEPTH (ZDP) lattice — an attempt to unify all of the above.** `zeta_depth.h`'s own header states the intent explicitly: *"Replaces the hand-written admission estimators (leaf_frame_member, blob_choice_rbp_scan, the s128 5-term gate, the s130 8-byte window law, the s131 BAL refuse) with the predicate every one of them was approximating: is the rsp depth at this program point statically known?"* `zdp_tier()` computes one three-way verdict (`ZDP_TIER_STANDING` / `ZDP_TIER_ACTIVATION` / `ZDP_TIER_SPINE`) from a lattice, meant to replace the "FIVE unrelated spellings of *where does this node's storage live*" the pre-s136 code had accumulated. A companion finding from the same rung (§2, second entry below) is itself a methodology warning worth preserving: the first ZDP cut asked the frame-slot registry functions *during planning* and the diff it produced was not an offset but a **label-counter mismatch** — because the registry functions have emit-time side effects, so *merely observing the plan perturbed the compiler* (the s132 monitor lesson — a verdict on a different program — recurring in a new spot). The fix was to keep the registry as the offset authority and let the planner (`zdp_tier`) supply only the tier, touching nothing.

**⛔ VERIFIED AT THE COMMIT LANDING THIS DOC, NOT ASSUMED: the s136 unification is not the live path.** `git grep zdp_tier(` outside `zeta_depth.c` itself returns nothing — `zdp_tier` is called only from a census/report function inside its own file. `emit.cpp` still calls `sn4_alt_carrier`, `sn4_blob_choice_scan`, `resume_carrier_ok`, and `blob_choice_rbp_scan` directly (23 references at the commit checked). So as of today, **both systems coexist**: the s121–s189 estimators are what actually gates codegen, and `zdp_tier`/ZDP is a computed-but-unconsumed second opinion. Whether that's deliberate (ZDP staged for a future migration) or drift (the migration started and stalled) is not resolved by anything recovered here — it's exactly the kind of question this doc exists so a future session doesn't have to re-derive from scratch. If you're about to change either system, grep both call sites fresh before assuming either one is "the" authority.

## 3. PER-FUNCTION RATIONALE (verbatim from the pre-strip source)

### 3.1 `sn4_alt_carrier()` — `src/emitter/emit.cpp`, the ALT-carrier killswitch

> ⭐ s127 THE ONE AUTHORITY for the SN4-ALT-CARRIER killswitch (R-4(a) defer class, witness probe/arbnostore/arbno_defer_altarg_red). TWO READERS, one function, flips in lockstep (the s124 two-reader law): (1) the :1143 ARBNO span scan — an unsealed DEFER span member becomes the admitted choice carrier for the af->PAIR(1) edge; (2) the blob β-dispatch — a stored blob whose body_root is a single unsealed ALTERNATE with ALL-LEAF siblings routes β to the ALT's own interior β (the depth story: leaf arms carve nothing, so res's fixed -32 lands rsp EXACTLY on the 32B arm record — s121's 150 conviction was arms WITH interior carves, refused here by sn4_blob_choice_scan). A third reader (blob_frame_bytes safety-floor framing for refused-carrier targets) was landed and RETRACTED same-session — see the ⛔ conviction at blob_frame_bytes: zd cross-blob pricing must learn the frame first. RESIDUAL HAZARD, opt-in only, honestly stated: ARBNO(\*p) where p resolves at match time to a REFUSED-carrier blob that γ-suspends retained interior — the admitted af edge's second lap can jmp through the retained record's cursor field (rip=0 bomb) where OFF silently wrong-answers; the honest-bomb-over-silent-wrong trade is deliberate (RULES) and the class IS the named next rung. =1 opt-in; default OFF byte-identical (pending the corpus ON-arm sweep + Lon's flip grant, the R-7/s124 protocol).

**Default-ON grant** (s127, verbatim): Lon in-chat — *"All your choices. I'm with you on this."* — given over the full gate table: crosscheck 318 m3 A/B +1/-0 zero regressions, arbnostore ON 10/10 m3+m4, seam 4/4, arbnofence 4/4, named movers 8/8 incl. 150/151 both arms, OFF byte-identity 122/122 by git-stash pristine manifest, beauty 10/622 NEUTRAL both arms. Killswitch inverted, never deleted (rule R-7): `SCRIP_ALT_CARRIER=0` restores the pre-s127 emission byte-identically.

### 3.2 `sn4_blob_choice_scan()` — the one classification scan

> ⭐ s127 ONE SCAN, TWO READERS (β-dispatch admission + blob_frame_bytes widening — never spell the classification twice, the s68/s70 disease): walks THIS blob graph counting unsealed choice nodes, flagging FENCE1, and verifying every non-choice member is a pure scanner-register leaf (the same op-filter as the :1143 span scan; an unrecognised op refuses by construction, the s125 op-filter principle).

Exit ports are excluded from the count by construction: *"every blob graph closes with the SUCCEED/FAIL terminal pair (the s120 FINDING's own words) — exit-port ROUTING, not matchers; they carve nothing on the spine and are neutral to the depth story. Measured here by gdb: the witness's 3-matcher blob is a 5-node cfg {SUCCEED, FAIL, ALTERNATE, LIT, LIT}."*

### 3.3 `resume_carrier_ok()` — the tier-1/2/3 admission predicate (the seat04 cluster)

> ⭐ s124 CARRIER OP-FILTER, stated BEHAVIOURALLY (no language name, FACT RULE NO-LANGUAGE-IDENTITY): tier 1 = leaf GENERATORS whose beta EXTENDS (one more instance/char) — the s121 class, admitted unconditionally. tier 2 (seam walk) = DETERMINISTIC elements whose beta is a self-undo fail-through that jumps LEFTWARD to the previous element's beta; entering the seam at its right end therefore walks the whole chain to the leftmost beta, which alone routes to omega. The seam is ALREADY BUILT by the element templates (measured s124: `n1_match_len_beta: sub r14d,0; jmp n0_match_arbno_beta`) — this predicate only stops REFUSING to enter it. REFUSED and left at the lbl_omega default: DISJUNCTION/ALTERNATE (s121 board conviction — an ALT carrier reads its 32B arm record at a depth the winning arm shifted; needs its own depth story), DEFER, FENCE1, ASSIGN_\* capture wrappers, BREAKX, and every op not named here — an unlisted op keeps s123 behaviour EXACTLY, so the widening is safe by construction.

**Tier 3, the exact rationale seat04 was blocked without** (s189, ruling HQ-73):

> ⭐⭐⭐ TIER 3 (s189, row alt-seam-tier, ruling HQ-73): a disjunction whose beta RE-YIELDS at the same cursor. NOT behind sn4_seam_walk() — tier 3 is not a seam walk, there is no leftward chain to enter and nothing to walk; the one killswitch is SCRIP_ALT_SEAM_TIER at the lattice, so all four consumers flip in lockstep (the s124 two-reader law). ⛔ THE ONE-CHOICE-NODE CONJUNCT IS EARNED, NOT INHERITED, AND IT IS A CLASS PREDICATE NOT A PER-OP FILTER (FACT RULE NO-PER-OP-FILTER): every IR_MATCH_ALTERNATE is admitted on the SAME question — is the 32B arm record this blob's beta will read still at the depth it was written. **MEASURED s189, three witnesses, one field apart: probe/passthru/ptw_min_fence_left_altresume and ptw_min_fence_alttop read nc=1 and CURE (oracle-equal match); ptw_min_alttop_nofence_ctl reads nc=2 — an ALT arm carrying a NESTED ALT, whose own `sub rsp,32` shifts the frontier under the outer record — and SIGSEGVs (rc=139) with the tier admitted and nothing else changed. lf=0 and fn=1 on BOTH cures, so neither leaf_ok nor has_fence separates a cure from the crash; the choice-node COUNT alone does.** ⛔ THE SECOND CONJUNCT WAS EARNED THE SAME WAY, ONE CRASH LATER: crosscheck/patterns/150_pat_star_var_fence_alts_no_arbno — a standing corpus GREEN whose own header says 'any future FENCE fix must keep this passing' — SIGSEGV'd (rc=139) at nc=1, and its diagnostic reads sd=1. Its `outer = (*cmd | *cmd *cmd | *cmd *cmd *cmd)` with `cmd = FENCE('a' | 'ab')` gives every arm a RIGHT-SEALED DEFER, and s177 PF-1b already wrote down why that cannot be re-entered from outside: a seal==1 DEFER's beta IS the fence-demarcation frame unwind, correct only in its OWN activation context, so an arm ending in one jumps that unwind against a foreign activation. The fact is not re-derived here: `_sd` is already computed at this function's ONE caller and is PASSED IN, so the sealed-defer classification keeps exactly one spelling (the s68/s70 disease this file names elsewhere). Tier 1 and tier 2 ignore the parameter, so they stay byte-identical. This is s121's ALT-depth conviction re-measured and STILL LIVE at ALT-FLAT (s202 makes arm INTERIORS footprint-0, which is not the same as an arm containing another carving choice node) and it is s126's finding verbatim: one-deep records compose by contiguity, two-deep do not yet. ⛔ The second ALT admission below (s127/s128) keeps its OWN wider gate and is NOT collapsed into this one: its `!_fn && (_lf || cro)` conjuncts would have REFUSED both cures here (both read lf=0 fn=1), so the two arms answer different questions and merging them would trade this rung's cures for its own.

This is the block seat04 was one `git show` away from losing entirely — it names the three witness programs, the exact `lf`/`fn`/`nc` readings that separate a cure from a crash, and the decisive sentence: *the choice-node COUNT alone does [separate a cure from a crash].*

### 3.4 `blob_choice_rbp_scan()` / `sn4_choice_rbp_off()` — the rbp-resident choice record (s128)

> ⭐ s128 ADMISSION for the rbp-resident choice record, ONE SCAN consumed by blob_frame_bytes (+32), sn4_choice_rbp_off (the template/dispatch offset) — lockstep by construction. Admits EXACTLY the refused-leaf single-choice blob (_nc==1, !_lf, !_fn: the captarm/beauty-Command class) and ONLY when the blob is ALREADY FRAMED by existing law (registry demand or wire-clobber): the s127 zd conviction stands VERBATIM — this predicate NEVER frames a zero-demand blob, so no ZD-priced caller-territory cell ever meets a frame it was not priced against. Leaf blobs (_lf) keep the proven s127 contiguity arm; _nc>1 stays the refused two-record-composition class (s126); _fn stays the FENCE semantic refusal (manual p.125/p.204).

The record's home once admitted: *"the record lives at [rbp - blob_frame_bytes() .. +31], strictly BELOW every registry slot (whose rbp offsets do not move), preserving 16B parity (push 8 + carve stays 16-aligned since 32 ≡ 0 mod 16)."* Depth-immunity mechanism, stated directly: *"res re-seats rbp from record[+24], alternate_β reads [rbp+cro+8], and the winning arm's interior carve (retained on the spine across γ, unwound by its own β chain) can no longer shift the read."*

**s177 PF-1b later retired most of this widening** (the pass-thru-frame law homes every scratch-cell leaf in the blob's own frame, so the s127/s128 tightenings this admission needed are "structurally gone" per that rung's own comment) — but `blob_choice_rbp_scan` itself remains the frame-sizing and offset authority; §2 above is the up-to-date summary of what still gates on it.

### 3.5 `blob_frame_bytes()` — the one authority for the activation carve

> ⭐ R-4(b) THE ONE AUTHORITY for the blob activation's carve BELOW the pushed caller rbp: 24 (entry wires r10/r11 + pad, 16B-parity with the push) + 16 \* (registry candidates in this blob graph); 0 when the blob has neither slot demand nor a wire-clobbering interior — and 0 is the byte-identical legacy shape (no push, no whack, the γ record keeps its pad, res its two loads): every such blob is untouched by this rung. Read by the α_body prologue, the CLASS D γ record, res landing and ω whack, so all four agree by construction (one scan, four sites — the s66 coherent-worlds law). s128: + 32 for the rbp-resident CHOICE RECORD when blob_choice_rbp_scan() admits...

### 3.6 `zdp_tier()` and the ZDP lattice — `src/contracts/zeta_depth.{c,h}` (s136)

> ⭐⭐⭐⭐⭐ ζ-ONE U-1 — THE UNIFIED ZETA ZONE PLAN (Lon in-chat s136: "Complete the unified zeta offset calculation graph traversals and the template code that uses that data. Finish the work to properly calculate and use offset from RSP and RBP based on standing, activation frames, and spine cells.") ONE TRAVERSAL, ONE VERDICT PER NODE, consumed by ONE accessor. Before this, the tree had FIVE unrelated spellings of "where does this node's storage live" — zone_ref (2 callers, leaf cell only), x86_zop/FRQ/FR (1151 callers, rsp-only, trusting x86_rsp_slide_known()'s hardwired 1), and 59 direct RDQ("rbp",..) sites in the seven ζ-family templates — so the PLANNER's opinion and the TEMPLATE's opinion about one operand could differ with nothing to detect it. ... THE THREE ZETAS ARE THE THREE ANSWERS (Lon's register ruling s81): ζ-STANDING -> rbp, the match-lifetime root; ζ-ACTIVATION-FRAME -> rbp, per \*P defer / per re-entrant site; ζ-SPINE -> rsp, compile-time-constant FORTH/LIFO.

**Methodology warning from the same rung** (⛔⭐⭐⭐, worth preserving on its own):

> THE PLANNER DOES NOT CALL THE REGISTRY, AND THAT IS THE WHOLE FINDING OF THIS RUNG (s136, MEASURED). The first cut asked capture/arbno/fence/leaf_frame_slot for every node and moved 81 of 656 programs — and the diff was NOT an offset: it was a LABEL COUNTER (`.Lbynamefnzd4` -> `.Lbynamefnzd3`). THE REGISTRY FUNCTIONS HAVE EMIT-TIME SIDE EFFECTS, so merely OBSERVING the plan perturbed the compiler. That is the s132 monitor lesson wearing a new costume: a verdict on a DIFFERENT program. The offset therefore stays where it already was — the CUSTOMER'S OWN STAGED FIELD, passed into ZREF by the call site that knows which customer it is — and the planner supplies only the TIER, which zdp_tier computes purely from the lattice and touches nothing.

The lattice itself, `zeta_depth.h`: *"(depth at use) - (depth at allocation), total wherever depth is path-invariant, failing in exactly two ways: (a) an unknown COUNT of allocations between a RESULT's write and its read, (b) a beta label reached with an unknown accumulation. BOTH ARE ONE PREDICATE: the meet at a join point disagreed. ... The failure list stops being a list and becomes the TOP set — the list becomes the code, which is the whole point of the rung."*

## 4. WARNINGS — DO NOT RE-LEARN THESE THE HARD WAY

- **⛔ s127 CONVICTION — do not widen the choice-record safety-floor frame by blob shape.** The first cut of R-4(a)'s safety floor did, and *measured five OFF=PASS → ON=SEGV movers: 120/131/165/181/182, all rc=139*. Root cause: a zero-demand blob's interior is ZD-priced — its leaf suspension cells live at static offsets the **caller's** carve reserved (gdb-measured on `120_pat_calc_add`: a span cell at `blob-entry+20`, caller territory by design). Framing it put the same baked spelling at `[rbp-4]` and shredded the frame head's own r10 wire slot (`rip=0x2_00401544`). A safety-floor frame for refused-carrier choice blobs needs the pricing to see the frame bytes first.
- **⛔ FENCE cannot be re-entered from outside its own activation.** A `seal==1` DEFER's beta *is* the fence-demarcation frame unwind, correct only in its own activation context — an arm ending in one that jumps that unwind against a foreign activation is the s177-documented hazard tier 3 (§3.3) had to account for.
- **⛔ Residual, opt-in-only hazard in `sn4_alt_carrier`, stated honestly rather than hidden:** `ARBNO(*p)` where `p` resolves at match time to a refused-carrier blob that γ-suspends retained interior — the admitted `af` edge's second lap can jump through the retained record's cursor field (`rip=0` bomb) where OFF silently wrong-answers. The honest-bomb-over-silent-wrong trade is deliberate (RULES) and named as its own next rung, not yet closed as of the source recovered here.
- **The s127/s128 gates are NOT interchangeable with the s189 tier-3 gate**, and the pre-strip comment says so explicitly: tier 3's `lf=0 fn=1` cures would have been *refused* by s127/s128's own wider `!_fn && (_lf || cro)` conjuncts. They answer different questions; do not collapse them into one predicate.

## 5. WHERE THIS SITS IN THE 200-COL STYLE / RATIONALE-RECOVERY PROJECT

This doc is the first relocation for `recover-stripped-design-rationale`. Indexed at `.github/RATIONALE-INDEX.md` under `sn4_blob_choice_scan`, `resume_carrier_ok`, `blob_choice_rbp_scan`, `sn4_alt_carrier`, `blob_frame_bytes`, `zdp_tier`. Remaining clusters (ranked by stripped-rationale-comment count, same commit) are queued as separate rows — see that FINDING's §"REMAINING CLUSTERS" for the list and `QUEUE.tsv` for the live rows.
