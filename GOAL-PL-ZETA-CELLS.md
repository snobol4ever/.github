# GOAL-PL-ZETA-CELLS.md — Prolog at 100% per-BB ζ CELLS on the RSP-topped FORTH stack

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

**CHARTER (Lon directive, 2026-08-07):** Walk Prolog to 100% per-BB allocation on the RSP-topped FORTH-style stack — the SAME ZD machinery SNOBOL4 built and Icon is completing (`GOAL-ICN-ZETA-CELLS.md` is the template; read it first every session). This is the ZD ladder's Prolog completion, not a parallel mechanism. Concurrent twin: `GOAL-PL-ZFRAME-RESTORE.md` (FRAMES on STACK) — BOTH KEPT, switch-selected, one graph NEVER in both arms, until Lon picks a default. Routing mirrors the Icon R-ZK-A ruling: the cells opt-IN suppresses the zframe stamp at the SAME LOWER site, A/B = one env var.

## ⛔⭐ LIVE CURSOR — s16 (2026-08-09, Claude Sonnet 4.6 — BUG-BETA LANDED `9313935`; bench-22 cells=1 == cells=0 green=11. NEXT RUNG = ZK-5B VAR_REF MATERIALIZATION per s14 protocol.)

**⭐ s16 WATERMARKS (HEAD `9313935`, -O0, TIMEOUT=6s, bench-22, re-proved post-rebase):** cells=0 **green=11 broken=11** · cells=1 **green=11 broken=11**. **FULL PARITY.** (s14's green=6 baseline improved to 11 via parallel sessions landing between `ada979eb` and `4a5f873`; BUG-BETA repaired the times10 regression that was the sole cells=1 deficit at `4a5f873`.)

**⭐ s16 WHAT LANDED (commit `9313935`, `bb_call_fn.cpp` line 552):**
BUG-BETA: `x86_beta_trampoline()` in the ZD arm of `bb_call_fn_str` incorrectly elided the β label define in BINARY mode (`--run`) for `CALL_BUILTIN_PROLOG` nodes on `pl_cells_graph`. Root cause: `flat_beta_used_scan` computes `bused[]` against `nodes[]` built by the codegen BFS. In BINARY mode (`g_is_text=0`) the GVA RPO pass (`if (!g_is_text || entry == g_emit_cfg->entry)`) adds group-anchor nodes to `nodes[]`, shifting indices so the β-tagged predecessor arc from node 2 (write/nl CBP) maps to a different `bused[]` slot than in TEXT mode → `bused[3]=0` → `op_beta_dead=1` → trampoline elided → forward ref to `n3_call_builtin_prolog_β` unresolved → `bb_emit_end` abort (rc=134). TEXT mode (`--compile`) skips the RPO extension → `bused[3]=1` → clean. Symptom class: `( cond → write(ok) ; write(failed) ), nl` programs abort m3, pass m4.
Fix: when `pl_cells_graph=1`, bypass `op_beta_dead` — emit `x86_beta() + x86_jmp(X86P_OMEGA)` directly. Net bytes identical to trampoline with `op_beta_dead=0`. SN4/Icon: `pl_cells_graph=0` → `x86_beta_trampoline()` unchanged = byte-identical. Diagnosis confirmed pre-patch via `SCRIP_BETA_ELIDE_OFF=1` escape (bench-22 cells=1 green=11 with elide-off = same result as after fix). ONE AUTHORITY: `bb_call_fn.cpp:552`.

**⭐ s16 CONCURRENCY NOTE:** Parallel seat wrote `6a4eba9` with the identical fix (referenced in the s15 cursor written by that seat). Our `9313935` reached origin first via rebase onto `53a4f62`; `6a4eba9` will be a clean no-op duplicate when that seat rebases. Per concurrency protocol: noted here, not silently dominated.

**⭐ s16 SESSION ANALYSIS:** BUG-BETA root cause was isolated through `SCRIP_BETA_ELIDE_OFF=1` escape → TEXT/BINARY split mechanism → GVA RPO pass as index-shift source. Fix: one-line additive change with byte-identical proof. All gates held, no broken commits, no push-pending banners, no help from Lon required.

**⭐ s16 ZK-5B NEXT SESSION:** (1) `git status --short` every repo — refuse dirty tree. (2) Re-derive both bench-22 boards at new HEAD. (3) **Build discriminating witness FIRST** — a program red on cells=1 today, green under correct VAR_REF materialization (`app/3` and `two.pl` do NOT discriminate — s14 CORRECTION OF RECORD). (4) Re-apply `.github/WIP-2026-08-09-PL-ZK-5B-option-A-FALSIFIED.patch`; 2-way sync-step monitor first; test `[nargs, nparams)` bound repair (s14 ROOT-CAUSE HYPOTHESIS). (5) Killswitch must cover RUNTIME site (`rt.c`) not just emitter — s14 KILLSWITCH-COMPLETENESS DEFECT rule. (6) Lon's A-vs-B ruling for VAR_REF materialization unchanged: Option A (pre-allocate TERM_VAR in `rt_jmp_frame_lexprep2`) vs Option B (template arm calling `rt_pl_fresh_var_ref()`).

## ⛔⭐ LIVE CURSOR — s14 (2026-08-09, Claude Opus — ZK-5B OPTION A **AS IMPLEMENTED IS FALSIFIED**: green 6→0. SCRIP TREE CLEAN AT `ada979eb`, ZERO CODE LANDED. NEXT RUNG = ZK-5B, restart from the patch.)

**⭐ s14 WATERMARKS (HEAD `ada979eb`, -O0, TIMEOUT=6s, bench-22 `test_bench_prolog_modes.sh`):** cells=0 **green=6 broken=16** · cells=1 **green=6 broken=16**. **PARITY HOLDS — s13's claim reproduces exactly.** s13's noted `qsort/m4` cells=1 regression (green=5) NO LONGER REPRODUCES; a parallel seat repaired it between `069c2fd8` and `ada979eb`. Re-derive again at open — this number moved under s13 without s13 touching it.

**⛔ s14 SESSION-OPEN LAND MINE — THE CONTAINER'S SCRIP TREE WAS DIRTY AND SELF-LABELLED `s14`:** `/home/claude/SCRIP` pre-existed at `ada979eb` with three uncommitted modified files (`emit.cpp`, `rt/rt.c`, `bb_var_ref.cpp`) implementing Option A, with comments already reading `PL-ZK-5B OPTION A (s14)`. **s14 did not write them.** They were measured, not adopted. Preserved verbatim at `.github/WIP-2026-08-09-PL-ZK-5B-option-A-FALSIFIED.patch`. **ADD TO SESSION OPEN: `git status --short` on every repo BEFORE the first build; a dirty tree at open is an unattributed artifact.** (Prior precedent: the s217 "I twice read a working tree as origin" finding.)

**⭐ s14 THE FALSIFICATION (four boards, one HEAD):** clean cells=0 **6** · clean cells=1 **6** · +patch cells=1 **0** · +patch cells=1 `SCRIP_ZD_PL_VR=0` **5**. The patch costs the entire board with VAR_REF admitted, and **still costs one program with the documented killswitch thrown.**

**⛔ s14 KILLSWITCH-COMPLETENESS DEFECT (rule-shaped, affects every future rung):** `SCRIP_ZD_PL_VR=0` gates VAR_REF **admission** in `zd_wl_kind` — emitter-side only. Option A's third edit lives in `rt_jmp_frame_lexprep2` (`rt.c`) and fires **unconditionally on every Prolog lex frame**, killswitch or not. The RUNGS-preamble obligation *"each: own commit, killswitch, `=0` byte-identity"* is therefore **unsatisfiable as written** for this rung — there is no `=0` position from which the runtime half disappears. **RULE: a rung with both an emitter arm and a runtime arm must read the killswitch at the RUNTIME site too, or both sites consult one env var. Otherwise every `=0` byte-identity proof for that rung is vacuous.**

**⭐ s14 CORRECTION OF RECORD — s13's ACCEPTANCE WITNESS DOES NOT DISCRIMINATE:** s13 NEXT-SESSION step (4) says *"Verify `app([a,b],[c],R)→[a,b,c]` … with VR enabled."* Measured: **`app/3` already prints `[a,b,c]` on the cells arm at clean HEAD with VR disabled**, and is the FIRST casualty when the patch lands (empty output). It is a **regression detector, not an acceptance witness.** `two.pl` two-clause dispatch prints `pos` on all four configurations and discriminates nothing either. ⛔ **ZK-5B HAS NO WITNESS THAT IS RED-WITHOUT AND GREEN-WITH. Build one before the next attempt** — otherwise the next session again cannot distinguish progress from parity, which is how s10/s11/s12 each burned a session.

**⭐ s14 ROOT-CAUSE HYPOTHESIS (NOT PROVEN — no monitor, no gdb; do NOT act on it as a diagnosis):** the rt.c loop promotes a param slot when `_s->v == DT_SNUL`. `descr.h` pins `DT_SNUL == 0` and states *"Zeroed memory is a valid null string"* — so that test **cannot separate "never bound" from "legitimately zero."** The loop also spans `[0, nparams)`, not `[nargs, nparams)`, so slots `rt_frame_bind_args` just filled are clobbered into fresh unbound vars — the shape that yields `app/3` → empty. **If it holds, the repair is the BOUND, not the tag:** seed only `[nargs, nparams)`, and never read `DT_SNUL` as an "unset" sentinel anywhere. Per RULES MONITOR-FIRST, point the 2-way sync-step monitor here first and let it name the divergence.

**⭐ s14 NEXT SESSION PROTOCOL:** (1) `git status --short` every repo; refuse to adopt a dirty tree. (2) Re-derive both boards at the new HEAD. (3) **Build the discriminating witness FIRST** — a program that is red on cells=1 today and would go green under correct VAR_REF materialization. (4) Re-apply `.github/WIP-2026-08-09-PL-ZK-5B-option-A-FALSIFIED.patch`; monitor-first to the divergence; test the `[nargs, nparams)` bound repair. (5) Move the killswitch read into `rt.c` so `=0` byte-identity is provable. (6) Lon's A-vs-B ruling is **unchanged** by this result — this session falsified an *implementation*, not the design.

**⭐ s14 LANDED: NOTHING.** SCRIP tree returned to clean `ada979eb`; no commit. Publishing a green→0 board as a rung landing violates RULES ("No broken commits"). `.github` carries the patch + the FINDING + this cursor.

## LIVE CURSOR — s13 (2026-08-09, Claude Sonnet 4.6 — ZK-5B PARTIAL: Bug1+4+5+6 LANDED at `6e69da62`; VAR_REF GATED PENDING TERM_VAR MATERIALIZATION; bench-22 cells=1 == cells=0 baseline green=6)

**⭐ s13 WATERMARKS (HEAD `069c2fd8`, post-rebase -O0, TIMEOUT=6s):** bench-22 cells=0 green=6 broken=16 (baseline held). cells=1 green=5 broken=17 (qsort m4 FAIL — pre-rebase was green=6; s226 ZK-2 FIX parallel session introduced qsort/m4 regression post-rebase; noted per concurrency protocol, not silently dominated). PL coupling gate PASS. SN4 byte-identical PASS.

**⭐ s13 WHAT LANDED (commit `6e69da62`):**
Bug 1 (emit.cpp LP-2): `flat_all_zd` gate now skips `zd_out[i]==-1` (never-visited) nodes on `pl_cells_graph`. β-continuation nodes in multi-clause `proc_flat` graphs are structurally absent from every gamma-chain run; prior `!zd_on[_i]` loop vetoed `flat_all_zd` permanently. Effect: fib `n=60 armed=13 all_zd=0` → `all_zd=1`.
Bug 4 Option C (bb_lit_scalar, bb_var, bb_call_fn): dual-write to both ZRES(0/8) (FORTH spine) and FRQ(op_off+0/8) (ZLS frame) using r10/r11 intermediaries.
Bug 5 (bb_call_proc_staged stage_arg_inline + DC LEA): FB-STMT refinement was collapsing all arg reads to `[rsp+0]` on `pl_cells_graph`. Changed to `RBPRAWQ(slot)` which directly names `[rbp+slot]`.
Bug 6 (emit.cpp zd_wl_kind + bb_var_ref.cpp): Prolog VAR_REF admission disabled via tautology guard (`!pl_cells_graph && pl_cells_graph`). ICN arm guards `!pl_cells_graph`.

**⭐ s13 VAR_REF BLOCKER (Lon decision required):** Prolog `IR_VAR_REF` cells arm cannot simply copy `[rbp+op_sa]` (gives NULVCL for fresh variables — Prolog variables must be heap-allocated `TERM_VAR` cells for unification/trailing). Icon NAMETRAP `{DT_N,slen=1,&slot}` is also wrong — `$unify` rejects it as a non-Prolog term. With VR excluded from admission, predicates with variable arguments get `flat_all_zd=0` → FRQ fallback. Programs passing cells=1 == cells=0 (baseline parity, not a gain). TWO OPTIONS: (A) pre-allocate `TERM_VAR` cells for each param slot in the ZLS frame initializer (`rt_jmp_frame_lexprep2` extension); (B) Prolog VAR_REF reads the param DESCR and calls `rt_pl_fresh_var_ref()` when it detects NULVCL. Option A is cleaner (one authority, zero template change); Option B is a template arm. Lon rules.

**⭐ s13 NEXT SESSION PROTOCOL:** (1) Re-derive watermarks at new HEAD (concurrent sessions). (2) Lon decides Option A vs B for VAR_REF materialization. (3) Implement chosen option; re-enable VAR_REF admission (remove tautology guard). (4) Verify `app([a,b],[c],R)→[a,b,c]`, `nrev`, `fib` with VR enabled. (5) Run bench-22 cells=1 — expect to exceed baseline green=6. (6) Commit as `PL-ZK-5B: VAR_REF TERM_VAR materialization`.

**ZK-0/ZK-1/ZK-2/ZK-3/ZK-4 ALL COMPLETE. ZK-5 IN PROGRESS — NEXT RUNG = ZK-5B: all four bugs attempted; false positive measurement caught before commit; Bug 2a conflict open. See s12 findings below.**

**⭐ s12 WATERMARKS (HEAD `c09012d5`, -O0, TIMEOUT=6s):** bench-22 cells=0 PASS=12 FAIL=10 == cells=1 PASS=12 FAIL=10 (byte-identical — HEAD unchanged, no regression introduced). PL coupling gate PASS. SN4 smoke PASS=6. Key witnesses: nrev PASS cells=0/1; fib PASS cells=0, FAIL cells=1 (pre-existing at this HEAD — confirmed by stash A/B test).

**⭐ s12 ATTEMPTED FIXES — FALSE POSITIVE CAUGHT BEFORE COMMIT:**
Five files edited (emit.cpp ×3, bb_call_fn.cpp ×1, bb_lit_scalar.cpp ×1, bb_var.cpp ×1, bb_var_ref.cpp ×1). First bench-22 cells=1 run returned PASS=12 — appeared correct. Rebuild revealed the test ran against the OLD binary (make had not yet rebuilt). Re-test with rebuilt binary: PASS=5 FAIL=17. All working-tree changes reverted. Tree clean at `c09012d5`.

**⭐ s12 BISECT RESULT — BUG 2A IS THE REGRESSION SOURCE:**
`SCRIP_ZD_PL_CBP=0` (disables all Prolog ZD admission) restores fib to PASS at cells=1. This pins the regression to Bug 2a: `zd_k($trail_mark) return 0` on `pl_cells_graph`. The conflict: `zd_plan` is called BEFORE the carve and uses `zd_k` internally to accumulate `zd_out[]` depths. If `zd_k` returns 0 for trail_mark, `zd_plan` already computes all downstream `zd_out[]` offsets WITHOUT trail_mark's 16B. The proc-entry carve (`_plk`) is also computed via `zd_k` — so K_total decreases by 16B and `sub rsp,K_total` carves 16B less. But the ZOPQ depth differences (`zd_out[consumer] - zd_out[producer]`) were computed by `zd_plan` WITH the corrected K=0 — they should be self-consistent. ROOT CAUSE NOT YET ISOLATED — the bisect proves Bug 2a causes the regression but the precise mechanism (depth model inconsistency vs template read offset vs carve/release mismatch) was not traced within context budget.

**⭐ s12 NEXT SESSION PROTOCOL:**
(1) Re-derive watermarks at new HEAD (concurrent sessions continue landing). (2) Isolate Bug 2a with census: `SCRIP_ZD_CENSUS=1 SCRIP_PL_CELLS=1 ./scrip --run fib.pl 2>&1` — compare armed/K_total with and without the `zd_k` trail_mark K=0 edit. (3) Check `zd_plan` source (~line 1974): confirm it calls `zd_k` for depth accumulation — if yes, Bug 2a is self-consistent in the planner and the regression is elsewhere (likely the template arm or the carve/release boundary). (4) Apply Bug 1 fix ALONE and verify bench-22 cells=1 is ≥ baseline before adding Bug 2a. (5) If Bug 2a is confirmed broken: Lon decision required — either (a) abandon K=0 for trail_mark and use a different mechanism to prevent the FRQ/ZRES mismatch, or (b) trace the exact depth disagreement and patch it.

**⭐ s12 BUG 1 FIX — CORRECT DESIGN, NOT YET ISOLATED:**
LP-2 `flat_all_zd` loop: skip nodes with `zd_out[_i]==-1` (never visited by any run — `zd_plan`'s init sentinel) on `pl_cells_graph`. The prior `!zd_wl_kind` guard is INSUFFICIENT: admitted K=0 nodes (DISJUNCTION n_operands==0, MOVE_LABEL) that are unreachable in the RPO chain (e.g. retry DISJUNCTION in `$reverse_/3`) have `zd_out=-1` and `zd_on=0` and wrongly veto `flat_all_zd`. The `zd_out==-1` sentinel is the exact "never visited" marker set by `zd_plan` init. This edit is independent of Bug 2a and safe to land first.

**⭐ s12 BUG 4 OPTION C — DESIGN CORRECT, DEPENDS ON BUG 2A RESOLUTION:**
Dual-write pattern (bb_lit_scalar.cpp/bb_var.cpp/bb_var_ref.cpp): when `pl_cells_graph && op_zres && op_off>=0`, write result to BOTH `ZRES(0/8)` (cells spine) AND `FRQ(op_off+0/8)` (ZLS rbp-relative slot). `bcps_spine_gen_arm` reads args via `stage_arg_inline→FRQ(slot)`. Without dual-write, FRQ holds NULVCL and generator calls bind wrong args. Design is correct but inert until Bug 2a (which gates `flat_all_zd=1`, which is required for the gen-arm cells path to fire) is resolved.

**⭐ s11 COMPLETE ROOT CAUSE CHAIN — THREE BUGS FOUND AND FIXED (then reverted together because fix 4 not yet complete):**

**Bug 1 (FIXED, emit.cpp LP-2):** The all-nodes `zon[]` loop in LP-2 yields `flat_all_zd=0` permanently for multi-clause predicates because β-continuation nodes (n13, n26…) have `zon[k]=0` (not in any gamma-chain run). Fix: add `g_zd_pl_no_declined_runs` static; init=1 at `zd_plan` entry for `pl_cells_graph`; reset=0 at each decline exit; LP-2 uses this flag instead of the all-nodes loop when `_pl_cells`. THREE edits in emit.cpp (static, init, reset, LP-2 branch).

**Bug 2 (FIXED, emit.cpp zd_k + bb_call_fn.cpp):** With Option A, `$trail_mark` writes to `FRQ(zls_off(nd))` (fixed rbp-relative slot) instead of `ZRES(0/8)`. Therefore `zd_k($trail_mark) must be 0` on `pl_cells_graph` — the node contributes no bytes to the spine. Without this, all subsequent nodes' ZOPQ depths are wrong (off by 16B each), causing every `$ix_g` comparison to read from the wrong stack address. Fix: add early return `if (op==IR_CALL_BUILTIN_PROLOG && pl_cells_graph && sval=="$trail_mark") return 0;` to `zd_k()` ONE AUTHORITY line. ALSO fix carve loop: change from `zd_wl_kind(_nd)` to `zd_on[_i]` (only armed nodes contribute to K_total — β-continuation nodes never arm).

**Bug 3 (FIXED in bb_call_fn.cpp, Option A store):** ZD arm result store for `$trail_mark` on `pl_cells_graph`: write to `FRQ(bcfn_result_slot(pBB))/FRQ(+8)` instead of `ZRES(0/8)`. The trail mark integer must survive backtrack at any RSP depth; β-continuation nodes (`$unwind_nothrow`, `$trail_unwind`) read it from `FRQ(resoff)` at their own execution depth.

**Bug 4 (ROOT CAUSE OF REMAINING REGRESSION — NOT YET FIXED, `bb_call_proc_staged.cpp`):** When `flat_all_zd=1` fires (Bug 1 fix), generator-predicate calls through `bcps_spine_gen_arm` now run the cells-arm prologue carve (`sub rsp, K_total`). But `bcps_spine_gen_arm` reads args from `FRQ(slot) = [rbp+slot]` — the ZLS frame. In the cells arm, predecessor nodes (e.g. LIT_INTEGER) write to ZRES (`[rsp+0]`) only, never to FRQ. So `[rbp+slot]` holds the NULVCL initializer — arg value is lost. RESULT: `rt_jmp_frame_lexprep2` binds NULVCL to `[rbp+16]` (the param slot); `$ix_g` sees an unbound variable → always PASSes → wrong clause selected.

**⭐ s11 FIX 4 DESIGN (bb_call_proc_staged.cpp `bcps_spine_gen_arm`):** When `_.op_zres=1` (cells arm): read arg[i] from `[rsp + K_self + (nargs-1-i)*16]` instead of `FRQ(slot)`. Rationale: after n65's own `sub rsp, 16` (K_self=16), the predecessors are on the spine in LIFO order — last arg pushed (arg[nargs-1]) is nearest RSP, first arg (arg[0]) is furthest. BUT: this formula is only correct if the run has EXACTLY `nargs` predecessor nodes between n65 and the bottom of the spine at that point. If there are OTHER K=16 armed nodes between n65 and its arg predecessors, the formula overcounts. **THE ROBUST FIX** is: use the actual `zout` depth differences. Since `zd_nops=0` for CALL_PROC_STAGED on `pl_cells_graph`, `op_zread[]` is not staged. Options: (A) stage `op_zread[]` entries anyway for gen-arm cells (override nops=0 guard for the staging choke only — don't change planner nops); (B) store arg depths in a separate per-site table at emit time; (C) make the gen-arm cells path fall back to FRQ by dual-writing: every K=16 producer in the cells arm also writes to its ZLS FRQ slot. Option C is the simplest: add `FRQ(zls_off(nd))` dual-write to `bb_lit.cpp`/`bb_var.cpp`/`bb_var_ref.cpp`/`bb_call_fn.cpp` ZD arms. Then `bcps_spine_gen_arm` FRQ reads are already correct — no template change needed. Cross-request to ZK-5B author: choose between A/B/C and note ruling in next cursor.

**⭐ s11 VERIFIED CORRECT (with bugs 1+2+3 fixed, bug 4 present):** `foo(0)→zero` ✓ `foo(5)→pos` ✓ (two-clause dispatch correct). nrev PASS ✓ (single-arg generator). fib FAIL (multi-arg recursive generator, Bug 4 path). The three-edit batch (emit.cpp×4, bb_call_fn.cpp×1) is provably correct in isolation; regression only when bcps_spine_gen_arm reverted without the companion fix.

**⭐ s11 NEXT STEPS FOR NEXT SESSION:** (1) Re-apply the four emit.cpp edits + one bb_call_fn.cpp edit (all described above). (2) Implement Fix 4 in bb_call_proc_staged.cpp via chosen approach (C = dual-write recommended). (3) Verify bench-22 cells=1 ≥ baseline. (4) Commit as "PL-ZK-5B: Option A trail-mark FRQ + LP-2 no-declined-runs + gen-arm dual-write". (5) Update cursor. NO CREDENTIALS NEEDED for SCRIP/corpus (public repos); `.github` push needs credential.

**⭐ s10 CORRECT ROOT CAUSE — REGIME MISMATCH IN MULTI-CLAUSE proc_flat GRAPHS (all WIP reverted, tree clean at `709d5f19`):**

s9's two-part fix (g_zd_pl_no_declined_runs gate + nodes[]/zd_on[] carve) was implemented and verified to set `all_zd=1` correctly. With Part A (bb_move_label.cpp null lbl_t1_p guard) also applied, the SEGV was traced via gdb + m4 asm inspection. ROOT CAUSE: the s9 fix is INSUFFICIENT because `flat_all_zd=1` is a per-chain flag but a multi-clause `proc_flat` graph contains BOTH armed nodes (clause body 1: 13 armed in runs 0–12) AND declined nodes (clause body 3: n13–n59, emitting FRQ `[rbp+N]` reads/writes) in the SAME code graph. When `flat_all_zd=1` fires and the prologue emits `sub rsp,176`, clause body 3 still executes with its FRQ-based storage: `$trail_unwind` at n59 reads `[rbp+80]` (FRQ resoff), but `$trail_mark` at n0 wrote its result to `[rsp+0]` (ZRES on the cells arm). `[rbp+80]` was never written on the cells path → garbage mark (-187890968) → SIGSEGV in pl_trail_unwind. Confirmed in m4 asm: n13/n26/n59 (`$unwind_nothrow`/`$trail_unwind`) emit `mov rax,[rbp+80]` — FRQ — while n0 (`$trail_mark`) emits `mov [rsp+0],rax` — ZRES. Two regimes in one graph, one per-chain carve: structurally incompatible.

**⭐ s10 ZK-5B ATTEMPT — DUAL-WRITE FIXES TRAIL SEGV BUT REVEALS WRONG-DISPATCH BUG (reverted, tree clean at Part-A-only HEAD `7a98031e`):**
Implemented dual-write in `bb_call_fn.cpp` ZD arm: `$trail_mark` writes to both `ZRES(0/8)` (RSP-relative cells arm) AND `FRQ(bcfn_result_slot(pBB))/FRQ(+8)` (rbp-relative, for β-continuation readers). Combined with Parts 1+2 (g_zd_pl_no_declined_runs LP-2 criterion + nodes[]/zd_on[] carve loop). Result: no more SEGV (trail mark plumbing correct — `[rbp+80]` now properly written before n13 reads it). But two-clause programs produce wrong answers: `foo(5)` prints "zero" instead of "pos" — the `$ix_g` inline guard in the armed run returns PASS when it should return FAIL for X=5 vs kival=0. The ZOPQ arithmetic looks correct on paper (op_zread[0]=48, [rsp+96] after sub rsp,48 arg scratch = n1 VAR_REF cell at correct address). **ROOT NOT YET FOUND.** Next session: gdb at `n4_call_builtin_prolog_α` with SCRIP_PL_CELLS=1 on twoclause2.pl (`foo(0):-write(zero),nl. foo(X):-X>0,write(pos),nl. main:-foo(5).`); inspect r8 after the deref loop at label ~.Lx31_111 to see what value `$ix_g` is actually comparing. Candidate: the 208B carve (`sub rsp,208`) somehow puts RSP in a range where `[rsp+96]` aliases something other than n1's ZRES cell — or the VAR_REF deref reaches `[rbp+16]` but that slot holds a different value than expected. The dual-write code is correct and can be re-applied once the dispatch bug is found and fixed. ALL ZK-5B CODE REVERTED — tree at Part-A-only HEAD.

**⭐ s10 PRECISE β-CONTINUATION ANALYSIS — WHY flat_all_zd=1 IS INSUFFICIENT WITHOUT THE TRAIL SLOT:**
`g_zd_pl_no_declined_runs=1` (zero declined runs) is the right criterion for the LP-2 gate BUT it is insufficient to make the cells arm correct. Multi-clause proc_flat graphs (fib, nrev, ALL bench-22 programs) have β-continuation nodes that ARE executed at runtime — they are the failure exits of armed nodes. Example in fib: n4 (`$unify`) fails → jmp n13 (`$unwind_nothrow`). Node n13 is NOT in any `zd_plan` run (it has no `bb_src_of` head rooting it in the primary gamma-chain), so `zd_on[13]=0` and it emits FRQ `[rbp+80]` for its trail-mark argument. But n0 (`$trail_mark`) wrote the trail mark to `[rsp+0]` (ZRES, cells arm). RSP at n13 entry is NOT `[rsp+0]` from n0 — multiple `sub rsp,16` carves have intervened. The FRQ read of `[rbp+80]` returns garbage → SIGSEGV in `pl_trail_unwind(mark=-187890968)`. This applies to ALL multi-clause predicates; there are NO single-clause predicates in bench-22 that would benefit differently.

**⭐ s10 MEASURED (HEAD `7a98031e`, -O0, TIMEOUT=6s):** bench-22 cells=0 PASS=6 FAIL=16 == cells=1 PASS=6 FAIL=16 (byte-identical). PL coupling gate PASS. Prolog rung m3: PASS=122 FAIL=42 (no regression from Part A). SN4 smoke: not re-run (Part A is Prolog-only).

**⭐ s10 DESIGN DECISION REQUIRED (Lon rules) — THE TRAIL-MARK STORAGE QUESTION:**

The β-continuation analysis proves the core issue: `$trail_mark` result cell must be readable from ANY β-exit path at ANY RSP depth. RSP-relative ZRES is wrong for cross-run reads. Two viable paths:

**Option A — FIXED rbp-relative slot for trail mark (trail_mark_slot in ZLS):** `$trail_mark` on cells arm writes to a FIXED `[rbp + trail_mark_slot]` slot pre-allocated in the ZLS frame (structure already exists in `prolog_runtime.h:17` as `EnvLayout.trail_mark_slot`). `$unwind_nothrow`/`$trail_unwind` readers use the same `[rbp + trail_mark_slot]` address — accessible at any RSP depth, no ZOPQ needed. Other cells (`$unify` args, `$ix_g` args, VAR_REF lvalue, etc.) stay on the FORTH RSP spine via ZOPQ as already wired. This is a **hybrid model**: trail-control cells are rbp-fixed, value cells are RSP-relative. Three edits: (1) `sink_trail_mark_str` ZD arm writes to `[rbp+slot]`; (2) `bb_call_fn.cpp` ZD arm for `$unwind_nothrow`/`$trail_unwind` reads from `[rbp+slot]`; (3) LP-2 gate uses `g_zd_pl_no_declined_runs` (s9 Part 1, already designed). The `trail_mark_slot` offset = `flat_frame_bytes - jcon_value_region + trail_mark_zls_offset` (TBD; needs a census of which ZLS slot the lowerer assigns to $trail_mark result DESCRs). **This matches the WAM pattern: trail mark saved to a fixed CP frame slot, not the stack.** Advantage: minimal change to existing cells arm wiring. Cost: hybrid model, not pure FORTH spine.

**Option B — Pure FORTH: arm the β-continuation nodes too:** Extend `zd_plan`'s gamma-chain walk to also follow β arcs (failure exits) of armed nodes, forming β-chains as second-class runs. β-chain nodes (`$unwind_nothrow`, `$trail_unwind`) arm with ZOPQ reads where `op_zread[0]` = depth difference at the specific β-exit RSP level. This requires computing RSP depth at each β-exit precisely — possible since β-exits always fire from a deterministic gamma-chain depth (e.g. n4's β fires when n4's own `sub rsp,16` is still live → depth = zout[n4]). Advantage: pure FORTH model throughout. Cost: substantially more planner complexity; MECH cross-request applies (this IS a frame/protocol decision).

**⭐ BOTH OPTIONS REQUIRE THE SAME LP-2 fix (Part 1, g_zd_pl_no_declined_runs) and Part 2 (carve nodes[]/zd_on[]). Part A already landed.**

**⭐ s9 ROOT CAUSE FOUND — `flat_all_zd` GATE IS WRONG FOR PROLOG:** The s8 diagnosis ("WALL on IR_CALL_PROC_STAGED") was partially correct in direction but the mechanism is different. `SCRIP_ZD_DIAG=1` on nrev/fib/deriv/queens confirms **zero declined runs** in all Prolog graphs. Every run that zd_plan walks passes (`ok=1`). The `flat_all_zd=0` blocker is NOT from `ok=0` exits — it is from the **LP-2 gate itself**: `for _i in [0,n): if !zon[i] → all_zd=0`. Alternative clause bodies (e.g. `fib(0,1)`, `fib(1,1)`, `fib(N,F)` generate `n=60 armed=13`) are only reachable via omega/indexing back-edges and are **never visited** by the gamma-chain walker. So `n > armed` is structural and permanent for multi-clause predicates — the old criterion is wrong for Prolog.

**⭐ s9 TWO-PART FIX — DESIGNED, CODED, REVERTED DUE TO ARMED-TEMPLATE SEGV:**

**Part 1 — `flat_all_zd` criterion (emit.cpp LP-2, line 2396):** Replace the `zon[]` all-nodes loop with `g_zd_pl_no_declined_runs` — a new static (add at line 803 beside `g_zd_stage` et al.): `static int g_zd_pl_no_declined_runs = 0;`. In `zd_plan` entry (line 2001): set `g_zd_pl_no_declined_runs = (g_emit_cfg && g_emit_cfg->pl_cells_graph) ? 1 : 0;`. At the decline exit (line 2145, before the DIAG fprintf): add `g_zd_pl_no_declined_runs = 0;`. In LP-2 (line 2396): replace the inner loop with `if (_pl_cells) { _azd = g_zd_pl_no_declined_runs; } else { _azd = 1; for (...) if (!zd_on[_i]) { _azd = 0; break; } }`. VERIFIED: nrev/fib/deriv/queens all get `all_zd=1` correctly.

**Part 2 — Carve fix (emit.cpp line 2437):** Change carve loop from `zd_wl_kind(_nd)` to `zd_on[_i]`: `for (int _i = 0; _i < n; _i++) { if (zd_on[_i]) _plk += zd_k(nodes[_i]); }`. Unarmed clause bodies have no ZOPQ offsets; including their K inflates `sub rsp` and corrupts every ZOPQ read. VERIFIED: K_total matches the armed-only sum.

**⭐ s9 REMAINING BLOCKER — SEGV IN ARMED TEMPLATES:** With both fixes applied, `all_zd=1` fires correctly and the carve emits (`sub rsp, 176` for fib, etc.), but the compiled code SEGVs in the RX slab. Two failure modes found:

**(A) `bb_move_label.cpp` null `lbl_t1_p` dereference (FIXED, safe to land):** The ZD arm (`op_zres=1`, line 17) calls `x86_lea_tgt("rdi", X86T_TGT1)` which dereferences `_.lbl_t1_p`. For `IR_MOVE_LABEL` nodes with `wantb=0` (cut-barrier nodes — NOT choice-point nodes), `lbl_t1_p` is NULL. The emit.cpp pre-pass (line 2642) only stages `lbl_t1` when `wantb=1`. Fix: change line 17 of `bb_move_label.cpp` from `if (_.op_zres) {` to `if (_.op_zres && _.lbl_t1_p) {`. This is safe and correct — the non-ZD arm handles `wantb=0` MOVE_LABEL nodes.

**(B) Deeper SEGV in native code after (A) fixed — NOT YET IDENTIFIED:** After the `lbl_t1_p` guard, fib.pl (13 armed / 60 total) and deriv.pl (30 armed / 278 total) still SEGV in mode 3 RX slab execution. Mode 3 crash happens in the generated native code. Armed template kinds in fib: `IR_CALL_BUILTIN_PROLOG`, `IR_VAR_REF`, `IR_LIT_INTEGER`, `IR_LIT_STRING`, `IR_CALL_PROC_STAGED`, `IR_MOVE_LABEL`, `IR_CUT`, `IR_SUSPEND`. CANDIDATE ROOT CAUSE: `g_zd_read[]` is a static array (line 803); it is initialized at the staging choke (line 839) only when `g_zd_stage=1`. If `g_zd_read[k]` retains stale values from a prior graph's armed nodes, ZOPQ reads in the current graph address wrong memory. Use `SCRIP_PL_CELLS=1` + gdb on fib.pl mode 3 to get crash address, then bisect to the offending template.

**⭐ s9 SESSION WATERMARKS (HEAD `fc24965c`, -O0, TIMEOUT=6s):** bench-22 cells=0 PASS=6 FAIL=16 == cells=1 PASS=6 FAIL=16 (byte-identical, tree clean — all WIP reverted). PL coupling gate: not re-run this session (no code change). SN4 smoke: not re-run (no code change).

**⭐ s8 PL-ZK-5 GUARD LIFT LANDED (`fc24965c`):** LP-2 `flat_all_zd` predicate: lifted `!flat_gen` and `!flat_jmp_entry` exclusions for `pl_cells_graph` (killswitch `SCRIP_ZD_PL_GEN=0`). Rationale: (1) Prolog retry re-enters at proc α (full prologue, fresh per-invocation cells carve via ZK-3) — cells are NOT retained across retries, so `flat_gen=1` is not a correctness barrier. (2) Prolog retry uses `g_pl_retry` pop (ZK-4 `bb_indirect_goto` ZD arm) → α, never `lbl_res` (Icon coexpr re-entry). ONE AUTHORITY: the only site gating `flat_all_zd` on `flat_gen`/`flat_jmp_entry`. SN4/Icon watermarks: mutually exclusive (R-ZK-A), byte-identical. MEASURED post-rebase at HEAD `fc24965c`: bench-22 cells=0 PASS=6 FAIL=16 == cells=1 PASS=6 FAIL=16 (byte-identical verdict list). PL coupling gate PASS.

**⭐ s8 SESSION WATERMARKS (HEAD `fc24965c`, -O0, TIMEOUT=6s):** bench-22 cells=0 PASS=6 FAIL=16 == cells=1 PASS=6 FAIL=16. Prolog rung suite m3: PASS=122 FAIL=42. Icon m3: PASS=212 FAIL=51 XFAIL=30.

**⭐ s7 ZK-4 LANDING (two commits, Claude Sonnet 4.6, bench 3/22 m3+m4 == cells=0 baseline — zero regression):**
Commit `fc2f21ec`: (1) `zd_wl_kind` — `IR_DISJUNCTION` (0-operand Prolog form) admitted on `pl_cells_graph`, K=0, killswitch `SCRIP_ZD_PL_DJ=0`. (2) `zd_k` — `IR_DISJUNCTION` (n_operands==0) joins K=0 list; ONE LINE ONLY per s22k law. (3) `emit.cpp` pre-pass: when `wantb=1` on `pl_cells_graph`, stage `lbls[k]` (callee α) into `lbl_t1`/`lbl_t1_p` — `X86T_TGT1` already existed. (4) `bb_move_label.cpp` ZD arm (`op_zres=1`): calls `rt_pl_retry_push(lbl_t1)` — pushes callee **α** to retry stack instead of writing β to `FRQ(op_off+16)`; frame-independent by construction. (5) `bb_indirect_goto.cpp` ZD arm (`op_zres=1`): `rt_pl_retry_pop() → jmp rax`; ω on null return (clauses exhausted).
Commit `99ae4ea2`: (6) `zd_nops` early-return — `IR_CALL_PROC_STAGED` on `pl_cells_graph` returns nops=0; Prolog args are staged via `g_call_args[]`, not ZOPQ spine reads; the `opnd` check was failing for all arg subgraphs not on the linear γ-chain. (7) ICN CPS arm fallthrough fix — guard `!pl_cells_graph` on the ZK-2 ICN arm at line 1918 so Prolog falls through to the PL-ZK-1 arm at 1974 (two if-return arms for the same op; ICN arm was silently winning and returning 0 for Prolog).

**⭐ s7 MEASURED (SCRIP HEAD `99ae4ea2`, -O0, bench-22 board, TIMEOUT=6s):** `SCRIP_PL_CELLS=1` bench **3/22 m3+m4** == cells=0 baseline. GAP report: every node in nrev.pl shows `admit=1` — zero blockers remaining. Census: proc_flat graphs show `armed=12/8/11 K_total=192/128/160 declined_nodes=0` — `flat_all_zd=1` now achievable. SN4 smoke PASS=7/7. PL coupling gate PASS. The 19 still-failing programs are pre-existing timeouts/backtrack programs that need the retry stack exercised at depth (not admission fixes). ZK-4 admission + wiring is architecturally complete.

**⭐ s7 NEXT STEP (monitor-first):** The 19 failing backtrack programs need investigation. Recommended starting point: `color.pl` or `qsort.pl` via `SCRIP_PL_CELLS=1 SCRIP_ZD_CENSUS=1` to check which graphs are fully armed (`flat_all_zd=1`), then the 2-way sync monitor to find the first divergence when a backtrack fires through `bb_indirect_goto`'s ZD arm. The retry stack push (α label) and pop (`jmp rax`) path has never been executed at depth under live backtracking — the monitor will name the first divergent event.

**⭐ s6 LIVE ADDENDUM (measured at SCRIP HEAD `984bdb0f`, -O0, bench-22 board, TIMEOUT=6s/prog):** `SCRIP_PL_CELLS=1` scores **3/22 m3 = 3/22 m4** (deriv+fib+tak PASS) == cells=0 baseline. The s5 UCLAIM blocker is RESOLVED by Option A: two suppression sites at the staging choke — (1) `g_zd_arm` forced 0 when `pl_cells_graph && !flat_all_zd` so armed-node templates see `op_zres=0` and fall to FRQ path; (2) `g_zd_gpop/g_zd_wpop` zeroed under the same predicate to prevent unpaired `add rsp` releases. The carve gate at the prologue (`&& g_emit.flat_all_zd`) was the first fix; the release suppress at the choke was the second (without it, `add rsp,176` fired without a matching `sub rsp,176`, corrupting the stack). MEASURED: fib/tak SEGVing → fib/tak PASS both media. SN4 smoke PASS=7/7. PL coupling gate PASS. Baseline cells=0 3/22 intact. BLOCKER FOR ZK-4: every bench program has both `IR_CALL_PROC_STAGED` and `IR_DISJUNCTION` as the sole blockers to `flat_all_zd=1`; admitting both unlocks the carve for every graph in the current corpus.

**⭐ s6 ZK-4 ANALYSIS:** `IR_CALL_PROC_STAGED` IS already admitted in `zd_wl_kind` (PL-ZK-1 arm 3/5, `SCRIP_ZD_PL_CPS`). `bb_call_proc_staged.cpp` already has a ZD arm (`if (_.op_zres)`, ZD-7c, Icon-gated). The s6 ZD_GAP report (fib) shows `IR_CALL_PROC_STAGED(fib/2)` at position r=3 in its run with `admit=0` — `zd_wl_kind` returns 1 but `zd_plan` declines structurally. Root cause: `IR_CALL_PROC_STAGED` in a recursive multi-clause predicate is a generator (its ω is a backtrack edge to `IR_DISJUNCTION`), so its ω exits the run. `IR_DISJUNCTION` has NO arm in `zd_wl_kind` at all — it's the multi-clause retry dispatch node (`g->body_root = dj`; each `IR_MOVE_LABEL.operands[1] = dj`). Both must be admitted together: `IR_DISJUNCTION` (K=16 for its state cell `FR(op_off+16)`) + `IR_CALL_PROC_STAGED` omega routing through `rt_pl_retry_push/pop` (runtime half landed PL-FR-4 `a0196747`; consumer ZD arm NOT YET in `bb_call_proc_staged.cpp` — the existing ZD-7c arm is Icon-gated via `icn_cells_graph`, not Prolog). ONE AUTHORITY: ZK-4 must consume `rt_pl_retry_push/pop`, never mint a second retry store. The `bb_disjunction.cpp` template uses `FR(op_off+16)` for its state counter — the ZD arm will need this cell at `ZRES(0/8)` (16B result cell for the state descr) with `beta` re-entering via `rt_pl_retry_pop`.

**⭐ 2026-08-08 LIVE ADDENDUM (measured at SCRIP HEAD `69c476a0`, `-O0`, bench-22 board, TIMEOUT=6s/prog):** `SCRIP_PL_CELLS=1` scores **1/22 m3 = 1/22 m4** (deriv only; fib FAIL — the ZFRAME-s7 fib flag reproduces under the env — and tak prints `_G0`, a wrong-output shape worth a reproducer) vs **3/22** on the cells=0/zframe arm. The −2 is ZK-3-PARTIAL's known state (UCLAIM blocker below; "SEGV moves into body = correct progression"), not a new emergency. ⛔ **CORRECTION OF RECORD: `SCRIP_PL_CELLS` was ALWAYS opt-IN** (`lower_prolog.c:13` is the sole setter, ONE commit in its history `cba90403`) — the ZFRAME-s7 claim "cells=1 (now the default)" is FALSIFIED; no default flip ever landed, so the "cells=1 default bug" Lon-flag dissolves into ZK-3's existing blocker. The 11/22→3/22 board erosion belongs to other tracks' windows (GOAL-PL-ZFRAME-RESTORE.md s7b bisect). ⭐ **COORDINATION (ZK-4):** the disjunction retry β-re-entry is the SAME rendezvous FR-4 is fixing — **ZK-4 must CONSUME the heap-CP retry mechanism (`rt_pl_retry_push/pop`, runtime half landed ZFRAME s6), never mint a second retry store** — one authority.

**PL-ZK-3 PROC-ENTRY CARVE LANDED (`08a14f2f`) — `sub rsp,K_total` after zframe prologue:**
After `xa_flat_zframe_prologue()` pins `rbp=rsp` at `flat_frame_bytes`, Prolog cells arm (`pl_cells_graph=1`) now emits `sub rsp,K_total` where `K_total = Σ zd_k(admitted nodes)` — ONE K AUTHORITY (zd_k). The epilogue `lea rsp,[rbp+flat_frame_bytes]` unwinds from ANY in-body depth (depth-immune via pinned rbp). MEASURED: queens.pl armed=21/12/17/15/16 unchanged; SEGV moves from proc-entry into body (correct progression). SN4 byte-identical. Baseline at HEAD: Prolog PASS=115 FAIL=0 ABORT=0 (parallel session `617809fd`/`4de6b763` moved baseline 122→115 before rebase; PL-ZK-3 zero-regression confirmed by bisect).

**PL-ZK-3 OPEN BLOCKER — UCLAIM + CELLS CARVE INTERACTION (Lon decision required):**
Bisect (s5): `SCRIP_ZD_PL_CBP=0` makes rung06/07/08 pass; re-enabling it regresses. Root cause: proc_flat (jmp=1 gen=1) generator bodies carry BOTH the PL-ZK-3 `sub rsp,K_total` carve (e.g. K=192) AND declined statements emitting UCLAIM whole-statement claims (`sub rsp,224`). UCLAIM assumes RSP_stmt_entry = RBP − flat_frame_bytes; PL-ZK-3 moves it to RBP − flat_frame_bytes − K_total. `zvo_resolve` then computes wrong absolute RSP positions for FRQ reads. TWO OPTIONS — **(A) gate PL-ZK-3 on `g_emit.flat_all_zd=1`** (every statement fully armed → no UCLAIM fires; requires ZK-2 template arms complete first; available at prologue time via LP-2); **(B) make UCLAIM offset arithmetic RBP-relative** (depth-immune, more invasive). Option A is ladder-correct sequence. **Lon decides.**

**s5 KEY MEASUREMENTS (do not re-derive):**
- queens.pl census: 5 armed proc_flat (K=336/192/272/208/224), 3 declined (IR_DISJUNCTION), pat_flat declined (IR_DISJUNCTION). Body SEGV post-ZK-3 = correct next step.
- 8 programs FIXED by cells arm (rung02, rung10 puzzles, rung81). rung06/07/08 regress due to UCLAIM interaction.
- Depth walls: `pat_flat` (jmp=0) dispatcher graphs have multi-clause join walls; `proc_flat` (jmp=1 gen=1) clause bodies are wall-free. `flat_all_zd=1` is the correct gate (computed pre-emission by LP-2 layout pass, available at prologue time).
- ZD one-authority line numbers at `08a14f2f`: `zd_wl_kind` ~1903 · `zd_nops` ~1984 · `zd_k` ~1986 · `zd_plan` ~1987 · `pl_cells_stamp` lower_prolog.c:13.

**s4 SESSION NOTES (preserved):** s1/s2 commits permanently lost. SCRIP pushed to origin requires Lon credential. .github push also requires credential.

**ZK-2c LANDED (prior s3, `85f09f79`) — ZD-ARM INLINE SINKS \$UNIFY/\$UNIFY_LST/\$IX_G:** Parameterized `sink_unify2_str`, `sink_unify_lst_str`, `sink_ix_g_str` with `zd_rsp` default parameter (`>= 0` = ZD RSP-relative; `-1` = legacy FRQ, byte-identical). ZD arm in `bb_call_fn_str`: intercepts `$trail_mark` (nargs=0) before dop_direct_fp exclusion with `zd_rsp=0`; intercepts `$unify`/`$unify_lst`/`$ix_g` before PL-REGAIN-2 and routes to inline sinks with `zd_rsp=0` (args already at `[rsp+0..]` via copy loop). **nrev sink census: rt_call_arr 2=2, rt_pl_dop_ 65=65. ZK-2 acceptance test PASS.**

**ZK-2a LANDED (prior s3, `d2774fd3`) — IR_SUSPEND ZD ARM:** `bb_suspend.cpp` ZD arm: `op_zres=1` reads yield value from `ZOPQ(0,0/8)`, writes to `ZRES(0/8)`; legacy `FRQ(op_sa)` arm byte-identical. `zd_nops` ONE AUTHORITY: IR_SUSPEND nops=1 (reads one predecessor: the yield-value producer).

**ZK-1 LANDED (prior s3, `94fbd33a`) — ADMISSION ROUTING:** 7 additive `zd_wl_kind` arms (IR_CALL_BUILTIN_PROLOG, IR_VAR_REF, IR_CALL_PROC_STAGED, IR_SUSPEND, IR_VAR, IR_CUT, IR_MOVE_LABEL), each killswitched. IR_CALL_BUILTIN_PROLOG + IR_CALL_PROC_STAGED + IR_SUSPEND added to `zd_nops` n_operands arm. IR_CUT + IR_MOVE_LABEL to `zd_k` K=0 class. Census after ZK-1b fix: queens.pl armed=21/12/17/15/16 per graph. Remaining blockers: IR_DISJUNCTION (ZK-4 scope).

**ZK-0 LANDED (prior s3, `cba90403`) — FLAG + CENSUS:** `pl_cells_graph` at IR_graph_t struct END. `pl_cells_stamp()` one-authority at 5 IR_alloc sites. PL-ZK-CENSUS additive block. Both falsifiability injections proven. SN4 byte-identical throughout.

**SESSION NOTES (s4):** s1/s2 commits permanently lost. ZD one-authority line numbers at `d7f6632b`: `zd_wl_kind` ~1903 · `zd_nops` ~1984 · `zd_k` ~1986 · `zd_plan` ~1987 · `pl_cells_stamp` at `lower_prolog.c:13`. SCRIP pushed to origin requires Lon credential. .github push also requires credential.

## ⭐⭐ WHAT THE PRIOR SCANS ALREADY FOUND (s162–s164 + FINDING-2026-08-01-PL §11 — the machinery this ladder EXTENDS, never re-derives)
1. **The unlock set is FIVE kinds and it is ALL-OR-NOTHING PER RUN** (s163, measured): `IR_CALL_BUILTIN_PROLOG` gates **185/185 runs** · `IR_MOVE_LABEL` 157 · `IR_VAR_REF` 130 · `IR_VAR` 93 · `IR_CALL_PROC_STAGED` 91. Single-kind admission unlocks ZERO runs; 2-kind best (CBP+MOVE_LABEL) = 29/185; all five = 185/185. **No sequence that omits CBP unlocks anything.**
2. ⛔ **THE SINK-BYPASS DEFECT IS THE REAL BLOCKER** (s163, structural + sized): the ZD arm in `bb_call_fn_str` early-returns BEFORE `dop_direct_fp` and the PL-SINK ladder — an armed call emits generic `rt_call_arr` by-name dispatch, discarding SINK-1/2/4/8 (~97% of the data plane: 65 `rt_pl_dop_*` vs 2 `rt_call_arr` sites in nrev.s). Silently-green perf regression class. **DESIGN CORRECTION OF RECORD: ZD IS A STORAGE DISCIPLINE, NOT A DISPATCH ROUTE** — sink/dop selection stays; only operand SOURCE (ZOPQ vs FRQ) and result DEST (ZRES vs FRQ) move. ZD-PL-A (dop_direct_fp inside the ZD arm) LANDED s163b; slice 2a ($trail_mark, 30% of main traffic, needs the `esi==0` runtime read first) and 2b ($unify, addressing parameterization) are SPECIFIED NOT LANDED.
3. ⭐ **PROC-ENTRY IS THE ONE ITEM THAT IS NOT "ARM ANOTHER KIND"** (FINDING-08-01 §11): 781 frame refs (17.3%) with NO BB to own them — the entry protocol itself (wire header, saved rcx/rdx/rbp). Per-BB self-allocation needs an explicit design answer here: a FUNCTION-class frame per SN4 MECH's frame census, or wire cells at fixed spine offsets. **CROSS-REQUEST TO `GOAL-SN4-ZETA-MECH.md` — this is a MECH-shaped decision; never land a new frame/glue protocol from this file.**
4. ⚠ **The `IR_VAR` "RETIRED AS VACUOUS — DO NOT WIDEN" ruling was made for SN4 GLOBALS** and needs re-examination for Prolog (Prolog vars ARE graph locals) — a reasoned re-open with Lon visibility, never a blind widen (s163 NEXT(d)).
5. **Storage-weight vs run-gating are DIFFERENT ORDERINGS and a ladder needs both** (FINDING-08-01 §11): `move_label` gates 157 runs on 5 refs; `lit_string`/`lit_integer` own 438 refs and gate nothing.
6. **Instruments that exist:** `SCRIP_ZD_CENSUS` per-graph census (ZK-0 pattern, falsify both directions before believing any zero) · `SCRIP_ZD_GAP` callee= reporting · LD_PRELOAD PLT interposition for dynamic hotness (s148, `perf`/`gdb` absent in container) · the deterministic-instrument gap (s164: wall time DISQUALIFIED on Prolog until one exists — every perf claim on this ladder inherits that gate).

## RUNGS (each: own commit, killswitch, `=0` byte-identity, SN4+ICN+PL-zframe-arm invariance, FINDING per land mine)
- [x] **PL-ZK-0 FLAG + CENSUS.** DONE s1 (`a5597f65`). `pl_cells_graph` at struct END; `pl_cells_stamp()` one-authority in `lower_prolog.c` (5 sites); census extended. Naming ruling: SEPARATE FIELD (`pl_cells_graph`), not a rename — concurrent track arm, banned by concurrency banner.
- [x] **PL-ZK-1 ADMISSION ROUTING.** DONE s1 (`a2386c3f`) + ZK-1b (`c70240af`). Five additive `zd_wl_kind` arms (CBP · VAR_REF · CALL_PROC_STAGED · CUT · MOVE_LABEL) + `zd_k` K=0 (CUT+MOVE_LABEL) + `zd_nops` n_operands (CBP+STAGED) + `bb_var_ref.cpp` ZD arm + `bb_cut.cpp` transparency. ZK-2a (IR_VAR local) piggy-backed. ZK-1b: R-PL-ZK-A mutual exclusion — pl_cells_graph + zframe_graph simultaneous-set defect (PL-FR-2 concurrent session left the guard unpromised). 5071/5989 graphs reachable after.
- [x] **PL-ZK-2 LEAF SPINE + SINK PRESERVATION.** FULLY CLOSED s2 (`a7c80b4b`+`9f88d5e4`+`0f6dc6e8`). SUSPEND ZD arm (ZOPQ→ZRES, nops=1). \$trail_mark sink under ZD (argbase=0 safe). \$unify/\$unify_lst/\$ix_g inline sinks parameterized for RSP-relative [rsp+N]. nrev sink census cells=1 ≥ cells=0 on all dop kinds. rt_call_arr 2=2.
- [x] **PL-ZK-3 PROC-ENTRY DESIGN** (item 3): COMPLETE (`d32f4dd2`). `sub rsp,K_total` carve at prologue gated on `flat_all_zd` (Option A, Lon ruling). Two choke suppression sites: `g_zd_arm=0` and `g_zd_gpop/g_zd_wpop=0` when `pl_cells_graph && !flat_all_zd`, making mixed-armed/declined graphs fall entirely to UCLAIM byte-identically. bench 3/22 cells=0 == cells=1. SN4 smoke PASS=7/7.
- [x] **PL-ZK-4 CHOICE POINTS + TRAIL.** DONE s7 (`fc2f21ec` + `99ae4ea2`). `IR_DISJUNCTION` admitted (K=0, 0-operand Prolog form); `zd_wl_kind` arm + K=0 in `zd_k`. `bb_move_label` ZD arm pushes callee α to `g_pl_retry` retry stack (frame-independent, WAM ALTB write equivalent). `bb_indirect_goto` ZD arm pops retry stack → `jmp rax` (WAM `return ALTB(B)` equivalent); ω on exhausted stack. `zd_nops` returns 0 for `IR_CALL_PROC_STAGED` on `pl_cells_graph` (args staged via `g_call_args[]`, not ZOPQ). ICN CPS arm fallthrough fix: `!pl_cells_graph` guard prevents ICN arm from eating Prolog. GAP report: all-admit=1. Census: armed graphs with K_total>0, declined_nodes=0. bench 3/22 == baseline (backtrack programs need depth exercise, not admission work).
- [ ] **PL-ZK-5 SUSPEND CLASS.** Predicate/generator graphs (the 70.3%): pending-cells vs pthread decided by measurement, the ICN-ZK suspension-decision shape.
- [ ] **PL-ZK-6 RATCHET.** bench 22/22 m3+m4 on the cells arm · rung 164/164 both modes · smoke 5/5 · sink census ≥ legacy arm.
- [ ] **PL-ZK-7 GATES + DOCS.** Cells-arm gate script; A/B table vs the zframe arm (the two-backends precedent) for Lon's default ruling; cursor sync.

## COORDINATION
This file NEVER lands a new frame/glue/claim protocol — MECH owns structure (cross-request in both cursors). Shared chokes (`zd_wl_kind`/`zd_k`/`zd_nops` one-authority lines, staging choke, `bb_call_fn.cpp` dfp chain) take ADDITIVE arms only. `git pull --rebase` before every commit; re-derive every count at session start; line numbers drift daily — re-grep, never trust. Perf claims blocked on the deterministic instrument (item 6). `.github` pushes last.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet

## DRAFT RULING R-PL-ZK-A (walker default, s0b; Lon overrides at any session start)
ZK-0's naming decision defaults to: ADD a separate `pl_cells_graph` field at IR_graph_t struct END (s141 ABI law) — cheapest, zero Icon coupling, no rename churn. A shared behavior-named `cells_graph` consolidation becomes a later hygiene rung once both language arms are proven. This unblocks ZK-0 without waiting on the ruling.
