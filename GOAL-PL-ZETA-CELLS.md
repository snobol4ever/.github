# GOAL-PL-ZETA-CELLS.md — Prolog at 100% per-BB ζ CELLS on the RSP-topped FORTH stack

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (8)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

**CHARTER (Lon directive, 2026-08-07):** Walk Prolog to 100% per-BB allocation on the RSP-topped FORTH-style stack — the SAME ZD machinery SNOBOL4 built and Icon is completing (`GOAL-ICN-ZETA-CELLS.md` is the template; read it first every session). This is the ZD ladder's Prolog completion, not a parallel mechanism. Concurrent twin: `GOAL-PL-ZFRAME-RESTORE.md` (FRAMES on STACK) — BOTH KEPT, switch-selected, one graph NEVER in both arms, until Lon picks a default. Routing mirrors the Icon R-ZK-A ruling: the cells opt-IN suppresses the zframe stamp at the SAME LOWER site, A/B = one env var.

## ⛔⭐ LIVE CURSOR — s6 (2026-08-08, Claude Sonnet 4.6)
HEAD `d32f4dd2`. **ZK-0/ZK-1/ZK-2/ZK-3 ALL COMPLETE ON-CHAIN AT ORIGIN HEAD.** **NEXT RUNG = PL-ZK-4 — IR_DISJUNCTION + IR_CALL_PROC_STAGED admission; must consume `rt_pl_retry_push/pop` (landed PL-FR-4 `a0196747`), not mint a second retry store (see ZK-4 COORDINATION note).**

**⭐ s6 LIVE ADDENDUM (measured at SCRIP HEAD `d32f4dd2`, -O0, bench-22 board, TIMEOUT=6s/prog):** `SCRIP_PL_CELLS=1` scores **3/22 m3 = 3/22 m4** (deriv+fib+tak PASS) == cells=0 baseline. The s5 UCLAIM blocker is RESOLVED by Option A: two suppression sites at the staging choke — (1) `g_zd_arm` forced 0 when `pl_cells_graph && !flat_all_zd` so armed-node templates see `op_zres=0` and fall to FRQ path; (2) `g_zd_gpop/g_zd_wpop` zeroed under the same predicate to prevent unpaired `add rsp` releases. The carve gate at the prologue (`&& g_emit.flat_all_zd`) was the first fix; the release suppress at the choke was the second (without it, `add rsp,176` fired without a matching `sub rsp,176`, corrupting the stack). MEASURED: fib/tak SEGVing → fib/tak PASS both media. SN4 smoke PASS=7/7. PL coupling gate PASS. Baseline cells=0 3/22 intact. BLOCKER FOR ZK-4: every bench program has both `IR_CALL_PROC_STAGED` and `IR_DISJUNCTION` as the sole blockers to `flat_all_zd=1`; admitting both unlocks the carve for every graph in the current corpus.

**⭐ s6 ZK-4 ANALYSIS:** `IR_CALL_PROC_STAGED` IS already admitted in `zd_wl_kind` (PL-ZK-1 arm 3/5, `SCRIP_ZD_PL_CPS`). `bb_call_proc_staged.cpp` already has a ZD arm (`if (_.op_zres)`, ZD-7c, Icon-gated). The s6 ZD_GAP report (fib) shows `IR_CALL_PROC_STAGED(fib/2)` at position r=3 in its run with `admit=0` — `zd_wl_kind` returns 1 but `zd_plan` declines structurally. Root cause: `IR_CALL_PROC_STAGED` in a recursive multi-clause predicate is a generator (its ω is a backtrack edge to `IR_DISJUNCTION`), so its ω exits the run. `IR_DISJUNCTION` has NO arm in `zd_wl_kind` at all — it's the multi-clause retry dispatch node (`g->body_root = dj`; each `IR_MOVE_LABEL.operands[1] = dj`). Both must be admitted together: `IR_DISJUNCTION` (K=16 for its state cell `FR(op_off+16)`) + `IR_CALL_PROC_STAGED` omega routing through `rt_pl_retry_push/pop` (runtime half landed PL-FR-4 `a0196747`; consumer ZD arm NOT YET in `bb_call_proc_staged.cpp` — the existing ZD-7c arm is Icon-gated via `icn_cells_graph`, not Prolog). ONE AUTHORITY: ZK-4 must consume `rt_pl_retry_push/pop`, never mint a second retry store. The `bb_disjunction.cpp` template uses `FR(op_off+16)` for its state counter — the ZD arm will need this cell at `ZRES(0/8)` (16B result cell for the state descr) with `beta` re-entering via `rt_pl_retry_pop`.

**⭐ 2026-08-08 LIVE ADDENDUM (measured at SCRIP HEAD `69c476a0`, `-O0`, bench-22 board, TIMEOUT=6s/prog):** `SCRIP_PL_CELLS=1` scores **1/22 m3 = 1/22 m4** (deriv only; fib FAIL — the ZFRAME-s7 fib flag reproduces under the env — and tak prints `_G0`, a wrong-output shape worth a reproducer) vs **3/22** on the cells=0/zframe arm. The −2 is ZK-3-PARTIAL's known state (UCLAIM blocker below; "SEGV moves into body = correct progression"), not a new emergency. ⛔ **CORRECTION OF RECORD: `SCRIP_PL_CELLS` was ALWAYS opt-IN** (`lower_prolog.c:13` is the sole setter, ONE commit in its history `cba90403`) — the ZFRAME-s7 claim "cells=1 (now the default)" is FALSIFIED; no default flip ever landed, so the "cells=1 default bug" Lon-flag dissolves into ZK-3's existing blocker. The 11/22→3/22 board erosion belongs to other tracks' windows (GOAL-PL-ZFRAME-RESTORE.md s7b bisect). ⭐ **COORDINATION (ZK-4):** the disjunction retry β-re-entry is the SAME rendezvous FR-4 is fixing — **ZK-4 must CONSUME the heap-CP retry mechanism (`rt_pl_retry_push/pop`, runtime half landed ZFRAME s6), never mint a second retry store** — one authority.

**PL-ZK-3 PROC-ENTRY CARVE LANDED (`08a14f2f`) — `sub rsp,K_total` after zframe prologue:**
After `xa_flat_zframe_prologue()` pins `rbp=rsp` at `flat_frame_bytes`, Prolog cells arm (`pl_cells_graph=1`) now emits `sub rsp,K_total` where `K_total = Σ zd_k(admitted nodes)` — ONE K AUTHORITY (zd_k). The epilogue `lea rsp,[rbp+flat_frame_bytes]` unwinds from ANY in-body depth (depth-immune via pinned rbp). MEASURED: queens.pl armed=21/12/17/15/16 unchanged; SEGV moves from proc-entry into body (correct progression). SN4 byte-identical. Baseline at HEAD: Prolog PASS=115 FAIL=0 ABORT=0 (parallel session `617809fd`/`4de6b763` moved baseline 122→115 before rebase; PL-ZK-3 zero-regression confirmed by bisect).

**PL-ZK-3 OPEN BLOCKER — UCLAIM + CELLS CARVE INTERACTION (Lon decision required):**
Bisect (s5): `SCRIP_ZD_PL_CBP=0` makes rung06/07/08 pass; re-enabling it regresses. Root cause: proc_flat (jmp=1 gen=1) generator bodies carry BOTH the PL-ZK-3 `sub rsp,K_total` carve (e.g. K=192) AND declined statements emitting UCLAIM whole-statement claims (`sub rsp,224`). UCLAIM assumes RSP_stmt_entry = RBP − flat_frame_bytes; PL-ZK-3 moves it to RBP − flat_frame_bytes − K_total. `zvo_resolve` then computes wrong absolute RSP positions for FRQ reads. TWO OPTIONS — **(A) gate PL-ZK-3 on `g_emit.flat_all_zd=1`** (every statement fully armed → no UCLAIM fires; requires ZK-2 template arms complete first; available at prologue time via LP-2); **(B) make UCLAIM offset arithmetic RBP-relative** (depth-immune, more invasive). Option A is ladder-correct sequence. **Lon decides.**

**s5 KEY MEASUREMENTS (do not re-derive):**
- queens.pl census: 5 armed proc_flat (K=336/192/272/208/224), 3 declined (IR_DISJUNCTION), pat_flat declined (IR_DISJUNCTION). Body SEGV post-ZK-3 = correct next step.
- 8 programs FIXED by cells arm (rung02, rung10 puzzles, rung81). rung06/07/08 regress due to UCLAIM interaction.
- Depth walls: `pat_flat` (jmp=0) dispatcher graphs have multi-clause join walls; `proc_flat` (jmp=1 gen=1) clause bodies are wall-free. `flat_all_zd=1` is the correct gate (computed pre-emission by LP-2 layout pass, available at prologue time).
- ZD one-authority line numbers at `08a14f2f`: `zd_wl_kind` ~1903 · `zd_nops` ~1984 · `zd_k` ~1986 · `zd_plan` ~1987 · `pl_cells_stamp` lower_prolog.c:13.

**s4 SESSION NOTES (preserved):** s1/s2 commits permanently lost. SCRIP pushed to origin requires Lon credential. .github push also requires credential.

**ZK-2c LANDED (prior s3, `85f09f79`) — ZD-ARM INLINE SINKS \$UNIFY/\$UNIFY_LST/\$IX_G:** Parameterized `sink_unify2_str`, `sink_unify_lst_str`, `sink_ix_g_str` with `zd_rsp` default parameter (`>= 0` = ZD RSP-relative; `-1` = legacy FRQ, byte-identical). ZD arm in `bb_call_fn_str`: intercepts `$trail_mark` (nargs=0) before dop_direct_fp exclusion with `zd_rsp=0`; intercepts `$unify`/`$unify_lst`/`$ix_g` before PL-REGAIN-2 and routes to inline sinks with `zd_rsp=0` (args already at `[rsp+0..]` via copy loop). **nrev sink census: rt_call_arr 2=2, rt_pl_dop_ 65=65. ZK-2 acceptance test PASS.**

**ZK-2a LANDED (prior s3, `d2774fd3`) — IR_SUSPEND ZD ARM:** `bb_suspend.cpp` ZD arm: `op_zres=1` reads yield value from `ZOPQ(0,0/8)`, writes to `ZRES(0/8)`; legacy `FRQ(op_sa)` arm byte-identical. `zd_nops` ONE AUTHORITY: IR_SUSPEND nops=1 (reads one predecessor: the yield-value producer).

**ZK-1 LANDED (prior s3, `94fbd33a`) — ADMISSION ROUTING:** 7 additive `zd_wl_kind` arms (IR_CALL_BUILTIN_PROLOG, IR_VAR_REF, IR_CALL_PROC_STAGED, IR_SUSPEND, IR_VAR, IR_CUT, IR_MOVE_LABEL), each killswitched. IR_CALL_BUILTIN_PROLOG + IR_CALL_PROC_STAGED + IR_SUSPEND added to `zd_nops` n_operands arm. IR_CUT + IR_MOVE_LABEL to `zd_k` K=0 class. Census after ZK-1b fix: queens.pl armed=21/12/17/15/16 per graph. Remaining blockers: IR_DISJUNCTION (ZK-4 scope).

**ZK-0 LANDED (prior s3, `cba90403`) — FLAG + CENSUS:** `pl_cells_graph` at IR_graph_t struct END. `pl_cells_stamp()` one-authority at 5 IR_alloc sites. PL-ZK-CENSUS additive block. Both falsifiability injections proven. SN4 byte-identical throughout.

**SESSION NOTES (s4):** s1/s2 commits permanently lost. ZD one-authority line numbers at `d7f6632b`: `zd_wl_kind` ~1903 · `zd_nops` ~1984 · `zd_k` ~1986 · `zd_plan` ~1987 · `pl_cells_stamp` at `lower_prolog.c:13`. SCRIP pushed to origin requires Lon credential. .github push also requires credential.

## ⭐⭐ WHAT THE PRIOR SCANS ALREADY FOUND (s162–s164 + FINDING-2026-08-01-PL §11 — the machinery this ladder EXTENDS, never re-derives)
1. **The unlock set is FIVE kinds and it is ALL-OR-NOTHING PER RUN** (s163, measured): `IR_CALL_BUILTIN_PROLOG` gates **185/185 runs** · `IR_MOVE_LABEL` 157 · `IR_VAR_REF` 130 · `IR_VAR` 93 · `IR_CALL_PROC_STAGED` 91. Single-kind admission unlocks ZERO runs; 2-kind best (CBP+MOVE_LABEL) = 29/185; all five = 185/185. **No sequence that omits CBP unlocks anything.**
2. ⛔ **THE SINK-BYPASS DEFECT IS THE REAL BLOCKER** (s163, structural + sized): the ZD arm in `bb_call_fn_str` early-returns BEFORE `dop_direct_fp` and the PL-SINK ladder — an armed call emits generic `rt_call_arr` by-name dispatch, discarding SINK-1/2/4/8 (~97% of the data plane: 65 `rt_pl_dop_*` vs 2 `rt_call_arr` sites in nrev.s). Silently-green perf regression class. **DESIGN CORRECTION OF RECORD: ZD IS A STORAGE DISCIPLINE, NOT A DISPATCH ROUTE** — sink/dop selection stays; only operand SOURCE (ZOPQ vs FRQ) and result DEST (ZRES vs FRQ) move. ZD-PL-A (dop_direct_fp inside the ZD arm) LANDED s163b; slice 2a ($trail_mark, 30% of main traffic, needs the `esi==0` runtime read first) and 2b ($unify, addressing parameterization) are SPECIFIED NOT LANDED.
3. ⭐ **PROC-ENTRY IS THE ONE ITEM THAT IS NOT "ARM ANOTHER KIND"** (FINDING-08-01 §11): 781 frame refs (17.3%) with NO BB to own them — the entry protocol itself (wire header, saved rcx/rdx/rbp). Per-BB self-allocation needs an explicit design answer here: a FUNCTION-class frame per SN4 MECH's frame census, or wire cells at fixed spine offsets. **CROSS-REQUEST TO `GOAL-SN4-ZETA-MECH.md` — this is a MECH-shaped decision; never land a new frame/glue protocol from this file.**
4. ⚠ **The `IR_VAR` "RETIRED AS VACUOUS — DO NOT WIDEN" ruling was made for SN4 GLOBALS** and needs re-examination for Prolog (Prolog vars ARE graph locals) — a reasoned re-open with Lon visibility, never a blind widen (s163 NEXT(d)).
5. **Storage-weight vs run-gating are DIFFERENT ORDERINGS and a ladder needs both** (FINDING-08-01 §11): `move_label` gates 157 runs on 5 refs; `lit_string`/`lit_integer` own 438 refs and gate nothing.
6. **Instruments that exist:** `SCRIP_ZD_CENSUS` per-graph census (ZK-0 pattern, falsify both directions before believing any zero) · `SCRIP_ZD_GAP` callee= reporting · LD_PRELOAD PLT interposition for dynamic hotness (s148, `perf`/`gdb` absent in container) · the deterministic-instrument gap (s164: wall time DISQUALIFIED on Prolog until one exists — every perf claim on this ladder inherits that gate).

## RUNGS (each: own commit, killswitch, `=0` byte-identity, SN4+ICN+PL-zframe-arm invariance, FINDING per land mine)
- [x] **PL-ZK-0 FLAG + CENSUS.** DONE s1 (`a5597f65`). `pl_cells_graph` at struct END; `pl_cells_stamp()` one-authority in `lower_prolog.c` (5 sites); census extended. Naming ruling: SEPARATE FIELD (`pl_cells_graph`), not a rename — concurrent track arm, banned by concurrency banner.
- [x] **PL-ZK-1 ADMISSION ROUTING.** DONE s1 (`a2386c3f`) + ZK-1b (`c70240af`). Five additive `zd_wl_kind` arms (CBP · VAR_REF · CALL_PROC_STAGED · CUT · MOVE_LABEL) + `zd_k` K=0 (CUT+MOVE_LABEL) + `zd_nops` n_operands (CBP+STAGED) + `bb_var_ref.cpp` ZD arm + `bb_cut.cpp` transparency. ZK-2a (IR_VAR local) piggy-backed. ZK-1b: R-PL-ZK-A mutual exclusion — pl_cells_graph + zframe_graph simultaneous-set defect (PL-FR-2 concurrent session left the guard unpromised). 5071/5989 graphs reachable after.
- [x] **PL-ZK-2 LEAF SPINE + SINK PRESERVATION.** FULLY CLOSED s2 (`a7c80b4b`+`9f88d5e4`+`0f6dc6e8`). SUSPEND ZD arm (ZOPQ→ZRES, nops=1). \$trail_mark sink under ZD (argbase=0 safe). \$unify/\$unify_lst/\$ix_g inline sinks parameterized for RSP-relative [rsp+N]. nrev sink census cells=1 ≥ cells=0 on all dop kinds. rt_call_arr 2=2.
- [x] **PL-ZK-3 PROC-ENTRY DESIGN** (item 3): COMPLETE (`d32f4dd2`). `sub rsp,K_total` carve at prologue gated on `flat_all_zd` (Option A, Lon ruling). Two choke suppression sites: `g_zd_arm=0` and `g_zd_gpop/g_zd_wpop=0` when `pl_cells_graph && !flat_all_zd`, making mixed-armed/declined graphs fall entirely to UCLAIM byte-identically. bench 3/22 cells=0 == cells=1. SN4 smoke PASS=7/7.
- [ ] **PL-ZK-4 CHOICE POINTS + TRAIL.** `IR_DISJUNCTION` K + retry β re-entry WITHOUT release (the non-popping law); trail interplay with cell release on backtrack — the transit guarantee (bindings pop on retreat).
- [ ] **PL-ZK-5 SUSPEND CLASS.** Predicate/generator graphs (the 70.3%): pending-cells vs pthread decided by measurement, the ICN-ZK suspension-decision shape.
- [ ] **PL-ZK-6 RATCHET.** bench 22/22 m3+m4 on the cells arm · rung 164/164 both modes · smoke 5/5 · sink census ≥ legacy arm.
- [ ] **PL-ZK-7 GATES + DOCS.** Cells-arm gate script; A/B table vs the zframe arm (the two-backends precedent) for Lon's default ruling; cursor sync.

## COORDINATION
This file NEVER lands a new frame/glue/claim protocol — MECH owns structure (cross-request in both cursors). Shared chokes (`zd_wl_kind`/`zd_k`/`zd_nops` one-authority lines, staging choke, `bb_call_fn.cpp` dfp chain) take ADDITIVE arms only. `git pull --rebase` before every commit; re-derive every count at session start; line numbers drift daily — re-grep, never trust. Perf claims blocked on the deterministic instrument (item 6). `.github` pushes last.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet

## DRAFT RULING R-PL-ZK-A (walker default, s0b; Lon overrides at any session start)
ZK-0's naming decision defaults to: ADD a separate `pl_cells_graph` field at IR_graph_t struct END (s141 ABI law) — cheapest, zero Icon coupling, no rename churn. A shared behavior-named `cells_graph` consolidation becomes a later hygiene rung once both language arms are proven. This unblocks ZK-0 without waiting on the ruling.
