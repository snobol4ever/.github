# FINDING — Z4 s8: the capture-start regression was a PARSE PREFIX COLLISION, and the CAS bottom-marker deletes the first variable-depth reach (2026-07-29, Claude)

## LANDING 1 — capture-start fixed (cell-stack 4/5 → 5/5; closes the `d79a427a..cca948c5` bisect item)
**Mechanism.** On an UNPINNED graph `x86_fr32_prefix()` is the plain `"dword ptr [rsp + "` spelling, so `x86_parse`'s flat-frame prefix check captures EVERY rsp operand — raw FORTH-cell spellings included — and the frame encoders then add `op_flat_disp`. Per box the displacement SELF-CANCELS (store and load at the same depth reach the same displaced slot), which is why the window boxes (SPAN et al.) survived while silently squatting on the flat area above the statement mark. A CROSS-DEPTH cell read cannot self-cancel: capture COND (depth 64) read SAVE's delta (stored at depth 48) with a raw offset that already encoded the 16-byte difference, the compensation double-counted it, and COND read `mark+16` — a descriptor tag whose dword is 1 — instead of the delta at `mark+0`. One mechanism explains BOTH recorded defect shapes (anchored drops the first char; unanchored-after-an-element starts at 0: a different neighboring slot holding 0).
**Fix.** A raw-cell marker spelling `[rsp# + N]`: two parse rules routed to XK_RSP32/64 AHEAD of the colliding prefix check, plus the capture family's private `rspd()` switched to it — writer (SAVE's alpha delta store) and readers (COND/IMM `op_fc_disp`) flip together in one file, so the pair cannot desync. Encoders re-canonicalize, so emitted TEXT and BINARY stay standard (R10).
**Proof.** Probe matrix: cell-stack **5/5** (capture 1500000 == ref, first time since the regression) · frame-rsp 5/5 · frame-r12 5/5 · cell-heap die-list UNCHANGED (span/arbno/capture rc=139 — the parked Z4-8 pre/post baseline preserved). m4 compiled+linked+run matches ref; both defect-shape repros correct. Byte-identity MEASURED by stash round-trip: arith/span/arbno/fib `.s` byte-identical pre→post; capture differs by EXACTLY TWO instructions (`[rsp+48]`→`[rsp+0]` store; `[rsp+80]`→`[rsp+16]` read). Deterministic ×3.

## LANDING 2 — CAS-MARKER (Lon s8 directive): the pend stack carries its own bracket
Lon's ruling: reaching across VARIABLE-length zeta sections is bogus — the conditional-assignment stack "can instantiate a bottom marker at each start of a new pattern match; the cond-asgn sequence code just scans to the marker", and HEAD/RELEASE is the other offender. Implemented: HEAD pushes a tag-0 sentinel entry onto `[RT_CAS_TOP]` (varname pointers are never 0); RELEASE's success pump recovers `mark` by scanning down to the sentinel (`rdi = marker+24`, value-identical to the old bracket by construction) and the post-close restore re-scans and pops entries + marker wholesale (the pump's C calls clobber r10; nested matches inside pumped assignments push balanced markers, so the top returns); HEAD's fail exit scans-and-pops the same way. DELETED: the flat `op_off+32` bracket slot save/reload at all three sites AND release_pump's `zc_frame` fork for it — one config-blind, depth-free mechanism serves every basis. Verified: same full matrix as above (3×5/5, heap die-list identical), m4 capture OK with the marker push visible in the emitted asm, pattern_test output unchanged against the frame-r12 oracle. Corpus regen: 17 demo `.s` files re-emitted (every match statement changes, as expected), regen stable.

## THE CENSUS — Lon's FORTH thesis is measured, not assumed
Thesis: every BB operand's offset from RSP is compile-time known because the producing BBs executed in known order; no operand should ever be reached across a variable-length section. Census of `op_fc_disp` consumers + raw rsp producers at HEAD: the value spine (`bb_unop`, `bb_binop_arith`, `bb_binop_concat_slot`, `bb_assign_global`) reads operand cells at STATIC frontier offsets at bracket depth 0 — the thesis already working. The ONLY variable-depth reaches are (1) capture COND→SAVE (`fc_cond_fp`) and (2) HEAD/RELEASE bracket recovery (`fc_head_fp`, the rfc() arms) — exactly the two families Lon named. Landing 2 deleted the pend-bracket half of (2). REMAINING to reach pure FORTH: **M2** — route SAVE/COND deltas onto the side path entirely (gate `fc_save_active`/`fc_cond_fp` off → the proven `rt_cap` + pend machinery serves all shapes; deletes reach (1) and retires the ZB-FC-3c mechanism whose addressing Landing 1 repaired — Landing 1 remains the diagnosis that exposed the collision class); **M3** — carry `rsp_mark`+`patstk` in the marker entry (fields +8/+16, pushed by HEAD, restored by RELEASE from the scan) and delete `fc_head_fp` → the rfc() window reads go; **M4** — with both reaches gone, audit what still feeds `op_flat_disp`/`fc_leaf_walk`/the per-graph rbp pin: whatever remains addresses FRAME slots, not operands, and shrinks toward deletion.

## HONEST RESIDUALS
`roman.sno` SEGVs under cell-stack (rc=139) — REPRODUCED ON THE PRE-FIX BUILD, pre-existing, unrelated to both landings; frame-r12 runs it: the next cell-stack die-list item. Treebank demos emit 0 bytes under cell-stack pre-fix (uncharacterized). The systemic `rsp#` flip for the remaining plain-spelling cell families (value spine at depth 0 today, ALT, arbno trd/trq) is a named follow-up — flip each family's writers+readers together, never one side. All numbers RT_OPT=-O0, relative-only.
