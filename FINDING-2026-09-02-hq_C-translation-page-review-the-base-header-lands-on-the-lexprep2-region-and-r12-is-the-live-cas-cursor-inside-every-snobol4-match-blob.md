# FINDING 2026-09-02 (hq_C) — review of `ARCH-PROLOG-BYRD-BOX-TRANSLATION.md` against the tree: the § A.1 header at `[rbp+0..48)` lands on the region `lexprep2` zeroes and `bb_suspend` owns; r12 is NOT free — it is the live C.A.S. cursor inside every SNOBOL4 match blob and the scratch register of Prolog's own sinks; a live C.A.S. island allocator exists and is carve-only; five smaller corrections

**Tree:** SCRIP `2748100d` (= `922cfaf4` + this seat's dead-globals rung `d42d2918` + the watermark re-pin) · .github `5ad7cf09` (ceo's four routing commits of Lon's register grants are IN this review) · `RT_OPT=-O0` · MODE `DUO` (file read). Every anchor was grepped on that tree today; the page's own rule holds — the symbol is the key, the line number rots (the sweep moved every one the page cites by ~20 lines: `IR_CUT` :688→:710, the cut rewiring :191-208→:213-230, `cx.cut_ω = uwf` :816→:839, `$unwind_nothrow` :840→:862).

**Asked by ceo (telegram `sweep-landed`, item 2; then `5ad7cf09`: "hq_C checks whether a live C.A.S. allocator exists to share"):** review § A.1 frame record, § B.6 cut, § B.10 catch, § C globals map, § E rungs. **Verdict: the page is right about the shape (one frame; cursor, resume and mark inside it; no CP stack; no global trail; the B/B0 pair; cut as `B := B0`; with Lon's grants, B on r13, ROOT on r14, the ball on r15, the trail on r12). It is wrong about WHERE the header sits, and the r12 grant needs two facts the ABI header hides.** Corrections in order of cost if missed.

## C1 (§ A.1) — the header cannot sit at `[rbp+0..48)`: that region is zeroed by `rt_jmp_frame_lexprep2` on EVERY β re-entry and read as "already suspended" by `bb_suspend`

Under clause (a) as landed, `rbp` is pinned AFTER the carve (`xa_flat.cpp` zframe prologue: `sub rsp,kt` · `[kt-24]=γ` · `[kt-16]=ω` · `[kt-8]=caller base` · `rbp=rsp`), so `[rbp+0]` is the BOTTOM of the frame — the start of the data region `[0, kt-32)` (`xa_flat.cpp`: `data_bytes = kt - 32`). Today that region is:

- **zeroed whole on every β re-entry**: `rt_jmp_frame_lexprep2(fb, …, region_bytes)` does `memset(fb, 0, region_bytes)` and then writes the pending trail-mark pair at `fb+0` / `fb+8` (`rt.c`). A header at `[rbp+0..48)` holding `F.B F.B0 F.CUR F.RES F.HI F.TRMARK` would be erased at the exact moment β needs `F.RES` and `F.CUR` — until PZ-5 deletes lexprep2's Prolog arm, which the page orders AFTER rung 2.
- **owned by the suspend template**: `bb_suspend` tests `FRQ(0)` as its "already suspended" flag (`mov rax,FRQ(0); test rax,rax; je L61; mov FRQ(0),0; call rt_pl_zf_resume_clear; mov rax,FRQ(op_sb); jmp rax`) and its non-ZRES arm banks the yielded DESCR at `FRQ(0)/FRQ(8)`.

The page knows the pair exists (*"the same reason today's `[fb+0]/[fb+8]` 'already suspended' pair is base-relative"*) and then places `F.B`/`F.B0` on top of it without saying the pair goes, and without noticing the memset.

**Recommended correction (a pure move, no rebase):** put the header at the TOP, beside the wire header that already lives there — `[kt-32]` is the free word today (the dc pad; the PZ-4 baton's NINETEENTH-pass block already banks the yield-time rsp there), `[kt-24]=γ`, `[kt-16]=ω`, `[kt-8]=caller base`. Extend the carve by the header size and define **`F.B` (and therefore r13's value) as the ADDRESS OF THE HEADER (`rbp + kt - HDR`), not `rbp`**: every box knows its own `kt` at compile time, so its header is a fixed `[rbp + const]` for the owner, and a binder holding only `B` reads `[B+off]` at fixed offsets — which also makes **`F.HI` unnecessary** (`HI = B + HDR`). Nothing in `[0, kt-32)` moves, no `zls` offset changes, lexprep2's memset cannot reach it, and the killswitch arm stays byte-identical. The alternative (shift every `zls` offset by 48) re-homes every `FRQ` in every Prolog graph and is exactly the rebase clause (a) was designed to avoid.

## C2 (§ A.1 "The B wire", § B.10, § F.3) — SUPERSEDED by Lon's rulings (`e1efdadb`, `cd1f48be`): r10/r11 are the statement number and the BB node id; B rides r13, the ball r15. Two residues survive the ruling

The draft's `r10 = B` / `r11 = ball` is void as written and the page now says so. What the ruling does not say and the code does: (i) **the α half of the B wire is not needed at all** — the callee already receives the caller's base at α (`[kt-8] ← rbp`, `xa_flat.cpp` PL-ZA-1 arm), so `F.B0 := [callerbase + hdr + 0]` is one load through the pin even before r13 exists; with r13 live it is `F.B0 := r13`. (ii) `x86_port_hook`'s diag-regs arm writes `mov r11, nid` at every α/β DEF site — that IS the ruling's mechanism, not a hazard any more; the ball on r15 crosses β DEF sites unharmed. (iii) r13/r14/r15 are the σ/δ/Δ scanning pins of every SNOBOL4 match blob and Icon scan (`rtx_abi.inc` BLOB PINS); the polyglot-boundary save the page promises must include **r12** (below) — a quad, not a triple.

## C3 (§ A.1 r12 = TR, § C, `5ad7cf09`'s question) — r12 is NOT free: it is the C.A.S. cursor inside every SNOBOL4 match blob today, banked at `[rbp-8]` around calls, and the scratch register of Prolog's own sinks. A live C.A.S. island allocator EXISTS — and it is carve-only, so it is not a stack Prolog can share

`rtx_abi.inc` says *"r12 IS FREE — NOT A PIN … 'r12 conditional-assignment stack pointer (own mmap area)' … is DEAD HISTORY"*. Measured on `2748100d`:

| where | `"r12"` uses | role |
|---|---|---|
| `bb_match_begin.cpp` / `bb_match_end.cpp` / `bb_match_capture.cpp` / `bb_scan_match.cpp` / `bb_match_replace.cpp` / `bb_match_arbno.cpp` | 13 / 17 / 10 / 8 / 3 / 3 | **the live C.A.S. cursor**: `bb_match_begin` emits `note cas_top; mov [r12+0],0; note cas_rsp_mark; mov [r12+8],rsp; add r12,24` per entry and reloads `mov r12,[rbp-8]` after calls (`bb_match_begin.cpp`, `bb_match_end.cpp` ×2) — "not a pin" means *not preserved across rtx entries, so the blob banks it in its own frame*; it does not mean unused |
| `xa_flat.cpp` | 24 | scratch (`lea r12,[rip+sym]`, sig staging loops) in the flat-graph prologue every Prolog graph runs |
| `bb_call_fn.cpp` | 16 | scratch in **Prolog's own sinks**: `lea r12,[rip+g_pl_trail]`, `[rip+g_hp_fr]`, `[rip+g_plw_dot_sl]`, `mov r12,[r8+8]` — the quartet this seat deleted today used r12 the same way |
| `bb_define.cpp`, `emit.cpp`, `x86_asm.h` | 2 / 2 / 2 | scratch / register-name tables |
| `bb_create.cpp` | 1 | `contract_regs[6] = {r12, r13, r14, r15, rbx, rsp}` — r12 is ALREADY in a preserve-contract set at the co-expression boundary |

**The allocator:** `pattern_match.c` — `RT_CAS_ISLAND_BYTES = 8 MB`, `g_cas_base`/`g_cas_used` reserved through `rt_slab_region` on first carve, `rt_cas_carve(bytes)` is a **bump allocator with no free** that aborts on exhaustion (*"carve exceeds the island (raise RT_CAS_ISLAND_BYTES)"*), exported `rt_cas_roots(base, bytes)` and `rt_cas_live_span` for the GC. The per-blob C.A.S. *areas* are carved from it once; the *stack* is r12 walking inside a carved area during a match.

**Answer to `5ad7cf09`'s question:** yes, a live C.A.S. allocator exists — and Prolog should NOT share it. It is carve-once (no pop), it is sized for pattern areas, and it aborts the process when full: a long backtracking run trailing into it would kill the program with a SNOBOL4 error text. **Recommend:** the Prolog trail is its own `rt_slab_region` (the same primitive `rt_cas_carve` uses — no new allocator), base and cap two fields of the ROOT record on r14, top in r12, GC roots through a Prolog sibling of `rt_cas_roots` (the entries hold `{addr, old}` where `old` may be a heap cell). And the grant carries a **rung-0 before rung 2b**: re-home every Prolog-reachable scratch use of r12 (`xa_flat.cpp` 24, `bb_call_fn.cpp` 16, `bb_define.cpp` 2, `emit.cpp` 2) to a register no Prolog box pins, graded by `.s` byte-identity on the SNOBOL4 and Icon control arms (SHARED-NODE law: `bb_call_fn.cpp` and `xa_flat.cpp` are shared nodes). Without it, the first `lea r12,[rip+…]` in a unify sink destroys the trail top mid-clause. The polyglot boundary saves r12 with r13–r15 (a quad).

## C4 (§ B.6) — `rsp := rbp` at the cut assumes rsp == rbp at every inter-goal point; hq_P measured 34 rsp moves including a `pop rsp` in one body

Under the post-carve pin `rbp` equals the α-time rsp. The cut box's `rsp := rbp` is correct only if the body's rsp is back at the α value between goals — an invariant nobody has measured: hq_P's rebase finding (`adc17766`) counted 34 rsp moves in the fact/2 body, including a `pop rsp`. **Before PZ-3 relies on it: grep the `.s` of the cut witnesses for every `rsp` write between two goal boxes and assert the net is zero; if it is not, the cut banks its site rsp (the `[kt-32]` word again, or a spine cell) and restores THAT.** With TR on r12, the cut also needs no log promotion — the page already says so after the second grant; `F.TRMARK` of the surviving choice is simply left where it is.

## C5 (§ C, § E rung 9) — three of the 29 exported symbols and the `g_zeta_mode` compare are already gone

This seat's dead-globals rung (`d42d2918`) deleted `g_resolve_b3_call_mark`, `g_resolve_bb_table`, `g_resolve_bb_count` from `nm -D` (29 → 26) and the `lea/mov/cmp/je` quartet on `g_zeta_mode` in the `$trail_mark` sink (0 references in emitted code after the regen, corpus `a0cca818`). § E rung 9 (P6) lists *"`g_zeta_mode` compare gone"* — strike it, it is landed; the sink itself stays (it is the inline trail-mark fast path), only the compare went. `g_zeta_mode` the DEFINITION is now reader-less in `src/` and in emitted code; CLAUDE.md's keep-banner has lost its stated reason (routed to ceo, not deleted — scope).

## C6 (§ A.1 `F.ACT[s]`) — the token is `callgen.act + 8`, and the `zls` kind map describes that word as "pad (unused)"

`bb_call_proc_staged.cpp`'s β arm reads `FRQ(act + 8)` (the retained frame / region pointer, before `mov rsp,[rax+24]; jmp [rax+32]`); `zeta_storage.c` registers `act+0` as *"the spine arm's epilogue-once flag (0/1, α-zeroed)"* and `act+8` as *"callgen.act pad (unused)"*. Name `act+8`, and note the zls description is stale (kind `ZK_RAW` is correct — it is a stack address, never a GC pointer).

## C7 (§ E rung 1 DONE-WHEN) — `SCRIP_ZSM=1` keys the ζ-depth rbp mode, not a box-event census

`x86_zdp_rbp_on()` reads `SCRIP_ZSM`; the event census is `SCRIP_ZSM_CENSUS` (`runtime_init.c`, `zsm_census()`), with `_LEAK_REPORT`, `_OVERPOP`, `_BSKEW`, `_AEXP`, `_ALL` siblings. Write `SCRIP_ZSM_CENSUS=1` or the clause measures a different instrument. `x86_port_hook(site, port)` exists as named; `SCRIP_PL_TRACE` does not exist anywhere in `src/` — new (a getenv-cached function-scope static is the sanctioned shape, no banner needed).

## C8 (§ E rung 2) — one rung carries the header, the register plan, the trail on r12, the unify-sink rewrite, the landing AND six symbol deletions; none of it is gradeable alone

The PZ-4 baton's own plan (NINETEENTH pass) lands (b)+(c)+(d) — retain, landing, β re-entry through `F.RES` with the yield-time rsp banked in the frame — as ONE push graded on `chain2b.pl` `1 2 3`, `nested.pl` `2-1 3-1 3-2`, `deep.pl` four solutions, both modes, with the SNOBOL4 arm as control. That is a rung a seat can finish in a session and prove. Recommend: **2·0** = the r12 scratch re-home of C3 (byte-identity control) · **2a** = header at the top (C1) + retain + landing + β through `F.RES`/`F.CUR`, CP stack still fed and then deleted in the same push once the three witnesses are green · **2b** = TR on r12 with `F.TRMARK`, the unify sinks' conditional push, `g_pl_trail`/`g_pl_env_area`/`g_resolve_mark_stack` deleted, hq_P's constant-per-rep test as the gate. The page's rung-2 DONE-WHEN then becomes 2b's; 2a's is the three witnesses.

## What the review did NOT find wrong

§ B.3's choice box (cursor + resume in the frame, Jcon `Alt` with the temporary promoted) matches `bb_disjunction.cpp` (`FR(op_off+16)` is today's spine-cell cursor) and `bb_move_label.cpp` (`rt_pl_retry_push`) exactly; § B.10's catch-as-choice-frame is the right shape and `rt_pl_throw_set` does copy the ball; § B.11's log rule (log into the youngest CHOICE's frame, promote only at cut) was the WAM's and the second grant makes it the WAM's literally — TR on r12, conditional push keyed on "older than r13"; § C's homes for every runtime global are consistent with the census and with this seat's rung; ROOT on r14 answers § F.4 cleanly. r13–r15 are callee-saved, so every C helper preserves them for free, as the page says.

**Routed:** telegram `dead-globals-landed-watermark-bisected-and-the-page-review` to ceo pointing here; the page is ceo's to amend (DRAFT until reviewed — this is the review).
