# FINDING 2026-09-02 (ceo, TRIO, second account) — RUNG 6 LANDS: every deterministic builtin of the rung is a direct leaf through the shared `bb_call` box, binding leaves carry the trail context through one rtx trampoline each, and the REPORTED master board moves 50 → 169 of 400 in both modes

**Trees:** SCRIP `c286e513` (step 1: `is/2`, the arithmetic tree, the six arithmetic comparisons) + `b19efced` (the rest of the rung), both rebased on origin `52904468` (scripts-only since `7d0ed0f8`); corpus `62dc7995` (hq_B's re-cut of witness (d) and the power re-pins) + the ceo's promotion commit (below); `.github` this commit. `RT_OPT=-O0`. Box clock 17:05–18:15 CDT. Lon's orders that started it, in-chat to ceo 16:55–17:05: *"Let's get Prolog to 100%."* · *"Just start climbing the rungs from simple syntax to more complex syntax as you go along."* · *"Get back on Prolog now that we are cooking with it."*

## 1. What landed, in the rung's own order (simple syntax first)

1. **`is/2` and the arithmetic tree** — `lower_arith_val` rebuilds the Proebsting value tree the cut deleted: `$ax_<op>` `IR_CALL` value nodes as operands (32 evaluable functors: `+ - * / // div mod rem ** ^ min max gcd xor >> << /\ \/`, unary `- + abs sign truncate integer float floor ceiling round sqrt msb \ sin cos atan log exp float_integer_part float_fractional_part`, `pi`), `$is_v` unifying the left side with the value; `< > =< >= =:= =\=` as `$cmp_<rel>` leaves over two value trees. The runtime bodies (`dop_ax`, `dop_cmp`, `rt_pl_dop_is_v_c`) had survived the cut with an emitter table of 15 rows; the table gains 32 `$ax_*` rows and `PL_AX_LEAF` mints the 31 missing `rt_pl_dop_ax_<op>` wrappers. `^`/`**` on integers with a non-negative exponent yield an integer by repeated squaring with overflow detection; `/` on integers yields an integer when exact — both the swipl default the master refs are cut from (hq_B measured 34 of 35 producible refs byte-identical to swipl 9.0.4 and re-pinned the two gprolog-era `1024.0` refs).
2. **Standard order and type tests** — `== \== @< @=< @> @>=` (`rt_pl_atop_cell`), `compare/3`, `var nonvar atom number integer float atomic compound callable ground is_list acyclic_term`.
3. **Write family** — `writeq print write_canonical writeln tab put_char halt/0,1 flush_output`, `format/1,2` (a non-list second argument is wrapped, as swipl does); `tab/1` lowers its argument as an arithmetic value.
4. **Conversions** — `atom_length atom_concat atom_chars atom_codes atom_number atom_string upcase_atom downcase_atom string_concat string_length string_lower string_upper string_to_atom number_string string_chars string_codes atomic_list_concat/2,3` (split mode when the list is unbound) `concat_atom/2,3 char_code number_codes number_chars name char_type` (det mode, first argument bound) `term_string term_to_atom atom_to_term/3` (both directions, the bindings list).
5. **Term construction and sorting** — `functor arg =.. copy_term numbervars/1,3 succ plus sort msort`.
6. **Input** — `get_char peek_char read/1`: a term is read from stdin up to its end dot, parsed by the runtime-linked `prolog_parse` as the argument of a `'$rd'/1` fact and converted to cells with variables keyed by NAME (the raw parse carries the name in `v.sval`; slots are assigned later by the driver — the first cut keyed on `v.ival` and produced a fresh cell per occurrence, caught by the `f(X,Y,X)` probe).

**Mechanism, the same for every leaf:** `pl_det_leaves` (name, arity → `$sym`) in `lower_prolog.c` → `pl_leaf_lv` (operands as lvalues so a leaf can bind) → `IR_CALL` → `bb_call.cpp`'s `dop_direct_fp` table → a direct `call` of the C body (read-only leaves `dop_pl_<name>(args, n)`) or of an rtx trampoline `rt_pl_dop_<name>` (binding leaves: `PL_CTX_LEAF(nm)` saves `r12`/`r13` in a stack ctx, calls `rt_pl_dop_<name>_c(args, n, cx)`, restores `r12` — the `$is_v` shape). A `$sym` outside the table would fall to the by-name path the cut forbids, so every leaf is in the table. **Binding goes through the trail:** the surviving `rt_pl_*_cell` bodies of `unification.c` take `pl_tr_ctx_t *cx` and bind through `plc_unify_into_cell_cx` / `plc_unify_cells_cx` / `plc_bind_cx`, which delegate to `plw_unify_cells` / `plw_bind` (`plw_unify_cells_x`, `plw_unify_cell_val`, `plw_bind_x` exported hidden from `by_name_dispatch.c`); `cx == NULL` keeps the old untrailed path for the rung-7/8 machinery still to be re-derived. The 37 trampoline names are enrolled in THE ONE allow-list of `pl_quad_regs_scan.py` by routine name (the rung-1 ruling). A stream arity (`write/2 nl/1 format/3 read/2 …`) refuses naming rung 9; a rung-6 name at an unwired arity refuses as *"builtin arity not wired"*.

## 2. Measured

| measurement | rung-1 tree `7d0ed0f8` | this tree `b19efced` | instrument |
|---|---|---|---|
| ladder `--only 6` | REFUSE (no witnesses) → RED 0/12 on corpus `6936651c` (hq_P's fail-once) | **PASS 12/12** (6 witnesses × 2 modes) on corpus `62dc7995` | `test_prolog_ladder.sh --only 6` |
| ladder `--to 1` | PASS 4/4 | PASS 4/4 | `test_prolog_ladder.sh --to 1` |
| fresh `--compile` of the six witnesses | rc=2 (refuse) | rc=0, `rt_call_arr_bl` 0, `_rkfn` 0, Prolog-only globals 0 in every `.s` | hq_P's corrected rung-6 criterion |
| refusal census, first refusal per master entry | 383 entries: 51 compile+pass; refuse 2: 78 · 3: 9 · 4: 3 · 5: 44 · **6: 132** · 7: 2 · 8: 4 · 9: 35 · 10: 25 | 400 entries: **172 compile, 171 pass m3**; refuse 2: 83 · 3: 9 · 4: 3 · 5: 52 · 6: 1 (`plus/2`, no such builtin) · 7: 2 · 8: 5 · 9: 43 · 10: 30 | `scratchpad/census.sh` (every entry `--compile`d then run m3 vs its ref) |
| REPORTED master board (not a gate until rung 10) | 50/383 both modes | **169/400 both modes** (m3 fail=207 = refusals naming their rung; m4 fail=1 = `simple_program_97`, the directive-less power entry; xfail 22, xpass 2) | `corpus_suite_harness.py run … --lang prolog --modes m3,m4` |
| Prolog smoke | 2/5 | 3/5 | `test_smoke_prolog.sh` |

The two XPASS entries the board now reports are named in the promotion commit for hq_B (the marker owner); the ceo promoted only the six rung-6 witnesses, proven by `list` (read_suite) and the board in the same commit per the INTERIM PROMOTION PROTOCOL.

**Hard arms (SHARED-NODE VERDICT SCOPE — `bb_call` is every language's box), pristine build of the rebased tree in an isolated sibling root (`scratchpad/root6`, SCRIP worktree + corpus worktree), script `scratchpad/gates6c.sh`:** `make` rc=0 · `nm -D` Prolog-only data symbols 0 · `strip_comments.py --check` rc=0 · Icon smoke 14/14 both modes · Icon STRICT rung suite `PASS=264 FAIL=6 BADEXIT=1 XFAIL=27 TOTAL=298` in all three modes (the pinned watermark, unchanged) · `make test` rc=0: SNOBOL4 `m3 PASS=1679 FAIL=0 · m4 PASS=1679 FAIL=0 SKIP=0 · MISSING=0` GATE OK, every gate OK, quad gate `GATE PASS(0)` 0 unenrolled r12–r15 writes over 9 compiled witnesses (was 4 — the rung-6 witnesses now compile), canaries c1/c2/c3 OK (script `scratchpad/gates6c.sh`, 17:50–18:03 CDT, corpus `6936651c` for the non-Prolog arms)

Step 1 alone (`efafb6fc`, pre-rebase, same `src`) had already passed the same set: SNOBOL4 m3/m4 1679/0, Icon smoke 14/14, STRICT rung suite `264/6/1/27 of 298` in all three modes, `nm -D` 0, strip 0, quad gate 0 unenrolled writes, `make test` rc=0.

## 3. What is NOT closed, honestly

- **The row's DONE-WHEN is cumulative (`--to 6`) and cannot pass before hq_C's rungs 2–5 land** — the row stays ASSIGNED/RUNNING with this landing in its ledger; `--only 6` is the landing gate (hq_P's arm, `b8cbe837`).
- **No rung-6 `ALL.trace` blocks yet:** `test_gate_pl_port_trace.sh --to 6 --cut` REFUSES on `ladder__rung02_choice_redo` (rung 2 unlanded; the instrument correctly says "ZERO trace lines" is not "no ports"); the gate needs an `--only N` arm (asked of hq_P, telegram `trace-gate-needs-only-n-for-the-rung-6-cut`); the six blocks are cut the moment it exists.
- **`read/1` leaks its `PlProgram` per call:** `prolog_program_free` is declared in `prolog_parse.h` and defined nowhere; a loop reading 10⁵ terms grows. Row for the parser owner.
- **No bigints:** `2^100` overflows to a float; swipl prints the integer. `char_type/2` is det-only (first argument bound); swipl's generator mode is rung 7's. The `**`/`^` integer rule follows swipl, not ISO — hq_B asked for a dialect ruling; ruled at the Prolog cursor: the master's oracle is swipl 9.0.4 with default flags.
- **`simple_program_97`** carries a 4-line ref the oracle recipe cannot produce (no initialization directive) — hq_B's open question, ruled at the Prolog cursor.
- Pre-existing over-200-column lines in `unification.c`/`by_name_dispatch.c` (not mine, counted by hq_B's style oracle) are untouched; every line the ceo wrote is ≤ 200.

## 4. For the ladder

Rungs 2–5 are hq_C's and are the first blocker for 147 entries; rung 9 (catch/throw, existence errors, streams) for 43; rung 10 (call/N, assert, DCG, flags) for 30. When rung 5 lands, `--to 6` closes this row and the census is re-run from origin.
