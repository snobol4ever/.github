# ARCH-DYNAMIC-STORAGE — NO FIXED LIMIT A PROGRAM CAN REACH

**Opened 2026-09-23 14:57 CDT by the ceo on Lon's word, in-chat, verbatim:** *"You know I like dynamic structures. I love the realloc. We, of course, are using 100% GC heap and private arenas now. But, we must still handle almost everything without a FIXED LIMIT. We should have all dynamic things be able to grow dyamically and not be static. Scan the SCRIP for all fixed arrays, fixed buffers, etc. Determine which should be dynamic. THen let's figure a way to make them dynamic but using GC HEAP instead."* This page is the census, the classification rule, the mechanism and the conversion list. The instruments are `SCRIP/scripts/audit_fixed_caps_census.py` (the census) and `SCRIP/scripts/util_dyn_caps_witness.sh compile|runtime|census` (the criteria), both on origin at SCRIP `9f5233f70`; the first full census is `findings/fixed-caps-census-2026-09-23-9f4da0b64.tsv` (1406 rows).

## 1. THE CENSUS (first reading, SCRIP `9f4da0b64`, 2026-09-23 14:5x CDT)

| population | count |
|---|---|
| array declarations with a constant bound (literal or UPPER_CASE macro), 345 source files, 14 generated files skipped | **1406** |
| of those at file, static or struct-field scope (a table, not a scratch local) | **366** |
| file/field tables whose bound macro is NEVER compared in its own file (no guard of any kind: a silent overrun) | **49** |
| distinct bound macros · cap-shaped `#define`s with a numeric value | 99 · 97 |
| `realloc(` calls in the tree | **0** (the 09-18 eradication sweep) |
| compile-time tables bound by a population cap (checker `compile`) · runtime tables (checker `runtime`) | **64 · 34** |

By directory (file+static / field / local / param): runtime 47/0/448/18 · lower 34/7/140/5 · emitter 32/16/57/0 · ir 35/6/26/0 · driver 15/13/52/2 · runtime/core 29/1/69/11 · runtime/rt 22/3/34/0 · templates/bb 32/0/27/2 · templates/x86 21/2/13/0 · parsers (six) 13/20/105/3.

**How the caps behave today when a program crosses them, measured, not read:** a 70-argument Icon call (`CALL_ARGS_MAX` 64) SILENTLY TRUNCATES — arguments 65..70 arrive as `&null`, `error 102` fires, iconx prints 2485; 200 SNOBOL4 DEFINEs (`SNO_DEF_MAX` 128) refuse LOUDLY at `f129` where `sbl -bf` prints 20100; 600 Prolog predicates (`MAX_PREDS` 512) refuse with NO OUTPUT where gprolog prints 600 lines; `CALL_STACK_MAX` 256 returns FAILDESCR silently; `GLOBAL_MAX` bombs on one path and silently `return`s on two others; `ZSM_N` and the `SNO_*_note` tables silently stop recording; 49 tables have no comparison at all. A 3000-deep SNOBOL4 recursion matched the oracle — depth is not one of today's walls.

## 2. THE CLASSIFICATION RULE

One question per declaration: **can a legal program of the industry-standard language make it overflow?**

- **Class A — bounded by the machine or by the language definition. STAYS FIXED, named on this page.** Register planes (16), one instruction's encoding buffer (`X86_INTERNAL_MAX`), an enum's count (`IR_OP_COUNT`), the wire header (`MW_HDR_BYTES`), `NKLASS` 3, `GC_REP_POPS` 5, `PAS_SET_BYTES` 32 (an ISO Pascal set is 0..255 by the standard), the DESCR tag set.
- **Class B — scratch for ONE value of fixed width.** `char buf[32]` for a 64-bit integer or a hex address. STAYS FIXED when sized from the type; ⛔ program-derived text (a name, a path, a message, a term, a token) never passes through one — that is class C wearing a buffer.
- **Class C — a POPULATION a program grows: tables, stacks, pools, caches-without-eviction, string and token buffers. MUST BE DYNAMIC.** Every entry of § 6.

The ratchet counts only file/static/field declarations today (366). The 750 locals are a second census: a local `char name[256]` holding an identifier is class C and is converted when its file is touched; the page's rule binds it now, the ratchet later.

## 3. WHERE EACH POPULATION LIVES — THE ONE PLACE LON'S WORD MEETS A STANDING RULING

- **RUNTIME populations → THE GC HEAP**, as Lon said: a typed vector block (`HB_DVEC` for DESCR elements, `HB_PVEC` for pointers to heap blocks, a raw kind for words the collector must never read as pointers), ROOTED, SLIDING with every collection like every other live aggregate (`ARCH-GC-COMPILE-TIME-FRAME-MAPS.md` § 7: the collector guesses nothing; the heap walks typed visitors).
- **COMPILE-TIME populations → THE COMPILE-TIME ARENA through `ct_grow`** (`src/ir/ct_arena.h`: `ct_alloc`, `ct_zalloc`, `ct_grow`, `ct_drop`; `ct_grow` has realloc's contract since SCRIP `d2efe8a8f` — grow in place when it can, else alloc, copy and `ct_drop` the old block — already used by `ast.h` for tree children). ⛔ NOT the GC heap, and this is the ceo's reading against the letter of Lon's sentence, stated to Lon: CEO-864 rules compile-time structures live in the compile-time arena, never in the collected heap, and CTO-151 (SCRIP `bda7691cc`, today) is the fresh proof — a heap string baked into runtime-compiled code was reclaimed at the first collection and read as garbage. Emitted code and the tables that describe it must not move or die under the collector. If Lon wants the compiler's tables on the GC heap too, that is a design change to the collector's contract (pinned or immortal blocks), which § 7 of the GC page forbids today; his call, not the ceo's.

## 4. THE MECHANISM — two vectors, one shape, growth by doubling

### 4.0 THE ONE PRIMITIVE IS REALLOC, AND NOTHING FANCIER (Lon 2026-09-23 15:2x CDT, in-chat to the ceo, verbatim: *"So do not do anything more fancy then a grown in place and copy and grown just like realloc does. Of course grow in place only occurs at the frontier. So we will have some alloc and move into doubling areas. Right?"* — right; CEO-1211)

Both allocators are bump allocators with ONE frontier — the GC heap's `g_hp_top` (below `g_hp_end`, inside the hard cap `g_hp_cap_end`), the compile-time arena's `ct_cur` (inside the current 4 MB chunk). A grow of block `p` to `n` bytes is exactly realloc's three cases, in this order:
1. **It fits.** `n` is within the block's capacity: return `p`. (Arena blocks are power-of-two size classes, so the slack is often already there; heap blocks are exact to 16 bytes.)
2. **It is the last block at the frontier.** The block ends at the frontier: extend it IN PLACE by advancing the frontier by the difference. On the GC heap the difference is carved exactly as a fresh allocation carves it — the pacing line check that sets `g_gc_pending` (never a collection inside), the lazy commit inside the hard cap, the quarantine release, the zero-unless-virgin rule, the birth ledger told — and then the header's `size` grows; in the arena `ct_cur` advances and the header takes the larger class.
3. **Otherwise it moves.** Allocate `n` at the frontier, copy the old payload, return the new block, and the old block is FREED: on the GC heap by the collector (it is unreferenced once the root is rewritten; a sliding heap has no other free), in the arena by `ct_drop`, which returns it to its size bin — ✅ LANDED at SCRIP `d2efe8a8f` (cfo, 2026-09-23 17:29 CDT): `ct_grow` takes realloc's contract (a grow that fits returns `p`, the last block at `ct_cur` grows in place, otherwise alloc, copy and `ct_drop` the old block), where it used to abandon the old block and leak the whole growth history; gate `test_gate_ct_grow_takes_reallocs_contract_and_frees_the_old_block.sh`. A block above the largest bin (8 MB) is its own mapping: map new, copy, unmap old.

The growth POLICY is the vector's: doubling, `newcap = max(2*cap, need)`. The realloc only decides WHERE: a vector that is the most recent allocation keeps doubling in place; once something is allocated after it, its next grow moves it to the frontier at double the size, where it can grow in place again. **Nothing fancier:** no segmented or chunked vectors, no free-list search for an interior gap to grow into (the heap's secondary carve window after a compaction is NOT a growth frontier — a block carved there moves when it grows), no `mremap`, no shrink. The contract is realloc's own: after a move the old pointer is dead — and on a moving heap a pointer is also dead after any safe point, which is THE RE-READ RULE below. Primitive names: `rt_gcheap_realloc(p, n)` beside `rt_gcheap_alloc` in `gc_heap.c` (the cto's row) and `ct_grow` given this contract in `ct_arena.c` (the cfo's row: read the 53 existing call sites in 31 files for an alias held across a grow before the drop lands, and run the masters under `SCRIP_CT_POISON=1`, which paints a dropped block 0xDD so a stale alias reads garbage loudly; the four flex scanners call it as `yyrealloc`, whose contract already is realloc's).

**Runtime, `gv_t` (src/runtime/rt/gc_vec.h, the cto's row):** a header OUTSIDE the heap — a static per table, registered once as a root in `core_gc_roots` — holding `DESCR_t blk` (the heap block, typed by its kind), `uint32_t len, cap, esz`. `gv_reserve(v, kind, need)`: when `need > cap`, `newcap = max(2*cap, need, 8)`, `rt_gcheap_alloc(kind, newcap*esz)`, `memcpy` the live prefix, point `blk` at the new block — through `rt_gcheap_realloc` (§ 4.0): in place when the block ends at `g_hp_top`, else a doubled block at the frontier with the old one left for the collector; the allocator never collects (CEO-812), so a grow from C is safe at any point. The header replaces the table's own fixed static in place, so a conversion adds no global (RULES.md § FACT RULES). Element access is ALWAYS `gv_at(v, i)` through the header: ⛔ THE RE-READ RULE — a C local holding an element pointer across any call that can reach a safe point is the CTO-151/holder class (`c_rt_assign_var_body`'s stale `vc` today); index, never cache. Kinds: DESCR elements → `HB_DVEC` (visited as an array of DESCR); pointers to heap blocks → `HB_PVEC` (typed by target kind in the layout); raw words → the raw kind (no pointers, never scanned). One root per table; the collector relocates the block and rewrites the root like every root.

**Compile-time, `cv_t` (src/ir/ct_vec.h, the cfo's row):** `void *p; uint32_t len, cap, esz`; `cv_reserve` doubles through `ct_grow` with realloc's contract (§ 4.0): in place at `ct_cur`, else a doubled block with the old one returned to its bin by `ct_drop`. Every fixed table of § 6's compile-time list becomes a `cv_t` field of the structure that owned it, initialized empty, grown at its first write.

**Hash tables with a fixed bucket count** (`VAR_BUCKETS` 512, `STAGE2_PL_PRED_TABLE_SIZE` 256, `HT_INIT_SIZE` 512) are class C in disguise: a fixed bucket array degrades to a linear chain; they rehash at load factor 1 into a vector twice the size (a `gv_t`/`cv_t` of buckets).

## 5. THE DISCIPLINE (the law line in RULES.md § FACT RULES points here)

1. A population grows on demand; **the ONLY refusal is allocator exhaustion**, loud, naming the table and the size it asked for. No silent drop, no silent truncation, no silent FAIL, no wrong value — the four shapes § 1 measured are each a defect class.
2. Until a table is converted it COMPARES AND REFUSES LOUDLY at its cap (the zls row's discipline, kept): the 49 unguarded tables get their comparison first (the coo's row), then their conversion (the owning officer's row).
3. **The ratchet:** `util_dyn_caps_witness.sh census` reads the census against `scripts/fixtures/dyn_caps/BASELINE` (366) — the count may only fall, and a landing that lowers it lowers BASELINE in the same commit; a new fixed table is RED unless this page names it class A or B first. The coo wires it as a blocking arm (`test_gate_dyn_caps_ratchet.sh`).
4. **Every conversion lands with a witness that exceeds the OLD cap and reads the ORACLE's answer** (the three of `util_dyn_caps_gen.py` are the first; each converted table adds its own), plus the gates it touched and `make preflight`; a shared-node conversion owes the CEO-757 arms.
5. A cap macro that survives as a GROWTH HINT (initial capacity) is renamed `*_INIT` so the census stops reading it as a limit; a macro that survives as a class-A bound is listed in § 2.

## 6. THE LIST — WHICH SHOULD BE DYNAMIC (class C), by owner

**Compile-time (the cfo's row; 64 tables today, printed by the checker's `compile` mode):** `frame_layout.c` FL_MAX_ENTRIES 262144 · FL_MAX_FIELDS 524288 · FL_MAX_SCOPES 16384 · FL_MAX_GRAPHS 16384 and its literal tables (`fvl[2048] za[1024] fcm[1024] fcs[512] fcab[512] fvb[512]`) — the zls row's four successive walls on the gprolog 60-module compiland (row `zls-tables-static-caps-vs-dynamic`, folded here) · `zeta_depth.c` ZDP_CAP 8192 · emitter BB_PATCH_MAX 65536, SMX_STRTAB_CAP 8192, SMX_CSETTAB_CAP 256, AB_FNCELL_MAX 1024, XA_BB_EMIT_PAIR_MAX 1024, ZD_NOPS_MAX 64, FLAT_CHAIN_SET_MAX 512, FLAT_DATA_LBL_MAX 32, WASM_* and the literal statics `g_nid_key/val[262144] g_chain_entry_key[65536] g_zls_lay[32768] g_gc_map_names[8192] g_fbm_nd[8192] g_lblfold_from[4096] g_bnr_vnode/vname[4096]` · `stage2.h` STAGE2_LABEL_MAX 4096, PROC_TABLE 256, PL_PRED_TABLE 256 buckets, FRAME_SLOT 64, MOD 64 · `lower_snobol4.c` SNO_DEF_MAX 128 (**the 200-DEFINE witness**), SNO_PAT_MAX 256, SNO_EXPR_MAX 4096, SNO_FZW_MAX 256, `e[4096]`; `tree_to_sno.c` SNO_LOOP_STACK_MAX 64, `linebuf[16384]` · `lower_prolog.c` PL_BB_TABLE_MAX 256, PL_INIT_GOALS_MAX, PL_CONSULT_FILES_MAX, `valias[1024] cache[1024]`; `prolog_lower.c` MAX_PREDS 512 (**the 600-predicate witness**) · `lower_icon.c` `icn_record_names[512]`; `icon_lex.c` `icn_pp_src[1024]`; `icon_parse.h` `errmsg[512]` · `lower_raku.c` RK_GRAM_MAX 64, `blks[512]`; `parsers/raku/re.*` MAX_STATES 512, MAX_GROUPS 16, MAX_CAPLOG 32 (a regex with 17 groups is refused) · `snocone_lex.*` `text[65536] strbuf[65536]` (a 64 KB token), `sc_*_table[512]` · driver SC_DAT_MAX_TYPES 1024, INIT_MAX 64, SHADOW_MAX 32, CALL_STACK_MAX 256 (silent FAIL), `g_script_exception[512]`, `flatbuf[1024]`.

**Runtime (the cto's row; 34 tables today, printed by the checker's `runtime` mode):** `by_name_dispatch.c` CALL_ARGS_MAX 64 (**the 70-argument witness**, silent truncation), `el[4096]`, the two `static char text[65536]` of Prolog `read/1` (a term longer than 64 KB is cut), `g_tap_todo_reason[512]`, `g_dtax_bid[1025]` · `rt.c` RT_FRAME_STACK_MAX 256, RT_FRAME_SLOT_MAX 64, RT_MAX_CAPTURES 256, EXPRESSION_REG_MAX 256, RT_INITIAL_MAX 8192, RT_DC_FNS_MAX 8192, RT_PROC_LOC_MAX 4096, DCR_CELL_CACHE_SIZE 2048 · `rt_runtime.c` SAVE_MAX 4096, SEQ_CACHE_MAX 64, SUSP_GEN_CACHE_MAX 64, BB_DCAP_MAX 32 · `gen_runtime.*` GLOBAL_MAX 4096 (`global_names`, one bomb path and two silent), FRAME_STACK_MAX 256, FRAME_SLOT_MAX 64, FRAME_DEPTH_MAX 16, EVERY_GEN_SLOT_MAX 16, SCAN_STACK_MAX 16 · `core.c` VSTACK_MAX 1024, VAR_BUCKETS 512 (rehash), FIELD_ACCESSOR_MAX 1024, NV_MEMO_N 2048 · `pattern_match.c` RT_CAS_CAPO_MAX 1024, RT_DCAP_NVCACHE_N 16, `g_sno_defer_cells[4096]` · `runtime_init.c` ZSM_N 65536, ZDP_RBP_TAB_N 65536, ZSM_TRACE 2048 · `unification.c` `seen[1024]` (a term with more than 1024 nodes in the cycle check) · `name_binding.c`, `icon_runtime.c` ICN_STACK_MAX 256 · driver FH_MAX 64 (the 65th open file) · `prolog_atom.c` HT_INIT_SIZE 512 (grows? verify, else rehash).

**Instrumentation that may stay fixed but must refuse loudly (class A by declaration, the coo audits):** `bbprof.c` BBPROF_PC_CAP, `g_altstack[65536]`; `gc_heap.c` `g_ah_tn[512]` (the allocation histogram); `portcount.c`; `g_trace_value_last_text[512]`.

## 7. THE ROWS (minted and doorbelled 2026-09-23 14:57 CDT, rank 1 behind the home stretch and GC-100)

- **cto** — `runtime-every-population-table-grows-on-the-gc-heap-no-fixed-limit-a-program-can-reach`: `gv_t` over the typed vector kinds, the 34 runtime tables, the 70-argument witness first; DONE-WHEN `util_dyn_caps_witness.sh runtime` GREEN (RED at mint).
- **cfo** — `compiler-every-population-table-grows-in-the-compile-time-arena-through-ct-grow-no-fixed-limit-a-program-can-reach`: `cv_t` over `ct_grow`, the 64 compile-time tables, the 200-DEFINE and 600-predicate witnesses first; DONE-WHEN `util_dyn_caps_witness.sh compile` GREEN (RED at mint). The six PARKED rows `zls-tables-static-caps-vs-dynamic`, `{emitter,lower,parser,driver,runtime}-fixed-caps-to-dynamic` are SUPERSEDED into these two.
- **coo** — `instruments-the-fixed-caps-ratchet-is-a-blocking-arm-and-every-remaining-cap-refuses-loudly`: `test_gate_dyn_caps_ratchet.sh` wired blocking over the census mode; the 49 unguarded tables compare and refuse loudly until converted; DONE-WHEN the gate wired and green AND the census mode's unguarded count 0 (RED at mint: no gate, 49).
