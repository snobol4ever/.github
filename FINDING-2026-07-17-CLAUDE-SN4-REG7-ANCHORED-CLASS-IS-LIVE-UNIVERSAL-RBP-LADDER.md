# FINDING 2026-07-17 (s81, Claude, attended) — REG-7's DELETE LIST IS LIVE MACHINERY; THE SEAL IS A UNIVERSAL-RBP LADDER, NOT A DELETION

**ONE LINE:** the anchored-window class REG-7 says to delete is LIVE and load-bearing (65 decline events across the numbered crosscheck corpus; 10/155 feature + 4 benchmark `.s` emit the window today); deleting it un-replaced breaks those programs. The coherent seal = mechanize REG-MAP law 1 (universal rbp = activation flat base) FIRST, then the deletions fall out. Ladder below, every rung file:line-receipted.

## MEASUREMENTS (do not re-derive)
1. **Decline is live.** `SCRIP_TAIL_DIAG=1` sweep, numbered crosscheck corpus: **58 × `no-arbno` · 5 × `walk` · 2 × `multi-arbno`** (65 events; 198 lines across the wider 572-file `.sno` sweep). The `!tail_ok` registration (`lower_snobol4.c:1481-1484`) fires for every non-ARBNO pattern statement that reaches tail candidacy plus the residual walk/multi-arbno declines.
2. **Windows are emitted today.** `mov rbp, rsp` (the `op_anchor_head` materialization, `bb_match_head.cpp:40`): **10 of 155 feature `.s` + 4 corpus benchmark `.s`** (e.g. `053_pat_alt_commit.s:279`, `056_pat_star_deref.s:103`, `strings/cross.s:403`). Reconciliation with s79's "EMPTY" claim: s79 measured **blob-interior** `op_anchor_head` only (the rbp-sharing hazard at `x86_asm.h:315`'s ⚠) — main-graph windows were never claimed empty. s80's "r12 = pend + seed only" is likewise consistent: windows moved to **rbp** at REG-3; the r12 census cannot see them.
3. **Window base ≡ flat base.** `bb_match_head.cpp` FLAT-FIRST comment + artifact `053`: at head-α under RSP, rsp == flat base (LIFO balance between statements, F1s80), so the window's `mov rbp, rsp` pins rbp to the FLAT base — the same value a prologue seed would give. Inside the window, all flat refs are `[rbp+off]` with NO disp; **rsp keeps serving as the moving push cursor** (mark save `mov [rbp+272], rsp` in 053); zero rbp arithmetic in the window body.
4. **Align dance is already rbp-safe.** `x86_align_enter` (`x86_asm.h:1291`) is a **no-op** under RSP && !flat_pat (ZB-OWN-1a G1 landed: rsp ≡ 0 mod 16 always); the flat_pat arm uses the register-free push-rsp/push/and form; `x86_anchor_enter`'s deep-window flavor push/pop-preserves rbp. No C-call path clobbers a resident rbp.
5. **The uncovered activation class = non-pat jmp-entry blobs** (`xa_flat.cpp:154-165` BINARY, TEXT twin nearby): dyn procs + EVAL/DEFER fragments under ZS-2. Header ABOVE the frame (`[rsp+kt-24/16]` wires), **no rbp save/seed/restore anywhere in the arm**. Under a universal `[rbp+off]` prefix these graphs would read the CALLER's rbp = wrong frame. This is the one genuinely new construction the seal needs.
6. **The pat-blob arm is the finished exemplar.** `xa_flat.cpp:167-177`: caller-zr save at `[rsp+24]`, `zr = rsp+32` (under RSP+flat_pat zr==rbp ⇒ emits `lea rbp,[rsp+32]`, visible at `053.s:14`), both-edge restore landed s79 (`[zr−8]` + `%s_res` record[+8] re-pin). The non-pat arm converts by adopting this same shape (or its header-above mirror with a dedicated header slot).

## WHY THE CURSOR READ WRONG (for the record, not blame)
The REG-7 one-liner ("delete op_anchored/…") compresses REG-MAP law 1's **"converts the ENTIRE op_anchored decline class by construction"** into its consequence (the deletions) while omitting the construction (universal rbp). Same failure family as the file's five prior re-derivation corrections: the plan text was written before the s79/s80 landings settled which register serves which role (rsp = FORTH cursor, rbp = frame base — the two roles zr currently conflates).

## THE LADDER (REG-7-U; one rung per commit, gates green, crosscheck watermark m3 303/4 · m4 293/13/1 · DIVERGE=10 identical names re-verified fresh this session)
- [ ] **U1 — OUTER SEED.** `mov rbp, rsp` after the frame carve in the outer-graph arm, BOTH twins (`xa_flat.cpp:186-189` binary beside the REG-4b/REG-6 seeds — bytes `48 89 E5`; `:246-252` text). Additive; nothing reads it yet. Gate: build + smokes 7/7×2 + crosscheck watermark byte-identical (seed is dead weight until U3).
- [ ] **U2 — NON-PAT JMP-ENTRY RBP.** Arm `xa_flat.cpp:154` (+ TEXT twin): save caller rbp (header slot — kt-8 and kt-32 are the unused pads; pick one, document it), `mov rbp, rsp` seed, restore on BOTH exit edges (the s61 both-edges lesson; find the γ/ω landings + the `lea rsp,[zr+K-32]` absolute unwind and mirror s79's restore discipline). ⚠ s69 mine neighborhood: byte-verify every new encoder shape vs `as`; monitor-first on any divergence. Gate: crosscheck watermark identical (still no consumer — pure save/restore is value-neutral).
- [ ] **U3 — CONSUMER FLIP.** `x86_fr32_prefix`/`x86_fr64_prefix` (`x86_asm.h:317-318`) → unconditional `"rbp + "` under RSP; FR/FRQ (`:772/:797`) delete the `op_flat_disp` term; delete `bb_match_head.cpp:40`'s gated materialization (rbp already = base). Blob boxes byte-identical (prefix already rbp); anchored boxes value-identical (same register, same base); granted boxes flip `[rsp+off+disp]`→`[rbp+off]` = the bulk `.s` diff, expected and honest. Gate: crosscheck watermark + regen ×3 (feature/benchmark/demo) + spot-diff one anchored artifact (window region value-identical) and one granted artifact.
- [ ] **U4 — THE DELETIONS (the original REG-7 list, now actually dead).** `op_anchored`/`op_anchor_head` fields (`emit.h:445-446`) + delivery (`emit.cpp:862` anchored half) + `fcanc`/`fcah` registrars (`zeta_storage.c:668-679`) + the `!tail_ok` registration (`lower_snobol4.c:1479-1485` — decide with Lon: delete the tail-grant machinery too since disp compensation is gone, or park it) + zr/zr_num anchored arms (`x86_asm.h:315-316`). Gate: completion greps (r12 only via pend shapes; `grep op_anchored src/` == 0) + watermark.
- [ ] **U5 — +40 RECLAIM + zr SEAL.** Delete the per-head `+40` outer-rbp save (`bb_match_head.cpp:39`) + both-exit restores (locate in `bb_match_release.cpp`) — redundant once rbp is seeded at every activation boundary and every re-pointer restores (blobs s79, non-pat U2). Then `x86_zr()`/`x86_zr_num()` → decide the flat_pat arm with Lon: blob interior pushes currently ride rbp (zr flat_pat arm); moving them to the shared rsp stream is ZS-2's own principle but is a separate behavioral change — seal to `"rsp"/4` only if that conversion is in scope, else seal to `flat_pat?rbp:rsp` and note the residue. Gate: watermark + census r12-as-ζ 0/308 + the TR-7-style SPITBOL A/B scream number.

## SESSION RECEIPTS (s81)
Fresh container: packages + build + oracle green; smokes 7/7×2; crosscheck **m3 303/4 · m4 293/13/1 · DIVERGE=10** — s80 watermark exact, identical fail names. Zero source edits landed (one aborted str_replace never applied; both trees verified clean). SPITBOL manual grounded for the session's constructs: Ch.18 match algorithm (the pushdown stack the pend/dcap machinery natively implements), Ch.9 ARBNO/BAL/ABORT/FAIL/FENCE/SUCCEED, Ch.7 unevaluated expressions (DT_E/DEFER).
