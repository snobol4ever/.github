diff --git a/src/emitter/emit.cpp b/src/emitter/emit.cpp
index 654da639..6d29daa3 100644
--- a/src/emitter/emit.cpp
+++ b/src/emitter/emit.cpp
@@ -1140,7 +1140,7 @@ static int walk_bb_node_inner(IR_t * nd, FILE * out) {
                                             * two different container kinds -- t1.sno was ALREADY producing wrong output (m3=DIFF, m4=DIFF) at the pre-edit baseline (this was never RBP-protected: op_arbno_rbp's own qualification never covered ALTERNATE-bearing bodies, only the plain K0
                                             * arm did, silently), and this session's ARBNO-RBP removal changed its FAILURE MODE (silent DIFF -> m3 SIGSEGV) without being its root cause.  The bomb this exclusion feeds (bb_match_arbno.cpp's op_arbno_body_k0 check) converts BOTH classes from
                                             * silent-wrong-answer or crash into a loud, honest compile-time refusal -- matching the codebase's own established discipline (W-7) rather than leaving either as an unbombed correctness hole. */
-   { int _du=0; int _ds0=-1,_ds1=-1; for(int _j=0;_j<g_emit_cfg->n;_j++){if(g_emit_cfg->all[_j]==nd->operands[1])_ds0=_j;if(g_emit_cfg->all[_j]==nd->operands[2])_ds1=_j;} if(_ds0>_ds1){int _t=_ds0;_ds0=_ds1;_ds1=_t;} if(_ds0>=0) for(int _j=_ds0;_j<=_ds1;_j++){IR_t*_m=g_emit_cfg->all[_j]; if(_m&&_m!=nd&&(int)_m->op==IR_MATCH_DEFER&&!_m->pat_static)_du=1;} g_emit.op_arbno_body_defer_unsafe=_du; }   /* W-7 INTERIM GUARD (HOME-WIRES s38): SAME body span as op_arbno_body_k0 above, SAME containment idiom -- flags an IR_MATCH_DEFER member whose pat_static==0 (can transitively recurse, manual p.122's *X rule).  Independent of op_arbno_body_k0.  Consumed by bb_match_arbno() to decline the unconditional plain-frameless fallback with x86_bomb.  See emit.h field note for the full record. */   /* ⭐ ARBNO-FRAME containment dual of the op_body_has_arbno scan above: THIS node inside any OTHER ARBNO's [operands[1]..operands[2]] span = the nested/unbounded tier -> per-activation header frame (see emit.h field note; Lon directive 2026-08-06) */ } else { g_emit.op_sa = -1; g_emit.op_sb = -1; g_emit.op_arbno_chain = 0; } }
+   { int _du=0; int _ds0=-1,_ds1=-1; for(int _j=0;_j<g_emit_cfg->n;_j++){if(g_emit_cfg->all[_j]==nd->operands[1])_ds0=_j;if(g_emit_cfg->all[_j]==nd->operands[2])_ds1=_j;} if(_ds0>_ds1){int _t=_ds0;_ds0=_ds1;_ds1=_t;} int _alt=0,_ref=0; if(_ds0>=0) for(int _j=_ds0;_j<=_ds1;_j++){IR_t*_m=g_emit_cfg->all[_j]; if(_m&&_m!=nd&&(int)_m->op==IR_MATCH_DEFER&&!_m->pat_static)_du=1; if(!_m||_m==nd)continue; int _o=(int)_m->op; if(_o==IR_MATCH_ALTERNATE||_o==IR_DISJUNCTION){ if(_m->seal)_ref=1; else _alt=1; continue;} switch(_o){case IR_MATCH_LIT:case IR_MATCH_LEN:case IR_MATCH_ANY:case IR_MATCH_NOTANY:case IR_MATCH_POS:case IR_MATCH_RPOS:case IR_MATCH_TAB:case IR_MATCH_RTAB:case IR_MATCH_SPAN:case IR_MATCH_BREAK:case IR_MATCH_REM:break;default:_ref=1;break;} } g_emit.op_arbno_body_defer_unsafe=_du; g_emit.op_arbno_body_actframe=(_alt&&!_ref)?0:1; }   /* ⭐⭐⭐ s125 R-4(a) ARBNO INTERIOR CHOICE POINT: op_arbno_body_actframe==0 (af back-out edge ADMITTED) iff the body span carries an ALTERNATE/DISJUNCTION resume record AND EVERY OTHER span member is a pure scanner-register leaf matcher.  STRICT WHITELIST, safe BY CONSTRUCTION -- an unrecognised op ANYWHERE in the span refuses and keeps s124 behaviour exactly, so no future container has to be remembered here.  DELIBERATELY NOT A BLACKLIST: the first cut enumerated refused containers and MEASURED WRONG -- 151_pat_arbno_inline_fence_backtrack (ARBNO(FENCE('a'|'ab'))) still hung at rc=124 because FENCE() does not reach this span as IR_MATCH_FENCE1, so the blacklist never saw it.  FENCE must refuse for a SEMANTIC reason (manual p.125/p.204: FENCE makes the alternatives inside it invisible to a matcher that is backing up, which is the one thing the construct exists to forbid), and 151 is the exact FENCE-sealed twin of the witness this rung fixes -- ARBNO(FENCE('a'|'ab')) vs ARBNO('a'|'ab'), opposite required verdicts on the same shape. */   /* W-7 INTERIM GUARD (HOME-WIRES s38): SAME body span as op_arbno_body_k0 above, SAME containment idiom -- flags an IR_MATCH_DEFER member whose pat_static==0 (can transitively recurse, manual p.122's *X rule).  Independent of op_arbno_body_k0.  Consumed by bb_match_arbno() to decline the unconditional plain-frameless fallback with x86_bomb.  See emit.h field note for the full record. */   /* ⭐ ARBNO-FRAME containment dual of the op_body_has_arbno scan above: THIS node inside any OTHER ARBNO's [operands[1]..operands[2]] span = the nested/unbounded tier -> per-activation header frame (see emit.h field note; Lon directive 2026-08-06) */ } else { g_emit.op_sa = -1; g_emit.op_sb = -1; g_emit.op_arbno_chain = 0; } }
                                     /* PS-3 (s152) DEFER-TAIL candidacy (decided ENTIRELY here, emit drive time -- LOWER untouched, so a decline is byte-identical chain): body bracket must be EXACTLY one
                                      * WRITE-ONCE defer (seal==2, s142 class: wrcount==1 + fz-safe, so mid-match reassignment cannot exist) whose bound name is PROLOGUE-DOMINATED (assignment executes
                                      * before any label/goto ⇒ before any match ⇒ the slow defer path with its foreign frontier shape is unreachable) plus inert LIT_STRING/GOTO wiring, none of it
@@ -1281,7 +1281,7 @@ extern int           g_gva_active;
 extern IR_graph_t *  g_emit_cfg;
 #define DRIVE_FILL(nd,a,s,f,b) do { \
     g_emit.op_zls2_bytes = 0; g_emit.op_zls2_slot = -1; g_emit.op_zls2_ops = 0; g_emit.op_selfload = 0; \
-    g_emit.op_fc_bytes = 0; g_emit.op_fc_base = -1; g_emit.x86_fc_synth = 240; g_emit.op_fc_fpmax = -1; g_emit.op_fc_disp = -1; g_emit.op_fc_wbytes = 0; g_emit.op_arbno_chain = 0; g_emit.op_arbno_nzq = 0; g_emit.op_tail = 0; g_emit.op_tail_fpb = 0; g_emit.op_tail_fpl = 0; g_emit.op_tail_seal = 0; g_emit.op_tail_ncap = 0; g_emit.op_arbno_dt = 0; g_emit.op_arbno_dt_susp = 0; g_emit.op_defer_leaf_susp = -1; g_emit.op_tail_dfr = 0; g_emit.op_tail_fpr_rsp = 0; g_emit.op_body_has_arbno = 0; g_emit.op_arbno_framed = 0; g_emit.op_arbno_body_k0 = 0; g_emit.op_arbno_body_kk = 0; g_emit.op_fence_body_kk = 0; g_emit.op_fence_frame_off = -1; g_emit.op_arbno_body_defer_unsafe = 0; g_emit.op_cap_anchor = 0; g_emit.op_arbno_rbp = 0; \
+    g_emit.op_fc_bytes = 0; g_emit.op_fc_base = -1; g_emit.x86_fc_synth = 240; g_emit.op_fc_fpmax = -1; g_emit.op_fc_disp = -1; g_emit.op_fc_wbytes = 0; g_emit.op_arbno_chain = 0; g_emit.op_arbno_nzq = 0; g_emit.op_tail = 0; g_emit.op_tail_fpb = 0; g_emit.op_tail_fpl = 0; g_emit.op_tail_seal = 0; g_emit.op_tail_ncap = 0; g_emit.op_arbno_dt = 0; g_emit.op_arbno_dt_susp = 0; g_emit.op_defer_leaf_susp = -1; g_emit.op_tail_dfr = 0; g_emit.op_tail_fpr_rsp = 0; g_emit.op_body_has_arbno = 0; g_emit.op_arbno_framed = 0; g_emit.op_arbno_body_k0 = 0; g_emit.op_arbno_body_kk = 0; g_emit.op_fence_body_kk = 0; g_emit.op_fence_frame_off = -1; g_emit.op_arbno_body_defer_unsafe = 0; g_emit.op_arbno_body_actframe = 0; g_emit.op_cap_anchor = 0; g_emit.op_arbno_rbp = 0; \
     /* ⛔⭐ CARVE-DATA-ERAD (s23a, Lon 2026-08-01 "delete the ENTIRE data structure that stores the whole-graph data ... it is GONE NOW"): the fc_leaf_disp fill is DELETED -- op_flat_disp is permanently 0, the whole-graph displacement table has NO consumer, and the +op_flat_disp terms downstream are arithmetically dead pending janitorial code deletion.  The ⛔ step-2 falsification's 3-program cost is accepted by the directive. */ \
     bb_label_t *_fs__=bb_label_fold((s)), *_ff__=bb_label_fold((f)); \
     g_emit.lbl_α=(a)->name; g_emit.lbl_γ=_fs__->name; g_emit.lbl_ω=_ff__->name; g_emit.lbl_β=(b)->name; \
diff --git a/src/emitter/emit.h b/src/emitter/emit.h
index d72e0a04..7ae7cee6 100644
--- a/src/emitter/emit.h
+++ b/src/emitter/emit.h
@@ -637,6 +637,7 @@ typedef struct {
     int                          op_cap_frame_off; /* ⭐⭐⭐ ACTIVATION-FRAME SLOT REGISTRY (THREE ZETAS s87, transient-frame arm DELETED s88): staged from capture_frame_slot(nd) at the IR_MATCH_ASSIGN_SAVE/COND/IMM drive cases -- REPLACES bb_match_capture.cpp's OWN transient push-rbp/pop-rbp arm (s81/s83), which was measured (D11 probe) to collide with ARBNO-FRAME slots textually nested inside its SAVE..IMM span: CAPTURE's own push established a NEW, different rbp than STANDING's; an ARBNO between SAVE and IMM wrote its cell relative to THAT transient rbp; IMM's pop then discarded it before the ARBNO's own retries were done, corrupting the counter on backtrack -- the exact crossover class Lon's own prior per-construct-transient-frame attempt for ARBNO/CAPTURE hit and reverted before this goal began. Same shared frame_slot_scan numbering as op_arbno_frame_off (interleaved textual order, ONE index space) so a capture and an ARBNO nested inside it are ALWAYS assigned different, non-overlapping offsets in the SAME already-live frame -- neither pushes or pops anything of its own, so neither can clip the other's lifetime. -1 = unassigned: bb_match_capture.cpp now BOMBS honestly (s88) -- when this node's own op_frame_need==1 and op_cap_frame_off==-1 (emit_match_rbp()==0, or no enclosing MATCH_BEGIN in g_emit_cfg -- the PAT$N stored-pattern-blob boundary), the s81/s83 transient-frame arm this comment used to describe as the fallback is DELETED, not merely unreached; the same boundary bomb bb_match_arbno.cpp's own ARBNO-FRAME arm already takes at op_arbno_frame_off==-1. When op_frame_need==0 for the node, none of this applies -- havehome()'s ordinary spine arm fires as always. APPENDED AT STRUCT END per the s141 ABI law. */
     int                          op_fence_body_kk; /* ⭐ R-4(a) SLICE 2 (s95): staged IR_MATCH_FENCE1 body-span ΣK -- the sum of zd_k (THE ONE K AUTHORITY) over the committed body members [operands[0]..operands[1]] (same all[]-order containment idiom as op_arbno_body_kk / the DEFER-unsafe scan); the σ-commit release reads the watermark quad at FRQ(off+32+op_fence_body_kk) because a body-interior COMMITTED cell (a STANDING-slotted capture SAVE's own 16B carve, non-popping by THE MODEL until the bracket whack) leaves the frontier exactly ΣK below α's store on the SUCCESS edge -- H14 witness FENCE(LEN(2) . W): the as-edge `mov rsp,[rsp+176]` read 16B under α's quad and whacked RSP to garbage (movaps fault in libc under rt_dcap_end_ok_open; r12/r13 trashed by the corrupted MATCH_END frame). 0 for every non-FENCE1 node and for the K=0 body class (byte-identical by construction). APPENDED AT STRUCT END per the s141 ABI law. */
     int                          op_fence_frame_off; /* ⭐ R-4(f) SLICE 3 (s96, Fable 5; DESIGN OF RECORD Lon s95: only `S ? P` and `*P` establish frames -- FENCE1 pushes NO frame; its watermark is a BB RESULT value whose committed frontier at σ is DYNAMIC when the body holds a container (K16 ARBNO commits kk+16 per instance, ALT carves 32B on the taken arm), so no RSP-relative offset is correct): the FENCE1 watermark rides a SLOT IN THE CURRENT ACTIVATION FRAME [rbp+off] -- the third customer of the shared frame_slot_scan registry (fence_frame_candidate() container-in-body -> fence_frame_slot()); α `mov [rbp+off],rsp`, every release `mov rsp,[rbp+off]` (σ-commit whack + its rbp-unwind MARK, PAIR(3), β). -1 = not a candidate (K=0 / static-K bodies keep the RSP quad byte-identical). Witnesses probe/bb/probes/{H08,H29} m3. APPENDED AT STRUCT END per the s141 ABI law. */
+    int                          op_arbno_body_actframe; /* ⭐⭐⭐ R-4(a) ARBNO INTERIOR CHOICE POINT (s125): 1 = this IR_MATCH_ARBNO's body span contains an ACTIVATION-FRAME PUSHER (IR_MATCH_DEFER, whose β is written `mov rsp,rbp; pop rbp` = unwind MY OWN activation).  Stated BEHAVIOURALLY, no language name: the discriminator is whether the body carrier's β UNWINDS AN ACTIVATION or RESUMES THROUGH A LIVE SPINE RECORD.  Consumed ONLY by bb_match_arbno_frame()'s af edge.  WHY: s123 deleted that edge wholesale behind sn4_defer_resume() on the premise that "no instance retains any interior choice point today" -- MEASURED FALSE for a non-defer body (s125 gdb, witness probe/arbnostore/arbno_alt_inline_red): an ALTERNATE body leaves its 32B arm record FULLY LIVE at af ([rsp+0]=saved cursor, [rsp+8]=resume stub, [rsp+16]=next arm), and `add rsp,32` at the ALT's own exhaust stub lands rsp EXACTLY on the previous instance's record because every instance of one ARBNO is the SAME template hence the SAME record size.  The edge is therefore CORRECT for record-resuming bodies (manual Ch.18 step 6: pop the stack to the most recent alternative) and CATASTROPHIC only for activation-pushing ones (s123's rbp climb e8e8->e938->ea00->ea60->0).  Same body span and containment idiom as op_arbno_body_defer_unsafe; that field flags pat_static==0 defers only, this one flags EVERY defer, so neither is a re-spelling of the other.  APPENDED AT STRUCT END per the s141 ABI law. */
 } sm_emit_t;
 extern sm_emit_t g_emit;
 static inline int emit_patpin_probe(void) { static int v = -1; if (v < 0) { const char * e = getenv("SCRIP_PROBE_PATPIN"); v = (e && *e == '1') ? 1 : 0; } return v; }   /* ⛔ DIAGNOSTIC PROBE ONLY, DEFAULT OFF, NEVER A LANDING: re-admits flat_pat to the pin so the pre-DEL-T1 blob frame can be measured against HEAD in one tree.  Lon forbade the restore; this exists to SIZE the remaining D-2 debt, not to pay it. */
diff --git a/src/templates/bb_match_arbno.cpp b/src/templates/bb_match_arbno.cpp
index 76bf7c56..70d76c70 100644
--- a/src/templates/bb_match_arbno.cpp
+++ b/src/templates/bb_match_arbno.cpp
@@ -145,7 +145,7 @@ static std::string bb_match_arbno_frame() {
          + x86("def", PAIR(3))
          + x86("mov", "eax", RDD("rbp", off + 0))
          + x86("cmp", "r14d", "eax")
-         + IF(!sn4_defer_resume(), x86("jne", PAIR(1)))   /* ⭐⭐⭐ s123 THE OVER-POP EDGE.  af means "the body produced no further instance".  This edge said "so back one instance out via the body's beta" -- but the failing instance already returned through the fast-path glue `pop rbp; jmp <this>_af`, so ITS activation is gone, and the body's beta is written as "unwind MY OWN activation" (mov rsp,rbp; pop rbp).  It therefore eats the BLOB's saved caller-rbp, and each lap climbs one more caller frame: measured e8e8 -> e938 -> ea00 -> ea60 -> 0, then af reads [0-32] and SEGVs (gdb, witness 148).  Latent at default because beta never enters the ARBNO from outside; reachable the moment the s121 resume route lands, which is why the two are ONE killswitch.  Dropping it is CORRECT, not merely safe: ARBNO grows monotonically and never shrinks (manual p.121 `"" | PAT | PAT PAT | ...`), so exhaustion IS omega -- backing out instance-by-instance only ever existed to reach a previous instance's interior choice points, and no instance retains any today (that is R-4(a), witness probe/arbnostore/arbno_defer_altarg_red).  MEASURED +6/-0 by hand-patch before landing: 119/129/148/149 rc=139 -> PASS, 139/143 held, arbnostore 8/9 held. */
+         + IF(!sn4_defer_resume() || !_.op_arbno_body_actframe, x86("jne", PAIR(1)))   /* ⭐⭐⭐ s125 R-4(a): THE EDGE IS BODY-CONDITIONED, NOT KILLSWITCH-CONDITIONED.  af means "the body produced no further instance"; PAIR(1) is the body's β = its INTERIOR CHOICE POINT, which is exactly what manual Ch.18 step 6 says to pop to.  s123 deleted this edge wholesale on the premise that "no instance retains any interior choice point today" -- MEASURED FALSE (s125 gdb, `ARBNO('a'|'ab')` on 'ab'): an ALTERNATE body's 32B arm record is FULLY LIVE at af with [rsp+8] holding the pending resume stub and [rsp+16] the untried arm, and the ALT's own exhaust stub already did `add rsp,32` landing rsp on the PREVIOUS instance's record (same template every instance => same record size => contiguity by construction).  Deleting the edge therefore shipped a SILENT WRONG ANSWER on the non-defer body class (oracle match / SCRIP nomatch) as an s124 DEFAULT.  The s123 SEGV is real but belongs to ACTIVATION-PUSHING bodies only (IR_MATCH_DEFER: β = `mov rsp,rbp; pop rbp` eats the blob's caller-rbp, rbp climbs e8e8->e938->ea00->ea60->0, witnesses 119/129/148/149, all four `ARBNO(*cmd)`).  op_arbno_body_actframe is that discriminator, stated behaviourally (unwinds-an-activation vs resumes-through-a-live-record), and the killswitch is kept as the OUTER gate so SCRIP_DEFER_RESUME=0 stays byte-identical legacy.  ORIGINAL s123 NOTE FOLLOWS: */   /*  af means "the body produced no further instance".  This edge said "so back one instance out via the body's beta" -- but the failing instance already returned through the fast-path glue `pop rbp; jmp <this>_af`, so ITS activation is gone, and the body's beta is written as "unwind MY OWN activation" (mov rsp,rbp; pop rbp).  It therefore eats the BLOB's saved caller-rbp, and each lap climbs one more caller frame: measured e8e8 -> e938 -> ea00 -> ea60 -> 0, then af reads [0-32] and SEGVs (gdb, witness 148).  Latent at default because beta never enters the ARBNO from outside; reachable the moment the s121 resume route lands, which is why the two are ONE killswitch.  Dropping it is CORRECT, not merely safe: ARBNO grows monotonically and never shrinks (manual p.121 `"" | PAT | PAT PAT | ...`), so exhaustion IS omega -- backing out instance-by-instance only ever existed to reach a previous instance's interior choice points, and no instance retains any today (that is R-4(a), witness probe/arbnostore/arbno_defer_altarg_red).  MEASURED +6/-0 by hand-patch before landing: 119/129/148/149 rc=139 -> PASS, 139/143 held, arbnostore 8/9 held. */
          + x86_omega();
 }
 /*----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
