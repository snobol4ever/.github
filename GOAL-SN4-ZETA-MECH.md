# GOAL-SN4-ZETA-MECH — SNOBOL4 ζ-CELLS ON THE STACK: MECHANISM LADDER (FORTH-style RSP spine, RBP minimal)

## ⛔⭐⭐⭐ CROSS-GOAL BULLETIN — 2026-08-10 s12: LADDER WREG IS LIVE, r10/r11 ARE CLAIMED WIRE REGISTERS (owner-directed cross-post; authority = `GOAL-PASSTHRU-RBP-ERAD.md` LADDER WREG, `.github` `6de2dbff`)

**Lon runs 5 seats concurrently: PASSTHRU-RBP-ERAD + RTCC + ZETA-MECH + ZETA-CLIMB + SNOBOL4-RTX.** Charter, verbatim: *"remove the stupid PROC shim around patterns and use proper PASS-THRU glue using R10 and R11."* Pattern blobs stop being procedures: the `proc_PAT$N_α` shim (304B carve · γ/ω wire stores · SCANBASE fills · `g_zctx` push · resume-slot store) and its CLASS-D exit ceremony are DELETED; call sites enter the blob's FIRST INTERIOR BOX directly with `lea r10,[rip+site_γ] · lea r11,[rip+site_ω] · jmp <first_box>`; exits are `jmp r10` (success) / `jmp r11` (fail after drain). **rΓ=r10 · rΩ=r11 are RESERVED product-wide, all three glue kinds, ONE authority (a per-kind split is the mixed regime ZW16 convicted).** LAW: blob-interior pending cells capture/restore the pair at push/β, else it is `g_blob_ctx`'s single-cell defect in register clothing.

**WHY THIS IS YOURS: YOU OWN WREG-3.** The contract handed to you from PASSTHRU: scope = blob-interior pending cells ONLY (statement-inline cells pay nothing) · layout delta = +16B pair slot · capture {r10,r11} at push · restore at β BEFORE any blob code runs. Soundness rests on the drain law you own: cells pop in spine order, so crossing from blob B's pendings into blob A's restores A's wires before A executes. **Mechanics are yours** (cell allocation, ARBNO-iteration interaction, K-conversion residue); the PT-4 PREREQ (ARBNO/SPAN/BREAK K-conversion in blob context) still binds interiors. ⛔ **HIGHEST COLLISION SURFACE:** you are actively landing in the exact emit.cpp frame arms WREG-2 deletes — coordinate BEFORE WREG-2 cuts. `dc_recur` is expected to flip green under WREG-3 (M-1b's save-by-value, single-field form ×2).

**s12b MEASURED FLOORS (same container, BY SET — every seat's regressions are judged against these):** probe **33/5** m3 · **33/4** m4 · crosscheck/patterns **82/40** m3 · **78/44** m4 · broad-336 **270/66** m3 · **262/68/6** m4. Honest census Σest ≈ **263** (NOT the stale 557; crosscheck committed `.s` were pre-deletion — re-measure fresh, do not trust those artifacts). Runner semantics: `$(cat ref)` vs `$(cmd)` (ENV-TRAP (a)); namespace `/tmp` evidence per seat.

## ⚙️ CONCURRENT BY DEFAULT — AND THE REPOS MOVE UNDER YOU

**Many seats run this file's siblings at the same time. Edit any file, commit and push whenever a rung is buildable and green — mid-session, per rung. Never park work or decline an edit on concurrency grounds; stranding has cost this project far more than merging ever has.** Git merges; `git pull --rebase` and resolve normally.

**⛔ ASSUME ORIGIN MOVED SINCE YOU LAST LOOKED.** Another seat may have landed in your exact files while you were reading them.
- `git pull --rebase` before every push; **re-prove THIS file's gate/watermark after any rebase** — shared state moves under you and a watermark measured pre-rebase is void.
- `git log origin/main..HEAD` at orientation AND before handoff. **A clean `git status` is NOT a clean tree** — it hides local commits a peer seat left in a shared working copy.
- Place trees at canonical absolute paths (`/home/claude/{SCRIP,corpus,.github,x64}`) BEFORE running any gate: **many scripts grade a tree by absolute path.**
- Prefer **one clone per seat**; two seats in one working copy silently overwrite each other's uncommitted edits, and a global gitconfig scrambles attribution.
- Push **code repos before `.github`**, so no FINDING ever describes an unpushed tree.
- Push needs a credential — **ask Lon in chat and wait.** Never write push status into a doc.
- `bash scripts/handoff_status.sh` verbatim is the ONLY push truth. Not this file, not a commit message.

**Semantic collisions (two seats claiming one register) are caught MECHANICALLY by the claim gates, not by scheduling.** That is why no window is needed.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-11 s38 (Opus 5 — **ARBNO DEFECT RENAMED AND OWNED: IT IS THE LEGACY ROUTE, NOT THE BODY WIDTH** · s37's KILLSWITCH EXONERATION FALSIFIED AS VACUOUS · NO COMPILER CODE CHANGED)

**⛔ s38b HASH-CITATION CORRECTION (CONSOLIDATED-2, 2026-08-11):** all six s38b hashes (`7dfb803a` `560c63e4` + corpus ×4) are MISSING from origin, but the repair CONTENT is at HEAD rebased (`bb_match_arbno.cpp:45` `SCRIP_ARBNO_FPRPOP`). Watermark 160/1 stands for content, not those hashes. D12/D13 mechanism now fully named (blob γ/ω/res never restore rbp — view-continuity): see `GOAL-PASSTHRU-RBP-ERAD.md` CONSOLIDATED-2 + its FINDING; ruling (b) answered by Lon in the RETIRE direction, ruling (a) is subsumed by the PASSTHRU fork (a)/(b) now routed.

**⭐⭐⭐ s38b AT HANDOFF — A CONCURRENT SEAT LANDED THE REPAIR, AND s38 VERIFIED IT. NEW WATERMARK: m3 160/1/0/2 · m4 159/2/0/2 (`SCRIP_RTCC=0`) — REGRESSION {D12·D13} only.** While s38 worked, a **concurrent seat sharing this container's persistent working copies** committed SCRIP `7dfb803a` (+`560c63e4` regen) and corpus `0affc04e`/`e707f815`/`091d45cc`/`d24999fa` locally (00:30–01:01; authorship carries this seat's gitconfig because config is global — the two seats' work is interleaved in these hashes). **`7dfb803a` = TWO-CALCULATORS RETIRED: the N02-FIX β pop popped right-spine cells that ZD-5b const-wpop (emit.cpp:1448, THE ONE AUTHORITY since s32) had ALREADY popped — β arrived at element_base+16 and every header read was displaced** (its comment nails s38's m3/m4 asymmetry: m3 read an accidental 0 = correct first-instance delta, hence 1-passes-then-SEGV; m4 read 0x7fff from 1 instance). `SCRIP_ARBNO_FPRPOP=1` restores the double-pop for same-build bisect. **s38 verified from a fresh rebuild, raw both modes: m3 fails exactly {D12, D13, fence_probe}; m4 fails exactly {A06, D12, D13, fence_probe} — the `e707f815` pruned baselines are EXACTLY truthful, ~21 probes repaired across N/G/H/X incl. N32 (sq=0 alternation route!) and N33.** This VALIDATES the s38 route finding below (the legacy arm was the owner) and **answers ruling (b) de facto: REPAIR — Lon please RATIFY.** Ruling (a) — s37(B) push/pop guard for D12/D13 — REMAINS OPEN and is now the only regression pair standing. ⛔ **SEAT-ISOLATION HAZARD for RULES:** two seats raced ONE working copy (one seat's uncommitted XFAIL edits were silently overwritten by the other's commit; attribution scrambled by shared global gitconfig). Proposed rule: **one clone per seat** or serialize; at orientation ALWAYS `git log origin/main..HEAD` — a clean `git status` is NOT a clean tree. The s38 findings below were measured BEFORE the fix landed and stand as the pre-repair record; §5's ⛔ (K16 gate widening) stands unchanged.

**WATERMARK PROVED at open AND close (HEAD SCRIP `565ecfa8` / corpus `0affc04e`):** m3 **137/22/0/2** · m4 **135/24/0/2** (`SCRIP_RTCC=0`) — REGRESSION set **D12·D13 only**, identical open to close. ⚠ Raw counts differ from s37's `134/15`/`133/16` because the suite GREW 151 → 161 probes at corpus `0affc04e`; the **SET** matches, which is the mandated comparison. FINDING: `FINDING-2026-08-11-CLAUDE-OP5-SN4-MECH-S38-ARBNO-DEFECT-IS-THE-LEGACY-ROUTE-...md`.

**⛔ CURSOR HASH CORRECTION — s37's headline `7aa32a3d` DOES NOT EXIST IN ORIGIN** (`git cat-file -t` → not a valid object name). The landed commit is **`565ecfa8`**, which s37 addendum (A) names correctly; the two halves of that cursor disagreed and the headline was the wrong one. STALE-ORIENTATION (b) with a live hash — a session trusting it would rebase against a commit that was never pushed.

**⭐⭐ THE ARBNO DEFECT IS THE LEGACY ARM. RENAMED, BOUNDED, INSTRUMENTED, COVERED.** s37 called it *"ARBNO cannot supply ≥2 instances of a FIXED-WIDTH body; SPAN is the only kind that works."* Both halves of that name are wrong and the name hid the owner. Measured with the compiler's OWN diagnostic `SCRIP_ARBNO_DIAG=1` (prints `framed/k0/sq/kk/osv/route` per site — an excellent instrument the cursor never mentioned): **correlation 1.000, zero exceptions — every specimen routing `FRAMELESS_K` is CORRECT; every specimen routing `legacy` is WRONG or SEGVs.** N27 SPAN `k0=0 kk=16 sq=1` → FRAMELESS_K ✅ · N28 lit `k0=1 kk=0` → legacy ❌ · N24 bare lit → legacy 💥 · N33 ANY `k0=1` → legacy ❌ · N32 ALTERNATION `sq=0` → legacy 💥. **Body kind is NOT the defect axis — it is the INPUT to the routing gate `emit.cpp:955` `if (_sq && !_k0 && _kk > 0 && !_fr && !_osv)`.** SPAN carries `zd_k != 0` and clears the gate; literal/LEN/ANY carry `zd_k == 0` ⇒ `_k0=1` ⇒ declined; an ALTERNATE in the body span sets `_sq=0` ⇒ declined by a **DIFFERENT CLAUSE**. "Fixed width" is a coincidence of which leaves happen to carve and does not predict N32. **Three things invisible under the old name:** (1) **the manual's OWN p.122 example `PAIRS = POS(0) ARBNO('AA'|'BB'|'CC') RPOS(0)` on `'CCBBAAAACC'` SEGVs** — a documented tutorial pattern, no probe covered it; (2) the instance-count boundary is ROUTE-dependent, not "≥2" — on legacy the CONCAT form fails at **≥1** (`POS(0) LEN(1) ARBNO(',' LEN(1)) RPOS(0)` on `'a,b'` → `=F`), while on K16 instance count is IRRELEVANT (SPAN-tailed body green at 0,1,2,3,4); (3) the repair target is the ARM, not the leaves.

**⛔⛔ s37's KILLSWITCH EXONERATION WAS VACUOUS BY CONSTRUCTION — THE INFERENCE, NOT JUST THE RESULT.** s37: *"Four killswitches EXONERATED (byte-identical both settings) … the K-conversion arms are not the owner."* Measured: `SCRIP_ARBNO_K16=0` on the FAILING probes N24/N28 is byte-identical (`e1af30f380e5`/`77b5d891eb3f` both settings) — but on the PASSING control **N27 it DIFFERS (`bf5cc8323bc7` → `fc049c8c4a09`) and flips `=S` → `=F`.** The switch is LIVE; it *manufactures* the defect on a healthy specimen. It was measured on specimens **already in its OFF state** (`k0=1` ⇒ never on the K16 arm), so byte-identity was **guaranteed a priori and carried zero information**. The bytes were read correctly; the conclusion drawn from them pointed s38 away from the arm that IS the owner. **METHOD RULE (adopt): a killswitch A/B is VACUOUS unless the specimen is demonstrably ON the arm the switch disables. Prove residency FIRST (`SCRIP_ARBNO_DIAG=1` settles it in one run), then A/B.** Same shape as the FINDING-07-30 "vacuous by symmetry"/"by volume" convictions: the probe ran and could not have moved.

**MINTED (corpus, goldens + XFAIL baseline in the same commit).** N22–N31 were already minted by another seat at `0affc04e` — cursor item (1) is DONE, and the census re-run was avoided by checking first. But **all ten reach legacy via `k0=1`; NONE drove `_sq=0`**, so the alternation entrance stayed dark. Added **N32** (ALTERNATION, manual p.122 PAIRS verbatim — enters via `sq=0`) and **N33** (ANY body, `k0=1`, differs from the passing control N27 ONLY in leaf kind = the tightest control pair available). `mkrefs.sh --verify` → **163 current / 0 drifted**. `SUITE.md` N-row 31 → 33, now naming which entrance each row exercises.

**⛔ EXPERIMENT RUN, MEASURED, REVERTED — DO NOT RETRY THE ONE-LINE GATE WIDENING.** The arm's own comment claims it *"widens ARBNO-LON past body-K0"* while its gate literally requires `!_k0`, so admitting `kk==0` was the obvious repair. Implemented behind a default-off `SCRIP_ARBNO_K16_WIDE`, built clean, measured: **N28 `=F` → ABORT (strictly worse), N24 SEGV → SEGV (no movement). Reverted; tree clean, behaviour re-verified after rebuild.** The K16 arm re-homes at a σ junction using a one-level-in static offset **equal to `kk`**; at `kk==0` that offset collapses onto the current cell and the commit/retract arithmetic degenerates. The arm is **structurally** dependent on `kk>0`, not accidentally gated. A K0 body needs the arm to carve a 16B body-stride cell **on the body's behalf** — a real design rung with a layout delta, not a predicate edit. No killswitch left behind: a known-broken switch is exactly the M-5 debt this ladder deletes.

**NEXT ENTRY POINT.** ⛔ **TWO LON RULINGS PENDING, both cheap:** (a) **s37(B) push/pop guard alignment for D12/D13 — UNCHANGED, still the sole REGRESSION set, deliberately untouched here** (bolting a compensating pop onto one exit is the two-calculators disease); (b) **NEW — the legacy ARBNO arm: REPAIR it, or RETIRE it by teaching the K16 arm to carve a body cell for K0 bodies?** §5 rules out the predicate edit, so this is a LAYOUT decision and it is the WHOLE ARBNO class. **Do NOT re-derive the route table or the vacuity proof** — both measured, each reproducible in one command. If the ruling is RETIRE, the rung is: K16 carves a 16B body-stride cell when `kk==0`, σ junction reads it at the uniform offset, gate becomes `_sq && !_fr && !_osv`, measured against N22–N33 as the ready-made witness set; the `sq=0` alternation entrance is a SEPARATE, LATER rung (path-dependent bodies are genuinely harder; N32 is its witness). **Regen ×3 NOT owed this session — emit.cpp was reverted, zero codegen bytes changed.**

## ⭐⭐ s37 (Opus 5 — H31 REPAIRED BY ZCTX · M-1b LARGELY LANDED · OVERFLOW FALSIFIED · ARBNO defect first sighted, misnamed — see s38)

**WATERMARK PROVED at open (HEAD SCRIP `bce9a4b0` / corpus `bea31de0`):** m3 **134/15/0/2** · m4 **133/16/0/2** — REGRESSION set is now **D12·D13 only**. **H31 IS GREEN BOTH MODES** (+1 pass each vs s36's 133/15/0/3 · 132/16/0/3 BY SET). FINDING: `FINDING-2026-08-10d-CLAUDE-OP5-SN4-MECH-S37-...md`.

**⛔ s36's M-1b PRESCRIPTION IS LARGELY OVERTAKEN — DO NOT RE-RUN ITS READER CENSUS.** `0970838f` (ZCTX BASE STACK, Fable s11) DELETED `g_blob_ctx` and replaced it with `g_zctx[66]` (`[0]`=depth · `[1]`=current base · `[2+i]`=spill; push α+β, pop γ+ω; both media). `grep -rn 'g_blob_ctx\|rt_blob_ctx_ptr' src/` == 0 outside the comment. **M-1b Steps 1–3 are done in substance.** ⚠ The landed shape is a STACK, not s36d's zero-global design — **Lon ruling needed**: does the base stack satisfy s36d, or is it an intermediate that still owes the per-box-cell form?

**D12/D13 — DISCRIMINATOR SURVIVED FALSIFICATION, WITH NEGATIVE CONTROLS ON EVERY NEIGHBOURING AXIS.** Both SEGV both modes. Ablation (all oracle-checked): construct-only PASS · top-level defer w/ non-recursive ITEM PASS · recursive ITEM w/ top-level `LIST` direct **SEGV** · recursive ITEM **without ARBNO** PASS · non-recursive `*Q` inside ARBNO PASS · ARBNO-owning blob activated inside ARBNO **without self-recursion** PASS. ⇒ It is NOT defer, NOT ARBNO, NOT nesting, NOT blob-in-blob. **It is: an activation of blob A beginning while an activation of A is still live.** ARBNO only supplies the backtracking pressure that forces the recursive alternative while the outer activation is open.
**⛔ ZCTX OVERFLOW IS FALSIFIED** — a depth-graded D12 series crashes at depth 1; capacity 64 is never approached. Residue = **push/pop ASYMMETRY on a failure exit** (entered, drained by a route that does not pop ⇒ `[0]`/`[1]` desync ⇒ outer re-bases off a foreign base). Next: gdb the CLASS-D γ (`emit.cpp` ≈2808) / ω (≈2827) pops against the α push (≈2375). CRASH ⇒ gdb-reachable per RULES §1.

**⛔⛔ SUPERSEDED BY s38 — READ THE s38 CURSOR INSTEAD. Two claims below are FALSE: (i) the defect is NOT "fixed-width body" (ALTERNATION bodies fail too, incl. the manual's own p.122 PAIRS; the real axis is the `legacy` vs `FRAMELESS_K` ROUTE); (ii) the killswitch EXONERATION is VACUOUS — `SCRIP_ARBNO_K16` is LIVE and flips the passing control N27 `=S`→`=F`; it was A/B'd on specimens already in its OFF state. Boundary "2+" is route-dependent, not general. Retained verbatim for history only.**
**⭐⭐ [s37, SUPERSEDED] RANKS ABOVE D12/D13: ARBNO CANNOT SUPPLY ≥2 INSTANCES OF A FIXED-WIDTH BODY (no blob, no defer, no recursion; BOTH MODES).** `'abab' ? POS(0) ARBNO('ab') RPOS(0)` → oracle `=S`, SCRIP **SEGV**. Boundary exact: 0 instances PASS · 1 PASS · **2+ SEGV**. Body-kind: **SPAN is the ONLY kind that works** — literal/literal, LEN/LEN, literal/LEN, LEN/literal all give a **SILENT WRONG ANSWER** (`=F` on a matching subject) in the concatenated form `X ARBNO(',' Y)`. Four killswitches EXONERATED (byte-identical both settings): `SCRIP_ARBNO_K16`, `_LATCH`, `_FRAMELESS`, `SCRIP_ZPOP_FOLD_OFF` — the K-conversion arms are not the owner. **⛔ THE 141-PROBE GATE IS DARK TO THIS: every ARBNO probe uses a SPAN body (X07 = manual p.121 shape, PASSES).** Suite-coverage hole, not just a bug. **MINT THE PROBES FIRST** (five instance-count rows + five body-kind rows, repros in the FINDING §3), then hunt. Independent of the ζ mechanism ⇒ NOT blocked behind M-1b. [s38: probes minted — N22–N31 by another seat at `0affc04e`, N32/N33 by s38; all ten of N22–N31 enter legacy via `k0=1` and none drove `sq=0`.]

**NEXT ENTRY POINT (batched per OPS-4):** (1) mint the ARBNO witnesses into `corpus/probe/bb/probes/` + XFAIL baseline in the same commit; (2) hunt the ARBNO ≥2-instance owner (killswitches exhausted — gdb the SEGV form, it is the loud twin of the silent one); (3) gdb the ZCTX pop asymmetry for D12/D13; (4) route the s36d-vs-base-stack ruling to Lon.

**⛔ s37 ADDENDA (Opus 5, MECH rung M-1c landed partial — SCRIP `565ecfa8`):**

**(A) ZCTX kt-dependence FIXED (`565ecfa8`).** The base became per-activation at `0970838f` but the *offset* did not. Every reader computed `[g_zctx[1] + its OWN kt − 24/16/32/40]` against whichever activation was on top. D12 emits carves 64/80/128/224 — PAT$0 (kt=128) stores γ at `[rsp+104]`; PAT$1 (kt=224) stores at `[rsp+200]` — so a PAT$0 stub with rax → PAT$1's carve reads 104 into a 224-byte region → 0 → `jmp 0`. Fix: α publishes `hdr = base+kt−40`; quad at fixed `hdr+0/+8/+16/+24`; ω unwind `lea rsp,[hdr+40]` ≡ `base+kt`; own base stashed in the reclaimed `[kt-8]` dead pad = `hdr+32` for the retry whack. **Not the s36-rejected trap** — that deref'd a *foreign* activation needing `parent_kt`; this is the activation's *own* base at a kt-free offset. gdb-verified: `hdr−base = 0xB8 = kt−40` at kt=224. **0 REG · 0 repair · both modes.** (A prerequisite, not the cure — D12/D13 still red, per (B).)

**(B) PUSH/POP GUARD ASYMMETRY — THE SURVIVING D12/D13 DEFECT, ROUTED TO LON.** After (A), gdb shows `g_zctx[1]` is handed a bogus pointer: depth=1, "header" holds `0x0000000600000006` in the wire slot, base implies kt=608 — never a header, just reused stack. Static push/pop count balances (2/2, both media); the **guards do not**: push = `if (g_emit.flat_jmp_entry)` (emit.cpp:2373); pop = `_blob_wire = !_wire_stub && flat_jmp_entry && flat_pat` (emit.cpp:2806). Any `flat_jmp_entry` graph outside that intersection **pushes and never pops** = POP DEBT (LIFO law hazard (b)). `emit.cpp:2955` sets `flat_jmp_entry=1` for a non-`flat_pat` class, making it structural. ⛔ **LON RULING NEEDED: which guard to align?** CLASS-O and `_wire_stub` exits are the other party. Do NOT bolt a compensating pop onto one exit — that is the two-calculators disease ONE-SYSTEM exists to kill. Once ruled, the fix is a one-line guard change, both media, measured against D12/D13 + full 151 both modes.

**(C) RTCC DEFAULT-ON (upstream `c4cb8813`) IS NOW LIVE AND EXPOSES THE WREG GAP.** Fable s13 flipped `g_rtcc_on` from 0 to 1. The commit's own comment: *"at OFF a blob's wires are clobbered by plain SysV rules and the failure is a wild jump, not a wrong answer."* **Measured:** m4 with RTCC=ON (default) → 3/16/0/**132** REGRESSION; with `SCRIP_RTCC=0` → 133/16/0/2, watermark fully restored. This is not a regression from this session. **WREG hasn't landed** — there are no `lea r10` call sites anywhere in the templates. RTCC-ON is correct and safe only after WREG is in place. ⛔ **M-1c (push/pop guard fix + WREG-3 capture/restore) is now a BLOCKER for the default-on RTCC path**. Every seat running m4 without `SCRIP_RTCC=0` will see 130+ regressions until WREG lands.

**(D) THE CLAIM GATE HAS A HOLE.** `scripts/test_gate_rtcc_claimed_regs.sh` — RULES's "replacement for the window" — hardcodes **r9 only** and runs **INFORMATIONAL** by default (`pass --strict to enforce`). It cannot see an r10/r11 collision. Route to RTCC to widen the claimed-register set to a data structure and enforce strict. The WREG r10/r11 landmine in the six ZCTX sequences (confirmed in this session's source read) is exactly what that gate should catch mechanically.

## ⭐⭐ s37 (Opus 5 — ZCTX KT-DEPENDENCE DELETED, LANDED `565ecfa8` [⛔ this heading formerly read `7aa32a3d`, WHICH DOES NOT EXIST IN ORIGIN — corrected s38]; D12/D13 RESIDUE IS A SECOND DEFECT: ASYMMETRIC PUSH/POP GUARDS)

**WATERMARK PROVED at open AND close (HEAD `bce9a4b0`, 13 commits past s36's `a5533659`):** m3 **134/15/0/2** · m4 **133/16/0/2** — BY SET identical open to close. **+1 pass both modes vs s36: H31 was repaired by `0970838f`.** FINDING: `FINDING-2026-08-10d-CLAUDE-OP5-SN4-MECH-S37-ZCTX-MOVED-THE-BASE-BUT-NOT-THE-OFFSET-...md`.

**⛔ s36's M-1b PRESCRIPTION IS OVERTAKEN — `g_blob_ctx` IS ALREADY GONE.** `0970838f` (Fable s11) replaced it with `g_zctx[66]`, a depth-counted base stack (push α+β, pop γ+ω). `grep -rn 'g_blob_ctx\|rt_blob_ctx_ptr' src/` == 0. M-1b Steps 1–3 are DONE by another seat; what survives of M-1b is Step 4 (POP MODEL) and Step 5 (GATES).

**DEFECT ZCTX DID NOT FIX, NOW FIXED (`565ecfa8` — this line formerly read `7aa32a3d`, corrected s38):** the base became per-activation; the OFFSET stayed compile-time-local. Every reader computed `[g_zctx[1] + its OWN kt - 24/16/32/40]` against whichever activation was on top. D12 emits carves **64/80/128/224** (PAT$0 kt=128 stores γ at `[rsp+104]`; PAT$1 kt=224 at `[rsp+200]`), the two blobs mutually recurse, so a PAT$0 stub reads 104 into a 224 carve → 0 → `jmp 0`. **ONE mechanism explains both sides:** correct iff all live activations share a kt (16/16 single-activation green, H31 repaired); wrong otherwise. Same objection s36 raised against a `[kt-8]` LINK — it binds a base deref'd with a local kt equally. **FIX:** α publishes `hdr = base+kt-40`; quad at fixed `hdr+0/8/16/24`; ω unwind `lea rsp,[hdr+40]` ≡ `base+kt`; α stashes its own base in the reclaimed `[kt-8]` pad = `hdr+32` for the retry whack. ⛔ NOT the `[kt-8]` trap — that deref'd a FOREIGN activation needing `parent_kt`; this is the activation's OWN base at a kt-free offset. Both media, 8 arms, gdb-verified header (`hdr−base = 0xB8 = kt−40` at kt=224), **0 REG / 0 repair both modes.**

**⭐ NEXT ENTRY POINT — RUNG M-1c: MAKE THE ZCTX PUSH AND POP GUARDS ONE PREDICATE.** Static counts balance (2/2, both media); the guards do not: push = `if (g_emit.flat_jmp_entry)` (emit.cpp:2373) · pop = `_blob_wire = !_wire_stub && flat_jmp_entry && flat_pat` (emit.cpp:2806). Every `flat_jmp_entry` graph outside that intersection **pushes and never pops** = POP DEBT (LIFO law hazard (b)); emit.cpp:2955 sets `flat_jmp_entry=1` for a non-`flat_pat` class, so it is structural. Measured: the faulting "header" held `0x0000000600000006` in the wire slot and a base implying kt=608 — never a header, just reused stack. ⛔ **Which side to align is a DESIGN CALL (the CLASS-O/`_wire_stub` exits are the other party) — ROUTE IT, do not bolt a compensating pop onto one exit; that is the two-calculators disease ONE-SYSTEM exists to kill.** Witnesses D12/D13 (probe), both modes.

**⛔ WREG LANDMINE — LADDER WREG IS NOT IN THE CODE.** No `lea r10` at any call site; live glue is still `bb_glue_pass_wires` = `lea rcx,γ + lea rdx,ω + jmp rax`. But all six ZCTX sequences use **r10/r11 as scratch**. The moment WREG-3 lands they destroy rΓ/rΩ. s37 deliberately did NOT re-allocate them (scope) — it is live-on-arrival for whoever lands WREG-3, which the bulletin assigns HERE.

**⛔ THE CLAIM GATE HAS A HOLE.** `scripts/test_gate_rtcc_claimed_regs.sh` — RULES's "replacement for the window" — knows **r9 only** and runs **INFORMATIONAL** by default. It cannot see an r10/r11 collision. If it is load-bearing for 5 seats, the claimed-register set must be DATA and it must run strict. Cross-goal: RTCC owns the gate.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-10 s36 (Opus 5 — ROOT CAUSE NAMED · s35 BISECT VOIDED · 08-10b PREMISE FALSIFIED · REPAIR IS SAVE-BY-VALUE, `[kt-8]` IS A TRAP; NO CODE WRITTEN)

**WATERMARK RE-PROVED at open AND close (HEAD `a5533659`, 3 commits past s35's `c7e085fd`):** m3 **133/15/0/3** · m4 **132/16/0/3** — REGRESSION set D12·D13·H31 both modes, digit-identical to s35 on a different container. ⭐ **m4 now measured** (both 08-10b findings were m3-only). FINDING: `FINDING-2026-08-10c-CLAUDE-OP5-SN4-MECH-S36-DEPTH-NOT-BLOB-PRESENCE-...md`.

**⛔ DO NOT RUN s35's BISECT — IT IS VOID.** `git bisect a5c22641..main` (~6 builds on a 1-CPU container) is unnecessary. The DEL-T1 trio (`1af93e3a` D-1 · `1f96143c` D-2 · `ef8a3052` D-3) was already convicted by two parallel seats on 08-10b, and s36 confirmed the mechanism from source at HEAD. s35's "capture-δ interior" framing was inherited from the **s9 cursor, which 08-10b had already falsified**.

**⛔ "EVERY BLOB-BEARING PROGRAM FAILS" IS FALSIFIED.** D10 (18 `proc_PAT` defs, 6 `g_blob_ctx` refs) and D11 PASS in **both** modes. Constructed oracle-compared witnesses separate count from depth: `W3_selfrec` — ONE stored pattern, the *same 6-ref footprint as the passing D10* — HANGS because it re-enters. **The discriminator is DYNAMIC ACTIVATION RE-ENTRANCY — not blob presence, not blob count, not static structure.** Falsification attempted and survived: the 3 nested-footprint survivors (`113` side-by-side `*cmd1 *cmd2`; `181` four independent patterns; `118` where `outer` is assigned AFTER the match so `*outer` defers to null and the inner blob never activates) are all dynamically depth-1. Both proxy-error directions follow: `066`/`115` are static count 1 but re-entrant (broken); `118` is static count 2 but depth 1 (green). ⛔ Any census ranking by STATIC blob count misclassifies in both directions — rank by re-entrancy. Corpus stratification (122 pattern programs, within-HEAD, no second build): no-blob 20/70 broken (background) · single-activation 2/18 · **nested 31/34 (91%)**; the 2 single-activation casualties are `066_..._nested` / `115_..._recursive`, both re-entrant at count=1. ⭐ **True non-re-entrant blob programs: 16 of 16 GREEN — single-activation pass-thru WORKS.**

**ROOT CAUSE (read from source at HEAD, not inherited):** the per-activation spine cell is ALREADY CORRECT — α carves `[rsp+kt-24/-16/-32/-40]`. The defect is the **process-global mirror** `g_blob_ctx[5]` (`pattern_match.c:624`): every reader re-bases off the GLOBAL, never off its own cell — scanhit `2734` · scanfail `2738` · β res-stub `2763/2773` · CLASS-D γ `2815` · CLASS-D ω `2832`, each `mov rcx,[rdx+0]`. Nesting ⇒ inner α clobbers the mirror ⇒ outer γ/ω re-base off a foreign activation. Line 624's own comment licenses the cell only under a **"NON-NESTING assumption, TT_DEFER excluded"**, while the gate `emit.cpp:2358` keys on `flat_pat` alone and never tests it. D12's `*LIST` IS `TT_DEFER`. Manual Ch.9 p.123 + p.122 (D12 is that example verbatim): the matcher is a **pushdown**; one cell cannot be a stack.

**⭐ s36d — read M-1b's LON MODEL RULING first: the blob header LAYER is deleted, not rethreaded; nothing special about `*name`; per-box wires in per-box cells + pop-scan drain + record-carried β + statement-frame scan state.**

**⭐ NEXT ENTRY POINT — RUNG M-1b, now GLOBAL-EXECUTE (⛔ Lon ruling s36c SUPERSEDES the s36b save-bracket: `g_blob_ctx` is DELETED, not bracketed — no globals for activation state, and no re-spelling of one). The base threads through record[+8] (already carries it) + the wires/spine per the rung's Step 1 census. The save-bracket text below is retained as the FALLBACK ONLY if Step 1 finds an unreachable site AND Lon re-rules. SAVE-BY-VALUE ON THE SPINE (superseded fallback). ⛔ `[kt-8]` IS A TRAP — DO NOT USE IT.** The dead pad invites a dynamic link; **measured and rejected twice over:** (a) the readers consume FIVE words, `[kt-8]` is one slot; (b) **`kt` is not uniform** — one program emits carves of 64/80/128/224 (D12) and 64/80/144/160/176/192 (H31) — so a parent's header at `[parent_base+parent_kt-24]` is **unreachable**, `parent_kt` being unknowable at the restoring blob's compile time. A link/pointer CANNOT be dereferenced. **Correct design:** (1) α, before the existing carve: `sub rsp,48` (48 not 40 — 16-byte alignment; licensed raw-rsp per LAWS) + store `g_blob_ctx[0..4]` → `[rsp+0..32]`; then the existing `sub rsp,kt` + fills + publish, leaving the save block at `[base+kt .. base+kt+32]`. (2) γ-suspend · ω-total-failure · β-res-stub each already load the base via `mov rcx,[rdx+0]` — add `g_blob_ctx[0..4] ← [rcx+kt .. rcx+kt+32]`. **Every site uses its OWN kt and OWN base; no cross-blob knowledge.** Register-free, D-1's charter intact. ⚠ **Price first:** the +48 shifts the spine — interior `rsp+op_flat_disp` readers are base-relative (unaffected), but ω drain-to-entry-depth and any ΣK ZPOP fold must absorb it; check MODEL-THE-POP before trusting a green suite. **BOTH MEDIA MANDATORY, ~8–10 arms.** Prove with D12/D13/H31 + `W2_nest`/`W3_selfrec`, then full 151×2, then the Icon 303-emission content-hash sweep (COORDINATION: any emit.cpp edit), then regen ×3 (OWED, NOT-CONCURRENCY-SAFE — Lon routes).

**⛔ ROUTING — THE REVERT CALL HAS CHANGED.** The 08-10b seat recommended reverting the DEL-T1 trio on the premise that no blob entry works. That premise is falsified: reverting hands back `mov rbp,rsp` in EVERY blob to fix a defect confined to the re-entrant class, discarding 16-of-16 working pass-thru. **Recommend fix-forward via the dynamic link; revert is the fallback if `[kt-8]` cannot hold the link.** Lon's call.

**STILL UNROUTED (s35 raised it, unchanged):** `GOAL-PASSTHRU-RBP-ERAD` is absent from PLAN.md Active Goals and from the CONCURRENT SET (9) while landing commits touching `lower_snobol4.c`.

## ⭐⭐ s31 (Sonnet 4.6) — N21 LANDED; XFAIL rebased; SCRIP `eaa2b8ec`, corpus `0eca14dc`. POS/RPOS const-arg orphaned 16B cell in frameless ARBNO body → op_wpop+=16 guarded by ZC_PORT_FORTH+g_zd_arm+!fc_geom. m3 133/17 · m4 131/19. [promoted to ONE-SYSTEM authority this session]

## ⛔⭐⭐⭐ LADDER ONE-SYSTEM (Lon ruling 2026-08-09) — ONE ADDRESSING AUTHORITY; the 24 fc_* + 362 terms + FR/trd/RBPRAWD locals all consolidate or die
**DESIGN:** one function, one home (extend `zeta_storage.c` — it already owns the graph + zls layout; contract in `contracts/`): `addr_of(graph, producer, consumer, cell) → {mode, base, disp}` with mode ∈ RSP_STATIC (authority PROVES no dynamic extent {ARBNO, DEFER-blob, ALT-retry, MATCH-re-entry, ACTIVATION} between producer and consumer — IR-checkable) · PIN (extent-α rbp, [pin+k]) · PUBLISHED (producer lea's its cell into a fixed home at its OWN depth; consumer reads through). One calculator cannot disagree with itself: producer and consumer ask the SAME oracle, so the entire mismatch class is unconstructible; carry-never-predict becomes an INTERNAL arm of the authority, not a law templates must remember. Both media by construction (register-relative or published — never absolute).
- [ ] **OS-0 CENSUS + CONTRACT** — enumerate every address-computing site (24 fc_*, emit.cpp op_* staging, zls_off/zvo_resolve, FR/FRQ/x86_fb_pinned, trd/RBPRAWD/HDRA, flat_frame_*, fpr_rsp); freeze the API + mode enum + dynamic-extent set in `contracts/addr.h`; DIAGNOSTIC DUMP mode (per-read answer) is part of the contract — it is the instrument that supersedes most of OPS-2's monitor surgery (an address-mode diff beats a stdout diff).
- [ ] **OS-1 AUTHORITY LIVE, DARK** — implemented in zeta_storage.c; answers logged beside every legacy calculation on the 141-probe corpus, zero emission change; divergence report = the work queue ranked.
**⭐ LON AMENDMENT (2026-08-09, binding):** capture anchor = the **enclosing PATTERN-MATCH ACTIVATION frame**, NOT the statement — per-activation by construction (shared pattern objects, *LIST re-entrancy). MATCH_BEGIN's frame for top-level; the blob's OWN frame for blob-resident captures. COND nuance: cursor cell is frame-local, the pending store still escalates to the outermost match's success trail (`.` fires on whole-match success). Modes final: PIN=match-activation anchor · PUBLISHED=extent base (ARBNO chain link) · RSP_STATIC=extent-free direct calc.
- [ ] **OS-2·SLICE-1 — IMM/COND ANCHOR (land-ready spec; witnesses: os1_runtime_k, D05, D06, stitch trio, xc318):** in emit.cpp IR_MATCH_ASSIGN_IMM + _COND: **DELETE both `_fb`/`_adj` compensation blocks whole** (the `rt_proc_get_fn`/`rt_fn_frame_bytes_known` dance — malformed per DYNAMIC-DEPTH, unreachable for *VAR anyway). Route SAVE-cell READ **and** WRITE (producer symmetric — the X/Y/Z lesson) through the match-activation rbp arm (MATCH_BEGIN pins it: flat_pat prologue; accessor = pinned FR / x86_frame_off_* family — seat verifies which arm is depth-immune, PB-2's `x86_frame_off_rsp` is the anti-pattern to avoid here, it is the RSP arm). Blob-resident captures: verify PATCTX-in-blob-frame (s15 CLIMB finding) already gives per-activation homes; if reads still rsp-form, same edit inside blob emission. Gate: os1 runs oracle-identical (a/abcd/=S, no ZHP abort), D06 flips, D05 stays, 141-probe 0-REG both modes, XFAIL shrinks same commit. SLICE-2 = ARBNO published extent-base (D09/D11, headers via chain-link lea). SLICE-3 = N08/N09 (element-interior writes to match-frame cells, covered by slice-1 rule at ARBNO scope).
- [ ] **OS-2..n MIGRATE BY FAMILY, DELETE SAME SLICE** — value spine → captures → ALT → ARBNO → DEFER → FENCE → functions; per slice: family's consumers switch to the authority, the family's LOCAL calculator is DELETED in the SAME commit (killswitch per slice; XFAIL may transiently grow behind it — minimal-diff SUSPENDED for this ladder, the license). Open D06/D09/D11, N08/N09/N16/N21, H21/H29 land as their family's slice, never as point fixes.
- [ ] **OS-FINAL** — external `fc_*` callers == 0 · compensation-term grep in emit.cpp dispatch + templates == 0 · cross-extent `[rsp+computed]` gate green · 141-probe + xc318 ≥ floor BOTH modes · regen ×4 · FINDING.
**NEXT (reordered by bottleneck):** 0) **OS-2·SLICE-ARBNO-FPR — N21/t6m FPR_RSP frameless fix** (root-cause fully diagnosed s31; `fc_frameless_fpr_rsp` in zeta_storage.c + `op_frameless_fpr_rsp` in emit.h + staging in emit.cpp + `IF(fpr>0,add rsp,fpr)` at frameless β; gate: N21 XPASS, run_suite 0-REG both modes, regen ×3). 1) **OPS-2 MON-CAP** (unblocks the D-class, largest family both modes). 2) CLIMB works the attackable set NOW with live instruments: N08→N09→N16 (ARBNO outer capture — statement-level divergence, monitor CAN see it), then H29 (CRASH = gdb-reachable per RULES), H21. 3) A06-m4 bisect. 4) D06/D09/D11 only AFTER OPS-2. Then M-2 remaining as ladder-ordered.

## ⛔ LAW — LIFO-EXTENT / DYNAMIC-DEPTH (Lon ruling 2026-08-09)
Match-time extents are STRICTLY LIFO: nothing on the ζ spine outlives anything pushed before it, and the retreat releases in exact reverse push order (the transit guarantee). The FORTH spine is correct BECAUSE this holds. The hazard class is therefore never "non-LIFO lifetime" — it is (a) **DYNAMIC DEPTH**: a cell's rsp-distance varies per activation/element (ARBNO element N base = element1_base − (N−1)×op_sb; DEFER blobs enter at foreign depth), so any STATIC `[rsp+k]` or rbp-derived read computed for one depth is wrong at another (SAME OFFSET ≠ SAME OBJECT); and (b) **POP DEBT**: a failure path that skips a pop the success path owed (BUG-2's 16B right-spine residue) breaks the LIFO invariant the design is entitled to assume. Diagnose every stack defect as one of these two; a theory phrased as "the lifetime isn't LIFO" is malformed on arrival.

## ⛔⭐⭐⭐ LADDER OPS — INSTRUMENT + THROUGHPUT BOTTLENECKS (opened s29 per Lon: "find the bottlenecks and put operations in place")
- [x] DONE — OPS-1 (ee8707d8)
- [ ] **OPS-2 · MON-CAP — monitor sees stdout-only divergence** — the D06 lesson (CLIMB S23 ANTI-PATTERN §1): all trace events agree, exit 0, yet stdout differs — the mandatory instrument is BLIND to the class currently blocking C-6/D-family. Add capture-level events (per-statement OUTPUT/capture writes) to the 2-way sync-step protocol so a stdout-only divergence brackets between two events and the monitor exits nonzero. Gate: D06 divergence BRACKETED (statement named) by the monitor before any D-class code is read; RULES §1 blind-class clause satisfied.
- [ ] **OPS-3 · CREDENTIAL-ASK AT HANDOFF** — RULES 6b landed s29 (behavioral, the s19–s26 conviction: 8 sessions stranded, BUG-7 re-derived). Gate: zero future cursors/commits containing "push pending"; every handoff transcript shows the ASK; verified each rebase.
- [ ] **OPS-4 · SESSION BATCHING** — the fixed per-session tax (orientation + open/close watermark ×2 modes + regen + rebase reconcile) is amortized only by MULTI-RUNG sessions: default plan ≥2 rungs or 1 rung + 1 OPS item per session; single-bug sessions only when the bug is the session (Lon routes exceptions). Gate: cursor NEXT lists a batch, not one item.

## ⛔⭐⭐⭐ RUNG M-SLEN — LON DIRECTIVE (routed 2026-08-08; standing order ≥2 months old, finally executed — the s220 "NEEDS LON'S ROUTING" park was directive-tracking rot): POPULATE DESCR.slen AT ASSIGNMENT + MATCH_BEGIN = REGISTER-ONLY, ZERO C CALLS
**End state (Lon verbatim intent):** MATCH_BEGIN reduced to "a minimum of register manipulation and NO C call" — save/restore bracket {RBP where deep, R12 CAS discipline, R13/R14/R15 outer Σ/δ/Δ} + `mov r13,[cell.s]` · `mov r15d,[cell.slen]` · `xor r14d,r14d` · CAS sentinel. `rt_match_enter` DELETED (its five jobs: gen bump=dead post-CAPGEN-ERAD · dcap init=relocated · coercion/VARVAL=lowerer spine · Σ/Σlen mirror=inline stores→RTCC block · LENGTH=slen field). Exits likewise: `rt_match_ctx_restore` call replaced by two inline Σ/Σlen mirror stores; both C bodies tombstoned (rtx_match.S tombstone precedent).
- [ ] **S0 AUDIT + BASELINE.** Re-prove watermark both modes. slen consumer census: (a) ⛔ DT_N DISCRIMINATOR OVERLOAD — descr.h:109-113 keys NAMETRAP/VARREF on slen∈{2,1,0} over DT_N ⇒ populate is **DT_S-SCOPED ONLY**; (b) every `.slen==0`-means-unknown reader (grep runtime+templates+rtx .S) — the standing "no fast arm keyed on slen!=0" rule INVERTS to slen-is-truth, each reader ruled; (c) mint census: STRVAL vs BSTRVAL vs concat/substring/coerce result sites.
- [ ] **S1 POPULATE.** Every DT_S mint carries true length: STRVAL strlens ONCE at mint (per-mint replaces per-match — 2,000,001 strlens/run on pattern_bt_deep → mint count); length-knowing producers (concat, splice, LEN capture, int→str) write it directly. Killswitch `SCRIP_SLEN=0` restores mint-zero byte-identical. Instrument: slen==0 count at match enter must read **0** on pattern_bt_deep; gate = probe suite ≥ floor BY SET both modes.
- [ ] **S2 SUBJECT MATERIALIZATION.** Lowerer guarantees subject cell is materialized DT_S before MATCH_BEGIN — INT/REAL/NAME subjects get COERCE_STRING+deref on the spine (bb_coerce_string exists). MATCH_BEGIN precondition BY CONSTRUCTION: `v==DT_S && s!=NULL && slen valid`. MONITOR-FIRST on first divergence.
- [ ] **S3 INIT RELOCATION.** `rt_dcap_lazy_init` → scrip.c startup beside `rt_gcheap_warmup` (W1-GC-WARMUP precedent, both entry sites).
- [ ] **S4 MATCH_BEGIN REWRITE — Done and Done.** Template emits the register-only α above; `rt_match_enter` call deleted; exit `rt_match_ctx_restore` calls → inline Σ/Σlen stores; both C/asm bodies tombstoned. Gates: probe both modes ≥ floor BY SET · roman + length-family witnesses (RPOS/RTAB/REM/LEN) · MONITOR-FIRST on first red · regen ×3 · killswitch-OFF byte-identical until fold.
- [ ] **S5 STRIKE THE PARK.** GOAL-SNOBOL4-RTX slen row = pointer here (done in routing commit); invert the no-slen-fast-arm standing rule tree-wide; FINDING + both cursors.
(MECH s20 cursor: HFC-WINDOW landed 99ba2393; m3 112R->80R; 32 fixed; next FENCE class)

## ⛔ DEFINITION OF DONE (inherits BB HQ DoD; selectors already gone)
1. ω-coverage 100% of K>0 boxes (U-GATE instrument), crosscheck+benchmarks. 2. Orphan-adds 0 outside audited whitelist. 3. UCLAIM MACHINERY PHYSICALLY DELETED (not just census 0): `zvo_uclaim_k`, owner-table declined extension, staging twins, head `sub rsp,Kc` arm — greps == 0. 4. Wall census == framed-enter count over the framed set {STATEMENT(unknown-extent) · MATCH_BEGIN(blob/mech-2) · FENCE1 · FUNCTION}. 5. Exactly THREE glue spellings tree-wide (see GLUE TAXONOMY); THE GLUE DOES NOT WHACK — grep-enforced. 6. Killswitch fold complete (M-5 list) — per-kind gate names == 0 outside this doc. 7. xc318 BY SET ≥ pre-flip baseline (m3 280 / m4 260) both modes · rc=139 tail cleared · bench 18/21 hold-or-better · regen ×4 · `rbp-op-refs` < 14198 (08-03f) and trending down.

## ⭐⭐⭐ THE MODEL (canonical, one place — consolidated from HQ s23k/08-03b/s21x-c/08-06c)
**THERE IS NO GRAPH FRAME.** Every BB `sub rsp,K` at α for its OWN result (16B DESCR) + locals; blob-interior scanner kinds are K=0 BY DESIGN (ride r13/r14/r15 + claim slots). γ NEVER frees. **THE UNWIND (4 clauses):** 1. ROLL UP — α carves own K; spine accumulates NON-POPPING. 2. ROLL DOWN — FAILURE NEVER WHACKS: box N's ω restores own cursor/CAS state, frees OWN K only, jumps to pred's β; one-shot box's β IS its ω-continuation (`add rsp,K; jmp pred_β`); RSP at β == RSP at γ by induction; the depth-knowledge problem is DELETED not solved. 3. WHACK forward-only at fenced sync (FENCE commit; MATCH_END = SN4's language fence). 4. WHACK at final success — STATEMENT_END.γ sole statement release; STATEMENT_BEGIN.β sole fail terminus (arrives at entry depth BY CONSTRUCTION). **WHACK CONTRACT (6):** two whack-free mechanisms only — DETERMINABLE `add rsp,ΣK` at final success / INDETERMINABLE empty RBP frame at the indeterminacy boundary, whack `mov rsp,rbp; pop rbp`; whack sites exactly {end-STATEMENT, end-MATCH, FENCE commit, FUNCTION return}; THE GLUE DOES NOT WHACK; STATEMENT_END sole statement whack authority (op_zgpop home); operator frees ONLY own result ONLY at ω, never operands; own α claim ~16–64B, statement head carrying whole claim = UCLAIM debt. **STATEMENT-PORT LAWS:** fail → BEGIN.β (release may whack-free BY ADD — unwind + re-base guarantee rsp==claim base on arrival); success → END.α; BEGIN frames ONLY unknown-extent statements. **SCOPE LAW:** statement level only, no function-level processing.

## ⭐⭐ FRAME CENSUS — which constructs require a frame (RBP-MINIMALITY LAW)
RBP fires ONLY where extent is runtime-indeterminable. **THE SET (refined 2026-08-06, ARBNO dropped): {STATEMENT (unknown-extent only, mech-1 `add rsp,ΣK` otherwise) · MATCH_BEGIN (blob/mech-2 case: `push rbp; mov rbp,rsp`, whack at MATCH_END/head-ω) · FENCE1 (law-4 watermark quad, depth-free `[rbp+off]`) · FUNCTION (DEFINE call frame)}.** Frames INDEPENDENT, nested via saved-RBP chain (nested indeterminacy = nested frames, HQ 2026-08-03); interiors `[rbp+8+off]`, never rsp+uclaim phantoms. **ARBNO IS FRAMELESS** (Lon's intuition CONFIRMED — ARB-LON-K16 `512ff8cd`/`c7a276f6`, FINDING-2026-08-06): needs exactly ONE 16B frontier cell {Δ0 (delta-cursor at entry, σ = cursor-compare exhaustion test), yield} — no frame, no counter, no links, no chain; ALL operands rsp-relative; body-K0 class rides in place; K16 commits are non-popping (composition laws: outer-pending cells + net-delta). RO constants sealed `[rip+disp]`. **Emitter rbp pin predicate** (`rpin()` = flat_deep_arrival || flat_pat || flat_gen; + `jepin()` = flat_jmp_entry && flat_deep_arrival, X12 fix `0c5987a7`); `x86_fb()` per-graph RSP-or-RBP via `x86_fb_pinned()` ONE selector both media; ZC_FRAME_R12 arm DELETED (`da8c2347`). Every NEW rbp use must name its indeterminacy in-comment or it is a violation. Register contract (ARCH-ICON, verified): R13=Σ subject base · R14=δ cursor · R15=Δ length · R12=DCAP/CAS top (CAS-R12-UNIFY `6g`) · RBX=heap frontier (dormant under ZC_PORT_FORTH — grants spend as `sub rsp,K`).

## ⭐⭐ GLUE TAXONOMY (exactly three; the two invocation kinds Lon names + the frame pair)
1. **ONE-SHOT** — deterministic box: β ≡ ω-continuation trampoline `add rsp,K; jmp pred_β` (x86_beta_trampoline; ΣK ZPOP fold legal iff every folded β whitelisted-trivial). 2. **PASS-THRU** — transfer box adopts caller wires, zero frame: `bb_glue_pass_wires` (lea rcx,γ + lea rdx,ω + jmp rax) — DEFER, GOTO, DYNAMIC-FLAT `jmp [entry]` invocation (Design-of-Record class A). 3. **FRAMED** — `bb_glue_framed_enter/leave` push/pop rbp pair, THE ONLY RBP WRITER (Design-of-Record class B, DYNAMIC-FRAMED). Current source also carries `bb_glue_flat{,_enter,_leave}`, `bb_glue_outer_whack`, `bb_glue_wire_*` — M-4 collapses these into the three or proves each a spelling of one (outer_whack likely violates THE-GLUE-DOES-NOT-WHACK → its release moves to STATEMENT_END/MATCH_END authority).

## ⛔ LAWS & TRAPS (carried, binding; full text GOAL-SNOBOL4-BB.md — the load-bearing subset inline)
ζ gate = "zero mid-body cell" not "zero rsp" (legit raw-rsp: GLUE · C-ABI ALIGN · CSTACK SWAP · PROLOGUE). NO PROCESS-SCOPE FLAG DRIVES A GRAPH-SCOPE REGIME. POSITIVE-CONTROL + DENOMINATOR + MODEL-THE-POP (γ=zout−zgpop, ω=zout−K−zwpop). COMPARE m4 NEVER m3; BY SET never count; `setarch -R` (S231 ASLR min-rail landed); env-length controls (127/152 class — post-M-6 must be IMPOSSIBLE; any flake then is a LEAD). CENSUS SHELF-LIFE — re-run never cite; HEAD-STAMP. Artifact truth: `.s` = compiler-that-ran; sweep the COMPILER. Seal is GRAPH-scope (re-entrancy of the MATCHER, not membership). DEFER-DEEP LOAD-BEARING until recursive-defer passes (roman ≥10 is the witness). SUSPENDED-CELL: no rsp cell suspends across γ above ARBNO/DEFER extent. RESULT-IS-THE-CELL; SOLE-CONSUMER FENCE; UNION-TAG; NODE-EXACT HANDOFF; SAME OFFSET ≠ SAME OBJECT; match-bracket window = (MATCH_BEGIN, next MATCH_BEGIN). MEASURED NEGATIVES (DO NOT RETRY): frame-slot route to DCAP marker (register arithmetic `sub r12,24` is the only depth-free form) · `x86_fc_cells()=(FORTH||HEAP)` flip · RBPPAIR γ/ω mirror (1016_eval) · umin clamp for the 224 residual · naive op_zres defer-arm suppressing slow pump. STALE-BINARY TRAP (`grep error:` both logs; and ⚠ `grep -c` EXITS 1 ON ZERO — never chain `&&` off it, measured 2026-08-08 as a silently-skipped sweep). MONITOR-FIRST for every divergence (RULES.md §1); monitor dark ⇒ MON-RE first.

## ⭐ HISTORY CONSOLIDATION — authority index for every stack fact (titles are in `.github/`; one line each)
Design of record + sliding offsets s21x (DESIGN-SN4-*, STF-UNFLIP, NOFC arc: FINDING-07-31h/i — NOFC asymmetric, non-popping spine fixes 33, five pops cost 20–48%). Carve deletion s23g–s23o (artifact-truth; probe suites cannot certify emission — only `.s` region diff). ZW-12/13/15/16 canonical frame (FINDING-08-03 ZW13 dark-since-S24A nblob clause; ZW16 r12 not coherent across backtrack → CAS-R12-UNIFY `6g` one authority). MECH2-R12-FIX `590b9140`. SEQ-ERAD arc (08-04b DAG premise falsified; 08-04g fc_walk_range over-deletion; 08-05 nested-ARBNO rsp3 root cause; killswitch-law violation recorded — the no-revert-path lesson this ladder's per-slice git-revert discipline answers). ARBNO-LON frameless + ARB-LON-K16 composition laws (08-06). CAS sentinel clean `6n`. FENCE-SEMANTICS `8ab4c266` (07a; manual pp.125-126/209: FENCE0 abort-on-backup+anchors-at-head; FENCE1 p.128 pass-through, arms invisible). LADDER PB complete 07b–07d (PATREF invalid+deleted; STITCH ruling: plain-ref = BUILD snapshot per manual p.86 — `LEN(N)` captures N at construction, `*N` defers, p.122 `*` sole recursion vehicle; witnesses pb_snapshot_imm/stitch_* in corpus/probe). JE-PIN X12 `0c5987a7` + W-2a/W-2b (07-session). PB-4 measure (FINDING-07e: snapshot-DEFER false-dynamic + admitted head-claim survival — both now M-2/M-3 customers). Bug 6 diagnosis + HEAD-PIN recipe (W-1 notes, ARMED-REGIME DEBT re-measure 07e: COMBO 127/3/0/11 set A05 A06 F04 G05 G09 G17 G18 G21 H14 H20 H23). REGIME DELETION slice 1 `de837576` (this cursor).

## RUNGS (walk in order; each: own commit, buildable, git-revert path, gates vs CRATER BASELINE by set, FINDING per land mine)
- [x] DONE — M-0 RE-PROVE (bff5604e)
- [x] DONE — M-1 BUG 6 (14ee075d)
- [ ] **M-1b GLOBAL-EXECUTE — DELETE `g_blob_ctx`, THREAD THE BASE (⛔ RED-SUITE UNBLOCKER: D12·D13·H31 + the 91% re-entrant class. LON RULING s36c, verbatim intent: "We do not use globals for hacks like that" — the save/restore-bracket alternative is REJECTED; the global DIES):** ⭐ **LON MODEL RULING s36d (supersedes s36c's rethread framing): NOTHING IS SPECIAL ABOUT `*name` DEFER — the blob-level header/publication/exit-stub LAYER is the bug's habitat and is DELETED, not rethreaded.** Wires PASS THROUGH to the interior boxes (no blob-level stub exists to need a cell); EACH interior box keeps its own γ/ω wires in its OWN α-carved cell — `[rsp+k]`, k compile-time, valid at that box's β/ω because LIFO has drained everything above before β is entered; recursion = fresh activation, fresh cells, nothing more; ω total-failure drains by CAS-POP-SCAN to the α sentinel (idiom already landed, `bb_match_end` L(10)) — RSP-only, no anchor; β resume is RECORD-carried (record[+8] precedent); statement-scope scan/retry state (δ0, flag) lives on the LICENSED rbp MATCH_BEGIN keeper — per-STATEMENT, never per-blob. Step 1's census is therefore a DELETION census: each of the 5 reader sites either DISSOLVES with the layer or names its per-box-cell / statement-frame home. Prior claim "five sites cannot be RSP-relative" analyzed the header layer as load-bearing — with the layer gone, the sites go with it. the per-activation cell is CORRECT and the record[+8] channel ALREADY carries the cell base per suspension (D-2's own comment, emit.cpp:2757) — the global was a bypass of machinery that already exists. Rethread every reader to a per-activation source, then physically delete. **Step 0 RED baseline:** D12/D13/H31 (probe) + `W2_nest`/`W3_selfrec` (sources FINDING-08-10c §2; promote WITH the fix, never before) RED both modes at open. **Step 1 READER CENSUS (measured s36c: definition+accessor = pattern_match.c:624-625 ONLY; all consumers in the ONE emit.cpp blob_act region):** for each of the ~6 read sites (scanhit ≈2734 · scanfail ≈2738 · β res-stub ≈2763/2773 · CLASS-D γ ≈2815 · CLASS-D ω ≈2830/2832) name WHERE the base legitimately lives at that site's execution moment — β already has it (record[+8]); γ-suspend must WRITE it into the record it is creating (it does — that is what [+8] is); scanhit/scanfail/ω must receive it via the wire set or a spine slot at a depth-invariant coordinate the site can compute. If any site truly cannot reach a per-activation source, STOP and route to Lon — do not reintroduce a shared cell under a new name (that includes "one register claimed globally": same hack, different spelling). **Step 2 REWIRE:** convert each reader; α's publication block DELETED with them; TEMPLATE-ONLY discipline (`bb_emit_x86(x86(...))` idiom, per the 2734 site, never the raw `emit_text_n`/`ef_b2` arm); killswitch `SCRIP_BLOB_GCTX=1` = legacy global arm retained ONE rung for same-build A/B bisect, then dies in M-5. BOTH MEDIA at every arm. **Step 3 PHYSICAL DELETE:** `g_blob_ctx` + `rt_blob_ctx_ptr` removed from pattern_match.c; gate `grep -rn 'g_blob_ctx\|rt_blob_ctx_ptr' src/` == 0 (killswitch arm exempt until M-5 fold, then == 0 unconditionally). **Step 4 POP MODEL:** any spine-slot coordinate introduced in Step 1 must be priced through MODEL-THE-POP (γ=zout−zgpop, ω=zout−K−zwpop) and the ω drain-to-entry-depth accounting — never suite-green alone. **Step 5 GATES, in order:** witnesses green both modes → 151×2 ≥ the pre-regression `a5c22641` mark (m3 136/15/0/0 · m4 135/16/0/0) → crosscheck/patterns nested-class re-sweep (31/34 broken must collapse; bar EXCLUDES the pre-existing no-blob background 20/70 and 11 blob-free silent-wrongs) → `066`/`115` green → Icon 303-emission content-hash sweep (emit.cpp touched) → regen ×3 (NOT-CONCURRENCY-SAFE, Lon routes) → FINDING + cursor + strike this rung. ⚠ SCOPE HONESTY: this is the WIDE variant — Step 1's census may surface a site whose only depth-invariant source is a wire-shape change touching bb_glue_pass_wires consumers; if so the rung grows and the cursor must say by how much BEFORE proceeding.
- [ ] **M-2 ROUTE THE HOMELESS (decline == ROUTING, never a verdict):** (a) sealed-DEFER graphs (roman's class): nested-capable protocol = **M-1b's threaded-base design** (global DELETED per Lon ruling s36c; rbp-free; supersedes both the push-rbp FRAMED prescription AND the s36b save-bracket). After M-1b lands, TEST whether the graph-scope seal can retire on the new mechanism (roman ≥10 is the witness) rather than assuming it. (b) no-END runs: no head claim at all — pure per-box K (they are the ZPOP-fold class). (c) zdyn veto tree becomes routing to (a). (d) INFLATION BISECT (O-PB-2 method, exactly one of three): claim-base span arithmetic / op_zdepth double-count / zvo_resolve base move — `SCRIP_ZD_GAP=1` fprintf per site on any.sno; tripwire: stmt claim ≤ Σ member K + frame constant. Exit: `zvo_uclaim_k` returns 0 every run by construction; roman ≥10 correct (recursion witness moves to CLIMB C-6 if a residual defect is non-structural).
- [ ] **M-3 UCLAIM PHYSICAL DELETION:** zvo_ent_t owner-table declined extension · zvo_uclaim_k · g_zd_uclaim/g_zd_udout/g_zd_uhead staging twins · head `sub rsp,Kc` arm · veto diagnostics. Greps == 0: `zvo_uclaim\|g_zd_uclaim\|g_zd_udout\|g_zd_uhead` in src/. Loose-whitespace UCLAIM census grep `sub\s+rsp,\s*[2-9][0-9][0-9]` over regen'd SN4 `.s` == 0 (single-space spelling is a FALSE INSTRUMENT).
- [ ] **M-4 GLUE UNIFICATION:** collapse bb_glue_flat*/outer_whack/wire_* into the THREE (or prove each a spelling); outer_whack's release re-homes to the whack authorities; gate: grep the glue namespace == exactly the three families + THE-GLUE-DOES-NOT-WHACK grep (no `add rsp`/`mov rsp,rbp` inside glue bodies except framed_leave).
- [ ] **M-5 KILLSWITCH FOLD + LEGACY RESIDUE:** fold-or-die: SCRIP_ZD_{PROC,CAP,FENCE1,DYNSCOPE,DYNARM,ALT,SR,PATREF,GAP-diag-survives}, SCRIP_ZW_{FRAME,NBLOB,5}, SCRIP_{UNWIND,STMT_FRAME,ARBNO_LATCH,ARBNO_K16,ARBNO_FRAMELESS,ZPOP_FOLD_OFF,PAT_BUILD,NOFC,SLOT_ELIDE,SUBJ_DYN,STF_ELIDE_OFF,JEPIN}. Diagnostics + non-SN4 gates survive. ENDJMP/op_wsteal residue · ZW-5 depth stubs · dead op_zw/op_zw2 arms post-M-4 · legacy chain arm (M-2(a) is the nested-capable replacement — then the 19 sq=0 programs route framed and the chain dies) · marker scans L(5)/L(8) (delete iff CLIMB proves carry-capable replacement; else record as the sole survivor with reason).
- [ ] **M-6 FRAME-MINIMALITY + ω AUDIT:** U-GATE ω-coverage 100% K>0 (DENOMINATOR LAW) · orphan whitelist audited then 0 · wall census == framed-enter count over the frame set · rbp-op-refs < 14198 trending down · every rpin()/jepin() term justified · env-pad class impossible (frames kill absolute-sensitivity).
- [ ] **M-7 FINISH:** DoD checklist 1–7 green · completion greps · xc318 BY SET ≥ 280/260 both modes · regen ×4 · HQ file LADDER W struck as absorbed-done · FINDING + cursor.

## COORDINATION
CLIMB (`GOAL-SN4-ZETA-CLIMB.md`) has right-of-way on per-defect fixes; a fix requiring NEW protocol/structure is a MECH rung — cross-request in both cursors, never landed twice. ICN sessions: shared hunk = zd_wl_kind (ZK-1) — pull --rebase, eyeball that hunk, re-run the Icon 303-emission content-hash sweep (procedure in this file's cursor; parent = last known-good) after ANY emit.cpp/emit.h/x86_asm.h edit. `.github` pushes last.

## WATERMARK OF RECORD
= this file's LIVE CURSOR gate lines. **Proved s31 (HEAD `53a4f62c`/corpus `dbec8660`):** run_suite m3 **146/4/0/0** · m4 **144/6/0/0** · matrix m3 **146/4** · m4 **144/6**. Crater baseline m3 124/18/0/0 · m4 116/26/0/0 is the floor; every rung ≥ it BY SET; DoD 7 restores ≥ pre-flip xc318 280/260. Re-prove at open and close; never carry counts across container speeds.

