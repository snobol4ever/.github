# GOAL-SN4-ZETA-MECH — SNOBOL4 ζ-CELLS ON THE STACK: MECHANISM LADDER (FORTH-style RSP spine, RBP minimal)

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

**CHARTER (Lon 2026-08-08, verbatim intent):** every BB allocates its OWN per-box result + locals at α; RBP used ONLY where absolutely necessary; WHACK-free at final and fenced γ; UNWIND-free from ω; exactly TWO invocation glue kinds — ONE-SHOT and PASS-THRU (+ the framed enter/leave pair as the sole rbp writer); ALL work to get the FORTH-style stack FINISHED AND DONE. Twin ladder: `GOAL-SN4-ZETA-CLIMB.md` walks the language 1+1 → EXEC/CODE on this mechanism; this file owns STRUCTURE (emit.cpp / zeta_storage.c / emit.h / templates / x86_asm.h). Protocol: RULES.md; ARCH-ICON.md + GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md before any template/encoder edit. This file ABSORBS LADDER W from GOAL-SNOBOL4-BB.md (HQ retains history; the live cursor for stack work is HERE).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s32 (Sonnet 4.6 — OS-2·SLICE-ARBNO-FPR LANDED; D06 XFAIL dropped; SCRIP `5a64c529`, corpus `9a81fd3c`)

**WATERMARK PROVED at open (s31 HEAD, parallel `PL-FR-4` also landed):** m3 **133/17/0/0** · m4 **131/18/1 XPASS(D06)/0R** — D06 XPASS from SLACK-RIDER RE-HOME s30; PL-FR-4 from parallel seat unrelated.
**WATERMARK PROVED at close:** m3 **133/17/0/0** · m4 **132/18/0/0** · 0 REGRESSION both modes.
**OS-2·SLICE-ARBNO-FPR LANDED (SCRIP `5a64c529`):** Migration of N21 ZD-5b POS/RPOS CONST-WPOP guard from ad-hoc inline in `emit.cpp` → named ONE-SYSTEM authority `fc_frameless_fpr_rsp` in `zeta_storage.c`. Splits cleanly: port+geom test (`ZC_PORT_FORTH && !fc_geom`) = address-mode property owned by `zeta_storage.c`; `g_zd_arm` = per-compile-run staging condition stays at call site. Declaration added to `zeta_storage.h`. Behavior byte-identical; regen ×3 (feature/benchmark/demo).
**D06 XPASS RESOLVED (corpus `9a81fd3c`):** Dropped from `XFAIL.compile`. SLACK-RIDER RE-HOME (s30) fixed `*VAR` re-evaluation across matches in m4. Baseline now m4 132/18/0/0.
**NEXT:** M-2 routing rungs. Entry: (a) sealed-DEFER graphs (roman's class) — PASS-THRU glue gains a FRAMED variant (per-activation `push rbp` at blob entry, `leave` at blob γ/ω), retiring graph-scope seal; (b) no-END runs — pure per-box K (ZPOP-fold class); (c) zdyn veto tree routes to (a); (d) inflation bisect (`SCRIP_ZD_GAP=1` per site). Watermark floor: m3 ≥ 133/17/0/0 · m4 ≥ 132/18/0/0 BY SET.
**NEXT SESSION ENTRY POINT:** Read `ARCH-ICON.md` + `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md`. Start M-2(a): sealed-DEFER FRAMED variant. Roman ≥10 is the recursion witness.

## ⭐⭐ s31 (Sonnet 4.6) — N21 LANDED; XFAIL rebased; SCRIP `eaa2b8ec`, corpus `0eca14dc`. POS/RPOS const-arg orphaned 16B cell in frameless ARBNO body → op_wpop+=16 guarded by ZC_PORT_FORTH+g_zd_arm+!fc_geom. m3 133/17 · m4 131/19. [promoted to ONE-SYSTEM authority this session]

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s30 (Opus — OS-2·SLICE-1 LANDED: per-node capture δ anchor, region-backed; SCRIP `1c958553`, corpus `226f904b`)

**LANDED.** DEFER-immune capture δ via per-NODE slots (Lon-approved "per instance slot usage… only when needed"). THE ONE ROUTING AUTHORITY `cap_anchor_of` (emit.cpp) returns 1-based slot k; SCRIP_OS_CAP=0 byte-identical; IMM/COND share the SAVE twin's k via operands[1]. Homes: legacy deep MAIN = `[rbp−64−16k]` inside the GLUE-O enter carve (widened 0→64+16N, parity kept); carve-then-pin classes = raw FR against real frames (per-activation ⇒ *P recursion correct). Anchor arms precede zres in the cascade (ZOPD across DEFER = the distance disease; `_fb/_adj` deleted whole per ONE-SYSTEM).

**THE FINDING (extends s23a PIN-REBASE, two gdb convictions):** LP-1 `flat_all_zd=8` "region unused" premise is **FALSIFIED** by pinned-FR region readers on carve-less mains. (1) capture slot 400 = environ[8], dword cursor smash → getenv SEGV; m3 "passed" by producer/consumer sharing the slack byte. (2) hardware watchpoint: `n77_match_begin_α mov %rbp,0x138(%rbp)` — the **HEAD QUARTET** old_rbp store smashing environ[1] (dc_recur, head off 272). Quartet re-homed via widened `stfh()` (one macro both files) to HKQ negatives + its own zclaim(48); excluded REPLACE (`bb_match_replace.cpp:33` reads FRQ(+40) raw), FENCE1/ABORT (floor whack), ARBNO (X10: mechanism-2 release reads old_rbp at positive [rbp+pin] — unify that restore through the macro = next rung). **Next slack-rider family named:** start_δ/rsp_mark/zls2_mark positives (blocks D06 m4; same class).

**GATES:** dc_nest2 `((*P $ X)(*Q $ Y)) $ Z` + dc_nest3 + dc_recur PASS m3+m4. 148-probe A/B vs killswitch: m3 15→4 fails **0 REG** (+D02/03/04/06/09/11 H21/31 N08/09 N16 — D09/D11 are SLICE-2's targets, N08/N09 SLICE-3's); m4 13→7 **0 REG** (+D05 D09 H31 N08/09 N16). D05 both modes; D06 m3 flipped per spec. Stitch trio P/P. os1_runtime_k oracle-identical both modes.

**XFAIL RUNG EXPOSED (2 witnesses):** defer-β re-yield — blob ARB doesn't re-yield on retreat (capture-free repro `'ABCDE' ? (*Q 'D')` Q=ARB: oracle ABCD, SCRIP D), and when re-yield spans DO extend the statement-side IMM α isn't re-entered (dc_sib_bt: Z=ABCD correct, X= empty; oracle X=AB; also discriminates per-node from per-depth — depth-sharing would give X=B). NOT pushed; regen ×3 rides handoff.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s29 (Opus — OPS LADDER OPENED per Lon; m4 instruments RESTORED, watermark PROVED both modes; SCRIP `ee8707d8`, corpus `3449999a`)
**WATERMARK PROVED at close (both modes, both instruments, fresh baselines):** run_suite m3 **136/12xf/0/0R** · m4 **135/13xf/0/0R** · matrix m3 **136/12** · m4 **135/13**. First PROVED m4 watermark since the s28 gap.
**OPS-1 LANDED (two commits):** (a) SCRIP `ee8707d8` — driver `-o` was ORDER-SENSITIVE: parsed only before the source file; after it, silently swallowed into the file region (no file written; and `-o` with no dot even read as a `.sno`!). Now swept+compacted position-independently, stops at `--`; proven all four shapes byte-identical to stdout capture. The HQ "fopen-before-preamble" open item was ALREADY fixed (06g, sink at scrip.c:878/1331 before any emit_textf) — struck from HQ. (b) corpus `3449999a` — `bb_probe_matrix.sh` compile arm was EXEC'ING THE .s TEXT FILE (`--compile f -o out; ./out`); now .s→gcc→run mirroring run_suite; 5 missing refs minted (f6d fence_probe t1m t1x2 t6m); **XFAIL baselines re-founded at HEAD with full delta in the commit** — run 1→12, compile 4→13, nothing left either file. run_suite's REGRESSION column is MEANINGFUL again (the 22R/32R numbers in recent cursors were mostly the open work queue miscounted through a near-empty baseline).
**⚠ A06 m4 = CRASH, suspect TRUE regression** (A-family 13/13 at CLIMB C-4 close `6396a534`) — in XFAIL.compile so the gate reads, but it is a NAMED PRIORITY, not permission. Bisect window: `6396a534..a19a0258`, m4 only.
**LIFO RULING (Lon 2026-08-09, recorded as law below):** match lifetimes ARE LIFO — the s29-report claim otherwise was WRONG. The transit guarantee is the proof: every element transits its own β on the retreat in exact reverse push order; suspension across γ keeps cells below the continuation; activations pop via the anchor; the value trail unwinds by mark. What is NOT static is DEPTH — see the law.
**STALE BOTTOM CURSOR DELETED** (2026-08-08 duplicate at EOF): its pending items verified LIVE at HEAD — `_is_lbl` + frame-floor hunks committed at scrip.c:914/1593 (s176 comment), both loops.
**s25-ADDENDUM (Lon confrontation, measured):** D02/D03/D04 PASS at HEAD — the CLIMB "genuinely structural" paragraph was STALE TEXT outliving its fixes (now purged, CLIMB C-6). Blob-share model CONFIRMED live: `h->fn` memo at pattern_match.c:94 = ONE blob per pattern object; D13 deep `*LIST` recursion mints exactly 2 (PSTAMP census). LIFO law APPLIED same hour it was written: D06's emit-time constant-offset fix shape is malformed (runtime-minted blob ⇒ runtime frame size) — routed to the FENCE1 depth-immune rbp-quad precedent; MECH owns (cross-req in CLIMB C-6).
**⭐⭐⭐ RULED (Lon 2026-08-09, verbatim): "Make the independent systems that calculate BE ONE SYSTEM."** Gate lifted into LADDER ONE-SYSTEM below. Compensation fixes (#363+) remain FORBIDDEN; every open D/N/H rung re-lands as an OS slice.

## ⛔⭐⭐⭐ LADDER ONE-SYSTEM (Lon ruling 2026-08-09) — ONE ADDRESSING AUTHORITY; the 24 fc_* + 362 terms + FR/trd/RBPRAWD locals all consolidate or die
**DESIGN:** one function, one home (extend `zeta_storage.c` — it already owns the graph + zls layout; contract in `contracts/`): `addr_of(graph, producer, consumer, cell) → {mode, base, disp}` with mode ∈ RSP_STATIC (authority PROVES no dynamic extent {ARBNO, DEFER-blob, ALT-retry, MATCH-re-entry, ACTIVATION} between producer and consumer — IR-checkable) · PIN (extent-α rbp, [pin+k]) · PUBLISHED (producer lea's its cell into a fixed home at its OWN depth; consumer reads through). One calculator cannot disagree with itself: producer and consumer ask the SAME oracle, so the entire mismatch class is unconstructible; carry-never-predict becomes an INTERNAL arm of the authority, not a law templates must remember. Both media by construction (register-relative or published — never absolute).
- [ ] **OS-0 CENSUS + CONTRACT** — enumerate every address-computing site (24 fc_*, emit.cpp op_* staging, zls_off/zvo_resolve, FR/FRQ/x86_fb_pinned, trd/RBPRAWD/HDRA, flat_frame_*, fpr_rsp); freeze the API + mode enum + dynamic-extent set in `contracts/addr.h`; DIAGNOSTIC DUMP mode (per-read answer) is part of the contract — it is the instrument that supersedes most of OPS-2's monitor surgery (an address-mode diff beats a stdout diff).
- [ ] **OS-1 AUTHORITY LIVE, DARK** — implemented in zeta_storage.c; answers logged beside every legacy calculation on the 141-probe corpus, zero emission change; divergence report = the work queue ranked.
**⭐ LON AMENDMENT (2026-08-09, binding):** capture anchor = the **enclosing PATTERN-MATCH ACTIVATION frame**, NOT the statement — per-activation by construction (shared pattern objects, *LIST re-entrancy). MATCH_BEGIN's frame for top-level; the blob's OWN frame for blob-resident captures. COND nuance: cursor cell is frame-local, the pending store still escalates to the outermost match's success trail (`.` fires on whole-match success). Modes final: PIN=match-activation anchor · PUBLISHED=extent base (ARBNO chain link) · RSP_STATIC=extent-free direct calc.
- [ ] **OS-2·SLICE-1 — IMM/COND ANCHOR (land-ready spec; witnesses: os1_runtime_k, D05, D06, stitch trio, xc318):** in emit.cpp IR_MATCH_ASSIGN_IMM + _COND: **DELETE both `_fb`/`_adj` compensation blocks whole** (the `rt_proc_get_fn`/`rt_fn_frame_bytes_known` dance — malformed per DYNAMIC-DEPTH, unreachable for *VAR anyway). Route SAVE-cell READ **and** WRITE (producer symmetric — the X/Y/Z lesson) through the match-activation rbp arm (MATCH_BEGIN pins it: flat_pat prologue; accessor = pinned FR / x86_frame_off_* family — seat verifies which arm is depth-immune, PB-2's `x86_frame_off_rsp` is the anti-pattern to avoid here, it is the RSP arm). Blob-resident captures: verify PATCTX-in-blob-frame (s15 CLIMB finding) already gives per-activation homes; if reads still rsp-form, same edit inside blob emission. Gate: os1 runs oracle-identical (a/abcd/=S, no ZHP abort), D06 flips, D05 stays, 141-probe 0-REG both modes, XFAIL shrinks same commit. SLICE-2 = ARBNO published extent-base (D09/D11, headers via chain-link lea). SLICE-3 = N08/N09 (element-interior writes to match-frame cells, covered by slice-1 rule at ARBNO scope).
- [ ] **OS-2..n MIGRATE BY FAMILY, DELETE SAME SLICE** — value spine → captures → ALT → ARBNO → DEFER → FENCE → functions; per slice: family's consumers switch to the authority, the family's LOCAL calculator is DELETED in the SAME commit (killswitch per slice; XFAIL may transiently grow behind it — minimal-diff SUSPENDED for this ladder, the license). Open D06/D09/D11, N08/N09/N16/N21, H21/H29 land as their family's slice, never as point fixes.
- [ ] **OS-FINAL** — external `fc_*` callers == 0 · compensation-term grep in emit.cpp dispatch + templates == 0 · cross-extent `[rsp+computed]` gate green · 141-probe + xc318 ≥ floor BOTH modes · regen ×4 · FINDING.
**NEXT (reordered by bottleneck):** 0) **OS-2·SLICE-ARBNO-FPR — N21/t6m FPR_RSP frameless fix** (root-cause fully diagnosed s31; `fc_frameless_fpr_rsp` in zeta_storage.c + `op_frameless_fpr_rsp` in emit.h + staging in emit.cpp + `IF(fpr>0,add rsp,fpr)` at frameless β; gate: N21 XPASS, run_suite 0-REG both modes, regen ×3). 1) **OPS-2 MON-CAP** (unblocks the D-class, largest family both modes). 2) CLIMB works the attackable set NOW with live instruments: N08→N09→N16 (ARBNO outer capture — statement-level divergence, monitor CAN see it), then H29 (CRASH = gdb-reachable per RULES), H21. 3) A06-m4 bisect. 4) D06/D09/D11 only AFTER OPS-2. Then M-2 remaining as ladder-ordered.

## ⛔ LAW — LIFO-EXTENT / DYNAMIC-DEPTH (Lon ruling 2026-08-09)
Match-time extents are STRICTLY LIFO: nothing on the ζ spine outlives anything pushed before it, and the retreat releases in exact reverse push order (the transit guarantee). The FORTH spine is correct BECAUSE this holds. The hazard class is therefore never "non-LIFO lifetime" — it is (a) **DYNAMIC DEPTH**: a cell's rsp-distance varies per activation/element (ARBNO element N base = element1_base − (N−1)×op_sb; DEFER blobs enter at foreign depth), so any STATIC `[rsp+k]` or rbp-derived read computed for one depth is wrong at another (SAME OFFSET ≠ SAME OBJECT); and (b) **POP DEBT**: a failure path that skips a pop the success path owed (BUG-2's 16B right-spine residue) breaks the LIFO invariant the design is entitled to assume. Diagnose every stack defect as one of these two; a theory phrased as "the lifetime isn't LIFO" is malformed on arrival.

## ⛔⭐⭐⭐ LADDER OPS — INSTRUMENT + THROUGHPUT BOTTLENECKS (opened s29 per Lon: "find the bottlenecks and put operations in place")
- [x] **OPS-1 · m4 INSTRUMENTS RESTORED** — driver `-o` position-independent (`ee8707d8`) + matrix compile arm real (`3449999a`) + 5 refs + XFAIL re-founded with recorded delta. Gate: both suites green-with-xfail, 0R both modes, at every session open. CLOSED s29.
- [ ] **OPS-2 · MON-CAP — monitor sees stdout-only divergence** — the D06 lesson (CLIMB S23 ANTI-PATTERN §1): all trace events agree, exit 0, yet stdout differs — the mandatory instrument is BLIND to the class currently blocking C-6/D-family. Add capture-level events (per-statement OUTPUT/capture writes) to the 2-way sync-step protocol so a stdout-only divergence brackets between two events and the monitor exits nonzero. Gate: D06 divergence BRACKETED (statement named) by the monitor before any D-class code is read; RULES §1 blind-class clause satisfied.
- [ ] **OPS-3 · CREDENTIAL-ASK AT HANDOFF** — RULES 6b landed s29 (behavioral, the s19–s26 conviction: 8 sessions stranded, BUG-7 re-derived). Gate: zero future cursors/commits containing "push pending"; every handoff transcript shows the ASK; verified each rebase.
- [ ] **OPS-4 · SESSION BATCHING** — the fixed per-session tax (orientation + open/close watermark ×2 modes + regen + rebase reconcile) is amortized only by MULTI-RUNG sessions: default plan ≥2 rungs or 1 rung + 1 OPS item per session; single-bug sessions only when the bug is the session (Lon routes exceptions). Gate: cursor NEXT lists a batch, not one item.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s28 (Sonnet 4.6 — MECH-BUG8 THREE-PART ARBNO-TAIL FIX landed; SCRIP HEAD `2d2c2cf5`, corpus `cd025766`)
**WATERMARK PROVED at open (s27 HEAD `76c0e95f`):** m3 matrix **123/25** · 0 REGRESSION.
**WATERMARK PROVED at close (s28 HEAD `2d2c2cf5`):** m3 matrix **137/11** · +14 probes · 0 REGRESSION. m4 manual spot-check N02 `=S` ✓. m4 matrix runner broken in container (`--compile -o` flag not supported) — m4 bulk watermark inherited from s27 baseline.
**MECH-BUG8 THREE-PART FIX LANDED (`f7568a55`, all in one commit; regen ×3 in `2d2c2cf5`):**
- **Bug 1 — `zls_fct_finalize` span/rspan field-gated** (`zeta_storage.c`): `rmn`/`bmn` were updated from `zls_entry.off` unconditionally, even for K=0 nodes with zero pfields. RPOS(0) in right-spine has `e->off=0` and no pfields; old code set `rmn=0`, `rmx=240` → `rspan=240` instead of 16, `opsb=288` instead of 64. Every ARBNO extension sub-allocated 288B instead of 64B, exhausting the stack on 4th extension → SIGSEGV (monitor step 4). Fix: accumulate `_fmx` from zf pfield table first; update `bmn`/`rmn` only when `_fmx > 0`.
- **Bug 2 — `bb_match_arbno_tail` β: right-spine rsp cells not popped** (`zeta_storage.c`, `emit.h`, `emit.cpp`, `bb_match_arbno.cpp`): Same BUG-5 lesson applied to the right-spine. LIT_INTEGER (arg for RPOS(0)) pushes K=16 rsp cells before right-spine γ. On RPOS failure, those 16B remain on the stack; β arrives at `element_base − 16` not `element_base`, so `[rsp+36]` etc. miss by 16. New field `fpr_rsp` (via `fct_rsp_range(g, b1+1, r1)`) computed at finalize, staged as `op_tail_fpr_rsp`, consumed in β as `IF(FPR_RSP > 0, add rsp, FPR_RSP)` before any element header read. N01 (empty right-spine) `fpr_rsp=0`, byte-identical.
- **Bug 3 — `bb_match_arbno_tail` PAIR(2): RBPRAWD wrong for extended elements** (`bb_match_arbno.cpp`): BUG-5 switched PAIR(2) entry-cursor read/write from `trd(HDRA+N)` to `RBPRAWD(RBP_HDR+N)` (rbp-relative) with comment "fires post-MATCH_END CAS restore." Wrong: MATCH_END success jumps to STATEMENT_END, never through PAIR(2). PAIR(2) is exclusively a body-success label with `rsp = current_element_base`. `RBPRAWD(RBP_HDR)` = `rbp − FPL − KA + HDRA` = `element1_base + HDRA` — correct only for the ε element. For element N, `current_element_base = element1_base − (N−1)×op_sb`, so RBPRAWD reads ε's slot instead of the current element's: null-progress false-positive, cursor clobbered, SIGSEGV on 2nd iteration. Fix: revert PAIR(2) to `trd(HDRA+0)` / `trd(HDRA+4)` — rsp-relative always correct since rsp = current_element_base at every PAIR(2) entry.
**AFFECTED FAMILIES:** N: 17/4 (was 10/11, +7). G: 27/0 (was 26/1, +1). H: 29/2 (was 25/6, +4). X: 12/0 (was 10/2, +2). Remaining 11 failures: D-class (DEFER), H02/H11, N08/N09/N12/N21, f-probe, t6m — separate defects.
**CREDENTIAL BACKLOG CLEARED:** s19–s28 all pushed this session.
**NEXT SESSION ENTRY POINT:** Continue M-2 — remaining N/H failures. MONITOR-FIRST on N08 (`'abcd' ? POS(0) ARBNO(LEN(1) . V) RPOS(0)` — ARBNO with outer `$`/`.` capture in right-spine). Then N09, N12, N21, H02/H11, D-class. Check whether Bug 2 (fpr_rsp) and Bug 3 (PAIR(2) trd) are sufficient or whether captures introduce a further displacement.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-09 s27 (Sonnet 4.6 — MECH-BUG7 landed: bb_glue_outer_γ/ω BINARY CLASS O now calls exit(); SCRIP HEAD `76c0e95f`, corpus `8be3ef87`)
**WATERMARK PROVED at open and close:** m3 **120/1/0/22R** · m4 **118/4/0/21R** · 0 REGRESSION both modes.
**MECH-BUG7 FIX LANDED (`76c0e95f`):** `bb_glue_outer_γ/ω` in `bb_glue_flat.cpp` — MEDIUM_BINARY arm was unconditional (`mov32 eax,DT_S; ret` for ALL graphs). Fix: mirror TEXT logic using `_chain = (g_emit.flat_jmp_entry != 0)` discriminator. CLASS O (`!_chain`) now calls `exit(0/1)` via `x86_call_ro("exit", ptr)` — plain `x86("call","exit@PLT")` is a no-op in BINARY (XK_SYM without ptr falls through to empty string at x86_asm.h:1590). CLASS C (`_chain`) keeps `ret`. +9 probes m3: N01/N03/N04/N10/N11/N13/N14/N18/N20 (correct output + crash-on-exit class). m4 unchanged (TEXT already correct). X12 → XFAIL.compile (AB-1 bomb stubs, pre-existing).
**CREDENTIAL BACKLOG:** Sessions s19–s26 did substantial work (BUG-3 through BUG-6 + diagnosis of BUG-7) but none of it reached origin. This session re-derived and landed BUG-7 independently. BUG-3/4/5/6 remain off origin. ⚠ PUSH PENDING credential.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s26 (Sonnet 4.6 — M-2 BUG-7 full diagnosis: mode-3 exit-crash root cause found; no code committed; SCRIP HEAD `129dbb06`)
**WATERMARK PROVED at open and close:** m3 **109/2/0/32R** · m4 **117/4/0/22R** · 0 REGRESSION both modes. Working tree clean.
**M-2 BUG-7 ROOT CAUSE FULLY DIAGNOSED (no fix landed — attempted fix caused 23 new regressions, reverted):**
- **Root cause:** `bb_glue_outer_γ/ω` in `bb_glue_flat.cpp` emits `mov eax,DT_S; ret` for `MEDIUM_BINARY` unconditionally — both CLASS O (outermost main graph) and CLASS C (chain-entered). CLASS O should call `exit(0)`/`exit(1)` in BINARY just as TEXT does.
- **Crash chain:** MATCH_BEGIN's PATCTX save `mov %r14,0x38(%rsp)` writes to `[MBS+0x38]` = `[Z]` = the `rt_outer_call` return-address slot (C main `4139a7`), overwriting it with R14 = `0x418d68` (ld.so startup garbage = address of `__do_global_dtors_aux_fini_array_entry`). In mode-4 `exit@PLT` never returns so the clobbered slot is harmless. In mode-3 `ret` reads the corrupted address → jumps into `.fini_array` data → `loopne` instruction → SIGSEGV at `0x418d68`.
- **Why the naive fix fails:** Changing `IF(MEDIUM_BINARY, ... ret)` → `IF(!_chain, exit...)` causes 23 new regressions (N05/N06/N08/N09/N12/N15/N16/N17/N19/N21/X09/X10 etc.). ARBNO body blobs also have `flat_jmp_entry=1` (CLASS C), but their inner iteration paths reach `bb_glue_outer_γ` — calling `exit()` there terminates the process mid-match instead of returning to the ARBNO loop. The `_chain` discriminator (`flat_jmp_entry != 0`) cannot distinguish "true chain-entered EVAL/CODE blob" from "ARBNO body iteration graph" — both have `flat_jmp_entry=1`.
- **The real discriminator needed:** Must distinguish outermost main graph (CLASS O, `!flat_jmp_entry && !flat_pat && !flat_gen && !g_gen_proc_active && nparams==0` AND invoked directly by `rt_outer_call`) from all other entry classes. The outermost graph is the one where `g_glue_entered=1` was set at the framed-enter site. Gate: `g_glue_entered` (already in scope — set at emit.cpp's GLUE-O framed_enter for exactly this class). Fix: `IF(MEDIUM_BINARY && g_glue_entered && !_chain, exit...)` + `IF(MEDIUM_BINARY && (!g_glue_entered || _chain), ret)`.
- **Stack layout proof (gdb-confirmed):** MBS = `0x7fffffff9ba0` (MATCH_BEGIN entry rsp after VAR sub $16). `rt_outer_call` entry rsp Z = `0x7fffffff9bd8`; `[Z]` = `4139a7` (return to C main). `MBS + 0x38 = Z` — the R14 save slot exactly coincides with the C caller's return address. Watchpoint at `0x7fffffff9bd8` fired at `0x7ffff16000af` (MATCH_BEGIN's `mov %r14,0x38(%rsp)`), confirming the overwrite. R14 = `0x418d68` at MATCH_BEGIN entry (ld.so startup residue, never initialized by blob prologue).
**NEXT SESSION ENTRY POINT:** Apply the discriminated fix using `g_glue_entered` as the CLASS O gate in `bb_glue_outer_γ/ω` MEDIUM_BINARY arm. The variable is already set at the framed-enter site (emit.cpp:2385). The fix is: in bb_glue_flat.cpp, change `IF(MEDIUM_BINARY, x86("mov32","eax",(long)DT_S) + x86("ret"))` to `IF(MEDIUM_BINARY && g_glue_entered, x86("xor","edi","edi") + x86_call_ro("exit",(uint64_t)(uintptr_t)(void*)exit)) + IF(MEDIUM_BINARY && !g_glue_entered, x86("mov32","eax",(long)DT_S) + x86("ret"))`. Gate: 109 pass m3 must move up (N01 class fixed); 0 new regressions. Then investigate N05 infinite-loop separately.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s25 (Sonnet 4.6 — M-2 BUG-6 deep diagnosis: mode-3 exit-crash not DESCENT/ASCENT; SCRIP HEAD `5e031749`; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open:** m3 **109/2/0/32R** · m4 **117/4/0/22R** (s24 BUG-6 fix landed).
**MODE-3 EXIT-CRASH ROOT CAUSE NARROWED (no code landed):**
- Mode-3 N01 still crashes post-BUG-6 (correct output `=S`, then SIGSEGV at `0x418d68`). Mode-4 N01 passes clean. BUG-6 fix IS in effect for both modes (confirmed: no `sub rsp,80` in new asm). The crash is in the ELF finalizer (`_fini` region), triggered by `exit()` from main_γ — but gdb break-on-exit never fires, meaning the crash occurs DURING `call exit@PLT` execution, before glibc's `exit()` body runs.
- **The crash is NOT from rbp corruption:** at mode-3 main_γ, `rbp = 0x7fffffffe900` (valid stack addr), `rsp = 0x7fffffff9bb0`. `mov rsp,rbp` → rsp=0x7fffffffe900; `pop rbp` reads `[0x7fffffffe900]`. If that slot holds a code address (`0x418d68`), rbp becomes garbage → subsequent `call exit@PLT` builds a frame with corrupt rbp → glibc dtor walks corrupt rbp chain → SIGSEGV at `loopne` (a data byte in the fini array interpreted as code).
- **`[main_rbp]` corruption hypothesis:** main_α_body does `push rbp; mov rbp,rsp; sub rsp,8`. `[rbp] = caller's rbp` (libc startup frame). Something during the pattern match overwrites `[rbp]` = `[main_rbp]`. The MATCH_BEGIN PATCTX save is at `[rsp+40]` where rsp=cas_rsp_mark=statement_base-16=main_rbp-24. `[rsp+40]=[main_rbp+16]` — above main's saved-rbp slot, safe. But the pump alignment saves (`push r14/r15/r13; sub rsp,8` at cas_rsp_mark level) go BELOW cas_rsp_mark into the hfc carve region, NOT above — safe.
- **MONITOR protocol required:** Per RULES.md, mode-3 vs SPITBOL oracle divergence is found by `scripts/test_monitor_2way_sync_step_bin.sh N01.sno`. First diverging event names the land mine. Next session should lead with this.
- **N05 infinite loop** (ARBNO β reads wrong rsp post-MATCH_END) is a separate structural issue — MATCH_END's CAS restore resets rsp to original_rsp, then β at `n12_match_arbno_β` reads element header from wrong depth. Separate from BUG-6.
**WATERMARK at close:** m3 **109/2/0/32R** · m4 **117/4/0/22R** · 0 REGRESSION both modes. Push owed (credential).
**NEXT SESSION ENTRY POINT:** Run monitor on N01: `bash scripts/test_monitor_2way_sync_step_bin.sh /home/claude/corpus/probe/bb/probes/N01.sno` to find first mode-3 divergence from oracle. Identify the corrupt memory write. Also investigate N05 infinite-loop separately — the MATCH_END→ARBNO-β rsp continuity after the CAS restore.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s24 (Sonnet 4.6 — M-2 BUG-6: PATCTX-DESCENT/ASCENT deleted + BRACKET-GATE !op_tail removed; SCRIP HEAD `e5b81213`; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open:** m3 **109/2/0/32R** · m4 **110/4/0/29R** · 0 REGRESSION both modes (1 probe improved vs s23 from parallel ICN-FR-5/ZK-2 commits).
**M-2 BUG-6 LANDED `e5b81213` (bb_match_end.cpp, m4 +7 probes: 117/4/22R):**
- **PATCTX-DESCENT deleted:** `IF(hfc() && ZC_FRAME_RSP, x86_zclaim(80))` in `release_pump()` success path removed. Root cause: commit `9368fac6` introduced the 80B descent expecting MATCH_BEGIN had done `sub rsp,80` BEFORE the PATCTX saves — that reorder was subsequently superseded (current MATCH_BEGIN does `sub rsp,32` AFTER the saves, `cas_rsp_mark = pre-32B-carve rsp`). After CAS restore rsp=cas_rsp_mark, FRQ(48..72) already resolves correctly with NO descent. The 80B descent was reading 80B below the save addresses → garbage rbp/r13/r14/r15 restores → crash at main_γ `pop rbp`.
- **PATCTX-ASCENT deleted:** `IF(hfc() && ZC_FRAME_RSP, x86_zrelease(80))` paired deletion.
- **BRACKET-GATE !op_tail exclusion removed:** `IF(_.op_dval == 0.0 && _.flat_deep_arrival && !_.op_tail, ...)` → `!_.op_tail` dropped. PAIR(2) RBPRAWD reads fire BEFORE MATCH_END α; rbp restore in release_pump is safe for tail candidates too. ARBNO omega L(2) (exhaust only) is NOT the right restore site for first-match-success path.
- **Mode-4 effect:** +7 probes (N01/N03/N04/N08/N09/N10 + 1 other class). **Mode-3 unchanged (32R):** the JIT binary path crashes in a DIFFERENT way — mode-4 N01 exits 0 clean, mode-3 N01 still segfaults. The mode-3 crash is NOT the DESCENT/ASCENT bracket — it lives elsewhere in the mode-3 JIT path (different encoding of the same template logic, or a mode-3-specific frame setup issue).
**WATERMARK at close (m4 only confirmed post-fix):** m3 **109/2/0/32R** (unchanged) · m4 **117/4/0/22R** · 0 REGRESSION both modes. Regen ×3 done.
**NEXT SESSION ENTRY POINT:** Diagnose why mode-3 N01 still crashes after mode-4 fix. The two-path theory: (a) mode-3 JIT encodes via `x86_zclaim`/`x86_zrelease` differently from text asm — confirm the deleted `x86_zclaim(80)` in `release_pump()` also suppresses the mode-3 binary encoding, or (b) a mode-3-specific issue. Quick probe: run N01 under `--run` with `SCRIP_ZD_GAP=1` or a printf at the mode-3 exit path to confirm whether rsp/rbp are correct on entry to main_γ. The mode-3 N05 infinite loop (ARBNO β reads wrong rsp after MATCH_END) is a separate structural issue that mode-4 also likely hits for ARBNO-extension cases. Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s23 (Sonnet 4.6 — M-2 BUG-5 DIAG: fpl+PAIR(2) applied; SCRIP HEAD `29c89b9a`; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open AND close:** m3 **108/2/0/33R** · m4 **106/4/0/33R** · 0 REGRESSION both modes.
**CURSOR HYPOTHESIS FALSIFIED:** `x86_zc_frame() != ZC_FRAME_RSP` was wrong. Tail arm selected for all 33R.
**fpl FIX `40665ab9`:** `fct_rsp_range` (zd_k semantics inline) replaces `fc_geom` sum. N01: fpl 0→16.
**PAIR(2) RBPRAWD applied `29c89b9a`:** `RBPRAWD(RBP_HDR+0/4)` at PAIR(2) entry/yield cursor. Verified same bytes as `trd()` at element yield frontier — PAIR(2) fires at body-yield depth, NOT after MATCH_END CAS restore. Formula: N05: FPL=32 KA=352 HDRA=304 → RBP_HDR=-80; [rbp-80]=entry cursor ✓.
**M-2 BUG-5 ROOT CAUSE REFINED (33R failure modes):**
- **N01/N03/N04/N10 (correct output, crash exit):** Match succeeds, output printed, crash in post-match cleanup path. Post-match stack corruption from earlier in the ARBNO extension flow.
- **N05 (infinite `a a a...`):** ARBNO extension β reads element header from wrong rsp. After MATCH_END CAS restore (`mov rsp,[r10+8]`=original_rsp), β at `n12_match_arbno_β` reads `[rsp+308]` (element yield cursor) where rsp=original_rsp — but the element is below that. Reads dirty stack → gets 0 → r14d reset to 0 → infinite match at position 0.
- **N07 (PASS):** No ARBNO extension needed (epsilon match only).
**NEXT SESSION ENTRY POINT:** The β extension path reads element header fields (`[rsp+HDRB+4]` = yield cursor, `[rsp+HDRB+0]` = entry cursor) at the post-MATCH_END-whack rsp (=original_rsp), which is wrong. The element is at `[original_rsp - (FPL+KA)]`. β reads happen BEFORE `sub rsp, op_sb` — so at β entry rsp should be the OLD element yield frontier, not original_rsp. Investigate: does MATCH_END's σ route correctly restore rsp to the element level before invoking β, or does it leave rsp at original_rsp? The CAS restore in MATCH_END's σ path (`mov rsp,[r10+8]`) resets rsp to original_rsp, then MATCH_BEGIN's loop increments the start_δ counter and re-executes the head. This means ARBNO extension goes through a FULL HEAD RE-EXECUTION (MATCH_BEGIN α again), and the β path is only triggered when MATCH_END calls ARBNO's β label directly. That routing mechanism is the next investigation target. Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s22 (Sonnet 4.6 — M-2 BUG-3+BUG-4 landed, ARBNO root cause fully diagnosed; SCRIP HEAD `cdc42711`; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open:** m3 **61/2/0/80R** · m4 **106/4/0/33R** (matches s20; RC-1 adds 2 xfails).
**M-2 BUG-3 LANDED `1bbe3370` (+47 probes, 80R→33R):** `zd_plan` zdh_match guard was `fc_head_fp>=0` (hfc-only). All non-hfc classes (G/H/D/N/X) had `zdh_match=-1` → STATEMENT_END used full accumulated depth → RSP overshoot → stack smash. Drop the guard: every IR_MATCH_BEGIN in an armed ZD run records zdh_match. Mech2 MATCH_BEGINs don't enter armed ZD runs. Gate: m3 108/2/0/33R.
**M-2 BUG-4 LANDED `2829ce8e` (MATCH_END hfc mirror, no probe movement):** MATCH_END's op_fc_wbytes gate was `fc_head_fp>=0`-only, missing the `|| fc_tail_head` arm MATCH_BEGIN uses. Fixed. No probe movement because remaining 33R are ARBNO/DEFER with a deeper structural issue.
**WATERMARK at close:** m3 **108/2/0/33R** · m4 **106/4/0/33R** · 0 REGRESSION both modes. Regen ×3 done.
**REMAINING 33R (full diagnosis):**
- **N-class (20) + X-class (2) = 22 ARBNO:** Structural mismatch between MATCH_BEGIN's HFC window and ARBNO's chain arm. Root cause (fully diagnosed, fix direction open): `fc_tail_head` is fin-blind — returns 1 as soon as `fc_tail_candidate` is registered (before finalization). MATCH_BEGIN sees `fc_tail_head=1` → fires `sub rsp,80` (HFC window, sigma/delta/Delta saved at `[rsp+48/56/64]`). ARBNO's emit-time check `_tailc = fc_tail_arbno(nd)` requires fin=1 — for N01, candidate IS finalized (no defer → `_dok=1` → `fct[c].fin=1`). So `_tailc=1` and `op_tail=1` should fire `bb_match_arbno_tail()`. BUT THE ASSEMBLY SHOWS THE CHAIN ARM. Something causes `op_tail=0` at ARBNO dispatch in mode-3 — likely `x86_zc_frame() != ZC_FRAME_RSP` at ARBNO's dispatch time during in-process JIT (not yet measured). The chain arm reads `FRQ(op_off+24)` and `FRQ(op_off+32)` which under the 80B-shifted stack layout land at wrong addresses → RSP corruption → crash with unaligned RSP.
- **G11, H09/H11/H12/H13/H21 (6): FENCE+ARBNO combinations** — same ARBNO structural issue
- **D06/D07/D09/D11 (4): DEFER** — separate M-2 sub-rung; not yet diagnosed
- **A10 (1): ALT exhausts with $ trace** — carry-over
**NEXT SESSION ENTRY POINT:** Add `fprintf(stderr,"ZCF=%d\n",x86_zc_frame())` at the ARBNO dispatch `_tailc` line to confirm whether `x86_zc_frame() != ZC_FRAME_RSP` is the blocker, or whether `fc_tail_arbno` returns 0 for a different reason. If confirmed, the fix is to ensure MATCH_BEGIN's `fc_tail_head` arm fires only when the candidate will actually finalize (perhaps gate on `fc_tail_head` AND `fct[i].fin` — but that requires a new `fc_tail_head_finalized` predicate called POST-finalize). Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s21 (Sonnet 4.6 — ORIENTATION + M-2 DIAGNOSIS; no code committed; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open:** m3 **29/1/0/112R** · m4 not re-proved (cloned at `c1b0ace1`, pre-s20; s20 landed in parallel at `99ba2393`). This session opened from the s19 crater baseline.
**SESSION WORK (diagnosis only — no code committed):** Full root-cause trace of the 112 post-M-3 PATCTX regression with independent confirmation of what s20 already fixed. Key findings documented for next session: (1) The `sub rsp,80` in MATCH_BEGIN comes from the **ZW-1 universal carve hook** (`zw_carve_k(nd)` → `op_fc_bytes=80`), NOT from the template's `IF(hfc(), x86_zclaim(80))` block — that block gates on `op_fc_wbytes>0` which is 0 for most graphs (only set when `fc_head_fp(nd)>=0 || fc_tail_head(nd)`). Consequence: the template's `hfc()` checks and the `PATCTX-DESCENT/ASCENT` sub/add brackets I explored are dead code for the 112-regression population — they never fire. (2) `cas_rsp_mark` uses `rax` (pre-sub) in the working-tree version, which is correct: after cas restore rsp=VAR-level, then PATCTX restores via FC window rebase `[rsp+off-op_fc_base]` land at the right slots IF the FC rebase was the fix. But the FC rebase is static template-time encoding — it doesn't compensate for the runtime rsp change after cas restore. (3) The correct fix requires either: (a) RBP-PIN fired after the ZW-1 sub (rbp=post-sub=depth-immune base for both saves and restores), or (b) `cas_rsp_mark=post-sub` + STATEMENT_END gpop includes the 80B. The s20 fix used a different approach (`zdh_match` tracking in zd_plan to correct gpop). (4) SPITBOL manual read: pattern-match algorithm (pp.204-208), FENCE bare vs FENCE(P) (pp.125-128), `*` unevaluated operator (pp.85-87, 122-123). All match what prior sessions logged.
**COLLISION NOTE:** s20 (`99ba2393`) landed in parallel during this session. This session's working tree stayed at `c1b0ace1` with clean diff throughout. No code conflicts.
**NEXT SESSION ENTRY POINT:** Pull --rebase to get s20's fixes (`99ba2393`), re-prove watermark (s20 claims m3 61/1/0/80R · m4 106/3/0/33R), then continue M-2 — FENCE0/FENCE1 class (G/H). MONITOR-FIRST on G04 or H02. Per s20 cursor: STF bracket graphs use HKQ addressing (depth-immune via `[rbp-N]`) and may already be correct. Regen ×3 owed (s20 codegen touched). Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s20 (Sonnet 4.6 — M-2 HFC-WINDOW: 32 probes fixed; SCRIP HEAD `99ba2393`; ⚠ PUSH PENDING credential)
**WATERMARK PROVED at open:** m3 **29/1/0/112R** · m4 **114/2/1/25R** (crater baseline, confirmed at session open).
**M-2 HFC-WINDOW LANDED `99ba2393`:** Root cause of all 112 post-M-3 m3 regressions diagnosed and partially fixed. M-3 deleted the UCLAIM head claim (`sub rsp,Kc`) which used to back MATCH_BEGIN's PATCTX fields (sigma/delta/Delta/capgen saves at `op_off+48..+72`). Post-M-3 those saves fired above entry rsp into the caller frame. Three-part fix: (1) `emit.cpp MATCH_BEGIN op_fc_wbytes 24→80` — enlarges FC window to cover all 80B of MATCH_BEGIN's ZLS locals; FC rebase `[rsp+off-op_fc_base]` steers saves from `[rsp+96..120]` (caller frame) into `[rsp+48..72]` (inside claim); (2) `bb_match_begin.cpp`: `x86_zclaim 32→80` + reorder so `sub rsp,80` fires BEFORE PATCTX saves (temporal fix — saves need the sub to have happened); CAS sentinel uses rax (pre-sub snapshot) for `cas_rsp_mark`; (3) `emit.cpp MATCH_END`: mirror 80B window so PATCTX restores read from the same addresses; (4) `zd_plan` Bug-2 gpop fix: track `zdh_match` at MATCH_BEGIN, use it (not accumulated `_wzdepth`) for STATEMENT_END gpop in hfc runs — cas_rsp_mark restore collapses in-bracket cells (LIT_INTEGER etc.), STATEMENT_END must only release pre-match producers.
**WATERMARK PROVED at close:** m3 **61/1/0/80R** · m4 **106/3/0/33R** · 0 REGRESSION both modes. 32 probes fixed (A01-A13 plus L/N-class pure ALT/LIT/SEQ graphs). m4 improved by 8.
**REMAINING 80R CLASSES:** D-class (DEFER/`*VAR` — ZWS frame protocol), G/H-class (FENCE0/FENCE1 — STF bracket), N/X-class (ARBNO — own K-allocation with saved_rsp/saved_rbp). Each has its own frame protocol separate from the hfc window fix; each is a separate M-2 sub-rung.
**NEXT SESSION ENTRY POINT:** Continue M-2 — next sub-rung is FENCE0/FENCE1 class (G/H failures). MONITOR-FIRST on G04 or H02. These use the STF bracket (`flat_stmt_frame=1`, HKQ addressing) — check if their PATCTX saves correctly use `HKQ` path (which is depth-immune via `[rbp-N]`) or also broken. Then D-class (DEFER with ZWS), then N/X (ARBNO). Regen ×3 owed (codegen touched). Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s20 (Sonnet — CAPGEN-ERAD landed per Lon ruling)
**g_cap_gen save/restore DELETED from MATCH_BEGIN α + both ctx_restore exits + rtx_match.S/C twin (SCRIP `CAPGEN-ERAD` commit).** Lon's ruling proved out: CAS-regime graphs (sole regime) scope captures structurally via the R12 LIFO — roman consulted the stamp ZERO times (0 rt_cap_* sites; 55 r12 CAS ops). Stamp's only consumer = the flat rt_cap lane (bb_match_capture COND/IMM arm, 6 legacy demos + arbno cell self-heal), which keeps its per-match bump but LOSES nested-restore until M-2 re-homes it to CAS — fold the lane's deletion into M-2 routing. HKQ(4)/FRQ(+72) slot = dead pad (CAS-SENTINEL-CLEAN precedent). Gates: probe m3 BYTE-IDENTICAL 29/1/0/112R · m4 114/3/0/25R set-identical + 3 failure-MODE improvements (D05 xfail CRASH→clean · D09 CRASH→clean with partial output). Regen ×3 (5+40+demo files, all deletions). ⚠ s235's "gate blind to failure-mode changes" — here modes CHANGED and are recorded. **NEXT: RUNG M-SLEN S0 (Lon-routed, below).**

## ⛔⭐⭐⭐ RUNG M-SLEN — LON DIRECTIVE (routed 2026-08-08; standing order ≥2 months old, finally executed — the s220 "NEEDS LON'S ROUTING" park was directive-tracking rot): POPULATE DESCR.slen AT ASSIGNMENT + MATCH_BEGIN = REGISTER-ONLY, ZERO C CALLS
**End state (Lon verbatim intent):** MATCH_BEGIN reduced to "a minimum of register manipulation and NO C call" — save/restore bracket {RBP where deep, R12 CAS discipline, R13/R14/R15 outer Σ/δ/Δ} + `mov r13,[cell.s]` · `mov r15d,[cell.slen]` · `xor r14d,r14d` · CAS sentinel. `rt_match_enter` DELETED (its five jobs: gen bump=dead post-CAPGEN-ERAD · dcap init=relocated · coercion/VARVAL=lowerer spine · Σ/Σlen mirror=inline stores→RTCC block · LENGTH=slen field). Exits likewise: `rt_match_ctx_restore` call replaced by two inline Σ/Σlen mirror stores; both C bodies tombstoned (rtx_match.S tombstone precedent).
- [ ] **S0 AUDIT + BASELINE.** Re-prove watermark both modes. slen consumer census: (a) ⛔ DT_N DISCRIMINATOR OVERLOAD — descr.h:109-113 keys NAMETRAP/VARREF on slen∈{2,1,0} over DT_N ⇒ populate is **DT_S-SCOPED ONLY**; (b) every `.slen==0`-means-unknown reader (grep runtime+templates+rtx .S) — the standing "no fast arm keyed on slen!=0" rule INVERTS to slen-is-truth, each reader ruled; (c) mint census: STRVAL vs BSTRVAL vs concat/substring/coerce result sites.
- [ ] **S1 POPULATE.** Every DT_S mint carries true length: STRVAL strlens ONCE at mint (per-mint replaces per-match — 2,000,001 strlens/run on pattern_bt_deep → mint count); length-knowing producers (concat, splice, LEN capture, int→str) write it directly. Killswitch `SCRIP_SLEN=0` restores mint-zero byte-identical. Instrument: slen==0 count at match enter must read **0** on pattern_bt_deep; gate = probe suite ≥ floor BY SET both modes.
- [ ] **S2 SUBJECT MATERIALIZATION.** Lowerer guarantees subject cell is materialized DT_S before MATCH_BEGIN — INT/REAL/NAME subjects get COERCE_STRING+deref on the spine (bb_coerce_string exists). MATCH_BEGIN precondition BY CONSTRUCTION: `v==DT_S && s!=NULL && slen valid`. MONITOR-FIRST on first divergence.
- [ ] **S3 INIT RELOCATION.** `rt_dcap_lazy_init` → scrip.c startup beside `rt_gcheap_warmup` (W1-GC-WARMUP precedent, both entry sites).
- [ ] **S4 MATCH_BEGIN REWRITE — Done and Done.** Template emits the register-only α above; `rt_match_enter` call deleted; exit `rt_match_ctx_restore` calls → inline Σ/Σlen stores; both C/asm bodies tombstoned. Gates: probe both modes ≥ floor BY SET · roman + length-family witnesses (RPOS/RTAB/REM/LEN) · MONITOR-FIRST on first red · regen ×3 · killswitch-OFF byte-identical until fold.
- [ ] **S5 STRIKE THE PARK.** GOAL-SNOBOL4-RTX slen row = pointer here (done in routing commit); invert the no-slen-fast-arm standing rule tree-wide; FINDING + both cursors.
(MECH s20 cursor: HFC-WINDOW landed 99ba2393; m3 112R->80R; 32 fixed; next FENCE class)

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s19 (Fable — MERGE s12-fable-m3: UCLAIM DELETION ON MAIN; SCRIP `3c5c1f71`, corpus `9c2e825f`; ⚠ PUSH PENDING credential)
**THE BRANCH IS MERGED.** M-3 (UCLAIM physical deletion `1351d299`) + M-3-FIX (`15d2af87`) + WL-DELETE (`b455c491`) now on main via merge `3c5c1f71`. Resolution (by the merging session, per protocol): branch deletion wins; rewoven main-side fixes = ICN-FR-5 suspend twin (field kept, staged 0 — zd_uk source deleted, ICN-FR owes recarve re-plumb from cells-world depth), PL-ZK-4 DISJUNCTION K=0, PL-ZK-3 pl_cells_graph arm+release suppress. Excised claim-world consumers: rpin() ARMED-PIN disjunct (bb_match_begin), body_carve compensation (bb_match_end), zd_uk suspend staging (emit.cpp).
**CRATER BASELINE (honest, GATE POLICY):** 141-probe m3 **29/1/0/112R** · m4 **114/2/1/25R** (pre-merge 141/1/0/0 · 139/3/0/0). WL-DELETE's own base said 29-pass/106R — delta is the 25 post-branch main commits' probes riding the flip. D06 XPASS→XFAIL-removal (`9c2e825f`) predates the merge and rides the crater like everything else.
**NEXT SESSION ENTRY POINT:** M-2 route-the-homeless on the merged tree — the 112R m3 set is the work queue; MONITOR-FIRST on the first ALT-class regression (A01). Regen ×3 owed (codegen touched). Push owed (credential).

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s17 (Sonnet 4.6 — BUG-1+BUG-2+D07-COLLISION: pb_snapshot_imm PASS; G24/H20 PASS; SCRIP HEAD `f18bcd87`, corpus `62393e5b`)
**WATERMARK PROVED:** m3 **137/5/0/0** · m4 **139/3/0/0** · 0 REGRESSION both modes. Proved at open AND close (up from s16 135/7: G24+H20 promoted from xfail to pass as XPASS).
**COLLISION RESOLVED (D07 re-regression from RTX parallel commit `55c045eb`):** RTX commit added a `TT_DEFER` arm to `sno_pat_node TT_LEN` in `lower_snobol4.c` that routed `LEN(*N)` through `sx_lower → case TT_DEFER → SNO$MKEXPR` (pattern builder), breaking D07. Fix: removed the TT_DEFER arm; `sno_pre_req` (which already unwraps one `*` level at its own line 1006) is the correct path for both bare `*var` and `*(arith)`. D08 (N set mid-match via immediate assignment) remains XFAIL — the RTX commit was incomplete anyway. Concurrency-protocol note: collision resolved by the rebasing session as required.
**BUG-1 FIX (PATCTX saves into caller frame):** `stmt_has_proc_invoke()` in `bb_match_begin.cpp` narrowed to exclude PATV$-prefixed pat_static=1 DEFER nodes. Those are BUILD-snapshot inlines (rt_defer_open slow-path: `push r14/r15/r13 + call`; no separately compiled proc blob with `sub rsp,K; mov rbp,rsp` prologue). Excluding them lets the ARMED-PIN fire for pb_snapshot_imm-class graphs: rbp is pinned to claim_base so `FRQ(op_off+88..120)` PATCTX saves land inside the 144B claim region, not in main's activation frame. True recursive `*P` DEFER nodes (non-PATV$ prefix) still trigger the guard — D10/D11/H21 confirmed safe. But note: D10/D11/H21 are all `*P` where P is also a PATV$-prefixed snapshot — so they ALSO benefit from the fix and now PASS (they were already dropped from XFAIL at `abdc4c9c`). G24 (FENCE0 with deferred `*P`) and H20 (FENCE1 over deferred `*P`) are same class → PASS → dropped from XFAIL.run.
**BUG-2 FIX (gpop over-count by 16):** `emit.cpp` `zd_plan()` line ~2107: extended `_wzdepth = zdh` selection to cover `!zws && !zwr && Kc>0 && r>hpos`. MATCH_END's `mov rsp,[rbp+64]` (zls2_mark restore) collapses all interior-cell depth back to claim_base; STATEMENT_END must use `zdh` (pre-head producer depth=32) not `zd` (=48=zdh+SAVE.K(16)), or `add rsp,N` over-releases by 16 into the caller frame. pb_snapshot_imm: `add rsp,192 → add rsp,176`.
**MONITOR NOTE:** monitor `spl vs scr` on pb_snapshot_imm still shows DIVERGE at step 4 (`PATV$0='Z'` vs `VALUE X='A'`). This is trace-semantic: SCRIP surfaces the pre-chain NV_SET(PATV$0←'Z') as a named-variable event; SPITBOL implements the BUILD snapshot internally and only surfaces the `$ X` immediate-assign result. Outputs agree, clean exit 0 both modes. Not a bug.
**NEXT SESSION ENTRY POINT:** M-2 rung — route the homeless. (a) Sealed-DEFER graphs (roman's class): nested-capable frame protocol — PASS-THRU glue at DEFER entry gains a FRAMED variant. Monitor-first on roman≥10 (the recursion witness). (b) no-END runs: pure per-box K. (c) zdyn veto tree routing to (a). The pb_snapshot_imm / BUILD-snapshot class is now fully resolved — next defect class is true recursive `*P` re-entrancy.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s16 (Sonnet 4.6 — PB-2 COMPLETE: D10/D11/H21 ARMED-PIN DEFER-guard; SCRIP HEAD `6bda3acb`, corpus `abdc4c9c`)
**WATERMARK PROVED:** m3 **135/7/0/0** · m4 **139/3/0/0** · 0 REGRESSION both modes. Proved at open AND close.
**PB-2 COMPLETE — ARMED-PIN DEFER-guard landed `6bda3acb`:**
Root cause of D10/D11/H21 regressions from WIP-PARK (`6899b18f`): IR_MATCH_DEFER compiles a PAT$ proc blob (proc_PAT$0) whose prologue does `sub rsp,K; mov rbp,rsp` and whose omega does `lea rsp,[rbp+K]; mov rbp,[rbp+88]; jmp rax`. Under the ARMED-PIN (rbp==claim_base at MATCH_BEGIN), the proc's saved-rbp slot `[rbp+88]` holds claim_base; proc omega then executes `lea rsp,[claim_base+K]`, pointing rsp INTO the claim region → SIGSEGV. Without the pin (rbp==ambient CRT frame), the proc omega's rsp restore lands in normal stack territory. Confirmed: all three probes (D10: ARBNO(*P) with SAVE; D11: ARBNO(*P) with ALT; H21: FENCE1 over deferred *P) contain IR_MATCH_DEFER. Fix: added `stmt_has_proc_invoke()` scan helper (O(n) over g_emit_cfg->all[], checks IR_MATCH_DEFER only — IR_MATCH_VALUE excluded as it uses rt_dcap_pump, not a compiled proc blob) and `!stmt_has_proc_invoke()` conjunct to rpin()'s ARMED-PIN arm. IR_MATCH_VALUE exclusion confirmed correct: pb_snapshot_imm has `$ X X` where second X is DEFER (not VALUE), and pb_snapshot_imm's rc=134 stack-smash is the PRE-EXISTING Bug 1/Bug 2 from s15 cursor — present at HEAD before any s16 changes.
**m4 BONUS FIXES (parallel session between s15 and s16):** F02 G15 N07 N08 N09 N16 N21 all XPASS in m4 → dropped from XFAIL.compile. m4 watermark improved 132/10/0/0 → 139/3/0/0.
**pb_snapshot_imm STATUS:** still crashes rc=134 (correct output `S A` then `__stack_chk_fail` SIGABRT) in BOTH ARMED_PIN=0 and ARMED_PIN=1, at HEAD before s16 changes. The s15 two-bug diagnosis stands — Bug 1 (PATCTX saves into caller frame via FRQ under un-pinned GLUE-O rbp) and Bug 2 (gpop over-count by 16 in emit.cpp:2105) remain open. The s15 stash (pctx_rsp read-side for bb_match_end/bb_match_replace) does NOT persist (ephemeral container).
**NEXT SESSION ENTRY POINT:** (1) pb_snapshot_imm Bug 1: widen rpin() for the static-extent non-DEFER case — the ARMED-PIN now correctly fires only on DEFER-free graphs, so the next step is to widen it to cover the PATCTX-save population (Kc>0 graphs without DEFER, where FRQ under ambient rbp lands in caller frame). Monitor-first on pb_snapshot_imm m3. (2) Reconstruct the pctx_rsp() read-side from the s15 cursor description (bb_match_end.cpp + bb_match_replace.cpp). (3) Bug 2: gpop fix in emit.cpp ~2105 (`zdh` instead of `_wzdepth`). All three go in as one commit.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s15 (Sonnet 4.6 — pb_snapshot_imm diagnosis; SCRIP HEAD `69c476a0`, corpus `e74c0a00`)
**WATERMARK PROVED:** m3 **135/7/0/0** · m4 **132/10/0/0** · 0 REGRESSION both modes. Proved at open AND close.
**H-CLASS RESOLVED:** All H-family probes (H01–H30) are 100% green in both modes — prior sessions fixed them all. The s11 "H-class as next entry point" was superseded. H20 remains xfail.run only (correct output then SIGSEGV on teardown — pre-existing class).
**pb_snapshot_imm ROOT CAUSE TRACED (two bugs, neither fixed this session):**
Bug 1 — PATCTX SAVES INTO CALLER FRAME: MATCH_BEGIN's normal arm (no ZWS/ZWR frame) emits `FRQ(op_off+48/56/64/72)` = rbp-relative PATCTX saves, but `rpin()` doesn't fire for this graph because `flat_layout_unknown=0` (ZD planner knows Kc=144 statically) and no jepin. rbp is the caller's (main's) frame. Writes land at `[caller_rbp+96/104/112/120]` — inside main's activation, corrupting main's stack canary. Confirmed in m4 asm: `sub rsp,144` then `mov [rbp+96],r13` with rbp=main.rbp.
FIX ATTEMPT: added `|| (_.flat_deep_arrival && !_.op_zw && !_.op_zw2)` to `rpin()` in `bb_match_begin.cpp`. This correctly seeds rbp=claim_base before PATCTX saves. BUT it caused 3 new regressions (D01/D11/H21: correct output then SIGSEGV). Investigation: a prior session had already left PARTIAL companion work uncommitted in `bb_match_end.cpp` + `bb_match_replace.cpp` — a `pctx_rsp()` RSP-relative read function for the MATCH_END/REPLACE PATCTX restore side. These uncommitted changes were left in the working tree (NOT stashed), causing 45-46 regressions when discovered. STASHED as `s15-PATCTX-OIN-WIP`. The rpin() change was reverted. SCRIP working tree is now clean at origin HEAD.
Bug 2 — GPOP OVER-COUNT: ZD planner computes `zgpop[STATEMENT_END] = _wzdepth + Kc = 48 + 144 = 192` where `_wzdepth=zd=48=zdh(32)+SAVE.K(16)`. But MATCH_END's `mov rsp,[rbp+64]` (zls2_mark restore) brings rsp to claim_base, already releasing SAVE's 16 bytes. Correct gpop = Kc + zdh = 144 + 32 = 176. Over-release by 16 = `add rsp,192` overshoots α by 16, corrupting the caller's frame. Additionally, `[claim_base+88]` (old_rbp slot) gets overwritten with `0x433fd0` (`.data` address of `"PATV$0"` string literal) during match execution — exact write site not pinned before context ran out.
**WHAT'S NEEDED (two-part fix, MECH rung):**
(a) Companion set: `bb_match_begin.cpp` rpin() widening + `bb_match_end.cpp`/`bb_match_replace.cpp` pctx_rsp() RSP reads — the stash holds the read side; the write side needs the rpin() change re-landed carefully with the regressions understood. The D01/D11/H21 regressions from rpin() need diagnosis: likely the old_rbp slot (`[claim_base+88]`) is aliased to something else's valid data in those programs when rpin() seeds rbp=claim_base, and the PATCTX restores then corrupt that data. Must check op_off+40 in each regressing program.
(b) ZD planner gpop fix: in `zd_plan()` at `zgpop[i] = _wzdepth + kc` (emit.cpp ~2105), when `!zws && !zwr && r > hpos` (non-framed path, post-head node), use `zdh` instead of `_wzdepth` so zls2_mark-released interior K is not double-counted.
**WIP NOTE:** Stashes do NOT persist across sessions (container is ephemeral — confirmed s15). The read-side diff (bb_match_end.cpp + bb_match_replace.cpp pctx_rsp() + PCTX(k)) is captured in full in the s15 cursor text above. Both write-side (rpin() in bb_match_begin.cpp) and read-side must go in together once the D01/D11/H21 regressions are understood. SCRIP working tree is CLEAN at origin HEAD `9562737c`.
**NEXT SESSION ENTRY POINT:** (1) `git stash pop` in SCRIP and read the stashed diff carefully. (2) Understand why D01/D11/H21 regress with rpin() — add `SCRIP_ZWS_DIAG=1` + check `op_off+40` slot aliasing for those programs. (3) Fix the two bugs together as one MECH rung commit. (4) gpop fix in emit.cpp:2105.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-08 s11 (Sonnet 4.6 — orientation + A05/A06 root-cause trace; SCRIP HEAD `7ea1ac41`, corpus `ce266950`)
**WATERMARK PROVED:** m3 **135/7/0/0** · m4 **132/10/0/0** · 0 REGRESSION both modes. (Parallel sessions since s10 landed fixes; this session proved at current HEAD — no code written here.)
**A05/A06 STATUS:** Both PASS at current HEAD (`7ea1ac41`). Root cause fully traced this session: the s10 hypothesis ("b491998e changes fc_arm_member at registration time") was incorrect — fc_arm_member is LOWER-time, untouched by the ZD planner. True mechanism: under `SCRIP_ZD_CAP=1` (default ON), SAVE/COND are admitted to the ZD arm. The ZD cross-head staging in emit.cpp:2748 fires for COND (`_xh=1`, `_xh_zdh=0`) and computes a wrong `g_zd_read=296` — but COND's run is not armed (`g_zd_arm=0`, K=0), so this read is staged but never consumed; both SAVE and COND take the flat `rt_cap_push`/`rt_cap_top` path in both media. A05/A06 passed once the stale `libscrip_rt.so` was rebuilt via `make` — the 10 regressions in s10 were a stale-binary artifact from a parallel session's incomplete push. Monitor `spl vs scr` on A05: 0 divergences, exit 0.
**SPITBOL MANUAL READ:** pp.85-87 (deferred `*`); pp.122-123 (recursive patterns); pp.125-126/222-223 (FENCE0 aborts match on backup; FENCE1 hides arm alternatives only, backup passes through).
**NEXT SESSION ENTRY POINT:** H-class (FENCE1-over-ARBNO: H08/H10/H14/H15/H23/H26/H28/H29). Monitor-first on H08. Root: `_xh` cross-head offset correct for simple LEN(*N) but wrong inside ARBNO/FENCE1 bodies that re-enter at variable RSP depth. Design per M-2(a): FRAMED glue variant (per-activation `push rbp` at blob entry) for re-entrant blob class. Run `bash /home/claude/corpus/probe/bb/run_suite.sh` (m3) and `MODE=compile bash run_suite.sh` (m4) to prove watermark before any code edit.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s10 (Sonnet 4.6 — MON-NULL-STMT + A05/A06 root cause; SCRIP HEAD `1a0b6d3e`, corpus `5b7589e2`)
**WATERMARK RE-PROVED:** m3 **125/7/0/10** · m4 **125/7/0/10** · 10 REGRESSION both modes.
**A05/A06 ROOT CAUSE DIAGNOSED:** `b491998e` broke A05/A06. Monitor confirmed divergence at step 6 — scr emits `LABEL stno=6` (failure) vs spl `VALUE W=STRING(2)='xy'`. True cause: `_xh_zdh>=0` guard in ZD staging fired on `_xh_zdh=0`, placing subject DESCR 296B into garbage above the blob; fix is `_xh_zdh>0` (landed s11).
**NEXT SESSION ENTRY POINT (superseded by s11):** H-class after A05/A06 fix.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s9 (Sonnet 4.6 — ZD-5b-LEN cross-head fix; SCRIP HEAD `b491998e`, corpus `5b7589e2`)
**WATERMARK RE-PROVED:** m3 **125/7/0/10** · m4 **125/7/0/10** · 10 REGRESSION both modes.
**10 REGRESSIONS (pre-existing M-2(a) open, ALT-capture + FENCE1-over-ARBNO class):** A05 A06 H08 H10 H14 H15 H23 H26 H28 H29. Root: static `_xh` cross-head offset is wrong inside ARBNO/FENCE1 blob re-entries (those re-enter at variable RSP depth). Routing them to FRAMED glue (per-activation `push rbp` at blob entry) is the M-2(a) sequel.
**D07 FIX (LEN(\*N)/LEN(N) with variable operand) landed `b491998e`:** Three-part fix in `src/emitter/emit.cpp` zd_plan. (1) `claim[k]!=hi` guard in blob-closure Kc loop — pre-head run members had `cm[k]=1` and `zuh[k]=-1`, letting the Kc assignment stomp their `zud` and break `_xh` detection. (2) `zud[hi]=zdh` store — run-head STATEMENT_BEGIN now holds `zdh` (depth before ZW_FRAME_TOTAL insertion) so the `_xh` formula recovers the exact Kc+ZW crossing offset. (3) Revised `_xh` staging — `_xh_zdh = zd_ud[zd_uh[i]]`; guard `(zd_out[i]-_xh_zdh+K)>0`; correct `ZOPQ = zd_ud[consumer] - K + zdh - zout[producer]`. D07 PASS, dropped from XFAIL.run (`corpus 5b7589e2`).
**SESSION NOTES:** Orientation-only open (no credential until end). Repos cloned: SCRIP `682016d6`, corpus `19503fcb`. Rebase at push absorbed SCRIP `01440ed4` (ZK-34-FIX) and corpus `b592b8ac`/`7e9e152b` (XFAIL cleanup). SPITBOL manual read: pp.86-87 (deferred `*`, type coercion — confirms `rt_coerce_int_d` coerces string N to int, so LEN(*N) is valid), pp.122-123 (recursive patterns, `*` sole vehicle), pp.125-128 (FENCE0/FENCE1 semantics). Full root-cause trace of D07 through ZD depth model; identified three-part fix already in WIP working tree; verified fix + cleared XFAIL.
**NEXT SESSION ENTRY POINT:** M-2(a) sequel — fix the 10 regressions. Class: ALT capture (A05/A06) and FENCE1-over-ARBNO (H08/H10/H14/H15/H23/H26/H28/H29). The `_xh` cross-head offset is correct for simple LEN(*N) but wrong inside ARBNO/FENCE1 bodies that re-enter at variable RSP depth. Design: MONITOR-FIRST on A05 to find divergence; likely needs FRAMED glue variant or per-node RSP-depth annotation for re-entrant blob members. D08 (immediate-assign N via `SPAN(...) $ N LEN(*N)`) and D02-D04/D06 (*P deferred re-evaluation) are separate open tracks after A05-class is resolved.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s8 (Sonnet 4.6 — ORIENTATION SESSION; emit.cpp comment fix; SCRIP HEAD `682016d6`, corpus `19503fcb`)
**WATERMARK RE-PROVED:** m3 **134/8/0/0** · m4 **126/16/0/0** · 0 REGRESSION both modes. (m4 shows 126/16 vs prior 124/18 — two additional probes pass due to PL-FR/ICN-FR/ZWS fixes that landed between s7 and this session; no regressions.) HEAD-stamped `682016d6`.
**SESSION NOTES:** Orientation-only session (no credential until end). Repos cloned: SCRIP `6a87662b` (WIP HEAD), corpus `19503fcb`. WIP HEAD had unterminated comment in emit.cpp line 2096 (M-1-FIX-3 comment: spurious early `*/` before `blob-interior` continuation) — pure comment fix, zero behavioral change, committed `682016d6`. Watermark re-proved clean. SPITBOL manual read: p.86-87 (BUILD vs deferred `*`), p.122-123 (recursive patterns), p.125-128 (FENCE/FENCE(P)). CLIMB cursor read: two MECH cross-requests confirmed — (CR-1) D02/D03/D04 lazy `bb_compile_pat_tree_sz` during live match; (CR-2) D06/D07/D08 `rt_defer_get_pat_fn` rejects integer N for `LEN(*N)` — manual p.87 requires coercion. Both are M-2(a) routing work.
**PREVIOUS CURSOR (s7, 2026-08-08):** m3 **134/8/0/0** · m4 **124/18/0/0**. M-0 re-proved. ZWS-FENCE0-UCLAIM fix + XFAIL.compile baseline landed `bff5604e` (not on origin at clone time — absorbed into current HEAD via subsequent commits). M-2(a) root cause diagnosed.
**NEXT SESSION ENTRY POINT:** M-2(a) — two fix tracks, do in order: (1) CR-2/D06-D08: `rt_defer_get_pat_fn` in `src/runtime/snobol4/pattern_match.c` — add integer coercion branch after DT_P check (INTVAL_fn → pat_len); MONITOR-FIRST on D06. (2) Roman/PATV$: per-activation PATV$ storage in `lower_snobol4.c`/`pattern_match.c` — suffix PATV$ names with per-DEFINE-call-frame counter OR store in FRQ-relative frame slots; MONITOR-FIRST on roman≥10. Seal removal blocked until CR-1 (D02-D04 lazy-compile) also resolved.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s7 (Sonnet 4.6 — A05/A06 m4 FIXED by CLIMB; SCRIP `7ca07e8f`, corpus `82f127c7` — push pending credential)
**A05/A06 m4 RESOLVED (CLIMB s7):** `x86_zop` regime-4 else arm gains `_m2=(op_mech2_kc>0 && !x86_fc_on())?8:0` — mech2 blob non-own-cell nodes (`ASSIGN_SAVE` push path, `ALTERNATE` state slots) now emit `[rsp+off+8]` = `[α-Kc+off]` (the designed blob address). Root: `push rbp` in mech2 boundary shifted RSP by −8 before `sub rsp,Kc`; `x86_zop` regime-4 emitted `[rsp+off+0]`, missing the +8. `op_flat_disp=0` (CARVE-DATA-ERAD), `op_zdepth=0` (no own carve), `zvo_resolve` skipped (mech2 suppresses UCLAIM), `x86_fb_data()=false` (`op_fb_rbp=0` blob-interior) — all four paths to the correction were dark. m3 survived by main-stack slack; m4 CRT stack put the +0 address into unmapped memory. Fix: FINDING-2026-08-07h. 1-line `x86_asm.h`. XFAIL.compile: A05 A06 removed corpus `82f127c7`. Watermark post-fix: m3 **134/8/0/0** · m4 **131/11/0/0** · 0 REGRESSION. F02/G15 m4 xfail unchanged (UCLAIM pinned-rbp path, different class).
**NEXT SESSION ENTRY POINT:** M-2(a) — roman witness (rc=0, already passes). Sealed-DEFER graphs: nested-capable frame protocol — PASS-THRU glue at DEFER entry gains a FRAMED variant. See M-2 rung definition.

## ⭐⭐⭐ LIVE CURSOR — 2026-08-07 s5 (Sonnet 4.6 — M-1 DONE; SCRIP `14ee075d`, corpus `a60ce7c6`)
**M-0 CONFIRMED:** m3 **128/14/0/0** · m4 **123/19/0/0** at open. No drift.
**M-1 COMPLETE: all named probes fixed.** A05/A06 m4 remain XFAIL.compile (separate open item, M-1 cursor defined them as XFAIL — exit condition met).
Fix 1 (`8e46f05f`): mech-2 MATCH_END reads `r12` not stale `[RT_DCAP_TOP]` → H14/H23 PASS both modes.
Fix 2 (`e8a5f74b`): ZB-FC-3c coherence — `fc_cond_fp` checks `fc_arm_member(save)` at query time → A05/A06 PASS m3.
Fix 3 (`33e9f2f3`): ZWS backward-edge wpop=0 — ZD planner forward-only guard (k>r) wrongly gave oin=0 for members whose ω is a backward edge within the ZWS run. Side-effects: F03/F05/G06/G22/G23 PASS m4.
Fix 4 (`14ee075d`): **rt_gcheap_warmup** — `gc_static_segs_init` drives `dl_iterate_phdr` which uses `movaps` (RSP 0 mod 16 required). When triggered lazily from inside mech-2 MATCH_BEGIN frame via `rt_dcap_pump→c_rt_str_alloc`, RSP ≡ 8 mod 16 → SIGSEGV. Fix: call `rt_gcheap_warmup()` from `scrip.c` after `core_lib_init()`, on normal C stack before `rt_outer_call`; `gc_static_segs_init` guard (`g_gc_nseg >= 0`) makes subsequent JIT calls no-ops. → F04 m3 PASS, G17 m3 PASS (same root). XFAIL.run updated.
**Probe close: m3 130/12/0/0 · m4 123/19/0/0** (crater 124/18 · 116/26). CREDENTIAL NOTE: PAT needed for push.
**NEXT SESSION ENTRY POINT (s5, superseded):** M-2(a) — roman witness. A05/A06 m4 SEGV resolved by CLIMB s7 (see cursor above).

## ⛔ DEFINITION OF DONE (inherits BB HQ DoD; selectors already gone)
1. ω-coverage 100% of K>0 boxes (U-GATE instrument), crosscheck+benchmarks. 2. Orphan-adds 0 outside audited whitelist. 3. UCLAIM MACHINERY PHYSICALLY DELETED (not just census 0): `zvo_uclaim_k`, owner-table declined extension, staging twins, head `sub rsp,Kc` arm — greps == 0. 4. Wall census == framed-enter count over the framed set {STATEMENT(unknown-extent) · MATCH_BEGIN(blob/mech-2) · FENCE1 · FUNCTION}. 5. Exactly THREE glue spellings tree-wide (see GLUE TAXONOMY); THE GLUE DOES NOT WHACK — grep-enforced. 6. Killswitch fold complete (M-5 list) — per-kind gate names == 0 outside this doc. 7. xc318 BY SET ≥ pre-flip baseline (m3 280 / m4 260) both modes · rc=139 tail cleared · bench 18/21 hold-or-better · regen ×4 · `rbp-op-refs` < 14198 (08-03f) and trending down.

## ⭐⭐⭐ THE MODEL (canonical, one place — consolidated from HQ s23k/08-03b/s21x-c/08-06c)
**THERE IS NO GRAPH FRAME.** Every BB `sub rsp,K` at α for its OWN result (16B DESCR) + locals; blob-interior scanner kinds are K=0 BY DESIGN (ride r13/r14/r15 + claim slots). γ NEVER frees. **THE UNWIND (4 clauses):** 1. ROLL UP — α carves own K; spine accumulates NON-POPPING. 2. ROLL DOWN — FAILURE NEVER WHACKS: box N's ω restores own cursor/CAS state, frees OWN K only, jumps to pred's β; one-shot box's β IS its ω-continuation (`add rsp,K; jmp pred_β`); RSP at β == RSP at γ by induction; the depth-knowledge problem is DELETED not solved. 3. WHACK forward-only at fenced sync (FENCE commit; MATCH_END = SN4's language fence). 4. WHACK at final success — STATEMENT_END.γ sole statement release; STATEMENT_BEGIN.β sole fail terminus (arrives at entry depth BY CONSTRUCTION). **WHACK CONTRACT (6):** two whack-free mechanisms only — DETERMINABLE `add rsp,ΣK` at final success / INDETERMINABLE empty RBP frame at the indeterminacy boundary, whack `mov rsp,rbp; pop rbp`; whack sites exactly {end-STATEMENT, end-MATCH, FENCE commit, FUNCTION return}; THE GLUE DOES NOT WHACK; STATEMENT_END sole statement whack authority (op_zgpop home); operator frees ONLY own result ONLY at ω, never operands; own α claim ~16–64B, statement head carrying whole claim = UCLAIM debt. **STATEMENT-PORT LAWS:** fail → BEGIN.β (release may whack-free BY ADD — unwind + re-base guarantee rsp==claim base on arrival); success → END.α; BEGIN frames ONLY unknown-extent statements. **SCOPE LAW:** statement level only, no function-level processing.

## ⭐⭐ FRAME CENSUS — which constructs require a frame (RBP-MINIMALITY LAW)
RBP fires ONLY where extent is runtime-indeterminable. **THE SET (refined 2026-08-06, ARBNO dropped): {STATEMENT (unknown-extent only, mech-1 `add rsp,ΣK` otherwise) · MATCH_BEGIN (blob/mech-2 case: `push rbp; mov rbp,rsp`, whack at MATCH_END/head-ω) · FENCE1 (law-4 watermark quad, depth-free `[rbp+off]`) · FUNCTION (DEFINE call frame)}.** Frames INDEPENDENT, nested via saved-RBP chain (nested indeterminacy = nested frames, HQ 2026-08-03); interiors `[rbp+8+off]`, never rsp+uclaim phantoms. **ARBNO IS FRAMELESS** (Lon's intuition CONFIRMED — ARB-LON-K16 `512ff8cd`/`c7a276f6`, FINDING-2026-08-06): needs exactly ONE 16B frontier cell {Δ0 (delta-cursor at entry, σ = cursor-compare exhaustion test), yield} — no frame, no counter, no links, no chain; ALL operands rsp-relative; body-K0 class rides in place; K16 commits are non-popping (composition laws: outer-pending cells + net-delta). RO constants sealed `[rip+disp]`. **Emitter rbp pin predicate** (`rpin()` = flat_deep_arrival || flat_pat || flat_gen; + `jepin()` = flat_jmp_entry && flat_deep_arrival, X12 fix `0c5987a7`); `x86_fb()` per-graph RSP-or-RBP via `x86_fb_pinned()` ONE selector both media; ZC_FRAME_R12 arm DELETED (`da8c2347`). Every NEW rbp use must name its indeterminacy in-comment or it is a violation. Register contract (ARCH-ICON, verified): R13=Σ subject base · R14=δ cursor · R15=Δ length · R12=DCAP/CAS top (CAS-R12-UNIFY `6g`) · RBX=heap frontier (dormant under ZC_PORT_FORTH — grants spend as `sub rsp,K`).

## ⭐⭐ GLUE TAXONOMY (exactly three; the two invocation kinds Lon names + the frame pair)
1. **ONE-SHOT** — deterministic box: β ≡ ω-continuation trampoline `add rsp,K; jmp pred_β` (x86_beta_trampoline; ΣK ZPOP fold legal iff every folded β whitelisted-trivial). 2. **PASS-THRU** — transfer box adopts caller wires, zero frame: `bb_glue_pass_wires` (lea rcx,γ + lea rdx,ω + jmp rax) — DEFER, GOTO, DYNAMIC-FLAT `jmp [entry]` invocation (Design-of-Record class A). 3. **FRAMED** — `bb_glue_framed_enter/leave` push/pop rbp pair, THE ONLY RBP WRITER (Design-of-Record class B, DYNAMIC-FRAMED). Current source also carries `bb_glue_flat{,_enter,_leave}`, `bb_glue_outer_whack`, `bb_glue_wire_*` — M-4 collapses these into the three or proves each a spelling of one (outer_whack likely violates THE-GLUE-DOES-NOT-WHACK → its release moves to STATEMENT_END/MATCH_END authority).

## ⛔ LAWS & TRAPS (carried, binding; full text GOAL-SNOBOL4-BB.md — the load-bearing subset inline)
ζ gate = "zero mid-body cell" not "zero rsp" (legit raw-rsp: GLUE · C-ABI ALIGN · CSTACK SWAP · PROLOGUE). NO PROCESS-SCOPE FLAG DRIVES A GRAPH-SCOPE REGIME. POSITIVE-CONTROL + DENOMINATOR + MODEL-THE-POP (γ=zout−zgpop, ω=zout−K−zwpop). COMPARE m4 NEVER m3; BY SET never count; `setarch -R` (S231 ASLR min-rail landed); env-length controls (127/152 class — post-M-6 must be IMPOSSIBLE; any flake then is a LEAD). CENSUS SHELF-LIFE — re-run never cite; HEAD-STAMP. Artifact truth: `.s` = compiler-that-ran; sweep the COMPILER. Seal is GRAPH-scope (re-entrancy of the MATCHER, not membership). DEFER-DEEP LOAD-BEARING until recursive-defer passes (roman ≥10 is the witness). SUSPENDED-CELL: no rsp cell suspends across γ above ARBNO/DEFER extent. RESULT-IS-THE-CELL; SOLE-CONSUMER FENCE; UNION-TAG; NODE-EXACT HANDOFF; SAME OFFSET ≠ SAME OBJECT; match-bracket window = (MATCH_BEGIN, next MATCH_BEGIN). MEASURED NEGATIVES (DO NOT RETRY): frame-slot route to DCAP marker (register arithmetic `sub r12,24` is the only depth-free form) · `x86_fc_cells()=(FORTH||HEAP)` flip · RBPPAIR γ/ω mirror (1016_eval) · umin clamp for the 224 residual · naive op_zres defer-arm suppressing slow pump. STALE-BINARY TRAP (`grep error:` both logs; and ⚠ `grep -c` EXITS 1 ON ZERO — never chain `&&` off it, measured 2026-08-08 as a silently-skipped sweep). MONITOR-FIRST for every divergence (RULES.md §1); monitor dark ⇒ MON-RE first.

## ⭐ HISTORY CONSOLIDATION — authority index for every stack fact (titles are in `.github/`; one line each)
Design of record + sliding offsets s21x (DESIGN-SN4-*, STF-UNFLIP, NOFC arc: FINDING-07-31h/i — NOFC asymmetric, non-popping spine fixes 33, five pops cost 20–48%). Carve deletion s23g–s23o (artifact-truth; probe suites cannot certify emission — only `.s` region diff). ZW-12/13/15/16 canonical frame (FINDING-08-03 ZW13 dark-since-S24A nblob clause; ZW16 r12 not coherent across backtrack → CAS-R12-UNIFY `6g` one authority). MECH2-R12-FIX `590b9140`. SEQ-ERAD arc (08-04b DAG premise falsified; 08-04g fc_walk_range over-deletion; 08-05 nested-ARBNO rsp3 root cause; killswitch-law violation recorded — the no-revert-path lesson this ladder's per-slice git-revert discipline answers). ARBNO-LON frameless + ARB-LON-K16 composition laws (08-06). CAS sentinel clean `6n`. FENCE-SEMANTICS `8ab4c266` (07a; manual pp.125-126/209: FENCE0 abort-on-backup+anchors-at-head; FENCE1 p.128 pass-through, arms invisible). LADDER PB complete 07b–07d (PATREF invalid+deleted; STITCH ruling: plain-ref = BUILD snapshot per manual p.86 — `LEN(N)` captures N at construction, `*N` defers, p.122 `*` sole recursion vehicle; witnesses pb_snapshot_imm/stitch_* in corpus/probe). JE-PIN X12 `0c5987a7` + W-2a/W-2b (07-session). PB-4 measure (FINDING-07e: snapshot-DEFER false-dynamic + admitted head-claim survival — both now M-2/M-3 customers). Bug 6 diagnosis + HEAD-PIN recipe (W-1 notes, ARMED-REGIME DEBT re-measure 07e: COMBO 127/3/0/11 set A05 A06 F04 G05 G09 G17 G18 G21 H14 H20 H23). REGIME DELETION slice 1 `de837576` (this cursor).

## RUNGS (walk in order; each: own commit, buildable, git-revert path, gates vs CRATER BASELINE by set, FINDING per land mine)
- [x] **M-0 RE-PROVE** — m3 134/8/0/0 · m4 124/18/0/0 · 0 REGRESSION both modes. ZWS-FENCE0-UCLAIM fix + XFAIL.compile baseline correction landed (SCRIP `bff5604e`, corpus `c7c91186`). CLIMB cursor skew reconciled. HEAD-stamped.
- [x] **M-1 BUG 6 — STF+MECH-2 NESTED FRAME** (F04-FIX `14ee075d` — heap warmup; F03/F05/G06/G22/G23/H14/H23/A05/A06(m3)/F04/G17 all PASS m3; A05/A06 m4 resolved by CLIMB s7 `1c90fa8f` — x86_zop +8 mech2 blob correction, FINDING-08-07h). Full exit: A05/A06/F04/G17/H14/H23 all PASS both modes.
- [ ] **M-2 ROUTE THE HOMELESS (decline == ROUTING, never a verdict):** (a) sealed-DEFER graphs (roman's class): nested-capable frame protocol — PASS-THRU glue at DEFER entry gains a FRAMED variant (per-activation `push rbp` at blob entry, leave at blob γ/ω), retiring the graph-scope seal; DEFER re-entrancy = nested frames per the HQ ruling. (b) no-END runs: no head claim at all — pure per-box K (they are the ZPOP-fold class). (c) zdyn veto tree becomes routing to (a). (d) INFLATION BISECT (O-PB-2 method, exactly one of three): claim-base span arithmetic / op_zdepth double-count / zvo_resolve base move — `SCRIP_ZD_GAP=1` fprintf per site on any.sno; tripwire: stmt claim ≤ Σ member K + frame constant. Exit: `zvo_uclaim_k` returns 0 every run by construction; roman ≥10 correct (recursion witness moves to CLIMB C-6 if a residual defect is non-structural).
- [ ] **M-3 UCLAIM PHYSICAL DELETION:** zvo_ent_t owner-table declined extension · zvo_uclaim_k · g_zd_uclaim/g_zd_udout/g_zd_uhead staging twins · head `sub rsp,Kc` arm · veto diagnostics. Greps == 0: `zvo_uclaim\|g_zd_uclaim\|g_zd_udout\|g_zd_uhead` in src/. Loose-whitespace UCLAIM census grep `sub\s+rsp,\s*[2-9][0-9][0-9]` over regen'd SN4 `.s` == 0 (single-space spelling is a FALSE INSTRUMENT).
- [ ] **M-4 GLUE UNIFICATION:** collapse bb_glue_flat*/outer_whack/wire_* into the THREE (or prove each a spelling); outer_whack's release re-homes to the whack authorities; gate: grep the glue namespace == exactly the three families + THE-GLUE-DOES-NOT-WHACK grep (no `add rsp`/`mov rsp,rbp` inside glue bodies except framed_leave).
- [ ] **M-5 KILLSWITCH FOLD + LEGACY RESIDUE:** fold-or-die: SCRIP_ZD_{PROC,CAP,FENCE1,DYNSCOPE,DYNARM,ALT,SR,PATREF,GAP-diag-survives}, SCRIP_ZW_{FRAME,NBLOB,5}, SCRIP_{UNWIND,STMT_FRAME,ARBNO_LATCH,ARBNO_K16,ARBNO_FRAMELESS,ZPOP_FOLD_OFF,PAT_BUILD,NOFC,SLOT_ELIDE,SUBJ_DYN,STF_ELIDE_OFF,JEPIN}. Diagnostics + non-SN4 gates survive. ENDJMP/op_wsteal residue · ZW-5 depth stubs · dead op_zw/op_zw2 arms post-M-4 · legacy chain arm (M-2(a) is the nested-capable replacement — then the 19 sq=0 programs route framed and the chain dies) · marker scans L(5)/L(8) (delete iff CLIMB proves carry-capable replacement; else record as the sole survivor with reason).
- [ ] **M-6 FRAME-MINIMALITY + ω AUDIT:** U-GATE ω-coverage 100% K>0 (DENOMINATOR LAW) · orphan whitelist audited then 0 · wall census == framed-enter count over the frame set · rbp-op-refs < 14198 trending down · every rpin()/jepin() term justified · env-pad class impossible (frames kill absolute-sensitivity).
- [ ] **M-7 FINISH:** DoD checklist 1–7 green · completion greps · xc318 BY SET ≥ 280/260 both modes · regen ×4 · HQ file LADDER W struck as absorbed-done · FINDING + cursor.

## COORDINATION
CLIMB (`GOAL-SN4-ZETA-CLIMB.md`) has right-of-way on per-defect fixes; a fix requiring NEW protocol/structure is a MECH rung — cross-request in both cursors, never landed twice. ICN sessions: shared hunk = zd_wl_kind (ZK-1) — pull --rebase, eyeball that hunk, re-run the Icon 303-emission content-hash sweep (procedure in this file's cursor; parent = last known-good) after ANY emit.cpp/emit.h/x86_asm.h edit. `.github` pushes last.

## WATERMARK OF RECORD
= this file's LIVE CURSOR gate lines. **Proved s31 (HEAD `53a4f62c`/corpus `dbec8660`):** run_suite m3 **146/4/0/0** · m4 **144/6/0/0** · matrix m3 **146/4** · m4 **144/6**. Crater baseline m3 124/18/0/0 · m4 116/26/0/0 is the floor; every rung ≥ it BY SET; DoD 7 restores ≥ pre-flip xc318 280/260. Re-prove at open and close; never carry counts across container speeds.


