# ⛔⭐⭐⭐ GOAL-HQ-UNIFY — hq_U, THE FIFTH HQ: THE ONE MACHINE UNDER THREE SYNTAXES

**MODEL: Claude Fable 5.1 since 2026-09-06 20:4x CDT (Lon, in-chat to ceo, verbatim: *"I set B, C, and U to Fable 5.1."*; GOAL-CEO CEO-376) — the ceo named hq_B, hq_C and hq_U as the three HQs carrying the hardest problems (CEO-375); the mandate, the lane and the row in hand are unchanged.**

**Opened 2026-09-05 12:1x CDT by Lon, in-chat to ceo, verbatim:** *"Would having 5 HQ's help? One per language?"* · *"Or multiple HQ's segregated along any axis would work?"* · *"What's a good name for a fifth HQ?"* (ceo: HQ-UNIFY, `hq_U` — the verb grammar of COMPLETE / PERFORM / BEAUTIFY / TEST) · *"The /home/claude_U folder is ready to populate."* Root `/home/claude_U`, populated by the ceo the same sitting (SCRIP `53138c1e5` · corpus `b5c8540e3` · .github `23db9738` at cloning; hooks installed; identity LCherryholmes in all three; digest copied from hq_T's with its own identity block; postoffice `hq_U/` with `HQ` = `ceo`). Record: GOAL-CEO CEO-291.

## THE MANDATE

The Byrd-box machine, the driver and the runtime are ONE engine under three syntaxes (SNOBOL4 patterns, Icon generators, Prolog backtracking — ARCH-ENGINE.md, ARCH-LANGUAGES.md). Until this HQ opened, that engine lived inside hq_C's lane beside the Prolog rebuild, and every cross-language regression was owned by whichever HQ noticed it. Two of them surfaced on 2026-09-05 alone: the HOST argv staging that burned Icon's list serial 1 (a SNOBOL4 landing, an Icon board red; cured by the ceo on Lon's order, CEO-291) and the CAP_NEST rsp-relative capture whose live depth is a run-time value (hq_C, cured as an addressing mode, CEO-289). **hq_U owns the shared engine end to end and every regression that crosses a frontend boundary.**

## THE LANE (MASTER-PLAN § THE LANES — moved here from hq_C's "every shared-engine class" at opening)

- `src/templates/bb/` (the 134 boxes), `src/templates/x86/x86_asm.h` (the one encoder), `src/templates/xa/` (emission helpers), `src/emitter/`, `src/optimizer/` — the machine.
- `src/driver/` — one path, all languages; program-argument staging, mode 3/4 startup, the stale-binary refusal.
- `src/runtime/core/`, `src/runtime/rt/`, `src/runtime/rtx/` (the hand-written asm runtime, ARCH-{SNOBOL4,ICON,PROLOG}-RTX.md), `src/ir/` (the three zetas: `zeta_{choices,depth,storage}.h`, `descr.h`).
- EVERY CROSS-LANGUAGE REGRESSION: a landing in one frontend that moves another frontend's board is hq_U's from the bisect on, whichever HQ bisected it. The finder files the FINDING and the row; hq_U cures.
- NOT hq_U's: parsers and lowerers (`src/parsers/`, `src/lower/`) stay with the language HQ; a defect reachable through one frontend only is that HQ's until measurement proves it shared.

## LAWS THAT BIND EVERY hq_U LANDING (compact; RULES.md is the parent)

- **SHARED-NODE VERDICT SCOPE (RULES.md):** a shared-node change is graded on every frontend that lowers to the node — `grep -c IR_<NODE> src/lower/lower_*.c` names the boards owed — with SNOBOL4 FAIL=0 over the printed denominator plus the Icon master watermark as control arms, before its verdict is quotable; a board names BOTH hashes (SCRIP and corpus) and is re-run after `pull --rebase`, before push.
- **NO PER-OP FILTER:** all members of a BB family are the same; a defect reachable through one member is a class defect — fix the class or leave it visibly red.
- **EMISSION DISCIPLINE:** every x86 instruction is produced only inside `x86(...)` in `x86_asm.h`; templates emit zero binary; BOTH-MEDIUM MANDATORY (`test_gate_template_medium_invisible.sh`).
- **THE THREE ZETAS and the BB FRAME-PLACEMENT CRITERION (Lon 2026-08-27):** RESULT/LOCALS stay on the RSP spine iff every consumer reaches them at a fixed compile-time offset on every path; the moment unbounded growth can intervene they move to an RBP activation frame; only genuine escapers go to the heap. ζ has no modes, selectors or switches.
- **NO NEW GLOBALS** without Lon's explicit in-chat grant that session (the ⛔ banner naming global, type, file, purpose, and why registers and the stack cannot carry it — CEO-291 is the precedent: `g_main_args_v`).
- **ASM-DIFF-FIRST** (RULES.md § ABSOLUTE RULES): mint the smallest repro, diff the emitted `.s` between a passing sibling and the witness within ONE mode, gdb only then.
- **ZERO COMMENTS, zero blank lines, 200-char lines** in C/C++/asm (`strip_comments.py --check` is the first arm of `make test`).

## SESSION SETUP (every session)

1. `git -C <repo> fetch origin && git -C <repo> merge --ff-only origin/main` in SCRIP, corpus, .github; `cd SCRIP && make` (incremental; `make pristine` only on a stale-binary refusal or a release point).
2. `bash scripts/s4e_msg.sh check` — read, act, `clear`. MODE is line 1 of `/home/resources/postoffice/MODE`; under QUARTET/QUINTET you measure AND cure; under FLEET-<n> your seats (none assigned at opening — the RE-CUT of MASTER-PLAN § THE 16-SEAT CUT gives hq_U a contiguous range when Lon re-lanes) walk and witness, you cure.
3. Read this file's LIVE CURSOR, then `SCORE.md` § THE SEPTEMBER 10 GRID for the boards your lane moves (every row's M and V cells are shared-engine sensitive).
4. Your first act every sitting: `ls .github/FINDING-$(date +%F)-*` for any FINDING naming a cross-language regression or a shared node; each one is a row in your lane by LANE-BY-CURE (RULES.md), provisional until you write its `LANE REVIEW` ledger line.

## LIVE CURSOR

**✅ 2026-09-09 hq_U — LANDED: A VARIABLE OPERAND DEREFERENCES AT THE OPERATION. SCRIP `df41c4250`.** FINDING `FINDING-2026-09-09-hq_U-a-variable-operand-derefs-at-the-operation-and-scrip-was-right-for-bare-locals-by-accident.md`. Gate `test_gate_icn_a_variable_operand_derefs_at_the_operation.sh`, wired at `Makefile:194` and adopted into the ratchet floor. CEO-471, hq_S's bare-global-operand class.

**THE DEFECT.** Icon keeps a bare variable operand as a **variable** and dereferences it each time the operation executes. `emit.cpp:910` hands a bare **local** its **live varslot**, which the assignment writes back to — so SCRIP was right for locals **by accident**. A **global** or a **static** has no frame varslot and resolved to the producing `IR_VAR` node's **one-time snapshot slot**, while the assignment wrote to the GVA at `[r9 + k]`: read and write stopped addressing the same storage. `every G := G + !L` kept the last element (IPL `gediff`); `i + (i := 5)` printed `6` where `iconx` prints `10`.

- ⭐⭐ **THE CURE WAS CHOSEN BY A PROGRAM THAT WAS ALREADY GREEN, and that is the transferable part.** `every G := !L + G` — the same expression with the operands **swapped** — is correct today, because the global's read box already sits **after** the generator in the α chain. So the machine needed no new operand kind and no new addressing mode; it needed the read **wired where it already works**. `lower_icon.c` now chains a bare variable with no live frame location after its sibling (`icn_operand_derefs_late`). ⛔ **When a defect has a green twin, the twin is the design** — cheaper than either cure the row proposed, and it is found by varying the witness rather than by reading the emitter.
- ⛔⭐ **I AM CORRECTING MY OWN FINDING FROM THIS MORNING.** `...icon-argument-lists-must-stage-variables-and-dereference-at-the-call-and-**operators-are-not-affected**.md` says operand dereference is already correct. **False.** The probe behind it was `i + (i := 10, 0)` with `i` a **local** — I varied the *operator* and published a sentence about *operators*, while the axis that actually decides it, the **storage class**, never moved. Measured now: global, static, subscript and record field are all wrong; the bare local is the single accidental green. **The argument class and the operator class are ONE RULE.** A green probe licenses only the sentence its axes support.
- ⛔ **THE TWO CANDIDATE CURES IN THE ROW WERE BOTH DECLINED, WITH REASONS.** A **GVA-relative live location in the operand model** has to be taught to every box that reads an operand (`COERCE_NUMERIC`, `BINOP`, `CMP_TEST`, `IDENT`, `DIFFER`, the `COERCE_*` family), and a half-taught version is exactly the NO-PER-OP-FILTER violation; **the generator resume path** is the same fix expressed as special-casing one edge. Moving the chain fixes every consumer at once because it fixes the **wiring**, not the readers. A **frame mirror** stays refused for hq_S's reason: a callee can assign the global.
- **ARMS, all re-proven after `pull --rebase` and before the push:** gate **GREEN 30/30 cured → RED 18/30 on a reverted-cure control build**, in that order (CEO-381), **all twelve control gradings green on BOTH sides** so it is not red-by-default · **`gediff` m3=FAIL m4=FAIL → m3=PASS m4=PASS**, the DONE-WHEN proven red first · **IPL 83/89 both modes**, up from 82 · **Icon master 741/754 both modes, watermarks held** on SCRIP `df41c4250` corpus `05dbe274e` · gate-wiring ratchet CLEAN (WIRED 154) · SnoM re-stamped **1870/1894 unchanged** on the tree carrying the cure, by the runner inside `make test`.
- ⛔⭐ **THE BOARD CANNOT SEE THIS CURE AND I SAY SO RATHER THAN LET 741 IMPLY IT.** My pre-rebase board read **740/751 with a red list byte-identical by name** to the row `hq_V` had already written for that tree; the +1 to 741/754 is the corpus moving under both arms. **The master carries no entry of this shape**, and the `.github` rebase proved it the hard way: origin's cell said **740/754** against my **741/754** — same denominator, one apart, so **a measurement report and not a merge conflict**. I diffed the red lists by NAME instead of taking a side: origin's carried `procedure_every_alt_47`, mine did not, and that entry is cured by **`a76ab4b70` ("&allocated is a live counter")**, one of the eight commits I rebased ONTO. The `+1` is not mine to claim, and the program has **no reorder site at all** to give me one. The gate and `gediff` are the instruments; the board is the control — the same shape as the scan-subject landing.
- ⚠️ **A `make test` RED I DID NOT CAUSE, NAMED PER CEO-463 AND THEN CURED ANYWAY:** `test_gate_score_tables_agree` was red fleet-wide — SCORE.md's icon **display** cell said `739/751` where its **grid** cell said `740/751`. Pure file read of `.github/SCORE.md`, unreachable from my diff. Rather than name it and land past it, I let my own board's row write cure it: `board_icon_master.sh` on the committed tree rewrote both halves in place. It is now green.
- **⚠️ NOT CLAIMED:** a **subscript** `A[1]` and a **record field** `r.v` still dereference early (`A[1] + (A[1] := 5)` → `6` where `iconx` gives `10`) — not bare names, so they need the variable staged **as** a variable, the argument-dereference shape (the coo's CEO-452 row). **Deliberately not pinned** in the gate; pinning our answer would write it into the floor as the expectation. The reorder is applied to the **left** operand only; a non-binop construct (a subscript's index, a `to`-by clause) was not probed.

**✅ 2026-09-09 hq_U — LANDED: AN ICON SCAN SUBJECT MUST BE STRING-CONVERTIBLE. SCRIP `0cd14ec02`.** FINDING `FINDING-2026-09-09-hq_U-the-checked-icon-helpers-raise-but-their-boxes-have-no-failure-edge-so-ampersand-error-converts-nothing.md`. Gate `test_gate_icn_scan_subject_must_be_string_convertible.sh`, wired at `Makefile` and adopted into the ratchet floor. CEO-460's scan-subject row.

**THE DEFECT.** `[] ? write("in")` installed the list as `&subject` and ran the body; icont raises **103**. Measured across the type space against `icont`/`iconx` v9.5.25a: **string, integer, real, cset convert; list, null, table, set, procedure, file are refused** — a clean split, and `core_icn_chars_ok`'s existing `DT_S/DT_I/DT_R/DT_C` predicate matches it exactly. `rt_scan_enter` is the one door (`bb_gen_scan` is emitted only by `IR_SCAN`/`IR_SCAN_ENTER`, both Icon-only), so one call to the already-exported `core_icn_argtype_check(lo, hi, 103)` is the whole cure. No new helper, no new global, no language name past lower.

- ⛔⭐ **THE BOARD CANNOT SEE THIS CURE, AND I SAY SO RATHER THAN LET THE NUMBER IMPLY IT.** Icon master A/B on one tree with only this change as the variable: **737/749 both modes on BOTH arms, reds byte-identical by name** (on the stale corpus both arms shared — see the correction below; on the merged tree and fresh corpus the board reads **739/751, watermark up from 738**). No master entry scans a non-string subject. The gate is the only instrument in the tree that can tell the two builds apart — the same shape as my by-name scanning landing, and the reason the gate is the DONE-WHEN and the board is only the control.
- ⛔⭐⭐ **I ALMOST FILED A FALSE ACCUSATION AGAINST ANOTHER SEAT, AND THE ONLY THING THAT STOPPED IT WAS PULLING THE CORPUS.** My board read **737/749** against a watermark of **738** that `52a162e21` had pinned minutes earlier. I did the honest-looking thing — rebuilt with my one line reverted, re-ran the whole board on the same tree, got **737 again, same 12 reds** — and drafted a receipt saying the regression entered between the tree that earned 738 and HEAD, with a telegram to the coo. **All of it was wrong.** My `corpus` clone was **5 commits behind** for both arms: the population was 749 where origin's was 751. On the merged tree and a fresh corpus the same board reads **739/751, watermark UP from 738** — green, nothing regressed, nobody to report.
- ⭐ **THE A/B WAS SOUND AND THE FRAMING WAS NOT, WHICH IS THE WHOLE LESSON.** Both arms shared one stale corpus, so "my change moves nothing" was and is true. What a same-corpus A/B cannot tell you is whether the corpus is the right one — **it controls for the variable you chose and is silent about the one you forgot.** An A/B exonerates a DIFF; it never validates a BASELINE. PULL-BEFORE-TRUST is written for all three repos and I pulled all three at the sitting's start, then measured 40 minutes and two boards later as if that reading were still good. ⛔ **A CORPUS READING HAS THE SAME SHELF LIFE AS A MODE READING** — re-pull immediately before the board, not once at the top of the sitting; MEASURE-THEN-REBASE covers the CONTROL arm and the CORPUS, not just the verdict.
- ⭐ **AND THE TELL WAS SITTING IN THE CONFLICT THE WHOLE TIME:** the `.github` rebase collided on `SUITES.tsv` with origin's row reading `739 751` against my `737 749`. **A DENOMINATOR THAT DISAGREES IS NOT A MERGE CONFLICT, IT IS A MEASUREMENT REPORT** — two populations sharing a name, the trap `SCORE.md` documents in four separate cells. I took origin's rows rather than overwrite a newer, fuller population with a staler one, which was the right call for the wrong reason until I checked the count. `wc -l corpus/tests/icon/ALL.csv` cost one second and settled it.
- ⭐ **A SECOND DEFECT THE ROW DID NOT ASK FOR, FILED INSTEAD OF HALF-CURED.** The `&error` control arm I wrote for this gate failed — and then failed identically on a build with my cure reverted, and on `tab("x")` and `"a" to 5`, which predate today entirely. **The checked Icon helpers RAISE but their boxes have no FAILURE EDGE**: `core_icn_error` honours `&error` only where a `setjmp` frame is installed (`arithmetic.c`, `by_name_dispatch.c`, `runtime_eval.c` — the whole population), while `core_icn_to_int_check` and friends just *return* after raising. Under `&error := 1` the program then reports neither the error nor the failure and exits 0. It is a BOX problem, not a helper one — a box's failure is the ω edge, not a return value — so it is one shape across five boxes and its own row.
- ⛔ **I REMOVED THAT ARM FROM THE GATE RATHER THAN PIN IT.** Grading it against our current behaviour would have written our own wrong answer into the floor as the expectation. It is named in the gate's own summary line and in the FINDING, so the gap is visible without being ratified.
- **ARMS, all re-run after `pull --rebase` and before the push:** gate **RED 18/26 uncured → GREEN 24/24 cured**, re-proven green on the rebased tree · Icon master **739/751 both modes, watermark UP from 738**, on SCRIP `4078021ea` corpus `36f9e9ea9`; the A/B that proves this change inert is **737/749 identical on both arms**, valid as an A/B and measured on a corpus 5 commits stale · SNOBOL4 master **1893/1917 FAIL=0 both modes** as the shared-state control arm (`scan_subj` is shared with `bb_match_*`, though `rt_scan_enter` is not reachable from SNOBOL4 — CEO-405: the node census is the FLOOR of the owed set, never its ceiling) · `strip_comments --check` 385/0 · LF 4927/0 · `emit_no_lang` PASS · gate-wiring ratchet CLEAN.
- **⚠️ NOT CLAIMED:** the `&error` class is **diagnosed, not cured**, and the scan refusal inherits it. The three `setjmp` sites are named from a grep, not a reachability proof. `&errorlimit`/`&errorvalue` past the first conversion is unprobed. The 739 watermark is **not mine to re-pin** — my A/B says this change is inert on the master, so the +1 over 738 belongs to whoever earned it.

**⛔⭐ 2026-09-09 hq_U — THE CEO-437 ABLATION HAS NO ANSWER, AND THAT IS THE RESULT. NO CURE LANDED.** FINDING `FINDING-2026-09-09-hq_U-the-arbno-casualty-survivor-split-is-reachability-not-structure-so-the-ablation-has-no-answer.md`. Trees SCRIP `01eb996ca` corpus `f7c68a8c5`; fingerprints `52cd0c0468307184cf58a0448f45990d` / `b2c37a9a519e1d1fc0a2ff07c9bf2363`, restored byte-identical after every arm. Oracle arm: **live-oracle, swap stamp 20260909T033439Z** (CEO-440 f); pinned-ref arm agrees on all thirteen.

- ⭐ **CEO-439's FENCE-OVER-A-CHOICE-POINT AXIS IS FALSIFIED BY THE ASM DIFF IT ASKED FOR.** Casualty `arbno_fence_pos_branch_30` and survivor `_20` have **byte-identical instruction streams** — 1152 lines each, entire diff is three string-length immediates (subject `'aa'`→`'aab'` and two message lengths). Same FENCE, same choice point, same emitted code, opposite sides of the split. No source-level or emission-level predicate can separate them because **there is nothing to separate**; a discriminator hunt over a population that emits the same code cannot terminate, and this is the third axis to fail against it.
- ⭐ **THE REAL DISCRIMINATOR IS REACHABILITY: a carrier moves iff it commits ≥1 ARBNO instance AND the match must still FAIL** — iff it traverses the suppressed recede `jmp`. Rebuilt with the arm unconditional and graded all thirteen family entries m3+m4 under `setarch -R`: **exactly the six named casualties moved** (`_3` hang, `_30/_31/_34/_35` SIGSEGV, `_36` wrong answer); the five that match and the two that never build an ARBNO stayed green.
- ⭐ **`_19` IS THE CONTROL AND IT READS AS A COUNTER-EXAMPLE UNTIL YOU READ IT:** it expects a failed match and does recede, but with **zero** committed instances, so it leaves by the `je L(3)` — and in the suppressed build the `je` target and the fall-through are both ω, so zero-instance is correct either way. That is why the rule is "traverses the recede jmp", not "expects a failed match".
- ⛔⭐ **I AM CORRECTING MY OWN PRIOR ENTRY: THE PREDICATE IS NOT "PROTECTING A REAL POPULATION".** With the arm suppressed, what is left falls through to an unconditional ω — ARBNO **concedes at exhaustion**, which is the right final answer for a program whose expected verdict is a *failed* match. Those six are green today **by accident**, and they are the only six in the family that would ever exercise the edge. They are not a population to protect; **they are the entire test population for the cure**, and the correction inverts the acceptance test: success is those six printing their expected FAIL output, not staying untouched.
- ⭐ **THE MECHANISM, gdb ON `_34` m4: rbp WALKS OUTWARD ONE ACTIVATION FRAME PER LAP.** ARBNO's ζ cells are rbp-relative (`[rbp-48/-44/-40]`) and the recede `jmp` targets the deferred body's β. The body owns an activation frame; its concede pops it, so the host's exhaust entry is re-entered with rbp on the **enclosing** frame. Laps: `…edfe8 → …ee048 → …fe120 → …fe180 → (nil)` → SIGSEGV on the arm's *first* instruction, with **`r14` stuck at 1 every lap** — the cursor never rolls back because the rollback writes into a frame nobody reads. The `_3` hang is the same loop. This is the amended BB FRAME-PLACEMENT CRITERION (CEO-380): the host must **promote itself** to an RBP activation frame instead of assuming one exists.
- ⛔ **NEGATIVE RESULT, AND ITS KIND IS THE POINT.** `emit_arbno_rbp()` / `emit_arbno_rbp_unwind()` (`emit.cpp:2276,2281`) are a fully-written, hardcoded-off feature that looks exactly like the designed cure. Both set to 1 with the arm unconditional: **nothing moved**. The emitted asm carries **zero** `arbno_rbp_unwind` notes — its four call sites are in `bb_match_{begin,end,fence1}.cpp`, none on this path. So the statement is **"it does not reach the exhaust arm"**, not "it was insufficient" — which decides whether the next seat extends the feature or looks elsewhere.
- ⚠️ **NOT CLAIMED:** no cure landed, tree byte-identical to baseline. The 89-carrier figure is quoted from the entry below, not re-measured; I measured the thirteen-entry family exhaustively. That the other 82 are unreachable-by-input generally is **expected from the `_20`/`_30` pair, not measured**.

**⛔ 2026-09-09 hq_U — DIAGNOSIS LANDED, NO CURE YET: THE TWO RANK-0 NESTED-ARBNO CRASHES ARE ONE DEFECT, AND ITS EMISSION REACHES 89 GRADED ENTRIES THAT ALL PASS.** FINDINGs `FINDING-2026-09-09-hq_U-the-arbno-exhaust-recede-edge-is-not-emitted-when-its-body-lives-in-an-activation-frame-and-the-surviving-cmp-is-dead.md` and `FINDING-2026-09-09-hq_U-the-26-snobol4-master-xfail-markers-classified-and-exactly-two-are-bimodal.md`. Trees SCRIP `60d58c05b` corpus `3b10e1590`, binary md5 `e9b4f3312769`/`62a6beac7178`.

**THE DEFECT.** `bb_match_arbno_frame()` emits its exhaust-recede arm inside `IF(!sn4_defer_resume() || !_.op_arbno_body_actframe, …)`. When both hold, the `je`, the cursor rollback, the recede `jmp` and the `def L(3)` **all vanish** and what is left is `cmp r14d, eax` followed by an **unconditional** `jmp` to ω — a dead compare in front of a conditional edge whose target was never laid down. hq_P's `rip=0x0` row (CEO-414) and the ceo's `arbno_bal_tab_replace_branch_1` (CEO-425/426) are **the same defect**, and the ceo's routing hunch was right.

- ⭐ **THE TWO CRASH ADDRESSES LOOK LIKE TWO BUGS AND ARE ONE.** Under `setarch -R`: hq_P's is `RIP=0x0` (slot never written), the ceo's is `RIP=0x7fffffbf92d0` with `rsp=0x7fffffbf9240` — **a stack address 0x90 above rsp**, an indirect jump into a non-executable page. A never-written slot reads 0; one written with a frame pointer reads as a stack address. Same missing edge, different leftovers.
- ⭐ **THE NONDETERMINISM IS ASLR AND ONLY ASLR, AND THAT IS THE INSTRUMENT EVERYONE NEEDED TONIGHT.** The jump target IS a stack address, so ASLR moves it: 6 SEGV / 6 `nomatch` over 12 runs with ASLR on, **10/10 SEGV under `setarch -R`**. Pinning the layout makes it reproducible. No timing, no concurrency, no scheduler.
- ⛔ **THE KILLSWITCH PROVES CAUSATION AND IS NOT THE CURE — SAID BEFORE ANYONE REACHES FOR IT.** `SCRIP_DEFER_RESUME=0` off one build removes the crash in **both** witnesses; neither becomes correct (`nomatch` and a hang, against `sbl -bf` `match` on both). Reverting the condition trades a SIGSEGV for a wrong answer and would read as a green crash count. The cure must emit the arm **with** an actframe-correct rollback; the staged hypothesis is that the arm re-enters the body by the same door as β and rolls back the cursor but **not** β's `mov r12, AFCQ(8)` pend cursor — one build to test, not yet attempted.
- ⛔⭐ **HOW FAR IT REACHES, AND BOTH HALVES MUST BE SAID TOGETHER: 1927 entries swept, 325 ARBNO-bearing, 95 carry the signature — 89 of them GRADED, and ALL 89 PASS.** Re-ran the 89 six times each (534 runs): **zero moved.** The signature is **necessary, not sufficient** — the suppressed arm only bites a program that drives an ARBNO to exhaustion and then needs to recede. Those 89 greens are **conditional on the input, not on the edge existing**, and a corpus addition that drives the recede converts one of them into the ceo's witness.
- ⚠️ **I AM NOT CALLING THE BOARD REPRODUCIBLE, AND hq_P IS WHY.** Three whole-board runs on one tree (plain / `setarch -R` / plain) are identical **by entry name, not merely by total** — that name-level diff was hq_P's unsolicited mid-flight catch and it was the right one, since a compensating pair of flips holds a total still. But A and C are one repeat each of a coin there is reason to think is biased: the honest headline is **no movement observed**, not determinism proved.
- ⭐ **THE XFAIL CENSUS, FOR hq_T, CLASSIFICATION ONLY:** 26 markers, 5 runs m3 + 3 runs m4 each — **2 bimodal** (the same pair hq_T named 2026-09-04, independently reproduced), 6 stable crash, 4 stable hang, 11 stable wrong-answer, 2 with no m4 binary at all, 1 passing (`indirect_replace_1`, the standing `xpass=1`). Three of the six crashers **crash in m3 and merely answer wrong in m4**, so the marker hides a per-mode split too. hq_T's boundary kept: curing these is the entry owner's lane (hq_P for the SNOBOL4 master reds), not mine.
- ⛔ **MY OWN SWEEP PRINTED A CLEAN EMPTY RESULT WHILE EXAMINING ZERO ENTRIES**, because `list` and `extract` both require a `ref` argument I omitted and both failed silently into an empty name list. It read exactly like *no graded entry carries the defect* — the reassuring answer. Caught only because the script printed its denominator; re-run with a hard refusal below 100 names. **A sweep that cannot enumerate must refuse, not report.**
- ⛔⭐ **THE CURE CEO-430/434 NAMES WAS BUILT, MEASURED AND REVERTED — AND THE RULING'S PREMISE IS NOW MEASURED FALSE.** Emitting the arm unconditionally with β's missing pend rollback (`mov r12, AFCQ(8)`) **cures the ceo's witness completely** (`match` 6/6, promoted to XPASS) and **breaks eight previously-green entries**: board `1873/1899 FAIL=0` → **`1865/1899 FAIL=1 crash=4 hang=3`**, both modes. **Seven of the eight are signature carriers**, which is what confirms the arm's re-enablement did it. ⭐ **So the predicate is PROTECTING a real population**: it suppresses an arm 7 graded programs cannot survive and 2 xfail witnesses need. The next step is an **ablation across the 89 carriers to find what the 7 casualties have that the other 82 do not** — not another guess at the rollback set. Reverted; `scrip`/`libscrip_rt.so` md5 back to `e9b4f3312769`/`62a6beac7178`, byte-identical to the build the 1873/1899 board was measured on, so that board still describes this tree.
- ⭐ **ABLATION STARTED (CEO-437), FIRST CUT: 89 CANDIDATES ARE NOW 12.** Testing whether the ARBNO body carries a **FENCE wrapping an alternation**: 6 of 7 casualties have it, 6 of 82 survivors have it too, 76 survivors do not — **a large enrichment (50% vs 1.3%) and NOT a separator**, so it is not the discriminator and I am not treating it as one. Both crash witnesses carry **no FENCE at all**, and the carriers with a *non-alternating* FENCE (`FENCE('a')`, `FENCE(*C)`) survive — so the axis is FENCE **over a choice point**: the recede sends ARBNO back into a body whose FENCE has already sealed the alternatives it is being asked to reconsider. ⛔ **The residual question is now small and exact: what separates `arbno_fence_pos_branch_30/31/34/35/36/3` from `arbno_fence_pos_branch_20/27/28/29/33`?** Same generated family, same top-level shape — so the next arm is an **emitted-asm diff inside that family of twelve**, not another source-level predicate.
- ⭐ **`scrip`'s OWN md5 DID NOT MOVE ACROSS A REAL TEMPLATE CHANGE** — emitter and templates link dynamically, so only `libscrip_rt.so` moved. A scrip-only fingerprint would have called this "no change", exactly as `test_corpus_snobol4.sh`'s header warns. Fingerprint both, and the second is the one that matters.
- ⚠️ **NOT CLAIMED:** hq_S's aisnobol/SPITCORE non-executable-page jump is **predicted** to be the same family and **not reproduced** (it needs SPITCORE loaded). 31 entries could not be extracted and were never checked, so 95 is a floor. `SnoM 1873/1899 FAIL=0 both modes` written to SCORE.md through the runner.


**✅ 2026-09-09 hq_U — SECOND LANDING: A PATTERN-CAPTURED SUBSTRING NOW COMPARES BY ITS OWN LENGTH. SCRIP `c36f0db13`.** FINDING `FINDING-2026-09-09-hq_U-a-hand-written-asm-twin-drops-a-guard-its-c-original-has-and-passes-every-test-the-c-passes.md`. Gate `test_gate_sno_captured_substring_compares_by_its_own_length.sh`, wired and adopted.

**THE DEFECT.** Subject `512`, capture the first character: `SIZE`=1, `OUTPUT`=`5`, `DATATYPE`=STRING, `capture+0`=5 — and `LT LE GT GE EQ NE` all compared it as **512**. `rt_coerce_num2_d` has a C twin that is correct (it routes through `rt_cstr_d`, which materialises a NUL-terminated copy for an unterminated slice) and a hand-written asm twin in `rtx_icnnum.s` that runs instead, whose `SCAN_SIMPLE_INT` loads `d.s` from `[SRC+8]` and scans to NUL, **never reading `slen` at `[SRC+4]`**. Six instructions bail to the C twin when `s[slen]` is non-zero — literally `rt_cstr_d`'s own test.

- ⭐ **THE AXIS IS NUL-TERMINATION, NOT WHOLE-VS-PROPER-SUBSTRING**, and the last-character capture is what proves it: it ends at the buffer end, is already terminated, and compared **correctly on the uncured build**. hq_P found this defect independently within the hour and published the mechanism as *"the captured descriptor inherits a numeric field from the subject"* — falsified by gdb (the descriptor arrives PERFECT: `v=DT_S slen=1 s→"512"`) and by that counter-example. They reproduced it on their own uncured build and corrected their FINDING. **Acting on the published mechanism would have sent a seat into descriptor construction, the wrong file.**
- ⭐ **THIRD OCCURRENCE OF ONE CLASS IN FIVE DAYS** — a length-carrying descriptor read through an API that only understands NUL (09-05 `LPAD`/`RPAD` `strlen` on a slice; this; and the same night's coroutine scan-semantics twin). Census: of three asm twins, **`rtx_arith.s` does the right thing twenty lines away in a sibling file**, which is exactly why arithmetic was right and comparison wrong on the same value.
- ⛔ **A NEGATIVE RESULT I CHECKED RATHER THAN ASSUMED:** unguarded `strlen` in `DUPL_fn`/`TRIM_fn` *looks* like a live instance and is **not** — `VARVAL_fn` materialises. C accessors defend their callers; **asm bypasses accessors by construction**, so the twins are the real population. `rtx_match.s` is owed an entry-by-entry audit and is explicitly **not** claimed safe.
- ⭐ **THE FLIP AND WHY THE BOARD MISREPORTED IT:** `gimpel-conversions` died at `ERROR 246 -- stack overflow`, **two levels downstream** of the wrong comparison — `SPELL_100` asks `GE(N,100)` about a value that prints `1` and compares `100`, so it re-takes the hundreds branch forever. A seat taking that row off the board alone tunes stack limits and never arrives. Snoflake **108 → 110/124**. hq_T placed the general form in the test standard: **a symptom that names a resource is the one most likely to be misattributed, because the board itself supplies the wrong hypothesis** — and the discriminator is *ablate the input rather than raise the limit*.

**ARMS, re-run after each rebase:** SNOBOL4 master 1898 graded `m3 FAIL=0 m4 FAIL=0` · Icon master **704/704 FAIL=0** both modes · Prolog ladder 533/568 FAIL=35 **identical** to the pre-cure arm · gate RED 3/3 on a control build with only the guard reverted, GREEN 3/3 with it.

**⚠️ NOT CLAIMED, ALL CHECKED:** the board's `xpass=1` on `indirect_replace_1` **passes on the control build too** — a pre-existing stale marker, not my flip, not promoted. The identity gate's 8 `rung41_rt_*` improvements are on **both** arms and were not re-pinned; my commit did not earn them. The 43 owed `.s` artifacts were **identical on both builds** — pre-existing (the coo has since shown 23 were phantom, from the `.file` directive).

**⛔ AND ONE I CAUSED AND REPORTED:** my snoflake board run's grid regeneration **deleted the `gimpel` PACKAGE_INVENTORY clause** from SCORE.md — the ceo's silent-revert machine. Caught by `test_gate_score_inventory_clauses_never_vanish`, which was sitting on disk **wired to nothing** and which I only ran because the wiring ratchet refused my handoff. Discarded the edits wholesale rather than re-typing the digits, reported to the coo, then wired the gate green. Separately corrected my own SnoM row, which had moved an announcement cell off `done` on a **denominator convention**, not a regression.

**NEXT, ACCEPTED AND RANKED:** hq_P's `snobol4-nested-arbno-segfaults-with-a-null-port-jump-to-address-zero` (rank 0, CEO-414 — `rip=0x0` and saved `rip=0x0`, an **unwired port**, not stack exhaustion; DONE-WHEN includes absorbing the witness and lowering the orphan floor in the same commit). Then the ceo's m4 generated-symbol serial offset (nine fewer allocations before the first visible serial; m3 is the correct arm). Then hq_C's `IR_COERCE_NUMERIC` emit-time guard sink. Snoflake G-P has 8 reds left, all live-oracle-diffed with no stored refs, so structurally clear of the dead-pinned class (CEO correction: the 85 was never 85 winnable).

**✅ 2026-09-09 hq_U — CEO-401 AS AMENDED: A BY-NAME GENERATOR CALL NOW INHERITS ITS CALLER'S SCANNING ENVIRONMENT. SCRIP `d4799c97a` corpus `9bb7c9cd4`.** FINDING `FINDING-2026-09-08-hq_U-a-config-dir-stdin-companion-is-invisible-to-the-absorber-and-the-starved-ref-hid-a-by-name-scanning-defect.md` (CURED section). Gate `test_gate_icn_byname_generator_call_inherits_the_scanning_environment.sh`, wired at `Makefile` and adopted into the ratchet floor.

**THE CURE, one branch and one discriminator.** `scrip_coswitch` called `rt_scan_state_reset()` on the way into a newly created context and `rt_scan_state_apply()` on the way back. Correct for a user `create`, which owns its `&subject`/`&pos`; wrong for a by-name generator CALL, which is an ordinary procedure call that merely happens to be dispatched through the same co-expression machinery. `scrip_coctx_t` gains `inherit_scan`, set by `rt_proc_call_gen_h` and cleared explicitly by BOTH initialisers (`scrip_coexpr_create` mallocs without zeroing — a defaulted discriminator here is a silent wrong answer).

- ⛔ **BOTH ENDS OF THE SWITCH READ ONE VARIABLE, OR IT IS HALF A CURE.** With only the reset skipped, the callee matched correctly and then had `&pos` **rewound under the caller** on the return path, so `text ? (goal() & pos(0))` still failed. One build to learn — the same shape as the `IR_GOTO_DEFERRED` γ/ω lesson: when a defect lives at a switch, BOTH directions carry it.
- ⭐ **THE DISCRIMINATOR IS NOT "ICON", AND THAT IS WHAT MAKES IT LEGAL.** Measured: `rt_proc_call_gen_h` is reached from Icon's `rt_call_value_gen_h`, Prolog's `rt_pl_goal_gen_h_c`, and Raku (`lower_raku.c:591`). So the flag means **"this coroutine implements a CALL, not a `create`"** — behavioural, which is what the no-lang doctrine requires. A `LANG_*` test would have been the easy version and the wrong one.
- ⛔ **NO ENV KILLSWITCH SHIPPED.** The working copy carried `SCRIP_ICN_BYNAME_SCAN_INHERIT`; deleted before landing. A killswitch on a semantic correctness cure is a selector for a wrong answer — ζ's retired env twins are the precedent.

**⭐⭐ THE FOURTH HYPOTHESIS WAS THE RIGHT ONE, AND THE ceo TOOK THE LESSON INTO THE LAW (CEO-405).** Three earlier explanations were each consistent with every measurement I had, and each died only against a tool I had not yet used. **AN ASYMMETRY TELLS YOU THERE ARE TWO MECHANISMS; IT DOES NOT TELL YOU WHICH TWO.** The `&pos`-correct/`&subject`-empty split was real data pointing at a real split, and I read a two-carrier reset as a half-saved register file: `&pos` is an inline `r14` read and `r14` survives the switch, while `&subject` and the `match` builtin read globals the reset had cleared. **A watchpoint on `scan_subj` cost one command and would have answered it at hypothesis one.** I retracted the published `rt_scan_sync_*`/`r13`/`r15` cause BEFORE landing rather than after; the ceo accepted it in full and amended CEO-401 to the one-branch cure.

**⭐⭐ AND THE LAW ITSELF WAS THE DEFECT, NOT ANYONE'S READING OF IT (CEO-405, .github `ce687e83`).** SHARED-NODE VERDICT SCOPE says compute the boards owed by counting IR node references across the lowerers. For this change that census answers **ICON ONLY**, while the SNOBOL4 `bb_match_*` templates ride the same `r13`/`r15` and the same scan globals. **A seat obeying the law to the letter would have shipped this ungraded on SNOBOL4 and been correct by the text.** The census names the **FLOOR** of the owed set, never its ceiling: ask what STATE the change touches, grep for the carriers of that state across the templates and runtime as well, and when the two answers disagree the wider one is owed. I graded SNOBOL4 and Raku on that basis, not on the census.

**MEASURED — every arm re-run AFTER `pull --rebase`, before the push (MEASURE-THEN-REBASE, which I walked into on 09-06 and did not repeat):**
- **Icon master `704/704 FAIL=0` both modes**, AST `153/153`, with `procedure_every_alt_replace_4` named OUTSIDE per CEO-409 and the re-cut entry graded FED.
- **SNOBOL4 master** `1898 graded, m3 FAIL=0 m4 FAIL=0 (1871 pass + 27 xfail, crash=0 hang=0 unproven=0 skip=0), ast 28/28` — the collateral arm; `lower_snobol4.c` reaches this dispatch **zero** times.
- **Icon per-entry identity** examined=1557 regressions=0 vanished=0. The 8 `rung41_rt_*` improvements are present on BOTH arms (they came with the pulled tree) and are **NOT re-pinned here — this commit did not earn them.**
- **Prolog ladder 533/568 FAIL=35, IDENTICAL on control and cured.** **Raku master** `656/820, the 656 matching the last recorded row exactly -- a control arm, NOT a same-tree A/B, and I say so`.
- Gate **PASS 8/8** cured, **FAIL 4/8** on a control build with only the three runtime files reverted; the `direct` and `coexpr` arms pass on BOTH sides, so the gate is not red-by-default and the co-expression arm is what would catch this cure broadening into `create`.

**⛔⭐ THE FALSE GREEN IS GONE AND IT DID NOT BECOME A RED — THE TWO HALVES LANDED WITHIN THE HOUR FROM DIFFERENT HANDS.** hq_T's re-cut (`corpus 6c94504c0`) fed master entry 831 its own stdin and cut its ref from `iconx` while I was measuring. Yesterday that re-cut would have converted a manufactured green into a named red; with this cure the fed program matches `iconx` **exactly, 8 of 8 lines, in both modes** (it printed `rejected` eight times before). That separation is CEO-401 (1) working as designed: **a cure and its oracle from the same hand is how a false green is born the second time**, so hq_T cut the ref and I cured the engine, and neither of us graded our own work.

**⚠️ OPEN, NOT FOLDED IN, AND PROVEN PRE-EXISTING: `suspend goal()` BY NAME PRODUCES NOTHING.** `every x := drive(gen)` where `drive` does `suspend goal()` prints nothing in both modes, rc=0, against the oracle's `1 2 3`. **Identical on the control build, so it is not a regression from this cure** and I am not claiming it as one. A by-name generator in a CALL position is green (wired gate); in a SUSPEND position it is silently empty. Owed as its own row.

**⛔ OWED ITEM 4 IS VOID** (the scan-sync ABI does not need to carry the subject; `rt_scan_sync_*` is innocent, no calling convention moved). **ITEM 5 STILL OPEN**: `bb_call.cpp:466/520`, the name test standing in for a behavioural one, and the generator box's missing sync IN.

**INHERITED AND STILL NOT STARTED:** hq_P's `rtx_icnsub.s` `.Lsub_table:` dead-arm row; `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census (still rank 1); hq_C's `IR_COERCE_NUMERIC` guard-sink class (accepted, queued behind the all-twelve-on-SNOBOL4 cut); hq_P's cold-first-builtin-call finding (accepted into this lane, queued).

**✅ 2026-09-06 hq_U — CEO-361 AUTHORED AND LANDED, SCRIP `4e81927d5`. A DEFERRED GOTO LEAVING A MATCH STATEMENT NOW UNWINDS TO THE MATCH WATERMARK.** FINDING `FINDING-2026-09-06-hq_U-a-deferred-goto-leaving-a-match-statement-over-popped-the-watermark-and-rsp-climbed-out-of-its-own-frame.md`. Gate `test_gate_sno_byname_goto_zeta_unwind_in_a_loop.sh` (8 arms, refuses rc=2 under-graded).

**THE CURE, two lines and one extracted helper.** `zd_plan` pops a statement-exiting node down to the match-begin watermark (`zdh_match + emit_match_begin_stfh_k()`), not to zero — the match epilogue already released the bytes above it. The rule was gated on `IR_STATEMENT_END || IR_STATEMENT`. A by-name `:($X)` out of a match statement exits through an **`IR_GOTO_DEFERRED` chain head**, which was not in the set, so it emitted `add rsp,<full>` and over-popped by exactly the watermark. Both ports of the head carry the pop (γ = special-transfer landing, **ω = the hop to the next test, and ω is the port the ordinary case takes**), so curing γ alone left the witness crashing — one build to learn. The predicate existed in **two hand-copied expressions**; both now call one helper, so a future kind cannot be admitted to one and not the other.

- ⭐ **AN OVER-POP, NOT A LEAK, AND THE THRESHOLD IS WHAT SAYS SO.** rsp CLIMBS 16 bytes per traversal (`e070→e080→e090→e0a0` before, flat at `e060` after) until it walks past its own frame base. The witness died at **~4200 iterations** on an 8MB stack; a leak exhausting 8MB predicts ~262000 — **two orders of magnitude out**, and 4200×16=67KB instead matches the ~73KB from the loop's rsp to the frame base. The crash *presents* as stack exhaustion; only the number falsifies that reading.
- ⛔ **THE HANDED-OVER DIAGNOSIS NAMED THE RIGHT FILE, FUNCTION AND NODE AND STILL POINTED AT THE WRONG AXIS.** hq_S read it as *"the unwind is planned onto the wrong node of the chain"* (1 of 4 planned). Measured, **one-planned-node is correct by design** — the head pops on both exits and the rest run unwound, which is exactly why their attempt 1 double-popped and crashed. The planner had the right node and the **wrong amount**. ⭐ The tell was cheap and I had it early: I minted a witness reproducing the *stated* mechanism (1 of 8 planned) and it ran **green in both modes**. **A repro of the stated mechanism that does not reproduce the symptom falsifies the mechanism** — worth minting one before curing, precisely so it can fail that way.
- ⛔ **NOT THE WANG/POKEV CURE, and hq_S reached the same conclusion independently before I reported it.** Both still SIGSEGV. Their `95ac6d3d0` collapses the dead special-transfer chain and their witness still dies identically. Remaining mechanism is theirs and named: a by-name **entry** into a label inside a DEFINE body never seats the return continuation. My cure is **still load-bearing after** `95ac6d3d0` (gate RED on `aeee1ce62`).
- **CONTROL-ARM BAR (CEO-365) DISCHARGED**, comparison tree `aeee1ce62` = landed minus the commit, same corpus, built in a separate root: SNOBOL4 master **identical** (pass=1831 fail=1 skip=0 xfail=27 xpass=3), Prolog ladder **identical** 533/568 FAIL=35, Icon 22 programs byte-identical, owed `.s` artifacts the **same 2** (pre-existing; this change owes zero). Tolerated red named with its row: `user_function_keyword_branch_3`, hq_P's rank-0 `&FNCLEVEL` row.
- ⛔ **THE BOARD CANNOT SEE THIS CURE AND I SAY SO INSTEAD OF LEANING ON A GREEN.** Over 75 corpus programs compiled both ways, **zero** emitted files differ and only **one** carries the node at all (and its goto is not inside a match). Same shape as the FENCE cure — the gate is the only instrument.
- ⚠️ **I WALKED INTO MEASURE-THEN-REBASE AND AM RECORDING IT RATHER THAN QUIETLY RE-RUNNING.** Measured a board, rebased, pushed. I "checked" the rebase with `git diff --stat <old> <new> -- src/`, saw two familiar lines, and concluded script-only; it had actually pulled **26 files / 680 insertions including four codegen files**. Re-measured on the landed pair; numbers unchanged, which is **luck, not a defence**. ⭐ I read a **filtered** diff as a **complete** one — the same instrument class as `command -v` and a truncated `ls`.
- ⭐ **AND ONE FALSE FLIP I ALMOST PUBLISHED.** The no-cure build first showed `skip=22 / "scrip --compile failed"`, which read as *the cure flips 22 programs*. It was a **scratch-tree artifact**: those entries open with `-INCLUDE`, whose files live in `corpus/include/` — a **sibling of the SCRIP root** that a `git archive` checkout does not have. Symlinking the sibling corpus made the 22 vanish and the two boards became identical. A comparison root is not a comparison tree until everything the harness resolves *relative to it* exists; the tell was that `simple_assign_2` cannot plausibly be fixed by a zeta-unwind change.

**INHERITED, UNSTARTED, AND NOT SILENTLY DROPPED:** hq_P hands me (RTX lane) `rtx_icnsub.s` `.Lsub_table:` — an unconditional `jmp .Lsub_bail` makes the whole table arm dead, while `rtx_init.c` still asserts `sizeof(TBPAIR_t)==48` on its behalf and taxed their live `TBPAIR_t` change. Their ask is narrow: delete block+assertion together, or leave one line at `.Lsub_table` saying it is disabled and STALE (next-pointer walk at offset 40 that is now `hkey`, plus a baked 256-bucket mask). Not started — needs a row.

**STILL RANK 1 AND STILL NOT STARTED: `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census.**

**✅ 2026-09-06 hq_U — RANK-0 CURED AND LANDED, SCRIP `b2766d84c` corpus `470f78a62`. THE SNOBOL4 FLOOR REACHES FAIL=0 IN BOTH MODES.** Rows 586 (`charset-primitive-loses-its-set-after-a-null-alternation-branch`, rank 0), 554 (`snobol4-pos-rpos-dynamic-operand-*`) and 585 (`emit-operand-frame-allocator-*`) are CLOSED on their own three DONE-WHENs, none folded, exactly as CEO-304 ruled. FINDING `FINDING-2026-09-06-hq_U-a-pattern-primitive-operand-staged-before-match-begin-is-addressed-from-rbp-*`.

**THE CURE, and it is the criterion rather than a new mechanism.** A pattern-primitive operand staged **before** `match_begin` and read by a matcher element **inside** that match is now addressed from **RBP**. `match_begin` does `push rbp; mov rbp, rsp` unconditionally under `emit_match_rbp()`, so the operand's existing staging cell sits at a fixed positive offset **above** RBP on every path, while its distance from RSP is a run-time fact. `K = 8 + (zd_out[match_begin] - zd_out[producer]) + xh(producer, match_begin)`; the walked range lies before the match opens so it provably carries no ALTERNATE and no DEFER — **path-independent by construction**. Derived and then checked three ways against the emitted `.s` before a line of cure was written.
- ⛔ **NO SPILL IS EMITTED, and the deviation from the ruled letter is recorded rather than left in the diff.** CEO-304 said "spilled at `match_begin`". A spill needs the same `K`, then pays a copy and frame bytes to relocate a value already at a fixed RBP-relative address and already immune to everything inside the match. Same criterion, same RBP arm (`op_zread_xf`), one fewer instruction.
- ⭐ **ONE DISPLACEMENT CURED BOTH ROWS, which is why the ceo's "554/585/586 are ONE cure" was structurally right before anyone could see why:** `op_zread_xf[k]` is the RBP arm of **both** addressing paths — `ZOPQ`/`ZOPD` (charset consumer, `op_zres=1`) and `XSAQ`/`XSAD` (POS consumer, `op_zres=0`, base `op_sa`).
- ⛔ **hq_S's COUNTER PRINT, LEDGERED FIRST per CEO-307, and it settles their bounded caveat: THE RESUME PAIR PERSISTS.** Read structurally off the emitted `.s` rather than sampled: the DEFER γ-suspends with `push rcx; push rax; jmp <merge>` — **both words live at the merge, no pop on that path** — and the pair is discarded only on the recede (`add rsp, 8; pop rax`). So each live suspension needs its own home, one frame slot pair cannot hold hq_S's measured seven, and **candidate B is dead as MEASURED, not as reasoned**.

**MEASURED — SHARED-NODE VERDICT SCOPE discharged, all arms re-run after the rebase, before the push:** SNOBOL4 master `total=1854 · m3 1819 FAIL=0 · m4 1819 FAIL=0 · crash=0 hang=0 unproven=0 skip=0` (PASS+FAIL+XFAIL covers the printed denominator — nothing hiding in a SKIP column) · Icon ladder 548/572 and Prolog ladder 484/568 **identical in both arms of an A/B with `emit.cpp` as the only variable** · Icon master `entries=824 m3 659 m4 659/671` watermarks HELD. **Blast radius, measured not argued: ONE owed artifact whose entire diff is four lines** (`demos/snobol4/json/json.s`, `coerce_string` operand rsp→rbp); Icon bench 20 unchanged/0 updated and Prolog bench 23 emitted/0 changed are **byte-identical**.

⛔ **`make test` CARRIES ONE RED THAT IS NOT MINE AND I PROVED IT RATHER THAN ASSERTING IT:** `test_gate_pl_quad_regs` FAIL(1), 4 unenrolled r12-r15 writes over 402 witnesses — **identical on a build without my change**. Prolog rtx lane, routed, not a blocker for this landing.

⚠️ **OPEN AND NAMED SO IT IS NOT INHERITED EITHER WAY:** the board reads `m4_xpass=1`, `fence_capture_imm_capture_replace_branch_1` — a stale XFAIL marker owed a promotion (THERE IS NO XFAIL). **I have NOT established whether this cure is what turned it green and I am not claiming it.**

⭐ **TRANSFERABLE, and it nearly cost the landing half its scope: A GUARD WRITTEN FOR SAFETY CAN SILENTLY NARROW A CURE PAST ITS OWN ROW.** My first admission test also demanded zero `IR_MATCH_END` in the linear range between producer and consumer. It cost nothing to write, broke no test, and **excluded the entire POS/RPOS class** — whose consumers are arm bodies laid out *after* `MATCH_END` though they execute *inside* the match. It would have shipped as a complete cure with a green gate beside it. The tell was not a failing test; it was that **the ceo had already ruled the two rows were ONE cure**, so a change closing one and not the other was a claim contradicting the ruling and had to be explained rather than shipped. **A conservative predicate is still an assertion about the world** — here, that linear position implies execution scope, which is the assertion this engine keeps punishing. Third arrival this week at the same sentence: **ζ liveness follows NESTING; the walk follows linear ORDER.**

⭐ **hq_B's instrument, generalised and now used in anger: AN OWED-ARTIFACT COUNT IS A BLAST-RADIUS INSTRUMENT, NOT A HANDOFF CHORE.** It told me the true reach of a graph-general emitter change — four lines, one program, Icon and Prolog byte-identical — before any board could, and it is what turns "graph-general" from a worry into a number.

**STILL RANK 1 AND STILL NOT STARTED: `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census.** Also open in this lane: row 599 (`match-head-cell-capgen-save-restore-*`, hq_S prefers `capture_frame_slot` over the documented-dead `head.incoming____`; DONE-WHEN still the mint placeholder — my call to record), and the four standing co-signs (hq_R catch/3 veneers, hq_S DEFINE driver term, hq_I/hq_B generator ABI, hq_P's `IR_GOTO_DEFERRED` omega whitelist).

**⛔ ceo RULING 2026-09-06 08:24 CDT (CEO-304) — THE CHARSET CURE IS YOURS; hq_S CO-SIGNS.** Row `charset-primitive-loses-its-set-after-a-null-alternation-branch` (rank 0): hq_U AUTHORS the match-frame-slot cure for a dynamic pattern-primitive operand (the `capture_frame_slot` precedent — `emit_match_rbp()` alone, no `blob_frame_scope`; spilled at `match_begin`; `ZOPQ`/`XSAQ` on their RBP arm) as a DESIGN landing; hq_S co-signs the SNOBOL4 board. ⛔ AMENDED CEO-305 (4) on your second message: the ceo does not choose between candidate A (that RBP match-frame slot) and candidate B (MATCH_DEFER's resume pair in the ALTERNATE's existing 32-byte frame) — the BB FRAME-PLACEMENT CRITERION does: every consumer reaches the operand at a fixed compile-time offset on every path. B is quotable only when the charset gate carries the falsifier you named (a defer re-entered under the same alternate: ARBNO over the alternate, and a deferred pattern that itself suspends — both modes, proven red on a build without the cure) and hq_S's semantics answer is on the baton; A is the fallback the moment that falsifier reds. Whichever lands writes its reason into `ARCH-ENGINE.md` beside the criterion. Rows `snobol4-pos-rpos-dynamic-operand-returns-a-silently-wrong-answer` and `emit-operand-frame-allocator-is-switched-off-by-name-for-any-non-pat-dollar-graph` are the same cure and close on their own DONE-WHENs. The routing rule's exposing-HQ arm is for shared-NODE classes; `IR_MATCH_ALTERNATE` is SNOBOL4-only and the cure is in `src/emitter/` — lane-by-cure. The Icon ladder + master and the Prolog ladder are control-arm watermarks in your landing commit (both hashes, after the rebase) beside SNOBOL4 FAIL=0 over the printed denominator. Release seat11's claim yourself; the seat's gate `test_gate_sno_charset_dynamic_operand_null_alt.sh` PASS(0) on all 7 fixtures, unmodified, is the DONE-WHEN. Your retraction is carried by the ceo (CEO-304 (2)): switched off by EMISSION PATH (`flat_jmp_entry`, emit.cpp:3590; the name test :44 is the downstream second term, zeroed at :2851) — CEO-302 rewritten, superseding lines in hq_S's and your 09-05 FINDINGs, QA lines in three batons. The allocator row keeps its name; its GOAL line and the `test_gate_sno_pos_rpos_dynamic_operand.sh` header are yours to correct with the cure — ⚠ at SCRIP `495aeb974` that header carries neither "by name" nor `flat_pat`; verify before editing.

**⛔ ceo RULING 2026-09-06 08:24 CDT (CEO-304) — THE CHARSET CURE IS YOURS; hq_S CO-SIGNS.** Row `charset-primitive-loses-its-set-after-a-null-alternation-branch` (rank 0): hq_U AUTHORS the match-frame-slot cure for a dynamic pattern-primitive operand (the `capture_frame_slot` precedent — `emit_match_rbp()` alone, no `blob_frame_scope`; spilled at `match_begin`; `ZOPQ`/`XSAQ` on their RBP arm) as a DESIGN landing; hq_S co-signs the SNOBOL4 board. ⛔ AMENDED CEO-305 (4) on your second message: the ceo does not choose between candidate A (that RBP match-frame slot) and candidate B (MATCH_DEFER's resume pair in the ALTERNATE's existing 32-byte frame) — the BB FRAME-PLACEMENT CRITERION does: every consumer reaches the operand at a fixed compile-time offset on every path. B is quotable only when the charset gate carries the falsifier you named (a defer re-entered under the same alternate: ARBNO over the alternate, and a deferred pattern that itself suspends — both modes, proven red on a build without the cure) and hq_S's semantics answer is on the baton; A is the fallback the moment that falsifier reds. Whichever lands writes its reason into `ARCH-ENGINE.md` beside the criterion. RESOLVED CEO-307 (4): the falsifier is on the charset baton — hq_S's five witnesses, seven defer evaluations under one alternate, unwinding correctly — so B is dead as stated and hq_U builds A, the RBP match-frame slot, ledgering the one counter print hq_S asked for first. Rows `snobol4-pos-rpos-dynamic-operand-returns-a-silently-wrong-answer` and `emit-operand-frame-allocator-is-switched-off-by-name-for-any-non-pat-dollar-graph` are the same cure and close on their own DONE-WHENs. The routing rule's exposing-HQ arm is for shared-NODE classes; `IR_MATCH_ALTERNATE` is SNOBOL4-only and the cure is in `src/emitter/` — lane-by-cure. The Icon ladder + master and the Prolog ladder are control-arm watermarks in your landing commit (both hashes, after the rebase) beside SNOBOL4 FAIL=0 over the printed denominator. Release seat11's claim yourself; the seat's gate `test_gate_sno_charset_dynamic_operand_null_alt.sh` PASS(0) on all 7 fixtures, unmodified, is the DONE-WHEN. Your retraction is carried by the ceo (CEO-304 (2)): switched off by EMISSION PATH (`flat_jmp_entry`, emit.cpp:3590; the name test :44 is the downstream second term, zeroed at :2851) — CEO-302 rewritten, superseding lines in hq_S's and your 09-05 FINDINGs, QA lines in three batons. The allocator row keeps its name; its GOAL line and the `test_gate_sno_pos_rpos_dynamic_operand.sh` header are yours to correct with the cure — ⚠ at SCRIP `495aeb974` that header carries neither "by name" nor `flat_pat`; verify before editing.

**2026-09-05 16:3x–16:5x CDT hq_U — OCTET, THIRD SEGMENT: ONE ROW CURED AND CLOSED, ONE CO-SIGN REFUSED, ONE "PUSHED" CLAIM THAT WAS NOT.**

**ROW `snobol4-capture-start-shifts-when-an-alternation-inside-an-alternation-branch-is-taken` — DONE, SCRIP `ee6744d04`.** ceo assigned it rank 0 with *"cure it; do not hand it back."* The capture SAVE banks its start on the ζ-SPINE and COND reads it at a STATIC rsp offset, but every `IR_MATCH_ALTERNATE` pushes 32 bytes of SPINE at α and keeps it live for backtracking — so the live depth at COND is an ARM-TAKEN-AT-RUNTIME fact. Decisive: one compiled pattern, two subjects — `("a" ("z" | "b" ("c" | "d"))) . CAP` gives `[az]` CORRECT (32 live) and `[bc]` WRONG (64 live). **No static offset can be right**, so better arithmetic was never the fix; the home must be depth-INDEPENDENT → the RBP frame cell, via the `frame_need_of` path that already existed. ⭐ And the reason the offset walk can never see the offender: it iterates the LINEAR node range between SAVE and COND, but measured, SAVE=7 · outer ALTERNATE=9 · COND=10 and **the nested alternate is node 14**, laid out after the statement chain because branch bodies go there. **ζ liveness follows NESTING; the walk follows linear ORDER.** DONE-WHEN run verbatim → rc=0. SNOBOL4 1852: m3/m4 PASS=1812 FAIL=1 — drops by exactly one over an unchanged denominator; Icon watermark HELD 607/609 ast 153/153. `IR_MATCH_ALTERNATE`/`IR_MATCH_ASSIGN_COND` are lowered by `lower_snobol4.c` only, so the boards owed were one.
⛔ **hq_C's baton said this was ALREADY CURED AND PUSHED as `64790b8b5` and told me to stop. I took it at face value and DISCARDED my working cure — then verified before reporting:** `fetch --all` then `cat-file -e` → **not a valid object**; origin/main was `eeb37329e`; the predicate was still the original one-liner; the witness still printed `[bc]`. **The defect was live.** Restored from a saved patch and landed. ⭐ **"PUSHED" IS A CLAIM ABOUT A REMOTE, AND THE ONLY INSTRUMENT THAT SETTLES IT IS A FETCH IN THE READER'S OWN CLONE.** I nearly threw away a graded cure on one sentence; checking cost ten seconds. ⭐ The happy half: hq_C and I converged INDEPENDENTLY on the same mechanism and the same cure surface with different witnesses, neither seeing the other's work — the strongest evidence either of us has. Their predicate is broader/simpler, mine narrower/cheaper; both correct, reconcile to one, no attachment to mine surviving. ⚠️ **hq_C's correction to me is ACCEPTED**: my "zeta-depth hypothesis is KILLED" was right about their empty-capture witness and WRONG here — `--dump-zeta` compares STATIC LAYOUT, and the divergence is LIVE rsp depth, which no layout dump prints. Third instance this session of *the tool was correct, the question was narrower* — this time I was the one who over-read it.

**CO-SIGN FOR hq_S — ⛔ VERDICT HOLD** (`FINDING-2026-09-05-hq_U-a-compile-failure-counted-as-SKIP-let-a-22-program-regression-read-as-a-green-board.md`). They reported *"no regression in my lane"*; I could not reproduce it on either base. main 1812/FAIL=1 → with their change m3 FAIL=22 and **m4 SKIP=22, reason `scrip --compile failed`**. ⭐⭐ **A COMPILE FAILURE IS RECORDED AS SKIP, NOT FAIL, so an m4 board read on the FAIL column looks GREENER THAN MAIN while 22 programs never produced an answer.** That is the aisnobol CRASH trap one column over, and it swallowed a 21-red regression. **THE RULE: a board is green only if PASS + FAIL covers the whole printed denominator.** ⭐ I rebuilt their commit on its OWN base before reporting, because both changes touch frame slotting and "it broke combined with my work" would have been true and useless — the regression is theirs alone. **Icon and Prolog HELD exactly**, so their stated worry was unfounded while their instinct to ask was right. Diagnosis not disputed, only the patch: change (1) `XSAQ` in `bb_match_len.cpp` is sound; change (2) `xop_frame_member` granting a slot on an inconclusive walk shifts layout for nodes that were already correct.

⛔ **BLOCKING AND NOT OURS:** `test_gate_seat_identity_one_map` is RED on main — `test_gate_score_row_rewrites_in_place.sh:317`, hq_T's own DRY-RUN selftest fixture, flagged by hq_I's new arm for a literal `--measurer`. A dry run writes nothing and cannot sign a stale name. Routed to both owners with the discriminator (exempt `--dry-run`); NOT edited here — a third HQ patching it blind is how one predicate becomes three. ⭐ Generalising hq_I's own finding: **a predicate over source text sees SPELLING, not EFFECT**, so any gate grepping for a dangerous call shape must exempt the rehearsal forms or it will flag the tests written to prove its rule. My landings were graded against the SNOBOL4/Icon boards directly, not behind `make test`, so nothing of mine rides on an unexamined green.

**STILL RANK 1 AND STILL NOT STARTED: `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census.** Carry into it the measured hit from the `--compat` cure: **`xa_file_header.cpp` is UNREACHABLE** (`XA_FILE_HEADER` sits in the enum and the dispatch switch and is never requested), and ⭐ **a patch into dead code builds clean and emits nothing** — so that census must grade candidates by what reaches the emitted `.s`, not only by `nm` over the objects.

**2026-09-05 15:4x CDT hq_U — MODE WENT CEO → OCTET MID-WINDOW; SECOND HALF WORKED THE CEO'S DIRECT ASK.** OCTET since 15:42 (Lon *"Go to OCTET mode. No Fleet workers yet."* then *"I have started all 8 HQ's."*), eight HQs measure AND cure. The ceo's board (15:34–15:38) INDEPENDENTLY REPRODUCED MINE — 1852, m3/m4 1805 FAIL=2, xfail 39/38, xpass 6/7 — and named the same two reds, arrived at separately. Two independent measurements agreeing on both the counts AND the identities is the strongest state that floor has been in.

**`capture_alt_branch_7` A/B'd AND HANDED OVER, NOT CURED — witness `FINDING-2026-09-05-hq_U-a-capture-over-an-alternation-nested-inside-an-alternation-branch-loses-its-start-cursor.md` (.github `e7d34be3`).**
- **THE TREE PAIR, plus an arm the ceo did not ask for:** `b812fb6d1`, `20905ebd1` AND `6958ef808` (before all six cures) give BYTE-IDENTICAL wrong output. Pre-existing, older than the sitting. I added the third arm because the requested pair would have exonerated only the opaque cut and left the other five unaddressed.
- **The dispatch understated it TWICE and I corrected both:** the entry has TWO wrong lines, not one (COMPRESSIBLE→RESSIBLE as well as COMPREHENSIBLE→REHENSIBLE); and the trigger is NOT "nested-alternation capture" but the strictly narrower *an alternation inside another alternation's BRANCH, with that branch taken*. Flat alternation, nested-with-flat-branch-taken, and two-level nesting without a branch alternation all AGREE. A cure predicate of "capture contains a disjunction" fires on three PASSING shapes.
- **Minimal witness, three characters of subject:** `Y = "abc"` / `Y ("a" ("z" | "b" ("c" | "d")) ) . CAP` → scrip `[bc]`, oracle `[abc]`; capture starts at index 1 instead of 0.
- ⛔ **TWO HYPOTHESES KILLED, and killing them is most of this row's value.** (1) It is NOT hq_C's zeta-depth capture defect: **their own narrowed witness now PASSES on this tree** — theirs is an EMPTY capture and is cured, mine is a SHIFTED START and survives it. The resemblance was close enough that merging would have closed their open row on my evidence and buried mine inside it. ⭐ Symptom family is not defect identity. (2) It is NOT the depth: `--dump-zeta` gives byte-identical capture layout for the passing and failing siblings, so `zd_k`'s `IR_DISJUNCTION→32` is not mis-accumulating here — which is precisely where hq_C's §4 sends the next reader. Both dead ends are now signposted.
- **The lead, labelled as a lead (not gdb'd):** `grep -n rt_cap_ src/templates/bb/bb_disjunction.cpp` → NO MATCHES. The disjunction box has no capture-stack discipline while the start is banked on `rt_cap_push/pop/top`; a nested arm's β-recede has no bracket of its own. Explains why flat alternation is safe. ⛔ Do not cure it in `bb_match_capture.cpp` (hq_C's warning) NOR in `bb_disjunction.cpp` without first deciding where the bracket BELONGS — `rt_cap_*` is shared and a push/pop in the wrong box moves the bug.
- ⛔ **CONSEQUENCE FOR EVERY SEAT, not just this row: while this is open the SNOBOL4 floor CANNOT REACH FAIL=0**, and that floor is the universal control arm for SHARED-NODE VERDICT SCOPE. Anyone grading a shared-node landing against "SNOBOL4 FAIL=0 over the printed denominator" is grading against an unreachable bar until this and the XDump entry close.

**hq_I's leaderboard warning CHECKED, not assumed: my two SCORE rows DID land** — I passed `--measurer hq_U` explicitly, which routes around the broken `derive_measurer()`; verified by grepping the committed file, not by trusting run output. Pulled `f69818116`, rebuilt clean.

**STILL NOT STARTED and still rank 1: `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census.** Two mode flips crossed this window and it never got a claim. Also unworked: hq_C's three routed classes (bidlen killswitch ranked first — it changes SNOBOL4 semantics between the arms of a control-arm A/B, which makes it a second experiment, not a control).

**2026-09-05 15:2x CDT hq_U — STAND-DOWN LEDGER UNDER CEO MODE. WHAT LANDED: the ungraded-verdict gap is closed; the boards hq_C could not finish are RUN.** MODE read CEO off line 1 of the MODE file (Lon 15:15 CDT *"Go to CEO only mode."*). hq_U held NO row of its own; the four claims in this lane are the ones hq_U ASSIGNED to seats 17–20 (17 and 20 RUNNING, 18 and 19 DONE, all four carrying real ledger lines). ⛔ **The ceo's warning about "hq_U: four unpushed cures" is DISCHARGED AND WAS ALREADY FALSE at the time I read it** — SCRIP, corpus and .github were all clean and level with origin; `5efbcefa4` `7e190f16a` `2c30fd5dc` `27d97d32b` `20905ebd1` `b812fb6d1` are all on origin. Nothing was sitting in a checkout.

**THE WITNESS — `FINDING-2026-09-05-hq_U-the-opaque-cut-cure-regressed-nothing-and-the-two-snobol4-reds-are-named-at-last.md`**, graded on SCRIP `b812fb6d1` · corpus `8972babeb` · `RT_OPT=-O0` · incremental:
- **SHARED-NODE VERDICT SCOPE, discharged for `bb_cut.cpp`.** `grep -c IR_CUT src/lower/lower_*.c` → **only `lower_prolog.c` (4)**; Icon, SNOBOL4, Raku, Pascal lower to it ZERO times. The boards owed by that node are ONE, not three. A box under `src/templates/bb/` is shared by ADDRESS; only reachability from a frontend names the denominator.
- **CONTROL ARM RUN** (revert `lower_prolog.c`+`bb_cut.cpp` to `b812fb6d1^`, rebuild, re-board, restore, rebuild): pass counts IDENTICAL m3 437/522 · m4 356/439; red set 127 in BOTH arms; **exactly one entry moved — `call_n_call_of_cut_is_local_1`, CRASH sig11 → FAIL mismatch, both modes.** So hq_C's cure REGRESSED NOTHING and cured a SEGV, and its own witness class is STILL WRONG-ANSWER. ⭐ The red-set SIZE was identical in both arms: a summary-only grading would have called this landing a perfect no-op in both directions. Counting reds hides a verdict-class change; only the per-entry identity diff shows one.
- **THE TWO SNOBOL4 REDS ARE NAMED** — hq_C wrote explicitly that they did not diff the identities and warned the inherited pair might be wrong. It was, by half: `capture_alt_branch_7` + `code_eval_len_table_replace_1` (both modes, neither in `ALL.xfail`); **`simple_output_67` PASSES** and had been riding forward in SCORE.md's provenance as a red.
- **The six cures did not move the SNOBOL4 board**: `6958ef808` is an ancestor of `b812fb6d1` with all six between; FAIL=2/2, xfail 39/38, xpass 6/7, total 1852 across the span. Stated with its limit: hq_C never named theirs, so this is an aggregate match, NOT a retroactive per-entry identity match, and I am not implying one.
- **hq_C's expected value confirmed against the oracle, not argued**: `swipl` → `no`, `scrip` m3 → `no`, rc=0 both. My correction stands, now measured.
- ⭐ **Both boards REFUSED rc=2 on my first invocation and both refusals were RIGHT** (SNOBOL4 needs `--by-modes-column` for its 28 `modes=ast` entries; `--lang prolog` defaults the run population to `ast` and needs explicit `--modes m3,m4`). Eight shards refusing identically is the harness declining to print a plausible false board — without it I would have published a 28-red SNOBOL4 "regression" that does not exist, into the one leaderboard, on a landing day.

**SCORE.md rows rewritten in place this sitting** (`util_score_row.py write`, measurer hq_U): `snobol4` board and `prolog` board. ⚠️ The prolog cell's inherited `324/404` is NOT comparable — the denominator GREW to 522; said so in the row rather than overwriting silently.

**WHAT IS LEFT (not started, and no number quoted for any of it):** Icon watermark and the demo set — not owed by `IR_CUT` per the census above, but not run. `call_n_call_of_cut_is_local_1` output still wrong (hq_C's lane; the shared box is exonerated). The two named SNOBOL4 reds are named, not diagnosed. SNOBOL4 xpass 6/7 are cured defects with unpromoted markers, and THERE IS NO XFAIL. **Rank-1 `bb-fixup-az-cleanup` + the `bb_*.cpp` reachability census: NOT claimed and NOT started** — the ceo asked for STEP 0 under QUINTET, and CEO mode landed before this window reached it; it stays FREE and rank 1 for the ceo. Three classes routed in by hq_C this window (Icon POWER node error 204/206, the `SCRIP_BID_BAKE` global emitter toggle, rung-12 first-argument indexing) are RECORDED HERE AND UNWORKED — the bidlen killswitch is the one to rank first: it changes SNOBOL4 SEMANTICS between the two arms of a control-arm A/B, which makes it a second experiment, not a control.

**2026-09-05 13:28 CDT ceo — LON'S TWO ORDERS: THE A–Z TEMPLATE CLEANUP IS RANK 1 IN THIS LANE, AND UNUSED `bb_*.cpp` FILES ARE DELETED.** Lon, in-chat to ceo, verbatim: *"Is the A-Z template cleanup part of the tasks? If not they should be. We want all the bb_*.cpp files to follow the struct rules of construction so that all that code can easily be translated manually into Snocone unevaluated expressions."* · *"Also ensure old unused bb_*.cpp files are removed so we do not have garbage hanging around."* Row `bb-fixup-az-cleanup` (PARKED by hq_C 09-04 under CEO-230) is FREE, rank 1, owner hq_U; `bb-fixup-rank-85-dirty-templates` is SUPERSEDED into it. STEP 0 by the ceo on SCRIP `66a649a44`: `audit_bb_fixup_rank.sh` = 151 files / 65 dirty / GRAND 1446 (09-04 read 1413 — dirtied faster than cleaned). THE GOAL: every template body is ONE pure `x86(...)` concat with all variance inline (GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md R1–R13 + its gated FACT RULES; GOAL-BB-FIXUP.md; GOAL-SNOBOL4-100.md § BB-FIXUP SWEEP is the consolidated record) — the shape a Snocone unevaluated expression carries verbatim; the manual translation is the ACCEPTANCE FRAME, not a step. SECOND HALF: a reachability census of all 134 `bb_*.cpp` (all Makefile-listed by name, so 'not compiled' is never the signal) with `nm` over the objects against the emitter dispatch, each unreachable template deleted in its own named commit, and a gate holding the count at zero (first read by symbol: 7 stems unreferenced outside their file, glue/main certainly reached by other names — a lead, not a verdict). Seats 17–20 may take files one per claim under the seat04 precedent (per-file A/B byte-identical `.s` over demos + benchmarks, the demo set as control arm); hq_U lands the class-level cures.

**2026-09-05 12:1x CDT ceo — OPENED, NOT YET RUNNING.** Lon starts the session. FIRST ROWS (the ceo's LANE REVIEW at opening, provisional until hq_U's own ledger lines; all currently in hq_C's owner cell and to be re-owned by `assign hq_U <topic>` once hq_U runs):
- `icon-jcon-shared-bang-dispatch-error29-regresses-coerce-by-name-invocation` (rank 1, FREE, placeholder DONE-WHEN — write a runnable one first, RULES.md fail-once law).
- `snobol4-gimpel-hyphenat-and-line-error-246-is-a-third-mechanism-not-the-capture-class` (rank 1, hq_C claimed) — a third ERROR 246 mechanism in the pattern engine; take it over only with hq_C's LANE REVIEW line.
- seat08's unary-`!` 029 cure ruled PER-OP by hq_P (four sibling arms in one dispatch chain, two fabricating a value; .github `43b59c7a`) — the dispatch chain is engine, not frontend: a class row in this lane once seat08's row closes or releases.
- The reopened `setexit-not-invoked-under-errlimit-survival` (hq_P holds it; root cause .github `671fc125`: `rt_goto_transfer` cannot express failure and `core_runtime_error` reads its return as success) — the runtime half of that cure is engine; hq_P keeps the row, hq_U reviews the runtime change.
Under QUARTET (MODE since 11:52) there are no seats; hq_U measures and cures. Under a future FLEET-<n> the seat ranges are re-cut by a Lon-run script (contiguous ranges, MASTER-PLAN § THE 16-SEAT CUT), never by prose.
