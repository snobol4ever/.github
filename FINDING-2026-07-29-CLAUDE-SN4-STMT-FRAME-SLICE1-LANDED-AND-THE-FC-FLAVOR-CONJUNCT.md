# FINDING 2026-07-29 (s21x-d) — SN4 STMT-FRAME SLICE 1 LANDED OPT-IN GREEN, AND THE LICENSE MUST READ THE STORAGE PLANNER'S FLAVOR, NOT THE KIND

**Design of record:** the s21x-c LON DIRECTIVE block atop `GOAL-SNOBOL4-BB.md` (per-BB one-instruction carves; sliding RSP offsets; RBP only at STATEMENT/FUNCTION/ARBNO/FENCE1). This session executed its named NEXT: **the per-graph all-or-nothing regime — classifier + env switch + first consumer wiring of the s21x-c encoders + per-BB carves at the emit choke point.** Lon re-issued the directive live at session start (roman `sub rsp,1344` counterexample pasted verbatim).

## WHAT LANDED (SCRIP, one commit; env `SCRIP_STMT_FRAME=1`, DEFAULT OFF)
- **`emit_stmt_frame_scan`** (emit.cpp, beside the FB-STMT scan): slice-1 classifier, wired at the `emit_chain` choke (FLATDISP-5b single-writer discipline). ADMIT iff every node REACHABLE from entry is in the closed whitelist {IR_LIT_INTEGER, IR_VAR, IR_BINOP, IR_ASSIGN} **AND every producer (LIT/VAR) is in zeta_storage's fc registry** (`fc_geom(nd,&k) != 0`). Guard conjuncts mirror FB-STMT (outer mains only: !jmp_entry !pat !gen !gen_proc !resumable nparams==0). `g_emit.flat_stmt_frame` appended at struct end (s141 ABI law).
- **Drive-loop consumers**: per-head stubs `st_x` (x86_stmt_leave, fall-through) + `st_pre` (x86_stmt_enter) at every `bb_src_of` head, first-emitted head st_x-exempt (prologue is its only entrant); edge retargets beside the SPD-2 scanhit idiom (head edges → st_x/st_pre; chain exits → two leave-cut stubs `_stγ/_stω` emitted before the lbl_γ define). **Zero templates touched** — the cut's `mov rsp,rbp` makes every legacy inline pop dead-but-harmless.
- **xa_flat**: four pad-only regime arms (BINARY+TEXT × prologue/epilogue): `sub rsp,8` in, `eax code + add rsp,8 + ret` out; no carve, no `rep stosb`, no anchor, no rbx, no rbp. BINARY keeps the legacy `cmp esi/jne β` entry protocol; TEXT mirrors legacy's absence of it (pre-existing per-medium asymmetry, replicated not repaired).
- **Seed**: `SCRIP/seed/test_sno_stmt_frame_3.{sno,s}` — the FIRST COMPILER-EMITTED regime artifact (seeds 1/2 are the hand embodiments), oracle-exact.

## GATES (all run this session)
Default arm: **compiler output byte-identical** (witness diff + all five `.s` regen sweeps = zero changed across 1,144 artifacts: benchmark, demo, feature, crosscheck, programs) · watermark **m3 329/5 · m4 324/2 SKIP=8** with fail-set membership diffed programmatically EMPTY vs the s21x-c baseline. Regime arm (`SCRIP_STMT_FRAME=1` corpus-wide): **same watermark, fail-set diff EMPTY** — the "may only add passes" license met with zero churn. Witnesses sl1/sl2/sl0 + the seed oracle-exact BOTH modes. `test_gate_rbp_census_ratchet` unseeded=0 · `test_gate_emit_no_lang` OK · `test_gate_template_medium_invisible --strict` FAILS **identically at HEAD** (pre-existing WIP baseline, verified by stash A/B — not this session's regression). Armed census: **7 of 316** crosscheck programs take the regime (023/024/025 add-sub-mul, 029 precedence, 010 assign_integer, 002 output_integer_literal, 007 output_null_var) — all pass both modes in the new shape.

## ⭐ HEADLINE DEFECT + LESSON — 026/027 SHOT THE RETURN ADDRESS; THE GATE MUST READ WHAT THE EMITTER WILL DO
First regime watermark: m3 held, **m4 dropped exactly 026_arith_divide + 027_arith_exponent** — programs the kind-whitelist ADMITS. Emitted-byte autopsy: their lits take the **flat-slot template arm** (`mov [rsp+32..56]`), whose offsets presume the graph carve's region; under the regime (no carve) offset 40 at bracket depth is **B+24 = the caller's return address**, overwritten by the literal's descriptor word → `ret` to garbage. m3 passed on entry-layout luck only — the write is equally wrong there. ROOT: the kind whitelist and the storage planner are INDEPENDENT authorities — zeta_storage's fc registration (the s191 FORTH machinery) declines DIV/EXP trees while accepting ADD/SUB/MUL, and the FLAVOR, not the KIND, selects the template arm. FIRST FIX FALSIFIED SAME SESSION: rejecting any reachable `zls_off(c) >= 0` over-rejects — grants exist even on fc-emitted nodes (sl1's lits carry zls_off=32 yet carve TOS). MEASURED RULE (per-node fc_geom census on sl1 vs 026): producers are fc=1 k=16, consumers (BINOP/ASSIGN) are fc=0 by design (TOS, no own cell) — the conjunct is **producer-only**: `(LIT|VAR) && !fc_geom => reject`. `fc_geom` is const, registry-final at the choke because `emit_chain` runs `zls_fct_finalize` first.

## ⭐ SECOND DEFECT (caught by rsp-trace before it ran) — CROSSING EDGES MUST LEAVE-THEN-ENTER
The first wiring retargeted head edges straight to the enter stub: each statement→statement transition leaked the 16B bracket (fresh `push rbp` stacked on the unpopped prior one; chain γ would `ret` off-base). The seed's law is explicit — `stmt_*_end/_fail` cut BEFORE the jmp. Fix = the st_x/st_pre split above.

## DISCOVERIES (banked)
1. **Lower seeds inert per-graph companions**: two `IR_SAVE_RESTORE` nodes (op 14 — the s176 linkage family, already in the enum) + an `IR_CALL "SNO$NRET"` / empty `IR_LIT_STRING` pair, unreachable without DEFINE. Forces REACHABILITY-scoped classification (BFS mirror restricted to whitelist ops — safe superset by construction since a reached non-whitelist op rejects before its unknown edge classes matter); name-testing the companions would be a language sentinel, BANNED.
2. **`emit_chain` fires on multiple graphs per compile**, including a 3-node {SUCCEED, FAIL, NULL} sentinel graph — every per-graph field must assume this (the classifier rejects it via reachable=0).
3. `IR_LIT(c).sval` is union-aliased with ival — printing it unguarded on an integer lit is a %s-on-(char*)1 SEGV (one debug iteration lost to this; guard by op).

## NEXT RUNGS, IN ORDER
1. **Whitelist widen, one construct per rung** (CELL-1a discipline): IR_LIT_STRING / string globals next, then the **IR_CALL slice** consuming `x86_call_frame_enter` + the floaters + `IR_SAVE_RESTORE` roles — the seeds' full DEFINE shape, which retires the proc-linkage family for admitted graphs.
2. **Default-flip license** (⛔ Lon ruling): regime is corpus-green opt-in; flipping default ON needs the call to widen coverage first or Lon's explicit go at 7/316.
3. The dead-but-harmless legacy pops on retargeted edges are cosmetics once a rung wants them gone (template-side, not blocking).
