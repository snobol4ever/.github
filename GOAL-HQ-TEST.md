# ⛔⭐⭐⭐ GOAL-HQ-TEST — hq_T, THE FOURTH HQ (opened 2026-09-03 ~16:30 CDT box clock on Lon's word: *"Open hq_T now."*; T = TEST)

**Root:** `/home/claude_T` · **identity:** `hq_T` (mailbox `/home/resources/postoffice/hq_T/inbox`, ask target `ceo`) · **model:** Claude Opus 5 Max, like the other HQs · **why (ceo, CEO-170):** three HQs answered for sixteen seats while two held the ladder's critical path; hq_P had 11 unread seat asks and hq_B 10 — a seat waiting on its HQ is the fleet's stall shape.

## MANDATE

1. **THE TEST STANDARD PROGRAM** — sovereign `GOAL-TEST-SUITE-CONSISTENCY.md` (Lon 2026-09-03, verbatim there: one test standard for all seven languages). First deliverable: ONE ladder-runner template (`test_<lang>_ladder.sh` body, from `test_prolog_ladder.sh`) and ONE generic port-trace gate the language rows instantiate — never seven copies. Umbrella row `test-suite-consistency-seven-languages-one-standard` (rank 0).
2. **THE LEADERBOARD** — `.github/SCORE.md` under the FACT RULE (every suite run by any session rewrites its row); the helper every runner calls, row `score-md-rows-are-rewritten-by-the-runner-that-measured-them` (rank 0); `handoff_status.sh` WARNs on a row 25+ commits stale.
3. **ASK TARGET** for seat07 (Raku roast), seat10 (Pascal), seat11 (Raku), seat12 (SNOBOL4/Snocone/Rebus), seat14 (Icon trace gate); answer asks in the file each ask names, dispatch the class rows they mint by lane, one seat per lowerer file.
4. **AUDIT NOTHING** — the ceo audits; you measure and cure in your lane (RULES § MEASURE AND CURE), and you never touch a Prolog ladder rung (hq_C/hq_P's).

## SESSION SETUP

`git -C SCRIP pull --rebase origin main` (corpus, .github too) · `cd SCRIP && bash scripts/s4e_msg.sh check` · read this file, then `GOAL-TEST-SUITE-CONSISTENCY.md`, then `RULES.md` in ≤50-line pages · `bash scripts/s4e_msg.sh next` resumes your rank-0 rows. Your first build is `make` (incremental; the pristine law is loosened, RULES § FACT RULES 2026-09-03).

## LIVE CURSOR

**2026-09-03 17:40-18:05 (hq_T, MANDATE 2 — THE LEADERBOARD — CLOSED as a mechanism):** `scripts/util_score_row.py` on disk and in `make test`. `write` rewrites ONE cell in place with a per-column provenance clause (both repo hashes, `RT_OPT` **read from the Makefile, never typed**, box clock, measurer); `check [--no-fetch]` reports every row's staleness against origin; `gate_score_row` in `lib_gate.sh` is the one bash line for runners, non-fatal by design; `test_gate_score_row_rewrites_in_place.sh` (11 arms, <1s, offline) is wired into the blocking set. **`handoff_status.sh` now carries the staleness WARN** the mandate names — warn-only and fetch-free, so it can never destroy a live build the way the `.s` verifier once did.

⭐ **The `check` arm paid for itself immediately, on its first real run:** 6 row-measurements at or past the 25-commit threshold (raku 67 behind, prolog 51, pascal 32/29, icon 25, snobol4 25) and **3 rows — polyglot, rebus, snocone — with no checkable hash at all**. That last class is the *unfalsifiable row* SCORE.md's own text warns about at length and had no instrument to detect; it does now, and it is a standing WARN until those three are re-measured.

⛔ **One defect this session could not close and is naming instead:** the top grid is generated by `util_build_score_md.py`, whose applier splices from `| Language |` through the `_tree:` stamp — so **a full regeneration will overwrite every row written since**, silently and with older numbers where a gate is UNPROVEN. The two tools now disagree about who owns the grid. Mitigated, not cured: a row-write demotes the `_tree:` line to `(LAST FULL REGENERATION ONLY ...)`, since it was already false — it read `generated 2026-09-02T02:35Z` while six of eight rows carried 2026-09-03 provenance from four sessions, claiming a coverage it did not have. **The real cure is that the generator must merge rather than splice** (skip a cell whose row provenance is newer than its own run, or refuse). That is a change to seat08/hq_B's file and it is rowed, not taken.

**Mandate 1 (the test standard):** Raku master re-measured and its inventory cell closed; a false-board trap in `corpus_suite_harness.py --by-modes-column` found and closed with a refusal (details + the control arm in `GOAL-TEST-SUITE-CONSISTENCY.md`'s cursor).

**Mandate 3 (ask target):** seat07's eight-hour-stranded Lon override answered — the override released an obligation, it did not forbid the work, so both it and the Raku re-point stand; moot forward under QUARTET, where the Raku lane is hq_T's. Routed to ceo.

**2026-09-03 16:35 (ceo, at opening):** root populated (clones at SCRIP `e751405f` · corpus `7e415942` · .github `78036d8f`, hooks, settings, digest, `refs/`), first `make` running, two rank-0 rows assigned, five seats re-targeted. Nothing measured by hq_T yet. Whoever sits here first: the ladder-runner template is the one thing five seats are waiting on.
