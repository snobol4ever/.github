# ⛔⭐⭐⭐ GOAL-HQ-TEST — hq_T, THE FOURTH HQ (opened 2026-09-03 ~16:30 CDT box clock on Lon's word: *"Open hq_T now."*; T = TEST)

**Root:** `/home/claude_T` · **identity:** `hq_T` (mailbox `/home/resources/postoffice/hq_T/inbox`, ask target `ceo`) · **model:** Claude Opus 5 Max, like the other HQs · **why (ceo, CEO-170):** three HQs answered for sixteen seats while two held the ladder's critical path; hq_P had 11 unread seat asks and hq_B 10 — a seat waiting on its HQ is the fleet's stall shape.

## MANDATE

1. **THE TEST STANDARD PROGRAM** — sovereign `GOAL-TEST-SUITE-CONSISTENCY.md` (Lon 2026-09-03, verbatim there: one test standard for all seven languages). First deliverable: ONE ladder-runner template (`test_<lang>_ladder.sh` body, from `test_prolog_ladder.sh`) and ONE generic port-trace gate the language rows instantiate — never seven copies. Umbrella row `test-suite-consistency-seven-languages-one-standard` (rank 0).
2. **THE LEADERBOARD** — `.github/SCORE.md` under the FACT RULE (every suite run by any session rewrites its row); the helper every runner calls, row `score-md-rows-are-rewritten-by-the-runner-that-measured-them` (rank 0); `handoff_status.sh` WARNs on a row 25+ commits stale.
3. **ASK TARGET** for seat07 (Raku roast), seat10 (Pascal), seat11 (Raku), seat12 (SNOBOL4/Snocone/Rebus), seat14 (Icon trace gate); answer asks in the file each ask names, dispatch the class rows they mint by lane, one seat per lowerer file.
4. **AUDIT NOTHING** — the ceo audits; you measure and cure in your lane (RULES § MEASURE AND CURE), and you never touch a Prolog ladder rung (hq_C/hq_P's).

## SESSION SETUP

`git -C SCRIP pull --rebase origin main` (corpus, .github too) · `cd SCRIP && bash scripts/s4e_msg.sh check` · read this file, then `GOAL-TEST-SUITE-CONSISTENCY.md`, then `RULES.md` in ≤50-line pages · `bash scripts/s4e_msg.sh next` resumes your rank-0 rows. Your first build is `make` (incremental; the pristine law is loosened, RULES § FACT RULES 2026-09-03).

## LIVE CURSOR

**2026-09-03 16:35 (ceo, at opening):** root populated (clones at SCRIP `e751405f` · corpus `7e415942` · .github `78036d8f`, hooks, settings, digest, `refs/`), first `make` running, two rank-0 rows assigned, five seats re-targeted. Nothing measured by hq_T yet. Whoever sits here first: the ladder-runner template is the one thing five seats are waiting on.
