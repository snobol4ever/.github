# HANDOFF_2026-05-19l.md — EC-WASM-SM: WASM arms in all SM_templates

**Date:** 2026-05-19
**Session:** Claude Sonnet 4.6
**Goal:** GOAL-HEADQUARTERS.md — EC-WASM-SM (unified emitter SM_templates)

---

## What was done

### EC-WASM-SM (one4all `268619c1`, two commits)

**Commit 1 (`6d949969`):** Add IS_WASM arms to all 5 SM_template files:
- `sm_push_pop_lits.c` — 7 functions: push_lit_i/s/f, push_null, void_pop, push_var, store_var
- `sm_arith.c` — 9 functions: concat, neg, coerce_num, exp, add, sub, mul, div, mod
- `sm_compare.c` — 3 functions: stno, acomp, lcomp
- `sm_control.c` — 7 functions: jump/s/f, halt, return/freturn/nreturn (×3 variants each)
- `sm_pat.c` — 30 functions: full SM_PAT_* + SM_EXEC_STMT
- `sm_template_common.h` updated: IS_WASM note + `wasm_intern_str`/`wasm_intern_name` declarations
- `emit_core.c`: `wasm_intern_str` and `wasm_intern_name` promoted from `static` to extern

**Commit 2 (`268619c1`):** Wire `emit_wasm_from_sm` to call SM_template functions:
- `sm_templates.h` created — forward decls for all SM_template functions
- `emit_core.c` includes `SM_templates/sm_templates.h`
- `emit_wasm_from_sm` switch body replaced: each opcode now calls the template function instead of inlining the WAT fprintf calls
- `sm_ctx_t ctx` constructed per-instruction for control/return family
- Call/SuspendValue still inline (WASM frame logic requires WasmUserFn* lookup, not yet in template)

---

## State

```
one4all: 268619c1  (EC-WASM-SM)
corpus:  92e103f   (unchanged)
.github: (this commit)
--interp:  194/265  (held)
broker:    23/26    (held)
smoke ×2:  7/0 5/0 (held)
```

## Architectural fact now true

Every SM opcode (except CALL_FN/SUSPEND_VALUE frame logic) dispatches through one template
function that handles all modes via IS_JVM/IS_JS/IS_NET/IS_WASM. The inline WASM-only
fprintf blocks in `emit_wasm_from_sm` are gone — replaced by template calls.

## Next

Remaining work toward full unification:
1. CALL_FN/SUSPEND_VALUE frame logic into a template function (needs WasmUserFn* accessible from template)
2. Wire JVM/JS/NET walkers to call SM_template functions instead of their own inline switch arms (drains the silo per-target switch bodies in emit_core.c)
3. Eventually: delete the per-target inline switch bodies; only one switch exists (the SM_templates dispatch)
