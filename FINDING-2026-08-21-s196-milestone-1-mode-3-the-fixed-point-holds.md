# FINDING s196 — MILESTONE 1, MODE 3: THE FIXED POINT HOLDS
**HQ (Claude Fable 5), 2026-08-21 s196. Verified at pristine (HQ-27). The both-modes DoD is NOT claimed — the m4 lane remains.**

## THE MEASUREMENT
`cd corpus/programs/snobol4/demo/beauty && scrip beauty.sno < beauty.sno` → rc=0, **40,971 bytes, byte-identical to the input file** (`diff` empty; md5 `6f1671c0757729992ae01a6bdf16f081` both sides), on the native engine, mode 3, at pristine SCRIP HEAD. `board_beauty_m1.sh` reads **m3 10/10, first red: none**, and prints its own `⭐M1-FIXED-POINT` marker on the full-file rung. Batteries at the same tree: prechain 7/7 · **ptc 82/82 (the full grid)** · nret 3/3 · fw 7/7 · smokes SNOBOL4 7/7 both modes, Icon 14/14, Prolog 3/5 standing. Regens run (no movers — the cured shapes are runtime-built).

## THE TWO BRICKS THAT FELL TODAY (each one line, each earned by the instruments Lon ordered built)
**Rung 40 — the want-name discard.** `rt_proc_call_prologue` received the banked want-name parameter and never used it: every interior full-road call permanently destroyed the ambient want, so capture-target thunks (the `&`-built reduce actions, `epsilon . *EXPR$…` with NRETURN tails) had their by-name results rvalue-ized at the fragment boundary and the s191 strict guard — correct per the oracle — refused the corpse into `MATCH_END` ω. The design was already whole (compile-time `want_name` flag, emitted `SNO$WANTNM` arm, tail keep): one line — `rt_g_want_name = wn` at prologue end — completed it. Found by the 2-way monitor walking the shipped road to 2,089 agreed events with the new skip knobs, then a gdb write-ledger on the flag.

**Rungs 80/160/320/622 — the missing defer term.** The zd operand-offset staging (`emit.cpp:2967`) compensates a reader's distance to its producer's spine cell for MATCH_BEGIN frames (+64) and arm records (+32) — and never for `IR_MATCH_DEFER`, whose **both** runtime arms ride a 16-byte record into the continuation on success (blob road: the caller PUSH-PUSH wire pair; run_all road: `{saved-cursor, resume}`), read straight off the emitted `.s`. Every box downstream of a defer read its pre-chain operands 16 bytes off — `Gen.inc:42`'s LEN computed a 0 indent and the beautifier's output collapsed. One clause — `+16` per defer in the `_xh` walk — and the ladder ran to the fixed point. Found via the monitor's first-ever VALUE-payload divergence (step 3,044), the first standalone witness in seven attempts (six lines, `corpus/probe/prechain/`), and ASM-DIFF.

## WHAT REMAINS FOR FULL M1 — the m4 lane (3/10), ONE mechanism family, measured
`ss_Parse` (rungs 40+) is a downstream symptom (beauty.sno:466's computed goto sees `t='Parse'` only on a wrong tree). The behavioral face: after 1,781 payload-identical events, one SHIFT/REDUCE decision inverts (SPL/m3 → nPush/202; m4 → Reduce/364); the six-char witness is now rc=0-SILENT in m4. The root family, measured side-by-side with `SCRIP_EXPR_DBG`: **fragment lowering is not image-invariant** — the m3 process's runtime EVALs dedup-match the static compile's thunk registry (unsalted reuse, `t=19, want_name=0`) while the m4 image's fresh process mints salted direct-call thunks (`EXPR$0F1`, `t=45, want_name=1`) — the same reduce-pattern text, structurally different code. Row `m4-fragment-lowering-parity`, rank 0, first step written.

## ⛔ FALSIFIED THIS SESSION — do not re-spend
The `rt_nret_fix` deref removal (breaks rvalue NRETURN) · the want-lifecycle repairs alone (necessary hygiene, insufficient) · the `zd_k` PATV$ K-flip (no effect on the witnesses) · scalar pre-chain order in either direction (each `SCRIP_PRE_ORDER` arm fails a different prechain witness) · standalone reconstructions five and six (the carrier had to be caught in-beauty first; the seventh attempt, cut from the monitor's payload bracket, finally reproduced standalone).
