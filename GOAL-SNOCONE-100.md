# ⛔⭐⭐⭐ GOAL-SNOCONE-100 — SNOCONE: C-SYNTAX SNOBOL4 SELF-HOSTS ITSELF, ONE PLAN, ONE FILE

**Minted 2026-08-27 by ceo on Lon's order (GOAL-CEO.md CEO-30), consolidating this session per THE LOOP task `goal-consolidate-snocone`.** Lon, in-chat to ceo, verbatim in substance: *"We want one GOAL file for each language at this point."* Pattern authority: the three existing `-100` files (`GOAL-SNOBOL4-100.md`, `GOAL-ICON-100.md`, `GOAL-PROLOG-100.md`) — LIVE CURSOR at top, FRONT STATUS, standing laws with provenance, rungs.

**This file REPLACES (deleted this session; full text in `.github` git history at `70c6b7f3` and before):** `GOAL-LANG-SNOCONE.md` · `GOAL-PARSER-SNOCONE.md` · `GOAL-PST-SNOCONE.md` · `GOAL-SNOCONE-BEAUTY.md` · `GOAL-SNOCONE-CLAWS5.md` · `GOAL-SNOCONE-DEMOS.md` · `GOAL-SNOCONE-IN-SNOCONE.md` · `GOAL-SNOCONE-IR-BB.md` · `GOAL-SNOCONE-SM.md` · `GOAL-SNOCONE-SWITCH-BACKENDS.md` · `GOAL-SNOCONE-TREEBANK-LIST.md`. Any reference to one of those eleven names, anywhere in `.github`/SCRIP/corpus, resolves HERE. Their standing laws are restated below; per-file dispositions are in the LEDGER at the bottom. **NOT absorbed:** `ARCH-LANGUAGES.md` (the language/front-end spec — single source of truth for Snocone syntax, still separate and still authoritative; consult it, do not duplicate it here).

⛔ **CONSOLIDATION DISCLOSURE — read before trusting anything below as current.** The eleven source files span 2026-04-13 through 2026-07-21 — real, substantial technical history, faithfully carried forward — but **the single most important fact about Snocone's current state exists in NONE of them**: see FRONT STATUS immediately below. Per this project's own standing lesson (root `CLAUDE.md`'s Lon-path finding): a durable file must not inline a volatile fact, and the live task queue is where Snocone's truth is moving fastest right now. Where two absorbed files disagree, both readings are kept below with their dates — this is a MOVE, not a rewrite; no law text was altered, and no conflict was silently resolved.

## LIVE CURSOR

- **SC-PARSERS-NO-REWRITE-2026-09-24 (hq_snocone, SCRIP `b83cdf352`) — ⛔⭐⭐⭐ LON'S RULE: THE parser_*.sc PROGRAMS ARE NEVER REWRITTEN; A BUG FIX IS A FEW CHARACTERS.** Verbatim: *"Ensure there is no re-write of any of the parser_*.sc programs. At most a few characters will need to be chaned to fix a bug."* and *"what possesed you to rewrite. do you need to train on SPITBOL manual. And the ARCH-*PARSER*.md files. *SYNTAX*.md files. Get with the program."* The 09-23 rewrites of parser_rebus.sc and parser_snocone.sc (SCRIP 241563a2e, d1455f883 — 468 and 501 changed lines, grammars mirroring the C frontends) are WITHDRAWN: both restored byte-for-byte to their 08-23 originals (a8c822e37) at `b83cdf352`; the unpushed parser_snobol4.sc rewrite is withdrawn; all seven parser_*.sc are originals. ⛔ **The SC-PARSERS-2026-09-23 entry below that says "GRAMMARS REWRITTEN AS PEG MIRRORS" and its rebus 49/49 / snocone 458/458 SPITBOL numbers describe the withdrawn rewrites — they are history, not state.** What stays is the environment repair, which is where every real cause was: tree_to_sno.c (structural pattern primitives, bare labels, real literals, underscore names), global.sc's &MAXLNGTH line, tdump.sc's REAL(), and the two zd_plan cures. MEASURED on the originals at `b83cdf352`: sbl arm (transpiled chain on sbl -bf) rebus 36/49, snocone 141/458; .sc arm (SCRIP) rebus 0/6 (FATAL blob_layout_build, a SCRIP defect), snocone 19/101. METHOD from here: read the SPITBOL manual, ARCH-*PARSER*.md and *SYNTAX*.md first; diagnose each failure to a minimal witness; fix SCRIP where the fault is SCRIP's, and a parser only by a few characters.
- **SC-PARSERS-ZD-2026-09-23 (hq_snocone, SCRIP `cbc4ee1cb` corpus `511cb4452`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐ THE SELF-HOSTED REBUS PARSER RUNS ON SCRIP: parser_rebus.sc 6/6 PARSED on its corpus population (util_parser_sc_census.py, the .sc arm), from 0/6.** Every parser_*.sc died printing its tree (bootstrap/tdump.sc) on TWO pre-existing zd_plan defects, both cured with killswitches and a gate: `df6b55cb5` a back edge out of a blob run is an exit (an if/else on a match with an alternation, nested in an if that returns, recursing from a while, leaked 192 bytes a lap and SIGSEGVed on return; SCRIP_ZD_BLOBBACK=0 reds it); `32620db58` ZD-5B alternation arms are opt-in (an armed SPAN(v) corrupted the capture after the alternation, CORRUPT CAPTURE ENTRY, or double-popped on backtrack; SCRIP_ZD_5B=1 reds it); `cbc4ee1cb` test_gate_zd_a_back_edge_leaves_a_blob_run_and_alternation_arms_stay_unplanned.sh (4 witnesses x m3+m4 vs sbl -bf through --transpile, 2 fail-once arms), wired and in the floor. Control arm by killswitch census on one binary over all seven masters: 8 SNOBOL4 entries move, all with the old configuration's verdict and rc in both modes; Icon, Prolog, Raku, Rebus, Pascal, Snocone byte-identical. ARCH-ZD-DEPTH-PLANNER.md §5/§8 carry the ruling. **NEXT:** parser_snocone.sc's .sc arm now dies [ZGC-STALE] (a stale heap pointer, the collector's -- all GC is the cto's, CEO-1181; witness handed over); 5 corpus sources refuse (Parse Error) and are to be declared or cured; then the snobol4 rewrite (in the working tree, --vs-ast 299/306 simple_output), pascal, icon, prolog, raku.
- **SC-BENCH-2026-09-23 (hq_snocone, SCRIP `5ad13645a` corpus `279f3d60a`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ THE SNOCONE BENCHMARKS DOUBLE AS TESTS: 16/16 PROGRAMS PRINT THEIR REF IN ALL SIX RUNS (m3/m4 x process/iter/time), AND THE SUITE IS A ROW OF THE GRID, `SncBench` (Lon, CEO-1221: *"all language benchmarks need to be run through the three-angle harness"*, *"The reason to force a benchmark to also be a test is to guarantee it is not vacuous."*).** **REF:** all 16 `.ref` equal sbl -bf on the SNOBOL4 twin with the same input, and every one pins a computed value (a sum, fib, SIZE, `matched bytes=N` returned by the kernel, the demos' real output) — measured, not assumed. **IN:** every input reader has its `.input` (hq_icon, corpus `ee26cdb9c`). **MEMORY:** demo/json.sc allocates two ARRAY(262144) and DECLARES 16384 KB in `json.heap_kb` (heap clause 7: 8192 aborts rc=134, 16384 matches, both modes; corpus `5c339f31f`). **PRISTINE:** calculator-1.sc and json.sc lost their TIME()/TERMINAL self-timing (clause 1; corpus `279f3d60a`; stdout untouched). **THE HARNESS (SCRIP `5ad13645a`):** `scripts/bench_wrap_snocone.py` writes, from a source it never edits, one program that runs the test (the program's own stdout) and then repeats the workload silently with ONE `BENCH iters= ns=` line on TERMINAL — a `// *BENCH kernel=FN` program repeats FN over data read once, an unmarked program with no INPUT repeats its whole body with OUTPUT spelled as a sink; `scripts/test_snocone_bench_suite.sh` grades every program x m3/m4 x process (tools/bench_rusage) / iter / time, REF diffed on EVERY run, OUT kept per run, timing printed as scouting data only (CEO-1219). **PUBLISHED BY HAND** (SUITES.tsv `snc-bench`, SCORE.md suite table, board-lines, README_RUNNER): util_score_row.py cannot write a benchmark suite row without overwriting the grid's B cell, which is the two-number TIMING reading — that path is the coo's instruments-benchmarks-enter-the-suite-grid row, asked. The README suite table's REPORTED currency arm lags (it did before this row: three Pascal rows and SncM); the blocking --pinned arm is green. **FINDING, NOT CURED:** SCRIP ignores DETACH(.OUTPUT) — after it, `OUTPUT = x` still prints in both modes; SPITBOL stops printing. The SNOBOL4 twins calculator-1/2.sno and json.sno still self-time and json.sno needs the same heap declaration (hq_snobol4's, told).
- **SC-BEAUTY-2026-09-23 (hq_snocone, SCRIP `ecf3aea38` corpus `d0e86b9ca`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ THE SNOCONE BEAUTY SUITE READS 20/20 IN BOTH MODES WITH NO XFAIL, AND A WIRED GATE GRADES IT EVERY BUILD (Lon's first: *"Get snocone beauty test suite working first."*, CEO-1212).** The suite is the container `corpus/tests/snocone/beauty_modules.sc` + `.ref` (test_Gen .. test_tree). **CURES (SCRIP, one defect each, each with a killswitch that reds its witness):** `fb66a2cb2` zd_plan releases a replacement's subtree at MATCH_REPLACE when no match watermark is open (SCRIP_ZD_REPL_FREE=0 reds test_Qize, test_case); `73214b8e0` an omega edge back to an earlier node of the run is an exit, as a gamma back edge already was (SCRIP_ZD_OBACK=0 reds test_strings, test_trace); `5b228d355` an if/else whose test is a pattern match seeds its else arm from the match's omega depth (SCRIP_ZD_MATCHDIAMOND=0 reds test_trace); `680219f04` --transpile sanitizes a leading-underscore procedure name in its DEFINE prototype and at every call. **FAULTY TEST FIXED (corpus `d0e86b9ca`):** test_ShiftReduce wrote `.value(_s)` on a tree (fields t v n c; `value` is the stack link's field) -- ERROR 041 on sbl -bf and SCRIP alike; the library's own Shift reads `.v(s)`. Four banners were stale XPASS markers; all 8 are promoted. **WIRED:** `test_gate_snocone_beauty_suite_reads_every_test_in_both_modes_with_no_xfail.sh` (`ecf3aea38`, blocking, in the wiring floor 485->486) replaces test_beauty_snocone_all_modes.sh (renamed into it) and test_beauty_snocone_subsystems.sh (retired): both read corpus/snocone/demo/snobol4/beauty/test, gone since the 08-29 re-grid, so the suite had been graded by nothing for three weeks. **CONTROL ARMS (base = clean `73e02c870`, head = the series on it; the series was then rebased twice onto `ecf3aea38`'s parent with no conflict and re-proven there):** asm with file:line comments normalized is byte-identical for every Icon 826, Prolog 563, Raku 920, Rebus 43 master entry; 41 Snocone, 26 Pascal and 12 SNOBOL4 entries move and every one prints the clean tree's stdout and rc in both modes (82/82, 52/52, 24/24 entry-modes); attribution by killswitch: REPL_FREE 1 Snocone + 5 SNOBOL4, OBACK 26 Snocone + 26 Pascal + 6 SNOBOL4, MATCHDIAMOND 15 Snocone + 1 SNOBOL4, all-off byte-identical to the clean tree. The zd gates are green; two zd-adjacent gates are non-green on the clean tree too and unchanged: test_gate_zd_omega_head_acceptance.sh REFUSES (witness corpus/tests/pascal/boolptr.pas is gone), test_gate_fz_release.sh (unwired) is red at LOCK 4 on all eight witnesses. make preflight 60/0. SncM 337/337 both modes on the rebased head. **⛔ HASHES CITED BEFORE THE REBASE:** the MATCHDIAMOND and gate commit messages and corpus `d0e86b9ca`'s message cite pre-rebase hashes that never reached origin (rewording pushed-bound history was refused): bef647970 = `fb66a2cb2`, 978930970 = `73214b8e0`, 0f0f21bbe = `5b228d355`, df7c41084 = `680219f04`, fc2aa9bf3 = `ecf3aea38`. **ROWED ON THE CEO'S ASK (CEO-1218):** `snocone-binary-operators-read-the-spitbol-manual-table-slash-below-star-query-left-and-the-opsyn-binaries-parse` -- `8 / 2 * 2` reads 8 and `12 / 3 * 2 - 1` reads 7 where SPITBOL reads 2 and 1; `?` is right-associative and that chain FATALs in lower_snobol4; binary & @ # % ~ are a syntax error; DONE-WHEN graded live against sbl -bf, RED at mint. hq_snobol4 rowed its own ? twin in snobol4.y.
- **SC-PARSERS-2026-09-23 (hq_snocone, SCRIP `ff2300d0e` corpus `39ca43e26`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ LON SEATED THIS LANE ON THE SEVEN SELF-HOSTED PARSERS: parser_rebus.sno 49/49 AND parser_snocone.sno 458/458 ON SPITBOL; THE ONE DEFECT UNDER ALL SEVEN IS CURED; THE .sc ARM NOW STOPS AT TWO SCRIP RUNTIME DEFECTS.** Lon, in-chat to hq_snocone 2026-09-23 15:2x CDT: *"Get each parser_*.sc working starting with the easiest. First get parser_*.sno working. Then use those working SNO to get the SC ones working. Test the parsers using all the corpus sources."* — and via the ceo (CEO-1212): *"read the SPITBOL manual for expression syntax, and the Snocone parser for statement syntax"*, *"All of the parser_*.sno programs worked at 100% in the past."* parser_<lang>.sno is `scrip --transpile` of the 14-file bootstrap chain plus parser_<lang>.sc, run on `sbl -bf -s64m`. **(1) WHY EVERY .sno STOPPED:** `edc59ed26` (09-16) made POS..RTAB structural nodes and tree_to_sno.c never learned them — 120-160 `?TT_26?`..`?TT_35?` placeholders per parser (SCRIP `241563a2e` cures it, plus bare labels instead of `label OUTPUT =` (the D5 blank-line artifact) and real literals from dval (the D1 segfault)); global.sc's `&MAXLNGTH = 1024` killed every source over 1 KB (ERROR 205); tdump.sc called REAL(), absent from SPITBOL. **(2) GRAMMARS REWRITTEN AS PEG MIRRORS OF THE C FRONTENDS** (rebus.y + rebus.l's semicolon rule; snocone_parse.y + snocone_lex.c's whitespace-decided operators; expressions per the SPITBOL manual's Chapter 15 table), trees in ast.h's TT_* kinds (CEO-770): rebus 0→49/49 (`241563a2e`), snocone 141→458/458 and all 57 SCRIP/bootstrap/*.sc (`d1455f883`). The instrument is `scripts/test_bootstrap_parsers.py <lang> --arm sbl|m3|m4|sc3|sc4` (every corpus file of the language + every master entry, names every non-parse; `--vs-ast` diffs the snobol4 tree against `scrip --dump-ast` statement by statement). **(3) THE ONE DEFECT (section below) IS CURED, SCRIP `b17de897a` + `077dfc98a`:** the capture thunk EXPR$0 is entered with rt_g_want_name=1; a nested nreturning call's by-name consult read that LIVE flag and zeroed it, so OUTER's name was dereferenced. HQV-12's DEFINE-shim protocol now also runs in the CLASS-C chain frame a Snocone function reaches (park the request in [kt-16], zero it, restore at both exits; user procedures only, a '$' thunk passes it through). Witness MATCH both modes. Control arms: asm byte-identical on snobol4 1982 · rebus 43 · pascal 246 · prolog 563 · raku 929 (base `a24b30162` vs head); snocone 35 and icon 154 entries move and read SncM 337/337 · Icon 826/826 both modes. **SncM 337/337 both modes on `ff2300d0e`** — the 337th entry (rung24_output_associated_write) graded for the first time. **(4) ⛔ FINDING, NOT CURED — snocone_parse.y puts * and / on one level:** `OUTPUT = 8 / 2 * 2;` prints 8 on SCRIP (and on SPITBOL through --transpile, which inherits the C tree), 2 on SPITBOL and SCRIP for the SNOBOL4 twin; the manual gives * priority 9 over / at 8. Also `?` right-associative in both C frontends where the manual says left. A --transpile oracle cut cannot see either. **(5) THE .sc ARM (the goal) NOW REACHES, ON EVERY REBUS SOURCE:** `rt_dcap_pump: CORRUPT CAPTURE ENTRY refused ... Deferred re-entry invalidated the outer frame` (a pattern match inside a function run as a deferred capture target — Shift's `v ? (POS(0) whitespace) =` — invalidates the outer match's capture frame), then `[ZGC-STALE]` SIGSEGV even at SCRIP_HEAP_KB=4194304; the .sno on SCRIP (m3) prints the right first tree and SIGSEGVs in TDump. **NEXT:** parser_snobol4.sno (rewrite in progress: the old grammar printed WRONG trees — uncounted gotos displaced the statement, every string read empty, every unlabelled statement carried an empty label; the new one agrees with --dump-ast on 299/306 simple_output entries, 6 skipped for -INCLUDE); then pascal (101/1317 on the old grammar), icon, prolog (loops forever on one clause), raku (the mechanical translation of Rakudo's Grammar.nqp, CEO-1212); the SCRIP runtime pair in (5) for the .sc arm. ⛔ The section THE SEVEN SELF-HOSTED PARSERS below (0/7, 09-16) is superseded by this entry.
- **SC-RTCCB-STALE-MECHANISM-2026-09-22 (hq_snocone, SCRIP `d9c66f2c6` corpus `cb2e2d49e` .github `d793599a6`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ FOLLOW-UP ON THE ROW BELOW: THE "HOLDER HUNT" cto's OWN CTO-132 ENTRY LEFT OPEN ("one run from knowing which class they have") IS ANSWERED — GENUINE COLLECTOR EFFECT, MECHANISM NAMED, STILL NOT LANDED.** Same sitting, independent of the entry below: also re-ran this lane's own gate (`test_gate_snocone_gc_share_named_and_master_clean_under_forced_collection.sh`) and found ITS OWN "tiny" arm stale — it pinned `SCRIP_HEAP_MB=1` (1024 KB) believing it was the tiny arena, but CEO-1095 (2026-09-21) lowered the shipped compiled-in default to 128 KB, so that pin had been 8x LOOSER than shipped for a day, the same false-clean shape ARM 1 already guards against. Fixed IN THIS LANE'S OWN FILE (one-file, in-lane, rule 7): "tiny" now pins `SCRIP_HEAP_KB=64` (`GC_HEAP_KB_FLOOR`, the smallest legal value and the only one still below 128 KB shipped). Re-ran corrected: still RED, same witness (`arb_span_break_replace_1`) at stress 1/3/5/8/16, both modes — confirms the row below is not an artifact of the old miscalibrated arm. ⭐ **THEN RAN IT WITH `SCRIP_ZETA_TELEM=1 SCRIP_GC_MAPS=1` OVER A MINIMAL 4-STATEMENT EXTRACTION** (BREAK-4 then ANY-1/ANY-2/ANY-3, deterministic on repeat, fails identically m3 and m4). The LAST `[ZGC]` regeneration before the corrupted `OUTPUT` line prints `[GC-WALK-RTCCB] slot=7 ...` then `[GC-RTCCB-STALE] slot=7 word=0x... block=type2/32B forwarded=0 moved=0 unrepaired=0 swept=1` — `gc_heap.c:1446`'s own SUMMARY text names the mechanism: a value the emitter spilled into the shared caller-saved `rtccb` wire-park block (`x86_asm.h:12`, 376-390) before a safe-point poll, which the collector's range-table walk skips by shape, so nothing visits it; `swept=1` means the block was never forwarded and the arena reissued its bytes — exactly the `0xDB` poison the row below and the cfo already matched byte-for-byte. **This closes cto's open question at CTO-132/CTO-130 (the reusable instrument "run at an arena large enough that collections=0; if it still fails it is not a GC defect"): at confirmed `collections=0` this witness is byte-clean (SC-TENET-2026-09-20 below), and at every confirmed collection it corrupts with the RTCCB-STALE signature — so it IS a genuine collector effect, not the pure-emission class cto's own six `rt_scan_needle` sites turned out to be.** Also the SAME global architecture as the long-open, never-dispatched row `emit-rtccb-wire-spill-block-is-a-global-measure-re-entrancy-then-move-it-into-the-box-frame` (ceo CEO-313, 2026-09-06, still minted with no runnable DONE-WHEN) — but a DIFFERENT hazard of that global than the one that row's STEP 1 asks for: not re-entrancy clobbering the parked wires, but the collector never rooting them at all during a live window. **NOT LANDED — `x86_asm.h` and `gc_heap.c` are shared nodes, not this lane's files.** Reported directly to cto (who owns chunk A, safe-point polls, and opened this exact thread) with the full telemetry and the minimal repro, per CLAUDE.md's shared-node ASK rule; offered as a ready-made STEP-1 witness for the open CEO-313 row if cto wants it for chunk A. Carrying cto's own count forward since nothing newer is mine to report: **183 of 253 instrumented, 70 open, identity asserted.**
  ⛔⭐⭐ **CORRECTED WITHIN THE HOUR, BEFORE IT LANDED IN A RULING — MY OWN PROPOSED CURE DIRECTION WAS ALREADY FORBIDDEN.** Reading `scripts/test_gate_gc_the_caller_saved_spill_block_never_holds_a_heap_reference.sh` (cto 2026-09-20 CTO-153, ruled by the cfo) AFTER sending the message above: that gate already tracks this exact class in full — slot classification, UNREPAIRED vs SWEPT, a fail-once plant, the works — and its own text rules out "teach the collector's walk to visit rtccb" outright: 32 untagged words, visiting them is a conservative scan by definition, and CEO-812 forbids that with no exceptions. The gate's own cure is already written: move the live value OUT of rtccb (a frame slot under the frame's static map, or a tagged DESCR cell in the shield's `arr[]`, CEO-972) — never make the collector look INTO it. cto had proposed exactly the forbidden direction in reply and was about to ASK the ceo to split CEO-313 on it; corrected both cto and the ceo the same sitting (message `correction-do-not-ask-ceo-to-make-the-walk-visit-rtccb`) before CEO-1141 (which ratified cto taking the rtccb half with this lane's extraction as its STEP-1 witness) could bake in the wrong premise. **This lane's actual contribution, once corrected: a real, non-synthetic slot-7 `swept=1` witness for an already-built, already-REPORTED gate** — its own header says slot 7/8 had previously only been reproduced by a synthetic plant (`SCRIP_GC_PLANT_RTCCB=7`), never observed in the wild; `arb_span_break_replace_1` (at the now-reverted `bb_binop_concat_slot.cpp:69`) is that observation. Also ran the zero-collection arm the ceo (CEO-1137) and cto both separately asked for on this witness: `SCRIP_HEAP_MB=512`, stress unset — `collections=0 grew=0`, output byte-identical to the oracle ref, both before and after SCRIP `58820a280` reverted the one poll this witness's corruption traced to. ⭐ **THE LESSON, kept because it is reusable and not because the finding was wrong: naming a mechanism correctly (RTCCB-STALE, SWEPT) does not by itself license a cure direction — the architecture ruling on what CAN cure it (CEO-812) has to be read before proposing what should.**

- **SC-DECTET-GC-REGRESSION-2026-09-22 (hq_snocone, SCRIP `156ef964d` corpus `4a3bca9c9`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ COMPONENT 4 (WORKING) FOUND ITS FIRST REAL DEFECT: A SHARED-NODE REGRESSION, BISECTED TO ONE COMMIT, NOT LANDED BY THIS LANE.** ceo-1129/1130 DECTET ask (component 4, "not measurable because nobody was on it"). Re-ran the unforced master (clean: 336/336 both modes) and this lane's own forced-collection gate on the fresh HEAD, which had moved ~90 commits (heavy officer safe-point-poll landings) since the last clean reading at SCRIP `77922bcf8`. **The gate is RED now** — it was clean at `77922bcf8`. One entry, `arb_span_break_replace_1` (ARB/SPAN/BREAK/ANY/LEN captures), prints GARBLED BINARY BYTES (not a crash, not a clean wrong value) under forced stress at both the tiny and shipped arena, at every stress point 1/3/5/8/16 tested.
  ⛔⭐ **BISECTED (git bisect, 7 steps, rebuild+diff each step): first bad commit `c6009782862886bd35d89b71648b4460f96e03cb`** ("gc safe points: four DESCR-returning sites take a bare poll after their store"), which adds `x86_rt_gc_poll()` after the descriptor store in FOUR SHARED templates — `bb_var_ref.cpp`, `bb_deref.cpp`, `bb_unop.cpp`, `bb_binop_concat_slot.cpp`. **The commit's own message names three of the four as shared nodes and says the other frontends' arms were OWED since `e42cc0909` — that owed verification is what surfaced this, 90 commits and roughly 11 hours after landing.** Parent commit is clean on the witness. Full evidence, ablation status and the overlap note: `findings/FINDING-2026-09-22-hq_snocone-c6009782-four-shared-bb-templates-corrupt-output-under-forced-collection.md`.
  ⛔ **NOT LANDED, NOT WIDENED — reported up per lane law.** `hq_snobol4` independently claimed a row the same window (`snobol4-the-pattern-replacement-class-prints-a-wrong-answer-under-collection-and-changes-its-fingerprint-per-poll-set`) that reads like the same root cause from the SNOBOL4 side; told them directly with the bisect result. ASK sent to the cfo (reviewer map) with the full measurement. Which of the four sites is actually wrong (poll-placement-vs-`rtcc_rl` or a frame-map gap at that program point) is NOT established by this lane — named as unmeasured rather than guessed. ⛔⭐ **CORRECTED BY hq_snobol4, 2026-09-22 ~18:3x CDT — NOT THE SAME ROOT CAUSE, CHECKED BY MECHANISM AND BY ANCESTRY.** Their witness (`user_function_eval_arbno_replace_branch_2`) SIGSEGVs at `rt_defer_probe_run` on a stale varname — a missing root on the deferred-call descriptor road (`bb_match_defer.cpp`), not a bare-poll-after-store on a DESCR-returning site — and `c6009782862886bd35d89b71648b4460f96e03cb` is **not an ancestor** of the tree where they first found it (`f839e933b`, 2026-09-21): their crash predates this lane's commit entirely and is unchanged base-to-head across it. Two independent shared-node GC defects in the same window, not one — do not fold them together.
  ⭐ Also answered ceo-1130's peak-RSS ask from the same master run: Maximum resident set size 58928 kbytes, elapsed 0:49.67, tree `156ef964d`. Side note sent along with it: the harness preamble now reads `ARENA SCRIP_HEAP_KB=128` as the shipped default (CEO-1101's hard-cap ruling), not the "512 MB shipped" this cursor's own older entries still cite — not yet reconciled against every older LIVE CURSOR line below, flagged rather than silently carried forward.
  ⛔⭐ **FOLLOW-UP, SAME ROW, NOT A NEW ONE (hq_snocone, 2026-09-22 ~18:2x CDT, SCRIP `56504708a` corpus `cb2e2d49e` .github `6656ac725`, incremental `make`) — cfo INDEPENDENTLY NARROWED THE FOUR SITES TO ONE BY A ONE-BIT ABLATION, AND THIS LANE CLOSED cto's OPEN DISCRIMINATOR.** cfo reproduced `arb_span_break_replace_1` standalone and, four rebuild-and-diff arms, found `bb_binop_concat_slot.cpp:69` alone accounts for the DIFF — removing only that poll passes at stress 1/3/5, removing any of the other three still DIFFs. **All three replacement poll forms tried at that site still DIFF** (`x86_rt_gc_poll_res` · `poll_res` + re-store to `ZRES(0)`/`ZRES(8)` · the poll moved after `rtcc_rl`), so it is not a poll-form or placement defect — a live value at that point is unrooted and reclaimed, and the corrupted bytes are runs of `0xDB`, the poison fill `gc_heap.c:1454` memsets over vacated ground after a compaction (mechanism read off the source by cfo, not argued). Separately, cto asked this lane to run a zero-collection control before trusting a GC diagnosis, since their own six `rt_scan_needle` sites looked identical and turned out to be a pure emission defect present even with the collector switched off. **Ran it:** `SCRIP_GC_STRESS` forces a collection directly every Nth allocation (`gc_heap.c` ~line 282), independent of arena size, so arena size alone cannot silence a nonzero stress value — confirmed by measuring `SCRIP_GC_EXERCISE=1` at both `SCRIP_HEAP_MB=1` and `=512`: stress=1/3/5 read the identical collection counts (25/9/6) at both arena sizes. At a real, instrument-confirmed `collections=0` (stress=0, either arena), output byte-matches the oracle ref every time; at every point where a collection actually ran, output DIFFs every time with the same `0xDB` signature (checked independently with `cmp -l`). **Unlike cto's sites, this one is a genuine collection effect** — the defect never appears at confirmed zero collections and always appears once one runs — so cfo's unrooted-value reading stands and a pure-emission explanation is ruled out for this site. Reported to both officers; **still not landed** — `bb_binop_concat_slot.cpp` is a shared template, `c6009782` is the ceo's landing, and the holder hunt (which live value, why unrooted) remains open and is not this lane's to take.

- **SC-EXECUTIVE-STANDDOWN-2026-09-21 (hq_snocone, SCRIP `29764b9e4` corpus `b3dd2932b`, sweep measured on pre-commit tree `27630286a`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ ITEM 4 OF THE GC COMMITMENT (ZERO GRADINGS LOST TO THE COLLECTOR), ARENA AXIS ADDED AND MEASURED CLEAN; STOOD DOWN MID-ROW BY CEO-1051 BEFORE A HEAD RE-VERIFICATION.** Assigned by ceo-1040 ("item 4 is yours"); the queue's own row (`snocone-gc-zero-gradings-lost-to-the-collector-across-the-arena-and-stress-axes-named-not-counted`) REFUSED to dispatch on `next` because it was minted with a placeholder DONE-WHEN (`exit 1`, no acceptance criterion) — fixed in the task baton rather than routed around, since fixing a broken baton in this lane's own row is this lane's to do.
  ⛔⭐ **THE GAP WAS THE ARENA AXIS, NOT THE STRESS AXIS.** The prior row's gate (`test_gate_snocone_gc_share_named_and_master_clean_under_forced_collection.sh`) already isolated STRESS (tiny arena, band 1/16) but never once compared the tiny arena to the shipped one — the exact mirror of the hole hq_snobol4 found in hq_prolog's first cut of SCRIP `46f348488` (a band varying the arena alone reads green over a stress-sensitive class; here a band varying stress alone would read green over an arena-sensitive class, had one existed). Extended the gate with **ARM 0** (shipped arena, stress=0 baseline, re-measured fresh rather than cited from a past sitting) and **ARM 4** (the same stress band, run at the shipped arena): ARM4(N) vs ARM0 isolates STRESS ALONE, ARM3(N) vs ARM4(N) isolates ARENA ALONE, holding the other axis fixed at every point — landed at SCRIP `4cced14cd` (rebased `29764b9e4`). Arena-alone-AT-STRESS-0 is not a usable cell here: ARM 1 already proved 0/336 entries collect at the tiny arena with no forced stress (SncM's own 100% board is silent on the collector), so a comparison built on it would agree trivially while testing nothing — arena is tested composed with a forcing stress, never alone against an inert cell. Added a cheap SELF-TEST of the comparison/naming logic itself (a planted synthetic name-set difference) as the fail-once precondition on the instrument, ahead of spending a single `scrip` invocation.
  **MEASURED, pinned tree SCRIP `27630286a` corpus `b3dd2932b`:** ARM 0 baseline (shipped, stress=0) NAME-SET empty, total=336. Band **{1, 3, 5, 8, 16}** × arena **{1 MB, shipped}** × modes **{m3, m4}**: every point DECIDABLE (`collectors=336/336` at every one of the ten arm-points; regenerations 1269/1269 · 837/837 · 681/681 · 540/540 · 481/481, tiny-then-shipped per stress point — arena size does not change the collection count under forced stress, as the mechanism predicts), every point's NAME-SET empty in BOTH arenas: **zero STRESS-axis divergent pairs and zero ARENA-axis divergent pairs at every stress point tested.** The historical `eval_datatype_defer_1` regression (RED at stress=1 before the cure) does **not** reproduce — confirms hq_snobol4's DT_X landing (SCRIP `83b8bc9d2`) still holds on this tree.
  ⛔⭐ **NAMED AS UNMEASURED, NOT ROUNDED UP (CEO-1051's explicit order on stand-down):** (a) **(arena, stress=0) is untestable-by-construction with this corpus**, at either arena — not a clean reading, a non-reading; ceo-1040's declared axis point "stress=0" is answered by ARM 0's baseline (which is clean) but the CROSS comparison at stress=0 specifically cannot be formed. (b) **HEAD re-verification (ceo-1040 precondition 3) was NOT done** — stood down before a second pass; whoever holds this row next re-measures on HEAD before citing these numbers as current, since officers are landing collector changes while languages are stood down. (c) The ARENA-axis fail-once is a self-test of THIS SCRIPT's comparison logic, not a real-code planted divergence — this lane holds no cure lane and does not plant bugs in shared runtime to test its own gate; a stronger proof would come from the cfo/coo's differential oracle (`util_gc_differential.py`). (d) ARM 2's static census remains windowed at whatever fraction the cto's shielding-road arm currently measures (39% on 2026-09-20) — `NO-MAP=0` is a zero over that fraction, never the whole road. cto independently confirmed the same sitting that their `util_gc_unmapped_store_census.py` landing (SCRIP `3e1d66566`) leaves ARM 2 unaffected (NO-MAP=0, members/undecidable unchanged).
  ⭐ **SIDE FINDING, discovered while working this row, unrelated to its own population:** `SCORE.md`'s snocone "display board" cell was silently losing its own live `336/336` reading inside `util_score_row.py agree` — a false "superseded" match on the bare word "was" inside "...WHICH IT **WAS** NOT YESTERDAY", ~3000 characters downstream of the fraction with no clause delimiter (`·`/`—`/`;`) anywhere in between, so the clause window the parser builds ran the whole distance and swallowed it. Not a data error — the 336/336 figure itself was always correct — a prose-parsing false positive, matching exactly what hq_icon and the coo separately flagged the same day. Fixed with one inserted `·` immediately after the fraction (`.github`, this sitting); verified with `python3 scripts/util_score_row.py agree` before (reported STALE) and after (silent) the edit. **Lesson for this lane's own writing, not just a bug report: a long undelimited prose run is invisible risk to any downstream reader that scans for context around a number — bound a live figure in its own clause the moment it is written, not after a tool complains.**
  Handed to the **cfo** (CEO-802 reviewer map: icon/snobol4/snocone/pascal) per CEO-1051's stand-down; task baton at `snocone-gc-zero-gradings-lost-to-the-collector-across-the-arena-and-stress-axes-named-not-counted.task.md` carries the full receipt and a `## NEXT` naming the four items above for whoever picks it up.

- **SC-TENET-CLOSED-2026-09-20 (hq_snocone, SCRIP `81c00a5be` corpus `b3dd2932b` .github `d78ec404a`, RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ THE CONDITION-1 ROW IS CLOSED AND COMPLETENESS OPENS IN SNOCONE — AND THIS LANE DID NOT CURE IT.** The one red this row was blocked on, `eval_datatype_defer_1` (`d = *1;` then `OUTPUT = EVAL(d);`, the null string under collection where `sbl -bf` says `1`), was cured by **hq_snobol4's DT_X landing, SCRIP `83b8bc9d2`**: `gc_cell_visit` was missing `DT_X`, so a descriptor in an emitted spine slot fell through to `return 0`, the walk filed its payload as a raw heap word, nothing marked the block and the arena re-issued it in place. ⭐ **THE HANDOFF IS THE RESULT, NOT A DETOUR.** This lane measured the defect to a two-line witness, proved it reproduced from the SNOBOL4 frontend and at the shipped 512 MB arena, called it a SHARED node and REFUSED to land the cure — and it was cured, in the lane that owned the node, by a seat that reached it from its own witness. The cost of that refusal was one telegram; the cost of landing it would have been a collector change made by the lane least able to grade its blast radius.
  - **DONE-WHEN GREEN**, `test_gate_snocone_gc_share_named_and_master_clean_under_forced_collection.sh` rc=0: **ARM 2** `witnesses=336 shielded_stores=1480 members=1176 undecidable=304 BELOW-REGION=1176 RAW-SLOT=0 GAP=0 ABOVE-REGION=0 OUTSIDE-LAYOUT=0 NO-MAP=0` — class 2, this lane's own class, still EMPTY ⛔⭐⭐ **QUALIFIED THE SAME SITTING, ON THE cto's OWN ARM (CTO-157), AND IT WEAKENS THIS ZERO: `unread_static=2349` AGAINST `shielded=1480`.** The cto landed an arm (SCRIP `dc406e95b`) that counts the shielding road the census REFUSES to read — the emitter spills r8/r10/r11 across every poll into a rip-relative static block, and the store reader returns nothing for any operand carrying rip. Run over these same 336 entries it reads `CENSUS unmapped-store LANG sc witnesses=336 members=1176 undecidable=304 shielded=1480 unread_static=2349` — **1480 of 3829 sites, UNDER 39%.** By the cto's own stated criterion this is the raku case, not the clean case: **`NO-MAP=0` is a zero over a fraction of the road and does NOT stand as it reads.** ⛔ **WHAT SURVIVES, AND THE SPLIT IS THE POINT: this row had TWO INDEPENDENT ARMS AND ONLY ONE IS WINDOWED.** ARM 2 is that static census — windowed at 39%. **ARM 3 is BEHAVIOURAL** and is not: the whole 336-entry master graded against the SPITBOL oracle under forced collection at `arena_mb=1` across six band points, every point proven to actually collect, NAME SET empty at all six. It does not care which stores a reader can parse. So TENET condition 1 keeps real evidence under it; what it loses is a static census that covers the road. ⭐ **A STATIC ZERO AND A BEHAVIOURAL GREEN FAIL DIFFERENTLY AND MUST NEVER BE FOLDED INTO ONE CELL** — the cto's column certified raku *decided completely* while raku's master was losing 65 gradings at stress 16, which is this exact error one lane over. This gate keeps the two arms apart by accident of design; making that explicit is owed. · **ARM 1** decidable at BOTH band points, `collectors=336/336` at stress 1 and 16 (1269 · 481 regenerations), so neither point is inert · **ARM 3** `total=336 m3_pass=336 m4_pass=336` at `arena_mb=1` across band `"1 16"`, **NAME-SET empty at both points**. The witness gate `test_gate_gc_snocone_deferred_eval_survives_a_collection.sh` — RED on origin yesterday **by design** — is GREEN in BOTH frontends, ref cut from `sbl -bf` at gate time and never pinned.
  - ⛔⭐ **CEO-1025 ANSWERED, AND SNOCONE'S ANSWER IS A DOUBLE ZERO THAT CANNOT VALIDATE THE INSTRUMENT.** The ceo's ruling splits `no_layout` into (a) NEVER-EMITTED (a compiler refusal — a completeness debt wearing a GC census's clothes, which does NOT gate completeness) and (b) EMITTED-WITHOUT-A-LAYOUT (the correctness population, which does). Measured rather than quoted on today's tree: `ZLS-MAP lang=snocone graphs=400 words=15782 unkinded=0 holes=0 graded=336 no_layout=0 no_layout_declared=0` — **both buckets are EMPTY**, so this lane has no member to sort. ⭐ **Reported to the ceo WITH ITS OWN LIMIT NAMED:** a lane with zero members cannot exercise the new third field, so a green from snocone on that discriminator would be vacuous and was explicitly not offered as evidence. The field has to be proven fail-once where members exist (raku's family reads rc=1 RED, prolog's ten read rc=2).
  - ⛔⭐ **THREE CORRECTIONS TAKEN FROM OTHER LANES THE SAME SITTING, EACH WEAKENING A CLAIM OF THIS LANE'S — RECORDED BECAUSE THE WEAKER SENTENCE IS THE TRUE ONE.** (1) **hq_raku on the pathname null:** this lane tested their path-length class at seven name lengths and over the whole master with a 60-character-longer `TMPDIR` and got a clean null — but after they CURED their defect the path sensitivity vanished with it, which means a null is *evidence that this witness has no window reachable at one-character granularity*, **NOT** evidence that this lane is exempt from the class. Their wording, adopted. By the same mechanism the six-point post-cure re-sweep says the ONE window this lane knew about is closed, never that no other window exists in the population — **lower bound both times**. (2) **hq_raku on `od`:** they found 29 programs recorded as *prints empty* that print **zero bytes** — an absent STATEMENT, not a null STRING, which is a lost control path rather than a lost value, and no comparator distinguishes them. This lane's witness is characterised as the null string on three ablations (the statement SUCCEEDED with its failure branch not taken · a second `EVAL` read the procedure name as the empty string · `DATATYPE` still answered `EXPRESSION`) — statement ran, value present, payload empty. ⛔ **But bytes were never compared, and cannot be now**: re-opening the defect means deleting a line from `gc_cell_visit` in a SHARED runtime file, which this lane does not edit even for a probe. Recorded as resting on inference, not on `od`. (3) **hq_prolog on CEO-1025:** this lane told the ceo *yours are the ten that can* exercise the new discriminator; their ten are all cause (a), so they can prove the NEVER-EMITTED arm and **not** the other, and no single lane validates the field. Corrected to the ceo by this lane rather than left in their outbox.
  - ⭐ **AND A THIRD CLASS THIS LANE'S INSTRUMENT CANNOT NAME, FROM hq_prolog VIA hq_raku — VANISHED.** hq_prolog lost **68 pairs of 1043** out of their stress arm's population: entries that simply stop being graded, so a point can read green for having stopped measuring. This lane grades **336 at every band point, control and stress alike**, so nothing vanished here — ⛔ **but the gate does not ASSERT that, it merely happens to be true.** Making the equal-denominator check an explicit arm rather than a coincidence is owed before this lane grades another band. That is the SECOND time in one sitting a number of this lane's was right for a reason nothing was checking (the first being the inert tiny arena).
  - ⭐⭐ **THE REUSABLE HALF, AND IT IS THE OPPOSITE LESSON FROM YESTERDAY'S.** Yesterday this cursor recorded that *a board carrying the knob in its own verdict line is not thereby exercising the knob* — the population was narrowed and no instrument could say so. Today's is the mirror: **a red you correctly refuse to cure is still your row, and it can close without you touching it.** The lane's job on a shared-node defect is to hand over a witness sharp enough that the owning lane recognises it from their own — which is what happened here, in the opposite direction, before this lane even asked again.

- **SC-TENET-2026-09-20 (hq_snocone, SCRIP `1348c995c` corpus `8486bb1e2` RT_OPT=-O0, incremental `make`) — ⛔⭐⭐⭐ THIS LANE'S FIRST ROW UNDER MODE TENET IS THE GC SHARE, AND THE TINY-ARENA BOARD THAT LOOKED LIKE THE ANSWER WAS INERT.** MODE line 2 / CEO-1011: *completeness does not open in a language until that language's GC share is measured clean by oracle diff at the tiny arena.* Row `snocone-gc-the-snocone-share-of-the-unmapped-slot-population-censused-by-name-and-the-master-clean-under-forced-collection` (rank 0, hq_snocone, officer cfo); `snocone-ladder-top-rung-census-from-the-snocone-manual-is-the-score` is PARKED-AWAITING it, self-clearing.
  - ⛔⭐⭐ **SncM READS 336/336 BOTH MODES AT `SCRIP_HEAP_MB=1` — AND NOT ONE OF THOSE 336 ENTRIES COLLECTS EVEN ONCE.** A census of `[ZGC] regeneration` lines over the same population at the same arena reads **`collectors=0 non_collectors=336 regenerations=0`**: no entry allocates a megabyte, so the tiny arena never collects and that green board is a statement about this corpus, not about the collector. The board prints `arena_mb=1` in its own verdict line while meaning nothing by it. **No rc, no denominator and no FAIL=0 anywhere in a pass/fail instrument can say this** — the question *did the population do the thing I am grading it for* has to be asked separately and first, and it is ARM 1 of the row gate, which REFUSES rc=2 rather than ever reporting green over a population that did not collect.
  - **THE CENSUS THAT NAMES (`util_gc_unmapped_store_census.py`, all 336 entries materialized by ORIGIN):** `witnesses=336 shielded_stores=1480 members=1176 undecidable=304 BELOW-REGION=1176 RAW-SLOT=0 GAP=0 ABOVE-REGION=0 NO-MAP=0`. **`NO-MAP=0` is this lane's own number — class 2, the per-language class the cto assigned to the HQs — and it is EMPTY.** The 1176 are all BELOW-REGION (the cto's classes 1 and 4), named in full and claimed as a residual, never as a defect list: that census's own header says a negative offset is where spine words live BY CONSTRUCTION and was never the discriminator (CFO-114, CFO-136). By site kind: `call` 954 · `assign_var` 130 · `subscript` 44 · `kw_assign` 24 · `field_var` 8 · `lit_integer` 6.
  - ⛔⭐⭐ **THE DEFECT, MINIMAL AT TWO LINES AND NOT SNOCONE-SPECIFIC:** `d = *1;` then `OUTPUT = EVAL(d);`. `sbl -bf` says `1`. SCRIP says `1` with no collection and **the null string** with one — `rc=0` both times, and the statement **SUCCEEDS** (a `:F()` branch is not taken), so no conditional in any program can see it. Made able to fail (`SCRIP_GC_STRESS=1` → `collectors=336/336`, 1268 regenerations) the master reads `m3_fail=1 m4_fail=1` over `total=336`: one entry, **`eval_datatype_defer_1`** (origin `ladder__rung11_unary_operators_deferred_asterisk`), `fp=5e2133a7` in both modes. **The SNOBOL4 twin reproduces it identically**, so the cure is a SHARED node and is not this lane's to land — handed to the **cfo** under Lon's escalation order. ⛔ **It reproduces at the SHIPPED 512 MB arena too: the arena is not the discriminator**, a collection landing on one particular allocation is — stress 2, 3, 4, 5 and 8 are all CORRECT, and stress=2 does the SAME number of collections as stress=1. Also measured: `SCRIP_GC_BUDGET_MB` with no collection is correct (not the slow allocation path) · `EVAL(*1)` with no variable fails too (the loss is inside the EVAL road, not in a stored value) · `DATATYPE(d)` still says `EXPRESSION` (the tag survives, the payload does not) · a SECOND `EVAL(d)` reads the procedure name as **the empty string** (`[GZ-10] procedure '' has no stackless slab`) · `EVAL("1 + 1")` on a STRING is clean, so it is the `*expr` `DT_E` road. Full evidence and the four CEO-1019 candidate answers: `findings/FINDING-2026-09-20-hq_snocone-the-tiny-arena-never-collects-over-sncm-and-forcing-it-finds-a-silent-wrong-answer-in-deferred-eval.md`.
  - **THE TWO GATES** (both `RULING` in `gate_wiring.tsv` with their reasons): `test_gate_gc_snocone_deferred_eval_survives_a_collection.sh` — the witness, both frontends, **ref cut from `sbl -bf` at gate time and never pinned**, swept by `make test-arena` and RED on origin today, which is where that recipe says a collector finding belongs · `test_gate_snocone_gc_share_named_and_master_clean_under_forced_collection.sh` — the row's DONE-WHEN, three arms, ~5 min, deliberately NOT in `make test` (CONDITION 2: the blocking set does not scale with seats, so that wiring is the coo's call).
  - ⛔⭐ **CEO-1024 ANSWERED, NOT ASSUMED — AND THE BAND IS NOT INERT.** The ceo's challenge (the fleet graded at stress 1, 3 and 5; hq_raku found 36 programs wrong at 16; hq_snobol4 has a witness red at 25 and green at 10/12/16/20/35/50, so the points are **not monotone in the plant**) landed after the first sweep, which was 1–8 and therefore inside the weak band. Re-run across **1 · 2 · 3 · 4 · 5 · 8 · 10 · 12 · 16 · 20 · 25 · 32 · 35 · 50 · 64 · 100**: **RED at `stress=1` ALONE**, same one entry, empty NAME SET everywhere else. **And the decidability question was asked at every point**, because a higher plant collects LESS often and a high band point can be inert for exactly the reason the tiny arena was — `collectors=336/336` at 1, 5, 16, 25, 64 and 100 (1268 · 681 · 481 · 357 · 343 · 338 regenerations). *Clean above 5* here is a reading, not a band artifact. The gate grades a BAND (`SNC_GC_BAND`, default `1 16`), refuses rc=2 on an inert point, prints a **per-point NAME SET** and names the plant **as a value** in every line (unset IS stress 0).
  - **THE SPINE-CELL GRID, on the cto's pointer:** `GRID_POP` over all 336 entries reads **`calls=5518 on_grid=4364 off_grid=0 undecidable=1154 unreached=4163 cell=16`**, all six arms green including the PLANT arm — so snocone answers that question fully and off-grid is zero.
  - **TWO LATE CHALLENGES, TESTED NOT ASSUMED AWAY.** CEO-1021's discriminator (same wrong answer at stress 0 means it is a representation disagreement and not the collector): **NO** — this witness is byte-correct at stress 0 at both arenas and wrong from stress 1, so the collector attribution is checked. hq_raku's pathname class (the plant counts ALLOCATIONS, argv is allocated, so path length decides which allocation a collection lands on, and they flip a program by renaming its file): tested at **seven name lengths from 1 to 40 characters** — identical at every one — and the whole master re-run with `TMPDIR` 60 characters longer at stress 1, 5, 8 and 16 — **identical board and identical NAME SET**. ⛔ Still published as a **LOWER BOUND** rather than a population, as hq_raku is relabelling their own 36, and the gate's band spans both ends (`1 16`) because *above 5 is not safer than 1-3-5, it is different*.
  - ⭐ **THE REUSABLE HALF: a board that carries the knob in its own verdict line is not thereby exercising the knob.** The instrument was honest, the denominator real, the FAIL=0 true — and the conclusion would have been false. Same family as `command -v` answering *is it on PATH* and `$?` answering about the pager, except the narrowing here was in the POPULATION, where no refusal, default or gate can catch it.

- **SC-LON-2026-09-16 (10:2x CDT, `date`-read, routed by the ceo, CEO-770) — ⛔⭐⭐⭐ LON: THE SEVEN SELF-HOSTED PARSERS PARSE EVERY CORPUS PROGRAM INTO A PROPER `tree_t` WITH PROPER `TT_*` TYPES.** In-chat to ceo, verbatim: *"Ensure for Snocone that all seven parser-*.sc programs should work by parsing all the available corpus code into proper tree_t with proper TT_* types."* Measured at SCRIP `dc06e3a51`: `SCRIP/bootstrap/` carries SIX parsers — `parser_{icon,prolog,raku,rebus,snobol4,snocone}.sc` (374/337/426/267/253/276 lines; `TT_` spellings 117/80/197/39/73/87) — and NO `parser_pascal.sc`; the only scripts naming them are `test_parser_snocone.sh` and `run_parser_sync_monitor.sh`. The row is `snocone-the-seven-self-hosted-parsers-parse-every-corpus-program-of-their-language-into-tree-t-with-tt-types` (rank 0, hq_snocone, officer cfo): the seventh parser written in Snocone on the shared bootstrap library; one runner per language that prints programs shipped / parsed / refused with names over the whole corpus of that language (masters, packages, demos, benchmarks, library); a gate proven fail-once on a corrupted tree; a language-specific acceptance question is an ASK to that language's HQ with the witness, never a silent skip.

## ⛔⭐⭐⭐ SncM IS AT 100% — 336/336 BOTH MODES, ZERO XFAIL (hq_snocone 2026-09-16, SCRIP `5cc7ba393` corpus `e10826fe5`)

Board: `total=336 shipped=336 outside=0 · m3_pass=336 m3_fail=0 m3_crash=0 · m4_pass=336 m4_fail=0 m4_crash=0 SKIP=0 · xfail=0 xpass=0`. Gates on the same tree: ladder 264/264 rungs 0..23 · smoke PASS=5 FAIL=0 · preflight 50 arms 0 red · `test_gate_no_xfail_survives.sh` census reads snocone `csv=0 allxfail=0 files=0 UNION=0`.

⛔ **THE HEADLINE IS NOT "TWELVE BUGS FIXED" — ELEVEN OF THE TWELVE WERE FAULTS IN THE TEST, NOT IN THE COMPILER.** Every one was settled by running the construct's faithful twin under `sbl -bf` rather than by reading our own output or the entry's own note. Four refs were re-cut from the oracle, seven sources were repaired while their refs were left **untouched** (the ref agreeing with the oracle while the source disagreed is precisely the evidence that the source was the broken half), and one test was deleted with its reason. **A standing xfail is not evidence that the compiler is wrong.** This lane's twelve said so for three days.

## ⛔⭐⭐ STANDING LAW OF THIS LANE — THE DEFINING DOCUMENT IS NOT AUTOMATICALLY THE SPEC (hq_snocone 2026-09-16, minted after it nearly cost a lexer change)

`corpus/tests/snocone/report.md` is Andrew Koenig's original Snocone paper, and `SCORE.md` makes it this language's standard *for the score* — where no public conformance suite exists, the ladder to the top of the defining document's census IS the score. ⛔ **That does NOT make it the syntax spec.** SCRIP's Snocone emulates **C surface syntax over SPITBOL semantics**, and where the paper and that design disagree on SYNTAX, the C reading plus the SNOBOL4 operator set wins.

**The ruling that settled it** (Lon 2026-09-16, in-chat, verbatim): *"The '#' is an operator, if it is an operator in SNOBOL4. Snocone emulates C, so /* ... */ and // ... are the comments."* The condition was verified rather than assumed: `ARCH-LANGUAGES.md` lists `#` as an undefined **binary** operator available for `OPSYN` (left, pri 7), and the SPITBOL manual (lines 5204–5206) lists it among the unused unary *and* binary operators, with the worked example `OPSYN('#','DIFFER',2)`.

**How close this came to landing backwards.** The paper says, flatly, *"Comments begin with a # character and end at the end of the line."* A census had already shown that across 168 `.sc` files the only bare `#` outside a string or comment was the failing test itself — so curing the lexer would have broken nothing measurable, passed every gate, and been wrong. **A change that breaks no test is not thereby correct.**

| the paper says | SCRIP's Snocone is | disposition |
|---|---|---|
| `#` begins a comment | `#` is an OPSYN operator; comments are `//` and `/* */` | test deleted, recorded in `ALL.excluded.txt` with a do-not-re-mint note |
| `a[i,j]` — Algol-like subscripts | same — brackets, **not** SNOBOL4's `a<i>` | two tests repaired from `arr<i>` to `arr[i]` |
| alternation is `\|`, concatenation is `&&` | same | five tests repaired from `(a, b)` to `(a \| b)` |

⭐ Note the table cuts **both ways** — the paper was RIGHT about subscripts and alternation and wrong only where SCRIP's C emulation overrides it. So the rule is not "distrust the paper"; it is **name which authority governs the question you are asking** — the paper for what the language *is* and for the score's census, `ARCH-LANGUAGES.md` plus the SPITBOL operator set for how SCRIP *spells* it.

## ⛔⭐⭐ STANDING LAW OF THIS LANE — SNOCONE SPELLS THINGS TWICE, SO A CENSUS OF ONE SPELLING REPORTS THE OTHER AS ABSENT (hq_snocone 2026-09-16, written at hq_snobol4's request after it caught us BOTH in one day)

Snocone is C-syntax SNOBOL4, and the C syntax did not replace the SNOBOL4 spelling — **it sits beside it**. So this language has construct pairs where two different surface forms produce the same runtime object, and **a grep for one of them answers, truthfully, about half the population.** Measured instances, both inside twelve hours:

| the question asked | the spelling grepped | the spelling MISSED | what the miss cost |
|---|---|---|---|
| how many functions does the corpus declare? | `function` | **`procedure`** | 8 declarations, including `Fib`, `QSort`, `Reverse`, `IsPalindrome`, `SplitWords`, `Double`, `MayFail`, `NullFn` |
| how many DATA registrations are in the master? | `DATA(` | **`struct`** | 10 of 13 — I reported "three" to another HQ and was wrong by a factor of four |

⛔ **`struct point { x, y }` IS A RUNTIME RECORD REGISTRATION.** It is the Snocone spelling of `DATA('point(x,y)')` and creates a constructor plus one accessor per field at runtime, declared statically nowhere — identical to the `DATA` form in everything except how it is typed. A resolver, a census or a blast-radius read that keys on `DATA` sees three entries where twelve exist.

**THE RULE:** before quoting any population count for this lane, name BOTH spellings of the construct or state that you checked there is only one. ⭐ And the general form, which is why this is law and not a note: **a narrow true answer is indistinguishable from a broad one at the point of reading.** The grep did not fail, the shell did not warn, and nothing was wrong except the question. This is the same class as the instrument traps in root `CLAUDE.md` (`command -v` answering *is it on PATH*, `$?` after a pipeline answering about the pager) — except that here there is no instrument to blame: the narrowing was in the fingers, so no refusal, default or gate can catch it.

## ⛔⭐⭐⭐ THE ONE DEFECT UNDER ALL SEVEN PARSERS — A USER FUNCTION CALLED INSIDE A DEFERRED CAPTURE TARGET FAILS THE MATCH, SNOCONE ONLY (hq_snocone 2026-09-16, three-line witness)

**THE WITNESS, and it contains no parser:**

```snocone
function INNER(x) { INNER = .dummy; nreturn; }
function OUTER(t) { INNER('v'); OUTER = .dummy; nreturn; }
if ('' ? epsilon . *OUTER('P')) OUTPUT = 'MATCH'; else OUTPUT = 'FAIL';
```

| arm | answer |
|---|---|
| `sbl -bf` on the SNOBOL4 twin | **MATCH** |
| `scrip` on the SNOBOL4 twin | **MATCH** |
| `scrip` on the Snocone source above | **FAIL** |

**The class:** a **user-defined** function call nested *inside* a deferred conditional-assignment target (`… . *F()`) makes the match fail. Remove the nested call and it matches. Call a **builtin** instead (`SIZE`) and it matches. It is the nesting of a *user* call that breaks it.

⛔ **THIS IS WHY ALL SEVEN PARSERS SCORE ZERO, AND IT IS ONE BUG, NOT SEVEN.** Every `parser_*.sc` is built on `Shift` and `Reduce` from `bootstrap/ShiftReduce.sc`, and both are invoked exactly this way — `p . thx . *Shift(t, thx)` and `epsilon . *Reduce(tag, n)` — while their bodies call `Push`, `Pop` and `tree`, all user functions. So every reduction in every grammar silently fails the match, and the failure surfaces at the top as a bare `Parse Error`. The parsers are not (only) stale: **the language they are written in cannot run them today.**

**The ablation, in the order it actually went, because the wrong turns are the useful part:**

| probe | result | what it killed |
|---|---|---|
| `epsilon . dummy` (plain variable target) | MATCH | not conditional assignment itself |
| `LEN(1) . *F()` standalone | MATCH | not deferred targets as such |
| same, with the 14-file library chain prepended | MATCH | not the library, not the `OPSYN` of `~`/`&` |
| 6-parameter function called with 2 args | MATCH | not arity |
| `nreturn` from inside an `if` block | MATCH | not nreturn placement |
| a statement that FAILS inside the body (`GT(xTrace,4)…`) | MATCH | not statement failure |
| full hand-clone of `Push` as the direct target | MATCH | not `Push`'s body |
| a wrapper calling that same clone **nested** | **FAIL** | ⭐ the nesting |
| a wrapper calling the builtin `SIZE` nested | MATCH | user functions only |

⭐ **TWO WRONG ROOT CAUSES I PUBLISHED TO MYSELF AND KILLED BY CHECKING.** (1) `ShiftReduce.sc` calls `tree(` lowercase while `tree.sc` defines `Tree` — a real-looking case mismatch in a case-sensitive language. It is NOT a bug: `tree.sc:1` declares `struct tree { t, v, n, c }`, so `tree(…)` is the **runtime record constructor**, invisible to any static census. Exactly the DATA-registration trap this lane already has a law about. (2) I read `DIFFER(p)` returning the null string as "p is null" and concluded `reduce()` returned nothing; DIFFER returns null **on success**, so it had been returning a valid pattern all along. Both were caught by running one more probe instead of writing the fix.

⛔ **DO NOT "FIX" `ShiftReduce.sc`, `semantic.sc` OR `counter.sc` FOR THIS.** They are correct SPITBOL and they run correctly under `sbl -bf` and under our own SNOBOL4 frontend. The defect is in the Snocone frontend's lowering of a deferred capture target, and that is where the cure belongs. `FoldOp` (referenced at `semantic.sc:66`, defined nowhere) is a separate real gap and does not cause this.

## ⛔⭐⭐⭐ THE SEVEN SELF-HOSTED PARSERS — MEASURED 2026-09-16 BY hq_snocone: SEVEN EXIST, **ZERO RUN**, AND THE SCORE IS 0/7

`parser_pascal.sc` is written and landed (SCRIP `49b1baeb7`), so the directory now holds **seven** — icon, pascal, prolog, raku, rebus, snobol4, snocone. ⛔ **That is a count of files, not a score.** The measured score on CEO-770's actual question — do they parse corpus programs into a proper `tree_t` with proper `TT_*` types — is **ZERO**, and every claim below is a command's answer, not a reading of a doc.

**ALL THREE DRIVERS ARE BROKEN, AND THE WORST OF THEM IS SILENT.**

| driver | what it does today | why |
|---|---|---|
| `test_parser_snocone.sh` | REFUSE rc=2 | graded the ast `parser` family Lon retired 2026-09-13; extraction yields an empty directory |
| `run_parser_sync_monitor.sh` | REFUSE rc=2 | still resolves its runtime chain under `corpus/SCRIP/`, deleted months ago |
| `run_scrip_parser.sh` | **rc=0, NO OUTPUT** | passes the 14-file chain as bare positionals; `scrip` takes the FIRST as the source and the rest as program args, so it runs `global.sc` alone and exits clean |

⭐ The third is the one to fear: a green exit code over a run that did nothing. Proven by a three-way probe — `--run global.sc` alone, `--run global.sc parser.sc`, and `--run parser.sc global.sc` — where the first two agree (rc=0, silent) and only the third executes a parser.

⛔ **THE ROOT BLOCKER: SNOCONE HAS NO WORKING INCLUDE MECHANISM.** `-L` is the library flag (`src/driver/scrip.c:994`), and it is collected for **every** language — but it is honoured only on the SNOBOL4 path, which expands it into `-INCLUDE` lines and re-parses through `sno_parse_ast` (`:1218`). The Snocone branch calls `snocone_compile()` and **never reads `n_preload`** (`:1153`+). So `scrip -Lglobal.sc … parser_snocone.sc` silently drops every library and dies at `error 22: Undefined function called` on the parser's first library call. A `.sc` program cannot load a chain at all.

**AND THE PARSERS THEMSELVES DO NOT PARSE.** Working around the include hole by concatenating the chain by hand — which demonstrably works, since `digits`, `nl`, `nPush`, `nTop` and `SPAN` all resolve and behave under it — not one parser parses a trivial program of its own language:

| parser | on a trivial program of its own language |
|---|---|
| snocone, snobol4, raku, rebus | `Parse Error` |
| icon | `ERROR 246 — stack overflow` |
| prolog | `fc_save_register: TABLE FULL at cap 256` |
| all six, with the `corpus/library/` chain instead of `bootstrap/` | **core dump** |

⭐ **AND IT IS NOT SCRIP'S FAULT.** The Snocone chain transpiled to `.sno` and run under **SPITBOL** answers `Parse Error` too. So the defect is in the parsers or their chain, not in the engine running them — which is exactly what `run_parser_sync_monitor.sh` existed to discriminate, and it is one of the three that no longer runs.

⛔ **TWO LIBRARY DIRECTORIES EXIST AND THEY ARE NOT THE SAME.** `SCRIP/bootstrap/` (lowercase `gen.sc`, `qize.sc`, `tdump.sc`) and `corpus/library/` (capitalised `Gen.sc`, `Qize.sc`, `TDump.sc`, plus `XDump.sc`, `ReadWrite.sc`, `FENCE.sc` which bootstrap lacks). `test_snocone_beauty_self_host.sh` uses the corpus one; the parser drivers use bootstrap. Any runner built for this row must **declare which chain it loads**, because they are different populations and one of them core-dumps.

⭐ **A FINDING ABOUT THE ORACLE, from writing the Pascal parser:** for Pascal, `scrip --dump-ast` is **not a pure parse tree** — the C frontend lowers as it parses (`and`→`TT_MUL`, `or`→`TT_ADD`, `not a`→`TT_EQ(a,0)`, `true`→`TT_EQ(1,1)`, real `/` forced by folding a `1.0` into the left operand, and `a mod b`→`TT_MOD(TT_ADD(TT_MOD(a,b),b),b)`). So "the .sc parser must match the C parser's dump" is, for Pascal alone, parsing **plus** replicating those rewrites. The `mod` one duplicates its right operand three times and there is no subtree-copy primitive in the tree library, so `parser_pascal.sc` emits the faithful `TT_MOD(a,b)` and names the divergence in its own header. The other six languages' dumps are genuine parse trees.

**WHAT THIS ROW ACTUALLY NEEDS, in order:** (1) an include mechanism for `.sc`, or a declared concatenating loader; (2) one runner per language printing shipped / parsed / refused **with names**, over that language's whole corpus; (3) the parsers made to parse at all — starting from a one-line program, not from the corpus; (4) only then a tree-diff against `--dump-ast`. ⛔ Nothing in this row can be scored until (1) and (3) land, and a runner built before them will print a denominator over a population that cannot run.

## ⭐⭐⭐ FRONT STATUS — READ THIS FIRST: RESTORATION IS THE ACTIVE FRONT, NOT ANYTHING BELOW (as of 2026-08-27)

⭐ **Lon extended scope 2026-08-24 (s272, in-chat to ceo), verbatim in substance:** *"We are doing well enough to extend scope to Snocone, Raku, and Pascal — at least get them to where they were before the huge month-long ZETA development phase."* Same sitting: *"Throw Rebus in there too — should just fall out, we do not have many tests for it."* Routed via GOAL-CEO.md CEO-20/CEO-21, minted by hq_C. **Reading:** the month of SNOBOL4/Icon-focused THREE-ZETAS conversion work (the bulk of what `GOAL-SNOBOL4-100.md`/`GOAL-ICON-100.md` document) landed on the SAME shared emitter/lowering pipeline Snocone rides, and regressed working Snocone behavior along the way. None of the eleven absorbed files know this happened — their newest entry (IR-BB, 2026-07-21) predates it.

**Five rows are live in the postoffice task queue right now** (`/home/resources/postoffice/tasks/*.task.md`, `QUEUE.tsv`) — **these, not the ladders below, are Snocone's actual current front. Check `s4e_msg.sh next` / `QUEUE.tsv` for live state before acting on anything in this file's RUNGS section:**
- **`snocone-returns-codegen`** ⭐ top of the restoration tier, gates `beauty.sc` — Snocone function returns broken in codegen three ways; the self-host ladder is green through `global.sc` and aborts the moment `trace.sc` (which contains `nreturn`s) is added. Snapshot at consolidation time: picked up 2026-08-27 by seat02, DONE-WHEN reported met, closing — **verify current state, do not trust this line after today.**
- **`snocone-restore-prezeta`** — the umbrella row: get Snocone (and Rebus, riding along) back to where it stood before the zeta month. DONE-WHEN wires `test_crosscheck_snocone.sh` + `test_smoke_rebus.sh` + `test_corpus_snobol4.sh` all green. **⛔ UPDATE 2026-08-28 (seat02): DONE-WHEN MET, task CLOSED via `s4e_msg.sh done`. Both blocking child defects (`snocone-nested-while-in-function-segv` SCRIP `748f7698`, `snocone-nreturn-after-indirect-assign-wrong-value` SCRIP `c5510d23`) were already landed by prior sessions but never re-verified together on one fresh pristine build — this session did that: crosscheck 8/8, Rebus 4/4, SNOBOL4 893/893 both modes exit 0, and (closing a gap flagged twice before but never checked) Icon rung suite re-verified fresh at 248-250/297, comfortably above the 232 baseline, no regression from either shared-node fix. No code changed this session. Full receipts in the task file's own LEDGER — verify current state, do not trust this line after today.**
- **`snocone-relop-parse-regression`** — 18 `crosscheck/snocone/**` programs that previously compiled to real (24–46KB) `.s` now fail to parse outright. Sample: `:==:` (string-equality relop) inside an `if (...)` condition no longer parses (`rungB09_str_eq.sc:4`). rungB09 (6, string relops) + rungB10 (6, numeric relops) are almost certainly ONE shared root cause in the condition/relop parse path; rungB12 (5) + `coverage_sno_nodes.sc` unconfirmed same-or-different cause.
- **`snocone-while-for-loop-second-iteration-error5`** ⭐⭐ — `while`/`for` loops run exactly ONE iteration then crash `** Error 5 ... Undefined function or operation` on the second condition check. Confirmed NOT a parser bug (`--dump-ast` shows a correct `TT_WHILE` node) — downstream in lowering/codegen, shared between `while` and `for`. **⛔ UPDATE 2026-08-27 (seat07), re-verified same day after a further rebase: this framing was a MISDIAGNOSIS, not a bug — the loop machinery was always correct. Root cause was `ADD()`, a non-existent SNOBOL4 function, called as an increment idiom by 10 corpus fixtures; every one crashed on its first call to it, not on a second condition-recheck. Fixed: `corpus@6c8822b9` (fixtures) + `SCRIP@3dc642fd` (unrelated `-no-pie` link gap found en route). One distinct, NOT-yet-fixed bug remains in this bucket — `B02_nested_break.sc`, an m4-only SIGSEGV, cross-referenced but not confirmed same-cause against `snocone-nested-while-in-function-segv`. Full writeup: `FINDING-2026-08-27-seat07-snocone-while-for-error5-was-add-not-a-function-not-a-loop-bug.md`. Task CLOSED via `s4e_msg.sh done`.**
- **`snocone-crosscheck-remaining-parse-and-wrongoutput-gaps`** — 19 more `crosscheck/snocone` files failing (7 parse errors + 12 wrong-output), explicitly heterogeneous — not the while/for bug, not the SN4-PAT subset gap, and NOT assumed to share one cause the way the relop row's 17 did.

⭐ **SUITE-HARNESS-LANG-CONFIGS-RULED-LOOSE (seat12, 2026-08-27, re-verified not re-derived).** `corpus_suite_harness.py`'s `LANG_CONFIGS` table deliberately carries no Snocone entry, unlike prolog/pascal/rebus (all three landed the same day, from the sibling row `suite-harness-lang-configs`). Ruled by three independent seats (seat05, seat06, seat08) and re-confirmed here against the live queue, not trusted from citation alone: `corpus/tests/snocone/parser-fixtures/` is PASS=8/FAIL=59 against its pinned `.ref` (AST shape drift — the compiler now wraps assignment in `TT_ASSIGN`; the refs predate that shape — see `FINDING-2026-08-27-seat08-parser-fixture-ast-oracles-drifted-snocone-59of67-icon-153of153-plus-a-masking-set-e-bug.md`), and correctness is independently in flux the same day via the while/for-loop bug named above (`snocone-while-for-loop-second-iteration-error5`, still FREE/unassigned in `QUEUE.tsv` as of this note). Re-pinning `.ref` files or adding a `LANG_CONFIGS["snocone"]` entry now would risk baking a currently-broken shape in as canonical, or being invalidated within hours as the loop bug is fixed — "a correctness call this row has no standing to make unilaterally" (seat08, endorsed by seat05/seat06; not re-litigated here, only re-checked for staleness). **Unblocks when:** `snocone-while-for-loop-second-iteration-error5` lands AND someone with standing on Snocone AST design rules compiler-vs-oracle on the `TT_ASSIGN` drift; only then attempt the `LANG_CONFIGS["snocone"]` entry (row `suite-harness-lang-configs`, closed on this ruling) and its downstream sibling `tests-consolidate-snocone` (PARKED on the identical ruling). **(First half of this AND met 2026-08-27 — see the while/for UPDATE above; still blocked on the second half, the AST-drift ruling.)** Full chain: `FINDING-2026-08-27-seat05-tests-consolidate-snocone-same-harness-gap-plus-active-ast-drift.md`, `FINDING-2026-08-27-seat08-parser-fixture-ast-oracles-drifted-...md`, `FINDING-2026-08-27-seat12-suite-harness-lang-configs-...md`.

**What this means for a session picking up Snocone work:** the restoration tier is a regression hunt against a known-working recent baseline, not greenfield feature work. It very likely explains (and should be checked against) several "open" items in the historical ladders below before assuming they're still genuinely open bugs in the pre-zeta sense — a rung marked done in, say, April may simply be regressed now, which is a different problem with a different fix (find what the zeta month broke) than what the rung's own text describes.

⭐ **BEAUTY.md's "ON HOLD" ruling (2026-05-02, below) is SUPERSEDED by this resumption** — `snocone-returns-codegen` explicitly names `beauty.sc running beside beauty.sno` as "Lon's direct want" and is actively working the exact blocker (`trace.sc`'s `nreturn`s) that the old ladder left at SB-6.E.7-H. Do not treat the ON-HOLD banner in the BEAUTY SELF-HOST section below as current; it is the state that was true 2026-05-02 through 2026-08-24 and no longer.

⛔ **THREE KNOWN STALENESS HAZARDS in everything below — verify against the live tree before trusting, per this project's own ONE-AUTHORITY habit:**
1. **Execution-mode flags.** Every absorbed file's gate commands read literally `(--run, --run, --run)` — three identical placeholders where three DISTINCT flag names (an IR tree-walk mode, an SM-interp mode, a JIT mode) originally stood, lost to an unrecorded rename pass at some point before this consolidation. Root `CLAUDE.md` documents that **modes 1 and 2 are now DELETED outright** — only mode-3 (`--run`, BINARY) and mode-4 (`--compile`, TEXT) exist today. The three-mode model these files describe is therefore doubly stale. Do not guess at the lost flag names; re-derive the intended check from the current two-mode architecture instead.
2. **Corpus paths.** Spot-checked 2026-08-27: `corpus/programs/snobol4/demo/beauty/` (cited throughout) is GONE — the re-grid (see root `CLAUDE.md` § Corpus layout) moved it to `corpus/demo/snobol4/beauty/` (`.sno`) and `corpus/demo/snocone/beauty/` (`.sc`, with its own `test/`+`docs/`). `corpus/SCRIP/` (the self-host scratch dir `tree.sc`/`lower.sc`/`sm_interp.sc`/`parser_*.sc` lived in per the SM and IN-SNOCONE ladders) is GONE — the self-host bootstrap library now lives at **`SCRIP/bootstrap/`** (confirmed present: `assign.sc case.sc counter.sc gen.sc global.sc match.sc omega.sc parser_{icon,prolog,raku,rebus,snobol4,snocone}.sc qize.sc semantic.sc ShiftReduce.sc sm_interp.sc stack.sc tdump.sc trace.sc tree.sc` + a `tests/` dir) — **but `lower.sc`/`lower_driver.sc`, load-bearing for the SM-LOWER ladder below, were NOT found there or anywhere else searched.** Unverified whether they were folded into the shared `src/lower/lower_snobol4.c` (plausible — see below) or genuinely lost in the corpus→SCRIP/bootstrap migration. Whoever next touches SM-LOWER: check this first.
3. **Lowering has unified.** `src/frontend/snocone/` today holds only `snocone_lex.{c,h}`, `snocone_parse.{y,tab.c,tab.h}`, `snocone_driver.{c,h}` — `snocone_lower.c` and `snocone_cf.c`, cited constantly below as "main work here," are gone. This is consistent with, not contradicted by, IR-BB's own newest entries (2026-07-21), which already show Snocone-specific fixes landing in the SHARED `src/lower/lower_snobol4.c` — matching root `CLAUDE.md`'s current one-pipeline architecture ("Language identity stops at lower"). Treat any reference below to `snocone_lower.c`/`snocone_cf.c` as a pointer to whatever superseded it, not a live filename.

## ⛔⛔⛔ FACT RULE — ZERO C BYRD BOX FUNCTIONS (byte-identical banner in all eleven absorbed files; canonical text shared with every language's BB goal)

```
╔══════════════════════════════════════════════════════════════════════════════════════════════════╗
║  ⛔ ABSOLUTE RULE — ZERO C BYRD BOX FUNCTIONS — NO EXCEPTIONS — READ THIS BEFORE WRITING CODE  ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════╣
║                                                                                                  ║
║  A C Byrd box (C BB) is ANY C function with this signature:                                     ║
║                                                                                                  ║
║      DESCR_t foo(void *zeta, int entry)                                                         ║
║                                                                                                  ║
║  implementing four-port logic (α / β / γ / ω).                                                  ║
║                                                                                                  ║
║  THERE MUST BE ZERO OF THESE IN THE CODEBASE. NOT ONE. NONE. EVER.                              ║
║                                                                                                  ║
║  ALL Byrd boxes are x86 ASSEMBLY emitted at runtime by the emitter.                             ║
║  If you want a BB, you EMIT it. You do not write a C function for it.                           ║
║                                                                                                  ║
║  The only permitted C functions with (void *zeta, int entry) signature are:                     ║
║    • icn_lazy_box  — infrastructure shim, not a generator                                       ║
║    • icn_bb_dcg    — infrastructure DCG driver, not a generator                                 ║
║                                                                                                  ║
║  If you just wrote DESCR_t foo(void *zeta, int entry) { ... } — DELETE IT.                     ║
║  Implement it as an IR_block_t DCG (ir_exec.c + lower_icn.c) driven by icn_bb_dcg.             ║
║  See IR_ICN_UPTO in ir_exec.c and lower_icn_upto() in lower_icn.c as the template.             ║
║                                                                                                  ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```

**HISTORY (from `GOAL-SNOCONE-IR-BB.md`, byte-identical to the SNOBOL4/Icon/Prolog/Raku BB goals' copy):** this prohibition stood unenforced for at least two months — Lon ordered these C `(ζ, int entry)` byrd boxes deleted at least three separate times, and each time a session refused, re-introduced them, or reverted the deletion "to keep the build green." Finally deleted 2026-06-01: the `pl_*_fn` family, `gen_bb_dcg`, `gen_bb_oneshot`, `resolve_bb_dcg`, `bb_deferred_var`/`_exported`, `fail_box`, dead `bb_cap`/`bb_atp` declarations, and the `bb_broker` driver itself. **KEEPING THE BUILD GREEN IS NOT A LICENSE TO PRESERVE A FORBIDDEN BOX** — when this signature and a green build conflict, the signature loses. **COMPLETION TEST:** (a) `grep -rnE 'DESCR_t[[:space:]]+[A-Za-z_]+[[:space:]]*\([[:space:]]*void[[:space:]]*\*[[:space:]]*[a-z]*[[:space:]]*,[[:space:]]*int[[:space:]]+entry' src/ --include=*.c --include=*.cpp --include=*.h | grep -v typedef` == 0; (b) no `bb_broker` driver exists; (c) every emitted box is entered by a jump to an α/β label, never a C call with an `entry` int.

## ⛔ FACT RULE — NO AST WALKING IN MODES 2/3/4 (banner from `GOAL-SNOCONE-SM.md` / `GOAL-LANG-SNOCONE.md`)

```
╔══════════════════════════════════════════════════════════════════════════════════════════════════╗
║  ⛔ NO AST WALKING IN MODES 2/3/4 — see RULES.md § "NO AST WALKING IN MODES 2, 3, OR 4"         ║
╠══════════════════════════════════════════════════════════════════════════════════════════════════╣
║  Sess 2026-05-15g removed all tree_t* dereferences from sm_interp.c (mode 2) and                ║
║  sm_jit_interp.c (mode 3). Stubs print [NO-AST] <opcode> on stderr.                              ║
║                                                                                                  ║
║  If a gate breaks with [NO-AST] FOO — write fresh SM/BB lowering for FOO.                       ║
║  Do NOT restore the AST-walking call.  Do NOT route through proc_table_call or any              ║
║  other back-door that hands a tree_t* to mode-2/3/4 code.                                       ║
║                                                                                                  ║
║  Mode 1 (`--run` standalone AST interp) is unchanged and remains the reference path.        ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
```
⛔ **This banner's own mode numbering is now stale** — see FRONT STATUS staleness hazard #1. Its PRINCIPLE (no AST/IR walking during compiled execution) is current and restated more precisely by the FACT RULE immediately below, drawn from the same source file; treat that one as the operative text and this banner as the historical form it grew from.

## ⛔ FACT RULE — NO AST AND NO IR DURING MODE-3/MODE-4 EXECUTION (byte-identical across the language BB goals)

**During the EXECUTION of a mode-3 (`--run`) or mode-4 (`--compile`) program, NOTHING reads or writes the AST (`tree_t`) or the IR (`IR_t`/`IR_graph_t`).** The compiler reads the IR exactly ONCE — before execution — to emit the mode-3 RX-slab image or the mode-4 `.S` source; thereafter the produced machine code runs with ZERO reference to either tree. A runtime helper (`rt_*`) operates only on `Term*`/`DESCR_t` values, never on `IR_t*` or `tree_t*`. **THE ONE EXCEPTION — `EVAL()` and `CODE()`**, which by definition compile a string into executable form at runtime. **FORBIDDEN on the mode-3/4 run path:** any `rt_*` (or template-called) function that takes an `IR_t*`/`IR_graph_t*`/`tree_t*`, walks `->operands`/`->c[]`/`->t`/`->op`, or bakes a live `IR_t*`/`tree_t*` address into emitted code. A box not yet converted is a LOUD `x86_bomb(msg)`, never a silent IR/AST read. **COMPLETION TEST:** (a) no mode-3/4-reachable `rt_*` reads AST/IR (grep for `IR_t*`/`tree_t*`/`IR_LIT`/`->op`/`->t` == 0, excepting `EVAL`/`CODE`'s rail and the mode-2-only `IR_interp_once`); (b) no function bakes a live tree address into emitted bytes.

## ⛔ FACT RULE — NO VALUE STACK, EVER (byte-identical across the language BB goals)

**SCRIP HAS NO VALUE STACK. NO SESSION, IN ANY LANGUAGE, MAY CREATE ONE.** (Lon directive, 2026-05-31.) Every value a BB graph computes or holds lives INSIDE a box: read-only `[rip+disp]`, or read-write `[ζ=r12+off]`. A consumer reads a producer's result directly from that producer's slot — never via push/pop through a global arena. The `g_vstack` global array is DELETED and must NEVER be resurrected, under any name (`*_vstack[]`, `value_stack`, `g_estack`, a hand-rolled push/pop arena moving values between boxes). (KEEP, not a value stack: the Prolog trail, the choice-point ledger, the C call stack for genuine recursion, an explicit indexed per-activation frame array.) **COMPLETION TEST:** (a) `grep -rn 'g_vstack' src/` == 0; (b) no new global/static push/pop value arena exists; (c) `scripts/test_gate_no_vstack.sh`'s `g_vstack` line reads 0.

## ⛔ FACT RULE — NO -O2 BUILDS, EVER (root `CLAUDE.md`/RULES.md, Lon s262/s267 — SUPERSEDES the O0-DEV text below)

`GOAL-SNOCONE-IR-BB.md` carried an elaborate "`-O1`/`-O2` are perf-only, `-O0` for feature work" rule (Lon s119, 2026-07-21) with its own Makefile-default mechanism (`RT_OPT ?= -O0`) and an explicit note that its body was "byte-identical across the six GOAL-*-BB files." **That framing is now DEAD, exactly as `GOAL-ICON-100.md` and `GOAL-PROLOG-100.md` already record for their own languages: Lon's later, broader ruling (s262, reissued s267) is `RT_OPT` is `-O0` for development AND benchmarks AND demos — there is no arm that justifies `-O2`, full stop.** Never pass `RT_OPT="-O2 …"`, never build an `-O2` RT_TAG, never quote an `-O2` number as current state. Label every perf number with its `RT_OPT`; it now reads `-O0`. Authority: root `CLAUDE.md` § NO -O2 BUILDS EVER / `.github/RULES.md`.

## ⛔ Session-close law: pointer, not a duplicate

`GOAL-SNOCONE-IR-BB.md` carried its own copy of the "HANDOFF COMPLETE requires a confirmed push" + "the word HANDOFF is forbidden in the assistant's own prose" rules (Lon, 2026-06-24). Those are project-wide, not Snocone-specific, and RULES.md's own Handoff sequence (§ "HANDOFF COMPLETE" / `handoff_status.sh`) is the current, live, parent authority — duplicating it here would only let the two drift. Follow RULES.md; nothing about this pointer changes what it requires.

---

## ⭐⭐⭐ THE MODEL — HOW SNOCONE FITS THE SHARED PIPELINE

Snocone is **C-syntax SNOBOL4**: same semantics, same runtime, same IR (`LANG_SNO`), different surface syntax (braces/semicolons instead of column-position statements, `if`/`while`/`for`/`function` instead of `:S()`/`:F()`/`DEFINE`). It is not a separate machine the way Icon (generators) or Prolog (backtracking/unification) are — it rides SNOBOL4's Byrd-box pipeline directly, which is why so much of its history is "port this SNOBOL4 program to Snocone syntax" and "fix this shared-pipeline bug that happens to surface through Snocone first."

```
.sc → snocone lex/parse (src/frontend/snocone/: snocone_lex.c, snocone_parse.y, snocone_driver.c)
    → lowering (historically snocone_lower.c/snocone_cf.c; unified into the shared src/lower/
      lower_snobol4.c by 2026-07-21 per IR-BB's own newest entries — see staleness hazard #3)
    → the same optimizer_run → emitter → templates → x86-64 pipeline every language shares
    → mode-3 (--run, BINARY) / mode-4 (--compile, TEXT) — see staleness hazard #1 on stale mode counts
```

Pattern match: `subject ? pattern` lowers to a STMT_t with subject+pattern fields, routed through the exact same broker/BB path as SNOBOL4's `subject 'pattern' = replacement`. Control flow (`if`/`while`/`for`/`return`/`freturn`/`nreturn`/`procedure`) lowers to the SNOBOL4 statement model's goto/label STMT_t sequences — Snocone's frontend does the C-syntax → goto/label translation once, at parse/lower time; nothing downstream needs to know it started as `if`/`while` rather than `:S()`/`:F()`.

**Snocone → SNOBOL4 translation table** (for writing Snocone tests by eye from working SNOBOL4 programs — from `GOAL-LANG-SNOCONE.md`):

| SNOBOL4 | Snocone |
|---------|---------|
| `X = "hello"` | `x = "hello";` |
| `OUTPUT = X` | `OUTPUT = x;` |
| `X Y :S(L)` | `if (x ? y) { goto L; }` |
| `X 'pat' = 'repl'` | `x ?= pat <- repl;` (or `subject ? pattern` scan form) |
| `DEFINE('F(A,B)')` | `procedure F(a, b) { ... }` |
| `:S(L) :F(M)` | `if (...) { goto L; } else { goto M; }` |
| `IDENT(A,B)` / `INTEGER(X)` / `DIFFER(A,B)` | same builtin names, lowercase args |

**The self-host bootstrap library** — the shared `.sc` runtime that six languages' self-hosted parsers (`parser_{icon,prolog,raku,rebus,snobol4,snocone}.sc`) and Snocone's own self-hosted lower/interp effort build on — lives at **`SCRIP/bootstrap/`** today (moved from the eleven files' `corpus/SCRIP/`; see staleness hazard #2, including the unresolved `lower.sc`/`lower_driver.sc` question).

**Two still-open language-design questions, unresolved across the absorbed files — flagged, not picked:**
1. ⛔ **The "Koenig pivot" (`GOAL-LANG-SNOCONE.md`, 2026-04-18 session) vs. the standing three-tier-operator ruling (`GOAL-SNOCONE-CLAWS5.md`, 2026-04-17).** One day before the pivot, Lon explicitly RULED to KEEP three-tier comparison operators (`==`/`!=`/`<`/... → numeric; `:==:`/... → lexical; `::`/`:!:` → identity) — "collapse was considered and rejected." The very next day's pivot session records: "retire `&&` as explicit concat operator; make Snocone Koenig-faithful... Whitespace-sensitive implicit concatenation. Drop C comparison operators (`==`, `!=`, `<=`, `>=`, `<`, `>`) in favor of SNOBOL4 builtins (`EQ`, `NE`, `LE`, `GE`, `LT`, `GT`)." **These two rulings directly conflict on comparison operators specifically.** Muddying it further: every `PARSER-SC` session from 2026-05-04 onward (weeks after the pivot) continued defining and using `==`/`!=`/`<`/`>`/`<=`/`>=` as real Snocone tokens (`GOAL-PARSER-SNOCONE.md` PARSER-SC-6a/6c), with no recorded reversal of the pivot — so in practice the language never stopped accepting them, whatever the pivot session intended. **Unresolved: was the pivot fully adopted, partially adopted (implicit concat only, operators untouched), or itself superseded?** A "mooted" future top-level language `SCRIPT` unifying Snocone++/Prolog/Icon syntax was floated in the same pivot session as a possible real destination for this redesign — checked 2026-08-27: **no `GOAL-LANG-SCRIPT.md` exists**, so that direction was never opened as real work.
2. ⛔ **SB-6.E.7-L — locals-as-parameters semantic bug.** `function fn(a, b, x, y, z)` today means "a,b formal, x,y,z local," but a caller passing `fn(p, q, 1, 2, 3)` initializes the supposed-locals to the extra arguments instead of null — a real, live semantic bug (currently not biting because no caller happens to over-supply args). Andrew Koenig's original 1981 SNOCONE used `procedure name(args) locals { body }` — locals named after the closing paren, always null on entry regardless of caller arity. Proposed but never landed: extend `snocone_parse.y`/`snocone_lex.l` to accept that form, lower locals with forced-null init distinct from formal-parameter binding, then convert the ~17 `.sc` files under `corpus/demo/snocone/beauty/` that currently rely on the collapsed (buggy) form.

---

## ⛔ DEFINITION OF DONE

1. **Restoration first (current gate):** `test_crosscheck_snocone.sh` + `test_smoke_rebus.sh` + `test_corpus_snobol4.sh` all green — Snocone (and Rebus, riding along) back to zero regressions against its pre-zeta-month baseline. This is the live, minted DONE-WHEN of `snocone-restore-prezeta`; nothing below matters until this is true again.
2. **The demo ladder** — `porter.sc`, `claws5.sc`, `treebank-list.sc`, `treebank-array.sc` each PASS oracle diff (SPITBOL or CSNOBOL4 `-bf`, whichever is the working oracle per program) in both current modes, zero `-P` flags beyond what the `.sno` sibling needs.
3. **`beauty.sc` self-hosts** — `scrip --run` on `beauty.sc` + its 16-lib chain, reading `beauty.sno`, produces output byte-identical to SPITBOL running `beauty.sno` on the same input (the SB-6 gate). Actively being pursued via `snocone-returns-codegen`/`snocone-restore-prezeta` — see FRONT STATUS.
4. **PARSER-SC self-hosts** — `parser_snocone.sc` parses Snocone source (including its own `beauty.sc`) into the canonical IR tree, `tree_equal` against the existing C frontend on the full rung corpus. **Reached PASS=67/67, beauty.sc 1148/1148 byte-identical, 2026-05-07** — re-verify this hasn't regressed under the zeta month (folded into `snocone-restore-prezeta`'s `test_crosscheck_snocone.sh` arm).
5. **SM self-hosts** — `lower.sc` + `sm_interp.sc` (Snocone-authored) lower and execute the shared AST for an arbitrary program, not just hand-built test ASTs. Blocked on SL-13d (parser → `Lower_collect` wiring) since 2026-05-12; not reopened since.
6. **The Snocone compiler is written in Snocone** (`GOAL-SNOCONE-IN-SNOCONE`'s charter) — Stage 1 (`snocone.sc`, Snocone-authored) compiles itself, Stage 1 output ≡ Stage 2 output. Architecture decided, implementation mostly ON HOLD pending `beauty.sc`-quality pattern-based parsing (item 3).
7. **IR-BB native lowering** — all 14 `beauty-sc` subsystem drivers PASS under `--run` via native BB-graph control-flow + pattern lowering (not the tree-walk interpreter). At 19/20 as of 2026-07-21 (only `trace` remains, root-caused as a `DATATYPE(unset var)` runtime-model divergence, unrelated to lowering).
8. **Switch-table backend** — `--switch-style=table`/`chain`/`auto` selectable per-site/file/compile, ≥3× speedup on the table arm, byte-identical output either way. Zero rungs started.
9. Gates strict and green: `test_smoke_snocone.sh`, `test_parser_snocone.sh`, `test_gate_snocone_beauty_suite_reads_every_test_in_both_modes_with_no_xfail.sh`, `test_crosscheck_snocone.sh`, `handoff_status.sh` COMPLETE.

## THE INSTRUMENT (boards are RUN, never transcribed; re-derive every count fresh — every number below is dated and may already be stale)

```bash
bash scripts/test_smoke_snocone.sh                    # baseline: PASS=5 (last measured)
bash scripts/test_parser_snocone.sh                    # self-host parser gate: PASS=67 (2026-05-07)
bash scripts/test_gate_snocone_beauty_suite_reads_every_test_in_both_modes_with_no_xfail.sh   # WIRED blocking: the beauty container corpus/tests/snocone/beauty_modules.sc, m3+m4, no XFAIL -- 20/20 both modes 2026-09-23
python3 scripts/corpus_suite_harness.py extract $S4E_HOME/corpus/tests/snocone/beauty_modules.sc $S4E_HOME/corpus/tests/snocone/beauty_modules.ref test_<name> /tmp/t.sc   # one beauty test alone (replaces the retired test_beauty_snocone_subsystems.sh)
bash scripts/test_snocone_beauty_self_host.sh --diff --quiet   # SB-6 fingerprint vs SPITBOL oracle
bash scripts/test_crosscheck_snocone.sh                # 3-mode divergence check — the current restoration gate
bash scripts/test_self_host_smoke.sh                   # SM ladder (SL/SI rungs), 16/30+ target
bash scripts/test_smoke_unified_broker.sh              # cross-language broker smoke (Snocone contributes ~5)
bash scripts/test_gate_snocone_returns_codegen.sh       # new 2026-08-24: the current top-of-tier gate
./scrip --monitor file.snc                              # in-process IR/SM/JIT sync-step divergence finder
```
⛔ Every flag above is quoted as last recorded in the absorbed files; re-verify against `./scrip` (no `--help` exists — see root `CLAUDE.md`) before trusting a flag spelling.

## ⛔ LAWS THAT BIND EVERY RUNG (RULES.md is the parent; compact, deduplicated from all eleven files)

- **NEVER patch corpus `.sc`/`.sno` source to work around a runtime bug.** Fix the runtime. Repeated as an explicit Lon directive at least three separate times across the absorbed files (sessions #73, 2026-06-24, and others) because it kept almost happening — a bug found via a `.sc` port is real evidence about the shared runtime, and papering over it in the port hides that evidence.
- **⭐ THE BODY-PART-CORRESPONDENCE PRINCIPLE (Lon, 2026-05-02, the standing translation law for every `.sno`/`.inc` → `.sc` port):** mentally erase `:F(LBL)`/`:S(LBL)`/`:(LBL)` plumbing and goto-label targets from the source — what remains is a stream of **body parts** (assignments, expressions, pattern matches, calls). Those body parts go into the `.sc` port **unchanged in name and content.** Then wrap them with structured control (`if`/`else`/`while`/`for`/`break`/`return`/`freturn`/`nreturn`). It is a one-to-one body-part correspondence — the control envelope is new, the bodies are identical. Forbidden moves: renaming identifiers (except function-name-shadow collisions), inventing intermediates not in the source, adding branches not in the source, dropping comments or `OPSYN` calls, reasoning about *when* the source statement would have failed and reimplementing that logic instead of porting it. Expression-IR should end up byte-identical between `.sc` and `.sno`/`.inc`; only control-flow IR differs.
- **No automated mass-transforms on `.sc` code.** A scripted "strip braces from single-statement bodies" sweep landed broken code (`else Shift = .dummy; nreturn;` with no guarding `if`) and had to be reverted. Every `.sc` transform is hand-verified per file.
- **No dead-code pruning when porting.** *"The code is turned on by settings"* (Lon) — every construct in the canonical `.sno`/`.inc` gets ported regardless of whether it appears reachable at default settings.
- **Case sensitivity is a frontend concern, not a flag the user should need.** Since SCRIP `5535b229`, every non-SNOBOL4 frontend (`snocone_cf_compile`, and the Icon/Raku/Prolog/Rebus equivalents) calls `sno_set_case_sensitive(1)` itself at compile entry — `--case-sensitive` should never be required on the command line for a `.sc`/`.icn`/`.pl`/`.raku`/`.reb` file.
- **Pattern globals convention:** `&ANCHOR = 0` always (never 1), `&FULLSCAN = 1` always, set at program start.
- **`&&` in this history is Snocone's original explicit concatenation operator**, juxtaposition is whitespace-implicit concat in later ports — see the unresolved Koenig-pivot conflict above before assuming either is currently "the" rule.
- One-authority naming: `E_*` IR tags, `x86(...)` sole binary/text producer, TEMPLATE-ONLY, BOTH-MEDIUM — all inherited unchanged from the shared cross-language laws in root `CLAUDE.md`; not re-derived per-language here.

---

## RUNGS — the demo ladder (porter / claws5 / treebank-list / treebank-array)

⚠ **Three separate, only partly-reconciled tracking threads cover this same program family across the eleven absorbed files** — kept apart below rather than silently merged, per the conflict-preservation rule:

- **`GOAL-SNOCONE-DEMOS.md` (SD-1..SD-5, earliest, no session dates)** — the oldest telling. Left `treebank.sno`'s `*group` self-reference as an open "CRITICAL BUG," using filenames (`treebank.sno`, `treebank.input`) that the later threads explicitly renamed away from (→ `treebank-list.sno` / `treebank4.input`, to make room for a `treebank-array.sno` sibling). **Fully superseded** by the other two threads' later, more-developed state; kept only for provenance.
- **`GOAL-SNOCONE-CLAWS5.md` (CL-1..CL-5) and `GOAL-SNOCONE-TREEBANK-LIST.md` (TB-1..TB-4)** — a parallel, more detailed pair of files diagnosing **"SC-26"**: a hypothesized pattern-engine bug where `(PAT . var) . *fn(var)` — a chained capture-then-deferred-call — silently fails to invoke `*fn`. TREEBANK-LIST.md fully root-caused it as **three separate bugs** (unified CAPTURE/CALLCAP naming-list ordering in `snobol4_nmd.c`; `E_INDIRECT(E_FNC)` not recognized as a deferred-callcap target in `interp.c`; `bb_callcap`'s `spec_t`→`DESCR_t` return-type UB in `stmt_exec.c`) — the first two fixed, the third (which CLAWS5.md's own diagnosis says blocks both files) left pending.
- **`GOAL-LANG-SNOCONE.md`'s own D-1..D-4 + SC-24..SC-31 thread** covers the identical three programs under different rung IDs, and its later 2026-04-18 session **explicitly reframes SC-26 as a wrong hypothesis**: *"the identical pattern works correctly under scrip --run for .sno programs... the three .sc demo programs fail from three distinct causes"* unrelated to the original capture-call theory (a `&&`-vs-juxtaposition mismatch under the just-announced Koenig pivot; a pattern-match failure in `treebank-list.sc`'s `ARBNO(*group) && *delim && RPOS(0))`; an undiagnosed parse failure in `treebank-array.sc`). **Neither correction cites or resolves the other** — CLAWS5/TREEBANK-LIST's `bb_callcap` fix was never confirmed against LANG-SNOCONE's later three-distinct-causes finding, and LANG-SNOCONE's finding was never checked against whether the `bb_callcap` return-type fix had already landed. **Whoever next touches these three programs: re-derive from scratch which of these diagnoses (if any) still holds** rather than resuming either ladder's numbered steps as-is.

**`porter.sno`/`porter.sc` (D-1, LANG-SNOCONE's own thread, latest state 2026-04-18 session 3):** the goal-complete banner below is directly contradicted by this — `porter.sc` was found to hang on user labels inside procedures (SC-27, since made moot by an architectural fix), then (after the case-sensitivity architectural fix landed) found to have a **genuine correctness bug**: on `porter.input`'s 8th expected word "abat" it emits "ab" and the process OOMs on the full 23531-line input. Root cause not found — suspected interaction between `@ "stem"` pattern-assignment and RTAB/RPOS backtracking (does a trial-alternative's assignment survive or wrongly persist across backtrack?). **Two follow-up bugs filed and left open:** SC-27 (labels inside `procedure` bodies mis-lower / hang the parser — not on `porter.sc`'s critical path since it's goto-free, but blocks any future `.sc` using them) and SC-28 (`procedure f(x) (i)` locals-paren syntax emits `DEFINE("foo(x)i")`, missing the required comma before locals — see the SB-6.E.7-L design question above, same underlying locals-syntax gap).

**`GOAL-LANG-SNOCONE.md`'s own top banner says "GOAL COMPLETE — all 22 steps done, all phases PASS"** (session 2026-04-15/16) — this predates and is contradicted by the porter.sc bugs found in the 2026-04-18 session recorded in the SAME file. Carried forward as written, flagged as internally inconsistent, not silently resolved either way.

## RUNGS — beauty.sc self-host (SB-*)

⏸ **Put ON HOLD by Lon, session 2026-05-02 #19** — *"you will never find it then. You have no methodology to find it. So the end of that. Put this goal on hold."* — after three sessions produced different framings of the SB-6.E.7-H rollback bug (rollback / leak / failure-walker / abandoned-seal) without converging or landing a fix, and minimal reproducers in isolation would not surface it (only the full beauty.sc + 16-lib configuration triggers it). **⭐ Superseded by the Aug-2026 restoration tier — see FRONT STATUS. Resumption criterion as originally written:** a new diagnostic angle on SB-6.E.7-H — an in-place `*Parse`-grammar bisector, a last-agree/first-disagree trace harness between scrip and SPITBOL, or an unrelated compiler change that incidentally exposes the bug smaller. `snocone-returns-codegen`'s discovery that the blocker is actually in `nreturn` codegen (not the rollback framing this section describes) may itself be exactly such a new angle — unconfirmed whether it's the same bug wearing a different diagnosis or a second, independent one.

**State at hold:** three baseline gates green throughout (`smoke_snocone` PASS=5, `beauty_snocone_all_modes` PASS=42 SKIP=3, `smoke_unified_broker` PASS=49); SB-6 fingerprint `lines=98 stderr=4488 parse_err=12 internal_err=0 rc=124`, 19 diff hunks vs the 622-line SPITBOL oracle (down from 91 hunks earlier the same day — a mechanical goto-based main-loop port, byte-for-byte faithful to `beauty.sno`'s `main00..main05`, got 5× closer to oracle and cleared two whole error classes even though it exposed the rollback bug more aggressively). All 16 lib `.sc` files under `corpus/demo/snocone/beauty/` (moved from the file's original `SCRIP/test/beauty-sc/beauty/`) were spot-checked body-part-faithful to their `.inc` counterparts.

**Closed rungs (SB-2 through SB-6.H):** lexer/scan-replacement fixes; per-file faithful `.inc`→`.sc` ports (all 16 libs + beauty.sc itself, across three increasingly strict audit passes — see below); `E_VLIST`/`E_NOT` lowering fixes; the `sno`-prefix strip from BOTH `beauty.sno` and its `.sc` port (67 names, one real collision found and fixed: a parser pattern `snoSorF` would have collapsed onto an unrelated runtime variable `SorF` had the rename not pre-renamed the variable first); `SC_T_`/`T_` token-name collapse.

**⭐ The audit-pass history is the file's own best evidence for why the body-part-correspondence law above exists as a LAW, not a suggestion:** pass #1 (session #11) "cleared" all 17 files as faithful; Lon found a violation pass #1 had missed (`if (~(t = EVAL(t))) { nreturn; }` in `ShiftReduce.sc::Reduce`, a correct-but-differently-styled translation pass #1 waved through), which reopened pass #2 with sharpened, explicit per-line scrutiny rules; pass #2 in turn found that pass #1's `trace.sc::T8Trace` "fix" had merely re-syntaxed the same polarity inversion rather than correcting it. **The mandate that survived: line-level scrutiny against the source, never block-level skimming — "this looks fine" without reading both sides is not an audit.**

**Open rungs, as last recorded (2026-05-02, before the hold):**
- **SB-5c.2** — `claws5.ref`'s 95 lines don't match either oracle's output on `claws5.input`; likely generated from a different corpus (`CLAWS5inTASA.dat`) than the trimmed input it's checked in beside. Corpus-curation fix, not a runtime bug.
- **SB-5d.4** — maintain the zero-`goto` invariant already achieved in `treebank-list.sc`/`treebank-array.sc`; any future `:S()`/`:F()` port work must translate to structured Snocone, never `goto`.
- **SB-6.E.6** — a "confirm-with-Lon anti-rationalization pass": write minimal behavioral tests for the Snocone invariants Lon holds most strongly, run under both scrip and SPITBOL, report divergence between spec and implementation OR between spec and what Lon actually intended (as distinct from what the audit's author rationalized from reading the code). Requires Lon; never run.
- **SB-6.E.7-L** — the locals-as-parameters syntax gap; see the open design questions above.
- **SB-6.E.7-H** — the runtime rollback bug itself (globals silently reset across a successful `*Parse` match under the full configuration only). The active blocker at hold time; see FRONT STATUS for its probable current successor/manifestation.
- **SB-6.E.7-I** — `Pop()` returning `t='Label'` instead of a reduced `Stmt` tree on certain inputs; may resolve automatically once SB-6.E.7-H lands, unconfirmed.
- **SB-6.G** — ~14 lines of `E_UN_*` trampoline boilerplate in `snocone_lex.c`; narrow, non-blocking cleanup.
- **SB-7** — final gate script + commit once SB-6 is green.

**Move plan, deferred until SB-6 is green (unexecuted):** relocate the beauty-sc sources from their original `SCRIP/test/beauty-sc/beauty/` into `corpus/programs/snobol4/demo/beautify/` as the "BEAUTIFY" (Snocone-implementation-reads-SNOBOL4-source) counterpart to "BEAUTY" (SNOBOL4-implementation-reads-its-own-source) — **note the corpus re-grid already relocated the sources once, to `corpus/demo/snocone/beauty/`, independent of and before this planned move; re-derive whether the BEAUTIFY move plan still applies to the current path before executing it.**

## RUNGS — SM self-host: `lower.sc` + `sm_interp.sc` (SL-* / SI-*)

The M2 self-hosting path: reimplement the SM_Program compile+execute pipeline (`src/runtime/x86/lower.c` and `sm_interp.c`'s C originals) in Snocone, so the same interpreter that runs `.sc` programs can also lower an AST to SM and execute it. Two ladders (`SM-LOWER`, `SM-INTERP`), formerly two files, merged 2026-05-12 since they share corpus/gates/setup.

⭐ **Predecessor, folded in here 2026-08-29 via `goal-files-major-consolidation`: `GOAL-SM-LOWER-REFACTOR.md` (was: 240 ln), the shared C-side prerequisite this whole ladder's own text names as its blocker** ("The Snocone port (SL-1+) is a transcription exercise *after* this refactor lands, not before"). It rebuilt the **shared, cross-frontend** `lower.c` — the file six frontends (SNOBOL4, Snocone, Icon, Rebus, Prolog, Raku) feed one AST into — from a 1,200-line monolithic switch into a kind→handler dispatcher (`SR-1..SR-15`, 2026-05-11), then collapsed the parallel `CODE_t`/`STMT_t` linked-list-of-structs into the same AST tree the frontends already produce (`SI-1..SI-13`, same week) — freeing those type names and making the eventual Snocone self-host a one-to-one translation rather than a port-with-cleanup. **Self-stamped fully closed, all 28 rungs done, every one with a real SCRIP commit hash** (`f209b8d3` through `dba8c612`/`508b2324`/`71df89af`/`007a2082`; the trailing SI-13 lands `AST_t`/`AST_e` → `TT_*`-family naming, the vocabulary still live today). **Re-verified fresh this landing, not trusted from the file's own claim:** `src/parsers/snobol4/snobol4.l`'s `sno_parse_ast`/`parse_program_tokens_ast` and `src/driver/stmt_ast.c`'s `stmt_to_ast`/`code_to_ast` shim are exactly the SI-2/SI-4 deliverables, present today at the current `src/parsers/` path (post the frontend→parsers rename — see the process-housekeeping file's addendum on `GOAL-SRC-REORG.md` for that rename's own date/commit). One deferred item, correctly self-flagged rather than silently dropped: `CODE_t`/`STMT_t` no longer *drive* the pipeline (SI-1..SI-8, confirmed — `sm_preamble`/`execute_program` consume `AST_t` only), but the TYPES themselves still exist today as a parse-time-only intermediate for five of six frontends (the `code_to_ast`/`stmt_to_ast` shim above, plus `snocone_driver.c`'s `snocone_parse_program`) — full type deletion was always explicitly named as this SM-LOWER ladder's own job (item 5's "Full type deletion deferred to `GOAL-SNOCONE-100.md`'s SM-LOWER ladder"), not a loose end this fold silently drops. Whoever eventually finishes SL-13d and the rest of this ladder inherits that closing task already correctly addressed to them.

**Closed (all landed 2026-05-11/12, one intense two-day push):** the full `SL-0..SL-13c` lowering ladder (label table, goto/jump lowering, literal/variable/arithmetic/comparison/control expression lowering, pattern-expression lowering, deferred-expression thunks, `EVAL(*expr)`) and the full `SI-1..SI-17b` interpreter ladder (dispatch loop, literals, jumps, arithmetic, exponentiation, pattern-match statement execution, all primitive and combinator patterns, comparisons, function calls via `APPLY`, function returns/frames, computed goto, opcode stubs for the 87 SM_* opcodes, corpus mirrors for literals/assign/arithmetic/control).

**Nine real Snocone-vs-SNOBOL4 gotchas found and documented along the way** (worth preserving as a standing reference for anyone writing more `.sc` runtime code): `~` is pattern alternation, not boolean negation — use `GE(...)` for numeric negation; `|` is pattern alternation, not boolean OR; `$name ? pat = repl` doesn't write back through dollar-indirection — use a local copy + explicit write-back; `APPLY` returns `''` on builtin failure, detect via `if (r = APPLY(...))` not by inspecting the return; `+x` truncates to INTEGER, use `x + 0.0` for REAL; Snocone is case-sensitive even where SNOBOL4 folds — `OUTPUT` must be uppercase; Snocone's `switch` compiles to a linear `IDENT` chain today, not O(1) dispatch (see SWITCH-BACKENDS below); the lower.sc tag for exponentiation is `TT_POW`, not `TT_EXP`; the canonical statement tag from a real parser is `'STMT'`, not `'TT_STMT'` (slot lookups are tag-agnostic so both worked, but only `'STMT'` is what `parser_snobol4.sc`/`parser_snocone.sc` actually emit).

**⛔ Open, stalled since 2026-05-12, not reopened since — SL-13d: wire `parser_snobol4.sc`/`parser_snocone.sc`'s main loop to call `Lower_collect(stmt)` per parsed statement**, the last piece for a genuinely all-Snocone pipeline (parse → lower → interp, zero C). Investigated once: `parser_snobol4.sc` already calls `Lower_collect(result)` + `Lower_run()` correctly, but on `trivial.sno` the parse tree comes back empty — `Src ? Compiland` matches but `Top()`/`Pop()` return an empty value, so the AST never populates. Two `Error 5` at load time hint at `SQize`/OPSYN/EVAL failures during grammar construction, corrupting shift/reduce patterns. **Not pursued further per an explicit Lon directive at the time** (redirected to a three-frontend crosscheck approach instead — Track B below) — this is a real open blocker, not an abandoned dead end; the directive was about sequencing, not about the bug not mattering.

**Phase 3 (run the existing corpus through `sm_interp.sc`, not just hand-built mirrors) — three tracks, all partial:**
- **Track A (hand-built corpus mirrors):** 6 of 10 planned mirrors done (`si_15a_literals`, `si_15b_arith`, `si_17a_assign`, `si_17b_control` + two folded into SI-15). Open: `si_17c_while`, `si_17d_for` (probe `TT_FOR` coverage first), `si_17f_strings`, `si_17h_wordcount`. Two more (`si_17e_proc`, `si_17g_multiproc`) blocked on `lower_proc_skeletons`, a Phase-4 stub (`function lower_proc_skeletons() { return; }`).
- **Track B (`scripts/dump_ir_to_ast_builder.py`, a mechanical `--dump-ast`-text → `.sc`-AST-builder converter):** never written. This was the last recorded "NEXT" item (2026-05-12) and the ladder has not moved since.
- **Track C (full pipeline via SL-13d):** blocked on SL-13d above.
- **SI-19/20/21** (bulk-run harness, bug-fix sweep, Phase-3 closing gate at ≥30 self-host tests, currently 16): not started.

**Phase 4 (deferred by design, needs host infrastructure beyond plain Snocone):** BB scheduler opcodes (`SM_BB_PUMP*`), generator coroutine opcodes (`SM_SUSPEND*`/`SM_RESUME`/`SM_GEN_TICK`), Icon frame-slot opcodes, pattern-callback opcodes, `lower_proc_skeletons`. All currently silent/stub in `sm_interp.sc`.

## RUNGS — "the Snocone compiler is written in Snocone" (`SS-*`, GOAL-SNOCONE-IN-SNOCONE)

The second bootstrap-moment goal, pairing with Milestone 1 (SNOBOL4's `beauty.sno` self-host): prove Snocone is expressive enough to host its OWN compiler — frontend, IR, lowering — written in itself. File layout decided (`snocone.sc` driver + `value.sc`/`ir.sc`/`lex.sc`/`parse.sc`/`interp.sc`/`runtime.sc`, using Snocone `struct`/`DATA()` for `Val`/`Node`/`Tok` records).

**SS-0 (analysis) done:** the Snocone frontend emits only ~15-20 of the shared IR's ~90 `EKind` values — critically, it does **not** emit `E_IF`/`E_WHILE`/`E_FOR`/`E_RETURN`/`E_LOOP_BREAK`/`E_LOOP_NEXT` at all; control flow is already lowered to labels/gotos at PARSE time, so a hand-written `interp.sc` needs no control-flow cases whatsoever — only a flat statement loop executing goto/label `STMT_t` nodes. This significantly simplifies the original algorithm sketch.

**SS-1 (step plan) done.** **SS-2 through SS-15 (the actual `value.sc`/`ir.sc`/`lex.sc`/`parse.sc`/`interp.sc` build, TDD ladder against `.ref` files) are ALL still `⏸ ON HOLD`, unstarted** — because `parse.sc` was explicitly scoped to depend on `beauty.sc` (GOAL-SNOCONE-IN-SNOCONE's own note: "beauty.sc is the eventual model for parse.sc... until it lands, parse.sc uses recursive-descent") — and `beauty.sc` was itself put on hold shortly after (see above). **This entire ladder is a downstream dependent of the beauty.sc / SB-6 thread and should resume alongside or after it, not independently.**

## RUNGS — IR-BB: native BB-graph control-flow + pattern lowering (`SC-1..SC-9`, GOAL-SNOCONE-IR-BB)

⭐ **This is the most recently touched real technical thread in the whole absorbed corpus (2026-07-21) — later than SM (2026-05-12), later than BEAUTY's hold (2026-05-02), later than the demo ladder (2026-04-18).** Goal: route Snocone's `if`/`while`/`for`/`procedure`/pattern-match through native BB-graph lowering (mode-3/4, compiled) rather than the tree-walk interpreter, closing the gap subsystem-by-subsystem against the 14 `beauty-sc` drivers.

**State: 19/20 beauty-suite subsystem tests PASS** (up from 17/20 the same session, from a 2026-04-13 baseline of 3/14). Only `trace` remains, root-caused as a `DATATYPE(DT_SNUL)` divergence (SCRIP returns `"NULL"`, SPITBOL returns `"STRING"`, for an unset/never-assigned variable — a runtime-model difference, not a lowering bug; fix direction is LOWER supplying the per-language unbound-slot default, since the runtime's `bn_type_datatype` is deliberately language-blind).

**Six fixes landed the same session, all oracle-confirmed, zero regressions (SCRIP `2be85ff2`, `src/lower/lower_snobol4.c` +45/−15):** `while`/`do-while`/`for` conditions now route match (`TT_SCAN`) conditions through `IR_GOTO` correctly; `TT_GOTO_U/S/F` added to lowering (previously a FATAL "tree kind 127"); indirect capture targets (`PAT . $'$B'`) now lower via a new `sno_capt_name()`; match-and-replace via a bare `TT_SEQ` LHS (`junk ch = ;`) now synthesizes the implied scan; deferred pattern-primitive arguments (`BREAK(*expr)`) now lower for pre-statement-valued args (mid-match-capture-dependent args remain a further rung).

**⛔ Next rung, as last recorded — two root causes on the SNO$MKPAT / pattern-value path, neither fixed:**
1. **Segment-boundary reset.** `sm_preamble` lowers each `.sc` file as a separate segment; `lower_sno_stage2` resets `g_sno_npat=0` on every call, so the pattern-blob counter (`PAT$n`) restarts per segment and collides across segments — "SNO$MKPAT: compiled pattern blob 'PAT$N' not registered" ×65 + segfault. Fix direction: don't reset `g_sno_npat` on non-first segments, or lower the merged program once.
2. **Indirect pattern-var targets** (`$'  ' = White`). The parser defines patterns under indirect names (`subj->t == TT_INDIRECT`), bypassing the MKPAT-collection path (which only guards `subj->t == TT_VAR`) — same shape as the indirect-capture fix already landed, not yet extended here.

**Earlier session's findings, still load-bearing context:** the entire parser chain "lowers clean" (every FATAL gone) as of this session — the transpile backend (`--transpile` → SPITBOL text) got three control-flow stubs closed (`do`-`while`, augmented-assign, `switch`-as-chain) the same week; and a coverage-climb session against the oracle found `LEQ` means lexically-EQUAL (not less-or-equal), comparison predicates return null string on success not a boolean, `@var` cursor-capture is unary-only, and SPITBOL identifiers cannot start with `_` (relevant to any transpiled-output naming).

**Three still-open gaps flagged for a future session (ranked, from a coverage-climb session):** (1) OPSYN-slot binary-operator tokens (`~ & @ # %`) are lexically defined but have no grammar productions — parses fail on any use-site, breaking the `ARCH-LANGUAGES.md` "superset of SPITBOL" guarantee; (2) `--transpile` mis-parenthesizes `subj ? pat = repl` inside an `if`/`while` condition, producing SPITBOL `ERROR 212` + a segfault; (3) `ARCH-LANGUAGES.md` documents a `function f(a) local1, local2 {` locals-after-`)` syntax with no matching grammar production — the same locals-syntax gap as SB-6.E.7-L above, documented independently here.

## RUNGS — PARSER-SC: self-hosted Snocone parser (`PARSER-SC-0..10`) — ✅ COMPLETE (2026-05-07)

A Snocone program (`parser_snocone.sc`, now at `SCRIP/bootstrap/parser_snocone.sc`) that parses Snocone source via one `Compiland` PATTERN — sharing `tree.sc`/`stack.sc`/`counter.sc`/`ShiftReduce.sc`/`semantic.sc`/`gen.sc`/`tdump.sc` with the five sibling self-hosted parsers (SNOBOL4, Icon, Prolog, Raku, Rebus) — and producing an IR tree `tree_equal` to the existing C frontend's, executing identically.

**All ten rungs closed:** atom/assignment (SC-0/1), infra cleanups to the cross-`PARSER-*` style guide (SC-INFRA-1/2/3 — no leading `_`, no single-statement `{}` bodies, 120-char dividers, `~`/`&` OPSYN shorthand over longhand `shift`/`reduce`, `$' '`/`$'  '` whitespace-token aliases defined once), control flow (SC-3), function def+call (SC-4), pattern-match `expr ? pat` (SC-5), the full `beauty.sc` crosscheck (SC-6, in four sub-passes — see below), augmented assign (SC-7), `break`/`continue` with loop-label stacks (SC-8), `struct` (SC-9), `switch`/`case`/`default` (SC-10). Final: **PASS=67 FAIL=0, `beauty.sc` 1148/1148 statements byte-identical to the oracle.**

**Worth preserving: two real scrip pattern-engine bugs found and fixed along the way**, since they're the kind of subtle engine-level defect likely to recur elsewhere:
- **The `$'  ' = *White` alias regression.** A whitespace alias assigned via `*White` (deferred value-context evaluation) stores a frozen-`EXPR_t` (`DT_E`) descriptor; when later referenced in PATTERN context via `E_INDIRECT`, both the SM path (`SM_PAT_DEREF`, `sm_interp.c`) and the IR tree-walk path (`interp_eval_pat`, `eval_pat.c`) had no `DT_E` case and fell through to a degenerate empty-name pattern reference, breaking recursive grammar rules built on the alias. Fixed on both paths by adding a `DT_E` branch that thaws the frozen expression via `PATVAL_fn`/`EVAL_fn`, mirroring the existing `case E_VAR` handling.
- **The Compiland off-by-one / stranded-first-statement bug.** `(E_Parse & 'nTop()')` embeds `nTop()` as a string inside an `EVAL`'d deferred pattern, called at match time by `Reduce(t, n)`. On large, structurally complex input (full `beauty.sc`, 1148 statements) this returned a count one short, stranding the FIRST-pushed (oldest) statement on the tree stack while popping everything above it. Root cause of the miscount was never fully pinned down; the fix sidesteps it entirely — `reduce_prim(E_Parse)` calls `TopCounter()` as its literal first operation inside the match-time action, before any stack pops, eliminating the binding-time ambiguity. A companion, harder bug (heap corruption / SIGSEGV at intermediate input sizes) turned out to be unrelated: `cache_get_fresh` shallow-copied `arbno_t`'s heap-allocated `stack` pointer, so a `realloc()` inside one cached copy's growth left a sibling copy's pointer dangling — fixed with a deep copy, plus unifying two previously-inconsistent local `arbno_t`/`arbno_frame_t` definitions into one canonical `bb_box.h` version.

## RUNGS — PST-SC: parser_snocone.sc as a pure syntax tree (Phase 2) — BLOCKED

Sibling effort to `PARSER-SC` above but scoped differently: rewrite `parser_snocone.sc` (then 934 LOC) to use ONLY the "pure" primitive set (`shift`/`reduce`/`nPush`/`nInc`/`nPop`/`nTop`/`assign`), deleting ~110 hand-rolled helper functions the original port had accreted (label synthesis, body-collection state machines, leaf pushers, etc.) down to ~350 LOC of grammar + driver only. **Phase 1 (the C parser it mirrors) is clean and DONE (verified 2026-05-19).** Phase 2's mechanical deletion+rewrite steps (SC-SC-1..5) are all marked done in the source, but the file's own trailing state notes leave a real, unresolved blocker:

**`bb_deferred_var`'s variable-stored-pattern path is broken.** `Id = ANY(&UCASE &LCASE '_')` then `*Id` used inside another pattern resolves through `PATND_t` (the pattern-node type, `XKIND_t`-tagged) rather than the compiler's `IR_t`, but `bb_build_brokered()` (the function `bb_deferred_var` calls) only understands `IR_t*`. `PATND_t.kind` and `IR_t.t` are both first-field ints but their enum VALUES don't align (`XDSAR=21` maps onto `IR_REPEAT=21`), so every `PATND_t` kind falls to `emit_flat_ir()`'s `default: → fail`. **Recommended fix (self-contained in the runtime layer, not requiring IR changes):** add a `bb_build_patnd(PATND_t*)` switching on `XKIND_t` to the same `emit_bb_x*` primitives `bb_build_brokered` already uses for `IR_t`, and replace the direct `(PATND_t*)` casts in `stmt_exec.c`'s `bb_deferred_var` with calls to it. Never attempted; blocks any further Phase 2 progress since every variable-stored-pattern test (the bulk of what would exercise the rewritten grammar) fails at this one function.

## RUNGS — switch backends: chain vs. table lowering (`SW-1..SW-8`, GOAL-SNOCONE-SWITCH-BACKENDS) — NOT STARTED

Motivated by the IR-BB and BEAUTY threads both independently hitting the same wall: Snocone's `switch` compiles to a linear `IDENT` chain (O(n)) today, and the `beauty.sc` port's `pp()`/`ss()` polymorphic-dispatch functions (30+ string-keyed cases each) are exactly the shape that wants O(1) dispatch instead. Spec exists in `ARCH-LANGUAGES.md ## Switch backends` (not absorbed here — still the live spec). Selection is three-layer (per-switch `[[table]]`/`[[chain]]` attribute → file-level `#pragma snocone switch=...` → driver `--switch-style=chain|table|auto`), `auto` picks `table` at ≥5 same-type-literal cases. Zero of the eight rungs (frontend attribute/pragma/flag parsing, the actual perfect-hash table lowering, the `auto` heuristic, per-backend ports for the dormant JVM/.NET/JS/WASM backends, the benchmark fixture, and applying it to `beauty.sc::pp()`/`ss()` to close both the IR-BB `trace` gap's sibling dispatch bugs and the perf question at once) have been started. This is real, well-specified, low-risk feature work sitting idle behind the higher-priority restoration tier.

---

## LEDGER — RETIRED NAMES + per-file disposition (full text of all eleven: `.github` git history at `70c6b7f3` and before)

- **`GOAL-LANG-SNOCONE.md`** — the original demo ladder (D-1..D-4: porter/claws5/treebank-list/treebank-array) + the case-sensitivity-is-a-frontend-concern architectural fix (SCRIP `5535b229`). Declared "GOAL COMPLETE" 2026-04-16, then reopened real correctness bugs in `porter.sc` two days later in the same file (unresolved at absorption) — see the demo-ladder section above.
- **`GOAL-PARSER-SNOCONE.md`** — the self-hosted `PARSER-SC` ladder. ✅ Genuinely complete: PASS=67, `beauty.sc` 1148/1148 byte round-trip, 2026-05-07.
- **`GOAL-PST-SNOCONE.md`** — Phase 1 (C parser) complete; Phase 2 (pure-primitive `parser_snocone.sc` rewrite) blocked on `bb_build_patnd` (`PATND_t`/`IR_t` enum-value collision). See PST-SC section above.
- **`GOAL-SNOCONE-BEAUTY.md`** — `beauty.sc` self-host (`SB-*`). Put ON HOLD by Lon 2026-05-02; **superseded 2026-08-24 onward by the live restoration tier** — see FRONT STATUS.
- **`GOAL-SNOCONE-CLAWS5.md`** — `claws5.sc`'s "SC-26" capture-call tracking thread. Its own diagnosis (three sub-bugs, two fixed) was never reconciled with `GOAL-LANG-SNOCONE.md`'s later finding that the SC-26 hypothesis itself was wrong — see the demo-ladder conflict note above.
- **`GOAL-SNOCONE-DEMOS.md`** — the earliest-stage treebank/claws5 tracking (`SD-*`). Fully superseded by the CLAWS5/TREEBANK-LIST/LANG-SNOCONE threads; kept only for provenance.
- **`GOAL-SNOCONE-IN-SNOCONE.md`** — "the Snocone compiler written in Snocone" (`SS-*`). Architecture + IR-emission census done; implementation (`SS-2` onward) on hold as a declared dependent of `beauty.sc` landing.
- **`GOAL-SNOCONE-IR-BB.md`** — native BB-graph control-flow + pattern lowering (`SC-1..9`). **The most recently touched real technical work in the whole corpus** (2026-07-21); 19/20 beauty subsystems; next rung is the SNO$MKPAT segment-boundary + indirect-pattern-var pair. See section above.
- **`GOAL-SNOCONE-SM.md`** — self-hosted `lower.sc`/`sm_interp.sc` (`SL-*`/`SI-*`). Stalled at SL-13d since 2026-05-12; not reopened since. `lower.sc`/`lower_driver.sc`'s current location is unverified — see staleness hazard #2.
- **`GOAL-SNOCONE-SWITCH-BACKENDS.md`** — chain-vs-table `switch` lowering (`SW-1..8`). Fully speculative; zero rungs started.
- **`GOAL-SNOCONE-TREEBANK-LIST.md`** — `treebank-list.sc`'s "SC-26" tracking thread, sibling of the CLAWS5 file above; same unreconciled-supersession note applies.
- **`GOAL-SM-LOWER-REFACTOR.md`** — added 2026-08-29 (a different consolidation pass, `goal-files-major-consolidation`, not the original 2026-08-27 Snocone mint). The shared C `lower.c` refactor prerequisite for this file's own SM-LOWER ladder. Fully closed, 28/28 rungs. See § RUNGS — SM self-host above.
