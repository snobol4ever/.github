# SRC DEEP SCAN — 2026-08-23 s268 (CEO seat, Lon's charter)

**Charter (Lon, in-chat, CEO-9 + this session):** deep scan of ALL src/ C/CPP/H → propose a proper folder structure and a proper build system for the scrip executable. Announcement (2026-08-24 target) precedes execution; Lon rules timing. **Added this session (Lon, verbatim in substance):** *"One big task: identifying the major #define switches that govern compile-time behavior. For instance the four ZETA modes — there is just ONE now. Delete all the combinations, remove the guards, leave all the proper raw code. We have too many switches, all for a history to get to where we are today. It is passover cleaning time."*

**Binding constraints on any proposal (CEO-9):** TEMPLATE-ONLY EMISSION · BOTH-MEDIUM · no-lang-past-lower · PEERS · zero-blank-lines C style · m3≡m4 invariant · per-checkout objdir (HQ-27) · flag-keyed build cache · gates stay green through any move · script paths (149 scripts reference src/) · announcement timing.

**Method:** 12 read-everything subsystem agents (each read its area IN FULL) + CEO mechanical censuses + a 13th switch-census agent (report lands as 13-switches.md). Reports 01–12 are single-instrument agent output — **verify before carving** (that is hq_C's task).

## Reports
01 parser snobol4+snocone · 02 lower · 03 parser raku+rebus+pascal · 04 templates bb_[m-z] · 05 emitter+optimizer · 06 parser icon+prolog · 07 templates xa_*+x86_asm.h · 08 templates bb_[a-l] · 09 contracts+driver+machine+include+tools · 10 runtime top · 11 backends · 12 runtime core/builtins/rt/rtx · 13 switches (pending)

## CEO mechanical censuses (measured this session)
- Tree: 381 C/C++/H files, 98,507 lines (src/ minus backends); + 17 hand .S; backends 40 files ~26.6k; 107 headers.
- Build: Makefile RT_PIC_SRCS = explicit 262-file list, ALL compiled -fPIC into libscrip_rt-<tag>.so; scrip binary = driver/scrip.c alone linked against it. Zero object-basename collisions today, but objdir flattens with $(notdir) — reorg must keep uniqueness or mirror the tree.
- **30 on-disk sources outside the build**: scrip.c/csnobol4_shim.c/sync_monitor.c (other rules) + 27 carve candidates: contracts/ast_verify.c, driver/rs23_diag.c, lower/lower_snobol4.gz5-parked-*.c, optimizer/arith_fold.c, parser/icon/{icn_main,icon_lex_test,icon_parse_test}.c, parser/prolog/ 8 test files, parser/rebus/rebus_main.c, parser/snobol4/test_lex.c, rtx/ 6 test/bench/shim files, tools/ 4 files. (+2 .bak in parser/prolog.)
- **Orphan headers (CEO grep: zero includers found)**: runtime/builtins/box_rt.h (independently confirmed by report 12), parser/prolog/pl_resolve.h (confirmed by 06), parser/snobol4/unicode_alpha_ranges.h 661 lines (confirmed by 01), and templates/bb_common.h (~40 Prolog rt prototypes per report 08 — orphan status is CEO-grep-only, ⚠ RECONCILE: hq_C verify before carving).
- **Scripts↔src coupling**: 149 scripts reference src/ paths (dir-level refs: runtime 102, templates 92, parser 88, backends 70, emitter 37, driver 22, contracts 20, lower 17). **18 scripts name 51 src/ paths that no longer exist** (fossil refs incl. two mandatory handoff-regen scripts + 6 test_gate_* — spot-check: some fossils are comments; test_gate_bb_one_box.sh carries deleted bb_alt.cpp in its live list). Gates need re-proof they can still fail.
- Makefile: `make test`/`test-ir`/`test-all` are recipe-less .PHONY false-greens. Makefile:34,361 comments still carry the RETIRED O0-DEV-O2-BENCH text (superseded by RULES.md § NO -O2 BUILDS, s262).

## Headline cross-report findings (synthesis pending hq_C verification + hq arbitration)
1. **~2,000+ lines of grep-verified dead code inside live files** on top of the 27 unbuilt files: bb_match_arbno ~306/426 lines dead under a fn named _DELETED_ARMS (04); driver_call.c ~240-line unreachable AST interpreter gated by permanently-NULL g_exec_prog, shipped in every .so (09); 15 of 18 xa_dispatch arms + ~13 xa_ files unreachable (07); emit_str.cpp JVM/NET half + bomb_bytes (the documented "sole legacy exception" is itself dead) (05); unification.c meta-interpreter scaffold (10); pat_pool 4MB RWX + runtime_shim.h + box_rt.h + resolution.c jmp_buf CP stack (12); prolog dead prototype families (06).
2. **Misplaced-code map**: scrip_cc.h (cross-language contract under parser/snobol4, ≥25 external includers) → contracts/ (01); re.c NFA engine → runtime (03); tree_to_sno.c + binop_apply out of lower (02); pl_cell/pl_area/pl_cell_conv.h + icon_runtime.c out of parser (06); sil_macros.h out of emitter (05,12); is_global/global_register compile-time code out of runtime (10); aggregates.c → rt/, rt_gram_trampoline.S → rtx/ (10); machine/sm_prog.c → driver-side (09); dissolve src/include/ (09); bb_pat_build.cpp stays in rt/ but rename rt_pat_jit.cpp (12).
3. **Rule-violation hot spots**: scrip.c reads language-identity flags 42× past lower (09); xa_flat.cpp dead raw-byte cluster (07); zeta_storage.h/zeta_depth.h included by NOBODY — 12+ consumers hand-extern (09); core.h/rt.h declare functions after #endif (12); csnobol4_shim.c hardcodes /home/claude (portable-home breach) (09); core.c includes ../../../scripts/ (12); unconditional SEQ fprintf on hot N-stack path (12). Templates themselves are CLEAN (zero MEDIUM_*, zero raw-byte, zero lang branches across all 131 bb_ files — 04,08).
4. **backends/ = ~26k LOC, zero live consumers, driver/net+jvm don't compile, zero overlap with sibling snobol4jvm/snobol4dotnet repos** → move out of src/, strip xa_prologue/xa_epilogue PLATFORM_* remnants + run-jvm/run-net targets (11).
5. **DRY families for the reorg**: cset-dispatch block ×5 in match boxes (04); ISO term-compare duplicated unification.c↔rt_runtime.c (10); two parallel Prolog parsers, DCG clauses parsed twice (06); pascal.y 89-global symbol table (03); by_name_dispatch.c two god-functions 2355+1851 lines → split per frontend (10); emit.cpp 3-way split + emit.h 4-way split (05); x86_asm.h 3-way split under umbrella (07); rt.c/rt.h Prolog split (12).
6. **Generated parsers**: snobol4 in sync; snocone PROVABLY STALE vs its .y; rebus flex output alone named lex.rebus.c; dead divergent regen path (snobol4/Makefile + build_snobol4_frontend.sh); dead rebus CLI set (01,03).
7. **Landmines (not dead, worse)**: eval_node/eval_ast_pat abort-stubs with live callers; ir_call_proc [NO-IR-INTERP] silently no-ops from 6 Raku dispatch sites; sm_call_proc/invocation.c SM-residue suspect needing trace (10,12).

## HQ assignments (dispatched s268 via postoffice)
- **hq_C**: adversarial verification of DEAD/carve claims; zeta/switch collapse safety proof; source-layout proposal draft.
- **hq_P**: build-system proposal (RT list model, .so boundary, objdir, test wiring, regen policy, cache preservation); switch collapse build-side; scripts sweep plan.
CEO arbitrates the two drafts into one proposal for Lon. Nothing moves before Lon rules.
