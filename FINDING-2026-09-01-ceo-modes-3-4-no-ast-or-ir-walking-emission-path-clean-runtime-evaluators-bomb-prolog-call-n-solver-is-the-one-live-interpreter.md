# FINDING 2026-09-01 (ceo, on Lon's in-chat order ~20:35: *"ensure that no AST or IR walking occurs whatsoever in mode 3 and mode 4. Go check and report back which is the case."*)

**Tree:** SCRIP `6ede7c31`, `make pristine`, `RT_OPT=-O0`, this root (`/home/claude`), 2026-09-01 20:30–20:45. Law audited: RULES.md:82 **NO AST WALKING IN MODES 2/3/4** and RULES.md:84 **NO SM/BB WALKING AT RUNTIME IN MODES 3/4**. Sweep by a read-only agent over `src/`, every non-obvious claim re-verified by ceo by hand; runtime behaviour measured with gdb witnesses, not inferred.

## VERDICT, in four layers

1. **Emission path (emitter · templates · optimizer): AST-FREE.** `tree_t` (the AST node, `src/ir/ast.h:112-121`) appears in `src/emitter` once as a forward declaration (`emit.h:97`), in `src/templates` 0 times, in `src/optimizer` 0 times; `->c[` / `->t` on AST pointers: 0 / 0 / 0 in those three trees (the `->n`/`->v[` at `emit.h:145-149` are `pl_gz_callee_vec_t`, not AST). Lowering is the last read of the AST. Law 82 holds — **by construction, not by enforcement** (no gate greps for it; see layer 4).
2. **Runtime interpretation of AST / IR / SM / BB in modes 3 and 4: NONE EXISTS.** `sm_interp_run`: 0 hits under `src/` (`grep -rn sm_interp src/`). Every AST/SM evaluator symbol still exported by `out/libscrip_rt.so` is a refusal, not a walker: `eval_ast` = stub printing `[NO-AST]` (`src/driver/driver_ast_stubs.c:6`) · `eval_ast_pat` = `abort()` "AST-walk evaluator deleted" (`src/runtime/pattern_match.c:614`) · `eval_node` = `abort()` (`src/runtime/runtime_eval.c:296`) · `exec_stmt` = `abort()` "legacy executor deleted (B0)" (`src/runtime/core/stmt_exec.c:23`) · `ir_call_proc` prints `[NO-IR-INTERP]` (`src/runtime/rt_runtime.c:404`). The driver's only run path in mode 3 is `emit_chain(...)` → `rt_outer_call(fn)` (`src/driver/scrip.c:1993-2001`); the two "refusing to silently run the SM interpreter" branches (`scrip.c:2013,2017`) `abort()` and name an interpreter that no longer exists. The hand-written runtime asm (`src/runtime/rtx/*.s`, `rt.c`) references no `SM_*` symbol (0 hits). Icon lowering writes 0 SM opcodes. **Law 84 holds; its exception clause ("via `sm_interp_run` until `bb_pl_*.cpp` lands") is moot — the thing it excepts is gone** (law cleanup owed next session; one law change already spent this session).
3. **What DOES run at run time that a reader could mistake for walking — three classes, reported honestly:**
   - **Runtime COMPILATION (legal by the thesis, not walking):** `libscrip_rt.so` bundles the whole back end (`Makefile:105-330` RT_PIC_SRCS: `emit.cpp`, ~140 `bb_*.cpp`, `optimizer/`, the lowerers, the SNOBOL4 parser). `EVAL`, `CODE` and runtime-built pattern values are **compiled to x86 blobs and jumped into**: witness `X = EVAL('1 + 2')` → gdb shows `emit_chain` entered a second time at run time (prefix `pat_flat`), no evaluator hit, prints `3`. Pattern values built at run time are JIT-compiled **once at construction** (`pattern_match.c:63-107` → `bb_pat_build.cpp:84-110` → `optimizer_run` → `emit_chain`); no per-match-step walk of any pattern structure.
   - **Runtime dispatch by NAME (not AST/IR; already rowed — P6):** 4 emitted call sites reach `try_call_builtin_by_name_bl` (`by_name_dispatch.c:5456`), which still resolves by `memcmp`/`strcmp` (`:5534`, `:5560+`). Witness: every `EVAL`/`CONVERT` call in the programs above hit it.
   - ⛔ **ONE LIVE RUNTIME INTERPRETER, over runtime DATA (out of the law's letter, inside its spirit): Prolog `call/N`.** `rt_pl_call_gen` (`by_name_dispatch.c:4752`) builds a solver tree from the goal term by `strcmp` on functor names (`","`, `";"`, `"->"`, `"\\+"`, `once`, `ignore`; `:4508-4557`) and drives it with `plc_det_exec`'s `switch (s->op)` (`:4493`) / `plc_next` (`:4570`). Witness `G = write(hi), call(G)` → gdb: `rt_pl_call_gen` → `plc_det_exec` → `hi`. This walks runtime-constructed goal terms, not compiler IR or AST — but it is a dispatch loop over a tree at run time, the shape the thesis replaces with wiring. The unknown-functor→user-predicate failures (C10) live in this path. **Lon's call (STRATEGY):** rule whether `call/N` goals go through the runtime compiler (EVAL's precedent: build the graph, `emit_chain`, jump) or the solver stays as a sanctioned runtime service.
4. **Dead AST code still linked into the runtime, and one armed-but-dead deferred walk (rows owed, hq_C):** `scope_patch` (`src/runtime/name_binding.c:93-111`, a recursive `tree_t` walk, zero callers) · `static_get`/`static_set` (`:45,58`, zero callers) · the AST statement executor `src/driver/driver_call.c:139-380` over `g_exec_prog`, which is `NULL` at `driver_ast_stubs.c:4` and **never assigned anywhere** · four `tree_t*` retained past lowering (`src/ir/stage2.h:15,22,44-45`) · ⛔ `compile_to_expression()` (`pattern_match.c:589-596`) stores a `tree_t*` into a **runtime DESCR cell** (`DT_E`) from `CONVERT(x,'EXPRESSION')` (`core.c:1226-1229`); its only consumer bombs. **Measured consequence:** `E = CONVERT('1 + 2','EXPRESSION')` / `OUTPUT = EVAL(E)` prints `snobol4:1: error: parse error: syntax error` at RUN TIME and exits **rc=0 with no output** in BOTH modes; `sbl -bf` prints `3` — a silent wrong answer, not a walk.

## Gates (layer 4 evidence)
- `test_gate_sm_dead.sh`: ran on this build — `SM-execution-surface live references: 0 (MAX 1)`, rc=0. It counts only `sm_interp_run(`/`sm_run_native(`/`g_vstack[`; **not wired into `make test`** (`Makefile:75-81` runs six scripts; this is not one).
- `test_gate_emit_no_lang.sh`: rc=0 (`LANG-BLIND`) — polices language identity, **not** AST fields. **No gate anywhere greps `tree_t`/`->t`/`->c[`/`->n`/`->v` in `src/emitter`, `src/templates`, `src/optimizer`.**
- ⛔ `test_gate_lower_isolation.sh` and `test_gate_runtime_isolation.sh`: **rc=1 with ZERO lines of output** — they die under `set -euo pipefail` because their pattern says `parser/` and the tree says `parsers/` (`grep -c 'include.*parser/'` = 0 → the pipeline fails). Real populations today: 5 files under `src/lower/` and 14 under `src/runtime/` `#include` a `parsers/` header. An instrument that dies silently is the false-FAIL class (RULES § THE INSTRUMENT LAWS).
- RULES.md:88 cites a `--dump-sm` flag that does not exist (`grep -c dump-sm src/driver/scrip.c` = 0); `test_sm_generator_ch14.sh` and `test_self_host_smoke.sh` build a deleted `src/runtime/sm/sm_interp_test.c`.

## Witness programs (all in `/tmp/…/scratchpad/wit/`, breakpoints on `scope_patch exec_stmt eval_ast eval_ast_pat eval_node plc_det_exec rt_pl_call_gen try_call_builtin_by_name_bl emit_chain`)
| program | breakpoints hit, in order | output / rc |
|---|---|---|
| `OUTPUT = 'plain'` | `emit_chain` (compile) only | `plain`, exit normal |
| `X = EVAL('1 + 2')` | `emit_chain` · `try_call_builtin_by_name_bl("EVAL")` · `emit_chain` (runtime compile) | `3` |
| `CONVERT(…,'EXPRESSION')` + `EVAL(E)` | `emit_chain` · by-name `CONVERT` · by-name `EVAL` | runtime parse error, **rc=0, no output** (sbl: `3`) |
| `G = write(hi), call(G)` (`.pl`) | `emit_chain` ×3 (compile, incl. `proc_$call/1,/2`) · `rt_pl_call_gen` · `plc_det_exec` | `hi` |

## Rows owed (minted by the HQs, not the ceo)
hq_C: (1) delete the dead AST walkers and their stubs from the runtime library (`scope_patch`, `static_get/set`, `driver_call.c:139-380` + `g_exec_prog`; keep or drop the bombs with their last callers) — DONE-WHEN: `nm -D out/libscrip_rt.so` exports none of `eval_ast eval_ast_pat eval_node exec_stmt scope_patch`; (2) `CONVERT(x,'EXPRESSION')` compiles at CONVERT time through EVAL's path and never stores a `tree_t*` in a cell; a runtime parse error is a red exit, never rc=0 — witness above must print `3` in both modes. hq_B: (3) a gate that greps `tree_t|->t\b|->c\[|->n\b|->v\b` in `src/emitter src/templates src/optimizer` (0 today, proven red on a scratch insertion) and `test_gate_sm_dead.sh` wired into `make test`; (4) the two isolation gates refuse loudly (rc=2 + message) instead of dying silently, then grade the real 5/14 populations against a law decision; (5) doc rot: RULES.md:88 `--dump-sm`, the two scripts naming `sm_interp_test.c`. Lon: the `call/N` ruling (layer 3).
