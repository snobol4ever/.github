# FINDING — CORRECTED IN PLACE: the picker's state-blindness was already fixed upstream (`3f70b073`/`a0d65eed`) before this session started; I was working from a 19-commit-stale clone and nearly shipped a redundant, inferior duplicate

**Session:** 2026-08-23 seat05 (`/home/claude05`, Claude Sonnet 5), THE LOOP queue row `161-o2-red` (rank 0, claimed via `next`). This FINDING was rewritten mid-session after discovering the correction below — the original version claimed credit for a fix that already existed on `origin/main`. Left as a corrected record rather than deleted, per this fleet's own "don't erase, correct in place" convention (STALE-ORIENTATION handling elsewhere in project history).

## WHAT ACTUALLY HAPPENED, IN ORDER

1. `next` locked `161-o2-red` for me at `rank 0`. The baton's header says `state: PARKED (Lon s258)`; its ledger is Lon's verbatim ruling to stop chasing the O1/O2 defect (C runtime slated for ASM replacement, sunk cost). Per `CLAUDE.md` §THE LOOP item 6 (Lon's word overrides the brief), I did not resume that chase.
2. Reading the baton's own ledger showed `seat08` hit this exact mis-serve earlier today and filed `FINDING-2026-08-23-seat08-parked-row-served-as-rank0-free-no-unclaim-verb.md`, documenting that `s4e_msg.sh`'s picker (PASS 3 of `next`) skipped a row on claim-file existence only, never on `state` — so a `PARKED` row with no live claim dispatched exactly like a `FREE` one.
3. I independently re-derived the same root cause by reading `scripts/s4e_msg.sh` directly, and **hand-edited PASS 3 in my local checkout** to add a `state == FREE` allow-list check, released my (mis-served) claim, and wrote a first version of this FINDING claiming the fix.
4. Before pushing, `hq_C` messaged me directly: `161-o2-red is PARKED BY LON (s258, reaffirmed s261 verbatim...) ... Release the claim (s4e_msg.sh unclaim 161-o2-red 'parked by Lon')`. That message named a verb, `unclaim`, that did not exist in my checkout. Rather than assume hq_C was mistaken, I checked `origin/main` directly.
5. **`origin/main` was 19 commits ahead of my clone.** Commit `3f70b073` ("s4e_msg: unclaim + park verbs, a load-bearing state column, and a protocol-staleness refusal", authored earlier the same day, 13:49:05 -0500) had already: added `unclaim` (release a claim you never should have taken, refuses on DONE or not-yours, appends the release to the baton LEDGER); added `park` (writes the `state` column and appends a ledger line — this is what produced the `STATE -> PARKED` lines already visible in the baton, including one hq_C wrote at 20:14Z **while I was mid-edit**, i.e. hq_C's own checkout was current and active on this exact row concurrently); fixed PASS 3 to skip any row whose state isn't `FREE`/empty; and **added a `PROTOCOL-VERSION`-gated refusal so that `next` on a stale clone now refuses to dispatch at all**, specifically because this same defect class (a stale script silently reverting to worse dispatch behavior with no warning) had already bitten two other seats that day. `a0d65eed` refined `park` further (won't clobber another seat's live RUNNING claim).
6. My local fix was **strictly worse** than the shipped one in one concrete way: I wrote `[ "$state" = "FREE" ]` (strict equality only); upstream wrote `case "$step" in FREE|'') ;; *) continue;; esac` (also accepts empty state, for backward compatibility with any row never migrated to carry a state value). Shipping mine over origin's would have been a regression, not a fix.
7. **Discarded my local edit** (`git checkout -- scripts/s4e_msg.sh`, uncommitted, nothing lost) and ran `git pull --rebase origin main` (fast-forward, no conflicts — 41 files, well beyond just this script; unrelated work from other seats/HQs landed in the same 19 commits).

## THE ACTUAL ROOT CAUSE (for the record, since it's still worth stating precisely)

`s4e_msg.sh next`'s PASS 3 (rank-sorted free-row picker) read `QUEUE.tsv`'s 4 fields (`rank·topic·owner·state`, V2-2 format) into loop variables but — before `3f70b073` — only ever tested claim-file existence, never `state`. A `PARKED` row with no live claim was mechanically indistinguishable from `FREE`. `161-o2-red` sat at `rank 0`, so it pre-empted every genuine `FREE` rank-0 row (`expr-define-alpha-block-missing`, `zeta-frame-rsp-capture-home`, `zeta-frame-rsp-second-wild-write`, `zeta-cell-heap-segv`, `free-r11`, `json-alternate-af-spin`, confirmed present in `QUEUE.tsv` at the time) for any seat whose claim on it had lapsed. `3f70b073` closes it with an allow-list (`FREE`/empty dispatches; anything else — including any future state value — is skipped), plus the `park`/`unclaim` verbs seat08's FINDING had identified as the missing lifecycle tools, plus the protocol-version refusal that stops a stale clone from ever running the old, unsafe picker silently.

## WHAT I ACTUALLY CONTRIBUTED

- Independent convergent confirmation of seat08's root-cause read (for whatever that's worth — it wasn't needed; the fix already existed).
- Caught my own staleness before pushing a regression, by taking hq_C's message at face value enough to check `origin/main` rather than assuming my local script was authoritative.
- This corrected FINDING, so a future reader doesn't trust the original (wrong) claim.
- Did **not** resume the O1/O2 chase at any point — that ruling was never in question.

## LESSON, STATED PLAINLY SINCE THIS PROJECT'S CULTURE ASKS FOR IT

`git fetch && git diff HEAD origin/main` before editing shared control-plane tooling (`s4e_msg.sh`, `QUEUE.tsv`, anything under `postoffice/`) would have shown this in one command, before any local edit. I read the script's *content* carefully and correctly, but never checked whether that content was *current*, on infrastructure that changes under active multi-seat editing within the same hour. `3f70b073`'s own justification #3 (protocol-staleness refusal) exists precisely because "did I read the code right" and "is the code I read the code that's actually running fleet-wide" are different questions — and my clone answers the second one silently and wrong unless asked.

## OUTCOME

No source change from this FINDING. `161-o2-red` claim already released cleanly pre-pull (archived at `/home/resources/postoffice/claims.released.seat05-161-o2-red-picker-fix-20260823/161-o2-red.claim`); nothing further needed there. Correction message sent to hq_C. Proceeding to real rank-0 `FREE` work via a fresh `next` on the now-current script.
