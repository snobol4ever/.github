# FINDING 2026-08-09h (Fable) — the bench tail is THREE defects, not one: MB-CARVE authority landed, the DEFER hit-store environ smash gated, and the CAS pop was arithmetic against a register C cannot move

Repos at open: SCRIP `26bdc332`, corpus `b29acfe1`, .github `8c5377c2`. 09g's prescribed rung executed; its **17→21 prediction is FALSIFIED** — the four SEGVs were three mechanisms, and the leak was masking the other two. Bench moves **17 → 19 of 22**.

## 1. MB-CARVE (09g §5, LANDED as prescribed)
ONE authority in emit.cpp beside zd_plan: `sn4_mb_stfh()` (predicate, formerly twin-spelled in bb_match_begin/end) + `sn4_mb_carve_k()` (64 iff stfh fires without `flat_stmt_frame`). Consumers: (a) `zdh_match += carve_k` at the zd_plan record site — STATEMENT_END's γ release now frees pre-head producers + the head's carve (witness asm: `add rsp,32` → the model was releasing to the pre-carve depth while the mark restores land post-carve); (b) bb_match_begin's anchor-exhaust ω tail gets own-K `x86_zrelease` AFTER the old_rbp restore (the HKQ zone must survive the `rt_match_ctx_restore` C call — restoring rsp earlier would put the zone below rsp for that call); (c) `zd_k` stays 0 — the depth model's interior zread staging deliberately untouched (carve invisible between α and the mark in model AND staged reads, as before). **Gates:** witness `zleak_matchbegin_stfh` rc=0 at 3M BOTH modes (was rc=139); `SCRIP_OS_CAP=0` byte-identical ×3 — witness/pattern_bt exact, roman modulo ONE `movabs` host address that shifted a page because the compiler .so grew (setarch -R determinism proven; the sanctioned RO-load form is rebuild-variant by nature).

## 2. SECOND DEFECT — the DEFER/VALUE γ-landing g_scan_hit_start store smashes environ (pre-existing, leak-masked)
pattern_bt at **N=10** still SEGV'd — structural, not accumulative — and under `SCRIP_OS_CAP=0` too, exonerating fix 1 and convicting `26bdc332`. gdb: fault inside `__GI_getenv` walking a half-smashed pointer; the store `mov dword [rbp+448], eax` (the .Lx_4 hit landing) against main's GLUE-O pin == **environ[32]'s low dword** (pinned rbp 0x…ea08 + 448 = 0x…ebc8; environ vector at 0x…eac8). `op_scan_head_off` is the HEAD's `FR(op_off)` — the coordinate the stfh arm ABANDONED for HKD. Its SOLE reader is bb_match_replace (emit.h:463's own words), and THE SAME AUTHORITY excludes REPLACE from the stfh arm — so in exactly the class where the home is abandoned, no reader can exist. **Fix:** both writers (bb_match_defer, bb_match_value) gated `!sn4_mb_carve_k()`; suppression and REPLACE-exclusion retire together if REPLACE is ever admitted. pattern_bt: N=10 and full 500k green.

## 3. THIRD DEFECT — the W-1c.3 NO-SCAN pop was arithmetic against a callee-saved register the C pump cannot move
pattern_bt_deep (8M) still died at ~360ms, **insensitive to ulimit -s** — arena, not stack. gdb over 1000 `n3_match_assign_cond_α` hits: **r12 drifts exactly 24 B/iteration**; wall at 0x7ffff3bffff8 after ~700k capture-matches (16.8 MB headroom / 24). The NO-SCAN comment's premise — "pump has consumed every entry … r12 returns at top callee-saved" — is self-refuting: callee-saved means the pump CANNOT move r12, so every COND pend entry above the marker stayed pushed forever. pattern_bt's prior "OK" at 500k was **12 MB of margin, not correctness**. **Fix (regime-independent, all match graphs):** the single `sub r12,24` becomes the tag-0 marker scan (the L(5)/L(9) discipline), landing r12 ON the marker — licensed by the fence law: MATCH_END is the commit, everything above the HEAD's marker is dead post-pump. pattern_bt_deep green at full 8M. **xc318 BY SET both modes IDENTICAL old-vs-new** (stash-cycle diff of FAIL(m3)/FAIL(m4)/DIVERGE sets: empty; m3 284/33 · m4 277/37/3 · DIV=4 both sides) — the global edit is regression-free.

## 4. REMAINING TAIL (routed, not fixed)
`string_pattern` + `roman` = the **REPLACE-class positive-home family** — the exact "pre-existing risk, unchanged" the OS-2 comment recorded when excluding REPLACE. Same getenv/environ signature (gdb on a 2-replacement repro: smash before `c_rt_dcap_end_ok_open`'s `getenv("SCRIP_DCAP_TRACE")`). Slot census for the admission rung: span-start `FR(op_off)` (fix-2's store must REDIRECT not suppress once a reader exists — candidate home: the [rbp-8] HKQ pad dword), end-cursor `FRQ(op_off+24)`, `op_sa` subject mirror, `op_sb` replp, old_rbp `+40` (bb_match_replace:33 → `stfh()?HKQ(0):FRQ`). ⛔ Widening the 64 claim collides with capture δ at `[rbp-64-16k]` (k=1 occupies [-80,-64)) — the region formula at emit.cpp:2404 and caphome must shift together. This is OS-2·SLICE-REPLACE, s31's censused seven-homes rung. ALSO EXPOSED: string_pattern has an m3 HANG at ~500 replacements the arena crash was masking (2-replacement repro is m3-correct); separate defect, uninvestigated. `eval_dynamic` 30s hang both modes: unchanged, 09g's pre-declared separate defect.

## 5. INSTRUMENT CONVICTIONS (mine, this session)
(a) A stash-cycle for the roman killswitch md5 ended `git stash pop` WITHOUT rebuild — the probe suite's "1 REGRESSION" and a 6/6 witness SEGV were measured on the PRE-fix binary; those numbers are VOID (the STALE-BINARY TRAP's stash-pop cousin: **pop is not a build**). (b) First killswitch md5 flagged roman before checking m4 run-to-run determinism — three md5s from ONE binary differ (ASLR-embedded movabs); raw md5 over default m4 text is a FALSE INSTRUMENT for roman-class programs; `setarch -R` + host-addr normalization is the sound spelling.

## 6. WATERMARKS AT CLOSE (this tree, pre-push)
141-probe matrix: m3 **134/17/0/0** · m4 **133/18/0/0** (floor+1 both modes, the +1 = the witness). Bench m4: **OK=19 FAIL=0 CRASH=3 (of 22)**. xc318: m3 284/33 · m4 277/37/3 · DIV=4, BY SET == baseline. Regen ×3 done (benchmark/feature/demo).

## 7. SPITBOL COMPARISON (m4 vs sbl, self-reported ms; ⛔ RT_OPT=O0 per O2-DIRECTED-ONLY — sbl is a production-optimized binary, so every ratio below is SCRIP's O0 floor, not its ceiling)
Faster than SPITBOL: string_concat **14.4×** (10 vs 144) · var_access **3.7×** (417/1557) · arith_loop 3.2× (15/48) · op_dispatch 3.2× (41/132) · arith_int 3.1× (1571/4857) · arith_mixed 3.1× (768/2375) · **pattern_bt 1.9×** (279/526) · **pattern_bt_deep 1.6×** (5104/8196) — the two programs this session unlocked land already-faster at O0. Slower: eval_fixed 0.9× (316/277) · arith_str 0.75× (467/350) · func_call ~0.5× (2005/977; overhead twin 1926/996) · fibonacci 0.37× (514/192) · string_manip 0.23× (2850/658) · table_churn 0.28× (1211/338) · table_access 0.12× (2886/356) · mixed_workload 0.08× (2464/188). Reading: the value-spine/pattern machinery already beats SPITBOL at O0; the deficit is concentrated in exactly LADDER AB's customer (DEFINE call protocol: func_call/fibonacci) and the table runtime (table_access/churn/mixed_workload — an RTX-family target). No -O2 was run (not directed).
