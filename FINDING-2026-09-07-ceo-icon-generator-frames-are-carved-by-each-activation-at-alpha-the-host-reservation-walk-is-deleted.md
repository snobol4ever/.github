# FINDING 2026-09-07 (ceo, MODE CEO → EXECUTIVE) — Icon generator frames are carved by each activation at its α; the host's reservation walk, the 63x multiplier and the depth table are deleted (Lon: "We do not pre-carve. Each BB carves it's own memory.")

Tree: SCRIP `9c6ad224f` (rebased on hq_C's `ef4801066`; `c1e639692` on top carries only the cfo seat-map entries), corpus `ead228cde`. Binary built 08:2x CDT, incremental `make`, RT_OPT=-O0.

## THE CLAIM
An Icon procedure that called generators pre-carved, in its prologue, one region per generator call site, sized by a transitive walk of every callee (`icn_gen_host_slice`, 2026-08-29). Under recursion the walk sums the tree of all possible activations (exponential), was cut by a 63-slot multiplier (66 MB on jtran's `main`), and the caller's walk and the callee's own walk cut a cycle at different depths, so a callee's call sites addressed storage past the region it was handed: a silent wild write (hq_B, 2026-09-06). Lon stopped the show at 07:07 and ordered the cure at 07:2x: nothing is pre-carved; each box carves its own memory.

## THE CURE (N-3)
- The generator's α carves its own frame on the spine below the six entry words: `lea rax,[rsp-carve]` (carve = align16(ft)+48), the header H=R+ft filled as before ([H+0] caller rbp, [H+8] γ, [H+16] ω, [H+24] ANCHOR=rsp0), `rbp:=H`, `rsp:=R`. The call site pushes a placeholder word where the region pointer went, so the entry layout and rsp0=[rsp+48] are unchanged.
- γ leaves rsp where the callee left it and banks it at [H+40]. The caller's γ landing no longer restores rsp: the callee's frame and everything it suspended stay live below the caller, which continues below them. β restores rsp from [H+40] and jumps the resume label at [H+32]. ω pops to the ANCHOR as before. LIFO makes it safe: when a suspended generator is resumed, everything carved after its γ is dead.
- The by-name twin (`rt.c` `rt_genp_spine_enter_n2`) follows the same landing and β and no longer allocas a region.
- Hosts pin rbp (the cto's rung A, `b9cef65ef` local, folded in): the caller's rbp saved at [kt-8], `rbp := frame bottom`, epilogues `lea rsp,[rbp+kt]; mov rbp,[rbp+kt-8]`, the create banks rbp. Frame cells (XK_FR spellings via `x86_fb_num`) are spelled through rbp; spine cells (the `rsp#` regime through `x86_zop`, `x86_zref`, `x86_frame_off`) keep literal rsp with the compile-time slide. That is Lon's rule read from the code: results and locals on the RSP spine unless growth can intervene between γ and β. The first cut that moved spine cells too broke `write("hello")` (five smokes red); confining the pin to the frame prefix cured it. `SCRIP_ICN_HOST_PIN=0` is the control arm.
- DELETED: `icn_gen_host_*` (slice, reserve_walk, reserve, reserved, reserve_offset, area_as_host, layout_audit, reserve_selftest), `N2_SELFREC_SLOTS`, `N2_RESERVE_MAX_DEPTH`, `icn_genframe2_selfrec`, `icn_gen_is_selfrec`, the depth bomb, `SCRIP_GENHOST_DIAG`, the `[GENHOST]` refusals, and hq_B's gate `test_gate_icn_genhost_layout_consistent.sh` with its Makefile arm and wiring row — its population (the two walks) no longer exists, so it could only print a false 0.

## RECEIPTS (all on this tree unless labelled)
- Eleven witnesses vs iconx, both modes: w1 host+generator, w2 a<->b mutual recursion, w3 hq_B's fan-out `suspend h(n-1)+h(n-2)` (hung before), w4s deep recursion (and 30000 deep under SCRIP where iconx overflows at 3000), w5 generators hosting generators across suspends, w6 generator as co-expression body, w7 three co-expressions interleaved, w8 staged co-expressions, w9 create through a procedure variable, hq_B's seven-shape battery, hq_B's mut_gen. All MATCH.
- Icon smoke 15/15 both modes. Icon master `board_icon_master.sh` run-graded both modes 697/702, the same five standing reds (cure-only binary 08:1x; rebased tree: see the push receipt in GOAL-CEO CEO-382). SNOBOL4 `test_corpus_snobol4.sh` 1858/1858 FAIL=0 both modes (07:48, pre-rebase cure tree).
- jtran (the cto's row, Lon: *"The ultimate test is the JCON compiler."*): preproc stage 58 bytes = oracle; driven directly, yylex 12 tokens, parse 1, ast2ir 1 result — all SAME as the icont-built jtran. The symbolic stage prints 0 bytes: NOT this landing's — see below.

## THE NEXT DEFECT, ISOLATED AND MINTED
`dump_simple` (jcon `dump.icn`) hangs on any record on the coo's and hq_T's clean origin binaries too. Four lines reproduce it: `every write(n ? (move(1) & tab(0)))` prints one line under iconx and loops under SCRIP. Cause read in `emit.cpp` `codegen_flat_chain_body`: the scan's re-entry target (`g_scan_body_beta`) falls to the body node's α label whenever `flat_beta_kind_keeps` is false, even though the β label is emitted — so backtracking into a conjunction inside a scan re-runs `tab(0)` instead of failing out. A WIP cure (prefer `betas[k]` when `bused[k]`; mark IR_SCAN operand[2] used) fixes nine witnesses and holds the Icon master at 697/702, but drops jcon's newline-as-semicolon token (`lexer.icn` `lex_newline`, token 5 of `t1.icn`) — diff archived at `postoffice/handoff/ceo-icon-scan-body-beta-reenter-WIP-regresses-jcon-lexer-token5-2026-09-07.diff`. Row: `icon-scan-resumes-its-body-at-alpha-not-beta-so-backtracking-into-a-conjunction-inside-a-scan-loops` (rank 0, the ceo). A second shape, `if "x" == (n ? ("a" & "b")) then .. else ..` losing its else branch, is noted there.

## WHAT THIS DOES NOT YET DO
Procedure locals are still carved at the host prologue; Lon's FORTH rule (carve each local where first used) is the follow-on rung, recorded in RULES.md BB FRAME-PLACEMENT CRITERION.
