# FINDING — GC-U-7 ROOTS: scanner interface + coexpr ctx/pkg conservative roots landed; HB_ZBLK un-pinned under registration; HB_WS un-pin MEASURED-BLOCKED by the malloc→WS reference hole (s90, 2026-07-17, Claude)

**SCRIP at `740292a4` + this session's commit.** Directive: "Finish implementing the RBX topped Garbage Collection."

## What landed
1. **The scanner interface (root registration).** `rt_gc_root_pin_add/del(p)` (containing block pinned at every collect) and `rt_gc_root_range_add/del(lo,hi)` (conservative scan at every collect) in gc_heap.{c,h}. Clients: `rt_zh_bump_slow` registers each refill window (pin+range; never deleted — the REG-4 v0 leak-by-design survives as pinned blocks, unchanged); the coexpr stack carve registers at create, unregisters at destroy.
2. **§6b finding ii RETIRED — collection proceeds with live coexprs.** The `g_scrip_coexpr_live` decline is deleted. Per-ctx conservative roots: `gc_spill[6]` (rbx/rbp/r12–r15 spilled by inline asm in `scrip_coswitch` immediately before EVERY suspension — union with the full stack-window scan covers every callee-saved original: a value is either still in its register at the sem_wait or was ABI-spilled to the stack the scan reads), `xmit[2]`, and the entry pkg register block (+8..+56). Live coexpr stack windows: registered pin + full conservative range (guard page excluded). When the collector runs ON a coexpr thread, the suspended main [stack] region is scanned in full (fresh maps parse — the low bound grows) plus the root ctx spill/xmit; the current thread's anchor..top scan is window-aware (`gc_stack_top` was wrong off-main: anchor-on-coexpr-stack to main-stack-top spans unmapped VA). Registry: `g_co_gc_head` linked at create, unlinked at destroy; `g_scrip_coexpr_live` now decrements at destroy (was a one-way latch). Fixed a latent pre-existing bug en route: `stk_win/stk_guard` were malloc garbage until first activation; destroy-before-activate would have mprotect'd garbage — zeroed at create.
3. **HB_ZBLK UN-PINNED, registration-governed.** Both clients cover themselves by construction (windows: registered forever; coexpr stacks: registered while alive, HB_FILL at destroy) — dead coexpr stacks reclaim. NOTE: `scrip_coexpr_destroy` has NO call sites in the tree today; the reclaim path is code-complete and gate-covered, exercised end-to-end only when an Icon-level coexpr lifetime rung arrives.
4. **Marked HB_WS/HB_ZBLK promote to HBF_PIN at forward** (fwd=self, never slid, never adjusted-through — the manual pin-3 XNBLK class), and marked HB_WS payloads get a TRANSITIVE conservative fixpoint scan. This closes a LATENT s88 SOUNDNESS GAP: TR-3's second pass moved pointer-bearing DESCR_t into ws, so WS payloads reference DT_S blocks nothing else roots; s88 never scanned WS contents and survived on fragmentation luck.
5. **Statics census** via `dl_iterate_phdr` (writable PT_LOAD of exe + libscrip; includes .bss, unlike a maps-path parse), scanned ws_only at every collect. Census runs ONCE ON MAIN at `rt_gcheap_init` — the C probe measured `__dl_iterate_phdr` FAULTING when first driven lazily inside a collect on a coexpr pthread.
6. **Two collect flavors refined:** gateway = `gc_collect_ex(2)` (current-stack scan pins WS/ZBLK ONLY — value-world keeps the proven precise seam treatment); exhaustion/manual = `(1)` full conservative, as before.

## ⚠ THE MEASURED HOLE — why HB_WS stays blanket-pinned (do not un-pin it without the split)
A window-first allocation variant (reuse the largest reclaimed gap before extending the high-water) was built as an optimization and became the DETECTOR: within 3 regenerations under `SCRIP_GC_STRESS=3`, `rt_call_arr(fname="xxxxxxxx1")` — an INTERNED FUNCTION NAME's HB_WS block, whose only references live in raw-malloc'd IR/driver structures NO root layer can see, was reclaimed and its bytes reused by churn strings ("Error 5 in statement 0", then longjmp through a clobbered buf). **Top-first is EQUALLY unsound — the slide itself overwrites dead regions — and survived 6,710 stress regenerations only because ~634 pins left almost nothing moving. Fragmentation luck, not soundness.** Consequence: HB_WS re-pinned at reset; window-first WITHDRAWN (with WS pinned its motivation vanishes; the SIL top-bump order stands). **Follow-up rung named: WS-CLASS SPLIT** — DESCR-only by_name string results → a collectable class (they are the hot fresh-string traffic; un-pinning them is what restores frontier-drop); malloc-referenced registries/interned names → immortal class. The TR-3 ROOT+EDGE rows already name the sides; TR-5's raw-malloc census is the mechanism. Shared multi-frontend file ⇒ 4-language gates (the TR-3(a) lesson).

## Evidence
- **Gates ALL AT BASELINE:** crosscheck EXACT m3 303/4 · m4 293/13/1 · DIVERGE=10, fail-sets IDENTICAL (diff-verified, before AND after) · sno smokes 7/7×2 · icon 12/2+10/4 · prolog 3/2×2 · authored lines 200-col clean, zero blank lines added.
- **Coexpr collections (the rung's bar):** icon `create (1 to 300)` driven through 300 activations with per-activation string churn — k=300, rc=0, ZERO declines, regenerations firing with the coexpr live-suspended, in mode-3 AND mode-4 (compiled via the smoke link line), plain AND `SCRIP_GC_STRESS`.
- **Churn stress (s88 fallout metric):** 4000-iter DUPL/table probe, `STRESS=3`: 6,710 regenerations, output correct, verify OK every cycle. First collect reclaims 172 dead compiler-era WS blocks (623→451). With WS re-pinned, live-WS accumulation dominates thereafter (12,807 pins at #6710) — the honest v1 profile until the split; sno normal-run behavior ≈ s89 exactly (collections only at exhaustion, never reached).
- **Bisect-proven pre-existing:** coexpr wrapping a USER-PROCEDURE generator (`create (gen(n))` with `suspend`) SEGVs — stash-rebuild at `740292a4` reproduces both probe shapes identically. Only builtin-expression creates are in the passing set; routes to the owed refs/icon-master gen-proc work (s89).
- **C destroy probe: WITHDRAWN as evidence.** Standalone-linked probe faults inside glibc `_int_new_arena`/`alloc_new_heap` on the coexpr thread's first malloc (persists under `MALLOC_ARENA_MAX=1`, frames purely glibc) — a standalone-link/fixed-VA environment interaction, out of rung scope; scrip binaries are unaffected (icon coexpr gates above). Its one yield, the census-on-main fix (§5), is landed.

## Open
WS-CLASS SPLIT (above) → then un-pin HB_WS + revisit window-first. ZH-RETIRE sweep (s89). Ruling (C) rc=134 unchanged, Lon's. refs/icon-master gen-proc consult still owed; the user-proc-generator coexpr SEGV lands there.
