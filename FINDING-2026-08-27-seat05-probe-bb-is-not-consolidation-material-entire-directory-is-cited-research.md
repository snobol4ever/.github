# FINDING 2026-08-27 seat05 — `probe-consolidate-bb`: the whole directory is cited, gated research material, not loose test clutter — extend the row's own STANDALONE-KEEP carve-out from 11 files to all 188

**Date:** 2026-08-27 · **Seat:** seat05 (FLEET-16) · **Row:** `probe-consolidate-bb` (a fan-out child of `corpus-suites-consolidation`, minted by ceo 2026-08-28 in the house-cleaning push) · **Zero source/corpus edits** — investigation only, no conversion attempted, no file touched.

## Headline

The row's own GOAL text already carves out 11 `.c` files as "ARCH-cited reference embodiments — STANDALONE-KEEP." Investigating the directory end to end, the **identical reasoning applies to essentially the entire remaining 188 `.sno` files** — they are not a generic loose-test population waiting for mechanical `corpus_suite_harness.py convert` treatment; they are a hand-crafted, individually-named, externally-cited SNOBOL4/Byrd-box correctness-research corpus, with:
- files paired 1:1 with the very `.c` reference embodiments this row already agreed to leave alone,
- a live `test_gate_*.sh` script that hardcodes two of these files by exact path,
- a mature, actively-maintained XFAIL/XPASS-baselined gate suite (`probes/`, 166 files) with no equivalent concept in the target one-liner/banner suite format,
- and citations by bare filename in the **live `GOAL-SNOBOL4-100.md`** (the file THE LOOP's own session-start protocol calls authoritative) plus several `.github` NOTE/FINDING files and `profile/s264-board/` artifacts.

Converting any of this mechanically would break a live gate script, orphan citations in the project's own most-trusted coordination document, and silently discard a working regression-detection mechanism (XFAIL/XPASS) the target format cannot represent. **Recommendation: extend STANDALONE-KEEP to the whole directory; this row's DONE-WHEN (zero `.sno`/etc. under `probe/bb`) should not be pursued as written.**

## The evidence, by sub-area

**Top-level 14 loose `.sno` files** (`c4b c4 c5 c6 c7 f1 t1`, `test_sno_alt_d0/d5`, `test_sno_call2bb_1/2`, `test_sno_stmt_frame_1/2/3`) — none referenced by any `.sh`/`.py` in `probe/bb/` itself, but:
- `BB-CHALLENGE-LADDER.md`'s own table cites `t1.sno`, `c4.sno`/`c4b.sno` by bare filename as the "oracle" column paired against `test_sno_1.c`/`test_sno_4.c`'s "C" column — a permanent, load-bearing reference table describing named, dated bug investigations (e.g. "Case 4 — CORRECTED 2026-08-04 (Lon ruling + `f1.sno` witness)"), not a test roster.
- `test_sno_alt_d0.sno`/`test_sno_alt_d5.sno` pair by exact-name match with `test_sno_alt_d0.c`/`test_sno_alt_d5.c`, already on this row's own STANDALONE-KEEP list.
- `test_sno_call2bb_1.sno`/`_2.sno` are hardcoded by **exact absolute path** in `SCRIP/scripts/test_gate_call2bb_stub_regime.sh:9` (`S1="$PROBE/test_sno_call2bb_1.sno"`) — a live gate, not documentation. Also cited in `.github/NOTE-2026-08-13-s67-LEAK-CENSUS-AND-FENCE-WITNESS-SETS.md`, `.github/NOTE-2026-08-13-s68-LEDGER2-STAGING-GAP.md`, `.github/profile/s264-board/{list.bb_probes,results.tsv}`.
- `test_sno_stmt_frame_1/2/3.sno`: file 3's own header comment states its role directly — *"seeds 1/2 are the HAND embodiments; this .s is honest scrip output"* — a three-file lineage documenting one bug's investigation, not independent test cases.

**`probes/` (166 `.sno` + a matching `.ref` for most, per `SUITE.md`'s own count of 161 "current" — drifted since the doc was written, not re-derived further here)** — a complete, mature, working test-infrastructure subsystem:
- `run_suite.sh` is "THE GATE": diffs SCRIP output against pinned SPITBOL (`sbl -b`) goldens, with wait-status-based crash detection (SIGSEGV is swallowed by SCRIP's own handler and exits 0 — a plain stdout+`$?` check is a proven false-green, per `SUITE.md`'s own documented incident) and an **XFAIL baseline**: PASS / xfail (known defect) / XPASS (a fix landed, drop it) / REGRESSION (gate fails) — four states, not two.
- `mkrefs.sh --verify` independently re-derives every golden from live `sbl -b` and fails if the pinned `.ref` has drifted — a second, orthogonal correctness check the one-liner suite format has no equivalent for.
- Individual probes are cited by stable ID throughout `BB-PROBE-MATRIX.md` (contrast pairs like `L16` vs `L17`, `N13` vs `N14`) and `SUITE.md`'s own bug classification (`D07`, `D08`, `H01` named individually with dedicated root-cause prose — `H01` gets its own 15-line section below the family table).
- **The XFAIL/XPASS distinction has no representation in the target suite format.** The parent row's format is strictly PASS-or-convert (byte-equal-or-no-delete requires the *original* to already be green); this row's own `SUITE.md` currently shows `XFAIL.run`/`XFAIL.compile` as **empty files** (0 bytes each, checked directly — `SUITE.md`'s prose claim of "46 xfail" is stale documentation, not re-verified against a live `run_suite.sh` run this session, flagged rather than assumed either way), so the true current pass/fail split is unconfirmed — but the mechanism existing at all, empty or not, is the point: a suite text file has no way to encode "this one is expected to fail until further notice" the way `run_suite.sh`+`XFAIL.*` does.

**`probes_x01_witness_s39/` (7 files) and `witness_wreg_s39/` (1 file)** — both session-39-tagged, both carrying their own `README.md`, both hit directly by `grep -rl` in **`.github/GOAL-SNOBOL4-100.md`** — the live goal file THE LOOP's own session-start protocol names as authoritative for SNOBOL4 work — plus the same `profile/s264-board/` artifacts cited above.

## What this means for the row

The task's own GOAL text framed this as file-count consolidation (188 sno / 177 ref, 11 c files carved out) — a reasonable first-pass census for a broad fan-out, but not an investigation of what the files actually *are*. That investigation is what this session did, and it changes the picture: **there is no safe subset here to mechanically convert.** Every candidate checked (14 top-level standalone files, the 166-file `probes/` gate suite, both witness subdirectories) has a specific, verified reason it cannot go through `corpus_suite_harness.py convert` without real, avoidable harm — the same *class* of finding seat08 already made repeatedly on the parent row for Icon's `.xfail` representation, Snocone's AST-oracle drift, and `beauty`'s three incompatible fixture shapes: a live blocker, not a mechanical gap.

This is a **scope question for whoever is coordinating the fan-out (ceo minted this row; hq_C/seat08 own the parent methodology)**, not a unilateral call for this session to make alone — the stakes (breaking a live gate script, orphaning citations in the authoritative goal file) are exactly the kind RULES.md's own precedent treats as requiring a routed decision rather than a seat's own judgment, even though the evidence here is unusually one-sided. Sent as a non-blocking note to both, per the established pattern (seat08's Icon-xfail and beauty findings were routed the same way, not blocking, not unilaterally resolved).

**Recommendation, for whoever rules on it:** extend `probe-consolidate-bb`'s existing STANDALONE-KEEP carve-out from the 11 `.c` files to the entire directory, and mark the row CLOSED-AS-EXCEPTION (same disposition class as SNOBOL4 crosscheck's `arith`/`control`/`coverage` — a real, principled, permanent exception, not a blocker awaiting a fix) rather than leaving its DONE-WHEN standing as an unreachable target. If a future session or Lon specifically wants `probes/`'s XFAIL mechanism represented in the suite format, that is new format design work (a peer to the Icon-xfail question already on file) and belongs in the parent row's own format-amendment history, not invented ad hoc here.
