# GOAL-ICON-BB.md — Icon, 100% Byrd Boxes, from zero

## ⛔ FACT RULE — ASM-RUNTIME: THE RUNTIME GETS FAST BY BEING HAND-WRITTEN x86, NOT BY BEING COMPILED HARDER (Lon directive, 2026-07-25 s159)

**The destination is a runtime built from hand-written x86 assembly fast paths shipped as `*.s` files and linked into `libscrip_rt.so` — registers and hand-chosen instruction sequences, NOT C that a compiler is asked to rescue.** C in the runtime is scaffolding: correct, readable, and the reference semantics for each operation. It is not the performance artifact. **THIS IS WHY THE BUILD STAYS AT `-O0` (or at most `-O1`) — the optimization level is not a lever we are declining to pull, it is a lever we are deliberately not building on.** A runtime whose speed comes from `-O2` is a runtime whose speed lives in GCC's inliner, evaporates under a compiler upgrade, and cannot be reasoned about register-by-register. A runtime whose speed comes from `*.s` leaves is one we own.

**THE s159 MEASUREMENT IS THE EVIDENCE FOR THIS DIRECTION, NOT AGAINST IT.** s156/s157/s158 each deferred `-O2` on `libscrip_rt.so` as "the biggest untried lever." s159 ran it under the full s126 protocol and it delivered **1.15×** — see `FINDING-2026-07-25-CLAUDE-ICN-BID-1-BUILTIN-ID-DISPATCH-AND-O2-FALSIFIED.md`. Asking the compiler to optimize the C bought almost nothing, because what is slow is *what the code does* (an O(n) by-name scan, a `DESCR_t` copied by value through every call, a heap cell minted per subscript), not how tightly it is compiled. Those are fixed by choosing different instructions and keeping values in registers — which is what an `.s` leaf does and what `-O2` cannot do for you.

**THE PROVEN PATTERN (s157, PERF-ASM-HELPERS, SCRIP `32ffde7f`) AND ITS PROVEN LIMIT:** `rt_deref` and `to_int` were rewritten as hand-written x86 (`+6%` nqueens n=10 at `-O0`), small-shape-inline / large-shape tail-jump to a C `*_slow` sibling, SysV AMD64 ABI verified against the emitted caller. **The limit found the same session: this works ONLY for leaves that make ZERO internal calls.** An asm `rt_subscript_var` came out ~3% SLOWER because two internal calls forced callee-saved push/pop around them, and the hand-written prologue lost to what the C already did. **So the rule for choosing an asm target is not "is it hot" — it is "is it hot AND a zero-internal-call leaf AND does it carry values that want to stay in registers across the whole body."**

**HOW AN ASM LEAF IS ADDED (the shape to follow):**
- The `.s` file lives beside the runtime source and is added to the Makefile's `RT_PIC_SRCS` so it links into `libscrip_rt.so` like any other TU. It must be `-fPIC`-clean (no absolute relocations into the `.so`).
- SysV AMD64 ABI, verified against the ACTUAL emitted caller, not against the C prototype's appearance: `DESCR_t` (16 bytes) comes in `RDI:RSI` and returns in `RAX:RDX`. Confirm by reading the emitted box, per `ARCH-ICON.md`'s register contract.
- **Honor the live register contract — it is NOT free real estate.** `RSP`=ζ under the `ZC_FRAME_RSP` default, `RBP`=ζ value-slot frame base, `R13`=Σ subject base, `R14`=δ cursor, `R15`=Δ subject length, `RBX` reserved as the WS/GC bump frontier under the HEAP arm. Clobbering any of these from a runtime leaf is a landmine that will not show up until a scan-heavy or GC-heavy program runs.
- Small/hot shape inline in asm; every cold, large, or error shape **tail-jumps to a C `*_slow` sibling** that keeps the reference semantics. The C stays; it stops being the hot path.
- Gate: full Icon suite unchanged (`PASS=249 FAIL=12 XFAIL=32` at s159) **plus** an A/B at identical emitted code — build one `P.o` and link it against two `.so` builds, per the DYNLINK recipe below.

**WHAT THIS MEANS FOR THE PERF LADDER:** a rung is "make this operation cheaper in instructions and registers," never "raise the optimization level." Any perf number reported must still carry its `RT_OPT` label (s126), and the Makefile default stays `-O0`. **`-O2` is now documented as measured-and-declined, not untried — do not re-propose it as the big lever.**

**LIMITATION (do not oversell — same honest shape as every rule here):** a markdown rule cannot make hand-written asm correct, and asm defects are far more expensive than C defects (wrong register saved, ABI mismatch, a clobber that only manifests under GC). The enforcement that works is the pairing: **zero-internal-call leaves only, a C `*_slow` sibling always retained, and the 293-program suite plus an identical-emitted-code A/B before any asm rung is called done.**

## ⛔ FACT RULE — O0-DEV: FEATURE BUILDS ARE `-O0`; `-O1`/`-O2` ARE PERF-ONLY (Lon directive, 2026-07-21 s119)

**While developing, debugging, or iterating on any FEATURE, EVERY build is `-O0`. `-O1` and `-O2` are FORBIDDEN during feature work and are reserved EXCLUSIVELY for perf/benchmark/release measurement.** The runtime `libscrip_rt.so` at `-O2` takes MINUTES (heavy template TUs), which is intolerable in a compile→test→fix loop and burned real session time repeatedly. `scrip` itself already builds `-O0` (Makefile `CBASE`/`CXXRT`); the offender was the runtime `.so`, whose `RT_OPT` default was `-O2`.

**THE MECHANICAL ANCHOR (why this is a FACT RULE, not a convention):** the Makefile default is now `RT_OPT ?= -O0 …` (SCRIP `Makefile` lines ~33 + ~281), so a bare `make libscrip_rt` / `make scrip` / `build_scrip.sh` is `-O0` by DEFAULT — the fast path is the path you get for free. `-O2` is now EXPLICIT opt-in, used ONLY for measurement:
```
make RT_OPT="-O2 -g -fno-strict-aliasing -fwrapv -fno-omit-frame-pointer" libscrip_rt   # perf/bench ONLY
PERF=1 bash scripts/jcon_selfhost_build.sh                                               # perf .so via the selfhost builder
```
Benchmark builders that need `-O2` already pass it explicitly (`jcon_selfhost_build.sh PERF=1`; the official-oracle trees build their own way), so the default flip does NOT silently corrupt any perf number — a perf run that forgets `-O2` is a mis-measurement the operator owns, not a default that lies.

**COMPLETION TEST:** (a) `grep -nE 'RT_OPT *[?:]?= *-O0' Makefile` matches (default is `-O0`) and no un-opted `RT_OPT ?= -O2` remains; (b) session-setup / feature-dev build scripts (`build_scrip.sh`, smoke/crosscheck runners) invoke `make` with NO `RT_OPT` override (so they inherit `-O0`); (c) any `-O2` in a script is either a monitor/oracle-side helper (separate lib) or gated behind an explicit perf flag (`PERF=1`); (d) this FACT RULE body is byte-identical across the six GOAL-*-BB files (md5-locked, per the Prolog file's sibling-verbatim note).

**LIMITATION (do not oversell — same honest shape as the other rules here):** a Makefile default and a markdown rule cannot COERCE a session to avoid typing `RT_OPT=-O2` during feature work; they make the fast path the default and the slow path a deliberate, visible choice. The human reviewer remains the real enforcer — **reject any feature-work handoff whose build log shows `-O2` on the runtime `.so`.**

## ▶ LIVE CURSOR (updated every handoff — RULES.md STALE-ORIENTATION rule)
- **🏆 SORT-1 + SORT-2 — THE ELEPHANT WAS AN O(n²) SORT, NOT DISPATCH (2026-07-25 s161, Claude Opus 4.5) — SCRIP `2d191182` + `9a82c7e2`. ⚠ LOCAL ONLY, PUSH BLOCKED (no credential this session) — re-push before trusting origin. Suite PASS=249 FAIL=12 XFAIL=32 after BOTH commits (zero regression). tgrlink Ir 2,242,064,012 → 1,543,486,439 = −31.2%, 1.453×. Wall 350→251ms. Honest geomean 0.573× → 0.666×. Output byte-identical to iconx at every step. See `FINDING-2026-07-25-CLAUDE-ICN-SORT-1-QUADRATIC-SORT-IS-THE-ELEPHANT-AND-FALLBACK-CHAIN.md`.**
  **⛔ READ BEFORE PLANNING ANY PERF RUNG: s160's proposed next rung (an asm `VARVAL_fn` leaf) would have been the WRONG FIX.** `VARVAL_fn` looked like "6,630,400 calls, 12.4 per dispatch, 11.3%" — but **6,476,858 of those calls come from ONE source line**, the inner comparison of an **O(n²) insertion sort** in `sort()`, present in BOTH sort paths (table `DT_T` and list `DT_DATA`). It called `VARVAL_fn` on both operands, and `VARVAL_fn` on a `DT_I` does `snprintf` + `rt_ws_strdup_c` — **two heap strings per comparison**, 3,238,429 comparisons. That loop alone was ~497M Ir ≈ **22% of the program**. Replaced with a **stable merge sort** (identical permutation by construction: old loop breaks on `cmp<=0`, merge takes left unless right is strictly less — one stable order exists per comparator, so output cannot move) plus an **allocation-free `sort_key_cstr`** (`DT_S`→`.s`, `DT_SNUL`→`""`, `DT_I`→stack buffer, else `VARVAL_fn`). Comparator predicate unchanged. **−25.4% Ir; `VARVAL_fn` left the top-13 entirely.** **LESSON — same one as s159 `-O2` (1.15×) and s160 jump table (2.4%): making each step cheaper cannot fix a complexity class. Before optimizing a high-call-count function, check whether ONE caller generates all the calls.**
  **🔎 s160's OPEN QUESTION IS CLOSED.** The unattributed 224M strcmp: `script_try_call_builtin_by_name` 90.4M (4,331,127 calls), `try_call_builtin_by_name` 80.1M, `data_field_ptr` 23.1M, `NV_GET_fn` 11.6M. SORT-2 then found that fallback is a **288-arm linear chain never given the BID-1 treatment** — 138.2M Ir (8.26%) for only **18,831 calls = 7,341 Ir each**. An env-gated probe (`SCRIP_STCB_PROBE`, since removed) showed tgrlink reaches it with exactly **two names**: `where` (9,416) and `seek` (9,415), both ~230 arms deep. Hoisted both to the front → **−7.8%**.
  **⚠ A FIRST-CHAR GUARD THERE IS WRONG — DO NOT SHIP ONE.** Lines 1630–2400 suggest every literal starts with `$`/`_`. The **full 2,259-line body** has 288 literals of which **68 are bare names** (`open` `close` `trim` `reverse` `length` `substr` `where` `seek` …), several also real Icon builtins. **Audit the whole function, not the window you printed.**
  **▶ NEXT RUNG — KILL THE PER-DISPATCH NAME WALKS.** The preamble walks the same name string **three times** before reaching the jump table: dtax hash loop 45.0M (2.91%), `bid_of` hash loop 37.7M (2.44%), a second `strlen` feeding the SNOBOL4-uppercase switch 22.2M (1.44%) — ~127M (8.2%) over 533,135 dispatches. (1) cheap+safe: compute the length ONCE, thread it through `bid_of` / dtax bound / `_fl`. (2) structural (s159/s160 track 1, still unstarted): `lower_icon.c` resolves a known builtin to its `BID_*` at LOWER time and emits the integer so the runtime never sees a name — kills `bid_of` and most of the 247.5M preamble self-cost. **THEN the by-name field elephant:** `strcasecmp` 100.2M is entirely `FIELD_GET_fn` (71.6M, 1,704,085 calls) + `FIELD_SET_fn` (28.4M, 675,009 calls); 44 by-name list-frame sites remain in `by_name_dispatch.c` (27 GET + 17 SET) — adopt the `rt_list_view` constant-slot idiom. ⛔ Do NOT revive a `(DATBLK_t*, name-pointer)` memo cache; s160 rejected it as unsound and that still holds.
  **⚠ CORRECTNESS GAP FOUND, DELIBERATELY NOT FIXED (own rung):** canonical Icon `anycmp` (`refs/icon-master/src/runtime/rcomp.r`) orders by **type collating number first** via `order()`, comparing only within a type. SCRIP does "both-int numeric, else stringify and strcmp" — agrees on homogeneous tables/lists, **diverges on mixed-type**. Changing sort ORDER would move output on the 249 green tests, so it was left alone.
  **⚠ METRIC:** Ir is the honest metric; wall-clock only on `tgrlink`/`geddump` (`version` is 4ms of pure startup). `rsg`'s 2.83× is noise/short-circuit until its DIVERGE is explained. `geddump` diverges 11,222L vs oracle 10,145L — pre-existing, unchanged by this session.
  **⛔ HONEST GAP TO LON'S TARGET: 2–3× means 3.0× more from 0.666×.** This session bought 1.16×. The two structural rungs above are compiler-side, not runtime-side, and they are the ones that change the shape of the cost.
- **🏆 BID-2 JUMP TABLE + ⛔ DISPATCH IS NOT THE ELEPHANT (2026-07-25 s160, Claude Opus 4.5) — SCRIP `ac92b5b6`. Suite PASS=249 FAIL=12 XFAIL=32 (zero regression). Honest geomean 0.615× → 0.642× at `-O0`. tgrlink Ir 2,323M → 2,246M (−3.3%). See `FINDING-2026-07-25-CLAUDE-ICN-BID-2-JUMP-TABLE-AND-DISPATCH-IS-NOT-THE-ELEPHANT.md`.**
  **⛔ READ BEFORE PLANNING ANY DISPATCH RUNG: finishing the integer-ID work bought 2.4%, NOT 34%.** BID-1 left the 197 `_bid ==` tests a LINEAR CHAIN and the hot builtins sit late in it (tgrlink: right=157 get=120 integer=126 put=150 abs=103 atan=106), so s160 generated a `switch(_bid)` jump table (125 cases → 123 labels, inserted after the preamble so nothing live is skipped). Removing the ENTIRE chain walk bought **55M Ir of 2,323M**. REASON: `try_call_builtin_by_name` is 66.8% INCLUSIVE and ~2,911 Ir per dispatch, but that is **not dispatch overhead — the builtin BODIES are written inline inside the function.** Its 477M self-cost is real work. **Do not spend another rung making dispatch cheaper; the by-name scan pathology is now paid down.**
  **▶ NEXT RUNG — EMIT INLINE FAST PATHS; STOP ROUTING SCALARS THROUGH THE RUNTIME.** `a + b` on two integers currently pays: arg marshal to frame array → `rt_call_arr` (**setjmp**) → `rt_gc_point_arr` → `$` check → **~30-strcmp operator chain** → `rt_num_arith` (**second setjmp**) → impl. Hundreds of instructions for an `add`. Emit a DT_I test + native `add`, call out only on the slow shape. **`dop_direct_fp` in `bb_call.cpp` is the in-tree precedent** ("direct det leaf: no by-name dispatch"); the operator analog is sound **by construction** — operators (`+ - * [] ||`, relops) are not identifiers and can never be shadowed by a user procedure or record type. Name it by behavior, not language (no-language-identity FACT RULE). THEN `VARVAL_fn` (**6,630,400 calls = 12.4 per dispatch**, 11.3%): ask why the bodies call it 12× per call before making each call faster.
  **⚠ NEGATIVE RESULTS, do not retry blind:** (a) first-char guard on the DT_DATA field scan removed only **4.5%** of strcmps — the 227M strcmp cost is **still unattributed, re-derive its caller first**; (b) **case-folded guard in `FIELD_GET_fn` measured WORSE and was REVERTED** (`strcasecmp` 102M→80M but `FIELD_GET_fn` 62M→101M, net **+1.8% Ir**) because `tolower()` is an out-of-line locale call at `-O0` — **any case-insensitive fast path must use an inline ASCII fold, never `tolower()`.**
  **⚠ METRIC:** wall-clock on this corpus is startup-dominated (`concord` moved 97→114ms across a change that LOWERED Ir; `version` is 4ms of pure startup). **Ir is the honest metric; wall-clock only on `tgrlink`/`geddump`.** New diagnostic: `SCRIP_BID_PROF=1` prints the builtin-id histogram.
  **⚠ HONEST GAP TO LON'S TARGET: 2–3× FASTER means closing ~4× from 0.642×. No single rung on this ladder is worth 4×** — `iconx` is a bytecode interpreter (`switch((int)lastop)` in `interp.r`); beating it needs emitted code that stops calling the runtime for scalar ops, per the next rung above.
- **🏆 BID-1 BUILTIN-ID DISPATCH + ⛔ `-O2` FALSIFIED (2026-07-25 s159, Claude Opus 4.5) — SCRIP: BID-1 committed on top of `0620ed86`. Suite PASS=249 FAIL=12 XFAIL=32 at BOTH `RT_OPT=-O0` AND `RT_OPT=-O2` (zero regression). Honest geomean vs iconx: 0.566× → 0.644× (BID-1, -O0) → 0.742× (BID-1, -O2). See `FINDING-2026-07-25-CLAUDE-ICN-BID-1-BUILTIN-ID-DISPATCH-AND-O2-FALSIFIED.md`.**
  **⛔ READ THIS BEFORE PLANNING ANY PERF RUNG: `-O2` on the runtime `.so` IS NOT THE BIG LEVER. It is 1.15×.** s156/s157/s158 all deferred it as "the biggest untried lever"; Lon directed it s159, it was run under the full s126 protocol (forced `rm -rf out/rt_pic`, detached+polled, worktree verified BEFORE, `.so` mtime 14:59:49→15:16:44 and 22MB→215MB verified AFTER, zero `O0` in build log), and it delivered **1.15×**. REASON: the dominant cost is an **O(n) algorithm**, and `-O2` makes each step cheaper without making the scan non-linear. At `-O2` post-BID-1, `try_call_builtin_by_name` is STILL #1 at 20.13% with `__strcmp_avx2` #2 at 13.90% — **~34% of the program is by-name dispatch even after `-O2`.** Makefile default REMAINS `-O0` (untouched); label every perf number with its `RT_OPT`.
  **WHAT BID-1 DID:** `try_call_builtin_by_name` held **197 `!strcmp(fn,"literal")` tests over 176 names** and was issuing **41.6M `strcmp` calls** from only 533K invocations (the `g_dtax` cache only short-circuits ctors/synonyms; real builtins fall through the whole chain). `scripts/gen_builtin_ids.py` mechanically rewrites each to `(_bid == BID_X)` and generates `src/runtime/builtin_ids.h` (176 names, 1024-slot open-addressed table, max probe 6). One hash at entry replaces up to 197 string compares; an unknown name hashes to id 0, matches no constant, and falls through exactly as before (correctness is structural, not empirical). **Ir 3.353B → 2.319B (−31%) at -O0. tgrlink 434→296ms (1.47×), rsg 1.40×, ipxref 1.25×, geddump 1.20×.**
  **⚠ THE s158 CURSOR'S LEAD WAS WRONG — AND SO WAS MINE UNTIL I PROFILED.** s158 named rvalue-VCELL elimination in `rt_subscript_var` as "~23% Ir" and the next rung. On `tgrlink` that function is **0.60%**. The 23% figure came from profiling `queens`, whose inner loop is subscript/alloc-bound; the corpus at large is dispatch-bound. `queens` is the ONE program BID-1 did not move (1.00×). **Profile the program you are actually trying to speed up, and say which program a percentage came from.**
  **▶ NEXT RUNG — reframed by the s159 ASM-RUNTIME directive (see the FACT RULE at the top of this file).** The gap is NOT closed by compiling C harder — that was measured at 1.15× and declined. Two tracks, in this order: **(1) STRUCTURAL, in the compiler:** `lower_icon.c` resolves a known-builtin callee to its `BID_*` at LOWER time and emits the integer, so the runtime never sees a name (`try_call_builtin_by_name` 20.13% at -O2); plus Icon's classic global field numbers so `bb_field_get.cpp` stops emitting `dat_field_get(fname,obj)` BY NAME (`strcasecmp` 5.81%). BID-1 already built the name→id table these need — `src/runtime/builtin_ids.h` is generated and can be consumed at lower time. **(2) ASM LEAVES, per the ASM-RUNTIME rule:** the standing candidate list wants re-derivation against the zero-internal-call test, but `VARVAL_fn` is the strongest current one — **13.20% of tgrlink at -O2, 6,630,400 calls, and its hot arms (DT_S passthrough, DT_I via snprintf+strdup) are leaf-shaped**; the DT_S arm is a pure register move that C spends ~36 instructions on because `DESCR_t` is passed BY VALUE. Split it: asm fast path for DT_S/DT_SNUL/DT_I-small, tail-jump to a C `VARVAL_slow` for DT_R/DT_A/DT_DATA/DT_P. **Do NOT retry an asm `rt_subscript_var` (s157 measured ~3% SLOWER — two internal calls force callee-saved push/pop; it fails the zero-internal-call test).**
  **⚠ REJECTED this session (do not retry blind): goto/label switch-dispatch over the existing chain — the chain's top level is NOT a clean run of `if` blocks (unconditional `{ size_t _fl = strlen(fn); switch(...) }` SNOBOL4-uppercase block sits in it), so a `goto` past it skips live code.** **REVERTED deliberately: a `(DATBLK_t*, fn)`-POINTER-keyed memo cache over the entry field-scan — correct only if a `fn` buffer's contents can never change under a stable address, which could not be proven; a stale hit is silently wrong and the suite would not reliably catch it. Key it on CONTENT and verify on every hit if revived.**
- **🏆 ALLOC-DETAX + BSYMBOLIC + HONEST-BENCH (2026-07-25 s158, Claude Sonnet 4.6) — SCRIP commit `0620ed86`. Suite PASS=249 FAIL=12 XFAIL=32 (zero regression). queens -n10 warm-min-9: 69→65ms (+5.8%). Two deliverables: (1) `rt_gcheap_alloc`/`rt_agg_alloc` de-tax fast-path (BP-2 pattern from SNOBOL4 ladder): every alloc paid histogram check, atexit branch, arena-init, two `getenv` static-guards and budget accounting before a 3-instruction bump; `g_alloc_detax` flag bypasses all of it once guards are resolved, falling through to full path for GC/window/stress. (2) `scripts/honest_icon_bench.sh`: oracle-diffing runner that compares stripped output vs iconx. Exposed that README's 3.1×/13.8× wins at `f405c6a7` were banner-only: `options()` aborted `main` silently (all programs linking `options`/`post` = all claimed wins; honest geomean was 0.50× = 2× slower than iconx). Arizona Icon = bytecode interpreter (`iconx` = `switch((int)lastop)` over `Op_*` in `interp.r`; no `iconc` in this distro). **NEXT PERF RUNG (callgrind says ~47% of Ir in two clusters): rvalue VCELL elimination — `rt_subscript_var` mints `VCELL_t`+`NAMETRAP` on every subscript path including rvalue uses (~23% Ir); queens does 3 subscripts per inner iteration. Prior SUBSCRIPT-FUSE rung falsified a version that added a template branch while leaving alloc fully taxed — worth re-running now that alloc path is cheaper. Compile-time record field numbers (BP-1 analog, `try_call_builtin_by_name` 3.21%) is the next layer. `-Wl,-Bsymbolic-functions` on the `.so` relink (adds to the Makefile `shared` line) removes ~12% dynamic-linker symbol resolution overhead (side-effect of DYNLINK commit; `do_lookup_x`+`_dl_lookup_symbol_x`+`_dl_relocate_object` in callgrind); measured 5.6% on queens but startup-bounded — add to Makefile separately when Lon directs. `geddump` emits 11,222L vs oracle 10,145L at HEAD — real divergence, no FZ cluster yet. `honest_icon_bench.sh` exposes it; use it instead of `test_icon_bench_corpus.sh` for all future bench work.**
- **🏆 LIST-SLOT-ACCESS — LANDED SPEEDUP (2026-07-25, Claude Sonnet 4.6) — SCRIP commit `9c0cdd32` (pushed). Icon 1.22x FASTER (was 1.17x). Suite PASS=249 FAIL=12 XFAIL=32 (zero regression).** Found by **callgrind RT-vs-BB profiling** (valgrind installable: `apt-get install -y valgrind`, root, no sudo). **KEY INSIGHT — Icon is like PROLOG not SNOBOL4: ~85% of instructions in the RT (.so), only ~10-14% in the emitted BB code.** The dominant RT cost was that **Icon lists are `DT_DATA` frame-records (JCON-style), and every list op did by-name `FIELD_GET_fn` lookups (gen_type/frame_size/frame_elems), each a case-insensitive `strcasecmp` scan = 42% of all instructions.** Why all 5 prior micro-opts failed: they shaved the 14% rt_subscript_var glue while ignoring the 42% by-name-lookup elephant. **FIX (per Lon directive "use slot numbers within frames, not names"):** the list layout is FIXED by `DEFDAT_fn("list(frame_elems,frame_size,gen_type,frame_cap)")` → slot 0 elems, 1 size, 2 gen_type. `rt_list_view()` (pattern_match.c top) indexes those CONSTANT slots directly (cached DATBLK-identity pointer-compare confirms a list frame; gen_type value distinguishes list from other frame kinds), replacing ALL SIX by-name sites in pattern_match.c (subscript/2D/real-index/size). MEASURED (mode-4 n=12 warm-min): baseline 3187 → 2603 ms = **1.22x**; strcasecmp 22%→~0%; instruction count 35.3M→22.3M (-37%). **NEXT (evidence-backed): (1) the SAME by-name frame pattern still lives in rt.c:1581, rt_runtime.c:469, by_name_dispatch.c:901, string_ops.c — adopt `rt_list_view` there too (helps *list/list-iteration-heavy programs; NOT in nqueens hot loop so unmeasured here). (2) BIGGER: emitted user-record `.field` access compiles to `dat_field_get(fname, obj)` BY NAME (bb_field_get.cpp) — same strcasecmp cost for record-heavy Icon; the real fix is Icon's classic compile-time GLOBAL FIELD NUMBERS (assign each field name an int at compile time, emit the int, dat_field_get does int-index not strcasecmp) — a lowerer+runtime change, dynamically-typed so needs the per-record-type field-number table. (3) post-fix callgrind top costs: rt_subscript_var glue 26.9%, BB ~14%, rt_gcheap_alloc+agg_alloc ~12%, rt_assign_var 5%, rt_deref 4%. (4) -O2 on runtime .so still the biggest untried lever (needs Lon EXPLICIT directive). **METHOD: use callgrind BEFORE optimizing — static/dynamic-count intuition was wrong 5x in a row.**
- **✅ O0-PERF-FLOOR-REACHED (2026-07-25, Claude Sonnet 4.6) — superseded by LIST-FIELD-CACHE above. Five -O0 micro-opts falsified (alloc-pool, subscript-deref fusion, accessor-inline, asm-rt_subscript_var ~3% slower due to 2-internal-call callee-saved push/pop, + prior setjmp/dispatch/hash/GC). Conclusion at the time was "O0 at its floor" — CORRECT for those micro-levers, but the real lever (list-field-cache, above) was a by-name-lookup hotspot that only callgrind revealed. asm-of-hot-leaves works ONLY for zero-internal-call leaves (to_int, rt_deref, +6% prior). -O2 on the runtime .so remains the big untried lever (needs Lon's EXPLICIT in-session directive: `make RT_OPT="-O2 -g -fno-strict-aliasing -fwrapv -fno-omit-frame-pointer" libscrip_rt`, detached+polled, verify .so mtime moved, label perf numbers with RT_OPT).**
- **✅ SUBSCRIPT-FUSE-FALSIFIED (2026-07-25, Claude Sonnet 4.6) — SCRIP tree stays at DYNLINK `21f374a9` (fusion code built, measured, then REVERTED — no SCRIP code landed this rung). Icon suite PASS=249 FAIL=12 XFAIL=32 (zero regression; tree pristine).** Built the value-subscript fusion rung and MEASURED IT: ~1-3% SLOWER in both mode-3 and mode-4 (removed 3M allocs + 3M boxes + 3M deref calls → NOTHING). Reverted. Details: new `rt_subscript_value` (DT_A returns element directly, else fallback to exact `rt_deref(rt_subscript_var(...))`); `bb_subscript.cpp` branch on `_.op_ival`; `lower_icon.c` TT_IDX rvalue (line 443) gated `SCRIP_SUBSCRIPT_FUSE`, marks subscript `ival=1` + skips IR_DEREF box. Correct (92/2680/14200 both states) but no win. First half of the O0-floor conclusion above.
- **✅ DYNLINK-SCRIP (2026-07-25, Claude Sonnet 4.6) — SCRIP `21f374a9`. Icon suite PASS=249 FAIL=12 XFAIL=32 (zero regression). `scrip` executable now links `out/libscrip_rt.so` dynamically (Lon directive 2026-07-25) — `make scrip` drops from 118s to 1s; runtime edits require only `make libscrip_rt` (≈22s incremental, ≈1-3s single-file). `scrip` binary drops from 22MB to 161KB. Both `--run` (mode-3) and `--compile` (mode-4) use the same `libscrip_rt.so`. Makefile: added 21 template `.cpp` files to `RT_PIC_SRCS`; `scrip:` target = compile `scrip.c` only + link `.so` via `-Wl,-rpath`. **⚠ PROCESS: after editing runtime `.c`/template `.cpp`, `make libscrip_rt` then `make scrip`. Header edits (e.g. descr.h) are NOT dep-tracked — `rm -rf out/rt_pic` to force full recompile.** Mode-4 perf-A/B recipe: `scrip --compile --target=x86 P.icn > P.s && as --64 -o P.o P.s && gcc -no-pie -o P.bin P.o out/libscrip_rt.so -lm -lstdc++`. To A/B a runtime change with IDENTICAL emitted code: build one P.o, link it against two different `.so` builds.
- **✅ DYNLINK-SCRIP (2026-07-25, Claude Sonnet 4.6) — SCRIP `21f374a9`. Icon suite PASS=249 FAIL=12 XFAIL=32 (zero regression). `scrip` executable now links `out/libscrip_rt.so` dynamically (Lon directive 2026-07-25) — `make scrip` drops from 118s to 1s; runtime edits require only `make libscrip_rt` (≈22s incremental, ≈3s single-file). `scrip` binary drops from 22MB to 161KB. Both `--run` (mode-3) and `--compile` (mode-4) now use the same `libscrip_rt.so`. Makefile change: added 21 missing template `.cpp` files to `RT_PIC_SRCS`; `scrip:` target replaced with: compile `scrip.c` only + link `out/libscrip_rt.so` via `-Wl,-rpath`. **⚠ PROCESS NOTE UPDATED: after editing any runtime `.c` or template `.cpp`, run `make libscrip_rt` then `make scrip` (both fast: ≈22s + 1s). Old note said "make scrip takes 118s" — that was the static-link era.** Mode-4 standalone binary recipe (for pure-runtime perf A/B): `scrip --compile --target=x86 P.icn > P.s && as --64 -o P.o P.s && gcc -no-pie -o P.bin P.o out/libscrip_rt.so -lm -lstdc++ -Wl,-rpath,$PWD/out`.
- **✅ PERF-ASM-HELPERS (2026-07-24, Claude Sonnet 4.6) — SCRIP `32ffde7f`. Icon suite PASS=249 FAIL=12 XFAIL=32 (zero regression). +6% nqueens n=10, mode-4 warm-min A/B at -O0.** Hand-written x86 asm fast paths for `rt_deref` + `to_int` (the two highest-volume small runtime leaves at ~1.3M + 20M calls/bench respectively). Small=asm, large=C (your directive): hot shapes inlined in asm, cold/large shapes tail-jump to C `rt_deref_slow`/`to_int_slow`. ABI: SysV AMD64, DESCR_t in RDI:RSI→RAX:RDX, verified vs emitted caller. All three benchmarks (nqueens, subscript-weighted, subr-read) correct; full 293-program Icon suite unchanged. Session also produced a rigorous multi-hypothesis diagnosis (setjmp, arithmetic dispatch, global hash, GC churn, VCELL allocation all falsified as dominant costs at -O0; real cost is distributed call overhead + emitted box-transition density). **NEXT PERF RUNG: dead-result elision (BP-9 analog for result slots).** Design scoped and ready: pre-emit `flat_result_used_scan` (node `i` live iff appears in any `operands[]` — `dead_pure`'s `ref[]` primitive), `g_emit.op_result_dead` flag, `SCRIP_RESULT_ELIDE` hatch defaulting OFF (zero-regression by construction, BP-5/6/9 pattern). Gate: result materialization is per-template (not centralized), so each template needs both-medium result-store elision gating — this is a dedicated rung the size of BP-9, start with full session budget.
- **✅ ICON-REAL-FORMAT-TRAILING-ZERO (2026-07-24, Claude Opus 4.8) — Icon 239→248/22→13/32 (+9, zero regressions). NOT YET COMMITTED (local only).** Integer-valued reals printed `2.` instead of Icon's `2.0` (e.g. `write(4.0)`, `write(1.5+2.5)`). ROOT CAUSE: Icon's `write`/`string()` shared SNOBOL4's SPITBOL-pure `real_str` (string_ops.c), whose documented contract DROPS trailing zeros (`4.0`→`4.`, correct for SNOBOL4 — `.ref` corpus verified: SNOBOL4 wants `0.`/`1.`/`237.`, Icon wants `4.0`/`2.0`). These are DIFFERENT language semantics; sharing one formatter was the bug. FIX: added `icon_real_str` in string_ops.c (delegates to `real_str` then applies canonical Icon `cnv.r rtos()` rule — append `0` if result ends in `.`; exponential/fractional forms untouched). Repointed ONLY the two Icon-reachable sites: `out_write_descr` real branch + `string()` builtin real branch in by_name_dispatch.c. Verified isolation: `real_str` stays SPITBOL bare-dot (SNOBOL4 unaffected); the other two `out_write_descr` callers are Prolog `$write` which intercept `DT_R` via `pl_real_iso_str` BEFORE reaching it, so the change is Icon-exclusive. Cleared: rung17_real_arith_real_{add,conv,mul}, rung19_pow_toby_{pow_var,real_toby_neg,real_toby_pos}, rung26_pow_real_pow, rung30_builtins_misc_sqrt, rung37_math_hello. **Also confirmed 3 doc-listed fails already pass at HEAD (free): jcon_coerce, jcon_mffsol, jcon_prepro. New fail not in doc clusters: rung36_jcon_mathfunc.** ⚠ PROCESS NOTE for next session: mode-3 `--run` uses the runtime copy STATICALLY LINKED INTO the `scrip` binary, NOT `libscrip_rt.so` — after editing any runtime `.c`, `make scrip` (not just `make libscrip_rt`) or `--run` won't see the change. **NEXT: commit this, then attack the 13 remaining rung36_jcon_* (Cluster B var/scan emit-aborts have file:line pointers; C 3 SEGVs need MONITOR — but no gdb/ltrace in this container, use stderr-probe bracketing).**
- **🟡 JTRAN-SELF-HOST — SUPERSEDED (2026-07-24, Lon directive re-confirmed): NO JVM. Oracle is icont/iconx only. The 2026-07-23 JTRAN/.class constant-pool work below is OUT OF SCOPE — do not resume it.** icont/iconx v9.5.25a oracle builds from the uploaded icon-master source (`make Configure name=linux && make`).
  **SESSION FINDINGS (2026-07-23, Claude Sonnet 4.6 — fresh container, built from scratch):**
  **BUILD:** `make scrip` + `make libscrip_rt` (both -O0, 1 core) → clean. `icont`/`iconx` v9.5.25a oracle built from uploaded source. All 17 jtran modules compile via `SCRIP_BETA_ELIDE_OFF=1 scrip --compile` → 507K asm lines, zero errors → links clean. Oracle jtran rebuilt with generated do_ops.icn/interface.icn (these MUST be included — omitting them causes `&null` in lex tables at lexer.icn:14, runerr 106; the generator scripts `oplexgen.icn`/`interfacegen.icn` must be run first via `scrip --run` then semicolonized).
  **PIPELINE STAGES VERIFIED BYTE-IDENTICAL TO ORACLE:**
  (1) `preproc <mod> : stdout` — verified on `hi.icn` (trivial) and `preprocessor.icn` (591 lines, 19014 bytes) ✅
  (2) `preproc : yylex : devnull` — verified, clean exit on all inputs tested (NO multi-token yylex crash at HEAD) ✅
  **FULL BYTECODE PIPELINE (`preproc : yylex : parse : ast2ir : optim : bc_File`) on `hi.icn`:**
  - `lhi.class` (program class): byte-identical, 1175 bytes ✅
  - `links`: identical ✅
  - `p_lhi$main.class` (compiled main procedure): SCRIP=988 bytes, oracle=983 bytes — DIFFERS ❌
  **THE ONE DIVERGENCE — constant-pool entry ordering:** Both files contain the identical 35 UTF8 strings and 69 total constant-pool entries. Divergence starts at cp#58: SCRIP interns `error`/(I)V/jcon/iRuntime then `Resume`; oracle interns `Resume` first then `error`. This is a pure ordering difference — the same methodrefs for two `jcon.iRuntime` calls (`error(I)V` and `Resume()Ljcon/vDescriptor;`) are interned in swapped order. The 5-byte file-size difference (262 vs 257 in the post-cp body) propagates entirely from the cp reordering. The root cause is an evaluation-order difference in SCRIP for two consecutive `j_create_methodref` / `j_cp_Utf8` calls in `gen_bc.icn` — the order in which SCRIP visits two adjacent code-generation sites differs from the oracle by one swap.
  **NEXT SESSION START:** (0) `cd /home/claude/work/SCRIP` + `make scrip libscrip_rt` (already clean at HEAD; -O0). (1) Build oracle jtran: `cd /tmp && mkdir -p jt-oracle && cd jt-oracle && cp /home/claude/work/corpus/programs/icon/jcon-compiler/*.icn . && icont -o jtran_oracle dump.icn do_ops.icn preprocessor.icn lexer.icn ast.icn parse.icn ir.icn keyword.icn irgen.icn gen_bc.icn gen_symbolic.icn gen_dot.icn gen_ucode.icn optimize.icn interface.icn bytecode.icn jtran_main.icn` (note: do_ops.icn + interface.icn must come from running `scrip --run oplexgen.icn > do_ops_raw.icn && python3 tools/semicolonize_icon.py do_ops_raw.icn do_ops.icn` and same for interface). (2) Build SCRIP-jtran: follow `scripts/jcon_selfhost_build.sh` steps 1-6, SKIP step 7 (Java/jcon.zip — LON DIRECTIVE: no Java). (3) FIND THE ORDERING DIVERGENCE: in `gen_bc.icn`, find the two `j_create_methodref` or `j_create_method` calls for the `error` and `Resume` iRuntime methods (search `error` near line 1302 `Resume`). The oracle visits `Resume` before `error`; SCRIP visits `error` before `Resume`. Look for an `|`-alternation, a `every`, or two adjacent procedure calls whose evaluation order could differ. MONITOR-FIRST: run `scrip --run gen_bc.icn` is not directly possible (it's a library); instead run the full jtran pipeline on a minimal program with a runtime error path, diff the .class constant-pool entries using the Python parser (see this session's findings — 69 cp entries, utf8/class/methodref comparison). (4) Once the ordering is fixed and `p_lhi$main.class` is byte-identical on hi.icn, scale to larger programs (rung36 jcon modules) and irgen.icn full pipeline. (5) Corpus target: clear the 9 cosmetic real-format fails first (likely a single `printf`-style fix for `2.0` vs `2.` in real-number output in `arithmetic.c` or `io_format.c`) to free the FAIL count to ~13 genuine bugs.
- **⬛ (prior) JTRAN-NAME-BUILTIN blocker + type() DT_E fix — SCRIP `02eb13d4`.** name() now RESOLVED (above). type() DT_E fix (procedure/function value → `"procedure"`, `fmisc.r:1174`) landed, cleared `89_proc_value`. `88_swap_lv` (`s[2:4] :=: s[5:6]`→`aedebc` not `aedbcf`, unequal-length section swap) still open, NOT jtran-exercised.

- **✅✅✅✅✅ ICON-PARSER-PREC-5FIXES (2026-07-21, Claude Sonnet 4.6) — SCRIP `3c96859c`. Icon 240/21/32 (zero regression; 240 vs prior 241 = env math-drift present in unmodified HEAD). Oracle: icont/iconx v9.5.25a.** (1) **`===`/`~===` PRECEDENCE (self-host blocker):** were parsed at assignment level (looser than `|`); `a===5|a===6` parsed as `a===(5|a)===6`. Moved into `is_relop()`/`relop_ekind()` in `icon_parse.c`. Unblocks all JCON modules using `while tok===lex_LOCAL|tok===lex_STATIC`. (2) **`|` vs `to` PRECEDENCE:** canonical Icon `|` tighter than `to`; SCRIP had them swapped. Rewired: `parse_to→parse_alt→parse_rel`, `parse_activate→parse_to`. Final ladder loosest→tightest: `&(and) > ?(scan) > :=(assign) > @(activate) > to > |(alt) > comparison`. (3) **`?` PRECEDENCE:** was tighter than `:=`; canonical looser (`y:="AB"?move(1)` => `y="AB"`). Pulled into new `parse_scan()` level between `parse_and` and `parse_assign`. (4) **`?:=` DESUGARING (`lower_icon.c`):** `s ?:= rhs` was silent no-op (AUGOP_SCAN has no binop_tt). Now desugars to `s := (s ? rhs)`. (5) **`sort(list,i)` 2-arg form (`by_name_dispatch.c`):** was Undefined; nargs==1||2 now accepted. **OPEN:** `to` with generator limit — `1 to (2|4)` yields `1 2 3 4`, oracle `1 2 1 2 3 4`. hi-generator `mr` advances one step too early. Root in `lower_to()` — needs α-force GOTO trampoline (same idiom as TT_SCAN at lower_icon.c:623). AST shape newly reachable via fix 2. **NEXT:** (1) Fix `to`-gen-limit; (2) rebuild 17-module jtran (`corpus/programs/icon/jcon-compiler/` + `semicolonize_icon.py` + SCRIP m4); (3) compile `local`/`static` programs through SCRIP-jtran (fix 1 gates this); (4) `.u1`/`.u2` byte-compare vs icont-jtran.

- **✅✅ JCON-SELFHOST-SET-OPS + WHILE-ALT (2026-07-21, Claude Sonnet 4.6) — SCRIP `(see commit)`. Icon 241/20/32. SELF-HOST BASE CASE PROVEN: `hello, world` end-to-end SCRIP-jtran→jlink→JVM = oracle.** (1) **SET OPS:** `++`/`--`/`**` on set operands routed through cset path. Added `set_union`/`set_diff`/`set_inter` in `aggregates.c`; patched `arithmetic.c` and `by_name_dispatch.c`. Unblocked `bc_createvars`' `bc_var_set -- params -- locals -- statics`. (2) **WHILE/UNTIL ALTERNATION:** `while A|B do` entered body once then exited (disjunction RESUMED on loop-back, not RESTARTed). Fix: α-force trampoline `CENT=build(IR_GOTO)` with `lc_γ_to_α`/`lc_ω_to_α`; body targets `CENT`. Mirrors `lower_every` pattern.

- **✅ ICN-LOCAL-SHADOW (2026-07-20, Claude Sonnet 4.6) — SCRIP `37c45277`. Icon 241/20/32.** One-line fix in `lower_icon_resolve_call_kinds()`: `if (graph_has_local(g, vn)) continue;` before the `IR_VAR→IR_PROC_VALUE` rewrite. A declared local was rewritten to a record-field accessor proc-value when the field name matched a builtin (`a` from `record u_cset(a)`). Fix: declared local/param always shadows any global/builtin/field-accessor.

- **✅✅✅✅ FOUR RESUME/VALUE FIXES (2026-07-19, Claude Sonnet 4.6) — SCRIP `abce8b1d`. Icon 241/20/32.** (1) Generator in non-last call arg (`lower_call` `chain_live` gate). (2) `\!gen` null-filter re-pump (`IR_UNOP_TEST` ω→uβ). (3) `image()` string→function upgrade removed. (4) `TT_NUL` list-literal arm. Together: SCRIP-run `interfacegen.icn` BYTE-IDENTICAL to icont oracle.

- **✅✅ ICN-CASE-ALT-CLOSE + runerr() (2026-07-19, Claude Sonnet 4.6) — SCRIP `6f56ca41`. Icon 241/20/32.** (1) TT_CASE: resumable selectors route to `ksel_ω=chain_next` (alternated selector now falls through to next clause). Unblocked `parse_expr11`. (2) `runerr(i,x)` added to `by_name_dispatch.c`.

- **✅✅✅ IS_VARREF_fn REBASE REPAIR + COEXPR PIPELINE (2026-07-19 s92+, Claude Sonnet 4.6) — SCRIP `7e9e414f`. Icon 252/9/32.** `IS_VARREF_fn` DT_N slen 0/1/2 all three forms; 11 sites in `pattern_match.c`. `bb_create` frame bases fixed. `proc/1` registry fallback. Coexpr frame snapshot. 4-way bench queens(10): iconx 233ms / JCON 804ms / SCRIP-m3 1849ms / SCRIP-m4 1832ms.

- **✅✅✅ ICN-GOTO-SURVEY + α-FORCE + DEAD-GOTO REAP (2026-07-18, SCRIP `607974bb`→`71133e8b`, Claude Fable 5) — Icon 246/14/32.** (1) Two dj α-entry tramps unified → `icn_dj_α_entry`. (2) α-force mechanism: `lc_γ_to_α`/`lc_ω_to_α`, edge tag `"α!"` — emitter honors free. dj tramp eradicated; `lower_every mark→b_entry` force-α'd. (3) Sites 8+6: body-less every GOTO + generator-keyword seed GOTO deleted. (4) Dead-goto reap: `dead_goto.c` fixpoint reaps bypassed IR_GOTO nodes. Continuation-channel tramps (sites 2/4/7a) still created-then-reaped; protocol conversion now optional purity.

- **✅✅✅ ICN-MOVE-LABEL-ERAD SLICES 1–4 (2026-07-18, SCRIP `a2ae523e`→`580d230c`, Claude Sonnet 4.6) — Icon 244–245/14–15/32.** `lower_alt`: one IR_DISJUNCTION nary self-state box; σ/φ retag; α-entry trampoline. `lower_if`: committed 2-arm variant, C-fail lands φ-glue, T/E fail=ω-OUT (commit). IR_INDIRECT_GOTO retired globally. `icn_arm_result` wiring-kind filter shared to both. Icon MOVE_LABEL producers = ZERO.

- **✅✅✅ ICN-APPLY-SPINE + FRONTIER-PIN (2026-07-18 s94, SCRIP `a88cb120`) — Icon 243→244/15→14/32. geddump BYTE-IDENTICAL to oracle.** Binary `!` apply: now IR_CALL_VALUE `op_sval="apply"` with `bcps_spine_gen_arm` transfer + once-flag. Frontier-pin: every γ/ω landing pins rsp in act slot `[act+8]`; β restores before `jmp [rsp]`. `~===` → `TT_NIDENTICAL`/`BINOP_NEQV=23`. `is_resumable` gaps closed (TT_BANG_BINARY, computed-callee TT_FNC, unop wrappers).

- **✅ PARSER FIXES (2026-07-18, SCRIP `2e8c7788`) — Icon 242/15/32.** Prefix-`.` deref TT_DEREF; position-free `case` default; canonical `break`-operand via `icn_begins_nexpr`. `ZLS_MAX_GRAPHS` 256→4096.

- **✅✅✅ NCB-1d + GENP SLICE-2 (2026-07-17 s90–s91, SCRIP `496e62f4`→`cf29b23c`) — Icon 205→237→242/15/32.** Det lex procs on jmp-entry; depth-immune rbp-absolute epilogues; regime record `rt_proc_t.jmp_entry`. Generator procs: per-instance pthread stack; `rt_genp_thread_entry` asm thunk; `bb_suspend` yields via `rt_genp_yield`. Fixed: 3× rung03_suspend_* + genqueen + recogn.

## ▶▶ ZERO-FAILURE MANDATE: END STATE = 289/0/0. Re-derive counts fresh from `test_icon_all_rungs.sh` — never from prose.

### ▶ FAIL-ZERO — current live FAILs (21 as of 3c96859c; env math-drift accounts for ~8)

**Cluster A — env math-drift (not code bugs; re-run to confirm):** `rung17_real_arith_*`, `rung19_pow_toby_*`, `rung26_pow_real_pow`, `rung30_builtins_misc_sqrt`, `rung37_math_hello`. These vary run-to-run; re-derive fresh before attacking.

**Cluster B — emit-time IR aborts:**
- [ ] **FZ-B1** — `rung36_jcon_var` — `emit_drive IR_ASSIGN guard: nameless 2-operand assign`. Fix in `lower_icon.c` TT_ASSIGN lvalue path. Also needs `rt_icon_variable(name)` per-proc name→frame-offset table for locals.
- [ ] **FZ-B2** — `rung36_jcon_scan` — `bb_call marshal: IR_VAR arg names a local with no LOWER-granted varslot (TE-4)`. Grant varslot in `ir_drive_slot_assign`.

**Cluster C — SEGV rc=139:** Run under `CSN_NO_SEGV_HANDLER=1` for clean backtrace; MONITOR→bracket→gdb.
- [ ] **FZ-C1** — `rung36_jcon_endetab`
- [ ] **FZ-C2** — `rung36_jcon_fncs1`
- [ ] **FZ-C3** — `rung36_jcon_scan2`

**Cluster D — parse errors (`function call: expected ) (got ;)`):**
- [ ] **FZ-D1** — `rung36_jcon_htprep` (line 160)
- [ ] **FZ-D2** — `rung36_jcon_prepro` (line 39) — *(note: may already be fixed; re-run first)*

**Cluster E — wrong output, ran clean:** MONITOR-FIRST; bracket first divergent event.
- [ ] **FZ-E1** — `rung36_jcon_args`
- [ ] **FZ-E2** — `rung36_jcon_coerce` *(⚠ can emit ~241MB; always `| head -c` when running manually)*
- [ ] **FZ-E3** — `rung36_jcon_mffsol`
- [ ] **FZ-E4** — `rung36_jcon_mindfa`
- [ ] **FZ-E5** — `rung36_jcon_kwds`
- [ ] **FZ-E6** — `rung36_jcon_scan1`
- [ ] **FZ-E7** — `rung36_jcon_string`

### ▶ XFAIL-ZERO — 32 `.xfail` markers to clear
Per marker: (1) remove `.xfail`, run, read real failure; (2) fix SCRIP or fix source artifact; (3) delete marker. Check each — some may already pass (free win).

**Known-reason markers:**
- [ ] **XZ-3** `radix` — bignum: literals > 64 bits need arbitrary-precision ints.
- [ ] **XZ-4** `lgint` — same bignum gap.
- [ ] **XZ-5** `ck` — generative arg to `tab(span-1|0)`; `Image()` needs generator-in-arg.
- [ ] **XZ-7** `profsum` — `next` inside `line ? {}` doesn't restart enclosing `while`.
- [ ] **XZ-8–11** (reserved)

**Empty markers (24) — classify on fresh run, then fix or delete:** `arith btrees case checkfpx collate cxprimes diffwrds errkwds errors evalx every fncs geddump gener image io iobig large misc nargs others prefix recent sets sieve sorting struct toby`.
- [ ] **XZ-E-BIGNUM** — arithmetic cluster (`arith`, `checkfpx`, `cxprimes`).

## Permanent notes

**⛔ ORACLE IS icont/iconx — NEVER INSTALL JAVA OR RUN THE JVM SELF-HOST PATH (Lon directive, 2026-07-21 s121).** Validating SCRIP's Icon front-end does NOT require Java, `jcon.zip`, `jtran`→`jlink`→JVM, or any JCON bytecode execution. That whole pipeline (installing the JDK, building the JVM runtime, running `.class` files) is a TIME SINK and was mistakenly walked end-to-end in s121 before this note. The ONLY sanctioned correctness check is: run the Icon program under `scrip --run` (mode 3) and/or `scrip --compile`+link (mode 4), run the SAME program under Arizona Icon (`icont -s prog.icn -x`, which compiles to ucode and runs via `iconx`), and DIFF the two outputs. Build the oracle once from the uploaded source: `cd icon-master && make Configure name=linux && make` → `bin/icont`, `bin/iconx` (v9.5.25a). For the JCON *compiler* modules (`tran/*.icn`, library modules with no `main()`), the Java-free self-host-equivalent is: `icont` builds the real `jtran`, run it to translate a program to JCON ucode/`.u1`/`.u2`, and BYTE-COMPARE against SCRIP-jtran's emitted bytecode as DATA — no JVM needed to compare bytes. Bottom line: if a step needs `java`/`javac`/`jar`, it is the wrong step.


**⚠ Harness blind spot:** `test_icon_all_rungs.sh` grades stdout only (exit code discarded). Use `/tmp/icon_m3_honest.sh` for crash-aware CLEAN/DIRTY split.

**FZ-B1 `var` detail:** `lower_lvalue_var` has no case for `variable(...)` as lvalue → TT_ASSIGN mints nameless placeholder → emitter abort. Awaits Lon call on name-table form (sealed RO blob vs startup-built table).

**FZ-E scan root (recogn/scan/scan1/scan2):** emitter wires SCAN_MATCH fail-edge to arm-B beta (resume, mid-flight) not alpha (fresh start). Land mine: `emit.cpp` ~L887-904 / IR_SCAN_SEQUENCE case ~L1101.

**Open residuals (not in FAIL-ZERO):** `type()`/`image()` for record constructors return `"function"` not `"procedure"`/`"record constructor N"` — `by_name_dispatch.c` type() DT_E branch. Pre-pinned regressions: geddump/tgrlink → `git revert 7aade169`; ipxref → LOWER-side `lower_alt` arm-interior BFS slot-wiring.

**Baseline (2026-07-01, SCRIP `6a509382`):** PASS=190 FAIL=63 XFAIL=36 /289. **R12 baseline (SCRIP `b404fb95`):** 242/15/32.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet
**Architecture:** `ARCH-ICON.md` · `ARCH-x86.md` · `GOAL-ICON-BB-NATIVE.md`

## Session-close / push protocol
See RULES.md — `scripts/handoff_status.sh` verbatim stdout is the ONLY sanctioned completion claim.
