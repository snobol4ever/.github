# FINDING (2026-07-25, s154, Claude) — HEAT-1 H1b SEQ-GLUE CONSUMER-RETARGET FOLD LANDED (v1, default-ON, `SCRIP_SEQ_FOLD=0` pristine) + PS-3 SLICE-2 RECORD-PEEK DESIGN: the pop is a POINTER RESTORE, not a size add.

## Session heatmaps (profile_box_histogram.sh on rail-gen'd loops, RT_OPT=-O0, cyc-proxy)
calculator-2 (ratio 2.25): xchain 34.2% (7.0M Bcm) · rt_defer_open+close 15.6% · PAT$N_α ≈20% · PAT$N_γ ≈8% · _var_hash+NV_GET/SET ≈12% · __memmove_chk 3.8%.
treebank (1.34): **xchain 60.4%** (16.8M Bcm, 32M D1m) · PAT$N_α ≈26% · PAT$N_γ ≈10% · runtime ≈0.2%.
claws5 (0.75) / json (0.86): 97–100% inside ONE anonymous rt:0x… = the runtime-lazily-compiled pattern blob (RX slab, no nm symbols — the profiler's HEAT-3 SPLICEMAP gap). Same templates emit that blob ⇒ template/driver cuts land there invisibly.
Rail full board this session (bench_sno_rail.sh, fence stems via /tmp input-map copy — the committed rail knows only the 5 base stems; extending it is a 2-line case edit, left undone): calc-1 2.07/fence 1.18 · calc-2 2.25/1.60 · treebank 1.34/**0.76** · claws5 0.75/0.73 · json 0.86/0.87.

## H1b LANDED (SCRIP working tree, THIS session)
Mechanism: `bb_match_sequence.cpp`'s FC (`op_fc_seq`) and SEQ-STATIC (`op_seq_static`) arms emit `as: jmp γ` / `af: jmp ω` — pure trampolines. At the mint (`flat_drive_match_alt`, immediately before its `DRIVE_FILL`), when `fc_seq_on(nd) || (seq_static_on(nd) && g_seq_static_cur)` and port mode ∉ {INSTRUMENTED, ALLOC} (hook-quiet guard — canary/zls2 hooks make stubs non-trivial there), register `bb_label_alias(na_s[i], fold(node_γ))` / `bb_label_alias(na_f[i], fold(node_ω))`. Chase (`bb_label_fold`, 16-hop fixpoint, table reset in BOTH emitter inits) at every jmp funnel, medium-symmetric by construction: `DRIVE_FILL` γ/ω (name+pointer — text bakes names at concat time), `emit_jmp_label`, `bb_emit_patch_rel32` (binary catch-all incl. X-records), `x86_pair_tgt` (the one pair funnel serving both mediums' F-record/text-name paths). Stubs still emitted ⇒ earlier-emitted consumers stay correct unfolded. Files: emitter/emit.h (3 decls), emitter/emit.cpp (core + 4 sites + registration), templates/x86_asm.h (x86_pair_tgt wrap).
**Gates run:** census calc-1 `.s` consumer-jumps-into-pure-trampolines **60 → 30** (labels/jumps counts unchanged 99/559); crosscheck ONE script both modes → failing sets EXACTLY the s153 watermark (m3: test_case; m4: test_case+214/215/216; DIVERGE=3) — zero new failures; smokes 7/7; identity OK vs sbl on treebank-match + calculator-1-match m4; `SCRIP_SEQ_FOLD=0` `.s` **byte-identical to pristine baseline**.
**OWED (not run, do NOT claim):** same-moment interleaved rail A/B OFF/ON (census is topology, not wall); poison-armed crosscheck lane; `.s` regen ×3; the 5-stem rail re-board.
**v2 (the other 30):** forward wires consume before their stub registers. Fix = hoist the γ/ω wire-resolution (emit.cpp ~1978–2088, the SEQRP/SEQFOLD landmine block) into a pre-pass so all chain aliases register before any node emits — OR binary-only pending-patch rewrite is easy but VIOLATES mode-3/4 1:1 (text can't rewrite printed jmps); rejected. ALT-class σ-glue (valueless arms) is a further admission candidate — census the remainder first.

## PS-3 SLICE 2 DESIGN — the record-peek cursor (`[rsp+8]→[rbp+56]`): POP = POINTER RESTORE
Ground truth (emit.cpp:2173): the suspended-activation record is `[rsp+0]=γ-resume-address, [rsp+8]=the suspended activation's frame reg (region base)`. Therefore the defer leaf's exhaust/unwind arithmetic needs NO size at all: **restore rsp from the recorded base** (the jmp-entry contract's own register), instead of `add rsp, SUSP-imm` (the s153 compile-time price at bb_match_defer.cpp:168). Consequences, in order: (1) the registry-UNIFORM gate dies — arriving blobs of ANY footprint restore exactly; (2) RECURSIVE targets (treebank `*group`) admit — each activation's record carries its own base; (3) `zls_fct_finalize` late-pass no longer needs the SUSP price for pure-defer bodies (mixed static+defer bodies keep static parts priced). Mechanism sketch: tail/defer α mirrors the peeked base into the fixed header slot `[rbp+56]` (the "cursor") so β/fail-glue read it without a live record on top. THE ONE OPEN RULING (write BEFORE implementing, per H2d precedent): the ε-resume PHANTOM PAD has NO record ⇒ no base to restore — the zero-guard (`[rsp+0]==0`) must still consume a pad share; options: (A) pad share stays a FIXED per-leaf placeholder size (pad exists only for the cascade arithmetic; real activations never use it) — cleanest, keeps `op_tail_dfr` zeroing; (B) tail α writes sentinel bases into the pad. Recommend (A). Canary first: 182 + a NEW recursive-target arbno canary (the `*group` shape reduced) BOTH modes, then treebank rail — the FINDING-s153-predicted 58% lever.

## Container facts
valgrind absent by default (`apt-get install -y valgrind` works). Rail's `$HERE/../scrip` default breaks for /tmp script copies — export `SCRIP=` explicitly. Same-parent corpus/SCRIP layout satisfied at /home/claude.
