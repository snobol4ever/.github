# GOAL-CTO.md — THE CTO SEAT: THE HARDEST PROBLEM, HANDS ON THE CODE (opened 2026-09-06 11:19 CDT on Lon's word)

Lon, in-chat to ceo, 2026-09-06, verbatim: *"So maybe let's have a CTO to work the hardest problem and you will remain CEO. Then those will both be Fable 5.1. I like that."* · *"I will stop here and rename two folders: /home/claude_ceo and /home/claude_cto."*

## LIVE CURSOR
- **CTO-29 (2026-09-12 13:5x CDT, cto; MODE TRIO, CEO-617 lane: THE PROLOG ISO LADDER) — RUNG 1 LANDED: SCRIP `863ebc2a9` (on the ceo's `3e99f5306`), corpus `5b144b436`, .github this entry; FINDING `FINDING-2026-09-12-cto-prolog-iso-rung-1-a-directive-assert-was-never-marked-dynamic-a-call-local-cut-pruned-its-caller-and-the-database-builtins-refused-instead-of-raising.md`; row `prolog-iso-rung-1-database-builtins-iso-errors-and-call-local-cut` (rank 0, DONE-WHEN = `scripts/test_gate_pl_iso_rung1_database_builtins_and_call_local_cut.sh`, wired into make test).** Three lowerer defects: (1) a DIRECTIVE assert was never marked dynamic (`!_isdir` since be912e760) so `:- assertz(foo(1)).` raised permission_error(modify, static_procedure) — the shape of every INRIA assert goal; (2) a cut inside call/1 pruned its caller — cuts now carry their barrier SCOPE id in ival, the disjunction and conjunction wirers prune only their own scope, and the meta-call path returns one IR_GOTO callω plus the goal's real redo (`cx->meta_redo`); (3) the database builtins REFUSED at compile time on literal non-callables and builtin heads instead of raising the ISO term (type_error(callable), permission_error(modify|access), type_error(predicate_indicator), abolish's atom/integer/not_less_than_zero/max_arity), and a number in goal position now raises before anything runs. Plus max_arity=1024 as an integer (was the atom 'unbounded') with functor/3's representation_error, and 1.0 @< 1. MEASURED both modes vs the coo's 15:59 board: asserta_1 4→16/24, assertz_1 4→16/24, abolish_1 5→12/16, retract_1 7→13/18, retractall_1 6→10/15, clause_2 8→11/16, current_predicate_1 4→7/23, cut_0 13→16/21, call_1 15→16/20, term_comparison 35→36, compare_3 21→23, functor_3 20→21; INRIA per-goal probe 40→16 red of 445 (3 errata, 7 the runner's own driver helpers); ladder 540→542/568; smokes prolog/polyglot/snobol4/icon green; make test = origin's standing set, nothing new. ALSO LANDED (Lon 2026-09-12: *"Has a full feature attribute TSV/CSV file been created for all the Prolog test suites"*): `scripts/util_prolog_iso_attributes.py` → `corpus/packages/prolog/{logtalk_iso,inriasuite}/ALL.csv` (3617/445 rows, 254 feature flags, progress-database entry names), `--check` the staleness gate, `--triage` the board join — the Logtalk failure set by section reads format_3 207 · format_2 151 · write_term_3 142 · builtins 113 · unbounded 83 · operators 49 · encodings 49 · stream_property_2 46 · op_3 34 · open_4 33 · read_term_3 32 · setof_3 30. NEXT (rung 2): the runtime-unknown head — a runtime key→store map beside the 64 root cells and meta-call wrappers for the database builtins (`asserta(_)`, `four(F), asserta(F)`, `abolish(foo/X)`, `clause(_,B)`, `current_predicate(F/1)` and `call(assertz(foo(1)))` → existence_error today); the `$fc/3` helper unreachable from a meta-called `,`/2 inside findall/3 is filed in the FINDING. FIRST, per CEO-620: cherry-pick fbf0f675e onto main with make test + the SNOBOL4 and Icon smokes.
- **CTO-28 (2026-09-12 CDT date-read, cto; MODE EXECUTIVE since 10:41, ceo holds src/runtime src/lower src/templates src/emitter per CEO-613) — THE REGRESSION TICK: COO-62's THREE REDS ON MY TWO LANDINGS ARE CURED WITH NO TRADE IN TAG `retired/cto-regression-cure-2026-09-12` (= `fbf0f675e`, readable on origin), HELD FOR THE CEO'S LAND/REVERT/HOLD WORD; corpus `5e77e46c4`, .github this entry.** (1) `arbno_pos_rpos_branch_1` (deferred capture inside `ARBNO(*cmd)` came out empty): my r12 rollback at the recede double-counted the body's own pend rollback — deleted. (2) `arbno_fence_pos_branch_3` (`ARBNO(FENCE('a' | 'ab') | 'c')`, hang then wrong answer): the lowerer DROPS `FENCE(P)` to `P` inside an ARBNO body (`sno_pat_node` TT_FENCE, `!sno_in_arbno`); the old concede hid it, the recede exposed it; the per-instance snapshot removed the reason for the exclusion, so it is lifted and the oracle's f1/f2/f4 probes agree. (3) `fence_break_rem_branch_1` (SEGV at T7 `FENCE(('a' | 'x') . C1)`): 61553ed9e's pushed β cell changed the flat alternation's γ footprint (32→48) under the FENCE1 U2 release's static `+32`; the cell is now pushed ONLY for an arm that leaves live frames at γ (`alt_arm_complex`: nested alternation, ARBNO, DEFER, FENCE) and a primitive arm keeps the in-place write, so the footprint every static consumer assumes stands — measured: the three reds PASS both modes, the 13 CEO-564 flips all kept, ARBNO probe no movers, nested-alt probe one mover HANG→PASS, both rows' DONE-WHENs rc=0, right-sealed 17/17 and every gate I touch green (two intermediate forms — the actframe gate and the 16B record — were measured and rejected because each traded two programs). Also in the tag: `image/1`'s three pinned allocations → `rt_heap_alloc_c` (CEO-605's measurement found every `image()` call leaked a pinned block; micro.icn heap-exhausted where iconx stays at 3.9 MB; row `icon-every-image-call-leaks-a-pinned-block-…` stays OPEN: string/integer images no longer exhaust, `image([])` still leaks elsewhere, and my RSS bound met the 512 MB heap ceiling, not a leak). CEO-605 GC row: MEASURED, zero-reclaim regenerations reach NO kernel (23 SNOBOL4, 19 Icon, 39 demos); stays open rank 2 as ruled. CORPUS LESSON, paid twice: an XFAIL marker lives in FOUR places — ALL.xfail, the ALL.sno banner, the ALL.csv column AND the ALL.ref banner — 69c63f976 touched one, 1e3459762 three, and the master pair's banner mismatch refused the two ast-refs gates (preflight 2 red, mine) until 5e77e46c4. Three landings on main this sitting (`77779d96b`, `97667e4fd`, `61553ed9e`); the fourth waits on the ceo's word. NEXT: on LAND, cherry-pick the tag onto main with the FINDING per claim and close the tick; on HOLD/REVERT, act as told; then the image row's list leak and the fourth ARBNO mechanism (FENCE1 rsp restore from a zero cell).
- **CTO-27 (2026-09-12 CDT date-read, cto; MODE NONET, order of work icon,snobol4,prolog) — A NESTED ALTERNATION RECEDED INTO PING-PONGED BETWEEN ITS TWO β PORTS: SCRIP `61553ed9e` = origin/main, read back; row `snobol4-a-nested-alternation-receded-into-after-its-inner-arm-succeeded-ping-pongs-between-the-two-beta-ports-forever` (minted on my measurement, CEO-605 assigned the residue) closed by computed `done`.** CEO-564's third mechanism was mis-named by the fuzzer: `arbno_fence_span_replace_branch_2` has no ARBNO and no FENCE; a variant ladder isolated it to `'bz' ('a' | ('b' | 'c')) 'y'` (SPITBOL nomatch, we hung, inline and deferred). ALT-FLAT addressed its 32B record as `[rsp+k]` from every port, so with the inner alternation's record still on the stack the outer's σ stub wrote its β target into the inner's cell and both β ports jumped to each other — the live PC under gdb on both witnesses was that `jmp [rsp+8]`. CURE: the σ stub pushes a 16B cell holding the β target at the alternative's own γ-time stack top and β pops it; ω and the next-alternative stubs unchanged; rbp arm untouched. GATE `test_gate_a_nested_alternation_receded_into_resumes_its_own_arm_not_the_outer_beta` WIRED 16/16, pins re-verified live against SPITBOL at pin time; controls include recede-then-WIN. DONE-WHEN rc=0; 48-program nested-alternation scratch probe one mover HANG→PASS both modes, 0 regressed; 127-program ARBNO probe no movers; smokes, preflight, ratchet, right-sealed gate all green. FOURTH MECHANISM NAMED, not cured: `fence_arb_span_replace_branch_1` SEGV — SPAN β entered with rsp==0 after `fence1_β: mov rsp,[rsp+144]` restores from a zero cell; its own row. CEO-564 tally: 4 + 5 + 1 cured, 1 remains, 1 unmeasured. Three landings this sitting (`77779d96b`, `97667e4fd`, `61553ed9e`). NEXT (CEO-605): the zero-reclaim GC-scheduling row on hq_V's finding, MEASURED FIRST over the SNOBOL4 and Icon benchmark kernels with `SCRIP_ZETA_TELEM=1`, hq_U co-signing; the FENCE1 rsp-restore SEGV after it.
- **CTO-26 (2026-09-12 CDT date-read, cto; MODE NONET, order of work icon,snobol4,prolog) — AN ASSIGNMENT WHOSE TARGET IS A VALUE IS ICON'S RUN-TIME 111: SCRIP `97667e4fd` = origin/main, read back; row `icon-an-augmented-assignment-to-a-comparison-aborts-at-the-binop-guard-instead-of-raising-111-at-run-time` (minted on the coo's fev.icn report, Icon outranking the SNOBOL4 residue) closed by computed `done`.** The IPL compile tier's `gprogs/fev.icn` aborted rc=134 at the BINOP operand-slot guard under the runner's ICONPATH; the node was a BINOP with ZERO operands from `(1 < focuswidth) -:= 1` — `TT_AUGOP`'s fallback for a target that is neither a `TT_VAR` nor an lvalue shape built the BINOP and never pushed its operands (the second BINOP shape after the IR_FAIL fold of `38382dc58`). Oracle: a comparison yields a VALUE, iconx raises 111 "variable expected, offending value: 5" at RUN time with the operation computed, and a failing comparison fails the expression silently. The witness set found the second face: plain `f() := 3` died in `rt_assign_var`'s `[IDX] BOMB` — the sink my own `12971dfc9` had called "already runtime-checks DT_N"; it checked, then bombed. CURE: the augmented fallback lowers the target once and wires it as the lvalue branch does (`IR_ASSIGN_VAR(target, BINOP(op, DEREF(target), rhs))`); the sink raises `core_icn_error(111, var)` and returns FAIL under `core_icn_active()` (the guard arithmetic.c uses), BOMB kept for other frontends. GATE `test_gate_an_assignment_to_a_value_raises_111_at_run_time_instead_of_aborting` WIRED, 16/16 (8 probes × m3+m4 pinned from iconx + 8 sink-absence arms; g02 failing-comparison, g07 variable, g08 subscript are the controls); pre-cure red measured by hand on the same programs — the gate's own fail-once run was REFUSED by the freshness preflight (snapshot older than the tree) and that is recorded, not worked around. Verdict: DONE-WHEN rc=0 (fev both modes + witness + control), a_fail_operand gate 16/16, preflight 33/0, wiring ratchet 28/0, Icon smoke 15/15 ×2, SNOBOL4 smoke 7/7, right-sealed gate 17/17. fev.icn itself stays UNGRADABLE at run time (no graphics oracle contract). NAMED, not cured: the traceback FRAME text (`{5 := 4}` vs the assign box's frame) is the traceback-voice class. FINDING-2026-09-12-cto-an-assignment-whose-target-is-a-value-… NEXT: `s4e_msg.sh next` for the lane's top row; the two FENCE-shaped ARBNO residuals stay named in the closed ARBNO baton.
- **CTO-25 (2026-09-12 CDT date-read, cto; MODE NONET, order of work icon,snobol4,prolog) — THE ARBNO FRAME ARM'S RECEDE LANDED ON AN INSTANCE THAT NO LONGER EXISTED: SCRIP `77779d96b`, corpus (nine XFAIL markers retired) — both = origin/main, read back; row `snobol4-arbno-frame-arm-can-never-recede-into-its-own-body-the-exhaust-branch-is-deleted-with-its-rollback` closed by computed `done`.** The 09-11 finding's premise was half right: the recede branch was compiled out only for the INNER box (bare-TAB body), the outer already receded — and its "variant B hangs c_nodefer" does not reproduce on HEAD in either mode. The real mechanism, hand-traced on HEAD's asm with NO defer in the witness: `bb_match_arbno_frame()` kept ONE rbp cell per BOX (start/mark/r12) and every body member's frame slot is per BOX too, so outer instance k+1's α overwrote the inner ARBNO's cell and TAB's saved cursor, and the recede into instance k's body β (CEO-589's port, which is right) resumed an already-exhausted instance forever. CURE: the rbp slot holds only a chain HEAD; α and every COMMIT push a cell on the machine stack below the body's live frames (`start@0 mark@4 r12@8 prev@16` + a snapshot of the BODY's slot window — the contiguous 16-byte-unit run `arbno_body_slot_window()` computes with `frame_slot_scan()`'s own walk); EXHAUST pops the top cell, sets the cursor from its mark, concedes on the α cell, else restores the snapshot and recedes with rsp exactly where that instance's γ left it. The second face was found by the first cut (chain alone SEGV'd the control inside the deferred pattern's own ARBNO; a gdb port trace showed TAB β restoring its cursor from the overwritten leaf slot) — which is why the snapshot exists. MEASURED, output vs spitbol, m3+m4, -O0, never rc: DONE-WHEN rc=0; CEO-564's 12 red programs + 35 shape witnesses (scratch, not a board) 13 flipped SEGV/HANG→PASS/PASS, 0 regressed; all 127 corpus programs containing ARBNO (scratch copies) one mover `nested_arbno_rpos` SEGV→PASS both modes, 0 regressed; right-sealed-defer gate 17/17; preflight 33/0. Of CEO-564's 12: 4 cured at `51add4eec`, 5 here, 2 remain (`arbno_fence_span_replace_branch_2` HANG, `fence_arb_span_replace_branch_1` SEGV — the third mechanism, FENCE-shaped), 1 unmeasured (`array_replace_branch_2`, -INCLUDE). The coo's XPASS=4 telegram answered by retiring the four markers (their cure was CTO-24's) plus today's five, each with its measurement in the commit; the coo's next pass is the reading. FINDING-2026-09-12-cto-the-arbno-frame-arm-kept-one-cell-per-box-… NEXT: the coo's `fev.icn` BINOP-guard abort under the IPL runner's ICONPATH (Icon outranks; queued behind this landing by my own word), then the two FENCE-shaped residuals.
- **CTO-24 (2026-09-11 21:3x CDT date-read, cto; MODE NONET, order of work ICON/SNOBOL4/PROLOG per CEO-567) — TWO LANDINGS: THE GATE THAT WAS REDDING `make test` FOR TWELVE OF THIRTEEN SEATS (`9fb1cb35e`, CEO-547 part 2) AND FOUR OF THE TWELVE CRASH/HANG PROGRAMS (`51add4eec`, CEO-564) — both = origin/main, read back.**
  (1) **CEO-547 was never the guard, it was the gate.** `test_gate_harness_refusal_is_rc2.sh` arms 4, 6, 7, 8 and 9 each GRADED A REAL CORPUS MASTER, and ONE RUNNER, ONE BOARD (CEO-523) correctly refuses a run whose population is under the corpus tree to any seat but the coo — so those five arms collected a ONE-RUNNER rc=2 instead of the behaviour they assert. Part 1 narrowed the guard by population; this is the other half, and it belongs in the gate: an acceptance gate for a refusal CODE has no business grading the shared corpus. ⛔ TWO OF THE FIVE READ **GREEN** ON THE WRONG rc — arm 5 asserted only `rc=2` and the one-runner refusal is also 2; arm 8 asserted only that `has suffix` was absent, which it is in any refusal that never reached the suffix check. Both now assert what the refusal SAYS. ⛔ AND ARM 4's rc CHECK WAS DEAD CODE: a trailing backslash joined `then ck ok "…"` to `else ck no "…"`, so the else became four more ARGUMENTS to `ck ok` and the `if` had no else branch — on the failing path nothing ran and nothing was counted. The banner claimed 16 checks while 15 performed. 18/18 now, cost unchanged.
  (2) **Row `snobol4-a-right-sealed-defer-tears-down-its-zeta-frame-twice` (minted and claimed on my own measurement after CEO-564 assigned the 16 CRASH + 8 HANG class).** Found ASM-DIFF-FIRST on a pair one FENCE apart. A `MATCH_DEFER` whose pattern is RIGHT-SEALED — rightmost element a FENCE, so receding into it can never yield another alternative, `sno_pat_right_sealed` sets `nd->seal = 1` — is given a ζ-ACTIVATION FRAME: α does `push rbp; mov rbp,rsp`, and BOTH exits tear it down, γ at `L(4)` and ω at `L(5)`. **The β port tore it down a second time.** β is only ever entered AFTER γ has already popped — it is the consumer downstream of γ receding back in — so by then `rbp` holds the CALLER's frame base: `mov rsp,rbp` moves rsp somewhere wild, `pop rbp` takes a word off it, and the box jumps out with rsp and rbp both corrupt. That is why it never looked like the stack exhaustion an unbounded ARBNO would produce: rsp still high, rbp 0, control inside the mode-3 slab. Three lines deleted. FLIPPED, graded on OUTPUT against spitbol in both modes and never on rc (CEO-564, on hq_S's measurement that a crash row had been "cured" into a silent truncation every rc-based predicate passed): `arbno_fence_pos_replace_branch_2`, `arbno_fence_rpos_replace_branch_1`, `fence_arb_tab_replace_branch_1`, `fence_arb_tab_replace_branch_2` — four programs, eight snobol4-master cells, CRASH → PASS.
  **THE 24 CELLS WERE 12 PROGRAMS, NOT 24** (latest reading per program × mode since 09-08, from the progress DB): 8 CRASH programs × 2 modes + 4 HANG × 2. Four are cured. ⛔ THE RESIDUE IS A SECOND MECHANISM AND IT IS NAMED, NOT IMPLIED — none of `arbno_bal_tab_replace_branch_1`, `arbno_fence_tab_replace_branch_1`, `arbno_span_tab_replace_branch_1`, `fence_arb_span_replace_branch_1` (still SEGV) or `arbno_fence_span_replace_branch_2`, `arbno_pos_rpos_branch_81`, `arbno_span_break_replace_branch_1` (still hang) carries a right-sealed defer; `array_replace_branch_2` is the one `-INCLUDE` program and is unmeasured. **I keep the row.**
  GATE: `test_gate_right_sealed_defer_beta_no_double_teardown.sh`, WIRED into `make test`, hermetic (mktemp witnesses, grades no corpus population, so not a board — every seat can run it), 17/17, **proven red before the cure at 13 of 17 with both controls still green**. The two controls are load-bearing: `G0 = LEN(0)` (seal=2, no ζ-frame) and inline `ARBNO(FENCE(LEN(0)))` (no DEFER at all) are the same shape one FENCE apart — a cure that moved them would be one that stopped BUILDING the frame rather than one that stopped popping it twice.
  CONTROL ARM on every other frontend: all 39 `test_smoke_*.sh` run with the change and without it on the same tree — verdicts BYTE-IDENTICAL, nothing moved either way. No board run anywhere; ONE RUNNER still binds.
  ⛔ **`make test` IS STILL RED AND NOT BY EITHER OF THESE.** It now reaches `test_gate_baton_donewhen_runnable.sh`, red at 90 uncloseable rows against a ceiling of 89 — and the two newest rows in its list were minted MINUTES earlier by other seats with prose DONE-WHENs. That ceiling reds the whole fleet's blocking set the moment any seat mints a prose criterion. Reported to the ceo as a standing race; not patched here, because lowering a ceiling I did not earn is how a ratchet stops ratcheting.
- **CTO-23 (2026-09-11 20:3x CDT date-read, cto; MODE NONET) — TWO ROWS CLOSED: A RE-ENTERED CALL REPORTS ITS OWN CALL-SITE LINE (`a1c6c96be`, corpus `a9dbae261`) AND THE ONE-RUNNER GUARD STOPPED REDDING THE BLOCKING SET FOR TWELVE SEATS (`522d6f8cc` + `4d74c1a77`) — all = origin/main, read back.** (1) Row `icon-a-traced-call-after-a-generator-resumption-reports-the-line-last-executed-not-the-call-site` (mine, minted on my own measurement after the lane read empty) closed by computed `done`: `g_line` is stored by IR_LINE_MARK boxes emitted once per STATEMENT, and a call re-entered through the recede edge — its argument generator resumed, or an enclosing every/alternation driving it again — never passes its statement mark twice, so the &trace call line AND the traceback's caller line both read whatever line ran last (`every foo(1|2|3)` traced foo(2)/foo(3) at foo's own `return` line; with NO &trace at all the traceback read `foo(3) from line 7` where icont reads 2). One site in `lower_call`: every γ edge into the call node enters through a LINE_MARK carrying the call site's line — the callee leaf's line, since the parser sets none on a TT_FNC node. NOT gated on `cx->want_lines`, deliberately: the traceback witness mentions no &trace and such a gate leaves it red, and that arm is the load-bearing one in the new gate. 90-program arizona mode-3 sweep before/after moved `tracer` 52 → 40 and NOTHING ELSE (71 green before, 71 after, no program worse). COST NAMED NOT HIDDEN: a program that is nothing but 3M user-procedure calls costs ~8.6% more wall at -O0 (0.1414 → 0.1536 s/run); the cheaper shape — the line stored inline in the call box instead of a box of its own — needs the line on the call node and is a follow-up, not that row. (2) CEO-547 part 1: `_one_runner_guard()` ran at the top of `cmd_run` BEFORE it knew what was being graded, so a gate's own two-entry mktemp fixture was refused rc=2 exactly like a 763-entry master and `make test` was red on a clean origin tree for twelve of thirteen seats. What makes a run a board is THE POPULATION IT GRADES, not the entry point: the guard now takes the suite it holds and fires only under the corpus tree, mirrored in both copies; called with NO suite path it still judges the run a board, so a caller that does not say what it grades is refused rather than waved through (arms 7/9/12 are controls asserting exactly that). modes_declaration_travels FAIL(1) → PASS(0); capture_stdin_and_red_exit green; harness_refusal_is_rc2 6 red → 4. ⛔ AND THE UGLIER FIND UNDER IT: `test_gate_one_runner_one_board` read 6 of 14 red inside a computed `done` and 0 of 14 from a bare shell ON THE SAME TREE, because the bus exports `S4E_DONE_WHEN_RUN=1` and that is an exemption from the guard six of its arms measure — a verdict that measured its caller, flipping in the FLATTERING direction for the invoking seat. It pins its environment now; arm 13 asserts the pin survives the next edit; 15/15 both ways. The gate grew 9 arms → 15, watched go both ways (4 red on a control with only the guard copies reverted). ⛔ NAMED, NOT CURED: `test_gate_harness_refusal_is_rc2` keeps 4 red arms because it runs the REAL Rebus corpus master — part 2's shape applied to a gate the ruling did not name — and `test_gate_harness_transitive_companions` keeps the 1 HANG it had on origin. ⛔ CEO-547 PART 2 IS HELD ON ITS OWN CONDITION: the per-entry identity gate comes out of make test only in the SAME push as the coo's board-side wiring (it is the only instrument that can see a count-neutral swap); the full handover went to the coo and no answer has come. NEXT: the coo's answer on part 2, then whatever slice of the trace class the ceo cuts me — arizona var stays mine and reads green both modes, hq_R graded it independently and agrees.
- **CTO-22 (2026-09-11 0x:xx CDT date-read, cto; MODE NONET) — ARIZONA var BYTE-IDENTICAL IN BOTH MODES: SCRIP `380086a18` = origin/main, read back.** The display row's named residue (main's own parameter) was a NAME gap, not a slot gap: the root activation's base and (k+1)*16 were right, main has no procedure record so `rt_proc_pname` was null and the slot was skipped. Parameter names now live in the registry beside the local names (copied at registration — the graph's strings were dead at display time and the name printed empty), mode 3 from the emitter, mode 4 as an emitted table with a startup call. hq_C's named 203 site folded in: BINOP_POW's big arm raises value-less as Arizona's fmisc.r:573 does. hq_R's generator-prologue LCL-SEED (`43c46e25b`) is in my neighbourhood and agreed. var 33 → 1 → 0 across the three display landings; display, generator-record and argument-type gates PASS; smokes green; preflight 33/0; ratchet PASS. NEXT: the ceo's answer to my ask for the next named bug; nothing owed on the bus.
- **CTO-21 (2026-09-11 00:xx CDT date-read, cto; MODE NONET) — A STANDALONE BINARY REGISTERS ITS PROCEDURE NAME METADATA: SCRIP `6c453e4d0` = origin/main, read back; corpus Icon `.s` regenerated.** Row `icon-a-standalone-binary-registers-no-procedure-name-metadata-so-display-and-frames-have-no-names-in-mode-4` (mine, minted on hq_V's two green-m3/red-m4 display witnesses) closed by computed `done`: mode 4 emitted `rt_proc_register_rec` with pnames=0 and no local names or offsets, never noted a global, and a main-only program got no `module_init` at all. Per Icon graph the driver now emits the parameter-name array into the record's empty quad, a local-name array and a `.long` array of vslot offsets (the same map mode 3 registers from the emitter) with two startup calls; the root graph through `polyglot_main_bb_idx`; every lowerer-noted global re-noted at startup; the guard admits an Icon root with names. Both witnesses byte-identical both modes; Arizona var m4 83 → 1 = m3; the generator gate grades its display witness in mode 4 too; seven smokes green as the shared-edit control arm; preflight 33/0; ratchet PASS. Five rows closed this sitting (traceback, argument-type discipline, display locals, generator record, mode-4 names) plus the ||| regression cured. NEXT: whatever CEO-532's lane names; the one named residue is main's own parameter in display (the root graph's parameter is not at (k+1)*16 from the tap base).
- **CTO-20 (2026-09-10 23:5x CDT date-read, cto; MODE NONET, nine HQs; CEO-526/532/533/534/535) — THREE LANDINGS: DISPLAY READS EVERY ACTIVATION'S LOCALS (`8a166bc95`), THE ||| BY-NAME REGRESSION CURED (`8d6febf51`), A GENERATOR RECORDS ITS ACTIVATION (`3e68226ea`) — all = origin/main, read back.** (1) Row `icon-display-reads-a-procedure-s-locals-per-activation` closed by computed `done`: the one-pointer estimate did not hold (the emitter's nlocals reads 5 where four are declared; named locals sit at the low end of the region in reverse order and even that is not general) — the map exists only in the compile-time vslot table, so each local's OFFSET is registered beside its name where the layout is built (the driver's registration runs before layout and sees an empty table); display reads base+offset for every displayed activation, prints the statics, and images values in display's own form with the sixteen-character elision; Arizona var 33 → 1 (main's own parameter, named residue); mode 4 has NO per-procedure name metadata at all (83 differing lines, its own row). (2) CEO-533: giving `|||` its own IR code left the by-name spelling on the string sink — one line, the gate grew the by-name shapes, coexpr 29 → 9. (3) CEO-534/535, the ceo's FINDING only-main-records-an-activation, one root under three lanes: the generator prologue ran neither the trace event nor the record; it records now (record-ONLY kind-4 tap, split from the CALL event so trace output stays byte-identical — the first cut moved coexpr 9 → 18 and was reverted to the split) under the level &level already counts; three-frame traceback main()/f(3)/g(3) byte-identical to icont both modes, a generator's display prints its locals; gate wired. Five rebases onto a moving origin this landing; two Makefile duplicates from stacked hunk resolutions dropped before the push (ratchet PASS). Rows closed this sitting: traceback (CTO-18), argument-type discipline (CTO-19), display locals, generator record. NEXT: the ceo re-measures the suspend/resume class on `3e68226ea`; mine is whatever CEO-532's lane names next — the m4 name-metadata gap is the natural row (display and the traceback's frames in a standalone binary).
- **CTO-19 (2026-09-10 22:3x CDT date-read, cto; MODE EXECUTIVE, four officers; CEO-516/520/523/524/526/527/529/530/531) — ICON'S ARGUMENT TYPE DISCIPLINE LANDED: SCRIP `5af157f79` = origin/main, read back.** Row `icon-argument-type-discipline-an-operation-coerces-where-iconx-raises` (mine, minted and closed by computed `done`). Icon converts freely between string, cset and number and refuses everything else; this engine coerced everything, so a program under `&error` conversion saw wrong values instead of converted failures. Eleven sinks, each measured against icont 9.5.25a first: `|||` was LOWERED AS `||` (BINOP_LCONCAT is its own code now, 108 unless both operands are lists, and the box CONCEDES instead of storing a failed descriptor — that store was what turned the next subscript into a SPITBOL 235); `||` 103; the string relations 103 and their result is the string they compared (the rtx fast path returned a number unconverted); `*` 112 for a null or a procedure (rtx fast path answered 0); `?` 113; subscript 114 at both entries; a call of a name that is neither procedure nor builtin 106; and the argument signatures of pos/tab/move/right/left/center/repl/trim/close/seek/where/display through ONE table at the builtin dispatch. THE ENGINE HOLE, separate and mine: with no operator overload defined, `rt_relop_overload` returning 0 jumped to the RAW INTEGER COMPARE, so `1 <= [1]` compared an integer against a list pointer and succeeded — it falls to the runtime comparison now, which raises 102. A failed value passes every check untouched (the coercion sink was raising 102 on a FAILDESCR). Language identity stops at the parser: operator sinks are chosen by IR code, shared sinks ask `core_icn_active()`. Arms: new gate `test_gate_icn_argument_type_discipline.sh` WIRED into make test (16 refusals + 24 conversions, both modes, plus a SNOBOL4 control arm); `rung36_jcon_errors` 470 → 171 differing lines with all 75 expression values correct; seven language smokes green (icon 15/15, snobol4 7/7, prolog 5/5, pascal 9/9, raku 10/10, rebus 4/4, snocone 5/5); preflight 33/33; comment gate 0. No board of mine: MODE EXECUTIVE gives the board to the coo (Lon's ONE RUNNER word, read mid-sitting). ALSO THIS SITTING: CTO-18's traceback landed (`a5683281b`); CEO-520's stale 21 h waiter of mine killed by pid; the display-locals row measured and REPORTED as a registry rather than the estimated one pointer (tap experiment reverted, var.icn segfaulted on the emitter's local count; the measurement is on that row's baton). NEXT: the `&error` residue (`&errorvalue` text and one 116 in rung36_jcon_errors), then display-locals if the ceo hands me the registry; coexpr, the trace-shape class and transmit are the ceo's and the cfo's by CEO-529/531.
- **CTO-18 (2026-09-10 17:xx CDT date-read, cto; MODE NONET → EXECUTIVE mid-sitting; CEO-516/520/523) — AN ICON RUN-TIME ERROR PRINTS ICONT'S WHOLE TRACEBACK: SCRIP `a5683281b` = origin/main, corpus `b922682b8` (Icon bench `.s` regenerated), read back.** Row `icon-fatal-report-per-frame-traceback-with-expression-text-and-per-file-names` (minted on CEO-516's correction, mine) landed: the prologue's entry tap was guarded by `&trace`, so the runtime knew no activation; it is unconditional now (the guard inside the hook), `main` gets the same tap, and the hook records `{name, base, np, caller line, caller file}` in a table indexed by the `rt_k_level` the prologue already increments — the epilogue's decrement is the pop, no exit hook, no new global. Builtin frames ride a C-stack chain around `try_call_builtin_by_name_bl`; the operator reaches the sink through the coercion node's code word (`COERCE_OP_*`, set by the Icon lowerer), the arithmetic slow path, `rt_div_zero` (201/202 as iconx) and the relational sink; one printer serves both Icon sinks; 16-char image truncation; 204 without a value. DONE-WHEN PASS both modes (re-cut to a direct two-mode diff: the ONE RUNNER guard `018d8e00d` refuses the jcon runner to a non-coo seat); eight of eleven icont witnesses byte-identical both modes; Icon master 756/759 (pre-rebase, the three standing reds); preflight 33/0; comment gate 0. Residues named in the FINDING: the call-site line after a generator resumption and generator frames themselves (CEO-468's hook), `*&null`/`"a" ||| 1`/`5.0 / 0` failing silently, the stale Prolog bench `.s` (dropped from this landing). Sitting also: CEO-520 one stale 21 h waiter of mine killed by pid; Lon's ONE RUNNER word read mid-verify — my background `make test` stopped, no board of mine since. NEXT per CEO-523: (1) `procedure_record_every_replace_12` scan_match memcmp crash by assign, (2) ichartp's keyword reversible-assignment abort, (3) the Zona tracer/transmit/coexpr/errors CRASH class.
- **CTO-17 (2026-09-10 11:0x CDT date-read, cto; MODE NONET; CEO-465/498/501/504/506/513) — A GENERATOR BODY RESTS AT C PARITY: SCRIP `484f2fb2c` = origin/main, corpus `eba05dbc8`, read back.** Row `icon-a-suspend-plus-a-call-through-a-procedure-value-segfaults-and-diffn-diffu-now-reach-it` (hq_R's mint) landed: `bb_call_value.cpp` padded its two runtime calls by 8 in the generator regime (`5f4b2d4c2`, 2026-08-29, when a generator body rested at 8 mod 16); the body has rested at 0 mod 16 since the N-2 word / CFO-36 / `d78d0bfca`, and the box is ENTERED BY JMP (hq_S's FINDING, cited by filename), so the pad entered the runtime at 0 and `rt_proc_enter`'s seven pushes handed the callee a frame at 8 -- glibc's `movaps` in `snprintf` faulted. The pad and its predicate are deleted; the claim (two sites, held by memory) is now held by `test_gate_icn_generator_regime_calls_the_runtime_at_c_parity.sh`, wired, four witnesses x two modes against icont, red on `db870418b`. hq_U's A/B: diffu, diffn, geddump flip byte-identical to their `.std` both modes. Arms: Icon master 756/758 both modes (the two standing reds), SNOBOL4 master 1893/1917 FAIL=0, IPL 89/89 with diffn/diffu green, geddump on `0 INDI` at icont's own 103, five smokes, comment gate 0. Artifacts regenerated (corpus). FINDING named for the claim. NEXT per CEO-513: the m4 compiler abort and the fatal-report follow-through (per-frame traceback lines, `g_file` for linked procs); the rtx suspended/resumed trace hook (CEO-468) after.
- **CTO-16 (2026-09-10 10:0x CDT date-read, cto; MODE NONET; CEO-452/471/489) — THE FATAL-REPORT IDENTITY LEAK IS CURED: SCRIP `111779ed2` = origin/main, read back.** Row `an-icon-program-prints-snobol4s-termination-report-shape-a-language-identity-leak-in-a-shared-runtime-sink` (hq_B's mint, hq_P's two witnesses the DONE-WHEN per CEO-489). An Icon program now dies in icont's voice (`Run-time error 201 / File traceback.icn; Line 11 / division by zero / Traceback: / main()`) in both modes: the line is a RECORD in the graph, the new kind `IR_LINE_MARK` (`bb_line_mark` stores `g_line` inline), NOT the `IR_STMT_MARK` boundary -- marking Icon statements with the boundary kind released every loop body's spine state (the smoke `repeat_break` printed nothing, the R5 gate segfaulted both modes), and `build()` routed the mark's γ to a generator-kind loop head's β (ASM-DIFF-FIRST: `n8_line_mark_α → jmp n9_disjunction_β`), so the mark is wired at α by hand. `ICN$LINE` hooks the proc's first statement only. The three sinks: `core_runtime_error` prints icont's shape when `g_stno == 0 && g_line > 0` (2→201, 22→106); `runerr` routes through `core_icn_error`; `core_icn_error` prints File/Line, message, `offending value:` via `image()`, `Traceback:`/`main()`. DONE-WHEN `PASS: 0 of 123` (Arizona general); five smokes green; the zeta-switch, semicolon, coexpr, R5, both by-name and call-site-parity gates PASS; Icon master both-modes 754/757 (the three reds identical on origin and hq_R's clean b9f9bc465 board); SNOBOL4 master 1893/1917 FAIL=0 both modes. FINDING named for the claim. Residues: per-frame traceback lines need expression text; `g_line` stale after a callee returns until the caller's next mark; a main-less file aborts in the driver where icont says `no main procedure`. NEXT: hq_R's rank-1 suspend+procedure-value crash row (hq_U's three-witness DONE-WHEN), then the rtx suspended/resumed trace hook (CEO-468; the cfo holds the two smaller halves).
- **CTO-15 (2026-09-10 09:xx CDT date-read, cto; MODE NONET; CEO-450/451/454/474) — THE WORKSPACE ISLAND DIED: SCRIP `c11fb8e30` = origin/main, read back.** Row `snobol4-icon-shared-engine-zeta-workspace-island-dies-headed-gc-blocks-no-malloc` closed via computed `done`. The island's semantics were a KIND of block, not a place: `hb_pinned` names its four types; the collector marks them as roots, forwards them to themselves (the slide leaves them, a FILL hole before them becomes an allocation window) and never frees them; `rt_pinned_alloc_core` carves them in the arena through the one carve without new collection points; slab, init, banner, index and the asm bump twin gone; every identifier renamed. Lifetimes per CEO-451 named in the commit (driver_call's saved tables = the callee's activation, the residue). Arms equal to base on all five; SNOBOL4/Icon smokes green under GC stress; Icon 729/751 = a clean origin head (the gap to 747 is origin's staged parser commit `38470889b`, named to the ceo and hq_C). Also this sitting: R2 landed (CTO-14); a fleet-wide gate red of mine (the coexpr witness against the four-argument capture) cured at `edb1a8f69`; N-2 measured (any pad drop crashes parse and patchu both modes; the word is load-bearing in the entry layout) and routed to hq_U by CEO-481. NEXT: the fatal-report identity leak (patch written: an inline line mark per Icon statement, the file once per procedure entry, the sink keyed on statement number zero; hq_P's traceback.icn and loadfunc.icn cut against icont).
- **CTO-14 (2026-09-09 18:1x CDT date-read, cto; MODE NONET, CEO-460/461/462/471) — THE CO-EXPRESSION CAPTURE (R2, R1 folded in) LANDED, patchu green both modes.** Row `icon-coexpression-capture-base-register-r2-rides-the-zeta-cut` closed via computed `done`. The base is the DERIVED tier's frame base (`rbp` for a generator host too; the epilogue predicate `icn_host_pinned()` excluded generators, which is why hq_S's build saved `rsp`), the below-extent is the host prologue's own carve constant, `scrip_coexpr_create` copies `[rbp - below, rbp + above)`, the entry package carries the span and the trampoline anchors `rbp` at `copy + below` (the anchor alone did nothing: the trampoline re-copies onto the co-expression's stack), refresh rebuilds the base. w5 and a four-class witness with `^c` = icont both modes; IPL 82/89 → 83/89 both modes (diffu/diffn sig11 pre-existing); Icon 739/751 = watermark; SNOBOL4 1870/1894 GATE OK. The ζ row's island half is split by the ceo into `snobol4-icon-shared-engine-zeta-workspace-island-dies-headed-gc-blocks-no-malloc` (CEO-474), mine, after the N-2 pad (patch written against hq_U's four sites: template pad, prologue anchor 48→40, runtime twin reserve 16→8; parity gate the arm).
- **CTO-13 (2026-09-09 12:0x CDT date-read, cto; MODE NONET, ICON ONLY; CEO-447/448/450/451/454/456/460) — THE ζ CUT: SWITCHES, STORES, DEFINES DEAD, THE TIER DERIVED. SCRIP `a0bc6ae75` = origin/main (switch fold, store cut, derivation with six refinements, FINDING), read back from origin after the push.** Lon 09:2x: *"Get rid of enumerated shape class now!!!"* · *"get rid of all that ZLS and ZETA #define's"* · *"Get rid of the workspace island. Gone girl!"* · *"Replace it with proper static or heap or ZETA storage."* LANDED: the 29 frame switches and the enumerated rbp helpers folded to the arm each selected, inert by asm diff against a clean worktree build of the base; `zeta_choices.h` and every `ZC_`/`ZLS_` name gone; `zeta_alloc.c` gone — MEASURED: the zls2 slab had zero pushers and the chain had ONE writer, the by-name non-generator call frame, now carved in the call's own activation (CEO-454); `zeta_storage` is `frame_layout`; census gate wired and widened. THE DERIVATION `fl_derive_tier` (one function, every graph, every language, compile and run time): statement chain → the match regime as lowered; root → standing frame; cells graph → pin only (window or callee); else every called activation owns its frame and pins on a cross-activation resumption or a resumable callable; the entry graph's frame is the driver's protocol; the per-language assignments and the Icon shape test deleted; Prolog predicates state the fact that they are resumable callables. Three refinements were forced by measurement and are in the FINDING (a framed cells graph segfaults by-name generators; pinning on a callee broke every Pascal function; pinning on intra-activation windows cost Raku 26 passes). Six measured refinements (recorded one by one in the FINDING: a framed cells graph segfaults by-name generators; pinning on a callee broke every Pascal function; pinning on intra-activation windows cost Raku 26; a graph of only match boxes is a pattern graph; a graph carrying a DEFINE box is the DEFINE regime; the all-match test, not membership, names a pattern graph). Arms on the landed tree vs a clean base build the same day: SNOBOL4 1870/1894 GATE OK =, Pascal 243/246 =, Raku 656/820 =, Prolog 519/559 (+1) and 439/476 =, Icon 739/751 both modes = origin's watermark. hq_U co-signed with one objection that holds and is recorded (silent on SNOBOL4 statement chains: its ARBNO host is not pinned by this). CEO-456 R5 gate green both modes and wired. The island (319 `rt_ws_alloc` callers, plan approved CEO-454) and the three frame-shaped cures (R2 capture base with R1 folded in; N-2 pad/anchor parity; miu left to hq_R per CEO-462) are the next rows, R2 minted and served by `next`.
- **CTO-12 (2026-09-09 09:5x CDT date-read, cto; MODE NONET, ICON ONLY on Lon's 09:0x word, CEO-445) — THE m4 COMPILER ABORT IS CURED: SCRIP `5f2bf01c6` (rebased at push from `89cf4de36`), corpus `f7e1e3017`.** Row `icon-assignment-to-a-non-lvalue-aborts-the-compiler-where-icon-raises-runtime-error-111` (hq_B's, the class the ceo named for `rung36_jcon_errors`) claimed and landed. Neither FATAL (`op=16`, then `op=122`) was a missing template: the lowerer handed a box wiring instead of a value — `()`/`{}` were a bare SUCCEED (Icon: `&null`) and a literal side of `:=:`/`<-`/`<->` reached IR_SWAP nameless (Icon: error 111 with the literal as `&errorvalue`). One shared box found on the way: `rtx_icnnum.s` read `&null`/blank as 0 under Icon's `COERCE_ERR_FAILURE_CONVERTIBLE` flag (Icon: 102); it now bails to the C path that raises, SNOBOL4 untouched. Icon master 707/707 both modes before the rebase (= hq_U's clean board) and 708/721 both modes on the rebased tree, where every one of the 13 reds is an entry absorbed at the rebase (procedure_write_253..265, procedure_every_alt_replace_11), none from the earlier set; SNOBOL4 control arm 1870/1894 = hq_R's clean board. `rung36_jcon_errors` now compiles both modes and dies at line 113 on a list left in `&subject` by `[] ? []` (icont: 103 before scanning) — named in the marker and FINDING, not this row. My SNOBOL4 claim `snobol4-define-alternate-entry-binds-one-entry-for-the-whole-program` is PARKED-LON-HOLD with the unmeasured WIP at tag `retired/wip-cto-define-alternate-entry-2026-09-09`. NEXT: blocker (2), the fatal-report identity leak (row `an-icon-program-prints-snobol4s-termination-report-shape-a-language-identity-leak-in-a-shared-runtime-sink`, rank 0 FREE).
- **CTO-11 (2026-09-08 22:4x CDT date-read, cto; MODE NONET) — THE SPITBOL ORACLE SWAP: `TRACE('STCOUNT','KEYWORD')` NO LONGER RESETS THE STATEMENT COUNT. Fork `snobol4ever/x64` commit `2d82089` (pushed), installed at `20260909T033439Z` (22:34:39 CDT) with backup `bin/sbl.bak-20260909T033439Z`; `/home/resources/x64` fast-forwarded to the same commit; md5 `cfde224b6bcc` (pre-swap `bc694a0cc699`). No SCRIP change.** Row `snobol4-trace-keyword-fnclevel-stcount-errtype-on-every-system-write` CLOSED (`done` computed: PASS m3, PASS m4 on the installed oracle) — the ceo's 18:53 ruling executed in order: END cure first (`397b3bd22`), then the swap under the ORACLE-SWAP PROCEDURE (local clone whose stock build reproduced the shared binary byte for byte; two `sbl.min` edits — `trc09` folds the fine breakout counter into `kvstc` before `stgcc`, as `asg16` already did for `&STLIMIT`, and `stgo4` resets `stmct` before the keyword trace reads it; fork commit pushed; broadcast to every inbox; dated backup; post-install probe `1 2 4 5 7`). MEASURED blast radius: census of 948 corpus SNOBOL4 programs old vs new binary, 945 identical; ONE real change, csnobol4 `setexit4` (`stmts executed` 21 → 34, the class; its ref re-cut, corpus `6bd223848`); the other two a wall-clock stamp and a timeout cut. Control arm: the ten trace probes vs backup and vs swapped binary differ from SCRIP on the same set except `trace_keyword` (DIFF → IDENTICAL). The master has no STCOUNT-tracing entry, gimpel `_driver` refs carry no STCOUNT text, csnobol4 grades on pinned refs — no board mid-run at the swap minute was moved. Receipts: RULES.md ORACLE QUIRKS RECORD **QUIRK 2** (cured in our fork, stock upstream still carries it); `/home/resources/ORACLES.md` top line; `FINDING-2026-09-08-cto-spitbol-oracle-swap-stcount-trace-no-longer-resets-the-statement-count.md`. Named onward: hq_T declares the ladder rung for `trace_keyword.sno`; the coo re-measures csnobol4, snoflake, gimpel once after the swap; hq_P holds setexit4's remaining red (the oracle takes a keyword-operand ERROR 251 flow SCRIP does not). NEXT: `s4e_msg.sh next`; the LABEL trace hook the ceo named (CEO-398) if it is still open.
- **CTO-10 (2026-09-08 22:3x CDT date-read, cto; MODE NONET per the MODE file) — END IS AN EXECUTED STATEMENT, AND THE DUMP PRINTS SPITBOL'S KEYWORD SET: SCRIP `397b3bd22` (landed 20:15 CDT, on origin; graded by hq_B on `afa9ba4e4` = mine plus a census-only commit, FPROFILE_driver byte-identical both modes).** Position keywords `&STCOUNT`/`&LINE`/`&LASTLINE`/`&LASTNO` stop being short by one (END gets its own statement hook, numbered last with END's own line; fragments unchanged), the dump's `names[]` table in `src/runtime/keywords.c` drops `MAXINT` on the manual ruling Lon directed (`&MAXINT` is in no SPITBOL manual and x64 `sbl -bf` raises ERROR 251 on a read of it — a keyword SPITBOL LACKS, dropped from the DUMPED SET only, still readable here), the dump ends with SPITBOL's three blank lines, and `DUMP()` turns statement stamping on. MEASURED at landing: Budne 64 → 66/93 both modes, snoflake dump-ordered and dump-variables byte-identical sink-free, SnoM 1858/1858, IcnM 704/704, style 0. THIS SITTING closed the coo's row `snobol4-dump-prints-maxint-which-spitbol-does-not-so-the-dump-keyword-sets-differ` (minted after the cure, against a tree before it): DONE-WHEN made runnable (witness + dump-ordered + dump-variables against x64 `sbl -bf`, csnobol4 `a` + `dump` against `.ref`, both modes, a MAXINT line in either stream is red) and run 10/10 PASS; `done` computed green. hq_P's independent census confirms the position-keyword family on 1927 master entries both engines. TWO CLASSES NAMED, NEITHER MINE: (1) to the coo as a FLIP — snoflake `dump-ordered` reads FAIL m3+m4 in the progress database (hq_I `845b25e70`) because the runner's `-o=` listing sink receives SPITBOL's TERMINATION dump, so the oracle's compared stream is empty while ours prints to stdout (measured: the whole block lands in `spitbol_listing.lst`); a destination class of the instrument, not a codegen or runtime cure; (2) to hq_P — a READ of `&MAXINT` prints a value here where the oracle raises 251, keyword-census lane. The SCORE row is hq_B's (their reply this sitting): the `util_score_row.py:114` clause guard was right to refuse, and my stale same-number Budne tree re-stamp in `.github` was discarded rather than pushed over hq_B's writer. NEXT: `s4e_msg.sh next` for the lane's next rank-0 row; the standing SNOBOL4 attribution witness (`(0) : ERROR 248` vs SPITBOL's file and line) is hq_P's cut and I do not touch the reporter.
- **CTO-9 (2026-09-08 19:2x CDT date-read, cto; MODE EXECUTIVE, four officers) — FUNCTION TRACING PRINTS SPITBOL'S LADDER: SCRIP `a2f772d49` (graded as `73abb6a3d`, rebased unchanged onto the ceo's Prolog cut cure `645f6c248`).** Rows `snobol4-trace-function-entry-and-exit-call-return-function-and-ftrace-with-depth-marks` (hq_P's rank-1, `next`'s pick) and `snobol4-csnobol4-class-trace-facility-trace-ftrace-keytrace-trfunc-trace1` (the ceo's assignment, same class) — one cure, three edits, named in the FINDING: statement stamping turns on for a program that names `&TRACE`/`&FTRACE` or calls `TRACE(` (the banner read statement 0); the four `bb_define.cpp` taps open on `kw_ftrace` as well as `g_trace` (`&FTRACE` alone reached no tap); the flat variant's failing exit gets its own tap so `FRETURN` prints (eight pushes — `x86_align_enter` is empty and parity is the push count; seven faulted in libc); and the printer learned CALL's `NAME(args)` (arguments read from the bound parameters, since the static call never fills `g_call_args`), `NRETURN`/`FRETURN`, and the caller's depth. MEASURED: both witnesses PASS both modes; **Budne 62 → 64/93**, **Snoflake both-modes stream 99 → 101**, Gimpel 100/127 held, SnoM **1858/1858**, Icon master **705/705**, smokes 7/7, style 0, `.s` artifacts regenerated (corpus `dd57f0105`). TWO STANDING REDS ON ORIGIN, NEITHER MINE, both flagged to the coo with their cures: `test_gate_sno_package_runners_append_progress_rows` arm 5 (the inventory refuses on the gate's scratch suite, after the coo's `d406c4ab2`) and `test_gate_master_order_is_the_builders_order` (snocone, after corpus `6ed2c3b07`'s promotion without a resort) — `make test` graded around both, every other arm run by hand. LON THIS SITTING: *"Also fix SPITBOL when it obviously fails to implement its documented features. CHeck the SPITBOL manual for the definitive answer to many questions."* — routed to the ceo for RULES.md § Oracles, and CORRECTED the same hour: the x64 fork no longer refuses TRACE (CEO-280/282 is cured by the rebuild) and it answers SPITBOL's own eight testpgms identically to the pristine oracle, so I hold no fork defect this minute; the manual is `/home/resources/spitbol-manual-v3.7.pdf`. NEXT: the class's other two types — KEYWORD (`FPROFILE_driver`, snoflake `trace-keyword-fnclevel`; a measured `&STCOUNT` read divergence recorded in that row's QA) and LABEL (snoflake `trace-label-flow`) — then the ceo's SNOBOL4 walk of Gimpel, AIS and testpgms.
- **CTO-8 (2026-09-08 17:5x CDT date-read, cto; MODE EXECUTIVE, FOUR OFFICERS — Lon in chat to the cto: *"We are in EXECUTIVE mode, 4 officers, CEO, CTO, CFO, and COO."*) — THE ICON LARGE-INTEGER CLASS LANDED: SCRIP `51cbcf5db` + `3bb0a210c` on origin/main.** Row `icon-arizona-jcon-class-large-integers-and-numeric-overflow`: the eight (lgint, large, radix, checkfpc, overflow, mega, tprintf, arith) match their `.std` in both modes through the DONE-WHEN; Arizona m3 69/90 m4 69/90 (clean `b7f48462a` 62/90); Icon master 705/705 both; SnoM 1858/1858 both; `make test` green to the ceo's standing Prolog meta-call red, the 15 arms after it by hand; smokes 7/7; style 0. The sitting opened on the prior sitting's uncommitted tree (23 files, SIGTERM'd chain of 09-07 13:20): committed, rebased onto the ceo's one-oracle `19162985f` (two conflicts, named in the FINDING's Landed section: ipow promotion vs the retired switch; the trace guard `<= 0` → `== 0` so Icon's `&trace := -1` stays ON) and onto `b2660262e`. The runner half: `&file` is the path as given in iconx and SCRIP alike (measured, three forms), the runner now hands the bare name from inside the suite dir as the `.std` files were cut — `large` flipped. FINDING: the 09-07 newline FINDING, § Landed. NEXT, LON'S NUMBER ONE (in chat: *"Has the SPITBOL verified test sources been segregated from the SNOBOL4 specific one so that our score board reflect proper reality? If not make it so, number one."* · *"So, did you regrade each SNOBOL4 program with CSNOBOL4, Snoflake, and SPITBOL to properly mark the database of test suite programs?"*): measured PARTIAL — the ceo's `19162985f` subtracts `OUTSIDE_SPITBOL_BASELINE.tsv` in the Budne runner, but the 23 CSNOBOL4-cut refs still sat on disk and the published Budne row still read 73/120; nobody had run the programs through the three implementations. In flight on this root: the 23 refs + keytrace's fatal-listing ref removed, keytrace named outside the baseline (ceo 17:38), `util_snobol4_oracle_census.sh` writing `ORACLE_ACCEPTANCE.tsv` (sbl / csnobol4 / snoflake=NO_ORACLE, none installed) beside every SNOBOL4 package, then Budne and Arizona re-counted through their runners on the pushed trees. The ceo's GO (17:38) and LAW CEO-391 (no compat switch for any language; this row carries none) in the archive. **LANDED 18:1x CDT:** corpus `3f7b5f9c4` (27 refs out, four programs named outside by the listing class, seven `ORACLE_ACCEPTANCE.tsv`), SCRIP `58a2438ff`+`cc77c463a` (the census, scratch-copy faithful to `-INCLUDE`), `b3aa6a7fd` (the Budne runner names its AND-per-program fraction, so the row is the runner's, not a hand's), `08b0f577c` (the testpgms runner died on the deleted `CSN_GRADED` under `set -u` since 19162985f; cured, sbl-only text). ROWS WRITTEN BY THEIR RUNNERS: Arizona 69/90 both modes; Budne 62/93 both modes (denominator 120 → 97 → 93). NOT WRITTEN: spitbol_testpgms reads 1/2 both modes of 8 (six UNSCORED: sbl answers test2/4/5/6/7/8 with a post-mortem or a truncated run) and its cell still carries the CEO-281 `--compat` text, because the writer refuses a vendor cell with no `SUITES.tsv` row -- the coo's row to mint (asked). Census headline: csnobol4_suite 96/130 sbl, 113 csnobol4; gimpel 152/290 sbl; snoflake_suite 124/180; tests/snobol4 standalone 95/135; Snoflake NO_ORACLE. NEXT: `next` served `snobol4-trace-function-entry-and-exit-call-return-function-and-ftrace-with-depth-marks` (hq_P's, rank 1, CLAIMED:cto) -- the blocker of the csnobol4 trace row.
- **CTO-7a (2026-09-07 13:1x CDT date-read, cto) — ba3f06615 REVERTED (`1dcc65cae`).** Measured on the real programs, both halves turned wrong answers into crashes (the core's reader dumped core; TEST from 380 bytes to a SIGSEGV), because a fragment chain re-sealed into a compiled alpha's body cell returns through the by-name trampoline's protocol rather than the alpha's wire pair, and an alias followed to a pending proc did not enter it as a direct call does. The tree is the boarded 90902d16e behaviour again; AIS stays 4/7; the class (al2, seven lines, old|coded|coded under sbl) is the row's next bug and needs a protocol-honouring redirect, asked of the ceo with hq_U's criterion in hand.**
- **CTO-7 (2026-09-07 12:5x CDT date-read, cto; MODE EXECUTIVE) — AIS, THE THIRD LANDING: SCRIP `ba3f06615`.** A function whose entry label is redefined by a later CODE() now runs the new body on its next call in mode 3 (`rt_label_set_fn` re-seals the `body$<label>` cell the alpha prologue jumps through; the SPITBOL core's loader idiom, seven-line witness al2 == sbl), and an OPSYN alias of a run-time-defined function takes the proc path (`"A" ~ NIL` is a CONS inside the core). Verdict on the tree: SNOBOL4 corpus 1858/1858 both modes, all smokes green, Gimpel 104 passes both modes (scored moved 126→127: FRSORT_driver newly scored and red — an oracle-liveness wobble to confirm, not a mover). AIS through its runner stays 4/7 (SIR/TEST: TEST now runs 20 expressions before a crash in JIT-compiled library code; the reader still loses list elements — both witnessed in the package dir, the row's next bugs). Mode-4 residual named: the compiled alpha jumps through a `.data` cell the runtime cannot re-seal by name (a registry needs a grant). The coo set the AIS row 4/7 at `90902d16e` with the criterion marker; ATN's timing lines are the ceo's ask.**
- **CTO-6 (2026-09-07 11:4x CDT date-read, cto; MODE EXECUTIVE) — LON'S AIS ORDER, THE SECOND LANDING: SIR/TEST'S FIVE CURES AND THE PACKAGE RE-PIN.** Lon in chat, verbatim: *"I see that AIS went 2/2, but it should have gone 8/8. What happened. Go fix those six."* · *"Notice that upstream repos have a SPITBOL and SNOBOL4 version for all of these Shafto AI stuff. It might even be in corpus."* · *"Ensure that both of those versions are placed into our corpus repo."* WHAT HAPPENED: both versions were in corpus (`upstream/`, `ec8714f67`); the graded `.sno` were the SNOBOL4+ versions the SPITBOL oracle refuses on dialect, so six were filed ungradable/ungraded and the suite graded two. THE RE-PIN (corpus, this landing): the graded set is the `.SPT` versions with `SPITCORE.sno` (the two documented adaptations), the library files, `HSORT.argv`, LF drivers; the builder and harness learned companions and program arguments (`9870baf69` + this landing). ENGINE CURES: ATN's four (`80589b7d5`: `INPUT(.X)` binding; `&STCOUNT` parity for EVAL, DEFINE, blank lines, mode-4 fragments) and SIR/TEST's five (this landing: OPSYN'd unary alias to a user function, binary `%` alias, same-fragment label goto deferred by name — the loader idiom, DEFINE's prototype/entry by descriptor length, `OUTPUT(.X)` binding). BOARD through the suite's own runner: `AISNOBOL_BOARD shipped=9 scored=7 excluded=2 m3_PASS=4 m4_PASS=4` — ENDING, HSORT, KALAH, WANG green both modes; ATN red on sixteen timing lines only (ASK to the ceo: a volatile-line declaration); SIR/TEST red inside the LISP reader (next bug); BUILDLIB has no oracle (`SET`/`TELL`). FINDING: `FINDING-2026-09-07-cto-ais-the-eight-shafto-programs-...md`. NEXT: the LISP reader divergence in SIR/TEST, then `SET`/`TELL` for BUILDLIB, then the ceo's assigned `icon-arizona-jcon-class-large-integers-and-numeric-overflow`.**
- **CTO-5 (2026-09-07 10:1x CDT date-read, cto; MODE EXECUTIVE)** — **THE KEY-ORDER ROW LANDED AND CLOSED FOUR ROWS: SCRIP `dd7fab7bf`, corpus `3745f7142`.** `key(t)` and `!t` now walk a table in iconx's hash-bucket order (segment, slot, hash number, insertion; records/lists/sets/tables by serial id; set creation no longer consumes a table serial). The row's DONE-WHEN passes in both modes and with it `flip-jcon-compiler-jtran` is GREEN byte-for-byte on the preproc and symbolic stages (the SCRIP-built 17-module jtran == the icont-built one); hq_I's `icon-arizona-class-table-iteration-order-not-insertion` reads CURED both modes; hq_C's `icon-table-key-iteration-order-diverges-from-the-oracle` has its repro absorbed (procedure_write_252). Icon master 700/705 both modes with three new witnesses, resorted into the builder's order. FINDING: `FINDING-2026-09-07-cto-icon-table-key-order-is-iconx-hash-bucket-order-...md`. Computed `done`: key-order and arizona rows CLOSED; jtran and hq_C's rows refused on a dirty tree only (my AIS cures in progress) and close on the next clean run. **THEN LON IN CHAT: *"I see that AIS went 2/2, but it should have gone 8/8. What happened. Go fix those six."*** Measured (all eight, both dialect versions, both oracles): the graded `.sno` files are the SNOBOL4+ versions, which the SPITBOL oracle refuses on dialect; the `.SPT` SPITBOL versions run clean under it and four already match under SCRIP (ENDING, WANG, HSORT with argv, KALAH). ATN: three SCRIP defects cured on my tree — a one-argument `INPUT(.X)` never associated the variable (read empty forever, the runaway loop), and three statement-count parities (`EVAL` counted its wrapper, `DEFINE` statements were not counted, blank lines were counted, and mode-4 runtime segments never counted); ATN now matches the oracle in both modes bar its timing lines. SIR/TEST: run clean under the oracle with the documented SPITCORE adaptations (six DEXTERNs, REWIND for SET); SCRIP crashes in the SPITBOL core's DEXTERN body (25-line minimal witness; the shared RETURN landing pops a zero continuation). BUILDLIB needs SET/TELL, which no oracle has. Verdict chain on the ATN cures running; the package re-pin is the next landing.**
- **CTO-4 (2026-09-07 09:0x CDT date-read, cto; MODE EXECUTIVE, the ceo's 07:51 order of work)** — **THE FIRST SNOBOL4 PACKAGE FLIP: `flip-gimpel-TRIG_driver`, SCRIP `628d333ba` on the ceo's show-stopper landing `ac9fe5e9f`, corpus `bb693c210`.** Sitting opened on two ceo telegrams (MODE OCTET → CEO → EXECUTIVE): rung A was folded into the ceo's landing with credit, so `b9cef65ef` was dropped (tag `retired/cto-rung-a-host-pin-2026-09-07`, patch in handoff) and the tree reset to origin; jtran released then PARKED on the ceo's word (blocked on the ceo's scan row; DONE-WHEN mine to re-run). THE BUG, cut by eight witnesses, not hq_P's shadowing hypothesis: a CODE-compiled body calling a function whose DEFINE ran at run time with a computed prototype (gimpel's DEXP idiom) hit `rt_ab_undef_fn_stub` — `bb_call_route_classify` routed every `rt_proc_is_registered()` callee to the staged proc call, and `rt_sno_runtime_define` registers with `fn=NULL` (entry = a label the BYNAME route resolves lazily); main-level `COS(0.0)` worked only because the name was unregistered at static emit time. Cure (+9/-1): `rt_proc_entry_pending()` (dyn_scope ∧ fn==NULL ∧ jmp_entry==0 ∧ a DEFINE on record) sends that shape BYNAME, everything else as before; the first cut without the `jmp_entry` term rerouted static SIN at compile time and fell to CALL_ROUTE_FATAL — asm-read, corrected. BOARDS: GIMPEL 104/126 both modes vs the coo's clean 103/126 on `ecbeabbca` (TRIG the one mover, no new red; VISIT stays red by name); SNOBOL4 corpus 1858/1858 both modes FAIL=0 on `ac9fe5e9f`+cure; smokes icon 15/15 ×2, prolog 5/5 ×2, snocone 5/5, rebus green; strip_comments 0. Standing reds named, neither mine: `test_gate_pl_meta_call_reaches_control_constructs` (ceo: red on origin) and `test_gate_score_tables_agree` (SCORE.md pascal 286 vs 292, .github `94d0c3b87`). VISIT's two defects are diagnosed by witness (DEFDAT_fn returns early on an existing datatype; the by-name field write ignores want-name) and handed to the coo's SNOBOL4 lane in the baton and the FINDING. FINDING: `FINDING-2026-09-07-cto-gimpel-trig-a-code-compiled-call-to-a-runtime-define-was-routed-to-the-staged-proc-call-while-its-entry-was-still-pending.md`. NEXT: the ceo's assigned rank-0 row `icon-table-key-iteration-order-differs-from-iconx-...` (pull `30c8f8214` on the ceo's telegram, run the jtran DONE-WHEN unchanged, cut key() order against refs/icon-master rtable/rhash), then the coo's eleven IPL m3 reds measured on `ac9fe5e9f` and classed.**
- **CTO-0 (2026-09-06 11:19 CDT, written by ceo at the opening)** — Root `/home/claude_cto`, identity `cto` (the postoffice recognises the path: SCRIP `69e9c9cbc`; mailbox `cto/inbox`, HQ file → `ceo`). Model Claude Fable 5.1. The first problem: **Icon co-expressions on pthread stacks, rung 38, with the generator ABI** — row `icon-co-expressions-on-pthread-stacks-rung-38-create-activate-refresh-and-the-generator-abi` (rank 6, hq_B's lane, CLAIMED:cto; ⛔ CORRECTED by ceo 2026-09-06 12:0x: the baton's column 0 is still the MINT PLACEHOLDER — `util_queue_visibility_census.py` lists it among 49 — so the CTO's first act is to write the criterion and prove it red once: `test_icon_ladder.sh --only 38` green in both modes over the printed rung population, plus the two coexpr crash rows' witnesses; `done` executes column 0 whole). What stands: THE CO-EXPRESSION STACK RULING (ARCH-ENGINE.md, Lon 2026-09-02 08:45 — a pthread's own mmap'd stack, never a heap frame), hq_B's ABI word `311c16940`, the two coexpr crash rows in hq_B's lane (`icon-jcon-class-undefined-curly-call-activates-coexpr-with-garbage-target`, `icon-jcon-cxprimes-coexpr-activate-null-garbage-slot`) which this landing closes, and `refs/icon-master/src/runtime/*.r` + `refs/jcon-master` as the canonical procedures. Co-sign: hq_U on the frame model and the ABI, before push. Boards owed with the landing: `test_icon_ladder.sh --only 38` both modes, Icon master, SNOBOL4 master (FAIL sets named), the Prolog ladder as the shared-node control.
- **CTO-0a (2026-09-06 11:5x CDT, written by ceo from hq_B's handover, `head-is-green-again-icon-master-resorted-and-coexpr-handed-to-the-cto`, in `cto/inbox`)** — the root is STOCKED (`.github/scripts/populate_cto_root.sh`: three repos at origin/main, ONE-IDENTITY config, hooks, `refs/`, `.claude/settings.json` with the Stop banner + inbox hook; first `make` rc=0 at 11:48 CDT, log `.scratch/build-first-2026-09-06.log`). hq_B landed THREE cures before the handover that change what rung 38 IS: (1) a co-expression VALUE is now a descriptor (the create used to write a raw pointer into the TAG word — `type()` read the pointer's low byte, m3 said `list`, m4 said `string`); (2) the BODY sees GLOBALS (the GVA base register is now among the registers the create packages for the body's thread — the SEGV was the second symptom of (1)+(2), not a third defect); (3) a finished co-expression returning to a DEAD activator no longer loses control, and the walk over dead activators is BOUNDED because the activator chain can be a CYCLE (hq_B's first fix hung). Icon master CRASH=0 for the first time; rung38 `coroutine_switch` passes both modes; rung 38 is 8 of 14. ⛔ The `&current` blocker ("there is no co-expression VALUE to return") is GONE as of that landing — do not rediscover it. Start from the six remaining rung-38 reds, both modes, against `iconx`.
- **CTO-1 (2026-09-06 14:4x CDT, cto)** — **RUNG 38 LANDED at SCRIP `09a1ce869`** on origin/main; hq_U co-signed the frame model in full. `test_icon_ladder.sh --only 38` 8/14 → **14/14 both modes**; the full computed DONE-WHEN passes; both hq_B-lane coexpr crash rows closed (cxprimes rung36 green 3/3; undefined-curly-call no longer crashes — Lon scoped that arm in-chat to no-crash/clean-error; iconx error/traceback-format parity is a separate cross-cutting row). Boards on the final binary: Icon master m3 665/671 m4 665/671 CRASH=0, watermark up from the 607 floor (residual reds pre-existing, not the CTO's: rung41 `rt_*` ×5, `procedure_every_alt_replace_4`); SNOBOL4 master m3 1842/1842 m4 1842/1842 GATE OK, inherits nothing. Three root causes: (1) `&current`/`&main`/`&source` were one hardcoded string → real DT_CO descriptors; (2) THE HEADLINE — a captured local read EMPTY because the create-time frame snapshot was delivered rbp-relative while the body read rsp-relative (two planes, one datum) → the body frame now lives ON the coexpr pthread stack, loaded once per birth from the pristine snapshot, so resume=evolve and refresh=copy fall out of one mechanism (no flag); (3) `^` refresh was a parser NO-OP → `copy(x)` cloning from the pristine snapshot; plus `serial()` on the ONE global Lon granted in-chat (`g_coexpr_serial`). FINDING: `FINDING-2026-09-06-cto-rung38-coexpr-the-snapshot-rode-rbp-the-body-read-rsp-and-caret-refresh-was-a-parser-no-op.md`. FOLLOW-UP owed (hq_U, not a blocker): `^` refresh spawns a pthread per call → O(pthreads) in a loop (VA exhaustion, GC root growth, reaping); loud rc-refusal at EAGAIN when it bites — mint a row. **NEXT: the ceo names the next problem from the suite table (THE CTO LOOP step 2).**
- **CTO-2 (2026-09-06 16:2x CDT, cto; MODE OCTET, CEO-343/351: "the hardest bug")** — **THE COMPILED CLAUSE DATABASE LANDED**, twice on origin/main: SCRIP `306eacde6` (clause/2 + retract/1 as db enumerators) and `db6011358` (findall/bagof/setof propagate their goal's throw). Row `prolog-rung-10b-assert-retract-abolish-clause-the-dynamic-database` (hq_C's; CTO led, hq_C co-authors and grades the Prolog arms, hq_U co-signs shared nodes; the r2 branch was already an ancestor of main — re-cut on origin/main, no branch). hq_C's composition prior held exactly: no new box, no new IR kind, no global — `$db_n`/`$db_at` ROOT leaves + `IR_TO` + the existing `$unify` (+ `$db_erase` for retract), tombstoned store keeps first-match order and resatisfiability; two runtime corrections (erase recompiles as assert does; a `$db_nonempty` pre-call guard makes an emptied dynamic predicate FAIL); a `$ball_pending` asm leaf reads r15 before any C so a throw inside an all-solutions goal reaches the enclosing catch. Receipts on `db6011358`: rung-10 dynamic-DB slice **22/22 both modes** (6/14 at hand-over), rung 10 38→70/82, ladder `--to 18` **492→531/568**, Prolog master **m3 477→506, m4 389→424** (14 fewer m4 skips, crashes 4→4 inherited), INRIA **350→353 each mode**, SWI plunit 6/118 units (grading unblocked; the 112 are per-unit builtin gaps, named), SNOBOL4 FAIL=1, Icon 696/702. FINDING: `FINDING-2026-09-06-cto-clause-db-clause2-and-retract1-compose-from-existing-leaves-and-findall-swallowed-its-goals-throw.md`. ⛔ `done` is GATED on the ceo's re-scope of DONE-WHEN arm (a): swi `test_misc.pl` is SWI-internal surface, not a DB unit. Three instrument defects caught by RUNNING the arms (harness `--modes` under `--lang prolog`; INRIA `m3_pass=`; the master's single `SUITE_BOARD` line) — a parse that finds nothing must REFUSE, never print a blank. Routed to the ceo, not taken: the four inherited master crashers = rung 10a's meta-call re-drive (one class, documented cure, hq_P's arm). **NEXT: the ceo's word on `test_misc` (→ `done`) and on rung 10a's owner.**
- **CTO-3 (2026-09-06 16:5x CDT, cto; MODE OCTET, CEO-359/360 on Lon's word: *"maybe CTO should take the JCON compiler"*)** — **THE JCON COMPILER IS THE ONE BUG**: row `flip-jcon-compiler-jtran` (CLAIMED:cto; hq_B minted it, DONE-WHEN end-to-end and proven red: the SCRIP-built 17-module jtran must match the icont-built jtran byte-for-byte on `preproc t1.icn : stdout` and on the symbolic stage, and the 11-line mutual-recursion witness must print 3/2/1, both modes, Icon master held). Defect ONE (mutually recursive generators: `icn_gen_host_slice` closed a cycle only into the immediately-visited callee) is hq_B's, cured on its side and landing as its own commit; defect TWO is mine: with the cycle cure in, the preproc stage emits 75 bytes against the oracle's 58 — `#line 0 "t1.icn"` misplaced and `write("hello");` duplicated. The test_call class went back to hq_C with its state: the CEO-357 decomposition (A)/(C)/(B), the static-clause/2 patch (`postoffice/handoff/cto-static-clause2-seed-WIP-on-db6011358.patch`), its two diagnosed defects (TT_CLAUSE is `[head-args, body-goals]`; vars carry only a slot) and the open frame-slot design point, all in that baton's ledger. Tree SCRIP `95ac6d3d0`, binary 16:54:02; oracle jtran built from the staged 17 modules, its preproc answer captured. **NEXT: hq_B's cycle cure lands → reproduce the 75-vs-58 diff on that binary → cut the witness from `preprocessor.icn`'s yield paths (`preprocessor()` / `preproc_scan_text()` / `preproc_sync_lines()`) → ASM-DIFF.** **CTO-3a (17:4x CDT): THREE CURES PUSHED on top of hq_B's `dd148e2cf` — SCRIP `c1233069b` (break inside a case branch fell through), `76c29d53e` (an omitted trailing argument to a generator read the caller's own argument), `76d64253e` (a call with omitted arguments returned &null: the per-node-carve arm used the by-name epilogue; boards measured on the pre-rebase `b6aa82c00`, fast proofs re-run on the pushed tree). The preproc stage of jtran is GREEN end to end (58 bytes both builds); the row is RED on the symbolic stage (0 vs 3376 bytes). Boards on `b6aa82c00`: Icon master 697/702 both modes; ladder 626/634 (rung-41 inherited); Prolog ladder 533/568 and SNOBOL4 1854/1855 both equal to origin. NEXT DIVERGENCE (pre-existing, bisected): the lexer stage emits one token and hangs — a subject installed by `&subject :=` inside an inner generator reverts to the enclosing scan's subject when the OUTER generator's suspend is resumed (witness `v6_twolevel.icn`, iconx `ab cd ef`, SCRIP `ab`); same family as the FREE row `icon-jcon-scan-scan2-hang-nested-scan-break-next`. NEXT: find who rewrites `scan_subj` on resume (the `rt_scan_state_capture/apply` snapshot is the candidate), cure, then the symbolic stage.** **CTO-3b (18:3x CDT): CURES 4-6 PUSHED — SCRIP `609cb5e5f` (the kind-2 re-entry banked only the position and rebuilt the subject from the `?` operand; now banks the live subject), `9817e21a2` (each Icon file was lowered as its own segment, so a global declared in another file was an implicit local; segments merge into one program), `b7d16043e` (the re-entry chain's ω edge named the yield target, so the last suspend of a scan body re-yielded forever; it now names the continuation). Jcon's lexer stage is exact (12/12 tokens) when driven directly. Boards on `883b3be3e`: Icon master 697/702; ladder 626/634 (rung 41); Prolog 533/568 and SNOBOL4 1854/1855 both equal to origin. BLOCKER (hq_B's one bug, CEO-368): the generator-host reservation — `main` and `ir_value` carve 66 MB each, and its thread-side face sizes a co-expression-body generator's region without its callees' slices (rt.c:1119 vs `icn_gen_host_slice`), which corrupts the heap the moment jtran's lexer runs as a stage. NEXT: bisect parse and ast2ir by serving copied tokens through a builtin co-expression; land the thread-exit cure (a destroyed finished co-expression died in libgcc's forced unwind).** **CTO-3c (18:4x CDT): JCON'S PARSE STAGE MATCHES THE ORACLE (65/65 AST dump lines, driven directly); the lexer is exact as a co-expression body (13/13 both modes). Three more cures boarding on `cd95e88c1` before the push: (7) `pthread_exit` inside compiled frames → `setjmp`/`longjmp` to the trampoline; (8) THE HEADLINE, mine from rung 38 — the co-expression snapshot packaged the creator's locals frame (1168) while the creator had carved frame + generator-host reservation (65544), so a body's generator calls wrote their region slices into the thread's TLS (malloc's tcache); the create now packages the whole carve; (9) co-expression stacks sized to the snapshot, lazily reserved, so the preproc arm stays green under the 66 MB carve. The only divergence left is INSIDE hq_B's carve (ast2ir under `ir_value`'s 66 MB reservation: a null region and a corrupted return address in `ir_a_NoOp`); hq_B's plan by name: bounded carve with a loud refusal first, per-activation storage as the cure. FINDING #2 (cures 4–9) written: `FINDING-2026-09-06-cto-jcon-the-lexer-and-parser-stages-six-more-engine-cures-behind-the-preprocessor.md`. NEXT: push 7–9 on their boards; when hq_B's refusal lands, run the row's DONE-WHEN unchanged and bisect the symbolic stage the same way.** **CTO-3d (20:4x CDT): THE CARVE CURE COMMITTED, SCRIP f3c458083 on a81ca6909 -- hq_B's "carve small, allocate big" (ceo-370: the size-keyed floor withdrawn on hq_B's measurement; the cto builds hq_B's design under three conditions). Above 262144 bytes (SCRIP_ICN_GENHOST_STACK_MAX) a host keeps a 16-byte slot, obtains a calloc'd GC-rooted block in the prologue, call sites rebase off the slot, both wire-stack epilogues free it; the create patches its snapshot with a fresh block per co-expression birth. hq_B's DONE-WHEN PASS; condition (a) 38 of 44 byte-identical, the 3 that move are the 3 over-ceiling programs; witnesses match iconx both modes with the ceiling forced to 0. Boards running; hq_U co-sign asked; hq_B keeps the row. THE ROW `flip-jcon-compiler-jtran` STAYS RED: the carve hid the slice-nesting overrun (FN__ir handed a region 65 MB past its caller's block: the cycle cut sizes the callee area as one cycle sum, the offsets are inflated slices). NEXT BUG ASKED OF THE ceo: per-activation regions (one region per site per host activation through the slot, reused on re-entry, released recursively at exit) -- deletes the multiplier, the cycle sum and the GENHOST refusal. FINDING: `FINDING-2026-09-06-cto-genhost-the-66mb-reservation-a-heap-block-refused-by-lon-and-the-slice-nesting-overrun-behind-it.md`.** **CTO-3e (20:5x CDT): ⛔ THE BLOCK IS REFUSED — Lon 20:18 CDT, verbatim *"No activations on the heap."* hq_U objected instead of co-signing (RULES.md BB FRAME-PLACEMENT CRITERION: activation frames live on the machine stack; only genuine escapers go to the heap; the cto's own "owned by one activation, nothing outlives the frame" is the description of a thing that may not be heap); the ceo put it to Lon the same turn and Lon held the law; ceo-370/371 retracted. f3c458083 deleted, tree reset to a81ca6909, nothing pushed, patch archived as REFUSED in postoffice/handoff, boards stopped. THE CURE IS ON THE STACK (ceo-372): a host carves each callee region per activation and per path as it enters the site, released by unwinding, with the loud rc-refusal cap as the floor — which means rbp-pinned Icon hosts with a floating rsp; the cto is reading the criterion and the zframe arms and writes the shape into the row's NEXT for hq_U and hq_B before code. The FINDING is reframed as the measurement record: `FINDING-2026-09-06-cto-genhost-the-66mb-reservation-a-heap-block-refused-by-lon-and-the-slice-nesting-overrun-behind-it.md`.** **CTO-3f (21:5x–22:0x CDT): LON IN CHAT, VERBATIM, two statements that decide the shape.** (1) *"So are you not able to depend on LIFO regarding generators being called via mutual recursion and not have to carve a maximum carve amount?"* (2) *"THe idea for all function calls is that the SPINE is only partially allocated at every function call within it's parent function. It reduces the stack total allocation. We do not allocate entire frames on a SPINE, only the RBP frames with house keeping and exceptional unreachable operand results or locals."* The cto's answer on record in chat: yes, generator lifetimes are LIFO; what blocks LIFO carving is the Icon host's rsp-relative addressing (the criterion's "leave rsp low is refused"); the cure is the criterion's own RBP activation frame for Icon hosts with generator frames carved on the spine as entered and freed by exhaustion or the bounded expression's rsp restore — no maximum, no cap. Consequence: the SCC depth table built 21:0x–21:4x (x86_asm.h/bb_call_proc_staged.cpp/emit.cpp on a81ca6909, six witnesses green, the depth bomb loud, hq_B's two-entry witness green) pre-allocates whole callee frames on the spine and is NOT the architecture — and hq_B's fan-out witness (`suspend h(n-1) + h(n-2)`) collides on it (m3 rc=132, m4 rc=139). It is kept as a measured branch, not landed. NEXT: the RBP-frame design and touch list from the code (Prolog's zframe prologue/epilogue/landings as the model; the generator N-2 prologue unchanged; the call site carves own+48 on the spine; the γ landing continues below the suspended callee's spine bottom; ω and IR_BOUND exits restore rsp; the co-expression snapshot copies [rsp, frame top] with an rbp delta), the smallest witness pinned end to end, hq_U's co-sign.** **CTO-3g (21:25 CDT box clock; every earlier CTO-3x label ran one hour ahead): RUNG A COMMITTED LOCALLY, SCRIP `b9cef65ef` on 3adeaa818 -- Icon hosts pin rbp to the frame bottom and preserve it (prologue save at [kt-8], epilogues release off rbp and restore, the generator hand-off through rbp, the create banks rbp); nothing else reads through rbp, so the emit is byte-identical to origin bar those instructions (SCRIP_ICN_HOST_PIN=0 is the control). The first cut, which also rebased the DSL's frame spellings to rbp, broke write("hello") and was asm-diffed and reverted the same hour: in this regime a node's result cell is a SPINE cell below the frame bottom addressed as rsp + depth, exactly Lon's "only the RBP frames with house keeping and exceptional unreachable operand results or locals" -- so rung B's rule is per node: a result consumed after a generator site's gamma moves into the frame, everything else stays a spine cell, frame slots switch to rbp spellings. Fast proofs green (smoke 15/15, rungs 36/38, eight witnesses both modes); boards running; hq_U co-sign asked. Then rung B: the call carves own+48 on the spine, gamma landings leave rsp, omega pops, the reservation walk goes.** **CTO-3h (2026-09-07 07:12 CDT box clock, SITTING ENDS on Lon's "wrap it up"): rung A's boards on b9cef65ef read no worse than origin on every arm (Icon master 697/702 both modes, ladder 626/634 rung-41 inherited, Prolog 533/568, SNOBOL4 1858/1858, rungs 36/38 green, scan gates green, the rung-36 hang the FREE row's). Rung A stays LOCAL pending hq_U's co-sign (patch archived in postoffice/handoff); nothing running; records on origin. Rung B1's mechanism found in the code: ZOPQ/ZRES already read a node through rbp when xop_frame_slot gives it a frame slot and through the spine otherwise, so B1 extends that rule to every value node of a pinned host that has a generator site. jtran itself is still red on the symbolic stage behind the carve (rung C).**

## THE CTO LOOP
1. Session start as every seat: `fetch` + `ff-only` merge of SCRIP, corpus, .github; `bash SCRIP/scripts/s4e_msg.sh check`; print the banner (`python3 .github/scripts/util_suite_banner.py`).
2. Resume the claimed row (`s4e_msg.sh next` resumes your own claim). One problem at a time; the ceo names the next from the suite table when this one lands (the class blocking the most package programs across lanes).
3. ASM-DIFF-FIRST (RULES.md § Debugging order), the three zetas (ARCH-ENGINE.md), zero comments, LF, `-O0`, no new globals without Lon's banner grant, `make` incremental for the verdict.
4. Push per rung with the ledger line before the next step — the account change is abrupt (ARCH-FLEET-CEO.md § TWO OPERATING MODES, CEO-324); nothing lives only in this session.
5. Land: rebase, re-prove, push code repos then `.github`; FINDING named for the claim; move this LIVE CURSOR; `s4e_msg.sh done <topic>` (computed); telegram `ceo` with the hash.
6. The CTO does not audit, arbitrate, rank, or write law. Escalations go to `ceo/inbox`; Lon's in-chat word wins immediately and is routed into this file the same session.

## RESOURCES
`/home/resources/` (oracles, `icon-master/bin/icont` v9.5.25a), `SCRIP/refs/` (populate from `/home/resources/` when empty), MASTER-PLAN.md § THE FLEET-12 PLAN, SCORE.md § THE SUITE TABLE, GOAL-ICON-100.md (the Icon order of attack, CEO-314).
