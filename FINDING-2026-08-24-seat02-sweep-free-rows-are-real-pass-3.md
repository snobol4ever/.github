# FINDING — sweep-free-rows-are-real, pass 3: 138 true-free (was 134), only 2 new rows, both confirmed LIVE

**Session:** 2026-08-24 seat02, THE LOOP row `sweep-free-rows-are-real`, STEP 3 of the prior pass's own instructions (`.github/FINDING-2026-08-24-seat02-sweep-free-rows-are-real-pass-2.md`): re-run the cross-reference against current HEAD. Snapshot: SCRIP `411bd9de` (pulled forward from the row's cited `69449f94` baseline — one new commit landed, fresh `make -j4 scrip` done), corpus `35b7d034` (unchanged since pass 2), `.github` `99d17ff0` (unchanged since pass 2; the row's cited pass-2 baseline `35b7d034` was actually corpus's hash reused imprecisely for both repos in the task file's shorthand — `.github` had already moved past that by the time this session started).

## HEADLINE

- **True-free count now: 138** (state==FREE, no `claims/*.claim`), up from **134** at pass 2 — only **+4 net in ~19 hours**, a sharp slowdown from pass 1→2's +50%/day. The growth-outpacing-consumption trend the last two passes flagged appears to have eased, at least for this window.
- **Only 2 topics are genuinely new since pass 2**: `callgrind-opaque-bb-labels`, `perf-replace-map-cache-revalidation`. Both independently verified LIVE, no corrections needed, nothing to mint.
- Claims hygiene unchanged in shape: 43 orphaned claim files with no matching QUEUE.tsv row, all `DONE`-terminated (same pattern pass 2 found, not chased — correctly swept already, row-factory discipline: don't chase what isn't real).
- Zero duplicate topic rows in QUEUE.tsv (pass 1's one dup stayed fixed; checked fresh).
- **Net QUEUE.tsv change this pass: 0 edits.** First pass where independent verification turned up nothing to fix — see WHY THIS PASS WAS SHORT below for why that isn't a reason to skip verification in future passes.

## METHOD (unchanged from pass 1/2, fully reusable)

true-free = `QUEUE.tsv` `state==FREE` minus `claims/*.claim`, both freshly read. To isolate what's new since pass 2 without a literal pass-2-end snapshot file (pass 2 didn't save one under its own name), built a "known-classified" union from: pass 1's pre-edit backup (`QUEUE.tsv.bak.seat02-sweep-20260823T212807Z`, FREE-topic universe, 141 topics) UNION pass 2's explicit 28-new-topic list transcribed verbatim from its own FINDING doc's tables UNION pass 2's 2 self-mints (`lib-gate-cd-order-silent-skip`, `conform-opsyn-operator-rebind-gz5-fatal`) = 171 known topics. Diffed current 138 true-free against this union → 2 candidates.

**Sanity-checked the method itself** two ways before trusting the 2-candidate result:
1. Reverse diff — known-classified topics no longer appearing as *any* row in current QUEUE.tsv (not just no longer free): 19 found, and every one is independently explained — pass 1's exact 8 ALREADY-FIXED retirements, the `icon-r0-bisect`→`sn4-m1-r0-bisect` rename, and 10 more rows that some other seat/HQ claimed and closed (swept via `s4e_msg.sh sweep`) in the interim. Nothing unaccounted for.
2. Zero duplicate topic names anywhere in current QUEUE.tsv (166 data rows, all unique topics).

Given only 2 candidates, skipped forking — direct verification was cheaper and just as thorough:

- **`callgrind-opaque-bb-labels`**: claims every compiler-emitted BB-port label (and `main` itself) is ELF-untyped, so callgrind folds the entire compiled-program body into one opaque `???` bucket, inflating every "unattributed remainder" figure project-wide. Verified directly: compiled a trivial witness (`OUTPUT = "hi"` / `END`) with `./scrip --compile witness.sno -o out.s < /dev/null` — mode-4's `-o` targets the assembler-TEXT stage (consistent with CLAUDE.md's own description of mode 4 as `TEXT asm → as + gcc -no-pie`; `scrip` itself doesn't invoke the assembler/linker). The emitted `.s` has **zero `.type`/`.size` directives anywhere**, including on `main:` — confirms the mechanism exactly as briefed, on a fresh minimal witness rather than trusting the cited `string_manip.sno` measurement alone.
- **`perf-replace-map-cache-revalidation`**: claims `bn_replace`'s 4-slot cache at `src/runtime/by_name_dispatch.c:5094-5113` re-validates via hash(first-byte+length)+2×memcmp on every call, even for compile-time-literal FROM/TO arguments that never change. Read the cited lines directly at current HEAD: `static struct {...} g_rm[4]` (line 5095), hash `((unsigned char)fv[0]*31u + fl) & 3u` (line 5106), two `memcmp` validations against the cached `.f`/`.t` buffers before reusing the map (line 5107). Matches the brief exactly, line-for-line, no drift.

## WHY THIS PASS WAS SHORT

Both new rows were freshly minted the same day (2026-08-24) by seat04 and seat01 respectively, as row-factory splits off the `perf-string-runtime` umbrella row — each already carrying a receipt FINDING citation and a precise `## NEXT` with exact file:line pointers and an explicit "don't cure everything in one sitting, mint sub-pieces" clause of their own. High-quality mints needing no rewrite. Unlike the `conform-*` family in pass 2 (where 4/19 needed real corrections despite looking equally solid on first read), independent verification here found both claims fully accurate on first check. **This is a result, not a reason to skip the verification step next time** — pass 2's lesson (plausible narratives can hide real corrections even at high LIVE rates) stands; this batch was just small enough, and well-formed enough, that verification was fast and came back clean both times.

## CAVEAT — STILL A SNAPSHOT

Same standing caveat as passes 1 and 2: this is a point-in-time count, and the queue is still live with multiple other seats/HQ minting into it continuously. A future session should re-run the same cross-reference against current HEAD (method fully reusable, documented consistently across all three FINDINGs in this series) rather than trust this number's currency. **Unlike the prior two passes**, this window's growth was slow (+4 vs +45) and turned up zero defects to fix — three data points now (Δ+45, then Δ+4, with the process itself finding nothing broken this round) suggest the backlog may be stabilizing rather than still runaway. Whoever plans fleet sweep cadence may want to revisit whether this row still needs sub-daily passes or can move to a slower one — that's an HQ cadence call, not something this row decides unilaterally (row-factory: observe and report, don't set fleet policy).
