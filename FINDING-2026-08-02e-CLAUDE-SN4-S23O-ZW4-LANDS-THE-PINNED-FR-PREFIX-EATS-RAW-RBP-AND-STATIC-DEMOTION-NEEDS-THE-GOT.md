# FINDING s23o (2026-08-02e) — ZW-4 LANDS; THE PINNED FR-PREFIX EATS RAW-RBP OPERANDS; STATIC DEMOTION NEEDS THE GOT

**Session:** s23o, Claude. **Directive (Lon):** "Complete the NON-POPPING FORTH-style RSP ZETA stack with a C-style RBP used occasionally only when absolutely necessary. Continue." + "All your choices. I'm with you on this."
**Landed:** SCRIP `7b209188` (source) · `61bacbba` (feature .s) · corpus `b3f08a4a`/`d24b2bbb` + crosscheck regen (all four regens insertions==deletions).

## 1. ZW-4 FULL LANDED
SPD-2's scanhit/scanfail per-medium pair (the named forbidden shape: one snprintf GAS blob + one hand-counted rel8 byte twin, `0x75 0x2E`-class displacements that break on any edit) is retired into single medium-invisible `x86()` chains. `g_anchor` is now `static` in keywords.c; `rt_anchor_g` (alias attribute) is the ONE exported name; `rt_anchor_ptr()` the sole C accessor. New vocabulary: `x86_reg_disp32_cmp_imm` (family-convention mod10+disp32, byte shapes checked against `as`), width-aware `x86_inc_r`, `x86_load_got` (`XK_RIPGOT`, `"[rip@got + __]"`), the `rbp#` raw-cell escape, and a loud cmp no-arm guard.

## 2. ⭐⭐⭐ THE PINNED FR-PREFIX EATS RAW-RBP OPERANDS — AND A GUARDLESS MNEMONIC DROPS THEM SILENTLY
On a pinned graph `x86_fr64_prefix()` IS the plain `"qword ptr [rbp + "` spelling, so `RDQ("rbp", K)` parses **XK_FR64**, not XK_REGDISP. `mov` has FR arms (frame encoders; bump suppressed under pin → accidentally byte-identical), but `cmp` had NONE and returned the empty string: **both guard cmps of the retry blocks vanished from the emitted stream** — mode-3 AND mode-4 — and every probe stayed green on whatever stale flags the preceding jump left. The `.s` region diff against the saved BEFORE text was the ONLY instrument that caught it. This is ZB-FC-1 (the bb_match_arb precedent) wearing a cmp: the fix set is (a) the `rbp#` escape (verbatim twin of the documented `rsp#` valve — raw machine-frame slots must not route the ζ frame encoders), and (b) cmp now aborts loudly on unhandled frame/cell kinds, mov-precedent wording. **LAW RESTATED: a probe suite cannot certify an emission rewrite; only a region diff of the emitted text can. And every mnemonic that can receive a cell operand needs the loud guard — an instruction that emits nothing is never legitimate.**

## 3. ⭐⭐ STATIC DEMOTION OF A .so DATA CELL NEEDS THE GOT — THE EXPORTED-GLOBAL ERA WORKED ONLY BY COPY-RELOC INTERPOSITION
First attempt (static + alias, TEXT still `lea [rip + rt_anchor_g]`) failed m4 ONLY: `-no-pie` executables take a **copy relocation** on directly-addressed .so data — the exe allocates its own copy — and the .so's writes to the now-`static` cell are local-bound, non-interposable, so writer and reader split cells (&ANCHOR=1 wrote the .so; the emitted test read the exe's zero). The old exported `g_anchor` worked precisely BECAUSE interposition rebound the .so's own accesses onto the exe's copy. rtx_match.S already states the rule for `g_patstk_sp` (ARCH 0(c): "MUST GO THROUGH THE GOT"); it is now available to the front-end as `x86("lea"/"mov", reg, "[rip@got + __]", ptr, name)` — BINARY movabs unchanged, TEXT `mov reg,[rip+name@GOTPCREL]`. **Any future demotion of a runtime data cell that emitted TEXT references must ride this form.**

## 4. HONESTLY NOT DONE
`g_patstk_sp` deletion + rtx_match.S lazy-init arms: **six live template readers** (bb_match_begin ×4, bb_match_end ×2) still emit the mark machinery; per ZW-4's own rung text those die via ZW-1/2 regen. Sequenced behind ZW-1/2 — attempting now breaks every match program both modes. Also observed (out of scope, candidate ONE-AUTHORITY sweep): core.c carries a SECOND anchor cell `kw_anchor` with its own ANCHOR read/write paths beside keywords.c's `g_anchor`; both live at HEAD, consistent per probes.

## 5. GATES (all HEAD-stamped this session)
Probes (inline + stored-pattern &ANCHOR semantics, SPITBOL manual p.204 step 6) m3+m4 == oracle · **crosscheck 318 BY SET IDENTICAL both modes vs parent binary (stash A/B, not remembered counts): m3 280/27/10 · m4 266/39/10/2L, non-pass sets byte-equal** · bench board **18/21 record hold** (roman FAIL, eval_fixed/eval_dynamic CRASH = the pre-existing residue, RT_OPT=-O0) · regen ×4 all insertions==deletions (1381/2856/24303 + demo) · medium-invisible --strict at its pre-existing WIP baseline (bb_glue_flat 2 + xa_flat 8, neither touched).
SPITBOL manual consulted for the construct: anchored/unanchored scan (tutorial pp.77-78), the match algorithm step 6 + flowchart (p.204), and divergence note #1 — &ANCHOR sampled only at match start — which is ZW-1's anchor_snapshot cell; this rung preserved live-read behavior deliberately.
