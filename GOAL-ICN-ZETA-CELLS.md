# GOAL-ICN-ZETA-CELLS.md — Icon at 100% per-BB ζ CELLS on the RSP-topped FORTH stack (LVA + GVA + the suspension decision)

## ⛔ CONCURRENCY PROTOCOL (Lon 2026-08-08) — THE CONCURRENT SET (9)
`GOAL-SNOBOL4-BB` · `GOAL-SN4-ZETA-MECH` · `GOAL-SN4-ZETA-CLIMB` · `GOAL-SNOBOL4-RTX` · `GOAL-ICN-ZFRAME-RESTORE` · `GOAL-ICN-ZETA-CELLS` · `GOAL-PL-ZFRAME-RESTORE` · `GOAL-PL-ZETA-CELLS` · `GOAL-RTCC` run CONCURRENTLY against one tree. Each session COMMITS along the way (per-rung, buildable, git-revert path) but PUSHES ONLY AT SESSION END: `git pull --rebase` → re-prove THIS file's gate/watermark post-rebase → push code repos first, `.github` last → `handoff_status.sh` verbatim (the only push truth). This reduces contention points. Watermarks are SHARED STATE — re-prove at open AND close; expect parallel-landed commits in the end-of-session rebase. A semantic collision there (two fixes for one defect) is resolved BY THE REBASING SESSION and NOTED in its cursor — never silently dominated (the `zd_zdh`/`_xh_zdh` lesson, CLIMB s14). ⛔ Rungs that their own file marks NOT-CONCURRENCY-SAFE (e.g. RTX-11/12: `x86_asm.h`/`bb_match_release` + `.s` regen ×3; RTX's GVA-slot ζ-ladder item) MUST NOT run while any other seat is active — Lon's routing.

**CHARTER (Lon directive, 2026-08-07):** "I still want a COMPLETE ZETA CELLS on the STACK solution. Scan what SNOBOL4 has been doing to convert to 100% per-BB allocation on the RSP-topped FORTH-style stack… Make a plan for Icon to use RSP-topped stack with per-BB allocation, handle procedure locals (LVA) and globals (GVA). We have R13-R15 for the BB_SCAN_* family. For the 100% CELLS on stack all must be in separate pthreads or we'll use the more complicated ARBNO-style constructs. Whichever." This ladder walks Icon to 100% on the SAME ZD machinery SNOBOL4 built — it is the ZD ladder's Icon completion, not a parallel mechanism.

## ⛔⭐ LIVE CURSOR — s222 (2026-08-08, Claude Sonnet 4.6 — ZK-2 IR_VAR_REF PINNED-LOCAL ARM CONFIRMED; SCRIP `1ec7f9d6`)
**NEXT RUNG = ZK-2 (continue — open: IR_MAKE_LIST, IR_DISJUNCTION n-ary, unpinned IR_VAR_REF).** VAR_REF pinned-local arm confirmed working post-rebase at `1ec7f9d6`. REBASE NOTE: parallel session moved the IR_VAR_REF arm comment on HEAD; conflict at emit.cpp line 1947 resolved by keeping our ICN-CELLS extension (additive, no semantic collision). **Watermarks post-rebase:** default m3 **PASS=202 FAIL=8 XFAIL=12** · CELLS=1 m3 **PASS=169 FAIL=49 XFAIL=19** · SN4 smoke 6/6 IDENTICAL (R-ICN-D ✔). No .s regen. **ZK-2 REMAINING OPEN (priority order):** (1) IR_MAKE_LIST — template arm shape: `sub rsp,n*16` / copy `[rsp+op_zread[i]+n*16]` into `[rsp+i*16]` / `mov rdi,rsp` / call rt_make_list / `add rsp,n*16` / ZRES; needs zd_nops n_operands arm + zd_wl_kind icn_cells arm (SCRIP_ZD_ICN_ML=0 killswitch) + build+witness. (2) IR_DISJUNCTION n-ary (queens/palindrome/fib) — `alt_i` suspended-state generator, pending ZK-6. (3) Unpinned IR_VAR_REF — LEA-drift, pending ZK-6. **ZK-6 IS THE COMMON BLOCKER for (2) and (3).** interp.r efp/gfp single-stack evidence favors Option P (pending cells on spine). Rung spec requires measurement to confirm.

## ⛔⭐ LIVE CURSOR — s222 (2026-08-08, Claude Sonnet 4.6 — ZK-2 IR_VAR_REF PINNED-LOCAL ARM LANDED; SCRIP `47c68f90`)
**NEXT RUNG = ZK-2 (continue — remaining kinds: relational/CMP {0,1}-generator BINOP_TEST already admitted at zd_wl_kind:1959; IR_VAR/IR_ASSIGN locals already at :1896; remaining open blockers from ZK-0 histogram = IR_MAKE_LIST, subscript/field, string builtins; unpinned VAR_REF deferred to ZK-6).** Session recovered stashed s221 VAR_REF work, applied post-WL-DELETE (s12 merge `3c5c1f71`). KEY FINDING: Blocker-2 from s221 (partial-arm NAMETRAP hazard) is structurally resolved by WL-DELETE — IR_DEREF now unconditionally admitted at emit.cpp line 1906; VAR_REF+SUBSCRIPT+DEREF arms atomically on every pinned graph. Only Blocker-1 (unpinned LEA drift) remains as a guard (x86_fb_pinned() in zd_wl_kind). Stash also contained an IR_MAKE_LIST ZD arm in bb_make_list.cpp (no matching zd_wl_kind admission arm — dead code); reverted, deferred to next ZK-2 slice. **Watermarks re-derived at `3c5c1f71` (WL-DELETE base):** default m3 **199/7/5** · CELLS=1 m3 **176/60/26** · SN4 smoke 6/7 (pattern test pre-existing WL-DELETE crater, not ZK-2). Verdict diff CELLS=1 before/after: **zero delta** (pinned graphs already passing; unpinned correctly decline). Witnesses: write(x) armed=4/4 K_total=48 ✓; write(x+1) armed=7/7 K_total=96 ✓; fib(10)=55 both arms both media ✓; jcon_kross correct CELLS=1 (Blocker-2 confirmed resolved) ✓; rung23 output=42 correct (pin guard working; exit SEGV pre-existing with VR=0 killswitch). Two files: emit.cpp IR_VAR_REF arm comment updated (stale ⛔ warning removed, WL-DELETE resolution noted); bb_var_ref.cpp ZD arm (op_zres && op_sa>=0). No .s regen (CELLS=0 default path byte-identical). **NEXT ZK-2 INVESTIGATION — IR_TO+LVA DEPTH-MODEL BUG (rung02_proc_locals `every total := total + (1 to n)`, got 4 expected 15):** ZD_TO_STAGE debug (emit.cpp:2672) shows IR_TO at index 5: `zd_out[i]=96, zd_out[k]=48 g_zd_read=48, zd_out[k]=64 g_zd_read=32`. Graph has `n` (param, LVA cell 0) + `total` (local, LVA cell 1) carved at α before the run; IR_LIT(1) and IR_VAR(n) are the IR_TO predecessors at indices 3 and 4. The depth model counts LVA carves in `zd_out` but the `ZOPQ` computation `zd_out[consumer]-zd_out[producer]` gives the wrong offset for the lo/hi args because the LVA α-carve depth is already baked into `zd_out` for the LVA nodes but the IR_TO predecessors sit AFTER the LVA cells in execution order, so the difference is correct by construction — but the resulting `g_zd_read` values (48 and 32) suggest the counter+result cells of IR_TO are being read at the WRONG offsets by the `bb_to.cpp` ZD arm (ZLOC(0) counter is at [rsp+16] relative to IR_TO's own α carve, but ZOPQ(0) reads from [rsp+48] and ZOPQ(1) from [rsp+32], which means the predecessors' cells ARE at those offsets and IR_TO's own cell is at [rsp+0]). SUSPECT: `bb_to.cpp` ZD arm reads `ZOPQ(0,0/8)` for lo and `ZOPQ(1,0/8)` for hi using the staged `op_zread[0]` and `op_zread[1]` depths, but `ZLOC(0)` (the counter) is at a fixed `[rsp+16]` RELATIVE TO IR_TO'S OWN ALPHA — if the ZOPQ depths account for the LVA carve base but `ZLOC(0)=[rsp+16]` is absolute-from-current-rsp, there is a 16-byte skew whenever nparams+nlocals > 0. Read `bb_to.cpp` ZD arm and compare ZLOC(0) vs ZOPQ(0) relative-vs-absolute addressing before touching anything.

## ⛔⭐ LIVE CURSOR — s221 (2026-08-08, Claude Sonnet 4.6 — ZK-2 IR_VAR_REF PINNED ARM LANDED; SCRIP `69a71bdd`)
**NEXT RUNG = ZK-2 (continue — IR_VAR_REF landed for pinned graphs; unpinned + VAR_REF-in-subscript-run remain).** IR_VAR_REF admitted on the cells arm for pinned, local-only graphs. Two files: `emit.cpp` zd_wl_kind additive arm (icn_cells_graph + x86_fb_pinned() + !is_global(), killswitch SCRIP_ZD_ICN_VR=0); `bb_var_ref.cpp` ZD arm (`_.op_zres && x86_fb_pinned()`) — writes `{DT_N, &rbp_slot}` to ZRES(0/8). The downstream `IR_DEREF` (already ZD-admitted line 1931, `bb_deref.cpp` ZD arm already extant) reads `ZOPQ(0,0/8)`, calls `rt_deref`, writes dereferenced value to its own ZRES — the full lvalue→rvalue chain arms atomically. **STANDING OPEN:** (a) unpinned (rsp-based) graphs — LEA drift problem still applies (rsp+depth+sa address stored in ZRES(8) reaches different memory after subsequent sub rsp carves); needs a re-home mechanism or depth-at-snapshot correction; (b) IR_VAR_REF as SUBSCRIPT base — when the run is VAR_REF→SUBSCRIPT, subscript's ZD arm reads ZOPQ(0,0/8) expecting a raw TABLE DESCR but VAR_REF produces {DT_N,&slot}; subscript's ZD arm must call rt_deref on operand-0 before passing to rt_subscript (or bb_subscript needs a separate cells-arm nametrap-deref path). These two classes are measured open; the all-or-nothing run gate keeps them declined as a unit. **Watermarks at commit HEAD:** default m3 **PASS=204 FAIL=28 XFAIL=26** (rung09_loops_repeat_counter is the documented rc=139 pre-existing SEGV, stdio-flush race, both arms identical, s219) · CELLS=1 census: `write(x)` pat_flat armed=4/4 K_total=48 ✓; `write(x+1)` armed=7/7 K_total=96 ✓; fib(10)=55 CELLS=1 ✓; globals_on_stack=0 (ZK-5 assertion held) ✓ · SN4 smoke 7/7 (R-ICN-D) ✓. No `.s` regen (codegen for CELLS=0 default path unchanged; CELLS=1 cells-arm only).

## ⛔⭐ LIVE CURSOR — s221 (2026-08-08, Claude Sonnet 4.6 — ZK-2 TWO-BLOCKER FINDING; SCRIP HEAD f41f21b7 unchanged; doc-only)
**NEXT RUNG = ZK-2 — ARM IR_DEREF FIRST, THEN IR_VAR_REF.** IR_VAR_REF admission was attempted and fully measured this session. Two independent blockers found and documented; all edits reverted; tree clean. **BLOCKER 1 — UNPINNED GRAPH LEA DRIFT:** `bb_var_ref` ZD arm emits `lea rdx, FRQ(op_sa)`; for unpinned graphs (x86_fb()=rsp), FRQ bakes `[rsp + op_sa + depth_at_alpha]`. As subsequent sub rsp,16 carves accumulate, the RSP-relative address drifts — measured concretely: `n3_var_ref_α` emits `lea rdx,[rsp+288]` and `n8_var_ref_α` also emits `lea rdx,[rsp+288]` but RSP has moved 80B between them, reaching different absolute memory; rung23_table_basic produced silent-wrong (no output, rc=0). Pin guard `x86_fb_pinned()` fixes this class. **BLOCKER 2 — LVALUE/RVALUE CONSUMER SPLIT (pinned graphs):** `rt_subscript_var` ALWAYS returns NAMETRAP `{DT_N,slen=2,vc}` — lvalue consumers (rt_assign_var) handle it correctly; rvalue consumers (right(), write() via IR_CALL_BUILTIN_ICON) expect a plain value DESCR and misbehave. `IR_DEREF` (the rvalue-extraction node, calls rt_deref, already has a full ZD arm in bb_deref.cpp) is NOT yet admitted on the cells arm. Without it, VAR_REF+SUBSCRIPT+DEREF cannot arm atomically, and a partial-arm graph (13/28 in xprint, jcon_kross flat_gen) produces truncated output. MEASURED: `SCRIP_ZD_ICN_VR=0` → jcon_kross correct; with VAR_REF admitted on pinned graph → jcon_kross truncated. **s220 DIAGNOSIS CORRECTED:** rt_subscript_var, rt_assign_var, and rtx_icnsub.S ALL already handle {DT_N,slen=1} correctly via IS_VARREF_fn+rt_deref paths — no consumer template changes needed; the blockers are structural. **CORRECT NEXT STEPS:** (1) add IR_DEREF admission arm to zd_wl_kind (icn_cells_graph gate, killswitch SCRIP_ZD_ICN_DEREF, K=16 default, nops=1 already in zd_nops, bb_deref.cpp ZD arm already exists — one line in zd_wl_kind only); (2) re-admit IR_VAR_REF with x86_fb_pinned() + icn_cells_graph guards — the run gate then ensures VAR_REF+SUBSCRIPT+DEREF arm atomically. Witnesses: rung23_table_basic (lvalue path) + jcon_kross (rvalue path) + fib(10) ZK-4 retention + SN4 crosscheck. Watermarks at HEAD (s219/s220 values, no codegen this session): default m3 **218/45/30** (±1 flake rung09_loops_repeat_counter) · CELLS=1 m3 **184/79/30**. Full finding: `FINDING-2026-08-08-CLAUDE-SN46-ZK2-VAR-REF-TWO-BLOCKERS-UNPINNED-LEA-AND-LVALUE-RVALUE-SPLIT.md`.

## ⛔⭐ LIVE CURSOR — s219 (2026-08-08, Claude Sonnet 4.6 — ZK-5 COMPLETE; SCRIP `16e88990` / `49bb21f1`)
**NEXT RUNG = ZK-2 (LEAF SPINE COMPLETE — relocated after ZK-5 per s218 order sync).** ZK-5 landed this session: GVA routing confirmed correct (M3 GVA active, `g_gva_active=1`, `gva_index_of` staged via op_gva_k for all IR_VAR/IR_ASSIGN globals on cells arm; `bb_var_global`/`bb_assign_global` templates use `ABSQ(RT_GVA_VA+k*16)` — no spine cells for globals). Census assertion added to `[ZK-CENSUS]` line: `globals_on_stack=0` field. Gate: `scripts/test_gate_icn_zk5_gva.sh` — 3 locks GREEN. Witnesses: `global_heavy.icn` (3 globals, byte-agree default=3/cells=3); palindrome (byte-agree yes/no/yes); ZK-4 fib(10)=55 both media held. Suite at commit HEAD: default m3 **186/10** · CELLS=1 m3 **164/42/5** · SN4 smoke 7/7 (R-ICN-D held). Two additive edits in emit.cpp census block (armed-node loop + fprintf); zero emission changes; zero g_emit writes; census inert when SCRIP_ZD_CENSUS≠1.
⚠ **WATERMARK INSTABILITY FOUND (parallel s219 walker, same session):** three runs of one unchanged binary gave 218/45/30, 217/46/30, 219/44/30. Verdict-list diff names exactly two oscillating tests: `rung09_loops_repeat_counter` (REAL SEGV on baseline arm, rc=139 6/6, PASS/FAIL decided by a stdio-flush race) and `rung37_subscript_genproc` (clean in isolation, harness-timing flake). NOT caused by this ladder. ⛔ ZK-8's ratchet must NOT be baked until both are fixed or quarantined. METHOD for future walkers: diff per-test verdict lists (not totals) whenever a watermark disagrees by ±1. Full write-up + monitor-first next steps in `FINDING-2026-08-08-CLAUDE-SN46-ICN-WATERMARK-IS-NOT-A-NUMBER-TWO-FLAKY-TESTS-AND-A-REAL-SEGV-UNDER-A-FLUSH-RACE.md`.

## ⛔⭐ LIVE CURSOR — s218 (2026-08-08, Fable — HEAD RE-DERIVE both arms + rung/checkbox sync; SCRIP `69c476a0`, doc-only, no code)
**NEXT RUNG = ZK-5 (unchanged).** Both arms re-proved at HEAD: baseline m3 **218/45/30** (+1 vs the s217 217, 0 regressions) · **CELLS=1 m3 184/79/30 EXACT match** to the ZK-4 close — no drift, R-ZK-A isolation holding (ZFRAME twin landed FR-4 at the same HEAD; see its s9 cursor). m4 default via sweep instrument = 210/60/30 T=300 (recipe + denominator caveat in ZFRAME s9; canonical runner = its FR-6d). **m4 CELLS=1: NOT completed** — the sweep WEDGED after ~jcon_lists with 77 FAILs logged: the instrument had no timeout on the gcc/as stage and a CELLS-emitted `.s` churned it unbounded. Two facts for the next walker: (a) any m4 runner needs the gcc stage timeboxed; (b) whatever `.s` CELLS=1 emits right after rung36_jcon_lists (alphabetical) is a compile-artifact witness worth keeping. **ZK-5 note:** the GUARD half already landed (`63280689` — globals excluded from cells-arm ZD admission); remaining = GVA routing proper + census assertion. **Doc sync this session:** ZK-1/ZK-3/ZK-4 checkboxes ticked per their landed cursors (the list had lagged the cursor, violating stop-at-first-`[ ]`); open ZK-2 remainder RELOCATED after ZK-5 so walk order agrees with the declared NEXT — rationale on the rung itself.

## ⛔⭐ LIVE CURSOR — s217 (ZK-4 COMPLETE at SCRIP `087bd707`)
**NEXT RUNG = ZK-5 — GVA: globals stay OFF the stack by design. Route IR_VAR/IR_ASSIGN GLOBAL arm to GVA slots. Interlock with GOAL-ICN-GVA-M3.**

**ZK-4 COMPLETE — s217 LANDED at `087bd707`:** LVA (params + locals + return value) on the RSP FORTH spine.
Two commits this session:
- SLICE 3 (`26fb52e0`): SUCCEED-GIN narrowed by beta-omega conjunct — `write(42)` m3 CELLS=1 rc=0 ✓
- SLICE 4 (`087bd707`): IR_RETURN admitted on cells arm — proc body fully armed. THREE ONE-AUTHORITY EDITS: zd_wl_kind (SCRIP_ZD_RETURN=0 killswitch, gated icn_cells_graph, det-return only); zd_k (IR_RETURN K=0 class); zd_nops (1-operand arm). ZD template arm in bb_return.cpp: ZOPQ(0,0/8) → FRQ(0)/FRQ(8) → x86_gamma() with op_zgpop K_total release.
CENSUS: f(x) `proc_flat n=5 armed=5 K_total=64 declined_nodes=0` ✓; f(x, local y) `proc_flat n=7 armed=7 K_total=80 declined_nodes=0` ✓.
COMPLETION CRITERION: fib(10) m3 CELLS=1 → 55 rc=0 ✓; fib(10) m4 CELLS=1 (full pipeline: compile→as→gcc→run) → 55 rc=0 ✓. Icon suite baseline: PASS=217 FAIL=46 XFAIL=30 (no regression). CELLS=1: PASS=184 FAIL=79 XFAIL=30.

**ZK-4 SLICE 3 COMPLETE — s217 LANDED at `26fb52e0` — m3 CELLS=1 SEGV CLOSED:**
ROOT CAUSE: `zd_plan` SUCCEED-GIN arm (`emit.cpp:2095`) set `gin=1` (suppressing `zgpop`) whenever CALL_BUILTIN's γ arc terminated at IR_SUCCEED — including the top-level `write(42)` topology where ω→FAIL, not a beta port. The prior comment claimed "SUCCEED gamma always routes to a consumer beta, never to the statement terminal" which `write(42)` falsifies: its ω is FAIL, not TO.β. Without `zgpop`, the γ `jmp main_γ` carries no `add rsp,K_total`; in m3 the outer RX machinery sees RSP 32B short and SEGVs after correct output. In m4 `exit()` ignores RSP — invisible until m3 ran.
ONE-LINE FIX (`emit.cpp:2095`): add `&& port_sz_beta(nodes[i]->ω.sz)` conjunct to SUCCEED-GIN — fires only when ω carries the beta-tag (0xce 0xb2), i.e. the every-body topology. The write(42) ω is FAIL, so `gin` stays 0, `zgpop` gets its K_total=32, and the statement-terminal γ jmp emits `add rsp,32` before `jmp main_γ`. ONE AUTHORITY: `port_sz_beta` is the sole beta-tag sniff (`emit.cpp:1873`).
**INVARIANTS:** `write(42)` m3+m4 CELLS=1 → `42` rc=0 ✓ · `every write(1 to 3)` m3+m4 CELLS=1 → `1 2 3` rc=0 ✓ · SN4 smoke 7/7 × 2 ✓ · Icon suite PASS=218 FAIL=45 XFAIL=30 (no regression) ✓

**ZK-4 SLICE 2 LANDED (WIP, s216 `a3db5358`) — f1 m4 GREEN (42 rc=0):** Four additive edits in `emit.cpp` + `xa_flat.cpp`, all gated on `icn_cells_graph`. THREE ROOT CAUSES of the exit-path SEGV diagnosed and fixed:
1. **DC stub jumped to `lbl_α_body` (bypassing flat_lcl_proc prologue).** `flat_dc_body_p` selected `lbl_α_body` for non-zframe graphs. But the CLASS ZF epilogue uses `xa_flat_wire_hdr_base() = flat_frame_bytes+(np+nl)*16` as kt_adjusted. DC stub sub'd only `flat_frame_bytes+16` — for f1 (np=1,nl=1) this is a 32-byte RSP mismatch at the epilogue's `lea rsp,[rbp+kt_adjusted]` → SEGV on exit. **FIX:** `flat_dc_body_p = &lbl_α` for cells-arm CLASS ZF (mirrors `xa_flat_class_zf()` ONE AUTHORITY).
2. **`lbl_α` not defined in binary mode for cells-arm.** Only text mode + zframe got `emit_label_define_bb(&lbl_α)`. Cells-arm DC stub then referenced an unresolved label → abort. **FIX:** extend binary-mode define condition.
3. **`rt_lcl_proc_args_install` reads `g_pcall[top-1].nargs = 0` on DC path** (no pcall record pushed) → zero args installed → `x=&null`, `x+1=1` (wrong). The zframe track uses `rt_icn_zframe_args_install` (reads `g_call_args[]` directly). **FIX:** cells-arm flat_lcl_proc prologue calls `rt_icn_zframe_args_install`; non-cells keeps `rt_lcl_proc_args_install`. ONE AUTHORITY: `_use_zframe_install` gate at the prologue emit site.
4. **DC stub branch in `xa_flat_dc_stub_str`** extended: cells-arm CLASS ZF routes to the arg-staging path (`rt_arg_stage`→`g_call_args[]`, double-push r11, `jmp lbl_α`). Predicate mirrors `xa_flat_class_zf()`.
**INVARIANTS:** f1 m4: `write(f(41))` → `42` rc=0 ✓ · ZK-3 held (`every write(1 to 3)` → `1 2 3` rc=0) · SN4 byte-identity 34/34 IDENTICAL (R-ICN-D) · Icon suite PASS=218 FAIL=45 XFAIL=30 (no regression).
**ZK-4 SLICE 3 COMPLETE — s217 LANDED at `26fb52e0`:** m3 CELLS=1 statement-terminal release gap fixed. One-line edit to `emit.cpp` zd_plan SUCCEED-GIN arm.
ROOT CAUSE (confirmed by ZD diag + IR dump): `zd_plan` SUCCEED-GIN arm (emit.cpp:2095) set `gin=1` whenever CALL_BUILTIN's γ terminated at IR_SUCCEED, suppressing `op_zgpop`. For `write(42)` the γ→SUCCEED arc IS the statement-terminal exit (IR_SUCCEED.γ = NULL), not an intra-statement relay. `zgpop[CALL_BUILTIN]=0` → x86_gamma() emits no `add rsp,K_total` → RSP 32B short at `main_γ` in m3 → SEGV after correct output.
FIX: additive conjunct `&& port_sz_beta(nodes[i]->ω.sz)` — same beta-tag discriminant used at emit.cpp:1300 (op_sb gate). The every-body topology (ω carries 0xce 0xb2 routing to IR_TO.β) still suppresses; `write(42)` (ω→FAIL, no tag) now stages gpop=32 correctly.
VERIFIED: `write(42)` m3 CELLS=1 → `42 rc=0` ✓ · `every write(1 to 3)` m3+m4 CELLS=1 → `1 2 3` ✓ · `write(f(41))` m3 CELLS=1 → `42 rc=0` ✓ · SN4 byte-identity IDENTICAL ✓ · Icon suite baseline PASS=217 FAIL=46 XFAIL=30 (no regression) · Icon suite CELLS=1 PASS=184 FAIL=79 XFAIL=30.

**ZK-4 SLICE 1 LANDED (s215 `9e169ab9`):** CLASS ZF routing; `xa_flat_wire_hdr_base()` + `xa_flat_class_zf()` ONE-AUTHORITY helpers. f1 printed 42 (value correct), SEGV on exit.

**ZK-3 COMPLETE — s215 LANDED at `a96369e0`:** m3 CELLS=1 SEGV root-caused and fixed.
ROOT CAUSE: `lower_icon.c` zframe stamp loop set `zframe_graph=1` on ALL Icon graphs including `icn_cells_graph=1` graphs — violating R-ZK-A mutual exclusion. Driver's ICN-FR-2 branch then routed cells-arm main through `icn_zf_main_call` (rcx/rdx wire pointers the cells arm never reads → SEGV). THREE ADDITIVE EDITS: (1) `lower_icon.c` stamp loop gains `&& !icn_cells_graph` conjunct — PRIMARY R-ZK-A enforcement at source; (2) `scrip.c` m4 TEXT driver: `if (bbg->zframe_graph && !bbg->icn_cells_graph)` defence-in-depth; (3) `scrip.c` m3 BINARY driver: same at `icn_zf_main_call` site. VERIFIED: `procedure main(); end` m3 CELLS=1 rc=0; `every write(1 to 3)` m3+m4 CELLS=1 prints `1 2 3` ✓ — ZK-3 witness GREEN BOTH MEDIA.
**IR.h:** Both `zframe_graph` and `icn_cells_graph` kept — dual embodiment, calloc-zeroed, never both set. Mutual exclusion now enforced at the stamp site.
**ZK-2 PARTIAL (leaf spine):** `bb_var.cpp` ZD arm; `bb_assign_local.cpp` ZD arm. Remaining: `IR_KEYWORD_ICON`, `IR_RETURN`, `IR_CONJUNCTION`, `IR_DISJUNCTION`, `IR_CALL_PROC_STAGED`.
**NEXT SESSION:** ZK-4 LVA — proc α-grant carves `(nparams+nlocals)×16` as FIRST cells; params install from `g_call_args[]`; locals null-init. See ZK-4 rung spec below. Recursion witness: fib.icn.

**s214 DETAIL (prior session):** ZK-3 s214 — TWO ADDITIONAL FIXES (emit.cpp + bb_call_fn.cpp): (1) bb_call_fn.cpp op_zgpop pre-zero on op_sb arm; (2) emit.cpp GLUE-O icn_cells_graph guard. `every write(1 to 3)` m4 GREEN. m3 SEGV open → closed this session.

**s213-cont LANDED at `4b0f349b` (prior session detail):** Five fixes in three files (emit.cpp + bb_call_fn.cpp + bb_to.cpp):
1. `emit.cpp:3001` — zframe prologue suppressed for icn_cells_graph (R-ZK-A).
2. `emit.cpp:2077` — SUCCEED-GIN: gamma->IR_SUCCEED on icn_cells_graph = intra-stmt (gin=1).
3. `emit.cpp:1300` — op_sb every-body flag: detects ir_a_Every body-CALL topology via IR_GOTO chase.
4. `emit.cpp:719` — write-route bypass.
5. `bb_call_fn.cpp` — op_sb=1 arm: zeroes op_wpop, calls x86_omega().
6. `bb_to.cpp` — Fix broken `inc ZLOC(0)`: load-inc-store replaces missing XK_RSP64 inc encoder.

### ZK-0 BASELINE TABLE (re-derive every session at YOUR head — three tracks move daily)
Census instrument: `SCRIP_ICN_CELLS=1 SCRIP_ZD_CENSUS=1 ./scrip --run <prog.icn>` — emits `[ZK-CENSUS]` per graph when `icn_cells_graph=1`. Infrastructure: `icn_cells_graph` field at `IR.h:256` (APPENDED AT STRUCT END s141), set by `lower_icon.c:1226+1300` (only setter), read at `emit.cpp:2299`. Census block: `emit.cpp:2296-2326`. Line numbers drift — re-grep before trusting.
**FALSIFIABILITY PROVEN (both injections, s1):** (1) `write(42)` → `armed=2 K_total=32` (IR_LIT_INTEGER K=16 + IR_CALL_BUILTIN_ICON K=16 — census moves UP on known-armed program); (2) `local x; x:=5; write(x)` → `armed=0, blockers: IR_ASSIGN count=1 IR_VAR count=1` — census names the correct first-blocker on known-declined program.
**ZD one-authority line numbers at `f2751777`** (re-grep every session): `zd_wl_kind` ~:1903 · `zd_nops` ~:1966 · `zd_k` ~:1968 · `zd_plan` ~:1969 · census block ~:2296 · staging choke ~:833.
**IR_TO status:** whitelisted in `zd_wl_kind` (:1959) + K=32 in `zd_k` (:1968) but declined in census because its runs contain `IR_BOUND`/`IR_UNMARK` (the `every` bounded-expression wrapper) — those are the ZK-3 bound-terminal constructs not yet in the whitelist. `IR_BOUND` is the first-blocker on the `every i := 1 to 3` witness program.
**Blocker histogram across test programs (at HEAD, cells arm, before any ZK-1+ work):**
- hello.icn: `n=2 armed=2 K_total=32 declined=0` — pure lit+builtin, fully armed already ✓
- palindrome.icn: `n=36+9 armed=0` — blockers: IR_ASSIGN(4) IR_VAR(6) IR_BINOP_TEST(2) IR_DISJUNCTION(1) IR_CONJUNCTION(1) IR_VAR_REF(2) IR_RETURN(2) IR_CALL_PROC_STAGED(3)
- roman.icn: `n=64+9 armed=0` — blockers: IR_MAKE_LIST(2) IR_ASSIGN(5) IR_VAR(6) IR_BOUND(1) IR_VAR_REF(3) IR_BINOP_TEST(1) IR_CONJUNCTION(1) IR_UNMARK(1) IR_CALL_PROC_STAGED(3)
- wordcount.icn: `n=21 armed=0` — blockers: IR_ASSIGN(3) IR_VAR(3) IR_SCAN_ENTER(1) IR_SCAN_UPTO(1) IR_SCAN_TAB(2) IR_SCAN_MANY(1) IR_CONJUNCTION(1) IR_SCAN(1)
- queens.icn: all graphs armed=0 — blockers: IR_DISJUNCTION(3) IR_RETURN(1) IR_VAR_REF(3) IR_BINOP_TEST(3) IR_BOUND(1) IR_CONJUNCTION(1) IR_UNMARK(1) IR_CALL_PROC_STAGED(3)
**SN4 byte-identity (R-ICN-D):** `SCRIP_ICN_CELLS=1` is invisible to SNOBOL4 — `lower_snobol4.c` never sets `icn_cells_graph`; byte-identity verified on assign/control/strings corpus subsets.
**ZK-1 target:** the `zd_plan` per-node consultation already reaches Icon graphs (the whole-graph early-return wall was deleted in s23a CARVE-DATA-ERAD). The blocker for IR_TO runs is `IR_BOUND`/`IR_UNMARK` membership — ZK-1 resolves the per-graph admission routing so the planner consults each node, not a graph-level veto. Per the goal file: the cells arm needs a "plan anyway, release-by-bracket" mode for graphs `zd_stub_ok` declined whole-graph (pinned=flat_pat||flat_gen||flat_deep_arrival). Re-read `zd_stub_ok` at your head to verify what still pins before touching it.

## ⛔ CONCURRENCY BANNER — THREE LIVE TRACKS ON ONE EMITTER (read before any edit)
1. **SNOBOL4 PB** (`GOAL-SNOBOL4-BB.md`, PLAN top priority) — statement-execution ladder on the SHARED emitter. Its watermark is a gate on every commit here.
2. **ICN-ZFRAME-RESTORE** (`GOAL-ICN-ZFRAME-RESTORE.md`, concurrent Sonnet session, started 2026-08-07) — **FRAMES on STACK**: whole-graph ζ frame per Icon graph behind `zframe_graph`/`SCRIP_ICN_ZFRAME`, anchor `8d0665c8` parity target 252.
3. **THIS FILE** — **CELLS on STACK**: per-BB α-grant on the RSP FORTH spine, non-popping, one terminal release, 100% coverage.
Tracks 2 and 3 are Lon's deliberate dual embodiment ("We would have FRAMES on STACK and CELLS on STACK being developed simultaneously") — the ARCH-ICON two-backends precedent (Lon 2026-06-03): BOTH KEPT, switch-selected, A/B-able, until Lon picks a default. **ARMOR:** (a) every behavioral edit flag/env-gated with `=0` reproducing pre-rung bytes EXACTLY (identity is a completion criterion, the ZFRAME 2d idiom); (b) struct fields APPENDED AT END (s141 ABI law); (c) shared choke sites (`zd_wl_kind`/`zd_k`/`zd_nops` ONE-AUTHORITY lines, the ZD staging choke ~emit.cpp:833, the BLOB-GRANT block) take ADDITIVE arms only — never edit another track's arm; (d) SN4 crosscheck byte-identity every commit (R-ICN-D borrowed verbatim); (e) `git pull --rebase` before every commit; re-derive every count at session start; (f) line numbers in this file drift daily — re-grep, never trust.

## ROUTING RULING (draft R-ZK-A; Lon overrides at any session start)
The two Icon storage arms select on ONE switch at ONE site: the LOWER site where ZFRAME's R-ICN-A sets `zframe_graph`. `SCRIP_ICN_CELLS=1` (opt-IN while this ladder walks) suppresses `zframe_graph` for graphs this ladder has admitted, routing them to the cells arm; unset ⇒ ZFRAME's arm (or today's path where ZFRAME hasn't landed yet). One graph is NEVER in both arms. A/B = flip one env var, rebuild nothing. Named for WHAT it selects (per-activation storage regime), never a language, per the NO-LANGUAGE-IDENTITY FACT RULE — the env prefix follows the existing `SCRIP_ICN_FB`/`SCRIP_ICN_ZFRAME` precedent.

## WHAT THE SN4 SCAN FOUND (read off HEAD `f2751777`, 2026-08-07 — the machinery this ladder EXTENDS, never re-invents)
1. **The ZD spine is live and Icon-aware already.** `emit.cpp`: `zd_wl_kind` (~:1903, per-kind admission whitelist, per-family env killswitches `SCRIP_ZD_SR/MATCH/ALT/CAP/PATREF…`), `zd_nops` (~:1966, ONE AUTHORITY for staged operand reads → `op_zread[]` depth differences), `zd_k` (~:1968, ONE AUTHORITY for K = α-carve bytes: 0 transparent/sink · 16 default · 32 `IR_TO`), `zd_plan` (~:1969, run detection + depth model + `zgpop[]` terminal release + the UCLAIM/`zvo` owner table). **s211 already opened Icon's door: `IR_TO` (K=32, β re-enters WITHOUT release — the non-popping law verbatim) and the LIT kinds are admitted; `IR_CALL_BUILTIN_ICON` is in `zd_nops` (s208) and admission landed via ICN-ZD `6967f531`.** This ladder finishes what s211 started.
2. **The staging choke is the ONE authority** (~:833–839): planner numbers land at g_emit exactly once; `op_zres/op_zgpop/op_zread[]/op_zkind[]` cleared UNCONDITIONALLY so glue/non-drive fills never inherit a regime. The s22k SECOND-SPELLING disease (K spelled twice → silent stack skew) is the standing reason every constant lives on ONE line.
3. **The non-popping law + release discipline:** zero intermediate pops; γ-jmp X86H hook emits the release; static form `add rsp, K_total` at the statement terminal; **dynamic form** (ARB-LON-K16, 2026-08-06) `g_arbk16_stmt` latch → `op_stmt_dyn` rides every node to the terminal → mech-2 whack `lea rsp,[rbp+op_zgpop]` on BOTH exits — because a static ΣK under-frees by 16×commits when growth is variable. Under STF the statement leave-stub `mov rsp,rbp; pop rbp` is the depth-independent cut and staged pops are ZEROED at the choke (ZGPOP-STF s22a).
4. **The composition laws for suspended-state-on-the-spine** (FINDING 2026-08-06 ARB-LON-K16): **re-homing** — a live cell that must stay at a static offset is re-carved at the frontier on each σ commit ([rsp+kk] read → carve 16 → copy forward; kk = Σ zd_k over the span, THE ONE K AUTHORITY); **Law A** — an inner dynamic activation under a legacy outer must net ZERO delta or decline; **Law B** — a pending cell riding the frontier ACROSS a construct forbids interposition. Fail-side static release stays lawful because the exhaust cascade retracts every committed cell during unwind.
5. **The ζ two-lifetime-class law** (`src/contracts/zeta_choices.h` header): control-flow-lifetime ζ rides the machine stack (ZC_PORT_FORTH cells on rsp); LONG-LIVED ζ (suspended generator frames, coexpr stacks) heap-allocates via `rt_zls_alloc`. **This ladder's charter REFINES that law for Icon generators:** their suspended state moves to A STACK (ZK-6 decides which). Coexpr stacks stay pthread (Lon charter, both files). The ZC_PORT_INLINE note also records the r12 wall: a register cursor breaks sibling refs — cursor-stays-a-memory-cell is the proven shape.
6. **SN4 GVA precedent:** globals ride GVA slots, off-stack, and "GVA slots already won" (FINDING 2026-07-31 SN4-RTX) — the model for ZK-5.

## REGISTER CONTRACT FOR THIS LADDER (verify against x86_asm.h, the register source of truth, every session)
- **RSP** = the FORTH spine top. Grants `sub rsp,K` at α; releases ONLY at bound terminals (ZK-3).
- **R13/R14/R15 = RESERVED for the BB_SCAN_* family** (Σ subject base · δ cursor · Δ length — Lon, this charter). The cells arm NEVER claims, spills to, or scratches them. Gate in ZK-8.
- **R12 — FREE FOR THIS LADDER'S USE (Lon ruling 2026-08-07).** Use it for whatever is best — pending-record top (analogue of SN4's DCAP/CAS r12 role), suspension base, or any other per-BB management role ZK-6 or ZK-7 finds optimal. ⛔ The RULES.md gate `test_gate_icn_no_stack` was written to ban the OLD value-stack r12-as-TOS; Lon's explicit 2026-08-07 release supersedes that restriction for this ladder's arm. Document what ZK-6 assigns r12 to in this cursor and in the gate script so future walkers know the ruling applies.
- **RBP** — belongs to the FRAMES track for graph frames; the cells arm touches it ONLY as the bound-bracket base where the mech-2 dynamic whack requires one (the ARB-K16 shape), and only inside this arm's own bracket protocol.
- **RBX** = heap bump-frontier (dormant under ZC_PORT_FORTH) — untouched. **RAX/RCX/RDX** scratch + wire ABI (rcx=γ rdx=ω at proc enter). GVA addressing per ZK-5 interlock.

## RUNGS (stop at first `- [ ]`; every rung committable + SN4 byte-identity + Icon suite non-regression on BOTH arms; killswitch per behavioral family)

- [x] **ZK-1 — ADMISSION** ✅ (s213 `5144d4be` zframe-exclusion bypass + R-ZK-A mutual exclusion at the stamp site `cffe7507`; criterion met — per-node consultation proven on the every-body class, census falsifiability s1; checkbox synced s218): per-node planning must reach pinned graphs. The ICN-CARVE-2 tension verbatim: `pinned = (flat_pat||flat_gen||flat_deep_arrival)` negates three of `zd_stub_ok`'s six conjuncts, so pinned graphs return at the TOP of `zd_plan` and no per-node arm is consulted (queens 10/10 `deep=1, stub_ok=0`, s204). Resolve IN the planner for the cells arm — a per-graph "plan anyway, release-by-bracket" mode — never a workaround. ⚠ ZFRAME's arm CHANGES what is pinned; this rung keys on the cells arm's own routing (R-ZK-A), reads ZFRAME state but edits none of it. Completion: census shows per-node consultation on ≥1 previously top-declined graph class; both other tracks' watermarks held.

- [x] **ZK-3 — BOUND TERMINALS** ✅ COMPLETE both media (s213–s215, SUCCEED-GIN β-tag conjunct `26fb52e0`/`b4ac8795`; checkbox synced s218)**: where Icon releases.** SN4 releases at the STATEMENT; Icon's semantic analogue is the BOUNDED EXPRESSION — the point where all generators die and results are limited to one. Deliverable: (a) the bounding map for SCRIP-Icon lowering (semicolon-separated expressions, control-structure control clauses, every-do bodies) — ground truth `refs/jcon-master/tran/irgen.icn` bounding + `refs/icon-master/src/runtime/interp.r` mark/efail/unmark; (b) `zd_plan` terminal-release (`zgpop`) lands at bound terminals for cells-arm graphs; (c) the dynamic latch: any variable-growth construct inside the bound (suspends surviving a σ-commit analogue) sets the `op_stmt_dyn`-style verdict so the terminal emits the mech-2 whack, not a static ΣK (the ARB-K16 lesson: static under-frees by 16×commits). Completion: a bound with a generator inside releases to entry depth on BOTH exit edges, witnessed in both media; fail-edge retract cascade proven on an exhaust witness.

- [x] **ZK-4 — LVA** ✅ COMPLETE s217 (slice-4 `14e6f33f`; fib(10)=55 both media; CELLS=1 suite 184/79/30; checkbox synced s218)**: procedure params + locals as cells.** The α-grant of a proc graph carves `(nparams+nlocals)×16` as its FIRST cells; params install from `g_call_args[]` (the s210/s211 mechanics), locals null-init. Reference staging: STATIC sites read planner-staged depths exactly like `op_zread`; DYNAMIC sites (post-growth) read via the bound-bracket base or a base cell — decide by evidence, record the ruling. The admission edit is the ZD-2h line (`SCRIP_ZD_PINLOCAL` scaffold, landed inert s204): conjunct becomes "global, OR local on cells-arm graph" — behaviorally named. ⚠ Lon's ⛔ on that line was released for the s210 plan; confirm it extends to this arm at session start. Recursion witness: fib.icn — each activation grants fresh cells deeper on the spine; LIFO return unwinds them (the s89 ruling: gen procs are ordinary BBs on the calling spine; escapees are coexpr only). Completion: `f("hello")` + fib green on the cells arm both media · SN4/ZFRAME held.

- [x] **ZK-5 — GVA: globals stay OFF the stack by design.** ✅ COMPLETE s219 (`16e88990`). Route the `IR_VAR`/`IR_ASSIGN` GLOBAL arm to GVA slots — process-lifetime storage is not control-flow-lifetime ζ and never occupies a cell. ⚠ INTERLOCK: `GOAL-ICN-GVA-M3` owns Icon GVA addressing ([rbx+k*16] mode-3 arena per its ladder; ARCH-ICON records ABSQ absolute as the contract) — this ladder takes NO position on the addressing, consumes whichever wins, and asserts only: census shows globals=GVA, stack cells for globals=0. Completion: census assertion green (`globals_on_stack=0` field in `[ZK-CENSUS]`); global-heavy witness byte-agrees both arms; gate `scripts/test_gate_icn_zk5_gva.sh` 3/3 PASS. GUARD half (`63280689`) + routing confirmed (`g_gva_active=1`, `gva_index_of` staged, ABSQ template arm). Two additive edits in emit.cpp census block.

- [ ] **ZK-2 — LEAF SPINE COMPLETE (finish what s211 started).** ⬅ RELOCATED after ZK-5 (s218 order sync: the cursor's NEXT=ZK-5 conflicted with stop-at-first-`[ ]`; ZK-5 unblocks the top blocker class in the ZK-0 histogram — IR_VAR/IR_ASSIGN globals across palindrome/roman/wordcount/queens — and ZK-2's open relop-{0,1}-generator kinds walk naturally beside ZK-7. Landed so far: BUILTIN admission `6967f531` · CALL_PROC_STAGED `4de6b763`.) Whitelist + template ZD arms for the remaining STATIC value-spine kinds: `IR_CALL_BUILTIN_ICON` end-to-end (admission landed `6967f531`; verify arg ZOPQ staging against `zd_nops`'s n_operands arm and the template's read side) · `IR_VAR`/`IR_ASSIGN` (locals deferred to ZK-4; globals to ZK-5) · relational/CMP kinds (they are {0,1} GENERATORS per ARCH-ICON/ocomp.r — γ yields a cell, ω fails; K=16, ports per `ir_opfn`) · subscript/field · string builtins. Every constant on its ONE-AUTHORITY line (`zd_k`/`zd_nops`/`zd_wl_kind`), per-family killswitch, BOTH-MEDIUM, TEMPLATE-ONLY (no `MEDIUM_*`, no raw bytes — the named forbidden shape). Completion: armed-node census strictly UP · a non-popping trace (16→32→48…, ONE terminal release) witnessed on a rung program in BOTH media · suite non-regressing.

- [ ] **ZK-6 — THE SUSPENSION DECISION (Lon: "pthreads or ARBNO-style. Whichever.").** The DYNAMIC residue — suspended generator state that must survive while computation piles above it — gets ONE mechanism, chosen by measurement:
  **Option T — THREAD STACKS.** DYNAMIC-verdict activations run as implicit co-expressions on the LANDED pthread+semaphore machinery (`rt_coexpr.c` + bb_create/activate/coret/cofail, 2026-07-01). Cells are 100%-on-a-stack by construction (each activation's OWN stack, static depths within it). Cost: create/activate latency + semaphore ping-pong per suspend/resume; mitigations (pool, DYNAMIC-only admission) are part of the prototype. ⚠ ZFRAME's R-ICN-B ("no pthread-per-generator") is scoped to ITS track and does not bind here — but the s89 LIFO ruling it cites is EVIDENCE for P.
  **Option P — PENDING CELLS ON THE ONE SPINE.** The ARB-LON-K16 mechanics generalized: suspended cells persist non-popping below the frontier; β resumes against a recorded base (record shape ≈ SN4 DCAP/CAS entries: resume-addr + base + saved scan state — top register/cell per the REGISTER CONTRACT's named Lon decision); re-homing where a cell must track the frontier; composition laws A/B govern nesting; bound-terminal whack releases. CANONICAL PRECEDENT: Arizona `interp.r` runs generators on ONE interpreter stack via efp/gfp mark/esusp/efail — 100%-cells-on-one-stack has shipped for decades; read it before prototyping.
  PROCEDURE: witness set (nested every over `to`; disjunction resumed across depth; recursive generator proc; suspend inside an argument list); measure Ir (callgrind — the honest metric per GOAL-ICON-BB's protocol), peak RSS, suite delta, both media. DECIDE; record the loser under STANDING NEGATIVES with its numbers. Lean P on today's evidence (LIFO ruling + iconx precedent + zero thread overhead + landed composition laws) — but the rung's output is the MEASUREMENT, not the lean. Completion: decision + numbers in this cursor; winner's per-kind plan written into ZK-7.

- [ ] **ZK-7 — WALK THE GENERATOR FAMILY** with ZK-6's winner: `IR_TO_BY` · `IR_DISJUNCTION` · `IR_REPALT` · `IR_PROC_GEN` (suspend/fail-return protocol) · every/limit/bang per the GOAL-ICON-BB-NATIVE taxonomy · scan control `?` and the ICN-SCAN family (position-returners vs cursor-movers per ARCH-ICON — R13/R14/R15 discipline is THIS rung's sharpest edge). One rung-let per kind; each cites its `ir_a_*` four-port topology from `refs/jcon-master/tran/irgen.icn` (the 43-procedure ground truth) and lands: ONE-AUTHORITY constants + template arm + killswitch + witness. ⚠ If the ICN-FB-1 arrival-depth table exists by then (GOAL-ICON-BB ladder), CONSUME it — if this ladder produces the topology table first, land it as `.github/ICON-IRGEN-TOPOLOGY.md`, SINGLE COPY (the 2026-08-04 BB-embodiment lesson), and point both goals at it. Completion: per-kind census flips armed; .bss arena bytes for that kind → 0; suite monotone.

- [ ] **ZK-8 — 100% ASSERTION + GATES (default flip is LON'S CALL, not the ladder's).** `scripts/test_gate_icn_zcells.sh`: on the cells arm — whole-graph carve = 0 · .bss backtrack arenas = 0 · heap-ζ for generators = 0 (coexpr pthread stacks exempt by charter) · locals/params = cells · globals = GVA (0 stack cells) · R13/R14/R15 grep on every template arm this ladder added = 0 · `SCRIP_ICN_CELLS=0` byte-identical to the other arm's regime · SN4 crosscheck identity · suite floor RATCHET (baked count, descending-failure, never a zero-assert). Triple watermark re-prove (SN4 + Icon + Prolog) before proposing any default. Completion: gate committed + green; proposal memo for Lon's default decision (frames vs cells) with both arms' numbers side by side.

## SCOPE FENCES
Correctness residuals (jcon FAIL clusters, XFAILs) stay owned by GOAL-ICON-BB's FAIL-ZERO/XFAIL-ZERO. Anchor-parity restoration is ZFRAME's finish line, not this one's. Coexpr code: ZERO edits (pthread, by charter, both tracks). This ladder's finish = ZK-8 green with the cells arm at watermark parity with the frames arm (or a Lon-accepted delta), and the decision memo delivered.

## CROSS-CUTTING LAWS
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY EMISSION (missing instruction ⇒ add the encoder in x86_asm.h) · ONE-AUTHORITY constants (the s22k law — K and nops change ONE LINE ONLY) · PEERS RULE (no BB_t fields; operands via ir_operand_push) · NO-LANGUAGE-IDENTITY past LOWER (behavioral names only) · MONITOR-FIRST on any divergence (Icon: `CSN_NO_SEGV_HANDLER=1` + gdb spin/ignore counters) · CONSULT CANONICAL SOURCES (`refs/` per session: jcon irgen.icn + icon-master runtime/*.r; interp.r is ZK-6's required read) · O2-DIRECTED-ONLY · TIME-BOXED EXPLORATION (report context % at natural checkpoints unasked) · timeouts 8s/30s · handoff step-4 `.s` regen when codegen touched (+ `update_icon_bench_asm.sh`, DEMO+BENCHMARK only) · LIVE CURSOR moves every handoff · "HANDOFF COMPLETE" only from `scripts/handoff_status.sh` verbatim stdout · push needs Lon's credential.

## SESSION SETUP (every session)
```bash
git clone https://github.com/snobol4ever/.github.git /home/claude/.github
git clone https://github.com/snobol4ever/corpus /home/claude/corpus
git clone https://github.com/snobol4ever/SCRIP  /home/claude/SCRIP
cd /home/claude/SCRIP && git config user.name "LCherryholmes" && git config user.email "lcherryh@yahoo.com"
bash scripts/install_system_packages.sh && rm -f scrip && make -j4 scrip && make libscrip_rt
mkdir -p refs   # symlink Lon's unzipped icon-master + jcon-master archives; verify ls refs/... before trusting
bash scripts/test_icon_all_rungs.sh   # fresh watermark FIRST — never inherit a count from prose
# Then: grep -n "zd_wl_kind\|zd_k(\|zd_nops\|zd_plan" src/emitter/emit.cpp — line numbers drift daily
```

## HONEST LIMITS
(a) Three tracks share one emitter; the flag-gates + per-commit identity proofs are the only real armor — use them every commit, not at handoff. (b) ZK-6's lean is an inference; the measurement rules. (c) This file cannot coerce its walker; the LIVE CURSOR, the gates, and Lon's review are the enforcement.

**Authors:** Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude Sonnet
**Architecture:** `ARCH-ICON.md` · `ARCH-ZETA-LOCAL-STORAGE.md` · `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` · sisters: `GOAL-ICN-ZFRAME-RESTORE.md` (frames twin) · `GOAL-ICON-BB.md` (suite owner; ICN-CARVE absorbed here)
