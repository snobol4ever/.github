# FINDING s178-L2 (2026-08-20, HQ, Fable 5) — ASM-DIFF VERDICT: THE BEAUTY WALL'S L2 IS A ζ-VALUE-OPERAND PRICED AS A FLAT RSP OFFSET ACROSS A SEAM WHOSE RETAINED DEPTH IS VARIABLE BY CONSTRUCTION

**Pair (one ingredient apart, per ASM-DIFF-FIRST):** `probe/bfn/bfn_break_lit_ctl.sno` (PASS: `Parse = nPush() BREAK('x') 'x'`) vs the bw6i shape (`Parse = nPush() BREAK(b) b`, `b='x'`; over-fires 6× then matches). Measured at SCRIP `fef486f8` (post L1-flip), RT_OPT -O0, m3; the m4 face of the same family is uniform never-fire/nomatch.

## The two blobs (mode-4 `.s`, FN__PAT$1)
- **lit**: `n8_match_defer` (the nPush()-value seam, patv slot 0) → `n9_match_break` (cset baked as immediate: `cmp esi,120`) → `n10_match_lit`. ZERO spine reads of operands. PASSES.
- **var**: `n8_var` (reads snapshot cell `PAT$1$A1` = b's build-time value, correctly WRITTEN by the build statement at `n59_assign`) → `n9_coerce_string` (match-time `rt_coerce_str_d`, result on the SPINE) → `n10_match_defer` (seam: `jmp rax` into the fn-returned pattern's own blob, γ wired `.Lx16_4`→break, ω `.Lx16_5`) → `n11_match_break` reads its needle **`[rsp+40]`/`[rsp+36]` # coerce_string** → `n12_match_defer` (the bare `b` element, patv slot 2).

## The mechanism, to the byte
`n11`'s needle offsets assume rsp at break-α == the depth the planner modeled. But the seam box `n10` has TWO arrival roads with DIFFERENT retained depths: the **fast γ road** leaves the inner blob's 4-push γ-resume record (32B) on the spine — MANDATORY, `n10_β` dispatches `jmp [rsp]` through it — **plus whatever the inner pattern itself suspended (unbounded: an ARBNO/defer inside the fn-returned pattern retains arbitrarily)**; the **C road** (`rt_patv_defer_run_all`) pushes 16B. The emitter KNOWS: `.Lx16_5` compensates `add rsp,16; add rsp,32` on ω, and s101's own hazard comment (emit.cpp:692) states "the fast path retains the target's own suspended records, so a SAVE..COND span crossing ANY defer has no compile-time-constant RSP distance." The same truth applies to the ζ-VALUE-OPERAND plane (`op_zread`/`ZOPQ`, staged by zd_plan at the emit choke) and was never enforced there. `[rsp+40]` therefore reads inside the retained record/garbage → BREAK scans a garbage cset → wrong-fail at anchors, layout decides the face (bw6 nomatch / bw6i over-fire / bw6k SEGV / name-swap flips) — ONE wild mechanism, all four faces.
**Secondary observation (defect b):** each failed anchor attempt fires the capture call once (6 fires / 6 anchors vs oracle's 1-at-commit) — capture-name resolution happens per attempt on the defer road. With (a) fixed, anchor 0 succeeds and the count converges to oracle on these witnesses; (b) stays open as its own question (fires-on-failed-attempt), re-grade after (a).

## Why only tier-2 escalation is correct (ARCH-PASSTHRU ladder 0c)
No flat compensation exists: the two roads differ (32+unbounded vs 16). The cell must leave RSP. Precedent in the file's own history: bb_match_break's LEGACY arm reads the needle via **FRQ (frame-relative, depth-immune)**; the ZD arm re-priced it flat and broke the crossing. PF-1a (`aab14749`) built exactly this escalation for LFC scratch cells (`leaf_frame_member` → blob rbp frame); the ζ-value operand with a seam-crossing def→use span is the SAME law, next plane. **The rung is PF-1c: a value-operand producer in a seam-enterable graph whose consumer span crosses a MATCH_DEFER homes its result cell in the blob's OWN rbp frame; consumers read the rbp spelling. Follows `sn4_pt_frame` (ensemble authority, default ON), own override for A/B.**
