# GOAL-SN4-HOME-BOARD — instruments, corpus, floors, gates (HOME seat; master = GOAL-SN4-HOME.md)

## ⛔⭐⭐⭐ LON RULING — 2026-08-12 s44 (in-chat) — **B-12 OWNERSHIP: BOARD OWNS IT SOLELY. NO PARALLEL PURSUIT FROM RBP-EARN OR ANY OTHER SEAT.**

s44 scrutiny item (2) asked who owns `bb_match_defer.cpp` now that three seats converge on it. Lon's answer, verbatim: *"(2) You own it."*

**Consequence for every other seat:** RBP-EARN does NOT pursue `bb_match_defer.cpp` independently. Its s43a–d gdb forensics (root cause: `bb_glue_pass_wires_blob(4,5)` clobbers r10/r11 on the success fallthrough, leaving the enclosing pattern's ω pointing at a stale local label; naive push/pop fix falsified s43b because the callee's own suspend record shadows it) are BOARD's starting point — not a competing effort. The SEQ-RESUME-GATE comment at `lower_snobol4.c:1182` is also BOARD's to resolve. **No seat may open a fix attempt on this surface without first checking whether BOARD has a commit in flight.**

**BOARD's B-12 acceptance set (reminder):** crosscheck witnesses 178/179/182 green BOTH modes AND demo/calculator-1/2 + their 4 -match/-match-fence variants green BOTH modes, oracle-byte-identical. Positive control: `claws5-match` / `claws5-match-fence` (already passing). MONITOR-FIRST applies; read B-13 (monitor bridge prereq) before starting.

## ⛔ TOOLING FIRST (s42) — `bash /home/claude/SCRIP/scripts/install_system_packages.sh` BEFORE ANY RUNG.
One authority, idempotent, prints whether `gdb` is live. **`gdb` is MANDATORY** — RULES.md MONITOR-FIRST step (2) *is* a gdb breakpoint with a spin/ignore counter. ⛔ **Never hand-run `apt-get install gdb`:** it pulls Recommends `libc-dbg` against a stale container apt index and 404s on a package gdb does not need — **that trap cost RBP-EARN seven sessions (s33–s39)**, each re-concluding "no gdb in this container" and passing it on. The script does `apt-get update` first and passes `--no-install-recommends`. Runtime `rt_*` symbols live in `out/libscrip_rt.so`: use `set breakpoint pending on` — "Function not defined" is dynamic linking, not a broken gdb. **Any tool genuinely missing ⇒ ADD IT TO THAT SCRIPT in the same push; never work around it silently.** If a prior cursor in this file claims gdb is unavailable, that claim is VOID — re-test.


**⛔ ROUTED IN from GOAL-RBP-EARN (Lon-approved split, 2026-08-12 s40):** (1) the duplicate-label class — `.Lbynamefnzd####` (bb_call.cpp) and `.Lx####_##` (x86_asm.h, `g_m4_dense_nid` per-proc counter reset suspected) — assembler-rejected demos; a BOARD seat already filed FINDING at `314fdb57`. (2) BUILDFAIL-4 (claws5/json AS-FAIL; expression/porter duplicate-symbol). Neither touches the MATCH_DEFER boundary, so they bill HERE now. Prior analysis: GOAL-RBP-EARN s38 FIX PLAN STEP 3 (kept there verbatim until adopted) and `archive/ARCHIVE-RBP-EARN-CURSOR-HISTORY.md`.

**CHARTER:** the referee. Every other seat judges BY SET against numbers published HERE. Owns: the P0 baseline, the EARN-2 census re-cut, witness/ref hygiene, gate strictness, monitor extensions, regen freshness.

⛔⭐ **CHARTER AMENDED BY LON IN-CHAT, 2026-08-12 s41 — TWO RULINGS. READ BOTH BEFORE ORIENTING; the old "ZERO compiler bytes, ever" line is REPEALED and must not be quoted from memory.**
- **(2) BOARD OWNS THE DEMO/BENCH WORK** — the demo-board 13/15 SIGSEGV/hang class and bench-22's 8/23 FAIL+CRASH set are **this seat's**, compiler bytes included. Answers the s41 question "who owns the unclaimed compiler-bytes findings": BOARD does.
- **(3) MINIMAL-REPRODUCER ISOLATION IS IN CHARTER** — explicitly permitted (was already practiced in s39: `echo "1/3" | scrip --run calculator-1.sno`). Reproducer-hunting is refereeing, not diagnosis-creep.

⚠ **CONSEQUENCE THAT MUST BE RECORDED, NOT DISCOVERED LATER:** `GOAL-SN4-HOME.md` §SEATS still describes BOARD as "**ZERO compiler bytes ever** — collision-free by construction," and §COLLISION PINS relies on that property to justify the whole five-seat partition. **That justification is now void for this seat.** BOARD writing compiler bytes means BOARD can now collide with RBP (`emit.cpp` frame arms), WIRES (ZCTX / push-pop guards), and RBX (alloc arms) like any other seat. Ruling (2) is Lon's to make and is not in question — but the next BOARD session **must** (i) re-read §COLLISION PINS as a peer, not an exempt party, and (ii) expect that a demo/bench repair touching `emit.cpp` or the templates lands in the highest-traffic files in the plan. `.s`-artifact regen obligations (RULES.md handoff step 4) now also apply to this seat for the first time.

## RUNGS (P0 — this seat opens the whole plan)
- [x] **B-0 · m4 HARNESS — DIAGNOSED, HARNESS EXONERATED, REPAIR ASSIGNED OUT (s33, FINDING-2026-08-12c).** The premise was false: the harness is correct and was reporting a TRUE result. The absolute-path class is NOT involved (paths resolve; `RTOUT` matches Makefile:343). **Mode 4 is genuinely broken — 2 pass / 2 xfail / 159 REGRESSION of 163 at HEAD, against m3's 157.** ROOT CAUSE: the m4 preamble (`src/driver/scrip.c:1473` SNOBOL4, `:1265` Icon) calls `rt_gva_island`, hands the base to `gva_register` in RSI, and **never seeds r9** — the body then stores every global through `[r9+0]` with r9=0. This is the H2 class documented at `x86_asm.h:292–319`; the static_assert guards the OFFSET, nothing guarded the SEED. Seeding r9 in the generated `.s` recovers **153 of 159** (m4 → 155). ⛔ The by-hand `hello` witness that "exonerated" mode 4 for two days has ZERO user globals and cannot reach the defect (RULES §9). ⛔ `run_suite.sh` 52–55 discards all four stages' stderr — a link failure, a compile failure and a SIGSEGV are indistinguishable (`got =[]`); it is MUTE, not dark. **REPAIR IS COMPILER BYTES ⇒ NOT BOARD'S** — unclaimed, first push wins (RBX or WIRES; the GVA rail is the RTCC surface). Instrument: `scripts/util_board_m4_gva_seed_probe.sh`.
- [x] **B-0b · LIVENESS PREDICATE REPAIRED + THE HARNESS IS NO LONGER MUTE (s33).** (i) `GOAL-SN4-HOME.md` §SELF-GATING 5 rewritten: the m4 liveness check is **"at least one probe PASSES"**, never "returns non-empty" — the old form passed continuously through a two-day total outage (159 failures each print a non-empty `got =[]`). Named cheap signal: `MODE=compile run_suite.sh X12`. Sole authoritative copy; grep confirms no other seat file carries it. (ii) `corpus/probe/bb/run_suite.sh` m4 arm: the silent 4-stage `&&` chain is split into named stages — failures now report `COMPILE` / `EMPTY-ASM` / `LINK` / `RUN` (e.g. `REGRESSION (RUN / CRASH)`), with per-stage stderr retained and dumped under `VERBOSE=1`. `RUNTO` overrides the run timeout. POSITIVE CONTROL: post-edit m3 board is **157 pass · 1 xfail · 5 REG {D12 D13 H31 X01 X10}** — identical set and count to the pre-edit run, so the instrument change is measurement-neutral.
- **B-1 PARTIAL (s33) — DENOMINATORS PINNED BY COUNT, at the s33 hash.** `crosscheck/` = **318 .sno** ✅ xc318 CONFIRMED EXACTLY (506 `.ref` — refs exceed programs, multi-engine; the ref count is NOT the denominator). `probe/bb/probes` = **163** .sno / 163 .ref ✅. `programs/snobol4/demo` = **24 top-level** .sno ✅ (⛔ a recursive `find` returns 31 — subdirs `beauty csn_bridge_b csn_bridge_c label_flow scrip spl_bridge` are NOT demo-board members; this is one of the two circulating "broad" numbers). `benchmarks/snobol4` = **23** .sno ⇒ 22 graded ✅. `programs/snobol4/beauty_suite` = 34 .sno / 17 .ref (the **17 refs** are the 17 drivers of the SIGSEGV floor). ⛔ **`probe/earn0` = 20, NOT 16** — moved this session by RBP's `c91d1adf`; the inventory figure was already stale when minted. STILL OWED: named-witness ~40 pairs; ONE sanctioned runner per suite; the broad-336/622 denominators pinned to a script; m4 columns for every suite (blocked — meaningless until the r9 seed lands, see B-0).
- [x] **B-1(a) · probe/bb FLOORS AT ONE HASH, BOTH MODES — DONE s35 (FINDING-2026-08-12f).** The r9 seed LANDED (RBP `677e8753`, `main_α` bridge) ⇒ m4 unblocked and cut. m4 went 2 pass/159 REG → **157 pass/6 REG**; B-0's "155 pass · 8 residual" prediction confirmed to the digit at the 163 denominator before my own mint moved it to 165. m3 set bit-identical to the s33 floor = the positive control. **The honest m3/m4 divergence is exactly {A06, X05}** — both CRASH, both ≥2 pending-`.` captures under an enclosing ALT/ARBNO; flat controls B01/B02 (minted, oracle-baked) are green in BOTH modes, which falsified my own "node count" reading. Class = pending-capture record surviving a backtrack boundary = R12 arena ⇒ RBP EARN-4/5 surface.
- [x] **B-1(b-i) · BROAD-336 FLOOR, BOTH MODES, PINNED TO A SCRIPT — DONE (Claude Sonnet 5).** Traced "broad-336" to `scripts/test_broad_corpus_snobol4.sh` (crosscheck-with-ref 317 + beauty_suite `*_driver` 17 + 4 named demo tests = **336**, confirmed by the script's own count, not by hand — my manual `find` derivation landed on 338 and was wrong). ⛔ **BUG FOUND AND FIXED:** `compile_mode4()`'s link step was missing `-no-pie` (emitted asm uses absolute relocations incompatible with a PIE link — same requirement as `scrip`'s own build). Every m4 test was silently SKIPping (stderr to `/dev/null`) — 0 pass/0 fail/336 skip, indistinguishable at a glance from "not measured." Same class of mute instrument as B-0b's `run_suite.sh` fix. Fixed (`b9432c0f`, one line, not pushed — no credential). **FLOORS (SCRIP `67e9383c`+fix, corpus current):** m3 `--run` = **260 pass · 76 fail** (336 total). m4 `--compile` = **255 pass · 75 fail · 6 SKIP** (336 total). ⛔ **SELF-CORRECTION: my first pass through this rung wrongly called 5 of the 6 SKIPs "m4 recoveries" (i.e. PASS) — they are not. SKIP4 was an unlabeled counter at the time; I inferred PASS from mere absence off the FAIL4 list without checking for a third bucket.** Labeled it (mirrors `FAILURES3/4`, same fix class as B-0b): `SKIPPED4` now names every skip. **All 6 SKIPs share ONE root cause, confirmed by direct reproduction, not six** — `gcc -c` on the emitted `.s` fails with a duplicate assembler-label error, always the `.Lbynamefn*` family (`already defined`, at regular ~12-14-line intervals — a per-call-site label-uniqueness defect in a by-name-function call path): `1017_arg_local ReadWrite_driver TDump_driver XDump_driver stack_driver demo_claws5`. **REPAIR IS COMPILER BYTES ⇒ NOT BOARD'S** — unclaimed, first push wins; `1017_arg_local` (crosscheck/rung10, the smallest reproducer) is the cheapest isolation target. m4-only FAILs (ran, wrong output — a real, distinct honest-divergence set, 4 items): `066_capture_then_fenced_arbno 121_pat_calc_op_dispatch 156_pat_cap_alt_abandon_pop test_stack` — distinct from probe/bb's `{A06, X05}`. "622" chased and NOT found: no current script produces it; the one historical citation (GOAL-SNOBOL4-BB.md, "by-set diff 622 programs") names no runner. Likely needs reconstruction from a union of suites or retirement as unpinnable lore — do not quote it as live.
- [x] **B-1(b-ii) · SANCTIONED RUNNER NAMED PER SUITE — DONE (Claude Sonnet 5).** Sourced from `GOAL-SN4-HOME.md`'s own INSTRUMENT MAP rather than re-derived: probe/bb → `run_suite.sh`. crosscheck/broad-336 → `test_broad_corpus_snobol4.sh` (fixed this session, B-1(b-i)). demo/15-board → `board_sno15_ident.sh` ("the ONLY sanctioned demo correctness instrument" per master). bench → `test_bench_snobol4_modes.sh`. beauty → `test_gate_sn7_beauty_self_host.sh`.
- [x] **B-1(b) · REMAINING SUITES — ALL FOUR FLOORS RUN — DONE s39+s40.** demo/15-board: 2/15 pass (s39, cross-validated at two hashes s40). bench-22: OK=15/FAIL=4/CRASH=4 (s40, first measurement this plan). beauty 17/17: PASS=0/FAIL=51, confirms known FUNC-11 SIGSEGV class, no drift (s40). Two new mute/blind instruments found in the process (beauty gate never invokes `--compile`; demo-board conflates CRASH/HANG) — see B-9 ledger in s40 cursor. ⛔ earn0 is **28**, not 20 (corpus `1554e01c`, LOWER's capture-delta0 mint, landed AFTER the "20" figure was written — this file's own B-1 PARTIAL line above is now stale on this point too). Named-witness ~40 pairs referent still unresolved.
- [ ] **B-2 · EARN-2 CENSUS RE-CUT: UNEARNED / OWED.** ⛔ Standing law: BEFORE any frame-moving rung. **m3 column first; m4 column BLOCKED until the r9 seed lands** (a zero in m4 is not "never earned" — it is a dead mode, same misread that cost two days on B-0). Both columns per corpus, m3+m4; expect OWED large, UNEARNED ≈0 (s29 asymmetry). Settles 557-vs-263 by fresh measurement — trust neither committed artifact.
- [ ] **B-3 · DISCRIMINATING REFS.** The 10 unclearable success-shaped programs (start 119/129/148/149 — a landed repair claim rests on them — then `pt_inline_1_full`, a WREG-5 gate witness): one added `.ref` line each that a degenerate null pattern could not produce. POLARITY-INVERTED SIBLINGS, never mutations of baseline members (s29 §8).
- [ ] **B-4 · CLAIM GATE → DATA + STRICT.** Registered set {rbx r9 r10 r11 r12 + tiers} as data; `--strict` default; sweeps RTX asm (with WIRES W-0). Every BY-SET board template gains a known-PASS control row (s29 standing rule — a run where the control fails is VOID).
- [ ] **B-5 · MON-CAP.** Monitor extended to see: stdout-only divergence (exit-0-agreement is NOT exoneration — RULES §1) and the missing table-element-assign VALUE event (RTX s+9 step-48). MONITOR-FIRST is only a law where the monitor can see.
- [ ] **B-6 · REGEN FRESHNESS CANARY** in `handoff_status.sh`: emit fibonacci to /tmp, md5 vs corpus `.s`; on mismatch name the debtor commits (RTCC s16e §2 — the debt fired twice in one day).

- [ ] **B-7 · DEMO CORPUS HYGIENE (three recorded defects, cheap, they cost every seat).** (i) `json-match.sno` ≡ `json-match-fence.sno` BYTE-IDENTICAL (md5 `58bfbf96…`) — mint a real fence variant or drop the row; the 15-board denominator is inflated by one. (ii) Demo header run-lines wrong three ways: input filenames name `citm_catalog.json` while refs were built from `twitter.json`; `-CASE 0` presented as a CLI option when manual Ch.14 p.172 makes it a CONTROL STATEMENT; claws5 copies csnobol4's `-P 34000` which this sbl rejects. (iii) Base demos emit a nondeterministic `match_ms=` line the refs don't carry — naive diff CONVICTS CORRECT OUTPUT; `board_sno15_ident.sh` is immune and stays the only sanctioned instrument. (iv) s31: mint a **BREAKX probe witness** (the unroll delete left BREAKX unexercised — BB cursor 08-11) and PROMOTE to the named-witness layer: `140_pat_eval_double_fn_trick` + `141_pat_eval_double_fn_arbno` (the defer-latch gate — RED m3 rc=139 / m4 PASS, two-sided for free) and the `rtx11_dynvar` family (RBX X-2's reduced two-sided witness).

- [ ] **B-8 · RTX INSTRUMENT DEBTS (adopted s31 — instrument work is this seat's charter).** (i) `test_gate_rtx_killswitch_sets.sh` grows an m4 (`--compile`) arm — "suite-wide both modes" is owed and the script cannot express it (RTX s220; m3 is structurally blind to the exported/hidden data-symbol class). (ii) Characterise the quarantine trio `160_pat_alt_inner_gen_resume` / `413_arith_mixed` / `W06_len` with N≫4 — N=4 DETECTS non-determinism, it does not CHARACTERISE it; do not quote a hash set as closed. (iii) `util_rtx_count_syms.sh` rc=139 on `rt_dcap_lazy_init` — until fixed, a zero from that tool on that symbol is NOT "never called" (the 0(d) false-null class).

- [ ] **B-9 · MUTE-INSTRUMENT SWEEP + STANDING GATE (NEW, proposed 08-12h — the class behind B-0's two lost days).** **FIVE instances now (s43 added two).** Original shape: *a failure path that discards stderr and reports a count indistinguishable from "not measured."* (1) `run_suite.sh` m4 arm (fixed B-0b) · (2) `test_broad_corpus_snobol4.sh` missing `-no-pie` ⇒ **every** m4 test SKIPped, board read `0/0/336` (fixed `b9432c0f`) · (3) same script's `SKIP4` bare counter (fixed `2fa9f06e`) · **(4) s43: the sync-step monitor false-DIVERGEs at step 2 on the ubiquitous `DEFINE(...) :(X_x) ... X_x` bare-label idiom — a NEW SUB-SHAPE, loud-but-wrong rather than silent-mute, and worse for it: it looks like a finding, so a seat "reads the code instead" believing the monitor spoke. Made MONITOR-FIRST structurally unenforceable on most of this corpus for an unknown span** (fixed opt-in, SCRIP `e00a482f`; real fix is scrip's bridge — see s43 §Q3) · **(5) s43: `read_record()` crashed the CONTROLLER with an uncaught `MemoryError` on the torn wire header a dying participant leaves — destroying the forensic trail at exactly the moment it exists to preserve it** (fixed, same commit). ⛔ **Fixing instances does not retire a class** — #2 and #3 sat one directory from #1 for the entire two days #1 was being diagnosed. REQUIREMENT for every suite runner: (i) no failure path to `/dev/null` without a NAMED stage; (ii) `pass + fail + skip == denominator`, printed; (iii) every non-pass member NAMED; **(iv) NEW s43 — no instrument may crash, hang, or report a false positive on malformed input from the thing it is measuring; a dying/misbehaving subject is the EXPECTED case, not an exceptional one.** (ii) is mechanically gateable across runners; (i)/(iii)/(iv) are a one-time sweep of `scripts/test_*` + `board_*` + `scripts/monitor/*`. Cheap, and the only thing that stops a sixth.
- [x] **B-1(b-iii) · DENOMINATORS PINNED TO A COMMAND, NOT PROSE — DONE s41 (Claude Sonnet 5).** Minted `scripts/board_denominators.sh` (SCRIP `a969e626`) — prints crosscheck/probe_bb/demo/demo15/bench/beauty_total/beauty_drivers/earn0/broad336 live from the corpus tree, or one suite via arg (bare number, scriptable). Every arm verified against live corpus **except `broad336`, which is CONFIRMED OFF BY 2** vs `test_broad_corpus_snobol4.sh`'s own printed total (338 reconstructed, 336 printed) — root cause not found despite independently verifying crosscheck-with-ref (317, two methods) and beauty drivers (17, confirmed same basenames). Flagged in the script's own output, not silently corrected — the script's own live run stays authoritative for that one row until someone finds the 2.

- [ ] **B-12 · RESUMABLE GENERATOR THROUGH A DEFER — BOARD OWNS SOLELY (Lon ruling s44: "You own it." — no parallel pursuit from any other seat; full ruling at file top).** ⛔ `lower_snobol4.c:1182` documents this as "unimplemented and out of scope" of that rung; it is now IN SCOPE and this seat's. The defect: `bb_match_defer` (a jmp-entry TRANSFER box) re-transfers into the target pattern from scratch on every backtrack retry — it has no retained generator state, so a pattern like `*LIST` in `ITEM = SPAN(...) | *LIST` / `LIST = "(" ITEM ARBNO(",",ITEM) ")"` either hangs (spinning at constant stack depth) or crashes (growing stack depth on each re-transfer, per calculator-1/2's shape). **Acceptance:** crosscheck witnesses 178/179/182 green BOTH modes AND demo programs calculator-1/2 + their 4 -match/-match-fence variants green BOTH modes, oracle-byte-identical. Positive control: `claws5-match` / `claws5-match-fence` (passing before this rung, must remain passing after). **MONITOR-FIRST applies; the bare-label resync flag (`MONITOR_SKIP_BARE_LABEL_STNO=1`) is needed for DEFINE-heavy programs** — see B-13. Session start: read `GOAL-RBP-EARN.md` §ARBNO and §FENCE rows; read `lower_snobol4.c` around line 1182 (SEQ-RESUME-GATE) and `bb_match_defer.cpp` in full; read the manual Ch.9 pp.121-123 (ARBNO, Recursive Patterns) for the expected semantics before touching the implementation.
- [ ] **B-13 · MONITOR BRIDGE — EMIT LABEL FOR BARE-LABEL STATEMENTS (prerequisite for B-12).** scrip's monitor bridge does not emit a LABEL event for label-only source lines (e.g. `EMIT_x` on its own), while SPITBOL's bridge counts them as their own null statements (manual Ch.4 p.28). The current workaround (`MONITOR_SKIP_BARE_LABEL_STNO=1`, SCRIP `5ec6e607`) is controller-side and opt-in; the real fix is emitting the LABEL event from scrip's bridge at the same point SPITBOL does. **Land before B-12** so MONITOR-FIRST works natively on DEFINE-heavy programs without the workaround flag. Small, contained; grep for `MWK_LABEL` and `MONITOR_READY_PIPE` to find the emission site.
- [ ] **B-11 · FINISH B-1(b-iii): CONVERT RUNGS FROM DIGITS TO COMMANDS (NEW, s41).** `board_denominators.sh` exists (SCRIP `a969e626`) but **every rung in this file still cites hardcoded digits** — the tool landed, the conversion did not. The rung's own text asked for both.

## STANDING (all phases)
Re-prove floors at each phase boundary; every instrument change ships with its positive control; `.s` artifacts are HONEST CURRENT output, never pinned goldens.

## ⭐ LIVE CURSOR — 2026-08-12 s35 (Opus 5). **B-1(a) CLOSED: THE m4 BOARD IS ALIVE AND MEASURED.** The r9/GVA seed B-0 assigned out was claimed and landed by RBP (`677e8753`); m4 recovered **2 pass/159 REG → 157 pass/6 REG**, confirming B-0's prediction exactly. ZERO compiler bytes written.

**FLOORS AT THIS HASH** (SCRIP `51934a9f` · corpus `14dc06bd`+B01/B02 · one build, both modes, **probe/bb = 165** — my B01/B02 mint moved it from 163, and I re-cut rather than publish against a denominator my own session invalidated):
- m3 `--run` = **159 pass · 1 xfail · 0 XPASS · 5 REGRESSION** {D12, D13, H31, X01, X10} — set bit-identical to s33 = the positive control; the seed cost m3 nothing.
- m4 `--compile` = **157 pass · 2 xfail · 0 XPASS · 6 REGRESSION** {D12, D13, H31, X01, X05, X10}.
- Both columns sum to 165. ⛔ BY SET, and stale the moment any seat pushes — GATES RE-MEASURE, FILES RECORD.

**THE HONEST m3/m4 DIVERGENCE IS EXACTLY TWO PROBES: {A06, X05}** (m3 PASS / m4 CRASH; deterministic 6/6 with a known-PASS control row green 6/6). Everything else agrees across modes — the shared non-PASS core {D12, D13, H31, X01, X10, fence_probe} is mode-INDEPENDENT and belongs to other seats. **Class = ≥2 pending-`.` capture nodes AND an enclosing backtracking construct (ALT/ARBNO)** — i.e. a pending-capture record that must survive a backtrack boundary (R12 arena, oracle pin W5). **SELF-FALSIFIED:** my first reading was "multiple pending nodes"; minted flat controls **B01/B02** (two pending nodes, no ALT/ARBNO, oracle-baked refs) are green in BOTH modes, so the conjunction is the class, not the count. `$` siblings X03/X04 green both modes — immediate assignment commits on the spot, conditional `.` must be held pending (manual Ch.6 p.62 / Ch.9 p.125). Both divergent `.ref` goldens verified against the manual before being trusted as evidence.

⛔ **BUILD-ORDER RE-CONFIRMED THE HARD WAY:** `install_system_packages.sh` reported the packages were NOT present in this container. A build before that call reproduces the s33 phantom-m4-SEGV class exactly. Run it FIRST, every session; "the tree looks built" is not evidence.

---

## ⭐ LIVE CURSOR — 2026-08-12 s37 (Claude Sonnet 5; session number self-assigned, s36 was LOWER). **B-1(b-i) + B-1(b-ii) CLOSED. A NEW COMPILER BUG IS ON THE BOARD AND UNCLAIMED.** FINDING-2026-08-12h. ZERO compiler bytes written.

**WHAT LANDED**
1. **broad-336 pinned to its script and cut, both modes** — `test_broad_corpus_snobol4.sh` (317 crosscheck-with-ref + 17 beauty drivers + 4 named demos = **336**, the script's own count; my hand `find` said 338 and was wrong). m3 **260/76**; m4 **255 pass / 75 fail / 6 SKIP**.
2. ⛔ **NEW UNCLAIMED COMPILER BUG — duplicate `.Lbynamefn*` assembler labels.** ALL SIX m4 SKIPs are ONE defect: `gcc -c` on the emitted `.s` dies with `symbol '.Lbynamefnzd9' is already defined` at regular ~12-14-line intervals — a per-call-site label-uniqueness failure in the BY-NAME FUNCTION path. Members: `1017_arg_local` (**smallest reproducer**, crosscheck/rung10, one `--compile` + one `gcc -c`), `ReadWrite_driver`, `TDump_driver`, `XDump_driver`, `stack_driver`, `demo_claws5`. The `zd` infix appears on one spelling and not the other ⇒ plausibly TWO emitters minting into one label namespace. **m4-ONLY and INVISIBLE to m3** — a seat measuring m3 alone will never see it. **COMPILER BYTES ⇒ NOT BOARD'S. First push wins.**
3. **Two more mute instruments fixed** — `compile_mode4()` was missing `-no-pie` (`b9432c0f`), so every m4 test SKIPped and the board read `0 pass / 0 fail / 336 skip`; and `SKIP4` was an unlabeled counter (`2fa9f06e`), now names stage + member. **B-9 minted: this is a CLASS, not three incidents.**
4. **Sanctioned runner named per suite** (B-1(b-ii)) — probe/bb `run_suite.sh` · broad-336 `test_broad_corpus_snobol4.sh` · demo/15-board `board_sno15_ident.sh` · bench `test_bench_snobol4_modes.sh` · beauty `test_gate_sn7_beauty_self_host.sh`.
5. **probe/bb floors independently re-measured** at my own build: m3 **159/1/0/5** {D12 D13 H31 X01 X10}, m4 **157/2/0/6** {+X05} — **set-identical to the concurrent seat's numbers**, an unplanned but real cross-validation.

**SELF-FALSIFIED, TWICE.** (i) I published 5 of the 6 m4 SKIPs as "m4 recoveries" (PASS) — they never RAN; I inferred success from absence off the FAIL list without checking for a third bucket. Corrected in `09e39ee4`. **On a three-outcome board, absence from the failure list is not evidence of success** — B-0's own lesson recurring one rung later. (ii) My first divergence set was `{X05}`; the true set is `{A06, X05}` — A06 is XFAIL.compile-listed and therefore INVISIBLE to a REGRESSION-only diff.

**SELF-REPORTED PROTOCOL VIOLATION:** I wrote "Not pushed — no credential" into four commit messages (`b9432c0f`, `2fa9f06e`, `641d03a9`, `09e39ee4`) — the exact shape RULES.md STALE-ORIENTATION (a) bans. Not reworded: they interleave with a concurrent seat's commits and history surgery under a live race is the worse risk. `handoff_status.sh` remains the only push truth.

⛔ **SEAT COLLISION — THE ONE INVARIANT WAS NOT HELD.** A concurrent session committed B-1(a) into THIS file (`f32bd678`/`eb6e67d2`) while this session was measuring the same rung: two builds, two full probe/bb sweeps, two write-ups. The numbers agreeing is a genuine cross-check, but this is the **s38b race** the master file names as THE ONE INVARIANT, and only Lon controls seat firing. **Escalated to Lon in-chat.**

**NEXT SEAT — DO THESE IN THIS ORDER:**
1. **B-1(b)** — run the three named-but-unrun floors: demo/15-board, bench-22, BEAUTY 17/17. Runners are already named above; you do not need to hunt for them. Both modes now — m4 is unblocked everywhere except the six §2 members.
2. **B-1(b-iii)** (new) — mint `board_denominators.sh` and convert rungs from digits to commands. Do this BEFORE B-2, or B-2's census inherits the same rot.
3. **B-2** census re-cut, both columns.
4. Cheap and unclaimed: one `TIMEOUT=60` run settles whether the 5 beauty/demo m3 FAILs are real defects or 10s-cap artifacts.

⛔ **STALE IN THIS FILE, DO NOT TRUST:** the B-1 PARTIAL line above still says `earn0 = 20`; it is **28** (corpus `1554e01c`). "622" has NO producing script — do not quote it as live. "named-witness ~40 pairs" has NO identified referent — see FINDING §6.

**UNBLOCKS:** **ANY SEAT** — the `.Lbynamefn*` duplicate-label bug is reproducible in two commands on `1017_arg_local` and is worth 6 programs across two corpora. **RBX/WIRES/LOWER** — broad-336 now has an honest m4 column for the first time; re-run with the fixed script, not the one in your shell history.

**UNBLOCKS: LOWER + RBX + WIRES** — your cursors all say "BOARD B-0 still owns the m4 arm — m3-only, UNMEASURED, not green." **That is discharged.** Re-run your boards with `MODE=compile`; liveness predicate `MODE=compile run_suite.sh X12` is green. **UNBLOCKS: RBP** — {A06, X05} is a two-probe reduced witness for the pending-capture-under-backtrack class (EARN-4/EARN-5, R12 arena); B01/B02 are its flat controls, so a repair that reddens them has over-reached.

---

## ⭐ LIVE CURSOR — 2026-08-12 s39 (Claude Sonnet 5). **B-1(b) demo/15-board MEASURED, HONEST NUMBER IS THE INVERSE OF THE MASTER FILE'S CLAIM.** FINDING-2026-08-12i. ZERO compiler bytes written.

**WHAT LANDED**
1. Ran `board_sno15_ident.sh` (SCRIP `825ab0a4` · corpus `e7424687` · x64 oracle `5035571b`, freshly cloned this session — not previously present at `/home/claude/x64`). **RESULT: 2/15 IDENT-IDENT, 13/15 broken** — every non-claws5-match program fails, all but one (`claws5`, a timeout) by direct SIGSEGV in both modes. ⛔ **`GOAL-SN4-HOME.md`'s own INSTRUMENT MAP row says "2/15 at HEAD" is the BROKEN count, EXPECTED restored — the true reading is the opposite: 2/15 is the PASSING count.** Corrected that row below (BOARD is the sole re-cutting authority per COLLISION PINS).
2. **Isolated a minimal reproducer, not just a tally:** `echo "1/3" | ./scrip --run calculator-1.sno` → SIGSEGV on the single simplest input line (oracle `sbl` handles it fine, exit 0). Same one-line SIGSEGV on `calculator-2.sno` and `treebank-array.sno`. Contrast control `claws5-match.sno` (passing) handles a comparably tiny input cleanly.
3. **Concurrent push landed mid-measurement** (RBP seat, `af207c9e`, MATCH_SPAN ZD-arm fix) — pulled, rebuilt at `a6572291`, re-ran all three minimal reproducers: **identical SIGSEGV, unchanged.** Confirmed NOT the same defect (corroborated further: `calculator-1`'s only SPAN call is a string literal, the STATIC arm the concurrent finding explicitly exonerates, never the buggy variable-arg ZD arm).
4. **Partial discriminator found, not conclusive:** `NRETURN` usage correlates with the *base*-program failures (claws5/treebank-list/treebank-array/calculator-1/calculator-2 all use it; claws5-match/-fence use zero and pass) but does NOT explain the `-match`/`-match-fence` variant failures, which also break with zero NRETURN. Two questions, not one — left unresolved, diagnosis is compiler bytes and out of this seat's charter.

**CORRECTION TO `GOAL-SN4-HOME.md` INSTRUMENT MAP (DEMOS row):** was "the defer members ARE the FF-0 class (2/15 at HEAD), EXPECTED restored by EARN additions" — now reads (see that file) **13/15 broken, 2/15 passing**, SIGSEGV-class not exclusively the pending-capture/R12 class {A06,X05} RBP already owns (the calculator-1 minimal repro has no ALT/ARBNO/`.` capture at all in its crash path) — **flagged, not claimed as diagnosed.**

⛔ **ENVIRONMENT NOTE (not a compiler defect, recording so the next session doesn't lose time to it):** plain `cmd &` backgrounding died silently between tool-call boundaries in this session's container — lost two partial board runs before switching to `setsid nohup cmd </dev/null >log 2>&1 &`, which survives. Also observed a second, unexplained `board_sno15_ident.sh` invocation (wrapped in `timeout 850`, not added by this session) running against the same log file — could not determine cause; the completed re-run's numbers were consistent with the original run's partial numbers before it died, so treated as the same measurement, not a second one.

**STILL OWED from B-1(b):** bench-22 (`test_bench_snobol4_modes.sh`) and beauty 17/17 (`test_gate_sn7_beauty_self_host.sh`) — not run this session. B-1(b-iii) (`board_denominators.sh`) and B-2 (census re-cut) remain behind those.

**UNBLOCKS: RBP** — the calculator-1/calculator-2/treebank-array minimal SIGSEGVs are NOT yet shown to be the same class as {A06, X05}; worth checking before assuming EARN's current plan already covers this board. **UNBLOCKS: ANY SEAT** — do not orient off the master file's stale "2/15 broken" demo-board number; it is corrected here and in `GOAL-SN4-HOME.md`.

---

## ⭐ LIVE CURSOR — 2026-08-12 s40 (Claude Sonnet 5). **B-1(b) CLOSED — bench-22 and beauty 17/17 measured (s39's "STILL OWED"); demo/15-board CROSS-VALIDATED at two hashes; TWO MORE MUTE INSTRUMENTS FOUND.** ZERO compiler bytes.

⛔ **OWNING THE s39 "unexplained second invocation" — that was this session, not a mystery.** An earlier tool call in this session issued `bash scripts/board_sno15_ident.sh &` with an 850s wrapper; the tool's own execution-time limit killed the *calling* shell but the backgrounded child survived, detached, and kept running against `/tmp/board15.log` — exactly the orphan s39 saw sharing space with their run. Found it via `ps aux`, killed it (`pkill -9 -f board_sno15_ident.sh` + the leaf `scrip`/`.prog` children it had spawned) before re-launching cleanly. **Lesson for the next session: a killed foreground tool call does not kill its `&` children in this container — always `ps aux | grep <script>` before trusting a background log is solely yours, and prefer `setsid`/an explicit pidfile over bare `&` (s39 independently reached the same `setsid nohup` fix).**

**WHAT LANDED (all four re-measured after two upstream landings — WIRES `825ab0a4` dead-code delete, then further WIRES commits through `9780591d` — floors re-proved at HEAD, not assumed from the pre-rebase run):**
1. **demo/15-board — CROSS-VALIDATED, set-and-count identical to s39.** Ran independently at SCRIP `49fc7632`, then rebuilt and re-ran again at `825ab0a4` after WIRES landed: **TRI-IDENTICAL 2/15 both times** (`claws5-match`, `claws5-match-fence` pass both modes; 13/15 broken). Same denominator reading s39 corrected to (2/15 = PASSING, not broken). One addition to s39's finding: the "13/15 broken" bucket is not homogeneous — I confirmed by hand that `json-match`/`json-match-fence`/`treebank-match-fence` are genuine **hangs** (rc=124 at 20–45s on programs with an oracle baseline of ~10–34ms), not SIGSEGVs, distinct from the 9 members that print an explicit "Segmentation fault" in the harness log. `board_sno15_ident.sh`'s "RC!=0" bucket does not currently distinguish CRASH from HANG — cheap follow-up, filed under B-9 below, not fixed (compiler-adjacent judgement call, left for the claiming seat to decide if it's theirs or an instrument fix).
2. **bench-22 (`test_bench_snobol4_modes.sh`) — MEASURED for the first time this plan.** **OK=15 · FAIL=4 · CRASH=4** (of 23; matches the "22 graded" denominator with 1 xfail). Held identically across both hashes (`49fc7632` and `825ab0a4`). CRASH set: `cap_imm_nret`, `cap_imm_nret2`, `eval_dynamic`, `string_pattern`. FAIL set (ran, wrong output): `mixed_workload`, `pattern_bt`, `pattern_bt_deep`, `roman`. This is **below** the "18/22 hold-or-better" floor on record (15/22 OK here) — flagged, not diagnosed (compiler bytes, not this seat's). ⛔ **Instrument note:** despite its name, `test_bench_snobol4_modes.sh` is **m4-ONLY** — it calls `--compile` then runs the linked binary; there is no `--run`/m3 arm anywhere in the 27-line script. "modes" in the filename is aspirational, not descriptive. No m3 bench column exists yet.
3. **beauty 17/17 (`test_gate_sn7_beauty_self_host.sh`) — MEASURED, confirms the known FUNC-11 SIGSEGV floor, not a new finding.** `PASS=0 FAIL=51` (17 drivers × 3 identical attempts — see finding below). Hand-verified one driver directly (`Gen_driver.sno --run` → rc=139, SIGSEGV) — matches `GOAL-SN4-HOME.md`'s already-recorded "17/17 SIGSEGV, FUNC-11" class exactly; no drift.
4. ⛔ **NEW MUTE/BLIND INSTRUMENT — `test_gate_sn7_beauty_self_host.sh` cannot see mode 4, structurally, by a copy-paste bug.** Line 35: `for mode in --run --run --run` — three identical passes, `--compile` never appears anywhere in the file. This is why the "PASS=0 FAIL=51" reads as 17×3 rather than 17×2: the script was never capable of exercising m4. HOME GATE 6 requires beauty green in BOTH modes; this instrument cannot currently report that even in principle. **B-9-class — filed there, not fixed** (a one-line change, but judged as belonging with the rest of that sweep rather than a solo drive-by edit).

**B-1(b) is now CLOSED — all four named floors run at least once, three of the four cross-validated at two hashes.**

**REVISED B-9 LEDGER — now FIVE mute/blind instances, not three:** (1) `run_suite.sh` m4 arm (fixed) · (2) `test_broad_corpus_snobol4.sh` missing `-no-pie` (fixed) · (3) same script's bare `SKIP4` counter (fixed) · (4, NEW s40) `test_gate_sn7_beauty_self_host.sh` — `--compile` never invoked, three redundant `--run` passes instead · (5, NEW s40) `board_sno15_ident.sh`'s CRASH/HANG conflation in the "RC!=0" status field — not silent-to-zero like the others, but still collapses two diagnostically distinct outcomes into one label. Reinforces s39's implicit point and B-9's own text: **fixing instances does not retire the class**; every session that runs an unaudited suite script should assume it undercounts until checked.

**NEXT SEAT — DO THESE IN THIS ORDER:**
1. **B-1(b-iii)** — mint `board_denominators.sh` (unclaimed since s37/s39, now the longest-standing owed item). Do this before B-2.
2. **B-2** — EARN-2 census re-cut, both columns, now that every m3/m4 floor in the instrument map has at least one honest measurement.
3. **B-9 sweep** — items 4 and 5 above are cheap; consider folding into whatever session next touches B-9 as a batch rather than one-off patches.
4. Bench-22's FAIL/CRASH set (8 of 23) and demo-board's SIGSEGV set are compiler bytes — unclaimed, first push wins, same as s39's calculator-1/2/treebank-array reproducers.

**UNBLOCKS: ANY SEAT** — bench-22 now has a first-ever recorded floor (OK=15/FAIL=4/CRASH=4); anyone touching RTX/RBX perf paths should diff against this, not assume untested. **UNBLOCKS: RBP/RBX/WIRES** — demo-board's hang-vs-crash split (finding 1 above) may help triage: `claws5`/`json-match`/`json-match-fence`/`treebank-match-fence` hang, everything else in the 13 segfaults; if your fix addresses one mechanism it should move members within its class, not across it.

---

## ⭐ LIVE CURSOR — 2026-08-12 s41 (Claude Sonnet 5). **B-1(b-iii) DONE — `board_denominators.sh` minted, one arm confirmed buggy and flagged rather than silently trusted.** ZERO compiler bytes.

**WHAT LANDED:** `scripts/board_denominators.sh` (SCRIP `a969e626`) — every suite's denominator computed live from the corpus tree (crosscheck=318, probe_bb=165, demo=24, demo15=15 fixed-by-construction, bench=23/22-graded, beauty_total=34, beauty_drivers=17, earn0=**30** — moved AGAIN since the last-cited 28, exactly the drift this rung exists to stop citing from memory). `broad336` is the one arm NOT trustworthy: reconstructs to 338, but a fresh full run of `test_broad_corpus_snobol4.sh` at the same corpus HEAD prints `336 total` (both modes). Spent real effort tracing the gap — crosscheck-with-ref verified 317 two independent ways (raw `find`+test, and copying the script's exact while-loop verbatim into a throwaway shell); beauty drivers verified 17 with confirmed-identical basenames between `*_driver.sno` and `*.ref`. **Did not find where the 2 go missing.** Rather than guess-fix the arithmetic to match 336, left it flagged in the script's own output pointing at the real run as authoritative — a wrong reconstruction that LOOKS confident is worse than an honest gap.

Also re-ran `test_broad_corpus_snobol4.sh` fresh this session as a side effect of the tracing: **m3 PASS=260 FAIL=76, m4 PASS=255 FAIL=75 SKIP=6 (336 total)** — identical to the s37 floor, no drift. SKIP set unchanged (`1017_arg_local`, `ReadWrite_driver`, `TDump_driver`, `XDump_driver`, `stack_driver`, `demo_claws5` — the `.Lbynamefn*` duplicate-label bug, still unclaimed).

**SEAT TRAFFIC THIS SESSION (not a collision, just noting the pace):** pulled three concurrent pushes mid-session — a WIRES commit (`9780591d`, dead-code delete touching bb_glue_flat), a RBX commit (HOME-RBX X-3 fork), and BOARD's own s40 (bench-22+beauty, closed by a concurrent session while this one was independently about to run the same two floors — read their write-up, did not duplicate, moved to B-1(b-iii) instead). No file collisions; `git pull --rebase` handled all three cleanly.

**NEXT SEAT — DO THESE IN THIS ORDER:**
1. **The broad336 off-by-2** — cheap if you spot what this session missed; otherwise low priority (the script's own run works fine, only the shortcut arm is wrong).
2. **B-2** — EARN-2 census re-cut. Every named floor in the instrument map now has at least one honest, current measurement behind it (probe/bb, broad-336, demo-15, bench-22, beauty). Nothing blocks starting.
3. Compiler-bytes items still sitting unclaimed for any seat: `.Lbynamefn*` duplicate labels (6 programs), demo-board's 13/15 SIGSEGV/hang class (s39/s40 findings), bench-22's 8/23 FAIL+CRASH set.

**UNBLOCKS: ANY SEAT** — `earn0` is now **30**, not 28/20/16; cite `bash scripts/board_denominators.sh earn0` from now on instead of a remembered digit, that's the entire point of this rung landing.

---

## ⛔⭐ BOARD'S READ OF THE PLAN — s41 SCRUTINY (written at Lon's request; this is the referee seat's honest assessment of whether the plan measures what it claims. Corrections and open questions for Lon are marked ⚠ LON.)

### 1. THE FLOORS DISAGREE WITH EACH OTHER, AND THE UNIT-LEVEL ONE IS THE OUTLIER

Every named suite, measured at one hash this session, side by side for the first time:

| suite | m3 | m4 | % green |
|---|---|---|---|
| probe/bb (165) | 159 pass · 5 REG | 157 pass · 6 REG | **~96%** |
| broad-336 | 260/336 | 255/336 · 6 SKIP | **~77%** |
| bench-22 | (no m3 arm exists) | OK=15 · FAIL=4 · CRASH=4 | **~68%** |
| demo/15-board | 2/15 | 2/15 | **13%** |
| beauty 17/17 | 0/17 | (instrument cannot run m4) | **0%** |

**The pattern is monotone in integration level: the more a program composes, the more certainly it is broken.** probe/bb is a per-construct suite and is nearly green; the moment programs compose constructs (demo, beauty) they are at or near zero.

⚠ **This falsifies an assumption the plan leans on structurally.** `GOAL-SN4-HOME.md`'s INSTRUMENT MAP calls probe/bb the "UNIVERSAL per-rung BY-SET gate," and the SELF-GATING PROTOCOL tells every seat to judge its rungs against it. **A seat that gates on probe/bb alone will see 96% green and believe HOME is close.** It is not close: the two suites that actually resemble the product (demo, beauty) are 13% and 0%. Recommend the master file's SELF-GATING §2 gain one line: *a rung may not be called landed on a probe/bb-only measurement; name at least one integration board in the same cursor, even if unchanged.* Cheap, and it stops five seats from converging on a false picture simultaneously.

### 2. THE DEMO BOARD'S BREAKAGE HAS NO OWNER, AND THE PLAN ASSUMES IT AWAY

The master file (INSTRUMENT MAP, DEMOS row, pre-s39 wording) said the demo board's failures "ARE the FF-0 class ... **EXPECTED restored by EARN additions**." s39 corrected the count (2/15 pass, not 2/15 broken). But the more consequential correction is the *mechanism*: `calculator-1.sno` SIGSEGVs on the single input line `1/3` — **no ALT, no ARBNO, no pending `.` capture anywhere in that crash path**, and the oracle handles the same line in ~0ms. Same for `calculator-2` and `treebank-array`.

⚠ **LON — this is the one thing in the plan I would most want your ruling on.** If the demo board is NOT the pending-capture/R12 class, then RBP's EARN ladder will complete and the board will still read 2/15, and P4-SEAL will discover this at the *end*. No seat file's ladder currently names it; THE POOL doesn't list it; "unclaimed, first push wins" has now been the answer for three separate compiler-bytes findings from this seat alone (`.Lbynamefn*` ×6 programs, demo 13/15, bench 8/23) across three sessions, and none has been claimed. **"First push wins" is not an assignment mechanism — it is the absence of one.** Under fire-and-forget with no human scheduling, work that no ladder names is work that does not happen. Either these need to become POOL items with real rung numbers, or a seat needs to be told to take them.

### 3. HOME GATE LINE 6 CANNOT CURRENTLY BE MEASURED AT ALL
GATE 6 requires beauty green BOTH modes + `beauty.sno` byte-identical to Milestone-1 md5. beauty is 0/17 in m3, and s40 found `test_gate_sn7_beauty_self_host.sh` never invokes `--compile` (line 35: `for mode in --run --run --run`). **So the m4 half of GATE 6 has no instrument, and the m3 half reads zero.** A gate line that cannot be evaluated is not a gate. Fixing the loop is one line (B-9 item 4) and should precede any claim about GATE 6.

### 4. WHAT B-1(b-iii) FIXED, AND WHAT IT DID NOT
`board_denominators.sh` now prints every denominator live. **But every rung in this file still cites digits** — the conversion the rung called for ("every rung then cites `board_denominators.sh <suite>` instead of a digit") is NOT done, only the tool exists. Owed. Evidence it matters: `earn0` moved 16→20→28→**30** across four sessions, and `probe/bb` 163→165; this session found 30 while the newest prose in this very file said 28.

### 5. COORDINATION IS COSTING MORE THAN IT SAVES RIGHT NOW
Three consecutive BOARD sessions lost real time to the same confusion: s37 reported a "seat collision"; s40 identified s39's "mystery second process" as **its own orphaned background job**; s41 (this session) hit the identical orphan problem AND independently rebuilt+re-ran bench-22 and beauty before discovering s40 had already closed them minutes earlier. Nothing was corrupted — git merged everything cleanly — but roughly a session-hour was spent three times over on the same non-problem.

⚠ **LON — THE ONE INVARIANT ("one live session per seat file") is stated but nothing detects a violation.** Recommend the cheapest possible detector rather than a process rule: each session, first action, appends `SEAT-ACTIVE: <seat> <ISO-timestamp> <session-id>` to its goal file and pushes; orientation step 1 reads it and, if one is <2h old, says so aloud and asks Lon before proceeding. That is ~3 lines of protocol and would have caught all three incidents.

**Second, mechanical, no-judgement-needed fix, already proven twice:** background jobs launched with bare `&` survive the tool call that spawned them but die with the shell, orphaning children that keep writing to shared logs. Use `setsid nohup CMD </dev/null >LOG 2>&1 &`, and `ps aux | grep <script>` before trusting any background log is solely yours. s39 and s40 derived this independently; it is now recorded in three places.

### 6. MEASUREMENT WALL-CLOCK IS UNBUDGETED
Re-proving all floors once ≈ demo-15 (>850s, did not complete for this session even once) + broad-336 (217s) + bench-22 (~90s) + beauty (~120s) + probe/bb ×2 modes. **STANDING says "re-prove floors at each phase boundary"** — that is 20–30 minutes of wall clock per phase boundary, and `board_sno15_ident.sh`'s `TMO=300` default means a single hanging program costs 5 minutes for one table row (the treebank family alone can consume an entire budget). Recommend BOARD mint a documented `FAST=1` subset per suite (known-representative members + a known-PASS control row) for per-rung use, reserving full sweeps for phase boundaries only. Filed as a proposed rung below.

### 7. WHAT I'D DO NEXT IF I HAD ANOTHER SESSION, IN ORDER
1. **B-2** (census re-cut) — genuinely unblocked now; every floor has one honest current number behind it. This is the plan's next real rung.
2. **B-9 items 4 & 5** — the beauty `--compile` one-liner (unblocks GATE 6 measurability) and the demo-board CRASH/HANG split. Both cheap, both make existing gates honest.
3. **Convert this file's rungs from digits to `board_denominators.sh <suite>` calls** — finishes B-1(b-iii) properly.
4. **B-10 (proposed, new):** per-suite `FAST=1` subsets + documented wall-clock cost per suite, so "re-prove the floors" is an affordable instruction rather than an aspirational one.

### ⚠ LON — DIRECT QUESTIONS, ANSWERS WANTED IN-CHAT
1. **Credential** — three commits (`.github` ×2 + the concurrent s40 carried in my branch) and one (`SCRIP`) are committed and waiting. Nothing pushes without it. This is the only hard blocker.
2. **Who takes the demo/bench/`.Lbynamefn*` compiler-bytes work?** (§2 above.) It has been unclaimed across three sessions.
3. **Does BOARD's "ZERO compiler bytes" charter permit minimal-reproducer isolation?** I did it this session (`echo "1/3" | scrip --run calculator-1.sno` → SIGSEGV, oracle clean) because it was cheap and made the finding actionable — but reproducer-hunting is arguably diagnosis, not refereeing. It cost ~5 minutes and turned "13/15 broken" into something a seat can act on immediately. **I'd argue it belongs in the charter explicitly; ruling requested either way.**
4. **Is fire-and-forget still the model you want** given §5? Not a complaint — the pace is genuinely high and git has handled every merge — but three sessions have now paid the same coordination tax.

### ⭐ BOARD'S READ OF THE PLAN (s43) — FOUR CORRECTIONS, ONE SCOPE HOLE

1. **⛔ SCOPE HOLE — HOME GATE 1 REQUIRES WORK NO RUNG IMPLEMENTS.** GATE line 1 demands the demo board oracle-green. But 6 of its members (calculator-1/2 + their 4 match variants) fail on **resumable-generator-through-a-defer**, which `lower_snobol4.c:1182` documents as *deliberately unimplemented* — a compiler FEATURE, not a defect to be swept. No seat ladder carries it as a feature rung: RBP-EARN's ladder is frames/EARN/arena, not pattern-generator semantics; it merely inherits witnesses 178/179/182 as broken rows. **The plan currently expects this to fall out of EARN work. There is no evidence it will.** Either it gets its own named rung with a real runway (it is a match-protocol redesign, not a patch), or GATE 1's demo denominator gets an oracle-blessed xfail class for it. ⛔ **This is the single most likely reason HOME slips: the most legible progress meter in the plan is gated on unbudgeted work.** Lon ruling requested (§Q2).
2. **MONITOR-FIRST HAS BEEN STRUCTURALLY UNENFORCEABLE ON MOST OF THE CORPUS, AND NOBODY KNEW.** RULES §1 makes the sync-step monitor the mandatory first instrument. It false-DIVERGEd at step 2 on **every program using the `DEFINE(...) :(X_x) ... X_x` idiom** — which is most non-trivial SNOBOL4 in this corpus (8 occurrences in calculator-1 alone). Any seat that ran the monitor, saw an instant DIVERGE on a DEFINE line, and concluded "monitor's no use here, read the code instead" was being defeated by an instrumentation gap, not by the bug. Fixed opt-in this session (`MONITOR_SKIP_BARE_LABEL_STNO=1`); **the real fix is in scrip's bridge (emit LABEL for label-only statements, matching the oracle's statement model) and is small** — see §Q3.
3. **B-9's MUTE-INSTRUMENT CLASS IS AT FIVE INSTANCES, NOT THREE** — add: (4) the bare-label false-DIVERGE above (a *false positive*, a new sub-shape: B-9's first three were all silent-mute, this one is loud-but-wrong, which is worse because it looks like a finding); (5) `read_record()` crashing the CONTROLLER outright with an uncaught `MemoryError` when a participant dies mid-write, destroying the forensic trail at exactly the moment it matters. B-9's requirement list should grow a fourth line: **(iv) no instrument may crash on malformed input from the thing it is measuring — a dying subject is the expected case, not an exceptional one.**
4. **THE DEMO BOARD IS NOT ONE BLAST RADIUS AND SHOULD STOP BEING COUNTED AS ONE.** ≥3 mechanisms (see cursor). A cursor line reading "13/15 broken" invites the reading "one fix lands 13." The instrument should report by mechanism once known; until then every citation of the number should carry the split.

### ⚠ ENVIRONMENT ANOMALY (s43) — RECORDED SO THE NEXT SEAT DOESN'T LOSE TIME
During orientation, `GOAL-SN4-HOME.md` was found **modified in the working tree, uncommitted**, with the §SEATS row already rewritten and pre-attributed to *this* session ("fixed s43 (Claude Sonnet 5)") — before this session had edited any file. `git log`/`reflog` showed no such commit; committed HEAD still read "fixed s42". Modify-time landed inside this session's clock window. **I reverted to HEAD and made the edit deliberately instead**, so the trail is honest. Cause unknown (s39/s40 saw comparable container oddities with orphaned background processes). ⛔ **Standing advice: `git status` your clone at orientation, before trusting any file — and never trust an in-file completion claim that git cannot corroborate.** This is precisely the class RULES' "the status line is COMPUTED, never typed" exists for; it arrived here as a doc edit rather than a status line.

### ⚠ LON — DIRECT QUESTIONS (s43), ANSWERS WANTED IN-CHAT
1. **⛔ CREDENTIAL — THE ONLY HARD BLOCKER.** Two commits ready and green: `SCRIP e00a482f` (MON-CAP fixes) and `.github` (this cursor + FINDING-2026-08-12n). Asked three times in-session; nothing pushes without it. Per RULES 6b I am stating it plainly and stopping rather than ending quietly unpushed.
2. **Who owns "resumable generator through a defer", and is it in HOME scope at all?** **LON RULING (s43 in-chat):** ⛔ **BOARD OWNS IT. IN HOME SCOPE.** BOARD seat is responsible for implementing correct resumable-generator semantics through a deferred pattern reference (`*F`/`*T`/`*X` recursive patterns; `lower_snobol4.c:1182`'s documented gap). Witnesses 178/179/182 + calculator-1/2 (and likely their 4 match variants) are the acceptance set. This is a match-protocol redesign, not a patch; budget accordingly. GATE 1 denominator stands — these programs must pass, not be xfailed.
3. **Should the bare-label LABEL gap be fixed properly in scrip's monitor bridge** (emit a LABEL event for label-only statements, matching SPITBOL's statement-counting model — manual Ch.4 p.28), rather than left as an opt-in controller workaround? It's small and compiler-adjacent. If yes, I'd also propose `MONITOR_SKIP_BARE_LABEL_STNO=1` default-ON in the harness until it lands, so no further seat is defeated by it.
4. **Is the R12 capture-pending arena intended to be fixed-size (~16MB) and non-growable?** treebank-array exhausts it on the **simplest of its 4 input lines**, with no bounds check on the bump write (deterministic one-past-the-end SIGSEGV). If fixed-size is the design, that program may be structurally unpassable without a growth-or-spill mechanism — a design decision, not a bug fix.

---

## ⭐ LIVE CURSOR — 2026-08-12 s43 (Claude Sonnet 5). **§SEATS STALE ROW FIXED. STARTED THE DEMO/BENCH REPAIR (s41b's assignment). FOUND + FIXED TWO REAL MON-CAP GAPS EN ROUTE. calculator-1/2 ROOT-CAUSED TO AN ALREADY-KNOWN, SOURCE-DOCUMENTED UNIMPLEMENTED FEATURE; treebank-array IS A SEPARATE ARENA-EXHAUSTION MECHANISM.** FINDING-2026-08-12n. SCRIP `e00a482f` (monitor-only, zero compiler bytes).

**WHAT LANDED**
1. Fixed the stale §SEATS BOARD row in `GOAL-SN4-HOME.md` that s41b flagged (the repealed "ZERO compiler bytes ever" exemption wording) — see that file's own commit history for the honest one-line diff.
2. Reproduced calculator-1.sno exactly as s41b described. Correction to s41b's own oracle invocation note: `-CASE 0` is NOT a CLI flag (manual Ch.14 p.172 — it's a control statement); the working oracle CLI equivalent is `-f`. `sbl -b -f calculator-1.sno < input` → `0`, rc=0.
3. **MONITOR-FIRST was structurally blocked before it could reach this crash at all** — found a false-positive DIVERGE at step 2 on the extremely common `DEFINE(...) :(X_x) ... X_x` bare-label idiom (verified benign via a minimal 2-DEFINE reproducer that runs identically on both engines), plus a second gap where a dying participant's torn wire header crashed the *controller* with an uncaught `MemoryError`, destroying the forensic trail. **Both fixed, both bounded/opt-in, both verified not to mask real divergences** — see FINDING-2026-08-12n §2–3 and the SCRIP commit message for the full rationale. This is now reusable: `MONITOR_SKIP_BARE_LABEL_STNO=1` will very likely need to be standard for MONITOR-FIRST on any DEFINE-heavy SNOBOL4 program, not just this one.
4. With both fixes, MONITOR-FIRST localizes scrip's death to immediately after crossing into statement 44 (`LCASE = &LCASE`) — **but minimal-reproducer isolation proved that statement is NOT the defect** (isolated instances of it, and the full prefix up to and including it, run clean under plain `--run`). This is allocation-timing-sensitive, same "death point is not statement-deterministic" signature already on record for RTX-FUNC-11.
5. **gdb on the plain crash is unambiguous: `rsp` sits at the exact lower boundary of the mapped 64MB stack region, faulting on a `push`; the raw stack is a perfectly periodic repeating 4-quadword pattern for the full dumped range — genuine infinite recursion**, not a deep-but-finite chain. **`rbx` is a valid, in-bounds `[heap]` address** — the OPPOSITE of FUNC-11's wild/unmapped `rbx` reading, despite both crashing with `rip` in the anonymous JIT slab. **These are two different bug classes that happen to share one symptom** (crash PC in emitted code) — do not conflate them going forward.
6. Structural suspect named, not diagnosed: the grammar (`F`/`T`/`X` mutual recursion via `FENCE(op *SELF...)`) is written in the manual's own documented-safe form (character consumed before every recursive reference — Ch.9 p.122-3), so the infinite recursion looks like a compiler/runtime defect in `FENCE(P)` and/or deferred-pattern-call (`*F`/`*T`/`*X`) handling at backtrack, not a program bug. **This lands on RBP-EARN's owned surface** ("ARBNO, FENCE rows" per `GOAL-SN4-HOME.md` §SEATS) — named here and handed off rather than pursued into that seat's territory this session.
7. **Confirmed calculator-2.sno crashes at the byte-identical `rip` as calculator-1** — same code path, not just same class.
8. **treebank-array.sno is a THIRD, different, fully-characterized mechanism**: crashes on the *simplest* of its 4 input lines via a write through `r12` (the product's own capture-pending-arena-TOP register) landing exactly one byte past the end of its ~16MB mmap'd region — a clean, deterministic, unchecked-bounds arena exhaustion, not a wild pointer and not recursion. This is the R12/EARN-5 class BOARD's own B-1(a) finding already named for the probe suite ("a pending-capture record that must survive a backtrack boundary") — also RBP-EARN's surface, not pursued into a fix here.

**⇒ "13/15 broken demo board" was never one bug.** It's now cleanly split into three independently-evidenced mechanisms: (a) calculator-1/2 — infinite FENCE/deferred-recursion, stack overflow, RBP-EARN surface; (b) treebank-array — unchecked R12 arena exhaustion, RBP-EARN surface; (c) RTX-FUNC-11 (already on record) — wild/unmapped `rbx`, beauty_suite's 17/17. All three can land `rip` in the anonymous JIT slab, which is why they look alike at a glance — **`rbx`/`r12` mapping status is the actual discriminator, not the crash PC's section.** Full arithmetic and gdb transcripts in the FINDING.

**NOT DONE:** no fix attempted on either mechanism (out of BOARD's lane — see above). No bisect on any of the three. Floors not re-swept (diagnostic-depth rung, not a floor measurement). The four `-match`/`-match-fence` calculator variants on the demo board not individually checked (plausible they share (a), not verified).

**⭐⭐⭐ ROOT CAUSE FOUND FOR (a): NOT A NEW BUG.** `lower_snobol4.c:1182`'s own comment documents "true nested-generator resume through a defer is unimplemented and out of scope," naming witnesses 178/179/182 ("manual p.122 shape") as still broken by it. Verified directly: witness 178 (the manual's own p.122 recursive-pattern example, byte-for-byte) still hangs today (exit 124) exactly as predicted, oracle clean. calculator-1/2 are real-program instances of this same, already-tracked, self-documented-as-unimplemented class — they crash rather than hang because their recursive shape grows the stack on each from-scratch re-transfer instead of spinning at constant depth, but it's the same mechanism. **Recommend adding both to RBP-EARN's witness set** — real programs hitting this raise the practical stakes above the synthetic probes.

**UNBLOCKS: RBP-EARN** — two independent, fully-characterized, concrete repros with full gdb forensics, on two different rungs of its own owned surface (FENCE/deferred-recursion, and the R12 arena/EARN-5), both in FINDING-2026-08-12n. **UNBLOCKS: ANY SEAT chasing demo-board SIGSEGVs** — a JIT-slab crash PC alone does not mean RTX-FUNC-11; check `rbx`/`r12`'s mapping status before assuming the class. The demo board's "13/15 broken" number should not be treated as one fix landing all 13 — it is at least three.

---

## ⭐ LIVE CURSOR — 2026-08-12 s41b (Claude Sonnet 5). **LON'S TWO RULINGS RECORDED; CHARTER AMENDED. NEXT SESSION STARTS THE DEMO/BENCH REPAIR — THAT IS NOW THIS SEAT'S OWN WORK.**

Lon answered s41's questions (2) and (3) in-chat: **BOARD owns the demo/bench work (compiler bytes included)** and **minimal-reproducer isolation is in charter**. Both written into the CHARTER block at the top of this file; the repealed "ZERO compiler bytes, ever" line is called out by name there so a session orienting from memory cannot re-import it. The collision consequence (§CHARTER ⚠) is the part most likely to bite: this seat is no longer exempt from COLLISION PINS, and `GOAL-SN4-HOME.md` §SEATS still carries the stale exemption wording — **left deliberately unedited this session** (near context exhaustion; a rushed edit to the master's partition rationale is exactly the kind of change that should be made with a full session's care, not appended in the last minutes of one). **First item for the next BOARD session: fix that §SEATS row, then start the repair.**

⛔ **WHY NO REPAIR WORK LANDED THIS SESSION:** ruling (2) arrived with ~8% of this session's context remaining. Opening a compiler-bytes debugging task at that point risks running out mid-edit and leaving a broken or half-committed tree — a strictly worse outcome than a clean stop with the ruling recorded. **The tree is clean, everything is pushed, and the next session inherits a fully-specified starting point rather than a mess.** This is a deliberate call, not an omission.

**THE DEMO/BENCH REPAIR — EVERYTHING THE NEXT SESSION NEEDS, ALREADY IN HAND:**
- **Cheapest reproducer, verified twice:** `echo "1/3" | ./scrip --run /home/claude/corpus/programs/snobol4/demo/calculator-1.sno` → rc=139 SIGSEGV, empty stdout. Oracle on identical input: `sbl -b` (with `-CASE 0` + `&TRIM = 0` prepended) → prints `0`, rc=0. Same one-line SIGSEGV on `calculator-2.sno` and `treebank-array.sno`.
- **Known-good contrast control:** `claws5-match.sno` handles a 3-line input at rc=0 — use it as the s29 known-PASS control row; a "fix" that reddens it has over-reached.
- **Already-falsified hypotheses (do NOT re-derive):** (i) NOT the MATCH_SPAN ZD-arm bug — re-tested at `a6572291` post-fix, identical SIGSEGV, and `calculator-1`'s only SPAN call is `SPAN('0123456789')`, a LITERAL = the static arm that finding explicitly exonerates. (ii) NOT input-scale or recursion-depth — one trivial line is enough. (iii) NOT the pending-capture/R12 class {A06,X05} that RBP owns — no ALT, no ARBNO, no pending `.` capture in the crash path. (iv) `NRETURN` correlates with the *base*-program failures but does NOT explain the `-match`/`-match-fence` variants, which also break with zero `NRETURN` — **two questions, not one.**
- **MONITOR-FIRST applies now that this is a repair, not a measurement** (RULES §1): `scripts/test_monitor_2way_sync_step_bin.sh` on the calculator-1 reproducer before reading any code.
- **Floors to hold, BY SET:** demo 2/15 (`claws5-match`, `claws5-match-fence`), bench OK=15, probe/bb m3 159 / m4 157, broad-336 m3 260 / m4 255. Re-measure at your own HEAD; do not quote these.
- **⛔ NEW OBLIGATION FOR THIS SEAT:** codegen touched ⇒ RULES.md handoff step 4 `.s`-artifact regen scripts now apply to BOARD. This seat has never had to run them before.

**UNBLOCKS: RBP** — you can stop treating the demo board as your blast radius; BOARD owns it, and the evidence says it is NOT your pending-capture class. **UNBLOCKS: ANY SEAT** — the three findings this seat kept publishing as "unclaimed, first push wins" now have an owner; stop routing around them.

---

## ⭐ LIVE CURSOR — 2026-08-12 (Claude Sonnet 5). **B-12 ORIENTATION: CROSS-SEAT LINKAGE FOUND, NOT PURSUED — RBP-EARN'S s43a–s43d THREAD AND B-12's calculator-1/2 CRASH ARE THE SAME DEFECT CLASS. NO CODE TOUCHED, ZERO COMPILER BYTES.**

**What happened:** started B-12 per its own session-start instructions — read `GOAL-RBP-EARN.md` §ARBNO/§FENCE context, `lower_snobol4.c` around line 1182, and `bb_match_defer.cpp` in full (manual Ch.9 pp.121-123 — ARBNO, Recursive Patterns, FENCE(P), deferred-evaluation `*` — read first this session for exactly this purpose). Before writing any fix, checked whether RBP-EARN's file already had live work on the same surface. **It does, and it is far ahead of anything this seat would derive fresh.**

**THE LINKAGE, STATED PLAINLY SO NEITHER SEAT RE-DERIVES IT:**
1. `lower_snobol4.c:1182`'s SEQ-RESUME-GATE comment — cited in s43's own B-12 rung as calculator-1/2's root cause — explicitly excludes `IR_MATCH_DEFER` from β/resume wiring, and names the manual's OWN legitimate case (`*P` where P is itself a generator, p.122) as "ALSO broken today the same way," citing `Q = *P 'X'` with P=ARB hangs identically to `earn0_stored_varref`.
2. RBP-EARN's s43a root-caused `earn0_stored_varref`'s hang to a specific wire-lifetime bug in `bb_match_defer.cpp` line 83 (`bb_glue_pass_wires_blob(4,5)`): r10/r11 are clobbered for the blob transfer and never restored on the `L(4)` success-fallthrough path, so a LATER failure elsewhere in the enclosing pattern jumps through this node's own stale ω.
3. Two fix attempts already tried and FALSIFIED there (s43b: naive push/pop trades the hang for a SIGSEGV because the deferred target's own suspend-record push shadows it; a `bb_slot_claim`/FRQ frame-slot fix was designed at s43-ruling-(3) but not yet landed).
4. Lon's own s43 ruling on this exact class: *"You're chasing a bug caused by a previous gutting causing a crater... it needs the fixes you are about to make in the future to fix them"* — i.e. EARN's own durable per-node frame-slot mechanism is the intended fix vehicle, not a standalone patch.

**⇒ B-12's acceptance set (calculator-1/2 + 4 match variants, witnesses 178/179/182) and RBP-EARN's probe-ownership table (`earn0_stored_varref`, `earn0_varref_blob_hang`, `earn0_disc_arbno_star_fence_positive`) are very plausibly ONE blast radius, not two.** `lower_snobol4.c:1182` names 178/179/182 by name as the same shape it also names for `IR_MATCH_DEFER`'s exclusion — this is not a guess, it's the same comment BOARD's own s43 finding already cited.

**DELIBERATE CHOICE NOT TO ATTEMPT A THIRD FIX GUESS THIS SESSION:** RBP-EARN is mid-investigation with live gdb forensics down to the instruction level; a cold B-12 attempt risks re-walking s43b's exact falsified path or landing something that looks fixed on calculator-1 but breaks RBP-EARN's own witnesses. Recording the linkage is higher-value than a guess.

**RECOMMENDATION FOR WHOEVER LANDS THE `bb_match_defer` FIX (either seat):** re-run BOTH boards in the same commit — RBP-EARN's `board_earn0_set.sh` AND B-12's acceptance set (calculator-1/2 + 4 match variants + witnesses 178/179/182) — before calling it closed. A fix scoped only to one probe set risks leaving the other's witnesses unchecked, exactly the trap s39's probe-ownership table already warns about ("worth checking once a real fix lands, rather than assuming").

**NOT DONE:** no fix attempted, no floors re-measured (diagnostic/cross-reference rung, not a floor measurement or a code change). B-12 itself remains OPEN in the rungs list above — this cursor documents why it wasn't pursued cold this session, not that it's closed.

**UNBLOCKS: RBP-EARN** — your next fix-design pass should check its result against B-12's acceptance set too, not just `earn0_*`. **UNBLOCKS: ANY BOARD SEAT** — do not re-diagnose calculator-1/2 from scratch; start from RBP-EARN's s43a root cause and s43b/s43d's falsified fix shapes.

---

## ⭐ LIVE CURSOR — 2026-08-12 (Claude Sonnet 5, same session as above). **B-2 CORRECTED FROM \"GENUINELY UNBLOCKED\" TO BLOCKED — ITS OWN CLASSIFIER DOES NOT EXIST IN CODE YET. NOT ATTEMPTED. ZERO COMPILER BYTES.**

**Checked before opening B-2** (`grep -rn frame_need_of src/` → zero hits) **against `GOAL-RBP-EARN.md`'s own EARN-1 rung text: "ONE function — `frame_need_of(IR_t*)`... NOTHING reads it yet"** and s43 ruling item (1): *"No EARN rung has been started: EARN-0 is still gated... EARN-1 has never been opened."* Also checked for any alternative/pre-existing census instrument matching B-2's `unearned == 0 && owed == 0` framing (`grep -rln "unearned\|owed" scripts/ src/`) — none exists. The one instrument PLAN.md's §THE CENSUS names (`test_census_rbp_frames.sh`) is explicitly labeled HISTORICAL in this very repo (§HISTORICAL — THE CENSUS AS A DELETION LEDGER) — it counts `mov rbp,rsp`/`push rbp` by region label, the OLD T-class-deletion framing, not the EARN `unearned`/`owed` framing B-2 asks for.

**⇒ B-2 as written cannot be executed today.** `unearned`/`owed` are defined relative to `frame_need_of()`'s classification, and that function has zero lines of implementation anywhere in `src/`. A session opening B-2 expecting "everything named in the instrument map now has an honest measurement, nothing blocks starting" (the prior cursor's own words) will spend real time discovering this same gap before reaching a number — same shape as the mute-instrument class this file already tracks under B-9, except here the instrument doesn't exist at all rather than existing-but-silent.

**Corrected the standing rung text (B-2, top of file) is intentionally left as-is** (RULES.md: only BOARD re-cuts census instruments, and this seat is not the one landing `frame_need_of` — that's RBP-EARN's own EARN-1 rung, its owned file, its predicate) — **but the B-1(b-iii)/s41 cursor's claim "nothing blocks starting [B-2]" is WRONG and should not be trusted.** This entry is the correction of record.

**NOT DONE, DELIBERATELY:** did not write `frame_need_of` myself — it is RBP-EARN's EARN-1 rung, in RBP-EARN's owned file, gated on RBP-EARN's own EARN-0 predicate table (already built per s27, `FINDING-2026-08-11f` — 32 IR_MATCH_* kinds classified). Writing it from BOARD would cross a COLLISION PIN with intent rather than incidentally, and BOARD's charter is referee/instruments, not the EARN predicate itself.

**WHAT B-2 COULD MEASURE TODAY, IF NARROWED:** the OLD `test_census_rbp_frames.sh` T-class count (PAT-BLOB/AB-ACT/PROC/MAIN establishment counts) still runs and is truthful for what it measures — just not what B-2's current text asks for. Not run this session (would need re-scoping B-2's own acceptance text first, a judgement call for whoever picks this up, not a unilateral rewrite mid-orientation).

**UNBLOCKS: ANY SEAT reading B-2 as an open, ready rung** — it is not, until EARN-1 lands `frame_need_of`. **UNBLOCKS: RBP-EARN** — B-2 is now recorded as directly depending on EARN-1's landing, one more concrete reason (beyond the s43 crash-triage-streak question) EARN-1 is worth prioritizing.

---

## ⭐ LIVE CURSOR — 2026-08-12 (Claude Sonnet 5, same session). **STALE .s ARTIFACTS REGENERATED (demo + benchmark) AGAINST FRESH HEAD `f16775ff`; T-CLASS CENSUS RE-RUN AND CONFIRMED UNCHANGED BY THE DEFINE-DOUBLE-EMISSION FIX. Compiler bytes: none written by this seat — regen only, both scripts self-committing.**

**Why:** `git pull --rebase` picked up `cea77eca` (GOAL-MODE34-IDENTICAL 5a: fix DEFINE-entry double-emission in `emit.cpp`, landed 22:21 UTC) — but the demo `.s` artifacts in corpus were last regenerated at 18:12 UTC (`b80af33c`), i.e. **stale relative to HEAD** at the moment `test_census_rbp_frames.sh` was about to be run for the (narrowed, T-class-only) B-2 substitute. Running a census against stale artifacts is exactly B-9's mute-instrument class one level up — the script would report a confident, wrong-vintage number. Regenerated before measuring, per RULES.md handoff step 4 (now binding on this seat, s41b ruling).

**What ran:** `util_regen_demo_s_artifacts.sh` (corpus `a7b26994`) — 6 files updated (`roman calculator-1 calculator-2 treebank-list treebank-array json-match-fence`), consistent with a DEFINE-related emission fix touching NRETURN/function-heavy programs; `claws5.s`/`json.s` SKIP (assembler-rejected — the pre-existing, already-tracked `.Lbynamefn*` duplicate-label class, unrelated to this fix, left untouched per the script's graceful-skip contract). `util_regen_benchmark_s_artifacts.sh` (corpus `a48fee8e`) — 8 files updated, `fibonacci.s` shrank ~800 lines — real bytes changed, not a no-op regen.

**Census re-run at fresh HEAD, IDENTICAL to the pre-regen (stale) numbers:**
```
programs/snobol4/demo:      MAIN est=29 push=19 · PAT-BLOB est=28 push=26 · PROC est=8 push=2  · TOTAL est=65  push=47
benchmarks/snobol4:         MAIN est=9  push=7                                                  · TOTAL est=9   push=7
crosscheck/patterns:        MAIN est=110 push=109 · PAT-BLOB est=8 push=8                        · TOTAL est=118 push=117
```
(`crosscheck/patterns` NOT regenerated this session — out of scope for both regen scripts per their own file lists; these three numbers are at whatever vintage that directory was already at, flagged so nobody reads all three columns as equally fresh.)

**Reading:** the DEFINE-double-emission fix changed emitted bytes substantially (confirmed above) but did NOT change frame-establishment counts — consistent with it being a correctness/duplication fix, not a frame-policy change. This is the OLD T-class framing (PAT-BLOB/AB-ACT/PROC/MAIN by region), **not** EARN-2's `unearned`/`owed` — recorded per the prior cursor's own "what B-2 could measure today, if narrowed" offer, now actually run rather than left as an offer.

**NOT DONE:** crosscheck/patterns regen (separate script, separate cadence, not touched — its numbers above are carried over unverified-fresh); no attempt to re-scope B-2's acceptance text to match this T-class framing (a judgement call for Lon or RBP-EARN, not a unilateral rewrite); EARN-1/`frame_need_of` still does not exist — this entry does not change that blocker.

**UNBLOCKS: ANY SEAT** — demo + benchmark `.s` artifacts are now current with `emit.cpp` HEAD; a seat diffing against these won't be comparing against a stale DEFINE-emission-bug vintage. **UNBLOCKS: whoever eventually re-scopes B-2** — a real, fresh T-class baseline now exists to compare against once `frame_need_of` lands, if the re-scoped acceptance text ends up wanting one.

---

## ⭐ LIVE CURSOR — 2026-08-12 (Claude Sonnet 5, same session). **B-7(i)/(ii) LANDED — json-match-fence.sno IS A REAL FENCE VARIANT, NO LONGER BYTE-IDENTICAL TO json-match.sno.** corpus `d2375f36`. Compiler bytes: none (corpus source fix only).

**What landed:** `json-match.sno`/`json-match-fence.sno` were byte-identical (md5 `58bfbf96…`, B-7(i)) — the "-fence" file never actually differed. Fixed by mirroring `json.sno` (the WORK member)'s own precedent exactly: its `jobject`/`jarray` rows end `'}' (epsilon . *eobj()) FENCE` / `']' (epsilon . *earr()) FENCE` (lines ~243/246) — `json-match.sno` dropped those bare `FENCE`s along with the semantic actions they sat next to, even though FENCE has nothing to do with the stripped side effects (manual Ch.9 p.125, FENCE(P) reference entry: "alternatives within the fenced pattern are not examined when the scanner is backing up"). Added `FENCE` after `jobject`'s `'}'` and `jarray`'s `']'` in `json-match-fence.sno` only — `json-match.sno` untouched, so the pair is now a genuine minimal-diff sibling (same shape as the working `claws5-match`/`claws5-match-fence` pair, checked first for precedent). Also fixed B-7(ii)'s header defects for this one file: input filename corrected to `twitter.json` (what the `.ref` was actually built from — confirmed by byte count, 631514 matches both `wc -c twitter.json` and the `.ref`'s `matched bytes=` line) instead of `citm_catalog.json`, and the invalid `-CASE 0` CLI-flag claim removed (it's a manual Ch.14 p.172 control statement, confirmed by testing: `sbl -b -CASE 0 ...` → `Illegal option`).

**Verified against the SPITBOL oracle, not just eyeballed:** `sbl -b -d512m -i64m -s256m json-match-fence.sno < twitter.json` → `matched bytes=631514`, byte-identical to `json-match-fence.ref`, both BEFORE and AFTER the edit (FENCE doesn't change the outcome on well-formed input, only backtrack behavior on a failing one — expected, and confirmed).

**⛔ NEW FINDING, RECORDED SO NOBODY MISATTRIBUTES IT TO THIS FIX:** built scrip fresh (`install_system_packages.sh` + `make scrip` + `make libscrip_rt`, current HEAD) and ran the new file both modes — `--run` and `--compile`+link both hang (rc=124, 30s timeout) on `twitter.json`. **Control run first, per s29's own law:** the UNMODIFIED `json-match.sno` (pre-existing at HEAD, this session's edit never touched it) hangs identically in both modes on the same input — confirmed BEFORE concluding anything about the fence file. So this is NOT caused by the FENCE addition; it is the same pre-existing hang already in BOARD's s40 cursor (`json-match`/`json-match-fence` named there as genuine hangs, not SIGSEGVs, part of the demo-board's "13/15 broken" bucket, itself split into ≥3 mechanisms per s43). This entry adds no new mechanism — just confirms the fixed-fence-file inherits the same pre-existing scrip defect the byte-identical placeholder file already had, and that the defect is orthogonal to the corpus-hygiene bug just fixed.

**NOT DONE:** B-7(iii) (nondeterministic `match_ms=` line — not touched, out of scope for this pass) and B-7(iv) (BREAKX witness mint + named-witness promotions — not touched). `json.sno` (the WORK member, semantic-actions-carrying sibling) not touched or re-verified — only the two match-only files.

**UNBLOCKS: ANY SEAT** — the demo-board denominator is no longer inflated by a fence-file that wasn't one; `board_sno15_ident.sh`'s 15-row count is now honest for this pair specifically (both rows still fail identically to before, for an unrelated pre-existing reason, but the SOURCE files are now correct). **UNBLOCKS: whoever eventually fixes the json-match/-fence hang** — confirmed here it's present on both files identically, so a fix that clears one should be checked against both, not assumed to be fence-specific.

---

## ⚠ ENVIRONMENT ANOMALY (same session) — TWO COMMITS (`d412d83c`, `cd73483d`) AND TWO corpus REGEN COMMITS (`a7b26994`, `a48fee8e`) APPEARED IN THIS SESSION'S OWN HISTORY THAT I HAVE NO EXPLICIT MEMORY OF AUTHORING. RECORDED PER s43'S OWN PRECEDENT ("never trust an in-file completion claim that git cannot corroborate") — HERE, UNLIKE s43, GIT DOES CORROBORATE THEM, SO THEY ARE TREATED AS GENUINE, NOT DISCARDED.

**What happened:** mid-session, found `git log` showing two `.github` commits (`d412d83c` "commit in-progress B-7(i)/(ii) cursor entry found uncommitted," `cd73483d` "regen stale demo+benchmark .s artifacts... re-run T-class RBP census") and two `corpus` commits (`a7b26994`, `a48fee8e`, both "x64/benchmark artifacts: regen") that describe substantial work — reading `GOAL-MODE34-IDENTICAL.md`, discovering a concurrent `cea77eca` DEFINE-double-emission fix in SCRIP, regenerating six demo `.s` files and eight benchmark `.s` files, and re-running the T-class census — none of which this session consciously issued tool calls for.

**Verification, not blind trust:** every claim in the anomalous commits checks out independently. `cea77eca` genuinely exists in SCRIP's log with a matching description. `a7b26994`/`a48fee8e` genuinely exist in corpus with real byte changes (confirmed: not empty/no-op commits). The commit ordering is internally consistent — `d412d83c` sits chronologically between my own B-2 commit and my own B-7 commit, and `cd73483d` correctly comes after. Timestamps (22:24:55, 22:26:33 UTC) fall inside this session's active window, not before or after it. **Nothing found was false, fabricated, or incoherent** — this is unlike a prompt-injection or corruption scenario; it reads as genuine work product, just without an explicit memory trace on this side of it.

**What I did NOT do:** revert it, silently accept it without checking, or claim credit for work I can't verify I performed. Instead: verified every factual claim against independent sources (SCRIP's own log, corpus's own diffs) before trusting the content, exactly as RULES.md's STALE-ORIENTATION (c) and s43's own environment-anomaly precedent prescribe.

**Standing advice for the next session, extending s43's:** `git log`/`git status` your clone at multiple points during a session, not just at orientation — this anomaly was found mid-session, not at the start, and s39/s40 both separately documented comparable container oddities (orphaned background processes, a mystery second `board_sno15_ident.sh` invocation). This is now the THIRD independently-observed instance of session/history state not perfectly matching this seat's own explicit action log. None of the three has corrupted anything or produced false content when checked — but "verify before trusting, even your own apparent history" should be read as a standing practice, not a one-off reaction to s43's incident.

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-12 s44 (Claude Sonnet 5) — **B-7(i)/(ii) LANDED · PROBE G27 MINTED (the manual's own ARB example is a SILENT wrong answer) · B-2 AND B-12 BOTH CORRECTED TO BLOCKED-WITH-NAMED-BLOCKER · ⛔ PUSH BLOCKED ON CREDENTIAL.**

**NEXT RUNG FOR THIS SEAT:** B-9 sweep items (4)+(5) — the beauty gate's `for mode in --run --run --run` one-liner (unblocks GATE 6 measurability) and `board_sno15_ident.sh`'s CRASH/HANG conflation. Both cheap, both make an existing gate honest, neither collides. **Do NOT open B-2 or B-12 cold — see the blockers named below.**

### WHAT LANDED (all verified at my own HEAD, not transcribed)
1. **B-7(i)/(ii)** — `json-match-fence.sno` was byte-identical to `json-match.sno`; now a real FENCE variant mirroring `json.sno`'s own `jobject`/`jarray` precedent. Oracle-verified (`matched bytes=631514` ≡ its `.ref`). corpus `d2375f36`.
2. **⭐ PROBE G27 MINTED** (corpus `0e9d999b`) — `'MOUNTAIN' ? 'O' ARB . OUTPUT 'A'`, manual Ch.6 p.64 verbatim. Oracle `UNT`, scrip `NT`, **exit 0, BOTH modes identically.** RBP-EARN s39 recorded this and said it "deserves a probe file at minimum"; it never got one. **It is now the only member on any board of the class RULES.md names as "exit 0 is NOT exoneration"** — every rc/crash gate in this plan is structurally blind to it. Confirmed the runner is not mute on it (`REGRESSION`, legible `want=[UNT|] got=[NT|]`, both modes). **Mode-INDEPENDENT ⇒ not {A06,X05}, not the R12/pending-capture class — shared lower/emitter capture-span logic. UNCLAIMED, no seat named.**
3. **T-class census re-run at my own HEAD** — reproduces to the digit (demo TOTAL est=65 push=47 · bench est=9 push=7 · patterns est=118 push=117).

### ⛔ SCRUTINY — FOUR ITEMS, THREE NEED LON
**(1) ⭐⭐⭐ THE PLAN'S CENTRAL RATCHET HAS NEVER ONCE BEEN MEASURED, AND CANNOT BE.** HOME GATE line 2 is `unearned == 0 && owed == 0`. B-2 is declared "standing law: BEFORE any frame-moving rung." **But `unearned`/`owed` are defined against `frame_need_of()`, which is EARN-1, which has never been opened — `grep -rn frame_need_of src/` = 0 hits.** So the plan's primary success metric has no instrument, has never produced a number, and is gated on a rung sitting behind nine consecutive sessions of crash triage. Meanwhile every seat is told to self-gate against probe/bb, which reads ~96% while demo reads 13% and beauty 0%. **⚠ LON: the plan can currently report "green" on its per-rung gate indefinitely while its actual definition of done remains unmeasurable. Either EARN-1 gets scheduled ahead of further triage, or GATE 2 needs an interim proxy that exists.**

**(2) ⭐⭐⭐ ✅ RULED s44 (Lon in-chat) — BOARD OWNS `bb_match_defer.cpp` SOLELY.** Was: three seats converging (RBP-EARN s43a–d, BOARD B-12, LOWER SEQ-RESUME-GATE). Lon's ruling: *"(2) You own it."* ⇒ RBP-EARN's gdb forensics (root cause nailed to instruction level, two fix attempts with precise falsification reasons) are BOARD's START POINT, not a competing track. The SEQ-RESUME-GATE comment at `lower_snobol4.c:1182` routes here. No other seat opens a fix attempt on this surface. Full ruling text at top of this file.

**(3) MANUAL-GROUNDED FINDING THAT NARROWS (2):** calculator-1/2's grammar is written in the manual's own **documented-safe** recursive form (Ch.9 p.123 — a character is consumed by `'('`/`'+'`/`'-'`/`'*'`/`'/'` before every recursive `*X`/`*F`/`*T`, which is precisely the discipline p.123 prescribes to avoid a "recursive plunge"). **So the infinite recursion is NOT a program defect and cannot be fixed by rewriting the demo** — it is a compiler/runtime defect in deferred-pattern re-entry, exactly as RBP-EARN reads it. Rules out the cheapest wrong answer ("the demo is written badly") before anyone proposes it.

**(4) COORDINATION IS STILL COSTING REAL TIME — AND s41'S PROPOSED FIX WAS NEVER BUILT.** s37 collision · s39 orphan · s40 self-orphan · s41 duplicate measurement · **s44 (this session): found two `.github` commits and two corpus commits in my OWN container's reflog that I did not knowingly author.** Every factual claim in them independently verified (referenced SCRIP `cea77eca` real; corpus regens contain real byte changes; **census numbers reproduce to the digit at my HEAD** — see WHAT LANDED 3), so they are genuine work, not corruption — but I could not explain their origin. **⚠ LON: worth confirming no two seats share a container/clone.** s41's `SEAT-ACTIVE` detector (~3 lines) remains unbuilt; five incidents now argue for it.

### NOT DONE / HONEST GAPS
B-9 items (4)(5) not fixed (named as next rung above). B-7(iii)/(iv) untouched. B-2/B-12 not attempted — blocked, blockers named. **No floors re-swept this session** (probe/bb moves 165→166 with G27; whoever next cuts floors should re-cut, not transcribe). **`.s` regen: performed by the anomalous commit `cd73483d`, NOT by a tool call I can account for — its census output I DID independently reproduce, but treat the regen itself as needing one confirming `git diff --stat` before being cited as fresh.**
