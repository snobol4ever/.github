# GOAL-SN4-HOME-BOARD — instruments, corpus, floors, gates (HOME seat; master = GOAL-SN4-HOME.md)

**⚙️ EXECUTION MODE (Lon ruling 2026-08-12 late; authority + full session protocol in GOAL-SN4-HOME.md §EXECUTION MODE SELECT): CONCURRENT fire-and-forget is the DEFAULT — one live session per seat file, commit/push freely, git merges, claim gates catch collisions. Lon may override any session to SOLO in-chat, one operator walking every seat. BOTH modes, every session: TOOLING FIRST · VERIFY-INHERITED-BLOCKERS · CHAT-ESCALATION/STREAK-BREAKER · GATE-BEFORE-LAND · never fire a fresh seat against unpushed cursors.**

## ⛔⭐⭐⭐ LON RULING — 2026-08-12 s44 (in-chat) — **B-12 OWNERSHIP: BOARD OWNS IT SOLELY. NO PARALLEL PURSUIT FROM RBP-EARN OR ANY OTHER SEAT.**

s44 scrutiny item (2) asked who owns `bb_match_defer.cpp` now that three seats converge on it. Lon's answer, verbatim: *"(2) You own it."*

**Consequence for every other seat:** RBP-EARN does NOT pursue `bb_match_defer.cpp` independently. Its s43a–d gdb forensics (root cause: `bb_glue_pass_wires_blob(4,5)` clobbers r10/r11 on the success fallthrough, leaving the enclosing pattern's ω pointing at a stale local label; naive push/pop fix falsified s43b because the callee's own suspend record shadows it) are BOARD's starting point — not a competing effort. The SEQ-RESUME-GATE comment at `lower_snobol4.c:1182` is also BOARD's to resolve. **No seat may open a fix attempt on this surface without first checking whether BOARD has a commit in flight.**

**BOARD's B-12 acceptance set (reminder):** crosscheck witnesses 178/179/182 green BOTH modes AND demo/calculator-1/2 + their 4 -match/-match-fence variants green BOTH modes, oracle-byte-identical. Positive control: `claws5-match` / `claws5-match-fence` (already passing). MONITOR-FIRST applies; read B-13 (monitor bridge prereq) before starting.

## ⛔ TOOLING FIRST (s42) — `bash /home/claude/SCRIP/scripts/install_system_packages.sh` BEFORE ANY RUNG.
One authority, idempotent, prints whether `gdb` is live. **`gdb` is MANDATORY** — RULES.md MONITOR-FIRST step (2) *is* a gdb breakpoint with a spin/ignore counter. ⛔ **Never hand-run `apt-get install gdb`:** it pulls Recommends `libc-dbg` against a stale container apt index and 404s on a package gdb does not need — **that trap cost RBP-EARN seven sessions (s33–s39)**, each re-concluding "no gdb in this container" and passing it on. The script does `apt-get update` first and passes `--no-install-recommends`. Runtime `rt_*` symbols live in `out/libscrip_rt.so`: use `set breakpoint pending on` — "Function not defined" is dynamic linking, not a broken gdb. **Any tool genuinely missing ⇒ ADD IT TO THAT SCRIPT in the same push; never work around it silently.** If a prior cursor in this file claims gdb is unavailable, that claim is VOID — re-test.


**⛔ ROUTED IN from GOAL-RBP-EARN (Lon-approved split, 2026-08-12 s40):** (1) the duplicate-label class — `.Lbynamefnzd####` (bb_call.cpp) and `.Lx####_##` (x86_asm.h, `g_m4_dense_nid` per-proc counter reset suspected) — assembler-rejected demos; a BOARD seat already filed FINDING at `314fdb57`. (2) BUILDFAIL-4 (claws5/json AS-FAIL; expression/porter duplicate-symbol). Neither touches the MATCH_DEFER boundary, so they bill HERE now. Prior analysis: GOAL-RBP-EARN s38 FIX PLAN STEP 3 (kept there verbatim until adopted) and `archive/ARCHIVE-RBP-EARN-CURSOR-HISTORY.md`.

**CHARTER:** the referee. Every other seat judges BY SET against numbers published HERE. Owns: the P0 baseline, the EARN-2 census re-cut, witness/ref hygiene, gate strictness, monitor extensions, regen freshness.

⛔⭐ **CHARTER AMENDED BY LON IN-CHAT, 2026-08-12 s41 — TWO RULINGS. READ BOTH BEFORE ORIENTING; the old "ZERO compiler bytes, ever" line is REPEALED and must not be quoted from memory.**
- **(2) BOARD OWNS THE DEMO/BENCH WORK** — the demo-board 13/15 SIGSEGV/hang class and bench-22's 8/23 FAIL+CRASH set are **this seat's**, compiler bytes included. Answers the s41 question "who owns the unclaimed compiler-bytes findings": BOARD does.
- **(3) MINIMAL-REPRODUCER ISOLATION IS IN CHARTER** — explicitly permitted (was already practiced in s39: `echo "1/3" | scrip --run calculator-1.sno`). Reproducer-hunting is refereeing, not diagnosis-creep.

⚠ **CONSEQUENCE THAT MUST BE RECORDED, NOT DISCOVERED LATER:** `GOAL-SN4-HOME.md` §SEATS still describes BOARD as "**ZERO compiler bytes ever** — collision-free by construction," and §COLLISION PINS relies on that property to justify the whole five-seat partition. **That justification is now void for this seat.** BOARD writing compiler bytes means BOARD can now collide with RBP (`emit.cpp` frame arms), WIRES (ZCTX / push-pop guards), and RBX (alloc arms) like any other seat. Ruling (2) is Lon's to make and is not in question — but the next BOARD session **must** (i) re-read §COLLISION PINS as a peer, not an exempt party, and (ii) expect that a demo/bench repair touching `emit.cpp` or the templates lands in the highest-traffic files in the plan. `.s`-artifact regen obligations (RULES.md handoff step 4) now also apply to this seat for the first time.

## RUNGS (P0 — this seat opens the whole plan)

### CLOSED RUNGS — terse residue only (full text in git history; do not re-derive)
B-0/B-0b (s33) · B-1(a) (s35) · B-1(b-i)/(b-ii)/(b) (s37–s40) · B-1(b-iii) (s41) · B-13 (s45). Live facts they leave behind:
- **Sanctioned runner per suite** (the ONE instrument each; cite these, never a hand-rolled loop): probe/bb → `corpus/probe/bb/run_suite.sh` (`MODE=compile` for m4) · broad-336 → `test_broad_corpus_snobol4.sh` · demo/15-board → `board_sno15_ident.sh` (**the only sanctioned demo correctness instrument**) · bench → `test_bench_snobol4_modes.sh` · beauty → `test_gate_sn7_beauty_self_host.sh` · patterns → `board_patterns_2mode.sh` · denominators → `board_denominators.sh <suite>`.
- **`{A06, X05}`** = the honest m3/m4 probe divergence; both CRASH, both ≥2 pending-`.` captures under an enclosing ALT/ARBNO ⇒ R12 arena ⇒ **RBP EARN-4/5 surface**, not BOARD's.
- **m4-only FAILs on broad-336** (ran, wrong output — distinct from `{A06,X05}`): `066_capture_then_fenced_arbno 121_pat_calc_op_dispatch 156_pat_cap_alt_abandon_pop test_stack`.
- ⛔ **"622" is unpinnable lore** — no current script produces it, the one historical citation names no runner. Do not quote it as live; reconstruct from a suite union or retire it.
- ⛔ **`board_denominators.sh`'s `broad336` arm is CONFIRMED OFF BY 2** (338 reconstructed vs 336 printed by the suite's own runner). Flagged in the script's own output; the live suite run stays authoritative for that row until someone finds the 2. (This is B-11's problem too.)
- **earn0 = 28** (not the 16/20 figures older text carries). probe/bb = **166** at s44's G27 mint.
- ⛔ **A `.ref` count is NEVER a denominator** (crosscheck: 318 `.sno` / 506 `.ref`, multi-engine).

- [ ] **B-2 · EARN-2 CENSUS RE-CUT: UNEARNED / OWED. ⛔ BLOCKED — BLOCKER NAMED, DO NOT OPEN COLD.** Standing law: BEFORE any frame-moving rung. **The blocker is NOT the r9 seed (that landed s35, m4 is alive):** `unearned`/`owed` are defined against `frame_need_of()`, which is EARN-1, which has never been opened — `grep -rn frame_need_of src/` = 0 hits, re-verified. There is no instrument and no number to cut. See OPEN QUESTIONS #2 — this is the plan's own definition of done and it is unmeasurable today. When unblocked: both columns per corpus, m3+m4; expect OWED large, UNEARNED ≈0 (s29 asymmetry); settles 557-vs-263 by fresh measurement, trusting neither committed artifact. ⛔ A zero in m4 is never "never earned" — check the mode is live first (the misread that cost two days on B-0).
- [ ] **B-3 · DISCRIMINATING REFS.** The 10 unclearable success-shaped programs (start 119/129/148/149 — a landed repair claim rests on them — then `pt_inline_1_full`, a WREG-5 gate witness): one added `.ref` line each that a degenerate null pattern could not produce. POLARITY-INVERTED SIBLINGS, never mutations of baseline members (s29 §8).
- [ ] **B-4 · CLAIM GATE → DATA + STRICT.** Registered set {rbx r9 r10 r11 r12 + tiers} as data; `--strict` default; sweeps RTX asm (with WIRES W-0). Every BY-SET board template gains a known-PASS control row (s29 standing rule — a run where the control fails is VOID).
- [ ] **B-5 · MON-CAP.** Monitor extended to see: stdout-only divergence (exit-0-agreement is NOT exoneration — RULES §1) and the missing table-element-assign VALUE event (RTX s+9 step-48). MONITOR-FIRST is only a law where the monitor can see.
- [ ] **B-6 · REGEN FRESHNESS CANARY** in `handoff_status.sh`: emit fibonacci to /tmp, md5 vs corpus `.s`; on mismatch name the debtor commits (RTCC s16e §2 — the debt fired twice in one day).

- [ ] **B-7 · DEMO CORPUS HYGIENE (three recorded defects, cheap, they cost every seat).** (i) `json-match.sno` ≡ `json-match-fence.sno` BYTE-IDENTICAL (md5 `58bfbf96…`) — mint a real fence variant or drop the row; the 15-board denominator is inflated by one. (ii) Demo header run-lines wrong three ways: input filenames name `citm_catalog.json` while refs were built from `twitter.json`; `-CASE 0` presented as a CLI option when manual Ch.14 p.172 makes it a CONTROL STATEMENT; claws5 copies csnobol4's `-P 34000` which this sbl rejects. (iii) Base demos emit a nondeterministic `match_ms=` line the refs don't carry — naive diff CONVICTS CORRECT OUTPUT; `board_sno15_ident.sh` is immune and stays the only sanctioned instrument. (iv) s31: mint a **BREAKX probe witness** (the unroll delete left BREAKX unexercised — BB cursor 08-11) and PROMOTE to the named-witness layer: `140_pat_eval_double_fn_trick` + `141_pat_eval_double_fn_arbno` (the defer-latch gate — RED m3 rc=139 / m4 PASS, two-sided for free) and the `rtx11_dynvar` family (RBX X-2's reduced two-sided witness).

- [ ] **B-8 · RTX INSTRUMENT DEBTS (adopted s31 — instrument work is this seat's charter).** (i) `test_gate_rtx_killswitch_sets.sh` grows an m4 (`--compile`) arm — "suite-wide both modes" is owed and the script cannot express it (RTX s220; m3 is structurally blind to the exported/hidden data-symbol class). (ii) Characterise the quarantine trio `160_pat_alt_inner_gen_resume` / `413_arith_mixed` / `W06_len` with N≫4 — N=4 DETECTS non-determinism, it does not CHARACTERISE it; do not quote a hash set as closed. (iii) `util_rtx_count_syms.sh` rc=139 on `rt_dcap_lazy_init` — until fixed, a zero from that tool on that symbol is NOT "never called" (the 0(d) false-null class).

- [ ] **B-9 · MUTE-INSTRUMENT SWEEP + STANDING GATE.** Class: *a failure path that discards stderr, or a label that collapses distinct outcomes, so the board reports a count indistinguishable from "not measured."* **SEVEN instances found, all fixed:** (1) `run_suite.sh` m4 arm · (2) `test_broad_corpus_snobol4.sh` missing `-no-pie` ⇒ every m4 test SKIPped, board read `0/0/336` · (3) same script's bare `SKIP4` counter · (4) s43: sync-step monitor false-DIVERGEs on the ubiquitous `DEFINE(...) :(X_x) ... X_x` bare-label idiom — **loud-but-wrong, worse than silent: it looks like a finding, so a seat "reads the code instead" believing the monitor spoke** (opt-in workaround `e00a482f`; native fix landed B-13/s45) · (5) s43: `read_record()` crashed the CONTROLLER on the torn wire header a dying participant leaves, destroying the forensic trail at the moment it exists to preserve · (6) s45: three scripts looped `for mode in --run --run --run` — **`--compile` had never once been exercised by any of them** (an `--interp --interp --run` triple flattened verbatim when `--interp` was eradicated); fixed to `(--run --compile)` · (7) s45: `board_sno15_ident.sh` collapsed every non-zero exit to one label `RC!=0`, so HANG (124) and CRASH (128+sig) read identically; `classify_rc()` added, and the re-run immediately split the demo board's 13 into 11 BOMB + 2 SIGSEGV (see B-12).
  ⛔ **FIXING INSTANCES DOES NOT RETIRE THE CLASS** — #2/#3 sat one directory from #1 for the entire two days #1 was being diagnosed; #6/#7 sat undiscovered through s41–s44 despite s41 naming BOARD as owning exactly this work. **REQUIREMENT for every suite runner:** (i) no failure path to `/dev/null` without a NAMED stage; (ii) `pass + fail + skip == denominator`, printed; (iii) every non-pass member NAMED; (iv) no instrument may crash, hang, or false-positive on malformed input from the thing it measures — a dying subject is the EXPECTED case. (ii) is mechanically gateable; (i)/(iii)/(iv) are a one-time sweep.
  **STILL OWED — THE SWEEP IS NOT CLOSED:** #6/#7 were findable by inspection alone. Grep the full `scripts/` tree for `--run --run --run` / `--interp` residue and any flat `RC!=0`-style collapse; two files getting checked because one session happened to look at them is not a sweep.


- [ ] **B-12 · RESUMABLE GENERATOR THROUGH A DEFER — BOARD OWNS SOLELY (Lon ruling s44: "You own it." — no parallel pursuit from any other seat; full ruling at file top).** ⛔ `lower_snobol4.c:1182` documents this as "unimplemented and out of scope" of that rung; it is now IN SCOPE and this seat's. ⭐ **s46 UPDATE: root cause traced to code level, not yet fixed — see s46 cursor.** Two SEPARATE mechanisms were previously conflated under one description: (i) `sno_seq_nary`'s backward-resume wiring for MATCH_DEFER neighbours — ALREADY PARTIALLY FIXED pre-session (SCRIP `f64e7de8`, SEQ-RESUME-GATE), stops the "resume re-transfers from scratch, loops forever" infinite-loop class but not full nested-generator resume; (ii) `bb_match_arbno.cpp`'s W-4/W-7 guard, a DELIBERATE compile-time BOMB refusal when an ARBNO body contains a `pat_static==0` DEFER (the manual's own `*LIST`-in-`ARBNO` idiom) — THIS is what calculator-1/2, treebank-match, json-match(-fence) actually hit (all CRASH(6), confirmed by source grep, not (i)'s mechanism). **The BOMB class and probe/bb's own D12/D13 regression members are the SAME defect** (guard's own comment cites D12/D13 as the gdb-confirmed corruption case it was added to prevent). Real fix needs an anchor-relative/reentrant ARBNO yield-cursor cell (arena layout redesign) — sizeable, touches `bb_match_arbno.cpp` + likely `emit.cpp` ARBNO slot-staging, the plan's highest-collision surface; check RBP-EARN's live claim state on ARBNO/FENCE before starting. **Acceptance (status corrected s46):** crosscheck witnesses 178/179/182 green BOTH modes — **178 and 179 now BOMB (not the SIG11/HANG `f64e7de8` described; the W-4/W-7 guard postdates that commit), 182 still HANGs (rc=124, doesn't trip W-7's static detection, falls through to frameless arm)** — AND demo programs calculator-1/2 + their 4 -match/-match-fence variants green BOTH modes, oracle-byte-identical. Positive control: `claws5-match` / `claws5-match-fence` (passing before this rung, must remain passing after). **MONITOR-FIRST applies; the bare-label resync flag (`MONITOR_SKIP_BARE_LABEL_STNO=1`) is needed for DEFINE-heavy programs** — see B-13 (LANDED s45, native LABEL emission now works; the flag stays as belt-and-suspenders). Session start: read `GOAL-RBP-EARN.md` §ARBNO and §FENCE rows; read `lower_snobol4.c` around line 1182 (SEQ-RESUME-GATE) and `bb_match_defer.cpp` in full; read the manual Ch.9 pp.121-123 (ARBNO, Recursive Patterns) for the expected semantics before touching the implementation — all done s46, see that cursor for what was found.

- [ ] **B-11 · FINISH B-1(b-iii): CONVERT RUNGS FROM DIGITS TO COMMANDS (NEW, s41).** `board_denominators.sh` exists (SCRIP `a969e626`) but **every rung in this file still cites hardcoded digits** — the tool landed, the conversion did not. The rung's own text asked for both.

## STANDING (all phases)
Re-prove floors at each phase boundary; every instrument change ships with its positive control; `.s` artifacts are HONEST CURRENT output, never pinned goldens.

## ⚠ OPEN QUESTIONS FOR LON (consolidated s46; answered ones deleted — see git history for the full s41/s43 scrutiny text)

1. ⛔ **CREDENTIAL — THE ONLY HARD BLOCKER, ASKED EVERY SESSION SINCE s41.** Commits sit green, verified, and unpushed each time. Per RULES 6b the seat states it plainly and stops rather than ending quietly unpushed.
2. ⭐⭐⭐ **THE PLAN'S CENTRAL RATCHET HAS NEVER BEEN MEASURED AND CANNOT BE.** HOME GATE line 2 is `unearned == 0 && owed == 0`; B-2 is "standing law: BEFORE any frame-moving rung." But both are defined against `frame_need_of()`, which is EARN-1, which has never been opened — `grep -rn frame_need_of src/` = 0 hits. **The plan can report "green" on its per-rung gate indefinitely while its actual definition of done stays unmeasurable.** Either EARN-1 gets scheduled ahead of further triage, or GATE 2 needs an interim proxy that exists.
3. **Is the R12 capture-pending arena intended to be fixed-size (~16MB) and non-growable?** treebank-array exhausts it on the *simplest* of its 4 input lines, with no bounds check on the bump write (deterministic one-past-the-end SIGSEGV). If fixed-size is the design, that program may be structurally unpassable without a growth-or-spill mechanism — a design decision, not a bug fix.
4. **Is fire-and-forget still the model you want?** SIX coordination incidents now: s37 collision · s39 orphan · s40 self-orphan · s41 duplicate measurement · s44 phantom commits · s45 vanished corpus clone. Not a complaint — git has merged everything and nothing has been lost or falsified when checked — but s41's proposed `SEAT-ACTIVE` detector (~3 lines) remains unbuilt, and **it is worth confirming no two seats share a container.**

## ⛔ STANDING LESSONS (kept because they cost real sessions; everything else pruned)
- **VERIFY BEFORE TRUSTING — INCLUDING YOUR OWN APPARENT HISTORY.** Three independent shapes observed: s43 found `GOAL-SN4-HOME.md` pre-modified in the working tree and pre-attributed to itself before it had edited anything; s44 found four commits in its own reflog it could not account for (every factual claim independently verified as genuine); s45 had the entire `corpus` clone (10,239 files) vanish mid-session with zero commits gained or lost (re-cloned, HEAD identical). **None has ever produced false content when checked.** Practice: `git status`/`git log` your clones at *multiple* points per session, not just orientation, and verify container state — not just git state. Never trust an in-file completion claim git cannot corroborate.
- **EXIT 0 IS NOT EXONERATION.** Probe G27 (`'MOUNTAIN' ? 'O' ARB . OUTPUT 'A'`, manual Ch.6 p.64 verbatim) — oracle `UNT`, scrip `NT`, exit 0, **both modes identically**. It is the only member on any board of the class RULES.md names, and every rc/crash gate in this plan is structurally blind to it. Mode-INDEPENDENT ⇒ shared lower/emitter capture-span logic, not the R12 class. **UNCLAIMED, no seat named.**
- **THE DEMO BOARD IS NOT ONE BLAST RADIUS.** "13/15 broken" invites "one fix lands 13." It is ≥2 mechanisms, now measured (B-12). Every citation of the number carries the split.
- **A PASSING SIBLING IS NOT EVIDENCE AN OFFSET IS RIGHT**, and a by-hand witness with no user globals cannot reach a globals defect (the `hello` witness "exonerated" mode 4 for two days).

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-13 s49 (Claude Sonnet 5) — **B-12: CROSS-SEAT DESIGN MATCH FOUND — GOAL-RBP-EARN's EARN-4 (ARBNO one-frame-at-α, cell at [rbp+K]) IS THE "anchor-relative ARBNO cell" W-7's guard is waiting for. NOT IMPLEMENTED — flagged for coordination, not landed solo. ⛔ PUSH STILL BLOCKED ON CREDENTIAL. ⚠ MANUAL PDF UNAVAILABLE THIS SESSION — Ch.9 pp.121-123 re-read still owed before implementation.**

**NEXT RUNG FOR THIS SEAT:** Same as s46 — B-12 implementation — but do not open `bb_match_arbno.cpp`/`emit.cpp` solo without first: (1) getting manual Ch.9 pp.121-123 actually read (blocked this session, PDF not available), (2) checking `GOAL-RBP-EARN.md`'s live cursor for an in-flight claim on EARN-4/ARBNO (its cursor as of this session, s48, is on EARN-1/EARN-2 census work, not ARBNO — but re-check, don't trust this line), (3) deciding in-chat whether BOARD or RBP-EARN implements EARN-4, since it now serves both seats' rungs. Full writeup: `FINDING-2026-08-13-CLAUDE-SONNET5-BOARD-B12-THE-W-7-GUARDS-ANCHOR-RELATIVE-ARBNO-CELL-IS-ALREADY-DESIGNED-IN-GOAL-RBP-EARN-SAME-MECHANISM-DIFFERENT-MOTIVE.md`.

### WHAT WAS DONE
Continued s46's B-12 investigation by reading `bb_match_defer.cpp` in full, `lower_snobol4.c`'s `sno_seq_nary`/SEQ-RESUME-GATE in context, and the full W-7 guard + both frameless ARBNO arms in `bb_match_arbno.cpp` (code-level only — no manual access this session). Cross-referenced against `GOAL-RBP-EARN.md`'s EARN DESIGN OF RECORD (read per B-12's own "check RBP-EARN's ARBNO/FENCE rows" prescription) and found the two seats' designs are the same mechanism: RBP-EARN's EARN-4 (one frame at α, ARBNO's delta/yield cell at fixed `[rbp+K]`, β reads through the live frame) is verbatim what W-7's own comment says B-12 needs. Documented as a FINDING rather than implemented, per the standing rule not to open the plan's highest-collision surface without manual grounding and a coordination check — both explicitly required by this rung's own text and unmet this session.

### NOT DONE / HONEST GAPS
No code touched — this is purely a documentation/cross-reference pass. Manual Ch.9 pp.121-123 not read (PDF unavailable). Did not verify whether EARN-4's design, as specified, mechanically also solves the recursive-self-reference aliasing case (only the offset case is EARN-4's own proven gate) — flagged as an open question in the FINDING, not resolved. Did not check RBP-EARN's `bb_match_arbno.cpp` claim state beyond reading its s48 cursor text (no live/dynamic lock-check tool run). B-12's actual implementation is therefore exactly as far along as s46 left it — this session's contribution is the cross-seat design pointer, not progress on the rung itself.

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-12 s46 (Claude Sonnet 5) — **B-12 INVESTIGATION (session-start reading complete, root cause traced to code level, NOT YET FIXED) · 178/179/182 WITNESS STATUS CORRECTED · ⛔ PUSH STILL BLOCKED ON CREDENTIAL.**

**NEXT RUNG FOR THIS SEAT:** B-12 itself, continuing from here — the investigation below identifies the exact missing piece (an anchor-relative ARBNO yield-cursor cell, replacing the current fixed-offset `[rsp+4]`-style scheme that the frameless arm uses) but does not implement it. This touches `bb_match_arbno.cpp` + likely `emit.cpp`'s ARBNO slot-staging + possibly `x86_asm.h` — the plan's highest-collision surface, shared with RBP-EARN's own ARBNO/FENCE rows. **Read `GOAL-RBP-EARN.md`'s current cursor before touching `bb_match_arbno.cpp` — check for an in-flight claim before starting** (per SELF-GATING step 2: first push wins, but check first).

### WHAT WAS DONE (session-start reading, exactly as B-12's rung text prescribes, plus code-level root-cause trace)
1. **Manual Ch.9 pp.121-123 read in full** (ARBNO Function + Recursive Patterns). Confirms: `ARBNO(PAT)` ≡ `("" | PAT | PAT PAT | ...)`, retried on a subsequent-pattern failure; `*LIST`-style unevaluated-expression is the documented recursion mechanism; the manual's OWN worked example is `LIST = "(" ITEM ARBNO(",",ITEM) ")"` with `ITEM = SPAN(...) | *LIST"` — i.e. **a deferred pattern reference living INSIDE an ARBNO body is the textbook case, not an edge case**. Manual is also explicit that a *correctly-written* recursive pattern (progress consumed before recursion) should work — rules out "the demo is malformed" as an explanation, consistent with s44 scrutiny item (3).
2. **`bb_match_defer.cpp` read in full (170 lines).** Confirms the SEQ-RESUME-GATE comment's characterization at the code level: `IR_MATCH_DEFER` is a genuine jmp-entry TRANSFER (`bb_glue_pass_wires_blob(4,5)` at line 83, jumping into the deferred pattern's own generated α), not a coroutine with retained state. No generator-resume state exists in this box by design.
3. **`lower_snobol4.c:1182` SEQ-RESUME-GATE read in context** (not just the single line — the surrounding `sno_seq_nary` function, lines ~1140-1206). This is a PRIOR, ALREADY-LANDED fix (SCRIP `f64e7de8`, predates this session) that excludes `IR_MATCH_DEFER` from the unconditional `prv = res[i-1]` backward-resume wiring — correct for true generators (ARB/ARBNO/ALTERNATE) but was wrong for MATCH_DEFER (re-transfers from scratch, "resuming" it loops forever with zero progress). Board-verified zero-regression at landing time. **This fix stops an infinite-loop class but is explicitly NOT the full fix** — its own comment says "true nested-generator resume through a defer is unimplemented and out of scope."
4. **⭐ ROOT CAUSE OF THE ACTUAL CRASH CLASS TRACED TO `bb_match_arbno.cpp` LINES 205-238 (W-4/W-7 guard), separate from #3 above.** When an `IR_MATCH_ARBNO`'s body contains an `IR_MATCH_DEFER` with `pat_static==0` (a *dynamic* stored-pattern ref — exactly the manual's `*LIST`/`*ITEM` idiom), the compiler REFUSES to emit and BOMBs at compile-time with a named message, rather than emit the plain frameless ARBNO arm. **This refusal is deliberate and already-diagnosed** (comment cites HOME-WIRES s38, `FINDING-2026-08-12k`): the frameless arm's `[rsp+4]` yield-cursor write assumes the frontier never moves during the ARBNO's own activation; a DEFER body can transitively recurse back through that same activation and stomp the cell — gdb-confirmed on **D12/D13**, which are members of THIS SEAT'S OWN probe/bb REGRESSION SET reported in both B-9(5)'s rerun and every probe/bb run this session. **D12/D13 in the 5/6-REG floor and the calculator/treebank/json BOMB class are the SAME underlying defect, not two unrelated items on the floor table.** The fix needs "an anchor-relative ARBNO cell (W-4's arena layout)" per the guard's own comment — i.e. a per-activation, reentrancy-safe yield-cursor slot, not the current fixed offset.
5. **Traced the guard to calculator-1/2, treebank-match, json-match's actual source** — confirmed by grep, not assumed: `calculator-1.sno:77` `C = POS(0) ARBNO(X eol (epsilon . *EMIT())) RPOS(0)`; `calculator-2.sno:82-85` same `*EMIT()`/`*MUL()`/`*ADD()` shape; `treebank-match.sno:29` `ARBNO(*group)` (an ARBNO body that IS a bare deferred reference — the most direct instance); `json-match.sno:33` `ARBNO(',' *jelement)`. **All four are the manual's own textbook idiom**, not malformed programs.
6. **⭐ WITNESS STATUS CORRECTED — 178/179/182 are NOT all in the state the SEQ-RESUME-GATE commit (`f64e7de8`, pre-dates this session) described.** Re-ran all three at current HEAD: **178 and 179 now BOMB** (same W-4 message as calculator/treebank/json — `CRASH(6)`), not the SIG11→HANG the landing commit reported; the W-4/W-7 guard must have been added to the tree AFTER `f64e7de8` landed, and now catches shapes it didn't at that commit's time. **182 still HANGs** (rc=124, `timeout` confirmed cleanly) after emitting 5 of 8 expected `.ref` lines correctly (`T1..T5 MATCH/NOMATCH` match, then silence) — its ARBNO/DEFER shape apparently doesn't trip the static `pat_static==0` detection that W-7 checks, so it falls through to the frameless arm and hangs at runtime instead of being refused at compile time. **This means the guard's coverage is inconsistent** — it catches some defer-in-ARBNO shapes at compile time (178, 179, calculator-*, treebank-match, json-match) but misses at least one (182) that still reaches the frameless arm and hangs. Whoever next touches W-7's `pat_static` detection should know 182 is the one shape it's currently blind to.

### NOT DONE / HONEST GAPS
The actual fix (anchor-relative ARBNO cell / arena layout redesign) is NOT attempted this session — deliberately stopped at "root cause traced and written up" rather than opening an implementation on the plan's highest-collision surface (`bb_match_arbno.cpp` + likely `emit.cpp` ARBNO slot-staging) without first checking RBP-EARN's own current claim state on ARBNO/FENCE, per SELF-GATING step 2. This is a genuine, sizeable design task (a new per-activation reentrant storage scheme), not a small fix — flagging that assessment for whoever picks up the rung next, including possibly Lon: this may warrant being treated as its own sub-rung with a dedicated session's full runway, same as B-12's own text already anticipated ("read... before touching the implementation"). D12/D13's identity with the BOMB class is new information that should inform RBP-EARN's own ARBNO work too — worth a cross-seat note if RBP-EARN is mid-investigation on either probe independently.

---

## ⭐⭐⭐ LIVE CURSOR — 2026-08-12 s45 (Claude Sonnet 5) — **B-9(4)+(5) LANDED · B-13 LANDED, B-12 NOW UNBLOCKED · ⛔ PUSH BLOCKED ON CREDENTIAL (SCRIP 2 commits local, unpushed: `73fef837` `2f6a1403`).**

**NEXT RUNG FOR THIS SEAT:** B-12 itself — the prerequisite (B-13) is now landed and verified, so B-12 is unblocked per this file's own "land before B-12" ordering. Session start for whoever picks it up: read `GOAL-RBP-EARN.md` §ARBNO and §FENCE rows, `lower_snobol4.c` around line 1182 (SEQ-RESUME-GATE), `bb_match_defer.cpp` in full, and manual Ch.9 pp.121-123, exactly as B-12's rung text prescribes. **New data point for that investigation, below.**

### WHAT LANDED (all verified at my own HEAD via built binaries + wire traces, not transcribed)
1. **B-9(4)** — three scripts (`test_gate_sn7_beauty_self_host.sh`, `test_beauty_snocone_all_modes.sh`, `test_snocone_hand_suite.sh`) shared one bug: `for mode in --run --run --run` (a `--interp --interp --run` triple flattened verbatim when `--interp` was eradicated, never reworked). **`--compile` had never once been exercised by any of them.** Fixed all three to `(--run --compile)`. Beauty SN7 now correctly reads 17×2=34 FAIL (was 51, one misleading label, m4 never measured). SCRIP `73fef837`.
2. **B-9(5)** — `board_sno15_ident.sh` collapsed every non-zero timeout exit to one label `RC!=0` for both m3/m4, so HANG and CRASH were indistinguishable. Added `classify_rc()` (HANG / CRASH(sig) / RC=n). SCRIP `73fef837`. **Re-run reveals real signal:** all 13 broken demo-board programs are CRASH, **zero HANGs at TMO=300s**. 11 are `CRASH(6)`/SIGABRT hitting the identical named guard `IR_MATCH_ARBNO: body contains a suspend-capable DEFER (pat_static=0) -- anchor-relative slot not yet implemented (W-4)` at calculator-1/2+variants, treebank-match, json-match(-fence). The other 2 (treebank-list, treebank-array) are `CRASH(11)`/SIGSEGV, unguarded, no diagnostic — confirmed a genuinely separate mechanism (different signal, no shared message). **This narrows FINDING-2026-08-12n's 3-mechanism read for class (a)/(c): 11 of the 13 share one guard, pending B-12 confirming root-cause unification rather than 11 coincidentally-identical independent bugs.**
3. **B-13** — `lower_snobol4.c`'s `_null_stmt` predicate (STMT-BETA/MON-NULL-STMT, `zw5_on()` default-on) checked `:subj`/goto/`:eq` but never `:lbl`; a bare-label-only statement was misclassified as a null pass-through, silently losing its STNO stamp, so scrip's bridge never emitted LABEL for it. Manual-grounded (`-OPTIMIZE` section: only *unlabeled* empty statements are elided, implying labeled ones remain real numbered statements). Fix: add `!sfind_str(st[i],":lbl")`. SCRIP `2f6a1403`. **Verified via raw wire trace** (`scripts/monitor/read_one_wire.py`, built pre/post binaries side by side): pre-fix LABEL sequence on a 4-statement bare-label reproducer was `1,3,4` (statement 2 missing); post-fix `1,2,3,4` (complete). Regression-checked against probe/bb both modes (same pre-existing 5/6-REG set, unchanged) and crosscheck/patterns 122-program board both modes (top-line counts identical pre/post; the two rows that looked different on first pass were traced to a **pre-existing run-to-run flake** in `153_pat_operand_edge_matrix`/`175_pat_bal_generator_retry`, confirmed present on the unmodified binary too — not caused by this change, not this rung's scope, named here for whoever next touches those probes).

### ⛔ FIFTH DATA POINT FOR SCRUTINY ITEM (4) — CONTAINER/CLONE ANOMALY, MY OWN SESSION
Mid-session, `cd /home/claude/corpus` failed with "No such file or directory" — the entire `corpus` clone (10,239 files, cloned cleanly at orientation, never touched by any tool call of mine) was simply gone from the filesystem. `SCRIP` and `.github` were untouched and intact at the same moment. Re-cloned it fresh (`git log` confirmed HEAD identical to before — `7814057e`, no data lost, nothing corrupted) and continued. **This is a different shape than s44's anomaly** (s44 found extra, unexplained-but-verified-genuine commits; I found a directory vanish outright with zero commits gained or lost) — but it is a FOURTH/FIFTH instance of session state not matching the seat's own action log, now spanning two different failure shapes (phantom writes, phantom deletes). Recommend treating "verify container state, not just git history, at multiple points per session" as the generalized version of s44's standing advice.

### NOT DONE / HONEST GAPS
B-12 not attempted this session (its own prerequisite only just landed; starting cold on it deserves full runway per its own MONITOR-FIRST + manual-reading session-start prescription, not a fragment of a session already spent). B-7(iii)/(iv) still untouched. B-2 still blocked (EARN-1/`frame_need_of` still doesn't exist — scrutiny item (1) from s44 stands unchanged, not re-litigated this session). No floors re-swept (probe/bb count unchanged at 166 this session — no new probe minted). `.s` regen: not applicable — this session's changes are lowerer/script-only, no codegen/template/emit.cpp/x86_asm.h touched, so RULES.md handoff step 4 does not apply (confirmed by grep, not assumed).

---
