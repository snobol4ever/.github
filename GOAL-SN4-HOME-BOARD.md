# GOAL-SN4-HOME-BOARD — instruments, corpus, floors, gates (HOME seat; master = GOAL-SN4-HOME.md)

**CHARTER:** the referee. Every other seat judges BY SET against numbers published HERE. **ZERO compiler bytes, ever** — collision-free by construction. Owns: the P0 baseline, the EARN-2 census re-cut, witness/ref hygiene, gate strictness, monitor extensions, regen freshness.

## RUNGS (P0 — this seat opens the whole plan)
- [x] **B-0 · m4 HARNESS — DIAGNOSED, HARNESS EXONERATED, REPAIR ASSIGNED OUT (s33, FINDING-2026-08-12c).** The premise was false: the harness is correct and was reporting a TRUE result. The absolute-path class is NOT involved (paths resolve; `RTOUT` matches Makefile:343). **Mode 4 is genuinely broken — 2 pass / 2 xfail / 159 REGRESSION of 163 at HEAD, against m3's 157.** ROOT CAUSE: the m4 preamble (`src/driver/scrip.c:1473` SNOBOL4, `:1265` Icon) calls `rt_gva_island`, hands the base to `gva_register` in RSI, and **never seeds r9** — the body then stores every global through `[r9+0]` with r9=0. This is the H2 class documented at `x86_asm.h:292–319`; the static_assert guards the OFFSET, nothing guarded the SEED. Seeding r9 in the generated `.s` recovers **153 of 159** (m4 → 155). ⛔ The by-hand `hello` witness that "exonerated" mode 4 for two days has ZERO user globals and cannot reach the defect (RULES §9). ⛔ `run_suite.sh` 52–55 discards all four stages' stderr — a link failure, a compile failure and a SIGSEGV are indistinguishable (`got =[]`); it is MUTE, not dark. **REPAIR IS COMPILER BYTES ⇒ NOT BOARD'S** — unclaimed, first push wins (RBX or WIRES; the GVA rail is the RTCC surface). Instrument: `scripts/util_board_m4_gva_seed_probe.sh`.
- [x] **B-0b · LIVENESS PREDICATE REPAIRED + THE HARNESS IS NO LONGER MUTE (s33).** (i) `GOAL-SN4-HOME.md` §SELF-GATING 5 rewritten: the m4 liveness check is **"at least one probe PASSES"**, never "returns non-empty" — the old form passed continuously through a two-day total outage (159 failures each print a non-empty `got =[]`). Named cheap signal: `MODE=compile run_suite.sh X12`. Sole authoritative copy; grep confirms no other seat file carries it. (ii) `corpus/probe/bb/run_suite.sh` m4 arm: the silent 4-stage `&&` chain is split into named stages — failures now report `COMPILE` / `EMPTY-ASM` / `LINK` / `RUN` (e.g. `REGRESSION (RUN / CRASH)`), with per-stage stderr retained and dumped under `VERBOSE=1`. `RUNTO` overrides the run timeout. POSITIVE CONTROL: post-edit m3 board is **157 pass · 1 xfail · 5 REG {D12 D13 H31 X01 X10}** — identical set and count to the pre-edit run, so the instrument change is measurement-neutral.
- **B-1 PARTIAL (s33) — DENOMINATORS PINNED BY COUNT, at the s33 hash.** `crosscheck/` = **318 .sno** ✅ xc318 CONFIRMED EXACTLY (506 `.ref` — refs exceed programs, multi-engine; the ref count is NOT the denominator). `probe/bb/probes` = **163** .sno / 163 .ref ✅. `programs/snobol4/demo` = **24 top-level** .sno ✅ (⛔ a recursive `find` returns 31 — subdirs `beauty csn_bridge_b csn_bridge_c label_flow scrip spl_bridge` are NOT demo-board members; this is one of the two circulating "broad" numbers). `benchmarks/snobol4` = **23** .sno ⇒ 22 graded ✅. `programs/snobol4/beauty_suite` = 34 .sno / 17 .ref (the **17 refs** are the 17 drivers of the SIGSEGV floor). ⛔ **`probe/earn0` = 20, NOT 16** — moved this session by RBP's `c91d1adf`; the inventory figure was already stale when minted. STILL OWED: named-witness ~40 pairs; ONE sanctioned runner per suite; the broad-336/622 denominators pinned to a script; m4 columns for every suite (blocked — meaningless until the r9 seed lands, see B-0).
- [x] **B-1(a) · probe/bb FLOORS AT ONE HASH, BOTH MODES — DONE s35 (FINDING-2026-08-12f).** The r9 seed LANDED (RBP `677e8753`, `main_α` bridge) ⇒ m4 unblocked and cut. m4 went 2 pass/159 REG → **157 pass/6 REG**; B-0's "155 pass · 8 residual" prediction confirmed to the digit at the 163 denominator before my own mint moved it to 165. m3 set bit-identical to the s33 floor = the positive control. **The honest m3/m4 divergence is exactly {A06, X05}** — both CRASH, both ≥2 pending-`.` captures under an enclosing ALT/ARBNO; flat controls B01/B02 (minted, oracle-baked) are green in BOTH modes, which falsified my own "node count" reading. Class = pending-capture record surviving a backtrack boundary = R12 arena ⇒ RBP EARN-4/5 surface.
- [x] **B-1(b-i) · BROAD-336 FLOOR, BOTH MODES, PINNED TO A SCRIPT — DONE (Claude Sonnet 5).** Traced "broad-336" to `scripts/test_broad_corpus_snobol4.sh` (crosscheck-with-ref 317 + beauty_suite `*_driver` 17 + 4 named demo tests = **336**, confirmed by the script's own count, not by hand — my manual `find` derivation landed on 338 and was wrong). ⛔ **BUG FOUND AND FIXED:** `compile_mode4()`'s link step was missing `-no-pie` (emitted asm uses absolute relocations incompatible with a PIE link — same requirement as `scrip`'s own build). Every m4 test was silently SKIPping (stderr to `/dev/null`) — 0 pass/0 fail/336 skip, indistinguishable at a glance from "not measured." Same class of mute instrument as B-0b's `run_suite.sh` fix. Fixed (`b9432c0f`, one line, not pushed — no credential). **FLOORS (SCRIP `67e9383c`+fix, corpus current):** m3 `--run` = **260 pass · 76 fail** (336 total). m4 `--compile` = **255 pass · 75 fail · 6 SKIP** (336 total). BY SET: m4 recovers 5 that m3 fails (`ReadWrite_driver TDump_driver XDump_driver stack_driver demo_claws5` — untested whether these are m3-side TIMEOUT artifacts against the same 10s cap a compiled binary clears easily). m4-only fails (4): `066_capture_then_fenced_arbno 121_pat_calc_op_dispatch 156_pat_cap_alt_abandon_pop test_stack` — this corpus's own honest divergence, distinct from probe/bb's `{A06, X05}`. ⛔ **STILL OWED:** the 6 m4 SKIP names — `SKIP4` is a bare counter with no label list, a smaller version of the same muteness; needs the same treatment `FAILURES3/4` already got. "622" chased and NOT found: no current script produces it; the one historical citation (GOAL-SNOBOL4-BB.md, "by-set diff 622 programs") names no runner. Likely needs reconstruction from a union of suites or retirement as unpinnable lore — do not quote it as live.
- [ ] **B-1(b) · REMAINING SUITES AT ONE HASH.** ⛔ m4 is NO LONGER BLOCKED anywhere — cut both columns. Still owed: named-witness ~40 pairs · earn0 **20** · xc318 · demo **24 top-level**/15-board · bench **22-graded** · BEAUTY 17/17 SIGSEGV floor. Name ONE sanctioned runner per suite; pin the broad-336/622 denominators to a script. Inventory (INSTRUMENT MAP in master): probe/bb **163** · named-witness ~40 pairs · earn0 **20** (NOT 16 — RBP moved it this session c91d1adf) · **crosscheck/ = 318 exactly (xc318)** · demo **24 top-level** (NOT 31 — recursive find sweeps subdirs `beauty csn_bridge_* label_flow scrip spl_bridge` which are NOT board members; this is very likely one of the two circulating "broad" numbers) · 15-board · bench **22-graded** (23 total, 1 xfail) · BEAUTY 17/17 drivers SIGSEGV at HEAD — record as m3 floor, it is RBX's meter. Publish verbatim. ⛔ Name ONE sanctioned runner per suite; pin the broad-336/622 denominators to a script; verified clean build (≈258-line log; `rm -rf out/rt_pic` — a relink passes every doorway check). **m4 liveness after seed:** `MODE=compile run_suite.sh X12` (X12 + `zleak_matchbegin_stfh` are the two m4 survivors at s33; if X12 breaks for other reasons use `zleak_matchbegin_stfh` as backup — they survive for structurally different reasons).
- [ ] **B-2 · EARN-2 CENSUS RE-CUT: UNEARNED / OWED.** ⛔ Standing law: BEFORE any frame-moving rung. **m3 column first; m4 column BLOCKED until the r9 seed lands** (a zero in m4 is not "never earned" — it is a dead mode, same misread that cost two days on B-0). Both columns per corpus, m3+m4; expect OWED large, UNEARNED ≈0 (s29 asymmetry). Settles 557-vs-263 by fresh measurement — trust neither committed artifact.
- [ ] **B-3 · DISCRIMINATING REFS.** The 10 unclearable success-shaped programs (start 119/129/148/149 — a landed repair claim rests on them — then `pt_inline_1_full`, a WREG-5 gate witness): one added `.ref` line each that a degenerate null pattern could not produce. POLARITY-INVERTED SIBLINGS, never mutations of baseline members (s29 §8).
- [ ] **B-4 · CLAIM GATE → DATA + STRICT.** Registered set {rbx r9 r10 r11 r12 + tiers} as data; `--strict` default; sweeps RTX asm (with WIRES W-0). Every BY-SET board template gains a known-PASS control row (s29 standing rule — a run where the control fails is VOID).
- [ ] **B-5 · MON-CAP.** Monitor extended to see: stdout-only divergence (exit-0-agreement is NOT exoneration — RULES §1) and the missing table-element-assign VALUE event (RTX s+9 step-48). MONITOR-FIRST is only a law where the monitor can see.
- [ ] **B-6 · REGEN FRESHNESS CANARY** in `handoff_status.sh`: emit fibonacci to /tmp, md5 vs corpus `.s`; on mismatch name the debtor commits (RTCC s16e §2 — the debt fired twice in one day).

- [ ] **B-7 · DEMO CORPUS HYGIENE (three recorded defects, cheap, they cost every seat).** (i) `json-match.sno` ≡ `json-match-fence.sno` BYTE-IDENTICAL (md5 `58bfbf96…`) — mint a real fence variant or drop the row; the 15-board denominator is inflated by one. (ii) Demo header run-lines wrong three ways: input filenames name `citm_catalog.json` while refs were built from `twitter.json`; `-CASE 0` presented as a CLI option when manual Ch.14 p.172 makes it a CONTROL STATEMENT; claws5 copies csnobol4's `-P 34000` which this sbl rejects. (iii) Base demos emit a nondeterministic `match_ms=` line the refs don't carry — naive diff CONVICTS CORRECT OUTPUT; `board_sno15_ident.sh` is immune and stays the only sanctioned instrument. (iv) s31: mint a **BREAKX probe witness** (the unroll delete left BREAKX unexercised — BB cursor 08-11) and PROMOTE to the named-witness layer: `140_pat_eval_double_fn_trick` + `141_pat_eval_double_fn_arbno` (the defer-latch gate — RED m3 rc=139 / m4 PASS, two-sided for free) and the `rtx11_dynvar` family (RBX X-2's reduced two-sided witness).

- [ ] **B-8 · RTX INSTRUMENT DEBTS (adopted s31 — instrument work is this seat's charter).** (i) `test_gate_rtx_killswitch_sets.sh` grows an m4 (`--compile`) arm — "suite-wide both modes" is owed and the script cannot express it (RTX s220; m3 is structurally blind to the exported/hidden data-symbol class). (ii) Characterise the quarantine trio `160_pat_alt_inner_gen_resume` / `413_arith_mixed` / `W06_len` with N≫4 — N=4 DETECTS non-determinism, it does not CHARACTERISE it; do not quote a hash set as closed. (iii) `util_rtx_count_syms.sh` rc=139 on `rt_dcap_lazy_init` — until fixed, a zero from that tool on that symbol is NOT "never called" (the 0(d) false-null class).

## STANDING (all phases)
Re-prove floors at each phase boundary; every instrument change ships with its positive control; `.s` artifacts are HONEST CURRENT output, never pinned goldens.

## ⭐ LIVE CURSOR — 2026-08-12 s35 (Opus 5). **B-1(a) CLOSED: THE m4 BOARD IS ALIVE AND MEASURED.** The r9/GVA seed B-0 assigned out was claimed and landed by RBP (`677e8753`); m4 recovered **2 pass/159 REG → 157 pass/6 REG**, confirming B-0's prediction exactly. ZERO compiler bytes written.

**FLOORS AT THIS HASH** (SCRIP `51934a9f` · corpus `14dc06bd`+B01/B02 · one build, both modes, **probe/bb = 165** — my B01/B02 mint moved it from 163, and I re-cut rather than publish against a denominator my own session invalidated):
- m3 `--run` = **159 pass · 1 xfail · 0 XPASS · 5 REGRESSION** {D12, D13, H31, X01, X10} — set bit-identical to s33 = the positive control; the seed cost m3 nothing.
- m4 `--compile` = **157 pass · 2 xfail · 0 XPASS · 6 REGRESSION** {D12, D13, H31, X01, X05, X10}.
- Both columns sum to 165. ⛔ BY SET, and stale the moment any seat pushes — GATES RE-MEASURE, FILES RECORD.

**THE HONEST m3/m4 DIVERGENCE IS EXACTLY TWO PROBES: {A06, X05}** (m3 PASS / m4 CRASH; deterministic 6/6 with a known-PASS control row green 6/6). Everything else agrees across modes — the shared non-PASS core {D12, D13, H31, X01, X10, fence_probe} is mode-INDEPENDENT and belongs to other seats. **Class = ≥2 pending-`.` capture nodes AND an enclosing backtracking construct (ALT/ARBNO)** — i.e. a pending-capture record that must survive a backtrack boundary (R12 arena, oracle pin W5). **SELF-FALSIFIED:** my first reading was "multiple pending nodes"; minted flat controls **B01/B02** (two pending nodes, no ALT/ARBNO, oracle-baked refs) are green in BOTH modes, so the conjunction is the class, not the count. `$` siblings X03/X04 green both modes — immediate assignment commits on the spot, conditional `.` must be held pending (manual Ch.6 p.62 / Ch.9 p.125). Both divergent `.ref` goldens verified against the manual before being trusted as evidence.

⛔ **BUILD-ORDER RE-CONFIRMED THE HARD WAY:** `install_system_packages.sh` reported the packages were NOT present in this container. A build before that call reproduces the s33 phantom-m4-SEGV class exactly. Run it FIRST, every session; "the tree looks built" is not evidence.

**NEXT:** B-1(b) — remaining suites at one hash, BOTH columns (named-witness ~40 pairs · earn0 20 · xc318 · demo 24/15-board · bench 22-graded · BEAUTY 17/17), one sanctioned runner per suite, broad-336/622 pinned to a script. Then B-2 census re-cut, whose m4 column is ALSO now unblocked.

**UNBLOCKS: LOWER + RBX + WIRES** — your cursors all say "BOARD B-0 still owns the m4 arm — m3-only, UNMEASURED, not green." **That is discharged.** Re-run your boards with `MODE=compile`; liveness predicate `MODE=compile run_suite.sh X12` is green. **UNBLOCKS: RBP** — {A06, X05} is a two-probe reduced witness for the pending-capture-under-backtrack class (EARN-4/EARN-5, R12 arena); B01/B02 are its flat controls, so a repair that reddens them has over-reached.
