# FINDING — 2026-08-22 seat03 — RUNG E-4 (`bb_define.cpp`): r11/ω ERADICATED, r10 UNCHANGED, MONITOR-SAVE LICENSED CLASS (6)

**Rung:** `E-4` = `src/templates/bb_define.cpp` ONLY, from `DISPATCH-R10-R11-ERADICATION.md`'s by-file ladder (HQ s256, 2026-08-22), dispatched to seat03 directly by HQ inbox message the same session. Continues `FINDING-2026-08-22-seat3-free-r10-census-and-eradication.md` (this seat, prior session — the r10 eradication and the earlier partial γ→rcx move on this same file) and `FINDING-2026-08-22-seat4-free-r11-census-and-the-define-activation-shim-survivor.md` (first identified this file's ω/r11 half as a live, unfixed survivor). SCRIP measured pristine (`make pristine` EXIT=0) at `1aaa1d39`, the commit this session's baseline was taken against.

## 0. DISPATCH RECONCILIATION (process note, not a correctness claim)

`s4e_msg.sh next` this session locked QUEUE.tsv's `free-r10` row (rank 0, TASK 1 OF 3 in the old whole-tree by-register ladder). `DISPATCH-R10-R11-ERADICATION.md` §"THE SHAPE CHANGED" retires that split as a work assignment ("the rows stay as the tracking pair") in favor of by-FILE rungs E-1..E-6/A-1..A-4 — and HQ's own inbox message to this seat, read before `next` was called, had already dispatched exactly one of those file rungs directly: **E-4 = `bb_define.cpp`**, with per-site guidance matching this file's two known survivors (the shim's ω half at the old `:471`/`:525`, and the `:138-145` monitor save). QUEUE.tsv itself has not yet been edited to carry E-1..E-6/A-1..A-4 as their own rows — `free-r10`/`free-r11` are still its only two rows for this ladder. This session worked the **file rung (E-4)** as HQ's more specific, more current dispatch, using the `free-r10` claim slot as the tracking record since that is what the queue mechanism offers today. **Recommend to HQ:** replace `QUEUE.tsv`'s `free-r10`/`free-r11` rows with the ten E/A rungs (or re-rank them under the existing rows) so `s4e_msg.sh next` stops handing out the retired whole-tree brief text to whichever seat is topmost.

## 1. STARTING CENSUS (measured fresh this session, before any edit)

`grep -noE '\br1[01][dwb]?\b' src/templates/bb_define.cpp` — 15 site-**lines**, matching `DISPATCH-R10-R11-ERADICATION.md`'s E-4 row (`r10=2 r11=13`) exactly:

| Lines | Mechanism | Function (role) |
|---|---|---|
| 139, 144 | Monitor push-all-8-GPRs around `mon_emit_call_bin` | `bb_define_activate` (role 7) |
| 302, 303 | DEFINE-SITE cell-pointer scratch (`_ab` activation-binding mode) | `bb_define_bind` (role 6) |
| 438 (×2 tokens), 439, 440 | `FRESTORE`'s per-formal-argument restore pointer (SIG arm only) | `bb_define_sr` role 4, `fnsig()` arm |
| 471, 474 (dead), 479 | Shim entry: ω `lea`+`push` | `bb_define_sr` role 4, `fnsig()` arm |
| 525, 528 (dead), 533 | Shim entry: ω `lea`+`push` (mirror) | `bb_define_sr` role 4, TINY-REAL arm |

`test_gate_wreg_claim.sh`'s own independent count: **13 lines / 16 occ**, unlicensed — confirms the census exactly (16 = 14 r11 tokens + 2 r10 tokens; line 438 carries two r11 tokens on one line, which is why occ > lines).

## 2. CLASSIFICATION AND WHAT MOVED (per-site liveness reviewed before any edit; destinations restricted to `rax·rcx·rdx·rsi·rdi` per `DISPATCH-R10-R11-ERADICATION.md`'s binding rule — never r8 (ANCHOR), r9 (GVA), r10/r11 themselves)

**(a) LICENSED, NOT MOVED — lines 139/144, the monitor-hook register save.** HQ's rung-E4 dispatch carried Lon's direct ruling, verbatim in substance: *"under a DEDICATED claim a save/restore PRESERVES the value, it does not clobber it."* This site pushes all eight non-rax caller-saved GPRs before `mon_emit_call_bin` and pops them after, because it runs at a DEFINE box's jmp-entry point and cannot know what an enclosing box's chain left live in any register — it assigns r10/r11 no meaning, it protects whatever meaning something else gave them, so it is not "scratch" and cannot be moved. **Licensed under a new registry class (6), "diagnostic-register preservation"** in `scripts/wreg_claim_registry.txt` (pinned `occ=4`) and re-stated in `ARCH-SNOBOL4-RTX.md` §2. This answers seat3's standing `q-free-r10-zero-scope` question for this file: "zero uses" means zero dedicated/wire uses, not zero textual mentions.

**(b) SCRATCH, MOVED — lines 302/303, `bb_define_bind`'s cell-pointer write.** `r11 → rcx`. Runs immediately after `x86_scan_sync_in_rr()` following the `rt_define_site` call; every candidate destination is dead at that point (the call's own arg registers are already consumed), and `rcx` matches this file's established γ-transit convention. `x86_pair_loop()`, called immediately after, is invoked identically from two OTHER return points in the same function that never touch `rax`/`rcx` at all, so it assumes no incoming register state.

**(c) SCRATCH, MOVED (recompute, not spill) — line 438, `FRESTORE`'s per-formal restore pointer.** `r11 → rax`, **recomputed once per 8-byte half** rather than held across both reads. Liveness at this exact site: `rcx` is the live SIG-block base pointer for the entire `FRESTORE` body (every `SIGQ(d)` reference implicitly reads `[rcx+d]`); `rdx` is the live loop bound (`cmp rdx,i` every iteration); `r8` is the live extension-args base pointer; `rsi`/`rdi` are live **across** `FRESTORE` from its caller when invoked at the γ-landing call site (`mov rdi,GQ(rgx,0); mov rsi,GQ(rgx,8); FRESTORE(80); ...; mov rax,rdi; mov rdx,rsi` — `FRESTORE`'s C++ source is emitted verbatim at both the γ (lid=80) and ω (lid=150) call sites, so anything it must not clobber at lid=80 it must also avoid clobbering at lid=150, even though lid=150 does not need it). That leaves **no free destination among the five**, matching `DISPATCH-R10-R11-ERADICATION.md`'s explicit fallback ("spill to the stack — do not force a rename onto an occupied register"); resolved instead by re-deriving the pointer from `SIGQ(24+8*i)+r8` a second time rather than holding it, which needs no borrowed register and no stack bookkeeping. `rax` is free at both computation points (its only role is the scratch destination for the value each read produces, dead again the instant that value is consumed). Cost: 2 extra instructions in a cold path (per-formal-argument function-return restore), never hot enough to matter, and this file is `-O0` dev / no separate optimizer pass touches emitted asm text.
```
- mov r11, [rcx+24+8i] ; add r11,r8 ; mov rax,[r11+0] ; store ; mov rax,[r11+8] ; store
+ mov rax,[rcx+24+8i] ; add rax,r8 ; mov rax,[rax+0] ; store ; mov rax,[rcx+24+8i] ; add rax,r8 ; mov rax,[rax+8] ; store
```

**(d) LIVE γ/ω CARRIER, MOVED — lines 471/479 (SIG arm) and 525/533 (TINY-REAL arm), the shim's ω transit.** `r11 → rax`. This is the site `FINDING-2026-08-22-seat4-...-shim-survivor.md` first identified as live and high-blast-radius (every `DEFINE`'d-procedure activation) and correctly declined to touch. γ was already on `rcx` (this seat, prior session). Liveness: both arms' per-formal swap-by-map loop (immediately before the `lea`/`push` pair) uses `rax`/`rdx`/`r8` (+`rdi` in the SIG arm) as scratch entirely WITHIN the loop, all dead the instant the loop closes — nothing survives the loop into the `lea`/`push`/`jmp [body_cell]` sequence except values already committed to memory (GVA globals, stack extension slots). No register state is expected to cross the `jmp` into the (separately compiled) procedure body except via the stack, per the law-0a layout. Pushed in the same order as before (ω first, then γ), so `[rsp+0]=γ [rsp+8]=ω` is unchanged.

**(e) DEAD, RENAMED FOR TEXTUAL CONSISTENCY, NOT DELETED — lines 474/528, the `fnrbp()==1` "RBP-FUNCTION WRITER" arm's half of (d).** `static int fnrbp(void) { ...; if (v == 1) v = 2; if (v < 0 || v > 2) v = 2; return v; }` — the value `1` is unconditionally remapped to `2` before the clamp, so `fnrbp()` can only ever return `0` or `2`; **`IF(fnrbp() == 1, ...)` can never be true, and this six-line arm (here and in the RETURN/FRETURN/NRETURN floaters at role 1/2/-1, lines 556-566, which hold no r10/r11) never contributes one byte to any compiled output.** This is not a fresh discovery of the shape — `GOAL-SNOBOL4-100.md`'s own register table already documents the identical pattern for a *different* file: *"bb_save_restore.cpp:161/213 behind `fnrbp()==1`, unreachable — clamp forces 1→2."* `bb_define.cpp` has the same disease and was not yet cross-referenced. **Renamed `r11 → rax` here too (mirroring (d) exactly) rather than deleted**, because deleting six lines of dead code is a scope decision outside a register-migration rung, not because leaving them mattered — since the branch never emits, correctness here is unfalsifiable by construction and the rename exists purely so the file's own textual grep (this rung's DONE-WHEN #1) reads zero. **Flagged for HQ, not actioned:** whether to delete the `fnrbp()==1` arms in `bb_define.cpp` (and cross-check `bb_save_restore.cpp`'s already-known instance, and anywhere else the same clamp shadows a branch) is a dead-code cleanup rung of its own, separate from this one.

## 3. VERIFICATION (own baseline measured this session, before any edit, at `1aaa1d39`; all re-measured after)

- `make pristine` EXIT=0, both before and after.
- **SNOBOL4 crosscheck** (`test_crosscheck_snobol4.sh --run`): **byte-identical before/after** — `--run` 320/321, `--compile` 319/321+1skip, same single standing-red program (`160_pat_alt_inner_gen_resume`, documented non-deterministic-on-pure-C-path witness, unrelated).
- **Icon crosscheck:** 4/4, unchanged. **Snocone crosscheck:** 6/8, unchanged (`beauty_arith` pre-existing, unrelated). Prolog not gated — documented fleet-wide flaky (non-deterministic run-to-run even with edits fully stashed, per `FINDING-2026-08-22-seat3-...`'s §7), and `bb_define.cpp`'s role-0 CALL2BB slice (Prolog's own path through this file) carries zero r10/r11 mentions, confirmed by direct read — out of this rung's reach either way.
- **Passthru combinatorial grid** (`board_passthru_combo.sh both ptc`, the gate seat4 established as the right evidence for this exact shim): **82/82 m3, 82/82 m4**, unchanged before/after — includes the `*_fn2`/`*_fn3` rows that call a `DEFINE`'d procedure to get a deferred pattern.
- **Two live gates:** `test_gate_emit_no_lang.sh` OK, byte-identical output before/after. `test_gate_template_medium_invisible.sh --strict`: unchanged pre-existing FAIL (8 sites, all `xa_flat.cpp`, tracked WIP debt this rung did not touch and did not worsen) — byte-identical output before/after.
- `test_gate_sno_pat_reg.sh --strict`: OK, byte-identical (r10=0 in the SNOBOL pattern family, unchanged).
- `test_gate_wreg_claim.sh`: `bb_define.cpp` moved from **13 lines/16 occ unlicensed** to **LICENSED, pinned occ=4 OK** (the monitor-save only). `x86_asm.h`'s pre-existing `DRIFT` (pinned occ=23, measured 22) is unchanged from this session's own pre-edit baseline — confirmed pre-existing (matches `FINDING-2026-08-22-seat3-...`'s session-2 note), not caused here, not re-pinned (out of scope, different file, different owner).
- `test_gate_wreg_claim_binary.sh`: CLEAN before and after, byte-identical.
- **Direct witness compiles + RUNS** (not just `.s` inspection): `DEFINE('F1()')` with `*F1()` called mid-pattern (seat4's exact shape) — emitted `.s` shows `lea rcx,[F1_γ]; lea rax,[F1_ω]; push rax; push rcx`, zero r10/r11 anywhere in the file; ran correctly in both mode-3 (`--run`) and mode-4 (`--compile`→`gcc -no-pie`→execute), output `START`/`MATCHED` both times. A second witness, `DEFINE('DOUBLE(X)')` with one formal argument (exercises the SIG-arm `FRESTORE` loop item (c) above), emitted the exact recompute-per-half shape shown in §2(c), ran correctly in both modes, output `START`/`abab` both times (confirms `X` round-trips through the restore correctly, not just that it compiles).
- `.s`-artifact regen run in full per RULES.md handoff step 4 (codegen touched): all six scripts in order, zero new/unexpected failures — every EMIT-FAIL/AS-FAIL/REJECTED-BY-AS name matches a name already documented as pre-existing in `FINDING-2026-08-22-seat3-...`'s §7 (Snocone-family emit-fails, `coverage_sno_nodes.s`, Prolog `swi_tests` AS-FAILs, `cal`/`crypt`/`sendmore` REJECTED-BY-AS). `benchmark`/`demo`/`crosscheck`/`programs`/`prolog_bench` auto-committed to `corpus` (`e...`→`3efb3f05`/`a89ff8c5`/`ab7bbba7`, `programs`+`prolog_bench` reported **zero changes** — expected, those cover Icon/Prolog/Rebus, none of which reach this file's role-4/6/7 code). `feature` regen auto-committed to `SCRIP` (`78b1988d`) since its artifacts live under `SCRIP/test/`, not `corpus/`.

## 4. AFTER THIS SESSION

`bb_define.cpp`: **r10 unchanged at 2 (the licensed monitor-save half)**; **r11 down from 13 to 2 (the licensed monitor-save half) — the DEFINE-activation shim's ω carrier, the last unlicensed survivor named in `ARCH-SNOBOL4-RTX.md` §2, is fully off r10/r11.** `ARCH-SNOBOL4-RTX.md` §2 amended in the same commit as required for a register-contract rung: the monitor-save bullet re-stated as class (6) diagnostic-register preservation (ruled, not pending); the DEFINE-activation-shim bullet updated from "r10's half now fixed, r11's half still open" to fully closed, with both witness confirmations cited. `scripts/wreg_claim_registry.txt` gained the `bb_define.cpp occ=4` entry.

## 5. WHAT'S LEFT (for whoever continues this ladder)

Per `DISPATCH-R10-R11-ERADICATION.md`'s rung table: E-1 (`bb_call_fn.cpp`, 86 sites, Prolog fast path — largest remaining single file), E-2 (the nine-file `bb_scan_*`/idiom family, 58 sites, "most mechanical rung on the board"), E-3 (`xa_flat.cpp`, Icon's own mechanism, out of this ladder's language scope per `ARCH-SNOBOL4-RTX.md` §2's own carve-out), E-5 (`emit.cpp`'s frameless-suspend cache + `bb_call_proc_staged.cpp`/`bb_scan_match.cpp`, still the highest-risk survivor per both this ladder and `free-r10`'s own FINDING), E-6 (`x86_asm.h`, encoder internals + the RTCC binary-call-stub), and the four RTX hand-asm rungs A-1..A-4 (228 sites, never previously in scope per seat6). E-4 (this rung) is the only one closed so far. Also open, not this rung's business but flagged in §0 above: QUEUE.tsv's stale `free-r10`/`free-r11` rows, and in §2(e) above: the `fnrbp()==1` dead-code pattern (now confirmed in two files).
