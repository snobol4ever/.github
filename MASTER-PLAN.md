# ⛔⭐⭐⭐⭐ MASTER-PLAN — SEPTEMBER 10: SEVEN LANGUAGES AT 100% ON x86/x64 (Lon 2026-09-03 · QUARTET today, cut for FLEET-16)

**Lon, in-chat to ceo, 2026-09-03 ~18:05 CDT, verbatim:** *"I want to announce on September 10th and I'm wishing to have all 7 languages at 100% on x86/x64 platform. Ensure the plan can scale with 16 fleet workers. Re-divide all the work so it is more parallel. We have 4 HQ's. Let's optimize the writing and walking of these fine-tuned rung ladders in the new test suites. Concentrate also on getting package's test suite working 100% for the announcement. If we work smart we can do this in one week."* · ~18:20: *"Use the ONE LEADERBOARD for all sessions to share."* Scope authority: postoffice `ANNOUNCEMENT.md` (date block). MODE authority: `/home/resources/postoffice/MODE` (QUARTET as this was written) — this plan is walkable under QUARTET by the four HQs and under FLEET-16 by their seats without a rewrite.

## ⛔⭐⭐⭐ THE SNOBOL4 CUT — SNOBOL4 ONLY UNTIL IT WORKS 100% (Lon 2026-09-04 17:37 CDT, in-chat to ceo, verbatim: *"Well, considering we were meant to have SNOBOL4 and Icon working before Prolog. It is time to change priority. Work on SNOBOL4 only until it works 100%."* · 17:38: *"All HQ's and all Fleet workers are on SNOBOL4 only."*; ceo CEO-240)

**⛔⭐ AMENDED 2026-09-06 08:3x CDT — THE ORDER OF WORK IS SNOBOL4, ICON, PROLOG AND PASCAL (Lon 2026-09-06 08:14 CDT, in-chat to ceo, verbatim: *"Add Pascal to the active list of development. Keep Raku on hold."*): every `pascal-` row is un-held (FREE, lane hq_P: master 176/178, fpc_tests 130/181, PAT 284/427, rung09 and the ladder); Raku, Rebus and Snocone rows stay PARKED-LON-HOLD. Same sitting: THE PACKAGE LOCKDOWN (below).**

**⛔⭐ AMENDED AGAIN 2026-09-05 12:5x CDT — THE ORDER OF WORK IS SNOBOL4, ICON AND PROLOG (Lon 2026-09-05 12:4x CDT, in-chat to ceo, verbatim: *"I'm thinking things are happening too slowly, so we are going to FLEET-20. Populate the new folders and ready for FLEET-20 running SNOBOL4, Icon, and Prolog."*; ceo CEO-292).** The 36 Prolog rows (`prolog-*`, `gnu-suite-9-run-fails-triage`, `tests-consolidate-prolog-pz4-blocked-33`) went PARKED-LON-HOLD → FREE at 12:4x (`QUEUE.tsv.bak.unhold-prolog-*` beside the queue); Pascal, Raku, Rebus and Snocone rows (98) stay PARKED-LON-HOLD. MODE is QUINTET since 12:5x (Lon: *"I just started HQ-UNIFY."*); the next flip is FLEET-20 on Lon's word, § THE 20-SEAT CUT below. Every line below that says "SNOBOL4 AND ICON" reads "SNOBOL4, ICON AND PROLOG" from this amendment on.

**⛔⭐ AMENDED 2026-09-05 10:29 CDT — THE ORDER OF WORK IS SNOBOL4 AND ICON, BOTH TO 100% (Lon 2026-09-05 10:14 CDT, in-chat to ceo, verbatim: *"Check the current state of SNOBOL4 tracing in SCRIP and delegate fixes. Well it seems you do not have enough for SNOBOL4 regarding going to FLEET-16. So then we open up development to both Icon and SNOBOL4. We are going to FLEET-16 now."*; ceo CEO-282).** MODE is FLEET-16 since 10:19 on the RE-CUT ranges (01–03 hq_B · 04–06 hq_C · 07–11 hq_P · 12–16 hq_T; 03 and 06 returned from hq_T). hq_B's lane is Icon (the 31 held Icon rows are FREE again) plus snoflake/aisnobol/dotnet; the SNOBOL4 table below stands for hq_C, hq_P and hq_T; Prolog, Pascal, Raku, Rebus and Snocone rows stay PARKED-LON-HOLD (38 free ones parked at 10:2x). MODE line 2 carries no one-language freeze phrase because two languages are open; the hold state is the fence. Every line below that says "SNOBOL4 ONLY" reads "SNOBOL4 AND ICON" from this amendment on.

**MODE FLEET-8 ON SNOBOL4 ONLY since 2026-09-04 18:24 CDT (Lon, verbatim: *"Go to FLEET-8 mode now. SNOBOL4 only."*, after *"Let's go to FLEET-16 mode. SNOBOL4 only until 100%, then Icon to 100%. All 4 HQ's are Claude Opus 5."* at 18:15 and the ceo's measurement of 14 pickable SNOBOL4 rows for 16 seats: hq_B 2 · hq_C 2 · hq_T 4 · hq_P 6 at rank ≤ 1):** two seats per HQ, the first two of each range — 01+02 hq_B · 04+05 hq_C · 07+08 hq_P · 03+06 hq_T (Lon 18:27: *"Just to be clear I started seats 1 through 8."* — seat03 and seat06 re-laned to hq_T, HQ files rewritten with a backup); seats 09–16 are not started. FLEET-16 OPENS when every lane has at least four pickable SNOBOL4 rows (the ceo measures it each tick from QUEUE.tsv: FREE, rank ≤ 1, owner = the lane). THE FOUR DIMENSIONS ON SNOBOL4 (Lon 18:18, verbatim: *"We want completeness and performance and beauty along with the testing."*): hq_C COMPLETENESS (gimpel + every shared-engine class), hq_P PERFORMANCE (the SNOBOL4 B cell against SPITBOL, the granted `g_comm_any_active` store fast path, the `perf-`/`callgrind-` rows) beside csnobol4 and testpgms, hq_B BEAUTY (the SNOBOL4 public face — README § SNOBOL4 grids, demos, the beauty program) beside snoflake/aisnobol/dotnet, hq_T TESTING (xfails, the ladder to the top, instruments, boards). THE ORACLE SWAP is installed (18:19, x64 `c0dc231`; ORACLES.md; the FINDING): the SIGSEGV-on-exit class is gone; refs cut from error-exit shapes before 18:19 are suspect.

**The order of work is SNOBOL4 first, everyone, until every SNOBOL4 industry-standard suite reads 100% (SCORE.md V cells at 17:37: csnobol4_suite 52/118 · gimpel 57/126 · snoflake 0/180 never run-graded · aisnobol 1/2 · dotnet 5/5 = 115/431); then Icon; Prolog after.** MODE is FLEET-16 ON SNOBOL4 ONLY (MODE line 2). Every lane below is a SNOBOL4 lane for the duration; seats pick SNOBOL4 rows in their own HQ's lane; class rows minted under a suite umbrella carry that suite's HQ as owner so its seats pick them; every non-SNOBOL4 claim is PARKED by its holder with the ledger line `priority: SNOBOL4 only (Lon 2026-09-04 17:37)` (push first) — exceptions: a cross-language instrument a SNOBOL4 board depends on, and the shared-oracle crash row. THERE IS NO XFAIL: the 184 snobol4 xfails are 184 reds.

| HQ (seats) | SNOBOL4 suite / cell | what 100% means there | first step |
|---|---|---|---|
| **hq_P** (07–11) | csnobol4_suite 52/118 → 118; **SPITBOL's own testpgms 1–4 → 4/4 both modes** (Lon 2026-09-04 17:57 CDT; umbrella `snobol4-spitbol-testpgms-four-programs-to-100-percent-both-modes`, RED at mint: test1 red, tests 2–4 ungradable while the oracle crashes); the shared oracle (`snobol4-oracle-sbl-bf-sigsegv-…`); the B cell | every csnobol4 program matches `csnobol4/snobol4` in both modes; the oracle never SIGSEGVs mid-cut | the 25 FAIL · 40 REJECT · 1 CRASH censused by class (umbrella claimed by hq_P), rank-1 class rows for seats 07–11 |
| **hq_C** (04–06) | gimpel 57/126 → 126; every shared-engine class any lane files (pattern engine, runtime, GC) | every gimpel program matches SPITBOL `-bf`; no shared-engine class row open | `snobol4-gimpel-suite-126-to-100-percent-by-class` (re-laned to hq_C 17:4x): census by class, rank-1 rows for seats 04–06 |
| **hq_B** (01–03) | snoflake 0/180 (NEVER run-graded) · aisnobol 1/2 · dotnet 5/5 | the three suites run-graded against SPITBOL and at 100% | run-grade snoflake FIRST (`snobol4-snoflake-suite-180-to-100-percent-by-class`, `snoflake-suite-scrip-only-gap`, `snobol4-aisnobol-and-dotnet-suites-to-100-percent`, all re-laned to hq_B 17:4x), then classes |
| **hq_T** (12–16) | the SNOBOL4 instruments: the 184 xfails (61 `ALL.csv` · 122 `ALL.xfail` · 1 marker), the ladder to the green-book top, every SNOBOL4 runner refusing rc=2 on oracle rc=139, the SCORE rows; **vendor + runner + V cell for SPITBOL's testpgms** (`snobol4-spitbol-testpgms-vendored-as-a-package-suite-with-a-runner-and-a-score-cell`) | zero xfails; the ladder census complete to the top rung; no board can read a truncated ref | `snobol4-ladder-top-rung-census-and-rungs-10-to-top`, `snobol4-ladder-feature-combinations-after-isolation`, `snobol4-parser-fixtures-and-port-trace-gate-…` (re-laned 17:4x); the xfail umbrella is hq_P's to hand over by LANE REVIEW |

**THE ORDER OF ATTACK (2026-09-04 20:51 CDT): GOAL-SNOBOL4-100 § THE ORDER OF ATTACK — wave 1 the shared-engine rows HQ-held (the capture mechanism, the m4-gate regression, RETURN after a code-object transfer, the SETEXIT mechanism), wave 2 the long tail in parallel on seats (csnobol4 classes, snoflake's 26, the xfail classes), wave 3 the mode-4 compile failures, wave 4 error semantics against SPITBOL, wave 5 the ~40 programs that need a ruling (lowercase `end`, oracle-unscorable) named beside the cell.**


## THE GRID IS SCORE.md (one leaderboard; nothing in this file restates a number)

⛔⭐⭐⭐ **100% MEANS 100% OF THE INDUSTRY-STANDARD LANGUAGE (Lon 2026-09-03 20:45 CDT, in-chat to ceo, verbatim, correcting the ceo's reading of the Raku 89%: *"Well then you misunderstood what 100% means. It means 100% of the industry standard language."*).** The score of a language is its performance against the industry standard — the defining document and the public conformance suite that grades it — NEVER against our own master suite, smoke or ladder. Our masters, smokes and ladders are INSTRUMENTS on the way (they find the class defects fast); they are not the denominator. Where a public conformance suite exists, 100% = every test of that suite passes, run-graded, against its oracle, over its printed population; where none exists, 100% = the language's defining document censused into the ladder (`LADDER.tsv`, THE LADDER RECIPE) and the ladder green to its top. The headline `PROGRESS` line (hq_T's `util_score_row.py progress`) is computed on THIS basis; a percentage over our own suites is a second, labelled line, never the headline.

| Language | THE INDUSTRY STANDARD (defining document · reference implementation) | The conformance suite that grades it (population · vendored?) | Where there is no public suite |
|---|---|---|---|
| SNOBOL4 | the green book (Griswold · Poage · Polonsky, *The SNOBOL4 Programming Language*) · SPITBOL / CSNOBOL4 | csnobol4_suite 124 · gimpel 289 · snoflake 180 · aisnobol 8 · dotnet 14 — all vendored under `corpus/packages/snobol4/`, each graded by its OWN oracle | the green-book construct census is the ladder top |
| Icon | *The Icon Programming Language* (Griswold & Griswold) · Icon v9.5 (`icont`/`iconx`) | arizona 124 files (89 graded) · ipl 851 · jcon_tests 91 (81 graded) — vendored under `corpus/packages/icon/` | the Icon book's construct census is the ladder top |
| Prolog | ISO/IEC 13211-1 · SWI-Prolog and GNU Prolog | swi_tests 249 files (114 graded) · gnu_prolog 62 — vendored; ⛔ the ISO conformance suite itself (the INRIA suite, `inriasuite.pl`) is NOT vendored — hq_T vendors it, it is THE denominator | — |
| Pascal | ISO 7185 · Free Pascal `-Miso` (rival) and Pascal-P5 (reference sources, vendored 440 files) | fpc_tests 181 — vendored; ⛔ the ISO 7185 validation suite (PAT / Pascal Validation Suite) is NOT vendored — hq_T vendors it, it is THE denominator | the ISO 7185 § 6 census is the ladder top |
| Raku | the Raku language specification · Rakudo | roast — THE official spec test suite (`refs/roast`, per-root, gitignored; seat14 counts ~986 files), graded RUN not compile — ⛔ it is not in `corpus/packages/` and it is THE denominator | — |
| Snocone | Koenig's Snocone manual · the reference Snocone preprocessor | none public | the manual's construct census IS the standard; the ladder to its top is the score |
| Rebus | Griswold's Rebus report (a SNOBOL4/Icon hybrid) · the reference Rebus preprocessor | none public | the report's construct census IS the standard; the ladder to its top is the score |

The cells (SUPERSEDED as the DENOMINATOR by the block above; kept as the instruments): the 100% grid is `SCORE.md` § THE SEPTEMBER 10 GRID: seven languages × four measured cells — **M** master suite FAIL=0 both modes · **L** construct ladder green to its top rung · **V** vendor/package suites 100% against their own oracle · **B** benchmarks, the README grid on the two-number three-angle basis vs the rival the public runs (Lon 2026-09-03 ~18:24: *"100% coverage of features, and 100% package test suites and benchmarks"*; hq_P owns B for all seven, I26) — plus the cross-language cells. A cell is 100% only over its PRINTED denominator with every xfail reasoned in writing (the xfail count is published beside the 100%). The runner that measures a cell rewrites it (FACT RULE); the ceo reads the grid to say what is left and never re-runs a suite to answer.

## THE LANES (EIGHT HQs since 2026-09-05 15:0x CDT — Lon: *"8 HQ's?"* · *"I just created S, I, and R root folders."*; the ceo's cut, CEO-293: each HQ owns ONE cure surface; hq_S HQ-SUSTAIN the SNOBOL4 runtime, hq_I HQ-INSPECT the Icon suites, hq_R HQ-RESOLVE the Prolog builtins — GOAL-HQ-{SUSTAIN,INSPECT,RESOLVE}.md; ⛔ THE ROUTING RULE CHANGES WITH THE CUT: a shared-node class is AUTHORED by the HQ whose language exposed it and CO-SIGNED by hq_U, which grades the other frontends' boards — hq_U reviews, it does not queue. Five HQs from 12:20 — hq_U, HQ-UNIFY, owns the shared machine and every cross-language regression, GOAL-HQ-UNIFY.md; each language HQ owns whole languages end to end so every cell has exactly one owner — under QUARTET the HQ measures AND cures; under FLEET-16 its Sonnet seats measure, walk and witness, and the HQ, being Opus, cures the classes they file)

⛔⭐⭐⭐ **PACKAGES FIRST (Lon 2026-09-04, in-chat to ceo, verbatim: *"do not consider our stupid tests as part of the percentage; only the packages of industry standard ones count for a percentage"* · *"Well. Let's change the performance by fixing those tests. Are there tasks to complete all those."* · *"Put gnu_prolog test suite on the list. Put snoflake test suite on the list."* · *"If GNU Prolog ships with Prolog source; that is the test suite I am talking about. You make the programs run, you measure the output, make a REF file, and place it into a ONE-LINER or a MULTI-LINER Python test harness."*):** THE DEFINITION: every vendored package's SHIPPED PROGRAMS are its test suite — run each against the package's oracle, cut a `.ref`, place the pair in the one-liner or multi-liner Python harness format; a shipped expected output is the ref where one exists. THE PERCENTAGE is the V column over the shipped populations and nothing else (SCRIP `5d2c9a3b2`): a package not yet run-graded counts as ZERO of its population until its programs run; our master, AST fixtures and ladders are printed as ours, never counted; a language with no package (snocone, rebus) has no percent. THE LIST — fourteen packages, one umbrella row each at the head of `QUEUE.tsv`, rank 0, computed DONE-WHENs: hq_P csnobol4_suite 118/124 · snoflake 180 · gimpel 289 · aisnobol 8 · dotnet 14 · fpc_tests 181 · PAT 429; hq_B arizona 124 · jcon_tests 91 · ipl 851; hq_C swi_tests 249 · INRIA 445 · gnu_prolog 62 (+29 ExamplesPl); hq_T roast 1464 (the ceo holds the cure row). THE INSTRUMENT (hq_T rank 0): `every-vendored-package-absorbed-into-the-one-liner-or-multi-liner-python-harness-with-oracle-cut-refs` — one container per package, graded through `corpus_suite_harness.py`, the package runner a thin face, smallest packages first. Every class row of those suites sits at rank 1; the isolation-ladder rows sit behind; a suite seat's row IS its umbrella; every landing is judged by whether a package number went up and rewrites its V cell through `util_score_row.py --column vendor --suite <name>`. THE LINE UNDER THIS DEFINITION at 2026-09-04 13:43 CDT: sno 26% · icn 8% · pl 53% · raku 0% · pas 20% · ALL 18% over five. Measured record before this order (CEO-222): in seven days only fpc_tests (+9) and arizona (+3) moved, and no package was on the board before 2026-09-03 16:00.

⛔⭐⭐ **ALL OUR TESTING IS THE ONE-LINER / MULTI-LINER HARNESS (Lon 2026-09-04, in-chat to ceo, verbatim: *"For every source from every language that we've generated or that existed in corpus which is not part of any third-party package, if you can get it to run and it has output then add that to the test suite list. I.e. All our testing should be ONE-LINER and MULTI-LINER Python test suite. Oh yeah, CEO, have you ensured that all test sources have been moved into the ONE-LINER and MULTI-LINER? Now, keep the packages in their existing form and get them working and generate REF files and also add those to our test suites."*):** ⛔ `corpus/programs/*` is EXCLUDED (Lon 2026-09-04: *"Go ahead and exclude programs/* folders."* — the 08-27 parser-only ruling on it stands). THE ANSWER WAS NO — `util_unabsorbed_census.py` (SCRIP `e0495afb2`, programs/ excluded at `53a843f67`) counts 898 of our own sources outside the seven masters (999 before the exclusion) (374 loose pairs with a ref, 167 parser/coverage fixtures, 458 loose sources with no ref; 24 modules and 71 excluded-by-name are accounted) against 4,302 master entries: snobol4 171 · icon 224 · prolog 188 · raku 121 · snocone 78 · pascal 64 · rebus 52. THE ROWS (rank 0, one per language in its lane): `<lang>-every-non-package-source-that-runs-with-output-absorbed-into-the-master-with-oracle-refs` — DONE when the census reads zero owed for the language (absorbed, or named in `ALL.excluded.txt` with the reason it cannot run with output) and the master stays FAIL=0 both modes; and hq_T's `master-builder-absorbs-demos-benchmarks-programs-and-loose-pairs-additively` (demos, benchmarks and programs absorbed ADDITIVELY at a small count with the recipe in `ALL.csv`, refs cut from the oracle, parser fixtures as `modes=ast`, exclusions by name). PACKAGES stay in their existing form; refs are generated from each package's oracle and the harness container is ADDED beside the shipped files (row `every-vendored-package-absorbed-…`).


| lane | languages / cells | why this split | FLEET-16 seats (postoffice `<seat>/HQ` — ⭐ CORRECTED 2026-09-04 by seat07: this column still read the pre-19:30 by-function numbers; the re-lane (§ THE 16-SEAT CUT, "RE-LANED 2026-09-03 ~19:30 into contiguous ranges") moved the NUMBERS, not the function, and the live `HQ` files were already right — this table's own copy was the only thing stale, and disagreeing with the file that governs is worse than saying nothing, per this project's own repeated finding about this exact digest-decay shape) |
|---|---|---|---|
| **hq_C** correctness | PROLOG: M 404 · L rungs 10–12 then the § E top · V swi_tests + gnu_prolog — the SHARED-ENGINE classes it carried until 2026-09-05 (frozen-model changes, shared boxes, the scan+while+suspend resume-at-α) MOVED to hq_U, GOAL-HQ-UNIFY.md; hq_C keeps Prolog whole | the deepest hole (271/404, 31 parked umbrella rows) and the one lane that changes shared nodes | 08 · 09 under FLEET-12 (05–08 under FLEET-20) — builtins, error terms, streams and the vendor suites MOVE to hq_R at the FLEET-12 flip |
| **hq_U** unify (opened 2026-09-05, Lon: *"What's a good name for a fifth HQ?"* · *"The /home/claude_U folder is ready to populate."*) | THE SHARED ENGINE: `src/templates/bb/` · `x86_asm.h` · `src/templates/xa/` · `src/emitter/` · `src/optimizer/` · `src/driver/` · `src/runtime/{core,rt,rtx}/` · `src/ir/` (the three zetas) — plus EVERY CROSS-LANGUAGE REGRESSION from the bisect on (2026-09-05: the HOST argv staging that burned Icon's list serial 1; the CAP_NEST rsp-relative capture) — plus THE A–Z TEMPLATE CLEANUP at rank 1 (Lon 2026-09-05, verbatim: *"We want all the bb_*.cpp files to follow the struct rules of construction so that all that code can easily be translated manually into Snocone unevaluated expressions."* · *"ensure old unused bb_*.cpp files are removed"*; row `bb-fixup-az-cleanup`, 151 files / 65 dirty / GRAND 1446 at opening) | the machine is ONE engine under three syntaxes; both regressions found on opening day crossed a frontend boundary, and the HQ that owned the engine also owned the largest language rebuild | 11 under FLEET-12 (17–20 under FLEET-20) — hq_U keeps ir, emitter, optimizer, encoders, driver, runtime core/rt, the boxes two or more frontends reach, every cross-language regression and the A–Z cleanup; it CO-SIGNS shared landings authored by the exposing HQ |
| **hq_S** sustain (opened 2026-09-05 15:0x) | THE SNOBOL4 RUNTIME: builtins · I/O and file association · keywords · error handling and limits (SETEXIT, &ERRLIMIT, &STLIMIT) · tracing · V csnobol4_suite 119 + the SPITBOL testpgms — every SNOBOL4 class whose cure lives in `src/runtime/{builtins,rt,core}`, `rtx_match.s`, `rtx_str.s` | the SNOBOL4 package score (60/119) is a runtime score, and hq_P was carrying frontend, patterns, runtime and the B cell alone | 03 · 04 under FLEET-12 |
| **hq_I** inspect (opened 2026-09-05 15:0x) | THE ICON SUITES: V arizona 89 · jcon_tests 81 · ipl 851 · co-expressions (rung 38) · the Icon runtime files `rtx_icn*.s` — every Icon class a vendored suite exposes | Icon's package score (8% on the progress line) is where the gap is; hq_B keeps the frontend, generators, the master and the public face | 07 under FLEET-12 |
| **hq_R** resolve (opened 2026-09-05 15:0x) | THE PROLOG BUILTINS: ISO 13211-1 § 8 builtins · § 7.12 error terms · streams and term I/O · V INRIA 445 + swi_tests + gnu_prolog — every Prolog class whose cure is a builtin or the runtime | INRIA is THE Prolog denominator and its reds are builtin-shaped; hq_C keeps the frontend, clause DB, cut and control | 10 under FLEET-12 |
| **hq_B** beautify / Icon | ICON: M red-by-2 + the STRICT suite · L · V arizona 89 · jcon_tests 81 · ipl 851 · jcon demo 4/4 — plus the public face: README, demos, the polyglot board | Icon is the second-largest gap and all presentation is hq_B's | 05 · 06 under FLEET-12 (01–04 under FLEET-20) — the vendored suites and `rtx_icn*.s` MOVE to hq_I at the flip |
| **hq_P** speed | SNOBOL4: V csnobol4_suite 118 + gimpel/snoflake/aisnobol/dotnet, the 70 xfails reasoned · SNOCONE: M 273 re-measured and cured · PASCAL: M 5 reds · V fpc_tests 59 + the ISO 7185 PAT suite · rung09 — plus the **B cell for all seven**: every README grid on the two-number three-angle basis vs the rival by D6 (I26; Rebus needs a benchmark set first) | SNOBOL4/Snocone/Pascal are already hq_P's perf languages; their remaining work is suites, not engine | 01 · 02 under FLEET-12 (09–12 under FLEET-20) — runtime, builtins, I/O, keywords, tracing and the csnobol4 suite MOVE to hq_S at the flip; hq_P keeps the parser, the lowerer, the 38 match/scan boxes and the B cell |
| **hq_T** test suites | THE STANDARD: the three missing ladders written (icon, snocone, rebus), the top-rung census for all seven, the four missing package gates (snoflake, gimpel, ipl, aisnobol+dotnet), `board_packages.sh` into `make test`, `util_score_row.py`, the walker over all seven ladders, the xfail-reason gate · RAKU: M 139 (42 ast_fail) · V roast graded · REBUS: M 48 re-measured and cured · L | instruments are one body for seven languages; Raku and Rebus are the two lanes with no engine owner | 12 under FLEET-12 (13–16 under FLEET-20) |

A ladder written by hq_T is CURED by the language's lane owner: the writer files the red rung with its witness, the owner cures it. A red found in any suite is a class row in the owner's lane; a class that lives in a shared node is routed to hq_U (hq_C until 2026-09-05 12:20) with the witness and graded on every frontend that lowers to the node (RULES § SHARED-NODE VERDICT SCOPE). Prolog's ladders stay T, C, P below; hq_C's current rungs are § E rungs 10–12 and the master/swi/gnu census rows.

## ⛔⭐⭐⭐ THE PACKAGE LOCKDOWN — EVERY SHIPPED PROGRAM GRADED OR NAMED UNGRADABLE (Lon 2026-09-06 08:2x CDT, in-chat to ceo, verbatim: *"Fix the never graded business. Let's lock down our package testing and make it complete."*; routed by the ceo 2026-09-06 08:13 CDT, CEO-303)

**THE ORDER:** every program a vendored package SHIPS is GRADED against the package's own oracle, or is NAMED UNGRADABLE with the oracle's own reason — never silently dropped, never counted as passed. The denominator of a package percent is the SHIPPED population (the POPULATION LAW, 2026-09-04) and the ungraded remainder prints beside it (Lon 2026-09-05) until it reads zero. "Never graded" is a debt with an owner, not a category.

**THE INVENTORY AT THE ORDER (ceo file census 2026-09-06 08:2x, corpus `b940d0be0`; the runners' printed counts supersede it the moment they exist):** Icon — arizona 124 shipped / 89 refs · jcon 91 / 83 · ipl 851 / 60 run-graded (836 of 1066 never graded, hq_T 09-05). Prolog — swi_tests 249 / 9 (and those 9 cut through our own shim the oracle cannot load — 51 of 57 expected verdicts NO TESTS RAN: 0 valid, ungraded = 249; hq_R 2026-09-06 08:2x from hq_C's finding) · gnu_prolog 62 self-checking, published as counts · INRIA 445 graded (functor-bis a negative control). SNOBOL4 — csnobol4_suite 133 / 122 · gimpel 290 files / 127 refs · snoflake 180 self-checking (173 graded) · aisnobol 9 / 1 · dotnet 14 / 0 · testpgms 8 (4 scored, 4 named UNSCORED by the oracle's refusal). Pascal — fpc_tests 181 / 181 · PAT 429 (self-checking). Raku — roast ~986 `.t`, 4 run-graded.

**THE ROWS (rank 0, one per lane, minted 2026-09-06 with one criterion: the runner's own inventory line reads `ungraded=0`; REFUSES rc=2 until the instrument lands):** the instrument first — hq_T `every-package-runner-prints-shipped-graded-ungraded-and-ungradable-and-the-leaderboard-carries-the-inventory` (one line, one shape, `shipped=N graded=N ungraded=N ungradable=N`, computed from the package directory, `UNGRADED.tsv` and `UNGRADABLE.tsv` beside each package, the four numbers summing, carried into the cell and the PROGRESS line, a gate over all runners) · hq_I `icon-every-shipped-program-in-arizona-jcon-and-ipl-graded-against-icont-or-named-ungradable` · hq_R `prolog-every-shipped-program-in-swi-tests-and-gnu-prolog-graded-against-swipl-or-gprolog-or-named-ungradable` · hq_S `snobol4-every-shipped-program-in-csnobol4-suite-and-spitbol-testpgms-graded-or-named-ungradable` · hq_P `snobol4-every-shipped-program-in-gimpel-snoflake-aisnobol-and-dotnet-graded-against-sbl-or-named-ungradable` · hq_P `pascal-every-shipped-program-in-fpc-tests-and-pat-graded-against-fpc-or-named-ungradable` (ACTIVE: Lon 2026-09-06 08:14 CDT, verbatim *"Add Pascal to the active list of development. Keep Raku on hold."* — THE ORDER OF WORK IS SNOBOL4, ICON, PROLOG AND PASCAL; every `pascal-` row un-held; Pascal is hq_P's lane under THE LANES) · hq_T `raku-every-roast-file-run-graded-against-rakudo-or-named-ungradable` (PARKED-LON-HOLD: Raku, Rebus and Snocone stay held).

**THE RULES OF THE WORK:** (1) refs are cut from the ORACLE — stdin and argv sidecars in the `c178cba44` shape, a self-checking suite by its own verdict, never from SCRIP's own output, never through our own shims (hq_C's swi finding: a suite stuck at zero cannot tell you its oracle is broken). (2) A batch is preceded by the census that names WHY each program is ungraded (no ref, needs input, oracle refuses it, container, library file), and the ungradable ones are named with the reason and stay in the denominator. (3) A program the oracle itself refuses is UNGRADABLE, not a SCRIP defect and not a pass. (4) Closing a row on the first passing witness is forbidden (hq_I, 09-05); a DONE-WHEN that grades rc alone is forbidden (hq_I, 09-05). (5) THE ORDER OF WORK stands: SNOBOL4, Icon, Prolog; the seats walk their lane's row by class, the HQs cure. (6) DONE means every package prints `ungraded=0`, and the PROGRESS line prints inventory 0 (or `N ungradable, named`) for every language with a public suite.

## ⛔ FLEET-16 READINESS — a computed check; the ceo reports READY to Lon when every line reads true (Lon ~18:24: *"Let me know when you have locked down a good plan and we are ready to start up all 16 fleet workers."*)

| # | line | command (from `/home/claude`) | must read | reading 2026-09-03 18:24 |
|---|---|---|---|---|
| 1 | every ⭐ rung on every ladder has a baton with a runnable DONE-WHEN | `python3 SCRIP/scripts/util_ladder_walk.py --quiet \| grep -c 'V1 RUNG-WITHOUT-ROW'` and the same for `V5` | 0 and 0 | ✅ 18:49: V1 = 0 · V5 = 0 — hq_T minted its ten; the ceo minted the last eight under CEO mode (Lon: *"tag you are it"*), each with a runnable DONE-WHEN proven to fail today (three live, five by clause on today-shaped and clean-shaped output) · ✅ 2026-09-04 11:52 CDT: V1 = 0 · V5 = 0 (the fifteen CEO-209 rows placed in the ladder tables this pass) |
| 2 | sixteen seats can each step onto a rung (V1 = 0 alone supplies ~40 rungs; this line guards against a queue where off-ladder rows sit ahead of them) | `awk -F'\t' '!/^#/ && NF>=4 && $1<=1 && $4=="FREE"' /home/resources/postoffice/QUEUE.tsv \| wc -l` with walker V4 = 0 (nothing off-ladder ahead of a rung) across ≥ 3 lanes | ≥ 16 and V4 = 0 | ✅ 18:49: 25 FREE on-ladder rows at rank ≤ 1, V4 = 0 · 2026-09-04 11:52 CDT: 62 FREE rows at rank ≤ 1 across four lanes (hq_B 7 · hq_C 23 · hq_P 19 · hq_T 13); V4 re-read after this pass placed the twenty inversions (the CEO-209 rows plus three strays) on their ladders |
| 3 | the seat map matches THE 16-SEAT CUT | `for s in 01 02 03; do cat /home/resources/postoffice/seat$s/HQ; done` (hq_B) and likewise 04 05 06 (hq_C), 07 08 09 10 11 (hq_P), 12 13 14 15 16 (hq_T) | all match | re-laned by the ceo 2026-09-04 11:52 CDT to the RE-CUT ranges (01–03 hq_B · 04–06 hq_C · 07–11 hq_P · 12–16 hq_T) |
| 4 | every seat digest carries the 2026-09-03 proclamation (pristine lifted · QUARTET/FLEET series · `send` to the HQ-file target) | `grep -L 'THE PRISTINE BUILD IS LOOSENED' /home/claude??/CLAUDE.md /home/claude_?/CLAUDE.md \| wc -l` | 0 | ✅ 18:35: Lon ran `propagate_pristine_and_quartet.py` — 20 of 20 carry the proclamation, 0 say `send hq`, 20 carry the QUARTET line (10 digests keep a superseded pristine sentence below the proclamation, which names itself as superseding) · ✅ 2026-09-04 11:52 CDT: 0 missing |
| 5 | MODE flipped by the ceo on Lon's word and every seat told to run `next` | `head -1 /home/resources/postoffice/MODE` | FLEET-16 | ✅ 19:03 CDT: FLEET-16 — flipped by the ceo on Lon's 19:01 word (*"We are going to FLEET-16 with the new plan … Here let the fun begin!!!"*), 20 telegrams sent, lines 1–4 re-read GREEN at 19:02. Before that: CEO at handoff (Lon ~18:47: *"No HQ is currently running … We are in CEO mode."*) — Lon 2026-09-03 ~18:41: *"when I start a new CEO I will switch to FLEET-16 mode with 4 HQ's and one CEO"*; the next ceo runs ONE command on his word: `bash /home/claude/.scratch/fleet16_go.sh` (rewrites MODE line 1 keeping the history, telegrams the 16 seats their role from THE 16-SEAT CUT and the 4 HQs their first act — assign each seat its first row — and prints the ARCH-FLEET-CEO.md mode-history line to commit) · 2026-09-04 11:52 CDT: MODE is QUARTET; the flip is Lon's word — `bash /home/claude/.scratch/fleet16_go.sh` (ROLE table rewritten to the RE-CUT) |

The HQs mint their TO-MINT rungs tonight (each DONE-WHEN runnable, proven to fail once, `S4E_DONE_TIMEOUT` unset) and re-rank off-ladder rank-0/1 rows to ≥ 2; the ceo re-runs lines 1–4 every sitting and telegrams Lon READY with the five readings.

## ⛔⭐ THE HOURS — THE BUDGET IS 37.5 HOURS OF USAGE, NOT A WEEK (Lon 2026-09-03 ~18:25 CDT, in-chat to ceo, verbatim: *"I should make more clear that instead of 1 week due to usage credits we really have only 25 hours."* · ~18:27: *"You can add another 12.5 hours since one account will reset."*). H0 = 2026-09-03 ~18:25 CDT · H+37.5 = 2026-09-05 ~07:55 CDT (Lon 19:01 at the FLEET-16 flip says it as *"100% on all languages in 40 hours"* — the 37.5 hours of usage stays the binding budget; 40 is the round number). The D-labels elsewhere in this file map through this table: D0/D1 = H0→H+2 · D2–D4 = H+2→H+32 · D5 = H+32 · D6 = H+36.5 · D7 = H+37.5.

| window | what is true at the end of it |
|---|---|
| **H0 → H+2 (18:25–20:25 09-03)** | every ⭐ rung minted with a runnable DONE-WHEN (walker V1 = 0 — that alone puts ~40 rungs in the queue, more than 16 seats need); off-ladder rank-0/1 rows re-ranked ≥ 2; the digests carry the proclamation (Lon's script); the ceo reports READY and Lon flips MODE to FLEET-16; every STALE cell re-run on origin by its lane (snocone, rebus, the csnobol4 52-vs-58, Icon's three readings, the polyglot pair) so the walk starts from measured numbers |
| **H+2 → H+32 (20:25 09-03 → ~02:25 09-05)** | THE WALK, cheapest 100% first: masters within a dozen reds (Raku 1 · Icon 4 + the reconciliation · Pascal 11) go to FAIL=0 first; then every ladder green to every rung that exists (the census names its unbuilt rungs as OWED beside the cell); then package suites by class, largest classes first; a red that will not be cured by H+32 becomes a REASONED xfail — the cell reads 100% of its gradable denominator with the xfail count beside it, never a bare 100% |
| **H+32 → H+36.5 (~02:25–06:55 09-05)** | CHECKPOINT (loose, no pristine): one pushed tree; every cell re-run on it by its runner; hq_P publishes every README grid on the two-number three-angle basis (the B cells); `.s` artifacts regenerated; the announcement text with the xfail counts beside each 100% in front of Lon |
| **H+36.5 → H+37.5** | Lon's read; announce or hold |

Honesty line, written before the walk starts: in 37.5 hours Prolog's master (271/404, rungs 10–12 unbuilt) and the largest package suites (csnobol4 66 non-pass · fpc 59 · arizona 44 · jcon_tests 38 · swi 32 + an m4 cause) will not all reach a bare 100%. The product at H+36.5 is every cell HONEST — FAIL=0 over its gradable denominator, reasoned xfails counted beside it, every number tree-labelled with its command — and Lon reads the counts.

## THE DAYS (box clock — SUPERSEDED as a clock by THE HOURS above; the day rows keep their content as the order of the work; every day ends with `SCORE.md` rewritten by the runners and the ceo's remaining-to-100% read off it — the instrument is `python3 scripts/util_score_row.py check` (hq_T, SCRIP `8f4d0d9b1`): contacts origin, runs no suite, names every row past the 25-commit staleness threshold and every row with no checkable hash, in seconds)

| day | what is true at the end of it |
|---|---|
| **D0 Thu 09-03** | this plan pushed; each HQ has re-ranked its lane (rank 0 = its current rungs and its suite census rows; everything not on the grid at rank ≥ 2 until 09-10); the seat-held rows inherited by their HQ (`unclaim … "inherited under QUARTET"` → `assign`); the DONE and dead rows swept out of `QUEUE.tsv` (ceo) |
| **D1 Fri 09-04** | EVERY cell of the grid carries a number measured on origin ≤ 24 h old with a tree label — the STALE cells first (snocone, rebus, raku master, gimpel/snoflake/aisnobol/dotnet, the csnobol4 52-vs-58 and Icon 376/377 reconciliations, the polyglot 3/7-vs-8/2); the three missing ladders exist to rung ≥ 5; the four missing package gates print their denominators; every xfail list carries a reason per entry or the entry is a red; `board_packages.sh` runs REPORTED under `make test` |
| **D2 Sat 09-05 · D3 Sun 09-06 · D4 Mon 09-07** | THE WALK: each lane cures by class, rung by rung; Prolog rungs 10–12 land; masters go to FAIL=0; package suites climb; every landing rewrites its cell |
| **D5 Tue 09-08** | every cell reads 100% over its printed denominator or names its remaining reds as reasoned xfails; `board_packages.sh` turns BLOCKING at the measured floor (a ratchet: red on any fall) |
| **D6 Wed 09-09** | CHECKPOINT (loose, on Lon's ~19:10 order — no pristine build anywhere; sixteen seats never queue behind one build): one pushed tree, each lane `git pull --rebase` → incremental `make` → every cell re-run on that tree by its runner (the stale-binary refusal is the guard), README perf grids (three angles, WORK/OVERHEAD basis with the ≥ 50% refusal) for all seven, `.s` artifacts regenerated, the announcement text in front of Lon with the xfail counts beside each 100% |
| **D7 Thu 09-10** | announce |

## THE LADDER RECIPE — writing and walking, optimized (hq_T owns the standard; the lanes walk it)

1. **The rung list is a census of the public reference, never an invention.** One rung per construct topic in the order the language's own book teaches it — SNOBOL4 the green book chapters, Icon the Icon book, Prolog ISO §7–9 as § E already lays it out, Raku the roast S-numbers, Pascal ISO 7185 §6, Snocone its paper, Rebus its manual. hq_T writes each census as `corpus/tests/<lang>/config/LADDER.tsv` (rung, slug, chapter, state) — declared, never derived (the `MODES.tsv` precedent); its last line IS the top rung, and the grid's L cell is 100% when `test_<lang>_ladder.sh --to <top>` is green in both modes.
2. **A rung is tiny and oracle-cut.** ≤ 20 lines, one construct, its ref cut from the oracle (never hand-typed), failing once before it lands (a corrupted ref must read FAIL). Rungs live in ONE `ladder.<ext>` + `ladder.ref` at the suite root, banner-delimited, paired by the builder in a scratch tree (hq_T 2026-09-03: a family is one source file, not a directory; a torn pair took every SNOBOL4 board on the box down for 40 minutes). ⭐ **EVERY CONSTRUCT RUNG CARRIES AT LEAST ONE CASE THE CONSTRUCT MUST REFUSE (hq_P 2026-09-03 20:15, ruled into the recipe by ceo 20:40): a witness whose expected answers are all YES cannot detect a predicate that always says YES — hq_P's wrong Pascal rung-09 cure read every string relation as TRUE and turned the rung green in both modes; only `'abc' = 'abd'` graded against `fpc -Miso` caught it. The refusing case is cut from the oracle like every other ref.**
3. **Suite reds feed the ladder; walking the ladder cures the suites.** Every red a master or package suite names is ablated to a minimal witness and filed under the rung of the construct it exercises (`RUNG:<lang>/NN` on the row's LINKS line); a construct no rung covers becomes the NEXT rung. The ladder grows from the suites the public already knows, and a lane's order of work is its rung order — the `*-censused-by-class-and-cured` rows are where this happens.
4. **Rung N+1 opens when rung N is green in both modes; rungs on disjoint function families run in parallel** (the T ladder's seams table is the precedent). That is how sixteen seats walk seven ladders without a register or template collision: one seat per rung, one rung per seat, and a shared-node change goes to hq_C.
6. **Minting and walking alternate, rung by rung — never a whole ladder written first, never a cure waited on** (Lon ~18:30: *"I suppose you plan on minting and walking the feature rung ladders simultaneously or rather back and forth."* — yes, back and forth): the walker mints rung N from the census (witness + oracle ref, proven to fail once against a corrupted ref), runs it in both modes; GREEN → rung N+1; RED → the witness IS the class row for the HQ (Opus) and the walker keeps minting ahead on the next disjoint family while the HQ cures behind it. The ladder ahead of the current rung is the census; the ladder behind it is the regression floor (`--to N-1` stays green on every push). hq_T's rung-0–5 skeleton comes first only where no ladder exists yet (icon, snocone, rebus).
5. **One runner body** (`lib_ladder.sh`) serves seven stanzas: a runner fix lands for all seven at once; `test_<lang>_ladder.sh --to <top>` is the lane's landing gate on every push; SNOBOL4 FAIL=0 and Icon's watermark stay the control arms on every push regardless of lane. Stale runner headers (`test_snobol4_ladder.sh` still says ZERO origins) are hq_T's D1.

## THE 12-SEAT CUT — twelve walkers under eight HQs, contiguous ranges (PREPARED 2026-09-05 15:03 CDT by the ceo on Lon's word: *"So how many HQ's and how many Fleet workers should we have? 8 HQ's?"* · *"I just created S, I, and R root folders."*; the ceo's answer: twelve — the box carries ~21 sessions before boards stop completing, and the shortage measured that afternoon was CURE capacity, not witnesses; FLIPS on Lon's word via `fleet12_go.sh`; until then THE 20-SEAT CUT below governs)

**THE RANGES:** 01–02 hq_P (SNOBOL4 frontend + patterns) · 03–04 hq_S (SNOBOL4 runtime) · 05–06 hq_B (Icon frontend + generators) · 07 hq_I (Icon suites) · 08–09 hq_C (Prolog frontend + clause DB) · 10 hq_R (Prolog builtins) · 11 hq_U (the shared machine) · 12 hq_T (instruments). Seats 13–20 finish-if-in-reach or push + ledger + unclaim at the flip; Lon closes their windows.

| seat | role | HQ |
|---|---|---|
| **seat01** | SNOBOL4 isolation walker — rungs 24, 28, 33 and rung14's keywords (patterns, unevaluated expressions, recursion), FORMS then PAIRS | hq_P |
| **seat02** | SNOBOL4 packages — gimpel · snoflake · aisnobol · dotnet by class; the B cell on the two-number basis | hq_P |
| **seat03** | SNOBOL4 runtime walker — rungs 25–27, 29–32 (predicates, string functions, real math, I/O, system, SORT, error handling) | hq_S |
| **seat04** | SNOBOL4 csnobol4 suite + SPITBOL testpgms by class; the tracing program | hq_S |
| **seat05** | ICON isolation walker — rungs 38–42 (co-expressions, numeric/bit, strings, files/run-time, keywords) | hq_B |
| **seat06** | ICON master + STRICT rung suite + every xfail | hq_B |
| **seat07** | ICON suites — arizona · jcon · ipl by class | hq_I |
| **seat08** | PROLOG isolation walker — rungs 14–18 (evaluable functors, I/O, term I/O, halt and friends, ISO error terms) | hq_C |
| **seat09** | PROLOG master + swi_tests + gnu_prolog by class; the un-held rows census | hq_C |
| **seat10** | PROLOG INRIA — the bindings comparator, then the reds by ISO section | hq_R |
| **seat11** | CROSS-LANGUAGE — the base-vs-patched differential over the three open masters, the fuzz class, the port traces | hq_U |
| **seat12** | INSTRUMENTS — the closure census, the oracle-vs-ladder census, the SCORE reconciliations, the generators | hq_T |

Twelve Sonnet seats walk, census and witness; eight Opus HQs cure — each on ONE surface. The go line: `GO_QUOTE="<Lon's words>" bash /home/claude/.scratch/fleet12_go.sh` (re-lanes seat HQ files 01–12, MODE → FLEET-12, 12 role telegrams, 8 HQ telegrams, 8 stand-down telegrams to seats 13–20).

### THE 20-SEAT CUT (GOVERNING until the FLEET-12 flip)

## THE 20-SEAT CUT — four seats per HQ, five HQs, contiguous ranges (CUT 2026-09-05 12:5x CDT by the ceo on Lon's word, verbatim: *"I suppose we should increase fleet by 4 to be FLEET-20. Do you agree? Then we would have 4 workers per HQ."* · *"I'm thinking things are happening too slowly, so we are going to FLEET-20. Populate the new folders and ready for FLEET-20 running SNOBOL4, Icon, and Prolog."*; the 16-seat RE-CUT of 2026-09-04 is in git history below this header's commit; its WHO FIXES WHAT and COLLISION RULES stand with one word changed: the shared-node lander is hq_U)

**THE RANGES (contiguous per HQ — a seat's number says its HQ):** 01–04 hq_B (Icon) · 05–08 hq_C (Prolog) · 09–12 hq_P (SNOBOL4) · 13–16 hq_T (instruments, xfails, ladder, generators) · 17–20 hq_U (the shared engine, cross-language regressions). The postoffice `seat<nn>/HQ` files carry exactly this since 12:5x; `HQ.bak-fleet16` beside seats 04, 07, 08 and 12 keeps the FLEET-16 lane. THE ORDER OF WORK IS SNOBOL4, ICON AND PROLOG; Pascal, Raku, Rebus and Snocone rows stay PARKED-LON-HOLD, so no seat below carries them.

| seat | role (its first row is the ⭐ rung of that ladder; it never holds two languages' `src/` at once) | HQ |
|---|---|---|
| **seat01** | ICON isolation walker — `icon-ladder-every-feature-in-isolation-with-variations`: rung families (FORMS from the Icon book) over the built rungs; then the combinations row | hq_B |
| **seat02** | ICON suites — arizona and jcon_tests by class, ipl's compile-graded denominator kept honest | hq_B |
| **seat03** | ICON master + STRICT rung suite — the run-graded reds and every XFAIL under THERE IS NO XFAIL | hq_B |
| **seat04** | ICON combinations walker (PAIRS once seat01's isolation reads green) + the SNOBOL4 packages in hq_B's lane: snoflake · aisnobol · dotnet by class | hq_B |
| **seat05** | PROLOG isolation walker — `tests/prolog/config/LADDER.tsv` from § E in hq_T's schema FIRST, then families rung by rung; the § E top | hq_C |
| **seat06** | PROLOG INRIA — the bindings comparator that turns the outcome-class upper bound into a true score, then the reds by ISO section as class rows | hq_C |
| **seat07** | PROLOG master + swi_tests (the m4 single cause) + gnu_prolog by class; the dynamic-DB witnesses after hq_C merges r2 | hq_C |
| **seat08** | PROLOG un-held rows census — the 36 rows freed 2026-09-05: re-measure each on origin, ablate every red to a minimal witness on its rung, tombstone the stale ones | hq_C |
| **seat09** | SNOBOL4 isolation walker — the green-book census rungs built, then families (FORMS) from rung 0 | hq_P |
| **seat10** | SNOBOL4 packages — csnobol4_suite by class (oracle csnobol4, never sbl) and the SPITBOL testpgms | hq_P |
| **seat11** | SNOBOL4 TRACING — the eight-row tracing program in hq_P's lane (TRACE types, the SPITBOL banner, every sink), witnesses cut from `sbl -bf` | hq_P |
| **seat12** | **BENCHMARKS** — the B cells on the two-number three-angle basis vs each rival, SNOBOL4 and Icon first; never touches `src/` | hq_P |
| **seat13** | **INSTRUMENTS** — the ladder forms checker, the feature-coverage census, the SCORE.md table reconciliations, the mint-side DONE-WHEN refusals; never touches `src/` | hq_T |
| **seat14** | **XFAILS** — every remaining xfail across the three open languages fixed as a faulty test or filed as a class row with its witness (THERE IS NO XFAIL) | hq_T |
| **seat15** | **LADDERS** — the seven-point standard walked over snobol4, icon, prolog: rungs DECLARED and not built are RED; the TRACE rungs; `util_ladder_walk.py` clean | hq_T |
| **seat16** | **GENERATORS** — grammar-based random and exhaustive program generators graded against the oracle, divergences absorbed as witnesses; lands generators and corpus entries, never compiler fixes | hq_T |
| **seat17** | CROSS-LANGUAGE REGRESSION BISECTOR — every landing that moves another frontend's board: the base-vs-patched `.s` and verdict differential over all three open masters (hq_T's 1859-entry differential is the template), bisected to the commit, witness filed to hq_U | hq_U |
| **seat18** | PATTERN-ENGINE WITNESS WALKER — the shared Byrd boxes SNOBOL4 patterns exercise (operand parking per site, ARBNO, SPAN/ANY/BREAK snapshots, capture): minimal witnesses against `sbl -bf`, port traces checked, class rows on the rung they exercise | hq_U |
| **seat19** | RUNTIME + DRIVER WITNESS WALKER — `runtime/{core,rt,rtx}` and `driver/`: every path that cannot express failure (the `rt_goto_transfer` family, SETEXIT/ERRLIMIT compositions, the m4 registration path), each a minimal m3+m4 witness against the oracle | hq_U |
| **seat20** | FUZZ + PORT TRACES — the fuzz crash class over all three open languages and the `ALL.trace` port-trace refs (`test_gate_{sno,icn,pl}_port_trace.sh`); cross-language by construction; witnesses only, never compiler fixes | hq_U |

Twenty Sonnet seats walk, census and witness; the five Opus HQs cure — hq_U every shared-node class, hq_C Prolog whole, hq_B Icon whole, hq_P SNOBOL4 whole, hq_T the instruments. The go line: `GO_QUOTE="<Lon's words>" bash /home/claude/.scratch/fleet20_go.sh` (MODE → FLEET-20, 20 seat telegrams, 5 HQ telegrams).

### The 16-seat RE-CUT of 2026-09-04 (SUPERSEDED 2026-09-05 by the 20-seat cut above; kept for the provenance of the collision rules)

## THE 16-SEAT CUT — seven isolation walkers · five suite census-and-witness seats · one benchmark seat · two instrument seats · one generator seat (RE-CUT 2026-09-04 11:52 CDT by the ceo on Lon's word: *"You can distribute the work any way in which you see fit."* — the previous cut of 2026-09-03 is in git history; Lon 2026-09-03 ~18:29: *"A seat for each language walking the ladder is 7 of 16 sessions … And do not forget a seat for benchmarks."* · ~18:30: *"HQ's would be good bug fixers being Opus versus the Fleet are Sonnet."*)

**WHO FIXES WHAT.** ⛔⭐ Lon 2026-09-05 ~13:3x, in-chat to hq_T, verbatim: *"Why are you not delegating running simple tasks like a test suite run to a fleet worker. You should be fixing complex bugs."* — under any FLEET-<n>, an HQ does not run suites, boards or `make test` by hand: a seat runs them and files the class rows; the HQ, being Opus, cures. Reading the MODE value is not the same as changing behaviour on it (hq_T's own finding the same hour). The Fleet seats are Sonnet: they WALK — mint the next rung family from the census (forms, then pairs), cut each ref from the oracle, run it both modes, push per rung; run a suite, classify its reds, ablate each class to a minimal witness, mint the class row with that witness; cure only what is fixture-, xfail-, or instrument-level. The HQs are Opus: they FIX — every class row a seat files in their lane, in rung order, on the seat's witness; every shared-node class routed from any lane goes to hq_C. A seat that finds itself editing `src/` for more than one small, witnessed change hands the class to its HQ and moves to the next rung.

**COLLISION RULES (why this cut is parallel).** (1) One language's `src/` per seat, and one seat per language's parser/lowerer at a time: the walker seat owns `corpus/tests/<lang>/` ladder entries and the census file; the suites seat owns `corpus/packages/<lang>/` classes and never the ladder; where a language has two seats they split walker vs suites, never two walkers on one ladder. (2) Rungs on disjoint function families run in parallel inside one lane (the T ladder's seams table is the precedent); rung N+1 opens when rung N is green in both modes. (3) hq_U alone (hq_C until 2026-09-05 12:20) lands shared-node changes (`x86_asm.h`, `bb_*.cpp` shared boxes, `lower_*` nodes with more than one frontend), graded on every frontend that lowers to the node — a seat never edits them, it routes the witness. (4) The benchmark seat and the two instrument seats never touch `src/`; the generator seat lands generators and corpus entries, never compiler fixes. (5) Hot files are single-writer by construction: `SCORE.md` through `util_score_row.py` only; `QUEUE.tsv` through `s4e_msg.sh` only; `LADDER.tsv` by its walker only. (6) Push per rung, `pull --rebase` before push, re-prove the gate after the rebase; the claim gates catch register collisions.

**THE RANGES (contiguous per HQ — a seat's number says its HQ):** 01–03 hq_B · 04–06 hq_C · 07–11 hq_P · 12–16 hq_T. The postoffice `seat<nn>/HQ` files carry exactly this; `HQ.bak-fleet8` beside each keeps the FLEET-8 pairing.

| seat | role (its first row is the ⭐ rung of that ladder; it never holds two languages' `src/` at once) | HQ |
|---|---|---|
| **seat01** | ICON isolation walker — `icon-ladder-every-feature-in-isolation-with-variations`: rung families (FORMS from the Icon book) from rung 0 over the 36 built rungs; then the combinations row | hq_B |
| **seat02** | ICON suites — arizona 43/89 and jcon_tests 40/81 by class (the class rows ICN2/ICN-jcon), ipl's compile-graded denominator kept honest | hq_B |
| **seat03** | ICON master + STRICT rung suite (the run-graded reds, the 26 XFAIL under THERE IS NO XFAIL); becomes the combinations walker when seat01's isolation reads green | hq_B |
| **seat04** | PROLOG isolation walker — `tests/prolog/config/LADDER.tsv` from § E in hq_T's schema FIRST, then families rung by rung; the § E top | hq_C |
| **seat05** | PROLOG INRIA — the bindings comparator that turns 268/445 (outcome-class upper bound) into a true score, then the reds by ISO section as class rows | hq_C |
| **seat06** | PROLOG master 324/404 + swi_tests (the m4 0/114 single cause) + gnu_prolog by class; the dynamic-DB witnesses after hq_C merges r2 | hq_C |
| **seat07** | SNOBOL4 isolation walker — rungs 11–16 of the green-book census built, then families (FORMS) from rung 0 | hq_P |
| **seat08** | SNOBOL4 packages — csnobol4_suite 52/118 by class (oracle csnobol4), then gimpel · snoflake · aisnobol · dotnet | hq_P |
| **seat09** | PASCAL isolation walker + the ISO 7185 PAT and fpc_tests 128/181 classes (one seat: the master is 166/166 green) | hq_P |
| **seat10** | SNOCONE isolation walker — the 51-row census becomes a built ladder, then families | hq_P |
| **seat11** | **BENCHMARKS** — the B cells for all seven on the two-number three-angle basis vs each rival; the Prolog 32/32 UNPROVEN cause is hq_C's defect row, not this seat's | hq_P |
| **seat12** | RAKU isolation walker — `tests/raku/config/LADDER.tsv` from the roast S-numbers FIRST, then families | hq_T |
| **seat13** | RAKU roast — 924 of 986 parse_fail censused by class (the parser gap IS the Raku score, 4/986 today), each class a witness row for hq_T | hq_T |
| **seat14** | REBUS isolation walker + combinations — all 12 rungs built, so FORMS and PAIRS are the whole Rebus program | hq_T |
| **seat15** | **INSTRUMENTS** — `ladder-forms-column-and-forms-check-…` (every other lane's DONE-WHEN refuses until it lands: FIRST), then `feature-coverage-census-…`, then the SCORE.md table reconciliations | hq_T |
| **seat16** | **GENERATORS** — Lon 2026-09-04: grammar-based random and exhaustive program generators (the `expressions.py` lineage; every valid program up to length N with a measurable output, graded against the oracle); brief lands with the ceo's ARCH scan (CEO-210) | hq_T |

Seven isolation walkers (01 04 07 09 10 12 14) · five suite seats (02 03 05 06 08 13 — six, counting seat13's roast census) · one benchmark seat (11) · two instrument seats (15 and, once the checker lands, 15 again on coverage) · one generator seat (16) — all Sonnet; the four HQs (Opus) are the bug fixers behind them. The HQs keep their own hands on the shared-engine and instrument work (hq_C every shared-node class and the r2 merge; hq_T the census schema and the leaderboard; hq_B the public face; hq_P the B cells' review).

### Provenance — the 2026-09-01 opening order and the 2026-09-02 TRIO block, kept verbatim (SUPERSEDED as orders by QUARTET 2026-09-03 17:20 and by the lanes above; Prolog stays the deepest lane, no longer the only one)


**Opened 2026-09-01 by ceo on Lon's order, in-chat, verbatim in substance:** *"We are going to mode FLEET-16. Prolog is the #1 priority. All around test, performance, Term struct eradication."* → *"Make a master plan and being CEO ensure the fleet walks along the ladders."*
**Authority:** RULES.md is law; GOAL-PROLOG-100.md (s273 DESCR ruling + the 2026-09-01 blocks), GOAL-HQ-COMPLETE.md, GOAL-HQ-PERFORM.md, GOAL-HQ-BEAUTIFY.md stay sovereign for their lanes. **This file is sovereign for one thing only: THE ORDER OF THE WORK** — which rows are rungs, on which ladder, and what "walking" means for the picker, the HQs and the ceo. It is rewritten in place by ceo every sitting from the walk instrument's output (never appended; the standing table is the record — see [[rewrite-the-standing-line]] discipline in GOAL-CEO).
**Instrument:** `python3 SCRIP/scripts/util_ladder_walk.py` — joins the rung tables below with `/home/resources/postoffice/{QUEUE.tsv,QUEUE.done.tsv,claims,tasks}` and prints every rung's state plus the violations (V1 rung without row · V2 off-ladder seat · V3 stale park · V4 rank inversion · V5 unowned block · V6 orphan Prolog row). Exit 0 clean · 1 violations · 2 refuse. It answers *does the QUEUE agree with the PLAN*; `s4e_msg.sh fleet` answers *is the seat working*. Read both.

⛔⭐ **TRIO, 2026-09-02 10:05 → , PROLOG ONLY (Lon 10:50: *"I told the HQ's to work only on Prolog. They were confused and began Icon work."*):** the four (ceo + hq_C + hq_P + hq_B, all HQs Fable 5.1) work `ARCH-PROLOG-THREE-ZETAS.md` and nothing else until its § 6 program gate reads green; every non-Prolog row below is PARKED by definition for the duration. ceo holds T9 (Term deleted, pushing); hq_C PZ-4 → PZ-5 → C36; hq_P P7 (pulled forward to right after the Term push) → P6 → the van Roy worst-of-N gate; hq_B T7 → the GC-range witness → C37 → the Prolog-grading instruments.


## HOW THE FLEET WALKS (the rules; the ladders are below)

1. **A rung is a row.** Every unit of fleet work under this plan is a `QUEUE.tsv` row with a baton under `tasks/`, and every such row is exactly one rung of exactly one ladder. A row of ANY language on no ladder is an ORPHAN (2026-09-03; Prolog-only before) the owning HQ places or retires the same sitting (V6).
2. **Rank encodes the ladder.** Rank 0 = the CURRENT rung(s) of a ladder (marked ⭐ below); rank 1 = every other rung; rank ≥ 2 = off-ladder work. `next()` serves the topmost FREE row, so a seat that runs `next` steps onto a rung by construction. The ceo re-ranks each sitting from the walk (V4).
3. **A seat on an off-ladder row** lands it if its DONE-WHEN is within reach this sitting; otherwise pushes its progress, writes the LEDGER/NEXT, `unclaim`s (or `park`s on a real blocker) and runs `next`. Nobody rips a dirty tree (V2 is a notice, not a purge).
4. **HQs own ladders; the ceo owns the plan.** hq_C: T, C (Prolog) · hq_P: P, SNO, SC, PAS · hq_B: ICN · hq_T: I, RAKU, REB (the 2026-09-03 lanes; before that hq_B + ceo held I). An HQ minting a Prolog row writes `LADDER:<T|C|P|I|SNO|SC|ICN|PAS|RAKU|REB> RUNG:<id>` on its LINKS line and tells ceo the rung; ceo adds the row to the table the same sitting. A rung marked `TO-MINT:<lane>` is a mint the lane OWES (V1).
5. **Rung exit = the row's DONE-WHEN. Ladder exit = the gate in its header.** The plan is done when every cell of `SCORE.md` § THE SEPTEMBER 10 GRID reads 100% over its PRINTED denominator, both modes where the instrument grades both (2026-09-03; before: the four Prolog/instrument gates below). SNOBOL4's blocking floor (`make test`) is the control arm on every landing regardless of ladder (RULES § SHARED-NODE VERDICT SCOPE); Icon's pinned watermark likewise.
6. **Parallelism is per rung, never per row.** Rungs on one ladder may run concurrently when they touch disjoint function families (the T ladder's seams are the table in the umbrella's `## NEXT`); two seats never hold one rung. A blocked rung is spelled `BLOCKED-ON:<row>` / `PARKED-AWAITING:<row>` so `next()` un-parks it by itself when the blocker lands (V3 catches the ones spelled otherwise).
7a. **Modes.** The plan is mode-independent: under FLEET-n the seats are the walkers and V2 (off-ladder seat) is live; under DUO/TRIO the HQs are the walkers of their own ladders (hq_C: T, C · hq_P: P · hq_B: I), V2 is moot, and the rank rule still orders the work. Lon 2026-09-01 evening: *"Time to wrap it up on all fleet workers. Go to mode TRIO."* — the seats wrapped to clean pushes and their rungs returned to FREE with ledgers; the HQs resume them by rank.
7. **The ceo walks every sitting:** run the instrument · fix V4/V5 by rank/owner edits (snapshot `QUEUE.tsv.bak.ceo-walk-*` first) · route V1/V6 to the owning HQ as one telegram with the list · note V2/V3 · rewrite the tables below in place · audit ~3 DONE rungs by re-running their DONE-WHENs (the CEO LOOP's AUDIT step, now aimed at rungs).

## MEASURED STATE AT OPENING — SUPERSEDED 2026-09-03 by `SCORE.md` § THE SEPTEMBER 10 GRID (the 2026-09-01 numbers stay in git history at `.github` `b6499924^`; nothing here restates a number — one leaderboard)

## THE LADDERS

### LADDER T — Term→DESCR eradication (owner hq_C · umbrella `prolog-term-to-descr-eradication` · GATE: the umbrella DONE-WHEN — `term.h` and `pl_cell_conv.h` deleted, tree-wide `Term` word-refs 0, prolog smoke 5/5 both modes, parser suite PASS, master board not worse than its baseline)

✅ **Term IS GONE — SCRIP `46db4457`, 2026-09-02 10:40, ceo's hands on Lon's 10:35 order: `grep -rcw Term src` = 0, both headers deleted, ratchet at zero, boards not worse (master 348/348 = origin's set; SNOBOL4 1679/0 both modes). T9's last clause (board fail=0 both modes) is the redesign's program gate, so the row stays OPEN as the umbrella; T1–T8's purpose is achieved and they close with it.** ⛔⭐ **LON ORDER 2026-09-01 ~18:44 — THE SLICES ARE RETIRED; THE UMBRELLA T9 IS THE ONLY RUNG, EXECUTING NOW BY hq_C's OWN HANDS** (*"So then it is time to whack it. Completely remove the Term struct and all it's references. I do not want to see it every again. Whatever process is being done is too slow. So whack it now!"*). T1–T6 released by their seats (push-or-salvage within ten minutes), T7 parked behind T9, T9 FREE → hq_C claims; finish line = zero `Term` word-refs under `src/` and `term.h`/`pl_cell_conv.h` gone, boards not worse. Rows below are kept for their DONE-WHENs, which close on the umbrella.

| rung | row | lane | gate |
|---|---|---|---|
| ⭐T1 | prolog-term-descr-s1-write-format-printers | seat | slice DONE-WHEN (78 Term lines → 0: write/format/atom_op/char_type + prolog_builtin.{c,h}) |
| ⭐T2 | prolog-term-descr-s2-compare-sort-pairs | seat | slice DONE-WHEN (65 → 0: compare/sort/keysort/pairs/bag + rt_runtime.c compare helpers) |
| ⭐T3 | prolog-term-descr-s3-copy-findall-nb-aggregate | seat | slice DONE-WHEN (34 → 0: copy_term/findall/nb_/aggregate) |
| ⭐T4 | prolog-term-descr-s4-typetest-functor-univ-succ | seat | slice DONE-WHEN (57 → 0; deletes prolog_unify.c) |
| ⭐T5 | prolog-term-descr-s5-dynamic-db-flags-streams | seat | slice DONE-WHEN (41 → 0: assert/retract/clause/flags/streams) |
| ⭐T6 | prolog-term-descr-s6-parser-builds-tree-t-directly | seat | slice DONE-WHEN (112 → 0: prolog_parse.c, prolog_lower.c, prolog_atom.c) |
| ⭐T7 | prolog-term-descr-s7-dead-resolution-env-layer-deleted | seat | slice DONE-WHEN (22 → 0: resolution.{h,c} env layer, zero callers) |
| T8 | prolog-term-descr-s8-term-h-and-converters-deleted | seat | umbrella DONE-WHEN; BLOCKED-ON T1–T7 (spelled BLOCKED-ON s6; ledger carries the rest) |
| T9 | prolog-term-to-descr-eradication | hq_C | umbrella closes on T8's green; hq_C sweeps the ledger and tells ceo |

### LADDER C — Prolog correctness and tests (owner hq_C · GATE: master board `fail=0 crash=0` both modes over the printed denominator (371 today) · rung suite 15/15 both modes with red names printed · GNU and SWI conformance suites graded on SCORE.md with their denominators · crosscheck ORACLE_MISS 89 → 0 or each miss a row · PZ-4/PZ-5 landed so the multi-clause classes die)

⛔⭐⭐⭐ **LON 2026-09-02 10:00 — *"The answer to how Prolog is doing is TERRIBLE. If we did not do proper THREE ZETAS for Prolog then go do a complete re design. I want that bad code deleted now."* → THE PROLOG REDESIGN ON THE THREE ZETAS is one program and the head of this ladder:** BUILD = ⭐C21 PZ-4 (RBP activation frame, first, hq_C now) → C25 PZ-5; DELETE = ⭐C36 (plc solver), P7 (setjmp failure/cut), P6 (by-name cascade), C37 (polled throw flag), T9 (Term to zero, 75 at `5ebc7961`); design page **`ARCH-PROLOG-THREE-ZETAS.md` LANDED 10:25 (`8426dc57`) — sovereign for the order of this lane's work**; gate = rung suite 15/15 both modes · van Roy REFUSE 0 · master ≥ 351/351 · SNOBOL4 floor. Every other C rung queues behind these.

| rung | row | lane | gate |
|---|---|---|---|
| ⭐C1 | prolog-directive-only-file-fatals-no-main-bb-graph | seat | the 139-entry driver class dies; master board re-run, printed denominator |
| C2 | prolog-master-board-remaining-reds-classified-by-stderr-signature | hq_C | after C1: re-run the master board, classify every remaining red by stderr signature, one row per class (the 5 `fail` entries included); re-measure crosscheck ORACLE_MISS (REFUSES rc=2 until C1 is DONE, by construction) |
| C3 | prolog-rung-suite-reds-rowed-by-class | hq_C | DONE 2026-09-02 (hq_B, post-cut SCRIP `7432838a` / corpus `542de174`): the suite grades rc (3 greens were vacuous refusals of empty-output witnesses) and names every red with the driver's ladder rung (`REFUSED-LADDER rung N`); **0/15 both modes today**, all 15 classed by HIGHEST construct into four `prolog-rung-red-class-*` rows parked under the ladder umbrella — rungs 6 (1), 8 (5), 9 (1), 10 (8); 15/15 is reached at rung 10 by construction and is REPORTED until then (RULES § THE PROLOG REBUILD GATE cl. 4) |
| C4 | prolog-backtracking-yields-first-solution-only | seat | row DONE-WHEN |
| C5 | prolog-call-n-multiarg-target-wrong | seat | row DONE-WHEN — SUPERSEDED 2026-09-01 ~20:22: folded into C10 `prolog-variable-goal-dispatch-to-user-predicate` (hq_C's ablation: one runtime-dispatch defect, unknown functor to a user predicate of arity ≥ 1, wearing three row numbers; `prolog-master-red-class-initialization-goal-fails-with-main` PARKED-AWAITING C10, P1 exonerated) |
| C6 | prolog-existence-error-not-catchable-iso | seat | row DONE-WHEN |
| C7 | prolog-forall-wrong-answer-rung57 | seat | row DONE-WHEN |
| C8 | prolog-missing-string-pairs-builtins | seat | row DONE-WHEN |
| C9 | prolog-var-goal-dispatch-through-catch-call-silently-fails | seat | row DONE-WHEN |
| C10 | prolog-variable-goal-dispatch-to-user-predicate | seat | row DONE-WHEN |
| C11 | prolog-abolish-leaves-predicate-defined-but-empty | seat | row DONE-WHEN |
| C12 | prolog-write-canonical-prints-dot-functor-form-for-lists | seat | row DONE-WHEN (coordinate with T1: the writer is being rewritten) |
| C13 | prolog-two-programs-broken-post-churn | seat | row DONE-WHEN |
| C14 | prolog-queens-and-sentences-segv-are-more-rt-jmp-frame-lexprep2-witnesses | seat | row DONE-WHEN |
| C15 | prolog-plw-unify-cells-var-nonvar-trail-smash | seat | row DONE-WHEN (coordinate with T2) |
| C16 | prolog-findall-directive-replace-segv | hq_C | row DONE-WHEN (coordinate with T3) |
| C17 | prolog-bench-12-kernels-output-diverge | hq_C | 12 kernels output-exact vs `.expected`; PARKED-AWAITING P1 |
| C18 | test-gate-pl-gz-dead-instruments-measure-a-retired-path | seat | dead gates retired or re-pointed, named in the commit |
| C19 | prolog-bak-files-dead-code-sweep | seat | row DONE-WHEN |
| C20 | prolog-next | seat | GOAL-PROLOG-100 LIVE CURSOR's first incomplete step (a pointer rung; the seat names what it did) |
| C21 | prolog-pz4-gamma-retain-activation-frames | hq_C | PZ-4 keystone; BLOCKED-ON P1 — ⚠ 2026-09-02 09:20 (hq_B, FINDING `a4a7cfff`): the BLOCKED-ON P1 edge self-cleared but the mechanism PZ-4 waits for — Prolog RBP frame promotion, the row's own clause (a) — never landed; baseline 0 rbp-relative / 323 rsp-relative / 0 `push rbp` (pristine `5839cf13`); `x86_fb()`/`x86_fb_pinned()` are stubs answering rsp/0 for every language. Goes to hq_C by ASSIGNMENT after T9 zero and C36; no fifth seat — **⭐⭐⭐ LON 2026-09-02 09:55: *"give Prolog its activation ZETA like all the rest … Go fix it."* → FIRST IN THE ORDER, hq_C building now; design = Icon's RBP frame model; T9's tail pauses or goes to a seat at hq_C's call; C36 after** |
| C22 | prolog-multiclause-uninit-lexprep-frame | hq_C | PARKED-AWAITING C21 |
| C23 | prolog-sendmore-cryptarithm-segv | hq_C | PARKED-AWAITING C21 |
| C24 | prolog-between-generator-backtrack-crash | hq_C | PARKED-AWAITING C21 |
| C25 | prolog-pz5-in-frame-resume-delete-pending-mailbox | hq_C | PZ-5, BLOCKED (spell the blocker) |
| C26 | tests-consolidate-prolog-pz4-blocked-33 | hq_B | 33 entries join the master suite; re-check whether still PZ-4-gated |
| C27 | prolog-gnu-conformance-ok-fail-print-zero-bytes-both-modes | seat | GNU conformance suite `test_prolog_gnu_suite.sh` — at 43e30f0d (reproduced live at 54cf54fd): `GNU_SUITE_BOARD total=62 lib=45 ok=15 ok_pass=6 ok_fail=9 reject=2 unexpected=0` (rc=1); the 9 ok_fail entries print 0 bytes in both modes vs gprolog's output — rowed by class, SCORE.md row |
| C28 | prolog-swi-conformance-22-reds-rowed-by-class | seat | SWI conformance suite `test_prolog_swi_suite.sh` — at 43e30f0d: `Suite totals: PASS=92 FAIL=22 TOTAL=114 mode=--run` (m3 only — the m4 arm is owed by the runner); 22 reds rowed by class, SCORE.md row |
| C30 | prolog-bare-directive-loses-conjuncts-after-the-first-at-parse-time | hq_C | row DONE-WHEN (hq_C mint 2026-09-01; parse-time conjunct loss in a bare directive) |
| C29 | prolog-iso-coverage-missing-builtins-rowed-by-family | seat | ISO coverage (`audit_prolog_iso_coverage.sh`): step one wires the gprolog builtin list from /home/resources/gprolog-master (the audit prints "MISSING canonical source" on this root today), then the missing-builtin list becomes rows by family |
| C31 | prolog-master-red-class-ast-dump-refs-134-entries | seat | the 133 clause-only entries whose `.ref` is `--dump-ast` output: grade them with the instrument that wrote the ref (or re-cut the refs to `--run` output) so the master board stops counting a right compiler as 133 fails; was BLOCKED-ON P1 by proximity, unblocked by ceo 2026-09-01 ~20:10 on hq_C's per-entry evidence (FINDING `d2b1391f`) |
| ⭐C32 | prolog-findall-directive-replace-5-returns-empty-list-silently | seat | the ONE genuine silent wrong answer on the post-C1 board (`findall_directive_replace_5` prints `[]`, wants `[20,30]`, rc=0, no stderr); minted by hq_C 2026-09-01 ~20:12 at rank 0 on the C2 per-entry sweep (FINDING `d2b1391f`), placed by ceo the same sitting |
| C33 | prolog-master-red-class-abort-signal-6-four-entries | seat | the abort/signal-6 crash class on the post-C1 board (between_ite_naf_1, findall_dcg_directive_1, forall_ite_directive_1, list_directive_2); minted by hq_C 2026-09-01 ~20:14 at rank 1 on the C2 per-entry sweep; the four SEGVs stay with C16 |
| ⭐C34 | prolog-member-2-redo-smashes-stack-canary | seat | `member(Z,[1,2,3]), write(Z), nl, fail` prints 1, 2, then `*** stack smashing detected ***` rc=134 — a core builtin aborting on its second redo; pre-diagnosed (canary trip = fixed-size local overrun on member/2's redo; P1 ζ-depth class excluded by hq_P's three-arm measurement; reproduces on ce196d78); minted by hq_P from seat06's witness, ranked 0 by ceo 2026-09-01 ~20:24 |
| ⭐C35 | prolog-functor-3-name-output-segvs-write-regression-bisected-to-9368c3b0 | seat | THE 1-ENTRY PASS DELTA, NAMED BY A/B (hq_C): `functor_ite_univ_1` passed at `8eac17da`, SEGVs at `ce196d78`; bisected to `9368c3b0` (T4 slice 4: functor/3's Name output is an atom cell write/1 dies on); witness `functor(foo(a,b),N,A), write(N), write(A)` wants `foo2`; DONE-WHEN proven red thrice and green on `9368c3b0^`; minted rank 0 by hq_C 2026-09-02 — T4's holder (seat04) carries the same witness on T4's DONE-WHEN |
| ⭐C36 | prolog-call-n-compiles-through-eval-and-the-plc-runtime-solver-is-deleted | seat | LON ORDER 2026-09-01 ~20:53 (*"Remove that Prolog runtime interpreter, we already decided that Prolog should be re-using EVAL and CODE."*): delete the `plc_*` runtime goal solver (`by_name_dispatch.c` :1129-:4752, 136 refs, `rt_pl_call_gen`); `call/N` and every runtime-constructed goal compile through the EVAL/CODE runtime-compilation path and run as wired boxes; C10's witnesses become the acceptance tests; rank 0 — ⚠ SEQUENCED BEHIND T9 (ceo 2026-09-02 08:30): four seats picked it in twelve hours between releases; held at rank 3 until T9 reads zero, then back to rank 0 the same minute; seat09's WIP branch `wip/prolog-call-n-metacall-synthesis` + seat15's two findings are the start. **Lon 2026-09-02 09:02: *"If that solver-deletion challenge becomes too much for the Fleet, it might should be done by HQ or yourself."* → after zero it is hq_C's own milestone (T9+1), ceo the fallback; no fifth seat** |
| C37 | prolog-exceptions-uncaught-propagation | seat | seat12's find from C11: nothing outside `catch/3` ever checks `rt_pl_throw_pending()` (zero checks in `src/driver/*.c` and `src/templates/bb/*.cpp`; all 8 live in `by_name_dispatch.c`'s catch/3 machinery), so a thrown error outside any catch is swallowed instead of propagating to an uncaught-error exit; rank 1; placed by ceo 2026-09-01 ~18:43 |
| C38 | prolog-if-then-else-swallows-an-exception-thrown-in-the-condition | seat | `( Cond -> Then ; Else )` swallows an exception thrown while evaluating Cond and takes Else instead of propagating uncaught per ISO (seat14's find from C11, hq_C verified against swipl before minting); rank 1; placed by ceo 2026-09-01 ~21:10 |
| C39 | prolog-toplevel-main-multiclause-no-fallthrough-on-backtrack | seat | a multi-clause top-level `main/0` under `:- initialization(main)` never falls through to its second clause on backtrack — a common idiom (fail-driven loops across top-level main), blast radius likely wider than one program; rank 1; placed by ceo 2026-09-01 ~21:10 |
| C40 | prolog-iso-family-arithmetic | seat | C29's deliverable: the ISO builtins missing in the arithmetic family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C41 | prolog-iso-family-atoms-strings | seat | C29's deliverable: the ISO builtins missing in the atoms-strings family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C42 | prolog-iso-family-control | seat | C29's deliverable: the ISO builtins missing in the control family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C43 | prolog-iso-family-database | seat | C29's deliverable: the ISO builtins missing in the database family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C44 | prolog-iso-family-flags | seat | C29's deliverable: the ISO builtins missing in the flags family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C45 | prolog-iso-family-streams-io | seat | C29's deliverable: the ISO builtins missing in the streams-io family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C46 | prolog-iso-family-term-inspection | seat | C29's deliverable: the ISO builtins missing in the term-inspection family, rowed by seat15 2026-09-02 ~08:20 with a per-family DONE-WHEN; rank 1; placed by ceo 08:35 |
| C47 | prolog-det-call-bypasses-dynscope-procedure-body | seat | a deterministic call bypasses the dynamic-scope procedure body (seat mint, 2026-09-02); re-ranked 2 → 1 and placed by ceo 08:35 |
| C48 | prolog-lower-pi-name-segv-test-dcg | seat | seat mint from the C27/C28 conformance sweeps (2026-09-02); rank 2 → 1 and placed by ceo 08:55 |
| C49 | prolog-m4-duplicate-symbol-multiclause-emit | seat | mode-4 emits a duplicate symbol for a multi-clause predicate (seat mint, 2026-09-02); placed by ceo 08:55 at rank 1 |
| C50 | prolog-plunit-registration-silent-fail-empty | seat | plunit registration fails silently and the suite runs empty (seat mint from C28); placed by ceo 08:55 at rank 1 |
| C51 | prolog-swi-test-term-undefined-function | seat | SWI test term uses a function SCRIP leaves undefined (seat mint from C28); placed by ceo 08:55 at rank 1 |
| C52 | prolog-gnu-suite-ciaolib-use-module-warning | seat | C27 deliverable (seat09's GNU conformance sweep, 2026-09-02): the suite's ciaolib `use_module` emits a warning SCRIP treats differently; rank 2 → 1, placed by ceo 09:10 |
| C53 | prolog-gnu-suite-whole-pl-missing-bip-list | seat | C27 deliverable: the whole-file GNU suite run exposes the list of missing built-in predicates — rowed so each family gets a home; rank 2 → 1, placed by ceo 09:10 |
| C54 | prolog-g-stage2-runtime-reads-may-be-blind-under-compile | seat | seat mint 2026-09-02: runtime code that reads `g_stage2` may see nothing under `--compile` (the standalone binary never runs the compiler's stage-2 fill) — the same mode-4 blindness class as the atom-init regression; rank 2 → 1, placed by ceo 09:15 |
| C55 | prolog-gnu-suite-all-pl-noarg-stack-overflow | seat | C27 deliverable (seat11's GNU sweep, 2026-09-02): the whole `all.pl` run with no argument overflows the stack; rank 2 → 1, placed by ceo 09:29 |
| ⭐C56 | prolog-rung-10b-assert-retract-abolish-clause-the-dynamic-database | hq_C | **RUNG 10b — REOPENED 2026-09-03 22:12 by ceo on Lon's override** ("The clauses are compiled to Byrd Boxes, so then should the dynamic calls to add and remove the code. It is all code not data." · "Remove the interpreter ... Delete it now."), so the earlier DONE is VOID: the interpreter half is deleted and the rebuild compiles each asserted clause through `emit_chain`. Was DONE 2026-09-03 (hq_C engine + seat05 witnesses, SCRIP `4d7a81f2a` corpus `8fb65789b`): the dynamic database in ONE named root cell per predicate off `r14` (§ B.15 + § C); `--only 10` 12/14 with all five dynamic-DB witnesses green both modes, rung suite 0/15 → 13/15 both modes, DONE-WHEN rc=0. The rung is not WHOLE until 10a lands |
| C56a | prolog-rung-10b-m3-unbound-arg-self-binds-when-the-clause-is-runtime-compiled | hq_C → seat06 | **RUNG 10b, C56's child** — minted rank 0 by hq_C 2026-09-03 with its criterion proven RED as written (`scripts/test_gate_prolog_dynamic_db_both_modes.sh`, four arms {ground,unbound}x{m3,m4}: 3 PASS, `FAIL m3 unbound: want [1] got [ERROR 246 stack overflow]`). MODE 4 IS CURED on branch `wip/prolog-compiled-clause-db-r2` (`6d62b64be`) — its whole defect was a missing `bb_pool_init()`, which reads as NOBUILD and so is indistinguishable from a compiler refusal. The one arm left: after `assertz`, an m3 call with an UNBOUND argument self-binds (infinite `c_VARVAL_fn` deref); a GROUND argument is correct. ⭐ Registration is EXONERATED — `pl_runtime_define_pred` now mirrors `runtime_eval.c:149-193` (CODE/EVAL's runtime compile) step for step; so are the dc path, the const-unify leaf and the optimizer, each by a measured off-switch. The lead: `SCRIP_PL_TRACE=1` AND `SCRIP_PORT_COUNTS=1` BOTH cure it — two instruments sharing only *emit more code at port sites*, i.e. uninitialised/stale frame storage |
| C74 | prolog-van-roy-kernels-six-of-21-do-not-run-in-m3-and-it-is-four-classes-not-one | hq_C | **Routed by ceo 23:03 from hq_P's I26** ("22 van Roy kernels 32/32 UNPROVEN on a documented m3 crash class"). ⛔ hq_C re-measured on SCRIP `5d3a83030` (pristine) / corpus `a0fd13004` and the report's shape is wrong twice: there are **21** kernels, not 22, and **15 of them exit 0 today**; and it is **FOUR classes, not one** — (a) trail-arena refusal rc=2 `exhausted after 1048575 conditional bindings` (ham, zebra) — a resource ceiling, and the first question is whether conditional bindings are ever popped on backtrack, because a leak reads exactly like a small arena; (b) rc=124 timeout at 30s (meta_qsort) — which cannot distinguish "needs 31s" from "never finishes", so no hang may be filed without a duration; (c) rc=1 ERROR 246 stack overflow (qsort); (d) ⭐ **wrong answer** (queens, queens_8) — prints `_G0`, the NAME of an unbound variable, where swipl prints the solution list. (d) is hq_C's lane and is the one to take first. ⛔ **NOT the 10b mechanism** — ceo asked me to check before minting: zero `assert/retract/abolish/clause` calls in any failing kernel, so none reaches `pl_runtime_define_pred`, the only path `zls_forget_graph_nodes()` touches. DONE-WHEN `scripts/test_gate_prolog_vanroy_kernels_m3.sh`, proven RED as written (rc=1, `kernels=21 non-zero-exit=6`) |
| C57 | prolog-call-n-compiles-through-eval-and-the-plc-runtime-solver-is-deleted | hq_P | **RUNG 10a** (the ceo's 19:45 ask: the walker could not see either half of rung 10 because neither had a C-row): `call/N`, `phrase/2,3` and the variable goal through `bb_call_value` + the runtime goal compiler; `ladder__rung10_call_n` is the ONLY red left in `--only 10` and the only red in `--to 11`. SWI 0/114 stays blocked on it |
| ⭐C71 | prolog-rung-12-first-argument-indexing-no-clause-step-on-a-bound-first-arg | seat05 → hq_P | **RUNG 12 (BX-IX)** — minted rank 0 by hq_C 2026-09-03 with its criterion proven RED (rc=1, `m3 clause-step fails on an indexed call: 1`). ⛔ THE LADDER ALREADY CALLS RUNG 12 GREEN (`--only 12` 2/2) because its one witness grades the ANSWER, and a predicate that walks its clause chain answers exactly like one that jumps to the matching clause; § E row 12's criterion is STRUCTURAL (zero β labels, `Exit` not `*Exit`), so the row's DONE-WHEN greps the port trace and the emitted `.s` instead. seat05 WALKS (witnesses for a bound first arg, an unbound one, a last-clause match); hq_P CURES (§ B.3 static choice cut at α). Third rung this sitting found green over a population that could not see its own feature |
| C58 | prolog-rung-10-flags-cleanup-and-output-redirection-builtins-refuse | seat | **RUNG 10, the other half of § E row 10** (nb_setval/nb_getval/b_setval/b_getval and the settable flags as ζ-STANDING root slots keyed at compile time, ceo ruling 2026-09-02 18:50) — seat06's census, placed here by hq_C 2026-09-03 |
| C59 | prolog-rung-red-class-introspection-builtins-is-ladder-rung-7 | seat | **RUNG 7** — seat06's census; the generator family's introspection builtins. ⚠ Rung 7 is where `repeat/0` also has no witness (§ E row 7 as amended) |
| C60 | prolog-bagof-setof-free-variable-grouping-with-caret-is-rung-8b | seat | **RUNG 8b** — the `^` free-variable grouping `lower_prolog.c` refuses by name; it is one of the two remaining rung-suite reds (`rung44_setof_group`). V6 orphan placed by hq_C 2026-09-03 on the ceo's 19:45 ask |
| C61 | prolog-builtins-fail-silently-instead-of-throwing-iso-type-errors | seat | **RUNG 9** — an ISO type_error that fails silently is a wrong answer wearing a legal one; seat06's census |
| C62 | prolog-is-2-raised-errors-escape-catch-3-uncaught | seat | **RUNG 9** — an `is/2` error escaping `catch/3` is the same family as the existence-ball crash hq_C cured this sitting (C68); seat06's census |
| C63 | prolog-dcg-phrase-3-leaves-output-list-argument-uninstantiated | seat | **RUNG 10 (DCG, § B.16)** — `ladder__rung10_dcg_phrase` passes, so this is the phrase/3 arm the witness does not reach; seat06's census |
| C64 | prolog-meta-call-bridge-does-not-reach-builtins-or-operators | seat | **RUNG 10a** — a meta-call that reaches user predicates but not builtins is C57's contract, not a separate machine; seat06's census |
| C65 | prolog-multiclause-variable-goal-dispatch-skips-matching-clause | seat | **RUNG 10a** — variable-goal dispatch skipping a matching clause; C57's witness list; seat06's census |
| C66 | prolog-display-1-unimplemented | seat | **RUNG 6** — `display/1` does not exist yet; one of four write/read/stream-family gaps in seat06's census (C66-C66c). The walker reads ONE topic per row, so they are four rows |
| C66a | prolog-write-term-2-3-unimplemented | seat | **RUNG 6** — `write_term/2,3` unimplemented; seat06's census |
| C66b | prolog-read-term-from-atom-3-unimplemented | seat | **RUNG 6** — `read_term_from_atom/3` unimplemented; seat06's census |
| C66c | prolog-extended-stream-and-char-io-builtins-unimplemented | seat | **RUNG 6** — the extended stream/char-IO builtins; `rung66_current_stream`, one of the two remaining rung-suite reds, is this family; seat06's census |
| C67 | prolog-reverse-2-succeeds-on-aliased-input-output-list | seat | **RUNG 2** (multi-clause user predicates over lists) — an ordinary clause-resolution defect, not a library gap; seat06's census |
| C67a | prolog-user-defined-list-recursion-leaves-tail-var-unbound | seat | **RUNG 2** — user-defined list recursion leaving the tail variable unbound; seat06's census |
| C68 | prolog-existence-error-ball-arity-raw-int-not-descr-crashes-catch-unify | hq_C | **RUNG 9 — DONE 2026-09-03 (hq_C)**: cured from the other end by seat05's rung-10b abolish witness. Not the arity field: an INTERNED atom (`DT_A`) in the engine-built ball meeting a STRING atom (`DT_S`) in the program-written catcher fell past every arm of `plw_unify_cells` into `rt_descr_equal` → `VARVAL_fn`, which reads the payload as an array. seat06's backtrace was exact and its inference was wrong |
| C69 | prolog-number-atom-2-ref-tests-nonexistent-predicate-should-be-atom-number-2 | seat | **FIXTURE, rung 6** — the `.ref` tests a predicate that does not exist in any Prolog; a corpus defect, curable by the seat that finds it (LADDER RECIPE: fixture-level cures stay with the walker) |
| C70 | prolog-swi-class-ref-coverage-9-of-249-swi-tests-files | seat | **C28's child, off-ladder by construction** (it is about the SUITE's own coverage, not a construct): 9 of 249 vendored SWI files carry a `.ref`, so the suite grades 114 and the rest are unmeasured. V6 orphan placed here by hq_C 2026-09-03 rather than retired — the denominator question is real and belongs to seat07 |
| C72 | prolog-swi-tests-66-files-genuine-parser-errors-unrelated-to-rung-10 | hq_C | **RUNG 1-2 (the parser, not a construct rung)** — seat07's mint from the swi_tests census: 66 vendored files fail to PARSE, and that is NOT rung-10-gated, so it is the part of the SWI denominator that can move before `call/N` lands. Claimed by hq_C 2026-09-03 |
| C73 | division-by-zero-is-a-silent-null-not-an-error-in-at-least-snobol4-and-icon | hq_C | **RUNG 6 (arithmetic errors), routed from hq_P as a SHARED-NODE class** — THREE languages, not two: `arithmetic.c` returns FAILDESCR for a zero divisor and FAILDESCR is not an error in Icon (expression failure), SNOBOL4 (statement failure) or Prolog (goal failure). **PROLOG ARM CURED 2026-09-03 (hq_C, SCRIP `1b9bea901`)**: `$ax_zguard` raises `error(evaluation_error(zero_divisor), context(Op/2,_))` from the lowerer, never from the shared runtime. The SNOBOL4 (ERROR 014 + ERRLIMIT/SETEXIT) and Icon (run-time error 201) arms stay open with the Prolog arm as their worked example |
| ⭐C72 | prolog-iso-conformance-inria-suite-vendored-and-graded | hq_T vendors + writes the runner (rank 0, minted ceo CEO-187 on Lon 21:05); reds → seat07 class rows for hq_C | `INRIA_SUITE_BOARD total= m3_pass= m3_fail= m4_pass= m4_fail=` printed both modes, SCORE Prolog V cell via `util_score_row.py write --suite INRIA` — THE ISO 13211-1 denominator |
| ⭐C73 | prolog-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect | hq_C (rank 0; Lon 21:30) | `ALL.csv` xfail 0 of the printed total (1 today) and `ALL.wantrc` entries all honest; refs from `swipl`/`gprolog` |
| ⭐C75 | prolog-ladder-every-feature-in-isolation-with-variations | hq_C (rank 0, CEO-209) | write `tests/prolog/config/LADDER.tsv` from § E in the shared schema first; then every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_prolog_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) |
| C76 | prolog-ladder-feature-combinations-after-isolation | hq_C (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |
| ⭐C77 | omega-release-skipped-on-the-jcc-path-when-zls2-or-flat-glue-carves-with-op-wpop-zero | hq_C (rank 0, CEO-206; shared encoder) | hq_P's hypothesis confirmed or refuted in one run (`SCRIP_ZETA_OMEGA_TRACE=1` vs `SCRIP_PORT_EXIT_AUDIT=1`), cure graded on every frontend |

### LADDER P — Prolog performance (owner hq_P · GATE: all 22 van Roy kernels output-exact and published on the two-number basis, three angles, vs swipl and gprolog (`bench_prolog_vanroy.sh` + `bench_triangulate_prolog.sh`); then the per-kernel multiples climb with the runtime services rewired — the T ladder's cell services are the substrate)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐P1 | calling-convention-depth-tracked | hq_P | = the row's DONE-WHEN: sieve+bubble m4 rc=0, optbypass not regressed, SNOBOL4 FAIL=0 both modes over the printed denominator (the critical path: C17, C21, P8 wait on it). fbench is NOT this class — measured: still SEGVs after both arms inside transitxsurface_omega with walls=0 in every proc; it is the nested-proc epilogue-γ promotion site and belongs to the port-exit value-contract row's witness list (ceo ruling 2026-09-01 on hq_P's ask) — **DONE 2026-09-02 09:05** (landed `f9a90958` under the landing rule; `done` rc=0 on pristine `c6190d9e`; seven BLOCKED-ON rows self-cleared: C21 C14 C17 P8 + three off-ladder) |
| ⭐P11 | port-exit-value-contract-untagged-rax-forges-dt-fail | seat | a port exit may hand an UNTAGGED raw value into a calling convention whose failure sentinel ordinary data can forge — a SILENT WRONG ANSWER across three languages (18 sites; fbench.pas is its witness, not P1's); hq_P ruled the sentinel design ((c), taken whole) — cross-language doctrine of P1's family, hence rank 0 beside it |
| P2 | prolog-plw-floor-bypass-safety-unproven | seat | row DONE-WHEN once hq_P cuts it (REFUSE(2) today) |
| P3 | bench-grids-rebase-to-two-number-basis | hq_B | the two shipped grids republished on WORK/OVERHEAD |
| P4 | bench-triangulation-rerun-fixed-harness | hq_P | three angles re-run on the cured harness; P9 DONE 19:20 → un-parked FREE rank 1 by ceo (TRIO cycle 2; the triangulation table is the MEASURED bucket P9's board reads); AGREE rows published, DISAGREE withheld |
| P5 | prolog-hot-services-by-ir-with-owning-slice | seat | ranked census of the Prolog runtime's hottest services by callgrind Ir at fixed work, each tagged with the T slice that owns it (hq_P mint 2026-09-01) |
| P6 | prolog-builtins-wired-at-compile-time-not-dispatched-by-name | hq_P | Prolog builtins dispatched by NAME at run time → wired at compile time (Lon s264: the compiler does the extra work, not the runtime); measured on P5's top service first — MINTED by hq_P 2026-09-01 ~20:20 at rank 1, deliberately NOT blocked on P5 (a seat can measure by_name_dispatch.c:5454 vs :5456 today) |
| P7 | builtin-setjmp-mechanism-and-perf-reland | hq_P | the setjmp win re-landed with its mechanism established (hq_C's FENCE revert stands until then) |
| P8 | optimizer-off-path-segvs-so-the-emergency-bypass-is-not-a-correct-path | seat | BLOCKED-ON P1 |
| P9 | prolog-vanroy-21-board-two-number-basis | seat | the van Roy board published on WORK/OVERHEAD, three angles, vs swipl and gprolog; a kernel joins the grid only output-exact (hq_P mint 2026-09-01) |
| P10 | prolog-trail-mark-crosses-call-boundary-in-fixed-rsp-slot | seat | the Prolog implementer of P1's doctrine — trail mark transported in the fixed [rsp+32] slot across the call boundary, measured witness set (hq_P mint 2026-09-01) |

### LADDER I — Instruments and dispatch (owners hq_B + ceo · GATE: every ladder gate above is a computed instrument with a SCORE.md row; the dispatcher serves rungs first; the walk runs clean)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐I10 | optbypass-pin-stable-subset | hq_C | the optbypass watermark pins a STABLE subset, so an XFAIL-marker promotion (population 1654→1646→1726…) no longer re-breaks `make test` for every seat and no longer blocks a landing (P1 held by it 2026-09-01 evening); interim protocol: a promotion that moves the population re-pins in the same commit or is not pushed |
| I11 | donewhen-lines-that-cannot-parse-as-shell-so-done-can-never-close-them | hq_C | every live DONE-WHEN parses as shell (`bash -n` is the narrower question — the row grades that each can REACH a verdict: rc 0/1/2 on a probe, never a syntax error); hq_C mint from its own s8 slip |
| ⭐I12 | scoped-kill-helper-only-pids-under-my-root | seat | `s4e_kill_mine.sh <pattern>` signals only PIDs whose /proc cwd is under the caller's root, refuses box-wide forms; digests name it and ban `pkill -f` (seat07 self-report; hq_P 18:28) — RE-RANKED 1 → 0 by ceo 2026-09-01 ~18:33 at the FLEET-16 flip (agreed with Lon before it): the two instruments that made sixteen seats expensive this afternoon land before any other rung |
| I13 | census-function-scope-mutable-statics-under-src | seat | every function-scope mutable `static` under `src/` converted to read-at-use or carrying a cited grant on its line, and the NO-NEW-GLOBALS diff check extended into function bodies (RULES.md line 169 clarification `ecd9001c`); minted by hq_P 2026-09-01 ~20:26 at rank 1 on the ceo ruling — the five `zd_plan` siblings are its first witnesses |
| ⭐I14 | dead-ast-walkers-still-linked-into-libscrip-rt-so | seat | LON ORDER 2026-09-01 ~20:53 (*"Remove the dead AST walkers."*): delete `scope_patch`, `static_get/set`, the `driver_call.c:139-380` executor and `g_exec_prog`, and the stubs `eval_ast`/`eval_ast_pat`/`eval_node`/`exec_stmt` with their externs from the runtime library — `nm -D out/libscrip_rt.so` exports none of them; rank 0 |
| ⭐I15 | gates-scan-retired-directories-and-report-clean-on-trees-they-never-opened | seat | FALSE GREEN (hq_C, rank 0): `test_gate_no_c_to_bb.sh:40` scans `src/machine` and `src/contracts`, which do not exist since the re-grid, and never scans `src/ir` (their merged home, 26 entries incl. IR.h/SM.h/descr.h/ast.h) — it greps a nonexistent tree, finds nothing and exits green; THIRTEEN scripts carry live references to retired `src/{contracts,machine,include}`; DONE-WHEN demands the right scope and a reached verdict, not rc=0 |
| I16 | convert-expression-holds-an-ast-pointer-and-eval-silently-wrong-answers | seat | `CONVERT(x,'EXPRESSION')` stores a `tree_t*` in a runtime DT_E cell; `EVAL(E)` prints a parse error at RUN TIME and exits rc=0 with no output in both modes (sbl prints 3) — compile at CONVERT time through EVAL's path, never hold an AST pointer in a cell, and a runtime parse error is a red exit; sibling of I14 (the deleted stubs were its only consumer); rank 1 |
| I17 | build-governor-holds-pristine-while-box-idles | seat | hq_P's `make pristine` sat 9.5 h overnight behind the fleet-wide build governor while the box idled (22:05 → 07:41); DONE-WHEN reproduces the hold on an idle box and proves the release; minted by hq_P on the ceo ask 2026-09-02, seat01 holds it; rank 1 |
| I18 | banner-perturbs-the-seats-own-checkout-measure-first | seat | hq_P's lead: does `s4e_msg.sh banner` (the Stop hook) build or census in the seat's own checkout on every response, perturbing live measurements? The REBUILD arm is already cured (`handoff_status.sh:130`, `--skip-pristine` default) so the row is scoped to the untested CENSUS arm — a refutation is as valuable as a cure; minted by hq_P 2026-09-02 on the ceo ask; rank 1 |
| I19 | master-suite-builder-honours-deferral-contract-and-scopes-absorption | seat | ceo ruling on hq_P's ask (FINDING `ff4b0aa2`): `util_build_master_suite.py` reads PENDING.md/KEEP.md (the gate enforces a contract the builder cannot see), gains `--absorb-only`, validates before it writes (today it rewrites the master then refuses rc=2), refuses unknown flags with `--help`; minted by hq_P 2026-09-02, seat01 holds it; rank 1 |
| ⭐I8 | master-suite-board-refuses-under-fleet-load-slot-lock-or-load-scaled-timeout | seat | WHO KILLS HARNESS RUNS BY NAME: four refusals across two seats in one 3-minute window (18:17–18:20) BEFORE the known 18:28 pkill → a FINDING names the sender with evidence, and a killed run prints its signal and cause (depends on I9). The capacity premise in the slug is RETRACTED by its author (seat06: board PASSED at loadavg 27 in 500 s); seat04's 748 s under load stands — callers' budgets exceed it or run detached |
| I9 | test-corpus-snobol4-refusal-discards-the-harness-stderr | seat | the board stops piping the harness through 2>/dev/null: a refusal, a kill and a crash print DIFFERENT things (precondition of I8's "who sent the SIGTERM" step; hq_C mint from seat11's finding) |
| ⭐I7 | park-marks-last-row-only-when-clearing-own-claim | hq_B | `park` marks `.last-row` only on the branch that clears the caller's own claim; gate proves unclaim/done/park-own each write it and park-not-owned leaves it (seat14 FINDING 0803ecca; rank 0 because the FLEET-16 all-hands told seats to park) |
| ⭐I1 | make-pristine-per-root-flock-second-builder-waits | seat | two concurrent `make pristine` in one root both exit 0 and the binary passes the smoke — RE-RANKED 1 → 0 by ceo 2026-09-01 ~18:33 at the FLEET-16 flip (agreed with Lon before it): the two instruments that made sixteen seats expensive this afternoon land before any other rung |
| I2 | next-tiebreak-by-mint-time-not-file-order | hq_B | a newer row at equal rank is served first; proven both ways by a gate |
| I3 | score-md-master-board-row-every-language | hq_B | SCORE.md carries the master-board row for every language that has an `ALL.*` suite (Prolog's was missing 2026-09-01) |
| I4 | prolog-rung-suite-prints-the-name-of-every-red-rung | seat | `test_prolog_rung_suite.sh` prints the NAME of every red rung beside its totals (proof at mint: "FAIL=11 but 0 RED lines name a rung") |
| I5 | prolog-term-wordref-ratchet-gate-in-make-test | seat | per-file `Term` word-ref ratchet gate in `make test` (pins from the 490 census; red on any rise; refuses rc=2 when it cannot see src/); hq_C checked: no T slice carries a make-test gate |
| I6 | sweep-free-rows-are-real | ceo | the standing ceo umbrella: every sitting runs the visibility census AND `util_ladder_walk.py`; the plan's tables are rewritten from the output (cadence widened 2026-09-01) — the standing umbrella: the D0 sweep of 76 DONE rows ran under it (CEO-174; the former I24 line) |
| ⭐I20 | score-md-rows-are-rewritten-by-the-runner-that-measured-them | hq_T | `util_score_row.py`: one row from numbers a runner ALREADY measured, never re-running a suite; every runner calls it; the SEPTEMBER 10 GRID cells included |
| ⭐I21 | board-packages-into-make-test-reported-then-blocking | TO-MINT:hq_T | `board_packages.sh` runs every package gate with printed denominators; REPORTED under `make test` by D1, BLOCKING at the measured floor (ratchet) by D5 |
| I22 | ladder-walk-reads-all-seven-language-ladders | TO-MINT:hq_T | `util_ladder_walk.py` parses the SNO/SC/ICN/PAS/RAKU/REB tables and the new `LADDER:<tok>` LINKS tokens; V6 orphan = any row of any language on no ladder |
| I23 | xfail-entries-carry-a-reason-or-are-reds | TO-MINT:hq_T | SUPERSEDED 2026-09-03 22:50 by THERE IS NO XFAIL (Lon 21:30): a gate certifying that every xfail carries a reason would certify the resting state Lon abolished; folded into I29 + the per-language resolution rows |
| I25 | polyglot-demos-board-one-number | TO-MINT:hq_B | SCORE.md carries 3/7 at one row and 8/2 at another → one tree-labelled number; demo05 SIGABRT and demo10 wrong answer named |
| I26 | readme-perf-grids-three-angles-all-seven | TO-MINT:hq_P (D6) | every README grid triangulated, WORK/OVERHEAD basis with the ≥ 50% refusal (RULES CEO-173), tree/RT_OPT/mode/oracle named |
| ⭐I28 | progress-banner-percent-complete-per-language-from-the-leaderboard | hq_T (ASSIGNED, rank 1) | Lon ~18:33 (*"a score of percentage in a banner … a progress indicator of any kind"*): `util_score_row.py progress` prints ONE line from the grid (per language Σpass/Σtotal over M+V printed denominators, unmeasured suites at 0 over their file count, `?` on stale cells, ALL over all seven), the Stop-hook banner prints it last on every seat and HQ, and every runner prints it after rewriting its row |
| ⭐I29 | test-gate-no-xfail-survives-every-suite-xfail-population-printed-red-while-any-is-above-zero | hq_T (rank 0; Lon 21:30 THERE IS NO XFAIL) | `scripts/test_gate_no_xfail_survives.sh` in `make test`: prints `XFAIL_CENSUS <lang> csv=N files=M` for all seven from `ALL.csv` + `.xfail` files, rc=1 while any N or M > 0, rc=2 when a suite dir is unreachable; the score counts xfail as FAIL |
| ⭐I30 | ladder-forms-column-and-forms-check-every-declared-form-and-pair-has-a-witness | hq_T (rank 0, CEO-209) | one census schema for seven `LADDER.tsv` (FORMS, PAIRS columns) + `util_ladder_forms_check.py` with `--selftest`; every isolation/combination DONE-WHEN refuses rc=2 until it lands |
| ⭐I31 | feature-coverage-census-hundreds-per-feature-and-combinations-all-seven | hq_T (rank 0, CEO-208) | entries per feature and per feature pair over the feature columns all seven masters carry, per-language floors, a leaderboard cell |
| I32 | zero-byte-refuse-witnesses-cannot-be-ladder-graded-because-write-block-suite-appends-a-newline | hq_T (rank 1) | placed on the ladder by the ceo walk 2026-09-04: an instrument row, graded on its own witness |
| ⭐I33 | exhaustive-program-enumerator-to-length-n-with-oracle-divergences-as-witnesses | hq_T (rank 0, CEO-210; seat16 under FLEET-16) | `util_program_enumerator.py --selftest` green and `--lang snobol4 --to-length 6 --report` measured (rc 0 or 1, divergences NAMED as master witnesses); counts at N=1..10 in tokens and characters on the baton ledger (GEN_DEPTH) | 


### LADDER SNO — SNOBOL4 to 100% (owner hq_P · GATE: the `SCORE.md` snobol4 row reads M FAIL=0 both modes with every xfail reasoned, L `--to <top>` green, V csnobol4_suite / gimpel / snoflake / aisnobol / dotnet each 100% over printed denominators by their own oracle)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐SNO1 | snobol4-csnobol4-suite-non-pass-censused-by-class-and-cured | TO-MINT:hq_P | reconcile the grid's 58/58 (`4118b58f`) with the detail 52/118 (`d24e99d8`) FIRST — one number, one tree; then 25 FAIL + 40 REJECT + 1 CRASH censused by class, each class a child row or a reasoned xfail; oracle csnobol4, never sbl |
| ⭐SNO2 | snoflake-suite-scrip-only-gap | hq_P (re-ranked 1 → 0 CEO-174) | snoflake 180 `@expect` files graded on one tree with a printed denominator, reds by class |
| SNO3 | snobol4-gimpel-aisnobol-dotnet-measured-with-dedicated-gates | TO-MINT:hq_T (gates) · hq_P (cure) | gimpel 289 / aisnobol 8 / dotnet 14 each have a gate that prints its denominator and a SCORE.md row; reds by class |
| SNO4 | snobol4-master-xfails-reasoned-or-cured | TO-MINT:hq_P | every `ALL.xfail` entry (70) carries a reason a reader accepts or is a red to cure; the count published beside the M cell |
| SNO5 | snobol4-construct-ladder-from-rung-0-with-trace-refs | seat12 → hq_T (audited GREEN CEO-173) | rungs 0–9 green (landed); closes through the computed done |
| SNO6 | snobol4-ladder-top-rung-census-and-rungs-10-to-top | TO-MINT:hq_T (write) · hq_P (cure) | `config/LADDER.tsv` from the green-book chapters; `--to <top>` green both modes |
| SNO7 | snobol4-parser-fixtures-and-port-trace-gate-against-ampersand-trace | seat12 → hq_T (rank 2) | lands if within reach this week |
| ⭐SNO8 | division-by-zero-is-a-silent-null-not-an-error-in-at-least-snobol4-and-icon | hq_C (shared-node cure; found hq_P, rank 1 owner hq_C per CEO ruling 21:03) | green-book arithmetic error handling: `1/0` raises SPITBOL's **error 014** and an armed `SETEXIT` + `&ERRLIMIT` handler RUNS (witness `keyword_replace_1` prints `PREV=[]`, matching `sbl -bf`). ⛔ SHARED NODE — `binop_apply` (`lower_common.c:120`) returns FAILDESCR, i.e. FAILURE not ERROR; five lowerers reference TT_DIV and the languages disagree on the surface (SPITBOL 014 · Icon 201 · ISO Prolog `evaluation_error(zero_divisor)` · Pascal runtime error · Raku already has its own `rt_script_die_surface`). Branch on WHAT DIFFERS, never a language name. **Same topic is rung ICN-? on LADDER ICN (hq_B tables it) — one topic in two tables is legal to the walker.** CONTROL ARMS: SNOBOL4 FAIL=0 over the printed denominator + the Icon watermark, plus a proof the cure did NOT convert a legitimate FAILURE into an error (SNOBOL4 uses failure as control flow, so a `:F` branch on a genuinely failing expression must still be taken). Carries four of seat08's fourteen SNO4 classes: `keyword_replace_1`, `_2`, `branch_10`, `branch_11` |
| ⭐SNO9 | snobol4-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect | hq_P (rank 0; Lon 21:30 THERE IS NO XFAIL) | `ALL.csv` xfail column reads 0 of the printed total and `ALL.xfail` is empty or gone; master FAIL=0 both modes over the same denominator; each of the 68 resolved as test-fixed (ref re-cut from `sbl -bf`) or class-rowed and cured |
| ⭐SNO10 | snobol4-ladder-every-feature-in-isolation-with-variations | hq_P (rank 0, CEO-209) | every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_snobol4_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) |
| SNO11 | snobol4-ladder-feature-combinations-after-isolation | hq_P (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |

### LADDER SC — Snocone to 100% (owner hq_P · GATE: M FAIL=0 both modes over the printed denominator (273 today), corpus suite 10/10, L `--to <top>` green)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐SC1 | snocone-master-remeasured-on-origin-and-reds-classified | TO-MINT:hq_P | the 08-29 175/273 is STALE: re-run on origin, tree-labelled, reds by class (child rows), SCORE.md rewritten |
| ⭐SC2 | snocone-construct-ladder-and-parser-fixtures | seat12 → hq_T (re-ranked 1 → 0 CEO-174) | rungs 0–≥5 by D1 (the Snocone paper's census to top by D2); red rungs to hq_P |
| SC3 | snocone-crosscheck-remaining-parse-and-wrongoutput-gaps | seat12 → hq_T (rank 2) | folds into SC1's classes |
| ⭐SC4 | snocone-ladder-top-rung-census-from-the-snocone-manual-is-the-score | hq_P, seat12 walks (rank 0, minted ceo CEO-187) | `corpus/tests/snocone/config/LADDER.tsv` from the manual + the self-hosting `snocone.sc`, `--to <top>` green both modes — for Snocone the census IS the score (Lon 20:45) |
| ⭐SC5 | snocone-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect | hq_P (rank 0; Lon 21:30) | `ALL.csv` xfail 0 of the printed total (23 today); refs from the reference translator under `sbl -bf` |
| ⭐SC6 | snocone-ladder-every-feature-in-isolation-with-variations | hq_P (rank 0, CEO-209) | every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_snocone_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) — the 51-row census becomes a built ladder on the way |
| SC7 | snocone-ladder-feature-combinations-after-isolation | hq_P (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |

### LADDER ICN — Icon to 100% (owner hq_B · GATE: M **FAIL=0 over the printed denominator** run-graded AND ast, both modes — ⛔ the count is NOT pinned here: this line read `381/381` while the master stood at 409 run-graded of 562, and ICN1's DONE-WHEN pinning `entries=534` could never pass again (ceo CEO-179). A gate names the property; the runner prints the population. — and the STRICT rung suite FAIL=0 BADEXIT=0 with its 26 xfails reasoned, L `--to <top>` green, V arizona 89/89 · jcon_tests 81/81 · ipl graded with a number · jcon demo 4/4 byte-identical)

| rung | row | lane | gate |
|---|---|---|---|
| ✅ICN1 | icon-master-six-run-graded-reds-cured | seat03 → hq_B · **DONE** | 381/381 both modes; the three disagreeing readings (376/377/377) reconciled to one tree-labelled number first |
| ⭐ICN2 | icon-arizona-suite-49-reds-censused-by-class-and-cured | seat01 → hq_B | 45/89 → 89/89; the children `icon-arizona-class-*` (generator-forward-reference-reservation seat13, silent-segv-no-diagnostic seat13, vendored-ipl-library-residual-defects seat05 CLAIMED) re-laned to hq_B |
| ⭐ICN3 | icon-jcon-suite-39-non-pass-censused-by-class-and-cured | seat02 → hq_B | 43/81 · 41/81 → 81/81 both modes; the 2 "not fully reconciled" named first; child `icon-jcon-class-genhost-recursive-generator` seat14 → hq_B; the jtran link (the former ICN6) was CURED by hq_C before its mint — SCRIP `f4d69ac83`, no frozen-model change — so `test_demo_icon_jcon.sh` 4/4 byte-identical rides here |
| ✅ICN4 | icon-master-board-is-two-below-watermark-and-the-board-never-names-the-failures | hq_B · **DONE, then found FALSE GREEN, then corrected** | FIRST: the board names every failure (`--by-modes-column` prints only the two summary lines — hq_C confirmed independently) and the three readings in one day (377 hq_B `e751405f` · 378 hq_C `f4d69ac83` · floor 379) become ONE tree-labelled number; ICN1 cannot start without it |
| ICN5 | icon-ipl-runner-and-denominator | TO-MINT:hq_T (gate) · hq_B (cure) | 851 `.icn` graded (compile-graded at least; run-graded where a `.std` exists) with a printed denominator and a SCORE.md row |
| ✅ICN7 | icon-construct-ladder-from-rung-0 | seat01 · **DONE** (ceo-audited 56/56 + icont re-grade) | CHEAPER than assumed (hq_T): `test_icon_ladder.sh` already exists on the shared `lib_ladder.sh` body and refuses rc=2 correctly — the work is cutting witnesses and absorbing them into the master, not building a runner (the same for snocone and rebus); rungs 0–≥5 by H+2 from the Icon book's chapter census; red rungs to hq_B |
| ICN8 | icon-strict-rung-suite-reds-and-xfails-reasoned | hq_B (owner set CEO-181; assign to seat03/seat04 when free) | 4 FAIL + 1 BADEXIT cured, 26 XFAIL each with a reason, 1 XPASS promoted |
| ICN9 | icon-bench-correct-suspend-residue | hq_P (FREE, rank 0) | the perf arm; stays with hq_P |
| ⭐ICN10 | icon-ladder-top-rung-census-from-the-icon-book | hq_B, seat01 walks (rank 1, minted ceo CEO-187) | `corpus/tests/icon/config/LADDER.tsv` ≥ 10 rungs censused from the Icon book, `test_icon_ladder.sh --to <top>` green both modes, every rung with a refusing case |
| ICN11 | division-by-zero-is-a-silent-null-not-an-error-in-at-least-snobol4-and-icon | hq_C (shared node) | Icon runtime **error 201** is book semantics; a silent null is not. Tabled on LADDER SNO as well — one defect, two ladders, and hq_C cures the shared node (CEO-182) |
| ICN12 | icon-real-stringification-prints-17-digits-and-does-not-narrow-a-whole-valued-to-by | hq_B (rank 1 as a construct rung) | `2.0^0.5` → icont `1.414213562`, SCRIP `1.4142135623730951`; and `every 3.0 to 1.0 by -1.0` → icont bare `3/2/1`, SCRIP `3.0/2.0/1.0`. ⛔ Establish FIRST whether real Icon NARROWS a whole-valued to-by result or only renders it differently — the two cures differ and only one is right (seat01 flagged the ambiguity; hq_B verified both readings against icont). ⚠ Precision affects EVERY program that writes a real, so census the blast radius before cutting refs |
| ICN13 | icon-member-on-a-table-returns-the-stored-value-instead-of-the-tested-key | hq_B (rank 1) | `insert(t,"a",42); write(member(t,"a"))` → icont `a`, SCRIP `42`. member() is a predicate that succeeds with its ARGUMENT, not a lookup; the wrongness is silent whenever key and value print alike (seat01 found, hq_B verified against icont) |
| ⭐ICN14 | icon-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect | hq_B (rank 0; Lon 21:30) | `ALL.csv` xfail 0 of the printed total AND zero `.xfail` marker files under the STRICT rung suite (27 today); refs re-cut from `icont`/`iconx` or the defect cured |
| ⭐ICN15 | icon-ladder-every-feature-in-isolation-with-variations | hq_B (rank 0, CEO-209) | every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_icon_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) |
| ICN16 | icon-ladder-feature-combinations-after-isolation | hq_B (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |

### LADDER PAS — Pascal to 100% (owner hq_P · GATE: M 149/149 both modes, V fpc_tests 181/181 vs `fpc -Miso` and the ISO 7185 PAT suite vendored and graded, L `--to <top>` green including rung09)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐PAS1 | pascal-fpc-suite-62-reds-censused-by-class-and-cured | seat10 → hq_P | 122/181 → 181/181; the 59 by class |
| ⭐PAS2 | pascal-master-eleven-reds-cured | hq_P (minted by ceo CEO-175, FREE rank 0) | m3 153/164 · m4 153/164 → 164/164: ELEVEN reds, not five (hq_P 21:00 — seat09's five plus rung09_strings and five `parser__*` in nobody's ledger); one census, cured by class |
| PAS3 | pascal-ladder-rung09-strings-packed-array-equality | TO-MINT:hq_P | rung09 green both modes (ISO 7185 packed-array `=`, run-time error 102) |
| PAS4 | pascal-parser-fixtures-and-the-iso-7185-pat-suite | FREE → hq_T (vendor + gate) · hq_P (cure) | the public suite ANNOUNCEMENT.md names: vendored under `packages/pascal/`, graded vs fpc, SCORE.md row |
| PAS5 | pascal-m4-site1-forloop-backedge-64byte-excess | hq_P | frame/perf row (its two siblings `pascal-fbench-nested-function-self-assign-null-name` and `pascal-quick-m3-recursive-reps-cliff-13` stay BLOCKED-ON `pascal-m4-for-spine-leak-64b-per-iter`); behind PAS1–3 in rank |
| PAS6 | pascal-ladder-top-rung-census-and-rungs-10-to-top | TO-MINT:hq_T | ISO 7185 §6 census → `config/LADDER.tsv`; `--to <top>` green |
| ⭐PAS7 | pascal-iso-7185-validation-suite-pat-vendored-and-graded | hq_T vendors + writes the runner (rank 0, minted ceo CEO-187); reds → seat11 class rows for hq_P | `PAT_SUITE_BOARD` printed both modes vs `fpc -Miso`, SCORE Pascal V cell via `--suite PAT` — THE ISO 7185 denominator beside fpc_tests |
| ⭐PAS8 | pascal-ladder-every-feature-in-isolation-with-variations | hq_P (rank 0, CEO-209) | every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_pascal_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) |
| PAS9 | pascal-ladder-feature-combinations-after-isolation | hq_P (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |
| PAS10 | pascal-fbench-nested-function-self-assign-null-name | hq_P (rank 1) | placed on the ladder by the ceo walk 2026-09-04: a Pascal class row, graded on its own witness |
| PAS11 | pascal-quick-m3-recursive-reps-cliff-13 | hq_P (rank 1) | placed on the ladder by the ceo walk 2026-09-04: the m3 recursion cliff, graded on its own witness |

### LADDER RAKU — Raku to 100% (owner hq_T · GATE: M ast_fail=0 over the printed total, smoke FAIL=0 REFUSED=0 both modes, V roast graded with a number and a hash, L `--to <top>` green)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐RAKU1 | raku-master-ast-parity-mismatches-exposed-by-the-repointed-runner | hq_T (re-ranked 1 → 0 CEO-174) | ONE run-graded red (`method_sub_for_replace_1`, both modes) → 0; AST parity already FAIL=0 (83/97, 14 xfail) — the row's minted 42 was the ceo's false board (harness modes trap, cured `ca96ba948`); hq_T re-cuts GOAL + DONE-WHEN to the true board and keeps the slug |
| ⭐RAKU2 | raku-roast-100-percent-compile | seat07 → hq_T | `raku_roast_scoreboard.sh` prints a number with a tree label on SCORE.md (compile-graded roast is the V cell's first floor; run-graded where refs exist) |
| RAKU3 | raku-smoke-two-refusals-cured-and-the-full-suite-runner-repointed | seat07 → hq_T | re-cut CEO-173, passes rc=0; closes through the computed done |
| RAKU4 | raku-smoke-724-inline-probes-absorbed-into-the-master-with-rakudo-refs | seat11 → hq_T | the 724 probes become master entries with Rakudo-cut refs (M's denominator grows; the landing states the new denominator) |
| RAKU5 | raku-silent-wrong-answers | seat11 → hq_T | by class |
| RAKU6 | raku-ladder-top-rung-census-and-rungs-10-to-top | TO-MINT:hq_T | roast S-number census → `config/LADDER.tsv`; subs+signatures, classes/roles, regex/grammars, exceptions, lazy lists (seat11's named-unbuilt list) |
| RAKU7 | resort-the-raku-master-into-the-builders-order | seat07 → hq_T (rank 2) | housekeeping; lands if within reach |
| RAKU7b | test-raku-ir-full-suite-skips-rc-0-when-its-population-directory-is-absent | seat07 → hq_T (rank 2) | the runner REFUSES rc=2 on an absent population (cured per SCORE.md; close) |
| RAKU8 | raku-frontend-real-world-syntax-gaps | hq_P (BLOCKED-ON RAKU2) | the perf arm opens when roast is graded |
| RAKU8b | bench-rivals-raku-pascal | hq_P (BLOCKED-ON RAKU8) | the README rivals grid, D6 |
| ⭐RAKU9 | raku-roast-run-graded-runner-and-score-cell | hq_T owns the instrument, seat14 runs it (rank 0, minted ceo CEO-187) | every roast `.t` RUN through scrip both modes and graded on TAP; `ROAST_BOARD total= m3_run_pass= … compile_only=` with the roast commit; SCORE Raku V cell — compile-only is not a score |
| ⭐RAKU10 | raku-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect | hq_T (rank 0; Lon 21:30) | `ALL.csv` xfail 0 of the printed total (156 today: 14 ast + 142 run, the 720 absorbed probes auto-marked at absorption) — each re-cut from Rakudo or class-rowed and cured |
| ⭐RAKU11 | raku-ladder-every-feature-in-isolation-with-variations | hq_T (rank 0, CEO-209) | write `tests/raku/config/LADDER.tsv` from the roast S-numbers first; then every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_raku_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) |
| RAKU12 | raku-ladder-feature-combinations-after-isolation | hq_T (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |

### LADDER REB — Rebus to 100% (owner hq_T · GATE: M FAIL=0 both modes over the printed denominator (48 today), smoke 4/4, L `--to <top>` green)

| rung | row | lane | gate |
|---|---|---|---|
| ⭐REB1 | rebus-master-remeasured-on-origin-and-reds-classified | hq_T (minted, FREE rank 0) | RE-MEASURED by hq_T at the mint (SCRIP `ac4e0bf4f` corpus `91a05530`, .github `309566d5`): the 0/48 is REAL — m3 0/48 with 45 FAIL + 3 HANG, m4 0/48 with 43 FAIL + crashes; the three hangs had been hidden for five days behind a bare 0/48. Reds by class, hangs first (a hang and a wrong answer are different defects); the least likely of the seven to reach a bare 100% in 37.5 hours — the plan says so here, not at H+30 |
| ⭐REB2 | rebus-construct-ladder-parser-fixtures-and-a-real-master | seat12 → hq_T (re-ranked 1 → 0 CEO-174) | rungs 0–≥5 by D1; the manual's census to top |
| ⭐REB3 | rebus-ladder-top-rung-census-from-the-rebus-report-is-the-score | hq_T, seat16 walks (rank 0, minted ceo CEO-187) | `corpus/tests/rebus/config/LADDER.tsv` from the Rebus report + reference preprocessor, `--to <top>` green both modes — for Rebus the census IS the score (Lon 20:45) |
| REB4 | rebus-benchmark-set-before-its-grid | hq_T writes, seat13 measures (rank 2, off-ladder, minted ceo CEO-187) | ≥ 3 Rebus benchmark programs with WORK/OVERHEAD stamps + `bench_triangulate_rebus.sh`; the B cell grid follows |
| ⭐REB5 | rebus-ladder-every-feature-in-isolation-with-variations | hq_T (rank 0, CEO-209) | every construct of the census in every FORM its reference lists, one tiny oracle-cut witness per form `ladder__rungNN_<construct>_<form>`, rung by rung from 0, both modes; `test_rebus_ladder.sh --to <top>` green AND `util_ladder_forms_check.py --phase isolation` rc=0 (Lon 2026-09-04: every feature in isolation with variations) — all 12 rungs are built, so forms and pairs ARE the remaining Rebus work |
| REB6 | rebus-ladder-feature-combinations-after-isolation | hq_T (rank 1 until isolation green) | every construct combined with the rungs below it (PAIRS column: the reference's own combinations, then loop/call/pattern-or-goal/I-O/data-structure), witnesses `ladder__rungNN_<construct>_with_<lower>`; opens when the isolation row is green; `--phase all` rc=0 |

## WHEN THE PLAN CHANGES

Lon's word changes it instantly and the ceo routes the change into this file the same sitting (MODE discipline). An HQ that needs a rung re-ordered, split, or added asks ceo with the measurement; the ceo rewrites the table in place and names the change in the commit. A rung that turns out to be a dead letter (its row SUPERSEDED/RETIRED) is removed from the table, never left as decoration — the instrument would otherwise report a MISSING rung forever, and a census that re-reports correctly-retired work trains its readers to skim it.
