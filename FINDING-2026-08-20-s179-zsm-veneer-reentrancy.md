# FINDING s179 (HQ, Fable 5, 2026-08-20) — THE WIRE BANK IS A GLOBAL, SO THE MACHINE COULD NOT EVEN BE OBSERVED SAFELY: ZSM-ALL's PERTURBATION WAS THE RTCC VENEER'S NON-REENTRANT rtccb

**One sentence:** widening the ZSM to every box (law 0b: `rbp@β == rbp@α`, `SCRIP_ZSM_ALL=1`) flipped verdicts on 52/106 pass-thru rows — almost exactly the `_fn`/EVAL/CODE (fragment/proc-call) road — because the event call was emitted as `x86("call")` = the RTCC veneer, which banks r10/r11 in the ONE GLOBAL `rtccb` block; an event firing inside emitted code that a C road had re-entered overwrote the outer road's banked wires, so the outer round-trip reloaded the fragment's continuations instead of its own.

## The rung (what landed)
1. **ZSM-ALL** (`x86_asm.h` + `runtime_init.c`): under `SCRIP_ZSM=1 SCRIP_ZSM_ALL=1`, EVERY box's four ports emit events — frameless boxes as kinds 5..8 (`α· β· ω· γ·`), normalized onto the existing FSM by ONE identity (`F := rbp@α`), so the β/γ checks read literally as Lon's law 0b. Zero new globals (one function local). Default OFF byte-identical; `SCRIP_ZSM=1` alone unchanged vs the s142 instrument.
2. **Audit round 1 (green witness `ptc0f_var2`):** γ bombed on the FIRST run — rbp moved α→γ, skew 19528 — a WHACK-OWNING box (the MATCH_END shape: its own body legally retires the construct frame between its α and γ, the cross-node-whack case the file already documents). Ruling applied: **β stays FATAL (the law); γ/ω/FSM-state checks on frameless boxes downgrade to counted-not-fatal** (an instrument that convicts passing programs cannot testify about failing ones). No per-op filter (the classification is "did THIS run's α push", not an op name); no new state (prints gated on the existing violation counters ≤ 8).
3. **Audit round 2 (the conviction):** with bombs quieted, the green witness passed clean — but `fwctx_standalone_ctl` (a PASSING program) went `match → nomatch` under the instrument, and the ptc board flipped **52/106**, the flip set = every `_fn` road row + all of classes 6/7 + ptx — i.e., precisely the roads that round-trip C ↔ emitted code.
4. **The asm receipt (TEXT, `ptc0f_fn2` under the env):** every `call rt_zdp_ev@PLT` sat inside `mov [rip+rtccb+56], r10 / mov [rip+rtccb+64], r11 / call / reload` — the veneer. The clobber: outer road banks its wires in `rtccb+56/64` and calls C; the C pump re-enters emitted code (the fragment); a ZSM event there re-banks the FRAGMENT's r10/r11 into the SAME global slots; when the outer C call returns, its veneer reloads the fragment's wires. Flat/var-road rows never re-enter emitted code mid-window — matching the flip census exactly.
5. **The cure (landed):** `x86_zsm_ev` now banks the caller-saved VM tier **on the stack** (`push r8..r11`, reentrant by construction) and calls **`call_bare`** — the PF-2 stack-pair argument in miniature, at the instrument site. Same rsp mod-16 parity as the old form; rax left unsaved exactly as the veneer form left it.

## ⭐ THE PF-2 IMPLICATION (routed to ARCH-PASSTHRU law 0a's case file)
Lon's ruling — the stack pair wins, r10/r11 freed, "the registers looked cool, but then you stored them" — is not only a cleanup: **a global wire bank makes the machine unobservable**, because ANY instrumentation, GC hook, or nested crossing that touches the bank inside an open window corrupts the outer activation's continuations. Stack-banked pairs are reentrant by construction. The instrument tripped over exactly the pathology PF-2 retires; PF-2 is therefore a prerequisite for trustworthy observation, not just for cheaper exits.

## First live data from the audited instrument (pre-cure run, to re-derive post-cure)
On `fwctx_conjunction` (the beauty frontier): ONE `γ· node=10112 with no live α (state=FRESH)` — a success transfer from a box whose α never fired — ABSENT from the passing control's run. Candidates: optimizer label-folding blind spot vs a real wrong-entry landing. **Re-derive under the cured (bare-call) instrument before acting** — the pre-cure run was a perturbed program.

## Status / receipts owed
Rebuild in flight at write time; owed: ctl stays `match` under ZSM_ALL · ptc board flip census collapses · plain board unchanged (m3 105/106 · corpus 330/7 · 324/12) · the fwctx γ-no-α re-derived.
