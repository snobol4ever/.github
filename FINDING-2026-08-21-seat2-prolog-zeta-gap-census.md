# FINDING 2026-08-21 (seat2, Claude Fable 5) — PROLOG ζ-CONVERSION GAP CENSUS: the SN4 road, the ten gaps, and the laws Prolog inherits

Method: 1,603-commit history scan (2026-07-18..08-21) + full read of ARCH-ZETA-LOCAL-STORAGE.md / GOAL-SNOBOL4-100.md durable sections / DESIGN-SN4-{REGISTER-PLANES,ZW-ZD-OPUS-PLAYBOOK,ZD5B}.md / EXTRACT-{Z4-R12,PL-FRAME}.md + code read of `src/contracts/zeta_{storage,choices,depth}.*`, the match templates, `lower_prolog.c`, the Prolog templates and runtime. Anchors spot-verified at SCRIP `7153ccd7`. Ordered by Lon in-chat this session: *"a new world of code… EXACTLY like SNOBOL4: THREE ZETAS, FORTH-style stack, all of it."*

## 1. THE SN4 ROAD (16 campaigns, the dependency order a sibling conversion walks)
C1-C2 **observable first** (ζ-pop census, per-box profiler) → C3 **values** (FORTH cells for every value kind, shape-invariant geometry) → C4 **addressing** (ONE offset function `x86_frame_off`, rsp base) → C5 **design by hand** (seed `.s` embodiments ruled by Lon BEFORE compiler changes; basis closed at {RSP,RBP}, `c26a398a`) → C6 **the leap**: universal per-BB claim through α/β glue hooks, across the board never construct-at-a-time (`158738e8`/`f52d5877`), ZD-1 non-popping spine default-on (`d492b909`) → C7-C11 **consolidation by deletion**: NOFC default (`259b9cdb`), CARVE-KILL (`ef9a7d2c`+`1ba33ea6` — whole-graph prologue/epilogue emitters deleted; SN4 survived because it was already per-BB, **Prolog/Icon were not** — the stranding EXTRACT-PL-FRAME records), UCLAIM/whitelist/blob-frame/capture-global deletions, RBP ERADICATION (`a50037ae`, 879→1), wires-only linkage (`855a12a5`), FUNCTION LINKAGE = BLOB INTERFACE r10/r11 (`433dc27e`) → C12 **RBP earned back as THREE ZETAS** (`bfd8e00e` STANDING/FRAME/SPINE behind one `frame_need_of()`; `db728001` cells substrate is THE routing, one system) → C13 DEFINE-at-statement, Greek labels (`fd16fbea`), seam-walk → C14 **ζ-ONE + the ZDP lattice + ZSM** (`4453b165`, `adc17627`; every-port physical-depth probe) → C15-C16 **laws mechanized**: per-op filter and declaration tables die into the lattice (`44b8b82c`/`18d5bfe1`), FENCE0 whack + FENCE1-DEPTH, seam tiers incl. HQ-73 ALT tier-3 (`28e2122a`), resume-surface-=-rightmost (s190), PF passthru laws (scratch leaves in own frame `aab14749`; **choice record homes in the nearest enclosing activation frame; ALT pushes NO frame of its own** `75e3170e`), ZSM-ALL + the automatic bug finder (`85bc4b99`).
Nearly every campaign is class (i) shared emitter/templates/contracts — **Prolog inherits the lattice, tiers, wires, encoders, and instruments for free.** Prolog's own substrate landed mid-road (C9): `pl_cells_graph` + 8 PL-ZK admission arms (emit.cpp:2237-2245, K=0 kinds), proc-entry cells carve (ZK-3), DISJUNCTION/retry wiring (ZK-4), zframe stamp + FR-4 β-resume (PL-FR); the last Prolog-specific commit is `00e05838` (PL-Z-1a γ-retain arm, opt-in). **What Prolog is owed is the C12-C16 tail.**

## 2. THE TEN GAPS (current mechanism → SN4 mechanism; file:line at `7153ccd7`)
1. **γ-release vs γ-RETAIN (keystone).** Prolog zframe epilogue-γ does `add rsp,kt` (xa_flat.cpp:548-589), releasing the activation a CP points into; β must rebuild a fresh frame. SN4/Icon LIFO CONTRACT (Lon s242): α carves, ω tears down, γ RETAINS; canonical law gprolog `Local_Top=max(B,E)` (wam_archi.def:120). Opt-in twin exists (`SCRIP_PL_GAMMA_RETAIN`, xa_flat.cpp:470); enabling pair = caller re-anchor + top-graph exclusion (FINDING-2026-08-16-CLAUDE-PL-Z1A §3).
2. **Choice points: C-heap word stacks vs spine/frame records.** IR_MOVE_LABEL pushes clause-redo addresses via C calls onto `g_pl_retry`/`g_pl_cp_stack` (bb_move_label.cpp:19-31, rt.c:1883-1932); 0-operand IR_DISJUNCTION α pops-and-jmps (bb_indirect_goto.cpp). SN4: 32B choice record carved at α or homed at a depth-immune frame slot (`sn4_choice_rbp_off`, emit.cpp:2488), address-dispatch, LIFO; the scan-blob choice cell {resume · δ/TAG(0=CHOICE 1=BASE 2=FENCE 3=ARBNO_GUARD) · prev_CP · saved_MH} (x86_asm.h:2125, Lon 2026-07-28) is the designed WAM-CP twin (resume≡ALTB, prev_CP≡BB, saved_MH≡TRB).
3. **Resume protocol: 6-global pending-cursor handshake vs in-frame resume.** `g_pl_zf_pending_*` + `rt_pl_zf_resume_set/clear` + lexprep2 override + xa_flat γ-intercept + $trail_mark N0-SUPPRESS + zf3 triple stack + bcps fresh-frame re-open (rt.c:1961-1978, bb_call_proc_staged.cpp:833-871, bb_call_fn.cpp:369-383) — all consequences of gap 1. SN4/Icon: continuation lives at `[fb+resume_slot]` of the RETAINED frame. Largest single deletion.
4. **Trail marks.** `$trail_mark`/`$unwind_nothrow`/`$trail_unwind` node triples threaded through lowering (lower_prolog.c:821-851) + tm_lo/tm_hi shuttling. SN4 slot: the record's saved_MH quad. Trail DATA stays runtime (`g_pl_trail`, resolution.c:22) — RULING 4's arena law: undo survives forward whacks, does not commit on failure, commit-then-whack.
5. **Cut.** Compile-time ωeff rewiring only (thread_goals, lower_prolog.c:200-217); runtime CP entries already pushed are never truncated — a structural leak. SN4: TAG-2 watermark restore (FENCE mechanics; `x86_cell_cut_keep` x86_asm.h:2148); canon `Assign_B(BB(B))` / SWI `discardChoicesAfter`.
6. **Dual arms.** zframe (default, lower_prolog.c:1394) vs cells (opt-in `SCRIP_PL_CELLS`) with separate retry stacks and 8 `SCRIP_ZD_PL_*` killswitches. SN4 closed the identical fork: cells substrate is THE routing (`db728001`); the four-config selector is on SN4's own deletion list.
7. **Builtin call arms.** ZD spine-cell arm exists but ships a ZRES→FRQ DUAL-WRITE crutch (bb_call_fn.cpp:509,549) + an unconditional-β workaround for a BINARY `bused[]` node-index shift.
8. **Lattice coverage.** zdp_* switches enumerate SN4+Icon kinds; Prolog kinds ride ⊤/legacy. `zdp_has_pairs` already lists IR_DISJUNCTION (zeta_depth.c:62); IR_DISJUNCTION is deliberately NOT in the ALT tier family (a different family/template, not a per-op filter) — it needs its OWN tier admission, tier-3-shaped per HQ-73's motion definition.
9. **No statement bracket.** Prolog graphs never mint the statement-terminal structure op_zgpop keys on (choke emit.cpp:1054-1059 is language-blind and waiting).
10. **Register wires.** Prolog proc-enter γ/ω ride rcx/rdx (pre-C11 convention); the law is R10/R11 LIVE-never-scratch, WIRE-ORDER γ@+0/ω@+8 `push r11; push r10`, FUNCTION LINKAGE = BLOB INTERFACE (`433dc27e`). Also inherited hazard: THE RTCC BANK DOES NOT NEST — a C-entered blob without the register plane jumps residue (the SN4 M1 wall, s194); Prolog adds no such entry.

**Global census (eradication surface under the no-globals law):** ~40 `g_pl_*`/`g_resolve_*`/`g_plw_*` file-scope variables (list in the agent census, headline: `g_pl_retry*`, `g_pl_cp_stack*`, `g_pl_zf3*`, `g_pl_zf_pending_*` ×6, `g_pl_trail`, `g_resolve_*` interp engine). Gaps 2+3+5 delete the 15 CP/resume globals in one stroke.

## 3. THE TWELVE FACTS THE CONVERSION MUST HONOR (compressed; authorities in the docs-agent digest)
(1) tiers keyed by LIFETIME, verdict from `zdp_tier` — refusal to SPINE is the safety property (zeta_depth.c:131). (2) non-popping: α carves own K, γ suspends, ω frees own K, ONE terminal release; GLUE NEVER WHACKS. (3) four Greek ports; γ→β cross-box = 0 (s133 measured). (4) depth is a lattice WITH ⊤; ⊤'s only cure is a base that does not move. (5) a choice record is read at the depth it was written; resume surface = rightmost box. (6) tier admission is semantic (extend / leftward self-undo / re-yield-same-cursor). (7) wires R10/R11, RTCC does not nest, ONE-ENTRY. (8) m3 proves nothing about m4 — compiler-process facts need an emitted-image twin (seal law). (9) undo state in a base-pinned arena; commit-then-whack. (10) indeterminable extent ⇒ frame + O(1) whack; determinable ⇒ pure arithmetic; failure never whacks; derive-don't-accumulate. (11) ONE AUTHORITY per fact (x86_frame_off/zone_ref, zd_k, planner choke). (12) process law: no new globals without banner grant; killswitch byte-identity + md5 blast radius; pristine verdicts; ASM-DIFF-FIRST.

## 4. FALSIFIED-ELSEWHERE THAT BINDS HERE (do not re-spend)
α:β global-LIFO pairing instruments (α runs once, β re-fires); "passing sibling proves the offset" reasoning; per-op filters and declaration tables (semantics live in the lattice); ZW16 "CAS into ζ cells" reverted (RULING 4); per-statement rbp bracket (STF-UNFLIP `4c79de4c`); whole-graph carve re-lands (`7f1b77c7` "we scope at a different level now").
