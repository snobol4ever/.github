# GOAL-SN4-HOME-RBX — RBX = GC heap-top; allocation + GC coverage (HOME seat; master = GOAL-SN4-HOME.md)

## ⛔ TOOLING FIRST — `bash /home/claude/SCRIP/scripts/install_system_packages.sh` BEFORE ANY RUNG.
Idempotent; prints whether `gdb` is live. **`gdb` is MANDATORY** (MONITOR-FIRST step 2 *is* a gdb breakpoint with a spin/ignore counter). ⛔ Never hand-run `apt-get install gdb` — bare apt pulls Recommends `libc-dbg` against a stale index and 404s; that trap cost RBP-EARN seven sessions (s33–s39). Runtime `rt_*` symbols live in `out/libscrip_rt.so`: use `set breakpoint pending on`. Any tool genuinely missing ⇒ ADD IT TO THAT SCRIPT in the same push. Any prior cursor claiming gdb is unavailable is VOID — re-test.

**CHARTER (Lon s30):** SNOBOL4 addressing is RSP + RBP + **RBX GC-heap-top relative**. Formalize rbx as the allocation frontier (today's DESCR mint pointer), make emitted code allocate inline against it, and make the GC see every register- and arena-resident root. rbx is callee-saved — the C boundary is free, same property as rbp.

## ⭐ STATE OF THE TREE — orient from THIS. Refresh by MEASUREMENT every session.
⛔ Every row cites a command run against the working tree in the session that last verified it. A row verified only by reading a commit message or a prior FINDING's prose is NOT verified — that error cost s40c and s41 one wrong plan each.

| mechanism | landed? | commit | last verified how |
|---|---|---|---|
| X-0 CONTRACT written | **CLOSED s33** | — | § THE CONTRACT below is the deliverable |
| X-1 / RC-8a GC scan coverage | **CLOSED s33** | `9ecb75a9` | s33 gate `test_gate_rc8a_gc_coverage.sh` + positive control (`SCRIP_GC_UNROOT={cas,rtcc}` re-opens the hole) |
| CELL_HEAP RSP carve (s36 fork **a**) | **YES — since s37** | `9780591d` | s41b: `git log -- src/templates/bb_glue_flat.cpp` + live `IF(...CELL_HEAP...)` arms + HEAP re-sweep 36/122 |
| `x86_fc_hit`/`x86_fc_on` HEAP read-gate (slice-2 read side) | **NO — attempted, net-regressive, REVERTED** | `73c1ac33` → `69b51acc` | s41b BY-SET sweep 20/122 vs 36/122 floor |
| heap-block-relative residence (s36 fork **b**) | **NO — never attempted by anyone** | — | — |
| REG-4b rbx bump frontier | YES, but **dormant** — `ZC_PORT_HEAP` is not the compiled default | `x86_asm.h:2320-2333` | s41b: default port confirmed FORTH(6), `scrip.c:470` |
| X-3 HEAP floor | **36/122** (carve on, read-gate FORTH-only) | at `69b51acc` | s41b direct re-measure; matches s37 exactly |
| FORTH baseline (the invariant that must never move) | **77/122** | at `69b51acc` | s41b own-HEAD sweep, set-identical before/after revert |

## RUNGS
- [ ] **X-2 · FUNC-11 — ⛔ PREMISE CORRECTED s34: NOT AN ALLOCATION DEFECT.** Mechanism is a per-iteration RSP release imbalance (`+0xCC0`) on `-INCLUDE`d loop bodies (`FINDING-2026-08-12f`). Fix site NOT located; next search = how `-INCLUDE` is expanded upstream + `--dump-ir` diff between arms (⛔ NOT by grepping emitter/lowering for `INCLUDE` — zero hits, checked s34). **Manual-level argument, do not re-sweep:** SPITBOL v3.7 Ch.19 note 7 — a user-created name's variable block is *never reclaimed*, so runtime-created-variable population is unbounded and monotone **by the language definition**; a fixed include-scoped capacity is wrong by construction and cannot be fixed by raising a constant (three falsified threshold constants agree). Instrument first: counter in the runtime alloc path distinguishing STATIC from RUNTIME variable-block creation; show unbounded growth on the witness before proposing a fix. Also owns the `-INCLUDE` coverage hole (crosscheck has ZERO live `-INCLUDE`; promote `probe/rtx11_dynvar_{inline,include}` with BOARD). Witnesses (corpus `14dc06bd`): `probe/rtx_func_11_{inline,include}.sno` + `probe/rtx11_dynvar_{inline,include}.sno`, refs baked, two-sided. **ACCEPTANCE: `programs/snobol4/beauty_suite/` 34 files green both modes** (17/17 BEAUTY drivers SIGSEGV at HEAD). ⛔ **SEATING IS AN OPEN QUESTION FOR LON — see OPEN ASKS.**
- [ ] **X-3 · INLINE BUMP-ALLOC ARMS.** Hot mint paths go register-only against rbx (M-SLEN precedent: zero C calls), killswitched per family, both media, bisectable. **This is NOT "write a bump allocator"** — REG-4b exists and works; it is "promote an existing proven arm to the default port and make its residence real." ⛔ s40c's port-gate-only fix was measured BY SET as net-regressive (20/122 vs the 36/122 floor, 58 BROKEN / 1 REPAIRED) and reverted; s41b established there is no carve-then-gate sequencing to do (the carve landed at s37, before the gate-widen). **s42 answered the "why": see LIVE CURSOR — the divergence is a SECOND FORTH-only consumer gate, not `op_fc_base`.** Two costs X-3 owes at promotion time (neither owed to `pz` — § D): pin/range monotone growth per refill, and false retention from integers in ZH refill blocks that look like heap addresses. Measure both on a long-running program before promoting HEAP as default.
- [ ] **X-4 · ZHP EXHAUSTION CLASS RE-CHECK.** The s29 rc=134 arm (`[ZHP] heap exhausted`) is plausibly LOWER Defect B's mechanism — verify AFTER HOME-LOWER L-2 lands; do not inherit the plausibility as fact. ⛔ **REQUIRES predicate, re-evaluate EVERY session against `GOAL-SN4-HOME-LOWER.md` directly (never `git log` prefixes — that inference was wrong at s39, corrected s40):** L-2 was still `[ ]` unstarted at s41b, L-1 also `[ ]`; LOWER landed L-3/L-3b out of order. **STILL BLOCKED as of s41b.** Cheap relative to X-3 and fully in-charter (ZHP = the frontier's own exhaustion path) — run it the session the predicate flips.
- [ ] **X-5 · RTCC RESIDUE (RC-8b + RC-8c).** ⛔ Gate on X-1 is now GREEN (X-1 closed s33). **RC-8b arg tier** (rax rcx rdx rsi rdi): claim it or stop paying — 5 of the veneer's 14 instructions are pure cost at HEAD (stores reloaded by nothing). `wb_bin`'s RAX-as-base story must be PROBED before dropping — instrument RTCC s16c: walk the 12 `x86("rtcc_wb")` sites and identify the call each brackets; do NOT re-derive the false zero from `x86_rtcc_call` greps. **RC-8c:** stage XMM8–15 or AMEND the charter to 9 GPRs — no rung stands behind the XMM claim at all; amendment belongs on GOAL-RTCC. RC-5 anchor re-open + RC-7 fold stay PARKED in GOAL-RTCC pending Lon.

## GATES (every rung)
RC-8a coverage gate (self-arming) · probe + bench BY SET vs P0 floors · positive control on every census (a gate that cannot fail for the right reason is not a gate) · FINDING + cursor move.
**Per-rung bar for X-3 (offer, awaiting Lon):** *zero new BROKEN by set; REPAIRED may be zero* — the FORTH-superset condition is the rung-CLOSING bar only. Without this, a change trading 56 passes for 2 crash-fixes reads as ordinary partial progress (it did, for a full session).

## ⭐ LIVE CURSOR — 2026-08-12 (Sonnet 5, fresh seat) — **CENSUS CLOSED. ONE FIX LANDED (`fc_alt_active`), ONE ATTEMPTED AND REVERTED (`op_subj_cell`) — measured regressive 19/122 vs the 36/122 floor, root-caused to `x86_fc_hit` (still bare FORTH), not the registration side. Full detail: FINDING-2026-08-12v.**

Answered s42's own "CENSUS THE CLASS" rung completely: every bare `x86_port_mode()==/!=ZC_PORT_FORTH` in `emit.cpp`+`x86_asm.h` at current HEAD (`5547de99`, moved past this file's `a037b637` — re-verified live, not inherited), producer traced for each. Matched set was exactly the two FINDING-2026-08-12p already named. `fc_alt_active` genuinely was a mismatch (producer `fc_alt_register` port-blind) and widening it is measured byte-identical BY SET on both ports (FORTH 84/122 0/0, HEAP 36/122 0/0) — landed, needed one new accessor (`fc_cells_active()`, public wrapper over the private `fc_cells_on()`, added to preserve its ONE-PORT-OPINION law rather than break the encapsulation).

`op_subj_cell` looked like the same shape and is NOT: widening it to match the registration regressed HEAP to 19/122 (0 repaired, 17 broken — DIFF-dominant), same two headline witnesses (`038_pat_literal`/`039_pat_any`) s42 already saw when re-testing s40b's original patch locally. Traced the real mechanism this time, past the registration side-table: `FR`/`FRQ` → `x86_zop_regime` only returns the TOS-relative regime when `x86_fc_hit()` says so, and `x86_fc_hit` is STILL bare FORTH (the exact s40b/s41 site, never moved). Registration marks intent; `x86_fc_hit` decides where the byte physically lands. This is now a second, independently-measured data point for OPEN ASK #2 below (residence-class fixes are capped until `x86_fc_hit`/`x86_fc_on` move, already tried once, already reverted) — strengthens the seat's own standing recommendation **(i)**.

Also re-measured own-HEAD FORTH floor: **84/122**, not the 77/122 this file had recorded — floor drift, not a regression (three intervening RBP-EARN commits between `a037b637` and HEAD, all FORTH-side fixes; HEAP floor unmoved at 36/122, matching exactly). Recorded per STALE-ORIENTATION so the next seat doesn't misread it.

**NOT DONE:** X-3 rung itself unchanged (`[ ]`) — this narrows the open question, doesn't close it. X-2/X-4/X-5 untouched this session. `.s` artifact regen not applicable (byte-identical fix, nothing to regen). Local commit only in both repos — **push blocked on credential, per RULES 6b, asked in-chat this session.**

## ⭐ LIVE CURSOR — 2026-08-12 s42 (Sonnet 5) — **s41b's OPEN QUESTION ANSWERED. ZERO COMPILER BYTES; HEAD unchanged at `a037b637`.**

`op_fc_base`/`fc_geom` are **NOT** where HEAP-vs-FORTH diverges. Both were read in full, not inferred: `fc_geom()` (`zeta_storage.c:742`) is provably port-blind — its only port read is `fc_cells_on()` (`:268`), explicitly `FORTH || HEAP`, and K is 16 for these kinds in either port; `zls_build()` (`:430`) has **no** direct port read anywhere in its body. §A's "K is the SAME static-K grant" half therefore SURVIVES.

**The divergence is a SECOND, independent FORTH-only consumer gate that s40b's patch never reached** — `op_subj_cell` at **`emit.cpp:1358`**, gated bare `x86_port_mode() == ZC_PORT_FORTH` rather than the `fc_cells_on()` FORTH-or-HEAP class. The SUBJECT-CELL *producer* (inside `zls_build`, port-blind) carves the subject literal's cell and leaves it at TOS under HEAP exactly as under FORTH; the *consumer* flag is forced 0, and `bb_match_begin.cpp` branches its entire subject read on it — line 42 pops TOS when true, line 40 reads the flat slot `FRQ(op_sa)` when false. Under HEAP the head reads a slot the armed producer no longer writes: stale data, silent wrong answer, no crash. Comment-confirmed as known-mutually-exclusive design (bb_match_begin.cpp:41: *"on subject-cell-granted graphs the OFF world popped and never wrote op_sa"*). Same bug class as s40b's `x86_fc_on`/`x86_fc_hit`, different site. Full trace + citations: **`FINDING-2026-08-12p`**.

**METHOD (all four steps per s41b's instruction):** re-applied s40b's `73c1ac33` diff LOCALLY/uncommitted → sanity-checked it reproduces the two ORIGINAL witnesses' documented DIFFs (`041_pat_span` `'no digits'` vs `'12345'`; `158_pat_cap_arbno_each_iter` empty vs `a/b/c`) → found two NEW small two-sided witnesses (`038_pat_literal` → `no match`, `039_pat_any` → `no vowel`; both trivial single-primitive subject-position matches) → `SCRIP_RBX_FIELD_TRACE=1` FORTH vs HEAP, md5-stable across repeats. Trace shape: same 22 `X86H_DEF/ALPHA` lines both ports, but FORTH grants four boxes carve-only (`fc_bytes=16 fc_base=-1`) while HEAP grants ONE, windowed (`fc_bytes=16 fc_base=128`). Local edit reverted before write-up; `git diff` clean.

**⛔ NEXT RUNG — CENSUS THE CLASS BEFORE RE-WIDENING ANYTHING.** Do NOT fix `op_subj_cell` alone and re-measure: that repeats s41's lesson (one seam closed, others open, looks like progress, is net-regressive, wrong mechanism blamed). Grep every bare `x86_port_mode() == ZC_PORT_FORTH` / `!= ZC_PORT_FORTH` in `emit.cpp` + `x86_asm.h`; for each, check whether its PRODUCER-side registration is `fc_cells_on()`-gated (FORTH-or-HEAP) or also bare-FORTH. **Only mismatched pairs are bugs of this class** — a producer that is also bare-FORTH is consistent by construction and out of scope. Candidates spotted, NOT individually verified: `fc_alt_active` (`emit.cpp:1190`), `fc_seq_on` (`:1193`), POS/RPOS CONST-WPOP arm (`:1487`), bare guards at `:2053`/`:2639`, assorted `fc_*_active` predicates in `zeta_storage.c`. Fix the matched set in ONE commit, then re-run `board_patterns_set.sh` against the 36/122 floor. Acceptance unchanged: **BY SET, not by count.**

## ⭐ PRIOR CURSORS (condensed; full text in git history)
- **s41b (Sonnet 5)** — self-correction caught before push: s41's proposed "land the carve first" next rung was a NO-OP. `git log -- src/templates/bb_glue_flat.cpp` shows `bb_glue_flat_enter`/`_leave` converted bomb→carve at **s37 `9780591d`**, hours before s40b/s40c's session started; s37's own commit message calls it *"necessary but NOT sufficient."* Post-revert HEAD re-measured **36/122**, exactly s37's floor. **The s40c regression is 100% attributable to the gate-widen ALONE**, layered on an already-safe carve. Also produced the STATE OF THE TREE table above (its own offer #2, now implemented — a session orients from the table, not the prose).
- **s41 (Sonnet 5)** — ran s40c's mandated BY-SET sweep. **REGRESSION: HEAP 20/122 vs the 36/122 floor, 58 BROKEN / 1 REPAIRED.** Reverted `73c1ac33` via `69b51acc`; FORTH set-identical post-revert (77/122, 0/0). `FINDING-2026-08-12o`. The revert and the measurement stand; only its proposed next step was wrong (corrected by s41b, same session).
- **s40b** — field-attribution traced: `op_zls2_bytes` is DEAD at the `X86H_DEF/ALPHA` choke, `op_fc_bytes` is the only live grant, REG-4b's `hk` selector was never the problem. The blocker s37/s39 stopped at does not exist as stated.
- **s33–s40** — X-0 + X-1 closed; §A's "dormant-and-safe" claim falsified by measurement (see § A). Full text in git history.

## ⛔⭐ OPEN ASKS FOR LON (none actionable from a seat)
1. **⭐ X-2 SEATING — EIGHT SESSIONS UNCALLED (s34 → s42).** Mechanism named, fix site not found, every session records "seating still open" and moves on — correct per the self-gating protocol, and exactly how a rung starves. It is the flagship blocker (17/17 BEAUTY drivers SIGSEGV). **Does X-2 stay in RBX, move to RBP/EARN (it is an RSP-discipline defect — EARN's surface far more than the frontier's), or become a POOL item?** Any of the three unblocks it; none can be chosen by a seat.
2. **⭐ IS X-3's RESIDENCE HALF ACTUALLY REQUIRED FOR HOME? (charter ruling sought, s39.)** **No HOME GATE line names the heap port or residence.** GATE 3 names rbx only in the claim-gate sense (HAZARD-1's territory, filed to BOARD/WIRES). The charter's literal ask — the frontier, "today's DESCR mint pointer, formalized" — already exists, is corpus-exercised, and is GC-visible since X-1. Residence adds a ~5:1 blast radius (26 FORTH-gated consumer sites vs 5 HEAP-aware, 8 files) underneath `x86_zop`, THE ONE OPERAND ADDRESS AUTHORITY. **(i)** X-3 = fork (a) only, stop, spend the seat on X-4/X-5, residence re-opened post-HOME; or **(ii)** keep residence in scope and **add a HOME GATE line for it**. Seat recommends **(i)** — a rung this expensive with zero gate coverage is how a plan burns sessions on work its own definition of done never asked for.
3. **X-4's GATE IS PINNED TO SOMETHING NOBODY IS WORKING ON (five sessions).** LOWER landed L-3/L-3b while L-1/L-2 sit unstarted. **(a)** confirm the gate is real and X-4 stays parked, **(b)** move L-2 into THE POOL, or **(c)** re-examine whether X-4 truly needs L-2 — the stated rationale is that L-2's consumer hang is *plausibly* the ZHP mechanism, and *plausibly* is doing heavy load-bearing work for a five-session block. If only plausible, X-4 could run now and confirm or kill it, cheaper than waiting.
4. **THE ONE INVARIANT HAS NO MECHANICAL BACKING (s39 top ask).** It broke on **two seats in one day** (this seat s35/s36; WIRES s35). Recommend: **every session pushes a one-line seat claim as its FIRST action**, so the mandatory orientation pull reveals an occupied seat before a duplicate spends its investigation. Converts an invariant enforced by human memory into one enforced by git — the same move RULES already made for push-truth and register collisions.
5. **PROCESS:** `.github` commit `d3cf45df` writes push status into its commit message (*"Push status: NOT pushed, credential still pending"*) — the exact STALE-ORIENTATION (a) / RULES 6b violation. Another seat's commit inside a stack; **not rewritten unilaterally.** Reword-before-push or accept as-is: Lon's call.

## FILED OUTWARD (other seats / RULES)
- **→ MASTER FILE:** the self-gating protocol says skip a FALSE `⛔ REQUIRES` and never wait, but never says **re-test it next session** — a gated rung can starve while every session behaves correctly. X-4 is the live instance. One sentence fixes it: *walk your rungs top-down INCLUDING ones you skipped; a REQUIRES predicate is re-evaluated every session, never inherited.*
- **→ RULES.md — "TREE STATE IS READ FROM THE TREE."** This file has recorded the same failure three times (s40b, s41, s41b). Offer: *any claim about what is or is not landed must cite a command run against the working tree in the same session (`git log -- <path>`, `grep`, or a measurement) — never a commit message, a rung number, a fork-naming in a prior FINDING, or an adjacent session's prose.* Same shape as STALE-ORIENTATION (b), extended from cursors to code state; would have caught s40c, s41, and the s39 `git log`-prefix inference.
- **→ RULES.md (two more):** **(a)** *a static table is not an attribution* — `--dump-zeta` gives the offset space, never which mechanism granted a node's K; pairing asm hunks to it by eye produced a confident wrong attribution. **(b)** *a guard protects a call site, not a class* — `bb_glue_flat_enter`'s deliberate `x86_bomb` never fired because REG-4b is a structurally independent second authority for the same "K bytes for this box" decision.
- ⛔ **HAZARD-1 → BOARD + WIRES:** `xa_flat.cpp:520,526` clobbers rbx as a scratch save slot (Icon ICN-FR-2 zframe ω) — proves the claim gate does not yet cover rbx.
- ⛔ **→ BOARD:** all 15 `crosscheck/gc` programs trigger ZERO natural collections (instrument live: 2432 under stress). **The GC suite does not exercise the GC.**
- ⛔ **→ BOARD (instrument):** `board_patterns_set.sh snap` cannot finish inside this container's tool-call wall clock at `PAT_TIMEOUT=30`, and backgrounding does not survive across tool calls — it dies mid-corpus leaving a truncated, *plausible-looking* snapshot indistinguishable from a complete one. Offer: resumable/chunked mode + a `.complete` sentinel written only on full traversal. ⛔ Also: the s41b ad-hoc driver writes a FIXED filename and destroyed its own pre-revert snapshot via `mv` — do not reuse as-is.
- **→ BOARD:** promote `rtx11_dynvar_{inline,include}` to the named-witness layer; mint ≥1 live `-INCLUDE` member per rung suite (crosscheck's zero-`-INCLUDE` hole is a demonstrated correctness gap, not hygiene); pin the `beauty_suite` acceptance denominator (34 files vs 17 drivers).
- **→ BOARD/WIRES — `.s` ARTIFACT DEBT, NOT RBX'S:** committed `crosscheck/patterns/*.s` are stale at HEAD — drift is `mov r10,r12` → `mov r8,r12` (r10/r11 eradication family) plus an added `call rtcc_load_all@PLT`, 3 of 3 sampled, very likely corpus-wide. Both are WIRES/RTCC landings. This seat deliberately did NOT regen: doing so commits another seat's churn (~122 files) under an RBX cursor. Per RULES step 4 the regen belongs to the session that touched the codegen.
- **→ RBP/EARN:** `probe/rtx11_dynvar_include.sno` is the **first known RED-path instance** of the EARN-0b LIFO-violation class; the LIFO instrument has never been pointed at it. One command, likely decisive.
- **→ RBP / LOWER:** `ARB . A` capture diverges from oracle at HEAD; a capture loop SIGSEGVs where oracle prints 2290. Both pre-existing.
- **→ RULES.md (s34):** **(a)** an instrument must survive the failure mode under study (`SCRIP_ALLOC_HIST`'s `atexit()` cannot observe a crash class — its null result is uninformative); **(b)** a probe whose construction supplies a known threshold-shifting factor cannot test the factor it varies.
- **FLOOR DRIFT, named for BOARD (floors are BOARD's):** own-HEAD FORTH measured **77/122** at s41b vs 76/122 recorded earlier. +1, unexplained, not chased — almost certainly ordinary concurrent-seat drift per the STALENESS LAW. Recorded so a future session does not read a phantom +1 as its own repair.

---

# ⭐ THE CONTRACT (X-0 deliverable, s33 — ONE AUTHORITY for the SNOBOL4 register file and the rbx frontier)

⛔ **Supersedes REGISTER-LAYOUT.md entirely for SNOBOL4** (its "rbx = DESCR BASE POINTER" is the same frontier seen from 2026-05-31 and is SUBSUMED — a mint pointer IS a frontier). **DESIGN-SN4-REGISTER-PLANES.md's "rbx = VALUE plane / E frame base" is DEAD FOR SNOBOL4** — that doc's own status line says nothing implemented, and E-as-rbx is unrepresentable alongside the frontier claim.

### A · STATE AT HEAD — the frontier is BUILT, and it is NOT the default port
REG-4b (s78), `x86_asm.h:2320-2333`, gated `site==X86H_DEF && port==X86P_ALPHA && hk>0 && x86_port_mode()==ZC_PORT_HEAP`:
`mov rax,rbx` · `add rbx,K` · `cmp rbx,[RT_WS_LIMIT]` · `ja L60` → `mov edi,K` / `call rt_zh_bump_slow` / `mov rbx,[RT_WS_TOP]` · `L61`.
⛔ **`ZC_PORT_HEAP` is port 7; the compiled default is `ZC_PORT_FORTH` (6)** (`scrip.c:470`; `--zeta-port=heap`, `SCRIP_ZETA_PORT=7`). So rbx is **architecturally claimed but dormant on the default path.** K is `op_zls2_bytes` (when `ops==0`) else `op_fc_bytes` — the SAME static-K grant `fc_geom` hands the FORTH port, spent as `sub rsp,K` there and as the rbx bump here. Port-blind geometry, port-selected flavor. **s42 re-verified the geometry half directly: `fc_geom` and `zls_build` are both port-blind in code.**

⛔ **CORRECTED s36–s38 BY MEASUREMENT.** The original text claimed the HEAP block was "allocated-but-unread — the deliberate proving configuration, semantics untouched." **FALSE: HEAP scored 31–36/122 against a 76/122 FORTH baseline, with silent-wrong-output (DIFF) as the DOMINANT failure mode, not crash.** The bump half is genuinely sound and unread — that survives. What was false is the "so nothing happens" conclusion: consumers do NOT go quiet when they lose their FORTH window. `x86_fc_hit` hard-gated to FORTH, so under HEAP every granted box's reference fell through `x86_zop_regime` to regime 3/4 and read/wrote a **correctly-numbered but never-reserved** flat-frame slot. Dormant-and-safe was the claim; dormant-and-silently-corrupting was the fact.
⭐ **STANDING LESSON FOR THIS FILE:** *"the consumers stay gated so nothing happens" is an assertion about a FALLBACK PATH, and a fallback path is exactly the thing that must be measured rather than reasoned about.* The §A author reasoned it; three sessions inherited it; one 2-line witness falsified it.

### B · THE 16-ROW GPR MAP

| reg | class | SNOBOL4 role at HOME | who writes it | live across C? |
|---|---|---|---|---|
| **rax** | caller-saved | scratch; **fresh-block base out of the REG-4b bump** (HAZARD-3); X-5 candidate | anyone | no |
| **rcx / rdx / rsi** | caller-saved | scratch; RTCC arg-tier candidates (X-5) | anyone | no |
| **rdi** | caller-saved | scratch; first C arg; X-5 candidate | anyone | no |
| **rbx** | **callee-saved** | ⭐ **GC HEAP-TOP / ALLOCATION FRONTIER** — next free byte in the current ζ-heap block | **α only** (REG-4b) + flat prologue seed + `rt_zh_bump_slow` publish/reload | **YES — by SysV, and the contract depends on it** |
| **rsp** | special | FORTH spine — box operands, ζ cells, choice/resume records; compile-time-constant offsets always | carve (`sub rsp,K`) / cut | yes |
| **rbp** | **callee-saved** | **EARNED frames ONLY** (`frame_need_of`); ONE ENTER at α; `[rbp+ANCHOR]` chain to MATCH_BEGIN | **α/ω SOLE writer** (EARN-11) | yes |
| **r8** | caller-saved | RTCC slot | RTCC | via veneer only |
| **r9** | caller-saved | **GVA base** (RTCC LIVE claim) | RTCC | via veneer only |
| **r10 / r11** | caller-saved | **rΓ / rΩ wires** (both glue kinds, one product-wide convention) | template-emitted per-activation saves | via veneer/save only |
| **r12** | **callee-saved** | **capture-pending arena TOP** (CAS discipline, mmap'd); restored at backtrack re-entry (oracle pin W5) | CAS push/pop | yes |
| **r13 / r14 / r15** | **callee-saved** | **Σ subject BASE · δ CURSOR · Δ subject LENGTH/END** | match entry; δ's restore rides the choice record | yes |

**Corollary — the callee-saved set is FULLY SUBSCRIBED:** `{rbx, rbp, r12, r13, r14, r15}` = `{frontier, earned frame, pending arena, Σ, δ, Δ}`. Six of six. **No callee-saved register remains for any future claimant**; every further claim must come out of the caller-saved tier via RTCC veneering (X-5) or by evicting an existing tenant. Recorded before someone spends it twice.

### C · RBX = FRONTIER INVARIANT
1. **rbx always points at the next free byte** of the current ζ-heap block: `g_zh_block_base ≤ rbx ≤ [RT_WS_LIMIT]`.
2. **rbx is never a live-object pointer** — one-past-the-last-allocation. Nothing may dereference it; only `rax = rbx` (pre-bump copy) is an object base.
3. **`[RT_WS_TOP]` (`pin_va.h:11`) is the SYNC CELL, not the frontier.** Authoritative only (i) across a refill and (ii) at the outer-graph seed; STALE by design between them. Reading it to learn the frontier mid-graph reads a lie.
4. **Cold rbx is NOT self-healing** (`garbage + K` vs limit is unsigned luck). The outer flat prologue SEEDS rbx from `[RT_WS_TOP]` (zero page ⇒ rbx=0 ⇒ first guard trips ⇒ lazy init free). **Inner entries — jmp-entry procs, EVAL/CODE fragments — SEED NOTHING and inherit rbx live.** Their correctness rests entirely on § E.
5. **ω emits nothing.** No frontier decrement; reclamation belongs to the LIVE/DEAD lifecycle, never a pop. Allocation is monotone within a block.

### D · SAFEPOINT PORTS
A **safepoint** is any boundary where `gc_collect_ex` can run: every `call` that may reach `rt_gcheap_alloc`, plus `COLLECT()`. **α is a safepoint** (slow arm → `rt_zh_bump_slow` → `rt_gcheap_alloc` under `ZC_ZH_IN_GCHEAP=1`, `zeta_choices.h:108`). Roots gather at `gc_heap.c:625-638`: pins → ranges → WS interior → `rt_gc_ws_roots` → (env) static blanket → coexpr roots → **`setjmp(jb)` + conservative scan** → C stack → `gc_root_zeta` → core/gen/args → shields.
⭐ **The register file is rooted by the `setjmp` trick alone** (`:634`) ⇒ rbx/r12/r13/r14/r15 are GC-visible only on the `!pz && cons_stack` path.
⛔ **CORRECTED s33 BY X-1 MEASUREMENT — the original text predicted arming rbx would disable `pz` and charged that cost to X-3. FALSE: `pz` is ALREADY DEAD AT HEAD and X-3 does not owe it.** RTCC's constructor (`rtcc_init.c:24`) calls `rtcc_gc_register()` unconditionally with `g_rtcc_on` defaulting to 1, so `g_gc_rpin_n >= 1` before `main`, so `pz` is false in every run and `cons_stack` is forced to 1 (`:622`). Every `[GC-COV]` line across the X-1 census reads `pz=0 cons_stack=1` (2432 collections on one stressed program). **Standing lesson: I predicted a cost from reading the guard and was wrong about who already paid it — the census line is cheap, run it before pricing anything else.**

### E · C-CROSSING RULE
rbx is callee-saved under SysV, so **an ordinary C call preserves the frontier for free** — the property that makes rbp workable and the reason the charter pairs them.
- **No veneer is needed or permitted for rbx.** RTCC veneering exists for the caller-saved tier; adding rbx would create a second writer and break § F.
- **Any hand-written asm that clobbers rbx must save/restore it** — a live obligation, not hypothetical: see HAZARD-1.
- **Coexpression switch must carry rbx.** `bb_create.cpp:22` stores `{r12,r13,r14,r15,rbx,rbp}` into the coexpr frame — exactly the § B callee-saved tenancy, correct by construction. **Do not add a seventh name there without amending § B.**

### F · ONE AUTHORITY — who may move rbx
**Exactly three writers; no fourth without amending this section:** (1) **α under `ZC_PORT_HEAP`** — `add rbx,K` (REG-4b, `x86_asm.h:2323`), the only *fast-path* writer; (2) **the outer flat prologue** — seeds from `[RT_WS_TOP]`, the only *seeding* writer; (3) **the REG-4b slow arm** — `mov rbx,[RT_WS_TOP]` after `rt_zh_bump_slow` publishes, the only *refill* writer. Everything else — templates, RTX hand asm, runtime C, wires, ZCTX — **reads never, writes never.** Enforcement is a claim-gate row over rbx (HOME GATE line 3); the gate is what makes this checkable rather than aspirational.

### G · OVERFLOW → ISLAND GROWTH PATH
`rt_zh_bump_slow(bytes)` (`zeta_heap.c:152-168`): `need=(bytes+15)&~15`; `blk = max(need+16, 1MB)`; carve via `rt_gcheap_alloc(HB_ZBLK, blk)` **from the collected span**; on success **`rt_gc_root_pin_add(base)` AND `rt_gc_root_range_add(base, base+blk)`**; publish `[RT_WS_TOP]=base+need`, `[RT_WS_LIMIT]=base+blk`. Failure ⇒ `abort()` with `[ZH-BUMP] FATAL`.
- **The whole 1MB block is a root RANGE** — every bump-allocated object is conservatively scanned wholesale, which is what makes the inline fast path safe **without a write barrier**.
- **Old-block tail residue is LEAKED BY DESIGN (v0).**
- ⛔ **Pins and ranges are added and NEVER removed** (`root_pin_del`/`root_range_del` exist, never called here) ⇒ monotone growth of both the pinned set and the scanned area ⇒ **GC cost rises with refill count, forever.** A number to measure in X-3, and a candidate for the LIVE/DEAD promotion REG-4b's comment defers to.

### H · GC COVERAGE — X-1's work order, DISCHARGED s33 (kept as law + record)
**Pin ≠ scan.** `rt_gc_root_pin_add` keeps the *containing block* alive; `rt_gc_root_range_add` walks the *interior* and marks referents. **A pinned-only region's contents are invisible to the marker.** Covered by the correct (pin+range) pattern: ζ-heap refill blocks (`zeta_heap.c:161`), coexpr stacks (`rt_coexpr.c:74,170`), callee-saved registers via `setjmp` (conditionally — § D). Two gaps found and CLOSED at X-1 (`9ecb75a9`):
- **RTCC block** (`rtcc_init.c:26`) — pinned, never scanned; every DESCR it held was unreachable. Latent only because the arg tier is unclaimed — **X-5 makes it live**, which is exactly why X-5 was gated on X-1.
- **CAS capture-pending island** (`pattern_match.c:603-614`) — neither pinned nor scanned; carved from `rt_slab_region`, **not** the collected span. ⭐ **Root cause was a FALSE COMMENT, not an oversight:** the CAS-1 banner claimed coverage "by `RT_SLAB_GC_ROOTS` today" — that macro is `#define RT_SLAB_GC_ROOTS 0` (`rt_slab.h:14`) **gating ZERO `#if` bodies tree-wide**; it was the TR-3 libgc compensation, died with libgc at TR-4, and took the coverage with it while leaving the sentence. `rt_cas_roots` had zero consumers from the day it was written. Fixed via `rt_cas_live_span` + `gc_root_cas`; banner corrected in place.
⇒ **Positive control is mandatory on any coverage gate** — one that passes because nothing was allocated is not a gate; it must be shown to FAIL on a deliberately unrooted DESCR.

### I · SPITBOL SEMANTIC ANCHORS (manual v3.7 — the behaviour SCRIP owes, paraphrased)
- **Ch.4 Caution + Ch.19 note 12 — integer/address discrimination.** SPITBOL's collector separates small integers from addresses by MAGNITUDE, which forces object size below `&MAXLNGTH`/MXLEN and requires MXLEN to sit numerically below the workspace start. **SCRIP does not inherit that constraint** (DESCR carries a type tag; discrimination is by tag, not range) — but SCRIP's *conservative* `gc_cons_scan` reintroduces the same ambiguity wherever it scans raw ranges, which is now everywhere the frontier blocks live. Expect **false retention** from integers that look like heap addresses: a cost, not a bug, and it belongs in the X-3 measurement.
- **Ch.19 note 7 — variable blocks are permanent.** A user-created name (label, function, or variable) allocates a variable block that is **never reclaimed**; the manual explicitly steers associative data to TABLE for this reason. ⭐ **This is the semantic X-2 must satisfy:** runtime-created variables are an unbounded, monotone, never-freed population, so any *fixed* include-scoped capacity is wrong by the language definition — it has to grow. That retires the three falsified threshold constants by argument, not by another sweep.
- **Ch.19 note 8 — CODE blocks ARE collectible** when no longer accessible. With § C.4 (EVAL/CODE fragments inherit rbx and seed nothing), CODE-block collection is a re-entry edge the frontier must survive — belongs in the HOME GATE line 7 inventory.
- **Ch.11 `COLLECT(i)`** — forces a collection, returns free words, and **FAILS** when it cannot obtain `i` words. Failure (not an error) is the contract; SCRIP's `COLLECT` must preserve SNOBOL4 failure semantics. The manual also notes forcing collections early always costs time.

### J · CONTRACT HAZARDS (each with a site; none fixed in X-0 — zero code)
- **HAZARD-1 · `xa_flat.cpp:520,526` uses rbx as a SCRATCH SAVE SLOT.** The ICN-FR-2 zframe generator-ω does `mov rbx,rax` … `mov rbp,rbx`, i.e. it **clobbers rbx across a call**, relying on callee-saved to protect its own value. Icon's surface, not SNOBOL4's — but the **same emitter and the same `x86_asm.h`**, and a direct § F violation the moment the two planes share a graph (EVAL/CODE, coexpr, any polyglot path). **It also proves the claim gate does not yet cover rbx** — this would have fired. Assign: claim-gate row (BOARD/HOME GATE 3) + a WIRES-style scratch eradication for the Icon arm. Do not "fix" it silently from this seat; file it.
- **HAZARD-2 · `[RT_WS_TOP]` has two writers with different meanings.** `rt_zh_bump_slow` writes it as *the published frontier*; the flat prologue reads it as *a seed*. Nothing writes it back at graph exit, so the cell trails the register afterward. Benign today (only refill+seed read it); a live defect the instant anything else consults it. **Pinned here so it is never "fixed" by adding a third reader.**
- **HAZARD-3 · the fresh block base lives only in `rax` across the α slow arm.** Fast path `rax=rbx` pre-bump; slow path `rax` = `rt_zh_bump_slow`'s return. `rax` is caller-saved and **not in the `jmp_buf`**, so a collection between the bump and the first rooted store cannot see it *as a register*. **Currently harmless** because the whole block is a root range (§ G) — but that means **the block-range root is load-bearing for fast-path safety**, not a convenience. Anyone narrowing the range to live extents must add a barrier or root `rax`. Stated now, because the narrowing is the obvious future optimization.
