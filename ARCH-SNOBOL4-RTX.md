# ARCH-SNOBOL4-RTX.md — The SNOBOL4 Runtime in Optimized x86-64 Assembly

**Minted s162 (2026-07-25, Claude + Lon). READ THIS BEFORE ANY RTX RUNG. It is the register/ABI/build contract; the ladder lives in `GOAL-SNOBOL4-RTX.md`.**
**⛔ POINTER CORRECTED s186 (2026-07-27):** this line previously read *"the ladder lives in `GOAL-SNOBOL4-BB.md` §SN4-RTX"*. **That section had been deleted** at `.github` `0c595551` (s176), taking 8 unchecked rungs with it, while this pointer kept naming it — so anyone following this file's own instruction landed on nothing and could reasonably conclude RTX had never had a ladder. The ladder is recovered verbatim from `950e6a9f` and now lives in its own goal file, run concurrently with the ζ-storage ladder. **This is the (a)-class rot named in RULES.md's STALE-ORIENTATION rule: a document asserting a location it cannot know is still true.** Same lesson as §7 step 0 — a pointer, like a symbol name, must be verified against the tree, not inherited from prose.

## 1. RULINGS OF RECORD (Lon, s162 — all four forks resolved)
1. **SYNTAX: Intel, one project-wide.** GNU `as` with `.intel_syntax noprefix` — registers on the left, exactly what `scrip --compile` already emits (verified: `claws5.s` line 1). ONE assembler (as, via `gcc -c`), ONE syntax, everywhere. nasm remains oracle-tooling only (`build_spitbol_archive.sh`).
2. **C BOUNDARY: libc only.** No direct `syscall`. Revisit only if a measured rung nominates an I/O edge.
3. **MIGRATION: dual-build with per-family kill-switch, then eradicate.** C bodies stay as the fallback + bisection oracle until a family is watermark-proven, then that family's C is deleted.
4. **REGISTERS: phase-1 blob pins UNCHANGED; RTX owns the volatile nine internally.** Cross-boundary pin promotion and registerized signatures are phase-2 (RTX-CC), measured, per-family.

## 2. REGISTER CONTRACT
**Blob pins (UNCHANGED in phase 1 — RTX must preserve all of these across every entry/exit):**
| reg | role |
|---|---|
| rbx | arena heap top — DESCR mint pointer (16-byte DESCR in 64-bit) |
| r12 | ⛔ **FREE — NOT A PIN (corrected s205).** `ZC_FRAME_R12` was DELETED outright at ZR-RSPRBP-1 (Lon directive 2026-07-27, SCRIP `da8c2347`), zero `#if` consumers. ζ basis set is CLOSED at RSP and RBP. The prior entry ("conditional-assignment stack pointer — its own mmap area") is DEAD HISTORY. |
| r13 | Σ subject base ptr |
| r14 | δ subject cursor |
| r15 | Δ subject length/end |
| rsp | ζ + C stack (ONE stack — we do NOT adopt SPITBOL's compsp/osisp two-stack switch; our ζ frames already live on rsp, s157) |
| rbp | ζ frame base IN PINNED GRAPHS ONLY (suspended generators, pattern blobs, deep-arrival — prologue seeds `mov rbp,rsp`); **rsp** for depth-static determinate graphs (free GPR; `op_flat_disp` compensation). ⛔ **CORRECTED s205: prior entry said "ζ frame base" unconditionally — WRONG since FLATDISP-8 (s197) made the selection per-graph.** ONE selector: `x86_fb_pinned()` = `emit_rec_pin()`. **NEVER hardcode `[rbp+off]` or `[rsp+off]` in RTX asm or templates — use `FR(off)`/`FRQ(off)` which resolve via `x86_fb()` → `x86_fb_pinned()`.** Verified from `x86_asm.h:347-361`. |
**These six GP pins are exactly the SysV callee-saved set — the historical reason C runtime interop was free. RTX routines called via C signatures inherit that safety automatically; hand-written entries that clobber any pin must save/restore it explicitly. ⛔ r12 is NOT in this set (see above) — it is free.**

**RTX working set (free at every blob→runtime entry, zero boundary cost):** rax rcx rdx rsi rdi r8 r9 r10 r11 + xmm0-15. Internal role conventions (SPITBOL lessons, adopted):
| reg | RTX internal role |
|---|---|
| rax | value word of the descriptor pair; result; W0 |
| rdx | tag:slen word of the descriptor pair; mul/div companion |
| rsi / rdi | string source / dest — movs/lods/stos/scas/cmps native (SPITBOL xl/xr) |
| rcx | length/count — rep and shift-count native; `cl` for char work (SPITBOL wa) |
| r10 | idiv divisor scratch — keeps rdx clear of the div blast zone (SPITBOL's exact trick) |
| r8 r9 r11 | scratch / secondary args |
| xmm0 | REAL scratch (mxcsr note: set once at init; save/restore around libc math calls — int.asm precedent) |
**Condition results use EFLAGS + setcc/jcc, never SPITBOL's memory `fl` byte. `cld` is a global invariant: set at init; any `std` user restores before return.**

**THE DESCRIPTOR-PAIR CONVENTION (the ABI cornerstone, verified from `src/contracts/descr.h:28`):** `DESCR_t` = 16 bytes = word0 `{DTYPE_t v; uint32_t slen}` + word1 `union{char*s; int64_t i; double r; void*p; ARBLK_t*arr; …}`. Two INTEGER-class eightbytes ⇒ **SysV already passes a by-value DESCR_t in a register pair and returns it in rdx:rax.** Consequence: phase-1 ports that keep the C signatures get descriptor-in-registers in/out FOR FREE — no template churn, no convention invention. Phase-2 (RTX-CC) adds what SysV cannot: S/F signaled in EFLAGS instead of a DT_FAIL compare, >2-arg register staging, multi-value returns.

## 3. BOUNDARY PROTOCOLS
**blob→RTX (phase 1):** every exported symbol keeps its EXACT current C signature. Speed comes from the bodies (no -O0 C, fused fast paths, register discipline, rep-string idioms), not the call convention. Templates untouched ⇒ no `.s` regen, no both-medium work, in this phase.
**blob→RTX (phase 2, RTX-CC):** per-family registerized signatures; every template call-sequence change goes through `x86(...)` encoders in `x86_asm.h` per RULES (TEMPLATE-ONLY EMISSION, BOTH-MEDIUM); `.s` regen ×3 fires.
**RTX→libc:** `and rsp,-16`-equivalent alignment guarantee (track parity; the entry from blob is a `call`, so rsp≡8 mod 16 at entry — one push realigns), save live volatiles on the stack, `call foo@PLT` (m4) / `call foo` (m3 — same source, PLT relaxes), restore. **No SPITBOL-style global reg_block:** libc never inspects SCRIP state (unlike SPITBOL's osint functions, which read compiler registers from globals — int.asm:127-137). The pins are callee-saved, so libc preserves them for free.

## 4. FILE LAYOUT · BUILD · KILL-SWITCH
- **Source:** `src/runtime/rtx/rtx_abi.inc` (this contract as asm macros: pin names, save/restore macros, gate macro) + `src/runtime/rtx/rtx_<family>.s` (alloc, str, call, agg, arith, nv, match, pat, misc). `.intel_syntax noprefix` at top of every file. C code style rules do not apply to `.s`; comment generously — asm needs it (SPITBOL sbl.asm precedent).
- **Build:** each `.s` → `gcc -c` → linked into BOTH `scrip` (mode 3) and `libscrip_rt.so` (mode 4). ONE source, both modes identical by construction. Makefile: add `RTX_OBJS` to both link lines.
- **Kill-switch (per family):** C body renamed `c_<name>` (mechanical, one commit per family). asm exports `<name>`: `cmp byte ptr [rip+rtx_gate_<fam>], 0 ; je c_<name> ; <asm body>`. `runtime_init` reads `SCRIP_RTX_<FAM>` (default ON after the family's gates are green) into the gate bytes. Eradication rung deletes gate + `c_` body. Byte-identity proof at every landing: `SCRIP_RTX_<FAM>=0` run is md5-identical to pristine-C build output on the full crosscheck.

## 5. THE REPLACEMENT INVENTORY (measured s162 across all committed SNOBOL4 `.s` artifacts; ~110 runtime symbols; regenerate mechanically at RTX-1 — do not trust this list blindly)
**⛔ CORRECTION OF RECORD (RTX-2, s163) — THIS TABLE ALREADY ROTTED ONCE, EXACTLY AS ITS OWN CAVEAT WARNED.** The ALLOC row below originally read `blk_alloc blk_free`. Both are **DEAD**: zero live call sites, C in `archive/backend/` (not built), and they surfaced only because `util_rtx_symbol_inventory.sh` swept 33 `.s` files without separating live `--compile` output (26, first line `.intel_syntax noprefix`) from 5 legacy `scrip-cc -asm` nasm-syntax references and 2 macro libraries. A whole rung was aimed at symbols the compiler never emits. The script now filters on the live marker (fixed s163) and the row is corrected. **The ladder ORDER survived the correction** — CALL/STR counts are identical on the clean sweep (`rt_call_arr` 232 · `rt_proc_call_epilogue_` 178 · `str_concat_d` 152) — only ALLOC's contents were wrong. ⚠ TWO STANDING CONSEQUENCES: (a) a static blob→runtime call count CANNOT see families the blob reaches indirectly — `rt_gcheap_alloc` has ZERO blob call sites yet underlies every string and aggregate mint, so this table ranks the CALL BOUNDARY, not the hot path; use dynamic evidence (s161-style ablation) to rank work; (b) before porting any family, `grep` its symbols in `src/` and confirm a live definition exists — the two-minute check that would have caught this.
| family | symbols (principal) | C source |
|---|---|---|
| ALLOC | **`rt_gcheap_alloc`** (the SIL bump mint — every DT_S string, aggregate, WSC and PLJ block carves here) · `rt_str_alloc` `rt_str_dup` · `rt_ws_alloc` (285 in-runtime sites) `rt_ws_realloc` `rt_ws_strdup` `rt_ws_alloc_c` `rt_ws_strdup_c` · `rt_agg_alloc` `rt_plj_alloc` · `rt_gcheap_carve` (static, fused into the port) (+ descr mint idioms; zls/ζ mark-release stay template-side) ~~blk_alloc blk_free~~ **DEAD — see correction above** | **rt/gc_heap.c** (rt_arena.c is Prolog cterm/cellws islands, NOT this family) |
| STR | **`str_concat_d`** (152 static sites, #2 overall) · `rt_coerce_str_d` (2 static; indirect reach only) · `rt_substr` · `str_repeat_d` (+SIZE/TRIM/DUPL/REPLACE builtins) ~~rt_concat rt_lcomp rt_acomp~~ **DEAD — declaration-only in `rt/rt.h:69/80/81`, no definition anywhere in the tree, zero call sites (RTX-3, s164; second phantom family in two rungs)** | string_ops.c string_builtins.c |
| CALL | ⛔ **s188 DYNAMIC CORRECTION — `rt_call_arr`'s 232 static refs are COLD.** Measured mode-3: **5/16/8/12** calls on the four rail demos and **FLAT at 8 from N=1 to N=64**; hot ONLY in `string_manip` 10.0M · `eval_fixed` 1.0M · `roman` 400K. It lives in **`by_name_dispatch.c:4642`, NOT `rt/rt.c`** (this row's C-source cell is wrong for it), and is a 13-line `_setjmp` wrapper over static `rt_call_arr_impl`. **`try_call_builtin_by_name` is called 1:1 with it in all 16 benchmarks and `APPLY_fn` 0 in all 16 ⇒ port the PAIR or neither.** rt_proc_call_open(_slim) **`rt_proc_call_epilogue_γ` `rt_proc_call_epilogue_ω` `rt_proc_call_epilogue_slim_γ` `rt_proc_call_epilogue_slim_ω`** (⛔ RTX-4 s165 CORRECTION: this cell previously read `rt_proc_call_epilogue_(slim_)` — a TRUNCATION, not a symbol. The real exports carry literal UTF-8 GREEK letters; the trailing `_` was the prefix left after the Greek codepoint was dropped. GNU `as` accepts UTF-8 identifiers — proven by shipping code, `rt.c`'s existing inline asm already does `jmp rt_proc_call_epilogue_γ`.) `rt_proc_call_epilogue_ret` rt_proc_open_fn rt_call_arr rt_call_named_proc rt_arg_stage rt_proc_register/set_* rt_proc_reset ~~rt_call rt_do_return rt_define(_entry) rt_frame~~ **NO LIVE DEFINITION — `rt_frame` is a PREFIX (`rt_frame_prep`/`rt_frame_bind_args`), the rest are header-declaration-only (RTX-4, s165)** | ⛔ **`rt/rt.c` IS THE CALL SPINE (1667 lines)** — `invocation.c` is 80 lines and `core/name_save.c`/`core/argval.c` are 36/32. The old cell sent three rungs' worth of reading at the wrong file. |
| AGG | rt_subscript_var rt_deref rt_field_var record_register (+TABLE hash/lookup/insert internals) | aggregates.c values.c |
| ARITH | rt_num_arith rt_arith rt_binop_overload rt_coerce_num2_d **rt_cmp_d** [LANDED s203] rt_num_pos rt_num_neg ~~rt_incr rt_decr rt_exp rt_neg rt_coerce_num~~ [PHANTOMS s203: decl-only in rt.h, zero definitions, zero call sites — struck per ARCH §7] | arithmetic.c core/coerce.c |
| NV | ⛔⛔ **s208 DYNAMIC CORRECTION — THIS WHOLE FAMILY IS COLD; RTX-7 IS NOT A SPEED RUNG.** `NV_GET_fn` measures **ZERO calls** in var_access/func_call/string_manip/table_access/fibonacci/indirect_dispatch/op_dispatch and has **ZERO static call sites** in every benchmark `.s`; `rt_gvar_get_int`/`rt_gvar_assign_descr` **0 static sites**. Live ONLY under EVAL (`eval_dynamic`/`eval_fixed` 2.0M calls, scaling proven 2×), where `rdtsc` bounds `NV_GET_fn` at **0.303%** and `NV_SET_fn` at **0.276%** of the window (UPPER bounds — interposer overhead is inside the span) ⇒ **a perfect port wins ≤0.58%, below the ±3% null floor.** ⭐⭐ **CAUSE: THE BYPASSED FAMILY (eighth phantom shape) — the functions are live and correct, but the GVA SLOT ISLAND already removed the emitter's CALL SITES** (`var_access.s`: `rt_gva_island`+`gva_register` once at startup, then direct slots). Same mechanism as integer inlining vs `rt_num_arith` (s203). **A rung's premise decays when an unrelated rung succeeds.** ⇒ port only as correctness/eradication for RTX-12, never quoting a speed number. Original row follows. **NV_GET_fn NV_SET_fn** (both LIVE — verified defined s204) · rt_gvar_get_int rt_gvar_assign_{str,var,int,descr,pat,concat_parts} rt_gvar_arith rt_gva_island gva_register rt_subject_load_nv ~~rt_nv_get rt_nv_set~~ **PHANTOMS s204: decl-only `rt/rt.h:20-21`, zero definitions, zero call sites — struck per §7.** ⚠ This row is MIXED (real + phantom), the hardest shape to spot: the two names a rung is most likely to spot-check are the two that are real. | name_binding.c |
| MATCH | rt_match_enter rt_match_variant rt_match_value_* rt_match_replace rt_cap_{push,pop,top} rt_cap_assign_cursor rt_dcap_* rt_defer_{open,step,close,match,get_pat_fn} rt_scan_splice_empty | pattern_match.c |
| PAT | ⛔ **s204 SPELLING CORRECTION — THIS ROW NAMED `rt_pat_*` ON EVERY SYMBOL AND EVERY ONE OF THOSE IS A PHANTOM.** The live constructors carry NO `rt_` prefix. Measured from `nm -D out/libscrip_rt.so`, 32 defined: `pat_lit` `pat_any_cs` `pat_notany` `pat_span` `pat_break_` `pat_breakx` `pat_len` `pat_pos` `pat_rpos` `pat_tab` `pat_rtab` `pat_rem` `pat_arb` `pat_arbno` `pat_bal` `pat_alt` `pat_cat` `pat_epsilon` `pat_fail` `pat_succeed` `pat_abort` `pat_fence` `pat_fence_p` `pat_defer` `pat_mk_capt` `pat_mk_cset` `pat_mk_cursor` `pat_mk_nil` `pat_mk_num` `pat_pool_init` `pat_pool_reset` `pat_pool_emit`. ⚠ Note `pat_break_` carries a TRAILING UNDERSCORE and `pat_any_cs` is NOT `pat_any` — take both as the tree spells them (step 0b). ~~rt_pat_*~~ **DEAD — the `rt_pat_*` spellings are declaration-only in `rt/rt.h:38-67` (30 names, zero definitions).** This is the s165 MISRECORDED class, not the RTX-2 DEAD class: **the FEATURE is live and hot, only the NAME was wrong** — so a step-0 grep on the rung's own spelling returns the `rt.h` declaration, reads as ALIVE, then `nm` finds no definition and the family reads as a phantom. **The failure mode here is ABANDONING A REAL FAMILY, not porting a dead one.** | `pattern_match.c` (the constructors, `pat_lit` @ `:98`, `pat_alt` @ `:215`) · `rt/bb_pat_build.cpp` · `rt.c` re-declares them `extern` |
| MISC | rt_faildescr rt_is_truthy rt_last_ok rt_assign_var rt_goto_transfer rt_keyword_read(_snobol4) rt_bomb rt_unhandled_* execute_code_dyn comm_stno core_lib_init dtp_fn_of ~~rt_push_*/rt_pop_void/rt_halt_tos~~ **AUDIT DISCHARGED s204 BY MEASUREMENT — ALL DEAD.** `nm -D` finds ZERO definitions for any `rt_push_*`, `rt_pop_*`, or `rt_halt_tos`; each is declaration-only in `rt/rt.h` with exactly one src ref (its own declaration). ⇒ **RTX-10's "if dead, delete instead of port" branch is TAKEN — this is a DELETE, not a port, and no construct needs naming in the FINDING because none reaches them.** The NO-VSTACK rules already won; the header just never recorded it rt_write_any_nl + io | runtime_eval.c keywords.c io_format.c runtime_init.c |
| libc (stays) | memcmp memcpy strchr malloc-family read write | — |

## 6. PORT ORDER AND WHY (heat: s147/s148/s161 — claws5/json are runtime-bound; treebank/calc are emitter-ceremony-bound and keep their own ladder)
ALLOC → STR → CALL → AGG → ARITH → NV → MATCH → PAT → MISC/IO. Alloc first (everything mints); STR next (concat ×38 static in claws5, allocates); CALL is claws5's measured NRETURN lever (93% of time is semantic actions on BOTH engines, uniform 4–5× gap); AGG is claws5 layer C (~11 subscripts/token); PAT is construction-path (size-not-time, s160a) and ports LAST.

## 7. EVERY-RUNG PROTOCOL (the walking session's checklist)
0. ⭐ **STEP 0 IS FIVE CHECKS (fifth added s200): (a) live definition exists; (b) the spelling round-trips byte-identically (step 0b); (c) `nm` THE LINKAGE OF EVERY GLOBAL THE PORT WILL TOUCH; (d) ⭐⭐ PROVE THE SYMBOL IS EXECUTED IN THE WINDOW YOU INTEND TO MOVE; (e) ⭐⭐ CONFIRM THE SYMBOL IS NOT ALREADY ASSEMBLY.**
   **(e) — THE ALREADY-PORTED CHECK (minted s200, after RTX-5's rung named `rt_deref` as a port target and it turned out to have been hand-written asm all along, at `src/runtime/rt/rt_asm_helpers.S:8`).** ⛔ **THE ROOT CAUSE IS THIS CHECKLIST'S OWN GREP: the step-0 command below reads `--include=*.c --include=*.h --include=*.cpp` and therefore CANNOT SEE `.S` FILES.** An already-ported symbol returns zero hits and presents with the exact signature of a phantom — which is how s188 came to attribute the miss to the (real, but unrelated) non-UTF-8 `core/core.c` sitting in the same subtree. **A plausible neighbouring anomaly absorbed the blame for a filter defect.** ⇒ **THE EXISTENCE GREP NOW RUNS WITH `--include=*.S` ADDED, or with no `--include` filter at all** — a few extra hits cost nothing against a rung aimed at finished work. ⭐ **SIXTH PHANTOM-FAMILY MEMBER AND THE FIRST WHOSE FAILURE MODE IS DUPLICATED WORK RATHER THAN WASTED WORK — and therefore the most dangerous, because the re-port SUCCEEDS: batteries green, board unmoved, null result misread as "asm doesn't help here" when the truth is "that was already asm."** Ten-second check: `nm` the defining object, or grep the `.S` files. ⚠ **KNOWN UNGATED ASM — CORRECTED s202, NOW FOUR (was six): `rt_sg_scan_member`/`rt_sg_scan_nonmember`/`rt_sg_member` (`rt/rt_sg_scan.S`) and `rk_gram_enter_box` (`rt_gram_trampoline.S`, Raku). `rt_deref` and `to_int` came under the contract when RTX-5a reached `origin/main` at `9cc782fb` (gate `SCRIP_RTX_LEAF`, both C fallbacks — `rt_deref_slow` `pattern_match.c:1173`, `to_int_slow` `core/core.c:1991` — verified complete, kill-switch and two-sided falsification proven on that base).** ⛔ **THE s200 ENTRY WAS WRITTEN AGAINST A BRANCH: RTX-5a sat on `rtx-3b-s200` (3 ahead, 9 behind main) for two sessions, so this "six" was simultaneously stale as a plan and TRUE OF EVERY FRESH CLONE — including s201's. See the s202 cursor's branch-landed-vs-main-landed rule.** ⚠ **All four remaining are AT&T syntax, contrary to Ruling 1 ("Intel, one project-wide") — they sit outside this contract on TWO axes, not one.** Consequence to state plainly wherever it matters: **"all `SCRIP_RTX_*` gates off" STILL does NOT mean "pristine C runtime."** See `FINDING-2026-07-27m-...RT-DEREF-IS-ALREADY-ASM...md`.
   **(d) — THE RELEVANCE CHECK (minted s188, after RTX-4 slice 3 was aimed at a symbol that passes (a),(b) AND (c) and is never called in the timed region).** Count the symbol dynamically under the ACTUAL workload the rung will be graded on, at **two loop counts**, and confirm the count SCALES. `rt_call_arr` was 8 at N=1 and 8 at N=64 on `claws5-match` while compute_ms went 2→12 — setup-only, outside the rail's window, so an asm port moves the board by zero *by construction*. **A static call-site count CANNOT substitute: it measures the CALL BOUNDARY (how many places the emitter can reach the symbol from), which is a property of the program TEXT, not of execution — §5 caveat (a) says exactly this and was published at RTX-2, then not applied by the next three rungs.** No profiler exists in this container (`perf`/`valgrind`/`ltrace`/`strace`/`gdb` ALL absent); the working substitute is an `LD_PRELOAD` counting interposer chaining via `dlsym(RTLD_NEXT,…)`, which needs ZERO edits to the tree and intercepts mode-3 because `bb_call_fn.cpp` bakes the emitter-TU address. ⭐ **THE RUNG-LEVEL FORM OF s187's PROBE RULE: state what the BOARD would look like if this port did not exist at all; if that equals the post-port board, the RUNG is vacuous.** Ten minutes, before any asm.
   ⛔ **AND CHECK THE BOARD ITSELF, NOT ONLY THE SYMBOL:** s188 measured that the five rail demos touch ALLOC twice, STR twice and AGG/ARITH zero times in a whole N=64 run ⇒ **RTX-12's board is blind to RTX-1…RTX-7.** A green-but-unmoved board is not evidence of a bad port; it may be evidence of a blind instrument. Same class as §7 step 2b's unmoved-battery FALSE CLAIM, one level up.
   **THE ORIGINAL THREE CHECKS:** Exported (`nm` capital `B`/`D`) means PREEMPTIBLE and needs `[rip+sym@GOTPCREL]`; visibility-hidden is localised by the linker in the `.so` and stays `[rip+sym]` direct. **In the C they look identical** — `Σ`/`Σlen`/`g_monitor_bin` vs `g_pcall`/`g_pcall_top`/`rt_k_level` differ only in an attribute you cannot see at the use site. This bites SELF-CONTAINED ports specifically: a port that CALLS into C reaches those globals through the callee and never meets the split (RTX-4 slice 1 did not; slice 2 did). Same class as RTX-2's `g_hp_fr` link failure. **GREP THE FAMILY'S SYMBOLS IN `src/` AND CONFIRM A LIVE DEFINITION EXISTS** (added RTX-2, s163, after a whole rung was aimed at `blk_alloc`/`blk_free` — symbols with zero live call sites whose C sits unbuilt in `archive/backend/`). `grep -rn '<sym>' src/ --include=*.c --include=*.h --include=*.cpp --include=*.S` ⛔ **THE `--include=*.S` IS LOAD-BEARING AND WAS MISSING UNTIL s200 — without it an already-ported symbol reads as a phantom; that is exactly how RTX-5's rung came to name `rt_deref`, which has been hand-written asm in `rt/rt_asm_helpers.S` all along. See step 0(e).** plus a live-artifact sweep. Two minutes; skipping it cost a rung. ⭐ **STRENGTHENED RTX-3, s164 — IT HAPPENED AGAIN, DIFFERENT CAUSE:** RTX-3 named `rt_concat`/`rt_lcomp`/`rt_acomp`, all three declaration-only in `rt/rt.h` with no definition and zero call sites. RTX-2's phantoms came from an inventory script sweeping dead artifacts; RTX-3's came from ladder PROSE naming an API that never existed. **The common factor is that a rung's symbol list was written from a DOCUMENT rather than from the tree.** Therefore step 0 now ALSO requires: **strike the dead names out of the ladder rung text and this table in the SAME commit that discovers them** — RTX-2 corrected this table but left the RTX-3 rung line intact, which is exactly why the next session walked into the same wall.
1. **READ FIRST:** this file → the family's C source in full → its `sbl.asm` counterpart for shapes (grep the section; SPITBOL block layouts are NOT ours — steal idioms, never structures) → the SPITBOL manual chapter for every construct the family implements (Lon standing directive; e.g. CALL rung = **Ch.8 `Program-Defined Objects`** (DEFINE/RETURN/FRETURN, prototype string, local save-restore, by-name args) **+ Ch.9** (NRETURN, run-time compilation) — ⛔ **NOT Ch.13, which is `Running SPITBOL`**; corrected s165, but this instruction still said Ch.13 until s187; ARITH = mixed-mode coercion chapter; MATCH = Ch.9 deferred evaluation + p.122 well-foundedness + p.125 FENCE).
0b. ⭐⭐ **VERIFY THE NAME ROUND-TRIPS, DO NOT MERELY CONFIRM IT EXISTS (added RTX-4, s165 — THIRD divergence in three rungs, and step 0 as written does NOT catch it).** RTX-2's phantoms were DEAD names; RTX-3's were INVENTED names; RTX-4's were **LIVE NAMES RECORDED WRONG** — §5 read `rt_proc_call_epilogue_(slim_)`, a TRUNCATION of `rt_proc_call_epilogue_γ`/`_ω`, whose identifiers carry literal UTF-8 GREEK codepoints. A truncated name fails the step-0 grep and therefore LOOKS like a dead symbol — the wrong diagnosis, which sends the rung somewhere else entirely. **Take the symbol as the TREE spells it and confirm the doc's spelling is byte-identical.** Existence is not correctness.
2. **PORT** behind the family gate; C renamed `c_*` same commit.
2b. ⭐⭐ **RUN THE FALSIFICATION BUILD AGAINST *EVERY* BATTERY, NOT JUST THE FAMILY'S OWN — IT IS A COVERAGE INSTRUMENT, NOT AN ON/OFF PROOF (added RTX-4, s165).** RTX-1/2/3 used the broken-asm probe as a binary (red ⇒ the asm executes). The same build run across all batteries tells you **which batteries are evidence at all**. s165 measured: snobol4 314/1→**310/5** (real evidence), but Prolog **189/0 unchanged**, Icon **4/0 unchanged**, Snocone **8/0 unchanged** — three 'gates' that prove NOTHING about the port (they stay valid no-regression evidence for C-side and visibility changes only). Citing an unmoved battery as an asm gate is a FALSE CLAIM. The probe also names the exact coverage set: s165's landing is reached by 4 of 315 programs, all EVAL/defer — which RETARGETED the rung. Cost: one rebuild.
3. **GATES:** smokes 7/7×2 · full crosscheck BOTH modes at current watermark (s159–s161 constant: m3 314/1 · m4 309/4 · DIVERGE=3 — re-prove at session start before touching anything) · beauty · 15-demo board identity · kill-switch md5 byte-identity vs pristine.
4. **RAIL** same-moment A/B (`bench_sno_rail.sh`), label every number `RTX_<FAM>=on/off, RT_OPT=-O0` (RT_OPT now describes only the residual C).
5. **FINDING** doc + **LIVE CURSOR** move + handoff per RULES. `.s` regen ONLY if templates changed (phase 1: never).

## 8. SPITBOL LESSONS — ADOPTED vs REJECTED
**Adopted:** register-pinned runtime discipline; xl/xr/wa physical mapping (rsi/rdi/rcx) for the string engine; rdx/r10 div discipline; low-byte aliases (cl/dl) for char work; hardware stack as THE stack; cld invariant; mxcsr management; boundary save protocol (simplified — no global block).
**Rejected:** Minimal's 9-register ceiling (r8 r9 r11 r14 r15 rbp are UNUSED in all 19,064 lines of sbl.asm — we design for 15); the cp interpreter code-pointer (we are compiled, no interpreter loop); memory `fl` condition byte (EFLAGS); the N-exit-points-after-call convention (our α/β/γ/ω port model already covers multi-exit); two-stack compsp/osisp switching (our ζ is already on rsp); SPITBOL's block/descriptor formats (ours are `src/contracts/descr.h`, full stop).
