# GOAL-BB-FIXUP-Z-to-A.md — BB Template Sweep: Z → A (descending)

## ⛔⛔ CARVED OUT BY THE A→Z SESSION — DO NOT TOUCH THESE 10 (Lon directive, 2026-07-15 run #6)
The A→Z session took the 10 biggest violators (fresh `audit_bb_fixup_rank.sh` @ 16:40, HEAD b404fb95: 150/52/98/GRAND 875) as its carve-out. They are OWNED there. On your descent AND on wrap, **SKIP them** — the RESUME-PROCEDURE step-3 pick below must NOT land on any of these (add them to the `awk` exclusion). The 10:
```
bb_call · bb_call_fn · bb_call_proc_staged · bb_call_write_slot · bb_gather · bb_mapgrep · bb_match_alternate · bb_match_bal · bb_scan_sequence · xa_flat
```
5 of these sort BEFORE the current Z→A cursor (`bb_lit_scalar`) and are thus AHEAD of your descent — **bb_call, bb_call_fn, bb_call_proc_staged, bb_call_write_slot, bb_gather** — skip each when the descent reaches it. The other 5 (bb_mapgrep, bb_match_alternate, bb_match_bal, bb_scan_sequence, xa_flat) sort after `bb_lit_scalar` (already behind you) — they matter only on wrap. (`xa_flat` is excluded by your step-3 regex `bb_[a-z0-9_]+\.cpp` anyway, but it is doubly hands-off: it is ALSO under active R12-ERAD/ZC_FRAME WIP.) This carve is Lon's; it lifts on Lon's word. Mirror record: GOAL-BB-FIXUP-A-to-Z.md top + a pointer in BB-REVAMP-TRACKER.md.

## ⛔⛔ READ FIRST — PICK THE NEXT FILE (the rule that keeps getting botched)
**Z→A: the cursor only ever moves DOWN the alphabet — toward A, to names that sort EARLIER. NEVER up toward Z.** The one repeated failure is "jump to the nearest-Z dirty file" / "do the easiest file first" — both are A→Z = BACKWARD = WRONG.

**RESUME PROCEDURE (every cold start, before touching any file):**
1. `cursor=$(grep -m1 '^# CURSOR:' .github/BB-REVAMP-TRACKER.md | awk '{print $NF}')`. The tracker `# CURSOR:` line is the ONE source of truth for position (beats any watermark text).
2. If `$cursor` is non-CLEAN (`audit_bb_fixup_file.sh src/templates/<cursor>` rc≠0) → **work `$cursor`**.
3. Else next file = FIRST non-CLEAN file sorting **STRICTLY BEFORE `$cursor`** (toward A): `audit_bb_fixup_rank.sh` → `grep 'TOTAL=[1-9]'` → `grep -oE 'bb_[a-z0-9_]+\.cpp'` → `sort -ru` → `awk '$0 < "<cursor>"'` → take the top. Advance the cursor to it IN the same commit as that file's fix.

**FORBIDDEN (all A→Z = backward):** picking the nearest-Z / top-of-descending-list file (the "bb_var_global is FIRST stop" rule applies ONLY at a fresh lap-start, right after the cursor WRAPS to Z — never mid-lap); picking any file sorting AFTER `$cursor`; ordering by violation count / cheapest-first. Order is by FILE NAME ONLY, Z→A, from the cursor.

**Dirty files BEHIND the cursor** (re-dirtied by tightened CONVERSIONS or other goals' regressions) are NOT fixed mid-lap — they get re-swept after the cursor reaches the A-end (bb_aggregate_nb) and WRAPS to Z. Do not backtrack.

**Sanity check before editing:** confirm `name(file) < name(prev_cursor)` in A→Z sort. If it sorts after the cursor, STOP — you are going backward.

**Carved** 2026-06-12 (split from GOAL-BB-FIXUP.md). **Repo:** SCRIP + .github.
**Sister files:** GOAL-SNOBOL4-BB.md (canonical TEMPLATE SPEC v2 text) · GOAL-ICON-BB.md · GOAL-PROLOG-BB.md · `.github/BB-REVAMP-TRACKER.md` (cursor + per-file state).

## PURPOSE — sole owner of ALL BB template quality
This goal owns ALL BB cleanup: TIER H mechanical hygiene AND TIER S structural splits. All other BB hygiene/revamp goals are ABANDONED; everything routes through this cursor (Lon 2026-06-07). "Claude executes the design" = do clear/pinned redesigns in place without a flag-and-wait cycle, but STATE every design call plainly in the run record so Lon can veto live; genuinely ambiguous judgment calls still surface to Lon. These runs are ATTENDED.

## THE TARGET
**SPEC v2** (canonical text in GOAL-SNOBOL4-BB.md): no locals · ONE return per PLATFORM returning ONE concatenated string · ONE source line == ONE asm line · real Greek α β γ ω · no MEDIUM_* at template top level · zero emit_fmt() · ZERO raw bytes/BINARY anywhere in a bb_*.cpp (x86() internals are the ONLY emitter) · zero C comments · zero blank lines · ONE-IR-ONE-LOGIC (split in LOWER) · EMIT-BLIND (LOWER decides, templates emit). Regenerate whole, don't patch.
**FACT RULES** (canonical in GOAL-*-BB.md): `bb_bin_t` ABOLISHED · ONE MEDIUM INVISIBLE · NO C BYRD-BOX FUNCTIONS · NO VALUE STACK · NO DUPLICATED LOGIC · PER-BOX LOCAL STORAGE (RO `[rip+disp]` / RW `[ζ=r12+off]`) · x86-64 convention (Σ=r13 δ=r14 Δ=r15 ζ=r12) · TEMPLATE-ONLY EMISSION · LANGUAGE-BLIND TEMPLATES · **BOTH-MEDIUM MANDATORY**: every code-emitting function supports BOTH TEXT and BINARY; TEXT-only emitters abolished; any helper conditionally returning `std::string()` in one medium is an absolute violation.

## TWO TIERS (both executed by Claude)
- **TIER H** (mechanical, behavior-neutral): locals→inline/static helpers, return-shape, Greek ports, emit_fmt removal, comment/blank purge, MEDIUM-branch collapse into `x86()`, register conformance. Emitted asm IDENTICAL or gate-equivalent.
- **TIER S** (structural): ONE-IR-ONE-LOGIC splits + EMIT-BLIND fixes + de-cramming/de-fusing (LOWER arms, new IR codes, `_.op_*`/ζ-slot plumbing). One IR-split per commit chain; full gates per rung. Approach: read sub_kind()/neighbor-read logic → identify the split → add IR kinds (IR.h/scrip_ir.c) → split lowering (lower*.c) → add templates + emit_core dispatch → retire the old multi-logic template.

## ⛔ SOP — FIX EVERY FILE, NEVER SKIP
At each cursor stop the file MUST reach full conformance BEFORE the cursor advances — NO skip, NO advance-past, NO pin-and-wait, NO route-to-owner-goal. The cursor advances ONLY after `audit_bb_fixup_file.sh <file>` rc=0 AND the gate battery is green. The PURPOSE is to redesign whatever in the C++ template violates a rule that triggers a CONVERSION — add IR kinds, split lowering, extend `bb_prepare`/`x86()` encoders, per-logic templates + dispatch, whatever conformance needs. Structural work is the job, not a deferral. The ONLY brake on forward progress is the context budget (~70%): finish the file you can finish, commit, clean-handoff naming the NEXT cursor file. Behavior preservation governs: keep m2/m3/m4 parity + gates green; a TEXT/BINARY divergence repaired by both-medium unification is IN SCOPE.

## ⛔ LAWS OF THE LOOP
1. **BEHAVIOR-NEUTRAL ALWAYS.** Full gate battery green before AND after; TIER H proves asm equivalence (git-stash baseline `--compile` diff empty, or only the sanctioned transform).
2. **ONE FILE PER COMMIT, STRAIGHT TO MAIN.** No branches. Message `FIXUP <file>: <criteria cleared>`.
3. **PULL BEFORE, PUSH AFTER.** Cursor advance travels IN the same commit as the file's fix.
4. **NO SKIPPING — FIX IN PLACE.** Cursor advances only at audit rc=0 + gates green. No pin-skip, no Category-C advance, no owner-goal hand-off of the cursor file. HOT is not an excuse.
5. **SCOPE = MAKE THE CURSOR FILE CONFORM, INCLUDING ITS FUNCTIONAL BUGS.** This goal is the SOLE OWNER — no other owner goal, no future session, no Lon hand-off. A functional bug whose fix is part of making the template conform (e.g. a TEXT/BINARY divergence, an m3/m4 divergence) gets FIXED HERE. "Don't widen scope" means ONLY: don't fix bugs in genuinely separate subsystems (parser, runtime internals, an unrelated language) the cursor file doesn't touch. When in doubt, it is YOURS.
6. **GATES ARE THE ONLY BRAKE.** No green → revert tree, note, advance, commit.
7. **CONTEXT-BUDGET HANDOFF (~70%).** Finish current file, commit, push, watermark, stop cleanly.

## ⛔ THE CONVERSIONS (CV1–CV10) — apply every applicable one until `audit_bb_fixup_file.sh <file>` rc=0
A CONVERSION = a spec of what to TARGET and what to CHANGE it to. (R1–R6 are aliases CV1–CV6.)
- **CV1 — TERSE COMMENT.** Each `x86("comment", …)` → the bare `IR_<KIND>` the box dispatches from. Terseness only.
- **CV2 — INLINE MULTI-EMIT HELPERS.** Every helper composing ≥2 `x86`/`x86_*` emission calls (not the `bb_*` entry) inlined at its call sites + deleted. KEEP zero-emit fragment builders (label/value/slot computers spliced INTO one `x86()` operand) and the `x86()` primitive.
- **CV3 — DE-ESCAPE ins\*.** `x86("ins1"|"ins2"|"ins3"|"Lins1"|"Lins2", …)` are TEXT-only escape hatches (zero bytes in BINARY = ONE-MEDIUM violation) → replace each with a real byte-emitting `x86()` form. Cat A (existing encoder) trivial; Cat B needs a new XK_RIPLBL/XK_LBLDIFF encoder added + byte-verified in x86_asm.h first.
- **CV4 — V2 HYGIENE.** ONE return per PLATFORM · kill signature-line decls + static-helper constellations · drop MEDIUM_* head wrappers · 1-src-line == 1-asm-line · ≤200 col · purge non-separator C comments (KEEP only the CV1 box-name comment) · zero blank lines · add missing 200-char separators.
- **CV5 — ABSOLUTES.** zero raw bytes / MEDIUM_BINARY anywhere · zero emit_fmt() · real Greek α β γ ω · EMIT-BLIND (no neighbor inquiry). A file that can't meet this without losing logic is a ONE-IR-ONE-LOGIC violator → execute the split IN PLACE (per SOP; there is NO defer/pin-and-wait).
- **CV6 — NO LOCALS IN TOP-LEVEL TEMPLATE FUNCTIONS.** ZERO local-variable decls in the template function body. Values arrive via zero-emit value-computer helpers (helpers MAY have locals/params), `_.op_*`/`_.bb_*` slots, or **lambda parameters (parameters are not locals)**.
- **CV7 — VALID x86/x64 ASSEMBLY ONLY** (Lon 39th run, verbatim "only VALID x86/x64 assembly instructions are ALLOWED as 1st parameter of x86 function"). TARGET: any `x86("XXX", …)` where XXX is not a real mnemonic (mov/lea/add/cmp/jmp/je/call/ret/…) or a directive spelled as in the .s (.quad/.string). CHANGE: real spelling + an operand form mapping to the same encoder; decompose multi-line pseudo-ops per-line; encoders + medium switch stay inside x86_asm.h. Precedents: `ro_load_q`→`x86("mov", reg, ROQ(n))` (XK_ROSLOT); RO seal decomposes to `x86("def", L(n))+x86(".quad", …)` (quad) / `+ x86(".quad", LS(n), lit)+x86("label", LS(n))+x86(".string", lit)` (string) — byte-identical by construction. The `x86_ro_seal_*` free fns stay inside x86_asm.h.
- **CV8 — PLATFORM IF STATEMENTS PRESERVED.** Each `if (PLATFORM_*)` is its OWN separate if statement; PLATFORM_X86 stays fenced from JVM/.NET/JS/WASM (which stub out but keep the fence shape). "ONE return per PLATFORM" = one return INSIDE each platform's if (+ the empty fallthrough return), NEVER one merged return spanning platforms. The platform predicate never appears inside an IF() combinator arg.
- **CV9 — NO PARAMETERS / NO `_str` ON TEMPLATE FUNCTIONS.** Conformant shape = `std::string bb_X()` — NO `_str` postfix, ZERO parameters, NO extern-C wrapper (the `bb_alt` precedent: declared `std::string bb_alt();` in bb_templates.h, invoked `bb_emit_x86(bb_alt())` in emit_core). Rename `bb_*_str`→`bb_*`, drop all params. The audit checks CV9 (`bb_*_str` / `IR_t*` in sig).
- **CV10 — NO IR-GRAPH ACCESS FROM TEMPLATES.** No `pBB`, `_.node`, `g_emit_cfg`, `bb_operand_aux_get`, `IR_t` deref, `IR_LIT(node)`, child/operand walks — top level OR helpers. By call time EVERY value the template needs is in `g_emit` (scalar or collection); the driver-side `bb_prepare` (emit_bb.c) does ALL graph inspection and delivers via `_.op_*`/`_.bb_*` (add sm_emit_t fields as needed; precedent op_parts_n/tag/ival/str for IR_ALT). Zero `IR.h` surface in a template = the completion sign. The audit checks CV10 (`ir_call_arg`/`IR_LIT`). **Exception:** an arbitrary-depth term-tree builder that can't be pre-flattened (e.g. `emit_build_compound_term`, 15 callers) lives in a CV10-exempt support TU (`src/emitter/emit_term_build.cpp`), NOT in a bb_*.cpp.

**PER-FILE CHECKS** (run ONCE, after all CVs applied):
- **C1** `make -j4 scrip` + `make libscrip_rt` green. Set `LD_LIBRARY_PATH=/home/claude/SCRIP/out` for `--run` / linked m4 binaries.
- **C2 EMITTED-ASM IDENTITY** (object-byte-identity invalid at -O0): for each box the file changes, a firing probe → `--compile` .s, `bbN`-normalized A/B diff EMPTY (or only the sanctioned CV1 comment). Behavior-changing splits prove via prove_lower + gate-equivalence instead. **The freshly-built current binary IS the baseline — capture m3/m4 BEFORE editing.** m2 (`--run`) is NOT exposed at this HEAD (modes are `--run`=m3, `--compile`=m4); m2 doesn't use BB templates anyway, so SNOBOL4-pattern coverage is m3+m4. For DORMANT boxes use a throwaway lowering-retag vehicle and REVERT it.
- **C3** gate battery at floors: `test_gate_sno_pat_reg.sh --strict` TIER1+2=0 HARD · `test_snobol4_pat_rung_suite.sh` PASS-M4=19/0/0 · `test_gate_no_bb_bin_t.sh` 0 · `test_gate_no_handencoded_bytes.sh` 0 BAD · `test_gate_no_vstack.sh` 3 floor · `test_crosscheck_icon.sh` (HARD, currently pre-existing FAIL=4). For Prolog/Icon work also run their xchecks.
- **C4** rank: file TOTAL ↓, no growth elsewhere.
- **C5** ONE commit, `git pull --rebase`, push (SCRIP first, .github last), cursor-advance tick IN the same commit. If a rebase touches emit_bb.c/emit_core.c/x86_asm.h/lower_*, RE-A/B on the rebased tree (re-emit + diff vs pre-rebase captures).

## ⛔ IDIOMS & FINDINGS (durable across the lap)
- **emit_intern_str is DEAD** at HEAD: `g_flat_intern_str` = NULL (emit_bb.c) and its setter `lower_flat_set_intern_str` has zero call sites outside `src/attic` → it always returns NULL with no side effect. So `emit_intern_str(cs)` can be dropped and `strtab_label` made the sole label source. Re-verify each session (it's the one idempotency hazard).
- **The notany hazard:** when a string-cset template calls `strtab_label`/intern UNCONDITIONALLY before a dispatch branch, the intern side-effect (a `.S<n>` slot) fires even on branches that never use the label. Folding `strtab_label` into a branch-only path renumbers all later `.S` labels → m4 divergence. Keep `strtab_label` as an unconditional bare statement (or a comma-operator positioned before the branch). Caught by A/B, not by construction.
- **Shape idioms** (all proven, byte-identical):
  - single-linear string-cset, bare `strtab_label`: bb_match_break / breakx / notany / any.
  - dispatch + comma-operator `strtab_label` (intern before a branch): bb_match_capture; empty-guard-as-ternary (silent `std::string()`, not a bomb): bb_match_atp.
  - `std::string` label arithmetic with no locals → single capturing lambda `[&](std::string base){ return …; }(<local-free expr>)`: bb_match_arbno. (CV6: lambda params are not locals; the audit excludes capturing-lambda returns from the returns count, so a capturing lambda beats a static helper-function which would ADD a counted return.)
  - scratch-slot `FR(_.x86_scratch_off + N)` inlines: bb_match_arb.
  - fn-ptr call (drop `@PLT`): `x86("call", "fn", (uint64_t)(uintptr_t)(void *)(<sig>)fn)`. If a scalar-arm rt_* isn't declared in bb_common.h, add the extern.
- **DEAD-PATH boxes:** many Prolog builtin boxes are shadowed by an IR_DET_* path (GZ rewrite) and fire ZERO times — but the IR_DET_* fallback only `return 0`s on shapes it can't handle, so the bb arm is the LIVE fallback and CANNOT be deleted until IR_DET_* is TOTAL. Fire-probe each before assuming live vs dead.
- **Binary `MEDIUM_*` arm:** at HEAD many were already bombed (`x86_bomb`) by the IR-NEVER-TOUCHED work; emit-time IR walking is BLESSED (only RUNTIME IR deref is forbidden), so routing both mediums through emit-time `emit_build_compound_term` is correct.
- **Audit mechanics:** the rank tool prints CLEAN files too — extract dirty only via `grep 'TOTAL=[1-9]'`. Per-file audit needs the full path `src/templates/bb_*.cpp`, not the basename. `local_vars` is a whole-file count; the `const char *cs` form can evade the regex while `uint64_t`/`std::string` decls trip it — convert all locals regardless to match clean siblings.

## LAP-1 LEDGER (verbose per-session detail is in the matching HANDOFF-*.md; this is the terse record)
Lap 1 restarted at Z (bb_var_global) 2026-06-12 under BOTH-MEDIUM MANDATORY. Sessions, newest last:
- **var-frame pre-clean** (behind cursor, for next lap's Z-start): bb_var_frame_ref 5→0, bb_var_frame 4→0 (CV7 RDQ + rp lambda).
- **scan family**: bb_scan_bal 4→1\*, bb_scan_find 2→2\*, bb_scan_any 3→0, + tab/stmt/pos/move/many/match swept (CV2 ro_load_q→ROQ + ro_seal_str decomp; \*=counter-scope-trio sanctioned). bb_scan_stmt flagged [S] lit/graph split.
- **resolver family DELETED** (sessions 7–12): bb_resolve was the IR_BUILTIN meta-resolver dispatcher; sub-handlers converted parameterless one by one (retract_throw, type_test, term_io, term_inspect, list — all proven DEAD-path or both-medium-unified), then the PL-GZ GUT made IR_DET_* total-or-reject so the ENTIRE bb_resolve + 11 sub-handlers were deleted and IR_BUILTIN dispatch replaced with a loud abort (verified unreachable by 0 aborts across a 38-shape battery). The shared term-build cluster was relocated to `src/emitter/emit_term_build.cpp` (CV10-exempt). Audit gained CV9/CV10 greps in this stretch.
- **query/pattern family**: bb_query_frame 2→0; bb_pattern_{stub,nullary,lit,cat,arb,alt,unary_s,unary_i} all →0 (proto-blob payloads transformed programmatically + byte-verified; stub/atp/span_var/unary kinds confirmed dormant or builder-pending).
- **match family** (current stretch): bb_match_{tab,span_var,span,rtab,pos,notany,len} →0 (16th); bb_match_defer →0 (17th); bb_match_{capture,breakx} →0 (18th); bb_match_{break,atp,arbno} →0 (19th); **bb_match_{any,advance} →0 (20th, this session)**.

- **LAP 2 (LIVE-SET) opened 2026-07-09 per Lon** — cursor RESET to Z; scope = Makefile live set only (116/163 disk; 47 dormant skipped): bb_var_ref 3→0 · bb_var_global 1→0 · bb_var_frame_ref 5→0 (ORPHAN) · bb_var_frame 7→0 (ORPHAN) · bb_var 3→0 (dead flag-arms deleted) · bb_unop 9→0 · bb_to 4→0 (CV7 RO-quad-seal decomposition precedent).

## CURRENT STATE (live watermark — terse)

**WATERMARK 2026-07-19 (Claude Sonnet 4.6, LAP 2 LIVE-SET, 37th session — ONE file landed, cursor = bb_match_sequence → bb_match_replace.cpp):** bb_match_sequence 6→0 (SCRIP 487883ce, on origin). ALL TIER-H MECHANICAL, three changes: (1) returns_plus 3→0 — template fn folded into ONE nested ternary head `!PLATFORM_X86 ? std::string() : (sfc_cond) ? FORTH-arm : op_off<0 ? bomb : main-body`; `sfc()` helper INLINED into the condition (deleted) — necessary because 3 functions needing returns exceeded ≤2 rp budget; sfc() is a zero-emit single-call-site predicate so inlining is behavior-neutral (bb_scan_any/bb_make_list precedent); (2) sig_decls 1→0 + multi_x86 1→0 — `seq_dispatch_chain` helper's `std::string r` for-loop → emit-time RECURSION (`lo>=N ? std::string() : x86("cmp",...)+x86("je",...)+recurse(lo+1)`), one x86() per physical line; same loop→recursion recipe as bb_scan_alternate 36th-session; (3) extra_cmts 1→0 — purged the `/* ZB-FC-3b (ARCH-ZETA S13 Tier C, SEQUENCE...) */` 8-line prose block (lines 11–18), kept leading 200-char separator. FORTH arm and main arm (seq_dispatch_chain) BOTH covered. IR_MATCH_SEQUENCE is LIVE (SN4-NARY-SEQ: fence-free element list → one node; lowered by lower_snobol4.c:943, fired at emit.cpp:798). A/B --compile RAW BYTE-IDENTICAL ×4: both arms (FORTH port=default / main port=5=OWNED) × two depths (N=3 probe `"abcdefghi" "abc" "def" "ghi"` + N=2 `"abcdef" "abc" "def"`), zero diff lines, md5 identical for all four, no normalization. m3 (--run): "matched-three" / "matched-two" correct. DURABLE FINDING: PAIR(idx) is NOT pure (rotating static buffer static char b[8][16]); safe for loop→recursion because each PAIR result consumed immediately by its enclosing x86() call before any other PAIR runs — never >8 live pointers; ascending-order emission preserved regardless of C++ operand evaluation order. Regen: feature .s util_regen_feature_s_artifacts auto-committed pre-existing drift (arith/assign/capture/keywords/hello/.s files that don't fire IR_MATCH_SEQUENCE); committed as `8ae303df` before my FIXUP; superseded in final log by `487883ce` (FIXUP commit, 1 file only). Floors certified after: sno_pat_reg strict TIER1+2 0/0 HARD · no_bb_bin_t 0 · no_handencoded --strict 0 BAD · no_vstack 1 (rt.h floor) · pat rung PASS-M4=19 FAIL-M4=2 (same 2 zb no-oracle pre-existings) · icon xcheck PASS=4 FAIL=0 HARD. dirty∩live∩¬carve = 40 at open (recomputed live via Makefile; dirty via rank TOTAL≥1); 40→39 after. ENV: fresh container → `bash scripts/install_system_packages.sh` THEN `apt-get install -y libgc-dev`; nproc=1; binary ./scrip at repo root; LD_LIBRARY_PATH=/home/claude/work/SCRIP/out for --run/linked-m4; corpus+x64 NOT cloned. ⛔ OPEN rulings carried, unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach · HOT-vs-LAW-4 tension.

**NEXT (Z→A, live set):** bb_match_replace.cpp → bb_match_release → bb_match_head → bb_match_defer → bb_match_capture → bb_match_arbno → bb_lit → bb_limit (recompute dirty∩live∩¬carve at open; was 39 at 37th-session close). The bb_match_replace was swept in the 28th session but RE-DIRTIED — re-audit fresh.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_match_replace.cpp). Re-read the READ-FIRST banner before acting on it.**

**WATERMARK 2026-07-19 (Claude Opus 4.8, LAP 2 LIVE-SET, 36th session — ONE file landed, cursor = bb_scan_alternate → bb_match_sequence.cpp):** bb_scan_alternate 5→0 (SCRIP b309eb91 LOCAL, push PENDING credential). ALL TIER-H MECHANICAL, three changes: (1) returns_plus 1→0 — `!PLATFORM_X86` FOLDED into the return-ternary head (`return !PLATFORM_X86 ? std::string() : _.op_off<0 ? bomb : body`, bb_scan_any/bb_make_list precedent, CV8-clean), main fn now ONE return; (2) sig_decls 1→0 + multi_x86 1→0 — the `scanalt_dispatch_chain` helper's emit-time `for`-loop → emit-time RECURSION, dissolving the `std::string r` accumulator; call sites UNCHANGED (same params `(long N,int base,int lo)`, recursion `lo→lo+1` until `lo>=N` emits the identical cmp/je ascending chain); (3) over_col 1→0 + extra_cmts 1→0 — deleted the 324-col line-20 `/* rbp-dcap (s46 mirror…) */` prose block (RULES C-style: only the 200-char separator; the +20-dead-pad rationale survives in git + zeta_storage's scanalt.dcap-height field comment). KEPT `x86("comment","IR_SCAN_ALT_NARY")` verbatim — swept-clean sibling bb_match_alternate uses the analogous `IR_MATCH_ALT_NARY`, so `_NARY` is house convention (the audit does not inspect the comment-string) → ZERO .s drift. ⛔ DORMANT BOX: nothing tree-wide produces IR_SCAN_ALTERNATE (icn_scan_kind_for maps only tab/move/upto/any/many/find/match/pos/bal; Icon scan-alternation lowers to IR_DISJUNCTION→bb_disjunction, lower_alt lower_icon.c:868). A/B per C2 via THROWAWAY lowering-retag vehicle REVERTED: added `if(nd->op==IR_DISJUNCTION) nd->op=IR_SCAN_ALTERNATE` inside icn_retag_scan_body (lower_icon.c:264), REVERTED after proof (git confirms lower_icon.c clean; only the template is committed). Vehicle is EXACT because zeta_storage.c:134 grants IR_SCAN_ALTERNATE its zls slot (op_off≥0 → REAL body fires, not the bomb) and emit.cpp:1128 + flat_drive (1603/1676/1728/1870) group IR_SCAN_ALTERNATE == IR_DISJUNCTION identically, so the retag swaps ONLY the final dispatch, keeping op_ival/op_off/wiring identical. A/B --compile RAW BYTE-IDENTICAL: probe `procedure main(); "hello world" ? write(tab(3)|tab(5)|tab(7)); end` (single-line semicolon Icon form — the `s?{block}` multi-line form does NOT parse; known-good ref test/icon/jcon_audit/55_scan_move.icn), fires the box (rt_substr×4, IR_SCAN_ALT_NARY×1, REAL body), md5 IDENTICAL d75daa3d, ZERO diff lines, 5504==5504 bytes. Also byte-identical BY CONSTRUCTION (comment strips from .s; recursion==loop concat; `+` binds tighter than `?:` + right-assoc reproduces the platform branch). Floors certified after: pat rung PASS-M4=19 FAIL-M4=2 (same 2 zb no-oracle pre-existings act_arbno_in_define + arena_collection_grow) · sno_pat_reg strict TIER1+2 0/0 HARD · icon xcheck 4/0 HARD + m4 4/4 · no_bb_bin_t 0 · no_handencoded 0 BAD · no_vstack 1 (rt.h floor). ⛔ FINDING (DURABLE — loop→recursion recipe): a variable-length emit-time chain builder (N cmp/je pairs) canNOT be unrolled in source AND canNOT keep a `std::string r` accumulator without tripping the local_vars/sig_decls catch-22 (accumulator on its own indented line = local_vars; on the sig line = sig_decls). Emit-time RECURSION has NO accumulator → clears both while emitting the identical concat. Applies to any n-ary chain helper. ⛔ FLAGGED not swept (zero-drift protects the A/B): `IR_SCAN_ALT_NARY` is NOT a real IR kind (only IR_SCAN_ALTERNATE exists in IR.h); the box dispatches from IR_SCAN_ALTERNATE (emit.cpp:799). A strict-CV1-literal rename to `IR_SCAN_ALTERNATE` is a trivial 1-line follow-up (a sanctioned comment-only .s diff, bb_rev_swap precedent) but zero-drift kept for safety — Lon may want the literal. ENV: fresh container needed `bash scripts/install_system_packages.sh` THEN `apt-get install -y libgc-dev` (the script does NOT cover gc/gc.h); nproc=1 (full build ~min, incrementals fast); binary ./scrip at repo root; LD_LIBRARY_PATH=/home/claude/SCRIP/out for --run/linked-m4; corpus + x64 NOT cloned (regen a NO-OP for a byte-identical dormant-box A/B). dirty∩live∩¬carve = 31 measured post-clear (the 35th's "37" uses a different filter — likely incl carve-out; recompute at open). ⛔ OPEN rulings carried, unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach · the 27th-session HOT-vs-LAW-4 tension. ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: b309eb91 (SCRIP) + the .github cursor/watermark commit are LOCAL only — `git pull --rebase` + push (code-first, .github-last) PENDING credential; run `handoff_status.sh` after push to confirm.



### prior watermark (35th session, superseded)

**WATERMARK 2026-07-19 (Claude Opus 4.8, LAP 2 LIVE-SET, 35th session — ONE file landed, cursor = bb_var_ref → bb_scan_alternate.cpp):** bb_var_ref 4→0 (SCRIP 49d8d8ee LOCAL, push PENDING credential). ALL TIER-H MECHANICAL: extra_cmts 1→0 + over_col 2→0 (ONE delete — the `/* PL-REGAIN-3 s100 VCELL-fix */` prose block WAS both >200-col lines; RULES C-style allows only the 200-char separator; the constant 0x100000009=slen<<32|v and the two-mov inline DESCR stand alone in the code, rationale preserved in git + FINDING-2026-07-19-…-PL-PREGUT) + multi_x86 1→0 (the gva/frame ternary `_.op_gva_k>=0 ? x86("mov","rdx",GVA) : x86("lea","rdx",FRQ(sa))` split to one x86() per physical line, condition + both arms VERBATIM). A/B --compile RAW BYTE-IDENTICAL ×2, ZERO diff lines (minimal prolog var probe `id(X,X); main:-id(hello,Y),write(Y),nl` fires IR_VAR_REF ×5 rc=0; palindrome.pl fixture fires ×5 byte-identical, though it bombs rc=134 downstream on a PRE-EXISTING unimplemented shape, Law-5 not mine). Every x86() verbatim; the deleted C comment strips from .s so even the .s comment stream is unchanged. ⛔ HOT: file was ~hours old (PL-REGAIN-3 s100 box added TODAY by the Prolog-perf work-stream) — fixed IN PLACE per LAW-4 "HOT is not an excuse"; cursor pointed AT it, behavior-neutral so a late PL-REGAIN push just rebases. Floors certified after: prolog xcheck 4/0 · icon 4/0 HARD · rebus 4/0 · no_bb_bin_t 0 · no_handencoded --strict 0 · emit_no_lang OK · icn_no_stack 0≤127 · icn_one_reg_frame 0≤21 · no_vstack 0 · sno_pat_reg HARD TIER1+2 · icn_semicolon 3-locks. medium_invisible --strict FAILs but the SOLE offender is xa_flat.cpp(112) WIP (the carve-out file) — bb_var_ref contributes 0 (forbidden-token set IDENTICAL HEAD-vs-edited, both empty); floor HELD not green. ⛔ FLAGGED not swept (minimal diff protects the A/B): the `extern DESCR_t rt_var_ref_cell(DESCR_t*)` decl on line 8 is now UNUSED here (PL-REGAIN-3 inlined the DESCR) — removable, like bb_lit_scalar's g_gvar_callarg_live flag; Lon/user may want it retired. ENV: fresh container needed `apt-get install libgc-dev` AFTER install_system_packages.sh (script does NOT cover it; gc/gc.h); corpus + x64 NOT cloned (regen a NO-OP for a byte-identical A/B). Binary is ./scrip at repo root; LD_LIBRARY_PATH=/home/claude/SCRIP/out. GRAND/dirty: dirty∩live remainder behind cursor = 37. ⛔ OPEN rulings carried, unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach · the 27th-session HOT-vs-LAW-4 tension. ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: 49d8d8ee (SCRIP) + .github 7d4797e7 are LOCAL only — `git pull --rebase` + push (code-first, .github-last) PENDING credential; run `handoff_status.sh` after push to confirm.

**NEXT (Z→A, live set):** bb_scan_alternate.cpp → bb_match_sequence → bb_match_replace → bb_match_release → bb_match_head → bb_match_defer (recompute dirty∩live at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile` ∩ rank `TOTAL=[1-9]`; dirty∩live = 37 at 35th-session close). NB bb_scan_sequence is a CARVE-OUT (skip); bb_scan_alternate is NOT. The bb_scan_*/bb_match_* neighbors likely carry CV4 woven-ternary + CV7 ro-seal shapes — re-audit fresh under lap-2 CONVERSIONS.

**WATERMARK 2026-07-11 (Claude Opus 4.8, LAP 2 LIVE-SET, 32nd session — ONE file landed, cursor = bb_make_list.cpp):** bb_make_list 4→0 (SCRIP e0c7b9a9 LOCAL, push PENDING credential) — leaves the bb_match_* family for list construction. **local_vars 2→0** (CV6: `int n` inlined as `_.op_arg_slot_n`; `std::string s` accumulator dissolved — the two imperative `for` loops became ONE `FOR(0,(int)_.op_arg_slot_n,[&](int i){4 movs})` combinator in a single concat) + **returns_plus 2→0** (CV4: 3 returns → 1 woven ternary `!PLATFORM_X86 ? std::string() : op_off<0 ? bomb1 : !slotsReady ? bomb2 : body`). The `!PLATFORM_X86` return was FOLDED INTO the ternary head (bb_scan_any precedent — PLATFORM_X86 in a `?:` condition is CV8-clean, it is not an IF()-combinator arg); the element-slot guard (originally an early-return from inside a loop) became a single-line capturing-lambda IIFE predicate `![&](){for(...)if(_.op_arg_slot[i]<0)return 0;return 1;}()` (ret_lam-excluded). ⛔ FINDING (ret_lam mechanics, durable): the audit excludes a lambda `return` ONLY when it sits on the SAME line as the `[&](...){` opener — a multi-line FOR-body lambda's return (own line) IS counted, a single-line IIFE's return is NOT. Recipe for any loop-bearing box: fold the platform-return into the ternary head (−1 counted return) + single-line IIFE for the loop-guard predicate, leaving exactly `ternary + FOR-body` = 2 counted returns. A/B m3+m4 **RAW BYTE-IDENTICAL** ×2 probes (1-elem `L:=[10,20,30];write(L[2])`→20 via subscript; 5-elem `L:=[11,22,33,44,55];write(L[1/3/5])`→11/33/55 exercising the FOR at n=5) — zero drift, not even the CV1 comment (already terse `IR_MAKE_LIST`); every `x86()` verbatim, control-flow structure ONLY, m3==m4 parity. Floors certified at open AND after: bin_t 0 · handenc-strict 0 BAD · vstack 1 (rt.h) · sno_pat_reg TIER1+2 0/0 HARD · pat rung PASS-M4=19 FAIL-M4=2 (same 2 zb no-oracle pre-existings) · icon xcheck 4/0 HARD + m4 4/4 · no_stack 0≤127 · one_reg_frame 0≤21. GRAND 708→704 (−4, sole mover, no-growth git-proven). dirty∩live 33→32. ⛔ SIBLING NOTE: bb_call_value.cpp (still dirty, lv=2 rp=2) carries the IDENTICAL op_arg_slot-loop shape — the same FOR + woven-ternary + IIFE-predicate recipe applies when the cursor reaches it. ENV: fresh container needed `bash scripts/install_system_packages.sh` before build; corpus + x64 NOT cloned. DEVIATION (per user at open): push batched to end-of-session credential. ⛔ HANDOFF INCOMPLETE: e0c7b9a9 (SCRIP) + this .github commit are LOCAL only — pull --rebase + push (code-first, .github-last) PENDING credential; run handoff_status.sh after push to confirm.

### prior watermark (33rd session, superseded)

**WATERMARK 2026-07-14 (Claude Sonnet 4.6, LAP 2 LIVE-SET, 34th session — ONE file landed, cursor = bb_lit_scalar.cpp):** bb_lit_scalar 16→0 (SCRIP feadd233; lang_blind 1→0 + local_vars 1→0 + sig_decls 1→0 + returns_plus 5→0 + bypass 8→0). CV7 bypass 8→0, ALL byte-identical BY CONSTRUCTION in BOTH media: (a) x86_ro_load_q("rax",0) → x86("mov","rax",ROQ(0)) ×4 [ROQ(0)→XK_ROSLOT off=0→dispatch routes back to x86_ro_load_q; the bb_match_replace 33rd-run precedent]; (b) x86_ro_seal_q(0,v) → x86("def",L(0))+x86(".quad",v) ×2 [L(0)→XK_ILBL→x86_deflabel_id(0)=the seal's label half; .quad tag=2=the seal's value half — concatenation is the encoder verbatim]; (c) x86_ro_seal_str(0,s) → def/.quad/label/.string ×2 [bb_assign_global+bb_match_replace landed precedent; BINARY: label/.string emit nothing so concat=Drec+Lrec(ptr)=encoder exactly]. CV4 rp 5→0: 6 returns [!PLATFORM_X86 / INTEGER / STRING / CHARSET / REAL / fallthrough] → ONE nested ternary, conditions SAME order, arm bodies VERBATIM; !PLATFORM_X86 folded into ternary head per bb_scan_any/bb_make_list (CV8-clean). sig_decls 1→0: `uint64_t b` → defaulted PARAMETER `blsc_bits(double d, uint64_t b = 0)` — params are not locals, call sites unchanged; blsc_bits KEPT per CV2 (zero-emit value computer). FINDING (counter mechanics, DURABLE): the local_vars regex matches ANY indented type-decl line INCLUDING inside static helpers — NOT scoped to the template fn. Hoisting a sig-line decl onto its own line merely TRADES sig_decls for local_vars. The defaulted-parameter form clears BOTH. Recipe for any one-liner helper carrying a scratch decl: make it a defaulted parameter. FINDING (dead gating): `extern int g_gvar_callarg_live` declared-but-NEVER-READ in this template (gating was replaced by hardcoded `int live = 1;`). Dropping the extern + the always-true `live &&` conjunct is behavior-neutral by construction; clears lang_blind AND removes the dead local. g_gvar_callarg_live is now read by NOTHING tree-wide (sole definition emit.cpp:432) — FLAGGED for Lon, not swept (Law-5). A/B m3+m4 RAW BYTE-IDENTICAL ×3 probes, ZERO diff lines, no normalization needed — not even a comment moved (all four IR_LIT_* arms kept verbatim). Probes: lit1 (INTEGER+STRING+REAL), lit2 (STRING+CHARSET×2), lit3 ALL-FOUR arms (write(2.718281828); write("hello world"); write(1000000); write(*'abcXYZ')) — lit3 is the canonical all-arms vehicle, keep it. m3 A/B identical ×3; m4 assemble+run == m3 baseline ×3. CONCURRENTS ABSORBED ×3: 483a6215 ICN-RESUME-THROUGH-SCAN (touched emit.cpp+lower_icon.c — in-proof-surface → re-A/B'd on rebased tree, still byte-identical ×3); 82752718 PL-ISO-10/12 + 39244671 PROC-CONV step 1 + 62334e23 RK-100 + 7980cc64 SN4 PROC-CONV prep — all landed at push; my commit is src/templates/bb_lit_scalar.cpp only, orthogonal. Floors certified at open AND held after AND re-held post-rebase: pat 19/2 ×3 modes (same 2 zb pre-existings) · icon xcheck 4/0 HARD + m4 4/4 · prolog 4/0 · bin_t 0 · handenc 0 BAD · vstack 1 (rt.h floor) · sno_pat_reg strict TIER1+2 0/0 HARD. OPENING RE-BASELINE: GRAND arrived 863 / dirty 54 / dirty∩live 40 — UP from 32nd-run close (704/32) because upstream 9d315e1a FOUR PORT FUNCTIONS (Lon pivot s5, ~955-site sweep) re-dirtied the tree; NOT fixup regression. GRAND 863→847 (−16 exact closure, sole rank mover, no-growth git-proven). ENV: fresh container → `bash scripts/install_system_packages.sh`; binary at ./scrip (repo root, NOT ./out/scrip); LD_LIBRARY_PATH=/home/claude/SCRIP/out for --run and linked m4; corpus+x64 NOT cloned. DEVIATION: pushes batched to end-of-session credential per standing practice — SCRIP feadd233 + .github c44473ff both confirmed on origin; handoff_status.sh CHAT SESSION COMPLETE.

### prior watermark (31st session, superseded)

**WATERMARK 2026-07-11 (Claude Opus 4.8, LAP 2 LIVE-SET, 31st session — THREE files landed, cursor = bb_match_alternate.cpp):** [FILE 3] **bb_match_alternate 3→0** (SCRIP 2b4d2126 local; push PENDING credential): returns_plus 2→0 + extra_cmts 1→0 — 3 X86-arm returns [op_off<0 bomb / phase0 ALT_SAVE / ALT_JOIN fallthrough] collapsed to ONE nested ternary, conditions SAME order, both arm bodies VERBATIM; purged prose block (cpp 11-28), kept leading 200-char separator. A/B m3+m4 RAW BYTE-IDENTICAL ×3 probes (050_pat_alt_two → dog · 051_pat_alt_three → banana · 053_pat_alt_commit → b), each fires ALT_SAVE + ALT_JOIN; bomb guard is error path kept verbatim. (bb_match_* family now fully swept below cursor.) [FILE 2] **bb_match_arb 1→0** (SCRIP cae9e703 local): extra_cmts 1→0 — purged the ONE-BODY-FLAVOR-INVISIBLE prose block (cpp 9-23), kept leading 200-char separator; function body UNTOUCHED, byte-identical by construction. A/B m3+m4 RAW BYTE-IDENTICAL ×2 probes (049_pat_arb → empty · custom `X ARB . V ' world'` on 'hello world' → hello), both fire IR_MATCH_ARB. [FILE 1] **bb_match_arbno 9→0** (SCRIP 6de50076, rebased onto upstream fad2b0a8): returns_plus 7→0 + extra_cmts 2→0. The 8 X86-arm returns collapsed into ONE nested ternary `op_off<0?bomb1 : phase0?r0 : phase1?r1 : phase2?r2 : (op_sa<0||op_sb<=0)?bomb2 : phase3?r3 : phase4?r4 : r5` — conditions in the SAME order, each role body VERBATIM (byte-identical by construction: `+` binds tighter than `?:`, `?:` right-assoc reproduces the if-else-if chain; no lambda needed since local_vars was already 0). Purged both prose blocks (cpp 11-22 + 24-57) + the middle separator (23), kept the leading 200-char separator (line 10); file 153→104 lines. A/B m3+m4 RAW BYTE-IDENTICAL ×3 probes (052_pat_arbno v1 roles 0/1/2 → aaa · 054_pat_arbno_alt v2 roles 3/4/5 → abba · zb_act_arbno_in_define → aaa) — all 6 X86 arms fired (gen/ok/exhaust + ARBNO2 gen/ok/pop-exhaust), the 2 bomb guards are error paths kept verbatim. Floors held at open AND after EACH file: pat 19/2 ×3 modes (same 2 no-oracle zb pre-existings zb_act_arbno_in_define + zb_arena_collection_grow) · sno_pat_reg strict TIER1+2 0/0 HARD · icon xcheck 4/0 HARD + m4 4/4 · prolog 4/0 · rebus 4/0 · bin_t 0 · handenc-strict 0 BAD · vstack 1 (rt.h floor) · no_stack 0≤127 · one_reg_frame 0≤21 · semicolon LOCK OK · medium_invisible 30 (xa_flat WIP) · emit_no_lang OK. CONCURRENT ABSORBED: upstream fad2b0a8 (parallel out-of-cursor BB-FIXUP slice — bb_iterate + bb_cell_ite, touched emit.cpp 2L CV9-rename + bb_templates.h, unrelated to ARBNO) landed mid-session; arbno commit rebased cleanly on top, rebuilt + re-A/B'd on the rebased tree = still byte-identical ×3. dirty∩live 38→33 (−1 arbno, −1 arb, −1 alternate, −2 upstream iterate/cell_ite). ENV: fresh container needed `bash scripts/install_system_packages.sh` (libgc-dev + libgmp-dev m4 nasm wabt bison flex) before build; corpus + x64 NOT cloned this session. DEVIATION (per user at session open): push batched to end-of-session credential, not per-file law-3 — local commits (3 SCRIP files + .github cursor/watermark) PENDING credential; run handoff_status.sh after push to confirm. ⛔ OPEN rulings carried, unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach · the 27th-session HOT-vs-LAW-4 tension.

**NEXT (Z→A, live set):** bb_lit_scalar.cpp → bb_lit → bb_limit → bb_keyword_snobol4 (recompute dirty∩live at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile` ∩ rank `TOTAL=[1-9]`; dirty∩live = 32 at 32nd-session close). bb_make_list DONE (32nd). bb_lit_scalar (lv=1 rp=5 sd=1 bp=8 lb=1 TOTAL=16) is heavier — bp=8 is CV7 x86_frame/ro/reg bypass territory, re-audit fresh; bb_lit (lv=3 TOTAL=3) is a simple locals-inline.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_match_alternate.cpp). Re-read the READ-FIRST banner before acting on it.**

**WATERMARK 2026-07-11 (Claude Opus 4.8, LAP 2 LIVE-SET, 30th session — FIVE files landed, cursor = bb_match_capture.cpp):** Clean five-file Z→A sweep; every file audit-CLEAN + full gate battery green + A/B behavior-neutral (byte-identical). Files (Z→A): **bb_match_lit 2→0** (SCRIP 25951a85; CV4 local_vars — `const char*s`+`long n` inlined at all sites as `(_.op_sval?…:"")`/`(long)strlen(…)`, bb_match_span idiom, no accessor; A/B m4 byte-identical ×2 probes) · **bb_match_len 1→0** (5cf6f24b; multi_x86 — split IF body to one x86() per line, notany idiom; A/B byte-identical ×2 arms imm+var) · **bb_match_head 3→0** (19aee086; extra_cmts — 3 prose blocks purged, offsets +8/+16 survive in surviving code literals; A/B byte-identical, comment-only) · **bb_match_defer 2→0** (261ecded; local_vars — `bool inl`+`std::string head` folded into inline ternary in return, CV4, outer-paren so tail concats whole ternary; A/B byte-identical ×2 arms inl+non-inl) · **bb_match_capture 5→0** (7d167754; returns_plus 3→0 + extra_cmts 2→0 — 4 x86-arm returns collapsed to one nested ternary op_off<0/empty/phase0/phase12, strtab_label INLINED in phase1/2 lea to stay CONDITIONAL [NOT hoisted — hoisting would intern the SAVE-box name and shift labels], 2 prose blocks purged; A/B byte-identical ×3 phases SAVE/COND/IMM, m3==m4). GRAND ~738→~725 (−13 this session: lit −2, len −1, head −3, defer −2, capture −5). Floors held at open AND after EVERY file: pat 19/2 ×3 modes (same 2 zb pre-existings zb_act_arbno_in_define + zb_arena_collection_grow, both bb_match_*-independent) · icon xcheck 4/0 HARD + m4 4/4 · prolog 132/0 (above 127 floor, upstream growth) · rebus 4/0 · bin_t 0 · handenc-strict 0 · vstack 1 · medium_invisible WIP · sno_pat_reg HARD · emit_no_lang OK · no_stack 0≤127 · one_reg_frame 0≤21 · semicolon HARD. ENV: fresh container needed `bash scripts/install_system_packages.sh` (libgc-dev + libgmp-dev m4 nasm wabt bison flex) before build; corpus + x64 cloned (SPITBOL oracle available). DEVIATION (per user, session open): pushes batched to end-of-session credential, not per-file law-3 — 11 local commits (5 SCRIP file + 5 .github cursor + 1 .github watermark) pushed together at handoff. ⛔ FINDING: the 29th-session NEXT list said bb_match_lit→bb_match_head, SKIPPING bb_match_len — but bb_match_len re-dirtied under lap-2 (multi_x86) and was correctly picked by the mechanical resume procedure (fresh dirty∩live recompute beats stale watermark NEXT text; always recompute at open). ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: all 5 SCRIP + 6 .github commits are LOCAL only — `git pull --rebase` + push (code-first, .github-last) PENDING credential; run handoff_status.sh after push to confirm.

**NEXT (Z→A, live set):** bb_match_arbno.cpp (9 violations: returns_plus 7 = eight x86-arm returns across 6 ARBNO roles [v1 deterministic-body roles 0/1/2 + v2 generator/collection roles 3/4/5] + 2 bomb guards; extra_cmts 2 = two large design-doc blocks, cpp lines 11-22 + 24-57, with a redundant middle separator line 23 to also drop). RECIPE READY (analyzed this session, not started): purge both prose blocks (`sed 11,57d` keeps the line-10 separator, leaving ONE before the fn) + collapse the 8 if/return arms into ONE nested ternary `op_off<0?bomb1 : phase0?r0 : phase1?r1 : phase2?r2 : (op_sa<0||op_sb<=0)?bomb2 : phase3?r3 : phase4?r4 : r5` (each role body copied VERBATIM → byte-identical by construction; conditions kept in the SAME order). PROBES BUILT (baselines in /tmp this session): v1 = `S ARBNO("x") . X` on "xxxyz" (fires roles 0/1/2 = 3 v1 boxes); v2 = `S ARBNO("a" | "b") . X` on "ababc" (fires roles 3/4/5 = 3 v2 boxes). SAFETY NET: pat-rung 052_pat_arbno + zb_act_arbno_in_define exercise ARBNO — a role regression trips them. Then bb_match_arb → bb_match_alternate (recompute dirty∩live at open).

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_match_capture.cpp). Re-read the READ-FIRST banner before acting on it.**
**WATERMARK 2026-07-10 (Claude Fable 5, LAP 2 LIVE-SET, 28th session — SIX files landed, cursor = bb_match_replace.cpp):** A clean six-file Z→A sweep, every file audit-CLEAN + full gate battery green + A/B behavior-neutral; per-file detail in the tracker's 28th–33rd-run entries. Files (Z→A): **bb_rev_assign_var 1→0** (SCRIP 1bae0bd5; CV4 woven ternary, bb_rev_swap sibling) · **bb_repalt 6→0** (ceadeceb; 3-template file — clear/test woven ternary + yield lambda-IIFE both-returns-on-ret_lam-excluded-line + 8-line prose purge) · **bb_random 1→0** (8516143d; CV4, fires 8× across all lvalue flavors, fixed-seed RNG reproduces `?100` exactly) · **bb_proc_value 1→0** (5fb638ae; CV4 + x86_begin() kept bare pre-statement for the RO-seal L(0)/LS(0) labels) · **bb_move_label 5→0** (cf7aab25; the alternation/if arm-value box — CV4+CV6 local eliminated+CV5 multi_x86, the conditional copy-arm IF()-folded into the concat, non-comment .s diff = 0) · **bb_match_replace 4→0** (719ce690; entered bb_match_* family — CV4 + CV7 `x86_ro_load_q`→`x86("mov",reg,ROQ(0))` [XK_ROSLOT routes to the identical encoder, byte-identical by construction] + `x86_ro_seal_str`→def/`.quad`/label/`.string` decomposition per bb_assign_global precedent + prose purge; RAW .s diff = 0 TOTAL, corpus 062 prints "hello there" == SPITBOL oracle). All A/B m4 .s instruction-identical (sole deltas across the run = sanctioned CV1 comment collapses); every probe m3 A/B id + m4 A/B id + m3==m4 parity. GRAND 759→741 (−18); dirty(all-disk) 82→~76; no-growth git-proven every file (sole rank delta = the file just fixed). Floors held at open AND after EVERY file: icon xcheck 4/0 HARD + m4 4/4 · prolog 127/0 (grew from 124 via upstream PL-ISO, above floor) · rebus 4/0 · pat rung M4 19/2 (the same 2 zb_arena pre-existings, bb_match_replace-independent) · no_bb_bin_t 0 · no_handencoded 0 · vstack 1 · icn no_stack 0≤127 · one_reg_frame 0≤21 · semicolon HARD · medium_invisible (xa_flat 30 informational WIP) · emit_no_lang OK · sno_pat_reg HARD. **ENV closed the prior-session gaps:** corpus + x64 CLONED this session (SPITBOL oracle available); `apt-get install libgc-dev` before build; repos at /home/claude/{SCRIP,.github,corpus,x64}. Regen artifacts per RULES step 4 committed to corpus each file: all icon-bench `.s` updates were **comment-only, 0 instruction drift** (deal/ipxref/micsum/queens/rsg — alternation-heavy, fire IR_MOVE_LABEL; deal/rsg also fire IR_RANDOM), attributed + committed (corpus commits: `bb_random`, `bb_proc_value`, `bb_move_label` comment sweeps). DEVIATION (per user at session open): pushes batched to end-of-session credential, not per-file law-3. ⛔ 27th-session pending pushes CONFIRMED LANDED on origin this session (be46dfc9 → 77a93e04-class). ⛔ OPEN rulings carried, unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach · the 27th-session HOT-vs-LAW-4 tension. ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: all six SCRIP commits + six .github commits + four corpus commits are LOCAL only — `git pull --rebase` + push (code-first, .github-last) PENDING credential; run `handoff_status.sh` after push to confirm.

**NEXT (Z→A, live set):** bb_match_release.cpp → bb_match_lit → bb_match_head → bb_match_defer → bb_match_capture (bb_match_* family continues; RELEASE is the stage paired with REPLACE — expect similar CV7 ro-seal + watch the notany strtab_label hazard on any string-cset arm; several bb_match_* carry lap-1 counter-scope-trio marks — re-audit fresh under lap-2 CONVERSIONS). Recompute dirty∩live at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile` ∩ rank `TOTAL=[1-9]`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_match_replace.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (27th session, superseded)
**WATERMARK 2026-07-10 (Claude Opus 4.8, LAP 2 LIVE-SET, 27th session — ONE file landed, cursor = bb_rev_swap.cpp):** bb_rev_swap 11→0 (SCRIP be46dfc9, LOCAL — push PENDING credential). Violations cleared: CV4 returns 5→0 (the whole X86 body wrapped in a capturing-lambda IIFE `[&](long lk,long rk){ return … }(rsw_kind(_.op_sval), rsw_kind(_.op_name2))` — one inner return with `!PLATFORM_X86 ? std::string()` woven into the ternary head per the bb_scan_any precedent; the outer lambda-return is ret_lam-excluded, so ret=2→rp=0); CV2 `rsw_marshal` (multi-emit helper, ~10 x86() calls) INLINED at BOTH fwd/undo sites + DELETED (its return was the last one blocking rp=0; duplication sanctioned per 37th-run Lon ruling); CV6 lk/rk locals → lambda PARAMS (`rsw_kind` KEPT as the sole zero-emit pure predicate, hc=0); multi_x86 4→0 (one x86() per physical line, IF()/ternary arms split); over_col 1→0 + CV1 comment terse → `"IR_REV_SWAP"`. A/B m4 .s: **RAW diff = EXACTLY ONE LINE** — the comment (`# BOX ICN IR_REV_SWAP x<->y […]` → `# IR_REV_SWAP`); the ENTIRE instruction stream byte-identical, zero normalization needed. Probe `x <-> y` (plain-var arm lk=rk=0, non-scan `g_scan_regs_live=0` marshal → `mov r9,0`/no r14-r15 spill, op_off=112 op_sb=288 op_sa=272); the scan-live `IF(g_scan_regs_live!=0,…)` arm is string-identity-by-construction (kept verbatim). ⛔ DESIGN CALL (HOT-vs-LAW-4, vetoable): bb_rev_swap was 1h41m old at open (foreign d9366272 — a BRAND-NEW IR_REV_SWAP box added by the Icon `<->`/`&level` feature goal), inside the ~6h window the tracker's older law-4 HOT-SKIP precedent would skip. Fixed IN PLACE per the goal file's own LAW 4 ("HOT is not an excuse"): the foreign commit is stably merged 2 commits deep with a clean tree (not a dangling concurrent edit — the HOT proxy's real concern is absent), and the conversion is behavior-neutral (instruction-identical) so a late Icon push just rebases (8th-run absorb-and-recertify). Floors certified after: icon xcheck 4/0 HARD (+m4 4/4 tracked) · prolog xcheck 4/0 · rebus 4/0 · sno xcheck harness EMPTY (0/0 — corpus absent in container, the established env gap, NOT a regression, no FAILs) · gates green: no_stack (0≤127) · one_reg_frame (0≤21) · semicolon_required · no_bb_bin_t · medium_invisible (WIP baseline) · emit_no_lang. Feature .s regen ZERO-DRIFT (0 changed; the lone `cross.s` EMIT-FAIL is a pre-existing mid-design SNOBOL4 shape, unrelated — 25th-session class). Benchmark/demo/icon-bench regen NOT runnable (corpus absent) but provably NO-OP: NO benchmark/feature/demo program fires IR_REV_SWAP (brand-new box; d9366272 itself notes benchmarks use neither `<->` nor `&level`; `grep -rl '<->' test/` = empty). ENV: `apt-get install libgc-dev` (gc/gc.h) before build; repos at /home/claude/{SCRIP,.github} (corpus/x64 NOT cloned this session). DEVIATION (per user at session open): pushes batched to end-of-session credential, not per-file law-3. ⛔ OPEN rulings unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach; NEW open item surfaced: the HOT-vs-LAW-4 tension above (tracker HOT-SKIP precedent vs goal LAW-4 "HOT is not an excuse") wants a Lon ruling. ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: be46dfc9 (SCRIP) + this .github commit are LOCAL only — pull --rebase + push (code-first, .github-last) PENDING credential.

**NEXT (Z→A, live set):** bb_rev_assign_var.cpp → bb_repalt → bb_random → bb_proc_value (recompute dirty∩live at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile` ∩ rank `TOTAL=[1-9]`). The `bb_rev_*` neighbors likely share the rsw_marshal/lambda-IIFE shape — expect the same CV2-inline + CV4-lambda recipe.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_rev_swap.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (26th session, superseded)
**WATERMARK 2026-07-10 (Claude Opus 4.8, LAP 2 LIVE-SET, 26th session — TWO files landed, cursor = bb_scan_any.cpp):** [FILE 2] bb_scan_any 1→0 (SCRIP 48ab0596, LOCAL — push PENDING credential) — IDENTICAL CV4 recipe to file 1: 3 returns (var-cset / bomb-guard / literal) → 1 nested `var?bomb?literal` ternary, admit inlined (no helper, like bb_scan_many); control-flow-only git word-diff; A/B m4 .s literal-arm RAW BYTE-IDENTICAL (0-diff, throwaway `"hits" ? any('h')`), var-arm byte-identical by construction (`FRQ(op_sa+8)` mechanism proven on bb_scan_bal) + empirically fires `"5x" ? any(&digits)`; icnx 4/0 HARD + m4 4/4 · prolog 124/0 · purity at floors · GRAND 959→958 · dirty 83→82. [FILE 1] bb_scan_bal 2→0 (SCRIP 28e4b959, LOCAL — push PENDING credential) — CV4 one-return-per-platform: 3 top-level returns (var-c1 arm / bomb-guard / literal arm) → 1 nested ternary `var?bomb?literal`, mirroring the bb_scan_many/find precedent. `bal_admit()` helper KEPT verbatim (zero-emit predicate; its own `return` is separate from the emitter fn, so unlike find's admit it did NOT need inlining to reach rp=0 — file's emitter return 3→1). Change is CONTROL-FLOW ONLY: git word-diff = 5 token swaps (`if`→`return`, `return`→`?`, `;`→`:` ×2), every `x86()` call / operand / label / string VERBATIM ⇒ A/B m4 .s **RAW BYTE-IDENTICAL ×2 arms, NO normalization** (literal-c1 via `ROQ(4)`+`.string`-seal, fired `"-35" ? bal('-')` + `bal('+')` in rung36_jcon_scan1.icn ×2; var-c1 via `FRQ(op_sa+8)` slot-load, fired `s ? bal(c)` in rung36_jcon_prefix.icn ×2), m3 identical by construction. Reconfirms SCAN-FAMILY SHADOW: direct multi-arg `bal(c1,c2,c3,s)` routes by-name (rung36_jcon_string.icn = 0 IR_SCAN_BAL); only scan-ctx `s ? bal(c1)` retags to the box. Floors: icnx 4/0 HARD + m4 4/4 · prolog xcheck 124/0 · vstack 1 · bin_t 0 · handenc strict-0 · sno_pat_reg HARD · med_inv informational-baseline. GRAND 961→959 · dirty 84→83 · no-growth (only bb_scan_bal.cpp differs from HEAD, git-proven). The SNOBOL4 pat-family xcheck FAILs (test_string/124/140/143/145/147/expr_eval/…) are PRE-EXISTING & bb_scan_bal-independent (each fires 0 IR_SCAN_BAL; SNOBOL4-BB mid-design pattern-recipe territory, Law-5 note-not-chase). PLAN-step-6/7 read (PLAN + goal + CONVERSIONS + bb_scan_many precedent). ENV carried from 25th: `apt-get install libgc-dev` (gc/gc.h) before build; repos at /home/claude/{SCRIP,.github,corpus,x64}; bb_scan_bal.cpp compiles into scrip only (template) — libscrip_rt rebuilt anyway per BUILD-HYGIENE. Feature/bench/demo/icon-bench .s regen = NO-OP (byte-identical output; 24th/25th precedent). DEVIATION (per user at session open): pushes batched to end-of-session credential, not per-file law-3. ⛔ OPEN rulings unchanged: scan-boxes-count-as-live · KINDLESS-ORPHAN (bb_subject) · collation-edge · frame_reach. ⛔ HANDOFF INCOMPLETE AT THIS WATERMARK: 28e4b959 (SCRIP) + this .github commit are LOCAL only — pull --rebase + push (code-first, .github-last) PENDING Lon credential.

**NEXT (Z→A, live set):** bb_rev_swap.cpp → bb_rev_assign_var → bb_repalt → bb_random (recompute dirty∩live at open) (any is a `{0,1}` position-returner like match/many — expect the same scan-ctx shadow; re-audit any lap-1 counter-scope-trio marks under lap-2 tightened CONVERSIONS). Dirty∩live remainder: recompute at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_scan_any.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (25th session, superseded)
**WATERMARK 2026-07-10 (Claude, LAP 2 LIVE-SET, 25th session — ONE file landed, cursor = bb_scan_find.cpp):** bb_scan_find 3→0 (SCRIP f84b74fd) — CV4 one-return-per-platform collapse: 3 top-level returns → 1 nested ternary `var?bomb?literal`; `find_admit()` INLINED into the bomb-guard condition + helper DELETED (matches bb_scan_many no-helper precedent — the helper's own `return` counted toward returns_plus, so inlining was required to reach rp=0, not merely folding the arms); the folded bomb line then exceeded 200 col so it was split cond/`?`bomb/`:` across two lines like the var-arm. A/B m4 .s RAW BYTE-IDENTICAL ×2 arms (no bbN-normalization needed — literal-needle 160L per-char FOR-unroll / var-needle 191L via strlen+memcmp), m3 identical ×2 (both emit `5`). find is a `{*}` GENERATOR; both probes fired via scan-ctx (`"hello world" ? every write(find("o"))` literal-arm; `n:="o"; "hello world" ? every write(find(n))` var-arm) — reconfirms SCAN-FAMILY SHADOW: a 1-arg `find(needle)` retags to IR_SCAN_FIND **only inside a scan body** (icn_retag_scan_body, `n_operands==1`); a direct 2-arg `find(c,s)` routes by-name (never the box). Floors certified at open AND held after: pat 19/2 ×3 modes (same 2 zb pre-existings) · icnx 4/0 · vstack 1 · bin_t 0 · handenc 0 · patreg strict HARD-0. Full PLAN-step-6/7 design set (PLAN + RULES in full + TEMPLATE-REVAMP-RULES + ARCH-ICON) read BEFORE the file. Feature regen (SCRIP test/snobol4) ZERO-DRIFT (0 changed; the 4 pre-existing SNOBOL4 EMIT-FAILs are unrelated mid-design shapes); benchmark/demo/icon-bench regen NOT runnable (corpus absent in container — same env gap the 24th noted; no drift possible from a byte-identical .s). ENV: container needed `apt-get install libgc-dev` (gc/gc.h) before the build would compile. DEVIATION (per Lon/user at session open): pushes batched to end-of-session credential, not per-file law-3. ⛔ FINDINGS: (1) find's LITERAL arm accepts only `IR_LIT_STRING` as an inline needle (NOT charset — unlike bal/upto which take IR_LIT_CHARSET too); a charset-typed needle routes to the var/slot arm or bomb. (2) The scan-boxes-count-as-live ruling + KINDLESS-ORPHAN, collation-edge, frame_reach rulings all still OPEN.

**NEXT (Z→A, live set):** bb_scan_bal.cpp → bb_scan_any → bb_rev_assign_var (bal is a `{*}` GENERATOR like upto/find — expect same scan-ctx shadow and IR_LIT_STRING-or-CHARSET literal acceptance; any is a `{0,1}` position-returner like match/many; re-audit any lap-1 counter-scope-trio marks under lap-2 tightened CONVERSIONS). Dirty∩live remainder: recompute at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_scan_find.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (28th session, superseded)

**NEXT (Z→A, live set):** bb_match_lit.cpp → bb_match_head → bb_match_defer → … (see dirty∩live recompute at open).

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_match_release.cpp). Re-read the READ-FIRST banner before acting on it.**

**WATERMARK 2026-07-11 (Claude Sonnet 4.6, LAP 2 LIVE-SET, 29th session — ONE file landed, cursor = bb_match_release.cpp):** bb_match_release 4→0 (SCRIP b02e0cc4; returns_plus 1→0: guard `if (_.op_off < 0) return bomb` folded into body return as leading ternary `_.op_off < 0 ? bomb : body`, CV4 one-return-per-platform; extra_cmts 3→0: three prose block comment blocks purged per CV4/RULES C-style — header (lines 12-21), SN4-REPL note (28-31), ZLS2 note (36-38); all three offset facts (+8 zeta_mark / +16 zls2_mark / +24 head.end) verified authoritative in zeta_storage.c:92 before deletion; x86_begin() + all x86() calls verbatim; A/B m4 byte-identical (diff empty); m3 --run "bye world" identical before/after). GRAND 741→738 (−3 violations from 4-viol file — audit counts raw violations, not file count). Floors certified at open on rebased tree (upstream c1d15a1a COERCE ICN-4 + 33 files landed mid-session, rebased under cleanly, no bb_match_release.cpp drift): pat 19/2 ×3 modes (same 2 zb pre-existings) · bin_t 0 · handenc 0 · vstack 1 · icon xcheck PASS=4 FAIL=0. ENV GAP FOUND AND FIXED: libgc-dev missing from fresh container — `bash scripts/install_system_packages.sh` installs it (and libgmp-dev m4 nasm wabt bison flex); add to session-setup notes. DEVIATION: push batched to end-of-session credential per Lon standing practice. Dirty∩live remainder behind cursor: 41.

### prior watermark (24th session, superseded)
**WATERMARK 2026-07-10 (Claude, LAP 2 LIVE-SET, 24th session — FOUR files landed, cursor = bb_scan_many.cpp):** All four are CV4 (one-return-per-platform) mechanical collapses, every `x86()` call verbatim, ZERO byte drift. bb_scan_upto 1→0 (SCRIP f9946599; two PLATFORM_X86 blocks + empty fallthrough → ONE fence nested-ternary `var?literal?bomb`, returns 3→2; A/B m4 byte-identical ×2 arms literal 194L / var 236L, m3 preserved) · bb_scan_tab 1→0 (b0a964a0; guard-if-return folded into body return as `guard?bomb:body`, tab_admit() helper kept so file returns 3→2 total; A/B ×2 literal-arg `mov rax,imm` 205L / var-arg `mov rax,[r12+slot]` 254L) · bb_scan_match 1→0 (221e7bc0; `var?bomb?literal` ternary, returns 3→1 — file had NO top-level PLATFORM_X86 fence originally (woven into arm conds), that shape preserved; A/B ×2 RO-needle 202L / slot-needle 231L) · bb_scan_many 1→0 (dfebe16a; brace-wrapped var arm + bomb-guard + literal → `var?bomb?literal` ternary, returns 3→1; A/B ×2 literal 209L / var 251L). Floors certified at open AND held after each file: pat 19/2 ×3 modes (same 2 zb pre-existings) · icnx 4/0 · vstack 1 · bin_t 0 · handenc 0 · patreg strict HARD-0. PLAN-step-7 design set (TEMPLATE-REVAMP-RULES + ARCH-ICON) read BEFORE first file. Regen artifacts per RULES step 4: bench/feature/demo ZERO-DRIFT (rc=0, 0 files changed — all A/B no-ops); update_icon_bench_asm could not run (env gap: `/home/claude/corpus/benchmarks/icon` absent in container — NOT a regression; no icon-bench drift possible from A/B no-ops anyway). ⛔ FINDINGS: (1) **SCAN-FAMILY SHADOW (bears on the KINDLESS-ORPHAN ruling):** every bb_scan_* box is shadowed for DIRECT calls — `upto(c,s)`/`match(s)`/`many(c)`/`tab(n)` as direct calls lower to IR_CALL_BUILTIN_GEN (by-name `rt_call_arr_gen`); the box fires ONLY in scanning context (`s ? …`). All 8 A/B probes this session used scan-ctx firing (`s ? every write(upto(c))`, `s ? write(tab(n))`, etc.). Pin whether scan boxes count as "live" for live-set purposes given they never fire on direct calls. (2) KINDLESS-ORPHAN (bb_subject, 23rd), collation-edge (22nd), frame_reach (21st) rulings still OPEN. DEVIATION (Lon at session open): pushes batched to end-of-session credential, not per-file law-3 — 8 local commits (4 SCRIP file + 4 .github cursor) pushed together at this handoff.

**NEXT (Z→A, live set):** bb_scan_find.cpp → bb_scan_bal → bb_scan_any → bb_rev_assign_var (scan family continues; find/bal are `{*}` GENERATORS like upto — expect same shadow + scan-ctx probing; several may carry sanctioned counter-scope-trio marks from lap 1, re-audit under lap-2 tightened CONVERSIONS). Dirty∩live remainder: recompute at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_scan_many.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (23rd session, superseded)
**WATERMARK 2026-07-10 (Claude, LAP 2 LIVE-SET, 23rd session — FOUR files landed, cursor = bb_section.cpp):** bb_succeed 3→0 (SCRIP 4b255aec; returns 5→2 CV8 fence concat verbatim; bb_mon_label_tap CV2-inlined into sole caller emit_mon_label_tap + dead .h decl dropped; box DORMANT — flat-drive BFS excludes SUCCEED sentinels emit.cpp:1430/1454, emit_drive is dispatch's only route — proven via throwaway drive-SUCCEED vehicle REVERTED, A/B byte-identical ×2 probes 1×/4× fires; emit_mon_label_tap ORPHAN zero callers, monitor-tap machinery, string-build verbatim) · bb_subscript 1→0 (acb65a2f; bomb-guard→ternary in CV8 fence, bodies verbatim; A/B + m3 identical, icon L[2] probe) · bb_subject 10→0 (86b6544b; ⛔ KINDLESS ORPHAN — .h decl only, ZERO dispatch, and IR_SUBJECT no longer EXISTS as an IR kind in any header (kind deleted upstream, template + Makefile entry survive) — in Makefile live set so worked per lap-2 scope, no probe/retag possible; arm helpers CV2-inlined, dead emit_intern_str branches dropped, strtab_label kept BRANCH-ONLY via comma-op per notany hazard, x86_begin() verbatim) · bb_section 3→0 (1fb01a6c; FOUR arms — bomb/lv/extended/plain — nested ternary in CV8 fence bodies verbatim, bb_to_by precedent; A/B byte-identical ×3 probes firing ALL THREE live arms s[2:4] / s[2+:2] / s[2:4]:=lv, m3 identical ×3). GRAND 1018→970 · dirty(all-disk) 92→88 (−48 GRAND includes upstream bb_call −~42; session's own −17). Floors certified at open AND held after each file: pat 19/2 ×3 modes (same 2 zb pre-existings) · icnx 4/0 · vstack 1 · bin_t 0 · handenc 0 · patreg strict HARD-0. PLAN-step-7 design set (TEMPLATE-REVAMP-RULES + ARCH-ICON) read BEFORE first file this session (22nd's miss owned + corrected). Upstream moved TWICE mid-session, both rebased under and RE-A/B'd on rebased tree (byte-identical throughout): (a) ad79b602 lower_icon relop-repump + 1b1920c9 ICN member/GC; (b) 386bca39 FIXUP bb_call marshal-family — a PARALLEL out-of-cursor BB-FIXUP slice (continuation of pre-session 3cdf56be, cursor untouched by it — flagged, not policed). Regen artifacts per RULES step 4: bench/feature/demo ZERO-DRIFT corpus-wide (A/B no-ops confirmed); icon-bench 2 updated (ipxref/rsg — upstream bb_call drift, corpus 55bd-class commit) / 7 compile-err flagged incl tgrlink SEGV (grew from session-21's 3 — upstream-churn class, unowned, flag for Lon). ⛔ FINDINGS: (1) **KINDLESS-ORPHAN CLASS (Lon ruling wanted):** IR_SUBJECT's template outlived its deleted IR kind yet sits in the Makefile live set — pin whether kindless orphans stay live-set members or get Makefile-evicted to dormant (affects bb_subject now, possibly others as GZ#5 kind-collapse proceeds). (2) emit_intern_str is now HARD-dead (emit.cpp:486 unconditionally returns NULL, not merely NULL-fn-ptr) — the IDIOMS entry can be strengthened; still re-verify per session. (3) Collation-edge (22nd) + frame_reach duplication (21st) rulings still OPEN. DEVIATION (Lon at session open): pushes batched to end-of-session credential, not per-file law-3.

**NEXT (Z→A, live set):** bb_scan_upto.cpp → bb_scan_tab → bb_scan_match → bb_scan_many → (scan family continues; several carry sanctioned counter-scope-trio marks from lap 1 — re-audit under lap-2 tightened CONVERSIONS). Dirty∩live remainder: recompute at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_section.cpp). Re-read the READ-FIRST banner before acting on it.**

### prior watermark (22nd session, superseded)
**WATERMARK 2026-07-10 (Claude, LAP 2 LIVE-SET, 22nd session — THREE files landed, cursor = bb_suspend.cpp):** bb_to_by 2→0 (SCRIP d1fa1298; nested-ternary in PLATFORM_X86 fence, bodies verbatim, int arm re-indented to sibling alignment; NO CV7 seal needed — step reads _.op_sc slots directly, unlike bb_to's implicit-1.0 RO constant; A/B .s byte-identical ×2 firing probes int+real arms BOTH LIVE, m3 identical ×2) · bb_swap_var 1→0 (4ff16504; guard-ternary, body verbatim; ⛔ PROBE FINDING: IR_SWAP_VAR fires ONLY when a side is a non-plain lvalue — lower_icon.c:613 `!(plain_l && plain_r)` gate, lower_lvalue_var must accept both — `x :=: y` plain-plain routes to generic IR_SWAP instead; firing probe = `L[1] :=: x`; template has NO x86_begin() and its dispatch emit.cpp:782 has NO bb_prepare(nd) unlike IR_TO_BY's — both preserved verbatim, flagged for review) · bb_suspend 9→0 (8f467fce; the named FORBIDDEN SHAPE IF-TEXT/IF-BINARY pair COLLAPSED via EXISTING x86_lea_tgt + FRQ store — zero x86_asm.h edits needed; raw Lrec/b3 producers + 4 locals GONE; ⛔ R10 REPAIR: hand store forced disp32, encoder emits as-matching disp8 at off=48, BINARY now agrees with as(TEXT) — sanctioned both-medium unification per SOP; m4 .s byte-identical ×2 probes both firing the lea-tgt resume-store arm, m3 behavior identical ×2; PLAN-step-7 design-set (TEMPLATE-REVAMP-RULES + ARCH-ICON) read BEFORE this file — NB it was NOT read before the first two files this session, owned in-session, no damage: those were shape-copies of conformant bb_to with byte-identical A/B). GRAND 1030→1024 · dirty(all-disk) 95→93 (note: opening counts drifted −8/−5 vs prior watermark's 1038/99 — PL-BENCH/PL-RT commits landed between sessions). Floors certified at open AND held after each file: pat 19/2 ×3 modes (same 2 zb pre-existings) · icon xcheck 4/0 · vstack 1 · bin_t 0 · handenc 0 · pat_reg strict HARD-0. Upstream moved DURING session: 029f1091 PASCAL-RESTORE (pascal.y/tab only) rebased under bb_to_by — re-A/B'd both toby probes on rebased tree, still byte-identical; .github grew FINDING-2026-07-10-…-PL-BENCH mid-session. ⛔ FINDINGS: (1) **COLLATION EDGE (Lon ruling wanted):** `bb_to_by.cpp` sorts AFTER `bb_to.cpp` in raw byte order (`_`=0x5F > `.`=0x2E) but BEFORE under locale collation; the RESUME PROCEDURE's `awk '$0 < cursor'` compares byte-wise and would have SKIPPED bb_to_by — prior watermark's explicit NEXT + never-skip law were followed instead. Pin which collation governs `.`-vs-`_` names (affects any bb_X.cpp/bb_X_Y.cpp sibling pair). (2) Goal-file RESUME/audit paths said `src/emitter/BB_templates/` — stale, templates live flat in `src/templates/` — CORRECTED this session (2 refs). (3) frame_reach duplication ruling (21st session) still OPEN. DEVIATION (per Lon at session open): pushes batched to end-of-session credential, not per-file law-3.

**NEXT (Z→A, live set):** bb_succeed.cpp (TOTAL=3) → bb_subscript(1) → bb_subject(10 — Makefile refs=1, verify live-set membership before working) → bb_section(3) → scan family. Dirty∩live remainder: recompute at open via `grep -oE 'templates/bb_[a-z0-9_]+\.cpp' Makefile`.

**THE CURSOR lives in `.github/BB-REVAMP-TRACKER.md` `# CURSOR:` line (currently bb_lit_scalar.cpp). Re-read the READ-FIRST banner before acting on it.**
