# FINDING 2026-08-22 seat02 — rung E-2 addendum: the two files seat11 left open, plus a dispatch-race note

## 1. WHAT HAPPENED THIS SESSION

HQ dispatched rung `E-2` (`DISPATCH-R10-R11-ERADICATION.md`) to seat02 directly via inbox, separately from the `next`-resumed QUEUE.tsv claim (`table-int-keys-and-nd-subscript`, left untouched, still locked to seat02 for a future session). Before editing, seat02 read all 17 of E-2's listed files, derived a full per-site liveness plan (register choice + justification for all 58 nominal / 59 true occurrences — `bb_lit_scalar.cpp`'s `ls_dual` carries `r11` twice on one source line), and implemented, verified (SNOBOL4/Icon crosscheck, Icon all-rungs 293-test suite, Prolog/Snocone smoke, four required gates, all via git-stash A/B), and committed all 17 files.

`git pull --rebase` then revealed **seat11 had independently landed most of the same rung** (`4e569b50`, pushed while this session was mid-edit — a genuine HQ dispatch race, not a claim-system failure: `E-2` is tracked outside QUEUE.tsv's claim locks, by design, per `DISPATCH-R10-R11-ERADICATION.md`'s own text). Per `RULES.md`'s disposable-clone protocol, seat02 saved its commit as a patch, re-cloned SCRIP fresh to pick up seat11's (and seat06's `rung-E6-x86-asm-h`, `3cf83181`) landed state cleanly, and re-examined what remained.

## 2. WHAT SEAT11 LANDED VS. WHAT WAS LEFT

Seat11's `4e569b50` covered 15 of the 17 files, using a **better mechanism** than this session's original plan for the alignment-padding sites: `sub rsp,N`/`add rsp,N` instead of push/pop-a-different-register — strictly safer, since it removes any register-liveness dependency at all rather than trading one register's liveness for another's. For `bb_idx_get.cpp`/`bb_idx_set.cpp` they found and used `rdi` (this session had independently found `rdx`/`rsi` for the same two files — both liveness-correct; seat11's landed first). Seat11 also discovered (§3 of their FINDING) that `bb_idx_get.cpp`, `bb_idx_set.cpp`, `bb_subject.cpp`, and `bb_initial.cpp` are **dead code** — unreachable from any live dispatch path — and that the scan family and `bb_glit`/`bb_gcc` belong to Icon and Raku respectively, not SNOBOL4.

Seat11's own FINDING (§5, item 5) explicitly named the two files their session did not touch: **`xa_bb_macro_library.cpp`** (1 occ) and **`bb_lit_scalar.cpp`** (2 occ) — "different idioms, not verified this session, left for a future pass." This session closed exactly that gap:

- **`xa_bb_macro_library.cpp`**: the `DELTA_LOAD` `.macro`'s `mov eax,[r11]` is dead text — grepped every caller of `DELTA_LOAD`/`POS_α`/`RPOS_α` across the live `src/` tree and found none; the only invocations are in `archive/backend/` (explicitly archived, not built) and a static `seed/beauty_prog_0428.s` fixture. `r11` → `rdx`; nothing executes this path today, so the choice is arbitrary but harmless.
- **`bb_lit_scalar.cpp`**: `ls_dual(w)`'s `w==8` arm used `r11` as a memory-to-memory shuttle register (`ZRES(w)` → `FRQ(op_off+w)`); the `w==0` arm already uses `rax` at the same call site, and `ls_dual(0)` always completes (storing its result) before `ls_dual(8)` starts — so `rax` is provably free for both arms. `ARCH-SNOBOL4-RTX.md` §2 already named this exact site as the precedent case for `rax`. The whole helper is gated behind `g_emit_cfg->pl_cells_graph` (Prolog-only, killswitch `SCRIP_PL_CELLS`, default OFF per `lower_prolog.c`'s `pl_cells_stamp`) — verified inert under default config (no corpus `.s` artifact changed by this edit) **and** correct under the flag: `SCRIP_PL_CELLS=1 test_smoke_prolog.sh` mode-4 reads PASS=0 FAIL=5 identically before and after (git-stash A/B) — the all-fail is a separate, pre-existing WIP gap in that experimental path, unrelated to this fix.

## 3. RESULT

Combined state after seat06 + seat11 + this session: the templates/emitter `test_gate_wreg_claim.sh` sweep surface is **204 → 129 occurrences, 4 files** (`bb_call_fn.cpp` 93, `xa_flat.cpp` 22, `bb_scan_match.cpp` 8, `bb_call_proc_staged.cpp` 6 — rungs E-1/E-3/E-5, all explicitly out of scope for E-2). `test_gate_wreg_claim_binary.sh` now reports `r10/r11-shape-checks=0` (down from 161 at session start). `x86_asm.h`'s pre-existing pin drift is gone (seat06's E-6). Verified this session, fresh clone, git-stash A/B throughout: SNOBOL4 crosscheck 322/1(m3) 321/1+1skip(m4) DIVERGE=0 (same single pre-existing failure, `160_pat_alt_inner_gen_resume`), Icon smoke 14/14 both modes, Prolog smoke 3/5 (pre-existing). `test_gate_emit_no_lang.sh` OK, `test_gate_sno_pat_reg.sh --strict` OK, `test_gate_wreg_claim_binary.sh` CLEAN, `make pristine` EXIT=0. `.s` artifacts regenerated in RULES.md order in both `SCRIP` and `corpus`; two of this session's own earlier `corpus` commits (made against the since-abandoned 17-file build, before the seat11 conflict was discovered) were corrected in place by a second regen pass once the fresh clone was built — self-correcting, not a new defect.

## 4. WHAT'S STILL OPEN (unchanged from seat11's own §5, not re-litigated here)

`bb_call_fn.cpp` (93 occ, rung E-1), `bb_scan_match.cpp`/`bb_call_proc_staged.cpp` (rung E-5, high blast radius, grouped with `emit.cpp`), `bb_define.cpp`'s remaining licensed γ/ω survivor (its own gated rung), `xa_flat.cpp` (rung E-3, Icon's own generator-continuation mechanism, out of scope by design), and the RTX hand-asm families (rungs A-1..A-4, 225 occ, not walked by any session yet). Claim `free-r11` (QUEUE.tsv rank 0) stays OPEN.

## 5. FOR HQ — THE DISPATCH RACE ITSELF

`rung-E2-scan-idx-family`/`E-2` was sent to at least two seats (seat11 and seat02) as a direct inbox dispatch, outside QUEUE.tsv's atomic pick-and-lock. No harm resulted — `git pull --rebase` surfaced the collision immediately, on the honest end (a genuine conflict, not a silent double-apply), and the disposable-clone protocol resolved it cleanly with the two sessions' work composing rather than colliding. Flagging so HQ can decide whether direct cross-cutting-ladder dispatches should route through a lock (a QUEUE.tsv row, or a dedicated claims file for `DISPATCH-*.md` rungs) before the next rung goes out to more than one seat at once.
