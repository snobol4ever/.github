## ⛔ LIVE CURSOR — s181 (2026-07-26): **SURVEY ONLY — ZB-VAL-8b USE-PREDICATE PASS DESIGNED, NOT LANDED.** Baseline `92e926cf` clean. Full narrative: `FINDING-2026-07-26f-CLAUDE-SN4-ZB-VAL-8B-RESULT-USE-IS-MEASURED-AND-THE-IR-HAS-NO-USE-COUNT.md`. HEADLINES: (1) ⭐ **"IF IT IS USED" IS REAL AND DISCRIMINATING** — `LT(A,B) :S()F()`: result cell written, never read, dead; `OUTPUT = LT(A,B) 'yes'`: same cell read by `str_concat_d`. Blanket zero-grant is wrong; grant is a genuine per-instance use test. (2) ⛔ **IR HAS NO USE COUNT** — `IR.h` has no `n_uses`/`use_count`/`consumed`; `ir_node_produces_value()` is opcode-keyed, not instance-keyed. ZB-VAL-8b requires a NEW ANALYSIS PASS separating `operands[]` (value use → grant) from γ/ω wires (control use → no grant). (3) ⭐ **SLIDING-OFFSET MECHANISM IS LIVE DEFAULT** — `FR()`/`FRQ()` emit `[rsp + off - op_fc_base]` on `x86_fc_hit`; `ZC_PORT = ZC_PORT_FORTH` by default. 108/120 templates use only FR/FRQ → convert with ZERO edits on grant. 12 hand-write `rbp` — those 12 ARE the entire job, landing on the four wall shapes exactly. (4) ⭐ **HYBRID ALREADY SHIPS** — `lt1.sno` main: four `sub rsp,16` self-carves + two `mov rsp,rbp` bulk whacks in one frame today. (5) ⚠ **WINDOWS MUST BE PER-NODE COMPUTED** — `lt1` flat: A=128·B=112·C0=96·C1=80·T=64; per-node width = deepest-operand-flat − own-flat + 16 → C0:48·C1:64·T:48. `lt2` allocates same construct at different offsets; no constant table. (6) ⚠ **FALLBACK IS SILENT** — undersized window emits `[rbp+off]` silently; wants `test_gate_fc_no_residual_rbp.sh`. **NEXT RUNGS**: ZB-VAL-8b-USE (operand-vs-wire analysis, `zeta_storage.c`; falsify: `lt1` carves 64 not 80) → ZB-VAL-8c (per-node `op_fc_wbytes` 48/64/48) → the gate → release edges (γ + shared ω `add rsp,K`; read `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` first — NOT YET READ). WATERMARK: m3 314/1 · m4 312/1 · DIVERGE=0. `handoff_status.sh` is the push truth.

## ✅ s180 (2026-07-26): Survey ZB-VAL-8 — two blockers found + resolved on paper. RBP=two jobs (addressing=convenience/frontier=necessity). Four walls measured. ARBNO=compound of retry+fence, no private wall. 8a depth-index refactor DELETED. → `FINDING-2026-07-26e`.

## ✅ s179 (2026-07-26): ZB-VAL-6a CONCAT + ZB-VAL-6b UNOP landed. β-dead invariant confirmed empirically (β-trampoline never fires on spine → uniform `wpop=d*16` safe). ZB-VAL-7 relops DELETED (SNOBOL4 has no infix relops; predicates are functions). Watermark m3 314/1·m4 312/1·DIVERGE=0. → `FINDING-2026-07-26d`.

## ✅ s178 (2026-07-26): ZB-VAL-1..5 landed — value spine reaches arbitrary arith trees with typed global-var leaves, all rsp cells. Binop net = `add rsp,16`. Post-order geometry shape-invariant. Fallible-box ω = full unwind via BP-9 op_wpop. Watermark m3 332/2·m4 323/1. → `FINDING-2026-07-26`.

## ✅ s177 (2026-07-26): ZB-VAL-0 + CF-GATE landed — first value-spine FORTH cell + constant-fold gate. Watermark m3 332/2·m4 323/1.

## ✅ s176 (2026-07-26): SN4-FLAT-PROC landed. One-statement scope. IR_SAVE_RESTORE + bb_save_restore.cpp. RETURN/FRETURN/NRETURN = program-wide floaters via rt_flat_ret_snap. Watermark m3 314/1·m4 312/1·DIVERGE=0.

## ✅ s175 (2026-07-26): SN4-PARSE-ROMAN landed. Four parser defects fixed (label-only line, blank between goto parts, lineno always 0, silent parse errors). .l flex-regenerable again. Watermark m3 314/1·m4 309/4·DIVERGE=3. → `FINDING` (roman).
