## ⛔ LIVE CURSOR — s182 (2026-07-26): **ZB-VAL-8b-USE LANDED.** Baseline `92e926cf` → this session. Full narrative: `FINDING-2026-07-26g-CLAUDE-SN4-ZB-VAL-8B-USE-LANDED-THE-PREDICATE-ALREADY-EXISTED.md`. HEADLINES: (1) ⭐ **s181's "needs a NEW ANALYSIS PASS" WAS HALF WRONG** — `zls_mark_value_refs` (`zeta_storage.c:266`, SLOT-ELIDE S1, s133) ALREADY computes the operand-vs-wire use fact with the exclusion list crosscheck-tuned; it merely died as a local `lv[]` inside `zls_build`. The work was PERSISTING it and giving it consumers, not writing it. The IR half of s181 stands: `IR.h` genuinely has no use count. **LESSON: grep for the fact before specifying the pass.** (2) ✅ **LANDED** — `zls_entry_t.live` (defaults 1, only the elide path stamps a measured 0) + `zls_result_live()` + `g_emit.op_res_live`, promoted at ONE dispatch site (`emit.cpp:1305`), deliberately NOT in `DRIVE_FILL`. TWO consumers: `bb_cmp_test` folds its two null stores behind `IF(_.op_res_live,…)` (R6-conformant); `IR_CMP_TEST`+`IR_ASSIGN` admitted to `zls_elide_ok` (both audited zero-locals). (3) ⭐ **MEASURED** — `lt1` 216→**152**, `lt2` 200→**168**, `lt1` matching its SLOT-CENSUS floor EXACTLY; demo artifacts 91 changed frame-carves (`1192→1032`, `328→248`), net −82 lines. ⚠ do NOT quote the ~77KB diff sum — it double-counts per file. (4) ⭐ **OPPORTUNITY REMAINING: 4,007 dead quads / 643 graphs = 28.7% = 64KB** (census, 52 programs). (5) ✅ **ZERO REGRESSION, MEASURED BOTH SIDES** — crosscheck `m3 314/1·m4 312/1·DIVERGE=0` exact; gates 28/24 with the 24 failures BYTE-IDENTICAL to baseline via stash→rebuild→diff (that 24 is pre-existing debt — `pl_gz*`/`icn_*`/isolation/beauty — somebody should know it is red). (6) ⛔ **THE ASSIGN ELIDE IS SAFE BY EVIDENCE, NOT BY PROOF** — `bb_assign_global` is the only vfc release arm; a mis-sized window makes `FR()` fall back to `[rbp+off]` SILENTLY. `lt1` residual rbp unchanged (32) and crosscheck clean, but **`test_gate_fc_no_residual_rbp.sh` is now a PREREQUISITE, not a nicety.** ✅ **THE GATE IS NOW LANDED** — `scripts/test_gate_fc_no_residual_rbp.sh`, baseline **0 misses / 52 programs / 643 graphs**; `x86_fc_hit` now separates GRANTED from HIT and narrates each fallback under `SCRIP_FC_AUDIT=1` (emission-neutral: lt1 `.s` byte-identical). Gates now 29 PASS / 24 FAIL, failure set still byte-identical to baseline. **NEXT RUNGS**: widen the predicate to the remaining 28.7% (mechanical: census → confirm `zls_grant_locals`==0 → admit → crosscheck) → write-side store elision for the assign family → ZB-VAL-8c per-node `op_fc_wbytes` → **statement-root spine entry (the vfc gate is STILL `a->op == IR_ASSIGN`; s181's "lt1 carves 64 not 80" falsification is UNTESTED — this session moved the FLAT frame, not the rsp spine)** → the four walls. WATERMARK: m3 314/1 · m4 312/1 · DIVERGE=0. `handoff_status.sh` is the push truth.

## ✅ s181 (2026-07-26): Survey only — ZB-VAL-8b designed, not landed. Measured the use-test as real and discriminating (lt1 dead vs lt2 live); read the IR as having no use count (correct). Missed that zls_mark_value_refs already computes the fact. → `FINDING-2026-07-26f`.

## ✅ s180 (2026-07-26): Survey ZB-VAL-8 — two blockers found + resolved on paper. RBP=two jobs (addressing=convenience/frontier=necessity). Four walls measured. ARBNO=compound of retry+fence, no private wall. 8a depth-index refactor DELETED. → `FINDING-2026-07-26e`.

## ✅ s179 (2026-07-26): ZB-VAL-6a CONCAT + ZB-VAL-6b UNOP landed. β-dead invariant confirmed empirically (β-trampoline never fires on spine → uniform `wpop=d*16` safe). ZB-VAL-7 relops DELETED (SNOBOL4 has no infix relops; predicates are functions). Watermark m3 314/1·m4 312/1·DIVERGE=0. → `FINDING-2026-07-26d`.

## ✅ s178 (2026-07-26): ZB-VAL-1..5 landed — value spine reaches arbitrary arith trees with typed global-var leaves, all rsp cells. Binop net = `add rsp,16`. Post-order geometry shape-invariant. Fallible-box ω = full unwind via BP-9 op_wpop. Watermark m3 332/2·m4 323/1. → `FINDING-2026-07-26`.

## ✅ s177 (2026-07-26): ZB-VAL-0 + CF-GATE landed — first value-spine FORTH cell + constant-fold gate. Watermark m3 332/2·m4 323/1.

## ✅ s176 (2026-07-26): SN4-FLAT-PROC landed. One-statement scope. IR_SAVE_RESTORE + bb_save_restore.cpp. RETURN/FRETURN/NRETURN = program-wide floaters via rt_flat_ret_snap. Watermark m3 314/1·m4 312/1·DIVERGE=0.

## ✅ s175 (2026-07-26): SN4-PARSE-ROMAN landed. Four parser defects fixed (label-only line, blank between goto parts, lineno always 0, silent parse errors). .l flex-regenerable again. Watermark m3 314/1·m4 309/4·DIVERGE=3. → `FINDING` (roman).
