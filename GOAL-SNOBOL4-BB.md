## ⛔ LIVE CURSOR — s179 (2026-07-26): **ZB-VAL-6a CONCAT + ZB-VAL-6b UNOP LANDED — the value spine now carries THE SNOBOL4 operation, and the unop proves the NET-ZERO shape (no rsp instruction at all).** WATERMARK: m3 314/1 · m4 312/1 · DIVERGE=0 — WATERMARK-EXACT after EVERY rung (sole fail `test_case`, pre-existing). SCRIP `044d2ccc`+`92e926cf`; corpus `2cf53f35`+`3df944f3`. Full narrative: `FINDING-2026-07-26d-CLAUDE-SN4-ZB-VAL-6AB-CONCAT-AND-NET-ZERO-UNOP-AND-SN4-RELOPS-ARE-NOT-IR-BINOP.md`. HEADLINES: (1) **CONCAT needs NO type ladder and NO omega edge** — `str_concat_d` is type-blind and owns both the to-string coercion and the null-string identity (manual p.22: either operand null ⇒ other returned UNCOERCED; oracle-verified `(20-17) ''` → `3`/INTEGER in SPITBOL, m3, m4 alike), so the whole box is one call plus the net `add rsp,16`. (2) **UNOP NETS ZERO** — release 16 + carve 16 cancel, so `n3_unop_α` emits no rsp instruction and writes over its operand in place; `TT_MNS`/`TT_PLS` ONLY (manual p.181: unary `*` is DEFER, not size). Unops share the `fvb` operator registry and are counted against ITS cap; depth simulation `d` UNCHANGED; a unop may be the tree ROOT. (3) ⭐ **β-DEAD INVARIANT CONFIRMED EMPIRICALLY** — `x86_beta_trampoline` = β-label + `jmp ω`, and that jmp site is EXACTLY where `op_wpop` is spent; a live trampoline would over-pop 16 per operand consumed. It never fires (no trampoline emitted on any spine statement, verified by absence). THIS is what makes uniform `wpop=d*16` safe — previously implicit, now load-bearing and written down. ⛔ **THE LADDER WAS WRONG ABOUT ZB-VAL-7: SNOBOL4 RELOPS ARE NOT `IR_BINOP`** — measured, `LT(A,B)` lowers to COERCE/CMP boxes on `[rbp+...]` (`op75`/`op77`), never a `BINOP_CAT_RELOP`; SNOBOL4 has NO infix relational operator (manual p.182), the predicates are FUNCTIONS that succeed/fail (p.32). ZB-VAL-7 is an ICON/RAKU-anchored rung and does NOT advance the roman anchor. ⚠ **WATERMARK DISCREPANCY FOR LON:** `test_crosscheck_snobol4.sh` prints m3 314/1 · m4 312/1 (= **s176's** number), NOT s178's claimed 332/2 · 323/1 — measured 3× this session, identical every time. Name the runner that produces 332/323 or treat that prose as unmeasured. **NEXT RUNG: ZB-VAL-8 COERCE/CMP** (the SNOBOL4-anchored continuation — `IR_COERCE_NUMERIC`/`IR_COERCE_REAL`/`IR_CMP_TEST` on the spine; this is the FIRST routinely-taken ω, so the wpop mechanism finally gets RUNTIME exercise instead of byte-verification, and it is where the SPD-2 `rsp=rbp` wall should first come into view) · or **ZB-VAL-7 relops** re-labelled Icon/Raku · then the DIET. LATENT: `OUTPUT = 19 (2. / 3.)` (manual p.21 verbatim) is a PARSE ERROR in SCRIP; `test/snobol4/**` feature tests have NO `.ref` files. `handoff_status.sh` is the push truth — not this block.

## ⛔ LIVE CURSOR — s178 (2026-07-26): **ZB-VAL-1..5 LANDED — the value spine reaches ARBITRARY ARITH TREES with TYPED GLOBAL-VAR leaves, all rsp cells, zero rbp dependency.** WATERMARK: m3 332/2 · m4 323/1 — WATERMARK-EXACT after EVERY rung (test_case, omega_driver pre-existing). SCRIP `9ed464c8`+`3098b016`+`d87db590`+`0c2c96d6`; corpus `bb5bd087`+`fd7108fa`. Full narrative: `FINDING-2026-07-26-CLAUDE-SN4-ZB-VAL-1-TO-5-VALUE-SPINE-TREES-AND-DEPTH-SIMULATED-OMEGA-POPS.md`. HEADLINES: (1) binop fc arm = ONE-INSTRUCTION net allocation (`add rsp,16` releases both operand cells + carves RESULT); (2) post-order geometry is SHAPE-INVARIANT (`[rsp+24]/[rsp+8]` identical bytes for lit/lit, lit/result, result/result — byte-verified); (3) v5 typed arm: DT_DATA→overload (r9=lea deeper cell, becomes result after top release), non-DT_I→generic, DT_I fast — cells LIVE until each exit's net; (4) fallible-box ω = FULL UNWIND via registrar depth SIMULATION feeding the existing BP-9 op_wpop synth (byte-verified: inner add pops 48, mul/sub 32 in `2*(X+3)-X`); (5) new R7 encoder arm `lea reg,[rsp+off]` (SIB pre-existing); (6) TWO ZB-VAL-0 LATENTS CLOSED (capacity-atomic registration; consumer gate mirrors bb_assign_global routing — local-assign consumers would strand carves). WALL SIGHTING (not hit): SPD-2 scanfail already does `rsp=rbp` frontier restore — the spine meets it when value cells coexist with scan retries; ω-merge-depth is the same wall from the failure side, fenced away by registration proving single-depth arrival. LATENT: rt_num_arith coerces non-numeric strings to 0 where SPITBOL raises ERROR 001 — PRE-EXISTING (flat + stashed HEAD probed identical), so the emitted DT_FAIL unwind is byte-verified but not runtime-exercisable from SNOBOL4 source; registered producers' flat slots unread (diet rung later). **NEXT RUNG (pick one): ZB-VAL-6a CONCAT** (bb_binop_concat_slot on the spine — rt-call-with-cells-live proven by v5) · **ZB-VAL-6b UNOP** (net-ZERO: release 16 + carve 16 = rsp untouched, negate in place) · **ZB-VAL-7 RELOPS** (IR_BINOP_TEST, ω at depth). `handoff_status.sh` is the push truth — not this block.

## ⛔ LIVE CURSOR — s177 (2026-07-26): **ZB-VAL-0 + CF-GATE LANDED** — first value-spine FORTH cell + constant-fold gate. WATERMARK: m3 332/2 · m4 323/1 (test_case pre-existing; omega_driver pre-existing, fold-gate-neutral). **NEXT RUNG: ZB-VAL-1** — extend fc_geom grant to IR_LIT_INTEGER nodes feeding IR_BINOP (one-hop) so that `OUTPUT = 1 + 1` gets two rsp cells and the binop reads [rsp+0/8] + [rsp+16/24], releasing both before its own gamma. Fence: fc-linear spine only; any Tier-D box between lit and consumer → decline. Then ZB-VAL-2: IR_LIT_STRING same shape. Then ZB-VAL-3: IR_VAR (global-read). Statement-at-a-time, roman anchored. `handoff_status.sh` is the push truth — not this block.

## ⛔ LIVE CURSOR — s176 (2026-07-26): **RUNG SN4-FLAT-PROC LANDED — WATERMARK IMPROVED: m3 314/1 · m4 312/1 · DIVERGE=0** (the 21{4,5,6}_indirect_goto* trio + 213_indirect_name FIXED as a side effect of enterable-set emission). SNOBOL4 scoped at ONE STATEMENT: body extraction DELETED (`sno_reach_body`/`sno_lbl_index`/`SCRIP_SN4_STUB` gate gone); every entry-label DEFINE is an ENTRY-STUB (jmp-entry prologue → **IR_SAVE_RESTORE role 3 WIRE-ADOPT** → IR_GOTO_DEFERRED, entry resolved AT CALL TIME via rt_goto_transfer/LBL__). **NEW IR KIND `IR_SAVE_RESTORE`** (roles in ival: 1 RETURN floater, 2 FRETURN floater, 3 wire-adopt, 0 emitted-site OPEN = slice-2 reserved, template bombs) + `src/templates/bb_save_restore.cpp` + `jmp <reg>` encoder arm (x86_asm.h, FF /4 both mediums). **FLOATERS**: RETURN/FRETURN/NRETURN in every result_name==NULL graph are program-wide floater BBs (registry-reachable from ANY goto incl `$X`); they call `rt_flat_ret_snap` which **PEEKS (never pops)** the open activation's wire quad {γ-wire,ω-wire,entry-rsp,caller-rbp}, restore machine state, jmp the port wire — the pop/name-restore/result protocol lands UNCHANGED on the existing `c_rt_proc_call_epilogue_γ/ω` leaves (double-pop structurally impossible). Wires ride a **PARALLEL `g_pcall_wires` array** (rt_pcall_t stays 64B — `rtx_call.S` bakes the stride), zeroed at all 3 push sites, filled by `rt_flat_wire_adopt`. Stub carves a fresh **MAIN-layout frame** (`g_flat_frame_floor` = zls_g_region(main), eager-zero, all four driver loops) → recursion nests correctly (FACT 12! oracle-exact); per-statement ζ carve/release is the named DIET rung ahead. def-body DEFINEs keep the old self-contained protocol (correct, unconverged). Level-0 `:(RETURN)` = `core_runtime_error(18, "Return from level zero")` — trappable, per s175 ruling. LBL__ minting extended to DEFINE entries (non-CODE programs). **THREE DEFECTS FOUND+FIXED EN ROUTE**: (1) **ZLS LABEL-SPAN ROOTS** (`zeta_storage.c`) — the optimizer's GOTO-fold orphans anchors with no static in-edge (= flat bodies), so s174's wiring-root repair found nothing and body spans went ungranted (`drive_value_slot` FATAL on IR_COERCE_STRING); fix = root every labeled span via the existing group marks. (2) **ENTERABLE-SET EMISSION** — mode 4 never emitted label-only-reachable chains (old regime's main copies were dead code; extraction did the work); fix = **anchor-carrying group marks** (`zls_group_mark_anchor`, pointer-captured pre-fold, orphan-proof) seeded as chain-BFS roots in `codegen_flat_chain_body` — mode 4 now emits the same enterable set mode 3 pulls lazily; THIS is what fixed the indirect-goto DIVERGE trio. (3) **α-BINDING BUG `scrip.c:883`** — the m4 text loop passed `->entry` not `bb_proc_entry(...)`, binding `proc_LBL__<x>_α` at MAIN'S FIRST STATEMENT; the stub's transfer re-ran the whole program inside the call → recursion SEGV (m4 284/28 mid-session). One-line fix to the canonical fallback (loop 1513's shape). **PROOFS**: full crosscheck **m3 314/1 · m4 312/1 · DIVERGE=0** (sole fail both modes: test_case, pre-existing) · prolog 189/0 · icon differential BYTE-IDENTICAL to stashed HEAD (queens/rsg/tgrlink EMITFAIL pre-existing) · manual-derived probes oracle-exact (SHIFT p.105, FACT p.108 incl 479001600, level-0 error) · three util_regen scripts run+committed. **LATENT (noted, not chased)**: NRETURN-through-table `$fname = X` store loses the value (PRE-EXISTING — old HEAD printed "1", flat prints the by-name-assign line correctly; strictly closer); fall-into-END-during-a-call semantics untested (should exit program — suspect current shape returns like RETURN); m4 LBL__ blobs duplicate the enterable text (diet rung); loops 1328/1616 pass bare `proc_entry_node` (NULL⇒emit_chain no-op — audit which arms those serve); beauty gate script needs prebuilt binary absent from container. **NEXT RUNG (pick one): SN4-FLAT-CALLSITE** — slice 2, the emitted IR_SAVE_RESTORE role-0 OPEN (stage args → rt_proc_call_open → wires via rt_flat_wire_adopt with the site's own landings) + flat IR_CALL transfer, replacing the C-path rt_call_arr round-trip for statically-known DEFINE names — **or SN4-STMT-ZETA** — per-statement ζ (zls_build per stmt, carve/release at inter-statement edges) shrinking the main-sized activation frames. WATERMARK: m3 314/1 · m4 312/1 · DIVERGE=0. **`handoff_status.sh` is the push truth — not this block.**

## ⛔ LIVE CURSOR — s175 (2026-07-26): **RUNG SN4-PARSE-ROMAN LANDED** (Lon pivot: critique-driven session anchored at roman.sno/roman.s; parser prerequisite for the DEFINE-scoping rework). **FOUR PARSER DEFECTS FIXED** (`src/parser/snobol4/snobol4.l`, `snobol4.y`, regenerated `.lex.c`/`.tab.c`, `src/driver/scrip.c`): (1) **LABEL-ONLY LINE** was a parse error — `<LABEL_DONE>[ \t]` CONSUMED the next line's leading blank instead of `yyless(0)` like its sibling arms, so the next line's first ident mislexed as a label (manual p.28: a line of just a label is legal; sbl oracle accepts). (2) **BLANK BETWEEN GOTO PARTS** (`:S(RETURN) F(FRETURN)`) was a parse error — `<GT>[ \t]+` unconditionally returned T_CONCAT; now `gt_depth` paren counter: depth-0 skip, depth>0 concat (computed-goto `:($('A' N))` proven still oracle-identical; provably regression-free — any depth-0 blank was ALREADY a syntax error). (3) **ERROR LINE WAS ALWAYS 0** — `g_lx->lineno` never written; now per-token stash in the yylex wrapper. (4) **PARSE ERRORS WERE SILENT** — truncated program compiled rc=0 into a broken binary (roman shipped a 595-line .s covering 3 of 13 statements, died at runtime on `transfer to undefined label: ROMAN_END`); driver now checks `sno_nerrors` post-parse → loud message, rc=1, ZERO emission. ⭐ **PROVENANCE FINDING: the .l had DRIFTED from its committed artifact** — flex 2.6.4 (same version that made the committed .lex.c; bison byte-identical) could not regenerate it: 48 column-0 separators added to the RULES section post-generation (flex reads column-0 as a rule pattern → "unrecognized rule" ×hundreds) + a stale `sno_parse()` in the .l user-code section calling phantom `parse_program_tokens` (NO definition anywhere in the tree; the committed .lex.c never contained the fn — link breaks on regen). Fixed: separators indented one space (line numbers preserved), dead fn deleted; **.l is flex-regenerable in-session again**. **PROOFS**: sno smoke 7/7×2 · icon 14/14 · prolog 5/5 · all-langs hello 6/6 ROWS_DRIFT=0 · full crosscheck **WATERMARK-EXACT m3 314/1 · m4 309/4 · DIVERGE=3** identical named sets — run TWICE (post-lexer-fix and post-fatality). **roman.sno: 0 parse errors, 595→1763-line .s, 10 stmt blocks, BOTH modes byte-exact vs SPITBOL ref** (recursion+patterns+REPLACE). Demo blast radius: 1 of 22 `.s` change (roman only). **LATENT (noted, not chased)**: GT can't lex double-quoted strings or bare digits (silent `<GT>.` eater); dead `<LABEL>.` rule (flex warns); `test_lex.c`/`scrip_cc.h` still reference deleted `sno_parse`; `+`-continuation after a label-only line; ARCH-SNOBOL4.md §Parser describes CMPILE.c which no longer exists (doc-rot). **NEXT RUNG: SN4-FLAT-PROC** (Lon directive, ratified s175): cease proc scoping — highest scope ONE STATEMENT; DEFINE constant-folds (already: hoist-register + rt_proc_register); entry label = FLAT BB, NO prologue; CALLER does the RSP/RBP dance then jmp; RETURN/FRETURN/NRETURN = program-wide FLOATER BBs reached as ordinary reserved labels (any goto, incl. `$X`, reaches them for free); level-0 RETURN = runtime error; computed-prototype DEFINE stays loud-fatal; pattern blob-freeze default-on rides after. **IMPLEMENTATION MAP (read s175)**: BFS body extraction = `lower_snobol4.c:2159` (delete); `:2304` comment already states SPITBOL resolves entry AT CALL TIME → resolve entry via the LABEL REGISTRY (same table as rt_goto_transfer), no proc_<N>_α symbol needed; shared flat emitter = `emit.cpp:1773 codegen_flat_chain_body` (prefix-parameterized main+procs); entry framing = `xa_flat.cpp:186 xa_flat_prologue_str` — NOTE the **jmp-entry regime already half-implements the target**: caller already jmps with γ/ω wires in rcx/rdx at rsp≡0 mod 16; the header saves wires at [rsp+kt-24/-16/-8] + rbp seed; what moves caller-side = the carve (`sub rsp,kt`) + seed/zero flavors (LEXPREP2/NOFILL/poison/cap-grants) + wire saves; frame size must ride the proc record for late-bound calls (`sub rsp,[rec+size]`). Per-statement ζ granularity = zls_build per stmt + carve/release at every inter-statement edge (recursion then nests for free — this is what makes flat bodies recursion-safe). BOTH-MEDIUM + TEMPLATE-ONLY rules bind every moved instruction; monitor-first on any divergence. WATERMARK: m3 314/1 · m4 309/4 · DIVERGE=3. **`handoff_status.sh` is the push truth — not this block.**

## ⛔ LIVE CURSOR — s174 (2026-07-26): **RUNG SN4-FRAME-DIET LANDED** — ZLS slot elimination: entry-rooted reachability walk, slot0 need-predicate, default-arm phantom deleted, omega slot0 FAILDESCR stores deleted. **Changes**: `src/contracts/zeta_storage.c`, `src/emitter/emit.cpp`, `src/templates/xa_flat.cpp`. **(1) REACHABILITY**: `zls_build` now BFS-walks from `g->entry` over γ/ω/operand edges; only reachable nodes receive slot grants. Unreachable clusters (the synthetic NRETURN landing — `IR_LIT_STRING "" → IR_CALL SNO$NRET` — built unconditionally by `lower_snobol4.c:1816` for every graph lacking a user `NRETURN` label) held 5 ticks = 80B dead per every SNOBOL4 graph. Wiring-node roots added so label-chain producers are always granted. **CARVE-OUT**: graphs containing `IR_GOTO_DEFERRED`, `IR_INDIRECT_GOTO`, or a `CODE`-named call get **full grants** (all nodes reachable at runtime via label dispatch; static majority gets the diet). **(2) SLOT0**: `base=16` only when `nparams>0 || resumable_callable || any reachable IR_RETURN/IR_SUSPEND` present. The gen-delivery protocol (`xa_flat` γ reads `[rbp+0..15]`) and the rt.c lex epilogue (`[fb+0]`) are the live readers; outer mains with none of those carried a 16B cell nothing reads → `base=0`. **(3) PHANTOM**: `zls_grant_locals` default arm previously returned `1` for any `ir_node_produces_value` kind, burning a second field-free slot tick. DELETED after auditing every `[op_off+16]` reader across all templates: each belongs to an explicit arm above the default (`bb_scan_*`, `bb_match_head/arbno/release/replace`, `bb_disjunction`, `bb_call_value`, `bb_create`, `bb_limit`, `bb_repalt`, `bb_rev_swap`, `bb_to*`, `bb_move_label`, `bb_indirect_goto`, `bb_keyword_icon`, `bb_gen_scan`, `bb_make_list`). **(4) OMEGA STORES**: RSP call-entry ω arm's three `[rsp+0/4/8]` FAILDESCR memory stores deleted (BINARY: `C7 04 24` + `C7 44 24 04` + `48 C7 44 24 08`; TEXT: `mov dword ptr [rsp+0], 99` trio); `eax=99/edx=0` register pair retained as the fail signal. Gen/resumable and non-RSP arms untouched (their slot0 protocol is live). **(5) CAP GUARD**: `emit.cpp` IR_MATCH_ASSIGN_SAVE cap-offset guard `off < 16` → `off < 0` for base-0 frames. **RESULT**: hello frame `sub rsp,152` → `sub rsp,40`; ZLS region 144B → 32B; slots=2 (ASSIGN@+0, LIT@+16). ASSIGN result kept as directed (future elision). **REGRESSION FOUND AND FIXED MID-SESSION**: first reachability walk under-marked label chains in indirect-goto graphs (214/215/216/1020/1021); fix = full-grant predicate for dynamic-transfer graphs + wiring-node root widening; `drive_value_slot` FATAL fired cleanly per TMP-ERADICATE design. **PROOFS**: sno smoke 7/7×2 · icon 14/14×2 · prolog 5/5×3 · raku 681/681×2 · full crosscheck **WATERMARK-EXACT m3 314/1 · m4 309/4 · DIVERGE=3**, identical named fail sets (21{4,5,6}_indirect_goto*). All three util_regen scripts run; 3+16+N demo/bench/feature artifacts committed to corpus+SCRIP. **DEFERRED**: ASSIGN-result elision (R4, Lon ruling "not for now"); SN4-BYNAMEFN-DEDUP still queued; off*j argv-registration typo (dump-only lie); R5 call-entry NOFILL (rep stosb still fills 40B); n0→n1 store/reload round-trip. **NEXT RUNG: SN4-BYNAMEFN-DEDUP** — `bb_call.cpp:290` mints `.Lbynamefn<nid>` from per-chain nid; file-global `.L` namespace → 267 collisions in beauty; sibling sites: `bb_call.cpp:344` (`.Lbynamegenfn`) + `bb_call_proc_staged.cpp:475` (`.Lcall<nid>_pname`). Fix: mint from `g_flat_node_id` (already the uid standard). Completion: beauty `--compile` + `as`-accept + watermark unchanged. WATERMARK: m3 314/1 · m4 309/4 · DIVERGE=3. **`handoff_status.sh` is the push truth — not this block.**
