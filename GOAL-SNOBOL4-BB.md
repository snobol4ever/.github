## ⛔⭐ LIVE CURSOR — s21x (2026-07-29, FB-STMT session): **FB-STMT LANDED OPT-IN (SCRIP `81345044`) — per-node frame-base refinement, Lon directive "Change every RBP to RSP that can be. I want only the housekeeping data indexed by RBP." Default OFF after two same-session falsifications.**

**WATERMARK (re-proven at start AND close, default arm): m3 311/4 · m4 311/2 · DIVERGE=2** (140/141 eval, 160 alt-inner-gen-resume, test_case m3-only) — byte-identical to session-start baseline. Handoff step-4 `.s` sweeps run = no-ops by construction (default-off ⇒ compiler output byte-identical).

**MECHANISM:** `emit_fb_stmt_scan` (eligibility: pin sourced ONLY from statement-bracketed kinds DEFER/ARBNO/FENCE1/VALUE; any suspend/generator class, ABORT or CALLOUT disqualifies) + per-node bit map built at the `emit_chain` choke point; `x86_fb_data()` consulted ONLY by the five data accessors + FRQB (`x86_asm.h` ~363-367, ~873). Ceremony, record protocol and epilogues read `emit_jmp_pin_rbp`/`emit_rec_pin` UNTOUCHED — data spelling only; mixed spellings address identical bytes (rbp==base; static reader rsp==base−live, `x86_frame_off` compensates). Arm with `SCRIP_FB_STMT=1`; env-gated diagnostics under `SCRIP_FB_DEBUG=1`.

**MEASURED UNDER THE FLAG:** pinned-benchmark `[rbp+N]` value refs **1015 → 220**; survivors = match-family housekeeping ONLY (head Σ/δ/Δ/capgen/mark/bracket, release/replace, break/alternate/span/defer quads). call/binop/op75/op77/var/lit/assign/subscript/deref = **ZERO**. All four pinned benchmarks (mixed_workload/roman/pattern_bt/string_pattern — all pin via stored-pattern DEFER) output-correct; w1 witness oracle-exact BOTH modes vs `sbl`; census-gate unseeded=0 held.

**🔴 FALSIFIED PREMISE (why default OFF — do not flip without the license):** "statement exits rebalance rsp" (the 058 law) is FALSE on two measured paths: (a) **ARBNO hit-path element retention** — interior carves survive γ by design (t1.s; the free-on-final-success rung banked in `3ea60fc2`'s FINDING is the same fact) — `180_pat_arbno_defer_nonrecursive` SEGVs on the statement AFTER a successful arbno; (b) **DEFER/VALUE-alt backtrack re-pump** of a retained blob — `072_pat_star_var_alt_backtrack`. Default-on regressed 7 crosscheck programs (also 143/164/165/181/183); reverted to opt-in same session.

**RE-ARM LICENSE:** monitor-first on 180 then 072; either extend the bit map downstream of any retaining statement until its `release_to` provably sweeps, or land **free-on-final-success** first (restores the rebalance law outright) — then flip the default ON and this conversion completes.

**ALSO NAMED THIS SESSION:** `g_resumable_callable_active` LEAKS from PAT$ blob emission into outer mains (refreshed only at the jmp-entry arm, emit.cpp ~1821; measured on w1 — mixed_workload escaped only because RSUM's proc emission reset it). FB-STMT's guard reads the graph-own `resumable_callable` field as the workaround; the stale-emission-global class deserves its own sweep rung.

**NEXT RUNGS IN ORDER:** (a) **180 monitor dig → re-arm FB-STMT default** (the license above). (b) **FLATDISP-5a frameless invariant PAT$ blobs** — the structural companion; flat_pat/flat_gen/gen-proc graphs keep whole-graph rbp until it lands. (c) stale-emission-global sweep.

## ⛔⭐ LIVE CURSOR — s206 (2026-07-28): **RUNG ZHEAP REMAINS #1 AND IS NOW BLOCKED ON ONE LON RULING (ζ_self register, jointly with VSP). Port 7 was NOT ACTUALLY SELECTABLE — two silent defects fixed, SCRIP `cca948c5`. The "keep locals flat" proving configuration is FALSIFIED: it does not exist.**

**NEXT RUNG = (a) LON RULING on §4 of the finding.** Watermark NOT re-proven this session (no emitter edit survived; default-port `.s` byte-identity verified instead — 256 lines, non-empty guard honored). No `.s` regen: `cca948c5` touches only `src/driver/scrip.c` + `src/runtime/rt/zeta_alloc.c`, so handoff step 4 is not triggered.

**✅ LANDED — PORT 7 WAS UNREACHABLE BY EVERY PATH EXCEPT ONE ENV VAR.** (1) `rt_zeta_port_set_mode()` clamped `m <= ZC_PORT_FORTH` (6); the bound was never raised when `ZC_PORT_HEAP` (7) landed, so **7 was SILENTLY reset to the compiled default**. The getter's unclamped `atoi` is why `SCRIP_ZETA_PORT=7` worked and every setter call did not. ⭐ **THE CONSEQUENCE THAT MATTERS FOR ZHEAP-8: `scrip.c:1216/1407` BAKE `call rt_zeta_port_set_mode` into MODE-4 OUTPUT, so a mode-4 binary emitted WITH heap carves ran with port mode FORTH — emitted code and runtime mode disagreeing, the exact storage-vs-addressing class this rung exists to cure. Any m3-vs-m4 A/B on port 7 before `cca948c5` was INVALID.** (2) `--zeta-port=heap` did not exist as a name. Both fixed; env == CLI byte-identical (287 lines, 4 carves); the CLI path previously **hid all three SEGVs** and now reproduces them.

**🔍 ROOT CAUSE OF THE SEGV — PORT-BLIND GRANT vs PORT-GATED CONSUMPTION (the real defect; the SEGV is only its symptom).** gdb: `rt_cap_push`, `pattern_match.c:755`, faulting on `rax=3` — an **uninitialized capture slot**. `fc_geom()` grants a 16B cell on EVERY port; every `x86_fc_*` consumer was FORTH-ONLY. So under HEAP one box gets contradictory answers: geometry says "granted" (⇒ carve fires, `hk` sized), `x86_fc_on`/`x86_fc_hit` say "not granted" (⇒ no `sub rsp`, locals spelled FLAT), `fc_save_active` says "granted" (⇒ SAVE takes the `rt_cap_push/pop/top` C path), and the port-blind flat prefix sum counts a carve that never happened. ⭐ **AND THE BLOCK BASE IS DROPPED ON THE FLOOR:** the carve emits `mov rax,rbx` and the NEXT instruction overwrites `rax`. Port 7 allocates per box and never addresses the block — "allocation-only" is one unparked register, nothing deeper.

**🔴 TRIED AND REVERTED WITH MEASUREMENTS — DO NOT RETRY AS A PREDICATE FLIP.** Experiment: one predicate `x86_fc_cells() = (FORTH||HEAP)` feeding `x86_fc_on` + `x86_fc_hit` — cells on rsp exactly as FORTH PLUS the carve, i.e. the "PROVING configuration" `ZC_PORT_HEAP`'s own header claims. **RESULT: 3 OK → 0 OK**, all six tripwire programs SEGV. gdb: **`#0 0x0000000000000000` — WILD CONTROL TRANSFER, not a data fault** = rsp imbalance, α push without its ω pop. **LESSON: the FORTH cell is a COUPLED PROTOCOL, not two instructions** — the pop lives at the `X86H_JMP`/`OMEGA` hook and reaches conditional-ω only via the `x86_fc_jcc_omega` invert+pop+jmp synth (gated on `x86_fc_on()`), with chained cells folded through `op_wpop`. Reverted clean; tripwire restored. ⇒ **THERE ARE EXACTLY TWO SELF-CONSISTENT CONFIGURATIONS, NOT THREE: FORTH (cells on rsp) and TRUE HEAP RESIDENCE (FR retargeted). The intermediate the port header describes DOES NOT EXIST.**

**⛔ THE BLOCKER, STRUCTURAL NOT PREFERENTIAL — `A-1`'s "addressing form unchanged" IS NOT FREE.** ζ_self must survive α→β→γ across arbitrary boxes and C calls ⇒ no caller-saved register works; all callee-saved are spoken for (RBX frontier · RBP control base · R12 capture · R13/R14/R15 Σ/δ/Δ). **A parked handle cannot rescue it: FR/FRQ return an OPERAND STRING and x86 has NO `[[rsp+k]+off]` addressing mode**, so a memory-resident ζ_self cannot be reached by any change confined to operand spelling. TWO SHAPES: **(A) dedicate a register** (release Σ/R13 — *the same register B-0 names for VSP*) · **(B) reload at each port define** into a scratch, templates stay mode-blind, but C calls inside a box invalidate it and the falsification above is the warning about ω-side routing. ⭐ **RULE ON ζ_self AND VSP TOGETHER — one question, two regions, at most one free register; deciding separately risks spending it on whichever is implemented first.**

**📖 CORRECTION TO s205 — THE `@` GATE'S EXPECTED OUTPUT MUST COME FROM `sbl`, NOT THE MANUAL.** s205 proposed the manual's `'FIX' ? @OUTPUT 'B'` as a cheaper gate. Good gate, **wrong transcript**: manual p.65–66 prints `0 1 2` + Failure (three); the real oracle prints `0 1 2 3` + Failure (**four**), and SCRIP matches the oracle exactly in BOTH ports. Baking the manual's text into a `.ref` = permanently-red gate against a correct compiler. The manual's PROSE (*"Cursor assignment is performed whenever the pattern match encounters the operator, including retries … even if the pattern ultimately fails"*, verified verbatim) is sound and remains ZHEAP-7's premise; the TRANSCRIPT is not. **Also re-confirmed at HEAD: `$` capture-START still broken** (`ARB $ OUTPUT` gives `A/AB/ABC/ABCD` vs oracle `⟨null⟩/B/BC/BCD`), so `@` is the correct witness.

**TRIPWIRE (rebuilt independently, at HEAD, `-O0`) — s205's split refined:** `t1_nomatch`/`t2_lit`/`t3_at` OK both ports · `t4_len`/`t5_dollar`/`t6_arbno` OK FORTH, **SEGV HEAP**. Note t1/t2 DO carve (`fc_vlit_active` grants literals a 16B cell), so the split is NOT "carves ζ" as s205 wrote — it is "does a carving box also consult the grant a SECOND time" (SAVE's path selection, the ω pop). Programs in `/home/claude/w/t[1-6]_*.sno`.

**NEXT RUNGS IN ORDER:** (a) ⛔ **LON RULING — ζ_self register jointly with VSP.** (b) FR/FRQ heap retarget after the ruling — ONE predicate, but census the ω routing (`x86_fc_jcc_omega`, `op_wpop`, `fc_save_active`) FIRST this time, not after. (c) **`fc_geom` port-awareness** — the port-blind grant vs port-gated consumption must become ONE decision; that is the actual defect. (d) ZHEAP-6 mmap value stack — unblocked independently of (a) only while VSP stays a global cell. (e) `$` capture-START / MARKER-CAPTURE. → `FINDING-2026-07-28-CLAUDE-SN4-ZHEAP-PORT-CLAMP-AND-FC-GRANT-DISAGREEMENT.md`

## ⛔⭐ LIVE CURSOR — s205 (2026-07-28, LON PIVOT DIRECTIVE): **THIS RUNG IS #1 — TOP AND FIRST. ζ LOCALS → GC HEAP · ζ RESULTS → MMAP STACK.** Everything else in this file, and the CELL ladder in `GOAL-SN4-CELL-MACHINE.md`, is BEHIND it. Nothing coded yet on this rung; the block below is the plan of record.

**THE RUNG, in Lon's two clauses:** (1) **ζ LOCALS MOVE TO THE GC HEAP** — the RBX bump frontier, `add rbx,K` replacing `sub rsp,K`, one instruction, same box-local constant, same self-relative `[ζ_self+k]` addressing, reclaimed by MARK/ADJUST/SLIDE. (2) **ζ RESULTS MOVE TO AN MMAP VALUE STACK** — its own mapped region, separate from ζ and from the control stack, 16B uniform DESCRs, scannable as a DESCR array. What is LEFT on RSP afterward is exactly the FORTH return stack: return addresses, call frames, C interop. That is the point — see the falsification restated below.

**PROPOSED EXECUTION ORDER (one deviation from the as-written numbering, flagged for Lon):** `ZHEAP-0` (parameterize the funnel — the kill-switch that makes every later step A/B-able) → `ZHEAP-2` (inline the carve; this is what makes RBX pay) → `ZHEAP-3` (structure map — verify, do not design) → `ZHEAP-4` (ζ threading) → `ZHEAP-6` (the mmap value stack) → `ZHEAP-7` (give-back = chain head only) → `ZHEAP-5` (GC/ADJUST, incl. the interior-pointer decision) → `ZHEAP-8` (gate) → **`ZHEAP-1` LAST, NOT SECOND.** ⚠ **DEVIATION AND WHY:** `ZHEAP-1` deletes `zeta_heap.c`, which holds the ONLY implementation of SLIDE we have (`memmove` + `g_zh_slid_bytes` + pin, relocation by handle-table indirection). Its own text says HARVEST BEFORE CUTTING. Deleting it at step 2 destroys the fallback while the replacement is unproven; running it after `ZHEAP-8` costs nothing and keeps a working comparator alive through the ladder. **Lon: veto this reorder if the intent was to cut early.**

**⚠ TWO CONTRADICTIONS IN THE AS-WRITTEN LADDER, RESOLVED HERE (both look blocking and are not):**
- **`RUN B` B-0 says "RULING NEEDED — which register is VSP"; `ZHEAP-6` says VSP starts as a KNOWN GLOBAL CELL, not a register.** `ZHEAP-6` is the later and correct form, and it carries its own reason: GC during a C call must be able to FIND the value-stack extent to scan it, which a pushed register does not provide. **NO REGISTER RULING IS NEEDED TO START THIS RUNG.** Access goes behind `x86("vsp_carve")` / `x86("vsp_operand")` from the first line of code, so the later promotion to a register is ONE encoder edit with a kill-switch and no template is written twice. B-0's Σ-release candidate is DEFERRED to that promotion, not a prerequisite. (Register pressure is real — RBX heap · RBP control-frame base · R12 capture · R13/R14/R15 Σ/δ/Δ all spoken for — which is exactly why not deciding now is cheap.)
- **`RUN A` A-4 leaves heap-end OPEN (guard page vs explicit check); `ZHEAP-2` already answers it: EXPLICIT FRONTIER COMPARE, NO GUARD PAGE** — a page fault cannot keep the virgin/zeroing logic honest. A-4 is CLOSED by ZHEAP-2; do not re-open it.

**⚠ COLLISION WITH THE CELL LADDER — REAL, NOT HYPOTHETICAL (CELL-0 + CELL-1a are ALREADY LANDED in SCRIP `63ee4164`/`04defc9f`):** the cell machine's own LAW reads *"carve 16 THEN push header; at extend entry locals are `[rsp+0..16)`"* (CELL-4) — i.e. it puts ζ LOCALS on RSP, which is what clause (1) moves off RSP. The ladders are NOT independent: (a) CHOICE CELLS are CONTROL and legitimately stay on RSP — no conflict there; (b) but every cell rung that CARVES LOCALS must be re-read against `ZHEAP-0`'s mode switch before it is written, or it lands legacy-shaped and is rewritten; (c) the s203 SEQ/ALT PREFIX-ALLOCATED 16B RESULT SLOTS **are** the ζ RESULTS of clause (2) — same objects, and this rung says they live in the mmap region, not on RSP. **Reconcile before either ladder emits another carve.** Per-graph all-or-nothing holds on both sides; mixed regimes in one blob are the s188 disease.

**📖 SPITBOL v3.7 MANUAL VERIFICATION (s205, read from the PDF, not from prior prose — every construct this rung directly touches):**
- **`$` immediate assignment — p.87 CONFIRMED VERBATIM.** *"Immediate assignment occurs whenever a subpattern matches, even if the entire pattern match ultimately fails."* **ZHEAP-7's load-bearing premise HOLDS**; a bump-restore give-back would reclaim memory reachable from a global. The s202 "free undo / give-back stays exact" downgrade was correct.
- **`@` cursor assignment — p.65–66 CONFIRMED, and the CITATION IN THE s204 BLOCK IS WRONG:** it reads "manual p.87 … `@` likewise", but `@` is p.65–66, not p.87. Text: *"Cursor assignment is performed whenever the pattern match encounters the operator, including retries. It occurs even if the pattern ultimately fails."* ⭐ **AND THE MANUAL HANDS US A BETTER GATE THAN ZHEAP-8's:** its own worked example `'FIX' ? @OUTPUT 'B'` prints `0` `1` `2` then **Failure** — a ONE-LINE, oracle-checkable program whose visible output IS the write-through-survives-failure property. Run it under `sbl -b` and under both `SCRIP_ZMODE` values BEFORE the ZHEAP-8 program is written; it isolates the one property ZHEAP-7 can silently break, at a fraction of the cost.
- **Magnitude discrimination — p.53 "Caution" CONFIRMED; s204's reading is accurate and the REJECTION STANDS.** *"The collection algorithm needs to distinguish between small integers and memory addresses. This effectively restricts the maximum size of a SPITBOL object … to be less than &MAXLNGTH bytes"*, plus the companion rule that the max object size *"must be numerically less than the starting memory address of SPITBOL's work space."* Both artifacts fall out of a one-word descriptor with no tag room. `DESCR_t` is 16B with a 4B `DTYPE_t`; we adopt neither the cap nor the unusable-low-memory rule.
- **🔴 NEW — THE RUNG CHANGES THE FAILURE MODE OF THE RECURSIVE PLUNGE. NOT RECORDED ANYWHERE IN THE LADDER; DECISION OPEN.** p.122–123: *"SPITBOL saves information on a stack during the pattern match process. Heavily recursive patterns and long subject strings can sometimes result in stack overflow … increase the size of SPITBOL's stack by using the –s command line parameter."* p.264: **error 246 "Stack overflow"**, *"usually … an error in a recursive function or pattern, with a resulting 'stack plunge'"*, **default stack 32KB, grown by `–sn`** (Ch.13 option table). **CONSEQUENCE:** today a plunge (`EXPRESSION = *EXPRESSION | "(" TERM ")"`, the manual's own example) exhausts the CONTROL stack — precisely the documented contract s197 cited when it reverted the DEFER-removal. Under this rung ζ locals are on the RBX heap and (under cells) failure delivery is pop+jmp not `ret`, so **the plunge consumes HEAP and may never touch RSP at all.** Three things follow, none free: **(a) WHICH CONDITION RAISES 246 MUST BE RE-DECIDED** — the natural site is `ZHEAP-2`'s frontier compare, which makes that compare an ERROR site, not merely an allocation slow path. **(b) `–s` MUST RE-TARGET.** It is a documented, oracle-visible knob with a documented default; if it keeps sizing the control stack while the plunge consumes heap, the manual's own stated remedy silently stops working. **(c) ⚠ THE WORST CASE IS NOT AN ERROR, IT IS A LIVELOCK.** `ZHEAP-7` says the frontier moves ONE WAY and the ζ chain is live by construction — so in a plunge NOTHING is reclaimable, and a collector invoked at the frontier compare will mark an ever-growing live chain, recover nothing, and be re-entered at once. That degrades a prompt bounded error into unbounded GC thrash. **The plunge must hit a bounded check that reports 246, reached BEFORE the collector is asked to save a hopeless situation.** Gate witness: the manual's own `EXPRESSION = *EXPRESSION | "(" TERM ")"` — SPITBOL errors immediately; SCRIP must too, in both `SCRIP_ZMODE` values.

**🔍 SOURCE RECON (s205, read from live SCRIP source, NOT from prose — s196's lesson applied). THE RUNG IS PARTLY BUILT AND DORMANT. SCOPE CHANGES; DO NOT WRITE `ZHEAP-0`/`ZHEAP-2` AS NET-NEW.**
- **THE FUNNEL IS ALREADY PARAMETERIZED — `ZHEAP-0` IS ESSENTIALLY DONE.** `src/contracts/zeta_choices.h` defines an EIGHT-flavor ζ port axis `ZC_PORT_{PLAIN,INSTRUMENTED,ALLOC,INLINE,CSTACK,OWNED,FORTH,HEAP}` = 0..7, selected by `SCRIP_ZETA_PORT=n` / `--zeta-port n`, read in the encoder as `x86_port_mode()` — switched INVISIBLY INSIDE `x86()`, which is exactly the discipline `ZHEAP-0` asks for. **Compiled default is `ZC_PORT_FORTH` (6).** ⚠ **ADDING A SECOND ORTHOGONAL `SCRIP_ZMODE={heap,stack}` AXIS WOULD CREATE A 2×8 REGIME MATRIX** — the exact combinatorial regime space the NO-INTERLEAVE law exists to prevent. **RECOMMEND: `ZHEAP-0` becomes "select and default flavor 7", not "add a switch."**
- **THE α-CARVE OF `ZHEAP-2` ALREADY EXISTS, WRITTEN AND BOTH-MEDIUM — `src/templates/x86_asm.h:1836–1848`,** gated `site==X86H_DEF && port==X86P_ALPHA && hk>0 && x86_port_mode()==ZC_PORT_HEAP`: `mov rax,rbx` · `add rbx,hk` · `cmp rbx,ABSQ(RT_WS_LIMIT)` · `ja`→aligned `call rt_zh_bump_slow` · reload `mov rbx,ABSQ(RT_WS_TOP)`. That IS `add rbx,K` + frontier compare + tail-to-C. The outer seed `mov rbx,[RT_WS_TOP]` is already emitted too (`xa_flat.cpp:398`, REG-4b). **So "GENERALIZE `sink_carve48`, do not invent" is superseded by something better: the carve is not merely generalizable, it is WRITTEN.** (Delta worth noting: it is coded as `ja L(60); jmp L(61)` — a jump-over, NOT the predicted-not-taken fall-through the rung specifies. Cosmetic, but the rung's wording and the code disagree.)
- **⭐ WHAT IS ACTUALLY MISSING — and the port's OWN comment says so honestly:** *"SLICE A IS THE ALLOCATION HALF ONLY: the box receives its ζ base in rax, but FR/FRQ frame plumbing under heap residence (`x86_zr` for escapee classes) is HZ-1's census slice."* So the **ALLOCATION half of clause (1) is built; the ADDRESSING half is not** — which is precisely `A-1`'s "addressing form unchanged", the part the rung assumed was free. **That, not the carve, is the first real work.**
- **🔴 CONTRADICTION — THE EXISTING HEAP PORT ASSUMES A REGION THAT IS NEVER COLLECTED; THIS RUNG REQUIRES ONE THAT SLIDES.** `ZC_PORT_HEAP`'s spec: *"ω emits NOTHING (C3: escapee reclamation = the region's own LIVE/DEAD lifecycle, **never collected, never slid**)."* `ZHEAP-5`/`ZHEAP-7` require the opposite: MARK/ADJUST/SLIDE is the reclamation story and the frontier moves one way. **The dormant arm is therefore NOT free reuse — its ω contract must change, and the structure map (`ZHEAP-3`) is what makes that possible.** Harvest the carve; do not inherit the lifecycle claim.
- **🔴 THE PIVOT REVERSES A STANDING LON DIRECTIVE — SAY SO PLAINLY, IT IS NOT AN OBJECTION.** `ZC_PORT_CSTACK`'s header records: *Lon directive 2026-07-09, "implement all ZETA storage for SNOBOL4 on the C stack. Then make it the default."* s205 moves ζ locals OFF that stack. **The strongest support for the reversal is written into the CSTACK port itself:** *"INHERITED STRUCTURAL LIMIT, stated honestly: a block that must SURVIVE a C return (suspended generator activations, the s6 long-lived class) cannot ride the C stack."* That limit is the disease the s195–s200 bug family kept re-presenting. **Lon: confirm the reversal is intended so the CSTACK default can be retired rather than left as a silent second regime.**
- **CHEAPEST NEXT ACT, BEFORE ANY EDIT: WAKE THE DORMANT ARM AND MEASURE IT** — build once, run the watermark under `SCRIP_ZETA_PORT=7` vs the `FORTH` default, and read what breaks. The arm has been dormant since s77, so it is unproven, not proven-good; the failure list is the real scope estimate for this rung and it costs one build to obtain. Per O2-DIRECTED-ONLY the build is `-O0` and every number carries that label.

**⭐ α/ω DISCIPLINE — LON RULING s205 ("two bumps at α, the result one retreats at ω; top is always the current result; FORTH LIFO holds"). CONFIRMED WITH TWO CORRECTIONS. This is the allocation contract both clauses implement.**
- **THE ASYMMETRY IS THE WHOLE DESIGN, AND IT IS FORCED BY THE MANUAL, NOT CHOSEN:** RBX (ζ locals) **NEVER retreats**; VSP (ζ results) **does**. Reason: `$`/`@` write-through survives a failed match (p.87, p.65–66, verified above), so a ζ local written during a failing attempt can be reachable from a global — the frontier must move one way and the collector reclaims. A RESULT slot is ANONYMOUS — nothing names it — so VSP may retreat freely. **Two regions exist precisely because the two lifetimes differ; this is the one-line justification for the whole rung.**
- **⚠ CORRECTION 1 — LEAVES BUMP; n-ARY OPERATORS DO NOT.** "Every BB bumps 16 at α" double-allocates: the operator's own slot lands ABOVE its operands and `[VSP+0]` stops being operand 0. The form that makes "top is always the current result" actually TRUE:
  - **LEAF (arity 0 — LIT, LEN, ANY, SPAN, POS/RPOS):** α = `VSP += 16` **and** `RBX += K` (the two bumps, as ruled). γ leaves it. ω = `VSP -= 16`.
  - **n-ARY OPERATOR (arity n ≥ 1):** α = `RBX += K` **only — no VSP bump.** It reads operands at `[VSP+0]`…`[VSP+16(n−1)]`, then at γ does `VSP -= 16(n−1)` and OVERWRITES the deepest slot, which it already owns. **Net VSP delta = 0.** So: two bumps for PRODUCERS, one bump + a γ retreat for CONSUMERS.
  - ⭐ **THIS RESOLVES s203's OWN SELF-CORRECTION IN ITS FAVOUR:** if each BB owns its result slot, SEQ has nothing to prefix-allocate and **SEQUENCE returns to being nothing** — exactly what the "cell_6 IS SUSPECT AT ITS ANCHOR" note predicted. ALT still carries arity for its retry edges, but not for allocation.
- **⚠ CORRECTION 2 — ω IS NOT THE ONLY RETREAT, AND BACKTRACK RESTORES *ABSOLUTELY*, NOT BY DECREMENT.** `VSP -= 16` is correct only for a leaf's own ω. The two shapes that killed cell_2 and cell_3 need an absolute restore:
  - **RETAINED ALT ARM** — its result is buried under later siblings' results. Those are dead on backtrack so LIFO is intact, but recovery is `VSP = saved`. **⭐ THIS ADDS A FIELD: THE CHOICE CELL MUST SAVE VSP BESIDE δ AND prev_CP.** Not in the cell contract today — add it at CELL-0's encoder, one slot, same cut restores all of {CP, δ, VSP, MH}.
  - **ARBNO with N dynamic instances** — N live results, N unknown at compile time, exactly one yielded. γ does `VSP = own_base + 16`.
- **⭐ HONEST STATEMENT OF WHAT LIFO BUYS — CONTAINMENT, NOT ELIMINATION.** LIFO holds, but ONLY because both dynamic spans are closed by absolute restore. cell_3's 1056B ARBNO span **still exists**; it is not removed by this design. What changes is that it now lives in a region with **NO interleaved control words**, so it cannot make any other box's operand offsets path-dependent. That is the cell_2 32B-gap disease cured BY CONSTRUCTION rather than by arithmetic, and it is the real win — claim that, do not claim the span is gone.
- **📉 CONSEQUENCE TO MEASURE EARLY — FAILURE BECOMES AN ALLOCATION-RATE EVENT.** Since ω never retreats RBX, every failed attempt ORPHANS its ζ carves on the heap (intended: the collector reclaims). So a heavily backtracking match allocates in proportion to ATTEMPTS, not to live state — a cost profile the RSP regime never had, because `add rsp,K` was free. **Measure on `pattern_bt` and `string_pattern` before the ladder commits**, `-O0`-labelled per O2-DIRECTED-ONLY. If the rate is bad, the answer is NOT bump-restore (the manual forbids it) but earlier collection or FENCE-SQUASH-style slide of the dead span.

**✅ MEASURED THIS SESSION (s205, `-O0`) — THE DORMANT ARM IS ALLOCATION-ONLY, AND THE SCOPE IS ONE ITEM.** Six minimal programs vs `sbl -b` under `SCRIP_ZETA_PORT` 6 and 7: port 7 is **OK on every box that carves NO ζ** (no-match, bare literal, `@`) and **SEGV on every box that carves** (`LEN(3) . X`, `$`+ARB, `ARBNO`). The split is exactly "does this box carve", which confirms the port's own admission by measurement: the bump lands, then FR/FRQ still resolve **rsp**-relative (`x86_fr32_prefix()` is hardcoded rsp, s196), so storage-at-rbx + addressing-at-rsp ⇒ garbage. **⇒ `A-1`'s "addressing form unchanged" IS the whole remaining cost of clause (1); the six-program table is the tripwire (3 SEGV → OK without moving the 3 OK).** Also found: `--zeta-port=heap` is REJECTED by the CLI (`scrip.c:442` name list omits `heap` and `owned`); only `SCRIP_ZETA_PORT=7` reaches the arm. 🔴 **AND, INDEPENDENT OF ZHEAP: `$` capture START is broken at HEAD in the DEFAULT port** — `'A' ARB $ OUTPUT 'E'` yields `A/AB/ABC/ABCD` vs oracle `⟨null⟩/B/BC/BCD`; counts and END cursors correct, every START 0 instead of 1 (s195 capture-extent class; MARKER-CAPTURE is the named fix). **⇒ The natural ZHEAP-7 gate witness is ALREADY RED, so use `@` instead — same manual-guaranteed property (p.65–66), GREEN at HEAD in both ports.** → `FINDING-2026-07-28-CLAUDE-SN4-ZHEAP-PORT7-IS-ALLOCATION-ONLY-AND-DOLLAR-CAPTURE-START-IS-BROKEN-AT-HEAD.md`

**⭐ ADDRESSING CONTRACT (s205, from Lon's $64,000 question). THREE REGIONS, TWO OPERAND CHANNELS, ONE CROSS-BOX RULE. To be written into the rung before any encoding work starts.**
- **THREE REGIONS AND HOW THEY ADDRESS:** (1) ζ LOCALS on the RBX heap: `[ζ_self+k]` — box's OWN base, delivered in `rax` at α, must survive the box's own α→β→γ span. Never addressed by another box. (2) ζ RESULTS on the mmap value stack: `[VSP+16k]` — compile-time constant, addressed by the consuming operator. (3) CONTROL on RSP: return addresses, call frames, C interop. Nothing else.
- **THE ONE INVARIANT THAT MAKES STATIC OFFSETS WORK:** every BB exits γ with exactly one 16B result on the value stack. No exceptions (ARBNO collapses via `VSP=own_base+16`; retained ALT restores VSP absolutely from its choice cell; a `*P` plunge still exits one). Given that, an operator with n children finds operand k at `[VSP + 16(n−1−k)]` — a compile-time constant. It needs to know arity, nothing else. **This is also why CELL-2 falsified ONE-STACK FORTH and not FORTH itself: the 32B gap was a choice cell interleaved between two results. Put control on RSP and there is nothing left on the value stack but uniform 16B DESCRs.**
- **TWO OPERAND CHANNELS:** (a) ANONYMOUS RESULTS — the normal path, `[VSP+16k]`. (b) NAMED VALUES — fetched from the variable descriptor, never from the stack. `*N` in `LEN(*N)` does a descriptor fetch of N; it never asks where SPAN's result went. This is why `$`/`@` write-through and deferred evaluation compose without the value stack knowing anything about them.
- **⛔ NO-CROSS-BOX-LOCALS RULE (falls directly out of heap addressing):** NO box reads another box's ζ locals. If state must cross a box boundary, it travels via a CELL on the chain (reached via MH/r12 or the choice-cell chain), never via `[ζ_other+k]`. **This rule resolves the SAVE/COND reach-over by design:** COND cannot read SAVE's capture-start via a static frame offset under heap residence — MARKER-CAPTURE's fix (put the start cursor in a MARK cell on the MH chain) is therefore not an optimization but the ONLY correct form under this regime. The pre-existing `$` start-cursor bug (START=0 instead of 1, measured s205) is that same reach-over already broken before the heap move.
- **HEAP IS CONTAINMENT, NOT ELIMINATION.** ARBNO's dynamic span (up to 1056B in the s203 seed) still exists on the VALUE STACK. What changes is that no control word is interleaved with it, so it cannot make any other box's operand offsets path-dependent. That is the cell_2 disease cured by construction.

---

## ⛔ LIVE CURSOR — s204 (2026-07-28): **DESIGN RULING (Lon) — ζ LOCALS MOVE TO THE HEAP; TWO STACKS + ONE HEAP. Nothing coded yet; this block is the plan of record. Everything else in SCRIP stays as-is.**

**THE THREE REGIONS.** (1) **VALUE stack** — 16B result DESCRs, uniform width, FORTH data stack; operator reads its n operands at `[+0]`,`[+16]`..`[+16(n-1)]`, compile-time constants forever. (2) **CONTROL stack** — RSP: return addresses + call frames + C interop, FORTH return stack. (3) **RBX HEAP** — ζ carves (locals + resumption state), bump frontier, `add rbx,K` replaces `sub rsp,K` — same ONE instruction, same box-local constant, same self-relative `[ζ_self+k]` addressing. Extras unchanged: R12 capture, Σ/δ/Δ.
**WHY TWO STACKS AND NOT ONE (the falsification restated):** a `call` pushes a return address — per-word bookkeeping interposed between results — which makes operand offsets PATH-DEPENDENT. That is the same disease as the retained-arm 32B gap (cell_2) and the ARBNO 1056B span (cell_3), wearing a third costume. Separating VALUE from CONTROL is the cure, and it is why FORTH has two stacks. Moving ζ locals off the value stack is what makes the FORTH depth contract hold: the only things left on it are uniform 16B DESCRs.
**⭐ NO MAGNITUDE TRICK — &MAXLNGTH-STYLE DISCRIMINATION IS REJECTED (Lon).** SPITBOL caps objects at MXLEN and forbids workspace below it (manual p.53/p.263) ONLY because its descriptor is one machine word with no room for a tag, so it discriminates pointer-vs-integer by numeric range. `DESCR_t` is 16 bytes with a 4-byte `DTYPE_t`. Every result slot says what it is. The cap and the unusable-low-memory rule buy us NOTHING and are not adopted.
**TWO DESCRIPTION MECHANISMS, DELIBERATELY DIFFERENT:** ζ LOCALS = **static STRUCTURE MAP**, per BOX TEMPLATE (not per activation — ARBNO ×10 instances = 10 carves, ONE map), RO, 2 bits/slot over four kinds {heap-ptr → ADJUST · code-addr → never (code does not slide) · stack-addr → never (control stack does not slide) · integer → never}, and the map STATES the locals|results split. ζ RESULTS = **DESCR tag**, per slot, runtime — type is unknown at compile time so results stay generic. Tagging locals was considered and REJECTED: pays per-activation for what is known per-template and doubles every 8-byte counter.
**MAP TRAVELS WITH THE CODE:** map pointer APPENDED to `DTP_t` beside `zsz` (`fn` stays offset 0 — `bb_match_defer` inline cache; `zsz` stays at 16 — PS-3 ARBNO stride latch). Same discipline that makes EVAL/CODE work: a runtime-compiled pattern mints its map with its code, one lifetime, no compile-time graph-global plan to be missing.
**⚠ GIVE-BACK IS NOT A FRONTIER BUMP-RESTORE — CORRECTNESS, NOT PERFORMANCE.** Immediate assignment `$` is GUARANTEED to survive a failed match (manual p.87: occurs whenever the subpattern matches, even if the entire match ultimately fails); `@` likewise; a deferred `*F(X)` can store on its way through. So a value written during a failing attempt is reachable from a global while sitting in the range a bump-restore would reclaim. THEREFORE: backtrack restores the ζ CHAIN HEAD only; the frontier moves one way and MARK/ADJUST/SLIDE reclaims. The s202 "free undo / give-back stays exact" claim is DOWNGRADED to "the cut orphans, the collector reclaims" — the 96B cell_6 measurement never exercised a surviving write-through and does not falsify this.
**RUN A — ζ TO THE HEAP.** A-0 structure-map contract (kinds, locals|results split, RO emission, `DTP_t` append). A-1 `sub rsp,K` → `add rbx,K` at α; addressing form unchanged. A-2 ζ THREADING: `prev_ζ` field + head; MARK walks chain and follows child-ζ slots (the `V11_ζ`-style caches in test_sno_3.c are already that edge) — **no per-call-site stack maps in hand-emitted x86**. A-3 give-back = chain head only, per the ⚠ above. A-4 heap-end: **DECISION OPEN — guard page vs explicit limit check at α.** Note α becomes a GC point either way; `sub rsp` had no such check.
**RUN B — VALUE STACK.** B-0 **RULING NEEDED — which register is VSP.** All SysV callee-saved are currently spoken for (RBX heap · RBP control-frame base · R12 capture · R13/R14/R15 Σ/δ/Δ); candidate release is Σ (constant for a match — reload or park in ζ). B-1 results 16B uniform even where type is statically known (POS/RPOS null, literals) — width is what keeps offsets constant; the tag is nearly free once width is fixed. B-2 register the value-stack EXTENT so ADJUST can scan it as a DESCR array.
**RUN C — COLLECTOR.** C-0 tags + map only; no magnitude discrimination anywhere. C-1 **INTERIOR POINTERS — DECISION OPEN.** `cat(x,y) = {x.σ, x.δ+y.δ}` returns x's base with a summed length, so every match result points INTO the middle of the subject; the tag says STRING without saying which block owns the bytes. Cheapest candidate: PIN the subject for match duration (one object, live by definition, makes every in-match result DESCR stable) vs a titled-block page index (`rt_arena.h` D6 title word is already "compaction insurance"). C-2 ADJUST walks three producers: ζ chain (map-driven), value stack (tag-driven), capture (R12).
**RUN D — GATE.** `P = 'a' *P | 'b'` on a long subject, `$ OUTPUT` inside the recursive arm, forced `COLLECT()` mid-match. Exercises in one program: sliding while ζ are live · chain traversal through recursive activations · adjustment of result DESCRs into string space · write-through survival (the bump-restore killer). Oracle: `sbl -b`. Monitor-first per RULES.md on any divergence.
**⭐ RUNG ZHEAP — ζ MOVES FROM THE RSP/RBP STACK TO THE RBX HEAP (Lon ruling s204). TWO ζ MODES ONLY; every other ζ regime is deleted.** Target default = HEAP: the collector is KNOWN to work; the stack regime is NOT known to work for locals+results together (that is what the s195–s200 bug family measured), so STACK is retained ONLY as the A/B comparator until the boundary claim is played out (`test_sno_1a.s` vs `test_icon_a.s`).
- **ZHEAP-0 — PARAMETERIZE THE FUNNEL.** `x86()` gains a ζ-medium switch: `ZMODE_STACK` (legacy `sub rsp,K`, `[rsp+k]`) vs `ZMODE_HEAP` (`add rbx,K`, `[ζ_self+k]`). ⛔ Switched INVISIBLY INSIDE the encoder, exactly as MEDIUM is — ONE `x86(...)` concatenation per instruction. A hand-written `IF(ZMODE_STACK,…)+IF(ZMODE_HEAP,…)` pair is the named forbidden shape of `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` R2/R7/R9/R10 and is an absolute violation. Templates NEVER learn which mode they are in. Env `SCRIP_ZMODE={heap,stack}`; **per-graph regime, never interleaved** (mixed regimes in one blob re-create the s188 depth-shift failure).
- **ZHEAP-1 — DELETE THE OTHER REGIMES.** `src/runtime/rt/zeta_heap.c` private 32MB ζ heap + handle table (`g_zh_tab`, `zh_hdr_t.handle`, `g_zh_free`, `SCRIP_ZH_MB`/`_TELEM`/`_NOMOVE`) and any surviving ZLS/`.bss` depth-arena path. ⚠ **HARVEST BEFORE CUTTING:** ZH already implements SLIDE (`memmove` + `g_zh_slid_bytes` + pin) and solves relocation by HANDLE TABLE INDIRECTION — one table write per moved block, zero pointer adjustment, at the cost of a load per ζ access. Its slide shipped OFF (`zh_nomove` defaults 1), so it is built-but-unproven. Record the trade in the finding; going direct-pointer + ADJUST must be a KNOWING choice, not an accident of deletion.
- **ZHEAP-2 — INLINE THE CARVE (this is what makes RBX pay).** Today a blob reaches `rt_gcheap_alloc` TWO call boundaries deep — zero blob call sites, because blobs go THROUGH `str_concat_d` (152 sites) / `rt_subscript_var` (RTX-2 finding). α must emit `add rbx,K` + frontier compare + predicted-not-taken fall-out to the C tail — no call in the hot case. **GENERALIZE `sink_carve48` (`src/templates/bb_call_fn.cpp`, PL-SINK-3), do not invent:** it already inlines this carve for a fixed 48B/HB_PLJ shape over `g_hp_fr`, whose offsets 0/8/16/24 (`top`/`end`/`virgin`/`zfull`) are BAKED and `_Static_assert`-guarded in `gc_heap.c` — if you change one, read the other. Fast path stays valid ONLY when armed + room + landing at/above `virgin` (no payload zeroing owed); otherwise tail to C. **This compare IS the heap-end answer — no guard page** (a page fault cannot keep the virgin/zeroing logic honest).
- **ZHEAP-3 — STRUCTURE MAP: REUSE, DO NOT DESIGN.** `src/contracts/zeta_storage.h` ALREADY defines the four kinds — `ZK_DESCR / ZK_RAW / ZK_PTR_GC / ZK_PTR_CODE` — and they are in live use (s202 address dispatch rides `ZK_PTR_CODE`). Verify coverage only: `ZK_PTR_GC`→ADJUST · `ZK_PTR_CODE`→never (code does not slide) · `ZK_RAW`→never (covers BOTH integers and saved-RSP, since a control-stack address never slides either) · `ZK_DESCR`→adjust by tag. Map is per BOX TEMPLATE (ARBNO ×10 instances = 10 carves, ONE map), emitted RO; map pointer APPENDED to `DTP_t` beside `zsz` (`fn` stays @0 — `bb_match_defer` inline cache; `zsz` stays @16 — PS-3 ARBNO stride latch) so it travels with runtime-compiled code for EVAL/CODE.
- **ZHEAP-4 — ζ THREADING.** `prev_ζ` field + head; MARK walks the chain and follows `ZK_PTR_GC` slots (the `V11_ζ`-style child caches in `test_sno_3.c` are already that edge). **No per-call-site stack maps in hand-emitted x86** — that is the whole reason to thread.
- **ZHEAP-5 — GC ENHANCEMENT.** ADJUST visits ζ blocks map-driven, in the ONE collected span (not a private heap). **DECISION OPEN — interior pointers:** `cat(x,y)={x.σ, x.δ+y.δ}` means every match result points INTO the subject, so a `ZK_DESCR` string slot names bytes without naming their owning block. Candidates: PIN the subject for match duration (one object, live by definition, makes every in-match result stable) · titled-block index (`rt_arena.h` D6 title word is already "compaction insurance") · ZH-style handle indirection (proven, costs a load). Pick before ZHEAP-8.
- **ZHEAP-6 — VALUE STACK: ITS OWN MMAP REGION.** Separate from ζ and from the control stack. VSP starts as a KNOWN GLOBAL CELL (not a pushed register) — required regardless, because GC during a C call must find the extent to scan it as a DESCR array. Access behind `x86("vsp_carve"/"vsp_operand")` so the later promotion to a register is ONE encoder edit with a kill-switch, and no template is written twice. Register candidate R10 ⚠ caller-saved (clobbered by every C call), is the SysV STATIC-CHAIN register (live if any called C was compiled as a nested function), and is fair game for a lazy-binding PLT thunk on first call through an unresolved symbol.
- **ZHEAP-7 — GIVE-BACK = CHAIN HEAD ONLY.** No frontier bump-restore on backtrack: `$` write-through is guaranteed to survive a failed match (manual p.87), so a bump-restore would reclaim memory reachable from a global. Frontier moves one way; MARK/ADJUST/SLIDE reclaims. α bumps; β bumps again on re-entry (fresh carve per value) — which is also what makes re-entry re-entrant, unlike the R12 dcap island.
- **ZHEAP-8 — GATE.** `P = 'a' *P | 'b'` long subject + `$ OUTPUT` in the recursive arm + forced `COLLECT()` mid-match: exercises sliding while ζ are live · chain traversal through recursive activations · `ZK_DESCR` adjustment into string space · write-through survival. Oracle `sbl -b`. Plus: A/B via `SCRIP_ZMODE` both ways · kill-switch byte-identity ON vs OFF (⚠ guard on NON-EMPTY output — the s163 near-miss compared two empty files and printed green) · watermark re-proven · monitor-first per RULES.md on any divergence · perf labeled `-O0` per O2-DIRECTED-ONLY.

---

## ⛔ DEMOTED s205 — the CELL-machine REDIRECT below was the #1 pointer until the s205 pivot above. The CELL ladder is now SECOND and inherits the storage-regime decision from RUNG ZHEAP; see the COLLISION note in the s205 block. Original banner preserved verbatim:

## ⛔ REDIRECT (2026-07-28, Lon design session): the ζ-ladder's NEXT RUNGS (a)/(b)/(c) from s200 are SUPERSEDED/ABSORBED by `GOAL-SN4-CELL-MACHINE.md` (contract: `DESIGN-SN4-CELL-MACHINE.md`). Do NOT resume the fc_geom PATREF one-liner; the split lands as CELL-7 there. Sessions on THIS file: rungs (d)/(e)/(f) remain valid but coordinate with the cell ladder before touching blob templates.

 Statement-level flat frames, Icon generators + suspend pin, monitor, watermark, the RTX ladder. Per-graph regime rule from the cell contract still holds — a blob compiles ALL-NEW or ALL-LEGACY, never interleaved.

## ⛔ LIVE CURSOR — s203 (2026-07-28): **SEQ/ALT PREFIX ALLOCATION LANDED (SCRIP `d5c7a72b`, seed/test_sno_cell_6.s) + REGISTER-PLANE MODEL AND TRIAL MATRIX (`.github` `90844fb6`, DESIGN-SN4-REGISTER-PLANES.md). No emitter touched — no artifact regen, no watermark re-proof; basis remains s202 ALT-FLAT 268/47 run · 267/46 compile · DIVERGE=2.** HEADLINES: (1) ✅ **LON'S RULING — IR_SEQUENCE and IR_ALTERNATE were about to be deleted as pure wiring; they are the ONLY nodes carrying an ARITY, and arity IS the allocation size.** Made PREFIX (α carves header + n slots); everything else stays POSTFIX writing into its own slot at a COMPILE-TIME offset. SEQ arity n → n slots (all live, all consumed); ALT arity n → 1 result slot + n−1 retry edges (one arm survives). Static offsets are legitimate because the BASE arrives with control: E rides every choice cell beside δ and is restored by the same cut. EVAL/CODE unaffected — per-node arity, not a graph-global geometry pass. (2) ⚠ **RETIRES PRIOR-CURSOR MECHANISMS — DO NOT ORIENT OFF THEM:** the SIBLING CHAIN (cell_3) and the REPUBLISH LAW (cell_4/5) are DEAD, along with ARBNO's nested-link special case and the fused 48B unit. Result slots (16B, parent-allocated) and choice cells (32B, pushed) separate cleanly — they were only ever one object because nobody owned the slots. The s202 cursor below names both as landed mechanisms; that text is superseded, not corrected in place. (3) ✅ **MEASURED, unexpected:** live frame on identical input 1056B → **96B** (cell_3 → cell_6). Two causes: parent-owned slots, plus choice cells becoming pure control so they can be POPPED — an ARBNO instance landing on the last arm leaves NO cell at all. Give-back stays exact (carried by surviving ALT cells, which is where SNOBOL4 semantics put it). The doc's FENCE-SQUASH retention cost largely evaporates. (4) 🔴 **SELF-CORRECTION, cell_6 IS SUSPECT AT ITS ANCHOR (Lon, same session):** SEQUENCE is NOT a control boundary — nothing ever returns to a sequence; Ch.18 step 6 pops the stack and never consults one, which is why it was deletable. Allocation boundaries must coincide with GARBAGE boundaries, and those are TURNING POINTS (cell pushes: ALT, ARB/ARBNO/BAL, FENCE, scan BASE, recursive re-entry — the RULE is "anchors at cell pushes", NOT `if (op==IR_ALTERNATE)`). If the anchor moves there, E and CP COLLAPSE (base restored because CP is restored — one act), the saved-E field disappears, and SEQUENCE returns to being nothing. UNTESTED. Known wrinkle: a turning point enclosing a subtree (ARBNO) leaves a dynamic number of internal cells between its anchor and what follows — an operator straddling it must hop via the enclosing cell's prev_CP to clear the subtree in ONE step, never walk it. cell_2's static offsets and cell_3's sibling chain BOTH looked free and BOTH died on ARBNO specifically; do not assume this one is free. (5) ✅ **REGISTER PLANES (Proebsting 1997 read):** ONE physical stack, four planes, four registers — RSP=TOP(WAM H) · RBP=CP(B) · RBX=E(E) · R12=MH(TR); r13/r14/r15=Σ/δ/Δ. One cell carries all four planes, one cut restores all four; that is the whole cure for "control decoupled from context". Proebsting allocates ONE STATIC TEMP PER AST OPERATOR AND NEEDS NO STACK AT ALL; his `ifstmt.gate` IS our `resume` field — we already had his machine, the stack is what we added. **BOUNDARY CLAIM (unproven):** a frame is needed ONLY where a node is MULTIPLY LIVE — ARBNO/ARB/BAL instances, recursive stored patterns, procedure activations — which is item-for-item the s195–s200 bug family. Every singly-live node keeps a static temp: no frame, no offsets, no allocation. **NEXT RUNGS IN ORDER:** (a) ⭐ **THE DECISIVE PAIR — `test_sno_1a.s` (Proebsting-pure, predicted DISPROVE: ARBNO instances collide on one temp) and `test_icon_a.s` (Proebsting-pure, predicted PROVE: every node singly-live).** Same mechanism, opposite predictions, both cheap; the boundary claim lives or dies exactly between them. Run these FIRST. (b) `test_sno_1c.s` turning-point anchor (rung 4 above) — ahead of CELL-0, so CELL-0's encoders get written ONCE against the right form. (c) `test_sno_2a/b`, `3a/b` — **axes deliberately BLANK: `test_sno_2.c`/`test_sno_3.c` HAVE NOT BEEN READ. Read, pick the axis, write the claim, THEN code.** (d) CELL-0 contract in x86_asm.h. (e) MARKER-CAPTURE, r9 park anchor for 123_pat_regex_alt_class, stored-pattern SEGV family. Matrix + predictions: `DESIGN-SN4-REGISTER-PLANES.md`.

## ⛔ LIVE CURSOR — s202 (2026-07-28): **CELL MACHINE SEEDS 2–5 + ICON_CELL_1 LANDED. SCRIP `1e881ce5`. No watermark change (seed-only commit, no emitter touched).** HEADLINES: (1) ✅ **test_sno_cell_2.s** — Bird|Blue|LEN(1) × 2-ary ALT: ONE 32B UNIT per box (result + choice record + δ save), postfix emission order, operand-before-operator invariant proven by runtime trace across a cross-operand backtrack. Falsifies the DESIGN-SN4-CELL-MACHINE.md FORTH contract (choice-cell gap — retaining arm leaves 32B between its result and the next operand; path-dependent, not a compile-time constant). Negative path (ZZZZZZZZ) verified against sbl. (2) ✅ **test_sno_cell_3.s** — POS(0) ARBNO(Bird|Blue|LEN(1)) RPOS(0): ARBNO breaks static [rsp+32k] (1056B span, 10 instances); fix = sibling chain (rbx/SIB, one 8B field, written by producer at its own α, restored by the cut from the same cell as δ). ARBNO nested-link law: a variable-arity producer republishes its own outer left link into every recursion (discovered first-pass as a bug: chain walked into instance level). Static slot and chain slot printed simultaneously — disagree, on purpose. Oracle (sbl): text=BlueGoldBirdFish len=16. (3) ✅ **test_sno_cell_4.s** — complete test_sno_1.c with `$ OUTPUT` write-through: yield ladder byte-identical to sbl (quickscan+fullscan, including the null first yield that test_sno_1.c itself suppresses). REPUBLISH law retrofitted here and in cell_3/5. (4) ✅ **test_sno_cell_5.s** — `P = 'a' *P | 'b'`, two call sites, unbounded recursion. CALL unit + CL (r12) saved in every choice cell, restored by the universal fail stub — control+depth+cursor+operands+call context arrive together in one cut. Kills the s197/s198/stored-pattern blocker class in the seed domain. Positive 'aaab' / negative 'aaax' both sbl-verified. (5) ✅ **test_icon_cell_1.s** — `every write(5 > ((1 to 2) * (3 to 4)))`: Icon generator = RETAIN box, every = jmp blob_ω, values 3,4 then EXHAUSTED. Generator re-init is free (cut reclaims dead inner, α carves fresh). ⭐ REPUBLISH LAW discovered here: consuming operator's result links to leftmost operand's prev_sib, not rbx, else consumed subtrees stay chain-visible. Retrofitted into cell_3/4/5. (6) ⛔ **WATERMARK NOT RE-PROVEN this session** (no emitter change; s202 ALT-FLAT last landed watermark = 268/47 run, 267/46 compile, DIVERGE=2 per s202 cursor below — that is the basis). **NEXT RUNGS:** CELL-0 contract landing in SCRIP (push_choice / fail_stub / cell tags / commit-walk helpers in x86_asm.h, additive, byte-verified); then CELL-1 scan bracket. Parallel: MARKER-CAPTURE per s202 ALT-FLAT cursor; r9 park-address anchor for 123_pat_regex_alt_class; stored-pattern SEGV family.

## ⛔ LIVE CURSOR — s200 (2026-07-28): **NO CODE LANDED. SCRIP HEAD 9b19bb5a (s199 DEFER-STAR), unchanged. Census NET=48, watermark m3 221/94 · m4 219/94 · SKIP=2 · DIVERGE=1 (W06_tab) — re-proven at session start, rebuilt clean.** HEADLINES: (1) 🔍 **BISECT COMPLETE — first bad commit is `2410f938` FLATDISP-1 (s188).** The stored-pattern SIGSEGV is the priced cost of FLATDISP-1, not a later regression. The commit body itself recorded the sacrifice: "Known-bad accepted by directive: mixed_workload, pattern_bt, string_pattern segfault." Remaining blocker named there: "fc_leaf_walk called on the pattern range only." (2) 🔍 **ROOT CAUSE LOCALIZED — `fc_geom()` omits IR_MATCH_DEFER.** Every carving leaf listed (SAVE, ARB, SPAN, TAB, RTAB, BREAK, BREAKX, BAL, REM, ALTERNATE, SCAN_TAB/MOVE/MATCH) except DEFER. Consequence: `op_flat_disp` is 16 short for every node after a DEFER; `FRQ` emits `[rsp+80]` where the slot is at `[rsp+96]`; `mov rsp,0x5`; `push r14`; SIGSEGV. Confirmed at gdb: fault address `[rsp+80]=0x5`, valid rsp at `[rsp+96]=0x7fffffffe8d0`. (3) 🔴 **CANDIDATE ONE-LINER TRIED AND REVERTED.** Adding `if (nd->op == IR_MATCH_DEFER) { if (k) *k = 16; return 1; }` beside the ALTERNATE case: m3 221/94→222/93 (net gain), but m4 SKIP 2→13 with `scrip --compile` aborting — regression hiding in a nicer FAIL count. Reverted. Flat 16 is wrong because `x86_frame_sink` names DEFER a deep window with runtime `fbytes`; crediting only 16 double-counts against the sink. (4) ✅ **THREE-WAY RBP TAXONOMY CLOSED (Lon design discussion).** Case 1 (ALT merge pad): no rbp needed, pad fires on yield edge only, one instruction per arm exit. Case 2 (DEFER dynamic window): `x86_frame_sink` parks old-rsp at `[rsp+0]`, `mov rsp,[rsp]` releases — rbp-free already. Case 3 (PAT$ scan-retry): `scanfail` arrives by jmp from arbitrary carve depth, parked-anchor trick fails — **rbp IS load-bearing here, these 42 refs are the correct answer, not unfinished work.** 48 = 42 PAT$ scan-retry (irreducible) + 6 marshal reads. (5) ✅ **OP-SPLIT DIRECTIVE (Lon, s200).** TT_VAR (eager, `S ? P`) and TT_DEFER (unevaluated `*`, `S ? *P`) both lower to IR_MATCH_DEFER today — one opcode, two geometries, fc_geom cannot answer correctly. **Split: `IR_MATCH_PATREF` (eager, static 16-byte carve only, safe to grant in fc_geom) and `IR_MATCH_DEFER` / `IR_MATCH_STAR` (unevaluated, dynamic fbytes sink).** After split: IR_MATCH_PATREF gets `k=16` in fc_geom (safe, full static carve); IR_MATCH_DEFER must NOT (dynamic part not expressible as constant); sno_defer_is_star side table simplifies to an opcode check. Lon also flagged the IR_MATCH_DEFER name as confusing alongside the DEFER keyword; better names: `IR_MATCH_STAR` (names the `*` operator) / `IR_MATCH_PATREF` (names the eager arm). **4-LINE GATE REPRODUCER:** `PAT = ','` / `S = 'alpha,beta,'` / `S PAT :F(NO)` — SPITBOL: `matched`, SCRIP: SIGSEGV. Far cheaper than any benchmark and not in the 94-FAIL set so it is a genuine falsifiable witness. → `FINDING-2026-07-28-CLAUDE-SN4-DEFER-GEOM-AND-PATREF-SPLIT.md`. **NEXT RUNGS IN ORDER:** (a) **IR_MATCH_PATREF SPLIT** — lower site (~1212-1218 lower_snobol4.c), fc_geom, emit dispatch, template split, sno_defer_is_star simplification. Monitor-first on 4-line reproducer. (b) **IR_MATCH_PATREF geometry** — after (a), `k=16` in fc_geom is the safe one-liner; verify no m4 compile abort. (c) **DEFER dynamic-sink geometry** — static 16 + runtime fbytes interaction with fc_leaf_walk prefix sum. (d) **ARBNO-ELEM dig** (s195). (e) **fc_cond FIRST-WINS audit**. (f) **SYM-VIS-M3**.

## ⛔ LIVE CURSOR — s199 (2026-07-28): **DEFER-STAR LANDED — rbp census 113 → 48, the biggest single drop in the ladder; the ~90 CEILING FROM s196/s197 IS SUPERSEDED.** SCRIP `9b19bb5a` (source) + `85bb6d19` (feature .s); corpus `814934ea` (benchmark .s) + `5afe1179` (demo .s). **WATERMARK RE-PROVEN AT SESSION START AND POST-CHANGE, HELD EXACTLY: m3 221/94 · m4 219/94 SKIP=2 · DIVERGE=1 (W06_tab); FAIL SETS BYTE-IDENTICAL BOTH MODES, ZERO MEMBERSHIP CHURN (diffed programmatically, not eyeballed); benchmark runtime signatures byte-identical pre/post.** HEADLINES: (1) ✅ **DEFER-STAR** — `emit_graph_has_deep_arrival()` counted EVERY `IR_MATCH_DEFER`, but that op is built by TWO lowering arms the node cannot tell apart: `TT_DEFER` (the `*` unevaluated-expression operator, lower_snobol4.c ~1212/1214) and `TT_VAR` (a bare pattern-valued variable in pattern position, ~1218 — the lowerer's own comment calls it the "twin"). **The manual separates them and that is the whole soundness argument:** "Recursive Patterns" p.122 — a recursive pattern is possible because *"the unevaluated expression operator makes the definition possible"*, and `*LIST` *"allows a forward reference to a pattern not yet defined"*. **Recursion REQUIRES `*`**; a stored pattern reused by name is built EAGERLY and cannot name itself. Narrowed to star-sourced only via a side table (`sno_defer_star_register`/`sno_defer_is_star`, zeta_storage.c — twin of the s195 `fc_pair_extent` idiom, NOT an IR_t field, PEERS RULE). (2) 🔍 **MEASURED ROOT — nobody had checked WHICH arm was arming the gate:** all four heavy benchmarks armed deep arrival via **exactly ONE `MATCH_DEFER` in `main`**, and **none contains a `*` in source** — every one was TT_VAR. roman 29→1 · mixed_workload 34→15 · string_pattern 23→14 · pattern_bt 23→14. The `PAT$` scan-retry pin (14) correctly survives — irreducible per s196: SPITBOL Ch.18 step 6 restarts at cursor+1 from ARBITRARY carve depth so `mov rsp,rbp` is the only way back, and `&ANCHOR` is a runtime keyword so no static classifier retires it. (3) ⚠ **THIS IS NOT THE s197 CHANGE — do not confuse them.** s197 dropped `IR_MATCH_DEFER` wholesale (also 113→48) and CORRECTLY reverted: recursive defer is genuinely unbounded and every recursive witness already sat inside the 94-FAIL set, so its green was false. This split keeps `*` deep and that is **VERIFIED, not argued** — the manual's own `ITEM`/`LIST` example still classifies deep (**49 rbp refs, 4 seeds**) while a bare-variable control drops to 14. s197's "no Use-A narrowing exists" was right about the axis it tried (deferred *scalar* vs deferred *pattern* — dead, `LEN(*N)` emits no `match_defer`) and missed this one: **eager vs unevaluated**, the axis the manual itself draws. (4) 📝 **CORRECTIONS:** s196's `flat_pat_susp` estimate is too optimistic — measured, the removable part of the γ/ω epilogue under an alternation-free blob is `push rbp` = **1 line/blob (3 total)**, not "~6/blob, ≤12" (γ's other two refs are reached from arbitrary depth, not statically convertible). The **~90 ceiling is superseded**: both s196 and s197 assumed the ~65 DEFER refs irreducible; most were TT_VAR. New floor **48**, now dominated by the genuinely irreducible `PAT$` scan-retry class. Manual also confirms BREAK/SPAN are single-result (p.69-71), supporting s196's alternation-free classification. (5) 🔴 **PRE-EXISTING, EXPLICITLY NOT INTRODUCED HERE:** a stored-pattern reference (`PAT = BREAK(',') . W ','` used as `S PAT`) SIGSEGVs in m3 on clean `84ea9f85` too — verified by stash+rebuild+rerun, not assumed. That is the **s198 stored-pattern segv**, and it is a second independent reproducer beside `NPAT = ':' / ? NPAT`. **NEXT RUNGS IN ORDER:** (a) **STORED-PATTERN SEGV** (s198 rung (a), unchanged as #1 — monitor-first per RULES.md; two reproducers now). (b) **DEFERRED-ARG BUG** (`LEN(*N)` empty capture). (c) **ARBNO-ELEM dig** (s195). (d) `flat_pat_susp` epilogue split — now known to be worth only ~3, deprioritize. (e) fc_cond FIRST-WINS audit. (f) SYM-VIS-M3. → `FINDING-2026-07-28-CLAUDE-SN4-DEFER-STAR-SPLIT-IS-THE-NARROWING-S197-MISSED.md`.

## ⛔ LIVE CURSOR — s198 (2026-07-28): **SCANBASE INDEPENDENTLY REPRODUCED AND LANDED (census 119→113); STORED-PATTERN SEGV LOCALIZED TO bb_match_release.cpp — FIX ATTEMPT TRIED AND REVERTED.** SCRIP `6c16762a`. WATERMARK RE-PROVEN: m3 221/94 · m4 219/94 SKIP=2 · DIVERGE=1 (W06_tab); FAIL SETS BYTE-IDENTICAL (zero churn). HEADLINES: (1) ✅ **SCANBASE** — origin HEAD was `62aaf9ff` (s195); `a0e83fe2` (s196 SCANBASE) was NOT on origin. Reconstructed from first principles: SPD-2 scan-slot prologue stores rebased rbp→rsp both media; R10 disp8/disp32 divergence fixed; all 4 encodings byte-verified vs `as`; premise re-proven (s196's "+1 instruction" reason was wrong — it's +7, but rsp is unmodified across all 7); ratchet lowered 119→113 in landing commit; watermark held exactly. (2) 🔴 **STORED-PATTERN SEGV — new bug, independent of deferred-arg, more fundamental.** `NPAT = ':' / ? NPAT` is the two-line reproducer — no capture, no pattern function, no deferred arg; inline `? ':'` passes; DIVERGE-blind (both modes fail, only oracle catches it). LOCALIZED: `match_release` arrives 16 bytes deeper than `match_defer` (rsp=0x…e8d0 vs 0x…e8e0; gdb-measured across the box sequence). Fault is `mov rsp, [rsp+64]` loading `2` (16 bytes past the saved slot). Root cause: `bb_match_release.cpp` tail arm uses raw uncompensated `RSP()` for a cross-box read; fix attempt (`+op_flat_disp`) measured: `op_flat_disp==0` for this box, offsets unchanged — the missing 16 is DYNAMIC (deep-window handoff, `x86_frame_sink` base = rsp+16), not static. Fix must use the parked anchor (`mov rsp,[rsp]`), not a static prefix sum. Reverted clean. (3) 🔴 **DEFERRED-ARG BUG** — `LEN(*N)` empty capture, `TAB(*I)` abort (IR op=89 no template), `SPAN(*C)` not yet tested. Bisection: inline deferred `LEN(*N)` gives empty capture (wrong value, not crash) independently of the stored-pattern segv. NOT ATTEMPTED this session — stored-pattern segv takes priority as it is more fundamental. **NEXT RUNGS IN ORDER:** (a) **STORED-PATTERN SEGV** — monitor-first on `NPAT = ':'` reproducer; map both entry paths into `match_release` (always-deep-window? or also balanced?) before anchor-deref fix. (b) **DEFERRED-ARG BUG** — `LEN(*N)` monitor-first after (a) resolves, as some of the 94 `056_pat_star_deref`/`070–075_pat_star_*` FAILs may dissolve under (a). (c) **ARBNO-ELEM dig** (s195 cursor). (d) **fc_cond FIRST-WINS audit** (s195). (e) **SYM-VIS-M3.** → `FINDING-2026-07-28-CLAUDE-SN4-SCANBASE-LANDED-AND-STORED-PATTERN-SEGV-LOCALIZED.md`.

## ⛔ LIVE CURSOR — s196 (2026-07-27): **SCANBASE LANDED (census 119→113) — AND FLATDISP-5a's PREMISE IS FALSIFIED; THE RUNG IS RETIRED.** SCRIP working tree (see §handoff). **WATERMARK RE-PROVEN AT SESSION START AND POST-CHANGE, HELD EXACTLY: m3 221/94 · m4 219/94 SKIP=2 · DIVERGE=1 (W06_tab); FAIL SETS BYTE-IDENTICAL (membership diffed, zero churn). Icon 4/0→4/0.** HEADLINES: (1) ✅ **SCANBASE** — the SPD-2 scan-slot prologue stores (`emit.cpp` scan_live arm) were hand-written `[rbp+kt-32]`/`[rbp+kt-40]` in BOTH media; they run ONE instruction after the jmp-entry `mov rbp,rsp` seed, so rsp==rbp==base by construction. Rebased to rsp, both media, 4 encodings byte-verified vs `as`. **R10 defect fixed in passing:** the rbp form emitted disp32 UNCONDITIONALLY and so diverged from its own TEXT arm whenever the offset fit a byte (kt−40=120 in every blob measured); new form picks the as-matching disp8/disp32. −6 refs (2 × 3 blob-bearing benchmarks); ratchet baseline lowered 119→113 in the landing commit per the gate's own rule. (2) 🧱 **FLATDISP-5a IS RETIRED — ITS PREMISE IS WRONG.** A PAT$ blob is NOT pinned by conservative flagging and NOT by suspension: it is pinned by the **unanchored scan-retry unwind** (`scanfail: … mov rsp, rbp; jmp _attempt`), which arrives at ARBITRARY carve depth. That is SPITBOL Ch.18 step 6 verbatim ("if &ANCHOR is zero, advance the starting cursor position by one and go to step 2") — restarting abandons the failed attempt's cells, and recovering base from a REGISTER is the only way back when rsp is at unknown depth. **The scan-retry is a FIFTH member of Lon's STATEMENT/FUNCTION/ARBNO/FENCE1 family.** MEASURED, not reasoned: string_pattern and mixed_workload's blobs contain **NO alternation** (`assign_cond assign_save break lit sequence` — exactly 5a's "invariant" class, all single-result per the manual; BREAKX/ARB/BAL are the real extenders) and STILL need the pin. Inlining them would relocate retry-from-depth into the caller's frame — straight back into the s195 displacement-mine class. **&ANCHOR is a RUNTIME keyword** (`cmp qword ptr [g_anchor],0`), so no static classifier can retire the loop. (3) 📋 **THE FLAG CONFLATION, and the honest remnant:** `flat_pat` is armed NAME-keyed (`strncmp(pname,"PAT$",4)`), never property-derived — same defect shape as the s193 5b root cause — and carries TWO meanings: (a) "is a PAT$ blob" (read by `scan_live`; **CORRECT as-is, do not narrow**) and (b) "γ RETAINS" (read by `emit_jmp_pin_rbp` + xa_flat 446/585). Only (b) is property-dependent and only its **EPILOGUE** half is addressable — the prologue seed must stay for the scan retry regardless. Renamed rung: **`flat_pat_susp` split, gating ONLY the retaining epilogue** (alternation-free blob ⇒ `_res` landing dead, ~6/blob, ≤12 total); NOT attempted — trace `_res` consumption first (the `lea rax,[rip+_res]` record is built unconditionally in BOTH blob classes). (4) 📋 **REVISED FLOOR — the ladder does not reach zero:** 113 = PAT$ internals 40 (scan-retry, irreducible bar ≤12) + `flat_gen` `_res` landings 22 (load-bearing, REG-7 U5 record contract) + `main` outer 31 (`xaf_deep`, partly relaxable second-order) + roman `match_sequence_af` 12 (**ARBNO — DO NOT TOUCH**, same family as the 5 crashers) + marshal 4 (register read, not a frame ref). **~113 → ~90 is the realistic reach; "drive to zero" walks into a wall the SPITBOL algorithm puts there.** (5) ⚠ **PROSE-ROT, three docs, named not fixed** (blast radius; parallel sessions share them): `ARCH-ICON.md` (MANDATORY step-6 read) says FR/FRQ resolve `[rbp+off]` "no depth compensation, sealed U5 s87" — FALSE, `x86_fr32_prefix()` is rsp-only; `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` (also mandatory) says ζ-scratch is `[r12+off]` — dead since R12-ERAD s65; `x86_asm.h:841` FR()'s comment says "rbp is depth-immune" three lines under the rsp-only prefix it calls. **NEXT RUNGS IN ORDER:** (a) **ARBNO-ELEM dig** (unchanged from s195, still #1 — 5 crashers, roman's 12, map the value-spine window addressing BEFORE any fix, monitor-first). (b) **`flat_pat_susp` epilogue split.** (c) fc_cond FIRST-WINS audit. (d) SYM-VIS-M3. → `FINDING-2026-07-27j-CLAUDE-SN4-SCANBASE-AND-FLATDISP-5A-PREMISE-FALSIFIED.md`.

## ⛔ LIVE CURSOR — s197 (2026-07-28): **NO CODE LANDED. SCRIP `a0e83fe2` (s196 SCANBASE, unchanged). Census NET=113, watermark m3 221/94 · m4 219/94 · DIVERGE=1 (W06_tab) — all re-proven at session start and confirmed clean at close.** HEADLINES: (1) 🔴 **DEFER REMOVAL TRIED AND REVERTED** — removing `case IR_MATCH_DEFER:` from `emit_graph_has_deep_arrival()` measured clean (census 113→48, watermark held exactly, 3 passing witnesses reclassified depth-static and survived). SPITBOL manual v3.7 "Recursive Patterns" (p.122-123) overturned it: DEFER is genuinely unbounded (`EXPRESSION = *EXPRESSION | "(" TERM ")"` = immediate stack overflow; depth tunable with `-s`). The false green exists because EVERY recursive-defer witness is already inside the 94-FAIL set — the change would have gone live the moment the ARBNO dig succeeded and presented as an ARBNO regression. **DO NOT RETRY THIS CHANGE** until recursive defer programs pass; see FINDING doc. (2) 🔴 **No Use-A narrowing exists** — proposed split: `*` as deferred scalar fetch (LEN/TAB/SPAN/ANY/etc. args, depth-static) vs `*` as deferred pattern reference (recursive plunge). Falsified: inline `LEN(*N)` emits NO `match_defer` — the box handles the deferred arg internally (kinds: `match_len_`/`match_head_`). Both `IR_MATCH_DEFER` emission sites in `sno_pat_node` are pattern-ELEMENT position (`TT_DEFER` and `TT_VAR`, lower_snobol4.c ~1210-1218). The ENTIRE `IR_MATCH_DEFER` population is pattern transfer; no scalar subset to peel off. (3) 🔴 **NEW BUG: DEFERRED-ARGUMENT FAMILY BROKEN BOTH MODES** — `LEN(*N)` empty capture, `TAB(*I)` SIGSEGV, `SPAN(*C)` wrong fail; control `LEN(3)` passes. Reproduces identically m3 and m4 (DIVERGE-blind — both modes wrong same way; only oracle catches it). Localized: `lower_snobol4.c` TT_SPAN (~1176) / TT_ANY (~1163) / TT_LEN arm (~260): `sno_cset_fold(t->c[0])` returns NULL for a deferred arg, falls to `sno_pre_req(cx, t, nd)` — that arm is the bug site (or its consumption in the box). **MONITOR-FIRST per RULES.md; `LEN(*N)` is cheapest reproducer (wrong VALUE, not crash). Check this BEFORE the ARBNO dig — some of the 94 named `056_pat_star_deref`, `070–075_pat_star_*` may be this root, not ARBNO.** (4) ⚠ **RSP/RBP CONVERSION CEILING IS ~90/113, NOT ZERO** — ~65 DEFER conservatism (correctly held, undecidable statically), ~22 `flat_gen` suspend protocol (load-bearing by design), ~6 marshal reads (register reads, not frame refs). Census ratchets on programs that SEGV at runtime (all four heavy benchmarks crash before AND after any change this session). (5) ⚠ **CONCURRENT WRITER** — HEAD moved `62aaf9ff`→`a0e83fe2` mid-session via `git commit` this session never issued; reflog shows `commit:` not `pull`. Silently invalidated the first measurement (reported "119→48" was actually s196+DEFER conflated; true DEFER-only = 113→48). All subsequent measurements HEAD-stamped. If parallel sessions share this volume, HEAD-STAMP EVERY MEASUREMENT. → `FINDING-2026-07-28-CLAUDE-SN4-DEFER-DEEP-IS-LOAD-BEARING-AND-DEFERRED-ARG-FAMILY-BROKEN.md`. **NEXT RUNGS IN ORDER:** (a) **DEFERRED-ARG BUG** — monitor-first on `LEN(*N)`, bracket in lower_snobol4.c `sno_pre_req` arm. (b) **ARBNO-ELEM dig** — per s195 cursor (value-spine window map first, then tail fix, then chain). (c) **fc_cond FIRST-WINS audit** (s195). (d) **SYM-VIS-M3**.

## ⛔ LIVE CURSOR — s195 (2026-07-27): **TWO FLATDISP DISPLACEMENT MINES KILLED — CROSSCHECK m3 185→221 · m4 183→219 · FAIL 130→94 · DIVERGE=1 (W06_tab) HELD; CENSUS NET=119 HELD; BENCH OK 8→11 FAIL 4→0 CRASH 4→5.** SCRIP `62aaf9ff` + 3 artifact sweeps (corpus `2e26d600` benchmark / `5be21cdc` demo; feature in SCRIP). **ONE DISEASE, THREE SITES — rsp-relative flat-frame reads missing depth compensation, LATENT under the always-seeded rbp frame, EXPOSED when s193/s194 made graphs depth-static** (prose-rot casualty: x86_asm.h's U3 comments still say FR=[rbp+off]; `x86_fr32_prefix()` is hardcoded rsp). HEADLINES: (1) ✅ **FLATDISP-LEAF-ORDER = the rt_dcap_pump segv root (the 130-blocker), monitor-first per RULES.md:** capture pairs allocate [COND,SAVE,inner…] but FLOW save→inner→cond, so fc_leaf_walk's running prefix registered COND 16 SHORT — COND's FR read of the SAVE slot landed on head's rsp-snapshot cell (039: saved_delta=0xffff9b20=the snapshot low-32; len=2−(−25824)=25826, arithmetic-consistent, single garbage source isolated). FIX: lowerer records each pair's inner allocation extent at build (`fc_pair_extent_*`, zeta_storage.c); fc_leaf_walk recurses [k+1,E) FIRST, registers COND at the resulting pfx (the S10c suspended depth). fc_cond (range-sum) audited order-independent/innocent. (2) ✅ **FLATDISP-LIVE-BUMP = the define-cluster root (double(5)→0):** bb_call_proc_staged's slim-call arg install reads staged args while its own `sub rsp,scc_sb` is live — fc_hit arm always carried +scc_sb, the FRQ fallback never did (depth-immune under rbp, 32 short under rsp; 083's arg read the rep-stosb-zeroed frame). FIX: `FRQB(off,bump)` beside FRQ in x86_asm.h, both branches add the live bump; offset rides the STRING so x86_parse keeps BINARY==TEXT (R10), zero encoder change. Cured ALL FOUR bench FAILs. (3) 🧱 **ARBNO FAMILY = THE NEXT DIG — BOTH ARMS INDEPENDENTLY BROKEN** (052-class; all 5 bench CRASHers): TAIL runaway fully characterized by per-label m4 gdb (β enters at the yield frontier, reads a saved-cursor the right-range RPOS literal's FLAT-slot store overwrote — [rsp+272]==elem+HDRB, the qword-6 high dword = the eternal-0 cursor, net −320/cycle, 64MB descent). TWO fixes tried and REVERTED with measurements: (a) fct_leaf_range cell-base — the literal did NOT move; its addressing is a THIRD scheme (value-spine window RSP(w)/zls, NOT fcl — it carries op_flat_disp=0 by the s190 sentinel-inherit law) that must be MAPPED before any fix is sound; (b) tail-lit-decline (the residues law's own class) — fired correctly but REGRESSED 4 green tail programs (221→217) for nothing: the CHAIN arm segvs on 052 too. Also found: stale FIRST-WINS fc_cond double-registration (052 wrap-COND read 336 vs finalize fpb=0 math 304 — sno_cap_fc's lowering-time registration precedes finalize's and wins). (4) 📋 FAIL→CRASH accounting: ONE bench program (roman, by elimination) moved FAIL→CRASH — probable MASK-REMOVAL (args now bind, program runs further into the arbno mine); verify when ARBNO lands. (5) 📋 Monitor scr granularity: scr emits LABEL events only, spl per-assignment VALUEs → shape-diverge at step 2 on ANY program; bracket theorem still worked at statement level; MON-RE-shaped rung: teach scr the VALUE events. (6) 📋 **SYM-VIS (Lon question s195):** m4 box α/β labels are ALREADY .symtab LOCALS (nm: 39 α/β syms in 052.prog, binding `t`; `break n17_match_arbno_β` + named backtraces worked all session) — .globl buys strip-resilience only and costs a directive per port on the beauty-scale path (34.5M lines, m4 termination unsettled). THE REAL GAP IS m3 (slab = `?? ()`): named rung **SYM-VIS-M3** = emit the perf JIT map (/tmp/perf-<pid>.map, START SIZE name per box port) from the binary label-define walk, gated SCRIP_PERF_MAP=1 — perf-native, zero symtab cost, gdb-loadable. **NEXT RUNGS IN ORDER:** (a) **ARBNO-ELEM dig** — map the value-spine window addressing for element residents FIRST, then tail fix, then chain arm, then re-judge tail-lit soundness; monitor-first per rung. (b) **fc_cond FIRST-WINS audit.** (c) **FLATDISP-5a** (untouched; the fz invariance/write-once/prologue machinery all exists in the lowerer, consumption is the net-new). (d) **SYM-VIS-M3.** (e) rt_dcap_pump rung (b) of s194 is CLOSED by (1). → `FINDING-2026-07-27i-CLAUDE-SN4-FLATDISP-CAPTURE-CALL-DISPLACEMENT-MINES-AND-ARBNO-ELEMENT-CLOBBER.md`.

## ⛔ LIVE CURSOR — s194 (2026-07-27): **FLATDISP-7 LANDED — JMP-ENTRY rbp PIN GATED PER-GRAPH, CENSUS NET 237→119 (−49.8%).** SCRIP `d95b1a98` + 3 artifact sweeps (benchmark/feature/demo). **WATERMARK m3 185/130 · m4 183/130 · DIVERGE=1 (W06_tab) — re-proven fresh at session start AND pre-commit, HELD EXACTLY.** HEADLINES: (1) ✅ **`emit_jmp_pin_rbp()` (emit.h, beside g_emit) = `flat_deep_arrival || flat_pat || flat_gen`** — the s193 xaf_deep single-reader discipline applied to the jmp-entry regime: every jmp-entry gate site in BOTH media (prologue save+seed: BINARY `hdr` + `xaf_jmp_hdr_x86` + 3 TEXT snprintf twins; determinate epilogues; role-3 wire-adopt) reads THIS, never the raw fields. **The flat_pat/flat_gen conjuncts are LOAD-BEARING, not conservatism**: a suspending γ RETAINS with rsp at the deep frontier and its record protocol reads the PINNED rbp by design — so an INVARIANT PAT$ blob (pure LIT body, classifies depth-static by kinds) keeps its pin, which is what makes 7 safe INDEPENDENT of 5a. 5a will make the conjunct fire less, never wrong — the s193 cursor's ordering (5a before 7) turned out unnecessary. (2) ✅ **Depth-static determinate epilogue arms (both media)** restore the pre-s90 rsp-relative form (`mov rdi,[rsp]` result reads, wire reads `[rsp+kt-24/-16]`, `add rsp,kt` = the ENTIRE teardown, zero rbp touch) — the form the s90 comment records as "true for SNOBOL4's determinate procs, FALSE for Icon", now selected per-graph, language-blind. New `xaf_ld64_rsp`/`xaf_addq_rsp` helpers do as-matching disp8/imm8 short forms (R10). (3) ✅ **Role-3 wire-adopt** (bb_save_restore.cpp) depth-static arm: header reads via rsp (runs immediately post-prologue, pre-carve, rsp==base in both regimes), caller rbp marshalled from the LIVE REGISTER (`mov rcx, rbp` — never saved, never clobbered). **Role-1/2 floaters correct UNCHANGED in both regimes**: unseeded ⇒ rbp still holds the caller's value ⇒ the snap-record restore is a same-value write. (4) ✅ **CENSUS: fibonacci/func_call/func_call_overhead/indirect_dispatch 24→1 net each** (residual = the wire-adopt marshal read, a register read not a frame ref; the 2 class-D are the floater restores), mixed_workload 49→36, roman 42→29, **twelve of 16 benchmarks now net ≤ 1**; pattern_bt/string_pattern 25 each UNCHANGED (PAT$ blob pins + fence boxes — the 5a population). Ratchet BASELINE 237→119. fibonacci 26→3 rbp TOTAL, fully accounted (2 prologue pairs + 2 epilogue quads + 4 wire-adopt + 2 class-D = the old 26; no hidden readers). (5) ✅ **Cross-language proof measured, not assumed**: Icon 4/0→4/0; SNOBOL4 bench-modes status column IDENTICAL per program baseline↔after (OK=8 FAIL=4 CRASH=4, all pre-existing, RT_OPT=-O0). (6) ⚠ **Prolog/Raku suites are NONDETERMINISTIC with the SAME binary** (measured both builds): Prolog FAIL↔SKIP churn 2/3/4↔58/59/60 = timeout-sensitivity; Raku flips 51/0↔50/1. **rk_subs: PRE-EXISTING 100% segv on BOTH builds** — baseline "passed" by consistently emitting NOTHING (empty×3 agree; no .ref so the suite falls to its run-vs-run arm); the emitted program also produces WRONG partial output ("0/hello " vs expected "14/hello raku/…") before crashing. FLATDISP-7 merely perturbs the flush race so the wrong bytes sometimes survive → the pre-existing breakage became VISIBLE as suite flake. Raku-ladder item, not a FLATDISP regression. (7) 📋 **Known cost, recorded not hidden**: +5 xa_flat medium-invisible WIP debt (101→106) — the new epilogue helpers are raw-byte in the legacy epilogue stream where x86() records would corrupt the byte-offset patch bookkeeping (`out_site = size()`); XA-FLAT-CONVERT retires the family wholesale. (8) 📋 **Carried forward from pruned cursors**: (s189) `ZC_FRAME` still holds dormant `ZC_FRAME_RBP`/`ZC_FRAME_R12` arms (`x86_zr`, `zeta_choices.h`, the `IF(ZC_FRAME != ZC_FRAME_RSP,…)` dances in bb_match_defer/release) — named follow-on, larger blast radius; (s186) 6 residual `.Lbynamefn` dup labels still block test_string/claws5/json artifact updates; beauty mode-4 termination unsettled (s184: 34.5M asm lines @280s, non-termination indicated NOT proven). **NEXT RUNGS:** (a) **FLATDISP-5a — frameless invariant PAT$ blobs** (`xa_pat_blob_invariant_n`, grep=0, NET-NEW): emit invariant pattern inline, no PAT$ proc, no DT_P round-trip — retires pattern_bt/string_pattern's 25s and shrinks the flat_pat conjunct's reach. (b) **rt_dcap_pump SIGSEGV** (`pattern_match.c:673`, blocks the 130, pre-existing since s191): `memcpy(copy, c->subj + e->saved_delta, len)` with `saved_delta`≈2³², dcap island only 24B, suspected field-offset disagreement between the `IR_MATCH_ASSIGN_COND` writer and the pump reader; `[ZHP] heap exhausted` is a secondary symptom. MONITOR-FIRST: 2-way monitor on `039_pat_any` or `041_pat_span`. → `FINDING-2026-07-27h-CLAUDE-SN4-FLATDISP-7-JMP-ENTRY-PIN-GATED-AND-RK-SUBS-PREEXISTING-SEGV.md`.

## ⛔ LIVE CURSOR — s193 (2026-07-27): **FLATDISP-5b/5c + FLATDISP-6 + ABORT-NODE + BRACKET-GATE LANDED. m3 185/130 · m4 183/130 · DIVERGE=1 (W06_tab) — FULL WATERMARK RECOVERY FROM s192 RED.** SCRIP `b3996516` (bracket gate) · `646e29ae` (feature sweep) · census gate `9edc982c`. **LON DIRECTIVE: "Have each BB allocate its RESULT value… Do it by one instruction, decrement RSP… Continue… until you hit a BRICK WALL and realize, oh I need a RBP stable base pointer… Several constructs we know we'll want RBP is for a STATEMENT, FUNCTION and ARBNO and FENCE1… Those are the only four."** HEADLINES: (1) ✅ **FLATDISP-5b: ROOT CAUSE WAS ARMING COVERAGE, NOT KIND LIST.** `emit_graph_has_deep_arrival()` was called only inside `emit_jmp_entry_for_proc`, but the gates reading `flat_deep_arrival` live on the *outer call-regime prologue arm* (xa_flat 382/383) — a path outer mains never armed, so they inherited cleared 0. Proven on 058: `main_α` emitted no rbp seed, then `n11_match_fence1_α` did `mov rsp, rbp` into the CRT caller's frame. Fix: classification moved to the `emit_chain` choke point — single writer, every graph (outer mains, PAT$ blobs, EVAL/CODE chains, rt pattern builds). (2) ✅ **FLATDISP-5c: BOTH-MEDIUM — `xaf_deep()` unifies all six outer-frame rbp gates.** `xaf_deep() = flat_deep_arrival || outer_nparams ≥ 1`. TEXT anchor-leave, TEXT prologue save+seed, TEXT epilogue reload, BINARY anchor-leave (the missing twin that caused m3-held/m4-broke in s192), BINARY epilogue success+fail reload — all six now read the SAME predicate. Latent bug fixed: TEXT gated the seed but stored `[rbp+16]/[rbp+24]` unconditionally (Icon param-0 bind). (3) ✅ **FLATDISP-6: deep list narrowed to the template-census rbp-reader set.** ALTERNATE/SEQUENCE/ARB/RETRY/RELEASE removed — comment-only in census, subsumed by HEAD (at the time). HEAD stayed because TT_ABORT lowered to a bare `IR_GOTO`, making abort-bearing statements classifier-invisible; dropping HEAD cost exactly 170/171 (m4 183→181→183, tripwire proven both directions). (4) ✅ **rbp CENSUS RATCHET GATE** (`test_gate_rbp_census_ratchet.sh`): compiler sweep (never artifacts), class-D scratch loads excluded (14 across 16 benchmarks), manual ratchet, baseline **NET=237**. First honest rbp instrument in this ladder (s184/s188 instruments were subset-only). (5) ✅ **ABORT-NODE:** TT_ABORT relowered from bare `IR_GOTO` to the real `IR_MATCH_ABORT` — dispatch case + drive arm (`op_off=-1`, no slot grant) + template linked in Makefile (it was never linked). Template's β→ω is *more* manual-faithful than the goto (SPITBOL manual Ch. 9: "backing up into ABORT kills the match"). With abort visible, HEAD left the deep list. (6) ✅ **BRACKET-GATE:** head/release/replace `+40` rbp save/restore bracket gated on `flat_deep_arrival` — the bracket exists for the ARBNO `zv()` borrow, dead in depth-static graphs. **Simple inline match statement now ZERO rbp — probe proven, output correct.** All three `.s` sweeps run per handoff step 4. **NEXT RUNGS:** (a) **FLATDISP-5a — frameless invariant PAT$ blobs** (`xa_pat_blob_invariant_n`): emit invariant pattern inline, no PAT$ proc, no DT_P round-trip — this will cut the 26-each in fibonacci/func_call/func_call_overhead/indirect_dispatch (all DEFINE-bearing, jmp-entry seeded unconditionally). (b) **FLATDISP-7 — gate jmp-entry seed** once blobs classify: the jmp-entry arm seeds unconditionally in both media (correct today because every PAT$ blob has fence/arbno/defer); once invariant blobs are frameless the arm can check per-graph. (c) **rt_dcap_pump SIGSEGV** (`pattern_match.c:673`) — blocking the 130 failing tests, pre-existing since s191. MONITOR-FIRST: 2-way monitor on `039_pat_any`. → `FINDING-2026-07-27g-CLAUDE-SN4-FLATDISP-5B5C-ABORT-NODE-BRACKET-GATE.md`.

---

## ↗ SN4-RTX MOVED OUT — s186 (2026-07-27)
The **SN4-RTX** ladder (runtime in optimized x86-64 asm) is **no longer in this file**. It lives in **`GOAL-SNOBOL4-RTX.md`**, with contract `ARCH-SNOBOL4-RTX.md`. It was split out so RTX can run CONCURRENTLY with the ζ-storage / value-spine work below.
⚠ **HOW IT WENT MISSING, recorded so the shape is recognizable next time:** the ladder was deleted from this file at `.github` `0c595551` (s176, 2026-07-26) by the shrink pass that terse-ified s175–s180. That pass removed **8 unchecked rungs (RTX-4-slice-2 … RTX-12)**, not just completed history, and left `ARCH-SNOBOL4-RTX.md` pointing at a `§SN4-RTX` section that no longer existed. Recovered verbatim from `950e6a9f`. **Pruning is for COMPLETED session state (RULES.md (c)); a live `- [ ]` ladder is not session state.**
⛔ **CONCURRENCY:** RTX phase-1 rungs touch ZERO templates by construction and are safe alongside this file's ladder. **RTX-11 (RTX-CC) and RTX-12 are NOT** — they edit `x86_asm.h` / templates and fire `.s` regen ×3. The crosscheck watermark is SHARED STATE between both ladders: re-prove it at session start.

---
## LIVE CURSOR — s202 ALT-FLAT (landed, SCRIP 0f31f7f7)
- ALT-FLAT LANDED (Lon design): zero-cell IR_MATCH_ALTERNATE, flat arms (fc_arm_member declines FORTH cells, registered AFTER admission walk — ORDER LOAD-BEARING, lower_snobol4.c ~1509), address dispatch via resume/next ZK_PTR_CODE cells (zls quad 1→2); alt_i + both cmp-chains + S10d pad machinery DELETED. Sigma stubs universal (emit.cpp gate = fc_sig presence).
- WATERMARK (re-proven this session, do NOT trust s200 numbers): baseline = after = 268/47 run, 267/46 compile, DIVERGE=2 (141_pat_eval_double_fn_arbno, W06_tab). Membership churn: 155_pat_cap_output_order FIXED both modes; 123_pat_regex_alt_class BROKE both modes.
- 123 ROOT CAUSE (measured, gdb): DEFER window `mov [rsp+256],rsp` / `mov rsp,[rsp+256]` is depth-sensitive; blob gamma RETAINS deep. Baseline delta 128 aliased a live rsp copy by luck; ALT-FLAT delta 144 (128 carve + 16 gamma-push, zero cells) reads a zeroed cell → rsp=0 → COND segv at rt_cap_top. Defer block byte-identical across builds. Repro /home/claude/w/f2.sno (+ f2.base.s/f2.s asm pair). NAMED FIX: r9 park-address anchor — defer sets r9=&park; blob prologue saves r9 beside rcx/rdx/rbp wire-saves (emitter site NOT YET LOCATED — greps for rcx/wire in emit.cpp failed; try xa/emit_text composition next); gamma reloads r9 before jmp; continuations `mov rsp,[r9]`. Nesting-safe, C-call-safe.
- NEXT RUNG (Lon directive "go get 'em"): MARKER-CAPTURE — SAVE.alpha pushes pend-stack MARK entry CARRYING start cursor (r12, depth-immune; frame slot dies); COND.alpha scans down to nearest open MARK, completes in place to {var,start,end}; SAVE.beta pops its mark; COND.beta re-opens completed→mark (backtrack exactness is THE risk). Deletes SAVE zls slot, COND cross-extent FRQ read, s195 fc_leaf_walk 1046-1049 special case. ONE-BOX merge becomes optional cosmetics afterward. Nested instantiations ride global LIFO + head dcap_mark.
- ALSO PENDING: arm-quad OVERLAY in zls_build (union max-not-sum — frame bytes currently SUM; the remaining half of "no waste"); rbp-pin-main structural rung retires the whole reach-over class (HEAD/RELEASE + defer window together); stored-pattern SEGV family (b3/c1/c2, /home/claude/w/) pre-existing, untouched.
- RET-dispatch verdict: superseded — address-over-index survived into the continuation cells; stack delivery unnecessary under flat arms.


