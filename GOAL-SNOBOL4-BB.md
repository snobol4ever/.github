# GOAL — SNOBOL4 on the shared BB spine
AUTHORS: Lon Jones Cherryholmes · Jeffrey Cooper M.D. · Claude   OPENED: 2026-07-03 · SN4-PAT + RE-LIGHT folded in 2026-07-06 · BB-OWNED-ζ set TOP PRIORITY 2026-07-06 · MAJOR REDUCTION 2026-07-08 (Claude Sonnet 5) — completed rungs deleted per RULES.md handoff step 1; verbatim history in git log and old handoffs under `.github/archive/` if ever needed.

## Session Setup

```bash
bash /home/claude/SCRIP/scripts/install_system_packages.sh   # libgc-dev + bison + flex + nasm etc — MUST run first; fresh containers lack libgc-dev and the build dies at core.h:8 gc/gc.h without it (s21 finding)
bash /home/claude/SCRIP/scripts/build_scrip.sh
bash /home/claude/SCRIP/scripts/build_spitbol_oracle.sh
```

Gate after setup:
```bash
bash /home/claude/SCRIP/scripts/test_smoke_snobol4.sh        # PASS=7
bash /home/claude/SCRIP/scripts/test_crosscheck_snobol4.sh   # see SUITE-ZERO watermark below
```

---

## ⛔ STANDING POINTER — read `GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` BEFORE writing/editing ANY `x86_asm.h` encoder or `xa_*`/`bb_*` template
A new encoder is NOT exempt from that doc's R2/R7/R9/R10 for being new: ONE `x86(...)` concatenation, consumed via `bb_emit_x86`, never a hand-written `IF(MEDIUM_TEXT,...)+IF(MEDIUM_BINARY,...)` pair (the "forbidden shape"). Mirrored in `PLAN.md`'s BB-CODEGEN DESIGN SET (session-start step 6).

---

## ⚠ SESSION STATE (Claude, 2026-07-10, s20 — 147 LANDED + DATATYPE DT_P FIXED; β-RESUME RUNG SPEC-COMPLETE) — SCRIP `cde0c38b`(DATATYPE DT_P)+`cefb4421`(sno_is_pattern_rhs defer fix), corpus no change, .github `6075157f`(FINDING β-resume) — PUSHED; **NEW WATERMARK m3 279/6, m4 278/5, DIVERGE=1(1017)** (147 green both modes; fail lists m3: expr_eval 124 140 141 143 145; m4: 124 140 143 145 1017)

**LANDED s20-a — 147 (`cefb4421`): sno_is_pattern_rhs SEQ/CAT now recognises TT_DEFER child as pattern-RHS.** Root: `OUTER = (*INNER ' ' *INNER)` degraded to DT_S (gdb: one space). Fix: TT_DEFER child under SEQ/CAT → return 1; bare `X = *V` stays EXPRESSION (oracle pin). SUPERSEDES old FINDING's three blob-re-entrancy suspects — root was pre-admission classification.

**LANDED s20-b — DATATYPE (`cde0c38b`): DT_P arm → "PATTERN".** Was falling through to "string". Oracle-confirmed. Zero regressions.

**s20 NEXT — β-RESUME RUNG (143+145), fully spec'd in `FINDING-2026-07-10-CLAUDE-SN4-BLOB-BETA-RESUME.md`.** Q1/Q2 answered: no callee esi dispatch exists; `is_generator` is caller-routing-only (dead end formally recorded). Caller diff ready: claim FR(16), stash fn+frame at γ, load+call(esi=1) at β, release+ω on exhaust (`bb_match_defer.cpp`, existing x86() vocab). Blob side: PAT$ graphs need explicit resumable-callable marker (NOT raw SUCCEED structure — EVAL shares it, esi audit owed) + `test esi,esi; jnz .Ltail_β` prologue via L(n). `IR_MATCH_DEFER` joins `ir_is_generator_kind` in same commit. `sno_pat_deterministic(TT_DEFER)` stays 1 this rung. READ THE FINDING before implementing.

**LANDED s19-a — test_stack (`0affec9e`): TT_NAME over DATA field ref + rt_nret_fix.** Two root causes for the s16 OPEN (i) FATAL: (1) `lower_snobol4.c` TT_NAME arm lacked a TT_FNC branch — `.sval(stk)` (name-operator over a DATA field accessor call) hit the sno_fatal. Fix: after the existing TT_IDX branch, intercept TT_FNC with a single arg where `rt_dat_field_of_any(fname)` confirms it is a known field name; build `IR_FIELD_VAR` with the object value as operand (same shape as the assignment-subject arm in the statement loop). (2) `rt_nret_fix` decoded DT_N via open-coded `slen==0`/`slen==1` branches, missing `slen==2` (NAMETRAP from `rt_field_var`). Fix: replace the two branches with a call to `rt_deref`, which handles all three DT_N forms uniformly. Both modes byte-identical to oracle. Feature .s regen: 38 files (layout change only, semantics unchanged).

**s19 RESIDUE / NEXT:** nested-blob-defer class (124/143/147) is the next highest-value rung — 8-line repro confirmed (`inner=('a'|'b')`, `outer=(*inner ' ' *inner)`, `'a b' POS(0) outer RPOS(0)` → SCRIP `no`, oracle `yes`), gdb installed. MONITOR-FIRST: run `scripts/test_monitor_2way_sync_step_bin.sh` on the 8-line repro to bracket first divergence before any gdb stab.

**LANDED s18-a — PATTERN OPERAND EDGES (`5bf79125`), the s17 NEXT rung, WIDENED to ALL primitives per Lon:** BREAK/BREAKX/ANY/NOTANY/SPAN (cset) + LEN/TAB/RTAB/POS/RPOS (int) with runtime args lower to arg-chain → `IR_COERCE_STRING`/`IR_COERCE_INTEGER` → operand edge into the primitive; chains HOIST to statement level (scx_t.pre[64] collected in the sno_pat_node walk, spliced between subject chain and IR_MATCH_HEAD, SEQ right-first walk + forward splice = left-to-right construction order) so SEMANTIC PIN 1 holds — probe p5 `LEN(1) $ V BREAK(V)` → `S:b:a` green. Literals bake unchanged (incl. cconst-folded once-assigned vars — probes MUST double-assign or they silently test the literal path; this bit s18's first matrix). TT_DEFER args ride the OLD element-placed path = match-time fetch = correct `*V` semantics (TAB/POS keep it; cset five + LEN exclude defer via the widened `sno_pat_supported`). `sno_pat_invariant` untouched (FZ freezes literal-only). Coercion helpers `rt_coerce_str_d`/`rt_coerce_int_d` (rt.c) carry oracle-pinned SPITBOL codes: null-cset 059/069/070/151/188, non-int 120/162/181/183/185, negative 121/163/182/184/186; str codes packed typecode|emptycode<<16. Templates: TAB-idiom `IF(op_sa>=0, mov …,FRQ(op_sa+8))` needle/count arms in break/breakx(×2 sites)/any/notany/span/len; two new boxes bb_coerce_string/bb_coerce_integer (bare-call per the bb_binop_concat_slot spine precedent). Emit: coerce drive/walk arms + explicit op_sa in the five cset match drive arms (g_emit is global — stale op_sa from prior nodes would have armed the slot arm spuriously) + chain-BFS ω-whitelist. `IR_COERCE_*` joined `ir_node_produces_value` (the LOWER-grant path; drive_value_slot aborts otherwise).

**LANDED s18-b — PRE-EXISTING BREAKX FIX (in `5bf79125`):** `IR_MATCH_BREAKX` was ABSENT from `ir_is_generator_kind` → capture backtrack-in (and SEQ resumable-leaf re-point) skipped BREAKX.β → the manual's flagship extend-on-rematch NEVER ran (literal probe p8 failed identically pre-rung, asm hand-trace proved the β block itself correct). Fix = add to generator-kind + exclude TT_BREAKX from `sno_pat_deterministic` (it yields >1 way; ARBNO v1 admission consistency). Pre-existing, bracketed by asm-trace not monitor (land mine was a missing enum case, statically visible).

**LANDED s18-c — PREDICATE OPERAND EDGES (`71989386`, Lon directive \"LT(x,y) → IR_VAR + IR_COERCE_NUMERIC ×2 + IR_LT; the INTEGER/REAL/STRING decision needs both operands\"):** numeric LT/LE/EQ/NE/GE/GT + lexical LEQ/LGE/LGT/LLE/LLT/LNE intercept at the TT_FNC arm (1–2 args; omitted arg = null) → `sx_pred_cmp`: argX chain → argY chain → two `IR_COERCE_NUMERIC` (operands[0]=self [1]=other — each validates SELF with its positional code, joins type with OTHER: either-real→both-real, strings parse int-then-real, null=0) or two permissive `IR_COERCE_STRING` (lexical; numerics stringify — oracle: LGT(10,9) FAILS lexically) → `IR_CMP_TEST` (ival=relop 0..5; rt_cmp_d sign; fail-jcc after `test eax,eax`: jne/je/jns/jg/jle/js; γ writes null-string to own slot). 24 positional codes oracle-pinned (numeric 101/102·109/110·111/112·118/119·147/148·149/150; lexical 122–133); first-position precedence (coerceX before coerceY). ⚠ KNOWN LIMITATION: the compile-time intercept SHADOWS a user DEFINE of these 12 names (corpus grep = 0 uses; same deviation class as the standing last-define-wins DEFINE rule) — lift = consult the defs[] scan before intercepting, small rung. `IR_CMP_TEST`/`IR_COERCE_NUMERIC` joined produces_value + the ω-whitelist beside BINOP_TEST.

**RULINGS RECORDED s18 (Lon, this session):** (1) FENCE(var)/ARBNO(var) take PATTERN-VALUED args = genuinely VARIANT structure → stay on the defer/recipe ride, OUTSIDE operand edges (gate already routes them there). (2) Strictly, protected pattern names (BAL/ARB/REM/FENCE/…) are variant-in-principle; ORACLE PIN: SPITBOL raises **ERROR 042 attempt to change value of protected variable** on assignment to ARB and to BAL — the compile-time name-folding is sound exactly BECAUSE of that protection; if any keyword ever unprotects them the fold breaks (standing caveat, no code change this rung).

**s18 FINDINGS/RESIDUE:** (i) test_string GREEN — the s16 OPEN include+2call bug is MOOT via the reroute exactly as s17 predicted; the underlying include statement-index misalignment (FZ counter/cconst/zls_group_mark consumers) is now LATENT with no red test — the monitor hunt s17 wanted is still owed if anyone cares, note kept. (ii) cross GREEN — GEM-0/GEM-1 unblocked; the SCAN-GEMINI ladder's \"fetch cset by name at runtime\" descriptions are dead, operand-edge+hoist supersedes (s17 already flagged bb_match_span_var's rt_nv_cstr as do-not-copy; that template is now unreachable from the live gate — dead-code sweep candidate). (iii) DESCR.slen is NOT reliably maintained by all writers (gdb: V=',' arrived DT_S slen=0) — VARVAL_fn already ignores it; rt_coerce_* tests NUL-termination, never slen; treat slen as advisory everywhere. (iv) TAB/POS literal-negative (e.g. `TAB(-1)` literal) still silently misbehaves vs oracle 184 — the coerce path fixes only runtime args; literal-arg range checks = small follow-on. (v) error-path m3 SEGV after `core_runtime_error` print persists (same class as s12's \"Error 5 then segfault\"); error-path cleanup rung still open. (vi) `test_smoke_sno_command_match.sh` line 29 hardcodes `$REPO/../corpus` and fails in the standard layout — script path bug, pre-existing. (vii) 5-stage doctrine stage-2 hoist DONE for the operand family; the TAB/POS `LEN(1) $ N TAB(N)` latent divergence s17 flagged is CLOSED by the same splice (defer-arg TAB keeps element placement by design).

**NEXT (7 remaining):** expr_eval/140/141 (DP-1/DP-7 deferred-call cluster) · 124/143/147 (nested-blob-defer FINDING file — 8-line repro confirmed, gdb installed, monitor-first bracket is the next step) · 145 (074/W07 blob β-resume) · 1017 (standing m4 diverge). Predicate follow-ons: user-DEFINE shadow lift; literal-arg predicate folding (GT(2,1) could compile-time fold).

---

## ⚠ SESSION STATE (Claude, 2026-07-10/s16 — B-RE: RUNTIME PATTERN CONSTRUCTION UN-RETIRED per Lon directive "any construct can contain a VARIANT part causing runtime construction versus compile-time PATTERN constant folding") — SCRIP `11c590ac`(triple: M3-FRAME-BYTES + MON-RE-M3 + B-RE-0/1/2) + `0f533e4f`(B-RE-3 generic recipes + 057 fix) + feature-.s regen commit, .github this commit, corpus untouched (bench/demo regen honest no-op) — ALL LOCAL, PUSH PENDING CREDENTIAL; **NEW WATERMARK m3 272/10, m4 271/7/4, DIVERGE=1(1017)** (test_case green BOTH modes incl. the full icase library — the first runtime-composed pattern in the tree; test_string moved m4 SKIP→FAIL = compiles now, blocked on the s16 OPEN bug below; fail lists otherwise byte-identical to s15); smokes sno 7/7, icon 12/12×2, prolog 5/5×3.

**LANDED s16-a — M3-FRAME-BYTES (in `11c590ac`):** the SNOBOL4 mode-3 proc-emit block (scrip.c ~1293) never harvested `g_last_flat_frame_bytes` → `rt_proc_set_frame_bytes` (its Icon-side sibling ~1385 does) — every SNOBOL4 proc frame alloca'd at the 4096 floor while re-lowered proc bodies address MAIN-graph-sized slot offsets (proc graphs re-lower the whole statement array; slots run past 23KB in beauty-scale programs). Silent OOB arena-slack writes under all zeta ports since forever; ZC_PORT_CSTACK's exact-size alloca turned them into main()-canary smashes (beauty drivers 48/51 red, all five ports A/B'd: only cstack detonates). Hunt: gdb `__stack_chk_fail` → canary-slot frame diff (4×DT_S+DT_P DESCR march) → NV_SET/table_set canary-poll bracketing → stepi-to-flip land mine `mov rax,[r12+0x5cf0]` inside proc_assign with r12=rsp+0x20 → mode-4 text confirm → sizing-vs-numbering split → the missing one-line harvest. Beauty suite 3→12 PASS drivers pre-B-RE (fence/assign/counter/trace); remaining beauty reds are downstream feature gaps, deprioritized per Lon ("beauty tests are too much — walk the rung ladder").

**LANDED s16-b — MON-RE-M3 (in `11c590ac`):** mode-3 monitor was STRUCTURALLY DARK — `emit_mon_label_tap` had ZERO callers, GVA writes bypass the NV value hooks, and optimizer `branch_chain` threads every IR_GOTO statement anchor away. All reinstated MONITOR_BIN-gated (byte-neutral otherwise): lower stamps `:stno` onto statement anchors (reads BOTH the ILIT and the include-path STRING attr forms — the parked accessor was right); emit's IR_GOTO drive arm fires the tap; `bc_stamped` protects stamped anchors from goto-threading; both m3 GVA-activation sites zero `n_gva_m3` under monitor so values route through the instrumented NV_SET path. Verified live: bracketed the s16 OPEN bug to its first divergence (below). Known residue: spl-vs-scr statement NUMBERING skews under `-INCLUDE` (spl counts include lines; harness alignment confounded) — a MON-RE follow-on rung, not blocking single-file hunts.

**LANDED s16-c — B-RE-0/1/2/3 (the un-retire, `11c590ac`+`0f533e4f`):** DT_P.p now points at a head `{fn, rcp}` (pattern_match.c DTP_t; producers `rt_gvar_assign_pat` + SNO$MKPAT wrap via `dtp_wrap_fn`; sole consumer `rt_defer_get_pat_fn` unwraps via `dtp_fn_of`). A VARIANT pattern part builds a RECIPE — nodes carry TREE KINDS VERBATIM (TT_QLIT/SEQ/ALT, cset family w/ runtime string, num family w/ runtime int, niladics, TT_ARBNO, FENCE(P) fn-form, both value captures w/ target name) — and the FIRST MATCH compiles it ONCE: recipe→tree_t rebuild→exported `sno_pat_tree_graph_rt` (the PAT$n 8-line shape, now public)→optimizer→slot-assign→`emit_chain` (bb_pat_build.cpp UN-ORPHANED into both build targets after being in-tree-but-unbuilt since retirement; its two legacy builders moved off the deleted `bb_build_flat` onto emit_chain). ONE FLAT BLOB per stored value — composition composes RECIPES, so it structurally cannot mint the open nested-blob-defer class; opaque-fn (recipe-less) composition REFUSES LOUDLY naming 124/143/147. Routing: sx_lower arms for TT_ALT (SNO$PBALT) + every primitive/capture (SNO$PBK/PBN/PB0/PBC generic makers); `str_concat_d` DT_P branch → pat_cat (blank-op pattern concat, type-directed at runtime); PATTMP$n rewrite at the unsupported-pattern fatal (root TT_FNC or any is_pattern_rhs) → hidden assign + TT_DEFER(var), riding the GREEN existing defer path. All 20 [B0] pat_* stubs now have bodies. 057 REGRESSION found+fixed en route: core.c seeds the NAMES FAIL/ARB/REM/SUCCEED/ABORT/BAL/FENCE as pat_*() VALUES at init — recipes made the first deferred use compile TT_FAIL and sno_pat_node lacked the arm; added TT_FAIL(→fail)/TT_SUCCEED(→succ)/TT_ABORT(→pat_seal) arms. Probes rc1–rc7 oracle-identical (variant ALT of builtin results; loop-grown CAT-of-ALT = the icase mechanism incl. null-string→epsilon start; BREAK(param) in DEFINE body single-call).

**⛔ s16 OPEN BUG (blocks test_string m3+m4 and cross; monitor-bracketed, 5-line repro):** `-INCLUDE`'d source + **TWO call sites** to a proc whose body does `BREAK(param) . capture` ⇒ the runtime cset arrives EMPTY (index('foobar','oba')/('xyz') → 7,7 instead of 2,0). Discriminators all proven: byte-identical INLINE source green; include+ONE call green; any-single-lib-line prefix (even `zzz = 1`) + include + two calls red; call ORDER irrelevant (first call already wrong ⇒ compile-time). Monitor first-divergence: step-2 spl `@1 END` vs scr `@1 CALL index` with the include stno-numbering skew — prime suspects are statement-INDEX consumers over the include-merged st[] (zls_group_mark's mstart windows / FZ write-counter / cconst), misindexing under include. Repro recipe (rebuild in /tmp): sE.sno = corpus/lib/string.sno lines 68-73 (the index fn) + any one extra statement; driver = -include + two `OUTPUT = index(...)` calls. NEXT: monitor with include-numbering normalized, or gdb the SNO$PBK arm's args[1] under the repro (empty-string arrival point brackets in one stop).

**s16 OTHER OPEN:** (i) test_stack = NAME over a DATA field ref `.sval(stk)` — sx TT_NAME needs a TT_FNC arm routing to `rt_field_var` (which already returns the correct NAMETRAP); small rung. (ii) cross additionally needs TT_CAPT_CURSOR (kind 44) in the sx arms — one more case in the s16-c pattern. (iii) recipe ARBNO/FENCE(P) arms exist in dtp_rcp_tree but have NO sx routing yet (TT_ARBNO/TT_FENCE in expression position still fatal) — deliberate, next B-RE rung with its own probes. (iv) BB-BRANCH AUDIT recorded for Lon: every C→BB transfer is a `call` (driver fn-ptr, m3_enter_with_rbx `call *`, rt_eval_run hand-asm call, all rt_call_proc_* / gen / resume); ONE sanctioned jmp = the coexpr pthread trampoline (fresh-stack entry, frame abandoned, alignment-deliberate); guard condition: tree builds -O0, so no compiler tail-jmps — re-audit if optimization ever lands; longjmp (ABRT_GUARD/driver ret_env) unwinds ACROSS ζ frames, sound under CSTACK's below-rsp-dead contract, no post-unwind ζ derefs found.

**s16 DISCLOSURES:** BB-CODEGEN DESIGN SET read at orientation = ARCH-ICON.md in full + GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md THE-RULES section only (R1–R13 + the g_emit FACT rule; the internal-labels/divvy-up tail unread — no template/encoder bodies were edited this session, zero x86_asm.h changes; edits were driver/lower/runtime/optimizer + one monitor-gated emit drive-arm, same class as the s14/s15 disclosed precedent — Lon to flag if the bar is higher). The `git add -A` on commit 1 initially swept all three chunks under a one-chunk message; amended to the accurate combined message before anything left the tree.

## ⚠ SESSION STATE (Claude, 2026-07-10, s15 — FZ-5 RE-PORT LANDED: once-assigned-invariant stored-pattern frozen-head inline at match sites) — SCRIP `3fcd5532`(FZ-5) + `f3a81a0f`(feature .s regen), corpus `d7dd78c5`(bench .s) + `6ac1a6ba`(demo .s), .github this commit — ALL LOCAL, PUSH PENDING CREDENTIAL (s14's own pending-push note is RESOLVED: fcb4e6c/52e1cad/04cc299 verified upstream, post-s14 Prolog sessions sit above them); **WATERMARK UNCHANGED m3 271/11, m4 270/6/6, DIVERGE=1(1017)** — fail lists byte-identical to s14; smoke sno 7/7, icon 12/12x2, prolog 5/5x3.

**LANDED s15-a — FZ-5 RE-PORT (`3fcd5532`): the FZ-5 open rung (line ~331) is CLOSED for the statement-level case.** Mechanism: `sno_fz_build_table` pre-pass beside cconst — own write counter (deliberately NOT cconst's: cconst's scan_indirect_target poisons every var referenced in a TT_SCAN subject, which would mark every matched-against pattern var as multiply-written; the FZ counter counts only real writes: plain assigns, splice subjects, captures incl `@`, SWAP/REVSWAP, DEFINE arg/local/fname saves) + program-unsafe bit (any indirect/unrecognized assignment subject; EVAL/CODE/CONVERT/INPUT/CLEAR call anywhere; KEYWORD and IDX subjects carved out as safe). `sno_pat_invariant` admits the literal-only SN4-PAT subset (folded csets, ILIT lengths, REM/ARB/FENCE, ARBNO/captures/SEQ/ALT of invariants) and EXCLUDES TT_DEFER/TT_FNC/runtime vars — so the fold cannot mint new instances of the open nested-blob class (124/143/147). Candidates (once-written + invariant) pre-collect their PAT$N; statement-level defer sites get a data-only IR_LIT_STRING operand naming the proc; `g_sno_in_patproc` suppresses marking inside pattern/expr proc bodies (broken class untouched); EVAL/CODE fragments hard-off. emit's IR_MATCH_DEFER drive arm resolves the operand (rt_proc_get_fn live fn in mode-3 — procs compile+register before main, scrip.c:1293<1310; `proc_<name>_α` label in mode-4) into bb_child_fn/lbl — consumed by bb_match_defer's dormant FZ-5b arm (survived GZ#5 unused; emit.h:479-480 fields were write-nowhere until now). **MEASURED: NO benchmark win** — pattern_bt 137.7s (was 144.0s), string_pattern 151.7s (was 147.3s), noise: the eliminated per-match name→fn hash fetch is dwarfed by the defer path's remaining per-match `rt_zls_alloc(65536)`+`rt_zls_release` pair and blob execution; the old FZ-5b 1.13×/2.81× wins were vs the RETIRED per-shape runtime-builder baseline. ⚠ DISCLOSED: (1) goto-before-assignment ordering hole mirrors the ORIGINAL FZ-5 gate verbatim (a match reached via goto before the single assignment executes sees the frozen head, not a null value; original shipped oracle-green with the identical hole; corpus idiom keeps it latent). (2) the emit.cpp drive-arm edit was made WITHOUT reading the BB-CODEGEN DESIGN SET (lower + dispatch-fill only; zero template/encoder bodies touched, templates byte-identical) — Lon to judge whether the set-read requirement covers drive arms.

**NEW RUNG — DEFER-ZLS (the cost FZ-5 exposed):** eliminate bb_match_defer's per-match rt_zls_alloc(65536)/rt_zls_release pair — candidates: ride MATCH_HEAD's statement-scope ZLS2 mark/release backstop (the frame already exists for the whole match), or a lazily-grown reusable arena. This is the real stored-pattern hot-loop rung; also still open: the pattern-tail attribution A/B (`--zeta-port=plain` vs CSTACK default flip) before blaming any single cause for the 600×-class benchmark tail.

**LANDED s14-a — v1-FENCE (`fcb4e6c`+`52e1cad`): ARBNO(FENCE(P)) rides v1 — THE SHARED-SLOT FRAME IS THE SEAL.** Closes 116/142/151. Answers the standing design question (which fail-target does a sealed element inside iteration N point at): NEITHER the statement fJ NOR a local retry — a fenced group is deterministic-from-outside (β≡ω), so its exhaust points where every v1 body element's fail already points: F. v1's "cannot re-choose completed iterations" property (the reason plain generators are BANNED from v1) is EXACTLY FENCE(P)'s commit semantics (manual Ch.19: alternatives in P visible only moving forward; backup passes through) — v2's COLLECTION exists to enable the resume the fence must PREVENT, so fenced bodies must NOT ride v2. Mechanism: `sno_pat_v1_fenced_ok` (all body elements deterministic OR FENCE(P)-fn with `sno_fence_inner_ok` inner; ≥1 fence) admits to v1 UNCHANGED; new `cx->pat_seal` parameterizes the spine's seal target (== pat_fail at statement level, == F inside an ARBNO body, set/restored in the TT_ARBNO arm) — retires the line-528 "seal jumps to statement fail" refusal reason and admits any-position fences. Inner-ok refuses (park, not fake): nested ARBNO, nested/bare FENCE (bare-FENCE backup = whole-match fail, a DIFFERENT target), captures-inside-fence.

**LANDED s14-b — ARBNO-EXHAUST CHASE (`04cc299`): capture-then-ARBNO resume stamp was clobbering the body-entry edge.** Minimal probe `'ab' ? POS(0) LEN(1) . V ARBNO(LEN(1)) RPOS(0)` — oracle V='a', SCRIP wrongly failed; discriminator: capture-then-ARB PASSED (ARB's ω is a true fail edge). Land mine read statically, no monitor needed: `sno_resume_ω_to`'s direct-repoint fallthrough hit G.ω — the REPURPOSED body-entry — so extension jumped into the left generator's β and the ARBNO never grew past its null yield. Fix = third chase arm (after ALT-s7 and COND): ARBNO's true leftward exhaust is F.ω (allocated at tail_idx+1, operands[0]==G). PRE-EXISTING (probe shape contains zero fence nodes — the s14-a diff cannot reach it); this is bracket-#4's named "mid-chain-generator capture" bug for the ARBNO case. Regression-guarded forever by new corpus tests 065/066 (refs oracle-minted, never hand-written).

**145 DIAGNOSIS COMPLETE (still red, now to ONE tracked blocker):** three stacked blockers — seal target (FIXED s14-a), capture-then-ARBNO (FIXED s14-b), whole-pattern-stored-in-blob β-resume (OPEN = the standing 074/W07 bracket: RPOS failing OUTSIDE the blob cannot resume the ARBNO INSIDE the returned blob). Probe ladder proving it: inline shape PASSES, leaf-stored (num) PASSES, whole-stored FAILS, oracle passes all three. 145 is now purely a 074/W07 client.

**s14 CORRECTIONS + BOUNDARIES (recorded, not faked):** (i) the s13-era note "151 must keep passing" was STALE — 151 was IN the s13 fail set (the live run rules); it now genuinely passes. (ii) test_case/stack/string trio blocker refined: fatal = tree kind 21 = TT_ALT reaching EXPRESSION lowering (not the pattern path) — still untriaged beyond that. (iii) expr_eval/140/141 = the deferred-`*fn()`-capture + NRETURN-target + recursive-`*expr` cluster (DP-1/DP-7 territory), multi-rung, not stabbed. (iv) ORIENTATION DEVIATION FOR AUDIT: the BB-CODEGEN DESIGN SET (ARCH-ICON.md, GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md) was NOT read this session — all three commits are lower-only (zero templates, zero encoders, zero x86_asm.h); if any reviewer judges the set applies to lower-only pattern-wiring work, re-audit against those docs. (v) handoff step-4 regens run: benchmark/feature/demo .s all "no changes" (the fixed shapes previously FATAL'd; the flipped tests live in crosscheck/, which the three scripts do not cover).

---

## ⚠ SESSION STATE ADDENDUM (Claude, 2026-07-10, s14-cont — post-e8f2bab triage: every remaining m3 fail now classed, one NEW probe-proven finding) — .github this commit; SCRIP/corpus unchanged since 04cc299/ba6d1c9 (regen scripts re-run at current head: zero .s diffs, honest no-op); watermark unchanged m3 271/11 m4 270/6/6 (282), DIVERGE=1(1017).

**NEW FINDING (own file, read it): `FINDING-2026-07-10-CLAUDE-SN4-NESTED-BLOB-DEFER-CALLOUT.md`** — 124/143/147 are ONE class: a stored blob whose own body DEFERs another stored pattern (blob-in-blob) mismatches; statement-level callouts all green (10-probe matrix, oracle-agreeing, minimal 8-line repro, one successful prediction). Supersedes this file's old FENCE-family grouping of those three. Next step is MONITOR-first (static budget honestly spent).

**REMAINING m3 FAILS, fully classed (nothing unexplained):** 124/143/147 → the nesting FINDING above · 145 → 074/W07 blob β-resume only (per 04cc299) · expr_eval/140/141 → DP-1 deferred-call-in-capture + DP-7 recursive defer · test_case → **LANDED s16** (B-RE runtime pattern composition); test_string → s16 OPEN include+2call bug (above); test_stack → NAME-over-field rung (s16 OTHER OPEN) — was: **runtime pattern composition** — `icase = icase (upr(letter) | lwr(letter))` builds a pattern VALUE from fn-call results at runtime, then `'Hello' icase('hello')` needs call-in-pattern-position dispatch on a runtime DT_P; its own rung (check `bb_pat_build.cpp` first) · cross → GEM-0/GEM-1 (ANY with runtime-variable arg, the SCAN-GEMINI ladder) · 1017 → the standing m4 diverge. **Stale-bracket corrections:** old #3 (SUBJECT PATTERN = REPL) is LANDED (lib/case.sno replacement statements execute; 062/063 green); 151 was red pre-session despite the "must keep passing" note, and is green now.

**AUDIT DISCLOSURE (Lon):** this session was lower-only (`lower_snobol4.c` edge/gate wiring; zero template/encoder/x86_asm.h edits), so the PLAN step-7 BB-CODEGEN DESIGN SET reads (ARCH-ICON / TEMPLATE-REVAMP) were deliberately skipped on that basis — flag if that call was wrong. Session context note: commits 52e1cad/04cc299/ba6d1c9/e8f2bab were verified by fresh gate + git inspection rather than session memory, per the computed-status doctrine.

## ⚠ SESSION STATE (Claude, 2026-07-10, s13 — 1011 DEFINE-IN-EXPRESSION LANDED + OFFICIAL-ORACLE PIVOT: untainted SPITBOL + CSNOBOL4 built, certified, benchmarked) — SCRIP `2f920212`(1011) + this doc + `scripts/build_official_oracles.sh` + `scripts/util_bench_snobol4_engines.sh`, ALL LOCAL, PUSH PENDING CREDENTIAL; **NEW WATERMARK m3 266/14, m4 265/5/10, DIVERGE=1(1017)** (1011 green both modes; fail lists byte-identical to s12 minus 1011); icon 12/12×2, prolog 5/5×3; benchmark suite **16/16 OK at TIMEOUT=120** (s12's 15/16 was the runner's own 30s timeout on eval_dynamic — full run ~93s, ref-correct).

**LANDED s13-a — 1011 DEFINE in expression position (`2f920212`):** `sno_prescan_expr` gains a DEFINE arm beside DATA/OPSYN — a literal-prototype `DEFINE('f(args)'[,'entry'])` in a statement-subject expression hoist-registers identically to the statement path (last-define-wins, the standing compile-time deviation), notes cconst names, and records the fname in a per-compile predef registry; `sx_lower`'s TT_FNC DEFINE fatal narrows to fold a REGISTERED call to the null-string literal (manual p219 — DEFINE returns null, never fails) while UNregistered positions (pattern/replacement fields, deferred exprs, EVAL/CODE fragments) stay loud fatals; the fragment guard now walks the whole statement tree and zeroes the registry. Statement-level probe proved arg-name==fname + entry-label redefine already oracle-correct — the expression gate was the only land mine.

**LANDED s13-b — OFFICIAL ORACLES (Lon: "ours is tainted" — CONFIRMED):** built + certified from upstream GitHub into `/home/claude/official/`; full procedure encoded in `scripts/build_official_oracles.sh` (READ ITS HEADER — it records the abandoned sbl-bootstrap path that generates SILENTLY WRONG tables). **TAINT RECORD:** (1) fork `snobol4ever/x64` sbl md5 `79cab92f…` ≠ official release `172b206b…`; the fork carries the deliberate `systm.c` patch documented in `build_spitbol_oracle.sh` (TIME() → CPU-time integer **ms** on CLOCK_PROCESS_CPUTIME_ID) whereas OFFICIAL `spitbol/x64` TIME() diffs are **nanosecond-scale** (its µs docstring is stale; ÷1e6 for ms — wall-calibrated); by WALL the fork is genuinely ~1.5–2.4× slower on most benches (fibonacci 199 vs 96ms, func_call 1020 vs 421, var_access 1569 vs 654). (2) fork `snobol4ever/csnobol4` = 2.3.3 base + FENCE(P)+STNO patches INCLUDING `v311.sil`, and carries a real engine defect — unanchored ANY skips alternate cursor positions (`test/any.sno` fails; invisible to `build_csnobol4_oracle.sh`, which builds `xsnobol4` directly and SKIPS the regression gate). Fork oracles stay for the monitor workflow (it depends on the patched TIME()); benchmarks and semantics arbitration use the officials. **CSNOBOL4 provenance chain:** official repo has no releases + git tree lacks generated tables + snobol4.org egress-blocked → two-stage bootstrap: fork kit (generated finals committed; `.2` staging + timestamp dance) → stage-0 `xsnobol4` (&ANCHOR=1 generators dodge its ANY bug) → official tree `make SNO="…/xsnobol4 -b"` (the `-b` is load-bearing: genc's stdout IS `snobol4.c2`) → official **144/144 regression green** incl. its own `any.sno` → **FIXED-POINT CERTIFIED**: the stage-1 binary regenerates all 11 generated files byte-identically (modulo generated-on/$Id headers) — stage-0 provably left no fingerprint.

**BENCHMARKS s13 (16/16 OK on every engine; self-ms, same .sno + .ref; ÷ratios vs official SPITBOL; SCRIP = m4, `-O0 -g` runtime build):**
| bench | SCRIP m4 | off-SPITBOL 4.0f | off-CSNOBOL4 2.3.4d | fork-sbl (CPU-ms) | SCRIP÷off-sbl |
|---|---:|---:|---:|---:|---:|
| arith_loop | 605 | 23.1 | 134.3 | 47 | 26× |
| eval_dynamic | 92665 | 376.6 | 1281.5 | 437 | 246× |
| eval_fixed | 2169 | 260.0 | 720.8 | 272 | 8.3× |
| fibonacci | 2129 | 90.6 | 539.0 | 193 | 23× |
| func_call | 7575 | 410.4 | 2452.0 | 1002 | 18× |
| func_call_overhead | 7570 | 414.5 | 2469.7 | 991 | 18× |
| mixed_workload | 3890 | 98.6 | 474.6 | 182 | 39× |
| op_dispatch | 1253 | 67.2 | 380.0 | 129 | 19× |
| pattern_bt | 22405 | 507.9 | 570.6 | 518 | 44× |
| roman | 3304 | 121.8 | 1065.6 | 168 | 27× |
| string_concat | 867 | 156.1 | 321.0 | 142 | 5.6× |
| string_manip | 11187 | 375.9 | 1718.4 | 691 | 30× |
| string_pattern | 27255 | 388.9 | 1869.0 | 717 | 70× |
| table_access | 4400 | 192.9 | 2639.8 | 361 | 23× |
| var_access | 6079 | 643.3 | 3733.8 | 1551 | 9.4× |

**⚠ CORRECTION — the standing "pattern_bt 0.77×, SCRIP beats SPITBOL" claim DOES NOT REPRODUCE** (44× behind official, ~43× by wall behind the fork; same source, same counts, ref-correct output). Either a real regression since that measurement or a different build config then; the `-O0` runtime is a plausible-but-UNPROVEN factor. Older cross-engine numbers in `corpus/BENCHMARKS.md` (SPITBOL 480 / CSNOBOL4 580 on pattern_bt) are consistent with TODAY'S officials (508/571) — the stale claim is the SCRIP side. **Rerun protocol:** `scripts/build_official_oracles.sh` once, then `scripts/util_bench_snobol4_engines.sh <label> <timeout> <divisor> <cmd…>` (divisors: official sbl 1000000, fork sbl 1, csnobol4 1) and `scripts/test_bench_snobol4_modes.sh` with `TIMEOUT=120` for SCRIP.

---

## ⚠ SESSION STATE (Claude, 2026-07-09, s12 — NRETURN LANDED + BENCHMARKS 15/16 + indirect_dispatch CORPUS FIX) — SCRIP `1076fd8d`(NRETURN) + `eb9279bb`(feature .s), corpus `fdefe421`(APPLY fix + minted .ref) + `eaef9330`/`ffbe72d0`/`72d663c2`(bench .s) + `44c33f18`(demo .s), REBASED onto the parallel 2026-07-09 upstream line (Raku-restore + Icon fixes + BB-FIXUP cluster, base `eb4bb6ff`; the feature-.s regen commit conflicted in-rebase, was SKIPPED per the honest-artifact design, and regenerated fresh on the merged tree) and PUSHED to origin this handoff; gate RE-PROVEN watermark-identical on the merged tree; **NEW WATERMARK m3 265/15, m4 264/5/11, DIVERGE=1(1017)** (+2/+2: 1013 m4 SKIP→PASS, 213 m3+m4 →PASS); fail lists byte-identical to s11 minus the two fixes; oracle (sbl) passes both refs 3/3 and 5/5.

**LANDED s12-a — benchmark suite 15/16 OK, 0 FAIL:** eval_dynamic double-confirmed timeout-only (full run ms:97691, output == ref; EVR unchanged). indirect_dispatch was NEVER oracle-valid — sbl rejects `R = $FN(X)` with ERROR 022 (unary `$` binds the CALL result, manual p212: indirect call is APPLY); SCRIP already agreed with the oracle. Source fixed to `APPLY(FN, X)`, .ref minted (`6`), green both modes.

**LANDED s12-b — NRETURN (bracket items 1013 + 213/005):** manual p133/p192 contract (result is a NAME via unary `.`; value-read derefs; `F() = v` assigns through). Mechanism: NRETURN label landing = marker chain (IR_LIT_STRING → IR_CALL SNO$NRET → exitnd) setting rt_g_ret_by_name; general TT_FNC assignment-subject arm (was a GZ#5 fatal) mirrors the TT_INDIRECT shape with a SNO$WANTNM pre-marker, reusing IR_ASSIGN_VAR store-through-name; rt.c leaf sinks (descr non-dyn tail / named / direct) consume via rt_nret_fix — entry saves+clears want flag, tail restores (nesting-safe through calls inside args), deref DT_N (NAMEVAL→NV_GET_fn, NAMEPTR→*ptr) BEFORE rt_name_restore. RESIDUALS: conditional-assignment target `PAT . F()` (manual p133 second context) NOT wired; NRETURN-of-non-name falls to the assign sink error, no dedicated message; monitor kw_rtntype still reports RETURN for NRETURN returns.

**FRESH BRACKET (s12 re-derive, per s11 NEXT; 1011 LANDED s13 and deleted here per handoff step 1):** 140 = expr tree-kind 42 (s13 triage: real path `crosscheck/patterns/`, ref `stk=B`, shape = EVAL-built pattern + deferred `*inner(...)` call capture target + NRETURN name-return) · 141 + test_string = SN4-PAT pattern shapes · test_case = expr tree-kind 21 · test_stack = name-operator form — all GZ#5 lowering-subset fatals; include-trio blocker confirmed NOT OPSYN. Also noted: `scripts/test_smoke_all_frontend_backend_matrix.sh` is broken pre-existing (bash syntax error line 50, unexpected EOF); error-path oddities on invalid `$FN(X)` source: m3 segfaults after printing Error 5, and runtime error says "statement 0".

---

## ⚠ SESSION STATE (Claude, 2026-07-09, s11 — 1015 OPSYN LANDED + ZETA ON THE C STACK LANDED AND MADE DEFAULT) — SCRIP `d6a9f332`(OPSYN) + `8dcf8afa`(feature .s) + `879a0d37`(CSTACK), REBASED onto upstream `8d5516ed` (the parallel 2026-07-09 ζ-infra + PL-RESTORE line: --zeta selector bake kept ABOVE the new core_lib_init line per its own ordering comment; rt_zeta_cstack coexists with rt_zeta_set_mode in zeta_alloc.c), corpus `0cb71fa9`+`dee5a2bf`+`eba1ab9d` (benchmark/demo/prolog-bench regens), all LOCAL, PUSH PENDING CREDENTIAL; **NEW WATERMARK m3 263/17, m4 262/6/12, DIVERGE=1(1017)** (1015 green both modes); fail lists byte-identical otherwise; watermark proven at default CSTACK AND SCRIP_ZETA_PORT=4 AND fallback =3; icon smoke 12/12×2; prolog rung suite 109/115 ×3 on the rebased tree (upstream PL-RESTORE ladder passing under the CSTACK default — the alloca proc frames + anchor proven against the full restored Prolog surface); raku 51/51, rebus 4/4, snocone 4/8 == INLINE A/B (pre-existing), polyglot 2/2×2; 10 standing gates PASS; medium_invisible informational xa_flat 29→30 (the new xaf_ fragment cluster — that file's own conversion rung); ARBNO-exhaust micro PLAIN 101ms / INLINE 102ms / CSTACK 100ms = parity.

**LANDED s11-a — 1015 OPSYN m4 diverge FIXED (`d6a9f332`, branch merged to main):** the parked DT_N/NV_name_from_ptr hypothesis was FALSIFIED by the prescribed gdb bracket — `oldname` arrived intact (DT_N slen=0 s="DUPL"); the land mine was downstream: **the m4 binary never ran `core_lib_init()`** (core.c:1613 — the master register_fn block: GT/LT/DUPL/SIZE/OPSYN/ARG/LOCAL/… into `_func_buckets`; m3 driver calls it at scrip.c:589; the sibling m4 emission path at :1176 emits it; the SNOBOL4-family m4 main path (~:1005) emitted neither), so `register_fn_alias("@","DUPL")` found old_entry=NULL → alias stored fn=NULL → `core_call_registered_fn("@")` returned 0. Fix = ONE emitted line: `call core_lib_init@PLT` before proc_startup. The empty-buckets asymmetry was invisible until OPSYN made the core fn table load-bearing for builtins (big-switch dispatch needs no registration). 1017's ARG/LOCAL diverge did NOT move (they registered but the divergence is failure-STYLE, per its standing record).

**LANDED s11-b — ZETA ON THE C STACK, DEFAULT (`879a0d37`; Lon: "implement all ZETA storage for SNOBOL4 on the C stack using RBP/RSP. Then make it the default."):** `ZC_PORT_CSTACK=4` — the INLINE protocol with the cursor cell RETIRED: **the ZLS2 cursor IS rsp** (everything below rsp is dead by the platform's own contract → C calls, signals, and libgc's conservative stack scan compose for free; the 512MB arena reserve is never touched under the default). FOUR SLICES, each gdb-bracketed against its own crash: **(A)** hook α-BUMP `sub rsp,k16`+prev-chain (16-rounded, ambient call alignment preserved), ω-RELEASE unchain+`lea rsp,[block+k16]`; statement backstop = ONE mov each — **the release arm must CLOSE the caller's open alignment-dance window, restore to mark, and RE-OPEN at the new position** (bare mov mid-window → the following `rt_dcap_end_*` call pushed its return address onto live stack; 038 m4 segfault). **(B)** graph entry/exit ANCHOR (xa_flat, frame-active arms only — grants and backstop are FRQ-keyed so rsp never moves frameless; innermost wrapper `push align-save; mov save,rsp; sub rsp,8` / restore-FIRST in both epilogue halves): a blob's granted construct γ-succeeds, hands its frame down, and RETURNS — **cross-unit reclamation is impossible on the C stack**, so every C-callable emitted unit restores entry-rsp at its own ret (075 crash: pop/ret at bumped rsp, r12=0, pc=0x10000). **(C)** C-side exact brackets → `alloca`: 4 proc-frame sites (rt_call_proc_descr/named/direct/sl) + the EVAL/CODE fragment frame, selected by `rt_zeta_cstack()` (zeta_alloc.c, runtime twin of x86_port_mode; disciplines independent → env mismatch safe) — **the s10 HONEST-SCOPE (a) 64KB-per-crossing fragment leak CLOSES**. **(D)** **rbx=GVA self-load at graph entry** from exported `g_gva_base` (set in `gva_register`, the one seam both modes cross): the rbx-flows-through-C assumption was -O0 codegen LUCK — slice C's alloca made GCC's stack-clash probe use rbx (`div rbx`), callee-saved at the C boundary but trashed exactly while `p->fn` runs (083: fault in the BB arena on `mov rax,[rbx+0x10]`); this retires the whole documented rbx-poison trap class for graph entries.

**DELIBERATE BOUNDARIES s11 (recorded, not faked):** (i) the memoized MAIN frame (rt_frame, rt.c:87) stays on the arena — allocated once, never freed, shared across driver re-entries (args pre-write scrip.c:1301, re-entry :1379); it participates in NO alloc/free discipline; moving it is a mapped follow-on (emitted sites :1010/:1180, driver sites :1290-1302/:1307-1379). (ii) suspended generator activations (rt_proc_call_gen*, the s6 long-lived class) stay on the heap provider BY CONSTRUCTION — a block that must survive a C return cannot ride the C stack; this is the flavor's inherited structural limit, stated in zeta_choices.h. (iii) fragment ping-pong label loops now nest C-stack (64KB alloca per crossing, ~128 levels on the 8MB stack) instead of leaking heap — same frame-recycling rung as before, different failure mode (loud overflow vs silent growth). (iv) SCRIP_ZETA_PORT=3 remains the proven INLINE arena flavor (fallback verified watermark-exact); =2 ALLOC keeps poison/telemetry/LIFO-abort proving.

**NEXT BRACKET for a following session (from the s11 fail ladder, unchanged fail-sets):** cluster (A) is now 1011/1013 + 140/141 + 213 (1015 DONE — deleted from the brackets below); the include-library trio (test_case/stack/string) did NOT unblock on OPSYN alone — re-derive their actual blocker from a fresh run before assuming.

---

## ⚠ SESSION STATE (Claude, 2026-07-08, s10 — EVAL(string) + CODE(string) LANDED, directive lifted) — SCRIP + corpus committed LOCALLY, PUSH PENDING CREDENTIAL; **NEW WATERMARK m3 262/18, m4 261/6/13, DIVERGE=1(1017)** (+4/+4: four new rung10 EVAL/CODE tests, all green both modes); fail lists byte-identical to s9 = zero regressions; 8 standing gates green; Icon 12/12×2, Prolog 5/5×3.

**LANDED s10 (Lon: "Get EVAL(string) and CODE(string) working. We're ready now since we have optimized BB-owned memory management"):** (1) **EVAL(<string>)** — the by_name_dispatch DT_S bomb replaced with a route to the canonical `EVAL_fn` (pattern_match.c: numeric-first per manual rule 1, then `eval_string_transient`'s parse→lower→optimize→slot-assign→emit_chain→run-on-fresh-frame path, cache retained). (2) **`lower_snobol4(prog)` implemented** (was itself a directive bomb): the runtime fragment entry = `sno_lower_fragment_at(prog, entry_idx)` — same statement array, entered at any index (the entry-label-DEFINE shape), now running **optimizer_run + ir_drive_slot_assign itself** (the driver's passes don't cover runtime graphs) with DEFINE-in-fragment a loud fatal. (3) **CODE(string)** — `code()` rewritten to SEGMENT-PER-LABEL: one graph+chain per fragment label (α = the label), each registered in a new runtime label registry (`rt_label_set_fn`); DT_C carries segment-0's fn; by-name "CODE" arm added (core's `register_fn` table is a separate dispatch that IR_CALL never consults — m4 said "Error 5 undefined function" until this landed). (4) **`IR_GOTO_DEFERRED`** (named per Lon; renamed from the in-tree IR_GOTO_DYN precedent, template FILE still bb_goto_dyn.cpp to avoid Makefile churn — rename on request) + revived template per R2/R4/R5 (the bb_match_replace marshal idiom): unknown-label and `$X`-indirect gotos lower to it; α marshals `rt_goto_transfer(name)`, γ wired to the graph's exit = the TERMINATION CASCADE (gotos never resume their source, so a returned transferee means END; the nest of clean returns exits through the driver). Resolution order: `$X` deref-recurse → END(return) → fragment registry (fragments OVERRIDE main labels per manual) → `LBL__<name>` pseudo-proc → DT_C variable (the lexer folds `:<C>` onto the plain-name form — label/variable conflation noted) → fault "transfer to undefined label". (5) **LBL__ pseudo-procs**: when the program uses CODE (`sno_scan_code_use`, the &STNO use-gate pattern), every main label becomes proc-table entry `LBL__<name>` (same statements, entry at that label) flowing through the EXISTING proc registration in BOTH mode drivers untouched — zero driver edits, m4 startup registration inherited. (6) Two one-line emitter fixes the monitor-free gdb hunt found: fragment emission must set `g_emit_cfg` to the fragment graph (the old cfg pointed at the ir_delete_all'd main graph → segfault walking freed IR) and set `g_frame_active=1` (frame isolation — fragments were sharing the caller's r12); plus `IR_GOTO_DEFERRED` added to emit's blanket sval-promotion whitelist (the second dispatch function's blanket CLOBBERED the promoted name to NULL right before the template ran — the "L"-became-"" bug).

**VERIFIED s10:** four new corpus tests `rung10/1019_eval_string / 1020_code_label_transfer / 1021_code_direct_goto / 1022_eval_fail` — oracle-refs, PASS m3+m4 (the 1020 shape is the manual's own flagship: main→fragment label, fragment loops statically, fragment gotos back to main's DONE). Crosscheck fail-lists byte-identical to s9 (the use-gate = zero perturbation for the 276 non-CODE programs; benchmark regen ZERO diffs). Feature regen touched 4 ancient rung10 .s = flat_α-era STALE CATCH-UP (verified by diff: prefix/preamble modernization, zero occurrences of the new machinery, none of those programs use CODE). Demo regen: roman.s — **roman was a pre-session HARD FATAL** ("goto to unknown label: ROMAN_END", proven by stash-rebuild-run round-trip) caused by a pre-existing PARSE error eating the statement that carries that label; it now compiles+runs and diverges from ref = strict progression, remaining blocker is the parser gap, not EVAL/CODE.

**LANDED s10-b (`2deca364`):** `sno_lower_fragment_at` recycles the zls compile-metadata tables (`zls_reset()`) per fragment compile — the process-global za[1024] geometry cap (zeta_storage.c, sized for the driver's one-compile-per-process life) killed the 24th runtime EVAL; every prior graph is sealed before any fragment lowers (both modes), so its entries are dead weight. **eval_dynamic benchmark ALIVE again** (was dead behind the directive bomb, so the recorded 176× figure was from an older era): 1M unique dynamic EVALs complete, ref-correct, **94.8s vs oracle 1.26s ≈ 75×** — the EVR rung's fresh baseline; per-eval cost is the full parse→lower→optimize→slot→emit→seal pipeline (~94µs), cache misses 100% on unique strings by design of the benchmark.

**[s10-c PARKED OPSYN — RESOLVED s11-a, entry above; branch merged to main.]**

**HONEST SCOPE s10:** (a) each dynamic crossing allocates one never-freed 64KB GC-visible frame and nests one C-stack level — fine for manual-shaped programs (a handful of crossings), a real rung (frame-recycling tail-transfer) before label-ping-pong loops; (b) `&ERRTEXT` not populated on EVAL/CODE failure (fragment parser is stderr-noisy while correctly failing — stdout clean); (c) DEFINE inside a fragment = loud fatal; (d) fragment graphs skip the &STNO/&STCOUNT statement hook; (e) computed `$(expr)` gotos still fatal (name-based `$X` works — 213's indirect-goto sub-tests now pass, 213 overall still fails at its NRETURN sub-test); (f) a user DEFINE named `LBL__X` would collide with the pseudo-proc namespace.

## ⚠ SESSION STATE (Claude, 2026-07-08, s9 — &STNO/&STCOUNT/&LASTNO LANDED) — SCRIP `c03d0891` (local; PUSH PENDING CREDENTIAL); **watermark m3 258/18, m4 257/6/13, DIVERGE=1(1017)** (082_keyword_stcount +1 both modes); zero regressions vs s8; standing gates untouched (change is runtime + use-gated lower post-pass, no template/x86_asm.h edits).

**LANDED s9:** Statement-counter keywords per SPITBOL manual (p195: `&STNO`=integer statement number of the CURRENT statement being executed; `&LASTNO`=previous statement executed; p158: `&STCOUNT`=running tally, incremented as each statement begins). Three files: (1) `keywords.c` — `g_stno`/`g_stcount`/`g_lastno` globals, three `kw_read` cases, and `rt_stmt_enter(long stno)` (`g_lastno=g_stno; g_stno=stno; g_stcount++`). (2) `by_name_dispatch.c` — `SNO$STMT` dispatch arm calling `rt_stmt_enter`, registered in the known-builtin list. (3) `lower_snobol4.c` — `sno_scan_stmtkw` sets a file-static `g_sno_uses_stmtkw` iff the program references any of the three keywords; when set, a post-pass in `sno_build_graph` (after the per-statement switch, before `free(anchor)`) inserts `IR_CALL SNO$STMT(i+1)` between each non-DEFINE `anchor[i]` and its body via `body=anchor[i]->γ.node; hook.γ=body; anchor.γ=hook`. **Use-gated => byte-zero perturbation for the 256 programs that don't read these keywords** (proven: crosscheck moved exactly +1/+1 with no other cell changing). Verified vs oracle on the sequential probe (STNO=3/STCOUNT=4/LASTNO=4) AND a loop probe that separates STNO from STCOUNT (STNO=5/STCOUNT=11/LASTNO=6, exact) — so the source-number-vs-execution-tally distinction is correct, not just the trivial case.

**s9 SCOPE/CAVEAT (honest):** statement numbering is `i+1` over the MAIN graph's non-DEFINE statement array — correct for straight-line/looping top-level programs (the only crosscheck consumer, 082). A program that reads `&STNO` from INSIDE a DEFINE'd function body would report the function-body-local index, not the global source line — no current test exercises that, flagged not faked. `&STLIMIT` still returns a fixed -1 (unlimited) in `kw_read`; the manual's "under negative STLIMIT, STCOUNT may not be incremented" nuance is NOT modeled (we always increment) — invisible to every current test, deferred.

## ⚠ SESSION STATE (Claude, 2026-07-08, s8 — GROUP-TRANSPARENT SEQ + REPL SPLICE LANDED) — SCRIP `8eafb524`+`c2034ded`+`190cba1f`, corpus `33beb3a2` unchanged (feature .s regen'd); **prior watermark m3 257/19, m4 256/7/13, DIVERGE=1(1017)** (word3/062/063/wordcount +2 green); standing gates green.

**LANDED s8 (in order):** (1) **GROUP-TRANSPARENT SEQ** (`8eafb524`): concatenation is associative, so a parenthesized fence-free group must not be an opaque element — the old 2-way TT_SEQ branch took the right construct's resume surface as its FIRST-ALLOCATED node, which for a right-nested group is its rightmost leaf, leaving the group's leftmost fail edge stuck at HEAD (`'AB CD' ? ARB . B (' ' 'C')` "succeeded" at the slid anchor with B=''; oracle B='AB'). Fix: `sno_seq_flatten_pat` recursively flattens every nested TT_SEQ into ONE element list so the single pairwise loop wires every seam uniformly (fences inside plain groups now also seal at their true spine position); `sno_seq_has_fence` retired. Closed word3 both modes. (2) **REPL SPLICE — doctrine stages 4/5** (`c2034ded`): `SUBJECT PAT = REPL` now lowers instead of fataling. New `IR_MATCH_REPLACE` (α-only splice) + `bb_match_replace.cpp` + `rt_match_replace` (builds prefix+repl+suffix, `NV_SET_fn` by name — GVA-forwarding-correct). `IR_MATCH_RELEASE` stashes the end cursor at head+24 when the statement carries a repl (`dval=1`, set by `sno_lower_match`), flushing captures first per manual Ch.6 "before replacement"; the repl expression chain runs BETWEEN release and the splice so a repl expr nesting its own match cannot stale r13/r14/r15. head+24 pad reclassified in `zeta_storage.c` (was "unused"). **Slice 1 scope:** plain-variable subject lvalue only — indirect/subscript splice targets are a loud `sno_fatal`, pending. Closed 062/063/wordcount + 2 dependents. **BYTE-NEUTRAL for match-only statements** (the `dval` gate keeps RELEASE identical) — proven by ZERO artifact drift on the benchmark/feature/demo regen; only feature .s that use replacement changed (`190cba1f`).

[s8's own "NEXT" fail-ladder pointer deleted here, 2026-07-10 handoff step 1 — fully superseded by s9 through s14-cont; see the addendum atop this file for the current classification of every remaining fail.]



**LANDED s7 (in order):** (1) **ZETA-INLINE first slice + DEFAULT RULING** (`c701b818`): `ZC_PORT_INLINE`(3) — BUMP/RELEASE + statement backstop as raw arithmetic on the exported `g_zls2_cur` cell at the port hook (zero C calls, zero template edits; cursor stays a memory cell, NOT r12 — the documented sibling-[r12+off] wall; register-cursor end state still waits on activation-local offsets); the compiled default. `SCRIP_ZETA_PORT=2` = the ALLOC proving flavor (poison/telemetry/LIFO aborts), overflow under INLINE = the unmapped space below the NORESERVE arena. (2) **RULING: ONLY α AND ω PARTICIPATE** (`bee987cd`, Lon: "beta will do a LOAD R12 [was wrong] — R12 will already be adjusted from the omega that landed there") — ZLS2_RESTORE retired at the authority (zls2_geom never grants it), both hook flavors' β arms deleted. **PERF (adversarial ARBNO-exhaust micro, 100k statement loop): ALLOC 2150ms → 534ms (the β restore WAS ALLOC's dominant cost), INLINE 419 → 335ms = PLAIN parity (341ms).** pattern_bt outputs identical, ~4% faster. (3) **CAPTURE CORRECTNESS** (`fbd5df08`): the dark `rt_dcap` commit-ring WIRED (begin at rt_match_enter · end_ok at IR_MATCH_RELEASE α · end_fail at head L(1) · depth-marked for EVAL nesting · is_imm honored — conditional `.` now commits ONLY at overall success per manual Ch.6, `$` immediate) killing the per-trial capture spam (ARB . OUTPUT printed '', 'U', 'UN', 'UNT'); PLUS the **ALT-EXHAUST CHASE** in `sno_resume_ω_to` — an ALTERNATE's leftward exhaust is the trailing T-join's γ, not the first-allocated save's dead ω, so capture-then-alternation matches (`'A' ARB . V ('B'|'C')`) wrongly FAILED outright — word1's root. `SCRIP_DCAP_TRACE=1` traces the ring. **REVERTED en route:** unpend-on-COND-β (ALT's try-next-alternative edge routes through that β and killed live captures — the residual dead-branch overcommit is accepted this slice; a correct discard needs LOWER to distinguish backtrack-past from alternative-switch; `rt_cap_unpend` stays in the runtime for that wiring).

**NEXT (fail-ladder, 23 m3 / 8 m4):** [SUPERSEDED by the s8 SESSION STATE ladder above — word3/wordcount closed, cross folded into the pattern family.]

**LANDED cont.-6 (in order):** (1) **ZC_PORT default PLAIN→ALLOC** (`9e37f154`) — the pending ruling, ruled: α/β/ω carry the ZLS2 bump/restore/release by compiled default; `SCRIP_ZETA_PORT=0` = escape hatch; artifact regen honest (pattern_bt.s +6, 35 feature .s, demo unchanged). (2) **GLOBAL ZETA ABANDONED** (`88510a7d`, Lon: "I want it gone NOW") — the immortal cached 64KB `g_main_frame` + all four strict-LIFO proc-frame sites (rt.c) moved OFF the ZLS-v1 header-chain ONTO the ZLS2 down-arena: `rt_frame()` = the arena's base frame; proc calls push at call / `release_to(fb+K)` at return (K 16-rounded both sides); recursion literally stacks ζ frames (FACT(10) probe: ten nested push/release pairs). Runtime-only — benchmark .s regen proved zero diffs. (3) **TWO-CLASS TAXONOMY** (`274e7752`, Lon ruling): control-flow-lifetime ζ = ZLS2 down-arena (the stack, r12-destined); LONG-LIVED ζ (persists data state, freed independent of control flow) = the HEAP — `ZC_ALLOC` default BUMP_LIFO→MALLOC, so v1 `rt_zls_alloc/release` IS the heap provider (malloc+GC roots+per-block free, the arm cont.-5b already proved watermark-EXACT+ASan-clean); the v1 up-arena is retired from the default build (never mmapped). **Zero globals remain in the ζ system: two providers, both lifetime-classed.**

**EMPIRICAL FINDING cont.-6 (verified, not inferred):** the surviving heap-class citizens are SNOBOL4-side or dormant, NOT Icon — `generators.icn` (suspend flagship) runs with ZERO v1 allocs (Icon re-pump = β-edges + arena frames); v1 traffic on the passing corpus = COLLECTION growth (pattern_match.c, ARBNO v2) + `rt_proc_call_gen` suspended frames (bb_call_proc_staged, language-blind `is_generator` flag; the SNOBOL4 *VAR probes 070/074 were also v1-silent — this path is machinery for the open β-resume-across-blob bracket more than a present consumer).

**ZETA-INLINE STATUS (updated s7): FIRST SLICE LANDED AND DEFAULT (see s7 SESSION STATE above).** As landed it differs from the sketch below in two ruled ways: (a) the cursor is the exported `g_zls2_cur` memory cell, not r12 — repointing r12 still breaks every sibling `[r12+off]` reference until the activation-local-offsets contract lands in `ir_drive_slot_assign`; that contract (per-activation offsets, cross-box reads at `[r12+myK+producer_off]`, statement backstop as two movs) remains THE open follow-on for the register-cursor end state; (b) **β carries NOTHING (Lon ruling s7)** — the ω that lands on a β already adjusted the cursor via its own RELEASE, so only α (BUMP) and ω (RELEASE) participate; the sketch's `β = mov r12,[slot]` reload is dead. Remaining from the original sketch: ZC_OVF_GUARD as an explicit PROT_NONE page (today the NORESERVE gap below the arena is the de-facto guard), and the widen-past-ARB/ARBNO step as new grants appear.

## ⚠ SESSION STATE (Claude, 2026-07-08, cont. 5) — THE FOUR PORT FUNCTIONS (Lon pivot: "create a unique x86() function call for EACH of the 4 port processing ... All the code HIDES inside these functions and the templates just call and concat. The TEMPLATES are NOT MODIFIED except for this initial setup") — COMMITTED LOCALLY, PUSH PENDING CREDENTIAL; watermark exact, Icon 12/12×2, Prolog 5/5, 8 gates green, SNOBOL4 artifact regen ZERO diffs (the pivot is byte-neutral); **cont.-5b same session: BB-OWNED-ζ proof battery + ZC_PORT ruling bench — ZB-PORTS / ZB-ACT-0 / ZB-ACT-1 checked ▶▶ COMPLETE in GOAL-IR-IMMUTABLE-EMIT.md**

**LANDED cont.-5 — x86_alpha/x86_beta/x86_gamma/x86_omega (x86_asm.h, defined under x86_deflabel):** the ONLY template-facing port surface — α/β INPUT defines are `x86_alpha()`/`x86_beta()`; γ/ω OUTPUT transfers are `x86_gamma()`/`x86_omega()` unconditional plus `x86_gamma(mnem)`/`x86_omega(mnem)` conditional overloads (variable/function-call mnemonics welcome — that's what the overload is FOR; FLAGS CONTRACT at the hook governs jcc-site flavor output). They wrap the int-level `x86_deflabel/x86_jmp/x86_jcc(X86P_*)` internals, so the whole x86_port_hook seam (ZLS2 grants, canary, ω-trace, poison, selfload, future flavors) is inherited unchanged — the four functions are the SURFACE, the hook stays the one flavor registry. **THE LOCK:** `x86_parse`'s XK_PORT string arm now `fprintf+abort`s at emit time ("string port operand RETIRED") — the bb_bin_t enforcement-by-deletion pattern; XK_PORT is unconstructible from strings, so a missed or reintroduced string-port site dies loudly the moment its template is driven (this tripwire is exactly what caught all three straggler families below). **THE SWEEP (one-time setup, ~955 port operations, 117 Makefile-live files):** six mechanical sed rules over literal forms (census after: 274 α / 181 β / 195 γ / 174 ω / 111 conditional-ω / 1 conditional-γ), bb_match_arbno.cpp REWRITTEN by hand (227→152 lines: template-local selfload statics + externs + SCRIP_ARBNO_PHASE_TRACE fprintf DELETED — zero flavor code, zero env reads, zero runtime externs beyond v2's rt_zcol_push; L(9)/L(2) single-exit markers kept), then three straggler families the lock exposed at runtime: (1) `x86("js","ω")` in bb_match_defer — **js was missing from the sweep's jcc alternation AND from the census that "verified" it** (the verification inherited the sweep's blind spot); fixed with the complete 30-mnemonic jcc family; (2) `x86("def/jmp/jcc", PORT_*)` macro spelling (R7's own doc form) in bb_call.cpp + bb_call_fn.cpp (15 sites) — compiled into **libscrip_rt**, hence the runtime aborts on `*VAR` deferred-pattern probes only; (3) variable/function-call mnemonics `x86(<ternary>, "ω")` (bb_binop_relop — took Icon smoke 6/12) and `x86(relop_*_fail_mnem(...), "ω")` (bb_call ×2) which every quoted-mnemonic census missed. **LESSON RECORDED — census patterns must key on the OPERAND, not the call shape:** the final gate `scripts/test_gate_port_functions.sh` greps comments-stripped code for `,[ ]*("α"|"β"|"γ"|"ω"|PORT_*)[ ]*\)` — comma + port operand + close-paren, catching all three families; reads 0; parked bb_callee_frame/bb_query_frame excluded (PARK-NEVER-DELETE — their old strings trip the lock if ever re-enabled, by design). emit.cpp port-idiom mentions were comments only (now reworded by the sweep).

**LANDED cont.-5 — SELFLOAD CARRIER RELOCATED INTO THE HOOK (the last template-resident flavor code):** bb_match_arbno's `x86_arbno_role0_alloc/role2_free` bodies moved verbatim into x86_port_hook arms gated `x86_selfload_mode()==ZC_SELFLOAD_ALLOC && _.op_selfload` — new promotion `op_selfload` (emit.h, DRIVE_FILL-inert default 0; emit.cpp IR_MATCH_ARBNO case sets 1 for op_phase 0 → alloc at the α define, 2 for op_phase 2 → free at the jmp-ω), the exact op_zls2_* pattern, so aliased roles 0/1 cannot fire the free (grant-style role keying, six-decoy-safe). rt_zls_alloc/release/arbno_step1_store/load externs join the rt_zls2 pair in x86_asm.h. ONE ordering delta: role 2's free now fires at the hook's JMP site — AFTER L(9)'s zero-byte def instead of before it (nothing jumps to L(9); marker-only, semantically identical; TEXT label line moves only under SELFLOAD=4). **PROVEN, not assumed:** forced-exhaust probe (match-only `ARBNO('A') 'Z'` against 'AAA') shows **STORE=4 / LOAD=4** — one alloc per role-0 activation, one free at role 2's true death, four anchor retries, carrier recycled; emitted .s carries exactly one store site + one load site; the zero-LOAD traces on succeeding probes are pre-existing correct semantics (a successful match abandons the frame via the success join — the statement-scope backstop reclaims it, never role 2).

**VERIFIED cont.-5 (all fresh, final tip):** SNOBOL4 crosscheck EXACT watermark twice (m3 252/24, m4 251/9/16, DIVERGE=1(1017)); the full previously-failing ARBNO set PASSes m3 incl. the three `*VAR` deferred probes (070/075/117) that exposed straggler family (2); PORT=2 spot-identical; Icon smoke 12/12×2 (was 6/12 mid-session — all six failures were control-flow constructs dying at the relop straggler, family (3)); Prolog 5/5×3; gates 8/8 (the 7 standing + new port gate). Artifact regens run per RULES: SNOBOL4 benchmark/feature/demo **ZERO diffs** — the byte-neutrality proof (pure wrappers; hook additions flag-gated); Icon bench 10 updated + Prolog bench 18 changed = **STALE-ARTIFACT CATCH-UP to ZB-ACT graph mark/release (landed 2026-07-07 s3)**, verified by diff (added rt_zls_mark@PLT prologue machinery, corpus commit `df357066` records the classification), NOT this pivot's output.

**HONEST SCOPE cont.-5:** the four functions carry no new semantics — they are surface + lock + relocation; every flavor axis and its default remains exactly cont.-4's. The `x86("def", L(n))`/internal-label and pair-loop paths are untouched (not ports). Templates are now frozen for flavor work by construction: a new flavor is one hook arm; a new port-op call shape is impossible without tripping the lock or the gate.

**LANDED cont.-5b — BB-OWNED-ζ FINISH (Lon: "Finish up the BB_OWNED rung"): the ZB-ACT-0 method-3/4/5 proof battery, ZB-ACT-1 acceptance, and the ZC_PORT ruling bench — all run this session, zero source edits (pure `make ZCFLAGS=` legs, default build restored and re-proven at the end).** Three-backing mode-invariance: MALLOC crosscheck watermark-EXACT; INFINITE vs LIFO **fail-lists byte-identical** (full-list diff); ASan-instrumented MALLOC runtime, four m4 probes **all clean** (selfload forced-exhaust / plain ARBNO / PORT=2 / hello; LSan silent — the live=1 main frame is reachable via `g_zls_cur`, live-by-design); telemetry **allocs==releases+1 observed literally** (5/4/live=1, identical under MALLOC and LIFO). Ruling bench: real corpus ≈0% (pattern_bt three interleaved rounds; non-pattern `.s` byte-identical — the apparent string_concat "speedup" proven container noise, same binary 3283→960ms across passes); adversarial ARBNO-exhaust micro-loop **4.2×** (82–84ms → 344–347ms, ~10M protocol C calls, outputs identical) — the worst-case per-activation C-call overhead the one-instruction-bump end state exists to shrink; full table at the BB-OWNED NEXT item. STEPS 1/2 of the rung now read DONE/PROVEN in place; step 3 (chain-coalesce) and ZB-ACT-2 (procedure grain) remain open by design; further pattern consumers stay deferred until DP-7 per the standing FINDING. Pre-existing flag: `test_gate_runtime_isolation.sh` fails on 8 Prolog/Raku parser-header includes in runtime files untouched by any of this — standing firewall debt, recorded, not mine.

## SESSION STATE (prior — Claude, 2026-07-08, cont. 4 — DEF-α AUDIT (82 fixes) + ZLS2 FRAME PROTOCOL INJECTED AT THE PORT SEAM (templates untouched, one body, flavor-invisible) **LANDED ON MAIN** (SCRIP main = `29baf0bb` [= `3f5c4a15` def-α + protocol], corpus `97dcd400` unchanged — artifact regen produced ZERO diffs again); watermark unchanged, proven identical across default / PORT=1 / PORT=2 / rbp+PORT=2 (poison compiled-default throughout)

**LANDED cont.-4 — DEF-α EVERYWHERE (Lon: "Ensure all active templates are using x86(\"def\",\"α\")"):** per-return-ARM audit over the 117 Makefile-live templates (function-granular, route-following); 82 scoped fixes — 80 mechanical `return x86_bomb(` → `return x86("def", "α") + x86_bomb(` across 44 files after per-function concat-safety verification (57 candidate functions all proven returned-whole/dispatched, never concatenated), + bb_subject's two ternary bomb branches by hand. `x86_bomb` itself stays def-less (concatenated mid-string in marshal helpers). Verified-dead skips: bb_every/bb_ite/bb_case_arm (0 dispatch sites), bb_scan_stmt (not in Makefile); bb_repalt differently-driven (α in bb_repalt_clear). The defs matter only when a bombed node's α is referenced (the unresolved-forward-ref emit abort this closes); byte-identical watermark proves no live program affected.

**LANDED cont.-4 — ZLS2 FRAME PROTOCOL AT THE PORT SEAM (Lon: "Remove the x86_port_mode function and all its usage in a top-level template function... The templates stay untouched for this entire operation"):** the ZC_PORT_ALLOC arms of bb_match_arb + bb_match_arbno roles 0/1/2 are DELETED — a template never branches on a flavor axis (R2's sibling; `x86_port_mode` survives only inside x86_asm.h encoders/hook, the sanctioned seam, same as the medium global). Activation STATE stays in the static graph-frame quad in every mode (state-in-block re-entrancy retires unexercised — same-node nesting needs DP-7). Under `SCRIP_ZETA_PORT=2` **x86_port_hook injects the whole protocol at the templates' existing port calls**, keyed on per-node **zls2_geom** grants (zeta_storage.c, the save-slot layout authority; the ARB_ZLS2_K/ARBNO_ZLS2_K constants relocated there): **BUMP** at the α define (push K + prev-chain + save-slot store); **RESTORE** at the β define (`rt_zls2_release_to(block)` — a β define IS the backtrack arrival; one cursor reset reclaims every failed successor's frame, the FAIL-DIRECTION release, since a mid-pattern exhaust ω lands on its predecessor's β: reclamation belongs to the frame you fail INTO); **RELEASE** before jmp-ω (unchain + `release_to(block+K)`), granted ONLY to roles whose ω-jumps are STATICALLY all activation-death (ARB's single exhaust ω; ARBNO role 2's outer-fail ω). **THE GRANT IS THE ω-DEATH CLASSIFIER** — role knowledge from the one layout authority; `op_omega_is_death` is never consulted (recorded broken, arbno L(9) note), and the six-decoy-ω trap cannot fire because aliased roles (ARBNO 0/1) carry no RELEASE grant. New machinery: `zls2_geom` + `ZLS2_BUMP/RESTORE/RELEASE` (zeta_choices.h) + `op_zls2_slot/op_zls2_ops` (DRIVE_FILL-inert) + `x86_zls2_release_to_reg` encoder; hook body relocated below the zls2 helpers it calls. Dormant direct-sub α arm preserved, re-keyed `bytes>0 && ops==0`. **BUG FOUND+FIXED en route:** first zls2_geom wrote K=32 for the ungranted ph1 — exactly the dormant arm's key — emitting `sub r12,32` at ph1's α (total frame corruption, 5 ARBNO tests); outputs now written only with a grant. γ carries no frame arm (success hands the live frame down) but the seam fires there for future trace/assert/GC flavors — the injection point Lon named for those.

**VERIFIED cont.-4 (all fresh, final tip):** default crosscheck byte-identical watermark THREE ways (results run, re-run, artifact regen zero corpus diffs); PORT=2 crosscheck watermark-identical (the injected protocol preserves every result); rbp+PORT=2 passes the full previously-failing ARBNO set; PORT=1 canary intact; `SCRIP_ZLS2_TRACE` shows one push + one release per activation with silent no-op restores under clean LIFO (release_to at cursor).

**LANDED cont.-3, SUPERSEDED cont.-4 (ALLOC arm deleted; state stays in the quad; frame protocol now injected at the port seam — see top) — ARB rides the ZLS2 arena (second consumer, `SCRIP_ZETA_PORT=2`, inert at PLAIN):** matched-length + saved-start move from the one static quad into a per-activation **16-byte prev-chained block**; save slot reuses ARB's OWN 8B pad at `x86_scratch_off+8` (grant split from IR_MATCH_REM in zeta_storage.c, **zero widening, no offset shifts** — the MATCH_HEAD pad-reuse precedent, safer than ARBNO's 1→2-quad widening); pop at ARB's single `L(0)` exhaust exit (ARB has exactly ONE `jmp ω` — no six-decoy ambiguity); `x86_zls2_push_call/pop_call` promoted from bb_match_arbno.cpp statics to x86_asm.h for sharing.

**LANDED — the CONSOLIDATED PORT HOOK (Lon cont.-2 directive):** `x86_port_hook(site, port)` in x86_asm.h is now THE seam at every port emission — the audit proved all port defines/jumps route through exactly `x86_deflabel`/`x86_jmp`/`x86_jcc` (XK_PORT dispatch) + `x86_pair_loop`'s β flush, and all four now call the hook; a new flavor (trace/assert/GC/stack experiment) is one arm, zero template edits, both mediums. **⛔ FLAGS CONTRACT stated at the hook: X86H_JCC flavor output MUST preserve CPU flags** (a jcc consumes the caller's cmp/test — this is WHY the canary was historically jmp-only; now explicit instead of implicit). Relocated verbatim: INSTRUMENTED canary (JMP), `SCRIP_ZETA_OMEGA_TRACE` (JMP+ω), ZLS2 α self-alloc sub (DEF+α, grant-gated). Free stays template-placed (six-decoy-ω finding stands). **SUPERSEDED cont.-4: release is now CENTRAL at the jmp-ω hook site, gated by the per-role zls2_geom grant (role-static death knowledge) — the six-decoy finding STANDS as the reason the grant, never a chain classifier, is the gate.**

**LANDED — ZC_FRAME r12↔rbp switch (Lon cont.-2 directive, "this IS a template change"):** `ZC_FRAME_R12`(default)/`ZC_FRAME_RBP` in zeta_choices.h + `SCRIP_ZETA_FRAME=rbp` env, the x86_port_mode idiom. Everything frame-relative flows through `x86_frame_mode/x86_zr/x86_zr_num/x86_align_save`: generalized `x86_r12_modrm` (name kept, 17 sites; r12 SIB path byte-identical, rbp no-SIB + forced-disp8-at-0 per `as`), new `x86_frame_rex` (conditional B, bare-0x40 suppressed) replacing every hard REX, FR/FRQ + the x86_parse FR32/FR64 prefixes in lockstep, canary test-ZR,ZR both mediums, `x86_align_enter/leave` (save reg = the OTHER of the pair — what makes the dance frame-safe) with **all 14 live hand-spelled dances swept** (arbno×3, capture×3, defer×4, head×1, release×1, subject×2) + 5 internal wrappers, arbno-v2's 6 literal r12 operands → `x86_zr()`, emit.cpp's α-selfload assert + gen-proc suspend store/jmp bytes generalized, xa_flat.cpp prologue/epilogue byte fragments via 5 `xaf_*` helpers with the rel32 **patch site now COMPUTED from built length** (the hand-count broke the moment fragment sizes became frame-dependent). Untouched by design: parked bb_callee_frame/bb_query_frame (not in Makefile), bb_create's 6-reg coexpr table (already saves BOTH r12 and rbp), the 5 pre-existing inert rbx dances. **RESULT FAR PAST "experiment": FRAME=rbp passes the ENTIRE crosscheck with the exact watermark fail-set, both modes, plus Icon 12/12×2 (exercising the converted suspend bytes), Prolog 5/5×2, polyglot 2/2×2, and rbp+PORT=2 combined.**

**TWO ENCODER BUGS FOUND AND FIXED (both via the ONE regression this session's own ARB work exposed — word2 under PORT=2):** (1) **`x86("mov", RDD(base,off), imm)` had NO dispatch arm and the mov fall-through SILENTLY emitted nothing** — ARB's α `len=0` store vanished from the emitted stream (proven by gdb disassembly of the live JIT page, not inferred); the first activation still passed because a virgin MAP_NORESERVE page reads as zero, so the bug survived two full crosschecks — added `x86_reg_disp32_store_imm32` (with a loud SIB-base guard the whole reg_disp32 family lacks) AND **made the mov fall-through a fatal bomb** (fired nowhere across 276×2×many runs — no other silent drop existed). (2) **`x86_frame_add_imm` hard-coded REX.B** — under the rbp frame it turned base-rbp modrm (low3=101) into base-**r13** (same low3): head-β's cursor bump wrote `[r13+off]` while every reader read `[rbp+off]`, the anchor never advanced, and the simplest literal match spun forever in mode-3 (mode-4 immune — its TEXT arm always spelled from x86_frame_text_mem; found by attaching gdb to the spin and disassembling).

**LANDED — POISON PROVING PASS (NEXT rung 2, adapted):** `rt_zls2_pop/release_to` now fill reclaimed bytes **0xDD** under ZC_POISON_FILL (compiled default) / `SCRIP_ZLS2_POISON` env — any use-after-pop or missed write-before-read becomes loud garbage instead of plausibly-stale data (the exact silence the word2 bug hid in). **PORT=2 and rbp+PORT=2 crosschecks under active poison: watermark-identical — the arena's write-before-read contract is PROVEN corpus-wide for both consumers, not assumed.**

**FINDING — the arbno/fence fail family is NOT ZLS's to fix:** 116/142/145/147/151 die at LOWER ("pattern shape outside the SN4-PAT subset") — `FENCE(P)` the function form is unlowered; probed before converting bb_match_fence, which would have changed nothing. The remaining scratch-quad primitives (REM/FENCE/SPAN/BREAK/BREAKX) have **no live overlapping-activation hazard in the current subset** (ARBNO-v2 bodies already get per-iteration privacy via the COLLECTION; v1 bodies don't backtrack into prior iterations by definition; the real overlap vector — recursive `*P` — is DP-7, unlanded). Converting them buys uniformity, not correctness — deliberately deferred rather than padded in.

**LANDED cont.-3, SUPERSEDED cont.-4 (ALLOC arms deleted; state stays in the quad; bump/restore/release now injected at the port seam — see top; the rt_zls2 machinery + statement backstop described here remain landed and are the protocol's runtime) — ARBNO v1 rides the ZLS2 arena under `SCRIP_ZETA_PORT=2` (ZC_PORT_ALLOC), fully inert at PLAIN:** the three activation cursors (entry/yield/cur_before) move from the ONE static graph-frame quad (the documented re-entrancy casualty) into a per-activation **32-byte ZLS2 block**; the current activation's block pointer lives in a per-node granted graph-frame **save slot** (v1 grant widened 1→2 quads, ptr at `op_off+16`, zeta_storage.c); the block's header (+0) **prev-chains** the previous activation's pointer, so the SELFLOAD=4 slice's single-static-carrier hazard (nested/re-entrant same-node activations) is structurally gone. **r12 is NEVER repointed** — every sibling `[r12+off]` untouched (the wall below stands; the block is reached via a scratch register loaded from the save slot at each role port). Free = own-constant `rt_zls2_pop(32)` at role 2's true exit, immediately before the `L(9)` single-exit label — never central (six-decoy-ω). New machinery: `rt_zls2_push/pop` (bare down-bump/up-bump, no headers, no zeroing, LIFO-violation aborts loudly, `SCRIP_ZLS2_TRACE=1` event trace) + `rt_zls2_mark/release_to` — the **statement-scope backstop** (head grant widened 1→2 quads, mark at `op_off+16`; MATCH_HEAD α marks, head's ω-choke `L(1)` + IR_MATCH_RELEASE's α both release to mark) which **closes the success-path leak** (an ARBNO that succeeds leaves via the pattern's success join, never its own role-2 pop; the backstop reclaims it — release-to-mark is idempotent with role 2's eager pop). Encoder additions per R7: `RDD(base,off)` dword reg-disp operand + `XK_REGDISP32` parse arm + 32-bit load/store encoders mirroring the 64-bit siblings.

**VERIFIED (all fresh, final tip binary):** crosscheck **flag-OFF = flag-ON = watermark, fail-sets character-identical** (m3 252/24, m4 251/9/16, DIVERGE=1(1017)) — the mode-invariance gate. Hand probes (manual's own LIST/PAIRS examples + a 5-statement backtrack stressor): m3+m4 × flag-off+flag-on all byte-identical to freshly-run SPITBOL oracle; trace audit over 28 activations shows **every MARK at arena top** (zero cross-statement leakage), every activation exactly one alloc + one free (role-2 pop on exhaust, backstop reclaim on success). Icon smoke 12/12×2, prolog 5/5×3, polyglot 2/2×2. Gates: no_bb_bin_t 0, no_vstack 1 (pre-existing, stash-proven), icn_no_stack 0, one_reg_frame 0, emit_no_lang PASS, emit_no_ir_mutation PASS. Artifact regen run (bench 13 files, feature 35 files, demo no-change); 3 bench EMIT-FAILs (mixed_workload/roman/string_pattern) proven **pre-existing at HEAD~1 rebuild** — the SUBJECT PATTERN = REPL splice gap, NEXT BRACKETS #3, not a regression.

**HONEST SCOPE:** v1-only (v2's rt_zcol COLLECTION untouched — already per-iteration correct); default-OFF; the prev-chain makes same-node nesting *structurally* correct but it is **not yet exercisable** (recursive patterns `*P`-through-ARBNO are DP-7, not landed) — first real exerciser arrives with DP-7. The dormant α-hook (`sub r12,K` at x86_deflabel, grant-gated) remains for future full-discipline consumers whose bodies don't reference the graph frame; ARBNO deliberately does NOT use it (repointing r12 is wrong here). SELFLOAD=4's carrier slice left untouched as its own proven record; PORT=2+SELFLOAD=4 simultaneously is not a supported combo (independent double alloc, harmless, pointless).

**NEXT (ZLS2 ladder — rungs 1 and 2 COMPLETED cont.-3 and deleted per handoff protocol):** (1) **RULED + LANDED cont.-6 (`9e37f154`) — ZC_PORT default IS ALLOC.** (Evidence record kept below for lineage:) Evidence (strengthened cont.-4: the protocol is now template-free at the port seam, grant-driven, and watermark-identical again post-rework): watermark-identical fail-sets across default/PORT=1/PORT=2/rbp/rbp+PORT=2/poison; write-before-read proven under 0xDD poison corpus-wide; both consumers (ARBNO v1, ARB) leak-free by trace audit. **PERF MEASURED cont.-5b (the bench this ruling was waiting for):** real-corpus cost is UNMEASURABLE — non-pattern benches emit byte-identical `.s` under PORT=2 (grants fire only on ARB/ARBNO, zero cost by construction; the string_concat/op_dispatch bench "deltas" were proven container noise, same binary swinging 3283→960ms across passes), and `pattern_bt` (12.7s, the corpus's pattern-heaviest, draws the statement backstop: mark + 2× release_to) times ≈0% across three interleaved rounds (PLAIN 12772/12781/12671 vs PORT2 12720/12715/12723 self-ms). The ADVERSARIAL bound: a pure ARBNO-exhaust micro-loop (20k failing `ARBNO('A') 'Z'` statements over a 30-char subject ≈ 10M protocol C calls, outputs identical both modes) runs **82–84ms PLAIN vs 344–347ms PORT2 = 4.2×** — the per-activation push/restore/release C-call overhead, amortized to nothing in real programs, and exactly what the recorded one-instruction-bump end state (strip zero-init/hiwater/roots → bare sub/add) exists to shrink. The ruling is Lon's; both numbers are now on the table. (2) trampoline retirement → rsp (the design's end state; ZC_FRAME_RBP's full-corpus pass de-risks the "rbp frees up" half of that plan). (3) further consumers (REM/FENCE/SPAN/BREAK/BREAKX/SPAN_VAR) only WITH a live overlap exerciser — i.e. after DP-7 lands recursive `*P`; see the FINDING above for why padding them in now proves nothing.

**ZLS2 ARENA (Lon directive prior session — register/design decisions RESOLVED, unchanged):** ONE register (r12), arena grows **DOWN**; α = `sub r12, K`, single-exit ω = `add r12, K` — down-growth makes BOTH constants the box's OWN emit-time frame size (up-growth needs the runtime predecessor's, unknowable at emit time). β needs no reload in pure LIFO (predecessor's pop lands r12 exactly on the resumed frame); bulk unwind (FENCE) reloads from a saved mark. **(cont.-4: the β-arrival cursor reset is now REAL machinery — the hook's RESTORE arm, release_to(block) at every granted β define.)** ARBNO variable data = per-iteration mini-frames pushed below, no slide (v1 contiguous — body dies before append; v2 keeps rt_zcol). Six-register contract confirmed from `bb_create.cpp`: r12=ζ r13=subject r14=cursor-save r15=len-save rbx=GVA rbp=match-family scratch. **C-STACK (Lon's Eureka, analyzed): sound in the END STATE ONLY** — today the proc trampoline re-enters emitted code, so a suspended generator's live frames would sit below rsp after the trampoline pops = clobbered; under the 4/28 direct-jmp ucall convention Lon's no-interleave caveat holds by construction, rsp becomes the frame pointer, the `and rsp,-16` idioms delete, r12 AND rbp free up, and libgc scans the C stack for free. The discipline is backing-agnostic: built on r12 now, rename to rsp when the trampoline retires. (sbrk = the heap, not the stack.)

---

## ⭐⭐⭐ TOP PRIORITY RUNG: BB-OWNED-ζ — per-BB self-allocating memory (Lon pivot)

**THE IDEA:** a GC distinct from SIL mark/slide/adjust (§6, `GOAL-IR-IMMUTABLE-EMIT.md`), marked by BB entry/exit instead of heap-reachability tracing. α allocs + sets R12 + saves for β; β loads only (no alloc); γ untouched; the jump to ω out of a construct's TRUE exit is the free. A Byrd Box is pure-functional — one entry-family, one exit-family — so block liveness is a structural fact of control position, not something a tracer discovers. Start with `ZC_ALLOC_INFINITE` (proves wiring) before anything reclaims. Same rung as ZB-ACT-0/1/2 in `GOAL-IR-IMMUTABLE-EMIT.md`, merged here; ARBNO is the first construct (documented casualty of the old single static slot; `bb_match_arbno.cpp` already exists).

**⚠ ω is NOT uniformly "the death."** Verified directly against `bb_match_arbno.cpp`: it emits SIX `jmp "ω"` sites across six roles (v1 roles 0/1/2, v2 roles 3/4/5), and only TWO (role 2, role 5) are the construct's genuine irreversible exit — the other four reuse the `ω` port name for internal control flow (body-entry, refuse-and-defer). Hooking `port==OMEGA` universally at the central `x86_jmp` dispatch would free on the internal jumps too; under a bump/LIFO backing this is **silent corruption downstream**, not a crash at the bad instruction.

**STATUS OF THE CLASSIFICATION SIGNAL, `op_omega_is_death` (emit.cpp ~1514, field comment emit.h ~398):** computed per-node in `codegen_flat_chain_body`, intended to be true iff an ω-edge falls through to the chain's own outer `lbl_ω` (never resolved inside the local chain). **Session finding (Claude Sonnet 5, 2026-07-08, `SCRIP_OMEGA_DIAG` trace, verified not inferred): this does NOT correctly distinguish role 2's true death in at least one real case.** `IR_MATCH_HEAD` (the pattern statement's own fail-continuation target) can sit INSIDE the same `codegen_flat_chain_body` chain window as the ARBNO node whose ω targets it — so `omega_resolved=1` fires for role 2 too, giving `op_omega_is_death=0` for the one case that should read 1. The formula as written treats "target found among this chain's own nodes" as sufficient for "clean internal handoff," but landing on the chain's own fail-continuation node is not the same thing as landing on a fresh activation's α. **Not yet fixed — flagged, not patched on a hunch, per this project's own methodology.** The free-call this signal would gate (`x86_zeta_free_call`) stays disabled regardless (it frees r12 itself, wrong for ARBNO's carrier-based design) — this finding doesn't change that, it means the signal can't yet be trusted to drive any future central-hook free either.

**MITIGATION LANDED THIS SESSION (Claude Sonnet 5, 2026-07-08) — single-exit label, sidesteps the classifier instead of fixing it.** `bb_match_arbno.cpp` role 2 (v1 exhaust) now `def L(9)` immediately before its true-death `jmp "ω"` — an ordinary local integer label (auto-namespaced `.Lx<x86_uid>_9` per node, the existing `L(n)`/`x86_internal_name` mechanism, NOT a new port; RULES.md's FOUR-PORTS rule is untouched). Role 5 (v2 exhaust)'s existing `L(2)` def is now documented as the same kind of marker (was already there, undocumented). Gives anything auditing the emitted stream — disassembler, future central hook, a person — one unambiguous, greppable address for "this activation is provably dead," independent of the broken chain-membership check. Verified: `.Lx<uid>_9:` appears exactly once per ARBNO-exhaust node instance in compiled output, nowhere near roles 0/1. Zero regressions (crosscheck byte-identical to pre-change watermark below). Does NOT yet wire the central `x86_jmp` hook to consume this label — that's real design work (how does the hook, which only sees the port at dispatch time, learn a preceding local label was just defined?), not attempted.

**CONFIRMED WORKING (Claude Sonnet 5, 2026-07-08; cont.-5 RELOCATION NOTE: the template-local `x86_arbno_role0_alloc/role2_free` pair described below now lives INSIDE `x86_port_hook`, keyed on the `op_selfload` promotion — behavior re-proven post-relocation: forced-exhaust STORE/LOAD=4/4, ASan-clean under MALLOC), by direct trace not code-reading alone:** under `SCRIP_ZETA_SELFLOAD=4` (`ZC_SELFLOAD_ALLOC`), every ARBNO α-invocation gets exactly one alloc and exactly one free — either via role 2's own true-ω (`x86_arbno_role2_free`, when that activation individually backtracks to exhaustion) or via `IR_MATCH_RELEASE`'s `rt_zls_release_to`-from-mark backstop at `sJ` (when the activation succeeds and leaves via the pattern's success join instead of ARBNO's own ω). **This closes the previously-open "success-path leak"** (an ARBNO that never reaches its own ω was leaking its block) — closes it via the chain-walk itself (`rt_zls_release_to` walks `g_zls_cur` back to the mark, freeing everything above it) rather than needing a per-statement list, because the singly-linked alloc chain already IS that list. Verified via `SCRIP_ZLS_RELEASE_TRACE=1`/`SCRIP_ARBNO_STEP1_TRACE=1` paired event-by-event on hand-written v1 and v2 probes; repeated α-hits on backtrack-heavy probes are legitimate independent unanchored-retry activations (SPITBOL retrying the whole pattern at each cursor position), not a bug. Crosscheck run with the flag on: byte-identical fail-list to flag-off (mode-invariance gate satisfied).

**HONEST SCOPE, updated cont.-3:** default-OFF (`ZC_SELFLOAD_OFF`), ARBNO-only, v1-only (phases 0/1/2; v2's phases 3/4/5 use the separate, already-working `rt_zcol_push` per-iteration COLLECTION mechanism, untouched by any of this). The cont.-2 note here — that extending to `bb_match_arb.cpp` was declined because inlining ARBNO-shaped alloc/free calls would duplicate the disabled central-hook approach — is **SUPERSEDED**: cont.-3 landed ARB via the ZLS2 save-slot-in-frame pattern (PORT=2 axis, not this SELFLOAD=4 carrier slice), which is the genuine generalization that decline was waiting for. This SELFLOAD=4 record stands as history of the carrier approach only.

**STEPS (status cont.-5b — the ZB-ACT-0/1/2 ladder in GOAL-IR-IMMUTABLE-EMIT.md now carries matching ▶▶ COMPLETE records):**
1. Per-BB self-alloc — **DONE for the current subset**: ARBNO v1 + ARB ride the ZLS2 port-hook protocol (cont.-3/4), the carrier record stands relocated into the hook (cont.-5). Further consumers (REM/FENCE/SPAN/BREAK/BREAKX) stay DEFERRED BY DESIGN until DP-7 lands a live overlap exerciser — see the FINDING above ("buys uniformity, not correctness").
2. Real reclamation — **PROVEN cont.-5b, the full three-backing battery**: `-DZC_ALLOC=ZC_ALLOC_MALLOC` crosscheck watermark-EXACT (real malloc/free per activation, corpus-wide) + **ASan m4 probes all clean** (selfload forced-exhaust, plain, PORT=2, hello; LSan silent — live=1 main frame is globally rooted, by design) + `BUMP_INFINITE` vs `BUMP_LIFO` **fail-lists byte-identical** (full-list diff) + telemetry **allocs==releases+1 observed literally** (5/4/live=1, identical under MALLOC and LIFO). Executed via `make ZCFLAGS=-DZC_ALLOC=...` (no source edits); default restored and re-proven.
3. Chain-coalesce optimizer pass — find chains of self-allocating BBs with single α-entry/single ω-exit, share ONE R12. Not started (open; ZB-ACT-2's procedure grain is the natural first coalesce).

**r12 CANNOT be repointed by a per-construct self-alloc without breaking every sibling box** — confirmed from the implementation side: `FR(off)` is `[r12+off]` for the WHOLE function, shared by every box in it. STEP 1 as built carries the pointer via the runtime-side carrier instead (`rt_zls_arbno_step1_store/load`, one static per construct, deliberately NOT shared across constructs — e.g. ARBNO and a hypothetical ARB carrier must stay separate, since `ARBNO(ARB)` would otherwise have the inner alloc stomp the outer's still-pending pointer). Correct for sequential re-entry only, not yet nested/concurrent activations of the same node — explicitly future work.

---

<!-- ════════════ SNOBOL4 TEST-SUITE LADDER CRAWL — live head ════════════ -->

## ⛔⛔ RUNG SUITE-ZERO — DRIVE THE 285-TEST CROSSCHECK TO ZERO RED (Lon directive, 2026-07-11, s21)

**THE ASK, VERBATIM: "Let's get them all fixed."** The suite is 285 programs at s21 start (290 after SZ-2b's five additions). `bash scripts/test_crosscheck_snobol4.sh` on a fresh origin clone measures m3 279/6/0, m4 278/6/1, DIVERGE=1 — NUMERICALLY identical to the s20 header's watermark. **⚠ CORRECTION (Claude s21, self-caught mid-session): s21's orientation claimed the tree was AT s20's hashes `cde0a38b`/`cefb4421` — it was not, and those hashes are NOT VALID OBJECTS in origin SCRIP (rebase onto `d05b2028` re-hashed them; the s20 content is present, the recorded hashes are dead). Verify HEAD hashes at orientation, never assert them from the goal file.** **⚠ PARALLEL-LINEAGE FINDING (s21): a beauty session (`HANDOFF-2026-07-10-CLAUDE-BEAUTY-PARTIAL-SESSION2.md`, SCRIP `6749c97c`) ran against a corpus baseline `7fe04a1a` counting ~304 crosscheck programs FAIL=17 — that corpus lineage WAS NEVER PUSHED (`git cat-file -t 7fe04a1a` = not a valid object in origin corpus) while its SCRIP side WAS pushed. Two corpus lineages are live; ~19 crosscheck tests exist only in an unpushed container. Whoever holds that container: push it or mint the tests again — the SCRIP-side beauty fixes it regression-covers are already on origin uncovered.** Container prereq discovered s21: a fresh sandbox has NO Boehm GC headers — `apt-get install -y libgc-dev` before `make` + `make libscrip_rt`, or the build dies at `core.h:8 gc/gc.h: No such file`. Add to session setup, it cost a build cycle to find.

**GATE FOR THE WHOLE RUNG:** `bash scripts/test_crosscheck_snobol4.sh` prints m3 FAIL=0, m4 FAIL=0, SKIP=0, DIVERGE=0 on all 285. No step may be called done while it leaves a red it claimed.

**THE SEVEN REDS ARE NOT SEVEN BUGS.** Six of the seven (SZ-1..SZ-6) are the SAME architectural wall wearing six faces: **a pattern that lives inside a compiled blob cannot have its internal generator state resumed, nor its speculative captures undone, from OUTSIDE that blob.** SZ-7 is genuinely unrelated (an error-path mode disagreement). SEQUENCE MATTERS — SZ-1 is the load-bearing one and is expected to move several of SZ-2..SZ-6 for free; re-run the full crosscheck after EVERY step and record which reds moved, because the step boundaries below are a HYPOTHESIS about how the class decomposes, not a proven partition. **If SZ-1 clears more than its own two tests, DELETE the steps it cleared rather than pretending they were separately earned.**

**METHOD (RULES.md, non-negotiable):** MONITOR-FIRST. Each step: read the SPITBOL manual for the constructs it touches → 2-way IPC sync-step monitor to bracket first-divergence ↔ last-agreeing → gdb spin-counter breakpoint in the bracket → single-step to the land mine → one targeted fix → both modes vs `.ref` → full crosscheck for regressions. STATIC-FIRST/ONE-STAB is permitted only where the land mine is statically visible (a missing enum case, s18-b's class); anything else escalates to the monitor.

### Steps

- [ ] **SZ-1 — β-RESUME ACROSS THE BLOB BOUNDARY.** Tests: **143** (`R = ARBNO(*LP)`, `LP = FENCE('a'|'b'|'c')`) + **145** (`expr = num . FIRST ARBNO(FENCE('+') num . LAST)`). Both print `fail`; oracle prints `matched aabbcc` / `first=1 last=5`. **The wall:** an outer failure (`RPOS(0)`) cannot re-enter a returned blob's internal generator to ask for its next alternative — the blob returns once and is done. 145 is already diagnosed down to THIS one blocker (its other two — seal target, capture-then-ARBNO — landed s14-a/s14-b); 143 converged into the same wall per `FINDING-2026-07-10-CLAUDE-SN4-BLOB-BETA-RESUME.md`, which SUPERSEDES the older nested-blob-defer FINDING (**do not resurrect that doc's three suspects — they were wrong**). **READ THAT FINDING BEFORE WRITING A LINE.** It is spec-complete: caller side claims FR(16), stashes fn+frame at γ, loads and re-calls with `esi=1` at β, releases + ω on exhaust (`bb_match_defer.cpp`, existing `x86()` vocab, no new encoder); blob side needs an explicit resumable-callable marker (**NOT** raw SUCCEED structure — EVAL shares that shape, an esi audit is owed) plus a `test esi,esi; jnz .Ltail_β` prologue via `L(n)`. `IR_MATCH_DEFER` joins `ir_is_generator_kind` in the SAME commit (the s18-b landmine class, pre-empted). `sno_pat_deterministic(TT_DEFER)` stays 1 this rung. **THE LOAD-BEARING STEP — do it first, expect it to move SZ-2..SZ-4 too.**
- [ ] **SZ-2a — THE SEAL TARGET FOR FENCE-INSIDE-ALT.** Test: **124** (`token = (kw . K | ident . I)`, `kw = FENCE('if'|'else'|...)`, subject `'iffoo'`). Prints `parse fail`; oracle prints `id=iffoo`. **SZ-2b's landing proved this is NOT a capture bug** (the pop/restore machinery is in and 124 is unmoved): the surviving hypothesis is control flow — `cx->pat_seal` is set to `F` only in the TT_ARBNO arm (lower_snobol4.c:990) and never for an ALT arm, so FENCE inside alt-1 seals to STATEMENT fail and alt-2 (`ident`) is never offered. Candidate fix: parameterize `pat_seal` to the enclosing ALT's next-alternative edge while lowering an alt arm, exactly as :990 does for ARBNO bodies (the s14-a class, never generalized to alternation). Static hypothesis from reading the lowerer — confirm by asm read or monitor before stabbing. Manual Ch.19 FENCE is the semantics arbiter; csnobol4 also fails this one, SPITBOL is the oracle.
- [ ] **SZ-2c — RESTORE THE TRANSIT GUARANTEE IN ALT (Lon protocol ruling, s21): "Each BB must be guaranteed a chance to do its post-processing before jumping to ω. Bypassing is not allowed unless it is a controlled optimization."** Corpus test **160** (KNOWN RED, oracle-minted): `'aXb' ? ('a' ARB . V | 'q') 'b'` — oracle `V=[X]` (the generator INSIDE the succeeded alternative must resume before any alternative switch, rightmost-choice-point order); SCRIP prints `fail` (switches alternatives instead). **Root:** JOIN.α (MARK) stores its OWN reload arm into `[slot+8]`, so resume-in from a failing right neighbour jumps straight to try-next-alternative — the succeeded alternative's interior boxes get NO β/ω transit: inner generators never resume (160), capture pops never fire (the reason SZ-2b's slot+4 height-restore exists). The ALT-RESUME comment's "documented residual" was never a residual; it is this bug. **Fix design (s21 recon):** mirror the seq builder — each alternative's resume surface = its subgraph's FIRST-ALLOCATED node (`int before=g->n; … g->all[before]`, the identical convention `sno_resume_ω_to` uses at every seq seam, proven live by SZ-2b's p5/p6 pop traces); JOIN's MARK stores THAT node's β as the continuation; interior exhaust lands the reload naturally (alternative lowered with `fail=JOIN`, ALTERNATE is generator-kind). A deterministic alternative (resume surface not generator-kind) keeps storing the own-reload — the controlled optimization, now actually controlled. **MECHANISM GAP (why not landed s21):** the MARK's `lea` targets its own internal label; storing ANOTHER node's β address needs a cross-node port-address materialization the emitter lacks — the SAME primitive `FINDING-2026-07-10-CLAUDE-SN4-BLOB-BETA-RESUME.md` (SZ-1) specifies (stash continuation at γ, re-enter at β). **Land SZ-2c WITH SZ-1 — one mechanism, not two inventions.** ⚠ **STANDING VIOLATION, PENDING LON'S RULING:** SZ-2b's ALTERNATE slot+4 height-save/restore is a semantic patch AT the choice point compensating for the missing transit — keeps 155–159 oracle-green today but is NOT the protocol shape. (a) leave marked temporary, delete in the SZ-2c landing commit (kept-green ladder); or (b) revert now, 156-class goes honestly red until SZ-2c (pure ladder). **RULING (Lon delegated the choice to Claude, s21): option (a) — KEEP, MARKED TEMPORARY, DELETE AT SZ-2c LANDING.** Rationale: it makes 155-159 oracle-correct TODAY (155 is the manual's own `. OUTPUT . OUTPUT` example, which printed one line of two before), and reverting trades real correctness for purity while the true mechanism is one session away. It is recorded as a violation, not laundered as a design. Whoever lands SZ-2c MUST delete the slot+4 machinery (`rt_dcap_height`/`rt_dcap_restore_to` + template arms) and prove 155–160 green through transit alone.
- [x] **SZ-2b — CAPTURE RING IS A PURE PER-ACTIVATION STACK (LANDED s21, SCRIP `6d0e8fdc`, corpus `42db1c62`).** Lon directive, transplanting the proven snobol4python `Δ` model: **COND.α pushes a pend (upsert DELETED — the keyed overwrite violated the manual's own `. OUTPUT . OUTPUT` example); COND.β pops it** (`rt_dcap_pop`, phase-1 only; the Python pop-after-yield fires exactly on backtrack-in-to-re-pump); **the ALTERNATE slot gained a dcap-height save at +4** (`rt_dcap_height`/`rt_dcap_restore_to`) — every JOIN reload arm that rewinds the cursor to the entry mark now truncates the ring to the entry height, so an abandoned alternative's pends die with its cursor progress (SCRIP's flat ALT deliberately skips interior unwind — the s7 unpend-on-β revert note in `bb_match_capture.cpp` is SUPERSEDED, its landmine dissolved by the height-restore). Success flush unchanged: in entry order from the activation mark, then freed. **Oracle-pinned by probes p1–p6, all six exact in BOTH modes, and minted as corpus regression tests 155–159** (refs sbl-generated, never hand-written): 155 OUTPUT entry-order (manual §cond-assign example — was printing one line of two), 156 alt-abandon pop (`V1=` null — was overcommitting `ab`), 157 the s7 regression guard (held), 158 ARBNO per-iteration captures (`a b c` — was printing only `c`; **this is DEMO-PAT GAP-2's commit-ring made unnecessary — a balanced stack needs no ring**), 159 re-yield discard. Crosscheck 290: fail lists byte-identical, zero regressions; sno smoke 7/7. `rt_cap_unpend` is now truly orphaned (extern removed from the template) — dead-code-sweep candidate, left in place this slice.
- [ ] **SZ-3 — DEFERRED-CALL-IN-CAPTURE EXECUTION (the `*FN()` side-effect idiom).** Test: **140** (`outer = EVAL('LEN(1) . *inner(' cs ')')`, the beauty Shift/Reduce double-function trick). Prints `stk=` (empty); oracle prints `stk=B` — i.e. the deferred call **never runs**, so the side-effect never happens and the capture writes nothing. This is **DEMO-PAT GAP-1 exactly** (`(epsilon . *FN())` capture targets never execute — the root cause of the claws5/treebank hang), so **fixing it here is fixing DP-1**, and the goal file's DP-1 rung must be marked landed by this step, not re-done later. Fix shape already scoped there: lower the deferred inner expr as a value-producing chain, RUN it at capture-commit, write the result — runtime capture-write is currently **name-only** and needs a computed-name/run-chain variant. Also needs the **nv set** ledger stamp (currently only requested). ⚠ 140's pattern arrives via `EVAL` of a runtime-built STRING, so it is ALSO an opaque-DT_P composition — if SZ-1 has not already routed EVAL-built patterns through a resumable/recipe-bearing path, this step inherits that too.
- [ ] **SZ-4 — DEFERRED-CALL-IN-CAPTURE, INSIDE AN ARBNO BODY.** Test: **141** (same EVAL double-function trick as 140, wrapped: `ARBNO(catch('cv'))`). **The only test in the suite that SKIPs mode-4** — `--compile` bombs honestly at `FATAL lower_snobol4 (GZ#5 subset): expression form not in the landed subset: tree kind 25` before it can run; mode-3 gets further and produces empty/wrong output. Two sub-problems, in order: **(a)** teach the lowerer tree kind 25 in ARBNO-body position so mode-4 COMPILES at all (turning the SKIP into a real, comparable FAIL — that is progress, book it); **(b)** then it is SZ-3's fix — **the GAP-2 half is ALREADY EVIDENCED FIXED by SZ-2b** (corpus 158: per-iteration ARBNO captures now commit in entry order; a balanced push/pop stack needs no commit-ring — mark **DP-2**'s ring design retired, verify the EVAL-body case here rather than rebuilding it). For sub-problem (a), the recipe-routing fix sketch in `HANDOFF-2026-07-10-CLAUDE-BEAUTY-PARTIAL-SESSION2.md` §BUG 1 is the same defect scoped from the beauty side — read it first. Do SZ-3 first: 141 is 140 under a loop, and a loop over a broken body is still broken.
- [ ] **SZ-5 — THE B-RE REFUSAL: OPAQUE-DT_P COMPOSITION.** Test: **expr_eval** (a full recursive-descent arithmetic parser built from `*Push()`/`*Unary()`/`*Binary()` side-effect patterns + `EVAL`). Both modes **abort** on the guard SCRIP itself prints: `[B-RE] runtime pattern composition over an opaque compiled pattern (recipe-less DT_P) would mint the nested-blob-defer class (124/143/147) — refused, park until that class lands`. **This is the tree TELLING US the dependency out loud: the refusal is a deliberate s16 placard, not a bug — it is parked ON the class SZ-1 exists to land.** So: SZ-1 (and SZ-3) first; then come back, and the honest work here is to **lift the refusal and see what breaks**, not to route around it. Success = the guard is either deleted (composition now safe) or narrowed to a genuinely-still-unsupported residue with a NAMED reason. `expr_eval` is the richest program in the suite (TABLE, NRETURN, `$` indirect assign, EVAL, real+integer arith, precedence, parens, recursion-through-`*expr`) — it will likely surface residue of its own after the guard lifts. Budget accordingly; it is LAST of the pattern six for a reason.
- [ ] **SZ-6 — RE-VERIFY THE PATTERN FIVE TOGETHER + REGRESSION SWEEP.** Not a bug — a **checkpoint**, because SZ-1..SZ-5 all touch the same defer/blob/capture spine and a fix for one has repeatedly (s14, s18) moved or masked another. Run the full 285 crosscheck; confirm 124/140/141/143/145/expr_eval all green in BOTH modes and DIVERGE unchanged; **regenerate the `.s` artifacts** (`util_regen_benchmark_s_artifacts.sh`, `util_regen_feature_s_artifacts.sh`, `util_regen_demo_s_artifacts.sh` — RULES.md handoff step 4, mandatory since every step above touches codegen); then re-run the DEMO-PAT claws5/treebank probes, because **DP-1 and DP-2 fall out of SZ-3/SZ-4** and those demos have been hanging on exactly those gaps — if they now run, say so honestly and benchmark them (DP-3/DP-4).
- [ ] **SZ-7 — 1017: MODE-4 ERROR-PATH DISAGREEMENT (the standing DIVERGE, unrelated to the six).** Test: **1017_arg_local**. `ARG`/`LOCAL` are **unimplemented builtins and are NOT required to be implemented by this step** — the checked-in `.ref` (regenerated from the real oracle, corpus `4f6f2943`) says the correct behavior is to **fail at assertion 1**, printing `FAIL 1017/001: ARG(.jlab,1) = A`. **Mode-3 already matches that exactly, with zero code changes.** Mode-4 does NOT: it prints `** Error 5 in statement 0 / Undefined function or operation` and then **aborts**. So the bug is: **the two modes handle a call to an undefined function differently**, and mode-4's handling additionally SEGVs/aborts after printing (the s18 finding (v) "error-path m3 SEGV after `core_runtime_error` print" is the same class from the other side — **this step should fix BOTH modes' error path, not just paper over 1017**). Mechanism not yet understood; s20 grepped source and found nothing, suspecting a baked `.rodata` string — **that grep was static-only; use the monitor**, the divergence is precisely what it exists to bracket. NOT a regression (the modes always disagreed; a stale `.ref` hid it). Two sub-goals: (a) undefined-function call FAILS the statement identically in both modes; (b) the error path does not abort/SEGV after printing. Implementing `ARG`/`LOCAL` for real is a SEPARATE, OPTIONAL follow-on — if done, the `.ref` must be REGENERATED from the oracle, never hand-edited.

**⚠ HONEST NOTE ON THIS RUNG'S OWN STRUCTURE (Claude, s21):** the seven steps are a partition of the SEVEN OBSERVED REDS, not seven proven-independent defects. I expect SZ-1 to be load-bearing well beyond its two tests and SZ-5's guard to be a consequence rather than a cause. **Whoever crawls this: after each step, re-run the full 285 and let the RESULT redraw the remaining steps.** Do not defend the shape of this ladder against evidence — delete steps that fixed themselves, split steps that turn out to be two, and say which happened.

---

## ▶▶ CURRENT STATE — CRAWLING THE TEST SUITE

**CROSSCHECK WATERMARK (re-verified 2026-07-08 cont.-6 at SCRIP `274e7752`/corpus `194e8300`; lineage `29baf0bb`/`97dcd400` — identical across default, PORT=1, PORT=2, FRAME=rbp, rbp+PORT=2, and PORT=2+0xDD-poison; lineage: matches the `ebfc7009`/`4f6f2943` record byte-for-byte):** `bash scripts/test_crosscheck_snobol4.sh` — LIVE watermark now in the s14 header above (m3 271/11, m4 270/6/6 on the 282-test suite); the 252/276 record below this line predates s10-s14 (a newly-exposed pre-existing mode-3/mode-4 disagreement on undefined-function-call handling, not a regression — see NEXT BRACKETS #2).

**DOCTRINE:** SNOBOL4 rides the same pipeline as Icon — `sno_parse_ast → lower_sno_stage2 → shared optimizer → ir_drive_slot_assign → the ONE language-blind emitter → mode-3/mode-4`. SCRIP follows SPITBOL semantics; oracle = `/home/claude/x64/bin/sbl -b`. Every statement: (1) build subject; (2) build pattern; (3) perform pattern; (4) build replacement; (5) perform replacement — pattern stages are now live (SN4-PAT family below).

**METHOD:** STATIC-FIRST → ONE STAB → THEN MONITOR. Read `.ref`, run both modes, reason from `.s`+IR+templates to the land mine; one targeted fix; verify both modes vs `.ref`. Escalate to the 2-way IPC sync-step monitor (bracket first-divergence↔last-agreeing, gdb spin-counter breakpoint, single-step to the land mine) only if the stab fails.

**NEXT BRACKETS (in order):** [#1 FENCE-in-ARBNO CLOSED s14 — see the s14 header; 145's residual moved to the 074/W07 blob β-resume bracket]
2. **`1017_arg_local` DIVERGE** — the checked-in `.ref` was simply wrong (real oracle fails at assertion 1; `ARG`/`LOCAL` are unimplemented builtins). Regenerated `.ref` (corpus `4f6f2943`); mode-3 now matches it with zero code changes. Mode-4 does NOT match — produces a different hard-failure style for the same undefined-function call. Mechanism not yet understood (grepped source, found nothing — may be a baked `.rodata` string). Not a regression; mode-3/mode-4 always disagreed here, the stale `.ref` just hid it.
3. **062/063 (capture) — `SUBJECT PATTERN = REPL` splice.** Stages 4-5 of the 5-stage model have no implementation yet. Scoped, well-understood, not a wiring gap.
4. **word1/word3/cross/wordcount** — four distinct bugs (capture-into-OUTPUT-keyword; mid-chain-generator capture; wrong-output-undiagnosed; unimplemented "tree kind 31" expression form).
6. **213/082** — `indirect_name`/`keyword_stcount`, long-standing, not yet root-caused.
7. **124/143/140/141** — EVAL-adjacent/compound-pattern probes, not yet individually triaged.
8. **test_case/test_stack/test_string** (corpus include-library) — OPSYN and splice are both landed (s8/s11) and the trio still fails: re-derive the real blocker from a fresh run.

**FINDINGS PARKED (own rungs, not faked):** FINDING-A EAGER-ASSIGN (`.` assigns at subpattern-success even on eventual failure; the `rt_dcap_*` deferred-capture protocol is orphaned — wire at match entry/success/fail); FINDING-B SEQ-CHAIN-THROUGH-DETS (a deterministic element between the fail point and a left generator blocks resume; the SN4-PAT-BETA-CHAIN rung); FOLD BLOB TIER (`IR_REF_INVARIANT` — string tier landed, blob tier remains).

---

## ⛔ SN4-PAT — SNOBOL4 PATTERN MATCHING (the feature spine)

Non-pattern SNOBOL4 subset complete (literals, vars, keywords, `.NAME`, arith+concat, unary, `$`, subscript/DEREF, calls, gotos/labels, every assignment LHS form, DEFINE/DATA/OPSYN). Pattern matching genuinely needed new opcodes (SNOBOL's floating-needle-over-a-pattern model isn't Icon's anchored SCAN family) — a RECONSTRUCTION of a family amputated at `8de0fb46`, design recovered from git (parent `41b53078`).

**THE MODEL:** `IR_MATCH_*` (28, matchers, inline needle, γ=success/ω=failure wiring) + `IR_PATTERN_*` (STITCH boxes only — per-element pre-D7-era builders are ABANDONED, do not resurrect) + `IR_REF_INVARIANT` (folded-constant sealed blob for VARIANT-free subpatterns).

**LANDED (SN4-PAT-0 through SN4-PAT-DEFER-CALLOUT v1) — all rungs through the full ladder below are `[x]` complete, verified vs SPITBOL oracle, zero regressions at each step. Compressed to what a future session needs, not the forensic trail:**
- Enum family re-added (34 members), templates revived one at a time (LEN → LIT → ANY/NOTANY → SPAN → BREAK/BREAKX → TAB/RTAB retrofit → POS/RPOS/REM/ARB → CAT (node-free, edge-threading only, no template) → phase-0 capture SAVE → ALT → FENCE (node-free fail-retarget, not a β→ω box) → ARBNO v1 (deterministic body, 3-phase, no COLLECTION) → ARBNO v2 (generator body, per-iteration COLLECTION via `rt_zcol_push`) → EXPRESSION datatype (`*EXPR`/EVAL, DT_X) → DEFER-CALLOUT v1 (stored true-PATTERN values as AOT-compiled `bb_box_fn` blobs, DT_P).
- **Load-bearing findings to remember, not re-derive:** `_.x86_scratch_off` was read by five templates but written nowhere until SPAN's rung granted it in `ir_drive_slot_assign` — same landmine recurs for any new scratch-slot consumer, grant before wiring. `IR_lit` is an anonymous union (sval/ival/dval overlap) — never write two members on one node (silently zeroes the other, caused real bugs). The match-family `push rbx; mov rbx,rsp; and rsp,-16` C-call idiom poisons the GVA base if the callee re-enters emitted GVA-reading code — swept to `rbp` in touched templates, others inert only while their callees never re-enter emitted code. The emitter's flat chain-BFS worklist originally followed ω only for BINOP/CALL/SUBSCRIPT/generator-kind, missing ω-only backtrack targets (ALT RESTORE) — fixed to follow ω for the whole match-family opcode range.
- **`SN4-PAT-DEFER-CALLOUT` NEXT-SESSION BRACKETS still open, in order:** (1) fresh-frame WIP patch exists (`.github/WIP-sn4pat-defer-freshframe-2026-07-06.patch`) but introduces a DIVERGE=2 (word2/word4) — a real inter-mode lifetime issue, not landed, needs bracketing before landing; addendum suggests the "lifetime divergence" framing may be wrong (an `INPUT`-at-EOF failure-propagation hypothesis instead — re-check before assuming the patch's own diagnosis). (2) DT_P through user functions (063-066 fence_fn cluster) — a DEFINE'd function returning FENCE(...) needs DT_P to survive call/return. (3) β-resume across the blob boundary (074/W07 cursor cluster) — outer backtrack can't re-enter a returned blob's internal generator yet. (4) remaining stragglers: 1011/1013/1015/1017, 082/213, test_case/stack/string+word/cross.

**PARK-NEVER-DELETE (standing directive, Lon) — applies repo-wide, not just this file.** Park parsed-out language code out of the Makefile; never delete it (the `8f3e4b23` "kept intact on disk" precedent). `src/lower/lower_snobol4.gz5-parked-41b53078.c` holds the recovered pre-GZ#5 pattern lowerer (1402 lines) — any dead-code sweep must exempt `*.gz5-parked-*` files and the templates-on-disk inventory (23 `bb_match_*`, 5 `bb_pattern_*` + stubs — all API-current, kept intact by `8f3e4b23`).

---

## SNOBOL4 RE-LIGHT LADDER — SNOBOL4 on the post-GZ#5 spine

**SCOPE NOTE:** relocated here from being cross-referenced; the Icon-only hard rule elsewhere is Icon-climb-scoped and doesn't by itself govern this ladder — flagged, not silently resolved.

**Constraint (still binding): `IR_e` is FROZEN AGAINST ADDITIONS for this climb** (deleting unused ops is directed, not forbidden). One authorized carve-out already landed: `IR_KEYWORD` → `IR_KEYWORD_ICON`/`IR_KEYWORD_SNOBOL4` split (case-fold keyword sets genuinely differ, SNOBOL4 vs Icon — classify-by-name doctrine, not an accidental new-op need). If a rung seems to need a new opcode, the rung is wrong — find the existing-vocabulary lowering or park the program.

### Steps
- [x] RL-0 baseline · [x] RL-1 driver reroute onto the one emitter · [x] RL-2 assignment+OUTPUT re-lowered.
- [~] **RL-4 — statement control flow.** END-goto LANDED (`:(END)`/`:S(END)`/`:F(END)` resolve via a registered label pointing at the exit node). REMAINING: `:S()/:F()` forms branching on a pattern-match result — that's really RL-5/RL-6 territory now that the match family is live; re-check whether RL-4's own remaining scope has already been absorbed by SN4-PAT before treating it as separately open.
- [ ] **RL-3 — concat + arith.** Value concat, `IR_BINOP`/`IR_UNOP`/`IR_BINOP_TEST`/`IR_UNOP_TEST`, `IR_LIT_*`. Gate: `concat/`, `arith/`, `arith_new/` green. **Likely already subsumed by SN4-PAT/current crosscheck state — re-verify against the live watermark above before treating as the next rung; the RL ladder's own numbers predate SN4-PAT.**
- [ ] **RL-5 — primitive patterns onto the SCAN family.** Likely superseded by SN4-PAT's `IR_MATCH_*` family landing since this ladder was written — re-verify before working this rung directly; SN4-PAT's ladder above is almost certainly the current form of this work.
- [ ] **RL-6 — alternation, backtrack, capture.** Same caveat — check against SN4-PAT's ALT/FENCE/ARBNO/capture landings above first.
- [ ] **RL-7 — DEFINE + user functions.** Check against SN4-PAT-DEFER-CALLOUT's current state first.
- [ ] **RL-8 — indirection, aggregates, keywords.** `$X`, ARRAY/TABLE, `&`-keywords. Check current crosscheck data/keywords/strings category state before assuming this is still greenfield.
- [ ] **RL-9 — full-suite green + stale-section purge.** Crosscheck FAIL=0 both modes, DIVERGE=0; then delete anything in this file the ladder's progress has superseded.

**⚠ Whoever picks up RL-3/5/6/7/8 next: the numbers/gates as written predate SN4-PAT and may already be satisfied by it. Re-derive against the CURRENT STATE crosscheck watermark above before treating any of these as a fresh start — do not re-do work SN4-PAT already did under a different name.**

---

## ▶ MONITOR — 2-way IPC sync-step bug-finder: LANDED, reference only

All MON-RE-0 through MON-RE-6 steps are `[x]` DONE — the monitor is built, proven (semantic acceptance met on `/tmp/mdiv.sno`, an 8-step agreement trace), and is the standing bug-finding method referenced throughout this file and RULES.md. Mechanics: `g_monitor_bin` runtime flag (zero overhead when unset), LABEL/VALUE/CALL/RETURN taps emitted into the flattener (not walked at runtime), `scripts/test_monitor_3way_sync_step_auto.sh` / `test_monitor_2way_sync_step_bin.sh`. See RULES.md's own MONITOR-FIRST directive for usage. Companion offline tool: `harness/probe/probe.py` (`&STLIMIT`/`&DUMP=2` replay) — lower priority, needs mode-3/4 to honor those keywords first, neither does yet.

---

## ▶ DEMO-PAT — claws5 + treebank mode-4 PASS + benchmarked

**WHY:** claws5/treebank are pattern-heavy; SPITBOL interprets patterns (graph-walk + backtrack history stack), SCRIP compiles backtracking into four-port native control flow — a structural advantage already visible elsewhere (pattern_bt 0.77×, SCRIP beats SPITBOL). Proves the win on real corpus programs.

**GROUND TRUTH:** both demos hang in both modes (old "PASS" claims describe the now-deleted mode-1/2 interpreter). Root causes localized via minimal repros:
- **GAP-1 (root cause of the hang) — deferred-eval function calls as capture targets `(epsilon . *FN())` never execute.** Both demos build their entire data structure via these calls; they no-op, data stays empty, the pretty-printer spins forever over an empty structure. Fix needs: lower the deferred inner expr as a value-producing chain, run it at capture-commit, write the result. Runtime capture-write is currently name-only — needs a computed-name/run-chain variant.
- **GAP-2 — captures inside a repeated ARBNO body don't commit** (needs a per-iteration commit-ring: truncate-to-mark on β-reentry, flush at overall-match γ).
- **GAP-3 (lower priority, demos use stored patterns so it doesn't block them) — inline non-literal pattern in SCAN context still bombs.**
- **treebank additional gaps:** GAP-4 recursive deferred pattern eval (`*group` referencing its own def); GAP-5 BAL in built patterns; GAP-6 EVAL-built patterns spliced into a match; GAP-7 user DATA types.

**SEQUENCING:** claws5 first (GAP-1+2), treebank second (claws5 fixes + GAP-4..7). Gate: `test_crosscheck_snobol4.sh` byte-identical to HEAD + the named demo behavior.

### Phase A — claws5 — all still open
- [ ] DP-0 — oracle in hand (`x64` clone works for general programs; claws5/treebank need `-P`/`-f` this `sbl` build doesn't support — `claws5.ref` short-form remains the gate). Still need: `run_demo_mode4.sh` + `test_3way_demo_snobol4.sh` skeleton.
- [ ] DP-1 — deferred-call-in-capture execution (the GAP-1 fix; not a 1-liner, needs a new runtime capture-write variant accepting a computed name; needs the **nv set** ledger stamp, currently only requested).
- [ ] DP-2 — capture-commit inside ARBNO (the GAP-2 fix; D3 commit-ring).
- [ ] DP-3 — claws5 end-to-end, no hang, regenerate `.s` artifact.
- [ ] DP-4 — claws5 benchmark vs SPITBOL, record the honest number.

### Phase B — treebank — all still open
- [ ] DP-5 — user DATA types in mode-4 (M4-DATA: constructor + field selectors).
- [ ] DP-6 — BAL in built patterns.
- [ ] DP-7 — recursive deferred pattern eval `*group` (needs a recursion guard — progress-before-recursion, per manual Ch.8).
- [ ] DP-8 — EVAL-built patterns spliced into a match at runtime.
- [ ] DP-9 — treebank end-to-end + benchmark.

**Prereq reads:** `SNOBOL4-5STAGE-OWNED-BUILD.md` (D3/B7/B8/B9/B10 — DP-1/2/6/7/8 are those rungs, demo-sequenced), `ARCH-SNOBOL4.md` §Native pattern, `src/emitter/bb_regs.h`, `src/runtime/rt/bb_pat_build.cpp`, `src/emitter/emit_core.c` matcher dispatch.

---

## ▶ NRG — Native Register Codegen ("beat the interpreter on statements")

**THESIS:** SPITBOL is indirect-threaded code (not fetch-decode-execute) — SCRIP can't win by removing a dispatch loop that threading already removed. SCRIP currently loses on statements because it replaced threading's one-indirect-branch NEXT with a full PLT call per primitive. The only way to beat threaded code is specialization threading structurally can't do: keep a proven-typed value in a register, emit raw ops, no per-op tagged-descriptor load/re-check. (Patterns are the separate battleground SCRIP already wins — DEMO-PAT above; this rung is statements only.)

**Order A→E→B→C→D** (cheap CFG/dataflow cleanups first, de-risking the register allocator; B flips arith-class benchmarks; C/D kill the call cluster). Gate per sub-rung: crosscheck byte-identical + both-medium + regenerate touched `.s` + A/B wall-ms.

- [ ] **NRG-A — VN: local value-numbering / redundant-load elimination.** **VN-0** value-number repeated `[rbx+k*16]` loads + their tag-checks within a straight-line box chain. **VN-1** drop store-then-reload of the same frame slot and dead `IR_VAR` boxes.
- [ ] **NRG-B — REG: register residency + static type tracking (the deep win).** **REG-0** liveness + single-type-throughout analysis (reuse FZ invariance-proof + GVA DT_I guard). **REG-1** allocate a proven counter to a callee-saved reg across a loop, spill only at exit/escape edges. **REG-2** extend to function-body locals proven DT_I (fibonacci `N`, INC `N`). Gate needs the register value re-materializable at every β/ω edge + fallback.
- [ ] **NRG-C — INL: leaf-function inlining.** **INL-0** inlinability analysis in lower (≤N statements, non-recursive, statically resolved — the existing `__proc[]` table already proves the static-call set). **INL-1** splice the callee's four-port graph at the call site with param→arg substitution. `R=INC(R)` → `R=R+1` inline. Gate: byte-identical + recursion guard + A/B func_call/func_call_overhead.
- [ ] **NRG-D — PROTO: specialized call protocol** (non-inlinable/recursive calls). **PROTO-0** per-call-site block save/restore through known param/return cells resolved at preamble; no per-call loop, no fastpath re-check. Needs the **nv set** ledger stamp (currently only requested). Gate: byte-identical + A/B fibonacci (recursive)/roman.
- [ ] **NRG-E — BOPT: branch chaining** (scaffolded `src/opt/branchopt.c` `bopt_chain`, UNWIRED). **BOPT-0** map IR ports→Byrd ports first (read-only, a written port→field table, no code change). **BOPT-1** wire as a LOWER→EMITTER pass + cycle guard (honor the prior crash: γ-spine doubles as operand-recovery structure — rechain only edges proven not to shorten that walk). **BOPT-2** dead-BB drop + fall-through layout. **BOPT-3** copy propagation (collapse temp-forwarding chains the four-port templates emit — a temp that just forwards a child's value should have its consumer read the source directly; dead-temp elimination follows).

**Prior investigation (session 27): general BOPT in-place γ/ω mutation attempted, crashed (γ-spine is overloaded as operand-recovery structure), quantified low corpus-wide yield (34/1686 collapsible branches, 0% in hot loops) — CLOSED as a perf lever, `src/opt/branchopt.{c,h}` retained as a dead-end record, full writeup in `docs/BOPT-DESIGN.md`.**

---

## ▶ SCAN-GEMINI — every scan/match primitive gets its variable-operand twin

Each scan/match primitive currently folds a literal argument at compile time; the variable-argument case (`POS(N)` where N is a runtime value, not `POS(3)`) needs its own twin arm. `bb_match_span_var.cpp` is the reference twin already landed.

- [ ] **GEM-0 — make the variable-cset pattern REACH the per-primitive native boxes (the prerequisite everything else sits behind).** A non-literal `SUBJ ? PAT` currently bombs in the whole-statement scan box (`bb_scan_stmt.cpp`, "non-literal pattern needs native PB-RB graph") rather than reaching any per-primitive box — so none of the twins below can even be exercised via the common inline-match path until the non-literal case is routed into the native decomposed-graph builder instead of bombing. Same root as DEMO-PAT GAP-3. Multi-file pattern-engine work, budget a full session, MONITOR-first if a partial graph diverges.
- [ ] **GEM-1 — ANY_VAR twin.** New opcode + template mirroring `bb_match_span_var`: fetch cset by name at runtime, same single-char test.
- [ ] **GEM-2 — NOTANY_VAR twin.** As GEM-1, inverted test.
- [ ] **GEM-3 — BREAK_VAR twin.** As GEM-1, BREAK semantics (advance until a fetched-cset char).
- [ ] **GEM-4 — BREAKX_VAR twin.** As GEM-3 plus BREAKX's retry-extend semantics.
- [ ] **GEM-5 — POS_VAR/RPOS_VAR** (integer fetch + same compare, one new opcode pair or a name-driven arm).
- [ ] **GEM-6 — TAB_VAR/RTAB_VAR** (same shape).
- [ ] **GEM-7 — LEN_VAR** (same shape).
- [ ] **GEM-8 — scan-family variable arms in inline context** (`bb_scan_many`/`bb_scan_upto` gain the variable arm the way `bb_scan_any` already carries both; then relax `scan_pat_m3_native_safe`).
- [ ] **GEM-9 — completeness gate** (`test_gate_scan_gemini.sh`: every primitive has both a literal and variable box reachable, proven by compiling one of each).

**Prereq reads:** `bb_match_span_var.cpp`, `lower_snobol4.c` TT_SPAN/ANY/NOTANY/BREAK/BREAKX/POS/TAB/RTAB/LEN arms, `emit_bb.c:2560`/`:3035`, `emit_core.c:371-383`, `rt.c:98 rt_nv_cstr`, `bb_regs.h`. NOTE: overlaps SCAN-TABLE (below) — GEM is correctness, SCAN-TABLE is speed; do GEM first.

### SCAN-FOLD — the only active scan-class rung besides GEM
- [ ] SCAN-FOLD-0 — coverage audit (no code change): confirm every literal-arg primitive seals its constant with zero frame load/runtime eval. Deliverable: a table.
- [ ] SCAN-FOLD-1 — close any unfolded literal arm the audit finds.
- [ ] SCAN-FOLD-2 — NSPAN (nullable span, new primitive, not a folding step) — PARKED until requested.

### SCAN-TABLE — baked character-class classifier (PARKED, "for later")
- [ ] SCAN-TABLE-0 — pick form (256-byte table vs bitmap; lean 256-byte, char MUST be unsigned).
- [ ] SCAN-TABLE-1 — literal cset → baked RO table.
- [ ] SCAN-TABLE-2 — runtime cset → built-once table (the bulk of the real win; covers `BREAK(WORD) SPAN(WORD)` in wordcount/claws5/treebank).

### SCAN-UNROLL — inline-compare for tiny literal csets (PARKED, "for later")
- [ ] SCAN-UNROLL-0 — threshold + dispatch (≤~10 chars → unrolled compares, else table/variable path).
- [ ] SCAN-UNROLL-1 — emit the unrolled compares per primitive semantics.

---

## ▶ Other open perf/correctness rungs (found on full re-read, previously under-captured)

- [ ] **SAB — string self-append capacity** (string_concat 8.8×, string_manip 11.3× behind SPITBOL). `S = S X` currently mallocs+copies O(n) every iteration. Fix: detect the compile-time self-append pattern `V = V <expr>`, back V with a string-builder cell `(ptr,len,cap)` that doubles cap — amortized O(1) append. **SAB-0** detect the pattern in lower · **SAB-1** string-builder runtime type + emit arm · **SAB-2** (⛔ correctness-critical: must preserve copy-on-assign value semantics — any read/alias of V must see an immutable snapshot, or SPITBOL's value semantics break) · **SAB-3** gate + A/B.
- [ ] **EVR — EVAL/CODE arena reclaim** (eval_dynamic 176× behind SPITBOL). Diagnosed: every EVAL call re-parses+re-lowers+bump-allocates fresh RX code that's never reclaimed. Confirmed NOT an OOM in this sandbox (completes in ~88s; the bench-script "crash" was its own 30s timeout) but still a real per-call leak. Fix: snapshot the BB pool top before `eval_build_chain`, run, restore (mprotect RX→RW back), free the per-call AST/IR. Lowest priority (one benchmark) — but see BBGC below for the more general version of this same problem.
- [ ] **PEEP-0 — peephole window.** Kill redundant spill/restore round-trips, `mov reg,reg` identities, dead stores, already-live-value lea/mov. Operate on the structured op-list before flatten. **PEEP-1** sub-expression elimination (CSE) within a BB — reuse an already-computed value instead of recomputing (complements BOPT-3).
- [ ] **CFOLD-0 — literal-expression fold in lower** (feeds BOPT). Fold compile-time-literal arithmetic, literal relops used as predicates (lets BOPT delete the dead arm), literal string concat, `SIZE('abc')`. **CFOLD-1** wire into BOPT (a folded always-true/false relop becomes an unconditional edge → BOPT prunes the dead branch). ⛔ **gate constraint:** fold only when operands are literals AND the op is side-effect-free AND the result matches SPITBOL semantics exactly (integer width/overflow, number↔string coercion, the null-string-identity rule already fixed once in `str_concat_d`) — each folded form must be verified against the oracle individually, not just "looks obviously foldable."
- [x] **FZ-5 — match-site direct reference** (LANDED s15-a `3fcd5532`, statement-level; see SESSION STATE for measurement + disclosures + the DEFER-ZLS follow-on) (follow-on to FZ-2/3/4, which are all landed). When the compiler proves a stored pattern variable is once-assigned-invariant, inline the sealed matcher head at the match site and skip the per-match `rt_defer_get_pat_fn` fetch — the real remaining hot-loop win for stored patterns. (Note: FZ-5a/5b under this name already landed per the SN4-PAT section above for the *inline* case — re-verify this item's exact remaining scope against that landing before starting; may be smaller than it looks or already done.)
- [ ] **GVA-4 — rbp GST hash path for runtime indirect refs** (optimization, not correctness — GVA-0's forwarding already guarantees correctness regardless). `$X` where X's runtime value names a GVA-backed variable: skip the full GST hash walk via a `gva_lookup_by_name`-style probe. Do only after GVA-0..3 are green and benchmarked (they are). Marginal impact expected (only helps indirect-ref-heavy code).
- [ ] **REC-COV — community-recognized corpora coverage.** Extend coverage into `corpus/programs/gimpel/` (145 `.sno`, no `.ref` yet) and `corpus/programs/snobol4/demo/` (18 `.sno`). Steps: honest runner + oracle-gen refs + triage table → gimpel → demo → promote counts to hard floors → re-ground any "Nx faster/slower" headline claims against the wider corpus, not just the 16-benchmark set.
- [ ] **BBGC — slide-compaction GC for the BB code arena.** ⬇ **LOW PRIORITY / EXPLORATORY — explicitly do NOT start ahead of GVA-4/REC-COV** (design-only spike, picked up only if the bump arena's lack of compaction becomes a real ceiling; the current LIFO-watermark EVR fix bounds but doesn't compact, so a long-running EVAL/CODE-heavy program could eventually exhaust it despite EVR). Vision: mark live BB blobs, sweep, slide survivors down to compact, re-stitch only the four ports. Viable because everything else baked into a sealed blob (register-relative state, RIP-relative-to-adjacent-RO, immediate constants, absolute-address runtime calls) needs zero fixup under a move — only the four ports (`rel32` call/jmp) do. Real hazard to solve: stored pointers to blob entry points held OUTSIDE the pool (EVAL cache, DT_C/DT_E descriptors, the label→code map, live return addresses on the C stack during an active BB frame) — mitigated by entry-table handle indirection + compacting only at a safepoint with no active BB frame. **BBGC-0** measure+baseline (build an EVAL/CODE stress program that actually exhausts the current bound) · **BBGC-1** root enumerator (read-only) · **BBGC-2** entry-table handle indirection · **BBGC-3** mark · **BBGC-4** safepoint discipline · **BBGC-5** sweep+slide-compact+re-stitch ports · **BBGC-6** trigger on overflow · **BBGC-7** gate (bounded-pool EVAL-heavy stress matches oracle, zero corpus regressions).

**Correction while re-reading (Claude Sonnet 5, 2026-07-08): OPSINGLE (delete `operand_aux`, one operand channel only) — this file's copy still lists it as open work, but it is ALREADY DONE.** PLAN.md's own IR-REDUCE row and a live grep both confirm `bb_operand_aux_set/get` has zero references outside `*-parked-*` files (exempt per PARK-NEVER-DELETE) — `IR_t.operands/n_operands` is the sole operand store, `SCRIP a3de01d2`. This file's own copy of the mandate was simply stale; do not re-attempt it.
- MON-RE-0..6 (the monitor): see its own section above, fully landed.
- FZ-4/FZ-5a/FZ-5b (pattern constant-folding + frozen-head inlining): landed, measured wins (string_pattern 1.13×, pattern_bt 2.81×), see `docs/` if the mechanism is ever needed again.
- DCR-1/2/3 (direct-call protocol): landed, func_call/fibonacci call-cluster cost roughly halved (still net-slower than SPITBOL on the call cluster specifically — see NRG above for why and what's next).
- RPF (relop GVA-parity) + table-get double-walk elimination: landed, moderate wins where relop/table-get dominate.
- KEYWORD-CONCAT bomb (`X = &UCASE &LCASE`): fixed, one-line root cause (nonleaf-flag gap in `sno_seq_flatten_ops`).
- Full three-way SCRIP/SPITBOL benchmark comparison: re-run several times across sessions: SCRIP is slower than SPITBOL on most benchmarks (the NRG rung above is the current plan to close this), competitive-or-faster specifically on pattern-heavy work.
