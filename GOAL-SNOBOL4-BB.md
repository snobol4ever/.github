# GOAL-SNOBOL4-BB — SNOBOL4 → native x86 Byrd-Box codegen

Frontend: SNOBOL4 → shared IR → BB emitter (mode-3 `--run` / mode-4 `--compile`). Protocol: RULES.md; template/encoder work requires the BB-CODEGEN design set (ARCH-ICON.md + GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md) FIRST. The crosscheck watermark is SHARED STATE across parallel sessions — re-prove at session start AND close. History pruned 2026-07-30 (Lon directive): completed cursors compressed into the HISTORY INDEX; full text lives in the FINDING docs + git (this file pre-shrink = `.github` `2f3fd45a`).

---

## ⛔⭐⭐ THE MODEL — READ THIS BEFORE ANYTHING ELSE. IT IS THE ONE THING SESSIONS KEEP GETTING WRONG. (Lon, s21x-r)

**THERE IS NO GRAPH FRAME. THERE IS NOT SUPPOSED TO BE ONE. NOT ONE PROGRAM REQUIRES PRE-ALLOCATION.**

Every BB, without exception:
1. **allocates its own storage at α** — one `sub rsp, K`, K = a few bytes, *its own*;
2. **reads and writes ONLY its own cell** — never `[rsp + graph_offset]`, never a slot someone else allocated;
3. **releases at γ/ω**;
4. **jumps to the next box.**

Boxes are wired **statically** (label / rel32) or, where the target is data, **dynamically** (indirect through a cell). They just run and allocate their own memory. **That is the entire execution model.** No pre-allocation. No whole-graph carve. No prefix-summed prologue. No zero-fill. No layout pass. No "region."

**THE CARVE THAT EXISTS TODAY IS A CORPSE, NOT INFRASTRUCTURE.** `flat_frame_bytes` is debt. The **1057 `FR`/`FRQ`/`FRQB` reader sites across 108 templates** are debt. They are what is left of the pre-BB world, and the per-BB cells are currently running *alongside* them (301/317 programs carry both). The job is not to reason about the frame, tune it, shrink it, or decide which constructs "need" it. **The job is to delete its customers until it has none, then delete it.**

### ⛔⭐⭐ HEAD RUNG — CARVE-ERAD: DELETE IT. THE MANIFEST IS ~62 SITES AND ONE LINE. (Lon directive, s21x-r, stated three times)

**Lon: "Remove the whole-graph carve code. Eradicate it from the source. We would not want you to continue getting confused for days at a time. You keep calling the function even though I keep telling you not to."** The directive is correct and the reason is correct: while the frame exists, sessions reason about it.

⭐ **THE 1065 `FR`/`FRQ`/`FRQB` READER SITES NEED ZERO EDITS.** They already resolve through ONE function, and that function carries BOTH authorities on one line:
```c
x86_asm.h:373  inline int x86_frame_off(int off) { return (...) ? off : off + (int)_.op_flat_disp + _.op_zdepth; }
```
`op_flat_disp` is LOWER's STATIC prefix sum — the carve's ADDRESS side. `op_zdepth` is the live per-BB side. **Delete the static term and every reader converts for free.** s21x-r twice proposed converting templates by hand (108 files / 1065 sites) — WRONG both times; that is the failure mode this section exists to name.

**MANIFEST (measured s21x-r):** `flat_frame_bytes` **31 sites** · `op_flat_disp` **24 sites** · carve/`jcon_value_region` emission **7 sites** · `x86_frame_off` **1 line**. That is the whole eradication.

**⛔ ORDER MATTERS — THESE ARE ONE OPERATION, NOT THREE.** `op_zdepth` today is **per-node, not cumulative** (`emit.cpp:821  g_emit.op_zdepth = x86_fc_on() ? (int)g_emit.op_fc_bytes : 0;` — the box's OWN K). Box A carves K, jumps to B; B's rsp is K deeper and B cannot see it. So:
1. **Make `op_zdepth` the RUNNING SUM of live carves at each node** (ZTOS-1's stated design — *"the emitter tracks live depth at compile time"* — is HALF BUILT: allocator spends per box, accessor compensates per box, nobody sums). Well-definedness at merges and β re-entry is the real design content; the SUSPENDED-CELL law (s21x-l) is the constraint.
2. **Drop `op_flat_disp` from `x86_frame_off`**, then delete its 24 sites.
3. **Delete `flat_frame_bytes`, the 7 carve sites, and the five-kind exclusion list** (emit.cpp:811) together.

**EVIDENCE THE HOOK IS ALREADY RIGHT (s21x-r, `SCRIP_BB_ALLOC_ALL=1`, committed):** with the exclusions dropped so EVERY box self-allocates through the α hook, **all 31 boxes already on the live-depth authority pass 31/31**. No template was converted. Corpus-wide it is m4 214/101 DIV=21 purely because the newly-armed boxes displace consumers still reading the static term — i.e. step 1 above, and nothing else.

**GATE:** `SCRIP_BB_ALLOC_ALL=1` passes **317/317 both modes** → then the deletion is a no-op removal of dead code rather than a regression. Bisect instruments already committed: `SCRIP_BB_ALLOC_ALL=1`, `SCRIP_NO_GRAPH_CARVE=1`, `SCRIP_BB_ALLOC=0`.

⚠ **DO NOT cut the 62 sites before step 1 lands.** Deleting the static authority while `op_zdepth` is still per-node regresses ~38-50 programs with no path back to green — a half-finished eradication is the one outcome worse than the frame existing. Step 1 IS the eradication; steps 2-3 are the cleanup that follows it in the same slice.

### ⛔ THE FAILURE MODE, NAMED — s21x-r hit every one of these in one session

- **Treating the frame as a design element to be reasoned about.** It is legacy. When a rung starts asking "which constructs need the frame?", the rung is already wrong.
- **Clamping/removing the carve while readers still address into it, then reading the breakage as necessity.** This is TAUTOLOGICAL: shrink the backing store, unconverted readers fault. s21x-r ran exactly this, got 38 failures, and wrote "38 programs still NEED the whole-graph carve." **VOID.** The 38 is a **DEBT COUNTER** — programs that reach a box nobody has converted yet.
- **Inventing an architectural rationale for the debt.** s21x-r claimed Ch.18 backtracking "needs a stable base to unwind to." **WRONG.** Backtracking is the FOUR PORTS plus LIFO per-box claim/release. `test_icon.c`, the reference embodiment: *state lives in the per-box ζ block, allocated fresh per α-entry; code is shared.* No frame. A suspended box keeps its own cell and re-enters at β — that is all "unwinding" is.
- **Misreading law 4's four RBP constructs as licensing a graph frame.** They name **which BASE REGISTER a construct uses for its OWN housekeeping** (ARBNO's children are variable-length). Law 1 forbids a graph frame outright: *never a site allocating more than a few bytes for one BB.*
- **Mistaking the SPELLING sweep for the CONVERSION.** s21x-q closed the ζ-spelling sweep (raw rsp → `x86_zclaim`/`x86_zrelease`, remainder classified NOT-ζ) and that closure is sound — but spelling closure is **not** conversion. 6 of 140 templates self-allocate; 108 still read the frame.

### ✅ THE ONLY DISCRIMINATING TEST

An experiment that removes the carve proves nothing while readers remain. **The discriminating move is the converse: convert one box's readers to its own cell, then watch the carve requirement DROP.** Progress = monotone decrease of the `SCRIP_NO_GRAPH_CARVE=1` failure count. Each failure names the box it is waiting on.

**COMPLETION GATE:** `SCRIP_NO_GRAPH_CARVE=1` passes **317/317 both modes** → then DELETE the carve and `flat_frame_bytes`, do not shrink them. This supersedes "zero raw rsp in templates" as the ζ gate: that one measured spelling, this one measures the thing actually wanted.

### WHY IT KEEPS GETTING MISREAD

The frame is *present in every `.s` file*, so it looks like infrastructure holding the program up. It is not. It is a corpse the new machinery is running beside, and every session that reasons about "when do we need it" has already accepted a premise the design rejects.

---

## ⛔⭐⭐ LIVE CURSOR — s21x-w (2026-07-31, Claude: ⭐⭐ **ZD-2 VERDICT WIDENING: THREE KINDS ONTO THE CELLS, DECLINES 1060 → 881, WATERMARK EXACTLY NEUTRAL.** SCRIP `5885d24f` (2a) + `4af624a2` (2d/2e). Crosscheck **m3 232/85 · m4 229/86/2 · DIV=1 {W04_arbno_basic}** — identical to the s21x-v record, **ZERO broke ZERO fixed both modes**. Arms proven non-vacuous by planner diag over 317 programs: BINOP 224 armed nodes · COERCE_NUMERIC 118 · CMP_TEST 97.)

⭐⭐ **THE FINDING THAT OUTRANKS THE RUNGS — A SECOND DUPLICATED AUTHORITY, FOUND AND CLOSED.** The drive-loop staging site (emit.cpp ~2308) **re-derived the operand count inline** — `(UNOP|ASSIGN)?1:BINOP?2:0` — instead of calling `zd_nops()`. Planner and driver held TWO OPINIONS of a kind's operand count. The instant `zd_nops` grew a kind, `zd_plan` ARMED its statements while staging still computed `_no=0`, left `g_zd_read` all-zero, and **every `ZOPQ`/`ZOPD` rendered `[rsp+0]` = the box's OWN result cell.** Witness `914_lgt`: `lea rdi,[rsp+0]; lea rsi,[rsp+0]; call rt_cmp_d` — an uninitialized cell compared against itself, so every predicate always failed. **29 programs broke and NOT ONE crashed; they returned wrong answers.** Staging now calls `zd_nops` — ONE authority, new kinds staged automatically. ⚠ **`g_zd_k` at :2306 is STILL a second copy of `zd_plan`'s K formula** — same disease, unexploded because every kind so far wants 0 or 16; fold it in the moment a kind needs another K.

⛔ **BISECT TRAP, NAMED (a future session will otherwise misread this):** armed singly the two kinds looked mostly fine (COERCE_NUMERIC alone 15/16, CMP_TEST alone 11/16) and catastrophic together (0/16). **That reads like an interaction and is NOT one** — it is arming COVERAGE: a statement carrying COERCE_NUMERIC almost always has a CMP_TEST or BINOP consumer, so with either unarmed the run declines on the other and few boxes arm at all. One bug, two symptoms. Do not go looking for a pairwise mechanism.

⛔ **INSTRUMENT TRAP, NAMED (cost this session a false VACUOUS verdict):** `x86("comment", ...)` **does NOT render into the emitted `.s`.** Grepping compiled asm for a template's comment string reports a LIVE arm as vacuous. The valid arm-check is `SCRIP_ZD_DIAG=1` armed lines (`[ZD] h=`) counted per kind, or the box's actual instruction shape. **Validate any arm-check against a known-live control (`bb_binop_arith`) before believing a null** — this is the ZD-side twin of the RTX step-0 "grew an arm check" lesson.

⭐ **VERDICT NOW:** LIT_*, VAR-global, UNOP MNS/PLS/SIZE/CSET_COMPL, BINOP explicit arith/cset **+ CONCAT**, ASSIGN-global, **+ COERCE_NUMERIC, + CMP_TEST**. Still out: RELOP/XREP (no arms), VAR-local/ASSIGN-local (pin gate), and the jmp-entry wholesale decline.

⛔ **ZD-2b IS VACUOUS BY CONSTRUCTION — DO NOT SPEND A RUNG ON IT.** `BINOP_XREP` has **zero** references in `lower_snobol4.c` and **zero** nodes in the crosscheck corpus; `lower_common.c:141` is a constant FOLDER returning `DESCR_t`, not an IR producer. It is reachable only from another frontend, so it belongs to that language's ladder, not this one.

✅⭐ **ZD-2j LANDED — `IR_DEREF` + `IR_ASSIGN_VAR`, AND THE 2f ARM IS NOW VALIDATED BY EXECUTION (SCRIP `2702f117`).** The two kinds 2f's null exposed as the real gate. Both by-value clones of the arith shape, both singular in dispatch (993/995), both keep a genuine `DT_FAIL` ω. DEREF = ONE operand, the `$` indirect reference (manual Ch.7 p.81: the operand's STRING VALUE names a variable, so ω is the name-not-resolvable edge). ASSIGN_VAR = TWO (operands[0] the VARIABLE, [1] the VALUE). **Declines 881 → 801. Newly armed: SUBSCRIPT 93 (was ZERO), ASSIGN_VAR 52, DEREF 30. Watermark did not move** — so the previously-unexercised 2f arm is now proven BY EXECUTION rather than by inspection, which is exactly what the `914_lgt` lesson requires (a wrong ZD arm returns wrong ANSWERS, not a crash, so an unexercised arm proves nothing until something arms it).

⛔⭐ **HISTORICAL — ZD-2f AS SHIPPED WAS VACUOUS — AND THE NULL IS THE FINDING (s21x-w, SCRIP `94ee83f1`).** The `bb_subscript` RVALUE arm is written, correct, and whitelisted for `n_operands==2` only (emit.cpp:992 sends other arities to `bb_section`, which has no ZD arm). **Declines did NOT move (881 → 881) and ZERO subscript nodes armed.** All 106 `IR_SUBSCRIPT` first-blockers merely re-exposed the next: **`IR_DEREF` 51 + `IR_ASSIGN_VAR` 51 + `IR_CALL` 4 = 106 exactly.** Subscript was never the gate — these statements are SNOBOL4 indirect-reference (`$`) and variable-assignment shapes. ⚠ **THE 2f ARM IS UNEXERCISED CODE**: committed so the next rung need not rewrite it, but NOT validated by execution. When DEREF/ASSIGN_VAR arm, any breakage is a candidate for the 2f arm too — validate it explicitly, remembering the `914_lgt` lesson that a wrong ZD arm returns wrong ANSWERS rather than crashing.

**⭐ NEXT — ORDERED (re-census first; first-blocker semantics re-rank on every rung):** (1) **the coerce tail** — `IR_COERCE_STRING` (17), `IR_COERCE_INTEGER` (7), `IR_KEYWORD_SNOBOL4` (13), `IR_FIELD_VAR` (5); expect 2e clones, likely one operand each, cheap. ⚠ lvalue-subscript assigns are a DIFFERENT node path (emit.cpp ~1259) — count the residue. (2) **ZD-2i tail**: `IR_COERCE_STRING` (17) + `IR_COERCE_INTEGER` (7) — read templates first, expect clones of 2e with one operand; then `IR_KEYWORD_SNOBOL4` (13), `IR_FIELD_VAR` (5). (3) **ZD-2h VAR-local/ASSIGN-local under the pin gate** — the depth-immune-base question, the "C-style RBP flavor" of the design (law 3/4: RSP until a genuine brick wall, then the RBP dance). (4) **ZD-5 MATCH FAMILY** — 227 declines, the actual 85/86-red target, 5a linear-match bridgehead first. (5) IR_CALL 483 = its own protocol rung. (6) The 130/131 clean-HEAD segv, unchased.

⚠ **CARRIED, UNCHANGED:** `test_string` m3 flake re-confirmed **regime-independent** this session (killswitch 8/20 segv vs armed 6/20, 40 runs) — **compare m4**, never m3, when gating. `130/131` still segv at clean HEAD.

## ⛔ PRIOR CURSOR — s21x-v (2026-07-31, Claude: ⭐⭐⭐ **ZD-1 LANDED — THE RSP FORTH-STYLE PER-BB STACK IS DEFAULT-ON CORPUS-WIDE AND ABOVE WATERMARK BOTH MODES.** SCRIP `d492b909`. WATERMARK **m3 244/92 · m4 238/90/8** over **336** programs (corpus grew from 317 — crosscheck gained tests; do not compare raw counts to the 229/88 era). FULL LEDGER, truncation-proof: fixed `028_arith_unary_minus` + `literals` in BOTH modes, **ZERO broken** (`SCRIP_ZD=0` killswitch in-harness reproduces baseline 242/236 exact). s21x-u's difference-of-two-depths law is now MECHANISM, witnessed in machine code both media.)

⭐⭐ **THE FIVE LAWS THAT 20 SESSIONS WERE MISSING — each named, implemented, witnessed (full text: commit `d492b909`):** (1) the offset is `δ_out(C) − δ_out(P)`, staged per consumer per operand (`op_zread[k]`) — a single depth cannot express it; (2) the ONE walk (`zd_plan`, emit.cpp) follows the **γ wires from each `bb_src_of` head** — statement members INTERLEAVE in `nodes[]` under right-first lowering (058: entry i=3, consumer i=5, foreign entry i=4 between), so any array-range segmentation resolves operands across statements (the first wide run's 97/96 collapse); (3) **all-or-nothing per STATEMENT** — the s21x-o reader-frontier law enforced at the only locally-decidable scope; one legacy reader or foreign carve declines the whole statement, which is what makes default-on every-BB arming survivable; (4) computed offsets ride the **raw channel** `[rsp# + N]` (the FOUR modes: `ZRES`/`ZLOC`/`ZOPQ`/`FR`) — no encoder-side compensation may touch a driver-computed number in either medium; (5) the port discipline keyed per-node (`op_zres`): **α carves once, β does NOT re-carve, γ does NOT release (the cell IS the product), ω releases own K, statement-terminal γ releases the statement (`op_zgpop`)** — rsp returns to statement entry on every crossing edge, statement scope by construction.

⭐ **VERDICT WHITELIST THIS RUNG (the value spine):** LIT_*, VAR-global, UNOP MNS/PLS/SIZE/CSET_COMPL, BINOP **explicit** arith/cset ops (⛔ CONCAT/RELOP/XREP dispatch to templates WITHOUT ZD arms — arming them was the 017 falsification), ASSIGN-global. ⛔ **JMP-ENTRY CHAINS DECLINE WHOLESALE** (`flat_jmp_entry` gate in `zd_plan`): EVAL/CODE/PAT$ fragments speak their own value-handoff protocol; a ZD cell + terminal gpop starves it — the `1019_eval_string` casualty, the ledger's ONE real break, closed by this gate. Instruments: `SCRIP_ZD=0` kill, `SCRIP_ZD_ONLY/SKIP` per-BB nid csv, `SCRIP_ZD_DIAG=1` full verdict+number trace.

⚠ **TWO FINDINGS, NEITHER ZD:** (1) `130_pat_two_star_fence_concat_outer` + `131_pat_boolean_expr_grammar` **segfault at clean HEAD `1c0124f1`**, deterministic, direct invocation, killswitch-immune, SKIP-immune, `env -i` and env-padded alike (repro: `./scrip --run corpus/crosscheck/patterns/130_*.sno`) — the harness records them only because their FAIL lines previously fell past the list truncation; unchased, out of this rung's scope. (2) ⛔ **THE CORPUS RUNNER TRUNCATED ITS FAIL LISTS TO `head -40` PER MODE** (93+ fails ⇒ 53+ invisible), which manufactured phantom regressions when real fixes scrolled new names into the window and cost this session hours of false chase — fixed to `-400` at SCRIP `67b90e2b`. **Never diff a truncation window; the COUNTS are authoritative, the printed list is not, on any log predating this fix.**

**⭐ NEXT — ORDERED, EACH GATED BY THE FULL-LEDGER CORPUS DIFF:** (1) **WIDEN THE VERDICT** kind-by-kind — CMP_TEST and COERCE_NUMERIC ZD arms (both call-shaped, same pattern as unop); BINOP CONCAT/RELOP/XREP arms in their own templates; VAR-local/ASSIGN-local need the frame-read question answered first (a local varslot read under an unpinned graph is displaced by ZD depth — either require the pin or copy through the cell). (2) **THE MATCH FAMILY ONTO THE CELLS** — the actual 92-red target: migrate FRQ/`op_flat_disp` readers to the four modes statement-by-statement; the per-statement verdict makes each migration individually measurable, which is the property every prior frontier lacked. (3) **TEACH THE JMP-ENTRY PROTOCOL THE CELL CONVENTION** (EVAL/CODE/PAT$ value handoff), then drop the wholesale decline. (4) **LEGACY ARM RETIREMENT** — once a kind's ZD arm covers every reachable staging, delete its vfc/vfcu/vfcb arm + the `op_fc_disp` registration it keyed on (the in-place net-zero tricks are the shyness ZD exists to end). (5) The 130/131 HEAD segfault, chased as its own finding.

## ⛔ PRIOR CURSOR — s21x-u (2026-07-30, Claude: ⭐⭐ **STEP 1 IS FALSIFIED BY MEASUREMENT (−170 PROGRAMS) AND THE "ORDERING DEFECT" IS THE LOAD-BEARING DISCRIMINATOR.** Implemented s21x-t's NEXT(1) faithfully — `op_zdepth` from the FINAL `op_fc_bytes`, derived at read time — and measured m3 229 → **59**. REVERTED, tree clean at SCRIP `1c0124f1`, nothing landed. Also measured: **all 88 m4 reds compile CLEAN, zero bomb stubs** — they are addressing/semantic divergences, not mid-design shapes. Step 2 SIZED: **46 colliding templates + a 15-template safe pilot set.** WATERMARK m3 229/88 · m4 227/88/2 · DIV=1 (m3 oscillates 229/230 on the `test_string` ASLR flake — **compare m4**). Full text: `FINDING-2026-07-30-CLAUDE-SN4-ZRUN-2-THE-LINE-ORDER-IS-THE-DISCRIMINATOR-AND-STEP-2-IS-46-TEMPLATES-OF-PLUMBING.md`.)  ⚠ **SUPERSEDED by s21x-v above: the difference-of-two-depths prediction is LANDED (`d492b909`); the STEP-2 FRX plumbing route below was never taken — ZD arms + the raw channel replaced it. Retained for the DO-NOT-RETRY laws and retractions only.**

⛔⭐⭐ **THE OPERAND OFFSET IS A DIFFERENCE OF TWO DEPTHS, NOT A DEPTH. THIS SUPERSEDES THE STEP-2 PLAN BELOW AND EXPLAINS EVERY FAILED ATTEMPT SINCE s21x-s.** For consumer C reading producer P: `offset(C→P) = (δ_at_read(C) − δ_out(P)) × 16`, where δ is depth-on-entry in cells from the forward walk. On `028` (chain `n0_lit → n1_unop → n2_assign`, δ 0→1→2): δ_out(n0)=1, δ_at_read(n1)=2 ⇒ **offset 16**, which is correct and adjacent. The emitter produces **32 = n0's carve + n1's carve = the STATEMENT TOTAL.** `op_zdepth` is depth-since-statement-head; an operand needs depth-since-PRODUCER. ⭐ **A single depth cannot express this no matter how well computed** — two consumers at different depths reading one producer need different constants, and only the difference is invariant. s21x-s (running sum), s21x-t (two variants), s21x-u (derived-at-read, −170) each refined a single depth; all four were therefore mis-specified, not mis-implemented. **SMALLEST TEST: record δ_out per producer, emit the difference at the read site, check `028` prints `-5`.**

⚠ **RETRACTED THIS SESSION (s21x-u), do not inherit:** (1) the FRX/FRQX cross-box accessor — built, measured **byte-identical `.s`**, because the live path for unop is `ZTOS`, not `FRQ(_.op_sa)`; the 46/15/29 census stands as fact but the plan built on it does not. (2) An ARBNO-based impossibility argument — **WRONG**: ARBNO wires children through PORTS and never reaches for operands at a computed offset; iteration that carves and releases per pass is depth-NEUTRAL at the join. Only a cross-box read that SPANS dynamic depth fails. 16 carves/20 releases in `052` is correct LIFO (one α carve, release on both γ and ω), not an imbalance.

⭐ **THE EXCEPTIONAL SET IS SMALL, CHARACTERIZED, AND ALREADY COUNTED.** δ_in(C) is well-defined iff all predecessors agree on δ_out; the only true failure is a merge where they disagree (multi-entry, β re-entry with live siblings, unequal-arm merges that don't rebalance). s21x-r already measured it: **32 multi-entry graphs** (up to 11, `test_string`) and **44 armed decisions / 44 distinct armed graphs 1:1, zero multi-entry armed** — the arming predicate is empirically the single-entry predicate. Those need a depth-immune base (rbp) with a seed that DOMINATES every spanning producer→consumer edge; nesting costs a link chase, and addressing ≠ liveness (a producer must outlive the read regardless of base). ⚠ FB-STMT migrates nodes OFF rbp onto rsp inside pinned graphs — against this analysis that removes the exception from the population that needs it.

⛔ **DO NOT RETRY STEP 1.** `emit.cpp:1063` resets `op_fc_bytes=0` per node and `zeta_storage.c:1078` returns 0 from `zw_carve_k` whenever `fc_geom` grants, so at the `:825` choke a GRANTED node reads 0 (adds nothing — it is rebased via `op_fc_base` in arm 2) while a CARVE-ONLY node reads its own K (adds K). **The source-line order ALREADY implements the granted-vs-carve-only split.** Unifying `op_zdepth` across the classes makes granted nodes double-add on top of the `op_fc_base` rebase — that is the entire −170. The ZTOS-1 "allocator and accessor cannot hold two opinions" claim is FALSE at HEAD, but the two opinions are the FEATURE.


## ⛔ PRIOR CURSOR — s21x-t (2026-07-30, Claude: **STEP 1 DIAGNOSED AS A PER-READER SELF-DOUBLE-ADD, NOT A PER-SPINE PREFIX.** ⚠ Its NEXT(1) was then IMPLEMENTED AND FALSIFIED by s21x-u at −170 programs — see the LIVE CURSOR above before acting on anything in this block. ⚠ Its witness table says `028_arith_unary_minus` is "74 lines"; the file is THREE lines (`OUTPUT = -5`) — stale detail, the `+16` conclusion itself still corroborates.) The s21x-s plan of record ("emit-time ζ-depth RUNNING SUM") was implemented exactly as specified and MEASURED WRONG; so was the obvious alternative (delete `op_zdepth` from the static arm, −36 programs). Both REVERTED, tree clean at SCRIP `1c0124f1`. WATERMARK re-derived and UNCHANGED: m3 229/88 · m4 227/88/2 · DIV=1. Full text: `FINDING-2026-07-30-CLAUDE-SN4-ZRUN-THE-STATIC-ARM-DOUBLE-ADD-IS-PER-READER-NOT-PER-SPINE.md`.)

**⭐ THE ENABLING FACT (measured, not assumed):** `fc_leaf_walk` sums `fc_geom`-granted cells ONLY (lower_snobol4.c:1066) and `zw_carve_k` returns 0 whenever `fc_geom` grants (zeta_storage.c:1078) — **the two carve sets are DISJOINT**. So (a) no term over the carve set can double-count `op_flat_disp`, and (b) `fc_leaf_register(x, pfx + own)` already carries **that node's own K** — but only for granted nodes. **The two classes need OPPOSITE compensation and one rule serves both today. That is the bug.**

**⭐ THE WITNESS — `028_arith_unary_minus`, 74 lines, two readers, opposite carves.** `n1_unop` (own carve 16) needs `[rsp+16]`, emits `[rsp+32]` — error +16. `n2_assign` (own carve 0) needs `[rsp+0]`, emits `[rsp+0]` — error 0. **The error EQUALS the reader's own carve and is zero when it carves nothing** — the signature of a self-double-add. A spine prefix would displace both readers equally; it does not. This one table falsifies the running sum.

**TWO FALSIFIED EXPERIMENTS, DO-NOT-RETRY (both in the FINDING):** (1) running sum of upstream suspended carves, subtracted — fixes `n1` BY COINCIDENCE (prefix happened to equal n1's own 16) and breaks `n2` to `[rsp+-16]`. (2) drop `op_zdepth` from the static arm globally — fixes the witness, **m3 193/124 · m4 190/125, −36 programs**, because `zw` carve-only readers have `own=0` in the prefix and `op_zdepth` is their ONLY compensation.

**⭐⭐ ZDUMP RUN — QUESTION RESOLVED, THREE OVERTURNS (full tuple in the FINDING):** `[ZDUMP] nid=1 off=0 disp=0 zdepth=0 fcbase=32 fcbytes=16` / `nid=2 off=16 disp=0 zdepth=16 fcbase=-1 fcbytes=16 -> 32`. (1) **`op_flat_disp` IS 0 ON EVERY NODE** — the static prefix is NOT involved in this defect; stop reasoning about it on this witness. (2) **LOWER's `op_sa` is ALREADY carve-inclusive** for a cross-box read (`off=16`, not 0), so `off` alone is correct and `op_zdepth` is the entire error. (3) ⭐ **`op_zdepth` is decided by SOURCE LINE ORDER:** `emit.cpp:825` reads `op_fc_bytes` AFTER `zw_carve_k` sets it (:818) but BEFORE the `fc_geom` case arms set it (:872+), so a GRANTED node measures `fcbytes=16, zdepth=0` while a CARVE-ONLY node with identical 16 bytes measures `zdepth=16`. **The allocator and accessor DO hold two opinions — the field's own comment and ZTOS-1's both claim that is impossible by construction. That claim is FALSE at HEAD and is the mechanism under this rung.**

**NEXT — TWO ORDERED STEPS, NOT THE CONDITIONAL.** (1) **Fix the ordering defect:** set `op_zdepth` from the FINAL `op_fc_bytes` (after the case arms) so the two classes stop differing by accident — a real behaviour change, measure it ALONE. (2) Then split the two offset MEANINGS colliding in x86_zop arm 4: a CROSS-BOX read (`op_sa`-derived, carve-inclusive ⇒ must NOT add `op_zdepth`) vs an OWN-CELL read (`op_off`/`x86_scratch_off`-derived, 0-based ⇒ MUST add). Carve-only nodes have `fcbase=-1` so arm 2 never rebases them and both meanings land in arm 4 indistinguishable — that collision, not the node class, is why Experiment 2 fixed 028 and cost 36 elsewhere. The fields are already distinct, so the discriminator may need no new plumbing.

**SUPERSEDED — THE CONDITIONAL AS FIRST STATED.** `op_flat_disp` is correct for exactly one node class: granted ⇒ static arm must NOT add `op_zdepth`; carve-only ⇒ it MUST. ⚠ **The predicate is NOT simply `op_fc_base >= 0` — resolve that FIRST:** ZPROBE reports `n1_unop` as fc_geom-DECLINED (k=16), which by that rule would say "add", yet adding is what emits the wrong 32. Suspects: `op_sa` for a cross-box read already expressed in the PRODUCER's granted frame, or `fc_leaf_register` reached through the pair/ALT recursion arms (:1057/:1062) at a different `pfx`. **Decisive instrument = one-line dump of `(nid, op, op_sa, op_flat_disp, op_zdepth, op_fc_base)` at the `x86_frame_off` call site on this 3-node program.** One run settles it.

**REFRAMING THE ASK (verified live):** ZOP-1 four-mode operand parameterization, ZTOS sliding offsets, allocation through `x86_alpha`/`x86_beta` (GLUE-3), BOTH glue codes (`bb_glue_flat` / `bb_glue_framed`), and per-BB dynamic arming are **ALL ALREADY LANDED**. The open work is not the allocation mechanism — it is the ADDRESSING RULE the readers use.

## ⛔ PRIOR CURSOR — s21x-s (2026-07-31, Claude: ⭐⭐ **WHOLE-GRAPH CARVE DELETED** (Lon directive). `flat_frame_bytes = 48` permanently; `SCRIP_NO_GRAPH_CARVE` deleted. `zw_carve_k` one-authority extracted. Two plan claims falsified. WATERMARK m3 229/88 m4 227/88/2 DIV=1. NEXT: emit-time ζ-depth running sum — the one remaining step to turn the 88 reds green.)

**⭐⭐ SCRIP `1c0124f1` CARVE-ERAD landed.** The value-region (`rg = jcon_value_region`) is deleted from `g_emit.flat_frame_bytes`. The residual 48 = wire header only (outside-γ/ω wires + saved rbp, this box's own four-port protocol). `SCRIP_NO_GRAPH_CARVE` bisect flag deleted — it simulated this and is now permanent.

**TWO PLAN-OF-RECORD CLAIMS FALSIFIED, WRITTEN INTO SOURCE (do not re-attempt):**
1. **"Drop op_flat_disp from x86_frame_off and every reader converts for free"** — WRONG. A/B: dropping the term costs 3 programs (226/91 vs 229/88 both modes). `op_flat_disp` is the prefix sum over **fc_geom-GRANTED suspended cells** (S10c) + 32B HEAD constant — those survive the carve deletion and still need compensating. The 24 sites are a **narrowing** not a deletion; term retained.
2. **"Teach the static prefix about per-BB carves at lower time"** — WRONG. `fc_leaf_walk` runs at `lower_snobol4.c:1771`; `zls_build` arrives at `:2411` — 640 lines later, same pass. `zw_carve_k` returns 0 for every node when called from the prefix walk. The static authority cannot be taught per-BB sizes at any price; the sizes haven't been decided yet. DO-NOT-RETRY note in source.

**LIVE BUG LOGGED:** `test_string` segfaults nondeterministically (rc=139, ~27% of environment sizes, ASLR + rsp sensitivity). Same multi-entry graph s21x-r measured at 11 entries. Not chased; likely cured by step 1.

**⭐ THE PROBE INSTRUMENT** (`SCRIP_ZPROBE=1`): instruments `walk_bb_node` with nesting depth + per-node K. Used to prove emission is FLAT (max nesting depth 0; all 85 FILL sites in the driver, templates never recurse). Retained in source, gated, zero cost when off.

**NEXT — STEP 1 = EMIT-TIME ζ-DEPTH RUNNING SUM.** The 88 reds go green when this lands. Shape: abstract interpretation over the four-port graph computing depth-on-entry per box. NOT a lower-side prefix (falsified above). NOT a save/restore around the emission walk (emission is flat). Must handle merges (γ→next) and β re-entry under the SUSPENDED-CELL law. The one function that matters is `walk_bb_node_inner` at the choke; `op_zdepth` is already the right field (set unconditionally from `op_fc_bytes` at emit.cpp:821). The question is what `op_zdepth` for a given box should be when other boxes have already carved on the same spine.

## ⛔⭐ PRIOR CURSOR — s21x-r (2026-07-30, Claude: ⭐⭐ **THE STF "DEFECT" WAS A PROCESS-SCOPE PLANNER FLAG, AND THE ARMED PATH WAS NEVER BROKEN.** ZLEAK-1 + ZLEAK-2 landed; declined-graph leaks 285 → 24 → **0**; ALL-OR-NOTHING PER GRAPH now provably holds corpus-wide. STF=1 is watermark-NEUTRAL. D2 and roman-under-gates both RETIRED by measurement.)

⛔ **ROUTING: READ `THE MODEL` AT THE TOP OF THIS FILE FIRST — every rung below assumes it.**

**LON DIRECTIVE (s21x-r):** "Complete ZETA CELLS on the RSP FORTH-style STACK." · "Remove the whole graph carve. We will not need any more." · "All your choices. I'm with you on this."

**LANDED (SCRIP `efb72d69` ZLEAK-1 · `ed9ea588` ZLEAK-2 · corpus `8cfe632c` D2 probe+ref):**
- **ZLEAK-1 (`zeta_storage.c:507`):** the STF elide-off was `static int eon` read ONCE from `getenv` — process scope driving a graph-scope regime. It applied to all 316 programs while emit.cpp:2536 armed 31; the 285 DECLINED graphs got `zls_grant` instead of `zls_grant_elide` + a skipped `zls_mark_value_refs`. **This was the ENTIRE 41-program m4 regression.** s21x-e's rationale (hole-free accumulated-offset traversal) is NOT load-bearing at HEAD → retired to killswitch `SCRIP_STF_ELIDE_OFF=1`.
- **ZLEAK-2 (`zeta_storage.c:737`):** SEQ-CELL, same shape, polarity aggravated — `!(z=='0')` made it ACTIVE-by-default under STF, contradicting s21x-l's own "SEQ-CELL dormant". **100% LEAK: zero armed customers**, fired only in declined graphs. → explicit opt-in `SCRIP_SEQ_CELL=1`, feature intact.

**⭐⭐ THE DECISIVE MEASUREMENT (s21x-q named it cheap+decisive and did not run it; both sets re-derived, not cited):** ARMED = **31 programs / 31 live graphs** (one each ⇒ census does NOT undercount ⇒ **hypothesis (b) DEAD**), all output/assign/concat/arith/goto. m4 REGRESSION = **41 programs, ALL pattern/capture/BAL**, zero arithmetic. **INTERSECTION = 0 — DISJOINT, not merely proper. All 31 armed PASS.** ⇒ **s21x-q's NEXT(1) is RETIRED**: there is no defect in the armed path to bisect. Hypothesis (a) holds in its strongest form.

**⭐ THE INSTRUMENT (reusable; run this before ANY default-flip):** after the regime is honest, EVERY graph reporting `live=0` MUST be byte-identical between regimes; any declined graph whose `.s` differs IS a process-scope leak. Per program: count `-> live=1` under `SCRIP_STF_DEBUG=1`, diff default `.s` vs gated `.s`. **No theory of the mechanism required; cannot miss one.** It caught ZLEAK-2 the moment ZLEAK-1 closed. Armed graphs differ 31/31 throughout — the null is NOT vacuous.

**⭐⭐ RETIRED BY MEASUREMENT THIS SESSION:** s21x-p's D2 (*"probe HEAP-EXHAUSTS under STF alone"*) — the probe returns `A` in **all four** regimes at HEAD; the exhaustion was the elide leak, not a match-release weld. **"roman-under-gates segv"** — recursive roman returns `MDCCLXXVI` under default/STF/CALL2BB/STF+CALL2BB. **"Any combo including `SCRIP_STMT_FRAME=1` is OFF-LIMITS"** — FALSIFIED for all three combos (see watermark). **The 31-vs-44 "unreconciled units" note** — RESOLVED: 44 is armed *programs* with `SUBJ_CELL=1` also on, same unit, different env (matches s21x-k's own 31→44). Not statements.

**⭐ FREE EVIDENCE FOR NEXT(2) — CALL2BB FIXES 140 AND 141 IN MODE-3.** s21x-q recorded {140,141} as surviving into the killswitch baseline, i.e. believed allocator-independent and immutable. **They are neither** — under `STF+CALL2BB` m3 goes 266/51 → **268/49** and DIVERGE **3 → 1**, leaving only `W04_arbno_basic`. The two-BB constant-folded DEFINE world resolves them.

**NEXT, DEPENDENCY ORDER (re-ordered by this session's measurements):**
1. ⭐ **ZLEAK-3 — SUBJ_CELL (150 leaks) and `fc_call_ok` (77 leaks). FULLY SPECIFIED AND DE-RISKED BELOW; one premise left to prove before coding.**
   **⚠ NOT the ZLEAK-1/2 shape.** SEQ-CELL had ZERO armed customers so opt-in was free; these arm 44 and 58 graphs and ALL of them differ, so a polarity flip would DELETE a live feature. And a blanket gate at the consumption site is WRONG: **the ASSIGN value-spine loop is NOT env-gated — it populates the same `fvr` registry on the DEFAULT path** (12 cell refs in default `039_pat_any`), and the SUBJECT loop calls the SAME `fc_vlit_register`/`fc_vread_register`, so nothing at emit time distinguishes a subject-registered node from an assign-registered one.
   **THE CIRCULARITY (why it is not a one-liner):** `emit_stmt_frame_scan` queries `fc_vread_fp` to DECIDE arming (SUBJ-ARM-1: a head with a subject-cell registration is what licenses the match statement), while the registration would need the arming verdict to avoid leaking. Registry ⇄ verdict.
   **THE RESOLUTION — PLAN-TIME FIXPOINT (register → scan → unregister on decline).** `emit_stmt_frame_scan(g, entry)` is a PURE function of (graph, entry, registry) — a BFS over a kind whitelist, no emit-time globals in its body — so it is callable at plan time. Unregistration is already an established mechanism here (the PS-3 late pass declines wholesale and unregisters SEQ). ⭐ **MEASURED, and it is the whole leak:** of the leaking declined graphs, **150 are declined by the SCAN and 0 by a conjunct** ⇒ the fixpoint catches **100%**, not a fraction. The registry becomes a PROPOSAL; the verdict stays the AUTHORITY.
   **⭐ PREMISE RESOLVED (s21x-r, instrument + guard landed SCRIP `30c96e72`) — the rung is UNBLOCKED for STF+SUBJ_CELL and SCOPED OUT for CALL2BB.** Measured over 317 programs / 595 choke decisions / 527 distinct (program,graph) pairs, with `g=%p entry=%p` added to the `[STF]` choke line:
   - **STRONG PREMISE FALSIFIED:** plan-time entry ≠ emit-time entry. **32 graphs are emitted from MULTIPLE entries** — up to **11** (`test_string`), all DEFINE / indirect-goto / EVAL shapes. The cursor's original wording asked for the wrong thing.
   - **VERDICT STABILITY: 0 violations / 32** — a graph gets the same verdict at every emission.
   - **ARMING IMPLIES SINGLE ENTRY: 44 armed decisions / 44 distinct armed graphs, 1:1.** ZERO multi-entry graphs are armed; they decline at every entry.
   ⇒ **ONE plan-time decision CAN match EVERY emission**: scan from `g->entry` (IR.h:232); for any graph the planner would arm that is the only entry, and a graph it declines is declined at all of them.
   **⛔⭐ BUT THE GUARD IMMEDIATELY CAUGHT WHAT THE MEASUREMENT MISSED — `1014_func_freturn` VIOLATES VERDICT STABILITY UNDER `SCRIP_CALL2BB=1` (verdict 0 then 1).** The same graph declines at one emission and arms at another — latent **s188 mixed-regime** disease, invisible because it is currently BENIGN (1014 still matches its ref; the m4 fail set is still baseline-identical). **The premise proven under SUBJ_CELL DOES NOT CARRY TO CALL2BB.** So: land the fixpoint for STF+SUBJ_CELL only; **fix CALL2BB's instability first, with `1014_func_freturn` as the named reproducer**, before extending it there. ⚠ LESSON: measuring a premise under one env and assuming it under another is exactly the error the guard exists to catch — it earned its keep within minutes of landing.
   **THE GUARD:** per-graph verdict/entry table at the choke, regime-gated on `_stf` (default path never executes it), **warn-only** by default so a currently-benign violation is not turned into a hard failure; `SCRIP_STF_STRICT=1` aborts. If either arm ever fires, ZLEAK-3 must be re-derived before it is trusted.
   **⚠ UNTIL THIS LANDS:** every measurement taken under `SUBJ_CELL` or `CALL2BB` conflates regime with leak — the likeliest reason the D2 autopsy chased a match-release weld that was never the fault.
2. ⭐⭐ **CARVE-ERAD — DELETE THE WHOLE-GRAPH CARVE. NOT ONE PROGRAM REQUIRES PRE-ALLOCATION (Lon directive, s21x-r).** Every BB allocates its own memory at α and releases at γ/ω; boxes are wired statically (and sometimes dynamically) and just jump. There is no graph frame in the design and there does not need to be one.
   ⛔ **CORRECTION OF RECORD — s21x-r's own first answer to this was WRONG and its reasoning is VOID.** The `SCRIP_NO_GRAPH_CARVE=1` clamp broke 38 programs and s21x-r reported that as "38 programs still NEED the whole-graph carve", with a Ch.18 backtracking rationale ("the scanner needs a stable base to unwind to") and a claim that the four RBP constructs are that brick wall. **All of it is wrong.** The clamp shrank the backing store while leaving **1057 `FR`/`FRQ`/`FRQB` reader sites** addressing into it — it measured UNCONVERTED READERS, not architectural necessity, and the result is very nearly tautological. Backtracking needs the PORTS plus LIFO per-box claim/release, not a pre-computed frame (`test_icon.c`, the reference embodiment: state lives in the per-box ζ block, allocated fresh per α-entry, code shared). And law 4's four RBP constructs name **which base register a construct uses for its OWN housekeeping** (ARBNO's children are variable-length) — NOT a graph frame. Law 1 already forbids one: *never a site allocating more than a few bytes for one BB.*
   **THE 38 ARE A DEBT COUNTER, NOT A REQUIREMENT.** They are the programs that reach a box that has not been converted yet.
   **THE WORK = CONVERT THE READERS.** 1057 sites across 108 templates; 6 templates already self-allocate. Queue by site count: `bb_match_arbno` 68 · `bb_binop_relop` 62 · `bb_to_by` 45 · `bb_call` 45 · `bb_to` 31 · `bb_call_value` 30 · `bb_unop` 27 · `bb_call_proc_staged` 25 · `bb_section` 24 · `bb_keyword_assign` 24 · `bb_scan_bal` 23 · `bb_match_head` 23 · `bb_match_breakx` 19 · `bb_binop_gvar_arith_slot` 19 · then a tail. Each conversion: the box claims its own cell at α, reads ITS OWN cell (never `[rsp + graph_offset]`), releases at γ/ω.
   **COMPLETION GATE (the honest one — it measures the thing actually wanted, unlike "zero raw rsp in templates"):** `SCRIP_NO_GRAPH_CARVE=1` passes **317/317 both modes**, then the carve is DELETED outright rather than shrunk, and `flat_frame_bytes` with it. The instrument bisects progress: the 38 shrinks monotonically as readers convert, and it names WHICH box each failure is waiting on.

3. ⭐ **FLIP + DELETE (was NEXT 3), now evidence-backed by the 140/141 result above** — default the two-BB world, delete the legacy proc jmp-entry carve arm (Lon: "DELETE that stupidity").
4. Retire the discriminator's `!flat_pat` conjunct (emit.cpp ~2535). ⚠ The 41 casualties were ALL pattern programs but they were casualties of the LEAK, not evidence pattern graphs can't be admitted — so this is now open on its own terms, no longer blocked. Name all gates from the `[STF] choke` line before cutting (`pat=0` graphs are rejected too).
5. Residual spine bisect — `W04_arbno_basic` is the sole surviving DIVERGE under CALL2BB and the one the killswitch removes ⇒ allocator-induced.
6. Framed-glue customers ARBNO/FUNCTION/FENCE1.
7. §3-of-s21x-q's unledgered `stfh` 48 — still the pre-identified landmine to fix in the same slice that admits the match family (path still dormant: 0/317 HKQ).

**⚠ CONCURRENCY:** watermark re-derived at start AND close. Start (316 programs): DEFAULT m3 265/51 · m4 264/50/2 · DIV=3, killswitch 312/4 · 312/2/2 · DIV=2 — EXACT match to s21x-q close, no parallel drift.

FINDING-2026-07-30-CLAUDE-SN4-ZLEAK-PROCESS-SCOPE-FLAGS-DRIVING-GRAPH-SCOPE-REGIMES.md

---

## ⛔⭐ WATERMARK OF RECORD (2026-07-31, s21x-v close — SCRIP `d492b909`, ZD default-ON)

| runner | m3 | m4 | DIVERGE | vs prior record |
|---|---|---|---|---|
| **crosscheck (317, runner of record)** | **232/85** | **229/86/2** | **1 {W04_arbno_basic}** | +3 / +2, DIV unchanged (was 229/88 · 227/88/2 · 1) |
| **broad corpus (336)** | **244/92** | **238/90/8** | — | +2 / +2 vs same-session killswitch baseline 242/236, full ledger: fixed 028+literals both modes, zero broken |

Runners: `test_crosscheck_snobol4.sh` (~75s/arm, DIV column) and `test_broad_corpus_snobol4.sh` (~110s, superset). ⛔ Broad-runner FAIL lists were `head -40`-truncated before SCRIP `67b90e2b` — counts on old logs are authoritative, printed lists are not. `SCRIP_ZD=0` = exact baseline reproduction (verified in-harness on the broad runner). 85/92 reds = the match-family debt counter; they convert via NEXT(2) of the live cursor.

**s21x-r close (retained for context):** DEFAULT m3 266/51 · m4 265/50/2 · DIV=3 · killswitch m3 312/4 · m4 312/2/2 · DIV=2.

---

## ⛔ PRIOR CURSOR — s21x-q (2026-07-30, retained: the ζ-spelling closure + the four legitimate raw-rsp classes are still binding law)

**⭐ THE SWEEP IS CLOSED, NOT PAUSED — the remaining 41 raw-rsp sites are ALL classified NOT-ζ. DO NOT "finish" it by converting them.** capture 3 · defer 3 · release 3 = CSTACK swap arms · bb_arith 1 = C-ABI align · call_proc_staged 9 = frame dance/pcall · glue_flat 2 + glue_framed 1 = THE GLUE PAIR · arbno 10 = ARBNO's own rung · xa_flat 9 = prologue + ICNBENCH align.
**LAW:** the ζ gate is not "zero rsp in templates", it is "**zero rsp that is a MID-BODY CELL**". Four legitimate raw-rsp classes: GLUE · C-ABI ALIGN · CSTACK SWAP ARM · PROLOGUE. A site outside those four is a defect.
**LANDED (ZREL-2):** `bb_match_head.cpp` ~40/~54 hfc() carves → `x86_zclaim(32)`; `bb_match_defer.cpp` ~168 leaf SUSP pop → `x86_zrelease`.
**⚠ INSTRUMENT LAW:** an arming census keyed on an immediate operand MUST anchor the operand end (`sub +rsp, +48$`) — a bare digit prefix matched `sub rsp, 488` and reported 101 false-armed programs. Cross-check with a second independent signature before believing either.
**⛔ s21x-q's stfh/HKQ match-arm analysis stands as SOURCE READING ONLY** — the path is dormant (0/317 HKQ) and its D2 conclusion is now doubly retired (see s21x-r above). Keep §3 as the pre-identified landmine for the slice that admits the match family.

---

## ⛔ PRIOR CURSOR — s21x-p (2026-07-30, retained for the CALL2BB exoneration; its D2 root cause is RETIRED)

**LON DIRECTIVES (s21x-p):** "Complete the ZETA cells on the RBP/RSP stack" · [roman.s double-add] "There should be ONE RSP decrement" · "that box [LEN] needs ZERO bytes" · "[proc_LBL__ROMAN_α] DELETE that stupidity. It should be a BB_SAVE_RESTORE and BB_CALL_* from the CONSTANT FOLDED DEFINE statement with proper glue. There is no frame operands offset from RBP anymore, just RSP." · "All your choices. I'm with you on this."

**LANDED (SCRIP `4c375fe2` GLUE-4 · `a42ac31f` ZREL-1 · `42e81405` LEN-GHOST · `2aec9a4b` js/jns · + `.s` regen ×3):**
- **GLUE-4:** `bb_glue_framed_*` WIRED — STATEMENT is the framed glue's first customer (emit.cpp head stubs + chain-exit cuts call the pair at K=0, `op_fc_bytes` zeroed at the site, R5b); `x86_stmt_enter/leave` DELETED. Byte-identical over 317 crosscheck programs × both regimes. Remaining customers: ARBNO/FUNCTION/FENCE1.
- **ZREL-1:** `x86_zclaim/x86_zrelease` — the spine-cell claim/release verbs beside ZTOS (x86_asm.h, defined in the stmt-family region per the line-419 definition-order convention). 9 raw-rsp sites converted (binop_arith mid-body pops — NEXT(3) discharged — assign_global vfc pops, concat_slot, match_head subject-pop + 48B stfh carve, save_restore sb pair), byte-identical. DELIBERATELY EXCLUDED (semantics, not shyness): C-ABI alignment dances (bb_arith:29), SAVE/COND protocol sites (splice ruling), CSTACK x86_zr() swap arms, ARBNO (rung rewrites it), call frame-dance (FUNCTION construct's own rung).
- **⭐ LEN GHOST-CELL (the roman double-add root):** `zls_mark_value_refs` counted `ASSIGN_COND/IMM.operands[0]` — the inner-entry WIRING edge (lower ~1327 "[0] inner entry") — as a value read, so EVERY capture-guarded leaf got live=1 and a spurious 16B grant (the second `add rsp,16` Lon spotted; the first is the legitimate SAVE-pend abandon-pop). Fix = skip `(COND|IMM, j==0)` ONLY; `[1]`=SAVE stays marked (the ZB-FC-3c cross-read the s4 audit protects). LEN was already in `zls_elide_ok` — only the false liveness blocked it. m3 251→256, m4 248→254, SKIP 36→18, DIV 3→2.
- **js/jns:** `x86_jcc_invert` lacked the pair and its abort was a LIVE DEFAULT-PATH COMPILE BOMB (conditional-ω synth on sign checks), not gated-only — 16 programs un-bombed. m3 →264, m4 →264, SKIP →2. Encoders pre-existed (0x88/0x89); inverter was the only hole.

**⭐⭐ THE D2 AUTOPSY (monitor bracket + minimal probe + label trace + static .s diff — the proc-demolition path is now SINGULAR):**
- **D2 IS MIS-ATTRIBUTED — CALL2BB IS EXONERATED.** 5-line probe (`DEFINE('F(N)T')` + capture match + one call; oracle="A", /tmp/d2_min.sno shape — re-create from this description). `SCRIP_CALL2BB=1` ALONE: probe PASSES, and **recursive roman computes `result: MDCCLXXVI` CORRECTLY (713ms)**. The two-BB constant-folded DEFINE world (laws 6/7: sr0 args-to-NV + staged call + floaters) is FUNCTIONALLY ALIVE INCLUDING RECURSION. Verified along the way: sr0 delivers args to NV correctly (gdb: NV[N] = proper 'AB' DESCR at blob entry), blob body reads formals from NV absolute (no [rbp+k] param cells — Lon's law already holds on this path).
- **THE BREAKAGE RIDES `SCRIP_STMT_FRAME=1` ALONE** (probe heap-exhausts with STF alone, no CALL2BB): `rt_dcap_pump` churns on garbage — RELEASE's head-cell reads are displaced.
- **ROOT, NAMED (one weld from fixed):** `bb_match_head`'s `stfh()` arm carves 48 bracket bytes (s21x-k SUBJ-ARM-2) that are LIVE at RELEASE.α, and **`bb_match_release` never got the matching arm** — its fixed `[rsp+16/24/32/96/120]` head-cell reads land inside the bracket slots (saved regs/pointers → the "`pUUUU" dcap-mark garbage, the poisoned `mov rsp,[rsp+32]` wholesale unwind = roman-under-gates' segv). Static proof: n7_match_release_α is BYTE-IDENTICAL between regimes — the reads never learned about the carve. s21x-k's "STF-EXIT-SEGV cured" was PARTIAL; RELEASE (and audit REPLACE + any other head-cell reader) is the un-welded remainder. **THE WELD: head-side carve and reader-side spelling must share ONE predicate (stfh) and ONE layout authority (the RBP-HOUSEKEEPING five-field bracket map, [rbp−48+8k]).**
- **fc_walk_range COHERENCE NOTE (do not "fix" blindly):** fp counts fc/WINDOWED cells only; carve-only cells net out by γ before downstream readers — COHERENT under the default regime by construction. The default arm's correctness depends on this; the STF defect is NOT here.
- **roman-ref CORRECTION (voids the s21x-p-early claim "roman fails at HEAD"):** roman's m3 diff is ONLY the nondeterministic `ms:` line roman.sno itself prints (line 19) vs a ref trimmed to the deterministic line. **roman is semantically GREEN at HEAD default.** Do not chase roman as a legacy-class residual; benchmark comparisons must strip/normalize the timing line.

**NEXT, DEPENDENCY ORDER (updated):** (1) ⭐ **THE RELEASE STFH WELD** (root above; probe is the reproducer, monitor-bracketed, backtrace = n7_match_release_α → c_rt_dcap_end_ok_open → rt_dcap_pump; commit the probe into corpus with its "A" ref in the same slice). (2) Retire the discriminator's `!flat_pat` conjunct (emit.cpp ~2535) once the match family's head-cell readers all speak the bracket authority. (3) **FLIP + DELETE:** default the two-BB world, delete the legacy proc jmp-entry carve arm (`sub rsp,N` + zero-fill + wire-park — the named anti-pattern, Lon: "DELETE that stupidity"). (4) Residual spine bisect (carried from s21x-o, against the NEW 264/264 baseline). (5) Framed-glue customers ARBNO/FUNCTION/FENCE1 (ARBNO = the variable-extent anchor, carried).

**⚠ CONCURRENCY:** watermark is SHARED STATE — re-derive at start. `SCRIP_BB_ALLOC=0` killswitch UNCHANGED all session: **m3 312/4 · m4 312/2/2 · DIV=2 {140,141}**.


---

## ⛔⭐ LON DIRECTIVE — s21x-c (2026-07-29): DESIGN OF RECORD = RBP/RSP FRAMES + FORTH-STYLE VARIABLE-LENGTH ζ CELLS. READ THIS BEFORE ROUTING ANYWHERE.

**TERMINOLOGY RULING: the s20x two-word name is BANNED — never write it, never speak it.** `GOAL-SN4-CELL-*.md` / `DESIGN-SN4-CELL-*.md` are DEAD routing targets (disk history only, outright deletion pending a Lon call).

**THE DESIGN (supersedes prior storage plans for BB RESULT/LOCALS wherever they conflict — including the s205 off-stack region split; RESULT and LOCALS both ride RSP):**
1. **Every BB allocates its own storage**: RESULT iff it has one AND it is used; LOCALS iff any. **ONE instruction: `sub rsp, K`.** Never a site allocating more than a few bytes for one BB.
2. **Sliding offsets, RSP-indexed**: the emitter tracks live depth at compile time; operands index from RSP, not RBP.
3. **RSP-only for EVERY box and construct until a genuine BRICK WALL**, then and only then the RBP/RSP dance (create the frame pointer forward, roll it back backward).
4. **The four RBP constructs: STATEMENT · FUNCTION · ARBNO · FENCE1** (~99.999% of storage is RSP; ARBNO's variable-length-children housekeeping is RBP). FUNCTION means only IR_CALL's frame dance — law 7.
5. **Named anti-pattern (roman)**: `sub rsp,1344` whole-graph carve + slot zero-fill + unconditional rbp pin. Whole-graph prefix-summed prologue carves are WRONG.
6. **DEFINE, constant-folded, emits exactly TWO BBs: IR_SAVE_RESTORE + IR_CALL** — and IR_CALL just sets up the RBP/RSP frame. That is all it does.
7. **SCOPE LAW: reduce the SCOPE of ALL processing DOWN to the STATEMENT level. NO FUNCTION-level processing WHATSOEVER.** IR_SAVE_RESTORE handles the globals (SNOBOL4's one NV namespace — fname/formals/locals are globals; manual Ch.19 save/restore); IR_CALL sets up the frame, nothing else; NO ζ in the call path except IR_SAVE_RESTORE's own carved slots (itself a BB — law 1). CONSEQUENCES: per-function/graph storage planning DIES (zls_build graph scope over); proc jmp-entry carve + zero-fill DIES; `[rbp+16/24]` param cells DIE (args land in NV globals); slot0 result protocol DIES (return value = the fname global at RETURN). Statement scope is the ceiling of every planning pass.

**s21x-f COMPANION DESIGN — DYNAMIC BOX GLUE:** any BB graph (one entrance, four ports) invokable by a full four-port DYNAMIC BOX: **(A) DYNAMIC-FLAT** — α: `jmp [entry_cell]`, β: `jmp [resume_record]`, γ/ω = supplied wire landings; zero frame, zero pcall record, zero C crossings; first customer **IR_MATCH_PATREF** (eager stored pattern, cannot recurse — manual p.122). **(B) DYNAMIC-FRAMED** — same shape + the RBP/RSP dance. Open rungs: x86_asm.h dynamic-flat glue primitives (both-medium, runtime cells, no rel32 patching) · PATREF rides dynamic-flat under the regime · fc_geom PATREF k=16 after the template splits its sink window. (The original list's CALL2BB slices LANDED s21x-g/h.)

---

## ⛔ LAWS & TRAPS (binding; falsified experiments — DO NOT RETRY)

- **⭐ NO PROCESS-SCOPE FLAG MAY DRIVE A GRAPH-SCOPE REGIME (s21x-r):** if a planner decision must agree with an emit-time per-graph verdict, it is computed PER GRAPH or deferred to the PS-3 late pass — never cached in a `static` from `getenv`. **`static int on = getenv(...)` inside a function taking an `IR_graph_t * g` is the greppable signature of the defect class.** It cost 41 m4 programs (ZLEAK-1) and hid a second leak behind it (ZLEAK-2), and it sent two sessions hunting a box-template/weld defect that did not exist. COMPLETION TEST = the declined-graph sweep below reporting 0 under every gate combo the session touches.
- **⭐ THE DECLINED-GRAPH SWEEP (s21x-r, the instrument — run before ANY default-flip):** every graph reporting `live=0` MUST be byte-identical between regimes; a declined graph whose `.s` differs IS a process-scope leak, by definition. Per program: count `-> live=1` under `SCRIP_STF_DEBUG=1`; diff default `.s` vs gated `.s`. Needs NO theory of the mechanism and CANNOT miss one. Always confirm the null is non-vacuous by checking armed graphs still differ.
- **CENSUS UNITS (s21x-r, resolving s21x-q's open note):** armed counts differ by ENV, not by unit — 31 programs under `STMT_FRAME` alone, 44 with `SUBJ_CELL`, 58 with `CALL2BB`. Always state the env with an armed count; "armed N/316" without its env is unreadable.

- **SUSPENDED-CELL (s21x-l):** no rsp cell may suspend across γ above an ARBNO/DEFER extent until the anchor lands (cursor above).
- **RESULT-IS-THE-CELL (s21x-k):** the consumer overwrites the popped source cell IN PLACE ([rsp+0/8], no pop, net-zero); flat result stores are the environ-smash disease (bb_assign_global cured; assign_local/stub bodies unaudited — NEXT 5).
- **RBP HOUSEKEEPING (s21x-k):** the five POST-UNWIND head fields ride statement-bracket slots `[rbp−48+8k]`, carved `sub rsp,48` at head α (+8 pad keeps C-call 16-align), spelled via x86_parse generic reg-disp, NO frame compensation BY DESIGN.
- **SOLE-CONSUMER FENCE (s21x-j):** subject registration requires IR_MATCH_HEAD be subjval's ONLY operand consumer (pure dataflow scan of g->all, no kind naming); replacement statements decline wholesale pending the splice ruling.
- **UNION-TAG (s21x-g):** IR_t sval/ival/dval are ONE union — roles normalized to clean op_ival/op_sval at the single dispatch point; the op_sval promotion whitelist must NEVER include IR_SAVE_RESTORE.
- **NODE-EXACT HANDOFF (s21x-g):** emission order is not a contract (BFS interleaves); producer→consumer keyed by CALL NODE POINTER. Corollary: EVERY template return path defines α (m3 dangling-label class; m4 masks it — TEXT resolves by name).
- **STUB DISCRIMINATOR (s21x-h):** the DEFINE-stub key is `g_flat_frame_floor > 0` (the driver's own verdict, mode-uniform); admit-all-jmp-entry regresses EVAL/CODE chains. A call's result cell is `op_fc_wbytes` WINDOW-ONLY, never an alpha-sub (would sit on sr0's live block and shift the c2 landings' restores by 16).
- **fc grant = per-kind dispatch preamble** (emit.cpp 843/852 3-line shape) until NEXT 4 consolidates; the base varies per kind — copy the shape, not a neighbor's base.
- **DEFER-DEEP LOAD-BEARING (s197):** do NOT drop IR_MATCH_DEFER from deep-arrival until recursive-defer programs pass — that green is false (every recursive witness sat inside the old FAIL set). The correct narrowing was eager-vs-unevaluated (s199 DEFER-STAR, landed).
- **s206 DO-NOT-RETRY:** the `x86_fc_cells()=(FORTH||HEAP)` predicate flip → wild jumps (3 OK → 0 OK). The ONE port opinion is `fc_cells_on` (Z4-6); port-blind grant vs port-gated consumption was the defect class.
- **SCAN-RETRY = 5th rbp member (s196):** PAT$ `scanfail` unwinds from arbitrary carve depth (SPITBOL Ch.18 step 6); `&ANCHOR` is a runtime keyword — no static classifier retires the pin.
- **GOTO J3 (s21x-e):** `case IR_GOTO` fires `emit_mon_label_tap` — the monitor's ONLY scr event source; GE-1 relocation is the prerequisite to ANY goto deletion.
- **ALL-OR-NOTHING PER GRAPH:** a blob compiles ALL-NEW or ALL-LEGACY, never interleaved.
- **STALE-BINARY TRAP (s21x-j):** `[ -x scrip ]` is not a build check — `grep -c error:` on BOTH make logs + fresh mtimes. Edit only from RAW viewed lines (two truncated-view self-inflictions on record).
- **op_flat_disp RIDES EMISSION ORDER** — any schedule/layout change must first prove per-chain disp locality (blocks the SRC-ORDER blind fix).
- **CROSSCHECK MASK:** stdout-only `|| true` hides post-output crashes; the rc-checking arm is NEXT 7 (safe to add since s21x-k).
- **CENSUS SHELF LIFE:** re-run, never cite. HEAD-STAMP every measurement when parallel sessions share the volume (s197 concurrent-writer incident).
- **PROSE-ROT (s196, partly fixed since):** verify doc claims against live source before citing (ARCH-ICON register table corrected 2026-07-18; TEMPLATE-REVAMP's r12 ζ-scratch note is stale history).

---

## ⛔ PENDING LON RULINGS
- **Default-flip license** for the statement regime (armed 44/316; gate-on == gate-off at HEAD).
- **FOUR-modes confirmation** — s21x-l read "the FOUR modes" as `ZC_STORAGE_{FRAME_R12,FRAME_RSP,CELL_STACK,CELL_HEAP}` (zeta_choices.h). Confirm or veto.
- **Replacement-splice ruling (s21x-j):** splice reads the cell, or a write-through — retires the sole-consumer fence for the replacement class (gates NEXT 2's replacement arm).
- **SRC-ORDER-LAYOUT ruling A/B/C** (ladder below).
- **RBP-SHED-7:** ⛔ blocked (ARBNO `zv()` borrow is inside the sanctioned four) — DO NOT START.
- (ZHEAP ζ_self ruling: moot unless Lon revives ZHEAP — see SUPERSEDED.)

---

## LADDERS (live, unchecked — a shrink pass must NEVER delete a live rung: s186 lesson)

### ⭐⭐ LADDER ZD — THE OPUS WALK (s21x-v prelim, ALL measurements done this session; each rung = the listed steps, each step gated by the FULL-LEDGER corpus diff both runners, killswitch on regression)

**CENSUS OF RECORD (SCRIP_ZD_DIAG over crosscheck 317 pgms, --compile, `db2ef88a` diag): 2114 nodes ARMED · 1060 statements DECLINED, first-blocker ranked:** IR_CALL 454 (+IR_SAVE_RESTORE 12, same bucket) · IR_MATCH_HEAD 227 · IR_BINOP non-arith 119 · IR_SUBSCRIPT 102 · IR_COERCE_NUMERIC 65 · IR_CMP_TEST 38 · IR_COERCE_STRING 16 · IR_COERCE_INTEGER 7 · IR_KEYWORD_SNOBOL4 12 · IR_FIELD_VAR 5 · IR_GOTO_DEFERRED 3. **Quick-win pool ≈ 359 statements behind MECHANICAL template arms (no CALL, no MATCH).** ⚠ First-blocker semantics: each fix re-exposes the next — RE-RUN THE CENSUS AFTER EVERY RUNG (3s: loop `SCRIP_ZD_DIAG=1 --compile` over crosshcheck, `grep DECLINED | uniq -c`). ⚠ Proc bodies are INVISIBLE to this census (flat_jmp_entry gate returns before diag) — VAR-local sizing waits on ZD-4.

- [x] **ZD-2a `bb_binop_concat_slot`** — LANDED s21x-w (`5885d24f`). ⛔ **2b `bb_binop_xrep_slot` SKIPPED, VACUOUS BY CONSTRUCTION** — XREP is not SNOBOL4-reachable (zero refs in `lower_snobol4.c`, zero corpus nodes; `lower_common.c:141` is a constant folder). ⛔ **2d `bb_cmp_test` + 2e `bb_coerce_numeric` LANDED TOGETHER** s21x-w (`4af624a2`) — they share statements, so arming either alone measures almost nothing (see the LIVE CURSOR's named BISECT TRAP).
- [ ] **ZD-2 VERDICT WIDENING (remainder: 2c relop, 2f subscript, 2g unop closure, 2h locals-under-pin, 2i tail) — MECHANICAL, zero redesign; every step is: clone the unop/binop ZD-arm pattern into the named template (operands via ZOPQ/ZOPD at staged differences, result to ZRES, ports unchanged) + one `zd_wl_kind` line + one `zd_nops` entry.** Template shapes READ AND CLASSIFIED this session:
  - [ ] 2a `bb_binop_concat_slot` — vfcc arm is already ONE type-blind call (`str_concat_d`, NO ω edge); purest clone.
  - [ ] 2b `bb_binop_xrep_slot` — identical shape, `str_repeat_d`.
  - [ ] 2c `bb_binop_relop` (185 lines, the one unread file — expect the arith fast/generic ladder; conditional ω already rides the invert synth).
  - [ ] 2d `bb_cmp_test` (38) — passes operand-descriptor ADDRESSES: `x86("lea", r, ZOPQ(k,0))` parses to the XK_RSP lea arm (x86_asm.h ~1416, VERIFIED); predicate-keyed conditional ω; IF(res_live) null ZRES.
  - [ ] 2e `bb_coerce_numeric` (65) + STRING (16) + INTEGER (7) — operands[0]=value, [1]=PARTNER type-peek ⇒ zd_nops=2, both zread-staged; reads via ZOPD/ZOPQ.
  - [ ] 2f `bb_subscript` RVALUE (≤102) — `rt_subscript_var` + ω("je"), clone; ⚠ lvalue-subscript assigns are a DIFFERENT node path (emit.cpp ~1259) — count the residue after 2f before touching.
  - [ ] 2g UNOP closure — NULL/NONNULL/NULLTEST_VAR into the ZD arm (never first-block today; needed so widened statements stop declining on them).
  - [ ] 2h VAR-local/ASSIGN-local UNDER THE PIN GATE — admit iff the graph's frame base is depth-immune (rbp pin; the per-graph flags are PROVEN live at zd_plan time by the flat_jmp_entry gate working). Unpinned graphs keep declining those statements. Copy-through-cell is the design fallback ONLY if pinned coverage measures thin.
  - [ ] 2i tail: `IR_KEYWORD_SNOBOL4` (12), `IR_FIELD_VAR` (5) — read template first, expect clones.
  - ⛔ **VERIFIED NON-ISSUE, do not re-derive:** the ω-chase (`flat_trivial_beta`) whitelists ONLY MATCH_POS/RPOS/ABORT/ASSIGN_SAVE — no ZD kind is chaseable, ΣK cannot miss a ZD cell. INVARIANT: if ZD ever admits a chase-whitelisted kind, the chase must learn zd K first.
- [ ] **ZD-3 LEGACY ARM RETIREMENT — MECHANICAL deletions, byte-measured:** per kind fully covered by its ZD arm, delete the vfc/vfcu/vfcb/vfcc arm + the `op_fc_disp` registration it keys (fc_vlit/fc_vbinop/fc_vread_fp/fc_vwpop customers). The in-place net-zero tricks are the shyness ZD ends.
- [ ] **ZD-4 LIFT THE JMP-ENTRY DECLINE — PROBE-THEN-FIX (DOWNGRADED from protocol redesign by this session's prelim):** ⭐ the EVAL handoff is NV-MEDIATED — `eval_chain_run_capture` reads `NV_GET_fn(EVAL_TMP)` (runtime_eval.c:202) — the fragment terminal is an ASSIGN-GLOBAL the ZD arm already serves, so the 1019 segv was blob ENTER/EXIT depth interplay, NOT the value path. STEP: `SCRIP_ZD_JMPENTRY=1` hatch bypassing the gate; diff 1019's fragment .s on/off (expect FEW lines, the s21x-v 3-hunk precedent); find the byte; fix; delete the gate. Partially unlocks the 454 CALL bucket (main-chain CALLs = the CALL protocol proper, stays declined pending its own rung).
- [ ] **ZD-5 MATCH FAMILY ONTO THE CELLS — the 227-head campaign; migration surface MEASURED: ~230 FRQ sites** (arbno 68 · head 23 · breakx 19 · span_var 13 · bal 11 · span/break 10 · release/alternate 9 · sequence 8 · fence1 7 · replace/defer/arb 6 · value/capture 4 · tab/rtab 3 · rem/notany ≤2):
  - [ ] 5a LINEAR-MATCH BRIDGEHEAD — fits the EXISTING linear planner unchanged: statements whose match is a straight single-yield run (head → LEN/POS/RPOS/SPAN/ANY/NOTANY/REM/TAB/RTAB literal elements → release; NO alt/arbno/fence/defer/capture). Admit those kinds; convert their 2–3-site templates' cross-reads to ZOPQ, own state to ZRES/ZLOC; single-yield β re-entry is the suspended-cell trampoline the discipline already owns.
  - [ ] 5b PLANNER EXTENSION for BRANCHING runs (alt arms, arbno cycles, defer) — δ through β re-entry + merge agreement per the s21x-u exceptional-set analysis (mark/unmark rebalance). ⛔ **THE ONE GENUINELY DESIGN-TIER ITEM ON THIS BOARD; everything above lands without it.**
  - [ ] 5c per-template conversions smallest-first by the site census, each ledger-gated.
- [ ] **ZD-6 STANDALONE FINDINGS:** (a) 130/131 clean-HEAD segv (deterministic, killswitch/SKIP/env-immune; repro in the s21x-v cursor); (b) DIV member W04_arbno_basic; (c) bb_op_name entries for ops 14/73–77.

### GOTO-ERAD (s21x-e survey verdict: the wire already IS the design — branch_chain rewires past IR_GOTO carrying port tags, dead_goto deletes husks; load-bearing consumers = J3 monitor taps + J4 operand-protected survivors; IR_GOTO_DEFERRED STAYS — its target is data)
- [ ] GE-0 census: compiler sweep of emitted `_goto_` boxes; autopsy the 037 operand-list protector.
- [ ] GE-1 ⛔ MONITOR TAP RELOCATION (the prerequisite): move emit_mon_label_tap to the statement-head walk (bb_src_of heads); delete bc_stamped/dg_mon exemptions; PROOF = 2-way monitor event-stream parity (also closes the s195 scr-granularity gap).
- [ ] GE-2 survivor unprotection (J4); default-arm .s shows zero `_goto_` boxes.
- [ ] GE-3 SNOBOL4 lower-side: label-fixup direct edges at the 14 minting sites (re-derive the ~624 both-edge router's chase semantics per site); `grep IR_GOTO lower_snobol4.c` → 0 excl. DEFERRED.
- [ ] GE-4..7 per-language, one file each, concurrent-safe: icon 24 · pascal 10 · prolog 8 · raku 7 sites.
- [ ] GE-8 emitter sweep: delete `case IR_GOTO`, the six sz-sniffing chase loops, bb_goto; branch_chain drops passthrough; dead_goto retires.
- [ ] GE-9 enum deletion; gate `test_gate_no_ir_goto.sh` == 0 plain matches.

### RBP-SHED (order 3→1→2→4→5; SHED-1/2/4/5 need the BB-CODEGEN design set and fire `.s` regen — NOT concurrency-safe; SHED-3 is emitter-C only, safe)
- [ ] SHED-3 REC-PIN-OWN: emit_rec_pin's `g_gen_proc_active||g_resumable_callable_active` are stale-emission globals leaking blob→main (w1 witness, refreshed only at emit.cpp ~1821); move to a graph-own g_emit mirror at the emit_chain choke; gate = ZETA-FB-1 divergence instrument reads 0 + w1 hook-skip rc 1→0.
- [ ] SHED-1 NPARAMS: retire the `g_flat_outer_nparams>=1` pin conjunct (xaf_deep, xa_flat.cpp:171) for depth-static graphs; first deliverable = the conjunct's unrecorded provenance; census FIRST (shelf life).
- [ ] SHED-2 ABORT-REBALANCE: route ABORT's kill through the statement fail exit (manual Ch.9/18 — immediate whole-match failure) so abort-bearing graphs stop disqualifying fb refinement; 170/171 tripwire BOTH directions.
- [ ] SHED-4 HOOK-ENCODE: scanhit/scanfail hooks (emit.cpp 2183/2197) through x86() — the named forbidden medium-branch shape with hand-spelled rbp; completion = byte-identical pattern_bt/string_pattern .s + medium-invisible gate green.
- [ ] SHED-5 ALIGN-DANCE-DELETE: retire the transient push-rbp alignment window per x86_asm.h:419's own death-path comment (run the env-gated assert clean first; dirty sites ARE the rung).
- [ ] SHED-6 pointer: PL-DC seed gating (xa_flat.cpp:763) → copy into GOAL-PROLOG-BB.md at the next Prolog session, then strike here.
- SHED-7 ⛔ blocked on Lon (rulings above).

### SRC-ORDER-LAYOUT (s21x-b, diagnosed; awaiting ruling): `# <source>` comments are CORRECT; the statement LAYOUT ORDER is scrambled (cm2: 1,5,2,3,4 · cm3 zero-goto: 1,2,4,3,5 — label-correlated, possibly two mechanisms; reorder exists by emission-walk time). Execution correct (control rides edges). ⛔ Blind-fix hazard = op_flat_disp emission-order dependence (law above). Ruling: (A) emit-side stno sort — prove/make disp schedule-independent first (byte-diff unpinned benchmarks under SCRIP_FB_STMT=1 is the falsifier), make the :stno store unconditional · (B) lower-side scramble bisect (cm3 is the cleanest probe: instrument node-id vs stmt-index at lc_build) · (C) accept. Gates either way: watermark start+close, census, `.s` regen ×5.

### FB-STMT RE-ARM (s21x; landed opt-in `SCRIP_FB_STMT=1`, default OFF — SCRIP `81345044`): per-node frame-base refinement; measured pinned-benchmark rbp value refs 1015→220, survivors = match-family housekeeping only. Falsified premise "statement exits rebalance rsp": (a) ARBNO hit-path element retention (180_pat_arbno_defer_nonrecursive) · (b) DEFER/VALUE-alt backtrack re-pump of a retained blob (072_pat_star_var_alt_backtrack); default-on regressed 7 programs, reverted same session. RE-ARM LICENSE: monitor-first 180 then 072; extend the bit map downstream of any retaining statement until release_to provably sweeps, OR land free-on-final-success — then flip the default.

### CARRIED OPEN (verify liveness before executing): MARKER-CAPTURE (s202 ALT-FLAT next rung — SAVE/COND pend-stack marks carrying start cursor, deletes SAVE's zls slot + COND's cross-extent FRQ read + the fc_leaf_walk special case) · r9 park-address anchor (the s202 named defer-window fix — the candidate mechanism for NEXT 1) · arm-quad OVERLAY in zls_build (union max-not-sum) · fc_cond FIRST-WINS audit (s195) · SYM-VIS-M3 perf JIT map (s195, `SCRIP_PERF_MAP=1`) · Icon generator-scan FORTH cells (fc_geom NOT-YET note: UPTO/FIND/MATCH/BAL inside a scan env).

---

## ↗ MOVED OUT / SUPERSEDED
- **SN4-RTX** → `GOAL-SNOBOL4-RTX.md` (contract ARCH-SNOBOL4-RTX.md). Phase-1 rungs touch zero templates, safe concurrent; **RTX-11/12 are NOT** (x86_asm.h + `.s` regen). Watermark is shared state between the ladders.
- **ZHEAP (s204/s205/s206) + the CELL ladder (s202/s203 seeds): SUPERSEDED by the s21x-c design of record** — RESULT and LOCALS both ride RSP; the whole-graph carve is REPLACED by per-BB carves, not re-plumbed. ZC_PORT_HEAP's rbx α-carve stays dormant-ratified (HZ-1, x86_asm.h ~1620); the s206 laws above still bind. Do not resume the CELL files (terminology ruling). Full plan text recoverable at `.github` `2f3fd45a`.

---

## HISTORY INDEX (one line per session; full text = FINDING docs + git)
- s21x-v (07-31) ⭐⭐⭐ ZD-1 LANDED: the RSP FORTH per-BB stack, default-ON corpus-wide, ABOVE WATERMARK both runners (crosscheck 232/85 · 229/86/2 · DIV=1; broad 244/92 · 238/90/8; fixed 028+literals both modes, ZERO broken, full ledger). THE FIVE LAWS (SCRIP `d492b909`): offset = δ_out(C)−δ_out(P) staged per operand (op_zread); the ONE walk zd_plan follows γ wires per bb_src_of head (statements INTERLEAVE in nodes[] — array-range segmentation was the 97/96 collapse); all-or-nothing per STATEMENT (reader-frontier law, locally decidable); raw channel [rsp# + N] for the FOUR modes (ZRES/ZLOC/ZOPQ/FR); port discipline per-node op_zres (α carve once, β no re-carve, γ suspend, ω own-K, terminal-γ gpop = statement scope by construction). Whitelist: LIT/VAR-global/UNOP ±,SIZE,COMPL/BINOP explicit-arith/ASSIGN-global; jmp-entry chains decline wholesale (flat_jmp_entry — the 1019_eval_string fragment-protocol casualty, closed). Templates: unop in-place trick + binop zrelease(16) shuffle retired in ZD arms. FINDINGS: 130/131 segfault at CLEAN HEAD (deterministic, killswitch/SKIP/env-immune, unchased); broad-runner head-40 truncation manufactured phantom regressions, fixed at `67b90e2b`. Artifacts regen ×3.
- s21x-r (07-30) ⭐⭐ THE STF DEFECT WAS A PROCESS-SCOPE PLANNER FLAG; THE ARMED PATH WAS NEVER BROKEN. ZLEAK-1 (`efb72d69`): the STF elide-off was `static int eon` from getenv applied to all 316 programs while emit armed 31 -- the ENTIRE 41-program m4 regression; s21x-e rationale not load-bearing, retired to `SCRIP_STF_ELIDE_OFF=1`. ZLEAK-2 (`ed9ea588`): SEQ-CELL same shape, active-by-default contradicting s21x-l's own "dormant", 100% leak (zero armed customers) -> opt-in `SCRIP_SEQ_CELL=1`. DECISIVE MEASUREMENT: armed set (31) and m4 regression set (41) are DISJOINT, all 31 armed PASS ⇒ s21x-q NEXT(1) retired. Declined-graph leak sweep 285 -> 24 -> 0: ALL-OR-NOTHING PER GRAPH now provably holds corpus-wide. RETIRED BY MEASUREMENT: D2 (probe returns "A" in all four regimes), roman-under-gates segv (MDCCLXXVI everywhere), "STF combos OFF-LIMITS" (all three m4-neutral, fail sets identical), the 31-vs-44 units note (same unit, different env). FREE: CALL2BB fixes 140+141 in m3, DIV 3->1 -- evidence for FLIP+DELETE. D2 probe committed to corpus (`8cfe632c`). FINDING-2026-07-30-CLAUDE-SN4-ZLEAK-PROCESS-SCOPE-FLAGS-DRIVING-GRAPH-SCOPE-REGIMES.md
- s21x-q (07-30) ZREL-2 + D2 RETIRED BY MEASUREMENT (stfh match arm DORMANT: 0/317 HKQ vs 31/317 live-and-all-arithmetic, control passes; weld withdrawn as vacuous; instrument law: anchor immediate-operand censuses -- "sub rsp, 48" matched "sub rsp, 488" and reported 101 false-armed). ζ-cell SPELLING sweep CLOSED — head:40/54 hfc carves → x86_zclaim(32), defer:168 leaf-SUSP → x86_zrelease; byte-identical 317/317 + all three .s regen scripts committed nothing; the remaining 41 raw-rsp sites ALL classified NOT-ζ (four legitimate classes: GLUE · C-ABI ALIGN · CSTACK SWAP ARM · PROLOGUE). STF defect RE-DIAGNOSED: release ALREADY has the stfh/HKQ arm; the real mechanism is head:32's x86_zclaim(48) carving OUTSIDE the grant, so op_zdepth (emit.cpp:820, set from op_fc_bytes only; HEAD excluded by kind at :811) is 0 and every non-HKQ [rsp+off] ref is displaced by 48 — emit.cpp:808's own law. Weld respecified as framed-glue K=48. Watermark UNCHANGED both ends; DIV membership captured {140,141,W04_arbno_basic}. FINDING-2026-07-30-CLAUDE-SN4-ZREL-2-ZETA-SPELLING-CLOSED-AND-THE-STFH-48-IS-AN-UNLEDGERED-SECOND-ALLOCATION-AUTHORITY.md
- s21x-p (07-30) GLUE-4 framed-glue wired (`4c375fe2`) + ZREL-1 zclaim/zrelease (`a42ac31f`) + LEN-GHOST liveness fix (`42e81405`, the roman double-add root: COND/IMM operands[0] is wiring) + js/jns default-path compile bomb (`2aec9a4b`); D2 AUTOPSY: CALL2BB exonerated (recursive roman correct under it alone), defect is STF-side = bb_match_head stfh 48B carve vs bb_match_release un-welded reads; roman-ref ms-line correction. m3 251/65 -> 264/52, m4 248/32/36 -> 264/50/2, killswitch EXACT. FINDING-2026-07-30-CLAUDE-SN4-S21XP-LEN-GHOST-AND-D2-IS-STF-NOT-CALL2BB.md
- s21x-o (07-30) ZTOS-1 sliding offsets (`b5754902`) + GLUE-3 wired byte-identical (`785e3a41`) + ZTOS-2 live depth on the rsp frame arm, one line, +37 both arms (`e064482e`); depth-displacement root cause found = allocator and operand address were two authorities that could not see each other; arming frontier proven to be a property of a box's READERS not the box; residual spine blocker measured and left NAMED-UNKNOWN. m3 211/105 -> 250/66, m4 208/72 -> 248/32, DIV=3 throughout. FINDING-2026-07-30-CLAUDE-SN4-ZTOS-1-SLIDING-OFFSETS-AND-THE-ARMING-FRONTIER-IS-THE-READERS-NOT-THE-BOX.md
- s21x-m (07-30) UNIVERSAL PER-BB RSP CARVE ACTIVATED BY DEFAULT (Lon directive); zw_node_k K-authority; two glue codes; SCRIP_BB_ONLY/SKIP bisect keys; three laws (four-port bracket · spine-is-already-the-cell-machine · pin-is-a-layout-contract). Ladder opens at m3 210/106, m4 208/72/36, DIV=3.
- s21x-l (07-30) SEQ-CELL dormant, suspended-cell law; record in SCRIP `7839c64e` commit message (FINDING doc pending handoff).
- s21x-k (07-30) SUBJ-ARM: first armed match statements 31→44; bracket rbp slots; result-is-the-cell; STF-EXIT-SEGV cured. FINDING-2026-07-30-CLAUDE-SN4-SUBJ-ARM-FIRST-ARMED-MATCH-STATEMENTS-ENVIRON-SMASH-ROOT-AND-STF-EXIT-SEGV-CURE.md
- s21x-j (07-30) SUBJECT-CELL v1; sole-consumer fence; stale-binary trap. FINDING-2026-07-30-CLAUDE-SN4-SUBJ-CELL-V1-LANDED-SOLE-CONSUMER-FENCE-AND-THE-STALE-BINARY-BUILD-OK-TRAP.md
- s21x-i (07-30) CALL2BB 3b dormant (SCRIP `8437c3d7`); sr0 rides the γ spine; D1/D2 fence defects. FINDING-2026-07-30-CLAUDE-SN4-CALL2BB-3B-V1-LANDED-DORMANT-SR0-RIDES-THE-GAMMA-SPINE-AND-TWO-FENCE-DEFECTS.md
- s21x-h (07-30) CALL2BB 3a: stub blobs ride the regime (`126645fc`); g_flat_frame_floor law; roman half-dead; gate-on 313/2 · 311/2 · DIV=0 (two-gate world). FINDING-2026-07-30-CLAUDE-SN4-CALL2BB-SLICE3A-STUB-BLOBS-RIDE-STMT-REGIME.md
- s21x-g (07-30) CALL2BB 2: role-0 prefix + staged skip (`3e3096a2`); union-tag + node-exact laws. FINDING-2026-07-30-CLAUDE-SN4-CALL2BB-SLICE2-ROLE0-PREFIX-AND-STAGED-SKIP.md
- s21x-f (07-29) OP-SPLIT PATREF/DEFER (`fc27a7b3`); CALL2BB slice 1 gated (`aa4cbff0`); dynamic-glue design ratified (kept above).
- s21x-e (07-29) admit-all classifier, registration-is-the-license (`35aa7cbd`); DIV/MOD/POW (`13a0dbc4`); frontier measured; GOTO-ERAD survey. FINDING-2026-07-29-CLAUDE-SN4-S21XE-ACROSS-THE-BOARD-CLASSIFIER-AND-THE-FRONTIER-IS-ORDINARY-SCALARS-IN-SUBJECT-POSITION.md
- s21x-d (07-29) per-graph all-or-nothing regime opt-in (`SCRIP_STMT_FRAME=1`); st_x/st_pre stubs; fc-flavor conjunct; armed 7/316. FINDING-2026-07-29-CLAUDE-SN4-STMT-FRAME-SLICE1-LANDED-AND-THE-FC-FLAVOR-CONJUNCT.md
- s21x-c (07-29) DESIGN OF RECORD (kept above in full).
- s21x-b (07-29) artifact provenance sweep (506 dead files deleted, SCRIP `2c22b2c5`); RBP-SHED + SRC-ORDER ladders written; regen scripts added.
- s21x (07-29) FB-STMT opt-in (`81345044`); rebalance premise falsified (kept above as ladder).
- s206 (07-28) port-7 selectability fixed (`cca948c5`); predicate-flip DO-NOT-RETRY (kept as law).
- s205/s204 (07-28) ZHEAP plan of record + α/ω discipline + addressing contract — superseded s21x-c.
- s203 (07-28) SEQ/ALT prefix-allocation seed (`d5c7a72b`) + register-planes model (DESIGN-SN4-REGISTER-PLANES.md); cell_6 self-suspect note.
- s202 (07-28) cell seeds 2-5 + icon_cell_1 (`1e881ce5`). s202 ALT-FLAT (`0f31f7f7`): zero-cell ALT, flat arms via fc_arm_member (registration order load-bearing, lower ~1509); 155 fixed, 123 broke — defer-window root + r9 park fix named.
- s200 (07-28) bisect → FLATDISP-1 first-bad; fc_geom DEFER omission; PATREF split directive; 4-line gate reproducer. FINDING-2026-07-28-CLAUDE-SN4-DEFER-GEOM-AND-PATREF-SPLIT.md
- s199 (07-28) DEFER-STAR eager/unevaluated split, census 113→48 (`9b19bb5a`). FINDING-2026-07-28-CLAUDE-SN4-DEFER-STAR-SPLIT-IS-THE-NARROWING-S197-MISSED.md
- s198 (07-28) SCANBASE reland (`6c16762a`); stored-pattern segv localized to bb_match_release. FINDING-2026-07-28-CLAUDE-SN4-SCANBASE-LANDED-AND-STORED-PATTERN-SEGV-LOCALIZED.md
- s197 (07-28) DEFER removal falsified (kept as law); deferred-arg family found; concurrent-writer incident. FINDING-2026-07-28-CLAUDE-SN4-DEFER-DEEP-IS-LOAD-BEARING-AND-DEFERRED-ARG-FAMILY-BROKEN.md
- s196 (07-27) SCANBASE; FLATDISP-5a premise falsified → scan-retry 5th member (kept as law). FINDING-2026-07-27j-CLAUDE-SN4-SCANBASE-AND-FLATDISP-5A-PREMISE-FALSIFIED.md
- s195 (07-27) two FLATDISP displacement mines killed, m3 185→221 (`62aaf9ff`); FRQB born. FINDING-2026-07-27i-CLAUDE-SN4-FLATDISP-CAPTURE-CALL-DISPLACEMENT-MINES-AND-ARBNO-ELEMENT-CLOBBER.md
- s194 (07-27) FLATDISP-7 per-graph pin gate, rbp census 237→119 (`d95b1a98`). FINDING-2026-07-27h-CLAUDE-SN4-FLATDISP-7-JMP-ENTRY-PIN-GATED-AND-RK-SUBS-PREEXISTING-SEGV.md
- s193 (07-27) FLATDISP-5b/5c + ABORT-NODE + bracket gate (`b3996516`); Lon's per-BB directive first stated. FINDING-2026-07-27g-CLAUDE-SN4-FLATDISP-5B5C-ABORT-NODE-BRACKET-GATE.md
- s186 (07-27) SN4-RTX split out to its own file; the shrink-pass lesson (8 live rungs deleted by a prior prune, recovered from `950e6a9f`).
