## ↗ SN4-RTX MOVED OUT — s186 (2026-07-27)
The **SN4-RTX** ladder (runtime in optimized x86-64 asm) is **no longer in this file**. It lives in **`GOAL-SNOBOL4-RTX.md`**, with contract `ARCH-SNOBOL4-RTX.md`. It was split out so RTX can run CONCURRENTLY with the ζ-storage / value-spine work below.
⚠ **HOW IT WENT MISSING, recorded so the shape is recognizable next time:** the ladder was deleted from this file at `.github` `0c595551` (s176, 2026-07-26) by the shrink pass that terse-ified s175–s180. That pass removed **8 unchecked rungs (RTX-4-slice-2 … RTX-12)**, not just completed history, and left `ARCH-SNOBOL4-RTX.md` pointing at a `§SN4-RTX` section that no longer existed. Recovered verbatim from `950e6a9f`. **Pruning is for COMPLETED session state (RULES.md (c)); a live `- [ ]` ladder is not session state.**
⛔ **CONCURRENCY:** RTX phase-1 rungs touch ZERO templates by construction and are safe alongside this file's ladder. **RTX-11 (RTX-CC) and RTX-12 are NOT** — they edit `x86_asm.h` / templates and fire `.s` regen ×3. The crosscheck watermark is SHARED STATE between both ladders: re-prove it at session start.

---

## ⛔ LIVE CURSOR — s189 (2026-07-27): **NO-SWITCH COLLAPSE — ZC_FLATDISP DELETED, RSP FRAME BASE IS UNCONDITIONAL.** SCRIP `c8424718`. **LON DIRECTIVE: "remove the symbol ZC_FLATDISP and all it's cohorts, because we will not be changing back. Make the code not switch on anything, but simply just do the right thing."** DONE: `ZC_FLATDISP` + `x86_flatdisp_on()` deleted — ENFORCEMENT BY DELETION, the same discipline that retired the `SCRIP_ZETA_FRAME` env override. `x86_frame_off(off) = off + g_emit.op_flat_disp` unconditional; `x86_fb()`→`"rsp"`, `x86_fb_num()`→`4`, fr32/fr64 prefixes→`[rsp + `, all now constants. **The rbp and r12 arms of those five accessors are GONE — there is no longer any way to spell an rbp frame reference.** VERIFIED: behaviour identical to the build it replaces (rbp 323, clean 12/16) and `util_regen_benchmark_s_artifacts.sh` reported "No changes — already current", proving a pure constant-fold with ZERO codegen delta. `grep -rn ZC_FLATDISP src/` = 0 in code (one prose mention documenting the deletion). NEXT RUNGS UNCHANGED from s188 below — FLATDISP-2 universal depth walk is still the one that retires the 3 known-bad. NOTE: `ZC_FRAME` itself still carries dormant `ZC_FRAME_RBP`/`ZC_FRAME_R12` arms elsewhere (`x86_zr`, `zeta_choices.h`, the `IF(ZC_FRAME != ZC_FRAME_RSP, ...)` dances in `bb_match_defer.cpp`/`bb_match_release.cpp`); collapsing THAT symbol the same way is a named follow-on, larger blast radius, not done here.

## ⛔ LIVE CURSOR — s188 (2026-07-27): **FLATDISP LANDED — RSP IS THE FRAME BASE, rbp -89%.** SCRIP `<this session>`. **LON DIRECTIVE: "Remove usage of EVERY RBP reference possible... It is ONE function that calculates the offsets... calculated based on the known sequence of BB's." + on the fallout: "Keep you change that made RSP the default. I do not care about 3 programs failing."** HEADLINES: (1) 🐞 **THE FC GATE COUNTS A SUBSET — THIS IS WHY THE LADDER NEVER CONVERGED.** `test_gate_fc_no_residual_rbp.sh` reports residual-rbp 0 (s185: `FC_BASELINE 13006 -> 0`) while the emitted code carries **2,951 `rbp` refs across the 16 benchmarks**. `x86_fc_hit` scores a miss only for OWN+full-cell; CROSS and OWN+window-only are correct-by-design AND STILL EMIT `[rbp+N]`. The gate answers "did a granted box miss its own window?", not "is rbp gone?". SAME SHAPE AS s184's patience-measuring FC gate — second time this instrument has reported convergence the bytes contradict. (2) ✅ **`x86_frame_off()` — THE ONE OFFSET FUNCTION.** `off + g_emit.op_flat_disp`, gated by build constant `ZC_FLATDISP` (default **1**; `-DZC_FLATDISP=0` is the A/B control, NOT an env switch — the compile-time-only frame law holds). Exactly FOUR consumers, nothing else may add a frame displacement: `x86_r12_modrm` (BINARY, compensated BEFORE the mod/disp-width choice), `x86_frame_text_mem` (TEXT — same function, so R10 holds by construction), `FR`, `FRQ` (fallback path only; the `fc_hit` window path already rebases to current rsp and must not double-compensate). `x86_fb()`→"rsp", `x86_fb_num()`→4, fr32/fr64 prefixes→`[rsp + `. (3) ✅ **MEASURED: 2,951 → 323 rbp (-89%), clean 15/16 → 12/16.** Refactor proven **byte-neutral with the gate off** (rebuilt at 0: 2,951 → 2,951 exactly). D=0 is EXACT not fallback: `xa_flat` seeds `sub rsp,K_total; mov rbp,rsp`, so at the seed rsp==rbp and a cell-free statement needs no compensation. All three `.s` sweeps regenerated. ⚠ **BUILD TRAP:** `make` does NOT track `x86_asm.h`; the first measurement this session read a stale binary (zero `.o` rebuilt). Always `touch src/templates/*.cpp src/emitter/*.cpp` after editing it; verify with `find . -name '*.o' -newer src/templates/x86_asm.h | wc -l`. (4) 📋 **BLOCKER IS ONE LINE, NOT A DESIGN GAP.** `fc_leaf_walk()` (`lower_snobol4.c:1038`) IS the prefix sum over the BB sequence Lon describes — correct, ALT handled by S10d pad-to-max. It is just called on ONE range: `if (fc_lin) fc_leaf_walk(g, before_pat, g->n, 32)`. Everything else gets `op_flat_disp=0`; where cells ARE pushed outside that range (the ZB-VAL `fc_vlit_active` grants are the live example) the address is wrong by the carve depth. **KNOWN-BAD, ACCEPTED BY DIRECTIVE:** `mixed_workload`, `pattern_bt`, `string_pattern` (SIGSEGV, pass at baseline); `eval_dynamic` timeout is **PRE-EXISTING** (fails identically at baseline). **NEXT RUNGS:** (a) **FLATDISP-2 UNIVERSAL DEPTH WALK** — call `fc_leaf_walk` over the whole graph with a per-statement reset at the S10e unwind boundary, registered AFTER the pattern-range call (first-match wins, so proven entries survive); retires the 3 known-bad. (b) **FLATDISP-3** — delete the now-dead `xa_flat` rbp seed/save/restore set (~half the 323). (c) **FLATDISP-4** — route the hand-spelled `[rbp+N]` template refs through the funnel (the other half). (d) **Fix the gate** to ratchet on compiler-output rbp count, not `x86_fc_hit` classification. → `FINDING-2026-07-27c-CLAUDE-SN4-FLATDISP-RSP-IS-DEFAULT-AND-THE-FC-GATE-COUNTS-A-SUBSET.md`. WATERMARK: m3/m4 crosscheck NOT re-proven this session (rung landed with known regressions by directive); rbp census 2951→323 is the live metric.

## ⛔ LIVE CURSOR — s187 (2026-07-27): **FLATDISP-RESTORE — op_flat_disp BACK IN FLIGHT.** SCRIP `804de628`. HEADLINE: fc_leaf_disp() has been computed on every compile since s65 and silently discarded since s83 (REG-7 U3 deleted the FR/FRQ consumer). Every session since has been re-deriving that displacement per-IR-kind through the fc_geom whitelist — which is why rbp eradication never converges. This session restored the groundwork: op_flat_disp field appended to g_emit (s141 ABI law), fc_leaf_disp(nd) delivered at the single DRIVE_FILL reset point in emit.cpp. Output byte-identical to baseline (verified by cmp). **[s188: the NEXT RUNG named here is now LANDED — see the s188 cursor above.]** WATERMARK: m3 314/1·m4 312/1·DIVERGE=0 (untouched this session).


## ⛔ LIVE CURSOR — s186 (2026-07-27): **SNOBOL4 IR_MATCH_* FAMILY CLOSED + SN4-FLAT-PROC RUNAWAY FIX.** SCRIP `2628ff67`. HEADLINES: (1) ✅ **ZB-FC-VALUE** — IR_MATCH_VALUE → ZERO-LAW. Its 16B matchvalue.pad was never read or written (emit arm sets op_off=-1, template addresses only FR(op_a_slot) and cross-box scan fields, resume already on rsp). Grant's own comment conceded "nothing to stash here" and kept the quad solely to avoid offset shifts — reason obsolete since s185's derived-extent work. ZERO-LAW: a box owning no private RW state has a zero cell as its FORTH form. Conversion map 26→25 unconverted. **CLOSES THE SNOBOL4 IR_MATCH_* FAMILY** — every kind now has a FORTH mechanism; only FENCE1 stays flat, NO-CONVERT by design. Measured: calculator-1 sub rsp,160→144 per MATCH_VALUE node, all downstream offsets correctly renumbered. GATES: m3 314/1 · m4 312/1 · DIVERGE=0 · FC residual-rbp 0. (2) ✅ **SN4-FLAT-PROC RUNAWAY FIX** — Bisected to 3681e719 (s176, mislabeled "regen remaining feature test .s artifacts" but carrying the entire SN4-FLAT-PROC implementation — 11 source files, 177 insertions, zero artifacts). Its ENTERABLE-CHAIN ROOTS hunk seeded the emission BFS with every labeled statement's anchor; the per-call nodes[] dedup cannot see across chain calls, so mode-4 re-emitted the whole enterable program per DEFINE stub. json.sno: 168→1871 statement blocks, 159 DISTINCT statements stamped up to 28x. Fix: seed anchors only on the entry chain, TEXT ONLY — mode 3 left byte-identical (it needs the seeds per-chain; a text-global emitted-set collapsed m4 to 286/29 because labels are per-chain namespaces). MEASURED: json 175320→29210 · treebank-list 104666→15777 · calculator-1 47960→11833 · claws5 25965→12179. GATES: m3 314/1 · m4 312/1 · DIVERGE=0. NOT CLOSED: 6 residual dup labels in json (.Lbynamefn nid-minting class, s184's named rung); beauty mode-4 did not complete in 240s (non-termination suspected, not proven). OPEN WORK: IR_CALL argv audit DONE — argv is live, escaping-by-pointer into rt_call_arr (marshal_call_arg writes each arg at argbase+i*16; lea rdi/rsi, FRQ(argbase) hands array address to C); genuinely different from MATCH_VALUE's dead pad; needs depth-compensation machinery, designed rung. NOTE: RTX has been split into its own GOAL and is being worked in a parallel session — do not enter RTX ladder rungs here. NEXT RUNGS: (a) util_fc_spine_census.sh cross-check — validate conversion map against emitted code (every CELL kind should show nonzero push count). (b) .Lbynamefn duplicate-label fix (nid-minting in bb_call.cpp, per-chain reset, file-global namespace). (c) IR_CALL family FORTH conversion (argv escapes, needs depth-compensation design). WATERMARK: m3 314/1·m4 312/1·DIVERGE=0.

## ⛔ LIVE CURSOR — s185 (2026-07-27): **FC METRIC RECLASSIFIED + BASE-CONVENTION HAZARD CLASS LANDED.** SCRIP `df8038d9`. HEADLINES: (1) ✅ **ZLS-CALL-BASE** — all three call arms in `zls_grant_locals` computed field offsets in node-base terms while receiving a locals base; argv/extra-quad offsets corrected. Phantom DESCR at +0 landing on another node's result quad eliminated. Gate: mode-3 crosscheck 314/1, fail set identical to rebuilt pre-change baseline; mode-4 14/14 call-heavy, DIVERGE=0. (2) ✅ **FC-AUDIT** — `x86_fc_hit` was counting correct-by-design behaviour (99.1% of the 13,006). Three classes: OWN+full-cell (the defect), OWN+window-only (HEAD's documented FLAT post-unwind fields — MUST stay rbp), CROSS (neighbour reads, normal emitter behaviour). True corpus residue was 108, then 0 after C_i fix. FC_BASELINE 13006 → 0. (3) ✅ **FC-108 CLOSED** — the 108 were also an artifact: `op_own_ci` = `loff + bytes` overstated every shifted node's end by exactly the shift (same base-convention class as ZLS-CALL-BASE). C_i now uses `zls_result_off() + zls_node_bytes()`. Codegen byte-identical across both classifier and C_i changes (md5 verified). (4) ✅ **`util_fc_conversion_map.py`** — durable source-derived tool classifying all IR kinds by seven FORTH conversion mechanisms (CELL / ZERO-LAW / ZERO-RETIRE / WINDOW / XBOX-READ / TAIL / NO-CONVERT). HEADLINE: of the SNOBOL4 IR_MATCH_* family, **exactly one kind remains unconverted: IR_MATCH_VALUE**. SEQUENCE is retired (fc_seq_active), DEFER/ARBNO are tail-granted, FENCE1 is NO-CONVERT by design (watermark quad must stay [rbp+off], depth-immune). The 27 UNCONVERTED are overwhelmingly Icon scan kinds and the call family. (5) 📋 **CORRECTS A WRONG TIER LIST** — a flat-frame field census cannot see mechanisms 2-6; "SEQUENCE→DEFER→FENCE1→VALUE" as Tier 1 was mis-scoped (SEQUENCE already done, FENCE1 impossible). NEXT RUNGS: (a) IR_MATCH_VALUE — single remaining SNOBOL4 match kind, small rung, closes the SNOBOL4 family. (b) `util_fc_spine_census.sh` cross-check to validate the map against emitted code (every CELL kind should show a nonzero push count). (c) IR_CALL (405 flat local fields) — largest genuinely uncommitted kind; audit its argv cross-box contract. WATERMARK: m3 314/1·m4 312/1·DIVERGE=0 (untouched). `handoff_status.sh` is the push truth.

## ⛔ LIVE CURSOR — s184 (2026-07-27): **FC GATE CORRECTED — IT WAS MEASURING WALL-CLOCK PATIENCE.** SCRIP `e63a8b6a`. HEADLINES: (1) ✅ **THE GATE'S "BASELINE 0" WAS NEVER TRUE OF THE SCRIPT** — it asserted `total -ne 0 -> exit 1`, measured 5,328, and so had been FAILING on every run since landing (which is why s183 said not to trust its headline). (2) ✅ **THREE DEFECTS, NOT ONE** — the rung named the timeout; measuring found three. `timeout`'s 124 was discarded by the pipe into `grep -c`, so beauty's truncated count was scored as data (3,984 @20s → 26,088 @120s — it measures how long you waited; this is the arithmetic reason s183 read ~5,600 where s184 reads 5,328 on the IDENTICAL tree). A failed compile scored as a clean 0. And no include path was set, so `demo/expression.sno` failed silently — with `SNO_LIB` seeded it compiles clean and reports **11,662 misses, the largest population in the corpus, invisible for the whole life of the old gate**. (3) ✅ **HONEST BASELINE = 13,006 over 50 cleanly-compiled programs**, deterministic across passes; the 1,344 non-beauty portion matches s183 exactly, confirming both sessions read the same tree. (4) ✅ **NOW A RATCHET** — classify OK/TIMEOUT/NOCODE, headline over OK only, fail on regression above `FC_BASELINE` or on any NEW timeout / NEW compile failure; known-bad named in-script and reported loudly every run. All three arms verified to fire. (5) 🐞 **NEW ORACLE-CONFIRMED PARSER DEFECT** — `:< C >` (direct Goto, manual p.187) is rejected by SCRIP; `:<C>` and `:S<C>` parse. SPITBOL accepts BOTH (`sbl -b` prints `1`). Interior blanks only. Real defect, quarantined not hidden. (6) ⚠ **beauty.sno MEASURED, NOT DIAGNOSED** — 627 source lines → **34,585,351 asm lines at 280s, still running**, rate FLAT across 7× time (115k/s @40s → 123k/s @280s). Non-termination is STRONGLY INDICATED BUT **NOT PROVEN** — the decisive cycle-detection experiment was cut for budget; do not record it as established. Also: the prior BYNAMEFN finding's "budget ≥5 min" completion test is WRONG (280s is nowhere near), and the label-collision↔runaway causal link is UNESTABLISHED — renaming labels on a compile that never yields a complete `.s` fixes nothing observable. MONITOR-FIRST does not reach this bug (compile-time, no runtime to trace). (7) **NEXT RUNGS** — settle beauty termination (two-window cycle detection) BEFORE the `.Lbynamefn` rename → fix `:< C >` parser defect → correct `REPO-corpus.md` (documents `demo/inc/`, which does not exist; real path `beauty_suite/`) → widen `zls_elide_ok` to IR_CALL (234 dead, zero locals, needs cross-box reader audit as bb_cmp_test got in s182) → IR_MATCH_ASSIGN_COND (123 dead) → ZB-VAL-8c per-node op_fc_wbytes → statement-root spine entry. WATERMARK: m3 314/1·m4 312/1·DIVERGE=0 (untouched this session). `handoff_status.sh` is the push truth.

## ✅ s183 (2026-07-27): **ZB-VAL-8b-USE-2 LANDED.** Baseline `6e7f13f1` → `e5ce9f12`. CHOKE-POINT FIX: `op_res_live` promotion moved from the IR_CMP_TEST dispatch arm to `walk_bb_node_inner` entry (the single path FILL/EMIT_PAIR_FILL/DRIVE_FILL all pass through) — the arm-local site was stale-unsafe because DRIVE_FILL resets ~20 op_* fields but NOT op_res_live. Write-side gating on both result-store pairs in `bb_assign_global`/`bb_assign_local`; IR_ASSIGN is 1,577 of 3,448 measured-dead result cells (46%) across 315 crosscheck programs. 5,146 instructions removed, deterministic, zero regression. [s183's item (5) — "FC gate baseline is wrong" — is CLOSED by s184 above, which found the cause was three defects in the gate, not one.]

## ✅ s182 (2026-07-26): **ZB-VAL-8b-USE LANDED.** `zls_entry_t.live` + `zls_result_live()` + `g_emit.op_res_live` promoted at IR_CMP_TEST arm. bb_cmp_test and IR_ASSIGN both gated. 5,146-line win measured (but note: op_res_live promotion site was stale-unsafe — fixed s183). → `FINDING-2026-07-26g-CLAUDE-SN4-ZB-VAL-8B-USE-LANDED-THE-PREDICATE-ALREADY-EXISTED.md`.

## ✅ s180 (2026-07-26): Survey ZB-VAL-8 — two blockers found + resolved on paper. RBP=two jobs (addressing=convenience/frontier=necessity). Four walls measured. ARBNO=compound of retry+fence, no private wall. 8a depth-index refactor DELETED. → `FINDING-2026-07-26e`.

## ✅ s179 (2026-07-26): ZB-VAL-6a CONCAT + ZB-VAL-6b UNOP landed. β-dead invariant confirmed empirically (β-trampoline never fires on spine → uniform `wpop=d*16` safe). ZB-VAL-7 relops DELETED (SNOBOL4 has no infix relops; predicates are functions). Watermark m3 314/1·m4 312/1·DIVERGE=0. → `FINDING-2026-07-26d`.

## ✅ s178 (2026-07-26): ZB-VAL-1..5 landed — value spine reaches arbitrary arith trees with typed global-var leaves, all rsp cells. Binop net = `add rsp,16`. Post-order geometry shape-invariant. Fallible-box ω = full unwind via BP-9 op_wpop. Watermark m3 332/2·m4 323/1. → `FINDING-2026-07-26`.

## ✅ s177 (2026-07-26): ZB-VAL-0 + CF-GATE landed — first value-spine FORTH cell + constant-fold gate. Watermark m3 332/2·m4 323/1.

## ✅ s176 (2026-07-26): SN4-FLAT-PROC landed. One-statement scope. IR_SAVE_RESTORE + bb_save_restore.cpp. RETURN/FRETURN/NRETURN = program-wide floaters via rt_flat_ret_snap. Watermark m3 314/1·m4 312/1·DIVERGE=0.

## ✅ s175 (2026-07-26): SN4-PARSE-ROMAN landed. Four parser defects fixed (label-only line, blank between goto parts, lineno always 0, silent parse errors). .l flex-regenerable again. Watermark m3 314/1·m4 309/4·DIVERGE=3. → `FINDING` (roman).
