## ⛔ LIVE CURSOR — s194 (2026-07-27): **FLATDISP-7 LANDED — JMP-ENTRY rbp PIN GATED PER-GRAPH, CENSUS NET 237→119 (−49.8%).** SCRIP `d95b1a98` + 3 artifact sweeps (benchmark/feature/demo). **WATERMARK m3 185/130 · m4 183/130 · DIVERGE=1 (W06_tab) — re-proven fresh at session start AND pre-commit, HELD EXACTLY.** HEADLINES: (1) ✅ **`emit_jmp_pin_rbp()` (emit.h, beside g_emit) = `flat_deep_arrival || flat_pat || flat_gen`** — the s193 xaf_deep single-reader discipline applied to the jmp-entry regime: every jmp-entry gate site in BOTH media (prologue save+seed: BINARY `hdr` + `xaf_jmp_hdr_x86` + 3 TEXT snprintf twins; determinate epilogues; role-3 wire-adopt) reads THIS, never the raw fields. **The flat_pat/flat_gen conjuncts are LOAD-BEARING, not conservatism**: a suspending γ RETAINS with rsp at the deep frontier and its record protocol reads the PINNED rbp by design — so an INVARIANT PAT$ blob (pure LIT body, classifies depth-static by kinds) keeps its pin, which is what makes 7 safe INDEPENDENT of 5a. 5a will make the conjunct fire less, never wrong — the s193 cursor's ordering (5a before 7) turned out unnecessary. (2) ✅ **Depth-static determinate epilogue arms (both media)** restore the pre-s90 rsp-relative form (`mov rdi,[rsp]` result reads, wire reads `[rsp+kt-24/-16]`, `add rsp,kt` = the ENTIRE teardown, zero rbp touch) — the form the s90 comment records as "true for SNOBOL4's determinate procs, FALSE for Icon", now selected per-graph, language-blind. New `xaf_ld64_rsp`/`xaf_addq_rsp` helpers do as-matching disp8/imm8 short forms (R10). (3) ✅ **Role-3 wire-adopt** (bb_save_restore.cpp) depth-static arm: header reads via rsp (runs immediately post-prologue, pre-carve, rsp==base in both regimes), caller rbp marshalled from the LIVE REGISTER (`mov rcx, rbp` — never saved, never clobbered). **Role-1/2 floaters correct UNCHANGED in both regimes**: unseeded ⇒ rbp still holds the caller's value ⇒ the snap-record restore is a same-value write. (4) ✅ **CENSUS: fibonacci/func_call/func_call_overhead/indirect_dispatch 24→1 net each** (residual = the wire-adopt marshal read, a register read not a frame ref; the 2 class-D are the floater restores), mixed_workload 49→36, roman 42→29, **twelve of 16 benchmarks now net ≤ 1**; pattern_bt/string_pattern 25 each UNCHANGED (PAT$ blob pins + fence boxes — the 5a population). Ratchet BASELINE 237→119. fibonacci 26→3 rbp TOTAL, fully accounted (2 prologue pairs + 2 epilogue quads + 4 wire-adopt + 2 class-D = the old 26; no hidden readers). (5) ✅ **Cross-language proof measured, not assumed**: Icon 4/0→4/0; SNOBOL4 bench-modes status column IDENTICAL per program baseline↔after (OK=8 FAIL=4 CRASH=4, all pre-existing, RT_OPT=-O0). (6) ⚠ **Prolog/Raku suites are NONDETERMINISTIC with the SAME binary** (measured both builds): Prolog FAIL↔SKIP churn 2/3/4↔58/59/60 = timeout-sensitivity; Raku flips 51/0↔50/1. **rk_subs: PRE-EXISTING 100% segv on BOTH builds** — baseline "passed" by consistently emitting NOTHING (empty×3 agree; no .ref so the suite falls to its run-vs-run arm); the emitted program also produces WRONG partial output ("0/hello " vs expected "14/hello raku/…") before crashing. FLATDISP-7 merely perturbs the flush race so the wrong bytes sometimes survive → the pre-existing breakage became VISIBLE as suite flake. Raku-ladder item, not a FLATDISP regression. (7) 📋 **Known cost, recorded not hidden**: +5 xa_flat medium-invisible WIP debt (101→106) — the new epilogue helpers are raw-byte in the legacy epilogue stream where x86() records would corrupt the byte-offset patch bookkeeping (`out_site = size()`); XA-FLAT-CONVERT retires the family wholesale. (8) 📋 **Carried forward from pruned cursors**: (s189) `ZC_FRAME` still holds dormant `ZC_FRAME_RBP`/`ZC_FRAME_R12` arms (`x86_zr`, `zeta_choices.h`, the `IF(ZC_FRAME != ZC_FRAME_RSP,…)` dances in bb_match_defer/release) — named follow-on, larger blast radius; (s186) 6 residual `.Lbynamefn` dup labels still block test_string/claws5/json artifact updates; beauty mode-4 termination unsettled (s184: 34.5M asm lines @280s, non-termination indicated NOT proven). **NEXT RUNGS:** (a) **FLATDISP-5a — frameless invariant PAT$ blobs** (`xa_pat_blob_invariant_n`, grep=0, NET-NEW): emit invariant pattern inline, no PAT$ proc, no DT_P round-trip — retires pattern_bt/string_pattern's 25s and shrinks the flat_pat conjunct's reach. (b) **rt_dcap_pump SIGSEGV** (`pattern_match.c:673`, blocks the 130, pre-existing since s191): `memcpy(copy, c->subj + e->saved_delta, len)` with `saved_delta`≈2³², dcap island only 24B, suspected field-offset disagreement between the `IR_MATCH_ASSIGN_COND` writer and the pump reader; `[ZHP] heap exhausted` is a secondary symptom. MONITOR-FIRST: 2-way monitor on `039_pat_any` or `041_pat_span`. → `FINDING-2026-07-27h-CLAUDE-SN4-FLATDISP-7-JMP-ENTRY-PIN-GATED-AND-RK-SUBS-PREEXISTING-SEGV.md`.

## ⛔ LIVE CURSOR — s193 (2026-07-27): **FLATDISP-5b/5c + FLATDISP-6 + ABORT-NODE + BRACKET-GATE LANDED. m3 185/130 · m4 183/130 · DIVERGE=1 (W06_tab) — FULL WATERMARK RECOVERY FROM s192 RED.** SCRIP `b3996516` (bracket gate) · `646e29ae` (feature sweep) · census gate `9edc982c`. **LON DIRECTIVE: "Have each BB allocate its RESULT value… Do it by one instruction, decrement RSP… Continue… until you hit a BRICK WALL and realize, oh I need a RBP stable base pointer… Several constructs we know we'll want RBP is for a STATEMENT, FUNCTION and ARBNO and FENCE1… Those are the only four."** HEADLINES: (1) ✅ **FLATDISP-5b: ROOT CAUSE WAS ARMING COVERAGE, NOT KIND LIST.** `emit_graph_has_deep_arrival()` was called only inside `emit_jmp_entry_for_proc`, but the gates reading `flat_deep_arrival` live on the *outer call-regime prologue arm* (xa_flat 382/383) — a path outer mains never armed, so they inherited cleared 0. Proven on 058: `main_α` emitted no rbp seed, then `n11_match_fence1_α` did `mov rsp, rbp` into the CRT caller's frame. Fix: classification moved to the `emit_chain` choke point — single writer, every graph (outer mains, PAT$ blobs, EVAL/CODE chains, rt pattern builds). (2) ✅ **FLATDISP-5c: BOTH-MEDIUM — `xaf_deep()` unifies all six outer-frame rbp gates.** `xaf_deep() = flat_deep_arrival || outer_nparams ≥ 1`. TEXT anchor-leave, TEXT prologue save+seed, TEXT epilogue reload, BINARY anchor-leave (the missing twin that caused m3-held/m4-broke in s192), BINARY epilogue success+fail reload — all six now read the SAME predicate. Latent bug fixed: TEXT gated the seed but stored `[rbp+16]/[rbp+24]` unconditionally (Icon param-0 bind). (3) ✅ **FLATDISP-6: deep list narrowed to the template-census rbp-reader set.** ALTERNATE/SEQUENCE/ARB/RETRY/RELEASE removed — comment-only in census, subsumed by HEAD (at the time). HEAD stayed because TT_ABORT lowered to a bare `IR_GOTO`, making abort-bearing statements classifier-invisible; dropping HEAD cost exactly 170/171 (m4 183→181→183, tripwire proven both directions). (4) ✅ **rbp CENSUS RATCHET GATE** (`test_gate_rbp_census_ratchet.sh`): compiler sweep (never artifacts), class-D scratch loads excluded (14 across 16 benchmarks), manual ratchet, baseline **NET=237**. First honest rbp instrument in this ladder (s184/s188 instruments were subset-only). (5) ✅ **ABORT-NODE:** TT_ABORT relowered from bare `IR_GOTO` to the real `IR_MATCH_ABORT` — dispatch case + drive arm (`op_off=-1`, no slot grant) + template linked in Makefile (it was never linked). Template's β→ω is *more* manual-faithful than the goto (SPITBOL manual Ch. 9: "backing up into ABORT kills the match"). With abort visible, HEAD left the deep list. (6) ✅ **BRACKET-GATE:** head/release/replace `+40` rbp save/restore bracket gated on `flat_deep_arrival` — the bracket exists for the ARBNO `zv()` borrow, dead in depth-static graphs. **Simple inline match statement now ZERO rbp — probe proven, output correct.** All three `.s` sweeps run per handoff step 4. **NEXT RUNGS:** (a) **FLATDISP-5a — frameless invariant PAT$ blobs** (`xa_pat_blob_invariant_n`): emit invariant pattern inline, no PAT$ proc, no DT_P round-trip — this will cut the 26-each in fibonacci/func_call/func_call_overhead/indirect_dispatch (all DEFINE-bearing, jmp-entry seeded unconditionally). (b) **FLATDISP-7 — gate jmp-entry seed** once blobs classify: the jmp-entry arm seeds unconditionally in both media (correct today because every PAT$ blob has fence/arbno/defer); once invariant blobs are frameless the arm can check per-graph. (c) **rt_dcap_pump SIGSEGV** (`pattern_match.c:673`) — blocking the 130 failing tests, pre-existing since s191. MONITOR-FIRST: 2-way monitor on `039_pat_any`. → `FINDING-2026-07-27g-CLAUDE-SN4-FLATDISP-5B5C-ABORT-NODE-BRACKET-GATE.md`.

---

## ↗ SN4-RTX MOVED OUT — s186 (2026-07-27)
The **SN4-RTX** ladder (runtime in optimized x86-64 asm) is **no longer in this file**. It lives in **`GOAL-SNOBOL4-RTX.md`**, with contract `ARCH-SNOBOL4-RTX.md`. It was split out so RTX can run CONCURRENTLY with the ζ-storage / value-spine work below.
⚠ **HOW IT WENT MISSING, recorded so the shape is recognizable next time:** the ladder was deleted from this file at `.github` `0c595551` (s176, 2026-07-26) by the shrink pass that terse-ified s175–s180. That pass removed **8 unchecked rungs (RTX-4-slice-2 … RTX-12)**, not just completed history, and left `ARCH-SNOBOL4-RTX.md` pointing at a `§SN4-RTX` section that no longer existed. Recovered verbatim from `950e6a9f`. **Pruning is for COMPLETED session state (RULES.md (c)); a live `- [ ]` ladder is not session state.**
⛔ **CONCURRENCY:** RTX phase-1 rungs touch ZERO templates by construction and are safe alongside this file's ladder. **RTX-11 (RTX-CC) and RTX-12 are NOT** — they edit `x86_asm.h` / templates and fire `.s` regen ×3. The crosscheck watermark is SHARED STATE between both ladders: re-prove it at session start.

---

## ⛔ LIVE CURSOR — s192 (2026-07-27): **FLATDISP-5 DEPTH-STATIC PREDICATE LANDED (TEXT ONLY) — ⛔ MODE-4 IS RED AND LEFT RED BY DIRECTIVE.** SCRIP uncommitted→see this session's hash. **LON DIRECTIVE: "Have each BB allocate its RESULT value, IF it has one and if it is used... Do it by one instruction, decrement RSP... Continue... until you hit a BRICK WALL and realize, oh I need a RBP stable base pointer" + "Do not revert anything, we are moving forward."** ⚠ **WATERMARK — THE TREE IS WORSE THAN BASELINE, DO NOT MISREAD THIS AS GREEN: baseline re-proven fresh on a clean clone BEFORE any edit = m3 185/130 · m4 183/130 · DIVERGE=1 (W06_tab), matching s191 exactly. AFTER this session = m3 185/130 (UNCHANGED — only the TEXT arm was gated) · m4 168/145 · DIVERGE=16.** The 15 newly-failing m4 programs are ALL fence/abort. HEADLINES: (1) ✅ **`flat_deep_arrival` PREDICATE** — `emit.h` field at struct end (s141 ABI law) + `emit_graph_has_deep_arrival(g)` beside `flat_gen`. 1 = an arrival can land BELOW the activation base → rbp-absolute restore; 0 = DEPTH-STATIC → the whole rbp quartet (save/seed/teardown/restore) is DELETED. Conservative by construction (unlisted kind ⇒ 1 ⇒ today's behaviour); NOT keyed on language per NO-LANGUAGE-SENTINEL. (2) ✅ **MEASURED 323 → 227 rbp (−96, −29.7%), EIGHT of sixteen benchmarks to LITERALLY ZERO** (arith_loop, eval_dynamic, eval_fixed, op_dispatch, string_concat, string_manip, table_access, var_access). No replacement instruction needed — `add rsp, Kt` was ALREADY in the TEXT epilogue; teardown collapses to `mov eax,1/xor edx,edx/add rsp,Kt/ret`. (3) 🐞 **THE rbp CENSUS IS FOUR POPULATIONS AND ONE IS NOT A FRAME REF.** A=150 ceremony · B=60 genuine unwind · C=99 housekeeping data refs · **D=14 `mov rbp,[rax+24]` = rbp as a SCRATCH GPR.** Any rbp-count ratchet (s188 rung (d)) MUST exclude class D or it can never read zero. THIRD mis-measuring instrument in this ladder (s184 patience-FC, s188 subset-FC, now the raw census). (4) 🧱 **THE WALL IS FENCE — a FOURTH beyond Lon's predicted STATEMENT/FUNCTION/ARBNO** — and the tripwire found it unaided: gating broke exactly 15 m4 programs, every one fence/abort. Already on record twice: the FORTH ledger has FENCE1 as "NO fc_geom BY DESIGN — the watermark quad must stay [rbp+off] (depth-immune) because the σ glue reads it at the dynamic post-P depth", and `bb_match_fence1.cpp:37` calls rbp "the activation's dynamic-ζ floor". (5) ⛔ **WHY THE FIX DID NOT TAKE — WRONG GRANULARITY.** Adding FENCE1+ABORT: no change. Adding the whole backtracking match family: still no change. The fence lives in the **PAT$0 blob, a SEPARATE GRAPH**; the outer statement graph has no fence node so it still classifies depth-static while depth leaks across the σ glue. **Depth-staticness must PROPAGATE callee→caller.** (6) ⛔ **BOTH-MEDIUM NOT SATISFIED** — only TEXT gated (hence m3 untouched); BINARY twins at `xa_flat.cpp` ~162/202/283/448-460 UNGATED. Nothing here is landable until they are. (7) ⛔ **ARTIFACT REGEN DELIBERATELY NOT RUN** (handoff step 4 deviation, recorded not hidden): the 3 sweeps would bake the regression into benchmark+feature+demo corpora across two repos. Run all three the moment m4 ≥ 183. (8) 📋 **`xa_pat_blob_invariant_n` DOES NOT EXIST** — grep = 0. Lon's s192 directive "ensure the PAT$0 blob invariant pattern DOES NOT use RBP, it needs no prologue or epilogue" is NET-NEW CONSTRUCTION, not a gate to flip. **NEXT RUNGS IN DEPENDENCY ORDER:** (a) **FLATDISP-5a PAT$ BLOB FRAMELESS** — build `xa_pat_blob_invariant_n`, emit invariant pattern inline, no prologue/epilogue, no DT_P round-trip; plausibly subsumes (b) since a frameless blob cannot leak depth. (b) **FLATDISP-5b CROSS-GRAPH DEPTH PROPAGATION** — a caller is depth-static only if every blob it transfers into is. (c) **FLATDISP-5c BINARY TWINS.** (d) **Fix the rbp gate to exclude class D.** → `FINDING-2026-07-27f-CLAUDE-SN4-FLATDISP-5-DEPTH-STATIC-PREDICATE-AND-THE-FENCE-WALL.md`.

## ✅ s183 (2026-07-27): **ZB-VAL-8b-USE-2 LANDED.** Baseline `6e7f13f1` → `e5ce9f12`. CHOKE-POINT FIX: `op_res_live` promotion moved from the IR_CMP_TEST dispatch arm to `walk_bb_node_inner` entry (the single path FILL/EMIT_PAIR_FILL/DRIVE_FILL all pass through) — the arm-local site was stale-unsafe because DRIVE_FILL resets ~20 op_* fields but NOT op_res_live. Write-side gating on both result-store pairs in `bb_assign_global`/`bb_assign_local`; IR_ASSIGN is 1,577 of 3,448 measured-dead result cells (46%) across 315 crosscheck programs. 5,146 instructions removed, deterministic, zero regression. [s183's item (5) — "FC gate baseline is wrong" — is CLOSED by s184 above, which found the cause was three defects in the gate, not one.]

## ✅ s182 (2026-07-26): **ZB-VAL-8b-USE LANDED.** `zls_entry_t.live` + `zls_result_live()` + `g_emit.op_res_live` promoted at IR_CMP_TEST arm. bb_cmp_test and IR_ASSIGN both gated. 5,146-line win measured (but note: op_res_live promotion site was stale-unsafe — fixed s183). → `FINDING-2026-07-26g-CLAUDE-SN4-ZB-VAL-8B-USE-LANDED-THE-PREDICATE-ALREADY-EXISTED.md`.

