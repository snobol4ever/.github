**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · IR INTERPRETER TOTALLY DELETED (Lon rule, verbatim: "IF it WALKS an IR graph to interpret it regardless of which mode, THEN it MUST be DELETED"). mode-2/`--interp` IS GONE; the new world is ONLY BBs running, no interpreter accessible. The engine (`IR_interp_node` walker + `IR_interp_once`/`_resume`/`_pump`) + EVERY IR-walking caller excised to `src/attic/IR_interp.c` (full 6091-line reference kept) and removed from the live tree; the survivor (`rt_scan_lit` + the NON-IR-walking Prolog runtime: rt_is/rt_succ/rt_atom_concat/rt_char_type/rt_copy_term/rt_format_term/rt_atom_chars_codes/type-tests) was stripped 6091->1822 lines and RENAMED `src/interp/IR_interp.c`->`src/interp/rt_runtime.c`; `IR_interp.h`->attic. DELETED outright (IR-walkers, zero live callers): `rt_scan`/`rt_scan_exec`/`rt_gvar_assign_concat` (scan shims that called IR_interp_once); `rt_findall`/`rt_aggregate`/`rt_catch`/`rt_aggregate_all_term` (Prolog meta-preds that EXECUTED goal graphs via IR_interp_once). BOMB-STUBBED (loud `[NO-IR-INTERP]` runtime fail; the IR-walking body deleted, symbol kept for the linker): `resolve_node_to_term` + `resolve_arith_eval` (the two IR-interpreting roots — neutralizes the Prolog GZ-cell arith/compare cluster `rt_pl_is_cell`/`rt_arith_cmp_nodes`/`rt_term_cmp_nodes`/`gz_eval_cell` that interpreted IR-node operands THROUGH them); `ir_call_proc` (walked IR via IR_interp_pump; `by_name_dispatch` is its caller); `bb_mapgrep_prepare` (interpreted the Raku map/grep sub-graphs at EMIT time). DRIVER `scrip.c`: `--interp` flag + `mode_interp` var + the entire `else if (mode_interp)` dispatch block + usage line + mutual-exclusion text ALL removed; the two "EXCISED — mode-2 (--interp) is the oracle" messages reworded to "native BB emission pending (no interpreter fallback)". `bb_scan_stmt.cpp` mode-3 non-literal arm is now `x86_bomb` (parity with mode-4, which already bombed it). Stale `IR_interp.h` includes purged from `box_rt.h`/`gen_runtime.c`/`resolution.c`. Makefile `IR_interp.c`/`.o`->`rt_runtime.c`/`.o`. Dead global `g_current_cfg` retained as an inert NULL (one orphaned reader). BUILD: `scrip` ✓ + `out/libscrip_rt.so` ✓ both clean. GATE RE-BASELINE — the contract moves from m2==m3==m4 to **m4 is the HARD GATE (intact); m3 drops to its native floor wherever it leaned on the interpreter** (mode-4 was already native on these shapes; deleting the crutch exposed where mode-3's native PB-RB emission isn't done): SMOKE m3 **7/7** m4 **7/7** (HARD) ✓ · PAT-RUNG m4 **19/19, 0-SKIP** (HARD) ✓ HOLDS, m3 **19->15** (052_pat_arbno / 053_pat_alt_commit / 054_pat_arbno_alt / 056_pat_star_deref bomb — they routed through rt_scan) · FENCE TIER1=0 TIER2=0 (HARD) ✓. The m2 column on smoke/pat-rung is now DEAD (harness still passes the removed `--interp`; that column is meaningless, not a regression). REALIZED BLAST RADIUS (forewarned, all intended under the rule): SNOBOL m3 non-literal patterns bomb (m4 unaffected) · Raku map/grep bombs · Prolog m3/4 findall/aggregate/catch/throw + GZ-cell arith/compare bomb (until native `bb_pl` lands) · EVAL hatch closed. COULD-NOT-RUN: the x86 full-corpus crosscheck (`test_crosscheck_x86_full_corpus.sh` expects the corpus at `/corpus/crosscheck`, not mounted in this env — path issue, unrelated to the surgery), so the corpus M3/M4 floor row was not re-captured this session. This kill is **iteration 0** of the DEAD-CODE REACHABILITY SWEEP rung (below). FILES: src/interp/rt_runtime.c (was IR_interp.c) · src/attic/IR_interp.c + IR_interp.h · src/emitter/BB_templates/{bb_scan_stmt.cpp,bb_mapgrep.cpp} · src/driver/scrip.c · src/runtime/builtins/{box_rt.h,gen_runtime.c,resolution.c} · src/interp/IR_interp_state.h · Makefile. CLEANUP FOLLOW-UPS (non-blocking): inert orphaned interp-support statics in rt_runtime.c (`g_current_cfg`+reader, `ir_is_single_shot`, `bb_is_gen_*`, `gen_resume_target`, the resolve_term_* helpers) — `-w` tolerates; the `src/interp/` dir name is now stale (holds only rt_runtime.c + the MIS-NAMED box-state header `IR_interp_state.h` = emit-time state structs, NOT interpreter — future dir/header rename); the test harness still wires the dead `--interp` (drop the m2 column).

**SESSION WATERMARK — 2026-06-14 · Claude · SCRIP base 3039f79 + 2 commits this session (3fd9798 SPAN one-shot β; e076f9e stored single-var cset M3/M4). TWO RUNGS LANDED + EVAL DESIGN CORRECTED BY LON (do-not-re-derive both). GATES all green, zero regress: smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP (m2==m3==m4) · fence TIER1=0 TIER2=0 HARD · corpus M2=186(=) M3=171(=) M4=166(=) SKIP=8(=) (held at floor; both rungs are latent capabilities with no corpus row, proven by oracle probes).**

**RUNG A — SPAN ONE-SHOT β (3fd9798).** SPAN literal+var-cset NATIVE MATCH templates (`bb_match_span.cpp`, `bb_match_span_var.cpp`) regenerated a SHORTER span (`count-1 … jmp γ`) on the β port — ARB/BREAKX behavior — violating SPITBOL one-shot semantics (manual Ch.6/18; only BREAKX/ARB/ARBNO/BAL regenerate). Wrong in m3/m4: `S='aab'; S SPAN('ab').A 'b'.B` gave `aa:b` (gave a char back so `'b'` matched) vs sbl/m2 `:` (match fails, SPAN does not give back). FIX (β port only): restore δ to the saved pre-span start (`FR(zo)`, written on every success; β only reachable post-success) + `jmp ω` — the correct one-shot fail-out, mirroring ANY/NOTANY/BREAK. Audited the whole one-shot family: SPAN was the LONE offender (ANY/NOTANY/BREAK already restore-and-fail; POS fails clean; TAB/RTAB/LEN/REM bare jmp ω washed by RETRY's per-attempt δ reset; BREAKX correctly regenerates). Probes m3==m4==sbl: `SPAN('ab').A 'b'.B`/aab=`:` · `SPAN('a').A 'a'`/aaa=`[]` · `SPAN('a').A 'b'.B`/aabb=`aa:b` (control) · `SPAN('x').A 'y'`/xxxy=`xxx` · `SPAN('a').A RPOS(0)`/aaa=`aaa`. Regenerating siblings unaffected (BREAKX/INTEGERS=INTEG, ARB/MOUNTAIN=UNT). Revealed (NOT introduced — IR_interp.c untouched) a pre-existing latent M2-interpreter SPAN-giveback (`SPAN('a').A 'a'`/aaa→m2 `[aa]`) + BREAKX (→m2 `G`); same family as parked M2-ARBNO-SHY, off the M3/M4 priority.

**RUNG B — STORED SINGLE-VAR CSET M3/M4 NATIVE (e076f9e); `rt_nv_cstr` STAMPED by Lon 2026-06-14.** The prior watermark's NEXT-(1). `P=ANY(var)`/SPAN/BREAK/BREAKX/NOTANY (single TT_VAR arg, no capture) now builds+matches in m3 AND m4 (was: m4 bombed in `bb_pattern_unary_s` var-cset `dval==1.0` → x86_bomb; m3 fell through). MECHANISM: the builder's var branch, at construction time, NV-gets the cset into r9 via `rt_nv_cstr(name)` BEFORE marshalling rt_pattern_build's args (rt_nv_cstr CLOBBERS rdi/rsi/rdx/rcx/r8 — ORDER MATTERS; r9 survives the subsequent arg-setup lea/mov), then bakes the pointer via the existing rt_pattern_build op_s slot. Literal branch byte-identical (var prepend + sealed-r9 lea are no-ops when dval!=1.0). CONSTRUCTION-TIME by parity with m2: rt_pattern_build STORES the op_s POINTER (no byte copy, pattern_match.c:751) and SNOBOL4 strings are immutable, so the build-time value is snapshotted; the builder reruns each `P=...` execution = per-eval rebuild. Probes m2==m3==m4==sbl (VARIABLE subject = routed native path): `P=ANY(a)` constr-time/bcz=`z` (a reassigned to abc after build, still {x,y,z}) · `SPAN(d)`/42rest=`42` · `BREAK(b)`/ab,cd=`ab` · `NOTANY(x)`/qzz=`z` · `BREAKX(s)`/abXc=`ab` · per-eval rebuild ANY(a) xy→bc=`xb`. IMMEDIATE control `S ANY(a).C` unchanged (sbl/m2/m3=`b`; m4 EMPTY = pre-existing immediate var-cset S2 gap, `bb_match_any.cpp` untouched). LITERAL subject (`'lit' ? P`) still S1-bomb. Ledger updated in SNOBOL4-5STAGE-OWNED-BUILD.md: `rt_nv_cstr` (nv GET, read) STAMPED; nv SET / raw allocator still REQUESTED. FILES: bb_pattern_unary_s.cpp (+rt_nv_cstr decl), bb_match_span.cpp, bb_match_span_var.cpp, SNOBOL4-5STAGE-OWNED-BUILD.md. NEXT for the stored-cset family (do-not-re-derive): (2) cset+CAPTURE compound `P=SPAN(d).N` (RHS is CAPTURE(SPAN), not bare cset) · (3) seq stored `P=SPAN(d) REM` (extend `sno_leaf_buildable`/`sno_build_leaf_ir` for TT_VAR cset arg) · (4) computed-concat cset `ANY(a b)`/`ANY(&UCASE &LCASE)` (match-time expr eval).

**⛔ EVAL — CORRECTED DESIGN (Lon directive THIS session; do-not-re-derive; SUPERSEDES the interp reading of the EVAL/CODE FACT-RULE exception).** EVAL must **NOT INTERPRET ANYTHING**. It MUST **EMIT BBs** and create an **EXPRESSION datatype just like the PATTERN datatype (DT_P)**. Pipeline: call the **PARSER → tree_t**, call **LOWER → IR_t**, call the **EMITTER → DESCR/DT_P/bb_box_fn**. **THERE IS NO INTERPRETER** in EVAL — `IR_interp_once` is FORBIDDEN inside EVAL (Lon overrode the old "IR_interp_once is the only sanctioned IR walker, reachable inside EVAL" clause). FINDINGS (verified this session, do-not-re-derive): (i) Current EVAL BOMBS — `EVAL_fn` (pattern_match.c:372) → CONVE_fn → EXPVAL_fn → `eval_node` (runtime_eval.c) is a B0b BOMB (AST-walk evaluator deleted). (ii) The ENTIRE compiler pipeline IS linked into libscrip_rt (verified via `nm -D out/libscrip_rt.so`): parser (`parse_expr_pat_from_str`, `sno_parse_string_ast`, `snobol4_parse`), lowerer (`lower_stage2` + lower_snobol4.c), IR (`scrip_ir.c`), EMITTER (`emit_core`, **`codegen_flat_build`**, all BB templates), AND IR_interp — so EVAL can run parser→lower→EMIT at runtime in all 3 modes. (iii) PARSE: arithmetic (`X+Y`, `2+3`, `X + Y`) does NOT parse as a SUBJECT — `parse_expr_pat_from_str` succeeds for var/lit/call/concat (`X Y`) but FAILS on binary arith (proven via harness). Arith lives in REPLACEMENT position. The fix: parse the EVAL string wrapped as a replacement — `sno_parse_string_ast(" Z_EVAL_Z = <src>\n")` then `stmt_attr_expr(stmt_attr_find(ast->c[0], ":repl"))` → full value-expr grammar; ALL doable in runtime_eval.c, NO parser-module edit (so NO bison regen). NB `sno_parse_string_ast` needs runtime init (uses `lex_open_str_initial`) — works INSIDE a running program (it's what `code()`/CODE() already use), FAILS in a bare harness (misleading; don't conclude it's broken). (iv) LOWER: `sno_arg_block`/`sno_arg_lower`/`lower_expr` (lower_snobol4.c ~93-105) lowers a value expression to a self-contained value-yielding `IR_graph_t` (256-node, IR_FAIL terminal); a ZEROED `snx_t cx={0}` is safe for value exprs (lower_expr only touches `cx->g` for binop/unop/lit/var/keyword/call). (v) hooks: `g_eval_str_hook`/`g_eval_pat_hook` set ONLY in scrip.c driver (mode-2/3 in-process), NULL in mode-4 standalone → CONVE/EXPVAL path. (vi) DELETE the stray `fprintf(stderr,"DBG IS_FAIL true!\n")` debug in EVAL_fn (pattern_match.c). **EVAL NEXT (emit-not-interp, do-not-re-derive):** (1) CONVE_fn: replacement-wrap parse → `:repl` tree_t (above). (2) Build an emit path: lower the expr tree → IR graph (sno_arg_block style), then EMIT to BB machine code into an expr pool (mirror `rt_pattern_build`/pat_pool for DT_P; `codegen_flat_build` is the candidate emitter entry) → a DTP_t-like executable head; wrap as an EXPRESSION DESCR (DT_E carrying executable code, the twin of DT_P). (3) EXPVAL_fn: replace the `eval_node(DT_E.ptr)` interp call with INVOKING the emitted expression (jmp entry, à la `rt_dtp_run` for patterns) → DESCR_t result. (4) `eval_node`: delete or repoint to the emit path; NO `IR_interp_once`. (5) Probe vs sbl m2==m3==m4: `EVAL('2 + 3')`→5 · `EVAL('X + Y')` X=2 Y=3→5 · `EVAL('SIZE("ab")')`→2. UNTOUCHED THIS SESSION (interp scaffold `sno_eval_expr_graph` was added then REVERTED — the LOWER entry above is documented so re-adding is trivial; it must feed the EMITTER, not IR_interp).

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base 448f529 + 1 commit this session. STORED SINGLE-VARIABLE CSET (NEXT-1) M2 LANDED — bare `P=ANY(var)`/`SPAN`/`BREAK`/`BREAKX`/`NOTANY` (single TT_VAR arg, NO capture) now build CORRECTLY in M2 via construction-time evaluate-and-bake; M3/M4 FAIL LOUDLY (bomb) pending a ledger stamp. Before: M2/M3 EMPTY, M4 bomb — `P=ANY(var)` fell through every LIVE build branch in `lower_assign` (each needed TT_QLIT/TT_ILIT) to the orphan `IR_ASSIGN_CONCAT+IR_SEQ` path that builds no working DT_P. CONSTRUCTION-TIME (oracle-verified, do-not-re-derive): STORED `P=ANY(a)` binds the cset when `P=...` executes, NOT at match — `a="xyz";P=ANY(a);a="abc";"bcz"?P.C`→sbl `z` (set {x,y,z}), NOT match-time `b`. Fix EVALUATES `a` at the build site and bakes the resulting string into the pattern OBJECT (the forbidden shortcut — route stored through the immediate match-time name lookup — returns `b` and diverges; NOT done). THE FIX (4 sites, 1 commit): (a) lower_snobol4.c `lower_assign` — NEW LIVE branch after the QLIT cset branch (~line 612): single-primitive cset with `c[0]->t==TT_VAR` → LIVE `DTP_ASSIGN` + `PATTERN_*`, `sval=NAME` + `dval=1.0` var-flag, UNIFORM dval across all 5 kinds (the OBJECT path has no BREAKX `ival`-collision, unlike the immediate matcher's split). QLIT branch UNTOUCHED → literal byte-identical. (b) IR_interp.c 5 cset object cases (`IR_PATTERN_ANY/NOTANY/SPAN/BREAK/BREAKX`, ~3379-3417): at the `rt_pattern_build` site, `dval==1.0 → cset = VARVAL_fn(NV_GET_fn(sval))` (current value baked into the built object); else literal `sval`. Build-cache guard `if(state)`→`if(state && dval!=1.0)` so a re-run of `P=...` REBUILDS with the current var value (construction-time-per-eval = SPITBOL rebuild-per-evaluation). Literal `dval=0` → guard + cset resolve IDENTICALLY to before (no M2 regression). (c) emit_bb.c dispatch (5 cset cases ~3053-3057): carry `g_emit.op_dval = IR_LIT(nd).dval`. (d) bb_pattern_unary_s.cpp: `dval==1.0` → LOUD `x86_bomb` (unconverted-box idiom: label α + bomb + def β + bomb) — M3/M4 var-cset-object FAIL LOUDLY rather than mis-bake the NAME as a literal cset (the silent-divergence anti-pattern). Literal `dval=0` skips the bomb → M3/M4 literal UNTOUCHED. PROBES (M2==sbl, 6): `P=ANY(a)` stored→`z` · loop-reassign `P=ANY(a)` (xy→bc)→`b` · `SPAN(d)`→42 · `BREAK(b)`→ab · `NOTANY(x)`→q · `BREAKX(s)`→abXc; IMMEDIATE control `ANY(a).C`→`b` UNCHANGED (immediate path untouched). GATES (non-decreasing HARD, all at floor, zero-regress): smoke 7/7/7 · pat-rung 19/19/19 no-SKIP · fence TIER1=0 TIER2=0 · corpus M2=186(=) M3=171(=) M4=166(=) SKIP=8(=). M2 held at 186 — the corpus has NO bare stored-single-var-cset row, so the capability is proven by the 6 probes, NOT yet a corpus row (latent until a test exercises it). LEDGER BOUNDARY (RULE 3): M3/M4 native var-cset-object needs `call rt_nv_cstr` (NV-get from emitted code, rt.c:257) at the build site to resolve the cset to a char* op_s — the ledger's "nv get/set · S2 operand fetch" item, STATUS still **REQUESTED** (not Lon-stamped). Until stamped, M3/M4 bomb. **NEXT (do-not-re-derive): (1) M3/M4 NATIVE — stamp `rt_nv_cstr`; in `bb_pattern_unary_s` the `dval==1.0` branch emits `mov rdi,[rip+name]; call rt_nv_cstr; mov r9,rax` in place of `lea r9,[rip+sealed]` (ONLY the op_s marshalling changes; clean char*, no DESCR_t ABI). Verify the emitted build re-runs each `P=...` execution. (2) cset+CAPTURE compound (`P = SPAN(d) . N` — RHS is `CAPTURE(SPAN…)`, not bare cset; my branch correctly skips it) — needs the cset+capture object path. (3) seq stored (`P = SPAN(d) REM`, multi-element) — extend `sno_leaf_buildable` (line 494) + `sno_build_leaf_ir` (line 526) for the TT_VAR cset arg so multi-element `TT_SEQ` builds. (4) computed-concat cset (`ANY(a b)`/`ANY(&UCASE &LCASE)` = old NEXT-2/064) — match-time expr eval. FILES THIS SESSION: src/lower/lower_snobol4.c, src/interp/IR_interp.c, src/emitter/emit_bb.c, src/emitter/BB_templates/bb_pattern_unary_s.cpp. Full detail: HANDOFF-2026-06-14-OPUS48-SNOBOL4-BB-STORED-CSET-SINGLE-VAR-M2.md.**

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base 4d37dcc (B7 FENCE) + 1 commit this session (1e3f30b). PB-RB-5 SINGLE-VARIABLE CSET HALF LANDED — and the prior watermark's NEXT (1) "064/065 = route flat_drive_capture through diamond-collapse" is a MISDIAGNOSIS, now corrected (do-not-re-derive the wrong path). VERIFY-FIRST FINDING: probing 064/065 against sbl showed the bug was NEVER FENCE/capture-specific NOR M4-only. The real bug = operand-variance in the cset family: a character-set primitive with a VARIABLE argument (`SPAN(var)`/`ANY(var)`/`BREAK(var)`/`BREAKX(var)`/`NOTANY(var)`) read NO operand and matched the variable's NAME as if it were the literal cset — wrong in M2 (the IR interpreter) AND M3, M4 link-fails. Smoking gun (now fixed): `SPAN(digits) . N` over `"42rest"` returned `st` (the only run of the letters `{d,i,g,t,s}` in the subject), and `POS(0) SPAN(d) RPOS(0)` over `"789"` "did-not-consume" (cset `{d}`); literal-cset `SPAN("0123456789")` was always correct. ROOT CAUSE (exact, code-level): lowering `lower_pat_node` (lower_snobol4.c:182-194) stored `IR_LIT(nd).sval = t->c[0]->v.sval` UNCONDITIONALLY for the cset family — for a TT_VAR arg that is the variable NAME, with no flag — whereas the integer family (POS/RPOS/LEN, lines 195-215) already discriminated `arg->t==TT_VAR||TT_KEYWORD → sval=name + dval flag` (the cut-#2 rung-2 `dval!=0` deferral) and was therefore correct. The interpreter was ALREADY equipped to look the variable up: SPAN reads `IR_LIT(bb).ival==1` (IR_interp.c:4300), ANY/BREAK/BREAKX/NOTANY read `dval==1.0` (3599/4272/4389) — SPAN must use `ival` because BREAKX already uses `ival==1` for its regenerate state (4286), so the cset family's variable-flag is split: SPAN→ival, the rest→dval. THE FIX (2 files, 1 commit 1e3f30b): (a) lower_snobol4.c cset cases now mirror the integer family — TT_VAR/TT_KEYWORD arg → `sval=name` + flag (SPAN `ival=1`; ANY/BREAK/BREAKX/NOTANY `dval=1.0`), literal arg → `sval=literal` byte-identical to before; (b) emit_bb.c `scan_pat_m3_native_safe` gains two lines deferring variable-cset scans to rt_scan (`(ANY|NOTANY|BREAK|BREAKX) && dval!=0.0` and `SPAN && ival==1`) — exactly how the integer family defers in M3. SAFETY: literal csets are provably byte-identical (the else-branch + the guard only re-routes variable shapes), so no literal-cset regression is structurally possible; the only behavioral change is variable csets, which were broken. SEMANTICS VALIDATED against sbl (the trap that kills the obvious "extend to stored patterns" shortcut): IMMEDIATE `ANY(a)` binds at the MATCH line (== construction, since immediate) — `a="xyz";a="abc";"bcz" ? ANY(a).C` → `b` in M2==M3==sbl ✓ (my fix is correct here). STORED `P=ANY(a)` binds at CONSTRUCTION time — `a="xyz";P=ANY(a);a="abc";"bcz" ? P.C` → sbl `z` (old set `{x,y,z}`), NOT `b`. So the stored-pattern cset must be EVALUATED-AND-BAKED at runtime construction, NOT looked up by name at match time; doing stored the quick name+flag way would silently return `b` (stale binding) and could pass the corpus while diverging from sbl — DO NOT DO IT. GATES (my-container floors, non-decreasing HARD): smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP (m2==m3==m4) · fence TIER1=0 TIER2=0 HARD · corpus M2 182→186 (+4) M3 169→171 (+2) M4=166 (held; variable-cset M4 was already failing so non-decreasing) SKIP=8(=), ZERO regress any mode. PROBES (now M2==M3==sbl): `SPAN(digits).N`/"42rest" anchored→2, unanchored→42 · `POS(0) SPAN(d) RPOS(0)`/"789"→consumed-all · `ANY(s).C`/"xQy"→x · `BREAK(b).C`/"ab,cd"→ab · `BREAKX(s).C`/"abXcd"→ab · `ANY(&UCASE).C`/"Hi"→H · literal `SPAN("0123456789").N`→2 (control, unchanged). 065 (`SPAN(digits)`, single var) M2/M3 NOW CORRECT; M4 still link-fails (`xcat15_right_β`). 064 STILL FAILS all modes by a DIFFERENT cause: its `ANY(&UCASE &LCASE)` arg is a COMPUTED CONCATENATION (also `ANY(a b)`), not a single var/keyword — needs match-time expression EVAL, out of scope here. **NEXT (do-not-re-derive; all three converge on the construction-time value-chain-into-ζ build-out = the genuine S2/PB-RB-5 rung, fresh-budget): (1) STORED single-var cset (`P=ANY(var)`) — builder `sno_build_leaf_ir` (lower_snobol4.c:526-532) constructs a native IR_PATTERN_* node ONLY when the arg is TT_QLIT; a TT_VAR arg falls through (`return NULL`) so the stored pattern goes via the deferred runtime-construction path, which carries the same unfixed bug. Fix MUST evaluate-and-bake the cset at runtime construction time (per the validated construction-time binding above), feeding the IR_PATTERN_* node — NOT a match-time name lookup. Touches the pattern-OBJECT path (used by every stored pattern) → land with the FULL gate suite as the regression net. (2) COMPUTED-CONCAT cset (`ANY(a b)`, `ANY(&UCASE &LCASE)` = what fails 064) — lower the cset-arg expression into the value chain, evaluate at the right time, bake; spans immediate + stored, M2/M3/M4. (3) M4 NATIVE LATE-READ + ALT cat-arm β (`xcat15_right_β`) — the native template (`bb_pattern_unary_s`) seals a compile-time literal cset slot; needs ζ late-read for variable/computed csets, PLUS the flat_drive_capture diamond-collapse/β back-wire for the ALT form (the prior watermark's item — real, but only the M4-ALT slice). Closes 064/065 in M4. FILES THIS SESSION: src/lower/lower_snobol4.c, src/emitter/emit_bb.c. Full detail: HANDOFF-2026-06-14-OPUS48-SNOBOL4-BB-CSET-OPERAND-VARIANCE-SINGLE-VAR.md.**

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base b2acdb4 (1 commit this session, rebased onto origin = behavior-neutral Icon IR_SCAN_* dead-code hygiene; combined tree rebuilt clean, all gates re-verified post-rebase). B7 FENCE NOW LANDED NATIVE in modes 3 AND 4 — three rungs, the B7 work the prior watermark only DIAGNOSED is now implemented (do-not-re-derive). RUNG B7a (bare-keyword FENCE + ABORT seal): bare `FENCE`/`ABORT` (the TT_VAR keyword form, manual Ch.9 p.124-125) now ABORT the ENTIRE match on backtrack with NO start-advance, matching sbl; was wrongly letting the unanchored scan advance the start (both templates `jmp ω`, but ω threaded through cat = `match_adv` = advance-start, NOT scan-overall-fail). 6 edits: (1) lower_snobol4.c bare-keyword FENCE sets `IR_LIT(nd).ival=1` (abort-seal marker; FENCE(P)/FENCE() function-form via TT_FENCE stay ival=0 = pass-through); (2) emit_bb.c new global `g_scan_seal_lbl`; (3) flat_drive_scan_native saves/sets `g_scan_seal_lbl=dcap_fail`/restores; (4) flat_drive_fence bare branch routes seal=g_scan_seal_lbl when ival==1 else lbl_ω; (5) IR_PAT_ABORT walk_bb_flat dispatch routes ω→seal; (6) scan_pat_m3_native_safe `(IR_PAT_FENCE && ival!=1)` → bare FENCE routes NATIVE in M3 (dropped from rt_scan guard), FENCE(P)-seal ival==0 STILL rt_scan in M3. RUNG B7b (nested-alternation-in-concatenation = embedded γ/ω alt DIAMOND mid-cat-spine, fixes 062/100/101/103 M4): root cause = `gather_lowered_cat_arms` walked the γ-spine emitting the diamond's first arm AND the ALT-join as two separate cat arms and NEVER visiting the 2nd arm (test 100 dropped LEN(2) entirely) → flat_drive_alt(join) saw no aux/kids → degenerate emit (forward γ-target collided with next backtrack β-label). FIX at GATHER level (NO IR mutation → M3/rt_scan untouched): collapse the diamond into ONE cat-arm slot (keep entry, drop join, record join in a parallel `joins[]` array advanced via `jn->γ`); new `emit_cat_diamond(entry,J,fwd,fail,arm_beta,id,armidx)` gathers branches via ω-chain bounded by `b->γ==J` and emits the alternation cascade (each branch fwd→continuation, fail→next-branch-or-cat-backtrack, arm_beta trampolines to branch0.β). `joins[]` threaded as a new param to gather_lowered_cat_arms + flat_drive_cat_arms; ALL callers pass NULL except flat_drive_match (cat_joins[64]) → fully backward-compatible. RUNG SUCCEED-CHAIN (fixes 059/060/067 M4): FENCE(P) of a SINGLE pattern lowers as `[LIT,FENCE-seal]→SUCCEED` with NO IR_PAT_CAT join → gather required a CAT-join terminator → returned 0 → flat_drive_match FATAL abort ("lowered chain shape not gatherable, entry kind=32 LIT next kind=47 FENCE"). FIX: gather also accepts a SUCCEED-terminated chain (`cat_out=NULL` → existing flat_drive_cat_arms `pBB==NULL` path), scoped to `joins!=NULL` so the capture path is byte-identical. REGRESSION CAUGHT+CORRECTED MID-RUNG: first version also stopped at/accepted IR_FAIL → broke 057_pat_fail_builtin (M3 18/1, M4 18/1, corpus M3 168) because IR_FAIL is a REAL always-fail ELEMENT that must be EMITTED (walked normally), not a chain terminator; narrowed BOTH the while-stop and the acceptance clause to IR_SUCCEED ONLY → 057 restored ("correctly failed"), pat-rung back to 19/19/19. GATES (my-container floors, non-decreasing HARD): smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP (m2==m3==m4) · fence TIER1=0 TIER2=0 HARD · corpus M2=182(=) M3=169(=) M4 159→166 (+7: 059/060/062/067/100/101/103), SKIP 11→8, ZERO regress any mode. PROBES (M3==M4==sbl): `'1AB+' ? ANY('AB') FENCE '+'`→Failure · `'ABC' ? FENCE 'B'`→Failure (anchored) · `'abc' ? LEN(1) FENCE LEN(2)`→match · `'xax' ? 'a' ABORT`→Failure. **NEXT (M4-still-failing, do-not-re-derive): (1) 064/065 = FENCE(P) with a `. N` CAPTURE — route `flat_drive_capture` (emit_bb.c, its OWN parallel gather+cascade ~line 423, deliberately passes joins=NULL this session) through the SAME diamond-collapse + SUCCEED-chain handling B7b/SUCCEED-CHAIN added to flat_drive_match; (2) ABORT-IN-ALTERNATION `(ANY('AB')|'1' ABORT)` — `gather_inline_alt_arms` (emit_bb.c ~412) follows ω, collects [ANY,'1',FAIL], DROPS the trailing ABORT in the CAT arm and wires '1'→success (separate, harder, structural); (3) 110-117 = FENCE(P)-as-var + `*cmd` star-var defer + POS(0)/RPOS(0) = B10 territory. THEN: drop FENCE(P)-seal (ival==0) from scan_pat_m3_native_safe so M3 FENCE(P) goes NATIVE off rt_scan (verify M3 no regress) → B9 ARBNO native → B10 defer/star-var (110-117) → S1 computed/literal subject → S5 replacement (`= R`, M4 still bombs) → S6 DELETE rt_scan → cut #3 alloc-registry+poison-bomb. FILES THIS SESSION: src/emitter/emit_bb.c, src/lower/lower_snobol4.c.**

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base 5bc5938 (1 commit this session, rebased onto concurrent 6dc3794 = behavior-neutral IR_SCAN_* prep; combined tree rebuilt clean, smoke 7/7/7 re-verified post-rebase). CUT #2 RUNG 2 LANDED — capture scans (`. V` cond-assign / `$ V` immediate-assign) now route NATIVE in mode-3 (off rt_scan), via the SAME flat_drive_scan_native→flat_drive_match→bb_match_capture→dcap-ring machinery mode-4 already proved. THE CHANGE (3 files): (a) NEW both-medium `emit_aligned_call_rt(sym,addr)` in emit_core.c (sanctioned binary-emit site; mirrors emit_jmp_label/emit_call_label which already branch on g_is_text — NOT a BB template, so no-MEDIUM_*-in-templates does not apply): TEXT branch BYTE-IDENTICAL to the prior `emit_text_n` dcap string (` push rbx; mov rbx,rsp; and rsp,-16; call SYM@PLT; mov rsp,rbx; pop rbx` — same FILE* sink, emitter_init_text sets bb_emit_out AND g_emit_sink identically), BINARY branch = `53 / 48 89 E3 / 48 83 E4 F0 / 48 B8 <abs8> / FF D0 / 48 89 DC / 5B` (push rbx; mov rbx,rsp; and rsp,-16; movabs rax,addr; call rax; mov rsp,rbx; pop rbx = x86_call_ro rax convention wrapped in the 16-byte align). (b) flat_drive_scan_native's three `if(g_is_text){emit_text_n(...)}` blocks (rt_dcap_begin/end_ok/end_fail, ~emit_bb.c:2238-2255) REPLACED by emit_aligned_call_rt → fire in BOTH mediums; mode-3 now sets g_rt_dcap_active so captures BUFFER (rt_dcap_record) then commit-on-success (rt_dcap_end_ok→rt_dcap_flush→NV_SET) instead of committing on doomed paths. (c) scan_pat_m3_native_safe: DROPPED IR_PAT_ASSIGN_IMM/IR_PAT_ASSIGN_COND (capture native), ADDED operand-variant exclusion `(IR_PAT_POS|IR_PAT_LEN|IR_PAT_TAB|IR_PAT_RTAB) && IR_LIT(nd).dval!=0.0` (variable arg → defers to rt_scan, PB-RB-5). PROOF M3 OFF rt_scan (instrumented rt_scan entry, then REVERTED before commit): capture probe `S ('x'|'b') . V`→0 RT_SCAN_HIT (native); FENCE control→1 RT_SCAN_HIT (still rt_scan=B7, also proves the probe fires). REGRESSION FOUND+FIXED MID-RUNG: dropping the capture exclusion exposed a LATENT rung-1 gap (rung-1 excluded only by KIND, never operand-variance) — 061 (`POS(N) 'a' . V`, N a loop var) mis-routed native, baked POS as a compile-time constant, never failed at N=3 → infinite :F(DONE) loop, M3 169→168; the dval!=0 guard (lower_snobol4.c sets dval=1.0/2.0 for variable/keyword position/length args, 0.0 for literal int) restores 169; isolated via two-binary corpus diff (exactly 061). GATES (my-container floors, non-decreasing HARD): smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP (m2==m3==m4) · fence TIER1=0 TIER2=0 HARD · corpus M2=182(=) M3=169(=) M4=159(=) SKIP=11 zero regress · beauty 6/17. PROBES m2==m3==m4==sbl: `('x'|'b').V`→b · `SPAN('a')$V`→aaa · `BREAK(',').A LEN(1).B`→RED/comma. **NEXT = B7 (FENCE native-binary), DIAGNOSED this session (oracle-grounded, do-not-re-derive) — TWO distinct problems, NOT just "drop IR_PAT_FENCE from the guard": (1) bare keyword FENCE/ARB/REM/FAIL/SUCCEED/ABORT parse as TT_VAR (snobol4.y bare T_IDENT→TT_VAR), NOT the primitive → variable refs (unset≈null) → WRONG in M2 AND M3, bomb in M4. Oracle-confirmed: `'1AB+' ? ANY('AB') FENCE '+'`→sbl Failure but M2=M3=match (FENCE-as-null-var collapses pattern to `ANY('AB') '+'`→backtracks→wrongly succeeds); `'ABC' ? FENCE 'B'`→sbl Failure, M2=M3=match; `LEN(1) FENCE LEN(2)`→all ok (no backtrack-through ⇒ accidentally right). Corpus fence tests that PASS use the FENCE(P) FUNCTION form (TT_FENCE via tal_fnc_close = different path) — so the prior watermark's "M2/M3 correct on 100/101/103" is TRUE for the function form AND bare-keyword FENCE is broken; both coexist. Fix per SNOBOL4-5STAGE-OWNED-BUILD.md B7 line ~242: recognize predefined-pattern NAMES on TT_VAR, OR runtime-init REM/ARB/FENCE/… gvars to built DT_P at startup (cleaner; covers assign+scan+bare-ref). (2) keyword-FENCE backtrack-seal: backtracking THROUGH FENCE must fail the ENTIRE match with NO start-advance (manual Ch.9 p.125 + Ch.18); PLUS the standing M4-FENCE-P case B TEXT β-wiring bug in flat_drive_fence (emit_bb.c:366) + cat-arm β back-wire (~emit_bb.c:279). Only after FENCE is semantically correct natively does B7 drop IR_PAT_FENCE from the guard. Ladder after: B9 ARBNO → B10 defer/star-var → S1 computed subject → S5 replacement (M4 still bombs `= R`) → S6 DELETE rt_scan → cut #3 registry+poison-bomb; rt_scan stays load-bearing for FENCE/ARBNO/defer/computed-subj/replacement in M3 until S6. FILES THIS RUNG: emit.h, emit_core.c, emit_bb.c. Full detail: HANDOFF-2026-06-14-OPUS48-SNOBOL4-BB-NO-IR-AT-RUNTIME-CUT-2-RUNG-2-CAPTURE.md.**

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base 1b71e43 + 1 commit this session. CUT #2 RUNG 1 LANDED — simple-primitive captureless scans now route NATIVE in mode-3 (no rt_scan, no IR-read for those shapes). THE CHANGE (emit_bb.c, 15 lines): (a) new guard `scan_pat_m3_native_safe(pg)` returns 0 if the pattern graph contains any of IR_PAT_ASSIGN_IMM / IR_PAT_ASSIGN_COND (capture), IR_PAT_ARBNO, IR_PAT_FENCE, IR_PAT_DEFER, IR_REF_INVARIANT, IR_PATTERN_DEFER; (b) the scan routing gate at `flat_drive_scan_stmt` changed from `MEDIUM_TEXT` to `(g_is_text || scan_pat_m3_native_safe(pg))` — so mode-4 TEXT behaviour is BYTE-IDENTICAL (the `g_is_text ||` short-circuits to the old condition) and mode-3 BINARY now ALSO takes `flat_drive_scan_native` for the safe shapes. PROOF BY CONSTRUCTION (this is the key correctness argument): when the guard passes and `flat_drive_scan_native` returns 1, `flat_drive_scan_stmt` RETURNS before reaching EMIT_PAIR_FILL→`bb_scan_stmt`→`rt_scan` — so for routed shapes the rt_scan IR-read is NOT emitted; mode-3 runs the same template-driven native MATCH chain mode-4 does. WHY THE GUARD IS NARROW (regression-driven, do-not-widen-without-the-builder): the FIRST cut used a coarse "captureless" guard → corpus M3 regressed 168→156 (−14), ALL in FENCE + ARBNO families (059/060/062/067/100/101/103/113/116/142/151 fence, W04_arbno_*, 116/142/151 arbno-fence) — those shapes are captureless but the native-BINARY MATCH path does not yet implement fence-seal / arbno-reentry correctly (they only ever passed m3 via rt_scan's IR interpreter; = B7 FENCE / B9 ARBNO ladder work, still owed in binary). Excluding FENCE/ARBNO/DEFER/REF_INVARIANT cleared all 14 → ZERO regressions. The native-binary MATCH wiring (IR_PAT_MATCH_HEAD/RETRY, flat_drive_cat_arms, element matchers) was NEVER exercised in mode-3 before this rung (mode-3 always used rt_scan) — it is now proven for the simple-primitive subset (SPAN/ANY/BREAK/NOTANY/LEN/TAB/RTAB/POS/RPOS/literal/ALT-of-those) but NOT for fence/arbno/defer. GATES ALL GREEN: smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP (m2==m3==m4) · fence TIER1=0 TIER2=0 HARD · broad corpus M2=182(=) M3=168(=) M4=158(=) SKIP=24, ZERO regress any mode (M3 count UNCHANGED — the regressed shapes were re-routed back to rt_scan, the newly-native simple shapes already passed via rt_scan, so net 0; the WIN is qualitative: those simple shapes no longer touch IR at runtime). PROBES m2==m3==sbl (captureless, builder-route, var subject): `S SPAN('ab') :S()F()`→hit · `S ('x'|'b') :S()F()`→hit · `S 'b' :S()F()`→hit. CAVEAT (could not measure): the aggregate "corpus under enforced total IR deletion" figure (would prove rt_scan-elimination breadth) still needs cut #3's alloc-registry — recursive IR_free segfaults (emission mutates IR_SCAN operand arrays). For the routed shapes the by-construction argument above is sufficient; the registry gives the global gate. NEXT RUNGS (the rt_scan elimination ladder, in order): **rung 2** = dcap BOTH-MEDIUM → then capture (`. V`/`$ V`) scans route native in m3 too. The ONLY blocker is the 3 dcap calls (rt_dcap_begin/end_ok/end_fail, all `void(void)`) in `flat_drive_scan_native` (~emit_bb.c:2211/2217/2223) are emitted TEXT-ONLY via `emit_text_n` inside `if(g_is_text)` blocks; emit_bb.c is C (can't call C++ `x86()`), and emit_core.c only has `emit_call_label` (rel32 to a LOCAL label) — there is NO C-callable both-medium ABSOLUTE-rt-call emitter (the abs64 call encoding lives only in C++ x86_asm.h, template-only). So rung 2 = add `emit_aligned_call_rt(sym,addr)` to emit_core.c (sanctioned binary-emit site) mirroring `emit_jmp_label`, emitting `push rbx;mov rbx,rsp;and rsp,-16;call <abs>;mov rsp,rbx;pop rbx` both-medium (need the abs64-call byte encoding — movabs r11,addr;call r11 = 49 BB <8 bytes> 41 FF D3, or check x86_asm.h's call-abs path), then replace the 3 `if(g_is_text){emit_text_n}` blocks with it and drop IR_PAT_ASSIGN_IMM/COND from the guard. Then **B7** = FENCE native-binary (drop IR_PAT_FENCE from guard), **B9** = ARBNO native-binary (drop IR_PAT_ARBNO; carries the B6 4th-β-slot note + per-instance scratch landmine), **B10** = star-var/defer (drop IR_PAT_DEFER/REF_INVARIANT/PATTERN_DEFER). Then **S1** = literal/computed subject (the mode-4 bomb "non-literal subject" + mode-3 rt_scan-with-literal-subj), **S5** = replacement (`= R`, both currently rt_scan in m3 / bomb in m4). When every shape routes native, **S6** = DELETE rt_scan + rt_scan binary arm of bb_scan_stmt; THEN cut #3 = land alloc-registry + poison-on-post-deletion-IR-touch, flip the gate to m2==m3==m4-under-total-deletion. Only after S6+registry is M3 a true twin of M4. FILES TOUCHED THIS RUNG: emit_bb.c only.**

---

**SESSION WATERMARK — 2026-06-14 · Opus 4.8 · SCRIP base 1b71e43 (orientation + VERIFICATION). VERIFIED THE NO-IR-AT-RUNTIME CLAIM AGAINST THE CODE (Lon asked: "is mode-3 a carbon copy of mode-4 with no IR? are they twins?"). ANSWER: M4 yes, M3 NO — not yet twins. The watermark's "cut #1" is exactly cut #1 of 3 and is the ONLY piece converted. EVIDENCE (all re-proven this session, do-not-re-derive): (1) `rt_scan` (DEFINED in `src/interp/IR_interp.c:202`, the interpreter file) takes `void *pat_graph`, casts to `IR_graph_t*`, and calls `IR_interp_once(pat)` INSIDE the match loop (line ~217) = full IR walk at runtime. (2) `bb_scan_stmt.cpp` has THREE arms: literal arm → `rt_scan_lit` (sealed strings, no IR ptr, clean BOTH mediums = cut #1's win); `MEDIUM_TEXT` arm = ALL `x86_bomb` for non-literals (mode-4 REFUSES to read IR); BINARY fall-through bakes a live `IR_graph_t*` via `x86_load_ro("rdi",…,_.op_scan_pat)` then `call rt_scan` (mode-3 READS IR). (3) The native escape `flat_drive_scan_native` (emit_bb.c:2194) is gated `MEDIUM_TEXT` at the routing site (emit_bb.c:2246) so MODE-3 NEVER TAKES IT. (4) `ir_delete_all` ALREADY fires before `fn()` on both run paths (scrip.c ~2895, ~2828) but `IR_free` (scrip_ir.c:369) frees ONLY the main graph node array, NOT the SCAN sub-graphs hanging off `IR_EXEC(nd).counter`/operands — so mode-3's baked `pat_graph` is ORPHANED-ALIVE, not freed; that leak is WHY the dangling read "works" (it reads leaked memory, not freed). Recursive-free segfaults (emission mutates IR_SCAN operand arrays) → the alloc-registry (cut #3) is the immune tool, NOT landed. EMPIRICAL DIFFERENTIAL (ran identical program `S='abc'; S ('x'|'b') = 'Q'; OUTPUT=S`): M2=aQc, M3=aQc, M4 emits `call rt_bomb;ud2` "non-literal pattern needs native PB-RB graph (pending)". SAME INPUT, M3 succeeds ONLY by reading IR, M4 bombs. NUMERIC PROOF the gap IS the cheat: corpus M3=168 > M4=158, the 10-test surplus being scan shapes rt_scan handles that M4 can't yet build natively; under enforced total IR deletion M3 collapses 168→91 (watermark's own figure) while M4 stays 158 unmoved. CONVERGENCE DIRECTION (counterintuitive — record so no session "fixes" it backwards): M3 and M4 do NOT converge by M4 catching up; they converge by M3 LOSING its rt_scan crutch (drop to native-only capability), THEN they are true twins differing only in x86()'s internal BINARY-vs-TEXT switch.

SCAN-SHAPE CONVERSION SURFACE (the cut-#2 map — which scan shapes route where, by medium):
| Pattern shape | Subject | Repl | M2 | M3 today | M4 today | cut-#2 target |
|---|---|---|---|---|---|---|
| single literal / cat-of-literals | any | any | IR-interp | `rt_scan_lit` (clean) | `rt_scan_lit` (clean) | DONE (cut #1) |
| non-literal (SPAN/ANY/LEN/ALT/…) | named var | none | IR-interp | **`rt_scan` (reads IR)** | `flat_drive_scan_native` (clean) | route M3 native too |
| non-literal | literal/computed | none | IR-interp | **`rt_scan` (reads IR)** | **bomb** ("non-literal subject") | native subj eval (S1) |
| non-literal | any | yes (`= R`) | IR-interp | **`rt_scan` (reads IR)** | **bomb** ("non-literal replacement") | native REPL+SUBST (S5) |
The ONLY TEXT-only dependency in the entire native route is the 3 dcap `if(g_is_text){emit_text_n(raw asm)}` blocks in `flat_drive_scan_native` (rt_dcap_begin/end_ok/end_fail, all `void(void)`); everything else is template-driven FILL (IR_PAT_MATCH_HEAD/RETRY, flat_drive_cat_arms, element matchers) and already both-medium. So CUT-#2 RUNG 1 = (a) those 3 dcap calls → both-medium via the `push rbx;mov rbx,rsp;and rsp,-16;call SYM;mov rsp,rbx;pop rbx` idiom (all encoders exist; cf. bb_dtp_assign.cpp:16-21); (b) drop the `MEDIUM_TEXT` condition at emit_bb.c:2246 so mode-3 BINARY routes native for the named-var-subject / non-literal / no-repl shape. GATE: smoke 7/7/7 · pat-rung 19/19/19 (m2==m3==m4) · corpus M3≥168 M4≥158 non-decreasing · fence HARD · probe `S='abc'; S ('x'|'b') . V; OUTPUT=V` → `b` in M3 (now native) AND M4. THEN widen to literal/computed subject (S1) and replacement (S5), then DELETE rt_scan = S6. BASELINES THIS SESSION (gate floors): smoke 7/7/7 · pat-rung 19/19/19 · corpus M2=182 M3=168 M4=158 SKIP=24.**

---

**SESSION WATERMARK — 2026-06-13 · Opus 4.8 · SCRIP base db2ad0e (2 commits this session, rebased onto concurrent d884849). RUNG (a) BINOP/UNOP-AS-FLAT-CHAIN-OPERAND fully CLOSED — both halves. Commit A (c0253bc) gvar UNOP-as-operand: NEW IR kind `IR_UNOP_GVAR_SLOT` + NEW template `bb_unop_gvar_slot.cpp` (bare-int NEG/POS from LIT_I / gvar via rt_gvar_get_int / slot operand → `mov FRQ(op_off),rax`, mirrors IR_BINOP_GVAR_ARITH_SLOT's gvar bare-int convention). ROOT CAUSE was: in the gvar flat chain a UNOP(NEG/POS) never produced a slotted value (`bb_unop` is gated on g_descr_flat_chain), so a binop with a UNOP operand (`-1+3` = ADD(UNOP(NEG,1),3)) failed `bb_slot_get` on its operand → the standalone arith-slot binop arm (emit_bb.c ~2764) did not fire → ASSIGN bombed `op_a_slot==-1`; and bare `OUTPUT=-X` hit walk_bb_node's IR_ASSIGN default (kind=5 unhandled, dropped) because op_a was IR_UNOP, absent from the accepted-child list. FIX SITES (A): IR.h + scrip_ir.c (kind adjacent to IR_BINOP_GVAR_ARITH_SLOT); bb_unop_gvar_slot.cpp (new); bb_templates.h decl; emit_core.c dispatch + IR_UNOP added to gvar ASSIGN accepted-child list (line ~450); emit_bb.c standalone gvar UNOP arm routes NEG/POS(LIT_I|VAR|slot)→IR_UNOP_GVAR_SLOT (the IR_UNOP case ~2922), and flat_drive_gvar_assign_binop now SKIPS re-walking c0 when `bb_slot_get(c0)>=0` (already walked standalone) — emits ASSIGN reading the slot; bb_gvar_assign.cpp NEW IR_UNOP rhs arm reads bare-int slot → rt_gvar_assign_int (mirror of the IR_BINOP arm). Commit B (db2ad0e) gvar POW-as-operand: extends the FUSED standalone-POW arm (emit_bb.c ~2727 — the POW whose γ-successor is the gvar ASSIGN, which emits IR_BINOP_GVAR_ARITH against the ASSIGN node = fused eval+assign-to-gvar) and the `bb_binop_gvar_arith` POW template. THREE B-fixes, all via existing POWER_fn + rt_gvar_assign_descr (NO new runtime calls): (1) const-fold UNOP(NEG/POS,LIT_I) operands → signed int, fixes `(-2)**3`=-8 (was kind=7: POW with a UNOP base missed the int-int arm, fell to flat_drive_binop_tree → binop_slot_kind(POW)=IR_BINOP → walk_bb_node default); (2) LIT_F operands → DT_R descr (tag + IEEE bits) passed to POWER_fn, fixes `2.0**8`=256., `2**0.5`=1.41…, `3.4**2`=11.56 (was 0./kind=7) — REAL BITS CARRIED IN bb_li/bb_ri (int64; survive walk_bb_node's clobber of op_dval/op_a_dval — DO NOT use op_dval as a carrier across FILL/EMIT_PAIR_FILL); (3) set op_sval on the fused arm so bb_fill_alpha derives op_parts_lbl[2] → fixes the M4 `lea rdi,[rip + ??]` junk in the POW path (2**8 M4 assembly was broken pre-existing). SPITBOL POW typing preserved (966bb35 POWER_fn convention): int**nonneg-int→int, any-real-operand→real, int**neg-int→real. GATES (post-rebase, verified before push): SNO smoke 7/7/7 HARD · pat-rung 19/19/19 no-SKIP · fence TIER1=0 TIER2=0 HARD · ICON smoke 12/12/12 (shared bb_binop_gvar_arith touch safe — Icon uses descr chain not this gvar POW arm) · broad corpus M2=182(=) M3=168(=) M4=158(+1: 027_arith_exponent flipped via the M4 label fix), SKIP 25→24, zero regress any mode. Both touched/new templates language-blind (TT_MNS/TT_PLS are shared AST op-token constants, like binop_slot_kind's BINOP_*), both-medium via x86(), no raw bytes, no MEDIUM_*. PROBES (M2==M3==M4==sbl): nested-arith -1+3=2 · 3+-1=2 · -2*-3=6 · 10--5=15 · -1+-2+-3=-6 · -X+10=3 · -X=-7 · X+Y*2=15; POW 2**8=256 · (-2)**3=-8 · 2.0**8=256. · 2**0.5=1.4142135623731 · 9**0.5=3. · 3.4**2=11.56 · 2**-1=0.5 · 10**9=1000000000 · (-3)**2=9. NEXT (do-not-re-derive): (b) `''+''`/`''+1`/`1+''` ADD-WITH-STRING coercion — flat_drive_binop_tree→binop_slot_kind emits empty for a string-operand ADD, ASSIGN then `call rt_bomb;ud2` (no value slot), line dropped in M3 (literals.sno 13-15). SPITBOL (Ch.3 p.21): null string→0 in arithmetic; null-string CONCAT returns other operand UNCHANGED (no coerce). Needs a runtime-coerce slot path (slot templates only handle int/var). (c) 084 concat-with-CALL (`S=S bump(2*J)`): gvar_seq_flatten can't flatten a call part → 'no parts' bomb; eval the call into a ζ-slot then concat. (d) 091/092 array `A<i>` assign+access, 093 table — M2 ALSO fails (all-mode; only ARRAY()/TABLE() create emitted, subscripts dropped) — LOWER-PRIORITY under Lon's ignore-M2 steer. (e) 094/095 DATA field-set (095 field write not persisting 10|20|10 vs 99; 094 m4 compile-abort) — note concurrent 2831781 const-folds const^const→IR_LIT_F in lower_icon.c (Icon only, not SNOBOL4). (f) bare `OUTPUT=&LCASE` cset-as-gvar-value aborts M3 at cset-print (pre-existing; no corpus test). (g) kw_read lacks distinct 'alphabet'/'stno' rows — NV fallback; add exact-value rows if a test needs precise semantics.**

---

## 🧹 DEAD-CODE REACHABILITY SWEEP — RUNG + STEPS (Lon 2026-06-14)

**RUNG — methodically remove ALL dead code by DETERMINISTIC reachability from the only things that run: mode-3 (the in-memory RX slab), mode-4 (the emitted `*.s`), and the supported CLI switches. Use asm/link tooling (ld `--gc-sections`, `nm`, `objdump`) as the ORACLE — no heuristics, no guessing. Every dead function/file goes to `src/attic/`, MIRRORING the source tree: `src/<a>/<b>/file.c` -> `src/attic/<a>/<b>/file.c` (whole-file-dead = `git mv`; partial-file-dead = excise the dead snippets into the mirror file with a provenance header, delete from live). The IR-interpreter kill (top watermark) is ITERATION 0 of this sweep.**

STEPS (run to fixpoint; the full gate suite is the safety net that proves each removal behavior-neutral):

1. **ROOTS — the emitted runtime surface.** Compile the FULL corpus (every language; exercise every CLI switch — `--run`, `--compile`, `--target=`, `--dump-*`, `--trace`, `--bench`, `--monitor`, `--case-sensitive`) to `.s`. Scrape every emitted symbol reference (`call <sym>@PLT`, `lea`/`mov` of a named symbol) -> `ROOTS_EMIT` = the runtime functions emitted code ACTUALLY calls. mode-3's RX slab is emitted by the SAME templates (absolute `movabs rax,&sym; call rax`), so its call-set == `ROOTS_EMIT` by template symmetry — confirm by scraping the emitter call-table (`x86("call","sym",(uint64_t)(uintptr_t)sym)`).
2. **scrip binary — the linker GC oracle.** Rebuild scrip with `-ffunction-sections -fdata-sections` and link `-Wl,--gc-sections -Wl,--print-gc-sections 2>gc.log`. The linker PROVES (deterministically, from `main` + kept refs) every `.text.<fn>`/`.data.<obj>` it removes -> `DEAD_LINKER`. Address-taken dispatch targets are RETAINED by the linker (it sees the reloc), so `--gc-sections` is SAFE — it never drops a live indirect-call target.
3. **libscrip_rt.so — static call-graph reachability.** `DEFINED = nm -D --defined-only`. `ROOTS = ROOTS_EMIT ∪ exported-API ∪ driver hook/dlsym names`. Build the caller->callee edge set via `objdump -d`; BFS(`ROOTS`) -> `REACHED`; `DEAD_RT = DEFINED − REACHED`. For each `DEAD_RT` symbol, confirm zero `call` edges from any `REACHED` fn AND zero address-taken reloc from a reachable dispatch table.
4. **Triage.** Partition `DEAD_LINKER ∪ DEAD_RT` into whole-file-dead (no surviving symbol in the file) vs partial-file-dead (file has both live and dead symbols).
5. **Move to attic (mirror the tree).** Whole-file-dead -> `git mv src/<path> src/attic/<path>`, drop from the Makefile. Partial-file-dead -> excise the dead functions, append them to `src/attic/<path>/<file>` (create the mirror file if absent) with a provenance header (origin path + commit + the reachability reason), delete from the live file. Bomb-tombstone (keep the symbol, loud-fail body) ONLY when a still-emitted call-site references a symbol whose body must die — otherwise full-remove.
6. **GATE (the net).** After EACH removal batch, the FULL suite (smoke / pat-rung HARD / fence / corpus, ALL languages) must be NON-DECREASING. Removal is reachability-PROVEN behavior-neutral, so ANY gate drop == a root was missed (an indirectly-reached fn) -> restore it, add its entry point to `ROOTS`, re-run. This converts "is it dead?" from judgment into a linker+gate PROOF.
7. **Fixpoint.** Removing dead code makes MORE code dead (callees of removed fns). Re-run 2-6 until `gc.log` is empty AND `DEAD_RT = ∅`.
8. **COMPLETION TEST.** `--gc-sections --print-gc-sections` removes nothing from scrip · `DEAD_RT = ∅` for libscrip_rt · the `src/attic/` mirror is excluded from the build · every gate green. Known indirect-call roots to PRE-MARK (else step 6 catches them the hard way): the `emit_core.c` IR_e dispatch table, the `bb_box_fn` / template registries, and any `rt_*` reached only via a function-pointer table.

## 🔴🔴🔴 TOP PRIORITY — GROUND-ZERO PATTERN BUILDING THROUGH RT FUNCTIONS · NO BB_INVARIANT

Every pattern BUILT at runtime via `rt_pattern_build`/`rt_pattern_stitch_cat`/`rt_pattern_stitch_alt`. All 8 `bb_pattern_*.cpp` builders done — `ins*`=0 ✅. All D7 rungs DONE. Full ladder: `SCRIP/SNOBOL4-5STAGE-OWNED-BUILD.md`. GATE: smoke 7/7/7 · pat-rung m2==m3==m4 no-SKIP · corpus non-decreasing · fence HARD.

---

## ⛔ SESSION DIRECTIVE — modes 2/3/4 CO-EQUAL HARD GATES. See `SCRIP/SNOBOL4-5STAGE-OWNED-BUILD.md` D7.

## ⭐ PIVOT (Lon 2026-06-07) — BUILDERS REPLACE SM · DT_P SEAL-ON-MATCH

(1) BB_INVARIANT/REF_INVARIANT — quit using. (2) No constant-folding. (3) BBs replace ALL SM. (4) ✅ bb_pat_*→bb_match_*. BUILDER TAXONOMY: `bb_pattern_nullary`·`_unary_i`·`_unary_s`·`_unary_p`·`_stitch`·`_capture`·`_defer`. DT_P: build-unsealed, seal-on-match.

## 🔴🔴 #0 PRIORITY — OWNED 5-STAGE BUILD

Master ladder: `SCRIP/SNOBOL4-5STAGE-OWNED-BUILD.md`. Arch: `.github/ARCH-SNOBOL4.md`. RoE: BBs/XAs through templates only; one template may serve multiple IRs; no runtime calls without ledger stamp; probe-first vs sbl; one rung = one commit; m2/m3/m4 co-equal hard gates; corpus non-decreasing HARD.

## MODE 4 BUG LADDER

Inventory: smoke m4 **7/7** · pat-rung m4 **19/19 no-SKIP** · broad m4 corpus **166/280** · beauty m4 **1/17**.

- [x] **M4-SMOKE-REGRESS ✅ (verified resolved 2026-06-13, eb98b8e)** — both halves gone: m3-concat = M3-CONCAT-MULTIPART (fixed this session); m4-define duplicate-label `.Lx2_0` no longer present in `bb_unify.cpp` (grep 0). Smoke 7/7/7; `DEFINE('DOUBLE(N)') … OUTPUT=DOUBLE(21)` with `< /dev/null` = `42` in M3/M4/sbl.
- [ ] **M4-CRASH** — `scrip --compile` must never abort/segfault: 29 corpus SKIPs + 4 beauty emit-crashes.
- [ ] **M4-FENCE native — REMAINING after B7a/B7b/SUCCEED-CHAIN landed (b2acdb4-successor; bare-keyword FENCE+ABORT seal, embedded-alt-diamond-in-cat-spine, and SUCCEED-terminated FENCE(P)-single chains all NATIVE in M3+M4; 062/100/101/103/059/060/067 now pass M4; corpus M4 159->166).** Remaining gaps (064/065 diagnosis CORRECTED 2026-06-14, see top watermark — it was NOT a FENCE/capture cat-arm issue and NOT M2/M3-correct; it was the cset operand-variance bug, single-var half now fixed in M2/M3): (1) **065** (`FENCE(SPAN(digits)|'') . N`, single-var cset) M2/M3 now CORRECT after the PB-RB-5 single-var rung; M4 still link-fails `xcat15_right_β` (native cset late-read + ALT cat-arm β owed). (2) **064** (`ANY(&UCASE &LCASE) FENCE(SPAN(alnum)|'') . ID`) fails ALL modes via the COMPUTED-CONCAT cset arg (needs match-time expression eval, value-chain-into-ζ) — distinct from 065. (3) **ABORT-in-alternation** `(ANY('AB')|'1' ABORT)` — `gather_inline_alt_arms` (emit_bb.c ~412) follows ω and collects [ANY,'1',FAIL], DROPPING the trailing ABORT in the CAT arm and wiring '1'->success (structural, harder). The real M4 close-out for 064/065 = native ζ late-read + flat_drive_capture diamond-collapse/β back-wire (emit_bb.c ~423). Gate m2==m3==m4 on 064/065 + the ABORT-alt probe, plus 058/059/060/061/062/067/100/101/103 hold.
- [x] **M4-ONESHOT (SPAN twins) ✅ 3fd9798** — `S='aab'; S SPAN('ab').A 'b'.B` → `:` in m3==m4==sbl (was m3/m4 `aa:b`). β→ω fix on SPAN literal+var templates; ANY/NOTANY/BREAK/LEN/TAB/RTAB/POS/RPOS β arms AUDITED = already correct. NB the original probe used a LITERAL subject (`'aab' ?`) which is the S1 gap (literal-subject bomb) — re-probe one-shot β with a VARIABLE subject (routed native path).
- [ ] **M4-CAPTURE-COND** — probe: `V='unset'; S='ab'; S 'a' . V 'x' :F(NO)` → m4 `nomatch a`, sbl `nomatch unset`.
- [ ] **M4-STARVAR** — 070/071/073/075/061+053 class. PB-RB-6: BB_PAT_BUILD for `*E`/`$NAME`/pattern-valued var.
- [ ] **M4-ARBNO-REENTRY** — matched ARBNO instance's remaining alternatives not re-enterable on backtrack.
- [ ] **M4-DATA** — 091/092/093/094/095/096.
- [ ] **M4-DEFINE** — 084/088/089.
- [ ] **M4-BUILTIN** — 076/077/081/082/097/006/030/020/014/015.
- [ ] **M4-BEAUTY** — 16 fails: link=5, diff=7, emit=4.
- [ ] **M4-REPL-NATIVE** — replacement natively (PB-RB-7). Probe: `S 'b' = 'X'` → `aXc`.

## ⛔ FACT RULE — LANGUAGE-BLIND BB/XA TEMPLATES (Lon, 2026-06-03)

No language-specific logic in any BB or XA template. Templates dispatch on IR shape/flags only. FORBIDDEN in `src/emitter/BB_templates/` and `XA_templates/`: `IR_LANG_*`, `LANG_*`, `is_<lang>`, language-named arms, hardcoded language-builtin names. Language-varying behavior → runtime (by-name dispatch) or LOWER (distinct IR shape). Inventory: `SCRIP/BB-TEMPLATES-LANG-AUDIT.md` (XA clean 2026-06-03). COMPLETION TEST: Tier-1 grep returns 0.

## ⛔ TEMPLATE SPEC v2 (Lon 2026-06-04) — REGENERATE, DON'T PATCH

No local variables · ONE return per PLATFORM · IF()/FOR() for conditionals/loops · ONE source line = ONE asm line · real Greek α β γ ω · no MEDIUM_TEXT/MEDIUM_BINARY at top level · zero emit_fmt() · zero C comments · zero blank lines · **ONE-IR-ONE-LOGIC:** N IR→1 BB allowed; 1 IR→1 BB norm; 1 IR→N BB never (split IR kinds in LOWER) · **EMIT-BLIND:** template reads only its own `_` context — never dereferences a neighbor node. Regenerate whole, never patch. Full directive: `HANDOFF-2026-06-04-OPUS48-SNOBOL4-BB-HYGIENE-SWEEP-SPEC-V2.md`.

## ⛔ `bb_bin_t` IS ABOLISHED (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md)

`bb_bin_t {sites,labels,is_def,bytes}` and `bb_emit_asm_result`/`bb_emit_asm_result_pairs` are DELETED. Every BB template returns ONE concatenation of `x86(...)` calls emitted by `bb_emit_x86(out)`. Patch sites are in-band tagged records (`L`/`J`/`D`/`L(n)`/`E`/`F`); `bb_emit_x86` discovers each byte position as it copies. FORBIDDEN: `struct bb_bin_t`, `bb_bin_t bin`, `bb_emit_asm_result(...)`, `bin.sites`/`bin.labels`/`bin.is_def`, `(int)b.size()` as patch offset, anywhere in `BB_templates/`/`XA_templates/`/`emit_str.*`. Not-yet-converted box → `x86_bomb(msg)` stub. COMPLETION TEST: (a) `emit_str.h` declares neither; (b) gate `test_gate_no_bb_bin_t.sh` reads 0; (c) every BB template emitted via `bb_emit_x86`; (d) build rc=0; (e) body byte-identical across four GOAL files.

## ⛔ ONE MEDIUM, INVISIBLE (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md)

A template NEVER writes an instruction twice (once as GAS, once as raw bytes) and NEVER branches on medium. Every instruction goes through ONE `x86(mnem,…)` call; encoder switches medium internally. FORBIDDEN in `BB_templates/*.cpp`: `x86_Lrec`, `x86_Jrec`, `x86_Drec`, `x86_b1(`, `x86_b2(`, `x86_b3(`, `bytes(`, `u8(`, `u32le`, `u64le`, any `IF(MEDIUM_BINARY,…)` or `IF(MEDIUM_MACRO_DEF,…)` carrying instruction bytes. Missing encoder → ADD to `x86_asm.h`. TEXT-only annotations (label/comment) with no binary counterpart are allowed. COMPLETION TEST: (a) zero raw-byte producers + zero `IF(MEDIUM_BINARY/MACRO_DEF)` in any `BB_templates/*.cpp`; (b) every instruction via `x86()`; (c) gate green under `--strict`; (d) body byte-identical across four GOAL files.

## ⛔ NO C BYRD-BOX FUNCTIONS (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md, GOAL-SNOCONE-IR-BB.md)

A byrd box is emitted machine code entered by JUMPING to α or β label — never a C function `DESCR_t NAME(void*,int entry)`. The `(ζ,int entry)` signature is FORBIDDEN. Brokered-BB calling convention is gone. `bb_box_fn` typedef KEEPS `(void*,int entry)` — survivors `rt.c:480/529/595` (C α-entry into DEFINE blobs); convert those to jmp-threading before touching the typedef. COMPLETION TEST: (a) grep for `(void*,int entry)` in defs == 0; (b) no `bb_broker`; (c) body byte-identical across five GOAL files.

## ⛔ NO AST AND NO IR DURING MODE-3/MODE-4 EXECUTION (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md, GOAL-SNOCONE-IR-BB.md)

**During the EXECUTION of a mode-3 (`--run`) or mode-4 (`--compile`) program, NOTHING reads or writes the AST (`tree_t`) or the IR (`IR_t`/`IR_graph_t`).** (Lon directive, 2026-06-13.) The compiler reads the IR exactly ONCE — before execution — to emit the mode-3 RX-slab image or the mode-4 `.S` source; thereafter the produced machine code runs with ZERO reference to either tree. A box's runtime values live INSIDE the box (RO `[rip+disp]`, RW `[ζ=r12+off]`); a runtime helper (`rt_*`) operates only on `Term*`/`DESCR_t` values, never on `IR_t*` or `tree_t*`. This subsumes the IR-NEVER-TOUCHED rule and extends it to the AST: an AST walker that does not EMIT IR is worthless — it may not exist on any run path, not even for mode 2. (The mode-2 `--interp` IR-graph interpreter `IR_interp_once` is the ONLY sanctioned IR walker, and it is reachable ONLY via `--interp`, never from a mode-3/4 produced binary.)

**THE ONE EXCEPTION — `EVAL()` and `CODE()`.** SNOBOL4's `EVAL` and `CODE` are dynamic-compilation builtins: by definition they compile a string into executable form AT RUNTIME (`CONVE_fn`→`EXPVAL_fn`, the `g_eval_str_hook`/`g_eval_pat_hook` rail). Reading/building an IR (or equivalent) at runtime is intrinsic to their meaning, so the prohibition does NOT apply INSIDE `EVAL()`/`CODE()` (and only there). No other construct, builtin, or runtime helper may read or write AST/IR during mode-3/4 execution.

**FORBIDDEN on the mode-3/4 run path:** any `rt_*` (or template-called) function that takes an `IR_t*`/`IR_graph_t*`/`tree_t*`, walks `->operands`/`->c[]`/`->t`/`->op`, reads `IR_LIT(...)`/`IR_EXEC(...)`, dispatches on `IR_e`/`tree_e`, or bakes a live `IR_t*`/`tree_t*` address into emitted code (the `emit_term_from_node_bin` pattern). A box NOT YET converted is a LOUD `x86_bomb(msg)`, never a silent IR/AST read.

**GUARD:** the run path's runtime objects are `Term*`/`DESCR_t` only. **COMPLETION TEST:** (a) no GZ template (`bb_cell_*`) and no mode-3/4-reachable `rt_*` reads AST/IR (grep of the run-path helpers for `IR_t*`/`tree_t*`/`IR_LIT`/`->op`/`->t` == 0, excepting `EVAL`/`CODE`'s `CONVE_fn`/`EXPVAL_fn` rail and the mode-2-only `IR_interp_once`); (b) no function bakes a live `IR_t*`/`tree_t*` into emitted bytes; (c) FACT RULE body byte-identical across all five GOAL-*-BB files.

## ⛔ NO VALUE STACK — EVER (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md, GOAL-SNOCONE-IR-BB.md)

No value stack in any form. Every box-held value lives in `[rip+disp]` (RO) or `[r12+off]` (RW frame). `g_vstack` is DELETED and must stay at zero. FORBIDDEN: global/static push/pop value arena, `rt_push_*`/`rt_pop_*`/`vstack_*`. KEEP (not value stacks): Prolog trail, choice-point ledger, C call stack for recursion, ARBNO per-activation frame array. Residual `vstack_*/rt_vstack_ops_t` scaffolding in `rt.c` is dead/aborting — do not wire. GATE: `test_gate_no_vstack.sh`. COMPLETION TEST: (a) `grep -rn 'g_vstack' src/` == 0; (b) no new push/pop arena; (c) body byte-identical across five GOAL files.

> **⭐⭐⭐ CORRECTED PATTERN ARCHITECTURE (Lon 2026-06-01).** A SNOBOL4 pattern = graph of EMITTED BYRD-BOXES (`bb_box_fn`). `DT_P` = HEAD BLOCK = `{entry, OUTSIDE-γ slot, OUTSIDE-ω slot}`. Build = SPLICE (wire ports). Runtime `STITCH_SEQ`/`STITCH_ALT` = runtime twins of `wire_seq`/`wire_alt`. `BB_LINK` = pure-tail `jmp [r12+slot]` for shared sealed heads.

## ⛔ SHARED-LOWERER ONE-FILE CONCURRENCY (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md)

`lower.c` = ONE file, ONE entry (`lower2`), ONE switch over `tree_e`. Prolog split to `lower_prolog.c` (d6d93c6). Rules: (1) ONE case per `TT_*`. (2) Language variation inside the case, branch on `cx.lang`. (3) Edit only your language's arm. (4) Missing arm → `lower_unhandled` (loud), never silent. (5) Shared scaffolding additive; signature changes lockstep across all three GOAL files. (6) `prove_lower2.sh` must stay green. COMPLETION TEST: (a) no duplicate `case TT_` per switch; (b) every case ends in real arm or `lower_unhandled`; (c) body byte-identical across three GOAL files; (d) `prove_lower2.sh` green.

## ⛔ TEMPLATE-ONLY EMISSION — ONE-DISPATCH CONCURRENCY (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md)

`emit_core.c` ONE switch over `IR_e` → per-box template fns in `{BB,SM,XA}_templates/`. Rules: (1) ONE dispatch case per `IR_*`. (2) ONE template file per box. (3) Edit only your language's boxes. (4) Missing box → loud default (assert/abort). (5) Makefile `RT_PIC_SRCS` append-only. (6) Gates: `util_template_purity_audit.sh`, `test_gate_em_template_byte_identity.sh`, `test_gate_em_template_matrix.sh` green. COMPLETION TEST: (a) no duplicate `case IR_` in `emit_core.c`; (b) zero forbidden byte-emitters outside templates; (c) body byte-identical across three GOAL files; (d) gates green.

## ⛔ NO DUPLICATED LOGIC (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md, GOAL-RAKU-BB.md)

Each piece of logic written ONCE. Box = PORT work (α/β/γ/ω wiring). Runtime = VALUE work (build term, compare, arithmetic, concat). FORM 1: same algorithm in two media — delete both, call `rt_*` once. FORM 2: emit-time logic that is a runtime job — belongs behind one `rt_*` call. FORM 3: operand box reimplemented inside consumer — consumer reads producer's slot, not the value inline. COMPLETION TEST: (a) no algorithm in both TEXT and BINARY arm; (b) no emit-time reimplementation of runtime value work; (c) no `pBB->α->ival/sval/dval` inside consumer box; (d) body byte-identical across four GOAL files.

## ⛔ X86-64 REGISTER / SUBJECT-MODEL CONVENTION (FACT — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md)

| Reg | Name | Role |
|-----|------|------|
| R13 | Σ | subject BASE ptr |
| R14 | δ | CURSOR (moving) |
| R15 | Δ | subject LENGTH/END (fixed) |
| R12 | ζ | BB-local RW FRAME base `[r12+off]` |
| R10 | — | per-BLOB DATA-block ptr |
| rbx | — | callee-saved scratch |
| rbp | — | DEFINE'd frame ptr when active |

**DUAL ROLE — R13/R14/R15 ALSO CARRY THE PROLOG TRAIL (RATIFIED Lon 2026-06-13).** Prolog has no subject string, so the subject trio Σ/δ/Δ is idle and instead carries the TRAIL — Prolog's one main attraction (its single shared binding-undo spine) — in the SAME base/cursor/end shape, casing preserved (UPPER = fixed, lower = moving):

| Reg | subject (SNOBOL4/Icon) | Prolog TRAIL — `Trail{stack;top;capacity}` |
|-----|------------------------|---------------------------------------------|
| **R13 = Σ** (UPPER, fixed) | subject BASE ptr | trail `stack` — base of the `Term*` array |
| **R14 = δ** (lower, moving) | CURSOR | trail `top` — the mark; "push" = ++, "unwind" = set back |
| **R15 = Δ** (UPPER, fixed) | subject LENGTH/END | trail `capacity` — the fixed bound |

The physical registers are SHARED — never live in two languages at once. A cross-language BB jump save/restores the trio (DEFERRED — its own later rung; not yet wired). The trail in registers replaces the `g_resolve_trail` symbol load with pure register traffic. **RBP stays RESERVED** (its brokered-frame role is dead under NO C BYRD-BOX; held for a future use TBD — Lon). This DUAL-ROLE addition is byte-identical across all three GOAL files; the subject rows above remain each file's own.

γ-success return: `rax=σ ptr`, `rdx=δ int`. Changing any assignment = lockstep update across all three GOAL files.

## ⛔ PER-BOX LOCAL STORAGE (FACT RULE — byte-identical in GOAL-SNOBOL4-BB.md, GOAL-ICON-BB.md, GOAL-PROLOG-BB.md)

Every box value: **(RO)** `[rip+disp]` sealed data, or **(RW)** `[ζ+off]` per-sequence frame. No AG ring, no value stack, no name-table round-trip for intermediates. COMPLETION TEST: (a) no `bb_exec_once`/AG-ring on mode-3/4 path; (b) no `g_vstack`/`rt_push_*`/`rt_pop_*`; (c) no `NV_GET`/`NV_SET` for intermediate values; (d) no `movabs …&pBB->slot`; (e) BINARY and TEXT arms do identical processing.

---

## ⭐⭐ REBUILT LADDER — PB-RB (CORRECTED PATTERN ARCHITECTURE)

`wire_seq`/`wire_alt` shared LOCKSTEP helpers. All PB-RB gates mode 4 HARD. Open:

- [ ] **PB-RB-5** — Operand-variant element matchers. `LEN(N)`/`SPAN(cvar)`: existing box reads operand late from ζ-slot. Prove + mode-3 `S LEN(2)`, `S SPAN('abc')`.
- [ ] **PB-RB-6** — BB_PAT_BUILD for structural variance. `*E`/`$NAME`/pattern-valued var.
- [ ] **PB-RB-7** — REPLACEMENT BB (ph.4) + SUBSTITUTION BB (ph.5). `S 'b' = 'X'` → `aXc`.
- [ ] **PB-RB-CONV** — IR_SCAN convergence: retire dual shape once native chain covers corpus breadth.
- [ ] **PB-RB-OPT** — All-invariant BLOB freeze after correctness rungs done.

## BROK residue (eradication ✅)

- [ ] ARBNO child-β re-entry gap: matched instance's remaining alternatives not re-enterable on backtrack. Own rung.
- `bb_box_fn` typedef survivors `rt.c:480/529/595` — convert to jmp-threading before touching typedef.

## Architecture references

- Mode-2 oracle: `src/interp/IR_interp.c`
- Flat driver: `src/emitter/emit_bb.c` (`codegen_gvar_flat_chain_body`, `walk_bb_flat`)
- Template dispatch: `src/emitter/emit_core.c`
- Template dir: `src/emitter/BB_templates/bb_pat_*.cpp`
- Lowering: `src/lower/lower.c` + `lower_prolog.c` + `lower_program.c`
- Bomb infra: `src/emitter/emit_str.{cpp,h}`
- M4 scan routing: literal pattern → `bb_scan_stmt` literal arm → `rt_scan_lit`; non-literal + named-var subject + NO repl → `flat_drive_scan_native`; else → `rt_scan` shim.

## ⏸ PARKED

- **M2-ARBNO-SHY** — m2 ARBNO greedy vs sbl shy; m4 already correct — mode-2-only bug.
- **SR-2** — save/restore→ζ-frame migration.
- **LOWER2 BOX LADDER** — parked.

## Session log

**Watermark (NO-IR-AT-RUNTIME cut #1; 4a8ba14; 2026-06-14 Opus 4.8).** smoke 7/7/7 HARD. pat-rung 19/19/19 no-SKIP. fence HARD. ICON 12/12/12. corpus M2=182(=) M3=168(=) M4=158(=) zero regress. THREE commits pushed (rebased onto concurrent a06554a bb_swap): (1) e089608/f9e6c02 DEFINE — abolished per-proc VIEW sub-graphs (`*fg=*g` saved in bbp → imprisoned sub-graph + the prior double-free); proc is now a SCALAR entry index `ProcEntry.sno_entry_idx` into the SINGLE main graph, emitted via `gvar_flat_chain_build_at`/`_text_at`, M2 via `bb_proc_entry`; DEFINE program now has exactly ONE graph. (2) 4a8ba14 SCAN literal arm IR-FREE both mediums — `bb_scan_stmt` M3 BINARY arm baked pattern/subj/repl IR SUB-GRAPH pointers into `rt_scan(void*pat_graph,...)` which WALKS IR AT RUNTIME (the line crossed); hoisted the literal arm out of the medium gate so literal patterns emit `rt_scan_lit` (sealed strings, zero IR ptr) in BOTH mediums; non-literal falls through (M4 bomb / M3 rt_scan still crosses). Lon's LINE: no IR-derived graph at mode-3 run / after mode-4 emission, EXCEPT CODE/EVAL. PROVEN via a graph-deletion REGISTRY (track every IR_alloc; ir_delete_all frees ALL incl. embedded sub-graphs): M4 already honors the line (158, unchanged under total deletion); M3 crosses via rt_scan (corpus 168→79 under deletion, lifted to 91 by cut #1). NEXT: cut #2 = non-literal native pattern build (extend `flat_drive_scan_native` to cover all primitives+repl, DELETE rt_scan — PB-RB/B-CONV/S6 in SNOBOL4-5STAGE-OWNED-BUILD.md; gate = pat-rung M3 under deletion 1→19); then cut #3 = land registry + poison-bomb on post-deletion IR touch. Recursive-IR_free-by-field-decode FAILED (emission mutates IR_SCAN operand arrays → segfault); registry (track-at-alloc) is the immune tool. Full detail: `HANDOFF-2026-06-14-OPUS48-SNOBOL4-BB-NO-IR-AT-RUNTIME-CUT-1.md`.

**Watermark (DEFINE smoke RESTORED 7/7/7; e089608; 2026-06-13 Opus 4.8).** smoke **7/7/7** HARD (was 7/6/6 entering — M3+M4 `define` FAILing). pat-rung 19/19/19 no-SKIP. fence HARD. ICON smoke 12/12/12. broad corpus M2=182(=) M3=168(=) M4=158(=) zero regress. 1 commit (rebased onto concurrent 738b950). The `define` smoke (`DEFINE('DOUBLE(X)') OUTPUT=DOUBLE(21)`→42) had regressed in M3/M4 after `ir_delete_all` (f0c3e29/9baaf64) started physically freeing the IR — concurrent Raku 738b950 flagged it "pre-existing baseline"; this fixes the ROOT CAUSE. TWO bugs: **(1) double-free** — `lower_snobol4.c:1009` builds each user-proc graph as a VIEW over main via `*fg=*g` (shallow copy → `fg->all` aliases `g->all`, same node array); both added to bbp; `bb_program_free→IR_free` then `free(bbg->all)` TWICE on the shared array → `free(): invalid pointer` (M3 runtime; M4 in the post-write `ir_delete_all`, `.s` already correct). FIX in `bb_program_free`: two-pass owner/view classification via per-node `->own` back-pointer (set at `IR_node_alloc`, sole allocator; a view's nodes have `->own != fg`) — pass-1 classify while nodes alive, pass-2 free (owner=full IR_free, view=struct-only). Two passes MANDATORY (lazy `all[0]->own` check = UAF after owner frees array). Language-neutral (non-SNO graphs all self-owned). **(2) M4 ABI** — after (1), M4 `define` ran but printed nothing (not 42): `sno_proc_startup` text emission hand-wrote a 4-arg ABI for `rt_proc_register` (`rdi=name; xor rsi; rdx=pnames; ecx=nparams`) but the fn takes 3 (rdi=name,rsi=pnames,rdx=nparams) → `pnames` read as `nparams`, `rsi=0` became pnames → registered with pnames=NULL → `rt_call_named_proc` binds no params → `X` unset in DOUBLE body. FIX: drop spurious 2nd arg (rdi/rsi/rdx), mirror the correct M3 C-call. Probes M2≡M3≡M4: `DOUBLE(21)`=42, `ADD3(10,20,12)`=42. NEXT: conditional-success-goto in proc bodies is a SEPARATE all-mode gap (M2 also fails; `:S(RETURN)` + recursion, sbl=120) — NOT regressed here; then rung (b) ADD-WITH-STRING coercion. Full detail: `HANDOFF-2026-06-13-OPUS48-SNOBOL4-BB-DEFINE-DOUBLEFREE-ABI.md`.

**Watermark (RUNG (a) CLOSED; db2ad0e; 2026-06-13 Opus 4.8).** smoke 7/7/7. pat-rung m2/m3/m4 19/19/19 no-SKIP. fence HARD. ICON smoke 12/12/12. 2 commits this session (rebased onto concurrent d884849): c0253bc gvar UNOP-as-operand (new IR_UNOP_GVAR_SLOT + bb_unop_gvar_slot template — fixes -1+3 / -2*-3 / -X+10 / bare -X, M2≡M3≡M4); db2ad0e gvar POW-as-operand (real operands via DT_R descr + const-fold signed-literal base + M4 op_parts_lbl[2] fix — fixes (-2)**3 / 2.0**8 / 2**0.5 / 3.4**2 / 2**-1, M2≡M3≡M4). Broad corpus M2=182(=) M3=168(=) M4 157→158 (+1: 027_arith_exponent via the M4 POW label fix). Next: rung (b) ADD-WITH-STRING coercion (''+1 / 1+'' — M3/M4-only, fits ignore-M2 steer).

**Watermark (D7 all rungs ✅; 900fee0; 2026-06-13 Sonnet 4.6).** smoke 7/7/7. pat-rung m2/m3/m4 19/19/19 no-SKIP. fence HARD. 3 commits this session: M3-CONCAT-MULTIPART (concat rdi ptr → IR_LIT(_.node).sval); M4-SMOKE-REGRESS verified resolved; FENCE(P) function form (lower as P+keyword-fence-seal — M2/M3 land, fence family M2 20→29 M3 21→30, M4-only emission bug remains as M4-FENCE-P case B). Broad corpus M2 172→181 M3 152→155. Next: M4-FENCE-P case B (TEXT-path fence-in-concat β-wiring).

## Session Setup

```bash
cd /home/claude/SCRIP
bash scripts/install_system_packages.sh
bash scripts/build_scrip.sh
make libscrip_rt
git clone https://TOKEN@github.com/snobol4ever/x64 /home/claude/x64
```

Gates — modes 2/3/4 CO-EQUAL HARD:
```bash
bash scripts/test_smoke_snobol4.sh                              # 7/7/7 HARD (define double-free + M4 ABI fixed e089608)
bash scripts/test_snobol4_pat_rung_suite.sh                     # 19/19 no-SKIP
SCRIP=$PWD/scrip bash scripts/test_mode4_only_corpus_snobol4.sh # 158/280 floor (broad-corpus container-sensitive; non-decreasing HARD)
bash scripts/test_gate_em_beauty_subsystems_mode4.sh            # 1/17 floor
bash scripts/test_gate_sno_pat_reg.sh                           # fence HARD
```

## Hard-won facts (do not re-derive)

- `flat_drive_cat_arms(pBB=NULL,arms,nc,...)`: pBB=NULL supported — emit tail trampolines explicitly.
- `operand_aux` is PER-GRAPH: walking a sub-graph (ARBNO inner, pattern graphs) requires switching `g_emit_cfg` first (save/restore, cf. emit_bb.c:1477).
- Flat emission of driver-owned kinds (PAT_CAT/PAT_ALT/PAT_FENCE/GCONJ) starts at JOIN node; always `ir_skip_alt_arms` before single-node walk.
- SPITBOL primitives ONE-SHOT: SPAN/BREAK/ANY/NOTANY/LEN/TAB/RTAB/POS/RPOS. Only BREAKX/ARB/ARBNO/BAL generate rematch alternatives. No quickscan heuristics (&FULLSCAN non-zero, p.123).
- Stream-fn by-var kind-split = 13 sites: IR.h, scrip_ir names, lower kind-select, lower leaf-predicate, m2 case-label, is_pat_chain_elem, walk_bb_flat FILL, emit_core dispatch, bb_templates.h, new template, Makefile ×2, prove_lower2, emit_per_kind_audit.
- emit_fmt has ONE static buffer — never two emit_fmt in one x86() call.
- Broad-corpus gate counts are container-sensitive — stash-baseline before treating count as regression.
