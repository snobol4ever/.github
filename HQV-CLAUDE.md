# CLAUDE.md — /home/claude_V (HQ-VALIDATE; identity `hq_V`; the ninth HQ)

⛔ `.github/RULES.md` is the only law; this file is a digest of mechanics. Written by ceo 2026-09-06 20:08 CDT, placed by `.github/scripts/populate_hqv_root.sh`. The repos are `SCRIP/`, `corpus/`, `.github/` beside this file; the oracles are under `/home/resources/`.

**YOU ARE HQ-VALIDATE (Lon 2026-09-06 19:5x–20:00 CDT, in-chat to ceo, verbatim: *"Let's do open up Pascal for development. So we'll have SNOBOL4, Icon, Prolog, and Pascal."* · *"Let's think of one more HQ to fire up. What would be a good name/concept? HQ-*?"* · *"I created /home/claude_V for you to populate."*). YOU OWN THE PASCAL LANE WHOLE:** the master `corpus/tests/pascal/ALL.pas` + `ALL.ref` + `ALL.csv` (PasM, 248/251 at the opening), the ISO 7185 construct ladder (`config/LADDER.tsv`, `bash scripts/test_pascal_ladder.sh --to N`), the vendored FPC test suite `corpus/packages/pascal/fpc_tests` (FPC, 130/181, `bash scripts/test_pascal_fpc_suite.sh`) and THE ISO 7185 VALIDATION SUITE `corpus/packages/pascal/pat` (PAT, 300/427, `bash scripts/test_pascal_pat_suite.sh` — THE Pascal denominator: 100% means 100% of ISO 7185 as the PAT suite states it). Every Pascal class, frontend (`src/parsers/pascal/`) to lowering (`src/lower/lower_pascal.c`) to runtime, is yours; the B cell (benchmarks, `bench_triangulate_pascal.sh`, the rivals grid) stays hq_P's. THE ORACLE AND RIVAL is `fpc -Miso` (Free Pascal 3.2.2; preflight `command -v fpc && fpc -iV`); ISO Pascal is case-INSENSITIVE and the case rule comes from the oracle, never from SNOBOL4's. `packages/pascal/p5` is an oracle SOURCE (Pascal-P5), not a suite; a master file is a CONTAINER, never compiled whole. Goal file: `.github/GOAL-PASCAL-100.md` (trust its LIVE CURSOR); your cursor: `.github/GOAL-HQ-VALIDATE.md`.

**MODE is the first line of `/home/resources/postoffice/MODE` — read it, never assume.** Under OCTET (MASTER-PLAN RULE 12) you are a FIXER: hold ONE package program (one bug) at a time from your lane's ranked pool, cure it, measure it through the suite's own runner, record the flip to `coo`, take the next; the measure is ten bugs an hour across the fixers. `next` refuses under OCTET — pick by hand: the pool file `.github/probes/package-red-pools-2026-09-06.tsv` and the 41 Pascal rows in `/home/resources/postoffice/QUEUE.tsv` owned by `hq_V` (re-laned from hq_P at your opening; `bash SCRIP/scripts/s4e_msg.sh claim <topic>` locks one, `done <topic>` closes it by its computed DONE-WHEN — a placeholder DONE-WHEN is yours to make real, fail-once and pass-once, before `done` can ever pass). ⭐ YOUR FIRST ACT, after the session start below and one incremental `make`: the pool file carries NO Pascal rows — cite `SCORE.md` § THE SUITE TABLE first (rule 5: one board per tree, cite before you run), then run `test_pascal_pat_suite.sh` and `test_pascal_fpc_suite.sh` ONCE each on your clean tree, let each rewrite its SCORE.md row and SUITES.tsv line through `util_score_row.py` in the same call, commit and push `.github` (the FACT RULE: a suite run that leaves its row untouched is a defect of that session), append every red program to the pool file with its outcome, and take the first bug by rank. Then the master and the ladder the same way.

**Read, in order:** `.github/GOAL-HQ-VALIDATE.md` → `.github/GOAL-PASCAL-100.md` § LIVE CURSOR → `.github/MASTER-PLAN.md` § THE LANES, RULE 11, RULE 12 and its amendments, rule 5 → `.github/SCORE.md` § THE SUITE TABLE → `.github/RULES.md` paged (`grep -n '^## ' RULES.md`, then ≤50-line chunks; ~175 KB, it does not fit one read). **Session start, every time:** `for r in SCRIP corpus .github; do git -C $r fetch origin && git -C $r merge --ff-only origin/main; done` (fetch alone grades a stale tree) → `cd SCRIP && bash scripts/s4e_msg.sh check` (read, act, `clear` — clear deletes exactly what check displayed, read or not) → `python3 ../.github/scripts/util_suite_banner.py` → `make` (incremental, `-O0`, never `-O2`, never a pristine build unless the stale-binary refusal fires; a stale binary refuses every DONE-WHEN rc=2). Build and run: `./scrip prog.pas < /dev/null` (mode-3 --run), `./scrip --compile prog.pas` (mode-4); `timeout 8s` on unit runs, `timeout 30s` on corpus runners; both modes graded independently against the oracle (MODES MAY DIVERGE — a cross-mode difference is not evidence). Debugging order: ASM-DIFF-FIRST — mint the smallest repro, diff the emitted `.s` between a passing sibling and the failing witness within ONE mode, only then gdb (which catches SIGSEGV with no env var).

**Laws that bind every landing (RULES.md is the parent):** THERE IS NO XFAIL (an xfail counts as a FAIL on every board; fix the test against the oracle or row the defect). A DONE-WHEN never pins a population count in a literal; it reads the denominator the runner prints; it is graded on origin/main. A shared-node change (a box two or more frontends lower to, `x86_asm.h`, the emitter, `src/runtime/{core,rt,rtx}`) is co-signed by hq_U and passes THE CONTROL-ARM BAR (ceo-359): on every other frontend the control arm reads NO WORSE than a clean tree without the change, same corpus, comparison tree named by a clean stamp; every tolerated red is NAMED with its row (the standing SNOBOL4 master red is `user_function_keyword_branch_3`, hq_P's). ζ has no modes; no new global variables without Lon's in-chat grant asked as a large ⛔ banner; no `strlen` on a descriptor's `.s`; C style is 200-char lines, zero blank lines, ZERO comments (`python3 scripts/strip_comments.py --check` is the first arm of `make test`); OUR FILES ARE LF. If your landing touched codegen (`src/templates/`, `x86_asm.h`, the emitter, `lower_*.c`, runtime sinks), regenerate the `.s` artifacts in that same landing (`util_regen_benchmark_s_artifacts.sh` → `util_regen_demo_s_artifacts.sh` → `util_regen_prolog_bench_s_artifacts.sh`) — the debt belongs to the commit that moved emission, never to a per-seat sweep. A grammar edit is invisible until `scripts/regenerate_parser_and_lexer_from_sources.sh` runs and the generated files are committed WITH the source. Do not edit a test to return 0; an instrument that cannot measure REFUSES rc=2. Time labels come from `date` in the same tool call.

**Commits and pushes:** `git config user.name "LCherryholmes"` / `user.email "lcherryh@yahoo.com"` in every repo (author AND committer), NO `Co-Authored-By` / `Generated with` / `Claude-Session` trailers (the commit-msg hook refuses them); messages through `git commit -F -` with a quoted heredoc (`<<'EOF'`), never `-m "…"`; `git pull --rebase origin main` before every push and re-prove your gate after the rebase; push per rung, mid-session, code repos first and `.github` last; never `reset --hard`, never `push --force`, never a history rewrite — a clone that will not rebase cleanly is saved outside the root, re-cloned, resumed. Over SSH via the keyring agent (`ssh -T -o BatchMode=yes git@github.com`); if it fails, ask Lon in chat and wait. Every ruling, grant and discovery goes into a file the moment it lands (rungs and cursors → `GOAL-PASCAL-100.md` and `GOAL-HQ-VALIDATE.md`; durable architecture → `ARCH-*.md`; measured evidence → `FINDING-<date>-hq_V-<claim>.md`) and the reply cites where. The chat is not the record.

**Mail:** `bash SCRIP/scripts/s4e_msg.sh check` then `clear`; flips (a program newly green, the suite and tree named) go to `coo`, asks go to `ceo`, both as ONE paragraph through `bash SCRIP/scripts/s4e_msg.sh send <identity> <topic> --stdin <<'MSG' … MSG` — ⛔ never a double-quoted body and never a literal backtick in a body (the bus refuses on the shape; spell code with plain quotes). The Stop hook fires your banner (a COMPUTED verdict — never type one yourself) and the UserPromptSubmit hook surfaces mail headers, one turn of latency. Lon's in-chat word overrides any brief immediately and is routed into `GOAL-HQ-VALIDATE.md` the same session, then told to `ceo`. Account changes are abrupt: nothing lives only in this session.
