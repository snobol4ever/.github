# HANDOFF 2026-05-19h5 — Sonnet 4.6 — EC-3 + EC-4 Complete

## one4all
`8890d685` — EC-4: unified emit_prologue/emit_epilogue in emit_core.c

## .github
This commit.

## Gates
- GATE-1 smoke_icon: PASS=5 FAIL=0 ✅
- GATE-2 smoke_unified_broker: PASS=23 FAIL=26 ✅
- GATE-3 icon_all_rungs --interp: PASS=194 FAIL=36 XFAIL=35 ✅

## Session work summary

### EC-3 COMPLETE (EC-3a through EC-3f)
Five `SM_templates/` files created, 63 unified functions, ~157 silo arms → one-line template calls:

| Step | File | Functions | Arms |
|------|------|-----------|------|
| EC-3a | `sm_push_pop_lits.c` | 7 | 21 |
| EC-3b | `sm_arith.c` | 9 | 27 |
| EC-3c | `sm_compare.c` | 3 | 9 |
| EC-3d | `sm_ctx.h` + `sm_control.c` (JUMP/S/F, HALT) | 4 | 12 |
| EC-3e | `sm_control.c` extended (RETURN/FRETURN/NRETURN ×3) | 7 | 27 |
| EC-3f | `sm_pat.c` (full SM_PAT_* + SM_EXEC_STMT) | 30 | 58 |

Infrastructure: `sm_template_common.h`, `sm_ctx.h`, `sm_ctx_t` struct (with `pc_to_fn[]`/`fn_names[]` for NET RETURN family).

### EC-4 COMPLETE
- `emit_prologue()` / `emit_epilogue()` in `emit_core.c`: IS_JVM/IS_JS/IS_NET dispatch
- All three `emit_*_program` entry points call unified fns
- `emit_ir_block` (emit_ir.c) calls unified fns instead of vtable slots
- `IR.h` added to `emit_core.h` includes (for `IR_block_t`)
- Static per-silo prologue/epilogue fns retained for EC-5 vtable registration (not yet deleted)

## NEXT: EC-5
Delete `emit_jvm.c`, `emit_js.c`, `emit_net.c`. Move IR walk infrastructure from `emit_ir.c` → `emit_core.c`. Delete `emit_ir.c` / `emit_ir.h`. Delete `IR_emit_vtable_t` struct. Update `emit_ir_targets.c`.

**Pre-work for EC-5:** Before deleting silo files, check whether `emit_jvm_scalar`, `emit_jvm_generator`, `emit_js_scalar`, `emit_js_generator`, `emit_net_scalar`, `emit_net_generator` (the vtable function-pointer slots) have real callers besides the vtable itself. If only used via vtable, they can be deleted with the files. If called directly, move to `emit_core.c` first.
