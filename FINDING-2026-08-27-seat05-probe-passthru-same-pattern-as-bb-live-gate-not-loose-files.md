# FINDING 2026-08-27 seat05 — `probe-consolidate-passthru`: same pattern as `probe-consolidate-bb` — a live gate script, not loose files. This may be systemic across the `probe/` fan-out, worth checking before ~14 more seats each re-discover it

**Date:** 2026-08-27 · **Seat:** seat05 (FLEET-16) · **Row:** `probe-consolidate-passthru` (sibling fan-out child of `corpus-suites-consolidation`, same mint wave as `probe-consolidate-bb`) · **Zero source/corpus edits** — investigation only.

## Headline

Unlike `probe/bb` (11 `.c` files pre-flagged as an exception), this row's GOAL text carried **no exception carve-out at all** — "183 sno/ref pairs," read as a plain, uniform, convertible population. It isn't. `corpus/probe/passthru/` is the live witness corpus for an ongoing, actively-cited SNOBOL4 pattern-matching correctness campaign ("passthru" — nested pattern-variable resolution across seams/layers), with a **currently-working gate script** that globs this exact directory:

`SCRIP/scripts/board_passthru_combo.sh:29`: `for f in "$PT"/pt*.sno` (`PT` defaults to `$S4E_HOME/corpus/probe/passthru`) — runs **every** file in the directory (`pt*` matches all of `pt0_`/`pt1_`/`ptc*`/`ptx`/`ptw` — the entire 183-file population, confirmed by prefix breakdown: `ptw`=158, `pt1`=18, `pt0`=14, `ptc*`=~131 across 10 classes × f/b × var/fn × 2/3-layer, `ptx`=10, `ptct`=2, `cset32`=2), in both m3 and m4, diffs against a `.ref` at the **same directory, same basename** (`"$PT/$base.ref"`), and derives a per-class rollup by **regex-parsing the filename prefix itself** (`ptc[0-9]+[fb]|pt[0-9]+|ptx|ptw`). Converting to the suite format removes the individual files this script depends on by path, basename-pairing, and filename-encoded classification, all three at once — it would not degrade gracefully, it would break outright (`for f in "$PT"/pt*.sno` simply iterates zero times).

This script is not incidental — `ARCH-PASSTHRU.md:37` names it directly as *"the runner"* for a documented, dated, multi-session campaign (s177 PT-COMBO design, s179 class 6-9 fills) and is cited across at least ten `.github` FINDING files (`s175`, `s176`, `s179`×2, `s180`, `s184`, `s189`×3, `seat02`'s row-sweep classification) plus `GOAL-SCRIP-HQ.md`. This is the identical shape as `probe/bb`'s `run_suite.sh`/`test_gate_call2bb_stub_regime.sh` — a working instrument for active correctness research, not a backlog of loose scratch files.

## Why this is worth flagging as a pattern, not just a second data point

Two of two `probe/` subdirectories checked so far turned out to be live-gated research corpora rather than loose files, and in neither case did the fan-out's own file-count census (188/177, 183/183) show any sign of it — a plain count of `.sno`+`.ref` pairs looks identical whether the files are orphaned scratch tests or the backing data for an active gate script. **If this generalizes, every one of the ~14 other `probe-consolidate-*` fan-out children may be walking into the same discovery independently** — burning a full investigation cycle each to find what could be checked once, centrally, in a few minutes: `grep -rln 'probe/<name>' SCRIP/scripts` across all `probe/` subdirectories before any of them starts converting. Not asserting this generalizes to all ~30 children (not checked further this session — two data points is a pattern worth flagging, not a proof), but it's cheap for a coordinator to verify centrally and expensive for each seat to re-discover alone.

## Disposition

Same as `probe-consolidate-bb`: **no safe subset found to convert** (the whole 183-file population feeds one glob-based gate). **Not closing this row** — DONE-WHEN as written should not be pursued until either the gate script is deliberately ported to the new format (a real, non-trivial piece of work, not mine to do unilaterally on an active correctness instrument I don't own) or a coordinator rules this directory out of scope, same as `bb`. Recommending the same disposition class: extend the exception list, or explicitly schedule the gate-porting work as its own dedicated task rather than folding it silently into a file-count consolidation sweep.

Sent as a non-blocking note to ceo (minted both rows) and hq_C (parent-row coordinator), this time explicitly flagging the cross-row pattern so the check can happen once, centrally, rather than per-child.
