<!-- GOAL-MODE34-IDENTICAL · mode-3 (--run, x86 BINARY) ≡ mode-4 (--compile, x86 TEXT), strictly 1:1 corresponding -->

**LIVE CURSOR (Claude Sonnet 5, 2026-08-12c/d, session mid-flight):** D1 (M34-2) is CLOSED — see M34-2 section, measured not assumed. **5b is ROOT-CAUSED and it is NOT the bug 12o described** — full detail in `FINDING-2026-08-12p-...-SPAN-VAR-INLINE-ARM-STALE-FRQ-OFFSET-...md`, read that first. Short version: `SPAN(var)` (dynamic charset) never matches in mode-3 because its INLINE guts arm (`bb_match_span.cpp`, `sp_gi()`) reads the operand via the legacy `FRQ`/`FR` accessor (no depth compensation, per `x86_asm.h`'s own "ZD-1 — THE FOUR MODES" doc), but executes 64 bytes deep inside `MATCH_BEGIN`/`MATCH_ASSIGN_SAVE` transient-stack nesting — measured with gdb, the read lands 56 bytes short of the operand's real DESCR and pulls stack garbage. This is a THIRD, previously-unexamined SPAN code path, distinct from the ZD arm `FINDING-2026-08-12i` already fixed (confirmed present in HEAD) and distinct from the static/literal arm, which has its OWN separate crash bug (Bug B, unexamined) whenever a literal SPAN's match succeeds. `POS`/`REM`/`.`-capture are exonerated (probe t4). **NOT YET FIXED** — the obvious fix (copy LEN's `zls_off`/`op_zres` fallback) is empirically unsafe to assume: `LEN(*var)` crashes in this same nesting shape, so that fallback path needs its own root-cause first. Next seat: root-cause `LEN(*N)`'s crash (cheapest, same class), THEN decide the SPAN fix, THEN Bug B (literal SPAN crash), THEN re-run `/home/claude/repro_5b.sno` (should pass once SPAN(var) works — no other changes expected needed). Prior: `FINDING-2026-08-12o-...` (5a fix + census, confirmed pushed at `f16775ff`), `FINDING-2026-08-12n-...` before that.

**5a IS FIXED (SCRIP `dbd85305`).** It was NOT a `_.nid` uniqueness bug — `bb_node_id`'s dense table is provably collision-free; the SAME node was walked TWICE by two independent top-level `emit_chain` calls. A plain DEFINE mints `LBL__<entry>` as a shared-graph pseudo-proc (`proc_entry_node` = the label anchor *inside main's own graph*) which emits the body once; main's own chain then pulls that same anchor in AGAIN via `codegen_flat_chain_body`'s group-root pass (`zls_g_group_anchor`). Fix = a pointer set of already-emitted chain entries, consulted by the group-root pass. Measured with a `nodes[]` dump, not inferred: the by-name call STUB graph is a clean `n=2` and is innocent.

**⭐ 5a WAS ALSO SILENTLY MISCOMPILING MODE-3.** The group-root guard is `(!g_is_text || ...)` and `g_is_text==0` in mode-3, so the pass fired UNCONDITIONALLY there; a binary emitter has no assembler to reject the duplicate symbol, so mode-3 got wrong CODE instead of a loud error. Consequence for this rung: **12n's claim that 5b is "wrong in BOTH modes, differently" is RETIRED — that was a 5a artifact.** Post-fix both modes agree (`   hello`) and 5b is ONE bug. GENERALISE: a mode-4-only symptom is NOT evidence of a mode-4-only defect wherever the emitter path is shared.

**Regression evidence:** crosscheck 318 before/after is `TOTAL=318 IDENTICAL=254 DIFFER=16 M3-MISS=46 M4-MISS=1 BOTH-FAIL=1` with the DIFFER and M3-MISS **SETS diffed byte-identical** (not just counts). Census committed: `SCRIP/docs/MODE34-PARITY-CENSUS-2026-08-12b.tsv` (`88b5f393`) — that also settles 12n's "pick a convention" item. Honest: **5a resolved ZERO of the 16 DIFFER**; 12n's hope that it explained several is falsified. `test_string` is not a 5a case (its m4 SEGV is pre-existing, verified against the pre-fix binary).

**⛔ NEXT SESSION STARTS WITH THE MONITOR, NOT WITH 5b.** MONITOR-FIRST was attempted and is BLOCKED: `PARTICIPANTS="spl scr"` DIVERGEs at step 2 on the `LABEL stno` sync event at statement 1, before any matching happens. Isolated to a **statement-COUNTING CONVENTION difference**: SPITBOL and SCRIP disagree by a constant +1 on the goto target of any `DEFINE(...) :(label)` line (reproduced on a DEFINE-only program with no patterns; a no-DEFINE program agrees fully, exit 0). Output is correct in both controls — this is harness noise, but the controller hard-stops on it, so the monitor can never reach 5b AND every future DEFINE-bearing SNOBOL4 hunt is blocked the same way.

**⭐ UPDATE (same session, during push/rebase):** `MONITOR_SKIP_BARE_LABEL_STNO=1` was already landed by the upstream commit `5ec6e607` (MON-CAP, BOARD s43) while this session was running. It is exactly the bounded, stno-verified fix proposed above. The note "cure proposed, not half-landed" in the FINDING doc is superseded — the cure IS landed. So the monitor blocker is GONE; next session can go straight to 5b with `MONITOR_SKIP_BARE_LABEL_STNO=1 PARTICIPANTS="spl scr" bash scripts/test_monitor_3way_sync_step_auto.sh /home/claude/repro_5b.sno`.

**Then 5b**, from the POST-FIX state (both modes `   hello`, oracle `hello`), not 12n's description. Manual semantics re-grounded this session and unambiguous: `POS(i)` is zero-width and never consumes; `(SPAN(s)|'')` is the manual's OWN nullable-span idiom; `.` is CONDITIONAL (deferred to whole-match success), unlike immediate `$`.

**Also still open and cheap: D1's timing A/B (M34-2-verify) — untouched this session, still the cheapest item on the board.**

**⛔ ROUTED IN from GOAL-RBP-EARN

**⛔ ROUTED IN from GOAL-RBP-EARN (Lon-approved split, 2026-08-12 s40):** `treebank-array` m4-only SEGV (rc=139, m3 green) — a MODE34-IDENTICAL violation, not a defer-boundary item, so it bills HERE now. Repro + prior analysis: GOAL-RBP-EARN's s38 FIX PLAN STEP 2 (kept there verbatim until this file adopts it) and `archive/ARCHIVE-RBP-EARN-CURSOR-HISTORY.md`.
> ⚠ **Read this one through M34-5c (added 2026-08-12b).** "m4-only SEGV, m3 green" is EXACTLY the shape 5a wore, and 5a turned out not to be m4-only at all — m3 was running the same defective shared path and simply had no assembler to complain with, so it emitted wrong code and looked green. **"m3 green" is not evidence m3 is correct; it is evidence m3 did not CRASH.** Before treating this as an m4 codegen bug, diff m3's output for `treebank-array` against the ORACLE (not against m3's own prior output, and not against "it ran"), and check whether the faulting path is guarded by a `g_is_text` conditional.

# ▶▶▶ NEXT SESSION — START HERE (created 2026-06-25, Lon directive)

**MANDATE.** SCRIP has EXACTLY TWO execution modes and they MUST be 1:1 corresponding:
- **mode 3 = `--run`** — x86 BINARY assembled in-process into a sealed RX slab, jumped into.
- **mode 4 = `--compile`** — x86 TEXT (GAS `.s`) to stdout, assembled by `as`/`gcc`, linked against `libscrip_rt.so`.

The two modes share ONE emitter (the `bb_*`/`xa_*` templates) and MUST share ONE driver setup. For the SAME source → SAME IR → SAME logical instruction sequence; the ONLY permitted difference is the LEAF materialization inside `x86(...)`: bytes for BINARY, GAS text for TEXT. Any place where mode-3 and mode-4 produce a DIFFERENT instruction stream — a different code PATH, a missing fast path on one side, a different runtime call, a different operand source — is a **DEFECT to be driven to zero (99.999% identity)**. "Same output" is necessary but NOT sufficient: two engines can print the same bytes via different code; this rung targets the CODE, then the output.

**MODES 1 AND 2 DO NOT EXIST.** Mode 1 (the AST-walk `eval_node` evaluator) and mode 2 (`--run`, the IR-graph software interpreter) are DELETED. The driver already exposes only `--run`/`--compile` (verified 2026-06-25 — no `--run` flag remains). What survives is RESIDUE: stale strings/comments in live code, dead `src/attic/` files, and stale prose in authoritative docs. All references on LIVE/authoritative surfaces must be purged. **Scope guard:** archival HANDOFF/SESSION records are history, NOT live descriptions — they are NOT rewritten (rewriting them would falsify the record and violates the "don't touch unrelated goal files" rule). Target only surfaces a reader consults to understand SCRIP *today*.

---

## CONFIRMED DIVERGENCES (grounded — session 2026-06-25, with the live binary)

- **D1 — GVA is mode-4-ONLY for SNOBOL4 (the headline defect).** Mode-4 SNOBOL4 emits `__gva` + `gva_register` and accesses globals via `[rbx+k*16]` (zero hash calls). Mode-3 SNOBOL4 (`src/driver/scrip.c` SNOBOL4 `--run` block, ~3322–3389) NEVER calls `gva_register` nor sets `g_gva_active`, so every global read/write silently falls back to `NV_GET_fn`/`NV_SET_fn` by-name hashing. Output matches; the instruction stream does NOT; mode-3 is ~7–8× slower (goal-file timings: arith_loop 862 ms mode-3 vs 113 ms mode-4). **Prolog and Icon mode-3 ALREADY set up GVA** in-process (`pl_gva_arena = GC_MALLOC(...)` + `gva_register` + `m3_enter_with_rbx`, ~3306–3315) — SNOBOL4 mode-3 is the lone hold-out. The fix pattern already exists in the same file.

- **D2 — The driver runs TWO separate, drifting setup blocks.** `mode_compile_x86` (TEXT, ~2678–3163) and `mode_run` (BINARY, ~3169–3400) each independently do proc registration, global collection, preamble construction, and per-language dispatch. They are parallel but NOT identical (D1 is one symptom). Duplicated setup is the ROOT CAUSE: a fast path added to one block is invisible to the other. Structural cure = ONE shared setup, parameterized only by emission medium.

- **D3 — DCR-2 is mid-flight and currently mode-4-only.** The function-call direct-dispatch optimization (`__proc[]` index table + `rt_call_proc_direct`) has its TEXT arm landed in `bb_call.cpp` and the driver wiring NOT yet done; `g_proc_direct_active` is never set, so it is presently DORMANT in both modes (behavior-neutral, build-verified). When activated it MUST be both-medium (mode-3 binary arm + mode-3 driver activation), or it becomes a fresh D1-class divergence. This rung GOVERNS DCR-2's completion.

- **D4 — The crosscheck gate is mode-4 ONLY.** `scripts/test_crosscheck_snobol4.sh` header: "MODE-4 ONLY (Lon directive 2026-06-06)". That directive is SUPERSEDED by this rung: gates must exercise BOTH modes and assert mode-3 output == mode-4 output == `.ref`. Until then, mode-3 regressions are invisible.

- **D5 — Mode-3 coverage gaps are unmeasured.** Mode-3 soft-falls ("[SBB] mode-3: statement shape not yet flat-emittable") on shapes mode-4 may handle. The set of programs mode-4 compiles but mode-3 cannot is unknown and must be enumerated (it is a divergence class).

---

## BASELINE CENSUS (M34-0 RUN — crosscheck corpus, 262 programs, 2026-06-25) — ⛔ SUPERSEDED, KEPT FOR HISTORY ONLY

⛔ **DO NOT gate against these numbers.** Superseded by the 2026-08-12 census in the LIVE CURSOR. Two reasons they cannot be compared directly: (1) the corpus grew 262→318 programs, so every count moved for reasons unrelated to codegen; (2) the harness that produced BOTH sets had the `-no-pie` bug (fixed `94d283c`) — the 2026-06-25 numbers were taken on a box whose gcc did not default to PIE, so they were accidentally valid there and would NOT reproduce here. Any future census must state the corpus size and the harness commit.

`scripts/test_mode34_parity.sh` landed and run over `/home/claude/corpus/crosscheck`:

**TOTAL=262 · IDENTICAL=191 · DIFFER=1 · M3-MISS=63 · M4-MISS=1 · BOTH-FAIL=6**

- **DIFFER=1 → `library/test_string`** (the priority bug — same program, different output per mode). Concretely: mode-3 prints 2 lines then **ABORTS** with `bb_scan: mode-3 non-literal pattern needs native PB-RB graph (rt_scan deleted — IR interpreter gone)` — a native-pattern hole left by the mode-2 excision that mode-3 never backfilled. mode-4 runs to completion but is **wrong** (`ltrim/rtrim/trimws` don't trim, `contains` FAILs). Both broken, differently. (Oracle `sbl -b` confirms the expected trimmed output.)
- **M3-MISS=63** — mode-4 compiles+runs, mode-3 soft-falls/empty. Dominated by native-pattern shapes: `*_pat_fence_*`, `*_pat_arbno_*`, `*_pat_calc_*`, `*_pat_json_*`, `W03/W04_*`, `word1..4`, `wordcount`, `cross`, `1017_arg_local`, `1018_apply`. These are the mode-3 BINARY-arm gaps (D5).
- **M4-MISS=1 → `expr_eval`** — mode-4 fails to compile, mode-3 runs. (mode-4 codegen gap.)
- **BOTH-FAIL=6** — `coverage_sno_nodes`, `064/065/106_pat_fence_*`, `071_builtin_ucase`, `072_builtin_lcase`.

The 191 IDENTICAL prove the shared-template design works where both arms exist; the 71 non-identical are the rung's worklist. Re-run the harness after every step; IDENTICAL must monotonically rise, DIFFER must hit 0 first.

## STEPS

### M34-0 — Parity harness + baseline census ✅ HARNESS LANDED + RUN (2026-06-25)
- [x] **M34-0a — `scripts/test_mode34_parity.sh`** — classifies every program IDENTICAL / DIFFER / M3-MISS / M4-MISS / BOTH-FAIL; `--tsv` census; exit nonzero on any DIFFER. DONE.
- [x] **M34-0b — baseline census run** over crosscheck (numbers above). NEXT: also run over benchmark + feature corpora and commit `docs/MODE34-PARITY-CENSUS-<date>.tsv`.

### M34-0-archived — (original measurement step, superseded by the run above)
- [ ] **M34-0a — `scripts/test_mode34_parity.sh`.** For each program in a corpus, run `--run` (mode 3) AND `--compile`→`as`→`gcc`→run (mode 4); capture both stdouts + exit status. Classify each program: `IDENTICAL` (3==4==ref), `DIFFER` (3≠4), `M3-MISS` (mode-3 soft-fall/empty while mode-4 produces output), `M4-MISS`, `BOTH-FAIL`. Emit a TSV census + summary counts. `timeout 8s` smoke / `timeout 30s` corpus. NO source change.
- [ ] **M34-0b — run the census over the SNOBOL4 crosscheck + benchmark + feature corpora.** Commit the TSV as `docs/MODE34-PARITY-CENSUS-<date>.tsv`. This is the ground-truth divergence list every later step closes against. Record the headline counts in this goal file's watermark.

### M34-2 — Close D1: SNOBOL4 mode-3 GVA parity — ✅ **CLOSED (Claude Sonnet 5, 2026-08-12c) — VERIFIED, NOT JUST ASSUMED**
**M34-2-verify result:** `corpus/benchmarks/snobol4/arith_loop.sno` (1,000,000-iteration loop, global `N` read+written every iteration — the exact shape D1's ~7–8× claim was built on), same box, same build, `RT_OPT` unset → default `-O0` (per O2-DIRECTED-ONLY, no `-O2` anywhere in this measurement). 3 runs each:
- mode-3 (`--run`): 22ms, 22ms, 24ms
- mode-4 (`--compile` → `gcc -no-pie` → run): 22ms, 23ms, 23ms
- oracle (`x64/bin/sbl -b`): 58ms (SPITBOL is not the perf comparison here, just a sanity check both engines still agree on `iterations: 1000000`)

**No gap.** Mode-3 and mode-4 are within measurement noise of each other; the old headline "862 ms mode-3 vs 113 ms mode-4" is gone. D1 is CLOSED — the `rt_gva_island()` pinned-VA mechanism (shared with Icon/Prolog/Raku, `ARCH-ICON.md` 2026-07-18) is the answer, and SNOBOL4 mode-3 already uses it; no driver edit needed. M34-2a/b (mirror-the-Prolog-block plan) are deleted below — they targeted a defect that no longer exists at the measured shape. If a *different* globals-heavy shape ever shows a gap, re-open under a new step number with its own measurement; do not reopen M34-2a/b, they were written against the stale `rbx=GVA base` mental model this rung's own prereq-reads banner already retired.

### M34-3 — Close D3: finish DCR-2 both-medium under this rung
- [ ] **M34-3a — mode-4 driver wiring.** In the SNOBOL4 mode-4 block: after `gva` emission, `proc_collect_reset()` + `proc_collect_graph(sbbg)`; if `proc_slot_count()>0`, emit `.bss __proc: .space n*8` + `.rodata __proc_names` (name strings + quad table) + preamble `lea rdi,__proc_names; lea rsi,__proc; mov edx,n; call rt_proc_table_fill@PLT` (placed AFTER `gva_register` — procs must be registered first by `proc_startup`). Set `g_proc_direct_active=1` around `gvar_flat_chain_build_text`.
- [ ] **M34-3b — mode-3 binary arm + driver activation.** Add the BINARY arm to `bb_call_gvar_userproc_str` (bake `mov edi, idx` via `rt_proc_index_of(fn)` at emit time — valid because the mode-3 driver registers all procs before chain build — then `x86_call_ro("rt_call_proc_direct", &rt_call_proc_direct)`). In the SNOBOL4 mode-3 block set `g_proc_direct_active=1` (no `.bss` table; the binary arm bakes the index immediate).
- [ ] **M34-3c — gate.** Crosscheck BOTH modes byte-identical to pristine HEAD fail-set (PASS=171 FAIL=84 SKIP=6, zero regressions). M34-0 census: func_call/fibonacci/roman `IDENTICAL` in both modes. A/B mode-4 func_call/fibonacci vs HEAD (the DCR-2 delta), AND mode-3 func_call/fibonacci vs HEAD (the binary arm delta). Re-ground 3-way (oracle `x64`, token-free).

### M34-4 — Close D2: unify the driver setup (the structural guarantee)
- [ ] **M34-4a — extract ONE shared setup.** Factor the per-language setup (proc registration, `gva_collect`, `proc_collect`, preamble plan) out of the two mode blocks into a single routine that returns a setup descriptor; the two blocks then differ ONLY in the emission leaf (binary slab vs text `.s`) and the entry (`m3_enter_with_rbx`/in-process call vs `main:` + `call flat_α`). After this, a fast path added once is visible to BOTH modes by construction — D1-class drift becomes structurally impossible.
- [ ] **M34-4b — gate.** Full M34-0 census unchanged-or-better; crosscheck BOTH modes byte-identical; all language gates green (SNOBOL4 first; Icon/Prolog/Pascal/Raku must not regress).

### M34-5 — Close D5: mode-3 coverage parity
⚠ **SCOPE CORRECTION (2026-08-12b, learned from the 5a fix):** this step was written as if every mode-3 gap were a MISSING ARM (M3-MISS, a soft-fall you can see). 5a was neither: it was a defect on the SHARED path whose mode-4 arm failed LOUDLY (`as` rejects a duplicate symbol) while its mode-3 arm failed SILENTLY (a binary emitter has no assembler, so it emitted wrong code and kept going). That class is invisible to an M3-MISS enumeration *and* invisible to any gate that only checks mode-3 for absence-of-output. M34-5c below is that class; it is not optional and is probably larger than the M3-MISS list.
- [ ] **M34-5a — enumerate M3-MISS.** From the M34-0 census, list every program mode-4 compiles but mode-3 soft-falls. Triage each shape's missing BINARY arm (a template whose `MEDIUM_BINARY` path is absent/bombs).
- [ ] **M34-5b — fill the missing binary arms** shape-by-shape until M3-MISS == 0 for the SNOBOL4 corpus. Each fix is a both-medium template completion; gate per shape.
- [ ] **M34-5c — ⭐ NEW: sweep the LOUD-IN-M4 / SILENT-IN-M3 class.** For every known mode-4 assembler or link failure (duplicate symbol, undefined symbol, relocation error), ASK WHETHER THE SAME SHARED CODE PATH RUNS IN MODE-3, and if so what mode-3 does instead of erroring. The mode-4 diagnostic is a free detector for a defect that mode-3 has no way to report. Method that worked for 5a: instrument the suspect emit-time identity (there, `bb_node_id`), dump `codegen_flat_chain_body`'s `nodes[]` per chain, and compare the node MULTISET against the graph — a node appearing twice is the bug, regardless of which medium noticed. Start from the guards that differ by medium: `grep -n 'g_is_text' src/emitter/emit.cpp` — every one of those is a place the two modes can silently part company, and at least one (the group-root pass) was already wrong.

### M34-6 — All gates run BOTH modes (the standing requirement)
- [ ] **M34-6a — crosscheck.** Rewrite `test_crosscheck_snobol4.sh` to run mode-3 AND mode-4 for every program and assert `m3==m4==ref`. Retire the "MODE-4 ONLY" header. A `DIFFER` is a hard FAIL.
- [ ] **M34-6b — benchmark/feature/regression + per-language gates.** Apply the same both-mode assertion to `test_bench_snobol4_modes.sh` (confirm it truly runs both), the feature/demo `.s` regen flows, and the Icon/Prolog/Pascal/Raku gates. Every "rung gate" in every goal file inherits the both-mode requirement.
- [ ] **M34-6c — parity in CI of the harness itself.** `test_mode34_parity.sh` becomes a first-class gate invoked by the handoff sequence whenever codegen is touched.
- [ ] **M34-6d — ⛔ NEW (2026-08-12, learned the hard way): THE HARNESS MUST SELF-CHECK BEFORE IT REPORTS.** The `-no-pie` bug (fixed `94d283c`) made every mode-4 link fail, and the harness cheerfully reported `IDENTICAL=0 DIFFER=0 M4-MISS=271` — a *clean-looking* census with a zero DIFFER count, i.e. it PASSED its own exit-code gate (`nonzero only on DIFFER`) while measuring nothing at all. **A gate that can silently degrade to measuring nothing is worse than no gate, because it manufactures false confidence.** Fix: before the corpus loop, compile+link+run a trivial known-good program (`OUTPUT = "x"`) through the FULL mode-4 pipeline; if it does not come back IDENTICAL, `exit 2` with "HARNESS BROKEN — not a corpus result" and print the toolchain error verbatim. Also make the summary refuse to print a census where `M4-MISS + BOTH-FAIL > 50%` of TOTAL without an explicit `--allow-mass-failure` flag. Apply the same self-check pattern to every both-mode gate M34-6a/b creates — this class of failure is generic, not specific to PIE.

### M34-7 — Template-level 1:1 audit (defense in depth)
- [ ] **M34-7a — both-arm presence audit.** For every `bb_*.cpp`/`xa_*.cpp`, confirm BOTH a BINARY and a TEXT arm exist and correspond instruction-for-instruction (no arm that emits a different op sequence). Reuse/extend the existing BOTH-MEDIUM gate scripts (`test_gate_template_medium_invisible.sh --strict`, the `MEDIUM_*`-in-templates greps from RULES.md). Any template emitting via raw bytes outside `x86_asm.h`, or gating output on `MEDIUM_*`, is a violation already forbidden — this step proves zero across the tree.
- [ ] **M34-7b — encoder coverage.** Every `x86("…")` mnemonic used by a template must have BOTH a binary encoder (in `x86_asm.h`) and a text form, so neither medium silently drops an instruction. Enumerate mnemonics; assert both forms exist.
- [ ] **M34-7c — ⭐ NEW (2026-08-12b): audit the medium-conditional guards, starting with one that is provably vacuous.** `codegen_flat_chain_body`'s group-root pass reads `if (g_emit_cfg && (!g_is_text || entry == g_emit_cfg->entry))`. Both halves are defective: (i) `!g_is_text` makes the whole guard a no-op in mode-3, so the pass fires there unconditionally — that asymmetry is what let 5a corrupt mode-3 silently; (ii) `entry == g_emit_cfg->entry` was evidently meant to mean "only for main's own top-level chain", but it is TRUE for ANY graph whose entry parameter equals its own `g->entry` — which includes every single-node stub graph (`sno_build_call_stub`'s) and every proc chain. **The 5a fix (`dbd85305`) routes AROUND this guard rather than repairing it**, deliberately, because deciding what it was FOR is a separate question from stopping the double-emission. Someone should make that decision: either give it a predicate that actually names "main's own chain", or delete it as vacuous. Until then it is a live trap — it reads like a restriction and restricts nothing. Same audit applies to every other `g_is_text` / `MEDIUM_*` conditional outside the `x86()` leaf: by this rung's own mandate the ONLY permitted medium difference is leaf materialization, so a medium-conditional guard on a WALK is a defect by construction.

---

## GATE PHILOSOPHY (applies to every step)
1. **Code parity before output parity.** A step is not done because outputs match — it is done when the mode-3 and mode-4 instruction streams correspond for the targeted shape AND outputs match.
2. **Both modes, every gate.** No gate may run a single mode. `m3==m4==ref` or FAIL.
3. **Oracle-grounded.** `x64` SPITBOL (`/home/claude/x64/bin/sbl -b`, token-free) is the semantic oracle for SNOBOL4/Snocone. Re-ground perf claims against it; do not assert speedups from SCRIP-vs-SCRIP alone.
4. **No regression.** Crosscheck fail-SET stays byte-identical to pristine HEAD across the whole rung (currently PASS=171 FAIL=84 SKIP=6).

## Prereq reads (BB-CODEGEN design set — NON-NEGOTIABLE for any step touching emission)
⛔ **CORRECTED 2026-08-12: `src/emitter/bb_regs.h` DOES NOT EXIST** (confirmed by `find`; `REGISTER-LAYOUT.md` carries its own correction banner saying the same, and `emit.h`'s header comment says it "was dead and dropped"). The register contract of record is **`ARCH-ICON.md` §"REGISTER CONTRACT"** (2026-07-18, explicitly billed as register truth for ALL BB codegen, all languages), backed by the live `src/templates/x86_asm.h`. Note its content contradicts the older `rbx=GVA base` claim repeated elsewhere in this file: **r13=Σ subject base · r14=δ cursor · r15=Δ length · r12=DCAP/CAS top (NOT the ζ frame) · rbx=WS/GC bump-frontier top · GVA globals address absolutely via `ABSQ(RT_GVA_VA + k*16)`, no register base.** `REGISTER-LAYOUT.md` is stale on the r12 row by its own admission — read it for the SPITBOL/CSNOBOL4 oracle maps only.
Also: `ARCH-x86.md` §"Boxes are stackless" + §"Flat-BB ABI"; `src/emitter/XA_templates/xa_flat.cpp` (glob preamble); `ARCH-SCRIP.md`. Plus, for the driver work: `src/driver/scrip.c` mode_compile_x86 + mode_run blocks side by side. Plus, for the driver work: `src/driver/scrip.c` mode_compile_x86 + mode_run blocks side by side.

## Build / probe
⛔ **CORRECTED 2026-08-12** — the old line here said `apt-get install -y libgc-dev` and linked `-lgc`. **Both are dead:** Boehm GC was deleted from the tree by GC-U-4 (s67); `scripts/install_system_packages.sh` is the ONE authority for box packages and does not install it. Run that script, not hand-rolled apt.
```bash
bash scripts/install_system_packages.sh && make -j4 scrip && make libscrip_rt
```
Tri-probe a program in BOTH modes (⛔ **`-no-pie` is MANDATORY** — this container's gcc defaults to PIE and SCRIP's `.o` carries `R_X86_64_32S` relocations that are illegal in a PIE link; omitting it produces a confusing link failure, which is exactly the bug that silently broke the parity harness until `94d283c`):
`./scrip --run p.sno` vs `./scrip --compile p.sno > p.s; gcc -no-pie p.s -Lout -lscrip_rt -lm -Wl,-rpath,$PWD/out -o p.bin; ./p.bin` vs oracle `/home/claude/x64/bin/sbl -b p.sno`.
