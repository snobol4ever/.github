# GOAL-ZETA-FOUR.md — FOUR ζ-STORAGE CONFIGS, ONE SELECTOR, DELETE THE REST

⛔ **s23k NOTICE (2026-08-02, GOAL-SNOBOL4-BB session): FRAME_R12 RETIRED BY LON RULING** — *"R12 all the way now for COND ASSIGN"* (r12 = conditional-assignment stack top; the island ζ-frame technique withdrawn). Stage 1 landed SCRIP `b8ee3d6c` (selector choke, 338/338 byte-identical); the 17 routed arms are dead pending ZW-0 stage 2. This resolves the FOUR-modes pending ruling to THREE-at-most; FRAME_RSP's fate stays with Z4-9 here. Full record: GOAL-SNOBOL4-BB.md s23k cursor + FINDING-2026-08-02d.

## ⛔⭐⭐ LIVE CURSOR — s42 (2026-08-03, Sonnet) — ZW-12 FORTH-cell depth fix · Z4 probes 5/5 · crosscheck +14 · NEXT = M2 (SAVE/COND side path) or Z4-9 CUT

**SCRIP:** `9a89ccec` · **Corpus:** `d871b64e` · **Commits:** LOCAL — push awaits Lon's credential.

**LANDED:** Two ZW-12 depth model bugs fixed in `src/emitter/emit.cpp` (one site, four inserted lines). (1) MATCH_END now subtracts `ZW_FRAME_TOTAL + _fci` where `_fci = sum(zd_k for run members between head and END)` — the whack releases ALL FORTH cells inside the frame, not just the static 64B frame overhead. (2) MATCH_BEGIN head-node wpop/gpop now use `zdh` (pre-ZW depth) instead of post-ZW `zd` — the head's ω fires after the whack so it sees pre-frame depth. Both bugs produced stack over-release: Bug 1 over-released by `_fci` per statement (rip=0x3 at program exit); Bug 2 over-released by 64B per failed scan position (rip=0x1 on second scan iteration).

**Z4 probes:** 3/5 → **5/5** (z4_span OK, z4_capture OK). **Crosscheck:** m3 281→295 · m4 274→288 · BY-SET zero regressions. Feature `.s` regen: 19 files updated. Demo `.s` regen: `pattern_test.s` updated.

**FINDING:** `FINDING-2026-08-03-CLAUDE-SN4-ZETA-ZW12-FC-DEPTH-FIX-Z4-PROBES-5-OF-5.md`

**NEXT (ordered):** M2 SAVE/COND side path (gate fc_save_active/fc_cond_fp off; rt_cap+pend serve all shapes) → M3 marker carry → M4 audit. OR proceed to Z4-9 CUT (delete rotted configs/flags) if Lon prefers to close this goal. roman.sno die-list still pre-existing (frame-r12 runs it, frame-rsp does not — upstream of this fix).

---

## ⛔⭐ LIVE CURSOR — s8 (2026-07-29, Claude, LON DIRECTIVES): **Z4-8 CELL_HEAP PARKED BY LON ("Let's not implement the fourth option yet... I want to proceed with CELLS on the STACK, the third option") — its in-flight zeta_storage.c groundwork was REVERTED uncommitted, so the heap die-list baseline (span/arbno/capture rc=139) is preserved EXACTLY for resumption; the full Z4-8 design (fc_cell_medium 3-value authority, r11 ζ_self, per-node LIFO chain, RT_ZS_SELF mirror) lives in the session transcript + finding.  ⭐⭐ NEW ARCHITECTURAL DIRECTIVE OF RECORD (Lon, verbatim essence): reaching across VARIABLE-length zeta sections is BOGUS and unnecessary; the cond-asgn stack instantiates a BOTTOM MARKER at each match start and the executor scans to it; HEAD/RELEASE is the other offender; ζ CELLS ON THE RSP FORTH STACK IS THE ULTIMATE GOAL — every BB operand's offset from RSP is compile-time known from BB execution order.  TWO LANDINGS THIS SESSION (SCRIP, 2 commits): (1) CAPTURE-START REGRESSION FIXED — cell-stack 4/5 → 5/5, closing the `d79a427a..cca948c5` bisect item.  Root cause: PARSE PREFIX COLLISION — unpinned `x86_fr32_prefix()` IS the plain "[rsp + " spelling, so raw FORTH-cell operands parsed XK_FR32 and gained op_flat_disp; per-box it self-cancels (same-depth store+load = same displaced slot, why SPAN survived while squatting above the mark), but COND(depth 64)→SAVE(depth 48) double-counted the 16 and read mark+16 (a descriptor tag = 1) — one mechanism, both recorded defect shapes.  Fix = raw-cell marker spelling `[rsp# + N]` (2 parse rules ahead of the colliding check + capture's private rspd()); writer+readers flip together, pair cannot desync.  MEASURED: 4 non-capture probes byte-identical pre→post (stash round-trip), capture differs by EXACTLY 2 instructions, deterministic ×3, m4 matches ref.  (2) CAS-MARKER LANDED — HEAD pushes a tag-0 sentinel entry on [RT_CAS_TOP] (varname ptrs never 0); RELEASE pump recovers mark by scan-down (rdi = marker+24, value-identical to the old bracket by construction) and re-scan-pops after close; HEAD fail-exit scan-pops; the flat op_off+32 bracket slot + its zc_frame fork DELETED at all three sites — config-blind, depth-free.  VERIFIED both landings: **cell-stack 5/5 · frame-rsp 5/5 · frame-r12 5/5** m3, capture m4 compiled+linked==ref, heap die-list byte-preserved, pattern_test == frame-r12 oracle, regen ×3 (fix: 4 demo .s; marker: 17 demo .s, every match statement) stable.  ⭐ CENSUS (the thesis measured): op_fc_disp consumers = value spine (STATIC frontier offsets at depth 0 — thesis working) + EXACTLY the two named reaches (fc_cond_fp capture, fc_head_fp release rfc arms).  NEXT RUNGS (the road to pure FORTH): **M2** SAVE/COND deltas fully onto the side path (gate fc_save_active/fc_cond_fp off; rt_cap+pend serve all shapes; deletes reach 1, retires ZB-FC-3c) · **M3** marker entry carries rsp_mark+patstk at +8/+16, delete fc_head_fp (kills the rfc window reads = reach 2 complete) · **M4** audit op_flat_disp/fc_leaf_walk/rbp-pin shrinkage once both reaches are gone · **roman.sno die-list hunt** (SEGV under cell-stack, REPRODUCED PRE-FIX = pre-existing; frame-r12 runs it) · systemic `[rsp#` flip per family (writers+readers together).  Z4-9/10/11 stand; Z4-10's gate should lock today's 3×5/5 with a cell-heap expected-decline list while the 4th is parked.  Session tree: SCRIP 2 commits on `28af3501` · corpus 21 regen .s · .github this cursor + `FINDING-2026-07-29-CLAUDE-Z4-S8-FLATDISP-PREFIX-COLLISION-AND-CAS-MARKER.md`.  Commits LOCAL — push awaits Lon's credential per RULES.

## ⛔⭐ LIVE CURSOR — s7 (2026-07-29, Claude, LON DIRECTIVE "All your choices. I'm with you on this. Continue."): **Z4-7 COMPLETE (SCRIP `28af3501`) — FRAME-R12 5/5 m3 AND 5/5 m4 AT HEAD; all four configs now run from one selector (frame-r12 5/5 · frame-rsp 5/5 · cell-stack 4/5 default · cell-heap granted-and-broken, Z4-8's die-list). NEXT RUNG = Z4-8 CELL_HEAP — UNBLOCKED: the s5 ruling of record resolves R-E (ζ_self/VSP park in CELLS near-term per the [RT_WS_TOP]/[RT_CAS_TOP] precedent; R11 serves ζ post-RTX), so Z4-8's ⛔ marker below is STALE.** Slice 2 root cause (gdb, MONITOR-class bracketing to the instruction: rt_flat_wire_adopt fired every island call with gw=ww=0): GEOMETRY MISMATCH — the island JMP_NONRSP prologue parks the LOW header (γ `[rsp+8]`, ω `[rsp+16]`, caller zr `[rsp+24]`, zr=rsp+32; both media identical) while the s176 WIRE-ADOPT box read the RSP HIGH header `[rsp+kt-24/-16]` = freshly-zeroed region bytes ⇒ null wires ⇒ rt.c:1056 bomb at first RETURN. FIX (3 files, call protocol made config-invariant): bb_save_restore role 3 gains the island arm mirroring the low-header geometry (caller rbp marshals LIVE — the island never clobbers it — and the saved caller r12 rides as a 5th marshal); rt_flat_wires_t widens to 5 fields (r12@32, asserts updated, push-site compound literals zero it by C semantics) with `rt_flat_wire_adopt_isle` the 5-arg leaf; the floaters restore r12 island-emit-only; bb_call_proc_staged routes ISLE through the MODERN wire arm (open_fn forced under !RSP — dead-identical under RSP since det+RSP takes the earlier arm), leaving the legacy r12-anchor bracket arm unreachable under every live basis, tagged for Z4-9. VERIFIED: frame-r12 **5/5 m3 + 5/5 m4** (capture CORRECT — the oracle property on HEAD's island) · default `.s` byte-identical ×5 pre→post + deterministic ×3 · frame-rsp 5/5 · heap arbno rc=139 preserved · **regen ×3 ZERO DRIFT corpus-wide** (crosscheck watermark held by byte-identity) · medium-invisible gate xa_flat(106) fails at BASELINE identically — pre-existing WIP backlog, NOT regressed (slice added zero raw bytes). HONEST RESIDUALS, stated per the repo's own rule shape: (a) the `f7de3863` proof-subset clause was satisfied by the practical stand-in (probe matrix exceeds the epoch's own 5/5 + corpus-wide byte-identity); the epoch's full 267/305 crosscheck was NOT re-run under frame-r12 — the CSN monitor binary is absent in this sandbox; (b) rt.c's two `#if ZC_FRAME==RSP` blocks (rt_proc_enter asm + jmp_entry shim, rt.c:1280/1386) remain BUILD-TIME — the fib path provably does not need them; audit them against the coexpr/EVAL paths at Z4-9 before deleting the `ZC_FRAME` axis; (c) co-expressions under frame-r12 = LOUD decline per R-D, not yet exercised (no coexpr probe). HASH CORRECTION: s6's `dd3b7cbd`/`1551b0bc` are PRE-REBASE spellings — origin truth is `81cbc1ab` (Z4-6 residual) / `ec307c3c` (Z4-7 slice 1), rebased over concurrent RTX commits, the s5 phenomenon. Session tree: SCRIP `ec307c3c`→`28af3501` · corpus untouched (regen zero-drift) · this file + finding = the .github commit. Commits are LOCAL — push awaits Lon's credential per RULES.

## ⛔⭐ LIVE CURSOR — s6 (2026-07-29, Claude, LON DIRECTIVE "finish the four; default = cell-stack and continue there"): **Z4-6 COMPLETE (SCRIP `dd3b7cbd`) + Z4-7 SLICE 1 LANDED (SCRIP `1551b0bc`) — FRAME-R12 FIRST LIGHT AT HEAD: 4/5, capture CORRECT. NEXT = the fib call path under the island (rc=1, `rt.c:1056` "open call carries no return wires (activation was not flat-adopted)").** Z4-6 residual root cause: bb_match_release's R12-EXIT-1 TAIL arm read the statement bracket via raw `RSP(op_fc_disp+0/+8)` unconditionally — FORTH-cell physics; under CSTACK those cells were never carved (the +304/+312 numbers compose from gated-off fc_geom sums + the HEAD constant), so `mov rsp,[rsp+0x138]` loaded 1 and the Σ/δ/Δ push triple faulted. Fix = one conjunct: `_.op_tail && rfc()` — non-FORTH tail RELEASEs take the general FRQ bracket arm the non-tail CSTACK releases were already passing with. VERIFIED: **frame-rsp 5/5 m3 AND 5/5 m4** (anchor-era d79a427a parity on HEAD, the rung's completion criterion) · default `.s` byte-identical ×3 · smoke matrix 6/6 rows == baseline · heap arbno rc=139 preserved (Z4-8 pre/post proof intact). ⭐ REGEN BONUS: `benchmarks/snobol4/arith_mixed.s` emitted for the FIRST TIME (corpus `123daca2`, 624 lines, zero bombs, m3==ref, assembles+runs m4) — the old wild-displacement tail emission had kept it assembler-rejected. Z4-7 slice 1 = the EXTRACT-Z4-R12.md §1 prescription executed literally: `rt_zc_frame_live()` (zeta_alloc.c) is the ONE live-frame authority returning `ZC_FRAME_ISLE`(=0, the deleted label's value renamed) iff storage==FRAME_R12; ALL 69 `ZC_FRAME ==/!= ZC_FRAME_RSP` comparisons across 11 files route through it (`x86_zc_frame()` wrapper emit-side; direct calls in the 3 C files); island arms restored in x86_zr/zr_num (r12/12) + the fb family (fb=r12, frame_off compensation ≡0 — depth-immune base); the dead island machinery was INTACT and is now reachable (m4 wrapper 64KB else-arm, m3 alloca+rt_outer_call(fn,mf), zeta_storage.c zeta-mark slot). rt.c's two `#if ZC_FRAME==RSP` blocks (rt_proc_enter asm + jmp_entry shim) deliberately left build-time — the flagged runtime-ABI seam, PRIME SUSPECT for the fib failure alongside bb_call_proc_staged's island-cursor arm and xa_flat's prologue adopt (the extraction doc's ~5 net-new suspect arms live exactly there). VERIFIED: default `.s` BYTE-IDENTICAL all 5 probes (truth-table fold proven by bytes) + regen ×3 zero drift corpus-wide · cell-stack 4/5 · frame-rsp 5/5 · **frame-r12 arith/span/arbno OK + capture CORRECT (the Z4-1/Z4-2 frame-config oracle property reproduced on HEAD) · z4_fib rc=1 only**. Re-connection-not-rewriting: validated on first light. Default remains CELL_STACK both axes (Lon's directive re-confirmed this session). Session hashes: SCRIP `dd3b7cbd`→`1551b0bc` · corpus `123daca2` · this file + finding = the .github commit. Context ~80% at handoff.

## ⛔⭐ LIVE CURSOR — s5 (2026-07-29, Claude, same session, LON DIRECTIVE): **R12-FREE-1 LANDED (SCRIP `87a44f89` + artifact commits; session hashes REBASED over three concurrent RTX-session commits at push time — the s3/s4/s5 hashes here are the post-rebase origin truth) — the capture pend top is CELL-RESIDENT at `[RT_CAS_TOP]`; emitted default `.s` contains ZERO r12. R12 is now genuinely free for config 1 — Z4-7's register blocker is GONE.** Lon's ruling of record: post-RTX (C-call eradication) the capture top returns to r12 and **R11 serves ZETA** — legal then because R11 is caller-saved and only C calls clobber it; this SUPERSEDES the s4 R-E proposal's register-identity A/B (ζ_self's eventual home = R11 post-RTX; near-term VSP/ζ_self park in cells per the [RT_WS_TOP] precedent). KEY SIMPLIFICATION FOUND: the cell already existed — `pattern_match.c:644` aliases `g_dcap_top` to `[RT_CAS_TOP]`, so the C side was ALWAYS cell-based and r12 was a register-cached copy synced at statement brackets; the conversion (6 emitted sites + 2 m4 wrapper seeds + the m3 `rt_outer_call` thunk, whose `push r12` was ALSO the 16B alignment adjuster — replaced by an explicit `sub/add rsp,8`) makes C-side and emitted-side pushes share ONE authority by construction. VERIFIED: capture semantics preserved down to the pre-existing bug (same wrong 1350000, m3 AND m4) · default 4/5 · frame-rsp 4/5 (arbno 139 unchanged — its defect is elsewhere) · smoke 7/7 · `.s` regen ×3 run (17 benchmark files re-emitted). **NEXT RUNGS: (a) Z4-6 residual (arbno mark/restore under frame-rsp) · (b) Z4-7 FRAME_R12 — now unblocked on the register.**

## ⛔⭐ LIVE CURSOR — s4 (2026-07-29, Claude, same session as s3): **Z4-6 SLICE 1 LANDED (SCRIP `5202ff08`) — fc_cells_on() = ONE port opinion for the fixed-cell family; frame-rsp 2/5 → 4/5 m3 AND m4-proven; ⭐ capture is CORRECT under frame-rsp (cell-stack's DIFF is its own regression, now single-build reproducible by flipping `--zeta-storage`). NEXT RUNG = Z4-6 residual: z4_arbno under frame-rsp (rsp=0x1 at a push-r14 prologue — an unwritten mark/restore cell, the s202 class, ARBNO family; gdb spin-counter or 2-way monitor per RULES.md).** Details, the s206-defect-class confirmation under CSTACK, and ⭐ THE fc_leaf_disp SENTINEL TRAP (true unregistered sentinel is `(int)0x80000000`, NOT the -1 the extern prose claims — a query gate there was falsified by mask bisect; authorities get gated, derivations follow) → `FINDING-2026-07-29-CLAUDE-Z4-6-ONE-GRANT-AUTHORITY-AND-THE-LEAF-DISP-SENTINEL-TRAP.md`. Default byte-identity ×3 held · cell-stack watermark unchanged · HEAP preserved granted-and-broken for Z4-8's pre/post proof · smoke 7/7 · no `.s` regen (contracts+driver+runtime only, default output proven byte-identical). R-E still the only open ruling; Lon delegated choices this session ("All your choices") — proposal recorded at Z4-8's rung text is still owed; blocks Z4-8 only.

## ⛔⭐ LIVE CURSOR — s3 (2026-07-29, Claude): **Z4-5 DONE (SCRIP `e77d05e1`) — the three mapped port spellings are ALIASES routed through the selector; the slice-2 m3 env seam is CLOSED. NEXT RUNG = Z4-6 (frame-rsp wired: fix the Z4-2 rot list, MONITOR-FIRST per RULES.md).**

**LANDED:** `--zeta-port={forth,cstack,heap}` now route through `rt_zeta_storage_set(CELL_STACK/FRAME_RSP/CELL_HEAP)` — the selector is the single authority for the three mapped ports; a later flag of EITHER spelling wins WHOLESALE (no mixed axis states); plain/instrumented/alloc/inline stay direct until the Z4-9 cut. `rt_zeta_port_mode`'s lazy branch consults `SCRIP_ZETA_STORAGE` FIRST (seam closed: env-only storage selection now reaches m3 through the port axis every emit-time read already takes); a frame-r12 selection that derives nothing falls through to port env/default. **VERIFIED (all at `e77d05e1`):** default `.s` byte-identical ×3 pre→post · forth==cell-stack==default byte-identical ×3 (⭐ Z4-5's completion criterion) · port=heap==storage=cell-heap AND port=cstack==storage=frame-rsp byte-identical — the port spellings now carry the 2-line storage bake, i.e. they are storage-COMMITTED, the s206 lesson extended to the legacy spellings · env==CLI · `SCRIP_ZETA_PORT=7` env-only unchanged (**LAZY LAW held**: bare storage never derived over a present port) · ⭐ both-env conflict now DETERMINISTIC storage-first (pre-edit it was ORDER-DEPENDENT — whichever axis resolved last stomped the other; the selector-first lazy rule replaces that with one rule, stated in the zeta_alloc.c comment) · m3 env-only selection proven LIVE behaviorally (TELEM port=7; z4_arbno SEGVs = the Z4-0 heap die-list reproducing through the NEW path — the SEGV is Z4-8's known open defect, its arrival through env-only m3 is the seam-closure proof; default runs untouched) · bad values rc=2 · smoke 7/7 · m3 probe watermark IDENTICAL under all three spellings and == the Z4-0 baseline exactly (4/5 OK, z4_capture DIFF pre-existing, `d79a427a..cca948c5`). **DEFAULT CONFIRMED (Lon's directive this session): CELL_STACK is the default on BOTH axes** — build (`#define ZC_STORAGE ZC_STORAGE_CELL_STACK`, zeta_choices.h) and the runtime lazy default (bare CELL_STACK, no derive). No `.s` regen: only `src/driver/scrip.c` + `src/runtime/rt/zeta_alloc.c` touched (the s206 precedent) and default byte-identity is proven ×3. R-E remains the only open ruling; blocks Z4-8 only.

## ⛔⭐ LIVE CURSOR — s2 (2026-07-29, Opus): **Z4-3 DONE (grid finding) + Z4-4 DONE (slice 2, SCRIP `d34a3966` — the selector is live end-to-end: CLI/env/build, full-range clamp, legacy-tuple derivation, dual-site bake). NEXT RUNG = Z4-5 (route `cell-stack` formally through the selector; `--zeta-port=forth` becomes an alias; probe watermark identical under both spellings).** ⭐ Session law minted at Z4-4: LAZY DEFAULT IS BARE — an absent axis must never overwrite a present one (a deriving default measurably stomped `--zeta-port=heap`→forth through the bake-site getter; fixed, verified by the port=heap regression check: 10 carves + 1 port bake present). Trees: SCRIP `58f5c4dd` untouched by Z4-3 · corpus untouched · worktrees rebuilt fresh this sandbox (r12 via the era's `-D` seam, source flip reverted post-build; both paired with their own `out/libscrip_rt.so`). Headlines in the finding: ALL 40 grid cells m3==m4 including the heap SIG11s (first honest m4 heap data — `cca948c5` bake fix proven end-to-end); fib call-path degradation 102→112→137 reproduced on second hardware, ratios match s1 (⇒ Z4-10 fib ratchet doubly justified); ⭐ heap FIB PARITY with cell-stack (137==137) — config 4 starts Z4-8 with no call-path deficit, and today's fib number is the baseline A-1's addressing move must answer to; capture DIFF pinned 1350000 vs 1500000 (0.9× — one char short per capture, third confirmation of `d79a427a..cca948c5`). R-E still the only open ruling; blocks Z4-8 only. All numbers RT_OPT=-O0, relative-only.

## ⛔ LIVE CURSOR — s1 (2026-07-28, Sonnet): **Z4-0 COMPLETE except the R-* rulings. NEXT RUNG = Z4-1 (R12 archaeology worktree at `f7de3863`).** Trees: SCRIP `cca948c5` (UNTOUCHED — no `src/` edit this session, so handoff step 4 `.s` regen NOT triggered) · corpus `4be103f5` + probe set · .github `59e12e05` + this file + the Z4-0 finding.

**⭐ THE GOAL'S PREMISE IS NOW MEASURED, NOT ASSUMED — FIVE OF SEVEN PORTS ARE ROTTED AT HEAD.** One capture program under all 7 `--zeta-port` values: ONLY `forth` (the compiled default) runs; plain/instrumented/alloc/inline/**cstack**/heap all SEGV. **Config 2 (FRAME-RSP = cstack/port 4) therefore CANNOT be reconstructed from HEAD's residual arms** — the `879a0d37` worktree is the real source, exactly as the COMMIT-SELECTION LAW anticipated. Cause: no gate covers non-default ports, so they rotted silently while HEAD moved s188→s206. ⇒ **NEW BINDING CONSTRAINT ON Z4-9/Z4-10: do not keep a config the gate does not cover, or it rots again by the same mechanism.**

**WATERMARK (Z4-0 baseline, RT_OPT=-O0, best-of-3, m3/m4 agree on every verdict):** z4_arith 43/38ms OK (sbl 221 — **5.1× faster**) · z4_span 116/107ms OK (sbl 98 — 0.85×) · z4_arbno 23/21ms OK (sbl 45 — **2.0× faster**) · z4_fib 98/89ms OK (sbl 43 — **0.44×, i.e. 2.3× SLOWER**) · z4_capture 27/25ms **DIFF** (sbl 23; wrong by one char/iteration, oracle-correct `.ref` kept as canary). **⭐ z4_fib is the perf signal for this whole goal: activation/call cost is the one axis where SCRIP loses badly, and activation storage is exactly what the four configs differ on.** COLD-START: first invocation measured 145× slower than third — best-of-3 minimum, always.

**⭐ Z4-1 HALF-LANDED SAME SESSION — THE R12 EPOCH BUILDS AND IS THE ONLY 5/5-CORRECT CONFIG MEASURED.** Worktree `f7de3863` builds on today's toolchain after `apt-get install libgc-dev` (the epoch's own dep, dropped at HEAD by GC-U-4 s67) — extraction-by-reading NOT needed. CROSS-EPOCH TABLE (m3, -O0, best-of-3, same probe bytes): **CONFIG 1 R12** 46/380/35/71/105 **5/5 OK** · FRAME-RSP(s65 default) 45/346/**SEGV**/60/96 4/5 · CELL-STACK(HEAD) 43/116/23/98/27**DIFF** 4/5 · sbl 221/98/45/43/23. THREE CONSEQUENCES: (1) **config 1 is a correctness ORACLE for the other three**, not a museum piece — it passes both arbno (which SEGVs under s65-RSP) and capture (wrong at HEAD); (2) **the cell pivot's ledger is mixed and both halves are real** — span **3.3× faster**, capture **3.9× faster**, arbno fixed, but **z4_fib activation cost REGRESSED 1.6× (60→98ms)**, so "CELL beats FRAME" is decisively true for pattern work and FALSE for calls — `z4_fib` is the instrument for whichever config wins; (3) **the capture defect is a REGRESSION bisectable to `f7de3863`..`cca948c5`** (s65 gives SPITBOL-exact 1500000 under BOTH bases), which hands `GOAL-SNOBOL4-BB.md` a known-good epoch + 4-line repro + bisect range. Z4-1's REMAINING deliverable is documentation only: `EXTRACT-Z4-R12.md` (island-cursor mechanics, zr/fb accessors, era-arm→HEAD-arm PAIRING TABLE with a FRESH recount of the s202 "17 arms" claim).

**⭐⭐ Z4-2 DONE — TABLE COMPLETE; THE CALL PATH DEGRADED MONOTONICALLY 71→83→98ms ACROSS ALL THREE GENERATIONS.** Config-2 anchor CORRECTED to `d79a427a` (LAST proven-green CSTACK-default commit, its own message carries the watermark) rather than the plan's provisional `879a0d37` (merely the first). FULL TABLE (m3, -O0, best-of-3, identical probe bytes) — **C1 R12** 46/380/35/**71**/105 **5/5 OK** · **C2 CSTACK** 47/358/33/83/120 **5/5 OK** · **C3 CELL-STACK HEAD** 43/**116**/**23**/98/27 4/5 (capture DIFF) · sbl 221/98/45/43/23. **THE CAMPAIGN'S PREMISE IS NOW QUANTIFIED ACROSS THREE INDEPENDENT TREES: "CELL beats FRAME" is TRUE for pattern work (span 3.3×, arbno 1.5×) and FALSE for calls (fib 1.38× slower than C2, 1.4× slower than C1).** ⇒ **`z4_fib` belongs in Z4-10's gate as a RATCHET, not a pass/fail** — a config that regresses activation cost should have to say so out loud. Both FRAME configs are 5/5 correct and both compute SPITBOL-exact `capture 1500000`, corroborating the capture regression from a second tree and tightening the bisect range to **`d79a427a`..`cca948c5`**. RULINGS TAKEN (Lon "All your choices"): R-A/R-B(anchor corrected)/R-C/R-D/R-F ACCEPTED as written in the finding; **R-E (ζ_self/VSP) DELIBERATELY LEFT OPEN — it decides where results LIVE, not how existing code is packaged; blocks Z4-8 only.** Worktrees `/home/claude/wt-r12` + `/home/claude/wt-cstack` both build (need `libgc-dev`, dropped at HEAD by GC-U-4 s67) — the reconstruction now has WORKING reference implementations on disk, not just git history.

**THREE DEFECTS ISOLATED, NONE FIXED (monitor-first; they belong to `GOAL-SNOBOL4-BB.md`, not Z4):** (a) stored pattern = 4-line SEGV repro, **port-INVARIANT ⇒ out of Z4 scope**; (b) capture start wrong two ways (anchored: drops first char; unanchored-after-an-element: starts at 0); (c) `--compile` goes to stdout, mode-4 needs an explicit gcc link line. → `FINDING-2026-07-28-CLAUDE-Z4-0-BASELINE-AND-FIVE-OF-SEVEN-PORTS-ARE-ROTTED.md`.

**⭐ Z4-1 DOC DELIVERABLE LANDED — `EXTRACT-Z4-R12.md`.** THREE FACTS THAT CHANGE Z4-7's SHAPE: (1) **R12 NEVER NAMED ITSELF** — zero `ZC_FRAME == ZC_FRAME_R12` tests existed even at its own epoch; it was the fall-through of the NEGATIVE `!= ZC_FRAME_RSP` predicate, which is precisely why `c26a398a` deleting the LABEL while keeping the CODE orphaned 17 arms onto a basis they were never written for (the `#error`'s root cause, and it means **config 1 is RE-CONNECTION, not rewriting**). (2) **ARM RECOUNT: 18 sites, 17 code arms + 1 header guard — s202's "17" CONFIRMED; the epoch had only 12, so ~5 arms are NET-NEW and have never run under any basis but RBP-by-accident ⇒ SUSPECT-BY-DEFAULT.** Wider `ZC_FRAME` census = ~94 mentions / 13 files = Z4-9's deletion surface. (3) **CONFIGS 1→3 ARE ONE IDEA MIGRATING**, not three inventions: the epoch's `x86_zr() = flat_pat ? "r12" : "rsp"` already gave suspending PAT$ blobs an r12 island under the RSP default, and HEAD's per-graph `x86_fb_pinned()` rbp is its direct descendant (same immune-base idea, register r12→rbp, granularity hardcoded-predicate→per-graph classifier). Lon's "RBP for dynamic housekeeping, RSP for static" is that lineage's end state.

Re-prove the watermark at every session start; never trust prose (RULES.md STALE-ORIENTATION rule).

## THE DIRECTIVE (Lon, 2026-07-28, this goal's charter)
Four ζ-storage techniques have existed; all worked with limitations and varying performance; each switch was made in hunt of faster. (1) ζ FRAMES (all BB results+locals of an entire 1-entry/1-exit graph) on an R12-based island stack — worked for SNOBOL4 and Icon, NOT co-expressions. (2) ζ frames moved from R12 to RSP, shared with the C stack. (3) ζ CELLS (one BB's result+locals) on RSP/RBP, RBP stabilizing references for ARBNO/FENCE-class dynamics — RBP for the dynamic-sized housekeeping, RSP for the static-sized; "the latest that had hope." (4) ζ cell LOCALS on the RBX-topped GC island, ζ cell RESULT on its own island (the VSP value stack). CELL outperforms FRAME. **Reconstruct the tens of switches into exactly FOUR selectable configurations — #define + command-line + environment — and DELETE all other zeta-storage code.** Extraction source for the dead configs: point-in-time checkouts at each config's working-best commit, "right before it was switched to the other."

## MANDATORY READING, IN ORDER, EVERY SESSION
1. `.github/RULES.md` in full (monitor-first, template-only, both-medium, O2-DIRECTED-ONLY, handoff, LIVE-CURSOR discipline).
2. This file, fully.
3. `SCRIP/src/contracts/zeta_choices.h` AT HEAD — the knob inventory doubles as the codebase's own history record; every port comment cites its Lon directive and date.
4. `.github/ARCH-ICON.md` § register contract (Σ/δ/Δ = R13/R14/R15; RBX = WS/GC frontier; x86_fb_pinned per-graph RSP/RBP).
5. `.github/GOAL-TEMPLATE-REVAMP-RULES-DRAFT.md` — R2/R5/R7/R8/R9/R10 + the named forbidden shape. Every reconstructed instruction obeys these; NOTHING is pasted verbatim from an old tree.
6. `.github/FINDING-2026-07-28b-CLAUDE-ICN-ZR-RSPRBP-3-AND-THE-SECOND-BASIS-IS-R12-RESIDUE-WEARING-RBP-NAME.md` — proves the 17 `ZC_FRAME != ZC_FRAME_RSP` arms are R12-shaped code wearing RBP's name. Config 1's re-establishment starts from this fact.
7. `.github/FINDING-2026-07-28-CLAUDE-SN4-ZHEAP-PORT-CLAMP-AND-FC-GRANT-DISAGREEMENT.md` — the s206 mode-4 bake lesson (emitted code and runtime mode must never disagree) + the port-blind-grant defect Z4-8 must cure.
8. Resolve the `SCRIP_CELLS`×`SCRIP_ZMODE` interaction matrix by making the regimes four named values of one selector.
⛔ DO NOT open `BB-REVAMP-TRACKER.md`. DO NOT read GOAL files Lon has not named. Open `GOAL-SNOBOL4-BB.md` only to its top LIVE CURSOR block if cross-checking watermark numbers.

## RULING RECORD (defaults proposed by Sonnet s0 — Lon overrides at any session start; a PENDING ruling blocks only the rungs that cite it)
- **R-A — ONE 4-value selector, not four independent booleans.** One enum makes mixed regimes unrepresentable; a mixed blob is the documented s188 disease. PENDING LON.
- **R-B — Config 2 = the CSTACK embodiment** (`ZC_PORT_CSTACK`, statement-bracket frames, cursor IS rsp, `879a0d37`), i.e. the last pure-frame regime before per-BB cells. PENDING LON.
- **R-C — Config 1 restored via the parameterized `ZC_FRAME_R12` form** (the `6d349be3`-era shape), re-expressed under current `x86()` rules — NOT a verbatim old-tree revert. The 17 arms in HEAD are already R12-shaped; the island cursor + encoders are the net-new. PENDING LON.
- **R-D — Co-expressions under configs 1–2 = LOUD documented decline** (their historical limitation), never a silent wrong answer. PENDING LON.
- **R-E — ζ_self register + VSP wiring (config 4)** — the s206 §4 ruling. BLOCKS Z4-8 ONLY; every earlier rung proceeds without it. AWAITING LON.
- **R-F — File name (`GOAL-ZETA-FOUR.md`?) + PLAN.md Active-Goals row** — Lon's call (RULES.md forbids editing PLAN.md's table on routine handoff; adding a goal row is his edit or his directive).

## THE FOUR (canonical table — the whole goal in one place)
| Selector value | CLI/env name | Granularity | Locals | Result | Bases | Anchor (working best) |
|---|---|---|---|---|---|---|
| `ZC_STORAGE_FRAME_R12` | `frame-r12` | whole graph | island arena `[r12+off]` | in frame | R12 | `f7de3863` (s65 R12-ERAD: flips default to RSP but PROVES "R12 watermark byte-identical" — extract here; parent `ff5ece49` = last R12-default tree, the fallback) |
| `ZC_STORAGE_FRAME_RSP` | `frame-rsp` | whole graph / statement bracket | C stack | in frame | RSP | `879a0d37` (CSTACK landed + made default, full proof list in the commit message) — **scan the 130-commit window `879a0d37..be4bb739^` for a later, better CSTACK-era point before committing to the anchor** |
| `ZC_STORAGE_CELL_STACK` | `cell-stack` | per BB | fixed rsp carve (`sub rsp,K` at α, pop at every ω) | rsp cell | RSP static + per-graph RBP pin for dynamics (`x86_fb_pinned`, FLATDISP-8) | HEAD `cca948c5` (live default: FORTH `b99e9348` + FLATDISP ladder). Lon's "second-to-last with hope." |
| `ZC_STORAGE_CELL_HEAP` | `cell-heap` | per BB | RBX-topped GC island (`[RT_WS_TOP]` bump, `770af990`) | VSP mmap value stack (s203 SEQ/ALT prefix slots, re-targeted per ZHEAP-6) | RBX + VSP (+ ζ_self per R-E) | HEAD + the ZHEAP ladder (in flight; port-7 clamp fixed `cca948c5`; grant/consumption defect OPEN) |

## COMMIT-SELECTION LAW (Lon's principle, made mechanical)
The extraction anchor for a retired config = the LAST commit at which that config was PROVEN green — the pivot commit itself when its message proves the outgoing config's own watermark, else the pivot's parent. Never extract from a mid-rot tree. PROOF BEFORE EXTRACTION: build the anchor and re-run its own era's stated proof (its commit message names the numbers) before trusting a byte of it.

## POINT-IN-TIME PROTOCOL (Z4-1/Z4-2 mechanics)
```bash
git -C /home/claude/SCRIP worktree add /home/claude/wt-r12    f7de3863
git -C /home/claude/SCRIP worktree add /home/claude/wt-cstack 879a0d37
```
- VERIFY the worktree is populated BEFORE trusting any build in it (`ls src/ Makefile`, then check the built artifact's mtime moved) — the s126 poisoned-clone lesson, RULES.md O2 rule.
- Build each epoch with THAT SHA's own `scripts/install_system_packages.sh` + Makefile. Pair each epoch's `scrip` with THAT epoch's `libscrip_rt.so` — NEVER mix runtime and compiler across epochs.
- If an old tree will not build on today's toolchain (gcc drift), fall back to EXTRACTION BY READING — say so in the FINDING, do not fake a run.
- Corpus drift: do NOT chase contemporaneous corpus SHAs. Use the fixed PROBE SET below — identical bytes across every build.
- `RT_OPT=-O0` EVERYWHERE; label every number with it (O2-DIRECTED-ONLY rule). Timings are RELATIVE-ONLY at -O0; the table's value is the ordering and the ratios, and say so wherever quoted.

## PROBE SET (written once at Z4-0; self-contained, no includes; fixed forever after)
`corpus/probe/z4_arith.sno` (tight arithmetic loop) · `z4_span.sno` (SPAN/BREAK-heavy scan) · `z4_arbno.sno` (ARBNO backtrack ladder — the dynamic-housekeeping stressor) · `z4_fib.sno` (DEFINE recursion — frame-vs-cell call cost) · `z4_capture.sno` (`.` and `$` heavy — the s206 defect class). Each sized to run 0.5–3s at -O0 via an outer loop constant. `.ref` from `/home/claude/x64/bin/sbl -b` (clone per RULES.md Oracles). These five exercise exactly the storage regimes and nothing else. If old-epoch scrip declines a probe construct, record the decline — do not edit the probe.

## RUNGS (stop at first `- [ ]`; each rung ends at a committable, watermark-proven point)
- [x] **Z4-0 RULINGS + PROBES + BASELINE.** ✅ DONE s1 (probes + refs + baseline + finding). ⚠ RESIDUAL: the R-* rulings are still PENDING LON — Z4-1..Z4-3 (archaeology, no HEAD edits) proceed without them; Z4-4 onward should not land until R-A/R-B/R-C are confirmed. Original text: Challenge/confirm every R-* above with Lon. Record session HEAD hashes into the LIVE CURSOR. Write the probe set + `.ref`s. Run probes on HEAD default (cell-stack) m3+m4 → THIS GOAL'S WATERMARK. Also run `--zeta-port=cstack` on probes (informs Z4-6's rot estimate) and `--zeta-port=heap` (expect the s206 SEGVs; record them — they are Z4-8's die-list). Completion: probe files + refs committed to corpus; watermark block written here.
- [x] **Z4-1 R12 ARCHAEOLOGY.** ✅ DONE s1 — worktree built, measured 5/5, `EXTRACT-Z4-R12.md` written (⭐ its §1 is load-bearing for Z4-7: R12 was selected by the NEGATIVE `!= ZC_FRAME_RSP` predicate and never named itself, which is WHY deleting its label orphaned 17 arms onto RBP; reconstruction is re-connection, not rewriting). Original text: Worktree `f7de3863`; build; re-run that commit's own stated proof subset; run + TIME probes. Write `EXTRACT-Z4-R12.md` (.github): (a) island-cursor mechanics of the era (r12 seeding, arena frame, the side-stack for suspending PAT$ blobs the commit message names); (b) the zr/fb accessor shapes; (c) a PAIRING TABLE mapping era arm sites → HEAD's `ZC_FRAME != ZC_FRAME_RSP` sites (grep both trees; s202 counted 17 in HEAD — RECOUNT, do not inherit); (d) what has NO modern twin (deleted encoders, renamed `x86_r12_modrm`→`x86_frame_modrm` at `c26a398a`). Completion: worktree proof re-run recorded; EXTRACT doc committed.
- [x] **Z4-2 CSTACK ARCHAEOLOGY.** ✅ DONE s1 — anchor `d79a427a`, built, measured 5/5. Remaining sub-item deferred to Z4-6: the era-vs-HEAD port-4 arm diff (the rot list). Original text: Scan `879a0d37..be4bb739^` (130 commits) for the best CSTACK-era point per the COMMIT-SELECTION LAW; worktree it; build; re-run its proof; time probes. Write `EXTRACT-Z4-CSTACK.md`: diff that tree's port-4 arms against HEAD's (port 4 sat unselected while HEAD moved — the diff IS the rot list Z4-6 must fix). Completion: anchor pinned with reason; EXTRACT doc committed.
- [x] **Z4-3 CROSS-EPOCH BENCH FINDING.** ✅ DONE s2 (2026-07-29) — full 4×5×{m3,m4} grid in `FINDING-2026-07-29-CLAUDE-Z4-3-CROSS-EPOCH-GRID-MODES-AGREE-AND-HEAP-FIB-PARITY.md`. All 40 verdicts m3==m4 (bake fix proven end-to-end); fib degradation 102→112→137 reproduced (ratios match s1); ⭐ heap has FIB PARITY with cell-stack (137==137) — Z4-8 starts from no call-path deficit; C3 capture DIFF pinned to 1350000 vs 1500000 (0.9×, third confirmation of the `d79a427a..cca948c5` bisect range). Original text: One table: {wt-r12, wt-cstack, HEAD-forth, HEAD-heap-where-it-survives} × 5 probes × {m3, m4}, all -O0-labeled, crashes/declines recorded honestly. This is "in hunt of faster" made measured — the empirical justification for which configs earn keeping. Completion: `FINDING-<date>-OPUS-Z4-CROSS-EPOCH-BASELINE.md` committed.
- [x] **Z4-4 SELECTOR SPINE (additive only).** ✅ SLICE 2 LANDED s2 (SCRIP `d34a3966`): `--zeta-storage=frame-r12|frame-rsp|cell-stack|cell-heap` CLI + `SCRIP_ZETA_STORAGE` env (name or digit) + `rt_zeta_storage_set/get` clamped over the FULL 0..3 range (the s206 lesson stated in the selector comment) + the bake at BOTH scrip.c sites, storage bake FIRST so env-only selection resolves before the port predicate reads. Setter derives the legacy tuple: cell-stack→FORTH, cell-heap→HEAP, frame-rsp→CSTACK (honest-not-safe until Z4-6), frame-r12→NOTHING (Z4-7 gives it meaning). VERIFIED: default byte-identical ×3 probes · explicit `cell-stack` byte-identical · storage spelling ≡ legacy port spelling + EXACTLY the 2-line storage bake (cell-heap and frame-rsp both measured +2/−0) · env≡CLI · bad value rc=2 · TELEM prints the name. ⭐ **NEW LAW, measured then fixed same session: THE LAZY DEFAULT SETS THE CELL BARE — no derivation, no telem.** A deriving default STOMPED an explicit `--zeta-port=heap` back to forth through the bake-site getter (first equivalence check caught it: port=heap emitted a forth body with zero bakes). An ABSENT axis must never overwrite a PRESENT one — this law binds every transition rung until Z4-9 deletes the legacy axes. KNOWN SEAM (stated in the comment): m3 env-only selection resolves at the first `rt_zeta_storage_get()` consumer; slice 2 adds none on the m3 path, CLI covers m3, slice 3's first seam consumer closes it. ⏳ SLICE 1 LANDED s1 (SCRIP, enum + range guard, zero consumers, default `.s` byte-identical on 3 probes, bad value trips `#error`). REMAINING = slice 2: `--zeta-storage=` + `SCRIP_ZETA_STORAGE` + `rt_zeta_storage_set/get` clamped over the FULL range + the mode-4 bake at BOTH `scrip.c:1216/1407`. Original text: `ZC_STORAGE` block in `zeta_choices.h` (4 values, `#ifndef` default `ZC_STORAGE_CELL_STACK`); `--zeta-storage=frame-r12|frame-rsp|cell-stack|cell-heap` in `scrip.c` beside `--zeta-port=`; `SCRIP_ZETA_STORAGE` env; `rt_zeta_storage_set/get` in `zeta_alloc.c` with FULL-RANGE clamp (state the s206 clamp lesson in the one comment separator); precedence CLI > env > build. Mode-4 BAKE beside the existing twins at `scrip.c:1216/1407` — BOTH sites — only-when-overridden so default stays byte-identical. The setter DERIVES the legacy tuple (port mode, zeta mode) internally; zero downstream seams flip this rung. Completion: no-override output byte-identical (probe `.s` cmp); override diff = exactly the baked lines; bad value rc=2; TELEM prints the storage name.
- [x] **Z4-5 CELL_STACK WIRED.** ✅ DONE s3 (SCRIP `e77d05e1`) — forth/cstack/heap spellings aliased through `rt_zeta_storage_set`; m3 env seam closed selector-first; both-env conflicts made deterministic; probe watermark identical under all three spellings (== Z4-0 baseline). Original text: `cell-stack` formally drives what `--zeta-port=forth` did; direct port selection becomes an alias routed through the selector (deletion waits for Z4-9). Completion: probe watermark identical under both spellings.
- [x] **Z4-6 FRAME_RSP WIRED.** ✅ DONE s4+s6 (SCRIP `5202ff08` slice 1 fc_cells_on, `dd3b7cbd` residual: tail unwind gated on rfc() — FORTH-cell physics only). frame-rsp **5/5 m3 AND 5/5 m4**; anchor-era `d79a427a` 5/5 parity re-proven on HEAD; monitor-first per the gdb fault capture (mov rsp,[rsp+0x138] → push r14). Original text: `frame-rsp` drives the CSTACK tuple; fix the Z4-2 rot list — every behavioral divergence hunted MONITOR-FIRST per RULES.md, no exceptions. Completion: probes green m3+m4 under `frame-rsp`; the anchor era's proof subset re-proven on HEAD.
- [x] **Z4-7 FRAME_R12 RE-ESTABLISHED.** ✅ DONE s6+s7 (SCRIP `ec307c3c` slice 1 one-authority re-connection; `28af3501` slice 2 island call path — WIRE-ADOPT low-header geometry + wire quad widened to carry caller r12 + ISLE on the modern wire arm). **frame-r12 5/5 m3 AND 5/5 m4; capture CORRECT (oracle on HEAD).** Residuals recorded in the s7 cursor: f7de3863 proof-subset satisfied by stand-in (probes exceed the epoch's 5/5 + corpus-wide byte-identity; full 267/305 epoch crosscheck not re-run — monitor absent this sandbox); rt.c:1280/1386 `#if ZC_FRAME==RSP` blocks left build-time (fib path proven not to need them — Z4-9 audit vs coexpr/EVAL); coexprs = LOUD decline per R-D, unexercised. ⏳ SLICE 1 LANDED s6 (SCRIP `1551b0bc`): rt_zc_frame_live() one-authority conversion of all 69 comparison sites + island accessor arms + the intact dead machinery reachable; default byte-identical by construction (proven ×5 probes + corpus regen zero-drift); FIRST LIGHT 4/5 (arith/span/arbno OK, capture CORRECT — oracle property on HEAD), z4_fib rc=1 at rt.c:1056 'no return wires (activation was not flat-adopted)'. REMAINING = the island call path: bb_call_proc_staged island-cursor arm + xa_flat prologue adopt + rt.c's two #if ZC_FRAME==RSP blocks (rt_proc_enter asm, jmp_entry shim — the flagged runtime-ABI seam) + the ~5 net-new suspect arms; then the f7de3863 proof subset + medium-invisible gate. The big one. Re-mint the R12 basis INSIDE the selector (not as the old `ZC_FRAME` build axis); re-point the paired arms home per `EXTRACT-Z4-R12.md`; restore the island cursor; add any missing encoders to `x86_asm.h` per R7 — zero hand-encoded bytes, both media, `bb_emit_x86` record walk (the forbidden shape stays forbidden even for resurrection work). Co-expressions: loud decline per R-D. Completion: probes (minus declared declines) green m3+m4 under `frame-r12`; the `f7de3863` proof subset green on HEAD-under-frame-r12; medium-invisible gate not regressed.
- [ ] **Z4-8 CELL_HEAP COMPLETED.** ⛔ BLOCKED ON R-E. Cure the s206 root cause structurally: ONE grant authority — `fc_geom`, `x86_fc_on/hit`, `fc_save_active`, and the flat prefix sum ALL read the selector; no site may hold a private port opinion. The carve's block base must be CONSUMED (s206: `mov rax,rbx` then rax overwritten — the base dropped on the floor). Then ζ_self + VSP per Lon's ruling; the s203 SEQ/ALT arity-sized prefix slots re-target to the VSP region (re-target, never re-derive). Completion: Z4-0's recorded SEGVs reproduce on the pre-fix build and die on the post-fix build; probes green under `cell-heap`.
- [ ] **Z4-9 THE CUT.** Delete: `ZC_PORT_PLAIN/INSTRUMENTED/ALLOC/OWNED` values + their hook arms; the `--zeta=` flag + user-facing `ZC_ZETA` axis (⚠ the zls2/zh PROVIDERS partly remain as config internals — the cut is the user-facing axis and dead flavors, not the machinery configs 2–4 stand on; map before cutting); `SCRIP_ZMODE` (it IS cell-stack vs cell-heap); the `ZC_FRAME` build axis + its RBP `#error` block (basis is now per-config + per-graph `x86_fb_pinned`); `ZC_SELFLOAD` + `SCRIP_ZETA_SELFLOAD` iff consumer grep = 0; freeze `ZC_ZH_IN_GCHEAP`/`ZC_COEXPR_STACK_GCHEAP` at 1, delete the 0 arms. KEEP: poison/telemetry/trace debug knobs, sizing MBs, the SG span-guts pattern switches (out of scope), `SCRIP_CELLS(+_TRACE)`/`SCRIP_DEFER_CELL` as rollout internals of configs 3–4 (sunset when conversion completes, their own ladder's call). Completion greps == 0 for every deleted symbol; full crosscheck watermark HELD; probes green all four configs.
- [ ] **Z4-10 GATE.** `scripts/test_gate_zeta_four.sh`: default byte-identity + all four configs × probes × m3/m4 with per-config expected-decline lists. Wire into the pre-commit gate habit. Completion: gate green; gate script committed.
- [ ] **Z4-11 PROSE SWEEP.** `zeta_choices.h` header rewritten to the four-config story (history compresses to a pointer at this goal + the FINDINGs); ARCH-ICON register-contract note; ARCH-ZETA-LOCAL-STORAGE banner; PLAN.md row per R-F. Completion: no doc advertises a deleted knob.

## CROSS-CUTTING LAWS (restated so the walker cannot miss them)
BOTH-MEDIUM MANDATORY · TEMPLATE-ONLY EMISSION (resurrected code included — re-express, never paste) · PER-GRAPH ALL-OR-NOTHING regimes (a blob mixing storage regimes is the s188 failure shape; the selector is process-global, which enforces this for free) · MONITOR-FIRST on any divergence · handoff step 4 `.s` regen whenever codegen is touched · O2-DIRECTED-ONLY · LIVE CURSOR updated every handoff or the handoff is not done · "HANDOFF COMPLETE" only from `scripts/handoff_status.sh` verbatim stdout · push needs Lon's credential — ask at session end, and a local commit is NOT a handoff.

## SESSION SIZING + CONTEXT BUDGET (for the Opus walker)
Expected 4–6 sessions: Z4-0..3 = session 1 (pure archaeology, NO HEAD edits — safe to run parallel to other goals); Z4-4..6 = session 2; Z4-7 = session 3, the largest; Z4-8 = session 4 (needs R-E); Z4-9..11 = session 5. Every rung boundary is a safe handoff point. CONTEXT DISCIPLINE (Lon tracks this — expect to be asked your percentage): read `zeta_choices.h` ONCE and refer back; grep with `-n` + `head` windows, never re-cat; do not open the big GOAL files beyond their LIVE CURSOR; old-epoch trees are for BUILDING and grepping, not for reading wholesale; report your approximate context percentage at natural checkpoints without being asked.

## HONEST LIMITS (do not oversell — the repo's own rule shape)
(a) The `f7de3863`-era tree may not build on today's toolchain; extraction-by-reading is the sanctioned fallback, stated plainly in the FINDING. (b) -O0 timings are relative-only; never quote them as absolute perf. (c) "17 arms" is s202's count — recount at Z4-7 start; parallel sessions move HEAD daily. (d) This file cannot coerce its walker; the LIVE CURSOR, the gates, and Lon's review are the enforcement that actually works — reject any handoff here whose cursor did not move.
