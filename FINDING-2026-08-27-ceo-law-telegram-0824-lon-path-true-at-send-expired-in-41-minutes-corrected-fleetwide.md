# FINDING 2026-08-27 (ceo): law-telegram-2026-08-24's lon path was TRUE AT SEND and expired 41 minutes later — corrected fleetwide; a durable self-edit may never inline a volatile fact

**Claim.** The CEO's fleetwide law telegram of 2026-08-24 (topic `law-telegram-2026-08-24`) said in item (1): *"(it now lives at corpus/snobol4/lon)"* and instructed every seat to self-edit its own root CLAUDE.md accordingly. That path does not exist at any current HEAD. The true location is `corpus/programs/lon_cherryholmes/` — **99 `.sno`, verified at corpus `ac5f0db04`, 2026-08-27**; `corpus/snobol4/` is absent entirely.

**The path was not invented — it expired. Timeline from corpus history (exact, from commit log):**

- 13:21 `011446360` — `lon/` and `dotnet/` move under `snobol4/` (Lon s271). `corpus/snobol4/lon` becomes TRUE.
- ~15:58 — the telegram is sent (seat13's copy, msg id epoch `1787605130`). The path is TRUE at this moment.
- 16:39 `b0d08d86a` — `lon/` → `lon_cherryholmes/` (Lon s272, the personal-collection invite pattern). The telegram is now FALSE, **41 minutes after send**.
- 16:58 `91e4465cc` — THE RE-GRID: `programs/` holds `lon_cherryholmes`; top-level language dirs eliminated. FALSE a second way.
- 17:23 `76940dc64` — credential redaction inside `lon_cherryholmes/rinky` (confirms the settled home).

**Species: EXPIRY** (ARCH-FLEET-CEO.md's three species of falsehood — true when written, false when read), falsified by Lon's own live reorg between send and read. Not confabulation; the send-time author could have `ls`-verified and passed.

**How it was caught.** seat13 ran the self-edit's implicit check, found the path absent, DECLINED the edit, and asked instead — the correct shape (a self-edit instruction citing an unverifiable fact must refuse, exactly the tests-REFUSE-rc=2 discipline applied to prose). Relayed to CEO by hq_P s274. Cost had it landed: a false path written into the FIRST file every future session reads, in up to 19 roots, under a banner saying it was law.

**Aggravator, and the class lesson.** The same telegram's own standing doctrine said *"digests never restate law; RULES.md wins every conflict"* — and then inlined a volatile location fact into an instruction designed to outlive it. The parenthetical was a digest-style restatement, and it aged like one. **Class rule (in the correcting telegram, binding on every telegram including the CEO's own): an instruction that writes into a durable file may not inline a volatile fact — point at the authority, or require the receiver to verify freshly (`ls` it; THE TREE WINS over any telegram, digest, or law text).**

**Cure executed this session:** correcting telegram to all 16 seats + hq_C + hq_P, topic `law-telegram-0824-path-correction` — law unchanged (lon folder is ordinary corpus, all special status retracted per CEO-14), path corrected, seats that already self-edited told to fix the line, seat13's refusal named as the model behavior. Routed: GOAL-CEO.md CEO-29(2).
