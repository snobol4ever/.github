# GOAL-CFO.md — THE CFO SEAT: THE ECONOMY (opened 2026-09-07 08:06 CDT on Lon's word)

Lon, in-chat to ceo, 2026-09-07 08:0x CDT, verbatim: *"I have an idea to hire another executive. How about a CFO, a Chief Financial Officer. Then we will have 4, CEO, CTO, COO, and CFO. What do you think of my idea?"* · *"The /home/claude_cfo is ready to populate."* The ceo's answer Lon accepted: the economy is real and unowned — eight runaway test processes pinned half the box from Sept 4–5 to Sept 7 morning, boards crawled at load 26 on the evening of Sept 6, and the fleet and nine-HQ experiments burned the credits Lon named — so the CFO owns it, on the condition that a fourth Fable seat is never idle: in spare cycles it works the easiest open bug.

## LIVE CURSOR

- **CFO-48 (2026-09-10 19:09 CDT, cfo, the un-gated half of my own 118fe2e4a + an economy red in the blocking set) -- SCRIP `bd64009ec`.** hq_C mailed that 118fe2e4a shipped TWO cures and pinned neither, wired a gate for the &errorvalue half, and named the list()/reads() size half as pinned by nothing but errors.icn, which is not a gate; they offered to write it, and by the time the offer arrived it was on origin, which is the duplicate this seat exists to stop -- I replied stand-down the same turn. `test_gate_icn_list_and_reads_size_argument_is_gated_like_iconx.sh`: 5 raising + 6 accepting shapes x 2 modes = 22 arms, every want cut from a freshly built icont at run time, 0.15s MEASURED, wired into `make test` and adopted into `gate_wiring.tsv` (which also adopted hq_C's errorvalue gate -- wired since yesterday and never in the population). ⛔ EVERY ARM RUNS BEHIND A PRIMER `(1 + "q")` RAISING 102, because an unwired size gate does not answer wrongly -- it raises NOTHING, the expression simply fails, and &errornumber/&errortext/&errorvalue still hold the previous error, so "raised 101 with the value" and "raised nothing and you are reading a corpse" render identically without it. Operands "zz", -7, "ww", 0, -3 are all distinct from the primer's value. ⭐ WATCHED GO BOTH WAYS (CEO-381): with ONLY by_name_dispatch.c restored to 118fe2e4a^ and the runtime rebuilt, rc=1 with 12 of 22 arms diverging -- the five raising arms x2 reading the primer's 102, PLUS the LIST_CVT pair going ABSENT because list("3") did not raise pre-cure, it FAILED, so its write never ran; that pair is the accepting side earning its place, the convertible-string half being broken in a DIFFERENT function (try_call_builtin_by_name_bl), so a raising-arms-only gate would have pinned half a cure. ⛔ THE PUSH HIT hq_C's OWN LESSON WITHIN THE HOUR: a Makefile rebase conflict where hq_C had EDITED the errorvalue recipe line to record their over-correction. I kept THEIR copy and added only my new line -- re-adding the version someone replaced is the wired-twice false-regression shape ratchet arm 7 exists for -- and read `git show --stat` on the pushed commit afterwards.
  - **ECONOMY, the finding of the sitting: `make test` -- THE blocking set -- is RED on a CLEAN origin tree for every seat but the coo, and the cause is CEO-523's own guard.** Measured with my changes stashed and the tree rebuilt, so it is origin's and not mine. `_one_runner_guard` runs at the TOP of `cmd_run` in `corpus_suite_harness.py` and refuses rc=2 for any non-coo seat REGARDLESS OF WHAT IS BEING RUN, so a gate feeding the harness its own two-entry mktemp fixture is refused exactly like a 763-entry master. Confirmed red: `test_gate_capture_stdin_and_red_exit.sh` (its arm prints "converted suite is not fully green:" with an EMPTY board line, because the refusal went to stderr and no SUITE_BOARD line was ever printed -- a refusal rendering as a content failure) and `test_gate_modes_declaration_travels.sh` (rc=1, 1.7s). `make test` stops at its first red so every later arm was unreached; the count is AT LEAST two. ⭐ THE SHARPER HALF: `test_gate_icon_master_per_entry_identity.sh` runs the genuine Icon master for 85s with no override, and THERE the refusal is the rule working exactly as written -- so `make test` WIRES A BOARD and cannot be green for a non-coo seat even after the fixture cases are cured. Routed to ceo with two cures and no edit of my own, the guard being governance: (1) narrow it to fire only for a suite under the corpus tree, since a mktemp fixture publishes no row and writes no score and is not a board by the rule's own definition -- recommended; or (2) have the three gates export S4E_ONE_RUNNER_OVERRIDE, loud and recorded but training every gate author to carry the override until the guard is a formality.
  - **LEDGER / BOARD COST (measured 2026-09-10 19:09 CDT):** `make test` 113.08s wall at load 3.6 on 16 cores -- and that is a FLOOR, not the cost, because it died at its first arm; the new gate 0.15s; `make preflight` 33 arms / 0 red. CREDITS: no new in-chat reading from Lon this sitting, so the ledger's last credit line stands and no burn rate is computed (>=2 h checkpoint averages only). COMPUTE: load 4.67 -> 5.10 over the sitting, 16 cores, up 2 days 2 h; runaway census CLEAN -- no process over 30 min elapsed above 20% CPU; disk 85G/125G, 72% (was 76% at 18:52, so ~5G was returned during the sitting). No kill grant sought or needed.
  - **NEXT (CEO-545, supersedes CEO-542's dynamic-loading class):** jcon tracing, 120 differing lines after the ceo's trace landing `a3eab74ad` (was 128), the biggest single file left in Icon. Pull first -- suspend/resume events are on origin and moved the file underneath. The residue is trace SHAPES, not missing events; hq_B landed one renderer where there had been two (`1add96ca9`) and named the StringLimit trap (it counts SOURCE characters, not IMAGE characters), to be checked before widening anything. hq_S holds cxtrace, the sibling file: ownership of any shared renderer is agreed between us BEFORE either edits it.

- **CFO-47 (2026-09-10 18:44 CDT, cfo, hq_V routing of the Icon master's LAST red) -- procedure_record_every_replace_12 MODE 4: 4 -> 0 DIFFERING LINES, SCRIP `e7e916e14`, corpus witness `cfc826b0f`.** On 118fe2e4a the entry already read 0 in mode 3 (hq_V's five wrong numbers were cured by composition under CFO-45/46) but 4 in mode 4: its last record `stop(&output, 'testing stop')` printed its line 8165 bytes in, at the stdout buffer boundary, instead of last. The stop arm ignored a leading file argument and wrote every value to stderr with its own formatting, and printed no newline with no arguments; iconx treats stop as write with &errout as the default file. The arm now mirrors write's body (file argument switches the destination, values via the write renderer, the line always ended, stdout and the destination flushed, exit 1). Three probes (file, no file, no args) match iconx on stdout, stderr and exit status in both modes; errors.icn stays 0 in both copies; the live witnesses 0; `make preflight` 33/0; NOTICE to hq_C named the arm before the edit.
- **CFO-46 (2026-09-10 18:40 CDT, cfo, CEO-532 lane, errors BY CLASS with hq_C) -- errors.icn 57 -> 0 DIFFERING LINES IN BOTH COPIES (arizona general and jcon), SCRIP `118fe2e4a`, corpus witness `2e8b94fe5`.** The last two classes: `list(x)`/`reads(f,n)` had no size gate (a non-integer size now raises 101 with the value, `list(-1)` and `reads(f,0)` raise 205, a convertible string size is accepted), and a negative real raised to a real power raised 206 WITH `&errorvalue = -36.0` where iconx raises it with no value (the keyword fails). NOTICE to hq_C named `icn_argtype_gate` before the edit; hq_C confirmed neither arm was in hand. Witness 0/0 in modes 3 and 4, the live witnesses 0, `make preflight` 33/0, one-line FLIP to ceo. NEXT in lane (hq_V routing, not a claim): the Icon master's last red `procedure_record_every_replace_12`, an errors-class program, to be re-measured on this HEAD since four of its five named numbers were cured under CFO-45.
- **CFO-45 (2026-09-10 17:2x -> 18:4x CDT, cfo, MODE NONET, CEO-532 lane: ARIZONA/JCON errors BY CLASS with hq_C) -- THREE CURES AND A GATE: errors 171 -> 115 -> 99 -> 57 DIFFERING LINES IN EACH OF THE TWO COPIES (the same program under two oracles), SCRIP `21ca52581`, `4c1ed8593`, `8528a3734`; the reader-with-no-writer GATE `70bf40831` WIRED.** The file is a CHAIN, not a list: `&error := -1` and a report of the four error keywords after every provoked failure, so one raise missing or extra shifts the countdown for every record after it -- each cut is one CLASS of missing raise, measured on a reduction against iconx first. (1) Element generation coerced ANY value to a string (`!image` yielded a procedure's image) and walked a cset with strlen (`!&cset`, whose first character is NUL, generated nothing): now 116 with the value for a procedure or the null value, the numeric coercion Icon really does kept, a cset's registered length used; and `sort/get/pop/pull/push/put` had no argument gate, so a wrong type fell to "Undefined function called": now 115/108 with the offending value in the same gate as `any/many/upto/tab/move/insert/member/key/read`. (2) `x[i:j]` on the null value quietly sliced "": two places treated `DT_SNUL` as a string with empty bytes; excluded by testing for bytes, so only a true `&null` reaches the tail and raises 110 with the value. (3) `&subject := []` succeeded and `&pos := "x"` failed silently: the Icon path assigns these through `rt_keyword_subject_set`/`rt_keyword_pos_set`, NOT the general keyword store where my first guard went -- now 103/101 with the value there, a cset becoming its string and a NUL surviving; and `pull[c]` named `&null` because the subscript base was lowered as a VAR_REF that dereferenced to null while the same name in value position was rewritten to a PROC_VALUE -- the post-lowering pass now covers IR_VAR_REF, its existing skip of assigned names keeping lvalues lvalues. hq_C and I cured the argument-type sink IN PARALLEL and it surfaced only as a git conflict on a helper we had both named `icn_arg_is_list`; owned: from now a one-line NOTICE names the function before I edit a shared runtime seam. Five witnesses cut from iconx, all both modes; the NUL, set, section and trace arms held throughout; `make preflight` 33/0 each time; NO boards from this seat (CEO-523/525).
- **THE GATE (`70bf40831`, on hq_B's bisect under CEO-534):** my cset perf landing had deleted THREE non-perf lines, not one, among them the write of `g_emit.op_activate_proc` -- declared, read by `bb_activate`, written by nobody, so the template's `main` default answered for 67 of transmit's 104 trace lines. `test_gate_emit_fields_read_by_a_template_have_a_writer.sh`: a 0.1 s source census of `g_emit` fields read as `_.op_*` minus those written as `g_emit.op_*`, pinned floor of three dead ones named with why; control-armed both ways on `op_activate_proc`, and its header records the two traps that fooled me first -- a PREFIX-sharing rename leaves the census green (the token is the longest lowercase run), and `comm` is byte-wise while `sort` under the UTF-8 locale ignores underscores (the first version passed its own control arm until `LC_ALL=C`). Wired into `test`. Named, not touched: `test_gate_gate_wiring_ratchet.sh` is RED on origin because `test_gate_boards_refuse_when_the_binary_moves.sh` is wired TWICE (`95e967517` 17:50, `323c63081` 18:02); told to hq_B and the ceo.
- **CFO-43 (2026-09-10 13:5x → 14:1x CDT, cfo, MODE EXECUTIVE, CEO-525 pace: one bug in hand, two an hour, one-line flips) — kwds CURED AND LANDED (SCRIP `acbd20462`): THE FIVE GRAPHICS KEYWORDS FAIL AS icont's DO AND `&progname` IS THE NAME IT WAS HANDED; MASTER ENTRY `procedure_every_alt_replace_4` m3=PASS m4=PASS.** `&col &row &x &y` answered 0 and `&window` answered `&null`, so nothing could tell a no-graphics build from a window at the origin — iconx FAILS all five, because a graphics keyword is a generator with nothing to produce when there is no window. `kw_read` now fails for them AND `rt_keyword_read` passes that failure through: it had been falling to a variable lookup that answered `&null`, the same list that already carried `&control`, `&interval`, `&meta`, `&shift` — the fix is one clause there, and without it the runtime change is invisible. `&progname` synthesised `"./"` + the source STEM in two places (the keyword lowering and the `__icn_progname` global initialiser) and the stem dropped the extension; it is now the program name AS HANDED, basename with extension, in both modes. ⭐ **Second cure of the hour** (set-insert `c09ad4d43` was the first), each pushed on its own DONE-WHEN with no board: `make preflight` 33/0, the touched gate green, and the four live witnesses re-run.
- **CFO-44 (14:1x CDT) — JCON J-Z MEASURED GREEN AND ONE FIXTURE FACT WORTH MORE THAN A FLIP:** on origin `acbd20462` the package files match their `.std` — `misc`, `nargs`, `sorting`, `struct` outright, and `others` **only when it is given its `others.dat` on stdin**: its second half is a word counter reading `read()`, so graded with `</dev/null` it prints nothing and reads as 53 missing lines. That is a runner fixture question (the `.dat` sidecar), not a defect, and it is named here so the next board reading does not mint a bug for it. My lane is then empty but for `extlvals`/`cfuncs`, which waits on the ceo's ruling (rank-1 row: cure the foreign iconx descriptor bridge, or rule outside the baseline).
- **CFO-42 (2026-09-10 13:2x → 13:4x CDT, cfo, MODE EXECUTIVE, CEO-523 lane item 3) — THE FOUR ARIZONA REDS RULED ON MEASUREMENT, NOT ON CONVENIENCE: `recent` IS ALREADY GREEN, `var` IS ONE MISSING BUILTIN, `cfuncs`/`extlvals` ARE A FOREIGN ABI AND THE SCOPE CALL IS THE CEO's.** (1) **`recent`: zero diff on origin** — it flipped with my set-generation cure an hour earlier; nothing owed, and I say so rather than bank it. (2) **`var`: `display(n, f)` is unimplemented** — it falls to the generic dispatcher and raises `Run-time error 106 procedure or integer expected` at var.icn line 63; minted rank 0 and taken, witness `display_prints_the_frames_locals_and_the_globals` cut from iconx (corpus `6d6fe17c3`, red on arrival) with the exact format pinned, and the baton's QA carries the reconnaissance: the dyn-scope path HAS an activation chain (`RT_AB_ANCHOR`/`AB_OFF_ANCHOR`) and `g_stage2.proc_table` has every name and slot, but Icon's zframe path joins no chain and records no frame-to-procedure name at runtime — that mapping IS the row, the rest is formatting. (3) **`cfuncs`/`extlvals` are NOT outside the baseline** and the easy ruling would have been wrong: the shared oracle RUNS them (`procedure bitcount(500) = 6`) because `libcfunc.so` ships in its own bin and its `&features` carry `dynamic loading` and `external values` where ours carry neither. Curing means `loadfunc` dlopening that library and bridging **another runtime's value layout** — `int f(int argc, descriptor *argv)` with `argv[0]` the return slot, iconx's `struct descrip` (`src/h/rstructs.h`) and the `D_Integer`/`RetInteger`/`Arg*` constants (`ipl/cfuncs/icall.h`) — a marshal for integer, string, real, null and an opaque external, plus a default FPATH and the two feature words. Bounded, but a bridge to a foreign ABI is a scope call above this seat: minted at rank 1 with **the ruling as its first arm** and put to the ceo.
- **CFO-41 (2026-09-10 12:5x → 13:1x CDT, cfo, MODE EXECUTIVE, Icon only, CEO-523 lane) — SET GENERATION: A STEP NOW WALKS TO THE SUCCESSOR OF THE LAST ELEMENT YIELDED, SO AN INSERT DURING THE WALK CANNOT SKIP OR RE-YIELD (SCRIP `c09ad4d43`, corpus `23b86f4d2`); jcon `gener.icn` READS `[ok]` ON ALL 62 LINES, BYTE-IDENTICAL TO `gener.std`.**
⭐ **hq_C's SPLIT WAS THE WHOLE CLUE:** every DELETE line `[ok]`, every INSERT line failing with elements not generated and elements generated twice. `table_icn_nth` sorted the entire set into iconx bucket order on EVERY step and memoised a POSITION in that vector — an insert that sorts ahead of the cursor shifts every later position by one (an original is skipped), and an insert that RESIZES the table changes `icn_mask`, so every element lands in a different bucket and the memoised position points into a wholly different order (elements already yielded come back). A delete never moves a position forward, which is why the delete arm stayed green and hid it for a day. ⛔ It also pinned a fresh vector per step and never freed it: my first witness, written without gener's own `limit` clause, exhausted a 512 MB heap — the reduction has to keep the limit.
⭐ **THE CURE IS A SUCCESSOR WALK, NOT A SNAPSHOT.** A step takes the smallest live element whose `(segment, slot, hash, insertion-order)` key is greater than the remembered one, under the mask captured when the walk began: an element inserted ahead of the cursor is simply never visited, one inserted behind it is, and a resize cannot reorder what is already past — iconx's own generation semantics. No allocation per step at all (the old path allocated and pinned one per step); the mask, the last key and a validity flag sit in `TBBLK_t` beside the existing `gen_idx`. ⛔ My first cut WAS a snapshot (indices into the insertion-order vector, malloc/free per generation) and it was wrong in the same way for a resize — the successor walk is the shape that needs no order to be stable.
⭐ **MEASURED, AND NO BOARDS BY ORDER (CEO-523):** the row's DONE-WHEN on the origin tree — the witness in both modes AND `jcon gener.icn` byte-identical to `gener.std` — PASS; the two touched gates green; `make preflight` 33 arms 0 red. Witness `inserting_into_a_set_while_generating_it_yields_every_original_once` (gener's own check reduced to counts: nine insert cases, nine delete cases, ref cut from iconx) at the handoff path. The master entry `procedure_every_scan_replace_13` flips with it, and Zona/Jcon `gener` with it. Row taken by `assign` from hq_C with the doorbell, and the baton's placeholder DONE-WHEN replaced with a real one first — `assign` refuses a placeholder, correctly.
- **CFO-40 (2026-09-10 11:4x → 12:4x CDT, cfo, MODE NONET → EXECUTIVE at 16:4x on Lon's word, Icon only) — CEO-496 CUT 2 LANDED AND THE ROW DONE (concord m4 PASSES iconx 5 of 5, 1.09x–1.14x, SCRIP `e6390409d`); THE CSET ARENA WRAP FOUND, FILED AND CURED AT RANK 0 (SCRIP `58ed77872`): THE CSET OPERATORS RUN ON THE 256-BIT TABLES AND THE 64 KB ARENA THAT SILENTLY OVERWROTE A LIVE CSET IS GONE; deal 0.5x → 0.6x OF iconx.**
⭐ **CUT 2, TWO CALL-COUNT FACTS:** the LIT_CHARSET box registered its literal on EVERY evaluation (5% of concord) — a 64-entry (pointer, length) cache in `rt_icn_cset_register` returns before the hash; and every concat paid `IS_REAL_fn`×2, `rt_big_is`×2, `descr_to_str`×2, `VARVAL_fn`×2 and `rt_sxt_match` as real calls at `-O0` to learn both operands were plain strings — `str_concat_fracdigit_d` and `c_str_concat_d` read the descriptor fields. The DONE-WHEN as minted, five runs on the origin tree after the rebuild: **1.09x 1.09x 1.14x 1.14x 1.09x**, outputs identical; shell-free `perf stat` 5.4 ms task-clock vs iconx 4.9, 288 page faults vs 117 (a 31 MB dynamic runtime against a small static iconx — the residue is the loader and the kernel, not our code; `-Wl,-Bsymbolic-functions` measured no gain and is closed). CEO-519 accepted it.
⛔ **THE FINDING ON THE WAY (FINDING-2026-09-10-cfo-the-cset-arena-wraps-under-a-live-value, .github `a961adb4c`):** `cset_union/diff/inter/canonical` each built a 256-byte presence table and wrote the sorted string into `static char icn_str_arena[65536]` with a bump pointer that RESET TO ZERO when full — so a cset held in a variable read as the 1137th cset built after it, both modes (iconx `'abc' 3 abc`, we `'137' 3 137`), and the length registry, keyed by the same pointer, took the newer length too. A wrong-answer class no board reached (their csets are few and small); the coo asked for it as a FINDING with a witness, the ceo minted its cure at rank 0 (CEO-519/520). Witness `a_cset_held_in_a_variable_survives_a_thousand_later_csets` (corpus `0fe51f37d`, red on arrival by design; hq_V holds it for absorption as the cure's flip).
⭐ **CUT 3, THE REPRESENTATION THE CEO NAMED:** the operators take each operand's 32-byte table (the registry's by pointer, or built from the bytes for an unregistered string), combine it (`|`, `& ~`, `&`), and write the canonical string ONCE from the result bits — walking only the non-zero bytes, since a 13-card hand is 13 bits of 256 (`cset_from_bits` was 12% of deal walking all 256) — into a pinned block through `kw_cset_intern`, which first returns an existing registered cset of the same content (the coo's content index), so equal csets are one pointer and nothing is ever reused under a live value; `cset_canonical` returns a registered, already-sorted string as itself; the arena is deleted. DONE-WHEN PASS both modes on origin; the cset-membership, trace and frame-variable witnesses and the SNOBOL4 keyword csets (the same registry) hold. **deal `-h 1000`: m4 29–32 ms vs iconx 17–20 (0.6x; the coo read 0.71x at load 1.4 on cut 2 and 42–45 vs 27–31 before), outputs identical beyond the `&version`/`&features` lines (hq_I's kwds mask class); concord 1.08x–1.23x.** perf on deal after: `try_call_builtin_by_name_bl` 9.6% is the top owner again — the coo's row (CEO-513).
⛔ **A FAULT OF MINE, OWNED THE SAME TICK AND NOW FLEET LAW (CEO-520):** killing my own board chain I used `pkill -f` on the runner script names; every seat is one Unix user, so the patterns reached hq_C's identity gate and harness (22 s old a minute later — told to hq_C with the window). And a `pkill -f` whose pattern the tool's own shell carries kills the shell (exit 144) — twice. Rules: wait on a pid, kill only pids you launched (recorded at launch), read `/proc/<pid>/cwd` before any pattern kill; my stuck waiters were listed by cwd and killed by pid this tick — and that cleanup killed my own live waiter too, because the tool's wrapper hides the command text from `ps`; the replacement waiter is on the board script's pid.
⭐ **BOARDS ON THE CUT-3 TREE (load 2–11):** Zona 81/90 (40 s) · IcnM 756/758 (57 s) · Budne 70/74 (33 s) · Gimpel 109/116 (64 s) · Jcon 69/77 (23 s) · GNU 11/11 (6 s) · PAT 292/427 (18 s) · IPL **107/108** (769 s; the denominator grows under other seats) · SnoM 1877/1902 (278 s) · `make -k test` 252 s, one red: the orphan ratchet at icon 22 over hq_V's floor 20 (`3e8670d97` carries the trace keeper as floor with both moves named apart; the eighth pair absorbed as `procedure_every_scan_replace_14`, byte-identical) — the ninth clears on its absorption. ⛔ From CEO-523 on, NO BOARDS from this seat: DONE-WHEN, touched gates, `make preflight`; the coo runs the one board.
- **STANDING RULE (cfo, 2026-09-10 11:5x CDT, after a fault of my own): A SEAT KILLS ONLY PIDS IT LAUNCHED.** Every seat is one Unix user, so `pkill -f <runner script name>` reaches every seat's copy of that runner — twice this hour mine matched hq_C's identity gate and harness runs (owned to hq_C and the ceo with the window). Record `$!` at launch, kill that pid and its children; before any pattern kill read `/proc/<pid>/cwd` to see whose it is. The compute census names a runaway by its cwd for the same reason.
- **LEDGER 2026-09-10 11:4x CDT (cfo):** credits: no reading from Lon · load 2–10 through the cut-2 boards · runaways 0 · disk 87G/125G 74% · flips this sitting: concord m4 **1.07x–1.21x of iconx, PASS 5 of 5** on the DONE-WHEN as minted (cut 2: the literal registers once; the concat reads a plain string's pointer and length from the descriptor instead of nine -O0 helper calls), IPL **102/103** (the denominator grew 89 → 103 under other seats; CRASH 1), everything else held (Zona 81, IcnM 756/758, SnoM 1877/1902, Jcon 69/78, Gimpel 109, Budne 70, GNU 11/11, PAT 292) · **BOARD COST (load 2–10):** Zona 69 s · IcnM 93 s · Budne 41 s · Gimpel 64 s · Jcon 25 s · GNU 7 s · PAT 19 s · IPL 625 s (the shorter IPL: fewer timeouts on a quiet box) · SnoM ~5 min · `make -k test` 194 s, one red: the orphan ratchet on my loose pairs (the trace keeper needs +1 on the floor in hq_V's hand; the cset pair absorbs; the arena-wrap pair is RED BY DESIGN until cut 3). Landed: SCRIP `e6390409d` · corpus `0fe51f37d` (the arena-wrap witness) · .github `a961adb4c` (FINDING-2026-09-10-cfo-the-cset-arena-wraps-under-a-live-value: a cset held in a variable reads as the 1137th built after it, both modes — a wrong-answer class the coo asked for as a FINDING with a witness; cut 3 cures it). Shell-free, `perf stat`: concord m4 **5.4 ms task-clock vs iconx 4.9**, 288 page faults vs 117 — the residue is a 31 MB dynamic runtime against a small static iconx; `-Wl,-Bsymbolic-functions` measured no gain (named, closed).
- **LEDGER 2026-09-10 11:0x CDT (cfo):** credits: no reading from Lon · load 5–13 through the landing boards · runaways 0 · disk 87G/125G 74% · flips this sitting: concord m4 0.56x → 1.00x of iconx (the row's own number), Zona 80 → **81/90**, Jcon **69/78 graded** both modes, IcnM 756/758 held, IPL 87/89 held, SnoM 1877/1902 held, Gimpel 109, Budne 70, GNU 11/11, PAT 292 held · **BOARD COST (load 5–13):** Zona 49 s · IcnM 68 s · Budne 40 s · Gimpel 68 s · Jcon 34 s · GNU 8 s · PAT 21 s · IPL 924 s · SnoM 296 s · `make -k test` 357 s — ONE red, `orphaned_witnesses_do_not_grow` at icon 21: my two loose pairs; hq_V measured the trace pair green and byte-identical but it STAYS LOOSE by construction (its 43 trace lines carry its own file name and the master builder renames entries), so the icon floor needs +1 in their hand, and the cset pair absorbs. Landed: SCRIP `539af6946` (on the coo's `3bbdfc8c7`) · corpus `5795c1cd8` (read from origin/main after the push).
- **CFO-39 (2026-09-10 09:4x → 11:0x CDT, cfo, MODE NONET, Icon only) — CEO-496, LON's QUESTION ("Why are we slower than an interpreter when we are a true compiler?"): CSET MEMBERSHIP IS A BIT TEST — concord IN MODE 4 FROM 0.56x OF iconx TO PARITY (SCRIP hash in the LEDGER line below, on the coo's registry index 3bbdfc8c7 as CEO-503 ordered).**
⭐ **THE MEASUREMENT FIRST.** The DONE-WHEN as minted on origin c8701b17e: concord m4 **75 ms vs iconx 42 ms (0.56x)** at load 6; the empty program iconx 2.5 ms, m4 3.8 ms. perf on the m4 binary (`/usr/lib/linux-tools-6.8.0-138/perf`; the `/usr/bin/perf` wrapper has no binary for this kernel) agreed with the ceo: 31% in `kw_cset_len`. ⭐ **THE SHAPE UNDER IT:** `many(&letters)` lowers to a `LIT_CHARSET` — the scan box's literal arm holds the characters as a read-only string the registry NEVER sees, so every character's `rt_icn_cset_member` fell through the pointer miss to the strcmp scan of every registered cset; and the loop called `rt_scan_needle` AND the member test PER CHARACTER. The runtime side of the fix is the coo's now (CEO-503: their pointer-and-content index landed first, deal 378 → 138 ms); mine goes on top: a 32-byte bit table beside every registry entry (`kw_cset_bits`, filled on append and on a length change), `rt_icn_cset_member` one bit test, and — the part that removes the registry from the hot path entirely — the LITERAL arms of `bb_scan_many` and `bb_scan_upto` seal the 256-bit table into read-only slots 3..6 at emit time (`x86_ro_seal_q`, both media) and test inline: `lea rip`, `movzx`, `bt [rdi], esi`, `jnc`. No call, no name, no registry per character — iconx's own shape.
⭐ **THE SECOND FIND WAS NOT A CSET AT ALL.** With csets fixed, `SCRIP_BID_PROF=1` named the hottest builtin by count: `ICN$LINE`, 7438 calls per concord run — the first statement of EVERY procedure body carried an `IR_CALL` to the builtin dispatcher to set `g_line` and `g_file` (the trace file column). It is now an `IR_LINE_MARK` carrying the file name; `bb_line_mark` stores both inline from a read-only string slot. `&trace` output is byte-identical (the trace witness and seven probes, both modes).
⭐ **RESULT: concord m4 30 ms vs iconx 30 ms (1.00x), outputs identical**, 0.94x–1.03x across runs on a box at load 5–7 — parity within the noise; the DONE-WHEN's `≤` PASSES on most runs and reads RED by 1 ms on some. Reported as parity, not as a pass: the ceo rules on the margin. What remains, measured: kernel 14% + loader (the 1.3 ms empty-program gap is `do_lookup_x`/`_dl_relocate_object` over a 31 MB `.so` with 2485 relocations, and page faults — not our code), `str_concat_d` 10% + `fracdigit` 4% (a GC safepoint and two bignum tests per concat), the builtin dispatcher 5.4% (the jump table covers 127 bids; `string`/`write`/`right` are in it), `kw_cset_find_ptr` 3.3% + `rt_icn_cset_register` 2.1% (the LIT_CHARSET box re-registers its literal on every evaluation — cut 2), `rt_substr` 4.4%.
⛔ **TRIED AND REVERTED, NAMED SO NOBODY TRIES IT AGAIN BLIND:** `__attribute__((always_inline))` on the `IS_*_fn` descriptor predicates in `core.h` (they are calls at `-O0`, 2–4% of the profile) BROKE MODE 3 for every program of every language — SIGSEGV in `rt_outer_call_delta0`, the slab entry — while mode 4 was fine; Gimpel read m3 0/116 on that build before I caught it with a one-line smoke. Cause not chased (the compiler binary and the `.so` see different copies of an inlined function; the slab wiring evidently takes an address). Also named: `-Wl,-Bsymbolic-functions` on the runtime `.so` for the loader cost — my trial linked the whole object dir and hit duplicate symbols; link it from `RT_PIC_OBJS` next time.
- **LEDGER 2026-09-10 10:1x CDT (cfo):** credits: no reading from Lon · load 5–7 through the trace boards (a quiet hour), 15 at the IPL end when the fleet's boards overlapped · runaways 0 · disk 87G/125G 74% · flips this sitting: the trace halves (IcnM 754/757 on my diff; hq_V's absorption of the starseq pair took it to 756/758 in the same hour), Zona 80 held, IPL **87/89 both modes** (1103 s @ 5 → 15), Budne 70, GNU 11/11, PAT 292/427 held · cost per flip: not computable without a credit reading. **BOARD COST ON THE QUIET BOX (load 5–7):** Zona **45 s** · IcnM **60 s** · Budne 31 s · GNU Prolog 6 s · Pascal PAT 18 s · IPL 1103 s (the IPL runner is ~18 min at any load: its cost is the 8 s timeouts and the 851 compiles, not the box) · `make -k test` **199 s** — the same set cost 473 s at load 14 this morning. Landed: SCRIP `6e40570bc` · corpus `dacee8c98` (read from origin/main after the push). ⭐ CEO-496 (csets by strcmp; concord m4 **75 ms vs iconx 42 ms = 0.56x** at load 6 on origin c8701b17e, the DONE-WHEN as minted) is RUNNING under my claim; CEO-503 orders the coo's registry index first and my bit tables on top — my stash holds the index, the per-entry bit tables and the inline 256-bit scan-box tables for the LITERAL arm (which is what `&letters` lowers to: a LIT_CHARSET the registry never even sees, so every character fell to the strcmp scan).
- **CFO-38 (2026-09-10 09:2x → 10:0x CDT, cfo, MODE NONET, Icon only) — THE CTO's TWO TRACE HALVES CURED AND LANDED (SCRIP hash in the LEDGER line below): THE CALL LINE PRINTS ONCE, AT THE RIGHT DEPTH, WITH EVERY PARAMETER, AND A LIST OR RECORD IS IMAGED AS iconx IMAGES IT; `&level` WAS ONE TOO HIGH ON EVERY SHORT-ARITY OR VALUE CALL.**
⭐ **THE ROOT WAS A DOUBLE COUNT, NOT A FORMAT.** `&trace := -1; f(1)` with `procedure f(a,b,c)` printed `f(1)` and then `| f(1,&null,&null)` one bar deeper, and `&level` inside read 3 where iconx says 2; `p := f; p(1,2,3)` the same even at full arity. A gdb watchpoint on `rt_k_level` in the mode-4 binary gave the pairing: the C open path (`rt_proc_call_prologue_lex`, reached by short-arity `open_detN`, by value calls through `rt_proc_call_gen_h`, and by `rt_proc_call_c_lex`) counts up and prints with the PASSED arguments; the Icon callee's own activation entry `FN__f` counts up and prints again with every PARAMETER; `f_γ`/`f_ω` count down; the C epilogue counts down again. Pascal's `FN__f` never touches the counter (its C pair is its only pair) and neither does Prolog's; a full-arity Icon call takes the direct `f_dcα` entry with no C pair at all, which is why only the C-path shapes doubled. ⛔ SNOBOL4's `&FNCLEVEL` was right throughout (1 1 0 against SPITBOL through `F(1)` and `APPLY`), so the dyn-scope pair was left exactly as it was.
⭐ **THE CURE: THE C OPEN MARKS EACH LEVEL AS CALLEE-OWNED OR NOT.** A procedure with a direct-call entry (`g_rt_dc_fns_store[idx]`, the `dcfn` the staged path jumps to with no prologue) and not a generator owns its level; for those the C open neither counts nor prints, and every C epilogue (`γ`, `ω`, `named`) consults the mark for the level it closes instead of counting down blindly — a 64K byte array indexed by level, cleared on close, sound under LIFO nesting; beyond the array the old count-and-print stands. ⛔ My first cut printed BEFORE the count and the generator line lost a bar (generators take the coexpr branch and are counted there; excluded from ownership) — the order is count then print, as it always was. **THE IMAGE HALF** is a trace-only imager in `core.c`: a top-level list prints `list_N = [e1,e2,e3,...,e(n-2),e(n-1),en]` past six elements (`[]` when empty), a top-level record `record T_N(f1,f2)`, nested aggregates in image()'s count form UNLESS empty (`list_4 = []`, `record e_1()` — iconx's own rule, probed on nine shapes), tables and sets always the count form; the return and suspend values the same. The `suspended`/`resumed` lines stay the cto's rtx-spine half (CEO-468) and are not touched.
⭐ **MEASURED ON THE DIFF (quiet box, load 5–7; each runner its own):** Zona **80/90 both modes** held (45 s) · IcnM **754/757 both modes** (60 s; up from 749 with other seats' cures rebased in) · Budne **70/74** held (31 s) · GNU Prolog **11/11** (6 s) · Pascal PAT **292/427** held (18 s) · IPL: LEDGER line · `make -k test` **199 s**, ONE red: `test_gate_fresh_binary_guard` census #2 names `test_gate_icn_loadfunc_returns_a_callable.sh` as a scrip-executing gate with no freshness guard — another seat's new gate, unreachable by this diff (CEO-463). Witness `trace_call_line_prints_every_parameter_and_images_a_list` (49 lines cut from iconx; short-arity, value call, `&level`, list/record/set/table/cset/procedure/function images, long list, empty nested) MATCHES both modes and is at the handoff path; the earlier pair was absorbed by hq_V in the same hour (the orphan gate reads at its floor again).
- **LEDGER 2026-09-10 09:1x CDT (cfo):** credits: no reading from Lon · load 4.6 at 08:15 → 14–17 through the board hour (five seats' boards overlapping) → 23 at 09:15 on 16 cores · runaways 0 (every long-etime process is a seat's bash; the compiles seen were seconds old) · disk 87G/125G 74% (+1G on the day) · flips this sitting: Zona 79 → 80 (ilib), IPL 85 → 86, Gimpel 108 → 109, SnoM 1870/1894 → 1877/1902 (denominator +8 from other seats; the one FAIL `simple_output_64` is the CEO-428 termination-block ref, pre-existing), IcnM 749 held, Budne 70 held, TPgm 1 held · cost per flip: not computable without a credit reading. **BOARD COST AT LOAD 14–17 (against the quiet-box column of 2026-09-09):** Zona **80 s** (37 s quiet) · IcnM **141 s** (45 s quiet) · IPL **1110 s** (no quiet reading yet) · Gimpel 56 s · Budne 42 s · TPgm 5 s · SnoM (corpus_suite_harness, `--by-modes-column`, both modes) **464 s @ 9.9 → 22** · `make test` to its first red **76 s**; `make -k test` full **473 s** (8 min 43 s quiet) — the blocking set costs roughly the same busy or quiet because its arms are short and serial; the suites cost 2–3× busy. Landed: SCRIP `c8701b17e` · corpus `b5440d452` (read from origin/main after the push; the first push of each was rejected on a moved origin and rebased once more).
- **CFO-37 (2026-09-10 08:14 → 09:3x CDT, cfo, MODE NONET, Icon only) — CEO-483 CLOSED AS ALREADY CURED, THEN CEO-484: A STRING HELD BY AN ICON FRAME VARIABLE WAS EXTENDED IN PLACE BY THE SPITBOL-STYLE APPEND TABLE — CURED (SCRIP hash in the push line below, 3 runtime lines, no template), ilib GREEN, Zona 79 → 80, IPL 85 → 86, Gimpel 108 → 109, IcnM HELD 749.**
⭐ **CEO-483 (movecorr shape B) WAS MINE FROM YESTERDAY.** The row's DONE-WHEN passed on origin 483717a0c in both modes before I touched anything: the cure is `183aee4a1` (CFO-34). The ceo's RED came from /home/claude_ceo/SCRIP at 8c8f88b1c with a 19:09 binary, six minutes before the cure landed — a stale checkout read as origin; the ceo retracted it (CEO-484). Row closed with that receipt, no diff.
⭐ **THE NEXT RED WAS ONE LINE OF ilib, AND IT WAS NOT A CALL SHAPE.** ilib through `test_icon_arizona_suite.sh` diffed on `starseq` alone (`suspend "" | (starseq(s) || !s)`): `aaab` where iconx says `aab`. Five reductions said the recursion was noise; the smallest shape is `y := m2("a")` where m2 RETURNS `s || "a"`, then `every write(y || !"ab")` prints `aaa aaab` and `write(y)` prints `aaab` (iconx: `aa`). `*y` still read 2 — the descriptor kept its length while the bytes under it grew. A gdb hardware watchpoint on `g_sxt_fr.owner` in the mode-4 binary gave the sequence, not a theory: the concat result is noted as the extension OWNER; the local store is a bare slot move that never breaks ownership; the next concat sees its left operand IS the owner and grows it in place (`rt_sxt_extend`), so every strlen reader (Icon `write` → `VARVAL_fn`) sees the grown string; and on resumption `rt_sxt_match` matched by POINTER ALONE and handed back the owner's grown length for an operand whose own length was 2 — `"aaa" || "b"`. ⛔ The inline twin `y := "a" || "a"` only LOOKED correct because two literals fold to one literal — it never tested the store. With `z := "a"; y := z || z` the local, a PARAMETER, a GLOBAL and a STATIC all fail the same way; a LIST element does not (aggregates.c breaks ownership on store). SNOBOL4 is sound: every SNOBOL4 variable is in the GVA the alias census scans, and its store path breaks ownership — `X = Z 'b'; OUTPUT = X 'c'; OUTPUT = X` and the function-param/local twin match SPITBOL both modes before and after.
⭐ **THE CURE IS A PROPERTY SWITCH, NOT A LANGUAGE BRANCH.** `rt_icn_zframe_args_install` — the Icon frame installer every procedure entry runs — calls `rt_sxt_frames_present()`, which sets the table's existing off switch (the `SCRIP_SXT_OFF` slot both the asm fast path and the C path already honour): a program with frame-resident variables cannot be alias-scanned, so it gets no in-place append. And `str_concat_d` refuses the owner match when the operand's own length differs from the noted length — a descriptor sharing the pointer with another length is a different string (a latent SNOBOL4 hazard too; kept). Cost of the switch: Icon's `s ||:= c` loop was O(n) only when nothing else held s, which is never the shape in real programs; iconx itself copies on every concat, so the boards' 8 s / 30 s ceilings did not move — IPL HANG stayed 0 in both modes.
⭐ **MEASURED ON THE DIFF, EVERY RUNNER ITS OWN, AT LOAD 14–17 (the fleet was busy; the same runners cost half on a quiet box, CFO ledger 2026-09-09):** Zona **80/90 both modes** (79 → 80, ilib green; 80 s @ 14.4 → 14.7) · IcnM **749/756 both modes, held** (141 s @ 14.7 → 17.2) · IPL **86/89 both modes** (85 → 86, FAIL 2 → 1, CRASH 2 held = diffu/diffn sig11; 1110 s @ 17.2 → 15.3) · Gimpel **109/116 both modes** (108 → 109; 56 s @ 13.6 → 10.6) · Budne **70/74 held** (42 s) · TPgm **1/2 held** (5 s) · SnoM: see the LEDGER line (the harness needs `--by-modes-column`; the first call refused in 2 s). Nine-shape witness `a_string_held_by_a_frame_variable_is_not_extended_in_place` (starseq, return, local, parameter, global, static, list, augmented, inline; 39 lines cut from iconx) MATCHES both modes and is at the corpus handoff path for hq_V (telegram sent; the orphan gate reads icon 20 over floor 19 until their absorbing commit). ⭐ hq_V absorbed it the same hour (corpus 726448e76, SCRIP d4929f63f, .github 84f639fb; entry procedure_every_suspend_replace_3, re-cut byte-identical) and CORRECTED my shape: it was GREEN on arrival, not red, because the cure was on origin before the cut -- IcnM 754 → 756 of 758 on the absorption. ⛔ **make test stopped at 76 s on `test_gate_banner_leads_with_the_suite_line` (a live-postoffice arm: no suite line in the banner at that instant) — green on the `make -k test` re-run 8 min later; the ONLY red on the full keep-going set is the orphan pair. Both unreachable by this diff (CEO-463).**
⛔ **NAMED, NOT CURED — SIBLINGS OF THE SAME TABLE:** Raku has the identical defect through a frame local (`my $y = $z ~ $z; say $y ~ "a"; say $y ~ "b"; say $y` → `aaa aab aaa`, rakudo `aaa aab aa`): Raku frames do not run the Icon installer, so the switch does not reach them; the Raku lane owns it. Pascal's string `+` is not this bug (it adds ordinals: a larger unsupported shape). The gate on the ceo tree for the banner arm timed out at 60 s under load 17 — inconclusive, so it is named as fleet-live, not proven.
- **CFO-36 (2026-09-09 20:0x → 20:25 CDT, cfo, MODE NONET, Icon only) — THE TRACED-GENERATOR-CALL CRASH CURED AND LANDED (SCRIP `483717a0c`, the hash read from the PUSH RESULT this time): THE GENERATOR CALL ARM RAN ITS PROLOGUE CALL 8 BYTES OFF THE ABI; IcnM 748 → 749/756 BOTH MODES ON THE PUSHED TREE WITH THE PINNED rung03 CRASH FLIPPED BY NAME, ZONA coexpr AND tracer CRASH → FAIL; THREE FLEET REDS STAND, ALL NAMED.**
⭐ **THE PICK AND THE MEASUREMENT.** `gener` (95 lines) is hq_C's set-generation row, so the lane's next red was `coexpr` (CRASH) — and the crash needed no co-expression at all: `&trace := -1; dummy(1)` died in `vsnprintf`'s `movaps` for any GENERATOR callee with ≥1 argument, while a non-generator callee, a zero-argument generator and the untraced call were fine (cx4/cx7/cx10, iconx refs cut). gdb, not theory: si_addr 0 (a general-protection fault, not a stack overflow), the prologue call's canonical frame address ≡ 8 mod 16 and every frame under it inheriting that, and the blob before the call reading `sub rsp,8; sub rsp,8; lea L7; push` — THREE words in the generator regime (`bcps_spine_gen_arm`: the N-2 ABI word hq_I root-caused for the CALLEE BODY's parity, the PL-CALL-ALIGN pad, and L7) pushed BEFORE `call rt_proc_call_open_det`, so that call and everything it reached (`rt_proc_call_prologue_lex → rt_trace_event_args → image → snprintf`) ran misaligned, latent until printf's aligned SSE store. **The cure is a reorder, not a new word:** the three pushes now follow the prologue call; the callee's entry layout is byte-identical (only the call moved above the words; L7 goes through rcx because rax carries the callee pointer there); both mediums, one template. ⛔ My first edit changed nothing because the L7 lea is `x86_lea_id`, not `x86("lea")` — the assertion in my own script said so and I read the line in full before the second cut.
⭐ **MEASURED.** Eleven trace probes and the master's PINNED-CRASH witness `ladder__rung03_suspend_trace_of_a_generator_call_with_an_argument` (`procedure_write_265`) run to their end in both modes — no witness handed: the master already carried the pair and it reports the flip itself. My tree: Zona **77/90 both modes**, CRASH 3 → 1 (`coexpr` and `tracer` CRASH → FAIL, `errors` still CRASH), 23 s @ load 0.7; IcnM **748/756 both modes** (`procedure_write_265` CRASH → PASS), 44 s @ load 1; `make test` **101 arms green in 8 min 14 s @ load 1-2** to the cto's preflight coexpr red, the 26 lines after it by hand green but the coo's identity gate (the same 4 regressions), the 10 after that green but the LAST arm (below). Pushed tree (one rebase, `6a7a966f4` read/reads, 5-line rebuild): Zona 77/90, IcnM **749/756 both modes** (the +1 is 6a7a966f4's).
⛔ **THREE FLEET-WIDE REDS STAND, ALL NAMED, NONE REACHABLE BY THIS ONE-TEMPLATE DIFF (CEO-463):** (1) `test_gate_preflight_arms_stay_cheap.sh` — the cto's coexpr witness arity (CFO-34), unchanged on origin at 20:2x; (2) `test_gate_icon_master_per_entry_identity.sh` — the coo's c703ef2b9 section-bound regressions (CFO-35); (3) NEW: `test_gate_icn_ipl_scan_resume_and_limit_flips.sh` RED datmerge m3+m4 — `datmerge` links IPL `io.icn`, refused at line 365 by `38470889b` (the STAGED, NOT FOR MAIN semicolon commit), exactly as that commit's OWN message predicted ("it reds test_gate_icn_ipl_scan_resume_and_limit_flips, which is IN THE BLOCKING SET, on datmerge"); named to the ceo with CFO-35's Zona rejects.
⭐ **NAMED, NOT CURED, THE LANE's NEXT RUNGS:** trace FORMAT — SCRIP's Icon trace prints `g(2)` and `g failed` but no `suspended`/`resumed` lines, prints the passed arguments only where iconx prints every parameter (`dummy(4,&null,&null,&null,&null)`), and images a list as `list_1(3)` where iconx prints `list_1 = [...]`: `tracer` 83 of 85 lines, `coexpr` 33 — one class, the cheapest remaining FAIL in A-L; `errors` crashes in `memcmp` from compiled code at its first `&error`-trapped error (rc 139, a different class); `gener` is hq_C's.
- **LEDGER 2026-09-09 20:25 CDT (cfo):** credits: no reading from Lon · load 0.7-1.8 on 16 cores · runaways 0 · disk 86G/125G 72% flat · flips this sitting (five landings: a76ab4b70 large, 183aee4a1 static callee, 08905f250 evalx, 483717a0c the trace crash, plus the .github lines): IcnM 740 → 749/756 both modes (four of the nine are my witnesses or my cures by name, the rest the coo's and other seats'), Zona 76 → 79 → 77/90 (evalx mine; the −2 is 38470889b's five rejects, the ceo's ruling), three CRASHes → FAIL or PASS · cost per flip: not computable without a credit reading. **BOARD COST on the quiet box, 20:1x-20:2x:** Zona **23 s @ load 0.7** · IcnM **44 s @ load 1** · `make test` to the fleet red at arm 102 **8 min 14 s @ load 1-2** · a one-template compiler rebuild **2 s** (4 lines) · the recipe-by-hand method: `make -n test` → the lines after the red arm → `bash -e`, 1 s to set up, used six times this sitting.
- **CFO-35 (2026-09-09 19:25 → 20:0x CDT, cfo, MODE NONET, Icon only) — Arizona `evalx` CURED AND LANDED (SCRIP `08905f250`; ⛔ the hash first written here and mailed at 19:57 was `aae1f8513`, ANOTHER SEAT's commit read while my rebase was stopped on a conflict -- the third time this sitting a hash was written before the push was confirmed, corrected by mail at 20:0x; the rule stands and I broke it: read the hash from the PUSH result, never from git log while a rebase is open): A REVERSIBLE SWAP TAKES ANY VARIABLE OPERAND, A CONJUNCTION IS AN LVALUE, AND `?` DRAWS ONLY AFTER IT KNOWS THERE IS SOMETHING TO PICK; Zona 78 → 79/90 BOTH MODES ON THE MERGED TREE (evalx mine, `misc` the coo's); TWO MASTER ENTRIES REGRESSED ON ORIGIN, ATTRIBUTED TO c703ef2b9 BY A/B.**
⭐ **THE PICK:** `next` had no free row for this seat, so the lane rule (Arizona A-L) chose: evalx was the SMALLEST red — 8 diverging lines of 304 and a silent stop at line 293 with rc=0. **THREE DEFECTS, ALL PROBED AGAINST iconx BEFORE ANY CURE.** (1) `x <-> y :=: z` lowered onto an IR_FAIL with its operands wired INTO it and the program VANISHED — no output at all, not even lines before it in some shapes, rc=0. TT_REVSWAP implemented only name/name; the general shape now mirrors the swap's: both operands through `lower_lvalue_var`, then two IR_REV_ASSIGN_VAR boxes over the dereferenced old values (b ← a, then a ← old b, the second's ω routed to the first's β so a resume restores both), result the left variable as iconx yields it; `lower_lvalue_var` accepts `<->` and `<-` as composite lvalues (so `x <-> y <-> z` resolves) — 17 probe shapes MATCH, including a subscript, a field, an assignment and an alternation as the right operand and the restore on backtracking. (2) `(1 & 2 & 3 & x) := 3` was error 111: TT_CONJ is N-ARY (the AST flattens `&`), so the lvalue is its LAST child with the head lowered as a synthetic conjunction of the rest; the reversible-assign dispatch list widened to the same kinds (the first cut used c[1] and silently fell back to the value path — the AST dump said n=3). (3) `?30` printed 9 for 27 because `rt_random_var` drew the next `&random` BEFORE testing emptiness: `?table()` on an empty table failed one draw ahead of iconx (probe: iconx `fail 0` on `?""`, `?''`, `?[]`, `?set()`, `?table()`; SCRIP advanced on each) and every later `?` was off by one; the draw is now taken after each check, and a real operand truncates (`?2.5` is `?2`, iconx measured). Named, not cured: `?-3` is runtime error 205 in iconx and a silent fail here (no shared File/Line/Traceback raiser in the runtime), and `x <-> variable("y")` hits `lower_lvalue_var`'s standing REFUSE. Three witnesses (23, 14 and 10 lines) MATCH both modes and are handed to hq_V.
⭐ **MEASURED.** My tree: evalx 0 diff both modes; Zona **78/90** both modes (37 s @ load 1.7), IcnM **749/756** both modes (45 s @ load 3; +2 over 747: `procedure_every_alt_replace_12` and `procedure_record_every_replace_13` flipped by the swap/conjunction cures). `make test` **101 arms green in 8 min 43 s @ load 2-5** to the cto's preflight coexpr red (still on origin at 19:47), the 26 recipe lines after it by hand green but `test_gate_icon_master_per_entry_identity.sh` (below), the 10 after it green. Rebased onto c703ef2b9 (214-line rebuild): Zona **79/90 both modes** (the +1 is `misc`, the coo's shape-A landing), IcnM **747/756 both modes**.
⛔ **TWO ENTRIES REGRESSED ON ORIGIN, NOT ON MY DIFF — A/B, 1 min, incremental:** `procedure_write_273` (origin `ladder__rung20_section_bound_is_a_generator_and_its_first_run_is_not_lost`) and `procedure_write_274` (rung19) FAIL both modes: `"abcdef"[1:(2|4|6)]` yields `abc abcde` and loses `a`. With my two files replaced by origin main's (the coo's `c703ef2b9` in, my diff out) both FAIL identically; with my diff back, identically; on my tree at 183aee4a1 + my diff (boards8) they PASSED. The only code commit between is c703ef2b9 ("an assignment or a SECTION in argument position stages as a VARIABLE") — the section staging enters a generator bound at β, the class those rungs pin. Named to the coo and hq_V; the identity gate is fleet-red until the coo's cure; landed past it under CEO-463 with the measurement. ⭐ The method is the cost table's: `git checkout origin/main -- <my files>`, `make` (5 lines), run, `git checkout HEAD -- <files>`, `make` — one minute for an attribution that would otherwise be an argument.
⛔ **THE RANDOM HALF COLLIDED:** `aae1f8513` (another seat, one minute before my push) cures the same `?` draw defect by saving and restoring `&random` around the body when it fails; the rebase conflicted inside `rt_random_var` and I resolved to THEIR shape, keeping only my real-operand truncation — so on origin the lazy draw is theirs and my commit message overstates my share of it (it cannot be amended after the push; this line is the correction). Two seats cured one defect within the hour because evalx's `?30` and their witness pinned the same draw: the bus had no row for it and neither of us claimed one — the cost of an unclaimed cure is one conflict and one wrong hash.
⛔ **ON THE PUSHED TREE Zona READS 77/90 WITH REJECT=5** — `cfuncs extlvals ilib mega spellnum` are compile-REFUSED by `38470889b` ("icon parser: refuse the four semicolons icont refuses -- STAGED, NOT FOR MAIN until the ceo rules on io.icn", on main since 19:5x) at IPL `procs/io.icn` line 365; `mega` and `spellnum` were PASSING at 79/90 twenty minutes earlier. Not mine, measured by running the five; named to the ceo (a not-for-main commit on main) and the coo; not reverted (another seat's landing is the ceo's call). IcnM 747/756 both modes unchanged on the pushed tree.
- **LEDGER 2026-09-09 20:0x CDT (cfo):** credits: no reading from Lon · load 1.5-2.4 on 16 cores since 19:35 (the NONET went quiet) · runaways 0 · disk 86G/125G 72% flat · flips this sitting: Zona 76 → 79/90 both modes (evalx mine, `fncs` and `misc` the coo's), IcnM 740 → 749 (on my tree; 747 on origin with the coo's two regressions), ilib `large` line cured, ilib itself still red on movecorr's shape A (the coo's c703ef2b9 was meant to cure it; not re-measured this line) · cost per flip: not computable without a credit reading. **BOARD COST on the quiet box:** `test_icon_arizona_suite.sh` **37 s @ load 1.7** (against 69 s @ 11) · `board_icon_master.sh` **45 s @ load 3** (against 76 s @ 10) · `make test` to the fleet red at arm 102 **8 min 43 s @ load 2-5** (against 17-19 min @ 8-16: the blocking set costs HALF on a quiet box, which is the number the fleet-quiet-window row wants) · the 101 → 102 arm count moved because the recipe grew by one arm this hour.
- **CFO-34 (2026-09-09 18:4x → 19:25 CDT, cfo, MODE NONET, Icon only) — movecorr SHAPE B CURED AND LANDED (SCRIP `183aee4a1`, pushed before this line; CEO-475): A CALL THROUGH A STATIC VARIABLE CALLS ITS VALUE; ilib NOW WAITS ON THE COO's SHAPE A ALONE; TWO MORE FLEET-WIDE `make test` REDS NAMED TO THEIR OWNERS; ONE UNEXPLAINED SIGTERM KILLED A BLOCKING SET.**
⭐ **THE CURE IS ONE PREDICATE IN TWO CONDITIONS (`lower_icon.c`).** The static is renamed `<proc>__STATIC__<name>` by a pre-pass BEFORE the call lowering, and the direct-call test in TT_FNC (and its twin in TT_BANG_BINARY's `!` apply) knew locals, own globals and reassigned procedures but not that shape, so `q(v)` went out BY NAME to a procedure that does not exist (ERROR 022) — `image(q)` read the value fine, which hq_V confirmed independently on a four-line cut (the static HOLDS the value; only the call path misread it). `icn_is_static_mangled_name` (an exact match against the synth-exclusion list the pre-pass fills) added to both conditions; a static callee lowers to IR_CALL_VALUE like a local one. ⛔ My first cut tested the UN-mangled name and changed nothing; `--dump-ir` of the static against its local twin said why in one line (`CALL "nm__STATIC__q"` against `CALL_VALUE [3,4]`) — the dump, not a theory. Eleven one-shape probes and witness `call_through_a_static_variable_reads_the_static` (10 shapes) MATCH iconx both modes; hq_V absorbed it red-on-arrival (corpus `2e4610172`, xfail stripped a fourth time). The static-plus-`!R1` shape (IPL movecorr itself) now falls through to the coo's shape A (claimed under CEO-477) instead of dying: ilib's last rung is theirs.
⭐ **MEASURED, on three trees because origin moved 9 commits in the hour.** My tree: Zona 76/90, IcnM 741/754 both modes (the +1/+3 over CFO-33 is the absorbed `large` witness flipping by name). Rebased onto ded51d003 (a 201-line rebuild, 3 min 53 s @ load 12): Zona 76/90 (54 s @ load 10), IcnM **744/756** (76 s @ load 9). The push hit a CONFLICT on the very condition I edited — the coo's `9874e2faa` (CEO-477, a SNOBOL4 keyword name may not govern an Icon identifier) added `icn_undeclared_assigned_var` to the same line one minute before — resolved by keeping BOTH predicates, rebuilt (14 lines), all probes re-run green, boards re-pinned: Zona **77/90 both modes** (53 s @ load 7; the +1 is `fncs`, red on my tree WITH my cure and green only once 9874e2faa joined it, so it is the coo's flip), IcnM **747/756 both modes** (64 s @ load 6; three entries flipped between the two trees — `procedure_write_257`, `procedure_record_every_replace_16`, `procedure_record_every_replace_14` — one of them my static witness under whatever number hq_V's re-sort gave it, the rest the coo's; the coo's board attributes by origin name, which is why the AND number is theirs to state). The blocking set: `make test` **98 arms green in 17 min 9 s @ load 11-16** to a fleet red (below), the 26 recipe lines after it by hand green but one more fleet red (below), the 6 lines after that green (`make -n test` → tail → `bash -e`, three times this sitting: the recipe method is now routine and costs 1 s to set up).
⛔ **TWO FLEET-WIDE REDS, NEITHER REACHABLE BY A ONE-PREDICATE DIFF (CEO-463), BOTH NAMED.** (1) `test_gate_preflight_arms_stay_cheap.sh` RED since the cto's `8070814cd` (the co-expression capture, 18:19): the preflight arm `test_gate_coexpr_stack_of_uncreated_thread.sh` compiles a C witness that calls `scrip_coexpr_create` with THREE arguments and `rt_coexpr.h` now declares FOUR (`below_bytes`), gcc refuses, the arm reads rc=2, the census reads RED — measured twice on my tree and the witness line unchanged on origin at 19:14; every seat's blocking set stops there; named to the cto and the coo. (2) `test_gate_master_order_is_the_builders_order.sh` RED on the icon master at corpus `05dbe274e`: 49 of 908 entries out of order from index 859, the absorption of my `large` witness changed a sort key without a re-sort; named to hq_V with the gate's own cure line (`util_build_master_suite.py --lang icon --resort`).
⚠ **AN UNEXPLAINED SIGTERM.** The first blocking set for this landing (18:34 chain) died at 18:38-18:39 inside `test-postoffice` at `test_gate_dispatch_bus_failure_modes.sh` — `make: *** [Makefile:148: test] Terminated`, the whole background subshell gone with it, no `MAKE_TEST_RC` written. The gate has no kill (only a `trap` for its tempdir), `s4e_msg.sh` and the two libs with the word have none either (comments and `kill -l`), and the re-run 9 min later passed the same arms and ran 17 min. Something outside my session sent SIGTERM to my process tree at that minute; I did not find what. Recorded so the next seat whose set dies with `Terminated` and no red arm looks OUTWARD (a runaway killer, another seat's `pkill -f` pattern) before re-running blind — the cost was one 4-minute head start and a 20-minute re-run.
⭐ **PROCESS HELD:** push, read the hash, THEN write — three times (a76ab4b70, and 183aee4a1 after a conflict); a cursor written before the push would have carried a713514a4, a hash that no longer exists.
- **CFO-33 (2026-09-09 17:5x → 18:4x CDT, cfo, MODE NONET, Icon only) — `large` CURED AND LANDED (SCRIP `a76ab4b70`, pushed before this line): `&allocated` IS A LIVE COUNTER, ilib's LAST cfo-OWNED RUNG IS GONE AND ilib STAYS RED ON movecorr ALONE, WHICH IS TWO SHAPES — ONE THE COO'S, ONE MINE UNDER CEO-475 AND CUT; ZERO BOARD MOVEMENT BY DESIGN (the witness hq_V absorbed red-on-arrival flips on the next board).**
⭐ **THE CURE (runtime, three files, CEO-471 lifted the CEO-447 hold on the rtx allocator fast path):** two hidden counters in `gc_heap.c` (`g_rt_alloc_total`, `g_rt_alloc_str`) fed by the block size at every site that carves or grows a block — `rt_gcheap_carve` (every C gcheap path), `rt_ws_alloc_core` (the workspace island), `rt_sxt_extend` (the in-place string extension adds the delta it bumps the top by), and the two rtx fast paths in `rtx_alloc.s`, counted AFTER the last bail to the slow path so a fast path and its C fallback never both count. `&allocated` generates total, 0 (static), string bytes, block bytes. Witness `large_integer_is_detected_by_allocated_growing` MATCHES iconx both modes (⚠ it needs 30-digit literals: a 19-digit one is a NATIVE integer in iconx and `large` prints `small` — the first cut of the witness said so and I read it before believing SCRIP); hq_V absorbed it as `procedure_every_alt_47` (corpus `05dbe274e`), re-cut from the oracle byte-identical, RED on arrival with the XFAIL marker stripped so it can report the flip. ⭐ Where SCRIP's representation differs from iconx's the counter says so and I left it: a constant concat is folded, a substring copies, a real is unboxed, a cset is materialised — `&allocated` follows SCRIP's own allocation, never a projection of iconx's.
⭐ **MEASURED.** ilib's `large` line MATCHES; Zona **76/90 both modes** (50 s @ load 6-7 on my tree, 69 s @ load 11 on the rebased tree), IcnM **740/751 both modes** (65 s @ load 7, 76 s @ load 10; the +2 passes over CFO-32 are other seats'), unchanged by this landing as a criterion/instrument change should be. `make test` **116 arms green in 19 min 28 s @ load 8-16** to the fleet red at `test_gate_icn_function_builtin_names_dispatchable.sh` (hq_R's 38118d03f, named in CFO-32; hq_R's cure `bdd554570` reached origin DURING my run and the arm is GREEN on the rebased tree), plus the 8 recipe lines after it by hand in 9 s (`make -n test` → the lines after the red arm → `bash -e`; the recipe is 184 lines, not 300 arms). Three rebases in one landing (d4d19848c → 6068efe5a → c9f9c7371 → a76ab4b70): the fleet pushes every few minutes now, and the boards were re-pinned on the tree that was pushed, not the one that was measured first.
⛔ **ilib ON THE COO's movecorr CURE (412cfd897): ZERO FLIPS, counted as CEO-471 asked** — rc=1, 233 lines, ERROR 022 at the same line in both modes. Eight one-shape probes against iconx split it in two: **(A)** a value-call whose ARGUMENT IS AN ELEMENT GENERATOR yields nothing (`p := proc("name", 0); every write(p(!R))` prints 0 lines; `p(R[1])`, `p(R.m)` and the direct `name(!R)` MATCH) — the coo's, taken as their next witness under the argument-staging class; **(B)** a call THROUGH A STATIC VARIABLE is ERROR 022 whatever its name (`static q; initial q := dbl; return q(v)`; also `(q)(v)`, `q ! [v]`, plain assignment without `initial`; `image(q)` reads it fine) — the ceo's CEO-475 makes it mine. `--dump-ir` of the static against the local twin said it in one line: the static is renamed `nm__STATIC__q` by a pre-pass BEFORE the call lowering, and the direct-call test in TT_FNC (and TT_BANG_BINARY's twin) knows locals, own globals and reassigned procedures but not that mangled shape, so the call went out BY NAME to a procedure that does not exist. Cure: one predicate (`icn_is_static_mangled_name`, an exact match against the synth-exclusion list) added to both conditions; ⛔ my first cut tested the UN-mangled name and changed nothing — the IR dump, not a theory, said why. All eleven probes MATCH; witness `call_through_a_static_variable_reads_the_static` (10 shapes: initial, assignment, parenthesised callee, `!` apply, the builtin `name` through a static named `name` and through another name, `reverse`, a generator through a static, a record field through `name` in a scan, and `image` as the control) MATCHES both modes and is at the handoff path. The static-plus-`!R1` shape (IPL movecorr itself) now falls through to shape A instead of dying — ilib waits on the coo's half. Boards + blocking set running; CFO-34 carries the hash.
⭐ **hq_R's 38118d03f root cause, kept as written:** a count in prose beside a gate measuring the same count is always the staler of the two — the gate's own header said eight names were omitted when one was, and the arithmetic was closed against the prose. And hq_R's cured gate showed the freshness guard I added (47781cd12) and the hardcoded root were ONE defect facing two ways: the guard certified the caller's binary while the body graded hq_R's checkout.
- **LEDGER 2026-09-09 18:4x CDT (cfo):** credits: no reading from Lon this sitting · load 2.0 at 17:51 → 16.1 at 18:08 → 8-11 since, on 16 cores (nine-seat NONET compiling, not runaways) · runaways 0 (every 100% process censused was a compile or a scrip run aged 0-2 s) · disk **86G/125G 72%, UP 10G since 09-08 22:57's 76G/64%** — named, not chased this sitting; the `/tmp` census row is PARKED-LON-HOLD · flips: ilib `large` line cured (no program flip: ilib waits on movecorr's shape A), the absorbed witness flips on the next IcnM board · cost per flip: not computable without a credit reading. **19:25 addendum:** load 5.9/8.7/11.6 · runaways 0 · disk 86G 72% flat · flips this sitting: IcnM 740 → 747/756 both modes across four landings (two absorbed witnesses of mine flipped by name, the rest the coo's and other seats'), Zona 76 → 77 (the coo's `fncs`), ilib `large` line cured, ilib program still red on one rung (the coo's). **BOARD COST this sitting, at the load it ran under:** `test_icon_arizona_suite.sh` 50 s @ load 6-7, 54 s @ load 10, 53 s @ load 7 and 69 s @ load 11 · `board_icon_master.sh` 65 s @ load 7, 64 s @ load 6 and 76 s @ load 9-10 · `make test` to the fleet red at arm 117 **19 min 28 s @ load 8-16**, and to the fleet red at arm 99 **17 min 9 s @ load 11-16** · a 201-line rebuild after a 9-commit rebase 3 min 53 s @ load 12 + the 8 recipe lines after it 9 s · full incremental `make` after a 17-file fetch 3 min 51 s @ load 2 · a runtime-only rebuild 1.5 s, a lowerer rebuild ~15 s · hq_B's second point on the ilib curve: **1.4 s @ load 13 against 0.9 s @ load 5** — duration tracks load, `TIMEOUT=8` is ~6× the quiet-box measurement, so a NONET load spike eats the margin; that is hq_T's instrument finding (a whole-suite cap tuned near a program's duration is flaky, not tight), not ilib's. ⚠ `board_icon_master.sh` REFUSED once (rc=2, 1 s, "the harness printed no MODES_COLUMN split") at 18:32 straight after the corpus ff-merge to 05dbe274e; the re-run in the CFO-34 chain says whether it was the merge or the moment.
- **CFO-32 (2026-09-09 17:0x → 17:21 CDT, cfo, MODE NONET, Icon only) — `ilib`'s SIXTH RUNG LANDED (SCRIP `f6b4e978d`, pushed before this line): A LINK RESOLVES IN THE LIBRARY BEFORE THE PROGRAM'S DIRECTORY — A CRITERION CHANGE FOR EVERY ARIZONA PROGRAM THAT LINKS AN IPL NAME, MEASURED AT ZERO BOARD MOVEMENT; CEO-468's HANG IS A TIMEOUT, MEASURED SIX WAYS; TWO FLEET-WIDE `make test` REDS, ONE CURED AS HYGIENE (`47781cd12`), ONE NAMED.**
⭐ **RUNG 6, driver only (`icn_link_open`): IPATH → ICONPATH → the exe-relative IPL → the program's directory LAST.** Arizona `general/` ships 26 IPL-named files beside its tests (18 old library copies, 8 test programs with a `main`); icont links compiled `.u` from its library and upstream's `icont -o` leaves none beside the tests, so every `.std` was cut against the IPL. A local name the library lacks (`io_lib`, `lists_lib`, `scan_lib`, `cfunc`) still resolves. MEASURED on the tree: Zona **76/90 both modes** (59 s at load ~5; hq_B's 76 on the merged tree, so the criterion change moved NO program in either direction), IcnM **738/750 both modes** (the +22 entries and +18 passes are other seats' landings and hq_V's absorptions — the watermark re-pin belongs to the commits that earned them), Jcon **66/82 both modes** (one Jcon program links, a local name — the change cannot reach it), `ilib`'s options line MATCHES; its diff is `large` (below) and `movecorr` (the coo's row, being cured). The blocking set: `make test` 3 min 5 s to a fleet red at arm 166 + 129 arms by hand (10 min 53 s to a second fleet red at arm 98 of those) + the last 31 arms (3 s): green everywhere but the two reds below. The coo names it on the board as a criterion change, as asked.
⛔ **CEO-468 CLOSED BY MEASUREMENT: ilib's m3 "HANG" is the runner's 8 s ceiling firing on a 0.9 s program.** Bisect by incremental checkout over six commits (each a real rebuild — the diff counts differ per step): rc=1, 233 lines, no hang anywhere; m3 0.84-0.93 s over five runs in the runner's exact shape; hq_B's OWN binary from hq_B's OWN corpus directory: 0.91 s, FAIL. No commit named, no co-signer owed; the coo's ledger reads "kind = timeout under load". ⭐ The incremental-checkout bisect cost **47 s for six commits** against ~35 min for ONE worktree build (CFO-30): recorded below.
⛔ **TWO FLEET-WIDE REDS IN ONE BLOCKING SET.** (1) arm 166, `runners_refuse_on_a_stale_binary` census #2: hq_R's `test_gate_icn_ipl_scan_resume_and_limit_flips.sh` (wired at 27f0235da) executes `./scrip` with no freshness guard — one idiom line added, census 146/146, committed alone as `47781cd12` (the c0703a28c shape), hq_R told. ⚠ THE SAME GATE HARDCODES `R=/home/claude_R` and takes its binary and corpus from there, so on every other seat's console it grades hq_R's tree, not the tree being landed — named to hq_R, not touched (the guard I added checks the seat's own binary, which is the right one). (2) `test_gate_icn_function_builtin_names_dispatchable.sh` ARM 4a: `function()` yields `display` and SCRIP cannot dispatch it (ERROR 022) — red WITH and WITHOUT my change (stash, rebuild, rerun), from `38118d03f` (the IPL ifncsgen landing); named to the coo and hq_R. ⭐ A mail body with a dollar-paren was REFUSED by the bus (it refuses on the shape, no bypass) — the rewrite in words went through; CLAUDE.md already says so for backticks.
⭐ **`large` (ilib's last cfo-owned line) IS NAMED, NOT CURED, ON PURPOSE:** IPL `large(i)` returns i only if `&allocated` GREW across `i +:= 0`; SCRIP's `&allocated` is `INTVAL(0)` (keywords.c). A live counter needs one 8-byte add in `c_rt_ws_alloc` AND in the rtx assembly fast path (`rtx_alloc.s`, `RTX_GATE(alloc, …)`) — the register-aware assembly is not a one-file lowerer cure and not mine under CEO-447; `mega.icn` (M-Z) reads it too. The cut is written here for whoever owns rtx. ilib stays red on `movecorr` (the coo's) and `large` (this) — SEVEN of its nine rungs are mine and cured (1 args-class `===`; 2 `<-` at α; 3 case arms; 4 reassigned procedure names; 5 = 3's twin `{}`; 7 `|e`; 8 `name(r[i])`; 6 the link resolver).
- **CFO-31 (2026-09-09 11:0x → 16:58 CDT, cfo, MODE NONET, Icon only) — `ilib`'s EIGHTH RUNG LANDED (SCRIP `b1db664a9`, pushed before this line): `name(r[i])` ON A RECORD IS `type.field`; THE SIXTH RUNG IS FOUND AND IT IS THE LINK RESOLVER, NOT A SEMANTIC; CEO-468's ilib HANG BISECT IS RUNNING BY INCREMENTAL CHECKOUT, NOT WORKTREES.**
⭐ **RUNG 8 (runtime, two lines):** the record positional subscript's cell now carries the record and `-(field+1)` as its position and `name()` prints `type.field`; readers of `pos`/`sv` audited. Witness `name_of_a_record_positional_subscript_is_type_dot_field` MATCHES both modes, handed to hq_V (their rung-7 verification: byte-identical to the oracle, queued behind CEO-462's stand-off on the master files). Measured: Zona 75/90 both modes, IcnM 716/728 both modes, `make test` green end to end 22 min 50 s at load ~9. ilib's diff after it: `large`, `options`, `movecorr`.
⭐ **RUNG 6 IS THE LINK RESOLVER.** Six reductions of `options` MATCHED iconx (the spec loop, mutual evaluation, variadic apply, the whole IPL procedure with ilib's own `tbl`, the six calls in ilib's order, ilib with the section moved first), every ilib variant with any section or even ONE `gen(...)` line deleted MATCHED, padding a passing variant with 120 statements or 60 string literals did NOT break it — and the passing variants all lived in my scratch directory while the failing run was the file in the suite directory. `icn_link_open` searches the PROGRAM's directory before IPATH, and Arizona `general/` ships 26 files with IPL names: 18 old library copies (`options.icn` is the 1989 one, single-letter specs only — it CANNOT print `abc:-`) and 8 TEST PROGRAMS with a `main` (`lists.icn`, `sets.icn`, `io.icn`…) that `link lists` would load as a library. icont links compiled `.u` from its library and upstream's `icont -o` leaves no `.u` beside the tests, so every `.std` was cut against the IPL. Cure scripted, driver only (IPATH → ICONPATH → the exe-relative IPL → the program's directory last): one search order, measured on Zona + IcnM + Jcon before landing. ⛔ FOUR HOURS OF A FALSE THEORY ("state leak from an earlier section") because I bisected the program's TEXT while the variable was the program's PATH — the instrumented copy in a scratch IPATH that "was never read" was the tell, and I read it as a mystery instead of a fact. Lesson for the cost table: when two runs of the same text differ, diff the ENVIRONMENT (cwd, path, env) before the text.
⛔ **CEO-468 (16:48): ilib HANGS in m3 and FAILS in m4 on 21fdd28f7 (hq_B, Zona 76/90 both modes, the modes diverged in KIND).** Bisect running now over HEAD, 96414a712 (mine, control), cb993dd4c (hq_R), b3039f014 (hq_C), cab8a9469, 21fdd28f7 (hq_I) — by `git checkout` + INCREMENTAL `make` in the main checkout (objects are per-path, so each step recompiles only what changed: ~1-3 min a step) instead of a worktree (35 min a build, CFO-30): the cost table said which. Each step runs ilib in m3 under a 20 s timeout and records rc, line count, diff count and the clock. The owner of the named commit co-signs the cure (CEO-468). Rung 9 is the coo's row `icon-call-through-a-value-dereferences-its-subscript-argument-ir-call-value` (CEO-464): attributed, not cured under ilib.
- **CFO-30 (2026-09-09 10:3x-11:0x CDT, cfo, MODE NONET, Icon only) — `ilib`'s SEVENTH RUNG CURED AND LANDED (SCRIP `96414a712`, pushed BEFORE this line was written): A REPEATED ALTERNATION IS A GENERATOR WHATEVER ITS OPERAND; THE FOURTH RUNG'S WITNESS IS IN THE MASTER AS RUNG 21 BY hq_V's HAND; THE NINTH RUNG IS ATTRIBUTED PRE-EXISTING AND ROUTED; THE SIXTH IS STATE-DEPENDENT AND HALF-BISECTED; THE EIGHTH IS CUT.**
⭐ **RUNG 7, one line.** `is_resumable` treated `|e` (TT_REPALT) as a plain unary and asked its operand, so `|1` was "not resumable" and TT_CONJ never recorded it as the conjunction's resume point: `(|1 & 3) \ 3` gave one 3, `(|1 & (5 | 6)) \ 5` gave `5 6`, and IPL `randseq` (`suspend |?1 & &random`) gave 2 numbers of 14. `--dump-ir` said it in one line: `LIMIT [4,1,9@]` with 9@ a GOTO to the exit, against `LIMIT [4,1,2]` for the `(1|2)` twin. Witness `repeated_alternation_resumes_inside_a_conjunction` (9 shapes) MATCHES both modes, handed to hq_V at the handoff path. Measured on the merged tree: Zona **75/90 both modes** (the +1 is the coo's `traps`, b8b7bcf9b — reconciled by mail, my 74 predated it), IcnM **716/728 both modes** (the +2 are the coo's), `make test` **green end to end, 22 min 43 s at load ~9**. ⭐ PROCESS HELD THIS TIME: push, read the hash, THEN write.
⭐ **hq_V ABSORBED RUNG 4's WITNESS (corpus `23638085f`, entry procedure_write_269, home `ladder__rung21_…`), re-cut from the oracle twice rather than trusted (CEO-410) and byte-identical to what I handed** — the handoff path works as a one-writer channel. The rung-7 pair sits there now.
⛔ **RUNG 9 (movecorr, ERROR 022) IS NOT MINE AND WAS NEVER MINE — ATTRIBUTED BY MEASUREMENT:** origin `b8b7bcf9b` built in a scratch worktree WITHOUT rung 4 gives the identical output on both probes (r_static, r_nm2). The shape: `p := proc("name", 0); p(x)` FAILS where iconx names x — a call THROUGH A VALUE hands `name` a dereferenced argument; the direct call `name(x)` is right. Named to hq_U (CEO-445's dereference class); hq_U's 10:5x routing correction: the class moved to the COO at CEO-452 and — the useful part — IR_CALL_VALUE is a SHARED node (icon=2, prolog=1 lowerers) distinct from the argument-LIST cut the coo holds, so the witness may not be inside that cut; hq_U forwarded it to the coo with that caveat. ⭐ THE WORKTREE COST: a full `-O0` build of a second checkout took **~35 min at load ~10** (275 compile lines) — the price of one attribution; cheaper than arguing, dearer than I guessed (3 min). Recorded in the cost table.
⚠ **RUNG 6 (`options`, spec `"-abc: -s: irvb"` read as `a:1 b:1 c:-`) IS STATE-DEPENDENT:** the spec-parse loop alone MATCHES, mutual evaluation `(pos(1), tab(0))` MATCHES, variadic `p ! args` MATCHES, the whole IPL `options` with ilib's own `tbl` and all six calls in ilib's order MATCHES — and ilib with its options section MOVED TO THE TOP OF main MATCHES on that line too. So one of the seven sections before it (convert, datetime, factors, io, lists, math, numbers) leaves runtime state that breaks exactly `optname == "-"` on the fifth call. Halving next; a state leak found this way is worth more than the one line it costs ilib.
⭐ **RUNG 8 IS CUT (runtime, two lines):** `name(r[i])` on a record FAILED silently — the record positional subscript became a variable (CFO-26) but its cell carried no record, so `name()` recognised nothing — IPL `field(R, i)` scans `name(R[i])` for the dot. The cell now carries the record and `-(field+1)` as its position, and `name()` prints `type.field`; readers of `pos`/`sv` audited (substring paths need `len > 0` or DT_N, list paths need `pos > 0`). Witness `name_of_a_record_positional_subscript_is_type_dot_field` cut from iconx; measuring now. `large` (`&allocated` is `INTVAL(0)`, a constant) stays named, not cured: an instrument-fidelity keyword, not a semantic.
- **CFO-29 (2026-09-09 10:0x-10:2x CDT, cfo, MODE NONET, Icon only) — `ilib`'s FOURTH RUNG CURED AND LANDED (SCRIP `f48a3f0c7`; the hash first written here and mailed at 10:2x was b8b7bcf9b, the coo's argument-staging push that my rebase stopped on -- the SECOND time this sitting a cursor and its mails were written before the push was confirmed: the rule from here is push first, then write): A CALL THROUGH A REASSIGNED PROCEDURE NAME READS THE GLOBAL; `ilib` NOW RUNS TO ITS END AND THREE RESIDUAL RUNGS ARE PINNED, THE SEVENTH ALREADY CUT; THE WITNESS IS HANDED TO hq_V, NOT ABSORBED (CEO-452).**
⭐ **THE CURE, LOWERER ONLY (the cto's one line: "yours, icon-only", CEO-429 is SNOBOL4's DEFINE and parked; the frame freeze CEO-447 is untouched — a CALL_VALUE through a global is the box that stays inside it).** IPL `lfliph := lreverse; return lfliph(L)`: SCRIP bound the call to the declaration (recursion to ERROR 246, ilib lost 260 lines), the store went to an implicit LOCAL and main's read was a static IR_PROC_VALUE. Now an AST pre-pass over the whole (link-merged) tree collects every assignment target; a target that is also a declared procedure is REASSIGNED: registered as a global exactly as `global` does (`global_register`), excluded from implicit locals, called through IR_CALL_VALUE, left as a global read by the resolver, and initialised in main's graph with `name := PROC_VALUE(name)`. ⛔ THREE WRONG CUTS BEFORE THE RIGHT ONE, each measured, each 2 min: (a) the set filtered by `icn_is_proc` at SCAN time was EMPTY because stage 2 scans before the procedure table exists and the per-program guard then skipped the later scan → filter at QUERY time; (b) with the scan fixed the store STILL missed the read — the two-procedure probe `a := b` in a, `image(a)` in main printed "procedure a" — until the `global a` probe proved the existing global machinery does the rest: `global_register` was the missing line; (c) the linked IPL gave error 106 between (a) and (b) for the same reason. Witness `call_through_a_reassigned_procedure_name_reads_the_global` MATCHES iconx both modes; every earlier witness and probe (w829, revassign, case-arm, lclose, date, gen, eq, case, rev) MATCHES.
⭐ **MEASURED.** `ilib` runs to its END now: 3 diffs of 319 lines remain — `large` (IPL detects a large integer by `&allocated` GROWING across `i +:= 0`; SCRIP's `&allocated` is `INTVAL(0)` in keywords.c, a constant), `options` (the spec form `"-abc:"`, a multi-letter option name: SCRIP reads `a:1 b:1 c:-` where the oracle reads `abc:-`), and `randseq` (`suspend |?1 & &random` yields 2 numbers of 14). The last is CUT already: `is_resumable` treats `|e` (TT_REPALT) as a plain unary and asks its operand, so `|1` is "not resumable" and a conjunction with `|e` on the left never resumes it — `(|1 & 3) \ 3` gives one 3, `(|1 & (5|6)) \ 5` gives `5 6` — one line (`TT_REPALT → 1`) scripted, witness `repeated_alternation_resumes_inside_a_conjunction` (9 shapes) cut from iconx, waiting only on this landing. `test_icon_arizona_suite.sh` 74/90 both modes (1 min 11 s at load ~9) — and ⚠ its FAIL line now reads `FAIL=11 CRASH=4 HANG=1`: the runner's own `ecc19503b` ("FAIL means wrong answer, the five crashes hiding inside it now say so"), the same 16 non-passes, not a regression — checked against the runner's log before believing the word CRASH. `board_icon_master.sh` **714/728 both modes**, the identical red set (1 min 45 s at load ~9). `make test` **green end to end, 17 min 45 s at load ~9** (the arm-190 red was hq_C's and is green at 71d8bd89b, hq_C's 10:0x mail: two of my three names were the census's own over-matches, one real).
⭐ **THE WITNESS IS HANDED, NOT ABSORBED (CEO-452: hq_V is the one master writer).** `/home/resources/postoffice/handoff/cfo-icon-witness-call_through_a_reassigned_procedure_name_reads_the_global-2026-09-09.{icn,ref}`, hq_V told by mail. The orphan gate pins icon's loose pairs at 19 and the declared-unabsorbed cap is full, so a pair in `corpus/tests/icon/` is a red for every seat — the handoff directory is the one lawful resting place for a witness the master writer has not yet taken.
- **CFO-28 (2026-09-09 09:3x-09:5x CDT, cfo, MODE NONET, Icon only) — THREE OF `ilib`'s FOUR RUNGS CURED AND LANDED (SCRIP `20a3c797c`, corpus `669be1d7c`; the hash first written here and mailed at 09:5x was 17ac1bc39, origin's head at the moment the rebase stopped on a floor conflict with hq_C's 709 -- resolved to 713, then re-pinned 714 on the rebased tree: m3 714 · m4 714 of 728, 1 min 15 s at load ~5): `x <- e` ENTERS ITS GENERATOR AT α, AND A CASE ARM'S BODY IS LOWERED ONTO ITS RESULT; ICON MASTER 713/728 BOTH MODES WITH hq_V's RUNGS IN THE DENOMINATOR, FLOORS 708 → 713; THE FLEET-WIDE SIDECAR RED ATTRIBUTED TO hq_C BY A CENSUS DIFF; THE 20 ICON BENCH `.s` MY args LANDING OWED, REGENERATED.**
⭐ **THE CURES ARE TWO WIRES IN `lower_icon.c`, NOT A BOX.** (1) TT_REVASSIGN's VAR branch wired the lvalue's γ with the generator-aware `γ_to`, which targets a generator's β (resume) port — so the RHS alternation was entered through its resume port: `x <- 2 | 3` → 3, `x <- 2 | 3 | 4` → 3 (resumed once, never exhausted), `x <- g()` with a suspending `g` resumed a frame that was never created and segfaulted. The α edge (`lc_γ_to`), which the IDX branch already used, cures the value and the crash together. (2) TT_CASE lowered each arm with a NULL continuation and then rewired the arm's ENTRY node's γ to the result assignment: an `every … do if … then fail` arm lost its whole body (the entry was the `1` of `1 to n` — `--dump-ir` showed the body UNREACHED — so IPL `equiv` succeeded on unequal lists and `lclose` returned its list unchanged) and an empty `{}` arm was never wired at all and fell out of the procedure with the case value (`saytime` → `0`). The arm is now lowered ONTO its result assignment (γ = asn, the TT_ASSIGN idiom). Witnesses `revassign_enters_its_generator_at_alpha` (20 shapes) and `case_arm_body_is_lowered_onto_its_result` (with IPL's `equiv` inlined) MATCH iconx in both modes and are absorbed + declared (entries 867 and 875); every reduced probe from the sitting (w_rev, w_gen, w_case, w_eq, w_conj, w_dl, w_say, w_lclose, w_date) MATCHES.
⭐ **MEASURED.** `test_icon_arizona_suite.sh` 74/90 both modes, unchanged: `ilib` is still red at its FOURTH rung (`lfliph := lreverse; return lfliph(L)` — a call through a reassigned procedure name binds to the declaration and recurses to ERROR 246; witness `call_through_a_reassigned_procedure_name_reads_the_global` is cut, 8 lines diverge, the patch is scripted and waits only on this landing's blocking set). `board_icon_master.sh` **713/728 both modes** pre-rebase (47.8 s at load ~5) and **714/728** on the rebased tree: the 13 FAIL + 2 CRASH are ALL hq_V's CEO-445 rungs (procedure_write_254…265, minted before their cures by design, mapped one by one through ALL.csv), my two witnesses PASS; floors re-pinned 708 → 713. THE BLOCKING SET: `make test` 3 min 20 s to a RED at arm 190 + the 97 arms after it by hand 11 min 38 s, all green.
⛔ **THE ARM-190 RED IS FLEET-WIDE AND NOT MINE — `master_sidecars_cover_stdin_and_argv`: stdin debt 54 above the pinned 53, argv 36 above 34.** The gate prints counts, not names; the census (`util_master_sidecar_census.py --tests-dir`) run on corpus `d812fe4df` (my last green) and on `03aa16e97`, name lists diffed: the three new entries are `procedure_every_scan_replace_13/record_coexpr_replace_1/record_every_replace_12` = rung36_jcon_gener/image/errors from hq_C's `af179eebd` (the five Jcon reds A-I joining the denominator) — they read stdin and take argv with no sidecar, so their refs grade a starved run. Named to hq_C with the diff, the coo told for the board. ⭐ THE METHOD IS THE FINDING: a count-only gate is attributed in two census runs and one diff (40 s), never by argument.
⭐ **HANDOFF STEP 4, TWICE.** The coo's 09:25 mail said 01eb996ca (my &DUMP landing) owed 20 benchmark + 4 demo SNOBOL4 `.s`; by the time I ran the regen on a clean tree they were already current (someone's `3ab7f6682`), but the verifier then named **20 Icon bench `.s` owed by my own args landing `5282bdb43`** (bb_binop_relop changed both mediums) — regenerated on the clean stashed tree, corpus `b1dc776d3`. The ilib landing touches only the lowerer, so those 20 are owed AGAIN by this commit and by the fourth-rung commit after it; one regen after the fourth rung, not three.
⛔ **PROCESS, MEASURED ON MYSELF:** I polled a `make test` log by counting its arm lines and read "67" for fifteen minutes after the run had already ENDED red at 09:39 — the count never moves once the run stops. Wait on the log's own end marker (`real`, the trailing `date`), never on a count. And `git stash` + `pull --rebase` + `stash pop` around a push conflicts whenever the stash holds runner-written score rows (`SCORE.md`, `SUITES.tsv`): those rows are the coo's and are regenerated by any board run — inspected, discarded, nothing lost.
- **CFO-27 (2026-09-09 09:02-09:2x CDT, cfo, MODE NONET, Icon only on Lon's 09:0x word) — ARIZONA `args` CURED AND LANDED (=== AND ~=== SUCCEED WITH THEIR RIGHT OPERAND UNTOUCHED), THE ICON MASTER 707 → 708/708 BOTH MODES, THE &DUMP GATE ADOPTED INTO THE WIRING FLOOR, THE SNOBOL4 CLAIM PARKED, AND THE `ilib` LADDER PINNED TO FOUR ENGINE DEFECTS WITH WITNESSES BEFORE ANY CURE.**
⭐ **THE SITTING OPENED ON AN UNRECORDED CURE — THE CFO-26 LESSON, AGAIN.** The tree held four modified SCRIP files and five modified corpus files from 23:xx with no cursor line: the `===`/`~===` cure for Arizona `args.icn` (`(proc("~===", 2))(1)` is `1 ~=== &null` and wants an empty line where SCRIP wrote `0`, because both operators were routed through the numeric relop coercion `=`/`~=` use). Re-measured before trusting it: witness `eqv_result_is_the_right_operand_untouched` MATCHES iconx in both modes; `test_icon_arizona_suite.sh` **73 → 74 / 90 both modes** (55.3 s at load ~11); `board_icon_master.sh` **708/708 both modes** with the witness absorbed as entry 829 (75.0 s at load ~10). Landed: corpus `d812fe4df` (absorbed + declared in MODES.tsv, one landing, both gates green), SCRIP `5282bdb43`. THE BLOCKING SET: `make test` to arm 180 (4 min 50 s) + the 105 arms after it by hand (12 min 25 s, REST_ARMS_GREEN) = ~17 min at load ~10, all green. ⛔ `make test` REFUSED rc=2 at arm 180 (`master_suite_builder_contract`: "corpus/tests is dirty") after 4 min 50 s at load ~10 — a refusal, not a red, and the order it teaches: **the corpus witness commits BEFORE the blocking set runs**, or the set stops 105 arms short. The remaining 105 arms were run by hand from the recipe (result below).
⭐ **HYGIENE-446 DONE (SCRIP `37a3fc8d9`).** `test_gate_sno_dump_matches_spitbol.sh` was already in the `test:` recipe at `01eb996ca`; what was missing was the wiring FLOOR (`gate_wiring.tsv`), so the ratchet could not protect it. `util_gate_wiring.py adopt` recorded it with three other wired-unrecorded gates (ceo409_mask, score_row_stamps, score_row_text_splices). Measured 2.3 s at load 12. NOT adopted, not mine, named: `test_gate_define_alternate_entry.sh` is on disk, in no recipe and undeclared (hq_P's DEFINE-class gate from CFO-26's arm-15 story).
⭐ **ICON-445: the SNOBOL4 claim `snobol4-value-call-poisons-next-lazy-comparator-oracle-quirk` is PARKED; my lane is Arizona A-L.** The FAIL list after `args`: **cfuncs coexpr errors evalx extlvals fncs gener ilib** (M-Z is hq_B's). `cfuncs` and `extlvals` both `link cfunc` and die at `loadfunc` (ERROR 022 undefined function) — a C-function loader, not a semantic; parked behind the semantics. `coexpr` segfaults at its co-expression `dummy(...)` traceback section and misorders `image` lines. `ilib` is ONE program that is a ladder: four rungs, each pinned to a ≤20-line witness against iconx BEFORE any cure, in the order the program hits them:
   (1) **`DateLineToSec` (year read as &null → today's date):** THE PORT THAT FIRED WRONG IS FOUND — `x <- 2 | 3` yields **3**, `x <- 2 | 3 | 4` yields **3** (resumed exactly once, not exhausted), `x <- 1 to 3` yields 1, and `x <- g()` with a suspending `g` SEGFAULTS. `--dump-bb` shows why: in `lower_icon.c` TT_REVASSIGN (VAR-lhs branch) the lvalue's γ is wired with `γ_to(lr, re)`, and `γ_to` routes to the target's **β** whenever the target is a generator kind — so the alternation is ENTERED through its resume port, skips its first alternative, and a procedure-call RHS resumes a frame that was never created. `:=` (TT_ASSIGN) never does this. The cure is one edge (`lc_γ_to`, the α entry, as the IDX branch already uses); the crash and the wrong value are the same wire. Witness `w_rev.icn`: 18 shapes, 11 diverge.
   (2) **`saytime` returns `0` instead of "just gone  three o'clock"** — a case arm whose body is the empty compound `{}` (`0: {}`) makes the PROCEDURE return the case value. `{}` parses as a zero-child TT_SEQ_EXPR, lowered to an IR_SUCCEED with the NULL γ the case lowering passes, and the arm wiring `else if (!bv) γ_to(be, asn)` never fires because `bv` is the SUCCEED itself — the arm's exit is never wired to the result assignment.
   (3) **`lclose` returns the list unchanged (IPL `equiv` succeeds on unequal lists)** — a `fail` inside `every … do if … then fail` inside a case arm does not fail the procedure. `--dump-ir` shows the arm's whole `every` body UNREACHED: the same case-arm wiring rewires the arm's ENTRY node's γ (`γ_to(be, asn)`, `be` = the `1` of `1 to 2`) straight to the result assignment, clobbering the body. (2) and (3) are one defect: the case lowering should lower each arm ONTO its result assignment (γ = asn, the TT_ASSIGN idiom) instead of rewiring the entry afterwards. The same rewrite idiom sits in the `break` lowering (`__break_result`) — named, not touched.
   (4) **`lfliph` overflows the stack** — `lfliph := lreverse; return lfliph(L)`: a call through a name that is ALSO a declared procedure binds to the declaration, not to the global variable the program just reassigned. An engine class (direct-call binding vs the global's value), the deepest of the four; last.
⛔ **TWO PROCESS LESSONS, MEASURED ON MYSELF.** (a) A background waiter written as `while pgrep -f board_icon_master.sh` matches ITS OWN command line and never exits; the `pkill -f` written to kill it then killed the shell issuing it (exit 144). Wait on a PID or a log line, never on a pattern that appears in the waiter. (b) A `git stash` around `git pull --rebase` touches the source mtimes, so the next runner refuses rc=2 (binary older than `by_name_dispatch.c`); the cost is one incremental `make` (5.5 s), not a finding — but a board started in the same breath as the stash pop is a wasted board.
- **CFO-26 (2026-09-08 22:16-23:0x CDT, cfo, MODE NONET) — TWO ARIZONA DEFECTS CURED WITH WITNESSES AND THE ICON MASTER AT 707/707, THE ECONOMY'S FIRST STRUCTURAL SAVING LANDED (`make test-boards`), FOUR FLEET-WIDE `make test` REDS FOUND AND THREE OF THEM CURED, &DUMP COMPLETENESS CURED, GATED AND DETECTOR-PROVED ON THE CEO'S 22:2x RULING (SPITBOL'S TABLE HASH RECOVERED FROM THE OUTSIDE).**
⭐ **THE ARIZONA CURES (SCRIP `2b9837385`, corpus `d45a173d3` + `fba886ed9`).** `next` served nothing in my ownership (every Arizona row still carries hq_I/hq_B from before NONET), so I took the class the MODE line gives me. Two rows closed on their own DONE-WHENs that had been sitting FREE with "CURED, close it" in their NEXT for four days (radix-notation, string-embedded-nul) — closing a done row is the cheapest flip on the board. Then the swap row's real cause: **a record's positional subscript `r[1]` was never a variable anywhere** — `c_rt_subscript_var` built a slot reference for arrays, tables, lists and strings and let a record fall through to the read-only path, so `r[1] := v`, `<-` and `+:=` bombed in `rt_assign_var` and `r[1] :=: r.d` silently kept both sides. Nine shapes isolated against icont before the cure, all green in both modes after. And the defect I had named-not-pinned in CFO-25: **a limit over a conjunction whose last conjunct is a plain value resumed into the conjunction's own failure GOTO** — `TT_CONJ` computed its rightmost resumable conjunct into `cx->conj_resumable` and NOTHING EVER READ IT (the second written-once-never-read field this seat has found in two days, after `g_jcon`); `TT_LIMIT` now prefers it whenever the last conjunct cannot resume. `((1|2|3) & 7) \ 5` → `7 7 7` in both modes. Measured: swap witness CURED both modes; Icon master 704/704 → **707/707 both modes** (entries 857 → 860, the three witnesses absorbed); Arizona 73/90 unchanged (the swap witness came from struct.icn, which has other reds); preflight 33 green; SNOBOL4 master 1899/1899 both modes on the cure binary, and RE-PROVED after the final rebase onto origin `a35c6d4d3` (five foreign commits, one a runtime change): Icon master 707/707, Arizona 73/90, swap witness CURED, preflight 33 green, SNOBOL4 master **1893/1917 FAIL=0 both modes, 3 m 28 s @ load 4** (the denominator moved because f3354f330 took eight oracle-refused entries outside the baseline), Icon per-entry identity regressions=0 improved=8 new=12 — a shared-runtime change graded on the other frontend per RULES § SHARED-NODE, on the tree that is pushed.
⛔ **A LOOSE PAIR HAS NO LAWFUL RESTING STATE, AND THE ceo's "stays a loose pair" RULING COULD NOT BE OBEYED.** Two gates together: `test_gate_orphaned_witnesses_do_not_grow` pins icon's loose pairs at 19 (mine took it to 22), and `test_gate_modes_declaration_travels` caps declared-but-unabsorbed families at 3 (already full; my seq declaration from CFO-25 was one of the four that redded make test for EVERY seat at 22:33 — hq_R named it to me at 22:37 while I was already at the one-line cure, corpus `974e94510`, pushed 22:38). The only green state is absorbed AND declared in one landing, and `--absorb-only <families>` scopes the absorption to exactly the named families (the whole-master renumbering I feared in CFO-25 was the `--additive` path, not this one). So I absorbed my three with it (entries 858 → 861, ALL.csv rows shift from 802 on in the builder's own order) and declared them; both gates green, the floors re-pinned 607 → 707 in `board_icon_master.sh` (they had never been re-pinned for the 704 either). hq_C is told; the three rung03_suspend_gen* orphans are not mine and are unchanged.
⭐ **THE ECONOMY'S FIRST STRUCTURAL SAVING: `make test-boards` (SCRIP `0110337cf`, on CEO-433/431).** `board_packages.sh` left the blocking recipe for its own target; the Makefile comment at the old position names the move. MEASURED, not projected: `make test` went **27 min (CFO-25) → 2 min 53 s to the same depth of arms** at load 5 — the rest of the saving is masked because every run this sitting stopped on a fleet red before the SNOBOL4 master (below); the arms after the red, run by hand, put the SNOBOL4 master at **234 s @ load 5**. The full green number is the first thing the next sitting records.
⛔⛔ **FOUR FLEET-WIDE `make test` REDS IN ONE HOUR, EACH STOPPING EVERY SEAT BEFORE ITS SNOBOL4 AND ICON CONTROL ARMS — THE ECONOMY POINT, ARRIVING AS FOUR INSTANCES.** (1) the orphaned seq declaration (mine, cured above); (2) `test_gate_score_tables_agree`: SCORE.md's two tables disagreed on snoflake 110 vs 108 of 124 — named to the coo per CEO-359, the coo cleared it on origin by 22:47; (3) `test_gate_runners_refuse_on_a_stale_binary` ARM 15: hq_P's 22:36 DEFINE witness gate executes ./scrip with no freshness guard (135 gates, 134 wired) — one idiom line added, census 135/135, SCRIP `c0703a28c`; (4) `test_gate_preflight_arms_stay_cheap` read every arm MISSING for any seat that EXPORTS `S4E_HOME` (it resolved `$ROOT` from the seat root, not the checkout; inside make test S4E_HOME is unset so it passed there) — SCRIP `679649287`. Two more reds are NOT mine and are named, not touched: `master_order_is_the_builders_order` snobol4 760 of 1929 out of order (a seat mid-absorb), and `score_row_denominator_includes_xfails` IcnM 704/704 excluding 20 xfails (the coo's COO-39 handoff is on it). ⭐ AND A RACE OF MY OWN, NAMED: I rebuilt the binary for the &DUMP cure while the by-hand arms were running, and every Prolog and Icon arm after that refused rc=2 (stale binary) — a refusal is not a red, but I lost the run. A seat with a build in flight does not run a board.
⭐ **THE 20 ICON BENCH ARTIFACTS REGENERATED (CEO-439) — AND DROPPED ON THE REBASE, BECAUSE THE coo HAD REGENERATED THE SAME 20 MINUTES EARLIER (corpus `60e34e0c6`), WHICH IS THE FINDING TWICE OVER:** `update_icon_bench_asm.sh` updated all 20 on my build (verifier CLEAN, owed=0 in all four chains); my commit conflicted with the coo's on every file and was skipped as superseded. The 386 changed lines in mine were ALL the seat's absolute path — the `.file` directive and every `.string "/home/claude_P/corpus/..."` the emitter bakes in for the program's own filename. The canonicalizer that strips ASLR label churn does not strip `/home/<seat>/`, so **every seat that touches the Icon lowerer owes 20 artifacts whose only change is its home directory.** Proposed to the ceo: normalize the seat root in the canonicalizer (or emit a repo-relative path), after which a regen diff is an emitter change and nothing else.
⭐ **THE -CASE ORPHAN.** My tree opened with an unrecorded 27-line `-CASE n` control-card cure in `snobol4.l` from 22:03 (CFO-25 never mentioned it). Stashed it, and the coo asked at 22:29 whether the dirty-tree case1 PASS was a cure in hand: it was, the row is the coo's, so I handed the exact hunk over by mail with its shape and the one caveat (folded names do not fold the string inside DEFINE); the coo landed it as SCRIP `403a7cc0e` at 22:47 with the SNOBOL4 master unchanged. ⛔ LESSON: a cure with no cursor line is a cure the next session cannot see; the cursor is written BEFORE the build, not after.
**&DUMP COMPLETENESS IS CURED, GATED, DETECTOR-PROVED AND LANDED (SCRIP `01eb996ca`; the ceo's 22:2x pivot; hq_S measured it on spitbol_testpgms test1).** Measured against `sbl -bf` (the 22:34 swapped binary, live, per CEO-440 clause f): SCRIP's dump omitted every ARRAY, TABLE, DATA, PATTERN, EXPRESSION, NAME and CODE value, printed null-valued variables and internal `EXPR$n` temps, printed OUTPUT nowhere (it lives under `_OUTPUT`), did not wrap at 120 bytes, and had no `&DUMP = 2` contents section at all; test1 wanted 13 lines SCRIP could not print. THE CURE: one creation serial shared by arrays, tables and data objects (the `#n`), stamped at every constructor — there were FOUR data-constructor sites, not two, the fourth in `src/driver/driver_data.c`, found only because the probe kept reading `#0` — with nested rows of an N-dimensional prototype handing their serial back; the ARRAY prototype text kept as given; `*X` names its temp `EXPR$n$X` so the dump prints `*X`; a by-name read of ARB/BAL/... marks it touched so the dump prints it only when the program named it (test1's `BAL = PATTERN`); and the rewritten walker with the 120-byte wrap that embedded newlines do NOT reset (SPITBOL counts bytes of the value, measured on &ALPHABET). ⭐ THE ONE I DID NOT EXPECT TO BE ABLE TO MATCH: a table's contents come out in SPITBOL's BUCKET order, and the hash turned out to be recoverable from the outside — a 1000-bucket table with an integer in every bucket makes each string key print right after its bucket's integer, and twelve probes fit exactly one model: **hash = XOR of the key's little-endian 8-byte words XOR its length, integers by magnitude, reals by bit pattern, mod the declared size, insertion order within a bucket.** ARBLK_t grew 48 → 56 bytes and the rtx layout pin moved with it (the .s reads only lo/hi/ndim/data by offset). MEASURED: five witnesses byte-identical to the oracle in both modes (cmp, not diff — the wrap and the trailing blank lines are the point); test1's dump section now differs from SPITBOL's ONLY in DIAGNOSTICS, &ERRLIMIT, &STCOUNT and &TRACE, the four counters of hq_S's traps row; SNOBOL4 master 1893/1917 FAIL=0 both modes (3 m 17 s @ load 1); Icon master 707/707; Arizona 73/90; preflight green. ⭐ THE INSTRUMENT, because nothing graded ever set &DUMP: `test_gate_sno_dump_matches_spitbol.sh`, wired into `make test` at 1.7 s, five witnesses x two modes cut live from the oracle — **10/10 green with the cure, 10/10 RED with it reverted**, proved by stash/rebuild/gate/pop/rebuild, not assumed. ⛔ TWO RACES OF MY OWN THIS SITTING, both the same shape: a source edit (even a reflow with no build) makes the binary older than the tree, and every guarded runner refuses rc=2 — the Icon boards refused once behind a rebuild and once behind a reflow. The rule I now keep: no edit under `src/` while a board is in flight, not even cosmetic. RESIDUAL, named: `.A<1>` (a NAME into an array cell) prints `NAME` because a cell reference carries no array name; and test1 itself will not flip until the traps row lands, since its four counters are that row's.
- **CFO-25 (2026-09-08 20:5x-22:4x CDT, cfo, MODE NONET) — MY OWN `seq` CURE HAD NO REGRESSION PROTECTION AT ALL; A WITNESS MINTED, DETECTOR-PROVED AND LANDED (corpus `3b10e1590`); A SECOND DEFECT FOUND WHILE MINTING IT; AND A 27-MINUTE `make test` COST ME A ONE-WORD CURE TO A RACE.**
⛔⛔ **ON THE CEO'S 22:1x REGRESSION-PROTECTION AUDIT — ONE OF MINE, AND WORSE THAN THE SHAPE DESCRIBED.** Not a witness leaving a denominator: a cure that NEVER HAD ONE. `196d19e79` moves no board and I now know WHY — the Icon master's only two `seq` uses are `every write(seq(1) \ 3)`, seq as the DRIVEN expression, which always worked. NOTHING GRADED ANYWHERE exercises seq as a non-final conjunct, which is the entire defect, so the cure was invisible to every denominator we have and a regression would have been silent. I had flagged "a correct cure that moves no board" as an oddity at the time; the ceo's audit is what made me ask why, and the answer was not "no flip", it was "no instrument". MINTED `corpus/tests/icon/seq_resumes_inside_a_conjunction.icn` — four shapes the master did not exercise — with its `.ref` cut from Arizona `icont`, never from SCRIP. ⭐ DETECTOR PROVED, NOT ASSUMED: reverted the one line, rebuilt, watched all four lines go red (`7` against `7 7 7 7 7`, `3` against `3 5 7 9 11`, `2 3` against `2 3 5 7 9`, `2 3` against `2 3 5 7 11`), restored, green in both modes. ⛔ AND THE BINARY I HAD KEPT ASIDE AS THE "BEFORE" COPY PASSED THE WITNESS — it links the rebuilt `libscrip_rt.so` and is not a valid detector. Had I trusted it I would have shipped a witness that proves nothing, which is the exact false-green our own gates carry a detector-proof arm to prevent. **THE GAP IS NAMED, NOT LEFT QUIET:** the family is declared in `config/MODES.tsv` as `m3,m4` (measured in both modes, not guessed from the family name, as that file's own warning requires) so it is ONE COMMAND from absorption — but I did not run that command, because `util_build_master_suite.py --additive` RENUMBERS THE WHOLE MASTER (463 insertions / 443 deletions on ALL.csv) and absorbs the other 20 undeclared families with it, including other seats' pending witnesses and `icon_display_builtin_unimplemented`. That is a denominator change belonging to whoever owns the master, not a side effect of adding one witness. ⛔ ALSO LEARNED THE HARD WAY: `--additive` WRITES WITHOUT `--write` — there is no dry run on that path, and I reverted the master twice before I understood that.
⭐ **A SECOND DEFECT, FOUND WHILE MINTING THE FIRST AND DELIBERATELY NOT PINNED: THE LIMITATION OPERATOR OVER A CONJUNCTION YIELDS EXACTLY ONE RESULT.** `((1|2|3) & 7) \ 5` gives SCRIP one 7 against Arizona's three; `((1 to 3) & 7) \ 5` the same; `(seq(3,2) & 7) \ 4` one against four. ⭐ THE CONTROL ARM IS THE POINT: `(1|2|3) \ 5` gives `1 2 3` in BOTH, so `\` alone is correct and it is losing resumption specifically over a CONJUNCTION — one defect in the limitation lowering, not a per-generator gap. It surfaced because my first witness draft pinned it by accident and could not go green; I dropped that line rather than commit a red into the master.
⛔ **THE `rename` CURE WAS DUPLICATED AND THE COST IS THE LESSON.** `809f18f9f` gave `rename()` a body and left `icn_function_names[]` — the table `lower_function_gen` walks to generate `function()` — without it, so the name was CALLABLE AND NOT REFLECTABLE. I fixed it, measured it (gate `fails=1` → `fails=0`, Icon master 704/704, Arizona 73/90, Jcon 57/82, identity `regressions=0` over 1557 entries) — and then held it 27 minutes behind `make test`, in which time another seat landed the identical one-word fix as `6883575c4` and my commit was correctly dropped as a duplicate. No harm done and the same fix stands, but the wasted effort is exactly the economy point below, arriving as a concrete instance rather than an argument. ⭐ THE `display` READING SURVIVES IT AND THE cto ASKED IT BE THE RULE: SCRIP yields 90 names against the oracle's 91, and the missing one is `display`, which SCRIP does not implement at all (`ERROR 022` where Arizona dumps the identifier tables). Arm 4b only flags names SCRIP DISPATCHES, which is why 90 is green. A reflective list must never name a builtin the engine cannot call; if `display` is implemented it lands WITH its list entry in the same commit.
⛔ **`make test` DID NOT COMPLETE AND I DID NOT CLAIM IT DID:** terminated by SIGTERM (RC=143) in `board_packages.sh` after 27 minutes. Every blocking arm ahead of that ran; the only GATE RED in the log is `test_gate_no_xfail_survives` (278 xfails across the seven suites), a fleet-wide census red that counts corpus markers and that no lowering change can cause or cure.
**THE ECONOMY, MEASURED 21:17 CDT.** Load 13.71/9.15/7.66 on 16 cores — thirteen seats compiling, not runaways; `df -h /` 76G of 125G (64%), down from 88G/74% on 09-07; and ⭐ **ZERO test processes older than two hours**, against the SEVEN at 96-99% CPU aged 14-59 h I censused on 09-07 in CFO-1. That class is gone and no kill grant was ever needed. **BOARD COST, each at the load it ran under:** `make preflight` 3.9 s @ load 4 (33 arms, NO build); the Icon conversions DONE-WHEN 6.9 s @ load 4; `test_icon_arizona_suite.sh` 40.3 s @ load 4.3; `board_icon_master.sh` 64.5 s @ load 4; the SNOBOL4 master via `corpus_suite_harness.py --by-modes-column` **7 m 41 s @ load 7**; `make test` **27 min @ load 5-13**. ⛔ AND THE SHAPE OF THAT 27 MINUTES IS THE FINDING: ~7.5 min is the SNOBOL4 master and ~10 min is `board_packages.sh`, whose own Makefile comment reads REPORTED, NOT BLOCKING — so every seat running the BLOCKING set pays ten minutes for an arm that CANNOT FAIL THE BUILD, with the 851-program IPL suite inside it that then TIMED OUT at its own 600 s ceiling and produced a timeout rather than a board. A seat that needs one gate should run that gate directly; I did not, and it cost me the `rename` landing.

- **CFO-24 (2026-09-08 20:0x-20:5x CDT, cfo, MODE NONET) — THE RUNTIME DEFINE CLASS IS FOUR FAILURES NOT TWO, MODE 3 NEVER RUNS THE DEFINE BOX, AND THE OBVIOUS CURE REGRESSES THREE MASTER ENTRIES — ABLATED AND BACKED OUT, TREE LEFT CLEAN.** The ceo routed hq_P's class to me (two defects, one mechanism, grade on BOTH witnesses). Both hq_P witnesses reproduce verbatim. ⭐ THERE IS A THIRD SYMPTOM NOBODY NAMED: wA in MODE 4 prints `orig(x)`/`orig(x)` — the runtime redefinition performed inside a function is **silently ignored**, no crash, no diagnostic. So the class is FOUR cells (wB m3 new/new, wB m4 new/new, wA m3 orig+SIGSEGV, wA m4 orig/orig), and a seat grading only m3 fixes the crash and leaves m4 wrong with nothing saying so. THE MECHANISM, top to bottom: the crash is a jump to address 0 because `rt_sno_runtime_define` (rt.c:609) sets `p->fn = 0` and zeroes the `alpha$<name>` cell and never installs the new entry — its caller `_DEFINE_` has the entry label in hand and does not pass it; the static half bakes `body_cell$F: .quad LBL__F2` and BOTH define sites emit `lea rax,[rip+LBL__F2]`, so the first statement seals the second's body; and the IR ALREADY KNOWS BETTER — each bind node carries its own `LIT_NAME` entry and there is even an accessor, `ir_define_bind_entry()` at IR.h:197 — the dedup is in `scrip.c`'s dentry construction, which resolves the entry by looking up the FUNCTION NAME in `s2->proc_table`, one row per name. Also `bb_define.cpp:424` is a two-medium disagreement on one line: text uses the statement's label, binary uses `rt_define_query`'s per-name answer. ⭐⭐ THE FACT THAT DECIDES THE CURE, and I proved it rather than inferred it: I wrote a candidate runtime binder with a diagnostic gated on `SCRIP_DEFINE_BIND_DIAG=1`, called from the DEFINE site in role 6 which `bb_define()` dispatches for BOTH mediums. In m4 it fires twice with the right per-statement entries AND CURES wB. **In m3 it never fires at all** — the emitted call is in the asm (grep counts 2) and mode 3 never runs it. Mode 3 binds DEFINE entirely at slab-seal time, so ANY cure written only in the template moves m4 and leaves m3 byte-for-byte unchanged. ⛔ AND THE OBVIOUS CURE IS WRONG, MEASURED NOT GUESSED: making dentry use the bind node's own entry cures wB m4 and XPASSes `user_function_replace_7`, but REGRESSES `user_function_26`, `user_function_replace_10` and `user_function_replace_branch_1` in m4 — caught by the SNOBOL4 master as a control arm, against hq_U's run 29 s earlier on clean `e4ff7e9c7` which had zero unmarked m4 non-PASS. Cause: a DEFINE with NO explicit second argument must bind to the `<fname>_α` ACTIVATION, and resolving its own body label routes around it. The two arms are COUPLED — restricting the patch to explicit retargets removes the regression AND removes the cure, because wB's first DEFINE has an implicit entry. NOT CURED; candidate BACKED OUT and the tree left clean at `397b3bd22` so no seat measures a board against a half-cure. Full write-up, four-cell table, backtrace and the ablation: `FINDING-2026-09-08-cfo-the-runtime-define-class-is-four-failures-mode-3-never-runs-the-define-box-and-the-obvious-cure-regresses-three-master-entries.md`. ⭐ THE CEO'S 20:4x QUESTION IS ANSWERED AND HIS HYPOTHESIS WAS WRONG IN A WAY WORTH RECORDING: the three m4 reds on my dirty tree were NOT the uncommitted Icon lowerer edit — that had already landed as `196d19e79` before the board ran — they were this DEFINE candidate in `src/driver/scrip.c`. On the clean tree all three run PASS in m4, verified by name. The control arm did its job either way. ALSO CLEARED THIS SITTING (cto's telegram, my lane): `test_gate_score_tables_agree.sh` was RED on `icon: display vendor says 71/90, grid V says 62/90` and blocking `make test` for every seat; I did not copy the number across by hand — I re-ran `test_icon_arizona_suite.sh` on the clean tree and let the runner do its own dual write, both halves now 72/90 with matching provenance, gate rc=0.

- **CFO-23 (2026-09-08 19:5x-20:2x CDT, cfo, MODE QUARTET -> NONET) — A REAL ICON SEMANTIC CURED THAT MOVES NO BOARD, THE PROGRAM IT WAS CHASING RE-DIAGNOSED, AND BOTH ICON ROWS RELEASED ON THE SNOBOL4 TELEGRAM (SCRIP `196d19e79`).** THE CURE: `seq()` is one of Icon's FIVE function generators (bal, find, key, seq, upto) and `icn_call_allow_gen()` in `src/lower/lower_icon.c:68` listed only three, so `is_resumable()` said no and the resume GOTO of an enclosing `suspend` was wired to the FAIL node instead of to the TO box. `suspend seq(3,2) & 7` yielded ONE 7 against Arizona's unbounded stream; `suspend seq(3,2)` alone was always right, which is why it hid. The IR is the whole proof in two lines — for `(1 to 9) & 7` the resume goto reads `8@ 2 2 GOTO` (node 2 is the TO box), for `seq(1) & 7` it read `8@ 7@ 7@ GOTO` (node 7@ is FAIL) — and `seq` and `to` lower to the SAME `IR_TO` box, so only the predicate ever differed. The IPL's `prime()` is built from `suspend 2 | ((i := seq(3,2)) & (not(...)) & i)`, so `pfactors`/`isprime`/`prdecomp`/`prime` all stopped after their second value. ⛔ I MEASURED BOTH DIRECTIONS WITH THE TWO BINARIES KEPT SIDE BY SIDE rather than trusting the after-number: Arizona 72/90, Jcon 56/82, Icon master 704/704 — IDENTICAL before and after. A correct oracle-checked cure with NO FLIP, and I am recording it as exactly that. `make test` green, 75 arms, exit 0. ⛔ `bal` DELIBERATELY LEFT OUT: its own generation is short (`every bal('+',,,"a+b+c")` gives 2 where Arizona gives `2 4`), and marking a short generator resumable turns a wrong FINITE answer into an INFINITE resumption — `bal(...) & 7` then yields 7 forever, a HANG where there was a FAIL. I added it, measured that, and backed it out. Cure bal's generation first, list it second. ⭐ I AM CORRECTING MY OWN 09-07 CENSUS LINE: `ilib` is NOT "a crash, and the crash is the finding" in the Icon library — it dies in the SHARED CALL-VALUE SPINE. `rt_call_value_resume_h` (`src/runtime/by_name_dispatch.c:1069`) dereferences `*hslot` as an `ICN_OPGEN_t *` and reads `g->magic` after testing only NON-NULL; when an `IR_CALL_VALUE` beta is entered in an activation whose ALPHA never ran, the cell holds the previous activation's handle instead of the 0 that alpha writes. Eight-line oracle-clean witness (Arizona prints A / 463410420 / B, rc=0), gdb backtrace and a full ablation — neither half alone crashes; the trigger is apply of a procedure value held in a PARAMETER, callee leaving live choice points, resumed THROUGH A LIMITATION — are in `FINDING-2026-09-08-cfo-a-call-value-resume-site-reads-a-handle-cell-its-own-alpha-never-wrote-and-segfaults-on-the-garbage.md`. ⛔ THE CRASH IS PRE-EXISTING AND I PROVED IT RATHER THAN ASSERTING IT: the baseline binary crashes on the same witness, so the `seq` cure did not cause it. But it DOES change which garbage sits in the cell — `ilib` HANGS at 125 lines without the cure and CRASHES at line 9 with it — which means **ilib's CRASH-vs-HANG verdict is not a measurement of anything until the spine is cured**, and no seat should report movement either way. ⭐ AND THE ROW'S OWN DONE-WHEN LIES: it copies only `<p>.icn`/`<p>.dat` into its scratch dir, but `io.icn` shells `ls io.[ids][tca][dnt]` and `ls io.i?n io.d?t io.s?d`, so `io` reads RED (`< io.std`) on a program that is CURED — measured in a directory containing `io.std`, io matches with ZERO differing lines. It is ceo-minted so I did not edit it; it needs one `cp "$SUITE/$p.std" .`. TRUE red set is SIX, not seven, and every one is blocked or spine-deep: fncs gener recent sorting recogn ilib. ROWS RELEASED per the ceo's 20:0x telegram (all twelve seats on SNOBOL4 until 100%, Lon 20:0x): `icon-record-serial-allocation...` (CLAIMED, unworked) and `icon-arizona-jcon-class-conversions-builtins-and-io-fidelity` (ASSIGNED) are both FREE, with the state, the two traps and the true red set written into the baton's NEXT block first. NEXT: the runtime DEFINE class the ceo routed to me — two defects, one mechanism, graded on BOTH witnesses.

- **CFO-0 (2026-09-07 08:06 CDT, ceo) — THE SEAT IS OPENED.** Root `/home/claude_cfo` (Lon created it 08:0x), stocked by `.github/scripts/populate_cfo_root.sh` (Lon runs it: the ceo cannot write into a sibling root), identity `cfo` in the bus (SCRIP `s4e_msg.sh`: root map, `s4e_root`, `s4e_hqboxes`, `s4e_is_hq`), postoffice `cfo/inbox` with `HQ` = ceo. MODE EXECUTIVE (2026-09-07 07:50 CDT): the working seats are ceo, cto, coo, cfo, all Claude Fable 5.1 at high effort (Lon 07:5x: *"I set each executive to Fable 5.1 at 'high' effort, not 'xhigh' as before."*). THE ORDER OF WORK IS SNOBOL4 AND ICON TO 100% for the announcement; the other five ship as they stand.
- **FIRST ACT (the CFO's, on its first sitting):** the runaway census. At 07:08 CDT on 2026-09-07 the ceo measured eight processes at 96–99% CPU each on a 16-core box: five `t.bin` test binaries started Sept 4 21:53 and Sept 5 11:34–13:42 under `/tmp/csh_run_*` (the corpus_suite_harness run directories), two `/home/claude_T/SCRIP/scrip --run arbno_pos_rpos_branch_81.sno` started Sept 6 18:09 and 18:23 (hq_T's fuzz row), one `iconx ./jtran_icont` started Sept 6 16:15 under hq_B's scratchpad. Name each with owner and age in this file, ask Lon in chat for a standing grant to kill any test binary older than a bound he names, record the grant here verbatim, then kill them and record the load before and after.
- **CFO-1 (2026-09-07 08:48 CDT, cfo) — THE RUNAWAY CENSUS, RE-MEASURED.** `uptime` load 9.00/9.23/9.12 on 16 cores, `df -h /` 88G used of 125G (74%). SEVEN runaways at 96–99% CPU each (the ceo's eighth, `iconx ./jtran_icont` under hq_B's scratchpad, is already gone): PID 1421287 `t.bin` cwd `/tmp/csh_run_lva5rxyf/tmpu86wbuz_` started Fri Sep 4 21:53 (2d 11h); PID 1258832 `t.bin` cwd `/tmp/csh_run_m42bpoy5/tmpf2kagsmq` Sat Sep 5 11:34; PID 3038486 `t.bin` cwd `/tmp/csh_run_lwvgcy6l/tmpk12ytwnn` Sat Sep 5 13:04; PID 3064534 `t.bin` cwd `/tmp/csh_run_d_6n83j0/tmp12qbe24y` Sat Sep 5 13:06; PID 3571014 `t.bin` cwd `/tmp/csh_run_7ujt_0r5/tmpqds6ezot` Sat Sep 5 13:42 (all five are corpus_suite_harness test binaries, owner unrecoverable from cwd, 43–59 h old); PID 1561608 and PID 1960798 `/home/claude_T/SCRIP/scrip --run arbno_pos_rpos_branch_81.sno` cwd `/tmp/csh_run_9kaolihq/…` and `/tmp/csh_run_9o71ha9k/…`, started Sun Sep 6 18:09 and 18:23 (hq_T's fuzz row, 14.5 h old). Nothing killed: no grant is recorded yet. ASKED LON IN CHAT this sitting for a standing grant to kill any `t.bin` or `scrip --run` test process under `/tmp/csh_run_*` or a seat scratchpad older than a bound he names (proposed: 2 hours). His words land here verbatim when given; load before/after follows the kill.
- **CFO-2 (2026-09-07 09:11 CDT, cfo) — FIRST BUG THROUGH THE BUS: pat's UNGRADED remainder reaches ZERO, and the PAT board gets 46 s cheaper.** `next` served the rank-0 inventory row every-package-runner-prints-shipped-graded-ungraded-and-ungradable-and-the-leaderboard-carries-the-inventory (blocker of the csnobol4/spitbol lockdown row) whose NEXT said no runner was left to wire; the cheapest remainder was pat's one UNGRADED row, iso7185pat.pas TIMEOUT ("fpc -Miso does not finish within 60s; raise the bound or profile why"). MEASURED, not raised: the oracle does not finish at 300 s either (ppcx64 100% CPU, no diagnostic); a prefix bisection stops at line 77 `label 0, 3, 9999, 0004;` and a three-line program declaring `label 0;` reproduces the spin under -Miso and -Mextendedpascal (labels 1, 01, 0004, 9999 compile; -Mfpc/-Mtp reject the program at line 114). ISO 7185 permits label 0, so this is fpc 3.2.2's own behaviour on a conforming program: moved to UNGRADABLE/ORACLE_REFUSES with that reason (corpus `0e9835068`). The runner now re-probes a declared-UNGRADABLE program at a 10 s bound through lib_inventory.sh's own reader and shouts RULING STALE if the oracle ever builds it (SCRIP, see the cost table). pat reads shipped=429 graded=427 ungraded=0 ungradable=2; the package-inventory gate passes (21 arms, 0 violations); the LF gate passes. Remaining UNGRADED in the row, none in the cfo's lane: ipl 200 rows (Icon package reds are the cto's class work), gimpel 3 + aisnobol 2 NEEDS_VENDORED_SOURCE (files that exist nowhere in corpus), jcon 1 NEEDS_RUNNER_WIRING (two-file icont link + argv sidecar). Pascal-P5's own pcom/pint exist only as source under `/home/resources/Pascal-P5/source`; building them would give the acceptance test its native oracle, noted, not done. Claim released.
- **CFO-3 (2026-09-07 09:34 CDT, cfo) — SECOND BUG THROUGH THE BUS: the port-trace instrument stops blaming its tracer for a witness that never compiled (SCRIP `1f87a7aa9`).** `next` served rank-1's blocker port-trace-zero-lines-check-cannot-tell-a-compile-time-refusal-from-a-tracer-that-is-not-firing (no rank-0 row was FREE). Proven red in 1 s, cured in the shared body lib_port_trace.sh that eight gates source: a --compile refusal is a named red row per witness and the walk continues, an m4 NOBUILD no longer trips the zero-lines check, and the message that remains says the witness compiled and ran. All eight arms graded as control on one binary before and after (see the cost table): four FAIL counts unchanged, pattern PASS, icn UNPROVEN either way (it carries its own copy of the fold, named in the row's NEXT for the icon lane), pl and raku moved from UNPROVEN-at-the-first-refusal to a full FAIL(1) measurement with 10 and 14 refusals named. The DONE-WHEN reads GREEN; closed by computed `done`. THE ECONOMY OF IT: the pl arm now costs 7 min instead of 1 because it measures 293 witnesses instead of dying at the tenth rung; that is the price of an honest denominator and it is recorded, not hidden.
- **CFO-4 (2026-09-07 09:53 CDT, cfo) — THIRD BUG THROUGH THE BUS: jcon's UNGRADED remainder reaches ZERO and three m4 programs flip on a runner defect found on the way (corpus `4a97848e1`, SCRIP `1e8b5e951`).** `next` served the inventory row again (its DONE-WHEN probe times out at 120 s, so it is served unverified every time; named in the row's NEXT). The cheapest remainder was jcon's link1.icn NEEDS_RUNNER_WIRING: cut link1.ref from the shared icont/iconx two-file build with argv `a b c` (link1.args; byte-stable, README names both sidecars as ours), taught the runner to read a program's `link` lines as its module list, take argv from .args and grade .std-or-.ref. The first full run read link1 PASS m3 / FAIL m4: the m4 binary was handed the driver's `--` as argv[1] and printed `{ -- }` first, a pre-existing defect that also sat on every .dat program reading *args in m4. Cured (separator for m3 only); jcon m4 46 → 49 (link1, htprep, mffsol; geddump moved FAIL → CRASH, SCRIP's own, same verdict as m3), m3 49/91; PACKAGE_INVENTORY jcon shipped=91 graded=82 ungraded=0 ungradable=9. Inventory gate PASS, LF gate PASS, SCORE.md icon/vendor and THE SUITE TABLE's Jcon row rewritten by the runner (49/91 both modes). Claim released; the remainder (ipl 200 in the cto's lane, gimpel 3 + aisnobol 2 needing upstream sources) is in the row's NEXT.
- **CFO-5 (2026-09-07 10:06 CDT, cfo) — FOURTH BUG THROUGH THE BUS: THE TRACE TRUNK IS DONE (SCRIP `a4a01ffb3`; corpus `ff41d793d`, `1805a4ba1`, `49c5c55f0` + the icon bench .s).** `next` served rank-1's blocker snobol4-trace-types-v-l-c-r-k-a-print-spitbols-banner-in-both-modes (owner hq_P). Its DONE-WHEN was red on ONE line: &LASTNO inside a VALUE trace fired after `N = F(2)` returned read 4 (the trace function's own statement) where SPITBOL reads 12. A five-reading witness against sbl -bf showed the rule: SPITBOL restores &STNO/&LINE to the CALLING statement when a DEFINE'd function returns. Cured in the DEFINE template beside the &FNCLEVEL bookkeeping it mirrors: the caller's g_stno/g_line go into a per-nesting-level slot (`rt_stno_stack[rt_k_level]`, libscrip_rt) at both entry sites and come back at all three exit sites, rax/rcx only, one engine so both modes. Controls: the three witnesses PASS m3+m4, the LASTNO witness == oracle m3+m4, SNOBOL4 master 1858/1858 FAIL=0 both modes, Icon master 704/705 (its one red, procedure_every_alt_replace_4, is red on origin before this change and green only in the ceo's unpushed `7e15ab713`), pattern gate PASS, strip_comments clean. Every function prologue changed, so all four .s artifact sets were regenerated and pushed (benchmark 20, demo 10, prolog bench 23, icon bench 20). Closed by computed `done`; the six trace event rows it blocked open by themselves. Still owed to the row's LANDING clause and named in its NEXT: the witness as a SNOBOL4 ladder rung, which "hq_T declares" and hq_T is stood down, so it is the coo's SNOBOL4 lane to number. TWO ECONOMY FINDINGS on the way, both routed to the coo for the ceo: (1) `next`'s 120 s DONE-WHEN probe of the package-inventory row runs board_packages.sh, the timeout kills the parent only, and the ipl child ran on for six minutes under my identity (178 progress rows at 14:47Z) and drove a stray IPL score stamp the coo reverted: an over-budget probe is a runaway by construction and costs a whole package board per `next`; (2) a board-shaped DONE-WHEN cannot be probed inside the budget at all, so that row is served unverified every time.
- **CFO-6 (2026-09-07 10:25 CDT, cfo) — A RULING ROW RELEASED WITH ITS BILL, AND GIMPEL'S UNGRADED REMAINDER REACHES ZERO.** `next` served snobol4-value-call-poisons-next-lazy-comparator-oracle-quirk (rank-1's blocker, owner hq_S): six visits and five releases in gdb on SPITBOL's garbage collector for an oracle accident that SCRIP already gets right. Released untouched with the reasoning in its ledger and ruling (b) proposed to the ceo by mail with the cost tally. Then the package-inventory remainder in my own lane: upstream gimpel (/home/resources/gimpel) ships the three "missing" include targets under DOS 8-character names (STRINGOU.INC, RESOLUTI.INC, SYSTEM.INC) while the include lines use the long names. stringout.sno vendored as a byte-identical alias, FRSORT_driver graded (ref cut from sbl -bf, SCRIP red both modes: m3 wrong order, m4 ERROR 022), TIMER/TIMEGC ruled NONDETERMINISTIC on two-run oracle evidence (wall-clock timings) with their aliases deliberately NOT vendored, because the scorecard grades live and a first attempt with them in-tree over-summed the inventory by two. GIMPEL_BOARD m3 104/127 m4 104/127 (the denominator grows by one honest red), inventory shipped=290 graded=127 ungraded=0 ungradable=163, inventory gate PASS, LF gate PASS. aisnobol ATTEMPTED AND REVERTED: its SNOCORE is in the package's own upstream/ dir, but vendoring it makes the suite builder absorb SIR/TEST against a page-formatted listing carrying ERROR 116 with no "in statement" footer, which sbl_died() cannot see: an instrument gap (routed to the coo, written into the row's NEXT), not a corpus edit; nothing of it is on origin. Six of the eight lockdown packages now read ungraded=0; ipl (200, the cto's class lane) and aisnobol (2, behind the builder gap) remain. CORRECTION (10:29): .github commit `240a2c7ed`'s message says the gimpel runner rewrote SCORE.md; it did not (that runner grades via scorecard_snobol4.sh and writes no cell), so only this file changed in that commit and the Gimpel row 104/127 reaches the table through the coo's tick, to whom the numbers went by mail.
- **CFO-7 (2026-09-08 17:2x-17:3x CDT, cfo) — FIFTH BUG THROUGH THE BUS: a REFUSED score write stops lying about what it wrote, and the last two hand-set suite rows write themselves (SCRIP `b2660262e`).** My claimed rank-0 row score-row-write-rewrites-the-board-then-refuses was sitting UNCOMMITTED in this root at the start of the sitting -- STEP 1 and STEP 2 done, nothing on origin. `util_score_row.py write` persisted SCORE.md and only then called suite_sync, so a refusal exited 2 over a board that HAD moved and the three runners reading that rc printed 'SCORE.md NOT UPDATED -- record this row by hand' over a row they had just written. The whole suite-mirror DECISION is hoisted above the write as `suite_sync_decide()`: a refusal now leaves both files byte-identical, rc=2 means nothing was written, and the one genuinely partial case (banner --set fails AFTER the board write) names which file moved and which did not. Proof is a cmd_selftest arm that snapshots the scratch board and asserts byte-identity after a refusal; SELFTEST PASS end to end, test_gate_score_cell_no_silent_prose_loss.sh GATE PASS(0) 9 arms -- the row's DONE-WHEN, both clauses. THE COO'S HALF, which was the inbox brief this sitting: arizona was cured in b7f48462a by naming its fraction, but snoflake and ipl still passed --suite with no --suite-pass/--suite-total over a --text carrying several distinct bare fractions, so fraction_from_text correctly declined to guess (proved against the real functions: Flake 3 competing fractions, IPL 2) and the coo hand-set both every run. Both now name the fraction their own prose already calls the table's reading -- BOTH_STREAM/TOTAL for Flake, the oracle-diffed RUN tier M3_RUN_PASS/RUN_GRADED for IPL. MEASURED THROUGH THE SUITE'S OWN RUNNER, not asserted: `test_snoflake_suite.sh` on the pushed tree printed `suite table: snoflake -> 99/180 on 2026-09-08 (tree b2660262e) -- SUITES.tsv rewritten in the same call` with NO 'NOT UPDATED' line, and 99/180 is EXACTLY what the coo had been setting by hand, which is the check that the runner names the RIGHT fraction and not merely a resolvable one. ipl is wired identically and is the cto's board to run. Flipped to the coo; ipl stays hand-set until its board runs once.
- **CFO-8 FINDING (2026-09-08 17:18-17:31 CDT, cfo) — `make test` IS RED AT ARM 2 OF ~65 FOR ANY SEAT THAT HAS NOT COMMITTED YET, AND SELF-HEALS ON THAT SEAT'S FIRST COMMIT.** `test_gate_banner_leads_with_the_suite_line` ARM 4 matches the verdict with `(OK|STOP) [A-Z]` while the banner has a THIRD class, `NOTHING LANDED`, printed when a session has no commit and no FINDING; the arm reads LIVE session state, so it grades the seat rather than the tool and breaks `test-postoffice`'s own hermetic membership rule. Measured on ONE tree with no edit to gate or banner: GATE FAIL 1-of-6 pre-commit, GATE PASS 6-arms immediately after commit `6a61e40d9`; stash-controlled, so it is origin's and not this working tree. ⛔ AND IT WAS HIDING A REAL RED: run 1 died in 49.4 s having certified arms 1-2 while calling itself THE blocking set; run 2, one scoring commit later, ran 477.1 s, went 56 gates green and hit `test_gate_pl_meta_call_reaches_control_constructs` 2-of-23, arm (b) 'if-then-else must COMMIT' -- a meta-called `(C->T;E)` does not commit, CURE names ';'/2 dispatch in lower_pl_stage2. NOT MINE and NOT TAKEN: `git diff --name-only origin/main` is three scripts, zero C/C++/.y/.l, and `git diff --stat origin/main -- src/ bootstrap/` is empty, so the compiler that failed was built from origin's sources. Routed to the ceo for the Prolog lane with the one-character interim cure and the structural row proposed; full receipts in FINDING-2026-09-08-cfo-make-test-dies-at-arm-2-....md.
- **CFO-9 (2026-09-08 17:26 and 17:34 CDT, ceo -> cfo) — TWO RULINGS RECEIVED, AND MY NEXT ROW CARRIES THE SWITCH.** (a) THE ICON DIALECT RULING (ceo 17:26, GOAL-ICON-100.md sec RULING, on my ASSIGNED rank-1 row icon-arizona-jcon-class-conversions-builtins-and-io-fidelity, now UNBLOCKED): the dialect comes from NOWHERE -- delete the `#SRC: JCON` switch and `g_jcon` (one global fewer) and SCRIP speaks Arizona Icon v9.5.25a everywhere; a jcon .std line stating a jcon-implementation fact (&features naming Java/UNIX/graphics/dynamic loading, &regions 0/0/0, progname) puts that program OUTSIDE THE BASELINE -- named in OUTSIDE_ARIZONA_BASELINE.tsv beside its package with the lines and the reason, OUT of the denominator, never masked per line (the SNOBOL4 OUTSIDE_SPITBOL_BASELINE precedent); &regions reports SCRIP's REAL sizes with Icon's default heap sizing adopted from iconx (0/500000/500000) so a default run reads truthfully, as -s/-m mirror SPITBOL, and never a size SCRIP does not have. Control arms: Icon master + both package suites + `nm -D` free of `g_jcon`, one landing. (b) THE LAW (ceo 17:34, fleet-wide, RULES sec Oracles, FACT RULE NO COMPATIBILITY SWITCH FOR ANY SCRIP LANGUAGE, CEO-391), Lon verbatim: *"Okay make it so then. So no compat switch for any of our SCRIP language. Right?"* -- one oracle and one feature set per language: SNOBOL4 SPITBOL (our x64 fork, `sbl -bf`); Icon Arizona icont/iconx; Prolog THE SUPERSET (ISO the core and default, every non-conflicting GNU/SWI builtin added, conflicts through ISO prolog flags set in the program or by a runner directive, NEVER a compiler mode -- this supersedes the 09-04 two-switch ruling); Pascal `fpc -Miso`; Raku rakudo/roast. No --compat, no dialect global, no source marker, no semantic env switch; what an oracle refuses or only another implementation provides is OUTSIDE THE BASELINE, named beside its package, out of the denominator, never hidden. ⭐ THE LAW ASKS EACH SEAT TO SAY IN ITS LEDGER WHETHER ITS ROW CARRIES A SWITCH: MINE DOES -- the icon row above IS the `#SRC: JCON` / `g_jcon` deletion, so (a) is the Icon instance of (b) and I take it as my next row. Nothing in the row I closed this sitting (score-row-write-rewrites-the-board-then-refuses) carries a switch of any kind.
- **CFO-10 (2026-09-08 17:4x CDT, cfo) — THE ARM-2 FALSE RED IS CURED AS RULED, AND ITS STRUCTURAL HALF IS MINTED (SCRIP `348eed586`).** ceo RULED 17:39 on my finding: *"a hermetic gate never reads live session state; land the one-character class now and mint the structural row under your ownership, since the finding is yours and it is the economy's: a seat paying 8 minutes to find a red at arm 57 is a cost line."* ARM 4 of test_gate_banner_leads_with_the_suite_line now counts all THREE verdict classes; proved on the real lines -- `⚠ NOTHING LANDED` was no-match before and matches after, while `✅` and `⛔` match under both, so no regression -- and `make test-postoffice` is green. Minted banner-gate-arm-4-reads-live-session-state-so-it-grades-the-seat-not-the-tool (rank 1, owner hq_T, FREE) for the structural half: the classes are still TYPED IN THE GATE, so the next class anyone adds reds arm 2 again, and the arm still grades the seat rather than the tool. ⛔ ITS DONE-WHEN READS RED ON THE COMMIT THAT CURED THE SYMPTOM, BY DESIGN, and I checked that it does -- my first draft of that criterion read GREEN because a python `\x` escape wrote the emoji as literal text and the grep could never match, which is a false-green criterion of exactly the class this seat filed a finding about an hour earlier; caught by running it before minting rather than after. ⛔ CORRECTION CARRIED INTO THE FINDING: I routed the hidden Prolog red as arm (b) on the strength of the gate's printed CURE line; the ceo re-measured it as arm (c) (a cut inside a meta-called goal answers [] where swipl says [fwd]) and has claimed it. The CURE line is STATIC PROSE printed beside a MEASURED failure count, so it keeps naming the original cure's subject after that arm goes green -- I read the summary and not the arm-by-arm lines, which is one command I did not pay for.
- **CFO-11 (2026-09-08 17:5x-18:0x CDT, cfo) — THE ICON DIALECT RULING IS IMPLEMENTED, MEASURED, AND HELD UNPUSHED ON ONE QUESTION THAT IS THE CEO'S (local `fb61b8201`, deliberately not on origin).** ⭐ THE MEASUREMENT THAT CHANGED THE JOB: `g_jcon` was WRITTEN ONCE AND NEVER READ ANYWHERE in the tree, so the `#SRC: JCON` switch has been DEAD CODE and the marker never changed behaviour; what actually happened is that `&features` and `&regions` hard-coded JCON's answers UNCONDITIONALLY for every Icon program. There was no dialect to delete, only jcon facts asserted as if they were Icon's. Done: g_jcon and the marker gone (`nm` and `nm -D` both free of it, a ruled control arm); `&features` drops Java and graphics and gains keyboard functions, VERIFIED TRUTHFUL BEFORE CLAIMING IT (SCRIP implements getch/getche/kbhit); `&regions` 0/500000/500000; `&progname` adopts iconx's invocation convention, the same principle the ruling used for regions, with the blast radius measured first (kwds.std is the only .std in either suite pinning a progname). MEASURED: kwds matches its .std BYTE-FOR-BYTE IN BOTH MODES; Arizona 70/90 both modes; Jcon 55/82 both modes; Icon master 704/705 both modes. ⛔ THE HOLD: 704 is against a 705/705 baseline I measured BY STASHING MY OWN CHANGES AND REBUILDING, so the regression is genuinely mine and not waved through. The single red, procedure_every_alt_replace_4, has a ref in corpus/tests/icon/ALL.ref that PINS SCRIP'S OWN PRE-RULING OUTPUT -- &features naming Java/dynamic loading/graphics, &regions 0/0/0, bare &progname -- i.e. exactly the jcon facts the ruling deletes. It is a SELF-PIN, not an oracle. Re-cutting a master ref so my own change reads green is fitting the oracle to the code, so it is NOT done: three options put to the ceo by mail (re-cut from icont/iconx; rule it OUTSIDE the baseline; or the ceo names another). ⛔ A CORRECTION I OWE MYSELF: I first argued the two master reds could not be mine because neither program referenced &features -- and I was WRONG. procedure_record_limit_replace_1 reaches the feature list through the PREPROCESSOR (`precheck(_JAVA, "Java")`), so my keyword grep found nothing while the coupling was real. The stash-and-rebuild baseline answered in one measurement what source-reading got backwards; that three minutes is the cheapest money this seat spent today.
- **CFO-12 (2026-09-08 18:0x CDT, cfo) — SECOND ROW MINTED FROM THE SAME SITTING: THE FEATURE LIST AND THE PREPROCESSOR SYMBOL TABLE ARE ONE FACT IN TWO FILES, ENFORCED BY NOTHING.** `feats[]` in `src/runtime/keywords.c` and `pre[]` in `src/parsers/icon/icon_lex.c` encode the same fact in two layers because Icon's preprocessor defines a symbol per feature, and Icon's own suite cross-checks them. Editing ONE side made SCRIP contradict itself out loud (`error: unexpected predefined symbol for Java`, `error: no predefined symbol for keyboard functions`) and the ONLY thing that caught it was one entry buried in an 858-entry board. Minted icon-feature-list-and-preprocessor-symbol-table-must-agree-and-nothing-enforces-it (rank 1, owner hq_B, FREE), DONE-WHEN reads RED today and carries a MUTATION arm so the gate cannot pass vacuously. Same invariant-enforced-by-nothing class as this morning's arm-2 red -- two in one day, from opposite ends of the tree.
- **CFO-13 (2026-09-08 18:0x-18:1x CDT, cfo) — recogn IS NOT A CHEAP CURE, AND I AM CORRECTING MY OWN ESTIMATE TO SAY SO.** My 09-07 NEXT called recogn *"a real backtracking defect and the cheapest honest next cure, 6 diff lines"*. Measured this sitting into a FOUR-CELL TRUTH TABLE, same procedure and same scanning expression throughout, SCRIP against Arizona icont: direct+return OK · variable+return OK · direct+suspend OK · **VARIABLE CALL OF A GENERATOR BROKEN**. THE RULE: a generator invoked through a procedure-valued variable does not inherit the enclosing scanning environment -- the match sees no subject, returns `&null`, and `&pos` never advances (direct `v="c" pos=2`, variable `v=&null pos=1`, oracle `v="c" pos=2` both ways). It is NOT the keywords (a variable-called NON-generator reads &subject/&pos correctly) and NOT variable dispatch as such (a variable-called generator that does no matching works inside a scan): it is the GENERATOR FRAME, i.e. row 676 by-name-dispatched-generator-regions-...-per-the-frame-law, rank 1, owner hq_U, CLAIMED by an HQ STOOD DOWN under MODE EXECUTIVE -- claimed and unworked. recogn rejects ALL EIGHT inputs including the trivial "c", not "nothing but the middle case" as I wrote. gener/recent/sorting are the three my own NEXT said to re-measure after recogn on a shared-cause theory, so up to FOUR of this row's eight remaining reds may sit behind 676 rather than being cheap wins. Asked the ceo to release 676 or to send me to fncs and io instead; neither taken yet, witness in the row's NEXT. ⛔ TWO PROCESS ERRORS OF MINE, both caught and both cheap: (1) I first measured recogn with NO STDIN and read "zero output" as a defect -- it reads `while line := read()`, so an empty stdin is a measurement artifact, and CLAUDE.md's own rule (`< /dev/null` on compile steps ONLY, never on a run fed by a file) is exactly the rule I broke; (2) `s4e_msg.sh send` takes its body as a DOUBLE-QUOTED SHELL WORD, so a backtick pair is COMMAND SUBSTITUTION -- a backticked identifier in my mail to the ceo ran a nonexistent command and DELIVERED THE SENTENCE WITH THE WORD SILENTLY REMOVED. Corrected by a follow-up mail and written into this root's CLAUDE.md mechanics, because a hole in a delivered sentence is invisible to the reader.
- **CFO-14 (2026-09-08 18:2x CDT, LON IN CHAT, VERBATIM) — *"Do not depend on a comment #SRC: JCON for anything."*** Recorded the same session per the digest rule. STATUS IN CODE: ALREADY SATISFIED AND MEASURED, not asserted -- `grep -rn 'SRC: *JCON\|SRC:JCON' SCRIP/src SCRIP/scripts` returns ZERO after this sitting's deletion (the marker parse and `g_jcon` were the only dependence and they are gone in `04db4ef68`); the surviving occurrences are inert TEXT inside corpus test sources and PROSE in the goal files recording the retirement, neither of which is a dependence. ⛔ BUT IT CORRECTS MY OWN REASONING, WHICH IS THE POINT OF THE INSTRUCTION: in my 18:2x mail to the ceo I classified `procedure_every_alt_replace_4` as jcon source BECAUSE IT CARRIES THE MARKER. That is depending on the comment -- for classification rather than for behaviour, but depending on it. THE HONEST BASIS IS THE ORACLE, AND I ALREADY HAD IT: Arizona icont REFUSES to compile that entry ('Line 76 # }: invalid case clause'), while the Arizona kwds.icn compiles clean through the same icont seconds earlier, so the oracle is alive and it is THAT ENTRY it refuses. Under CEO-391's own clause -- what an oracle refuses is OUTSIDE THE BASELINE -- the refusal is the whole argument and the marker adds nothing to it. Correction sent to the ceo the same minute. ⭐ THE GENERAL FORM, worth keeping: a comment cannot be evidence. It is not checked by anything, it travels with a copy-paste, and here it was upstream Arizona content sitting in 36 files that had nothing to do with our dialect. Where I want to know what a program IS, the answer comes from running it through the oracle, not from reading a line somebody typed above it.
- **CFO-15 (2026-09-08 18:1x-18:3x CDT, cfo) — THE AGREEMENT GATE IS BUILT AND WIRED, THE LAST SCORE-ROW MEMBER IS CURED, AND TWO COMMITS ARE HELD (local `84c62c5ee` on `04db4ef68`).** (1) THE GATE the ceo approved as mine: `test_gate_icon_features_and_preprocessor_symbols_agree.sh` reads `feats[]` out of the runtime and `pre[]` out of the parser and refuses a symbol that names no feature. ⭐ THE DIRECTION IS ASYMMETRIC AND I MEASURED IT RATHER THAN ASSUMING A BIJECTION: Arizona icont reports "environment variables" in &features and defines NO `_ENVIRONMENT_VARIABLES` (live `$ifdef` probe against /home/resources/icon-master, beside `_KEYBOARD_FUNCTIONS` and `_PIPES` which it DOES define), so a feature without a symbol is NORMAL and only a symbol without a feature is wrong. A gate broader than its rule gets disabled by the first person it blocks for a good reason. WIRED into `preflight_arms.txt` -- 33 arms, 2.18 s against a 40 s ceiling -- because a gate in no runner is not measuring, which is this morning's finding applied to my own work. Its row's DONE-WHEN MUTATES a scratch copy through `ICON_PRE_SRC` and reads GREEN only because the gate actually fails on the mutation. (2) THE COO ROUTED ME THE LAST MEMBER of my closed score-row class: `test_snobol4_spitbol_testpgms_suite.sh` was refusing for a reason ONE STEP FURTHER ALONG than arizona/snoflake/ipl -- not guess-the-fraction but no-suite-row-named, because it reaches the writer through `gate_score_row`, a wrapper that could not say `--suite-pass`/`--suite-total` AT ALL. Passed through and wired. ⭐ THE WRAPPER'S OWN COMMENT BLOCK ALREADY RECORDS THIS EXACT LESSON one argument earlier (it could not say `--suite` either, cured by someone else): *a convenience wrapper that cannot express a distinction the underlying tool has silently narrows what the caller can say.* Twice in one file is a pattern, not an accident. HONEST SCOPE: with m3 and m4 agreeing at 1/2 the fraction would resolve today anyway -- naming it is what keeps it resolving when the modes diverge; the BLOCKING defect was the missing `--suite`. ⛔ BOTH COMMITS HELD, and the hold is now ONLY the master entry: the ceo ruled (a) re-cut and PUSH, but the re-cut is impossible -- Arizona icont REFUSES that entry ('Line 76 # }: invalid case clause') while the Arizona kwds.icn compiles clean through the same icont seconds earlier, so the oracle is alive and there is no answer to cut. Reported with a recommendation and awaiting the ruling.
- **CFO-16 (2026-09-08 18:3x-18:5x CDT, cfo) — copy() OF A RECORD WAS NOT COPYING AT ALL (local `531f7fd61`, held with the rest).** Took fncs as the next unblocked red in my row. Its 446 differing lines have two named causes and the first one turned out to be far worse than the row's NEXT described ("copy() of a record must get a fresh serial"): `copy()`'s handler covers tables/sets, lists and co-expressions and then falls through to `*out = src`, which is right for an immutable and WRONG for a record -- `x := r(1,2); y := copy(x); y.a := 99` left **x.a = 99** against the oracle's 1. Scope isolated BEFORE curing: list, table and set all copy correctly, ONLY records aliased. Cured with a shallow field copy, as Icon's is. Icon master 704/705 both modes, byte-identical to before the change. ⛔ AND THE HALF I DID NOT SHIP, WITH THE REASON: the serial. My first attempt took it from `src.u->type->serial_next`, which is WRONG TWICE -- `dat_alloc_fill` gives every instance its OWN `DATBLK_t` copy so that counter is shared with nothing, AND it never initialises `serial_next` on that block, so reading it is READING UNINITIALISED MEMORY (it returned 0, not even a valid Icon serial). Backed out to carrying the source's id: today's displayed behaviour exactly, no new wrongness, aliasing gone. ⭐ fncs DID NOT MOVE AT ALL (446 lines before and after) because what fncs measures IS the numbering, not the aliasing -- so the real bug I cured is one the suite does not grade, and the suite's red needs the other half. Minted icon-record-serial-allocation-runs-through-three-constructors-and-a-per-instance-datblk-so-a-copy-cannot-get-a-fresh-number (rank 1, hq_B, FREE): serials run through THREE constructors against TWO DatType definitions and the shared counter lives on the DRIVER's, which a mode-4 binary does not link, so the runtime cannot ask for one the way it does for lists/tables/sets. DONE-WHEN reads RED today with the exact got-vs-want. THREE SCRIP COMMITS NOW HELD (`04db4ef68` dialect, `84c62c5ee` gate+testpgms, `531f7fd61` copy), all behind one unanswered question: the master self-pin entry the ceo ruled to re-cut and that Arizona refuses to compile.
- **CFO-17 (2026-09-08 19:0x CDT, cfo, on Lon's question "Why not get all the masters working 100%") — TWO OF THE SEVEN MASTER NUMBERS WERE HAND-SET AND ONE IS BADLY WRONG.** Checked the tree column of every master row in SUITES.tsv: five carry a commit hash, TWO carry a CELL STAMP (`PasM tree=cell-09-06`, `RebM tree=cell-09-03`) -- hand-set, never runner-written. Re-ran both at a clean pushed tree `08674b65c`. **PASCAL: 243/246 both modes** (`SUITE_BOARD total=246 m3_pass=243 m3_fail=3`, m4 identical). The cell says 248/251 -- the GAP of 3 is right, the DENOMINATOR is wrong. All three reds are named ladder rungs: rung01_var_assign_multi_group, rung02_arithmetic_mod_neg, rung05_proc_func_forward. Pascal is three cures from 100%. ⛔ **REBUS: 38/43 both modes, ONE real FAIL and FOUR stale XPASS markers -- against a cell that says 15/48.** The board has been understating rebus by ~2.5x. The single FAIL is `assign_exchange_undefined`: *output matched but rc=1, expected 0 (declare want_rc if this is correct)* -- THE OUTPUT MATCHES, only the exit code differs, and the harness itself names the cure. The four XPASS are stale xfail markers, and XFAIL was abolished by Lon 2026-09-03, so they should not exist. Separately ast_pass=64 ast_fail=32 of 96, informational by the icon precedent. ⛔ I TOLD LON THE OPPOSITE ONE MESSAGE EARLIER -- that rebus at 15/48 was "a frontend that is mostly not built rather than nearly done" -- reading the stale cell as if it were a measurement. That is the exact error this seat exists to catch, made by this seat, on the number it was quoting. Corrected to Lon in the same exchange and routed to the coo, whose tick it is, because a denominator change is a criterion change. ⛔ AND A SECOND ERROR IN THE SAME HOUR: my first two runs used the harness WITHOUT `--lang`, which silently falls back to ONE-ENTRY-PER-LINE and produced confident nonsense about a short ref file and an inconsistent wantrc sidecar. I nearly filed both as corpus defects. What caught it was the marker counts coming back 251 and 251 -- the numbers did not fit the story, so the story was wrong. A harness that changes population semantics on a missing flag rather than refusing is worth a row of its own.
- **CFO-18 (2026-09-08 19:0x-19:2x CDT, cfo) — THE ICON DIALECT ROW IS LANDED, THE HOLD IS OVER, AND THE MASTER GAINED AN OUTSIDE-BASELINE MECHANISM IT DID NOT HAVE (SCRIP `d44b67457` + `c2178f2b6`, corpus `77f067c4f`).** ceo RULED (i): a master entry the language's oracle cannot run is outside the master's baseline exactly as a package program is. There was NO such mechanism for a master, so I built it: `--outside` on the harness (name/CLASS/reason, the three columns and the discipline of a package's OUTSIDE_SPITBOL_BASELINE.tsv) with THREE GUARDS, because a list that shrinks a denominator must not be able to do it quietly -- a declared file that is missing REFUSES, a malformed line REFUSES, and a name not in the suite REFUSES, the last proved live against a probe naming `no_such_entry_at_all`. ⛔ MY FIRST WORKING VERSION MASKED THE VERY THING THE RULING SAYS NEVER TO MASK: board_icon_master.sh captures the harness and prints only what it greps, so the entry dropped from the denominator was INVISIBLE on the board whose number it changed. Caught by checking rather than assuming -- `grep -c OUTSIDE` on the board log returned 0. The board now prints every OUTSIDE_BASELINE line BEFORE the counts. ⛔ AND ONE DEPARTURE FROM THE CEO'S OWN TEXT, ON LON'S WORD: the ceo's suggested reason cited the `#SRC: JCON` marker; Lon had ruled that hour *"Do not depend on a comment #SRC: JCON for anything"*, so the recorded reason is the ORACLE'S REFUSAL alone -- icont's error text, the control that Arizona kwds.icn compiles clean through the same icont, and the ALL.csv `origin` column, which is data rather than a comment. Told the ceo I had departed and why. RESULT, all runner-written: Icon master **704/704 FAIL=0 both modes** (row written by the board itself at `c2178f2b6`), Arizona 70/90, Jcon 55/82, `nm`/`nm -D` free of `g_jcon`, score tables agree PASS(0). The 705->704 move is attributed as a CRITERION CHANGE in every commit. ⭐ A STALE-BINARY REFUSAL FIRED ON THE WAY and I read it correctly rather than as a red: the rebase and `--amend` rewrote source mtimes, so the board refused rc=2 until a rebuild. That is the guard working, and CLAUDE.md says so in as many words. TWO ROWS MINTED: the ceo's census (master refs cut from our own output, rank 1) and -- promised to Lon -- corpus-suite-harness-run-without-lang-silently-regrades-the-suite-one-entry-per-line-instead-of-refusing (rank 0, hq_T), whose DONE-WHEN reads RED today and reproduces the exact confident-wrong-answer I nearly filed as a corpus defect.
- **CFO-19 (2026-09-08 19:0x-19:3x CDT, cfo) — `rename()` WAS DECLARED WITH AN ARITY AND HAD NO BODY (SCRIP `809f18f9f`).** `next` served my own unfinished icon row (RESUME), so I took io, the cheapest red not behind another row. It died partway with `ERROR 022 -- Undefined function called`: `rename` sits in the builtin arity table as `{"rename",2}` and had NO IMPLEMENTATION ANYWHERE. `remove()` beside it works, which is exactly why the gap was invisible -- the pair reads as implemented. Cured as the C `rename()`, mirroring `remove()`: &null on success, failure on error, both matching Arizona. **io 73 -> 32 differing lines and no longer dies partway.** Icon master 704/704, Arizona 70/90, unchanged. ⭐ SPELLED WITH `strcmp` AND NOT A BID, DELIBERATELY: `builtin_ids.h` is GENERATED from the `!strcmp(fn,"NAME")` sites, so `BID_rename` cannot be referenced until the generator runs, and hand-editing a generated header would desync it from its generator with NOTHING to catch it -- no gate checks that pair today. getch/getche are already spelled this way. ⛔ io's REMAINING 32 LINES ARE TWO DEFECTS, ISOLATED WITH WITNESSES AND LEFT UNCURED ON PURPOSE, both written into the row's NEXT: (A) a FILE ARGUMENT INSIDE `write()`/`writes()` must REDIRECT the arguments after it to that file and SCRIP ignores it entirely -- `write("A", f, "B", &output, "C")` gives `ABC` on stdout with nothing in f, against the oracle's `A`/`C` on stdout and `B` in f. That is a real Icon semantic touching write and writes together, worth 12 of the 32 lines, and it is a change rather than a patch. (B) `sort()` orders FILE values by descriptor where Arizona orders them by image -- the same sort()-type-order item the ceo's coexpr note names. THE HONEST SHAPE OF THIS ROW NOW: of its eight remaining reds, FOUR are behind other rows (recogn/gener/recent/sorting behind 676; fncs behind the serial row), io needs (A), and ilib segfaults. The cheap ones are gone.
- **LEDGER 2026-09-08 19:05 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 2.73/2.63/2.24 on 16 cores · runaways 0 (the one 100% process is the cto's `scrip --compile` on a corpus package, aged **1 s**) · disk 76G/125G 64% (+1G) · flips: not re-measured · cost per flip: not computable without a credit reading. ⭐ Used `S4E_DISPATCH_PROBE_TIMEOUT=30` on `next` rather than the 120 s default -- the knob is in this file's own cost table and it is the cheapest saving available on a verb every seat runs.
- **CFO-20 (2026-09-08 19:09-19:1x CDT, ceo -> cfo) — MODE EXECUTIVE -> QUARTET: EIGHT WORKING SEATS, AND THE ECONOMY BASELINE TAKEN AT THE MOMENT OF THE CHANGE.** Lon in-chat to ceo, verbatim: *"I'm firing up 4 HQ's: HQ-COMPLETE, HQ-PERFORM, HQ-UNIFY, and HQ-TEST."* MODE file line 1 is `QUARTET` (read, not assumed). The four executives continue and four HQs join: hq_C the Icon master and the Icon package reds NO EXECUTIVE HOLDS, hq_P the SNOBOL4 master and ladder rungs 24-33, hq_U the shared engine and cross-language regressions, hq_T the instruments and THE MASTER-REF SELF-PIN CENSUS (CEO-395) -- which is the row I minted this evening, so it is hq_T's now and I will not take it. My lane is UNCHANGED: the economy + the Icon Arizona/Jcon class. The ceo's operational warning, which I am following: eight seats landing at once means PULL BEFORE YOU MEASURE and re-prove the gate after every rebase. ⭐ THE BASELINE, measured at the change so QUARTET can be priced against EXECUTIVE rather than argued later: **8 seats alive, confirmed by process and not by prose** (4 HQ + 4 executives) · **load 4.25/3.09/2.55** on 16 cores at 19:11 against an EXECUTIVE range of 0.69-2.73 across the whole post-reboot evening, so the 1-minute has roughly DOUBLED while the 15-minute has not caught up -- an hour's reading gives the real QUARTET average, a spike does not · 13 roots totalling 17G of 125G. ⛔ AND A DISK ALARM I DID NOT RAISE, BECAUSE I MEASURED TWICE: the first reading was 80G/68% during hq_U's build burst (225 processes under its SCRIP), i.e. +4G in six minutes, which extrapolates to exhausting the free 39G within the hour. The SECOND reading, minutes later, reads 75G/64%: the peak was TRANSIENT BUILD SPACE, not growth. A rate from ONE sample is not a rate. claude_C is the one root outlier at 5.0G (3.9G of it SCRIP) against 1.2-1.8G elsewhere -- worth its owner knowing, not a fleet problem. ⭐ THE ASK THAT JUST DOUBLED IN VALUE, put to the ceo for Lon: the standing kill grant. Every runaway this seat has logged came from a `t.bin` or a `scrip --run` under `/tmp/csh_run_*` or a seat scratchpad; the population that can produce one has gone from four seats to eight while the box stayed at 16 cores; and the seven runaways of CFO-1 held half this box for three days and were ended by a REBOOT rather than by anyone's decision, because no grant existed. Same bound as proposed 2026-09-07: any such process older than 2 hours.
- **CFO-21 (2026-09-08 19:2x-19:4x CDT, cfo, MODE QUARTET) — io GOES 73 -> 7 DIFFERING LINES ON TWO CURES (SCRIP `809f18f9f`, `e2bb6141e`).** `rename()` had an arity and no body; then the real one: **a file argument inside `write()`/`writes()` must redirect the arguments after it**, and SCRIP only ever honoured a file in position 0, so every later file argument was written as a VALUE to the destination chosen at the start -- io.icn's four such calls came out interleaved on stdout with the files empty. ⛔ I GOT THE NEWLINE RULE WRONG ONCE AND THE MEASUREMENT CORRECTED ME: my first version emitted the newline only when the destination being left had been WRITTEN to, which is the intuitive rule. Arizona emits one on EVERY file argument except one in position 0. The case that settles it is a REPEATED file argument -- `write(">stdout", t1, ">1c", t2, t2, ">2c", &output)` puts an EMPTY LINE in t2 for the second t2, which a was-it-written rule cannot produce -- and `write(f,"S",g,"T")` proves the position-0 exemption. SCRIP now reproduces the four-call witness byte-for-byte. ⛔ AND A WRONG COUNT OF MY OWN, caught: io runs `ls io.s?d` through `system()`, so measuring it in a scratch dir WITHOUT io.std produced two diff lines that were my harness's fault, not SCRIP's. Re-measured in a faithful directory. io's remaining 7 are two further isolated defects, neither touched: sort() orders files by descriptor where Arizona orders by image, and read() does not treat `\r` as a line terminator where Arizona does. Icon master 704/704, Arizona 70/90, Jcon 55/82 -- all unchanged, io still red until its last 7 go.
- **CFO-22 (2026-09-08 19:2x-20:0x CDT, cfo, MODE QUARTET) — THE ARIZONA io TEST IS CURED, 73 DIFFERING LINES TO ZERO IN BOTH MODES (SCRIP `809f18f9f`, `e2bb6141e`, `384033ade`). Arizona 70/90 -> **71/90 BOTH MODES**, row written by the runner at `384033ade`.** Five defects, each isolated against Arizona icont BEFORE it was written: `rename()` had an arity and no body · a file argument inside `write()`/`writes()` only ever worked in position 0 · the u/t open mode was PARSED AND THROWN AWAY · `sort()` left files in insertion order · stdout was not flushed before `pclose`. ⭐ THE ONE I WANT REMEMBERED: **the u/t mode was computed and discarded.** `icn_open_cmode` already worked out `un` for u/t and used it only to append `'b'` to the C mode -- a no-op on Linux -- and dropped it ENTIRELY for a pipe, which is the case the test exercises. So read() had ONE behaviour regardless of what the program asked for. A flag that is parsed, not used, and not refused is worse than an unimplemented one: the program says `"up"`, nothing complains, and the answer is silently the other mode's. ⛔ AND THE ONLY m3/m4 DIVERGENCE IN THE TEST, named because that is the shape the one-engine rule exists to prevent: mode 4 alone printed `--- closing output pipe` AFTER the pipe's own output, because our stdout was still buffered when `pclose` let the child through. m3 passed and m4 failed on the SAME SOURCE. Cured by flushing stdout before pclose, as iconx does. ⛔ TWO MISTAKES OF MINE ON THE WAY, both caught by building: I got the write() newline rule wrong first (was-it-written instead of positional -- a REPEATED file argument settles it), and I added a house-style separator line WITHOUT its closing `*/`, which commented out `icn_open_cmode` and produced an undefined-reference at link. The separators are 200 chars wide and I had only ever seen them truncated at 150. Icon master 704/704 both modes and Jcon 55/82 both modes, unchanged throughout.
- **LEDGER 2026-09-08 19:24 CDT (cfo, QUARTET load trend for the ceo):** credits: still no reading from Lon · load **1.41/1.71/2.26** on 16 cores -- ⭐ LOWER than the 4.25 spike I measured at 19:11 and INSIDE the EXECUTIVE range, so **the QUARTET doubling was SPIN-UP COST, not steady state**: four HQs building at once, not four HQs working at once. The honest read is that eight seats cost the box little more than four once the builds are done, and I would not have known that from the 19:11 sample alone · runaways 0 · disk 75G/125G 63%, flat · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 22:57 CDT (cfo):** credits: no reading from Lon this sitting · load 3.37/3.88/4.31 on 16 cores (thirteen seats; my own make test in flight) · runaways 0 (the one 100% process is a `t.bin` aged 3 s) · disk 76G/125G 64%, flat since 21:17 · flips since CFO-25: Icon master 704 → 707/707 both modes (three absorbed witnesses), two Arizona rows closed on computed DONE-WHENs, one swap-class row cured, `make test` 27 min → 2 min 53 s to the same arm depth · cost per flip: not computable without a credit reading. **BOARD COST this sitting, at the load it ran under:** `test_icon_arizona_suite.sh` 36.8 s @ load 1.0 and 41.0 s @ load 1.5 · `board_icon_master.sh` 43.7 s @ load 1.5 and 44.2 s @ load 5 · `make preflight` 3.8 s @ load 2 · `test_snobol4_spitbol_testpgms_suite.sh` 1.9 s @ load 5 · `test_corpus_snobol4.sh` (the SNOBOL4 master, both modes) 234 s @ load 5 · `update_icon_bench_asm.sh` 2.6 s · `util_verify_s_artifacts_owed.sh` ~60 s (clones corpus) · `make test` to the first fleet red 2 min 30 s – 2 min 59 s @ load 4-5.
- **LEDGER 2026-09-08 23:36 CDT (cfo, at the &DUMP landing):** credits: no reading from Lon this sitting · load 2.20/1.72/1.76 on 16 cores · runaways 0 · disk 76G/125G 64%, flat · flips since 22:57: &DUMP cured with a 10-witness gate (spitbol_testpgms test1's dump section now four lines from SPITBOL, all hq_S's), SNOBOL4 master 1893/1917 FAIL=0 both modes re-proved on the dump binary · cost per flip: not computable without a credit reading. **BOARD COST:** `test_corpus_snobol4.sh` 3 m 17 s @ load 1 (was 3 m 28 s @ load 4 and 234 s @ load 5 — the master's wall time barely moves with load; it is serial), `board_icon_master.sh` 34.2 s @ load 1, `test_icon_arizona_suite.sh` 33.7 s @ load 1, `test_gate_sno_dump_matches_spitbol.sh` 1.7 s, the detector proof (two rebuilds + two gate runs) ~25 s.
- **LEDGER 2026-09-09 08:37 CDT (cfo, on Lon's "next"):** credits: no reading from Lon · load 0.59/0.54/0.38 on 16 cores (the fleet slept nine hours: origin did not move in any repo between my 23:39 push and 08:37) · runaways 0 · disk 76G/125G 64%, flat · flips since 22:57 (`util_progress_flips.py --since 10h`, m3): master 1, package 6, benchmark 0 · cost per flip: not computable without a credit reading. ⛔ THE COST LINE OF THE MORNING: both fleet reds I named at 23:40 still stand on origin at 08:37 (score_tables_agree, snoflake grid 112 vs display 113 of 124; the stdin sidecar at 54 over 53), so for nine hours every seat's `make test` has stopped at arm 43 with no SNOBOL4 or Icon control arm. The grid cell is a two-number hand edit from the runner's own printed line (the writer refuses that prose cell; the coo closes it the same way), so I closed it; the sidecar entry needs its author's input, never invented.
- **THE LEDGER (append one line per reading, `date`-read):** credits (Lon's word, verbatim, with the time) · load (1/5/15) · runaways (count) · disk / % · flips since the last reading (from the coo's measure) · cost per flip.
- **LEDGER 2026-09-07 08:51 CDT (cfo):** credits: no reading from Lon this sitting (none since the seat opened) · load 11.24/10.32/9.61 on 16 cores (a coo IPL compile burst and the cfo's own first `make` on top of the seven runaways) · runaways 7 (unchanged from CFO-1; no grant recorded, nothing killed) · disk 90G/125G 76% (+2G since CFO-1) · flips since CFO-1 08:48: master 0, package 2 (`util_progress_flips.py --since 2h --per hour`, m3, rows 10505 in window) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:20 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 10.51/10.71/10.78 on 16 cores (the cto compiling jtran on top of the seven runaways) · runaways 7 (unchanged; no grant recorded, nothing killed; the two hq_T `scrip --run` are 15 h old, the five `t.bin` 44–59 h) · disk 88G/125G 74% (−2G since 08:51: my scratch make logs and the coo's tmp dirs came and went) · flips in the hour 08:20–09:20 CDT (`util_progress_flips.py --since 1h --per hour`, m3, rows 11805): master 0, package 17 (13 in the 13Z bucket, 4 in the 14Z bucket; the coo's Gimpel 104/126 and IPL 75/89 landings) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:39 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 9.95/11.52/11.72 on 16 cores (the 5-min and 15-min carry the cfo's eight parallel port-trace arms of 09:2x) · runaways 7 (unchanged; a fresh `t.bin` under `/tmp/csh_run_6nevwlxv` seconds old is somebody's live harness run, not a runaway) · disk 90G/125G 76% · flips in the 30 min since 09:07 (`util_progress_flips.py --since 30m`, m3, rows 9651): master 4, package 13 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:54 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 7.80/9.33/10.63 on 16 cores (the quietest reading yet: only the seven runaways and the desktop) · runaways 7 (unchanged, 15.5 h and 44–60 h old; no grant recorded) · disk 91G/125G 76% · flips in the 20 min since 09:34 (`util_progress_flips.py --since 20m`, m3, rows 5922): master 5, package 8 (the cfo's three jcon m4 among them) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 10:13 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 11.93/9.90/9.85 on 16 cores (the ceo compiling an Icon package and a `toby_bin` jcon witness on top of the seven runaways) · runaways 7 (unchanged, 15.8 h and 44.5–60 h old; no grant recorded) · disk 93G/125G 78% (+2G since 09:54: the four regenerated .s artifact sets and the corpus clone the drift check makes under /tmp) · flips in the 15 min since 09:58 (`util_progress_flips.py --since 15m`, m3, rows 5469): master 1, package 0 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 10:33 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 9.24/9.94/10.04 on 16 cores (the ceo's SNOBOL4 master harness linking t.bin on top of the seven runaways) · runaways 7 (unchanged: the two hq_T `scrip --run` 16.1 h old, the five `t.bin` 44.8–60.6 h; no grant recorded, nothing killed) · disk 91G/125G 77% · flips in the 15 min since 10:14 (`util_progress_flips.py --since 15m`, m3, rows 6524): master 1, package 2 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 12:41 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 7.69/8.17/7.94 on 16 cores (the quietest yet; my five landing boards had just finished) · runaways 7 (unchanged, 18.5 h and 47–63 h old; no grant recorded, nothing killed) · disk 91G/125G 77% · flips in the 35 min since 12:06 (`util_progress_flips.py --since 35m`, m3, rows 5282): master 1, package 0 (my five Arizona flips are on a dirty tree and count only once landed) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 17:14 CDT (cfo, first sitting after the box came back):** credits: still no reading from Lon (none since the seat opened 2026-09-07) · ⭐ THE BOX REBOOTED 2026-09-08 16:58 CDT (`uptime -s`, `who -b`), and that single event settled the whole runaway census this seat has carried open since CFO-1: load **1.36/0.79/0.44** on 16 cores at 17:14 (against 7.69-11.93 for every reading of 2026-09-07) · runaways **0** -- all seven (five `t.bin` 44-63 h old, two hq_T `scrip --run` 16 h old) are gone, KILLED BY THE REBOOT AND NOT BY ME: no grant was ever recorded and nothing was ever killed under one, so the standing-grant ask stays OPEN for the next batch rather than being answered by this · the one `/tmp/csh_run_mxk75vie` + `t.bin` alive at 17:14 is the ceo's LIVE harness run, aged **0 s** (`ps -eo etimes`, cwd `/home/claude_ceo/SCRIP`) -- named here so it is never miscounted as the eighth · disk **74G/125G 62%**, down 17G from 91G/77% at 12:41 CDT 2026-09-07: that 17G WAS the orphaned `/tmp/csh_run_*` trees, which is the first hard price tag this seat can put on the runaway class · flips in the 24 h to 17:17 (`.github/scripts/util_progress_flips.py --since 24h --per hour`, m3, 43 rows in window): master 0, package 0, benchmark 0 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 17:38 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 2.06/2.23/2.04 on 16 cores (the box has stayed quiet 40 min after the reboot; contrast 7.69-11.93 for every reading of 09-07) · runaways 0 -- ⚠ ONE WATCH ITEM, NOT A RUNAWAY: PID 707636 `/home/claude_cto/SCRIP/scrip --run arbno_fence_span_replace_branch_2.sno` at 100% CPU, aged **1 s**, so it is the cto's live fuzz row; NAMED HERE BECAUSE IT IS THE SAME SHAPE AS THE TWO 16-HOUR RUNAWAYS OF CFO-1 (`arbno_pos_rpos_branch_81.sno`, hq_T's fuzz row) -- the arbno fuzz class is the one that has produced hangs, so a `scrip --run arbno_*` still alive at the next reading is a runaway and not a slow test · disk 75G/125G 63% (+1G in 24 min, ordinary board churn) · flips in the hour to 17:38 (`--since 1h`, m3, 8682 rows in window): master 0, package 3, benchmark 0 -- the fleet is landing again after the reboot · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 18:06 CDT (cfo, on Lon's "continue"):** credits: still no reading from Lon · load **0.69/1.25/1.70** on 16 cores, the quietest reading this seat has ever recorded · ⭐ THE ARBNO WATCH ITEM FROM 17:38 IS CLEARED: no `scrip --run` and no `t.bin` alive at all, so the cto's `arbno_fence_span_replace_branch_2.sno` completed normally and my call that it was live work rather than the eighth runaway was right -- naming it with its age is what made that checkable instead of a guess · runaways 0 · disk 75G/125G 63% (flat since 17:38) · flips: not re-measured this tick · cost per flip: not computable without a credit reading. HOUSEKEEPING: the held icon commit `fb61b8201` is parked on branch `held/icon-dialect-awaiting-ceo-ruling` and main is back on origin, so the hold cannot block an unrelated landing and cannot be lost.
- **LEDGER 2026-09-08 18:41 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 1.76/1.85/2.05 on 16 cores · runaways 0 (the one process at 100% is `csnobol4` running the coo's snoflake suite, aged **5 s** -- live work, named with its age so it is checkable) · disk 75G/125G 63%, flat for an hour · flips: not re-measured this tick · cost per flip: not computable without a credit reading. ⭐ HOUSEKEEPING THAT WAS MY OWN ERROR TO FIX: I had THREE commits held behind ONE unanswered question, and only one of them caused the thing being questioned. Split them -- the agreement gate and the copy() cure are independent of the master self-pin and are now PUSHED (`1d84a33a1`, `08674b65c`) with Icon master **705/705 FAIL=0 both modes**, which also proves the 704/705 was entirely the dialect commit's doing. Only the dialect commit is still held, on `held/icon-dialect-awaiting-ceo-ruling`. Holding unrelated work behind an unrelated ruling is a cost I was paying for no reason. ⛔ AND I WAS OVERTAKEN ON PART OF IT: origin landed `ebc3a2915` and `2d5d33099` -- the testpgms/gate_score_row cure I had in hand -- while I was holding. They chose to BYPASS the wrapper rather than widen it, so I DROPPED my version rather than fight the conflict; the work was duplicated because I sat on a ready cure. The lesson is the same one as the split: a hold has a price and it is paid in other people's time as well as mine.
- **LEDGER 2026-09-09 09:06 CDT (cfo, at the args landing):** credits: no reading from Lon this sitting · load 11.69/10.71/7.62 on 16 cores (thirteen seats' live boards and compiles: the top five processes were 0-2 s old) · runaways 0 (no process older than two hours above 5% CPU) · disk 77G/125G 65%, +1G since 23:36 · flips since 23:36: args cured (Arizona 73 → 74/90, master 707 → 708).
- **LEDGER 2026-09-09 09:5x CDT (cfo, at the ilib-3 landing):** credits: no reading from Lon this sitting · load 2.81, 4.86, 6.25 on 16 cores · runaways 0 (processes older than 2 h above 5% CPU) · disk 86G/125G 72% · flips since 09:06: ilib rungs 1-3 (no board move yet: ilib's fourth rung holds the row), Icon master 708 → 713/728 by denominator growth + 2 witnesses.
- `board_icon_master.sh` · 47.8 s · load ~5 · 881 entries, 728 run-graded both modes (SCRIP 17ac1bc39-DIRTY at the time; the +20 entries vs 09:06 are hq_V's rungs + my two).
- `make test` TO ARM 190 (the sidecar fleet red) · 3 min 20 s · load ~5; the 97 arms after it by hand · 11 min 38 s · load ~5 = ~15 min for the whole blocking set at load 5 (17 min at load 10 this morning).
- **LEDGER 2026-09-09 10:2x CDT (cfo, at the ilib-4 landing):** credits: no reading from Lon this sitting · load 9.24, 9.35, 8.87 on 16 cores · runaways 0 · disk 84G/125G 71% · flips since 09:5x: ilib rung 4 (Zona holds 74/90: ilib's remaining 3 rungs), master 714/728 unchanged (witness handed, not absorbed).
- `make test` GREEN END TO END · 17 min 45 s · load ~9 · SCRIP b8b7bcf9b — the first full green measurement of the whole blocking set this seat has (CFO-26's 2 min 53 s stopped on a fleet red; CFO-27's 4 min 50 s + 12 min 25 s was in two pieces).
- `test_icon_arizona_suite.sh` · 1 min 11 s · load ~9 (38 s at load ~5 an hour earlier) · `board_icon_master.sh` · 1 min 45 s · load ~9 (47.8 s at load ~5).
- **LEDGER 2026-09-09 11:0x CDT (cfo, at the ilib-7 landing):** credits: no reading from Lon this sitting · load 2.48, 0.87, 0.58 on 16 cores · runaways 0 · disk 81G/125G 68% · flips since 10:2x: ilib rung 7 (Zona 75/90 on the merged tree, +1 the coo's; IcnM 716/728, +2 the coo's).
- A SECOND CHECKOUT's FULL BUILD (git worktree + make, -O0) · ~35 min · load ~10 · the price of attributing one defect to origin by measurement; plan it, do not discover it.
- `make test` GREEN END TO END · 22 min 43 s · load ~9 · SCRIP 96414a712 (17 min 45 s an hour earlier at the same load: the box carries more boards now).
- **LEDGER 2026-09-09 16:58 CDT (cfo, at the ilib-8 landing):** credits: no reading from Lon this sitting · load 5.34, 6.29, 8.91 on 16 cores · runaways 0 · disk 83G/125G 70% · flips since 11:0x: ilib rung 8; rung 6 diagnosed (link order); CEO-468 hang bisect in flight.
- **LEDGER 2026-09-09 17:21 CDT (cfo, at the ilib-6 landing):** credits: no reading from Lon this sitting · load 3.89, 3.93, 5.61 on 16 cores · runaways 0 · disk 81G/125G 68% · flips since 17:0x: ilib rung 6 (a criterion change, zero board movement), one fleet hygiene cure, CEO-468 closed as a timeout.
- BISECT BY INCREMENTAL CHECKOUT (git checkout + make in the main checkout, per-path objects) · 47 s for 6 commits · load ~5 · vs ONE worktree full build ~35 min (CFO-30): the right instrument for 'which commit' when the candidates are recent and the tree is otherwise clean.
- `test_icon_arizona_suite.sh` · 59 s · load ~5 · `board_icon_master.sh` · 1 min 4 s · load ~5 · 750 entries · `test_icon_jcon_suite.sh` · 1 min 23 s · load ~5.
- **BOARD COST TABLE (append as measured):** runner · wall seconds · load during · tree.
- `test_icon_arizona_suite.sh` · 55.3 s · load ~11 · SCRIP 37a3fc8d9-DIRTY (the args cure) — 44 s at load 2 in CFO-2x: load 11 costs +25%.
- `board_icon_master.sh` · 75.0 s · load ~10 · 861 entries, 708 run-graded both modes — 44 s at load 2: load 10 costs +70% on this runner (it runs two modes in sequence).
- `make test` TO ARM 180 (the corpus-dirty refusal) · 4 min 50.8 s · load ~10 · the 105 arms after it run by hand: see CFO-27.
- `board_icon_master.sh` · 44.8 s / 43.6 s · load ~2 · 858 entries, 705 run-graded both modes -- and the ORIGIN-BASELINE version of it (stash + full rebuild + board) cost ~3 min total, which is the price of answering "is this regression mine" with a measurement instead of an argument. It was worth it: the argument was wrong.
- `test_icon_arizona_suite.sh` · 54.2 s then 44.1 s · `test_icon_jcon_suite.sh` · 73.4 s then 71.7 s · load ~2, three boards in parallel on the post-reboot box
- ⭐ REBUILD ASYMMETRY, worth knowing before an edit: touching `src/runtime/keywords.h` (a runtime header) costs a **125.7 s** rebuild; touching `src/lower/lower_icon.c` (one .c) costs **1.0 s**. Same landing, same seat, 125x apart -- so batch header edits and never discover one late.
- `make test-postoffice` · 45.0 s · load ~2 · SCRIP `348eed586` -- ⚠ ITS OWN RECIPE DOCUMENTS "~13s MEASURED" and the target's stated rule is "keep this target seconds-cheap": it has grown 3.5x past its written budget with no arm noticing, because nothing pins it the way preflight_arms_stay_cheap pins preflight. A budget that lives only in a comment is not a budget.
- `make test` STOPPED AT THE ARM-2 FALSE RED · 49.4 s · load 1.36 · SCRIP `b7f48462a-DIRTY` -- it certifies arms 1-2 ONLY and reads as the whole blocking set; the true price of a pre-landing run for a seat that has not committed
- `make test` RUN TO ITS FIRST REAL RED (post-commit) · 477.1 s · load ~2 · SCRIP `6a61e40d9` · 56 gates green then test_gate_pl_meta_call_reaches_control_constructs 2-of-23 -- so a full blocking set is ~8 min on a quiet box, ten times the false-red run it hides behind
- incremental `make` after a pull that touched `src/driver` + runtime (19162985f) · 3.3 s · load 2.4 · the whole cost of not running a board on a stale binary
- `test_snoflake_suite.sh` (180 programs x 2 modes, live `sbl -bf` oracle, writes its own SCORE.md cell and SUITES.tsv row) · 36.7 s · load 2.4 · SCRIP `b2660262e`
- `test_gate_banner_leads_with_the_suite_line.sh` · ~1.5 s · `test_gate_score_cell_no_silent_prose_loss.sh` (9 arms) · ~3 s · `util_score_row.py selftest` · ~2 s -- the three arms that carry the score-row row's DONE-WHEN, together cheaper than one board
- `test_pascal_pat_suite.sh` · 69.6 s · load 10.9 (1-min) · SCRIP `ac9fe5e9f` before the 10 s probe; 60 s of it was the fpc oracle spinning on iso7185pat.pas, 9.6 s the 427 rejection tests × 2 modes
- `test_pascal_pat_suite.sh` · 23.6 s · load 13.0 · SCRIP `9a7318868` (the 10 s probe: two declared-UNGRADABLE acceptance programs, one 10 s spin + one 0.1 s rejection)
- `fpc -Miso` on iso7185pat.pas alone · never finishes (killed at 300 s and 240 s) · load 10 · fpc 3.2.2; a 5-minute wait per attempt is the cost of "raise the bound"
- `test_gate_package_runners_print_the_inventory.sh` · 4.5 s · load ~12 · SCRIP `ac9fe5e9f`
- first `make` of a fresh cfo checkout · 195 s · load ~10 · SCRIP `ac9fe5e9f`
- port-trace arms, eight in parallel at load ~11, SCRIP `9a7318868` (old body) → `1f87a7aa9` (cured body), wall seconds before → after: sno 47.7 → 43.6 · sc 14.5 → 16.7 · pas 18.1 → 19.8 · reb 22.6 → 24.1 · pattern 36.9 → 38.3 · icn 33.1 → 34.0 · raku 7.6 (died at rung 12) → 29.4 (68 witnesses) · pl 62.1 (died at rung 10) → 418.3 (293 witnesses)
- `test_icon_jcon_suite.sh` · 85.2 s · load 12.7 · SCRIP `1f87a7aa9` + the wiring uncommitted (91 shipped, 82 graded × 2 modes, 20 s timeout) · 76.1 s · load 9.0 · SCRIP `1e8b5e951`; the m3 half alone 35.7 s
- `test_corpus_snobol4.sh` (SNOBOL4 master, 1858 × 2 modes + 28 ast) · 287 s · load ~9 · SCRIP `1e8b5e951` + the STNO cure uncommitted
- `board_icon_master.sh` (Icon master, 858 entries) · 67 s · load ~9 · same tree
- `test_gate_pattern_shared_box_witnesses.sh` · 36 s · load ~9
- `util_verify_s_artifacts_owed.sh` (dry-run drift check over four sets) · ~60 s; the four regens themselves 1.1 + 4.0 + 5.8 + 3.6 s
- `s4e_msg.sh next` when it serves the package-inventory row · 120 s of probe (its budget) PLUS an orphaned package board that ran ~6 min more (measured 09:39 → 09:47 CDT via 178 ipl progress rows)
- `test_snobol4_gimpel_suite.sh` (144 drivers × 2 modes via scorecard_snobol4.sh, live oracle) · 54.6 s · load 11.1 · corpus with the three aliases · 56.3 s · load ~11 · with one alias
- `util_build_package_suite.py` on aisnobol (9 programs, oracle refs cut) · 1.2 s · `test_snobol4_aisnobol_suite.sh` · 8.6 s
- `s4e_msg.sh next` with `S4E_DISPATCH_PROBE_TIMEOUT=30` · ≤30 s of probe per call instead of 120 s (the env knob the dispatcher already honours; the orphan it leaves is the same)
- `board_icon_master.sh` · 52 s · load 8.8 · SCRIP 2594133f5 + the conversions cures uncommitted
- `test_icon_arizona_suite.sh` · 63.6 s · load ~9 · same tree · `test_icon_jcon_suite.sh` · 77.4 s · `test_corpus_snobol4.sh` · 270 s · `test_pascal_pat_suite.sh` · 19.2 s (all five in parallel at load ~9 → 8)

## THE CFO LOOP
1. `check` the inbox; route economy asks to Lon in chat, grants into this file the same turn.
2. The ledger line (above), every sitting and on every Lon "next".
3. The runaway census; kill only under a recorded grant.
4. `next` — the EASIEST FREE rank-0 row; cure; measure through the suite's own runner; flip to `coo`; repeat.
5. A landing = pushed + the runner's row rewritten + the ledger line; the banner verdict is computed, never typed.
