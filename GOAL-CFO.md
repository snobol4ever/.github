# GOAL-CFO.md — THE CFO SEAT: THE ECONOMY (opened 2026-09-07 08:06 CDT on Lon's word)

Lon, in-chat to ceo, 2026-09-07 08:0x CDT, verbatim: *"I have an idea to hire another executive. How about a CFO, a Chief Financial Officer. Then we will have 4, CEO, CTO, COO, and CFO. What do you think of my idea?"* · *"The /home/claude_cfo is ready to populate."* The ceo's answer Lon accepted: the economy is real and unowned — eight runaway test processes pinned half the box from Sept 4–5 to Sept 7 morning, boards crawled at load 26 on the evening of Sept 6, and the fleet and nine-HQ experiments burned the credits Lon named — so the CFO owns it, on the condition that a fourth Fable seat is never idle: in spare cycles it works the easiest open bug.

## LIVE CURSOR

- **CFO-0 (2026-09-07 08:06 CDT, ceo) — THE SEAT IS OPENED.** Root `/home/claude_cfo` (Lon created it 08:0x), stocked by `.github/scripts/populate_cfo_root.sh` (Lon runs it: the ceo cannot write into a sibling root), identity `cfo` in the bus (SCRIP `s4e_msg.sh`: root map, `s4e_root`, `s4e_hqboxes`, `s4e_is_hq`), postoffice `cfo/inbox` with `HQ` = ceo. MODE EXECUTIVE (2026-09-07 07:50 CDT): the working seats are ceo, cto, coo, cfo, all Claude Fable 5.1 at high effort (Lon 07:5x: *"I set each executive to Fable 5.1 at 'high' effort, not 'xhigh' as before."*). THE ORDER OF WORK IS SNOBOL4 AND ICON TO 100% for the announcement; the other five ship as they stand.
- **FIRST ACT (the CFO's, on its first sitting):** the runaway census. At 07:08 CDT on 2026-09-07 the ceo measured eight processes at 96–99% CPU each on a 16-core box: five `t.bin` test binaries started Sept 4 21:53 and Sept 5 11:34–13:42 under `/tmp/csh_run_*` (the corpus_suite_harness run directories), two `/home/claude_T/SCRIP/scrip --run arbno_pos_rpos_branch_81.sno` started Sept 6 18:09 and 18:23 (hq_T's fuzz row), one `iconx ./jtran_icont` started Sept 6 16:15 under hq_B's scratchpad. Name each with owner and age in this file, ask Lon in chat for a standing grant to kill any test binary older than a bound he names, record the grant here verbatim, then kill them and record the load before and after.
- **CFO-1 (2026-09-07 08:48 CDT, cfo) — THE RUNAWAY CENSUS, RE-MEASURED.** `uptime` load 9.00/9.23/9.12 on 16 cores, `df -h /` 88G used of 125G (74%). SEVEN runaways at 96–99% CPU each (the ceo's eighth, `iconx ./jtran_icont` under hq_B's scratchpad, is already gone): PID 1421287 `t.bin` cwd `/tmp/csh_run_lva5rxyf/tmpu86wbuz_` started Fri Sep 4 21:53 (2d 11h); PID 1258832 `t.bin` cwd `/tmp/csh_run_m42bpoy5/tmpf2kagsmq` Sat Sep 5 11:34; PID 3038486 `t.bin` cwd `/tmp/csh_run_lwvgcy6l/tmpk12ytwnn` Sat Sep 5 13:04; PID 3064534 `t.bin` cwd `/tmp/csh_run_d_6n83j0/tmp12qbe24y` Sat Sep 5 13:06; PID 3571014 `t.bin` cwd `/tmp/csh_run_7ujt_0r5/tmpqds6ezot` Sat Sep 5 13:42 (all five are corpus_suite_harness test binaries, owner unrecoverable from cwd, 43–59 h old); PID 1561608 and PID 1960798 `/home/claude_T/SCRIP/scrip --run arbno_pos_rpos_branch_81.sno` cwd `/tmp/csh_run_9kaolihq/…` and `/tmp/csh_run_9o71ha9k/…`, started Sun Sep 6 18:09 and 18:23 (hq_T's fuzz row, 14.5 h old). Nothing killed: no grant is recorded yet. ASKED LON IN CHAT this sitting for a standing grant to kill any `t.bin` or `scrip --run` test process under `/tmp/csh_run_*` or a seat scratchpad older than a bound he names (proposed: 2 hours). His words land here verbatim when given; load before/after follows the kill.
- **CFO-2 (2026-09-07 09:11 CDT, cfo) — FIRST BUG THROUGH THE BUS: pat's UNGRADED remainder reaches ZERO, and the PAT board gets 46 s cheaper.** `next` served the rank-0 inventory row every-package-runner-prints-shipped-graded-ungraded-and-ungradable-and-the-leaderboard-carries-the-inventory (blocker of the csnobol4/spitbol lockdown row) whose NEXT said no runner was left to wire; the cheapest remainder was pat's one UNGRADED row, iso7185pat.pas TIMEOUT ("fpc -Miso does not finish within 60s; raise the bound or profile why"). MEASURED, not raised: the oracle does not finish at 300 s either (ppcx64 100% CPU, no diagnostic); a prefix bisection stops at line 77 `label 0, 3, 9999, 0004;` and a three-line program declaring `label 0;` reproduces the spin under -Miso and -Mextendedpascal (labels 1, 01, 0004, 9999 compile; -Mfpc/-Mtp reject the program at line 114). ISO 7185 permits label 0, so this is fpc 3.2.2's own behaviour on a conforming program: moved to UNGRADABLE/ORACLE_REFUSES with that reason (corpus `0e9835068`). The runner now re-probes a declared-UNGRADABLE program at a 10 s bound through lib_inventory.sh's own reader and shouts RULING STALE if the oracle ever builds it (SCRIP, see the cost table). pat reads shipped=429 graded=427 ungraded=0 ungradable=2; the package-inventory gate passes (21 arms, 0 violations); the LF gate passes. Remaining UNGRADED in the row, none in the cfo's lane: ipl 200 rows (Icon package reds are the cto's class work), gimpel 3 + aisnobol 2 NEEDS_VENDORED_SOURCE (files that exist nowhere in corpus), jcon 1 NEEDS_RUNNER_WIRING (two-file icont link + argv sidecar). Pascal-P5's own pcom/pint exist only as source under `/home/resources/Pascal-P5/source`; building them would give the acceptance test its native oracle, noted, not done. Claim released.
- **CFO-3 (2026-09-07 09:34 CDT, cfo) — SECOND BUG THROUGH THE BUS: the port-trace instrument stops blaming its tracer for a witness that never compiled (SCRIP `1f87a7aa9`).** `next` served rank-1's blocker port-trace-zero-lines-check-cannot-tell-a-compile-time-refusal-from-a-tracer-that-is-not-firing (no rank-0 row was FREE). Proven red in 1 s, cured in the shared body lib_port_trace.sh that eight gates source: a --compile refusal is a named red row per witness and the walk continues, an m4 NOBUILD no longer trips the zero-lines check, and the message that remains says the witness compiled and ran. All eight arms graded as control on one binary before and after (see the cost table): four FAIL counts unchanged, pattern PASS, icn UNPROVEN either way (it carries its own copy of the fold, named in the row's NEXT for the icon lane), pl and raku moved from UNPROVEN-at-the-first-refusal to a full FAIL(1) measurement with 10 and 14 refusals named. The DONE-WHEN reads GREEN; closed by computed `done`. THE ECONOMY OF IT: the pl arm now costs 7 min instead of 1 because it measures 293 witnesses instead of dying at the tenth rung; that is the price of an honest denominator and it is recorded, not hidden.
- **CFO-4 (2026-09-07 09:53 CDT, cfo) — THIRD BUG THROUGH THE BUS: jcon's UNGRADED remainder reaches ZERO and three m4 programs flip on a runner defect found on the way (corpus `4a97848e1`, SCRIP `1e8b5e951`).** `next` served the inventory row again (its DONE-WHEN probe times out at 120 s, so it is served unverified every time; named in the row's NEXT). The cheapest remainder was jcon's link1.icn NEEDS_RUNNER_WIRING: cut link1.ref from the shared icont/iconx two-file build with argv `a b c` (link1.args; byte-stable, README names both sidecars as ours), taught the runner to read a program's `link` lines as its module list, take argv from .args and grade .std-or-.ref. The first full run read link1 PASS m3 / FAIL m4: the m4 binary was handed the driver's `--` as argv[1] and printed `{ -- }` first, a pre-existing defect that also sat on every .dat program reading *args in m4. Cured (separator for m3 only); jcon m4 46 → 49 (link1, htprep, mffsol; geddump moved FAIL → CRASH, SCRIP's own, same verdict as m3), m3 49/91; PACKAGE_INVENTORY jcon shipped=91 graded=82 ungraded=0 ungradable=9. Inventory gate PASS, LF gate PASS, SCORE.md icon/vendor and THE SUITE TABLE's Jcon row rewritten by the runner (49/91 both modes). Claim released; the remainder (ipl 200 in the cto's lane, gimpel 3 + aisnobol 2 needing upstream sources) is in the row's NEXT.
- **CFO-5 (2026-09-07 10:06 CDT, cfo) — FOURTH BUG THROUGH THE BUS: THE TRACE TRUNK IS DONE (SCRIP `a4a01ffb3`; corpus `ff41d793d`, `1805a4ba1`, `49c5c55f0` + the icon bench .s).** `next` served rank-1's blocker snobol4-trace-types-v-l-c-r-k-a-print-spitbols-banner-in-both-modes (owner hq_P). Its DONE-WHEN was red on ONE line: &LASTNO inside a VALUE trace fired after `N = F(2)` returned read 4 (the trace function's own statement) where SPITBOL reads 12. A five-reading witness against sbl -bf showed the rule: SPITBOL restores &STNO/&LINE to the CALLING statement when a DEFINE'd function returns. Cured in the DEFINE template beside the &FNCLEVEL bookkeeping it mirrors: the caller's g_stno/g_line go into a per-nesting-level slot (`rt_stno_stack[rt_k_level]`, libscrip_rt) at both entry sites and come back at all three exit sites, rax/rcx only, one engine so both modes. Controls: the three witnesses PASS m3+m4, the LASTNO witness == oracle m3+m4, SNOBOL4 master 1858/1858 FAIL=0 both modes, Icon master 704/705 (its one red, procedure_every_alt_replace_4, is red on origin before this change and green only in the ceo's unpushed `7e15ab713`), pattern gate PASS, strip_comments clean. Every function prologue changed, so all four .s artifact sets were regenerated and pushed (benchmark 20, demo 10, prolog bench 23, icon bench 20). Closed by computed `done`; the six trace event rows it blocked open by themselves. Still owed to the row's LANDING clause and named in its NEXT: the witness as a SNOBOL4 ladder rung, which "hq_T declares" and hq_T is stood down, so it is the coo's SNOBOL4 lane to number. TWO ECONOMY FINDINGS on the way, both routed to the coo for the ceo: (1) `next`'s 120 s DONE-WHEN probe of the package-inventory row runs board_packages.sh, the timeout kills the parent only, and the ipl child ran on for six minutes under my identity (178 progress rows at 14:47Z) and drove a stray IPL score stamp the coo reverted: an over-budget probe is a runaway by construction and costs a whole package board per `next`; (2) a board-shaped DONE-WHEN cannot be probed inside the budget at all, so that row is served unverified every time.
- **CFO-6 (2026-09-07 10:25 CDT, cfo) — A RULING ROW RELEASED WITH ITS BILL, AND GIMPEL'S UNGRADED REMAINDER REACHES ZERO.** `next` served snobol4-value-call-poisons-next-lazy-comparator-oracle-quirk (rank-1's blocker, owner hq_S): six visits and five releases in gdb on SPITBOL's garbage collector for an oracle accident that SCRIP already gets right. Released untouched with the reasoning in its ledger and ruling (b) proposed to the ceo by mail with the cost tally. Then the package-inventory remainder in my own lane: upstream gimpel (/home/resources/gimpel) ships the three "missing" include targets under DOS 8-character names (STRINGOU.INC, RESOLUTI.INC, SYSTEM.INC) while the include lines use the long names. stringout.sno vendored as a byte-identical alias, FRSORT_driver graded (ref cut from sbl -bf, SCRIP red both modes: m3 wrong order, m4 ERROR 022), TIMER/TIMEGC ruled NONDETERMINISTIC on two-run oracle evidence (wall-clock timings) with their aliases deliberately NOT vendored, because the scorecard grades live and a first attempt with them in-tree over-summed the inventory by two. GIMPEL_BOARD m3 104/127 m4 104/127 (the denominator grows by one honest red), inventory shipped=290 graded=127 ungraded=0 ungradable=163, inventory gate PASS, LF gate PASS. aisnobol ATTEMPTED AND REVERTED: its SNOCORE is in the package's own upstream/ dir, but vendoring it makes the suite builder absorb SIR/TEST against a page-formatted listing carrying ERROR 116 with no "in statement" footer, which sbl_died() cannot see: an instrument gap (routed to the coo, written into the row's NEXT), not a corpus edit; nothing of it is on origin. Six of the eight lockdown packages now read ungraded=0; ipl (200, the cto's class lane) and aisnobol (2, behind the builder gap) remain. CORRECTION (10:29): .github commit `240a2c7ed`'s message says the gimpel runner rewrote SCORE.md; it did not (that runner grades via scorecard_snobol4.sh and writes no cell), so only this file changed in that commit and the Gimpel row 104/127 reaches the table through the coo's tick, to whom the numbers went by mail.
- **CFO-7 (2026-09-08 17:2x-17:3x CDT, cfo) — FIFTH BUG THROUGH THE BUS: a REFUSED score write stops lying about what it wrote, and the last two hand-set suite rows write themselves (SCRIP `b2660262e`).** My claimed rank-0 row score-row-write-rewrites-the-board-then-refuses was sitting UNCOMMITTED in this root at the start of the sitting -- STEP 1 and STEP 2 done, nothing on origin. `util_score_row.py write` persisted SCORE.md and only then called suite_sync, so a refusal exited 2 over a board that HAD moved and the three runners reading that rc printed 'SCORE.md NOT UPDATED -- record this row by hand' over a row they had just written. The whole suite-mirror DECISION is hoisted above the write as `suite_sync_decide()`: a refusal now leaves both files byte-identical, rc=2 means nothing was written, and the one genuinely partial case (banner --set fails AFTER the board write) names which file moved and which did not. Proof is a cmd_selftest arm that snapshots the scratch board and asserts byte-identity after a refusal; SELFTEST PASS end to end, test_gate_score_cell_no_silent_prose_loss.sh GATE PASS(0) 9 arms -- the row's DONE-WHEN, both clauses. THE COO'S HALF, which was the inbox brief this sitting: arizona was cured in b7f48462a by naming its fraction, but snoflake and ipl still passed --suite with no --suite-pass/--suite-total over a --text carrying several distinct bare fractions, so fraction_from_text correctly declined to guess (proved against the real functions: Flake 3 competing fractions, IPL 2) and the coo hand-set both every run. Both now name the fraction their own prose already calls the table's reading -- BOTH_STREAM/TOTAL for Flake, the oracle-diffed RUN tier M3_RUN_PASS/RUN_GRADED for IPL. MEASURED THROUGH THE SUITE'S OWN RUNNER, not asserted: `test_snoflake_suite.sh` on the pushed tree printed `suite table: snoflake -> 99/180 on 2026-09-08 (tree b2660262e) -- SUITES.tsv rewritten in the same call` with NO 'NOT UPDATED' line, and 99/180 is EXACTLY what the coo had been setting by hand, which is the check that the runner names the RIGHT fraction and not merely a resolvable one. ipl is wired identically and is the cto's board to run. Flipped to the coo; ipl stays hand-set until its board runs once.
- **CFO-8 FINDING (2026-09-08 17:18-17:31 CDT, cfo) — `make test` IS RED AT ARM 2 OF ~65 FOR ANY SEAT THAT HAS NOT COMMITTED YET, AND SELF-HEALS ON THAT SEAT'S FIRST COMMIT.** `test_gate_banner_leads_with_the_suite_line` ARM 4 matches the verdict with `(OK|STOP) [A-Z]` while the banner has a THIRD class, `NOTHING LANDED`, printed when a session has no commit and no FINDING; the arm reads LIVE session state, so it grades the seat rather than the tool and breaks `test-postoffice`'s own hermetic membership rule. Measured on ONE tree with no edit to gate or banner: GATE FAIL 1-of-6 pre-commit, GATE PASS 6-arms immediately after commit `6a61e40d9`; stash-controlled, so it is origin's and not this working tree. ⛔ AND IT WAS HIDING A REAL RED: run 1 died in 49.4 s having certified arms 1-2 while calling itself THE blocking set; run 2, one scoring commit later, ran 477.1 s, went 56 gates green and hit `test_gate_pl_meta_call_reaches_control_constructs` 2-of-23, arm (b) 'if-then-else must COMMIT' -- a meta-called `(C->T;E)` does not commit, CURE names ';'/2 dispatch in lower_pl_stage2. NOT MINE and NOT TAKEN: `git diff --name-only origin/main` is three scripts, zero C/C++/.y/.l, and `git diff --stat origin/main -- src/ bootstrap/` is empty, so the compiler that failed was built from origin's sources. Routed to the ceo for the Prolog lane with the one-character interim cure and the structural row proposed; full receipts in FINDING-2026-09-08-cfo-make-test-dies-at-arm-2-....md.
- **CFO-9 (2026-09-08 17:26 and 17:34 CDT, ceo -> cfo) — TWO RULINGS RECEIVED, AND MY NEXT ROW CARRIES THE SWITCH.** (a) THE ICON DIALECT RULING (ceo 17:26, GOAL-ICON-100.md sec RULING, on my ASSIGNED rank-1 row icon-arizona-jcon-class-conversions-builtins-and-io-fidelity, now UNBLOCKED): the dialect comes from NOWHERE -- delete the `#SRC: JCON` switch and `g_jcon` (one global fewer) and SCRIP speaks Arizona Icon v9.5.25a everywhere; a jcon .std line stating a jcon-implementation fact (&features naming Java/UNIX/graphics/dynamic loading, &regions 0/0/0, progname) puts that program OUTSIDE THE BASELINE -- named in OUTSIDE_ARIZONA_BASELINE.tsv beside its package with the lines and the reason, OUT of the denominator, never masked per line (the SNOBOL4 OUTSIDE_SPITBOL_BASELINE precedent); &regions reports SCRIP's REAL sizes with Icon's default heap sizing adopted from iconx (0/500000/500000) so a default run reads truthfully, as -s/-m mirror SPITBOL, and never a size SCRIP does not have. Control arms: Icon master + both package suites + `nm -D` free of `g_jcon`, one landing. (b) THE LAW (ceo 17:34, fleet-wide, RULES sec Oracles, FACT RULE NO COMPATIBILITY SWITCH FOR ANY SCRIP LANGUAGE, CEO-391), Lon verbatim: *"Okay make it so then. So no compat switch for any of our SCRIP language. Right?"* -- one oracle and one feature set per language: SNOBOL4 SPITBOL (our x64 fork, `sbl -bf`); Icon Arizona icont/iconx; Prolog THE SUPERSET (ISO the core and default, every non-conflicting GNU/SWI builtin added, conflicts through ISO prolog flags set in the program or by a runner directive, NEVER a compiler mode -- this supersedes the 09-04 two-switch ruling); Pascal `fpc -Miso`; Raku rakudo/roast. No --compat, no dialect global, no source marker, no semantic env switch; what an oracle refuses or only another implementation provides is OUTSIDE THE BASELINE, named beside its package, out of the denominator, never hidden. ⭐ THE LAW ASKS EACH SEAT TO SAY IN ITS LEDGER WHETHER ITS ROW CARRIES A SWITCH: MINE DOES -- the icon row above IS the `#SRC: JCON` / `g_jcon` deletion, so (a) is the Icon instance of (b) and I take it as my next row. Nothing in the row I closed this sitting (score-row-write-rewrites-the-board-then-refuses) carries a switch of any kind.
- **CFO-10 (2026-09-08 17:4x CDT, cfo) — THE ARM-2 FALSE RED IS CURED AS RULED, AND ITS STRUCTURAL HALF IS MINTED (SCRIP `348eed586`).** ceo RULED 17:39 on my finding: *"a hermetic gate never reads live session state; land the one-character class now and mint the structural row under your ownership, since the finding is yours and it is the economy's: a seat paying 8 minutes to find a red at arm 57 is a cost line."* ARM 4 of test_gate_banner_leads_with_the_suite_line now counts all THREE verdict classes; proved on the real lines -- `⚠ NOTHING LANDED` was no-match before and matches after, while `✅` and `⛔` match under both, so no regression -- and `make test-postoffice` is green. Minted banner-gate-arm-4-reads-live-session-state-so-it-grades-the-seat-not-the-tool (rank 1, owner hq_T, FREE) for the structural half: the classes are still TYPED IN THE GATE, so the next class anyone adds reds arm 2 again, and the arm still grades the seat rather than the tool. ⛔ ITS DONE-WHEN READS RED ON THE COMMIT THAT CURED THE SYMPTOM, BY DESIGN, and I checked that it does -- my first draft of that criterion read GREEN because a python `\x` escape wrote the emoji as literal text and the grep could never match, which is a false-green criterion of exactly the class this seat filed a finding about an hour earlier; caught by running it before minting rather than after. ⛔ CORRECTION CARRIED INTO THE FINDING: I routed the hidden Prolog red as arm (b) on the strength of the gate's printed CURE line; the ceo re-measured it as arm (c) (a cut inside a meta-called goal answers [] where swipl says [fwd]) and has claimed it. The CURE line is STATIC PROSE printed beside a MEASURED failure count, so it keeps naming the original cure's subject after that arm goes green -- I read the summary and not the arm-by-arm lines, which is one command I did not pay for.
- **CFO-11 (2026-09-08 17:5x-18:0x CDT, cfo) — THE ICON DIALECT RULING IS IMPLEMENTED, MEASURED, AND HELD UNPUSHED ON ONE QUESTION THAT IS THE CEO'S (local `fb61b8201`, deliberately not on origin).** ⭐ THE MEASUREMENT THAT CHANGED THE JOB: `g_jcon` was WRITTEN ONCE AND NEVER READ ANYWHERE in the tree, so the `#SRC: JCON` switch has been DEAD CODE and the marker never changed behaviour; what actually happened is that `&features` and `&regions` hard-coded JCON's answers UNCONDITIONALLY for every Icon program. There was no dialect to delete, only jcon facts asserted as if they were Icon's. Done: g_jcon and the marker gone (`nm` and `nm -D` both free of it, a ruled control arm); `&features` drops Java and graphics and gains keyboard functions, VERIFIED TRUTHFUL BEFORE CLAIMING IT (SCRIP implements getch/getche/kbhit); `&regions` 0/500000/500000; `&progname` adopts iconx's invocation convention, the same principle the ruling used for regions, with the blast radius measured first (kwds.std is the only .std in either suite pinning a progname). MEASURED: kwds matches its .std BYTE-FOR-BYTE IN BOTH MODES; Arizona 70/90 both modes; Jcon 55/82 both modes; Icon master 704/705 both modes. ⛔ THE HOLD: 704 is against a 705/705 baseline I measured BY STASHING MY OWN CHANGES AND REBUILDING, so the regression is genuinely mine and not waved through. The single red, procedure_every_alt_replace_4, has a ref in corpus/tests/icon/ALL.ref that PINS SCRIP'S OWN PRE-RULING OUTPUT -- &features naming Java/dynamic loading/graphics, &regions 0/0/0, bare &progname -- i.e. exactly the jcon facts the ruling deletes. It is a SELF-PIN, not an oracle. Re-cutting a master ref so my own change reads green is fitting the oracle to the code, so it is NOT done: three options put to the ceo by mail (re-cut from icont/iconx; rule it OUTSIDE the baseline; or the ceo names another). ⛔ A CORRECTION I OWE MYSELF: I first argued the two master reds could not be mine because neither program referenced &features -- and I was WRONG. procedure_record_limit_replace_1 reaches the feature list through the PREPROCESSOR (`precheck(_JAVA, "Java")`), so my keyword grep found nothing while the coupling was real. The stash-and-rebuild baseline answered in one measurement what source-reading got backwards; that three minutes is the cheapest money this seat spent today.
- **CFO-12 (2026-09-08 18:0x CDT, cfo) — SECOND ROW MINTED FROM THE SAME SITTING: THE FEATURE LIST AND THE PREPROCESSOR SYMBOL TABLE ARE ONE FACT IN TWO FILES, ENFORCED BY NOTHING.** `feats[]` in `src/runtime/keywords.c` and `pre[]` in `src/parsers/icon/icon_lex.c` encode the same fact in two layers because Icon's preprocessor defines a symbol per feature, and Icon's own suite cross-checks them. Editing ONE side made SCRIP contradict itself out loud (`error: unexpected predefined symbol for Java`, `error: no predefined symbol for keyboard functions`) and the ONLY thing that caught it was one entry buried in an 858-entry board. Minted icon-feature-list-and-preprocessor-symbol-table-must-agree-and-nothing-enforces-it (rank 1, owner hq_B, FREE), DONE-WHEN reads RED today and carries a MUTATION arm so the gate cannot pass vacuously. Same invariant-enforced-by-nothing class as this morning's arm-2 red -- two in one day, from opposite ends of the tree.
- **CFO-13 (2026-09-08 18:0x-18:1x CDT, cfo) — recogn IS NOT A CHEAP CURE, AND I AM CORRECTING MY OWN ESTIMATE TO SAY SO.** My 09-07 NEXT called recogn *"a real backtracking defect and the cheapest honest next cure, 6 diff lines"*. Measured this sitting into a FOUR-CELL TRUTH TABLE, same procedure and same scanning expression throughout, SCRIP against Arizona icont: direct+return OK · variable+return OK · direct+suspend OK · **VARIABLE CALL OF A GENERATOR BROKEN**. THE RULE: a generator invoked through a procedure-valued variable does not inherit the enclosing scanning environment -- the match sees no subject, returns `&null`, and `&pos` never advances (direct `v="c" pos=2`, variable `v=&null pos=1`, oracle `v="c" pos=2` both ways). It is NOT the keywords (a variable-called NON-generator reads &subject/&pos correctly) and NOT variable dispatch as such (a variable-called generator that does no matching works inside a scan): it is the GENERATOR FRAME, i.e. row 676 by-name-dispatched-generator-regions-...-per-the-frame-law, rank 1, owner hq_U, CLAIMED by an HQ STOOD DOWN under MODE EXECUTIVE -- claimed and unworked. recogn rejects ALL EIGHT inputs including the trivial "c", not "nothing but the middle case" as I wrote. gener/recent/sorting are the three my own NEXT said to re-measure after recogn on a shared-cause theory, so up to FOUR of this row's eight remaining reds may sit behind 676 rather than being cheap wins. Asked the ceo to release 676 or to send me to fncs and io instead; neither taken yet, witness in the row's NEXT. ⛔ TWO PROCESS ERRORS OF MINE, both caught and both cheap: (1) I first measured recogn with NO STDIN and read "zero output" as a defect -- it reads `while line := read()`, so an empty stdin is a measurement artifact, and CLAUDE.md's own rule (`< /dev/null` on compile steps ONLY, never on a run fed by a file) is exactly the rule I broke; (2) `s4e_msg.sh send` takes its body as a DOUBLE-QUOTED SHELL WORD, so a backtick pair is COMMAND SUBSTITUTION -- a backticked identifier in my mail to the ceo ran a nonexistent command and DELIVERED THE SENTENCE WITH THE WORD SILENTLY REMOVED. Corrected by a follow-up mail and written into this root's CLAUDE.md mechanics, because a hole in a delivered sentence is invisible to the reader.
- **CFO-14 (2026-09-08 18:2x CDT, LON IN CHAT, VERBATIM) — *"Do not depend on a comment #SRC: JCON for anything."*** Recorded the same session per the digest rule. STATUS IN CODE: ALREADY SATISFIED AND MEASURED, not asserted -- `grep -rn 'SRC: *JCON\|SRC:JCON' SCRIP/src SCRIP/scripts` returns ZERO after this sitting's deletion (the marker parse and `g_jcon` were the only dependence and they are gone in `04db4ef68`); the surviving occurrences are inert TEXT inside corpus test sources and PROSE in the goal files recording the retirement, neither of which is a dependence. ⛔ BUT IT CORRECTS MY OWN REASONING, WHICH IS THE POINT OF THE INSTRUCTION: in my 18:2x mail to the ceo I classified `procedure_every_alt_replace_4` as jcon source BECAUSE IT CARRIES THE MARKER. That is depending on the comment -- for classification rather than for behaviour, but depending on it. THE HONEST BASIS IS THE ORACLE, AND I ALREADY HAD IT: Arizona icont REFUSES to compile that entry ('Line 76 # }: invalid case clause'), while the Arizona kwds.icn compiles clean through the same icont seconds earlier, so the oracle is alive and it is THAT ENTRY it refuses. Under CEO-391's own clause -- what an oracle refuses is OUTSIDE THE BASELINE -- the refusal is the whole argument and the marker adds nothing to it. Correction sent to the ceo the same minute. ⭐ THE GENERAL FORM, worth keeping: a comment cannot be evidence. It is not checked by anything, it travels with a copy-paste, and here it was upstream Arizona content sitting in 36 files that had nothing to do with our dialect. Where I want to know what a program IS, the answer comes from running it through the oracle, not from reading a line somebody typed above it.
- **CFO-15 (2026-09-08 18:1x-18:3x CDT, cfo) — THE AGREEMENT GATE IS BUILT AND WIRED, THE LAST SCORE-ROW MEMBER IS CURED, AND TWO COMMITS ARE HELD (local `84c62c5ee` on `04db4ef68`).** (1) THE GATE the ceo approved as mine: `test_gate_icon_features_and_preprocessor_symbols_agree.sh` reads `feats[]` out of the runtime and `pre[]` out of the parser and refuses a symbol that names no feature. ⭐ THE DIRECTION IS ASYMMETRIC AND I MEASURED IT RATHER THAN ASSUMING A BIJECTION: Arizona icont reports "environment variables" in &features and defines NO `_ENVIRONMENT_VARIABLES` (live `$ifdef` probe against /home/resources/icon-master, beside `_KEYBOARD_FUNCTIONS` and `_PIPES` which it DOES define), so a feature without a symbol is NORMAL and only a symbol without a feature is wrong. A gate broader than its rule gets disabled by the first person it blocks for a good reason. WIRED into `preflight_arms.txt` -- 33 arms, 2.18 s against a 40 s ceiling -- because a gate in no runner is not measuring, which is this morning's finding applied to my own work. Its row's DONE-WHEN MUTATES a scratch copy through `ICON_PRE_SRC` and reads GREEN only because the gate actually fails on the mutation. (2) THE COO ROUTED ME THE LAST MEMBER of my closed score-row class: `test_snobol4_spitbol_testpgms_suite.sh` was refusing for a reason ONE STEP FURTHER ALONG than arizona/snoflake/ipl -- not guess-the-fraction but no-suite-row-named, because it reaches the writer through `gate_score_row`, a wrapper that could not say `--suite-pass`/`--suite-total` AT ALL. Passed through and wired. ⭐ THE WRAPPER'S OWN COMMENT BLOCK ALREADY RECORDS THIS EXACT LESSON one argument earlier (it could not say `--suite` either, cured by someone else): *a convenience wrapper that cannot express a distinction the underlying tool has silently narrows what the caller can say.* Twice in one file is a pattern, not an accident. HONEST SCOPE: with m3 and m4 agreeing at 1/2 the fraction would resolve today anyway -- naming it is what keeps it resolving when the modes diverge; the BLOCKING defect was the missing `--suite`. ⛔ BOTH COMMITS HELD, and the hold is now ONLY the master entry: the ceo ruled (a) re-cut and PUSH, but the re-cut is impossible -- Arizona icont REFUSES that entry ('Line 76 # }: invalid case clause') while the Arizona kwds.icn compiles clean through the same icont seconds earlier, so the oracle is alive and there is no answer to cut. Reported with a recommendation and awaiting the ruling.
- **THE LEDGER (append one line per reading, `date`-read):** credits (Lon's word, verbatim, with the time) · load (1/5/15) · runaways (count) · disk / % · flips since the last reading (from the coo's measure) · cost per flip.
- **LEDGER 2026-09-07 08:51 CDT (cfo):** credits: no reading from Lon this sitting (none since the seat opened) · load 11.24/10.32/9.61 on 16 cores (a coo IPL compile burst and the cfo's own first `make` on top of the seven runaways) · runaways 7 (unchanged from CFO-1; no grant recorded, nothing killed) · disk 90G/125G 76% (+2G since CFO-1) · flips since CFO-1 08:48: master 0, package 2 (`util_progress_flips.py --since 2h --per hour`, m3, rows 10505 in window) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:20 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 10.51/10.71/10.78 on 16 cores (the cto compiling jtran on top of the seven runaways) · runaways 7 (unchanged; no grant recorded, nothing killed; the two hq_T `scrip --run` are 15 h old, the five `t.bin` 44–59 h) · disk 88G/125G 74% (−2G since 08:51: my scratch make logs and the coo's tmp dirs came and went) · flips in the hour 08:20–09:20 CDT (`util_progress_flips.py --since 1h --per hour`, m3, rows 11805): master 0, package 17 (13 in the 13Z bucket, 4 in the 14Z bucket; the coo's Gimpel 104/126 and IPL 75/89 landings) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:39 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 9.95/11.52/11.72 on 16 cores (the 5-min and 15-min carry the cfo's eight parallel port-trace arms of 09:2x) · runaways 7 (unchanged; a fresh `t.bin` under `/tmp/csh_run_6nevwlxv` seconds old is somebody's live harness run, not a runaway) · disk 90G/125G 76% · flips in the 30 min since 09:07 (`util_progress_flips.py --since 30m`, m3, rows 9651): master 4, package 13 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 09:54 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 7.80/9.33/10.63 on 16 cores (the quietest reading yet: only the seven runaways and the desktop) · runaways 7 (unchanged, 15.5 h and 44–60 h old; no grant recorded) · disk 91G/125G 76% · flips in the 20 min since 09:34 (`util_progress_flips.py --since 20m`, m3, rows 5922): master 5, package 8 (the cfo's three jcon m4 among them) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 10:13 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 11.93/9.90/9.85 on 16 cores (the ceo compiling an Icon package and a `toby_bin` jcon witness on top of the seven runaways) · runaways 7 (unchanged, 15.8 h and 44.5–60 h old; no grant recorded) · disk 93G/125G 78% (+2G since 09:54: the four regenerated .s artifact sets and the corpus clone the drift check makes under /tmp) · flips in the 15 min since 09:58 (`util_progress_flips.py --since 15m`, m3, rows 5469): master 1, package 0 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 10:33 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 9.24/9.94/10.04 on 16 cores (the ceo's SNOBOL4 master harness linking t.bin on top of the seven runaways) · runaways 7 (unchanged: the two hq_T `scrip --run` 16.1 h old, the five `t.bin` 44.8–60.6 h; no grant recorded, nothing killed) · disk 91G/125G 77% · flips in the 15 min since 10:14 (`util_progress_flips.py --since 15m`, m3, rows 6524): master 1, package 2 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-07 12:41 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 7.69/8.17/7.94 on 16 cores (the quietest yet; my five landing boards had just finished) · runaways 7 (unchanged, 18.5 h and 47–63 h old; no grant recorded, nothing killed) · disk 91G/125G 77% · flips in the 35 min since 12:06 (`util_progress_flips.py --since 35m`, m3, rows 5282): master 1, package 0 (my five Arizona flips are on a dirty tree and count only once landed) · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 17:14 CDT (cfo, first sitting after the box came back):** credits: still no reading from Lon (none since the seat opened 2026-09-07) · ⭐ THE BOX REBOOTED 2026-09-08 16:58 CDT (`uptime -s`, `who -b`), and that single event settled the whole runaway census this seat has carried open since CFO-1: load **1.36/0.79/0.44** on 16 cores at 17:14 (against 7.69-11.93 for every reading of 2026-09-07) · runaways **0** -- all seven (five `t.bin` 44-63 h old, two hq_T `scrip --run` 16 h old) are gone, KILLED BY THE REBOOT AND NOT BY ME: no grant was ever recorded and nothing was ever killed under one, so the standing-grant ask stays OPEN for the next batch rather than being answered by this · the one `/tmp/csh_run_mxk75vie` + `t.bin` alive at 17:14 is the ceo's LIVE harness run, aged **0 s** (`ps -eo etimes`, cwd `/home/claude_ceo/SCRIP`) -- named here so it is never miscounted as the eighth · disk **74G/125G 62%**, down 17G from 91G/77% at 12:41 CDT 2026-09-07: that 17G WAS the orphaned `/tmp/csh_run_*` trees, which is the first hard price tag this seat can put on the runaway class · flips in the 24 h to 17:17 (`.github/scripts/util_progress_flips.py --since 24h --per hour`, m3, 43 rows in window): master 0, package 0, benchmark 0 · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 17:38 CDT (cfo, on Lon's "next"):** credits: still no reading from Lon · load 2.06/2.23/2.04 on 16 cores (the box has stayed quiet 40 min after the reboot; contrast 7.69-11.93 for every reading of 09-07) · runaways 0 -- ⚠ ONE WATCH ITEM, NOT A RUNAWAY: PID 707636 `/home/claude_cto/SCRIP/scrip --run arbno_fence_span_replace_branch_2.sno` at 100% CPU, aged **1 s**, so it is the cto's live fuzz row; NAMED HERE BECAUSE IT IS THE SAME SHAPE AS THE TWO 16-HOUR RUNAWAYS OF CFO-1 (`arbno_pos_rpos_branch_81.sno`, hq_T's fuzz row) -- the arbno fuzz class is the one that has produced hangs, so a `scrip --run arbno_*` still alive at the next reading is a runaway and not a slow test · disk 75G/125G 63% (+1G in 24 min, ordinary board churn) · flips in the hour to 17:38 (`--since 1h`, m3, 8682 rows in window): master 0, package 3, benchmark 0 -- the fleet is landing again after the reboot · cost per flip: not computable without a credit reading.
- **LEDGER 2026-09-08 18:06 CDT (cfo, on Lon's "continue"):** credits: still no reading from Lon · load **0.69/1.25/1.70** on 16 cores, the quietest reading this seat has ever recorded · ⭐ THE ARBNO WATCH ITEM FROM 17:38 IS CLEARED: no `scrip --run` and no `t.bin` alive at all, so the cto's `arbno_fence_span_replace_branch_2.sno` completed normally and my call that it was live work rather than the eighth runaway was right -- naming it with its age is what made that checkable instead of a guess · runaways 0 · disk 75G/125G 63% (flat since 17:38) · flips: not re-measured this tick · cost per flip: not computable without a credit reading. HOUSEKEEPING: the held icon commit `fb61b8201` is parked on branch `held/icon-dialect-awaiting-ceo-ruling` and main is back on origin, so the hold cannot block an unrelated landing and cannot be lost.
- **BOARD COST TABLE (append as measured):** runner · wall seconds · load during · tree.
- `board_icon_master.sh` · 44.8 s / 43.6 s · load ~2 · 858 entries, 705 run-graded both modes -- and the ORIGIN-BASELINE version of it (stash + full rebuild + board) cost ~3 min total, which is the price of answering "is this regression mine" with a measurement instead of an argument. It was worth it: the argument was wrong.
- `test_icon_arizona_suite.sh` · 54.2 s then 44.1 s · `test_icon_jcon_suite.sh` · 73.4 s then 71.7 s · load ~2, three boards in parallel on the post-reboot box
- ⭐ REBUILD ASYMMETRY, worth knowing before an edit: touching `src/runtime/keywords.h` (a runtime header) costs a **125.7 s** rebuild; touching `src/lower/lower_icon.c` (one .c) costs **1.0 s**. Same landing, same seat, 125x apart -- so batch header edits and never discover one late.
- `make test-postoffice` · 45.0 s · load ~2 · SCRIP `348eed586` -- ⚠ ITS OWN RECIPE DOCUMENTS "~13s MEASURED" and the target's stated rule is "keep this target seconds-cheap": it has grown 3.5x past its written budget with no arm noticing, because nothing pins it the way preflight_arms_stay_cheap pins preflight. A budget that lives only in a comment is not a budget.
- `make test` STOPPED AT THE ARM-2 FALSE RED · 49.4 s · load 1.36 · SCRIP `b7f48462a-DIRTY` -- it certifies arms 1-2 ONLY and reads as the whole blocking set; the true price of a pre-landing run for a seat that has not committed
- `make test` RUN TO ITS FIRST REAL RED (post-commit) · 477.1 s · load ~2 · SCRIP `6a61e40d9` · 56 gates green then test_gate_pl_meta_call_reaches_control_constructs 2-of-23 -- so a full blocking set is ~8 min on a quiet box, ten times the false-red run it hides behind
- incremental `make` after a pull that touched `src/driver` + runtime (19162985f) · 3.3 s · load 2.4 · the whole cost of not running a board on a stale binary
- `test_snoflake_suite.sh` (180 programs x 2 modes, live `sbl -bf` oracle, writes its own SCORE.md cell and SUITES.tsv row) · 36.7 s · load 2.4 · SCRIP `b2660262e`
- `test_gate_banner_leads_with_the_suite_line.sh` · ~1.5 s · `test_gate_score_cell_no_silent_prose_loss.sh` (9 arms) · ~3 s · `util_score_row.py selftest` · ~2 s -- the three arms that carry the score-row row's DONE-WHEN, together cheaper than one board
- `test_pascal_pat_suite.sh` · 69.6 s · load 10.9 (1-min) · SCRIP `ac9fe5e9f` before the 10 s probe; 60 s of it was the fpc oracle spinning on iso7185pat.pas, 9.6 s the 427 rejection tests × 2 modes
- `test_pascal_pat_suite.sh` · 23.6 s · load 13.0 · SCRIP `9a7318868` (the 10 s probe: two declared-UNGRADABLE acceptance programs, one 10 s spin + one 0.1 s rejection)
- `fpc -Miso` on iso7185pat.pas alone · never finishes (killed at 300 s and 240 s) · load 10 · fpc 3.2.2; a 5-minute wait per attempt is the cost of "raise the bound"
- `test_gate_package_runners_print_the_inventory.sh` · 4.5 s · load ~12 · SCRIP `ac9fe5e9f`
- first `make` of a fresh cfo checkout · 195 s · load ~10 · SCRIP `ac9fe5e9f`
- port-trace arms, eight in parallel at load ~11, SCRIP `9a7318868` (old body) → `1f87a7aa9` (cured body), wall seconds before → after: sno 47.7 → 43.6 · sc 14.5 → 16.7 · pas 18.1 → 19.8 · reb 22.6 → 24.1 · pattern 36.9 → 38.3 · icn 33.1 → 34.0 · raku 7.6 (died at rung 12) → 29.4 (68 witnesses) · pl 62.1 (died at rung 10) → 418.3 (293 witnesses)
- `test_icon_jcon_suite.sh` · 85.2 s · load 12.7 · SCRIP `1f87a7aa9` + the wiring uncommitted (91 shipped, 82 graded × 2 modes, 20 s timeout) · 76.1 s · load 9.0 · SCRIP `1e8b5e951`; the m3 half alone 35.7 s
- `test_corpus_snobol4.sh` (SNOBOL4 master, 1858 × 2 modes + 28 ast) · 287 s · load ~9 · SCRIP `1e8b5e951` + the STNO cure uncommitted
- `board_icon_master.sh` (Icon master, 858 entries) · 67 s · load ~9 · same tree
- `test_gate_pattern_shared_box_witnesses.sh` · 36 s · load ~9
- `util_verify_s_artifacts_owed.sh` (dry-run drift check over four sets) · ~60 s; the four regens themselves 1.1 + 4.0 + 5.8 + 3.6 s
- `s4e_msg.sh next` when it serves the package-inventory row · 120 s of probe (its budget) PLUS an orphaned package board that ran ~6 min more (measured 09:39 → 09:47 CDT via 178 ipl progress rows)
- `test_snobol4_gimpel_suite.sh` (144 drivers × 2 modes via scorecard_snobol4.sh, live oracle) · 54.6 s · load 11.1 · corpus with the three aliases · 56.3 s · load ~11 · with one alias
- `util_build_package_suite.py` on aisnobol (9 programs, oracle refs cut) · 1.2 s · `test_snobol4_aisnobol_suite.sh` · 8.6 s
- `s4e_msg.sh next` with `S4E_DISPATCH_PROBE_TIMEOUT=30` · ≤30 s of probe per call instead of 120 s (the env knob the dispatcher already honours; the orphan it leaves is the same)
- `board_icon_master.sh` · 52 s · load 8.8 · SCRIP 2594133f5 + the conversions cures uncommitted
- `test_icon_arizona_suite.sh` · 63.6 s · load ~9 · same tree · `test_icon_jcon_suite.sh` · 77.4 s · `test_corpus_snobol4.sh` · 270 s · `test_pascal_pat_suite.sh` · 19.2 s (all five in parallel at load ~9 → 8)

## THE CFO LOOP
1. `check` the inbox; route economy asks to Lon in chat, grants into this file the same turn.
2. The ledger line (above), every sitting and on every Lon "next".
3. The runaway census; kill only under a recorded grant.
4. `next` — the EASIEST FREE rank-0 row; cure; measure through the suite's own runner; flip to `coo`; repeat.
5. A landing = pushed + the runner's row rewritten + the ledger line; the banner verdict is computed, never typed.
