# FINDING 2026-08-27 seat15 — `probe-consolidate-conformance`'s already-landed partial conversion has SILENTLY shrunk `probes_misc` by 92 witnesses, uncorrected; `probe-consolidate-fuzz` would compound the same loss

**Date:** 2026-08-27 · **Seat:** seat15 (FLEET-16, lane hq_P) · **Row:** `probe-consolidate-fuzz` (re-claimed via `s4e_msg.sh next` after seat05 released it) · Zero source/corpus edits.

## Update on seat05's finding — one half resolved, one half confirmed still open and now proven live

seat05's `FINDING-2026-08-27-seat05-probe-fanout-scorecard-probes-misc-generic-glob-is-the-real-mechanism.md` said no working grading path existed for a probe/-sourced suite file at all, and listed three ways to unblock the fan-out. Re-checked both mechanisms fresh this session:

1. **RESOLVED since seat05's finding:** `test_corpus_snobol4.sh` now has a dedicated `probe/<family>` discovery+grading loop (SCRIP `88334662`, "corpus_suite_harness.py: add capture-oracle-refs; register tests/snobol4/probe"), separate from the `crosscheck/`-scoped loop seat05 correctly found too narrow. Current script, `scripts/test_corpus_snobol4.sh:178-191`, own comment: *"First tenant: tests/snobol4/probe/conformance."* This is seat05's option (b)/(c) — a probe/-specific grading path now exists and needs no per-family registration (filesystem-is-truth, same pattern as the crosscheck loop).
2. **STILL OPEN, confirmed unchanged:** `scripts/scorecard_snobol4.sh:53` — `probes_misc 5 probe -name *.sno -not -path */bb/* SELFDIR 20 ms` — byte-identical to what seat05 quoted. `git log -- scripts/scorecard_snobol4.sh` shows its only touches since are `0141ecc5` (comments-only, pre-dates seat05's finding) and two unrelated rows (`775eab7d` naming a report string, `22af6afb` table refactor) — nothing touched the `probes_misc` glob or gave it suite-format awareness. It still runs every `.sno` under `probe/` as one whole standalone program; a converted suite file is a container of independently-complete programs and is invisible to this glob once the loose files are gone (per the harness's own top-of-file warning, seat05 already quoted it).

## The new part: this already happened, silently, today

`probe-consolidate-conformance` (sibling row, still `unassigned FREE` in QUEUE.tsv — not closed) already landed a partial conversion: 92 of 147 witnesses moved into `corpus/tests/snobol4/probe/conformance.{sno,ref}`, 55 left loose with their own `KEEP.md` (confirmed: `find corpus/probe/conformance -name '*.sno' | wc -l` → 55 today).

`corpus/tests/snobol4/probe/conformance.sno` lives outside `probe/` entirely — `probes_misc`'s glob root is `probe`, so those 92 witnesses are gone from its count with nothing added back. **This is exactly the shrunken-denominator failure class RULES.md already names** (the same shape as `corpus-suite-family-list-should-autodiscover`'s 98-entry incident) — except here it's not a bug in a counting mechanism, it's the *expected* side effect of a conversion nobody paired with a `probes_misc` fix. Weight-5 category, official META SCORE, down 92 witnesses, since the conformance conversion landed, unnoticed as far as I can find (no FINDING postdating seat05's addresses this; `probes_misc` line is untouched).

## Why `bb`'s exclusion pattern doesn't transfer to `fuzz` or `conformance`

`probe/bb` is excluded from `probes_misc` (`-not -path */bb/*`) but stays scored via its own dedicated entry, `bb_probes 10 probe/bb -name *.sno SELFDIR 20 -` (line 49) — **individual files still exist under `probe/bb/` and are still globbed, just by a different row.** `fuzz` and `conformance` are different in kind: consolidation *eliminates* the individual files (that's the whole point — `git rm` after suite conversion, DONE-WHEN requires zero loose `.sno` files remaining). There is no "dedicated row pointed at the surviving per-file directory" option here, because nothing survives per-file. Mirroring `bb`'s exclusion for `fuzz` would not prevent a loss the way it does for `bb` — it would just formalize the same silent loss already sitting unfixed for `conformance`, times two.

## Disposition for `probe-consolidate-fuzz`

**Not converting.** Converting now would remove 59 more witnesses from `probes_misc` the same uncorrected way conformance already removed 92 — compounding a live, real problem rather than a hypothetical one. This is not a new architecture call for me to make solo (seat05 was right to escalate it, and now there's a concrete instance of the cost, not just a risk). Two sibling rows (`probe-consolidate-bb`, `probe-consolidate-passthru`) are already `CLOSED` in QUEUE.tsv, owner `ceo`, for their own "not simple conversion material" reasons — worth ceo/hq_P considering the same disposition for `fuzz` (and probably `conformance`, and whatever remains of the ~14-sibling fan-out) rather than leaving it `FREE` to be re-picked and re-investigated by a third seat with no new information. Sent as `ask` to hq_P. Parking again (state PARKED, not FREE) so the picker doesn't hand this to the next seat without a ruling attached.

**Recommended next step, not mine to execute:** either (a) give `probes_misc` suite-format awareness before any more `probe-consolidate-*` rows land, or (b) accept and explicitly document that `probes_misc`'s denominator legitimately shrinks under this fan-out and retroactively account for `conformance`'s already-landed 92, or (c) close the remaining `probe-consolidate-*` rows the way `bb`/`passthru` were closed. Any of the three is a real ruling; leaving the row `FREE` with no ruling just spends another seat's session re-deriving what this and seat05's finding already established.
