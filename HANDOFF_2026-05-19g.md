# HANDOFF — 2026-05-19g (Sonnet 4.6)

**Goal:** GOAL-HEADQUARTERS (HQ maintenance session)
**Repos:** one4all + .github

---

## What was done this session

### 0. Session baseline established
Full quick test matrix run across all 6 frontends × 5 backends at session start:

| Frontend | Result |
|----------|--------|
| SNOBOL4 --interp | 7/0 ✅ |
| Icon --interp | 194/265 (36 fail, 35 xfail) ✅ |
| Prolog | 5/0 ✅ |
| Snocone | 2/3 (3 pre-existing fails) |
| Rebus | 4/0 ✅ |
| Raku | 5/0 ✅ |

| Backend | Result |
|---------|--------|
| x86/interp | 7/0 ✅ |
| JVM | 7/6 (pre-existing) |
| .NET | 0/9 (ilasm not installed in container) |
| JS | 4/2 (pre-existing) |
| WASM | SKIP (wat2wasm not installed) |
| broker | 23/26 ✅ |

### 1. DAI-8 C17 — `snobol4_stmt_rt.c` deleted
- **Method 1 + Method 6 audit:** 43 of 44 functions confirmed zero external callers; `stmt_init` GC'd by linker because `scrip.c` and `interp_globals.c` provide static-shadowing stubs.
- **Action:** Deleted entire file (447 LOC).
- **Commit:** `d48681fb` → pushed `0635b537`
- All gates floor held.

### 2. DAI-8 C18 audit — no safe deletions
- `static_get`/`static_set` (`icn_runtime.c`): called from live `sm_call_proc` — Method 7 chain anchored.
- `rt_set_last_ok` (`rt.c`): called from `rt_match_blob` (PLT-live in emit_sm.c) — Method 7 chain anchored.
- `emit_core.c` 98 symbols: all have real call sites in `emit_bb.c`/`emit_sm.c`; GC-sections false positive caused by `emit_bb.c` compile failure in audit build.
- **Conclusion:** DAI-8 sweep is complete. No further easy dead-code clusters exist.

### 3. EC-2 — `emit_bb_node()` created, 5/18 BB kinds migrated

New function `emit_bb_node(IR_t *nd, FILE *out)` added to `src/emitter/emit_core.c`.
Declaration added to `src/emitter/emit_core.h`.

Migrated BB kinds (JVM + JS + .NET arms for each):

| Kind | Commit |
|------|--------|
| `IR_PAT_LIT` | `12e4ab86` |
| `IR_PAT_ANY` | `8b8af8cc` (pushed `c48cf0c6`) |
| `IR_PAT_NOTANY` | same |
| `IR_PAT_SPAN` | same |
| `IR_PAT_BREAK` | same |

ec_* helper functions added to `emit_core.c` (to be removed from silos in EC-5):
`ec_js_escape`, `ec_jvm_class_hdr`, `ec_jvm_init_ms_str`, `ec_net_escape_ldstr`,
`ec_net_class_hdr`, `ec_net_charset_class`, `ec_net_alpha_hdr`, `ec_net_beta_hdr`,
`ec_net_fail_ret`, `ec_net_cursor_load`, `ec_net_ms_length`, `ec_net_spec_of`.

All gates floor held after every commit.

---

## Watermark

```
one4all: c48cf0c6   (EC-2: 5/18 BB kinds in emit_bb_node)
corpus:  92e103f    (unchanged)
.github: 35ed4286
--interp:    194/265
smoke ×6:    5/0 5/0 2/3 4/0 5/0 7/0
broker:      23/26
jvm:         7/6
js:          4/2
net:         0/9  (ilasm absent)
wasm:        SKIP  (wat2wasm absent)
DAI-BOMB:    0
```

---

## Next steps

**EC-2 continues** — 13 BB kinds remaining in order:

1. `IR_PAT_ARB`
2. `IR_PAT_ARBNO`
3. `IR_PAT_CAT`
4. `IR_PAT_ALT`
5. `IR_PAT_LEN`
6. `IR_PAT_POS` / `IR_PAT_POS` (rpos variant via `nd->ival2`)
7. `IR_PAT_TAB` / rtab variant
8. `IR_PAT_REM`
9. `IR_PAT_FENCE`
10. `IR_PAT_ABORT`
11. `IR_PAT_ASSIGN_IMM`
12. `IR_PAT_ASSIGN_COND`

After EC-2 complete → EC-3 (SM instruction arms), EC-4 (prologue/epilogue), EC-5 (delete silos).

**Session start protocol:** clone `.github`, read PLAN.md + RULES.md, open `GOAL-HEADQUARTERS.md`, run `bash scripts/build_scrip.sh` from `one4all/`, run gate suite, continue EC-2 at `IR_PAT_ARB`.

---

## Files changed

- `src/emitter/emit_core.c` — +377 LOC (emit_bb_node + ec_* helpers + 5 BB kinds × 3 backends)
- `src/emitter/emit_core.h` — +3 lines (struct IR_t fwd decl + emit_bb_node decl)
- `src/runtime/snobol4/snobol4_stmt_rt.c` — DELETED (−447 LOC)
