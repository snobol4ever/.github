# FINDING 2026-08-27 (ceo): PAS-DISPLAY (r13/r14/r15 lexical display) was RULED by Lon 2026-06-27, LANDED the same day, then silently regressed — and a later audit mislabeled the regression "DEAD ARCHITECTURE"

**The ruling (recovered from git history, GOAL-PASCAL-BB.md at `bc1e825a4`):** "PAS-DISPLAY: lexical display in r13/r14/r15 — O(1) enclosing-scope access (**Lon directive, 2026-06-27 — TOP PRIORITY FOR PASCAL**)." Mapping FROZEN: rbx=L0 (globals via GVA) · r13=display[1] · r14=display[2] · r15=display[3] · current frame register · L≥4 keeps the chain-walk as fallback. Entry: `push display[L]; mov display[L],<frame>`; exit: `pop display[L]` — O(1) per call, O(1) per access, and the save/restore makes it RECURSION-CORRECT by construction.

**It LANDED, all five rungs, same day (2026-06-27, M3+M4):** DISPLAY-0 characterize (nest3.s walk quantified) · DISPLAY-1 save/set/restore in xa_flat prologue/epilogue, **gated purely on the lexical-level value — no language enum** (SNOBOL/Icon/Prolog procs have decl_level=0, so the emit-no-lang law was satisfied by construction) · DISPLAY-2 reads · DISPLAY-3 writes+byref · DISPLAY-4 deep-nest fallback with `deep5.pas` witness (levels 1-3 via registers, zero walk; L4 single-hop).

**Then it vanished without a ruling.** A later audit session measured zero r13/r14/r15 refs in all 141 `.s` (deep5's reads emitting `[rbx+16]` arena access) and wrote "**FINDING 1 — PAS-DISPLAY is DEAD ARCHITECTURE**" — reading the ABSENCE as the design rather than as a REGRESSION of a standing Lon directive. The replacement that grew in its place is today's `__up_*` arena cells: one cell per (proc,var), shared across recursive activations, zero save/restore (this date's Pascal scoping FINDING) — i.e., the regression's substitute is also semantically broken under recursion, which the display design had solved by construction. EXPIRY plus a wrong verdict on top: the same class as the banner-as-liveness and done-token conflations — an instrument (a grep of current .s) answering "what is" and being read as "what was decided."

**The verified-free argument was in the original and answers this date's CEO objection:** r13/r14/r15 are Σ/δ/Δ ONLY in graphs that emit pattern boxes; a Pascal program emits ZERO, verified by grep then, still true now. Registers are per-graph resources. ⛔ **CEO retraction (this date, in the open):** my report to Lon recommended a standing-memory display over the register display partly on a cross-language collision argument — that argument was wrong in principle (per-graph planes) AND already answered by the 2026-06-27 record. Lon's ruled design stands; the standing-display remains only as the L≥4 overflow fallback the ruling itself specified.

**Revival note:** the frozen map's "current frame" register predates the s73 REG-MAP six-register world — a revival keeps r13/r14/r15 exactly as frozen and maps current-frame to RBP (today's activation base). Row `pas-display-revival` minted; it also subsumes the uplevel2/3 recursion bug (`pascal-uplevel-nested-proc-hang`) since display save/restore is the cure class.
