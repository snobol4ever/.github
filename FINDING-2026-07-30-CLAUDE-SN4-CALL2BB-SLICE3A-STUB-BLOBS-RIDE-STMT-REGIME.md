# FINDING 2026-07-30 — SN4 CALL2BB slice 3a: DEFINE stub blobs ride the statement regime (the whole-graph carve dies on armed stubs)

Session: s21x-h (Claude).  Scope: SCRIP src/emitter/emit.cpp ONLY (9 insertions, 4 deletions — no template, planner, or runtime edits).  SCRIP `126645fc`.  Directive of record: Lon s21x-c restated verbatim this session — "get rid of proc_LBL__ROMAN_α already. What is the hold up?  I should see a BB for SAVE_RESTORE and a type of BB_CALL. There is NO FRAME RELATIVE addressing anymore for operands."

## What landed

Under `SCRIP_STMT_FRAME=1 && SCRIP_CALL2BB=1`, a jmp-entry DEFINE stub blob whose reachable body passes the ONE statement-frame classifier now rides the regime: `flat_frame_bytes` collapses to **48** (32B wire header + slot0 — xa_flat's `>=48` invariant is the floor), the s176 main-region floor, the whole-graph carve, and the zero-fill all die on that graph, and the body statements self-allocate per-BB exactly like an armed main.  Two edits, both at existing choke points: (1) the classifier's exempt set grows a GATED call family — `IR_CALL`, `IR_CALL_PROC_STAGED`, `IR_SAVE_RESTORE`, `IR_GOTO_DEFERRED` — all four self-allocating or control-sink by construction (sr0 carves/releases its own block per slice 2; the staged call keeps rsp balanced on every arm and exits ride the record protocol; floaters restore machine state wholesale; a deferred goto jmps away and every landing protocol it can reach normalizes rsp/rbp); (2) the emit_chain choke's regime license gains a jmp-entry arm `_stfj`, and on admit overrides kt/seed/layout/cap to the 48-byte header shape.  Zero new encoders (`x86_lea_id`/`RDQ`/`x86_jmp_reg`/`rt_flat_wire_adopt` would have sufficed for a caller-side glue and were not even needed — the caller side is untouched), zero new runtime leaves, zero template edits: the floaters were already geometry-agnostic (they restore whatever `{γw, ωw, rsp, rbp}` the opener installed) and `FRQ`/`FRQB` already carry the fc window arm, so the whole slice is a licensing change.

## The armed shape (witness `DOUBLE(X) = X + X`, m4)

`proc_DOUBLE_α: sub rsp, 48; mov [rsp+24], rcx; mov [rsp+32], rdx; slot0 zeroed` — no rbp save/seed (pin off: !deep && !pat && !gen, so the wire-adopt takes its depth-static arm with caller rbp live in the register), then per-statement `push rbp; mov rbp, rsp` brackets, per-BB `sub rsp, 16` cell carves with `[rsp+0]/[rsp+8]` stores, BINOP consuming the top two cells, and the RETURN floater's `rt_flat_ret_snap` → restore rcx/rbp/rsp from the record → `jmp rcx`.  This is the s21x-c design executing end-to-end on a DEFINE'd function.

## LAW — the DEFINE-stub discriminator is the driver's floor verdict, not a node shape (two falsifications deep)

1. **Admit-all-jmp-entry is WRONG**: the first cut admitted every non-pat/gen/deep jmp-entry citizen and gate-on regressed `expr_eval`, `161_pat_defer_fn_nested_match`, `1016_eval`, `1019_eval_string` — runtime EVAL/CODE chains are jmp-entry citizens too, and their enter/exit protocol (rt_chain_enter, own wire discipline) is NOT the pcall record's.  2. **Entry-node role-3 keying is WRONG**: scoping the admit to `entry->op == IR_SAVE_RESTORE && ival == 3` (the wire-adopt head, the driver's own s176 floor predicate) fixed m3 but silently missed the whole m4 driver path, whose emit_chain entry parameter is not the wire-adopt node — measured as the witness blob emitting the floored 400 in `--compile` while `--run` armed.  3. **CORRECT: `g_flat_frame_floor > 0`** — all four driver proc-emission loops (scrip.c 882/1340/1528/1633) compute the role-3 predicate on the graph-table entry and set the floor exactly and only around DEFINE-stub emissions; it is still live at the emit_chain choke (emit_jmp_entry_clear runs after) and every other jmp-entry citizen — PAT$ patprocs, EVAL/CODE chains, rt pattern builds — leaves it 0.  The discriminator is therefore the driver's own verdict, computed once, mode-uniform.  Conservative edge: an empty-main program floors 0 and declines.  The choke debug line now prints `deep/floor/c2/stfj` so the next reader measures instead of guessing.

## The two-family surprise (roman is HALF-dead, honestly)

A DEFINE-bearing program emits TWO proc families: the dyn-scope-table proc the CALL actually enters (`proc_DOUBLE`, `proc_ROMAN`) and the LBL__ label pseudo-proc for goto/CODE transfers (`proc_LBL__DOUBLE`, `proc_LBL__ROMAN`).  On roman under the gates: **`proc_ROMAN_α` is the old TINY-stub shape (role-3 wire-adopt → deferred transfer, both exempt) — it ARMS and its floored carve (1568 under the regime's elide-off zls) collapses to `sub rsp, 48`**; the transfer then chain-enters `proc_LBL__ROMAN`, the body-bearing blob, which DECLINES (deep=1 + `IR_LIT_STRING` subject-position rejects — the measured s21x-e frontier) and keeps its legacy fresh-frame carve, which is what makes the pattern-bearing body statements' `[fb+off]` reads correct.  Roman result `MDCCLXXVI` matches the sbl oracle m3+m4 under the gates (the only output diff is roman's own `ms:` timing line).  So Lon's named target is half-retired: the stub carve is gone; **the body blob's carve is structurally pinned to the SUBJECT-CELL / match-family registration rung** — the body statements cannot self-allocate until IR_MATCH_HEAD's subject consume converts to TOS and subject/capture producers register fc.  That is the (a) rung of the s21x-e cursor and remains the critical path.

## Verification (all on final `126645fc`)

Gate OFF: **HELD EXACTLY** — m3 311/4 · m4 311/2 SKIP=2 · DIVERGE=2, fail sets = baseline verbatim.  Gate ON: **m3 313/2 · m4 311/2 SKIP=2 · DIVERGE=0** — identical to slice-2 gate-on, now with armed stubs in the mix (both DEFINE-bearing crosscheck programs arm ≥1 stub blob in m4).  Witnesses m3+m4 × gate off/on vs the sbl oracle: DOUBLE (armed body blob) 10/42, recursive FIB (body declines on the LT builtin + arg-position lits — honest) 55/1, roman result-exact.  All three regen sweeps artifact-clean (corpus dirty files: 0 — default path byte-identical, roman.s untouched; the feature sweep's five EMIT-FAILs are the pre-existing s189 stale set).

## Next slices (in dependency order)

(3b) ARG/RESULT-CELL registration: extend zls_build's value-spine walk so staged-call ARG producer chains register as fc leaves and the CALL node itself earns a 16B fc result cell — the consumers are ALREADY window-ready (`x86_fc_hit` arms in bb_call_proc_staged, the fc arm inside FRQB in sr0, the FRQ(off) result store), so this is planner-only; it arms call-bearing MAIN statements (`OUTPUT = DOUBLE(21)`) and simple call-bearing bodies.  Tree-composed calls (`DOUBLE(2)+DOUBLE(3)`, FIB's `FIB(N-1)+FIB(N-2)`) additionally need the call to net-pop its arg cells (wpop registration + the alpha-carve arm) so the binop top-two-cell geometry holds — model it in the d-simulation before emitting anything.  (a) SUBJECT-CELL: the match-family registration that lets body blobs like proc_LBL__ROMAN arm — the true kill of the remaining roman carve.  (c) builtin-call family (`LT/GT/...` predicates) for FIB-shaped bodies.  Default-flip on SCRIP_CALL2BB: gate ON remains strictly better at HEAD; Lon's call.
