# EXTRACT-PL-FRAME.md — PL-FR-1 archaeology: anchor `20b56c9a` → HEAD pairing (first pass, s0 2026-08-07)

Anchor worktree `/home/claude/wt-pl-last` (built, 22/22 both modes). HEAD `c0372fec` (0/22). Line numbers are HEAD-of-today; re-grep every session. Rows marked ⚠VERIFY are the residual FR-1 work; everything else is decided.

## THE HEADLINE
CARVE-KILL `ef9a7d2c`+`1ba33ea6` deleted **all nine prologue arms** (BARE · FRAME_NONRSP · FRAME_RSP · GEN_RESUMABLE · JMP_EAGER · JMP_NONRSP · LEXPREP2 · NOFILL · STMT_FRAME — grep of the minus-lines) **plus both epilogues**. SN4 survived because its path had already moved to the ZW/ZD per-BB machinery; Prolog and Icon had not. ICN-FR-2 (`bcf05d33`) rebuilt a graph-flag-keyed, language-blind subset at HEAD: `xa_flat_zframe_prologue()` / `_epilogue_γ()` / `_epilogue_ω()` + dc stub (xa_flat.cpp:187–282), dispatched at emit.cpp:2340/2376/2756/2765/2783/3000, `zframe_graph` twin populated at the emit choke (:3000), frame-bytes fallback (:3001), and **the ZD exclusion at emit.cpp:1906 (`if (g_emit.zframe_graph) return 0;`) — row (f)'s bypass, ALREADY BUILT and flag-keyed.** The REUSE case is therefore strong: PL-FR-2's core is making `lower_prolog.c` the second `zframe_graph` stamper behind `SCRIP_PL_ZFRAME`, then walking the Prolog-specific deltas below.

## PAIRING TABLE (rows per GOAL-PL-ZFRAME-RESTORE PL-FR-1(a)–(g))
**(a) LEXPREP2 prologue — Prolog's dominant arm (279 corpus hits, s150).** Anchor xa_flat.cpp:219–~240: `xaf_jmp_hdr_x86()` (:164 — `sub rsp,kt` + wires kt-24/-16 + caller-rbp kt-8 + pin-gated seed) + `xaf_anchor_enter_x86()` (:156) + `mov rdi,rsp` + `mov32 esi,seed_off` + `mov32 edx,kt-32` + call `rt_jmp_frame_lexprep2` — the lazy-seed protocol. ⭐ `rt_jmp_frame_lexprep2` SURVIVES at HEAD (rt.c:1552). HEAD's zframe prologue does the hdr+pin but NOT the lexprep2 call. ⚠VERIFY: whether Prolog frames need the lexprep2 seed (NULVCL fill of the locals region / suffix zeroing) or whether `rt_icn_zframe_args_install`-style direct install + explicit zero suffices. Verdict: REUSE hdr; ADD a Prolog seed arm (or a shared zero-fill) per the verify outcome.
**(b) FRAME_RSP outer arm — main graphs (185 hits, s149).** Anchor :297–~327: `xaf_outer_frame_k()` (:129, **65544-byte floor**) + rep-stosb zero fill + anchor-enter + rbx/r12 absolute seeds + caller-rbp save + `mov rbp,rsp` + nparams `main(args)` sub-arm + `cmp esi,0; jne extlbl β`. HEAD zframe prologue sizes from the flat layout pass, no 65544 floor, ⚠VERIFY zero-fill: Prolog cells assume zeroed frames (the lexprep2/NOFILL heritage); an unzeroed restored frame is the uninitialised-read class. Verdict: REUSE shape; the floor + fill decision is FR-2's first measured choice.
**(c) Pin predicates.** `emit_jmp_pin_rbp` / `emit_heap_fb_adopt` / `emit_rec_pin` / `emit_rec_fb` IDENTICAL anchor (emit.h:611–614) vs HEAD (:635–637) except HEAD's pin adds `zframe_graph` (xa_flat:278 note). Verdict: REUSE outright. ⛔ Carry the anchor's own law verbatim: THE PIN IS A LAYOUT CONTRACT, NOT A SPELLING SWITCH (emit.h:611 comment — the measured SEGV from blind pin-flipping).
**(d) Arg landing.** Anchor Prolog: caller stages into `g_call_args` (bb_call_proc_staged inline, s145 STAGE-1) → callee C half `rt_frame_bind_args` (rt.c:614, called :827/:842) — the double-copy the sink ladder wants gone LATER; restore AS-WAS first. Icon's HEAD path uses `rt_icn_zframe_args_install` (rt.c:1046) reading `g_call_args[]` directly. ⚠VERIFY: Prolog lexical prep (the "lexprep" in the name — rt.c:1393 block: open_det + prologue_lex + lexprep2 collapse) vs the simple install; nested/lexical frames may need the full prep. Verdict: likely OWN-ARM (a thin `rt_pl_zframe_enter` that composes the surviving C pieces), decided by the verify.
**(e) Slot grants.** `ir_drive_slot_assign` (scrip_ir.c:261) + `drive_value_slot` FATAL (emit.cpp:1087) present at BOTH trees. ⚠VERIFY coverage: every Prolog value-producing kind granted at HEAD (the census instrument is the FATAL itself; sweep the 22 board under the flag before believing it).
**(f) Double-billing bypasses for a flagged Prolog graph.** ALREADY BUILT flag-keyed: ZD exclusion emit.cpp:1906. ⚠VERIFY the rest by sweep, not argument: the ZD-PL-A arm in bb_call_fn.cpp (keys on `op_zres` — inert when nothing arms, but prove it), NOFC, FB-STMT `flat_fb_refine`, anything `de837576` (SN4 regime deletion) made unconditional that reaches Prolog kinds. `SCRIP_PL_ZFRAME=0` must reproduce today's exact bytes — the completion criterion inherits ICN-FR-2's proof shape.
**(g) Epilogue/whack.** Anchor: both-media epilogues (deleted by `1ba33ea6`); ω label via `flat_fail_p` (:31). HEAD: `xa_flat_zframe_epilogue_γ/ω` dispatched at emit.cpp:2756/2765 — wire-header read + caller-rbp restore + jmp, the FR-3 shape. Verdict: REUSE; Prolog's delta is the FR-4 class below.
**(FR-4 preview — the genuinely Prolog-shaped row.** Predicate/backtrack graphs: anchor served them via the heap-fb-adopt arm (`push rbp; mov rbp,rdi`, frame survives β-resume; the C allocator half in rt.c SURVIVES) + the choice-point machinery (`IR_DISJUNCTION`, trail). The adopt EMISSION sites died with the file; the predicate survives. ⚠VERIFY: which of the 22-board's predicate graphs take adopt vs LEXPREP2 at the anchor (`SCRIP_XAF_MARK=1` sweep — the instrument is in the anchor tree at :163 and is exactly for this). ⛔ s164 standing law: do NOT blind-widen `emit_fb_stmt_scan` to `IR_DISJUNCTION`.)

## VERIFY QUEUE (= FR-1 completion, next session; each is one sweep or one read, no HEAD edits)
1. `SCRIP_XAF_MARK=1` sweep of the 22 board at the anchor → per-arm hit table for Prolog specifically (the s150 method).
2. Read `rt_jmp_frame_lexprep2` + `rt_frame_bind_args` + the rt.c:1393 slim-prep block → settle (a)/(d) seed-and-landing.
3. Flagged-graph FATAL sweep for (e); bypass sweep for (f) with the `=0` byte-identity harness.
4. Zero-fill decision for (b) — measured, not argued.

**Method note:** anchor worktrees are for building + grepping, never wholesale reading; every claim above carries its line anchor for that reason.

## VERIFY QUEUE RESULTS (s0b 2026-08-07 — QUEUE CLOSED, FR-1 COMPLETE)
1. **Board arm table (`SCRIP_XAF_MARK=1` emit sweep, all 22 at the anchor): FRAME_RSP ×22 · LEXPREP2 ×101 · every other arm ZERO** (incl. NOFILL, GEN_RESUMABLE, JMP_*, adopt-class). ⇒ FR-2 board parity needs exactly TWO arm shapes; the adopt/backtrack-frame question moves WHOLLY to FR-4 and needs beyond-board witnesses there.
2. **Seed/landing settled by READ (anchor rt.c:1518 lexprep2 + :614 bind_args):** `rt_jmp_frame_lexprep2(fb,suffix_off,region_bytes)` = pcall-record registration (`c->fb=fb`, lex-only guard) + NULVCL seed of slot 0 + **NULVCL splat of [suffix_off, region_bytes)** + `rt_frame_bind_args` (g_call_args → `fb+16*(i+1)` copy, NULVCL pad of missing params, variadic rest via make_list/agg, named_rest empty-string seed); optional `SCRIP_ZLS_POISON` pre-fill. **VERDICT for rows (a)+(d): REUSE the surviving C wholesale — FR-2's Prolog jmp-entry arm re-emits the anchor's hdr + `mov rdi,rsp` / `mov32 esi,seed_off` / `mov32 edx,kt-32` / call sequence. No new runtime function.**
3. **RE-SCOPED into FR-2 completion (already in its rung text):** the flagged-graph FATAL sweep (row e) and the `=0` byte-identity bypass sweep (row f) require the flag to exist — dynamic halves move; static half done in row (f).
4. **CLOSED by item 2:** the NULVCL splat IS the zero-fill for jmp-entry frames; the FRAME_RSP outer arm keeps its rep-stosb + 65544 floor as-was. Restore as-was; re-measure only if FR-2 witnesses fail.
