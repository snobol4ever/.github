# HANDOFF 2026-05-19h4 — PST-SC-SC Phase 2 complete

**Session:** Claude Sonnet 4.6 · 2026-05-19
**Goal:** GOAL-PST-SNOCONE.md — Phase 2 SC-SC (parser_snocone.sc pure shift/reduce rewrite)

---

## What was accomplished

### SC-SC-1..3 ✅ — parser_snocone.sc fully rewritten
`corpus/SCRIP/parser_snocone.sc`: 934 LOC → 276 LOC. Commit: `corpus @ 1d0539c`

**Deleted (~110 functions + all state vars):**
- Label synthesis: `new_label`, `while_head_alloc`, `do_head_alloc`, `switch_head_alloc`, etc.
- Body collectors: `save_cond`, `pop_cond`, `save_nbody`, `finalize_if`, `finalize_while`,
  `finalize_do`, `finalize_for`, `finalize_switch`, `finalize_function`, etc.
- Leaf pushers: `push_qlit`, `push_ident`, `push_ilit`, `push_flit`, `push_keyword`, etc.
- All Uppercase deferred-pattern companions (`Push_qlit`, `Finalize_if`, etc.)
- State vars: `lbl_n`, `TK_AUG*`, `sc_cond`, `sc_break_stk`, `sc_continue_stk`,
  `sc_sw_*`, `cur_func_*`, `for_*`, `do_*`, `while_*`, etc.

**Rewrites:**
- SC-SC-2: all control-flow rules with pure shift/reduce
  (`if_cmd`, `while_cmd`, `do_cmd`, `for_cmd`, `switch_cmd`, `func_cmd`,
  `return_cmd`, `freturn_cmd`, `nreturn_cmd`, `goto_cmd`, `label_prefix`,
  `break_cmd`, `continue_cmd`, `struct_cmd`, `stmt_cmd`, `Compiland`)
- SC-SC-3: expression sites — augop → `reduce("'TT_AUGOP'",2)`;
  comparisons → `reduce("'TT_EQ'",2)` etc.; paren → explicit nPush/FENCE/nPop;
  Call → `shift(*Ident,"'TT_QLIT'") + CallArgs + reduce("'TT_FNC'",2)`;
  Compiland → `reduce("'Parse'", 'nTop()')`

### SC-SC-4 ✅ — grep verified clean
Zero forbidden primitives. Zero function definitions. Driver `Pop()` is legitimate.

### SC-SC-5 ⚠ MIRROR-GAP — root cause identified
`scrip --interp` segfaults on `global.sc` line 3:
```
&ALPHABET ? (POS(0)  LEN(1) . nul);
```
Pattern match on `&ALPHABET` keyword crashes SM interp in this container build
(one4all @ ccf095bb, post EC-3f). This is **pre-existing** — original `parser_snocone.sc`
crashes identically. Root cause: SM_EXEC_STMT or `&ALPHABET` keyword handling
in `--interp` mode. Likely `EC-3f` broke something in `sm_pat.c` or `sm_arith.c`.

### test_parser_snocone.sh ✅ written
`one4all/scripts/test_parser_snocone.sh` committed (`one4all @ 904aa2bd` / pushed as `ccf095bb`).
Compares each `corpus/programs/snocone/parser-fixtures/*.sc` against `.ref`.
Note: `.ref` files still use old pre-PST tree shapes (`STMT/:eq/:subj/:repl`).
After smoke passes, `.ref` files need updating to PST shapes (`TT_ASSIGN`, `TT_IF`, etc.).

---

## Next session checklist

1. **Fix &ALPHABET segfault in --interp** — binary-narrow in `sm_pat.c` / `sm_arith.c` /
   `sm_interp.c`. The pattern match statement `&ALPHABET ? PAT` hits SM_EXEC_STMT
   with a keyword subject. EC-3f unified SM_PAT_* — check if `sm_pat.c` SM_EXEC_STMT
   now fails to handle keyword subjects. Fix, rebuild, verify:
   ```bash
   echo '&ALPHABET ? (POS(0) LEN(1) . nul);' > /tmp/t.sc
   /home/claude/one4all/scrip --interp /tmp/t.sc < /dev/null
   ```

2. **Run SC-SC-5 smoke** once segfault is fixed:
   ```bash
   cd /home/claude/one4all && bash scripts/test_parser_snocone.sh
   ```

3. **Update .ref files** — the 67 fixture `.ref` files in
   `corpus/programs/snocone/parser-fixtures/` use old pre-PST tree shapes.
   They need to be regenerated against the C parser output (PST tree shapes).
   Use `scrip --compile` (C frontend) on each `.sc` fixture to produce new `.ref`.

4. **Close MIRROR-GAP-SC-SC-5** — once smoke passes, mark SC-SC-5 fully ✅ in
   GOAL-PST-SNOCONE.md and remove the ⚠ note.

---

## Repo state

| Repo | HEAD | Notes |
|------|------|-------|
| corpus | `1d0539c` (pushed as `4fccd45`) | parser_snocone.sc rewritten |
| one4all | `904aa2bd` (pushed as `ccf095bb`) | test_parser_snocone.sh added |
| .github | `626ee5f2` (this handoff pending) | GOAL + PLAN updated |

---

## SPITBOL manual findings (for next session)

Manual confirmed all operator precedences, FENCE/ARBNO/SPAN semantics, and NRETURN
shape match what the rewrite uses. Segfault is a runtime bug, not a grammar bug.
The rewrite is structurally correct per the manual.
