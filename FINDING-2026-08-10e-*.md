
---

## 5. ⭐ THE SPLICE DEFECT OWNS `test_case` — A C-9 RESIDUAL IS CLOSED (cause named, not just re-measured)

s39 left `test_string`/`test_case` as *"signatures have MOVED … need a clean re-measure before being trusted either way."* Re-measured, and the cause found.

**⚠️ MEASUREMENT TRAP FIRST — CWD IS PART OF THE INVOCATION.** Run from anywhere but the corpus root, **the ORACLE ITSELF fails** (rc=1, banner + error): these programs pull in `lib/` and HEAD's own commit subject is *"regen-feature INCLUDE PATH repair: compile from the CORPUS root so `-include 'lib/*.sno'` resolves."* From `/home/claude/corpus`, oracle rc=0 and **byte-matches the committed `.ref` for both** — the refs are trustworthy. Any measurement of these two from the wrong cwd is void.

**CORRECTED SIGNATURES (from corpus root, m3):**
- `test_case` — **rc=134, not s39's rc=124.** Aborts after 3 correct lines: `[WSI] workspace island exhausted (1024 MB, 22366969 blocks)`.
- `test_string` — **rc=0 with WRONG OUTPUT.** An rc-only check scores this green. It is not.

**ROOT CAUSE OF `test_case`, CONFIRMED BY DIRECT PROBE.** `lib/case.sno:34,36` is the failing shape verbatim: `str POS(0) ANY(&UCASE &LCASE) . letter =` and `str POS(0) LEN(1) . ch =`. Minimal probe:

```
        S = 'abc'
LOOP    S POS(0) LEN(1) . C =     :F(DONE)
```
| | iter1 | iter2 | iter3 |
|---|---|---|---|
| oracle | got=a rest=`[bc]` | got=b rest=`[c]` | got=c rest=`[]` → done |
| SCRIP m3 | got=a rest=`[abc]` | got=a rest=`[abc]` | got=a rest=`[abc]` … **never converges** |

**The capture is correct on every pass; the splice is a complete no-op (span `[0,0)`), so the subject never shrinks.** That is the manual's ALOOP non-convergence exactly (Ch.6 p.73; restated as p.142 #6 — *omit the removal and you repeatedly find the same first item*). The loop allocates until the workspace island is exhausted ⇒ **rc=134**. s35 guessed this shape (*"canonical ALOOP delete-all idiom … went non-convergent"*) from a clobbered subject; the mechanism is now named and reproduced in 5 lines.

**⇒ `test_case` is NOT a separate C-9 residual. It is a CUSTOMER of §2's splice defect.** Fix §2 and re-measure before spending a rung on it.

**`test_string` — RELATED SHAPE, DIFFERENT SIGNATURE, LEFT UNCONVICTED.** `lib/string.sno:34` lstrip is `s POS(0) (SPAN(ws) | '') REM . r =`. Probe: oracle → `r=[hello] s=[]`; SCRIP → `r=[   hello] s=[   hello]`. The splice is again a no-op — **but here the CAPTURE IS ALSO WRONG** (`r` keeps the leading blanks, i.e. the alternation took the `''` arm or `REM` read from a stale cursor). §2's signature is *capture correct, splice wrong*; this is *both wrong*. **Possibly a second defect (alternation-arm preference / post-SPAN cursor) riding the same statement.** Do NOT assume §2's fix clears `test_string` — re-measure it separately after the fix.

**REVISED C-9 RESIDUAL PICTURE:** what s35/s39 recorded as three-to-four independent residuals is at most two. `test_case` ⇒ §2. `test_string` ⇒ §2 **plus** an unconvicted capture/alternation component. `061` ⇒ s39's variable-arg class (untouched here). The TAB/POS splice defect is therefore the **highest-leverage single fix on the C-9 board**.
