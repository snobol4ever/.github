# HANDOFF-2026-06-07-OPUS48-SNOBOL4-BB-B2-PROTO-LIT.md

**Goal:** GOAL-SNOBOL4-BB · B-LADDER (SCRIP/SNOBOL4-5STAGE-OWNED-BUILD.md) · rung B2 PROTO-LIT — **LANDED ✅ SCRIP `fb43dd1`.**

## What landed
First D6 builder. `bb_pattern_lit.cpp`: PIC call-free LIT prototype blob + runtime builder code. Blob FIXED LAYOUT: `+0` lit_ptr · `+8` lit_len · `+16` out_γ · `+24` out_ω (abs64 patch sites) · **β entry at +32 — UNIVERSAL convention for every element proto going forward** · α at gas label-diff offset. Blob sits IN-LINE after the builder's `jmp γ` (never executed in place; `rep movsb` copies it into the B1 RWX pool) — deviation from the ladder's ".rodata" wording, recorded in the tick: copy semantics identical, avoids section-switch interplay with XA framing. Metadata struct COLLAPSED: fixed offsets + assembler-resolved label-diffs (`.Lpb_e-.Lpb_s` size, `.Lpb_a-.Lpb_s` entry) — nothing counts bytes, the assembler discovers everything (the bb_bin_t law extended to TEXT). One-shot β per SPITBOL law: reload len, `sub r14d`, `jmp [out_ω]` (bb_lit δ-restore semantics). Builder: bump-alloc `g_pat_pool_cur` (kw_anchor RIPSEAL idiom + store-back), copy, fill operands (strtab lea + len imm), write `DTP_FRAG_t {entry, γ_site, ω_site}` to ζ `[r12+op_off+0/8/16]`.

Plus: 26 kinds `IR_PATTERN_*`/`IR_DTP_ASSIGN` in IR.h + knames (dump-readable, taxonomy explicit-per-primitive) + emit_core dispatch; rest of family bomb-stubbed through `bb_pattern_stub(msg)` — the BOMB messages are the B3–B10 work queue. `lower_pattern_build` (lower_sno.c, exported in lower_internal.h): TT_QLIT arm + NULL-fallback default. **UNROUTED by design** — rerouting now would pull live pattern-assign shapes off the passing shim path (corpus regression); routing is B3's flat_drive_assign pattern-rhs arm.

## Proof
Standalone harness (tracker 17th-run pattern): `#include` the template TU + stub `g_emit`/`g_medium`/`g_platform`/intern/byte-sinks, call `bb_pattern_lit_str()` directly, wrap output in `.intel_syntax noprefix`, `as` rc=0, objdump end-to-end verification (frag stores at ζ+48/56/64 for op_off=48; slots/β/α offsets; FF25 indirect exits). Harness recipe lives in this handoff's session transcript; stubs needed: g_emit, g_pat_pool_cur, emit_intern_str, strtab_label, rt_bomb, g_platform=BB_PLATFORM_X86, g_medium=BB_MEDIUM_TEXT, u32le/u64le, bb_emit_byte, bb_emit_patch_rel32, bb_label_define, emit_text_n.

## Findings (ring-wide, do not re-derive)
1. **x86() SILENT DROP:** parsed forms return EMPTY for unrecognized operand shapes — `lea r,[rax+imm]` and `lea r,[r13+rax]` are NOT in the parser repertoire (only specific keyed forms like `[r13 + rcx]`, `[rip + __]`). Three instructions vanished from first emission; only objdump caught it. RULE: objdump-verify every operand shape new to a template; convert misses to ins2 (sanctioned TEXT-only) or add encoders.
2. **gas accepts everything the design needs:** label-diff immediates (`mov ecx, .Le-.Ls`), symbol-diff lea disp, `mov [rip+sym], reg` stores, `rep movsb`, `jmp qword ptr [rip+lbl+16]` — all verified by `as` probe before writing.
3. **B3 PROBE NOTE:** the emitted store to `g_pat_pool_cur` relies on copy-relocation unification under the -no-pie m4 link (lib-side pat_pool_init writes through its GOT to the exe copy — analytically coherent). Sanity-probe at first LIVE builder execution: confirm pool base/cur seen by emitted code matches pat_pool_init's.

## Next session: B3 STITCH-ALT + DTP-ASSIGN + DEFER-RUN
Gate: **053 PASSES, pat-rung 19/19 no-SKIP.** Pieces: (a) bb_pattern_alt stitch (D2 port equations one layer down — patch ω-chain through the dangling sites; B2's frag contract is ready); (b) rt_gvar_assign_pat (sanctioned family of rt_gvar_assign_str — nv set is REQUESTED in the permission ledger, confirm stamp); (c) routing: flat_drive_assign pattern-rhs arm (emit_bb.c:1842 FATAL site) calls lower_pattern_build-produced chains; driver claims frag ζ slots (bb_slot_alloc16 + bb_slot_alloc contiguous = 24B) and sets op_off; (d) defer shim drives DT_P: load Σ/δ/Δ, jmp entry, out slots → return trampolines. Per-element proto pattern to clone: bb_pattern_lit.cpp is the template-of-templates (β +32 convention, label-diff idioms, ins2 list).

## Gates at handoff (all floors)
smoke m4 7/7 HARD · pat-rung M4 18 PASS/0 FAIL/1 SKIP (053 — B3's gate) · broad m4 corpus 148/280 (non-decreasing HARD held) · fence test_gate_sno_pat_reg HARD green. Beauty suite not re-run (no live-path change; floor 1/17 stands).
